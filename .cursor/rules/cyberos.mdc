---
description: CyberOS task workflow (HITL-gated). Always apply.
alwaysApply: true
---
This repo runs CyberOS. Canonical instructions: `.cyberos/AGENT-ENTRY.md` (workflow). Root `AGENTS.md` is the thin CyberOS spine (not the memory protocol). Memory protocol: `.cyberos/memory/AGENTS.md`.
Work is tasks; HITL is required at the two human-acceptance gates; run gates with `bash .cyberos/cuo/gates/run-gates.sh`. Never push/deploy/merge without an operator instruction.
