{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main CyberOS window. HTTP, keychain, and the launch update check all run in the Rust backend, never invoked from the webview, so no extra plugin permissions are granted here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
