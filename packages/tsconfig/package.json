{
  "name": "@cyberos/tsconfig",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo 'TODO: add build for tsconfig'",
    "test": "echo 'TODO: add tests for tsconfig'",
    "lint": "echo 'TODO: add lint for tsconfig'",
    "typecheck": "echo 'TODO: add typecheck for tsconfig'"
  }
}
