{
  "name": "@cyberos/eslint-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo 'TODO: add build for eslint-config'",
    "test": "echo 'TODO: add tests for eslint-config'",
    "lint": "echo 'TODO: add lint for eslint-config'",
    "typecheck": "echo 'TODO: add typecheck for eslint-config'"
  }
}
