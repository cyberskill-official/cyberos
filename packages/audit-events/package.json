{
  "name": "@cyberos/audit-events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo 'TODO: add build for audit-events'",
    "test": "echo 'TODO: add tests for audit-events'",
    "lint": "echo 'TODO: add lint for audit-events'",
    "typecheck": "echo 'TODO: add typecheck for audit-events'"
  }
}
