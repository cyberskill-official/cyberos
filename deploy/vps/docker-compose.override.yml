# Local validation override: provisions config the base images/compose do not yet wire.
services:
  ai-gateway:
    environment:
      AI_GATEWAY_CONFIG_DIR: /config/tenants
    volumes:
      - ../../services/ai-gateway/config:/config:ro
