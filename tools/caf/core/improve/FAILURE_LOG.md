# Failure log

Maps recurring failures to candidate edits. **Promote a fix only after the failure recurs** (Rule of Three: 1st time = note it, 2nd time = candidate edit, promoted = new version via `improve/CRITIC.md`). One row per observation.

> Version ids below the public 1.0.0 release ("v1 era", v2.0.0–v2.1.1) are
> internal pre-release identifiers; that lineage lives in git history. Rows are
> historical records and are never renumbered.

| Date | Project / run | Failure observed | Retro item | Seen before? | Candidate edit | Promoted to version? |
|------|---------------|------------------|-----------|--------------|----------------|----------------------|
| 2026-04 (v1 era) | Qi Men metaphysics app | Benchmarked vs Palantir/IBM Watsonx with no relevant public benchmark | #2 | 2nd time | Honest-targets gate with URL-or-INTERNAL rule | v2.0.0 (R2) |
| 2026-04 (v1 era) | React/Three.js app | "React Profiler" listed as a CLI verify command; metrics "verified via static code analysis" | #1 | 2nd time | Verbatim-output evidence gate; GUI tools banned in Verify column | v2.0.0 (R1) |
| 2026-04 (v1 era) | Small/clean repo | Low-value findings manufactured to satisfy "5 issues per loop" quota | #3 | 2nd time | Severity-weighted findings; "no significant findings" is a valid outcome | v2.0.0 (R7) |
| 2026-04 (v1 era) | All 4 runs | Stopped arbitrarily at loop 1–2 because "match SOTA Top 5" exit was unreachable | #4 | 4th time | Diminishing-returns stop rule + LOOP_BUDGET | v2.0.0 (Phase 4) |
| 2026-06-10 | Critic cycle 1 (this repo) | One fenced output block can "cover" N measured rows — partial fabrication passes R1's letter | #1 | Variant of v1 failure #1 (2nd occurrence of the family) | Require `$ <verify command>` opener per measured metric | v2.0.1 (R1 templates) |
| 2026-06-10 | Critic cycle 2 (this repo) | Gated approval lives only in conversation → resumed sessions can't honor it (gate degrades to autonomous) | #7 | v1 had `status: APPROVED`; regression introduced by v2.0.0 rewrite | `Approved:` line artifact + Phase 3 restriction | v2.1.0 (Phase 2/3) |
| 2026-06-10 | Critic cycle 3 (this repo) | R1 says NOT-APPLICABLE, Phase 5 said N-A → compliant runs flagged as violations | #6 | Queued in cycle-2 retro (2nd observation of vocabulary drift) | Single closed status set across R1 and Phase 5 | v2.1.1 (Phase 5) |
| 2026-06-10 | Critic cycle 4 (this repo) | Run artifacts don't state their MODE — post-hoc audit can't distinguish autonomous from gate-skipping | #7 | 1st observation — NOT promoted (Rule of Three) | Echo `Mode:` in the Scope & method line if it recurs | v1.1.0 (recurred — see campaign-2 row) |
| 2026-06-10 | Ownership review / blind-spot audit (this repo) | Even with `Mode: gated` echoed, a run that executes tasks and never writes an `Approved:` line validates CLEAN — the v2.1.0-era check fired only when the line existed (live exploit: synthetic gated run → "CLEAN — no violations"; see improve/BLINDSPOTS.md BS-08) | #7 | 2nd observation of the cycle-4 family | Echo `Mode:` in Scope & method; validator flags gated loop sections with executed tasks and no `Approved:` line | v1.1.0 (Phase 2 template + B14) |
| 2026-06-10 | Structural review, campaign 3 (this repo) | CONFIG improvisation: handed a half-filled CONFIG (placeholders intact, out-of-set MODE/DEPTH), agents fill the gaps themselves and audit against invented constraints; neither Phase 0 nor the validator checked CONFIG sanity (review gap G-D; BLINDSPOTS BS-13) | #10 | Documented norm across agent runs on new codebases (review: "the most likely first failure" on diverse client repos) | Phase 0 preflight: STOP and ask on placeholder / out-of-set CONFIG; validator CONFIG-PLACEHOLDER / CONFIG-BAD-ENUM + PROTECTED_AREAS auto-load | v1.2.0 (Phase 0 + B17/B18) |
| 2026-06-10 | Structural review, campaign 3 (this repo) | Template escape: output that doesn't follow the Phase 2 template (prose, malformed tables) validated CLEAN by slipping past every tripwire — the rules were only as load-bearing as the output's formatting (review gap G-B; BLINDSPOTS BS-12) | #6 | Acknowledged narrowly in rules.json for P2-GATE since v1.1.0; the review showed it is global (2nd observation of the family) | Harness structure check TEMPLATE-NONCONFORMANT (no protocol edit: Phase 2 already mandates the exact template; the validator just couldn't see refusal) | Harness 2026-06-10 (B15/G05/G06) — protocol unchanged by design |
| 2026-06-10 | Architect review, campaign 4 (this repo) | Fence-blind parser (F-1): R1 forces raw output into fences, tools emit markdown — quoted GFM-table lines were parsed as artifact tables. Live exploit both ways: compliant run → 3 bogus R5 violations; fence-only tables falsely satisfied the template check (BS-12's closure had a fence-shaped hole) | #6 | 3rd observation of the BS-12 family, one layer down | Fence-aware parse_tables; quoted evidence neither trips checks nor satisfies the template | Harness 2026-06-10 (G07/B19) — protocol unchanged |
| 2026-06-10 | Architect review, campaign 4 (this repo) | Final-column escape (F-2): a backlog benchmark table with an extra `Final` column disabled R1+R2 entirely — uncited "Palantir-grade, beats IBM Watsonx" + "measured by reading the code" validated CLEAN. The two founding failure modes, back through formatting | #1, #2 | Direct descendant of the two v1-era founding rows | BACKLOG metric tables always checked; column shape selects semantics, never disables | Harness 2026-06-10 (B20) — protocol unchanged |
| 2026-06-10 | Architect review, campaign 4 (this repo) | CONFIG preflight precision (F-4): `List<OrderDTO>` generics and `< seed.txt >` shell redirection read as placeholders; ` #4211` in a value silently truncated — legitimate client configs blocked at Phase 0 by the v1.2.0 feature itself | #10 | 1st observation (the feature was one day old); fixed same-day with precision fixture | Placeholder = whole-value wrap or template stems; comments strip at >=2 spaces; enum on first token | Harness 2026-06-10 (G08; B17 still traps) — protocol unchanged |
| 2026-06-10 | Architect review, campaign 4 (this repo) | Version skew (F-5): artifacts don't state which protocol version produced them, so the validator assumes the current template — a pre-v1.1.0 artifact (no Mode echo) now reads TEMPLATE-NONCONFORMANT; consultancy fleets will hold mixed-version artifacts | #6 | 2nd observation of the self-describing-artifact family (1st: cycle-4 "artifacts don't state their MODE" → v1.1.0) — promotion bar met | Phase 2 Scope line echoes `Protocol: vX.Y.Z`; validator gates template requirements by the stated version | v1.3.0 (Phase 2 template + version-aware validation) |
| 2026-06-12 | Pilot run 1 — kymondongiap (first live runner-mode field run) | Report provenance gap: in runner mode the target has no AUDIT.md, so `build_report` left `protocol_version: null` even though the BACKLOG's Scope line echoes `Protocol: v1.3.0` — the JSON report (field-data / aggregate substrate) dropped provenance the artifact plainly states | #6 | 1st observation; harness completeness gap exposed by the runner-mode feature itself, fixed same run | `build_report` falls back to the first loop's stated `Protocol:` echo when no target AUDIT.md; copy mode still wins | Harness 2026-06-12 (G11 report carries v1.3.0; B18 copy unchanged; CI report-contract asserts it) — protocol unchanged |
| 2026-06-12 | Pilot run 1 — kymondongiap Phase 3 (this repo's auditor self-audit) | Approval-parse brittleness: `check_approvals` takes the FIRST `^Approved:` match, so a stray "Approved: …" written as PROSE elsewhere in a loop section shadows the real gate line → every executed task false-flags GATED-UNAPPROVED-EXEC. Surfaced when the agent wrote "Approved: all 7." in an execution-results note. | #7 | 1st observation — NOT promoted (Rule of Three). Defensible current behavior (ambiguous artifact rightly flagged), but brittle. Agent-side fix applied (don't write "Approved:" in prose). | Candidate if it recurs: anchor approval parse to the gate section or take the last match, and/or require approved tokens to look like task IDs / "none" | NOT promoted — candidate logged; artifact reworded this cycle |
| 2026-06-12 | Pilot run 1 — kymondongiap L1-T7 (managed-Postgres cutover) | Live-datastore verification gap: closing T7 took THREE rounds of real-environment debugging the existing vectors never surfaced — plain `postgresql://` needed async-driver coercion; asyncpg/psycopg2/greenlet were not installed; then Supabase requires SSL (asyncpg off by default) and the direct host is IPv6-only vs the `aws-1` IPv4 pooler. A "code-complete" fix read as done but would have 500'd at connect in prod; only a live round-trip against the real endpoint proved it. | #11 (new: live-infra verification) | 1st observation — NOT promoted (Rule of Three). Caught here only because the maintainer supplied live credentials; a fleet run without them would have shipped a latent connect failure. | Candidate if it recurs: when a managed datastore/external service is in scope, the infra/architecture vector should require a live-connectivity smoke (driver + SSL + host reachability against the real endpoint) before a persistence/connectivity task may be marked DONE | NOT promoted — candidate logged |
| 2026-06-13 | T4 cross-model (Gemini/Antigravity) vs Claude — kymondongiap | Below-floor staleness: Claude's own below-floor note said CORS+credentials "becomes High the moment cookie auth is added"; Claude then ADDED auth (T2) but never re-classified CORS. Gemini, auditing fresh, scored it High. The protocol lets a below-floor item keep its old severity even after a same-loop fix changes its premise. | #7 | 1st observation (novel — surfaced only by the cross-model diff). | Candidate if it recurs: at end of Phase 3, re-evaluate below-floor items whose premise was changed by a completed task; promote any that cross the floor into the next loop's backlog | v1.4.0 (Phase 4 re-eval bullet + fixture G12 + rules.json PHASE4-REEVAL) — promoted at maintainer request at 1 observation; **Rule of Three explicitly waived by Stephen 2026-06-13** (single observations are normally logged, not codified) |
| 2026-06-13 | T4 cross-model (Gemini/Antigravity) vs Claude — all 3 pilots | Severity calibration + recall both diverge across models: kymondongiap = same issues, Gemini High vs Claude below-floor; 3d-table = Gemini caught a real High (multi-instance Three.js) Claude missed; mock-exam = Claude caught a real High (client-trusted score) Gemini missed (Gemini stopped at "baseline commands pass"). | DEPTH/#3 | 1st cross-model run — direct evidence toward the queued DEPTH-semantics / severity-floor candidate, and a "baseline-pass ≠ no-findings" shallow-audit signal. | Evidence logged toward DEPTH-semantics candidate; "no-significant-findings requires more than baseline commands passing" is a second candidate. Needs a 2nd cross-model run to promote either. | NOT promoted — evidence logged (field-data pilot/XMODEL-RESULTS.md) |
| 2026-06-13 | kymondongiap — post-audit CI (maintainer caught it) | Audited target shipped RED: a lint fix scoped to `src/` left the target's own `ruff check .` (which covers `tests/` + 10 `src` E402) failing in CI; the audit declared "done", pushed, and deployed without ever re-running the target's full RUN_COMMANDS after the change. The artifact-validator passed (it cannot run builds), so nothing caught the regression. | #9 (new: target health) | 1st observation, but the maintainer mandated it as a required mechanism: "strictly verify the target still runs without error after the audit." | Phase 4/5 target-health gate: re-run the target's full RUN_COMMANDS after any change + record `Target health: PASS/FAIL` in HANDOFF; harness `verify-target.sh` enforces it (exit non-zero on fail/hang/timeout); validator flags a completed HANDOFF lacking the line. | v1.5.0 (Phase 4 gate + Phase 5 line + core/evals/verify-target.sh + TARGET-HEALTH-UNVERIFIED + G13/B27) — promoted at maintainer request (Rule of Three waived; the failure is concrete + recurrence-prone across the fleet) |
