# Retrospective — Project: AUDIT.md (self) | Protocol: v1.2.0 | Date: 2026-06-10 | Mode: autonomous (critic cycle — campaign 3, cycle 1)

**Context.** Campaign 3 opened, per register cadence, with a full blind-spot re-verification (BS-01…BS-11 all hold against current validate.py / run-evals.sh / fixtures) and adopted the 2026-06-10 structural review as its evidence base. Five new register rows: BS-12 and BS-13 closed by harness work this campaign; BS-14/BS-15/BS-16 accepted with named residuals.

**The one change (MINOR → v1.2.0).** Phase 0 gains a CONFIG preflight: if any CONFIG value still contains `<placeholder>` text, or MODE / DEPTH / BENCHMARK_MODE / SEVERITY_FLOOR is outside its allowed set, STOP and ask the human — never improvise CONFIG. Instruction budget: 161 → 164 lines (cap 200).

- **Trigger:** review gap G-D — CONFIG improvisation is the documented norm for agents handed partial configs, and the most likely first-run failure on diverse client repos. FAILURE_LOG row added and promoted this cycle.
- **Harness, same cycle:** CONFIG preflight in validate.py (`CONFIG-PLACEHOLDER`, `CONFIG-BAD-ENUM`; trap B17) and PROTECTED_AREAS auto-load into R3 (B18 — proves the violation is findable with NO `--protected` flag, retiring the double entry, gap G-F). Registered under P0-CONFIG in rules.json. Evals: **24/24 green**; baseline re-recorded at v1.2.0.
- **Separate (non-protocol) hardening landed before this cycle, attribution clean:** TEMPLATE-NONCONFORMANT meta-tripwire + B15/G05/G06 (BS-12); precision pack G03/G04 + compound exact-set B16 (G:B 2:14 → 6:18, review gap G-C); `--report json|sarif` findings export (G-G); retro aggregator (F7); LICENSE/NOTICE/CONTRIBUTING/SECURITY + CI gate enforcing suite, version sync, baseline sha256, and snapshot immutability (G-A). The protocol diff for v1.2.0 is exactly the three Phase 0 preflight lines + the title bump.

Scores (cycle-as-run):

1. Metrics with command + raw output (or honest UNMEASURED)?            2
2. Targets cited-with-URL or INTERNAL (no fake SOTA)?                   2
3. Avoided inventing findings (no padding)?                             2
4. Loop stopped for a real reason?                                      2
5. 3-strike circuit breaker behavior?                                   2 (n/a — no failed validations; nothing thrashed)
6. BACKLOG/HANDOFF (here: changelog/versions/baseline) correctly formed? 2
7. PROTECTED_AREAS / public contract behavior-preserving?               2 (no fixture weakened; all 16 prior expectations intact)
8. Commits atomic, descriptive, temp scripts cleaned?                   2
9. Changes sensible and genuinely valuable?                             2
10. Stayed on scope (one protocol change only)?                         2

TOTAL: 20/20 Lowest-scoring item(s): none below 2. Candidate ONE-LINE prompt edit (if any): R2 offline clause and DEPTH semantics remain queued (review §4.1) — deliberately NOT promoted; both wait for live-run evidence per Rule of Three. BS-16 tracks the offline case. Evidence: evals/baseline.json (24/24 at v1.2.0); improve/versions/AUDIT-v1.2.0.md; CHANGELOG.md v1.2.0 entry; FAILURE_LOG rows "CONFIG improvisation" (promoted v1.2.0) and "Template escape" (harness-closed); git log this campaign.
