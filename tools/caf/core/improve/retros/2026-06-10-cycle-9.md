# Retrospective — Project: AUDIT.md (self) | Protocol: v1.3.0 | Date: 2026-06-10 | Mode: autonomous (critic cycle — campaign 4, cycle 1)

**Context.** Campaign 4 opened from the Principal-Architect review (docs/ARCHITECT-REVIEW-2026-06-10.md, gitignored scratch): four flaws, each confirmed by live exploit BEFORE being claimed, per this repo's evidence bar. Three were harness-class and landed as the calibration-sprint infra batch (fence-aware parsing F-1, Final-column escape F-2, artifact-IO verdicts F-3, CONFIG precision F-4 — plus waivers, aggregation, report schema, COMPLIANCE mapping). FAILURE_LOG rows recorded for all four; BS-12's register row was amended honestly (its closure had a fence-shaped hole).

**The one change (MINOR → v1.3.0).** Phase 2's Scope & method line now opens with `Protocol: <this file's title version>` — artifacts become self-describing, and the validator judges each loop section by the template of the version it states (version-aware gating, `CURRENT_PROTOCOL` held in lockstep by the docs-sync checker). Net size change: 0 lines (164 stays 164).

- **Trigger:** F-5 — validating a pre-v1.1.0 artifact with the current validator produced TEMPLATE-NONCONFORMANT false positives; consultancy fleets will hold mixed-version artifacts. Second observation of the self-describing-artifact family (first: cycle-4 "artifacts don't state their MODE" → v1.1.0 Mode echo). Rule-of-Three promotion bar met.
- **Harness, same cycle:** trap B24-missing-protocol-echo (current run omitting the echo is nonconformant) + precision sibling G10-legacy-version-artifact (stated v1.0.0 with no Mode echo must pass). 30 fixtures strengthened to the new Scope line; the Mode/Protocol regexes were generalized to field-separator-aware matching (the strengthening itself caught the anchoring bug — 29 fixtures went red until fixed, which is the suite doing its job during surgery).
- **Worth recording:** the docs-sync gate blocked BOTH infra commits' first attempts (11 stale fixture-count surfaces; then a missing CHANGELOG entry) — the two governance rules added earlier today are already enforcing on their own author.

Scores (cycle-as-run):

1. Metrics with command + raw output (or honest UNMEASURED)?            2
2. Targets cited-with-URL or INTERNAL (no fake SOTA)?                   2
3. Avoided inventing findings (no padding)?                             2 (all four findings were live exploits, not speculation)
4. Loop stopped for a real reason?                                      2
5. 3-strike circuit breaker behavior?                                   2 (one red intermediate state, root-caused and fixed first try)
6. Artifacts (changelog/versions/baseline/registry) correctly formed?   2
7. Fixtures never weakened; strengthening only?                         2 (all 32 prior expectations intact, 2 added)
8. Commits atomic, descriptive, temp scripts cleaned?                   2
9. Changes sensible and genuinely valuable?                             2
10. Stayed on scope (one protocol change only)?                         2

TOTAL: 20/20 Lowest-scoring item(s): none below 2. Candidate ONE-LINE prompt edit (if any): DEPTH semantics and R2 offline clause remain the queued candidates — still no live-run evidence; still deliberately not promoted (Rule of Three). Evidence: evals/baseline.json (34/34 at v1.3.0); improve/versions/AUDIT-v1.3.0.md; CHANGELOG v1.3.0; FAILURE_LOG campaign-4 rows; git log (calibration sprint commit + this release commit).
