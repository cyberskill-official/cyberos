# Retrospective — Project: AUDIT.md (self) | Protocol: v1.4.0 | Date: 2026-06-13 | Mode: autonomous (critic cycle — campaign 5, cycle 12; maintainer-directed)

**Context.** Maintainer (Stephen) explicitly directed this cycle: "draft the 'below-floor staleness' framework promotion (fixture + one-line Phase-4 addendum)." Executed autonomously during a 5-hour unattended window. The change is committed LOCALLY and is NOT pushed/released — the maintainer ratifies on return (push + tag move + GitHub Release are his go).

**Step 1–2 (evidence + critique).** Evidence: FAILURE_LOG 2026-06-13 row "below-floor staleness" — the T4 cross-model diff on kymondongiap caught that Claude's own below-floor note said CORS "becomes High the moment auth is added", Claude then added auth (T2) but never re-classified CORS; Gemini, auditing fresh, scored it High. The wording gap: nothing in the protocol makes the auditor revisit a sub-floor item after a same-run task changes its premise, so a stale severity silently survives into the stop decision.

**Step 3 — ONE minimal change.** A single Phase-4 bullet (before the stop test): re-rate any below-`SEVERITY_FLOOR` item whose premise a task completed this loop changed; if it now meets the floor, carry it into the next loop's backlog as an OPEN finding — a stale below-floor severity is never a stop reason. Classification: **MINOR** (new rule). Budget: +5 lines, AUDIT.md 164→169 (≤200); nothing trimmed (well under budget).

**Rule-of-Three deviation, recorded.** This candidate is at **1 observation** (novel, surfaced only by the single cross-model diff). The protocol normally logs a single observation rather than codifying it (CRITIC Step 3; FAILURE_LOG Rule of Three). It was promoted anyway **at the maintainer's explicit instruction** — the waiver is recorded in the FAILURE_LOG row, the CHANGELOG entry, and the rules.json coverage note so the exception is auditable and not mistaken for the protocol's normal bar. If the next field run does not surface the pattern again, this is a candidate to watch for revert.

**Step 4 — coverage (honest).** Behavior rule, **gold-only** — whether an agent actually re-rated a below-floor item is a semantic/recall property, not soundly machine-detectable without false positives (cf. the F-2/F-4 false-positive corrections; same posture as R7's padding note). The validator does NOT flag the omission. New rules.json rule `PHASE4-REEVAL`; new fixture `G12-below-floor-reeval` pins the conformant two-loop re-promotion so a future validator change cannot wrongly flag it. A **sound opt-in enforcement** (a structured `escalates to <Sev> when <ID> done` clause + a deterministic check that fires only on that clause) is written up in the session handoff as a proposed future cycle, deliberately NOT bundled here (one change per cycle).

**Step 4 — gate.** Suite **38/38 green** (was 37/37; +G12). `run-evals.sh --record` re-pinned baseline.json at v1.4.0 (version-drift guard passed: AUDIT.md title == package.json). `check-docs-sync.py` green — all version + fixture-count surfaces agree (package.json, pyproject.toml, CITATION.cff, validator CURRENT_PROTOCOL=(1,4,0), CHANGELOG, versions/AUDIT-v1.4.0.md snapshot, README, site/index.html). AUDIT.md/baseline otherwise untouched.

Scores (cycle-as-run): 1:2 2:2 3:2 (one rule, no padding) 4:2 5:n/a 6:2 7:2 8:2 9:2 (the rule is genuinely valuable — the cross-model diff is direct evidence the gap costs real recall) 10:1 (on-scope per the maintainer's request, but it codifies a 1-observation candidate, which is a deliberate deviation from the standing bar — hence not a clean 2). TOTAL **19/20**. Lowest item: 10 — Rule-of-Three waived by maintainer instruction (logged). Candidate ONE-LINE prompt edit (if any): none further this cycle. Evidence: core/evals/baseline.json (38/38 at v1.4.0); G12 fixture + docs; rules.json PHASE4-REEVAL; CHANGELOG v1.4.0; FAILURE_LOG 2026-06-13 row (promoted column updated). Local commit only — unpushed pending maintainer go.

## CAMPAIGN STOP

**Stop condition: (c) — the maintainer requested a specific change (the below-floor-staleness promotion) and it is reached.** The other open candidate (DEPTH / severity-calibration, FAILURE_LOG 2026-06-13 row 2) stays explicitly deferred — it still needs a 2nd cross-model run to promote.

| Cycle | Version | Change | Evals |
|---|---|---|---|
| 12 (campaign 5) | v1.4.0 | MINOR: Phase 4 re-evaluates below-floor items whose premise a completed task changed (+G12). Promoted at 1 obs by maintainer instruction (Rule of Three waived, logged). | 38/38 |

Watchlist for the next campaign: (1) does the below-floor re-eval rule earn its keep on the next field run, or revert; (2) the sound opt-in enforcement of it (structured clause + check) if the maintainer wants it machine-enforced; (3) DEPTH/severity-calibration still gated on a 2nd cross-model run.
