-- AI Gateway schema migrations land here, owned by FR-AI-001..022.
-- This placeholder exists so that sqlx::migrate!() resolves a non-empty migrations/
-- directory at compile time. The real schema (cost_ledger, holds, holds_expired,
-- model_alias_map) lands when FR-AI-001 ships.
SELECT 1;
