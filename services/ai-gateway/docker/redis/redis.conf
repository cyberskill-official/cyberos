# FR-AI-017 — Redis config for per-tenant AI response cache.
# Production-tuned for ~100 tenants × 100MB budget.

maxmemory 10gb
maxmemory-policy allkeys-lru
hash-max-listpack-entries 128
hash-max-listpack-value 1024
appendonly no
save ""
