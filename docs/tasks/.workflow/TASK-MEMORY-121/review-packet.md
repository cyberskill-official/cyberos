# TASK-MEMORY-121 review packet - status-drift reconciliation (2026-07-12)

Implemented by a parallel session (services/memory/src/interaction/, 2207 lines incl. tests); status stuck at implementing. Clause verification of the existing code (18/18):

#1 row kind memory.interaction_event enumerated in the frozen schema PASS; #2 field set per schema (required set + payload wrapping documented in the schema description) PASS; #3 no raw content - content_ref pointer/hash doctrine in schema + content_ref.rs PASS; #4 content_ref closed union PASS (content_ref.rs); #5 chained via shared writer PASS (cyberos_audit_chain::emit_genesis_with_op, emit.rs:156); #6 op derived from event_class PASS (emit_genesis_with_op call shape); #7 emit API in emit.rs PASS; #8 consent gate on TASK-EVAL-001 acknowledgment ledger PASS (consent_gate.rs reads monitoring_notice/subject_acknowledgment directly - deliberately no services/eval dependency, documented); #9 validation of module/event_type prefix/event_class enums PASS (event.rs); #10 size bounds as named constants MAX_ATTRIBUTES_BYTES=2KiB / MAX_BODY_BYTES=16KiB PASS; #11 canonical deterministic body PASS (fixed field order serialisation); #12 60s consent-verdict cache PASS; #13 OTel metrics - implemented as structured tracing events carrying the exact label set, with the obs pipeline deriving counters (module header documents this as the crate's metrics path; recorded as a deviation-with-rationale, native meters deferred to TASK-MEMORY-122/OBS); #14 RLS inherited (events ARE l1_audit_log rows) + dedicated interaction_event_rls_test.rs PASS; #15 frozen schema_version 1 + additive-only x-evolution clause PASS; #16 typed builder PASS (event.rs:164); #17 event_id idempotent on replay (chain UPSERT) PASS; #18 closed enums documented as cardinality contract PASS (schema + §11).

Beyond spec: interaction/backfill.rs (309 lines) - not in new_files; extra scope from the implementing session, noted for the record.

Tests: 697 lines across interaction_event_test / _rls_test / _contract_test. NOT run here (no Rust toolchain in the sandbox); evidence = CI at the implementing commits + operator cargo run.
