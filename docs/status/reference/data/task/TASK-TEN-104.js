window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-104"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship the 90-day offboarding contract — closed 4-state FSM + scheduled transitions + read-only freeze + dead-letter recovery + dual-signoff irreversible wipe. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>tenant_offboarding_state</code> table: <code>(tenant_id UUID PRIMARY KEY REFERENCES tenants(id), offboarding_state offboarding_state NOT NULL, initiated_at TIMESTAMPTZ, initiated_by_subject_id UUID, initiated_reason TEXT, scheduled_advance_at TIMESTAMPTZ, extension_count INT NOT NULL DEFAULT 0 CHECK (extension_count BETWEEN 0 AND 2), total_grace_days INT NOT NULL DEFAULT 90 CHECK (total_grace_days BETWEEN 30 AND 180), terminating_a_started_at TIMESTAMPTZ, terminating_b_started_at TIMESTAMPTZ, terminated_at TIMESTAMPTZ)</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>offboarding_state</code> Postgres enum with exactly 4 values (per DEC-500): <code>'active'</code>, <code>'terminating_a'</code>, <code>'terminating_b'</code>, <code>'terminated'</code>. Adding a 5th is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> define <code>tenant_offboarding_log</code> table: <code>(id BIGSERIAL, tenant_id UUID, from_state offboarding_state, to_state offboarding_state NOT NULL, changed_at TIMESTAMPTZ NOT NULL DEFAULT now(), changed_by_subject_id UUID NOT NULL, reason TEXT, signer1_subject_id UUID, signer2_subject_id UUID)</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> (per DEC-508).</li></ol>\n<ol><li><strong>MUST</strong> enforce the closed FSM transition matrix (per DEC-500 + DEC-502):</li></ol>\n<ul><li><code>active → terminating_a</code> (operator-initiated offboarding).</li><li><code>terminating_a → active</code> (cancellation; only within 30-day window).</li><li><code>terminating_a → terminating_b</code> (scheduled advance after 30 days; OR TASK-TEN-202 fast-track).</li><li><code>terminating_b → terminated</code> (scheduled advance after 60 days IN B + CSO+CLO dual-signoff).</li><li><code>active → terminating_b</code> (TASK-TEN-202 hostile-termination fast-track; CEO+CLO+CSO sign-off). All other transitions REJECTED at trigger + handler.</li></ul>\n<ol><li><strong>MUST</strong> ship the FSM at <code>services/ten/src/offboarding/fsm.rs</code> as a closed Rust function <code>validate_transition(from, to) -&gt; Result&lt;(), InvalidTransition&gt;</code>. The trigger <code>enforce_offboarding_fsm</code> mirrors the matrix at DB level.</li></ol>\n<ol><li><strong>MUST</strong> ship the scheduled advance job at <code>services/ten/src/offboarding/scheduler.rs</code> running hourly (per DEC-503). Query: <code>SELECT tenant_id, offboarding_state FROM tenant_offboarding_state WHERE scheduled_advance_at &lt;= now() AND offboarding_state IN ('terminating_a','terminating_b')</code>. For each row:</li></ol>\n<ul><li>If <code>terminating_a</code> → transition to <code>terminating_b</code>; emit memory row; wipe data to dead-letter bucket per §1 #12.</li><li>If <code>terminating_b</code> → DO NOT auto-advance; emit <code>ten.dual_signoff_required</code> notification to ops; manual transition only. Idempotent — re-running on already-advanced tenant is a no-op (trigger predicate enforces).</li></ul>\n<ol><li><strong>MUST</strong> enforce read-only freeze in <code>terminating_a</code> (per DEC-509). The TASK-AUTH-004 JWT issuer consults <code>read_only_gate::is_tenant_read_only(tenant_id)</code> — true → JWT's <code>scope_grants</code> filtered to read-only operations (read | list | view; write | create | update | delete excluded). Existing tokens issued pre-freeze are NOT invalidated; their writes fail at handler with 423 <code>tenant_read_only</code> + emit <code>ten.read_only_write_attempted</code> memory row.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/ten/offboarding/initiate</code> handler. Body: <code>{tenant_slug, reason, total_grace_days?}</code>. Caller MUST have role <code>root-admin</code> per TASK-AUTH-101. Validates:</li></ol>\n<ul><li>Tenant exists and is in <code>active</code> state.</li><li>reason 1–500 chars.</li><li>total_grace_days defaults to 90; if specified, must be 30-180. On success: UPDATE state to <code>terminating_a</code>; set <code>terminating_a_started_at = now()</code>, <code>scheduled_advance_at = now() + INTERVAL '30 days'</code> (or proportional); emit <code>ten.offboarding_initiated</code> + <code>ten.terminating_a_entered</code> memory rows.</li></ul>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/ten/offboarding/cancel</code> (per DEC-510). Caller MUST have role <code>root-admin</code> OR <code>tenant-admin</code> for the target tenant. Body: <code>{tenant_slug, reason, confirmation: \"I_UNDERSTAND_CANCELLATION\"}</code>. Validates:</li></ol>\n<ul><li>Tenant is in <code>terminating_a</code> state (NEVER allowed in terminating_b or terminated).</li><li>confirmation string matches exactly. On success: UPDATE state to <code>active</code>; clear <code>terminating_a_started_at</code>, <code>scheduled_advance_at</code>; emit <code>ten.terminating_cancelled</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/ten/offboarding/extend</code> (per DEC-512). Caller MUST have role <code>clo</code> (Chief Legal Officer) per TASK-AUTH-101. Body: <code>{tenant_slug, additional_days, reason}</code>. Validates:</li></ol>\n<ul><li>Tenant in <code>terminating_a</code> OR <code>terminating_b</code>.</li><li>additional_days in [1, 30].</li><li>reason non-empty.</li><li>extension_count &lt; 2. On success: UPDATE <code>scheduled_advance_at = scheduled_advance_at + additional_days</code>; UPDATE <code>extension_count = extension_count + 1</code>; UPDATE <code>total_grace_days += additional_days</code>; emit <code>ten.offboarding_extended</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/ten/offboarding/finalize-termination</code> (per DEC-506). Body: <code>{tenant_slug, signer1_subject_id, signer2_subject_id, reason, confirmation: \"I_AUTHORISE_IRREVERSIBLE_DELETE\"}</code>. Validates:</li></ol>\n<ul><li>Tenant in <code>terminating_b</code>.</li><li><code>signer1</code> has role <code>cseco</code> (Chief Security Officer) per TASK-AUTH-101.</li><li><code>signer2</code> has role <code>clo</code>.</li><li><code>signer1 != signer2</code>.</li><li>confirmation matches. On success: UPDATE state to <code>terminated</code>; set <code>terminated_at = now()</code>; emit <code>ten.tenant_terminated</code> memory row; trigger TASK-TEN-106 attestation row.</li></ul>\n<ol><li><strong>MUST</strong> wipe tenant data to dead-letter S3 bucket on <code>terminating_a → terminating_b</code> transition (per DEC-505). The dead-letter writer:</li></ol>\n<ul><li>Streams Postgres tables tagged with the tenant_id to S3 (per-table CSV/JSONL export).</li><li>Encrypts at rest with KMS key separate from production keyspace.</li><li>Applies S3 Object-Lock COMPLIANCE mode with retention = <code>terminating_b_started_at + 60 days</code> (or per-tenant policy override; min 30 days).</li><li>Deletes the rows from production Postgres after S3 commit confirmation.</li><li>Emits <code>ten.dead_letter_written</code> row carrying <code>byte_count</code>, <code>table_count</code>, <code>kms_key_id</code>, <code>s3_bucket</code>, <code>retention_until</code>.</li></ul>\n<ol><li><strong>MUST</strong> support <code>POST /v1/ten/offboarding/restore-from-dead-letter</code> (per DEC-513). Body: <code>{tenant_slug, signer1_subject_id, signer2_subject_id, reason, confirmation: \"I_AUTHORISE_DEAD_LETTER_RESTORE\"}</code>. Validates same shape as §1 #11 (CSO + CLO dual-signoff). Tenant must be in <code>terminating_b</code>. On success: restore rows from S3 dead-letter into production Postgres; UPDATE state to <code>terminating_a</code> (re-enter the 30-day grace); emit <code>ten.dead_letter_restored</code> memory row sev-1.</li></ol>\n<ol><li><strong>MUST</strong> emit 8 memory audit row kinds (per DEC-507):</li></ol>\n<ul><li><code>ten.offboarding_initiated</code> — first initiation.</li><li><code>ten.terminating_a_entered</code> — entry into read-only grace.</li><li><code>ten.terminating_b_entered</code> — entry into dead-letter grace.</li><li><code>ten.terminating_cancelled</code> — cancellation from terminating_a.</li><li><code>ten.tenant_terminated</code> — irreversible terminal state.</li><li><code>ten.offboarding_extended</code> — extension applied.</li><li><code>ten.dead_letter_restored</code> — restore from dead-letter; sev-1.</li><li><code>ten.read_only_write_attempted</code> — write rejected during terminating_a.</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>reason</code> and <code>initiated_reason</code> fields via TASK-MEMORY-111 before chain commit.</li></ol>\n<ol><li><strong>MUST</strong> ensure the FSM trigger (<code>enforce_offboarding_fsm</code>) covers ALL forbidden transitions (per DEC-514). Trigger raises specific error codes per illegal transition; tests assert each.</li></ol>\n<ol><li><strong>MUST</strong> pin <code>terminated_at</code> immutable post-set — once terminated, the row's terminated_at cannot be changed. A <code>BEFORE UPDATE</code> trigger rejects.</li></ol>\n<ol><li><strong>MUST</strong> enforce <code>extension_count &lt;= 2</code> at DB CHECK constraint (per DEC-512). Third extension attempt fails at INSERT/UPDATE with <code>extension_count_exceeded</code>.</li></ol>\n<ol><li><strong>MUST</strong> ensure the read-only gate caches per-tenant state with 60-second TTL (matching TASK-AUTH-109's pattern). Cache invalidated on state transition.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>total_grace_days</code> ∈ [30, 180] at handler initiate + extend. Below 30 (GDPR minimum reasonable grace) → 400; above 180 → 400.</li></ol>\n<ol><li><strong>MUST</strong> support per-tenant <code>dead_letter_retention_days</code> override in <code>tenant_offboarding_state</code> (default 60; min 30). Allows high-trust tenants longer recovery window OR high-risk tenants shorter retention.</li></ol>\n<ol><li><strong>MUST</strong> complete state transition handlers in ≤ 200 ms p95 (excluding async dead-letter wipe). <code>offboarding_perf_test</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>ten.offboarding.{initiate,advance,cancel,extend,terminate,restore,write_attempted}</code> with <code>outcome</code> attribute (success | invalid_transition | wrong_state | already_terminated | wrong_role | signer_role_mismatch | self_co_sign | confirmation_mismatch | extension_count_exceeded | dead_letter_unavailable).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>ten_offboarding_state_count{state}</code> (gauge — count of tenants per state).</li><li><code>ten_offboarding_transitions_total{from_state, to_state, outcome}</code> (counter).</li><li><code>ten_offboarding_read_only_writes_blocked_total{tenant_id}</code> (counter — sev-3 alarm at &gt; 100/h indicates stuck client).</li><li><code>ten_dead_letter_bytes_total{tenant_id}</code> (counter — bytes wiped to dead letter).</li><li><code>ten_offboarding_active_extensions{tenant_id}</code> (gauge — current extension_count).</li></ul>\n<ol><li><strong>MUST</strong> ship <code>GET /v1/ten/offboarding/state/{tenant_slug}</code> for operator visibility. Returns <code>{state, initiated_at, scheduled_advance_at, days_remaining_in_state, extension_count, can_cancel: bool, can_extend: bool, can_terminate: bool}</code>. Caller MUST be root-admin or tenant-admin for the target tenant.</li></ol>\n<ol><li><strong>MUST</strong> support <code>POST /v1/ten/offboarding/force-advance</code> for emergency operator override — caller MUST be root-admin. Forces FSM transition with explicit reason. Emits memory row at sev-1 (operator override is forensically critical). Used for <code>terminating_b → terminated</code> after dual-signoff handler when scheduled advance hasn't fired yet.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why closed 4-state FSM (DEC-500)?</strong> Each state corresponds to a distinct legal+operational meaning. <code>active</code> = customer pays + has full access. <code>terminating_a</code> = grace period for export + cancellation. <code>terminating_b</code> = data already wiped to dead-letter; only restoration is recovery. <code>terminated</code> = irreversible per Object-Lock. A 5th state (e.g. <code>suspended</code> for non-payment) is task-TEN-2xx — distinct lifecycle, separate FSM. Closed enum prevents drift.</p>\n<p><strong>Why 30+60 = 90 days default (DEC-501)?</strong> GDPR Art. 17 doesn't specify a fixed grace, but industry practice is 90 days (matches Salesforce, Microsoft 365, Zendesk). 30-day RO + export covers the \"I changed my mind\" case; 60-day dead-letter covers the \"I changed my mind two months later\" case. Per-tenant config allows 30-180 for high-trust or high-risk variants.</p>\n<p><strong>Why terminating_b → active forbidden (DEC-502)?</strong> By terminating_b entry, the production data has been wiped to S3 dead-letter. \"Active\" requires production data; restoring from dead-letter goes via <code>terminating_a</code> re-entry (a fresh 30-day grace). Forbidding the direct path prevents partial-restore confusion.</p>\n<p><strong>Why hourly scheduled advance (DEC-503)?</strong> Day-pinned transitions (30 day, 60 day) don't need minute-level precision. Hourly job is cheap to run and gives &lt; 1h transition latency. The scheduler is idempotent — <code>WHERE scheduled_advance_at &lt;= now() AND state = '&lt;expected&gt;'</code> prevents double-advance under concurrent runs.</p>\n<p><strong>Why scheduled job DOESN'T auto-terminate (DEC-506, §1 #6)?</strong> <code>terminating_b → terminated</code> is irreversible (Object-Lock COMPLIANCE). Auto-firing would mean a buggy job could mass-terminate tenants. Manual dual-signoff CSO+CLO is the deliberate gate; the scheduled job emits notification but never advances.</p>\n<p><strong>Why CSO + CLO dual-signoff (DEC-506, §1 #11)?</strong> Terminated tenant = permanent data loss = legal-grade decision. CSO (Chief Security Officer) confirms operational readiness; CLO (Chief Legal Officer) confirms legal sufficiency. Two distinct roles + non-self-co-sign prevents single-operator catastrophic action.</p>\n<p><strong>Why read-only freeze at JWT issuance (DEC-509, §1 #7)?</strong> Filtering at JWT issuance time is cheap (one lookup); enforcing at every handler is expensive (N × handler-count). The cache (60s TTL) makes the hot path fast. Existing tokens are not invalidated — their writes fail at handler (defense in depth) with clear 423 + emit audit.</p>\n<p><strong>Why 423 status for read-only write (RFC 4918)?</strong> \"Locked\" is the WebDAV semantic but increasingly used for \"resource exists but is read-only\". 403 would suggest permission; 410 would suggest gone. 423 communicates \"the data is here but you cannot write to it now\".</p>\n<p><strong>Why max 2 extensions per cycle (DEC-512, §1 #18)?</strong> Extensions are escape valves for stuck client cleanups; not policy-as-extension. 2 × 30 days max = 60 days additional = total 150 days. Below 30 days minimum is too aggressive; &gt; 180 days defeats the offboarding's intent.</p>\n<p><strong>Why dead-letter restore is sev-1 memory row (DEC-513, §1 #14)?</strong> Restore is a rare + consequential event (operator decided \"we shouldn't have offboarded; restore\"). Sev-1 ensures every restore is visible in OBS digests for root-cause analysis.</p>\n<p><strong>Why dead-letter Object-Lock COMPLIANCE (DEC-505, DEC-506)?</strong> Compliance mode prevents even AWS root account from deleting before retention expires. Governance mode would allow root override; we want the irreversibility guarantee.</p>\n<p><strong>Why per-tenant dead_letter_retention_days override (§1 #21)?</strong> Some tenants want 30 days (privacy-strict EU); some want 90 days (extra recovery margin). Range 30-90 + per-tenant config preserves the GDPR-floor + tenant-flexibility tradeoff.</p>\n<p><strong>Why explicit confirmation string per handler (§1 #9, #11, #13)?</strong> Destructive operations (cancel, terminate, restore) are rare; operator confirms by typing the exact string. Prevents accidental click-through.</p>\n<p><strong>Why TASK-TEN-202 fast-track bypasses 30-day terminating_a (DEC-511)?</strong> Hostile termination (regulatory order, court injunction) demands faster action than 90-day grace allows. CEO+CLO+CSO triple-sign-off (vs CSO+CLO dual for normal flow) reflects the elevated authority. This task ships the FSM that TASK-TEN-202 consumes; the fast-track handler is TASK-TEN-202.</p>\n<p><strong>Why FSM at trigger AND handler (DEC-514, §1 #16)?</strong> Trigger catches direct SQL access (e.g. operator psql session bypassing handlers). Handler provides clearer error responses (HTTP status, JSON body). Both ensure invariant holds.</p>\n<p><strong>Why pin scheduled_advance_at at entry (DEC-515, §1 #6)?</strong> Captures the target transition time as a column — scheduler queries directly. Extensions UPDATE this; cancellation clears. The pinning makes the day-pinned transition explicit rather than implicit from <code>terminating_a_started_at + 30 days</code> (which would re-derive on every job run, susceptible to drift if base column changes).</p>\n<p><strong>Why CLO role for extension, not root-admin (§1 #10)?</strong> Extension is a legal-judgment call (is this delay reasonable + non-prejudicial?). CLO is the natural role; root-admin is more about technical operations. Per RBAC catalogue both have authority, but the principle of least privilege puts extension under CLO.</p>\n<p><strong>Why tenant-admin can also cancel (§1 #9)?</strong> Cancellation is \"I changed my mind\" — the tenant's own decision. tenant-admin (representing the tenant) has legitimate authority. root-admin (representing CyberSkill ops) can also cancel for operational reasons (e.g. payment received late).</p>\n<p><strong>Why read_only_write_attempted as a memory row + alarm (§1 #14, §1 #24)?</strong> Sustained write attempts during freeze (&gt; 100/h) signal a stuck client that hasn't observed the read-only state — operator notification helps identify integrations needing attention before terminating_b wipe.</p>\n<p><strong>Why force-advance is sev-1 (§1 #26)?</strong> Operator overrides of FSM are forensically critical — \"operator advanced to terminated before scheduled time\" is exactly the kind of event regulators want to see in audit chains. Sev-1 ensures unmissable.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0004 — tenant_offboarding_state</h3>\n<pre><code class=\"language-sql\">-- services/ten/migrations/0004_tenant_offboarding_state.sql\n\nBEGIN;\n\nCREATE TYPE offboarding_state AS ENUM ('active', 'terminating_a', 'terminating_b', 'terminated');\n\nCREATE TABLE tenant_offboarding_state (\n    tenant_id                       UUID         PRIMARY KEY REFERENCES tenants(id),\n    offboarding_state               offboarding_state NOT NULL DEFAULT 'active',\n    initiated_at                    TIMESTAMPTZ,\n    initiated_by_subject_id         UUID,\n    initiated_reason                TEXT         CHECK (initiated_reason IS NULL OR length(initiated_reason) BETWEEN 1 AND 500),\n    scheduled_advance_at            TIMESTAMPTZ,\n    extension_count                 INT          NOT NULL DEFAULT 0 CHECK (extension_count BETWEEN 0 AND 2),\n    total_grace_days                INT          NOT NULL DEFAULT 90 CHECK (total_grace_days BETWEEN 30 AND 180),\n    dead_letter_retention_days      INT          NOT NULL DEFAULT 60 CHECK (dead_letter_retention_days BETWEEN 30 AND 90),\n    terminating_a_started_at        TIMESTAMPTZ,\n    terminating_b_started_at        TIMESTAMPTZ,\n    terminated_at                   TIMESTAMPTZ\n);\n\nALTER TABLE tenant_offboarding_state ENABLE ROW LEVEL SECURITY;\nCREATE POLICY ten_offboarding_state_policy ON tenant_offboarding_state\n    USING (current_setting('auth.is_root_admin', true) = 'true'\n           OR tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (current_setting('auth.is_root_admin', true) = 'true'\n                OR tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON tenant_offboarding_state FROM cyberos_app;\nGRANT INSERT, UPDATE (offboarding_state, initiated_at, initiated_by_subject_id, initiated_reason,\n                     scheduled_advance_at, extension_count, total_grace_days, dead_letter_retention_days,\n                     terminating_a_started_at, terminating_b_started_at, terminated_at)\n    ON tenant_offboarding_state TO cyberos_provisioner;\n\n-- FSM enforcement (DEC-514)\nCREATE OR REPLACE FUNCTION enforce_offboarding_fsm() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.offboarding_state = OLD.offboarding_state THEN RETURN NEW; END IF;\n    IF NOT (\n        (OLD.offboarding_state = 'active'         AND NEW.offboarding_state = 'terminating_a')\n        OR (OLD.offboarding_state = 'active'      AND NEW.offboarding_state = 'terminating_b')   -- TASK-TEN-202 hostile\n        OR (OLD.offboarding_state = 'terminating_a' AND NEW.offboarding_state = 'active')        -- cancel\n        OR (OLD.offboarding_state = 'terminating_a' AND NEW.offboarding_state = 'terminating_b') -- scheduled\n        OR (OLD.offboarding_state = 'terminating_b' AND NEW.offboarding_state = 'terminating_a') -- dead-letter restore\n        OR (OLD.offboarding_state = 'terminating_b' AND NEW.offboarding_state = 'terminated')    -- finalize\n    ) THEN\n        RAISE EXCEPTION 'invalid_offboarding_transition: % -&gt; %', OLD.offboarding_state, NEW.offboarding_state USING ERRCODE = 'P0090';\n    END IF;\n    -- Immutable terminated_at post-set\n    IF OLD.terminated_at IS NOT NULL AND NEW.terminated_at IS DISTINCT FROM OLD.terminated_at THEN\n        RAISE EXCEPTION 'terminated_at_immutable' USING ERRCODE = 'P0091';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_ten_offboarding_fsm BEFORE UPDATE ON tenant_offboarding_state\n    FOR EACH ROW EXECUTE FUNCTION enforce_offboarding_fsm();\n\n-- Seed: every tenant starts in 'active'\nINSERT INTO tenant_offboarding_state (tenant_id, offboarding_state)\nSELECT id, 'active' FROM tenants ON CONFLICT (tenant_id) DO NOTHING;\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0005 — append-only log</h3>\n<pre><code class=\"language-sql\">-- services/ten/migrations/0005_tenant_offboarding_log.sql\n\nBEGIN;\n\nCREATE TABLE tenant_offboarding_log (\n    id                       BIGSERIAL    PRIMARY KEY,\n    tenant_id                UUID         NOT NULL REFERENCES tenants(id) ON DELETE RESTRICT,\n    from_state               offboarding_state,\n    to_state                 offboarding_state NOT NULL,\n    changed_at               TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    changed_by_subject_id    UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    reason                   TEXT,\n    signer1_subject_id       UUID,\n    signer2_subject_id       UUID\n);\n\nCREATE INDEX ten_offboarding_log_tenant_idx ON tenant_offboarding_log (tenant_id, changed_at DESC);\n\nALTER TABLE tenant_offboarding_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY ten_offboarding_log_policy ON tenant_offboarding_log\n    USING (current_setting('auth.is_root_admin', true) = 'true'\n           OR tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (current_setting('auth.is_root_admin', true) = 'true');\n\nREVOKE UPDATE, DELETE ON tenant_offboarding_log FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.3 — FSM validator</h3>\n<pre><code class=\"language-rust\">// services/ten/src/offboarding/fsm.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, sqlx::Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"offboarding_state\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum OffboardingState { Active, TerminatingA, TerminatingB, Terminated }\n\nimpl OffboardingState {\n    pub const ALL: &amp;'static [OffboardingState] = &amp;[\n        OffboardingState::Active, OffboardingState::TerminatingA,\n        OffboardingState::TerminatingB, OffboardingState::Terminated,\n    ];\n}\n\n#[derive(Debug, thiserror::Error)]\n#[error(\"invalid_offboarding_transition: {from:?} -&gt; {to:?}\")]\npub struct InvalidTransition { pub from: OffboardingState, pub to: OffboardingState }\n\npub fn is_valid_transition(from: OffboardingState, to: OffboardingState) -&gt; bool {\n    use OffboardingState::*;\n    matches!((from, to),\n        (Active, TerminatingA)\n        | (Active, TerminatingB)       // TASK-TEN-202 hostile\n        | (TerminatingA, Active)       // cancel\n        | (TerminatingA, TerminatingB) // scheduled\n        | (TerminatingB, TerminatingA) // dead-letter restore\n        | (TerminatingB, Terminated)   // finalize\n    )\n}\n\npub fn validate_transition(from: OffboardingState, to: OffboardingState) -&gt; Result&lt;(), InvalidTransition&gt; {\n    if is_valid_transition(from, to) { Ok(()) } else { Err(InvalidTransition { from, to }) }\n}</code></pre>\n<h3>3.4 — Scheduler</h3>\n<pre><code class=\"language-rust\">// services/ten/src/offboarding/scheduler.rs\nuse uuid::Uuid;\nuse chrono::{DateTime, Utc};\nuse crate::offboarding::fsm::OffboardingState;\n\npub async fn run_hourly_advance(db: &amp;sqlx::PgPool) -&gt; anyhow::Result&lt;()&gt; {\n    // Find tenants ready to advance\n    let candidates: Vec&lt;(Uuid, OffboardingState, DateTime&lt;Utc&gt;)&gt; = sqlx::query_as(r#\"\n        SELECT tenant_id, offboarding_state, scheduled_advance_at\n        FROM tenant_offboarding_state\n        WHERE scheduled_advance_at &lt;= now()\n          AND offboarding_state IN ('terminating_a','terminating_b')\n        FOR UPDATE SKIP LOCKED\n    \"#).fetch_all(db).await?;\n\n    for (tenant_id, state, _scheduled) in candidates {\n        match state {\n            OffboardingState::TerminatingA =&gt; {\n                advance_a_to_b(db, tenant_id).await?;\n            }\n            OffboardingState::TerminatingB =&gt; {\n                // DO NOT auto-advance to terminated (per DEC-506 — dual signoff required)\n                notify_ops_terminating_b_ready(db, tenant_id).await?;\n            }\n            _ =&gt; continue,\n        }\n    }\n    Ok(())\n}\n\nasync fn advance_a_to_b(db: &amp;sqlx::PgPool, tenant_id: Uuid) -&gt; anyhow::Result&lt;()&gt; {\n    let mut tx = db.begin().await?;\n    // Mark state transition (trigger validates)\n    let dead_letter_days: i32 = sqlx::query_scalar(\n        \"SELECT dead_letter_retention_days FROM tenant_offboarding_state WHERE tenant_id = $1 FOR UPDATE\"\n    ).bind(tenant_id).fetch_one(&amp;mut *tx).await?;\n\n    sqlx::query(r#\"\n        UPDATE tenant_offboarding_state\n        SET offboarding_state = 'terminating_b'::offboarding_state,\n            terminating_b_started_at = now(),\n            scheduled_advance_at = now() + ($2 || ' days')::interval\n        WHERE tenant_id = $1\n          AND offboarding_state = 'terminating_a'::offboarding_state\n    \"#).bind(tenant_id).bind(dead_letter_days as i64).execute(&amp;mut *tx).await?;\n\n    // Log + audit\n    sqlx::query(r#\"\n        INSERT INTO tenant_offboarding_log (tenant_id, from_state, to_state, changed_by_subject_id, reason)\n        VALUES ($1, 'terminating_a'::offboarding_state, 'terminating_b'::offboarding_state, $2, 'scheduled_advance')\n    \"#).bind(tenant_id).bind(system_subject_id()).execute(&amp;mut *tx).await?;\n\n    crate::offboarding::audit::emit_terminating_b_entered(&amp;mut tx, tenant_id).await?;\n    tx.commit().await?;\n\n    // Async: wipe to dead-letter (returns soon; actual work in background)\n    tokio::spawn(crate::offboarding::dead_letter::wipe_tenant_to_dead_letter(tenant_id, db.clone()));\n    Ok(())\n}\n\nasync fn notify_ops_terminating_b_ready(db: &amp;sqlx::PgPool, tenant_id: Uuid) -&gt; anyhow::Result&lt;()&gt; {\n    // Emit OBS sev-3 notification — operator must invoke finalize-termination handler\n    metrics::counter!(\"ten_offboarding_b_ready_count\", \"tenant_id\" =&gt; tenant_id.to_string()).increment(1);\n    Ok(())\n}\n\nfn system_subject_id() -&gt; Uuid {\n    Uuid::nil()  // Schedule runs as system; subject_id_hash16 = nil\n}</code></pre>\n<h3>3.5 — Read-only gate</h3>\n<pre><code class=\"language-rust\">// services/ten/src/offboarding/read_only_gate.rs\nuse std::sync::Arc;\nuse std::time::{Duration, Instant};\nuse tokio::sync::RwLock;\nuse std::collections::HashMap;\nuse uuid::Uuid;\nuse crate::offboarding::fsm::OffboardingState;\n\npub struct ReadOnlyGate {\n    cache: Arc&lt;RwLock&lt;HashMap&lt;Uuid, (bool, Instant)&gt;&gt;&gt;,\n}\n\nimpl ReadOnlyGate {\n    pub fn new() -&gt; Self {\n        Self { cache: Arc::new(RwLock::new(HashMap::new())) }\n    }\n\n    pub async fn is_tenant_read_only(&amp;self, tenant_id: Uuid, db: &amp;sqlx::PgPool) -&gt; bool {\n        if let Some((ro, t)) = self.cache.read().await.get(&amp;tenant_id) {\n            if t.elapsed() &lt; Duration::from_secs(60) {\n                return *ro;\n            }\n        }\n        let state: Option&lt;OffboardingState&gt; = sqlx::query_scalar(\n            \"SELECT offboarding_state FROM tenant_offboarding_state WHERE tenant_id = $1\"\n        ).bind(tenant_id).fetch_optional(db).await.ok().flatten();\n        let ro = matches!(state, Some(OffboardingState::TerminatingA) | Some(OffboardingState::TerminatingB) | Some(OffboardingState::Terminated));\n        self.cache.write().await.insert(tenant_id, (ro, Instant::now()));\n        ro\n    }\n\n    pub async fn invalidate(&amp;self, tenant_id: Uuid) {\n        self.cache.write().await.remove(&amp;tenant_id);\n    }\n}</code></pre>\n<h3>3.6 — Initiate handler</h3>\n<pre><code class=\"language-rust\">// services/ten/src/handlers/offboarding.rs (excerpt)\nuse axum::{Json, extract::State, http::StatusCode};\nuse cyberos_auth::rbac::Role;\nuse crate::offboarding::{fsm::OffboardingState, audit};\nuse uuid::Uuid;\n\n#[derive(Deserialize)]\npub struct InitiateRequest {\n    pub tenant_slug: String,\n    pub reason: String,\n    pub total_grace_days: Option&lt;i32&gt;,\n}\n\npub async fn initiate_offboarding(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Json(req): Json&lt;InitiateRequest&gt;,\n) -&gt; Result&lt;StatusCode, ApiError&gt; {\n    if !claims.roles().contains(&amp;Role::RootAdmin) {\n        return Err(ApiError::PermissionDenied);\n    }\n    if req.reason.is_empty() || req.reason.len() &gt; 500 {\n        return Err(ApiError::ReasonInvalid);\n    }\n    let grace = req.total_grace_days.unwrap_or(90);\n    if !(30..=180).contains(&amp;grace) {\n        return Err(ApiError::GraceDaysOutOfRange);\n    }\n\n    let tenant_id = state.repo.find_tenant_by_slug(&amp;req.tenant_slug).await?\n        .ok_or(ApiError::TenantUnknown)?;\n\n    let mut tx = state.db.begin().await?;\n    // The 30/60 split holds the proportion: 30/(30+60) = 1/3 → terminating_a = grace/3\n    let a_days = (grace * 30) / 90;\n    sqlx::query(r#\"\n        UPDATE tenant_offboarding_state\n        SET offboarding_state = 'terminating_a'::offboarding_state,\n            initiated_at = now(),\n            initiated_by_subject_id = $2,\n            initiated_reason = $3,\n            terminating_a_started_at = now(),\n            scheduled_advance_at = now() + ($4 || ' days')::interval,\n            total_grace_days = $5\n        WHERE tenant_id = $1 AND offboarding_state = 'active'::offboarding_state\n    \"#).bind(tenant_id).bind(claims.subject_id()).bind(&amp;req.reason)\n       .bind(a_days as i64).bind(grace).execute(&amp;mut *tx).await?;\n\n    sqlx::query(r#\"\n        INSERT INTO tenant_offboarding_log (tenant_id, from_state, to_state, changed_by_subject_id, reason)\n        VALUES ($1, 'active'::offboarding_state, 'terminating_a'::offboarding_state, $2, $3)\n    \"#).bind(tenant_id).bind(claims.subject_id()).bind(&amp;req.reason).execute(&amp;mut *tx).await?;\n\n    audit::emit_offboarding_initiated(&amp;mut tx, tenant_id, claims.subject_id(), &amp;req.reason).await?;\n    audit::emit_terminating_a_entered(&amp;mut tx, tenant_id).await?;\n    tx.commit().await?;\n    state.read_only_gate.invalidate(tenant_id).await;\n    Ok(StatusCode::CREATED)\n}</code></pre>\n<h3>3.7 — Dual-signoff termination</h3>\n<pre><code class=\"language-rust\">// services/ten/src/handlers/offboarding.rs (continued)\n#[derive(Deserialize)]\npub struct FinalizeTerminationRequest {\n    pub tenant_slug: String,\n    pub signer1_subject_id: Uuid,\n    pub signer2_subject_id: Uuid,\n    pub reason: String,\n    pub confirmation: String,\n}\n\nconst TERMINATE_CONFIRMATION: &amp;str = \"I_AUTHORISE_IRREVERSIBLE_DELETE\";\n\npub async fn finalize_termination(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Json(req): Json&lt;FinalizeTerminationRequest&gt;,\n) -&gt; Result&lt;StatusCode, ApiError&gt; {\n    if req.confirmation != TERMINATE_CONFIRMATION {\n        return Err(ApiError::ConfirmationMismatch);\n    }\n    if req.signer1_subject_id == req.signer2_subject_id {\n        return Err(ApiError::SelfCoSign);\n    }\n    // Verify signer roles\n    let s1_roles = state.repo.subject_roles(req.signer1_subject_id).await?;\n    let s2_roles = state.repo.subject_roles(req.signer2_subject_id).await?;\n    if !s1_roles.contains(&amp;Role::Cseco) {\n        return Err(ApiError::SignerRoleMismatch { required: Role::Cseco });\n    }\n    if !s2_roles.contains(&amp;Role::Clo) {\n        return Err(ApiError::SignerRoleMismatch { required: Role::Clo });\n    }\n\n    let tenant_id = state.repo.find_tenant_by_slug(&amp;req.tenant_slug).await?\n        .ok_or(ApiError::TenantUnknown)?;\n    let mut tx = state.db.begin().await?;\n    sqlx::query(r#\"\n        UPDATE tenant_offboarding_state\n        SET offboarding_state = 'terminated'::offboarding_state,\n            terminated_at = now(),\n            scheduled_advance_at = NULL\n        WHERE tenant_id = $1 AND offboarding_state = 'terminating_b'::offboarding_state\n    \"#).bind(tenant_id).execute(&amp;mut *tx).await?;\n\n    sqlx::query(r#\"\n        INSERT INTO tenant_offboarding_log (tenant_id, from_state, to_state, changed_by_subject_id, reason, signer1_subject_id, signer2_subject_id)\n        VALUES ($1, 'terminating_b'::offboarding_state, 'terminated'::offboarding_state, $2, $3, $4, $5)\n    \"#).bind(tenant_id).bind(claims.subject_id()).bind(&amp;req.reason)\n       .bind(req.signer1_subject_id).bind(req.signer2_subject_id).execute(&amp;mut *tx).await?;\n\n    audit::emit_tenant_terminated(&amp;mut tx, tenant_id, req.signer1_subject_id, req.signer2_subject_id, &amp;req.reason).await?;\n    tx.commit().await?;\n    state.read_only_gate.invalidate(tenant_id).await;\n\n    // Trigger TASK-TEN-106 attestation row (out of scope here; placeholder)\n    Ok(StatusCode::OK)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>OffboardingState enum closed at 4</strong> — active, terminating_a, terminating_b, terminated.</li><li><strong>Seed at migration</strong> — every existing tenant gets offboarding_state='active' row.</li><li><strong>active → terminating_a happy path</strong> — root-admin initiate → 201; row updated; <code>ten.offboarding_initiated</code> + <code>ten.terminating_a_entered</code> rows emitted.</li><li><strong>terminating_a → active cancel</strong> — within 30-day window → 200; row reverts; <code>ten.terminating_cancelled</code> row.</li><li><strong>terminating_b → active forbidden</strong> — trigger raises invalid_offboarding_transition.</li><li>**terminated → * forbidden** — trigger rejects.</li><li><strong>active → terminated direct forbidden</strong> — must pass through B (only TASK-TEN-202 fast-track allows active → terminating_b).</li><li><strong>Scheduled job advances A → B</strong> — tenant with scheduled_advance_at &lt; now() → state flips to terminating_b after hourly run; dead-letter write triggered.</li><li><strong>Scheduled job DOES NOT advance B → terminated</strong> — terminating_b with past scheduled_advance_at → notification only.</li><li><strong>CSO + CLO dual-signoff terminate</strong> — valid signers + correct confirmation → state → terminated; <code>ten.tenant_terminated</code> row.</li><li><strong>Single-signer terminate rejected</strong> — same signer for both fields → 400 self_co_sign.</li><li><strong>Wrong signer role rejected</strong> — non-CSO at signer1 → 400 signer_role_mismatch.</li><li><strong>Confirmation string mismatch</strong> — wrong confirmation → 400 confirmation_mismatch.</li><li><strong>Extension by CLO succeeds</strong> — additional_days in [1,30] + extension_count &lt; 2 → 200.</li><li><strong>Extension by non-CLO rejected</strong> → 403.</li><li><strong>3rd extension rejected</strong> → 409 extension_count_exceeded.</li><li><strong>Read-only freeze in terminating_a</strong> — JWT issued for tenant in terminating_a has scope_grants filtered to read-only.</li><li><strong>Write attempt in terminating_a</strong> → 423 tenant_read_only + <code>ten.read_only_write_attempted</code> row.</li><li><strong>Dead-letter write on A→B transition</strong> — S3 object created with Object-Lock COMPLIANCE; <code>ten.dead_letter_written</code> row.</li><li><strong>Dead-letter restore by CSO+CLO</strong> — restores rows + state → terminating_a + sev-1 audit.</li><li><strong>terminated_at immutable</strong> — UPDATE attempt → trigger raises terminated_at_immutable.</li><li><strong>append-only tenant_offboarding_log</strong> — UPDATE/DELETE rejected.</li><li><strong>append-only via auth_provisioner role only</strong> — cyberos_app blocked.</li><li><strong>GET /offboarding/state returns days_remaining + can_cancel + can_extend + can_terminate</strong>.</li><li><strong>Force-advance by root-admin</strong> → state advances + sev-1 audit row.</li><li><strong>8 memory audit kinds emit correctly</strong> — one per lifecycle event.</li><li><strong>OTel span emitted per handler</strong> — outcome populated.</li><li><strong>Counter <code>ten_offboarding_state_count{state}</code> reflects current counts</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/ten/tests/offboarding_fsm_test.rs\nuse cyberos_ten::offboarding::fsm::{OffboardingState::*, is_valid_transition};\n\n#[test]\nfn valid_transitions() {\n    for (from, to) in [(Active, TerminatingA), (Active, TerminatingB),\n                       (TerminatingA, Active), (TerminatingA, TerminatingB),\n                       (TerminatingB, TerminatingA), (TerminatingB, Terminated)] {\n        assert!(is_valid_transition(from, to));\n    }\n}\n\n#[test]\nfn forbidden_transitions() {\n    for (from, to) in [(Active, Terminated), (Terminated, Active),\n                       (Terminated, TerminatingA), (Terminated, TerminatingB),\n                       (TerminatingB, Active)] {\n        assert!(!is_valid_transition(from, to));\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ten/tests/offboarding_terminating_a_read_only_test.rs\n#[tokio::test]\nasync fn write_attempt_returns_423_during_grace(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_in_terminating_a().await;\n    let token = ctx.issue_token_for_tenant(tenant).await;\n    let resp = ctx.post_with_token(\"/v1/some/write-endpoint\", json!({\"x\":1}), &amp;token).await;\n    assert_eq!(resp.status(), 423);\n    let body: serde_json::Value = resp.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"tenant_read_only\");\n    let rows = ctx.memory_audit_rows(\"ten.read_only_write_attempted\").await;\n    assert_eq!(rows.len(), 1);\n}\n\n#[tokio::test]\nasync fn read_attempt_succeeds_during_grace(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_in_terminating_a().await;\n    let token = ctx.issue_token_for_tenant(tenant).await;\n    let resp = ctx.get_with_token(\"/v1/some/read-endpoint\", &amp;token).await;\n    assert_eq!(resp.status(), 200);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ten/tests/offboarding_dead_letter_restore_dual_signoff_test.rs\n#[tokio::test]\nasync fn restore_requires_both_cso_and_clo(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_in_terminating_b_with_dead_letter().await;\n    let cso = ctx.subject_with_role(Role::Cseco).await;\n    let other = ctx.subject_with_role(Role::Cfo).await;\n    let err = ctx.post_as_root_admin(\"/v1/ten/offboarding/restore-from-dead-letter\", json!({\n        \"tenant_slug\": tenant.slug,\n        \"signer1_subject_id\": cso,\n        \"signer2_subject_id\": other,   // not CLO\n        \"reason\": \"test restore\",\n        \"confirmation\": \"I_AUTHORISE_DEAD_LETTER_RESTORE\",\n    })).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"signer_role_mismatch\"));\n}\n\n#[tokio::test]\nasync fn restore_succeeds_with_proper_signers(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_in_terminating_b_with_dead_letter().await;\n    let cso = ctx.subject_with_role(Role::Cseco).await;\n    let clo = ctx.subject_with_role(Role::Clo).await;\n    let resp = ctx.post_as_root_admin(\"/v1/ten/offboarding/restore-from-dead-letter\", json!({\n        \"tenant_slug\": tenant.slug,\n        \"signer1_subject_id\": cso,\n        \"signer2_subject_id\": clo,\n        \"reason\": \"Customer disputed termination; ops decided to restore\",\n        \"confirmation\": \"I_AUTHORISE_DEAD_LETTER_RESTORE\",\n    })).await.unwrap();\n    assert_eq!(resp.status(), 200);\n    let updated = ctx.fetch_offboarding_state(tenant.id).await;\n    assert_eq!(updated.offboarding_state, OffboardingState::TerminatingA);\n    let rows = ctx.memory_audit_rows(\"ten.dead_letter_restored\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"severity\"], \"sev-1\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ten/tests/offboarding_extension_max_2_test.rs\n#[tokio::test]\nasync fn third_extension_rejected(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_in_terminating_a().await;\n    let body = json!({\"tenant_slug\": tenant.slug, \"additional_days\": 10, \"reason\": \"ext1\"});\n    ctx.post_as_clo(\"/v1/ten/offboarding/extend\", body.clone()).await.unwrap();\n    let body2 = json!({\"tenant_slug\": tenant.slug, \"additional_days\": 10, \"reason\": \"ext2\"});\n    ctx.post_as_clo(\"/v1/ten/offboarding/extend\", body2).await.unwrap();\n    let body3 = json!({\"tenant_slug\": tenant.slug, \"additional_days\": 10, \"reason\": \"ext3\"});\n    let err = ctx.post_as_clo(\"/v1/ten/offboarding/extend\", body3).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"extension_count_exceeded\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ten/tests/offboarding_terminated_irreversible_test.rs\n#[sqlx::test]\nasync fn terminated_to_active_rejected_at_trigger(pool: sqlx::PgPool) {\n    let tid = seed_terminated_tenant(&amp;pool).await;\n    set_role_provisioner(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE tenant_offboarding_state SET offboarding_state = 'active'::offboarding_state WHERE tenant_id = $1\")\n        .bind(tid).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"invalid_offboarding_transition\"));\n}\n\n#[sqlx::test]\nasync fn terminated_at_mutation_rejected(pool: sqlx::PgPool) {\n    let tid = seed_terminated_tenant(&amp;pool).await;\n    set_role_provisioner(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE tenant_offboarding_state SET terminated_at = now() + interval '1 day' WHERE tenant_id = $1\")\n        .bind(tid).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"terminated_at_immutable\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 8 memory row builders follow the canonical pattern; dead_letter wipe uses TASK-DOC-001 S3+KMS pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-TEN-001</strong> — tenant provisioning (this task consumes tenants table).</li></ul>\n<p><strong>Downstream (2 placeholders):</strong></p>\n<ul><li><strong>TASK-TEN-105</strong> — signed-bundle export (consumes terminating_a state to gate exports).</li><li><strong>TASK-TEN-106</strong> — permanent-delete attestation row (consumes terminating_b → terminated transition).</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AUTH-101</strong> — RBAC; root-admin (initiate/force), tenant-admin (cancel own), CLO (extend), CSO (terminate-signer1), CLO (terminate-signer2).</li><li><strong>TASK-AUTH-004</strong> — JWT issuer hooks read_only_gate at issuance.</li><li><strong>TASK-AI-003</strong> — memory audit bridge.</li><li><strong>TASK-MEMORY-111</strong> — PII scrub reason fields.</li><li><strong>TASK-OBS-007</strong> — sev-3 alarm on &gt; 100/h read-only-write attempts; sev-1 always on terminated + restore.</li><li><strong>TASK-DOC-001</strong> — S3 Object-Lock COMPLIANCE pattern for dead-letter bucket.</li><li><strong>TASK-TEN-202</strong> — hostile-termination fast-track (consumes active → terminating_b transition).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/ten/offboarding/initiate</h3>\n<pre><code class=\"language-json\">{\n  \"tenant_slug\": \"acme-corp\",\n  \"reason\": \"Customer requested termination per contract clause 12.3; effective 2026-08-15\",\n  \"total_grace_days\": 90\n}</code></pre>\n<h3>8.2 — POST /v1/ten/offboarding/finalize-termination</h3>\n<pre><code class=\"language-json\">{\n  \"tenant_slug\": \"acme-corp\",\n  \"signer1_subject_id\": \"&lt;cseco-uuid&gt;\",\n  \"signer2_subject_id\": \"&lt;clo-uuid&gt;\",\n  \"reason\": \"60-day dead-letter grace expired; no restoration request received; legal cleared.\",\n  \"confirmation\": \"I_AUTHORISE_IRREVERSIBLE_DELETE\"\n}</code></pre>\n<h3>8.3 — GET /v1/ten/offboarding/state/acme-corp</h3>\n<pre><code class=\"language-json\">{\n  \"offboarding_state\": \"terminating_a\",\n  \"initiated_at\": \"2026-05-15T10:00:00Z\",\n  \"scheduled_advance_at\": \"2026-06-14T10:00:00Z\",\n  \"days_remaining_in_state\": 14,\n  \"extension_count\": 0,\n  \"can_cancel\": true,\n  \"can_extend\": true,\n  \"can_terminate\": false\n}</code></pre>\n<h3>8.4 — ten.tenant_terminated memory row (sev-1)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.tenant_terminated\",\n  \"severity\": \"sev-1\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"signer1_subject_id_hash16\": \"abc1234567890def\",\n  \"signer2_subject_id_hash16\": \"fed0987654321abc\",\n  \"reason_scrubbed\": \"[REDACTED-DURATION] dead-letter grace expired; no restoration request received; legal cleared.\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.5 — ten.read_only_write_attempted memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.read_only_write_attempted\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"attempted_path\": \"/v1/proj/issues\",\n  \"attempted_method\": \"POST\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Per-tenant read-only operation allowlist</strong> — slice 4 (e.g. \"allow time-tracking close-outs during terminating_a\").</li><li><strong>Automated dead-letter expiry job (terminating_b → terminated)</strong> — operator-manual at slice 1.</li><li><strong>Bulk offboarding (multiple tenants in one transaction)</strong> — out of scope.</li><li><strong>Webhook to tenant on state transitions</strong> — slice 4.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid FSM transition</td><td>trigger</td><td>400/500</td><td>Designed</td></tr><tr><td>terminated_at mutation</td><td>trigger</td><td>rejected</td><td>Designed</td></tr><tr><td>Self-co-sign on terminate</td><td>handler</td><td>400</td><td>Designed</td></tr><tr><td>Wrong signer role</td><td>handler</td><td>400</td><td>Designed</td></tr><tr><td>Confirmation string mismatch</td><td>handler</td><td>400</td><td>Designed</td></tr><tr><td>3rd extension</td><td>CHECK + handler</td><td>409</td><td>Designed</td></tr><tr><td>Extension out of [1,30]</td><td>handler</td><td>400</td><td>Designed</td></tr><tr><td>total_grace_days out of [30,180]</td><td>DB CHECK + handler</td><td>400</td><td>Designed</td></tr><tr><td>dead_letter_retention_days out of [30,90]</td><td>DB CHECK</td><td>INSERT/UPDATE fails</td><td>Designed</td></tr><tr><td>Concurrent scheduler runs</td><td>FOR UPDATE SKIP LOCKED</td><td>One wins per tenant</td><td>Designed</td></tr><tr><td>Dead-letter S3 write fail</td><td>error response</td><td>500 + sev-1</td><td>Retry; sev-1 alarm</td></tr><tr><td>Object-Lock COMPLIANCE not applied</td><td>reconciliation check</td><td>sev-1</td><td>Verify mode + reapply</td></tr><tr><td>memory audit fail mid-tx</td><td>rollback</td><td>500</td><td>memory_writer health</td></tr><tr><td>Read-only gate cache stale</td><td>60s TTL + invalidate</td><td>Brief delay</td><td>Designed</td></tr><tr><td>Non-CLO extension</td><td>role check</td><td>403</td><td>Designed</td></tr><tr><td>Non-root-admin initiate</td><td>role check</td><td>403</td><td>Designed</td></tr><tr><td>Cancel in terminating_b</td><td>state check</td><td>400 wrong_state</td><td>Designed</td></tr><tr><td>Cancel in terminated</td><td>state check</td><td>400 wrong_state</td><td>Designed</td></tr><tr><td>Force-advance by non-root-admin</td><td>role check</td><td>403</td><td>Designed</td></tr><tr><td>Scheduler ran but tenant already advanced (concurrent)</td><td>trigger 0 rows updated</td><td>No-op</td><td>Designed</td></tr><tr><td>append-only log UPDATE</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>Subject deleted while log refs</td><td>FK RESTRICT</td><td>DELETE auth.subjects fails</td><td>Soft-delete</td></tr><tr><td>Cross-tenant RLS access</td><td>USING</td><td>0 rows</td><td>Designed</td></tr><tr><td>Tenant in terminated reads</td><td>RLS allows</td><td>Read OK</td><td>Designed</td></tr><tr><td>Read-only-write rate &gt; 100/h</td><td>counter alarm</td><td>sev-3</td><td>Operator investigation</td></tr><tr><td>Dead-letter restore without S3 backup</td><td>dead_letter not found</td><td>500 dead_letter_unavailable</td><td>Manual recovery via Object-Lock copy</td></tr><tr><td>Concurrent finalize-termination + restore</td><td>row lock + trigger</td><td>Serial</td><td>Designed</td></tr><tr><td>Missing scheduled_advance_at</td><td>scheduler skip</td><td>None</td><td>Designed</td></tr><tr><td>Scheduler job timing drift</td><td>hourly cadence</td><td>Sub-1h transition</td><td>Designed</td></tr><tr><td>auth provisioner role not granted</td><td>service refuses to write</td><td>INSERT fails</td><td>Grant role</td></tr><tr><td>Tenant in terminating_b reads</td><td>RLS allows</td><td>Read OK</td><td>Designed</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Closed 4-state FSM</strong> — adding states is ADR.</li><li><strong>30+60 default = 90 days</strong> — matches industry SaaS norm; configurable [30,180].</li><li><strong>terminating_b → active forbidden</strong> — production data already wiped; restore goes via terminating_a.</li><li><strong>Hourly scheduled advance + idempotent SKIP LOCKED</strong> — no double-advance under concurrent runs.</li><li><strong>DOES NOT auto-terminate</strong> — dual-signoff required at every terminating_b → terminated.</li><li><strong>Read-only freeze at JWT issuance + handler check</strong> — defense in depth.</li><li><strong>423 status for tenant_read_only</strong> — WebDAV-style semantic.</li><li><strong>Max 2 extensions</strong> — bounded escape valve.</li><li><strong>Restore sev-1 audit</strong> — rare + consequential.</li><li><strong>Object-Lock COMPLIANCE</strong> — even root account can't delete dead letter.</li><li><strong>Per-tenant dead_letter_retention_days override</strong> — 30-90 days.</li><li><strong>Explicit confirmation strings</strong> — operator deliberate confirmation.</li><li><strong>TASK-TEN-202 fast-track via active → terminating_b</strong> — this task ships FSM; TEN-202 ships the triple-signoff handler.</li><li><strong>FSM at trigger + handler</strong> — direct SQL + handler both blocked.</li><li><strong>scheduled_advance_at pinned at entry</strong> — extensions UPDATE this.</li><li><strong>CLO for extension, root-admin for initiate</strong> — role separation.</li><li><strong>tenant-admin can cancel own</strong> — tenant's own decision.</li><li><strong>Read-only-write attempts emit + alarm</strong> — stuck-client visibility.</li><li><strong>Force-advance sev-1</strong> — operator override forensic-critical.</li><li><strong>8 memory audit kinds</strong> — selective operator queries.</li><li><strong>PII scrub reason</strong> — chain holds scrubbed.</li></ul>\n<hr>\n<p><em>End of TASK-TEN-104.</em></p>";
