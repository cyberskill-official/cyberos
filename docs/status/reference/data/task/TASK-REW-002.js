window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REW-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The REW service <strong>MUST</strong> ship parameter versioning at <code>services/rew/src/params/</code> with immutable snapshots + version-pinned payslip computation + monthly replay-equivalence CI, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>param_kind</code> against closed enum per DEC-2161.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0002</code>: ``<code>sql CREATE TABLE rew_param_versions ( version_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, kind TEXT NOT NULL CHECK (kind IN ('tax_bracket','si_rate','overtime_multiplier','allowance_cap','bonus_formula')), value_jsonb JSONB NOT NULL, effective_from DATE NOT NULL, effective_to DATE, source_law_reference TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), created_by UUID NOT NULL ); CREATE INDEX param_kind_effective_idx ON rew_param_versions(tenant_id, kind, effective_from DESC); ALTER TABLE rew_param_versions ENABLE ROW LEVEL SECURITY; CREATE POLICY param_rls ON rew_param_versions USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON rew_param_versions FROM cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> lookup at <code>loader.rs::get(tenant_id, kind, effective_at)</code> per DEC-2162:</li></ol>\n<ul><li>Returns version effective at the date.</li><li>Deterministic — same params → same result (critical for TASK-REW-005).</li></ul>\n<ol><li><strong>MUST</strong> run monthly replay-equivalence test via TASK-MCP-007 per DEC-2163:</li></ol>\n<ul><li>For last 12 months of payslips, re-run compute with original version_id.</li><li>Expect byte-identical output.</li><li>Failure → sev-1 alert + CI block.</li></ul>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/rew/params               (CFO; new version) GET  /v1/rew/params/{kind}?at=... (lookup at date) POST /v1/rew/replay-test/trigger  (CFO manual) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2164. PII per TASK-MEMORY-111: param values (mostly rates/brackets) ok in memory as they're public; member-specific compute hashes only.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from lookup → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior version per DEC-2162 (REVOKE UPDATE/DELETE).</li></ol>\n<ol><li><strong>MUST NOT</strong> use <code>now()</code> or random in compute path per DEC-2163.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why versioning (DEC-2160)?</strong> TASK-REW-005 payroll must reproduce historical payslips exactly; mutable params break replay.</p>\n<p><strong>Why replay test (DEC-2163)?</strong> Without automated check, drift creeps in unnoticed; monthly CI catches early.</p>\n<p><strong>Why immutable (DEC-2162)?</strong> Audit lineage requires unmutable history; corrections via new version with new effective_from.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample param lookup:</p>\n<pre><code class=\"language-json\">GET /v1/rew/params/tax_bracket?at=2026-06-01\n\n{\n  \"version_id\": \"uuid\",\n  \"kind\": \"tax_bracket\",\n  \"value_jsonb\": [\n    {\"min_vnd\": 0, \"max_vnd\": 5000000, \"rate\": 0.05},\n    {\"min_vnd\": 5000000, \"max_vnd\": 10000000, \"rate\": 0.10}\n  ],\n  \"effective_from\": \"2025-01-01\",\n  \"source_law_reference\": \"Decree 152/2020 Art. 7\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>param_kind enum cardinality 5</strong>. 2. <strong>Immutable rows (REVOKE)</strong>. 3. <strong>Lookup by effective_at</strong>. 4. <strong>Replay test monthly via cron</strong>. 5. <strong>100% match for prior periods</strong>. 6. <strong>Failure → sev-1 + CI block</strong>. 7. <strong>4 memory audit kinds emitted</strong>. 8. <strong>PII: param values public; compute hashed</strong>. 9. <strong>RLS denies cross-tenant</strong>. 10. <strong>CFO-only write</strong>. 11. <strong>Trace_id preserved</strong>. 12. <strong>Append-only via REVOKE</strong>. 13. <strong>JSONB schema validated per kind</strong>. 14. <strong>Source law reference recommended</strong>. 15. <strong>Effective_to NULL = current</strong>. 16. <strong>Index on (kind, effective_from)</strong>. 17. <strong>Lookup performance &lt; 5ms</strong>. 18. <strong>Annual refresh runbook</strong>. 19. <strong>Replay test produces diff report on failure</strong>. 20. <strong>Deterministic (no now/random)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn version_lookup_at_date() {\n    let ctx = TestContext::with_two_tax_bracket_versions(\"2024-01\", \"2025-01\").await;\n    let v_2024 = ctx.get_param(\"tax_bracket\", \"2024-06-01\").await;\n    let v_2025 = ctx.get_param(\"tax_bracket\", \"2025-06-01\").await;\n    assert_ne!(v_2024.version_id, v_2025.version_id);\n}\n\n#[tokio::test]\nasync fn immutability_enforced() {\n    let ctx = TestContext::with_param_version().await;\n    let r = ctx.try_update_param(ctx.version_id).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn replay_equivalence_100pct() {\n    let ctx = TestContext::with_12_months_payslips().await;\n    let result = ctx.run_replay_test().await;\n    assert_eq!(result.match_pct, dec!(100.0));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-REW-001. <strong>Downstream:</strong> TASK-REW-005 (payroll compute uses versioned params). <strong>Cross-module:</strong> TASK-HR-005 (compares versioning approach), TASK-MCP-007 (replay cron), TASK-MEMORY-111 (audit).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Replay test fail</td><td>sev-1 alert</td><td>CI block</td><td>investigate diff</td></tr><tr><td>Lookup at date with no version</td><td>error</td><td>404</td><td>seed gap</td></tr><tr><td>Two versions same date</td><td>UNIQUE on (kind, effective_from)</td><td>reject</td><td>use later date</td></tr><tr><td>JSONB schema invalid</td><td>validator</td><td>400</td><td>fix shape</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Non-CFO write</td><td>role check</td><td>403</td><td>request CFO</td></tr><tr><td>Decimal precision drift</td><td>rust_decimal</td><td>inherent</td><td>inherent</td></tr><tr><td>Cron skipped</td><td>catch-up</td><td>inherent</td><td>inherent</td></tr><tr><td>Test data corrupt</td><td>sev-1</td><td>manual review</td><td>data fix</td></tr><tr><td>Param value range invalid</td><td>range check</td><td>400</td><td>fix</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Replay test reruns TASK-REW-005 compute with member context at original period; compares to stored payslip blob hash.</li><li>§11.2 Cron via TASK-MCP-007 <code>kind: 'rew.replay_equivalence_test'</code>, monthly 1st at 02:00.</li><li>§11.3 Failure produces diff report uploaded to TASK-DOC-001 for CFO review.</li><li>§11.4 memory audit body: tenant_id, kind, version_id; param values ok (not PII).</li><li>§11.5 JSONB schema per kind documented in code constants + tested.</li></ul>\n<hr>\n<p><em>End of TASK-REW-002 spec.</em></p>";
