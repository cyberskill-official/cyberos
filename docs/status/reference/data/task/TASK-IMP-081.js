window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-081"]="<h2>§1</h2>\n<ol><li>The <code>changes</code> job (<code>.github/workflows/deploy.yml</code>) <strong>MUST</strong> gain a <code>web_src</code> paths-filter output covering <code>apps/web/**</code> and <code>VERSION</code>, alongside the existing <code>services</code> output - so a source or VERSION change to the web app is machine-detectable the same way a services change already is.</li><li>A new <code>rebuild-web</code> job <strong>MUST</strong> run only when <code>web_src == 'true'</code> (never unconditionally): <code>stamp-sw.mjs</code>'s cache id is <code>new Date().toISOString()</code>-derived, not content-hash-derived, so an ungated rebuild would produce a spurious diff - and a spurious bot commit - on every single deploy.yml trigger, including ones that never touched the web app.</li><li>When gated in, the job <strong>MUST</strong> rebuild <code>apps/web</code> (<code>npm ci &amp;&amp; npm run build</code>, identical to the command TASK-IMP-080 §1 already prints as the manual rebuild instruction) and, if <code>apps/console/web</code> differs from the committed copy, commit it back to <code>main</code> as <code>cyberos-bot &lt;bot@cyberskill.world&gt;</code> and push using the same <code>VERSION_BUMP_SSH_KEY</code> deploy key <code>version.yml</code> already uses to bypass the branch ruleset - no new secret, no new bot identity.</li><li>The commit message <strong>MUST</strong> carry <code>[skip ci]</code>: unlike <code>version.yml</code>'s bump commit (which must stay visible to tag-triggered workflows per TASK-IMP-071), this commit only refreshes a served artifact and has no reason to re-trigger deploy.yml a second time for itself.</li><li>A push blocked by the branch ruleset <strong>MUST</strong> degrade to a <code>GITHUB_STEP_SUMMARY</code> warning, exactly like <code>version.yml</code>'s existing degrade path - never a hard job failure, so a ruleset misconfiguration cannot turn a client-only push red.</li><li>The <code>deploy</code> job's <code>needs:</code> <strong>MUST</strong> include <code>rebuild-web</code>, so <code>deploy.sh</code>'s VPS-side <code>git pull</code> always runs after any bundle-refresh push from this job has landed on <code>main</code> - closing the ordering gap, not just the detection gap.</li><li><code>rebuild-web</code> <strong>MUST</strong> be <code>continue-on-error: true</code>. It is additive to the services roll, not a gate on it: <code>apps/web</code> and <code>services/</code> are independent deployables, and a real build failure in this job (not just a blocked push) must not trip <code>deploy</code>'s shared <code>needs:</code>/<code>failure()</code> check and block an otherwise-healthy services roll for an unrelated breakage. The job still shows red in the Actions UI on a genuine failure - only its blast radius is contained.</li></ol>\n<p><em>Lean profile: one workflow file, one new job, one extended paths-filter output, one extended <code>needs:</code> list; the commit-back mechanics are a direct reuse of <code>version.yml</code>'s already-proven pattern rather than a new design.</em></p>\n<h2>§2 — Why this shape (Option A over Option B)</h2>\n<p>TASK-IMP-080 §9 named two possible shapes: (A) a CI leg that rebuilds and commits the bundle back, or (B) retiring the tracked-output model entirely - gitignoring <code>apps/console/web</code> and shipping it via a new job mirroring the <code>docs</code> job's <code>ship.sh</code> rsync pattern (which already gitignores <code>apps/console/docs</code> for exactly this reason).</p>\n<p>Option B is the architecturally cleaner end state and is what §9's own phrasing leans toward (\"ships it like the docs job... retiring the tracked-output model\"). It was not chosen here because it is a <em>migration</em>, not just a new job: <code>apps/console/web</code> is currently a git-pull-tracked directory that Caddy serves via the directory bind <code>deploy/vps/deploy.sh</code> documents. The commit that adds it to <code>.gitignore</code> would, on the VPS's very next <code>git pull</code>, remove those tracked files from the working tree - before any new <code>ship.sh</code>-style step could repopulate them - opening a live-site outage window with no way to rehearse or time the sequencing against the real VPS from this session. TASK-IMP-080 §9 explicitly flagged the deploy.sh caddy-bind implications as something to \"decide together,\" not something to force through unrehearsed to close out a SHOULD-priority follow-up.</p>\n<p>Option A carries none of that risk: it is a strictly additive job that reuses <code>version.yml</code>'s already-battle-tested commit-back mechanics (same bot identity, same deploy key, same graceful-degrade-on-blocked-push behavior) and changes nothing about how <code>apps/console/web</code> is tracked or served. It converts TASK-IMP-080's fix from \"the next commit fails loudly if someone forgets to rebuild\" into \"nothing needs to remember\" - the same posture upgrade <code>check-version-sync.sh</code> already gave every other artifact, now closing the one gap that check can only detect, not prevent.</p>\n<p>Sequencing note: because <code>version.yml</code>'s bump commit and this job's own trigger both watch <code>VERSION</code>, a push that changes both <code>apps/web/**</code> source and lands moments before a version bump can produce two <code>rebuild-web</code> runs in quick succession - one against the pre-bump VERSION, one (triggered by the bump commit itself, since <code>VERSION</code> is in the filter) against the correct post-bump VERSION. This is self-correcting by construction: the second run's diff against the first run's (stale-version) commit is non-empty, so it supersedes it with the correct state. No new race class is introduced beyond the one <code>version.yml</code> and <code>deploy.yml</code> already share today (a bump commit already re-triggers a second <code>deploy</code> roll); this job's <code>[skip ci]</code> commit message additionally prevents it from re-triggering yet another full workflow run for itself.</p>\n<h2>§5 (run 2026-07-13)</h2>\n<ul><li><code>python3 -c \"import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))\"</code>: parses clean. Job graph confirmed: <code>changes -&gt; {gate -&gt; build-images, rebuild-web} -&gt; deploy</code>, <code>changes.outputs</code> carries both <code>services</code> and the new <code>web_src</code>, <code>deploy.needs</code> now <code>[changes, build-images, rebuild-web]</code>. PASS</li><li><code>actionlint v1.7.12</code> (downloaded fresh, network egress available in-session) against <code>.github/workflows/deploy.yml</code>: zero findings (expression syntax, job-dependency graph, and embedded-shellcheck all clean). Exit 0. PASS</li><li>Dry-run of the new commit-back logic in an isolated scratch git repo (not the real remote; no push target configured, so any push attempt necessarily fails - exercising the exact degrade path):</li><li>No-diff branch: <code>git diff --quiet -- apps/console/web</code> correctly short-circuits to the \"nothing to commit\" message and <code>exit 0</code> without staging or committing anything. PASS</li><li>Diff branch: staged + committed as <code>cyberos-bot &lt;bot@cyberskill.world&gt;</code> with message <code>chore(web): rebuild served bundle @ 1.0.0 [skip ci]</code> (exact format); <code>git status --short</code> clean immediately after. PASS</li><li>Push-failure branch: <code>git push origin HEAD:main</code> against a remote-less scratch repo fails as expected, and the <code>if/else</code> correctly falls through to the <code>::warning::</code> + <code>GITHUB_STEP_SUMMARY</code> degrade path under <code>set -euo pipefail</code> without killing the script - confirming this mirrors <code>version.yml</code>'s existing graceful-degrade behavior rather than failing the job red. PASS</li><li>Not exercised (cannot be, from this sandbox): an actual push through the real <code>VERSION_BUMP_SSH_KEY</code> deploy key against the live branch ruleset, and a real <code>npm ci &amp;&amp; npm run build</code> run inside this specific job's checkout. Both use commands already proven elsewhere in this repo's own CI (this exact rebuild command is what TASK-IMP-080 §1 ran and verified green this same day; the deploy-key push pattern is what <code>version.yml</code> already runs on every bump). Flagging this gap explicitly for the human review gate rather than claiming an end-to-end run that did not happen.</li></ul>\n<h3>Testing pass (2026-07-13, post gate-1 \"go\")</h3>\n<p>Stephen approved gate 1 (human review) in chat. Re-ran the machine-checkable verification set unchanged since review, to confirm nothing drifted between review and test:</p>\n<ul><li><code>actionlint v1.7.12</code> against <code>.github/workflows/deploy.yml</code>: exit 0, zero findings (unchanged from the review-time run). PASS</li><li><code>python3 -c \"import yaml; yaml.safe_load(...)\"</code> re-parse: clean. Re-asserted programmatically: <code>rebuild-web.continue-on-error == true</code>, <code>rebuild-web.needs == \"changes\"</code>, <code>deploy.needs == [changes, build-images, rebuild-web]</code>, <code>changes.outputs.web_src</code> present. All PASS.</li><li>No code changes were made between the reviewing-gate verdict and this testing pass, so the scratch-repo dry-run results recorded above (no-diff short-circuit, diff-branch commit-and-push, push-failure degrade-to-warning) still stand as-is; not re-run since nothing they exercise changed.</li><li>Same \"not exercised\" gap as review time still applies and is still unclosed from this sandbox: no real push through the live deploy key, no real <code>npm ci &amp;&amp; npm run build</code> inside this job's actual checkout. This task proceeds to the human acceptance gate with that gap disclosed, not silently.</li></ul>\n<h3>Acceptance (2026-07-13)</h3>\n<p>Stephen approved gate 2 (human acceptance) in chat: \"i approve\". TASK-IMP-081 lands as <code>done</code>, <code>shipped: 2026-07-13</code>. The two sandbox-inherent gaps flagged above (live-deploy-key push, real build inside the actual CI checkout) remain unexercised — they close naturally on the first real <code>deploy.yml</code> run this job is present for, not before.</p>\n<h2>§9</h2>\n<ul><li>If <code>apps/console/web</code>'s serving model is ever revisited (Option B above), this job becomes dead weight to remove, not a blocker: nothing about it constrains that future migration, and the paths-filter/<code>needs:</code> wiring this task adds is straightforward to delete in the same change that gitignores the directory.</li><li>Not covered: a Capacitor <code>cap sync</code> step. <code>apps/web/android/**</code> and <code>apps/web/ios/**</code> are release.yml's concern (mobile shell sync on tag), not deploy.yml's; this job only ever touches <code>apps/console/web</code>.</li></ul>\n<h2>§10</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Recovery</th></tr></thead><tbody><tr><td><code>apps/web/**</code> changes but the branch ruleset blocks the bot's push</td><td><code>GITHUB_STEP_SUMMARY</code> warning on the run, same as <code>version.yml</code>'s existing degrade path</td><td>add/rotate the deploy key in the ruleset bypass list (docs/deploy/RELEASE.md), or rebuild manually per TASK-IMP-080's printed command</td></tr><tr><td><code>apps/web</code> genuinely fails to build (real <code>tsc</code>/<code>vite</code> error on main, not a blocked push)</td><td><code>rebuild-web</code> job shows red in the Actions UI (<code>continue-on-error: true</code> does not hide the failure, only its blast radius)</td><td>fix the web-app source; <code>continue-on-error</code> deliberately keeps this from blocking the unrelated services roll in the same run</td></tr><tr><td>two <code>rebuild-web</code> runs race a version bump</td><td>second run's diff is non-empty against the first (pre-bump) commit and supersedes it</td><td>none needed - self-correcting</td></tr><tr><td><code>deploy</code> job's VPS <code>git pull</code> races ahead of this job's push</td><td><code>needs: [..., rebuild-web]</code> on the <code>deploy</code> job forces ordering</td><td>none needed by design</td></tr><tr><td>this job's own commit somehow lacks <code>[skip ci]</code> in a future edit</td><td>it would re-trigger a redundant (but idempotent, harmless) deploy.yml run - not a correctness bug, just wasted CI minutes</td><td>restore <code>[skip ci]</code> in the commit message</td></tr></tbody></table>\n<p><em>End of TASK-IMP-081.</em></p>";
