window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> load a YAML cost table at startup from <code>services/ai-gateway/config/cost_rates.yaml</code> and expose <code>cost_table::lookup(provider, model) -&gt; Option&lt;CostRate&gt;</code> for synchronous in-memory lookup by every consumer that needs to estimate or reconcile cost. The loader:</p>\n<ol><li><strong>MUST</strong> read the YAML at gateway startup; the loader runs <strong>before</strong> any chat request can be served (startup ordering: loader → cost-table init → policy init → router init → HTTP listener bound).</li><li><strong>MUST</strong> validate every entry per the schema in §3: <code>input_per_1k_usd &gt;= 0</code> and <code>output_per_1k_usd &gt;= 0</code> (using <code>rust_decimal::Decimal</code> for precision), provider name MUST be one of the closed <code>ProviderKind</code> enum values (<code>bedrock</code>, <code>anthropic</code>, <code>openai</code>, <code>vertex</code>, <code>bge</code>), model name MUST be a non-empty string of length ≤ 256.</li><li><strong>MUST</strong> aggregate ALL validation failures into a single <code>LoaderInitError::Schema { failures: Vec&lt;FileFailure&gt; }</code>. Reporting only the first failure forces multi-deploy iteration; aggregation lets the operator fix everything at once.</li><li><strong>MUST</strong> reject the entire load on any aggregate failure — the gateway exits 1 at startup, no partial loading. Negative rates or unknown provider names are configuration errors that fail loud.</li><li><strong>MUST</strong> support hot-reload via the <code>notify</code> crate with a 100ms debounce (avoid burst-edit thrash). On a successful reload, the new in-memory table replaces the old via <code>ArcSwap::store()</code> atomically.</li><li><strong>MUST</strong> preserve the last-known-good table on hot-reload validation failure. If a malformed YAML is saved, the loader emits <code>tracing::error!</code> and <code>ai_cost_table_reload_failures_total{reason}</code> increments; the in-memory cache stays at the prior valid state. Hot-reload failure is non-fatal at runtime (unlike startup failure).</li><li><strong>MUST</strong> expose <code>cost_table::lookup(provider, model) -&gt; Option&lt;CostRate&gt;</code> as the only public read interface. The lookup is synchronous, returns <code>None</code> on miss (callers translate to their own error types), and completes in &lt;1µs p95 on a 100-entry table.</li><li><strong>MUST</strong> support concurrent reads from many tokio tasks without contention via the <code>ArcSwap&lt;HashMap&lt;(ProviderKind, String), CostRate&gt;&gt;</code> pattern. Read-path is lock-free; write-path (hot-reload) does a single atomic swap.</li><li><strong>MUST</strong> keep cost rates in <strong>USD only</strong>. Multi-currency conversion happens at invoice time (TASK-INV-002), not at cost-ledger time. Mixing currencies here would force every precheck/reconcile comparison to FX-convert, which is non-deterministic across timezones and breaks reproducibility.</li><li><strong>MUST</strong> use <code>Decimal</code> with 6 decimal places of precision (<code>NUMERIC(12,6)</code> semantically). Cost rates like <code>gpt-4o-mini</code>'s <code>$0.00015/1k</code> need sub-cent precision; floating point would lose accuracy on aggregation.</li><li><strong>MUST</strong> maintain a <code>last_updated</code> field at YAML top level (human-edited at every rate change). A CI lint MUST warn on entries older than 90 days; the warning is non-blocking but visible in PR reviews.</li><li><strong>MUST</strong> support a per-model <code>is_embedding: true</code> flag (in <code>CostRate</code>) where <code>output_per_1k_usd == 0.0</code> — embeddings have no output tokens by definition. This is informational metadata that downstream callers (TASK-AI-002 reconcile) can use to short-circuit output cost calculation.</li><li><strong>MUST</strong> emit OTel metrics: <code>ai_cost_table_lookups_total{provider,outcome}</code> (counter; outcome ∈ hit/miss), <code>ai_cost_table_entries_total</code> (gauge; current entry count), <code>ai_cost_table_reload_failures_total{reason}</code> (counter; reason ∈ parse_error/validation_error/io_error), <code>ai_cost_table_loaded_at_ts</code> (gauge; UNIX timestamp of last successful load), <code>ai_cost_table_lookup_latency_ns</code> (histogram).</li><li><strong>MUST</strong> expose <code>cost_table::loaded_at() -&gt; Option&lt;DateTime&lt;Utc&gt;&gt;</code> for the operator CLI (TASK-AI-021 <code>models pricing</code> subcommand) to show when the table was last loaded.</li><li><strong>MUST NOT</strong> persist cost rates anywhere except in-memory (no Postgres write at slice 2). The TEN module's per-tenant override table (TASK-TEN-006, P2) introduces DB-backed overrides; the YAML stays canonical and is read first.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why YAML and not a database?</strong> Same reasoning as TASK-AI-005's policy loader: cost rates change roughly weekly (a new model launches, Bedrock pricing updates, OpenAI deprecates <code>gpt-4-turbo</code>). Git-tracked YAML gives every rate change a PR + reviewer + diff. The audit trail of \"who lowered the chat.smart rate on 2026-04-15?\" is built into git blame, not in a separate audit log. The TEN module (TASK-TEN-006, deferred to P2) eventually adds a Postgres-backed override for per-tenant negotiated rates (e.g., enterprise tenants who get 10% off), but the YAML stays as the canonical bootstrap source and the fallback when TEN's DB is unreachable.</p>\n<p><strong>Why is <code>lookup()</code> synchronous (no async)?</strong> This is on the precheck hot path of every chat call. Precheck's budget is 50ms p95. The cost-table lookup is called twice in the hot path (once in <code>alias::resolve</code> for validation, once in <code>cost_ledger::precheck</code> for the estimate). Adding async I/O would add 2 × ~5ms = 10ms — a 20% chunk of the precheck budget for what is fundamentally a HashMap lookup. Keeping it synchronous and in-memory makes the lookup \"free\" (sub-microsecond).</p>\n<p><strong>Why aggregate validation failures?</strong> Same operator-experience reason as TASK-AI-005 §1 #11. An operator misconfiguring 3 entries (say, three new models added with copy-paste errors) gets all 3 failures in one error message. Without aggregation: fix one, redeploy, find the next, redeploy, find the third. Three deploys to find three errors. With aggregation: one deploy, all three visible.</p>\n<p><strong>Why USD only?</strong> Provider invoices arrive in USD. AI providers (Bedrock, Anthropic, OpenAI) all publish prices in USD. Tenant billing is in tenant currency (VND for Vietnamese tenants, EUR for EU, SGD for Singapore, etc.) — that conversion happens at invoice issue time in TASK-INV-002, using SBV's daily FX rate published at 16:00 ICT. Mixing currencies at the cost-ledger layer would force every comparison (\"is this call over the tenant's monthly cap?\") to FX-convert, which is non-deterministic across timezones AND breaks reproducibility (the same audit row could yield different \"in cap\" verdicts on different days if FX moved).</p>\n<p><strong>Why <code>Decimal</code> not <code>f64</code>?</strong> Floating point is wrong for money. <code>gpt-4o-mini</code>'s rate is <code>$0.00015/1k</code>. A call that uses 1000 prompt tokens + 500 completion tokens costs <code>1.0 * 0.00015 + 0.5 * 0.0006 = 0.00045</code>. In <code>f64</code>, you get <code>0.00044999999999999996</code> due to representation. In <code>Decimal</code>, you get exactly <code>0.000450</code>. Aggregating thousands of these for a month, the <code>f64</code> error compounds; the <code>Decimal</code> is exact. This is a \"decimal everywhere\" project — cost ledger uses <code>Decimal</code>, TASK-AI-001's holds use <code>Decimal</code>, TASK-AI-002's reconciliation uses <code>Decimal</code>. Consistency.</p>\n<p><strong>Why hot-reload at all?</strong> Two reasons. (1) Provider price changes happen mid-month (Anthropic dropped <code>claude-3-haiku</code> prices in March 2026; we want that effective immediately, not at next gateway restart). (2) New model launches: Bedrock adds <code>claude-3.5-haiku-20251022</code> and the cost-rate YAML edit is the only blocker between \"deploy and serve\" — without hot-reload, we'd need a full gateway restart for every new model. With hot-reload, it's a <code>git push</code> and ~100ms later the model is bookable.</p>\n<p><strong>Why 100ms debounce on the file-watch?</strong> YAML edits via vim/vscode often generate multiple file-modification events (atomic write produces a rename + a write). Without debounce, the loader would try to parse three times in 30ms — twice on partial files (parse errors) and once on the final file. With 100ms debounce, the loader waits until the editor settles, then parses once on the final state.</p>\n<p><strong>Why preserve last-known-good on hot-reload failure?</strong> Failing hard on hot-reload failure would mean a single bad YAML edit (a stray colon, a typo on a model name) takes the gateway offline. That's an unacceptable trade — the prior cost table was working fine; running with it for a few minutes while the operator fixes the YAML is much safer than failing requests with <code>503 NO_COST_TABLE</code>. The trade-off is asymmetric: startup failure is loud (operator sees the deploy fail immediately and fixes); runtime failure is silent-but-recoverable (operator sees the OBS alarm and fixes within minutes).</p>\n<p><strong>Why expose <code>is_embedding</code> metadata?</strong> TASK-AI-002's reconcile path computes <code>actual_usd = prompt_tokens × input_rate + completion_tokens × output_rate</code>. For embeddings, <code>completion_tokens</code> is always 0 by definition (embeddings produce vectors, not tokens). Without <code>is_embedding</code>, TASK-AI-002 has no way to validate that a non-zero <code>completion_tokens</code> value on an embedding response is a bug. With it, TASK-AI-002 can assert <code>is_embedding ⇒ completion_tokens == 0</code> and surface the bug at reconcile time.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<h3>Public function signatures</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cost_table.rs\n\n/// Synchronous lookup against the in-memory cost table.\n/// Returns None on miss; never panics; lock-free read via ArcSwap.\npub fn lookup(provider: &amp;ProviderKind, model: &amp;str) -&gt; Option&lt;CostRate&gt;;\n\n/// Returns the UNIX timestamp of the last successful load (or None if never loaded).\n/// Used by TASK-AI-021 operator CLI.\npub fn loaded_at() -&gt; Option&lt;DateTime&lt;Utc&gt;&gt;;\n\n/// Returns the current entry count (gauge value).\npub fn entry_count() -&gt; usize;\n\n/// Initialise the cost table at gateway startup.\n/// MUST be called before the HTTP listener is bound.\npub async fn init_cost_table(config_path: &amp;Path) -&gt; Result&lt;CostTableHandle, LoaderInitError&gt;;</code></pre>\n<h3>Types</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cost_table/schema.rs\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub struct CostRate {\n    pub input_per_1k_usd:  Decimal,    // NUMERIC(12,6) precision — sub-cent matters\n    pub output_per_1k_usd: Decimal,    // 0.0 for embeddings\n    pub is_embedding: bool,            // §1 #12 — embeddings produce no output tokens\n}\n\n#[derive(Debug)]\npub enum LoaderInitError {\n    /// One or more YAML files failed validation; ALL failures reported in one error.\n    Schema { failures: Vec&lt;FileFailure&gt; },\n    /// IO error reading the file (missing, permission denied, etc.).\n    IoError { path: PathBuf, source: std::io::Error },\n    /// Loader already initialised (programmer error — init called twice).\n    AlreadyInitialised,\n    /// notify watcher setup failed.\n    WatcherSetup(notify::Error),\n}\n\n#[derive(Debug, Clone)]\npub struct FileFailure {\n    pub path: PathBuf,\n    pub model: Option&lt;String&gt;,        // None if the failure is structural (not per-model)\n    pub provider: Option&lt;String&gt;,\n    pub errors: Vec&lt;String&gt;,           // human-readable error messages\n}\n\n/// Opaque handle that keeps the watcher alive. Drop = stop watching.\npub struct CostTableHandle {\n    _watcher: notify::RecommendedWatcher,\n}\n\n/// Internal: shape of the YAML before validation.\n#[derive(Debug, Deserialize)]\nstruct RawCostTable {\n    version: u32,\n    last_updated: NaiveDate,\n    #[serde(default)]\n    source: String,\n    rates: HashMap&lt;String, HashMap&lt;String, RawCostRate&gt;&gt;,   // provider → model → rate\n}\n\n#[derive(Debug, Deserialize)]\nstruct RawCostRate {\n    input_per_1k_usd: Decimal,\n    output_per_1k_usd: Decimal,\n    #[serde(default)]\n    is_embedding: bool,\n}</code></pre>\n<h3>YAML schema (full, with all slice-2 seed entries)</h3>\n<pre><code class=\"language-yaml\"># services/ai-gateway/config/cost_rates.yaml\nversion: 1\nlast_updated: 2026-05-15\nsource: |\n  anthropic.com/pricing (Anthropic native)\n  aws.amazon.com/bedrock/pricing (AWS Bedrock)\n  openai.com/pricing (OpenAI)\n\nrates:\n  bedrock:\n    anthropic.claude-3-5-sonnet-20241022-v2:0:\n      input_per_1k_usd:  0.003\n      output_per_1k_usd: 0.015\n    anthropic.claude-3-haiku-20240307-v1:0:\n      input_per_1k_usd:  0.00025\n      output_per_1k_usd: 0.00125\n    amazon.titan-embed-text-v2:0:\n      input_per_1k_usd:  0.00002\n      output_per_1k_usd: 0.0\n      is_embedding: true\n\n  anthropic:\n    claude-3-5-sonnet-20241022:\n      input_per_1k_usd:  0.003\n      output_per_1k_usd: 0.015\n    claude-3-haiku-20240307:\n      input_per_1k_usd:  0.00025\n      output_per_1k_usd: 0.00125\n\n  openai:\n    gpt-4o:\n      input_per_1k_usd:  0.0025\n      output_per_1k_usd: 0.01\n    gpt-4o-mini:\n      input_per_1k_usd:  0.00015\n      output_per_1k_usd: 0.0006\n    text-embedding-3-small:\n      input_per_1k_usd:  0.00002\n      output_per_1k_usd: 0.0\n      is_embedding: true\n\n  bge:                                          # self-hosted (TASK-AI-019) — cost = 0\n    bge-m3:\n      input_per_1k_usd:  0.0\n      output_per_1k_usd: 0.0\n      is_embedding: true\n    bge-reranker-v2-m3:\n      input_per_1k_usd:  0.0\n      output_per_1k_usd: 0.0\n      is_embedding: true                        # rerank is \"embedding-class\" for accounting</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>Happy lookup</strong> — Given <code>cost_rates.yaml</code> loaded with the §3 seed, <code>cost_table::lookup(&amp;ProviderKind::Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\")</code> MUST return <code>Some(CostRate { input_per_1k_usd: dec!(0.003), output_per_1k_usd: dec!(0.015), is_embedding: false })</code>.</li><li><strong>Miss returns None</strong> — <code>lookup(&amp;ProviderKind::Anthropic, \"nonexistent-model\")</code> MUST return <code>None</code>. No panic; no error log.</li><li><strong>Embedding flag set</strong> — <code>lookup(&amp;ProviderKind::Bedrock, \"amazon.titan-embed-text-v2:0\")</code> MUST return <code>CostRate { is_embedding: true, output_per_1k_usd: dec!(0.0), .. }</code>.</li><li><strong>Self-hosted BGE rates are 0</strong> — <code>lookup(&amp;ProviderKind::Bge, \"bge-m3\")</code> MUST return <code>CostRate { input_per_1k_usd: dec!(0.0), output_per_1k_usd: dec!(0.0), is_embedding: true }</code>.</li><li><strong>Aggregate failures on init</strong> — Given a YAML with 3 entries (one negative rate, one missing <code>output_per_1k_usd</code>, one with unknown provider name), <code>init_cost_table</code> MUST return <code>Err(LoaderInitError::Schema { failures })</code> with <code>failures.len() == 3</code>. Each <code>FileFailure</code> MUST identify the model + provider + specific error message.</li><li><strong>Negative rate rejected</strong> — <code>input_per_1k_usd: -0.01</code> rejected with <code>FileFailure { errors: vec![\"rate must be non-negative, got -0.01\"] }</code>.</li><li><strong>Lookup latency &lt; 1µs</strong> — Benchmark 100,000 lookups; total elapsed &lt; 100ms (mean &lt; 1µs). Measured via <code>cargo bench</code> Criterion benchmark.</li><li><strong>Concurrent reads zero contention</strong> — 1000 tokio tasks × 1000 lookups in &lt;500ms total (mean 0.5µs per lookup under contention).</li><li><strong>Hot reload swaps atomically</strong> — Modify <code>cost_rates.yaml</code> to add <code>claude-99-future</code>. Within 500ms (100ms debounce + ~50ms validation + swap), <code>lookup(&amp;Anthropic, \"claude-99-future\")</code> MUST return <code>Some(...)</code>. Concurrent in-flight lookups see either old or new (no torn read).</li><li><strong>Hot-reload failure preserves table</strong> — Modify YAML to invalid (e.g., <code>input_per_1k_usd: not-a-number</code>). Cache MUST remain at last-valid state. <code>ai_cost_table_reload_failures_total{reason=\"parse_error\"}</code> MUST increment. <code>tracing::error!</code> event emitted naming the file + line + error.</li><li><strong>Determinism property test</strong> — <code>proptest!</code> runs 500 random YAML fixtures (validity-mixed); each fixture's <code>init</code> MUST produce a stable <code>Result</code> across 10 reads (verified via <code>Debug</code> formatting equality).</li><li><strong><code>loaded_at</code> populated after init</strong> — After successful <code>init_cost_table</code>, <code>cost_table::loaded_at()</code> MUST return <code>Some(&lt;DateTime within last 1s of init call&gt;)</code>.</li><li><strong>OTel metrics emit correctly</strong> — After 100 lookups (90 hits + 10 misses), <code>ai_cost_table_lookups_total{outcome=\"hit\"}</code> MUST equal 90 and <code>{outcome=\"miss\"}</code> equal 10. Latency histogram MUST have 100 samples.</li><li><strong>Last-updated CI lint</strong> — A CI job (separate from this task's tests) MUST emit a warning if <code>last_updated</code> is older than 90 days. Tested via fixture with <code>last_updated: 2025-01-01</code> → lint emits warning.</li><li><strong>is_embedding consistency</strong> — A YAML with <code>is_embedding: true, output_per_1k_usd: 0.5</code> MUST be rejected at init with a <code>FileFailure { errors: vec![\"is_embedding: true requires output_per_1k_usd == 0.0, got 0.5\"] }</code>. The loader enforces §1 #12 invariant; downstream code can trust it.</li><li><strong>Failure list deterministic order (task-audit skill §3.9 rule 27)</strong> — Two <code>init</code> calls with the same malformed YAML MUST produce byte-identical <code>Vec&lt;FileFailure&gt;</code> sorted by <code>(path, model)</code>. The §6 skeleton's <code>validate_and_flatten</code> MUST end with <code>failures.sort_by(|a, b| a.path.cmp(&amp;b.path).then(a.model.cmp(&amp;b.model)))</code> so operator diffing across runs is reliable.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<p><strong>Integration test:</strong> <code>services/ai-gateway/tests/cost_table_test.rs</code></p>\n<pre><code class=\"language-rust\">use cyberos_ai_gateway::cost_table::{self, init_cost_table, ProviderKind, LoaderInitError};\nuse rust_decimal_macros::dec;\nuse std::path::PathBuf;\nuse std::time::Instant;\n\n#[tokio::test]\nasync fn lookup_bedrock_claude_sonnet() {\n    let _handle = init_cost_table(&amp;PathBuf::from(\"tests/fixtures/cost_table/valid_rates.yaml\"))\n        .await.unwrap();\n    let rate = cost_table::lookup(&amp;ProviderKind::Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\").unwrap();\n    assert_eq!(rate.input_per_1k_usd, dec!(0.003));\n    assert_eq!(rate.output_per_1k_usd, dec!(0.015));\n    assert!(!rate.is_embedding);\n}\n\n#[tokio::test]\nasync fn miss_returns_none() {\n    let _handle = init_cost_table(&amp;fixture(\"valid_rates.yaml\")).await.unwrap();\n    assert!(cost_table::lookup(&amp;ProviderKind::Anthropic, \"nonexistent\").is_none());\n}\n\n#[tokio::test]\nasync fn embedding_flag_set_for_titan() {\n    let _handle = init_cost_table(&amp;fixture(\"valid_rates.yaml\")).await.unwrap();\n    let rate = cost_table::lookup(&amp;ProviderKind::Bedrock, \"amazon.titan-embed-text-v2:0\").unwrap();\n    assert!(rate.is_embedding);\n    assert_eq!(rate.output_per_1k_usd, dec!(0.0));\n}\n\n#[tokio::test]\nasync fn negative_rate_rejected_at_init() {\n    let err = init_cost_table(&amp;fixture(\"negative_rate.yaml\")).await.unwrap_err();\n    match err {\n        LoaderInitError::Schema { failures } =&gt; {\n            assert!(failures.iter().any(|f| f.errors.iter().any(|e| e.contains(\"non-negative\"))));\n        }\n        _ =&gt; panic!(\"expected Schema error\"),\n    }\n}\n\n#[tokio::test]\nasync fn aggregate_three_failures() {\n    let err = init_cost_table(&amp;fixture(\"three_failures.yaml\")).await.unwrap_err();\n    match err {\n        LoaderInitError::Schema { failures } =&gt; assert_eq!(failures.len(), 3),\n        _ =&gt; panic!(\"expected aggregated Schema error\"),\n    }\n}\n\n#[tokio::test]\nasync fn hot_reload_picks_up_new_model() {\n    let tmp = TempDir::new().unwrap();\n    let path = tmp.path().join(\"cost_rates.yaml\");\n    std::fs::copy(\"tests/fixtures/cost_table/valid_rates.yaml\", &amp;path).unwrap();\n    let _handle = init_cost_table(&amp;path).await.unwrap();\n\n    // Modify YAML to add new model\n    std::fs::write(&amp;path, /* yaml with claude-99-future added */).unwrap();\n\n    let start = Instant::now();\n    loop {\n        if let Some(rate) = cost_table::lookup(&amp;ProviderKind::Anthropic, \"claude-99-future\") {\n            assert_eq!(rate.input_per_1k_usd, dec!(0.001));\n            assert!(start.elapsed() &lt; std::time::Duration::from_millis(500));\n            return;\n        }\n        if start.elapsed() &gt; std::time::Duration::from_millis(500) {\n            panic!(\"hot reload did not pick up new model within 500ms\");\n        }\n        tokio::time::sleep(std::time::Duration::from_millis(20)).await;\n    }\n}\n\n#[tokio::test]\nasync fn hot_reload_invalid_preserves_cache() {\n    let tmp = TempDir::new().unwrap();\n    let path = tmp.path().join(\"cost_rates.yaml\");\n    std::fs::copy(\"tests/fixtures/cost_table/valid_rates.yaml\", &amp;path).unwrap();\n    let _handle = init_cost_table(&amp;path).await.unwrap();\n\n    // Corrupt the YAML\n    std::fs::write(&amp;path, \"not: valid: yaml: at: all\").unwrap();\n    tokio::time::sleep(std::time::Duration::from_millis(200)).await;\n\n    // Cache should still serve the original valid rate\n    let rate = cost_table::lookup(&amp;ProviderKind::Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\");\n    assert!(rate.is_some());\n}\n\n// AC #11: determinism property test\nproptest! {\n    #[test]\n    fn determinism_across_reads(yaml in any_valid_cost_yaml()) {\n        let tmp = TempDir::new().unwrap();\n        let path = tmp.path().join(\"cost_rates.yaml\");\n        std::fs::write(&amp;path, &amp;yaml).unwrap();\n        let _handle = futures::executor::block_on(init_cost_table(&amp;path)).unwrap();\n        let snapshots: Vec&lt;_&gt; = (0..10).map(|_| {\n            cost_table::lookup(&amp;ProviderKind::Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\")\n        }).collect();\n        prop_assert!(snapshots.windows(2).all(|w| w[0] == w[1]));\n    }\n}\n\n// AC #15: is_embedding consistency\n#[tokio::test]\nasync fn is_embedding_with_nonzero_output_rejected() {\n    let tmp = TempDir::new().unwrap();\n    let path = tmp.path().join(\"cost_rates.yaml\");\n    std::fs::write(&amp;path, r#\"\nversion: 1\nlast_updated: 2026-05-15\nrates:\n  bedrock:\n    test-model:\n      input_per_1k_usd: 0.001\n      output_per_1k_usd: 0.5\n      is_embedding: true\n\"#).unwrap();\n    let err = init_cost_table(&amp;path).await.unwrap_err();\n    match err {\n        LoaderInitError::Schema { failures } =&gt; {\n            assert!(failures.iter().any(|f| f.errors.iter().any(|e| e.contains(\"is_embedding: true requires\"))));\n        }\n        _ =&gt; panic!(\"expected Schema error\"),\n    }\n}\n\n#[tokio::test]\nasync fn concurrent_1000_tasks_no_contention() {\n    let _handle = init_cost_table(&amp;fixture(\"valid_rates.yaml\")).await.unwrap();\n    let start = Instant::now();\n    let handles: Vec&lt;_&gt; = (0..1000).map(|_| {\n        tokio::spawn(async {\n            for _ in 0..1000 {\n                let _ = cost_table::lookup(&amp;ProviderKind::Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\");\n            }\n        })\n    }).collect();\n    futures::future::join_all(handles).await;\n    assert!(start.elapsed() &lt; std::time::Duration::from_millis(500));\n}</code></pre>\n<p><strong>Benchmark:</strong> <code>services/ai-gateway/benches/cost_table_lookup_bench.rs</code></p>\n<pre><code class=\"language-rust\">use criterion::{black_box, criterion_group, criterion_main, Criterion};\nuse cyberos_ai_gateway::cost_table::{self, ProviderKind};\n\nfn bench_lookup_hit(c: &amp;mut Criterion) {\n    // setup: init_cost_table once in test main\n    c.bench_function(\"cost_table::lookup hit\", |b| {\n        b.iter(|| {\n            cost_table::lookup(\n                black_box(&amp;ProviderKind::Bedrock),\n                black_box(\"anthropic.claude-3-5-sonnet-20241022-v2:0\"),\n            )\n        });\n    });\n}\n\nfn bench_lookup_miss(c: &amp;mut Criterion) {\n    c.bench_function(\"cost_table::lookup miss\", |b| {\n        b.iter(|| cost_table::lookup(black_box(&amp;ProviderKind::Bedrock), black_box(\"nonexistent\")));\n    });\n}\n\ncriterion_group!(benches, bench_lookup_hit, bench_lookup_miss);\ncriterion_main!(benches);</code></pre>\n<p>Run via:</p>\n<pre><code class=\"language-bash\">cd /Users/stephencheng/Projects/CyberSkill/cyberos\ncargo test -p cyberos-ai-gateway cost_table\ncargo bench -p cyberos-ai-gateway cost_table_lookup_bench</code></pre>\n<p>CI gate: tests run on every PR touching <code>services/ai-gateway/src/cost_table/**</code> or <code>services/ai-gateway/config/cost_rates.yaml</code>. Benchmark regression &gt; 20% fails the PR.</p>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cost_table.rs\n\nuse arc_swap::ArcSwap;\nuse once_cell::sync::OnceCell;\nuse std::collections::HashMap;\nuse std::path::{Path, PathBuf};\nuse std::sync::Arc;\nuse std::time::Instant;\nuse chrono::{DateTime, Utc, NaiveDate};\nuse rust_decimal::Decimal;\nuse rust_decimal_macros::dec;\nuse tokio::sync::mpsc;\nuse notify::{RecommendedWatcher, Watcher, RecursiveMode, Event, EventKind};\n\nuse crate::cost_table::schema::{CostRate, RawCostTable, FileFailure, LoaderInitError, CostTableHandle};\nuse crate::policy::ProviderKind;\n\nstatic TABLE: OnceCell&lt;ArcSwap&lt;HashMap&lt;(ProviderKind, String), CostRate&gt;&gt;&gt; = OnceCell::new();\nstatic LOADED_AT: OnceCell&lt;ArcSwap&lt;Option&lt;DateTime&lt;Utc&gt;&gt;&gt;&gt; = OnceCell::new();\n\nconst DEBOUNCE_MS: u64 = 100;\n\nmod metrics {\n    use once_cell::sync::Lazy;\n    use prometheus::{register_counter_vec, register_histogram, register_int_gauge, CounterVec, Histogram, IntGauge};\n\n    pub static LOOKUPS: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_cost_table_lookups_total\",\n        \"Cost-table lookups by provider and outcome\",\n        &amp;[\"provider\", \"outcome\"]\n    ).unwrap());\n\n    pub static RELOAD_FAILURES: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_cost_table_reload_failures_total\",\n        \"Cost-table reload failures by reason\",\n        &amp;[\"reason\"]\n    ).unwrap());\n\n    pub static ENTRY_COUNT: Lazy&lt;IntGauge&gt; = Lazy::new(|| register_int_gauge!(\n        \"ai_cost_table_entries_total\",\n        \"Current count of (provider, model) entries\"\n    ).unwrap());\n\n    pub static LOADED_AT_TS: Lazy&lt;IntGauge&gt; = Lazy::new(|| register_int_gauge!(\n        \"ai_cost_table_loaded_at_ts\",\n        \"UNIX timestamp of last successful load\"\n    ).unwrap());\n\n    pub static LOOKUP_LATENCY: Lazy&lt;Histogram&gt; = Lazy::new(|| {\n        prometheus::register_histogram!(\n            \"ai_cost_table_lookup_latency_ns\",\n            \"Cost-table lookup latency in nanoseconds\",\n            vec![100.0, 500.0, 1_000.0, 5_000.0, 10_000.0]\n        ).unwrap()\n    });\n}\n\npub fn lookup(provider: &amp;ProviderKind, model: &amp;str) -&gt; Option&lt;CostRate&gt; {\n    let started = Instant::now();\n    let result = TABLE.get()\n        .and_then(|s| s.load().get(&amp;(*provider, model.to_string())).copied());\n    let outcome = if result.is_some() { \"hit\" } else { \"miss\" };\n    // ISS-003 fix: use explicit stable string conversion, NOT format!(\"{:?}\", ...) which\n    // ties OBS dashboards to Rust enum variant names. ProviderKind::as_metric_label()\n    // is defined in policy/schema.rs and is rename-safe.\n    metrics::LOOKUPS.with_label_values(&amp;[provider.as_metric_label(), outcome]).inc();\n    metrics::LOOKUP_LATENCY.observe(started.elapsed().as_nanos() as f64);\n    result\n}\n\npub fn loaded_at() -&gt; Option&lt;DateTime&lt;Utc&gt;&gt; {\n    LOADED_AT.get().and_then(|s| **s.load())\n}\n\npub fn entry_count() -&gt; usize {\n    TABLE.get().map(|s| s.load().len()).unwrap_or(0)\n}\n\npub async fn init_cost_table(config_path: &amp;Path) -&gt; Result&lt;CostTableHandle, LoaderInitError&gt; {\n    let table = load_and_validate(config_path).await?;\n    let entry_count = table.len();\n    TABLE.set(ArcSwap::from_pointee(table)).map_err(|_| LoaderInitError::AlreadyInitialised)?;\n    LOADED_AT.set(ArcSwap::from_pointee(Some(Utc::now()))).map_err(|_| LoaderInitError::AlreadyInitialised)?;\n    metrics::ENTRY_COUNT.set(entry_count as i64);\n    metrics::LOADED_AT_TS.set(Utc::now().timestamp());\n\n    let watcher = spawn_watcher(config_path).await?;\n    Ok(CostTableHandle { _watcher: watcher })\n}\n\nasync fn load_and_validate(path: &amp;Path) -&gt; Result&lt;HashMap&lt;(ProviderKind, String), CostRate&gt;, LoaderInitError&gt; {\n    let yaml = std::fs::read_to_string(path).map_err(|source| LoaderInitError::IoError {\n        path: path.to_path_buf(), source,\n    })?;\n    let raw: RawCostTable = serde_yaml::from_str(&amp;yaml).map_err(|e| LoaderInitError::Schema {\n        failures: vec![FileFailure {\n            path: path.to_path_buf(), model: None, provider: None,\n            errors: vec![format!(\"yaml parse: {}\", e)],\n        }],\n    })?;\n    validate_and_flatten(raw, path)\n}\n\nfn validate_and_flatten(raw: RawCostTable, path: &amp;Path) -&gt; Result&lt;HashMap&lt;(ProviderKind, String), CostRate&gt;, LoaderInitError&gt; {\n    let mut failures: Vec&lt;FileFailure&gt; = Vec::new();\n    let mut out = HashMap::new();\n    for (provider_str, models) in raw.rates {\n        let kind = match parse_provider(&amp;provider_str) {\n            Ok(k) =&gt; k,\n            Err(e) =&gt; {\n                failures.push(FileFailure {\n                    path: path.to_path_buf(),\n                    provider: Some(provider_str.clone()),\n                    model: None,\n                    errors: vec![e],\n                });\n                continue;\n            }\n        };\n        for (model, rate) in models {\n            let mut model_errors: Vec&lt;String&gt; = Vec::new();\n            if rate.input_per_1k_usd &lt; dec!(0) {\n                model_errors.push(format!(\"input_per_1k_usd must be non-negative, got {}\", rate.input_per_1k_usd));\n            }\n            if rate.output_per_1k_usd &lt; dec!(0) {\n                model_errors.push(format!(\"output_per_1k_usd must be non-negative, got {}\", rate.output_per_1k_usd));\n            }\n            if model.is_empty() || model.len() &gt; 256 {\n                model_errors.push(format!(\"model name length must be 1..=256, got {}\", model.len()));\n            }\n            // ISS-002 fix: §1 #12 invariant — is_embedding ⇒ output_per_1k_usd == 0.0\n            if rate.is_embedding &amp;&amp; rate.output_per_1k_usd &gt; dec!(0) {\n                model_errors.push(format!(\n                    \"is_embedding: true requires output_per_1k_usd == 0.0, got {}\",\n                    rate.output_per_1k_usd\n                ));\n            }\n            if !model_errors.is_empty() {\n                failures.push(FileFailure {\n                    path: path.to_path_buf(),\n                    provider: Some(provider_str.clone()),\n                    model: Some(model.clone()),\n                    errors: model_errors,\n                });\n                continue;\n            }\n            out.insert((kind, model), CostRate {\n                input_per_1k_usd: rate.input_per_1k_usd,\n                output_per_1k_usd: rate.output_per_1k_usd,\n                is_embedding: rate.is_embedding,\n            });\n        }\n    }\n    if !failures.is_empty() {\n        return Err(LoaderInitError::Schema { failures });\n    }\n    Ok(out)\n}\n\nfn parse_provider(s: &amp;str) -&gt; Result&lt;ProviderKind, String&gt; {\n    match s {\n        \"bedrock\" =&gt; Ok(ProviderKind::Bedrock),\n        \"anthropic\" =&gt; Ok(ProviderKind::Anthropic),\n        \"openai\" =&gt; Ok(ProviderKind::OpenAI),\n        \"vertex\" =&gt; Ok(ProviderKind::Vertex),\n        \"bge\" =&gt; Ok(ProviderKind::Bge),\n        other =&gt; Err(format!(\"unknown provider '{}'; supported: bedrock|anthropic|openai|vertex|bge\", other)),\n    }\n}\n\n// ISS-003 fix: stable string conversion for OTel labels (defined here for slice 2;\n// will move to policy/schema.rs as the canonical home).\nimpl ProviderKind {\n    pub fn as_metric_label(&amp;self) -&gt; &amp;'static str {\n        match self {\n            Self::Bedrock =&gt; \"bedrock\",\n            Self::Anthropic =&gt; \"anthropic\",\n            Self::OpenAI =&gt; \"openai\",\n            Self::Vertex =&gt; \"vertex\",\n            Self::Bge =&gt; \"bge\",\n        }\n    }\n}\n\nasync fn spawn_watcher(path: &amp;Path) -&gt; Result&lt;RecommendedWatcher, LoaderInitError&gt; {\n    let (tx, mut rx) = mpsc::channel::&lt;Event&gt;(16);\n    let mut watcher = notify::recommended_watcher(move |res: notify::Result&lt;Event&gt;| {\n        if let Ok(ev) = res { let _ = tx.blocking_send(ev); }\n    }).map_err(LoaderInitError::WatcherSetup)?;\n    // ISS-004 fix: path.parent() returns Some(\"\") for bare filenames, not None.\n    let watch_dir: PathBuf = match path.parent() {\n        Some(p) if !p.as_os_str().is_empty() =&gt; p.to_path_buf(),\n        _ =&gt; {\n            tracing::warn!(?path, \"cost_rates.yaml has no parent dir; watching CWD instead\");\n            PathBuf::from(\".\")\n        }\n    };\n    watcher.watch(&amp;watch_dir, RecursiveMode::NonRecursive)\n        .map_err(LoaderInitError::WatcherSetup)?;\n\n    let path = path.to_path_buf();\n    tokio::spawn(async move {\n        let mut last_event_at: Option&lt;Instant&gt; = None;\n        loop {\n            tokio::select! {\n                Some(_event) = rx.recv() =&gt; {\n                    last_event_at = Some(Instant::now());\n                }\n                _ = tokio::time::sleep(std::time::Duration::from_millis(DEBOUNCE_MS)) =&gt; {\n                    if let Some(t) = last_event_at {\n                        if t.elapsed() &gt;= std::time::Duration::from_millis(DEBOUNCE_MS) {\n                            last_event_at = None;\n                            apply_reload(&amp;path).await;\n                        }\n                    }\n                }\n            }\n        }\n    });\n    Ok(watcher)\n}\n\nasync fn apply_reload(path: &amp;Path) {\n    match load_and_validate(path).await {\n        Ok(new_table) =&gt; {\n            if let Some(s) = TABLE.get() {\n                let count = new_table.len();\n                s.store(Arc::new(new_table));\n                metrics::ENTRY_COUNT.set(count as i64);\n            }\n            if let Some(s) = LOADED_AT.get() {\n                s.store(Arc::new(Some(Utc::now())));\n                metrics::LOADED_AT_TS.set(Utc::now().timestamp());\n            }\n            tracing::info!(?path, \"cost_table_reloaded\");\n        }\n        Err(e) =&gt; {\n            let reason = match &amp;e {\n                LoaderInitError::Schema { .. } =&gt; \"validation_error\",\n                LoaderInitError::IoError { .. } =&gt; \"io_error\",\n                _ =&gt; \"unknown\",\n            };\n            metrics::RELOAD_FAILURES.with_label_values(&amp;[reason]).inc();\n            tracing::error!(?path, ?e, \"cost_table_reload_failed\");\n        }\n    }\n}</code></pre>\n<p><em>Scaffold is suggestive. AC §4 is the contract.</em></p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Code dependencies (must exist before this task can build):</strong></p>\n<ul><li>None. This is foundational; TASK-AI-001, TASK-AI-002, TASK-AI-006 all depend on it.</li></ul>\n<p><strong>Concept dependencies:</strong></p>\n<ul><li><code>ProviderKind</code> enum from <code>services/ai-gateway/src/policy/schema.rs</code> (TASK-AI-005). The closed set <code>{Bedrock, Anthropic, OpenAI, Vertex, Bge}</code> is the same set this task's <code>parse_provider</code> recognises.</li><li><code>Decimal</code> precision invariant — every cost-bearing field across AI Gateway uses <code>rust_decimal::Decimal</code> for arithmetic; TASK-AI-001/002 already assume this.</li></ul>\n<p><strong>Operational dependencies:</strong></p>\n<ul><li><code>services/ai-gateway/config/cost_rates.yaml</code> exists on the deployed filesystem (Dockerfile copies it; k8s ConfigMap mounts it).</li><li>Filesystem supports <code>notify</code> (inotify on Linux, FSEvents on macOS); deployment env must NOT be a network FS that lacks event support (S3FS, etc.). Same constraint as TASK-AI-005.</li></ul>\n<p><strong>Crate dependencies (in <code>Cargo.toml</code>):</strong></p>\n<ul><li><code>serde</code>, <code>serde_yaml</code> — YAML parsing</li><li><code>rust_decimal</code>, <code>rust_decimal_macros</code> — fixed-point arithmetic</li><li><code>arc-swap</code> — lock-free atomic Arc swap</li><li><code>notify</code> — cross-platform file watching</li><li><code>once_cell</code> — OnceCell statics</li><li><code>prometheus</code> — OTel-compatible metrics</li><li><code>chrono</code> — timestamps for <code>loaded_at</code></li><li><code>proptest</code> (dev-dep) — for §5 property test</li><li><code>criterion</code> (dev-dep) — for §5 benchmark</li><li><code>tempfile</code> (dev-dep) — for hot-reload tests</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Caller in TASK-AI-001 (precheck)</h3>\n<pre><code class=\"language-rust\">let cost_per_1k = cost_table::lookup(&amp;provider, &amp;model)\n    .ok_or_else(|| PrecheckError::CostEstimateFailed { reason: \"no_cost_entry\".into() })?;\nlet estimated_usd = (Decimal::from(req.prompt_tokens) / Decimal::from(1000)) * cost_per_1k.input_per_1k_usd\n                  + (Decimal::from(req.expected_completion_tokens) / Decimal::from(1000)) * cost_per_1k.output_per_1k_usd;</code></pre>\n<h3>Caller in TASK-AI-006 (alias resolve)</h3>\n<pre><code class=\"language-rust\">if cost_table::lookup(&amp;kind, model).is_none() {\n    return Err(AliasError::ResolvedModelMissingCostEntry { provider: kind, model: model.to_string() });\n}</code></pre>\n<h3>Caller in TASK-AI-002 (reconcile — uses is_embedding to validate)</h3>\n<pre><code class=\"language-rust\">let rate = cost_table::lookup(&amp;hold.resolved_provider, &amp;hold.resolved_model)\n    .ok_or(ReconcileError::CostTableMissing { /* ... */ })?;\nif rate.is_embedding &amp;&amp; usage.completion_tokens &gt; 0 {\n    tracing::warn!(model = %hold.resolved_model, completion_tokens = usage.completion_tokens,\n        \"embedding response has non-zero completion_tokens — provider bug?\");\n}</code></pre>\n<h3>Caller in TASK-AI-021 (operator CLI <code>models pricing</code>)</h3>\n<pre><code class=\"language-bash\">$ cyberos-ai models pricing\nCOST TABLE — loaded at 2026-05-16T09:30:00Z (45 minutes ago)\n\nPROVIDER     MODEL                                            INPUT/1K   OUTPUT/1K   FLAGS\nbedrock      anthropic.claude-3-5-sonnet-20241022-v2:0        $0.003     $0.015\nbedrock      anthropic.claude-3-haiku-20240307-v1:0           $0.00025   $0.00125\nbedrock      amazon.titan-embed-text-v2:0                     $0.00002   $0.000      embedding\nanthropic    claude-3-5-sonnet-20241022                       $0.003     $0.015\nanthropic    claude-3-haiku-20240307                          $0.00025   $0.00125\nopenai       gpt-4o                                           $0.0025    $0.01\nopenai       gpt-4o-mini                                      $0.00015   $0.0006\nopenai       text-embedding-3-small                           $0.00002   $0.000      embedding\nbge          bge-m3                                           $0.0       $0.0        embedding (self-hosted)\nbge          bge-reranker-v2-m3                               $0.0       $0.0        embedding (self-hosted)</code></pre>\n<h3>OBS metric snapshot after running</h3>\n<pre><code># HELP ai_cost_table_lookups_total Cost-table lookups by provider and outcome\n# TYPE ai_cost_table_lookups_total counter\nai_cost_table_lookups_total{provider=\"Bedrock\",outcome=\"hit\"} 8421\nai_cost_table_lookups_total{provider=\"Bedrock\",outcome=\"miss\"} 12\nai_cost_table_lookups_total{provider=\"Anthropic\",outcome=\"hit\"} 421\n\n# HELP ai_cost_table_entries_total Current count of (provider, model) entries\n# TYPE ai_cost_table_entries_total gauge\nai_cost_table_entries_total 10\n\n# HELP ai_cost_table_loaded_at_ts UNIX timestamp of last successful load\n# TYPE ai_cost_table_loaded_at_ts gauge\nai_cost_table_loaded_at_ts 1763199600</code></pre>\n<h3>Failure example — aggregate validation</h3>\n<pre><code class=\"language-rust\">// init_cost_table call returns:\nErr(LoaderInitError::Schema {\n    failures: vec![\n        FileFailure {\n            path: PathBuf::from(\"config/cost_rates.yaml\"),\n            provider: Some(\"bedrock\".into()),\n            model: Some(\"claude-broken\".into()),\n            errors: vec![\"input_per_1k_usd must be non-negative, got -0.01\".into()],\n        },\n        FileFailure {\n            path: PathBuf::from(\"config/cost_rates.yaml\"),\n            provider: Some(\"typo_provider\".into()),\n            model: None,\n            errors: vec![\"unknown provider 'typo_provider'; supported: bedrock|anthropic|openai|vertex|bge\".into()],\n        },\n        FileFailure {\n            path: PathBuf::from(\"config/cost_rates.yaml\"),\n            provider: Some(\"openai\".into()),\n            model: Some(\"\".into()),\n            errors: vec![\"model name length must be 1..=256, got 0\".into()],\n        },\n    ],\n})</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. No deferred questions.</p>\n<p>For reference, the questions considered + resolved during authoring:</p>\n<ol><li><strong>~~Should the cost table support multi-currency?~~</strong> — RESOLVED: USD only (§1 #9 + §2 rationale). FX is TASK-INV-002's job.</li><li><strong>~~Should we use f64 or Decimal?~~</strong> — RESOLVED: Decimal (§1 #10 + §2). Money requires exact arithmetic.</li><li><strong>~~What's the debounce on hot-reload?~~</strong> — RESOLVED: 100ms (§1 #5 + §2 rationale). Avoids editor-burst thrash.</li><li><strong>~~Should hot-reload failure be fatal?~~</strong> — RESOLVED: no, preserve last-known-good (§1 #6 + §2). Asymmetric trade-off: startup fails loud, runtime fails soft.</li><li><strong>~~Should we have is_embedding metadata?~~</strong> — RESOLVED: yes (§1 #12 + §2). Helps TASK-AI-002 validate provider responses.</li></ol>\n<hr>\n<h2>§10 — Failure modes inventory (all error paths)</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>cost_rates.yaml</code> missing at startup</td><td><code>fs::read_to_string</code> <code>NotFound</code></td><td><code>LoaderInitError::IoError</code>; gateway exits 1</td><td>Operator creates file; redeploy</td></tr><tr><td>YAML syntax error at startup</td><td><code>serde_yaml::from_str</code> error</td><td><code>LoaderInitError::Schema</code> with one failure</td><td>Operator fixes YAML; redeploy</td></tr><tr><td>Negative rate at startup</td><td>Validation step</td><td><code>LoaderInitError::Schema</code> aggregating ALL failures</td><td>Operator corrects all flagged rates</td></tr><tr><td>Unknown provider name at startup</td><td><code>parse_provider</code> returns Err</td><td>Aggregated in Schema failures</td><td>Operator uses correct provider key</td></tr><tr><td>Empty model name at startup</td><td>Length check</td><td>Aggregated in Schema failures</td><td>Operator removes empty entries</td></tr><tr><td>Lookup for missing (provider, model)</td><td><code>HashMap::get</code> returns None</td><td><code>lookup</code> returns <code>None</code>; <code>ai_cost_table_lookups_total{outcome=\"miss\"}++</code></td><td>Caller translates to its own error type</td></tr><tr><td>Hot-reload YAML invalid</td><td><code>notify</code> event + re-validation fails</td><td>Cache preserved; <code>reload_failures_total++</code>; sev-2 OBS event</td><td>Operator fixes YAML; next event re-tries</td></tr><tr><td>Hot-reload IO error (file deleted)</td><td><code>read_to_string</code> error</td><td>Cache preserved; warn log</td><td>Operator restores file</td></tr><tr><td>Concurrent lookup + hot-reload</td><td><code>ArcSwap.load()</code> atomic snapshot</td><td>Reader gets old or new; never torn</td><td>No-op; by design</td></tr><tr><td>Filesystem doesn't support notify (NFS)</td><td><code>notify</code> falls back to polling</td><td>Hot-reload latency widens to ~30s; warn log on init</td><td>Document the constraint; operator chooses inotify-capable storage</td></tr><tr><td><code>last_updated</code> stale (&gt;90 days)</td><td>CI lint</td><td>Warning on PR; non-blocking</td><td>Operator refreshes prices in next sprint</td></tr><tr><td>Loader called twice (programmer error)</td><td><code>OnceCell::set</code> returns Err</td><td><code>LoaderInitError::AlreadyInitialised</code></td><td>Engineer fixes startup ordering</td></tr><tr><td>Watcher init on bare filename</td><td><code>path.parent()</code> returns Some(\"\")</td><td>Falls back to watching CWD; emits <code>tracing::warn!</code></td><td>Operator passes a fully-qualified path on next deploy</td></tr><tr><td><code>is_embedding: true</code> with non-zero output rate</td><td>Cross-field validation in <code>validate_and_flatten</code></td><td>Aggregated in Schema failures with explicit \"is_embedding requires output == 0\" message</td><td>Operator fixes YAML inconsistency</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes (informational, no normative force)</h2>\n<ul><li>Per provider's published pricing, rates change roughly monthly. The YAML's <code>last_updated</code> field is human-maintained; a CI lint flags entries older than 90 days for review. The lint is non-blocking — staleness is a warning, not a deploy gate.</li><li>The 6-decimal precision (<code>NUMERIC(12,6)</code>) accommodates <code>gpt-4o-mini</code>'s <code>$0.00015/1k</code> and <code>text-embedding-3-small</code>'s <code>$0.00002/1k</code> without precision loss. If a future provider publishes 8-decimal rates, the schema needs to widen — but unlikely in slice 2's horizon.</li><li>Multi-currency support is intentionally NOT in this task. The invoicing module (TASK-INV-002, P2) handles VND/USD/SGD/EUR conversion using SBV's daily FX rate. Cost-ledger comparisons happen in USD throughout.</li><li>The <code>is_embedding</code> flag is the only schema field with no provider-published equivalent — we set it manually in the YAML based on operational knowledge of which model produces vectors. If an operator forgets to set it on a new embedding model, TASK-AI-002's reconcile path emits a warn log but doesn't crash; the embedding cost would still be <code>0.0</code> because <code>output_per_1k_usd: 0.0</code> in the YAML.</li><li>The watcher's 100ms debounce can be tuned via env var <code>CYBEROS_AI_COST_TABLE_DEBOUNCE_MS</code> (default 100, min 10, max 5000). Most operators never touch it.</li><li>Cost-table reload doesn't invalidate in-flight precheck/reconcile transactions; each one captures the rate at its own time. If a price changes mid-call (rare), the call uses the rate that was active when precheck ran — not the rate at reconcile. This is intentional: estimate-and-reconcile arithmetic must use the same rate for consistency.</li><li>This task is the simplest \"config-loader\" pattern in slice 2. The same pattern repeats in TASK-AI-005 (tenant policy), TASK-AI-015 (ZDR attestations). All three share idioms (ArcSwap, notify, aggregate failures); a shared crate is a future refactor (slice 4) once we have 3+ instances to extract from.</li><li><strong>Decimal vs BIGINT minor (task-audit skill §3.4 rule 11) — boundary clarification:</strong> rates here use <code>rust_decimal::Decimal</code> for <code>input_per_1k_usd</code> / <code>output_per_1k_usd</code> because rates are <strong>constants</strong> with 7–9 decimal places (e.g. <code>$0.00015/1k</code>), not stored monetary state. Task-audit skill §3.4 rule 11 \"money MUST be stored as BIGINT minor\" applies at the <strong>storage tier</strong> — i.e. <code>cost_ledger.estimated_cost_minor</code> and <code>actual_cost_minor</code> (TASK-AI-001 §3). The product <code>rate × tokens</code> IS BIGINT minor when stored. The cross-boundary conversion happens in <code>cost_ledger::estimate_cost_minor</code>, which multiplies the Decimal rate by token count and rounds to the currency-decimal-aware minor unit via <code>Currency::USD.decimals()</code>. Future maintainers: do not \"fix\" rates to BIGINT minor — sub-cent precision matters for embedding pricing.</li></ul>\n<hr>\n<p><em>End of TASK-AI-007. Status: draft (10/10 target, expanded from compressed first-pass per workflow correction 2026-05-15). Run <code>task-audit</code> next.</em></p>";
