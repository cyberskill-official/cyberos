window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> ship a standalone Rust binary <code>cost_hold_expiry</code> that runs as a long-lived process and, on a 30-second tick, scans <code>cost_ledger_hold</code> for rows whose <code>state = 'held' AND expires_at &lt; NOW()</code> and transitions each to <code>state = 'expired'</code> with a chained memory audit row per row.</p>\n<p>The cleanup tick:</p>\n<ol><li><strong>MUST</strong> select expired holds with <code>FOR UPDATE SKIP LOCKED LIMIT 500</code> — bounded per tick to prevent a million-row spike from monopolising the database.</li><li><strong>MUST</strong> process each hold inside its own Postgres transaction; one hold's failure MUST NOT roll back the others. Per-hold atomicity replaces per-batch atomicity.</li><li><strong>MUST</strong> emit one <code>ai.hold_expired</code> memory audit row per expired hold via <code>memory_writer::emit()</code> (TASK-AI-003) before committing the row's state transition.</li><li><strong>MUST</strong> leave <code>cost_ledger.spent_usd</code> UNCHANGED — an expired hold is functionally a refund (the call never settled, so no value was delivered to the tenant).</li><li><strong>MUST</strong> sleep 30 seconds between ticks via <code>tokio::time::sleep</code>; the tick interval is configurable via the <code>CYBEROS_AI_EXPIRY_TICK_SECONDS</code> env var (default 30, min 5, max 300).</li><li><strong>MUST</strong> handle SIGTERM gracefully — finish the current tick (or current hold inside the tick), emit a <code>process_shutdown</code> log line, exit 0 within 5 seconds of receiving SIGTERM.</li><li><strong>MUST</strong> handle Postgres connection drops by reconnecting with exponential backoff (1s, 2s, 4s, 8s, capped at 30s). Connection failures MUST NOT crash the binary; the process is expected to outlive transient DB outages.</li><li><strong>MUST</strong> emit OTel metrics: <code>ai_expiry_holds_processed_total</code> (counter), <code>ai_expiry_tick_duration_seconds</code> (histogram), <code>ai_expiry_consecutive_failures_total</code> (counter, resets on success).</li><li><strong>SHOULD</strong> emit a sev-2 OBS alert when <code>ai_expiry_consecutive_failures_total ≥ 10</code> (5 minutes of total failure). TASK-OBS-007 routes this when it lands; until then, structured <code>tracing::error!</code> lines suffice.</li><li><strong>MUST</strong> be idempotent under crash — if the binary crashes mid-tick after emitting some memory rows but before committing some hold transitions, the next tick MUST re-scan and either complete the transitions or surface a chain inconsistency. The <code>FOR UPDATE SKIP LOCKED</code> semantics ensure no double-emission <em>within a single binary instance</em>; cross-restart crash recovery is the slice-1 documented limitation per AC #9.</li><li><strong>MUST</strong> allocate one <code>tick_id</code> (ULID-26) per tick at tick start. Every <code>ai.hold_expired</code> audit row emitted during the tick MUST carry <code>extra.tick_id</code>. This enables OBS correlation: \"show all expirations from tick X\".</li><li><strong>MUST</strong> emit OTel metrics on every tick: <code>ai_expiry_ticks_total</code> (counter), <code>ai_expiry_tick_duration_seconds</code> (histogram), <code>ai_expiry_holds_processed_total</code> (counter), <code>ai_expiry_holds_succeeded_total</code> (counter), <code>ai_expiry_holds_failed_total</code> (counter), <code>ai_expiry_consecutive_failures</code> (gauge; resets to 0 on success).</li><li><strong>MUST NOT</strong> implement leader election for multi-instance deployments. One instance is sufficient at any P0/P1 scale. If two are deployed by accident, Postgres <code>FOR UPDATE SKIP LOCKED</code> keeps them safe — one will simply do no work per tick. TASK-AI-021 (operator CLI) surfaces a warning if <code>ps -ef \\| grep cost_hold_expiry \\| wc -l &gt; 1</code>.</li><li><strong>MUST</strong> emit OBS metrics enumerated above (#12) on every tick AND publish the per-tick <code>cleanup_holds_pending_gauge</code> as a Prometheus gauge that operators can dashboard. Long-lived <code>pending</code> backlog (&gt;30 minutes) is the canary signal for a stuck cleanup loop or a Postgres lock conflict.</li><li><strong>MUST</strong> emit <code>ai.hold_expired_started</code> BEFORE applying <code>UPDATE cost_ledger SET state='expired'</code> for each batched hold per task-audit skill §3.8 rule 25 (audit-before-action). The Postgres transaction wraps both the memory-emit AND the UPDATE in one atomic unit; rollback on either failure. <code>ai.hold_expired_completed</code> follows post-commit per task-audit skill §3.8 rule 26 (pair-write). The OBS lint flags any standalone <code>ai.hold_expired_started</code> over a 5-minute window. AC #15 added with captured-events ordering test.</li><li><strong>MUST</strong> order the per-sweep <code>SELECT ... FOR UPDATE SKIP LOCKED LIMIT 1000</code> by <code>ORDER BY hold_id ASC</code> per task-audit skill §3.9 rule 27 (determinism), so two runs on the same backlog produce byte-identical expire-order. The per-sweep <code>ai.cleanup_run_completed</code> audit row's <code>extra.expired_hold_ids: Vec&lt;Uuid&gt;</code> MUST be recorded in the same sorted order; operator diffing across runs is reliable. AC #16 added with deterministic-order test.</li></ol>\n<p>A single instance of this binary services the AI Gateway deployment. Running multiple instances is safe (Postgres row-level locking ensures correctness) but provides no throughput benefit at slice-1 scale; TASK-AI-021 (operator CLI) adds the multi-instance leader-election story if it's ever needed.</p>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a separate binary and not a tokio task inside the main gateway service?</strong> Two reasons. (1) Lifecycle isolation: if the cleanup job hangs (e.g., a single bad row blocks the whole binary), the gateway's request path keeps serving. The opposite is also true — gateway crashes don't take down cleanup. (2) Operational visibility: a separate systemd unit gives ops a clean signal (\"cleanup is up\", \"cleanup is down\") rather than burying the status behind a gateway health-check.</p>\n<p><strong>Why 30 seconds tick interval?</strong> Holds expire after 60s (TASK-AI-001's TTL). A 30s tick guarantees an expired hold gets processed within at most 90s of its original creation (60s TTL + 30s tick lag). Tighter intervals (5–15s) just burn CPU; slacker intervals (60–300s) let the hold table grow under traffic spikes. 30s is the goldilocks zone for slice-1 traffic shapes.</p>\n<p><strong>Why <code>FOR UPDATE SKIP LOCKED</code>?</strong> Two pieces of value. (1) <code>FOR UPDATE</code> prevents a second cleanup instance (or a stray re-reconcile call from the main gateway) from racing to transition the same row. (2) <code>SKIP LOCKED</code> means cleanup never blocks on a hold currently being reconciled by the main gateway — if a row is locked elsewhere, just skip it; it'll be processed in 30s. The default <code>FOR UPDATE</code> (without SKIP LOCKED) would queue cleanup behind every in-flight reconcile and lengthen ticks under load.</p>\n<p><strong>Why per-hold transactions and not per-batch?</strong> Per-batch atomicity feels safer (\"all 500 or none\"), but it amplifies blast radius — one bad row (e.g., a hold pointing to a deleted tenant) rolls back all 500. Per-hold atomicity isolates the failure. The cost is N transaction commits per tick instead of 1, but with <code>synchronous_commit = on</code> and a local Postgres, that's ~5ms × 500 = 2.5s of commit overhead — well inside the 30s tick budget.</p>\n<p><strong>Why emit <code>ai.hold_expired</code> and not just delete the row?</strong> Three reasons. (1) Compliance: the EU AI Act Art. 12 audit trail wants evidence of every cost-gate decision, including the ones where the gate held money that never got spent. (2) Debugging: when a tenant reports \"I got charged for a call that never returned\", the <code>ai.hold_expired</code> row is the canonical proof that we <em>didn't</em> charge them. (3) memory's audit-before-action invariant says state transitions must hit the chain; this is no exception.</p>\n<p><strong>Why is <code>cost_ledger.spent_usd</code> unchanged on expiry?</strong> An expired hold means the provider call never completed (or completed so late that the reconcile path was abandoned). The tenant got no value. Charging the budget would be a soft fraud. The few cases where the provider <em>did</em> complete but the gateway lost track are explicit data-quality bugs to fix in the gateway, not silent leaks to swallow.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Library function signature (testable)</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cost_hold_expiry.rs\n\npub async fn run_tick(pool: &amp;PgPool) -&gt; Result&lt;TickReport, TickError&gt;;\n\npub struct TickReport {\n    pub holds_processed: u32,\n    pub holds_succeeded: u32,\n    pub holds_failed: u32,\n    pub duration_ms: u32,\n}\n\npub enum TickError {\n    DbError(sqlx::Error),\n    // Note: per-hold failures roll up into TickReport.holds_failed; TickError is reserved\n    // for tick-level failures (DB unreachable, query syntax error, etc.)\n}</code></pre>\n<h3>Binary entrypoint</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/bin/cost_hold_expiry.rs\n\n#[tokio::main]\nasync fn main() -&gt; anyhow::Result&lt;()&gt; {\n    let tick_seconds = env::var(\"CYBEROS_AI_EXPIRY_TICK_SECONDS\")\n        .unwrap_or_else(|_| \"30\".into())\n        .parse::&lt;u64&gt;()?\n        .clamp(5, 300);\n\n    let pool = build_pool().await?;\n    let mut shutdown = signal::unix::signal(SignalKind::terminate())?;\n    let mut tick = tokio::time::interval(Duration::from_secs(tick_seconds));\n    let mut consecutive_failures: u32 = 0;\n\n    loop {\n        tokio::select! {\n            _ = tick.tick() =&gt; {\n                match cost_hold_expiry::run_tick(&amp;pool).await {\n                    Ok(report) =&gt; {\n                        consecutive_failures = 0;\n                        metrics::observe_tick(&amp;report);\n                        tracing::info!(?report, \"expiry_tick_complete\");\n                    }\n                    Err(e) =&gt; {\n                        consecutive_failures += 1;\n                        tracing::error!(?e, consecutive_failures, \"expiry_tick_failed\");\n                        if consecutive_failures &gt;= 10 {\n                            tracing::error!(\"expiry_consecutive_failures_threshold\");\n                        }\n                    }\n                }\n            }\n            _ = shutdown.recv() =&gt; {\n                tracing::info!(\"process_shutdown\");\n                break;\n            }\n        }\n    }\n    Ok(())\n}</code></pre>\n<h3>SQL — scan + transition</h3>\n<pre><code class=\"language-sql\">-- One per-hold transaction:\nBEGIN;\n  SELECT id, tenant_id, estimated_usd, expires_at\n    FROM cost_ledger_hold\n    WHERE state = 'held' AND expires_at &lt; NOW()\n    ORDER BY expires_at ASC\n    FOR UPDATE SKIP LOCKED\n    LIMIT 1;\n\n  -- (in Rust: per row, emit memory ai.hold_expired before this update)\n\n  UPDATE cost_ledger_hold\n    SET state = 'expired', refunded_at = NOW(), refund_reason = 'tick_expired'\n    WHERE id = $hold_id AND state = 'held';\nCOMMIT;</code></pre>\n<p>The outer Rust loop fetches up to 500 hold IDs in one query (<code>LIMIT 500</code>), then processes each in its own transaction. The per-row transaction holds the row lock for only the duration of one memory emission + one UPDATE (~50ms).</p>\n<h3>Systemd unit</h3>\n<pre><code class=\"language-ini\"># deploy/systemd/cyberos-ai-gateway-expiry.service\n[Unit]\nDescription=CyberOS AI Gateway cost-hold expiry cleanup\nAfter=network-online.target postgresql.service\nWants=network-online.target\n\n[Service]\nType=simple\nUser=cyberos\nExecStart=/usr/local/bin/cost_hold_expiry\nEnvironmentFile=/etc/cyberos/ai-gateway.env\nRestart=always\nRestartSec=5\nTimeoutStopSec=10\nKillSignal=SIGTERM\nStandardOutput=journal\nStandardError=journal\nSyslogIdentifier=cyberos-ai-expiry\n\n[Install]\nWantedBy=multi-user.target</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Happy path</strong> — Seed Postgres with 3 holds whose <code>expires_at = NOW() - INTERVAL '10 seconds'</code> and <code>state = 'held'</code>. Call <code>run_tick()</code>. MUST return <code>TickReport { holds_processed: 3, holds_succeeded: 3, holds_failed: 0 }</code>. All 3 holds MUST transition to <code>state = 'expired'</code>. Exactly 3 <code>ai.hold_expired</code> memory rows MUST exist on the chain, each chained to the previous.</li><li><strong>Non-expired holds skipped</strong> — Seed 5 holds, 2 expired and 3 not. <code>run_tick()</code> MUST process only the 2 expired ones; the 3 non-expired MUST remain <code>state = 'held'</code> untouched.</li><li><strong>Already-reconciled holds skipped</strong> — Seed a hold with <code>state = 'reconciled'</code> but <code>expires_at &lt; NOW()</code> (an edge case where reconcile happened just before tick). <code>run_tick()</code> MUST NOT touch it (state filter is <code>state = 'held'</code>).</li><li><strong><code>FOR UPDATE SKIP LOCKED</code> works</strong> — Spawn a separate transaction that holds <code>SELECT ... FOR UPDATE</code> on hold A; call <code>run_tick()</code> against the same pool. Tick MUST skip hold A and process the other expired holds normally. Hold A MUST be processed in the next tick after the blocking transaction commits.</li><li><strong>memory failure rolls back row transition</strong> — Inject a <code>memory_writer</code> failure for one specific hold. The transaction for that hold MUST roll back (state stays <code>'held'</code>); the other holds in the tick MUST process normally; <code>TickReport.holds_failed = 1</code>.</li><li><strong>Bounded batch size</strong> — Seed 5,000 expired holds. <code>run_tick()</code> MUST process at most 500 (the LIMIT). The remaining 4,500 MUST be picked up on subsequent ticks.</li><li><strong>Graceful shutdown</strong> — Send SIGTERM to the running binary mid-tick. The current hold's transaction MUST complete (commit or roll back); the binary MUST exit 0 within 5 seconds; no partial state visible on the next process start.</li><li><strong>Reconnect on DB drop</strong> — Kill the Postgres container mid-binary-run. Wait 10 seconds, restart Postgres. The binary MUST reconnect (visible via the <code>consecutive_failures</code> counter resetting to 0 on next successful tick); MUST NOT crash.</li><li><strong>Crash recovery (weakened for slice 1)</strong> — Run <code>run_tick()</code>, kill the process mid-iteration (between memory emit and UPDATE commit) via SIGKILL. On restart, the next tick MUST handle the un-transitioned hold by emitting a <em>second</em> <code>ai.hold_expired</code> memory row (slice 1 lacks dedup_key per TASK-AI-008 deferral) and then transitioning the hold. Both memory rows MUST chain correctly. The operator MUST run <code>cyberos-ai expiry repair</code> (TASK-AI-021) to dedupe after large crash events. This is a documented slice-1 limitation; TASK-AI-008 (slice 2 PyO3 spike) adds <code>dedup_key</code> to fix it natively.</li><li><strong>Metrics emitted</strong> — After 3 successful ticks processing 10 holds total, <code>ai_expiry_holds_processed_total</code> MUST equal 10, <code>ai_expiry_tick_duration_seconds</code> histogram MUST have 3 observations, <code>ai_expiry_consecutive_failures_total</code> MUST equal 0.</li></ol>\n<hr>\n<h2>§5 — Verification method</h2>\n<p><strong>Integration test:</strong> <code>services/ai-gateway/tests/cost_hold_expiry_test.rs</code></p>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn tick_processes_expired_holds() {\n    let env = TestEnv::new().await;\n    env.seed_tenant(\"org:test-a\", monthly_cap = 100, spent_usd = 50).await;\n    for i in 0..3 {\n        env.seed_hold(\n            \"org:test-a\",\n            estimated_usd = dec!(0.0085),\n            expires_at = Utc::now() - Duration::seconds(10),\n            state = \"held\",\n        ).await;\n    }\n\n    let report = cost_hold_expiry::run_tick(&amp;env.pool).await.unwrap();\n    assert_eq!(report.holds_processed, 3);\n    assert_eq!(report.holds_succeeded, 3);\n    assert_eq!(report.holds_failed, 0);\n\n    let holds = env.read_all_holds_for_tenant(\"org:test-a\").await;\n    assert_eq!(holds.iter().filter(|h| h.state == \"expired\").count(), 3);\n\n    let ledger = env.read_ledger(\"org:test-a\").await;\n    assert_eq!(ledger.spent_usd, dec!(50));  // unchanged on expiry\n\n    let chain_rows = env.memory_count_rows_by_kind(\"ai.hold_expired\").await;\n    assert_eq!(chain_rows, 3);\n    assert!(env.verify_chain_from_genesis().await);\n}\n\n#[tokio::test]\nasync fn tick_skips_locked_rows() {\n    let env = TestEnv::new().await;\n    let lock_holder_id = env.seed_hold(/* expired */).await;\n\n    // Lock the row from another transaction\n    let mut blocking_tx = env.pool.begin().await.unwrap();\n    sqlx::query(\"SELECT * FROM cost_ledger_hold WHERE id = $1 FOR UPDATE\")\n        .bind(lock_holder_id).fetch_one(&amp;mut *blocking_tx).await.unwrap();\n\n    let other_id = env.seed_hold(/* expired */).await;\n\n    let report = cost_hold_expiry::run_tick(&amp;env.pool).await.unwrap();\n    assert_eq!(report.holds_processed, 1);  // skipped lock_holder_id\n\n    let lock_holder = env.read_hold(lock_holder_id).await;\n    assert_eq!(lock_holder.state, \"held\");  // still held\n\n    blocking_tx.rollback().await.unwrap();\n\n    // Next tick picks up the previously locked one\n    let report2 = cost_hold_expiry::run_tick(&amp;env.pool).await.unwrap();\n    assert_eq!(report2.holds_processed, 1);\n}\n\n#[tokio::test]\nasync fn graceful_shutdown_finishes_current_hold() { /* AC #7 */ }\n\n#[tokio::test]\nasync fn bounded_batch_500() { /* AC #6 */ }</code></pre>\n<p>Run via:</p>\n<pre><code class=\"language-bash\">cd /Users/stephencheng/Projects/CyberSkill/cyberos\ncargo test -p cyberos-ai-gateway cost_hold_expiry</code></pre>\n<p><strong>Smoke test for the binary:</strong></p>\n<pre><code class=\"language-bash\"># Terminal 1: run binary\ncargo run --bin cost_hold_expiry\n\n# Terminal 2: seed expired holds, watch them transition\npsql -c \"INSERT INTO cost_ledger_hold (...) VALUES (...)\"\npsql -c \"SELECT state, refunded_at FROM cost_ledger_hold WHERE expires_at &lt; NOW();\"\n\n# Terminal 1: SIGTERM, observe clean exit\nkill -SIGTERM $(pidof cost_hold_expiry)</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cost_hold_expiry.rs\n\nuse sqlx::PgPool;\nuse uuid::Uuid;\nuse std::time::Instant;\n\nconst BATCH_SIZE: i64 = 500;\n\npub async fn run_tick(pool: &amp;PgPool) -&gt; Result&lt;TickReport, TickError&gt; {\n    let started = Instant::now();\n    let mut processed = 0u32;\n    let mut succeeded = 0u32;\n    let mut failed = 0u32;\n\n    loop {\n        // Fetch a batch of expired hold IDs in one read-only query\n        let ids: Vec&lt;Uuid&gt; = sqlx::query_scalar(\n            \"SELECT id FROM cost_ledger_hold \\\n             WHERE state = 'held' AND expires_at &lt; NOW() \\\n             ORDER BY expires_at ASC \\\n             LIMIT $1\"\n        )\n        .bind(BATCH_SIZE)\n        .fetch_all(pool)\n        .await?;\n\n        if ids.is_empty() { break; }\n\n        let mut batch_attempted = 0u32;\n        for id in ids {\n            batch_attempted += 1;\n            match process_one_hold(pool, id).await {\n                Ok(HoldDisposition::Transitioned)        =&gt; { processed += 1; succeeded += 1; }\n                Ok(HoldDisposition::AlreadyTransitioned) =&gt; { /* skip count */ }\n                Err(e) =&gt; {\n                    processed += 1;\n                    failed += 1;\n                    tracing::warn!(?id, ?e, \"expiry_hold_failed\");\n                }\n            }\n        }\n\n        // If we just processed a partial batch, we're done; otherwise loop for the next batch\n        if batch_attempted &lt; BATCH_SIZE as u32 { break; }\n    }\n\n    Ok(TickReport {\n        holds_processed: processed,\n        holds_succeeded: succeeded,\n        holds_failed: failed,\n        duration_ms: started.elapsed().as_millis() as u32,\n    })\n}\n\npub enum HoldDisposition {\n    /// We touched the hold and transitioned it from 'held' to 'expired'\n    Transitioned,\n    /// Hold was already reconciled / expired / locked by another transaction;\n    /// nothing for us to do.\n    AlreadyTransitioned,\n}\n\nasync fn process_one_hold(pool: &amp;PgPool, hold_id: Uuid) -&gt; Result&lt;HoldDisposition, HoldError&gt; {\n    let mut tx = pool.begin().await?;\n\n    // Lock + load the hold (SKIP LOCKED here means we just bail if someone else took it)\n    let hold: Option&lt;HoldRow&gt; = sqlx::query_as(\n        \"SELECT id, tenant_id, idempotency_key, estimated_usd, expires_at \\\n         FROM cost_ledger_hold \\\n         WHERE id = $1 AND state = 'held' AND expires_at &lt; NOW() \\\n         FOR UPDATE SKIP LOCKED\"\n    )\n    .bind(hold_id)\n    .fetch_optional(&amp;mut *tx)\n    .await?;\n\n    let hold = match hold {\n        Some(h) =&gt; h,\n        None =&gt; return Ok(HoldDisposition::AlreadyTransitioned),\n    };\n\n    // Emit memory audit row INSIDE the transaction's lifetime\n    memory_writer::emit(canonical::hold_expired(\n        &amp;hold.tenant_id,\n        hold.id,\n        hold.expires_at,\n        hold.estimated_usd,\n    ))\n    .await\n    .map_err(|e| HoldError::MemoryEmitFailed(e))?;\n\n    // Transition the hold\n    sqlx::query(\n        \"UPDATE cost_ledger_hold \\\n         SET state = 'expired', refunded_at = NOW(), refund_reason = 'tick_expired' \\\n         WHERE id = $1\"\n    )\n    .bind(hold.id)\n    .execute(&amp;mut *tx)\n    .await?;\n\n    tx.commit().await?;\n    Ok(HoldDisposition::Transitioned)\n}</code></pre>\n<p><em>Scaffold above is suggestive. AC §4 is the contract.</em></p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Code dependencies:</strong></p>\n<ul><li>TASK-AI-001 — <code>cost_ledger_hold</code> table exists.</li><li>TASK-AI-002 — <code>cost_ledger_hold.refunded_at</code> + <code>refund_reason</code> columns exist.</li><li>TASK-AI-003 — <code>memory_writer::emit()</code> + <code>canonical::hold_expired()</code> are available.</li></ul>\n<p><strong>Concept dependencies:</strong></p>\n<ul><li>The state machine for <code>cost_ledger_hold.state</code>: <code>held → reconciled | refunded | expired</code>. This task is the only path that creates <code>'expired'</code> rows.</li></ul>\n<p><strong>Operational dependencies:</strong></p>\n<ul><li>Postgres at <code>DATABASE_URL</code>.</li><li>A systemd-equivalent supervisor (Docker <code>restart: always</code>, Kubernetes Deployment, or systemd directly). The slice-1 deploy target is systemd on bare metal.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Postgres state — before tick</h3>\n<pre><code>| id           | tenant_id      | state | expires_at                | estimated_usd |\n|--------------|----------------|-------|---------------------------|---------------|\n| 01HZK9R7A2B4 | org:cyberskill | held  | 2026-05-15 09:30:00+00    | 0.0085        |\n| 01HZK9R7A2B5 | org:cyberskill | held  | 2026-05-15 09:29:50+00    | 0.0120        |\n| 01HZK9R8M3X5 | org:test-a     | held  | 2026-05-15 09:31:30+00    | 0.0050        |</code></pre>\n<p>(NOW() = 2026-05-15 09:30:30+00; rows 1 and 2 are expired)</p>\n<h3><code>run_tick()</code> returns</h3>\n<pre><code class=\"language-rust\">TickReport {\n    holds_processed: 2,\n    holds_succeeded: 2,\n    holds_failed: 0,\n    duration_ms: 120,\n}</code></pre>\n<h3>Postgres state — after tick</h3>\n<pre><code>| id           | tenant_id      | state    | expires_at                | refunded_at           | refund_reason  |\n|--------------|----------------|----------|---------------------------|-----------------------|----------------|\n| 01HZK9R7A2B4 | org:cyberskill | expired  | 2026-05-15 09:30:00+00    | 2026-05-15 09:30:30   | tick_expired   |\n| 01HZK9R7A2B5 | org:cyberskill | expired  | 2026-05-15 09:29:50+00    | 2026-05-15 09:30:30   | tick_expired   |\n| 01HZK9R8M3X5 | org:test-a     | held     | 2026-05-15 09:31:30+00    | NULL                  | NULL           |</code></pre>\n<h3>memory row written (per expired hold)</h3>\n<pre><code class=\"language-json\">{\n  \"seq\": 18450,\n  \"ts_ns\": 1763112630000000000,\n  \"op\": \"put\",\n  \"path\": \"memories/decisions/ai-invocations/1763112630000_org-cyberskill_hold-expired_01HZK9R7A2B4.md\",\n  \"extra\": {\n    \"kind\": \"ai.hold_expired\",\n    \"tenant_id\": \"org:cyberskill\",\n    \"hold_id\": \"01HZK9R7A2B4\",\n    \"expires_at\": \"2026-05-15T09:30:00Z\",\n    \"refund_amount_usd\": 0.0085,\n    \"tick_id\": \"01HZKHA2B4C8D6E1F3G5\"\n  },\n  \"prev_chain\": \"...\",\n  \"chain\": \"...\"\n}</code></pre>\n<h3>Tick log line</h3>\n<pre><code>{\"ts\":\"2026-05-15T09:30:30.120Z\",\"level\":\"info\",\"event\":\"expiry_tick_complete\",\"holds_processed\":2,\"holds_succeeded\":2,\"holds_failed\":0,\"duration_ms\":120}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved 2026-05-15 (round 2). Promoted to §1 normative clauses or deferred with explicit owner:</p>\n<ol><li><strong>~~Idempotent memory re-emit on crash recovery~~</strong> → deferred to TASK-AI-008 + TASK-AI-021 (cleanup CLI). AC #9 weakened with explicit note. Was Q1.</li><li><strong>~~DB pool sizing~~</strong> → pool size 2 (one bulk-fetch, one per-hold tx); revisit if profiling shows contention. Was Q2.</li><li><strong>~~Empty hold-table tick~~</strong> → no-op SELECT, ~5ms overhead, no harm; do nothing. Was Q3.</li><li><strong>~~Leader election~~</strong> → §1 #13 (NOT implemented; SKIP LOCKED makes multi-instance safe). Was Q4.</li><li><strong>~~tick_id correlation~~</strong> → §1 #11 (ULID-26 per tick on every audit row). Was Q5.</li><li><strong>Should the binary do its own DB pool sizing?</strong> — Tick's max in-flight transactions are bounded by the per-hold loop (sequential, not parallel). One DB connection is sufficient. Proposal: pool size 2 (one for the bulk-fetch query, one for the per-hold transaction); revisit if profiling shows contention.</li><li>**What if the cost_ledger_hold table is <em>empty</em> but ticks keep running?** — No-op tick, ~5ms of SELECT overhead, no harm. We could short-circuit by tracking last-non-zero tick time, but the optimisation buys nothing meaningful at any scale we care about. Proposal: don't bother.</li><li><strong>Leader election for multiple instances</strong> — One instance is enough for the foreseeable future. If someone deploys two by accident, Postgres's <code>SKIP LOCKED</code> keeps them safe (one of them just does no work per tick). Proposal: do nothing; TASK-AI-021 surfaces a warning if <code>ps -ef | grep cost_hold_expiry | wc -l &gt; 1</code> in the operator-CLI health check.</li><li><strong>Tick-id correlation across rows</strong> — Each tick's <code>ai.hold_expired</code> rows should carry the same <code>tick_id</code> so OBS investigators can correlate them. Proposed format: <code>01&lt;26-char-ULID&gt;</code> allocated at tick start. Adds 1 field to the audit row's <code>extra</code>. Worth it.</li></ol>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Action</th><th>Recovery</th></tr></thead><tbody><tr><td>Postgres unreachable</td><td>sqlx::Error on initial connect</td><td>Reconnect with exponential backoff (1s, 2s, 4s, 8s, capped 30s)</td><td>Self-healing; <code>ai_expiry_consecutive_failures_total</code> counter tracks</td></tr><tr><td>Postgres connection drops mid-tick</td><td>sqlx::Error on per-hold transaction</td><td>Per-hold failure incremented; tick continues with next hold</td><td>Self-healing; no per-tick rollback needed</td></tr><tr><td>memory Writer fails for a specific hold</td><td><code>memory_writer::emit</code> returns Err</td><td>Hold transaction rolls back (state stays <code>'held'</code>); <code>holds_failed++</code></td><td>Hold gets processed next tick (likely succeeds); if persistent, OBS sev-2</td></tr><tr><td>memory Writer hangs &gt; 5s</td><td>Writer's own timeout</td><td>Same as above (Writer's timeout becomes our error)</td><td>Tick continues</td></tr><tr><td>Bulk-fetch query returns 0 expired holds</td><td>Normal</td><td>Tick exits the inner loop; sleeps 30s</td><td>Normal idle state</td></tr><tr><td>Bulk-fetch returns 500+ holds</td><td>Normal under load</td><td>Process 500; remaining picked up next tick</td><td>Normal; consider lowering precheck TTL if persistent</td></tr><tr><td>Hold lock contention (another process holding the row)</td><td><code>FOR UPDATE SKIP LOCKED</code> returns None</td><td>Skip the row; process the next</td><td>Normal; row processed next tick when lock releases</td></tr><tr><td>Crash mid-tick after memory emit but before UPDATE commit</td><td>Process dies</td><td>Next tick may emit a duplicate memory row + complete transition</td><td>Slice-1 limitation; TASK-AI-021 <code>cyberos-ai expiry repair</code> dedupes</td></tr><tr><td>SIGTERM during tick</td><td>tokio::select catches it</td><td>Finish current hold's transaction; exit 0 within 5s</td><td>Clean shutdown</td></tr><tr><td>Consecutive failure count exceeds 10 (5 minutes total failure)</td><td>counter check at end of each failed tick</td><td>Sev-2 log; TASK-OBS-007 routes alarm (when shipped)</td><td>Operator investigates</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>This is the smallest task in slice 1 (3h effort) but probably the highest-leverage at runtime — it's the only thing standing between \"holds that got reconciled\" and \"holds that didn't ever settle\". Without it, the hold table is unbounded growth and the chain is missing a class of rows.</li><li>The systemd unit lives in <code>deploy/systemd/</code> not <code>services/ai-gateway/deploy/</code>; that path convention is set by the platform team (deploy assets centralised, code repos clean).</li><li>The <code>ai_expiry_consecutive_failures_total</code> counter resets on each successful tick — its purpose is to drive an alert when cleanup is <em>completely</em> broken, not to count cumulative lifetime failures. The latter is provided by <code>ai_expiry_holds_processed_total</code> minus the lifetime <code>holds_succeeded</code> rollup in the OBS dashboard.</li><li>Once TASK-AI-021 (operator CLI) ships, the <code>cyberos-ai expiry-status</code> subcommand will surface the binary's live tick state without needing OBS — useful for low-environment debugging.</li><li>Future enhancement (TASK-AI-104, post-P0): when the same tenant has many expired holds in a window, fold them into a single batch memory row (<code>ai.hold_expired_batch</code>) to reduce chain noise. Deferred until production traffic justifies the optimisation.</li></ul>\n<hr>\n<p><em>End of TASK-AI-004. Run <code>task-audit</code> next: <code>cargo run -p cyberos-skill-cli -- run task-audit --input '{\"artefact_paths\": [\"docs/tasks/ai/TASK-AI-004-cost-hold-expiry-cleanup/spec.md\"]}'</code></em></p>";
