window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-085"]="<h1>TASK-IMP-085: Doc-driven workflow helpers, ship-manifest and backlog-mutate CLIs</h1>\n<h2>Summary</h2>\n<p>Two contracts in the ship loop are executable only by agent discipline today: the ship-manifest resume cache and the backlog-state-update byte rules. Ship two stdlib-only node CLIs in docs-tools - <code>ship-manifest.mjs</code> (init, record, verify, resume-line, delete; two-phase atomic; pins and staleness exactly per the workflow's Resume semantics) and <code>backlog-mutate.mjs</code> (status-cell flip with old-line byte verification and drift refusal; insert-row with uniqueness gate, grammar, stem placement, and section-count maintenance). Vendored to installed repos; ship-tasks doctrine names them so agents reach for the tool instead of hand-sed.</p>\n<h2>Problem</h2>\n<p>Both real runs on 2026-07-16 (sachviet batch, cyberos batch 1) skipped per-step manifest writes and executed every backlog flip with hand-crafted sed. Nothing failed, but the two strongest disciplines in the contract - resume-after-crash and refuse-on-drift - existed only as prose. Two swarm agents were in fact killed mid-run in batch 1; recovery worked from commit archaeology, which is exactly what the manifest exists to replace.</p>\n<p>&lt;untrusted_content source=\"modules/cuo/chief-technology-officer/workflows/ship-tasks.md\"&gt; The manifest MUST be rewritten after EVERY completed, failed, or conditionally-skipped step - no step's outcome goes unrecorded. Writes are two-phase atomic. &lt;/untrusted_content&gt;</p>\n<h2>Proposed Solution</h2>\n<p><code>node .cyberos/docs-tools/ship-manifest.mjs &lt;cmd&gt;</code> and <code>node .cyberos/docs-tools/backlog-mutate.mjs &lt;cmd&gt;</code>, stdlib only, deterministic, <code>--json</code> envelopes, documented exit codes. The manifest tool implements ship-manifest@1 verbatim (pin at init, per-step artefact sha256, verify walks staleness in the workflow's order, resume-line echoes the mandated format, delete on done). The backlog tool is the byte-discipline executor for backlog-state-update@2: it never moves or deletes rows, refuses when the pre-image drifted, and keeps section-header counts true. Grammar authority stays where it is; the tool encodes it. Both vendor through guarded build.sh copies (the 084 lesson: docs-tools does not auto-vendor) and the ship-tasks doctrine gains two pointer sentences so doc-driven agents discover them.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Keep discipline in prose and trust agents. Rejected: two real runs show the discipline is skipped under time pressure precisely when it matters (crash recovery).</li><li>Python implementations beside repair_task_yaml.py. Rejected: consumer repos are guaranteed node by the MCP/status tooling, not python3 versions; docs-tools' executable convention is .mjs.</li><li>One combined tool. Rejected: the two contracts version independently (ship-manifest@1 vs backlog-state-update@2) and only one of them is per-task session state.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: both contracts become machine-executed - a resumed run reproduces the workflow's resume line from the manifest, and a drifted backlog flip is refused with a non-zero exit instead of silently landing. Baseline: 0 of 2 contracts tool-backed (both runs to date). Deadline: this task's final acceptance.</li><li>Guardrail: the suite's atomicity and drift fixtures run on every gate execution.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the two CLIs, their suite, guarded build.sh vendor copies, two doctrine pointer sentences in ship-tasks.md.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Changing either contract's semantics (the tools implement, never redefine).</li><li>BRAIN memory emission (IMP-05, separate).</li><li>Per-task coverage scoping (IMP-14, separate).</li><li>Rewiring existing skills' prose beyond the two pointer sentences.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None upstream; reuses TASK-IMP-084's frontmatter-reader approach conceptually (fresh code, no import coupling). Cone-disjoint from TASK-IMP-086 (BACKLOG.md content) and TASK-IMP-087 (docs/release/): this task's suite mutates only fixture backlogs in TMP.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted by the model from IMPROVEMENT_HANDOFF.md IMP-04 and the two contract sources; implementation follows under ship-tasks supervision.</li><li><strong>Human review:</strong> PLAN approved by the operator on 2026-07-16; spec audit and both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>ship-manifest.mjs</code> MUST provide <code>init &lt;task-id&gt; --task-file &lt;spec&gt; --workflow-version &lt;v&gt;</code>, <code>record &lt;task-id&gt; &lt;step&gt; &lt;skill&gt; &lt;status&gt; [--artefact &lt;path&gt;] [--verdict &lt;v&gt;]</code>, <code>verify &lt;task-id&gt;</code>, <code>resume-line &lt;task-id&gt;</code>, and <code>delete &lt;task-id&gt;</code>, operating on <code>docs/tasks/.workflow/&lt;task-id&gt;.ship.json</code>.</li><li>1.2 Every manifest write MUST be two-phase atomic (<code>.tmp.&lt;nonce&gt;</code> then rename); <code>task_sha256</code> (of the spec at init) and <code>workflow_version</code> MUST be pinned at init; each recorded step MUST carry <code>{index, skill, status, artefact_path, artefact_sha256, verdict, completed_at}</code> with the artefact hashed at record time.</li><li>1.3 <code>verify</code> MUST implement the workflow's staleness order with distinct exit codes: workflow_version mismatch -&gt; exit 3 (needs_human); task_sha256 mismatch -&gt; exit 4 (all steps stale, history and routed_back_count retained); earliest artefact hash mismatch -&gt; exit 5 reporting that step; all intact -&gt; exit 0 with the first non-done step. <code>resume-line</code> MUST echo the workflow's mandated format (<code>resume &lt;task-ID&gt;: steps 1-N verified (K artefacts, hashes OK), continuing at step M/31 (&lt;skill&gt;). routed_back_count=R</code>).</li><li>1.4 <code>backlog-mutate.mjs flip &lt;task-id&gt; &lt;from&gt; &lt;to&gt; [--backlog &lt;path&gt;]</code> MUST locate the row by stem, verify both the status cell and the full old line byte-for-byte against the pre-image, refuse on drift or missing row with exit 6, rewrite exactly that line, and update the containing section header's status counts when the header carries counts.</li><li>1.5 <code>backlog-mutate.mjs insert &lt;task-id&gt; &lt;stem&gt; &lt;title&gt; &lt;status&gt; [--backlog &lt;path&gt;]</code> MUST enforce the uniqueness pre-image (no row for the id anywhere, exit 7 on violation), emit the regenerator-identical row grammar used by the target section, place the row in stem-ascending order inside the section's contiguous block, and update header counts.</li><li>1.6 Both tools MUST be node-stdlib-only and deterministic, support <code>--json</code> result envelopes, document exit codes in <code>--help</code>, and never modify any line outside the declared mutation (whole-file discipline; flip and insert change exactly one row plus at most one header line).</li><li>1.7 build.sh MUST vendor both tools into the payload docs-tools via guarded copies, gated by the suite (a payload without them fails t08).</li><li>1.8 ship-tasks.md MUST gain two pointer sentences: Resume semantics names <code>ship-manifest.mjs</code> as the doc-driven reference implementation alongside <code>ship_manifest.py</code>; the backlog-layout section names <code>backlog-mutate.mjs</code> as the byte-discipline executor for backlog-state-update writes.</li><li>1.9 The suite MUST land at <code>tools/install/tests/test_workflow_helpers.sh</code> covering the lifecycle, atomicity, staleness exits, drift refusal, uniqueness, counts, determinism, payload carry + install lay-down, and doctrine wiring.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: §1 #1.1) - full manifest lifecycle init/record/verify/resume-line/delete - test: <code>tools/install/tests/test_workflow_helpers.sh::t01_manifest_lifecycle</code></li><li>[ ] AC 2 (traces_to: §1 #1.2) - two-phase atomicity: a planted stale tmp never corrupts the manifest; pins recorded at init - test: <code>tools/install/tests/test_workflow_helpers.sh::t02_two_phase_atomic</code></li><li>[ ] AC 3 (traces_to: §1 #1.3) - staleness exits 3/4/5/0 and the exact resume line - test: <code>tools/install/tests/test_workflow_helpers.sh::t03_verify_staleness_exits</code></li><li>[ ] AC 4 (traces_to: §1 #1.4) - flip rewrites one line; drifted pre-image refused with exit 6 - test: <code>tools/install/tests/test_workflow_helpers.sh::t04_flip_and_drift_refusal</code></li><li>[ ] AC 5 (traces_to: §1 #1.5) - insert uniqueness (exit 7), grammar, stem placement - test: <code>tools/install/tests/test_workflow_helpers.sh::t05_insert_uniqueness_and_grammar</code></li><li>[ ] AC 6 (traces_to: §1 #1.4, #1.5) - header counts stay true across flips and inserts - test: <code>tools/install/tests/test_workflow_helpers.sh::t06_counts_maintained</code></li><li>[ ] AC 7 (traces_to: §1 #1.6) - --json envelopes, documented exit codes, byte-identical reruns, whole-file discipline (diff = 1 row + &lt;=1 header) - test: <code>tools/install/tests/test_workflow_helpers.sh::t07_json_and_determinism</code></li><li>[ ] AC 8 (traces_to: §1 #1.7) - payload carries both tools; scratch install lays them into .cyberos/docs-tools/ - test: <code>tools/install/tests/test_workflow_helpers.sh::t08_payload_and_install</code></li><li>[ ] AC 9 (traces_to: §1 #1.8) - doctrine pointers present in ship-tasks.md source and in the scratch payload's cuo/ship-tasks.md - test: <code>tools/install/tests/test_workflow_helpers.sh::t09_doctrine_wiring</code></li><li>[ ] AC 10 (traces_to: §1 #1.9) - the suite exists at its mandated path and is discovered by the tools/install/tests glob - verify: <code>bash scripts/tests/run_all.sh</code> lists test_workflow_helpers.sh among suites (ops check recorded in the gate log; glob discovery is the runner's contract).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Kill mid-write: only <code>.tmp.&lt;nonce&gt;</code> remains; next read ignores tmp files; t02 plants one and proves the main file wins.</li><li>Manifest for a task whose spec moved: verify reports task-hash staleness (exit 4), never a crash (t03).</li><li>Flip target row present twice (corrupted backlog): refuse with exit 6 naming both lines - never guess (t04).</li><li>Insert into a section whose header carries no counts (sachviet lifecycle layout): row lands, no header edit (t05).</li><li>Insert into an empty section (placeholder line only): placed as the first row of the block (t05).</li><li>CRLF backlog files: bytes preserved outside the mutated line; the tool neither normalizes nor introduces line-ending drift (t07's whole-file diff proves it).</li><li>Unicode titles in rows: stem sort is bytewise on the stem token only, titles never affect placement (t05).</li><li>Concurrency: two flips racing is out of scope by design - single-writer discipline stays with the workflow; the drift refusal is the guard when it is violated anyway (documented; t04 is the mechanism).</li><li>Security-class: tools read and write only the declared files under docs/tasks/; no shell-out, no network, no eval; <code>--json</code> output is data. Covered by code review plus t07's footprint diff.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<p>Duplicated intentionally with <code>## Scope</code> for template conformance: no contract semantics changes, no BRAIN emission, no coverage scoping.</p>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>Frontmatter status remains the record of truth; the backlog stays an index (tools repair toward frontmatter, never the reverse).</li><li>backlog-state-update grammar authority is unchanged; the tool encodes, never redefines.</li><li>Payload sync doctrine: rebuild, version-sync, full suite before commit.</li><li>HITL: both human-acceptance gates are recorded verdicts; the agent never sets done.</li></ul>\n<p><em>End of TASK-IMP-085.</em></p>";
