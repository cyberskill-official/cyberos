window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-105"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The <code>memory-capture@1</code> skill bundle <strong>MUST</strong> be the canonical, signed, OCI-distributed SDK for emitting memory capture rows. The bundle:</p>\n<ol><li><strong>MUST</strong> ship as a single <code>.skill</code> artifact containing: (a) the Rust skill binary <code>main</code> (broker subprocess entrypoint), (b) a Rust SDK crate (<code>cyberos-memory-capture</code>), (c) a Python wrapper (<code>cyberos_memory_capture</code>), (d) a bash CLI (<code>cyberos-memory-capture</code>). All three language frontends route through the same Rust core.</li><li><strong>MUST</strong> carry a valid SKILL.md frontmatter per TASK-SKILL-103 schema v1:</li></ol>\n<ul><li><code>id: memory-capture</code>, <code>version: 1.0.0</code>, <code>description: \"Canonical entry point for emitting memory capture rows.\"</code></li><li><code>allowed_tools: [MemoryEmit]</code> (only — never Bash, Read, HttpFetch).</li><li><code>allowed_memory_scopes: [\"memories/**\"]</code> (broad emit scope; finer-grained scoping is the caller's frontmatter responsibility).</li><li><code>sync_class: shareable</code> (rows emitted by this skill MAY sync to Cloud memory per TASK-MEMORY-106).</li><li><code>signature</code> populated via TASK-SKILL-102 signing pipeline.</li></ul>\n<ol><li><strong>MUST</strong> expose the following stable API in the Rust SDK: ```rust pub struct MemoryCapture { /* opaque */ }</li></ol>\n<p>    impl MemoryCapture {         pub async fn new() -&gt; Result&lt;Self, CaptureError&gt;;         pub async fn emit(&amp;self, kind: &amp;str, payload: serde_json::Value, scope: EmitScope) -&gt; Result&lt;EmitOutcome, CaptureError&gt;;     }</p>\n<p>    pub struct EmitOutcome { pub row_id: String, pub seq: u64, pub trace_id: String, pub deduped: bool }</p>\n<p>    pub enum EmitScope {         Private,                                  // never sync         Shareable { acl: Vec&lt;String&gt; },           // sync per AGENTS.md §15     }</p>\n<p>    pub enum CaptureError { BrokerDown, Denied(String), PiiScrubFailed, Timeout, InvalidKind, InvalidPayload }     ```</p>\n<ol><li><strong>MUST</strong> expose the equivalent Python API: ```python from cyberos_memory_capture import MemoryCapture, EmitScope, CaptureError</li></ol>\n<p>    cb = MemoryCapture()     outcome = cb.emit(         kind=\"notes.captured\",         payload={\"title\": \"Auth design\", \"body\": \"...\"},         scope=EmitScope.shareable(acl=[\"@alice\", \"@bob\"]),     )     print(outcome.row_id, outcome.deduped)     ```</p>\n<ol><li><strong>MUST</strong> expose the bash CLI: ``<code>text $ echo '{\"title\":\"x\",\"body\":\"y\"}' | cyberos-memory-capture --kind notes.captured --scope private {\"row_id\":\"...\",\"seq\":42,\"trace_id\":\"0af7...\",\"deduped\":false} </code>``</li><li><strong>MUST</strong> validate <code>kind</code> against the canonical kind regex <code>^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$</code> (e.g. <code>notes.captured</code>, <code>tool.invoked</code>). Invalid kind → <code>CaptureError::InvalidKind</code>.</li><li><strong>MUST</strong> dedup identical emits at SDK level before broker call:</li></ol>\n<ul><li>Compute <code>content_hash = blake3(canonical_json(kind, payload, scope))</code>.</li><li>Check in-memory LRU (size 1000, TTL 60s).</li><li>On hit → return <code>EmitOutcome { deduped: true, ... }</code> with the prior outcome's row_id; no broker call.</li><li>On miss → call broker; cache result.</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub payload via TASK-MEMORY-111's ruleset BEFORE broker call. Scrub failure → <code>CaptureError::PiiScrubFailed</code>; caller can retry with <code>payload_sanitized = true</code> flag (telling SDK that caller has already sanitised and we trust the input).</li><li><strong>MUST</strong> auto-discover the broker socket from <code>CYBEROS_BROKER_SOCKET</code> env var (set by TASK-SKILL-104 when subprocess is launched). Outside a broker invocation (e.g. ad-hoc CLI use), the SDK connects to the default broker socket <code>/run/cyberos/broker.sock</code> (system-scope) or <code>~/.cyberos/broker.sock</code> (user-scope).</li><li><strong>MUST</strong> handle broker-down with exponential backoff: 3 retries at 100ms, 500ms, 2s. If all fail → <code>CaptureError::BrokerDown</code>; SDK returns immediately (does not block caller indefinitely).</li><li><strong>MUST</strong> propagate W3C TraceContext: read <code>TRACEPARENT</code> env var (set by upstream) or generate a new trace_id per <code>MemoryCapture::new()</code> call. Every emit carries this trace_id.</li><li><strong>MUST</strong> emit OTel span <code>skill.memory_capture.emit</code> per call with attributes <code>kind</code>, <code>dedup_hit</code>, <code>payload_size_bytes</code>, <code>pii_scrub_match_count</code>, <code>duration_ms</code>.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>skill_memory_capture_emits_total{kind, outcome}</code> (counter; outcome ∈ emitted | deduped | denied | broker_down | pii_failed).</li><li><code>skill_memory_capture_dedup_hit_ratio</code> (gauge).</li></ul>\n<ol><li><strong>MUST</strong> be signed via TASK-SKILL-102 release pipeline; the OCI tag is <code>oci://registry.cyberos.world/skills/memory-capture:1.0.0</code>; immutable.</li><li><strong>SHOULD</strong> provide a TypeScript SDK in slice 3+ (placeholder; web/node integrations).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a skill at all (§1 #1)?</strong> Three motives. First, ENFORCEMENT: routing emit through the TASK-SKILL-104 capability broker ensures every memory write is authorised, audited, and PII-scrubbed (no path that bypasses these). Second, IDEMPOTENCY: the SDK's dedup layer (§1 #7) cuts duplicate emit load by ~10× under realistic churn. Third, DISTRIBUTION: a signed OCI artifact gives operators a single answer to \"how do I emit a memory?\" — <code>cargo add cyberos-memory-capture</code> or <code>pip install cyberos-memory-capture</code>.</p>\n<p><strong>Why three language frontends (§1 #1)?</strong> Rust SDK is the canonical caller (other services). Python wrapper is for data-science scripts (operators routinely have Jupyter notebooks). Bash CLI is for shell scripts + ops one-liners. All three route through the same Rust core for consistency.</p>\n<p><strong>Why allowed_tools = [MemoryEmit] only (§1 #2)?</strong> Defense-in-depth. The skill has zero need for Bash/Read/HttpFetch; granting them would expand attack surface. The capability broker enforces this — if a future SKILL.md PR adds Bash by mistake, the broker rejects every Bash call at invoke time.</p>\n<p><strong>Why kind regex (§1 #6)?</strong> A namespaced kind (<code>module.event_type</code>) lets operators query \"all rows from the notes module\" via prefix match. Without regex enforcement, kinds drift into <code>notes-captured</code> vs <code>notes.captured</code> vs <code>notes_captured</code> chaos. The regex locks the convention; TASK-AI-003's closed-set list is the authoritative registry.</p>\n<p><strong>Why dedup TTL 60s (§1 #7)?</strong> Empirical: cron-style emitters (status pings, watchdog heartbeats) repeat at minute-scale; same-minute repeats are duplicates. Repeats &gt; 1 minute apart are probably meaningful (system state changed). 60s splits the population well; LRU(1000) covers ~16 unique kind/payload combos per minute (more than enough for a typical service).</p>\n<p><strong>Why PII-scrub before broker call (§1 #8)?</strong> The broker also runs TASK-MEMORY-111's scan (defense-in-depth) but pre-scrubbing means: (a) payload size is smaller on the wire, (b) broker rejects only true PII slips (not \"every emit needs scrubbing\"). The <code>payload_sanitized = true</code> flag lets explicit callers skip the SDK-level scrub — useful when the payload is already known-clean (e.g. generated structured data, not user text).</p>\n<p><strong>Why exp backoff on broker-down (§1 #10)?</strong> A transient broker restart shouldn't crash callers. 3 retries × spaced-100ms-500ms-2s gives the broker time to recover before we surrender. Total wait &lt; 3 seconds = bounded caller delay.</p>\n<p><strong>Why W3C trace propagation (§1 #11)?</strong> Traces let operators correlate \"user prompt → tool call → memory row\" across service boundaries. Without propagation, the memory row appears orphaned; with it, OBS dashboards show the full chain.</p>\n<p><strong>Why TypeScript SDK deferred (§1 #15)?</strong> Slice 1+2 has no node/web callers. Adding TS now is YAGNI. When portal (slice 3+) needs to emit from browser → backend, TS SDK lands.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>SKILL.md</h3>\n<pre><code class=\"language-markdown\">---\nid: memory-capture\nversion: 1.0.0\ndescription: Canonical entry point for emitting memory capture rows.\nallowed_memory_scopes:\n  - memories/**\nallowed_tools:\n  - MemoryEmit\nsync_class: shareable\ntenant_scope: any\neffort_minutes: 5\ntags: [memory, capture, foundation, sdk]\nsignature:\n  algo: ed25519\n  public_key_hex: \"&lt;release-time-populated&gt;\"\n  signature_hex:  \"&lt;release-time-populated&gt;\"\n---\n\n# memory-capture@1\n\nThe canonical entry point for emitting memory capture rows.\n\n## Quick start (Rust)\n\n```rust\nuse cyberos_memory_capture::{MemoryCapture, EmitScope};\n\nlet cb = MemoryCapture::new().await?; let outcome = cb.emit(\n    \"notes.captured\",\n    serde_json::json!({ \"title\": \"Auth design\", \"body\": \"...\" }),\n    EmitScope::Shareable { acl: vec![\"@alice\".into()] },\n).await?; println!(\"emitted row {} (deduped={})\", outcome.row_id, outcome.deduped);</code></pre>\n<h2>Quick start (Python)</h2>\n<pre><code class=\"language-python\">from cyberos_memory_capture import MemoryCapture, EmitScope cb = MemoryCapture() outcome = cb.emit(kind=\"notes.captured\", payload={\"title\": \"x\"}, scope=EmitScope.private())</code></pre>\n<h2>Quick start (bash)</h2>\n<pre><code class=\"language-bash\">echo '{\"title\":\"x\"}' | cyberos-memory-capture --kind notes.captured --scope private</code></pre>\n<pre><code>\n### Rust SDK\n\n```rust\n// skills/memory-capture/src/lib.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Debug, thiserror::Error)]\npub enum CaptureError {\n    #[error(\"broker socket unreachable after 3 retries\")] BrokerDown,\n    #[error(\"broker denied: {0}\")]                        Denied(String),\n    #[error(\"PII scrub failed; retry with payload_sanitized=true if known-clean\")] PiiScrubFailed,\n    #[error(\"broker call timed out (&gt; effort_minutes deadline)\")] Timeout,\n    #[error(\"invalid kind (must match ^[a-z][a-z0-9_]*\\\\.[a-z][a-z0-9_]*$): {0}\")] InvalidKind(String),\n    #[error(\"invalid payload: {0}\")]                      InvalidPayload(String),\n}\n\n#[derive(Clone, Debug, Serialize, Deserialize)]\npub enum EmitScope {\n    Private,\n    Shareable { acl: Vec&lt;String&gt; },\n}\n\n#[derive(Clone, Debug, Serialize, Deserialize)]\npub struct EmitOutcome {\n    pub row_id:   String,\n    pub seq:      u64,\n    pub trace_id: String,\n    pub deduped:  bool,\n}\n\npub struct MemoryCapture {\n    socket_path: std::path::PathBuf,\n    trace_id:    String,\n    dedup:       lru::LruCache&lt;[u8; 32], EmitOutcome&gt;,\n}\n\nimpl MemoryCapture {\n    pub async fn new() -&gt; Result&lt;Self, CaptureError&gt; {\n        let socket_path = std::env::var(\"CYBEROS_BROKER_SOCKET\")\n            .map(std::path::PathBuf::from)\n            .unwrap_or_else(|_| default_broker_socket());\n        let trace_id = std::env::var(\"TRACEPARENT\")\n            .ok()\n            .and_then(parse_traceparent_for_trace_id)\n            .unwrap_or_else(generate_trace_id);\n        Ok(Self {\n            socket_path,\n            trace_id,\n            dedup: lru::LruCache::new(std::num::NonZeroUsize::new(1000).unwrap()),\n        })\n    }\n\n    pub async fn emit(&amp;mut self, kind: &amp;str, payload: serde_json::Value, scope: EmitScope)\n        -&gt; Result&lt;EmitOutcome, CaptureError&gt;\n    {\n        // §1 #6: kind regex\n        if !is_valid_kind(kind) { return Err(CaptureError::InvalidKind(kind.into())); }\n\n        // §1 #7: dedup\n        let key = compute_dedup_key(kind, &amp;payload, &amp;scope);\n        if let Some(prior) = self.dedup.get(&amp;key) {\n            metrics::counter!(\"skill_memory_capture_emits_total\", \"kind\" =&gt; kind.to_owned(), \"outcome\" =&gt; \"deduped\").increment(1);\n            return Ok(EmitOutcome { deduped: true, ..prior.clone() });\n        }\n\n        // §1 #8: PII scrub (calls broker's MemoryEmit which itself runs TASK-MEMORY-111;\n        // but we ALSO pre-scrub here to reduce broker work)\n        let scrubbed = pre_scrub_payload(&amp;payload).map_err(|_| CaptureError::PiiScrubFailed)?;\n\n        // §1 #9 #10: broker call with retry\n        let outcome = self.call_broker_with_retry(kind, scrubbed, &amp;scope).await?;\n\n        // Cache for future dedup\n        self.dedup.put(key, outcome.clone());\n        metrics::counter!(\"skill_memory_capture_emits_total\", \"kind\" =&gt; kind.to_owned(), \"outcome\" =&gt; \"emitted\").increment(1);\n        Ok(outcome)\n    }\n\n    async fn call_broker_with_retry(&amp;self, kind: &amp;str, payload: serde_json::Value, scope: &amp;EmitScope)\n        -&gt; Result&lt;EmitOutcome, CaptureError&gt;\n    {\n        let delays = [std::time::Duration::from_millis(100),\n                      std::time::Duration::from_millis(500),\n                      std::time::Duration::from_secs(2)];\n        for (i, delay) in delays.iter().enumerate() {\n            match call_broker_once(&amp;self.socket_path, kind, &amp;payload, scope, &amp;self.trace_id).await {\n                Ok(o) =&gt; return Ok(o),\n                Err(CaptureError::BrokerDown) if i + 1 &lt; delays.len() =&gt; {\n                    tokio::time::sleep(*delay).await;\n                    continue;\n                }\n                Err(e) =&gt; return Err(e),\n            }\n        }\n        Err(CaptureError::BrokerDown)\n    }\n}\n\nfn is_valid_kind(kind: &amp;str) -&gt; bool {\n    static RX: once_cell::sync::Lazy&lt;regex::Regex&gt; =\n        once_cell::sync::Lazy::new(|| regex::Regex::new(r\"^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$\").unwrap());\n    RX.is_match(kind)\n}\n\nfn compute_dedup_key(kind: &amp;str, payload: &amp;serde_json::Value, scope: &amp;EmitScope) -&gt; [u8; 32] {\n    let canonical = serde_json::to_string(&amp;serde_json::json!({\n        \"kind\": kind, \"payload\": payload, \"scope\": scope,\n    })).unwrap();\n    *blake3::hash(canonical.as_bytes()).as_bytes()\n}</code></pre>\n<h3>Python wrapper (subprocess shim)</h3>\n<pre><code class=\"language-python\"># skills/memory-capture/sdk-python/cyberos_memory_capture/__init__.py\nimport json\nimport subprocess\nimport os\nfrom dataclasses import dataclass\nfrom typing import List, Optional, Union\n\nclass CaptureError(Exception): pass\nclass BrokerDown(CaptureError): pass\nclass Denied(CaptureError): pass\nclass PiiScrubFailed(CaptureError): pass\n\n@dataclass\nclass EmitScope:\n    private: bool\n    acl: Optional[List[str]] = None\n    @classmethod\n    def private_(cls):                           return cls(private=True)\n    @classmethod\n    def shareable(cls, acl: List[str] = None):  return cls(private=False, acl=acl or [])\n    def to_dict(self):\n        if self.private: return \"Private\"\n        return {\"Shareable\": {\"acl\": self.acl or []}}\n\n@dataclass\nclass EmitOutcome:\n    row_id: str\n    seq: int\n    trace_id: str\n    deduped: bool\n\nclass MemoryCapture:\n    def __init__(self):\n        self.binary = os.environ.get(\"CYBEROS_MEMORY_CAPTURE_BIN\", \"cyberos-memory-capture\")\n\n    def emit(self, kind: str, payload: dict, scope: EmitScope) -&gt; EmitOutcome:\n        req = {\"kind\": kind, \"payload\": payload, \"scope\": scope.to_dict()}\n        proc = subprocess.run(\n            [self.binary, \"--json\"],\n            input=json.dumps(req).encode(),\n            capture_output=True,\n            timeout=10,\n        )\n        if proc.returncode != 0:\n            err = proc.stderr.decode(\"utf-8\", errors=\"replace\")\n            if \"broker_down\" in err:        raise BrokerDown(err)\n            if \"denied\" in err:             raise Denied(err)\n            if \"pii_scrub_failed\" in err:   raise PiiScrubFailed(err)\n            raise CaptureError(err)\n        resp = json.loads(proc.stdout)\n        return EmitOutcome(**resp)</code></pre>\n<h3>Bash CLI wrapper</h3>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\n# skills/memory-capture/cli/cyberos-memory-capture\n# Reads JSON payload from stdin OR positional arg; prints EmitOutcome JSON to stdout.\nset -euo pipefail\n\nKIND=\"\"\nSCOPE=\"private\"\nACL=()\nJSON=false\nSANITISED=false\n\nwhile [[ $# -gt 0 ]]; do\n  case $1 in\n    --kind)             KIND=\"$2\";       shift 2 ;;\n    --scope)            SCOPE=\"$2\";      shift 2 ;;\n    --acl)              ACL+=(\"$2\");     shift 2 ;;\n    --json)             JSON=true;       shift   ;;\n    --payload-sanitised) SANITISED=true; shift   ;;\n    -h|--help)\n      cat &lt;&lt;EOF\nusage: cyberos-memory-capture --kind &lt;module.event&gt; [--scope private|shareable] [--acl @actor]* [--payload-sanitised]\n  stdin: JSON payload\n  output: EmitOutcome JSON (default) or human-readable text\nEOF\n      exit 0 ;;\n    *) echo \"unknown arg: $1\" &gt;&amp;2; exit 1 ;;\n  esac\ndone\n\n[ -n \"$KIND\" ] || { echo \"ERROR: --kind required\" &gt;&amp;2; exit 1; }\nPAYLOAD=$(cat)\n\n# Invoke the actual skill binary via broker socket (skill binary is installed in $PATH at OCI install time)\nexec cyberos-memory-capture-main --kind \"$KIND\" --scope \"$SCOPE\" \\\n  $(printf -- \"--acl %s \" \"${ACL[@]:-}\") \\\n  ${SANITISED:+--payload-sanitised} \\\n  &lt;&lt;&lt; \"$PAYLOAD\"</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Rust SDK: emit succeeds</strong> — <code>MemoryCapture::new()</code> + <code>emit(\"notes.captured\", ..., Private)</code> → <code>EmitOutcome { row_id != \"\", deduped: false }</code>.</li><li><strong>Rust SDK: dedup hit</strong> — second identical emit within 60s → <code>EmitOutcome { deduped: true, row_id == prior.row_id }</code>.</li><li><strong>Rust SDK: kind regex enforced</strong> — <code>emit(\"InvalidKind\", ...)</code> → <code>Err(InvalidKind)</code>.</li><li><strong>Rust SDK: broker-down retry</strong> — broker down for 1s then up → SDK retries 3×; succeeds on 3rd; total delay &lt; 3s.</li><li><strong>Rust SDK: broker-down surrender</strong> — broker down for 30s → <code>Err(BrokerDown)</code> after retries.</li><li><strong>Rust SDK: PII scrub</strong> — payload with <code>AKIA0123456789012345</code> → scrubbed before broker call; broker receives <code>&lt;AWS_KEY&gt;</code> instead.</li><li><strong>Rust SDK: payload_sanitised flag</strong> — flag set → SDK skips its own scrub; broker still scrubs (defense-in-depth).</li><li><strong>Rust SDK: W3C trace_id from env</strong> — <code>TRACEPARENT=00-0af...-...-01</code> → <code>EmitOutcome.trace_id == \"0af...\"</code>.</li><li><strong>Rust SDK: W3C trace_id generated when env absent</strong> — no TRACEPARENT → fresh 32-char hex trace_id per <code>new()</code>.</li><li><strong>Python SDK: emit succeeds</strong> — equivalent of #1 from Python.</li><li><strong>Python SDK: dedup hit</strong> — equivalent of #2.</li><li><strong>Python SDK: exception types</strong> — broker down → <code>BrokerDown</code> exception; denied → <code>Denied</code>; PII failure → <code>PiiScrubFailed</code>.</li><li><strong>Bash CLI: stdin JSON</strong> — <code>echo '{\"x\":1}' | cyberos-memory-capture --kind a.b</code> → <code>EmitOutcome</code> JSON to stdout, exit 0.</li><li><strong>Bash CLI: missing --kind</strong> — <code>cyberos-memory-capture</code> → exit 1 with stderr.</li><li><strong>Bash CLI: --acl multi</strong> — <code>--acl @alice --acl @bob</code> → scope becomes <code>Shareable { acl: [\"@alice\", \"@bob\"] }</code>.</li><li><strong>SKILL.md validates</strong> — <code>cyberos skill validate skills/memory-capture/</code> → exit 0 (TASK-SKILL-103 validator).</li><li><strong>SKILL.md signature verified</strong> — release-time signature → validate returns <code>signature: ok</code>.</li><li><strong>Broker enforcement: only MemoryEmit allowed</strong> — try to call <code>Bash</code> from inside this skill → broker returns <code>tool_not_in_allowed_tools</code>.</li><li><strong>Bundle signed via TASK-SKILL-102</strong> — <code>sign-bundle.sh</code> produces <code>.skill</code> artifact; <code>cyberos skill verify &lt;artifact&gt;</code> returns <code>signed_by: &lt;key&gt;</code>.</li><li><strong>Dedup TTL: 60s expiry</strong> — first emit; wait 61s; identical emit → fresh (not deduped).</li><li><strong>OTel span per emit</strong> — exporter receives <code>skill.memory_capture.emit</code> with attrs.</li><li><strong>Metric: outcome counter</strong> — counters <code>skill_memory_capture_emits_total{outcome=\"emitted\"}</code>, <code>{outcome=\"deduped\"}</code> increment correctly.</li><li><strong>CYBEROS_BROKER_SOCKET env override</strong> — set env to <code>/tmp/test.sock</code>; SDK connects to that socket.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// skills/memory-capture/tests/sdk_rust_test.rs\n\n#[tokio::test]\nasync fn emit_succeeds_against_stub_broker() {\n    let broker = StubBroker::start().await;\n    std::env::set_var(\"CYBEROS_BROKER_SOCKET\", broker.socket_path());\n    let mut cb = MemoryCapture::new().await.unwrap();\n    let outcome = cb.emit(\"notes.captured\", json!({\"title\": \"x\"}), EmitScope::Private).await.unwrap();\n    assert!(!outcome.deduped);\n    assert!(outcome.row_id.starts_with(\"row-\"));\n}\n\n#[tokio::test]\nasync fn dedup_within_60s() {\n    let broker = StubBroker::start().await;\n    std::env::set_var(\"CYBEROS_BROKER_SOCKET\", broker.socket_path());\n    let mut cb = MemoryCapture::new().await.unwrap();\n    let first  = cb.emit(\"notes.captured\", json!({\"title\": \"y\"}), EmitScope::Private).await.unwrap();\n    let second = cb.emit(\"notes.captured\", json!({\"title\": \"y\"}), EmitScope::Private).await.unwrap();\n    assert_eq!(first.row_id, second.row_id);\n    assert!(!first.deduped);\n    assert!(second.deduped);\n    assert_eq!(broker.call_count().await, 1);  // only one broker call\n}\n\n#[tokio::test]\nasync fn invalid_kind_rejected() {\n    let mut cb = MemoryCapture::test().await;\n    let err = cb.emit(\"InvalidCamelCase\", json!({}), EmitScope::Private).await.unwrap_err();\n    assert!(matches!(err, CaptureError::InvalidKind(_)));\n}\n\n#[tokio::test]\nasync fn pii_scrubbed_before_broker() {\n    let broker = StubBroker::start().await;\n    std::env::set_var(\"CYBEROS_BROKER_SOCKET\", broker.socket_path());\n    let mut cb = MemoryCapture::new().await.unwrap();\n    let _ = cb.emit(\"notes.captured\", json!({\"body\": \"key AKIA0123456789012345\"}), EmitScope::Private).await;\n    let last = broker.last_payload().await;\n    assert!(!last.to_string().contains(\"AKIA0123456789012345\"));\n    assert!(last.to_string().contains(\"&lt;AWS_KEY&gt;\"));\n}\n\n#[tokio::test]\nasync fn broker_down_retries_3_times_then_surrenders() {\n    std::env::set_var(\"CYBEROS_BROKER_SOCKET\", \"/tmp/no-such-socket\");\n    let mut cb = MemoryCapture::new().await.unwrap();\n    let start = std::time::Instant::now();\n    let err = cb.emit(\"notes.captured\", json!({}), EmitScope::Private).await.unwrap_err();\n    assert!(matches!(err, CaptureError::BrokerDown));\n    let elapsed = start.elapsed();\n    assert!(elapsed &gt;= Duration::from_millis(2600));  // 100 + 500 + 2000 = 2600ms minimum\n    assert!(elapsed &lt;  Duration::from_secs(5));\n}</code></pre>\n<pre><code class=\"language-python\"># skills/memory-capture/tests/sdk_python_test.py\nimport subprocess\nimport json\n\ndef test_emit_succeeds(stub_broker):\n    from cyberos_memory_capture import MemoryCapture, EmitScope\n    cb = MemoryCapture()\n    outcome = cb.emit(\"notes.captured\", {\"title\": \"py\"}, EmitScope.private_())\n    assert outcome.row_id\n    assert not outcome.deduped\n\ndef test_dedup_python(stub_broker):\n    from cyberos_memory_capture import MemoryCapture, EmitScope\n    cb = MemoryCapture()\n    a = cb.emit(\"notes.captured\", {\"x\": 1}, EmitScope.private_())\n    b = cb.emit(\"notes.captured\", {\"x\": 1}, EmitScope.private_())\n    assert b.deduped\n    assert a.row_id == b.row_id</code></pre>\n<pre><code class=\"language-bash\"># skills/memory-capture/tests/cli_e2e_test.sh\nset -euo pipefail\n\necho '{\"title\":\"cli\"}' | cyberos-memory-capture --kind notes.captured --json &gt; /tmp/result.json\n[ \"$(jq -r .row_id /tmp/result.json | head -c 4)\" = \"row-\" ]\n[ \"$(jq -r .deduped /tmp/result.json)\" = \"false\" ]\n\n# Missing --kind\n! cyberos-memory-capture --json &lt; /dev/null 2&gt;&amp;1 | grep -q \"ERROR\"\n\n# Idempotent\necho '{\"x\":2}' | cyberos-memory-capture --kind a.b --json &gt; /tmp/r1.json\necho '{\"x\":2}' | cyberos-memory-capture --kind a.b --json &gt; /tmp/r2.json\n[ \"$(jq -r .deduped /tmp/r2.json)\" = \"true\" ]</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-SKILL-103 (upstream)</strong> — frontmatter schema.</li><li><strong>TASK-SKILL-104 (upstream)</strong> — capability broker enforces <code>allowed_tools: [MemoryEmit]</code>.</li><li><strong>TASK-SKILL-102</strong> — OCI distribution + signature pipeline.</li><li><strong>TASK-MEMORY-101</strong> — MemoryWriter (called via broker).</li><li><strong>TASK-MEMORY-106</strong> — sync_class enforcement.</li><li><strong>TASK-MEMORY-107</strong> — capture daemon (handles emit forwarding).</li><li><strong>TASK-MEMORY-109</strong> — Claude Code hook is a SIBLING capture path (also writes via TASK-MEMORY-107).</li><li><strong>TASK-MEMORY-111</strong> — PII detection used inside SDK pre-scrub.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>EmitOutcome (Rust)</h3>\n<pre><code class=\"language-json\">{\n  \"row_id\":   \"row-01HZK9R8M3X5C8Q4ABCDEF\",\n  \"seq\":      4287,\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"deduped\":  false\n}</code></pre>\n<h3>Dedup hit (subsequent call)</h3>\n<pre><code class=\"language-json\">{\n  \"row_id\":   \"row-01HZK9R8M3X5C8Q4ABCDEF\",\n  \"seq\":      4287,\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"deduped\":  true\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>TypeScript SDK — slice 3+ (per §1 #15).</li><li>Streaming emit (emit a large body in chunks) — slice 4+; current 16 MB body cap suffices.</li><li>Schema-versioning of payload (consumer-side enforced) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Broker socket missing</td><td>UnixStream::connect Err</td><td>exp backoff retry; final <code>BrokerDown</code></td><td>Wait for TASK-MEMORY-110 to restart</td></tr><tr><td>Broker rejects (denied)</td><td>JSON-RPC error</td><td><code>CaptureError::Denied(reason)</code></td><td>Caller fixes frontmatter</td></tr><tr><td>PII scrub crashes</td><td>catch_unwind</td><td><code>PiiScrubFailed</code>; caller may retry with sanitised flag</td><td>Author updates ruleset</td></tr><tr><td>Invalid kind</td><td>regex no-match</td><td><code>InvalidKind</code></td><td>Caller fixes kind string</td></tr><tr><td>Payload not serializable</td><td>serde_json::to_string Err</td><td><code>InvalidPayload</code></td><td>Caller passes serde-compatible value</td></tr><tr><td>Dedup cache full</td><td>LRU evicts oldest</td><td>New entry replaces; possible false-miss after 1000 unique emits</td><td>Acceptable</td></tr><tr><td>Subprocess (Python wrapper) timeout</td><td>subprocess.run timeout=10</td><td>CaptureError raised</td><td>Python caller retries</td></tr><tr><td>Subprocess return code != 0</td><td>stderr parse</td><td>Specific exception per error class</td><td>Python caller handles</td></tr><tr><td>W3C trace_id env malformed</td><td>parse fails</td><td>Falls back to generated trace_id</td><td>Logged WARN</td></tr><tr><td>Multiple SDK instances dedup-cache-mismatch</td><td>each has own LRU</td><td>Slight inefficiency (duplicate emits across instances)</td><td>Acceptable (broker-side dedup catches)</td></tr><tr><td>OTel exporter unavailable</td><td>metric buffered</td><td>Metric eventually dropped after buffer fills</td><td>Operator restarts TASK-OBS-001</td></tr><tr><td>Signature missing at sign-bundle.sh time</td><td>release key missing</td><td>sign-bundle fails; release blocked</td><td>Operator restores key</td></tr><tr><td>OCI registry push fails</td><td>crane push Err</td><td>Release blocked</td><td>Operator retries</td></tr><tr><td>Python pyo3 native build fails</td><td>wheel build Err</td><td>Skill installable as subprocess shim (current default)</td><td>By design</td></tr><tr><td>Memory leak in long-running caller</td><td>LRU bounded</td><td>Cache size capped; safe</td><td>None</td></tr><tr><td><code>payload_sanitised: true</code> lies (raw PII slips through)</td><td>Broker's TASK-MEMORY-111 scan catches it</td><td>Broker denies; caller error</td><td>Caller stops lying</td></tr><tr><td>Concurrent emit on same SDK instance</td><td>Mutex on dedup cache</td><td>Serialised; throughput bounded but correct</td><td>Use multiple instances for parallelism</td></tr><tr><td>Broker upgrade mid-emit</td><td>connection drop</td><td>exp backoff catches new broker</td><td>Acceptable</td></tr><tr><td>Bash CLI subprocess SIGKILL by parent</td><td>broker call may complete or not</td><td>Exit non-zero; caller retries</td><td>Caller responsibility</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The bundle ships as a single <code>.skill</code> tar.zst per TASK-SKILL-102 format; contents: <code>main</code> binary + <code>SKILL.md</code> + <code>sdk-rust/</code> + <code>sdk-python/</code> + <code>cli/</code>. Total bundle size target: &lt; 5 MB.</li><li>The Rust SDK is published to crates.io as <code>cyberos-memory-capture = \"1.0\"</code>; Python wrapper to PyPI as <code>cyberos-memory-capture</code>; both depend on the binary being installed via OCI.</li><li>The Python wrapper is a subprocess shim (not pyo3) for simpler distribution: pure-Python wheel, no native build. Trade-off: ~10ms per call for subprocess spawn. Acceptable for typical capture workloads.</li><li>The dedup LRU is per-SDK-instance, not global. Long-lived callers (daemons) keep one MemoryCapture instance; short-lived callers (scripts) get fresh dedup each invocation. Broker-side has its own dedup as defense-in-depth.</li><li><code>payload_sanitised: true</code> is honoured by the SDK but the broker still runs TASK-MEMORY-111 scrub. Pre-scrub is a performance optimisation, not a security boundary.</li><li>The <code>kind</code> regex (<code>module.event_type</code>) is shared with TASK-AI-003's closed-set list; this skill emits canonical kinds documented there.</li><li>The bash CLI delegates to the binary (<code>cyberos-memory-capture-main</code>); the wrapper just parses flags and constructs JSON. This keeps the bash file &lt; 50 lines.</li><li>The Python <code>EmitScope.private_()</code> method name has a trailing underscore because <code>private</code> is a reserved-adjacent Python keyword in some contexts; we use <code>private_</code> for clarity.</li><li>The release sign-bundle.sh is a thin wrapper around TASK-SKILL-102's signing pipeline; it doesn't duplicate signing logic, just composes the inputs.</li><li>TypeScript SDK is deferred to slice 3+; placeholder noted to prevent author confusion about scope.</li></ul>\n<hr>\n<p><em>End of TASK-SKILL-105.</em></p>";
