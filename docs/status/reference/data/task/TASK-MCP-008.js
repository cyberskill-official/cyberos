window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MCP-008"]="<h1>TASK-MCP-008: MCP elicitation store with confirmation round-trip</h1>\n<h2>Summary</h2>\n<p>Ship the MCP elicitation primitive in <code>services/mcp-gateway/</code>: closed 5-type / 5-status enums, fixed per-type response schemas, in-memory <code>ElicitationStore</code> for the no-DB path, Postgres store-of-record via <code>elicitation_pg.rs</code> + migration <code>0016_mcp_elicitations.sql</code>, and REST poll/respond/cancel. The load-bearing use is confirmation round-trip for TASK-MCP-006 destructive gating; enums/schemas for other types (including <code>file_upload</code>) exist, but full infra for NATS, LISTEN/NOTIFY, S3 upload, rate limit, timeout sweeper, and prune is deferred.</p>\n<h2>Problem</h2>\n<p>Server-initiated mid-call prompts are part of MCP 2025-11-25. Without them, missing args force fail-and-retry, and destructive tools cannot present a structured confirm/decline. The original engineering-spec targeted <code>services/mcp/src/elicitation/</code> with NATS push, LISTEN/NOTIFY wakeups, S3 presign, timeout jobs, and twelve <code>elicitation_*</code> integration test files — that tree was never built. As-built lives under <code>mcp-gateway</code> as two modules + one migration, with real in-crate and router tests.</p>\n<h2>Proposed Solution</h2>\n<p><strong>In-memory (<code>elicitation.rs</code>):</strong> <code>ElicitationType</code> / <code>ElicitationStatus</code> (cardinality 5 each), <code>response_schema</code>, <code>validate_response</code>, <code>ElicitationStore</code> with create / create_confirmation / respond / cancel / pending / confirmation_state / is_confirmed, retry cap <code>MAX_RETRIES=3</code>, idempotent resubmit.</p>\n<p><strong>Postgres (<code>elicitation_pg.rs</code> + <code>0016_mcp_elicitations.sql</code>):</strong> write-through store-of-record when a pool + authenticated caller exist — create_confirmation, confirmation_state, pending, respond (KMS-sealed payload + denormalized <code>confirmed</code>), cancel. Caller-scoped (DEC-1159) in the handler; append-only GRANT model (GUC RLS deferred).</p>\n<p><strong>HTTP (<code>router.rs</code>):</strong> <code>GET /v1/mcp/elicitations</code>, <code>POST /v1/mcp/elicitations/:id/respond</code>, <code>POST /v1/mcp/elicitations/:id/cancel</code>. Destructive <code>tools/call</code> (TASK-MCP-006) creates a confirmation elicitation and consults <code>confirmation_state</code> on re-invoke.</p>\n<p><strong>Audit (<code>oauth/audit.rs</code>):</strong> emitters for requested / responded / cancelled / validation_failed; <code>elicitation_timeout</code> defined for the deferred sweeper.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Full NATS push + LISTEN/NOTIFY blocking wait inside tool workers.</strong> Deferred: sync gateway uses poll + re-invoke; no long-running worker wait loop in this adopt.</li><li><strong>S3-presigned file_upload end-to-end.</strong> Deferred: schema/enum exist; upload URL generation and object verify do not ship here.</li><li><strong>Multi-file <code>elicitation/{request,response,poll,cancel,timeout_job,validate,file_upload,nats_push}.rs</code> tree under <code>services/mcp/</code>.</strong> Rejected: as-built is <code>elicitation.rs</code> + <code>elicitation_pg.rs</code> in <code>mcp-gateway</code>.</li><li><strong>Enable Postgres RLS policies with <code>auth.tenant_id</code> GUC now.</strong> Deferred: gateway does not set that GUC yet; isolation is handler <code>caller_subject_id</code> checks (documented in migration 0016).</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: confirmation create → respond <code>{confirmed:true|false}</code> → gate proceeds or aborts; invalid payloads 422 then terminal at retry cap; cancel clears pending; DB path seals payloads and is caller-scoped.</li><li>Guardrail: unit + router tests green without claiming NATS/S3/sweeper/prune; DB persistence covered by <code>#[ignore]</code> integration test when Postgres is available.</li></ul>\n<h2>Scope</h2>\n<p>In scope (as-built under <code>services/mcp-gateway/</code>):</p>\n<ul><li><code>src/elicitation.rs</code> — types, schemas, validation, in-memory store + unit tests</li><li><code>src/elicitation_pg.rs</code> — PG store-of-record for confirmation + respond/cancel/pending</li><li><code>migrations/0016_mcp_elicitations.sql</code> — enums, table, indexes, writer grants</li><li>Router REST poll/respond/cancel + TASK-MCP-006 confirmation integration</li><li>Audit emitters in <code>oauth/audit.rs</code> for the lifecycle kinds used on the hot path</li><li><code>db_slice_test.rs::elicitation_persists_seals_and_is_caller_scoped</code> (Postgres-gated)</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>NATS push transport</li><li>Postgres LISTEN/NOTIFY wakeups for blocking tool-side <code>elicit()</code></li><li>S3 / TASK-DOC-001 presigned <code>file_upload</code> realization (enums/schemas may exist; infra deferred)</li><li>Elicitation creation rate limit</li><li>Timeout sweeper job (columns exist; sweeper deferred; <code>elicitation_timeout</code> audit reserved)</li><li>30-day response-payload prune job</li><li>GUC-based RLS policies (handler isolation only in this adopt)</li><li>Claimed <code>elicitation_*</code> integration test filenames that do not exist (cite real in-crate / router / db_slice tests)</li><li>Sync-tool ban / TaskCtx::elicit long-running API (TASK-MCP-007 worker story)</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-MCP-001, TASK-MCP-004]</code> — gateway router + authenticated callers. Soft: <strong>TASK-MCP-006</strong> consumes confirmation elicitation for destructive gating (reciprocal integration). Related: TASK-MCP-007 (task-bound elicitations / worker wait deferred).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>ElicitationType::ALL</code> and <code>ElicitationStatus::ALL</code> MUST each contain exactly five variants with snake_case wire labels matching migration <code>0016</code> enums.</li><li>1.2 <code>response_schema</code> + <code>validate_response</code> MUST implement the fixed per-type shapes (string_input, single_choice, multi_choice, confirmation, file_upload schema) without requiring a general JSON Schema engine.</li><li>1.3 In-memory <code>ElicitationStore</code> MUST support create_confirmation, respond (Recorded / Invalid / ValidationFailed / AlreadyRecorded), cancel, pending, and confirmation_state.</li><li>1.4 Invalid responses MUST increment retries up to <code>MAX_RETRIES=3</code> then transition to <code>validation_failed</code>.</li><li>1.5 Identical resubmit of an already-recorded payload MUST be idempotent (<code>AlreadyRecorded</code>).</li><li>1.6 Migration <code>0016_mcp_elicitations.sql</code> MUST create <code>mcp_elicitations</code> with the closed enums, TTL columns, <code>confirmed</code> denormalized column, and append-only writer grants.</li><li>1.7 When a DB pool + authenticated caller are present, router MUST use <code>elicitation_pg</code> for create/respond/cancel/pending and MUST scope rows to <code>caller_subject_id</code>.</li><li>1.8 Router MUST expose <code>GET /v1/mcp/elicitations</code>, <code>POST .../respond</code>, <code>POST .../cancel</code>, and TASK-MCP-006 MUST create/consult confirmation elicitations on destructive <code>tools/call</code>.</li><li>1.9 This adopt MUST NOT claim NATS, LISTEN/NOTIFY, S3 file_upload infra, rate limit, timeout sweeper, or prune as shipped.</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - type and status enums have exactly five variants with correct wire labels - test: <code>services/mcp-gateway/src/elicitation.rs::type_and_status_have_exactly_five_variants</code></li><li>[ ] AC 2 (traces_to: #1.2) - confirmation schema requires confirmed boolean - test: <code>services/mcp-gateway/src/elicitation.rs::confirmation_schema_requires_confirmed_boolean</code></li><li>[ ] AC 3 (traces_to: #1.2) - validate_response accepts bool and rejects missing/wrong types - test: <code>services/mcp-gateway/src/elicitation.rs::validate_confirmation_accepts_bool_rejects_missing</code></li><li>[ ] AC 4 (traces_to: #1.2) - string and choice types validate correctly - test: <code>services/mcp-gateway/src/elicitation.rs::validate_string_and_choice_types</code></li><li>[ ] AC 5 (traces_to: #1.3,#1.8) - confirmation round-trip accept and decline - test: <code>services/mcp-gateway/src/elicitation.rs::confirmation_round_trip_accept_and_decline</code></li><li>[ ] AC 6 (traces_to: #1.4) - invalid responses retry then go terminal - test: <code>services/mcp-gateway/src/elicitation.rs::invalid_responses_retry_then_go_terminal</code></li><li>[ ] AC 7 (traces_to: #1.3) - confirmation_state reflects approval, decline, and absence - test: <code>services/mcp-gateway/src/elicitation.rs::confirmation_state_reflects_approval_decline_and_absence</code></li><li>[ ] AC 8 (traces_to: #1.5) - idempotent resubmit and cancel - test: <code>services/mcp-gateway/src/elicitation.rs::idempotent_resubmit_and_cancel</code></li><li>[ ] AC 9 (traces_to: #1.8) - HTTP poll/respond/validate/404 - test: <code>services/mcp-gateway/src/router.rs::elicitation_poll_respond_validate_and_404</code></li><li>[ ] AC 10 (traces_to: #1.8) - destructive gating hold/confirm/decline via elicitation - test: <code>services/mcp-gateway/src/router.rs::destructive_tool_without_confirmation_is_held</code></li><li>[ ] AC 11 (traces_to: #1.6,#1.7) - PG path seals payload and is caller-scoped (Postgres required) - test: <code>services/mcp-gateway/src/db_slice_test.rs::elicitation_persists_seals_and_is_caller_scoped</code></li><li>[ ] AC 12 (traces_to: #1.9) - Out of scope lists NATS/LISTEN/NOTIFY/S3/rate-limit/sweeper/prune; no AC claims them shipped - verify: <code>docs/tasks/mcp/TASK-MCP-008-elicitation/spec.md</code> Scope → Out of scope</li></ul>\n<h2>Verification</h2>\n<pre><code class=\"language-bash\">cd services &amp;&amp; cargo test -p cyberos-mcp-gateway elicitation\ncd services &amp;&amp; cargo test -p cyberos-mcp-gateway --lib router::tests::elicitation_poll_respond_validate_and_404\ncd services &amp;&amp; cargo test -p cyberos-mcp-gateway --lib router::tests::destructive_\n# Postgres-gated (ignored without DATABASE_URL / local pool):\ncd services &amp;&amp; cargo test -p cyberos-mcp-gateway --lib db_slice_test::elicitation_persists_seals_and_is_caller_scoped -- --ignored</code></pre>\n<p>Real tests to cite (do <strong>not</strong> cite non-existent <code>elicitation_*</code> integration filenames):</p>\n<table><thead><tr><th>Path</th><th>Covers</th></tr></thead><tbody><tr><td><code>services/mcp-gateway/src/elicitation.rs</code> unit tests listed in ACs 1–8</td><td>Enums, schemas, store semantics</td></tr><tr><td><code>services/mcp-gateway/src/router.rs::elicitation_poll_respond_validate_and_404</code></td><td>REST surface</td></tr><tr><td><code>services/mcp-gateway/src/router.rs</code> destructive_* tests</td><td>TASK-MCP-006 confirmation integration</td></tr><tr><td><code>services/mcp-gateway/src/db_slice_test.rs::elicitation_persists_seals_and_is_caller_scoped</code></td><td>PG seal + caller scope + idempotency (<code>#[ignore]</code> without Postgres)</td></tr><tr><td><code>services/mcp-gateway/migrations/0016_mcp_elicitations.sql</code></td><td>Schema / enums (applied by migrate path)</td></tr></tbody></table>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) on branch <code>batch/9a-mcp</code>, rewriting the engineering-spec body into task@1 grammar against as-built <code>services/mcp-gateway/</code> sources.</li><li><strong>Scope:</strong> Spec re-scoped to in-memory + PG confirmation/REST surface; NATS, LISTEN/NOTIFY, S3 file_upload infra, rate limit, sweeper, and prune ledgered under Out of scope; paths corrected to <code>mcp-gateway</code>; ACs cite real tests only.</li><li><strong>Human review:</strong> Required at the two HITL gates; this file is the batch/9a-mcp adopt rework (<code>entered_via: rework</code>, <code>routed_back_count: 1</code>).</li></ul>\n<hr>\n<p><em>batch/9a-mcp adopt — TASK-MCP-008 re-spec against as-built mcp-gateway elicitation.</em></p>";
