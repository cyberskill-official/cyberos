window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TIME-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TIME service <strong>MUST</strong> ship the TimeEntry schema as the canonical append-only record of \"Member X spent N minutes on issue Y at time Z\". Each requirement:</p>\n<ol><li><strong>MUST</strong> define the <code>time_entries</code> table with the following columns (full DDL in §3.1):</li></ol>\n<ul><li><code>id UUID PRIMARY KEY</code> — row identity.</li><li><code>tenant_id UUID NOT NULL</code> — RLS partitioning key.</li><li><code>member_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code> — who performed the work.</li><li><code>engagement_id UUID NOT NULL</code> — the engagement billable target (per TASK-PROJ-005; placeholder FK at slice 1).</li><li><code>issue_id UUID NOT NULL</code> — the specific issue (per TASK-PROJ-001).</li><li><code>ts_start TIMESTAMPTZ NOT NULL</code> — entry start in UTC.</li><li><code>duration_minutes INT NOT NULL CHECK (duration_minutes BETWEEN 1 AND 1440)</code> — minimum 1 minute, maximum 24 hours (per DEC-227 + DEC-228).</li><li><code>entry_kind entry_kind NOT NULL</code> — closed enum: <code>regular | overtime | weekend | holiday</code>.</li><li><code>entry_status entry_status NOT NULL DEFAULT 'draft'</code> — closed enum: <code>draft | submitted | approved | reverted</code> (TASK-TIME-006 transitions).</li><li><code>billable BOOLEAN NOT NULL DEFAULT false</code> — set by TASK-TIME-005's cascade; this task declares the column only (per DEC-223).</li><li><code>rate_card_snapshot JSONB</code> — populated at entry creation by TASK-TIME-005; snapshot of the engagement rate-card at the row's instant (per DEC-224); empty <code>{}</code> at slice 1.</li><li><code>entry_currency CHAR(3) NOT NULL</code> — ISO-4217; defaulted from engagement.invoice_currency at row creation (per DEC-229).</li><li><code>description TEXT</code> — nullable; 0–1000 chars; PII-scrubbable.</li><li><code>correction_to UUID REFERENCES time_entries(id) DEFERRABLE INITIALLY IMMEDIATE</code> — nullable; non-null on correction rows (per DEC-220 + DEC-225).</li><li><code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>.</li><li><code>created_by_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>.</li></ul>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> (task-audit skill rule 13). Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>. Cross-tenant reads return 0 rows; cross-tenant writes fail <code>permission_denied</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>entry_kind</code> Postgres enum with exactly 4 values (per VN-1 working-time classification): <code>'regular'</code>, <code>'overtime'</code>, <code>'weekend'</code>, <code>'holiday'</code>. Adding a 5th is an ADR. Holiday-list driving the kind defaulting is TASK-TIME-007's responsibility; this task just stores the column.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>entry_status</code> Postgres enum with exactly 4 values: <code>'draft'</code>, <code>'submitted'</code>, <code>'approved'</code>, <code>'reverted'</code>. State transitions are TASK-TIME-006's responsibility; this task ships the column at default <code>'draft'</code>.</li></ol>\n<ol><li><strong>MUST</strong> be <strong>append-only</strong> at the SQL grant layer (per DEC-230 + task-audit skill rule 12). Migration applies <code>REVOKE UPDATE, DELETE ON time_entries FROM cyberos_app;</code>. Mutations write a fresh row with <code>correction_to</code> pointing at the prior row (per §1 #6 below).</li></ol>\n<ol><li><strong>MUST</strong> support <strong>correction via new row</strong> (per DEC-220). The handler <code>POST /v1/time/entries/{id}/correct</code> creates a new row with:</li></ol>\n<ul><li><code>correction_to = &lt;prior_id&gt;</code>.</li><li>All other fields from the body OR copied from prior if unspecified.</li><li><code>tenant_id</code>, <code>engagement_id</code>, <code>issue_id</code>, <code>member_subject_id</code> MUST be identical to the prior row (per §1 #11). Cross-engagement correction is forbidden.</li><li>The same audit emission contract as <code>create</code> but with kind <code>time.entry_corrected</code>. Corrections are themselves correctable (chains, not trees — per DEC-226).</li></ul>\n<ol><li><strong>MUST</strong> enforce <strong>acyclic correction chains</strong> (per DEC-225). A <code>BEFORE INSERT</code> trigger walks <code>correction_to</code> upward; if the walk visits the new row's <code>id</code> (via a future re-correction back), the insert is rejected with <code>correction_cycle_detected</code>. A CI test (<code>correction_acyclic_test</code>) seeds a 5-row chain + an attempted cycle and asserts the reject.</li></ol>\n<ol><li><strong>MUST</strong> enforce <strong>chain (not tree) topology</strong> (per DEC-226). At most one row may have <code>correction_to = &lt;prior_id&gt;</code> for any given <code>prior_id</code>. A <code>BEFORE INSERT</code> trigger checks <code>EXISTS (SELECT 1 FROM time_entries WHERE correction_to = $new.correction_to)</code>; if so → reject with <code>prior_row_already_corrected</code>. Reasoning: \"what is the current value of entry X?\" must be unambiguous; trees create N parallel current values.</li></ol>\n<ol><li><strong>MUST</strong> ship the <code>current_time_entries_view</code> SQL view filtering to \"effective rows only\" — rows that no other row supersedes via <code>correction_to</code>. Definition: <code>SELECT * FROM time_entries WHERE id NOT IN (SELECT correction_to FROM time_entries WHERE correction_to IS NOT NULL)</code>. Downstream tasks (TASK-TIME-005 billable cascade, TASK-TIME-009 rollup) MUST read from the view, not the raw table.</li></ol>\n<ol><li><strong>MUST</strong> validate at API layer:</li></ol>\n<ul><li><code>duration_minutes</code> between 1 and 1440 (per DEC-227 + DEC-228; the DB CHECK constraint duplicates).</li><li><code>ts_start</code> not in the future (clock-skew tolerance: +5 minutes; entries beyond that → 400 <code>ts_start_in_future</code>).</li><li><code>entry_kind</code> parses to closed enum (<code>unknown_entry_kind</code> otherwise).</li><li>On correction: <code>correction_to</code> row exists, belongs to the same tenant + engagement + issue + member.</li><li><code>description</code> length 0–1000 chars.</li></ul>\n<ol><li><strong>MUST</strong> reject corrections where any of <code>tenant_id</code>, <code>engagement_id</code>, <code>issue_id</code>, <code>member_subject_id</code> differ from the prior row. A trigger <code>enforce_correction_inheritance</code> raises <code>correction_cross_scope</code> on violation. Reasoning: a correction is \"I logged this entry wrong\" — never \"I logged this entry under the wrong engagement\"; the latter is a new entry + a <code>time.entry_recorded</code> row.</li></ol>\n<ol><li><strong>MUST</strong> emit memory audit row <code>time.entry_recorded</code> on every create (non-correction row) and <code>time.entry_corrected</code> on every correction. Both rows carry <code>{entry_id, tenant_id, member_subject_id_hash16, engagement_id, issue_id, duration_minutes, entry_kind, entry_status, billable, ts_start, ts_ns_recorded}</code>. The correction row additionally carries <code>correction_to</code>.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub the <code>description</code> field via TASK-MEMORY-111 BEFORE chain commit. The PostgreSQL row retains the raw text (tenant-scoped + RLS-protected); the memory audit chain holds only the scrubbed form (task-audit skill rule 18).</li></ol>\n<ol><li><strong>MUST</strong> complete create/correct/get/list handlers in ≤ 50 ms p95. <code>entries_perf_test</code> asserts on 1000 iterations.</li></ol>\n<ol><li><strong>MUST</strong> expose REST handlers:</li></ol>\n<ul><li><code>POST /v1/time/entries</code> — create new entry; caller <code>Resource::TimeEntry + Action::Write</code>.</li><li><code>POST /v1/time/entries/{id}/correct</code> — create correction row; same permission.</li><li><code>GET /v1/time/entries/{id}</code> — fetch (effective via <code>current_time_entries_view</code> by default; <code>?include=history</code> walks the chain).</li><li><code>GET /v1/time/entries?member_subject_id=&lt;&gt;&amp;engagement_id=&lt;&gt;&amp;from=&lt;&gt;&amp;to=&lt;&gt;</code> — list with cursor pagination; defaults to <code>current_time_entries_view</code>.</li></ul>\n<ol><li><strong>MUST</strong> support idempotent creation via <code>Idempotency-Key</code> header (same semantics as TASK-AUTH-002 §1 #6).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>time.entry.{create,correct,get,list}</code> per handler with attributes: <code>tenant_id</code>, <code>member_subject_id_hash16</code>, <code>engagement_id</code>, <code>entry_id</code>, <code>outcome</code> (success | invalid_duration | invalid_kind | cross_scope_correction | cycle_detected | prior_already_corrected | permission_denied).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>time_entry_create_total{outcome, entry_kind}</code> (counter).</li><li><code>time_entry_correct_total{outcome}</code> (counter).</li><li><code>time_entry_correction_chain_depth</code> (histogram; alarm at p99 &gt; 5 — a deep chain suggests a workflow issue).</li><li><code>time_entry_duration_minutes</code> (histogram per <code>entry_kind</code>).</li><li><code>time_entry_count{tenant_id, entry_status}</code> (gauge).</li></ul>\n<ol><li><strong>MUST</strong> ship <code>entry_chain_walker(entry_id UUID) RETURNS SETOF UUID</code> SQL function that returns the chain from oldest (original) to newest (effective). Used by <code>GET ?include=history</code> and by the cycle-detection trigger. Maximum walk depth 100 (anti-infinite-loop safety floor).</li></ol>\n<ol><li><strong>MUST</strong> ensure <code>correction_to</code> FK is <code>DEFERRABLE INITIALLY IMMEDIATE</code> so the trigger sees the new row exists when validating self-references during the same transaction (Postgres-specific; needed for the cycle walker to see its own new row).</li></ol>\n<ol><li><strong>MUST</strong> maintain the <strong>rate_card_snapshot pattern</strong> (per DEC-224). When TASK-TIME-005 ships, it populates <code>rate_card_snapshot</code> at row creation with the engagement's then-current rate card. Mutations to the engagement's rate card NEVER alter past <code>rate_card_snapshot</code> values — the snapshot is frozen at the row's instant. Slice 1 ships the column with <code>{}</code> default; TASK-TIME-005 fills.</li></ol>\n<ol><li><strong>MUST</strong> support <strong>list filters</strong>: <code>?member_subject_id</code>, <code>?engagement_id</code>, <code>?issue_id</code>, <code>?from=&lt;ts&gt;</code>, <code>?to=&lt;ts&gt;</code>, <code>?entry_status</code>, <code>?billable</code>. Default page size 50, max 500. Cursor pagination on <code>(ts_start DESC, id)</code>.</li></ol>\n<ol><li><strong>MUST</strong> support <code>GET /v1/time/entries/{id}?include=history</code> returning the full chain <code>[&lt;original&gt;, &lt;correction_1&gt;, &lt;correction_2&gt;, ...]</code> ordered oldest to newest. Caller <code>Action::Read</code> on <code>TimeEntry</code>.</li></ol>\n<ol><li><strong>MUST</strong> ensure the <code>current_time_entries_view</code> performance is acceptable (&lt; 100 ms for 10K-entry tenant). Index: <code>CREATE INDEX time_entries_correction_to_idx ON time_entries (correction_to) WHERE correction_to IS NOT NULL;</code> — bounded by correction count (typically &lt; 5% of total).</li></ol>\n<ol><li><strong>MUST</strong> treat <code>created_by_subject_id</code> as the <strong>actor</strong> (who created the row), which MAY differ from <code>member_subject_id</code> (whose work is being recorded). For self-entry both are equal; for AM-on-behalf-of entry (TASK-TIME-003 manual form) they differ. Both fields are immutable per row.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why append-only with correction_to and not UPDATE in place (DEC-220, DEC-230)?</strong> Time entries are invoice-grade financial records. An UPDATE in place loses the prior value; the audit trail of \"what was originally claimed?\" disappears. Append-only via correction_to means every prior value is preserved; the chain is the legal record. SOC 2 + ISO 27001 audit-logging requirements (A.12.4) are satisfied by construction. The cost is the slight complexity of \"the current row is the one not pointed to by any other row's correction_to\" — but the <code>current_time_entries_view</code> collapses that to a single query.</p>\n<p><strong>Why a chain (not tree) topology for corrections (DEC-226, §1 #8)?</strong> If two rows could both correct the same prior, \"what is the current value?\" becomes ambiguous (which correction is the latest?). Allowing only one row per correction_to enforces a deterministic linearisation — the chain head is unambiguously \"the current value.\" The trigger rejects the second attempt with <code>prior_row_already_corrected</code>. Operators who need to correct an already-corrected row simply correct the current head (the chain extends).</p>\n<p><strong>Why acyclic enforcement via trigger (DEC-225, §1 #7)?</strong> A correction cycle (<code>A → B → A</code>) is a logic error that breaks every consumer (the chain walker would loop forever; the \"current value\" predicate has no answer). The trigger walks the chain at INSERT time and rejects the cycle. The CI test (<code>correction_acyclic_test</code>) seeds a deliberate cycle attempt to assert protection.</p>\n<p><strong>Why <code>correction_to</code> cross-scope rejection (§1 #11)?</strong> A correction is \"I logged the same thing wrong\" — same engagement, same issue, same member. Allowing the operator to \"correct\" entry-1 (engagement-A, issue-X) to a new value in (engagement-B, issue-Y) would let them effectively rewrite the engagement bill silently. Cross-scope corrections are blocked at trigger; the right action is <code>time.entry_reverted</code> (slice 2) plus a new <code>time.entry_recorded</code> under the new scope.</p>\n<p><strong>Why durations bounded 1 ≤ minutes ≤ 1440 (DEC-227, DEC-228)?</strong> The lower bound (1 minute) is operational — entries below 1 minute are typically test data or accidental clicks. The upper bound (1440 = 24 hours) prevents a single buggy entry from claiming a year of work; daily-cap enforcement (TASK-TIME-007) operates across rows, but per-row cap catches the most extreme typos before they reach the daily aggregator.</p>\n<p><strong>Why <code>rate_card_snapshot</code> JSONB on the row (DEC-224)?</strong> The billable amount of an entry is rate × hours. If <code>rate</code> is fetched at invoice generation by joining to the engagement's current rate card, a CFO bumping the rate card retroactively shifts every historic invoice line — silently. Snapshotting the rate card AT the row's instant freezes the billable basis. JSONB (not FK) is the right shape because rate cards have nested structure (per-role rates, member overrides, time-of-day adjustments), and we want the snapshot to be a pure value copy that's immune to FK cascades.</p>\n<p><strong>Why entry_kind closed at 4 values (§1 #3)?</strong> Working-time classification under VN Labour Code is <code>regular | overtime | weekend | holiday</code>. Each kind has different statutory rate multipliers (TASK-REW-004 ships). Allowing a 5th (e.g. <code>night_shift</code>) is an ADR — the rate-multiplier table would need to extend. Closed enum prevents drift.</p>\n<p><strong>Why entry_status separate from billable (§1 #1)?</strong> <code>entry_status</code> is workflow (\"has this been approved?\"); <code>billable</code> is financial classification (\"is this hour invoiced?\"). An entry can be <code>status=approved, billable=false</code> (approved internal work, not billed to client). They are orthogonal axes.</p>\n<p><strong>Why slice-1 ships billable=false default (DEC-223)?</strong> The billable cascade (TASK-TIME-005) is a non-trivial 4-step decision involving the engagement's non-billable categories, the rate card's role default, and member overrides. Splitting it to its own task keeps this schema task focused on integrity. Default <code>false</code> is conservative — if the cascade fails to set the flag, the entry is treated as non-billable (no invoice line) rather than mis-billed. TASK-TIME-005's tests assert the default is replaced on every entry.</p>\n<p><strong>Why entry_currency on the row (DEC-229)?</strong> Multi-currency tenants run engagements in VND, USD, SGD, etc. The entry's currency is the engagement's invoice currency at the row's instant. Snapshotting prevents the engagement's currency switch (rare but possible during contract renegotiation) from retroactively converting past entries. Invoice math is TASK-INV-001's concern; this task just preserves the source-of-truth currency.</p>\n<p><strong>Why created_by_subject_id distinct from member_subject_id (§1 #25)?</strong> Most entries are self-logged (member = creator). Some entries are AM-on-behalf-of (manual entry on someone else's behalf with their confirmation — TASK-TIME-003 ships that flow). Both fields capture different facts: \"whose work this records\" vs \"who pressed the button.\" Audit trails need both.</p>\n<p><strong>Why per-row immutability of all fields (implicit in DEC-230)?</strong> Combined with append-only at SQL grant, this means a row's content is its forever-record. Workflows that need \"edit\" semantics use the correction handler (which creates a new row pointing at the prior); UI affordances may present this as \"edit\" but the underlying mechanism is always insert-new.</p>\n<p><strong>Why <code>current_time_entries_view</code> instead of always-current column on the row (§1 #9)?</strong> Adding <code>is_current BOOLEAN</code> to the row creates a writer dependency — every correction would have to update the prior row's <code>is_current = false</code>, breaking append-only. The view filters at read time; the index on <code>correction_to WHERE correction_to IS NOT NULL</code> is small (~5% of rows).</p>\n<p><strong>Why DEFERRABLE INITIALLY IMMEDIATE on the self-FK (§1 #20)?</strong> The cycle-detection trigger walks <code>correction_to</code> to check whether the chain loops back to the new row's id. Standard (non-deferrable) FK constraints would reject the row before the trigger sees it. DEFERRABLE INITIALLY IMMEDIATE means the FK is checked at the end of the statement, after the trigger; the trigger can use the new row's id during validation.</p>\n<p><strong>Why list defaults to current view (§1 #22)?</strong> The 99% query pattern is \"what hours did this Member log this week?\" — and the answer is current-effective, not raw history. The 1% query \"show me the correction history of entry-1\" uses <code>?include=history</code>. Defaulting to history-aware listing would surface every superseded row, confusing operators.</p>\n<p><strong>Why chain max-depth 100 (§1 #19)?</strong> Practical chains are 1–3 rows (entry + one correction is typical; pathological cases reach 5–10). 100 is a safety floor — any chain that deep is either an integration bug or a stress test; the walker bails to prevent infinite-loop-like CPU consumption. Production chains hit 100 are alarmable.</p>\n<p><strong>Why <code>description</code> 0–1000 chars (§1 #10)?</strong> Short enough to discourage prose-narrative entries (which belong in PROJ issue comments, not TIME); long enough to let \"fixed merge conflict in chat/auth bridge — see PR-451\" fit. PII-scrubbed via TASK-MEMORY-111 before chain commit; operators warned that descriptions are visible to AM + CFO via TASK-TIME-006.</p>\n<p><strong>Why two memory audit row kinds (recorded + corrected) and not one (DEC-231)?</strong> Different operator queries: \"show me the original-creation activity for this engagement\" filters on <code>time.entry_recorded</code>; \"show me the correction activity\" filters on <code>time.entry_corrected</code>. A single kind would require an extra <code>is_correction</code> flag and degrade query selectivity.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0001 — time_entries</h3>\n<pre><code class=\"language-sql\">-- services/time/migrations/0001_time_entries.sql\n\nBEGIN;\n\nCREATE TYPE entry_kind AS ENUM ('regular', 'overtime', 'weekend', 'holiday');\nCREATE TYPE entry_status AS ENUM ('draft', 'submitted', 'approved', 'reverted');\n\nCREATE TABLE time_entries (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    member_subject_id      UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    engagement_id          UUID         NOT NULL,\n    issue_id               UUID         NOT NULL,\n    ts_start               TIMESTAMPTZ  NOT NULL,\n    duration_minutes       INT          NOT NULL CHECK (duration_minutes BETWEEN 1 AND 1440),\n    entry_kind             entry_kind   NOT NULL,\n    entry_status           entry_status NOT NULL DEFAULT 'draft',\n    billable               BOOLEAN      NOT NULL DEFAULT false,\n    rate_card_snapshot     JSONB        NOT NULL DEFAULT '{}'::jsonb,\n    entry_currency         CHAR(3)      NOT NULL CHECK (entry_currency ~ '^[A-Z]{3}$'),\n    description            TEXT         CHECK (description IS NULL OR length(description) BETWEEN 0 AND 1000),\n    correction_to          UUID         REFERENCES time_entries(id) DEFERRABLE INITIALLY IMMEDIATE,\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nCREATE INDEX time_entries_tenant_member_ts_idx ON time_entries (tenant_id, member_subject_id, ts_start DESC);\nCREATE INDEX time_entries_tenant_engagement_ts_idx ON time_entries (tenant_id, engagement_id, ts_start DESC);\nCREATE INDEX time_entries_correction_to_idx ON time_entries (correction_to) WHERE correction_to IS NOT NULL;\n\nALTER TABLE time_entries ENABLE ROW LEVEL SECURITY;\nCREATE POLICY time_entries_tenant_isolation ON time_entries\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Append-only enforcement (DEC-230)\nREVOKE UPDATE, DELETE ON time_entries FROM cyberos_app;\n\n-- Cycle detection on correction_to (DEC-225)\nCREATE OR REPLACE FUNCTION detect_correction_cycle() RETURNS TRIGGER AS $$\nDECLARE\n    walker UUID;\n    depth INT := 0;\nBEGIN\n    IF NEW.correction_to IS NULL THEN RETURN NEW; END IF;\n    walker := NEW.correction_to;\n    WHILE walker IS NOT NULL AND depth &lt; 100 LOOP\n        IF walker = NEW.id THEN\n            RAISE EXCEPTION 'correction_cycle_detected' USING ERRCODE = 'P0010';\n        END IF;\n        SELECT correction_to INTO walker FROM time_entries WHERE id = walker;\n        depth := depth + 1;\n    END LOOP;\n    IF depth &gt;= 100 THEN\n        RAISE EXCEPTION 'correction_chain_too_deep' USING ERRCODE = 'P0011';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_time_entries_no_cycle BEFORE INSERT ON time_entries\n    FOR EACH ROW EXECUTE FUNCTION detect_correction_cycle();\n\n-- Chain (not tree) enforcement (DEC-226)\nCREATE OR REPLACE FUNCTION enforce_chain_topology() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.correction_to IS NULL THEN RETURN NEW; END IF;\n    IF EXISTS (SELECT 1 FROM time_entries WHERE correction_to = NEW.correction_to AND id != NEW.id) THEN\n        RAISE EXCEPTION 'prior_row_already_corrected' USING ERRCODE = 'P0012';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_time_entries_chain_topology BEFORE INSERT ON time_entries\n    FOR EACH ROW EXECUTE FUNCTION enforce_chain_topology();\n\n-- Cross-scope correction rejection (§1 #11)\nCREATE OR REPLACE FUNCTION enforce_correction_inheritance() RETURNS TRIGGER AS $$\nDECLARE prior RECORD;\nBEGIN\n    IF NEW.correction_to IS NULL THEN RETURN NEW; END IF;\n    SELECT tenant_id, engagement_id, issue_id, member_subject_id\n        INTO prior FROM time_entries WHERE id = NEW.correction_to;\n    IF NOT FOUND THEN\n        RAISE EXCEPTION 'correction_target_missing' USING ERRCODE = 'P0013';\n    END IF;\n    IF prior.tenant_id != NEW.tenant_id\n       OR prior.engagement_id != NEW.engagement_id\n       OR prior.issue_id != NEW.issue_id\n       OR prior.member_subject_id != NEW.member_subject_id THEN\n        RAISE EXCEPTION 'correction_cross_scope' USING ERRCODE = 'P0014';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_time_entries_correction_inheritance BEFORE INSERT ON time_entries\n    FOR EACH ROW EXECUTE FUNCTION enforce_correction_inheritance();\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0002 — current view + chain walker</h3>\n<pre><code class=\"language-sql\">-- services/time/migrations/0002_time_entries_view.sql\n\nBEGIN;\n\nCREATE VIEW current_time_entries_view AS\n    SELECT * FROM time_entries\n    WHERE id NOT IN (\n        SELECT correction_to FROM time_entries WHERE correction_to IS NOT NULL\n    );\n\n-- Walk a chain from any node to its head (effective row).\nCREATE OR REPLACE FUNCTION entry_chain_walker(p_entry_id UUID) RETURNS SETOF UUID AS $$\nDECLARE\n    head UUID;\n    walker UUID;\n    depth INT := 0;\nBEGIN\n    -- Walk to the original.\n    walker := p_entry_id;\n    WHILE EXISTS (SELECT 1 FROM time_entries WHERE id = walker AND correction_to IS NOT NULL) AND depth &lt; 100 LOOP\n        SELECT correction_to INTO walker FROM time_entries WHERE id = walker;\n        depth := depth + 1;\n    END LOOP;\n    head := walker;\n    -- Walk down to the effective row, returning each id.\n    walker := head;\n    depth := 0;\n    LOOP\n        RETURN NEXT walker;\n        SELECT id INTO walker FROM time_entries WHERE correction_to = walker;\n        EXIT WHEN NOT FOUND OR depth &gt;= 100;\n        depth := depth + 1;\n    END LOOP;\nEND;\n$$ LANGUAGE plpgsql STABLE;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Rust types</h3>\n<pre><code class=\"language-rust\">// services/time/src/types.rs\nuse chrono::{DateTime, Utc};\nuse rust_decimal::Decimal;\nuse serde::{Deserialize, Serialize};\nuse sqlx::{FromRow, Type};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"entry_kind\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum EntryKind { Regular, Overtime, Weekend, Holiday }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"entry_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum EntryStatus { Draft, Submitted, Approved, Reverted }\n\nimpl EntryKind {\n    pub const ALL: &amp;'static [EntryKind] = &amp;[EntryKind::Regular, EntryKind::Overtime, EntryKind::Weekend, EntryKind::Holiday];\n}\n\nimpl EntryStatus {\n    pub const ALL: &amp;'static [EntryStatus] = &amp;[EntryStatus::Draft, EntryStatus::Submitted, EntryStatus::Approved, EntryStatus::Reverted];\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct TimeEntry {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub member_subject_id: Uuid,\n    pub engagement_id: Uuid,\n    pub issue_id: Uuid,\n    pub ts_start: DateTime&lt;Utc&gt;,\n    pub duration_minutes: i32,\n    pub entry_kind: EntryKind,\n    pub entry_status: EntryStatus,\n    pub billable: bool,\n    pub rate_card_snapshot: serde_json::Value,\n    pub entry_currency: String,\n    pub description: Option&lt;String&gt;,\n    pub correction_to: Option&lt;Uuid&gt;,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}</code></pre>\n<h3>3.4 — Validation</h3>\n<pre><code class=\"language-rust\">// services/time/src/validation.rs\nuse chrono::{DateTime, Duration, Utc};\n\npub const MIN_DURATION_MINUTES: i32 = 1;\npub const MAX_DURATION_MINUTES: i32 = 1440;\npub const FUTURE_TOLERANCE: Duration = Duration::minutes(5);\n\n#[derive(Debug, thiserror::Error)]\npub enum ValidationError {\n    #[error(\"duration_out_of_range: {0}\")]\n    DurationOutOfRange(i32),\n    #[error(\"ts_start_in_future: {0}\")]\n    TsStartInFuture(DateTime&lt;Utc&gt;),\n    #[error(\"description_too_long: {0}\")]\n    DescriptionTooLong(usize),\n}\n\npub fn validate_duration(minutes: i32) -&gt; Result&lt;(), ValidationError&gt; {\n    if minutes &lt; MIN_DURATION_MINUTES || minutes &gt; MAX_DURATION_MINUTES {\n        return Err(ValidationError::DurationOutOfRange(minutes));\n    }\n    Ok(())\n}\n\npub fn validate_ts_start(ts: DateTime&lt;Utc&gt;, now: DateTime&lt;Utc&gt;) -&gt; Result&lt;(), ValidationError&gt; {\n    if ts &gt; now + FUTURE_TOLERANCE { return Err(ValidationError::TsStartInFuture(ts)); }\n    Ok(())\n}\n\npub fn validate_description(desc: &amp;Option&lt;String&gt;) -&gt; Result&lt;(), ValidationError&gt; {\n    if let Some(d) = desc {\n        if d.len() &gt; 1000 { return Err(ValidationError::DescriptionTooLong(d.len())); }\n    }\n    Ok(())\n}</code></pre>\n<h3>3.5 — REST handlers (excerpt)</h3>\n<pre><code class=\"language-rust\">// services/time/src/handlers/entries.rs\nuse axum::{Json, extract::{Path, State, Query}, http::StatusCode};\nuse crate::types::*;\nuse crate::validation::*;\nuse crate::audit::entry_events;\n\n#[derive(Deserialize)]\npub struct CreateEntryRequest {\n    pub member_subject_id: Uuid,\n    pub engagement_id: Uuid,\n    pub issue_id: Uuid,\n    pub ts_start: DateTime&lt;Utc&gt;,\n    pub duration_minutes: i32,\n    pub entry_kind: EntryKind,\n    pub entry_currency: String,\n    pub description: Option&lt;String&gt;,\n}\n\npub async fn create_entry(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Json(req): Json&lt;CreateEntryRequest&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;TimeEntry&gt;), ApiError&gt; {\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::TimeEntry, Action::Write)?;\n    validate_duration(req.duration_minutes)?;\n    validate_ts_start(req.ts_start, Utc::now())?;\n    validate_description(&amp;req.description)?;\n\n    let id = Uuid::new_v4();\n    let mut tx = state.db.begin().await?;\n    let entry = sqlx::query_as!(TimeEntry, r#\"\n        INSERT INTO time_entries (id, tenant_id, member_subject_id, engagement_id, issue_id,\n            ts_start, duration_minutes, entry_kind, entry_status, billable,\n            rate_card_snapshot, entry_currency, description, correction_to, created_by_subject_id)\n        VALUES ($1, $2, $3, $4, $5, $6, $7, $8::entry_kind, 'draft'::entry_status, false,\n                '{}'::jsonb, $9, $10, NULL, $11)\n        RETURNING *\n    \"#,\n        id, claims.tenant_id(), req.member_subject_id, req.engagement_id, req.issue_id,\n        req.ts_start, req.duration_minutes, req.entry_kind as EntryKind,\n        req.entry_currency, req.description, claims.subject_id(),\n    ).fetch_one(&amp;mut *tx).await?;\n\n    entry_events::emit_entry_recorded(&amp;mut tx, &amp;entry).await?;\n    tx.commit().await?;\n    Ok((StatusCode::CREATED, Json(entry)))\n}\n\n#[derive(Deserialize)]\npub struct CorrectEntryRequest {\n    pub duration_minutes: Option&lt;i32&gt;,\n    pub entry_kind: Option&lt;EntryKind&gt;,\n    pub description: Option&lt;String&gt;,\n    pub ts_start: Option&lt;DateTime&lt;Utc&gt;&gt;,\n}\n\npub async fn correct_entry(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Path(prior_id): Path&lt;Uuid&gt;,\n    Json(req): Json&lt;CorrectEntryRequest&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;TimeEntry&gt;), ApiError&gt; {\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::TimeEntry, Action::Write)?;\n    let mut tx = state.db.begin().await?;\n    let prior: TimeEntry = sqlx::query_as!(TimeEntry, \"SELECT * FROM time_entries WHERE id = $1\", prior_id)\n        .fetch_one(&amp;mut *tx).await?;\n\n    let new_id = Uuid::new_v4();\n    let duration = req.duration_minutes.unwrap_or(prior.duration_minutes);\n    let kind = req.entry_kind.unwrap_or(prior.entry_kind);\n    let desc = req.description.or(prior.description.clone());\n    let ts_start = req.ts_start.unwrap_or(prior.ts_start);\n\n    validate_duration(duration)?;\n    validate_ts_start(ts_start, Utc::now())?;\n    validate_description(&amp;desc)?;\n\n    let entry = sqlx::query_as!(TimeEntry, r#\"\n        INSERT INTO time_entries (id, tenant_id, member_subject_id, engagement_id, issue_id,\n            ts_start, duration_minutes, entry_kind, entry_status, billable,\n            rate_card_snapshot, entry_currency, description, correction_to, created_by_subject_id)\n        VALUES ($1, $2, $3, $4, $5, $6, $7, $8::entry_kind, $9::entry_status, $10,\n                $11, $12, $13, $14, $15)\n        RETURNING *\n    \"#,\n        new_id, prior.tenant_id, prior.member_subject_id, prior.engagement_id, prior.issue_id,\n        ts_start, duration, kind as EntryKind, prior.entry_status as EntryStatus, prior.billable,\n        prior.rate_card_snapshot, prior.entry_currency, desc, prior.id, claims.subject_id(),\n    ).fetch_one(&amp;mut *tx).await?;\n\n    entry_events::emit_entry_corrected(&amp;mut tx, &amp;entry, &amp;prior).await?;\n    tx.commit().await?;\n    Ok((StatusCode::CREATED, Json(entry)))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>EntryKind closed at 4 values</strong> — <code>EntryKind::ALL.len() == 4</code>; Postgres enum has exactly 4 labels.</li><li><strong>EntryStatus closed at 4 values</strong> — same shape.</li><li><strong>RLS isolates by tenant</strong> — query as tenant-A returns 0 entries of tenant-B.</li><li><strong>Create entry happy path</strong> — valid body → 201 with <code>TimeEntry</code> JSON; one <code>time.entry_recorded</code> memory row.</li><li><strong>Create entry &lt; 1 minute</strong> — 400 <code>duration_out_of_range</code>.</li><li><strong>Create entry &gt; 1440 minutes</strong> — 400 <code>duration_out_of_range</code>.</li><li><strong>Create entry ts_start in future</strong> — 400 <code>ts_start_in_future</code>.</li><li><strong>UPDATE on time_entries blocked</strong> — <code>UPDATE time_entries SET duration_minutes = 100 WHERE id = $1</code> as <code>cyberos_app</code> user → permission denied.</li><li><strong>DELETE on time_entries blocked</strong> — same as #8 for DELETE.</li><li><strong>Correct entry happy</strong> — valid prior id + new duration → 201 with new row; original kept; new row's <code>correction_to = prior_id</code>.</li><li><strong>Correct entry cross-tenant rejected</strong> — correction targeting a prior in a different tenant → 400 <code>correction_cross_scope</code>.</li><li><strong>Correct entry cross-engagement rejected</strong> — modifying engagement_id in correction body → 400 <code>correction_cross_scope</code>.</li><li><strong>Tree topology rejected</strong> — two correctors of same prior → second insert raises <code>prior_row_already_corrected</code>.</li><li><strong>Cycle topology rejected</strong> — A → B → A attempted → <code>correction_cycle_detected</code>.</li><li><strong>Chain depth &gt; 100 rejected</strong> — synthetic 101-row chain → 102nd insert raises <code>correction_chain_too_deep</code>.</li><li><strong>current_time_entries_view filters correctly</strong> — query on view never returns rows that are pointed to by <code>correction_to</code>.</li><li><strong>GET ?include=history returns full chain</strong> — chain of 3 → 3 rows returned in oldest-first order.</li><li><strong>Idempotent create</strong> — same Idempotency-Key + same body → same entry.</li><li><strong>OTel span <code>time.entry.create</code> emitted</strong> — with <code>outcome=success</code>.</li><li><strong>OTel counter <code>time_entry_create_total{outcome=success, entry_kind=regular}</code> increments</strong> — per create.</li><li><strong>OTel counter <code>time_entry_correct_total{outcome=success}</code> increments</strong> — per correction.</li><li><strong>OTel histogram <code>time_entry_correction_chain_depth</code> observes</strong> — chain of 4 → observation of 4.</li><li><strong>Perf budget &lt; 50 ms p95</strong> — <code>entries_perf_test</code> 1000 iterations.</li><li><strong>Subject FK ON DELETE RESTRICT</strong> — <code>DELETE FROM auth.subjects WHERE id = &lt;member_subject_id&gt;</code> raises FK violation if entries exist.</li><li><strong>rate_card_snapshot defaults to <code>{}</code></strong> — slice 1 default until TASK-TIME-005 fills.</li><li><strong><code>created_by</code> distinct from <code>member_subject_id</code></strong> — AM-on-behalf-of entry creates row with <code>created_by = AM_id, member_subject_id = staff_id</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/time/tests/append_only_test.rs\n#[sqlx::test]\nasync fn update_blocked_at_grant(pool: sqlx::PgPool) {\n    set_role_app(&amp;pool).await;\n    let id = seed_entry(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE time_entries SET duration_minutes = 999 WHERE id = $1\")\n        .bind(id).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"permission denied\"));\n}\n\n#[sqlx::test]\nasync fn delete_blocked_at_grant(pool: sqlx::PgPool) {\n    set_role_app(&amp;pool).await;\n    let id = seed_entry(&amp;pool).await;\n    let err = sqlx::query(\"DELETE FROM time_entries WHERE id = $1\").bind(id).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"permission denied\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/time/tests/correction_acyclic_test.rs\n#[sqlx::test]\nasync fn direct_cycle_rejected(pool: sqlx::PgPool) {\n    let a = seed_entry(&amp;pool).await;\n    let b = correct(&amp;pool, a, /*..*/).await;\n    // Now attempt to correct b with correction_to = a → would form A→B→A cycle.\n    let err = insert_with_correction_to(&amp;pool, /*new_id=*/Uuid::new_v4(), /*correction_to=*/a).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"prior_row_already_corrected\"));\n    // (chain topology check fires first; cycle check fires for self-targeting case)\n}\n\n#[sqlx::test]\nasync fn self_reference_cycle_rejected(pool: sqlx::PgPool) {\n    let id = Uuid::new_v4();\n    let err = insert_self_referencing(&amp;pool, id).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"correction_cycle_detected\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/time/tests/current_view_test.rs\n#[sqlx::test]\nasync fn corrected_rows_omitted_from_current_view(pool: sqlx::PgPool) {\n    let a = seed_entry(&amp;pool).await;\n    let b = correct(&amp;pool, a, /* new duration */).await;\n    let rows: Vec&lt;Uuid&gt; = sqlx::query_scalar(\"SELECT id FROM current_time_entries_view WHERE id IN ($1, $2)\")\n        .bind(a).bind(b).fetch_all(&amp;pool).await.unwrap();\n    assert_eq!(rows, vec![b]);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/time/tests/correction_chain_test.rs\n#[sqlx::test]\nasync fn entry_chain_walker_returns_oldest_first(pool: sqlx::PgPool) {\n    let a = seed_entry(&amp;pool).await;\n    let b = correct(&amp;pool, a, ()).await;\n    let c = correct(&amp;pool, b, ()).await;\n    let chain: Vec&lt;Uuid&gt; = sqlx::query_scalar(\"SELECT entry_chain_walker($1)\").bind(c).fetch_all(&amp;pool).await.unwrap();\n    assert_eq!(chain, vec![a, b, c]);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; cycle/chain triggers are in §3.1; chain walker in §3.2.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-AUTH-003</strong> — RLS enforcement; same <code>current_setting('auth.tenant_id')</code> pattern.</li><li><strong>TASK-AUTH-101</strong> — RBAC catalogue; <code>Resource::TimeEntry + Action::Write/Read</code> matrix entry.</li></ul>\n<p><strong>Downstream (8 placeholders):</strong></p>\n<ul><li><strong>TASK-TIME-002</strong> — timer start/stop UI (creates entries via this task's API).</li><li><strong>TASK-TIME-003</strong> — manual entry form + VN Labour Code cap validation.</li><li><strong>TASK-TIME-005</strong> — billable cascade (populates <code>billable</code> + <code>rate_card_snapshot</code>).</li><li><strong>TASK-TIME-006</strong> — weekly approval flow (transitions <code>entry_status</code>).</li><li><strong>TASK-TIME-007</strong> — VN Labour Code Art. 107 OT cap hard-block at write.</li><li><strong>TASK-TIME-009</strong> — per-cycle billable rollup → INV.</li><li><strong>TASK-HR-008</strong> — performance signal aggregator (read-only consumer).</li><li><strong>TASK-RES-001</strong> — capacity-vs-demand matrix (joins on member × time).</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AI-003</strong> — memory audit bridge; receives <code>time.entry_recorded</code>, <code>time.entry_corrected</code>.</li><li><strong>TASK-PROJ-001</strong> — issue schema; <code>issue_id</code> FK target.</li><li><strong>TASK-PROJ-005</strong> — rate card schema; <code>rate_card_snapshot</code> source.</li><li><strong>TASK-MEMORY-111</strong> — PII detection for <code>description</code> scrubbing.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/time/entries request</h3>\n<pre><code class=\"language-json\">{\n  \"member_subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"engagement_id\": \"e1234567-1234-1234-1234-123456789012\",\n  \"issue_id\": \"i1234567-1234-1234-1234-123456789012\",\n  \"ts_start\": \"2026-05-16T09:00:00Z\",\n  \"duration_minutes\": 90,\n  \"entry_kind\": \"regular\",\n  \"entry_currency\": \"VND\",\n  \"description\": \"Worked on TASK-AUTH-101 spec review\"\n}</code></pre>\n<h3>8.2 — 201 CREATED response</h3>\n<pre><code class=\"language-json\">{\n  \"id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"member_subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"engagement_id\": \"e1234567-1234-1234-1234-123456789012\",\n  \"issue_id\": \"i1234567-1234-1234-1234-123456789012\",\n  \"ts_start\": \"2026-05-16T09:00:00Z\",\n  \"duration_minutes\": 90,\n  \"entry_kind\": \"regular\",\n  \"entry_status\": \"draft\",\n  \"billable\": false,\n  \"rate_card_snapshot\": {},\n  \"entry_currency\": \"VND\",\n  \"description\": \"Worked on TASK-AUTH-101 spec review\",\n  \"correction_to\": null,\n  \"created_at\": \"2026-05-16T09:01:00Z\",\n  \"created_by_subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\"\n}</code></pre>\n<h3>8.3 — POST correct request</h3>\n<pre><code class=\"language-json\">{ \"duration_minutes\": 105, \"description\": \"Worked on TASK-AUTH-101 spec review — corrected: forgot to add 15min lunch overlap\" }</code></pre>\n<h3>8.4 — time.entry_recorded memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.entry_recorded\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"entry_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"member_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"engagement_id\": \"e1234567-1234-1234-1234-123456789012\",\n  \"issue_id\": \"i1234567-1234-1234-1234-123456789012\",\n  \"duration_minutes\": 90,\n  \"entry_kind\": \"regular\",\n  \"entry_status\": \"draft\",\n  \"billable\": false,\n  \"entry_currency\": \"VND\",\n  \"description_scrubbed\": \"Worked on TASK-AUTH-101 spec review\",\n  \"ts_start\": \"2026-05-16T09:00:00Z\",\n  \"ts_ns_recorded\": 1747920731000000000\n}</code></pre>\n<h3>8.5 — time.entry_corrected memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.entry_corrected\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"entry_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8N\",\n  \"correction_to\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"member_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"engagement_id\": \"e1234567-1234-1234-1234-123456789012\",\n  \"issue_id\": \"i1234567-1234-1234-1234-123456789012\",\n  \"duration_minutes_old\": 90,\n  \"duration_minutes_new\": 105,\n  \"ts_ns_recorded\": 1747921000000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Standalone-engagement entries (no issue_id)</strong> — slice 2; some non-billable internal time has no specific issue.</li><li><strong>Holiday-list driving entry_kind defaulting</strong> — TASK-TIME-007 ships the holiday table.</li><li><strong>VN Labour Code OT cap enforcement</strong> — TASK-TIME-007 (this task allows the kind = overtime; the cap is TASK-TIME-007's gate).</li><li><strong>Billable cascade computation</strong> — TASK-TIME-005.</li><li><strong>Approval flow transitions</strong> — TASK-TIME-006.</li><li><strong>Per-cycle rollup emit to INV</strong> — TASK-TIME-009.</li><li><strong>Time-entry edit UI</strong> — TASK-TIME-002 + TASK-TIME-003.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>UPDATE on time_entries</td><td>SQL grant <code>REVOKE UPDATE</code></td><td>Permission denied at DB</td><td>None — designed</td></tr><tr><td>DELETE on time_entries</td><td>SQL grant <code>REVOKE DELETE</code></td><td>Permission denied at DB</td><td>None — designed</td></tr><tr><td>Duration &lt; 1 minute</td><td>DB CHECK + handler validation</td><td>400 + <code>duration_out_of_range</code></td><td>Caller fixes</td></tr><tr><td>Duration &gt; 1440 minutes</td><td>DB CHECK + handler validation</td><td>400 + <code>duration_out_of_range</code></td><td>Split into multiple entries</td></tr><tr><td>ts_start in future &gt; 5 min</td><td>Handler validation</td><td>400 + <code>ts_start_in_future</code></td><td>Use current time</td></tr><tr><td>Two correctors of same prior</td><td><code>prior_row_already_corrected</code> trigger</td><td>400 + error</td><td>Correct the chain head instead</td></tr><tr><td>A → B → A cycle</td><td><code>correction_cycle_detected</code> trigger</td><td>400 + error</td><td>Re-do correction without cycle</td></tr><tr><td>Chain &gt; 100 rows deep</td><td><code>correction_chain_too_deep</code> trigger</td><td>400 + error</td><td>Investigate workflow producing deep chains</td></tr><tr><td>Cross-tenant correction</td><td><code>correction_cross_scope</code> trigger</td><td>400 + error</td><td>New entry under correct tenant</td></tr><tr><td>Cross-engagement correction</td><td><code>correction_cross_scope</code> trigger</td><td>400 + error</td><td>Revert + new entry under correct engagement (slice 2 reverts)</td></tr><tr><td>Cross-issue correction</td><td>same</td><td>same</td><td>same</td></tr><tr><td>Cross-member correction</td><td>same</td><td>same</td><td>same</td></tr><tr><td><code>correction_to</code> references non-existent</td><td><code>correction_target_missing</code> trigger</td><td>400 + error</td><td>Verify prior id</td></tr><tr><td>Description &gt; 1000 chars</td><td>Handler validation</td><td>400 + <code>description_too_long</code></td><td>Shorten or move detail to PROJ comment</td></tr><tr><td><code>entry_currency</code> not 3 uppercase</td><td>DB CHECK</td><td>INSERT fails</td><td>Use valid ISO-4217</td></tr><tr><td>RLS bypass attempt</td><td>RLS <code>USING</code> predicate</td><td>0 rows returned</td><td>None — designed</td></tr><tr><td><code>member_subject_id</code> deleted while entries exist</td><td>FK ON DELETE RESTRICT</td><td>DELETE auth.subjects fails</td><td>Use HR termination flow</td></tr><tr><td>memory row emit fails mid-transaction</td><td>Outer rollback</td><td>500 <code>audit_failed</code>; entry not persisted</td><td>memory_writer diagnosis</td></tr><tr><td>Idempotency-Key reused with different body</td><td>Idempotency layer</td><td>409 <code>idempotency_key_reuse</code></td><td>New key</td></tr><tr><td><code>current_time_entries_view</code> slow on 10K-row tenant</td><td>Perf test</td><td>Sev-3</td><td>Verify <code>correction_to</code> index health</td></tr><tr><td><code>entry_chain_walker</code> exceeds depth 100</td><td>Function returns up to 100 rows</td><td>Truncated chain returned</td><td>Alarmable</td></tr><tr><td>OTel span attribute missing</td><td><code>otel_attrs_test</code></td><td>CI fails</td><td>Fix span builder</td></tr><tr><td><code>entry_kind</code> enum drift (someone adds <code>night_shift</code>)</td><td>Closed-enum test</td><td>CI fails</td><td>ADR + migration + code together</td></tr><tr><td><code>entry_status</code> enum drift</td><td>same</td><td>CI fails</td><td>same</td></tr><tr><td><code>rate_card_snapshot</code> mutated after row write</td><td>Append-only enforcement</td><td>Permission denied</td><td>None — designed</td></tr><tr><td>Race: concurrent corrections to same prior</td><td>First INSERT wins; second fails <code>prior_row_already_corrected</code></td><td>Second caller sees 400</td><td>Caller re-fetches and retries</td></tr><tr><td><code>description</code> contains PII not scrubbed</td><td>memory PII test</td><td>Pre-commit failure</td><td>Add PII rule</td></tr><tr><td>Chain walker called on entry id from different tenant</td><td>RLS filters out</td><td>Returns 0 rows</td><td>None — designed</td></tr><tr><td>Subject deleted but cleanup ordering wrong</td><td>FK ON DELETE RESTRICT</td><td>Migration fails</td><td>Restore subject first</td></tr><tr><td>Daily aggregation exceeds 24h via many sub-row entries</td><td>This task's per-row cap is 24h; aggregate cap is TASK-TIME-007</td><td>Out-of-scope here</td><td>TASK-TIME-007</td></tr><tr><td>Time-zone confusion: ts_start interpreted in local time</td><td>DB stores TIMESTAMPTZ — always UTC</td><td>None</td><td>Document for UI implementers</td></tr><tr><td>Description-edit on existing entry attempted</td><td>Handler omits from PATCH — there is no PATCH</td><td>Use correct endpoint</td><td>None — designed</td></tr><tr><td><code>billable</code> field set in create request</td><td>Handler ignores (slice 1; cascade sets)</td><td>Default false applied</td><td>TASK-TIME-005 takes over</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Append-only is the design assertion</strong> — every other invariant rests on it. SQL grant enforcement (not handler discipline) makes accidental UPDATE impossible.</li><li><strong>Correction chains, not trees</strong> — operators occasionally want \"two parallel corrections\" (e.g. \"what if duration was 90, what if it was 105\"). The chain rule forces them to pick one. The 1% of cases needing branching go through reverted-status + new entry.</li><li><strong>Cycle detection at trigger, not application</strong> — the trigger sees the actual DB state including the new row; application-layer checks would race with concurrent inserts.</li><li><strong><code>current_time_entries_view</code> is the default query target</strong> — downstream code should <code>SELECT * FROM current_time_entries_view WHERE engagement_id = $1</code> rather than the raw table. The index on <code>correction_to WHERE correction_to IS NOT NULL</code> keeps the NOT IN cheap.</li><li><strong><code>rate_card_snapshot JSONB</code> not FK</strong> — the rate card has nested shape (per-role rates, member overrides, time-of-day adjustments); FK to a rate-card-version table would cascade unwantedly. JSONB snapshot is the audit-grade pattern.</li><li><strong><code>entry_currency CHAR(3)</code> not VARCHAR</strong> — fixed-width for ISO-4217 codes; the CHECK constraint enforces uppercase 3-letter shape.</li><li><strong>Chain walker max-depth 100</strong> — safety floor; production chains &gt; 5 are rare. Alarmable.</li><li><strong>DEFERRABLE INITIALLY IMMEDIATE on self-FK</strong> — Postgres-specific quirk. Without it, the cycle-detection trigger fires before the new row's FK validates; with it, FK validation defers to statement end (which is fine because trigger handles cycle).</li><li><strong><code>created_by_subject_id</code> ≠ <code>member_subject_id</code> for AM-on-behalf-of entries</strong> — both fields are immutable; the memory row carries both hash16.</li><li><strong>PII scrubbing applies to <code>description</code></strong> — operators may inadvertently log \"called Person A re: their salary inquiry\"; TASK-MEMORY-111 rules strip.</li><li><strong><code>entry_status</code> default <code>draft</code></strong> — entries start in draft until submitted; TASK-TIME-006 ships the transition handlers. Reading drafts is allowed (caller can review own draft).</li><li><strong><code>billable</code> default false</strong> — conservative. Cascade (TASK-TIME-005) updates to true via correction (creating a new row with billable=true) at row-creation time, before the row is committed. Slice-1 entries are non-billable by default.</li><li><strong>Per-row 24h cap protects daily aggregator</strong> — TASK-TIME-007's daily-cap logic operates on rows; if one row could be 100h, the daily cap math underestimates. Per-row cap closes that hole.</li><li><strong>Idempotency-Key applies to create only</strong> — corrections are not idempotent in the same sense (the prior row determines the new row's contents); retrying a correction creates a new chain entry. Operators should be aware.</li><li><strong>PROJ issue FK is a logical foreign key</strong> — declared in §1 #1 but not enforced as SQL FK at slice 1 (cross-service FK is operationally complex). TASK-PROJ-001's data integrity is trusted; if an issue is deleted, time entries pointing at it become orphaned (still queryable, just decorative).</li><li><strong><code>engagement_id</code> similarly soft-FK</strong> — same reason; TASK-PROJ-005 owns the engagements table.</li><li><strong>No <code>updated_at</code></strong> — append-only means there's no update; <code>created_at</code> is the only timestamp.</li><li><strong>The cycle-detection trigger walks depth 100 max</strong> — beyond that, raises <code>correction_chain_too_deep</code>. Production chains hitting this are bugs.</li><li><strong><code>entry_chain_walker</code> returns oldest-first</strong> — that's the natural temporal order (\"here's how this entry evolved\"). UIs may render newest-first if appropriate.</li><li><strong>The view's <code>NOT IN</code> query</strong> — Postgres optimises with the <code>correction_to_idx</code> partial index; performance is bounded by correction count (~5% of rows).</li><li><strong>REVOKE applies to <code>cyberos_app</code> role only</strong> — superuser + migration role can mutate (for backups, manual repair, etc.). Production app code uses cyberos_app.</li><li><strong><code>entry_kind</code> and <code>entry_status</code> are orthogonal axes</strong> — workflow status (draft → submitted → approved) is independent of working-time classification (regular vs overtime).</li><li><strong><code>description</code> is NOT searchable via full-text</strong> at slice 1 — that's a task-TIME-2xx ambition. Slice 1 stores as TEXT.</li></ul>\n<hr>\n<p><em>End of TASK-TIME-001.</em></p>";
