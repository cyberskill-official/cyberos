window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-012"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The cycle-review generator <strong>MUST</strong> compose a draft review at every cycle close. The contract:</p>\n<ol><li><strong>MUST</strong> schedule a cycle-close detector running every 15 minutes; identifies cycles whose <code>ends_at &lt; NOW()</code> and <code>review_drafted_at IS NULL</code>; queues review generation for each.</li><li><strong>MUST</strong> compute <code>CycleStats</code> for each detected cycle:</li></ol>\n<ul><li><code>completed_count</code> (issues with status=Done where cycle_id matches).</li><li><code>incomplete_count</code> (status != Done|Cancelled).</li><li><code>cancelled_count</code>.</li><li><code>velocity_estimate_points</code> (sum of estimate values on completed issues; per TASK-PROJ-013 estimate field).</li><li><code>velocity_actual_hours</code> (sum of TASK-TIME-005 billable_hours on completed issues).</li><li><code>blocker_count_resolved</code> (TASK-PROJ-011 blockers resolved during cycle).</li><li><code>blocker_count_stale_at_close</code> (active blockers at cycle end).</li><li><code>time_in_status_p50</code> per status (median dwell).</li></ul>\n<ol><li><strong>MUST</strong> compose a markdown draft via TASK-AI-014 COO persona using a fixed prompt template: ``` You are the Chief Operations Officer reviewing cycle \"{{cycle_name}}\" ({{cycle_id}}). Cycle stats: {{stats_json}}. Top incomplete issues: {{incomplete_list}}. Stale blockers: {{stale_blockers}}. Compose a 200-400 word review covering:</li><li>Did we hit the cycle goal?</li><li>What slowed us down?</li><li>One specific learning per stale-blocker pattern.</li><li>Recommendation for next cycle's priorities. Tone: factual, constructive, Vietnamese-context-aware. Avoid blame. ```</li><li><strong>MUST</strong> save the draft as a memory memory at <code>memories/projects/&lt;engagement_id&gt;/cycle-reviews/&lt;cycle_id&gt;.md</code> with frontmatter <code>{kind: cycle_review, status: draft, sync_class: shareable, generated_by_persona: chief-operating-officer@&lt;version&gt;, generated_at: &lt;iso&gt;}</code>. The body is markdown.</li><li><strong>MUST</strong> emit <code>proj.cycle_review_drafted</code> memory audit row with payload <code>{cycle_id, engagement_id, memory_path, draft_byte_count, persona_version, generated_at_ns, trace_id}</code>.</li><li><strong>MUST</strong> NEVER auto-promote draft to <code>status: accepted</code>. Operator (COO) edits + accepts via UI / CLI; acceptance flips frontmatter <code>status</code> and emits <code>proj.cycle_review_accepted</code> row.</li><li><strong>MUST</strong> redact PII in input context via TASK-MEMORY-111 before LLM call (issue descriptions may contain customer info; redact before composition).</li><li><strong>MUST</strong> expose REST <code>POST /api/proj/cycles/:id/review-draft</code> for force re-generation (operator can iterate); preserves all prior drafts as memory revisions.</li><li><strong>MUST</strong> handle LLM failure gracefully: AI Gateway 5xx → retry 3× with exp backoff; final failure → save stats-only stub draft (no prose) + sev-3 alarm; operator iterates manually.</li><li><strong>MUST</strong> RLS-enforce.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_cycle_reviews_drafted_total</code> (counter).</li><li><code>proj_cycle_reviews_acceptance_minutes</code> (histogram — time from draft to accept; ops-quality signal).</li><li><code>proj_cycle_review_llm_failures_total</code> (counter).</li></ul>\n<ol><li><strong>MUST</strong> support locale-aware drafts: <code>cyberos_proj_engagement_settings.cycle_review_locale</code> (default <code>vi-VN</code>); prompt template includes the locale instruction so VN engagements get Vietnamese drafts.</li><li><strong>MUST</strong> include TASK-PROJ-013 estimate-vs-actual delta in stats: <code>velocity_estimate_vs_actual_ratio</code> (estimate_points / actual_hours). Operators see whether estimates were optimistic / pessimistic.</li><li><strong>MUST</strong> include \"Top 5 longest-in-status issues\" list in the input context: issues that spent the most cumulative time in any non-Done status, with status name + days.</li><li><strong>MUST</strong> include TASK-PROJ-011 stale-blocker recap by category: e.g. \"3 blockers stale on customer feedback, 2 on design review.\" Helps operator identify systemic patterns.</li><li><strong>MUST</strong> support per-engagement persona override: <code>cyberos_proj_engagement_settings.cycle_review_persona = \"coo\"|\"pm\"|\"engineering_lead\"</code>. Different engagements may want different review voice.</li><li><strong>MUST</strong> emit a <code>proj.cycle_review_iterated</code> audit row each time the operator re-generates a draft; payload includes iteration_count + reason (optional).</li><li><strong>MUST</strong> preserve all prior draft revisions as memory memory revisions (TASK-MEMORY-101 versioning); the latest is the canonical draft; operators can revert to a prior revision.</li><li><strong>MUST</strong> include <code>comparison_to_prior_cycle</code> section: if the prior cycle's review exists + was accepted, the prompt includes its stats for delta context (\"velocity improved 15% over prior cycle\").</li><li><strong>MUST</strong> redact LLM response too (not just input) before memory save — LLM may echo or hallucinate PII from training data.</li><li><strong>MUST</strong> support \"skip cycle\" annotation: operator marks a cycle \"no review needed\" (e.g. cycle with 1 issue, vacation week); skips draft generation. Audit row <code>proj.cycle_review_skipped</code> with reason.</li><li><strong>MUST</strong> include cycle's <code>learnings</code> section if the cycle had <code>learnings</code> field populated during the cycle (TASK-PROJ-014 kanban allows operator to add running learnings); concatenated into the prompt input.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why auto-draft (DEC-331)?</strong> COO time-on-review consistently runs 60-90min per cycle. A draft cuts to 15min edit. At 10 engagements × 26 cycles/year, that's 200 hours/year saved.</p>\n<p><strong>Why never auto-accept (DEC-331)?</strong> Reviews are quality artifacts — LLM-generated text needs human read. Auto-accept turns reviews into noise; manual accept keeps quality.</p>\n<p><strong>Why save as memory memory (DEC-332)?</strong> Reviews are knowledge — searchable, citable, referenced in future cycle planning. Saving to memory with <code>sync_class: shareable</code> makes them findable via TASK-MEMORY-108 search.</p>\n<p><strong>Why fixed prompt template (§1 #3)?</strong> Drift-prone if free-form. Fixed template = consistent structure across reviews → operators read faster.</p>\n<p><strong>Why fail-graceful with stats-only stub (§1 #9)?</strong> AI Gateway outage shouldn't block cycle close. Stats stub gives operator the numbers; they write the prose manually if needed.</p>\n<p><strong>Why force re-generate endpoint (§1 #8)?</strong> Operator finds draft poor → iterates with edits to underlying stats / context → re-runs. Each iteration is a new memory revision (history preserved).</p>\n<p><strong>Why redact before LLM (§1 #7)?</strong> External LLM provider (Anthropic/OpenAI) shouldn't see customer PII. TASK-MEMORY-111 redaction is the boundary. ZDR contract (TASK-AI-015) ensures even after-redaction data isn't retained.</p>\n<p><strong>Why locale-aware (§1 #12)?</strong> VN-team reviews in Vietnamese, English-team in English. Wrong locale = friction.</p>\n<p><strong>Why estimate-vs-actual ratio (§1 #13)?</strong> Operators tracking estimate accuracy need the ratio explicitly; baseline observation for TASK-PROJ-013 calibration.</p>\n<p><strong>Why top-5 longest-in-status (§1 #14)?</strong> Outliers tell the story; aggregates don't. The operator wants \"this issue spent 12 days in review\" not \"average review time was 3 days.\"</p>\n<p><strong>Why blocker recap by category (§1 #15)?</strong> Pattern detection: 3 blockers all \"waiting for customer\" = sales-process issue; 3 different categories = mixed bag.</p>\n<p><strong>Why per-engagement persona (§1 #16)?</strong> Internal-engineering retro voice differs from client-facing review tone. Persona override respects audience.</p>\n<p><strong>Why iteration audit (§1 #17)?</strong> Operators iterating drafts produce N revisions; tracking the iteration count surfaces \"did the COO have to rewrite 5 times?\"</p>\n<p><strong>Why preserve all revisions (§1 #18)?</strong> Comparison across iterations shows the editing pattern; revert is a safety net.</p>\n<p><strong>Why comparison to prior cycle (§1 #19)?</strong> Context is the value-add: \"velocity improved 15%\" beats raw \"velocity 32 points.\"</p>\n<p><strong>Why redact LLM response (§1 #20)?</strong> Defence in depth; LLM may hallucinate PII not in input (training data leak).</p>\n<p><strong>Why skip-cycle annotation (§1 #21)?</strong> Vacation weeks / 1-issue cycles generating drafts is noise. Explicit skip is the operator override.</p>\n<p><strong>Why include running learnings (§1 #22)?</strong> Operators capturing insights during the cycle (not just at close) get them reflected in the draft.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>CycleStats struct</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/cycle_review/mod.rs\n#[derive(Clone, Debug, Serialize)]\npub struct CycleStats {\n    pub cycle_id:                  uuid::Uuid,\n    pub completed_count:           i32,\n    pub incomplete_count:          i32,\n    pub cancelled_count:           i32,\n    pub velocity_estimate_points:  i32,\n    pub velocity_actual_hours:     f64,\n    pub blocker_count_resolved:    i32,\n    pub blocker_count_stale:       i32,\n    pub time_in_status_p50:        std::collections::HashMap&lt;String, f64&gt;,   // status → median dwell hours\n}</code></pre>\n<h3>Stats computation</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/cycle_review/stats.rs\npub async fn compute(pool: &amp;sqlx::PgPool, cycle_id: uuid::Uuid) -&gt; anyhow::Result&lt;CycleStats&gt; {\n    let counts = sqlx::query!(\n        \"SELECT\n            COUNT(*) FILTER (WHERE status = 'done')      AS completed,\n            COUNT(*) FILTER (WHERE status = 'cancelled') AS cancelled,\n            COUNT(*) FILTER (WHERE status NOT IN ('done', 'cancelled')) AS incomplete,\n            COALESCE(SUM(estimate) FILTER (WHERE status = 'done'), 0)::int4 AS velocity_points\n         FROM issues WHERE cycle_id = $1\",\n        cycle_id\n    ).fetch_one(pool).await?;\n\n    let hours = sqlx::query_scalar!(\n        \"SELECT COALESCE(SUM(duration_minutes), 0)::float8 / 60.0\n         FROM time_entries te JOIN issues i ON i.id = te.issue_id\n         WHERE i.cycle_id = $1 AND te.billable_snapshot = true\",\n        cycle_id\n    ).fetch_one(pool).await?.unwrap_or(0.0);\n\n    let resolved = sqlx::query_scalar!(\n        \"SELECT COUNT(*)::int4 FROM blocker_state bs JOIN issues i ON i.id = bs.issue_id\n         WHERE i.cycle_id = $1 AND bs.resolved_at IS NOT NULL\", cycle_id\n    ).fetch_one(pool).await?.unwrap_or(0);\n\n    let stale = sqlx::query_scalar!(\n        \"SELECT COUNT(*)::int4 FROM blocker_state bs JOIN issues i ON i.id = bs.issue_id\n         WHERE i.cycle_id = $1 AND bs.resolved_at IS NULL\", cycle_id\n    ).fetch_one(pool).await?.unwrap_or(0);\n\n    let time_in_status_p50 = compute_status_dwell_medians(pool, cycle_id).await?;\n\n    Ok(CycleStats {\n        cycle_id,\n        completed_count: counts.completed.unwrap_or(0) as i32,\n        incomplete_count: counts.incomplete.unwrap_or(0) as i32,\n        cancelled_count: counts.cancelled.unwrap_or(0) as i32,\n        velocity_estimate_points: counts.velocity_points.unwrap_or(0),\n        velocity_actual_hours: hours,\n        blocker_count_resolved: resolved,\n        blocker_count_stale: stale,\n        time_in_status_p50,\n    })\n}</code></pre>\n<h3>Composer</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/cycle_review/compose.rs\npub async fn compose(\n    pool: &amp;sqlx::PgPool,\n    cycle: &amp;Cycle,\n    stats: &amp;CycleStats,\n) -&gt; anyhow::Result&lt;String&gt; {\n    let incomplete = list_incomplete_issues(pool, cycle.id).await?;\n    let stale_blockers = list_stale_blockers(pool, cycle.id).await?;\n\n    // PII redaction (TASK-MEMORY-111)\n    let redacted_incomplete = redact_titles(&amp;incomplete);\n    let redacted_blockers   = redact_blockers(&amp;stale_blockers);\n\n    let prompt = format!(r#\"\nYou are the Chief Operations Officer reviewing cycle \"{}\" ({}).\n\nCycle stats:\n{}\n\nTop incomplete issues:\n{}\n\nStale blockers:\n{}\n\nCompose a 200-400 word review covering:\n1. Did we hit the cycle goal?\n2. What slowed us down?\n3. One specific learning per stale-blocker pattern.\n4. Recommendation for next cycle's priorities.\n\nTone: factual, constructive, Vietnamese-context-aware. Avoid blame.\n\"#, cycle.name, cycle.id,\n    serde_json::to_string_pretty(stats)?,\n    format_incomplete_list(&amp;redacted_incomplete),\n    format_blockers_list(&amp;redacted_blockers));\n\n    let resp = ai_gateway::chat_complete(ChatRequest {\n        persona: \"coo\".into(),\n        prompt,\n        max_tokens: 800,\n        ..Default::default()\n    }).await?;\n\n    Ok(resp.text)\n}</code></pre>\n<h3>Save as memory</h3>\n<pre><code class=\"language-rust\">pub async fn save_draft(\n    cycle: &amp;Cycle,\n    body: String,\n    persona_version: &amp;str,\n) -&gt; anyhow::Result&lt;String&gt; {\n    let memory_path = format!(\n        \"memories/projects/{}/cycle-reviews/{}.md\",\n        cycle.engagement_id, cycle.id\n    );\n    let frontmatter = format!(r#\"---\nkind: cycle_review\nsync_class: shareable\ncycle_id: {}\nengagement_id: {}\ngenerated_by_persona: {}\ngenerated_at: {}\n---\n\"#, cycle.id, cycle.engagement_id, persona_version,\n    chrono::Utc::now().to_rfc3339());\n\n    let full = format!(\"{frontmatter}\\n{body}\");\n    memory_writer::put_memory(&amp;memory_path, full.as_bytes()).await?;\n    Ok(memory_path)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Cycle close triggers draft</strong> — cycle.ends_at &lt; NOW + 15min poll → draft generated.</li><li><strong>Stats accurate</strong> — fixture: 5 done, 2 incomplete, 1 cancelled → counts match.</li><li><strong>Velocity points computed</strong> — sum of completed-issues' estimate field.</li><li><strong>Velocity hours from billable time entries</strong> — only billable_snapshot = true entries counted.</li><li><strong>Blocker counts split active vs resolved</strong> — accurate per TASK-PROJ-011 state.</li><li><strong>Time-in-status p50 computed</strong> — 10 issues went through in_progress for various durations → median present.</li><li><strong>Draft saved at canonical path</strong> — memory at <code>memories/projects/&lt;eng&gt;/cycle-reviews/&lt;cycle&gt;.md</code>.</li><li><strong>Frontmatter <code>status: draft</code></strong> — never auto-<code>accepted</code>.</li><li><strong>memory audit <code>cycle_review_drafted</code></strong> — row appears post-save.</li><li><strong>PII redacted before LLM</strong> — fixture title with email → redacted in prompt sent to AI Gateway.</li><li><strong>Force re-generate endpoint</strong> — POST → new draft saved as memory revision.</li><li><strong>LLM 5xx retries</strong> — mock gateway 503 thrice → retries with backoff.</li><li><strong>LLM permanent failure → stub</strong> — mock 503 four times → stats-only stub draft saved; sev-3 alarm.</li><li><strong>Acceptance flips status</strong> — operator UI accept → frontmatter status changes to accepted; <code>cycle_review_accepted</code> row.</li><li><strong>RLS isolates</strong> — tenant A can't see tenant B's reviews.</li><li><strong>OTel metric <code>proj_cycle_reviews_drafted_total</code></strong> — counter increments.</li><li><strong>Acceptance-minutes histogram</strong> — time from draft to accept measured.</li><li><strong>Locale-aware draft</strong> — engagement vi-VN → draft in Vietnamese; en-US → English (AC for §1 #12).</li><li><strong>Estimate-vs-actual ratio in stats</strong> — completed issues have estimate sum 40, actual 50 → ratio=0.8 (AC for §1 #13).</li><li><strong>Top-5 longest-in-status listed</strong> — fixture issues with varying dwell → top 5 in stats (AC for §1 #14).</li><li><strong>Blocker recap by category</strong> — fixture 3 customer-feedback blockers → grouped in input (AC for §1 #15).</li><li><strong>Per-engagement persona override</strong> — set persona=pm; observe persona_version=pm@... in audit (AC for §1 #16).</li><li><strong>Iteration audit emitted</strong> — force re-gen → <code>proj.cycle_review_iterated</code> row with iteration_count (AC for §1 #17).</li><li><strong>All revisions preserved</strong> — 3 re-generations → 3 memory memory revisions; latest = canonical (AC for §1 #18).</li><li><strong>Comparison to prior cycle in prompt</strong> — prior accepted cycle stats included if available (AC for §1 #19).</li><li><strong>LLM response redacted</strong> — fixture LLM output containing PII → memory memory has redacted form (AC for §1 #20).</li><li><strong>Skip-cycle annotation honoured</strong> — operator marks skip → no draft generated; audit row emitted (AC for §1 #21).</li><li><strong>Cycle learnings included</strong> — operator added learnings during cycle → included in prompt (AC for §1 #22).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cycle_close_generates_draft() {\n    let env = TestEnv::new().await;\n    let cycle = env.create_cycle_ending_yesterday().await;\n    env.add_completed_issues(cycle, 5).await;\n    env.run_close_detector().await;\n    let mem = env.memory.read_memory(&amp;format!(\n        \"memories/projects/{}/cycle-reviews/{}.md\", cycle.engagement_id, cycle.id\n    )).await.unwrap();\n    assert!(mem.contains(\"status: draft\"));\n    assert!(mem.len() &gt; 500);\n}\n\n#[tokio::test]\nasync fn pii_redacted_before_llm() {\n    let env = TestEnv::new().await;\n    let cycle = env.create_cycle().await;\n    env.add_issue_with_title_containing_email(cycle).await;\n    let mock = env.ai_gateway.spy().await;\n    env.run_close_detector().await;\n    let prompt = mock.last_prompt().await;\n    assert!(!prompt.contains(\"alice@cyberskill.world\"));\n    assert!(prompt.contains(\"&lt;EMAIL&gt;\"));\n}\n\n#[tokio::test]\nasync fn llm_failure_yields_stub() {\n    let env = TestEnv::new().await;\n    env.ai_gateway.always_503().await;\n    let cycle = env.create_cycle().await;\n    env.run_close_detector().await;\n    let mem = env.memory.read_memory_for_cycle(cycle.id).await.unwrap();\n    assert!(mem.contains(\"status: draft\"));\n    assert!(mem.contains(\"AI Gateway unavailable\"));   // stub body marker\n    assert!(mem.contains(\"completed_count\"));           // stats present\n}\n\n#[tokio::test]\nasync fn force_regenerate() {\n    let env = TestEnv::new().await;\n    let cycle = env.create_cycle_with_existing_draft().await;\n    let _ = post_review_draft(cycle.id).await.unwrap();\n    // Verify revision count\n    let revisions = env.memory.memory_revisions_for_cycle(cycle.id).await;\n    assert!(revisions.len() &gt;= 2);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + components above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-001</strong> — cycles + issues tables.</li><li><strong>TASK-PROJ-004</strong> — status FSM (Done detection).</li><li><strong>TASK-PROJ-011</strong> — blocker_state for blocker counts.</li><li><strong>TASK-AI-014</strong> — persona-stamped LLM calls.</li><li><strong>TASK-AI-015</strong> — ZDR enforcement on the external LLM call.</li><li><strong>TASK-MEMORY-111</strong> — PII redaction.</li><li><strong>TASK-CUO-101 (placeholder)</strong> — future routing of draft notifications.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.cycle_review_drafted\",\n  \"payload\": {\n    \"cycle_id\": \"cyc-...\",\n    \"engagement_id\": \"eng-...\",\n    \"memory_path\": \"memories/projects/eng-.../cycle-reviews/cyc-....md\",\n    \"draft_byte_count\": 2418,\n    \"persona_version\": \"coo@1.0.0\",\n    \"generated_at_ns\": 1747407137483000000,\n    \"trace_id\": \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Multi-language drafts (English vs Vietnamese based on engagement locale) — slice 4+.</li><li>Side-by-side comparison of multi-cycle reviews (operator picks elements from each) — slice 4+.</li><li>Auto-accept after N days idle — never (per DEC-331).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>AI Gateway 5xx</td><td>retry exhausted</td><td>stats-only stub; sev-3</td><td>Operator iterates manually</td></tr><tr><td>Cycle not closed yet</td><td>scheduler condition</td><td>skipped; checked next poll</td><td>None</td></tr><tr><td>Cycle deleted mid-generation</td><td>sqlx Err on read</td><td>task abort; logged</td><td>None</td></tr><tr><td>PII redactor crash</td><td>catch_unwind</td><td>redacted body marked <code>&lt;SCAN_FAILED&gt;</code>; LLM still called with safer placeholder</td><td>Operator updates ruleset</td></tr><tr><td>memory put_memory fails</td><td>sqlx Err</td><td>draft body NOT saved; sev-2</td><td>Operator retries via force endpoint</td></tr><tr><td>Concurrent close detect for same cycle</td><td>row-level lock on cycles table</td><td>One generates; other no-op</td><td>None</td></tr><tr><td>Operator never accepts</td><td>acceptance-minutes histogram tail</td><td>Visible in metrics</td><td>Operator nudge via CUO</td></tr><tr><td>Force-regenerate races acceptance</td><td>second draft saved as revision; status reverts to draft</td><td>Operator re-accepts</td><td>None</td></tr><tr><td>LLM returns &lt; 200 words</td><td>accept anyway (content may still be useful)</td><td>None</td><td>Operator edits</td></tr><tr><td>LLM returns &gt; 400 words</td><td>accept anyway</td><td>None</td><td>Operator trims</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>Memory path collision (multiple drafts)</td><td>memory handles revisions</td><td>All preserved</td><td>None</td></tr><tr><td>Locale config missing</td><td>defaults to vi-VN</td><td>None</td><td>None</td></tr><tr><td>Locale config invalid</td><td>falls back to en-US + warn</td><td>None</td><td>Operator fixes</td></tr><tr><td>Persona override invalid (e.g. \"ceo\" not in registry)</td><td>startup validation</td><td>422 at config save</td><td>None</td></tr><tr><td>Prior cycle review missing (first cycle)</td><td>comparison section omitted</td><td>draft still generated</td><td>None</td></tr><tr><td>Skip annotation race (cycle marked skip after draft generated)</td><td>annotation honoured next time</td><td>None</td><td>None</td></tr><tr><td>Skip annotation reversed</td><td>force re-generate runs</td><td>None</td><td>None</td></tr><tr><td>LLM response redaction overzealous</td><td>manual review catches</td><td>None</td><td>Operator</td></tr><tr><td>Stats query slow for large cycle</td><td>bounded by cycle.issue_count</td><td>sev-3 if &gt; 30s</td><td>None</td></tr><tr><td>Iteration count &gt; 20</td><td>bounded; warn at 10</td><td>None</td><td>Operator</td></tr><tr><td>Revision count &gt; 50</td><td>memory handles; UI paginates</td><td>None</td><td>None</td></tr><tr><td>Persona registry unavailable</td><td>falls back to default coo persona</td><td>None</td><td>Operator restores AI-014</td></tr><tr><td>Locale switch mid-cycle</td><td>uses locale-at-draft-time</td><td>None</td><td>None</td></tr><tr><td>Estimate field missing on issues</td><td>velocity_estimate=null; ratio undefined</td><td>None</td><td>None</td></tr><tr><td>All issues in cycle have no time entries</td><td>velocity_actual=0; ratio undefined</td><td>None</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The schedule is a Postgres cron via pg_cron OR a tokio interval (15min); chosen tokio for simpler local dev. Production may switch to pg_cron for high-availability.</li><li>The COO persona version (<code>coo@1.0.0</code>) is fetched from TASK-AI-014 registry at compose time; stamped in frontmatter for reproducibility.</li><li>The stats-only stub on LLM failure carries the JSON of CycleStats verbatim — operator can read and write the prose.</li><li><code>redact_titles</code> calls TASK-MEMORY-111's <code>scan_and_redact</code> per issue title; results cached for the cycle.</li><li><code>compute_status_dwell_medians</code> queries <code>issue_status_history</code> (TASK-PROJ-004) for transitions within cycle period.</li><li>Force-regenerate creates a new memory revision via TASK-MEMORY-101's content-addressed write; old revisions preserved by memory's natural append-only chain.</li><li>The 200-400 word target is a hint, not enforced; LLM may produce shorter or longer.</li><li>Locale-aware prompts include <code>Locale: {{locale}}. Compose the review in {{language_name}}.</code> Default is vi-VN.</li><li>The estimate-vs-actual ratio is informational; operators may have intentionally over/under-estimated; the LLM should not blame.</li><li>Top-5 longest-in-status uses <code>issue_status_history</code> durations; tied entries broken by issue create_at ascending.</li><li>Blocker recap by category groups via fuzzy text-matching on target_ref (slice 3+ uses ML clustering).</li><li>Per-engagement persona override defaults to \"coo\" if engagement setting absent.</li><li>Iteration audit row's reason field is operator-provided (optional); empty if not provided.</li><li>Revisions are linear (no branching); the latest is canonical; revert = save the old content as a new revision.</li><li>Comparison-to-prior-cycle fetches the most recent accepted review for the engagement; absent → section omitted.</li><li>LLM response redaction runs the same TASK-MEMORY-111 ruleset; same redaction tokens as input.</li><li>Skip-cycle annotation lives in <code>cycle.skip_review</code> BOOLEAN column; toggled via PATCH /cycles/:id.</li><li>Cycle learnings: optional <code>learnings TEXT[]</code> column on cycles; operator adds via PATCH; concatenated into prompt with each as a bullet.</li><li>We considered fully-automated review acceptance after N days (slice 4+) but rejected because: quality drift; operator trust loss.</li><li>The LLM call is rate-limited per tenant (TASK-AI-014's budget); 1 review per cycle is well under any reasonable cap.</li><li>Memory revision storage cost: ~5KB per revision × 5 revisions per cycle × 26 cycles/year × 10 engagements ≈ 6.5MB/year per tenant. Negligible.</li><li>The \"tone: factual, constructive, Vietnamese-context-aware. Avoid blame\" line in the prompt is the most important calibration; tested empirically against 20 sample cycles.</li><li>The cycle review is <code>sync_class: shareable</code> because it's a team artifact; ACL can restrict if engagement has it (per TASK-PROJ-002 engagement default ACL).</li><li>Operator acceptance triggers TASK-PROJ-002 <code>proj.decision</code>-style audit row with <code>decision_id</code> pointing at the cycle review memory.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-012.</em></p>";
