window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-117"]="<h1>TASK-SKILL-117: architectural-spike-author / -audit</h1>\n<h2>§1 - Description</h2>\n<p>A spike is the time-boxed investigation that turns \"we have 2+ plausible architectures\" into evidence an ADR can cite. The ADR skill already demands spike output; nothing can produce it. This task authors the pair and its artefact contract.</p>\n<p>Normative clauses:</p>\n<ol><li>A new artefact type <code>architectural-spike@1</code> MUST be defined with frontmatter: <code>spike_id</code> (SPIKE-&lt;task-ID&gt;-&lt;n&gt;), <code>task_id</code>, <code>question</code> (the single decision under investigation), <code>timebox_hours</code> (int, recorded up front), <code>options</code> (array, each <code>{name, hypothesis, evidence[], cost_estimate, risks[]}</code>), <code>recommendation</code> (names exactly one option), <code>confidence</code> (low|medium|high), <code>discarded</code> (array of <code>{name, reason}</code>), <code>created</code>. Body sections: <code>## Question</code>, <code>## Options probed</code>, <code>## Evidence log</code>, <code>## Recommendation</code>, <code>## Discard log</code>.</li><li><code>architectural-spike-author</code> MUST: take an audited task + repo-context-map as inputs (input envelope), declare its trigger conditions (an ADR is pending AND &gt;= 2 viable options exist, or the task introduces a dependency the repo has never used), enforce the timebox by recording planned vs actual hours, and HALT for the operator when actual exceeds planned by more than 50%.</li><li>Every <code>evidence[]</code> entry MUST cite something checkable: a file path in the repo, a command + its observed output, or an external URL. Unsupported assertions (\"X is faster\") without a citation MUST NOT count as evidence.</li><li><code>architectural-spike-audit</code> MUST score /10 against a RUBRIC.md with at least these rule families: SPK-STRUCT (frontmatter + sections complete, recommendation names exactly one probed option), SPK-EVID (&gt;= 2 options probed, every option has &gt;= 1 checkable evidence entry, recommendation cites evidence), SPK-BOX (timebox recorded, overrun HALT honored), SPK-DISC (discard log non-empty whenever options were rejected). Only 10/10 passes; the audit report format mirrors REPORT_FORMAT.md conventions of the existing full pairs.</li><li><code>architecture-decision-record-author/SKILL.md</code> MUST be updated to name <code>architectural-spike@1</code> as its spike input (replacing the dangling prose reference), and to state the fallback when no spike exists (lean profile: ADR options table carries the evidence inline).</li><li>Both skills MUST carry acceptance <code>TRIGGER_TESTS.md</code> with &gt;= 6 cases each (&gt;= 3 positive, &gt;= 3 negative), following the conventions of the existing trigger-test files (TASK-SKILL-112 lineage).</li><li>File layout MUST match the full-pair convention exactly as listed in <code>new_files</code>, so the TASK-SKILL-118 parity checker passes over this pair with zero exemptions.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The artefact is deliberately evidence-first: the rubric makes uncited claims worthless, which is what separates a spike from an opinion. Timebox recording (plan vs actual, HALT on 1.5x) encodes the whole point of a spike - bounded spend - as a checkable invariant rather than advice. Reusing the full-pair file layout means no new tooling: parity checks, chain-coverage checks, and the plugin build all treat the pair like the existing ten.</p>\n<h2>§3 - Contract</h2>\n<p>Input envelope (author): <code>{task_path, repo_context_map_path, question, timebox_hours, output_dir}</code>. Output envelope: <code>{spike_path, spike_id, recommendation, confidence, halted}</code>. Audit input: <code>{spike_path}</code>; audit output: <code>{verdict: pass|fail|needs_human, score, findings[]}</code>. Envelope JSONs are normative in <code>envelopes/</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Artefact contract is complete</strong> (§1 #1) - SKILL.md's artefact section defines every frontmatter field with types and the five body sections; a sample spike in the acceptance fixtures parses against it.</li><li><strong>Trigger conditions are declared and tested</strong> (§1 #2, #6) - author TRIGGER_TESTS.md contains &gt;= 6 cases including \"ADR pending with 2 options\" (fire) and \"single obvious option\" (no fire).</li><li><strong>Timebox HALT is normative</strong> (§1 #2) - PIPELINE.md contains the plan/actual recording step and the &gt; 1.5x HALT with operator wording; INVARIANTS.md lists it as an invariant.</li><li><strong>Evidence rule is enforceable</strong> (§1 #3, #4) - RUBRIC.md's SPK-EVID rules reject a fixture spike whose option carries only an uncited claim; the fixture and expected finding appear in audit acceptance tests.</li><li><strong>Rubric families complete, 10/10 gate</strong> (§1 #4) - RUBRIC.md defines SPK-STRUCT/EVID/BOX/DISC with rule IDs and states the 10/10 pass bar.</li><li><strong>ADR input is wired</strong> (§1 #5) - architecture-decision-record-author/SKILL.md names architectural-spike@1 and the lean-profile fallback; the dangling prose is gone.</li><li><strong>Layout parity</strong> (§1 #7) - every file in <code>new_files</code> exists; the TASK-SKILL-118 parity check (or, before it lands, a manual <code>ls</code> against the task pair layout) shows no missing file class.</li></ol>\n<h2>§5 - Verification</h2>\n<p>Acceptance-driven (skills are contracts, not code):</p>\n<ul><li><code>modules/skill/architectural-spike-author/acceptance/TRIGGER_TESTS.md</code> - AC 2 cases (&gt;= 6).</li><li><code>modules/skill/architectural-spike-audit/acceptance/TRIGGER_TESTS.md</code> - AC 4 fixture + expected SPK-EVID finding; plus a 10/10 clean fixture.</li><li>Structural check for AC 1, 3, 5, 6, 7: <code>bash tools/install/check-chain-coverage.sh</code> pair rule (from TASK-SKILL-116) plus <code>grep</code> assertions listed inline in the two TRIGGER_TESTS.md preambles (each acceptance file opens with its own \"how to verify this pair\" block, executable line by line).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<p>Author SKILL.md follows the section order of implementation-plan-author (description, triggers, inputs, artefact spec, pipeline pointer, HALT semantics). RUBRIC.md follows task-audit/RUBRIC.md's family/rule-ID formatting. Envelope JSONs mirror the existing pairs' shape (json-schema-lite objects with required arrays).</p>\n<h2>§7 - Dependencies</h2>\n<p>None upstream (authoring is self-contained). Blocks TASK-CUO-209 (the pair joins the vendored set there). TASK-SKILL-118's parity checker adopts this pair automatically once both land.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-yaml\"># spike frontmatter (fixture)\nspike_id: SPIKE-TASK-MEMORY-130-1\ntask_id: TASK-MEMORY-130\nquestion: \"MMR chain vs plain hash chain for audit checkpoints?\"\ntimebox_hours: 6\noptions:\n  - name: mmr\n    hypothesis: \"O(log n) inclusion proofs justify complexity\"\n    evidence: [\"bench: scripts/bench_mmr.sh output 2026-07-12\", \"modules/memory/cyberos/data/AGENTS.md §6.4\"]\n    cost_estimate: \"3-4 days\"\n    risks: [\"new dependency surface\"]\nrecommendation: mmr\nconfidence: medium\ndiscarded: [{name: plain-hash, reason: \"linear proof size fails NFR-PERF-012\"}]</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Whether ship-tasks gains an optional spike step between steps 2 and 3 is TASK-CUO-209 territory (workflow change), not this pair's.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Spike becomes a design doc (unbounded) - SPK-BOX fails the audit when actual hours are absent or the HALT was skipped; the timebox is data, not vibes.</li><li>Evidence rot (cited file later deleted) - audit checks citations resolve AT AUDIT TIME; later rot is caught by TASK-SKILL-119's anchor checker class, out of scope here.</li><li>Recommendation names an unprobed option - SPK-STRUCT explicit rule; fixture covers it.</li><li>Author invoked with no real alternatives - negative trigger tests pin \"single obvious option -&gt; do not fire\"; the ADR proceeds without a spike per §1 #5 fallback.</li><li>Confidence inflation - confidence is an enum the audit cross-checks: <code>high</code> with a single evidence entry per option triggers a SPK-EVID finding.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep artefact version pinned at @1 in both SKILL.md files; version bumps follow the contracts CHANGELOG discipline. The pair is NOT added to build.sh here - vendoring is TASK-CUO-209's single expansion, kept out of this task to avoid two writers on build.sh:28.</p>\n<p><em>End of TASK-SKILL-117.</em></p>";
