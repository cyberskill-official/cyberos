window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-096"]="<h1>TASK-IMP-096: Non-git installs state the ship-tasks git requirement</h1>\n<h2>Summary</h2>\n<p>Install succeeds on a plain directory, and the only clue that the governance loop cannot run there is a hook line's aside. Say the quiet part in the summary: one line, printed only when <code>.git</code> is absent, naming what ship-tasks needs and the exact command to get it.</p>\n<h2>Problem</h2>\n<p>A consumer who installs into a non-git folder discovers the requirement at their first phase commit - the most expensive place to learn it.</p>\n<h2>Proposed Solution</h2>\n<p>In the summary block, when the repo has no <code>.git</code>: print <code>cyberos install: this repo is not a git checkout - ship-tasks needs one; run: git init -b main &amp;&amp; git add -A &amp;&amp; git commit -m init</code>. Git installs print nothing new.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Refuse to install without git. Rejected: install legitimately serves doc-only and evaluation uses; the loop is optional until tasks ship.</li><li>Auto-run git init. Rejected: creating a repository is an operator decision (identity, default branch, what to commit), not an installer side effect.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a scratch non-git install prints exactly one such line and a git install prints none - suite-asserted every run. Baseline: only the hook aside exists. Deadline: final acceptance.</li><li>Guardrail: the line names a command that works verbatim on a fresh directory.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the summary line, the hygiene scenario.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any behavior change for git repos.</li><li>Running git commands on the consumer's behalf.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>Shares install.sh with TASK-IMP-094/095 - one agent, serial.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMP-12; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-4 PLAN approved 2026-07-17 (§0a); both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 When the target repo has no <code>.git</code>, the install summary MUST include one line stating ship-tasks requires a git checkout and naming the verbatim remedy (<code>git init -b main &amp;&amp; git add -A &amp;&amp; git commit -m init</code>).</li><li>1.2 When the target is a git checkout, the summary MUST NOT include the line.</li><li>1.3 Coverage MUST land as a hygiene scenario running install against a non-git scratch directory (CYBEROS_NO_MIGRATE path acceptable) and a git one.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - non-git scratch install shows the line once - test: <code>tools/install/tests/test_install_hygiene.sh::t09_nongit_summary_line</code></li><li>[ ] AC 2 (traces_to: #1.2) - git scratch install shows no such line - test: <code>tools/install/tests/test_install_hygiene.sh::t09_nongit_summary_line (git arm)</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A git worktree or submodule dir where <code>.git</code> is a FILE, not a directory: counts as a git checkout (test uses <code>git rev-parse</code> semantics, not a <code>-d .git</code> probe - matching install.sh's own root detection).</li><li>Bare-ish folders with a stale <code>.git</code> remnant: whatever <code>git rev-parse --show-toplevel</code> says is the truth the installer already uses; the line follows it.</li><li>Security-class: none - one echo; no commands executed on the consumer's behalf.</li></ul>";
