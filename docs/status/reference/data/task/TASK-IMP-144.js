window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-144"]="<h1>TASK-IMP-144: 1.5.0 transition-locked state engine</h1>\n<h2>Summary</h2>\n<p>Close the TASK-CUO-303 residual where an agent edits <code>spec.md</code> frontmatter <code>status:</code> and regenerates BACKLOG without ever calling <code>flip</code>. A single state-engine API owns status transitions; regenerators refuse to invent transition edges without an engine receipt.</p>\n<h2>Problem</h2>\n<p><code>backlog-mutate flip</code> mechanically locks the two HITL gates, but editing frontmatter to <code>done</code> and running <code>regen_backlog</code> rewrites the index from frontmatter alone — R-EXT-01 / ship-tasks accepted residual.</p>\n<h2>Proposed Solution</h2>\n<ol><li><strong>Receipts</strong> — every successful <code>backlog-mutate flip</code> writes a content-addressed receipt under <code>docs/tasks/_state/receipts/&lt;task&gt;--&lt;from&gt;--&lt;to&gt;--&lt;sha12&gt;.json</code> binding task_id, from, to, and (for HITL gates) verdict artifact coordinates.</li><li><strong>Regen refusal</strong> — <code>regen_backlog()</code> parses the previous BACKLOG; when an existing stem's status would change to the frontmatter value, it requires a matching receipt for that <code>(task_id, from, to)</code> triple; otherwise it exits non-zero and writes nothing. New stems (inserts) and identical statuses do not need receipts.</li><li><strong><code>task-state.mjs transition</code></strong> — documented single API: validate args, write frontmatter <code>status</code> to <code>&lt;to&gt;</code>, invoke <code>flip</code> (HITL flags required for gate transitions; IMP-143 artifacts mint), which writes the receipt. Agents MUST use this path; raw FM+regen inventing edges is refused.</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Git hooks only.</strong> Rejected: regen is the write path that invents edges; hooks would race and miss non-git callers.</li><li><strong>Delete regen.</strong> Rejected: regen remains the title/section authority; it must reflect engine-committed status, not invent it.</li><li><strong>Capability-scoped agent identities.</strong> Deferred to a follow-on 1.5.x milestone per the draft.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: FM edit to <code>done</code> + regen without a receipt refuses; <code>task-state transition</code> with HITL flags succeeds and leaves FM, BACKLOG, and receipt aligned.</li><li>Guardrail: existing <code>test_hitl_lock.sh</code> / <code>test_regen_backlog.sh</code> green; IMP-120 truth-precedes-index preserved for bare <code>flip</code>.</li></ul>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Receipts on flip, regen refusal, <code>task-state.mjs</code>, tests, ship-tasks residual retirement, CHANGELOG.</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Replacing the ship-tasks skill chain.</li><li>Cross-tool conformance kit.</li><li>Cryptographic agent identity / ACL (follow-on).</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-CUO-303, TASK-IMP-143]</code>.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) from the batch-9 draft.</li><li><strong>Scope:</strong> full AC authoring + implementation.</li><li><strong>Human review:</strong> session HITL override with recorded evidence (2026-07-25).</li></ul>\n<h2>1. Description</h2>\n<ul><li>1.1 Every successful <code>backlog-mutate flip</code> MUST write one receipt JSON under <code>docs/tasks/_state/receipts/</code> with <code>{schema:\"cyberos.transition@1\", task_id, from, to, evidence_sha256?}</code> and a self-hash field.</li><li>1.2 <code>regen_backlog</code> MUST refuse (non-zero, no write) when an existing BACKLOG stem would change status to a different frontmatter status unless a receipt exists for that exact <code>(task_id, prior_backlog_status, new_frontmatter_status)</code> triple.</li><li>1.3 New task stems appearing only in frontmatter (no prior BACKLOG row) MUST still be allowed without a receipt (insert / first index).</li><li>1.4 <code>task-state.mjs transition &lt;id&gt; &lt;from&gt; &lt;to&gt;</code> MUST write frontmatter status to <code>&lt;to&gt;</code>, then invoke flip with the same args/flags, and MUST exit non-zero if either step fails (no partial success: if flip fails after FM write, restore prior FM status).</li><li>1.5 Gate transitions still require IMP-143 / CUO-303 verdict flags; non-gate transitions stay flag-free.</li><li>1.6 <code>ship-tasks.md</code> MUST retire the \"accepted residual\" sentence for the frontmatter bypass and point at <code>task-state.mjs</code> + regen refusal.</li></ul>\n<h2>Acceptance Criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1–1.3) — <code>test_task_state_engine.sh</code> proves regen refuses invented edges and accepts receipt-backed ones.</li><li>[ ] AC 2 (traces_to: #1.4–1.5) — same suite proves <code>task-state transition</code> updates FM+BACKLOG+receipt; gate path needs verdict flags.</li><li>[ ] AC 3 (traces_to: #1.6) — ship-tasks + CHANGELOG document the closure.</li></ul>\n<h2>Test plan</h2>\n<ol><li><code>bash tools/install/tests/test_task_state_engine.sh</code></li><li><code>bash scripts/tests/test_regen_backlog.sh</code></li><li><code>bash .cyberos/cuo/gates/run-gates.sh</code></li></ol>";
