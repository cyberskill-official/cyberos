window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PORTAL-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship Progressive Web App support at <code>services/portal/src/pwa/</code> with per-tenant manifest, service worker for offline-cached views + push notifications, VAPID-signed Web Push, user notification preferences, mobile-first responsive UI, and 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>pwa_notification_kind</code> enum: <code>('workflow_status_changed','dsar_ready','genie_mention','channel_mention','billing_alert')</code> per DEC-1266. Cardinality asserts 5.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_pwa_subscriptions</code> at migration <code>0020</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, subject_id UUID NOT NULL, endpoint_url_kms_blob BYTEA NOT NULL, p256dh_key_kms_blob BYTEA NOT NULL, auth_key_kms_blob BYTEA NOT NULL, user_agent TEXT, enabled_kinds JSONB NOT NULL DEFAULT '[\"workflow_status_changed\",\"dsar_ready\",\"genie_mention\",\"channel_mention\",\"billing_alert\"]'::jsonb, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), last_used_at TIMESTAMPTZ, revoked_at TIMESTAMPTZ)</code>. Partial unique on <code>(subject_id, endpoint_url_sha256_for_indexing)</code> to dedupe per device.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_pwa_notifications_log</code> at migration <code>0021</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, subject_id UUID NOT NULL, kind pwa_notification_kind NOT NULL, payload_sha256 CHAR(64) NOT NULL, delivery_status TEXT NOT NULL CHECK (delivery_status IN ('sent','failed','rate_limited')), failure_reason TEXT, sent_at TIMESTAMPTZ NOT NULL DEFAULT now(), trace_id CHAR(32))</code>. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS scoped to subject_id self-access.</li></ol>\n<ol><li><strong>MUST</strong> generate per-tenant manifest at <code>GET /manifest.json</code> per DEC-1261 with caller's tenant brand applied: ``<code>json { \"name\": \"&lt;tenant_display_name&gt;\", \"short_name\": \"&lt;tenant_short_name&gt;\", \"icons\": [{ \"src\": \"/cdn/brand/&lt;slug&gt;/favicon.png?v=...\", \"sizes\": \"192x192\", \"type\": \"image/png\" }], \"theme_color\": \"&lt;brand.primary&gt;\", \"background_color\": \"&lt;brand.background&gt;\", \"display\": \"standalone\", \"start_url\": \"/\" } </code>``</li></ol>\n<p>Manifest cached 1h client-side; invalidated by brand-pack update via TASK-PORTAL-002 NATS event.</p>\n<ol><li><strong>MUST</strong> generate versioned service worker at <code>GET /service-worker.js</code> per DEC-1262 + DEC-1265 + DEC-1269. Worker code:</li></ol>\n<ul><li>Cache name <code>cyberos-portal-v{version}</code> (version = deploy timestamp).</li><li>Network-first strategy for <code>/v1/portal/views/*</code> with 24h cache fallback.</li><li>Cache-first for static assets (<code>/static/*</code>) with 30d TTL.</li><li>On activation: deletes stale cache versions.</li><li>Push event handler: shows notification with brand-themed icon + body from payload.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/portal/pwa/subscribe</code> body <code>{ endpoint, p256dh, auth, user_agent }</code>. Handler:</li></ol>\n<ul><li>Validates JWT.</li><li>KMS-encrypts endpoint + keys.</li><li>UPSERTs subscription row.</li><li>Emit <code>portal.pwa_subscription_created</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/portal/pwa/unsubscribe</code> body <code>{ subscription_id }</code> → revokes subscription. Emit <code>portal.pwa_subscription_revoked</code>.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>PATCH /v1/portal/pwa/preferences</code> body <code>{ enabled_kinds: [...] }</code> → updates <code>enabled_kinds</code> JSONB.</li></ol>\n<ol><li><strong>MUST</strong> dispatch push notifications per DEC-1263 via <code>pwa/push_dispatcher.rs</code>. Consumer of NATS events from TASK-PORTAL-006 (workflow_status_changed), TASK-PORTAL-008 (dsar_ready), TASK-PORTAL-005 (genie_mention), TASK-CHAT-005 (channel_mention), TASK-INV-001 (billing_alert). For each event:</li></ol>\n<ul><li>Lookup subscriptions for target subject_id WHERE kind IN enabled_kinds AND revoked_at IS NULL.</li><li>Sign VAPID JWT per RFC 8292.</li><li>POST to push endpoint with encrypted payload (Web Push encryption per RFC 8291).</li><li>Log delivery_status; emit <code>portal.pwa_notification_sent</code> or <code>portal.pwa_notification_delivery_failed</code>.</li></ul>\n<ol><li><strong>MUST</strong> rate-limit at 100 push/day/subject per DEC-1268. Excess → log <code>delivery_status='rate_limited'</code>; user not notified (avoid notification spam).</li></ol>\n<ol><li><strong>MUST</strong> sign push via VAPID per DEC-1263. Per-deployment VAPID keypair generated at deploy time; private key in KMS; public key embedded in service worker.</li></ol>\n<ol><li><strong>MUST</strong> mobile-first responsive per DEC-1271. Tailwind breakpoints: <code>sm: 640px, md: 768px, lg: 1024px, xl: 1280px</code>. Minimum viewport 320×568 supported.</li></ol>\n<ol><li><strong>MUST</strong> apply per-tenant brand to install-prompt + notification icon + push notification body (theme_color from TASK-PORTAL-002).</li></ol>\n<ol><li><strong>MUST</strong> version service worker on every deploy per DEC-1269. Cache key includes deploy timestamp; old caches deleted on activation.</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1270:</li></ol>\n<ul><li><code>portal.pwa_subscription_created</code> (sev-3)</li><li><code>portal.pwa_subscription_revoked</code> (sev-3)</li><li><code>portal.pwa_notification_sent</code> (sev-3 — high-volume, sampled 1%)</li><li><code>portal.pwa_notification_delivery_failed</code> (sev-2 — security signal could indicate compromised endpoint)</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub: payload_sha256 only in audit chain; raw payload in delivery_log only.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from source event through dispatcher to delivery log.</li></ol>\n<ol><li><strong>MUST</strong> cache only READ operations offline per DEC-1264. Write operations (POST/PUT/DELETE) queue in IndexedDB + retry on reconnect; service worker queues but never silently writes.</li></ol>\n<ol><li><strong>MUST NOT</strong> include sensitive data in push payload (push provider servers see payload until decrypted by browser). Payload = generic notification text + <code>workflow_id</code> opaque reference; user must open PWA to see content.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why per-tenant manifest (§1 #5, DEC-1261)?</strong> White-label = each tenant's portal looks like THEIR product. Installed app name = \"Acme Client Portal\" not \"CyberOS\". Manifest synthesis at request time matches that expectation.</p>\n<p><strong>Why network-first for views with 24h fallback (§1 #6, DEC-1265)?</strong> Views show live data; stale-cached display = confusing. Network-first = always-fresh when online; fallback ensures basic browsing offline.</p>\n<p><strong>Why VAPID per RFC 8292 (§1 #12, DEC-1263)?</strong> Web Push standard; without VAPID, browsers reject the subscription. Per-deployment key = single trust anchor.</p>\n<p><strong>Why rate-limit push at 100/day (§1 #11, DEC-1268)?</strong> Notification fatigue is real — beyond ~10/day users disable. 100 is generous upper bound; sustained high volume = bug or attack.</p>\n<p><strong>Why generic push payload (§1 #20)?</strong> Push providers (Apple, Google, Mozilla, Microsoft) see payload before it reaches the browser. Sensitive content in payload = third-party data exposure. Generic + ref-to-PWA pattern is industry standard.</p>\n<p><strong>Why mobile-first responsive design (§1 #13, DEC-1271)?</strong> Vietnamese market is mobile-first (&gt;70% smartphone-only users); other client markets trending same way. Desktop is the secondary experience.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0020_portal_pwa_subscriptions.sql\nCREATE TYPE pwa_notification_kind AS ENUM ('workflow_status_changed','dsar_ready','genie_mention','channel_mention','billing_alert');\n\nCREATE TABLE portal_pwa_subscriptions (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  subject_id UUID NOT NULL,\n  endpoint_url_kms_blob BYTEA NOT NULL,\n  endpoint_url_sha256 CHAR(64) NOT NULL,    -- for dedup index\n  p256dh_key_kms_blob BYTEA NOT NULL,\n  auth_key_kms_blob BYTEA NOT NULL,\n  kms_key_id TEXT NOT NULL,\n  user_agent TEXT,\n  enabled_kinds JSONB NOT NULL DEFAULT '[\"workflow_status_changed\",\"dsar_ready\",\"genie_mention\",\"channel_mention\",\"billing_alert\"]'::jsonb,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  last_used_at TIMESTAMPTZ,\n  revoked_at TIMESTAMPTZ\n);\nCREATE UNIQUE INDEX uniq_subscription_per_device\n  ON portal_pwa_subscriptions(subject_id, endpoint_url_sha256)\n  WHERE revoked_at IS NULL;\nALTER TABLE portal_pwa_subscriptions ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_pwa_subscriptions_rls ON portal_pwa_subscriptions\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND subject_id = current_setting('auth.subject_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND subject_id = current_setting('auth.subject_id')::uuid);\nREVOKE DELETE ON portal_pwa_subscriptions FROM cyberos_app;\nGRANT UPDATE (enabled_kinds, last_used_at, revoked_at) ON portal_pwa_subscriptions TO cyberos_app;\n\n-- 0021_portal_pwa_notifications_log.sql\nCREATE TABLE portal_pwa_notifications_log (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  subject_id UUID NOT NULL,\n  subscription_id BIGINT REFERENCES portal_pwa_subscriptions(id),\n  kind pwa_notification_kind NOT NULL,\n  payload_sha256 CHAR(64) NOT NULL,\n  delivery_status TEXT NOT NULL CHECK (delivery_status IN ('sent','failed','rate_limited')),\n  failure_reason TEXT,\n  sent_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_pwa_log_subject ON portal_pwa_notifications_log(subject_id, sent_at DESC);\nALTER TABLE portal_pwa_notifications_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_pwa_notifications_log_rls ON portal_pwa_notifications_log\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_pwa_notifications_log FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">GET    /manifest.json                                 (public, per-caller-tenant)\nGET    /service-worker.js                             (public, versioned)\nPOST   /v1/portal/pwa/subscribe                       (caller)\nPOST   /v1/portal/pwa/unsubscribe                     (caller)\nPATCH  /v1/portal/pwa/preferences                     (caller — toggle kinds)\nGET    /v1/portal/pwa/subscriptions                   (caller — own subscriptions)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>pwa_notification_kind cardinality</strong> — 5 values.</li><li><strong>Per-tenant manifest</strong> — GET /manifest.json returns tenant's brand <code>name</code>, <code>theme_color</code>, icons.</li><li><strong>Service worker versioned</strong> — current version differs from prior deploy's; old cache deleted on activation.</li><li><strong>Subscribe encrypts endpoint</strong> — endpoint/keys never in plaintext in DB.</li><li><strong>Push delivered</strong> — fixture event → push dispatched → notification log row with <code>sent</code>.</li><li><strong>Opt-out respected</strong> — user disables <code>genie_mention</code>; subsequent genie mentions → no push, no log row.</li><li><strong>Rate limit 100/day</strong> — 101st push → <code>rate_limited</code> log row + no actual push.</li><li><strong>VAPID signed</strong> — outgoing push request has valid VAPID JWT header.</li><li><strong>Offline cache 24h</strong> — disconnect network; cached views still loadable for 24h.</li><li><strong>Static asset 30d cache</strong> — repeated load shows cache-hit until 30d expiry.</li><li><strong>Manifest cache 1h</strong> — manifest re-fetched after 1h.</li><li><strong>Generic payload</strong> — push payload contains no sensitive data; only ref + generic text.</li><li><strong>Mobile-first viewport 320px</strong> — UI renders at 320×568 without horizontal scroll.</li><li><strong>Brand pack applied</strong> — install icon + theme color match TASK-PORTAL-002 active pack.</li><li><strong>NATS event → push</strong> — dispatcher subscribes to events; matching subscription gets push.</li><li><strong>Subscription dedup per device</strong> — same endpoint twice → second is no-op.</li><li><strong>Failed delivery logged</strong> — push provider 410 (gone) → marks subscription revoked + log failure.</li><li><strong>4 memory audit kinds emitted</strong> — subscribe + revoke + sent + failed all emit.</li><li><strong>Trace_id threaded</strong> — source event → dispatcher → push → log all share trace_id.</li><li><strong>Cache invalidation on deploy</strong> — new SW version triggers old-cache delete on activation.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// 5.1 manifest per-tenant\n#[tokio::test]\nasync fn manifest_returns_tenant_brand() {\n    let ctx = TestContext::with_branded_tenant(\"acme\", \"#1A73E8\").await;\n    let r = ctx.get_authenticated(\"/manifest.json\").await;\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert!(body[\"name\"].as_str().unwrap().contains(\"Acme\"));\n    assert_eq!(body[\"theme_color\"], \"#1A73E8\");\n}\n\n// 5.2 push delivered\n#[tokio::test]\nasync fn workflow_status_change_triggers_push() {\n    let ctx = TestContext::with_pwa_subscription(\"alice\").await;\n    ctx.publish_nats_event(\"workflow_status_changed\", json!({\"subject_id\": ctx.alice_id, ...})).await;\n    tokio::time::sleep(Duration::from_millis(500)).await;\n    let r = sqlx::query_as::&lt;_, (String,)&gt;(\"SELECT delivery_status FROM portal_pwa_notifications_log WHERE subject_id=$1\")\n        .bind(ctx.alice_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(r.0, \"sent\");\n}\n\n// 5.3 rate limit\n// 5.4 opt-out\n// 5.5 VAPID signature\n// 5.6 offline cache\n// 5.7 generic payload (no sensitive data)\n// 5.8 mobile viewport\n// 5.9 dedup\n// 5.10 audit emission</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-PORTAL-001 (views to cache offline). <strong>Cross-module:</strong> TASK-PORTAL-002 (brand in manifest + notifications), TASK-PORTAL-003 (auth for subscribe), TASK-PORTAL-005 (genie_mention event source), TASK-PORTAL-006 (workflow_status_changed event), TASK-PORTAL-008 (dsar_ready event), TASK-CHAT-005 (channel_mention), TASK-INV-001 (billing_alert), TASK-EMAIL-001 (fallback channel), TASK-AI-003, TASK-MEMORY-111. <strong>Downstream:</strong> None.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>portal.pwa_notification_sent</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.pwa_notification_sent\",\n  \"severity\": 3,\n  \"tenant_id\": \"8a2f...\",\n  \"trace_id\": \"...\",\n  \"occurred_at\": \"2026-05-17T...\",\n  \"payload\": {\n    \"subject_id_hash16\": \"f8a1...\",\n    \"kind\": \"workflow_status_changed\",\n    \"subscription_id\": 42,\n    \"payload_sha256\": \"9c4e...\"\n  }\n}</code></pre>\n<p>Manifest:</p>\n<pre><code class=\"language-json\">{\n  \"name\": \"Acme Client Portal\",\n  \"short_name\": \"Acme\",\n  \"icons\": [\n    { \"src\": \"/cdn/brand/acme/favicon.png?v=f8a1\", \"sizes\": \"192x192\", \"type\": \"image/png\" },\n    { \"src\": \"/cdn/brand/acme/splash.png?v=f8a1\", \"sizes\": \"512x512\", \"type\": \"image/png\" }\n  ],\n  \"theme_color\": \"#1A73E8\",\n  \"background_color\": \"#FFFFFF\",\n  \"display\": \"standalone\",\n  \"start_url\": \"/\",\n  \"scope\": \"/\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Notification scheduling (quiet hours, time-zone aware) — slice 3.</li><li><strong>Deferred:</strong> Rich notifications with action buttons — slice 3.</li><li><strong>Deferred:</strong> Background sync for write queueing — slice 3.</li><li><strong>Deferred:</strong> App badges (unread count) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Push endpoint returns 410 Gone</td><td>HTTP status</td><td>Mark subscription revoked + audit failed</td><td>Subscription pruned</td></tr><tr><td>Push endpoint returns 429</td><td>HTTP status</td><td>Retry after Retry-After; if persistent, mark failed</td><td>Inherent</td></tr><tr><td>VAPID JWT signing fails</td><td>KMS error</td><td>Sev-2 alert; notification dropped</td><td>KMS recovery</td></tr><tr><td>Manifest brand pack lookup fails</td><td>KMS or DB</td><td>Default brand applied + sev-3 log</td><td>Inherent fallback</td></tr><tr><td>Service worker version mismatch race</td><td>install/activate</td><td>Old/new SW coexist briefly; client refresh</td><td>Inherent SW lifecycle</td></tr><tr><td>Offline cache &gt; quota</td><td>browser eviction</td><td>Browser evicts LRU; sev-3 log</td><td>Inherent</td></tr><tr><td>Push payload &gt; 4 KiB</td><td>size check</td><td>413 from push provider; truncate to generic</td><td>Inherent</td></tr><tr><td>Subject revoked mid-push-dispatch</td><td>subscription RLS</td><td>Push not sent; log row</td><td>Inherent</td></tr><tr><td>Cross-tenant push dispatch attempt</td><td>RLS</td><td>0 subscriptions matched; no push</td><td>Inherent</td></tr><tr><td>Notification kind disabled</td><td>enabled_kinds check</td><td>No push; no log row (silent opt-out)</td><td>Inherent</td></tr><tr><td>Rate limit hit</td><td>counter</td><td><code>rate_limited</code> log; no push delivered</td><td>User receives next-day</td></tr><tr><td>Browser blocks notifications at OS level</td><td>subscribe returns Err</td><td>Log + user notified in UI</td><td>User enables in OS settings</td></tr><tr><td>Service worker registration fails</td><td>client-side error</td><td>PWA degraded to standard web; sev-3 log</td><td>User upgrades browser</td></tr><tr><td>Manifest icons 404</td><td>client browser error</td><td>Default-icon used</td><td>Brand pack uploads fixed</td></tr><tr><td>Encrypted payload decryption fails on browser</td><td>push silently dropped</td><td>Sev-2 audit; subscription marked suspect</td><td>Re-subscribe</td></tr><tr><td>Subscribing same device twice</td><td>dedup index</td><td>Second is no-op</td><td>Inherent</td></tr><tr><td>Subscription leaked (sold/lost device)</td><td>manual revoke endpoint</td><td>User revokes via /unsubscribe</td><td>User-initiated</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> <code>web-push</code> Rust crate handles VAPID signing + Web Push encryption per RFCs.</p>\n<p><strong>§11.2</strong> Manifest generated server-side per request (no cache key collision across tenants).</p>\n<p><strong>§11.3</strong> Service worker source code in <code>services/portal/web/pwa/service-worker.ts</code> compiled to JS at deploy time; version stamped via build step.</p>\n<p><strong>§11.4</strong> Push payload format: <code>{ \"kind\": \"&lt;enum&gt;\", \"title\": \"&lt;generic&gt;\", \"body\": \"&lt;generic&gt;\", \"url\": \"https://&lt;slug&gt;.cyberos.world/...\" }</code>. ≤ 4 KiB total per push provider limits.</p>\n<p><strong>§11.5</strong> VAPID keypair generated once per deployment via <code>cyberos-portal vapid-gen</code>; private key in KMS.</p>\n<p><strong>§11.6</strong> Rate limit Redis sliding-window per (subject_id, day).</p>\n<p><strong>§11.7</strong> Tailwind config tuned for mobile-first; default breakpoint chain starts mobile.</p>\n<p><strong>§11.8</strong> IndexedDB queue for offline writes (slice 3 enhancement); slice 2 = network-required for writes.</p>\n<p><strong>§11.9</strong> Notification dedup at client side via service worker check (avoid double-show on push + manual refresh).</p>\n<p><strong>§11.10</strong> Per-tenant Manifest can be heavily cached (1h client + edge); brand changes propagate via service-worker cache invalidation on next visit.</p>\n<hr>\n<p><em>End of TASK-PORTAL-007 spec.</em></p>";
