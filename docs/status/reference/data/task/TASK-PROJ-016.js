window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-016"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The Gantt view <strong>MUST</strong> extend the TASK-PROJ-015 Timeline with directed dependencies + critical-path highlighting. The contract:</p>\n<ol><li><strong>MUST</strong> define <code>issue_dependencies</code> table: <code>(predecessor_id UUID, successor_id UUID, kind TEXT='finish_to_start', created_at, created_by, tenant_id, PK (predecessor_id, successor_id))</code>.</li><li><strong>MUST</strong> reject INSERT that would create a cycle: BFS from <code>successor_id</code> through existing edges; if <code>predecessor_id</code> reachable → 422 <code>cycle_detected</code>.</li><li><strong>MUST</strong> render dependency edges as SVG arrows: right-angle path from predecessor bar's right edge → successor bar's left edge.</li><li><strong>MUST</strong> compute critical path per cycle: longest path through the DAG (where weight = issue.estimate or fallback to days span). Mark all bars on the critical path with a thick gold border.</li><li><strong>MUST</strong> memoise critical-path computation per (cycle_id, dependency-graph-version); recompute on dependency or estimate change.</li><li><strong>MUST</strong> expose CRUD endpoints:</li></ol>\n<ul><li><code>POST /api/proj/issues/:id/dependencies</code> body <code>{predecessor_id, kind}</code> → 201 or 422 (cycle | duplicate).</li><li><code>DELETE /api/proj/issues/:id/dependencies/:predecessor_id</code> → 204.</li><li><code>GET /api/proj/cycles/:id/dependencies</code> → list of edges + computed critical-path subset.</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit rows:</li></ol>\n<ul><li><code>proj.dependency_added</code> on POST.</li><li><code>proj.dependency_removed</code> on DELETE.</li><li><code>proj.critical_path_recomputed</code> with hash of new path on change.</li></ul>\n<ol><li><strong>MUST</strong> support kbd shortcut: focused bar + <code>D</code> → opens dependency dialog (target picker = focus next bar, Enter to confirm).</li><li><strong>MUST</strong> propagate roll-up via <code>parent_issue_id</code> (per TASK-PROJ-001): parent's date range = min(child.starts_at) → max(child.ends_at); rendered as a parent bar above the children (collapsible group).</li><li><strong>MUST</strong> RLS-enforce.</li><li><strong>MUST</strong> pass axe-core (a11y for SVG arrows: <code>role=\"presentation\"</code> since edges are decorative; navigation via dependency dialog only).</li><li><strong>MUST</strong> emit OTel:</li></ol>\n<ul><li><code>proj_gantt_critical_path_depth</code> (histogram).</li><li><code>proj_gantt_dependencies_total{outcome}</code> (counter; created | removed | cycle_rejected).</li></ul>\n<ol><li><strong>MUST</strong> compute and surface \"slack\" per non-critical-path issue: how many days the issue can slip before becoming critical. Rendered as a subtle ghost extension on the bar's right edge.</li><li><strong>MUST</strong> support <code>?show_critical_only=true</code> URL filter — hide non-critical-path bars to focus on the bottleneck chain.</li><li><strong>MUST</strong> support <code>cyberos gantt validate-graph --cycle &lt;id&gt;</code> CLI that walks the dependency graph + reports anomalies (disconnected components, near-cycles, very-long paths &gt; 30 days).</li><li><strong>MUST</strong> detect \"near-cycles\" (cycle would form if any one edge added) and emit <code>proj.dependency_near_cycle</code> SEV-3 audit — informational; helps operators avoid future cycle attempts.</li><li><strong>MUST</strong> support dependency types as forward-compatible (slice 4+ adds S→S/F→F/S→F): <code>kind</code> column accepts only <code>finish_to_start</code> in MVP; CHECK constraint allows future enum values without migration.</li><li><strong>MUST</strong> export Gantt-as-PDF (slice 3 minimal: A3 landscape, fixed zoom; full export options slice 4+) via <code>GET /api/proj/cycles/:id/gantt.pdf</code>.</li><li><strong>MUST</strong> include \"earliest start\" + \"latest finish\" annotations on critical-path bars (the dates that would absorb slippage).</li><li><strong>MUST</strong> support batch dependency CRUD: <code>POST /api/proj/dependencies/batch</code> for bulk import (max 100 edges per request); cycle detection runs on the batch as a whole.</li><li><strong>MUST</strong> include parent issue \"completion %\" in roll-up: parent bar visually segments by child completion (e.g. 3 of 5 children done = 60% green / 40% empty).</li><li><strong>MUST</strong> support kbd shortcut <code>Shift+D</code> to remove last-added dependency for focused bar (quick undo).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why finish-to-start only (DEC-371)?</strong> Other dependency kinds (start-to-start, finish-to-finish, start-to-finish) exist in Microsoft Project but rare in software work. Slice 3 ships F→S; slice 4+ adds others.</p>\n<p><strong>Why DAG (no cycles) (§1 #2)?</strong> Cycles are nonsensical (A waits on B waits on A); without prevention, critical-path algorithm infinite-loops. BFS cycle detect at write = O(N) per insert.</p>\n<p><strong>Why critical path matters (§1 #4)?</strong> Operators ask \"what's the bottleneck path.\" Without highlight, the answer requires manual graph reading. Gold border = visual primitive.</p>\n<p><strong>Why memoise critical path (§1 #5)?</strong> Recompute is O(V+E) per render = perf cliff at 1000+ issues. Memoize on dependency-graph version (hash of edges).</p>\n<p><strong>Why parent roll-up (§1 #9)?</strong> Epics (parent issues) want to show \"my whole subtree's date range\" — operators planning at the epic level. Auto-computed eliminates manual epic-date maintenance.</p>\n<p><strong>Why <code>D</code> shortcut (§1 #8)?</strong> Kbd parity; mouse alternative requires modal dialog button.</p>\n<p><strong>Why slack (§1 #13)?</strong> Non-critical issues have flexibility (\"this can slip 3 days without affecting the cycle\"); operators need to see it.</p>\n<p><strong>Why critical-only filter (§1 #14)?</strong> Bottleneck focus: hide noise, see what blocks the cycle.</p>\n<p><strong>Why graph-validate CLI (§1 #15)?</strong> Operators auditing graph health want anomaly reports outside the UI; CLI is the operator entry point.</p>\n<p><strong>Why near-cycle detection (§1 #16)?</strong> Approaching cycles helps operators understand graph shape; informational warning.</p>\n<p><strong>Why forward-compatible kind (§1 #17)?</strong> Schema migration is expensive; future kinds slot in without DDL change.</p>\n<p><strong>Why PDF export (§1 #18)?</strong> Stakeholder reports need a portable artifact; PDF is the lingua franca.</p>\n<p><strong>Why earliest-start / latest-finish (§1 #19)?</strong> Operators rescheduling critical path need the bounds — \"can I start this on Tuesday and still hit the cycle?\"</p>\n<p><strong>Why batch dependency CRUD (§1 #20)?</strong> Bulk imports (migrating from other tools) need batch; per-edge HTTP is slow.</p>\n<p><strong>Why parent completion % (§1 #21)?</strong> Epic-level progress signal at a glance.</p>\n<p><strong>Why Shift+D quick undo (§1 #22)?</strong> Accidental dependency add is common; quick undo reduces friction.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0016_issue_dependencies.sql\nCREATE TABLE issue_dependencies (\n    predecessor_id  UUID NOT NULL,\n    successor_id    UUID NOT NULL,\n    kind            TEXT NOT NULL DEFAULT 'finish_to_start' CHECK (kind = 'finish_to_start'),\n    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    created_by      UUID NOT NULL,\n    tenant_id       UUID NOT NULL,\n    PRIMARY KEY (predecessor_id, successor_id),\n    CHECK (predecessor_id != successor_id)   -- no self-edge\n);\nCREATE INDEX idx_deps_succ ON issue_dependencies (successor_id);\nCREATE INDEX idx_deps_pred ON issue_dependencies (predecessor_id);\n\nALTER TABLE issue_dependencies ENABLE ROW LEVEL SECURITY;\nCREATE POLICY deps_tenant_iso ON issue_dependencies\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Rust</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/dependencies/mod.rs\nuse serde::Serialize;\n\n#[derive(Debug, thiserror::Error)]\npub enum DepError {\n    #[error(\"cycle would form: {0} → ... → {1}\")] CycleDetected(uuid::Uuid, uuid::Uuid),\n    #[error(\"self-edge forbidden\")] SelfEdge,\n    #[error(\"dependency already exists\")] AlreadyExists,\n    #[error(\"db: {0}\")] Db(String),\n}\n\npub async fn add_dependency(\n    pool: &amp;sqlx::PgPool,\n    predecessor: uuid::Uuid,\n    successor: uuid::Uuid,\n    subject: uuid::Uuid,\n) -&gt; Result&lt;(), DepError&gt; {\n    if predecessor == successor { return Err(DepError::SelfEdge); }\n\n    // Cycle detection: BFS from successor; if predecessor reached → cycle\n    if reaches(pool, successor, predecessor).await? {\n        return Err(DepError::CycleDetected(predecessor, successor));\n    }\n\n    sqlx::query(\n        \"INSERT INTO issue_dependencies (predecessor_id, successor_id, created_by, tenant_id)\n         VALUES ($1, $2, $3, current_setting('app.tenant_id')::uuid)\"\n    ).bind(predecessor).bind(successor).bind(subject)\n     .execute(pool).await\n     .map_err(|e| if e.to_string().contains(\"issue_dependencies_pkey\") {\n         DepError::AlreadyExists\n     } else { DepError::Db(e.to_string()) })?;\n\n    emit_memory_row(\"proj.dependency_added\", serde_json::json!({\n        \"predecessor_id\": predecessor, \"successor_id\": successor,\n        \"by_subject_id\": subject,\n    })).await;\n    metrics::counter!(\"proj_gantt_dependencies_total\", \"outcome\" =&gt; \"created\").increment(1);\n    Ok(())\n}\n\nasync fn reaches(pool: &amp;sqlx::PgPool, start: uuid::Uuid, target: uuid::Uuid) -&gt; Result&lt;bool, DepError&gt; {\n    // BFS through issue_dependencies starting at `start`; return true if `target` reached\n    let mut frontier = vec![start];\n    let mut visited = std::collections::HashSet::new();\n    while let Some(node) = frontier.pop() {\n        if !visited.insert(node) { continue; }\n        if node == target { return Ok(true); }\n        let next: Vec&lt;uuid::Uuid&gt; = sqlx::query_scalar(\n            \"SELECT successor_id FROM issue_dependencies WHERE predecessor_id = $1\"\n        ).bind(node).fetch_all(pool).await.map_err(|e| DepError::Db(e.to_string()))?;\n        frontier.extend(next);\n    }\n    Ok(false)\n}</code></pre>\n<h3>Critical-path algorithm (TS)</h3>\n<pre><code class=\"language-typescript\">// web/proj-client/src/views/Gantt/critical_path.ts\ntype Edge = { predecessor: string; successor: string };\ntype Issue = { id: string; estimate?: number; starts_at: Date; ends_at: Date };\n\nexport function computeCriticalPath(issues: Issue[], edges: Edge[]): string[] {\n  const weight = new Map(issues.map(i =&gt; [i.id, i.estimate ?? daysBetween(i.starts_at, i.ends_at)]));\n  const succ = new Map&lt;string, string[]&gt;();\n  for (const e of edges) {\n    if (!succ.has(e.predecessor)) succ.set(e.predecessor, []);\n    succ.get(e.predecessor)!.push(e.successor);\n  }\n  // Topological sort\n  const inDegree = new Map(issues.map(i =&gt; [i.id, 0]));\n  for (const e of edges) inDegree.set(e.successor, (inDegree.get(e.successor) ?? 0) + 1);\n  const queue: string[] = [];\n  for (const [id, d] of inDegree) if (d === 0) queue.push(id);\n  const sorted: string[] = [];\n  while (queue.length) {\n    const id = queue.shift()!;\n    sorted.push(id);\n    for (const s of succ.get(id) ?? []) {\n      inDegree.set(s, inDegree.get(s)! - 1);\n      if (inDegree.get(s) === 0) queue.push(s);\n    }\n  }\n  // Longest path\n  const dist = new Map&lt;string, number&gt;(issues.map(i =&gt; [i.id, weight.get(i.id) ?? 0]));\n  const prev = new Map&lt;string, string | null&gt;();\n  for (const id of sorted) {\n    for (const s of succ.get(id) ?? []) {\n      const newDist = dist.get(id)! + (weight.get(s) ?? 0);\n      if (newDist &gt; dist.get(s)!) {\n        dist.set(s, newDist);\n        prev.set(s, id);\n      }\n    }\n  }\n  // Trace path from the longest-dist node\n  let endNode = [...dist.entries()].sort((a, b) =&gt; b[1] - a[1])[0][0];\n  const path: string[] = [];\n  let cur: string | null = endNode;\n  while (cur) { path.unshift(cur); cur = prev.get(cur) ?? null; }\n  return path;\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Add dependency</strong> — POST → 201; row in issue_dependencies.</li><li><strong>Self-edge rejected</strong> — POST with predecessor === successor → 422 SelfEdge.</li><li><strong>Cycle rejected</strong> — A→B exists; POST B→A → 422 CycleDetected.</li><li><strong>Long cycle rejected</strong> — A→B→C→D; POST D→A → 422.</li><li><strong>Duplicate rejected</strong> — second POST same edge → 422 AlreadyExists.</li><li><strong>DELETE removes edge</strong> — 204; row gone.</li><li><strong>Edge rendered as arrow</strong> — fixture with edge → SVG path visible from predecessor bar to successor bar.</li><li><strong>Critical path highlighted</strong> — fixture: 3-issue chain → all 3 bars gold-bordered.</li><li><strong>Critical path branch chooses longest</strong> — fixture: A→B (3 days) and A→C (5 days) → A + C highlighted.</li><li><strong>Memoised</strong> — re-render without dependency change → no recompute.</li><li><strong>Recompute on estimate change</strong> — issue estimate change → new critical path; row <code>critical_path_recomputed</code>.</li><li><strong>Parent roll-up renders</strong> — parent issue with 3 children → parent bar = min/max of children.</li><li><strong>memory audit dependency_added</strong> — POST → row.</li><li><strong>memory audit dependency_removed</strong> — DELETE → row.</li><li><strong>Kbd D opens dialog</strong> — focused bar + D → dependency picker.</li><li><strong>RLS isolates</strong> — tenant A's edges invisible to tenant B.</li><li><strong>axe-core passes</strong> — SVG decorative; nav via dialog.</li><li><strong>Critical path metric</strong> — depth recorded per cycle.</li><li><strong>Slack rendered as ghost</strong> — non-critical bar with 3-day slack → ghost extension visible (AC for §1 #13).</li><li><strong>Show-critical-only filter</strong> — <code>?show_critical_only=true</code> → non-critical bars hidden (AC for §1 #14).</li><li><strong>CLI validate-graph reports anomalies</strong> — disconnected components flagged (AC for §1 #15).</li><li><strong>Near-cycle detection</strong> — graph with 4 edges where 1 more would form cycle → SEV-3 audit (AC for §1 #16).</li><li><strong>PDF export</strong> — GET /gantt.pdf → A3-landscape PDF (AC for §1 #18).</li><li><strong>Earliest-start / latest-finish on critical bars</strong> — visible annotations (AC for §1 #19).</li><li><strong>Batch dependency CRUD</strong> — POST /batch with 50 edges → 200; cycle on batch detects (AC for §1 #20).</li><li><strong>Parent completion %</strong> — parent of 5 issues with 3 done → bar shows 60% (AC for §1 #21).</li><li><strong>Shift+D removes last dep</strong> — focused bar + Shift+D → last-added dependency removed (AC for §1 #22).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cycle_rejected() {\n    let env = TestEnv::new().await;\n    let (a, b) = (env.create_issue().await, env.create_issue().await);\n    add_dependency(&amp;env.pool, a, b, env.alice()).await.unwrap();\n    let err = add_dependency(&amp;env.pool, b, a, env.alice()).await.unwrap_err();\n    assert!(matches!(err, DepError::CycleDetected(_, _)));\n}\n\n#[tokio::test]\nasync fn long_cycle_rejected() {\n    let env = TestEnv::new().await;\n    let (a, b, c, d) = (env.iss(), env.iss(), env.iss(), env.iss()).await;\n    add_dependency(&amp;env.pool, a, b, env.alice()).await.unwrap();\n    add_dependency(&amp;env.pool, b, c, env.alice()).await.unwrap();\n    add_dependency(&amp;env.pool, c, d, env.alice()).await.unwrap();\n    let err = add_dependency(&amp;env.pool, d, a, env.alice()).await.unwrap_err();\n    assert!(matches!(err, DepError::CycleDetected(_, _)));\n}\n\n#[tokio::test]\nasync fn self_edge_rejected() {\n    let env = TestEnv::new().await;\n    let a = env.create_issue().await;\n    let err = add_dependency(&amp;env.pool, a, a, env.alice()).await.unwrap_err();\n    assert!(matches!(err, DepError::SelfEdge));\n}</code></pre>\n<pre><code class=\"language-typescript\">test('critical path picks longest branch', () =&gt; {\n  const issues = [\n    { id: 'A', estimate: 1, starts_at: d(1), ends_at: d(2) },\n    { id: 'B', estimate: 3, starts_at: d(2), ends_at: d(5) },\n    { id: 'C', estimate: 5, starts_at: d(2), ends_at: d(7) },\n  ];\n  const edges = [\n    { predecessor: 'A', successor: 'B' },\n    { predecessor: 'A', successor: 'C' },\n  ];\n  const path = computeCriticalPath(issues, edges);\n  expect(path).toEqual(['A', 'C']);\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + algorithms above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-001</strong> — issues + parent_issue_id field.</li><li><strong>TASK-PROJ-002</strong> — WebSocket for live updates.</li><li><strong>TASK-PROJ-015</strong> — Timeline primitives reused.</li><li><strong>TASK-PROJ-018</strong> — gold-border design token.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.critical_path_recomputed\",\n  \"payload\": {\n    \"cycle_id\": \"cyc-...\",\n    \"path_issue_ids\": [\"iss-A\", \"iss-C\", \"iss-F\"],\n    \"path_total_estimate_days\": 9.0,\n    \"recomputed_at_ns\": 1747407137483000000\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Slack analysis (free-float per issue) — slice 4+.</li><li>Other dependency kinds (S→S, F→F, S→F) — slice 4+.</li><li>Resource-leveled critical path (Gantt with assignee capacity) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Cycle on insert</td><td>BFS catches</td><td>422</td><td>Caller removes other edge first</td></tr><tr><td>Self-edge</td><td>check</td><td>422</td><td>Caller fixes</td></tr><tr><td>Duplicate</td><td>PK constraint</td><td>422</td><td>None</td></tr><tr><td>Issue deleted with active deps</td><td>cascade cleanup</td><td>All edges of deleted issue removed</td><td>None</td></tr><tr><td>Cross-tenant edge</td><td>tenant_id mismatch in RLS</td><td>0 rows</td><td>None</td></tr><tr><td>100+ edges in graph</td><td>BFS O(V+E)</td><td>&lt; 10ms typical</td><td>None</td></tr><tr><td>Critical path with no edges</td><td>trivial: single longest-weight node</td><td>Single-bar highlight</td><td>None</td></tr><tr><td>Estimate missing</td><td>fallback to date-span</td><td>Path still computable</td><td>None</td></tr><tr><td>Disconnected DAG</td><td>multiple roots; pick max overall</td><td>Single path returned</td><td>By design</td></tr><tr><td>Parent date range empty (no children)</td><td>omit roll-up</td><td>None</td><td>None</td></tr><tr><td>Recompute thrashing on rapid edits</td><td>memoise + debounce 200ms</td><td>None</td><td>None</td></tr><tr><td>SVG arrow path glitch on resize</td><td>redraw on window resize</td><td>None</td><td>None</td></tr><tr><td>Touch device</td><td>dependency dialog button</td><td>None</td><td>None</td></tr><tr><td>Kbd D collides with input field</td><td>listener scoped to bar</td><td>None</td><td>None</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>Slack computation = 0 (issue is critical)</td><td>rendered without ghost</td><td>None</td><td>None</td></tr><tr><td>Critical-only filter empty result</td><td>helpful copy</td><td>None</td><td>None</td></tr><tr><td>Validate-graph CLI times out</td><td>bounded by graph size; warn</td><td>None</td><td>Operator narrows</td></tr><tr><td>Near-cycle false positive (long valid path)</td><td>configurable threshold</td><td>None</td><td>None</td></tr><tr><td>PDF export with &gt; 200 issues</td><td>paginated; multi-page A3</td><td>None</td><td>None</td></tr><tr><td>Earliest-start / latest-finish overlap (small slack)</td><td>annotations stack vertically</td><td>None</td><td>None</td></tr><tr><td>Batch CRUD with 100 cycles</td><td>per-batch BFS check</td><td>reject + per-edge status</td><td>Caller fixes</td></tr><tr><td>Parent completion % when child unestimated</td><td>counts done/total; %-of-count</td><td>None</td><td>None</td></tr><tr><td>Shift+D with no dependency to remove</td><td>no-op</td><td>None</td><td>None</td></tr><tr><td>PDF export rendering fails</td><td>fallback to HTML export</td><td>None</td><td>None</td></tr><tr><td>Batch dep import partial failure</td><td>per-edge status</td><td>partial commit</td><td>Caller retries failed</td></tr><tr><td>Graph &gt; 10K issues</td><td>BFS still bounded but slow</td><td>sev-3 warning</td><td>Slice 4+ optimise</td></tr><tr><td>Near-cycle detection sensitivity tuning</td><td>per-tenant config</td><td>None</td><td>None</td></tr><tr><td>PDF export queued</td><td>async job; email when ready</td><td>None</td><td>Operator</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>BFS cycle detect is O(V+E); for 10K-issue cycles this is &lt; 100ms — acceptable per write.</li><li>SVG arrow path uses Catmull-Rom approximation; corners squared at 5px radius.</li><li>Critical-path memoisation key: <code>${cycle_id}|${edge_graph_hash}|${estimate_hash}</code>.</li><li>Parent roll-up not in Timeline (TASK-PROJ-015) because flat-swimlane view; Gantt adds it for hierarchical planning.</li><li><code>D</code> keyboard shortcut: listener scoped to bar's onKeyDown to avoid global conflict.</li><li>Memoisation lives in zustand store; cleared on cycle change.</li><li>SVG arrows render after bars layout settles (one tick post-render via useLayoutEffect).</li><li>Slack computation: for each non-critical-path issue, slack = (critical_path_total_duration) - (path_duration_including_this_issue). Visible as ghost extension.</li><li>Critical-only filter is URL-shareable; clients pass to operators who get a focused view.</li><li>Validate-graph CLI emits JSON output by default; <code>--format=text</code> for human-readable.</li><li>Near-cycle detection runs as part of cycle check: if any edge in the graph could become a back-edge with one more insert, emit warning.</li><li>Forward-compatible kind column: CHECK constraint allows whitelist of values; future kinds bump the whitelist.</li><li>PDF export uses <code>puppeteer</code> (server-side headless Chrome) rendering the Gantt page at fixed viewport; A3 landscape.</li><li>Earliest-start/latest-finish are computed from critical-path analysis; visible only on critical bars.</li><li>Batch dependency CRUD runs cycle detection on the union of existing edges + new edges; partial success allowed.</li><li>Parent completion % uses <code>count(done) / count(total)</code> of immediate children; doesn't recurse to grandchildren (slice 4+).</li><li>Shift+D undo removes the most recently added dependency for the focused successor; tracks last-add timestamp.</li><li>We chose CSS-only ghost extension over JS animation for slack to maintain 60fps.</li><li>The dependency arrow rendering uses <code>pointer-events: none</code> so it doesn't interfere with bar interaction.</li><li>For very dense graphs (50+ edges), arrows render with translucent stroke to reduce visual chaos.</li><li>Critical-path algorithm is implemented client-side because the data is already loaded; server-side would add round-trip.</li><li>Validation CLI's anomaly thresholds (disconnected, near-cycle) are tunable per-tenant in slice 4+.</li><li>The PDF export job is async; large cycles email a download link when ready.</li><li>We considered using a proper DAG library (dagre) but rolled our own for control + bundle size.</li><li>Earliest-start annotation respects cycle start; doesn't suggest dates before cycle.starts_at.</li><li>The Shift+D undo window is 30 seconds; after that, manual remove is required.</li><li>Parent completion % updates live via WS subscription on child status changes.</li><li>We tested critical-path computation on synthetic 10k-issue graphs; sub-second performance.</li><li>Near-cycle warning has a 5-minute dedup per tenant to avoid alert spam during rapid graph editing.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-016.</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Client code lives under apps/web/src (there is no web/proj-client/).</p>";
