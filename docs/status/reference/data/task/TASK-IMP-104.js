window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-104"]="<h1>TASK-IMP-104: Refuse an older payload over a newer .cyberos</h1>\n<h2>Summary</h2>\n<p><code>install.sh</code> reads the payload's VERSION, prints it, and never compares it against the <code>.cyberos/VERSION</code> already in the repo - the vendor step is an unconditional overwrite. An older payload therefore downgrades a consumer silently: skills disappear, doctrine reverts, and the only trace is a line nobody re-reads. Compare before vendoring, refuse a downgrade naming both versions, and allow the operator through with an explicit override.</p>\n<h2>Problem</h2>\n<p><code>install.sh:21</code> sets <code>avail_ver</code> from the payload's VERSION and <code>:41</code> prints it. No line compares it with the installed <code>.cyberos/VERSION</code>. Meanwhile <code>version.sh</code> and <code>lib/update-check.sh</code> already contain a version comparator - install simply never asks them.</p>\n<p>Nothing catches this downstream: workflow-version pins live in the payload being installed, so a downgraded machine reports the older pins as correct. The failure is silent by construction. Today there is exactly one version in the world, which is why it has never bitten - and exactly why it is cheap to fix before 1.0.0 puts a second one out there.</p>\n<h2>Proposed Solution</h2>\n<p>Before the vendor step, read <code>.cyberos/VERSION</code> when present and compare it against <code>avail_ver</code> using the comparator that already exists - reused, not rewritten. Equal proceeds silently (re-vendor is the documented idempotent path). Newer proceeds. Older refuses with a non-zero exit naming installed version, payload version, and the override. <code>CYBEROS_ALLOW_DOWNGRADE=1</code> proceeds with a warning that records both versions, because an operator deliberately pinning an older machine is a real case and the workflow should not lie to them about it.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Warn but proceed. Rejected: the defect is silence, and a warning on a step that continues is silence with extra text. The whole point is that a downgrade must be a decision.</li><li>Refuse with no override. Rejected: rolling back a bad release is a legitimate operator action, and a gate with no key gets bypassed by <code>rm -rf .cyberos</code> - which loses the operator's config.</li><li>Compare a manifest hash rather than a version. Rejected: overkill for a monotonic version line, and it answers \"different\" rather than \"older\", which is not the question.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: installing an older payload over a newer <code>.cyberos/</code> exits non-zero with both versions named, and the installed machine is untouched - suite-asserted. Baseline: today it overwrites silently.</li><li>Guardrail: same-version re-install stays silent and idempotent (the documented path), and <code>CYBEROS_ALLOW_DOWNGRADE=1</code> completes with both versions recorded in the summary.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the pre-vendor comparison in <code>install.sh</code>, the override, the summary line, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any migration of consumer data on upgrade or downgrade - this refuses; it does not transform.</li><li>Rewriting the version comparator (reuse <code>version.sh</code>'s).</li><li>Auto-fetching the correct payload on refusal - install vendors what it was handed.</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-103: both guard the vendor step in <code>install.sh</code>, and the order is normative - the version check MUST run before the lock is acquired, so a refused downgrade never takes a lock it will not use. Shipping 104 first would force 103 to re-open the same lines. Per TASK-IMP-101's depends_on evidence gate, 103's coverage-gate artefact is the evidence.</p>\n<p><code>version.sh</code>'s comparator already exists on main.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md IMP-24, verified against install.sh on merged main; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Before the first destructive vendor operation AND before the TASK-IMP-103 lock is acquired, <code>install.sh</code> MUST compare <code>avail_ver</code> against <code>.cyberos/VERSION</code> when that file exists and is parseable. A refused downgrade MUST NOT acquire the lock.</li><li>1.2 The comparison MUST reuse the existing comparator (<code>version.sh</code> / <code>lib/update-check.sh</code>); install MUST NOT carry a second implementation.</li><li>1.3 Payload version older than installed MUST refuse with a non-zero exit naming the installed version, the payload version, and the <code>CYBEROS_ALLOW_DOWNGRADE=1</code> override. It MUST NOT vendor.</li><li>1.4 <code>CYBEROS_ALLOW_DOWNGRADE=1</code> MUST proceed and MUST record both versions in the install summary, so the downgrade is legible after the fact.</li><li>1.5 Equal versions MUST proceed with no new output (idempotent re-vendor is the documented path and MUST NOT become noisy).</li><li>1.6 An absent, empty, or unparseable <code>.cyberos/VERSION</code> MUST proceed, naming the condition - a missing version is a first install or a damaged machine, and neither is a downgrade.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - older payload over newer installed exits non-zero, names both versions and the override, and leaves <code>.cyberos/</code> byte-identical - test: <code>tools/install/tests/test_install_version_guard.sh::t01_downgrade_refused</code></li><li>[ ] AC 2 (traces_to: #1.4) - with the override set, the same install completes and the summary records both versions - test: <code>tools/install/tests/test_install_version_guard.sh::t02_override_records_both</code></li><li>[ ] AC 3 (traces_to: #1.5) - same-version re-install completes with no added output vs today - test: <code>tools/install/tests/test_install_version_guard.sh::t03_equal_is_silent</code></li><li>[ ] AC 4 (traces_to: #1.6) - absent and unparseable VERSION both proceed with the condition named - test: <code>tools/install/tests/test_install_version_guard.sh::t04_missing_version_proceeds</code></li><li>[ ] AC 5 (traces_to: #1.2) - no second comparator: install delegates to the existing one - verify: recorded grep in the gate log showing install.sh calls the shared comparator and defines no version-compare function of its own (structural claim; same rationale as TASK-IMP-090 AC 1).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Pre-release / suffixed versions (<code>1.0.0-rc1</code> vs <code>1.0.0</code>): whatever the existing comparator already decides, and the suite pins that behavior rather than inventing a second opinion.</li><li><code>.cyberos/VERSION</code> newer than any released payload (a developer's local build): refuses, and the override is the documented path. Correct - install cannot know it is a dev build.</li><li>Damaged <code>.cyberos/</code> where VERSION is absent but the machine is present: proceeds per 1.6 and re-vendors, which is the repair path.</li><li>A payload with no VERSION file at all: <code>avail_ver</code> is already <code>unknown</code> on main; unknown MUST NOT be treated as older (it is not comparable), so it proceeds with the condition named.</li><li>Security-class: version strings are read from files inside the repo and the payload, are compared, and are never executed or interpolated into a command. No execution surface.</li></ul>";
