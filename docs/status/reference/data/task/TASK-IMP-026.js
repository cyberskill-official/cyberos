window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-026"]="<h1>TASK-IMP-026: Auto-revert on gate regression</h1>\n<h2>Summary</h2>\n<p>When main (or a protected branch) regresses a gate — especially a goldenset case — provide a <strong>documented, opt-in</strong> script that proposes a <code>git revert</code> pull request via <code>gh</code>. Never force-push. Never merge without an operator.</p>\n<h2>Problem</h2>\n<p>Gate regressions on main currently require a human to notice CI, identify the bad SHA, and hand-roll a revert. Without an opt-in helper, recovery is slow; with a silent auto-force-push, recovery would be unsafe.</p>\n<h2>Proposed Solution</h2>\n<p>Ship <code>tools/install/gate-auto-revert.sh</code> that, given a bad SHA:</p>\n<ol><li>Refuses to run unless <code>--dry-run</code> <strong>or</strong> <code>CYBEROS_AUTO_REVERT=1</code>.</li><li>In dry-run: prints the revert commit subject, branch name, and the <code>gh pr create</code> plan (including optional goldenset failure note / last-gate-failure class) without mutating git remotes.</li><li>In live mode: creates a local revert commit on a new branch and opens a PR with <code>gh pr create</code> (no <code>--merge</code>, no force-push flags ever present in the script).</li><li>Document at <code>docs/verification/gate-auto-revert.md</code>.</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Silent <code>git push --force</code> to main.</strong> Rejected: forbidden by CyberOS doctrine and this mission.</li><li><strong>Auto-merge revert PRs.</strong> Rejected: still requires operator; script must not merge.</li><li><strong>Only a CI workflow with no local script.</strong> Rejected: dry-run testability is poorer; script+docs is the unit.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: dry-run tests green; script contains no force-push/merge invocations.</li><li>Guardrail: without opt-in env and without <code>--dry-run</code>, exit non-zero and write nothing.</li></ul>\n<h2>Scope</h2>\n<p>In scope: gate-auto-revert.sh, docs, tests, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Enabling CYBEROS_AUTO_REVERT in default CI.</li><li>Force-push or auto-merge.</li><li>Replacing human HITL on the revert PR.</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-IMP-008]</code> — revert PR body SHOULD cite goldenset failure context when provided; goldenset path is standardized by 008.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent, batch/12c.</li><li><strong>Human review:</strong> session operator Stephen Cheng.</li></ul>\n<h2>§1 - Description (normative)</h2>\n<ol><li><code>tools/install/gate-auto-revert.sh [--dry-run] [--goldenset-case &lt;id&gt;] [--failure-json &lt;path&gt;] &lt;bad-sha&gt; [base-branch]</code> MUST exist.</li><li>Without <code>--dry-run</code>, the script MUST refuse unless <code>CYBEROS_AUTO_REVERT</code> is exactly <code>1</code> (literal), exiting 2 with an actionable message.</li><li>The script MUST NEVER invoke <code>git push --force</code>, <code>git push -f</code>, <code>gh pr merge</code>, or <code>git merge</code> onto the base branch.</li><li>Dry-run MUST print a plan including: bad SHA, proposed branch <code>revert/&lt;shortsha&gt;</code>, <code>git revert --no-edit &lt;sha&gt;</code> intent, and <code>gh pr create</code> title/body outline; exit 0; no branch checkout required beyond read-only <code>git rev-parse</code> / <code>git show</code>.</li><li>Live mode (opt-in) MUST: verify SHA exists; create branch from base (default <code>main</code>); <code>git revert --no-edit &lt;bad-sha&gt;</code>; <code>git push -u</code> (fast-forward only); <code>gh pr create</code> with body naming goldenset case and/or failure JSON class when provided.</li><li>Docs MUST state the opt-in contract and the never-force-push / never-merge invariants.</li><li>Suite <code>test_gate_auto_revert.sh</code> MUST cover dry-run plan, refuse-without-opt-in, and absence of force-push/merge strings in the script.</li></ol>\n<h2>Acceptance criteria</h2>\n<ul><li>[x] AC1: <code>--dry-run &lt;sha&gt;</code> exits 0 and prints revert plan without needing CYBEROS_AUTO_REVERT. (t01)</li><li>[x] AC2: live mode without env exits 2. (t02)</li><li>[x] AC3: script source has no force-push or pr-merge invocations. (t03)</li><li>[x] AC4: dry-run body includes <code>--goldenset-case</code> when passed. (t04)</li><li>[x] AC5: docs page states opt-in + never force-push/merge. (t05)</li></ul>\n<h2>Test plan</h2>\n<p><code>bash tools/install/tests/test_gate_auto_revert.sh</code></p>";
