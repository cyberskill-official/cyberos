window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-113"]="<h1>TASK-IMP-113: Skill trust measurement - pass rates as a report, not a gate</h1>\n<h2>Summary</h2>\n<p>All 53 skills have identical standing forever. <code>task-lint</code> (deterministic, 8-scenario suite) and <code>code-review-author</code> (a model's opinion) are trusted equally, and no pass-rate data exists for any of them because nothing records per-skill outcomes. Log pass/fail per invocation and render the table. Tiers are a REPORT for the operator, never a licence for the machine.</p>\n<h2>Problem</h2>\n<p>\"Which of our 53 skills actually works?\" is answered today by vibes. Nothing distinguishes a skill that is provably right from one that is plausibly right, and nothing notices when a skill degrades. The article's line is exact: \"turn up autonomy as trust grows\" is not a mechanism; a table with rules is.</p>\n<p>The honest limit is also exact. Their ledger governs unattended shipping, which our two gates forbid by design. So the tier cannot be a gate here - what transfers is the measurement, which IMP-20 needs (to know which skill is degrading) and IMP-23 needs (to price the loop).</p>\n<h2>Proposed Solution</h2>\n<p><code>skill-log.mjs</code> appends <code>&lt;skill&gt; &lt;pass|fail&gt; &lt;task-id&gt; &lt;iso8601&gt;</code> at each audit verdict; <code>--render</code> prints skill, runs, passes, rate, and a tier label. Tier thresholds mirror the article (auto &gt;= 20 runs and &gt;= 95 %; watch &lt; 10 runs or &lt; 90 %) because they are as good as any and someone tested them - but the label is INFORMATION. No workflow reads it, no gate consults it, and a skill at 60 % is a finding for the operator, not a signal to the machine.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Tiers as a gate (the article's design). Rejected: it exists to allow unattended shipping, which deletes our premise. Adopting the gate would be adopting their product.</li><li>Infer pass rates from git history. Rejected: an audit verdict is not visible in a diff, and reconstructing it is a guess.</li><li>Log every skill invocation including reads. Rejected: the signal is the verdict, and logging everything buries it.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: after a batch, <code>--render</code> shows real runs and rates per skill - suite-asserted against a fixture ledger. Baseline: zero data on any skill.</li><li>Guardrail: no workflow reads a tier; the ledger is append-only and never gates anything.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>skill-log.mjs</code>, the append at audit verdicts, <code>--render</code>, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any gate, threshold, or routing decision keyed on a tier.</li><li>Unattended shipping at any tier - the two HITL gates are unconditional.</li><li>Model or prompt quality evaluation.</li></ul>\n<h2>Dependencies</h2>\n<p>None logically. Pairs with TASK-IMP-110 (consumer) and TASK-IMP-114 (sibling metric).</p>\n<p><strong>Serialisation note:</strong> touches <code>install.sh</code> (shared with TASK-IMP-103, 104) and <code>ship-tasks.md</code> (shared with 108, 109, 114, 115). Per §11a the parent serialises shared-tree writes - these MUST NOT run as concurrent swarm members.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §11 IMP-30, adapting BUILD 4's mechanism to a doctrine that forbids its purpose.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>skill-log.mjs</code> MUST append one row per audit verdict: skill name, <code>pass|fail</code>, task id, ISO-8601 timestamp.</li><li>1.2 The ledger MUST be append-only; the helper MUST NOT rewrite or delete rows.</li><li>1.3 <code>--render</code> MUST print skill, runs, passes, rate, and tier label.</li><li>1.4 Tier labels MUST be informational. No workflow, gate, or queue may read a tier to decide anything, and the helper's own docs MUST say so.</li><li>1.5 A skill with zero runs MUST render as <code>no data</code> rather than 0 % - an unmeasured skill is not a failing one.</li><li>1.6 The ledger MUST live at <code>docs/tasks/.workflow/skill-trust.tsv</code> and MUST be gitignored. TASK-IMP-090's seed covers <code>*.ship.json</code> and <code>*.manifest.json</code> only, so this task MUST extend the seed with <code>skill-trust.tsv</code> using 090's append-once discipline - the ledger is NOT covered today and asserting otherwise would be a false claim.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - appended verdicts render with correct runs, passes, and rate - test: <code>tools/install/tests/test_skill_log.sh::t01_append_and_render</code></li><li>[ ] AC 2 (traces_to: #1.2) - the helper never rewrites an existing row - test: <code>tools/install/tests/test_skill_log.sh::t02_append_only</code></li><li>[ ] AC 3 (traces_to: #1.5) - a zero-run skill renders <code>no data</code> - test: <code>tools/install/tests/test_skill_log.sh::t03_zero_runs_no_data</code></li><li>[ ] AC 4 (traces_to: #1.6) - the seed gains the ledger pattern append-once (no duplicate on re-install) and <code>git check-ignore docs/tasks/.workflow/skill-trust.tsv</code> exits 0 - test: <code>tools/install/tests/test_skill_log.sh::t04_ledger_gitignored</code></li><li>[ ] AC 5 (traces_to: #1.4) - no workflow or gate reads a tier - verify: recorded grep in the gate log showing zero reads of the tier label outside the renderer (a negative structural claim; same rationale as TASK-IMP-090 AC 1).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A skill renamed between runs: the ledger shows two skills. Correct - the old name's history belongs to the old name, and merging them would fabricate continuity.</li><li>A run cut mid-flight (the API spend limit this run hit twice): no verdict, so no row. The ledger records verdicts, not attempts - and a skill must not be marked failed because the harness died.</li><li>Concurrent appends from a swarm batch: append-only rows of one line each; the OS handles it for small writes, and the helper MUST NOT read-modify-write the file.</li><li>A ledger with 10k rows: render aggregates in one pass, never loads a structure per row.</li><li>Security-class: writes a TSV of names and verdicts; reads it back to count. Skill names come from the workflow, not from user input, and nothing is executed.</li></ul>";
