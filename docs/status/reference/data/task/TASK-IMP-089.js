window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-089"]="<h1>TASK-IMP-089: task@1 template drops the duplicate out-of-scope section 4</h1>\n<h2>Summary</h2>\n<p>Every conforming spec has carried two out-of-scope homes - the PRD half's <code>## Scope &gt; ### Out of scope / Non-Goals</code> (the rubric's home, SEC-006/QA-006) and the template's <code>## 4. Out of scope / non-goals</code> - reconciled by a pointer line in all nine specs shipped this run. Per the recorded IMP-07 decision, the template drops section 4 and renumbers Protected invariants from 5 to 4; the rubric needs no change because it never required section 4.</p>\n<h2>Problem</h2>\n<p>Mandated duplication is authoring tax and drift risk: two homes for one statement invite divergence, and every audit this run logged the reconciliation as a wontfix finding.</p>\n<h2>Proposed Solution</h2>\n<p>Remove the <code>## 4. Out of scope / non-goals</code> block from TASK-TEMPLATE.md, renumber <code>## 5. Protected invariants...</code> to <code>## 4.</code>, and update the template schema test to assert the new shape (single out-of-scope home, invariants at 4). Existing specs are untouched - the rubric accepts both shapes since section 4 was never a rule.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Make section 4 optional with guidance. Rejected by the decision: an optional duplicate still invites divergence.</li><li>Keep both and document the idiom. Rejected: nine specs of pointer lines is the evidence against it.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a spec authored from the new template carries exactly one out-of-scope home and lints clean (schema test asserts the shape on every run). Baseline: two homes plus a pointer line in 9 of 9 specs this run. Deadline: final acceptance.</li><li>Guardrail: the payload-vendored template matches source byte-for-byte after rebuild (existing template gates keep covering it).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the template block removal, renumbering, schema-test update.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Rewriting existing specs (both shapes stay valid to the rubric).</li><li>The per-type body templates (feature/bug/chore carry the PRD half only - no section 4 exists there).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None; cone is the template file plus its schema test (disjoint from all batch members).</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the recorded IMP-07 decision; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-3 PLAN approved 2026-07-16; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 TASK-TEMPLATE.md MUST NOT contain a <code>## 4. Out of scope / non-goals</code> section; <code>## Scope &gt; ### Out of scope / Non-Goals</code> remains the single home.</li><li>1.2 The Protected-invariants section MUST be renumbered to <code>## 4.</code> with its content unchanged.</li><li>1.3 test_template_schema.sh MUST assert the new shape: no duplicate out-of-scope heading in TASK-TEMPLATE.md, invariants present at section 4.</li><li>1.4 The rebuilt payload MUST carry the updated template (covered by the existing vendor gates; asserted once in the schema test against a scratch build).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2) - template shape: single out-of-scope home, invariants at 4 - test: <code>scripts/tests/test_template_schema.sh::t08_single_out_of_scope_home</code></li><li>[ ] AC 2 (traces_to: #1.3) - schema test fails on a reintroduced duplicate (fixture) - test: <code>scripts/tests/test_template_schema.sh::t08_duplicate_reintroduction_fails</code></li><li>[ ] AC 3 (traces_to: #1.4) - scratch payload carries the new shape - test: <code>scripts/tests/test_template_schema.sh::t08_payload_carries_shape</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Existing specs with the old shape: untouched and still rubric-valid; the schema test targets the TEMPLATE, never the corpus (AC 1).</li><li>Downstream prose citing \"section 5 invariants\": grep at implement time and update only template-adjacent references, not historical specs.</li><li>Security-class: none - prompt-text template edit gated by tests.</li></ul>";
