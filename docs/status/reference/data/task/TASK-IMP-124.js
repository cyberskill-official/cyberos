window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-124"]="<h1>TASK-IMP-124: authors do not check what they originate</h1>\n<h2>Summary</h2>\n<p>Seven audits across two tasks and three authors found one recurring defect: authors verify what they INHERIT and ship falsehoods they ORIGINATE. Round 4 of TASK-IMP-122 re-measured every inherited claim correctly and then asserted <code>1525</code> as the cone's file count in the guardrail AC, where the true count is 1534 and 1525 is the count of the cone the spec explicitly rejects. TASK-IMP-121's rewrite corrected all six inherited mis-citations and then shipped an impossibility proof that is false in four lines of awk. Add TRACE-007, a judgment-family rule requiring every ORIGINATED claim to carry a re-runnable derivation, and restructure the AI Authorship Disclosure so it is a claim the audit tests rather than an attestation it credits.</p>\n<h2>Problem</h2>\n<p>An inherited claim carries a provenance. It came from somewhere, that somewhere might be wrong, so the author looks. An originated claim arrives already believed: the author IS the provenance, so nothing prompts a check, and nothing in the workflow flags it as needing one. The gates are all downstream of that. The result, measured across seven independent audits:</p>\n<p><strong>1. A false number, asserted normatively, under a sentence certifying it was not.</strong> TASK-IMP-122 rewrite 4 (<code>63705483</code>) closed both CRITICALs, broke a four-round flat line at 8/10, and had every inherited citation re-measured and correct. Its AC 10 (<code>:280</code>) asserts <code>102dc507</code> \"over 1525 files per side\". Derived at HEAD <code>15894b1e</code> over <code>dist/cyberos</code>:</p>\n<table><thead><tr><th>cone</th><th>command (files, then <code>build.sh:353</code>'s <code>_rsha()</code> pipeline)</th><th>count</th><th>digest</th></tr></thead><tbody><tr><td>§1.4 REJECTS</td><td><code>find cuo plugin mcp lib docs-tools -type f</code></td><td><strong>1525</strong></td><td><code>86cafee8</code></td></tr><tr><td>§1.4 MANDATES</td><td>the same, plus <code>memory</code> minus <code>memory/store/</code>, plus the six root scripts</td><td><strong>1534</strong></td><td><code>102dc507</code></td></tr></tbody></table>\n<p>1525 + 3 + 6 = 1534. The spec pairs the mandated cone's digest with the rejected cone's count, in the guardrail AC, and a test written faithfully to that AC asserts 1525 and <strong>FAILS on a correct implementation</strong>. Thirty-eight lines above it, the AI Authorship Disclosure (<code>:242</code>) reads: \"Every numeric and line-number claim was re-measured against source at HEAD by this author during this rewrite.\" The claim was true of everything round 4 inherited and false of the one number it invented. <strong>The one clause meant to prevent the failure certified it instead</strong> - and it could, because it is self-certifying: the author attests to their own diligence and nothing tests the attestation.</p>\n<p><strong>2. A false impossibility proof, load-bearing, disproved by a counter-example nobody was asked for.</strong> TASK-IMP-121's rewrite (<code>7309cd80</code>) dissolved four CRITICALs, fixed all six inherited mis-citations, and reproduced every inherited number exactly. Then, at <code>15894b1e:.../TASK-IMP-121/spec.md:168</code>: an operator hook with no trailing newline \"cannot be restored byte-exact ... The information is destroyed at append time, so no uninstall-side rule can invert it.\" Measured by this author, replaying <code>install.sh:851-853</code> verbatim against a 6-byte <code>exit 0</code>:</p>\n<pre><code>line-oriented strip (the spec's rule):   6B -&gt; 7B   DIFFERS\nbyte-oriented rule (4 lines of awk):     6B -&gt; 6B   BYTE-EXACT\nsame rule, newline-terminated control:   7B -&gt; 7B   BYTE-EXACT\nsame rule, three-newline control:        9B -&gt; 9B   BYTE-EXACT</code></pre>\n<p>The information is not destroyed. <code>install.sh:851-853</code> always prepends exactly one <code>\\n</code> before the marker, so the byte preceding the marker is always ours, and the operator's terminator survives as the presence or absence of a blank line above it. The error is a <strong>level confusion</strong>: the author reasoned in LINES, found no blank line to consume in the no-newline case, and generalised \"my line-oriented rule cannot\" to \"no rule can\". Their own evidence - \"16 -&gt; 17 bytes with the candidate strip\" (<code>15894b1e</code>, <code>:49</code> and <code>:168</code>) - supports the narrow claim and was published as the universal one. That false proof narrowed §1.6, which excuses a live §1.8 violation and scopes AC 6/7 so the suite can never catch it.</p>\n<p><strong>And it is why §1.2 requires a citation to name a revision.</strong> While this task was being drafted, TASK-IMP-121's spec was rewritten in the working tree. A bare <code>spec.md:168</code> now returns an unrelated AC; the impossibility has moved to <code>:178</code>, where that rewrite RETRACTS it - independently reaching the same four measurements printed above. The anti-example is unaffected, because it is pinned to the commit where the claim shipped. A citation into a live document is a claim with a half-life, and this one expired inside a single session.</p>\n<p><strong>3. A dead path in the author's own frontmatter, that no gate reads.</strong> Found by this author while sizing this task. <code>TASK-IMP-118/spec.md:22</code> FORMERLY declared, in <code>modified_files</code>, a path that never existed: <code>git show d4b1419e:docs/tasks/improvement/TASK-IMP-118-tests-must-test-their-clause/spec.md | sed -n '22p'</code> returns 'tools/install/docs-tools/templates/task-audit/RUBRIC.md', while <code>ls tools/install/docs-tools/templates/</code> returns \"No such file or directory\" and the five real copies live under <code>modules/skill/</code> and the build outputs. TRACE-003 checks test paths and nothing else, so an originated citation about the author's own cone was caught by no gate - the operator removed it by hand at <code>f653a0ff</code>, which pins this instance to <code>d4b1419e</code>. 118 is the task that generalised this defect class; it carried an instance of the class it names.</p>\n<p><strong>The earlier instance, and the shape of the cure.</strong> <code>docs/tasks/_audits/2026-07-18-phase-corpus-measurement.md</code> exists because three specs shipped four false counts from greps of one subdirectory reported as corpus-wide facts. That file did one thing differently: <code>:8</code> carries \"Reproduce: <code>node docs/tasks/_audits/measure-phase.mjs \"$(pwd)\"</code>\". Re-running it at HEAD prints <strong>550</strong> where the file's table (<code>:14</code>) says <strong>551</strong> - a real drift, found in one command, explained in one more (<code>TASK-IMP-123</code>'s spec was deleted at <code>f8899d64</code>). The number that carried its derivation was checkable by a stranger in seconds. <code>1525</code> carried none and took an independent audit.</p>\n<p><strong>Why every gate held.</strong> task-lint is clean on all of it - four rounds running (<code>TASK-IMP-122/audit.md:12</code>). FM/SEC/COND/TRACE are structural and have no view into whether a number is true. COND-004 (<code>RUBRIC.md:62</code>) requires the disclosure to EXIST with three labelled bullets; it constrains nothing the Scope bullet may assert, so \"every claim was re-measured\" satisfies it exactly as well as the truth would. The model audit reads a document and can only re-derive what it can see a derivation for. And the human gates were shown a green suite and a truthful-looking summary. No check failed. No check was pointed at this.</p>\n<h2>Proposed Solution</h2>\n<p>Two halves of one mechanism, both in <code>audit_rubric@2.0</code>.</p>\n<p><strong>(a) TRACE-007 - every originated claim carries its derivation.</strong> Judgment family, beside TRACE-006. For every claim the spec ORIGINATES in a governed class, the audit states the claim, states its derivation, re-runs it, and FAILS when the derivation is absent, does not re-run, does not reproduce the claimed value, or <strong>supports a narrower scope than the claim asserts</strong>. Three classes, three derivation forms: a NUMBER needs a command, a script invocation, or an arithmetic composition of other derived values; a CITATION needs <code>file:line</code> plus the text found there; a UNIVERSAL NEGATIVE needs a record of the witness search, and the audit must attempt one counter-example before accepting it. The point is not to make the author trustworthy. It is to stop the reader having to trust: a claim that carries the command that produced it can be re-run by a stranger, and the phase-corpus file proves a stranger will.</p>\n<p><strong>(b) The disclosure becomes a claim, not a certificate.</strong> COND-004 gains a shape: the <code>Scope:</code> bullet MUST partition its verification claims into <strong>re-derived and CONFIRMED</strong>, <strong>re-derived and CORRECTED</strong>, and <strong>measured and ADDED</strong>, MUST state whether the audit's own figures were treated as claims to verify, and MUST NOT make an unscoped universal attestation. \"Every claim was re-measured\" is unfalsifiable, which is why it survived being false. The partition is falsifiable: a number in the CONFIRMED set that no derivation reproduces is a finding, and a governed originated claim in none of the three sets is a finding. The audit tests the partition against the document and never credits it.</p>\n<p>This is not invented here. TASK-IMP-122 rewrite 5 (<code>spec.md:279-304</code>) wrote exactly this disclosure voluntarily, one round after the failure, and it is the working prototype: three labelled partitions, the corrections named (<code>1525 -&gt; 1534</code>), and an explicit statement that the round-4 audit's own figures \"were treated as claims to verify, not facts to copy\".</p>\n<p><strong>Kinship, stated in the rubric.</strong> TRACE-006 fails a test that asserts less than its clause's VERB. TRACE-007 fails a derivation that supports less than its claim's SCOPE. They are one defect at two levels - the author is the wrong reader of their own work - and 121's §3 is the crossover case: a measurement of the author's own candidate rule, standing in for a claim about all rules, is a weaker assertion wearing a stronger clause's words.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>(c) The author of a rewrite must not be the author of the draft.</strong> REJECTED, and rejected by the evidence offered for it. The score history is real - TASK-IMP-122 went 4/6/6/6 under one author and 8 on the first round with a fresh one (<code>audit.md:6</code>; <code>7309cd80</code>) - and author rotation IS already the working method for a different defect (patching only what the audit names). It does not touch this one. Both fresh-author rounds shipped an originated falsehood: round 4 the false 1525, the 121 rewrite the false impossibility. Rotation changes who INHERITS; it does not make anyone check what they originate. Worse, it is the mechanism by which this failure became visible: an author who re-measures everything inherited is left with exactly one unchecked category, and it is theirs. Rotation without TRACE-007 converts inherited-claim failures into originated-claim failures and calls it progress. Kept as practice, refused as this task's answer.</li><li><strong>Make the audit re-derive every number.</strong> Rejected: the audit cannot re-derive what it cannot see a derivation for. An auditor facing <code>1525</code> with no command must either invent the measurement (which is what caught it - once, expensively, by a subagent who happened to build all four cones) or take it. TRACE-007 asks the author to pay a cost they have already paid: the command is in their scrollback at origination time and gone by review time. §1.9 puts the instruction at that moment for exactly this reason.</li><li><strong>Put derivation-checking in task-lint.</strong> Rejected, and the reasons are two. Executing a derivation out of a spec body runs arbitrary shell from a document the rubric itself treats as capable of carrying injected content (SAFE-001..004 exist because spec bodies quote untrusted material). And a lint that appeared to verify derivations would restore the false assurance this task removes - the TASK-IMP-118 §1.5 argument, unchanged.</li><li><strong>Follow TASK-IMP-118 §1.5 exactly and add NOTHING mechanical.</strong> Rejected on a measured difference, not a preference. 118's argument is that a structural check shaped like TRACE-006 would PASS 108 §1.7's original assertion, because the string genuinely is in the file. Apply 118's own test here: a check that the disclosure's Scope bullet carries the three partition labels FAILS rewrite 4's disclosure (it carries none) and PASSES rewrite 5's (it carries all three). It discriminates the motivating case. So the shape enters the machine floor and nothing else does - §1.8 bounds it explicitly, and its finding message must name itself as shape-only so a green lint is never read as a verified disclosure.</li><li><strong>Require a derivation for EVERY claim, inherited included.</strong> Rejected: it prices the check at the volume that guarantees it is skipped, and it targets the category authors already check. Inherited claims are covered by TRACE-007 through the disclosure's own partition (an inherited number the author reproduced sits in CONFIRMED and must survive re-derivation), which is where the evidence says the cost belongs.</li><li><strong>Extend TRACE-003 to <code>modified_files</code> and every other asserted path.</strong> Out of scope, named in §Scope. It is real (Problem 3), it is structural rather than judgment, and it is a different rule with a different family. Filing it as its own task is TASK-IMP-117's precedent, not this task's job.</li><li><strong>Effort 13 hours.</strong> Breakdown in Scope; it sums to the field.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: re-auditing TASK-IMP-122 rewrite 4's AC 10 (<code>63705483:280</code>) against the amended rubric FAILS on TRACE-007, naming the absent derivation and the 1525/1534 substitution. Baseline: it passed five rubric families and scored 8/10, its highest.</li><li>Primary: re-auditing TASK-IMP-121 <code>15894b1e:spec.md:168</code> against the amended rubric FAILS on TRACE-007's universal-negative class, and the audit records the attempted counter-example. Baseline: it was caught only because an auditor independently wrote the awk; nothing asked them to.</li><li>Primary: re-auditing TASK-IMP-122 rewrite 5's disclosure (<code>15894b1e:spec.md:279-304</code>) against amended COND-004 PASSES on all three partitions. A rule that fails its own worked prototype is mis-drafted, and this is the arm that would say so.</li><li>Guardrail: <code>task-lint</code> executes nothing from a spec body and adds exactly one shape check. Baseline: it executes nothing today; the count of checks that re-run spec-supplied commands must stay 0.</li><li>Guardrail: TRACE-007 is reported and never auto-failed against tasks audited before it existed. Baseline: 183 <code>done</code> specs (derived: <code>node docs/tasks/_audits/measure-phase.mjs \"$(pwd)\"</code>, <code>done_total = 183</code>), none of which could have carried a derivation.</li></ul>\n<h2>Scope</h2>\n<p>In scope: TRACE-007's rule text and worked anti-examples in <code>modules/skill/task-audit/RUBRIC.md</code> §9; the COND-004 amendment in §4; <code>task-audit/SKILL.md</code>'s per-claim instruction and its disclosure test; <code>task-author/SKILL.md</code>'s record-at-origination instruction; one shape check in <code>task-lint.mjs</code> with its arm in <code>test_task_lint.sh</code>; a new <code>test_authorship_derivation.sh</code>; and the recorded re-audit of the three motivating documents as acceptance evidence.</p>\n<p>Breakdown (sums to <code>effort_hours: 13</code>): TRACE-007 rule text incl. the classes and the scope-of-claim rule 2.0h; two worked anti-examples quoted with true values and commands 1.5h; COND-004 amendment text 1.0h; <code>task-lint.mjs</code> shape check + <code>test_task_lint.sh</code> arm 1.5h; <code>task-audit/SKILL.md</code> 1.0h; <code>task-author/SKILL.md</code> 0.5h; <code>test_authorship_derivation.sh</code> 3.0h; the recorded three-document re-audit 2.0h; vendored-copy parity and payload check 0.5h.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Extending TRACE-003 (or any rule) to <code>modified_files</code>, <code>new_files</code> and <code>service</code>. Problem 3 is real - <code>TASK-IMP-118/spec.md:22</code> carried a live instance until <code>f653a0ff</code> removed it by hand, and the gate gap it exposed did not go with it - but it is a structural check in a different family. File separately.</li><li>Re-auditing the corpus against TRACE-007. 183 <code>done</code> specs predate the rule; §1.10's three documents are the acceptance evidence, and the sweep is the operator's scope decision. TASK-IMP-118 handled its own corpus question the same way, and TASK-IMP-117's before it.</li><li>Any change to TRACE-001..005, to TRACE-006, or to the coverage gate. TRACE-007 fires at spec correctness, beside TRACE-006, before code.</li><li>Fixing the documents this task cites. Both are already being repaired by their own tasks - TASK-IMP-122's 1525 by rewrite 5 at HEAD, TASK-IMP-121's impossibility by a rewrite in flight in the working tree, which retracts it. That is the point and not a reason to drop the anti-examples: each was found by one auditor doing arithmetic nobody asked for, and the next one will not be. This task is why they were findable, not their repair.</li><li>A lint that verifies any derivation, re-runs any command, or asserts any claim is true. §1.8 forbids it.</li><li>Rotating rewrite authorship. Kept as working practice; not encoded here (Alternatives).</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-118: it introduces TRACE-006 into <code>modules/skill/task-audit/RUBRIC.md</code> and edits <code>task-audit/SKILL.md</code>; this task adds TRACE-007 beside it, in the same section, into the same two files, and §1.5 requires TRACE-007's text to cite TRACE-006's. <code>grep -c 'TRACE-006' modules/skill/task-audit/RUBRIC.md</code> returns <strong>0</strong> at HEAD <code>15894b1e</code> - 118 is <code>ready_to_implement</code> and unimplemented - so this task cannot state its kinship against a rule that is not there, and the two cones collide on both files regardless. 118 lands first. Note for the batch: 118's declared cone once included a <code>modified_files</code> path that did not resolve (<code>spec.md:22</code>); the operator dropped it at <code>f653a0ff</code>, so 118's cone is now purely <code>modules/skill</code> and that one-real-file-short caveat no longer applies.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> drafted 2026-07-18 by an author who wrote none of TASK-IMP-121 or TASK-IMP-122. This document is about authors not checking what they originate, and it originates every claim in it, so the discipline it proposes is applied to itself and scoped the way §1.6 would require. <strong>Re-derived and CONFIRMED:</strong> <code>1525</code>/<code>86cafee8</code> and <code>1534</code>/<code>102dc507</code> with their component split 1525+3+6 (run against <code>dist/cyberos</code> at HEAD <code>15894b1e</code>, clean worktree, using <code>build.sh:353</code>'s own <code>_rsha()</code> pipeline); the audit ledger of seven audits, cross-checked against two <code>audit.md</code> files and two commit messages by different arithmetic. Both were supplied to this author by a prior audit and were re-run rather than copied. <strong>Measured and ADDED:</strong> the disproof of TASK-IMP-121's impossibility claim, executed rather than reasoned (6B-&gt;7B line-oriented vs 6B-&gt;6B byte-oriented, with two byte-exact controls, GNU Awk 5.1.0) - a rewrite of 121 landed the same four measurements in the working tree independently while this was being drafted, which cross-checks it; the phase-corpus file's 551-vs-550 drift and its cause; <code>TASK-IMP-118/spec.md:22</code>'s unresolvable <code>modified_files</code> path. <strong>Re-derived and CORRECTED:</strong> the task id - 123 is retired, not free (<code>source_decisions</code>); and every citation into <code>TASK-IMP-121/spec.md</code>, which was rewritten under this draft mid-session - <code>:168</code> was a bare path when written and returned an unrelated AC when re-run twenty minutes later, so all such citations are now pinned to <code>15894b1e</code> and §1.2 gained the revision requirement that this near-miss earned. Every <code>file:line</code> above was opened and read at HEAD <code>15894b1e</code>. <strong>Not derived, and marked rather than asserted:</strong> whether the byte-oriented rule is portable beyond GNU Awk - untested, and immaterial to the disproof, which needs one witness and not a portable one; and the count of DISTINCT authors, which <code>15894b1e</code> records as three and which this author cannot verify from git (every commit carries one committer). §3 carries both.</li><li><strong>Human review:</strong> the operator approved filing this as a task rather than applying it to the rubric, 2026-07-18 (<code>source_decisions</code>). The design choice among (a)/(b)/(c), the rejection of (c), and the decision to admit one shape check into task-lint against TASK-IMP-118 §1.5's blanket rule are this author's, each with rationale in Alternatives. No HITL gate has been run: this task is <code>draft</code>.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>modules/skill/task-audit/RUBRIC.md</code> §9 MUST carry <code>TRACE-007</code> in the judgment family: for every governed claim (§1.2) the spec ORIGINATES (§1.3), the audit states the claim, states its derivation, RE-RUNS that derivation, and MUST fail on any of four conditions, each named in the rule text - the derivation is ABSENT, does not RE-RUN, does not REPRODUCE the claimed value, or SUPPORTS A NARROWER SCOPE than the claim asserts.</li><li>1.2 <code>TRACE-007</code> MUST define exactly three governed claim classes and, for each, what discharges its derivation AND what does not: NUMERIC - a command, a script invocation, or an arithmetic composition of other derived values that reproduces the value; a value with prose provenance and no reproducing form does NOT discharge it. CITATION - a <code>file:line</code> plus the text found there, and where the cited document is itself in flight the REVISION it was read at; a line number alone does NOT discharge it, and a bare path into a moving document does NOT discharge it. UNIVERSAL NEGATIVE - a record of the witness search naming what was tried and how each failed, and the audit MUST attempt one counter-example before accepting it; a measurement of the author's own candidate does NOT discharge a claim about all candidates.</li><li>1.3 <code>TRACE-007</code> MUST define an ORIGINATED claim as one whose provenance is this document rather than a cited prior artefact, and MUST require the audit to partition the spec's governed claims into originated and inherited and to scrutinise the ORIGINATED set FIRST - an inherited claim carries a provenance that invites the check, an originated one arrives already believed, and nothing else in the workflow flags it.</li><li>1.4 <code>TRACE-007</code> MUST carry two worked anti-examples, each quoting the false claim verbatim, naming the true value, giving the command that reproduces it, and citing the REVISION the claim shipped in rather than a path: TASK-IMP-122 rewrite 4's AC 10 (<code>63705483:280</code> - <code>102dc507</code> paired with 1525, true count 1534) for the NUMERIC class, and TASK-IMP-121's <code>15894b1e:spec.md:168</code> (byte-exact restore \"impossible\", disproved in four lines of awk) for the UNIVERSAL NEGATIVE class. Both documents are under active repair; an anti-example that cites a live path documents nothing once the path is fixed.</li><li>1.5 <code>TRACE-007</code>'s text MUST state its kinship to <code>TRACE-006</code> and cite it by rule_id: TRACE-006 fails a test asserting less than its clause's VERB, TRACE-007 fails a derivation supporting less than its claim's SCOPE, and both are one defect - the author is the wrong reader of their own work.</li><li>1.6 <code>RUBRIC.md</code> §4's <code>COND-004</code> MUST require the <code>## AI Authorship Disclosure</code>'s <code>Scope:</code> bullet to partition its verification claims into three labelled sets - <code>re-derived and CONFIRMED</code>, <code>re-derived and CORRECTED</code>, <code>measured and ADDED</code> - MUST require it to state whether the audit's own figures were treated as claims to verify, and MUST forbid an unscoped universal attestation of the form \"every claim was re-measured\", which is unfalsifiable and which certified the false 1525 published beneath it.</li><li>1.7 <code>modules/skill/task-audit/SKILL.md</code> MUST instruct the auditor to treat the disclosure as a claim under TRACE-007 and never as evidence of diligence: to test each of the three partitions against the document - a value in <code>CONFIRMED</code> that no derivation reproduces is a finding, a governed originated claim in none of the three sets is a finding - and to record both the claim and its re-run in the audit body.</li><li>1.8 <code>tools/install/docs-tools/task-lint.mjs</code> MUST check ONLY the SHAPE of the disclosure partition - that COND-004's three labels are present in the <code>Scope:</code> bullet - and MUST NOT execute any command from a spec body, re-run any derivation, or assert that any claim is true; its finding message MUST name the check as shape-only. Executing a spec's derivations would run arbitrary shell out of a document the rubric treats as able to carry injected content (SAFE-001..004), and a floor that appeared to verify derivations would restore the assurance TRACE-007 exists to remove.</li><li>1.9 <code>modules/skill/task-author/SKILL.md</code> MUST instruct the author to record a governed claim's derivation into <code>source_pages</code> AT THE MOMENT the claim is originated, not at review time - the command is in the author's scrollback when the number is written and gone when it is audited, which is why the cost of the check is paid by whoever pays it last.</li><li>1.10 The amended rubric MUST FAIL <code>TASK-IMP-122</code> rewrite 4's AC 10 (<code>63705483:280</code>) on TRACE-007 and <code>TASK-IMP-121</code>'s <code>15894b1e:spec.md:168</code> on TRACE-007's universal-negative class, and MUST PASS <code>TASK-IMP-122</code> rewrite 5's disclosure (<code>15894b1e:spec.md:279-304</code>) on amended COND-004 - a rule that cannot fail the cases that motivated it is decoration, and one that fails its own worked prototype is mis-drafted. The three documents are cited at the revisions audited, so the evidence does not evaporate when the two live tasks are repaired.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - <code>modules/skill/task-audit/RUBRIC.md</code> §9 CARRIES a <code>TRACE-007</code> row in the judgment family whose text names all four failure conditions (absent / does not re-run / does not reproduce / narrower scope) and the state-claim-state-derivation-re-run sequence; the test MUST FAIL if any of the four is missing and MUST FAIL if TRACE-007 appears in a mechanical family - test: <code>tools/install/tests/test_authorship_derivation.sh::t01_rubric_carries_trace_007</code></li><li>[ ] AC 2 (traces_to: #1.2) - TRACE-007 DEFINES exactly three claim classes (NUMERIC, CITATION, UNIVERSAL NEGATIVE) and for each names both what discharges the derivation and what does NOT; the test MUST FAIL if any class lacks its non-discharging half, MUST assert the universal-negative row names the author's-own-candidate case, and MUST assert the citation row names the revision requirement for a document in flight - test: <code>tools/install/tests/test_authorship_derivation.sh::t02_three_classes_with_both_halves</code></li><li>[ ] AC 3 (traces_to: #1.3) - TRACE-007 DEFINES <code>originated</code> against cited-prior-artefact provenance and REQUIRES the audit to partition governed claims into originated/inherited with the originated set scrutinised first; the test MUST FAIL if the partition is described without the ordering - test: <code>tools/install/tests/test_authorship_derivation.sh::t03_originated_defined_and_ordered_first</code></li><li>[ ] AC 4 (traces_to: #1.4) - TRACE-007 CARRIES both worked anti-examples, each with the false claim quoted verbatim, the true value, a reproducing command, and a REVISION rather than a live path; the test MUST assert <code>1525</code>, <code>1534</code>, <code>102dc507</code> and <code>86cafee8</code> all appear in the numeric anti-example, that the universal-negative anti-example carries a runnable counter-example, and that both cite a commit sha that <code>git cat-file -e</code> resolves; MUST FAIL if either is present only by reference or cites a bare path - test: <code>tools/install/tests/test_authorship_derivation.sh::t04_both_anti_examples_are_worked</code></li><li>[ ] AC 5 (traces_to: #1.5) - TRACE-007's text STATES the kinship and CITES <code>TRACE-006</code> by rule_id, carrying both halves (verb vs assertion; scope vs derivation); the test MUST FAIL if TRACE-006 is cited without the contrast or the contrast is stated without the citation - test: <code>tools/install/tests/test_authorship_derivation.sh::t05_kinship_to_trace_006_stated</code></li><li>[ ] AC 6 (traces_to: #1.6) - <code>RUBRIC.md</code> §4's COND-004 REQUIRES the three partition labels and the audit-figures statement and FORBIDS an unscoped universal attestation; the test MUST FAIL if any label is absent, if the prohibition is absent, or if the prohibition is stated without naming the 1525 case as its reason - test: <code>tools/install/tests/test_authorship_derivation.sh::t06_cond_004_requires_the_partition</code></li><li>[ ] AC 7 (traces_to: #1.7) - <code>modules/skill/task-audit/SKILL.md</code> INSTRUCTS the auditor to test the disclosure as a claim, naming both findings (a CONFIRMED value no derivation reproduces; a governed originated claim in none of the three sets) and requiring both halves in the audit body; the test MUST FAIL if the skill instructs the auditor to credit the disclosure - test: <code>tools/install/tests/test_authorship_derivation.sh::t07_skill_tests_the_disclosure</code></li><li>[ ] AC 8 (traces_to: #1.8) - <code>task-lint.mjs</code> FAILS a disclosure whose <code>Scope:</code> bullet is missing any of the three labels, PASSES one carrying all three, and its message contains \"shape\"; and it EXECUTES NOTHING from a spec body - asserted by linting a fixture whose body contains a command that would create a sentinel file and asserting the sentinel does not exist, and by grepping the source for <code>exec</code>/<code>spawn</code>/<code>eval</code>/<code>Function(</code> in the COND-004 path; the test MUST FAIL if any derivation is re-run - test: <code>tools/install/tests/test_task_lint.sh::t09_cond_004_partition_shape_only</code></li><li>[ ] AC 9 (traces_to: #1.9) - <code>modules/skill/task-author/SKILL.md</code> INSTRUCTS the author to record the derivation into <code>source_pages</code> at origination time, naming that moment; the test MUST FAIL if the instruction is present but deferred to review time - test: <code>tools/install/tests/test_authorship_derivation.sh::t08_author_records_at_origination</code></li><li>[ ] AC 10 (traces_to: #1.10) - a recorded re-audit of the three documents against the amended rubric returns FAIL on <code>63705483:280</code> (TRACE-007, numeric), FAIL on <code>TASK-IMP-121/spec.md:168</code> (TRACE-007, universal negative), PASS on <code>TASK-IMP-122/spec.md:279-304</code> (COND-004) - verify: MANUAL, and manual by construction. TRACE-007 is a judgment rule; whether it fires is a model verdict over a document, and any shell test of it would assert that a recorded audit SAYS FAIL, which is weaker than the clause's verb and is the TASK-IMP-118 defect committed inside the task that generalises it. The artefact is three re-audits by an auditor who did not write this spec, filed beside it, each naming the rule, the claim, the derivation it re-ran and the value it got.</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A claim with NO derivation possible: TRACE-007 does not fire on a claim marked as a gap. TASK-IMP-121's <code>15894b1e:spec.md:168</code> is the anti-example precisely because it was NOT marked as a gap - it was asserted as a proof. \"I could not measure this\" is a derivation of a different kind and discharges the rule; \"no rule can\" is a universal negative and does not.</li><li>A citation into a document that moves BETWEEN authoring and audit: this happened to this spec, mid-draft (Problem 2). <code>TASK-IMP-121/spec.md:168</code> was accurate when written and returned an unrelated AC before the draft was finished, because a rewrite retracted the claim. NOT a TRACE-007 failure against the author - it is why §1.2's CITATION class requires a revision for a document in flight. A <code>file:line</code> into a live document is a claim with a half-life; a <code>sha:file:line</code> is not.</li><li>A number ORIGINATED by the audit, not the spec: covered, and this is the sharpest row. The round-4 audit supplied <code>1534</code> and rewrite 5 re-derived it rather than copying it (<code>spec.md:292</code>). An audit's figure is an inherited claim to the author and an originated one to the auditor. TRACE-007 governs the spec; §1.6 forces the author to say which side they treated it as.</li><li>A derivation that re-runs and reproduces a DIFFERENT value than when written: this is the phase-corpus case (551 -&gt; 550, <code>f8899d64</code> deleted a spec). NOT a TRACE-007 failure - the derivation did its job by surfacing the drift. It is a STALE-001 disposition, and TRACE-007's text must route it there rather than fail an honest number that the tree moved under.</li><li>A derivation that re-runs on the author's machine and not the auditor's: fails TRACE-007 (\"does not re-run\"). This is intended and is the same standard TRACE-003 sets for test paths. A command only its author can run is prose.</li><li>Two authors, one claim - author B reproduces author A's number and asserts it: it is INHERITED for B and B's disclosure must place it in <code>CONFIRMED</code>. If B did not re-derive it, it belongs in no partition and B must say so. Round 4 got this right on everything it inherited; the rule exists for the other column.</li><li>A clause with two verbs (§1.8: MUST check only shape, MUST NOT execute): both are compared under TRACE-006 and AC 8 asserts both halves separately. This is TASK-IMP-118 §3 row 5, and this task must not fail it.</li><li>SECURITY: a spec body carrying a derivation crafted to be executed. §1.8 forbids any execution from a body in the machine floor, and the auditor re-runs derivations under the same human-gated conditions as any other command they run. SAFE-001..004 already treat spec bodies as able to carry injected content; TRACE-007 must not become the reason a tool starts trusting them.</li><li>SECURITY: a derivation that reproduces the value and lies about what it measures (<code>echo 1525</code>). TRACE-007 fails it under \"supports a narrower scope than the claim asserts\" - <code>echo</code> derives a literal, never a count. The rule is not \"a command is present\"; that reading is exactly the shape-check trap §1.8 bounds, and it is why §1.1 is judgment-family.</li><li>DEGRADATION: TRACE-007 applied to a task audited before it existed - reported, never auto-failed. 183 <code>done</code> specs (derived: <code>measure-phase.mjs</code>, <code>done_total = 183</code>) could not have carried derivations. TASK-IMP-118 §3 row 11's disposition, unchanged.</li><li>DEGRADATION: the auditor cannot run a derivation (no network, missing fixture) - reported as unverified, never credited as confirmed. Unreadable is not satisfied; TASK-IMP-118 §3 row 10's disposition.</li><li>NULL/EMPTY: a spec that originates no governed claim (a pure policy change) - TRACE-007 is silent and COND-004's partitions may be empty, but the labels must still be present and each empty set must say so. An absent partition and an empty one are different claims.</li><li>BOUNDS: a derivation STRONGER than the claim (a full corpus scan behind a single count) - passes. Stronger is never a finding, per TASK-IMP-118 §3 row 3.</li><li>The disproof witness in <code>source_pages</code> was measured under GNU Awk 5.1.0 and its portability is untested. Immaterial: disproving \"no rule can invert it\" needs one witness, not a portable one. Named here so it is not mistaken for a recommendation to TASK-IMP-121, whose fix is its own to choose.</li><li><code>15894b1e</code> records \"three authors\" and this author cannot verify author identity from git - every commit in this range carries one committer. The audit COUNT (seven) is derived and holds; the author count is inherited and is used nowhere normative.</li></ul>\n<p>&lt;/content&gt; &lt;/invoke&gt;</p>";
