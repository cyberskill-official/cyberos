window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-128"]="<h1>TASK-IMP-128: Run the test suite in CI on ubuntu</h1>\n<h2>Summary</h2>\n<p>No workflow runs <code>scripts/tests/run_all.sh</code>. The suite is enforced only by a local pre-commit hook, on one macOS laptop, and only for commits that trigger it. One suite file has consequently never executed anywhere. Add an ubuntu job that runs the suite, converting a local convention into an enforced gate and giving the suite its first run on a platform other than the author's machine.</p>\n<h2>Problem</h2>\n<p>Two facts, both measured on 2026-07-20 against HEAD:</p>\n<ul><li>No file under <code>.github/workflows/</code> invokes <code>scripts/tests/run_all.sh</code>.</li><li>No file under <code>.github/workflows/</code> invokes <code>tools/install/tests/test_release_assets.sh</code>.</li></ul>\n<p>The second compounds the first. <code>test_release_assets.sh</code> self-skips on macOS with the message \"needs GNU tar (BSD/macOS host); runs on ubuntu/CI\". It does not run on ubuntu/CI, because CI does not run the suite at all. The test names a CI that would execute it, and that CI does not exist - so the file has never run on any machine, while reporting a skip that reads like coverage deferred rather than coverage absent.</p>\n<p>The wider consequence is the point. The pass figure this release was judged against is enforced only by the local pre-commit hook: on the committer's machine, and only for code changes. A contributor without the hook, or any docs-only commit, bypasses it entirely. Every \"suite green\" claim in the 1.0.0 release rests on a human having run it by hand on one macOS host.</p>\n<p>That single-platform enforcement has a measured cost. Five bash-3.2 and BSD-userland defects were fixed on 2026-07-19 - including a <code>sed -i</code> misuse that made a tamper-detection test pass unconditionally on macOS. A suite that ran on ubuntu would have failed on the GNU-vs-BSD divergence rather than silently passing.</p>\n<h2>Proposed Solution</h2>\n<p>Add a job to the existing CI that checks out the repo on <code>ubuntu-latest</code> and runs <code>bash scripts/tests/run_all.sh</code>. On ubuntu the suite executes all 37 files including <code>test_release_assets.sh</code>, giving that file its first execution and giving every other file its first run on a non-macOS host.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>A matrix across ubuntu and macOS. Deferred, not rejected: ubuntu is where the never-executed file runs and where the userland differs from the development host, so it is the higher-value half. A macOS leg can be added once the ubuntu leg is green and its runtime is known.</li><li>Rely on the pre-commit hook and document that contributors must install it. Rejected: the hook is machine-local and untracked, skipped for docs-only commits, and bypassed by <code>--no-verify</code> - which was itself used during this release. A convention enforced only by the honour system on one machine is the condition this task exists to end.</li><li>Run only <code>test_release_assets.sh</code> in CI. Rejected: it fixes the one file that has never run and leaves the other 36 still enforced by a single laptop.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: the suite runs on <code>ubuntu-latest</code> on every push and pull request, and a deliberately broken test fails the workflow. Baseline today: zero suites execute in CI and no test failure can fail a workflow.</li><li>Guardrail: <code>test_release_assets.sh</code> reports an executed result rather than a skip on the ubuntu leg - the file that has never run, runs.</li></ul>\n<h2>Scope</h2>\n<p>In scope: one CI job invoking the existing suite entrypoint, and whatever minimal fixes the suite needs to pass on ubuntu.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Rewriting or restructuring the suite - this task runs what exists.</li><li>A macOS or Windows leg (deferred; see Alternatives).</li><li>Coverage measurement or thresholds.</li><li>Removing or weakening the local pre-commit hook - it stays as the fast local signal.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Adjacent to TASK-IMP-127: both concern guarantees the release makes that are not mechanically enforced.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the 2026-07-19 B4 verification pass. The two absence claims are grep results against <code>.github/workflows/</code> at HEAD, re-run on 2026-07-20. An earlier draft of this finding stated that a release dispatch would give <code>test_release_assets.sh</code> its first execution; that was wrong - the payload job runs <code>release-assets.sh</code>, the producer, not the test - and the corrected claim (no workflow runs it, so it has never executed) is what this spec carries.</li><li><strong>Human review:</strong> scope and granularity approved at the 2026-07-20 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 A CI job MUST run <code>bash scripts/tests/run_all.sh</code> on <code>ubuntu-latest</code> for every push and pull request to the default branch.</li><li>1.2 A failing test MUST fail the job - the suite's non-zero exit MUST propagate to the workflow conclusion, and MUST NOT be masked by <code>continue-on-error</code>, <code>|| true</code>, or an ignored exit code.</li><li>1.3 <code>tools/install/tests/test_release_assets.sh</code> MUST execute rather than self-skip on the ubuntu leg, and its result MUST be included in the suite outcome.</li><li>1.4 The job MUST report the per-suite pass, fail, and skip counts in its log so a reader can see which files ran without re-running them locally.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - the workflow file declares a job on <code>ubuntu-latest</code> whose run step invokes <code>scripts/tests/run_all.sh</code>, triggered on push and pull_request - test: <code>tools/install/tests/test_ci_runs_suite.sh::t_suite_job_declared</code></li><li>[ ] AC 2 (traces_to: #1.2) - a deliberately failing test in a fixture suite causes the runner to exit non-zero, and no step in the job path swallows that exit code - test: <code>tools/install/tests/test_ci_runs_suite.sh::t_failure_propagates</code></li><li>[ ] AC 3 (traces_to: #1.3) - on a Linux host <code>test_release_assets.sh</code> runs its assertions instead of taking the GNU-tar skip branch - test: <code>tools/install/tests/test_ci_runs_suite.sh::t_release_assets_executes_on_linux</code></li><li>[ ] AC 4 (traces_to: #1.4) - the suite entrypoint emits pass, fail, and skip counts to stdout - test: <code>tools/install/tests/test_ci_runs_suite.sh::t_counts_reported</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Suites that assume BSD userland may fail on first ubuntu run. That is the finding, not a blocker: each failure is either a real portability defect to fix or a test to make platform-explicit. The job MUST NOT be merged with failures suppressed to make it green.</li><li>A suite requiring network or credentials MUST skip explicitly with a stated reason rather than fail, and the skip MUST be visible in the counts required by 1.4.</li><li>Suite runtime on a cold ubuntu runner is unknown; if it exceeds the job default timeout the timeout MUST be raised rather than the suite trimmed.</li><li><code>run_all.sh</code> invoked from a different working directory MUST still resolve its suite paths - CI checks out to a path unlike any local one.</li><li>Security-class: the job runs repository test code on a CI runner with the default token. It MUST NOT require elevated permissions, and MUST NOT be granted secrets - no suite in <code>run_all.sh</code> needs them, and granting them would expose secrets to test code on pull requests from forks.</li></ul>";
