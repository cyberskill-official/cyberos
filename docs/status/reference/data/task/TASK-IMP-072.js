window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-072"]="<h1>TASK-IMP-072: Repo-wide version consistency</h1>\n<h2>§1 - Description</h2>\n<ol><li>The stamper's coverage MUST be total: tauri.conf.json, src-tauri/Cargo.toml (about-dialog/crate metadata), apps/web/package.json, Android versionName+versionCode, iOS MARKETING_VERSION+CURRENT_PROJECT_VERSION, and the repo mcp/package.json (build.sh's payload stamp becomes a no-op).</li><li>version.yml's bump commit MUST run <code>stamp --apply</code> and commit every stamped file - a release commit carries the entire codebase to the new version, never just VERSION+CHANGELOG.</li><li>payload-gate MUST run <code>stamp --check --exit-code</code> so any manual drift is a red build.</li><li>The pre-commit hook MUST refuse a staged VERSION change whose artifacts drift, printing the exact one-line fix (<code>stamp --apply &amp;&amp; git add -u</code>).</li><li>Store-side invariants MUST hold: versionCode/CURRENT_PROJECT_VERSION derive from the monotonic BUILD_NUMBER (never VERSION), and the Play high-water guard (&gt;10700) stays.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Stamp-at-build kept binaries honest but let the repo lie between releases - a 1.0.0 that greps as 0.1.0 in five files is not \"1.0.0 across the whole codebase\". Three enforcement points (bump, gate, hook) close every path a stale stamp could survive.</p>\n<h2>§3 - Contract</h2>\n<p><code>node scripts/stamp-release-version.mjs [--apply|--check --exit-code]</code>; exit 10 on drift in check mode. Bump commit file set fixed in version.yml.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Coverage total</strong> (§1 #1) - <code>--check</code> at a fresh VERSION lists exactly the seven files; Cargo + mcp included.</li><li><strong>Bump carries all</strong> (§1 #2) - version.yml's git add names every stamped file after the apply step.</li><li><strong>Gate red on drift</strong> (§1 #3) - the payload-gate step exists; drift exits 10.</li><li><strong>Hook refuses drifting VERSION commits</strong> (§1 #4) - staged VERSION + drift = commit rejected with the fix line.</li><li><strong>Monotonic store counters</strong> (§1 #5) - versionCode==CURRENT_PROJECT_VERSION==BUILD_NUMBER; high-water guard intact.</li></ol>\n<h2>§5 - Verification</h2>\n<p>Live: <code>--check</code> output (7-file drift list at 0.4.0), hook rejection then acceptance around the 1.0.0 commit, gate step grep, version.yml git-add grep. Store counters asserted by the stamper's own output line.</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>stampCargo helper + two stampJson targets; three wiring blocks.</p>\n<h2>§7 - Dependencies</h2>\n<p>Rides TASK-IMP-071 (bump commits now visible to CI, so the gate actually runs on them).</p>\n<h2>§8 - Example payloads</h2>\n<p><code>VERSION=1.0.0 BUILD_NUMBER=10706 (androidVersionCode + iosBuildNumber)</code> + <code>all release artifacts already match VERSION</code>.</p>\n<h2>§9 - Open questions</h2>\n<p>None blocking. apps/console carries no version field (nothing to stamp); services' Cargo.tomls version internal crates, deliberately out of scope (they are not store artifacts).</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>New version-bearing file added later - the gate misses it until listed; the stamper header documents \"add here + version.yml git add\".</li><li>BUILD_NUMBER reused after a failed upload - Play/ASC reject; bump BUILD_NUMBER, never reuse.</li><li>Hook bypassed with --no-verify - payload-gate catches on push.</li><li>Stamp regex misses a reformatted pbxproj - check mode reports non-match as drift, loudly.</li><li>mcp double-stamp (repo + build.sh) - idempotent by construction.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>The 1.0.0 release commit itself is the first full-codebase stamp (operator-prepared, hook-verified).</p>\n<p><em>End of TASK-IMP-072.</em></p>";
