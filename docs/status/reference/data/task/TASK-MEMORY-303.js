window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MEMORY-303"]="<h1>TASK-MEMORY-303: Memory hardening - schema single-source, INTEROP.md, walker + doctor wiring</h1>\n<h2>Summary</h2>\n<p>Five memory-protocol promises are broken in ways that compound: the schema has forked (the root + vendored copies lack the StoreAcl definitions §14.4.7 makes normative, while the package-data copy has them) and the drift test that should have caught it points at a nonexistent path and silently skips; the §14.1-mandated <code>INTEROP.md</code> does not exist; using the protocol's own §7.7 dream or §18 session features would create <code>dreams/</code>/<code>sessions/</code> dirs the doctor's canonical-layout allowlist rejects, and the walker invariants AGENTS.md names for those features are undeclared; the live store is already FROZEN_RECOVERABLE from stray <code>adrs/</code> + <code>impl-plans/</code> dirs; and §18.7's <code>extra.session_id</code> stamping is unwired in the canonical writer. This task restores schema single-sourcing with a real drift test, authors INTEROP.md, teaches the walker the protocol's own features, repairs the live store under operator gate, wires <code>cyberos doctor</code> into the machine-gate floor where memory is installed, and stamps <code>extra.session_id</code>.</p>\n<h2>Problem</h2>\n<p>Audit finding H10 plus the memory-row medium items, all verified first-hand 2026-07-23:</p>\n<ol><li><strong>Schema fork, guarded by a skipping test.</strong> <code>modules/memory/cyberos/data/memory.schema.json</code> (the copy the Python package loads) carries <code>StoreAcl</code>/<code>StoreAclEntry</code>/<code>StoreAclMode</code>; <code>modules/memory/memory.schema.json</code> (the copy <code>build.sh:161</code> vendors into every payload) does not. Consumers therefore validate STORE.yaml ACLs against a schema that has never heard of them. <code>test_schema_drift.py</code> exists to catch exactly this and catches nothing: <code>_COMMITTED</code> points at <code>modules/memory/docs/memory.schema.json</code>, which does not exist, so every test in the module <code>pytest.skip</code>s - a green that means \"did not look\".</li><li><strong>INTEROP.md is a dangling MUST.</strong> AGENTS.md §14.1 binds non-ledger consumers to a &lt;= 6,000-char <code>INTEROP.md</code>; no such file exists anywhere. Every cross-agent consumer is currently bound to a document nobody can read.</li><li><strong>The walker rejects the protocol's own features.</strong> <code>_CANONICAL_TOP_LEVEL_DIRS</code> omits <code>sessions</code> (§18.2 bodies) and <code>dreams</code> (§7.7.4 artefacts): a store that exercises dreaming or transcripts goes doctor-RED. The invariants AGENTS.md explicitly names for these features (<code>dream-applied-row-has-provenance</code> §7.7.2, <code>store-yaml-acl-valid</code> §14.4.7, the §18.8 session lifecycle set) are absent from <code>memory.invariants.yaml</code>.</li><li><strong>The live BRAIN is frozen.</strong> <code>.cyberos/memory/store/</code> carries stray top-level <code>adrs/</code> and <code>impl-plans/</code> (measured: two dirs today; TASK-MEMORY-261's earlier context listed five, three since cleaned). <code>layout-root-canonical</code> fails, so §12 forces protocol-compliant agents to refuse writes - the audit trail is silently absent while the repo's own doctrine (AGENT-ENTRY.md #4) tells agents to record decisions into it.</li><li><strong>§18.7 unwired.</strong> The canonical writer never stamps <code>extra.session_id</code>, so even with an active session, put/move/delete rows carry no session linkage and the §18.8 walker checks would have nothing to verify.</li></ol>\n<h2>Proposed Solution</h2>\n<p><strong>Schema:</strong> regenerate <code>modules/memory/memory.schema.json</code> from the generator (<code>tools/cyberos_generate_schema.py</code>, whose Struct source already emits StoreAcl per <code>cyberos/core/store_acl.py</code>); fix <code>test_schema_drift.py</code>'s <code>_COMMITTED</code> to the real root-copy path and add <code>modules/memory/tests/test_schema_single_source.py</code> asserting (a) the generator's <code>--check</code> passes against the root copy, (b) root and package-data copies are byte-identical, (c) <code>build.sh</code> vendors from the root copy path (source-grep), and (d) the drift test can never silently skip - a missing committed schema is a FAIL, not a skip. <strong>INTEROP.md:</strong> author <code>modules/memory/INTEROP.md</code> (&lt;= 6,000 chars) covering the §14.1 consumer subset - read paths, the no-write rule for <code>audit/</code>/<code>HEAD</code>/<code>.lock</code>, canonical-writer routing, <code>STORE.yaml</code> ACL honor-for-writes (§14.4.6), sync_class export semantics (§14.3) - and vendor it via <code>build.sh</code> next to the schema. <strong>Walker:</strong> add <code>sessions</code> and <code>dreams</code> to <code>_CANONICAL_TOP_LEVEL_DIRS</code>; declare and implement the missing invariants (<code>dream-applied-row-has-provenance</code>, <code>store-yaml-acl-valid</code>, <code>session-lifecycle</code> covering §18.8's four checks) in <code>memory.invariants.yaml</code> + <code>invariants.py</code>. <strong>Store repair:</strong> an operator-gated <code>move</code> of the stray dirs' contents into their canonical homes per the ADR disposition TASK-MEMORY-261 specifies (decision first if 261 is unshipped), leaving the audit chain intact, ending with <code>cyberos doctor</code> OK on the live store. <strong>Doctor gate:</strong> <code>run-gates.sh</code> gains a doctor gate that runs when <code>.cyberos/memory/store/</code> exists AND the memory CLI is importable (<code>python3 -m cyberos doctor</code>), SKIPs (with provenance line) when either is absent, and fails RED on doctor FAIL. <strong>Session stamping:</strong> <code>writer.py</code> stamps <code>extra.session_id</code> on every put/move/delete row while <code>sessions/.active</code> names an active session (§18.7), covered by <code>test_session_id_stamping.py</code>.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Roll the package-data copy back to match the root (drop StoreAcl).</strong> Rejected: §14.4.7 makes StoreAcl normative and TASK-MEMORY-117 shipped its enforcement; the root copy is the stale side, so unification is package-data-forward.</li><li><strong>Point build.sh at the package-data copy instead of regenerating the root.</strong> Rejected: leaves two tracked copies whose equality nothing enforces - the current defect with the arrow flipped. Single-sourcing means one generator output, every copy byte-identical, a test that fails on divergence.</li><li><strong>Write INTEROP.md into <code>.cyberos/memory/</code> directly.</strong> Rejected: <code>.cyberos/</code> is the installed, machine-refreshed tree (gitignored); the source of truth belongs in <code>modules/memory/</code> beside the protocol docs and vendors outward like the schema does (§0.4's update rule: machine updates refresh docs without touching <code>store/</code>).</li><li><strong>Auto-repair the live store in this task without an operator gate.</strong> Rejected twice over: §0.3 memory-file immutability plus the standing instruction that BRAIN mutations here are operator-gated; and TASK-MEMORY-261 already specifies the decision procedure (add-to-canonical vs relocate) - executing before that ADR would guess the disposition. The gate is a HITL halt inside this task's implementation.</li><li><strong>Wire doctor into gates unconditionally.</strong> Rejected: most consumer installs have no memory store; an unconditional gate would RED every repo that never opted into memory. Presence-gated with a loud SKIP line preserves the fail-closed posture where memory exists without taxing repos where it does not.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release - every tracked + vendored <code>memory.schema.json</code> is byte-identical and StoreAcl-bearing; <code>pytest modules/memory/tests/test_schema_drift.py</code> executes (not skips) and passes; <code>INTEROP.md</code> exists &lt;= 6,000 chars and ships in the payload; <code>cyberos doctor</code> on the live store reports OK (0 layout errors) and on a store with <code>sessions/</code> + <code>dreams/</code> dirs reports OK; run-gates on this repo shows the doctor gate PASS. Baselines today: copies differ, drift test skips, no INTEROP.md, live store FAILs layout, no doctor gate.</li><li>Guardrail: the full <code>modules/memory</code> pytest suite stays green; stores WITHOUT memory installed see exactly one new SKIP line in run-gates and no behavior change; the audit chain on the live store is append-only through the repair (verified by <code>cyberos verify</code> before/after).</li></ul>\n<h2>Scope</h2>\n<p>In scope: schema regeneration + drift-test fix + single-source test, INTEROP.md authoring + vendoring, walker allowlist + three new invariant families, the operator-gated live-store repair, the presence-gated doctor gate in run-gates.sh, §18.7 stamping in writer.py, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>The single-source-of-truth refactor for the canonical-dir set across scaffolders and the five-artifact-dir ADR - TASK-MEMORY-261's scope; this task executes the live-store move under that ADR and adds the two protocol dirs to the allowlist, nothing more.</li><li>Fixing the applier that raw-writes artefacts to the store root - TASK-MEMORY-302 (bug, draft) owns the root cause; this task repairs the state it left behind.</li><li>PII/denylist gating inside <code>put</code>, sidecar format (b) emission, tmp-file nonce compliance - real §5/§8.3 gaps, deliberately deferred to keep this task shippable; recorded here so the deferral is discoverable.</li><li>The BRAIN recording of the audit itself - TASK-IMP-140's final step, which this task unblocks (<code>blocks: [TASK-IMP-140]</code>).</li></ul>\n<h2>Dependencies</h2>\n<p>Blocks TASK-IMP-140 (its §13 BRAIN-recording step needs the store un-frozen by this task's repair). Related: TASK-MEMORY-261 (draft - specifies the layout ADR + single-sourcing this task's repair executes under; runs first inside this task's HITL flow if still unshipped), TASK-MEMORY-302 (draft bug - the applier root cause), TASK-MEMORY-117 (done - shipped the StoreAcl enforcement the stale schema copies contradict), TASK-MEMORY-119 (done - shipped the transcript ledger whose §18.7 stamping this task completes).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> the schema diff (definition-key comparison), the drift test's phantom path, the INTEROP.md absence, the allowlist contents, the live store's stray dirs (two, not the plan-era five), the writer's session silence, and the gates' doctor silence were all measured first-hand at HEAD; no BRAIN writes were performed during authoring.</li><li><strong>Human review:</strong> the hardening plan was operator-approved 2026-07-23; the package-data-forward unification direction and the repair's HITL gating are recorded decisions for the review acceptance gate.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>modules/memory/memory.schema.json</code> MUST be regenerated so that the generator's <code>--check</code> passes against it and it carries the StoreAcl/StoreAclEntry/StoreAclMode definitions; the package-data copy and every vendored copy MUST be byte-identical to it. One generator, one content, N copies.</li><li>1.2 <code>test_schema_drift.py</code> MUST point <code>_COMMITTED</code> (and its docstring regen command) at the real committed path, and a missing committed schema MUST fail the test rather than skip - a conformance test that can skip on its trigger condition is not a conformance test.</li><li>1.3 A new <code>modules/memory/INTEROP.md</code> MUST exist at &lt;= 6,000 characters covering the §14.1 consumer subset (read paths; MUST NOT write <code>audit/</code>, <code>HEAD</code>, <code>.lock</code>; canonical-writer routing for chain-touching ops; §14.4.6 STORE.yaml honor-for-writes; §14.3 sync_class semantics), and <code>build.sh</code> MUST vendor it into the payload beside the schema.</li><li>1.4 <code>_CANONICAL_TOP_LEVEL_DIRS</code> MUST include <code>sessions</code> and <code>dreams</code>, and <code>memory.invariants.yaml</code> + <code>invariants.py</code> MUST declare and implement <code>dream-applied-row-has-provenance</code> (§7.7.2: every dream-applied row carries extra.dream_id + extra.proposal_id), <code>store-yaml-acl-valid</code> (§14.4.7: every STORE.yaml validates against the schema's StoreAcl), and <code>session-lifecycle</code> (§18.8: start/end pairing, monotonic turn_seq, no orphan turns). Each new invariant MUST fail on a constructed violating fixture and pass on a clean store.</li><li>1.5 The live store's stray top-level dirs (<code>adrs/</code>, <code>impl-plans/</code> at authoring time) MUST be relocated to their canonical homes via ledger-recorded <code>move</code> operations under an explicit operator approval recorded at this task's HITL gate, following the disposition ADR per TASK-MEMORY-261 (executing 261's decision step first if it is unshipped). After the repair, <code>cyberos doctor</code> on the live store MUST report zero layout errors and <code>cyberos verify</code> MUST confirm the chain intact.</li><li>1.6 <code>run-gates.sh</code> MUST gain a <code>doctor</code> gate that runs <code>python3 -m cyberos doctor</code> when <code>.cyberos/memory/store/</code> exists and the module is importable, maps doctor FAIL to gate RED, and emits a provenance SKIP line when store or CLI is absent. The gate MUST NOT change behavior on repos without memory.</li><li>1.7 <code>writer.py</code> MUST stamp <code>extra.session_id</code> on every put/move/delete audit row while <code>sessions/.active</code> names an active session, and MUST NOT stamp when no session is active (§18.7).</li><li>1.8 <code>CHANGELOG.md</code> MUST record the schema unification, INTEROP.md, the walker/doctor additions, and the store repair.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - generator <code>--check</code> exits 0 against the root copy; root, package-data, and a scratch payload's vendored copy hash identically; the root copy contains the three StoreAcl definition keys - test: <code>modules/memory/tests/test_schema_single_source.py::test_all_copies_identical_and_acl_bearing</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>test_schema_drift.py</code> collects and runs (0 skips) on this repo, and monkeypatching <code>_COMMITTED</code> to a missing path makes it FAIL not skip - test: <code>modules/memory/tests/test_schema_single_source.py::test_drift_test_cannot_skip</code></li><li>[ ] AC 3 (traces_to: #1.3) - INTEROP.md exists, <code>len(read_text()) &lt;= 6000</code>, contains the five mandated content anchors, and appears in a scratch payload build - test: <code>modules/memory/tests/test_interop_doc.py::test_interop_present_bounded_vendored</code></li><li>[ ] AC 4 (traces_to: #1.4) - a seeded store with <code>sessions/</code> + <code>dreams/</code> dirs passes layout; three constructed fixtures (dream row missing proposal_id; malformed STORE.yaml; session turn after session.end) each fail exactly their invariant; a clean store passes all three - test: <code>modules/memory/tests/test_walker_sessions_dreams.py::test_new_invariants_pass_and_fail_correctly</code></li><li>[ ] AC 5 (traces_to: #1.5) - on the live store post-repair: <code>cyberos doctor</code> reports zero layout errors, <code>cyberos verify</code> passes, the relocation rows are present on the chain, and the operator approval is recorded at the HITL gate (verified at review; the repair itself is demonstrated on a fixture store cloned from the live layout) - test: <code>modules/memory/tests/test_walker_sessions_dreams.py::test_repair_fixture_relocation_preserves_chain</code></li><li>[ ] AC 6 (traces_to: #1.6) - run-gates on a scratch repo WITH a seeded healthy store shows <code>PASS doctor</code>; with a store seeded to violate layout shows <code>FAIL</code> and RED exit; with no store shows the SKIP provenance line and unchanged exit - test: <code>tools/install/tests/test_doctor_gate.sh::t01_doctor_gate_three_states</code></li><li>[ ] AC 7 (traces_to: #1.7) - with an active session, a put/move/delete each carry <code>extra.session_id</code> equal to the active id; with no active session the key is absent - test: <code>modules/memory/tests/test_session_id_stamping.py::test_stamp_present_iff_active</code></li><li>[ ] AC 8 (traces_to: #1.8) - CHANGELOG's top entry names all four deliverable groups - test: <code>modules/memory/tests/test_interop_doc.py::test_changelog_records_hardening</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong><code>_SANDBOX_FRAGMENTS</code> contains <code>/sessions/</code>:</strong> the sandbox check tests the STORE'S OWN PATH, not entries inside it - adding a <code>sessions/</code> child dir does not trip it; a store legitimately installed under a path containing <code>/sessions/</code> remains rejected as before. The walker test includes this non-interference case.</li><li><strong>Store with legacy v1 debris AND the new dirs:</strong> <code>layout-root-canonical</code> keeps rejecting genuinely unknown dirs; only the two protocol-mandated names are added. The repair clause covers exactly the live store's measured strays; anything else found at repair time is surfaced to the operator, not auto-moved.</li><li><strong>Doctor gate on a FROZEN store mid-repair:</strong> until 1.5 completes, the new gate would RED this repo's own runs. Implementation order inside the task is therefore repair-before-gate-wiring, and the HITL review verifies the ordering was honored (the spec makes the order normative via this edge case).</li><li><strong><code>python3 -m cyberos</code> present but a different package:</strong> the gate probes importability of the cyberos memory module specifically (e.g. <code>python3 -c \"import cyberos.core\"</code> exit 0), not merely a binary named cyberos - the name-collision lesson from TASK-IMP-130 applied to gating.</li><li><strong>Session file exists but is stale (crashed session):</strong> §18.7 stamping trusts <code>sessions/.active</code>; a stale marker means rows carry a dead session id - accepted for this task (transcript-ledger lifecycle hygiene is TASK-MEMORY-119's domain), and the stamping test documents it.</li><li><strong>INTEROP.md growing past 6,000 chars later:</strong> the bound is normative (§14.1); the test pins it so a future edit that exceeds it fails CI rather than silently violating the protocol it documents.</li><li><strong>Security-class:</strong> the doctor gate executes only the repo's own installed memory module; INTEROP.md is documentation; the repair uses ledger-recorded moves under operator approval - no new execution or exfiltration surface. Session ids in audit rows are opaque ULIDs, no PII.</li></ul>";
