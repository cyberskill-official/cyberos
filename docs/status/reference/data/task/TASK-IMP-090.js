window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-090"]="<h1>TASK-IMP-090: task-author manifests default to untracked .workflow session state</h1>\n<h2>Summary</h2>\n<p>Author-run manifests are session state, but the skill defaults them into the tracked task tree and the .workflow gitignore seed only covers ship manifests - so this run's three batch manifests landed tracked. Per the recorded IMP-11 decision: the skill default moves to <code>docs/tasks/.workflow/</code>, the gitignore seed grows <code>*.manifest.json</code>, the three tracked manifests leave the index, and a tracked batch summary in <code>_audits/</code> becomes the durable approval record.</p>\n<h2>Problem</h2>\n<p>Session caches in tracked space churn history and get mistaken for records; the actual record the operator needs (what was planned, who approved, what landed) belongs in <code>_audits/</code> like the sachviet run's batch summary.</p>\n<h2>Proposed Solution</h2>\n<p>Four small moves: (1) SKILL.md CONTRACT_ECHO default becomes <code>docs/tasks/.workflow/task-author.&lt;slug&gt;.manifest.json</code>; (2) install.sh's .workflow/.gitignore seed writes both <code>*.ship.json</code> and <code>*.manifest.json</code>, and appends the manifest pattern once to an existing seed that lacks it; (3) <code>git rm --cached</code> the three batch manifests in this repo and update its .workflow/.gitignore; (4) write <code>docs/tasks/_audits/IMPROVEMENT-BATCHES-2026-07-16.md</code> summarizing batches 1-3 (members, verdicts, evidence commits) as the tracked approval record.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Keep manifests tracked as the audit trail. Rejected by the decision: they churn per-step and duplicate what _audits records once.</li><li>Ignore-only without the _audits summary. Rejected: removing tracking without a durable record would orphan the PLAN approvals.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a fresh install's .workflow/.gitignore covers both patterns and a new author run leaves zero tracked manifest files (hygiene scenario on every run). Baseline: three tracked manifests today. Deadline: final acceptance.</li><li>Guardrail: the _audits summary exists and names every batch member with its evidence commit.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the skill default line, the seed patterns plus append-once migration, this repo's index cleanup, the _audits summary.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Ship-manifest handling (already untracked by the existing pattern).</li><li>Rewriting git history (the files leave the index going forward; history keeps them).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>Shares the install.sh cone with TASK-IMP-088 - same agent, serial order per the batch plan. Disjoint from 089/091/092.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the recorded IMP-11 decision; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-3 PLAN approved 2026-07-16; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 task-author SKILL.md's CONTRACT_ECHO default manifest_path MUST become <code>docs/tasks/.workflow/task-author.&lt;slug&gt;.manifest.json</code>, with the caller override unchanged.</li><li>1.2 install.sh's .workflow/.gitignore seed MUST cover <code>*.ship.json</code> and <code>*.manifest.json</code>; when the file exists carrying only the ship pattern, the manifest pattern MUST be appended exactly once (idempotent on re-install).</li><li>1.3 This repo's three tracked batch manifests MUST leave the git index (<code>git rm --cached</code>), with the local .workflow/.gitignore updated so they stay on disk untracked.</li><li>1.4 A tracked approval record MUST land at <code>docs/tasks/_audits/IMPROVEMENT-BATCHES-2026-07-16.md</code> naming each batch's members, PLAN approval, HITL verdicts, and evidence commits.</li><li>1.5 Hygiene coverage MUST land as t07 scenarios in test_install_hygiene.sh for 1.2's fresh-seed and append-once paths.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - skill default names .workflow - verify: recorded grep of SKILL.md CONTRACT_ECHO in the gate log (prose contract; the skill is not executable in isolation).</li><li>[ ] AC 2 (traces_to: #1.2, #1.5) - fresh seed carries both patterns; existing seed gains the pattern exactly once across two installs - test: <code>tools/install/tests/test_install_hygiene.sh::t07_workflow_gitignore_patterns</code></li><li>[ ] AC 3 (traces_to: #1.3) - <code>git ls-files docs/tasks/.workflow</code> returns empty - verify: recorded command output in the gate log (repo-state chore).</li><li>[ ] AC 4 (traces_to: #1.4) - the _audits record names all 11 shipped members with commits - verify: recorded grep set in the gate log (single tracked document, same rationale as TASK-IMP-087 ACs).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Operator-customized .workflow/.gitignore with extra lines: append-once adds the one pattern and touches nothing else (AC 2's second-install assertion).</li><li>The removed-from-index manifests reappearing dirty: the updated local gitignore keeps them invisible (AC 3).</li><li>Security-class: none - gitignore and prose edits plus an index operation with recorded evidence.</li></ul>";
