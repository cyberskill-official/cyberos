window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-008"]="<h1>TASK-IMP-008: Goldensets as first-class gate inputs</h1>\n<h2>Summary</h2>\n<p>Make goldensets a real, documented gate input for the CyberOS 1.x <strong>install/payload</strong> surface: a sealed <code>tools/install/.awh/goldenset.yaml</code> + baseline, a runner that prefers <code>awh</code> and degrades cleanly when awh is absent, suite coverage, and a lightweight CI step alongside the existing module awh-gate.</p>\n<h2>Problem</h2>\n<p>Module goldensets exist under <code>modules/*/.awh/</code> and feed <code>awh-gate.yml</code>, but the install/payload tooling that ships CyberOS 1.x has no first-class goldenset. <code>awh_goldenset_from_task.py</code> helps author module tasks; nothing standardizes an install-path goldenset or a skip-clean runner for hosts without awh.</p>\n<h2>Proposed Solution</h2>\n<ol><li>Author <code>tools/install/.awh/goldenset.yaml</code> with lightweight, offline-safe tasks (VERSION semver, help.sh, run-gates script presence/shebang check, coverage-ratchet --help).</li><li>Commit a matching <code>eval-baseline.json</code> sealed from a green run.</li><li>Ship <code>tools/install/run-goldenset.sh</code> that: runs via <code>awh eval</code> when available; otherwise executes each task <code>cmd</code> with a tiny fallback runner; skips cleanly when <code>CYBEROS_SKIP_GOLDENSET=1</code> or when neither awh nor python3 is available (documented SKIP).</li><li>Document at <code>docs/verification/install-goldenset.md</code>.</li><li>Add an <code>install-goldenset</code> job to <code>awh-gate.yml</code> that does <strong>not</strong> require the docker/Postgres stack.</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Only document module goldensets.</strong> Rejected: mission asks for payload-relevant install goldenset.</li><li><strong>Require awh always.</strong> Rejected: local macOS contributors may lack awh; skip-clean is mandatory.</li><li><strong>Put install goldenset under modules/install.</strong> Rejected: install lives in <code>tools/install</code>; keep path honest.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: suite proves skip / awh-or-fallback run / fail-closed without baseline when forced.</li><li>Guardrail: existing module awh-gate job unchanged in spirit; install job is additive and offline-safe.</li></ul>\n<h2>Scope</h2>\n<p>In scope: install goldenset + baseline + runner + docs + suite + awh-gate.yml job + CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Regenerating every module goldenset.</li><li>Making awh a hard install dependency.</li><li>Auto-revert (TASK-IMP-026).</li></ul>\n<h2>Dependencies</h2>\n<p>None hard. Soft: awh CLI / <code>tools/awh</code> harness when present.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent, batch/12c.</li><li><strong>Human review:</strong> session operator Stephen Cheng.</li></ul>\n<h2>§1 - Description (normative)</h2>\n<ol><li><code>tools/install/.awh/goldenset.yaml</code> MUST exist with schema compatible with awh (<code>tasks:</code> list; each task has <code>id</code>, <code>cmd</code>, <code>weight</code>, <code>timeout_sec</code>).</li><li>At least three tasks MUST be offline-safe (no network, no docker, no cargo).</li><li><code>tools/install/.awh/eval-baseline.json</code> MUST be committed; awh-gate install job MUST fail closed if goldenset exists without baseline.</li><li><code>tools/install/run-goldenset.sh</code> MUST:</li></ol>\n<ul><li>exit 0 with a <code>SKIP</code> line when <code>CYBEROS_SKIP_GOLDENSET=1</code>;</li><li>prefer <code>awh eval … --baseline … --max-regression 0.0</code> when <code>awh</code> is on PATH or <code>python3 -m harness.cli</code> works with <code>PYTHONPATH=tools/awh</code>;</li><li>otherwise run each task <code>cmd</code> via a python3 YAML fallback;</li><li>if neither awh nor python3 is available, exit 0 with <code>SKIP</code> naming the reason.</li></ul>\n<ol><li><code>docs/verification/install-goldenset.md</code> MUST document path, runner, skip rules, and CI wiring.</li><li><code>.github/workflows/awh-gate.yml</code> MUST gain an <code>install-goldenset</code> job (no postgres/redis) that runs the install goldenset when <code>tools/install/**</code> changes (or always on PR — either is acceptable if documented).</li><li>Suite <code>test_install_goldenset.sh</code> MUST cover skip + runner presence + goldenset shape.</li></ol>\n<h2>Acceptance criteria</h2>\n<ul><li>[x] AC1: goldenset.yaml parses and lists &gt;= 3 tasks with id+cmd. (t01)</li><li>[x] AC2: baseline file present next to goldenset. (t02)</li><li>[x] AC3: <code>CYBEROS_SKIP_GOLDENSET=1</code> → exit 0 + SKIP. (t03)</li><li>[x] AC4: runner executes successfully on this host (awh path validates committed baseline; fallback executes committed tasks without baseline compare). (t04)</li><li>[x] AC5: docs page names runner + skip env + CI job. (t05)</li><li>[x] AC6: awh-gate.yml contains install-goldenset job referencing tools/install/.awh. (t06)</li></ul>\n<h2>Test plan</h2>\n<p><code>bash tools/install/tests/test_install_goldenset.sh</code></p>";
