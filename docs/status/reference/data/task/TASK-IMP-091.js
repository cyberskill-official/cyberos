window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-091"]="<h1>TASK-IMP-091: regen_backlog emits every status and recomputes Totals from frontmatter</h1>\n<h2>Summary</h2>\n<p>The function every insert contract cites as the byte authority cannot regenerate a truthful index: its ACTIVE-status filter drops done rows entirely - the proven root cause of the fourteen-task drift TASK-IMP-086 repaired. Make regen_backlog emit one row per task folder for every status, recompute the repo-wide Totals line from a frontmatter tally, and gate it with a parity test that regenerates today's corpus and byte-compares the improvement section against the committed file.</p>\n<h2>Problem</h2>\n<p>TASK-IMP-086's mandated trial recorded the failure precisely: regen deleted the three committed done rows, emitted nothing for 068-081 (all done), and left a Totals line three short of the corpus. An index regenerator that silently drops terminal statuses turns the audit trail into fiction the moment tasks ship.</p>\n<h2>Proposed Solution</h2>\n<p>Remove the ACTIVE filter from row emission (the index lists the whole corpus; the queue's eligibility logic stays elsewhere), keep grammar and stem ordering byte-identical to the committed convention, and compute Totals from a per-status frontmatter tally at regen time. A new suite regenerates against the live corpus into a scratch copy and asserts (a) byte-parity of the improvement section with the committed file and (b) Totals equal to the tally.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Re-point the byte-authority citation at backlog-mutate.mjs and retire regen. Rejected for now: full-section regeneration from frontmatter remains the recovery tool for exactly the drift class 086 hit; fixing it is one filter.</li><li>Emit done rows into a separate archive section. Rejected: the committed convention (086's repair, reviewed and merged) lists them in place with the status tag.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: regen on today's corpus reproduces the committed improvement section byte-for-byte and a true Totals line, asserted by the suite on every run. Baseline: regen output deletes 17 rows and undercounts Totals by 3 (recorded trial). Deadline: final acceptance.</li><li>Guardrail: a fixture corpus with one task per status yields one row per status (the filter cannot silently return).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the emission filter, the Totals computation, the parity suite.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Other sections' drift repair (regen fixes the mechanism; running it repo-wide is an operator choice).</li><li>The migrate/adopt phases of the script (row regeneration only).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None; cone is the python script plus a new scripts/tests suite (disjoint from all batch members).</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the recorded TASK-IMP-086 trial evidence; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-3 PLAN approved 2026-07-16; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 regen_backlog MUST emit exactly one row per task folder for EVERY frontmatter status (the ACTIVE filter is removed from emission), preserving the committed row grammar and stem-ascending order.</li><li>1.2 The Totals line MUST be recomputed from a per-status tally of the corpus frontmatter at regen time.</li><li>1.3 Regenerating today's corpus MUST reproduce the committed improvement section byte-for-byte (rows and header), proven in the suite against a scratch copy.</li><li>1.4 A fixture corpus containing one task in each lifecycle status MUST yield one row per status.</li><li>1.5 The suite MUST land at scripts/tests/test_regen_backlog.sh, discovered by the run_all glob.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - live-corpus regen is byte-identical to the committed section - test: <code>scripts/tests/test_regen_backlog.sh::t01_live_corpus_parity</code></li><li>[ ] AC 2 (traces_to: #1.2) - Totals equals the frontmatter tally - test: <code>scripts/tests/test_regen_backlog.sh::t02_totals_true</code></li><li>[ ] AC 3 (traces_to: #1.4) - per-status fixture emits every row - test: <code>scripts/tests/test_regen_backlog.sh::t03_every_status_emitted</code></li><li>[ ] AC 4 (traces_to: #1.5) - suite discovered by run_all - verify: suite listed in the runner output (ops check in the gate log; glob discovery is the runner's contract).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Titles containing the row separator or other task ids (081's title quotes TASK-IMP-080): emitted verbatim; parity is byte-level so any mangling fails t01.</li><li>A folder with unparseable frontmatter: regen MUST fail loudly naming the file, never emit a guessed row (asserted inside t03's fixture set).</li><li>Sections other than improvement: untouched by this task's assertions; the mechanism fix applies wherever the script is pointed.</li><li>Security-class: none - a read-compute-write script over tracked markdown, gated by byte-parity.</li></ul>";
