window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-205"]="<h1>TASK-CUO-205: Single backlog write path</h1>\n<h2>§1 - Description</h2>\n<p>BACKLOG.md is the index every workflow trusts, yet only one of its two writers is governed. This task extends the backlog-state-update contract from status-cell rewrites to row insertion and routes /create-tasks through it, leaving exactly one audited write path to the file.</p>\n<p>Normative clauses:</p>\n<ol><li>The artefact MUST bump to <code>backlog-state-update@2</code> with a <code>mutation_kind</code> enum of exactly <code>{status-cell-only, insert-row}</code>. <code>@1</code> artefacts (implicitly status-cell-only) MUST remain valid inputs to the audit during a one-release transition window.</li><li><code>insert-row</code> payload MUST carry: <code>task_id</code>, <code>slug</code>, <code>title</code>, <code>class</code> (product|improvement), <code>status</code> (10-value enum), <code>module</code>, and <code>expected_absent: true</code>. Its concurrency gate is the inverse of <code>old_line</code>: the audit MUST verify no row for <code>task_id</code> existed in the pre-image and exactly one exists in the post-image.</li><li>The inserted row MUST match the regenerator's format byte-for-byte: <code>- [&lt;status&gt;] &lt;task-ID-slug&gt; - &lt;title&gt;</code> with the <code> (improvement)</code> suffix when <code>class: improvement</code>, placed inside the module's <code>## &lt;module&gt;</code> section (section created per regen conventions when absent), rows kept in sorted order within the section.</li><li>The audit rubric MUST gain insert-row rules: BSU-INS-001 (row absent before, present once after), BSU-INS-002 (format + suffix exact), BSU-INS-003 (placed in the correct module section, sort order kept), BSU-INS-004 (no other line of the file changed except that section's header counts when they are present), BSU-INS-005 (status is a valid enum value and equals the task frontmatter status at write time).</li><li><code>plugin/commands/create-tasks.md</code> step 3 MUST instruct delegation to <code>backlog-state-update-author</code> + <code>-audit</code> (one insert-row mutation per task, batched per module section allowed), and MUST NOT instruct inline editing of BACKLOG.md. The step MUST remind that task frontmatter stays the record of truth and the backlog the index.</li><li>/ship-tasks behavior MUST be unchanged (status-cell-only remains its only mutation kind); <code>workflow_complete</code> audit-row semantics of the existing contract are untouched.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Insert-row is the smallest extension that removes the ungoverned writer: same file, same optimistic-concurrency philosophy (uniqueness instead of byte-match), same audit discipline. Reusing the regenerator's row grammar means <code>migrate_improvement_to_task.py --backlog</code> remains a drop-in rebuild at any time - the inserted rows are indistinguishable from regenerated ones, so the two mechanisms can coexist without churn.</p>\n<h2>§3 - Contract</h2>\n<pre><code class=\"language-json\">// backlog-state-update@2, mutation_kind=insert-row (input envelope fragment)\n{\n  \"mutation_kind\": \"insert-row\",\n  \"task_id\": \"TASK-TEN-208\",\n  \"slug\": \"TASK-TEN-208-rls-audit-sweep\",\n  \"title\": \"RLS audit sweep\",\n  \"class\": \"improvement\",\n  \"status\": \"ready_to_implement\",\n  \"module\": \"ten\",\n  \"expected_absent\": true\n}</code></pre>\n<p>Audit verdict: pass | fail | needs_human, score /10, findings keyed BSU-INS-00x (plus the existing status-cell rules for that kind).</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>@2 enum is closed and back-compatible</strong> (§1 #1) - the author SKILL.md declares exactly the two kinds; an @1 artefact (no mutation_kind) audits as status-cell-only with a transition note, not a failure.</li><li><strong>Duplicate insert is refused</strong> (§1 #2) - an insert-row mutation for an task_id already present in the pre-image fails audit with BSU-INS-001.</li><li><strong>Row grammar is regenerator-identical</strong> (§1 #3) - inserting a row then running the regenerator produces zero diff for that row (fixture asserts byte equality, both classes: with and without the improvement suffix).</li><li><strong>Section handling</strong> (§1 #3) - insert into an existing module section keeps sort order; insert for a module with no section creates the section per regen conventions (fixture covers both).</li><li><strong>Whole-file discipline</strong> (§1 #4) - a mutation that also touches an unrelated line fails BSU-INS-004.</li><li><strong>Status honesty</strong> (§1 #4 BSU-INS-005) - inserting a row whose status differs from the task file's frontmatter fails audit.</li><li><strong>Command doc delegates</strong> (§1 #5) - create-tasks.md step 3 names the pair and contains no inline-edit instruction (the current \"done inline\" wording is gone).</li><li><strong>Ship path untouched</strong> (§1 #6) - the ship workflow doc and the status-cell rules are diff-clean apart from the @2 version reference.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-markdown\"># modules/skill/backlog-state-update-author/acceptance/INSERT_ROW_CASES.md\n# Executable case table (each case: pre-image fixture, mutation JSON, expected verdict + finding).\nCASE-01 insert into existing section, product row          -&gt; pass          # AC 3, 4\nCASE-02 insert with improvement suffix                     -&gt; pass          # AC 3\nCASE-03 duplicate task_id in pre-image                        -&gt; fail BSU-INS-001   # AC 2\nCASE-04 new module section created, sorted                 -&gt; pass          # AC 4\nCASE-05 stray edit to an unrelated line                    -&gt; fail BSU-INS-004   # AC 5\nCASE-06 row status != task frontmatter status                -&gt; fail BSU-INS-005   # AC 6\nCASE-07 @1 artefact (no mutation_kind)                     -&gt; pass (transition note)  # AC 1\nCASE-08 regenerator round-trip byte equality               -&gt; pass          # AC 3</code></pre>\n<p>Plus doc assertions for AC 7/8: grep create-tasks.md for the delegation wording and absence of inline-edit wording; <code>git diff</code> scope check on the ship workflow doc.</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Author SKILL.md: add the mutation_kind section, the insert payload table, and the placement algorithm (find <code>## &lt;module&gt;</code> header; binary-insert by row string; create section per regen template when missing). Audit SKILL.md + RUBRIC.md: add the BSU-INS family. Command doc: rewrite step 3 to the delegation form.</p>\n<h2>§7 - Dependencies</h2>\n<p>Sequencing preference (not a hard dep): TASK-SKILL-118 first, so RUBRIC.md exists as the place BSU-INS rules land; if this task goes first, the rules land in SKILL.md prose and migrate. TASK-CUO-208 later threads template choice through the same command doc - independent sections, no conflict.</p>\n<h2>§8 - Example payloads</h2>\n<p>Pre-image row absent; post-image gains exactly:</p>\n<pre><code>- [ready_to_implement] TASK-TEN-208-rls-audit-sweep - RLS audit sweep (improvement)</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Batched inserts (one artefact carrying N rows for one module) are permitted by §1 #5's \"batched per module section allowed\"; the audit treats each row against BSU-INS-001..005 individually.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Two agents insert concurrently - second audit sees the first's row in its pre-image only if it re-reads; the <code>expected_absent</code> gate plus post-image single-occurrence check turns a race into a deterministic fail-and-retry, mirroring put_if semantics from the memory protocol.</li><li>Module header counts drift (headers carry status counts) - BSU-INS-004 permits touching that section's header counts only; the regenerator remains the periodic reconciler.</li><li>Insert against a hand-mangled BACKLOG (nonstandard section headers) - placement algorithm requires the exact <code>## &lt;module&gt;</code> grammar; anything else -&gt; needs_human, never a guess.</li><li>Class/suffix mismatch (product row with improvement suffix) - BSU-INS-002 exact-format rule catches it.</li><li>Transition-window ambiguity (@1 vs @2) - one release later, @1 acceptance drops; the audit SKILL.md carries the sunset date at implementation time.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the placement algorithm identical to <code>regen_backlog()</code> in scripts/migrate_improvement_to_task.py (same sort key: row string ascending) - cite the function in the SKILL.md so future edits to either side know about the other.</p>\n<p><em>End of TASK-CUO-205.</em></p>";
