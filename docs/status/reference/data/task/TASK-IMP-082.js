window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-082"]="<h1>TASK-IMP-082: Status page provenance stamp becomes a corpus fingerprint (byte-stable)</h1>\n<h2>Summary</h2>\n<p>Change the status renderer's default provenance stamp from the current git HEAD to a deterministic fingerprint of the render inputs themselves. Today every bare re-render on an advanced HEAD differs by exactly the stamp, and committing the page advances HEAD again - a self-chasing loop the renderer's own comment describes but only mitigates behind an env pin nobody sets. A corpus fingerprint ends the chase in every flow: re-renders are byte-stable until the task corpus, CHANGELOG, or VERSION actually changes.</p>\n<h2>Problem</h2>\n<p>The stamp default is repo position, not corpus content:</p>\n<p>&lt;untrusted_content source=\"tools/docs-site/render-status-hub.mjs:304-307\"&gt; // CYBEROS_COMMIT pins the provenance stamp. A page staged by the pre-commit hook necessarily // carries the PARENT commit's sha (the new one does not exist yet), so a later re-render would // differ from it by the stamp alone. Pinning lets a freshness check compare CONTENT. const COMMIT = process.env.CYBEROS_COMMIT || gitCommit(ROOT); &lt;/untrusted_content&gt;</p>\n<p>No production caller (install, migrate, status-page.sh, run-gates, the pre-commit hooks) sets <code>CYBEROS_COMMIT</code>, so the mitigation is dead code in practice. Reproduced twice on the sachviet consumer repo on 2026-07-16: each re-install on an unchanged corpus dirtied the tree by one stamp; committing that (df24cb3) armed the next diff. A tracked, generated file that can never be clean is a standing false-positive in every <code>git status</code>.</p>\n<h2>Proposed Solution</h2>\n<p>Make the default stamp a content fingerprint: the first 12 hex chars of sha256 over the ordered render inputs the script already reads (every task spec's bytes in sorted path order, CHANGELOG.md, VERSION). Properties: no git invocation on the default path, no wall clock, identical corpus in means identical page out - including the hook flow, because the page itself is not a render input. <code>CYBEROS_COMMIT</code> keeps winning when set, so any caller that wants a git sha can still pin one. The visible label shape (\"built from <code>&lt;stamp&gt;</code>\", footer, cs-data <code>commit</code> field) stays as is; only the value's derivation changes.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Stamp from the last commit touching the inputs (<code>git log -1 --format=%h -- docs/tasks CHANGELOG.md VERSION</code>). Rejected: still chases in the pre-commit flow (the staged corpus's commit does not exist yet, so the landed page stamps the parent and the next render differs), and it needs git, which the renderer otherwise avoids.</li><li>Drop the stamp entirely. Rejected: the stamp is the freshness check the comment at :304 wants - \"compare CONTENT\" is exactly what a corpus fingerprint is; removing it removes the check.</li><li>Have every caller set CYBEROS_COMMIT. Rejected: five call sites across two hook variants plus consumer repos; one changed default beats N remembered call sites.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: re-render churn goes to zero - two consecutive renders on an unchanged corpus are byte-identical, and render -&gt; commit page -&gt; render is byte-identical. Baseline (today, sachviet evidence): every such cycle produces a 3-line diff. Deadline: this task's final acceptance.</li><li>Guardrail: a real corpus change still changes the stamp exactly once (the freshness check keeps working), asserted by the same suite on every run.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the stamp derivation in <code>render-status-hub.mjs</code>, its three output surfaces (header meta, footer, cs-data JSON), and a regression suite.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any other renderer behavior (lenses, drawer, spec chunks, KPI math).</li><li>The hook scripts and their staging flow (unchanged; they simply stop producing churn).</li><li>A freshness CLI that compares stamp vs corpus (possible follow-up, not here).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None upstream. Downstream: consumer repos inherit via the payload docs-tools copy (build.sh:171); TASK-IMP-083 and TASK-IMP-084 are cone-disjoint batch siblings.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted by the model from IMPROVEMENT_HANDOFF.md IMP-01 plus direct source investigation; implementation follows under ship-tasks supervision.</li><li><strong>Human review:</strong> PLAN approved by the operator on 2026-07-16; spec audit and both HITL acceptance gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The renderer's default stamp MUST be <code>fp-</code> plus the first 12 lowercase hex chars of sha256 over the ordered render inputs: every task spec file's raw bytes in sorted repo-relative path order, then CHANGELOG.md bytes when present, then VERSION bytes when present. The <code>fp-</code> prefix makes fingerprints distinguishable from git shas at a glance.</li><li>1.2 <code>CYBEROS_COMMIT</code>, when set and non-empty, MUST override the default exactly as today (explicit pin wins).</li><li>1.3 Two consecutive renders over an unchanged corpus MUST produce byte-identical index.html (whole file, not just the stamp).</li><li>1.4 Rendering, committing the rendered page, and rendering again MUST produce byte-identical output - the page's own bytes MUST NOT be a render input (the chase case).</li><li>1.5 Any change to a render input MUST change the stamp on the next render, and the render after that MUST be stable again.</li><li>1.6 The default path MUST NOT invoke git; rendering in a directory that is not a git checkout MUST produce the same fingerprint semantics.</li><li>1.7 All three stamp surfaces (header meta line, footer, cs-data <code>commit</code> field) MUST carry the same value; no other page content changes.</li><li>1.8 A regression suite MUST land at <code>scripts/tests/test_render_stamp.sh</code> covering 1.3, 1.4, 1.5, 1.2, and 1.6, discovered by the existing run_all glob.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: §1 #1.1, #1.7) - stamp is the fp- fingerprint on all three surfaces - test: <code>scripts/tests/test_render_stamp.sh::t01_fingerprint_on_all_surfaces</code></li><li>[ ] AC 2 (traces_to: §1 #1.3) - double render is byte-identical, on a populated AND an empty corpus - test: <code>scripts/tests/test_render_stamp.sh::t02_double_render_stable</code></li><li>[ ] AC 3 (traces_to: §1 #1.4) - render, commit page, render is byte-identical - test: <code>scripts/tests/test_render_stamp.sh::t03_commit_chase_ended</code></li><li>[ ] AC 4 (traces_to: §1 #1.5) - corpus edit changes the stamp exactly once - test: <code>scripts/tests/test_render_stamp.sh::t04_corpus_edit_changes_once</code></li><li>[ ] AC 5 (traces_to: §1 #1.2) - CYBEROS_COMMIT override wins - test: <code>scripts/tests/test_render_stamp.sh::t05_env_pin_wins</code></li><li>[ ] AC 6 (traces_to: §1 #1.6) - non-git directory renders with fingerprint, no git spawned - test: <code>scripts/tests/test_render_stamp.sh::t06_no_git_needed</code></li><li>[ ] AC 7 (traces_to: §1 #1.8) - suite is discovered by run_all - verify: <code>bash scripts/tests/run_all.sh</code> lists test_render_stamp.sh among suites (ops check recorded in the gate log; the glob discovery is the runner's own contract).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Empty corpus (0 tasks, no CHANGELOG): fingerprint over VERSION alone (or the empty input set) MUST still be deterministic - t02 runs this shape.</li><li>Task file with CRLF or trailing-whitespace edits: bytes changed means stamp changes - correct by definition (content is the contract); documented so nobody \"fixes\" it.</li><li>Sorted path order MUST be bytewise (locale-independent sort) or the fingerprint drifts between machines - implementation sorts with a fixed comparator, t02 re-runs under LC_ALL=C and a UTF-8 locale.</li><li>Very large corpora: hashing is streaming (per-file update), no full concat buffer - inspection note in review, no perf gate.</li><li>CYBEROS_COMMIT set to empty string MUST fall through to the default (matches current <code>||</code> semantics) - covered inside t05.</li><li>Security-class: none - no new input surface; the renderer already reads exactly these files. Nearest concern is hash choice: sha256 (node crypto), not a homegrown hash; asserted by code review.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<p>Duplicated intentionally with <code>## Scope</code> for template conformance: hooks, freshness CLI, and all other renderer features are untouched.</p>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>Determinism doctrine: no wall clock, no randomness, no environment-dependent output beyond the documented pin.</li><li>The status page stays a pure function of the corpus; markdown remains the record of truth.</li><li>Payload sync doctrine: rebuild dist, version-sync, full suite before commit.</li><li>HITL: both human-acceptance gates are recorded verdicts; the agent never sets done.</li></ul>\n<p><em>End of TASK-IMP-082.</em></p>";
