window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-139"]="<h1>TASK-IMP-139: Corpus hygiene - UNREVIEWED disposition, module-case lint, stuck-WIP triage</h1>\n<h2>Summary</h2>\n<p>Three corpus-integrity debts: 170 non-draft specs (151 of them <code>done</code>) still carry the <code># UNREVIEWED</code> markers the 2026-07-14 migration attached to compliance fields with the instruction \"a human MUST confirm before this task leaves draft\" - so the corpus's <code>ai_authorship</code> and <code>eu_ai_act_risk_class</code> claims on shipped work are unconfirmed and every one of those files fails FM-112 if linted; 251 specs carry mixed-case <code>module:</code> values with no lint rule guarding the field; and 12 tasks sit in <code>implementing</code> - eleven since mid-May - with every forward mid-state otherwise unoccupied, evidence the full lifecycle is rarely exercised and stale WIP accumulates silently. This task dispositions the markers under an explicit operator fork (bulk-clear with recorded verdict vs re-audit wave), normalizes module case and adds the missing lint rule, and triages the 12 via <code>task-reconcile</code> with per-task operator verdicts.</p>\n<h2>Implementation note: two operator gates inside this task</h2>\n<p><strong>Gate 1 - the UNREVIEWED fork (do not pre-empt).</strong> The marker disposition is a process decision the operator must make explicitly, recorded as a dated <code>source_decisions</code> entry on this spec before any marker is touched:</p>\n<ul><li><strong>Branch clear:</strong> one recorded bulk verdict (\"the operator accepts the migrated <code>ai_authorship</code>/<code>eu_ai_act_risk_class</code> values on the enumerated 170 files as-is\") authorizes removing all markers mechanically; the verdict text ships in the commit body and CHANGELOG. Cheap, honest about being a batch acceptance, leaves per-file confirmation undone forever.</li><li><strong>Branch re-audit:</strong> a wave re-runs task-audit's compliance families over the 170 files, confirming or correcting the two fields per file before its markers drop; markers survive on any file whose fields a human has not yet confirmed. Expensive, produces real per-file confirmation.</li></ul>\n<p><strong>Gate 2 - the stuck-WIP triage (per-task verdicts).</strong> Each of the 12 <code>implementing</code> tasks gets a <code>task-reconcile</code> evidence report (what the ladder says actually shipped vs the status), and the operator records ONE verdict per task - resume / route back per STATUS-REFERENCE §1.3 / <code>on_hold</code> - via the standard override path (which emits <code>memory.status_overridden</code> once TASK-CUO-303 lands, or its documented equivalent before). This task performs no status flip on its own authority; TASK-APP-001 (July-created) is expected to be a legitimate resume, which is exactly why the verdicts are per-task, not batch.</p>\n<h2>Problem</h2>\n<p>Verified first-hand 2026-07-23:</p>\n<ol><li><strong>Compliance fields unconfirmed at scale (audit H2).</strong> The migration marked every auto-set <code>ai_authorship</code> / <code>eu_ai_act_risk_class</code> with <code># UNREVIEWED ... a human MUST confirm before this task leaves draft</code>. 170 files left draft anyway - 151 all the way to <code>done</code>. FM-112 exists precisely to stop this (error, marker past draft) but only fires when the linter runs against a file, which happens at authoring/audit time, not retroactively over the corpus. The EU-AI-Act-adjacent fields on shipped work are, today, machine guesses wearing confirmed clothes.</li><li><strong><code>module:</code> field ungoverned.</strong> 251 files say <code>module: AUTH</code>-style uppercase inside lowercase folders. Nothing breaks loudly (the regenerator groups by folder), but the field feeds the status hub and any future module-scoped tooling, and task-lint has NO rule for it - the only frontmatter field of its kind with zero validation.</li><li><strong>Stale WIP, invisible.</strong> 11 of 12 <code>implementing</code> tasks are ~10 weeks old with zero forward mid-states occupied corpus-wide; either work stalled silently or statuses were never advanced. Both are lifecycle-integrity failures, and nothing surfaces them (the stuck-WIP sentinel is a v3.x roadmap item; this triage is the manual pass that clears today's backlog of doubt).</li></ol>\n<h2>Proposed Solution</h2>\n<p><strong>Markers:</strong> enumerate the 170 files mechanically (the enumeration script ships in the test suite so review sees the exact set); halt at Gate 1 for the fork verdict; execute the chosen branch; either way the end state is zero <code># UNREVIEWED</code> markers on non-draft specs whose fields carry a recorded confirmation trail, and FM-112 keeps guarding the future. <strong>Module case:</strong> one mechanical commit lowercasing the 251 <code>module:</code> values; add a task-lint rule (next free FM id per the rubric's numbering, documented in RUBRIC.md in the same change per the lint-and-rubric-ship-together discipline) asserting <code>module:</code> is lowercase AND equals the containing <code>docs/tasks/&lt;module&gt;/</code> folder name; regen + status hub verified unchanged (the field now matches what the folder always said). <strong>Triage:</strong> run <code>task-reconcile</code> per stuck task, attach each evidence report to the triage record, halt at Gate 2 for per-task verdicts, and apply exactly the operator's verdicts through the standard paths. <strong>Guard:</strong> <code>scripts/tests/test_corpus_hygiene.sh</code> pins the end states - zero non-draft markers, zero module-case mismatches, lint rule fires on fixtures - and rides <code>run_all.sh</code>'s glob so regression is loud.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Author picks the marker branch (bulk-clear, it's cheaper).</strong> Rejected: the plan's approval boundary explicitly reserves \"the T9 UNREVIEWED bulk decision\" as an operator fork; a batch acceptance of compliance fields is a liability posture only the operator can adopt.</li><li><strong>Silently drop the markers as lint noise.</strong> Rejected: the marker IS the record that a human never confirmed the fields; deleting it without a verdict manufactures confirmation - the exact laundering FM-112 exists to prevent.</li><li><strong>Make the module-case rule warning-severity.</strong> Rejected: after the normalization commit the corpus is 100% conformant, so error-severity costs nothing and prevents re-drift; a warning would be the always-ignorable kind.</li><li><strong>Batch verdict for the 12 stuck tasks (\"route all back\").</strong> Rejected: the set is heterogeneous - TASK-APP-001 is nine days old and plausibly live; the OBS/MCP eleven may have real partial implementations the evidence ladder will surface. Twelve two-minute decisions beat one wrong batch decision.</li><li><strong>Build the stuck-WIP sentinel (auto-detection) in this task.</strong> Rejected: roadmap v3.x per the audit; this task clears the existing debt manually and leaves automated detection to its own spec (G13 in TASK-IMP-140 defines the gate; the hub work is future).</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release - zero <code># UNREVIEWED</code> markers on non-draft specs (from 170 files / 336 markers), zero mixed-case <code>module:</code> values (from 251), the new lint rule live in task-lint + RUBRIC.md, and all 12 formerly-stuck tasks carrying a dated per-task verdict record with statuses reflecting those verdicts. Baselines as measured 2026-07-23.</li><li>Guardrail: <code>python3 scripts/migrate_improvement_to_task.py --backlog</code> output is byte-stable across the normalization (module grouping comes from folders, which do not change), and no task's <code>status</code> changes except through a recorded Gate-2 verdict.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the marker enumeration + fork execution, the 251-file normalization, the lint rule + rubric documentation, the 12 reconcile runs + verdict application, the hygiene test suite, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Re-auditing the CONTENT of any done task beyond the two compliance fields (Branch re-audit confirms fields, not whole specs).</li><li>The automated stuck-WIP sentinel on the status hub (v3.x; G13's definition lives in TASK-IMP-140).</li><li>Changing FM-112's semantics or the draft-gate rules (TASK-IMP-108 owns status semantics).</li><li>The <code>memory.status_overridden</code> emission mechanics - TASK-CUO-303; Gate-2 verdicts use whatever recorded-override path exists when this task runs.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Related: TASK-IMP-100/101 (task-reconcile, the Gate-2 instrument), TASK-IMP-108 (status semantics + FM-115/116 the reconcile verdicts may set), TASK-IMP-117 (the FM-001 conformance precedent for corpus-wide frontmatter sweeps - the module-case normalization follows its mechanical-commit pattern).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> the 170/336/151 marker census, the 251 module-case census, and the 12-task list with creation dates were measured by script against the working tree at authoring time (the audit's 167/148 and \"6 MCP\" figures are corrected to measured values, discrepancies recorded in source_pages); no spec's status or content was modified during authoring.</li><li><strong>Human review:</strong> the hardening plan was operator-approved 2026-07-23 with the UNREVIEWED fork explicitly reserved; both gates in this spec implement that reservation.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The marker disposition MUST NOT begin until a dated operator verdict selecting Branch clear or Branch re-audit is recorded on this spec; the enumeration of affected files (mechanically derived, 170 at authoring) MUST be attached to the verdict so the operator approves a known set, not a description.</li><li>1.2 After the chosen branch executes, zero non-draft spec.md files may contain <code># UNREVIEWED</code>, and the confirmation trail MUST exist: Branch clear = the bulk verdict text in commit body + CHANGELOG; Branch re-audit = per-file confirmation in each file's audit record. Draft specs keep their markers (FM-112 permits them there; they are honest).</li><li>1.3 All <code>module:</code> frontmatter values MUST be lowercase and equal to the containing <code>docs/tasks/&lt;module&gt;/</code> folder name, via one mechanical normalization commit (251 files at authoring).</li><li>1.4 <code>task-lint.mjs</code> MUST gain an error-severity rule (next free FM id) enforcing 1.3's invariant, and <code>modules/skill/task-audit/RUBRIC.md</code> MUST document the rule in the same change - the lint and the rubric ship together per the authoring discipline.</li><li>1.5 Each of the 12 <code>implementing</code> tasks MUST receive a <code>task-reconcile</code> evidence report, and any status change MUST be applied only per a recorded per-task operator verdict (resume / route back / on_hold) through the standard override path. This task MUST NOT flip any status on its own authority, and the triage record MUST cover all 12 (a verdict may legitimately be \"resume unchanged\").</li><li>1.6 A new suite <code>scripts/tests/test_corpus_hygiene.sh</code> MUST assert: zero non-draft markers corpus-wide; zero module-case mismatches; the lint rule fires on a mixed-case fixture and on a folder-mismatch fixture and passes on a conformant one; and the backlog regenerator is byte-stable across a re-run (idempotence guard). It registers via the <code>run_all.sh</code> glob.</li><li>1.7 <code>CHANGELOG.md</code> MUST record the chosen marker branch, the normalization count, the new lint rule id, and the triage outcome summary (n resumed / n routed back / n held).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - the spec carries the dated fork verdict with the attached enumeration BEFORE any marker-touching commit (verified in spec + git history at review) - test: <code>scripts/tests/test_corpus_hygiene.sh::t01_fork_verdict_recorded</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>grep -rl '# UNREVIEWED' docs/tasks/**/spec.md</code> intersected with non-draft statuses returns empty, and the branch-appropriate confirmation trail exists (bulk verdict text present, or per-file audit confirmations) - test: <code>scripts/tests/test_corpus_hygiene.sh::t02_no_nondraft_markers</code></li><li>[ ] AC 3 (traces_to: #1.3) - a corpus scan finds zero <code>module:</code> values that are non-lowercase or unequal to their folder name - test: <code>scripts/tests/test_corpus_hygiene.sh::t03_module_case_conformant</code></li><li>[ ] AC 4 (traces_to: #1.4) - task-lint exits non-zero naming the new rule id on a mixed-case fixture and on a folder-mismatch fixture, exits 0 on a conformant fixture, and RUBRIC.md documents the id - test: <code>scripts/tests/test_corpus_hygiene.sh::t04_lint_rule_fires</code></li><li>[ ] AC 5 (traces_to: #1.5) - twelve reconcile evidence reports exist, each stuck task has a dated verdict record, every status change in the triage commit maps 1:1 to a verdict, and no other task's status changed - test: <code>scripts/tests/test_corpus_hygiene.sh::t05_triage_verdict_per_task</code></li><li>[ ] AC 6 (traces_to: #1.6) - the suite runs green under <code>bash scripts/tests/run_all.sh</code> discovery and the regenerator produces byte-identical BACKLOG.md on consecutive runs post-normalization - test: <code>scripts/tests/test_corpus_hygiene.sh::t06_registered_and_idempotent</code></li><li>[ ] AC 7 (traces_to: #1.7) - CHANGELOG's top entry names the branch, the count 251, the rule id, and the triage tally - test: <code>scripts/tests/test_corpus_hygiene.sh::t07_changelog_records_hygiene</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>New UNREVIEWED files accrue between authoring and implementation:</strong> the enumeration is re-derived at Gate 1 (the 170 is the authoring-time census, not a frozen list); the verdict covers the re-derived set, which is why 1.1 requires attaching it.</li><li><strong>A file is BOTH mixed-case and marker-bearing:</strong> the normalization commit is field-mechanical and marker-preserving; order is normalization first (no judgment), markers second (gated) - so the mechanical half never waits on the fork.</li><li><strong><code>_archive/</code> and <code>_audits/</code> trees:</strong> the census and the sweeps scope to <code>docs/tasks/*/TASK-*/spec.md</code> exactly as the layout suite does; archived flat files are historical record and are not rewritten.</li><li><strong>A stuck task's reconcile evidence shows it actually FINISHED (code shipped, status never advanced):</strong> the ladder surfaces it; the verdict is still the operator's - likely a forward flip through the standard gates with evidence attached, not a silent correction. The spec deliberately does not enumerate verdict outcomes beyond the three canonical ones plus \"resume unchanged\".</li><li><strong>Branch re-audit finds a genuinely wrong risk class on a done task:</strong> correcting the field is in scope (that is the point of the branch); anything larger it uncovers routes to a new task rather than scope-creeping this one.</li><li><strong>Case-only rename collisions on case-insensitive filesystems:</strong> no file renames occur - only frontmatter VALUES change; folder names were already lowercase, so APFS/NTFS case folding is never exercised.</li><li><strong>Security-class:</strong> text-only corpus edits + read-only evidence tooling; the compliance fields' VALUES change only under recorded human verdicts; no execution surface. The lint rule reduces spoofing surface (a task claiming module <code>AUTH</code> while living in <code>improvement/</code> now fails loudly).</li></ul>";
