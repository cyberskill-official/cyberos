window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-303"]="<h1>TASK-CUO-303: Mechanical HITL lock - verdict-gated flips + memory.status_overridden</h1>\n<h2>Summary</h2>\n<p>The two human-acceptance gates (<code>reviewing -&gt; ready_to_test</code>, <code>testing -&gt; done</code>) are doctrine in STATUS-REFERENCE §1.4 and EXECUTION-DISCIPLINE §2a, but nothing mechanical refuses them: <code>backlog-mutate.mjs</code> flips any enum-legal transition, the promised <code>memory.status_overridden</code> audit row is unwritable by the doc-driven appender (closed 4-kind list), and <code>HITL_REQUIRED=\"true\"</code> in <code>gates.env</code> is read by nothing. An agent that ignores the prompt can self-approve its own work end to end. This task adds a verdict gate to the single backlog write path, adds the missing audit-row kind, and removes the dead flag.</p>\n<h2>Problem</h2>\n<p>Audit finding C2, verified first-hand on 2026-07-23:</p>\n<ol><li><strong>No transition lock.</strong> <code>cmdFlip</code> (<code>backlog-mutate.mjs:277</code>) enforces the status enum, a byte pre-image, and truth-precedes-index (TASK-IMP-120) - all integrity checks, none of them authority checks. The two transitions that doctrine reserves for a recorded human verdict flip exactly like any other.</li><li><strong>The promised audit row cannot exist.</strong> STATUS-REFERENCE §1.4 says every human verdict or override emits one <code>memory.status_overridden</code> aux row. <code>memory-append.mjs</code> - the only writer available to doc-driven (non-Python) workflows - refuses every kind outside its closed four (<code>memory-append.mjs:102</code>). The doctrine promises an audit trail the tooling cannot produce.</li><li><strong>A dead flag implies enforcement that does not exist.</strong> <code>install.sh:319</code> writes <code>HITL_REQUIRED=\"true\"</code> into every <code>gates.env</code>; nothing reads it. A reader auditing the machine sees a flag named like a lock and reasonably concludes one exists.</li></ol>\n<h2>Proposed Solution</h2>\n<p>Gate the two human-acceptance transitions in <code>cmdFlip</code> behind two new required flags: <code>--verdict-by &lt;actor&gt;</code> (non-empty identity string) and <code>--verdict-evidence &lt;path&gt;</code> (an existing, non-empty file - the review note, test-acceptance note, or transcript the human produced). A flip of <code>reviewing -&gt; ready_to_test</code> or <code>testing -&gt; done</code> without both flags refuses with a new distinct exit code 8 and a message quoting STATUS-REFERENCE §1.4; all other transitions are untouched. When the flags are present, the flip proceeds and additionally appends one <code>memory.status_overridden</code> row (payload <code>{actor, task_id, prior_status, new_status, reason: evidence-path}</code>) via <code>memory-append.mjs</code> when a BRAIN store is resolvable; when no store exists, the evidence file itself is the record and the flip still succeeds - append-failure on a <em>present</em> store, however, fails the flip (audit-before-action). Extend <code>memory-append.mjs</code>'s closed kind list with <code>status_overridden</code> and its payload validation. Remove the dead <code>HITL_REQUIRED</code> variable from the <code>gates.env</code> generator, keeping the prose comment about the two human gates. Update <code>ship-tasks.md</code>'s HITL step descriptions to pass the new flags, and add a CHANGELOG entry marking the new refusal as breaking for any tooling that flips the two gate transitions bare.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Consume <code>HITL_REQUIRED</code> (make the lock conditional on it).</strong> Rejected: the lock is doctrine, not configuration - EXECUTION-DISCIPLINE §2a governs platform-wide and offers no opt-out; a flag that looks like it can disable the lock by editing a gitignored file would replace a dead lie with a live one.</li><li><strong>Verify the verdict actor is human (signature, identity attestation).</strong> Rejected for this task: signed/attributed verdict artifacts are the 1.4.x roadmap item the audit already names. This task makes the verdict <em>recorded and refusable</em>, not cryptographically attributable - the honest increment that closes the self-approval path for compliant tooling.</li><li><strong>Lock the transitions in the spec frontmatter instead (a pre-commit hook rejecting status edits).</strong> Rejected: frontmatter is written by humans and agents alike in editors; a commit-time reject fires after the work is staged, refuses legitimate operator overrides, and cannot capture WHO decided. The flip executor is where authority is asserted (TASK-CUO-205 made it the single write path); guarding there is both sufficient for tooling and non-intrusive for operators.</li><li><strong>Auto-generate the evidence file when absent.</strong> Rejected: an auto-generated verdict is a self-approval with paperwork; the entire point is that the file preexists the flip because a human produced it.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release, a bare <code>backlog-mutate flip &lt;id&gt; reviewing ready_to_test</code> (or <code>testing done</code>) exits 8 with no file written, and the same flip with <code>--verdict-by</code> + <code>--verdict-evidence</code> succeeds and (when a store is present) lands exactly one <code>memory.status_overridden</code> row on the chain. Baseline today: the bare flip succeeds silently.</li><li>Guardrail: zero behavior change for every other transition - the existing backlog-mutate coverage in <code>tools/install/tests/test_workflow_helpers.sh</code> and the mechanical spine in <code>tools/install/tests/test_e2e_skeleton.sh</code> pass unmodified (except where the e2e drives the two gate transitions, which gains the flags), and rework/off-ramp flips (<code>-&gt; ready_to_implement</code>, <code>-&gt; on_hold</code>, <code>-&gt; closed</code>) require no verdict flags.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>cmdFlip</code> verdict gate + exit code 8, <code>memory-append.mjs</code> kind extension + payload validation, <code>install.sh</code> gates.env generation (drop the dead variable, keep the prose), <code>ship-tasks.md</code> HITL step invocation updates, CHANGELOG entry, and the new test suite.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Cryptographic signing or identity verification of verdict actors (1.4.x roadmap; this task records, it does not attest).</li><li>Locking direct frontmatter edits or the <code>regen</code> path - an agent editing <code>spec.md</code> by hand bypasses any tool gate; that residual is explicitly accepted and documented in ship-tasks.md, with the transition-locked state engine (1.5.0 roadmap on the 1.x line) as the full closure. The G2 benchmark checker (TASK-IMP-140) asserts the tool-path refusal this task ships.</li><li>The route-back ceiling constant (TASK-CUO-304) and gate-floor behavior (TASK-CUO-302) - separate tasks in this batch.</li><li>Retroactively generating <code>status_overridden</code> rows for historical transitions.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Builds on TASK-CUO-205 (done - made <code>backlog-mutate.mjs</code> the single documented backlog write path, which is what gives a lock in this tool its force) and TASK-IMP-120 (done - truth-precedes-index; this task's gate runs AFTER those refusals so the refusal precedence is: missing row / drift / truth-mismatch first, verdict gate last). TASK-IMP-140's benchmark gate G2 verifies this task's refusal in CI - soft forward reference via <code>related_tasks</code>, no cycle.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> every <code>source_pages</code> line was read at HEAD in this checkout during authoring; the absence of any <code>HITL_REQUIRED</code> consumer and the closed kind list were verified by repo-wide grep, not carried from the audit report.</li><li><strong>Human review:</strong> the hardening plan (including this task's scope bullet) was operator-approved on 2026-07-23; the remove-not-consume decision for <code>HITL_REQUIRED</code> is recorded in <code>source_decisions</code> for the reviewer to revisit at the review acceptance gate.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>backlog-mutate.mjs flip</code> MUST refuse the transitions <code>reviewing -&gt; ready_to_test</code> and <code>testing -&gt; done</code> unless BOTH <code>--verdict-by &lt;actor&gt;</code> (non-empty string) and <code>--verdict-evidence &lt;path&gt;</code> (a path that exists and is a non-empty regular file at flip time) are supplied. The refusal MUST use the new distinct exit code 8 (verdict required), MUST name STATUS-REFERENCE §1.4 in its message, and MUST NOT write any file. Every other transition MUST behave exactly as today, verdict flags ignored if supplied.</li><li>1.2 The verdict gate MUST evaluate AFTER the existing refusals (missing/duplicate row exit 6, pre-image drift exit 6, truth-precedes-index exit 6), so existing failure modes keep their codes and the new code 8 means exactly one thing: the transition was otherwise legal but no verdict was recorded.</li><li>1.3 <code>memory-append.mjs</code> MUST accept the kind <code>status_overridden</code> (emitted on-chain as op <code>status_overridden</code>, consistent with the existing four kinds), validating a payload object with required non-empty string fields <code>actor</code>, <code>task_id</code>, <code>prior_status</code>, <code>new_status</code>, <code>reason</code>. Unknown kinds MUST keep today's refusal behavior.</li><li>1.4 On a verdict-gated flip where a BRAIN store is resolvable (the same store-resolution the appender already implements), the flip MUST append exactly one <code>status_overridden</code> row whose payload carries <code>{actor: &lt;--verdict-by&gt;, task_id, prior_status, new_status, reason: &lt;--verdict-evidence path&gt;}</code>, and a failed append on a present store MUST fail the flip (audit-before-action: no index move without its audit row). When no store is resolvable, the flip MUST succeed without a row - the evidence file is the record - and MUST say so on stderr.</li><li>1.5 <code>install.sh</code> MUST stop emitting the <code>HITL_REQUIRED</code> variable into generated <code>gates.env</code> files, keeping the prose comment that states the two human-acceptance gates are never automated. No script consumes the variable today (verified 2026-07-23), so removal changes no behavior.</li><li>1.6 <code>ship-tasks.md</code>'s two HITL steps MUST document the flag-carrying flip invocation as the way the recorded human verdict advances the cell, and <code>CHANGELOG.md</code> MUST gain an entry marking the bare-flip refusal as a breaking change for tooling that automates the two gate transitions.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - bare <code>flip &lt;id&gt; reviewing ready_to_test</code> and <code>flip &lt;id&gt; testing done</code> exit 8, mention \"STATUS-REFERENCE\" and \"verdict\", and leave BACKLOG.md byte-identical; the same flips with both flags succeed; <code>flip &lt;id&gt; testing ready_to_implement</code> (route-back) needs no flags - test: <code>tools/install/tests/test_hitl_lock.sh::t01_bare_gate_flip_refused</code></li><li>[ ] AC 2 (traces_to: #1.1) - <code>--verdict-evidence</code> pointing at a missing path or an empty file refuses with exit 8 and no write; <code>--verdict-by \"\"</code> refuses with exit 8 - test: <code>tools/install/tests/test_hitl_lock.sh::t02_evidence_must_exist_nonempty</code></li><li>[ ] AC 3 (traces_to: #1.2) - on a row whose cell drifted from the recorded pre-image AND with verdict flags absent, the exit is 6 (pre-image) not 8, proving refusal precedence - test: <code>tools/install/tests/test_hitl_lock.sh::t03_refusal_precedence_six_before_eight</code></li><li>[ ] AC 4 (traces_to: #1.3) - <code>memory-append.mjs append &lt;store&gt; status_overridden</code> with a complete payload appends a chained row; each missing/empty required field refuses with exit 2 and writes nothing; an unknown kind still refuses - test: <code>tools/install/tests/test_hitl_lock.sh::t04_status_overridden_kind_validated</code></li><li>[ ] AC 5 (traces_to: #1.4) - a verdict-gated flip against a scratch repo WITH a seeded store lands exactly one <code>status_overridden</code> row (payload fields match the flags); with the store made unwritable the flip fails and BACKLOG.md is unchanged; with NO store the flip succeeds, no row, stderr notes the evidence file is the record - test: <code>tools/install/tests/test_hitl_lock.sh::t05_audit_before_action</code></li><li>[ ] AC 6 (traces_to: #1.5) - a scratch install's generated <code>gates.env</code> contains no <code>HITL_REQUIRED</code> substring and retains the human-gates prose comment - test: <code>tools/install/tests/test_hitl_lock.sh::t06_dead_flag_removed</code></li><li>[ ] AC 7 (traces_to: #1.6) - <code>ship-tasks.md</code> documents <code>--verdict-by</code> and <code>--verdict-evidence</code> at both HITL steps, and CHANGELOG's top entry mentions the refusal, the word \"breaking\", and exit code 8 - test: <code>tools/install/tests/test_hitl_lock.sh::t07_docs_and_changelog</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>Operator superset overrides (STATUS-REFERENCE §1.4)</strong> - e.g. <code>done -&gt; ready_to_review</code> re-audit, <code>ready_to_review -&gt; ready_to_test</code> skip-review - are NOT the two forward gate transitions and stay flag-free in this task. Widening verdict recording to all overrides is deliberate future scope; this task locks exactly the two transitions doctrine names as mandatory-human.</li><li><strong>Direct frontmatter edit + regen bypass:</strong> an agent can write <code>status: done</code> into spec.md and regenerate the backlog without ever calling <code>flip</code>. Accepted residual, stated in ship-tasks.md: the lock closes the documented tool path (the only path compliant workflows use); the 1.5.0 state engine closes the rest. The G2 checker tests the tool path.</li><li><strong>Evidence file is a directory or unreadable:</strong> treated as \"does not exist\" - refusal 8. The gate checks regular-file-ness and non-zero size, nothing else; content quality is the reviewer's judgment, not the tool's.</li><li><strong><code>--json</code> output mode:</strong> the refusal and the success MUST both carry the verdict fields in the JSON envelope so ship-manifest consumers can record them; the exit code is authoritative either way.</li><li><strong>Two flips racing on the same row:</strong> unchanged from today - the pre-image/optimistic-concurrency refusal (exit 6) fires before the verdict gate (AC 3's precedence), so the race loser cannot consume a verdict.</li><li><strong>Security-class:</strong> the verdict flags introduce no new execution surface (no eval, no shell-out); the evidence path is read for existence/size only, never executed or parsed. The appended row goes through the existing appender's §4.2 lock + two-phase write discipline.</li></ul>";
