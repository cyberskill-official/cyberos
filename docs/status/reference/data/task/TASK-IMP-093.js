window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-093"]="<h1>TASK-IMP-093: memory-append CLI, doc-driven runs can append chained BRAIN rows</h1>\n<h2>Summary</h2>\n<p>Every ship-tasks phase declares memory rows (workflow_phase_complete, workflow_complete, task_routed_back, artefact_write), but without the MCP writer a doc-driven run has nowhere to append them - the real consumer run parked payloads in a tracked _audits file instead of the chain. Ship <code>memory-append.mjs</code> in docs-tools: a minimal, protocol-honoring appender for exactly those four kinds, with a verify mode, so governed runs can keep the chain truthful from any environment that has node.</p>\n<h2>Problem</h2>\n<p>The BRAIN's value is the chain; rows that live in a parking file have no prev_chain, no HEAD ordinal, and no tamper evidence. The protocol is written and vendored - what is missing is a writer small enough to run inside a doc-driven session.</p>\n<h2>Proposed Solution</h2>\n<p><code>node .cyberos/docs-tools/memory-append.mjs append &lt;store-root&gt; &lt;kind&gt; &lt;payload.json|-&gt;</code> and <code>... verify &lt;store-root&gt;</code>. Append: acquire the store lock, read HEAD (8-byte LE u64), build the record (kind, payload, actor, at, prev_chain), compute <code>chain = SHA-256(canonical(record_minus_chain) || prev_chain)</code> per §6.3, two-phase-write the row file and the advanced HEAD per §4.1 (tmp + rename + fsync; the Darwin F_BARRIERFSYNC caveat documented in-file - node exposes fsync only), release the lock. A fresh store bootstraps HEAD=0 with a null prev_chain root. Verify: walk the rows, recompute every chain link, compare the tip to HEAD. Kinds outside the four workflow kinds are refused before any write. build.sh gains the guarded vendor copy alongside its three helper siblings.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Wire the full MCP memory server into doc-driven runs. Rejected: the server carries put_if, episodes, and checkpoints - far more surface than a run needs; a session without MCP still needs the append path.</li><li>Keep parking rows in _audits. Rejected: it is chain-shaped data with no chain; the 086 incident showed what unverifiable records cost.</li><li>Python instead of node. Rejected: docs-tools is node-stdlib by convention and node is the one runtime install already requires for the status page.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: three appends to a fresh scratch store advance HEAD by exactly 3 and verify recomputes the full chain clean, on every suite run. Baseline: zero doc-driven appends possible (rows parked in _audits). Deadline: final acceptance.</li><li>Guardrail: a tampered row byte makes verify exit non-zero naming the ordinal (chain property, not just a counter).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the two subcommands, the four kinds, fresh-store bootstrap, the suite, the build.sh vendor line.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>put_if, episodes, checkpoints, signed tree heads (§7) - the MCP server's domain.</li><li>Migrating the parked sachviet rows (operator choice later; the tool makes it possible).</li><li>Concurrent multi-writer arbitration beyond the protocol's lock file.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None upstream. Shares tools/install/build.sh with TASK-IMP-098 - same agent, serial order per the batch plan.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the vendored memory protocol and IMP-05; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-4 PLAN approved 2026-07-17 (§0a); both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The CLI MUST implement <code>append &lt;store-root&gt; &lt;kind&gt; &lt;payload.json|-&gt;</code>: lock, read HEAD (8-byte LE u64), build the record with prev_chain = current tip, compute chain per §6.3 (SHA-256 over canonical record-minus-chain concatenated with prev_chain), two-phase-write row and HEAD per §4.1 (tmp + rename + fsync; Darwin caveat documented in-file), unlock.</li><li>1.2 Kinds MUST be restricted to workflow_phase_complete, workflow_complete, task_routed_back, artefact_write; any other kind is refused with a non-zero exit BEFORE any write.</li><li>1.3 A fresh store (no HEAD) MUST bootstrap deterministically: HEAD=0, null-root prev_chain, directories created; a second append then chains normally.</li><li>1.4 The CLI MUST implement <code>verify &lt;store-root&gt;</code>: recompute every link, compare the tip against HEAD, exit non-zero naming the first bad ordinal on mismatch.</li><li>1.5 build.sh MUST vendor the file with the same guarded pattern as its helper siblings, and the payload copy MUST be gated in the suite against a scratch build.</li><li>1.6 The suite MUST land at tools/install/tests/test_memory_append.sh (run_all glob discovery).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - fresh store, three appends, HEAD advances by 3, rows chained - test: <code>tools/install/tests/test_memory_append.sh::t01_fresh_store_three_appends</code></li><li>[ ] AC 2 (traces_to: #1.4) - verify passes clean and fails on a tampered byte naming the ordinal - test: <code>tools/install/tests/test_memory_append.sh::t02_verify_and_tamper</code></li><li>[ ] AC 3 (traces_to: #1.2) - malformed kind refused, store byte-untouched - test: <code>tools/install/tests/test_memory_append.sh::t03_bad_kind_refused</code></li><li>[ ] AC 4 (traces_to: #1.5) - scratch payload carries the tool - test: <code>tools/install/tests/test_memory_append.sh::t04_payload_vendored</code></li><li>[ ] AC 5 (traces_to: #1.6) - suite discovered by the run_all glob - verify: runner output lists the suite (ops check recorded in the gate log; glob discovery is the runner's contract).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Interrupted append (tmp file present, rename never ran): next append MUST ignore/clean stale tmp files; HEAD and chain stay consistent (asserted inside t01 via a seeded stale tmp).</li><li>Payload on stdin (<code>-</code>) vs file: both accepted; non-JSON payload refused before write (t03 arm).</li><li>Concurrent append attempt while locked: second invocation fails fast with a clear message rather than corrupting (t02 arm with a held lock).</li><li>Security-class: writes only under the given store root; no network, no secrets; refuses paths escaping the store root.</li></ul>";
