window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-115"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>This task catalogues, classifies, and substitutes the stale <code>&lt;placeholder&gt;</code> syntax that survives in 134 production SKILL.md frontmatter fields across <code>modules/skill/</code>. These placeholders were inherited from earlier scaffold runs (pre-2026-05-19 template work) and never substituted with real values. They block Anthropic-host portability (Phase B transpilers fail on Reference B's bracket prohibition) and degrade operator UX in audit + runtime error messages.</p>\n<ol><li>The detection script <code>tools/sweep-placeholders/detect.py</code> <strong>MUST</strong> walk every <code>SKILL.md</code> under <code>modules/skill/</code>, parse the YAML frontmatter, and identify every field whose value contains a literal <code>&lt;word&gt;</code> pattern that is <strong>NOT</strong> the now-corrected <code>wrap_in_marker:</code> (per TASK-SKILL-113). Output: a structured JSON dump grouped by skill_path, field_path, and placeholder_token.</li><li>Known stale-placeholder fields (from the 2026-05-19 sweep audit) <strong>MUST</strong> be covered: <code>metadata.stage</code> (134 hits — <code>&lt;SDP §2 stage letter or \"cross\"&gt;</code>), <code>description</code> (28 hits — <code>&lt;input&gt;</code>, <code>&lt;artifact&gt;</code> etc.), <code>allowed_memory_scopes.write[*]</code> (16 hits — <code>&lt;scope-glob&gt;</code>), <code>name</code> (2 hits — <code>&lt;artifact&gt;-author</code>/<code>&lt;artifact&gt;-audit</code> in <code>_template/*</code> scaffolds only), and <code>depends_on_contracts[*].{id, pin_path}</code> (4 hits — <code>&lt;artifact&gt;</code>).</li><li>The substitution engine <code>tools/sweep-placeholders/suggest.py</code> <strong>MUST</strong> propose a concrete substitution per placeholder by reading: (a) the skill's <code>SKILL.md</code> body's CONTRACT_ECHO block (which often names the real artefact type), (b) <code>STANDALONE_INTERVIEW.md</code> (which lists real input field names), (c) <code>references/MANIFEST_SCHEMA.md</code> (which carries the real artefact id), (d) the persona's <code>MODULE.md</code> entry (which lists the SDP stage letter). Suggestions are advisory; operator review is mandatory.</li><li>The substitution engine <strong>MUST NOT</strong> auto-apply suggestions. The right substitute depends on operator domain knowledge that the engine can approximate but not guarantee. The engine emits a single <code>tools/sweep-placeholders/report.md</code> listing every skill + every placeholder + the recommended fix; operator reviews + edits the report; the sweep step then applies the operator-approved values.</li><li>The auditor rule <strong>MUST</strong> be <code>SKB-030 placeholder-free-frontmatter</code> (added to <code>SKILL_BUNDLE_RUBRIC.md</code>) with severity <code>warning</code> for <code>status: draft</code> skills and severity <code>error</code> for <code>status: accepted</code> or higher. Auto-fix: never (operator-attestation required).</li><li>The Python validator <code>cuo.placeholder_check.scan(skill_path) -&gt; ScanResult</code> <strong>MUST</strong> return a list of <code>PlaceholderHit(field_path: str, value: str, suggested_substitution: str | None)</code>. The CI gate runs <code>python -m cuo.placeholder_check --catalog modules/skill/</code> and exits non-zero if any production skill has hits.</li><li>The sweep timing <strong>SHOULD</strong> be done in persona-grouped batches rather than one mega-commit (134 files in one commit makes diff review impractical). Recommended batches: P0 personas first (cpo, cto — ~12 skills), then P1 personas (ceo, coo, cfo, chro, cseco, clo, caio — ~30 skills), then P2+ in order. Each batch ships as a separate commit with <code>chore(skill): SKB-030 sweep — &lt;persona-name&gt; (&lt;N&gt; skills)</code> message.</li><li>The body XML form <code>&lt;untrusted_content source=\"...\" page=\"...\"&gt;…&lt;/untrusted_content&gt;</code> <strong>MUST NOT</strong> be touched anywhere. SKB-030 fires only on frontmatter values; body markup is preserved per TASK-SKILL-113 §1 #3.</li><li>Each per-skill substitution <strong>MUST</strong> preserve the SKILL.md's audit hash chain compatibility. The substitution is a frontmatter-only edit; body bytes are unchanged. Where a fine-tune signal triggers as a side effect (e.g. <code>skill_version</code> MINOR bump per AGENTS.md §11), the operator follows the standard fine-tune cycle.</li><li>Per-skill substitutions <strong>MUST</strong> be operator-attested via a <code>chore(skill):</code> commit message containing a one-line rationale per field (e.g. <code>metadata.stage: 'b' (SDP §2 Requirements — body §3 PLAN phase references stage b)</code>).</li><li>The CI gate <strong>MUST</strong> run <code>cuo.placeholder_check --catalog modules/skill/ --fail-on-error</code> on every PR after the sweep is complete. Until the sweep finishes, the gate runs as <code>--fail-on-error-status-accepted-only</code> so scaffold/draft skills don't block PRs.</li><li>Pre-existing exceptions: the <code>_template/author/SKILL.md</code> and <code>_template/audit/SKILL.md</code> files are intentional scaffolds and <strong>MUST</strong> retain their <code>&lt;artifact&gt;</code> placeholders (they're literal substitution tokens for <code>cp</code> operations, not real values). The detector exempts paths under <code>_template/</code>.</li><li>The post-sweep verify script <code>tools/sweep-placeholders/verify.py</code> <strong>MUST</strong> assert: zero stale-placeholder hits in any non-<code>_template/</code> SKILL.md; the placeholder_check validator agrees; every modified file still parses as valid YAML.</li><li>The sweep report <strong>MUST</strong> be committed alongside the substitutions (lives at <code>tools/sweep-placeholders/report-&lt;YYYY-MM-DD&gt;.md</code>). Future operators reviewing the sweep can reconstruct the decision chain.</li><li>Registry version: this task is a v0.2.6 increment (post-v0.2.5 introduced by TASK-SKILL-113). The bump is documented in the repo-root <code>CHANGELOG.md</code> <code>[SKILL]</code> section.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a separate task rather than fold into TASK-SKILL-113 (§1 #1)?</strong> TASK-SKILL-113 specifically migrated the <code>wrap_in</code> field via a mechanical pattern match. The stale placeholders span DOZENS of distinct fields with NO common pattern. Each one needs operator domain knowledge to substitute correctly. Bundling the work into 113 would have either inflated 113 from 12h to 28h (wrong scope grouping) or shipped 113 incomplete (leaving 134 portability bugs). Separating the tasks honours the audit-fix-audit discipline — 113 closed one specific bug class; 115 closes the residual.</p>\n<p><strong>Why operator-attested rather than auto-substituted (§1 #4)?</strong> Each placeholder asks a domain question. <code>metadata.stage: &lt;SDP §2 stage letter or \"cross\"&gt;</code> — is this skill stage b (requirements), c (design), e (delivery), or cross-cutting? The right answer is in the skill's body, but extracting it requires understanding what the skill actually does. A pattern-matching auto-substituter would get this wrong 30-50% of the time (the body sometimes mentions multiple stages; some skills genuinely span multiple stages and should be \"cross\"). Operator review at 3.5 min/skill × 134 skills = 8 hours is the right tradeoff.</p>\n<p><strong>Why suggestions rather than fully manual (§1 #3)?</strong> Pure-manual would cost 15-20 min/skill × 134 = 30-45 hours. With suggestions, operator reads the suggestion + verifies via body skim (3-5 min/skill). 4× speedup with negligible correctness loss because the suggestion engine cites its sources (CONTRACT_ECHO, MANIFEST_SCHEMA, MODULE.md) — operator verifies by skim, not by re-derivation.</p>\n<p><strong>Why severity warning on draft + error on accepted (§1 #5)?</strong> Drafting is iterative; forcing the rule on every draft commit would slow first-pass authoring. Production skills are routed by the supervisor and shipped to hosts — they MUST conform. Same severity scheme as SKB-020..023 + SKB-050..057 (TASK-SKILL-111 + 112) for consistency.</p>\n<p><strong>Why persona-grouped batches (§1 #7)?</strong> Three reasons. (1) Diff review: a 134-file commit is unreviewable; 12-30 file batches are. (2) Risk isolation: a mistake in one batch is fixable without rolling back the rest. (3) Schedule fit: persona owners can review their own persona's sweep — distributes the review load.</p>\n<p><strong>Why body XML preserved (§1 #8)?</strong> Restates TASK-SKILL-113's invariant. The body XML is the runtime wrapper; the frontmatter is the declaration. Touching body XML would break wrapping semantics for every production skill that processes external bytes (i.e. all of them).</p>\n<p><strong>Why operator-attested commit messages (§1 #10)?</strong> Audit-chain integrity. Six months from now, when an operator wonders \"why does this skill have <code>metadata.stage: c</code> instead of <code>b</code>?\", the commit message has the one-line rationale. Without it, the substitution is unprovenanced — operators have to re-derive the reason from skill body context, defeating the speed-up.</p>\n<p><strong>Why <code>_template/</code> exemption (§1 #12)?</strong> The template files DELIBERATELY carry <code>&lt;artifact&gt;</code> placeholders. They're scaffolds; the placeholders are part of the contract for <code>cp -r _template/author/ &lt;new-skill&gt;/</code> followed by sed-substitution. Sweeping them would break the scaffold-and-substitute workflow.</p>\n<p><strong>Why a v0.2.6 increment (§1 #15)?</strong> Per DEC-182, frontmatter changes that consumers can detect at load time are MINOR-compatible. The placeholder sweep doesn't change schema (no field renames); it just substitutes literal values. Strictly, this is a content edit, not a registry change — but bumping registry to v0.2.6 makes the change traceable in changelogs and signals to downstream consumers that the catalog has been audit-swept.</p>\n<h2>§3 — API contract</h2>\n<h3>Detection script — <code>tools/sweep-placeholders/detect.py</code></h3>\n<pre><code class=\"language-python\">\"\"\"Detect stale &lt;placeholder&gt; syntax in modules/skill/**/SKILL.md frontmatter.\"\"\"\nfrom __future__ import annotations\nimport json\nimport re\nimport sys\nfrom pathlib import Path\nimport yaml\n\nPLACEHOLDER_RE = re.compile(r\"&lt;([a-zA-Z][a-zA-Z0-9_§ /\\\"|.()-]*)&gt;\")\nEXEMPT_PATHS = (\"_template/\",)\n\ndef find_hits_in_value(field_path: str, value, hits: list) -&gt; None:\n    if isinstance(value, str):\n        for m in PLACEHOLDER_RE.finditer(value):\n            tok = m.group(1)\n            # Whitelist &lt;br&gt; only (mermaid line-break in body; never expected in frontmatter)\n            if tok.lower() == \"br\":\n                continue\n            hits.append({\"field\": field_path, \"value\": value[:120], \"token\": tok})\n    elif isinstance(value, dict):\n        for k, v in value.items():\n            find_hits_in_value(f\"{field_path}.{k}\", v, hits)\n    elif isinstance(value, list):\n        for i, v in enumerate(value):\n            find_hits_in_value(f\"{field_path}[{i}]\", v, hits)\n\ndef scan(skill_path: Path) -&gt; dict:\n    text = skill_path.read_text(encoding=\"utf-8\")\n    if not text.startswith(\"---\\n\"):\n        return {\"skill\": str(skill_path), \"hits\": [], \"error\": \"no_frontmatter\"}\n    end = text.index(\"\\n---\\n\", 4)\n    fm = yaml.safe_load(text[4:end])\n    hits: list = []\n    find_hits_in_value(\"root\", fm, hits)\n    return {\"skill\": str(skill_path), \"hits\": hits}\n\ndef main() -&gt; int:\n    catalog_root = Path(\"modules/skill\")\n    results = []\n    for f in sorted(catalog_root.glob(\"**/SKILL.md\")):\n        if any(part in str(f) for part in EXEMPT_PATHS):\n            continue\n        r = scan(f)\n        if r.get(\"hits\"):\n            results.append(r)\n    print(json.dumps({\"total_skills_with_hits\": len(results), \"skills\": results}, indent=2))\n    return 0 if not results else 1\n\nif __name__ == \"__main__\":\n    sys.exit(main())</code></pre>\n<h3>Suggestion engine — <code>tools/sweep-placeholders/suggest.py</code></h3>\n<pre><code class=\"language-python\">\"\"\"Per-skill suggestion engine: propose substitutions based on body + sibling files.\"\"\"\nimport re\nfrom pathlib import Path\n\ndef suggest_for_metadata_stage(skill_path: Path) -&gt; str | None:\n    \"\"\"Read skill body, find SDP stage references, return the most-frequent stage letter.\"\"\"\n    text = (skill_path / \"SKILL.md\").read_text(encoding=\"utf-8\")\n    stages = re.findall(r\"stage[s]?\\s+([a-h])\\b\", text, re.IGNORECASE)\n    if not stages:\n        return None\n    from collections import Counter\n    counts = Counter(s.lower() for s in stages)\n    most_common = counts.most_common(1)[0][0]\n    return most_common\n\ndef suggest_for_description(skill_path: Path, field_value: str) -&gt; str | None:\n    \"\"\"Find &lt;input&gt;, &lt;artifact&gt; placeholders + propose concrete substitutions.\"\"\"\n    # Read CONTRACT_ECHO block for artefact type\n    text = (skill_path / \"SKILL.md\").read_text(encoding=\"utf-8\")\n    m = re.search(r\"template_version:\\s+([a-z][a-z_0-9-]*)@1\", text)\n    artefact = m.group(1) if m else None\n    if not artefact:\n        return None\n    # Substitute &lt;input&gt; → artefact, &lt;artifact&gt; → artefact in description\n    new_value = field_value\n    if \"&lt;input&gt;\" in new_value and artefact:\n        new_value = new_value.replace(\"&lt;input&gt;\", f\"{artefact} source\")\n    if \"&lt;artifact&gt;\" in new_value:\n        new_value = new_value.replace(\"&lt;artifact&gt;\", artefact)\n    return new_value\n\n# Full suggest() function dispatches by field path; per-field heuristics.</code></pre>\n<h3>Auditor rule — addition to <code>SKILL_BUNDLE_RUBRIC.md</code></h3>\n<pre><code class=\"language-markdown\">### SKB-030 — placeholder-free-frontmatter\n\n**Statement:** No SKILL.md frontmatter field value may contain literal placeholder syntax like `&lt;word&gt;` (excluding the explicitly-allowed `&lt;br&gt;` if it ever appears, which it shouldn't in frontmatter). Per TASK-SKILL-115. This is distinct from SKB-040 (no-xml-in-frontmatter, which targets the security boundary); SKB-030 targets the operator-UX + portability boundary.\n\n**Severity:** error on `status: accepted | building | shipped`; warning on `status: draft`. Exempt: any path under `_template/`.\n\n**Auto-fix:** never (operator-attestation required — see TASK-SKILL-115 §1 #4).\n\n**Check:** `python -m cuo.placeholder_check &lt;skill_path&gt;`; exit 0 if clean, exit 1 with structured error otherwise.</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>detect.py finds known stale placeholders</strong> — <code>python3 tools/sweep-placeholders/detect.py</code> on the catalog as-of-2026-05-19 reports exactly the 188 occurrences across 134 files (per the ANTHROPIC_GUIDE_DIGEST.md verification).</li><li><strong>detect.py exempts _template/</strong> — running detect.py reports zero hits in <code>_template/author/SKILL.md</code> or <code>_template/audit/SKILL.md</code> (those are scaffolds with intentional placeholders).</li><li><strong>detect.py whitelists <code>&lt;br&gt;</code></strong> — a <code>&lt;br&gt;</code> in any frontmatter value (theoretical; shouldn't happen but defended) does not register as a hit.</li><li><strong>suggest.py proposes a stage letter</strong> — for <code>task-author</code> (body references SDP §2(b) Requirements), suggest_for_metadata_stage returns <code>'b'</code>.</li><li><strong>suggest.py reads CONTRACT_ECHO for artefact type</strong> — for <code>task-author</code>, the description's <code>&lt;input&gt;</code> → <code>task source</code>.</li><li><strong>placeholder_check.scan returns structured PlaceholderHit list</strong> — given a skill with <code>metadata.stage: &lt;SDP §2 stage letter or \"cross\"&gt;</code>, returns one hit with <code>field_path='root.metadata.stage'</code> + <code>value='&lt;SDP §2 stage letter or \"cross\"&gt;'</code> + <code>suggested_substitution='b'</code> (or None if suggest fails).</li><li><strong>placeholder_check exit code 1 on hits</strong> — running on a skill with stale placeholders returns exit code 1; running on a clean skill returns 0.</li><li><strong>SKB-030 rule fires on production skill with placeholders</strong> — given a <code>status: accepted</code> skill with stale placeholders, the auditor reports one SKB-030 issue (severity error, status needs_human).</li><li><strong>SKB-030 rule fires as warning on draft skill</strong> — same input but <code>status: draft</code> → severity warning.</li><li><strong>Body XML preserved post-sweep</strong> — after running the full sweep, every body's <code>&lt;untrusted_content source=\"...\" page=\"...\"&gt;…&lt;/untrusted_content&gt;</code> markup is unchanged byte-for-byte.</li><li><strong>All 134 production skills are placeholder-free post-sweep</strong> — <code>python3 tools/sweep-placeholders/detect.py</code> exits 0; verify.py confirms.</li><li><strong>Persona-batch commits are reviewable</strong> — each batch commit's diff is ≤30 files; commit message follows <code>chore(skill): SKB-030 sweep — &lt;persona-name&gt; (&lt;N&gt; skills)</code> format.</li><li><strong>Operator-attestation in commit message</strong> — every batch commit includes one-line rationale per field-type substitution (e.g. \"metadata.stage values picked from each skill's body SDP-stage references; cross-cutting skills marked 'cross'\").</li><li><strong>verify.py asserts post-sweep invariants</strong> — running verify.py exits 0; checks (a) detect.py reports zero hits, (b) every modified file parses as valid YAML, (c) every modified file's <code>wrap_in_marker:</code> field is still <code>\"untrusted_content\"</code> (TASK-SKILL-113 invariant preserved), (d) body XML form unchanged via SHA256 comparison.</li><li><strong>CI gate integration</strong> — <code>python -m cuo.placeholder_check --catalog modules/skill/ --fail-on-error</code> runs as part of the existing CUO test suite; PRs touching SKILL.md frontmatter are gated.</li><li><strong>task-audit skill §3.13 rule 38f added</strong> — references TASK-SKILL-115 + SKB-030.</li><li><strong>Registry version bumped</strong> — v0.2.5 → v0.2.6 in CHANGELOG.md <code>[SKILL]</code> section.</li><li><strong>Sweep report committed</strong> — <code>tools/sweep-placeholders/report-2026-05-19.md</code> (or later date) lists every skill + every substitution decision.</li><li><strong>Idempotency check</strong> — running detect.py + sweep + detect.py again yields the same zero-hit state; no flapping.</li><li><strong>Cross-task reciprocity</strong> — TASK-SKILL-113's <code>blocks:</code> list updated to include TASK-SKILL-115; reciprocity sweep passes.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/cuo/tests/test_placeholder_check.py\nimport pytest\nfrom pathlib import Path\nfrom cuo.placeholder_check import scan, PlaceholderHit\n\ndef test_detects_metadata_stage_placeholder(tmp_path):\n    skill = tmp_path / \"SKILL.md\"\n    skill.write_text(\"\"\"---\nname: foo-author\nmetadata:\n  stage: &lt;SDP §2 stage letter or \"cross\"&gt;\nwrap_in_marker: \"untrusted_content\"\n---\nbody\n\"\"\", encoding=\"utf-8\")\n    result = scan(skill)\n    assert len(result.hits) &gt;= 1\n    assert any(h.field_path.endswith(\".stage\") for h in result.hits)\n\ndef test_ignores_template_path(tmp_path):\n    tpath = tmp_path / \"_template\" / \"author\"\n    tpath.mkdir(parents=True)\n    skill = tpath / \"SKILL.md\"\n    skill.write_text(\"\"\"---\nname: &lt;artefact&gt;-author\nmetadata:\n  stage: &lt;SDP §2 stage letter&gt;\n---\nbody\n\"\"\", encoding=\"utf-8\")\n    result = scan(skill)\n    # Template path → return empty hits (exempt)\n    assert result.exempt is True\n\ndef test_whitelists_br_tag(tmp_path):\n    skill = tmp_path / \"SKILL.md\"\n    skill.write_text(\"\"\"---\nname: foo\ndescription: \"Line 1&lt;br&gt;Line 2\"\n---\n\"\"\", encoding=\"utf-8\")\n    result = scan(skill)\n    assert len(result.hits) == 0  # &lt;br&gt; exempt\n\ndef test_wrap_in_marker_not_flagged(tmp_path):\n    skill = tmp_path / \"SKILL.md\"\n    skill.write_text(\"\"\"---\nname: foo\nmetadata:\n  stage: b\nwrap_in_marker: \"untrusted_content\"\n---\nbody\n\"\"\", encoding=\"utf-8\")\n    result = scan(skill)\n    assert len(result.hits) == 0</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>§3 covers the surface. Wiring:</p>\n<ol><li><code>tools/sweep-placeholders/detect.py</code> is the catalog scanner.</li><li><code>tools/sweep-placeholders/suggest.py</code> is the per-skill suggestion engine.</li><li><code>tools/sweep-placeholders/report.md</code> is the auto-generated report; operator edits to approve.</li><li><code>modules/cuo/cuo/placeholder_check.py</code> is the runtime validator (re-exported from <code>cuo</code> package).</li><li><code>modules/cuo/tests/test_placeholder_check.py</code> integrates with the existing CUO test suite.</li><li>SKILL_BUNDLE_RUBRIC.md gains SKB-030.</li><li>Task-audit skill gains §3.13 rule 38f.</li><li>Per-persona batches commit independently (P0 cpo + cto first, then P1, then P2+).</li></ol>\n<h2>§7 — Dependencies</h2>\n<p><strong>Depends on:</strong></p>\n<ul><li><strong>TASK-SKILL-113</strong> (XML-free frontmatter) — provides the <code>wrap_in_marker:</code> rename so the detector can correctly distinguish the now-fixed marker from stale placeholders.</li></ul>\n<p><strong>Blocks:</strong> none (independent of TASK-SKILL-114; orthogonal to TASK-SKILL-111 + 112).</p>\n<p><strong>Related:</strong> all of TASK-SKILL-111 / 112 / 113 / 114 — together they form the v0.2.5/v0.2.6 portability + foundation-discipline bundle.</p>\n<h2>§8 — Example payloads</h2>\n<h3>Example 1 — detect.py output (truncated)</h3>\n<pre><code class=\"language-json\">{\n  \"total_skills_with_hits\": 134,\n  \"skills\": [\n    {\n      \"skill\": \"modules/skill/transformation-roadmap-audit/SKILL.md\",\n      \"hits\": [\n        {\"field\": \"root.metadata.stage\", \"value\": \"&lt;SDP §2 stage letter or \\\"cross\\\"&gt;\", \"token\": \"SDP §2 stage letter or \\\"cross\\\"\"}\n      ]\n    }\n  ]\n}</code></pre>\n<h3>Example 2 — suggest.py output</h3>\n<pre><code>$ python3 tools/sweep-placeholders/suggest.py modules/skill/transformation-roadmap-audit\nField: root.metadata.stage\nCurrent: &lt;SDP §2 stage letter or \"cross\"&gt;\nSuggested: \"cross\"\nRationale: Body references stages b, d, e — multi-stage skill; choose \"cross\".</code></pre>\n<h3>Example 3 — Audit issue (SKB-030 firing)</h3>\n<pre><code>ISSUE\nid:              ISS-014\nrule_id:         SKB-030\nseverity:        error\ncategory:        placeholder_in_frontmatter\nlocation:        frontmatter root.metadata.stage\nevidence:        \"metadata.stage: &lt;SDP §2 stage letter or \\\"cross\\\"&gt;\"\ndescription:     \"Frontmatter field metadata.stage contains stale template placeholder syntax. Per TASK-SKILL-115 §1 #1, every production skill must carry concrete substituted values.\"\nsuggestion:      \"Run 'python3 tools/sweep-placeholders/suggest.py modules/skill/transformation-roadmap-audit' for a context-aware recommendation. The body references SDP stages b/d/e — suggest 'cross'.\"\nauto_fix_applied: false\nresolution:      null\nopened_at:       \"2026-05-19T17:00:00Z\"\nupdated_at:      \"2026-05-19T17:00:00Z\"</code></pre>\n<h2>§9 — Open questions</h2>\n<p><strong>All resolved during authoring.</strong></p>\n<p>Deferred:</p>\n<ul><li><strong>Per-persona owner sign-off</strong> at batch-commit time — out of scope for this task; handled by normal PR review process.</li><li><strong>Automated batch-commit grouping by persona</strong> — out of scope; the operator manually groups via <code>git add modules/skill/&lt;persona&gt;/</code>.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>suggest.py proposes wrong value</td><td>Operator review catches at report.md stage</td><td>No bad substitution shipped</td><td>Edit report.md; re-run sweep</td></tr><tr><td>Operator approves wrong substitution despite review</td><td>Audit log captures the wrong value; next fine-tune cycle on the skill surfaces the issue</td><td>Bad value lives until fine-tune</td><td>Standard fine-tune cycle corrects</td></tr><tr><td>Sweep accidentally touches body XML</td><td>verify.py SHA256-compares pre/post body bytes; mismatch → exit 1</td><td>Sweep batch rejected</td><td>Re-run with operator review of diff</td></tr><tr><td>134-skill sweep done in one mega-commit</td><td>Diff review fails (too large)</td><td>PR blocked</td><td>Split into persona batches</td></tr><tr><td>Migration runs while a fine-tune is mid-flight on the same skill</td><td>Conflict at commit; standard git merge resolves</td><td>Operator resolves conflict per file</td><td>Reapply suggested substitution + re-run verify</td></tr><tr><td>SKB-030 rule fires on draft skill mid-authoring</td><td>Severity warning only — author iterates without friction</td><td>No production impact</td><td>Substitute placeholder when promoting to accepted</td></tr><tr><td>_template/ exemption mis-fires (skips non-template skill)</td><td>detect.py output is incomplete</td><td>False clean signal</td><td>EXEMPT_PATHS path-prefix check is anchored to <code>_template/</code>; verify with <code>grep -l '_template/' tools/sweep-placeholders/detect.py</code></td></tr><tr><td>Registry version bump conflicts with concurrent TASK-SKILL-117 marker namespace work</td><td>Operator coordinates timing</td><td>Version bump skipped</td><td>Bump on TASK-SKILL-117 commit instead</td></tr><tr><td>Placeholder appears in body XML (false positive)</td><td>Detector only scans frontmatter; body bytes untouched</td><td>No false positive</td><td>Detector splits at <code>\\n---\\n</code> and only parses frontmatter</td></tr><tr><td>Operator misreads a multi-line YAML folded scalar as a placeholder</td><td>suggest.py shows full flattened context; operator confirms before approval</td><td>Edge case caught at review</td><td>Adjust report.md template to render flattened equivalent alongside raw</td></tr><tr><td>CI gate fires before sweep finishes</td><td><code>--fail-on-error-status-accepted-only</code> flag protects scaffold/draft skills during transition</td><td>PR not blocked on legitimate draft work</td><td>Operator transitions flag to <code>--fail-on-error</code> post-sweep completion</td></tr><tr><td><code>wrap_in_marker:</code> accidentally regressed during sweep</td><td>verify.py invariant (c) catches: marker still must equal <code>\"untrusted_content\"</code></td><td>Sweep batch rejected</td><td>Re-run with corrected substitution</td></tr><tr><td>Multi-stage skill ambiguity (which letter to pick?)</td><td>suggest.py recommends \"cross\"; operator can override</td><td>No silent miscategorisation</td><td>Operator review at report.md stage</td></tr><tr><td>Body references stage letter that doesn't exist in SDP (typo)</td><td>suggest.py returns None; operator must hand-edit</td><td>No silent bad value</td><td>Hand-edit + commit</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why persona-grouped batches over single mega-commit?</strong> Three reasons (already in §2): diff reviewability, risk isolation, schedule fit. Persona batches also align with the existing CHANGELOG taxonomy.</li><li><strong>Why preserve <code>_template/</code> placeholders deliberately?</strong> They're scaffolds. Removing them would break <code>cp -r _template/author/ &lt;new-skill&gt;/</code> + sed-substitute workflow. The template files DOCUMENT what placeholders go where — they're contract artefacts, not stale leakage.</li><li><strong>Why the broad placeholder regex (allowing §, /, \", etc. inside)?</strong> The actual stale values in the catalog include <code>&lt;SDP §2 stage letter or \"cross\"&gt;</code> — quite verbose. A naive <code>&lt;\\w+&gt;</code> regex would miss the multi-word placeholder. The chosen regex matches anything that LOOKS like an angle-bracket placeholder text fragment.</li><li><strong>Why suggest.py reads multiple sibling files (CONTRACT_ECHO, MANIFEST_SCHEMA, MODULE.md)?</strong> Different fields' correct substitutions come from different sources. metadata.stage comes from body SDP-stage references; description placeholders come from CONTRACT_ECHO template_version; depends_on_contracts come from MANIFEST_SCHEMA. One source-of-truth per field-type.</li><li><strong>Why operator-attested commit messages instead of an automated audit row?</strong> Both happen. The commit message is human-readable rationale; the audit row (per AGENTS.md §7) is the machine-readable record. Together they form the chain of custody for each substitution.</li><li><strong>Why 8 hours of mechanical sweep across 134 skills?</strong> Math: 3.5 minutes per skill = ~12 skills/hour. 134 / 12 ≈ 11 hours pre-suggestions; with suggest.py shortening review to ~2 min/skill, 8 hours is realistic.</li><li><strong>Why dependency on TASK-SKILL-113 instead of independent?</strong> TASK-SKILL-113 introduced <code>wrap_in_marker:</code> — without that rename in place, the detector might confuse the OLD <code>wrap_in: &lt;untrusted_content/&gt;</code> form (which 113 already migrated) with stale placeholders. TASK-SKILL-113 first; then TASK-SKILL-115 picks up the residual.</li></ul>\n<hr>\n<p><em>End of TASK-SKILL-115.</em></p>";
