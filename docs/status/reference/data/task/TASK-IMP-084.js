window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-084"]="<h1>TASK-IMP-084: task-lint, a deterministic machine floor under the task-audit rubric</h1>\n<h2>Summary</h2>\n<p>audit_rubric@2.0 calls itself machine-checkable, but nothing executes it - every FM enum, required section, and traceability citation is re-derived by a model on each audit. Ship <code>task-lint.mjs</code> in docs-tools: a zero-dependency node CLI that checks the mechanical rule families deterministically, emits rule_id-tagged findings, and exits non-zero on error severity. The task-audit skill runs it first and judges only what needs judgment (QA semantics, SAFE content, TRACE meaning). Consumer repos inherit the tool through the payload's docs-tools copy.</p>\n<h2>Problem</h2>\n<p>On the 2026-07-16 sachviet run, six spec audits were performed entirely by the model. The findings that mattered were judgment calls, but the majority of rubric surface walked each time was mechanical: frontmatter enums, title length, snake_case keys, required H2s, conditional sections, clause-to-AC citation presence, test-path resolution. A model re-checking those is spending its diligence budget where a 300-line script is strictly better - byte-stable, instant, and immune to fatigue. The rubric's own header makes the promise:</p>\n<p>&lt;untrusted_content source=\"modules/skill/task-audit/RUBRIC.md:1\"&gt;</p>\n<h1><code>audit_rubric@2.0</code> - machine-checkable Task rubric</h1>\n<p>&lt;/untrusted_content&gt;</p>\n<h2>Proposed Solution</h2>\n<p><code>node .cyberos/docs-tools/task-lint.mjs &lt;spec.md ... | dir&gt;</code> (in the platform repo: <code>tools/install/docs-tools/task-lint.mjs</code>). Node stdlib only. It parses frontmatter with a strict minimal YAML reader (scalars, lists, quoted strings - the subset the template uses; anything beyond that is itself an FM-001 finding), walks the body headings, and checks the mechanical families. Output: one line per finding, <code>SEVERITY rule_id file:line message</code>, sorted; <code>--json</code> for machines; exit 0 only when zero error-severity findings. The task-audit skill's loop gains one normative step: run the lint first when present, seed the report's mechanical findings from it, then perform the judgment families. The lint never replaces the audit; it floors it.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>A real YAML dependency (js-yaml) for parsing. Rejected: docs-tools is stdlib-only by convention (md.mjs, render-status-hub.mjs), payload stays dependency-free, and the template's frontmatter subset is small; a strict subset parser that FAILS LOUDLY on exotic YAML is safer than silently accepting what the audit families never defined.</li><li>Extending an existing tool (repair_task_yaml.py). Rejected: that script mutates (repairs); the lint must be read-only and rule_id-faithful, and mixing repair with verdicts blurs who changed what.</li><li>Wiring the lint as a hard gate in run-gates.sh now. Rejected for this task: gates belong to per-repo config; the skill-level wiring gets the value everywhere immediately, and repos can add it to config.yaml gates themselves. Revisit after adoption.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: the mechanical rule families run in milliseconds with zero model involvement - lint verdict on the three batch-1 specs and a green corpus sample matches the model audits' mechanical findings (no false errors). Baseline: 0 percent of rubric surface machine-executed. Deadline: this task's final acceptance.</li><li>Guardrail: seeded violation fixtures (one per family) each produce exactly their rule_id and a non-zero exit, on every suite run.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the lint CLI, its fixture-driven test suite, one normative wiring line in task-audit SKILL.md, payload presence gating.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Judgment families: QA-001..009 semantics (vanity metrics, risk-class dodging), SAFE-003 content scanning beyond marker presence, TRACE semantic sufficiency, XCHAIN/STALE manifest cross-checks.</li><li>Auto-fixing anything (read-only tool).</li><li>engineering-spec@1 profile rules (the lint targets template: task@1 detection; other profiles report <code>template_ambiguous</code> and stop, mirroring RUBRIC §10).</li><li>run-gates.sh wiring (per-repo choice, later item).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None upstream. Cone-disjoint from TASK-IMP-082 (renderer) and TASK-IMP-083 (install.sh); ships in the same parallel batch. Downstream: IMPROVEMENT_HANDOFF IMP-04's helpers can reuse its frontmatter reader.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted by the model from IMPROVEMENT_HANDOFF.md IMP-03 plus rubric source mapping; implementation follows under ship-tasks supervision.</li><li><strong>Human review:</strong> PLAN approved by the operator on 2026-07-16; spec audit and both HITL acceptance gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The lint MUST accept one or more spec paths or directories (directories recurse to <code>*/spec.md</code>) and MUST run on node stdlib alone.</li><li>1.2 The lint MUST implement the FM family mechanically: FM-001 (fences + parseable frontmatter per the strict subset), FM-002 (snake_case keys), FM-003 (duplicate keys), FM-004 (template equals task@1), FM-101 (title 1-72 after trim), FM-102 (author regex), FM-103/104/105/107/108/109 (closed enums, with FM-109 rejecting <code>unacceptable</code>), FM-106 (ISO 8601 with timezone), FM-110 (semver-or-quarter when present), FM-111 (real YAML boolean), FM-112 (no <code># UNREVIEWED</code> marker), FM-113 (duplicate_of iff status duplicate, resolving to an existing task folder), FM-114 (severity iff type bug).</li><li>1.3 The lint MUST implement SEC mechanically: SEC-001..007 required H2s present, SEC-008 each non-empty (at least one non-blank body line before the next heading), SEC-009 single H1 and no skipped heading levels (warning severity).</li><li>1.4 The lint MUST implement COND triggers mechanically: COND-001/002 sections when client_visible true, COND-003 with its three H3s in order when risk class is limited or high, COND-004 with the three labeled bullets when ai_authorship is not none.</li><li>1.5 The lint MUST implement the structural TRACE halves: TRACE-001 presence form (every numbered §1 clause containing a BCP-14 keyword is cited by at least one AC via <code>§1 #N</code> or <code>traces_to</code>), TRACE-002 presence form (every AC line carries a <code>test:</code> or <code>verify:</code> entry), TRACE-003 (every <code>test:</code> path segment before <code>::</code> is listed in frontmatter new_files or exists on disk). Semantic sufficiency stays with the model audit.</li><li>1.6 Findings MUST be emitted one per line as <code>SEVERITY rule_id file:line message</code>, sorted bytewise, with <code>--json</code> emitting the same findings as a JSON array; two runs on identical input MUST be byte-identical.</li><li>1.7 Exit code MUST be 0 with no error-severity findings, 2 otherwise; unreadable input or non-task@1 template detection MUST report <code>template_ambiguous</code> at error severity rather than guessing.</li><li>1.8 <code>modules/skill/task-audit/SKILL.md</code> MUST gain one normative loop step: when the lint is present (<code>.cyberos/docs-tools/task-lint.mjs</code> in installed repos, <code>tools/install/docs-tools/task-lint.mjs</code> in the platform repo), run it FIRST and seed the report's mechanical findings from its output; the model then audits the judgment families only.</li><li>1.9 A gating suite MUST land at <code>tools/install/tests/test_task_lint.sh</code>: the tool passes on this batch's three specs; one fixture per family (FM, SEC, COND, TRACE) produces exactly its rule_id and exit 2; the assembled payload carries <code>docs-tools/task-lint.mjs</code> and a scratch install lays it into <code>.cyberos/docs-tools/</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: §1 #1.1, #1.6, #1.7) - CLI shape, deterministic sorted output, exit codes - test: <code>tools/install/tests/test_task_lint.sh::t01_cli_and_determinism</code></li><li>[ ] AC 2 (traces_to: §1 #1.2) - FM family fixtures each yield their rule_id - test: <code>tools/install/tests/test_task_lint.sh::t02_fm_family</code></li><li>[ ] AC 3 (traces_to: §1 #1.3) - SEC family fixtures - test: <code>tools/install/tests/test_task_lint.sh::t03_sec_family</code></li><li>[ ] AC 4 (traces_to: §1 #1.4) - COND trigger fixtures - test: <code>tools/install/tests/test_task_lint.sh::t04_cond_family</code></li><li>[ ] AC 5 (traces_to: §1 #1.5) - TRACE structural fixtures (uncited clause, AC without test, dangling test path) - test: <code>tools/install/tests/test_task_lint.sh::t05_trace_family</code></li><li>[ ] AC 6 (traces_to: §1 #1.9) - green corpus: batch-1 specs lint clean - test: <code>tools/install/tests/test_task_lint.sh::t06_green_corpus</code></li><li>[ ] AC 7 (traces_to: §1 #1.9) - payload carries the tool and install lays it down - test: <code>tools/install/tests/test_task_lint.sh::t07_payload_and_install</code></li><li>[ ] AC 8 (traces_to: §1 #1.8) - the skill wiring line exists and names the lint-first order - test: <code>tools/install/tests/test_task_lint.sh::t08_skill_wiring_present</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Frontmatter using YAML the strict subset does not parse (anchors, multiline blocks): FM-001 error naming the line - loud, never a silent skip (t02 includes one).</li><li>A spec with zero numbered §1 clauses (pure PRD shape): TRACE-001 has nothing to check - the lint reports an info-level note, not an error (template allows judgment there; the model audit decides).</li><li><code>verify:</code> ACs (justified ops verification) satisfy TRACE-002's structural half by design - the lint checks presence, the model checks the justification (t05 includes one passing verify case).</li><li>CRLF files, BOM, trailing whitespace in headings - normalized for heading matching, reported nowhere (content bytes are the corpus's business).</li><li>Huge corpora: per-file processing, findings streamed after sort; no cross-file state except FM-113 resolution, which scans task folder names only.</li><li>Unicode titles: length counted in code points after trim (documented; FM-101's 72 is a display bound, not a byte bound).</li><li>Security-class: the lint reads files and never executes content; <code>--json</code> output is data, not code. The injection-marker scan (SAFE-003) stays with the model by scope - noted so nobody assumes the lint covers it.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<p>Duplicated intentionally with <code>## Scope</code> for template conformance: judgment families, auto-fix, other template profiles, and gate wiring are excluded.</p>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>The lint floors the audit, never replaces it - the skill text keeps the model responsible for judgment families and the 10/10 verdict.</li><li>docs-tools stays node-stdlib-only.</li><li>Payload sync doctrine: rebuild dist, version-sync, full suite before commit.</li><li>HITL: both human-acceptance gates are recorded verdicts; the agent never sets done.</li></ul>\n<p><em>End of TASK-IMP-084.</em></p>";
