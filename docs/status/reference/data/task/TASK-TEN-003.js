window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship the Stripe billing rail at <code>services/ten/src/billing/stripe/</code> — Customer/Subscription/Usage-Record lifecycle for tenants whose <code>billing_currency ∈ {USD, EUR, SGD, GBP}</code>, plan-change push from TASK-TEN-002, overage push from TASK-TEN-004, dispatcher for TASK-INV-003 webhook events, dunning state machine, refund flow, and 11 memory audit row kinds.</p>\n<ol><li><strong>MUST</strong> add columns to <code>tenants</code> via migration <code>0006_stripe_billing.sql</code>:</li></ol>\n<ul><li><code>billing_currency billing_currency_enum NOT NULL DEFAULT 'VND'</code> — closed enum from migration <code>0009</code>.</li><li><code>billing_rail TEXT NOT NULL CHECK (billing_rail IN ('stripe','vietqr_momo_zalo','internal')) DEFAULT 'vietqr_momo_zalo'</code> — derived from <code>billing_currency</code> at provisioning; <code>internal</code> reserved for founder tenant per DEC-805.</li><li><code>stripe_customer_id TEXT</code> — nullable; populated lazily per DEC-786.</li><li><code>stripe_subscription_id TEXT</code> — nullable; populated on first plan_change_push.</li><li><code>billing_contact_email TEXT NOT NULL</code> — collected at provisioning; used as Stripe Customer email.</li><li><code>dunning_state TEXT NOT NULL CHECK (dunning_state IN ('ok','retry_1','retry_2','retry_3','suspended')) DEFAULT 'ok'</code>.</li><li>Partial unique index <code>CREATE UNIQUE INDEX uniq_stripe_customer ON tenants(stripe_customer_id) WHERE stripe_customer_id IS NOT NULL</code>.</li><li>Partial unique index <code>CREATE UNIQUE INDEX uniq_stripe_subscription ON tenants(stripe_subscription_id) WHERE stripe_subscription_id IS NOT NULL</code>.</li></ul>\n<ol><li><strong>MUST</strong> define the closed <code>billing_currency_enum</code> Postgres type at migration <code>0009</code> with exactly 5 values: <code>VND, USD, EUR, SGD, GBP</code>. CI cardinality test asserts 5 (DEC-792 + task-audit skill rule 6 namespace pattern adapted for enum). A 6th value requires a schema migration + DEC entry.</li></ol>\n<ol><li><strong>MUST</strong> derive <code>billing_rail</code> from <code>billing_currency</code> at tenant provisioning (TASK-TEN-001 handler):</li></ol>\n<ul><li><code>VND</code> → <code>vietqr_momo_zalo</code> (TASK-TEN-102 path).</li><li><code>USD | EUR | SGD | GBP</code> → <code>stripe</code> (this task's path).</li><li>Founder tenant (<code>is_founder_tenant=true</code>) → <code>internal</code> (DEC-805) regardless of currency; Stripe API calls short-circuit with a no-op + sev-3 memory row <code>ten.stripe_founder_skip</code>.</li></ul>\n<ol><li><strong>MUST</strong> lock <code>billing_currency</code> at provisioning — <code>UPDATE tenants SET billing_currency = ...</code> is REJECTED by a row-level trigger (DEC-798). Changing currency = create new tenant + manual data migration (out-of-scope, task-TEN-2xx).</li></ol>\n<ol><li><strong>MUST</strong> define the compile-time price catalog in <code>services/ten/src/billing/stripe/price_catalog.rs</code> as <code>const PRICE_CATALOG: [PriceEntry; 12]</code>. The 12 entries are 3 plan tiers × 4 international currencies (Starter/Team/Enterprise × USD/EUR/SGD/GBP). Each <code>PriceEntry</code> carries <code>{ currency, tier, amount_minor: i64 }</code>. The Stripe Price IDs are NOT baked into the constant — they live in the <code>stripe_price_map</code> Postgres table (per residency × currency × tier × axis) populated by the deploy-time <code>cyberos-ten stripe-sync-prices</code> CLI per DEC-792. Overage axes (4 per tier per currency) are ALSO mapped via <code>stripe_price_map.axis ∈ {base, overage_api_calls, overage_ai_tokens, overage_storage, overage_seats}</code>, giving 5 metered prices per (currency, tier) — totalling 12 base + 48 overage = 60 distinct Stripe Price IDs per residency. The CI cardinality test asserts 12 base catalog entries (the overage prices are derived from base — same currency/tier, different axis). Reference monthly base amounts (slice 2 baseline; CFO may rev via a follow-up DEC entry):</li></ol>\n<table><thead><tr><th>currency</th><th>starter</th><th>team</th><th>enterprise</th></tr></thead><tbody><tr><td>USD</td><td>1_900 (¢)</td><td>9_900 (¢)</td><td>99_900 (¢)</td></tr><tr><td>EUR</td><td>1_900 (¢)</td><td>9_900 (¢)</td><td>99_900 (¢)</td></tr><tr><td>SGD</td><td>2_500 (¢)</td><td>13_000 (¢)</td><td>130_000 (¢)</td></tr><tr><td>GBP</td><td>1_500 (p)</td><td>7_900 (p)</td><td>79_900 (p)</td></tr></tbody></table>\n<p>Stripe Price IDs are populated by <code>cyberos-ten stripe-sync-prices</code> against the Stripe API (per residency Stripe account per DEC-801) and persisted to migration <code>0010_stripe_price_map</code>. CI test <code>stripe_price_catalog_cardinality_test</code> asserts exactly 12 entries and every entry has a non-empty <code>stripe_price_id_prod</code> after deploy-time sync.</p>\n<ol><li><strong>MUST</strong> create the Stripe Customer lazily on the first billing event for the tenant (subscription create, manual invoice, refund). The path is <code>services/ten/src/billing/stripe/customer.rs::ensure_customer(tenant_id)</code>:</li></ol>\n<ul><li>Lookup <code>tenants.stripe_customer_id</code>. If non-NULL → return.</li><li>Else: call Stripe <code>POST /v1/customers</code> with <code>Idempotency-Key: ten.&lt;tenant_id&gt;.customer_create.v1</code> and body <code>{ email: &lt;billing_contact_email&gt;, name: &lt;display_name&gt;, metadata: { cyberos_tenant_id: &lt;tenant_id&gt;, cyberos_residency: &lt;residency&gt;, cyberos_billing_currency: &lt;currency&gt; } }</code>.</li><li>On success: <code>UPDATE tenants SET stripe_customer_id = $1 WHERE id = $2</code> (partial-unique-guarded; concurrent attempts on same tenant race-safe via SELECT FOR UPDATE).</li><li>Emit memory row <code>ten.stripe_customer_created</code> with <code>(tenant_id, stripe_customer_id, currency, residency)</code>.</li></ul>\n<ol><li><strong>MUST</strong> create the Stripe Subscription at the first non-Starter plan_change OR at provisioning when <code>--seed-subscription</code> flag is set (default: subscription created lazily on plan_change → Team/Enterprise; Starter tenants get subscriptions too since Starter is paid per TASK-TEN-002 DEC-778). Path: <code>services/ten/src/billing/stripe/subscription.rs::ensure_subscription(tenant_id, plan_tier)</code>:</li></ol>\n<ul><li>Ensure customer exists (per §1 #6).</li><li>Resolve Stripe Price ID via <code>price_catalog.rs</code> lookup on <code>(billing_currency, plan_tier)</code>.</li><li>Call Stripe <code>POST /v1/subscriptions</code> with <code>Idempotency-Key: ten.&lt;tenant_id&gt;.subscription_create.&lt;plan_tier_ordinal&gt;</code> and body <code>{ customer: &lt;stripe_customer_id&gt;, items: [{ price: &lt;price_id&gt; }, { price: &lt;overage_meter_price_id&gt;, ... per axis }], billing_cycle_anchor: &lt;tenant.provisioned_at unix ts (DEC-788)&gt;, proration_behavior: \"create_prorations\", metadata: { cyberos_tenant_id, cyberos_plan_tier } }</code>.</li><li>Subscription Items for the 4 overage axes (DEC-789): one Stripe <code>subscription_item</code> per <code>(currency, axis)</code> with <code>recurring: { usage_type: \"metered\" }</code> — 4 metered prices per currency (api_calls overage, ai_tokens overage, storage overage, seats overage) stored in <code>stripe_price_map</code> alongside the base tier prices.</li><li>On success: <code>UPDATE tenants SET stripe_subscription_id = $1</code>.</li><li>Emit <code>ten.stripe_subscription_created</code>.</li></ul>\n<ol><li><strong>MUST</strong> push plan changes to Stripe (<code>services/ten/src/billing/stripe/subscription.rs::push_plan_change</code>) invoked synchronously from the TASK-TEN-002 <code>plan_change</code> handler AFTER the <code>tenant_plan_history</code> row commits, IN A SEPARATE TRANSACTION (Stripe API is external; no 2-phase commit). The handler:</li></ol>\n<ul><li>If <code>from_tier &lt; to_tier</code> (upgrade): Stripe <code>POST /v1/subscriptions/{id}</code> with <code>items: [{ id: &lt;base_item_id&gt;, price: &lt;new_price_id&gt; }]</code> and <code>proration_behavior: \"create_prorations\"</code> and <code>Idempotency-Key: ten.&lt;tenant_id&gt;.plan_upgrade.&lt;history_id&gt;</code>. Prorations land on the next invoice.</li><li>If <code>from_tier &gt; to_tier</code> (downgrade, deferred per TASK-TEN-002 DEC-773): create a Stripe Subscription Schedule (<code>POST /v1/subscription_schedules</code>) anchored to <code>effective_at</code> from history, with <code>proration_behavior: \"none\"</code>. <code>Idempotency-Key: ten.&lt;tenant_id&gt;.plan_downgrade.&lt;history_id&gt;</code>.</li><li>Emit <code>ten.stripe_subscription_updated</code> with <code>(from_tier, to_tier, history_id, stripe_invoice_item_ids[])</code>.</li></ul>\n<ol><li><strong>MUST</strong> push overages at billing-period close (<code>services/ten/src/billing/stripe/overage.rs::push_overage_for_period</code>) invoked from the TASK-TEN-004 <code>period_close</code> hook AFTER the aggregation completes. For each axis where <code>actual &gt; tier_cap</code>:</li></ol>\n<ul><li>Compute <code>overage = actual - tier_cap</code>.</li><li>Call Stripe <code>POST /v1/subscription_items/{item_id}/usage_records</code> with <code>{ quantity: &lt;overage&gt;, timestamp: &lt;period_end_unix&gt;, action: \"set\" }</code> and <code>Idempotency-Key: ten.&lt;tenant_id&gt;.overage_push.&lt;axis&gt;.&lt;period_end_unix&gt;</code>.</li><li>Emit <code>ten.stripe_overage_pushed</code> with <code>(axis, overage_quantity, period_end, stripe_usage_record_id)</code>.</li><li><strong>Push window:</strong> must complete within 1 hour of period_close (DEC-810). On failure → exponential backoff retry up to 24 h; persistent failure → sev-1 alert + memory row <code>ten.stripe_overage_push_failed</code>.</li></ul>\n<ol><li><strong>MUST</strong> dispatch TASK-INV-003 Stripe webhook events into TEN-side handlers via NATS subject <code>tenant.&lt;slug&gt;.ten.stripe.&lt;event_type&gt;</code> (DEC-793 + DEC-808). The consumer (<code>services/ten/src/billing/stripe/dispatch.rs</code>) is idempotent via <code>(tenant_id, stripe_event_id) UNIQUE</code> in <code>stripe_event_dispatch_log</code>. The relevant events:</li></ol>\n<ul><li><code>invoice.finalized</code> → audit only (<code>ten.stripe_invoice_finalized</code>).</li><li><code>invoice.payment_succeeded</code> → clear dunning + emit <code>ten.stripe_invoice_paid</code> + un-suspend tenant if <code>dunning_state='suspended'</code> (DEC-804).</li><li><code>invoice.payment_failed</code> → advance dunning state (per §1 #11) + emit <code>ten.stripe_invoice_payment_failed</code>.</li><li><code>customer.subscription.updated</code> → log only if drift from our state (alert sev-2 if Stripe state ≠ tenants.stripe_subscription_id-derived state).</li><li><code>customer.subscription.deleted</code> → emit <code>ten.stripe_subscription_cancelled</code> + DO NOT auto-terminate tenant (handled by TASK-TEN-104 termination flow which sets <code>cancel_at_period_end</code> first).</li></ul>\n<ol><li><strong>MUST</strong> advance the dunning state machine on <code>invoice.payment_failed</code> (<code>services/ten/src/billing/dunning.rs</code>):</li></ol>\n<ul><li><code>ok → retry_1</code> on first failure.</li><li><code>retry_1 → retry_2</code> on second.</li><li><code>retry_2 → retry_3</code> on third.</li><li><code>retry_3 → suspended</code> on fourth → trigger TASK-TEN-104 <code>tenant_suspended</code> status transition + emit <code>ten.tenant_billing_suspended</code> + <code>ten.stripe_dunning_advanced</code> with <code>(prior_state, new_state)</code>.</li><li>Any state + <code>invoice.payment_succeeded</code> → <code>ok</code> + un-suspend if suspended.</li><li>State transitions are OBSERVED via <code>stripe_event_dispatch_log</code> rows joined with <code>tenants.dunning_state</code> point-in-time snapshots; the canonical history is the immutable inbound webhook log + the memory chain row emitted on each advance (DEC-808 derivative). No separate <code>tenant_dunning_history</code> table is created — the existing event log + memory chain row carry the same information without schema duplication.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{id}/billing/refund</code> for refunds (<code>services/ten/src/handlers/billing_refund.rs</code>). Caller MUST have role <code>cfo</code> per TASK-AUTH-101 (DEC-791). Body: <code>{ stripe_charge_id, amount_minor, currency, reason }</code>. Validations:</li></ol>\n<ul><li><code>amount_minor ≤ original_charge_amount</code> (DEC-791 — no over-refund). Lookup original charge amount via Stripe <code>GET /v1/charges/{id}</code>.</li><li><code>currency == tenant.billing_currency</code> (DEC-809).</li><li>Call Stripe <code>POST /v1/refunds</code> with <code>Idempotency-Key: ten.&lt;tenant_id&gt;.refund.&lt;charge_id&gt;.&lt;amount_minor&gt;</code> and body <code>{ charge: &lt;id&gt;, amount: &lt;amount_minor&gt;, reason: requested_by_customer | duplicate | fraudulent }</code>.</li><li>Emit <code>ten.stripe_refund_issued</code> at sev-1 with <code>(tenant_id, stripe_refund_id, charge_id, amount_minor, currency, cfo_subject_id, reason)</code>.</li><li>Returns <code>201 CREATED</code> with <code>{ refund_id, status, expected_settlement_at }</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/admin/tenants/{id}/billing</code> (<code>services/ten/src/handlers/billing_show.rs</code>). Returns: ``<code>json { \"tenant_id\": \"...\", \"billing_rail\": \"stripe\", \"billing_currency\": \"USD\", \"stripe_customer_id\": \"cus_...\", \"stripe_subscription_id\": \"sub_...\", \"current_plan_tier\": \"team\", \"dunning_state\": \"ok\", \"billing_cycle_anchor\": \"2026-05-17T00:00:00Z\", \"next_invoice_date\": \"2026-06-17T00:00:00Z\", \"recent_invoices\": [ ... last 12 ... ], \"recent_dunning_events\": [ ... ] } </code>`<code> Caller MUST have role </code>tenant_admin<code> (own tenant) or </code>cfo` (any tenant).</li></ol>\n<ol><li><strong>MUST</strong> thread <code>Idempotency-Key</code> on every outbound Stripe write API call (DEC-794 + task-audit skill rule 12 derivative for external systems). Key format <code>ten.&lt;tenant_id&gt;.&lt;operation&gt;.&lt;resource_ref_or_period_ts&gt;</code>. Max 255 chars. Persisted in <code>stripe_api_calls</code> table with columns <code>(idempotency_key TEXT PRIMARY KEY, tenant_id UUID, operation TEXT, request_sha256 CHAR(64), response_status INT, response_body_sha256 CHAR(64), created_at TIMESTAMPTZ, ttl_until TIMESTAMPTZ DEFAULT now() + INTERVAL '7 days')</code>. A scheduled job prunes entries past <code>ttl_until</code> daily.</li></ol>\n<ol><li><strong>MUST</strong> honour Stripe API <code>Retry-After</code> header on rate-limit (429) responses (DEC-795). For 5xx responses without <code>Retry-After</code>, apply exponential backoff: <code>1s, 2s, 4s, 8s, 16s</code> capped at 5 min (max 5 retries). After exhaustion: memory audit <code>ten.stripe_api_call_failed</code> at sev-2 + return <code>502 BAD_GATEWAY</code> to caller with <code>{ error: \"stripe_unavailable\", retry_after_seconds: &lt;next_retry&gt; }</code>.</li></ol>\n<ol><li><strong>MUST</strong> route Stripe API calls through the per-residency Stripe API key (DEC-801). The api_client constructor takes <code>residency: Residency</code> and resolves <code>STRIPE_API_KEY_&lt;RESIDENCY&gt;</code> from KMS-encrypted secrets store. Wrong residency → wrong Stripe account → forensically catastrophic; CI test <code>stripe_residency_apikey_routing_test</code> asserts correct routing per residency.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> on <code>stripe_api_calls</code> and <code>stripe_event_dispatch_log</code> tables (task-audit skill rule 13). Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li></ol>\n<ol><li><strong>MUST</strong> REVOKE UPDATE, DELETE on <code>stripe_api_calls</code>, <code>stripe_event_dispatch_log</code>, and <code>stripe_price_map</code> from <code>cyberos_app</code> role (task-audit skill rule 12). Pruning of <code>stripe_api_calls</code> past TTL uses a separate <code>cyberos_pruner</code> role with DELETE grant only on past-TTL rows.</li></ol>\n<ol><li><strong>MUST</strong> emit 11 memory audit row kinds (DEC-784 expansion + task-audit skill rule 6 namespace pattern <code>^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$</code>):</li></ol>\n<ul><li><code>ten.stripe_customer_created</code> (sev-2)</li><li><code>ten.stripe_subscription_created</code> (sev-2)</li><li><code>ten.stripe_subscription_updated</code> (sev-2)</li><li><code>ten.stripe_subscription_cancelled</code> (sev-1)</li><li><code>ten.stripe_invoice_finalized</code> (sev-3)</li><li><code>ten.stripe_invoice_paid</code> (sev-2)</li><li><code>ten.stripe_invoice_payment_failed</code> (sev-2)</li><li><code>ten.stripe_dunning_advanced</code> (sev-1)</li><li><code>ten.tenant_billing_suspended</code> (sev-1)</li><li><code>ten.stripe_refund_issued</code> (sev-1)</li><li><code>ten.stripe_overage_pushed</code> (sev-3)</li></ul>\n<p>    Each row carries <code>trace_id</code> (32-char W3C hex per task-audit skill rule 23 + 24) and is PII-scrubbed via TASK-MEMORY-111 BEFORE chain commit (task-audit skill rule 18). <code>billing_contact_email</code> is hashed (<code>billing_contact_email_hash16</code>) in chain; full value retained only in tenant Postgres.</p>\n<ol><li><strong>MUST</strong> ship the <code>cyberos-ten stripe-sync-prices</code> deploy-time CLI (<code>services/ten/src/cli/stripe_sync_prices.rs</code>). Behaviour:</li></ol>\n<ul><li><code>--dry-run</code> (default): list every <code>(residency, currency, tier, axis)</code> permutation in <code>PRICE_CATALOG × AXES</code> (12 base + 48 overage = 60 per residency) and the Stripe Price ID it would create/lookup.</li><li><code>--apply</code>: for each entry without an existing Stripe Price ID (looked up via Stripe <code>GET /v1/prices?lookup_keys=...</code> with <code>lookup_key = \"cyberos.&lt;currency&gt;.&lt;tier&gt;.&lt;axis&gt;\"</code>), call Stripe <code>POST /v1/prices</code> with the catalog amount + <code>metadata: { cyberos_currency, cyberos_tier, cyberos_axis }</code>; persist returned Price ID to <code>stripe_price_map</code>.</li><li><code>--residency &lt;sg-1|eu-1|us-1&gt;</code> REQUIRED on <code>--apply</code> (CLI loads only that residency's KMS-encrypted Stripe API key per DEC-801); <code>--dry-run</code> defaults to all-residencies for reporting. Cross-residency API misuse is impossible because the loaded API key only addresses one Stripe account.</li><li>Idempotent on <code>(residency, currency, tier, axis)</code>; re-run is a no-op when all entries already mapped.</li><li>Exit codes from <code>cyberos-cli-exit</code> shared crate (task-audit skill rule 9): 0 success, 1 nothing-to-do, 64 invalid-arg, 73 stripe-create-failed, 77 perm-denied.</li></ul>\n<ol><li><strong>MUST</strong> support concurrent plan_change events safely. The plan_change handler acquires <code>SELECT ... FOR UPDATE</code> on the tenant row before computing the Stripe push; second concurrent plan_change blocks until first commits. Combined with the TASK-TEN-002 24h rate limit, plan_change concurrency is bounded.</li></ol>\n<ol><li><strong>MUST NOT</strong> call any Stripe write API for a tenant with <code>is_founder_tenant=true</code> (DEC-805). Guard at <code>services/ten/src/billing/stripe/api_client.rs::call()</code> entry — if <code>tenant.billing_rail = 'internal'</code>, return synthetic success with <code>Stripe-API-Bypass: founder</code> header for trace correlation + emit sev-3 memory row <code>ten.stripe_founder_skip</code> (informational; not in the 11 main kinds because founder is rare).</li></ol>\n<ol><li><strong>MUST NOT</strong> call any Stripe write API for a tenant with <code>billing_currency = 'VND'</code> (DEC-784). Guard at api_client entry — VND tenant routes through TASK-TEN-102 only. Cross-rail attempts return <code>400 BAD_REQUEST</code> with <code>{ error: \"wrong_billing_rail\", expected: \"vietqr_momo_zalo\", got: \"stripe\" }</code>.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub <code>billing_contact_email</code> and any reason text in refund/dunning audit rows via TASK-MEMORY-111 BEFORE chain commit (task-audit skill rule 18). Raw values retained in tenant Postgres (RLS-scoped); memory chain holds <code>billing_contact_email_hash16</code> and scrubbed reason.</li></ol>\n<ol><li><strong>SHOULD</strong> observe Stripe API latency p95 via OTel span <code>stripe.api.&lt;operation&gt;</code> (task-audit skill rule 22 + 24). Alarm sev-3 if p95 &gt; 2 s sustained 5 min; sev-2 if p95 &gt; 5 s.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a separate <code>billing/stripe/</code> subtree rather than putting Stripe code into <code>services/inv/</code> (§1 #1–§1 #5)?</strong> TASK-INV-003 is the <em>webhook receiver</em> — it does signature verification, idempotency dedupe, and writes raw <code>payment_receipts</code> rows. The TEN billing rail is the <em>outbound subscription lifecycle</em>: customer create, subscription create/update, usage record push, dunning state. These are different concerns: INV is rail-agnostic (\"a payment of $X arrived from Y\"), TEN owns the subscription contract with the customer. Mixing them couples webhook idempotency to subscription state machines and makes it impossible to add the TASK-TEN-102 VND rail without forking INV. The clean cut is: INV records inbound money, TEN orchestrates outbound rail calls + dispatches relevant inbound events back to TEN handlers via NATS (DEC-793).</p>\n<p><strong>Why lazy Stripe Customer creation rather than at tenant provisioning (§1 #6, DEC-786)?</strong> Provisioning happens before we know whether a tenant will ever transact. If the operator provisions 100 tenants for a demo and 90 are abandoned, eagerly creating 90 Stripe Customers leaks data into Stripe (PII residency concerns) and inflates Stripe Customer count for billing/audit. Lazy creation defers the externalisation until there's a real billing event — and the idempotency-key strategy means concurrent first-events don't race.</p>\n<p><strong>Why a 12-entry compile-time price catalog rather than runtime lookup from Stripe (§1 #5, DEC-792)?</strong> Two reasons. First, Stripe Price IDs are environment-specific (test vs prod, per-residency Stripe account) — runtime lookup requires API calls on every plan_change, which is latency + Stripe rate-limit pressure. Second, the price catalog IS the commercial contract — keeping it in source means a PR review enforces price changes (visible to engineering + legal + CFO). The <code>stripe_price_map</code> table is the binding between our internal <code>(currency, tier)</code> keys and Stripe's IDs, written once at deploy time by <code>stripe-sync-prices</code>.</p>\n<p><strong>Why per-residency Stripe API key (§1 #16, DEC-801)?</strong> EU-residency tenants billing through a Stripe US account violates GDPR data residency. Stripe operates separate legal entities per region (Stripe US, Stripe Singapore, Stripe Ireland for EU) with separate Connect accounts and separate KYC. Tenants on <code>eu-1</code> MUST bill through Stripe Ireland; <code>us-1</code> through Stripe US; <code>sg-1</code> through Stripe Singapore. A single global Stripe key is forensically catastrophic for compliance.</p>\n<p><strong>Why dunning state machine in TEN rather than relying on Stripe's smart retries alone (§1 #11)?</strong> Stripe smart retries handle payment retry timing but don't trigger our <code>tenant.status=suspended</code> transition. We need our own state machine that mirrors Stripe's retry attempts (event-driven via <code>invoice.payment_failed</code>) AND lands a domain transition (TASK-TEN-104 suspend → tenant CHAT goes read-only, no new metering events bill). Stripe is the source of truth for payment retry; TEN is the source of truth for tenant access state.</p>\n<p><strong>Why subscription cancellation is non-destructive (§1 #10, DEC-803)?</strong> Hard-cancel removes Stripe subscription immediately, but the tenant has paid through end-of-period — yanking access mid-period creates support escalations and refund pressure. <code>cancel_at_period_end=true</code> is industry standard: tenant retains access until they've used what they paid for; full termination via TASK-TEN-104 only when the period ends OR operator explicitly invokes hard-cancel.</p>\n<p><strong>Why founder-skip rather than just billing $0 (§1 #22, DEC-805)?</strong> Billing $0 still creates a Stripe Customer + Subscription, which is accounting noise (every monthly close has to explain why there's a $0 Stripe invoice for the founder tenant), and circular invoicing creates auditor confusion (\"CyberSkill invoicing itself?\"). Founder tenant short-circuits the whole rail; settled in internal accounting (not Stripe).</p>\n<p><strong>Why idempotency keys at our layer when Stripe already has them (§1 #14, DEC-794)?</strong> Stripe's idempotency keys protect Stripe — they're the <em>receiver</em>. We need our own keys to protect <em>us</em>: if our handler crashes after the Stripe call but before persisting the response, the retry sees Stripe's \"you already did this\" and we recover the result without double-side-effecting our own DB. Both layers must agree on key shape, which is why DEC-794 mandates the format.</p>\n<p><strong>Why overage push at period_close rather than per-event (§1 #9, DEC-789, DEC-810)?</strong> Per-event push to Stripe is rate-limit-prohibitive (Stripe limits ~100 req/s; a busy tenant might emit 10k metering events/period) and noisy (per-event partial usage records bloat invoices). Period_close aggregation gives one usage_record per axis per period — clean invoices + reasonable Stripe API load. The 1-hour push window matches Stripe's billing-finalization grace period.</p>\n<p><strong>Why per-residency Stripe price IDs (§1 #5, DEC-801 derivative)?</strong> A Stripe Price ID belongs to one Stripe account. If we tried to share <code>price_team_usd</code> across the US and Singapore Stripe accounts, it wouldn't exist in the other account → API error. Each residency's <code>stripe-sync-prices</code> populates that residency's account; the <code>stripe_price_map</code> table is keyed <code>(residency, currency, tier)</code> → <code>stripe_price_id</code>. Mid-provisioning failure where some residencies have prices and others don't is detectable at deploy via <code>--dry-run</code> listing un-mapped entries.</p>\n<p><strong>Why downgrade defers via Subscription Schedule rather than just setting a flag (§1 #8, DEC-797)?</strong> Stripe Subscription Schedule is the native Stripe primitive for \"switch to plan X at time T\". Re-implementing the deferral in our code means we'd have to (a) run a scheduled job at period boundary, (b) call Stripe to swap items, (c) reconcile with our plan_history. All three steps are failure points. Subscription Schedule moves the deferral into Stripe — when the period boundary hits, Stripe auto-swaps and emits <code>customer.subscription.updated</code> which we observe through dispatch (§1 #10).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema (migrations)</h3>\n<pre><code class=\"language-sql\">-- 0006_stripe_billing.sql\nALTER TABLE tenants\n  ADD COLUMN billing_currency billing_currency_enum NOT NULL DEFAULT 'VND',\n  ADD COLUMN billing_rail TEXT NOT NULL DEFAULT 'vietqr_momo_zalo'\n    CHECK (billing_rail IN ('stripe','vietqr_momo_zalo','internal')),\n  ADD COLUMN stripe_customer_id TEXT,\n  ADD COLUMN stripe_subscription_id TEXT,\n  ADD COLUMN stripe_subscription_items_map JSONB NOT NULL DEFAULT '{}'::jsonb,\n  ADD COLUMN billing_contact_email TEXT,  -- NOT NULL activated after 7-day backfill (§11.11)\n  ADD COLUMN dunning_state TEXT NOT NULL DEFAULT 'ok'\n    CHECK (dunning_state IN ('ok','retry_1','retry_2','retry_3','suspended'));\n\nCREATE UNIQUE INDEX uniq_stripe_customer ON tenants(stripe_customer_id)\n  WHERE stripe_customer_id IS NOT NULL;\nCREATE UNIQUE INDEX uniq_stripe_subscription ON tenants(stripe_subscription_id)\n  WHERE stripe_subscription_id IS NOT NULL;\n\n-- Trigger: billing_currency is immutable post-provisioning (DEC-798)\nCREATE OR REPLACE FUNCTION trg_billing_currency_immutable() RETURNS trigger AS $$\nBEGIN\n  IF OLD.billing_currency IS DISTINCT FROM NEW.billing_currency THEN\n    RAISE EXCEPTION 'billing_currency_immutable: cannot change billing_currency on existing tenant (DEC-798); create new tenant + manual migration';\n  END IF;\n  RETURN NEW;\nEND $$ LANGUAGE plpgsql;\nCREATE TRIGGER tenants_billing_currency_immutable\n  BEFORE UPDATE ON tenants\n  FOR EACH ROW EXECUTE FUNCTION trg_billing_currency_immutable();\n\n-- Trigger: founder tenants cannot have stripe_customer_id populated (DEC-805)\nCREATE OR REPLACE FUNCTION trg_founder_no_stripe() RETURNS trigger AS $$\nBEGIN\n  IF NEW.is_founder_tenant = true AND NEW.stripe_customer_id IS NOT NULL THEN\n    RAISE EXCEPTION 'founder_cannot_stripe: is_founder_tenant=true tenants cannot have stripe_customer_id (DEC-805)';\n  END IF;\n  IF NEW.billing_currency = 'VND' AND NEW.stripe_customer_id IS NOT NULL THEN\n    RAISE EXCEPTION 'wrong_billing_rail: VND tenants cannot have stripe_customer_id (DEC-784)';\n  END IF;\n  RETURN NEW;\nEND $$ LANGUAGE plpgsql;\nCREATE TRIGGER tenants_founder_no_stripe\n  BEFORE INSERT OR UPDATE ON tenants\n  FOR EACH ROW EXECUTE FUNCTION trg_founder_no_stripe();\n\n-- 0009_billing_currency_enum.sql (run BEFORE 0006)\nCREATE TYPE billing_currency_enum AS ENUM ('VND','USD','EUR','SGD','GBP');\n\n-- 0007_stripe_api_calls.sql\nCREATE TABLE stripe_api_calls (\n  idempotency_key TEXT PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  operation TEXT NOT NULL,\n  request_sha256 CHAR(64) NOT NULL,\n  response_status INT,\n  response_body_sha256 CHAR(64),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  ttl_until TIMESTAMPTZ NOT NULL DEFAULT now() + INTERVAL '7 days'\n);\nALTER TABLE stripe_api_calls ENABLE ROW LEVEL SECURITY;\nCREATE POLICY stripe_api_calls_rls ON stripe_api_calls\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON stripe_api_calls FROM cyberos_app;\n-- cyberos_pruner is the existing scheduled-job role defined in TASK-AUTH-003 §3.4;\n-- its DELETE grant is scoped to this table for TTL pruning (DEC-807):\nGRANT DELETE ON stripe_api_calls TO cyberos_pruner;\n\n-- 0008_stripe_event_dispatch_log.sql\nCREATE TABLE stripe_event_dispatch_log (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  stripe_event_id TEXT NOT NULL,\n  event_type TEXT NOT NULL,\n  dispatch_status TEXT NOT NULL CHECK (dispatch_status IN ('dispatched','duplicate','failed')),\n  failure_reason TEXT,\n  dispatched_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE(tenant_id, stripe_event_id)\n);\nALTER TABLE stripe_event_dispatch_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY sed_log_rls ON stripe_event_dispatch_log\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON stripe_event_dispatch_log FROM cyberos_app;\n\n-- 0010_stripe_price_map.sql\nCREATE TABLE stripe_price_map (\n  residency TEXT NOT NULL CHECK (residency IN ('sg-1','eu-1','us-1','vn-1')),\n  currency billing_currency_enum NOT NULL,\n  plan_tier plan_tier NOT NULL,\n  axis TEXT NOT NULL CHECK (axis IN ('base','overage_api_calls','overage_ai_tokens','overage_storage','overage_seats')),\n  stripe_price_id TEXT NOT NULL,\n  synced_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  PRIMARY KEY (residency, currency, plan_tier, axis)\n);\nREVOKE UPDATE, DELETE ON stripe_price_map FROM cyberos_app;\n-- Note: stripe_price_map is global config; no RLS (read-all-tenants).</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/ten/src/billing/stripe/price_catalog.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug)]\npub enum Currency { Vnd, Usd, Eur, Sgd, Gbp }\n\n#[derive(Copy, Clone, Eq, PartialEq, Debug)]\npub enum PlanTier { Starter, Team, Enterprise }\n\n#[derive(Copy, Clone, Debug)]\npub struct PriceEntry {\n    pub currency: Currency,\n    pub tier: PlanTier,\n    pub amount_minor: i64,\n}\n\npub const PRICE_CATALOG: [PriceEntry; 12] = [\n    PriceEntry { currency: Currency::Usd, tier: PlanTier::Starter,    amount_minor: 1_900 },\n    PriceEntry { currency: Currency::Usd, tier: PlanTier::Team,       amount_minor: 9_900 },\n    PriceEntry { currency: Currency::Usd, tier: PlanTier::Enterprise, amount_minor: 99_900 },\n    PriceEntry { currency: Currency::Eur, tier: PlanTier::Starter,    amount_minor: 1_900 },\n    PriceEntry { currency: Currency::Eur, tier: PlanTier::Team,       amount_minor: 9_900 },\n    PriceEntry { currency: Currency::Eur, tier: PlanTier::Enterprise, amount_minor: 99_900 },\n    PriceEntry { currency: Currency::Sgd, tier: PlanTier::Starter,    amount_minor: 2_500 },\n    PriceEntry { currency: Currency::Sgd, tier: PlanTier::Team,       amount_minor: 13_000 },\n    PriceEntry { currency: Currency::Sgd, tier: PlanTier::Enterprise, amount_minor: 130_000 },\n    PriceEntry { currency: Currency::Gbp, tier: PlanTier::Starter,    amount_minor: 1_500 },\n    PriceEntry { currency: Currency::Gbp, tier: PlanTier::Team,       amount_minor: 7_900 },\n    PriceEntry { currency: Currency::Gbp, tier: PlanTier::Enterprise, amount_minor: 79_900 },\n];\n\npub fn price_for(currency: Currency, tier: PlanTier) -&gt; Option&lt;&amp;'static PriceEntry&gt; {\n    PRICE_CATALOG.iter().find(|e| e.currency == currency &amp;&amp; e.tier == tier)\n}\n\n// services/ten/src/billing/stripe/api_client.rs\npub struct StripeClient {\n    residency: Residency,\n    api_key: SecretString,  // KMS-loaded per DEC-801\n    http: reqwest::Client,\n    idempotency_repo: IdempotencyRepo,\n}\n\nimpl StripeClient {\n    pub async fn call&lt;T: serde::de::DeserializeOwned&gt;(\n        &amp;self,\n        method: reqwest::Method,\n        endpoint: &amp;str,\n        idempotency_key: &amp;str,\n        body: &amp;impl serde::Serialize,\n    ) -&gt; Result&lt;T, StripeError&gt; {\n        // Check idempotency cache; if hit, return cached response_body\n        // Otherwise: POST with Idempotency-Key header, persist to stripe_api_calls\n        // Honour Retry-After on 429; exponential backoff on 5xx (1,2,4,8,16s)\n        // After 5 retries: memory audit `ten.stripe_api_call_failed` + return Err\n    }\n}\n\n// services/ten/src/billing/dunning.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug)]\npub enum DunningState { Ok, Retry1, Retry2, Retry3, Suspended }\n\nimpl DunningState {\n    pub fn advance_on_payment_failed(self) -&gt; DunningState {\n        match self {\n            DunningState::Ok       =&gt; DunningState::Retry1,\n            DunningState::Retry1   =&gt; DunningState::Retry2,\n            DunningState::Retry2   =&gt; DunningState::Retry3,\n            DunningState::Retry3   =&gt; DunningState::Suspended,\n            DunningState::Suspended =&gt; DunningState::Suspended,  // terminal\n        }\n    }\n    pub fn reset_on_payment_succeeded(self) -&gt; DunningState {\n        DunningState::Ok\n    }\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">POST   /v1/admin/tenants/{id}/billing/refund    (CFO-only)\nGET    /v1/admin/tenants/{id}/billing            (tenant_admin or CFO)</code></pre>\n<p>Refund request body:</p>\n<pre><code class=\"language-json\">{ \"stripe_charge_id\": \"ch_3OabcXYZ...\", \"amount_minor\": 9900, \"currency\": \"USD\",\n  \"reason\": \"requested_by_customer\" }</code></pre>\n<p>Refund response (201):</p>\n<pre><code class=\"language-json\">{ \"refund_id\": \"re_3PdefABC...\", \"status\": \"succeeded\",\n  \"expected_settlement_at\": \"2026-05-22T00:00:00Z\" }</code></pre>\n<h3>3.4 CLI</h3>\n<pre><code class=\"language-text\">cyberos-ten stripe-sync-prices [--dry-run | --apply] [--residency sg-1|eu-1|us-1]</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Currency lock at provisioning</strong> — provisioning a tenant with <code>--billing-currency USD</code> sets <code>tenants.billing_currency='USD'</code> and <code>billing_rail='stripe'</code>; subsequent <code>UPDATE tenants SET billing_currency='EUR'</code> raises trigger error <code>billing_currency_immutable</code>.</li><li><strong>Stripe Customer lazy + idempotent</strong> — first plan_change on a stripe-rail tenant creates one Stripe Customer; concurrent first plan_change attempts produce exactly one Customer (race-safe); re-invoking ensure_customer is a no-op.</li><li><strong>Subscription create with metered overage items</strong> — <code>ensure_subscription(tenant, Team)</code> creates a Stripe Subscription with one base item (Team price) and 4 metered items (one per overage axis); <code>tenants.stripe_subscription_id</code> populated.</li><li><strong>Plan upgrade prorates immediately</strong> — plan_change Starter→Team triggers Stripe <code>POST /v1/subscriptions/{id}</code> with <code>proration_behavior=create_prorations</code>; next invoice line items include proration amount.</li><li><strong>Plan downgrade defers via Schedule</strong> — plan_change Team→Starter triggers Stripe Subscription Schedule with <code>effective_at = next_period_boundary</code> and <code>proration_behavior=none</code>; current period unchanged.</li><li><strong>Overage push at period_close</strong> — TASK-TEN-004 period_close with <code>api_calls actual=550_000, cap=500_000</code> triggers Stripe usage_record with <code>quantity=50_000</code> and idempotency_key <code>ten.&lt;tid&gt;.overage_push.api_calls.&lt;period_end_unix&gt;</code>.</li><li><strong>Dispatcher idempotent on duplicate webhook</strong> — same <code>(tenant_id, stripe_event_id)</code> arriving twice via NATS produces one dispatch + one <code>dispatch_status='duplicate'</code> log row + no double-side-effect.</li><li><strong>Dunning state machine</strong> — three <code>invoice.payment_failed</code> events advance <code>dunning_state</code> ok→retry_1→retry_2→retry_3; fourth advances to <code>suspended</code> + triggers TASK-TEN-104 suspend transition.</li><li><strong>Un-suspend on payment_succeeded</strong> — tenant in <code>suspended</code> state + <code>invoice.payment_succeeded</code> event → <code>dunning_state='ok'</code> + TASK-TEN-104 resume transition.</li><li><strong>Refund CFO-only</strong> — POST refund with non-cfo subject returns 403 <code>forbidden</code>; with cfo subject returns 201 + emits sev-1 memory row <code>ten.stripe_refund_issued</code>.</li><li><strong>Refund amount cap</strong> — POST refund with <code>amount_minor &gt; original_charge_amount</code> returns 400 <code>refund_exceeds_charge</code>.</li><li><strong>Founder skip</strong> — founder tenant plan_change → no Stripe API call invoked; sev-3 memory row <code>ten.stripe_founder_skip</code> emitted.</li><li><strong>VND tenant cross-rail rejection</strong> — VND tenant invoking Stripe rail returns 400 <code>wrong_billing_rail</code> + no Stripe API call.</li><li><strong>API client retry on 5xx</strong> — Stripe 503 response triggers backoff 1s, 2s, 4s, 8s, 16s; sixth attempt fails permanently + emits <code>ten.stripe_api_call_failed</code> sev-2.</li><li><strong>API client honours Retry-After on 429</strong> — Stripe 429 + <code>Retry-After: 30</code> causes client to sleep 30s before retry.</li><li><strong>Price catalog cardinality</strong> — <code>stripe_price_catalog_cardinality_test</code> asserts <code>PRICE_CATALOG.len() == 12</code> and every entry has unique <code>(currency, tier)</code>.</li><li><strong>Residency API key routing</strong> — sg-1 tenant Stripe call uses <code>STRIPE_API_KEY_SG</code>; eu-1 uses <code>STRIPE_API_KEY_EU</code>; us-1 uses <code>STRIPE_API_KEY_US</code>; mis-route detected by sentinel-test calling against per-residency Stripe test accounts.</li><li><strong>Idempotency cache hit returns cached response</strong> — invoking same <code>Idempotency-Key</code> twice within 7d returns cached <code>response_body_sha256</code> without re-calling Stripe.</li><li><strong>PII scrubbing in memory chain</strong> — refund audit row carries <code>billing_contact_email_hash16</code> not raw email; reason field scrubbed via TASK-MEMORY-111.</li><li><strong>W3C trace_id propagated</strong> — every Stripe audit row carries 32-char hex <code>trace_id</code> matching the upstream caller's traceparent header.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>stripe_currency_lock_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn billing_currency_immutable() {\n    let ctx = TestContext::new().await;\n    let tenant_id = provision_tenant(&amp;ctx, \"acme\", \"USD\").await;\n    let res = sqlx::query(\"UPDATE tenants SET billing_currency='EUR' WHERE id=$1\")\n        .bind(tenant_id).execute(&amp;ctx.pool).await;\n    assert!(res.is_err());\n    assert!(res.unwrap_err().to_string().contains(\"billing_currency_immutable\"));\n}</code></pre>\n<h3>5.2 <code>stripe_customer_idempotency_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn ensure_customer_idempotent_under_concurrency() {\n    let ctx = TestContext::new().await;\n    let tenant_id = provision_tenant(&amp;ctx, \"acme\", \"USD\").await;\n    let client = StripeClient::test_mode(&amp;ctx);\n\n    let (r1, r2, r3) = tokio::join!(\n        ensure_customer(&amp;ctx, &amp;client, tenant_id),\n        ensure_customer(&amp;ctx, &amp;client, tenant_id),\n        ensure_customer(&amp;ctx, &amp;client, tenant_id),\n    );\n    let id1 = r1.unwrap(); let id2 = r2.unwrap(); let id3 = r3.unwrap();\n    assert_eq!(id1, id2); assert_eq!(id2, id3);\n\n    let count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM tenants WHERE stripe_customer_id IS NOT NULL AND id=$1\")\n        .bind(tenant_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(count, 1);\n}</code></pre>\n<h3>5.3 <code>stripe_dunning_state_machine_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn dunning_advances_then_suspends() {\n    let ctx = TestContext::new().await;\n    let tenant_id = provision_tenant(&amp;ctx, \"acme\", \"USD\").await;\n    activate_subscription(&amp;ctx, tenant_id, PlanTier::Team).await;\n\n    for expected in &amp;[DunningState::Retry1, DunningState::Retry2, DunningState::Retry3] {\n        dispatch_invoice_payment_failed(&amp;ctx, tenant_id).await;\n        assert_eq!(load_dunning(&amp;ctx, tenant_id).await, *expected);\n    }\n    dispatch_invoice_payment_failed(&amp;ctx, tenant_id).await;\n    assert_eq!(load_dunning(&amp;ctx, tenant_id).await, DunningState::Suspended);\n\n    let status: String = sqlx::query_scalar(\"SELECT status::text FROM tenants WHERE id=$1\")\n        .bind(tenant_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(status, \"suspended\");\n\n    let audit_rows = load_memory_rows_for_tenant(&amp;ctx, tenant_id).await;\n    assert!(audit_rows.iter().any(|r| r.kind == \"ten.tenant_billing_suspended\"));\n}</code></pre>\n<h3>5.4 <code>stripe_founder_skip_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn founder_tenant_never_calls_stripe() {\n    let ctx = TestContext::new().await;\n    let founder = provision_founder_tenant(&amp;ctx).await;\n    let stripe_mock = ctx.stripe_mock();\n\n    change_plan(&amp;ctx, founder, PlanTier::Team).await.unwrap();\n    assert_eq!(stripe_mock.calls(), 0);\n\n    let audit = load_memory_rows_for_tenant(&amp;ctx, founder).await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.stripe_founder_skip\"));\n}</code></pre>\n<h3>5.5 <code>stripe_api_retry_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn five_xx_triggers_exponential_backoff() {\n    let ctx = TestContext::new().await;\n    let mock = ctx.stripe_mock_returning(503).times(5).then(200);\n    let client = StripeClient::with_mock(&amp;ctx, mock);\n    let start = Instant::now();\n\n    let res = client.call_subscription_create(/* ... */).await;\n    let elapsed = start.elapsed();\n\n    assert!(res.is_ok());\n    assert!(elapsed &gt;= Duration::from_secs(1 + 2 + 4 + 8 + 16));\n    assert!(elapsed &lt; Duration::from_secs(60));\n}\n\n#[tokio::test]\nasync fn six_consecutive_5xx_emits_sev2() {\n    let ctx = TestContext::new().await;\n    let mock = ctx.stripe_mock_returning(503).times(6);\n    let client = StripeClient::with_mock(&amp;ctx, mock);\n\n    let res = client.call_subscription_create(/* ... */).await;\n    assert!(matches!(res, Err(StripeError::Unavailable { .. })));\n    let audit = load_memory_rows(&amp;ctx).await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.stripe_api_call_failed\" &amp;&amp; r.severity == 2));\n}</code></pre>\n<h3>5.6 <code>stripe_price_catalog_cardinality_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[test]\nfn price_catalog_has_12_entries_one_per_currency_tier() {\n    assert_eq!(PRICE_CATALOG.len(), 12);\n    let mut seen = std::collections::HashSet::new();\n    for e in &amp;PRICE_CATALOG {\n        assert!(seen.insert((e.currency, e.tier)), \"duplicate ({:?},{:?})\", e.currency, e.tier);\n        assert!(e.amount_minor &gt; 0);\n    }\n    let currencies = [Currency::Usd, Currency::Eur, Currency::Sgd, Currency::Gbp];\n    for c in &amp;currencies {\n        for t in &amp;[PlanTier::Starter, PlanTier::Team, PlanTier::Enterprise] {\n            assert!(seen.contains(&amp;(*c, *t)), \"missing ({:?},{:?})\", c, t);\n        }\n    }\n}</code></pre>\n<h3>5.7 <code>stripe_refund_cfo_only_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn refund_requires_cfo_role() {\n    let ctx = TestContext::new().await;\n    let tenant = provision_tenant(&amp;ctx, \"acme\", \"USD\").await;\n    let tenant_admin_token = mint_jwt(&amp;ctx, tenant, \"tenant_admin\");\n\n    let res = post_refund(&amp;ctx, tenant, &amp;tenant_admin_token, 1900).await;\n    assert_eq!(res.status(), 403);\n\n    let cfo_token = mint_jwt(&amp;ctx, tenant, \"chief-financial-officer\");\n    let res = post_refund(&amp;ctx, tenant, &amp;cfo_token, 1900).await;\n    assert_eq!(res.status(), 201);\n    let audit = load_memory_rows_for_tenant(&amp;ctx, tenant).await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.stripe_refund_issued\" &amp;&amp; r.severity == 1));\n}\n\n#[tokio::test]\nasync fn refund_amount_cannot_exceed_charge() {\n    let ctx = TestContext::new().await;\n    let tenant = provision_tenant(&amp;ctx, \"acme\", \"USD\").await;\n    let cfo_token = mint_jwt(&amp;ctx, tenant, \"chief-financial-officer\");\n    seed_charge(&amp;ctx, tenant, 1900).await;\n\n    let res = post_refund(&amp;ctx, tenant, &amp;cfo_token, 9999).await;\n    assert_eq!(res.status(), 400);\n    let body: serde_json::Value = res.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"refund_exceeds_charge\");\n}</code></pre>\n<h3>5.8 <code>stripe_dispatcher_idempotency_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn duplicate_webhook_dispatches_once() {\n    let ctx = TestContext::new().await;\n    let tenant = provision_tenant(&amp;ctx, \"acme\", \"USD\").await;\n    activate_subscription(&amp;ctx, tenant, PlanTier::Team).await;\n\n    let event_id = \"evt_3OabcXYZ\";\n    publish_stripe_event(&amp;ctx, tenant, event_id, \"invoice.payment_succeeded\").await;\n    publish_stripe_event(&amp;ctx, tenant, event_id, \"invoice.payment_succeeded\").await;\n\n    let dispatched: Vec&lt;(String, String)&gt; = sqlx::query_as(\n        \"SELECT stripe_event_id, dispatch_status FROM stripe_event_dispatch_log WHERE tenant_id=$1 ORDER BY id\"\n    ).bind(tenant).fetch_all(&amp;ctx.pool).await.unwrap();\n\n    assert_eq!(dispatched.len(), 2);\n    assert_eq!(dispatched[0].1, \"dispatched\");\n    assert_eq!(dispatched[1].1, \"duplicate\");\n}</code></pre>\n<h3>5.9 <code>stripe_overage_push_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn period_close_pushes_overage_within_one_hour() {\n    let ctx = TestContext::new().await;\n    let tenant = provision_tenant(&amp;ctx, \"acme\", \"USD\").await;\n    activate_subscription(&amp;ctx, tenant, PlanTier::Team).await;\n    seed_metering(&amp;ctx, tenant, \"api_calls\", 550_000).await;\n\n    let period_end = chrono::Utc::now().timestamp();\n    run_period_close(&amp;ctx, tenant, period_end).await;\n\n    let calls = ctx.stripe_mock().calls_to(\"usage_records\");\n    assert_eq!(calls.len(), 4); // one per axis; only api_calls is over cap\n    let api_call = calls.iter().find(|c| c.body[\"quantity\"] == 50_000).unwrap();\n    let key = api_call.headers[\"Idempotency-Key\"].as_str();\n    assert_eq!(key, format!(\"ten.{tenant}.overage_push.api_calls.{period_end}\"));\n}</code></pre>\n<h3>5.10 <code>stripe_residency_apikey_routing_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn sg_tenant_uses_sg_api_key() {\n    let ctx = TestContext::new().await;\n    let sg_tenant = provision_tenant_with_residency(&amp;ctx, \"acme\", \"SGD\", \"sg-1\").await;\n    let calls = ctx.stripe_mock().calls();\n    ensure_customer(&amp;ctx, &amp;client_for(&amp;ctx, \"sg-1\"), sg_tenant).await.unwrap();\n    assert_eq!(calls.last().unwrap().auth, ctx.kms_decrypt(\"STRIPE_API_KEY_SG\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract in §3 is the skeleton. Additional orchestrator wiring below.)</p>\n<h3>6.1 plan_change handler wire-up (modified <code>services/ten/src/handlers/plan_change.rs</code>)</h3>\n<pre><code class=\"language-rust\">// AFTER the existing tenant_plan_history INSERT commits:\nif tenant.billing_rail == BillingRail::Stripe {\n    let client = StripeClient::for_residency(tenant.residency).await?;\n    billing::stripe::subscription::push_plan_change(\n        &amp;ctx, &amp;client, tenant_id, history_id, from_tier, to_tier\n    ).await?;\n}\n// For internal (founder) and vietqr_momo_zalo rails, no-op or TASK-TEN-102 path.</code></pre>\n<h3>6.2 period_close hook wire-up (modified <code>services/metering/src/handlers/period_close.rs</code>)</h3>\n<pre><code class=\"language-rust\">// AFTER aggregation completes, for stripe-rail tenants only:\nif tenant.billing_rail == BillingRail::Stripe {\n    spawn_with_deadline(Duration::from_secs(3600), async move {\n        billing::stripe::overage::push_overage_for_period(\n            &amp;ctx, tenant_id, period_end_unix\n        ).await\n    });\n}</code></pre>\n<h3>6.3 NATS dispatcher (new <code>services/ten/src/billing/stripe/dispatch.rs</code>)</h3>\n<pre><code class=\"language-rust\">pub async fn run_dispatcher(ctx: AppCtx) {\n    let sub = ctx.nats.subscribe(\"tenant.*.ten.stripe.*\").await.unwrap();\n    while let Some(msg) = sub.next().await {\n        let event: StripeWebhookEvent = serde_json::from_slice(&amp;msg.payload).unwrap();\n        let dispatched = ctx.repo.stripe_event_dispatch_log\n            .upsert_idempotent(event.tenant_id, event.stripe_event_id, &amp;event.event_type).await;\n        if dispatched.is_new {\n            match event.event_type.as_str() {\n                \"invoice.payment_succeeded\" =&gt; handle_payment_succeeded(&amp;ctx, event).await,\n                \"invoice.payment_failed\"    =&gt; handle_payment_failed(&amp;ctx, event).await,\n                \"invoice.finalized\"         =&gt; handle_invoice_finalized(&amp;ctx, event).await,\n                \"customer.subscription.updated\"   =&gt; handle_subscription_updated(&amp;ctx, event).await,\n                \"customer.subscription.deleted\"   =&gt; handle_subscription_deleted(&amp;ctx, event).await,\n                _ =&gt; {}  // unrelated events ignored\n            }\n        }\n        // duplicate path: log only; no side effect\n    }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>TASK-INV-003</strong> Stripe webhook handler — signature-verified inbound events; bridges to TEN via NATS publish on relevant kinds.</li><li><strong>TASK-TEN-002</strong> Plan tiers + plan_change handler — TIER_PRICE_CENTS_MONTHLY constants + plan_change trigger point.</li><li><strong>TASK-TEN-004</strong> 4-axis metering + period_close — overage push hook + per-axis usage quantities.</li></ul>\n<p><strong>Cross-module (related_tasks):</strong></p>\n<ul><li><strong>TASK-TEN-001</strong> Provisioning — billing_currency captured at provisioning; billing_rail derived.</li><li><strong>TASK-TEN-101</strong> Self-serve signup — depends on TEN-003 being able to bill (TEN-003 blocks TEN-101).</li><li><strong>TASK-TEN-102</strong> VND domestic rail — parallel rail; TEN-003 ships first to establish rail abstraction (TEN-003 blocks TEN-102).</li><li><strong>TASK-TEN-103</strong> 4-residency provisioning — per-residency Stripe API key map consumed by TEN-003.</li><li><strong>TASK-TEN-104</strong> Tenant lifecycle (suspended/terminating) — dunning advances trigger TASK-TEN-104 status transitions.</li><li><strong>TASK-INV-006</strong> Cash application — reconciles Stripe receipts against TEN subscription invoices.</li><li><strong>TASK-AUTH-101</strong> RBAC catalogue — <code>cfo</code> and <code>tenant_admin</code> role gates.</li><li><strong>TASK-AI-003</strong> memory audit-row bridge — 11 new kinds register here.</li><li><strong>TASK-MEMORY-111</strong> PII scrubbing ruleset — billing_contact_email + reason text.</li><li><strong>TASK-OBS-007</strong> Auto-runbook — sev-1/sev-2 dunning + API-failure alerts route to CHAT/PagerDuty.</li></ul>\n<p><strong>Downstream (blocks):</strong></p>\n<ul><li><strong>TASK-TEN-102</strong> — VND rail follows the abstraction this task establishes.</li><li><strong>TASK-TEN-101</strong> — self-serve signup requires automated billing rail.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>ten.stripe_customer_created</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.stripe_customer_created\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.ten.billing\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"stripe_customer_id\": \"cus_3OabcXYZ\",\n    \"currency\": \"USD\",\n    \"residency\": \"us-1\",\n    \"billing_contact_email_hash16\": \"f8a1b2c3d4e5f607\",\n    \"stripe_api_call_idempotency_key\": \"ten.8a2f....customer_create.v1\"\n  }\n}</code></pre>\n<h3>8.2 Refund request</h3>\n<pre><code class=\"language-json\">{\n  \"stripe_charge_id\": \"ch_3OabcXYZ012345\",\n  \"amount_minor\": 9900,\n  \"currency\": \"USD\",\n  \"reason\": \"requested_by_customer\"\n}</code></pre>\n<h3>8.3 Refund response (201)</h3>\n<pre><code class=\"language-json\">{\n  \"refund_id\": \"re_3PdefABC987654\",\n  \"status\": \"succeeded\",\n  \"expected_settlement_at\": \"2026-05-22T00:00:00Z\",\n  \"audit_row_chain_hash\": \"9c4e7a8b6d2f1e3a5b9c7d4e6f8a2b1c4d7e9f3a6b8c2d5e7f9a1b3c5d7e9f1a\"\n}</code></pre>\n<h3>8.4 <code>ten.stripe_overage_pushed</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.stripe_overage_pushed\",\n  \"severity\": 3,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.metering.period_close\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-06-01T00:03:12.118Z\",\n  \"payload\": {\n    \"axis\": \"api_calls\",\n    \"overage_quantity\": 50000,\n    \"period_end_unix\": 1748736000,\n    \"stripe_usage_record_id\": \"ur_3Q1zxyABC\",\n    \"stripe_subscription_item_id\": \"si_3Pabc456\",\n    \"stripe_api_call_idempotency_key\": \"ten.8a2f....overage_push.api_calls.1748736000\"\n  }\n}</code></pre>\n<h3>8.5 <code>GET /v1/admin/tenants/{id}/billing</code> response</h3>\n<pre><code class=\"language-json\">{\n  \"tenant_id\": \"8a2f...\",\n  \"billing_rail\": \"stripe\",\n  \"billing_currency\": \"USD\",\n  \"stripe_customer_id\": \"cus_3OabcXYZ\",\n  \"stripe_subscription_id\": \"sub_3Pabc456\",\n  \"current_plan_tier\": \"team\",\n  \"dunning_state\": \"ok\",\n  \"billing_cycle_anchor\": \"2026-05-17T00:00:00Z\",\n  \"next_invoice_date\": \"2026-06-17T00:00:00Z\",\n  \"recent_invoices\": [\n    { \"stripe_invoice_id\": \"in_3PXY...\", \"amount_minor\": 9900, \"currency\": \"USD\",\n      \"status\": \"paid\", \"period_start\": \"2026-04-17T00:00:00Z\", \"period_end\": \"2026-05-17T00:00:00Z\" }\n  ],\n  \"recent_dunning_events\": []\n}</code></pre>\n<h3>8.6 NATS subject example (TASK-INV-003 → TEN dispatcher bridge)</h3>\n<pre><code class=\"language-text\">Subject: tenant.acme-sg.ten.stripe.invoice.payment_succeeded\nPayload:\n{\n  \"tenant_id\": \"8a2f...\",\n  \"stripe_event_id\": \"evt_3OabcXYZ\",\n  \"event_type\": \"invoice.payment_succeeded\",\n  \"stripe_invoice_id\": \"in_3PXY...\",\n  \"stripe_customer_id\": \"cus_3OabcXYZ\",\n  \"amount_paid_minor\": 9900,\n  \"currency\": \"USD\",\n  \"received_at\": \"2026-05-17T09:14:33.001Z\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 2. Deferred to later slices:</p>\n<ul><li><strong>Deferred:</strong> Stripe Tax integration for EU VAT — slice 3, task-TEN-1xx (placeholder — not yet specified).</li><li><strong>Deferred:</strong> Multi-currency support per tenant (currency change without new tenant) — out-of-scope per DEC-798; new tenant + manual migration is the documented path.</li><li><strong>Deferred:</strong> Stripe Connect / marketplace flows for task-PORTAL-XXX — out-of-scope; client tenants not yet billed-through their portal (P4 milestone).</li><li><strong>Deferred:</strong> Real-time invoice preview before plan_change commits — slice 3 enhancement; for now CFO uses Stripe Dashboard.</li><li><strong>Deferred:</strong> SEPA Direct Debit / BACS / ACH local-payment rails — slice 3+ (task-TEN-2xx); slice 2 is card-only via Stripe Payment Intents.</li><li><strong>Deferred:</strong> Annual billing cycle (slice 2 is monthly only) — slice 3, task-TEN-1xx.</li><li><strong>Deferred:</strong> Coupon / promo code support — slice 3, task-TEN-1xx.</li><li><strong>Deferred:</strong> Reconciliation job comparing TEN dunning_state vs Stripe subscription.status (slice 3 per §11.13).</li><li><strong>Deferred:</strong> Hourly overage push (vs once-per-period) — slice 3 per §11.10; reduces revenue-recognition lag.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Stripe API 5xx persistent (&gt; 5 retries)</td><td>api_client error counter; OTel span error rate</td><td>Operation returns 502 to caller; sev-2 memory row <code>ten.stripe_api_call_failed</code></td><td>Manual retry via <code>cyberos-ten stripe-retry-failed --since &lt;ts&gt;</code> (operator CLI in slice 3); for slice 2, CFO re-invokes the operation from Stripe Dashboard manually</td></tr><tr><td>Stripe webhook arrives before TEN customer record exists</td><td>dispatcher lookup <code>tenants WHERE stripe_customer_id=$1</code> returns 0 rows</td><td>sev-2 alert <code>ten.stripe_orphan_webhook</code>; dispatch_status='failed' with reason='no_matching_tenant'; webhook ack'd to Stripe (200) so no retry storm</td><td>Operator investigates via Stripe Dashboard; may indicate test-mode webhook hit prod or customer manually deleted</td></tr><tr><td>Duplicate Stripe Customer for one tenant (race lost)</td><td>partial unique index <code>uniq_stripe_customer</code> rejects second INSERT</td><td>Second concurrent call sees Postgres unique violation; api_client retries the SELECT path; returns existing customer_id</td><td>Inherent race-safety — partial unique index + SELECT FOR UPDATE pattern prevents persistent duplicates</td></tr><tr><td>billing_currency mismatch between webhook invoice and tenant record</td><td>dispatch handler checks <code>event.currency == tenant.billing_currency</code></td><td>sev-1 alert <code>ten.stripe_currency_drift</code>; webhook ack'd; dispatch_status='failed'</td><td>Manual reconciliation; likely indicates wrong-residency Stripe key used at customer creation — operator audit</td></tr><tr><td>Idempotency-Key collision on retry within 7d</td><td>stripe_api_calls table lookup hits</td><td>Cached response returned; no Stripe call; OTel span attribute <code>idempotency.hit=true</code></td><td>Inherent — idempotency is the desired behaviour</td></tr><tr><td>Idempotency cache expired (7d) but Stripe still has key (24h)</td><td>api_client makes new call; Stripe returns previous response</td><td>Stripe response cached fresh; behaviour identical to original</td><td>Inherent — Stripe's idempotency window &gt; ours; safe</td></tr><tr><td>Subscription Schedule downgrade conflicts with later upgrade</td><td>plan_change handler check for existing Subscription Schedule</td><td>New plan_change cancels prior Subscription Schedule via Stripe API; emits sev-2 <code>ten.stripe_schedule_cancelled</code></td><td>Operator informed; if upgrade after pending downgrade, the upgrade takes effect immediately + downgrade cancelled</td></tr><tr><td>Overage push failure after period_close (1h window exhausted)</td><td>overage push retry loop hits 24h deadline</td><td>sev-1 alert <code>ten.stripe_overage_push_failed</code>; period_close marked as <code>overage_push_pending</code></td><td>CFO manually pushes via Stripe Dashboard usage_records API; subsequent period_close reconciles</td></tr><tr><td>NATS dispatcher fails to consume (crash, network)</td><td>NATS lag metric; OBS dashboard</td><td>Backlog grows; webhooks ack'd to Stripe but not actioned in TEN</td><td>NATS replay from durable subject (slice 2 uses NATS JetStream with 7d retention); dispatcher resumes from last acked offset</td></tr><tr><td>Stripe Price ID drift (rev'd in Stripe Dashboard without sync)</td><td>next subscription_create sees mismatch between catalog amount and Stripe price.unit_amount</td><td>sev-2 alert <code>ten.stripe_price_drift</code>; operation proceeds with Stripe's value (Stripe is source of truth at API call time); memory row notes drift</td><td>CFO re-runs <code>cyberos-ten stripe-sync-prices --apply</code> to align catalog → Stripe; never the other direction</td></tr><tr><td>KMS unavailable when api_client constructor runs</td><td>KMS client timeout 5s</td><td>Constructor returns Err; calling handler returns 503; sev-2 memory row <code>ten.stripe_kms_unavailable</code></td><td>Inherent KMS retry on next request; if persistent, AWS KMS incident — page on-call</td></tr><tr><td>Tenant terminated mid-period (TASK-TEN-104) with active Stripe subscription</td><td>TASK-TEN-104 termination handler calls <code>services/ten/src/billing/stripe/subscription.rs::cancel(tenant_id)</code></td><td>Stripe subscription cancelled immediately (NOT cancel_at_period_end — termination is hard); pro-rata credit calculated by Stripe; emits <code>ten.stripe_subscription_cancelled</code> sev-1</td><td>TASK-TEN-104 termination flow handles refund decision (refund unused period vs. forfeit per ToS)</td></tr><tr><td>Webhook secret rotation mid-flight (TASK-INV-003 60s overlap)</td><td>Stripe sends event during overlap; TASK-INV-003 accepts both</td><td>Both old and new secret valid; no impact on TEN dispatcher (sees signed events either way)</td><td>Inherent overlap window</td></tr><tr><td>Founder tenant accidentally flipped to non-internal rail</td><td>guard at api_client checks <code>tenant.billing_rail</code></td><td>api_client returns Err <code>founder_cannot_stripe</code>; sev-1 memory row</td><td>Manual SQL fix + DEC entry explaining how the flip happened; trigger added in slice 3 to prevent UPDATE on is_founder_tenant</td></tr><tr><td>VND tenant configured stripe_customer_id by mistake</td><td>API client guard checks <code>billing_currency != VND</code></td><td>Returns 400 <code>wrong_billing_rail</code>; no Stripe call</td><td>Manual SQL cleanup + DEC entry; trigger added in slice 3 to prevent stripe_customer_id population on VND tenants</td></tr><tr><td>Dunning state machine drift (Stripe state ≠ our state)</td><td>reconciliation job (slice 3) compares Stripe subscription.status with tenants.dunning_state</td><td>sev-2 alert <code>ten.stripe_state_drift</code>; manual reconciliation required</td><td>CFO consults Stripe Dashboard + invokes <code>cyberos-ten dunning-reconcile &lt;tenant&gt;</code> (slice 3)</td></tr><tr><td>Refund attempted on already-refunded charge</td><td>Stripe returns 400 <code>charge_already_refunded</code></td><td>api_client returns Err; refund handler returns 400 <code>refund_failed</code> with Stripe error</td><td>CFO consults Stripe Dashboard; possibly attempting partial refund on already-fully-refunded — Stripe is source of truth</td></tr><tr><td>Stripe customer email mismatch on update (PII drift)</td><td>dispatcher detects <code>customer.updated</code> with email change</td><td>sev-3 memory row <code>ten.stripe_customer_email_changed</code>; tenants.billing_contact_email NOT auto-updated</td><td>Operator review; manual UPDATE if intentional</td></tr><tr><td>Subscription items list drift (extra metered items in Stripe)</td><td>reconciliation comparing our PRICE_CATALOG against Stripe subscription.items</td><td>sev-2 alert <code>ten.stripe_items_drift</code></td><td>Operator runs <code>cyberos-ten stripe-rebuild-subscription &lt;tenant&gt;</code> (slice 3) to align items</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> <code>async-stripe</code> crate is well-maintained but pin to exact version; Stripe API versions are date-stamped — our <code>Stripe-Version</code> header sent on every call (currently <code>2024-06-20</code> baseline). Bumping the date is an ADR with regression-test sweep.</p>\n<p><strong>§11.2</strong> The <code>billing/stripe/api_client.rs</code> retry logic uses the <code>backoff</code> crate's <code>ExponentialBackoff</code> builder; we customise to honour <code>Retry-After</code> over the backoff schedule when set (Stripe's <code>Retry-After</code> is gospel).</p>\n<p><strong>§11.3</strong> Idempotency keys MAX 255 chars per Stripe; UUID v4 + colon-delimited operation tag fits comfortably (<code>ten.&lt;36-char-uuid&gt;.&lt;operation&gt;.&lt;suffix&gt;</code> ~80 chars).</p>\n<p><strong>§11.4</strong> <code>stripe_event_dispatch_log</code> is strictly INSERT-only at the <code>cyberos_app</code> role (REVOKE UPDATE, DELETE per §3.1 + task-audit skill rule 12). <code>dispatch_status</code> is set at INSERT time as a single-pass write — the handler computes status (<code>dispatched</code>/<code>duplicate</code>/<code>failed</code>) before INSERT based on idempotency lookup and dispatch result. Retry-and-update for transient failures lands in slice 3 (task-TEN-2xx) with a separate <code>stripe_event_dispatch_retry</code> table that follows the correction_to pattern (task-audit skill rule 11 derivative).</p>\n<p><strong>§11.5</strong> The <code>cancel_at_period_end=true</code> cancellation primitive (§1 #10 + DEC-803) is the <em>normal</em> deactivation path; hard cancellation comes only from TASK-TEN-104 termination, which explicitly invokes Stripe's <code>DELETE /v1/subscriptions/{id}?prorate=true</code>.</p>\n<p><strong>§11.6</strong> Founder tenant detection is <code>tenants.is_founder_tenant=true</code>; the guard is at api_client entry, not per-handler, so all Stripe code paths are protected uniformly.</p>\n<p><strong>§11.7</strong> The <code>stripe_price_map</code> table is intentionally GLOBAL (no RLS) because Stripe Price IDs are not tenant-scoped — they're per-residency-Stripe-account, shared across all tenants in that residency. Tenant isolation is enforced by the (tenant.residency → API key) routing.</p>\n<p><strong>§11.8</strong> Per-currency Stripe Price IDs vs per-residency: GBP exists at slice 2 even though no residency defaults to GBP — it's an explicit override for eu-1 tenants who want GBP billing (e.g., UK customers post-Brexit). The catalog ships GBP entries; <code>stripe-sync-prices</code> populates them in the eu-1 Stripe account.</p>\n<p><strong>§11.9</strong> SubscriptionSchedule (§1 #8 downgrade path) outlives the subscription itself — even if the subscription is hard-cancelled, the schedule remains until executed or cancelled. TASK-TEN-104 termination must cancel any active SubscriptionSchedule for the tenant before deleting the subscription (else Stripe errors on the schedule's target).</p>\n<p><strong>§11.10</strong> The 1-hour overage push window (DEC-810) aligns with Stripe's billing-cycle-close grace period; pushing later means the overage lands on the NEXT period's invoice, which is acceptable but adds a 30-day delay to revenue recognition. Slice 3 adds a finer-grained push (hourly) for low-latency reporting.</p>\n<p><strong>§11.11</strong> <code>billing_contact_email</code> is collected at provisioning (TASK-TEN-001 + this task's modification of <code>tenant_create.rs</code> to accept the field). For backward compatibility with already-provisioned tenants (TASK-TEN-001 shipped before this), migration <code>0006</code> allows NULL initially with a 7-day operator backfill window before the NOT NULL constraint activates via a follow-up migration in slice 3.</p>\n<p><strong>§11.12</strong> The dunning state machine deliberately treats <code>Suspended → Suspended</code> on additional payment failures as a no-op (no further state advancement). Once suspended, further failures don't escalate; recovery via successful payment is the only exit. This matches operational reality: a suspended tenant's billing is \"in a state of repair\" — further failure signals don't add information.</p>\n<p><strong>§11.13</strong> Reconciliation jobs (slice 3) will compare our <code>tenants.stripe_subscription_id</code>-derived state against Stripe's live subscription.status and emit drift alerts. Slice 2 relies on webhook-driven state being eventually consistent.</p>\n<p><strong>§11.14</strong> The <code>Stripe-Version</code> header pinning (§11.1) is at the api_client level; per-call versioning override is NOT supported in slice 2 (some Stripe APIs require older versions for backward-compat — we'll cross that bridge when we hit it).</p>\n<p><strong>§11.15</strong> Audit row severities use the task-audit skill severity convention: sev-1 = revenue/security incident requiring CFO/operator attention; sev-2 = operational issue requiring eng response; sev-3 = informational/forensic. The 11-kind list at §1 #19 maps every kind to its severity.</p>\n<p><strong>§11.16</strong> The price catalog amounts (USD $19/$99/$999; SGD higher reflecting Singapore PPP) are slice 2 defaults; CFO reviews + revs via DEC entries. The CI test asserts cardinality (12) and uniqueness, not specific amounts — amount changes don't break the test.</p>\n<p><strong>§11.17</strong> <code>cyberos-ten stripe-sync-prices --dry-run</code> outputs JSON listing every (currency, tier) entry with its catalog amount and current Stripe Price ID (or <code>&lt;missing&gt;</code> if not yet synced). CI runs this nightly with <code>--dry-run</code> and alerts if any entry shows <code>&lt;missing&gt;</code> in any residency.</p>\n<p><strong>§11.18</strong> The NATS dispatcher (§6.3) runs as a separate binary <code>cyberos-ten-stripe-dispatcher</code> deployed alongside the TEN service; it scales horizontally (each instance consumes a slice of the NATS subject). Idempotency at the dispatch log means duplicates across instances are safe.</p>\n<p><strong>§11.19</strong> The handler's plan_change path (§6.1) invokes Stripe synchronously rather than async-queueing. The CFO-visible operation must reflect Stripe's reality quickly; the 200ms p95 target for Stripe API calls makes sync acceptable. Async-queue alternative considered but rejected as over-engineering for slice 2.</p>\n<p><strong>§11.20</strong> When <code>ensure_subscription</code> creates a subscription with 5 items (1 base + 4 overage), the Stripe Subscription Item IDs are persisted in <code>tenants.stripe_subscription_items_map JSONB DEFAULT '{}'</code> (added via migration <code>0006</code> per §3.1). JSON shape: <code>{ \"base\": \"si_3Pabc\", \"overage_api_calls\": \"si_3Pdef\", \"overage_ai_tokens\": \"si_3Pghi\", \"overage_storage\": \"si_3Pjkl\", \"overage_seats\": \"si_3Pmno\" }</code>. The overage push handler reads this map to locate the correct subscription_item_id per axis before calling <code>POST /v1/subscription_items/{id}/usage_records</code>.</p>\n<p><strong>§11.21</strong> Tests use <code>wiremock</code> to simulate Stripe responses (<code>ctx.stripe_mock()</code>); integration tests against Stripe test-mode are SEPARATE (run nightly, not in PR-CI) because Stripe rate-limits test-mode harshly. The wiremock-based tests validate our client logic; the nightly Stripe-test-mode integration validates end-to-end.</p>\n<p><strong>§11.22</strong> The 24h Stripe idempotency window vs our 7d cache: if Stripe expires the key but we still have it cached, our cache returns the cached response without consulting Stripe. This is desired — if Stripe has GC'd the key, it would re-process the call as new; our cache prevents that double-process.</p>\n<p><strong>§11.23</strong> Cross-tenant cache leakage in stripe_api_calls is prevented by RLS (§1 #17); cache lookups always include <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>. Property test: spawn two tenants concurrently issuing same operation; assert each gets their own response (no key collision).</p>\n<p><strong>§11.24</strong> The <code>is_downgrade</code> check in plan_change (§1 #8) compares tier ordinals: <code>enterprise &gt; team &gt; starter</code>. Same-tier plan_change is rejected upstream at TASK-TEN-002 #13 (<code>no_change</code> 409), so this task doesn't need to handle same-tier.</p>\n<p><strong>§11.25</strong> Reason text in refund + dunning audit rows is free-text; TASK-MEMORY-111 PII scrubbing applies the standard ruleset. CFO provides reason at refund time; system generates reason for dunning (<code>payment_failed_retry_&lt;n&gt;</code>).</p>\n<hr>\n<p><em>End of TASK-TEN-003 spec.</em></p>";
