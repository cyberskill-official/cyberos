window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-140"]="<h1>TASK-IMP-140: Benchmark gates G1-G16 - checkers, risk register, BRAIN recording</h1>\n<h2>Summary</h2>\n<p>The 2026-07-23 deep audit defined sixteen benchmark gates - drift-prevention criteria that make its findings re-checkable forever instead of a one-time snapshot. This task lands them as product: <code>docs/verification/benchmark-gates.md</code> carrying all sixteen definitions (embedded in full below, so this spec is self-contained); automated checkers for the six gates no sibling hardening task owns (G3 enum cross-check, G4 headline counts, G5 payload reference walker, G6 vendored-gate smoke, G13 stuck-WIP detector, G16 reinstall idempotency), wired into CI through the existing suite-discovery glob; the audit's seven risk entries appended to <code>docs/reference/risk-register.md</code> as R-EXT-* rows; and the audit + its decisions recorded into the BRAIN per §13 - a step that depends on TASK-MEMORY-303 unfreezing the live store first.</p>\n<h2>Problem</h2>\n<p>The audit's issue register is point-in-time: every finding it verified (fail-open gates, enum forks, payload gaps, schema drift...) regrows the moment attention moves, because nothing mechanical re-measures. The audit therefore defined G1-G16 with pass/fail criteria and automation tiers - but they exist only in a conversation transcript. Meanwhile <code>docs/reference/risk-register.md</code> predates the audit (its newest rows are project-context R-EXT entries, none covering self-approval, vacuous gates, or the config-wipe class the audit demonstrated), and the audit itself is recorded in no durable store: the BRAIN is FROZEN_RECOVERABLE (layout failure), so the repo's own doctrine - record decisions and audits into the BRAIN - is currently unsatisfiable.</p>\n<h2>Proposed Solution</h2>\n<p>Author <code>docs/verification/benchmark-gates.md</code> from the embedded definitions below: one section per gate (purpose, pass/fail, severity, test method, automation tier, checked files, owning checker), plus a status table the checkers can be diffed against. Implement <code>scripts/tests/test_benchmark_gates.sh</code> with one function per unowned gate - t_g03 (parse the status enum from STATUS-REFERENCE.md §1 and compare byte-for-byte against RUBRIC.md FM-104's list, <code>task-lint.mjs</code> STATUSES, <code>render-status-hub.mjs</code> STATUSES, and the vendored BACKLOG template's off-ramp vocabulary), t_g04 (recompute module/workflow/task counts and compare against README.md + docs/README.md headline claims), t_g05 (walk every path reference in vendored docs/skills against the built payload's file set - the skill-log.mjs class), t_g06 (scratch-install smoke: the vendored caf/awh gate entry points exit with semantic codes, never 127/not-found), t_g13 (report-only detector: in-flight statuses older than N=30 days emitted as a triage list; detection automated, decision human), t_g16 (install -&gt; reinstall on a scratch repo diffs <code>.cyberos/</code> clean modulo timestamped backups AND preserves a pre-set config.yaml override - the C1 wipe class). The suite auto-registers via <code>run_all.sh</code>'s glob and therefore rides TASK-IMP-128's CI job and the TASK-IMP-136 workflow when those land. Append the seven audit risk entries to the risk register as R-EXT rows with the register's full field set. Finally - after TASK-MEMORY-303 lands - record the audit verdict, the sixteen gates, and the hardening-wave decisions into the BRAIN as memory files + chained audit rows per §13, closing the loop the frozen store left open.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Sixteen checkers in this task (re-implement the owned ones too).</strong> Rejected: G1/G2/G7-G12/G14/G15 checkers ship inside the sibling tasks whose behavior they verify; duplicating them here creates two authorities per gate and guarantees drift between them. The doc maps ownership; this task fills only the gaps.</li><li><strong>benchmark-gates.md generated from the transcript verbatim.</strong> Rejected: the transcript is untrusted-by-protocol prose (§11) and phrased for a conversation; the doc needs testable pass/fail wording and checked-file lists an implementer can act on. The definitions below are the transcript's content normalized to that shape - same gates, same severities, same tiers.</li><li><strong>A new CI workflow for the gate suite.</strong> Rejected: <code>run_all.sh</code>'s glob is the repo's documented registration mechanism (\"the glob IS the registration\"); a dedicated workflow would be a second wiring to forget. CI arrival rides IMP-128/IMP-136, which own CI wiring.</li><li><strong>Record the audit to the BRAIN now, store frozen or not.</strong> Rejected: §12 forbids writes below READY; §1's pre-write checklist halts at state verification. Recording into a failing store would make the audit's own record a protocol violation - the dependency on TASK-MEMORY-303 is the honest sequencing.</li><li><strong>Fold the risk entries into a new document.</strong> Rejected: <code>docs/reference/risk-register.md</code> exists, has the R-EXT extension convention documented in its own header, and is reviewed in the founder weekly sync - extending it puts the audit risks where the eyes already are.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release - benchmark-gates.md exists with all sixteen gates in the normalized shape; <code>test_benchmark_gates.sh</code> green in <code>run_all.sh</code> with its six checkers each demonstrably failing on a constructed violation; the risk register carries the seven new R-EXT rows; and (post TASK-MEMORY-303) the BRAIN holds the audit record with chained rows. Baseline today: zero of the four exist.</li><li>Guardrail: <code>run_all.sh</code> stays green corpus-wide (the six new checkers must pass against the CURRENT repo the day they land - any gate that measures a not-yet-fixed defect lands as report-only until its owning task ships, and the doc's status table says which mode each gate is in).</li></ul>\n<h2>Scope</h2>\n<p>In scope: benchmark-gates.md, the six-checker suite, the risk-register extension, the BRAIN recording step, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>The checkers owned by sibling tasks (mapped in the doc; see source_decisions) and any change to those tasks' scopes.</li><li>The status-hub UI for G13 (the detector here is a report-only test output; the hub sentinel is the v3.x roadmap item).</li><li>Fixing any defect a checker finds - checkers measure; the owning tasks fix. A checker that fails on an unshipped fix runs report-only per the guardrail metric.</li><li>Editing the fifteen existing RSK rows or re-scoring them.</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-MEMORY-303]</code> - the BRAIN recording step (clause 1.6) requires the store un-frozen by 303's layout repair (303 carries the reciprocal <code>blocks</code> entry). The dependency gates only that clause: the doc, checkers, and register rows are implementable immediately, and the task may ship its non-BRAIN clauses while 303 is in flight per the slice plan, but final acceptance includes the recording. Soft references (no cycles): TASK-IMP-128 + TASK-IMP-136 carry the gate suite into CI; TASK-CUO-302/303/304, TASK-SKILL-202, TASK-IMP-137/138/139 own the mapped checkers.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> the G1-G16 definitions were extracted from the operator-approved audit conversation and normalized (not re-derived); every checked-file path and ownership mapping below was verified against the working tree at authoring time; the risk-register numbering convention was read from the register's own header.</li><li><strong>Human review:</strong> the hardening plan (Phase 3 scope: benchmark-gates.md, checkers, risk register, BRAIN recording) was operator-approved 2026-07-23.</li></ul>\n<h2>Gate definitions (G1-G16) - embedded normative reference</h2>\n<p>The content contract for <code>docs/verification/benchmark-gates.md</code>. Severity: how bad a regression is. Tier: <code>ci</code> (fully automated), <code>ci+human</code> (automated floor, periodic human judgment), <code>detect+human</code> (automated detection, human decision). Owner: the checker that enforces it.</p>\n<h3>G1 - Gate-floor non-vacuous (severity: critical, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> the machine-gate floor must never report GREEN having run nothing - vacuous green poisons both HITL gates downstream.</li><li><strong>Pass/fail:</strong> on a scratch install with an all-empty gate env, <code>run-gates.sh</code> exits non-zero (RED, distinct code) unless the operator sets the explicit empty-ack variable, which yields a distinct <code>EMPTY-ACKNOWLEDGED</code> line, never <code>GREEN</code>. Fail: exit 0 + GREEN on empty.</li><li><strong>Test method:</strong> scratch install; empty <code>gates.env</code>/config; assert exit code + output line.</li><li><strong>Checked files:</strong> <code>tools/install/gates/run-gates.sh</code>, <code>tools/install/install.sh</code> (autodetect + header).</li><li><strong>Owner:</strong> <code>tools/install/tests/test_fail_closed_gates.sh</code> (TASK-CUO-302).</li></ul>\n<h3>G2 - HITL mechanical lock (severity: critical, tier: ci; verdicts stay human)</h3>\n<ul><li><strong>Purpose:</strong> the two human-acceptance transitions must be refusable by the machine, not just forbidden by prose.</li><li><strong>Pass/fail:</strong> <code>backlog-mutate flip</code> refuses <code>reviewing-&gt;ready_to_test</code> and <code>testing-&gt;done</code> without a recorded verdict artifact (distinct exit code, no write); with <code>--verdict-by</code> + <code>--verdict-evidence</code> the flip succeeds and (store present) lands one <code>status_overridden</code> row. Fail: bare flip succeeds.</li><li><strong>Test method:</strong> e2e flip attempts without/with verdict on a scratch backlog.</li><li><strong>Checked files:</strong> <code>tools/install/docs-tools/backlog-mutate.mjs</code>, <code>memory-append.mjs</code>.</li><li><strong>Owner:</strong> <code>tools/install/tests/test_hitl_lock.sh</code> (TASK-CUO-303).</li></ul>\n<h3>G3 - Status-enum single source (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> one canonical 12-value status enum; every consumer (rubric, lint, hub, templates) agrees byte-for-byte - the 10-vs-12 fork class.</li><li><strong>Pass/fail:</strong> the enum parsed from <code>STATUS-REFERENCE.md</code> §1 equals the sets in <code>RUBRIC.md</code> FM-104, <code>task-lint.mjs</code> STATUSES, <code>render-status-hub.mjs</code> STATUSES, and the vendored BACKLOG template's off-ramp vocabulary. Fail: any mismatch, extra, or missing value anywhere.</li><li><strong>Test method:</strong> parse + set-compare across the five surfaces; mismatch message names surface and value.</li><li><strong>Checked files:</strong> <code>.cyberos/cuo/STATUS-REFERENCE.md</code> (source: <code>modules/skill/contracts/task/STATUS-REFERENCE.md</code>), <code>modules/skill/task-audit/RUBRIC.md</code>, <code>tools/install/docs-tools/task-lint.mjs</code>, <code>tools/docs-site/render-status-hub.mjs</code>, <code>tools/install/templates/BACKLOG.md</code>.</li><li><strong>Owner:</strong> <code>scripts/tests/test_benchmark_gates.sh::t_g03</code> (this task).</li></ul>\n<h3>G4 - Headline-count truth (severity: medium, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> README headline counts (modules / workflows / tasks) stay measured, not remembered.</li><li><strong>Pass/fail:</strong> counts recomputed from the tree equal the numbers claimed in <code>README.md</code> and <code>docs/README.md</code>. Fail: any drift.</li><li><strong>Test method:</strong> recount (module dirs, workflow files, task spec folders) and substring-compare.</li><li><strong>Checked files:</strong> <code>README.md</code>, <code>docs/README.md</code>.</li><li><strong>Owner:</strong> <code>scripts/tests/test_benchmark_gates.sh::t_g04</code> (this task).</li></ul>\n<h3>G5 - Payload completeness (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> every path a vendored doc/skill references exists in the built payload - the unvendored-<code>skill-log.mjs</code> class, where a workflow promises a tool the install never delivers.</li><li><strong>Pass/fail:</strong> a reference walker over the built payload's markdown/scripts resolves every intra-payload path; zero missing. Fail: any referenced path absent.</li><li><strong>Test method:</strong> build scratch payload; extract path references; stat each against the payload tree (extends the <code>check-chain-coverage.sh</code> approach).</li><li><strong>Checked files:</strong> <code>dist/cyberos/**</code> (built), <code>tools/install/build.sh</code> (vendor list).</li><li><strong>Owner:</strong> <code>scripts/tests/test_benchmark_gates.sh::t_g05</code> (this task).</li></ul>\n<h3>G6 - Vendored-gate executability (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> the optional CAF/awh gate entry points must run in a consumer-shaped install - semantic exit codes, never \"not found\"/127 or a structurally-impossible path (the vendored-CAF ROOT-resolution class).</li><li><strong>Pass/fail:</strong> in a scratch install, invoking the vendored caf gate (and awh path when enabled) yields a semantic exit (pass/fail/skip-with-reason); never 127, never a missing-root error. Fail: any structural failure.</li><li><strong>Test method:</strong> scratch install; invoke entry points with <code>CAF_ENABLED=true</code> fixtures; classify exits.</li><li><strong>Checked files:</strong> <code>.cyberos/cuo/gates/caf/caf_gate.sh</code> (vendored), the seeded <code>CAF_CMD</code>/<code>AWH_CMD</code> in <code>gates.env</code>.</li><li><strong>Owner:</strong> <code>scripts/tests/test_benchmark_gates.sh::t_g06</code> (this task).</li></ul>\n<h3>G7 - Skill quality floor (severity: high, tier: ci+human)</h3>\n<ul><li><strong>Purpose:</strong> vendored skills meet a minimum body/section floor and every author/audit pair carries its file classes - no more ~20-line stubs shipped as product.</li><li><strong>Pass/fail:</strong> <code>check-skill-floor.sh</code> green over the payload AND <code>check-pair-parity.sh</code> green with SCOPE = every vendored pair. Fail: any undersized skill or missing class file. Human half: periodic prompt-quality review (not mechanical).</li><li><strong>Test method:</strong> the two checkers against a scratch payload.</li><li><strong>Checked files:</strong> <code>dist/cyberos/cuo/skills/**</code>, <code>tools/install/check-pair-parity.sh</code>, <code>tools/install/check-skill-floor.sh</code>.</li><li><strong>Owner:</strong> <code>tools/install/tests/test_skill_floor.sh</code> (TASK-SKILL-202).</li></ul>\n<h3>G8 - Injection-discipline coverage (severity: high, tier: ci+human)</h3>\n<ul><li><strong>Purpose:</strong> every repo-reading vendored skill declares <code>untrusted_inputs</code> + wrapping rules - prompt-injection posture is a floor, not a virtue of the best skills.</li><li><strong>Pass/fail:</strong> each repo/artefact-reading vendored skill carries the <code>untrusted_inputs</code> frontmatter block AND a non-empty per-skill <code>references/UNTRUSTED_CONTENT.md</code>. Fail: either half missing on any such skill. Human half: quality of the wrapping rules.</li><li><strong>Test method:</strong> presence + shape scan over the payload skill set.</li><li><strong>Checked files:</strong> <code>dist/cyberos/cuo/skills/*/SKILL.md</code> + <code>references/</code>.</li><li><strong>Owner:</strong> <code>tools/install/tests/test_skill_floor.sh::t03</code> (TASK-SKILL-202).</li></ul>\n<h3>G9 - BRAIN health in gates (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> where memory is installed, store health is part of the machine floor - a frozen BRAIN silently dropping the audit trail is a gate matter, not a curiosity.</li><li><strong>Pass/fail:</strong> <code>run-gates.sh</code> runs <code>cyberos doctor</code> when the store + CLI exist (RED on doctor FAIL, provenance SKIP when absent), and the live store passes layout invariants. Fail: doctor absent from gates where memory exists, or store non-canonical.</li><li><strong>Test method:</strong> three-state scratch-repo matrix (healthy store / violating store / no store).</li><li><strong>Checked files:</strong> <code>tools/install/gates/run-gates.sh</code>, <code>modules/memory/cyberos/core/invariants.py</code>, <code>.cyberos/memory/store/</code> layout.</li><li><strong>Owner:</strong> <code>tools/install/tests/test_doctor_gate.sh</code> (TASK-MEMORY-303).</li></ul>\n<h3>G10 - Schema copy consistency (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> every <code>memory.schema.json</code> copy byte-identical and the drift test pointed at real paths - the StoreAcl fork class, where consumers validate against a schema missing normative definitions.</li><li><strong>Pass/fail:</strong> root, package-data, and vendored copies hash-identical; generator <code>--check</code> green; the drift test executes (cannot skip on a missing path). Fail: any divergence or a skipping guard.</li><li><strong>Test method:</strong> three-way hash + generator check + pytest collection assertion.</li><li><strong>Checked files:</strong> <code>modules/memory/memory.schema.json</code>, <code>modules/memory/cyberos/data/memory.schema.json</code>, payload <code>memory/memory.schema.json</code>, <code>modules/memory/tests/test_schema_drift.py</code>.</li><li><strong>Owner:</strong> <code>modules/memory/tests/test_schema_single_source.py</code> (TASK-MEMORY-303).</li></ul>\n<h3>G11 - Loop-bound single-sourcing (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> the loop constants doctrine names (route-back ceiling 3; debugging breaker 5) match every machine encoding - the 3-vs-2 fork class.</li><li><strong>Pass/fail:</strong> the ceiling parsed from ship-tasks.md §11b equals the <code>api.py</code> default, the CLI default, and the help text; (breaker: pinned the day it gains a machine constant). Fail: any surface disagrees.</li><li><strong>Test method:</strong> doctrine-parsing conformance test; loud failure on parse miss.</li><li><strong>Checked files:</strong> <code>modules/cuo/chief-technology-officer/workflows/ship-tasks.md</code>, <code>modules/cuo/cuo/api.py</code>, <code>modules/cuo/cuo/cli.py</code>.</li><li><strong>Owner:</strong> <code>modules/cuo/tests/test_doctrine_constants.py</code> (TASK-CUO-304).</li></ul>\n<h3>G12 - UNREVIEWED hygiene (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> no non-draft spec carries <code># UNREVIEWED</code> - compliance fields on shipped work are confirmed or the task is not shipped (FM-112, enforced corpus-wide instead of per-lint-invocation).</li><li><strong>Pass/fail:</strong> corpus scan finds zero non-draft spec.md files containing the marker. Fail: any hit.</li><li><strong>Test method:</strong> status-aware grep over <code>docs/tasks/*/TASK-*/spec.md</code>.</li><li><strong>Checked files:</strong> the task corpus.</li><li><strong>Owner:</strong> <code>scripts/tests/test_corpus_hygiene.sh::t02</code> (TASK-IMP-139).</li></ul>\n<h3>G13 - Stuck-WIP detection (severity: medium, tier: detect+human)</h3>\n<ul><li><strong>Purpose:</strong> in-flight statuses older than a threshold are surfaced for operator triage - eleven tasks sat in <code>implementing</code> for ten weeks with nothing noticing.</li><li><strong>Pass/fail:</strong> the detector lists every task in an in-flight status (<code>implementing</code>, <code>ready_to_review</code>, <code>reviewing</code>, <code>ready_to_test</code>, <code>testing</code>) whose last recorded transition (or <code>created_at</code> fallback) exceeds N=30 days; the list is report output, never an automatic status change. Fail (of the gate itself): detector absent or silent on a constructed stale fixture.</li><li><strong>Test method:</strong> corpus scan + fixture with a backdated in-flight task.</li><li><strong>Checked files:</strong> the task corpus; threshold configurable, default 30 days.</li><li><strong>Owner:</strong> <code>scripts/tests/test_benchmark_gates.sh::t_g13</code> (this task; report-only).</li></ul>\n<h3>G14 - CI parity &amp; stub honesty (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> the offline suite runs in root CI, the CAF fixtures validate in root CI, and no always-green stub workflow exists - green means checked, everywhere a check is named.</li><li><strong>Pass/fail:</strong> a root workflow invokes <code>run_all.sh</code>; a root workflow invokes the CAF eval validator + <code>caf_precommit_check.sh</code>; zero workflows carry the stub placeholder marker; no stub-named check is branch-protection-required. Fail: any half missing.</li><li><strong>Test method:</strong> workflow-content asserts + placeholder scan (+ operator-side protection query documented in the owning task).</li><li><strong>Checked files:</strong> <code>.github/workflows/**</code>, <code>.pre-commit-config.yaml</code> (or its absence), <code>.githooks/pre-commit</code>.</li><li><strong>Owner:</strong> <code>scripts/tests/test_ci_truth.sh</code> (TASK-IMP-136; run_all-in-CI half via TASK-IMP-128).</li></ul>\n<h3>G15 - Entry-point consistency (severity: medium, tier: ci+human)</h3>\n<ul><li><strong>Purpose:</strong> pointer files across all tools reference one workflow spine and the platform-vs-consumer identity of root AGENTS.md is explicit - an agent's first file load must reach task/HITL law.</li><li><strong>Pass/fail:</strong> root <code>AGENTS.md</code> reaches task law within its first 30 lines; every pointer file names <code>.cyberos/AGENT-ENTRY.md</code>; exactly one normative protocol source exists with all copies self-declaring. Fail: any invariant broken. Human half: the Branch A/B identity decision itself (TASK-IMP-138's fork).</li><li><strong>Test method:</strong> line-window + marker greps.</li><li><strong>Checked files:</strong> <code>AGENTS.md</code>, <code>CLAUDE.md</code>, <code>.cursorrules</code>, <code>.cursor/rules/cyberos.mdc</code>, <code>GEMINI.md</code>, <code>.github/copilot-instructions.md</code>, <code>.windsurfrules</code>, <code>tools/install/install.sh</code>.</li><li><strong>Owner:</strong> <code>scripts/tests/test_entrypoint_identity.sh</code> (TASK-IMP-138).</li></ul>\n<h3>G16 - Idempotent reinstall (severity: high, tier: ci)</h3>\n<ul><li><strong>Purpose:</strong> install -&gt; reinstall produces an equivalent <code>.cyberos/</code> (modulo timestamped backups) and never silently degrades operator config - the observed C1 wipe (a working TEST_CMD surviving only in a <code>.bak</code>) can never recur unnoticed.</li><li><strong>Pass/fail:</strong> on a scratch repo: two consecutive installs diff clean modulo the documented backup files; a pre-set <code>.cyberos/config.yaml</code> override survives byte-identical; no reader-visible vendored-tree absence during the loop. Fail: any silent config degradation or divergent tree.</li><li><strong>Test method:</strong> double-install diff + config-survival assert + reader poll (builds on <code>test_e2e_skeleton.sh</code> and TASK-IMP-137's atomic vendor).</li><li><strong>Checked files:</strong> <code>tools/install/install.sh</code>, <code>.cyberos/</code> (scratch), <code>tools/install/tests/test_e2e_skeleton.sh</code>.</li><li><strong>Owner:</strong> <code>scripts/tests/test_benchmark_gates.sh::t_g16</code> (this task; the reader-gap half also covered by TASK-IMP-137's t06).</li></ul>\n<h2>Risk-register extension (content contract for the seven R-EXT rows)</h2>\n<p>Each row carries the register's full field set (description, cause, impact, detection, prevention, recovery, automation tier), sourced from the audit: (1) <strong>Self-approval / skipped HITL</strong> - no mechanical lock; unreviewed work marked done; detect: audit chain lacks verdict rows; prevent: G2; recover: reconcile + <code>done -&gt; ready_to_review</code> flip. (2) <strong>Vacuous green gates</strong> - fail-open floor + autodetect unknown; false confidence at both HITL gates; prevent: G1; recover: re-run with restored config. (3) <strong>Config wipe on reinstall</strong> - gates.env regeneration; silent loss of the only test command (observed); prevent: G16 + durable config.yaml; recover: <code>.bak</code> restore. (4) <strong>Prompt injection via repo files</strong> - repo-reading skills without untrusted discipline; steered workflows in consumer repos; prevent: G8; detect: injection-marker scan. (5) <strong>Payload/doc divergence</strong> - docs referencing unvendored files; consumer workflows halt on missing tools; prevent: G5. (6) <strong>Partial install window</strong> - rm/cp vendor step; broken <code>.cyberos/</code> mid-install; prevent: staged swap (TASK-IMP-137) + G16; recover: re-run install. (7) <strong>BRAIN frozen-by-layout</strong> - store pollution; protocol-compliant agents must refuse writes, audit trail silently absent; prevent: G9; recover: operator-gated move (TASK-MEMORY-303).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>docs/verification/benchmark-gates.md</code> MUST carry all sixteen gate definitions in the normalized shape above (purpose, pass/fail, severity, tier, test method, checked files, owner), plus a status table stating per gate whether its checker is live or report-only and which task owns it. The doc MUST match the embedded definitions in this spec (same gates, same severities, same tiers) - it is their published home, not a fork.</li><li>1.2 <code>scripts/tests/test_benchmark_gates.sh</code> MUST implement the six unowned checkers (t_g03 enum cross-check, t_g04 headline counts, t_g05 payload reference walker, t_g06 vendored-gate smoke, t_g13 stuck-WIP report, t_g16 reinstall idempotency) with each checker demonstrably failing on a constructed violation, and MUST register through <code>run_all.sh</code>'s existing glob (no hand-list edit).</li><li>1.3 A checker whose gate measures a defect fixed by a sibling task that has not yet shipped MUST run in report-only mode (loud output, non-failing) with the doc's status table saying so, flipping to enforcing in the same change that lands the fix or in a one-line follow-up when the sibling ships first. The suite MUST be green on the repo the day it lands.</li><li>1.4 <code>t_g13</code> MUST NOT change any task's status - it emits the stale-WIP list for operator triage only (detection automated, decision human, per the gate's tier).</li><li>1.5 <code>docs/reference/risk-register.md</code> MUST gain the seven audit rows as <code>R-EXT-*</code> entries following the register's documented numbering and field conventions, each naming its preventing gate(s) and recovery path per the content contract above.</li><li>1.6 After TASK-MEMORY-303's repair lands (the <code>depends_on</code> edge), the audit verdict, the sixteen gate definitions (by reference to the doc), and the hardening-wave decisions MUST be recorded into the BRAIN as memory files through the canonical writer with chained audit rows, and the session's §13 end-of-response block MUST report the write. This clause MUST NOT execute against a store that <code>cyberos doctor</code> reports below READY.</li><li>1.7 <code>CHANGELOG.md</code> MUST record the four deliverables (doc, suite, register rows, BRAIN record).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC 1 (traces_to: #1.1) - benchmark-gates.md exists with sixteen <code>### G</code> sections each carrying the seven fields and the status table; a field-completeness scan finds zero gaps, and the doc's severities/tiers match this spec's - test: <code>scripts/tests/test_benchmark_gates.sh::t01_doc_complete_and_consistent</code></li><li>[x] AC 2 (traces_to: #1.2) - each of the six checkers passes on the repo and fails on its constructed violation fixture (six negative fixtures, one per checker) - test: <code>scripts/tests/test_benchmark_gates.sh::t02_checkers_fail_on_violations</code></li><li>[x] AC 3 (traces_to: #1.3) - the suite exits green at HEAD; any report-only gate prints its report block and the doc's status table names it report-only - test: <code>scripts/tests/test_benchmark_gates.sh::t03_green_at_head_reportonly_declared</code></li><li>[x] AC 4 (traces_to: #1.4) - running t_g13 against a fixture corpus with a backdated implementing task lists it and leaves every spec file byte-identical - test: <code>scripts/tests/test_benchmark_gates.sh::t04_g13_reports_never_mutates</code></li><li>[x] AC 5 (traces_to: #1.5) - the register carries exactly seven new R-EXT rows, each with all seven fields non-empty and at least one G-reference - test: <code>scripts/tests/test_benchmark_gates.sh::t05_risk_rows_complete</code></li><li>[x] AC 6 (traces_to: #1.6) - post-303: the BRAIN holds the audit record (memory files present, chain verify green, doctor READY before and after), demonstrated on the live store at final acceptance and on a fixture store in CI - test: <code>scripts/tests/test_benchmark_gates.sh::t06_brain_record_fixture</code></li><li>[x] AC 7 (traces_to: #1.7) - CHANGELOG's top entry names all four deliverables - test: <code>scripts/tests/test_benchmark_gates.sh::t07_changelog_four_deliverables</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>TASK-MEMORY-303 slips:</strong> clauses 1.1-1.5 + 1.7 are independent and ship; the task holds short of final acceptance until 1.6 executes on the un-frozen store - the depends_on edge plus 1.6's READY-precondition make the sequencing mechanical, not mannered.</li><li><strong>A sibling task lands AFTER this suite (checker exists before the fix):</strong> 1.3's report-only mode is the designed state; the flip-to-enforcing is part of the sibling's landing checklist via the doc's status table - the table is the coordination surface, so neither task needs the other's timeline.</li><li><strong>G4 counts change legitimately (new module, new tasks):</strong> the checker recomputes both sides; only the README claim can be stale. The failure message prints the measured numbers so the fix is a one-line doc edit - measured truth stays cheap to restore.</li><li><strong>G5 false positives on illustrative paths:</strong> the walker honors an explicit inline exemption marker (documented in the doc) for paths that are examples, not promises; every exemption is visible in the diff and greppable - the same allowlist-with-reasons pattern <code>chain-allowlist.txt</code> uses.</li><li><strong>G13 threshold tuning:</strong> N=30 days is the default judgment, configurable via env in the checker; the gate's pass/fail is about the DETECTOR existing and speaking, not about any particular task being stale.</li><li><strong>G16 vs machine-local files:</strong> the double-install diff excludes the documented machine-local set (timestamped <code>.bak</code>s, <code>docs/tasks/.workflow/</code> run-state) - the exclusion list lives in the checker with a comment per entry, so \"modulo timestamps\" cannot silently grow into \"modulo everything\".</li><li><strong>Security-class:</strong> checkers are read-only against the repo + scratch dirs (G6/G16 execute only the repo's own vendored scripts in throwaway installs); the BRAIN write goes through the canonical writer under its lock + ACL discipline; no new secrets, no network.</li></ul>";
