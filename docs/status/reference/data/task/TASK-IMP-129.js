window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-129"]="<h1>TASK-IMP-129: Uninstall MUST preserve .cyberos/config.yaml, the override home</h1>\n<h2>Summary</h2>\n<p>Three shipped surfaces tell the operator that durable overrides belong in <code>.cyberos/config.yaml</code>, and <code>install.sh</code> scaffolds it exactly once and never clobbers it. <code>uninstall.sh</code> then removes it with the rest of <code>.cyberos/</code>. The one file the system promises is durable does not survive the operation most likely to be followed by a reinstall.</p>\n<h2>Problem</h2>\n<p><code>gates.env</code> is machine-owned by design. <code>install.sh:299</code> regenerates it on every install from autodetection, and <code>install.sh:326</code> says so plainly - when regeneration changes the file it points the operator at the durable alternative: \"durable overrides belong in <code>.cyberos/config.yaml</code>\". That design is coherent and this task does not disturb it.</p>\n<p>The durable alternative is not durable. Three surfaces name <code>config.yaml</code> as the override home:</p>\n<ul><li><code>install.sh:326</code> directs the operator to it when gates.env is regenerated.</li><li><code>install.sh:328</code> scaffolds it exactly once, explicitly never clobbering (TASK-CUO-207 §1 #3) - a never-clobber promise only means something if the file persists.</li><li><code>run-gates.sh:24-25</code> reads it as the override layer, and <code>run-gates.sh:75</code> instructs the operator to \"Set commands in <code>.cyberos/config.yaml</code> (gates.build/lint/test/coverage)\".</li></ul>\n<p><code>uninstall.sh</code> contains zero occurrences of <code>config.yaml</code>. It removes <code>.cyberos/</code> wholesale, keeping only the BRAIN store under <code>CYBEROS_UNINSTALL_KEEP_BRAIN</code>. An operator who follows the instruction printed by install and by the gate runner loses their overrides at the next uninstall, and the reinstall scaffolds a fresh commented-out file as though they had never configured anything.</p>\n<p>This is the same principle TASK-IMP-126 §1.4 already established - \"None of 1.1-1.3 may remove an operator file\" - scoped there to <code>.mcp.json</code>, unmarked skill dirs, and foreign hook lines. <code>config.yaml</code> is operator content by construction: install writes it once and never again, so every subsequent byte in it is the operator's.</p>\n<p>Observed on this repo on 2026-07-20: an uninstall/install cycle regenerated <code>gates.env</code> with an empty <code>TEST_CMD</code>, because autodetect reported ecosystem \"unknown\" for a repo whose suite is <code>bash scripts/tests/run_all.sh</code> rather than an npm script. That silently reverted a same-day fix that had made the machine gates non-vacuous, and the gates would have reported GREEN while running no tests. Node repos were unaffected - autodetect populated build, lint and test correctly there - so the failure is specific to repos whose test entrypoint autodetect cannot name, which is exactly the population that most needs a durable override.</p>\n<h2>Proposed Solution</h2>\n<p>Add <code>config.yaml</code> to the set uninstall preserves, alongside the BRAIN store, and report it in the \"kept on purpose\" banner so the operator can see what survived. Separately, teach autodetect to recognise a shell test entrypoint so a bash-suite repo gets a populated <code>gates.env</code> rather than a silent empty one.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Preserve <code>gates.env</code> instead. Rejected: it contradicts TASK-CUO-207, which makes gates.env machine-owned and regenerated on purpose. Preserving it would carry stale autodetect provenance across installs and make the regeneration message a lie.</li><li>Move <code>config.yaml</code> outside <code>.cyberos/</code> so uninstall cannot reach it. Rejected: it is per-repo CyberOS configuration and belongs with the machine; the fix is for uninstall to honour the promise, not to relocate the file to route around uninstall.</li><li>Document that operators should back up <code>config.yaml</code> before uninstalling. Rejected: a durability promise discharged by asking the operator to do it themselves is not a promise.</li><li>Fix only autodetect and leave uninstall as-is. Rejected: autodetect will always have gaps, which is precisely why an override layer exists. The override layer must survive.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: an operator-edited <code>config.yaml</code> survives uninstall and its overrides are in effect after reinstall, with no manual restoration. Baseline today: it is deleted and reinstall scaffolds a fresh commented-out file.</li><li>Guardrail: a repo whose only test entrypoint is a shell script gets a populated <code>TEST_CMD</code> from autodetect rather than an empty one, so the vacuous-gate outcome is not reachable by default.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the uninstall preserve set and its banner, the autodetect rule for a shell test entrypoint, and arms in <code>test_install_hygiene.sh</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>The machine-owned status of <code>gates.env</code> (TASK-CUO-207) - unchanged.</li><li>The <code>config.yaml</code> schema or the set of overridable keys.</li><li>Re-running install across the fleet after the fix (an operator-gated action).</li><li>The BRAIN store's existing preservation behaviour and its <code>CYBEROS_UNINSTALL_KEEP_BRAIN</code> switch.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Extends the operator-file principle established by TASK-IMP-126 §1.4 to a file that task did not enumerate.</p>\n<p><strong>Corroboration from TASK-IMP-122 §1.5.</strong> That task, reasoning about the fingerprint cone rather than about uninstall, independently classifies <code>config.yaml</code> as install-generated-or-operator-owned and declares <code>exempt:config.yaml</code> so its contents never enter <code>rules_sha</code>. Two tasks reaching the same classification from unrelated directions is the strongest available evidence that the file is operator content; this task is the one that makes the classification survive an uninstall.</p>\n<p><strong>Boundary against TASK-IMP-121 (done).</strong> That task made uninstall leave the repo as it found it for <code>.agents</code> markers, <code>.gitignore</code>, the pre-commit hook, and the five native channel parents. It never reaches inside <code>.cyberos/</code>, because everything there was assumed to be machine-owned and disposable. <code>config.yaml</code> is the counterexample that assumption missed. This task does not revisit any surface 121 settled.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> the empty-TEST_CMD regression was observed directly during the 2026-07-20 estate sweep, on this repo. The initial framing - that uninstall wrongly destroys operator gate configuration in <code>gates.env</code> - was wrong and was corrected before drafting: reading <code>install.sh:324-326</code> showed gates.env is machine-owned by design, and the real defect is the destruction of <code>config.yaml</code>. Every source_pages line was read at HEAD; the \"zero occurrences\" claim is a grep count against <code>uninstall.sh</code>.</li><li><strong>Human review:</strong> scope and granularity approved at the 2026-07-20 PLAN gate, where the relationship to TASK-IMP-126 was surfaced before writing; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>uninstall.sh</code> MUST preserve <code>.cyberos/config.yaml</code> when it exists, and MUST report it in the \"kept on purpose\" banner alongside the other preserved paths.</li><li>1.2 After uninstall then install, an operator-set override in <code>config.yaml</code> MUST still be in effect - <code>run-gates.sh</code> MUST resolve the same gate commands it resolved before the cycle.</li><li>1.3 Gate-command autodetect MUST populate <code>TEST_CMD</code> for a repo whose test entrypoint is a shell script at a conventional path, rather than leaving it empty with ecosystem \"unknown\".</li><li>1.4 Preserving <code>config.yaml</code> MUST NOT keep any machine-owned file: <code>gates.env</code> MUST still be regenerated on install, and the rest of <code>.cyberos/</code> outside the documented preserve set MUST still be removed.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - a fixture writes an override into <code>config.yaml</code>, runs uninstall, and the file is present afterwards and named in the banner output - test: <code>tools/install/tests/test_install_hygiene.sh::t_config_yaml_preserved</code></li><li>[ ] AC 2 (traces_to: #1.2) - after uninstall then install, <code>run-gates.sh</code> resolves the gate command set by the operator's <code>config.yaml</code>, not the autodetected default - test: <code>tools/install/tests/test_install_hygiene.sh::t_overrides_survive_reinstall</code></li><li>[ ] AC 3 (traces_to: #1.3) - installing into a fixture whose only test entrypoint is a shell script produces a non-empty <code>TEST_CMD</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t_autodetect_shell_suite</code></li><li>[ ] AC 4 (traces_to: #1.4) - after the same cycle, <code>gates.env</code> is a freshly regenerated file and no other machine-owned path under <code>.cyberos/</code> survived uninstall - test: <code>tools/install/tests/test_install_hygiene.sh::t_machine_files_still_removed</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><code>config.yaml</code> absent at uninstall (never scaffolded, or deleted by the operator) MUST NOT fail the uninstall, and MUST NOT be reported as kept.</li><li>A <code>config.yaml</code> left entirely at defaults (every line commented) is still preserved - uninstall cannot distinguish \"unedited\" from \"deliberately reset to defaults\", and guessing would reintroduce the silent-loss failure.</li><li>After preservation, <code>.cyberos/</code> is non-empty when the machine is removed; the container-reclaim logic MUST report it kept rather than attempting to remove a non-empty directory.</li><li>A repo with both an npm test script and a shell entrypoint MUST keep the existing precedence rather than switch to the shell one - 1.3 fills a gap and MUST NOT re-rank the cases autodetect already resolves.</li><li>A shell entrypoint that exists but is not executable MUST still be detected; the gate invokes it via <code>bash</code>, so the executable bit is not the test.</li><li>Security-class: uninstall reads and removes paths under the repo root. Adding a path to the preserve set narrows what is removed and grants no new capability; the preserved file is data read by <code>run-gates.sh</code>, which already treats it as untrusted configuration.</li></ul>";
