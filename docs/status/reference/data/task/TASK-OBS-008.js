window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-008"]="<h1>TASK-OBS-008: OBS compliance views — tenant-scoped JSON API + Ed25519 proof</h1>\n<h2>Summary</h2>\n<p>Ship a read-only <code>obs-compliance-view</code> service over the memory audit chain. As-built surface is a flat Rust crate: <code>views.rs</code> (four regulation kind-filters), <code>auth.rs</code> (auditor JWT + tenant scope), <code>query.rs</code> / <code>summary.rs</code> / <code>window.rs</code> / <code>pii_scan.rs</code>, <code>proof.rs</code> (Ed25519 over canonical JSON — not <code>chain_proof.rs</code>), and <code>main.rs</code> (axum JSON HTTP API on <code>GET /:view</code> + <code>GET /healthz</code>). Responses are <code>{ payload, proof }</code> JSON envelopes.</p>\n<h2>Problem</h2>\n<p>The prior engineering-spec claimed a per-regime <code>views/{eu_ai_act,pdpl,soc2,iso27001}.rs</code> tree, <code>chain_proof.rs</code>, PDF/JSON exporters, Grafana <code>compliance.json</code>, and standalone integration tests. The live crate consolidates views into <code>views.rs</code>, names the signer <code>proof.rs</code>, serves JSON only from <code>main.rs</code>, and keeps unit tests inline in each module. FM-004 blocked re-entry (<code>task@1</code> frontmatter + <code>## §N</code> body).</p>\n<h2>Proposed Solution</h2>\n<p>Adopt the as-built layout:</p>\n<ul><li><code>views.rs</code> — <code>View</code> enum, slug parse, fixed <code>kinds()</code> per regulation (DEC-175)</li><li><code>auth.rs</code> — RS256 JWKS or HS256 dev verifier; <code>external_auditor</code> role; <code>enforce_tenant_scope</code></li><li><code>query.rs</code> — tenant + kind + window read on <code>l1_audit_log</code> with RLS GUC (read-only, DEC-177)</li><li><code>window.rs</code> — reject inverted or &gt;365-day windows</li><li><code>summary.rs</code> — <code>total_rows</code>, <code>by_kind</code> (<code>BTreeMap</code>), seq span</li><li><code>proof.rs</code> — Ed25519 <code>sign</code> / <code>verify</code> over canonical payload bytes (DEC-176)</li><li><code>pii_scan.rs</code> — email / VN CCCD / VN phone regex defence</li><li><code>main.rs</code> — authenticate → scope tenant → validate window → fetch → summarize → PII scan → sign → best-effort <code>obs.compliance_view_accessed</code> emit</li></ul>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Resume the engineering-spec paths as-is.</strong> Rejected: phantom modules (<code>chain_proof.rs</code>, <code>views/eu_ai_act.rs</code>) do not exist; FM-004 blocks.</li><li><strong>PDF export in slice 3.</strong> Rejected: JSON API + proof ship first; PDF deferred.</li><li><strong>Per-view export integration tests against live Postgres.</strong> Rejected: slice ships inline unit tests; dedicated integration suite deferred.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: four view slugs parse and filter kinds; cross-tenant <code>?tenant_id=</code> refused; Ed25519 proof round-trips; 365-day window enforced.</li><li>Guardrail: <code>cargo test -p cyberos-obs-compliance-view</code> green on the inline module tests cited below.</li></ul>\n<h2>Scope</h2>\n<p>In scope (as-built):</p>\n<ul><li><code>services/obs-compliance-view/src/{views,auth,proof,query,summary,window,pii_scan,main,lib}.rs</code></li><li>JSON HTTP API (<code>GET /eu-ai-act|pdpl|soc2|iso27001</code>, <code>GET /healthz</code>)</li><li>Inline <code>#[cfg(test)]</code> modules in the files above</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Per-regime file tree (<code>views/eu_ai_act.rs</code>, <code>views/mod.rs</code>, etc.)</li><li>PDF export (<code>export/pdf.rs</code>, wkhtmltopdf)</li><li>Grafana dashboard (<code>deploy/obs/grafana/dashboards/compliance.json</code>)</li><li>Postgres integration tests (<code>tests/eu_ai_act_test.rs</code>, <code>tests/cross_tenant_test.rs</code>, etc.)</li><li>Chain-of-custody manifest signing (TASK-OBS-009)</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-OBS-002]</code> (tenant-aware query proxy pattern). Soft: TASK-AUTH-004 JWT/JWKS; <code>cyberos-audit-chain</code> for access-audit emit; TASK-OBS-009 manifest builds on this crate.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>views.rs</code> MUST define exactly four <code>View</code> variants (EU AI Act, PDPL, SOC 2, ISO 27001) with stable URL slugs and non-empty <code>kinds()</code> filters per DEC-175.</li><li>1.2 <code>main.rs</code> MUST expose a JSON HTTP API: <code>GET /:view</code> for the four slugs and <code>GET /healthz</code>; successful responses MUST be a JSON envelope <code>{ payload, proof }</code> (not PDF).</li><li>1.3 <code>auth.rs</code> MUST verify auditor JWTs and MUST require the <code>external_auditor</code> role before serving any view (DEC-178).</li><li>1.4 <code>enforce_tenant_scope</code> MUST refuse a cross-tenant <code>?tenant_id=</code> query parameter when it differs from the JWT tenant (403).</li><li>1.5 <code>query.rs</code> MUST fetch rows read-only from <code>l1_audit_log</code> filtered by tenant, view kinds, and time window; it MUST NOT mutate the chain (DEC-177).</li><li>1.6 <code>window.rs</code> MUST reject inverted windows and windows wider than 365 days.</li><li>1.7 <code>proof.rs</code> MUST Ed25519-sign the canonical serialized payload bytes and expose offline <code>verify</code>; the module MUST NOT be named <code>chain_proof.rs</code> in the as-built tree.</li><li>1.8 <code>pii_scan.rs</code> MUST scan the serialized response for raw PII patterns before serve; matches MUST fail closed (HTTP 500 path in <code>main.rs</code>).</li><li>1.9 <code>summary.rs</code> MUST produce a summary block with stable <code>by_kind</code> ordering (<code>BTreeMap</code>) included in the signed payload.</li><li>1.10 <code>main.rs</code> SHOULD best-effort emit <code>obs.compliance_view_accessed</code> via <code>cyberos_audit_chain::emit_genesis</code> without failing the auditor read on emit error.</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - view slug parse round-trips all four regulations - test: <code>services/obs-compliance-view/src/views.rs::parse_roundtrips_slug_for_every_view</code></li><li>[ ] AC 2 (traces_to: #1.1) - every view selects a non-empty kind set - test: <code>services/obs-compliance-view/src/views.rs::every_view_selects_a_non_empty_kind_set</code></li><li>[ ] AC 3 (traces_to: #1.1) - headline kinds present per regulation - test: <code>services/obs-compliance-view/src/views.rs::views_select_their_headline_kinds</code></li><li>[ ] AC 4 (traces_to: #1.3) - external_auditor role authorized - test: <code>services/obs-compliance-view/src/auth.rs::auditor_role_is_authorized</code></li><li>[ ] AC 5 (traces_to: #1.3) - missing auditor role refused - test: <code>services/obs-compliance-view/src/auth.rs::missing_auditor_role_is_refused</code></li><li>[ ] AC 6 (traces_to: #1.4) - cross-tenant tenant_id param refused - test: <code>services/obs-compliance-view/src/auth.rs::cross_tenant_param_is_refused</code></li><li>[ ] AC 7 (traces_to: #1.6) - window over 365 days rejected - test: <code>services/obs-compliance-view/src/window.rs::a_window_over_the_limit_is_rejected</code></li><li>[ ] AC 8 (traces_to: #1.6) - window at 365-day limit allowed - test: <code>services/obs-compliance-view/src/window.rs::a_window_at_the_limit_is_allowed</code></li><li>[ ] AC 9 (traces_to: #1.7) - Ed25519 sign then verify round-trips - test: <code>services/obs-compliance-view/src/proof.rs::sign_then_verify_roundtrips</code></li><li>[ ] AC 10 (traces_to: #1.7) - tampered canonical bytes fail verify - test: <code>services/obs-compliance-view/src/proof.rs::a_tampered_response_fails_verification</code></li><li>[ ] AC 11 (traces_to: #1.8) - raw email caught by PII scan - test: <code>services/obs-compliance-view/src/pii_scan.rs::raw_email_is_caught</code></li><li>[ ] AC 12 (traces_to: #1.9) - summary counts group by kind - test: <code>services/obs-compliance-view/src/summary.rs::counts_group_by_kind_and_track_seq_span</code></li><li>[ ] AC 13 (traces_to: #1.5) - read-only tenant-scoped l1_audit_log query - verify: <code>services/obs-compliance-view/src/query.rs</code> <code>fetch_rows</code></li><li>[ ] AC 14 (traces_to: #1.10) - best-effort obs.compliance_view_accessed emit - verify: <code>services/obs-compliance-view/src/main.rs</code> <code>emit_genesis</code> block</li><li>[ ] AC 15 (traces_to: #1.2) - JSON HTTP routes live in main.rs (not per-view modules) - verify: <code>services/obs-compliance-view/src/main.rs</code> router + <code>View::parse</code></li><li>[ ] AC 16 (traces_to: #1.2,#1.7) - Out of scope lists phantom paths; new_files cite flat as-built tree only - verify: this spec Scope / new_files</li></ul>\n<h2>Verification</h2>\n<pre><code class=\"language-bash\">cd services &amp;&amp; cargo test -p cyberos-obs-compliance-view proof::\ncd services &amp;&amp; cargo test -p cyberos-obs-compliance-view views:: auth:: window:: pii_scan:: summary::</code></pre>\n<table><thead><tr><th>Path</th><th>Covers</th></tr></thead><tbody><tr><td><code>src/views.rs</code> tests</td><td>DEC-175 kind table + slug routing</td></tr><tr><td><code>src/auth.rs</code> tests</td><td>DEC-178 auditor role + tenant scope</td></tr><tr><td><code>src/window.rs</code> tests</td><td>365-day cap</td></tr><tr><td><code>src/proof.rs</code> tests</td><td>DEC-176 Ed25519 proof</td></tr><tr><td><code>src/pii_scan.rs</code> tests</td><td>Response PII defence</td></tr><tr><td><code>src/summary.rs</code> tests</td><td>Summary block shape</td></tr><tr><td><code>src/main.rs</code></td><td>JSON HTTP shell + handler pipeline</td></tr></tbody></table>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) on branch <code>batch/9b-obs</code>.</li><li><strong>Scope:</strong> Re-spec/adopt against as-built <code>cyberos-obs-compliance-view</code>; removed phantom per-regime tree, <code>chain_proof.rs</code>, PDF, Grafana dashboard, and Postgres integration tests from claimed surface.</li><li><strong>Human review:</strong> Required at the two HITL gates (<code>entered_via: rework</code>, <code>routed_back_count: 1</code>).</li></ul>\n<hr>\n<p><em>batch/9b-obs adopt — TASK-OBS-008 re-spec against as-built obs-compliance-view.</em></p>";
