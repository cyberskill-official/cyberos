window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-112"]="<h1>TASK-IMP-112: Structured review findings alongside the prose packet</h1>\n<h2>Summary</h2>\n<p><code>code-review.md</code> is a prose packet: excellent for a human, opaque to a machine. Nothing downstream can count findings, route them, or act on them. Emit <code>review-findings@1</code> (JSON) beside the markdown - one record per finding with file, line, severity, clause reference, and suggested fix - keeping the prose as the human artefact.</p>\n<h2>Problem</h2>\n<p><code>code-review-author/SKILL.md</code> declares <code>envelopes/input.json</code> and <code>envelopes/output.json</code> schema_refs at lines 44 and 56: the contract already anticipates structured output. The artefact does not emit it. The consequence is that \"how often is the reviewer corrected\" - the metric the whole improvement loop depends on - is unmeasurable, because findings are sentences.</p>\n<h2>Proposed Solution</h2>\n<p>Emit <code>review-findings.json</code> beside <code>code-review.md</code>, one record per finding: <code>file</code>, <code>line</code>, <code>severity</code> (severe | important | nit), <code>clause_ref</code>, <code>summary</code>, <code>suggested_fix</code>. The markdown stays exactly as it is and remains the artefact a human reads. The JSON is what a future CI step, the reconcile ladder, or the outer loop reads.</p>\n<p>Severity uses the three-value taxonomy rather than our High/Medium/Low prose: a nit and a severe finding are different kinds of thing, and a reviewer who cannot say \"this is a nit\" says nothing instead.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Replace the markdown with JSON. Rejected: the prose packet is what makes a review readable, and a human reading JSON reviews worse.</li><li>Parse the markdown into JSON after the fact. Rejected: parsing prose a model wrote is a guess about a guess.</li><li>Wait until a consumer needs it. Rejected: the consumers are specified (IMP-20, IMP-23) and the field is free to emit while the reviewer already knows the answer.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: every review emits a schema-valid findings file whose record count matches the markdown's finding count - suite-asserted. Baseline: no machine-readable output exists.</li><li>Guardrail: <code>code-review.md</code> is unchanged in shape - this task adds an artefact, it does not reformat the human one.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the <code>review-findings@1</code> schema, its emission, the audit's check that the two agree, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Posting findings anywhere (no CI, no PR integration).</li><li>Changing what the reviewer judges or how it decides severity.</li><li>Reformatting the markdown packet.</li></ul>\n<h2>Dependencies</h2>\n<p>None.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §8 IMP-22; verified against code-review-author/SKILL.md on merged main.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>code-review-author</code> MUST emit <code>review-findings.json</code> beside <code>code-review.md</code>, valid against <code>review-findings@1</code>.</li><li>1.2 Each record MUST carry <code>file</code>, <code>line</code>, <code>severity</code> (<code>severe | important | nit</code>), <code>clause_ref</code>, <code>summary</code>, and <code>suggested_fix</code>.</li><li>1.3 <code>clause_ref</code> MUST name the task §1 clause the finding bears on, or <code>null</code> when the finding is outside the spec's clauses (a real category, and pretending otherwise would fabricate a reference).</li><li>1.4 The JSON record count MUST equal the markdown's finding count - the two artefacts describe one review.</li><li>1.5 <code>code-review.md</code>'s shape MUST NOT change.</li><li>1.6 A review with zero findings MUST emit an empty array, not an absent file.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2) - a review emits schema-valid JSON with all six fields per record - test: <code>tools/install/tests/test_review_findings.sh::t01_schema_valid</code></li><li>[ ] AC 2 (traces_to: #1.4) - record count equals the markdown finding count; a mismatch reds at audit - test: <code>tools/install/tests/test_review_findings.sh::t02_counts_agree</code></li><li>[ ] AC 3 (traces_to: #1.3) - an out-of-spec finding carries <code>clause_ref: null</code> rather than a fabricated reference - test: <code>tools/install/tests/test_review_findings.sh::t03_null_clause_ref</code></li><li>[ ] AC 4 (traces_to: #1.6) - a clean review emits <code>[]</code> - test: <code>tools/install/tests/test_review_findings.sh::t04_empty_array_not_absent</code></li><li>[ ] AC 5 (traces_to: #1.5) - the markdown packet is byte-identical to today's for a fixture review - test: <code>tools/install/tests/test_review_findings.sh::t05_markdown_unchanged</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A finding spanning a range of lines: <code>line</code> records the first; the range belongs in <code>summary</code>. A schema that models ranges before anyone needs them is the gold-plating the guide warns about.</li><li>A finding about a deleted file: <code>file</code> names the path as it was in the diff; the record is about the diff, not the tree.</li><li>Severity disagreement between the JSON and the prose: the audit reds it (1.4's shape) - one review cannot say two things.</li><li>A file path containing characters that break naive JSON: the emitter MUST serialise properly rather than string-concatenate. Test with a quote and a backslash in a path.</li><li>Security-class: the reviewer writes a JSON file describing a diff. Nothing reads it as a command. Paths recorded are data, never interpolated.</li></ul>";
