window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-117"]="<h1>TASK-IMP-117 - the template teaches the violation it is linted for</h1>\n<p>FM-001 fires on 501 of 544 specs. The cause is not 501 authoring mistakes: it is line 2 of <code>tools/install/templates/TASK-TEMPLATE.md</code>, which ships <code>id: TASK-&lt;MODULE&gt;-&lt;NNN&gt;  # module-scoped, e.g. TASK-AUTH-001.</code> Every spec is born violating a rule the machine floor enforces, because the file that teaches the format contradicts the file that checks it.</p>\n<p>The rule is right. <code>id:</code> parses as <code>\"TASK-X-001  # module-scoped, ...\"</code> for any consumer that does not strip trailing comments - the exact defect that put <code>priority: MUST  # MUST | SHOULD</code> into TASK-AI-001..005's parsed values and wrote FM-001 in the first place. What was missing was a mechanism: the lint existed, the template disagreed, and nothing ever ran the two against each other.</p>\n<p>The template is VENDORED. Every repo that installed CyberOS carries this copy, so every consumer corpus has the same disease. A migrator that lives only in this repo fixes one of N corpora.</p>\n<h2>Summary</h2>\n<p>Fix <code>TASK-TEMPLATE.md</code> so specs are born FM-001 clean, ship a migrator in the payload so any installed repo can clean its own corpus, and migrate this repo's non-conformant specs. FM-001 has TWO structural classes: TRAILING COMMENTS (the template defect above) and NESTED MAPS (a top-level key whose value is an indented child map). The trailing-comment class was migrated corpus-wide at commit 4c02b556; this task's migrator carries a SECOND capability that clears the nested-map residual (4004 findings across 140 specs) plus one apostrophe edge, so FM-001 reaches 0 for real.</p>\n<h2>Problem</h2>\n<p>FM-001 fires on 501 of 544 specs - 92% of the corpus. 149 of them are <code>status: done</code>: they passed two human gates while violating a machine-floor rule. The cause is line 2 of the shipped template. Every spec inherits the violation by construction, and because the lint was never run corpus-wide, nobody found out. The template is vendored, so every consumer repo has the same disease.</p>\n<p>That framing is only the FIRST of FM-001's two structural classes. After the trailing-comment class was migrated corpus-wide (commit 4c02b556), a re-lint showed the count did NOT reach 0: a residual of 4004 findings across 140 specs remained, plus 1 residual trailing-comment. The 4004 are a SECOND class - nested-map frontmatter under a single top-level key, <code>build_envelope:</code>, whose indented children the strict task@1 reader flags one line at a time as \"indented line outside a block list\". No tool reads <code>build_envelope</code> (it is inert data), task-lint rejects nested maps by design, and a <code>done</code> sibling (<code>TASK-SKILL-104</code>) already carries the same envelope data FLAT at top level - the shape <code>batch-select</code> reads. The 1 residual is a defect in the migrator's own quote detector (a mid-value apostrophe in <code>broker's</code> read as an opening quote), not in the lint. The full evidence, with a command behind every number, is <code>docs/tasks/_audits/2026-07-18-fm001-nested-map-fork.md</code>.</p>\n<h2>Proposed Solution</h2>\n<p>Move every trailing frontmatter comment in the template to its own line above its field, keeping all guidance text. Ship <code>docs-tools/fm001-migrate.mjs</code> (stdlib, guarded, idempotent, <code>--check</code>) in the payload so consumers can run it themselves. The migrator carries TWO capabilities: it moves trailing comments own-line (class 1), and it FLATTENS a nested-map key by hoisting its children to top-level keys (class 2), reconciling any collision by order-preserving union and halting on a genuine scalar conflict. Migrate this corpus with both. The body is never touched, so the audit bindings survive - and the bound specs carry neither class, so the migrator is a no-op on every one of them.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Narrow FM-001 to only the fields consumers parse (id/status/priority/type). Rejected: the rule was written in response to a real defect, and a rule narrowed to fit a broken corpus stops being a rule.</li><li>Fix the template only, leave the 501. Rejected by the operator: the machine floor stays un-runnable repo-wide, which is exactly how this hid for the life of the project.</li><li>Delete the template's annotations instead of moving them. Rejected: they are the template's teaching value; an author reading <code>type: feature</code> with no hint is worse off.</li><li>Run the migrator across consumer repos from here. Rejected: shipping a tool the operator runs is a migration; reaching into another repo and rewriting 500 files is an incident.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>FM-001 count across <code>docs/tasks/*/*/spec.md</code> reaches 0 corpus-wide, across BOTH structural classes (trailing comments AND nested maps). The trailing-comment class went first (commit 4c02b556); this task's second capability clears the nested-map residual of 4004 findings across 140 specs plus the one apostrophe edge.</li><li>A spec authored fresh from the template lints clean with no hand-editing.</li><li>All 19 <code>audited_body_sha256_prefix</code> values are unchanged after the migration. The mechanism is §1.4 (body untouched) reinforced by disjointness: every audit-bound spec carries NEITHER class, so the migrator is a byte-for-byte no-op on it.</li><li><code>dist/cyberos/docs-tools/fm001-migrate.mjs</code> is present, so a consumer repo can do the same.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Claude (Fable 5) during the 2026-07-17 hardening run, via the CyberOS create-tasks and ship-tasks workflows.</li><li>Scope: spec drafted by the agent from a defect it surfaced while advancing TASK-IMP-116 past its review gate. The 501/544 and 149-done figures are measured, not estimated. The migration scope (all corpora, including done specs, not just live ones) is the operator's recorded decision - the agent had proposed the narrower live-specs-only option and was overruled.</li><li>Human review: @stephencheng at the ready_to_implement gate, and again at both HITL gates.</li></ul>\n<h2>Dependencies</h2>\n<p>None. FM-001 already exists in task-lint (TASK-IMP-084) and the <code>relUnderRoot</code> guard already exists in docs-tools (TASK-IMP-109). This task adds no rule and invents no guard; it makes the template obey a rule that has been on the books since the machine floor shipped.</p>\n<h2>1. Clauses</h2>\n<p>1.1 <code>TASK-TEMPLATE.md</code> frontmatter carries no trailing comments and no aligned continuation comments. Every comment is own-line, above the field it documents. No guidance text is deleted - the annotations are the template's teaching value and they move, they do not go. Test: <code>t01_template_is_clean</code></p>\n<p>1.2 A new payload helper <code>docs-tools/fm001-migrate.mjs</code> rewrites trailing frontmatter comments to own-line comments. It ships in the payload (build.sh vendor list) so any installed repo can run it against its own corpus. Node stdlib only, exit-code discipline (0 clean/migrated, 2 usage), <code>--json</code>, <code>--check</code> (report without writing). Test: <code>t02_migrator_moves_trailing_comments</code></p>\n<p>1.3 The migrator NEVER splits on a <code>#</code> inside a quoted value. <code>title: \"Fix the # parsing bug\"</code> is left byte-identical. A <code>#</code> that is not preceded by whitespace is not a comment. Test: <code>t03_hash_inside_value_is_not_a_comment</code></p>\n<p>1.4 The migrator NEVER touches the body. Only the frontmatter block between the first two <code>---</code> lines is in scope. This is what preserves <code>audited_body_sha256_prefix</code> on the 19 bound specs - proven live twice this session: a status edit moves the file hash and leaves the body hash fixed. Test: <code>t04_body_is_untouched_and_body_hash_holds</code></p>\n<p>1.5 Idempotent. Running the migrator twice produces a byte-identical file the second time. Test: <code>t05_idempotent</code></p>\n<p>1.6 Migration of this repo's corpus: all 501 specs, including the 149 at <code>status: done</code>. Per the operator's scope decision, done specs are migrated - the frontmatter is metadata about the task, not the shipped record, and the body hash that binds the audit is untouched by 1.4. Test: <code>t06_corpus_is_fm001_clean</code></p>\n<p>1.7 The migrator refuses to run outside a repo root it can confirm, using the same <code>relUnderRoot</code> guard as task-reconcile / coverage-scope / verify-goals: confine, exist, <code>git ls-tree HEAD</code> tracked. An untracked path is REFUSED, not migrated. Test: <code>t07_guard_refuses_untracked_and_escaping_paths</code></p>\n<p>1.8 The migrator ALSO flattens a nested-map frontmatter key - a top-level key whose value is an indented child block (e.g. <code>build_envelope:</code> followed by <code>  language: rust 1.81</code>), which the strict task@1 reader rejects one line at a time as \"indented line outside a block list\". It hoists the children to top-level keys: the child block is dedented by its own base indent and the parent line dropped, so <code>  new_files:</code> / <code>    - x</code> become top-level <code>new_files:</code> / <code>  - x</code>. This is GENERAL to any nested-map key per FM-001's definition, not hard-coded to <code>build_envelope</code>. Each child value and its order are preserved byte-for-byte (only leading indentation changes); a child that is itself a block list (<code>new_files:</code> / <code>modified_files:</code> with <code>- item</code> lines) becomes a top-level block list. A child block that is ALREADY a plain block list (all <code>- item</code>, e.g. <code>source_pages:</code>) is not a nested map and is left untouched. When a hoisted key already exists at top level the migrator RECONCILES by order-preserving union - two block lists merge, exact-duplicate item values deduped, nothing unique dropped; two scalars of equal value dedupe to one - and NEVER silently drops or overwrites. A genuine scalar conflict, or a list/scalar kind mismatch, HALTS that file: it is named, and nothing in it is migrated. Test: <code>t09_flattens_nested_map</code></p>\n<p>1.9 A quote is a string delimiter ONLY when it BEGINS the scalar value. In a PLAIN (unquoted) scalar a mid-token apostrophe is a literal character, so a block item such as <code>- allow ... broker's ... (per §1 #4 - seal stdin/stdout/stderr only)</code> correctly detects the <code> #4</code> as a trailing comment and moves it own-line - matching task-lint, which reads <code>broker's</code> as a literal apostrophe and <code> #</code> as a plain-scalar comment. A <code>#</code> inside a value that BEGINS with a quote (<code>label: 'issue # 42 stays'</code>, <code>title: \"Fix the # bug\"</code>) stays data. This corrects a defect in the 1.3 detector, which entered single-quote state on the mid-value apostrophe and so missed the comment (<code>docs/tasks/_audits/2026-07-18-fm001-nested-map-fork.md</code> §5: task-lint's quote model is right, the migrator's was wrong). No existing clause is weakened: 1.3's quoted-value protection (edges #5/#6) is preserved exactly - a value that BEGINS with a quote is still honoured. Test: <code>t10_apostrophe_then_hash_is_a_comment</code></p>\n<h2>Scope</h2>\n<p>In scope: <code>TASK-TEMPLATE.md</code>, the new <code>docs-tools/fm001-migrate.mjs</code> (BOTH capabilities - the trailing-comment move and the nested-map flatten, plus the §1.9 quote fix) + its suite, the build.sh vendor list, this repo's corpus (trailing-comment class already migrated at 4c02b556; this task clears the nested-map residual across 140 specs and the one apostrophe edge), and a note in the install summary telling a consumer repo the migrator exists.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Running the migrator against consumer repos. This ships the tool; the operator runs it per repo. An agent that reaches into another repo and rewrites 500 files is not a migration, it is an incident.</li><li>FM-112's <code># UNREVIEWED</code> markers. Moving a marker to its own line keeps it firing, correctly - a human still has not confirmed <code>ai_authorship</code>. Clearing FM-112 is a human attestation and this task does not forge it.</li><li>The other 2 rules TASK-EVAL-001 trips (FM-004, FM-112). Out of cone.</li><li>Any change to FM-001 itself. The rule is right for BOTH classes; only the template (class 1) and the specs (class 2) were wrong. Specifically, RELAXING FM-001 to accept nested maps (the investigation's route b) is REJECTED: nested maps are outside the documented task@1 subset, no tool reads <code>build_envelope</code>, and task-lint rejects them by design - accepting them would be a <code>task@2</code> schema bump, not a lint fix. This task takes route (a): migrate the nested maps to the flat shape a <code>done</code> sibling already uses. The lint is unchanged.</li></ul>\n<h2>3. Edge case matrix</h2>\n<table><thead><tr><th>#</th><th>Category</th><th>Trigger</th><th>Expected</th><th>Test</th></tr></thead><tbody><tr><td>1</td><td>NULL/EMPTY</td><td>frontmatter with no comments</td><td>byte-identical no-op</td><td>t05</td></tr><tr><td>2</td><td>NULL/EMPTY</td><td>file with no frontmatter block</td><td>refuse, exit 2, name the file</td><td>t07</td></tr><tr><td>3</td><td>BOUNDS</td><td>comment is the whole line already</td><td>left alone, not doubled</td><td>t05</td></tr><tr><td>4</td><td>BOUNDS</td><td><code>#</code> at column 0 inside frontmatter</td><td>already own-line, untouched</td><td>t05</td></tr><tr><td>5</td><td>MALFORMED</td><td><code>#</code> inside a double-quoted value</td><td>NOT a comment, untouched</td><td>t03</td></tr><tr><td>6</td><td>MALFORMED</td><td><code>#</code> inside a single-quoted value</td><td>NOT a comment, untouched</td><td>t03</td></tr><tr><td>7</td><td>MALFORMED</td><td><code>#</code> with no leading whitespace (<code>a#b</code>)</td><td>NOT a comment, untouched</td><td>t03</td></tr><tr><td>8</td><td>MALFORMED</td><td>list item <code>- path  # note</code></td><td>comment moves above the item, indent preserved</td><td>t02</td></tr><tr><td>9</td><td>MALFORMED</td><td>CRLF line endings</td><td>round-trip preserved, never normalized</td><td>t02</td></tr><tr><td>10</td><td>MALFORMED</td><td>aligned continuation comment (template lines 6-9)</td><td>folded into the own-line block</td><td>t01</td></tr><tr><td>11</td><td>CONCURRENT</td><td>two migrators, same file</td><td>two-phase atomic write, last wins, never truncated</td><td>t05</td></tr><tr><td>12</td><td>SECURITY</td><td>path escapes the repo root</td><td>REFUSED, not executed</td><td>t07</td></tr><tr><td>13</td><td>SECURITY</td><td>path exists but is untracked at HEAD</td><td>REFUSED - an untracked spec is not corpus</td><td>t07</td></tr><tr><td>14</td><td>DEGRADATION</td><td>git absent / not a repo</td><td>refuse and say so; never migrate unguarded</td><td>t07</td></tr><tr><td>15</td><td>DEGRADATION</td><td>file unreadable mid-run</td><td>report the file, exit non-zero, migrate nothing</td><td>t07</td></tr><tr><td>16</td><td>NESTED MAP</td><td><code>build_envelope:</code> with 6 map children</td><td>6 top-level keys; values + order preserved; FM-001 cleared</td><td>t09</td></tr><tr><td>17</td><td>NESTED MAP</td><td>top-level <code>new_files</code> + <code>build_envelope.new_files</code>, one item shared</td><td>order-preserving union; exact dup deduped; nothing dropped; no FM-003</td><td>t09</td></tr><tr><td>18</td><td>NESTED MAP</td><td>a <code>done</code> spec carrying <code>build_envelope</code></td><td>flattened; body untouched, so <code>audited_body_sha256_prefix</code> holds</td><td>t04, t09</td></tr><tr><td>19</td><td>MALFORMED</td><td>hoisted key collides as a scalar with a different value</td><td>HALT; name the file; migrate nothing in it</td><td>t09</td></tr><tr><td>20</td><td>MALFORMED</td><td>plain scalar <code>broker's ... #4</code> (apostrophe then <code> #</code>)</td><td><code> #4</code> detected as a comment and moved own-line</td><td>t03, t10</td></tr><tr><td>21</td><td>NESTED MAP</td><td>flatten output re-run through the migrator</td><td>byte-identical no-op (idempotent across both passes)</td><td>t05, t09</td></tr></tbody></table>\n<h2>4. Out of scope / non-goals</h2>\n<p>See \"## Scope -&gt; ### Out of scope / Non-Goals\" above - this section is the engineering half's pointer to it.</p>\n<h2>Acceptance criteria</h2>\n<ul><li>AC1: <code>TASK-TEMPLATE.md</code> passes task-lint with zero FM-001. Cited: <code>t01_template_is_clean</code>.</li><li>AC2: A spec authored fresh from the migrated template passes task-lint clean. Cited: <code>t01_template_is_clean</code>.</li><li>AC3: The migrator satisfies 1.2-1.5 and 1.7. Cited: <code>t02</code>..<code>t05</code>, <code>t07</code>.</li><li>AC4: <code>docs/tasks/*/*/spec.md</code> reports zero FM-001 corpus-wide. Cited: <code>t06_corpus_is_fm001_clean</code>.</li><li>AC5: The 19 audit-bound specs keep their <code>audited_body_sha256_prefix</code> byte-for-byte across the migration. Cited: <code>t04_body_is_untouched_and_body_hash_holds</code>.</li><li>AC6: <code>dist/cyberos/docs-tools/fm001-migrate.mjs</code> exists and matches source. Cited: <code>t08_payload_carries_it</code>.</li><li>AC7: The 140 nested-map (<code>build_envelope</code>) specs are flattened - their children hoisted to top-level keys, values and order preserved, collisions unioned - and the corpus FM-001 count is 0 INCLUDING them. Cited: <code>t09_flattens_nested_map</code> (capability, scratch corpus) plus a real-corpus check: <code>node tools/install/docs-tools/task-lint.mjs --json docs/tasks</code>, parsing the JSON array and filtering <code>rule_id === \"FM-001\"</code>, reports length 0 (before: 4005 = 4004 nested-map + 1 apostrophe). AC4's \"FM-001 = 0 corpus-wide\" is now genuinely reachable because this second capability clears the class AC1-AC6's trailing-comment migrator never modelled.</li></ul>";
