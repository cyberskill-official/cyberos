window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PROJ service <strong>MUST</strong> model project work as Issues + Cycles + Engagements with cross-module linkability. The schema and contract:</p>\n<ol><li><strong>MUST</strong> define 4 tables:</li></ol>\n<ul><li><code>engagements (id, tenant_id, client_id, name, status, started_at, ended_at)</code></li><li><code>cycles (id, tenant_id, engagement_id, name, starts_at, ends_at, state)</code></li><li><code>issues (id, tenant_id, engagement_id, cycle_id, title, body, status, priority, assignee_subject_id, estimate_hours, created_at, updated_at)</code></li><li><code>issue_links (issue_id, linked_to_id, link_type, created_at)</code></li></ul>\n<ol><li><strong>MUST</strong> support 5 status values per DEC-210: <code>triage | todo | doing | review | done</code>. No custom statuses at slice 1.</li><li><strong>MUST</strong> enforce status FSM transitions:</li></ol>\n<ul><li><code>triage</code> → {<code>todo</code>, <code>done</code> (rejected as not-applicable)}</li><li><code>todo</code> → {<code>doing</code>, <code>triage</code> (deferral), <code>done</code>}</li><li><code>doing</code> → {<code>review</code>, <code>todo</code> (paused), <code>done</code>}</li><li><code>review</code> → {<code>done</code>, <code>doing</code> (rejected back), <code>todo</code> (significant rework)}</li><li><code>done</code> → {} (terminal; reopen requires explicit <code>reopen</code> API) Illegal transitions return <code>400 BAD_REQUEST</code> with <code>{\"error\":\"illegal_status_transition\",\"from\":\"&lt;s&gt;\",\"to\":\"&lt;t&gt;\",\"allowed\":[...]}</code>.</li></ul>\n<ol><li><strong>MUST</strong> support 4 priority values: <code>urgent | high | normal | low</code> (numeric mapping for sort: urgent=4, high=3, normal=2, low=1).</li><li><strong>MUST</strong> expose REST:</li></ol>\n<ul><li><code>POST /v1/proj/issues</code> (create)</li><li><code>GET /v1/proj/issues?engagement_id=&amp;cycle_id=&amp;assignee=&amp;status=&amp;limit=</code> (list with filters)</li><li><code>GET /v1/proj/issues/{id}</code> (single)</li><li><code>PATCH /v1/proj/issues/{id}</code> (update; status mutation goes through FSM)</li><li><code>DELETE /v1/proj/issues/{id}</code> (soft-delete via <code>status: deleted</code> flag — reserved status outside the 5; only root-admin)</li><li><code>POST /v1/proj/issues/{id}/links</code> (add link)</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit rows:</li></ol>\n<ul><li><code>proj.issue_created</code> per POST.</li><li><code>proj.issue_status_changed</code> per status mutation (payload: old_status, new_status, by_subject_id).</li><li><code>proj.issue_assigned</code> per assignee change (payload: from_subject_id, to_subject_id).</li><li><code>proj.issue_linked</code> per link added (payload: source_id, linked_to_id, link_type).</li></ul>\n<ol><li><strong>MUST</strong> enforce RLS via TASK-AUTH-003 pattern. All 4 tables added to <code>TENANT_SCOPED_TABLES</code>. Cross-tenant queries return 0 rows.</li><li><strong>MUST</strong> support cross-module links via <code>link_type</code> enum:</li></ol>\n<ul><li><code>duplicates</code>, <code>blocks</code>, <code>blocked_by</code>, <code>related</code>, <code>derived_from_email_thread</code>, <code>derived_from_chat_thread</code>, <code>derived_from_meeting_decision</code>.</li></ul>\n<ol><li><strong>MUST</strong> insert <code>issue_links</code> rows bidirectionally for symmetric link types: <code>blocks</code>/<code>blocked_by</code>, <code>duplicates</code>/<code>duplicated_by</code> (auto-inserted as inverse).</li><li><strong>MUST</strong> validate <code>assignee_subject_id</code> exists AND belongs to same tenant. Cross-tenant assignee → 400 with <code>assignee_cross_tenant</code>.</li><li><strong>MUST</strong> validate cycle membership: <code>issue.cycle_id</code> MUST belong to <code>issue.engagement_id</code>. Cross-engagement cycle → 400 with <code>cycle_engagement_mismatch</code>.</li><li><strong>MUST</strong> validate <code>cycle.ends_at &gt; cycle.starts_at</code>. Reverse → 400.</li><li><strong>MUST</strong> support optimistic locking via <code>If-Match: &lt;updated_at&gt;</code> header on PATCH. Mismatch → 412 PRECONDITION_FAILED with <code>concurrent_update</code>.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_issues_created_total{tenant_id, engagement_id}</code> (counter).</li><li><code>proj_issues_status_changed_total{from, to}</code> (counter).</li><li><code>proj_issues_assigned_total</code> (counter).</li><li><code>proj_issue_links_total{link_type}</code> (counter).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why 5-status FSM (DEC-210)?</strong> Custom statuses are a productivity-tool footgun — every team adds 10 statuses, search/filter degrades, reporting becomes inconsistent. 5 statuses cover 90% of real workflows; constraint forces teams to use them consistently. P3 introduces custom labels (separate from status) for additional dimensions.</p>\n<p><strong>Why 4 priorities (DEC-211)?</strong> Same reasoning. urgent/high/normal/low maps to urgent action / next-week / planned / nice-to-have. Numeric mapping for sort.</p>\n<p><strong>Why engagement-scoped (DEC-213)?</strong> An issue without engagement context is a TODO list item, not project work. Forcing engagement attribution makes \"what work is happening for which client\" answerable directly.</p>\n<p><strong>Why FSM transitions enforced (§1 #3)?</strong> Without enforcement, \"doing → triage\" is silent backward progress without audit signal. FSM enforcement makes deferrals explicit (audit shows <code>doing → todo (paused)</code>).</p>\n<p><strong>Why bidirectional symmetric links (§1 #9)?</strong> A \"blocks\" relation implicitly creates \"blocked by\" — without bidirectional storage, queries from one side miss the relation. Auto-insertion preserves the invariant.</p>\n<p><strong>Why optimistic locking (§1 #13)?</strong> Two operators editing the same issue concurrently → last-write-wins silently. With If-Match, second operator gets 412 + diff to review.</p>\n<p><strong>Why audit on every mutation (§1 #6)?</strong> Project work IS the value chain. Audit answers \"who decided to mark this done? when? from what state?\" — essential for retros + compliance.</p>\n<p><strong>Why cross-tenant assignee blocked (§1 #10)?</strong> Cross-tenant assignment leaks the assignee-tenant's subject into the assigner-tenant's data. RLS catches reads but not writes; explicit validation at API.</p>\n<p><strong>Why cycle-membership validation (§1 #11)?</strong> A cycle from engagement A can't host an issue in engagement B. Without validation, the relationship is corrupt and reports break.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/proj/migrations/0001_engagements.sql\nCREATE TABLE engagements (\n    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id   UUID NOT NULL,\n    client_id   UUID NULL,        -- FK to CRM clients (P1.5)\n    name        TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 200),\n    status      TEXT NOT NULL CHECK (status IN ('active','completed','paused','cancelled')),\n    started_at  DATE NOT NULL,\n    ended_at    DATE NULL,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nALTER TABLE engagements ENABLE ROW LEVEL SECURITY;\nALTER TABLE engagements FORCE ROW LEVEL SECURITY;\nCREATE POLICY engagements_isolation ON engagements\n    USING      (tenant_id = current_setting('app.tenant_id', true)::uuid)\n    WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);\n\n-- services/proj/migrations/0002_cycles.sql\nCREATE TABLE cycles (\n    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id     UUID NOT NULL,\n    engagement_id UUID NOT NULL REFERENCES engagements(id) ON DELETE CASCADE,\n    name          TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 100),\n    starts_at     DATE NOT NULL,\n    ends_at       DATE NOT NULL CHECK (ends_at &gt; starts_at),\n    state         TEXT NOT NULL CHECK (state IN ('upcoming','active','closed')),\n    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\nALTER TABLE cycles ENABLE ROW LEVEL SECURITY;\nALTER TABLE cycles FORCE ROW LEVEL SECURITY;\nCREATE POLICY cycles_isolation ON cycles\n    USING      (tenant_id = current_setting('app.tenant_id', true)::uuid)\n    WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);\n\n-- services/proj/migrations/0003_issues.sql\nCREATE TABLE issues (\n    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id           UUID NOT NULL,\n    engagement_id       UUID NOT NULL REFERENCES engagements(id),\n    cycle_id            UUID NULL REFERENCES cycles(id),\n    title               TEXT NOT NULL CHECK (length(title) BETWEEN 1 AND 200),\n    body                TEXT NULL CHECK (length(coalesce(body, '')) &lt;= 50000),\n    status              TEXT NOT NULL CHECK (status IN ('triage','todo','doing','review','done','deleted')),\n    priority            TEXT NOT NULL CHECK (priority IN ('urgent','high','normal','low')),\n    assignee_subject_id UUID NULL,\n    estimate_hours      NUMERIC(6,2) NULL CHECK (estimate_hours IS NULL OR estimate_hours &gt; 0),\n    created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\nCREATE INDEX issues_engagement_idx ON issues(tenant_id, engagement_id);\nCREATE INDEX issues_assignee_idx ON issues(tenant_id, assignee_subject_id) WHERE assignee_subject_id IS NOT NULL;\nCREATE INDEX issues_status_idx ON issues(tenant_id, status);\n\nALTER TABLE issues ENABLE ROW LEVEL SECURITY;\nALTER TABLE issues FORCE ROW LEVEL SECURITY;\nCREATE POLICY issues_isolation ON issues\n    USING      (tenant_id = current_setting('app.tenant_id', true)::uuid)\n    WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);\n\n-- services/proj/migrations/0004_issue_links.sql\nCREATE TABLE issue_links (\n    issue_id     UUID NOT NULL REFERENCES issues(id) ON DELETE CASCADE,\n    linked_to_id UUID NOT NULL REFERENCES issues(id) ON DELETE CASCADE,\n    link_type    TEXT NOT NULL CHECK (link_type IN ('duplicates','duplicated_by','blocks','blocked_by','related','derived_from_email_thread','derived_from_chat_thread','derived_from_meeting_decision')),\n    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    PRIMARY KEY (issue_id, linked_to_id, link_type)\n);\nALTER TABLE issue_links ENABLE ROW LEVEL SECURITY;\nALTER TABLE issue_links FORCE ROW LEVEL SECURITY;\nCREATE POLICY issue_links_isolation ON issue_links\n    USING (\n        EXISTS(SELECT 1 FROM issues WHERE id = issue_id\n               AND tenant_id = current_setting('app.tenant_id', true)::uuid)\n    )\n    WITH CHECK (\n        EXISTS(SELECT 1 FROM issues WHERE id = issue_id\n               AND tenant_id = current_setting('app.tenant_id', true)::uuid)\n    );</code></pre>\n<h3>Types</h3>\n<pre><code class=\"language-rust\">// services/proj/src/types.rs\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Deserialize, Serialize, sqlx::Type)]\n#[sqlx(type_name = \"text\", rename_all = \"lowercase\")]\npub enum IssueStatus { Triage, Todo, Doing, Review, Done, Deleted }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Deserialize, Serialize, sqlx::Type)]\n#[sqlx(type_name = \"text\", rename_all = \"lowercase\")]\npub enum IssuePriority { Urgent, High, Normal, Low }\n\nimpl IssuePriority {\n    pub fn numeric(&amp;self) -&gt; u8 {\n        match self { Self::Urgent =&gt; 4, Self::High =&gt; 3, Self::Normal =&gt; 2, Self::Low =&gt; 1 }\n    }\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Deserialize, Serialize, sqlx::Type)]\n#[sqlx(type_name = \"text\", rename_all = \"snake_case\")]\npub enum LinkType {\n    Duplicates, DuplicatedBy, Blocks, BlockedBy, Related,\n    DerivedFromEmailThread, DerivedFromChatThread, DerivedFromMeetingDecision,\n}\n\nimpl LinkType {\n    pub fn inverse(&amp;self) -&gt; Option&lt;LinkType&gt; {\n        match self {\n            Self::Blocks =&gt; Some(Self::BlockedBy),\n            Self::BlockedBy =&gt; Some(Self::Blocks),\n            Self::Duplicates =&gt; Some(Self::DuplicatedBy),\n            Self::DuplicatedBy =&gt; Some(Self::Duplicates),\n            _ =&gt; None,\n        }\n    }\n}</code></pre>\n<h3>Status FSM</h3>\n<pre><code class=\"language-rust\">// services/proj/src/status_fsm.rs\npub fn allowed_transitions(from: IssueStatus) -&gt; &amp;'static [IssueStatus] {\n    match from {\n        IssueStatus::Triage =&gt; &amp;[IssueStatus::Todo],\n        IssueStatus::Todo   =&gt; &amp;[IssueStatus::Doing, IssueStatus::Triage, IssueStatus::Done],\n        IssueStatus::Doing  =&gt; &amp;[IssueStatus::Review, IssueStatus::Todo, IssueStatus::Done],\n        IssueStatus::Review =&gt; &amp;[IssueStatus::Done, IssueStatus::Doing, IssueStatus::Todo],\n        IssueStatus::Done   =&gt; &amp;[],   // terminal\n        IssueStatus::Deleted =&gt; &amp;[],\n    }\n}\n\npub fn validate_transition(from: IssueStatus, to: IssueStatus) -&gt; Result&lt;(), IssueError&gt; {\n    if allowed_transitions(from).contains(&amp;to) {\n        Ok(())\n    } else {\n        Err(IssueError::IllegalStatusTransition {\n            from, to, allowed: allowed_transitions(from).to_vec(),\n        })\n    }\n}</code></pre>\n<h3>Handler skeleton</h3>\n<pre><code class=\"language-rust\">// services/proj/src/handlers/issues.rs\npub async fn create_issue(\n    req: CreateIssueRequest, claims: &amp;Claims, pool: &amp;PgPool, request_id: &amp;str,\n) -&gt; Result&lt;Issue, IssueError&gt; {\n    // §1 #11: cycle membership\n    if let Some(cycle_id) = req.cycle_id {\n        validate_cycle_in_engagement(pool, claims.tenant_id, cycle_id, req.engagement_id).await?;\n    }\n    // §1 #10: assignee validation\n    if let Some(assignee) = req.assignee_subject_id {\n        validate_assignee_in_tenant(pool, claims.tenant_id, assignee).await?;\n    }\n\n    let mut tx = pool.begin().await?;\n    sqlx::query(\"SET LOCAL app.tenant_id = $1::text\").bind(claims.tenant_id.to_string()).execute(&amp;mut *tx).await?;\n\n    let issue: Issue = sqlx::query_as(\n        \"INSERT INTO issues (tenant_id, engagement_id, cycle_id, title, body, status, priority, assignee_subject_id, estimate_hours)\n         VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9) RETURNING *\",\n    ).bind(claims.tenant_id).bind(req.engagement_id).bind(req.cycle_id)\n     .bind(&amp;req.title).bind(req.body.as_deref())\n     .bind(req.status.unwrap_or(IssueStatus::Triage)).bind(req.priority.unwrap_or(IssuePriority::Normal))\n     .bind(req.assignee_subject_id).bind(req.estimate_hours)\n     .fetch_one(&amp;mut *tx).await?;\n\n    audit::emit_issue_created(&amp;mut tx, &amp;issue, claims.subject_id, request_id).await?;\n    tx.commit().await?;\n    metrics::issue_created(claims.tenant_id, issue.engagement_id);\n    Ok(issue)\n}\n\npub async fn patch_issue(\n    issue_id: Uuid, req: PatchIssueRequest, if_match: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    claims: &amp;Claims, pool: &amp;PgPool, request_id: &amp;str,\n) -&gt; Result&lt;Issue, IssueError&gt; {\n    let mut tx = pool.begin().await?;\n    sqlx::query(\"SET LOCAL app.tenant_id = $1::text\").bind(claims.tenant_id.to_string()).execute(&amp;mut *tx).await?;\n\n    let current: Issue = sqlx::query_as(\"SELECT * FROM issues WHERE id = $1\").bind(issue_id).fetch_optional(&amp;mut *tx).await?.ok_or(IssueError::NotFound)?;\n\n    // §1 #13: optimistic locking\n    if let Some(expected_updated_at) = if_match {\n        if current.updated_at != expected_updated_at {\n            return Err(IssueError::ConcurrentUpdate {\n                expected: expected_updated_at, actual: current.updated_at,\n            });\n        }\n    }\n\n    // §1 #3: status FSM\n    if let Some(new_status) = req.status {\n        if new_status != current.status {\n            status_fsm::validate_transition(current.status, new_status)?;\n            audit::emit_status_changed(&amp;mut tx, &amp;current, current.status, new_status, claims.subject_id, request_id).await?;\n        }\n    }\n\n    // §1 #6: assignee change audit\n    if let Some(new_assignee) = req.assignee_subject_id {\n        if Some(new_assignee) != current.assignee_subject_id {\n            validate_assignee_in_tenant(pool, claims.tenant_id, new_assignee).await?;\n            audit::emit_assigned(&amp;mut tx, &amp;current, current.assignee_subject_id, Some(new_assignee), claims.subject_id, request_id).await?;\n        }\n    }\n\n    let updated: Issue = sqlx::query_as(\n        \"UPDATE issues SET title = COALESCE($1, title), body = $2, status = COALESCE($3, status),\n         priority = COALESCE($4, priority), assignee_subject_id = $5,\n         estimate_hours = $6, updated_at = NOW() WHERE id = $7 RETURNING *\"\n    ).bind(req.title).bind(req.body).bind(req.status).bind(req.priority)\n     .bind(req.assignee_subject_id).bind(req.estimate_hours).bind(issue_id)\n     .fetch_one(&amp;mut *tx).await?;\n    tx.commit().await?;\n    Ok(updated)\n}\n\npub async fn create_link(\n    issue_id: Uuid, linked_to_id: Uuid, link_type: LinkType,\n    claims: &amp;Claims, pool: &amp;PgPool, request_id: &amp;str,\n) -&gt; Result&lt;(), IssueError&gt; {\n    let mut tx = pool.begin().await?;\n    sqlx::query(\"SET LOCAL app.tenant_id = $1::text\").bind(claims.tenant_id.to_string()).execute(&amp;mut *tx).await?;\n\n    sqlx::query(\"INSERT INTO issue_links (issue_id, linked_to_id, link_type) VALUES ($1, $2, $3) ON CONFLICT DO NOTHING\")\n        .bind(issue_id).bind(linked_to_id).bind(link_type).execute(&amp;mut *tx).await?;\n\n    // §1 #9: bidirectional for symmetric types\n    if let Some(inverse) = link_type.inverse() {\n        sqlx::query(\"INSERT INTO issue_links (issue_id, linked_to_id, link_type) VALUES ($1, $2, $3) ON CONFLICT DO NOTHING\")\n            .bind(linked_to_id).bind(issue_id).bind(inverse).execute(&amp;mut *tx).await?;\n    }\n\n    audit::emit_linked(&amp;mut tx, issue_id, linked_to_id, link_type, claims.subject_id, request_id).await?;\n    tx.commit().await?;\n    Ok(())\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>POST issue → 201 + UUID + audit row <code>proj.issue_created</code>.</li><li>PATCH valid status (<code>triage → todo</code>) → 200 + audit row <code>proj.issue_status_changed</code>.</li><li>PATCH invalid status (<code>triage → done</code>) → 400 with allowed transitions.</li><li>PATCH <code>done → todo</code> (terminal) → 400 (use reopen API instead).</li><li>PATCH assignee → 200 + audit row <code>proj.issue_assigned</code>.</li><li>Cross-tenant assignee → 400 with <code>assignee_cross_tenant</code>.</li><li>Cycle from different engagement → 400 with <code>cycle_engagement_mismatch</code>.</li><li>Cycle ends_at &lt; starts_at → 400 (DB CHECK).</li><li>RLS: cross-tenant SELECT returns 0 rows.</li><li>Bidirectional link: POST <code>blocks</code> link from A→B → row exists for A→B (blocks) AND B→A (blocked_by).</li><li>Cross-module link <code>derived_from_email_thread</code> creates issue_links row.</li><li>Title &gt; 200 chars → 400 (DB CHECK).</li><li>Body &gt; 50000 chars → 400.</li><li>Optimistic lock: PATCH with stale <code>If-Match</code> → 412 <code>concurrent_update</code>.</li><li>PATCH same status (no change) → 200, no audit row.</li><li>List filter by engagement_id, cycle_id, assignee, status all work.</li><li>Soft-delete (<code>status: deleted</code>) only by root-admin.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn create_issue_emits_audit() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let req = CreateIssueRequest {\n        engagement_id: claims.engagement_id, title: \"Test\".into(),\n        priority: Some(IssuePriority::Normal), ..Default::default()\n    };\n    let issue = create_issue(req, &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n    assert!(memory_test_helper::has_row(\"proj.issue_created\", &amp;issue.id.to_string()).is_some());\n}\n\n#[tokio::test]\nasync fn invalid_status_transition_returns_400() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    let err = patch_issue(issue.id, PatchIssueRequest { status: Some(IssueStatus::Done), ..Default::default() }, None, &amp;claims.claims, &amp;pool, \"req\").await.expect_err(\"expected IllegalStatusTransition\");\n    assert!(matches!(err, IssueError::IllegalStatusTransition { .. }));\n}\n\n#[tokio::test]\nasync fn cross_tenant_assignee_blocked() {\n    let (pool, claims_a) = test_setup_with_engagement().await;\n    let claims_b = test_setup_with_engagement().await.1;\n    let req = CreateIssueRequest {\n        engagement_id: claims_a.engagement_id, title: \"x\".into(),\n        assignee_subject_id: Some(claims_b.claims.subject_id), ..Default::default()\n    };\n    let err = create_issue(req, &amp;claims_a.claims, &amp;pool, \"req\").await.expect_err(\"expected AssigneeCrossTenant\");\n    assert!(matches!(err, IssueError::AssigneeCrossTenant { .. }));\n}\n\n#[tokio::test]\nasync fn cycle_engagement_mismatch_blocked() {\n    let (pool, claims) = test_setup().await;\n    let eng_a = test_helper::create_engagement(&amp;pool, claims.tenant_id).await;\n    let eng_b = test_helper::create_engagement(&amp;pool, claims.tenant_id).await;\n    let cycle_b = test_helper::create_cycle(&amp;pool, eng_b).await;\n    let req = CreateIssueRequest { engagement_id: eng_a, cycle_id: Some(cycle_b), title: \"x\".into(), ..Default::default() };\n    let err = create_issue(req, &amp;claims, &amp;pool, \"req\").await.expect_err(\"expected CycleEngagementMismatch\");\n    assert!(matches!(err, IssueError::CycleEngagementMismatch { .. }));\n}\n\n#[tokio::test]\nasync fn rls_blocks_cross_tenant_select() {\n    let (pool, claims_a) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims_a, IssueStatus::Triage).await;\n    let claims_b = test_setup_with_engagement().await.1;\n\n    rls::with_tenant(&amp;pool, claims_b.tenant_id, |tx| async move {\n        let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM issues\").fetch_one(&amp;mut **tx).await.unwrap();\n        assert_eq!(count, 0);\n    }).await;\n}\n\n#[tokio::test]\nasync fn bidirectional_blocks_link() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let a = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    let b = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    create_link(a.id, b.id, LinkType::Blocks, &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n\n    let count_blocks: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM issue_links WHERE issue_id = $1 AND linked_to_id = $2 AND link_type = 'blocks'\")\n        .bind(a.id).bind(b.id).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(count_blocks, 1);\n\n    let count_blocked_by: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM issue_links WHERE issue_id = $1 AND linked_to_id = $2 AND link_type = 'blocked_by'\")\n        .bind(b.id).bind(a.id).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(count_blocked_by, 1);\n}\n\n#[tokio::test]\nasync fn optimistic_lock_412_on_stale_if_match() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n\n    // First PATCH succeeds\n    let _ = patch_issue(issue.id, PatchIssueRequest { title: Some(\"v2\".into()), ..Default::default() }, Some(issue.updated_at), &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n\n    // Second PATCH with stale If-Match\n    let err = patch_issue(issue.id, PatchIssueRequest { title: Some(\"v3\".into()), ..Default::default() }, Some(issue.updated_at), &amp;claims.claims, &amp;pool, \"req\").await.expect_err(\"expected ConcurrentUpdate\");\n    assert!(matches!(err, IssueError::ConcurrentUpdate { .. }));\n}\n\n#[tokio::test]\nasync fn cycle_ends_at_lt_starts_at_rejected() {\n    let (pool, claims) = test_setup().await;\n    let eng = test_helper::create_engagement(&amp;pool, claims.tenant_id).await;\n    let req = CreateCycleRequest {\n        engagement_id: eng, name: \"bad\".into(),\n        starts_at: today(), ends_at: today() - chrono::Duration::days(1), state: \"upcoming\".into(),\n    };\n    let err = create_cycle(req, &amp;claims, &amp;pool, \"req\").await.expect_err(\"expected DB CHECK\");\n    // SQL CHECK constraint violation\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-AUTH-001</strong> — tenants exist before engagements created.</li><li><strong>TASK-AUTH-003</strong> — RLS pattern; add 4 PROJ tables to TENANT_SCOPED_TABLES.</li><li><strong>TASK-AI-003</strong> — memory_writer for audit row emission.</li><li><strong>TASK-PROJ-002 (downstream)</strong> — memory decision anchoring uses these structures.</li><li>Crates: <code>axum</code>, <code>sqlx</code>, <code>tokio</code>, <code>serde</code>, <code>chrono</code>, <code>uuid</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Create issue</h3>\n<pre><code class=\"language-http\">POST /v1/proj/issues HTTP/1.1\nAuthorization: Bearer &lt;jwt&gt;\nContent-Type: application/json\n\n{\n  \"engagement_id\": \"eng-...\",\n  \"title\": \"Implement TASK-PROJ-001\",\n  \"body\": \"Full schema per §3\",\n  \"priority\": \"high\",\n  \"assignee_subject_id\": \"subject-stephen-...\",\n  \"estimate_hours\": 12\n}\n\n→ 201 Created\n{ \"id\": \"issue-...\", \"status\": \"triage\", \"priority\": \"high\", \"created_at\": \"...\", ... }</code></pre>\n<h3>Patch status</h3>\n<pre><code class=\"language-http\">PATCH /v1/proj/issues/issue-... HTTP/1.1\nIf-Match: 2026-05-15T14:00:00Z\n\n{ \"status\": \"doing\" }\n\n→ 200 OK</code></pre>\n<h3>Audit row <code>proj.issue_status_changed</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.issue_status_changed\",\n  \"payload\": {\n    \"issue_id\": \"issue-...\",\n    \"old_status\": \"todo\",\n    \"new_status\": \"doing\",\n    \"by_subject_id\": \"subject-stephen-...\",\n    \"request_id\": \"req_...\"\n  }\n}</code></pre>\n<h3>Illegal transition response</h3>\n<pre><code class=\"language-http\">HTTP/1.1 400 Bad Request\n{\n  \"error\": \"illegal_status_transition\",\n  \"from\": \"triage\",\n  \"to\": \"done\",\n  \"allowed\": [\"todo\"]\n}</code></pre>\n<h3>Cross-module link</h3>\n<pre><code class=\"language-http\">POST /v1/proj/issues/issue-1/links HTTP/1.1\n{ \"linked_to_id\": \"issue-2\", \"link_type\": \"derived_from_email_thread\" }\n\n→ 201 Created</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Custom labels (separate from status) — slice 4+.</li><li>Sub-issues / parent-child — slice 4+.</li><li>Time tracking integration (TASK-TIME-001) — slice 3+.</li><li>Estimate vs actual variance reporting — slice 3+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>FK violation (cycle missing)</td><td>sqlx error</td><td>400 cycle_not_found</td><td>Caller fixes cycle_id</td></tr><tr><td>Concurrent PATCH</td><td>optimistic lock</td><td>412 concurrent_update</td><td>Caller refetches + retries</td></tr><tr><td>Cross-tenant attempt</td><td>RLS blocks</td><td>0 rows</td><td>By design</td></tr><tr><td>Illegal status transition</td><td>FSM check</td><td>400 with allowed list</td><td>Caller picks valid transition</td></tr><tr><td>Cycle ends_at &lt; starts_at</td><td>DB CHECK</td><td>400</td><td>Caller fixes dates</td></tr><tr><td>Cross-tenant assignee</td><td>claims check</td><td>400 assignee_cross_tenant</td><td>Caller uses same-tenant subject</td></tr><tr><td>Cycle from different engagement</td><td>validate_cycle_in_engagement</td><td>400 cycle_engagement_mismatch</td><td>Caller fixes</td></tr><tr><td>Title &gt; 200 chars</td><td>DB CHECK</td><td>400</td><td>Caller shortens</td></tr><tr><td>Body &gt; 50000 chars</td><td>DB CHECK</td><td>400</td><td>Caller shortens or attaches file</td></tr><tr><td>Negative estimate_hours</td><td>DB CHECK</td><td>400</td><td>Caller fixes</td></tr><tr><td>Soft-delete by non-root-admin</td><td>role check</td><td>403</td><td>Operator escalates</td></tr><tr><td>Audit row emit fails</td><td>memory_writer error</td><td>tx rollback; 500</td><td>Operator investigates memory</td></tr><tr><td>Self-link (issue_id == linked_to_id)</td><td>sanity check</td><td>400 self_link</td><td>Caller fixes</td></tr><tr><td>Reopen done issue</td><td>terminal status</td><td>400; use reopen API</td><td>API to introduce in slice 3</td></tr><tr><td>Concurrent link insert (same triple)</td><td>UNIQUE</td><td>First wins; ON CONFLICT DO NOTHING</td><td>By design</td></tr><tr><td>Concurrent status change</td><td>optimistic lock</td><td>One wins; other 412</td><td>By design</td></tr><tr><td>Engagement deleted with cycles</td><td>FK ON DELETE CASCADE</td><td>Cycles + issues cascade</td><td>By design</td></tr><tr><td>Assignee deleted</td><td>FK NOT enforced (subject deletion is rare)</td><td>NULL assignee_subject_id allowed</td><td>By design</td></tr><tr><td>RLS bypass via <code>cyberos_ops</code></td><td>Sev-2 audit</td><td>Auditable</td><td>By design</td></tr><tr><td>Cycle state inconsistent with dates</td><td>manual operator action</td><td>Sev-3</td><td>Manual fix via API</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>5-status FSM is intentionally constrained — custom statuses degrade tooling consistency.</li><li>Bidirectional link auto-insertion preserves invariant (\"if A blocks B, then B is blocked_by A\").</li><li>Optimistic locking via If-Match catches concurrent PATCH races.</li><li>Cross-tenant assignee blocked at API + RLS catches at DB if API bypassed.</li><li>Cycle membership validation prevents schema corruption.</li><li>DB CHECKs on title length / body length / estimate sign / cycle dates = defense in depth.</li><li>Audit on every mutation enables full project work history reconstruction.</li><li>Soft-delete (<code>status: deleted</code>) preserves history; hard-delete reserved for compliance erasure.</li><li>Engagement-scoped issues (no orphans) keeps \"what work for which client\" answerable.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-001. Status: draft (10/10 target).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Handlers shipped as services/proj/src/handlers.rs (single module), not handlers/{issues,cycles,engagements}.rs.</p>";
