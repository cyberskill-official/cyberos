window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OKR-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The OKR service <strong>MUST</strong> ship weekly check-in at <code>services/okr/src/checkin/</code> with 1-10 confidence + rationale + 4-week trend + Monday reminder, 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate confidence in 1-10 per DEC-2000 (CHECK constraint).</li></ol>\n<ol><li><strong>MUST</strong> validate <code>confidence_trend</code> against closed enum per DEC-2001.</li></ol>\n<ol><li><strong>MUST</strong> compute trend at <code>trend_calculator.rs::trend(kr, current_week)</code>:</li></ol>\n<ul><li>Average confidence of last 4 weeks (excluding current).</li><li>improving: current &gt; avg + 1</li><li>declining: current &lt; avg - 1</li><li>steady: within ±1</li><li>unknown: &lt;2 prior weeks of data</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0005</code>: ``<code>sql CREATE TABLE okr_weekly_checkins ( checkin_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, kr_id UUID NOT NULL, iso_week CHAR(8) NOT NULL,  -- 'YYYY-Www' e.g. '2026-W20' version INT NOT NULL DEFAULT 1, confidence INT NOT NULL CHECK (confidence &gt;= 1 AND confidence &lt;= 10), rationale TEXT NOT NULL, submitted_by UUID NOT NULL, submitted_at TIMESTAMPTZ NOT NULL DEFAULT now(), trace_id CHAR(32), UNIQUE (tenant_id, kr_id, iso_week, version) ); CREATE INDEX checkins_kr_week_idx ON okr_weekly_checkins(tenant_id, kr_id, iso_week DESC); ALTER TABLE okr_weekly_checkins ENABLE ROW LEVEL SECURITY; CREATE POLICY checkins_rls ON okr_weekly_checkins USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON okr_weekly_checkins FROM cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> correct via new row per DEC-2002 — <code>version</code> increments; original preserved.</li></ol>\n<ol><li><strong>MUST</strong> run Monday reminder cron per DEC-2003 — for each KR owner missing this-week check-in, emit <code>okr.checkin_missing_alert</code>.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/okr/krs/{id}/checkins              body: {confidence, rationale} GET  /v1/okr/krs/{id}/checkins              (history desc time) GET  /v1/okr/krs/{id}/trend                 (derived from history) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-2004. PII per TASK-MEMORY-111: rationale SHA-256 hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from submit → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior check-in per DEC-2002.</li></ol>\n<ol><li><strong>MUST NOT</strong> accept confidence outside 1-10 per DEC-2000.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 1-10 + rationale (DEC-2000)?</strong> Number alone is noise; rationale captures the why for retro/learning.</p>\n<p><strong>Why immutable with version (DEC-2002)?</strong> Audit lineage; can't backdate confidence to look good in retros.</p>\n<p><strong>Why ISO week (DEC-2002)?</strong> Calendar weeks standard; UNIQUE constraint per week+version.</p>\n<p><strong>Why Monday reminder (DEC-2003)?</strong> Common cadence; gives owner full Monday to complete.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample check-in:</p>\n<pre><code class=\"language-json\">POST /v1/okr/krs/{id}/checkins\n{\n  \"confidence\": 7,\n  \"rationale\": \"On track; one risk is Q3 hire delay impacting milestone 4.\"\n}</code></pre>\n<p>Response with trend:</p>\n<pre><code class=\"language-json\">{\n  \"checkin_id\": \"uuid\",\n  \"confidence\": 7,\n  \"iso_week\": \"2026-W20\",\n  \"trend\": \"steady\",\n  \"rolling_4w_avg\": 6.8\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Confidence 1-10 CHECK</strong>. 2. <strong>confidence_trend enum cardinality 4</strong>. 3. <strong>Rationale required</strong>. 4. <strong>Per-week immutable</strong>. 5. <strong>Correction via version+1</strong>. 6. <strong>UNIQUE(kr_id, iso_week, version)</strong>. 7. <strong>Trend calc from 4-week rolling</strong>. 8. <strong>&lt;2 prior weeks → trend=unknown</strong>. 9. <strong>Monday reminder cron</strong>. 10. <strong>3 memory audit kinds emitted</strong>. 11. <strong>PII scrubbed (rationale SHA256)</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>KR owner-only submit</strong>. 14. <strong>Trace_id preserved</strong>. 15. <strong>Append-only via REVOKE</strong>. 16. <strong>History query desc time</strong>. 17. <strong>Trend recompute on each new check-in</strong>. 18. <strong>Missing check-in alert sev-3</strong>. 19. <strong>ISO week format enforced</strong>. 20. <strong>Rationale length capped 2000 chars</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn confidence_range_enforced() {\n    let r = ctx.submit_checkin(ctx.kr_id, 11, \"test\").await;\n    assert!(r.is_err());\n    let r2 = ctx.submit_checkin(ctx.kr_id, 0, \"test\").await;\n    assert!(r2.is_err());\n}\n\n#[tokio::test]\nasync fn correction_creates_v2() {\n    let ctx = TestContext::with_checkin_v1().await;\n    ctx.submit_checkin_correction(ctx.kr_id, 8, \"corrected\").await;\n    let history = ctx.fetch_checkins(ctx.kr_id, \"2026-W20\").await;\n    assert_eq!(history.len(), 2);\n    assert_eq!(history[1].version, 2);\n}\n\n#[tokio::test]\nasync fn trend_improving_after_3w_climb() {\n    let ctx = TestContext::with_checkin_history(vec![5, 6, 7]).await;\n    ctx.submit_checkin(ctx.kr_id, 9, \"great week\").await;\n    let trend = ctx.fetch_trend(ctx.kr_id).await;\n    assert_eq!(trend, \"improving\");\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-OKR-001. <strong>Downstream:</strong> TASK-OKR-006 (Monday digest uses check-ins). <strong>Cross-module:</strong> TASK-MCP-007 (reminder cron), TASK-AUTH-101 (KR owner role), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Confidence out of range</td><td>CHECK</td><td>400</td><td>1-10</td></tr><tr><td>Rationale empty</td><td>validate</td><td>400</td><td>provide text</td></tr><tr><td>Duplicate version race</td><td>UNIQUE</td><td>409</td><td>retry with v+1</td></tr><tr><td>Trend with &lt;2 data</td><td>unknown</td><td>inherent</td><td>inherent</td></tr><tr><td>Cron skipped</td><td>catch-up</td><td>sev-3</td><td>inherent</td></tr><tr><td>KR owner inactive</td><td>skip reminder</td><td>inherent</td><td>reassign</td></tr><tr><td>Cross-tenant submit</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>ISO week parse fail</td><td>validate</td><td>400</td><td>YYYY-Www format</td></tr><tr><td>Rationale &gt; 2000 chars</td><td>validate</td><td>400</td><td>shorten</td></tr><tr><td>Concurrent submit</td><td>UNIQUE</td><td>second 409</td><td>retry</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Trend calc pure function: <code>(history: Vec&lt;(week, confidence)&gt;, current) → Trend</code>.</li><li>§11.2 Reminder cron via TASK-MCP-007 <code>kind: 'okr.checkin_reminder'</code>, Monday 09:00.</li><li>§11.3 ISO week computed via <code>chrono::IsoWeek</code>.</li><li>§11.4 memory audit body: kr_id, iso_week, confidence; rationale SHA256.</li><li>§11.5 UI: shows 8-week sparkline + current confidence + trend arrow.</li></ul>\n<hr>\n<p><em>End of TASK-OKR-005 spec.</em></p>";
