window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-136"]="<h1>TASK-IMP-136: CI truth - CAF evals in root CI, hook-claim honesty, stub workflow sweep</h1>\n<h2>Summary</h2>\n<p>Three CI surfaces claim enforcement that does not happen: the CAF eval suite (40 fixtures + a validator) runs in no CI because its workflow sits nested under <code>tools/caf/.github/</code> where GitHub never reads it; <code>.pre-commit-config.yaml</code> declares an awh gate hook for a framework the repo does not use (<code>core.hooksPath=.githooks</code> is the real mechanism, and it runs no awh hook); and 9 auto-generated stub workflows report green on every PR while their only step is an <code>echo</code>. This task wires the CAF evals + <code>caf_precommit_check.sh</code> into a real root workflow, makes the hook claims honest (wire awh into <code>.githooks/pre-commit</code>, drop the dead framework config), and sweeps the 9 stubs (delete by default, implement where the declaring spec's embedded YAML is complete) with a stub-honesty check so the class cannot regrow.</p>\n<h2>Problem</h2>\n<p>Audit finding H9, all verified first-hand 2026-07-23:</p>\n<ol><li><strong>CAF evals are dead in CI.</strong> <code>tools/caf/core/evals/validate.py --all</code> validates 40 audit fixtures; the only workflow that runs it lives at <code>tools/caf/.github/workflows/evals.yml</code> - a nested <code>.github</code> directory GitHub Actions never evaluates. <code>scripts/caf_precommit_check.sh</code> exists and is wired into nothing. The CAF gate is a load-bearing step (ship-tasks step 29); its own regression suite runs on no machine but a developer's, voluntarily.</li><li><strong>The pre-commit claims are split-brain.</strong> <code>.pre-commit-config.yaml</code> tells a reader that awh-gate, payload-build, and docs-build run at commit time via the <code>pre-commit</code> framework. The repo's real hook path is <code>.githooks/</code> (set by <code>core.hooksPath</code>); its <code>pre-commit</code> script covers payload-build and docs-build directly but never runs <code>.pre-commit-hooks/awh-gate.sh</code>. Result: the awh commit-time gate exists only as a claim in a file no tool reads.</li><li><strong>Nine always-green stubs.</strong> Auto-generated 2026-05-17 from task <code>build_envelope</code> references, each with a single <code>echo</code> step. Several declaring tasks (<code>TASK-AI-013</code>, <code>TASK-AI-015</code>, <code>TASK-MEMORY-102</code>...) are <code>done</code> - shipped work whose acceptance story includes a CI gate that gates nothing. A green check that checks nothing is strictly worse than a missing check: it terminates the reader's investigation at a lie.</li></ol>\n<h2>Proposed Solution</h2>\n<p>Add <code>.github/workflows/caf-evals-gate.yml</code> running <code>python3 core/evals/validate.py --all</code> (from <code>tools/caf/</code>) plus <code>bash scripts/caf_precommit_check.sh</code> on PRs touching <code>tools/caf/**</code> or <code>scripts/caf_*</code> and on a weekly schedule (drift net). Wire <code>.pre-commit-hooks/awh-gate.sh</code> into <code>.githooks/pre-commit</code> behind the same staged-paths pattern the other blocks use, then delete <code>.pre-commit-config.yaml</code> (reason recorded: dead config whose every live claim is now covered by the real hook; keeping it preserves a second, contradictory hook story). Sweep the 9 stubs with a per-file disposition table in the implementation PR: default DELETE (each deletion names its declaring task in the commit body so the gap stays discoverable); implement instead where the declaring task's spec embeds canonical YAML that is complete and whose runtime dependencies exist today. Add <code>scripts/tests/test_ci_truth.sh</code> asserting: a root workflow invokes <code>validate.py --all</code>; a root workflow invokes <code>caf_precommit_check.sh</code>; no root workflow contains the stub placeholder marker; and <code>.pre-commit-config.yaml</code> either does not exist or every hook it names is invoked by <code>.githooks/pre-commit</code> (the regrowth guard). CHANGELOG entry records the sweep and the deleted file.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Move <code>tools/caf/.github/workflows/evals.yml</code> to the root instead of writing a new workflow.</strong> Considered, partially adopted: the new root workflow reuses its steps where they fit, but the nested file also serves the standalone-CAF-repo split (pages/publish siblings) and is left in place for that context; the root workflow is the monorepo's own gate. The nested file alone can never fire here, which is the defect.</li><li><strong>Keep <code>.pre-commit-config.yaml</code> and adopt the pre-commit framework for real.</strong> Rejected: two hook mechanisms racing on one repo (<code>core.hooksPath</code> + framework-managed <code>.git/hooks</code>) is a conflict by construction; <code>.githooks/</code> is where every existing live gate (payload, docs, status, run_all, version-sync) already lives, and the framework adds a Python dependency for zero new coverage.</li><li><strong>Label the stubs (rename check to <code>STUB-...</code>) instead of deleting.</strong> Rejected as the default: a labeled always-green check still occupies a required-check slot and still summarizes green; the honest states are \"real gate\" or \"no gate\". Labeling is acceptable only as a transitional state and G14 (TASK-IMP-140) treats a labeled stub as non-compliant for branch protection either way.</li><li><strong>Implement all 9 stubs fully in this task.</strong> Rejected: several require infrastructure that does not exist yet (Chromatic project, VN-PII quarterly data refresh pipeline); implementing them here balloons an enforcement-truth task into six feature tasks. The disposition rule (implement only where the embedded YAML is complete AND dependencies exist) keeps the judgment bounded, and deletions leave a named trail back to their declaring tasks for future re-authoring.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release, the CAF eval suite fails a PR that breaks a fixture (verifiable by a deliberate scratch-branch break), zero workflows in root <code>.github/workflows/</code> carry the stub placeholder marker, and <code>test_ci_truth.sh</code> is green in <code>run_all.sh</code>. Baseline today: CAF evals run in no CI, 9 stubs report green everywhere, no truth check exists.</li><li>Guardrail: no existing live workflow (payload-gate, awh-gate, docs-prerender-gate, services, deploy...) changes behavior; <code>.githooks/pre-commit</code> total runtime stays under ~2 minutes for an awh-relevant commit (awh-gate.sh already scopes itself to changed modules).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the new root CAF workflow, awh hook wiring, <code>.pre-commit-config.yaml</code> removal, the 9-stub sweep with disposition table, the <code>test_ci_truth.sh</code> regrowth guard, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>The root CI job for <code>scripts/tests/run_all.sh</code> - authored as TASK-IMP-128 (draft); this task's <code>test_ci_truth.sh</code> rides in <code>run_all.sh</code> and therefore lands in CI the moment IMP-128 ships. No ordering constraint between the two.</li><li>Re-authoring replacement tasks for deleted stubs (the deletion trail names the declaring tasks; re-authoring is the operator's call per gap).</li><li>The G5/G14 benchmark-gate definitions and their CI meta-check - TASK-IMP-140; this task's checker is the mechanism G14 adopts.</li><li>Branch-protection rule changes (server-side; operator action, see Operator steps in edge cases).</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. TASK-IMP-128 (draft) owns the run_all.sh CI job this task deliberately does not duplicate. TASK-IMP-140 (benchmark gates) adopts this task's <code>test_ci_truth.sh</code> as the G14 checker - soft forward reference via <code>related_tasks</code>, no cycle. The stub declarers (TASK-AI-013/015/018, TASK-MEMORY-102, TASK-OBS-005, TASK-PROJ-018, TASK-REW-010) are listed in <code>related_tasks</code> because each deletion cites its declarer; none of their statuses change here.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> stub count/contents, declarer headers, validate.py usage, the nested workflow path, the dead framework config, and the absence of any run_all.sh CI invocation were all verified first-hand at HEAD; the TASK-IMP-128 overlap was discovered during authoring and is recorded as a plan adjustment in <code>source_decisions</code>.</li><li><strong>Human review:</strong> the hardening plan was operator-approved 2026-07-23; the delete-by-default stub disposition and the framework-config removal are recorded decisions for the reviewer to confirm at the review acceptance gate (both are reversible file operations on a branch).</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 A root workflow <code>.github/workflows/caf-evals-gate.yml</code> MUST run <code>python3 core/evals/validate.py --all</code> with working directory <code>tools/caf</code> and <code>bash scripts/caf_precommit_check.sh</code> from the repo root, triggered on pull requests touching <code>tools/caf/**</code> or <code>scripts/caf_*</code> and on a weekly <code>schedule</code>. A fixture regression or checker failure MUST fail the workflow.</li><li>1.2 <code>.githooks/pre-commit</code> MUST invoke <code>.pre-commit-hooks/awh-gate.sh</code> when staged paths match the module-source pattern that script scopes itself to, using the same herestring <code>matches()</code> idiom the hook already uses for its other blocks (the SIGPIPE pitfall documented in the hook header MUST NOT be reintroduced).</li><li>1.3 <code>.pre-commit-config.yaml</code> MUST be removed once 1.2 lands, with the removal reason in the commit body and CHANGELOG (dead mechanism; every live claim now covered by <code>.githooks/pre-commit</code>). If the operator vetoes removal at review, the fallback is a header comment declaring the file non-authoritative - but removal is the authored default.</li><li>1.4 Each of the 9 stub workflows MUST be dispositioned: DELETE by default, with the commit body naming the file and its declaring task; IMPLEMENT instead when the declaring task's spec embeds complete canonical workflow YAML whose runtime dependencies (secrets, services, data) exist today. The implementation PR MUST carry the 9-row disposition table so the review gate sees every call explicitly. No stub may survive unchanged.</li><li>1.5 A new suite <code>scripts/tests/test_ci_truth.sh</code> MUST assert offline: (a) some root workflow invokes <code>validate.py --all</code>; (b) some root workflow invokes <code>caf_precommit_check.sh</code>; (c) no file under <code>.github/workflows/</code> contains the stub placeholder marker (<code>Stub - see task specs</code> / <code>Stub — see task specs</code>); (d) <code>.pre-commit-config.yaml</code> is absent OR every <code>entry:</code> it declares is also invoked from <code>.githooks/pre-commit</code>. Because it matches <code>scripts/tests/test_*.sh</code>, <code>run_all.sh</code>'s glob registers it automatically.</li><li>1.6 <code>CHANGELOG.md</code> MUST gain an entry recording the CAF CI gate, the awh hook wiring, the framework-config removal, and the stub sweep with its disposition counts.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - <code>caf-evals-gate.yml</code> exists at the root workflows dir, names both commands, both trigger paths, and a schedule; a deliberately broken fixture on a scratch branch makes <code>validate.py --all</code> exit non-zero - test: <code>scripts/tests/test_ci_truth.sh::t01_caf_gate_wired</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>.githooks/pre-commit</code> invokes <code>awh-gate.sh</code> via the <code>matches()</code> herestring idiom (no <code>git diff --cached | grep -q</code> pipeline), and a commit staging only unrelated paths does not trigger it - test: <code>scripts/tests/test_ci_truth.sh::t02_awh_hook_wired_safely</code></li><li>[ ] AC 3 (traces_to: #1.3) - <code>.pre-commit-config.yaml</code> does not exist, and CHANGELOG names its removal - test: <code>scripts/tests/test_ci_truth.sh::t03_dead_config_gone</code></li><li>[ ] AC 4 (traces_to: #1.4) - zero files under <code>.github/workflows/</code> match the stub placeholder marker, and for every deleted stub the commit body names file + declaring task (asserted against the disposition table committed with the PR) - test: <code>scripts/tests/test_ci_truth.sh::t04_no_stub_survives</code></li><li>[ ] AC 5 (traces_to: #1.5) - <code>test_ci_truth.sh</code> runs green under <code>bash scripts/tests/run_all.sh</code> discovery and each of its four asserts fails when its precondition is broken in a scratch copy (self-test mode) - test: <code>scripts/tests/test_ci_truth.sh::t05_self_test_negative_paths</code></li><li>[ ] AC 6 (traces_to: #1.6) - CHANGELOG's top entry mentions caf-evals-gate, awh hook, the removed config file, and the stub disposition counts - test: <code>scripts/tests/test_ci_truth.sh::t06_changelog_records_sweep</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>Branch protection referencing a stub check name:</strong> deleting a workflow whose check is \"required\" would wedge PRs server-side. Operator steps (per ship-tasks' in-task guideline rule): before merging the sweep, run <code>gh api repos/:owner/:repo/branches/main/protection</code> and confirm no required status check names any of the 9; expected output: none do (they were never wired as required - verify, don't assume). If one is required, the operator removes it from protection first; the PR notes it.</li><li><strong><code>vn-pii-recall</code> name collision with a real gate elsewhere:</strong> TASK-AI-013 shipped a real recall gate as a test suite; only the stub <em>workflow</em> is swept. The deletion note names TASK-AI-013 so the \"CI gate\" claim in that done spec is traceable to this sweep rather than silently orphaned.</li><li><strong>CAF evals runtime on ubuntu CI:</strong> 40 fixtures are validation-only (no model calls); if wall time exceeds the job budget, the workflow may shard by fixture prefix - but it MUST NOT subset silently: all 40 run per gate invocation.</li><li><strong>awh-gate.sh on a machine without awh installed:</strong> the hook script already degrades with a warning; wiring it must preserve that (a missing optional harness warns, never blocks an unrelated commit) - same posture as the hook's docs-build block.</li><li><strong>A future task re-declares a workflow stub:</strong> <code>test_ci_truth.sh</code> assert (c) fails the moment a placeholder-marker file lands under <code>.github/workflows/</code>, which is the regrowth guard - the honest path for a future declarer is shipping the real YAML or nothing.</li><li><strong>Security-class:</strong> the new workflow runs repo-pinned scripts on ubuntu runners with default token permissions; it MUST declare <code>permissions: contents: read</code> explicitly, and it introduces no new secret usage.</li></ul>";
