window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship the <code>cyberos-ten provision</code> CLI as the canonical ops-driven tenant provisioning flow. Each requirement:</p>\n<ol><li><strong>MUST</strong> define the <code>tenants</code> table with: <code>id UUID PRIMARY KEY</code>, <code>slug TEXT NOT NULL UNIQUE CHECK (slug ~ '^[a-z][a-z0-9-]{2,40}[a-z0-9]$')</code>, <code>display_name TEXT NOT NULL CHECK (length(display_name) BETWEEN 1 AND 200)</code>, <code>status tenant_status NOT NULL DEFAULT 'provisioning'</code>, <code>plan_tier TEXT NOT NULL DEFAULT 'starter'</code> (placeholder until TASK-TEN-002 ships full enum), <code>residency residency_code NOT NULL DEFAULT 'vn-1'</code>, <code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>provisioned_at TIMESTAMPTZ</code>, <code>terminated_at TIMESTAMPTZ</code>, <code>provisioned_by_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>. Full DDL in §3.1.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>tenant_status</code> Postgres enum with exactly 5 values (per DEC-326): <code>'provisioning'</code>, <code>'active'</code>, <code>'suspended'</code>, <code>'terminating'</code>, <code>'terminated'</code>. State transitions land in TASK-TEN-104; slice 1 sets <code>provisioning</code> → <code>active</code> at end of successful provisioning.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>residency_code</code> Postgres enum with exactly 4 values: <code>'vn-1'</code>, <code>'sg-1'</code>, <code>'eu-1'</code>, <code>'us-1'</code>. Adding a 5th is an ADR (mirrors the global-residency pattern).</li></ol>\n<ol><li><strong>MUST</strong> ship the <code>cyberos-ten provision</code> CLI subcommand accepting flags:</li></ol>\n<ul><li><code>--slug &lt;slug&gt;</code> (required) — kebab-case identifier per the regex.</li><li><code>--display-name &lt;text&gt;</code> (required) — human-readable name.</li><li><code>--root-admin-email &lt;email&gt;</code> (required) — initial root-admin subject's email.</li><li><code>--root-admin-display-name &lt;text&gt;</code> (required) — initial root-admin display name.</li><li><code>--residency &lt;vn-1|sg-1|eu-1|us-1&gt;</code> (optional; default <code>vn-1</code> per DEC-324).</li><li><code>--plan-tier &lt;starter|team|enterprise&gt;</code> (optional; default <code>starter</code>).</li><li><code>--json</code> (optional; print result as JSON instead of human-readable).</li></ul>\n<ol><li><strong>MUST</strong> be <strong>idempotent on slug</strong> (per DEC-329). Re-running with same <code>--slug</code> AND same <code>--residency</code> AND same <code>--plan-tier</code> returns the existing tenant + exit code 1 (idempotent-match). Same slug + different residency or plan_tier → exit code 65 with error <code>slug_collision_different_attrs</code>.</li></ol>\n<ol><li><strong>MUST</strong> execute the provisioning flow as a <strong>transactional 5-step orchestration</strong> (per DEC-321 + DEC-322 + DEC-323 + DEC-325):</li></ol>\n<ul><li>Step 1: validate inputs (slug regex, email format, residency in enum); fail-fast.</li><li>Step 2: INSERT into <code>tenants</code> with <code>status='provisioning'</code> + create initial <code>tenant_status_history</code> row.</li><li>Step 3: create Postgres schema <code>tenant_&lt;slug&gt;</code> via <code>CREATE SCHEMA</code> (idempotent — <code>IF NOT EXISTS</code>).</li><li>Step 4: create NATS account + subject ACL: subject namespace = <code>tenant.&lt;slug&gt;.&gt;</code> (NATS multi-level wildcard); ACL grants the tenant-admin role pub/sub on the namespace only.</li><li>Step 5: write S3 bucket marker objects: <code>s3://cyberos-doc-&lt;residency&gt;-&lt;scope&gt;/&lt;tenant_id&gt;/.cyberos-tenant-marker</code> for each scope (per TASK-DOC-001 + TASK-EMAIL-001 bucket layout); marker contains tenant slug + provisioned_at + provisioned_by; existence of marker is the lookup contract.</li><li>Step 6: delegate to TASK-AUTH-001 <code>POST /v1/admin/tenants</code> (internal helper) to register tenant in AUTH + create initial root-admin subject with <code>tenant-admin</code> role via TASK-AUTH-002 + TASK-AUTH-101.</li><li>Step 7: UPDATE tenant status <code>provisioning</code> → <code>active</code>; UPDATE <code>provisioned_at = now()</code>; INSERT a <code>tenant_status_history</code> row.</li><li>Step 8: emit <code>ten.tenant_provisioned</code> memory audit row with full provenance. Any step failure rolls back the entire flow (Postgres transactions for steps 2/3/6/7; compensating actions for NATS + S3).</li></ul>\n<ol><li><strong>MUST</strong> generate the initial root-admin <strong>password server-side</strong> using a cryptographically secure RNG (per DEC-331). Password shape: 32-char base62 (alphanumerics + safe symbols). The password is:</li></ol>\n<ul><li>Hashed (bcrypt cost 12 per TASK-AUTH-002) before persistence.</li><li>Printed ONCE to the operator's terminal (stdout, with a clear marker <code>=== ROOT ADMIN PASSWORD (RECORD IMMEDIATELY) ===</code>).</li><li><strong>Immediately zeroised</strong> from CLI memory via the <code>zeroize</code> crate after print + hash.</li><li>NEVER logged to any file, NEVER included in any memory audit row.</li></ul>\n<ol><li><strong>MUST</strong> emit <code>ten.tenant_provisioned</code> memory audit row at the end of successful provisioning. The row carries <code>{tenant_id, slug, display_name, status: 'active', plan_tier, residency, provisioned_by_subject_id_hash16, root_admin_subject_id_hash16, ts_ns}</code>. The row does NOT carry the root-admin password (per DEC-331) or the email (PII-scrubbed to hash16 per TASK-MEMORY-111).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS on <code>tenants</code> AND <code>tenant_status_history</code> with the <strong>superuser-only</strong> policy: <code>USING (current_setting('auth.is_root_admin', true) = 'true')</code>. Tenant management is a CyberSkill-operator privilege, not a tenant-admin privilege. Tenant-admins read their own tenant via TASK-TEN-107's SPA (slice 3+).</li></ol>\n<ol><li><strong>MUST</strong> be <strong>append-only</strong> on <code>tenants</code> AND <code>tenant_status_history</code> at the SQL-grant layer (per DEC-328). <code>REVOKE UPDATE, DELETE ON tenants, tenant_status_history FROM cyberos_app;</code>. Status transitions create new rows in history table; the <code>tenants.status</code> column is updated via the elevated <code>cyberos_provisioner</code> role (used by this task's CLI + TASK-TEN-104's offboarding orchestrator).</li></ol>\n<ol><li><strong>MUST</strong> use the <code>cyberos_provisioner</code> SQL role for the CLI's writes — distinct from <code>cyberos_app</code> (which has REVOKE'd UPDATE/DELETE). The provisioner role:</li></ol>\n<ul><li>Has INSERT on <code>tenants</code>, <code>tenant_status_history</code>, <code>tenant_residency_map</code>.</li><li>Has UPDATE on <code>tenants.status</code>, <code>tenants.provisioned_at</code>, <code>tenants.terminated_at</code> (only those columns).</li><li>Cannot DELETE anything.</li><li>Is granted to the CLI's connection role at startup.</li></ul>\n<ol><li><strong>MUST</strong> validate slug uniqueness at INSERT time via the <code>UNIQUE(slug)</code> constraint. Conflict → 23505 → handler maps to either exit 1 (idempotent match per §1 #5) or exit 65 (collision_different_attrs).</li></ol>\n<ol><li><strong>MUST</strong> emit exit codes per task-audit skill rule 9 + DEC-330:</li></ol>\n<ul><li>0 = success (new tenant provisioned).</li><li>1 = success-idempotent (existing tenant returned).</li><li>64 = invalid argument (missing required flag).</li><li>65 = invalid data (slug regex fail; email format fail; residency unknown; slug_collision_different_attrs).</li><li>73 = cant-create (Postgres / NATS / S3 / AUTH step failed; reversible — operator should investigate + retry).</li><li>75 = temp-fail (transient infra failure; retry advised).</li><li>77 = permission-denied (operator lacks root-admin role).</li></ul>\n<ol><li><strong>MUST</strong> require the operator running the CLI to have the <code>root-admin</code> role per TASK-AUTH-101 (cross-tenant superuser). The CLI verifies via the operator's JWT before any state mutation. Missing role → exit 77 immediately.</li></ol>\n<ol><li><strong>MUST</strong> complete the full provisioning flow in ≤ 30 seconds p95 (excludes any human input — slack for Postgres + NATS + S3 + AUTH round-trips). <code>provision_perf_test</code> asserts on a local stack.</li></ol>\n<ol><li><strong>MUST</strong> print a structured success block to stdout on completion: ``<code> ✓ Tenant provisioned tenant_id:        &lt;uuid&gt; slug:             &lt;slug&gt; display_name:     &lt;text&gt; residency:        &lt;code&gt; plan_tier:        &lt;code&gt; postgres_schema:  tenant_&lt;slug&gt; nats_namespace:   tenant.&lt;slug&gt;.&gt; s3_prefix:        &lt;tenant_id&gt;/ root_admin_email: &lt;email&gt; === ROOT ADMIN PASSWORD (RECORD IMMEDIATELY) === &lt;32-char-password&gt; === END === </code>``</li></ol>\n<p>With <code>--json</code>, output is a single JSON object (omitting the password — it's printed in a dedicated stderr block).</p>\n<ol><li><strong>MUST</strong> emit OTel span <code>ten.provision</code> with attributes: <code>slug</code>, <code>residency</code>, <code>plan_tier</code>, <code>outcome</code> (success | idempotent | slug_collision | invalid_input | step_failed_&lt;step&gt; | permission_denied | timeout).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>ten_provision_total{outcome, residency}</code> (counter).</li><li><code>ten_provision_duration_ms{outcome}</code> (histogram).</li><li><code>ten_tenant_count{status, residency}</code> (gauge — periodic compute).</li><li><code>ten_active_tenants_total</code> (gauge — count of <code>status='active'</code>).</li></ul>\n<ol><li><strong>MUST</strong> ship the <code>tenant_residency_map</code> table for downstream consumers: <code>(tenant_id UUID PRIMARY KEY REFERENCES tenants(id), residency residency_code NOT NULL, set_at TIMESTAMPTZ, set_by_subject_id UUID)</code>. TASK-DOC-001's <code>residency::resolve()</code> + TASK-EMAIL-001's <code>residency::resolve()</code> + TASK-AI-016's residency policy all consume this table. Slice 1 ships the table; provisioning writes the initial row; TASK-TEN-103 ships per-tenant residency change.</li></ol>\n<ol><li><strong>MUST</strong> create the tenant's memory audit-chain anchor (per DEC-332): emit a special chain-bootstrap row that becomes the genesis row for the tenant's audit chain. TASK-AI-003's memory_writer initialises the chain head from this row; subsequent rows chain to it.</li></ol>\n<ol><li><strong>MUST</strong> create the AUTH side via TASK-AUTH-001's internal helper <code>auth::admin::tenants::provision_tenant(slug, display_name, root_admin_email, root_admin_display_name, root_admin_password_hash, operator_subject_id)</code> returning <code>(tenant_id, root_admin_subject_id)</code>. This helper is exposed from TASK-AUTH-001 specifically for TASK-TEN-001's use; it is NOT exposed via REST (operator privilege only).</li></ol>\n<ol><li><strong>MUST</strong> support <code>cyberos-ten list</code> returning <code>id, slug, status, residency, plan_tier, created_at, provisioned_at</code> for all tenants. Caller MUST have <code>root-admin</code> role.</li></ol>\n<ol><li><strong>MUST</strong> support <code>cyberos-ten get --slug &lt;slug&gt;</code> returning full tenant detail (excluding root-admin password — never retrievable).</li></ol>\n<ol><li><strong>MUST</strong> validate <code>--root-admin-email</code> against the standard email regex (same as TASK-AUTH-002 §1 #2) AND check it's NOT already an existing AUTH subject in any tenant.</li></ol>\n<ol><li><strong>MUST</strong> treat slug as <strong>canonical and immutable</strong> post-provisioning. There is no <code>--rename</code> flag; slug rename requires manual SQL + ADR + downstream impact analysis (out of scope for slice 1).</li></ol>\n<ol><li><strong>MUST</strong> support <code>--dry-run</code> flag: validates all inputs + checks slug availability + checks residency policy + simulates each step, but DOES NOT write anything. Returns exit 0 if all checks pass; exit 65/73/77 on issues. Used by TASK-TEN-101's signup form preflight (slice 3+).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why ops-driven CLI at slice 1, not self-serve signup (DEC-320)?</strong> Self-serve signup (TASK-TEN-101) needs a polished web form + Stripe payment + fraud detection + email-verification — substantial. Ops-driven CLI is 5h of work and gets the core lifecycle primitive landed. CyberSkill at P2 has ~10 vertical-pack customers; CLI is the right tool for the volume. P3 graduates to self-serve when volume justifies the additional surface.</p>\n<p><strong>Why per-tenant Postgres schema namespace (DEC-321, §1 #6)?</strong> RLS alone is a \"shared schema, isolated by predicate\" pattern — a single SQL bug (forgetting <code>WHERE tenant_id = ?</code>) leaks across tenants. Per-tenant schema namespace adds defence in depth: the <code>tenant_&lt;slug&gt;</code> schema becomes the search_path; tables in other tenants' schemas aren't even visible without explicit qualification. Combined with RLS on shared metadata tables (where multi-tenant queries are legitimate), the leak surface narrows significantly.</p>\n<p><strong>Why NATS subject namespace per tenant (DEC-322)?</strong> NATS subscriptions are subject-pattern based. A subscriber listening on <code>&gt;</code> (catch-all) would receive every tenant's events — catastrophic data leak. Per-tenant prefix <code>tenant.&lt;slug&gt;.&gt;</code> limits what a tenant's subscribers can listen to (enforced by NATS ACLs at the account level). Cross-tenant pub/sub requires explicit gateway routing — auditable + intentional.</p>\n<p><strong>Why S3 per-tenant prefix (DEC-323)?</strong> S3 bucket-level isolation is too coarse (would need a bucket per tenant — operationally painful). Prefix isolation (<code>&lt;tenant_id&gt;/</code>) lets all tenants share a bucket while bucket policies + IAM enforce per-prefix access. TASK-DOC-001 + TASK-EMAIL-001 use this prefix in their S3 keys; the marker object at <code>&lt;tenant_id&gt;/.cyberos-tenant-marker</code> is the prefix-creation evidence.</p>\n<p><strong>Why password printed once + zeroised (DEC-331, §1 #7)?</strong> Ops staff need to deliver the root-admin password to the new tenant's admin (out-of-band; e.g. via in-person handoff or encrypted channel). Persisting the password anywhere (file, log, memory audit) creates a leak surface. Printing once + immediate zeroise from CLI memory means the only persisted form is the bcrypt hash (per TASK-AUTH-002).</p>\n<p><strong>Why operator role check at root-admin (§1 #14)?</strong> Provisioning a tenant is a cross-tenant operation — the operator must be in tenant 0 (CyberSkill itself) with the <code>root-admin</code> role per TASK-AUTH-101. Allowing any tenant-admin would let any tenant operator create new tenants — privilege escalation.</p>\n<p><strong>Why exit code 1 for idempotent match (DEC-329, §1 #5, §1 #13)?</strong> Exit 0 = \"I did the work\"; exit 1 = \"I didn't do the work because it was already done correctly\". Operators scripting CLI invocations need to distinguish (e.g. provisioning script in CI should treat both as success but log differently). Standard \"well-known\" idiom matches <code>git pull</code> behaviour.</p>\n<p><strong>Why slug regex <code>^[a-z][a-z0-9-]{2,40}[a-z0-9]$</code> (§1 #1)?</strong> Slug is the canonical identifier: appears in Postgres schema name (<code>tenant_&lt;slug&gt;</code>), NATS namespace (<code>tenant.&lt;slug&gt;.&gt;</code>), S3 marker, URLs. All these systems have different identifier rules — the intersection is lowercase alphanumeric + hyphens, starting alphabetic, no trailing hyphen. 4-42 char length covers typical company shortnames.</p>\n<p><strong>Why default residency vn-1 (DEC-324)?</strong> CyberSkill's home market is Vietnam; default to the home jurisdiction so operators don't accidentally provision EU/US tenants in VN-residency. The <code>--residency</code> flag is explicit override; TASK-TEN-103 will ship per-tenant residency change for migrations.</p>\n<p><strong>Why transactional 5-step orchestration (§1 #6)?</strong> Each step touches a different system (Postgres, NATS, S3, AUTH); partial failures leave orphan state (NATS account without DB row, etc.). Compensating actions (delete NATS account, delete S3 marker, delete AUTH tenant) on any step's failure ensures cleanup. The Postgres parts use a single transaction; NATS + S3 + AUTH have their own commit points but rollback via compensating action.</p>\n<p><strong>Why <code>cyberos_provisioner</code> SQL role distinct from <code>cyberos_app</code> (§1 #11)?</strong> App code (the runtime) MUST NOT mutate tenants — that's a privileged operation. Splitting roles enforces: the provisioner role is granted only to the CLI's connection; the app role REVOKE's UPDATE/DELETE. A bug in app code can't accidentally suspend a tenant.</p>\n<p><strong>Why memory chain anchor at provisioning (§1 #20, DEC-332)?</strong> Every tenant has its own audit chain (per AGENTS.md §6 — chained rows with prev_chain). The genesis row is the bootstrap anchor that memory_writer initialises from. Without it, the first audit row for the tenant has no <code>prev_chain</code> value and chain validation fails.</p>\n<p><strong>Why dry-run mode (§1 #26)?</strong> TASK-TEN-101's self-serve signup form needs to validate inputs in real-time (before committing payment). Dry-run lets the form check everything without state mutation — fast feedback, clean separation.</p>\n<p><strong>Why slug immutable post-provisioning (§1 #25)?</strong> Slug is in Postgres schema name + NATS namespace + URL paths + S3 prefixes. Renaming requires coordinated mutation across all systems + downstream consumer awareness. The 1% of cases needing rename are deliberate operator events with ADRs; the API doesn't offer a shortcut.</p>\n<p><strong>Why list + get subcommands (§1 #22, #23)?</strong> Operators need to inspect existing tenants. <code>list</code> for inventory; <code>get</code> for detail. Both are read-only and require <code>root-admin</code> role.</p>\n<p><strong>Why email uniqueness check across all tenants (§1 #24)?</strong> A subject's email is the cross-tenant join key for \"I'm the same human in two tenants\". Allowing the same email in two new tenants creates ambiguity. Slice 1 enforces single-tenant-email; task-AUTH-2xx may add multi-tenant binding later (out of scope).</p>\n<p><strong>Why 30s p95 perf budget (§1 #15)?</strong> Provisioning touches 4 external systems + creates ~10 rows. 30s is generous — typical happy path is 5-10s. The p95 budget catches infrastructure outliers; outliers &gt; 30s = sev-3 alarm for ops investigation.</p>\n<p><strong>Why TenantStatus enum has 5 values (DEC-326)?</strong> Mirrors the conventional SaaS lifecycle: provisioning (mid-flight) → active → suspended (non-payment, etc.) → terminating (90-day offboarding contract) → terminated (irreversible wipe attestation). Slice 1 ships all 5 values; transitions implemented in TASK-TEN-104.</p>\n<p><strong>Why <code>tenant_residency_map</code> separate table (§1 #19)?</strong> Residency may change (rare but possible: tenant graduates from sg-1 to vn-1 for VN expansion). Separate table lets TASK-TEN-103 ship the change-residency workflow without touching the <code>tenants</code> table. The map is the canonical lookup for TASK-DOC-001 + TASK-EMAIL-001 + TASK-AI-016.</p>\n<p><strong>Why <code>provisioned_by_subject_id</code> recorded (§1 #1)?</strong> Accountability — every tenant has a creator operator. PDPL Art. 4 data minimisation is satisfied because the field stores the operator's UUID (already in the AUTH cluster), not their personal data.</p>\n<p><strong>Why no DELETE on tenants (§1 #10)?</strong> Tenants enter <code>terminated</code> state via TASK-TEN-104's 90-day offboarding contract; the row persists with <code>status='terminated'</code> and <code>terminated_at</code> set. Hard delete would lose the forensic record + would orphan FK references (audit rows, billing records).</p>\n<p><strong>Why JSON mode omits password (§1 #16)?</strong> JSON mode is intended for automation (scripts piping output to other systems). Including the password in JSON means it could end up in logs, command history, CI artifacts. Stderr block forces operators to handle it deliberately.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0001 — tenants table</h3>\n<pre><code class=\"language-sql\">-- services/ten/migrations/0001_tenants.sql\n\nBEGIN;\n\nCREATE TYPE tenant_status AS ENUM ('provisioning', 'active', 'suspended', 'terminating', 'terminated');\nCREATE TYPE residency_code AS ENUM ('vn-1', 'sg-1', 'eu-1', 'us-1');\n\nCREATE TABLE tenants (\n    id                          UUID         PRIMARY KEY,\n    slug                        TEXT         NOT NULL UNIQUE\n                                CHECK (slug ~ '^[a-z][a-z0-9-]{2,40}[a-z0-9]$'),\n    display_name                TEXT         NOT NULL CHECK (length(display_name) BETWEEN 1 AND 200),\n    status                      tenant_status NOT NULL DEFAULT 'provisioning',\n    plan_tier                   TEXT         NOT NULL DEFAULT 'starter',\n    residency                   residency_code NOT NULL DEFAULT 'vn-1',\n    created_at                  TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    provisioned_at              TIMESTAMPTZ,\n    terminated_at               TIMESTAMPTZ,\n    provisioned_by_subject_id   UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nCREATE INDEX tenants_status_idx ON tenants (status);\nCREATE INDEX tenants_residency_idx ON tenants (residency);\n\nALTER TABLE tenants ENABLE ROW LEVEL SECURITY;\n\n-- Only superuser / root-admin can see this table; tenant-admins read their own tenant via TASK-TEN-107.\nCREATE POLICY tenants_superuser_only ON tenants\n    USING (current_setting('auth.is_root_admin', true) = 'true')\n    WITH CHECK (current_setting('auth.is_root_admin', true) = 'true');\n\n-- Provisioner role for the CLI; distinct from cyberos_app.\nCREATE ROLE cyberos_provisioner;\nGRANT INSERT ON tenants TO cyberos_provisioner;\nGRANT UPDATE (status, provisioned_at, terminated_at) ON tenants TO cyberos_provisioner;\nGRANT SELECT ON tenants TO cyberos_provisioner;\n\nREVOKE UPDATE, DELETE ON tenants FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0002 — tenant_status_history (append-only)</h3>\n<pre><code class=\"language-sql\">-- services/ten/migrations/0002_tenant_status_history.sql\n\nBEGIN;\n\nCREATE TABLE tenant_status_history (\n    id                     BIGSERIAL    PRIMARY KEY,\n    tenant_id              UUID         NOT NULL REFERENCES tenants(id),\n    from_status            tenant_status,                              -- NULL on initial create\n    to_status              tenant_status NOT NULL,\n    changed_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    changed_by_subject_id  UUID         NOT NULL,\n    reason                 TEXT\n);\n\nCREATE INDEX tenant_status_history_tenant_idx ON tenant_status_history (tenant_id, changed_at DESC);\n\nALTER TABLE tenant_status_history ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_status_history_superuser_only ON tenant_status_history\n    USING (current_setting('auth.is_root_admin', true) = 'true')\n    WITH CHECK (current_setting('auth.is_root_admin', true) = 'true');\n\nGRANT INSERT, SELECT ON tenant_status_history TO cyberos_provisioner;\nREVOKE UPDATE, DELETE ON tenant_status_history FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Migration 0003 — tenant_residency_map</h3>\n<pre><code class=\"language-sql\">-- services/ten/migrations/0003_tenant_residency_map.sql\n\nBEGIN;\n\nCREATE TABLE tenant_residency_map (\n    tenant_id              UUID         PRIMARY KEY REFERENCES tenants(id),\n    residency              residency_code NOT NULL,\n    set_at                 TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    set_by_subject_id      UUID         NOT NULL\n);\n\n-- Allow cyberos_app read access — TASK-DOC-001, TASK-EMAIL-001, TASK-AI-016 all consume.\nGRANT SELECT ON tenant_residency_map TO cyberos_app;\nGRANT INSERT ON tenant_residency_map TO cyberos_provisioner;\nREVOKE UPDATE, DELETE ON tenant_residency_map FROM cyberos_app;\n-- UPDATE is granted to cyberos_provisioner for TASK-TEN-103's residency-change flow.\nGRANT UPDATE (residency, set_at, set_by_subject_id) ON tenant_residency_map TO cyberos_provisioner;\n\nCOMMIT;</code></pre>\n<h3>3.4 — Rust types</h3>\n<pre><code class=\"language-rust\">// services/ten/src/types.rs\nuse chrono::{DateTime, Utc};\nuse serde::{Deserialize, Serialize};\nuse sqlx::{FromRow, Type};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"tenant_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum TenantStatus { Provisioning, Active, Suspended, Terminating, Terminated }\n\nimpl TenantStatus {\n    pub const ALL: &amp;'static [TenantStatus] = &amp;[\n        TenantStatus::Provisioning, TenantStatus::Active, TenantStatus::Suspended,\n        TenantStatus::Terminating, TenantStatus::Terminated,\n    ];\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"residency_code\", rename_all = \"kebab-case\")]\n#[serde(rename_all = \"kebab-case\")]\npub enum ResidencyCode { Vn1, Sg1, Eu1, Us1 }\n\nimpl ResidencyCode {\n    pub const ALL: &amp;'static [ResidencyCode] = &amp;[\n        ResidencyCode::Vn1, ResidencyCode::Sg1, ResidencyCode::Eu1, ResidencyCode::Us1,\n    ];\n\n    pub fn as_str(self) -&gt; &amp;'static str {\n        match self {\n            ResidencyCode::Vn1 =&gt; \"vn-1\",\n            ResidencyCode::Sg1 =&gt; \"sg-1\",\n            ResidencyCode::Eu1 =&gt; \"eu-1\",\n            ResidencyCode::Us1 =&gt; \"us-1\",\n        }\n    }\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct Tenant {\n    pub id: Uuid,\n    pub slug: String,\n    pub display_name: String,\n    pub status: TenantStatus,\n    pub plan_tier: String,\n    pub residency: ResidencyCode,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub provisioned_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    pub terminated_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    pub provisioned_by_subject_id: Uuid,\n}</code></pre>\n<h3>3.5 — Provisioning orchestrator</h3>\n<pre><code class=\"language-rust\">// services/ten/src/provisioning/orchestrator.rs\nuse crate::types::*;\nuse uuid::Uuid;\nuse zeroize::Zeroizing;\n\npub struct ProvisionRequest {\n    pub slug: String,\n    pub display_name: String,\n    pub root_admin_email: String,\n    pub root_admin_display_name: String,\n    pub residency: ResidencyCode,\n    pub plan_tier: String,\n    pub operator_subject_id: Uuid,\n    pub dry_run: bool,\n}\n\npub struct ProvisionResult {\n    pub tenant_id: Uuid,\n    pub slug: String,\n    pub display_name: String,\n    pub residency: ResidencyCode,\n    pub plan_tier: String,\n    pub postgres_schema: String,\n    pub nats_namespace: String,\n    pub s3_prefix: String,\n    pub root_admin_subject_id: Uuid,\n    pub root_admin_email: String,\n    pub root_admin_password: Zeroizing&lt;String&gt;,   // printed once + zeroised\n    pub idempotent_match: bool,\n}\n\npub async fn provision(req: ProvisionRequest, ctx: &amp;Ctx) -&gt; Result&lt;ProvisionResult, ProvisionError&gt; {\n    // Step 1: validate (fail-fast)\n    crate::validation::slug(&amp;req.slug)?;\n    crate::validation::email(&amp;req.root_admin_email)?;\n\n    if req.dry_run {\n        return crate::dry_run::simulate(req, ctx).await;\n    }\n\n    // Step 2: idempotency check\n    if let Some(existing) = ctx.repo.find_by_slug(&amp;req.slug).await? {\n        if existing.residency != req.residency || existing.plan_tier != req.plan_tier {\n            return Err(ProvisionError::SlugCollisionDifferentAttrs);\n        }\n        return Ok(ProvisionResult::from_existing(existing));\n    }\n\n    let mut tx = ctx.db.begin().await?;\n    let tenant_id = Uuid::new_v4();\n\n    // Step 3: INSERT tenant (status='provisioning')\n    sqlx::query(\"INSERT INTO tenants (id, slug, display_name, status, plan_tier, residency, provisioned_by_subject_id) VALUES ($1, $2, $3, 'provisioning'::tenant_status, $4, $5::residency_code, $6)\")\n        .bind(tenant_id).bind(&amp;req.slug).bind(&amp;req.display_name)\n        .bind(&amp;req.plan_tier).bind(req.residency.as_str()).bind(req.operator_subject_id)\n        .execute(&amp;mut *tx).await?;\n    sqlx::query(\"INSERT INTO tenant_status_history (tenant_id, from_status, to_status, changed_by_subject_id, reason) VALUES ($1, NULL, 'provisioning'::tenant_status, $2, 'initial provisioning')\")\n        .bind(tenant_id).bind(req.operator_subject_id).execute(&amp;mut *tx).await?;\n    sqlx::query(\"INSERT INTO tenant_residency_map (tenant_id, residency, set_by_subject_id) VALUES ($1, $2::residency_code, $3)\")\n        .bind(tenant_id).bind(req.residency.as_str()).bind(req.operator_subject_id)\n        .execute(&amp;mut *tx).await?;\n\n    // Step 4: create Postgres schema namespace\n    crate::provisioning::schema_namespace::create(&amp;req.slug, &amp;mut tx).await?;\n\n    tx.commit().await?;\n\n    // Step 5: create NATS namespace (compensating action on failure)\n    let nats_namespace = format!(\"tenant.{}.&gt;\", req.slug);\n    crate::provisioning::nats_namespace::create(&amp;req.slug, &amp;ctx.nats).await\n        .map_err(|e| {\n            // Compensate: delete Postgres schema + tenant rows.\n            tokio::spawn(crate::provisioning::compensate::rollback(tenant_id, req.slug.clone(), ctx.clone()));\n            e\n        })?;\n\n    // Step 6: create S3 prefix markers (compensating action on failure)\n    let s3_prefix = format!(\"{tenant_id}/\");\n    crate::provisioning::s3_prefix::initialise(&amp;tenant_id, req.residency, &amp;ctx.s3).await\n        .map_err(|e| {\n            tokio::spawn(crate::provisioning::compensate::rollback_with_nats(tenant_id, req.slug.clone(), ctx.clone()));\n            e\n        })?;\n\n    // Step 7: AUTH bootstrap — create AUTH tenant + initial root-admin subject\n    let password = generate_password();\n    let bootstrap = crate::provisioning::auth_bootstrap::call(\n        tenant_id, &amp;req.slug, &amp;req.display_name,\n        &amp;req.root_admin_email, &amp;req.root_admin_display_name,\n        &amp;password, req.operator_subject_id, &amp;ctx.auth_client,\n    ).await.map_err(|e| {\n        tokio::spawn(crate::provisioning::compensate::rollback_with_nats_and_s3(tenant_id, req.slug.clone(), ctx.clone()));\n        e\n    })?;\n\n    // Step 8: UPDATE tenant status to 'active'\n    let mut tx2 = ctx.db.begin().await?;\n    sqlx::query(\"UPDATE tenants SET status = 'active'::tenant_status, provisioned_at = now() WHERE id = $1\")\n        .bind(tenant_id).execute(&amp;mut *tx2).await?;\n    sqlx::query(\"INSERT INTO tenant_status_history (tenant_id, from_status, to_status, changed_by_subject_id, reason) VALUES ($1, 'provisioning'::tenant_status, 'active'::tenant_status, $2, 'provisioning complete')\")\n        .bind(tenant_id).bind(req.operator_subject_id).execute(&amp;mut *tx2).await?;\n\n    // Step 9: emit memory audit row\n    crate::audit::tenant_events::emit_tenant_provisioned(\n        &amp;mut tx2, tenant_id, &amp;req.slug, &amp;req.display_name,\n        req.plan_tier.clone(), req.residency, req.operator_subject_id, bootstrap.root_admin_subject_id,\n    ).await?;\n\n    tx2.commit().await?;\n\n    Ok(ProvisionResult {\n        tenant_id,\n        slug: req.slug,\n        display_name: req.display_name,\n        residency: req.residency,\n        plan_tier: req.plan_tier,\n        postgres_schema: format!(\"tenant_{}\", &amp;req.slug),\n        nats_namespace,\n        s3_prefix,\n        root_admin_subject_id: bootstrap.root_admin_subject_id,\n        root_admin_email: req.root_admin_email,\n        root_admin_password: password,\n        idempotent_match: false,\n    })\n}\n\nfn generate_password() -&gt; Zeroizing&lt;String&gt; {\n    use rand::distributions::{Alphanumeric, DistString};\n    Zeroizing::new(Alphanumeric.sample_string(&amp;mut rand::thread_rng(), 32))\n}</code></pre>\n<h3>3.6 — CLI command</h3>\n<pre><code class=\"language-rust\">// services/ten/src/cli/provision.rs\nuse clap::Parser;\nuse cyberos_cli_exit::ExitCode;\nuse crate::provisioning::orchestrator::{provision, ProvisionRequest, ProvisionError};\nuse crate::types::ResidencyCode;\nuse std::str::FromStr;\n\n#[derive(Parser)]\npub struct ProvisionCmd {\n    #[arg(long)] pub slug: String,\n    #[arg(long)] pub display_name: String,\n    #[arg(long)] pub root_admin_email: String,\n    #[arg(long)] pub root_admin_display_name: String,\n    #[arg(long, default_value = \"vn-1\")] pub residency: String,\n    #[arg(long, default_value = \"starter\")] pub plan_tier: String,\n    #[arg(long, default_value_t = false)] pub json: bool,\n    #[arg(long, default_value_t = false)] pub dry_run: bool,\n}\n\npub async fn run(cmd: ProvisionCmd, ctx: AppCtx) -&gt; ExitCode {\n    // Operator role check\n    let operator = match ctx.current_operator().await {\n        Ok(op) if op.has_role(Role::RootAdmin) =&gt; op,\n        Ok(_) =&gt; { eprintln!(\"ERROR: caller must be root-admin\"); return ExitCode::PermissionDenied; }\n        Err(_) =&gt; return ExitCode::PermissionDenied,\n    };\n\n    let residency = match ResidencyCode::from_str(&amp;cmd.residency) {\n        Ok(r) =&gt; r,\n        Err(_) =&gt; { eprintln!(\"ERROR: unknown residency: {}\", cmd.residency); return ExitCode::InvalidData; }\n    };\n\n    let req = ProvisionRequest {\n        slug: cmd.slug,\n        display_name: cmd.display_name,\n        root_admin_email: cmd.root_admin_email,\n        root_admin_display_name: cmd.root_admin_display_name,\n        residency,\n        plan_tier: cmd.plan_tier,\n        operator_subject_id: operator.subject_id,\n        dry_run: cmd.dry_run,\n    };\n\n    match provision(req, &amp;ctx).await {\n        Ok(result) =&gt; {\n            if cmd.json {\n                let json = serde_json::to_string_pretty(&amp;result.public_view()).unwrap();\n                println!(\"{json}\");\n                eprintln!(\"\\n=== ROOT ADMIN PASSWORD (RECORD IMMEDIATELY) ===\");\n                eprintln!(\"{}\", *result.root_admin_password);\n                eprintln!(\"=== END ===\");\n            } else {\n                println!(\"✓ Tenant provisioned\");\n                println!(\"  tenant_id:        {}\", result.tenant_id);\n                println!(\"  slug:             {}\", result.slug);\n                println!(\"  display_name:     {}\", result.display_name);\n                println!(\"  residency:        {}\", result.residency.as_str());\n                println!(\"  plan_tier:        {}\", result.plan_tier);\n                println!(\"  postgres_schema:  {}\", result.postgres_schema);\n                println!(\"  nats_namespace:   {}\", result.nats_namespace);\n                println!(\"  s3_prefix:        {}\", result.s3_prefix);\n                println!(\"  root_admin_email: {}\", result.root_admin_email);\n                println!(\"  === ROOT ADMIN PASSWORD (RECORD IMMEDIATELY) ===\");\n                println!(\"  {}\", *result.root_admin_password);\n                println!(\"  === END ===\");\n            }\n            if result.idempotent_match { ExitCode::IdempotentMatch } else { ExitCode::Success }\n        }\n        Err(ProvisionError::SlugCollisionDifferentAttrs) =&gt; {\n            eprintln!(\"ERROR: slug exists with different residency or plan_tier\");\n            ExitCode::InvalidData\n        }\n        Err(ProvisionError::InvalidSlug) =&gt; { eprintln!(\"ERROR: invalid slug (must match ^[a-z][a-z0-9-]{{2,40}}[a-z0-9]$)\"); ExitCode::InvalidData }\n        Err(ProvisionError::InvalidEmail) =&gt; { eprintln!(\"ERROR: invalid email format\"); ExitCode::InvalidData }\n        Err(ProvisionError::StepFailed(step)) =&gt; { eprintln!(\"ERROR: step {step} failed; rollback in progress\"); ExitCode::CantCreate }\n        Err(ProvisionError::Transient) =&gt; { eprintln!(\"ERROR: transient infrastructure failure; retry advised\"); ExitCode::TempFail }\n        Err(e) =&gt; { eprintln!(\"ERROR: {e:?}\"); ExitCode::CantCreate }\n    }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Tenant status enum closed at 5</strong> — <code>TenantStatus::ALL.len() == 5</code>; Postgres enum has exactly 5 labels.</li><li><strong>Residency code enum closed at 4</strong> — same shape.</li><li><strong>POST provision happy path</strong> — valid input → exit 0; tenant row created with <code>status=active</code>; <code>tenant_&lt;slug&gt;</code> Postgres schema exists; NATS namespace registered; S3 marker objects written; root-admin subject created; <code>ten.tenant_provisioned</code> memory row emitted; password printed once.</li><li><strong>Idempotent on slug</strong> — re-run with same slug + same residency + same plan_tier → exit 1; same tenant row returned; NO duplicate memory row.</li><li><strong>Slug collision different residency</strong> → exit 65 with <code>slug_collision_different_attrs</code>.</li><li><strong>Invalid slug regex</strong> — slug with uppercase or trailing hyphen → exit 65.</li><li><strong>Invalid email format</strong> → exit 65.</li><li><strong>Unknown residency</strong> → exit 65.</li><li><strong>Operator without root-admin role</strong> → exit 77 immediately (no state mutation).</li><li><strong>Missing required flag</strong> → exit 64.</li><li><strong>UPDATE on tenants blocked from cyberos_app</strong> — <code>UPDATE tenants SET status='terminated'</code> as cyberos_app → permission denied.</li><li><strong>DELETE on tenants blocked from cyberos_app</strong> — same.</li><li><strong>tenant_status_history append-only</strong> — UPDATE/DELETE blocked.</li><li><strong><code>cyberos-ten get</code> returns tenant detail</strong> — exit 0; root_admin password NOT in output.</li><li><strong><code>cyberos-ten list</code> returns all tenants</strong> — root-admin only.</li><li><strong>Root admin password printed once + zeroised</strong> — memory inspection after CLI exit (test harness) shows the password bytes overwritten.</li><li><strong>Root admin password NOT in memory row</strong> — <code>ten.tenant_provisioned</code> row JSON contains no password-shaped field.</li><li><strong>Default residency vn-1</strong> — <code>--residency</code> omitted → tenant created with residency='vn-1'.</li><li><strong>--residency flag overrides</strong> — <code>--residency eu-1</code> → tenant created with residency='eu-1'.</li><li><strong>--dry-run validates without writing</strong> — exit 0; no tenant row created; no schema/NATS/S3 mutation.</li><li><strong>--dry-run reports validation failure</strong> — bad slug → exit 65; no state.</li><li><strong>tenant_residency_map row written</strong> — readable by cyberos_app.</li><li><strong>NATS namespace ACL applied</strong> — subscriber on <code>tenant.&lt;other-slug&gt;.&gt;</code> cannot receive messages on <code>tenant.&lt;slug&gt;.&gt;</code>.</li><li><strong>S3 marker object exists</strong> — <code>s3://cyberos-doc-vn-1-generic/&lt;tenant_id&gt;/.cyberos-tenant-marker</code> returns 200 on HEAD.</li><li><strong>AUTH side created</strong> — POST /v1/admin/tenants on AUTH returns same tenant_id + root_admin_subject_id; root-admin subject has <code>tenant-admin</code> role per TASK-AUTH-101.</li><li><strong>Provision rolls back on AUTH failure</strong> — mock AUTH to return 500 → tenant row + NATS + S3 marker cleaned up via compensating actions; exit 73.</li><li><strong>OTel span <code>ten.provision</code> emitted</strong> — <code>outcome=success</code>.</li><li><strong>Counter <code>ten_provision_total{outcome=success, residency=vn-1}</code> increments</strong> — per provisioning.</li><li><strong>Perf budget &lt; 30s p95</strong> — <code>provision_perf_test</code> 50 iterations.</li><li><strong><code>--json</code> mode omits password from stdout</strong> — password in stderr block only.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/ten/tests/provision_happy_test.rs\n#[tokio::test]\nasync fn happy_path_creates_everything() {\n    let ctx = TestStack::up().await;\n    let result = ctx.run_cli(&amp;[\n        \"provision\",\n        \"--slug\", \"acme-corp\",\n        \"--display-name\", \"ACME Corporation\",\n        \"--root-admin-email\", \"root@acme.example\",\n        \"--root-admin-display-name\", \"ACME Root Admin\",\n        \"--residency\", \"vn-1\",\n    ]).await;\n    assert_eq!(result.exit_code, 0);\n\n    // Check tenant row\n    let tenant = ctx.db.fetch_tenant_by_slug(\"acme-corp\").await.unwrap();\n    assert_eq!(tenant.status, TenantStatus::Active);\n    assert!(tenant.provisioned_at.is_some());\n\n    // Check Postgres schema\n    let schemas = ctx.db.list_schemas().await;\n    assert!(schemas.contains(&amp;\"tenant_acme-corp\".to_string()));\n\n    // Check NATS namespace\n    let nats_accounts = ctx.nats.list_accounts().await;\n    assert!(nats_accounts.iter().any(|a| a.subject_namespace == \"tenant.acme-corp.&gt;\"));\n\n    // Check S3 markers\n    let marker = ctx.s3.head_object(\"cyberos-doc-vn-1-generic\", &amp;format!(\"{}/.cyberos-tenant-marker\", tenant.id)).await;\n    assert!(marker.is_ok());\n\n    // Check AUTH-side root admin\n    let auth_subject = ctx.auth.get_subject_by_email(\"root@acme.example\").await.unwrap();\n    assert!(auth_subject.roles.contains(&amp;\"tenant-admin\".to_string()));\n\n    // Check memory audit row\n    let rows = ctx.memory_audit_rows(\"ten.tenant_provisioned\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"slug\"], \"acme-corp\");\n}\n\n#[tokio::test]\nasync fn idempotent_match_returns_existing() {\n    let ctx = TestStack::up().await;\n    let _first = ctx.run_cli(&amp;[\"provision\", \"--slug\", \"acme\", \"--display-name\", \"ACME\", \"--root-admin-email\", \"r@acme\", \"--root-admin-display-name\", \"R\", \"--residency\", \"vn-1\"]).await;\n    let second = ctx.run_cli(&amp;[\"provision\", \"--slug\", \"acme\", \"--display-name\", \"ACME\", \"--root-admin-email\", \"r@acme\", \"--root-admin-display-name\", \"R\", \"--residency\", \"vn-1\"]).await;\n    assert_eq!(second.exit_code, 1);\n    let rows = ctx.memory_audit_rows(\"ten.tenant_provisioned\").await;\n    assert_eq!(rows.len(), 1, \"no duplicate memory row on idempotent match\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ten/tests/provision_root_admin_test.rs\n#[tokio::test]\nasync fn password_printed_once_and_zeroised() {\n    let ctx = TestStack::up().await;\n    let result = ctx.run_cli_capture(&amp;[\"provision\", \"--slug\", \"acme\", /* ... */]).await;\n    assert_eq!(result.exit_code, 0);\n\n    // Password is in stdout (with marker block)\n    assert!(result.stdout.contains(\"=== ROOT ADMIN PASSWORD\"));\n    let password_line = result.stdout.lines().find(|l| l.trim().len() == 32 &amp;&amp; l.trim().chars().all(|c| c.is_alphanumeric())).unwrap();\n    let password = password_line.trim().to_string();\n\n    // Password is NOT in memory row\n    let rows = ctx.memory_audit_rows(\"ten.tenant_provisioned\").await;\n    let row_json = serde_json::to_string(&amp;rows[0]).unwrap();\n    assert!(!row_json.contains(&amp;password), \"password leaked into memory row\");\n\n    // Password is NOT in log files\n    let logs = ctx.read_log_files().await;\n    assert!(!logs.contains(&amp;password), \"password leaked into logs\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ten/tests/append_only_test.rs\n#[sqlx::test]\nasync fn tenants_update_blocked_from_app(pool: sqlx::PgPool) {\n    set_role_app(&amp;pool).await;\n    let id = seed_tenant_as_provisioner(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE tenants SET status = 'terminated'::tenant_status WHERE id = $1\")\n        .bind(id).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"permission denied\"));\n}\n\n#[sqlx::test]\nasync fn tenants_update_allowed_from_provisioner(pool: sqlx::PgPool) {\n    set_role_provisioner(&amp;pool).await;\n    let id = seed_tenant_as_provisioner(&amp;pool).await;\n    sqlx::query(\"UPDATE tenants SET status = 'terminated'::tenant_status, terminated_at = now() WHERE id = $1\")\n        .bind(id).execute(&amp;pool).await.unwrap();\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ten/tests/provision_namespace_isolation_test.rs\n#[tokio::test]\nasync fn two_tenants_get_distinct_namespaces() {\n    let ctx = TestStack::up().await;\n    let a = ctx.run_cli(&amp;[\"provision\", \"--slug\", \"acme\", /* ... */]).await;\n    let b = ctx.run_cli(&amp;[\"provision\", \"--slug\", \"biko\", /* ... */]).await;\n    assert_eq!(a.exit_code, 0);\n    assert_eq!(b.exit_code, 0);\n\n    let schemas = ctx.db.list_schemas().await;\n    assert!(schemas.contains(&amp;\"tenant_acme\".to_string()));\n    assert!(schemas.contains(&amp;\"tenant_biko\".to_string()));\n\n    let nats = ctx.nats.list_accounts().await;\n    assert!(nats.iter().any(|a| a.subject_namespace == \"tenant.acme.&gt;\"));\n    assert!(nats.iter().any(|a| a.subject_namespace == \"tenant.biko.&gt;\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; compensating-action functions follow the standard rollback pattern: delete the Postgres rows + NATS account + S3 marker if a later step fails.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-AUTH-001</strong> — tenant + subject create (this task delegates via internal helper).</li></ul>\n<p><strong>Downstream (2 placeholders):</strong></p>\n<ul><li><strong>TASK-TEN-002</strong> — 3 plan tiers (consumes <code>plan_tier</code> column).</li><li><strong>TASK-TEN-104</strong> — 90-day offboarding contract (uses the closed TenantStatus enum).</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AUTH-002</strong> — subject create (consumed via the AUTH bootstrap helper for root-admin creation).</li><li><strong>TASK-AUTH-101</strong> — RBAC catalogue (root-admin role + tenant-admin role assignment).</li><li><strong>TASK-AI-016</strong> — residency policy (consumes <code>tenant_residency_map</code> table).</li><li><strong>TASK-DOC-001</strong> — document repository (consumes <code>tenant_residency_map</code> for body bucket lookup).</li><li><strong>TASK-EMAIL-001</strong> — Stalwart mail server (consumes <code>tenant_residency_map</code> for body bucket lookup).</li><li><strong>TASK-AI-003</strong> — memory audit bridge (receives <code>ten.tenant_provisioned</code> row + chain anchor).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — <code>cyberos-ten provision</code> happy invocation</h3>\n<pre><code class=\"language-bash\">$ cyberos-ten provision \\\n    --slug acme-corp \\\n    --display-name \"ACME Corporation\" \\\n    --root-admin-email root@acme.example \\\n    --root-admin-display-name \"ACME Root Admin\" \\\n    --residency vn-1\n✓ Tenant provisioned\n  tenant_id:        01HG7V8B0K8M4Z8Z8M8M8M8M8M\n  slug:             acme-corp\n  display_name:     ACME Corporation\n  residency:        vn-1\n  plan_tier:        starter\n  postgres_schema:  tenant_acme-corp\n  nats_namespace:   tenant.acme-corp.&gt;\n  s3_prefix:        01HG7V8B0K8M4Z8Z8M8M8M8M8M/\n  root_admin_email: root@acme.example\n  === ROOT ADMIN PASSWORD (RECORD IMMEDIATELY) ===\n  Kj7Lp9MzQrTyVwBxCdEfGhJ3Kl5Nm6Op\n  === END ===</code></pre>\n<h3>8.2 — ten.tenant_provisioned memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.tenant_provisioned\",\n  \"tenant_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"slug\": \"acme-corp\",\n  \"display_name\": \"ACME Corporation\",\n  \"status\": \"active\",\n  \"plan_tier\": \"starter\",\n  \"residency\": \"vn-1\",\n  \"provisioned_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"root_admin_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.3 — JSON-mode output</h3>\n<pre><code class=\"language-bash\">$ cyberos-ten provision --slug acme-corp --display-name \"ACME\" --root-admin-email r@a --root-admin-display-name R --residency vn-1 --json\n{\n  \"tenant_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"slug\": \"acme-corp\",\n  \"display_name\": \"ACME\",\n  \"residency\": \"vn-1\",\n  \"plan_tier\": \"starter\",\n  \"postgres_schema\": \"tenant_acme-corp\",\n  \"nats_namespace\": \"tenant.acme-corp.&gt;\",\n  \"s3_prefix\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M/\",\n  \"root_admin_email\": \"r@a\",\n  \"root_admin_subject_id\": \"9b1deb4d-...\"\n}\n=== ROOT ADMIN PASSWORD (RECORD IMMEDIATELY) ===\nKj7Lp9MzQrTyVwBxCdEfGhJ3Kl5Nm6Op\n=== END ===</code></pre>\n<h3>8.4 — Idempotent-match exit</h3>\n<pre><code class=\"language-bash\">$ cyberos-ten provision --slug acme-corp --display-name \"ACME\" --root-admin-email r@a --root-admin-display-name R --residency vn-1\n✓ Tenant already exists (idempotent match)\n  tenant_id:        01HG7V8B0K8M4Z8Z8M8M8M8M8M\n  slug:             acme-corp\n$ echo $?\n1</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Plan tier closed enum</strong> — TASK-TEN-002 ships full 3-tier validation.</li><li><strong>Self-serve signup form</strong> — TASK-TEN-101 (P3).</li><li><strong>Per-tenant residency change workflow</strong> — TASK-TEN-103.</li><li><strong>90-day offboarding</strong> — TASK-TEN-104.</li><li><strong>Plan downgrade with quota violation</strong> — task-TEN-2xx.</li><li><strong>Branding config</strong> — task-TEN-2xx.</li><li><strong>Slug rename</strong> — out of scope; manual SQL + ADR.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Slug regex fail</td><td>DB CHECK + handler</td><td>exit 65 invalid_data</td><td>Use valid slug</td></tr><tr><td>Slug collision same attrs</td><td>UNIQUE + handler</td><td>exit 1 idempotent_match</td><td>None — designed</td></tr><tr><td>Slug collision different attrs</td><td>UNIQUE + handler diff check</td><td>exit 65 collision_different_attrs</td><td>Use different slug or align attrs</td></tr><tr><td>Invalid email</td><td>handler</td><td>exit 65</td><td>Use valid email</td></tr><tr><td>Unknown residency</td><td>enum parse</td><td>exit 65</td><td>Use vn-1 / sg-1 / eu-1 / us-1</td></tr><tr><td>Operator not root-admin</td><td>JWT role check</td><td>exit 77</td><td>Re-auth with root-admin role</td></tr><tr><td>Missing required flag</td><td>clap</td><td>exit 64</td><td>Provide flag</td></tr><tr><td>Postgres schema create fails (permission)</td><td>step 3 error</td><td>exit 73 + compensating rollback</td><td>Check cyberos_provisioner role perms</td></tr><tr><td>NATS account create fails</td><td>step 4 error</td><td>exit 73 + rollback Postgres</td><td>Check NATS API health</td></tr><tr><td>S3 marker write fails</td><td>step 5 error</td><td>exit 73 + rollback Postgres + NATS</td><td>Check S3 perms + KMS</td></tr><tr><td>AUTH bootstrap fails</td><td>step 6 error</td><td>exit 73 + full rollback</td><td>Investigate AUTH service health</td></tr><tr><td>Status transition <code>provisioning → active</code> fails</td><td>step 7 tx</td><td>exit 73; tenant left in <code>provisioning</code> state</td><td>Operator runs cleanup script</td></tr><tr><td>memory audit emit fails</td><td>step 8 tx rollback</td><td>exit 73; full rollback</td><td>memory_writer diagnosis</td></tr><tr><td>Password generation fails</td><td>step 7 internal</td><td>exit 75 transient</td><td>Retry</td></tr><tr><td>RNG entropy starvation</td><td>step 7</td><td>exit 75</td><td>Restart process</td></tr><tr><td>Operator deletes the printed password before saving</td><td>None — operator responsibility</td><td>Tenant unusable; force password-reset via AUTH</td><td>task-AUTH-2xx password reset flow</td></tr><tr><td>Postgres connection pool exhausted</td><td>sqlx error</td><td>exit 75</td><td>Wait + retry</td></tr><tr><td>Cross-tenant slug-derived schema collision (e.g. SQL injection in slug)</td><td>regex check + parameterised query</td><td>None</td><td>Regex prevents</td></tr><tr><td><code>cyberos_provisioner</code> role not granted to CLI's connection</td><td>startup check fails</td><td>Service refuses to start</td><td>Grant role</td></tr><tr><td><code>tenant_residency_map</code> INSERT fails</td><td>step 3 tx rollback</td><td>exit 73</td><td>Investigate cluster health</td></tr><tr><td>Idempotent re-run with different display_name</td><td>OK (idempotency only checks slug+residency+plan_tier)</td><td>exit 1; display_name from existing row</td><td>Use <code>cyberos-ten update display-name</code> (task-TEN-2xx)</td></tr><tr><td>NATS compensating action fails</td><td>logged; sev-1 alarm</td><td>Orphan NATS account</td><td>Operator manual cleanup</td></tr><tr><td>S3 compensating action fails</td><td>logged; sev-1 alarm</td><td>Orphan marker object (low cost)</td><td>S3 lifecycle cleanup</td></tr><tr><td>AUTH compensating action fails</td><td>logged; sev-1 alarm</td><td>Orphan AUTH tenant + subject</td><td>Operator manual cleanup via AUTH CLI</td></tr><tr><td>Dry-run side-effect leak</td><td>test asserts</td><td>CI fails</td><td>Fix orchestrator branching</td></tr><tr><td>OTel span attribute missing</td><td>otel_attrs_test</td><td>CI fails</td><td>Fix span builder</td></tr><tr><td>password length not 32 chars</td><td>unit test</td><td>CI fails</td><td>Fix RNG</td></tr><tr><td>password contains spaces or non-alphanumerics</td><td>unit test</td><td>CI fails</td><td>Fix RNG generator</td></tr><tr><td>concurrent provision same slug</td><td>first wins; second hits UNIQUE</td><td>exit 1 (if attrs match) or 65</td><td>Designed</td></tr><tr><td>schema_namespace creator forgets to use IF NOT EXISTS</td><td>rerun fails</td><td>CI test catches</td><td>Fix</td></tr><tr><td>AUTH internal helper signature drift</td><td>type error at compile</td><td>Build fails</td><td>Coordinate TASK-AUTH-001 + TASK-TEN-001 changes</td></tr><tr><td>Stale <code>tenant_residency_map</code> row after rollback</td><td>compensating action covers</td><td>Designed</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Ops-driven at slice 1</strong> — self-serve signup (TASK-TEN-101) lands at P3. CLI covers our P2 vertical-pack volume.</li><li><strong>Per-tenant schema namespace + NATS namespace + S3 prefix</strong> combine for defense-in-depth isolation; RLS alone is insufficient.</li><li><strong><code>cyberos_provisioner</code> SQL role distinct from <code>cyberos_app</code></strong> — privileged operations route through provisioner; app code can't touch tenants table.</li><li><strong>Password printed once + zeroise via <code>zeroize</code> crate</strong> — ops must save the password manually; subsequent retrieval requires password reset.</li><li><strong>Compensating actions on each step's failure</strong> — Postgres uses transactions; NATS + S3 + AUTH have their own commit points + manual rollback.</li><li><strong>Exit code 1 for idempotent match</strong> — distinguishes from exit 0 (did the work) for scripting; matches <code>git pull</code> idiom.</li><li><strong>Slug regex</strong> — kebab-case, 4-42 chars, no trailing hyphen; safe for Postgres schema name + NATS subject + S3 prefix + URL.</li><li><strong><code>auth.is_root_admin</code> GUC</strong> — set by the AUTH JWT middleware when operator's JWT carries the root-admin role; RLS policy on <code>tenants</code> consults this.</li><li><strong><code>tenant_residency_map</code> is the lookup contract</strong> — TASK-DOC-001 + TASK-EMAIL-001 + TASK-AI-016 all read from this table. Centralising prevents drift.</li><li><strong>memory chain anchor at provisioning</strong> — first audit row's <code>prev_chain</code> is the genesis hash; memory_writer initialises chain head from this.</li><li><strong><code>--dry-run</code> mode</strong> — validates everything, mutates nothing. Powers TASK-TEN-101's signup-form preflight.</li><li><strong>Slug immutable post-provisioning</strong> — schema name + NATS subject + S3 prefix all derive from slug; renaming requires coordinated multi-system mutation.</li><li><strong><code>provisioned_by_subject_id</code> is the operator's UUID</strong> — accountability without storing operator PII (PDPL Art. 4 satisfied).</li><li><strong><code>tenant_status_history</code> append-only</strong> — full lifecycle trace for forensics + compliance reviews.</li><li><strong>TenantStatus 5 values</strong> — ships all at slice 1; transitions to suspended/terminating/terminated land in TASK-TEN-104.</li><li><strong><code>cyberos-ten get</code> excludes password</strong> — there is no path to retrieve the original password; password reset is via AUTH (task-AUTH-2xx).</li><li><strong><code>--json</code> mode separates password to stderr</strong> — automation-friendly stdout JSON + force-attention stderr password block.</li><li><strong>AUTH internal helper exposed only for TASK-TEN-001</strong> — not a public REST endpoint; operator-privilege only.</li><li><strong><code>tenant.slug.&gt;</code> NATS subject pattern</strong> — Multi-level wildcard captures all events for the tenant; ACLs limit pub/sub to this namespace.</li><li><strong>S3 marker object pattern</strong> — <code>&lt;tenant_id&gt;/.cyberos-tenant-marker</code> is a small ~100-byte JSON; existence is the prefix-creation evidence.</li><li><strong>Operator JWT verification</strong> — slice 1 reads <code>auth.is_root_admin</code> GUC; slice 2 (TASK-TEN-101) adds full OAuth flow.</li><li><strong>30s p95 budget</strong> — generous; happy path is 5-10s.</li><li><strong>Closed enum cardinality tests</strong> at CI prevent drift between Rust + SQL enum sizes.</li><li><strong><code>cyberos-ten list</code> + <code>get</code></strong> — read-only inventory; useful for operators auditing the fleet.</li></ul>\n<hr>\n<p><em>End of TASK-TEN-001.</em></p>";
