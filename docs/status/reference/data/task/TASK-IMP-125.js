window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-125"]="<h1>TASK-IMP-125: <code>mechanical</code> means deterministic, not <code>docs-tools/</code></h1>\n<h2>Summary</h2>\n<p>TASK-IMP-115 gave every <code>ship-tasks</code> skill_chain step a <code>judgment: high | medium | mechanical</code> advisory field. Its own spec defines <code>mechanical</code> two incompatible ways: §1.2 keys it on BEHAVIOUR (\"a deterministic helper … no model judgment in the result\"), while AC 2 and the Success Metric key it on a PATH (\"a docs-tools helper\", \"precisely\"). The two readings diverge on exactly two steps — 28 (<code>awh-gate</code>) and 29 (<code>caf-gate</code>) — which are deterministic but whose executors live in <code>tools/awh/</code> and <code>scripts/caf_gate.sh</code>, not <code>docs-tools/</code>. This task picks the behaviour reading, makes §1.2, AC 2, and the Success Metric agree on it, re-tags steps 28-29 <code>mechanical</code>, and generalises the suite arm so a mechanical executor may live anywhere on disk.</p>\n<h2>Problem</h2>\n<p>The field's stated purpose is to tell a host which steps a model is NOT deciding, so the host can decline to spend judgment-grade reasoning on them (IMP-115 §1.2 \"no model judgment in the result\"; ship-tasks §11e:437 \"no model is deciding anything\"). That is a fact about behaviour — is a model in the loop? — not about a directory.</p>\n<p>But IMP-115 shipped the narrow reading. Steps 28 and 29 both carry <code>judgment: medium</code> (ship-tasks.md:65-66), and §11e:465-467 records why: both gates are deterministic, \"but their executors (<code>tools/awh</code>, <code>scripts/caf_gate.sh</code>) are not docs-tools helpers, and TASK-IMP-115's AC 2 scopes <code>mechanical</code> to docs-tools backing. They read <code>medium</code>: under-informative rather than wrong.\" The implementer violated no clause — §1.2 is a one-way definition (it says what <code>mechanical</code> IS, not what it is NOT), so labelling a deterministic-executor step <code>medium</code> breaks nothing, and the AC-2 test (<code>test_mechanical_steps_are_helper_backed</code>) only checks that mechanical steps ARE docs-tools-backed, never the reverse.</p>\n<p>The cost is precise and small: a host reading the advisory field is told to spend judgment-grade reasoning on two gates that provably need none. Both are deterministic — caf-gate's SKILL.md says \"The deterministic floor:\" (SKILL.md:66) and awh scores on subprocess exit codes (<code>runner.py:250,254</code>, <code>_run(task.cmd)</code> / <code>_run(task.check) == 0</code>, no model call). A definition that mislabels its own two most mechanical steps is a definition not yet doing its job.</p>\n<p>Note on provenance: §11e:466 claims \"caf-gate's skill says 'no LLM'\". It does not — <code>grep -c -i llm modules/skill/caf-gate/SKILL.md</code> returns 0. The \"no LLM\" phrase is in the workflow's own step-29 description (ship-tasks.md:66), and the SKILL.md says \"deterministic\" instead. The behaviour is real; the attribution is mislocated. This correction cites the true sources and does not repeat the mislocated one.</p>\n<h2>Proposed Solution</h2>\n<p>Decide the definition on behaviour and make the three IMP-115 sentences agree:</p>\n<ul><li><strong>§1.2 stays behaviour-keyed</strong> and is sharpened to say \"wherever its executor lives\".</li><li><strong>AC 2 is rewritten</strong> from \"delegates to a docs-tools helper\" to \"delegates to a deterministic executor named in the payload for that skill\".</li><li><strong>The Success Metric is rewritten</strong> the same way, and its \"precisely\" is reconciled to the behaviour predicate (mechanical ⟺ the step's result is produced by a deterministic executor with no model deciding).</li><li><strong>Steps 28 and 29 become <code>judgment: mechanical</code></strong>, each naming its executor (<code>tools/awh</code> for awh-gate, <code>scripts/caf_gate.sh</code> for caf-gate) in §11e's table, which grows two rows; the \"known rough edge\" paragraph (§11e:465-467) is dropped because it no longer describes a rough edge.</li><li><strong>The AC-2 suite arm generalises</strong>: the executor may live outside <code>docs-tools/</code>, while the three lie-vectors the original arm closed (skill in the table, executor exists on disk, executor anchored in the skill's own prose) are all kept.</li></ul>\n<p>Is \"which directory the executor lives in\" load-bearing? No — it is an accident of where things got put, and the payload itself proves it. <code>build.sh:137-141</code> vendors a deterministic executor from OUTSIDE <code>docs-tools/</code> (<code>scripts/caf_gate.sh</code> + <code>tools/caf</code> -&gt; <code>cuo/gates/caf/</code>), and <code>dist/cyberos/cuo/gates/caf/caf_gate.sh</code> exists on disk. So <code>docs-tools/</code> is not the only deterministic executor family the payload ships, and build.sh gives it no guarantee the other paths lack — it <code>cp</code>s docs-tools files exactly as it <code>cp</code>s caf. A definition keyed on the directory would call caf non-mechanical purely for its address while the payload ships it as a first-class deterministic gate: the definition rotting on contact with the payload it describes. Keying on behaviour cannot rot when a file moves.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Keep the narrow (docs-tools/) reading and just make §1.2 match it.</strong> Rejected: it would formally bless mislabelling the two most mechanical steps in the chain, and it ties <code>mechanical</code> to a filesystem address that the payload's own vendor step (caf, from <code>scripts/</code>) already contradicts. The field would be wrong the first time an executor moved.</li><li><strong>Widen the \"helper family\" to an explicit allow-list of three directories (<code>docs-tools/</code>, <code>tools/awh/</code>, <code>scripts/</code>).</strong> Rejected: an allow-list of paths is the same path-keyed rule with more entries — it rots identically on the next move and needs editing every time a deterministic tool lands somewhere new. Behaviour (\"a model is not deciding, and a named executor on disk does the work\") is the durable predicate.</li><li><strong>Leave IMP-115 as shipped and file only a doc note.</strong> Rejected: the contradiction is inside a normative spec (§1.2 vs AC 2 vs Metric). A note alongside it does not make the three agree, and the suite still enforces the narrow reading via a hard-coded path.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: steps 28 and 29 carry <code>judgment: mechanical</code>, and IMP-115 §1.2, AC 2, and the Success Metric state one behaviour-keyed definition with no surviving <code>docs-tools/</code> narrowing — suite-asserted plus recorded grep. Baseline: 28-29 are <code>medium</code>; the three sentences disagree.</li><li>Guardrail: no model string, price, or effort level is introduced, and nothing in the payload reads the <code>judgment</code> field to decide anything — the advisory guarantee IMP-115 established survives unchanged.</li></ul>\n<h2>Scope</h2>\n<p>In scope: IMP-115 §1.2 / AC 2 / Success Metric wording; steps 28-29's <code>judgment</code> value; §11e's mechanical-definition line, helper table, and rough-edge paragraph; the <code>test_mechanical_steps_are_helper_backed</code> arm and two supporting arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any change to what awh-gate or caf-gate DOES, when they run, or how step 30's done-flip reads their <code>GREEN</code>/<code>CLEAN</code> outcomes — this task touches the advisory label only.</li><li>Re-tiering any step other than 28 and 29.</li><li>Extending <code>judgment</code> to <code>create-tasks</code> or <code>plan</code> — still out of scope, exactly as IMP-115 left it.</li><li>Vendoring <code>tools/awh</code> into the payload — awh's ship status is a separate question this task does not open.</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-115: this task corrects three artefacts IMP-115 produced (its spec, its <code>ship-tasks</code> annotation, its suite arm). IMP-115 must be landed for there to be a definition to reconcile, so 125 comes after 115, never before. <code>service: modules/cuo</code> serialises 125 behind the in-flight <code>modules/cuo</code> cone per the ship-tasks parent-serialisation rule; it must not race it.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the IMP-115 spec and <code>ship-tasks</code> §11e, with every claim (the contradiction, the two bite-points, the two gates' determinism, the docs-tools vendoring check, the mislocated \"no LLM\" attribution) re-verified against source at commit 8e7ffdca by this author.</li><li><strong>Human review:</strong> filing approved at the 2026-07-18 IMP-115 review gate (recorded HITL verdict).</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The definition of <code>mechanical</code> MUST be keyed on behaviour — a deterministic executor produces the step's result with no model deciding anything — and MUST NOT be keyed on the directory the executor lives in.</li><li>1.2 IMP-115 §1.2, its AC 2, and its Success Metric MUST all state that one behaviour-keyed definition; no residual wording MUST narrow <code>mechanical</code> to a <code>docs-tools/</code> path.</li><li>1.3 Steps 28 (<code>awh-gate</code>) and 29 (<code>caf-gate</code>) MUST be annotated <code>judgment: mechanical</code>.</li><li>1.4 Every step marked <code>mechanical</code> MUST name, in the payload's own prose for that step's skill, the deterministic executor that produces its result, and that executor MUST exist on disk irrespective of its directory.</li><li>1.5 The AC-2 suite arm MUST pass a <code>mechanical</code> step whose executor lives outside <code>docs-tools/</code> and MUST still fail a <code>mechanical</code> label that names no executor, names one absent from disk, or names one not anchored in the payload's prose for that skill.</li><li>1.6 This task MUST NOT introduce any model string, price, or effort-level literal into the payload.</li><li>1.7 The <code>judgment</code> field MUST remain advisory: nothing in the payload MUST read it to decide anything.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - IMP-115 §1.2's reconciled text asserts determinism (\"deterministic executor\", \"no model deciding\") and contains no directory token (<code>docs-tools</code>, <code>scripts/</code>, <code>tools/</code>) - verify: recorded grep in the gate log (a prose-wording claim no suite can grade; same rationale as TASK-IMP-090 AC 1).</li><li>[ ] AC 2 (traces_to: #1.2) - IMP-115 §1.2, AC 2, and the Success Metric each carry the behaviour predicate and none carries \"docs-tools helper\" - verify: recorded three-way grep in the gate log (a cross-sentence consistency claim; same rationale as TASK-IMP-090 AC 1).</li><li>[ ] AC 3 (traces_to: #1.3) - steps 28 and 29 both carry <code>judgment: mechanical</code> in ship-tasks' skill_chain - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_gates_28_29_are_mechanical</code></li><li>[ ] AC 4 (traces_to: #1.4) - every mechanical step names an on-disk executor anchored in that skill's own payload prose - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_mechanical_steps_are_helper_backed</code></li><li>[ ] AC 5 (traces_to: #1.5) - the arm accepts <code>tools/awh</code> and <code>scripts/caf_gate.sh</code> as mechanical executors yet still rejects an unnamed, absent, or unanchored one - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_mechanical_executor_may_live_outside_docs_tools</code></li><li>[ ] AC 6 (traces_to: #1.6) - no model string, price, or effort literal appears anywhere in the payload after the change - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_no_host_specific_literals</code></li><li>[ ] AC 7 (traces_to: #1.7) - nothing in the payload reads the <code>judgment</code> field to gate the run - verify: recorded grep in the gate log (a negative structural claim; same rationale as TASK-IMP-115 AC 4 / TASK-IMP-090 AC 1).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A third deterministic gate added later whose executor is neither <code>.mjs</code> nor under <code>docs-tools/</code>: it is <code>mechanical</code> the moment it names an on-disk executor in its own prose — that is precisely the rot the behaviour reading prevents, and AC 5's arm admits it without an edit.</li><li>A step that names an executor which does not exist on disk (a typo or an invented tool): still fails, exactly as it did before — AC 5 keeps the exists-on-disk lie-vector closed.</li><li><code>tools/awh</code> is not vendored into the payload (<code>find dist/cyberos -iname '*awh*'</code> is empty): the annotation describes the WORK a host routes on, not what the payload ships, so the label is correct even where the executor is absent. Vendoring awh is a separate task (Non-Goals).</li><li>Step 23 (<code>coverage-gate-author</code>, <code>coverage-scope.mjs</code>) stays <code>medium</code>: its helper owns only the deterministic half and reserves <code>tests_failed</code> / <code>ecm_rows_uncovered</code> for the author skill (coverage-scope.mjs header), so a model still decides — the behaviour reading leaves it <code>medium</code>, unchanged.</li><li>Security-class: edits one enum value on two table rows, three sentences of prose, and one test fixture. No execution surface, no new input path.</li></ul>";
