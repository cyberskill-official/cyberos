window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EVAL-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The governance layer <strong>MUST</strong> make wide, day-1 capture and downstream evaluation lawful, access-bounded, time-bounded, and tamper-evident BEFORE any interaction event is captured or any evaluation is produced. It is the precondition gate for the whole BRAIN/EVAL workstream. The contract:</p>\n<ol><li><strong>MUST</strong> define a versioned per-tenant monitoring/data-processing notice. The <code>eval_notice</code> table carries <code>id UUID PK</code>, <code>tenant_id UUID</code>, <code>version INT</code>, <code>body_md TEXT</code> (the human-readable notice text), <code>body_sha256 TEXT</code> (hash of the exact text acknowledged), <code>lawful_basis_summary TEXT</code>, <code>published_at TIMESTAMPTZ</code>, <code>published_by UUID</code>, <code>is_current BOOL</code>. Exactly one row per tenant has <code>is_current = true</code>. Publishing a new version flips the prior current row to <code>false</code> in the same transaction. The notice table is APPEND-ONLY (no UPDATE/DELETE of a published version — a correction is a new version).</li></ol>\n<ol><li><strong>MUST</strong> record per-subject acknowledgment of a specific notice version. The <code>eval_ack</code> table carries <code>id UUID PK</code>, <code>tenant_id UUID</code>, <code>subject_id UUID</code>, <code>notice_version INT</code>, <code>notice_sha256 TEXT</code>, <code>acknowledged_at TIMESTAMPTZ</code>, <code>ack_method TEXT</code> (<code>console_click | sso_consent | hr_recorded | contract_addendum</code>), <code>UNIQUE (tenant_id, subject_id, notice_version)</code>. The <code>notice_sha256</code> MUST match the acknowledged notice's <code>body_sha256</code> (the subject acknowledged <em>that</em> text, not a moving target).</li></ol>\n<ol><li><strong>MUST</strong> expose <code>is_gated(subject_id) -&gt; bool</code>: a subject is <strong>gated</strong> (capture and evaluation BLOCKED for that subject) whenever there is no <code>eval_ack</code> row matching the tenant's <em>current</em> notice version. This is the precondition that makes wide day-1 capture lawful: the moment a person logs into the OS, capture is armed, but it does not record that person until their acknowledgment of the current notice is on file. TASK-MEMORY-121/122 capture emitters and TASK-EVAL-003 evaluation <strong>MUST</strong> consult <code>is_gated</code> and skip a gated subject (emitting a <code>eval.capture_gated</code> audit row instead, so the skip is itself recorded).</li></ol>\n<ol><li><strong>MUST</strong> define a data-category + purpose + lawful-basis registry. The <code>eval_data_category</code> table carries <code>id UUID PK</code>, <code>tenant_id UUID</code>, <code>category TEXT</code> (e.g. <code>chat_message</code>, <code>module_usage</code>, <code>task_activity</code>, <code>signin_presence</code>, <code>document_activity</code>), <code>declared_purpose TEXT</code>, <code>lawful_basis TEXT</code> (closed enum: <code>legitimate_interest | contract_performance | legal_obligation | consent</code>), <code>retain_days INT</code>, <code>minimisation_note TEXT</code>, <code>created_at</code>, <code>created_by</code>. A category <strong>MUST NOT</strong> be registrable without a non-empty <code>declared_purpose</code> and a <code>lawful_basis</code> from the enum. Capture for a category not present in the registry is rejected (no silent collection of undeclared categories).</li></ol>\n<ol><li><strong>MUST</strong> enforce scope = platform work-interactions ONLY. The registry <strong>MUST NOT</strong> admit a category representing keystroke logging, screen capture, microphone/camera capture, location tracking, or private-life monitoring; such a category is rejected at <code>register_category</code> with <code>422 category_out_of_scope</code>. This encodes DEC-2522 minimisation as a normative invariant, not a guideline.</li></ol>\n<ol><li><strong>MUST</strong> define a per-category retention policy and a retention/erasure sweeper. The <code>eval_retention_policy</code> table carries <code>tenant_id</code>, <code>category</code>, <code>retain_days INT</code>, <code>basis TEXT</code>, <code>updated_at</code>, <code>updated_by</code>. A background <code>retention-sweep</code> job (cron, daily) <strong>MUST</strong> delete or irreversibly redact Layer-2 projections (the <code>l2_memory</code> / <code>l2_entity</code> rows and EVAL artefacts) for a category whose <code>ingested_at</code> is older than <code>retain_days</code>. Nothing is retained without a policy. The sweeper <strong>MUST NOT</strong> mutate or delete the Layer-1 <code>l1_audit_log</code> chain (the chain is the append-only system of record; erasure operates on the queryable projections + EVAL outputs, and an <code>eval.subject_erased</code> row is itself appended to the chain to record the erasure).</li></ol>\n<ol><li><strong>MUST</strong> enforce access control on evaluation/monitoring data. <code>may_read(reader_subject_id, target_subject_id) -&gt; bool</code> resolves true iff ANY of: (a) the reader holds the founder/owner role (<code>cyberos_founder</code>); (b) the reader is a designated manager of the target via the <code>eval_access_grant</code> table's <code>manager_of</code> relationship; (c) <code>reader_subject_id == target_subject_id</code> (own record); (d) an explicit active grant row exists for the pair. All other reads resolve false. Enforcement is BOTH tenant RLS (<code>app.current_tenant_id</code>, per TASK-AUTH-003) AND the <code>may_read</code> check at the handler — defence in depth.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>eval_access_grant</code> table: <code>id UUID PK</code>, <code>tenant_id UUID</code>, <code>reader_subject_id UUID</code>, <code>target_subject_id UUID</code>, <code>grant_kind TEXT</code> (<code>manager_of | hr_reviewer | explicit | self</code>), <code>granted_by UUID</code>, <code>granted_at TIMESTAMPTZ</code>, <code>revoked_at TIMESTAMPTZ NULL</code>, <code>reason TEXT</code>. A grant is active when <code>revoked_at IS NULL</code>. Grant and revoke are mutations that emit audit rows (clause 12).</li></ol>\n<ol><li><strong>MUST</strong> emit one <code>eval.evaluation_read</code> audit row on EVERY read of another subject's evaluation/monitoring data (i.e. every call where <code>reader_subject_id != target_subject_id</code>). The row payload carries <code>{reader_subject_id, target_subject_id, grant_kind_used, resource, trace_id}</code>. A subject reading their OWN record does not require a grant but the read is still counted (an <code>eval.self_read</code> row, lighter weight). This makes every cross-person access traceable after the fact.</li></ol>\n<ol><li><strong>MUST</strong> expose data-subject rights endpoints: (a) <code>GET /v1/eval/me</code> returns the requesting subject's own monitoring/evaluation record (governed by clause 7c, always permitted for self); (b) <code>POST /v1/eval/me/requests</code> files a rectification or objection request (<code>request_kind ∈ rectification | objection | erasure_request | access_export</code>), stored in <code>eval_dsr_request</code> and QUEUED for a human — it is NOT auto-applied. The <code>eval_dsr_request</code> table carries <code>id</code>, <code>tenant_id</code>, <code>subject_id</code>, <code>request_kind</code>, <code>detail TEXT</code>, <code>status TEXT</code> (<code>open | acknowledged | actioned | declined</code>), <code>created_at</code>, <code>resolved_by NULL</code>, <code>resolved_at NULL</code>, <code>resolution_note NULL</code>.</li></ol>\n<ol><li><strong>MUST</strong> keep a human in the loop for anything consequential. No governance endpoint, and no downstream EVAL engine consuming this layer, may auto-apply an erasure-request, an objection, or any change that affects pay, progression, or employment. Those are recorded, surfaced, and decided by a person (cross-reference TASK-EVAL-003 HITL). The sweeper's <em>scheduled retention</em> deletion (clause 6) is the only automatic erasure, and it acts only on the policy the operator configured.</li></ol>\n<ol><li><strong>MUST</strong> emit one <code>l1_audit_log</code> row for EVERY governance mutation, via <code>services/shared/cyberos-audit-chain</code>'s <code>chain_anchor</code> helper, with these kinds: <code>eval.notice_published</code>, <code>eval.notice_acknowledged</code>, <code>eval.category_registered</code>, <code>eval.retention_changed</code>, <code>eval.access_granted</code>, <code>eval.access_revoked</code>, <code>eval.dsr_filed</code>, <code>eval.dsr_resolved</code>, <code>eval.retention_swept</code>, <code>eval.subject_erased</code>. Each row is tenant-scoped and subject-attributed. The governance layer is therefore itself tamper-evident, exactly like the data it governs.</li></ol>\n<ol><li><strong>MUST</strong> state explicitly, in code comments and in this spec, that fully-covert / no-notice collection is OUT OF SCOPE (DEC-2525). The disclosed notice (clause 1) plus the acknowledgment gate (clause 3) are the boundary of what this system does. A deployment that captures without a published, acknowledged notice is a misconfiguration the gate is designed to prevent, and is a legal risk to be confirmed with Vietnamese counsel before any such mode is ever considered.</li></ol>\n<ol><li><strong>MUST</strong> RLS-enforce every table on <code>tenant_id = current_setting('app.current_tenant_id')::uuid</code> (per TASK-AUTH-003) so one tenant's notices, acknowledgments, grants, and DSR requests are invisible to another.</li></ol>\n<ol><li><strong>MUST</strong> make <code>eval_notice</code>, <code>eval_ack</code>, <code>eval_access_grant</code>, and <code>eval_dsr_request</code> APPEND-ONLY for the runtime SQL role (<code>cyberos_app</code>): <code>REVOKE UPDATE, DELETE</code>. A notice correction is a new version; an acknowledgment is never un-said; a grant is revoked by setting <code>revoked_at</code> (which is the one permitted column update, gated to the admin role) rather than deletion; a DSR resolution writes <code>resolved_*</code> columns via the admin role. Append-only is the audit-trail invariant.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics: <code>eval_capture_gated_total{reason}</code> (counter; reason ∈ <code>no_ack | stale_ack_version</code>), <code>eval_evaluation_reads_total{grant_kind}</code> (counter), <code>eval_retention_swept_rows_total{category}</code> (counter), <code>eval_dsr_requests_total{request_kind}</code> (counter).</li></ol>\n<ol><li><strong>MUST</strong> treat a notice-version bump as re-gating: when a tenant publishes notice version N+1, every subject whose latest ack is for version ≤ N becomes gated again until they acknowledge N+1. <code>eval_capture_gated_total{reason=\"stale_ack_version\"}</code> increments for those subjects. This guarantees a materially changed monitoring scope is re-disclosed and re-acknowledged before capture resumes under it.</li></ol>\n<ol><li><strong>MUST</strong> provide a read-only <code>GET /v1/eval/governance/status</code> for the founder/managers that returns, per tenant: current notice version, count of acknowledged vs gated subjects, registered categories with their purpose + lawful_basis + retain_days, and open DSR request count. This is the operator's at-a-glance proof that the gate is healthy (everyone acknowledged, every category declared, retention set) before relying on any evaluation built on top.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why governance is Phase 0, not Phase 5.</strong> Every later phase inherits its legality from this gate. If capture (Phase 1) ships first, the company is collecting employee work-interaction data with no notice, no lawful basis on file, no access boundary, and no retention limit — which is the covert-surveillance posture that is both the legal risk under Vietnam's PDPD (Decree 13/2023/ND-CP) and Labor Code (45/2019/QH14), and the trust risk that makes a team resent the system. Building the gate first is cheap and it unblocks everything safely. This is the plan's own sequencing decision (DEC-2520).</p>\n<p><strong>Why a versioned notice with a hash (clause 1, 2).</strong> \"The employee was told\" is only defensible if you can show <em>what</em> they were told and <em>that</em> they agreed to that exact text. Versioning plus <code>body_sha256</code> pins the acknowledged text: a later edit to the notice produces a new version, and the old acknowledgment still proves what was disclosed at the time. Without the hash, an operator could quietly edit the notice and the ack would silently re-point at new terms.</p>\n<p><strong>Why the acknowledgment gate (clause 3) is what makes day-1 capture lawful.</strong> Stephen's decision is wide, day-1 capture — monitoring works the moment a person logs into the OS. That breadth is only lawful behind disclosure-and-acknowledgment. So capture is <em>armed</em> immediately, but each subject is <em>gated</em> until their acknowledgment of the current notice is recorded. The gate flips per-subject, automatically, the instant the ack lands. Capture emitters skip a gated subject and record the skip (<code>eval.capture_gated</code>), so even the absence of data is auditable.</p>\n<p><strong>Why a category/purpose/lawful-basis registry (clause 4, 5).</strong> PDPD expects purpose limitation and a lawful basis. A registry forces every collected category to declare <em>why</em> it is collected and <em>under what basis</em>, and refuses undeclared categories. Clause 5 hard-codes minimisation (DEC-2522): keystroke logging, screen capture, location, and private-life categories are rejected at registration. Scope is enforced by the system, not left to good intentions.</p>\n<p><strong>Why a retention sweeper that never touches Layer 1 (clause 6).</strong> \"Do not keep everything forever\" is a PDPD expectation and a liability limit. But the audit chain is append-only and tamper-evident by design — deleting from it would break the very integrity property the brain relies on. The resolution: retention erases the <em>queryable projections</em> and EVAL outputs (the L2 lens), while the L1 chain stays intact, and the erasure event is itself appended to L1. You bound what is <em>recallable</em> without breaking what is <em>provable</em>. This mirrors the established Layer-1-is-source / Layer-2-is-projection split (memory's <code>0001_layer2.sql</code>).</p>\n<p><strong>Why access control is both RLS and an explicit check (clause 7, 8).</strong> Tenant RLS keeps tenants apart, but within one tenant every employee shares a tenant_id — RLS alone would let any colleague read any colleague's performance file. The <code>may_read</code> resolution (founder, manager-of, self, explicit grant) is the <em>intra-tenant</em> boundary that RLS does not provide. Doing both is defence in depth: a handler bug that forgets the check still can't cross tenants, and a connection that somehow crosses tenants still hits the <code>may_read</code> gate.</p>\n<p><strong>Why a read-audit row per cross-person read (clause 9, 12).</strong> The data being read is consequential (it can inform pay and progression). \"Who looked at whose file\" must be reconstructable. A subject reading their own record is normal and frequent, so it gets a lighter <code>eval.self_read</code>; a manager reading a report's file is the event that must always leave a trace.</p>\n<p><strong>Why DSR requests are queued, not auto-applied (clause 10, 11).</strong> A subject can always read their own record and can file a rectification or objection — those are data-subject rights. But auto-applying an erasure or objection could destroy evidence or silently change an evaluation; and anything affecting employment must be a human decision (the plan's human-in-the-loop principle). So rights requests are first-class and recorded, and a person resolves them.</p>\n<p><strong>Why re-gate on a notice bump (clause 17).</strong> If the monitoring scope materially changes (a new category, a new purpose), the old acknowledgment no longer covers it. Re-gating forces re-disclosure and re-acknowledgment before capture continues under the new terms. This is the difference between a living consent record and a one-time checkbox.</p>\n<p><strong>Why a governance-status endpoint (clause 18).</strong> Before anyone trusts an evaluation, they need a one-glance answer to \"is the gate actually healthy?\" — is everyone acknowledged, is every category declared with a basis, is retention set. It turns the governance posture into something an operator can verify, not assume.</p>\n<p><strong>This is not legal advice.</strong> The clauses encode a defensible <em>shape</em> (disclosure, purpose limitation, access control, retention, data-subject rights, human-in-the-loop), grounded in the named Vietnamese instruments, but the notice text and the precise lawful-basis selection per category MUST be confirmed by Vietnamese counsel before go-live. The system enforces the structure; counsel confirms the content.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/eval/migrations/0001_governance.sql\n-- EVAL Phase-0 governance: notice + ack gate, category registry, retention, access, DSR.\n-- Reuses AUTH's per-tenant RLS GUC (app.current_tenant_id, TASK-AUTH-003) and the\n-- L1 audit chain (services/shared/cyberos-audit-chain). L1 (l1_audit_log) is the\n-- source of truth and is never mutated here; this module governs the L2 projections.\n\n-- 1. Versioned per-tenant monitoring/data-processing notice (append-only).\nCREATE TABLE eval_notice (\n    id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id             UUID NOT NULL,\n    version               INT  NOT NULL,\n    body_md               TEXT NOT NULL,\n    body_sha256           TEXT NOT NULL,\n    lawful_basis_summary  TEXT NOT NULL,\n    published_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    published_by          UUID NOT NULL,\n    is_current            BOOL NOT NULL DEFAULT TRUE,\n    UNIQUE (tenant_id, version)\n);\n-- Exactly one current notice per tenant.\nCREATE UNIQUE INDEX eval_notice_one_current\n    ON eval_notice (tenant_id) WHERE is_current;\n\n-- 2. Per-subject acknowledgment of a specific notice version (append-only).\nCREATE TABLE eval_ack (\n    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id        UUID NOT NULL,\n    subject_id       UUID NOT NULL,\n    notice_version   INT  NOT NULL,\n    notice_sha256    TEXT NOT NULL,\n    acknowledged_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    ack_method       TEXT NOT NULL CHECK (ack_method IN\n                     ('console_click','sso_consent','hr_recorded','contract_addendum')),\n    UNIQUE (tenant_id, subject_id, notice_version)\n);\n\n-- 4. Data-category + purpose + lawful-basis registry.\nCREATE TABLE eval_data_category (\n    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id        UUID NOT NULL,\n    category         TEXT NOT NULL,\n    declared_purpose TEXT NOT NULL CHECK (length(declared_purpose) &gt; 0),\n    lawful_basis     TEXT NOT NULL CHECK (lawful_basis IN\n                     ('legitimate_interest','contract_performance','legal_obligation','consent')),\n    retain_days      INT  NOT NULL CHECK (retain_days &gt; 0),\n    minimisation_note TEXT NOT NULL DEFAULT '',\n    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    created_by       UUID NOT NULL,\n    UNIQUE (tenant_id, category)\n);\n\n-- 6. Per-category retention policy.\nCREATE TABLE eval_retention_policy (\n    tenant_id   UUID NOT NULL,\n    category    TEXT NOT NULL,\n    retain_days INT  NOT NULL CHECK (retain_days &gt; 0),\n    basis       TEXT NOT NULL,\n    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_by  UUID NOT NULL,\n    PRIMARY KEY (tenant_id, category)\n);\n\n-- 8. Access grants (append-only; revoke = set revoked_at via admin role).\nCREATE TABLE eval_access_grant (\n    id                 UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id          UUID NOT NULL,\n    reader_subject_id  UUID NOT NULL,\n    target_subject_id  UUID NOT NULL,\n    grant_kind         TEXT NOT NULL CHECK (grant_kind IN\n                       ('manager_of','hr_reviewer','explicit','self')),\n    granted_by         UUID NOT NULL,\n    granted_at         TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    revoked_at         TIMESTAMPTZ,\n    reason             TEXT NOT NULL DEFAULT ''\n);\nCREATE INDEX eval_access_grant_lookup\n    ON eval_access_grant (tenant_id, reader_subject_id, target_subject_id)\n    WHERE revoked_at IS NULL;\n\n-- 10. Data-subject rights requests (resolution columns written by admin role).\nCREATE TABLE eval_dsr_request (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id       UUID NOT NULL,\n    subject_id      UUID NOT NULL,\n    request_kind    TEXT NOT NULL CHECK (request_kind IN\n                    ('rectification','objection','erasure_request','access_export')),\n    detail          TEXT NOT NULL DEFAULT '',\n    status          TEXT NOT NULL DEFAULT 'open' CHECK (status IN\n                    ('open','acknowledged','actioned','declined')),\n    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    resolved_by     UUID,\n    resolved_at     TIMESTAMPTZ,\n    resolution_note TEXT\n);\n\n-- 14. Tenant RLS on every table (TASK-AUTH-003 GUC).\nALTER TABLE eval_notice          ENABLE ROW LEVEL SECURITY;\nALTER TABLE eval_ack             ENABLE ROW LEVEL SECURITY;\nALTER TABLE eval_data_category   ENABLE ROW LEVEL SECURITY;\nALTER TABLE eval_retention_policy ENABLE ROW LEVEL SECURITY;\nALTER TABLE eval_access_grant    ENABLE ROW LEVEL SECURITY;\nALTER TABLE eval_dsr_request     ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY eval_notice_tenant ON eval_notice\n    USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);\nCREATE POLICY eval_ack_tenant ON eval_ack\n    USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);\nCREATE POLICY eval_category_tenant ON eval_data_category\n    USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);\nCREATE POLICY eval_retention_tenant ON eval_retention_policy\n    USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);\nCREATE POLICY eval_grant_tenant ON eval_access_grant\n    USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);\nCREATE POLICY eval_dsr_tenant ON eval_dsr_request\n    USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);\n\n-- 15. Append-only for the runtime role. revoked_at / resolved_* updates go via the admin role.\nREVOKE UPDATE, DELETE ON eval_notice        FROM cyberos_app;\nREVOKE UPDATE, DELETE ON eval_ack           FROM cyberos_app;\nREVOKE        DELETE ON eval_access_grant   FROM cyberos_app;  -- UPDATE allowed only for revoked_at (admin)\nREVOKE        DELETE ON eval_dsr_request    FROM cyberos_app;  -- UPDATE allowed only for resolution (admin)</code></pre>\n<h3>Rust API — the acknowledgment gate (clause 3, 17)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/notice/mod.rs\nuse uuid::Uuid;\n\n/// Why a subject is gated (mirrors the OTel `reason` label, clause 16).\n#[derive(Clone, Copy, Debug, PartialEq, Eq)]\npub enum GateReason {\n    NoAck,            // never acknowledged any notice\n    StaleAckVersion,  // acknowledged an older version; current notice bumped (clause 17)\n}\n\n/// A subject is GATED (capture + evaluation BLOCKED) unless they have acknowledged\n/// the tenant's CURRENT notice version. Returns None when not gated.\npub async fn gate_reason(\n    pool: &amp;sqlx::PgPool,\n    subject_id: Uuid,\n) -&gt; Result&lt;Option&lt;GateReason&gt;, sqlx::Error&gt; {\n    // Current notice version for this tenant (RLS scopes to the connection's tenant).\n    let current: Option&lt;i32&gt; = sqlx::query_scalar(\n        \"SELECT version FROM eval_notice WHERE is_current LIMIT 1\",\n    )\n    .fetch_optional(pool)\n    .await?;\n\n    let Some(current_version) = current else {\n        // No notice published yet ⇒ nobody can be captured ⇒ everyone gated.\n        return Ok(Some(GateReason::NoAck));\n    };\n\n    let latest_ack: Option&lt;i32&gt; = sqlx::query_scalar(\n        \"SELECT MAX(notice_version) FROM eval_ack WHERE subject_id = $1\",\n    )\n    .bind(subject_id)\n    .fetch_one(pool)\n    .await?;\n\n    Ok(match latest_ack {\n        Some(v) if v &gt;= current_version =&gt; None,                       // up to date ⇒ not gated\n        Some(_) =&gt; Some(GateReason::StaleAckVersion),                  // acked, but stale\n        None =&gt; Some(GateReason::NoAck),                               // never acked\n    })\n}\n\n/// The hot-path predicate TASK-MEMORY-121/122 + TASK-EVAL-003 call before capturing/evaluating.\npub async fn is_gated(pool: &amp;sqlx::PgPool, subject_id: Uuid) -&gt; Result&lt;bool, sqlx::Error&gt; {\n    Ok(gate_reason(pool, subject_id).await?.is_some())\n}</code></pre>\n<h3>Rust API — access resolution + read audit (clause 7, 9, 12)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/access/mod.rs\nuse uuid::Uuid;\n\n#[derive(Clone, Copy, Debug, PartialEq, Eq)]\npub enum GrantKind { Founder, ManagerOf, Self_, Explicit }\n\n/// Resolve whether `reader` may read `target`'s evaluation/monitoring data.\n/// Returns the grant kind used (for the audit row) or None if denied.\npub async fn may_read(\n    pool: &amp;sqlx::PgPool,\n    reader: Uuid,\n    target: Uuid,\n    reader_is_founder: bool,  // from the JWT role claim (TASK-AUTH-101 cyberos_founder)\n) -&gt; Result&lt;Option&lt;GrantKind&gt;, sqlx::Error&gt; {\n    if reader == target {\n        return Ok(Some(GrantKind::Self_));            // own record — clause 7c\n    }\n    if reader_is_founder {\n        return Ok(Some(GrantKind::Founder));          // clause 7a\n    }\n    // Active grant (manager_of / hr_reviewer / explicit) — clause 7b/7d.\n    let kind: Option&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT grant_kind FROM eval_access_grant\n          WHERE reader_subject_id = $1 AND target_subject_id = $2 AND revoked_at IS NULL\n          ORDER BY granted_at DESC LIMIT 1\",\n    )\n    .bind(reader)\n    .bind(target)\n    .fetch_optional(pool)\n    .await?;\n\n    Ok(match kind.as_deref() {\n        Some(\"manager_of\") =&gt; Some(GrantKind::ManagerOf),\n        Some(_)            =&gt; Some(GrantKind::Explicit),\n        None               =&gt; None,                   // denied\n    })\n}\n\n/// Guard a read of another subject's data: resolve access, then ALWAYS emit a read-audit\n/// row when reader != target (clause 9). Denied ⇒ Err(Forbidden), no data returned.\npub async fn guard_evaluation_read(\n    pool: &amp;sqlx::PgPool,\n    reader: Uuid,\n    target: Uuid,\n    reader_is_founder: bool,\n    resource: &amp;str,\n) -&gt; Result&lt;GrantKind, AccessError&gt; {\n    match may_read(pool, reader, target, reader_is_founder).await? {\n        Some(GrantKind::Self_) =&gt; {\n            crate::audit::emit(pool, reader, \"eval.self_read\",\n                &amp;serde_json::json!({ \"resource\": resource }).to_string()).await?;\n            Ok(GrantKind::Self_)\n        }\n        Some(kind) =&gt; {\n            crate::audit::emit(pool, reader, \"eval.evaluation_read\", &amp;serde_json::json!({\n                \"reader_subject_id\": reader, \"target_subject_id\": target,\n                \"grant_kind_used\": format!(\"{kind:?}\"), \"resource\": resource,\n                \"trace_id\": crate::trace_id(),\n            }).to_string()).await?;\n            Ok(kind)\n        }\n        None =&gt; Err(AccessError::Forbidden { reader, target }),\n    }\n}</code></pre>\n<h3>Rust API — governance-mutation audit (clause 12) reusing the shared chain</h3>\n<pre><code class=\"language-rust\">// services/eval/src/audit.rs\n// Reuse the canonical L1 chain emit so EVAL rows verify under memory's reconcile,\n// exactly like obs and auth do (services/shared/cyberos-audit-chain).\nuse uuid::Uuid;\n\n/// Append one governance row to l1_audit_log under the connection's tenant.\n/// `kind` is one of the clause-12 eval.* kinds; `body` is the canonical JSON payload.\npub async fn emit(\n    pool: &amp;sqlx::PgPool,\n    subject_id: Uuid,\n    kind: &amp;str,\n    body: &amp;str,\n) -&gt; Result&lt;i64, sqlx::Error&gt; {\n    let tenant_id: Uuid = sqlx::query_scalar(\n        \"SELECT current_setting('app.current_tenant_id', true)::uuid\",\n    )\n    .fetch_one(pool)\n    .await?;\n    // path namespaces the row by kind; genesis row (independent governance event).\n    let path = format!(\"eval/{}/{}\", tenant_id, kind);\n    cyberos_audit_chain::emit_genesis(pool, tenant_id, subject_id, &amp;path, body).await\n}</code></pre>\n<h3>Rust API — retention sweeper (clause 6)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/retention/mod.rs\n/// Daily sweep: for each (tenant, category) policy, erase L2 projections older than\n/// retain_days. NEVER touches l1_audit_log. Emits eval.retention_swept per category and\n/// eval.subject_erased per affected subject. Returns rows erased.\npub async fn run_retention_sweep(pool: &amp;sqlx::PgPool) -&gt; Result&lt;u64, sqlx::Error&gt; {\n    let policies: Vec&lt;(Uuid, String, i32)&gt; = sqlx::query_as(\n        \"SELECT tenant_id, category, retain_days FROM eval_retention_policy\",\n    )\n    .fetch_all(pool)\n    .await?;\n\n    let mut total: u64 = 0;\n    for (tenant_id, category, retain_days) in policies {\n        // L2 projection erasure only — the L1 chain is the immutable system of record.\n        let erased = sqlx::query(\n            \"DELETE FROM l2_memory\n              WHERE tenant_id = $1\n                AND frontmatter-&gt;&gt;'eval_category' = $2\n                AND ingested_at &lt; NOW() - ($3 || ' days')::interval\",\n        )\n        .bind(tenant_id)\n        .bind(&amp;category)\n        .bind(retain_days)\n        .execute(pool)\n        .await?\n        .rows_affected();\n        total += erased;\n        if erased &gt; 0 {\n            metrics::counter!(\"eval_retention_swept_rows_total\", \"category\" =&gt; category.clone())\n                .increment(erased);\n            // The erasure itself is appended to the L1 chain (auditable erasure).\n            emit_system(pool, tenant_id, \"eval.retention_swept\", &amp;serde_json::json!({\n                \"category\": category, \"retain_days\": retain_days, \"rows_erased\": erased,\n            }).to_string()).await?;\n        }\n    }\n    Ok(total)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Notice publish flips current</strong> — publishing version 2 sets version 1 <code>is_current=false</code> and version 2 <code>is_current=true</code> in one transaction; the partial unique index permits exactly one current row (AC for §1 #1).</li><li><strong>Notice is append-only</strong> — UPDATE/DELETE on <code>eval_notice</code> by <code>cyberos_app</code> → permission denied (AC for §1 #15).</li><li><strong>Ack pins the hash</strong> — an <code>eval_ack</code> whose <code>notice_sha256</code> != the current notice's <code>body_sha256</code> is rejected (AC for §1 #2).</li><li><strong>Un-acked subject is gated</strong> — <code>is_gated(subject)</code> returns true when the subject has no ack for the current version; TASK-MEMORY-121 capture skips them and emits <code>eval.capture_gated</code> (AC for §1 #3).</li><li><strong>Ack lifts the gate</strong> — after the subject acknowledges the current version, <code>is_gated(subject)</code> returns false and capture proceeds (AC for §1 #3).</li><li><strong>No notice ⇒ everyone gated</strong> — with zero published notices, <code>gate_reason</code> returns <code>NoAck</code> for any subject (AC for §1 #3, #13).</li><li><strong>Category needs purpose + basis</strong> — <code>register_category</code> with empty <code>declared_purpose</code> → rejected; with a <code>lawful_basis</code> outside the enum → rejected (AC for §1 #4).</li><li><strong>Out-of-scope category rejected</strong> — registering a <code>keystroke</code> / <code>screen_capture</code> / <code>location</code> category → <code>422 category_out_of_scope</code> (AC for §1 #5).</li><li><strong>Undeclared category not captured</strong> — a capture for a category absent from <code>eval_data_category</code> is refused (AC for §1 #4).</li><li><strong>Retention sweep erases past-retention L2 rows</strong> — an <code>l2_memory</code> row tagged with a category, older than <code>retain_days</code>, is deleted by the sweep; a fresher row is kept (AC for §1 #6).</li><li><strong>Sweep never touches L1</strong> — after a sweep, <code>l1_audit_log</code> row count is unchanged (only grew, by the <code>eval.retention_swept</code> / <code>eval.subject_erased</code> rows); no L1 row was deleted (AC for §1 #6, #11).</li><li><strong>Founder may read any</strong> — <code>may_read(founder, anyone)</code> → <code>Founder</code> (AC for §1 #7a).</li><li><strong>Manager-of may read report</strong> — with an active <code>manager_of</code> grant, <code>may_read(mgr, report)</code> → <code>ManagerOf</code> (AC for §1 #7b).</li><li><strong>Self may read self</strong> — <code>may_read(x, x)</code> → <code>Self_</code> with no grant needed (AC for §1 #7c).</li><li><strong>Stranger denied</strong> — <code>may_read(a, b)</code> with no grant and a not founder → None; the handler returns 403 (AC for §1 #7).</li><li><strong>Cross-person read emits audit</strong> — a permitted manager read of a report's record emits exactly one <code>eval.evaluation_read</code> row carrying <code>grant_kind_used</code> (AC for §1 #9, #12).</li><li><strong>Self read emits lighter audit</strong> — a self-read emits <code>eval.self_read</code>, not <code>eval.evaluation_read</code> (AC for §1 #9).</li><li><strong>Revoke removes access</strong> — after <code>revoked_at</code> is set, <code>may_read</code> no longer returns the grant; a subsequent read is denied; an <code>eval.access_revoked</code> row was emitted (AC for §1 #8, #12).</li><li><strong>DSR is queued, not applied</strong> — <code>POST /v1/eval/me/requests</code> with <code>erasure_request</code> creates an <code>open</code> <code>eval_dsr_request</code> row and emits <code>eval.dsr_filed</code>; no data is erased automatically (AC for §1 #10, #11).</li><li><strong>Self record always readable</strong> — <code>GET /v1/eval/me</code> returns the caller's own record regardless of grants (AC for §1 #10).</li><li><strong>Every governance mutation chains</strong> — publish, ack, register, retention-change, grant, revoke, dsr-file, dsr-resolve each append one <code>l1_audit_log</code> row whose <code>chain_anchor_hex</code> verifies under the shared helper (AC for §1 #12).</li><li><strong>Tenant isolation</strong> — tenant A's notices/acks/grants/DSR are invisible to tenant B under RLS (AC for §1 #14).</li><li><strong>Notice bump re-gates</strong> — a subject acked v1; tenant publishes v2; <code>gate_reason(subject)</code> returns <code>StaleAckVersion</code> and <code>eval_capture_gated_total{reason=\"stale_ack_version\"}</code> increments (AC for §1 #17).</li><li><strong>Governance status reflects truth</strong> — <code>GET /v1/eval/governance/status</code> reports current notice version, acknowledged vs gated counts, registered categories, and open DSR count consistent with the tables (AC for §1 #18).</li><li><strong>OTel counters increment</strong> — gated capture, cross-person read, swept rows, and DSR filing each increment their counter with the right label (AC for §1 #16).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn unacked_subject_is_gated_then_ack_lifts_it() {\n    let env = TestEnv::new().await;                // seeds tenant + subject, sets app.current_tenant_id\n    publish_notice(&amp;env.pool, 1, \"v1 monitoring notice\", env.founder()).await.unwrap();\n\n    assert!(is_gated(&amp;env.pool, env.subject()).await.unwrap());      // no ack yet\n    acknowledge(&amp;env.pool, env.subject(), 1, sha256(\"v1 monitoring notice\"),\n                AckMethod::ConsoleClick).await.unwrap();\n    assert!(!is_gated(&amp;env.pool, env.subject()).await.unwrap());     // ack lifts the gate\n}\n\n#[tokio::test]\nasync fn notice_bump_re_gates_until_reack() {\n    let env = TestEnv::new().await;\n    publish_notice(&amp;env.pool, 1, \"v1\", env.founder()).await.unwrap();\n    acknowledge(&amp;env.pool, env.subject(), 1, sha256(\"v1\"), AckMethod::ConsoleClick).await.unwrap();\n    assert!(!is_gated(&amp;env.pool, env.subject()).await.unwrap());\n\n    publish_notice(&amp;env.pool, 2, \"v2 wider scope\", env.founder()).await.unwrap();\n    assert_eq!(gate_reason(&amp;env.pool, env.subject()).await.unwrap(),\n               Some(GateReason::StaleAckVersion));                   // must re-acknowledge v2\n}\n\n#[tokio::test]\nasync fn stranger_read_denied_manager_read_audited() {\n    let env = TestEnv::new().await;\n    // No grant: stranger denied.\n    assert!(may_read(&amp;env.pool, env.alice(), env.bob(), false).await.unwrap().is_none());\n\n    // Grant manager_of, then a permitted read emits exactly one eval.evaluation_read.\n    grant_access(&amp;env.pool, env.alice(), env.bob(), GrantKind::ManagerOf, env.founder()).await.unwrap();\n    let before = env.count_audit(\"eval.evaluation_read\").await;\n    let kind = guard_evaluation_read(&amp;env.pool, env.alice(), env.bob(), false, \"record\").await.unwrap();\n    assert_eq!(kind, GrantKind::ManagerOf);\n    assert_eq!(env.count_audit(\"eval.evaluation_read\").await, before + 1);\n}\n\n#[tokio::test]\nasync fn retention_sweep_erases_l2_but_never_l1() {\n    let env = TestEnv::new().await;\n    set_retention(&amp;env.pool, \"chat_message\", 30, env.founder()).await.unwrap();\n    env.seed_l2_row(\"chat_message\", days_ago(40)).await;            // past retention\n    env.seed_l2_row(\"chat_message\", days_ago(5)).await;             // fresh\n    let l1_before = env.count_l1_rows().await;\n\n    let erased = run_retention_sweep(&amp;env.pool).await.unwrap();\n    assert_eq!(erased, 1);                                          // only the 40-day row\n    assert_eq!(env.count_l2_rows(\"chat_message\").await, 1);        // fresh row kept\n    assert!(env.count_l1_rows().await &gt;= l1_before);               // L1 only grew (sweep audit), never shrank\n}\n\n#[tokio::test]\nasync fn dsr_request_is_queued_not_auto_applied() {\n    let env = TestEnv::new().await;\n    let id = file_dsr(&amp;env.pool, env.subject(), DsrKind::ErasureRequest, \"remove my Q1 data\").await.unwrap();\n    let row = env.get_dsr(id).await;\n    assert_eq!(row.status, \"open\");                                // queued for a human\n    assert_eq!(env.count_l2_rows_for(env.subject()).await,\n               env.l2_baseline_for(env.subject()));                // nothing erased automatically\n}\n\n#[tokio::test]\nasync fn out_of_scope_category_rejected() {\n    let env = TestEnv::new().await;\n    let err = register_category(&amp;env.pool, \"keystroke_log\", \"perf\", LawfulBasis::LegitimateInterest, 30, env.founder())\n        .await.unwrap_err();\n    assert!(matches!(err, RegistryError::OutOfScope));             // clause 5 minimisation invariant\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Migration + the four core modules — notice gate, access resolution, audit emit, retention sweep — sketched in §3. <code>registry/mod.rs</code>, <code>rights/mod.rs</code>, and <code>handlers.rs</code> follow the same shape: RLS-scoped queries, closed enums via CHECK constraints, one <code>audit::emit</code> per mutation, RBAC role check from the JWT at the handler edge.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-AUTH-003</strong> — per-tenant RLS via <code>app.current_tenant_id</code> GUC + <code>cyberos_app</code> runtime role; every EVAL table inherits this tenancy + append-only REVOKE pattern. (depends_on)</li><li><strong>TASK-AUTH-101</strong> — the <code>cyberos_founder</code> role claim that <code>may_read</code> reads for the founder-can-read-any path.</li><li><strong>TASK-MEMORY-101</strong> — the L1 audit-chain writer protocol; EVAL reuses <code>services/shared/cyberos-audit-chain::chain_anchor</code> / <code>emit_genesis</code> so its rows verify under memory's reconcile.</li><li><strong><code>l2_memory</code> / <code>l2_entity</code></strong> (memory <code>0001_layer2.sql</code>) — the Layer-2 projections the retention sweeper erases (Layer 1 stays immutable).</li><li><strong>Consumed by TASK-MEMORY-121</strong> (interaction-event schema) and <strong>TASK-MEMORY-122</strong> (capture emitters): both call <code>is_gated</code> before recording a subject. (blocks)</li><li><strong>Consumed by TASK-EVAL-002</strong> (rubric) and <strong>TASK-EVAL-003</strong> (evaluation engine): evaluation runs only for non-gated subjects, reads are <code>guard_evaluation_read</code>-gated, and HITL for consequential outcomes is inherited from clause 11. (blocks TASK-EVAL-002)</li><li><strong>TASK-CUO-204</strong> — the GENIE/dream loop's denylist already protects PII-recall and auth invariants; EVAL governance tables join that protected set (no autonomous modification of a consent gate).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.notice_published\",\n  \"path\": \"eval/&lt;tenant&gt;/eval.notice_published\",\n  \"body\": {\n    \"version\": 2,\n    \"body_sha256\": \"9b0e8c5...\",\n    \"lawful_basis_summary\": \"Decree 13/2023/ND-CP Art.13 + Labor Code 45/2019/QH14\",\n    \"published_by\": \"7e57c0de-...\",\n    \"supersedes_version\": 1\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.evaluation_read\",\n  \"path\": \"eval/&lt;tenant&gt;/eval.evaluation_read\",\n  \"body\": {\n    \"reader_subject_id\": \"mgr-...\",\n    \"target_subject_id\": \"report-...\",\n    \"grant_kind_used\": \"ManagerOf\",\n    \"resource\": \"evaluation/2026-Q2\",\n    \"trace_id\": \"0af...\"\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.capture_gated\",\n  \"path\": \"eval/&lt;tenant&gt;/eval.capture_gated\",\n  \"body\": { \"subject_id\": \"newhire-...\", \"reason\": \"no_ack\", \"would_capture\": \"chat_message\" }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Resolved by Stephen's 2026-06-29 decisions (DEC-2520..2525). Deferred / for counsel:</p>\n<ul><li>The exact notice text and the lawful-basis selection per category MUST be confirmed by Vietnamese counsel before go-live (clause 13, §2 closing note). This task ships the structure; the content is a legal sign-off, not an engineering decision.</li><li>Whether <code>manager_of</code> is sourced from an HR org-chart (TASK-HR-001) or set explicitly via grants — Phase 0 supports explicit grants; the HR-derived auto-grant is a Phase-5 wiring (TASK-EVAL-004) once the org chart exists.</li><li>Cross-tenant founder visibility (a holding-company founder over multiple tenant orgs) — out of scope here; single-tenant founder only. Revisit if CyberSkill operates multiple tenants under one owner.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Capture attempted for un-acked subject</td><td><code>is_gated</code> returns true</td><td>Capture skipped; <code>eval.capture_gated</code> row</td><td>Subject acknowledges current notice</td></tr><tr><td>Notice published with no acks yet</td><td><code>gate_reason</code> = NoAck for all</td><td>Whole tenant gated; nothing captured</td><td>Roll out acknowledgment to staff</td></tr><tr><td>Operator edits a published notice in place</td><td>REVOKE UPDATE on <code>eval_notice</code></td><td>Permission denied</td><td>Publish a new version instead</td></tr><tr><td>Ack submitted for a stale/edited notice hash</td><td><code>notice_sha256</code> mismatch check</td><td>Rejected</td><td>Re-fetch current notice + re-ack</td></tr><tr><td>Stranger reads a colleague's record</td><td><code>may_read</code> returns None</td><td>403; no data returned</td><td>Request a grant from founder/manager</td></tr><tr><td>Cross-person read with audit emit failing</td><td>audit emit errors before data returned</td><td>Read refused (fail-closed)</td><td>Investigate audit pool; retry</td></tr><tr><td>Manager grant left active after role change</td><td>grant has no auto-expiry</td><td>Stale access</td><td>Revoke grant (sets <code>revoked_at</code>)</td></tr><tr><td>Retention sweep deletes an L1 row</td><td>impossible — sweep targets <code>l2_*</code> only</td><td>None</td><td>N/A by design</td></tr><tr><td>Retention sweep runs with no policy for a category</td><td>category not in <code>eval_retention_policy</code></td><td>Rows retained (no policy ⇒ no deletion)</td><td>Operator sets a policy</td></tr><tr><td>Out-of-scope category slips into registry</td><td>CHECK + <code>register_category</code> 422</td><td>Rejected at write</td><td>Use an in-scope category</td></tr><tr><td>DSR erasure auto-applied</td><td>clause 11 forbids; request only sets <code>open</code></td><td>No auto-erase</td><td>Human resolves the request</td></tr><tr><td>Covert mode requested (no notice)</td><td>gate blocks all capture; clause 13 flags</td><td>Nothing captured; legal-risk note</td><td>Counsel review before any such mode</td></tr><tr><td>Tenant A reads tenant B governance</td><td>RLS policy</td><td>0 rows</td><td>None — by design</td></tr><tr><td>Notice bumped, subjects not re-acked</td><td><code>StaleAckVersion</code>; capture pauses</td><td>Capture stops for stale subjects</td><td>Re-acknowledge v(N+1)</td></tr><tr><td>Two managers granted on same target</td><td>both grants valid</td><td>Both may read; both reads audited</td><td>None — by design</td></tr><tr><td>Revoke race (read in flight as revoke commits)</td><td>next read re-checks <code>revoked_at</code></td><td>At most one extra read, audited</td><td>None — bounded</td></tr><tr><td><code>app.current_tenant_id</code> unset on connection</td><td><code>current_setting(..., true)</code> is NULL → RLS yields 0 rows</td><td>Fail-closed (no data)</td><td>Middleware must set the GUC (TASK-AUTH-003)</td></tr><tr><td>Sweeper job fails mid-run</td><td>per-category loop; logged</td><td>Partial sweep; resumes next cron</td><td>Operator inspects logs</td></tr><tr><td>OTel exporter down</td><td>counters buffered then dropped</td><td>Metrics gap only</td><td>Restore TASK-OBS-001</td></tr><tr><td>L2 row missing <code>eval_category</code> frontmatter</td><td>sweep predicate misses it</td><td>Row not swept (retained)</td><td>Backfill category tag at ingest (TASK-MEMORY-122)</td></tr><tr><td>Founder role claim spoofed</td><td>JWT signature verify (TASK-AUTH-004)</td><td>Forged token rejected upstream</td><td>None — auth boundary</td></tr><tr><td>DSR backlog grows unattended</td><td><code>eval_dsr_requests_total</code> + status endpoint open count</td><td>Visible to operator</td><td>Staff resolves the queue</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The acknowledgment gate is the load-bearing clause: <code>is_gated</code> MUST be cheap because every capture emitter calls it per subject. Cache the <code>(subject, current_version)</code> → not-gated result for a short TTL (60s) per worker, invalidated on a notice bump; the un-cached path is two indexed lookups (current notice version, subject's max acked version).</li><li><code>gate_reason</code> distinguishes <code>NoAck</code> from <code>StaleAckVersion</code> only for the metric label and operator clarity; both are \"gated\". The hot path (<code>is_gated</code>) just needs the boolean.</li><li>EVAL reuses <code>services/shared/cyberos-audit-chain</code> rather than re-implementing chain math, so every <code>eval.*</code> row is byte-compatible with memory's reconcile invariant (the same property obs and auth rely on). Each governance event is a genesis row (<code>prev_hash_hex = NULL</code>) — governance events are independent, like obs's.</li><li>The retention sweeper operates on Layer-2 projections (<code>l2_memory</code>, <code>l2_entity</code>) and EVAL artefacts, never on <code>l1_audit_log</code>. This is the deliberate Layer-1-is-source / Layer-2-is-projection split (memory <code>0001_layer2.sql</code>): you bound recall without breaking provability. The erasure event is appended to L1 so the <em>fact</em> of erasure is permanent even though the <em>content</em> is gone.</li><li><code>eval_category</code> tagging on L2 rows is written by the TASK-MEMORY-122 capture emitters (the category each event belongs to). The sweeper reads <code>frontmatter-&gt;&gt;'eval_category'</code>; if a future schema promotes it to a typed column, the predicate moves with it.</li><li>Append-only is enforced by <code>REVOKE UPDATE, DELETE</code> from <code>cyberos_app</code>. The two legitimate updates — <code>eval_access_grant.revoked_at</code> and <code>eval_dsr_request.resolved_*</code> — are performed by the admin role (<code>cyberos_ops</code>), which bypasses the REVOKE; the runtime app role can never mutate a published notice, an ack, or delete a grant/DSR.</li><li><code>may_read</code> is intentionally a pure resolution returning the grant kind, separated from <code>guard_evaluation_read</code> which adds the side-effecting audit emit. This lets read-only callers (e.g. the status endpoint counting who-can-see-whom) resolve access without emitting a read row.</li><li>The founder path is role-based (<code>cyberos_founder</code> from the JWT, TASK-AUTH-101), not a grant row, so the founder never needs a self-issued grant to every subject. Designated managers and HR reviewers are grant rows so they are explicit, revocable, and auditable.</li><li>DSR requests are first-class rows precisely so the data-subject-rights obligation is demonstrable: a subject <em>can</em> object/rectify, the request <em>is</em> recorded, and a <em>human</em> resolves it. Auto-applying erasure was rejected — it could destroy evidence and it removes the human checkpoint the plan requires for anything consequential.</li><li>The notice-bump re-gate (clause 17) is what keeps consent <em>living</em>. A one-time checkbox at hire does not cover a later expansion of monitoring scope; bumping the version pauses capture for everyone whose ack is stale until they acknowledge the new terms. Operators should bump the version only on a <em>material</em> scope change, not for typo fixes (a typo fix that does not change scope can reuse the version if the hash is unchanged; if the text changes at all the hash changes, so in practice any text edit is a new version — keep notice edits deliberate).</li><li>This task is high-risk under the EU AI Act framing (employment-context monitoring feeding AI evaluation), so it carries the <code>## AI Risk Assessment</code> section below and its paired audit verifies the governance-first invariants explicitly. Stephen's sign-off and Vietnamese counsel's confirmation of the notice text are both preconditions to go-live.</li><li>The whole layer is deliberately boring and conservative: it adds gates, registries, audit rows, and retention limits. It does not itself analyse anyone. Analysis is TASK-EVAL-003, and it runs only inside the fence this task builds.</li></ul>\n<hr>\n<h2>AI Risk Assessment</h2>\n<h3>Data sources</h3>\n<p>This layer governs (it does not itself produce) the AI-fed employment analysis. The data it gates is platform work-interactions only — chat messages, module usage, task/project activity, sign-in/presence, document activity — each of which must be a <em>declared category</em> in the registry with a purpose and a lawful basis before it can be collected (clause 4). Out-of-scope sources (keystroke logging, screen capture, microphone/camera, location, private life) are rejected at registration (clause 5). No data is collected for a subject until that subject has acknowledged the current monitoring notice (clause 3). The source of truth is the tamper-evident L1 chain; this layer reads the tenant's notice/ack/grant/registry/retention state to decide what may flow into evaluation.</p>\n<h3>Human oversight</h3>\n<p>Human oversight is built into the layer (EU AI Act Article 14). Anything consequential — an erasure request, an objection, or any downstream change to pay, progression, or employment — is recorded and decided by a person, never auto-applied (clause 11). Reads of another person's evaluation data are restricted to the founder, designated managers, and the subject's own record (clause 7) and every cross-person read leaves an audit row (clause 9). The founder/managers have a governance-status view (clause 18) to confirm the gate is healthy before trusting any evaluation. The only automatic action is scheduled retention deletion, and it acts solely on the policy the operator configured (clause 6).</p>\n<h3>Failure modes</h3>\n<p>The safe state everywhere is \"do not capture, do not disclose\" reached by gating, not by guessing. A subject without a current acknowledgment is gated and not recorded (clause 3); the skip is itself audited. A notice-scope change re-gates everyone with a stale acknowledgment until they re-acknowledge (clause 17). A read without authorisation is denied and returns no data (clause 7), and the read-audit emit is fail-closed (a read that cannot be audited does not return data). An un-set tenant GUC makes RLS yield zero rows (fail-closed). The retention sweeper can only erase Layer-2 projections; it can never touch the Layer-1 chain, so erasure bounds recall without destroying the integrity record (clause 6). Covert collection is structurally prevented by the gate and explicitly flagged for counsel (clause 13).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Claude (Cowork), authoring this task from <code>docs/strategy/cyberos-brain-evaluation-plan.md</code> and Stephen's 2026-06-29 governance decisions (DEC-2520..2525), matching the repo's engineering-spec@1 house style.</li><li>Scope: full draft of this specification — the normative clauses, the governance schema, the access-resolution and retention-sweep sketches, the acceptance criteria, and the AI risk assessment.</li><li>Human review: Stephen reviews and approves before status moves past draft. Because this is the Phase-0 legal/trust gate for the whole BRAIN/EVAL workstream, the notice text and per-category lawful basis additionally require Vietnamese counsel sign-off, and the paired audit plus the CAF gate validate before any implementation merges.</li></ul>\n<hr>\n<h2>Operating mode (founder decision, 2026-06-30): quiet / in-product-silent</h2>\n<p>CyberSkill runs this in a quiet mode: the product shows employees NO monitoring or evaluation surface by default, and access to evaluation data is restricted to the founder and the responsible manager (plus the subject's own record on request). The disclosure that gives this its lawful basis is NOT an in-app banner or click — it is the signed monitoring-and-evaluation clause in the employment documents (see <code>docs/legal/data-monitoring-and-evaluation-notice.md</code>, EN + VN). This constrains the implementation: the per-subject acknowledgment that satisfies the §1 consent gate is recorded with <code>ack_source = 'signed_contract'</code> (the HR-recorded contract acceptance + the notice version), not an in-app acknowledgment, and the in-app notice surface is off by default. The signed-clause disclosure is the non-negotiable floor — there is no mode that captures or evaluates a subject with no acknowledgment row at all. Confirm the clause and lawful basis with Vietnamese counsel before go-live (PDPD Decree 13/2023/ND-CP + Labor Code 45/2019/QH14).</p>\n<hr>\n<p><em>End of TASK-EVAL-001.</em></p>";
