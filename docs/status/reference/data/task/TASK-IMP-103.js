window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-103"]="<h1>TASK-IMP-103: Install concurrency lock</h1>\n<h2>Summary</h2>\n<p><code>install.sh</code> removes the vendored machine and re-copies it with no lock, so a second install - or any reader touching <code>.cyberos/</code> during the window - can observe a half-vendored machine. Guard the vendor step with a <code>mkdir</code> lock carrying the owning pid and a start timestamp, refuse a concurrent install with both pids named, break a demonstrably stale lock with a warning, and release on every exit path.</p>\n<h2>Problem</h2>\n<p><code>tools/install/install.sh:57</code> runs <code>rm -rf \"$CY/cuo\" \"$CY/plugin\" \"$CY/mcp\"</code> and <code>:58</code> re-copies. Between those lines the machine does not exist. <code>grep -c \"install.lock\\|flock\" install.sh</code> returns 0 - there is no lock of any kind. Observed twice on a slow mount during the 2026-07-16 sachviet run as transient missing-file probes; reported as IMP-10 in IMPROVEMENT_HANDOFF.md and then never triaged across five batches, which makes it the only unexecuted finding from the original audit.</p>\n<p>The failure is transient and needs concurrency to bite, which is exactly why it has survived: nothing forces it to happen, and nothing stops it either. Every consumer that adds CI-driven installs, or runs an agent that reads <code>.cyberos/</code> while a human re-installs, reaches it.</p>\n<h2>Proposed Solution</h2>\n<p>Acquire <code>.cyberos/.install.lock</code> by <code>mkdir</code> (atomic on POSIX) before the vendor step, writing <code>pid</code> and <code>started_at</code> into it. On collision, read the lock: if the owning pid is alive on this host, refuse with both pids and the lock's age. If the lock is older than a threshold AND its pid is dead, break it with a warning naming what was broken. Release via <code>trap</code> on EXIT, INT, and TERM so a killed install does not wedge the next one. Same-host liveness only - the pid in a lock written by another machine on a shared mount means nothing, and the code must say so rather than guess.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><code>flock(1)</code>. Rejected: not present on stock macOS, and the payload's shell floor is POSIX + coreutils. <code>mkdir</code> is atomic everywhere we run.</li><li>Copy-then-swap (vendor to a temp dir, rename over). Rejected as the primary fix: <code>rename</code> over a populated directory is not atomic across all platforms, and the change is larger than the defect. Worth revisiting independently.</li><li>Do nothing (accept the window). Rejected: it is the last open finding from the audit that produced this backlog, and the fix is ~10 lines.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a second concurrent install refuses with a non-zero exit and both pids named, while the first completes untouched - suite-asserted. Baseline: today the second install proceeds and both write the same tree.</li><li>Guardrail: a stale lock (dead pid, past threshold) never blocks a legitimate install - it is broken with a warning, and the warning names the lock it broke.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>install.sh</code> lock acquire/refuse/break/release, the uninstall-side removal of a lock it owns, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Locking any operator-owned path. The lock lives inside <code>.cyberos/</code> (the machine we own) and never guards <code>docs/tasks/</code>.</li><li>Cross-host liveness detection on shared mounts - undecidable from a pid; the threshold is the only honest signal there.</li><li>Making the vendor step itself atomic (the copy-then-swap alternative above).</li></ul>\n<h2>Dependencies</h2>\n<p>None upstream. Blocks TASK-IMP-104 (its version check must precede this lock in <code>install.sh</code>) and TASK-IMP-106 (its summary reports this task's lock-removal branch). Ship 103 first; the three are parent-serialised per §11a and MUST NOT run as concurrent swarm members.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md IMP-10 plus a verification of the window on merged main; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>install.sh</code> MUST acquire <code>.cyberos/.install.lock</code> via <code>mkdir</code> before the first destructive vendor operation, and MUST write <code>pid=&lt;pid&gt;</code> and <code>started_at=&lt;iso8601&gt;</code> inside it.</li><li>1.2 On acquire failure where the recorded pid is alive on this host, install MUST refuse with a non-zero exit, naming the holding pid, this pid, and the lock's age. It MUST NOT proceed to the vendor step.</li><li>1.3 On acquire failure where the lock's age exceeds <code>CYBEROS_LOCK_STALE_SECS</code> (default 900) AND its pid is not alive on this host, install MUST break the lock, emit a warning naming the broken lock's pid and age, and continue.</li><li>1.4 A lock whose pid is dead but whose age is under the threshold MUST NOT be broken - it MUST refuse per 1.2, because a just-started install has not yet had time to look alive.</li><li>1.5 The lock MUST be released on EXIT, INT, and TERM via <code>trap</code>, including the refusal path (which MUST NOT release a lock it does not own).</li><li>1.6 <code>uninstall.sh</code> MUST remove <code>.cyberos/.install.lock</code> only when removing the machine, and MUST NOT treat a foreign lock as its own.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2, #1.5) - a second install against a held lock exits non-zero naming both pids; the holder's tree is unmodified - test: <code>tools/install/tests/test_install_lock.sh::t01_concurrent_refuses</code></li><li>[ ] AC 2 (traces_to: #1.3) - a lock older than the threshold with a dead pid is broken with a warning naming pid and age; install completes - test: <code>tools/install/tests/test_install_lock.sh::t02_stale_broken_with_warning</code></li><li>[ ] AC 3 (traces_to: #1.4) - a fresh lock with a dead pid refuses rather than breaking - test: <code>tools/install/tests/test_install_lock.sh::t03_fresh_dead_pid_refuses</code></li><li>[ ] AC 4 (traces_to: #1.5) - a SIGTERM'd install leaves no lock behind; the next install acquires cleanly - test: <code>tools/install/tests/test_install_lock.sh::t04_trap_releases_on_signal</code></li><li>[ ] AC 5 (traces_to: #1.6) - uninstall removes a lock it owns and keeps/names a foreign one - test: <code>tools/install/tests/test_install_lock.sh::t05_uninstall_lock_ownership</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Lock directory exists but is empty (killed between <code>mkdir</code> and the write): no pid to probe, so the threshold alone decides - under it, refuse; over it, break. Covered by t02/t03's shape.</li><li><code>.cyberos/</code> on a shared network mount where the pid belongs to another host: <code>kill -0</code> is meaningless. Treat an unreadable/foreign-host lock as alive until the threshold expires - the same conservative default TASK-IMP-093's lease uses.</li><li>pid reuse: a dead installer's pid recycled by an unrelated process reads as alive, so the lock survives to its threshold and is then broken. Conservative in the safe direction.</li><li>Read-only <code>.cyberos/</code> (permissions damage): <code>mkdir</code> fails for a reason that is not contention. The refusal message MUST distinguish \"held by pid N\" from \"cannot create lock\" or it sends the operator hunting a process that does not exist.</li><li>Non-git or first-ever install where <code>.cyberos/</code> does not exist yet: create the directory before acquiring - the lock cannot guard a path that is absent.</li><li>Security-class: the lock is inside the machine's own directory, contains a pid and a timestamp, and is never read as executable input. No untrusted content, no execution surface.</li></ul>";
