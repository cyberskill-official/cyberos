window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-114"]="<h1>TASK-IMP-114: Cost and cycle telemetry</h1>\n<h2>Summary</h2>\n<p><code>routed_back_count</code> is the only cycle metric and it is never aggregated; there is no wall-time or token accounting anywhere. Add a per-batch economics row to the status page - tasks shipped, route-backs, gate re-asks, wall time, and tokens where the harness exposes them - so \"is this loop worth running\" has a number.</p>\n<h2>Problem</h2>\n<p>This run's real numbers are exactly the data that would have helped before it hurt: two API spend-limit cutoffs, one batch losing three of four swarm agents mid-flight, and five batches whose relative cost nobody can compare. The articles are blunt that the loop depends on this measurement, and we have none of it.</p>\n<h2>Proposed Solution</h2>\n<p>Emit a per-batch row: batch id, tasks shipped, total route-backs, gate re-asks, wall time from first phase commit to final acceptance, and tokens when the harness reports them. Render it on the status page beside the corpus counts. Everything derives from artefacts that already exist except tokens, which is harness-dependent and therefore optional by construction - a metric that requires a specific host is a metric that expires.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Per-model cost estimates. Rejected: harness-specific, priced in dollars that change, and stale the day they are written.</li><li>Per-step timing. Rejected as premature: the batch is the unit an operator decides about.</li><li>A dashboard. Rejected: the status page exists and one row is the whole finding.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: after a batch, the status page shows its economics row with every non-optional field populated from artefacts - suite-asserted. Baseline: no cost or cycle data exists.</li><li>Guardrail: a harness that reports no tokens yields a row with tokens omitted and every other field present - the row degrades, it does not vanish.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the economics row, its derivation from existing artefacts, the status-page render, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Dollar cost estimates or per-model pricing.</li><li>Any budget gate or spend limit - this measures, it does not stop.</li><li>Per-step or per-skill timing (TASK-IMP-113 covers per-skill outcomes).</li></ul>\n<h2>Dependencies</h2>\n<p>None logically. Derives from phase commits, gate logs, and frontmatter.</p>\n<p><strong>Serialisation note:</strong> touches <code>render-status-hub.mjs</code> (shared with TASK-IMP-108) and <code>ship-tasks.md</code> (shared with 108, 109, 113, 115). Parent-serialised per §11a; never concurrent swarm members.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §8 IMP-23; the motivating incidents are this run's own recorded agent cutoffs.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 A per-batch economics row MUST record: batch id, tasks shipped, total route-backs, gate re-asks, and wall time from first phase commit to final acceptance.</li><li>1.2 Tokens MUST be recorded when the harness exposes them and MUST be omitted (not zeroed) when it does not - a zero would assert a fact nobody measured.</li><li>1.3 Every non-optional field MUST derive from an artefact that already exists; this task MUST NOT add a new writer to the phase path.</li><li>1.4 The row MUST render on the status page.</li><li>1.5 The row MUST NOT gate, block, or warn on any threshold.</li><li>1.6 Every rendered field MUST derive deterministically from committed artefacts, so a re-render of an unchanged corpus stays byte-identical per TASK-IMP-082's fp- fingerprint. A field that cannot be derived deterministically (a harness-reported token count that varies per read) MUST be omitted from the RENDERED row rather than rendered as a varying value.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - a fixture batch yields a row with all non-optional fields derived from its artefacts - test: <code>tools/install/tests/test_batch_economics.sh::t01_row_derived</code></li><li>[ ] AC 2 (traces_to: #1.2) - a harness reporting no tokens yields a row with tokens omitted and all else present - test: <code>tools/install/tests/test_batch_economics.sh::t02_tokens_optional</code></li><li>[ ] AC 3 (traces_to: #1.4) - the row renders on the status page - test: <code>tools/docs-site/tests/test_render_status_hub.sh::t09_economics_row</code></li><li>[ ] AC 4 (traces_to: #1.5) - no threshold, gate, or warning exists on any field - verify: recorded grep in the gate log (a negative structural claim; same rationale as TASK-IMP-090 AC 1).</li><li>[ ] AC 5 (traces_to: #1.6) - re-rendering an unchanged corpus is byte-identical, economics row included - test: <code>tools/docs-site/tests/test_render_status_hub.sh::t10_economics_row_deterministic</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A batch cut mid-flight (this run, twice): wall time has no end. The row MUST mark it <code>incomplete</code> rather than compute a duration to now - an unfinished batch has no wall time, and inventing one would be the fabrication the whole corpus guards against.</li><li>A batch of one task: the row is still emitted. Small batches are the comparison baseline.</li><li>Route-backs spanning two batches (a task deferred and resumed later): counted in the batch where the route-back happened, which is where the cost fell.</li><li>A batch with a re-run phase after a sandbox kill: wall time includes it, because it was real time spent.</li><li>Tokens reported by one harness and not another: the rendered page would differ per-author, breaking TASK-IMP-082's byte-stability. 1.6 resolves it - the token field lives in the artefact, never in the rendered row, unless it is itself committed.</li><li>Security-class: reads timestamps, counters, and commit metadata. Nothing executed, nothing interpolated.</li></ul>";
