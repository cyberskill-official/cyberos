window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-143"]="<h1>TASK-IMP-143: 1.4.x stuck-WIP hub + signed HITL</h1>\n<h2>Summary</h2>\n<p>Promote G13's report-only stuck-WIP detector onto the status hub as a visible triage surface (threshold N=30 days, operator decides), and replace honor-system <code>--verdict-by</code> actor strings with content-addressed attributed HITL verdict artifacts that gate flips must mint and bind.</p>\n<h2>Problem</h2>\n<ol><li>G13 (<code>test_benchmark_gates.sh::t_g13</code>) finds in-flight tasks older than 30 days, but only as suite stdout — operators never see it on the status hub they already open.</li><li>TASK-CUO-303 records a human verdict path (<code>--verdict-by</code> + <code>--verdict-evidence</code>), but the actor string is honor-system: any string unlocks the gate. Named Non-Goal of CUO-303; 1.4.x owns attribution.</li></ol>\n<h2>Proposed Solution</h2>\n<ol><li><strong>Stuck-WIP hub sentinel</strong> — <code>render-status-hub.mjs</code> renders a \"Stuck WIP (G13)\" section listing in-flight tasks whose age exceeds N=30 days against a deterministic as-of date (CHANGELOG date for the current VERSION, overridable by <code>CYBEROS_NOW</code> / <code>CYBEROS_HUB_ASOF</code>). Each row links the task id for triage (resume / route_back / on_hold). Report-only: no status mutation. As-of is committed-input-derived so TASK-IMP-082's fp- stamp stays byte-stable without an env pin.</li><li><strong>Attributed verdict artifacts</strong> — on every HITL gate flip, <code>backlog-mutate</code> mints (or validates) a content-addressed verdict JSON under <code>docs/tasks/_verdicts/</code> carrying <code>{schema, actor, timestamp, task_id, from, to, evidence_path, evidence_sha256, artifact_sha256}</code> where <code>artifact_sha256</code> hashes the canonical payload excluding itself. Gate flips refuse (exit 8) when the evidence file is missing/empty (unchanged) <strong>or</strong> when a pre-supplied artifact fails hash/actor/transition binding. The minted path is recorded on the flip JSON envelope and in the <code>status_overridden</code> reason field (evidence path remains the human note; artifact path is additional attribution).</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Cryptographic signatures (ed25519) as the floor.</strong> Deferred: content-addressed attribution closes the honor-system actor gap without key distribution; signatures remain a follow-on.</li><li><strong>Wall-clock ages on the hub.</strong> Rejected: breaks TASK-IMP-082 byte-stable stamps. As-of from VERSION's CHANGELOG date (or env pin in tests) keeps determinism.</li><li><strong>Leave G13 suite-only.</strong> Rejected: the draft and IMP-140 Non-Goal name the hub surface as the 1.4.x deliverable.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: status hub HTML contains a rendered \"Stuck WIP (G13)\" section (or an explicit empty-state line when none are stale) when as-of is pinned; gate flips mint a verifiable <code>_verdicts/*.json</code> artifact whose <code>evidence_sha256</code> matches the evidence file.</li><li>Guardrail: <code>test_hitl_lock.sh</code> and <code>test_render_status_hub.sh</code> stay green; fp- stamp unchanged for an unchanged corpus when as-of is derived from the same CHANGELOG/VERSION inputs.</li></ul>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Status-hub stuck-WIP render + tests.</li><li>Verdict artifact mint/validate helper + backlog-mutate integration + tests.</li><li>Docs: ship-tasks HITL steps, benchmark-gates G13 note, CHANGELOG, batch-9 / Wave 0 #7 close-out.</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Transition-locked state engine (TASK-IMP-144 / 1.5.0).</li><li>Changing which transitions are the two HITL gates.</li><li>Cryptographic signing keys / PKI.</li><li>Making G13 fail the suite (stays report-only).</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-IMP-140, TASK-CUO-303]</code> — both <code>done</code>. Soft: TASK-IMP-082 (stamp), TASK-IMP-108 (staleness render pattern).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) authoring from the batch-9 draft and CUO-303 Non-Goal.</li><li><strong>Scope:</strong> full AC authoring + implementation in this wave.</li><li><strong>Human review:</strong> session operator override — temporary HITL auto-approve with recorded evidence files (2026-07-25).</li></ul>\n<h2>1. Description</h2>\n<ul><li>1.1 The status hub MUST render a Stuck WIP (G13) surface for in-flight statuses (<code>implementing</code>, <code>ready_to_review</code>, <code>reviewing</code>, <code>ready_to_test</code>, <code>testing</code>) whose age in whole days against the hub as-of date exceeds <code>CYBEROS_G13_THRESHOLD_DAYS</code> (default 30). Age source: <code>created_at</code> then <code>created</code> frontmatter; unparseable ages are listed as unparseable, not silently dropped.</li><li>1.2 Hub as-of MUST be deterministic: <code>CYBEROS_NOW</code> or <code>CYBEROS_HUB_ASOF</code> when set; otherwise the dated CHANGELOG heading matching <code>VERSION</code>; otherwise refuse to classify by age and render an empty-state noting as-of unavailable (never read bare <code>Date.now()</code> into the page bytes).</li><li>1.3 Each stale row MUST name task id, status, age days, and the triage hint <code>resume / route_back / on_hold</code>, and MUST be markup outside the JSON payload (same render doctrine as TASK-IMP-108 §1.7).</li><li>1.4 On HITL gate flips (<code>reviewing→ready_to_test</code>, <code>testing→done</code>), after CUO-303 evidence checks pass, the flip MUST mint a verdict artifact under <code>docs/tasks/_verdicts/</code> (creating the directory as needed) whose payload binds actor, timestamp (<code>CYBEROS_NOW</code> when set, else UTC ISO), task_id, from, to, evidence_path (as given), evidence_sha256 (sha256 of evidence file bytes), and artifact_sha256 (sha256 of the canonical JSON without the artifact_sha256 field).</li><li>1.5 If <code>--verdict-artifact &lt;path&gt;</code> is supplied, the flip MUST validate that file's bindings (actor equals <code>--verdict-by</code>, task/from/to match, evidence_sha256 matches the evidence file, artifact_sha256 self-checks) and MUST NOT overwrite it; validation failure refuses exit 8 with no backlog write.</li><li>1.6 The flip JSON envelope MUST include <code>verdict_artifact</code> (path) and <code>evidence_sha256</code>; when a BRAIN store is present, the <code>status_overridden</code> payload <code>reason</code> MUST remain the evidence path (CUO-303 contract) and the artifact path MUST appear in flip stdout/JSON only (no BRAIN schema change required).</li><li>1.7 <code>ship-tasks.md</code> HITL steps and CHANGELOG Unreleased MUST document attributed verdict artifacts; Wave 0 decision #7 is closed in batch docs (no branch ruleset / nothing to scrub).</li></ul>\n<h2>Acceptance Criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1–1.3) — <code>tools/docs-site/tests/test_stuck_wip_hub.sh</code> pins as-of, plants a stale in-flight fixture, asserts rendered \"Stuck WIP (G13)\" markup with triage hint; asserts no <code>Date.now</code> / bare wall-clock path in the age classifier.</li><li>[ ] AC 2 (traces_to: #1.4–1.6) — <code>tools/install/tests/test_verdict_artifact.sh</code> (and extended <code>test_hitl_lock.sh</code>) prove mint on gated flip, hash bind, bad-artifact refusal exit 8, non-gate flips unchanged.</li><li>[ ] AC 3 (traces_to: #1.7) — ship-tasks + CHANGELOG + batch-8/9 Wave 0 #7 close-out mention the artifact path and the closed ruleset residual.</li></ul>\n<h2>Test plan</h2>\n<ol><li><code>bash tools/docs-site/tests/test_stuck_wip_hub.sh</code></li><li><code>bash tools/install/tests/test_verdict_artifact.sh</code></li><li><code>bash tools/install/tests/test_hitl_lock.sh</code></li><li><code>bash .cyberos/cuo/gates/run-gates.sh</code></li></ol>";
