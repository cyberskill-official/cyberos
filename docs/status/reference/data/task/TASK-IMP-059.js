window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-059"]="<h1>TASK-IMP-059: Wiki link-integrity gate</h1>\n<h2>Summary</h2>\n<p>Add a docs gate that fails on broken relative markdown links and missing TASK- cross-refs in frontmatter, so the corpus agents plan from cannot rot silently (deep-audit R51).</p>\n<h2>Problem</h2>\n<p>Task READMEs and specs accumulate dead relative links and <code>depends_on</code> edges pointing at tasks that never existed or were renumbered. Agents treat those paths as real.</p>\n<h2>Proposed Solution</h2>\n<p>Ship <code>tools/install/docs-tools/wiki-link-check.mjs</code> (+ allowlist), wire it into the install test suite and payload vendor copy, and keep the live <code>docs/</code> scan green via allowlist for known historical index debt.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Only warn (non-zero never) — rejected: green theatre.</li><li>Fail on every bare prose <code>TASK-</code> mention — rejected: specs cite future work; scope is links + frontmatter edges.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Suite <code>test_wiki_link_check.sh</code> pass=6 fail=0.</li><li>Live <code>docs/</code> scan exits 0 with committed allowlist.</li></ul>\n<h2>Scope</h2>\n<p>In scope: checker, allowlist, test suite, <code>build.sh</code> vendor lines.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Rewriting every historical module README index (allowlisted; follow-up sweep).</li><li>HTML docs-site link checking.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Composer session agent.</li><li><strong>Human review:</strong> session HITL with Stephen Cheng (session operator).</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 MUST provide <code>wiki-link-check.mjs</code> that scans <code>docs/**/*.md</code> (excluding <code>_archive/</code>), fails on broken relative non-http file links, skips <code>.html</code> targets, and supports an allowlist file.</li><li>1.2 MUST fail when <code>depends_on</code> / <code>blocks</code> / <code>related_tasks</code> frontmatter on <code>spec.md</code> cites a <code>TASK-XXX</code> id with no matching <code>docs/tasks/**/TASK-XXX*</code> folder, unless allowlisted.</li><li>1.3 MUST ship <code>tools/install/tests/test_wiki_link_check.sh</code> covering help, pass/fail fixtures, allowlist, live scan, and build vendor grep.</li><li>1.4 MUST vendor the checker (+ allowlist) from <code>tools/install/build.sh</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC 1 (traces_to: #1.1, #1.2) - checker exits 1 on broken link / missing TASK; 0 when clean</li><li>[x] AC 2 (traces_to: #1.3) - <code>test_wiki_link_check.sh</code> pass=6 fail=0</li><li>[x] AC 3 (traces_to: #1.4) - <code>build.sh</code> copies <code>wiki-link-check.mjs</code> and allowlist</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Pure <code>#anchor</code> links are not file targets.</li><li>Allowlist keys: <code>file -&gt; target</code>, bare <code>TASK-ID</code>, or whole-file path.</li></ul>";
