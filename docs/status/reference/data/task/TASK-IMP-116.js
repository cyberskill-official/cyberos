window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-116"]="<h1>TASK-IMP-116: Every mutation emits the whole file's truth, not just its section's</h1>\n<h2>Summary</h2>\n<p><code>backlog-mutate</code> retallies the section header of the row it touches and leaves the file-top <code>Totals:</code> line alone, by an explicit contract three tests pin. That line is the first number any reader sees, and it has no maintainer: the regenerator that owns it cannot run. Widen the declared mutation from two lines to three, so every write emits the whole file's truth - the completion of TASK-IMP-092's own argument.</p>\n<h2>Problem</h2>\n<p>An external review found <code>Totals: 336 draft, 4 ready_to_implement, 176 done</code> at line 10 of a file whose improvement section alone read <code>67 draft, 9 ready_to_implement, 39 done</code>. The index of truth had a lying header. Consumers reading the header report wrong queue counts.</p>\n<p>TASK-IMP-092 replaced incremental +1/-1 header adjustment with a full retally, and its rationale is decisive: <em>\"incremental adjustment faithfully preserves an inherited lie forever (the 086 incident's 34 vs true 20), while the retally makes every mutation emit the section's truth.\"</em> It then stopped at section headers. The same lie was free to grow one line higher, and it did.</p>\n<p>The line has no owner. <code>regen_backlog</code> writes it and cannot run - it reads <code>docs/improvement/memory/backlog.yaml</code>, retired in the 2026-07-08 migration. So the only mechanism that maintains <code>Totals:</code> is a script that raises FileNotFoundError.</p>\n<h2>Proposed Solution</h2>\n<p><code>retallyTotals(lines)</code>: find <code>^Totals: </code>, count every parseable row in the file by status, rewrite the line in the file's own convention. Called by both <code>flip</code> and <code>insert</code> after the section retally, reported in the result as <code>totals_line</code>. Absent or unparseable Totals returns null and is not an error - a file without the line is legal.</p>\n<p>The contract widens deliberately: a <code>backlog-state-update@2</code> write now declares <strong>one status cell + its section header + the file Totals</strong>. That is the amendment this task exists to make, and it is the reason this is a task rather than an edit: three tests pin the current boundary and the skill's contract states it.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Fix the regenerator instead</strong> (keep the footprint at two lines). Rejected on the operator's call: it leaves the line correct only when someone remembers to regenerate, which is precisely the \"nobody ran it\" failure that let the 086 rows sit wrong. A number maintained by a ritual is a number that rots between rituals.</li><li><strong>Leave it; it is correct on disk today.</strong> Rejected: it is correct by accident (one flip ran before the fix was reverted) and re-rots on the next mutation.</li><li><strong>Move the pins without amending the contract.</strong> Rejected explicitly: t06/t07/t11 encode the byte-discipline boundary of the audited write path. Editing that boundary by rewriting its tests is how a gate quietly stops gating.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: after any flip or insert, <code>Totals:</code> equals an independent count of the file's rows - suite-asserted, and true for every status. Baseline: the line is maintained by nothing and was wrong by 4 ready_to_implement and 4 done when the review found it.</li><li>Guardrail: the mutation stays minimal and auditable - exactly 3 lines change (row, section header, Totals) and never a fourth.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>retallyTotals</code> in backlog-mutate, its wiring into flip + insert, the <code>backlog-state-update@2</code> contract amendment, the three pinned tests.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Repairing <code>regen_backlog</code> (a real defect, its own task - this task makes the mutation self-sufficient, which is what removes the urgency).</li><li>Any change to row grammar, placement, or the uniqueness gate.</li><li>Retallying anything else in the file (a <code>Totals:</code> line is the only global counter; inventing more is not this task's job).</li></ul>\n<h2>Dependencies</h2>\n<p>None. <code>retallyHeader</code>, <code>parseRow</code>, and <code>STATUS_ORDER</code> all exist.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the 2026-07-17 Greptile P1 and the suite's refusal of a first, unamended fix attempt; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> the contract-extension decision is the operator's recorded 2026-07-17 answer; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>backlog-mutate</code> MUST expose <code>retallyTotals(lines)</code>, rewriting the <code>^Totals: </code> line from a full count of EVERY parseable row in the file, in the file's own rendering convention (lifecycle order, zero-count statuses omitted).</li><li>1.2 <code>flip</code> MUST call it after its section retally, and MUST report the rewritten line as <code>totals_line</code> in its result and message.</li><li>1.3 <code>insert</code> MUST call it on the same terms - a stale global total is stale whichever mutation caused it.</li><li>1.4 An absent or unparseable <code>Totals:</code> line MUST return null and MUST NOT be an error: a backlog without the line is legal and MUST NOT be given one.</li><li>1.5 Rows whose status token is outside the enum MUST NOT be counted, matching <code>retallyHeader</code> and <code>regen_backlog</code> - one counting rule, three callers.</li><li>1.6 The declared mutation footprint MUST be exactly three lines (row, section header, Totals) and MUST NOT grow further; <code>backlog-state-update-author</code>'s contract MUST state the new footprint.</li><li>1.7 A mutation whose Totals line is already correct MUST leave it byte-identical and MUST NOT report <code>totals_line</code> - a no-op is not a write.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2) - after a flip, Totals equals an independent count of the file's rows, and the result reports <code>totals_line</code> - test: <code>tools/install/tests/test_workflow_helpers.sh::t06_counts_maintained</code></li><li>[ ] AC 2 (traces_to: #1.3) - after an insert, the same holds - test: <code>tools/install/tests/test_workflow_helpers.sh::t06_counts_maintained</code></li><li>[ ] AC 3 (traces_to: #1.6) - the flip footprint is exactly 3 lines: row + section header + Totals, never a fourth - test: <code>tools/install/tests/test_workflow_helpers.sh::t07_json_and_determinism</code></li><li>[ ] AC 4 (traces_to: #1.4) - a backlog with no Totals line mutates cleanly and gains no Totals line - test: <code>tools/install/tests/test_workflow_helpers.sh::t11_footprint_holds_with_retally</code></li><li>[ ] AC 5 (traces_to: #1.7) - a mutation against an already-correct Totals leaves it byte-identical and reports no <code>totals_line</code> - test: <code>tools/install/tests/test_workflow_helpers.sh::t06_counts_maintained</code></li><li>[ ] AC 6 (traces_to: #1.5) - a row carrying an out-of-enum status is excluded from the Totals count - test: <code>tools/install/tests/test_workflow_helpers.sh::t06_counts_maintained</code></li><li>[ ] AC 7 (traces_to: #1.6) - the skill's contract states the three-line footprint - verify: recorded grep in the gate log (a prose contract; same rationale as TASK-IMP-090 AC 1).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A file with TWO <code>Totals:</code> lines (hand-edited): the FIRST is rewritten and the second left alone - the tool must not guess which one is canonical, and a second is a corpus defect for a human to see, not for a mutation to silently resolve.</li><li>A <code>Totals:</code> line inside a fenced code block (a doc example): <code>^Totals: </code> at column 0 inside a fence would match. Accepted risk, named: BACKLOG.md is a generated index, not prose, and a fence at column 0 there is already a corpus defect. Revisit if a real file trips it.</li><li>A backlog whose rows are all out-of-enum: the tally is empty; per 1.4's shape the line is left alone rather than rendered as <code>Totals: </code> with nothing after it.</li><li>Placeholder-only sections (<code>(none yet)</code>): parse as no row, count nothing - the same semantics <code>retallyHeader</code> already has.</li><li>Concurrency: mutations are serialised by the workflow, and <code>atomicWrite</code> already governs the file. Widening the write from 2 lines to 3 does not change that; the whole file is rewritten either way.</li><li>Security-class: counts rows already in the file and rewrites one line. No untrusted content is executed; no path is taken from input.</li></ul>";
