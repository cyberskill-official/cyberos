window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-086"]="<h1>TASK-IMP-086: Backfill the improvement backlog index rows 068-081 to frontmatter truth</h1>\n<h2>Summary</h2>\n<p>The improvement section of docs/tasks/BACKLOG.md indexes rows through TASK-IMP-067, but the corpus on disk reaches TASK-IMP-081 - fourteen tasks (including shipped ones like 074) have no row at all, and the section header counts predate the gap. Backfill one row per missing task with the status read from its frontmatter at write time, in stem order, matching the section's existing row grammar, and reconcile the header counts against the whole section.</p>\n<h2>Problem</h2>\n<p>Found live during batch-1's insert step: the uniqueness pre-image scan showed rows stopping at 067. An index that silently omits fourteen tasks defeats the queue (eligible work is invisible to ship-tasks' state engine) and makes the header counts fiction. STATUS-REFERENCE §1 names the repair direction:</p>\n<p>&lt;untrusted_content source=\".cyberos/cuo/STATUS-REFERENCE.md\"&gt; docs/tasks/BACKLOG.md is the index the state engine reads and keeps in lockstep with it (on any mismatch, repair the backlog toward frontmatter). &lt;/untrusted_content&gt;</p>\n<h2>Proposed Solution</h2>\n<p>A one-shot, reviewed backfill: read each missing task's frontmatter (id, title, status), emit <code>- [&lt;status&gt;] &lt;STEM&gt; - &lt;title&gt;</code> matching the section's existing untagged row grammar, place rows in stem-ascending order within the contiguous block, and recompute the section header's status counts from every row in the section afterward. No other section is touched; no existing row is edited beyond the header count line.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Full-section regeneration via <code>scripts/migrate_improvement_to_task.py regen_backlog</code> (the byte authority). Preferred if the script runs cleanly against today's corpus - the implementer MUST try it first and fall back to the surgical backfill only if the regenerator's output would churn unrelated rows; either path must satisfy the same ACs.</li><li>Leave it to TASK-IMP-085's insert tool run fourteen times. Rejected as a dependency: 085 ships in the same batch and coupling them serializes the batch; the chore is one review-sized diff either way.</li><li>Do nothing (frontmatter is the truth anyway). Rejected: the queue reads the index; invisible tasks cannot be selected, and wrong counts mislead every human glance.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: index parity - every docs/tasks/improvement/TASK-IMP-* folder has exactly one row and header counts equal the per-status row tally. Baseline: 14 folders unindexed, counts stale. Deadline: this task's final acceptance.</li><li>Guardrail: the diff touches only the improvement section (row insertions plus one header line), proven by the recorded <code>git diff --stat</code> in the gate log.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the fourteen missing rows, the header count line, one recorded parity check.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Other module sections' drift (audit them separately if wanted; this diff stays reviewable).</li><li>Changing row grammar or section layout.</li><li>A permanent repo-wide parity test (would go red on other sections' pre-existing drift; a follow-up once they are reconciled).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None. Cone note: this task owns docs/tasks/BACKLOG.md content during the implementing phase; the parent workflow's own phase flips also write that file but only at phase boundaries, which the batch schedule serializes.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted by the model from the batch-1 run finding; implementation follows under ship-tasks supervision.</li><li><strong>Human review:</strong> PLAN approved by the operator on 2026-07-16; spec audit and both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Every folder matching <code>docs/tasks/improvement/TASK-IMP-*</code> MUST have exactly one row in the improvement section after this task (fourteen backfilled: 068 through 081).</li><li>1.2 Each backfilled row's status cell MUST equal that task's frontmatter <code>status</code> at write time, and its title MUST be the frontmatter <code>title</code> verbatim.</li><li>1.3 Rows MUST use the section's existing grammar (<code>- [&lt;status&gt;] &lt;STEM&gt; - &lt;title&gt;</code>, untagged as the section's corpus rows are) and land in stem-ascending order within the contiguous row block.</li><li>1.4 The section header's counts MUST be recomputed from ALL rows in the section after the backfill and match a per-status tally exactly.</li><li>1.5 The change MUST NOT modify any line outside the improvement section, and MUST NOT edit any pre-existing row other than the header count line.</li><li>1.6 A parity check MUST be recorded in the gate log: folder count equals row count, zero duplicate stems, counts match tally (the three commands and their output).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: §1 #1.1, #1.2, #1.3) - fourteen rows present, statuses and titles from frontmatter, stem-sorted - verify: recorded parity commands in audit.md §gate-log (ops verification: a one-shot content chore; the permanent-test alternative is explicitly out of scope to avoid going red on other sections' pre-existing drift).</li><li>[ ] AC 2 (traces_to: §1 #1.4) - header counts equal the per-status tally - verify: recorded tally command and header line in audit.md §gate-log (same ops rationale).</li><li>[ ] AC 3 (traces_to: §1 #1.5) - diff footprint is rows + one header line inside the improvement section only - verify: recorded <code>git diff --stat</code> and hunk header list in audit.md §gate-log (same ops rationale).</li><li>[ ] AC 4 (traces_to: §1 #1.6) - zero duplicate stems repo-wide for TASK-IMP ids - verify: recorded duplicate-scan command output in audit.md §gate-log (same ops rationale).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A 068-081 task whose frontmatter status is an off-ramp (<code>on_hold</code>, <code>closed</code>): row carries that status; counts include it (AC 2).</li><li>Title containing <code> - </code> (the grammar's own separator): title is emitted verbatim after the first separator; the stem token is what parsers key on (AC 1).</li><li>The regenerator path (preferred alternative) reordering or reformatting pre-existing rows: that output violates §1 #1.5 - fall back to the surgical backfill (AC 3 catches it).</li><li>Folder present but spec.md missing/unparseable: surface it and halt rather than inventing a row (none known today; the guard is stated so the implementer checks).</li><li>Duplicate stem discovered during the pre-image scan: halt and surface (AC 4's scan is also the pre-check).</li><li>Security-class: none - a tracked markdown index edit with recorded evidence; no execution surface.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<p>Duplicated intentionally with <code>## Scope</code> for template conformance: other sections, grammar changes, permanent parity tests.</p>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>Frontmatter remains the record of truth; this repair flows one way, toward the index.</li><li>No row deletion, no row reordering of pre-existing entries.</li><li>HITL: both human-acceptance gates are recorded verdicts; the agent never sets done.</li></ul>\n<p><em>End of TASK-IMP-086.</em></p>";
