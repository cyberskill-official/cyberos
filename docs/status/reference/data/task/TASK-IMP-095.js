window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-095"]="<h1>TASK-IMP-095: gates.env regeneration names its backup and the durable home</h1>\n<h2>Summary</h2>\n<p>Every install regenerates gates.env and silently moves an edited prior copy to a timestamped .bak. The design is fine - config.yaml is the durable override home - but the operator learns that nowhere near the moment their edit vanishes. When the regenerated file differs from the prior one, print one line naming the backup path and pointing at config.yaml.</p>\n<h2>Problem</h2>\n<p>A silent clobber of an operator-edited file is a trust leak even when a backup exists: the sachviet run only found the .bak by listing the directory.</p>\n<h2>Proposed Solution</h2>\n<p>In step 3, after writing the new gates.env: if a prior copy existed and differs (cmp) from the regenerated file, print <code>cyberos install: gates.env regenerated (previous kept at &lt;bak&gt;); durable overrides belong in .cyberos/config.yaml</code>. Identical regeneration stays silent, and the freshly-created case (no prior file) stays silent.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Stop regenerating when edited. Rejected: gates.env is documented as machine-owned (TASK-CUO-207); config.yaml is the override home - changing ownership semantics is a bigger decision than a message.</li><li>Prompt interactively. Rejected: install must stay non-interactive for CI and agents.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: an edited gates.env followed by re-install produces exactly the notice line naming an existing .bak file; unedited re-install produces no notice - suite-asserted every run. Baseline: silence in both cases. Deadline: final acceptance.</li><li>Guardrail: fresh install (no prior file) stays silent (no false notice).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the diff check, the one message line, hygiene scenarios.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Changing regeneration or backup behavior itself.</li><li>Merging operator edits into the regenerated file.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>Shares install.sh with TASK-IMP-094/096 - one agent, serial.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMP-09; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-4 PLAN approved 2026-07-17 (§0a); both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 When a prior gates.env existed and the regenerated file differs from it, install MUST print one notice line naming the exact .bak path and <code>.cyberos/config.yaml</code> as the durable override home.</li><li>1.2 When the regenerated file is byte-identical to the prior one, and when no prior file existed, install MUST NOT print the notice.</li><li>1.3 Coverage MUST land as a hygiene scenario exercising both the edited and unedited paths.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - edited file + re-install -&gt; notice with a real .bak path - test: <code>tools/install/tests/test_install_hygiene.sh::t08_gates_env_regen_notice</code></li><li>[ ] AC 2 (traces_to: #1.2) - unedited re-install and fresh install -&gt; no notice - test: <code>tools/install/tests/test_install_hygiene.sh::t08_gates_env_regen_notice (silent arms)</code></li><li>[ ] AC 3 (traces_to: #1.3) - scenario runs inside the hygiene suite - verify: suite summary counts t08 (glob discovery is the runner's contract; recorded in the gate log).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Two installs in the same second (same .bak timestamp): the message names whichever .bak the step just wrote; the pre-existing <code>rm -f gates.env.bak.*</code> churn guard at line 72 is unchanged.</li><li>Operator edit that regeneration happens to reproduce byte-identically: silent by design (1.2) - nothing was lost.</li><li>Security-class: none - one echo of local paths; no content of the edited file is printed.</li></ul>";
