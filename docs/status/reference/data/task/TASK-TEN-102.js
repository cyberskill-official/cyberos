window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-102"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship the VND domestic billing rail at <code>services/ten/src/billing/vnd/</code> — token-bind / subscription / overage / refund / dunning across VnPay + Momo + ZaloPay, NATS-bridged per-PSP webhook dispatch from TASK-INV-005 + new INV webhook handlers, Decree 123 hóa đơn issuance with eHĐĐT signing + annual gap-free numbering, and 12 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define the closed <code>vnd_psp</code> Postgres enum at migration <code>0018</code>: <code>('vnpay','momo','zalopay')</code>. CI cardinality test asserts 3. Adding a fourth PSP requires schema migration + DEC entry.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vnd_payment_tokens</code> table at migration <code>0018</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, psp vnd_psp NOT NULL, payment_token_kms_blob BYTEA NOT NULL, kms_key_id TEXT NOT NULL, masked_account_hint TEXT, status TEXT NOT NULL CHECK (status IN ('active','revoked','expired')) DEFAULT 'active', bound_at TIMESTAMPTZ NOT NULL DEFAULT now(), revoked_at TIMESTAMPTZ, expires_at TIMESTAMPTZ)</code>. Partial unique <code>(tenant_id) WHERE status='active'</code> — one active token per tenant at slice 2.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vnd_psp_credentials</code> table at migration <code>0019</code>: <code>(id BIGSERIAL PRIMARY KEY, psp vnd_psp NOT NULL, residency residency NOT NULL CHECK (residency='vn-1'), credentials_kms_blob BYTEA NOT NULL, kms_key_id TEXT NOT NULL, status TEXT NOT NULL CHECK (status IN ('active','rotated','revoked')) DEFAULT 'active', created_at TIMESTAMPTZ NOT NULL DEFAULT now(), rotated_at TIMESTAMPTZ)</code>. Partial unique <code>(psp) WHERE status='active'</code> — one active credential per PSP. RLS scoped to vn-1 residency per DEC-980 + task-audit skill §8.1d.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vnd_invoices</code> table at migration <code>0020</code>: <code>(invoice_number TEXT PRIMARY KEY, tenant_id UUID NOT NULL, charge_ref TEXT NOT NULL, issued_at TIMESTAMPTZ NOT NULL DEFAULT now(), pre_tax_amount_vnd BIGINT NOT NULL, vat_amount_vnd BIGINT NOT NULL, total_amount_vnd BIGINT NOT NULL, line_items JSONB NOT NULL, signed_xml_kms_blob BYTEA, ehoadon_tax_authority_ref TEXT, pdf_s3_key TEXT, status TEXT NOT NULL CHECK (status IN ('issued','signed','cancelled')) DEFAULT 'issued')</code>. Append-only per task-audit skill rule 12 (cancellation = new compensating invoice).</li></ol>\n<ol><li><strong>MUST</strong> define <code>vnd_invoice_sequence</code> table at migration <code>0021</code>: <code>(year INT PRIMARY KEY, last_sequence INT NOT NULL DEFAULT 0)</code> for annual gap-free numbering per DEC-983 + Decree 123 §10. Sequence allocation via <code>SELECT ... FOR UPDATE</code> to guarantee no gap, no duplicate.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vnd_event_dispatch_log</code> table at migration <code>0022</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, psp vnd_psp NOT NULL, psp_event_id TEXT NOT NULL, event_type TEXT NOT NULL, dispatch_status TEXT NOT NULL CHECK (dispatch_status IN ('dispatched','duplicate','failed')), dispatched_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE(psp, psp_event_id))</code>. Inbound webhook idempotency per DEC-986. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both USING and WITH CHECK on all 5 VND tables, scoped to <code>tenant_id = current_setting('auth.tenant_id')::uuid</code> AND <code>current_setting('auth.residency') = 'vn-1'</code> (TASK-TEN-103 trip-wire integration).</li></ol>\n<ol><li><strong>MUST</strong> define the <code>VndPsp</code> trait at <code>billing/vnd/psp_trait.rs</code>: ``<code>rust #[async_trait] pub trait VndPsp: Send + Sync { async fn token_bind_url(&amp;self, tenant_id: Uuid, return_url: &amp;str, requested_at: DateTime&lt;Utc&gt;) -&gt; Result&lt;String, VndError&gt;; async fn token_bind_callback(&amp;self, callback_query: &amp;CallbackQuery) -&gt; Result&lt;PaymentToken, VndError&gt;; async fn charge(&amp;self, payment_token: &amp;PaymentToken, amount_vnd: i64, description: &amp;str, idempotency_key: &amp;str) -&gt; Result&lt;ChargeAck, VndError&gt;; async fn refund(&amp;self, original_charge_ref: &amp;str, amount_vnd: i64, idempotency_key: &amp;str) -&gt; Result&lt;RefundAck, VndError&gt;; async fn revoke_token(&amp;self, payment_token: &amp;PaymentToken) -&gt; Result&lt;(), VndError&gt;; fn verify_webhook_signature(&amp;self, headers: &amp;HeaderMap, body: &amp;[u8]) -&gt; Result&lt;(), VndError&gt;; } </code>``</li></ol>\n<p>Implementations: <code>VnPayClient</code>, <code>MomoClient</code>, <code>ZaloPayClient</code>.</p>\n<ol><li><strong>MUST</strong> support the token-bind flow at signup per DEC-963. Flow:</li><li>UI invokes <code>POST /v1/signup/vnd/token-bind-start</code> with <code>{ signup_session_id, psp }</code>.</li><li>Handler resolves the chosen PSP adapter; calls <code>token_bind_url(tenant_id, return_url, ...)</code> to get the PSP-hosted authorisation URL.</li><li>UI redirects user to PSP-hosted page.</li><li>User authorises (SMS-OTP from billing_contact_phone) on PSP page.</li><li>PSP redirects back to <code>/v1/signup/vnd/token-bind-return?session_id=...&amp;signed_params=...</code>.</li><li>Handler calls <code>psp.token_bind_callback(query)</code> to extract + KMS-encrypt the <code>payment_token</code>.</li><li>INSERT into <code>vnd_payment_tokens</code> with status='active'.</li><li>Emit <code>ten.vnd_token_bind_completed</code>. Total signup-flow target: &lt;30s same as TASK-TEN-101 SLO, accounting for ~10s PSP redirect+auth time.</li></ol>\n<ol><li><strong>MUST</strong> charge monthly subscription at billing_cycle_anchor per DEC-964. The <code>billing/vnd/subscription.rs::charge_monthly(tenant_id)</code> job:</li></ol>\n<ul><li>Lookup <code>tenants.billing_currency = 'VND'</code> + active payment_token + current plan_tier.</li><li>Compute VND amount = <code>PRICE_CATALOG.price_for(VND, plan_tier).amount_minor</code>.</li><li>Issue PSP charge via <code>psp.charge(token, amount, description, idempotency_key)</code> with key <code>vnd.&lt;tenant_id&gt;.subscription.&lt;period_ts&gt;</code>.</li><li>PSP responds <code>200 + processing</code>; real outcome arrives via webhook in &lt;5min per DEC-976.</li><li>Emit <code>ten.vnd_subscription_charged</code> on webhook success; <code>ten.vnd_subscription_charge_failed</code> on failure.</li><li>On success: issue hóa đơn (per §1 #16); on failure: advance dunning state.</li></ul>\n<ol><li><strong>MUST</strong> charge overage at period_close per DEC-967. The <code>vnd/overage.rs::charge_for_period(tenant_id, period_end)</code>:</li></ol>\n<ul><li>Compute aggregate overage = sum of (axis_actual - axis_cap) * per_axis_unit_price across 4 axes.</li><li>If aggregate &gt; 0: invoke <code>psp.charge(token, overage_vnd, \"Overage charges for period {period_end}\", idempotency_key)</code> with key <code>vnd.&lt;tenant_id&gt;.overage.&lt;period_end_unix&gt;</code>.</li><li>On success: issue hóa đơn + emit <code>ten.vnd_overage_charged</code>.</li><li>1-hour push window (per TASK-TEN-003 DEC-810); 24h retry deadline.</li></ul>\n<ol><li><strong>MUST</strong> advance dunning per DEC-965. Mirrors TASK-TEN-003 §1 #11:</li></ol>\n<ul><li><code>ok → retry_1 → retry_2 → retry_3 → suspended</code> on consecutive <code>vnd_subscription_charge_failed</code>.</li><li>Recovery: any successful charge → <code>dunning_state='ok'</code> + un-suspend if previously suspended.</li><li>On <code>suspended</code>: trigger TASK-TEN-104 tenant suspension + emit <code>ten.tenant_billing_suspended_vnd</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{id}/vnd/refund</code> for CFO-gated refunds per DEC-966. Body: <code>{ original_charge_ref, amount_vnd, reason }</code>. Validations:</li></ol>\n<ul><li>Caller has <code>cfo</code> role per TASK-AUTH-101.</li><li><code>amount_vnd ≤ original_charge_amount</code>.</li><li><code>tenant.billing_currency == 'VND'</code>.</li><li>Invoke <code>psp.refund(original_charge_ref, amount_vnd, idempotency_key=vnd.&lt;tenant&gt;.refund.&lt;charge_ref&gt;.&lt;amount&gt;)</code>.</li><li>On success: issue compensating hóa đơn (Decree 123 mandates refund invoices) + emit <code>ten.vnd_refund_issued</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{id}/vnd/token/revoke</code> per DEC-985. Caller has <code>tenant_admin</code> role. Handler:</li></ol>\n<ul><li>Lookup active token.</li><li>Call <code>psp.revoke_token(token)</code> (best-effort; PSP-side revoke).</li><li>UPDATE <code>vnd_payment_tokens.status='revoked'</code> + <code>revoked_at=now()</code>.</li><li>Emit <code>ten.vnd_token_revoked</code> sev-2.</li><li>Tenant is now without an active token; next monthly charge fails → dunning → suspension unless new token bound.</li></ul>\n<ol><li><strong>MUST</strong> support per-PSP webhook ingestion via TASK-INV-005 extension (modified_files: <code>services/inv/src/webhook/momo.rs</code> + <code>zalopay.rs</code> + their signature verifiers; VnPay already covered by TASK-INV-005). The INV layer:</li></ol>\n<ul><li>Verifies signature per DEC-971 (VnPay HMAC-SHA512 over query; Momo HMAC-SHA256 over body; ZaloPay HMAC-SHA256 over key1).</li><li>Idempotency dedupe on PSP <code>event_id</code>.</li><li>NATS-publish to <code>tenant.&lt;slug&gt;.ten.vnd.&lt;psp&gt;.&lt;event_type&gt;</code> per DEC-986. The TEN dispatcher (<code>billing/vnd/dispatch.rs</code>) consumes the NATS subject + dispatches into per-event handlers (subscription_charged, charge_failed, refund_completed, token_expired). Dispatch idempotency via <code>vnd_event_dispatch_log</code> UNIQUE <code>(psp, psp_event_id)</code>.</li></ul>\n<ol><li><strong>MUST</strong> issue a Decree 123 hóa đơn for every successful charge per DEC-968 + DEC-972 + DEC-983 + DEC-984. The <code>vnd/hoadon.rs::issue_invoice(tenant_id, charge_ref, total_vnd)</code>:</li></ol>\n<ul><li>Allocate next invoice number from <code>vnd_invoice_sequence</code> (gap-free, annual reset).</li><li>Compute pre_tax = total / 1.10 + vat = total - pre_tax (10% inclusive VAT per VN tax law).</li><li>Generate Decree 123 XML with line items (tenant taxpayer info pre-collected at signup + per-period subscription line + overage lines).</li><li>Sign via VN tax authority's eHĐĐT API; persist signed XML + tax authority ref in <code>vnd_invoices</code>.</li><li>Generate PDF on-demand (S3 cache); store S3 key in row.</li><li>Emit <code>ten.vnd_invoice_issued</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> use annual gap-free invoice numbering per DEC-983 + Decree 123 §10. The <code>hoadon_seq.rs::next_invoice_number(year)</code>:</li></ol>\n<ul><li><code>SELECT last_sequence FROM vnd_invoice_sequence WHERE year=$1 FOR UPDATE</code>.</li><li><code>UPDATE ... SET last_sequence = last_sequence + 1</code>.</li><li>Return <code>format!(\"CYBOS-{:02}{:02}{:02}-{:06}\", year_2_digit, month, day, new_sequence)</code>.</li><li>On rollback (issue_invoice tx fails after sequence allocated): NUMBER IS LOST. This is intentional — gap-free means \"no duplicate\", not \"no skipped\". Skipped numbers are auditable with explanation per Decree 123 §10 (the system logs reason for each skip).</li></ul>\n<ol><li><strong>MUST</strong> thread idempotency keys per DEC-970 + task-audit skill §8.3b. Per-PSP key adapter at <code>billing/vnd/idempotency.rs</code>:</li></ol>\n<ul><li>VnPay: <code>vnp_TxnRef</code> field on request.</li><li>Momo: <code>requestId</code> field.</li><li>ZaloPay: <code>app_trans_id</code> field. Internal canonical key format: <code>vnd.&lt;tenant_id&gt;.&lt;operation&gt;.&lt;period_ts_or_ref&gt;</code>. Adapter maps canonical → per-PSP shape. Stored in <code>vnd_idempotency_cache</code> (additional table — sub-migration into 0018).</li></ul>\n<ol><li><strong>MUST</strong> emit 12 memory audit row kinds per DEC-979 (task-audit skill rule 6 + §8 namespace):</li></ol>\n<ul><li><code>ten.vnd_token_bind_started</code> (sev-3)</li><li><code>ten.vnd_token_bind_completed</code> (sev-2)</li><li><code>ten.vnd_token_bind_failed</code> (sev-2)</li><li><code>ten.vnd_subscription_charged</code> (sev-2)</li><li><code>ten.vnd_subscription_charge_failed</code> (sev-2)</li><li><code>ten.vnd_overage_charged</code> (sev-3)</li><li><code>ten.vnd_refund_issued</code> (sev-1)</li><li><code>ten.vnd_dunning_advanced</code> (sev-1)</li><li><code>ten.tenant_billing_suspended_vnd</code> (sev-1)</li><li><code>ten.vnd_invoice_issued</code> (sev-2)</li><li><code>ten.vnd_token_revoked</code> (sev-2)</li><li><code>ten.vnd_psp_credential_rotated</code> (sev-1) PII-scrubbed via TASK-MEMORY-111: <code>billing_contact_phone_hash16</code>, <code>masked_account_hint_hash16</code>.</li></ul>\n<ol><li><strong>MUST NOT</strong> charge any tenant whose <code>billing_currency != 'VND'</code> via this rail per DEC-973 (symmetric to TASK-TEN-003 §1 #23). Guard at <code>vnd::api_client::call()</code> entry — cross-rail attempts return <code>400 + { error: \"wrong_billing_rail\", expected: \"stripe\", got: \"vnd\" }</code>.</li></ol>\n<ol><li><strong>MUST NOT</strong> charge the founder tenant per DEC-974. Guard mirrors TASK-TEN-003 §1 #22 — emits sev-3 <code>ten.vnd_founder_skip</code> informational row.</li></ol>\n<ol><li><strong>MUST</strong> lock <code>billing_currency</code> immutable post-provisioning per DEC-977 (already enforced by TASK-TEN-003's <code>trg_billing_currency_immutable</code> trigger).</li></ol>\n<ol><li><strong>MUST</strong> require <code>billing_contact_phone</code> at vn-1 signup per DEC-981. TASK-TEN-101's <code>SignupCompleteReq</code> body extended (in this task's modified_files) with optional <code>billing_contact_phone: Option&lt;String&gt;</code>; validation in <code>vnd::token_bind::start</code> requires it for VND tenants (returns 400 + <code>phone_required_for_vnd</code> if missing).</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub all audit rows per task-audit skill rule 18 + DEC-979. Phone hashed as <code>phone_hash16 = HMAC(global_salt, phone_e164_form)</code>; masked account hint (last-4 of bank account) hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread W3C <code>traceparent</code> end-to-end per task-audit skill rule 22-24. Signup → token-bind → callback → INSERT → audit row chain MUST share trace_id.</li></ol>\n<ol><li><strong>MUST</strong> support concurrent token-bind safely per task-audit skill §8.3. Per-tenant <code>SELECT ... FOR UPDATE</code> on <code>vnd_payment_tokens</code> during INSERT; concurrent binds for same tenant return 409 (active token already exists).</li></ol>\n<ol><li><strong>SHOULD</strong> observe per-PSP charge latency p95 via OTel histogram <code>vnd_charge_duration_seconds_by_psp</code> per task-audit skill rule 22. Alarm sev-2 if p95 &gt; 10s sustained 10 min (PSP-side issue).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why three PSPs (§1 #1, DEC-960)?</strong> Vietnamese consumer payment landscape is fragmented. VnPay dominates merchant-side (~60% market share); Momo dominates wallet-based payments (~40% of consumer wallets); ZaloPay is the Zalo-ecosystem default (~30% of social-app users). One PSP loses ~40-70% of potential customers depending on which one. Three covers &gt;95% with reasonable integration cost.</p>\n<p><strong>Why one PSP per tenant at slice 2 (§1 #2 partial-unique, DEC-960)?</strong> Multi-PSP-per-tenant adds combinatorial complexity (which PSP for next monthly charge? failover order? per-PSP credentials?) without immediate commercial benefit (a tenant picks one PSP at signup and rarely switches). Slice 3 adds failover; slice 2 keeps the rail simple.</p>\n<p><strong>Why PSP-native subscription primitives over manual monthly redirects (§1 #10, DEC-962)?</strong> Manual monthly redirects mean the user has to authorise every single month — ~30% drop-off per month per industry benchmarks = catastrophic churn. Token-bind authorises once; subsequent charges are headless. All three PSPs support this primitive (VnPay TokenPay, Momo recurring-token, ZaloPay wallet-bind); leveraging them is the conventional path.</p>\n<p><strong>Why hóa đơn issuance mandatory per charge (§1 #16, DEC-968)?</strong> VN Decree 123 §15 requires electronic invoice for every business-to-business transaction. Non-compliance = tax-authority enforcement + commercial-licence revocation. This is not optional commercial UX; it's legal commercial requirement.</p>\n<p><strong>Why gap-free annual sequence (§1 #17, DEC-983)?</strong> Decree 123 §10 + Circular 78 §3 mandate gap-free invoice numbering for tax authority audit. Gaps indicate hidden transactions = fraud signal. The <code>FOR UPDATE</code> allocation pattern + the documented \"skip = audit log\" approach handles rollback semantics legally.</p>\n<p><strong>Why async PSP responses + Postgres LISTEN/NOTIFY (§1 #10, DEC-976)?</strong> VN PSPs return <code>200 + processing</code> immediately; real outcome arrives via webhook within seconds-to-5min. Polling = wasted load; LISTEN/NOTIFY = real-time wake-up when the webhook handler INSERTs. Same pattern as TASK-INV-005's webhook dispatch.</p>\n<p><strong>Why per-PSP webhook signature variance (§1 #15, DEC-971)?</strong> Each PSP designed its signature scheme independently — VnPay HMAC-SHA512 over query params; Momo HMAC-SHA256 over JSON body; ZaloPay HMAC-SHA256 over key1. We can't normalise the upstream; the adapter pattern (<code>VndPsp::verify_webhook_signature</code>) hides the variance behind a uniform interface.</p>\n<p><strong>Why dunning state machine parallel to TASK-TEN-003 (§1 #12, DEC-965)?</strong> Operational simplicity. Two rails (Stripe + VND) with identical dunning state machines mean operators learn one model. Per-PSP dunning differences would explode the operator-mental-model surface.</p>\n<p><strong>Why per-PSP credentials with rotation (§1 #3, DEC-969)?</strong> Each PSP issues separate API keys. Rotation per PSP is independent (Momo might force-rotate quarterly; VnPay might do annually). Storing them KMS-wrapped + per-PSP table makes rotation independent + auditable.</p>\n<p><strong>Why billing_contact_phone required for VND (§1 #23, DEC-981)?</strong> VN PSPs require SMS-OTP for token authorisation (regulatory + anti-fraud). Without a phone, the token-bind redirect fails at the PSP side — user can't complete signup. Capturing at signup avoids the deadlock.</p>\n<p><strong>Why cross-rail block (§1 #20, DEC-973)?</strong> Tenant <code>billing_currency</code> is locked at provisioning (TASK-TEN-003 DEC-798). A VND tenant attempting Stripe rail = bug, either client-side or server-side. Fail closed at the rail entry; trip-wire catches at schema level too (TASK-TEN-103 §1 #8 cross-residency trigger).</p>\n<p><strong>Why VAT 10% inclusive (§1 #16, DEC-972)?</strong> VN consumer-facing prices conventionally show VAT-inclusive. Showing exclusive is unusual + breaks the user's mental model. Decree 123 mandates line-item breakdown (pre-tax + VAT + total) — the invoice carries the split; the price catalog uses inclusive amounts.</p>\n<p><strong>Why hóa đơn signing via tax authority eHĐĐT (§1 #16, DEC-984)?</strong> Decree 123 §13 requires either (a) sign with tax authority's central service OR (b) sign with our own digital cert pre-registered with tax authority. (a) is operationally simpler + always-up-to-date with regulatory changes; (b) requires our cert lifecycle management. Picked (a) for slice 2; (b) is slice 3 if we hit eHĐĐT availability issues.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema (key migrations)</h3>\n<pre><code class=\"language-sql\">-- 0018_vnd_payment_tokens.sql\nCREATE TYPE vnd_psp AS ENUM ('vnpay','momo','zalopay');\n\nCREATE TABLE vnd_payment_tokens (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  psp vnd_psp NOT NULL,\n  payment_token_kms_blob BYTEA NOT NULL,\n  kms_key_id TEXT NOT NULL,\n  masked_account_hint TEXT,\n  status TEXT NOT NULL DEFAULT 'active'\n    CHECK (status IN ('active','revoked','expired')),\n  bound_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  revoked_at TIMESTAMPTZ,\n  expires_at TIMESTAMPTZ\n);\nCREATE UNIQUE INDEX uniq_active_vnd_token ON vnd_payment_tokens(tenant_id) WHERE status='active';\nALTER TABLE vnd_payment_tokens ENABLE ROW LEVEL SECURITY;\nCREATE POLICY vnd_payment_tokens_rls ON vnd_payment_tokens\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND current_setting('auth.residency') = 'vn-1')\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND current_setting('auth.residency') = 'vn-1');\nREVOKE UPDATE, DELETE ON vnd_payment_tokens FROM cyberos_app;\nGRANT UPDATE (status, revoked_at, expires_at) ON vnd_payment_tokens TO cyberos_app;\n\nCREATE TABLE vnd_idempotency_cache (\n  canonical_key TEXT PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  psp vnd_psp NOT NULL,\n  per_psp_key TEXT NOT NULL,\n  request_sha256 CHAR(64) NOT NULL,\n  response_status INT,\n  response_body_sha256 CHAR(64),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  ttl_until TIMESTAMPTZ NOT NULL DEFAULT now() + INTERVAL '7 days'\n);\n\n-- 0021_vnd_invoice_sequence.sql\nCREATE TABLE vnd_invoice_sequence (\n  year INT PRIMARY KEY,\n  last_sequence INT NOT NULL DEFAULT 0,\n  notes JSONB NOT NULL DEFAULT '[]'::jsonb  -- skipped numbers + reasons per Decree 123 §10\n);\n\n-- 0020_vnd_invoices.sql\nCREATE TABLE vnd_invoices (\n  invoice_number TEXT PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  charge_ref TEXT NOT NULL,\n  issued_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  pre_tax_amount_vnd BIGINT NOT NULL,\n  vat_amount_vnd BIGINT NOT NULL,\n  total_amount_vnd BIGINT NOT NULL,\n  line_items JSONB NOT NULL,\n  signed_xml_kms_blob BYTEA,\n  ehoadon_tax_authority_ref TEXT,\n  pdf_s3_key TEXT,\n  status TEXT NOT NULL DEFAULT 'issued'\n    CHECK (status IN ('issued','signed','cancelled'))\n);\nALTER TABLE vnd_invoices ENABLE ROW LEVEL SECURITY;\nCREATE POLICY vnd_invoices_rls ON vnd_invoices\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND current_setting('auth.residency') = 'vn-1')\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND current_setting('auth.residency') = 'vn-1');\nREVOKE UPDATE, DELETE ON vnd_invoices FROM cyberos_app;\nGRANT UPDATE (signed_xml_kms_blob, ehoadon_tax_authority_ref, pdf_s3_key, status)\n  ON vnd_invoices TO cyberos_app;</code></pre>\n<h3>3.2 Rust types (selected)</h3>\n<pre><code class=\"language-rust\">// services/ten/src/billing/vnd/psp_trait.rs\n#[async_trait]\npub trait VndPsp: Send + Sync {\n    fn psp_kind(&amp;self) -&gt; VndPspKind;\n    async fn token_bind_url(&amp;self, ctx: &amp;TokenBindCtx) -&gt; Result&lt;String, VndError&gt;;\n    async fn token_bind_callback(&amp;self, query: &amp;CallbackQuery) -&gt; Result&lt;PaymentToken, VndError&gt;;\n    async fn charge(&amp;self, token: &amp;PaymentToken, amount_vnd: i64, description: &amp;str, idempotency_key: &amp;str) -&gt; Result&lt;ChargeAck, VndError&gt;;\n    async fn refund(&amp;self, original_charge_ref: &amp;str, amount_vnd: i64, idempotency_key: &amp;str) -&gt; Result&lt;RefundAck, VndError&gt;;\n    async fn revoke_token(&amp;self, token: &amp;PaymentToken) -&gt; Result&lt;(), VndError&gt;;\n    fn verify_webhook_signature(&amp;self, headers: &amp;http::HeaderMap, body: &amp;[u8]) -&gt; Result&lt;(), VndError&gt;;\n}\n\npub struct PaymentToken {\n    pub tenant_id: Uuid,\n    pub psp: VndPspKind,\n    pub raw_token: SecretString,  // KMS-decrypted at use\n    pub masked_account_hint: Option&lt;String&gt;,\n    pub bound_at: DateTime&lt;Utc&gt;,\n    pub expires_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n}\n\npub enum ChargeAck {\n    Processing { psp_request_id: String },  // async resolution via webhook\n    Completed  { psp_charge_ref: String },  // synchronous (rare)\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">POST   /v1/signup/vnd/token-bind-start              (public, signup-session-bound)\nGET    /v1/signup/vnd/token-bind-return             (PSP redirect callback)\nPOST   /v1/admin/tenants/{id}/vnd/token/revoke      (tenant_admin)\nPOST   /v1/admin/tenants/{id}/vnd/refund            (cfo)\nGET    /v1/admin/tenants/{id}/vnd/invoices          (tenant_admin or cfo — list)\nGET    /v1/admin/tenants/{id}/vnd/invoices/{num}    (tenant_admin or cfo — single, PDF download)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>vnd_psp cardinality</strong> — enum = exactly <code>{vnpay, momo, zalopay}</code>.</li><li><strong>Token-bind happy</strong> — start → PSP redirect → return → token persisted KMS-encrypted; <code>ten.vnd_token_bind_completed</code> emitted.</li><li><strong>Token-bind failure</strong> — PSP returns failure code → token NOT persisted + <code>ten.vnd_token_bind_failed</code> emitted.</li><li><strong>Monthly subscription charge</strong> — at billing_cycle_anchor, <code>subscription::charge_monthly(tenant)</code> invokes PSP charge; webhook resolves to success → <code>ten.vnd_subscription_charged</code> + hóa đơn issued.</li><li><strong>Async charge resolution</strong> — PSP responds <code>processing</code>; webhook arrives 30s later → handler unblocks via LISTEN/NOTIFY.</li><li><strong>Overage one-off</strong> — period_close with overage &gt; 0 triggers single VND charge with idempotency_key matching <code>vnd.&lt;tid&gt;.overage.&lt;period_end&gt;</code>.</li><li><strong>Dunning state machine</strong> — three consecutive <code>charge_failed</code> → <code>retry_3</code>; fourth → <code>suspended</code> + TASK-TEN-104 suspension.</li><li><strong>Refund CFO-only</strong> — non-cfo refund attempt returns 403; cfo returns 201 + sev-1 audit.</li><li><strong>Refund amount cap</strong> — refund &gt; original charge returns 400 <code>refund_exceeds_charge</code>.</li><li><strong>Token revoke</strong> — <code>POST /vnd/token/revoke</code> → token.status='revoked' + next charge fails.</li><li><strong>Cross-rail rejection</strong> — USD tenant invoking VND rail returns 400 <code>wrong_billing_rail</code>.</li><li><strong>Founder skip</strong> — founder tenant VND attempt is no-op + emits <code>ten.vnd_founder_skip</code> sev-3 (not in 12-kind core).</li><li><strong>Hóa đơn issuance</strong> — every successful charge produces a <code>vnd_invoices</code> row + signed XML.</li><li><strong>Hóa đơn sequence gap-free</strong> — 1000 concurrent issue requests produce 1000 unique invoice numbers with no duplicates (skipped on rollback IS allowed but logged in <code>notes</code>).</li><li><strong>Per-PSP webhook signature verify</strong> — VnPay HMAC-SHA512 + Momo HMAC-SHA256-body + ZaloPay HMAC-SHA256-key1 all verified correctly; bad sig → 401.</li><li><strong>Idempotency key per PSP</strong> — same canonical_key → same per-PSP key; replay request returns cached response.</li><li><strong>Dispatcher idempotency</strong> — same <code>(psp, psp_event_id)</code> twice → one dispatch + one duplicate log row.</li><li><strong>Residency isolation</strong> — non-vn-1 handler cannot read <code>vnd_payment_tokens</code> (RLS rejects).</li><li><strong>Billing_contact_phone required at vn-1 signup</strong> — VND signup without phone returns 400 <code>phone_required_for_vnd</code>.</li><li><strong>12 memory audit kinds emitted</strong> — happy flow + failure flow produces all 12 kinds across scenarios.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>vnd_psp_enum_cardinality_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn vnd_psp_has_exactly_3_values() {\n    let ctx = TestContext::vn1().await;\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\"SELECT unnest(enum_range(NULL::vnd_psp))::text\")\n        .fetch_all(&amp;ctx.pool).await.unwrap();\n    let mut labels = labels; labels.sort();\n    assert_eq!(labels, vec![\"momo\",\"vnpay\",\"zalopay\"]);\n}</code></pre>\n<h3>5.2 <code>vnd_token_bind_happy_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn vnpay_token_bind_completes_and_persists_kms_wrapped() {\n    let ctx = TestContext::vn1_with_psp(VndPspKind::Vnpay).await;\n    let session = ctx.email_verified_signup_session(\"alice@vn.example\", \"+84901234567\").await;\n    let start = ctx.post(\"/v1/signup/vnd/token-bind-start\").json(&amp;json!({\n        \"signup_session_id\": session, \"psp\": \"vnpay\"\n    })).send().await.unwrap();\n    let body: serde_json::Value = start.json().await.unwrap();\n    let redirect_url = body[\"redirect_url\"].as_str().unwrap();\n    let return_query = ctx.simulate_vnpay_user_auth(redirect_url, \"ok\").await;\n    let r = ctx.get(&amp;format!(\"/v1/signup/vnd/token-bind-return?{}\", return_query)).send().await.unwrap();\n    assert_eq!(r.status(), 200);\n    let row = sqlx::query(\"SELECT psp::text, status, payment_token_kms_blob FROM vnd_payment_tokens WHERE tenant_id=$1\")\n        .bind(ctx.tenant_for_session(session).await)\n        .fetch_one(&amp;ctx.pool).await.unwrap();\n    let blob: Vec&lt;u8&gt; = row.get(\"payment_token_kms_blob\");\n    assert!(blob.len() &gt; 0);\n    assert!(!std::str::from_utf8(&amp;blob).map(|s| s.contains(\"token=\")).unwrap_or(false), \"plaintext leak\");\n    assert_eq!(row.get::&lt;String,_&gt;(\"status\"), \"active\");\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.vnd_token_bind_completed\"));\n}</code></pre>\n<h3>5.3 <code>vnd_subscription_charge_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn monthly_charge_emits_audit_and_invoice() {\n    let ctx = TestContext::vn1_with_psp(VndPspKind::Momo).await;\n    let tenant = ctx.provision_vnd_tenant_with_token(\"acme-vn\", \"+84901234567\").await;\n    let _ = ctx.run_subscription_charge(tenant, PlanTier::Team).await;\n    ctx.simulate_psp_webhook_success(tenant, VndPspKind::Momo).await;\n\n    let charges: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT kind FROM memory_rows WHERE tenant_id=$1 AND kind LIKE 'ten.vnd_%'\"\n    ).bind(tenant).fetch_all(&amp;ctx.pool).await.unwrap();\n    assert!(charges.iter().any(|k| k == \"ten.vnd_subscription_charged\"));\n    assert!(charges.iter().any(|k| k == \"ten.vnd_invoice_issued\"));\n\n    let invoice_count: i64 = sqlx::query_scalar(\n        \"SELECT count(*) FROM vnd_invoices WHERE tenant_id=$1\"\n    ).bind(tenant).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(invoice_count, 1);\n}</code></pre>\n<h3>5.4 <code>vnd_async_charge_resolution_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn async_charge_resolves_via_listen_notify() {\n    let ctx = TestContext::vn1_with_psp(VndPspKind::Zalopay).await;\n    let tenant = ctx.provision_vnd_tenant_with_token(\"zlp-tenant\", \"+84901234567\").await;\n    let charge_fut = tokio::spawn(ctx.charge_with_wait(tenant));\n    tokio::time::sleep(Duration::from_millis(500)).await;\n    ctx.simulate_psp_webhook_success(tenant, VndPspKind::Zalopay).await;\n    let result = charge_fut.await.unwrap();\n    assert!(result.is_ok());\n}</code></pre>\n<h3>5.5 <code>vnd_dunning_state_machine_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn vnd_dunning_advances_then_suspends() {\n    let ctx = TestContext::vn1_with_psp(VndPspKind::Vnpay).await;\n    let tenant = ctx.provision_vnd_tenant_with_token(\"dunning-test\", \"+84901234567\").await;\n    for expected in &amp;[DunningState::Retry1, DunningState::Retry2, DunningState::Retry3] {\n        ctx.simulate_psp_webhook_failure(tenant, VndPspKind::Vnpay).await;\n        assert_eq!(ctx.load_dunning(tenant).await, *expected);\n    }\n    ctx.simulate_psp_webhook_failure(tenant, VndPspKind::Vnpay).await;\n    assert_eq!(ctx.load_dunning(tenant).await, DunningState::Suspended);\n\n    let status: String = sqlx::query_scalar(\"SELECT status::text FROM tenants WHERE id=$1\")\n        .bind(tenant).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(status, \"suspended\");\n}</code></pre>\n<h3>5.6 <code>vnd_hoadon_seq_gap_free_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn invoice_sequence_no_duplicates_under_concurrency() {\n    let ctx = TestContext::vn1_with_psp(VndPspKind::Vnpay).await;\n    let tenants: Vec&lt;_&gt; = (0..100).map(|i| async move {\n        ctx.provision_vnd_tenant(&amp;format!(\"conc-{i}\"), \"+84901234567\").await\n    }).collect::&lt;futures::stream::FuturesUnordered&lt;_&gt;&gt;().collect::&lt;Vec&lt;_&gt;&gt;().await;\n    let invoices = futures::future::join_all(tenants.into_iter().map(|t| ctx.issue_invoice(t, 100_000))).await;\n    let unique: std::collections::HashSet&lt;_&gt; = invoices.iter().filter_map(|r| r.as_ref().ok()).cloned().collect();\n    assert_eq!(unique.len(), invoices.iter().filter(|r| r.is_ok()).count(), \"duplicate invoice number issued\");\n}</code></pre>\n<h3>5.7 <code>vnd_refund_cfo_only_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn vnd_refund_requires_cfo_role() {\n    let ctx = TestContext::vn1_with_psp(VndPspKind::Momo).await;\n    let tenant = ctx.provision_vnd_tenant_with_charge(\"refund-test\").await;\n    let admin_token = ctx.mint_jwt(tenant, \"tenant_admin\");\n    let r = ctx.post(&amp;format!(\"/v1/admin/tenants/{tenant}/vnd/refund\")).bearer_auth(admin_token)\n        .json(&amp;json!({\"original_charge_ref\": \"ch_1\", \"amount_vnd\": 100_000, \"reason\": \"duplicate\"}))\n        .send().await.unwrap();\n    assert_eq!(r.status(), 403);\n\n    let cfo_token = ctx.mint_jwt(tenant, \"chief-financial-officer\");\n    let r = ctx.post(&amp;format!(\"/v1/admin/tenants/{tenant}/vnd/refund\")).bearer_auth(cfo_token)\n        .json(&amp;json!({\"original_charge_ref\": \"ch_1\", \"amount_vnd\": 100_000, \"reason\": \"duplicate\"}))\n        .send().await.unwrap();\n    assert_eq!(r.status(), 201);\n}</code></pre>\n<h3>5.8 <code>vnd_cross_rail_rejection_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn usd_tenant_blocked_from_vnd_rail() {\n    let ctx = TestContext::vn1().await;\n    let us_tenant = ctx.provision_us_tenant(\"usd-tenant\").await;\n    let r = ctx.attempt_vnd_charge(us_tenant).await;\n    assert!(matches!(r, Err(VndError::WrongBillingRail { .. })));\n}</code></pre>\n<h3>5.9 <code>vnd_psp_signature_verify_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn vnpay_signature_validates() {\n    let secret = b\"vnp_secret_key\";\n    let body = b\"vnp_Amount=10000&amp;vnp_TxnRef=abc\";\n    let mut mac = Hmac::&lt;Sha512&gt;::new_from_slice(secret).unwrap();\n    mac.update(body);\n    let sig = hex::encode(mac.finalize().into_bytes());\n    let mut headers = HeaderMap::new();\n    headers.insert(\"vnp-SecureHash\", sig.parse().unwrap());\n    let vnp = VnPayClient::with_secret(secret);\n    assert!(vnp.verify_webhook_signature(&amp;headers, body).is_ok());\n\n    // tampered body\n    let bad_body = b\"vnp_Amount=999999&amp;vnp_TxnRef=abc\";\n    assert!(vnp.verify_webhook_signature(&amp;headers, bad_body).is_err());\n}</code></pre>\n<h3>5.10 <code>vnd_residency_isolation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn non_vn1_handler_cannot_read_vnd_tables() {\n    let ctx = TestContext::with_all_residencies().await;\n    let vn_tenant = ctx.provision_vnd_tenant(\"acme-vn\", \"+84901234567\").await;\n    let eu_handler = ctx.handler_ctx_in(Residency::Eu1);\n    let rows: Vec&lt;(String,)&gt; = sqlx::query_as(\"SELECT id::text FROM vnd_payment_tokens WHERE tenant_id=$1\")\n        .bind(vn_tenant).fetch_all(eu_handler.pool()).await.unwrap_or_default();\n    assert_eq!(rows.len(), 0, \"RLS should block cross-residency read\");\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract in §3 is the skeleton; selected helpers below.)</p>\n<h3>6.1 Hóa đơn sequence allocation (gap-free)</h3>\n<pre><code class=\"language-rust\">// services/ten/src/billing/vnd/hoadon_seq.rs\npub async fn next_invoice_number(tx: &amp;mut PgTx&lt;'_&gt;, year: i32, occurred_on: NaiveDate) -&gt; Result&lt;String, HoaDonError&gt; {\n    let row = sqlx::query(\"SELECT last_sequence FROM vnd_invoice_sequence WHERE year=$1 FOR UPDATE\")\n        .bind(year).fetch_optional(&amp;mut **tx).await?;\n    let next = row.map(|r| r.get::&lt;i32,_&gt;(0) + 1).unwrap_or(1);\n    sqlx::query(\n        \"INSERT INTO vnd_invoice_sequence (year, last_sequence) VALUES ($1, $2)\n         ON CONFLICT (year) DO UPDATE SET last_sequence = $2\"\n    ).bind(year).bind(next).execute(&amp;mut **tx).await?;\n    Ok(format!(\n        \"CYBOS-{:02}{:02}{:02}-{:06}\",\n        year % 100, occurred_on.month(), occurred_on.day(), next\n    ))\n}</code></pre>\n<h3>6.2 Async charge resolution via LISTEN/NOTIFY</h3>\n<pre><code class=\"language-rust\">pub async fn charge_with_wait(ctx: &amp;AppCtx, tenant_id: Uuid, plan: PlanTier) -&gt; Result&lt;(), VndError&gt; {\n    let mut listener = sqlx::postgres::PgListener::connect_with(&amp;ctx.pool).await?;\n    listener.listen(&amp;format!(\"vnd_charge_{tenant_id}\")).await?;\n    let psp = ctx.vnd_psp_for(tenant_id).await?;\n    let ack = psp.charge(/* ... */).await?;\n    match ack {\n        ChargeAck::Completed { .. } =&gt; Ok(()),\n        ChargeAck::Processing { .. } =&gt; {\n            tokio::time::timeout(Duration::from_secs(300), listener.recv()).await\n                .map_err(|_| VndError::PspTimeout)?\n                .map_err(VndError::ListenerError)?;\n            Ok(())\n        }\n    }\n}</code></pre>\n<h3>6.3 NATS dispatcher</h3>\n<pre><code class=\"language-rust\">pub async fn run_dispatcher(ctx: AppCtx) {\n    let sub = ctx.nats.subscribe(\"tenant.*.ten.vnd.&gt;\").await.unwrap();\n    while let Some(msg) = sub.next().await {\n        let event: VndWebhookEvent = serde_json::from_slice(&amp;msg.payload).unwrap();\n        let dispatched = ctx.repo.vnd_dispatch_log.upsert_idempotent(event.psp, event.psp_event_id).await;\n        if !dispatched.is_new { continue; }\n        match event.event_type.as_str() {\n            \"subscription_charge_completed\" =&gt; handle_charge_completed(&amp;ctx, event).await,\n            \"subscription_charge_failed\"    =&gt; handle_charge_failed(&amp;ctx, event).await,\n            \"refund_completed\"              =&gt; handle_refund_completed(&amp;ctx, event).await,\n            \"token_expired\"                 =&gt; handle_token_expired(&amp;ctx, event).await,\n            _ =&gt; {}\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>TASK-TEN-003</strong> Stripe billing — provides billing-rail abstraction this task consumes (rail trait pattern + plan_change push hook + dunning state machine pattern + refund flow pattern); founder-skip + cross-rail block guards consumed.</li><li><strong>TASK-INV-005</strong> VietQR webhook — VnPay webhook handler shape consumed; extended for Momo + ZaloPay variants.</li></ul>\n<p><strong>Cross-module (related_tasks):</strong></p>\n<ul><li><strong>TASK-TEN-001</strong> Provisioning — billing_contact_phone capture at vn-1 signup.</li><li><strong>TASK-TEN-002</strong> Plan tiers — VND prices in catalog (PRICE_CATALOG VND entries).</li><li><strong>TASK-TEN-004</strong> 4-axis metering — period_close hook invokes vnd::overage::charge_for_period.</li><li><strong>TASK-TEN-101</strong> Self-serve signup — VND path now functional (TASK-TEN-101 §1 #13's 503 placeholder resolved).</li><li><strong>TASK-TEN-103</strong> Residency provisioning — vn-1 routing for PSP credentials + KMS.</li><li><strong>TASK-TEN-104</strong> Lifecycle — dunning advances trigger suspension.</li><li><strong>TASK-INV-006</strong> Cash application — VND receipts reconcile against subscription charges.</li><li><strong>TASK-AUTH-101</strong> RBAC — cfo + tenant_admin role gates.</li><li><strong>TASK-AI-003</strong> memory audit — 12 new kinds.</li><li><strong>TASK-MEMORY-111</strong> PII scrubbing — phone + masked account hashes.</li><li><strong>TASK-OBS-007</strong> Auto-runbook — sev-1 dunning + signature-failure alerts.</li></ul>\n<p><strong>Downstream (blocks):</strong> None at slice 2.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>ten.vnd_subscription_charged</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.vnd_subscription_charged\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.ten.vnd\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T03:14:32.847Z\",\n  \"payload\": {\n    \"psp\": \"momo\",\n    \"amount_vnd\": 1_290_000,\n    \"period_start\": \"2026-04-17T17:00:00Z\",\n    \"period_end\": \"2026-05-17T17:00:00Z\",\n    \"psp_charge_ref\": \"MOMO_TXN_3OabcXYZ\",\n    \"invoice_number\": \"CYBOS-260517-000042\",\n    \"billing_contact_phone_hash16\": \"9c4e7a8b6d2f1e3a\"\n  }\n}</code></pre>\n<h3>8.2 Token-bind start request/response</h3>\n<pre><code class=\"language-json\">// POST /v1/signup/vnd/token-bind-start\n{ \"signup_session_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\", \"psp\": \"vnpay\" }\n\n// response\n{ \"redirect_url\": \"https://sandbox.vnpayment.vn/paymentv2/vpcpay.html?vnp_Amount=10000&amp;vnp_TxnRef=...\",\n  \"expires_at\": \"2026-05-17T03:24:32.847Z\" }</code></pre>\n<h3>8.3 Hóa đơn (Decree 123 line items)</h3>\n<pre><code class=\"language-json\">{\n  \"invoice_number\": \"CYBOS-260517-000042\",\n  \"tenant_id\": \"8a2f...\",\n  \"issued_at\": \"2026-05-17T03:14:35.221Z\",\n  \"taxpayer\": { \"tax_id\": \"0312345678\", \"name\": \"ACME Vietnam JSC\", \"address\": \"...\" },\n  \"line_items\": [\n    { \"description\": \"Cyberos Team plan - billing period 2026-04-17 → 2026-05-17\",\n      \"quantity\": 1, \"pre_tax_amount_vnd\": 1_172_727, \"vat_rate\": 0.10, \"vat_amount_vnd\": 117_273, \"total_vnd\": 1_290_000 }\n  ],\n  \"totals\": { \"pre_tax_vnd\": 1_172_727, \"vat_vnd\": 117_273, \"total_vnd\": 1_290_000 },\n  \"ehoadon_tax_authority_ref\": \"GDT-2026-000123456\",\n  \"signed_xml_sha256\": \"9c4e7a8b6d2f1e3a...\"\n}</code></pre>\n<h3>8.4 <code>ten.vnd_refund_issued</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.vnd_refund_issued\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.cfo.456\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"psp\": \"vnpay\",\n    \"original_charge_ref\": \"VNP_TXN_3OabcXYZ\",\n    \"refund_amount_vnd\": 645_000,\n    \"compensating_invoice_number\": \"CYBOS-260517-000043\",\n    \"reason\": \"duplicate_charge\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 2. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-PSP failover within a single tenant — slice 3, task-TEN-2xx (placeholder).</li><li><strong>Deferred:</strong> PSP-side card whitelabeling (custom branding at PSP redirect) — slice 3.</li><li><strong>Deferred:</strong> Annual billing cycle for VND — slice 3 (monthly only at slice 2, mirrors TASK-TEN-003).</li><li><strong>Deferred:</strong> Coupon / promo code support — slice 3.</li><li><strong>Deferred:</strong> Hóa đơn cancellation flow (Decree 123 §17 conditions) — slice 3, task-TEN-2xx.</li><li><strong>Deferred:</strong> Bulk hóa đơn export to tax authority's quarterly upload format — slice 3.</li><li><strong>Deferred:</strong> Multi-PSP-per-tenant for redundancy — slice 3.</li><li><strong>Deferred:</strong> Recurring-charge advance-notice email (T-3 days reminder) — slice 3, task-EMAIL-1xx.</li><li><strong>Deferred:</strong> Per-PSP availability monitoring + auto-failover — slice 3, task-OBS-2xx.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>PSP token-bind redirect timeout</td><td>10-min TTL on token-bind session</td><td>Bind session expired; user re-clicks \"Sign up with VnPay/Momo/ZaloPay\"</td><td>Re-initiate; idempotent</td></tr><tr><td>PSP returns failure at callback (user cancelled SMS-OTP)</td><td><code>psp.token_bind_callback</code> returns Err</td><td>200 to UI with <code>{ status: \"user_cancelled\" }</code>; signup session remains email_verified for retry</td><td>User retries token-bind</td></tr><tr><td>Webhook signature invalid</td><td>HMAC verify fails</td><td>401 + <code>signature_invalid</code> + <code>inv.vnd_event_rejected</code></td><td>PSP-side rotated credential without our update; ops triages</td></tr><tr><td>Webhook replay (same psp_event_id)</td><td>dispatcher <code>(psp, event_id)</code> UNIQUE</td><td>200 + duplicate log row + no side effect</td><td>Inherent — idempotency</td></tr><tr><td>PSP charge API 5xx</td><td>api_client retries 3x with backoff</td><td>Charge marked failed → dunning advances</td><td>Stripe-style auto-retry; persistent → sev-2 alert</td></tr><tr><td>PSP charge timeout (&gt;5min wait)</td><td>LISTEN/NOTIFY timeout fires</td><td>Charge marked <code>processing_timeout</code>; webhook may arrive later → reconciliation</td><td>Reconciliation sweep runs hourly; manual review on persistent timeout</td></tr><tr><td>Hóa đơn signing service down (eHĐĐT API unavailable)</td><td>sign_invoice returns Err</td><td>Invoice persisted unsigned (status='issued', signed_xml NULL); retry job re-signs every 5min</td><td>eHĐĐT recovery; max 24h before sev-1 alert (Decree 123 §13 grace)</td></tr><tr><td>Hóa đơn sequence number duplicate (concurrent INSERT)</td><td>partial unique on invoice_number</td><td>Second INSERT fails; caller retries with next number</td><td>Inherent — <code>FOR UPDATE</code> + retry on conflict</td></tr><tr><td>Invoice sequence rollback (tx fails post-allocation)</td><td>sequence number \"skipped\"</td><td>Skipped number recorded in <code>vnd_invoice_sequence.notes</code> with reason</td><td>Auditable per Decree 123 §10</td></tr><tr><td>Cross-rail attempt (USD tenant invoking VND)</td><td>api_client guard</td><td>400 + <code>wrong_billing_rail</code> + <code>ten.vnd_cross_rail_rejected</code> sev-2</td><td>Handler bug investigation</td></tr><tr><td>Founder tenant VND attempt</td><td>api_client guard</td><td>No-op + sev-3 <code>ten.vnd_founder_skip</code></td><td>Inherent guard</td></tr><tr><td>Tenant has no active payment_token at monthly charge time</td><td>charge_monthly preflight</td><td>charge skipped + <code>ten.vnd_subscription_charge_failed</code> reason='no_token' + dunning advances</td><td>Tenant re-binds token via signup-style flow OR support intervention</td></tr><tr><td>PSP credential rotation overlap expired before rollout</td><td>api_client uses old credential</td><td>401 from PSP; sev-1 alert</td><td>Tenant_admin re-rotates; usual fix</td></tr><tr><td>KMS unavailable when decrypting payment_token</td><td>KMS timeout</td><td>charge skipped + sev-1 <code>ten.vnd_kms_unavailable</code></td><td>AWS KMS recovery; charge retried</td></tr><tr><td>Webhook arrives before charge_monthly job completes</td><td>LISTEN/NOTIFY race</td><td>Webhook handler runs first; charge_monthly's wait returns immediately on next NOTIFY</td><td>Inherent — atomic on (tenant, period)</td></tr><tr><td>Tenant phone number invalid / OTP undeliverable</td><td>PSP returns at token-bind start</td><td>400 + <code>phone_invalid</code>; user updates phone</td><td>User edits phone in signup flow</td></tr><tr><td>Cancellation invoice references a non-existent original</td><td>hoadon.cancel preflight</td><td>400 + <code>original_invoice_not_found</code></td><td>Manual review</td></tr><tr><td>RLS bypassed (residency drift detected)</td><td>TASK-TEN-103 trip-wire</td><td>INSERT/UPDATE blocked; sev-1 <code>ten.cross_residency_write_blocked</code></td><td>Inherent — defense-in-depth</td></tr><tr><td><code>vnd_invoice_sequence</code> for new year not yet seeded</td><td>year row missing</td><td>First-of-year INSERT triggers seed (Jan 1 02:00 UTC scheduled job)</td><td>Inherent — seed job</td></tr><tr><td>eHĐĐT signing succeeds but DB write fails</td><td>Postgres write error post-sign</td><td>Signed XML temporarily orphaned in eHĐĐT; sev-1 alert; reconciliation job re-links</td><td>Reconciliation runs nightly + matches eHĐĐT refs</td></tr><tr><td>Per-PSP credential leaked (suspicious activity)</td><td>OBS anomaly detection</td><td>Sev-1 alert; tenant_admin rotates immediately</td><td>Standard incident response</td></tr><tr><td>Hóa đơn line item amounts don't sum to total</td><td>pre_save validation</td><td>Reject + sev-2 log; charge proceeds without hóa đơn (illegal — operator must fix manually)</td><td>Operator validates + reissues</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> PSP adapters are independently versioned crates inside <code>services/ten/src/billing/vnd/</code> — bumping VnPay's API version doesn't touch Momo/ZaloPay.</p>\n<p><strong>§11.2</strong> Per-PSP API endpoints are loaded from <code>services/ten/src/billing/vnd/psp_endpoints.yaml</code> (environment-pinned URLs); test/sandbox URLs distinct from prod.</p>\n<p><strong>§11.3</strong> Hóa đơn line-item format follows Circular 78/2021/TT-BTC field-naming convention; XSD validation included in <code>hoadon.rs</code> before signing.</p>\n<p><strong>§11.4</strong> The <code>vnd_invoice_sequence.notes</code> JSONB stores skipped-number reasons per Decree 123 §10 (\"tx rolled back due to KMS unavailable at 2026-05-17T03:14\"); auditor can reconstruct gap rationale.</p>\n<p><strong>§11.5</strong> Per-PSP idempotency_key encoding constraints: VnPay <code>vnp_TxnRef</code> max 100 chars; Momo <code>requestId</code> max 50; ZaloPay <code>app_trans_id</code> format <code>yyMMdd_&lt;num&gt;</code> max 40. Adapter SHA-1-shortens canonical key when needed; collision risk negligible (~2^60 entropy).</p>\n<p><strong>§11.6</strong> Webhook signature verification timing-safe via <code>subtle::ConstantTimeEq</code> to prevent timing attacks.</p>\n<p><strong>§11.7</strong> Per-PSP test environments use sandbox accounts; integration smoke tests run nightly (not in PR CI) to validate signature + recurring-charge end-to-end.</p>\n<p><strong>§11.8</strong> Token expiration handling: VnPay tokens are 365d default; Momo 180d; ZaloPay 365d. Expiry monitor job emails tenant_admin T-30 days; auto-suspends at T+1 if no rebind.</p>\n<p><strong>§11.9</strong> SBV Circular 39/2014 PSP regulations: all 3 chosen PSPs are SBV-licensed e-payment providers; CyberSkill's merchant account requires SBV registration (Stephen handles this commercial workstream out-of-band).</p>\n<p><strong>§11.10</strong> The dunning state machine reuses TASK-TEN-003's <code>DunningState</code> enum (Ok/Retry1/Retry2/Retry3/Suspended); same advancement semantics; same un-suspend on success.</p>\n<p><strong>§11.11</strong> Per-PSP error mapping into our <code>VndError</code> enum: PSP-specific error codes documented in adapter source files; uniform <code>VndError::PspError { code, message, retryable }</code> shape consumed by upstream handlers.</p>\n<p><strong>§11.12</strong> The trace_id thread: signup_session → token_bind → PSP redirect (state param carries trace_id) → callback → INSERT → audit row. PSP-side may not preserve trace_id across redirect; the callback handler RE-INSTATES the original trace_id from server-side session lookup.</p>\n<p><strong>§11.13</strong> The <code>vnd_invoice_sequence</code> annual-reset is at Vietnam timezone (UTC+7) midnight, not UTC; matches tax authority convention.</p>\n<p><strong>§11.14</strong> Cross-PSP charge attempts (e.g., tenant bound to VnPay token but operator tries Momo) are prevented by <code>vnd_payment_tokens</code> query — there's only one active token per tenant, and its <code>psp</code> column dictates which adapter to invoke.</p>\n<p><strong>§11.15</strong> The 7-day idempotency cache TTL (mirror of TASK-TEN-003 DEC-807) — Stripe's idempotency window is 24h, VND PSPs vary (some don't enforce window). 7d is forensically generous + matches existing pattern.</p>\n<p><strong>§11.16</strong> Phone number canonical form for hashing: E.164 (<code>+84901234567</code>); user inputs locale-form (<code>0901 234 567</code>) normalised at signup.</p>\n<p><strong>§11.17</strong> The <code>description</code> field on PSP charge requests is user-visible on bank statement / wallet — <code>\"Cyberos Team plan - 2026-05\"</code> chosen for brand recognition + period clarity.</p>\n<p><strong>§11.18</strong> Refund hóa đơn (compensating invoice per Decree 123) carries negative line items + references original invoice via <code>original_invoice_ref</code> column (added in slice 3); slice 2 stores via JSONB free-form note.</p>\n<p><strong>§11.19</strong> OBS dashboard per-PSP success-rate watching alerts on sustained &lt;95% (3 PSP outages within a week = sev-1 escalation per TASK-OBS-007).</p>\n<p><strong>§11.20</strong> Cargo dependency on <code>hmac</code> + <code>sha2</code> (for VnPay HMAC-SHA512); already in workspace from TASK-INV-005's VietQR pattern.</p>\n<p><strong>§11.21</strong> Per-tenant PSP statistics (<code>vnd_charges_total</code>, <code>vnd_charges_failed_total</code> Prometheus counters) labelled by PSP for operator visibility.</p>\n<p><strong>§11.22</strong> The <code>vnd_event_dispatch_log</code> mirrors TASK-TEN-003's <code>stripe_event_dispatch_log</code> table shape; consistent operational model across rails.</p>\n<hr>\n<p><em>End of TASK-TEN-102 spec.</em></p>";
