window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-304"]="<h1>TASK-CUO-304: Unify the route-back ceiling - api.py default 2 vs doctrine's 3</h1>\n<h2>Summary</h2>\n<p>Doctrine (ship-tasks.md §11b) says a task halts for an operator verdict at <code>routed_back_count &gt;= 3</code> and explicitly says a task at 2 re-enters normally. The machine encodes 2: <code>modules/cuo/cuo/api.py</code> defaults <code>halt_on_repeat_rework: int = 2</code> and halts when <code>rbc &gt;= 2</code>, and <code>cli.py</code>'s flag defaults to 2 with matching help text. A drain run with defaults therefore halts one cycle earlier than the workflow contract promises. This task changes both defaults to 3 and adds a test that pins the Python defaults to the number parsed out of the doctrine text, so the constant can never fork silently again.</p>\n<h2>Problem</h2>\n<p>Audit finding H3, verified first-hand: <code>api.py:127</code> (<code>= 2</code>), <code>api.py:289</code> (<code>rbc &gt;= halt_on_repeat_rework</code>), <code>cli.py:584</code> (<code>default=2</code>, help \"default 2\"). Against ship-tasks.md §11b: halt at <code>&gt;= 3</code>; a task at 2 re-enters normally; the number three is a recorded judgment (\"the same task failed three DIFFERENT ways is evidence about the spec\"). Two consequences:</p>\n<ol><li><strong>Behavioral drift:</strong> a zero-touch drain (<code>cyberos-cuo ... run</code>) halts tasks at their second route-back - stricter than doctrine, so tasks that the workflow contract says should get one more attempt instead park for HITL early, and the halt brief cites a threshold the doctrine does not back.</li><li><strong>No pin:</strong> nothing fails when the two surfaces disagree. The fork happened once already (this finding); without a cross-check it will happen again the next time either side is edited alone.</li></ol>\n<h2>Proposed Solution</h2>\n<p>Change the default to 3 in both places (<code>api.py</code> <code>run()</code> signature, <code>cli.py</code> option default + help text). Add <code>modules/cuo/tests/test_doctrine_constants.py</code> which (a) reads the ceiling from ship-tasks.md §11b by regex (<code>routed_back_count &gt;= N</code> in the MUST-HALT bullet), (b) asserts <code>inspect.signature(cuo.api.run).parameters[\"halt_on_repeat_rework\"].default</code> equals that N, (c) asserts the click option's default equals that N, and (d) asserts the help text quotes the same N. Parsing the doc rather than hardcoding 3 in the test means a future deliberate change edits exactly one normative home (the doctrine) and the test then names every stale machine surface. Add a CHANGELOG entry noting the default change (drains now allow the third cycle before halting).</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Hardcode 3 in the test.</strong> Rejected: that creates a THIRD copy of the constant; when the doctrine changes deliberately, the test fails with a message that reads like the test is wrong, and someone \"fixes\" the test while the api keeps the old value. Parsing the doctrine makes the doc the single source and the test a conformance check.</li><li><strong>Move the constant into a shared config (e.g. <code>.cyberos/config.yaml</code> key).</strong> Rejected: §11b explicitly frames three as a recorded judgment, not tunable configuration; making it operator-tunable invites the \"set it to 99\" drift the ceiling exists to prevent. The CLI flag already provides per-run override for genuine exceptions, defaulted to doctrine.</li><li><strong>Treat api.py's 2 as intentional extra strictness and update the doctrine to 2 instead.</strong> Rejected: §11b's reasoning (\"failed three DIFFERENT ways is evidence about the spec\") is the recorded judgment with rationale; the code carries no rationale for 2 and predates §11b's v2.8.0 wording. The doc is the deliberate artifact; the code is the drift.</li><li><strong>Also pin the 5-fail debugging circuit breaker in the same test.</strong> Deferred, not rejected: measured 2026-07-23, the breaker exists only as workflow/skill prose with no Python constant to pin. The test file is named <code>test_doctrine_constants.py</code> (plural) precisely so the breaker pin lands there the day the breaker gets a machine encoding. Recorded in Open questions-style deferral under Scope.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release, a default drain halts a task only at its third route-back (<code>rbc &gt;= 3</code>), and <code>test_doctrine_constants.py</code> fails within one CI run if either Python default ever disagrees with ship-tasks.md §11b again. Baseline today: defaults are 2 and no cross-check exists.</li><li>Guardrail: <code>--halt-on-repeat-rework 0</code> (disable) and explicit non-default values keep today's exact semantics; the full <code>modules/cuo</code> pytest suite stays green.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the two default-value changes, the help-text correction, the doctrine-parsing pin test, a CHANGELOG entry.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Changing the halt semantics themselves (<code>rbc &gt;= ceiling</code>, halt brief, HALT file) - only the default number moves.</li><li>Pinning the 5-fail debugging circuit breaker - no machine constant exists to pin (measured; see Alternatives Considered). The test file is the designated future home.</li><li>The mechanical HITL lock on the two acceptance gates - TASK-CUO-303.</li><li>Benchmark gate G11 (\"Loop-bound single-sourcing\") CI wiring - TASK-IMP-140 runs this task's test as its G11 checker; soft forward reference, no cycle.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. TASK-IMP-108 (done) authored ship-tasks.md §11b, the doctrine side of this unification. TASK-IMP-140's G11 gate adopts <code>test_doctrine_constants.py</code> as its checker - forward reference only.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> every <code>source_pages</code> line was read at HEAD in this checkout; both defaults, the comparison operator, the help text, and the absence of any other machine encoding of the ceiling were verified by direct read + grep.</li><li><strong>Human review:</strong> the hardening plan (including this task's scope bullet, \"api.py 2 -&gt; 3 + doctrine-constant test\") was operator-approved on 2026-07-23.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>modules/cuo/cuo/api.py</code> <code>run()</code> MUST default <code>halt_on_repeat_rework</code> to 3, and the halt comparison MUST remain <code>rbc &gt;= halt_on_repeat_rework</code> so the default halts a task at its third route-back, matching ship-tasks.md §11b (\"a task at routed_back_count: 2 re-enters normally\").</li><li>1.2 <code>modules/cuo/cuo/cli.py</code>'s <code>--halt-on-repeat-rework</code> option MUST default to 3 and its help text MUST state the same number and keep documenting <code>0</code> as the disable value. The flag remains a per-run override; only its default is doctrine-bound.</li><li>1.3 A new test module <code>modules/cuo/tests/test_doctrine_constants.py</code> MUST parse the ceiling number from ship-tasks.md §11b's MUST-HALT bullet and assert all three machine surfaces equal it: the <code>run()</code> signature default, the click option default, and the number quoted in the option help text. The test MUST fail with a message naming both the doctrine value and the offending surface's value when they diverge.</li><li>1.4 The doctrine-parsing step MUST fail loudly (not skip) if the §11b pattern cannot be found - a silently-skipping conformance test is the schema-drift-test failure mode this batch is also fixing (TASK-MEMORY-303), and it must not be reproduced here.</li><li>1.5 <code>CHANGELOG.md</code> MUST gain an entry noting the default change and its behavioral effect (default drains now permit the third cycle before halting).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - <code>inspect.signature(cuo.api.run).parameters[\"halt_on_repeat_rework\"].default == 3</code> and a drain fixture with a task at rbc 2 re-enters while rbc 3 halts - test: <code>modules/cuo/tests/test_doctrine_constants.py::test_api_default_matches_doctrine</code></li><li>[ ] AC 2 (traces_to: #1.2) - the click option's default is 3 and its help text contains \"default 3\" and \"Set 0 to disable\" - test: <code>modules/cuo/tests/test_doctrine_constants.py::test_cli_default_and_help_match_doctrine</code></li><li>[ ] AC 3 (traces_to: #1.3) - the test derives N from ship-tasks.md (not a literal): patching the parsed doc text to <code>&gt;= 4</code> in-memory makes all three assertions fail with messages naming 4 vs 3 - test: <code>modules/cuo/tests/test_doctrine_constants.py::test_mismatch_names_both_sides</code></li><li>[ ] AC 4 (traces_to: #1.4) - deleting/renaming the §11b bullet in an in-memory copy makes the parser raise (test fails), never skip - test: <code>modules/cuo/tests/test_doctrine_constants.py::test_missing_doctrine_pattern_fails_loud</code></li><li>[ ] AC 5 (traces_to: #1.5) - CHANGELOG's top entry mentions <code>halt-on-repeat-rework</code> and the 2-to-3 change - test: <code>modules/cuo/tests/test_doctrine_constants.py::test_changelog_entry_present</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>Explicit <code>--halt-on-repeat-rework 2</code> after this change:</strong> still halts at the second route-back - the flag override is untouched; only the default moved. The halt brief already prints the effective value, so logs stay unambiguous.</li><li><strong><code>0</code> (disable):</strong> unchanged - the <code>if halt_on_repeat_rework and ...</code> guard in <code>api.py:289</code> already treats 0 as falsy/disabled; the pin test must not accidentally forbid 0 as an explicit value.</li><li><strong>A task already at rbc 2 when the new default ships:</strong> re-enters normally (doctrine-conformant where yesterday it would have halted); its next route-back (3) halts. No stored state needs migration - <code>routed_back_count</code> lives in frontmatter and is only compared at drain time.</li><li><strong>Doctrine wording drift:</strong> if §11b is reworded so the regex misses, the test fails loud per 1.4 - the failure message instructs the editor to update the parser and the machine surfaces together.</li><li><strong>Monorepo vs installed repo:</strong> the test lives in <code>modules/cuo/tests</code> (platform repo only); consumer installs receive the corrected defaults through the vendored payload but not the pin test - acceptable, since the constants fork in source, not per-install.</li><li><strong>Security-class:</strong> a pure constant + test change; no new inputs, no new execution surface.</li></ul>";
