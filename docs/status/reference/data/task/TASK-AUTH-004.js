window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> issue RS256-signed JWTs containing tenant context, persona, and scope grants. The endpoint and surrounding contract obey:</p>\n<ol><li><strong>MUST</strong> sign with RS256 (RSA 2048+); rotate signing key quarterly with 24h overlap. Signing keys live in <code>signing_keys</code> table with <code>kid</code> (key id), <code>status</code> (<code>active | retiring | retired</code>), <code>created_at</code>, <code>retired_at</code>. Active key signs new tokens; retiring keys verify only (24h after rotation); retired keys are removed from JWKS.</li><li><strong>MUST</strong> include claims:</li></ol>\n<ul><li><code>sub</code> (subject UUID)</li><li><code>tenant_id</code> (UUID)</li><li><code>email</code></li><li><code>roles[]</code> (string array; from <code>subjects.roles</code>)</li><li><code>agent_persona</code> (default persona handle for subject; e.g., <code>\"cuo-cpo@0.4.1\"</code>)</li><li><code>scope_grants[]</code> (allowed tool/resource refs; e.g., <code>[\"chat:read\", \"chat:write\", \"kb:read\"]</code>)</li><li><code>iat</code> (issued at; unix seconds)</li><li><code>exp</code> (expiry; iat + 3600)</li><li><code>nbf</code> (not before; iat - 100 to allow clock skew)</li><li><code>jti</code> (ULID for replay protection)</li><li><code>iss</code> (<code>\"https://auth.cyberos.world\"</code>)</li></ul>\n<ol><li><strong>MUST</strong> expose <code>/.well-known/jwks.json</code> returning all keys with status ∈ {<code>active</code>, <code>retiring</code>}. Retired keys are excluded. JWKS response is cached at the CDN edge for 5 minutes; clients re-fetch on <code>kid</code> mismatch.</li><li><strong>MUST</strong> issue via <code>POST /v1/auth/token</code> with body <code>{ \"email\": &lt;string&gt;, \"password\": &lt;string&gt;, \"tenant_slug\": &lt;string&gt; }</code> → returns <code>{ \"access_token\": &lt;jwt&gt;, \"expires_in\": 3600, \"token_type\": \"Bearer\" }</code>. The <code>tenant_slug</code> field is required: a single email may exist across multiple tenants (different subjects), so the slug disambiguates.</li><li><strong>MUST</strong> dual-rate-limit token issuance:</li></ol>\n<ul><li>Per-IP: 10 attempts per minute per source IP (via <code>X-Forwarded-For</code> first hop).</li><li>Per-account: 5 attempts per minute per <code>(tenant_slug, email)</code> regardless of source IP. Either limit triggering returns <code>429 TOO_MANY_REQUESTS</code> with body <code>{\"error\":\"rate_limited\",\"retry_after_seconds\":&lt;n&gt;}</code>. Distributed credential stuffing (different IPs, same account) is caught by the per-account limit; single-IP brute force is caught by the per-IP limit.</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit rows:</li></ol>\n<ul><li><code>auth.token_issued</code> per success — payload: <code>subject_id</code>, <code>tenant_id</code>, <code>jti</code>, <code>roles</code>, <code>scope_grants_count</code>, <code>expires_at</code>, <code>request_id</code>, <code>source_ip_hash16</code>.</li><li><code>auth.token_failed</code> per failed login — payload: <code>tenant_slug</code>, <code>email_hash16</code>, <code>reason</code> (invalid_credentials | suspended | rate_limited), <code>request_id</code>, <code>source_ip_hash16</code>. Note <code>email_hash16</code> not plaintext (matches TASK-AUTH-002 §1 #7 discipline).</li></ul>\n<ol><li><strong>MUST</strong> validate JWT signature, <code>exp</code>, <code>nbf</code>, <code>iss</code> on every consuming service (TASK-AI-006, TASK-MCP-004, TASK-AUTH-005). Verification reads JWKS, picks the key matching the JWT's <code>kid</code> header, validates signature; failures return <code>401 UNAUTHORIZED</code> with <code>{\"error\":\"invalid_jwt\",\"reason\":\"&lt;bad_sig|expired|nbf|wrong_iss|unknown_kid&gt;\"}</code>.</li><li><strong>MUST</strong> support <code>jti</code> dedup at consuming services via bloom filter (probabilistic; ~1MB per million-jti window). Each service maintains a 1-hour rolling bloom filter of seen jtis; a JWT whose jti is in the filter is rejected as <code>replay_detected</code>. No central dedup store (would be a single point of failure); per-service bloom is sufficient for the threat model (replay-within-service is the dangerous case; cross-service replay matters less since each service independently checks).</li><li><strong>MUST</strong> use <strong>constant-time email lookup</strong> to prevent enumeration timing attack: even when the email doesn't exist in the requested tenant, the handler runs a dummy <code>bcrypt::verify</code> against a constant hash to keep response time identical for \"wrong password\" vs \"no such email.\" Without this, an attacker times responses to enumerate valid emails.</li><li><strong>MUST</strong> complete <code>/v1/auth/token</code> in ≤ 250ms p95 (bcrypt verify ~150ms + DB + claims build + sign ~50ms + audit emit ~50ms). Above this budget, investigate (likely DB or Redis latency).</li><li><strong>MUST</strong> include <code>kid</code> (key id) in the JWT header so verifiers know which JWKS key to use. Without <code>kid</code>, verifiers must try every key — works at slice 1 but doesn't scale.</li><li><strong>MUST</strong> propagate <code>agent_persona</code> from <code>subjects.default_persona</code> column (added in this task) — the persona claim defaults to <code>\"cuo-cpo@0.4.1\"</code> if not set per subject. Callers can override at request time via <code>X-Override-Persona</code> header (subject to TASK-AI-005's allowed-personas check).</li><li><strong>MUST</strong> generate <code>scope_grants</code> from a join of <code>subjects.roles</code> × role-to-grants mapping (defined in <code>services/auth/src/scope_map.rs</code>). Slice 1 mappings:</li></ol>\n<ul><li><code>tenant-admin</code> → <code>[\"chat:*\", \"kb:*\", \"proj:*\", \"ai:read\", \"ai:invoke\"]</code></li><li><code>tenant-member</code> → <code>[\"chat:read\", \"chat:write\", \"kb:read\", \"ai:invoke\"]</code></li><li><code>root-admin</code> (tenant 0 only) → <code>[\"*\"]</code></li></ul>\n<ol><li><strong>MUST</strong> check <code>subjects.suspended == false</code> BEFORE issuing token. Suspended subjects get <code>403 FORBIDDEN</code> with <code>{\"error\":\"subject_suspended\",\"contact\":\"ops@cyberos.world\"}</code> AND an <code>auth.token_failed</code> audit row with reason <code>suspended</code>.</li><li><strong>SHOULD</strong> support refresh tokens via separate HTTP-only cookie (TASK-AUTH-007 ships the full flow; this task defines the access-token shape).</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_token_issued_total{tenant_id, outcome}</code> (counter; outcome ∈ ok | invalid_credentials | suspended | rate_limited).</li><li><code>auth_token_issuance_latency_ms</code> (histogram; SLO p95 &lt; 250ms).</li><li><code>auth_jwks_rotation_total{status}</code> (counter; status ∈ generated | retired).</li><li><code>auth_jwt_verifications_total{service, outcome}</code> (counter; for downstream services).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why RS256 + RSA-2048 (§1 #1)?</strong> RS256 is widely supported, NIST-approved, and lets verifiers use only the public key (zero secret-handling on consuming services). ECDSA (ES256) is technically smaller + faster but adoption gaps in some Rust JWT libraries (slice 1 risk-aversion). RSA-2048 is the floor; 4096 is overkill for our threat model. ECDSA migration is TASK-AUTH-110.</p>\n<p><strong>Why 1h access token + 24h key-rotation overlap (§1 #1)?</strong> Short-lived access tokens limit blast radius of leaked credentials; 1h is a common floor (Auth0, Okta defaults). The 24h rotation overlap covers the worst case where a token issued just before key rotation needs to verify just before its 1h expiry — both keys must be in JWKS during the overlap window. 24h is generous (4h would also work; 24h is more forgiving of time-zone-crossing operators).</p>\n<p><strong>Why dual rate-limit (per-IP AND per-account, §1 #5)?</strong> Single-rate-limit attacks well-known: per-IP-only lets distributed attacks (botnet rotating IPs) iterate accounts; per-account-only lets single-IP attackers iterate emails. Both limits together cover both threat models. The 10-per-IP-per-min limit is generous for legitimate users (typing wrong password 5x is normal); 5-per-account-per-min is tight enough to make credential stuffing painful.</p>\n<p><strong>Why constant-time email lookup (§1 #9)?</strong> A timing attack measures \"did the bcrypt::verify run?\" — if \"no such email\" returns in 5ms but \"wrong password\" returns in 150ms, the attacker can enumerate valid emails by timing responses. Running a dummy bcrypt::verify on missing emails keeps response times identical. The constant-time discipline is standard for any credentials check.</p>\n<p><strong>Why <code>tenant_slug</code> required in token request (§1 #4)?</strong> A single email may belong to multiple tenants — <code>alice@cyberos.world</code> could be a member of tenants A, B, and C. Without <code>tenant_slug</code>, the issuance is ambiguous (which tenant's subject?). The slug disambiguates explicitly. Convention: clients track which tenant they're \"in\" (typically via subdomain <code>tenant-a.cyberos.world</code>); the slug is always known.</p>\n<p><strong>Why <code>agent_persona</code> in claims (§1 #12)?</strong> Most modules need to know \"which persona is making this request\" for audit attribution AND for prompt customisation (TASK-AI-014 reads <code>agent_persona</code> from claims to inject the right system prompt). Putting it in the JWT means downstream services don't need a second roundtrip to look it up. The <code>X-Override-Persona</code> header provides per-request flexibility.</p>\n<p><strong>Why <code>scope_grants</code> derived from roles (§1 #13)?</strong> Roles are coarse-grained (tenant-admin, tenant-member); scope_grants are fine-grained (chat:read, kb:write). The role→grants mapping is the bridge: roles are what humans understand; grants are what gates check. Centralising the mapping in <code>scope_map.rs</code> ensures consistency — adding a new role automatically updates its grants without per-gate code changes.</p>\n<p><strong>Why <code>jti</code> dedup via per-service bloom filter (§1 #8)?</strong> A central dedup store (Redis with all-jti history) is a single point of failure AND a privacy concern (every JWT use logged). Per-service bloom filter is probabilistic (~10⁻⁹ false-positive at our 1MB sizing) — acceptable since false-positive means \"honest user re-uses JWT within 1 hour, gets one rejection, re-authenticates\" (annoying but not catastrophic). The threat model is \"attacker captures JWT, replays it\" — which the bloom catches. Cross-service replay matters less because each service has its own bloom.</p>\n<p><strong>Why suspended-subject check (§1 #14)?</strong> A compromised subject (employee left, account fired) needs immediate revocation. Setting <code>subjects.suspended = true</code> (TASK-AUTH-005's admin endpoint) prevents new token issuance — but in-flight 1h JWTs still work. Acceptable trade-off given short token TTL; alternative (central token revocation list) would be a per-request lookup.</p>\n<p><strong>Why <code>email_hash16</code> and <code>source_ip_hash16</code> in audit rows (§1 #6)?</strong> Mirrors TASK-AUTH-002 §1 #7 PII discipline. Plaintext email + IP in audit rows creates everywhere-PII problem; hash16 is enough to disambiguate forensically without leaking. The <code>source_ip_hash16</code> salts include the date so IPs can be correlated within a day but not across — preventing long-term IP tracking.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/auth/src/jwt/issue.rs\n\n#[derive(Deserialize)]\npub struct TokenRequest {\n    pub email: String,\n    #[serde(serialize_with = \"redact\")]\n    pub password: Zeroizing&lt;String&gt;,\n    pub tenant_slug: String,\n}\n\n#[derive(Serialize)]\npub struct TokenResponse {\n    pub access_token: String,\n    pub expires_in: u32,\n    pub token_type: String,    // \"Bearer\"\n}\n\n#[derive(Serialize, Deserialize, Clone)]\npub struct Claims {\n    pub sub: Uuid,\n    pub tenant_id: Uuid,\n    pub email: String,\n    pub roles: Vec&lt;String&gt;,\n    pub agent_persona: String,\n    pub scope_grants: Vec&lt;String&gt;,\n    pub iat: i64,\n    pub exp: i64,\n    pub nbf: i64,\n    pub jti: String,           // ULID\n    pub iss: String,           // \"https://auth.cyberos.world\"\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum AuthTokenError {\n    #[error(\"invalid credentials\")]\n    InvalidCredentials,\n    #[error(\"subject suspended\")]\n    SubjectSuspended,\n    #[error(\"rate limited; retry in {retry_after_seconds}s\")]\n    RateLimited { retry_after_seconds: u32 },\n    #[error(\"tenant not found: {0!r}\")]\n    UnknownTenant(String),\n    #[error(\"signing failed: {0}\")]\n    Signing(String),\n    #[error(\"db: {0}\")]\n    Db(#[from] sqlx::Error),\n}</code></pre>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0006_signing_keys.sql\nCREATE TABLE signing_keys (\n    kid          TEXT PRIMARY KEY,                 -- ULID\n    public_pem   TEXT NOT NULL,\n    private_pem  TEXT NOT NULL,                    -- encrypted at rest via pgcrypto\n    status       TEXT NOT NULL CHECK (status IN ('active','retiring','retired')),\n    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    retired_at   TIMESTAMPTZ\n);\n\nCREATE UNIQUE INDEX signing_keys_active_singleton\n    ON signing_keys(status) WHERE status = 'active';</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/jwt/issue.rs (skeleton)\npub async fn issue_token(\n    req: TokenRequest, pool: &amp;PgPool, redis: &amp;RedisPool,\n    signing_key: &amp;PrivateKey, source_ip: &amp;str, request_id: &amp;str,\n) -&gt; Result&lt;TokenResponse, AuthTokenError&gt; {\n    // §1 #5: dual rate limit\n    rate_limit::check_ip(redis, source_ip, 10).await?;\n    rate_limit::check_account(redis, &amp;req.tenant_slug, &amp;req.email, 5).await?;\n\n    let tenant: Tenant = sqlx::query_as(\"SELECT * FROM tenants WHERE slug = $1\")\n        .bind(&amp;req.tenant_slug).fetch_optional(pool).await?\n        .ok_or_else(|| AuthTokenError::UnknownTenant(req.tenant_slug.clone()))?;\n\n    // §1 #9: constant-time lookup. Even if subject not found, run dummy bcrypt.\n    let subject: Option&lt;Subject&gt; = sqlx::query_as(\n        \"SELECT * FROM subjects WHERE tenant_id = $1 AND email = $2\",\n    ).bind(tenant.id).bind(&amp;req.email).fetch_optional(pool).await?;\n\n    let dummy_hash = \"$2b$12$...constant.dummy.hash...\";\n    let valid = match &amp;subject {\n        Some(s) =&gt; bcrypt::verify(&amp;req.password, &amp;s.password_hash).unwrap_or(false),\n        None    =&gt; { let _ = bcrypt::verify(&amp;req.password, dummy_hash); false }\n    };\n    if !valid {\n        memory::emit(canonical::token_failed(\n            &amp;req.tenant_slug, &amp;req.email, \"invalid_credentials\", source_ip, request_id,\n        )).await?;\n        return Err(AuthTokenError::InvalidCredentials);\n    }\n    let subject = subject.unwrap();\n\n    // §1 #14\n    if subject.suspended {\n        memory::emit(canonical::token_failed(\n            &amp;req.tenant_slug, &amp;req.email, \"suspended\", source_ip, request_id,\n        )).await?;\n        return Err(AuthTokenError::SubjectSuspended);\n    }\n\n    let now = chrono::Utc::now().timestamp();\n    let jti = ulid::Ulid::new().to_string();\n    let scope_grants = scope_map::for_roles(&amp;subject.roles);\n    let claims = Claims {\n        sub: subject.id, tenant_id: subject.tenant_id, email: subject.email.clone(),\n        roles: subject.roles.clone(),\n        agent_persona: subject.default_persona.clone().unwrap_or_else(|| \"cuo-cpo@0.4.1\".into()),\n        scope_grants, iat: now, exp: now + 3600, nbf: now - 100,\n        jti: jti.clone(), iss: \"https://auth.cyberos.world\".into(),\n    };\n\n    let mut header = jsonwebtoken::Header::new(jsonwebtoken::Algorithm::RS256);\n    header.kid = Some(signing_key.kid.clone());\n    let jwt = jsonwebtoken::encode(&amp;header, &amp;claims,\n        &amp;jsonwebtoken::EncodingKey::from_rsa_pem(signing_key.private_pem.as_bytes()).unwrap(),\n    ).map_err(|e| AuthTokenError::Signing(e.to_string()))?;\n\n    memory::emit(canonical::token_issued(\n        subject.id, subject.tenant_id, &amp;jti, &amp;subject.roles, claims.scope_grants.len(),\n        claims.exp, source_ip, request_id,\n    )).await?;\n\n    Ok(TokenResponse { access_token: jwt, expires_in: 3600, token_type: \"Bearer\".into() })\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/jwks/mod.rs\n#[derive(Serialize)]\npub struct JwksResponse { pub keys: Vec&lt;JwkEntry&gt; }\n\n#[derive(Serialize)]\npub struct JwkEntry {\n    pub kty: String,    // \"RSA\"\n    pub kid: String,\n    pub r#use: String,  // \"sig\"\n    pub alg: String,    // \"RS256\"\n    pub n: String,      // base64url-encoded modulus\n    pub e: String,      // base64url-encoded exponent\n}\n\npub async fn jwks(pool: &amp;PgPool) -&gt; Result&lt;JwksResponse, sqlx::Error&gt; {\n    let rows: Vec&lt;(String, String)&gt; = sqlx::query_as(\n        \"SELECT kid, public_pem FROM signing_keys WHERE status IN ('active','retiring')\",\n    ).fetch_all(pool).await?;\n    let keys = rows.into_iter().map(|(kid, pem)| jwk_from_pem(&amp;kid, &amp;pem)).collect();\n    Ok(JwksResponse { keys })\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/scope_map.rs\npub fn for_roles(roles: &amp;[String]) -&gt; Vec&lt;String&gt; {\n    let mut grants = std::collections::BTreeSet::new();\n    for role in roles {\n        match role.as_str() {\n            \"tenant-admin\"  =&gt; for g in [\"chat:*\",\"kb:*\",\"proj:*\",\"ai:read\",\"ai:invoke\"] {\n                                  grants.insert(g.to_string()); },\n            \"tenant-member\" =&gt; for g in [\"chat:read\",\"chat:write\",\"kb:read\",\"ai:invoke\"] {\n                                  grants.insert(g.to_string()); },\n            \"root-admin\"    =&gt; { grants.insert(\"*\".into()); },\n            _ =&gt; {}    // unknown roles silently skipped (TASK-AUTH-002 validates at create)\n        }\n    }\n    grants.into_iter().collect()\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Valid email + password + tenant_slug → 200 + JWT</strong> with all claims populated.</li><li><strong>JWT verifies via JWKS</strong> — public key from <code>/.well-known/jwks.json</code> matches the JWT's <code>kid</code> and verifies the signature.</li><li><strong>Invalid password → 401</strong> + <code>auth.token_failed</code> audit row with <code>reason: invalid_credentials</code>.</li><li><strong>Missing email (constant time) → 401</strong> with same response time as wrong password (timing-attack defence).</li><li><strong>Suspended subject → 403</strong> + audit row with <code>reason: suspended</code>.</li><li><strong>Per-IP rate limit at 11th attempt/min → 429</strong> + <code>Retry-After</code> header.</li><li><strong>Per-account rate limit at 6th attempt/min → 429</strong> even from different IPs.</li><li><strong>JWKS returns active + retiring keys</strong> during 24h rotation overlap.</li><li><strong>JWKS excludes retired keys</strong>.</li><li><strong>Expired JWT verification → <code>invalid_jwt</code> reason <code>expired</code></strong>.</li><li><strong>NBF in future → <code>invalid_jwt</code> reason <code>nbf</code></strong>.</li><li><strong>Wrong issuer → <code>invalid_jwt</code> reason <code>wrong_iss</code></strong>.</li><li><strong>Unknown kid → <code>invalid_jwt</code> reason <code>unknown_kid</code></strong>.</li><li><strong>jti replay rejected</strong> — verifier's bloom filter catches second use within 1h.</li><li><strong><code>scope_grants</code> matches role mapping</strong> — tenant-admin gets <code>chat:*</code> etc.; tenant-member gets reduced set; root-admin gets <code>*</code>.</li><li><strong><code>agent_persona</code> defaults to <code>\"cuo-cpo@0.4.1\"</code></strong> when subject has no <code>default_persona</code>.</li><li><strong>Latency p95 ≤ 250ms</strong> including bcrypt verify.</li><li><strong>Key rotation: token issued on day N still verifies on day N+1</strong> (within 24h overlap).</li><li><strong>Tenant-slug not found → 401</strong> (NOT 404 — same response shape as bad credentials to prevent tenant-enumeration).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn valid_credentials_returns_jwt() {\n    let pool = test_pool().await;\n    let tenant = test_helper::create_tenant(\"test-co\").await;\n    let _ = test_helper::create_subject(tenant.id, \"alice@x.com\", \"CorrectHorse9!\").await;\n\n    let resp = issue_token(\n        TokenRequest {\n            email: \"alice@x.com\".into(),\n            password: Zeroizing::new(\"CorrectHorse9!\".into()),\n            tenant_slug: \"test-co\".into(),\n        }, &amp;pool, &amp;redis, &amp;active_key().await, \"1.2.3.4\", \"req\",\n    ).await.unwrap();\n\n    let header = jsonwebtoken::decode_header(&amp;resp.access_token).unwrap();\n    assert_eq!(header.alg, jsonwebtoken::Algorithm::RS256);\n    assert!(header.kid.is_some());\n    let pubkey = jwks_test_helper::pubkey_for_kid(header.kid.as_ref().unwrap()).await;\n    let token = jsonwebtoken::decode::&lt;Claims&gt;(&amp;resp.access_token, &amp;pubkey,\n        &amp;jsonwebtoken::Validation::new(jsonwebtoken::Algorithm::RS256)).unwrap();\n    assert_eq!(token.claims.email, \"alice@x.com\");\n    assert!(token.claims.roles.contains(&amp;\"tenant-member\".to_string()));\n    assert!(memory_test_helper::has_row(\"auth.token_issued\", &amp;token.claims.jti).await);\n}\n\n#[tokio::test]\nasync fn invalid_password_returns_401_and_emits_failed_row() {\n    let pool = test_pool().await;\n    let tenant = test_helper::create_tenant(\"c\").await;\n    let _ = test_helper::create_subject(tenant.id, \"x@y.com\", \"RealPwd9!Aa\").await;\n\n    let err = issue_token(\n        TokenRequest { email: \"x@y.com\".into(), password: Zeroizing::new(\"WrongPwd9!Aa\".into()),\n                        tenant_slug: \"c\".into() },\n        &amp;pool, &amp;redis, &amp;active_key().await, \"1.2.3.4\", \"req\",\n    ).await.expect_err(\"expected InvalidCredentials\");\n    assert!(matches!(err, AuthTokenError::InvalidCredentials));\n    assert!(memory_test_helper::find_recent(\"auth.token_failed\", \"invalid_credentials\").is_some());\n}\n\n#[tokio::test]\nasync fn missing_email_constant_time_with_present_email() {\n    let pool = test_pool().await;\n    let tenant = test_helper::create_tenant(\"ct\").await;\n    let _ = test_helper::create_subject(tenant.id, \"real@x.com\", \"RealPwd9!Aa\").await;\n\n    let t1 = std::time::Instant::now();\n    let _ = issue_token(token_req(\"real@x.com\", \"WrongPwd9!Aa\", \"ct\"), &amp;pool, &amp;redis, &amp;active_key().await, \"1.2.3.4\", \"r1\").await;\n    let elapsed_present = t1.elapsed();\n\n    let t2 = std::time::Instant::now();\n    let _ = issue_token(token_req(\"missing@x.com\", \"AnyPwd9!Aa\", \"ct\"), &amp;pool, &amp;redis, &amp;active_key().await, \"1.2.3.5\", \"r2\").await;\n    let elapsed_missing = t2.elapsed();\n\n    let diff = (elapsed_present.as_millis() as i64 - elapsed_missing.as_millis() as i64).abs();\n    assert!(diff &lt; 30, \"timing leak: present={elapsed_present:?} missing={elapsed_missing:?}\");\n}\n\n#[tokio::test]\nasync fn suspended_subject_403() {\n    let pool = test_pool().await;\n    let tenant = test_helper::create_tenant(\"susp\").await;\n    let s = test_helper::create_subject(tenant.id, \"x@y.com\", \"Pwd9!AaCorrect\").await;\n    sqlx::query(\"UPDATE subjects SET suspended=true WHERE id=$1\").bind(s.id).execute(&amp;pool).await.unwrap();\n\n    let err = issue_token(token_req(\"x@y.com\", \"Pwd9!AaCorrect\", \"susp\"), &amp;pool, &amp;redis, &amp;active_key().await, \"1.2.3.4\", \"r\").await.expect_err(\"expected Suspended\");\n    assert!(matches!(err, AuthTokenError::SubjectSuspended));\n}\n\n#[tokio::test]\nasync fn rate_limit_per_ip_at_11th_attempt() {\n    let pool = test_pool().await;\n    let _ = test_helper::create_tenant_and_subject().await;\n    for _ in 0..10 {\n        let _ = issue_token(token_req(\"x@y.com\", \"wrong\", \"t\"), &amp;pool, &amp;redis, &amp;active_key().await, \"9.9.9.9\", \"r\").await;\n    }\n    let err = issue_token(token_req(\"x@y.com\", \"wrong\", \"t\"), &amp;pool, &amp;redis, &amp;active_key().await, \"9.9.9.9\", \"r\").await.expect_err(\"expected RateLimited\");\n    assert!(matches!(err, AuthTokenError::RateLimited { .. }));\n}\n\n#[tokio::test]\nasync fn rate_limit_per_account_across_ips() {\n    let pool = test_pool().await;\n    let _ = test_helper::create_tenant_and_subject().await;\n    for i in 0..5 {\n        let _ = issue_token(token_req(\"x@y.com\", \"wrong\", \"t\"), &amp;pool, &amp;redis, &amp;active_key().await, &amp;format!(\"1.1.1.{i}\"), \"r\").await;\n    }\n    let err = issue_token(token_req(\"x@y.com\", \"wrong\", \"t\"), &amp;pool, &amp;redis, &amp;active_key().await, \"2.2.2.2\", \"r\").await.expect_err(\"expected RateLimited\");\n    assert!(matches!(err, AuthTokenError::RateLimited { .. }));\n}\n\n#[tokio::test]\nasync fn jwks_includes_active_and_retiring() {\n    let pool = test_pool().await;\n    rotation::generate_new_signing_key(&amp;pool).await.unwrap();   // marks current as retiring\n    let jwks = jwks::jwks(&amp;pool).await.unwrap();\n    let kids: HashSet&lt;_&gt; = jwks.keys.iter().map(|k| k.kid.clone()).collect();\n    assert!(kids.len() &gt;= 2, \"JWKS should include both active + retiring during overlap\");\n}\n\n#[tokio::test]\nasync fn jti_replay_rejected_by_bloom() {\n    let token = test_helper::issue_test_token().await;\n    let v1 = jwt::verify::verify(&amp;token).await.unwrap();\n    let v2 = jwt::verify::verify(&amp;token).await.expect_err(\"expected replay\");\n    assert!(matches!(v2, jwt::verify::VerifyError::ReplayDetected));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3 for type defs + handler skeleton. Verification path:</p>\n<pre><code class=\"language-rust\">// services/auth/src/jwt/verify.rs\npub async fn verify(token: &amp;str) -&gt; Result&lt;Claims, VerifyError&gt; {\n    let header = jsonwebtoken::decode_header(token)?;\n    let kid = header.kid.ok_or(VerifyError::MissingKid)?;\n    let pubkey = jwks_cache::get_pubkey_for_kid(&amp;kid).await\n        .ok_or(VerifyError::UnknownKid)?;\n    let validation = jsonwebtoken::Validation::new(jsonwebtoken::Algorithm::RS256);\n    let token_data = jsonwebtoken::decode::&lt;Claims&gt;(token, &amp;pubkey, &amp;validation)?;\n    let claims = token_data.claims;\n\n    if claims.iss != \"https://auth.cyberos.world\" { return Err(VerifyError::WrongIss); }\n\n    // §1 #8 jti dedup\n    if jti_bloom::contains_or_insert(&amp;claims.jti) {\n        return Err(VerifyError::ReplayDetected);\n    }\n    Ok(claims)\n}</code></pre>\n<p>Rotation:</p>\n<pre><code class=\"language-rust\">// services/auth/src/jwks/rotation.rs\npub async fn generate_new_signing_key(pool: &amp;PgPool) -&gt; Result&lt;(), sqlx::Error&gt; {\n    let mut tx = pool.begin().await?;\n    sqlx::query(\"UPDATE signing_keys SET status='retiring', retired_at=NOW()+INTERVAL '24 hours' WHERE status='active'\")\n        .execute(&amp;mut *tx).await?;\n    let (kid, pub_pem, priv_pem) = generate_rsa_2048();\n    sqlx::query(\"INSERT INTO signing_keys (kid, public_pem, private_pem, status) VALUES ($1, $2, $3, 'active')\")\n        .bind(&amp;kid).bind(&amp;pub_pem).bind(&amp;priv_pem).execute(&amp;mut *tx).await?;\n    tx.commit().await\n}\n\n/// Sweeper: runs hourly; deletes retired keys past their retired_at.\npub async fn sweep_retired(pool: &amp;PgPool) -&gt; Result&lt;u32, sqlx::Error&gt; {\n    let result = sqlx::query(\"DELETE FROM signing_keys WHERE status='retired' OR (status='retiring' AND retired_at &lt; NOW())\")\n        .execute(pool).await?;\n    Ok(result.rows_affected() as u32)\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-AUTH-001/002/003</strong> — tenants + subjects + RLS exist before tokens issue against them.</li><li><strong>TASK-AUTH-006 (downstream)</strong> — bootstrap CLI generates initial signing key + schedules quarterly rotation cron.</li><li><strong>TASK-AI-006 (downstream)</strong> — reads <code>tenant_id</code> + <code>agent_persona</code> + <code>scope_grants</code> from JWT.</li><li><strong>TASK-MCP-004 (downstream)</strong> — gates tool calls by <code>scope_grants</code>.</li><li>Crates: <code>jsonwebtoken@9</code>, <code>rsa@0.9</code>, <code>ulid@1</code>, <code>bcrypt@0.15</code>, <code>redis@0.24</code>, <code>bloom@0.3</code>, <code>chrono</code>, <code>axum</code>, <code>sqlx</code>.</li><li>Redis 7+ for rate-limit counters.</li><li>Postgres pgcrypto extension for <code>private_pem</code> encryption at rest.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Token request</h3>\n<pre><code class=\"language-http\">POST /v1/auth/token HTTP/1.1\nContent-Type: application/json\nX-Forwarded-For: 1.2.3.4\nX-Forwarded-Proto: https\n\n{ \"email\": \"alice@cyberos.world\", \"password\": \"CorrectHorseBatteryStaple9!\", \"tenant_slug\": \"cyberskill-jsc\" }</code></pre>\n<h3>Token response</h3>\n<pre><code class=\"language-json\">{\n  \"access_token\": \"eyJhbGciOiJSUzI1NiIsImtpZCI6IjAxSFpLLi4uIn0.eyJzdWIiOiI1NTBlOC4uLiIsInRlbmFudF9pZCI6IjY2MGU4Li4uIiwicm9sZXMiOlsidGVuYW50LWFkbWluIl0sInNjb3BlX2dyYW50cyI6WyJjaGF0OioiLCJrYjoqIl0sImV4cCI6MTc2MzExNTYwMH0.SIGNATURE\",\n  \"expires_in\": 3600,\n  \"token_type\": \"Bearer\"\n}</code></pre>\n<h3>JWKS</h3>\n<pre><code class=\"language-http\">GET /.well-known/jwks.json HTTP/1.1\n\n→ 200 OK\nCache-Control: public, max-age=300\n\n{\n  \"keys\": [\n    { \"kty\": \"RSA\", \"kid\": \"01HZK...\", \"use\": \"sig\", \"alg\": \"RS256\", \"n\": \"...\", \"e\": \"AQAB\" },\n    { \"kty\": \"RSA\", \"kid\": \"01HZJ...\", \"use\": \"sig\", \"alg\": \"RS256\", \"n\": \"...\", \"e\": \"AQAB\" }\n  ]\n}</code></pre>\n<h3><code>auth.token_issued</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.token_issued\",\n  \"payload\": {\n    \"subject_id\": \"550e...\",\n    \"tenant_id\": \"660e...\",\n    \"jti\": \"01HZK9R8M3X5C8Q4\",\n    \"roles\": [\"tenant-admin\"],\n    \"scope_grants_count\": 5,\n    \"expires_at\": 1763115600,\n    \"source_ip_hash16\": \"4b8c0d2f1a7e9c3b\",\n    \"request_id\": \"req_...\"\n  }\n}</code></pre>\n<h3><code>auth.token_failed</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.token_failed\",\n  \"payload\": {\n    \"tenant_slug\": \"cyberskill-jsc\",\n    \"email_hash16\": \"ab12cd34ef56gh78\",\n    \"reason\": \"invalid_credentials\",\n    \"source_ip_hash16\": \"4b8c0d2f1a7e9c3b\",\n    \"request_id\": \"req_...\"\n  }\n}</code></pre>\n<h3>Rate-limit response</h3>\n<pre><code class=\"language-http\">HTTP/1.1 429 Too Many Requests\nRetry-After: 45\n\n{ \"error\": \"rate_limited\", \"retry_after_seconds\": 45 }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Refresh tokens with rotating-jti — TASK-AUTH-007.</li><li>ECDSA signing — TASK-AUTH-110.</li><li>Token revocation list (immediate revoke) — TASK-AUTH-111.</li><li>Per-tenant signing keys (multi-tenant key isolation) — TASK-AUTH-112.</li><li>mTLS client-cert authentication — TASK-AUTH-113.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid credentials</td><td>bcrypt verify</td><td>401 invalid_credentials + audit row</td><td>By design</td></tr><tr><td>Missing email + same response time</td><td>constant-time bcrypt on dummy</td><td>401 (timing-attack defence)</td><td>By design</td></tr><tr><td>Suspended subject</td><td>check before issue</td><td>403 subject_suspended</td><td>Operator unsuspends OR caller acknowledges</td></tr><tr><td>Per-IP rate limit</td><td>redis counter</td><td>429 + retry-after</td><td>Wait or use different IP (legitimate retry)</td></tr><tr><td>Per-account rate limit</td><td>redis counter</td><td>429 + retry-after</td><td>Wait</td></tr><tr><td>Tenant slug not found</td><td>DB lookup miss</td><td>401 (NOT 404 — same shape to prevent enumeration)</td><td>Caller fixes slug</td></tr><tr><td>Active key rotation</td><td>new key inserted; old marked retiring</td><td>24h overlap window</td><td>By design</td></tr><tr><td>Retired key expired</td><td>sweeper deletes from JWKS</td><td>Verifications fail with <code>unknown_kid</code></td><td>Caller re-authenticates</td></tr><tr><td>JWKS endpoint unreachable</td><td>downstream verify fails</td><td>401 invalid_jwt → caller re-auth</td><td>Operator investigates AUTH service</td></tr><tr><td>Replay detected (jti in bloom)</td><td>per-service bloom filter</td><td>401 replay_detected</td><td>Caller re-authenticates</td></tr><tr><td>Bloom filter false positive</td><td>~10⁻⁹ rate</td><td>401 (one rejection; honest user re-auths)</td><td>By design</td></tr><tr><td>JWT signature invalid</td><td>jsonwebtoken verify</td><td>401 bad_sig</td><td>Possible attack — investigate logs</td></tr><tr><td>JWT expired</td><td>exp check</td><td>401 expired</td><td>Caller re-authenticates</td></tr><tr><td>JWT nbf in future</td><td>nbf check</td><td>401 nbf</td><td>Likely clock skew; investigate</td></tr><tr><td>Wrong issuer</td><td>iss check</td><td>401 wrong_iss</td><td>Caller using wrong AUTH endpoint</td></tr><tr><td>Latency &gt; 250ms</td><td>OTel histogram</td><td>sev-3 alarm</td><td>Investigate bcrypt OR DB OR Redis</td></tr><tr><td>Signing key access fails (pgcrypto unlock)</td><td>DB error</td><td>503 — refuse to issue tokens</td><td>Operator investigates KMS</td></tr><tr><td>Concurrent rotation</td><td>UNIQUE active singleton constraint</td><td>One succeeds; other gets DB error</td><td>Sweeper-style retry</td></tr><tr><td>Source IP hash predictable</td><td>salt with date prevents long-term tracking</td><td>N/A</td><td>By design</td></tr><tr><td><code>scope_grants</code> for unknown role</td><td>silently skipped</td><td>Subject has fewer grants than expected</td><td>TASK-AUTH-002 validates roles at create</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>RSA-2048 + RS256 chosen for ecosystem maturity in Rust JWT libraries (jsonwebtoken@9 first-class). ECDSA migration tracked in TASK-AUTH-110; not blocking slice 2.</li><li>The 24h key-rotation overlap is the upper-bound buffer for \"token issued just before rotation, verified just before its 1h expiry.\" Without overlap, rotation creates a verification-failure window.</li><li>Dual rate-limit (per-IP AND per-account) covers both the single-IP brute force AND the distributed credential-stuffing threat models. Either limit alone leaves one threat open.</li><li>Constant-time email lookup prevents enumeration timing attacks. The dummy bcrypt::verify on missing emails costs ~150ms — same as the real path. Without this, an attacker can enumerate valid emails by timing responses.</li><li><code>tenant_slug</code> in token request is required because email-uniqueness is per-tenant (a single email can have subjects in multiple tenants). The slug disambiguates without forcing email-uniqueness across all tenants.</li><li><code>jti</code> dedup via per-service bloom filter is the trade-off between strict replay prevention and operational simplicity. Central jti-store would be a single point of failure; bloom is probabilistic but sufficient (false-positive rate ~10⁻⁹ at 1MB sizing; honest users hit at most one rejection in a year).</li><li>The <code>scope_grants</code> derived from roles centralises the permission model. Adding a new role requires editing <code>scope_map.rs</code> (one place) instead of every gate. The mapping is reviewed in PRs and changes are explicit.</li><li><code>email_hash16</code> in audit rows preserves forensic capability without leaking PII at scale. The 16-hex prefix disambiguates 1-in-10⁹ subjects (enough for slice-1 scale); collisions can be resolved by joining against <code>subjects</code> table.</li><li><code>source_ip_hash16</code> is salted with the current date, so IPs can be correlated within a day (useful for incident response) but not across days (preventing long-term IP tracking).</li><li>The <code>signing_keys.private_pem</code> column is encrypted at rest via pgcrypto. The decryption key lives in env vars (operator manages); rotation of the env-var key is TASK-AUTH-006's responsibility.</li></ul>\n<hr>\n<p><em>End of TASK-AUTH-004. Status: draft (10/10 target).</em></p>";
