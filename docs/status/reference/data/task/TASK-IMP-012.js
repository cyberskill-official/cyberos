window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-012"]="<h1>TASK-IMP-012: Coverage measurement and ratchet</h1>\n<h2>Summary</h2>\n<p>CyberOS 1.x needs a coverage ratchet for the install/docs-tools surface — not a monorepo-wide <code>cargo llvm-cov</code>. This task lands a deterministic install-suite coverage metric, a committed baseline, and a ratchet that fails only when coverage regresses.</p>\n<h2>Problem</h2>\n<p><code>coverage-scope.mjs</code> maps a task diff to per-file coverage for authoring, but nothing ratchets the install suite itself. Autodetected <code>COVERAGE_CMD</code> may claim cargo llvm-cov on this monorepo even when the payload work under test is shell/Node. Without a baseline, coverage gates either do nothing useful or fail on absolute thresholds that are wrong for shell tooling.</p>\n<h2>Proposed Solution</h2>\n<p>Add <code>tools/install/docs-tools/coverage-ratchet.mjs</code> that measures <strong>install-script test-touch coverage</strong>: the fraction of <code>tools/install</code> scripts (<code>.sh</code> / <code>.mjs</code> under gates/, docs-tools/, and top-level, excluding <code>tests/</code>) whose basename is referenced by at least one file under <code>tools/install/tests/</code>. Compare against committed <code>tools/install/coverage-baseline.json</code>. Exit non-zero only when current pct is strictly below baseline pct. <code>--write-baseline</code> updates the baseline after a measured improvement. Vendor the tool via <code>build.sh</code>.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Wire cargo llvm-cov as the ratchet.</strong> Rejected: mission scopes 1.x away from platform-wide llvm-cov.</li><li><strong>Absolute 90% threshold only.</strong> Rejected: shell suites rarely have line instrumentation; ratchet-from-baseline is the honest floor.</li><li><strong>Extend coverage-scope.mjs in place.</strong> Rejected: scope tool is task-diff oriented; ratchet is suite-floor oriented. Keep companions, share docs-tools home.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: ratchet fails on constructed regression, passes at baseline, <code>--write-baseline</code> raises floor.</li><li>Guardrail: <code>run_all.sh</code> stays green; missing baseline fails closed (exit 2) rather than inventing 0.</li></ul>\n<h2>Scope</h2>\n<p>In scope: coverage-ratchet.mjs, coverage-baseline.json, tests, build.sh vendor line, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Instrumenting bash with kcov/c8.</li><li>Replacing COVERAGE_CMD autodetect for consumer repos.</li><li>Changing coverage-scope.mjs CLI contract.</li></ul>\n<h2>Dependencies</h2>\n<p>None. Soft companion: coverage-scope.mjs (IMP-098) remains the per-task authoring tool.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent, batch/12c.</li><li><strong>Human review:</strong> session operator Stephen Cheng.</li></ul>\n<h2>§1 - Description (normative)</h2>\n<ol><li><code>coverage-ratchet.mjs</code> MUST compute install-suite test-touch coverage over the closed path set: <code>tools/install/*.{sh,mjs}</code>, <code>tools/install/gates/*.{sh,mjs}</code>, <code>tools/install/docs-tools/*.{sh,mjs}</code> (files only; <code>tests/</code> excluded).</li><li>A script counts as covered when its basename appears as a substring in any <code>tools/install/tests/test_*.sh</code> (or other <code>test_*</code> file under that directory).</li><li>Output MUST include schema <code>coverage-ratchet@1</code>, <code>pct</code> (one decimal), <code>covered</code>, <code>total</code>, and sorted uncovered basenames.</li><li>Default mode MUST compare <code>pct</code> to <code>tools/install/coverage-baseline.json</code> (or <code>--baseline &lt;path&gt;</code>). If baseline missing → exit 2. If <code>pct &lt; baseline.pct</code> → exit 1 (regression). If <code>pct &gt;= baseline.pct</code> → exit 0.</li><li><code>--write-baseline</code> MUST rewrite the baseline file from the current measurement and exit 0 (does not require prior baseline).</li><li><code>--json</code> MUST print the measurement object to stdout; human summary may go to stderr.</li><li><code>build.sh</code> MUST vendor <code>coverage-ratchet.mjs</code> into the payload <code>docs-tools/</code> when present.</li><li>Suite <code>tools/install/tests/test_coverage_ratchet.sh</code> MUST cover regression / pass / write-baseline / missing-baseline.</li></ol>\n<h2>Acceptance criteria</h2>\n<ul><li>[x] AC1: Measurement against a fixture with known covered/uncovered counts matches expected pct. (t01)</li><li>[x] AC2: pct below baseline → exit 1. (t02)</li><li>[x] AC3: pct equal or above baseline → exit 0. (t03)</li><li>[x] AC4: missing baseline → exit 2. (t04)</li><li>[x] AC5: <code>--write-baseline</code> creates/updates baseline to current pct. (t05)</li><li>[x] AC6: payload build copies coverage-ratchet.mjs. (t06)</li></ul>\n<h2>Test plan</h2>\n<p><code>bash tools/install/tests/test_coverage_ratchet.sh</code></p>";
