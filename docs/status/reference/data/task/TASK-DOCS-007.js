window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DOCS-007"]="<h1>TASK-DOCS-007: Status hub v2 + zero-touch regeneration</h1>\n<h2>§1 - Description</h2>\n<p>Normative clauses:</p>\n<ol><li>The status-hub@1 shell MUST adopt the operator-reference dashboard language on CDS tokens: umber header band with ochre rule (title/subtitle/meta), an overall-progress card with a SEGMENTED bar (done=success green, in-flight=ochre, on-hold=muted) + count chips, a generated \"Now shipping\" callout listing in-flight tasks as chips, and a color legend. New slots (subtitle, meta:html, now:html, legend:html) are additive to the contract table.</li><li>The Roadmap tab MUST replace the 10-column board with MODULE CARDS: one card per module carrying a per-module segmented minibar, % done, and one chip per task (short id, status-colored, title+status tooltip, linked to its page); in-flight modules get the ochre card accent; the invalid-status bucket stays visible as its own card; the rollup table remains below.</li><li>The Changelog tab MUST render releases as tick-circle rows (newest = ochre \"now\", older = green done). The Backlog tab keeps its four facets with status rendered as chips.</li><li>Zero-touch regeneration MUST hold on all three surfaces: (a) local - the pre-commit hook regenerates dist/website whenever staged changes touch docs/tasks/<strong>, docs/</strong>, module docs, modules/templates/<strong>, tools/docs-site/</strong>, CHANGELOG.md, or VERSION (warn-not-block on build failure, skip with warning when node is absent); (b) published on push - deploy.yml's docs job path filters gain VERSION and modules/templates/**; (c) published on version bump - version.yml dispatches deploy.yml after pushing the [skip ci] bump commit, so the hub's VERSION stamp never goes stale (warn on dispatch failure).</li><li>Styling stays token-only (hex solely in vendored cds/*.css) and the page self-contained; suites keep the TASK-DOCS-006 assertions updated to the v2 structure.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The reference page communicates in bars and chips where v1 used columns of text; mapping it onto CDS tokens keeps one visual language. The three regeneration surfaces close every staleness path: the author's machine (hook), content pushes (paths), and the one commit class that suppresses CI ([skip ci] bumps -&gt; explicit dispatch).</p>\n<h2>§3 - Contract</h2>\n<p>Slot table (status-hub@1, additive): title, subtitle, meta:html, deck:html, now:html, legend:html, tab_*:html, footer. Chip grammar: span.chip.(done|active|hold|todo) with data-status, wrapped in a.chip-link when the task page exists.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Dashboard structure renders</strong> (§1 #1, #2) - fixture: overall bar with 50.0% done segment + count chips; module card grid with minibars and linked chips; callout appears when in-flight tasks exist.</li><li><strong>Changelog ticks + backlog chips</strong> (§1 #3) - newest release row carries the \"now\" tick; the status column renders chips.</li><li><strong>Zero-touch surfaces wired</strong> (§1 #4) - the hook contains the docs trigger + build call; deploy.yml paths carry VERSION + modules/templates/**; version.yml dispatches deploy.yml after a successful bump push.</li><li><strong>Token-clean + suites green</strong> (§1 #5) - no hex outside the token block; hub + legacy + templates suites pass on the v2 structure.</li></ol>\n<h2>§5 - Verification</h2>\n<p>test_render_status_hub.sh (t01/t06 updated) + test_render_roadmap.sh (legacy asserts preserved) + test_templates_module.sh + wiring greps recorded in the ship record.</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Shell v2 + builder fragment rewrite (single corpus object unchanged); hook block appended; two workflow edits.</p>\n<h2>§7 - Dependencies</h2>\n<p>TASK-DOCS-006 (the hub it restyles). TASK-IMP-068's hook infrastructure hosts the new trigger.</p>\n<h2>§8 - Example payloads</h2>\n<p>Summary line unchanged: <code>status-hub: 491 tasks (...), 20 releases, VERSION 0.2.0 (deck+3 tabs)</code>.</p>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Per-task history sparklines need event data the corpus does not carry - future scope.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Hook slows commits - the site build is seconds-scale; warn-not-block keeps commits unblocked on failure.</li><li>Dispatch loop risk - version.yml dispatches deploy.yml only (deploy never bumps VERSION); acyclic.</li><li>node absent on an operator machine - hook warns and skips; published surfaces unaffected.</li><li>Chip flood on giant modules (memory: 85) - 11.5px pills in narrow grid columns; acceptable past 100/module, pagination is future scope.</li><li>Reference hexes leaking - mapped to tokens at authoring; the hex greps enforce.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Chips show short ids (task- prefix stripped) for density; full id + title + status live in the tooltip.</p>\n<p><strong>Post-ship amendment (2026-07-12, TASK-IMP-071):</strong> §1 #4c's deploy-dispatch workaround is retired - with [skip ci] gone from bump commits, deploy.yml's VERSION path filter fires natively on the bump push. Surfaces (a) and (b) unchanged.</p>\n<p><em>End of TASK-DOCS-007.</em></p>";
