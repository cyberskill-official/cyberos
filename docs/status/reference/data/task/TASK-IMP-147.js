window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-147"]="<h1>TASK-IMP-147: Ingest inspect-harden</h1>\n<h2>Summary</h2>\n<p>Bring the inspect-harden package into CyberOS as four first-class skills (<code>inspection-report-author/audit</code>, <code>harden-record-author/audit</code>), fix the known <code>harden-plan.mjs</code> <code>operator_prerequisites</code> defect, wire <code>/inspect</code> and <code>/harden</code> for Claude plugin and native shared skills, and clarify that ship-tasks \"harden a task\" (<code>class: improvement</code>) is not <code>/harden</code> (inspection remediation).</p>\n<h2>Problem</h2>\n<p>Inspection → remediation lived as an external package. CyberOS had no vendored skills, no slash commands, and conductor language that overloaded \"harden\" with improvement-class shipping. The planner also misclassified findings that declare <code>operator_prerequisites</code> (shopass INS-F-0002).</p>\n<h2>Proposed Solution</h2>\n<ol><li>Ingest four skill trees under <code>modules/skill/</code> to the author/audit floor.</li><li>Align ledger contract for spec ≥1.2 to 75 disciplines; keep 1.0 goldens valid.</li><li>Fix <code>harden-plan.mjs</code> + extend <code>harden-plan-check.sh</code>.</li><li>Scaffold <code>harden-record-audit</code> (RUBRIC / AUDIT_LOOP / REPORT_FORMAT / triggers).</li><li>Vendor + pair-parity SCOPE + census 52→56; plugin commands; SHARED_CMDS; CLI stubs.</li><li>Conductor docs: ship-tasks §1a \"Not <code>/harden</code>\" note; plugin skill + help.</li></ol>\n<h2>Non-goals</h2>\n<ul><li>Applying shopass patch to a live target.</li><li>MCP tools for inspect/harden.</li><li>Merging <code>/harden</code> into <code>/ship-tasks</code>.</li><li>Committing package-root <code>reports/</code> / <code>evidence/</code> as runtime.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li><code>inspect-lint --selftest</code> green; all acceptance goldens lint clean.</li><li><code>harden-plan-check.sh</code> PASS including shopass INS-F-0002 non-agent.</li><li><code>build.sh</code> + pair-parity + channel tests green; census 56.</li><li>Task remains non-<code>done</code> until human HITL.</li></ul>";
