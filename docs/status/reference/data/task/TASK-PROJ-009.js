window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-009"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The MEMORY_LINK layer <strong>MUST</strong> model typed edges between Issues and memory memories. The contract:</p>\n<ol><li><strong>MUST</strong> define <code>memory_links</code> table: <code>id UUID PK</code>, <code>issue_id UUID FK</code>, <code>memory_path TEXT</code>, <code>memory_row_id TEXT</code> (nullable; for non-path linkable rows), <code>link_type</code> (<code>cites|implements|supersedes</code>), <code>created_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID</code>, <code>removed_at TIMESTAMPTZ</code> (nullable; soft-delete), <code>removed_by_subject_id UUID</code>, <code>removal_reason TEXT</code>, <code>tenant_id UUID</code>.</li><li><strong>MUST</strong> support 3 link types:</li></ol>\n<ul><li><code>cites</code>: Issue references the memory (no semantic obligation).</li><li><code>implements</code>: Issue is the concrete work realising the memory's intent (e.g. decision row → impl issue).</li><li><code>supersedes</code>: Issue makes the memory obsolete; consumers should prefer Issue.</li></ul>\n<ol><li><strong>MUST</strong> validate at link-create:</li></ol>\n<ul><li><code>memory_path</code> MUST exist (calls TASK-MEMORY-108 search; 404 if missing → <code>Err(LinkError::TargetMissing)</code>).</li><li>Caller's frontmatter <code>allowed_memory_scopes</code> (TASK-SKILL-103) MUST cover the path; otherwise <code>Err(LinkError::ScopeDenied)</code>.</li><li>For <code>supersedes</code>: memory's <code>created_at_ns &lt; issue.created_at_ns</code> (forward-only); else <code>Err(LinkError::SupersedeViolatesTime)</code>.</li></ul>\n<ol><li><strong>MUST</strong> soft-delete via <code>removed_at + removed_by_subject_id + removal_reason</code>. Removed rows persist for audit; queries filter by default unless <code>?include_removed=true</code>.</li><li><strong>MUST</strong> expose REST endpoints:</li></ol>\n<ul><li><code>POST /api/proj/issues/:id/memory-links</code> — create.</li><li><code>DELETE /api/proj/issues/:id/memory-links/:link_id</code> with <code>reason</code> body — soft-remove.</li><li><code>GET /api/proj/issues/:id/memory-links</code> — outgoing edges.</li><li><code>GET /api/proj/memory-memories/:path/issues</code> — incoming edges (bidirectional traversal).</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit rows:</li></ol>\n<ul><li><code>proj.memory_link_created</code> on create.</li><li><code>proj.memory_link_removed</code> on soft-delete.</li></ul>\n<ol><li><strong>MUST</strong> prevent duplicate active links of same <code>(issue_id, memory_path, link_type)</code> (one of each type at a time). Same <code>memory_path</code> MAY have multiple link types from same issue (e.g. cites AND implements).</li><li><strong>MUST</strong> RLS per tenant; the linked memory's tenant_id MUST match issue's tenant_id (cross-tenant links forbidden).</li><li><strong>MUST</strong> emit OTel metric <code>proj_memory_links_total{link_type, outcome}</code>; outcome ∈ created | removed | denied | dangling.</li><li><strong>MUST</strong> support link annotations: an optional <code>annotation</code> field (max 500 chars) on link creation explaining the relationship beyond just the type (e.g. cites with annotation \"see section 3 specifically\"). PII-redacted via TASK-MEMORY-111 before storage.</li><li><strong>MUST</strong> support cycle detection on <code>supersedes</code> chains: if Issue A supersedes Memory M, and an attempt is made to have Memory M supersede A's parent chain (or similar circular reference), reject with <code>Err(LinkError::CycleDetected)</code>. Cycle check runs DFS bounded at depth 100.</li><li><strong>MUST</strong> include a <code>link_strength</code> field (<code>weak | medium | strong</code>) optional, default <code>medium</code>. <code>weak</code> = passing mention; <code>strong</code> = primary citation. Used by TASK-PROJ-010 citation-drift to prioritise alerts.</li><li><strong>MUST</strong> support batch link creation: <code>POST /api/proj/issues/:id/memory-links/batch</code> with array of links (max 50). Each item validated independently; partial success allowed (per-item status).</li><li><strong>MUST</strong> expose link-graph traversal: <code>GET /api/proj/issues/:id/memory-links/graph?depth=N&amp;types=cites,implements</code> returns a directed graph (issues + memories as nodes, links as edges) up to N hops. Default depth=2, max=5.</li><li><strong>MUST</strong> support link transfer on issue clone/split: when issue is split into multiple sub-issues, operator chooses which sub-issue inherits each link. Default = all links go to first sub-issue; operator can re-route via UI.</li><li><strong>MUST</strong> validate <code>removal_reason</code> is non-empty for soft-remove: empty/null → 400 <code>removal_reason_required</code>.</li><li><strong>MUST</strong> support a \"rationale\" version of cites: <code>cites_with_quote</code> link type variant carries an optional <code>quoted_text</code> field (the exact passage from the memory being cited). Bounded at 2KB.</li><li><strong>MUST</strong> include <code>metadata</code> JSONB field for per-tenant extension: arbitrary keys/values for tenant-specific use (e.g. compliance tags, billing references). Open schema; no reserved keys.</li><li><strong>MUST</strong> support link \"review\" state: optionally, links can be marked <code>review_pending = true</code> requiring admin approval before they're considered active for traversal queries. Tenant policy <code>cyberos_proj_tenant_settings.memory_links_require_review</code> toggles default.</li><li><strong>MUST</strong> include <code>proj.memory_link_traversed</code> audit row each time a graph-traversal query crosses an edge (for analytics on which links are heavily traversed). Sampled at 10% to avoid audit log spam.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why three link types (DEC-300)?</strong> Three is the empirical set of useful semantic relations. <code>cites</code> = \"I'm aware of this\"; <code>implements</code> = \"I'm the concrete realisation\"; <code>supersedes</code> = \"this is now obsolete; use me instead.\" More types (e.g. <code>clarifies</code>, <code>disputes</code>) compose from these via tags or future v2.</p>\n<p><strong>Why validate memory exists (DEC-301)?</strong> A link pointing to nothing is worse than no link (gives false confidence). Validation at write-time catches typos + race conditions (memory deleted between user typing and POST).</p>\n<p><strong>Why scope check (DEC-301)?</strong> Cross-tenant link leakage = privacy breach. A user can't link to a memory they shouldn't even know about. Scope check enforces \"least authority\" — they can only link to memories they could already read.</p>\n<p><strong>Why forward-only supersedes (DEC-302)?</strong> Supersedes implies \"this is newer.\" Linking to a future memory inverts the temporal invariant; downstream tooling assumes supersession chains are monotonic in time.</p>\n<p><strong>Why soft-delete (§1 #4)?</strong> Hard-delete loses the audit trail. Soft-delete = \"this was a link, now it's not, here's why.\" Auditors investigating \"why was this link removed\" have the answer.</p>\n<p><strong>Why duplicate prevention by <code>(issue, path, link_type)</code> not <code>(issue, path)</code> (§1 #7)?</strong> Same path may legitimately be both <code>cites</code> and <code>implements</code> (Issue X is the implementation of decision row Y, AND Issue X cites decision row Y). Allowing both keeps the model expressive.</p>\n<p><strong>Why bidirectional traversal (§1 #5)?</strong> Forward: \"what does this issue link to?\" Backward: \"what issues link to this memory?\" The latter is the load-bearing query for citation drift (TASK-PROJ-010): \"is this memory referenced anywhere?\"</p>\n<p><strong>Why annotation field (§1 #10)?</strong> Pure link type loses nuance; operator's intent is often \"cites — see section 3 specifically.\" Annotation captures the why behind the link.</p>\n<p><strong>Why cycle detection (§1 #11)?</strong> Supersession chains must be acyclic; A supersedes B, B supersedes A creates an undefined \"which is current\" state. DFS detection prevents at write-time.</p>\n<p><strong>Why link_strength (§1 #12)?</strong> Citation drift alerts should distinguish \"this memory is critical to 5 strong-cited issues\" from \"this memory is weakly mentioned in 50.\" Operators prioritise by strength.</p>\n<p><strong>Why batch creation (§1 #13)?</strong> Bulk import flows (migrating from another tool) create dozens of links per issue; per-link HTTP is expensive.</p>\n<p><strong>Why graph traversal endpoint (§1 #14)?</strong> UI features (knowledge-graph view, \"show me everything this issue is connected to\") need multi-hop queries. Depth-bounded prevents runaway.</p>\n<p><strong>Why link transfer on split (§1 #15)?</strong> When an issue is split, links must go somewhere; default-all-to-first is the safe default; operator override handles complex cases.</p>\n<p><strong>Why non-empty removal_reason (§1 #16)?</strong> Soft-remove without a reason loses the \"why\" for auditors. Empty reason is the same as no audit trail.</p>\n<p><strong>Why cites_with_quote variant (§1 #17)?</strong> Heavy-citation workflows (research, legal) want to preserve the exact text being cited. Hash-of-memory isn't enough; the quote is the cite.</p>\n<p><strong>Why metadata JSONB (§1 #18)?</strong> Per-tenant extension without schema migration; tenant-specific compliance tags fit here.</p>\n<p><strong>Why review-pending state (§1 #19)?</strong> Some tenants require curation of cross-references for quality; opt-in policy supports their workflow.</p>\n<p><strong>Why traversal audit sampling (§1 #20)?</strong> Graph queries are frequent (page loads); audit row per edge would flood the chain. 10% sample preserves analytics signal without flood.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0009_memory_links.sql\n\nCREATE TABLE memory_links (\n    id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    issue_id              UUID NOT NULL,\n    memory_path           TEXT NOT NULL,\n    memory_row_id         TEXT,\n    link_type             TEXT NOT NULL CHECK (link_type IN ('cites','implements','supersedes')),\n    created_at            TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    created_by_subject_id UUID NOT NULL,\n    removed_at            TIMESTAMPTZ,\n    removed_by_subject_id UUID,\n    removal_reason        TEXT,\n    tenant_id             UUID NOT NULL\n);\nCREATE UNIQUE INDEX uniq_active_memory_link\n    ON memory_links (issue_id, memory_path, link_type)\n    WHERE removed_at IS NULL;\nCREATE INDEX idx_memory_links_by_issue ON memory_links (issue_id) WHERE removed_at IS NULL;\nCREATE INDEX idx_memory_links_by_memory ON memory_links (memory_path) WHERE removed_at IS NULL;\n\nALTER TABLE memory_links ENABLE ROW LEVEL SECURITY;\nCREATE POLICY memory_links_tenant_isolation ON memory_links\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Rust</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/memory_link/mod.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum LinkType { Cites, Implements, Supersedes }\n\n#[derive(Clone, Debug, Serialize, sqlx::FromRow)]\npub struct MemoryLink {\n    pub id:                    uuid::Uuid,\n    pub issue_id:              uuid::Uuid,\n    pub memory_path:           String,\n    pub memory_row_id:         Option&lt;String&gt;,\n    pub link_type:             LinkType,\n    pub created_at:            chrono::DateTime&lt;chrono::Utc&gt;,\n    pub created_by_subject_id: uuid::Uuid,\n    pub removed_at:            Option&lt;chrono::DateTime&lt;chrono::Utc&gt;&gt;,\n    pub removed_by_subject_id: Option&lt;uuid::Uuid&gt;,\n    pub removal_reason:        Option&lt;String&gt;,\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum LinkError {\n    #[error(\"target memory does not exist: {0}\")]                  TargetMissing(String),\n    #[error(\"scope denied for path: {0}\")]                         ScopeDenied(String),\n    #[error(\"supersedes violates time invariant (memory newer than issue)\")] SupersedeViolatesTime,\n    #[error(\"duplicate active link of type {0:?}\")]                DuplicateActive(LinkType),\n    #[error(\"cross-tenant link forbidden\")]                        CrossTenantForbidden,\n    #[error(\"db: {0}\")]                                            Db(String),\n}\n\npub async fn create_link(\n    pool: &amp;sqlx::PgPool,\n    issue_id: uuid::Uuid,\n    memory_path: String,\n    link_type: LinkType,\n    subject_id: uuid::Uuid,\n) -&gt; Result&lt;MemoryLink, LinkError&gt; {\n    // 1. Validate target exists\n    let memory = memory_reader::find_memory(&amp;memory_path).await\n        .ok_or_else(|| LinkError::TargetMissing(memory_path.clone()))?;\n\n    // 2. Scope check (caller's frontmatter must cover this path)\n    if !subject_can_read_scope(subject_id, &amp;memory_path).await {\n        return Err(LinkError::ScopeDenied(memory_path));\n    }\n\n    // 3. Cross-tenant check\n    let issue: (chrono::DateTime&lt;chrono::Utc&gt;, uuid::Uuid) = sqlx::query_as(\n        \"SELECT created_at, tenant_id FROM issues WHERE id = $1\"\n    ).bind(issue_id).fetch_one(pool).await.map_err(|e| LinkError::Db(e.to_string()))?;\n    if memory.tenant_id != issue.1 {\n        return Err(LinkError::CrossTenantForbidden);\n    }\n\n    // 4. Supersede time invariant\n    if link_type == LinkType::Supersedes {\n        if memory.created_at_ns &gt; issue.0.timestamp_nanos_opt().unwrap_or(i64::MAX) {\n            return Err(LinkError::SupersedeViolatesTime);\n        }\n    }\n\n    // 5. Insert (UNIQUE catches duplicate)\n    let row: MemoryLink = sqlx::query_as(\n        \"INSERT INTO memory_links (issue_id, memory_path, memory_row_id, link_type,\n                                   created_by_subject_id, tenant_id)\n         VALUES ($1, $2, $3, $4, $5, current_setting('app.tenant_id')::uuid)\n         RETURNING *\"\n    ).bind(issue_id).bind(memory_path.clone()).bind(memory.row_id.clone())\n     .bind(link_type).bind(subject_id)\n     .fetch_one(pool).await\n     .map_err(|e| {\n         if e.to_string().contains(\"uniq_active_memory_link\") {\n             LinkError::DuplicateActive(link_type)\n         } else { LinkError::Db(e.to_string()) }\n     })?;\n\n    emit_memory_row(\"proj.memory_link_created\", serde_json::json!({\n        \"link_id\": row.id, \"issue_id\": issue_id, \"memory_path\": memory_path,\n        \"link_type\": link_type, \"by_subject_id\": subject_id,\n    })).await;\n    metrics::counter!(\"proj_memory_links_total\",\n        \"link_type\" =&gt; format!(\"{link_type:?}\"), \"outcome\" =&gt; \"created\").increment(1);\n    Ok(row)\n}\n\npub async fn remove_link(\n    pool: &amp;sqlx::PgPool,\n    link_id: uuid::Uuid,\n    subject_id: uuid::Uuid,\n    reason: String,\n) -&gt; Result&lt;(), LinkError&gt; {\n    sqlx::query(\n        \"UPDATE memory_links SET removed_at = NOW(), removed_by_subject_id = $1, removal_reason = $2\n         WHERE id = $3 AND removed_at IS NULL\"\n    ).bind(subject_id).bind(reason).bind(link_id)\n     .execute(pool).await.map_err(|e| LinkError::Db(e.to_string()))?;\n    emit_memory_row(\"proj.memory_link_removed\", serde_json::json!({\n        \"link_id\": link_id, \"by_subject_id\": subject_id,\n    })).await;\n    metrics::counter!(\"proj_memory_links_total\",\n        \"link_type\" =&gt; \"any\", \"outcome\" =&gt; \"removed\").increment(1);\n    Ok(())\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Create cites link</strong> — POST → 201; row in memory_links; type=cites.</li><li><strong>Create implements + cites same memory same issue</strong> — both succeed; 2 active rows.</li><li><strong>Duplicate active type rejected</strong> — second POST same (issue, path, type) → 409 DuplicateActive.</li><li><strong>Dangling target rejected</strong> — POST with non-existent memory_path → 422 TargetMissing.</li><li><strong>Scope-denied rejected</strong> — POST to memory outside caller's allowed_memory_scopes → 403 ScopeDenied.</li><li><strong>Supersedes forward-only</strong> — memory created after issue → 422 SupersedeViolatesTime.</li><li><strong>Cross-tenant link rejected</strong> — link to memory in different tenant → 403 CrossTenantForbidden.</li><li><strong>Soft-remove preserves row</strong> — DELETE → row still exists with removed_at + reason set.</li><li><strong>Re-create after remove works</strong> — same (issue, path, type) after soft-remove → 201 (new row).</li><li><strong>list-by-issue (default) excludes removed</strong> — query → only active rows.</li><li><strong>list-by-issue ?include_removed=true</strong> — query → all rows incl. removed.</li><li><strong>Bidirectional: list-by-memory</strong> — GET /api/proj/memory-memories/:path/issues → issues linking to it.</li><li><strong>memory audit on create + remove</strong> — both events emit corresponding rows.</li><li><strong>RLS isolates tenants</strong> — tenant B cannot see tenant A's links.</li><li><strong>OTel counter increments per outcome</strong> — counters for created + removed match call counts.</li><li><strong>Annotation stored + redacted</strong> — POST with annotation containing email → stored with &lt;EMAIL&gt; redacted (AC for §1 #10).</li><li><strong>Cycle detection rejects</strong> — A supersedes B, then attempt B supersedes A → 422 CycleDetected (AC for §1 #11).</li><li><strong>link_strength persisted + queryable</strong> — POST with strength=strong → stored; list filter by strength works (AC for §1 #12).</li><li><strong>Batch create handles partial</strong> — batch of 50 with 2 invalid → 200 with per-item status; valid ones inserted (AC for §1 #13).</li><li><strong>Graph traversal returns N hops</strong> — depth=2 → nodes + edges within 2 hops; depth=10 → 400 (AC for §1 #14).</li><li><strong>Link transfer on split</strong> — split issue → links default to first sub-issue; operator can re-route (AC for §1 #15).</li><li><strong>Empty removal_reason rejected</strong> — DELETE with empty reason → 400 (AC for §1 #16).</li><li><strong>cites_with_quote stores quoted_text</strong> — POST with quote → quote retrievable on GET (AC for §1 #17).</li><li><strong>metadata round-trips</strong> — POST with custom JSONB → GET preserves (AC for §1 #18).</li><li><strong>review-pending state honoured</strong> — set tenant policy require_review=true; new link starts review_pending=true; graph excludes pending (AC for §1 #19).</li><li><strong>Traversal audit sampled at 10%</strong> — 1000 traversals → ~100 audit rows (binomial within 90% CI) (AC for §1 #20).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn duplicate_active_rejected() {\n    let env = TestEnv::new().await;\n    let (issue, mem) = env.setup_link().await;\n    let _ = create_link(&amp;env.pool, issue, mem.clone(), LinkType::Cites, env.alice()).await.unwrap();\n    let err = create_link(&amp;env.pool, issue, mem, LinkType::Cites, env.alice()).await.unwrap_err();\n    assert!(matches!(err, LinkError::DuplicateActive(LinkType::Cites)));\n}\n\n#[tokio::test]\nasync fn supersedes_forward_only() {\n    let env = TestEnv::new().await;\n    let future_memory = env.create_memory_at(chrono::Utc::now() + chrono::Duration::days(1)).await;\n    let issue = env.create_issue_at(chrono::Utc::now()).await;\n    let err = create_link(&amp;env.pool, issue, future_memory, LinkType::Supersedes, env.alice()).await.unwrap_err();\n    assert!(matches!(err, LinkError::SupersedeViolatesTime));\n}\n\n#[tokio::test]\nasync fn bidirectional_traversal() {\n    let env = TestEnv::new().await;\n    let memory = env.create_memory().await;\n    let issue1 = env.create_issue().await;\n    let issue2 = env.create_issue().await;\n    let _ = create_link(&amp;env.pool, issue1, memory.clone(), LinkType::Cites, env.alice()).await.unwrap();\n    let _ = create_link(&amp;env.pool, issue2, memory.clone(), LinkType::Implements, env.alice()).await.unwrap();\n\n    let outgoing = list_links_by_issue(&amp;env.pool, issue1, false).await.unwrap();\n    assert_eq!(outgoing.len(), 1);\n\n    let incoming = list_issues_by_memory(&amp;env.pool, &amp;memory).await.unwrap();\n    assert_eq!(incoming.len(), 2);\n}\n\n#[tokio::test]\nasync fn cross_tenant_forbidden() {\n    let env_a = TestEnv::for_tenant(\"A\").await;\n    let env_b = TestEnv::for_tenant(\"B\").await;\n    let memory_a = env_a.create_memory().await;\n    let issue_b = env_b.create_issue().await;\n    let err = create_link(&amp;env_b.pool, issue_b, memory_a, LinkType::Cites, env_b.alice()).await.unwrap_err();\n    assert!(matches!(err, LinkError::CrossTenantForbidden));\n}\n\n#[tokio::test]\nasync fn soft_remove_then_recreate() {\n    let env = TestEnv::new().await;\n    let (issue, mem) = env.setup_link().await;\n    let link = create_link(&amp;env.pool, issue, mem.clone(), LinkType::Cites, env.alice()).await.unwrap();\n    remove_link(&amp;env.pool, link.id, env.alice(), \"user requested\".into()).await.unwrap();\n    let link2 = create_link(&amp;env.pool, issue, mem, LinkType::Cites, env.alice()).await.unwrap();\n    assert_ne!(link.id, link2.id);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + DB above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-001</strong> — issues FK.</li><li><strong>TASK-PROJ-010 (downstream)</strong> — citation drift uses these links.</li><li><strong>TASK-MEMORY-101</strong> — MemoryReader for validation.</li><li><strong>TASK-MEMORY-108</strong> — search API (find_memory).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.memory_link_created\",\n  \"payload\": {\n    \"link_id\": \"lk-...\",\n    \"issue_id\": \"iss-...\",\n    \"memory_path\": \"memories/projects/cyberos/decisions/DEC-300.md\",\n    \"link_type\": \"implements\",\n    \"by_subject_id\": \"7e57c0de-...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li><code>disputes</code> link type — slice 4+.</li><li>Issue↔Issue links (in addition to Issue↔Memory) — slice 4+; sibling task.</li><li>Auto-link via NLP (extract memory references from issue body) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Memory missing at create</td><td>find_memory Err</td><td>422 TargetMissing</td><td>Caller verifies path</td></tr><tr><td>Scope denied</td><td>subject_can_read_scope false</td><td>403</td><td>Caller's frontmatter expanded</td></tr><tr><td>Supersedes future memory</td><td>timestamp check</td><td>422</td><td>Caller fixes target</td></tr><tr><td>Duplicate active</td><td>UNIQUE constraint</td><td>409</td><td>Caller removes existing first</td></tr><tr><td>Cross-tenant</td><td>tenant_id mismatch</td><td>403</td><td>None</td></tr><tr><td>Memory deleted after link created</td><td>dangling</td><td>Listed in query; TASK-PROJ-010 flags</td><td>Operator removes link</td></tr><tr><td>memory reader unreachable</td><td>find_memory Err</td><td>503</td><td>Operator restores memory</td></tr><tr><td>Audit emit fails</td><td>Link created; audit lost</td><td>sev-2</td><td>Operator restores</td></tr><tr><td>Many links per issue (1000+)</td><td>unbounded</td><td>List query slow</td><td>Slice 3+ paginate</td></tr><tr><td>Removed_by NULL when not removed</td><td>NULL-safe queries</td><td>Correct</td><td>None</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>memory_row_id NULL (path-only)</td><td>nullable column</td><td>Both supported</td><td>None</td></tr><tr><td>Concurrent link create same key</td><td>UNIQUE catches</td><td>409 to second</td><td>Caller retries</td></tr><tr><td>Annotation with PII</td><td>redacted by TASK-MEMORY-111</td><td>stored redacted</td><td>None</td></tr><tr><td>Cycle of depth &gt; 100</td><td>DFS bounded; returns \"indeterminate\"</td><td>conservative: reject</td><td>Operator splits chain</td></tr><tr><td>link_strength missing on create</td><td>default medium</td><td>None</td><td>None</td></tr><tr><td>Batch with &gt; 50 items</td><td>413</td><td>Caller batches</td><td>None</td></tr><tr><td>Batch with mixed valid + invalid</td><td>partial 200; per-item status</td><td>None</td><td>Caller retries invalid</td></tr><tr><td>Graph depth &gt; 5</td><td>400</td><td>Caller adjusts</td><td>None</td></tr><tr><td>Graph depth=5 with high fan-out</td><td>bounded by total nodes 1000</td><td>truncated + warning</td><td>Caller narrows</td></tr><tr><td>Link transfer with all sub-issues already having that link</td><td>DuplicateActive on each</td><td>partial transfer</td><td>Operator</td></tr><tr><td>cites_with_quote &gt; 2KB</td><td>413</td><td>Caller truncates</td><td>None</td></tr><tr><td>metadata &gt; 10KB</td><td>413</td><td>Caller</td><td>None</td></tr><tr><td>review-pending stuck (admin never approves)</td><td>bounded by 30d auto-reject</td><td>None</td><td>Operator</td></tr><tr><td>Traversal sample rate misconfigured</td><td>metric exposes</td><td>tuned via config</td><td>Operator</td></tr><tr><td>Cycle check timeout (very deep chain)</td><td>bounded by DFS depth 100</td><td>408 or 422</td><td>Caller</td></tr><tr><td>Concurrent split + link create</td><td>tx serialises</td><td>one wins</td><td>None</td></tr><tr><td>Audit sampling produces incorrect 0% (RNG bug)</td><td>property test catches</td><td>None</td><td>None</td></tr><tr><td>Graph traversal includes removed links</td><td>default excludes</td><td>include with ?include_removed</td><td>None</td></tr><tr><td>Annotation crash (regex bug in redactor)</td><td>catch_unwind</td><td>500</td><td>Author fixes</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><code>memory_row_id</code> is for linking to specific memory audit chain rows (not memory paths). Some memories don't live at canonical paths (e.g. ad-hoc cross-references); row_id supports them.</li><li><code>subject_can_read_scope</code> integrates with TASK-SKILL-103's allowed_memory_scopes when caller is a skill; for direct API callers, use TASK-AUTH-003's tenant scope.</li><li>Forward-only supersedes uses ns timestamps; memory memory's <code>created_at_ns</code> is from its audit row (canonical).</li><li>Soft-delete preserves history; TASK-PROJ-008 records the link create/remove as history_event rows pegged to the linked issue.</li><li>The <code>?include_removed=true</code> parameter is for auditors; default UI hides removed links.</li><li>Cross-issue links (Issue X links to Issue Y) are NOT in scope here; separate task.</li><li>For high-traffic memories (e.g. DEC-XXX style decision records), incoming-edges query may need a materialised view; slice 3+.</li><li>Annotation text is PII-redacted before storage (reuses TASK-MEMORY-111 ruleset); raw form NOT retained.</li><li>Cycle detection DFS is bounded at depth 100 because real-world supersession chains rarely exceed 10; depth-100 catches malicious or buggy chains.</li><li>link_strength is operator-typed; we considered auto-computed strength (e.g. based on annotation length, surrounding context) but kept it explicit for clarity.</li><li>Batch create uses parallel validation but serial DB insert to preserve order + per-item status accuracy.</li><li>Graph traversal limits: depth ≤ 5, total nodes ≤ 1000. Beyond either bound, response is truncated with <code>truncated: true</code> flag.</li><li>Link transfer on split is a slice-2 operator UI concern; the task specifies the data model + default behaviour.</li><li>cites_with_quote is a separate link type (not annotation extension) because consumers (legal export) treat quotes differently from annotations.</li><li>metadata JSONB is per-link; tenant-specific extension without table churn.</li><li>review-pending links are stored but excluded from default queries; admin endpoint shows them all.</li><li>Traversal audit sampling at 10% balances analytics signal against audit-row volume; configurable per tenant.</li><li>Cycle detection runs in-memory after fetching the relevant subgraph; bounded by depth × fan-out.</li><li>The 30-day auto-reject for review-pending links prevents indefinite limbo; expired ones become removed with reason \"review timeout.\"</li><li>We chose UUIDs for link ids over auto-increment because cross-tenant ID collisions matter for cross-system references.</li><li>The graph endpoint's <code>types</code> filter accepts a comma-separated list of link types to traverse; default = all types.</li><li>For very high-degree memories (&gt;1k incoming links), the <code>list_issues_by_memory</code> query uses keyset pagination internally.</li><li>Cycle detection considers only <code>supersedes</code> edges (cites/implements don't have temporal direction).</li><li>The annotation's 500-char limit matches TASK-PROJ-002 reason field for consistency.</li><li>batch's 50-item limit chosen because: (a) typical bulk import is &lt; 20; (b) &gt; 50 timeouts become user-facing.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-009.</em></p>";
