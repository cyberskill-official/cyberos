window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-145"]="<h1>TASK-IMP-145: TRACE citation truth for the skill quality floor</h1>\n<h2>Summary</h2>\n<p><code>TASK-SKILL-202</code> is <code>done</code> and every one of its seven acceptance criteria cites <code>tools/install/tests/test_skill_floor.sh</code> — a file that was never created. The floor it describes shipped as <code>scripts/tests/test_skill_stub_lint.sh</code> and is green. This task repoints the citations at the suite that exists, adds the three small checks needed so no criterion cites nothing, records the one half that stayed deferred, and fixes the same stale paths where benchmark gates G7 and G8 inherited them.</p>\n<h2>Problem</h2>\n<p><code>TASK-SKILL-202</code>'s own <code>testing-evidence.md</code> names the divergence under the heading \"Cited suite (TRACE-004) — path residual\": the implementation put the floor in <code>scripts/tests/test_skill_stub_lint.sh</code> (7 tests, green, auto-registered by <code>run_all.sh</code>'s glob), while the spec's <code>new_files:</code> and all seven AC lines kept naming <code>tools/install/tests/test_skill_floor.sh</code> plus a <code>tools/install/check-skill-floor.sh</code> checker whose build wiring was the optional F4 half and never landed. Gate-1 accepted the deviation and asked gate-2 to confirm; gate-2 accepted the task and the citations stayed.</p>\n<p>Two other surfaces inherited the same paths: <code>docs/verification/benchmark-gates.md</code> (G7 and G8 Owner + Checked-files rows) and the embedded G7/G8 definitions inside <code>TASK-IMP-140</code>'s spec, which is that doc's content contract.</p>\n<p>So the corpus carries a shipped task whose acceptance resolves nowhere and two benchmark gates naming a checker nobody can run. <code>verify-goals.mjs</code> refuses exactly this shape for goal predicates (\"the acceptance cites a test that no longer exists, which IS the finding\"); spec-level citations have no such guard, which is why this one survived a <code>done</code> flip.</p>\n<h2>Proposed Solution</h2>\n<p>Repoint each SKILL-202 criterion at the real function that proves it and correct the <code>new_files:</code> list to what shipped. Three clauses have no matching test today — §1.1 (delist + allowlist cleanliness), §1.2 (the <code>certify-nfrs.md</code> degradation notice) and §1.7 (the CHANGELOG record) — so rather than let those ACs cite nothing, add <code>t08_delist_and_allowlist_clean</code>, <code>t09_workflow_degrades_loud</code> and <code>t10_changelog_records_floor</code> to the existing suite: three greps over <code>build.sh</code>, <code>chain-allowlist.txt</code>, <code>certify-nfrs.md</code> and <code>CHANGELOG.md</code>, in the suite's existing counter/exit style. Rewrite SKILL-202 §1.5/§1.6 to describe the as-built mechanism (the detector is the suite's <code>floor_check</code>; the standalone checker and its build wiring stayed deferred) and record that deviation in <code>source_decisions</code>, so the normative text stops promising a file the repo does not have. Finally, correct the G7/G8 Owner and Checked-files rows in <code>benchmark-gates.md</code> and in TASK-IMP-140's embedded definitions, leaving severities and tiers untouched so <code>test_benchmark_gates.sh::t01</code>'s spec↔doc cross-check keeps passing.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Create <code>tools/install/tests/test_skill_floor.sh</code> as a shim that calls the real suite.</strong> Rejected: a second registration for one behaviour is the failure mode <code>run_all.sh</code>'s glob header argues against, and the shim would make the stale name permanent rather than retired.</li><li><strong>Build the missing <code>check-skill-floor.sh</code> + build wiring so the citations become true as written.</strong> Rejected for this task: that is real product work (a build-blocking gate), it was consciously deferred at ship time, and it does not need to gate the correction of a citation. Recording the deferral honestly is the smaller true statement.</li><li><strong>Leave the spec alone; the residual is documented in the evidence file.</strong> Rejected: the documentation is one file deep and the corpus surface (spec ACs, <code>new_files:</code>, two benchmark-gate rows) still reads as if the checker exists. A note that contradicts the artefact it annotates loses to the artefact.</li><li><strong>Repoint the citations and skip the three new tests.</strong> Rejected: three of the seven ACs would then cite nothing at all, which is worse than citing a wrong path — at least a wrong path is greppable.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: zero live references to <code>test_skill_floor.sh</code> / <code>check-skill-floor.sh</code> outside the historical <code>docs/batches/</code> ship notes, every SKILL-202 AC resolving to a function that exists, and <code>scripts/tests/test_skill_stub_lint.sh</code> green at 10/10.</li><li>Guardrail: <code>scripts/tests/test_benchmark_gates.sh</code> and the full <code>run_all.sh</code> stay green; no vendored skill, no build behaviour and no gate severity/tier changes.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the three new suite functions, SKILL-202's frontmatter + §1.5/§1.6 + AC block, the G7/G8 rows in <code>benchmark-gates.md</code> and TASK-IMP-140's spec, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Building <code>tools/install/check-skill-floor.sh</code> or wiring a floor check into <code>build.sh</code> (the deferred F4 half stays deferred — now recorded, not implied done).</li><li>Re-litigating any SKILL-202 outcome: the delisting, the 20 injection-discipline backports and the SCOPE expansion all shipped and are re-verified by the suite this task cites.</li><li>The historical <code>docs/batches/batch-8b-*</code> notes, which correctly record what was true on the day they were written.</li></ul>\n<h2>Dependencies</h2>\n<p>None. <code>TASK-SKILL-202</code> and <code>TASK-IMP-140</code> are both <code>done</code>; this task edits their citation surfaces only, changing no clause's meaning.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) auditing the post-1.2.0 plan's Wave 2 residuals against live <code>main</code>.</li><li><strong>Scope:</strong> the audit's own figures were treated as claims to verify, not as inputs. <strong>re-derived and CONFIRMED:</strong> <code>scripts/tests/test_skill_stub_lint.sh</code> passes 7/7 at HEAD, and its t01–t07 function names are as <code>testing-evidence.md</code> records them. <strong>re-derived and CORRECTED:</strong> the plan and the batch-8b notes describe the residual as a SKILL-202 spec matter only; a repo-wide grep at HEAD found the same stale paths in <code>docs/verification/benchmark-gates.md</code> and in TASK-IMP-140's embedded G7/G8 definitions, so the scope is four files, not one. <strong>measured and ADDED:</strong> three SKILL-202 clauses (§1.1, §1.2, §1.7) have no covering test in the as-built suite — measured by mapping each clause to the suite's functions — which is why this task adds t08–t10 rather than only repointing citations. No unscoped attestation is made about clauses outside that mapping.</li><li><strong>Human review:</strong> session operator instruction (2026-07-25) to close genuine plan gaps; HITL verdicts recorded with evidence at both gates.</li></ul>\n<h2>1. Description</h2>\n<ul><li>1.1 <code>scripts/tests/test_skill_stub_lint.sh</code> MUST gain <code>t08_delist_and_allowlist_clean</code>: <code>tools/install/build.sh</code>'s <code>VENDORED_SKILLS</code> list contains no <code>nfr-</code> entry and <code>tools/install/chain-allowlist.txt</code> contains no <code>nfr-</code> line.</li><li>1.2 The suite MUST gain <code>t09_workflow_degrades_loud</code>: <code>certify-nfrs.md</code> (source and vendored copies that exist) states at its routing step that the NFR skills are not yet shipped.</li><li>1.3 The suite MUST gain <code>t10_changelog_records_floor</code>: <code>CHANGELOG.md</code> records the delisting naming <code>TASK-CUO-209</code>, the backport count, and the parity SCOPE expansion.</li><li>1.4 Every acceptance criterion in <code>TASK-SKILL-202</code>'s spec MUST cite a function that exists in <code>scripts/tests/test_skill_stub_lint.sh</code>, and its <code>new_files:</code> MUST name what shipped rather than paths that were never created.</li><li>1.5 <code>TASK-SKILL-202</code> §1.5 and §1.6 MUST describe the as-built mechanism (floor detector inside the suite) and MUST record the deferred standalone-checker/build-wiring half as a named deviation rather than an unmet MUST.</li><li>1.6 <code>docs/verification/benchmark-gates.md</code> G7 and G8 MUST name <code>scripts/tests/test_skill_stub_lint.sh</code> in their Owner and Checked-files rows, and <code>TASK-IMP-140</code>'s embedded G7/G8 definitions MUST match — severities and tiers unchanged.</li><li>1.7 <code>CHANGELOG.md</code> Unreleased MUST record the citation repair.</li><li>1.8 The suite MUST gain <code>t11_skill_202_citations_resolve</code>, which parses every <code>test: &lt;path&gt;::&lt;fn&gt;</code> citation out of <code>TASK-SKILL-202</code>'s acceptance block and fails when the path is absent or the function is not defined in it — so this class of rot cannot survive another <code>done</code> flip silently.</li></ul>\n<h2>Acceptance Criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) — <code>build.sh</code>'s vendored list and <code>chain-allowlist.txt</code> carry no <code>nfr-</code> entry, and the check fails on a fixture that re-adds one - test: <code>scripts/tests/test_skill_stub_lint.sh::t08_delist_and_allowlist_clean</code></li><li>[ ] AC 2 (traces_to: #1.2) — every <code>certify-nfrs.md</code> copy states the NFR skills are not yet shipped at its routing step - test: <code>scripts/tests/test_skill_stub_lint.sh::t09_workflow_degrades_loud</code></li><li>[ ] AC 3 (traces_to: #1.3, #1.7) — <code>CHANGELOG.md</code> records the delisting naming TASK-CUO-209, the backport count and the SCOPE expansion, plus this task's citation repair - test: <code>scripts/tests/test_skill_stub_lint.sh::t10_changelog_records_floor</code></li><li>[ ] AC 4 (traces_to: #1.4, #1.5, #1.8) — every SKILL-202 acceptance citation resolves to a function defined in the cited file, and the deferred standalone-checker half is named in the spec rather than left as an unmet MUST - test: <code>scripts/tests/test_skill_stub_lint.sh::t11_skill_202_citations_resolve</code></li><li>[ ] AC 5 (traces_to: #1.6) — G7/G8 name the real suite in their Owner and Checked-files rows while the spec↔doc severity/tier cross-check stays green - test: <code>scripts/tests/test_benchmark_gates.sh::t01_doc_complete_and_consistent</code></li></ul>\n<h2>Test plan</h2>\n<ol><li><code>bash scripts/tests/test_skill_stub_lint.sh</code></li><li><code>bash scripts/tests/test_benchmark_gates.sh</code></li><li><code>bash scripts/tests/run_all.sh</code></li><li><code>bash .cyberos/cuo/gates/run-gates.sh</code></li></ol>\n<h2>3. Edge cases</h2>\n<ul><li><strong><code>certify-nfrs.md</code> exists in more than one place (source + built payload).</strong> t09 checks every copy it finds and fails naming the copy that lost the notice, rather than passing on the first hit.</li><li><strong>A future release rewrites the CHANGELOG's Unreleased section.</strong> t10 keys on the delisting record anywhere in the file, not on the top entry, so a release rollup does not turn a true statement into a red test.</li><li><strong>The NFR skills are genuinely implemented later and re-vendored.</strong> t08 then fails loudly, which is correct: re-vendoring is a reviewed change that must revisit SKILL-202's delisting record in the same commit.</li><li><strong>Security-class:</strong> documentation and test-only; the new functions read and pattern-match repo files, execute nothing, and add no network or secret surface.</li></ul>";
