window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-005"]="<h1>TASK-OBS-005: W3C TraceContext correlation</h1>\n<h2>Summary</h2>\n<p>Propagate W3C TraceContext across CyberOS services and embed <code>trace_id</code> into structured logs and RED histogram exemplars. As-built surface lives in <code>services/shared/cyberos-obs-sdk/src/{tracecontext,logging,exemplar}.rs</code> with <code>red.rs</code> counters (<code>record_tracecontext_extracted</code>, exemplar emission), the ai-gateway <code>trace_ctx</code> axum middleware in <code>services/ai-gateway/src/server/mod.rs</code>, and the auth JWT <code>traceparent</code> claim in <code>services/auth/src/jwt.rs</code>.</p>\n<h2>Problem</h2>\n<p>The original engineering-spec claimed <code>crates/cyberos-obs-sdk/</code>, separate integration test crates (<code>tracecontext_test.rs</code>, <code>end_to_end_correlation_test.rs</code>), <code>.github/workflows/obs-correlation-gate.yml</code>, and trace middleware on chat/memory. The live SDK is <code>services/shared/cyberos-obs-sdk/</code>; ai-gateway ships <code>trace_ctx</code> middleware; auth carries <code>traceparent</code> in JWT claims. The process evidence failed FM-004 (task@1 frontmatter + <code>## §N</code> body), <code>depends_on</code> incorrectly included TASK-OBS-004, and phantom paths blocked re-entry.</p>\n<h2>Proposed Solution</h2>\n<p>Adopt the as-built layout:</p>\n<ul><li><code>tracecontext.rs</code> — strict W3C version-00 parse, <code>extract_traceparent</code>, <code>inject_traceparent</code>, <code>hash16</code> for malformed values</li><li><code>logging.rs</code> — <code>request_span(trace_id, span_id, tenant_id)</code> + <code>init_json_subscriber</code> (span-scope JSON rendering)</li><li><code>exemplar.rs</code> — <code>record_with_exemplar</code> on <code>cyberos_duration_ms</code> via <code>red::record_request</code></li><li><code>red.rs</code> — <code>record_tracecontext_extracted(outcome)</code> with <code>extracted | malformed | missing_generated_new</code></li><li>ai-gateway <code>trace_ctx</code> — extract/generate, metric emit, <code>request_span</code> instrumentation, response <code>traceparent</code> echo</li><li>auth <code>jwt.rs</code> — optional <code>traceparent</code> claim round-trips through issue/verify</li></ul>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Resume the old engineering-spec as-is.</strong> Rejected: FM-004 blocks re-entry; paths and CI gate lie.</li><li><strong>Custom OTel-context tracing Layer (original §3 sketch).</strong> Rejected: as-built uses <code>tracing::Span</code> + JSON subscriber — no OTel tracer provider required at the log boundary; verifiable with in-memory capture.</li><li><strong>Block on TASK-OBS-004 LangSmith correlation before adopt.</strong> Rejected: OBS-004 is soft-related; slice-2 correlation primitives stand alone.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: every ai-gateway request carries a valid W3C trace context; logs emitted inside the request span include <code>trace_id</code> + <code>tenant_id</code>; duration histogram records exemplars.</li><li>Guardrail: <code>obs_tracecontext_extracted_total{outcome}</code> increments on the three middleware paths; JWT issue/verify preserves <code>traceparent</code>.</li></ul>\n<h2>Scope</h2>\n<p>In scope (as-built):</p>\n<ul><li><code>services/shared/cyberos-obs-sdk/src/{tracecontext,logging,exemplar,red}.rs</code></li><li>ai-gateway <code>trace_ctx</code> middleware + inline server tests + <code>tests/otel_propagation_test.rs</code></li><li>auth JWT <code>traceparent</code> claim (<code>jwt.rs</code>, <code>jwt_roundtrip_test.rs</code>)</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>TASK-OBS-004 LangSmith / AI-trace correlation ledger (soft: TASK-AI-022 OTel propagation helpers only)</li><li>Deleted <code>.github/workflows/obs-correlation-gate.yml</code> and <code>end_to_end_correlation_test.rs</code> (no live CI gate querying Loki + Tempo + LangSmith + Prometheus)</li><li>chat/memory HTTP <code>trace_ctx</code> middleware (not wired in this slice; auth README notes outbound RPC propagation deferred)</li><li><code>InstrumentedClient</code> HTTP wrapper (outgoing inject lives in TASK-AI-022 <code>propagation</code> module for ai-gateway router backends)</li><li>Subprocess <code>OTEL_TRACE_ID</code> env propagation and tokio-spawn <code>Instrument</code> enforcement beyond what ai-gateway already does</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-OBS-001, TASK-OBS-003]</code>. Soft: TASK-AI-022 (OTel propagation helpers in ai-gateway); TASK-OBS-004 (LangSmith trace_id alignment — not a hard gate for this adopt).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>parse_w3c_traceparent</code> and <code>extract_traceparent</code> MUST enforce W3C version-00 strictly: <code>00-{32 lower-hex}-{16 lower-hex}-{2 lower-hex flags}</code>; reject wrong version, wrong lengths, uppercase hex, and all-zero trace/span ids (DEC-160, DEC-162).</li><li>1.2 At every HTTP boundary, a missing <code>traceparent</code> MUST generate a fresh trace context; a malformed header MUST generate a fresh context and MUST NOT honour attacker-supplied ids. Requests MUST NOT be rejected — TraceContext is operational, not security (DEC-160).</li><li>1.3 Every structured log line emitted while handling a request MUST carry <code>trace_id</code>, <code>span_id</code>, and <code>tenant_id</code> via the canonical <code>request_span</code> + a JSON subscriber that renders the span scope (<code>logging.rs</code>, DEC-161).</li><li>1.4 <code>cyberos_duration_ms</code> histogram samples MUST pass through <code>exemplar::record_with_exemplar</code> so the OTel→Prometheus exporter can attach trace exemplars when a trace is in context (DEC-161, TASK-OBS-003).</li><li>1.5 <code>record_tracecontext_extracted(outcome)</code> MUST increment <code>obs_tracecontext_extracted_total{outcome}</code> for <code>extracted</code>, <code>malformed</code>, and <code>missing_generated_new</code> at the request boundary.</li><li>1.6 The ai-gateway <code>trace_ctx</code> middleware MUST stamp <code>RequestTrace</code> on the request, instrument the handler future with <code>request_span</code>, and echo the resolved <code>traceparent</code> on the response.</li><li>1.7 Auth JWT issue/verify MUST round-trip an optional W3C <code>traceparent</code> claim so token issuance stitches to the inbound request trace (AUTHORING §3.7 rule 22).</li><li>1.8 This adopt MUST NOT claim OBS-004 LangSmith correlation, the deleted <code>obs-correlation-gate.yml</code>, or chat/memory HTTP middleware as shipped.</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - strict W3C parse accepts valid / rejects bad shapes - test: <code>services/shared/cyberos-obs-sdk/src/tracecontext.rs::parses_a_valid_traceparent</code></li><li>[ ] AC 2 (traces_to: #1.1) - rejects non-00 version and wrong lengths - test: <code>services/shared/cyberos-obs-sdk/src/tracecontext.rs::rejects_non_zero_version</code></li><li>[ ] AC 3 (traces_to: #1.2) - extract returns Missing / Malformed(hash16) / Ok - test: <code>services/shared/cyberos-obs-sdk/src/tracecontext.rs::extract_missing_then_malformed_then_valid</code></li><li>[ ] AC 4 (traces_to: #1.2) - middleware generates valid traceparent when absent - test: <code>services/ai-gateway/src/server/mod.rs::response_carries_a_generated_traceparent_when_absent</code></li><li>[ ] AC 5 (traces_to: #1.6) - middleware echoes valid inbound traceparent - test: <code>services/ai-gateway/src/server/mod.rs::response_echoes_a_valid_inbound_traceparent</code></li><li>[ ] AC 6 (traces_to: #1.3) - log JSON carries trace_id and tenant_id inside request span - test: <code>services/shared/cyberos-obs-sdk/src/logging.rs::an_event_inside_the_request_span_carries_trace_id_and_tenant_id</code></li><li>[ ] AC 7 (traces_to: #1.4) - exemplar record is safe no-op before init - test: <code>services/shared/cyberos-obs-sdk/src/red.rs::record_with_exemplar_is_a_safe_noop_on_a_noop_meter</code></li><li>[ ] AC 8 (traces_to: #1.5) - tracecontext extracted counter safe before init - test: <code>services/shared/cyberos-obs-sdk/src/red.rs::record_tracecontext_extracted_is_a_safe_noop_before_init</code></li><li>[ ] AC 9 (traces_to: #1.7) - JWT issue then verify preserves traceparent - test: <code>services/auth/tests/jwt_roundtrip_test.rs</code></li><li>[ ] AC 10 (traces_to: #1.1) - hash16 is 16 hex chars, deterministic - test: <code>services/shared/cyberos-obs-sdk/src/tracecontext.rs::hash16_is_deterministic_16_hex</code></li><li>[ ] AC 11 (traces_to: #1.8) - Out of scope lists OBS-004 gate + chat/memory middleware - verify: this spec Scope / Out of scope</li></ul>\n<h2>Verification</h2>\n<pre><code class=\"language-bash\">cd services &amp;&amp; cargo test -p cyberos-obs-sdk tracecontext\ncd services &amp;&amp; cargo test -p cyberos-obs-sdk logging\ncd services &amp;&amp; cargo test -p cyberos-obs-sdk red::\ncd services &amp;&amp; cargo test -p cyberos-ai-gateway trace_ctx\ncd services &amp;&amp; cargo test -p cyberos-ai-gateway --test otel_propagation_test\n# jwt_roundtrip (ignored without Postgres):\ncd services &amp;&amp; cargo test -p cyberos-auth jwt_roundtrip -- --ignored</code></pre>\n<table><thead><tr><th>Path</th><th>Covers</th></tr></thead><tbody><tr><td><code>cyberos-obs-sdk/src/tracecontext.rs</code> (inline <code>#[cfg(test)]</code>)</td><td>W3C parse, extract, inject, hash16</td></tr><tr><td><code>cyberos-obs-sdk/src/logging.rs</code> (inline <code>#[cfg(test)]</code>)</td><td>Span-scope log enrichment</td></tr><tr><td><code>cyberos-obs-sdk/src/red.rs</code> (inline <code>#[cfg(test)]</code>)</td><td>Exemplar + tracecontext counter no-op safety</td></tr><tr><td><code>ai-gateway/src/server/mod.rs</code> (inline tests)</td><td><code>trace_ctx</code> generate + echo</td></tr><tr><td><code>ai-gateway/tests/otel_propagation_test.rs</code></td><td>TASK-AI-022 inject/extract roundtrip</td></tr><tr><td><code>auth/tests/jwt_roundtrip_test.rs</code></td><td>JWT <code>traceparent</code> claim</td></tr></tbody></table>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) on branch <code>batch/9b-obs</code>.</li><li><strong>Scope:</strong> Re-spec/adopt against as-built <code>cyberos-obs-sdk</code> + ai-gateway <code>trace_ctx</code> + auth JWT claim; dropped TASK-OBS-004 from <code>depends_on</code>; ledgered OBS-004 / CI gate / chat-memory middleware as Out of scope.</li><li><strong>Human review:</strong> Required at the two HITL gates (<code>entered_via: rework</code>, <code>routed_back_count: 1</code>).</li></ul>\n<hr>\n<p><em>batch/9b-obs adopt — TASK-OBS-005 re-spec against as-built TraceContext correlation.</em></p>";
