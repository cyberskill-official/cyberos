window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-APP-001"]="<h1>TASK-APP-001: Desktop CyberOS operations UI</h1>\n<h2>Summary</h2>\n<p>Expose CyberOS install operations from the Tauri + Svelte desktop shell at <code>services/memory/desktop/</code>: Build payload (<code>tools/install/build.sh</code>), list projects under <code>~/Projects</code>, Check via <code>dist/cyberos/version.sh</code>, and Install/Update via <code>dist/cyberos/install.sh</code>. As-built surface is <code>ops.rs</code> + the \"CyberOS Ops\" tab in <code>App.svelte</code>, documented in <code>tools/install/docs/guides/desktop-ops.md</code>.</p>\n<h2>Problem</h2>\n<p>IMP-139 left this task at <code>implementing</code> (operator resume). The UI and Rust commands already shipped, but the engineering-spec still claimed <code>install.sh --check</code> (does not exist), lacked task@1 adopt grammar / authorship disclosure, and had no audit. Reconcile dossier velocity evidence also conflated <code>apps/desktop</code> (console webview) with the ops shell.</p>\n<h2>Proposed Solution</h2>\n<p>Adopt the as-built ops path:</p>\n<ul><li><code>ops_build</code> → <code>bash tools/install/build.sh</code></li><li><code>ops_check</code> → <code>bash dist/cyberos/version.sh &lt;project&gt;</code> with <code>CYBEROS_NONINTERACTIVE=1</code> (IMP-070 key=value lines)</li><li><code>ops_install</code> → <code>bash dist/cyberos/install.sh &lt;project&gt;</code></li><li>Settings at <code>~/.cyberos/desktop-ops.json</code>; default checkout <code>~/Projects/CyberSkill/cyberos</code></li><li>Guards: checkout must contain <code>tools/install/build.sh</code>; payload scripts must exist; target must be a git repo; target ≠ checkout root</li><li>Residual unit tests for the checkout / project guards</li></ul>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Route back and rewrite.</strong> Rejected: operator Gate-2 said resume; code is live.</li><li><strong>Add <code>install.sh --check</code> alias.</strong> Rejected: duplicates <code>version.sh</code>; IMP-070 already owns the machine-parseable contract.</li><li><strong>Treat <code>apps/desktop</code> as this task.</strong> Rejected: that crate is the console webview wrapper; ops live under <code>services/memory/desktop/</code>.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: Build / Check / Install shell the canonical scripts; guards fail closed; Ops tab surfaces stdout+stderr and non-zero exits.</li><li>Guardrail: <code>cargo check</code> + <code>cargo test</code> green on <code>services/memory/desktop/src-tauri</code>.</li></ul>\n<h2>Scope</h2>\n<p>In scope (as-built):</p>\n<ul><li><code>services/memory/desktop/src-tauri/src/ops.rs</code> and command registration in <code>main.rs</code></li><li>Ops tab in <code>services/memory/desktop/src/App.svelte</code></li><li><code>tools/install/docs/guides/desktop-ops.md</code></li><li>Guard unit tests in <code>ops.rs</code></li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li><code>apps/desktop/</code> console webview releases (separate APP surface)</li><li>Native folder picker dialog (optional follow-up)</li><li>Reimplementing install/init inside Rust</li><li>Live Mac GUI smoke in CI (operator Mac; machine gate is cargo check/test)</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: []</code>. Soft: TASK-MEMORY-104 (desktop shell scaffold); TASK-IMP-142 (resume schedule).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The Ops UI MUST expose Build payload, project list + manual path, Check, and Install/Update.</li><li>1.2 Operations MUST shell out to <code>tools/install/build.sh</code>, <code>dist/cyberos/version.sh</code>, and <code>dist/cyberos/install.sh</code> — no reimplementation of init logic in the app.</li><li>1.3 Checkout path MUST be configurable, persisted, and default to <code>~/Projects/CyberSkill/cyberos</code>.</li><li>1.4 Command stdout+stderr MUST be returned to the UI; non-zero exit MUST set <code>ok: false</code>.</li><li>1.5 Before running, the app MUST validate checkout (<code>tools/install/build.sh</code> present) and, for Check/Install, the required payload script under <code>dist/cyberos/</code>.</li><li>1.6 Install MUST refuse the CyberOS checkout itself and refuse non-git targets.</li><li>1.7 Ops MUST NOT push, commit, or delete anything beyond what the canonical scripts already do.</li><li>1.8 This adopt MUST NOT claim <code>apps/desktop</code> or <code>install.sh --check</code> as the shipped Check path.</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1,#1.2) - ops_build shells build.sh - verify: <code>services/memory/desktop/src-tauri/src/ops.rs::ops_build</code></li><li>[ ] AC 2 (traces_to: #1.2) - ops_check shells version.sh with CYBEROS_NONINTERACTIVE - verify: <code>services/memory/desktop/src-tauri/src/ops.rs::ops_check</code></li><li>[ ] AC 3 (traces_to: #1.2) - ops_install shells install.sh - verify: <code>services/memory/desktop/src-tauri/src/ops.rs::ops_install</code></li><li>[ ] AC 4 (traces_to: #1.3) - settings default + persist path - verify: <code>services/memory/desktop/src-tauri/src/ops.rs::ops_get_settings</code></li><li>[ ] AC 5 (traces_to: #1.4) - OpResult carries ok + combined stdout/stderr - verify: <code>services/memory/desktop/src-tauri/src/ops.rs::OpResult</code></li><li>[ ] AC 6 (traces_to: #1.5) - missing build.sh rejected - test: <code>services/memory/desktop/src-tauri/src/ops.rs::require_checkout_rejects_non_checkout</code></li><li>[ ] AC 7 (traces_to: #1.6) - checkout-as-project refused - test: <code>services/memory/desktop/src-tauri/src/ops.rs::require_project_rejects_checkout_root</code></li><li>[ ] AC 8 (traces_to: #1.6) - non-git path refused - test: <code>services/memory/desktop/src-tauri/src/ops.rs::require_project_rejects_non_git</code></li><li>[ ] AC 9 (traces_to: #1.1) - Ops tab invokes ops_* commands - verify: <code>services/memory/desktop/src/App.svelte</code></li><li>[ ] AC 10 (traces_to: #1.7,#1.8) - Out of scope lists apps/desktop, install.sh --check, and non-destructive Ops - verify: this spec Scope / Out of scope / disallowed_tools</li></ul>\n<h2>Verification</h2>\n<pre><code class=\"language-bash\">cd services/memory/desktop/src-tauri &amp;&amp; cargo check\ncd services/memory/desktop/src-tauri &amp;&amp; cargo test</code></pre>\n<table><thead><tr><th>Path</th><th>Covers</th></tr></thead><tbody><tr><td><code>ops.rs</code> commands</td><td>Build / Check / Install / list / settings</td></tr><tr><td><code>ops.rs</code> unit tests</td><td>Checkout + project guards</td></tr><tr><td><code>App.svelte</code></td><td>Ops tab wiring</td></tr><tr><td><code>tools/install/docs/guides/desktop-ops.md</code></td><td>Operator contract</td></tr></tbody></table>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) on branch <code>batch/9c-app</code>.</li><li><strong>Scope:</strong> Re-spec/adopt against as-built <code>services/memory/desktop</code> ops; residual guard tests; deferred native folder dialog ledgered.</li><li><strong>Human review:</strong> Session HITL override 2026-07-24 (IMP-139 resume path).</li></ul>\n<hr>\n<p><em>batch/9c-app adopt — TASK-APP-001 re-spec against as-built memory desktop Ops.</em></p>";
