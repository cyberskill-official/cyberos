window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TEN-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The Metering service <strong>MUST</strong> emit one append-only <code>metering_events</code> row per quantifiable resource consumption event along exactly four closed axes — <strong>seats, api_calls, ai_tokens, storage_bytes</strong> — and <strong>MUST</strong> simultaneously emit one memory audit chain row per event for tamper-evident reconciliation.</p>\n<ol><li><strong>MUST</strong> define the closed 4-value Postgres enum <code>metering_axis = ('seats', 'api_calls', 'ai_tokens', 'storage_bytes')</code> with a CI cardinality test (<code>SELECT count(*) FROM pg_enum WHERE enumtypid = 'metering_axis'::regtype</code> equals 4). Adding a new axis requires a schema migration + a DEC-XXX decision entry (DEC-704).</li></ol>\n<ol><li><strong>MUST</strong> persist every metric event to the <code>metering_events</code> Postgres table as an append-only row. The table is created with <code>REVOKE UPDATE, DELETE FROM cyberos_app</code>. Only the privileged <code>metering_writer</code> role holds INSERT privilege; only <code>metering_reader</code> holds SELECT. The reversal pathway for refunds is via the FR-TIME-001 <code>correction_to</code> self-FK pattern — never an UPDATE/DELETE (DEC-702 + DEC-711).</li></ol>\n<ol><li><strong>MUST</strong> enforce idempotent recording via a <code>UNIQUE (tenant_id, axis, idempotency_key)</code> constraint on <code>metering_events</code>. The 24-hour retention window for idempotency keys is enforced by a partial index. Duplicate inserts MUST return success (idempotent semantics) without emitting a second memory audit row (DEC-715).</li></ol>\n<ol><li><strong>MUST</strong> emit exactly one memory audit chain row per metric event before the Postgres COMMIT. The audit row carries <code>{axis, tenant_id, quantity, unit, idempotency_key, occurred_at, source_service, postgres_event_id}</code> — no free-text descriptions, no PII. The Postgres row id and the memory chain hash MUST be cross-linked (event row stores <code>memory_chain_hash</code>, memory row stores <code>postgres_event_id</code>). Mismatch detected at close-period reconciliation triggers a sev-1 audit row (DEC-712 + DEC-714).</li></ol>\n<ol><li><strong>MUST</strong> route every memory audit row through the FR-MEMORY-111 pre-ingest PII detection layer. The metering payload is structured (no free text), so the detector returns \"clean\" 100% of the time in steady state; any non-clean result MUST be treated as a sev-1 ingest defect (the metering writer is the source of \"clean payloads only\" — if it emits PII, that is a code bug).</li></ol>\n<ol><li><strong>MUST</strong> integrate API-call metering at the auth middleware hot path (FR-AUTH-003 RLS-aware request scope). Every successful RLS-scoped HTTP request emits one <code>api_calls</code> event with <code>quantity = 1</code> and <code>unit = \"request\"</code>. Failed-auth requests do NOT emit (rejected at auth, not billed). The middleware writes via the WAL queue (§1 #14) on the response path — never on the request path — so a metering writer outage cannot impact request latency (≤ 200µs p99 overhead budget).</li></ol>\n<ol><li><strong>MUST</strong> integrate AI-token metering at the FR-AI-001 cost-ledger postcall reconcile hook. When <code>cost_ledger::postcall_reconcile()</code> resolves the actual provider response, it emits one <code>ai_tokens</code> event with <code>quantity = input_tokens + output_tokens</code>, <code>unit = \"token\"</code>, and <code>extra = {model_alias, provider, input_tokens, output_tokens}</code>. The reconcile path is already on the cost-ledger code path, so there is no additional hot-path overhead.</li></ol>\n<ol><li><strong>MUST</strong> snapshot seats at billing-period-close (last calendar day of the billing month at 23:59:59 UTC of the tenant's billing timezone, per DEC-705). The snapshot job queries <code>SELECT COUNT(*) FROM members WHERE tenant_id = $1 AND active_at IS NOT NULL AND deactivated_at IS NULL</code> and writes ONE <code>seats</code> event with <code>quantity = N</code>, <code>unit = \"seat\"</code>. Seats are NOT metered as join/leave events — the closing snapshot is authoritative. If a member is added and removed within the period, neither shows up in metering.</li></ol>\n<ol><li><strong>MUST</strong> snapshot storage at billing-period-close via two sub-counts (DEC-706):</li></ol>\n<ul><li><strong>S3 bytes</strong>: queries the S3 inventory report (CSV-format daily inventory delivered to <code>s3://cyberos-inventory/&lt;tenant&gt;/&lt;date&gt;.csv</code>) and sums <code>Size</code> column. The inventory is delivered with up to 24h lag, so the close job runs at +24h after period end.</li><li><strong>Postgres bytes</strong>: queries <code>pg_total_relation_size</code> summed across the tenant's RLS-scoped tables. The query joins <code>pg_class</code> to <code>cyberos.tenants</code> to derive size per tenant.</li><li>Emits ONE <code>storage_bytes</code> event with <code>quantity = s3_bytes + postgres_bytes</code>, <code>unit = \"byte\"</code>.</li></ul>\n<ol><li><strong>MUST</strong> define a closed 3-value <code>metering_overage_policy</code> enum (<code>block</code>, <code>warn</code>, <code>allow</code>). The per-tenant policy lives in <code>tenants.metering_overage_policy_yaml</code> and defaults to <code>warn</code>. Mutation requires <code>cfo</code> role per FR-AUTH-101 + a sev-2 memory audit row with a non-empty reason (DEC-710). The CI cardinality test asserts exactly 3 enum values.</li></ol>\n<ol><li><strong>MUST</strong> enforce overage policy at the API-call middleware:</li></ol>\n<ul><li><code>block</code>: when <code>current_period_api_calls + 1 &gt; monthly_cap</code>, return <code>402 PAYMENT_REQUIRED</code> with body <code>{error: \"overage_blocked\", axis: \"api_calls\", current, cap}</code>. No <code>metering_events</code> row is emitted (the call is rejected upstream).</li><li><code>warn</code>: when crossing <code>monthly_cap × warn_threshold</code> (default 0.80), emit a <code>metering.warn_threshold_crossed</code> memory audit row at sev-2. The metric event proceeds normally.</li><li><code>allow</code>: no enforcement; metric event proceeds; CFO reviews at period close.</li></ul>\n<ol><li><strong>MUST</strong> define a <code>metering_holds</code> table parallel to FR-AI-001's <code>cost_ledger_holds</code> for high-value operations (e.g., a bulk API ingest claims a 10,000-call hold before issuing). The hold uses the same 60s TTL + idempotency contract as the cost-ledger hold and emits a <code>metering.hold_claimed</code> memory audit row.</li></ol>\n<ol><li><strong>MUST</strong> expose a <code>GET /v1/usage</code> REST endpoint that returns the current billing period's usage along all four axes for the caller's tenant. The handler hits the <code>metering_current_period</code> materialized view (refreshed every 5 minutes per DEC-713), not the raw <code>metering_events</code> table. Response shape: ``<code>json { \"tenant_id\": \"ten_abc\", \"period_start\": \"2026-05-01T00:00:00Z\", \"period_end\": \"2026-05-31T23:59:59Z\", \"seats\": 12, \"api_calls\": 4823901, \"ai_tokens\": 12834729, \"storage_bytes\": 38291723123, \"overage_policy\": \"warn\", \"warn_thresholds_crossed\": [\"api_calls\"] } </code>``</li></ol>\n<ol><li><strong>MUST</strong> route every metering write through a bounded WAL queue (DEC-709) — 100,000-event in-memory ring buffer per writer. On Postgres outage, events queue in memory; on memory pressure (queue &gt; 90%), the writer logs a sev-1 memory audit row and rejects new events (back-pressure to caller). On Postgres recovery, the queue drains FIFO. The WAL queue is NOT persistent across writer process restart — restart loses queued events. This is acceptable because Postgres outages affect all of CyberOS, not just metering, and the missing events are derivable from the source-of-truth services (auth middleware logs, cost_ledger holds) within the period-close window.</li></ol>\n<ol><li><strong>MUST</strong> route every memory audit emission through the same WAL queue. Postgres COMMIT and memory audit emission are dual-write — both succeed or the WAL replay retries. Until both succeed, the metering event is NOT durable. The reconciliation job at period close compares Postgres row IDs against memory chain rows and surfaces any divergence as a sev-1 audit row.</li></ol>\n<ol><li><strong>MUST</strong> refuse any cross-tenant metering query. The <code>metering_events</code> table is RLS-scoped on <code>tenant_id</code> per FR-AUTH-003; the materialized view inherits RLS via SECURITY DEFINER + tenant-context check. Cross-tenant aggregation is reserved for CyberOS-internal <code>bookkeeper</code> role (used by the billing service alone).</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/usage/correction</code> for the CFO role (FR-AUTH-101) to issue a refund-style correction. The handler INSERTs a new <code>metering_events</code> row with <code>quantity = -N</code> and <code>correction_to = &lt;original_event_id&gt;</code>. The original row is never UPDATED or DELETED (DEC-711). Reason is required (≥ 10 chars) and surfaces in a sev-2 memory audit row.</li></ol>\n<ol><li><strong>MUST</strong> define a closed 3-value <code>metering_event_state</code> enum (<code>active</code>, <code>corrected</code>, <code>superseded</code>). On insertion, state = <code>active</code>. A correction_to event flips the parent to <code>superseded</code> via a trigger. A double-correction (correction of a correction) sets state = <code>corrected</code> on the intermediate row. Aggregation views filter on <code>state = 'active'</code> only.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>quantity</code> per axis at the recorder API:</li></ol>\n<ul><li><code>seats</code>: integer ≥ 0, ≤ 100,000.</li><li><code>api_calls</code>: integer ≥ 1 (corrections are negative; recorded as opposite-sign row, not negative quantity at recorder), ≤ 1,000,000 per single event (bulk write).</li><li><code>ai_tokens</code>: integer ≥ 1, ≤ 10,000,000 per single event.</li><li><code>storage_bytes</code>: integer ≥ 0, ≤ 10 TiB (10^13). Out-of-range MUST return <code>400 BAD_REQUEST</code> with <code>{error: \"metering_quantity_out_of_range\", axis, quantity, max}</code>.</li></ul>\n<ol><li><strong>MUST</strong> define a <code>metering_current_period</code> materialized view that aggregates <code>SUM(quantity) FILTER (WHERE state = 'active')</code> by <code>(tenant_id, axis, period_start, period_end)</code>. Refresh schedule: every 5 minutes via pg_cron. The refresh is <code>CONCURRENTLY</code> to avoid blocking read queries. The view carries a <code>last_refreshed_at</code> column for staleness detection (DEC-713).</li></ol>\n<ol><li><strong>MUST</strong> emit one of 7 closed memory audit kinds per metering event:</li></ol>\n<ul><li><code>metering.event_recorded</code> (sev-3, per metric event)</li><li><code>metering.warn_threshold_crossed</code> (sev-2, per crossing)</li><li><code>metering.overage_blocked</code> (sev-2, per blocked request)</li><li><code>metering.correction_issued</code> (sev-2, per refund)</li><li><code>metering.policy_changed</code> (sev-2, per CFO policy update)</li><li><code>metering.reconciliation_divergence</code> (sev-1, per close-job divergence)</li><li><code>metering.wal_queue_overflow</code> (sev-1, per queue-full event)</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/metering/period/close</code> as an ops-only handler (no tenant access; <code>bookkeeper</code> role only). The handler runs the seats + storage snapshot jobs for one tenant + emits the final aggregate memory audit row + freezes the period (writes to <code>metering_periods.frozen_at</code>). After freeze, no <code>metering_events</code> row may carry a timestamp inside the frozen period — the recorder rejects with <code>409 PERIOD_FROZEN</code>. Frozen periods are reopened only via a sev-1 ops manual action (no API path).</li></ol>\n<ol><li><strong>MUST</strong> validate <code>tenant_id</code> against the active tenant set on every recorder API call. A metering event for a terminated tenant (FR-TEN-104 terminal state) MUST be rejected with <code>404 TENANT_NOT_FOUND</code>. The recorder caches the active-tenant set with a 60s TTL (mirrors FR-AUTH-109 + FR-AUTH-105 pattern) to keep hot-path overhead bounded.</li></ol>\n<ol><li><strong>MUST</strong> maintain an <code>aggregator_state</code> table that tracks <code>(tenant_id, axis, period_start, period_end, status, frozen_at, last_aggregated_seq)</code>. The seq counter is incremented on every materialized view refresh; the period-close handler verifies that <code>last_aggregated_seq</code> matches the latest <code>metering_events.seq</code> for the period before freezing — defense-in-depth against the materialized view falling behind unbounded.</li></ol>\n<ol><li><strong>MUST</strong> define a <code>metering_event_dispute_log</code> append-only table for billing-team-driven dispute investigation. Each row links to one or more <code>metering_events.id</code> + carries a <code>resolution_notes</code> text field (≤ 4 KiB) + a closed <code>resolution_status</code> enum (<code>pending</code>, <code>confirmed_correct</code>, <code>corrected_via_refund</code>, <code>wontfix</code>). Only the <code>billing_disputes</code> role can INSERT (per FR-AUTH-101). Disputes are never deleted — closed disputes remain queryable for the 10-year retention window (FR-DOC-001 alignment).</li></ol>\n<ol><li><strong>MUST</strong> scrub all reason-bearing audit text through FR-MEMORY-111 pre-ingest PII detection (refund reason, dispute resolution notes, policy-change reason). Postgres holds the raw text (RLS-scoped); the memory chain holds the scrubbed version. Mismatch between Postgres raw and chain scrubbed is acceptable and expected — this is the FR-MEMORY-111 contract.</li></ol>\n<ol><li><strong>MUST</strong> support a closed <code>unit</code> enum per axis (<code>seat</code>, <code>request</code>, <code>token</code>, <code>byte</code>) with a CHECK constraint enforcing the <code>(axis, unit)</code> pair (seats×seat, api_calls×request, ai_tokens×token, storage_bytes×byte). Mismatched pair MUST reject with <code>400</code> at recorder. The closed pairing makes downstream billing math unambiguous — no need for per-unit conversion at aggregation time.</li></ol>\n<hr>\n<h2>§2 — Rationale (informative — preserve all 22 paragraphs)</h2>\n<p><strong>§2.1  Why 4 axes and not 7+.</strong> The original proposal carried 7 axes (seats, api_calls, ai_tokens, storage, bandwidth, function_invocations, scheduled_jobs). DEC-700 closed it at 4. The principle: bill on what costs us money, not what looks countable. Bandwidth is a derivative of api_calls + storage_bytes; function_invocations equals api_calls for the tenant's perspective; scheduled_jobs are bounded per-tenant (the platform caps them) and so are a flat cost not a metered one. The 4 axes that survived are the four where our infrastructure spend scales linearly with tenant behavior — seats drive identity-provider costs, api_calls drive compute, ai_tokens drive provider-pass-through, storage_bytes drives S3 + Postgres. Adding a fifth axis requires a DEC entry AND a real provider-bill scaling story, not a marketing wish.</p>\n<p><strong>§2.2  Why append-only and not UPDATE.</strong> DEC-702 makes <code>metering_events</code> append-only at SQL grant. The argument against UPDATE: a billing dispute that lands six months later cannot be resolved if intermediate rows have been \"cleaned up\". The argument for append-only: forensic accounting works on append-only ledgers; non-append-only ledgers collapse under audit pressure. The refund pathway via <code>correction_to</code> (DEC-711) preserves the original event row + records the negation as a fresh row; the parent flips to <code>superseded</code> via trigger (clause #18) but is never deleted. This is the same pattern as FR-TIME-001 timesheet corrections — once a pattern is correct, replicate it.</p>\n<p><strong>§2.3  Why memory audit per event, not per period.</strong> DEC-701: per-event memory audit is the dispute-resolution mechanism. When a tenant says \"you charged us for 5M API calls but we only made 4M\", we need to point at a chain row per disputed call (or batch of calls), not a single rolled-up period total. The 7M extra calls might be a Stripe webhook retry storm we didn't bill them for; might be a legitimate bot we didn't realize was theirs; might be a CyberOS-side bug. A per-event chain row turns the question into \"show us the rows you dispute\" — operational, resolvable. A per-period chain row collapses to \"trust our aggregator\". We chose operational over compact.</p>\n<p><strong>§2.4  Why dual-write to Postgres + memory and not memory-only.</strong> Postgres is the authoritative source for billing math because (a) Stripe + our billing service speak SQL, not chain hashes; (b) Postgres + RLS gives us tenant isolation at query time without re-implementing it in chain-walking code; (c) materialized views give us sub-second <code>GET /v1/usage</code> latency. memory-only would have made every usage query a chain-walk — slow and complex. DEC-712 makes Postgres authoritative for billing + memory tamper-evident for reconciliation. The reconciliation job at period close cross-checks them; divergence is a sev-1 incident.</p>\n<p><strong>§2.5  Why the API-call hot-path is response-side, not request-side.</strong> Clause #6: the metering middleware fires after the response status is known. Two reasons. First, failed-auth requests should NOT be billed — they were rejected before the tenant got any service. Second, request-side metering puts the WAL-queue write on the request critical path; response-side puts it on the response path, where 200µs of extra latency is invisible. The cost is that a 5xx server error after auth-success still gets billed (we did spend compute on it); we accept that — it's a tiny fraction of traffic and the tenant can dispute via §1 #25.</p>\n<p><strong>§2.6  Why seats and storage are snapshots, not events.</strong> Clause #8 + #9 + DEC-705 + DEC-706. Seats churn during a billing period (joiners + leavers) but our actual cost — identity-provider seats — is what's active at the close boundary. Billing on per-day snapshots would be more accurate but creates 30 events per period per tenant, and the cost (engineering + provider charges) doesn't scale linearly with intra-period churn. Storage is similar: S3 charges on monthly average, but the inventory report is end-of-period; using end-of-period bytes is a slight under-bill in the tenant's favor (a tenant who uploaded 100 GB on day 1 and deleted 99 GB on day 30 pays for 1 GB, not the integral). We accept the under-bill — it makes math simple and tenants happy.</p>\n<p><strong>§2.7  Why a closed 4-value enum and not a free string.</strong> DEC-704 + clause #1. The CI cardinality test asserts exactly 4 values. The benefit: every consumer can switch on the enum exhaustively; every dashboard query is total without joining a dimension table; every audit row is type-checkable. The cost: adding a fifth axis is a schema migration. Given the architectural argument in §2.1 — adding axes should be rare and intentional — this is the right tradeoff.</p>\n<p><strong>§2.8  Why the WAL queue is in-memory and bounded.</strong> Clause #14 + DEC-709. Persistent (on-disk) WAL would add a fsync per event — at 10k events/sec, the disk I/O alone consumes the latency budget. In-memory WAL costs the events between writer-restart and Postgres-recovery, but at our scale (≤ 50k events/sec per writer) the restart-loss is rare and recoverable from source services. The 90% threshold for back-pressure prevents the queue from collapsing the writer process; the sev-1 audit row at overflow makes the rare loss observable.</p>\n<p><strong>§2.9  Why per-tenant overage policy and not a global one.</strong> DEC-710. Different tenants have different risk tolerances: enterprise customers want <code>block</code> (compliance — no surprise spend); startups want <code>allow</code> (don't break my product). A global policy forces one shape on all; per-tenant lets the customer choose at signup. The CFO-only gate on policy mutation prevents a malicious operator from flipping a high-risk tenant from <code>block</code> to <code>allow</code> and racking up overage; the sev-2 memory audit row makes policy changes investigatable.</p>\n<p><strong>§2.10  Why the period-close handler is ops-only.</strong> Clause #22. A tenant cannot freeze their own period — that would let them stop billing mid-period. A tenant cannot reopen a frozen period — that would let them rewrite history. Both directions are ops-only. The freeze is per-tenant per-period (not global), so one tenant's billing failures don't block another's close.</p>\n<p><strong>§2.11  Why the materialized view refresh is every 5 minutes, not continuous.</strong> DEC-713 + clause #20. Continuous refresh would CPU-bind one Postgres process per tenant + axis. 5 minutes is the lag tenants tolerate for usage dashboards (they're checking, not closing books in real time) and gives the refresh enough time to amortize across multiple events. The <code>last_refreshed_at</code> column on the view lets dashboard UIs warn if data is stale (e.g., during refresh-job outage). For \"exact-now\" queries (e.g., a tenant about to hit overage), the API-middleware enforcement runs on raw <code>metering_events</code> not the view — the raw path is exact.</p>\n<p><strong>§2.12  Why <code>correction_to</code> and not a <code>void</code> flag.</strong> Clause #17 + DEC-711. The <code>correction_to</code> self-FK is the FR-TIME-001 pattern: the original row stays exactly as written; the correction is a fresh row with <code>quantity = -N</code>. A <code>void</code> flag would require UPDATE — append-only forbids that. A separate <code>voided_events</code> table would split the data — querying historical totals would need a UNION. The <code>correction_to</code> approach + state enum (clause #18) lets every analytical query filter <code>state = 'active'</code> and get the right answer.</p>\n<p><strong>§2.13  Why dispute logs are append-only too.</strong> Clause #25. A closed dispute that gets reopened in 18 months (rare but possible — regulator inquiry, internal audit) needs the full history of how it was investigated + resolved. Append-only preserves that. The closed <code>resolution_status</code> enum forces dispute outcomes into 4 well-defined buckets, making aggregation across many disputes meaningful (e.g., \"what's our dispute-confirmation rate?\").</p>\n<p><strong>§2.14  Why PII-free metric rows.</strong> DEC-714 + clause #4. Metering rows are structured: integers + ids + timestamps. There's no place for free-text PII. The PII detection (FR-MEMORY-111) is applied to the reason-bearing fields (refund reason, dispute resolution notes, policy-change reason) — clause #26. Keeping metric rows themselves PII-free means we can replicate them to billing systems (Stripe metadata, internal dashboards) without re-running PII detection.</p>\n<p><strong>§2.15  Why idempotency keys at 24h retention.</strong> Clause #3 + DEC-715. The 24h window covers reasonable retry windows from upstream consumers (provider webhooks, AI-gateway retries, scheduled-job retries) without bloating the unique index forever. After 24h, the idempotency key can be reused (the upstream caller's retry budget is exhausted by then; if it still retries, the duplicate is intentional and we should re-bill). The partial index keeps the index size bounded — about 30M entries at our top-end traffic.</p>\n<p><strong>§2.16  Why the <code>aggregator_state.last_aggregated_seq</code> cross-check.</strong> Clause #24. The materialized view is a derived structure; if its refresh job lags, the period-close handler could freeze a period based on stale view data. The seq cross-check forces the close handler to ensure the view has caught up to the latest <code>metering_events</code> row for the period before freezing. Without this, an outage of the refresh job at end-of-month could result in under-billing.</p>\n<p><strong>§2.17  Why a closed <code>unit</code> enum and per-axis pairing.</strong> Clause #27. A free <code>unit</code> field would let a buggy recorder write <code>(\"api_calls\", \"minute\")</code> and the billing system would happily aggregate minutes as if they were requests. The (axis, unit) CHECK constraint makes that a database rejection. The closed pairing pre-decides the semantics: api_calls is always counted in requests, ai_tokens always in tokens, storage_bytes always in bytes. Downstream billing math has zero ambiguity.</p>\n<p><strong>§2.18  Why metering of API calls runs in middleware not in service code.</strong> Clause #6. Every CyberOS service running auth middleware gets metering for free. Putting metering in service code would require every service to remember to emit; the omissions would cause underbilling. Middleware-level integration is a single integration point — wire it once at the auth crate, every service inherits it.</p>\n<p><strong>§2.19  Why AI tokens are metered at postcall reconcile, not pre-call estimate.</strong> Clause #7. FR-AI-001 emits an estimate at pre-call (for cap-check); the actual token count is only known after the provider response. Metering on the estimate would over-bill the tenant for refused calls and over-bill for prompts that the model truncates. Postcall reconcile is the truth-bearing moment.</p>\n<p><strong>§2.20  Why we cap <code>quantity</code> per single event.</strong> Clause #19. A single event row with <code>quantity = 10^9</code> could plausibly be a buggy bulk-write (e.g., a typo turning a daily report into a yearly one). The per-axis caps catch the obvious bugs. Genuinely-large rolled-up writes (e.g., a year-end retroactive correction) should go through the correction handler with multiple rows, not one mega-row.</p>\n<p><strong>§2.21  Why the dispute resolution_notes is capped at 4 KiB.</strong> Clause #25. Dispute notes are operational reading; they need to fit on a CFO's screen + be queryable in dashboards. 4 KiB is roughly two screens of single-spaced text — enough for \"here's what we found, here's the resolution\" + the original tenant complaint. Longer notes can be linked to a document in FR-DOC-001 (the document repository); the dispute log carries the document_id, not the full text.</p>\n<p><strong>§2.22  Why metering events for terminated tenants are rejected at recorder.</strong> Clause #23. A terminated tenant (FR-TEN-104) should produce no new charges. If a stray service emits a metering event for a terminated tenant after termination (race condition, cache lag), the recorder MUST drop it — not silently, but with a 404 and a sev-2 memory audit row at the recorder. The 60s active-tenant cache mirrors the pattern from FR-AUTH-109 + FR-AUTH-105 — bounded hot-path overhead, fast eventual consistency.</p>\n<hr>\n<h2>§3 — API &amp; schema</h2>\n<h3>§3.1 — Migration 0001: metering_events table</h3>\n<pre><code class=\"language-sql\">-- services/metering/migrations/0001_metering_events.sql\n\nCREATE TYPE metering_axis AS ENUM ('seats', 'api_calls', 'ai_tokens', 'storage_bytes');\nCREATE TYPE metering_unit AS ENUM ('seat', 'request', 'token', 'byte');\nCREATE TYPE metering_event_state AS ENUM ('active', 'corrected', 'superseded');\nCREATE TYPE metering_overage_policy AS ENUM ('block', 'warn', 'allow');\n\nCREATE TABLE metering_events (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id       UUID NOT NULL REFERENCES tenants(id),\n    axis            metering_axis NOT NULL,\n    unit            metering_unit NOT NULL,\n    quantity        BIGINT NOT NULL,\n    idempotency_key TEXT NOT NULL CHECK (length(idempotency_key) BETWEEN 1 AND 64),\n    occurred_at     TIMESTAMPTZ NOT NULL DEFAULT now(),\n    period_start    TIMESTAMPTZ NOT NULL,\n    period_end      TIMESTAMPTZ NOT NULL,\n    state           metering_event_state NOT NULL DEFAULT 'active',\n    correction_to   UUID REFERENCES metering_events(id),\n    source_service  TEXT NOT NULL CHECK (length(source_service) BETWEEN 1 AND 64),\n    memory_chain_hash TEXT NOT NULL CHECK (length(memory_chain_hash) = 64),  -- hex SHA-256\n    extra           JSONB NOT NULL DEFAULT '{}'::jsonb,\n    seq             BIGSERIAL NOT NULL,\n    CONSTRAINT axis_unit_pair CHECK (\n        (axis = 'seats'         AND unit = 'seat'   ) OR\n        (axis = 'api_calls'     AND unit = 'request') OR\n        (axis = 'ai_tokens'     AND unit = 'token'  ) OR\n        (axis = 'storage_bytes' AND unit = 'byte'   )\n    ),\n    CONSTRAINT quantity_range CHECK (\n        (axis = 'seats'         AND quantity BETWEEN 0 AND 100000) OR\n        (axis = 'api_calls'     AND quantity BETWEEN -1000000 AND 1000000) OR\n        (axis = 'ai_tokens'     AND quantity BETWEEN -10000000 AND 10000000) OR\n        (axis = 'storage_bytes' AND quantity BETWEEN 0 AND 10000000000000)\n    )\n);\n\n-- Idempotency within 24h\nCREATE UNIQUE INDEX metering_events_idem\n    ON metering_events (tenant_id, axis, idempotency_key)\n    WHERE occurred_at &gt; now() - INTERVAL '24 hours';\n\nCREATE INDEX metering_events_period ON metering_events (tenant_id, axis, period_start, period_end);\nCREATE INDEX metering_events_state ON metering_events (state) WHERE state = 'active';\nCREATE INDEX metering_events_correction ON metering_events (correction_to) WHERE correction_to IS NOT NULL;\n\n-- Append-only enforcement\nREVOKE UPDATE, DELETE ON metering_events FROM cyberos_app;\nGRANT INSERT, SELECT ON metering_events TO metering_writer;\nGRANT SELECT ON metering_events TO metering_reader;\n\n-- RLS\nALTER TABLE metering_events ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON metering_events\n    USING (tenant_id = current_setting('cyberos.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('cyberos.tenant_id')::uuid);\n\n-- Correction state-transition trigger\nCREATE OR REPLACE FUNCTION metering_apply_correction() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.correction_to IS NOT NULL THEN\n        -- Verify sign-opposite + same axis\n        DECLARE\n            parent_axis metering_axis;\n            parent_qty  BIGINT;\n            parent_state metering_event_state;\n        BEGIN\n            SELECT axis, quantity, state INTO parent_axis, parent_qty, parent_state\n                FROM metering_events WHERE id = NEW.correction_to\n                FOR UPDATE;\n            IF parent_axis IS NULL THEN\n                RAISE EXCEPTION 'metering_parent_not_found' USING ERRCODE = 'P0200';\n            END IF;\n            IF parent_axis != NEW.axis THEN\n                RAISE EXCEPTION 'metering_correction_axis_mismatch' USING ERRCODE = 'P0201';\n            END IF;\n            IF SIGN(NEW.quantity) = SIGN(parent_qty) AND NEW.quantity != 0 THEN\n                RAISE EXCEPTION 'metering_correction_sign_must_oppose' USING ERRCODE = 'P0202';\n            END IF;\n            IF parent_state = 'superseded' THEN\n                -- correcting a correction; intermediate flips to 'corrected'\n                UPDATE metering_events SET state = 'corrected' WHERE id = NEW.correction_to;\n            ELSE\n                UPDATE metering_events SET state = 'superseded' WHERE id = NEW.correction_to;\n            END IF;\n        END;\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql SECURITY DEFINER;\nCREATE TRIGGER metering_correction_trg\n    BEFORE INSERT ON metering_events\n    FOR EACH ROW\n    WHEN (NEW.correction_to IS NOT NULL)\n    EXECUTE FUNCTION metering_apply_correction();\n\n-- Period-freeze rejection\nCREATE OR REPLACE FUNCTION metering_reject_frozen() RETURNS TRIGGER AS $$\nDECLARE frozen TIMESTAMPTZ;\nBEGIN\n    SELECT frozen_at INTO frozen FROM metering_periods\n        WHERE tenant_id = NEW.tenant_id\n          AND axis = NEW.axis\n          AND period_start = NEW.period_start\n          AND period_end = NEW.period_end;\n    IF frozen IS NOT NULL THEN\n        RAISE EXCEPTION 'metering_period_frozen' USING ERRCODE = 'P0203';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql SECURITY DEFINER;\nCREATE TRIGGER metering_freeze_trg\n    BEFORE INSERT ON metering_events\n    FOR EACH ROW\n    EXECUTE FUNCTION metering_reject_frozen();</code></pre>\n<h3>§3.2 — Migration 0002: metering_periods + aggregator_state</h3>\n<pre><code class=\"language-sql\">-- services/metering/migrations/0002_metering_periods.sql\n\nCREATE TABLE metering_periods (\n    tenant_id           UUID NOT NULL REFERENCES tenants(id),\n    axis                metering_axis NOT NULL,\n    period_start        TIMESTAMPTZ NOT NULL,\n    period_end          TIMESTAMPTZ NOT NULL,\n    frozen_at           TIMESTAMPTZ,\n    final_quantity      BIGINT,\n    final_memory_hash    TEXT,\n    last_aggregated_seq BIGINT NOT NULL DEFAULT 0,\n    PRIMARY KEY (tenant_id, axis, period_start, period_end)\n);\n\nALTER TABLE metering_periods ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON metering_periods\n    USING (tenant_id = current_setting('cyberos.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('cyberos.tenant_id')::uuid);\n\n-- Per-tenant overage policy\nALTER TABLE tenants ADD COLUMN IF NOT EXISTS metering_overage_policy metering_overage_policy NOT NULL DEFAULT 'warn';\nALTER TABLE tenants ADD COLUMN IF NOT EXISTS metering_caps_yaml TEXT;  -- per-axis caps, validated at load</code></pre>\n<h3>§3.3 — Migration 0003: materialized view</h3>\n<pre><code class=\"language-sql\">-- services/metering/migrations/0003_metering_aggregates_view.sql\n\nCREATE MATERIALIZED VIEW metering_current_period AS\nSELECT\n    tenant_id,\n    axis,\n    period_start,\n    period_end,\n    SUM(quantity) FILTER (WHERE state = 'active') AS total_quantity,\n    COUNT(*) FILTER (WHERE state = 'active') AS event_count,\n    MAX(seq) AS last_seq,\n    now() AS last_refreshed_at\nFROM metering_events\nGROUP BY tenant_id, axis, period_start, period_end;\n\nCREATE UNIQUE INDEX ON metering_current_period (tenant_id, axis, period_start, period_end);\n\n-- pg_cron: refresh every 5 minutes\nSELECT cron.schedule('metering_view_refresh', '*/5 * * * *',\n    $$REFRESH MATERIALIZED VIEW CONCURRENTLY metering_current_period$$);</code></pre>\n<h3>§3.4 — Recorder API (Rust)</h3>\n<pre><code class=\"language-rust\">// services/metering/src/recorder.rs\n\nuse anyhow::Result;\nuse sqlx::PgPool;\n\n#[derive(Debug, Clone, sqlx::Type)]\n#[sqlx(type_name = \"metering_axis\", rename_all = \"snake_case\")]\npub enum Axis { Seats, ApiCalls, AiTokens, StorageBytes }\n\n#[derive(Debug, Clone)]\npub struct MeteringEvent {\n    pub tenant_id: uuid::Uuid,\n    pub axis: Axis,\n    pub quantity: i64,\n    pub idempotency_key: String,\n    pub source_service: String,\n    pub extra: serde_json::Value,\n}\n\npub struct Recorder {\n    pool: PgPool,\n    wal: WalQueue,\n    active_tenants: Arc&lt;RwLock&lt;HashSet&lt;uuid::Uuid&gt;&gt;&gt;,\n    cache_loaded_at: AtomicI64,\n}\n\nimpl Recorder {\n    pub async fn record(&amp;self, event: MeteringEvent) -&gt; Result&lt;()&gt; {\n        // §1 #23 active-tenant cache check\n        self.refresh_cache_if_stale().await?;\n        if !self.active_tenants.read().contains(&amp;event.tenant_id) {\n            return Err(MeterError::TenantNotActive(event.tenant_id).into());\n        }\n\n        // §1 #19 quantity validation (DB CHECK is the source of truth; this is fast-fail)\n        validate_quantity(event.axis, event.quantity)?;\n\n        // §1 #14 enqueue to WAL with back-pressure\n        self.wal.push(event).await?;\n        Ok(())\n    }\n\n    async fn refresh_cache_if_stale(&amp;self) -&gt; Result&lt;()&gt; {\n        let now = chrono::Utc::now().timestamp();\n        if now - self.cache_loaded_at.load(Ordering::Relaxed) &gt; 60 {\n            let active: HashSet&lt;uuid::Uuid&gt; = sqlx::query_scalar(\n                \"SELECT id FROM tenants WHERE state NOT IN ('terminated', 'never_active')\"\n            ).fetch_all(&amp;self.pool).await?.into_iter().collect();\n            *self.active_tenants.write() = active;\n            self.cache_loaded_at.store(now, Ordering::Relaxed);\n        }\n        Ok(())\n    }\n}</code></pre>\n<h3>§3.5 — Period-close handler</h3>\n<pre><code class=\"language-rust\">// services/metering/src/handlers/period_close.rs\n\npub async fn close_period(\n    pool: &amp;PgPool,\n    tenant_id: uuid::Uuid,\n    axis: Axis,\n    period_start: DateTime&lt;Utc&gt;,\n    period_end: DateTime&lt;Utc&gt;,\n) -&gt; Result&lt;PeriodCloseReceipt&gt; {\n    let mut tx = pool.begin().await?;\n\n    // §1 #24 verify aggregator caught up\n    let row = sqlx::query!(\n        r#\"SELECT\n              (SELECT COALESCE(MAX(seq), 0) FROM metering_events\n                  WHERE tenant_id = $1 AND axis = $2\n                    AND occurred_at &gt;= $3 AND occurred_at &lt; $4) AS events_max_seq,\n              (SELECT last_aggregated_seq FROM metering_periods\n                  WHERE tenant_id = $1 AND axis = $2\n                    AND period_start = $3 AND period_end = $4) AS agg_seq\"#,\n        tenant_id, axis as _, period_start, period_end\n    ).fetch_one(&amp;mut *tx).await?;\n    if row.events_max_seq &gt; row.agg_seq.unwrap_or(0) {\n        return Err(MeterError::AggregatorBehind { latest: row.events_max_seq, agg: row.agg_seq.unwrap_or(0) }.into());\n    }\n\n    // Take the snapshot from MV\n    let final_qty: i64 = sqlx::query_scalar!(\n        r#\"SELECT COALESCE(total_quantity, 0)\n              FROM metering_current_period\n              WHERE tenant_id = $1 AND axis = $2\n                AND period_start = $3 AND period_end = $4\"#,\n        tenant_id, axis as _, period_start, period_end\n    ).fetch_one(&amp;mut *tx).await?.unwrap_or(0);\n\n    // Freeze\n    let memory_hash = emit_final_memory(tenant_id, axis, final_qty, period_start, period_end).await?;\n    sqlx::query!(\n        r#\"UPDATE metering_periods\n              SET frozen_at = now(), final_quantity = $5, final_memory_hash = $6\n              WHERE tenant_id = $1 AND axis = $2 AND period_start = $3 AND period_end = $4\"#,\n        tenant_id, axis as _, period_start, period_end, final_qty, &amp;memory_hash\n    ).execute(&amp;mut *tx).await?;\n\n    tx.commit().await?;\n    Ok(PeriodCloseReceipt { tenant_id, axis, final_quantity: final_qty, memory_chain_hash: memory_hash })\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><code>metering_events</code> table created with 4-value <code>metering_axis</code> enum + 4-value <code>metering_unit</code> enum + 3-value <code>metering_event_state</code> enum.</li><li>CI cardinality test asserts exactly 4 axis values, 4 unit values, 3 state values, 3 overage policy values.</li><li><code>REVOKE UPDATE, DELETE</code> on <code>metering_events</code> confirmed at <code>\\dp</code> inspection.</li><li>Inserting two events with identical <code>(tenant_id, axis, idempotency_key)</code> within 24h returns success but only one row exists.</li><li>Inserting an event with <code>axis = api_calls, unit = token</code> fails with <code>axis_unit_pair</code> CHECK violation.</li><li>Inserting an event with <code>axis = seats, quantity = 200000</code> fails with <code>quantity_range</code> CHECK violation.</li><li>RLS prevents cross-tenant SELECT on <code>metering_events</code>.</li><li>memory audit row emitted per metering event with cross-link <code>postgres_event_id</code> ↔ <code>memory_chain_hash</code>.</li><li>API-call middleware emits <code>api_calls</code> event with <code>quantity = 1</code> per successful auth-scoped request.</li><li>Failed-auth request emits no metering event.</li><li>AI gateway <code>postcall_reconcile()</code> emits <code>ai_tokens</code> event with <code>quantity = input + output</code>.</li><li>Seats snapshot job emits ONE <code>seats</code> event per tenant per period at close boundary.</li><li>Storage snapshot job emits ONE <code>storage_bytes</code> event combining S3 + Postgres bytes.</li><li>Per-tenant overage policy mutation requires <code>cfo</code> role + reason ≥ 10 chars; sev-2 memory audit emitted.</li><li>Overage <code>block</code> policy returns <code>402 PAYMENT_REQUIRED</code> when cap exceeded; no metering event emitted.</li><li>Overage <code>warn</code> policy emits sev-2 memory audit at threshold-crossing; metric event proceeds.</li><li>Overage <code>allow</code> policy: no enforcement; metric event proceeds.</li><li><code>GET /v1/usage</code> returns 4-axis usage from <code>metering_current_period</code> view.</li><li>Materialized view refreshes every 5 minutes via pg_cron; <code>last_refreshed_at</code> advances.</li><li>Period-close handler verifies <code>last_aggregated_seq &gt;= max(events.seq)</code> before freezing; rejects on lag.</li><li>Inserting a metering event with <code>occurred_at</code> inside a frozen period fails with <code>metering_period_frozen</code> (SQLSTATE P0203).</li><li><code>POST /v1/usage/correction</code> requires <code>cfo</code> role + reason ≥ 10 chars; emits <code>correction_to</code> row.</li><li>Correction trigger flips parent row to <code>state = 'superseded'</code>; double-correction flips intermediate to <code>corrected</code>.</li><li>Correction with same-sign <code>quantity</code> fails with <code>metering_correction_sign_must_oppose</code> (P0202).</li><li>WAL queue at 90% utilization emits <code>metering.wal_queue_overflow</code> sev-1 memory audit + rejects new events.</li><li>Metering event for a terminated tenant (FR-TEN-104) returns <code>404 TENANT_NOT_FOUND</code> at recorder.</li><li>Active-tenant cache refresh interval is 60s; staleness causes one extra DB query, not stale-data acceptance.</li><li>Dispute log INSERT requires <code>billing_disputes</code> role; resolution_notes ≤ 4 KiB enforced.</li><li>Cross-tenant aggregation query via <code>bookkeeper</code> role bypasses RLS (SECURITY DEFINER); never via <code>cyberos_app</code>.</li><li>All reason-bearing audit text (refund reason, policy reason, dispute notes) scrubbed via FR-MEMORY-111 before memory chain emission.</li></ol>\n<hr>\n<h2>§5 — Verification (CI tests)</h2>\n<ul><li><code>cardinality_test_axis</code> — asserts <code>count(*) FROM pg_enum WHERE enumtypid = 'metering_axis'::regtype = 4</code>.</li><li><code>cardinality_test_unit</code> — 4.</li><li><code>cardinality_test_state</code> — 3.</li><li><code>cardinality_test_policy</code> — 3.</li><li><code>axis_unit_pair_test</code> — table-driven 16-cell test (4 axes × 4 units) asserts 4 valid, 12 invalid.</li><li><code>quantity_range_test</code> — boundary tests for each axis range.</li><li><code>append_only_test</code> — REVOKE inspection + attempt UPDATE/DELETE returns permission denied.</li><li><code>idempotency_test</code> — second insert with same key returns 200 + no second row.</li><li><code>correction_to_test</code> — sign-opposite trigger; state transitions; aggregation excludes superseded.</li><li><code>rls_isolation_test</code> — two tenants, cross-query returns empty.</li><li><code>frozen_period_test</code> — freeze + insert returns P0203.</li><li><code>wal_overflow_test</code> — fill queue to 90%; sev-1 audit emitted; new events rejected.</li><li><code>terminated_tenant_test</code> — terminate tenant; emit event; recorder returns 404 + sev-2 audit.</li><li><code>api_middleware_test</code> — 100 successful requests = 100 events; 100 failed-auth = 0 events.</li><li><code>ai_postcall_test</code> — mock provider response with 1500 input + 800 output tokens; assert <code>quantity = 2300</code>.</li><li><code>seats_snapshot_test</code> — seed 12 active + 3 deactivated members; assert snapshot = 12.</li><li><code>storage_snapshot_test</code> — seed 50 GiB S3 + 3 GiB Postgres; assert quantity ≈ 53 GiB in bytes.</li><li><code>overage_block_test</code> — set cap = 100; submit 101st event; returns 402; no event recorded.</li><li><code>overage_warn_test</code> — set cap = 100, threshold = 0.8; cross at 81st event; sev-2 audit emitted.</li><li><code>dispute_acl_test</code> — non-<code>billing_disputes</code> role INSERT to dispute_log fails with permission denied.</li><li><code>mv_refresh_test</code> — insert 10 events, wait 5 min, query view; assert sum = 10.</li><li><code>aggregator_lag_test</code> — insert event but block MV refresh; close-period returns AggregatorBehind error.</li></ul>\n<hr>\n<h2>§6 — File skeleton</h2>\n<pre><code>services/metering/\n├── Cargo.toml\n├── migrations/\n│   ├── 0001_metering_events.sql\n│   ├── 0002_metering_periods.sql\n│   └── 0003_metering_aggregates_view.sql\n├── src/\n│   ├── lib.rs                  # Recorder + Aggregator pub re-exports\n│   ├── recorder.rs             # §3.4 Recorder API\n│   ├── aggregator.rs           # MV refresh + close-period orchestration\n│   ├── wal_queue.rs            # bounded in-memory WAL + replay\n│   ├── policy.rs               # overage policy enforcement\n│   ├── memory_audit.rs          # 7-kind audit emission\n│   ├── axes/\n│   │   ├── seats.rs            # end-of-period snapshot job\n│   │   ├── api_calls.rs        # middleware integration glue\n│   │   ├── ai_tokens.rs        # cost_ledger postcall hook\n│   │   └── storage.rs          # S3 inventory + pg_total_relation_size\n│   ├── handlers/\n│   │   ├── usage_query.rs      # GET /v1/usage\n│   │   ├── usage_correction.rs # POST /v1/usage/correction\n│   │   └── period_close.rs     # POST /v1/metering/period/close (ops)\n│   └── error.rs                # MeterError enum\n├── config/\n│   └── default_caps.yaml       # platform-default per-axis caps\n└── tests/\n    ├── api_metering_test.rs\n    ├── ai_metering_test.rs\n    ├── seats_snapshot_test.rs\n    ├── storage_snapshot_test.rs\n    ├── append_only_test.rs\n    ├── overage_policy_test.rs\n    └── wal_replay_test.rs</code></pre>\n<hr>\n<h2>§7 — Dependencies &amp; blast-radius</h2>\n<p><strong>Depends on</strong>: FR-AI-001 (cost_ledger postcall hook for AI tokens), FR-TEN-001 (tenants table + provisioning), FR-AUTH-003 (RLS scope for middleware), FR-MEMORY-111 (PII scrubbing for reason fields).</p>\n<p><strong>Blocks</strong>: FR-TEN-003 (Stripe billing — billing service consumes <code>metering_current_period</code> + period-close finals as the per-axis charge basis).</p>\n<p><strong>Blast radius if broken</strong>:</p>\n<ul><li><strong>Under-bill</strong>: missing events → tenant pays less than the platform spends. Cumulative loss at scale.</li><li><strong>Over-bill</strong>: phantom events or correction failures → disputes + refunds + reputational damage.</li><li><strong>Cross-tenant leak</strong>: RLS misconfiguration would surface another tenant's usage. Compliance-critical.</li><li><strong>Chain divergence</strong>: Postgres-memory drift caught at period close but acted on at sev-1 reconciliation incident.</li></ul>\n<hr>\n<h2>§8 — Payload examples</h2>\n<h3>§8.1 — Successful api_calls event</h3>\n<pre><code class=\"language-json\">POST /v1/metering/internal/record\nAuthorization: Bearer &lt;service-token-with-metering_writer-role&gt;\n\n{\n  \"tenant_id\": \"ten_abc\",\n  \"axis\": \"api_calls\",\n  \"quantity\": 1,\n  \"idempotency_key\": \"req_2026-05-16T10:30:01.234Z_a8f3\",\n  \"source_service\": \"auth-middleware\",\n  \"extra\": {\"path\": \"/v1/documents/search\", \"method\": \"POST\", \"status\": 200}\n}\n\n200 OK\n{\n  \"event_id\": \"01J2C9X3K7M4N5...\",\n  \"memory_chain_hash\": \"7a3f9c2e1d5b8...\",\n  \"duplicate\": false\n}</code></pre>\n<h3>§8.2 — Overage blocked</h3>\n<pre><code class=\"language-json\">POST /v1/documents/search  (downstream of middleware)\n\n402 PAYMENT_REQUIRED\n{\n  \"error\": \"overage_blocked\",\n  \"axis\": \"api_calls\",\n  \"current_period_total\": 1000000,\n  \"cap\": 1000000,\n  \"policy\": \"block\",\n  \"contact\": \"billing@cyberos.world\"\n}</code></pre>\n<h3>§8.3 — Correction (refund)</h3>\n<pre><code class=\"language-json\">POST /v1/usage/correction\nAuthorization: Bearer &lt;cfo-token&gt;\n\n{\n  \"original_event_id\": \"01J2C9X3K7M4N5...\",\n  \"reason\": \"Provider double-billed our ai_tokens consumer; refunding 5000 tokens\",\n  \"axis\": \"ai_tokens\",\n  \"quantity\": -5000\n}\n\n200 OK\n{\n  \"correction_event_id\": \"01J2CAB9F3...\",\n  \"parent_state\": \"superseded\",\n  \"memory_chain_hash\": \"4f8e2a...\"\n}</code></pre>\n<h3>§8.4 — <code>GET /v1/usage</code></h3>\n<pre><code class=\"language-json\">GET /v1/usage\nAuthorization: Bearer &lt;tenant-admin&gt;\n\n200 OK\n{\n  \"tenant_id\": \"ten_abc\",\n  \"period_start\": \"2026-05-01T00:00:00Z\",\n  \"period_end\": \"2026-05-31T23:59:59Z\",\n  \"seats\": 12,\n  \"api_calls\": 4823901,\n  \"ai_tokens\": 12834729,\n  \"storage_bytes\": 38291723123,\n  \"overage_policy\": \"warn\",\n  \"warn_thresholds_crossed\": [\"api_calls\"],\n  \"view_last_refreshed_at\": \"2026-05-16T10:25:00Z\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<ul><li><strong>OQ-1</strong> (closed by DEC-705): seats snapshot at close, not mid-period events. Confirmed.</li><li><strong>OQ-2</strong> (closed by DEC-709): in-memory WAL acceptable for restart-loss; FR-OBS-009 captures the bounded loss event.</li><li><strong>OQ-3</strong> (closed by DEC-715): 24h idempotency retention sufficient for upstream retry budgets.</li><li><strong>OQ-4</strong> (open): granularity of warn_threshold — single threshold (0.80) or multiple (0.60 + 0.80 + 0.95)? Default single; revisit when first customer pushes back. Track in EVOLUTION.md.</li><li><strong>OQ-5</strong> (open): should <code>bookkeeper</code> cross-tenant queries themselves be audited? Currently they emit sev-3 audit at each query. Watch for over-emission noise; tighten if it becomes operational friction.</li></ul>\n<hr>\n<h2>§10 — Failure modes (32 rows)</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Sev</th><th>Handler</th></tr></thead><tbody><tr><td>1</td><td>Postgres outage during recorder write</td><td>sqlx error</td><td>1</td><td>WAL queue absorbs; back-pressure at 90%</td></tr><tr><td>2</td><td>memory audit subprocess hang</td><td>timeout 5s</td><td>1</td><td>Mark event WAL-pending; retry on replay</td></tr><tr><td>3</td><td>WAL queue overflow (&gt;90%)</td><td>metering.wal_queue_overflow sev-1</td><td>1</td><td>Reject new events; ops alarm</td></tr><tr><td>4</td><td>Materialized view refresh job dies</td><td>pg_cron heartbeat absent</td><td>1</td><td>Sev-1 alarm; usage queries fall back to raw COUNT</td></tr><tr><td>5</td><td>Active-tenant cache stale (terminated tenant still in cache)</td><td>60s TTL bounded staleness</td><td>3</td><td>Period-close reconciliation flags &amp; corrects</td></tr><tr><td>6</td><td>Cross-tenant RLS misconfiguration</td><td>rls_isolation_test fail</td><td>1</td><td>CI blocks deploy</td></tr><tr><td>7</td><td>Duplicate idempotency_key submission</td><td>UNIQUE INDEX constraint</td><td>3</td><td>Returns success; no second row</td></tr><tr><td>8</td><td>Quantity out of CHECK range</td><td>DB error 23514</td><td>3</td><td>Recorder returns 400 + sev-3 audit</td></tr><tr><td>9</td><td>axis/unit pair mismatch</td><td>DB CHECK violation</td><td>3</td><td>Recorder returns 400 + sev-3 audit</td></tr><tr><td>10</td><td>Correction with same-sign quantity</td><td>P0202 trigger</td><td>2</td><td>Reject + sev-2 audit</td></tr><tr><td>11</td><td>Correction targets non-existent parent</td><td>P0200 trigger</td><td>2</td><td>Reject + sev-2 audit</td></tr><tr><td>12</td><td>Period freeze attempt while aggregator behind</td><td>AggregatorBehind error</td><td>2</td><td>Reject; ops investigates MV refresh</td></tr><tr><td>13</td><td>Recorder receives event for terminated tenant</td><td>404 + active-tenant cache miss</td><td>2</td><td>Reject + sev-2 audit</td></tr><tr><td>14</td><td>Insert into frozen period</td><td>P0203 trigger</td><td>2</td><td>Reject + sev-2 audit</td></tr><tr><td>15</td><td>Non-cfo role attempts policy mutation</td><td>RBAC check</td><td>2</td><td>Return 403 + sev-2 audit</td></tr><tr><td>16</td><td>Non-cfo role attempts correction</td><td>RBAC check</td><td>2</td><td>Return 403 + sev-2 audit</td></tr><tr><td>17</td><td>Non-billing_disputes role inserts dispute_log</td><td>GRANT check</td><td>2</td><td>DB permission denied</td></tr><tr><td>18</td><td>resolution_notes &gt; 4 KiB</td><td>CHECK constraint</td><td>3</td><td>Reject 400</td></tr><tr><td>19</td><td>source_service &gt; 64 chars</td><td>CHECK constraint</td><td>3</td><td>Reject 400</td></tr><tr><td>20</td><td>idempotency_key length out of [1,64]</td><td>CHECK constraint</td><td>3</td><td>Reject 400</td></tr><tr><td>21</td><td>memory chain divergence at period close</td><td>reconciliation diff</td><td>1</td><td>Sev-1; ops manual investigation</td></tr><tr><td>22</td><td>S3 inventory CSV missing</td><td>snapshot job error</td><td>2</td><td>Retry up to 6h; if still missing, sev-2; skip storage event</td></tr><tr><td>23</td><td>pg_total_relation_size returns 0 (DB outage)</td><td>snapshot job sanity</td><td>2</td><td>Retry; if still 0, sev-2; manual review</td></tr><tr><td>24</td><td>Two writers race on same idempotency_key</td><td>UNIQUE constraint wins</td><td>3</td><td>First-writer-wins; second sees duplicate</td></tr><tr><td>25</td><td>Refund row inserted in already-frozen period</td><td>P0203</td><td>2</td><td>Reject; CFO re-issues in current period with note</td></tr><tr><td>26</td><td>Materialized view refresh CONCURRENT lock contention</td><td>pg_stat_activity</td><td>3</td><td>Retry next 5-min tick</td></tr><tr><td>27</td><td>API-middleware emission failure</td><td>WAL push error</td><td>1</td><td>Logged; back-pressure to upstream service</td></tr><tr><td>28</td><td>AI gateway postcall hook fails to emit</td><td>structured-log</td><td>1</td><td>Logged; reconciliation at period close detects</td></tr><tr><td>29</td><td>Tenant policy YAML invalid at load</td><td>schema validation</td><td>1</td><td>CI/ops alarm; old policy stays active</td></tr><tr><td>30</td><td>Cap value exceeds INT8 max</td><td>CHECK constraint</td><td>3</td><td>Reject policy update; 400</td></tr><tr><td>31</td><td>Period-close attempted on wrong tenant by bookkeeper role</td><td>RBAC + tenant context</td><td>2</td><td>Reject; sev-2 audit</td></tr><tr><td>32</td><td>Dispute resolution status invalid value</td><td>ENUM constraint</td><td>3</td><td>Reject 400</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Append-only is enforced at SQL grant + at the application layer (Recorder API has no UPDATE/DELETE methods). Defense-in-depth: a code bug that issued an UPDATE would still be rejected by Postgres.</p>\n<p><strong>§11.2</strong> The WAL queue is implemented as <code>tokio::sync::mpsc::channel(100_000)</code>. Send returns <code>Err</code> when buffer full; the recorder service converts that to a 503 + sev-1 memory audit.</p>\n<p><strong>§11.3</strong> The materialized view is <code>REFRESH MATERIALIZED VIEW CONCURRENTLY</code> — readers see the previous version until the refresh completes. The 5-min cadence is chosen to keep the refresh under 30s for ≤100k events per period per tenant.</p>\n<p><strong>§11.4</strong> The recorder service exposes an internal-only API (mTLS + service-token, no tenant access) on a separate port from the public REST surface. The metering_writer role is granted only to the service principal that holds the mTLS cert.</p>\n<p><strong>§11.5</strong> API-call metering is wired into the auth-crate middleware (FR-AUTH-003). The middleware emits to a process-local mpsc; a background task batches every 100 events / 100ms and ships to the recorder. Batching keeps the recorder API RPS bounded.</p>\n<p><strong>§11.6</strong> AI-token metering is a one-line addition to FR-AI-001's <code>postcall_reconcile</code>: after the cost ledger row is committed, the postcall hook emits to the recorder. The emission failure is logged but not retried inline — the period-close reconciliation will catch any drift.</p>\n<p><strong>§11.7</strong> Seats snapshot runs at billing-period-close as a cron job. The job is idempotent: re-running the same period emits the same event (same idempotency_key derived from <code>(tenant_id, axis, period_start, period_end)</code>).</p>\n<p><strong>§11.8</strong> Storage snapshot has two sub-jobs that run independently then aggregate. If one (S3 or Postgres) is failing, the other still emits; the partial event is later corrected via correction_to once the missing piece is recovered.</p>\n<p><strong>§11.9</strong> Overage policy enforcement runs at the auth middleware (where api_calls is metered) and at the AI gateway pre-call (where ai_tokens is metered). For seats + storage, enforcement is at period close — there's no per-event admission control because there are no per-event seat additions or storage upticks in the metering model.</p>\n<p><strong>§11.10</strong> Per-tenant caps live in <code>tenants.metering_caps_yaml</code> validated against a schema at load. Examples: <code>seats: 100</code>, <code>api_calls: 1000000</code>, <code>ai_tokens: 10000000</code>, <code>storage_bytes: 100000000000</code>. Unspecified axes use platform defaults from <code>config/default_caps.yaml</code>.</p>\n<p><strong>§11.11</strong> The <code>bookkeeper</code> role used by the billing service runs cross-tenant queries via SECURITY DEFINER functions that set <code>cyberos.tenant_id</code> to a sentinel <code>00000000-0000-0000-0000-000000000000</code> value that bypasses RLS. The function emits a sev-3 memory audit per call.</p>\n<p><strong>§11.12</strong> Tests use a real Postgres via <code>sqlx::testcontainers</code> — no mocked DB. Migrations run on every test fixture setup; the test container is reused per file (<code>#[tokio::test]</code> with shared pool).</p>\n<p><strong>§11.13</strong> The <code>metering_events.memory_chain_hash</code> is the memory row's chain hash from the canonical Writer. The bridge is via subprocess fork (mirrors FR-AI-001 audit emission); PyO3 is a future optimization.</p>\n<p><strong>§11.14</strong> Refunds via <code>correction_to</code> flow through the same recorder API as primary events — the API is generic over insertion. The CFO-only authorization is at the HTTP handler layer, not the recorder.</p>\n<p><strong>§11.15</strong> Dispute logs are visible to the CFO + billing_disputes roles; not visible to tenant admins. Disputes are operational records, not customer-facing.</p>\n<p><strong>§11.16</strong> The 60s active-tenant cache is per-process (each metering writer process holds its own). On tenant termination, the worst-case 60s window where the recorder still accepts events for the terminated tenant is acceptable because (a) terminated tenants' services are already shut down, so no upstream is emitting; (b) the FR-TEN-104 90-day grace handles any reactivation; (c) period-close reconciliation flags any stale events.</p>\n<p><strong>§11.17</strong> The pg_cron <code>metering_view_refresh</code> job logs a sev-1 audit if the refresh takes &gt; 4 minutes (signaling MV is approaching the 5-minute tick boundary). Operations triage tuning of axis indexing.</p>\n<p><strong>§11.18</strong> Period boundaries are computed from <code>tenants.billing_timezone</code> + <code>tenants.billing_period_kind</code> (monthly, quarterly). The recorder accepts any <code>occurred_at</code> falling within the corresponding period_start/period_end; the period derivation runs in the recorder before WAL push.</p>\n<p><strong>§11.19</strong> When the recorder is rolled to a new version, the in-memory WAL queue is drained before shutdown (graceful drain on SIGTERM, 30s timeout). After timeout, remaining events are logged as sev-1 audit. The deployment runbook documents this drain.</p>\n<p><strong>§11.20</strong> Cardinality CI tests run in the migration test suite, not at runtime. Adding a fifth axis would require updating the CI assertion + the DEC entry.</p>\n<p><strong>§11.21</strong> The 100_000 WAL buffer is sized for ~1 second of peak traffic at 100k events/sec. Bumping the buffer requires verifying memory footprint (each event ≈ 1 KiB → 100 MiB at full).</p>\n<p><strong>§11.22</strong> The <code>extra</code> JSONB field on <code>metering_events</code> is intentionally schemaless to absorb future axis-specific metadata without migration. The metering team curates a registry of well-known <code>extra</code> keys per axis in <code>services/metering/docs/extra_schema.md</code>.</p>\n<hr>\n<p><em>End of FR-TEN-004 spec.</em></p>";
