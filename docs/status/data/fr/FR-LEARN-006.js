window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LEARN-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The LEARN service <strong>MUST</strong> ship promotion workflow at <code>services/learn/src/promotion/</code> with CEO+CHRO dual-sign + cascade to HR + REW + CHAT, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>promotion_status</code> against closed enum per DEC-2131.</li></ol>\n<ol><li><strong>MUST</strong> initiate per DEC-2130 when FR-LEARN-004 council completes with recommendation=promote.</li></ol>\n<ol><li><strong>MUST</strong> enforce dual-sign at <code>dual_sign_gate.rs::can_execute(promotion)</code> per DEC-2133:</li></ol>\n<ul><li>Both CEO + CHRO signed</li><li>Same person can't sign both (separation of duties)</li></ul>\n<ol><li><strong>MUST</strong> cascade on executed at <code>cascade.rs::execute(promotion)</code> per DEC-2132:</li></ol>\n<ul><li>FR-HR-001 update member.mastery_level (transactional)</li><li>FR-REW-001 update comp_band (transactional)</li><li>FR-CHAT-005 announce in #all (non-blocking)</li><li>Rollback all on any failure</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0006</code>: ``<code>sql CREATE TABLE learn_promotions ( promotion_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, candidate_member_id UUID NOT NULL, council_id UUID NOT NULL, skill_id UUID NOT NULL, from_level INT NOT NULL CHECK (from_level &gt;= 1 AND from_level &lt;= 5), to_level INT NOT NULL CHECK (to_level &gt;= 1 AND to_level &lt;= 5), status TEXT NOT NULL DEFAULT 'pending_council' CHECK (status IN ('pending_council','council_recommended','ceo_signed','chro_signed','approved','declined','executed')), ceo_signed_by UUID, ceo_signed_at TIMESTAMPTZ, chro_signed_by UUID, chro_signed_at TIMESTAMPTZ, executed_at TIMESTAMPTZ, decline_reason TEXT, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (council_id) ); ALTER TABLE learn_promotions ENABLE ROW LEVEL SECURITY; CREATE POLICY promotions_rls ON learn_promotions USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON learn_promotions FROM cyberos_app; GRANT UPDATE (status, ceo_signed_by, ceo_signed_at, chro_signed_by, chro_signed_at, executed_at, decline_reason) ON learn_promotions TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/learn/promotions/{id}/ceo-sign POST /v1/learn/promotions/{id}/chro-sign POST /v1/learn/promotions/{id}/decline    body: {reason} GET  /v1/learn/promotions/{id} </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-2134. PII per FR-MEMORY-111: decline_reason SHA-256 hashed; status enum + level ints ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from initiate → sign → execute → cascade → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> execute without dual-sign per DEC-2130.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow same-person dual-sign per DEC-2133.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip cascade per DEC-2132.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why dual-sign (DEC-2130)?</strong> Promotion = career + financial impact; single-signer governance gap.</p>\n<p><strong>Why separation of duties (DEC-2133)?</strong> Same person both roles = no real second check.</p>\n<p><strong>Why cascade transactional (DEC-2132)?</strong> HR + REW + announcement must align; partial = embarrassment.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample promotion state:</p>\n<pre><code class=\"language-json\">{\n  \"promotion_id\": \"uuid\",\n  \"candidate_member_id\": \"uuid\",\n  \"from_level\": 3,\n  \"to_level\": 4,\n  \"skill_id\": \"uuid\",\n  \"status\": \"ceo_signed\",\n  \"ceo_signed_at\": \"2026-05-17T10:00:00Z\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>promotion_status enum cardinality 7</strong>. 2. <strong>CEO+CHRO dual-sign required</strong>. 3. <strong>Same person rejected</strong>. 4. <strong>Cascade transactional</strong>. 5. <strong>HR mastery updated</strong>. 6. <strong>REW comp band updated</strong>. 7. <strong>CHAT announcement non-blocking</strong>. 8. <strong>UNIQUE(council_id) — one promotion per council</strong>. 9. <strong>5 memory audit kinds emitted</strong>. 10. <strong>PII scrubbed (decline_reason SHA256)</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>CEO/CHRO role only</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Append-only via REVOKE except status cols</strong>. 15. <strong>Decline allowed at any pre-execute stage</strong>. 16. <strong>from_level + to_level CHECK 1-5</strong>. 17. <strong>to_level &gt; from_level enforced</strong>. 18. <strong>Status workflow enforced</strong>. 19. <strong>Cascade failure → rollback</strong>. 20. <strong>Council recommend=decline blocks promotion init</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn dual_sign_required() {\n    let ctx = TestContext::with_promotion_pending().await;\n    ctx.ceo_sign(ctx.promotion_id).await;\n    let r = ctx.try_execute(ctx.promotion_id).await;\n    assert!(r.is_err());  // CHRO missing\n    ctx.chro_sign(ctx.promotion_id).await;\n    let r2 = ctx.execute(ctx.promotion_id).await;\n    assert!(r2.is_ok());\n}\n\n#[tokio::test]\nasync fn same_person_rejected() {\n    let ctx = TestContext::with_promotion_pending().await;\n    ctx.ceo_sign_as(ctx.user_a, ctx.promotion_id).await;\n    let r = ctx.try_chro_sign_as(ctx.user_a, ctx.promotion_id).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn cascade_updates_hr_and_rew() {\n    let ctx = TestContext::with_dual_signed_promotion().await;\n    ctx.execute(ctx.promotion_id).await;\n    let hr_level = ctx.fetch_hr_mastery(ctx.member_id, ctx.skill_id).await;\n    let rew_band = ctx.fetch_rew_band(ctx.member_id).await;\n    assert_eq!(hr_level, 4);\n    assert!(rew_band.updated_at &gt; ctx.before_execute_time);\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-LEARN-004. <strong>Cross-module:</strong> FR-LEARN-005 (recommendation source), FR-HR-001, FR-REW-001, FR-CHAT-005, FR-AUTH-101 (CEO/CHRO), FR-MEMORY-111.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>One signer missing</td><td>gate</td><td>reject execute</td><td>wait</td></tr><tr><td>Same-person dual</td><td>validate</td><td>403</td><td>different signer</td></tr><tr><td>HR cascade fails</td><td>rollback</td><td>sev-1; status=approved-pending</td><td>retry</td></tr><tr><td>REW cascade fails</td><td>rollback</td><td>sev-1</td><td>retry</td></tr><tr><td>CHAT announce fails</td><td>non-blocking</td><td>sev-3</td><td>inherent</td></tr><tr><td>Duplicate promotion per council</td><td>UNIQUE</td><td>409</td><td>inherent</td></tr><tr><td>Council recommend=decline</td><td>block init</td><td>412</td><td>re-convene</td></tr><tr><td>Status workflow violation</td><td>check</td><td>409</td><td>follow workflow</td></tr><tr><td>Cross-tenant promotion</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>from_level &gt;= to_level</td><td>CHECK</td><td>400</td><td>fix levels</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Cascade order: HR (least-side-effect first) → REW (financial) → CHAT (visible last).</li><li>§11.2 Cascade transaction wraps HR + REW; CHAT outside (non-blocking).</li><li>§11.3 memory audit body: promotion_id, member_id, from/to level, status; decline SHA256.</li><li>§11.4 Decline at any pre-execute stage; council can be re-convened if needed.</li><li>§11.5 Future: peer announcement + congrats flow via FR-CHAT-005 reactions.</li></ul>\n<hr>\n<p><em>End of FR-LEARN-006 spec.</em></p>";
