window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LEARN-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The LEARN service <strong>MUST</strong> ship evidence at <code>services/learn/src/evidence/</code> with 5-kind closed enum + skill link + FR-DOC-001 scan ref + expiry cron, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>evidence_kind</code> against closed enum per DEC-2091.</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0002</code>: ``<code>sql CREATE TABLE learn_evidence ( evidence_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_id UUID NOT NULL, skill_id UUID REFERENCES learn_skills(skill_id), kind TEXT NOT NULL CHECK (kind IN ('degree','certification','course_completion','license','award')), name TEXT NOT NULL, issuer TEXT NOT NULL, issued_date DATE NOT NULL, expires_at DATE, verification_url TEXT, doc_id UUID,  -- FR-DOC-001 scan/photo verified BOOLEAN NOT NULL DEFAULT false, verified_by UUID, verified_at TIMESTAMPTZ, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); CREATE INDEX evidence_member_idx ON learn_evidence(tenant_id, member_id); CREATE INDEX evidence_expiry_idx ON learn_evidence(tenant_id, expires_at) WHERE expires_at IS NOT NULL; ALTER TABLE learn_evidence ENABLE ROW LEVEL SECURITY; CREATE POLICY evidence_rls ON learn_evidence USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON learn_evidence FROM cyberos_app; GRANT UPDATE (verified, verified_by, verified_at) ON learn_evidence TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> run expiry cron per DEC-2093 at 02:00 tenant_tz — flag certs expiring ≤30d; notify CHRO via FR-CHAT-005.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/learn/members/{id}/evidence       body: {kind, name, issuer, issued_date, expires_at?, verification_url?, doc_id?, skill_id?} POST /v1/learn/evidence/{id}/verify        (CHRO marks verified) GET  /v1/learn/members/{id}/evidence       (list) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2094. PII per FR-MEMORY-111: name + issuer text SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from add → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior evidence per DEC-2090 (append-only).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 5 kinds (DEC-2091)?</strong> Covers degree, certification, course, license, award — bounded.</p>\n<p><strong>Why expiry monitoring (DEC-2093)?</strong> Lapsed certifications expose compliance risk (e.g. legal practice cert).</p>\n<p><strong>Why FR-DOC-001 link (DEC-2092)?</strong> Visual evidence; auditors can see scans without downloading from external systems.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample evidence:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"certification\",\n  \"name\": \"AWS Solutions Architect Associate\",\n  \"issuer\": \"AWS\",\n  \"issued_date\": \"2025-01-15\",\n  \"expires_at\": \"2028-01-15\",\n  \"verification_url\": \"https://aws.amazon.com/verify/abc-123\",\n  \"doc_id\": \"uuid-scan\",\n  \"skill_id\": \"uuid-aws-skill\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>evidence_kind enum cardinality 5</strong>. 2. <strong>Issuer required</strong>. 3. <strong>Issued_date required</strong>. 4. <strong>expires_at optional</strong>. 5. <strong>doc_id optional FR-DOC-001 ref</strong>. 6. <strong>skill_id optional FR-LEARN-001 link</strong>. 7. <strong>Verification flag CHRO-only set</strong>. 8. <strong>Expiry cron 02:00 daily</strong>. 9. <strong>30d-expiring flagged + CHAT notification</strong>. 10. <strong>4 memory audit kinds emitted</strong>. 11. <strong>PII scrubbed (name+issuer SHA256)</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Append-only via REVOKE except verify cols</strong>. 15. <strong>expiry_idx for fast cron</strong>. 16. <strong>member_idx for profile view</strong>. 17. <strong>Verification_url stored as URL string</strong>. 18. <strong>FK to doc_id allows NULL</strong>. 19. <strong>FK to skill_id allows NULL</strong>. 20. <strong>Renewal = new evidence row (preserves history)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn evidence_kind_enum_enforced() {\n    let r = ctx.add_evidence(ctx.member_id, \"invalid_kind\", ...).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn expiry_30d_flagged() {\n    let ctx = TestContext::with_cert_expiring_in_25d().await;\n    ctx.run_expiry_cron().await;\n    let audits = ctx.fetch_memory_audits(\"learn.evidence_expired\").await;\n    assert!(!audits.is_empty() || ctx.check_expiring_alert_sent().await);\n}\n\n#[tokio::test]\nasync fn append_only_no_update() {\n    let ctx = TestContext::with_evidence().await;\n    let r = ctx.try_update_evidence(ctx.evidence_id, \"new name\").await;\n    assert!(r.is_err());\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-LEARN-001. <strong>Cross-module:</strong> FR-DOC-001 (scans), FR-MCP-007 (cron), FR-CHAT-005 (notification), FR-AUTH-101 (CHRO), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid kind</td><td>CHECK</td><td>400</td><td>use valid</td></tr><tr><td>Missing issuer</td><td>validate</td><td>400</td><td>provide</td></tr><tr><td>Cross-tenant doc_id</td><td>FK + RLS</td><td>404</td><td>inherent</td></tr><tr><td>Expiry cron skipped</td><td>catch-up</td><td>inherent</td><td>inherent</td></tr><tr><td>Verification by non-CHRO</td><td>role check</td><td>403</td><td>request CHRO</td></tr><tr><td>Expired evidence renewal</td><td>new row</td><td>inherent</td><td>inherent</td></tr><tr><td>Skill_id deleted</td><td>FK NULL</td><td>inherent</td><td>data fix</td></tr><tr><td>verification_url malformed</td><td>validate</td><td>400</td><td>fix</td></tr><tr><td>Cross-tenant evidence</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Large bulk import</td><td>batch</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Expiry cron via FR-MCP-007 <code>kind: 'learn.evidence_expiry'</code>, daily 02:00.</li><li>§11.2 Renewal flow: new evidence row with updated dates; UI links to prior for chain.</li><li>§11.3 memory audit body: member_id, evidence_id, kind; name+issuer SHA256.</li><li>§11.4 Verification process: CHRO manual, optional FR-AI-003 auto-verify via verification_url scrape.</li><li>§11.5 PDPL: evidence may contain PII; covered by tenant data scope.</li></ul>\n<hr>\n<p><em>End of FR-LEARN-002 spec.</em></p>";
