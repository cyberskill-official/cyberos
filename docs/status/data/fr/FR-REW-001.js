window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-REW-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The REW service <strong>MUST</strong> ship 3P income schema at <code>services/rew/src/comp/</code> with 8-kind enum + separate KMS keyspace + CFO-only decrypt + immutable rows, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>income_kind</code> against closed enum per DEC-2151.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0001</code>: ```sql CREATE TABLE rew_comp_records ( comp_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_id UUID NOT NULL, income_kind TEXT NOT NULL CHECK (income_kind IN ('p1_base','p2_allowance_housing','p2_allowance_transport','p2_allowance_meal','p2_allowance_other','p3_bonus_quarterly','p3_commission','p3_spot_award')), encrypted_amount_vnd BYTEA NOT NULL,  -- encrypted via REW keyspace KMS encryption_kms_key_arn TEXT NOT NULL, currency CHAR(3) NOT NULL DEFAULT 'VND', valid_from DATE NOT NULL, valid_to DATE, set_by UUID NOT NULL, correction_of UUID REFERENCES rew_comp_records(comp_id), trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); CREATE INDEX comp_member_idx ON rew_comp_records(tenant_id, member_id, income_kind, valid_from DESC); ALTER TABLE rew_comp_records ENABLE ROW LEVEL SECURITY; CREATE POLICY comp_rls ON rew_comp_records USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON rew_comp_records FROM cyberos_app;</li></ol>\n<p>   CREATE TABLE rew_comp_access_log (      log_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      accessor_id UUID NOT NULL,      comp_id UUID,      accessor_role TEXT NOT NULL,      access_kind TEXT NOT NULL,      succeeded BOOLEAN NOT NULL,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    ALTER TABLE rew_comp_access_log ENABLE ROW LEVEL SECURITY;    CREATE POLICY access_log_rls ON rew_comp_access_log      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON rew_comp_access_log FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> use separate KMS keyspace per DEC-2152 — <code>rew-{tenant_id}</code> alias, distinct from <code>hr-{tenant_id}</code> and <code>hr-cccd-{tenant_id}</code>.</li></ol>\n<ol><li><strong>MUST</strong> gate decrypt to ROOT-CFO only via <code>access_gate.rs::check(user)</code> per DEC-2152 — other roles → 403 + sev-1 audit.</li></ol>\n<ol><li><strong>MUST</strong> correct via new row per DEC-2153 — <code>correction_of</code> points to prior; prior's valid_to set.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/rew/comp                       (CFO sets/corrects) GET  /v1/rew/comp/{id}/decrypt          (ROOT-CFO only) GET  /v1/rew/members/{id}/comp-history  (CFO sees encrypted refs) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2154. PII per FR-MEMORY-111: encrypted_amount never in memory chain; income_kind enum ok; member_id (uuid) ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from set / decrypt → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> decrypt for non-CFO per DEC-2152.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior comp row per DEC-2153.</li></ol>\n<ol><li><strong>MUST NOT</strong> share KMS keyspace with HR per DEC-2152.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 8-kind enum (DEC-2151)?</strong> Captures real 3P composition; bounded prevents add-hoc additions.</p>\n<p><strong>Why separate keyspace (DEC-2152)?</strong> Defense in depth — HR breach doesn't expose comp; comp breach doesn't expose HR PII.</p>\n<p><strong>Why CFO-only decrypt (DEC-2152)?</strong> Principle of least privilege — even CHRO doesn't need decrypt access.</p>\n<p><strong>Why immutable (DEC-2153)?</strong> Payroll replay (FR-REW-002) requires deterministic history; retroactive changes break audit.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample comp set:</p>\n<pre><code class=\"language-json\">POST /v1/rew/comp\n{\n  \"member_id\": \"uuid\",\n  \"income_kind\": \"p1_base\",\n  \"amount_vnd\": 30000000,\n  \"valid_from\": \"2026-06-01\"\n}</code></pre>\n<p>Sample decrypt response (CFO-only):</p>\n<pre><code class=\"language-json\">{\n  \"comp_id\": \"uuid\",\n  \"income_kind\": \"p1_base\",\n  \"amount_vnd\": 30000000,\n  \"valid_from\": \"2026-06-01\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>income_kind enum cardinality 8</strong>. 2. <strong>CFO-only decrypt</strong>. 3. <strong>Non-CFO 403 + sev-1 audit</strong>. 4. <strong>Separate KMS keyspace (rew-{tenant})</strong>. 5. <strong>Immutable rows</strong>. 6. <strong>Correction via correction_of</strong>. 7. <strong>4 memory audit kinds emitted</strong>. 8. <strong>encrypted_amount never in memory chain</strong>. 9. <strong>RLS denies cross-tenant</strong>. 10. <strong>Trace_id preserved</strong>. 11. <strong>Access log append-only</strong>. 12. <strong>Member-week comp query indexable</strong>. 13. <strong>valid_from + valid_to range</strong>. 14. <strong>Currency CHAR(3) default VND</strong>. 15. <strong>set_by audit-traceable</strong>. 16. <strong>Tenant comp KMS key created at provisioning</strong>. 17. <strong>Cross-tenant KMS rejection</strong>. 18. <strong>CFO email notification on decrypt (sev-1)</strong>. 19. <strong>Append-only via REVOKE UPDATE/DELETE</strong>. 20. <strong>3P composition: P1 + P2 + P3 = total comp</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn non_cfo_decrypt_denied() {\n    let ctx = TestContext::with_encrypted_comp().await;\n    let r = ctx.try_decrypt_as(ctx.am_user, ctx.comp_id).await;\n    assert_eq!(r.status_code, 403);\n    let logs = ctx.fetch_access_log(ctx.comp_id).await;\n    assert!(logs.iter().any(|l| !l.succeeded));\n}\n\n#[tokio::test]\nasync fn separate_keyspace() {\n    let ctx = TestContext::with_comp_and_hr_records().await;\n    let comp_arn = ctx.fetch_comp_kms_arn(ctx.comp_id).await;\n    let hr_arn = ctx.fetch_hr_kms_arn(ctx.member_id).await;\n    assert_ne!(comp_arn, hr_arn);\n    assert!(comp_arn.contains(\"rew-\"));\n    assert!(hr_arn.contains(\"hr-\"));\n}\n\n#[tokio::test]\nasync fn immutable_append_only() {\n    let ctx = TestContext::with_comp_record().await;\n    let r = ctx.try_update_comp(ctx.comp_id, 50000000).await;\n    assert!(r.is_err());\n    let r2 = ctx.set_comp_correction(ctx.comp_id, 50000000).await;\n    assert!(r2.is_ok());\n    let row = ctx.fetch_comp_row(r2.new_comp_id).await;\n    assert_eq!(row.correction_of, Some(ctx.comp_id));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-HR-001. <strong>Downstream:</strong> FR-REW-002 (versioning), FR-REW-003 (P1 invariant), FR-REW-005 (payroll), FR-REW-007 (BP), FR-REW-010 (memory exclusion). <strong>Cross-module:</strong> FR-AUTH-105 (KMS), FR-AUTH-101 (CFO role), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>KMS decrypt fail</td><td>KMS err</td><td>sev-1</td><td>retry; check IAM</td></tr><tr><td>Non-CFO bypass</td><td>role check</td><td>403 + sev-1</td><td>investigate</td></tr><tr><td>Cross-tenant KMS</td><td>RLS + arn check</td><td>403 + sev-1</td><td>inherent</td></tr><tr><td>Income kind invalid</td><td>CHECK</td><td>400</td><td>use valid</td></tr><tr><td>Currency invalid</td><td>validate</td><td>400</td><td>use ISO 4217</td></tr><tr><td>Decimal precision loss</td><td>rust_decimal</td><td>inherent</td><td>inherent</td></tr><tr><td>Concurrent comp set</td><td>inherent</td><td>both append</td><td>inherent</td></tr><tr><td>Correction chain too deep</td><td>sanity warn</td><td>sev-3</td><td>inherent</td></tr><tr><td>Cross-tenant comp view</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>KMS key disabled</td><td>sev-1</td><td>inherent</td><td>CISO action</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 KMS key created at tenant provisioning via FR-AUTH-105 KMS module; alias <code>rew-{tenant_id}</code>.</li><li>§11.2 Amount stored encrypted at row level; decryption per-row via KMS.</li><li>§11.3 Encryption envelope: amount_vnd → JSON → encrypt → BYTEA.</li><li>§11.4 CFO email notification on decrypt via FR-EMAIL-009 (sev-1 path).</li><li>§11.5 memory audit body: comp_id, member_id, income_kind, access_kind; amount NEVER in chain.</li></ul>\n<hr>\n<p><em>End of FR-REW-001 spec.</em></p>";
