window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLUGIN-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PLUGIN module <strong>MUST</strong> ship the MCP bridge server at <code>services/plugin-host/</code> as a single Rust binary <code>cyberos-mcp-bridge</code>. The bridge implements MCP 2025-11-25 protocol and exposes 8 CyberOS tools (CUO + memory + SKILL) to any MCP-compliant host.</p>\n<ol><li><strong>MUST</strong> ship as single binary supporting BOTH transports per DEC-2410 + DEC-2411:</li></ol>\n<ul><li><strong>stdio</strong> — default for desktop hosts. Binary reads JSON-RPC frames from stdin, writes responses to stdout. Activated via <code>--transport stdio</code> or default when no flag.</li><li><strong>HTTP</strong> — for cloud/server hosts. Listens on <code>--listen 0.0.0.0:8082</code>. Uses MCP HTTP-streaming transport per spec. Activated via <code>--transport http</code>.</li></ul>\n<ol><li><strong>MUST</strong> implement the MCP 2025-11-25 handshake per FR-MCP-001 + DEC-2412:</li></ol>\n<ul><li><code>initialize</code> request: client sends <code>protocolVersion</code>, <code>capabilities</code>, <code>clientInfo</code>. Bridge responds with own <code>protocolVersion: \"2025-11-25\"</code>, <code>capabilities: {tools: {listChanged: false}, logging: {}}</code>, <code>serverInfo: {name: \"cyberos-mcp-bridge\", version: &lt;semver&gt;}</code>.</li><li><code>initialized</code> notification from client → bridge marks session ready.</li><li>Subsequent <code>tools/list</code> and <code>tools/call</code> requests proceed.</li></ul>\n<ol><li><strong>MUST</strong> expose exactly 8 tools at first ship per DEC-2413. Names follow SEP-986 per FR-MCP-003:</li></ol>\n<ul><li><code>cyberos.cuo.list_personas</code> — returns the 47 personas + metadata</li><li><code>cyberos.cuo.list_workflows</code> — for a given persona, return workflows + skill chains</li><li><code>cyberos.cuo.route</code> — two-stage natural-language router; returns persona+workflow</li><li><code>cyberos.cuo.execute_workflow</code> — runs a workflow chain (Tasks primitive — see clause 5)</li><li><code>cyberos.memory.read_audit</code> — returns audit rows for given (actor, kind, since_seq, limit); read-only</li><li><code>cyberos.memory.append_audit</code> — appends one audit row (write — requires <code>cyberos:memory:write</code> scope)</li><li><code>cyberos.skill.list_catalog</code> — returns 104 author+audit pairs + descriptions</li><li><code>cyberos.skill.invoke_skill</code> — invokes one skill by id with given inputs (Tasks primitive)</li></ul>\n<ol><li><strong>MUST</strong> wire each tool to its source-of-truth module per architecture:</li></ol>\n<ul><li>CUO tools → call <code>modules/cuo/</code> Python supervisor via subprocess invocation through <code>cyberos-cuo</code> binary, OR via in-process FFI if the supervisor ships as a library by then</li><li>memory tools → call <code>services/memory/</code> HTTP REST at <code>MEMORY_ENDPOINT</code> env var</li><li>SKILL tools → call <code>services/skill-broker/</code> (in flight per FR-SKILL-103) OR file-scan <code>modules/skill/</code> catalog if broker not ready</li></ul>\n<ol><li><strong>MUST</strong> implement MCP Tasks primitive per FR-MCP-007 for <code>cyberos.cuo.execute_workflow</code> + <code>cyberos.skill.invoke_skill</code> per DEC-2414. Long-running tools:</li></ol>\n<ul><li>Return <code>{task_id, status: \"running\"}</code> immediately on call</li><li>Support <code>tasks/get?id=&lt;task_id&gt;</code> for status polling (status: running / completed / failed / cancelled)</li><li>Support <code>tasks/cancel?id=&lt;task_id&gt;</code></li><li>Persist task state to PostgreSQL (<code>plugin_host.tasks</code> table) so reconnects resume in-flight tasks</li><li>10-minute default timeout; tools can override via input field</li></ul>\n<ol><li><strong>MUST</strong> be stateless across requests per DEC-2415. No in-memory session affinity; every request authenticates via OAuth-PKCE JWT (FR-PLUGIN-005) carrying <code>tenant_id</code> claim. The bridge MUST NOT cache identity between requests. Long-running tasks persist their state in Postgres, not in memory.</li></ol>\n<ol><li><strong>MUST</strong> return errors in 4 distinct classes per DEC-2416 + clause 8 error envelope below:</li></ol>\n<ul><li><code>input_validation</code> — input doesn't match tool's input_schema; HTTP 422 / JSON-RPC code -32602</li><li><code>authz_denied</code> — token scope insufficient for the tool; HTTP 403 / JSON-RPC code -32000</li><li><code>upstream_unavailable</code> — downstream service (memory HTTP, CUO subprocess, SKILL broker) failed or timed out; HTTP 503 / JSON-RPC code -32001</li><li><code>internal_error</code> — bridge bug; HTTP 500 / JSON-RPC code -32603 Every error response carries <code>error.class</code>, <code>error.message</code>, <code>error.trace_id</code>, and an actionable <code>error.hint</code>.</li></ul>\n<ol><li><strong>MUST</strong> emit OpenTelemetry spans per DEC-2417 for every tool call with attributes <code>cyberos.plugin_id</code>, <code>cyberos.tool_name</code>, <code>cyberos.tenant_id</code>, <code>cyberos.trace_id</code>, <code>cyberos.duration_ms</code>, <code>cyberos.outcome</code> (success / error_class). Spans flow to FR-OBS-001 collector via OTLP gRPC.</li></ol>\n<ol><li><strong>MUST</strong> honour tool-annotation gating per FR-MCP-006. Tools with <code>destructive: true</code> or <code>external_effect: true</code> annotations (<code>cyberos.memory.append_audit</code> for example) require the host to set <code>mcp/sampling/elicit_confirm: true</code> in the call envelope OR include a pre-signed confirmation token. Bridge rejects un-confirmed destructive calls with <code>authz_denied</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate every <code>tools/call</code> request against the tool's declared <code>input_schema</code> per FR-PLUGIN-001 clause 1 before invoking the upstream. Schema-violation calls produce <code>input_validation</code> errors WITHOUT touching the upstream.</li></ol>\n<ol><li><strong>MUST</strong> include CORS headers when running in HTTP transport with <code>--cors-origin &lt;pattern&gt;</code> flag. Default: deny all origins (no CORS headers). Operators set the flag explicitly per deployment.</li></ol>\n<ol><li><strong>MUST NOT</strong> retain client-supplied state across requests beyond what task persistence requires (DEC-2415). No session cookies, no auth caches longer than the JWT's natural validity.</li></ol>\n<ol><li><strong>MUST NOT</strong> include CyberOS-internal data in error messages — error bodies are user-safe but do NOT leak: memory audit row contents, JWT bytes, internal DB IDs (use trace_id instead), tenant data from other tenants.</li></ol>\n<ol><li><strong>MUST NOT</strong> speak any MCP protocol version other than 2025-11-25 in v1 per DEC-2412. Future protocol versions land via FR-MCP-001b/c successor FRs.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why single binary, both transports (DEC-2410)?</strong> Two binaries doubles the maintenance + signing + audit surface. The transport abstraction (<code>transport/mod.rs</code> trait) costs ~80 lines and saves duplicating the handler stack. Hosts pick the transport that fits — desktop hosts get stdio (no port binding), cloud hosts get HTTP (load-balanced).</p>\n<p><strong>Why stdio default (DEC-2411)?</strong> Claude Code, Cursor, Codex CLI all use stdio in 2026 — process-per-plugin model. HTTP would require port allocation per plugin, exposes attack surface, and requires the host to manage process supervision differently. Stdio matches existing host expectations.</p>\n<p><strong>Why MCP 2025-11-25 only (DEC-2412, clause 14)?</strong> Mixing protocol versions in one binary multiplies code paths quadratically. FR-MCP-001 already commits the gateway to this version. Plugin bridge inherits.</p>\n<p><strong>Why exactly 8 tools at first ship (DEC-2413)?</strong> Each tool is an API contract that must be stable (clients depend on the name + schema). Shipping 8 well-considered tools is far better than 30 hastily-named ones. The 8 cover the most common host workflows: orchestration (CUO 4), memory (memory 2), skill discovery (SKILL 2). Future tools land via FR-PLUGIN-002a/b/c.</p>\n<p><strong>Why subprocess invocation for CUO (clause 4)?</strong> CUO supervisor is Python; bridge is Rust. Without a Python embedding (PyO3 is heavy), the cleanest IPC is subprocess + JSON-RPC over stdin/stdout. Subprocess invocation also gives clean process isolation — a CUO crash doesn't take the bridge down.</p>\n<p><strong>Why HTTP REST for memory (clause 4)?</strong> memory service ships as a Fargate binary (per FR-MEMORY-104). HTTP REST is the natural client surface. Bridge can call it from any deployment context.</p>\n<p><strong>Why Tasks primitive for long-running tools (DEC-2414, clause 5)?</strong> CUO workflow chains run for 30 seconds to several minutes. A synchronous JSON-RPC response would either timeout or hold the connection. MCP Tasks primitive (FR-MCP-007) is the canonical solution: client receives a handle, polls status, optionally cancels. Reconnect-resume is essential for desktop hosts that may disconnect.</p>\n<p><strong>Why Postgres-backed task state (clause 5)?</strong> In-memory task state breaks reconnect-resume. SQLite works for single-instance, but bridge scaling to multiple instances (Fargate) requires shared store. Postgres is already in the stack for AUTH + memory; reusing it costs nothing.</p>\n<p><strong>Why stateless across requests (DEC-2415, clause 6)?</strong> Stateful bridges require sticky sessions, complicating load balancers and rolling deploys. Statelessness with JWT auth is the cloud-native default.</p>\n<p><strong>Why 4 error classes (DEC-2416, clause 7)?</strong> Hosts surface different UI for different error classes: input_validation → \"fix your input\" inline; authz_denied → \"request more scopes\" CTA; upstream_unavailable → \"try again in a moment\" retry banner; internal_error → silent telemetry + generic apology. Without the taxonomy, hosts default to generic messages and users don't know what to do.</p>\n<p><strong>Why required OTel spans (DEC-2417, clause 8)?</strong> Plugin tool-call failures span 4 systems (host → bridge → upstream module → downstream service). Trace correlation requires every hop to emit spans. The bridge is the middle hop and must emit; otherwise the trace is fragmented and root-cause analysis is hand-stitched.</p>\n<p><strong>Why CORS off by default (clause 11)?</strong> HTTP transport is for cloud hosts that authenticate before reaching the bridge. Open CORS would let any web page call the bridge with the user's token. Closed-by-default + explicit per-deployment opt-in is safe.</p>\n<p><strong>Why don't error messages leak audit data (clause 13)?</strong> Plugins run with user-scoped tokens. An error message that includes \"audit row seq 4827 was about &lt;subject&gt;\" would leak data the user is not authorised to read in this context. Trace_id is the safe correlation token.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Initialize handshake</h3>\n<p>Client → bridge:</p>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"initialize\", \"params\": {\n  \"protocolVersion\": \"2025-11-25\",\n  \"capabilities\": {\"sampling\": {}, \"elicitation\": {}},\n  \"clientInfo\": {\"name\": \"claude-code\", \"version\": \"0.42.0\"}\n}}</code></pre>\n<p>Bridge → client:</p>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 1, \"result\": {\n  \"protocolVersion\": \"2025-11-25\",\n  \"capabilities\": {\"tools\": {\"listChanged\": false}, \"logging\": {}},\n  \"serverInfo\": {\"name\": \"cyberos-mcp-bridge\", \"version\": \"1.0.0\"}\n}}</code></pre>\n<h3>tools/list response (shape)</h3>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 2, \"result\": {\"tools\": [\n  {\n    \"name\": \"cyberos.cuo.list_personas\",\n    \"description\": \"List the 47 active CyberOS personas (CTO, CFO, CPO, ...) with metadata for each.\",\n    \"inputSchema\": {\"type\": \"object\", \"properties\": {}, \"additionalProperties\": false},\n    \"annotations\": {\"destructive\": false, \"write\": false, \"external_effect\": false}\n  },\n  // ... 7 more tools\n]}}</code></pre>\n<h3>tools/call for long-running execute_workflow</h3>\n<p>Client → bridge:</p>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 3, \"method\": \"tools/call\", \"params\": {\n  \"name\": \"cyberos.cuo.execute_workflow\",\n  \"arguments\": {\n    \"persona\": \"chief-technology-officer\",\n    \"workflow\": \"architect-new-system\",\n    \"inputs\": {\"context\": \"Build a payment routing system for SEA markets\"}\n  }\n}}</code></pre>\n<p>Bridge → client (immediate, before workflow completes):</p>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 3, \"result\": {\n  \"content\": [{\"type\": \"text\", \"text\": \"{\\\"task_id\\\":\\\"t-abc123\\\",\\\"status\\\":\\\"running\\\"}\"}],\n  \"isError\": false\n}}</code></pre>\n<p>Client polls:</p>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 4, \"method\": \"tasks/get\", \"params\": {\"id\": \"t-abc123\"}}</code></pre>\n<p>Bridge → client when complete:</p>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 4, \"result\": {\n  \"task_id\": \"t-abc123\",\n  \"status\": \"completed\",\n  \"output\": {\"steps_executed\": 10, \"final_artifacts\": [...]},\n  \"duration_ms\": 47230\n}}</code></pre>\n<h3>Error envelope</h3>\n<pre><code class=\"language-json\">{\"jsonrpc\": \"2.0\", \"id\": 5, \"error\": {\n  \"code\": -32000,\n  \"message\": \"Tool 'cyberos.memory.append_audit' requires scope 'cyberos:memory:write' which is not in your token.\",\n  \"data\": {\n    \"class\": \"authz_denied\",\n    \"trace_id\": \"01HXXXXXXXXXXXXXXXXXXXXXXX\",\n    \"hint\": \"Re-authorise the plugin with the 'cyberos:memory:write' scope, or have an admin grant it.\",\n    \"missing_scopes\": [\"cyberos:memory:write\"]\n  }\n}}</code></pre>\n<h3>Postgres schema for task persistence</h3>\n<pre><code class=\"language-sql\">CREATE TABLE plugin_host.tasks (\n  task_id TEXT PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  plugin_id TEXT NOT NULL,\n  tool_name TEXT NOT NULL,\n  status TEXT NOT NULL CHECK (status IN ('running','completed','failed','cancelled')),\n  input JSONB NOT NULL,\n  output JSONB,\n  error JSONB,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  completed_at TIMESTAMPTZ,\n  timeout_at TIMESTAMPTZ NOT NULL,\n  trace_id CHAR(32) NOT NULL\n);\nALTER TABLE plugin_host.tasks ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tasks_rls ON plugin_host.tasks\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nCREATE INDEX ON plugin_host.tasks (tenant_id, status, created_at DESC);</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Initialize handshake succeeds</strong> — bridge returns protocolVersion 2025-11-25 on <code>initialize</code>.</li><li><strong>tools/list returns 8 tools</strong> — exactly 8 entries, all with SEP-986-conformant names.</li><li><strong>Tool name pattern enforced</strong> — bridge rejects renaming a tool to a non-SEP-986 string at compile time.</li><li><strong>execute_workflow returns task_id</strong> — synchronous response contains running status + task_id within 100ms.</li><li><strong>Task status polling works</strong> — <code>tasks/get</code> returns current status; transitions running → completed.</li><li><strong>Task cancellation works</strong> — <code>tasks/cancel</code> aborts upstream subprocess and marks task cancelled.</li><li><strong>Reconnect-resume works</strong> — disconnect/reconnect on same task_id retrieves current state from Postgres.</li><li><strong>Cross-tenant denied</strong> — token for tenant A cannot retrieve task created by tenant B (RLS).</li><li><strong>Input validation rejects malformed args</strong> — calling <code>cyberos.cuo.execute_workflow</code> with missing required <code>persona</code> returns input_validation error.</li><li><strong>Authz denied on missing scope</strong> — calling <code>cyberos.memory.append_audit</code> with read-only token returns authz_denied with <code>missing_scopes</code> array.</li><li><strong>Upstream unavailable on memory down</strong> — when memory HTTP is unreachable, bridge returns upstream_unavailable (not internal_error).</li><li><strong>Internal error on bridge bug</strong> — panic in handler returns internal_error envelope (panic_hook catches).</li><li><strong>All errors carry trace_id</strong> — 4 error classes, 4 fixture tests, all assert trace_id present.</li><li><strong>OTel span emitted per tool call</strong> — test inspects OTel exporter, finds span with cyberos.tool_name attribute.</li><li><strong>Destructive tool requires confirmation</strong> — <code>cyberos.memory.append_audit</code> without <code>elicit_confirm: true</code> returns authz_denied.</li><li><strong>Stateless across requests</strong> — second <code>tools/call</code> with new JWT for different tenant returns different tenant's results.</li><li><strong>Task timeout enforced</strong> — task running &gt; timeout_at marked failed; status: failed; error.class: internal_error with timeout hint.</li><li><strong>CORS denies by default</strong> — HTTP transport without <code>--cors-origin</code> flag returns no CORS headers; browser-origin OPTIONS denied.</li><li><strong>CORS allows configured origin</strong> — <code>--cors-origin https://example.com</code> adds Access-Control-Allow-Origin header on matching requests.</li><li><strong>stdio transport echoes JSON-RPC</strong> — send initialize via stdin → response on stdout; no port binding.</li><li><strong>HTTP transport binds port</strong> — <code>--listen 0.0.0.0:8082</code> opens port; <code>/healthz</code> returns 200.</li><li><strong>Tool error message does not leak audit content</strong> — failing <code>read_audit</code> for missing row returns generic message + trace_id, no body bytes.</li><li><strong>Initialize negotiates capabilities</strong> — client sends <code>capabilities.sampling: {}</code>; bridge responds without sampling in its own caps; client knows.</li><li><strong>Protocol version mismatch rejected</strong> — client sends <code>protocolVersion: \"2024-01-01\"</code>; bridge returns error code -32602 with hint.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/initialize_handshake_test.rs\n#[tokio::test]\nasync fn initialize_returns_2025_11_25() {\n    let bridge = TestBridge::new_stdio().await;\n    let resp = bridge.send(json!({\n        \"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\n        \"params\":{\"protocolVersion\":\"2025-11-25\",\"capabilities\":{},\"clientInfo\":{\"name\":\"test\",\"version\":\"0.1.0\"}}\n    })).await;\n    assert_eq!(resp[\"result\"][\"protocolVersion\"], \"2025-11-25\");\n    assert_eq!(resp[\"result\"][\"serverInfo\"][\"name\"], \"cyberos-mcp-bridge\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/tools_list_returns_8_tools_test.rs\n#[tokio::test]\nasync fn tools_list_has_exactly_8() {\n    let bridge = TestBridge::new_stdio().await.initialized().await;\n    let resp = bridge.send_method(\"tools/list\", json!({})).await;\n    let tools = resp[\"result\"][\"tools\"].as_array().unwrap();\n    assert_eq!(tools.len(), 8);\n    let names: Vec&lt;&amp;str&gt; = tools.iter().map(|t| t[\"name\"].as_str().unwrap()).collect();\n    let sep_pattern = regex::Regex::new(r\"^cyberos\\.[a-z][a-z0-9]*\\.[a-z][a-z0-9_]*$\").unwrap();\n    for name in &amp;names {\n        assert!(sep_pattern.is_match(name), \"name '{}' violates SEP-986\", name);\n    }\n    assert!(names.contains(&amp;\"cyberos.cuo.execute_workflow\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/execute_workflow_as_task_test.rs\n#[tokio::test]\nasync fn execute_workflow_returns_task_id_and_completes() {\n    let bridge = TestBridge::new_with_mock_cuo().await.initialized().await;\n    let resp = bridge.tools_call(\"cyberos.cuo.execute_workflow\", json!({\n        \"persona\": \"chief-technology-officer\",\n        \"workflow\": \"adr-quick-capture\",\n        \"inputs\": {\"title\": \"test\"}\n    })).await;\n    let body: serde_json::Value = serde_json::from_str(\n        resp[\"result\"][\"content\"][0][\"text\"].as_str().unwrap()\n    ).unwrap();\n    let task_id = body[\"task_id\"].as_str().unwrap().to_string();\n    assert_eq!(body[\"status\"], \"running\");\n\n    // Poll until complete (≤ 5s)\n    for _ in 0..50 {\n        let s = bridge.tasks_get(&amp;task_id).await;\n        if s[\"status\"] == \"completed\" { return; }\n        tokio::time::sleep(Duration::from_millis(100)).await;\n    }\n    panic!(\"task did not complete\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/error_class_taxonomy_test.rs\n#[tokio::test]\nasync fn missing_scope_returns_authz_denied() {\n    let bridge = TestBridge::new_with_read_only_token().await.initialized().await;\n    let resp = bridge.tools_call(\"cyberos.memory.append_audit\", json!({\n        \"kind\": \"decision\", \"body\": {}\n    })).await;\n    assert_eq!(resp[\"error\"][\"data\"][\"class\"], \"authz_denied\");\n    assert!(resp[\"error\"][\"data\"][\"missing_scopes\"]\n        .as_array().unwrap().contains(&amp;json!(\"cyberos:memory:write\")));\n    assert!(resp[\"error\"][\"data\"][\"trace_id\"].is_string());\n}\n\n#[tokio::test]\nasync fn malformed_input_returns_input_validation() {\n    let bridge = TestBridge::new_with_full_token().await.initialized().await;\n    let resp = bridge.tools_call(\"cyberos.cuo.execute_workflow\", json!({\n        // missing required 'persona'\n        \"workflow\": \"adr-quick-capture\", \"inputs\": {}\n    })).await;\n    assert_eq!(resp[\"error\"][\"data\"][\"class\"], \"input_validation\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/cross_tenant_denied_test.rs\n#[tokio::test]\nasync fn rls_prevents_cross_tenant_task_read() {\n    let tenant_a = TestBridge::new_for_tenant(\"a\").await.initialized().await;\n    let resp = tenant_a.tools_call(\"cyberos.cuo.execute_workflow\", workflow_args()).await;\n    let task_id = extract_task_id(&amp;resp);\n\n    let tenant_b = TestBridge::new_for_tenant(\"b\").await.initialized().await;\n    let resp_b = tenant_b.tasks_get(&amp;task_id).await;\n    assert_eq!(resp_b[\"error\"][\"data\"][\"class\"], \"input_validation\"); // task not found from b's view\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>Bridge crate layout:</p>\n<pre><code>services/plugin-host/\n├── Cargo.toml                              (cyberos-plugin-host crate)\n├── src/\n│   ├── main.rs                             (transport dispatch: stdio | http)\n│   ├── transport/\n│   │   ├── mod.rs                          (Transport trait)\n│   │   ├── stdio.rs                        (read stdin frames, write stdout)\n│   │   └── http.rs                         (axum router with /mcp endpoint)\n│   ├── handlers/\n│   │   ├── initialize.rs                   (capabilities negotiation)\n│   │   ├── tools_list.rs                   (static 8-tool registry)\n│   │   └── tools_call.rs                   (dispatch + Tasks for long-running)\n│   ├── tools/\n│   │   ├── cuo.rs                          (4 tools via cyberos-cuo subprocess)\n│   │   ├── memory.rs                        (2 tools via memory HTTP)\n│   │   └── skill.rs                        (2 tools via skill-broker / fs scan)\n│   ├── error.rs                            (4-class taxonomy + envelope)\n│   └── otel.rs                             (span emission)\n└── tests/                                  (5 integration tests)</code></pre>\n<p>Tool registry is static (<code>tools_list.rs::TOOLS: &amp;[ToolDef; 8]</code>) — no dynamic registration in v1.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> FR-PLUGIN-001 (manifest schema — bridge tools MUST match manifest tool list); FR-MCP-001 (MCP 2025-11-25 spec); FR-MCP-003 (SEP-986 naming).</li><li><strong>Downstream:</strong> FR-PLUGIN-003 (slash commands invoke bridge tools); FR-PLUGIN-007 (multi-runtime adapters bundle this binary).</li><li><strong>Cross-module:</strong> FR-MCP-006 (tool annotation gating — clause 9 enforces); FR-MCP-007 (Tasks primitive — clause 5 implements); FR-AUTH-004 (JWT verification, shipped); FR-MEMORY-101 (audit ingest, shipped); FR-OBS-001 (OTel collector — clause 8 emits to it).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<p>(See §3 for handshake, tools/list, tools/call, error envelopes, and DB schema.)</p>\n<h3>Sample tool: <code>cyberos.cuo.route</code> call</h3>\n<p>Request:</p>\n<pre><code class=\"language-json\">{\"jsonrpc\":\"2.0\",\"id\":7,\"method\":\"tools/call\",\"params\":{\n  \"name\":\"cyberos.cuo.route\",\n  \"arguments\":{\"query\":\"Architect a new payment routing system for Southeast Asian markets\"}\n}}</code></pre>\n<p>Response:</p>\n<pre><code class=\"language-json\">{\"jsonrpc\":\"2.0\",\"id\":7,\"result\":{\n  \"content\":[{\"type\":\"text\",\"text\":\"{\\\"persona\\\":\\\"chief-technology-officer\\\",\\\"workflow\\\":\\\"architect-new-system\\\",\\\"confidence\\\":0.92,\\\"alternates\\\":[\\\"chief-product-officer/product-roadmap\\\"]}\"}],\n  \"isError\":false\n}}</code></pre>\n<h3>Sample memory audit row from a plugin tool invocation</h3>\n<p>Emitted by <code>cyberos.memory.append_audit</code> itself, but also by <code>cyberos.cuo.execute_workflow</code> upon completion:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"plugin.invoked\",\n  \"actor_id\": \"00000000-...\",\n  \"tenant_id\": \"11111111-...\",\n  \"body\": {\n    \"plugin_id\": \"cyberos\",\n    \"plugin_version\": \"1.0.0\",\n    \"tool_name\": \"cyberos.cuo.execute_workflow\",\n    \"trace_id\": \"01HX...\",\n    \"duration_ms\": 47230,\n    \"outcome\": \"success\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<ul><li>~~Should the bridge embed CUO supervisor via PyO3?~~ → No, subprocess invocation per clause 4. Lighter dependency footprint, clean process isolation.</li><li>~~Should we support MCP 2024-11-05 alongside 2025-11-25?~~ → No, single version per DEC-2412 + clause 14. Multi-version handled in future FR.</li><li>~~Should task state live in SQLite for single-instance simplicity?~~ → No, Postgres per clause 5. Multi-instance is the deploy target.</li><li>~~Should CORS default to permissive for dev ergonomics?~~ → No, deny by default per clause 11. Dev opt-in is explicit <code>--cors-origin '*'</code>.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Client sends wrong protocol version</td><td>initialize handler version check</td><td>error -32602 with hint</td><td>Client upgrades to 2025-11-25</td></tr><tr><td>stdio frame malformed</td><td>JSON-RPC parser fails</td><td>error -32700 (parse error)</td><td>Client retries with valid frame</td></tr><tr><td>CUO subprocess crashes</td><td>tokio::process child exit code != 0</td><td>task marked failed; error class upstream_unavailable</td><td>Bridge spawns fresh subprocess on next call</td></tr><tr><td>memory HTTP times out</td><td>reqwest 5s timeout</td><td>upstream_unavailable</td><td>Retry with backoff or surface to user</td></tr><tr><td>Postgres unavailable</td><td>sqlx connection error</td><td>upstream_unavailable for task ops; degraded mode for short tools</td><td>Recovery on next call; alerting via FR-OBS-007</td></tr><tr><td>JWT signature invalid</td><td>auth middleware</td><td>authz_denied with hint \"re-authenticate\"</td><td>Client refreshes token</td></tr><tr><td>JWT expired</td><td>auth middleware exp claim check</td><td>authz_denied with hint \"token expired\"</td><td>Client refreshes via OAuth-PKCE (FR-PLUGIN-005)</td></tr><tr><td>Scope missing for destructive tool</td><td>tools_call.rs scope check</td><td>authz_denied with missing_scopes array</td><td>Client re-authorises with broader scope</td></tr><tr><td>Cross-tenant task fetch</td><td>Postgres RLS denies</td><td>input_validation \"task not found\" (intentionally generic)</td><td>Inherent — no recovery, no leak</td></tr><tr><td>Task timeout exceeded</td><td>tokio::time::timeout future</td><td>task marked failed; error.message includes timeout hint</td><td>Client invokes again with longer timeout if tool supports</td></tr><tr><td>Tool input fails JSON schema</td><td>tools_call.rs schema validator</td><td>input_validation with JSON-pointer to bad field</td><td>Client fixes input</td></tr><tr><td>Bridge panic in handler</td><td>catch_unwind in transport layer</td><td>internal_error envelope; OTel span tagged with outcome=panic</td><td>Service restart by Fargate health check</td></tr><tr><td>OTel exporter unreachable</td><td>exporter buffer fills</td><td>spans dropped (logged), tool call still succeeds</td><td>OTel recovers; spans buffered up to capacity</td></tr><tr><td>Concurrent task_id collision</td><td>Postgres PK collision</td><td>retry with new ID; if persistent, internal_error</td><td>Inherent (PK retry)</td></tr><tr><td>CORS preflight from disallowed origin</td><td>http handler checks origin allowlist</td><td>403 with explicit reason</td><td>Operator extends <code>--cors-origin</code> allowlist</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 <strong>Transport trait.</strong> <code>transport/mod.rs::Transport</code> exposes <code>async fn next_request(&amp;mut self) -&gt; Result&lt;Request&gt;</code> and <code>async fn send_response(&amp;mut self, resp: Response)</code>. stdio impl wraps tokio::io::BufReader; http impl wraps axum::Router state. Handlers are transport-agnostic.</li></ul>\n<ul><li>§11.2 <strong>Static tool registry.</strong> <code>tools_list.rs::TOOLS: &amp;[ToolDef; 8]</code> is a <code>phf</code> or simple <code>static</code> array. Each entry has <code>name</code>, <code>description</code>, <code>input_schema_json</code>, <code>annotations</code>, <code>handler_fn: fn(&amp;Args) -&gt; Result&lt;Output&gt;</code>. Adding tools is a code change, not config — keeps the contract tight in v1.</li></ul>\n<ul><li>§11.3 <strong>CUO subprocess invocation.</strong> Bridge spawns <code>cyberos-cuo execute &lt;persona&gt;/&lt;workflow&gt; --output-dir /tmp/&lt;task_id&gt; --invoker llm --memory-emit --actor &lt;subject_id&gt; --tenant &lt;tenant_id&gt;</code> and reads JSON output. Long-running tasks tail the output dir for step completions.</li></ul>\n<ul><li>§11.4 <strong>memory HTTP client.</strong> <code>reqwest::Client</code> with default 5s timeout, 3 retries with exponential backoff (250ms / 1s / 4s). Body bytes flow as <code>application/json</code>.</li></ul>\n<ul><li>§11.5 <strong>Task polling design.</strong> Client polls <code>tasks/get</code> every 500ms-1s typically. Bridge serves from Postgres directly (cheap read). For chatty clients, optional server-sent events (<code>tasks/stream?id=...</code>) lands in FR-PLUGIN-002a.</li></ul>\n<ul><li>§11.6 <strong>Reconnect resume.</strong> Task state in Postgres is the canonical record. On client reconnect, client re-sends <code>tasks/get</code>; bridge looks up by task_id (scoped by tenant_id via RLS). State is current as of last status update. Final output is delivered via the same poll loop.</li></ul>\n<ul><li>§11.7 <strong>Error envelope hints.</strong> Every error class has a <code>hint</code> field with an actionable suggestion. The hint text is in <code>error.rs::ERROR_HINTS</code> (HashMap&lt;&amp;str, &amp;str&gt;). This is the difference between \"permission denied\" and \"permission denied — re-authorise with scope X.\"</li></ul>\n<ul><li>§11.8 <strong>OTel span attributes.</strong> Each span captures: cyberos.plugin_id, cyberos.plugin_version, cyberos.tool_name, cyberos.tenant_id (hashed for privacy if FR-AI-011 PII redaction triggers), cyberos.trace_id, cyberos.duration_ms, cyberos.outcome. Spans flow via OTLP gRPC to FR-OBS-001 collector.</li></ul>\n<ul><li>§11.9 <strong>CORS implementation.</strong> axum's <code>tower-http::cors::CorsLayer</code> with allowlist origins. Default config has no <code>Allow-Origin</code> headers, which is browser-side denial.</li></ul>\n<ul><li>§11.10 <strong>Why no in-process Python.</strong> PyO3 adds heavy build dependencies (Python lib at link time) and complicates static binary distribution. Subprocess invocation costs ~10ms per call (acceptable for tool calls in the 100ms-30s range) and avoids the dependency.</li></ul>\n<ul><li>§11.11 <strong>Static binary.</strong> Compile with <code>RUSTFLAGS=\"-C target-feature=+crt-static\" cargo build --release --target x86_64-unknown-linux-musl</code> to produce a 100% static binary that installs without runtime deps. Matches the <code>services/auth/</code>, <code>services/memory/</code> patterns.</li></ul>\n<hr>\n<p><em>End of FR-PLUGIN-002 spec.</em></p>";
