window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-HR-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The HR service <strong>MUST</strong> ship CCCD photo encryption at <code>services/hr/src/cccd/</code> with separate KMS keyspace per tenant, ROOT-CHRO-only decrypt, sev-1 audit per access, consent gate, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>cccd_access_kind</code> against closed enum per DEC-1821.</li></ol>\n<ol><li><strong>MUST</strong> encrypt at <code>kms_wrapper.rs::encrypt(photo_bytes, tenant_id)</code>:</li></ol>\n<ul><li>Use tenant-specific KMS CMK with <code>KeyUsage=ENCRYPT_DECRYPT</code>, alias=<code>hr-cccd-{tenant_id}</code>.</li><li>Key created at tenant provisioning; rotation via CHRO action.</li></ul>\n<ol><li><strong>MUST</strong> require consent per DEC-1823 — <code>consent_token</code> parameter required at upload; validated against member's prior consent record.</li></ol>\n<ol><li><strong>MUST</strong> gate decrypt at <code>access_gate.rs::check(user, tenant)</code> per DEC-1820:</li></ol>\n<ul><li>Caller must have ROOT-CHRO role via FR-AUTH-101.</li><li>Other roles → 403 + emit <code>hr.cccd_access_denied</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> emit sev-1 audit on every decrypt per DEC-1822:</li></ol>\n<ul><li><code>hr.cccd_decrypted</code> with severity=1</li><li>CHRO email notification dispatched</li></ul>\n<ol><li><strong>MUST</strong> define tables at migration <code>0003</code>: ```sql CREATE TABLE hr_cccd_storage ( storage_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_id UUID NOT NULL UNIQUE, encrypted_photo BYTEA NOT NULL, encryption_kms_key_arn TEXT NOT NULL, consent_token UUID NOT NULL, consent_at TIMESTAMPTZ NOT NULL, uploaded_at TIMESTAMPTZ NOT NULL DEFAULT now(), trace_id CHAR(32) ); ALTER TABLE hr_cccd_storage ENABLE ROW LEVEL SECURITY; CREATE POLICY cccd_storage_rls ON hr_cccd_storage USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON hr_cccd_storage FROM cyberos_app; GRANT DELETE ON hr_cccd_storage TO cyberos_app;  -- CHRO can delete</li></ol>\n<p>   CREATE TABLE hr_cccd_access_log (      log_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      member_id UUID NOT NULL,      accessor_id UUID NOT NULL,      access_kind TEXT NOT NULL        CHECK (access_kind IN ('upload','decrypt_view','rotate_key','delete')),      accessor_role TEXT NOT NULL,      ip_address TEXT,  -- hashed      succeeded BOOLEAN NOT NULL,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    CREATE INDEX cccd_access_log_member_time_idx ON hr_cccd_access_log(tenant_id, member_id, created_at DESC);    ALTER TABLE hr_cccd_access_log ENABLE ROW LEVEL SECURITY;    CREATE POLICY cccd_log_rls ON hr_cccd_access_log      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON hr_cccd_access_log FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST   /v1/hr/members/{id}/cccd-photo          body: {photo_bytes, consent_token} (member-self or CHRO) GET    /v1/hr/members/{id}/cccd-photo          (ROOT-CHRO only; decrypts + audits sev-1) POST   /v1/hr/members/{id}/cccd-photo/rotate   (CHRO only — re-encrypt with new key) DELETE /v1/hr/members/{id}/cccd-photo          (member-self via DSAR or CHRO) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1824. PII per FR-MEMORY-111: encrypted_photo not in chain; access_kind + role ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id through all access paths.</li></ol>\n<ol><li><strong>MUST NOT</strong> decrypt for non-ROOT-CHRO per DEC-1820.</li></ol>\n<ol><li><strong>MUST NOT</strong> upload without consent_token per DEC-1823.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why separate KMS keyspace (DEC-1820)?</strong> Blast radius — compromise of system-wide key would leak all CCCDs; per-tenant key limits exposure to one tenant.</p>\n<p><strong>Why ROOT-CHRO-only decrypt (DEC-1820)?</strong> CCCD = government ID = sensitive; principle of least privilege. Audit team can't decrypt without explicit grant.</p>\n<p><strong>Why sev-1 on every decrypt (DEC-1822)?</strong> Internal abuse most common attack; every access surfaces immediately for CHRO review.</p>\n<p><strong>Why consent token (DEC-1823)?</strong> PDPL Law 91/2025 Art. 23 explicitly requires explicit consent for sensitive PII collection; token is consent proof.</p>\n<hr>\n<h2>§3 — API contract (see §1.7)</h2>\n<p>Sample consent recording (precondition to upload):</p>\n<pre><code class=\"language-json\">POST /v1/hr/members/{id}/cccd-consent\n{ \"consent_purpose\": \"ID verification for employment\", \"consent_text_hash\": \"sha256...\" }</code></pre>\n<p>Returns <code>consent_token</code> UUID for use in upload.</p>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>CCCD encrypted with separate KMS keyspace</strong>. 2. <strong>ROOT-CHRO-only decrypt</strong>. 3. <strong>Non-CHRO decrypt → 403 + access_denied sev-1 audit</strong>. 4. <strong>Sev-1 audit on every successful decrypt</strong>. 5. <strong>CHRO email notification on decrypt</strong>. 6. <strong>Consent token required for upload</strong>. 7. <strong>access_kind enum cardinality 4</strong>. 8. <strong>Per-tenant key alias</strong>. 9. <strong>Key rotation re-encrypts</strong>. 10. <strong>5 memory audit kinds emitted</strong>. 11. <strong>PII: encrypted_photo never in chain</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>IP address SHA256 hashed</strong>. 14. <strong>Trace_id preserved</strong>. 15. <strong>Access log immutable (no UPDATE/DELETE grant)</strong>. 16. <strong>One CCCD per member (UNIQUE)</strong>. 17. <strong>Delete via DSAR or CHRO action</strong>. 18. <strong>Member-self can upload + delete own</strong>. 19. <strong>Failed access logged with succeeded=false</strong>. 20. <strong>Key creation idempotent at tenant provisioning</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn non_chro_decrypt_denied() {\n    let ctx = TestContext::with_uploaded_cccd().await;\n    let r = ctx.try_decrypt_as(ctx.am_user, ctx.member_id).await;\n    assert_eq!(r.status_code, 403);\n    let logs = ctx.fetch_access_log(ctx.member_id).await;\n    assert!(logs.iter().any(|l| l.access_kind == \"decrypt_view\" &amp;&amp; !l.succeeded));\n}\n\n#[tokio::test]\nasync fn chro_decrypt_emits_sev1() {\n    let ctx = TestContext::with_uploaded_cccd_as_chro().await;\n    let r = ctx.decrypt_as(ctx.chro_user, ctx.member_id).await;\n    assert!(r.is_ok());\n    let audits = ctx.fetch_memory_audits(\"hr.cccd_decrypted\").await;\n    assert!(audits.iter().any(|a| a.severity == 1));\n}\n\n#[tokio::test]\nasync fn upload_without_consent_rejected() {\n    let ctx = TestContext::with_member_no_consent().await;\n    let r = ctx.upload_cccd_no_token(ctx.member_id).await;\n    assert_eq!(r.status_code, 412);  // Precondition Failed\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-HR-001. <strong>Cross-module:</strong> FR-AUTH-105 (ROOT-CHRO role), FR-MEMORY-111 (audit chain), FR-AUTH-101 (RBAC).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>KMS decrypt fail</td><td>KMS err</td><td>sev-1; failed audit</td><td>retry; check IAM</td></tr><tr><td>Non-CHRO bypass attempt</td><td>role check</td><td>403; sev-1</td><td>investigate</td></tr><tr><td>Consent token expired</td><td>validate</td><td>412</td><td>re-consent</td></tr><tr><td>Photo size &gt; 5MB</td><td>validate</td><td>400</td><td>resize</td></tr><tr><td>Key rotation mid-decrypt</td><td>re-fetch</td><td>inherent</td><td>inherent</td></tr><tr><td>Multiple consent tokens same member</td><td>use latest valid</td><td>inherent</td><td>inherent</td></tr><tr><td>KMS key disabled</td><td>sev-1 alert</td><td>inherent</td><td>CISO action</td></tr><tr><td>Cross-tenant decrypt attempt</td><td>RLS</td><td>0 rows + sev-1</td><td>inherent</td></tr><tr><td>DSAR delete after upload</td><td>cascade</td><td>inherent</td><td>per FR-PORTAL-008</td></tr><tr><td>Member-self decrypts own</td><td>OK (still sev-1)</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 KMS key created at tenant provisioning via FR-AUTH-105 KMS module.</li><li>§11.2 Photo stored as encrypted bytes in S3 (FR-DOC-001 path) + reference in DB.</li><li>§11.3 CHRO email notification: separate FR-EMAIL-009 send with subject \"CCCD decrypted: {member_name}\".</li><li>§11.4 memory audit body: member_id, accessor_id, access_kind, succeeded; IP SHA256.</li><li>§11.5 Consent token: signed JWT, 24h expiry, single-use; binds member_id + consent_purpose.</li></ul>\n<hr>\n<p><em>End of FR-HR-003 spec.</em></p>";
