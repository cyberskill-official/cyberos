window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLUGIN-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PLUGIN module <strong>MUST</strong> ship a marketplace publish surface at <code>services/plugin-host/src/marketplace/</code>. CLI <code>cyberos-plugin publish &lt;bundle&gt;</code> uploads a packed bundle to <code>plugins.cyberskill.world</code> (OCI-compatible registry); public plugins additionally mirror to <code>agentskills.io</code>. The marketplace server itself (registry API + UI) is scaffolded here and fully implemented in FR-PLUGIN-008a.</p>\n<ol><li><strong>MUST</strong> implement <code>cyberos-plugin publish &lt;bundle&gt;</code> CLI with flags per DEC-2470 + DEC-2471:</li></ol>\n<ul><li><code>--visibility {public,private,enterprise}</code> — default reads from manifest's <code>marketplace.visibility</code> (per FR-PLUGIN-001 schema)</li><li><code>--registry &lt;url&gt;</code> — defaults to <code>https://plugins.cyberskill.world</code></li><li><code>--mirror-agentskills</code> — boolean, default true for public visibility; force-false otherwise</li><li><code>--vetted-badge-token &lt;token&gt;</code> — optional, supplied by CyberSkill after manual review (clause 5)</li></ul>\n<ol><li><strong>MUST</strong> validate bundle integrity before upload per DEC-2475:</li></ol>\n<ul><li>Re-run <code>cyberos-plugin doctor &lt;bundle&gt;</code> and require all 8 INTEROP invariants pass</li><li>Re-validate canonical manifest against <code>manifest.schema.json</code> per FR-PLUGIN-001</li><li>Verify Sigstore Rekor anchor matches the bundle bytes</li><li>Verify bundle's target adapter output matches the per-target reproducibility check</li></ul>\n<ol><li><strong>MUST</strong> push to OCI-compatible registry at <code>plugins.cyberskill.world/v2/&lt;id&gt;/blobs</code> per OCI Distribution Spec v1.1:</li></ol>\n<ul><li>Bundle bytes pushed as <code>application/vnd.cyberskill.plugin.v1+zip</code> (or <code>+folder</code> for codex-cli)</li><li>Manifest pushed as <code>application/vnd.cyberskill.plugin.manifest.v1+json</code></li><li>Tag = <code>&lt;version&gt;</code> (e.g. <code>1.0.0</code>)</li><li>Authentication via OAuth-PKCE bearer token (per FR-PLUGIN-005)</li></ul>\n<ol><li><strong>MUST</strong> mirror public-visibility plugins to <code>agentskills.io</code> per DEC-2472:</li></ol>\n<ul><li>Only <code>marketplace.visibility == \"public\"</code> qualifies</li><li>Mirror uploads the canonical manifest + skills/ folder (NOT the binary, NOT the OAuth-PKCE-bound runtime fields)</li><li>agentskills.io receives an \"anthropic-skills\" subset of the manifest</li><li>Failure to mirror is a soft failure (publish to plugins.cyberskill.world succeeds, mirror retried via FR-PLUGIN-006 audit_outbox pattern)</li></ul>\n<ol><li><strong>MUST</strong> support the vetted-by-CyberSkill badge per DEC-2474:</li></ol>\n<ul><li>Badge token issued via out-of-band manual review process (initially) at <code>https://plugins.cyberskill.world/admin/vet</code></li><li>Token is JWT signed by CyberSkill with <code>aud: \"plugin:&lt;id&gt;:&lt;version&gt;\"</code></li><li>Publish embeds token in marketplace metadata; registry verifies signature; badge persists across reads</li><li>Token-less publish still succeeds; just no badge</li></ul>\n<ol><li><strong>MUST</strong> support three visibility modes per DEC-2471:</li></ol>\n<ul><li><code>public</code> — discoverable by any user; appears in marketplace search; mirrored to agentskills.io</li><li><code>private</code> — discoverable only within the publishing tenant; mirror disabled; install requires tenant membership</li><li><code>enterprise</code> — like private, but the registry is a separate tenant-isolated origin (Strategy Level 5 white-label); URL pattern <code>plugins.&lt;enterprise-domain&gt;.cyberskill.world</code></li></ul>\n<ol><li><strong>MUST</strong> validate revenue-share rules per DEC-2473:</li></ol>\n<ul><li>Free plugins (<code>marketplace.price_usd_per_month</code> absent or 0): no revenue share, badge irrelevant</li><li>Paid plugins (<code>marketplace.price_usd_per_month &gt; 0</code>): 70/30 author/CyberSkill split enforced server-side at billing time</li><li><code>marketplace.revenue_share_percent</code> defaults to 70 per manifest schema; values &lt; 70 trigger publish-time warning (author getting less than fair share)</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit row <code>plugin.published</code> per DEC-2476 with body containing:</li></ol>\n<ul><li><code>plugin_id</code>, <code>version</code>, <code>sha256</code> of bundle, <code>signature.rekor_uuid</code>, <code>visibility</code>, <code>vetted_badge: boolean</code>, <code>mirror_targets: [...]</code>, <code>trace_id</code> Audit emission follows FR-PLUGIN-006 retry semantics.</li></ul>\n<ol><li><strong>MUST</strong> enforce version monotonicity at registry — uploading version <code>1.0.0</code> after <code>1.0.1</code> MUST fail. SemVer 2.0 semantic ordering per FR-PLUGIN-001 clause 4.</li></ol>\n<ol><li><strong>MUST</strong> support <code>cyberos-plugin yank &lt;id&gt;@&lt;version&gt;</code> for emergency removal — yanked versions are hidden from search but remain installable by users with the SHA-256 (so existing installs don't break). Yank emits memory audit <code>plugin.yanked</code>.</li></ol>\n<ol><li><strong>MUST NOT</strong> publish a bundle that fails any of the 8 INTEROP invariants per DEC-2475 + clause 2.</li></ol>\n<ol><li><strong>MUST NOT</strong> mirror private or enterprise plugins to agentskills.io per DEC-2472 + clause 4. Private bundles MUST stay on CyberSkill's infrastructure.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow a paid plugin to set revenue_share_percent &gt; 100 — schema enforces 0-100 range; publish double-checks.</li></ol>\n<ol><li><strong>MUST NOT</strong> publish without a verified Sigstore signature anchor per clause 2.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why OCI-compatible registry (DEC-2470, clause 3)?</strong> OCI registries are the de-facto standard for bundle distribution. Docker Hub, GitHub Container Registry, AWS ECR all speak OCI. Using OCI gives us free integration with existing tooling (cosign signing, opens up multi-cloud mirroring). Marketplace UI sits on top of the OCI API.</p>\n<p><strong>Why three visibility modes (DEC-2471, clause 6)?</strong> Strategy Level 3 needs public (anyone installs); Strategy Level 1 OSS amplifies via public mirror; Strategy Level 4 (vertical packs) is mostly public but may be tenant-specific in early stages → private; Strategy Level 5 (enterprise white-label) is enterprise. Three covers all four levels cleanly.</p>\n<p><strong>Why mirror to agentskills.io (DEC-2472, clause 4)?</strong> Strategy §2 lists agentskills.io as the open Anthropic registry; CyberSkill is a citizen. Public plugins benefit from being discoverable via Anthropic's ecosystem search, not just CyberSkill's. Mirror gives reach without ceding ownership (registry of record remains plugins.cyberskill.world).</p>\n<p><strong>Why 70/30 split (DEC-2473, clause 7)?</strong> Industry standard (Apple App Store, Shopify post-Shopify Capital, Salesforce AppExchange). 70% is the \"fair share\" headline; deviation triggers a warning because plugin authors deserve protection from accidental low splits.</p>\n<p><strong>Why JWT-signed vetted badge (DEC-2474, clause 5)?</strong> Badge claim must be unforgeable. JWT signed by CyberSkill's marketplace key is verifiable client-side (<code>cyberos-plugin doctor</code> rechecks). Manual review at first; automated for security-only plugins later.</p>\n<p><strong>Why publish-time re-validation (DEC-2475, clause 2)?</strong> Local validation can be tampered with (modified <code>cyberos-plugin</code> binary). Server-side re-validation is the trust anchor. Same reason GitHub re-verifies signed commits.</p>\n<p><strong>Why version monotonicity (clause 9)?</strong> Out-of-order publish (1.0.1 then 1.0.0) confuses dependency resolvers. Either version is \"latest\"? Monotonic publish-by-version means <code>latest</code> tag always points at the highest version.</p>\n<p><strong>Why yank-not-delete (clause 10)?</strong> Permanent deletion breaks existing installs that have the SHA-256. Yank hides from discovery but preserves install ability. Same model as crates.io, npm.</p>\n<p><strong>Why audit publish (DEC-2476, clause 8)?</strong> Strategy §2 demands every action be audit-chained. Publish is a high-stakes action (introduces code into users' environments). Audit row is forensic record + DSAR-exportable.</p>\n<p><strong>Why no mirror for private/enterprise (clause 12)?</strong> Private plugins may contain tenant-confidential content (vertical packs for specific clients). Mirroring to a public registry leaks. Hard separation.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>CLI surface</h3>\n<pre><code class=\"language-text\">cyberos-plugin publish &lt;bundle&gt; [--visibility VIS] [--registry URL] [--mirror-agentskills] [--vetted-badge-token TOKEN]\ncyberos-plugin yank &lt;id&gt;@&lt;version&gt; [--reason TEXT]\ncyberos-plugin list [--visibility VIS] [--vetted]\ncyberos-plugin info &lt;id&gt;@&lt;version&gt;</code></pre>\n<h3>Plugin registry Postgres schema (scaffold for FR-PLUGIN-008a server)</h3>\n<pre><code class=\"language-sql\">-- services/plugin-marketplace/migrations/0001_plugin_registry.sql\nCREATE TABLE plugin_marketplace.plugins (\n  plugin_id TEXT NOT NULL,\n  version TEXT NOT NULL,\n  owner_tenant_id UUID NOT NULL,\n  visibility TEXT NOT NULL CHECK (visibility IN ('public','private','enterprise')),\n  bundle_sha256 BYTEA NOT NULL,\n  bundle_size_bytes BIGINT NOT NULL,\n  rekor_uuid TEXT NOT NULL,\n  vetted_badge_token TEXT,\n  vetted_at TIMESTAMPTZ,\n  price_usd_per_month NUMERIC(10,2),\n  revenue_share_percent SMALLINT NOT NULL DEFAULT 70,\n  yanked_at TIMESTAMPTZ,\n  yanked_reason TEXT,\n  published_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  published_by_subject UUID NOT NULL,\n  trace_id CHAR(32),\n  PRIMARY KEY (plugin_id, version)\n);\n\nCREATE INDEX ON plugin_marketplace.plugins (visibility, published_at DESC)\n  WHERE yanked_at IS NULL;\nCREATE INDEX ON plugin_marketplace.plugins (owner_tenant_id, visibility);\nCREATE INDEX ON plugin_marketplace.plugins (vetted_at) WHERE vetted_at IS NOT NULL;</code></pre>\n<h3>Publish flow</h3>\n<pre><code class=\"language-rust\">// services/plugin-host/src/marketplace/publish.rs\npub async fn publish(\n    bundle_path: &amp;Path,\n    visibility: Visibility,\n    registry: &amp;Url,\n    mirror_agentskills: bool,\n    vetted_badge_token: Option&lt;&amp;str&gt;,\n) -&gt; Result&lt;PublishResult&gt; {\n    // Step 1: validate bundle (re-run doctor + manifest schema)\n    let invariants = doctor(bundle_path)?;\n    if !invariants.all_pass() {\n        return Err(PublishError::InvariantViolation(invariants.failures));\n    }\n\n    // Step 2: parse manifest\n    let manifest = read_canonical_manifest_from_bundle(bundle_path)?;\n\n    // Step 3: verify Sigstore anchor matches bundle bytes\n    sigstore::verify(bundle_path, &amp;manifest.signature.rekor_uuid).await?;\n\n    // Step 4: push to plugins.cyberskill.world OCI\n    let push_result = oci_push(bundle_path, &amp;manifest, registry).await?;\n\n    // Step 5: mirror to agentskills.io if public\n    let mirror_status = if visibility == Visibility::Public &amp;&amp; mirror_agentskills {\n        mirror_agentskills::push(&amp;manifest, bundle_path).await.into()\n    } else {\n        MirrorStatus::Skipped\n    };\n\n    // Step 6: emit memory audit\n    audit::emit_published(\n        &amp;manifest, &amp;push_result.sha256, visibility, mirror_status.clone(),\n    ).await?;\n\n    Ok(PublishResult {\n        plugin_id: manifest.id,\n        version: manifest.version,\n        sha256: push_result.sha256,\n        rekor_uuid: manifest.signature.rekor_uuid,\n        mirror_status,\n        vetted_badge: vetted_badge_token.is_some(),\n    })\n}</code></pre>\n<h3>Vetted-badge token format</h3>\n<pre><code class=\"language-text\">JWT (RS256) signed by CyberSkill marketplace key:\n  iss = \"https://plugins.cyberskill.world\"\n  aud = \"plugin:&lt;id&gt;:&lt;version&gt;\"\n  sub = &lt;reviewer_subject_id&gt;\n  iat, exp (typically 90-day validity)\n  body:\n    review_id = \"REV-2026-...\",\n    review_date = \"...\",\n    findings_summary = \"...\"</code></pre>\n<h3>Mirror request to agentskills.io</h3>\n<pre><code class=\"language-http\">POST https://agentskills.io/v1/skills/publish HTTP/1.1\nContent-Type: application/json\nAuthorization: Bearer &lt;agentskills.io API key for CyberSkill org&gt;\n\n{\n  \"publisher\": \"cyberskill\",\n  \"skill_id\": \"cyberos\",\n  \"version\": \"1.0.0\",\n  \"skill_md_url\": \"https://plugins.cyberskill.world/cyberos/1.0.0/SKILL.md\",\n  \"license\": \"Apache-2.0\",\n  \"description\": \"...\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong><code>publish</code> rejects bundle failing doctor invariants</strong> — mock a bundle with broken signature; publish exits 1.</li><li><strong><code>publish</code> rejects unsigned bundle</strong> — bundle missing Sigstore anchor; publish exits 1.</li><li><strong><code>publish</code> pushes to OCI registry</strong> — mock registry receives PUT to <code>/v2/cyberos/blobs/uploads/</code>.</li><li><strong><code>publish</code> for public mirrors to agentskills.io</strong> — mock agentskills.io receives mirror call.</li><li><strong><code>publish</code> for private does NOT mirror</strong> — mock agentskills.io receives 0 calls.</li><li><strong><code>publish</code> emits plugin.published audit</strong> — memory row exists with kind='plugin.published'.</li><li><strong>plugin.published body has version + sha256 + rekor_uuid + visibility</strong> — body field check.</li><li><strong>Out-of-order version rejected</strong> — publish 1.0.1; publish 1.0.0 next; second fails.</li><li><strong>Yank hides from default search</strong> — publish; yank; <code>cyberos-plugin list</code> does not show yanked.</li><li><strong>Yanked plugin still installable by SHA-256</strong> — install by hash succeeds.</li><li><strong>Vetted badge persists on info</strong> — <code>cyberos-plugin info &lt;id&gt;@&lt;version&gt;</code> shows vetted: true when token verified.</li><li><strong>Bad vetted badge token rejected</strong> — JWT signed by wrong key; publish stores but <code>info</code> shows vetted: false with reason.</li><li><strong>Private plugin cross-tenant install denied</strong> — tenant B trying to install tenant A's private plugin fails.</li><li><strong>Enterprise plugin appears only at enterprise origin</strong> — <code>plugins.cyberskill.world</code> does NOT list it; <code>plugins.acme.cyberskill.world</code> does.</li><li><strong>revenue_share_percent &lt; 70 triggers warning</strong> — publish-time stderr warning; publish still succeeds.</li><li><strong>revenue_share_percent &gt; 100 rejected</strong> — schema + publish double-check fails.</li><li><strong>Mirror failure is soft</strong> — agentskills.io 500s; publish still succeeds; mirror row queued in retry table.</li><li><strong>Publish audit row scrubbed</strong> — body MUST NOT contain bundle bytes (only sha256).</li><li><strong><code>info</code> returns visibility, vetted status, version list</strong> — RPC works.</li><li><strong><code>list --vetted</code> filters to vetted-only</strong> — query semantics.</li><li><strong>OCI blob has correct media type</strong> — <code>application/vnd.cyberskill.plugin.v1+zip</code>.</li><li><strong>Publish requires OAuth-PKCE bearer token</strong> — anonymous publish fails 401.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/publish_invariant_check_test.rs\n#[tokio::test]\nasync fn publish_rejects_unsigned_bundle() {\n    let bundle = pack_bundle_without_signature().await;\n    let result = publish(&amp;bundle, Visibility::Public, &amp;mock_registry_url(), false, None).await;\n    assert!(matches!(result, Err(PublishError::InvariantViolation(_))));\n}\n\n#[tokio::test]\nasync fn publish_rejects_broken_doctor() {\n    let bundle = pack_bundle_with_wrong_tool_naming().await;\n    let result = publish(&amp;bundle, Visibility::Public, &amp;mock_registry_url(), false, None).await;\n    assert!(matches!(result, Err(PublishError::InvariantViolation(_))));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/publish_mirrors_to_agentskills_test.rs\n#[tokio::test]\nasync fn public_publish_mirrors() {\n    let agentskills_mock = MockAgentSkillsServer::start().await;\n    let bundle = pack_valid_bundle().await;\n    publish(&amp;bundle, Visibility::Public, &amp;mock_registry_url(), true,\n            Some(\"https://localhost/agentskills\")).await.unwrap();\n    let calls = agentskills_mock.recorded_calls();\n    assert_eq!(calls.len(), 1);\n    assert_eq!(calls[0].path, \"/v1/skills/publish\");\n}\n\n#[tokio::test]\nasync fn private_publish_does_not_mirror() {\n    let agentskills_mock = MockAgentSkillsServer::start().await;\n    let bundle = pack_valid_bundle().await;\n    publish(&amp;bundle, Visibility::Private, &amp;mock_registry_url(), true, None).await.unwrap();\n    assert_eq!(agentskills_mock.recorded_calls().len(), 0);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/publish_emits_audit_test.rs\n#[tokio::test]\nasync fn publish_emits_audit_row() {\n    let ctx = TestContext::with_memory_mock().await;\n    let bundle = pack_valid_bundle().await;\n    publish(&amp;bundle, Visibility::Public, &amp;ctx.registry, false, None).await.unwrap();\n    let rows = ctx.memory.fetch_rows(kind=\"plugin.published\").await;\n    assert_eq!(rows.len(), 1);\n    let body = &amp;rows[0][\"body\"];\n    assert_eq!(body[\"plugin_id\"], \"cyberos\");\n    assert_eq!(body[\"version\"], \"1.0.0\");\n    assert_eq!(body[\"visibility\"], \"public\");\n    assert!(body[\"sha256\"].as_str().unwrap().len() == 64);\n    assert!(body[\"rekor_uuid\"].is_string());\n    // body MUST NOT contain bundle bytes\n    let body_str = serde_json::to_string(body).unwrap();\n    assert!(!body_str.contains(\"PK\\x03\\x04\")); // zip magic\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/private_visibility_scoped_to_tenant_test.rs\n#[tokio::test]\nasync fn cross_tenant_private_install_denied() {\n    let ctx_a = TestContext::for_tenant(\"a\").await;\n    let bundle = ctx_a.pack_and_publish(Visibility::Private).await;\n\n    let ctx_b = TestContext::for_tenant(\"b\").await;\n    let result = ctx_b.install_by_id(&amp;bundle.plugin_id, &amp;bundle.version).await;\n    assert!(matches!(result, Err(InstallError::NotFound)));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract + Postgres schema in §3 are the skeleton. Full marketplace server implementation deferred to FR-PLUGIN-008a, which fleshes out search, browse UI, billing integration with FR-TEN-005, and admin review tooling.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> FR-PLUGIN-006 (audit emission for plugin.published / plugin.yanked); FR-PLUGIN-007 (per-target adapters produce the bundles being published).</li><li><strong>Downstream:</strong> FR-TEN-005 (vertical pack pricing — paid plugins billed via TEN service); FR-PORTAL-005 (branded Genie chat — enterprise marketplaces appear in branded portal); FR-PLUGIN-008a (full marketplace server: search, UI, billing).</li><li><strong>Cross-module:</strong> Strategy §4 Levels 1, 3, 4, 5 — every distribution-facing milestone in the strategy depends on this surface shipping at least at the publish-CLI level.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Publish result</h3>\n<pre><code class=\"language-json\">{\n  \"plugin_id\": \"cyberos\",\n  \"version\": \"1.0.0\",\n  \"sha256\": \"a1b2c3d4...\",\n  \"rekor_uuid\": \"24296fb24b8ad77a...\",\n  \"mirror_status\": \"mirrored\",\n  \"vetted_badge\": true,\n  \"registry_url\": \"https://plugins.cyberskill.world/cyberos/1.0.0\"\n}</code></pre>\n<h3><code>plugin.published</code> audit body</h3>\n<pre><code class=\"language-json\">{\n  \"plugin_id\": \"cyberos\",\n  \"version\": \"1.0.0\",\n  \"sha256\": \"a1b2c3d4...\",\n  \"rekor_uuid\": \"24296fb...\",\n  \"visibility\": \"public\",\n  \"vetted_badge\": true,\n  \"mirror_targets\": [\"agentskills.io\"],\n  \"size_bytes\": 184320,\n  \"trace_id\": \"01HX...\"\n}</code></pre>\n<h3>Vetted badge JWT (decoded)</h3>\n<pre><code class=\"language-json\">{\n  \"iss\": \"https://plugins.cyberskill.world\",\n  \"aud\": \"plugin:cyberos:1.0.0\",\n  \"sub\": \"reviewer-uuid-...\",\n  \"iat\": 1748000000,\n  \"exp\": 1755800000,\n  \"review_id\": \"REV-2026-042\",\n  \"review_date\": \"2026-05-19\",\n  \"findings_summary\": \"All FR-PLUGIN-001..007 invariants pass; manual review passed.\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<ul><li>~~Should the marketplace server ship in this FR?~~ → No. CLI publish + registry scaffolding in this FR; full server (search, UI, billing) in FR-PLUGIN-008a.</li><li>~~Should agentskills.io mirror be optional (off-by-default for public)?~~ → No, on by default per clause 1. Strategy §4 Level 1 OSS-distribution depends on agentskills.io presence.</li><li>~~Should revenue_share allow &gt; 70 for author (more generous)?~~ → Yes (schema 0-100); only &lt; 70 triggers warning. Author getting more than 70 is fine.</li><li>~~Should we support arbitrary review chains (multiple vetters)?~~ → Deferred to FR-PLUGIN-008b; v1 is single CyberSkill reviewer.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Bundle doctor invariants fail</td><td>publish.rs step 1</td><td>exit 1 with details</td><td>Re-pack bundle correctly</td></tr><tr><td>Sigstore anchor mismatch</td><td>sigstore::verify</td><td>exit 1</td><td>Re-sign bundle</td></tr><tr><td>Registry unreachable</td><td>OCI client</td><td>exit 1</td><td>Retry with backoff (manual)</td></tr><tr><td>agentskills.io unreachable</td><td>mirror call</td><td>soft fail; outbox queue</td><td>Mirror retries via FR-PLUGIN-006 outbox</td></tr><tr><td>Out-of-order version</td><td>registry monotonicity check</td><td>exit 1</td><td>Use higher version number</td></tr><tr><td>Yank without permission</td><td>registry RBAC</td><td>exit 1</td><td>Owner-or-admin only</td></tr><tr><td>Vetted badge token wrong sig</td><td>badge_verify</td><td>publish proceeds; badge: false; warning</td><td>Get badge re-issued</td></tr><tr><td>Vetted badge token expired</td><td>badge_verify exp check</td><td>badge: false</td><td>Renew via review process</td></tr><tr><td>Private plugin published to public origin</td><td>visibility check</td><td>exit 1</td><td>Use visibility=private + correct registry</td></tr><tr><td>Enterprise plugin to wrong enterprise origin</td><td>origin check</td><td>exit 1</td><td>Use the correct enterprise registry URL</td></tr><tr><td>Bundle byte change after sign</td><td>sigstore re-verify</td><td>exit 1</td><td>Re-sign</td></tr><tr><td>Cross-tenant private install</td><td>registry RLS</td><td>404</td><td>inherent</td></tr><tr><td>revenue_share &gt; 100</td><td>schema + double-check</td><td>exit 1</td><td>Author fixes</td></tr><tr><td>Audit emission fails</td><td>FR-PLUGIN-006 outbox path</td><td>publish succeeds; audit queued</td><td>Inherent retry</td></tr><tr><td>Publisher token lacks scope</td><td>OAuth scope check</td><td>401</td><td>Re-authenticate with publish scope</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 <strong>Why scaffold the marketplace server here.</strong> Full server is FR-PLUGIN-008a (registry API, search UI, browse, install metrics, billing). This FR ships the CLI side + Postgres schema so the publish path is operational. Server implementation can land independently because schema is locked.</li></ul>\n<ul><li>§11.2 <strong>OCI push library.</strong> Rust <code>oci-distribution</code> crate (4.x) handles the OCI v1.1 spec including blobs, manifests, tags. Adapter wraps it for the CyberSkill-specific media types.</li></ul>\n<ul><li>§11.3 <strong>agentskills.io API key.</strong> CyberSkill-org-level API key stored in AWS Secrets Manager at <code>plugins.cyberskill.world/agentskills_api_key</code>. Fetched once at process start; rotated quarterly.</li></ul>\n<ul><li>§11.4 <strong>Vetted badge token issuance flow.</strong> Out of scope of this FR; manual at <code>https://plugins.cyberskill.world/admin/vet</code> for v1. Future automation: scan bundle for known-bad patterns (FR-PLUGIN-008b).</li></ul>\n<ul><li>§11.5 <strong>Enterprise origin pattern.</strong> <code>plugins.&lt;enterprise-name&gt;.cyberskill.world</code> — DNS managed by CyberSkill; routed to the same OCI backend with tenant_id filter. Enterprise tenant_id derived from origin.</li></ul>\n<ul><li>§11.6 <strong>Mirror retry semantics.</strong> Mirror failures populate the FR-PLUGIN-006 outbox with kind=<code>plugin.mirror_retry</code> (special variant). Retry worker checks every 5 minutes. Mirror is best-effort, not blocking.</li></ul>\n<ul><li>§11.7 <strong>Why <code>yank</code> instead of <code>delete</code>.</strong> Yank model from crates.io. Permanent deletion breaks reproducibility of historical installs. Yank hides from discovery but preserves bytes. The bundle bytes can be force-deleted by admin (DSAR or court order); that emits a separate <code>plugin.deleted</code> audit kind.</li></ul>\n<ul><li>§11.8 <strong>Revenue billing integration.</strong> FR-TEN-005 receives memory audit <code>plugin.invoked</code> rows from paid plugins; bills the user tenant; pays the author tenant per <code>revenue_share_percent</code>. This FR ships the data; TEN does the money.</li></ul>\n<ul><li>§11.9 <strong>Why CyberSkill 30% (not lower).</strong> Below 20% the marketplace cannot fund: hosting, security review, fraud detection, OAuth issuance, audit chain storage. Above 30% authors balk. 30% is the equilibrium.</li></ul>\n<ul><li>§11.10 <strong>CLI ergonomics.</strong> <code>cyberos-plugin publish dist/cyberos-1.0.0.plugin --visibility public</code> is the canonical command. Defaults are aggressive (mirror on, OAuth-PKCE handshake automatic) so authors don't need to manage flags.</li></ul>\n<hr>\n<p><em>End of FR-PLUGIN-008 spec.</em></p>";
