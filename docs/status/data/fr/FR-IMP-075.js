window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-IMP-075"]="<h2>§1 — Description</h2>\n<ol><li><code>apps/desktop/src-tauri/Cargo.toml</code> <strong>MUST</strong> declare a <code>mas</code> feature (marker, no deps). Default builds (no feature) <strong>MUST</strong> be byte-for-byte behavior-identical to today: updater registered, launch check runs.</li><li>All three updater code sites in <code>lib.rs</code> (<code>spawn_update_check</code> definition, the <code>tauri_plugin_updater</code> registration block, the setup-time launch call) <strong>MUST</strong> change <code>#[cfg(desktop)]</code> to <code>#[cfg(all(desktop, not(feature = \"mas\")))]</code> — with <code>--features mas</code> the plugin is never registered and no update check is compiled in, so the configured <code>plugins.updater</code> block in tauri.conf.json becomes inert for the MAS target without touching that shared config (FR-APP-003 §1 #1 forbids mutating it).</li><li><code>release-mas.yml</code>'s build step <strong>MUST</strong> pass the feature (<code>--features mas</code>). If the Tauri CLI's cargo-feature passthrough flag differs on the pinned CLI major, the job fails loudly at build - an acceptable first-gated-run discovery, noted in-file (no macOS/cargo toolchain exists in this authoring environment to pre-verify).</li><li><code>docs/deploy/mac-app-store-submission.md</code> hard blocker #1 <strong>MUST</strong> flip to resolved-pending-first-build, with one honest residual noted: the target-scoped dependency still compiles into the binary as dead code (registration excluded); shrinking it to an optional dependency is possible later on a real toolchain but is not submission-blocking the way active self-update was.</li></ol>\n<p><em>Length note: sanctioned lean profile — a two-attribute Rust change plus one CI flag; §5's checks are the complete machine surface available pre-toolchain.</em></p>\n<h2>§2 — Why this design</h2>\n<p>Compile-time exclusion is the only path FR-APP-003's answer sheet accepts: runtime checks and config-overlay null-outs both leave the self-update code reachable and were explicitly rejected there as unverifiable submission-safety. A marker feature + <code>not(feature)</code> widening is the minimal diff that keeps the default build provably unchanged (the attribute is strictly narrower only when the feature is on).</p>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-toml\">[features]\ncustom-protocol = [\"tauri/custom-protocol\"]\nmas = []   # Mac App Store target: compiles the self-updater OUT (FR-IMP-075)</code></pre>\n<pre><code class=\"language-rust\">#[cfg(all(desktop, not(feature = \"mas\")))]   // x3, replacing #[cfg(desktop)]</code></pre>\n<pre><code class=\"language-yaml\">run: npx --yes @tauri-apps/cli@2 build --config src-tauri/tauri.mas.conf.json --bundles app --target universal-apple-darwin --features mas</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>Default-build neutrality: <code>grep -c '#\\[cfg(all(desktop, not(feature = \"mas\")))\\]' src/lib.rs</code> == 3 and <code>grep -c '#\\[cfg(desktop)\\]' src/lib.rs</code> == 0 — no site left half-gated.</li><li><code>mas = []</code> present in Cargo.toml <code>[features]</code>.</li><li>release-mas.yml build step carries <code>--features mas</code>; YAML parses.</li><li>Answer sheet blocker #1 updated; residual dead-code note present.</li><li>First real toolchain run (<code>cargo check</code> then <code>cargo check --features mas</code>, later the gated CI build) compiles both ways — expected-pending here, executed on Stephen's machine or the first MAS_RELEASE run.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\">grep -c 'not(feature = \"mas\")' apps/desktop/src-tauri/src/lib.rs        # 3\ngrep -c '#\\[cfg(desktop)\\]' apps/desktop/src-tauri/src/lib.rs           # 0\ngrep -n '^mas = \\[\\]' apps/desktop/src-tauri/Cargo.toml\ngrep -n 'features mas' .github/workflows/release-mas.yml\npython3 -c \"import yaml; yaml.safe_load(open('.github/workflows/release-mas.yml'))\"\n# pending real toolchain: cargo check &amp;&amp; cargo check --features mas</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>§3 is exhaustive.</p>\n<h2>§7 — Dependencies</h2>\n<p>Upstream: FR-APP-003 (done) discovered and scoped this. Downstream: unblocks <code>MAS_RELEASE=true</code> (remaining blockers are Stephen's account-side items). Batched with FR-IMP-074 (cone-independent).</p>\n<h2>§8 — Example payloads</h2>\n<p>Post-change gate output: <code>mas-entitlement-lint: OK</code> unchanged; MAS build log shows no <code>cyberos updater:</code> lines.</p>\n<h2>§9 — Open questions</h2>\n<ul><li>Optional-dependency shrink (dead code removal) — later, on a real toolchain, if App Review flags the compiled-but-unregistered plugin (unlikely; no update behavior exists to observe).</li><li>Tauri CLI <code>--features</code> passthrough name on the pinned major — confirmed at first gated run (clause 3 loud-failure posture).</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>cfg typo breaks the DEFAULT build</td><td>next desktop build/tag fails compile loudly</td><td>no silent ship</td><td>trivial attribute fix</td></tr><tr><td>CLI drops/renames --features</td><td>MAS job fails at build step</td><td>loud, pre-submission</td><td>adjust flag per CLI --help</td></tr><tr><td>feature on but a 4th updater site appears later</td><td>grep AC #1 in this spec + review norm</td><td>gate catches count drift</td><td>widen the new site</td></tr><tr><td>Apple flags dead plugin code</td><td>review feedback (external)</td><td>resubmit cycle</td><td>optional-dep follow-up (§9)</td></tr><tr><td>default build accidentally passes --features mas</td><td>only possible in release-mas.yml, which never builds the Developer ID channel</td><td>none</td><td>structural separation of workflows</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<p>The three-site count (not two, not four) comes from reading lib.rs, not convention — AC #1's grep pins it. Batched with FR-IMP-074 under v2.5.0 §11a.</p>\n<p><em>End of FR-IMP-075.</em></p>";
