window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PORTAL-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship client-initiated workflows at <code>services/portal/src/workflows/</code> with 5 closed-enum workflow kinds, auto-routing to assignees via per-tenant rules, CHAT-thread bridging, SLA monitoring, auto-prioritisation on security keywords, file attachments via FR-DOC-001, internal/external message separation, and 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>client_workflow_kind</code> enum: <code>('new_project_request','billing_inquiry','support_ticket','feature_request','general_question')</code> per DEC-1240. Cardinality test asserts 5.</li></ol>\n<ol><li><strong>MUST</strong> define closed <code>workflow_status</code> enum: <code>('submitted','acknowledged','in_progress','awaiting_client','resolved','closed','escalated')</code> per DEC-1243. Cardinality test asserts 7.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_workflow_submissions</code> table at migration <code>0018</code>: <code>(workflow_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, submitter_subject_id UUID NOT NULL, workflow_kind client_workflow_kind NOT NULL, status workflow_status NOT NULL DEFAULT 'submitted', title TEXT NOT NULL, body TEXT NOT NULL, attachments JSONB NOT NULL DEFAULT '[]'::jsonb, assignee_subject_id UUID, chat_thread_id UUID, sla_acknowledged_by TIMESTAMPTZ, sla_resolved_by TIMESTAMPTZ, acknowledged_at TIMESTAMPTZ, resolved_at TIMESTAMPTZ, escalated_at TIMESTAMPTZ, escalation_reason TEXT, submitted_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), trace_id CHAR(32))</code>. RLS scoped to <code>tenant_id AND (submitter_subject_id = auth.subject_id OR assignee_subject_id = auth.subject_id OR has_role('engagement_admin'))</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_workflow_routing_rules</code> at migration <code>0019</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, workflow_kind client_workflow_kind NOT NULL, engagement_id UUID, assignee_subject_id UUID NOT NULL, priority INT NOT NULL DEFAULT 100, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), created_by_subject_id UUID NOT NULL)</code>. Per-tenant rules; lower priority number wins; engagement_id NULL = tenant-wide rule.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/portal/workflows/submit</code> body <code>{ engagement_id, workflow_kind, title, body, attachment_s3_keys?: [...] }</code>. Handler:</li></ol>\n<ul><li>Validates JWT + Engagement membership.</li><li>Rate-limit per DEC-1249.</li><li>Validates workflow_kind in closed enum.</li><li>INSERTs workflow_submissions row with status='submitted'.</li><li>Auto-priority check per DEC-1247 — keyword scan body → if match, set status='escalated' + sev-1 alert.</li><li>Invokes router per §1 #6.</li><li>Creates CHAT thread per §1 #7.</li><li>Computes SLA timestamps from defaults + overrides.</li><li>Emit <code>portal.workflow_submitted</code> sev-2.</li><li>Returns 201 + <code>{ workflow_id, chat_thread_id, status, sla_acknowledged_by, sla_resolved_by }</code>.</li></ul>\n<ol><li><strong>MUST</strong> auto-route per DEC-1242. The <code>router.rs::route(tenant_id, workflow_kind, engagement_id)</code>:</li></ol>\n<ul><li>Lookup matching rules ordered by <code>(engagement_id NOT NULL DESC, priority ASC)</code>.</li><li>First match's <code>assignee_subject_id</code> wins.</li><li>No rule matches → fallback to engagement_admin role-holder.</li><li>UPDATE workflow row with <code>assignee_subject_id</code>.</li><li>Emit <code>portal.workflow_routed</code>.</li></ul>\n<ol><li><strong>MUST</strong> create CHAT thread per DEC-1241 via <code>chat_bridge.rs::create_thread(workflow_id)</code>:</li></ol>\n<ul><li>Calls FR-CHAT-005 create-thread API in the Engagement-scoped channel.</li><li>Channel selection per workflow_kind: new_project_request→#new-projects; billing_inquiry→#billing; etc.</li><li>Thread initial message = workflow body + attachments rendered.</li><li>Records <code>chat_thread_id</code> on workflow row.</li><li>Adds <code>client_workflow_id</code> reference on the thread.</li></ul>\n<ol><li><strong>MUST</strong> separate internal vs external messages per DEC-1245. CHAT messages in the thread:</li></ol>\n<ul><li>Default visibility = internal (engagement team only).</li><li>Messages flagged <code>client_visible: true</code> are mirrored back to portal UI for the submitter.</li><li>Portal client reply via <code>POST /v1/portal/workflows/{id}/reply</code> creates <code>client_reply</code> typed CHAT message (auto client_visible=true).</li><li>Emit <code>portal.workflow_client_reply</code>.</li></ul>\n<ol><li><strong>MUST</strong> monitor SLA per DEC-1244 via daily job:</li></ol>\n<ul><li>For each non-terminal workflow: check <code>sla_acknowledged_by &lt; now()</code> AND <code>acknowledged_at IS NULL</code> → emit <code>portal.workflow_sla_breach</code> sev-1 with breach_type='acknowledgement'.</li><li>Check <code>sla_resolved_by &lt; now()</code> AND <code>resolved_at IS NULL</code> → emit breach with type='resolution'.</li><li>Mark workflow <code>escalated_at = now()</code>.</li></ul>\n<ol><li><strong>MUST</strong> support file attachments per DEC-1246. Handler:</li></ol>\n<ul><li>Returns presigned S3 URLs from FR-DOC-001 for each attachment slot at submission-start.</li><li>Validates uploaded files: max 25 MiB each, max 5 per workflow, allowed types only (pdf, png, jpg, docx, xlsx).</li><li>Persists S3 keys + SHA-256 in <code>attachments</code> JSONB.</li></ul>\n<ol><li><strong>MUST</strong> auto-prioritise on security keywords per DEC-1247. The <code>auto_priority.rs::check(body, workflow_kind)</code>:</li></ol>\n<ul><li>For <code>support_ticket</code> kind: case-insensitive scan for {down, outage, breach, urgent, security, attack, leak, compromised, data loss}.</li><li>Match → status='escalated', escalation_reason='security_keyword_detected: &lt;keyword&gt;', sev-1 PagerDuty alert to engagement_admin.</li><li>Emit <code>portal.workflow_status_changed</code> with transition to escalated.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/portal/workflows?engagement_id=...&amp;status=...</code> for submitter list view. Returns own workflows + status mirror.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/portal/workflows/{id}</code> for detail — shows status + client-visible CHAT messages + SLA timestamps; never internal messages.</li></ol>\n<ol><li><strong>MUST</strong> support reopen per DEC-1251. <code>POST /v1/portal/workflows/{id}/reopen</code>:</li></ol>\n<ul><li>If <code>now() - closed_at &lt; 30 days</code> → status='awaiting_client' + audit row.</li><li>If beyond → 400 + <code>reopen_window_expired; submit_new</code>.</li></ul>\n<ol><li><strong>MUST</strong> send email on status change per DEC-1252 via FR-EMAIL-001. Per-tenant template overrides apply.</li></ol>\n<ol><li><strong>MUST</strong> emit 6 memory audit kinds per DEC-1248: submitted (sev-2), routed (sev-3), status_changed (sev-2), resolved (sev-2), sla_breach (sev-1), client_reply (sev-3).</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub: title + body SHA256 only in chain; raw in DB.</li></ol>\n<ol><li><strong>MUST</strong> rate-limit per DEC-1249 — 10 submissions per workflow_kind per hour per caller.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id across submit → route → CHAT-create → audit.</li></ol>\n<ol><li><strong>MUST</strong> be RLS-scoped: submitter sees own; assignee sees assigned; engagement_admin sees Engagement-wide.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why CHAT-thread per workflow (§1 #7, DEC-1241)?</strong> Conversation continuity. Workflow has chat history; engagement team chats internally; client sees client-visible portion. Re-implementing chat in PORTAL = waste; reusing CHAT-005 = audit + retention + search free.</p>\n<p><strong>Why auto-prioritise security keywords (§1 #11, DEC-1247)?</strong> Security incidents need response in minutes, not days. Keyword-based detection is crude but high-recall + zero-cost. False positives (e.g. \"the system was down for a minute\") get manual de-escalation; missed-positives (no keyword used) get standard SLA — acceptable.</p>\n<p><strong>Why internal/external message separation (§1 #8, DEC-1245)?</strong> Engagement team needs candid internal discussion (\"client is wrong, but let's gently educate\"). Surfacing those to client = brand damage. Default-internal + opt-in-external mirrors real consulting workflow.</p>\n<p><strong>Why 30-day reopen window (§1 #14, DEC-1251)?</strong> Resolved issues sometimes recur; convenient to reuse the original thread vs starting fresh. 30 days = \"still recent in context\" without indefinite resurrection.</p>\n<p><strong>Why per-tenant routing rules (§1 #6, DEC-1242)?</strong> Different tenants have different team structures. Hardcoded routing = wrong for everyone except CyberSkill. Rules + fallback to engagement_admin covers all cases.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0018_portal_workflow_submissions.sql\nCREATE TYPE client_workflow_kind AS ENUM ('new_project_request','billing_inquiry','support_ticket','feature_request','general_question');\nCREATE TYPE workflow_status AS ENUM ('submitted','acknowledged','in_progress','awaiting_client','resolved','closed','escalated');\n\nCREATE TABLE portal_workflow_submissions (\n  workflow_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  submitter_subject_id UUID NOT NULL,\n  workflow_kind client_workflow_kind NOT NULL,\n  status workflow_status NOT NULL DEFAULT 'submitted',\n  title TEXT NOT NULL,\n  body TEXT NOT NULL,\n  attachments JSONB NOT NULL DEFAULT '[]'::jsonb,\n  assignee_subject_id UUID,\n  chat_thread_id UUID,\n  sla_acknowledged_by TIMESTAMPTZ,\n  sla_resolved_by TIMESTAMPTZ,\n  acknowledged_at TIMESTAMPTZ,\n  resolved_at TIMESTAMPTZ,\n  escalated_at TIMESTAMPTZ,\n  escalation_reason TEXT,\n  submitted_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_workflow_submitter ON portal_workflow_submissions(submitter_subject_id, submitted_at DESC);\nCREATE INDEX idx_workflow_assignee ON portal_workflow_submissions(assignee_subject_id, status) WHERE status != 'closed';\nCREATE INDEX idx_workflow_sla_check ON portal_workflow_submissions(sla_acknowledged_by, sla_resolved_by) WHERE status IN ('submitted','acknowledged','in_progress');\nALTER TABLE portal_workflow_submissions ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_workflow_submissions_rls ON portal_workflow_submissions\n  USING (\n    tenant_id = current_setting('auth.tenant_id')::uuid\n    AND (\n      submitter_subject_id = current_setting('auth.subject_id')::uuid\n      OR assignee_subject_id = current_setting('auth.subject_id')::uuid\n      OR EXISTS (SELECT 1 FROM subject_roles\n                 WHERE subject_id = current_setting('auth.subject_id')::uuid\n                   AND role = 'engagement_admin'\n                   AND scope_engagement_id = portal_workflow_submissions.engagement_id)\n    )\n  )\n  WITH CHECK (\n    tenant_id = current_setting('auth.tenant_id')::uuid\n    AND submitter_subject_id = current_setting('auth.subject_id')::uuid\n  );\nREVOKE DELETE ON portal_workflow_submissions FROM cyberos_app;\nGRANT UPDATE (status, assignee_subject_id, chat_thread_id, sla_acknowledged_by, sla_resolved_by,\n              acknowledged_at, resolved_at, escalated_at, escalation_reason, updated_at)\n  ON portal_workflow_submissions TO cyberos_app;\n\n-- 0019_portal_workflow_routing_rules.sql\nCREATE TABLE portal_workflow_routing_rules (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  workflow_kind client_workflow_kind NOT NULL,\n  engagement_id UUID,\n  assignee_subject_id UUID NOT NULL,\n  priority INT NOT NULL DEFAULT 100,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  created_by_subject_id UUID NOT NULL\n);\nCREATE INDEX idx_routing_lookup ON portal_workflow_routing_rules(tenant_id, workflow_kind, engagement_id, priority);\nALTER TABLE portal_workflow_routing_rules ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_workflow_routing_rules_rls ON portal_workflow_routing_rules\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_workflow_routing_rules FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/portal/workflows/submit                (submitter)\nGET    /v1/portal/workflows?engagement_id=...     (own list)\nGET    /v1/portal/workflows/{id}                   (detail)\nPOST   /v1/portal/workflows/{id}/reply             (submitter reply)\nPOST   /v1/portal/workflows/{id}/reopen            (submitter; 30d window)\nPOST   /v1/admin/tenants/{tid}/workflow-routes     (tenant_admin: configure rules)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>client_workflow_kind cardinality</strong> — 5 values.</li><li><strong>workflow_status cardinality</strong> — 7 values.</li><li><strong>Submit creates CHAT thread</strong> — <code>POST /submit</code> → workflow row + chat thread created in proper channel.</li><li><strong>Auto-routing per rule</strong> — rule for tenant + workflow_kind → assignee_subject_id populated.</li><li><strong>Fallback to engagement_admin</strong> — no rule → engagement_admin assigned.</li><li><strong>Auto-priority on security keyword</strong> — body containing \"breach\" → status='escalated' + sev-1 alert.</li><li><strong>SLA breach detected</strong> — daily job marks workflow <code>sla_acknowledged_by &lt; now()</code> → emits sev-1 breach audit.</li><li><strong>File attachment ≤ 25 MiB</strong> — uploaded file 26 MiB → 413.</li><li><strong>Max 5 attachments</strong> — 6th attachment → 400.</li><li><strong>Internal messages hidden from submitter</strong> — CHAT message with <code>client_visible: false</code> NOT in submitter detail view.</li><li><strong>Client reply creates client_visible message</strong> — POST /reply creates CHAT message with type='client_reply' + visibility=true.</li><li><strong>Reopen within 30 days</strong> — closed workflow + 29 days later → reopen succeeds.</li><li><strong>Reopen beyond 30 days</strong> — 31 days later → 400.</li><li><strong>Email on status change</strong> — status transition triggers FR-EMAIL-001 send.</li><li><strong>Rate limit 10/hr/kind/caller</strong> — 11th submission of same kind → 429.</li><li><strong>6 memory audit kinds emitted</strong> — full lifecycle covers all 6.</li><li><strong>PII scrubbed</strong> — title/body SHA256 only in chain.</li><li><strong>Trace_id threaded</strong> — submit → route → chat-create → audit all share trace_id.</li><li><strong>RLS — engagement_admin sees engagement-wide</strong> — engagement_admin lists shows all workflows in their Engagement.</li><li><strong>RLS — submitter sees own only</strong> — submitter list excludes other submitters' rows.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// 5.1 submit creates CHAT thread\n#[tokio::test]\nasync fn submit_creates_workflow_and_chat_thread() {\n    let ctx = TestContext::with_engagement_subject().await;\n    ctx.seed_chat_channel(\"billing\").await;\n    let r = ctx.post_workflow(\"billing_inquiry\", \"Q on invoice 42\", \"...\").await;\n    assert_eq!(r.status(), 201);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert!(body[\"chat_thread_id\"].is_string());\n\n    let thread_exists: bool = sqlx::query_scalar(\n        \"SELECT EXISTS(SELECT 1 FROM chat_threads WHERE id=$1)\"\n    ).bind(body[\"chat_thread_id\"].as_str().unwrap().parse::&lt;Uuid&gt;().unwrap()).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(thread_exists);\n}\n\n// 5.2 auto-priority on breach keyword\n#[tokio::test]\nasync fn breach_keyword_escalates() {\n    let ctx = TestContext::with_engagement_subject().await;\n    let r = ctx.post_workflow(\"support_ticket\", \"URGENT: data breach detected\", \"...\").await;\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"status\"], \"escalated\");\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.workflow_status_changed\"\n        &amp;&amp; r.payload[\"new_status\"] == \"escalated\"));\n}\n\n// 5.3 SLA breach detection\n#[tokio::test]\nasync fn sla_breach_emits_sev1() {\n    let ctx = TestContext::new().await;\n    let wid = ctx.create_workflow_with_past_sla().await;\n    ctx.run_sla_monitor_job().await;\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.workflow_sla_breach\" &amp;&amp; r.severity == 1));\n}\n\n// 5.4 internal hidden, external visible\n#[tokio::test]\nasync fn submitter_sees_only_client_visible_messages() {\n    let ctx = TestContext::with_engagement_subject().await;\n    let wid = ctx.create_workflow().await;\n    ctx.as_engagement_team().post_chat_message(wid, \"internal note\", false).await;\n    ctx.as_engagement_team().post_chat_message(wid, \"client-visible reply\", true).await;\n\n    let r = ctx.as_submitter().get_workflow_detail(wid).await;\n    let messages: Vec&lt;&amp;str&gt; = ctx.extract_messages(r).await;\n    assert!(messages.contains(&amp;\"client-visible reply\"));\n    assert!(!messages.contains(&amp;\"internal note\"));\n}\n\n// 5.5 reopen 30d window\n#[tokio::test]\nasync fn reopen_within_window() {\n    let ctx = TestContext::new().await;\n    let wid = ctx.create_and_close_workflow().await;\n    ctx.travel_clock_forward(Duration::from_days(29)).await;\n    let r = ctx.reopen_workflow(wid).await;\n    assert_eq!(r.status(), 200);\n    let status: String = sqlx::query_scalar(\"SELECT status::text FROM portal_workflow_submissions WHERE workflow_id=$1\")\n        .bind(wid).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(status, \"awaiting_client\");\n}\n\n// 5.6..5.12: enum cardinality, rate limit, routing rule, attachment cap, email, audit</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-CHAT-005 (thread + message primitive). <strong>Cross-module:</strong> FR-PORTAL-001 (workflow shown in PORTAL list), FR-PORTAL-003 (IdP subject), FR-PORTAL-005 (Genie may surface workflow state), FR-PROJ-001 (new_project_request creates PROJ entity), FR-INV-001 (billing_inquiry may reference invoices), FR-AUTH-101 (engagement_admin role), FR-EMAIL-001 (notification + template overrides), FR-AI-003 (audit), FR-MEMORY-111 (PII scrub), FR-OBS-007 (sev-1 escalation routing). <strong>Downstream:</strong> None.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>portal.workflow_submitted</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.workflow_submitted\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.submitter.456\",\n  \"trace_id\": \"...\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"workflow_id\": \"0190...\",\n    \"engagement_id\": \"0190...\",\n    \"workflow_kind\": \"billing_inquiry\",\n    \"title_sha256\": \"9c4e...\",\n    \"chat_thread_id\": \"0190...\",\n    \"sla_acknowledged_by\": \"2026-05-17T13:14:32Z\",\n    \"sla_resolved_by\": \"2026-05-22T09:14:32Z\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Per-workflow custom fields (slice 3).</li><li><strong>Deferred:</strong> Workflow templates (slice 3).</li><li><strong>Deferred:</strong> Multi-step workflows with approval gates (slice 3).</li><li><strong>Deferred:</strong> Workflow analytics dashboard for tenant_admin (slice 3).</li><li><strong>Deferred:</strong> Bulk client reply (multi-workflow) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Unknown workflow_kind</td><td>enum check</td><td>400</td><td>Caller fixes</td></tr><tr><td>Engagement not in membership</td><td>scope check</td><td>403</td><td>Caller's engagements only</td></tr><tr><td>CHAT thread creation fails</td><td>FR-CHAT-005 error</td><td>Workflow row rolled back; 500</td><td>Operator investigates CHAT</td></tr><tr><td>Routing rule missing + no engagement_admin</td><td>fallback miss</td><td>Sev-1 alert; workflow remains 'submitted' unassigned</td><td>Tenant_admin assigns rule</td></tr><tr><td>Attachment upload fails</td><td>S3 error</td><td>503; caller retries</td><td>Inherent</td></tr><tr><td>Attachment &gt; 25 MiB</td><td>size check</td><td>413</td><td>Caller compresses</td></tr><tr><td>&gt; 5 attachments</td><td>count check</td><td>400</td><td>Caller reduces</td></tr><tr><td>Auto-priority false-positive</td><td>manual review</td><td>Engagement_admin de-escalates via status_change</td><td>Inherent UX</td></tr><tr><td>SLA breach not detected (job failure)</td><td>daily monitor + watchdog</td><td>Sev-2 alert</td><td>Operator runs monitor manually</td></tr><tr><td>Reopen beyond 30d</td><td>window check</td><td>400</td><td>New submission</td></tr><tr><td>Rate limit hit</td><td>counter</td><td>429</td><td>Caller waits</td></tr><tr><td>Submitter tries to see internal CHAT messages</td><td>client_visible filter</td><td>Internal hidden</td><td>Inherent</td></tr><tr><td>Cross-tenant submission</td><td>RLS rejects</td><td>403</td><td>Inherent</td></tr><tr><td>Email send fails</td><td>FR-EMAIL-001 error</td><td>Status change still committed; email retried by FR-EMAIL-001 job</td><td>Inherent retry</td></tr><tr><td>Submitter deprovisioned mid-workflow (FR-PORTAL-004)</td><td>session revoked</td><td>Subsequent actions 401; workflow row remains</td><td>Engagement team continues internally</td></tr><tr><td>Engagement_admin role lost mid-workflow</td><td>RLS at next read</td><td>Engagement_admin loses workflow visibility</td><td>Tenant_admin re-grants role</td></tr><tr><td>Status state machine invalid transition (e.g. resolved → submitted)</td><td>state guard</td><td>400 + <code>invalid_status_transition</code></td><td>Caller fixes flow</td></tr><tr><td>Reopen of escalated workflow</td><td>special handling</td><td>Status → awaiting_client; escalated_at cleared</td><td>Inherent</td></tr><tr><td>Concurrent status updates</td><td>optimistic lock via updated_at</td><td>Last writer wins; loser sees 409</td><td>Caller refetches + retries</td></tr><tr><td>Workflow with no chat_thread_id</td><td>bridge failure during creation</td><td>Row exists in degraded state; sev-2 alert</td><td>Background job retries CHAT create</td></tr><tr><td>Attachment SHA256 mismatch</td><td>server verification</td><td>Attachment rejected</td><td>Caller re-uploads</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Routing rule lookup uses index on (tenant_id, workflow_kind, engagement_id, priority); typical query &lt; 1ms.</p>\n<p><strong>§11.2</strong> Security keyword list maintained in <code>auto_priority.rs::SECURITY_KEYWORDS</code> const; review quarterly.</p>\n<p><strong>§11.3</strong> Status state machine: enforces valid transitions; e.g., submitted → acknowledged → in_progress → resolved → closed; escalated valid from any non-terminal.</p>\n<p><strong>§11.4</strong> SLA business-hours calculator uses tenant timezone + business calendar; defaults UTC weekdays.</p>\n<p><strong>§11.5</strong> CHAT thread channel mapping: per-tenant config; default <code>#workflow-&lt;kind&gt;</code>.</p>\n<p><strong>§11.6</strong> Client reply via portal UI: server creates CHAT message via FR-CHAT-005 with type='client_reply' + author=submitter_subject_id.</p>\n<p><strong>§11.7</strong> Email templates per FR-PORTAL-002 brand pack: per-tenant override fallback chain.</p>\n<p><strong>§11.8</strong> Rate limit Redis sliding-window per (caller, workflow_kind).</p>\n<p><strong>§11.9</strong> Attachments JSONB shape: <code>[{s3_key, sha256, size_bytes, filename, mime_type}]</code>.</p>\n<p><strong>§11.10</strong> Reopen audit reuses the same workflow_id (vs new row); preserves history continuity.</p>\n<hr>\n<p><em>End of FR-PORTAL-006 spec.</em></p>";
