window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-110"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The capture daemon's runtime supervision <strong>MUST</strong> consist of: (a) an OS-native init unit, (b) a <code>/healthz</code> HTTP endpoint, (c) a periodic sweeper for ephemeral state, (d) signal-driven graceful shutdown. The contract:</p>\n<ol><li><strong>MUST</strong> ship a systemd unit <code>cyberos-memory-capture.service</code> (Linux) AND a launchd plist <code>world.cyberos.memory-capture.plist</code> (macOS). The installer (<code>install-daemon.sh</code>) detects the OS and copies the appropriate file to:</li></ol>\n<ul><li>Linux: <code>/etc/systemd/system/cyberos-memory-capture.service</code> (system-scope) OR <code>~/.config/systemd/user/cyberos-memory-capture.service</code> (user-scope, default for non-root install).</li><li>macOS: <code>~/Library/LaunchAgents/world.cyberos.memory-capture.plist</code> (user-scope; system-scope deferred to slice 3).</li></ul>\n<ol><li><strong>MUST</strong> auto-restart on non-zero exit per OS-native semantics:</li></ol>\n<ul><li>systemd: <code>Restart=on-failure</code>, <code>RestartSec=5s</code>, <code>RestartSteps=5s 10s 30s 1m 5m</code> (capped at 5 min).</li><li>launchd: <code>KeepAlive: true</code>, <code>ThrottleInterval: 60</code> (launchd does not natively support exponential backoff; we implement it in the daemon's own startup logic by sleeping <code>min(2^crash_count, 300)</code> seconds before binding sockets, where <code>crash_count</code> is read from <code>/tmp/cyberos-memory-capture-crashes</code>).</li></ul>\n<ol><li><strong>MUST</strong> expose <code>GET /healthz</code> on <code>127.0.0.1:7777</code> (no external bind; loopback only). Response schema: ``<code>jsonc // 200 OK { \"status\": \"healthy\", \"version\": \"0.1.0\", \"uptime_seconds\": 14392, \"watched_folders\": 12, \"queue_depth\": 47, \"queue_capacity\": 10000, \"last_emit_ns\": 1747407137483000000, \"doctor_invariants\": {\"all_pass\": true, \"checked_at_ns\": 1747407100000000000} } // 503 Service Unavailable { \"status\": \"unhealthy\", \"reasons\": [\"doctor_invariant_failed: WatchedFolderResolvable\", \"queue_saturated\"], \"queue_depth\": 9876, \"queue_capacity\": 10000, \"doctor_invariants\": {\"all_pass\": false, \"first_failure\": \"WatchedFolderResolvable\"} } </code>``</li><li><strong>MUST</strong> return 503 when ANY of the following are true:</li></ol>\n<ul><li>Any FR-MEMORY-105 error-severity invariant fails (checked every 60s; result cached).</li><li>Queue depth ≥ 95% of capacity (9500 / 10000) for ≥ 30 seconds.</li><li>Last successful emit was &gt; 5 minutes ago AND watched folders are non-empty (likely deadlock).</li><li>The memory writer subprocess (per FR-MEMORY-101) is unreachable.</li></ul>\n<ol><li><strong>MUST</strong> run a sweeper task on a 60-second tokio interval that prunes:</li></ol>\n<ul><li><code>/tmp/cyberos-memory-claude-traces/&lt;uuid&gt;</code> files older than 1 hour (FR-MEMORY-109 trace cache).</li><li>Dedup-cache entries whose <code>stored_at</code> exceeded TTL (5 minutes per FR-MEMORY-107 §1 #4).</li><li>Old metric snapshots in <code>/tmp/cyberos-memory-metrics/</code> older than 24 hours.</li><li>Crash-count file <code>/tmp/cyberos-memory-capture-crashes</code> if last reset &gt; 1 hour ago (allows recovery from a transient crash storm without sticky exp-backoff).</li></ul>\n<ol><li><strong>MUST</strong> install signal handlers per AGENTS.md operational conventions:</li></ol>\n<ul><li><code>SIGTERM</code> → graceful shutdown: stop accepting new events, drain queue (deadline 5s), flush metrics, exit 0.</li><li><code>SIGINT</code> → same as SIGTERM (treat Ctrl-C like Stop).</li><li><code>SIGHUP</code> → reload manifest (per FR-MEMORY-107 §1 #15); does not exit.</li><li><code>SIGUSR1</code> → dump health snapshot to stderr (operator debugging).</li></ul>\n<p>6.5. <strong>MUST</strong> treat <code>SIGKILL</code> as expected (cannot be caught); systemd/launchd restart on the next cycle.</p>\n<ol><li><strong>MUST</strong> track crash count in <code>/tmp/cyberos-memory-capture-crashes</code> (single u32 LE; incremented at startup; reset to 0 after 5 minutes of stable uptime). The daemon uses this to compute its own startup-delay backoff on launchd.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>memory_capture_daemon_uptime_seconds</code> (gauge).</li><li><code>memory_capture_daemon_restart_count_total</code> (counter; survives restart via the crash-count file).</li><li><code>memory_capture_sweeper_pruned_total{kind}</code> (counter; kind ∈ trace_cache | dedup | metric_snapshot | crash_count_reset).</li><li><code>memory_capture_health_endpoint_total{status}</code> (counter; status ∈ healthy | unhealthy).</li></ul>\n<ol><li><strong>MUST</strong> emit OTel span <code>memory.capture.sweeper.tick</code> per sweep with attributes <code>pruned_trace_cache</code>, <code>pruned_dedup</code>, <code>pruned_metric_snapshots</code>, <code>duration_ms</code>.</li><li><strong>MUST</strong> emit a <code>memory.capture_supervisor_event</code> memory audit row when the daemon process starts (kind=<code>started</code>), reloads manifest (kind=<code>reloaded</code>), or exits (kind=<code>exited</code>, <code>exit_code</code>, <code>uptime_seconds</code>). This gives operators a first-class audit trail for \"when did the daemon last go down?\"</li><li><strong>MUST</strong> integrate with FR-OBS-007 alert routing: 503 on <code>/healthz</code> for ≥ 60 seconds triggers a sev-2 alert; ≥ 5 minutes triggers sev-1 (capture is effectively offline; user actions not being recorded).</li><li><strong>MUST</strong> support <code>cyberos memory capture status</code> CLI which hits <code>/healthz</code> and pretty-prints the response (human-friendly format with colour-coded badges).</li><li><strong>MUST</strong> support <code>cyberos memory capture logs [--follow] [--lines N]</code> which tails the daemon log file (<code>~/Library/Logs/cyberos-memory-capture.log</code> on macOS, <code>journalctl -u cyberos-memory-capture</code> on Linux).</li><li><strong>MUST</strong> be installed by <code>install-daemon.sh</code> and uninstalled by <code>uninstall-daemon.sh</code>. Install is idempotent; uninstall is total (removes unit file, stops daemon, removes /tmp files).</li><li><strong>SHOULD</strong> publish <code>cyberos-memory-capture.service</code> to a Homebrew formula (macOS) and a deb/rpm package (Linux) — slice 3+.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why systemd + launchd (§1 #1)?</strong> They are the canonical OS init systems for Linux + macOS. They handle: process supervision, restart on crash, log rotation, signal delivery, dependency ordering. Building a custom supervisor would re-implement 30 years of init-system engineering. Per DEC-161 we do not.</p>\n<p><strong>Why exp backoff capped at 5 min (§1 #2)?</strong> A daemon that crashes immediately on startup (e.g. corrupted manifest) without backoff would consume parent process CPU and flood logs. Exp backoff is the standard pattern. 5-min cap prevents long outages: operators see \"daemon down 5 min, sev-1 alarm\" and act; without a cap, an undetected misconfiguration could leave the daemon restart-locked for hours.</p>\n<p><strong>Why /healthz on 127.0.0.1:7777 (§1 #3)?</strong> Loopback-only because: (a) we don't want external network exposure on capture daemon; (b) the consumers (monitoring agents, FR-OBS-007 probes) run on the same machine. Port 7777 is unallocated by IANA + memorable. The endpoint is unauthenticated; loopback is the perimeter.</p>\n<p><strong>Why 503 on multiple conditions (§1 #4)?</strong> Operators need to distinguish \"daemon healthy\" from \"daemon up but useless.\" Queue saturated = events being dropped silently. Doctor invariant failed = capture wrong / unsafe. Stale emit = likely deadlock. Each condition warrants restart; 503 makes the supervisor handle it (systemd <code>Restart=on-failure</code> with a healthcheck wrapper).</p>\n<p><strong>Why 60s sweep interval (§1 #5)?</strong> Faster sweeping wastes CPU on idle systems; slower sweeping accumulates /tmp clutter. 60s is the empirical sweet spot: low overhead, low clutter. Operators can SIGHUP to force a sweep early (debugging).</p>\n<p><strong>Why crash-count file in /tmp (§1 #7)?</strong> launchd doesn't expose crash counts natively (unlike systemd's <code>Restart=on-failure</code> which has <code>RestartSteps</code> built in). To implement exp backoff on macOS, we maintain our own counter. /tmp is the right scope (per-machine, ephemeral); resets to 0 after stable uptime so transient crashes don't lock us into 5-min backoff forever.</p>\n<p><strong>Why supervisor_event audit rows (§1 #10)?</strong> Operators investigating \"memory had a gap between 14:00 and 14:23\" need to answer \"was the daemon down?\" Without a first-class audit row, they have to cross-reference systemd journalctl with memory — friction. The row turns the supervisor's behaviour into queryable data.</p>\n<p><strong>Why sev-2 → sev-1 at 5 min (§1 #11)?</strong> 60s of capture downtime is recoverable (most editor saves get caught on next batch). 5 min is significant data loss (8+ files of meaningful work). The escalation gives ops a window to self-resolve before paging.</p>\n<p><strong>Why no <code>--restart-now</code> CLI command?</strong> The OS init system owns the lifecycle. <code>systemctl restart cyberos-memory-capture</code> (Linux) and <code>launchctl kickstart -k gui/$UID/world.cyberos.memory-capture</code> (macOS) are the canonical paths. Adding a Cyberos-level wrapper duplicates them; operators benefit from learning the OS commands once.</p>\n<p><strong>Why <code>cyberos memory capture logs</code> (§1 #13)?</strong> Cross-platform log access is a friction point: <code>journalctl -u name --follow</code> on Linux vs tailing a file on macOS. The CLI command abstracts the difference.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>systemd unit</h3>\n<pre><code class=\"language-ini\"># install/systemd/cyberos-memory-capture.service\n[Unit]\nDescription=CyberOS memory capture daemon\nAfter=network.target\nDocumentation=https://docs.cyberos.world/runbooks/memory-capture-runbook.html\n\n[Service]\nType=simple\nExecStart=/usr/local/bin/cyberos-memory-capture --foreground\nRestart=on-failure\nRestartSec=5s\n# Exponential backoff (systemd 254+): 5s → 10s → 30s → 1m → 5m → 5m → ...\nRestartSteps=5\nRestartMaxDelaySec=5min\n# Resource limits (sane defaults; operator may override)\nMemoryMax=1G\nTasksMax=512\n# Graceful shutdown deadline (matches §1 #6)\nTimeoutStopSec=10s\n# Crash notification to journal (parsed by FR-OBS-001 collector)\nStandardOutput=journal\nStandardError=journal\n# Environment file (writer endpoint, manifest path, OTLP endpoint)\nEnvironmentFile=-/etc/cyberos/memory-capture.env\n\n[Install]\nWantedBy=default.target</code></pre>\n<h3>launchd plist</h3>\n<pre><code class=\"language-xml\">&lt;!-- install/launchd/world.cyberos.memory-capture.plist --&gt;\n&lt;?xml version=\"1.0\" encoding=\"UTF-8\"?&gt;\n&lt;!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\" \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\"&gt;\n&lt;plist version=\"1.0\"&gt;\n&lt;dict&gt;\n  &lt;key&gt;Label&lt;/key&gt;             &lt;string&gt;world.cyberos.memory-capture&lt;/string&gt;\n  &lt;key&gt;ProgramArguments&lt;/key&gt;  &lt;array&gt;&lt;string&gt;/usr/local/bin/cyberos-memory-capture&lt;/string&gt;&lt;string&gt;--foreground&lt;/string&gt;&lt;/array&gt;\n  &lt;key&gt;RunAtLoad&lt;/key&gt;         &lt;true/&gt;\n  &lt;key&gt;KeepAlive&lt;/key&gt;         &lt;true/&gt;\n  &lt;!-- launchd's anti-flap; daemon's own backoff handles longer delays --&gt;\n  &lt;key&gt;ThrottleInterval&lt;/key&gt;  &lt;integer&gt;60&lt;/integer&gt;\n  &lt;key&gt;ExitTimeOut&lt;/key&gt;       &lt;integer&gt;10&lt;/integer&gt;\n  &lt;key&gt;StandardOutPath&lt;/key&gt;   &lt;string&gt;/Users/USERNAME/Library/Logs/cyberos-memory-capture.log&lt;/string&gt;\n  &lt;key&gt;StandardErrorPath&lt;/key&gt; &lt;string&gt;/Users/USERNAME/Library/Logs/cyberos-memory-capture.err&lt;/string&gt;\n  &lt;key&gt;WorkingDirectory&lt;/key&gt;  &lt;string&gt;/tmp&lt;/string&gt;\n  &lt;key&gt;EnvironmentVariables&lt;/key&gt;\n  &lt;dict&gt;\n    &lt;key&gt;RUST_LOG&lt;/key&gt;        &lt;string&gt;info,cyberos_memory_capture=debug&lt;/string&gt;\n    &lt;key&gt;CYBEROS_MANIFEST&lt;/key&gt;&lt;string&gt;/Users/USERNAME/Library/Application Support/CyberOS/manifest.json&lt;/string&gt;\n  &lt;/dict&gt;\n&lt;/dict&gt;\n&lt;/plist&gt;</code></pre>\n<h3>healthz endpoint</h3>\n<pre><code class=\"language-rust\">// services/memory-capture/src/healthz.rs\nuse axum::{Router, routing::get, Json};\nuse axum::http::StatusCode;\nuse std::sync::Arc;\nuse std::time::{Duration, Instant};\nuse serde::Serialize;\n\npub struct HealthState {\n    pub daemon_start:   Instant,\n    pub queue_depth:    Arc&lt;std::sync::atomic::AtomicU64&gt;,\n    pub queue_capacity: u64,\n    pub last_emit_ns:   Arc&lt;std::sync::atomic::AtomicU64&gt;,\n    pub last_doctor:    Arc&lt;tokio::sync::RwLock&lt;DoctorSnapshot&gt;&gt;,\n    pub watched_count:  Arc&lt;std::sync::atomic::AtomicU32&gt;,\n}\n\n#[derive(Clone, Serialize)]\npub struct DoctorSnapshot {\n    pub all_pass:      bool,\n    pub first_failure: Option&lt;String&gt;,\n    pub checked_at_ns: u64,\n}\n\n#[derive(Serialize)]\npub struct HealthReport {\n    pub status:           &amp;'static str,\n    pub version:          &amp;'static str,\n    pub uptime_seconds:   u64,\n    pub watched_folders:  u32,\n    pub queue_depth:      u64,\n    pub queue_capacity:   u64,\n    pub last_emit_ns:     u64,\n    pub doctor_invariants: DoctorSnapshot,\n    #[serde(skip_serializing_if = \"Vec::is_empty\")] pub reasons: Vec&lt;String&gt;,\n}\n\npub async fn handler(state: Arc&lt;HealthState&gt;) -&gt; (StatusCode, Json&lt;HealthReport&gt;) {\n    let now = Instant::now();\n    let queue_depth = state.queue_depth.load(std::sync::atomic::Ordering::Relaxed);\n    let last_emit_ns = state.last_emit_ns.load(std::sync::atomic::Ordering::Relaxed);\n    let doctor = state.last_doctor.read().await.clone();\n    let watched = state.watched_count.load(std::sync::atomic::Ordering::Relaxed);\n\n    let mut reasons = Vec::new();\n    if !doctor.all_pass {\n        reasons.push(format!(\"doctor_invariant_failed: {}\", doctor.first_failure.clone().unwrap_or_default()));\n    }\n    if queue_depth &gt;= (state.queue_capacity * 95) / 100 {\n        reasons.push(\"queue_saturated\".into());\n    }\n    let stale_emit = watched &gt; 0\n        &amp;&amp; (unix_ns() as u128).saturating_sub(last_emit_ns as u128) &gt; Duration::from_secs(300).as_nanos();\n    if stale_emit {\n        reasons.push(\"stale_emit\".into());\n    }\n\n    let status = if reasons.is_empty() { \"healthy\" } else { \"unhealthy\" };\n    let code = if reasons.is_empty() { StatusCode::OK } else { StatusCode::SERVICE_UNAVAILABLE };\n    metrics::counter!(\"memory_capture_health_endpoint_total\", \"status\" =&gt; status).increment(1);\n\n    let report = HealthReport {\n        status, version: env!(\"CARGO_PKG_VERSION\"),\n        uptime_seconds: state.daemon_start.elapsed().as_secs(),\n        watched_folders: watched, queue_depth, queue_capacity: state.queue_capacity, last_emit_ns,\n        doctor_invariants: doctor,\n        reasons,\n    };\n    (code, Json(report))\n}\n\npub fn router(state: Arc&lt;HealthState&gt;) -&gt; Router {\n    Router::new().route(\"/healthz\", get({\n        let state = state.clone();\n        move || handler(state.clone())\n    }))\n}</code></pre>\n<h3>Sweeper</h3>\n<pre><code class=\"language-rust\">// services/memory-capture/src/sweeper.rs\nuse tokio::time::{interval, Duration};\nuse std::time::SystemTime;\nuse std::path::Path;\n\npub async fn run_sweeper_loop() {\n    let mut ticker = interval(Duration::from_secs(60));\n    loop {\n        ticker.tick().await;\n        let start = std::time::Instant::now();\n        let pruned_traces  = prune_dir_older_than(\"/tmp/cyberos-memory-claude-traces\", Duration::from_secs(3600));\n        let pruned_metrics = prune_dir_older_than(\"/tmp/cyberos-memory-metrics\",       Duration::from_secs(86400));\n        let reset_crashes  = maybe_reset_crash_count(\"/tmp/cyberos-memory-capture-crashes\", Duration::from_secs(300));\n        tracing::debug!(\n            pruned_trace_cache = pruned_traces,\n            pruned_metric_snapshots = pruned_metrics,\n            crash_count_reset = reset_crashes,\n            duration_ms = start.elapsed().as_millis() as u64,\n            \"memory.capture.sweeper.tick\"\n        );\n        metrics::counter!(\"memory_capture_sweeper_pruned_total\", \"kind\" =&gt; \"trace_cache\").increment(pruned_traces as u64);\n        metrics::counter!(\"memory_capture_sweeper_pruned_total\", \"kind\" =&gt; \"metric_snapshot\").increment(pruned_metrics as u64);\n        if reset_crashes {\n            metrics::counter!(\"memory_capture_sweeper_pruned_total\", \"kind\" =&gt; \"crash_count_reset\").increment(1);\n        }\n    }\n}\n\nfn prune_dir_older_than(dir: &amp;str, age: Duration) -&gt; u32 {\n    let now = SystemTime::now();\n    let mut pruned = 0u32;\n    if let Ok(rd) = std::fs::read_dir(dir) {\n        for entry in rd.flatten() {\n            if let Ok(meta) = entry.metadata() {\n                if let Ok(mtime) = meta.modified() {\n                    if now.duration_since(mtime).map(|d| d &gt; age).unwrap_or(false) {\n                        let _ = std::fs::remove_file(entry.path());\n                        pruned += 1;\n                    }\n                }\n            }\n        }\n    }\n    pruned\n}\n\nfn maybe_reset_crash_count(path: &amp;str, age: Duration) -&gt; bool {\n    if let Ok(meta) = std::fs::metadata(path) {\n        if let Ok(mtime) = meta.modified() {\n            if SystemTime::now().duration_since(mtime).map(|d| d &gt; age).unwrap_or(false) {\n                let _ = std::fs::write(path, 0u32.to_le_bytes());\n                return true;\n            }\n        }\n    }\n    false\n}</code></pre>\n<h3>Installer</h3>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\n# install/install-daemon.sh\nset -euo pipefail\n\ncase \"$(uname -s)\" in\n  Linux)\n    UNIT=cyberos-memory-capture.service\n    DEST=\"${XDG_CONFIG_HOME:-$HOME/.config}/systemd/user/$UNIT\"\n    mkdir -p \"$(dirname \"$DEST\")\"\n    install -m 644 \"$(dirname \"$0\")/systemd/$UNIT\" \"$DEST\"\n    systemctl --user daemon-reload\n    systemctl --user enable --now \"$UNIT\"\n    echo \"✓ installed and started → $DEST\"\n    ;;\n  Darwin)\n    PLIST=\"world.cyberos.memory-capture.plist\"\n    DEST=\"$HOME/Library/LaunchAgents/$PLIST\"\n    sed \"s|/Users/USERNAME|$HOME|g\" \"$(dirname \"$0\")/launchd/$PLIST\" &gt; \"$DEST\"\n    launchctl unload \"$DEST\" 2&gt;/dev/null || true\n    launchctl load -w \"$DEST\"\n    echo \"✓ installed and loaded → $DEST\"\n    ;;\n  *)\n    echo \"Unsupported OS: $(uname -s)\" &gt;&amp;2\n    exit 1\n    ;;\nesac</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>systemd: daemon starts on <code>systemctl --user start</code></strong> — fixture installs unit; <code>systemctl --user is-active</code> returns <code>active</code>.</li><li><strong>systemd: daemon restarts on non-zero exit</strong> — <code>kill -SEGV $(pidof cyberos-memory-capture)</code> → process restarts within <code>RestartSec=5s</code> ± 2s; <code>MainPID</code> differs.</li><li><strong>systemd: exp backoff cap at 5min</strong> — crash 10× rapidly → 10th restart waits ~5 min (within ±10s).</li><li><strong>launchd: daemon starts on <code>launchctl load</code></strong> — fixture installs plist; <code>launchctl print gui/$UID/world.cyberos.memory-capture</code> shows state=running.</li><li><strong>launchd: daemon restarts on crash</strong> — kill -9 the process → launchd restarts within ThrottleInterval=60s.</li><li><strong>launchd: daemon's own backoff</strong> — crash 5× rapidly → daemon-side <code>sleep(min(2^crash_count, 300))</code> is observable in stderr.</li><li><strong>/healthz: 200 on happy</strong> — fresh daemon, no failures → <code>curl 127.0.0.1:7777/healthz</code> returns 200; body has <code>status: \"healthy\"</code>.</li><li><strong>/healthz: 503 on doctor failure</strong> — inject a dangling-symlink fixture → doctor cache reports failure → next <code>/healthz</code> returns 503; reasons includes <code>doctor_invariant_failed</code>.</li><li><strong>/healthz: 503 on queue saturation</strong> — fill queue to 9500/10000 for ≥ 30 seconds → 503 with <code>queue_saturated</code>.</li><li><strong>/healthz: 503 on stale emit</strong> — block emitter for 6 minutes (test seam) → 503 with <code>stale_emit</code>.</li><li><strong>Sweeper prunes trace cache</strong> — write <code>/tmp/cyberos-memory-claude-traces/&lt;uuid&gt;</code> with mtime 2h ago → next tick removes it; metric <code>memory_capture_sweeper_pruned_total{kind=\"trace_cache\"}</code> increments.</li><li><strong>Sweeper prunes metric snapshots</strong> — fixture file in <code>/tmp/cyberos-memory-metrics/</code> with mtime 25h ago → pruned.</li><li><strong>Sweeper resets crash count after stable uptime</strong> — write crash-count file with mtime 6 min ago → sweeper resets to 0.</li><li><strong>SIGTERM graceful shutdown</strong> — send SIGTERM with full queue → daemon drains queue (≤ 5s), emits <code>memory.capture_supervisor_event</code> kind=<code>exited</code>, exits 0.</li><li><strong>SIGHUP reloads manifest</strong> — SIGHUP → daemon re-reads manifest; new folders watched; row <code>kind=reloaded</code> emitted.</li><li><strong>SIGUSR1 dumps health to stderr</strong> — SIGUSR1 → human-formatted health snapshot appears in stderr.</li><li><strong>supervisor_event row on start</strong> — fresh start → exactly one <code>memory.capture_supervisor_event</code> row with kind=<code>started</code>.</li><li><strong>supervisor_event row on exit</strong> — SIGTERM → row with kind=<code>exited</code>, <code>exit_code=0</code>, <code>uptime_seconds</code> present.</li><li><strong>install-daemon.sh idempotent</strong> — run twice → second run is a no-op; unit file byte-identical.</li><li><strong>uninstall-daemon.sh total cleanup</strong> — uninstall → unit/plist removed; daemon stopped; <code>/tmp/cyberos-memory-capture-crashes</code> removed.</li><li><strong>cyberos memory capture status</strong> — pretty-prints <code>/healthz</code> JSON with colour badges (PASS/WARN/FAIL per reason).</li><li><strong>cyberos memory capture logs --follow --lines 100</strong> — tails the appropriate log source; works on both Linux + macOS.</li><li><strong>OTel metrics: uptime + restart count</strong> — Prometheus registry has both metrics; restart count survives a SIGSEGV→restart cycle.</li><li><strong>FR-OBS-007 alert fires</strong> — 503 for 60s → sev-2 alert with <code>service: memory-capture</code> label.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/memory/tests/ingest_test.rs\n\n#[tokio::test]\nasync fn returns_200_when_healthy() {\n    let state = build_healthy_state();\n    let router = healthz::router(state);\n    let req = axum::http::Request::get(\"/healthz\").body(axum::body::Body::empty()).unwrap();\n    let resp = router.oneshot(req).await.unwrap();\n    assert_eq!(resp.status(), 200);\n    let body: HealthReport = parse_body(resp).await;\n    assert_eq!(body.status, \"healthy\");\n    assert!(body.reasons.is_empty());\n}\n\n#[tokio::test]\nasync fn returns_503_when_doctor_failed() {\n    let state = build_healthy_state();\n    *state.last_doctor.write().await = DoctorSnapshot {\n        all_pass: false, first_failure: Some(\"WatchedFolderResolvable\".into()),\n        checked_at_ns: unix_ns(),\n    };\n    let resp = healthz::router(state).oneshot(get_healthz_req()).await.unwrap();\n    assert_eq!(resp.status(), 503);\n    let body: HealthReport = parse_body(resp).await;\n    assert!(body.reasons.iter().any(|r| r.starts_with(\"doctor_invariant_failed\")));\n}\n\n#[tokio::test]\nasync fn returns_503_when_queue_saturated() {\n    let state = build_healthy_state();\n    state.queue_depth.store(9600, std::sync::atomic::Ordering::Relaxed);\n    let resp = healthz::router(state).oneshot(get_healthz_req()).await.unwrap();\n    assert_eq!(resp.status(), 503);\n    let body: HealthReport = parse_body(resp).await;\n    assert!(body.reasons.contains(&amp;\"queue_saturated\".to_string()));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory/tests/ingest_test.rs\n#[test]\nfn prunes_files_older_than_ttl() {\n    let tmpdir = tempdir().unwrap();\n    let dir = tmpdir.path().join(\"traces\");\n    std::fs::create_dir_all(&amp;dir).unwrap();\n    let path = dir.join(\"00000000-0000-0000-0000-000000000001\");\n    std::fs::write(&amp;path, b\"x\").unwrap();\n    // Backdate mtime by 2 hours\n    set_mtime(&amp;path, std::time::SystemTime::now() - std::time::Duration::from_secs(7200));\n\n    let pruned = sweeper::prune_dir_older_than(dir.to_str().unwrap(), Duration::from_secs(3600));\n    assert_eq!(pruned, 1);\n    assert!(!path.exists());\n}\n\n#[test]\nfn preserves_fresh_files() {\n    let tmpdir = tempdir().unwrap();\n    let dir = tmpdir.path().join(\"traces\");\n    std::fs::create_dir_all(&amp;dir).unwrap();\n    let path = dir.join(\"fresh\");\n    std::fs::write(&amp;path, b\"y\").unwrap();\n    let pruned = sweeper::prune_dir_older_than(dir.to_str().unwrap(), Duration::from_secs(3600));\n    assert_eq!(pruned, 0);\n    assert!(path.exists());\n}</code></pre>\n<pre><code class=\"language-bash\"># services/memory-capture/tests/restart_e2e_test.sh\n#!/usr/bin/env bash\nset -euo pipefail\n\nbash install/install-daemon.sh\nsleep 2\n[ \"$(systemctl --user is-active cyberos-memory-capture)\" = \"active\" ]\n\n# Crash + restart\nORIG_PID=$(systemctl --user show -p MainPID --value cyberos-memory-capture)\nkill -9 \"$ORIG_PID\"\nsleep 7\nNEW_PID=$(systemctl --user show -p MainPID --value cyberos-memory-capture)\n[ \"$ORIG_PID\" != \"$NEW_PID\" ]\n[ \"$(systemctl --user is-active cyberos-memory-capture)\" = \"active\" ]\n\n# Crash 10× rapidly → 10th restart should be at 5 min cap (sample 3 to keep CI under 5 min)\nfor i in 1 2 3; do\n  kill -9 $(systemctl --user show -p MainPID --value cyberos-memory-capture)\n  sleep 1\ndone\n# After 3 crashes, RestartSec should be 30s (per RestartSteps); verify within ±5s\nSTART=$(date +%s)\nwhile [ \"$(systemctl --user is-active cyberos-memory-capture)\" != \"active\" ]; do\n  sleep 1\n  [ $(($(date +%s) - START)) -lt 40 ] || { echo \"did not restart within 40s\"; exit 1; }\ndone\n\nbash install/uninstall-daemon.sh</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<pre><code class=\"language-rust\">// services/memory-capture/src/main.rs (excerpt — daemon entrypoint)\n\n#[tokio::main]\nasync fn main() -&gt; cyberos_cli_exit::ExitCode {\n    use cyberos_cli_exit::ExitCode;\n    let cli = Cli::parse();\n    cyberos_obs_sdk::init(\"memory-capture\");\n\n    // Crash-count exp-backoff (launchd-side)\n    if cfg!(target_os = \"macos\") {\n        let count = read_crash_count();\n        let delay_s = std::cmp::min(2u64.pow(count.min(10)), 300);\n        if delay_s &gt; 0 {\n            tracing::warn!(count, delay_s, \"delaying startup for exp backoff\");\n            tokio::time::sleep(Duration::from_secs(delay_s)).await;\n        }\n        increment_crash_count();\n    }\n\n    // Doctor gate (FR-MEMORY-105 §1 #13)\n    if let Err(e) = run_doctor_gate().await {\n        tracing::error!(?e, \"doctor invariant failure; refusing to start\");\n        return ExitCode::InternalError;\n    }\n\n    // Start the daemon (FR-MEMORY-107) and supervisory pieces\n    let daemon = cyberos_memory_capture::CaptureDaemon::start(cli.manifest, cli.dry_run).await\n        .expect(\"daemon start failed\");\n    let health_state = daemon.health_state();\n\n    // Bind /healthz on 127.0.0.1:7777\n    let healthz_listener = tokio::net::TcpListener::bind(\"127.0.0.1:7777\").await.unwrap();\n    tokio::spawn(async move {\n        axum::serve(healthz_listener, cyberos_memory_capture::healthz::router(health_state)).await.unwrap();\n    });\n\n    // Spawn sweeper\n    tokio::spawn(cyberos_memory_capture::sweeper::run_sweeper_loop());\n\n    // Emit supervisor_event:started\n    daemon.emit_supervisor_event(\"started\", None).await;\n\n    // Signal-driven main loop\n    let mut sighup  = tokio::signal::unix::signal(tokio::signal::unix::SignalKind::hangup()).unwrap();\n    let mut sigusr1 = tokio::signal::unix::signal(tokio::signal::unix::SignalKind::user_defined1()).unwrap();\n    let mut sigterm = tokio::signal::unix::signal(tokio::signal::unix::SignalKind::terminate()).unwrap();\n    let mut sigint  = tokio::signal::unix::signal(tokio::signal::unix::SignalKind::interrupt()).unwrap();\n    let start = std::time::Instant::now();\n    loop {\n        tokio::select! {\n            _ = sighup.recv()  =&gt; { let _ = daemon.reload().await; daemon.emit_supervisor_event(\"reloaded\", None).await; }\n            _ = sigusr1.recv() =&gt; { eprintln!(\"{:#?}\", daemon.health_state().snapshot().await); }\n            _ = sigterm.recv() =&gt; { daemon.stop().await; daemon.emit_supervisor_event(\"exited\", Some(0)).await; break; }\n            _ = sigint.recv()  =&gt; { daemon.stop().await; daemon.emit_supervisor_event(\"exited\", Some(0)).await; break; }\n        }\n    }\n    ExitCode::Ok\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-105 (upstream)</strong> — <code>cyberos doctor --only watched-folders</code> is the gate at boot AND the periodic check feeding <code>/healthz</code>.</li><li><strong>FR-MEMORY-107 (upstream)</strong> — capture daemon being supervised; this FR adds the supervision wrapper.</li><li><strong>FR-MEMORY-108 (related)</strong> — Cowork session capture FR is downstream consumer (its hook posts to this daemon's socket).</li><li><strong>FR-MEMORY-109 (related)</strong> — Claude Code hook capture; same socket consumer + the <code>/tmp/cyberos-memory-claude-traces/</code> cache this FR sweeps.</li><li><strong>FR-OBS-001 (related)</strong> — journal/log capture by the OTel collector reads our stdout/journalctl.</li><li><strong>FR-OBS-007 (downstream)</strong> — alert routing fires on 503-for-60s and 503-for-5min thresholds.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Happy <code>/healthz</code></h3>\n<pre><code class=\"language-json\">{\n  \"status\": \"healthy\",\n  \"version\": \"0.1.0\",\n  \"uptime_seconds\": 14392,\n  \"watched_folders\": 12,\n  \"queue_depth\": 47,\n  \"queue_capacity\": 10000,\n  \"last_emit_ns\": 1747407137483000000,\n  \"doctor_invariants\": {\n    \"all_pass\": true,\n    \"first_failure\": null,\n    \"checked_at_ns\": 1747407100000000000\n  }\n}</code></pre>\n<h3>Unhealthy <code>/healthz</code></h3>\n<pre><code class=\"language-json\">{\n  \"status\": \"unhealthy\",\n  \"version\": \"0.1.0\",\n  \"uptime_seconds\": 432,\n  \"watched_folders\": 12,\n  \"queue_depth\": 9876,\n  \"queue_capacity\": 10000,\n  \"last_emit_ns\": 1747406500000000000,\n  \"doctor_invariants\": {\n    \"all_pass\": false,\n    \"first_failure\": \"WatchedFolderResolvable\",\n    \"checked_at_ns\": 1747407100000000000\n  },\n  \"reasons\": [\n    \"doctor_invariant_failed: WatchedFolderResolvable\",\n    \"queue_saturated\",\n    \"stale_emit\"\n  ]\n}</code></pre>\n<h3><code>memory.capture_supervisor_event</code> (start)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.capture_supervisor_event\",\n  \"payload\": {\n    \"event\":     \"started\",\n    \"version\":   \"0.1.0\",\n    \"pid\":       4823,\n    \"host_id\":   \"mac-stephen-001\",\n    \"started_at_ns\": 1747407137483000000\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Homebrew / deb / rpm distribution — slice 3+; needs release pipeline.</li><li>Multi-user macOS install (system-scope LaunchDaemon under root) — slice 3+; needs permissions design.</li><li>Cross-host failover (one daemon, multiple hosts) — slice 5+; speculative.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Process SEGFAULT</td><td>OS signal</td><td>systemd/launchd restart per backoff</td><td>None — automatic</td></tr><tr><td>OOM kill</td><td>OOM-killer</td><td>Restart; sev-1 if MemoryMax repeatedly hit</td><td>Operator investigates leak or raises limit</td></tr><tr><td>Manifest corrupted (bad JSON)</td><td>Daemon exits 1 at boot</td><td>Backoff escalates to 5min; sev-1</td><td>Operator restores manifest</td></tr><tr><td>Doctor gate fails</td><td>Daemon exits 7 at boot</td><td>Backoff escalates; sev-2</td><td>Operator runs <code>cyberos doctor</code> to identify failure</td></tr><tr><td>/healthz port 7777 in use</td><td>bind() Err</td><td>Daemon exits 1 with stderr; sev-1</td><td>Operator frees port or sets env CYBEROS_HEALTHZ_PORT</td></tr><tr><td>Queue saturated for 30s</td><td>health check 503</td><td>systemd's healthcheck wrapper restarts (or sev-2 alarm)</td><td>Operator investigates emitter</td></tr><tr><td>Stale emit &gt; 5min</td><td>health check 503</td><td>Restart</td><td>Operator investigates writer</td></tr><tr><td>Sweeper task panics</td><td>tokio task watchdog</td><td>Process exits; restart</td><td>Operator investigates</td></tr><tr><td>/tmp full</td><td>sweeper writes fail</td><td>sweeper logs WARN; continues with in-mem</td><td>Operator frees /tmp; auto-recovers</td></tr><tr><td>Crash count file unreadable</td><td>read returns 0 (treated as fresh)</td><td>No backoff; possible rapid-restart</td><td>Operator restores file or accepts behaviour</td></tr><tr><td>Crash count file write fails (/tmp ENOSPC)</td><td>write Err</td><td>Backoff math degrades to \"no backoff\"; restart loop tight</td><td>Operator frees /tmp</td></tr><tr><td>Two daemon instances (operator error)</td><td>second instance: bind() Err on healthz port</td><td>Second exits 1; first continues</td><td>Operator stops one</td></tr><tr><td>systemd unit file ENOENT after upgrade</td><td>systemctl daemon-reload finds nothing</td><td>Daemon dies; uninstall trigger</td><td>Operator reinstalls</td></tr><tr><td>launchd plist syntax error</td><td>launchctl load Err</td><td>Daemon doesn't start</td><td>Operator fixes plist</td></tr><tr><td>OTel exporter down</td><td>metric/span buffering</td><td>Buffer fills; oldest dropped; daemon survives</td><td>Operator restores FR-OBS-001</td></tr><tr><td><code>cyberos memory capture status</code> can't reach /healthz</td><td>connection refused</td><td>CLI prints error; exit 1</td><td>Operator checks daemon is running</td></tr><tr><td><code>cyberos memory capture logs</code> on macOS but log file path wrong</td><td>tail Err</td><td>CLI prints error</td><td>Operator updates plist path or accepts default</td></tr><tr><td>RestartMaxDelaySec hit; daemon-down 5+ min</td><td>FR-OBS-007 sev-1 page</td><td>Operator investigates manually</td><td>Manual fix and restart</td></tr><tr><td>Network mount unavailable when daemon starts</td><td>manifest path unreadable</td><td>exit 1; backoff; sev-2</td><td>Operator remounts</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The systemd unit uses <code>RestartSteps</code> introduced in systemd 254 (mid-2024); systems on older releases get a fixed <code>RestartSec=30s</code> fallback (declared by <code>Conditional</code> block in the unit file).</li><li><code>--user</code> mode is the default install scope; system-scope (root daemon for all users) is gated behind <code>--system</code> and requires sudo.</li><li>launchd's <code>ThrottleInterval=60</code> means launchd waits 60s before restarting a process that exits within 10s of starting. Our daemon-side backoff layers on top: if the in-daemon <code>crash_count</code> says we've crashed 4 times rapidly, the daemon itself sleeps 16s before binding sockets — total wait ≈ 76s.</li><li><code>/healthz</code> uses Axum because it's already in the workspace (FR-OBS-002). Adding <code>hyper</code> directly would be smaller but inconsistent.</li><li>The doctor-cache TTL (60s) means a doctor failure may take up to 60s to surface in <code>/healthz</code>. Faster polling would burn CPU; slower would mask outages. 60s matches FR-OBS-007's expected sev-2 latency.</li><li>The crash-count file location (<code>/tmp/cyberos-memory-capture-crashes</code>) is intentionally NOT in the memory root — it's runtime state, not memory. /tmp is the right scope.</li><li>The <code>supervisor_event</code> rows are written via <code>MemoryWriter::emit</code> (not via the daemon's own queue) because they may need to fire AFTER the queue stops (e.g. exit path). They're synchronous with-deadline; if memory is down they're dropped (logged) — acceptable since the supervisor restart-state is also surfaced via systemd journal.</li><li>The <code>cyberos memory capture logs</code> CLI shells out to <code>journalctl</code> on Linux and <code>tail -F</code> on macOS; portable cross-platform log streaming would need a Rust journal-reader crate (<code>systemd@0.10</code>), deferred.</li><li><code>install-daemon.sh</code> and <code>uninstall-daemon.sh</code> are bash for the same reasons as FR-MEMORY-109's <code>install-hooks.sh</code>: small, calls standard tools (<code>systemctl</code>, <code>launchctl</code>, <code>sed</code>, <code>install</code>).</li><li>Tests for restart behaviour (<code>restart_e2e_test.sh</code>) take ~3 min wall-clock; gated behind CI label <code>e2e:supervision</code> to keep PR latency reasonable.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-110.</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Shipped inside modules/memory (serve.py healthz/sweeper), not a services/memory-capture crate.</p>";
