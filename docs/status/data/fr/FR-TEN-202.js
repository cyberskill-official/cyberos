window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TEN-202"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship hostile-termination override at <code>services/ten/src/hostile/</code> with CEO+CLO+CSO triple-sign + legal trigger doc + sev-1 CISO challenge window, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>hostile_trigger_kind</code> against closed enum per DEC-2411.</li></ol>\n<ol><li><strong>MUST</strong> require triple-sign at <code>triple_sign_gate.rs::can_execute(override)</code> per DEC-2410 — CEO + CLO + CSO; same-person across slots rejected.</li></ol>\n<ol><li><strong>MUST</strong> require legal trigger doc via FR-DOC-001 per DEC-2412 — case ref + brief description.</li></ol>\n<ol><li><strong>MUST</strong> cascade via fast-path FR-HR-009 termination + AUTH revocation at <code>cascade.rs::execute(override)</code> per DEC-2410 — bypass standard CEO+CFO sign (this override IS the sign).</li></ol>\n<ol><li><strong>MUST</strong> emit sev-1 CISO notification per DEC-2413 — 24h challenge window via <code>POST /v1/ten/hostile-overrides/{id}/challenge</code>.</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0011</code>: ``<code>sql CREATE TABLE ten_hostile_overrides ( override_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, subject_member_id UUID NOT NULL, trigger_kind TEXT NOT NULL CHECK (trigger_kind IN ('data_exfil_evidence','harassment_violation','criminal_charge','immediate_threat','regulatory_demand')), legal_doc_id UUID NOT NULL,  -- FR-DOC-001 ref case_ref TEXT NOT NULL, brief_description TEXT NOT NULL, ceo_signed_by UUID, ceo_signed_at TIMESTAMPTZ, clo_signed_by UUID, clo_signed_at TIMESTAMPTZ, cso_signed_by UUID, cso_signed_at TIMESTAMPTZ, status TEXT NOT NULL DEFAULT 'initiated' CHECK (status IN ('initiated','triple_signed','executed','challenged','reversed','failed')), executed_at TIMESTAMPTZ, ciso_challenge_deadline TIMESTAMPTZ, challenged_by UUID, challenged_reason TEXT, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); ALTER TABLE ten_hostile_overrides ENABLE ROW LEVEL SECURITY; CREATE POLICY hostile_rls ON ten_hostile_overrides USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON ten_hostile_overrides FROM cyberos_app; GRANT UPDATE (status, ceo_signed_by, ceo_signed_at, clo_signed_by, clo_signed_at, cso_signed_by, cso_signed_at, executed_at, ciso_challenge_deadline, challenged_by, challenged_reason) ON ten_hostile_overrides TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/ten/hostile-overrides                 (CEO/CLO/CSO initiates) POST /v1/ten/hostile-overrides/{id}/ceo-sign POST /v1/ten/hostile-overrides/{id}/clo-sign POST /v1/ten/hostile-overrides/{id}/cso-sign POST /v1/ten/hostile-overrides/{id}/execute    (auto on triple-sign) POST /v1/ten/hostile-overrides/{id}/challenge  (CISO within 24h) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-2414. PII per FR-MEMORY-111: brief_description SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from initiate → sign → execute → challenge → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> execute without triple-sign per DEC-2410.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip legal doc per DEC-2412 (FR-DOC-001 FK enforced).</li></ol>\n<ol><li><strong>MUST NOT</strong> bypass CISO notification per DEC-2413.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why triple-sign (DEC-2410)?</strong> Standard offboarding requires CEO+CFO; hostile fast-track substitutes CFO for CLO+CSO (legal + security). Even stronger gate.</p>\n<p><strong>Why legal doc requirement (DEC-2412)?</strong> Audit defense — \"why was this person force-terminated?\" must have case ref + description in writable doc.</p>\n<p><strong>Why CISO challenge (DEC-2413)?</strong> Prevent abuse (e.g. CEO+CLO+CSO collude to fire whistleblower). CISO can reverse within 24h.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample override:</p>\n<pre><code class=\"language-json\">POST /v1/ten/hostile-overrides\n{\n  \"subject_member_id\": \"uuid\",\n  \"trigger_kind\": \"data_exfil_evidence\",\n  \"legal_doc_id\": \"uuid-case-file-pdf\",\n  \"case_ref\": \"INC-2026-042\",\n  \"brief_description\": \"Forensic evidence of mass S3 download to external account\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>hostile_trigger_kind enum cardinality 5</strong>. 2. <strong>CEO+CLO+CSO triple-sign</strong>. 3. <strong>Same-person across slots rejected</strong>. 4. <strong>Legal doc required (FK)</strong>. 5. <strong>case_ref + brief_description required</strong>. 6. <strong>Cascade to FR-HR-009 fast-path</strong>. 7. <strong>AUTH revocation immediate</strong>. 8. <strong>sev-1 memory audit + CISO email</strong>. 9. <strong>24h CISO challenge window</strong>. 10. <strong>Challenge reverses + restores access</strong>. 11. <strong>5 memory audit kinds emitted</strong>. 12. <strong>PII scrubbed (description SHA256)</strong>. 13. <strong>RLS denies cross-tenant</strong>. 14. <strong>Trace_id preserved</strong>. 15. <strong>Append-only via REVOKE except status cols</strong>. 16. <strong>CEO/CLO/CSO + CISO role gates</strong>. 17. <strong>Status workflow enforced</strong>. 18. <strong>Reversal restores grants + access</strong>. 19. <strong>Override doesn't bypass labor law (CLO ensures)</strong>. 20. <strong>Audit log accessible to board</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn triple_sign_required() {\n    let ctx = TestContext::with_legal_doc().await;\n    let r = ctx.init_hostile(ctx.member, \"data_exfil_evidence\", ctx.doc_id).await;\n    ctx.ceo_sign(r.id).await;\n    ctx.clo_sign(r.id).await;\n    let try_exec = ctx.try_execute(r.id).await;\n    assert!(try_exec.is_err());  // CSO missing\n    ctx.cso_sign(r.id).await;\n    let exec = ctx.execute(r.id).await;\n    assert!(exec.is_ok());\n}\n\n#[tokio::test]\nasync fn legal_doc_required() {\n    let ctx = TestContext::with_member().await;\n    let r = ctx.try_init_hostile_no_doc(ctx.member).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn ciso_challenge_reverses() {\n    let ctx = TestContext::with_executed_override().await;\n    ctx.ciso_challenge(ctx.override_id, \"investigation incomplete\").await;\n    let o = ctx.fetch_override(ctx.override_id).await;\n    assert_eq!(o.status, \"challenged\");\n    let member = ctx.fetch_member(ctx.subject_member_id).await;\n    assert_eq!(member.status, \"active\");  // restored\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-TEN-104. <strong>Cross-module:</strong> FR-HR-009 (termination cascade), FR-AUTH-101 (roles), FR-DOC-001 (legal doc), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>One sig missing</td><td>gate</td><td>reject exec</td><td>get sig</td></tr><tr><td>Same-person dual</td><td>validate</td><td>403</td><td>different signer</td></tr><tr><td>Legal doc missing</td><td>FK</td><td>reject</td><td>upload doc</td></tr><tr><td>HR-009 cascade fails</td><td>rollback</td><td>sev-1</td><td>manual fix</td></tr><tr><td>AUTH revoke fails</td><td>sev-1</td><td>retry</td><td>manual revoke</td></tr><tr><td>Cross-tenant override</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>CISO not notified</td><td>sev-1 alert</td><td>inherent</td><td>escalate</td></tr><tr><td>Challenge past 24h</td><td>reject</td><td>inherent</td><td>new override for re-fire</td></tr><tr><td>Concurrent sign</td><td>UPDATE WHERE</td><td>first wins</td><td>inherent</td></tr><tr><td>Reversal mid-execute</td><td>sev-1</td><td>manual restore</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Triple-sign sequence: CEO + CLO + CSO; CISO not in sign chain (challenger role separate).</li><li>§11.2 Cascade uses FR-HR-009 internal API with <code>bypass_standard_co_sign=true</code> flag.</li><li>§11.3 memory audit body: override_id, trigger_kind, status; description SHA256.</li><li>§11.4 CISO email via FR-EMAIL-009 with override URL + 24h countdown.</li><li>§11.5 Reversal restores HR-009 termination + AUTH access; ESOP not reversed (out of scope — board decides).</li></ul>\n<hr>\n<p><em>End of FR-TEN-202 spec.</em></p>";
