window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AI-104"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI service <strong>MUST</strong> ship VN provider integration at <code>services/ai/src/providers/vn/</code> adding Viettel + FPT to FR-AI-016 region set, failover, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>vn_provider</code> against closed enum per DEC-2381.</li></ol>\n<ol><li><strong>MUST</strong> add to FR-AI-016 region set per DEC-2380 — modify <code>residency_resolver.rs</code> to include Viettel + FPT region strings for <code>Vn1</code>.</li></ol>\n<ol><li><strong>MUST</strong> dispatch at <code>vn/mod.rs::dispatch(tenant, request)</code> with failover per DEC-2383:</li></ol>\n<ul><li>Primary: Viettel; on 5xx/timeout → FPT</li><li>Both down → return <code>vn1_provider_outage</code> (distinct from <code>vn1_no_provider_yet</code>)</li></ul>\n<ol><li><strong>MUST</strong> store creds in KMS per DEC-2382 (CTO-only).</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0010</code>: ``<code>sql CREATE TABLE ai_vn_provider_creds ( tenant_id UUID NOT NULL, provider TEXT NOT NULL CHECK (provider IN ('viettel_cloud','fpt_cloud')), encrypted_creds_arn TEXT NOT NULL, api_account_id TEXT, active BOOLEAN NOT NULL DEFAULT true, set_by UUID NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY (tenant_id, provider) ); ALTER TABLE ai_vn_provider_creds ENABLE ROW LEVEL SECURITY; CREATE POLICY vn_creds_rls ON ai_vn_provider_creds USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); GRANT UPDATE (encrypted_creds_arn, api_account_id, active, set_by, updated_at) ON ai_vn_provider_creds TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text PUT /v1/ai/vn-providers/{provider}/creds      (CTO-only) GET /v1/ai/vn-providers/health                 (per-provider status) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2384. PII per FR-MEMORY-111: prompts hashed at FR-AI-006 layer (not duplicated here).</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from request → dispatcher → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> silently fall back outside VN per DEC-2380 (preserves regulatory contract).</li></ol>\n<ol><li><strong>MUST NOT</strong> allow non-CTO creds write per DEC-2382.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Viettel + FPT (DEC-2380)?</strong> Major VN cloud providers with data centers in VN; both serve LLM/embedding via partner agreements.</p>\n<p><strong>Why failover (DEC-2383)?</strong> Either provider individually unreliable; pair gives 99.5% combined uptime.</p>\n<p><strong>Why CTO creds (DEC-2382)?</strong> Provider integration involves contract terms + billing; CTO authority.</p>\n<p><strong>Why preserve refusal contract (DEC-2380)?</strong> FR-AI-016 <code>vn1_no_provider_yet</code> becomes <code>vn1_provider_outage</code> after integration — both-down case still refused, never silently routed elsewhere.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample provider health:</p>\n<pre><code class=\"language-json\">{\n  \"providers\": [\n    {\"provider\": \"viettel_cloud\", \"active\": true, \"status\": \"healthy\"},\n    {\"provider\": \"fpt_cloud\", \"active\": true, \"status\": \"healthy\"}\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>vn_provider enum cardinality 2</strong>. 2. <strong>FR-AI-016 region set extended</strong>. 3. <strong>Viettel primary</strong>. 4. <strong>FPT failover on Viettel 5xx</strong>. 5. <strong>Both down → refusal (not silent reroute)</strong>. 6. <strong>Refusal code <code>vn1_provider_outage</code> distinct from <code>vn1_no_provider_yet</code></strong>. 7. <strong>CTO-only creds</strong>. 8. <strong>Creds in KMS</strong>. 9. <strong>4 memory audit kinds emitted</strong>. 10. <strong>PII via FR-AI-006</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>Per-provider active flag</strong>. 14. <strong>Health endpoint shows status</strong>. 15. <strong>Append-only via REVOKE except status cols</strong>. 16. <strong>Failover latency &lt; 500ms</strong>. 17. <strong>Both-provider creds optional (single OK if other unavailable)</strong>. 18. <strong>Contract terms documented per provider</strong>. 19. <strong>Inactive provider skipped in dispatch</strong>. 20. <strong>Cross-tenant cred isolation</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn viettel_primary_dispatch() {\n    let ctx = TestContext::with_vn_creds_both().await;\n    let r = ctx.invoke(\"hello\", \"vn-1\").await;\n    assert!(r.dispatched_to == \"viettel_cloud\");\n}\n\n#[tokio::test]\nasync fn fpt_failover_on_viettel_5xx() {\n    let ctx = TestContext::with_vn_creds_both().await;\n    ctx.mock_viettel_500().await;\n    let r = ctx.invoke(\"hello\", \"vn-1\").await;\n    assert!(r.dispatched_to == \"fpt_cloud\");\n    let audits = ctx.fetch_memory_audits(\"ai.vn_provider_failover\").await;\n    assert!(!audits.is_empty());\n}\n\n#[tokio::test]\nasync fn both_down_refusal() {\n    let ctx = TestContext::with_vn_creds_both().await;\n    ctx.mock_viettel_500().await;\n    ctx.mock_fpt_500().await;\n    let r = ctx.try_invoke(\"hello\", \"vn-1\").await;\n    assert!(r.is_err());\n    assert!(r.error_code() == \"vn1_provider_outage\");\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-AI-016. <strong>Cross-module:</strong> FR-AI-006 (provider abstraction), FR-AUTH-105 (KMS), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Viettel API down</td><td>client err</td><td>failover to FPT</td><td>inherent</td></tr><tr><td>FPT API down</td><td>client err</td><td>both-down refusal</td><td>inherent</td></tr><tr><td>Creds expired</td><td>401</td><td>sev-1; failover or refuse</td><td>CTO rotate</td></tr><tr><td>Cross-tenant cred</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Provider deprecates API v1</td><td>per-client version</td><td>upgrade required</td><td>maintenance</td></tr><tr><td>Network partition to VN</td><td>timeout</td><td>refusal</td><td>inherent</td></tr><tr><td>Both providers concurrent quota</td><td>rate-limit</td><td>refusal</td><td>upgrade</td></tr><tr><td>Inactive provider with active flag false</td><td>skip</td><td>inherent</td><td>inherent</td></tr><tr><td>Concurrent dispatch</td><td>inherent</td><td>each isolated</td><td>inherent</td></tr><tr><td>Decimal precision N/A</td><td>inherent</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Viettel + FPT REST APIs; auth via API key + tenant-scoped quota.</li><li>§11.2 Failover threshold: 1 attempt at primary (no retry); immediate fall to secondary.</li><li>§11.3 memory audit body: tenant_id, provider, status; prompts hashed via FR-AI-006.</li><li>§11.4 Future: add VNG Cloud or other VN providers via enum extension.</li><li>§11.5 Per-tenant active flags allow CTO to disable a provider without removing creds.</li></ul>\n<hr>\n<p><em>End of FR-AI-104 spec.</em></p>";
