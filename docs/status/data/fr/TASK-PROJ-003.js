window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The collaborative-editing layer <strong>MUST</strong> use Yjs CRDT for rich-text fields and LWW for scalar metadata. The contract:</p>\n<ol><li><strong>MUST</strong> use Y.Doc to represent each issue. Specifically:</li></ol>\n<ul><li><code>Y.Text(\"description\")</code> — issue description.</li><li><code>Y.Array(\"comments\")</code> of <code>Y.Map { id: string, author_id: uuid, body: Y.Text, created_at: number }</code>.</li><li>Scalars (title, status, priority, assignee_id, cycle_id, estimate, labels) are NOT in Y.Doc; they're plain Postgres columns with LWW.</li></ul>\n<ol><li><strong>MUST</strong> persist Y.Doc state as snapshots in Postgres <code>yjs_state_snapshots</code> (one row per snapshot version per issue):</li></ol>\n<ul><li>Snapshot every 60s OR on graceful disconnect of last connected client.</li><li>Snapshot bytes = <code>Y.encodeStateAsUpdate(doc)</code>; compressed with <code>zstd</code> (level 6); typical &lt; 10 KB after 1000 edits.</li><li>Retain last 50 snapshots OR last 7 days (whichever is fewer per doc).</li></ul>\n<ol><li><strong>MUST</strong> restore Y.Doc on doc open:</li></ol>\n<ul><li>Read latest snapshot from <code>yjs_state_snapshots</code> → <code>Y.applyUpdate(doc, snapshot)</code>.</li><li>Read all binlog deltas since snapshot's <code>created_at</code> from <code>yjs_update_log</code> → apply.</li><li>Send initial sync to connecting client.</li></ul>\n<ol><li><strong>MUST</strong> be a transparent relay: the Rust server's role is forwarding <code>update</code> messages between connected clients of the same <code>doc_id</code>. It does NOT merge, does NOT authoritatively interpret CRDT operations. CRDT correctness is client-side per Yjs semantics.</li><li><strong>MUST</strong> authenticate WebSocket connections via JWT (TASK-AUTH-004); enforce RLS on doc_id (TASK-AUTH-003 — only tenant members can subscribe). Invalid auth → close with code 4001; tenant mismatch → close with 4003.</li><li><strong>MUST</strong> apply scalar LWW per field:</li></ol>\n<ul><li>Each scalar field has companion columns <code>&lt;field&gt;_updated_at_ns</code> (i64, unix ns) + <code>&lt;field&gt;_updated_by_subject_id</code> (uuid).</li><li>On write: if <code>incoming_updated_at_ns &gt; stored_updated_at_ns</code>, accept; else reject with <code>409 STALE_WRITE</code> and return current state for client to reconcile.</li><li>Tie-break on equal timestamps: lexicographic on <code>updated_by_subject_id</code> (deterministic).</li></ul>\n<ol><li><strong>MUST</strong> track awareness state (per-user cursor + selection + presence):</li></ol>\n<ul><li>Sent via Yjs Awareness Protocol on a separate channel within the same WebSocket.</li><li>Awareness state expires 30s after last heartbeat (user disconnected without close frame).</li><li>Sent at most 30 Hz (33ms throttle) per user per doc.</li></ul>\n<ol><li><strong>MUST</strong> buffer offline edits client-side:</li></ol>\n<ul><li>When WebSocket disconnected, accumulate updates in IndexedDB-backed Y.Doc.</li><li>On reconnect: send buffered updates first; server forwards; clients converge.</li><li>Buffer size capped at 5 MB per doc; overflow → emit <code>proj.yjs_buffer_overflow</code> audit row + UI banner (\"editing offline; some changes may be lost on reconnect\").</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit rows for significant CRDT events:</li></ol>\n<ul><li><code>proj.issue_collab_session_started</code> on first client connection (when prior was 0).</li><li><code>proj.issue_collab_session_ended</code> on last disconnect.</li><li><code>proj.issue_snapshot_persisted</code> per snapshot, with <code>{doc_id, version, bytes_compressed, applies_since_last_snapshot}</code>.</li><li><code>proj.yjs_buffer_overflow</code> on client-side buffer overflow (sent on reconnect).</li><li><code>proj.scalar_stale_write_rejected</code> on LWW reject with <code>{field, incoming_ts, stored_ts, attempted_by}</code>.</li></ul>\n<ol><li><strong>MUST</strong> propagate W3C TraceContext: WebSocket handshake carries <code>traceparent</code> header; relay includes trace_id in every forwarded message metadata for OBS correlation.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_yjs_active_connections{doc_id_bucket}</code> (gauge; doc_id bucketed to prevent cardinality blow-up).</li><li><code>proj_yjs_messages_forwarded_total{kind}</code> (counter; kind ∈ update | awareness | sync-step1 | sync-step2).</li><li><code>proj_yjs_snapshot_duration_seconds</code> (histogram).</li><li><code>proj_yjs_snapshot_bytes</code> (histogram; alert if p99 &gt; 1 MB).</li><li><code>proj_yjs_lwww_conflicts_total{field}</code> (counter — operator visibility into hot conflict fields).</li></ul>\n<ol><li><strong>MUST</strong> support graceful degradation when Postgres unavailable:</li></ol>\n<ul><li>Snapshot writes buffered in memory (cap 100 MB across all docs); replayed on Postgres recovery.</li><li>Reads of <code>yjs_state_snapshots</code> failure → start fresh Y.Doc (no history); UI shows \"history unavailable\" banner.</li></ul>\n<ol><li><strong>MUST</strong> be the only path for issue.description writes. Direct Postgres updates of <code>issues.description</code> are forbidden (enforced by absence of <code>description</code> column on the canonical issue table — it's a write-through view of latest Y.Doc snapshot).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why Yjs over alternatives (§1 #1, DEC-240)?</strong> Three reasons. First, MATURITY: Yjs has been production-deployed at scale (Notion, Linear, Figma's earlier versions). Second, ECOSYSTEM: TipTap (our chosen editor) has first-class Yjs binding. Third, PERFORMANCE: Yjs's compact binary format and incremental updates outperform Automerge / Loro for our scale (issue descriptions ≤ 50 KB, ≤ 10 concurrent editors).</p>\n<p><strong>Why Y.Text only for body fields, NOT scalars (§1 #1, DEC-241)?</strong> CRDT is expensive for scalar fields — a Y.Map for <code>status: \"in_progress\"</code> carries 50+ bytes of metadata for 12 bytes of value. LWW with version vectors is simpler (one i64 + one uuid per field) and correctness is acceptable: \"the last person to click <code>In Progress</code> wins\" matches user mental model.</p>\n<p><strong>Why server is relay-only (§1 #4, DEC-243)?</strong> Yjs's correctness is CLIENT-defined: clients converge regardless of message order. A server that tries to merge introduces a second authoritative interpretation that may diverge. Relay-only server is simpler (forward bytes) and matches Yjs's design intent. Trade-off: server can't enforce content validation; we accept this since rich-text fields are user content anyway.</p>\n<p><strong>Why snapshot every 60s (§1 #2, DEC-242)?</strong> Linear scaling: snapshot every-edit = Postgres write storm; snapshot once-per-day = 24h replay on reconnect (slow). 60s is the calibrated balance: ~3 MB/day write volume per doc, &lt; 1s replay budget.</p>\n<p><strong>Why scalar LWW with <code>_updated_at_ns</code> (§1 #6)?</strong> Three properties: (a) DETERMINISTIC: same timestamps + same tie-breaker = same outcome on every replica; (b) STALE DETECTION: clients can refuse stale local writes against server's view; (c) AUDITABLE: companion column <code>_updated_by_subject_id</code> is the \"who\" — clear in memory audit.</p>\n<p><strong>Why 30 Hz awareness throttle (§1 #7)?</strong> Mouse-cursor at 60 Hz overwhelms WebSocket → bursty traffic. 30 Hz = ~33ms granularity; human eye perceives 30 Hz as smooth. Heartbeat at 30 Hz keeps presence \"alive\" without flooding.</p>\n<p><strong>Why offline buffer cap 5 MB (§1 #8)?</strong> A user editing offline for an hour accumulates ~500 KB of CRDT operations on a heavy document. 5 MB covers ~10 hours of offline work — generous. Beyond that, the buffer overflow signals \"you really should reconnect\"; we degrade gracefully (newer edits drop) and emit an audit row.</p>\n<p><strong>Why audit start/end + snapshots (§1 #9)?</strong> Operators investigating \"who edited issue X last week\" need a session trail. Snapshot rows give time-series of edit volume per issue (forensics + product analytics).</p>\n<p><strong>Why W3C trace_id in WebSocket (§1 #10)?</strong> Operators tracing \"user clicked Save → server processed → memory row appeared\" need correlation. WebSocket sessions are long-lived; without trace propagation, every CRDT operation is orphaned in OBS.</p>\n<p><strong>Why Postgres-down degrades to memory buffer (§1 #12)?</strong> Editing must not block on database availability. 100 MB in-memory cap covers ~5 minutes of typical edit volume across all active docs; longer outages emit sev-1.</p>\n<p><strong>Why direct description writes forbidden (§1 #13)?</strong> Two paths = drift risk. A naive REST endpoint that writes <code>issues.description = '...'</code> would bypass CRDT, lose concurrent edits, and corrupt the Y.Doc state. The canonical issue table has no <code>description</code> column; description is materialised from latest snapshot via view.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Schema</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0003_yjs_state_snapshots.sql\n\nCREATE TABLE yjs_state_snapshots (\n    doc_id          TEXT NOT NULL,\n    version         BIGINT NOT NULL,\n    snapshot_bytes  BYTEA NOT NULL,\n    applies_count   INT NOT NULL,\n    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    tenant_id       UUID NOT NULL,\n    PRIMARY KEY (doc_id, version)\n);\nCREATE INDEX idx_yjs_state_snapshots_recent ON yjs_state_snapshots (doc_id, version DESC);\n\nCREATE TABLE yjs_update_log (\n    doc_id          TEXT NOT NULL,\n    seq             BIGSERIAL NOT NULL,\n    update_bytes    BYTEA NOT NULL,\n    applied_by_subject_id UUID NOT NULL,\n    applied_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    tenant_id       UUID NOT NULL,\n    PRIMARY KEY (doc_id, seq)\n);\nCREATE INDEX idx_yjs_update_log_replay ON yjs_update_log (doc_id, applied_at);\n\n-- LWW companion columns on issues table\nALTER TABLE issues\n    ADD COLUMN title_updated_at_ns BIGINT NOT NULL DEFAULT 0,\n    ADD COLUMN title_updated_by_subject_id UUID,\n    ADD COLUMN status_updated_at_ns BIGINT NOT NULL DEFAULT 0,\n    ADD COLUMN status_updated_by_subject_id UUID,\n    ADD COLUMN assignee_updated_at_ns BIGINT NOT NULL DEFAULT 0,\n    ADD COLUMN assignee_updated_by_subject_id UUID;\n\n-- RLS policy\nCREATE POLICY yjs_state_tenant_isolation ON yjs_state_snapshots\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Rust relay</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/yjs_relay.rs\nuse yrs::sync::{Awareness, AwarenessUpdate};\nuse yrs::Doc;\nuse tokio::sync::broadcast;\nuse std::sync::Arc;\n\npub struct DocRoom {\n    pub doc_id:    String,\n    pub doc:       Arc&lt;tokio::sync::RwLock&lt;Doc&gt;&gt;,\n    pub awareness: Arc&lt;tokio::sync::RwLock&lt;Awareness&gt;&gt;,\n    /// broadcast channel: forwards every received message to all connected clients\n    pub broadcast: broadcast::Sender&lt;Message&gt;,\n}\n\n#[derive(Clone, Debug)]\npub enum Message {\n    SyncStep1(Vec&lt;u8&gt;),                  // client requests state vector\n    SyncStep2(Vec&lt;u8&gt;),                  // server / peer sends missing updates\n    Update(Vec&lt;u8&gt;),                     // CRDT delta\n    Awareness(AwarenessUpdate),\n}\n\npub async fn handle_ws_connection(\n    ws: axum::extract::ws::WebSocket,\n    doc_id: String,\n    subject_id: uuid::Uuid,\n    tenant_id: uuid::Uuid,\n    room: Arc&lt;DocRoom&gt;,\n) {\n    let (mut sender, mut receiver) = ws.split();\n    let mut rx = room.broadcast.subscribe();\n\n    // 1. Send initial state to new client\n    let doc = room.doc.read().await;\n    let state_vec = yrs::encode_state_as_update_v1(&amp;doc, &amp;yrs::StateVector::default());\n    drop(doc);\n    let _ = sender.send(axum::extract::ws::Message::Binary(state_vec.into())).await;\n\n    // 2. Forward incoming + broadcast outgoing\n    loop {\n        tokio::select! {\n            // Incoming from this client\n            Some(Ok(msg)) = receiver.next() =&gt; {\n                if let axum::extract::ws::Message::Binary(bytes) = msg {\n                    let parsed = parse_y_protocol(&amp;bytes);\n                    match parsed {\n                        Some(Message::Update(update_bytes)) =&gt; {\n                            // Apply to room's Doc (so reconnections see latest)\n                            let mut doc = room.doc.write().await;\n                            let _ = yrs::apply_update_v1(&amp;doc, &amp;update_bytes);\n                            drop(doc);\n                            // Persist to update_log (async background task)\n                            let _ = state_persistence::log_update(&amp;room.doc_id, subject_id, tenant_id, &amp;update_bytes).await;\n                            // Broadcast to all other clients\n                            let _ = room.broadcast.send(Message::Update(update_bytes));\n                        }\n                        Some(Message::Awareness(aw)) =&gt; {\n                            let mut awareness = room.awareness.write().await;\n                            awareness.apply_update(aw.clone());\n                            drop(awareness);\n                            let _ = room.broadcast.send(Message::Awareness(aw));\n                        }\n                        Some(Message::SyncStep1(state_vec)) =&gt; {\n                            // client asking for missing updates\n                            let doc = room.doc.read().await;\n                            let missing = yrs::encode_state_as_update_v1(&amp;doc, &amp;state_vec);\n                            drop(doc);\n                            let _ = sender.send(axum::extract::ws::Message::Binary(missing.into())).await;\n                        }\n                        _ =&gt; {}\n                    }\n                }\n            }\n            // Outgoing from broadcast\n            Ok(msg) = rx.recv() =&gt; {\n                let bytes = serialise_message(&amp;msg);\n                let _ = sender.send(axum::extract::ws::Message::Binary(bytes.into())).await;\n            }\n        }\n    }\n}</code></pre>\n<h3>Snapshot scheduler</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/snapshot.rs\nuse std::time::Duration;\nuse tokio::time::interval;\n\npub async fn snapshot_loop(rooms: Arc&lt;RoomRegistry&gt;) {\n    let mut ticker = interval(Duration::from_secs(60));\n    loop {\n        ticker.tick().await;\n        for room in rooms.iter().await {\n            let start = std::time::Instant::now();\n            let doc = room.doc.read().await;\n            let snapshot = yrs::encode_state_as_update_v1(&amp;doc, &amp;yrs::StateVector::default());\n            drop(doc);\n            let compressed = zstd::encode_all(snapshot.as_slice(), 6).expect(\"zstd\");\n            let applies_count = room.applies_since_snapshot.swap(0, std::sync::atomic::Ordering::Relaxed);\n\n            let res = sqlx::query!(\n                \"INSERT INTO yjs_state_snapshots (doc_id, version, snapshot_bytes, applies_count, tenant_id)\n                 VALUES ($1, $2, $3, $4, $5)\",\n                room.doc_id, room.next_version().await, compressed, applies_count, room.tenant_id,\n            ).execute(&amp;pool).await;\n\n            metrics::histogram!(\"proj_yjs_snapshot_duration_seconds\").record(start.elapsed().as_secs_f64());\n            metrics::histogram!(\"proj_yjs_snapshot_bytes\").record(compressed.len() as f64);\n\n            match res {\n                Ok(_) =&gt; {\n                    emit_memory_row(\"proj.issue_snapshot_persisted\", serde_json::json!({\n                        \"doc_id\": room.doc_id,\n                        \"version\": room.current_version().await,\n                        \"bytes_compressed\": compressed.len(),\n                        \"applies_since_last_snapshot\": applies_count,\n                    })).await;\n                    prune_old_snapshots(&amp;room.doc_id).await;\n                }\n                Err(e) =&gt; {\n                    tracing::error!(doc_id = %room.doc_id, ?e, \"snapshot persist failed; buffering\");\n                    buffer::push(room.doc_id.clone(), compressed).await;\n                }\n            }\n        }\n    }\n}</code></pre>\n<h3>TypeScript client</h3>\n<pre><code class=\"language-typescript\">// web/proj-client/src/collab/YjsProvider.ts\nimport * as Y from 'yjs';\nimport { WebsocketProvider } from 'y-websocket';\nimport { IndexeddbPersistence } from 'y-indexeddb';\n\nexport interface YjsConfig {\n  docId:    string;\n  wsUrl:    string;          // wss://proj-sync.cyberos.world\n  jwt:      string;          // TASK-AUTH-004 access token\n  onStatus: (s: 'connecting'|'connected'|'syncing'|'offline') =&gt; void;\n}\n\nexport class YjsProvider {\n  doc:       Y.Doc;\n  awareness: any;\n  private wsProvider: WebsocketProvider;\n  private idb:        IndexeddbPersistence;\n\n  constructor(cfg: YjsConfig) {\n    this.doc = new Y.Doc();\n    this.idb = new IndexeddbPersistence(`proj-${cfg.docId}`, this.doc);   // offline buffer\n    this.wsProvider = new WebsocketProvider(cfg.wsUrl, cfg.docId, this.doc, {\n      params: { token: cfg.jwt },                 // server reads token from URL query\n      connect: true,\n      maxBackoffTime: 5000,\n    });\n    this.awareness = this.wsProvider.awareness;\n\n    this.wsProvider.on('status', ({ status }: any) =&gt; {\n      cfg.onStatus(status === 'connected' ? 'connected' : 'connecting');\n    });\n    this.wsProvider.on('sync', (synced: boolean) =&gt; {\n      cfg.onStatus(synced ? 'connected' : 'syncing');\n    });\n\n    // Offline detection\n    window.addEventListener('online',  () =&gt; this.wsProvider.connect());\n    window.addEventListener('offline', () =&gt; cfg.onStatus('offline'));\n  }\n\n  getText(field: string): Y.Text {\n    return this.doc.getText(field);\n  }\n\n  destroy() {\n    this.wsProvider.destroy();\n    this.idb.destroy();\n    this.doc.destroy();\n  }\n}</code></pre>\n<pre><code class=\"language-typescript\">// web/proj-client/src/collab/ScalarLWW.ts\nexport interface ScalarFieldUpdate&lt;T&gt; {\n  field:        string;\n  value:        T;\n  updated_at_ns: bigint;\n  updated_by_subject_id: string;\n}\n\nexport async function writeScalarLWW&lt;T&gt;(\n  issueId: string,\n  field:   string,\n  value:   T,\n  jwt:     string,\n): Promise&lt;{ accepted: true } | { accepted: false; current: T; reason: 'stale_write' }&gt;\n{\n  const now = BigInt(Date.now()) * 1_000_000n;  // ns\n  const subject = decodeJwtSubject(jwt);\n  const resp = await fetch(`/api/proj/issues/${issueId}/${field}`, {\n    method: 'PATCH',\n    headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${jwt}` },\n    body: JSON.stringify({ value, updated_at_ns: String(now), updated_by_subject_id: subject }),\n  });\n  if (resp.status === 409) {\n    const body = await resp.json();\n    return { accepted: false, current: body.current, reason: 'stale_write' };\n  }\n  return { accepted: true };\n}</code></pre>\n<h3>LWW backend handler</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/scalar_handlers.rs\npub async fn patch_scalar(\n    Path((issue_id, field)): Path&lt;(uuid::Uuid, String)&gt;,\n    State(pool): State&lt;sqlx::PgPool&gt;,\n    Json(req): Json&lt;ScalarPatchReq&gt;,\n) -&gt; Result&lt;(), (StatusCode, Json&lt;serde_json::Value&gt;)&gt; {\n    // Whitelist allowed fields\n    let allowed = [\"title\", \"status\", \"priority\", \"assignee_id\", \"cycle_id\", \"estimate\"];\n    if !allowed.contains(&amp;field.as_str()) {\n        return Err((StatusCode::BAD_REQUEST, Json(json!({\"error\": \"unknown_field\"}))));\n    }\n    // LWW: compare against stored timestamp\n    let stored: (i64, Option&lt;uuid::Uuid&gt;) = sqlx::query_as(&amp;format!(\n        \"SELECT {field}_updated_at_ns, {field}_updated_by_subject_id FROM issues WHERE id = $1\"\n    ))\n    .bind(issue_id).fetch_one(&amp;pool).await.unwrap();\n\n    if req.updated_at_ns &lt; stored.0\n       || (req.updated_at_ns == stored.0 &amp;&amp; req.updated_by_subject_id &lt; stored.1.unwrap_or_default())\n    {\n        // Stale write\n        let current: serde_json::Value = fetch_current(&amp;pool, issue_id, &amp;field).await;\n        emit_memory_row(\"proj.scalar_stale_write_rejected\", json!({\n            \"issue_id\": issue_id, \"field\": field,\n            \"incoming_ts\": req.updated_at_ns, \"stored_ts\": stored.0,\n            \"attempted_by\": req.updated_by_subject_id,\n        })).await;\n        metrics::counter!(\"proj_yjs_lwww_conflicts_total\", \"field\" =&gt; field.clone()).increment(1);\n        return Err((StatusCode::CONFLICT, Json(json!({\n            \"error\": \"stale_write\", \"current\": current\n        }))));\n    }\n    // Accept\n    sqlx::query(&amp;format!(\n        \"UPDATE issues SET {field} = $1, {field}_updated_at_ns = $2, {field}_updated_by_subject_id = $3 WHERE id = $4\"\n    ))\n    .bind(req.value).bind(req.updated_at_ns).bind(req.updated_by_subject_id).bind(issue_id)\n    .execute(&amp;pool).await.unwrap();\n    Ok(())\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Two clients edit description concurrently → both converge</strong> — Alice types \"X\" at offset 0; Bob types \"Y\" at offset 0; both clients end up with same text (e.g. \"XY\" or \"YX\" deterministically per Yjs).</li><li><strong>Comments are CRDT</strong> — Alice adds comment, Bob adds comment simultaneously → both clients see both comments in eventually-consistent order.</li><li><strong>Scalar LWW: later timestamp wins</strong> — Alice sets status=done at T=100; Bob sets status=in_progress at T=200 → final status=in_progress.</li><li><strong>Scalar LWW: stale write rejected</strong> — Bob still has status=todo (from T=50); writes status=cancelled at T=50 (his clock is behind) → server returns 409 with current state.</li><li><strong>Scalar LWW: tie-break deterministic</strong> — Alice + Bob both write at T=100; Alice subject_id \"a...\" &lt; Bob subject_id \"b...\" → Bob wins (lexicographically greater).</li><li><strong>Snapshot every 60s</strong> — fixture: 60 updates over 60s; assert exactly 1 snapshot row added to <code>yjs_state_snapshots</code>.</li><li><strong>Snapshot bytes compressed</strong> — empirical: snapshot &lt; 10 KB for issue with 1000 edits.</li><li><strong>Reconnect loads latest snapshot + binlog deltas</strong> — disconnect; add 50 binlog updates; reconnect → client sees state == server's current Y.Doc.</li><li><strong>Old snapshots pruned</strong> — &gt; 50 snapshots OR &gt; 7 days → oldest deleted; <code>proj_yjs_snapshots_pruned_total</code> increments.</li><li><strong>JWT-authenticated WebSocket</strong> — connect without <code>?token=</code> → close code 4001.</li><li><strong>Tenant mismatch → 4003</strong> — JWT for tenant A connects to doc owned by tenant B → close code 4003.</li><li><strong>Awareness state expires 30s</strong> — disconnect without close frame → other clients see presence indicator vanish after 30s.</li><li><strong>Awareness throttled at 30 Hz</strong> — fixture sends 100 cursor updates in 1s; server forwards ≤ 30.</li><li><strong>Offline edit + reconnect</strong> — disconnect; edit description; reconnect → server applies the buffered updates; all clients converge.</li><li><strong>Buffer overflow</strong> — &gt; 5 MB of offline edits → client emits <code>proj.yjs_buffer_overflow</code> on reconnect; UI banner shown.</li><li><strong>memory audit: session_started</strong> — first client connects → <code>proj.issue_collab_session_started</code> row.</li><li><strong>memory audit: session_ended</strong> — last client disconnects → <code>proj.issue_collab_session_ended</code> row.</li><li><strong>memory audit: snapshot_persisted</strong> — snapshot fires → row with <code>bytes_compressed</code>, <code>applies_since_last_snapshot</code>.</li><li><strong>memory audit: scalar_stale_write_rejected</strong> — LWW reject → row with <code>incoming_ts</code>, <code>stored_ts</code>.</li><li><strong>OTel: messages_forwarded counter</strong> — relay forwards 100 updates → counter at 100.</li><li><strong>OTel: snapshot_bytes histogram</strong> — large doc (50 KB) → snapshot recorded; alert at &gt; 1 MB.</li><li><strong>OTel: lww_conflicts_total per field</strong> — frequent assignee conflicts → operator sees <code>field=\"assignee_id\"</code> is the hottest.</li><li><strong>W3C trace propagation</strong> — handshake carries <code>traceparent</code>; relay logs include trace_id; OBS dashboard correlates.</li><li><strong>Postgres down: in-memory buffer</strong> — kill Postgres; clients continue editing; snapshots buffer; Postgres up → buffered snapshots flushed.</li><li><strong>Direct description writes forbidden</strong> — REST endpoint <code>PATCH /issues/:id description=X</code> → 404 / 405 (field not exposed).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/proj-sync/tests/yjs_relay_test.rs\n\n#[tokio::test]\nasync fn two_clients_converge_concurrent_edits() {\n    let env = TestEnv::new().await;\n    let alice = env.connect_as(\"alice\").await;\n    let bob   = env.connect_as(\"bob\").await;\n\n    // Alice + Bob both edit \"description\" at the same offset\n    alice.send_update(insert_text(\"description\", 0, \"Alice\")).await;\n    bob.send_update(insert_text(\"description\", 0, \"Bob\")).await;\n    tokio::time::sleep(Duration::from_millis(100)).await;\n\n    let a_text = alice.read_text(\"description\").await;\n    let b_text = bob.read_text(\"description\").await;\n    assert_eq!(a_text, b_text);    // converged\n}\n\n#[tokio::test]\nasync fn snapshot_every_60s() {\n    let env = TestEnv::with_paused_time().await;\n    let alice = env.connect_as(\"alice\").await;\n    for i in 0..60 { alice.send_update(insert_text(\"description\", 0, &amp;format!(\"{i}\"))).await; }\n    tokio::time::advance(Duration::from_secs(61)).await;\n    let count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM yjs_state_snapshots\")\n        .fetch_one(&amp;env.pool).await.unwrap();\n    assert_eq!(count, 1);\n}\n\n#[tokio::test]\nasync fn reconnect_loads_latest_snapshot() {\n    let env = TestEnv::new().await;\n    let alice = env.connect_as(\"alice\").await;\n    alice.send_update(insert_text(\"description\", 0, \"before disconnect\")).await;\n    env.force_snapshot().await;\n    alice.send_update(insert_text(\"description\", 17, \" ; more updates\")).await;\n    alice.disconnect().await;\n\n    let alice2 = env.connect_as(\"alice\").await;\n    let text = alice2.read_text(\"description\").await;\n    assert_eq!(text, \"before disconnect ; more updates\");\n}\n\n#[tokio::test]\nasync fn jwt_required() {\n    let env = TestEnv::new().await;\n    let result = env.try_connect_without_jwt().await;\n    assert!(matches!(result, Err(WsCloseCode(4001))));\n}\n\n#[tokio::test]\nasync fn tenant_mismatch_rejected() {\n    let env = TestEnv::new().await;\n    let bob_other_tenant = env.connect_as_other_tenant(\"bob\").await;\n    assert!(matches!(bob_other_tenant, Err(WsCloseCode(4003))));\n}</code></pre>\n<pre><code class=\"language-typescript\">// web/proj-client/tests/yjs_provider_test.ts\n\ntest('offline edit then reconnect converges', async () =&gt; {\n  const provider = new YjsProvider({ docId: 'iss-123', wsUrl, jwt, onStatus: noop });\n  await provider.waitConnected();\n  provider.getText('description').insert(0, 'before');\n  await provider.waitSync();\n\n  // Simulate offline\n  provider.wsProvider.disconnect();\n  provider.getText('description').insert(6, ' offline');\n  expect(provider.getText('description').toString()).toBe('before offline');\n\n  // Reconnect\n  provider.wsProvider.connect();\n  await provider.waitSync();\n  // Server should now have the offline edit\n  const serverDoc = await fetchServerState('iss-123');\n  expect(Y.encodeStateAsUpdate(serverDoc)).toContain(/* delta with \" offline\" */);\n});\n\ntest('stale scalar write returns 409', async () =&gt; {\n  // Alice writes at T=100\n  const a1 = await writeScalarLWW('iss-1', 'status', 'in_progress', aliceJwt);\n  expect(a1.accepted).toBe(true);\n\n  // Bob writes at T=50 (clock skew)\n  const b1 = await writeScalarLWW_atTime('iss-1', 'status', 'todo', 50n, bobJwt);\n  expect(b1.accepted).toBe(false);\n  expect(b1.reason).toBe('stale_write');\n  expect(b1.current).toBe('in_progress');\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-001</strong> — issue schema; we add LWW companion columns + new tables.</li><li><strong>TASK-PROJ-002 (upstream)</strong> — WebSocket sync engine; Yjs relay is a route within it.</li><li><strong>TASK-PROJ-017 (downstream)</strong> — Brief modal uses Yjs description binding.</li><li><strong>TASK-AUTH-003</strong> — RLS on <code>yjs_state_snapshots</code> + <code>yjs_update_log</code>.</li><li><strong>TASK-AUTH-004</strong> — JWT validation on WebSocket connect; tenant_id from claims.</li><li><strong>TASK-MEMORY-101</strong> — audit row emission.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>proj.issue_collab_session_started</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.issue_collab_session_started\",\n  \"payload\": {\n    \"doc_id\":            \"iss-01HZK9R8M3X5C8Q4\",\n    \"first_subject_id\":  \"7e57c0de-1234-5678-9abc-def012345678\",\n    \"started_at_ns\":     1747407137483000000,\n    \"trace_id\":          \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<h3><code>proj.issue_snapshot_persisted</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.issue_snapshot_persisted\",\n  \"payload\": {\n    \"doc_id\":                       \"iss-01HZK9R8M3X5C8Q4\",\n    \"version\":                      42,\n    \"bytes_compressed\":             8421,\n    \"applies_since_last_snapshot\":  37,\n    \"snapshot_duration_ms\":         12\n  }\n}</code></pre>\n<h3><code>proj.scalar_stale_write_rejected</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.scalar_stale_write_rejected\",\n  \"payload\": {\n    \"issue_id\":      \"iss-01HZK9R8M3X5C8Q4\",\n    \"field\":         \"status\",\n    \"incoming_ts\":   1747407100000000000,\n    \"stored_ts\":     1747407137000000000,\n    \"attempted_by\":  \"7e57c0de-...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>CRDT for <code>labels</code> array (currently LWW; may be desirable as Y.Array later) — slice 4+.</li><li>CRDT for cross-issue moves (drag from one cycle to another) — slice 4+.</li><li>Selective sync (subscribe to subset of fields to save bandwidth) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Postgres unavailable</td><td>sqlx Err</td><td>In-memory buffer (cap 100 MB); sev-2 alarm</td><td>Postgres restored; buffer flushed</td></tr><tr><td>Buffer overflow (Postgres long-down)</td><td>size check</td><td>Oldest snapshots dropped; sev-1 alarm</td><td>Operator extends Postgres downtime tolerance OR scales</td></tr><tr><td>WebSocket connection refused</td><td><code>accept</code> Err</td><td>Client retries with backoff</td><td>Network restored</td></tr><tr><td>JWT expired mid-session</td><td>server periodic check</td><td>Close code 4002; client refreshes + reconnects</td><td>Client transparently re-auths</td></tr><tr><td>Tenant changed mid-session (impossible by design)</td><td>RLS catches</td><td>Updates rejected at write time</td><td>None</td></tr><tr><td>Y.Doc memory leak (long-running room with no clients)</td><td>RoomRegistry GC after 10 min idle</td><td>Room dropped; reload from snapshot on next connect</td><td>None</td></tr><tr><td>Client clock far in the future</td><td>LWW accepts</td><td>Other client's writes rejected as stale</td><td>Operator reviews; clock-sync recommended</td></tr><tr><td>Client clock far in the past</td><td>LWW rejects all writes</td><td>Frequent 409s; UI banner \"your clock is behind\"; instruct to sync</td><td>None automatic</td></tr><tr><td>CRDT update corrupted in transit (network bit-flip)</td><td>Yjs internal checksum</td><td>Update dropped silently; client may need re-sync</td><td>Yjs handles</td></tr><tr><td>Awareness state churn (1000 cursor updates/sec)</td><td>30 Hz throttle</td><td>Client rate-limited; messages_forwarded counter shows skipped</td><td>None</td></tr><tr><td>Snapshot bytes &gt; 1 MB</td><td>size cap check</td><td>sev-2 alarm; investigate doc size</td><td>Operator considers splitting doc</td></tr><tr><td>Two snapshots same version (race)</td><td>unique constraint</td><td>Second insert fails; logged; original kept</td><td>None</td></tr><tr><td>Old snapshots not pruned (job crashed)</td><td>row count &gt; 50</td><td>sev-2 alarm; manual prune</td><td>Operator runs maintenance</td></tr><tr><td><code>applies_since_last_snapshot</code> overflow</td><td>i32 capacity (2.1B)</td><td>Never in practice</td><td>None</td></tr><tr><td>Client-side IndexedDB quota exceeded</td><td>y-indexeddb Err</td><td>Offline buffer disabled; banner shown</td><td>User clears storage</td></tr><tr><td>Multiple tabs of same user same issue</td><td>Yjs handles</td><td>All tabs converge; presence shows multiple cursors</td><td>By design</td></tr><tr><td>Awareness state of a non-existent user</td><td>server validates JWT first</td><td>Cannot happen; auth gates</td><td>None</td></tr><tr><td>LWW field name typo in PATCH URL</td><td>whitelist check</td><td>400 with <code>unknown_field</code></td><td>Client fixes</td></tr><tr><td>LWW value type mismatch (e.g. string for integer estimate)</td><td>serde reject</td><td>400 with details</td><td>Client fixes</td></tr><tr><td>Description binary update without proper Y-protocol framing</td><td>parse fails silently</td><td>Message dropped; client desyncs</td><td>Client reconnects</td></tr><tr><td>WebSocket pong missed</td><td>y-websocket auto-reconnects</td><td>&lt; 30s gap; awareness expires + re-establishes</td><td>Built-in</td></tr><tr><td>RLS policy bug (cross-tenant leak)</td><td>property test detects</td><td>CI blocked</td><td>Author fixes RLS</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><code>yrs</code> (the Rust Yjs port) is API-compatible with JavaScript Yjs; same wire format. We use <code>yrs::sync::Awareness</code> for awareness protocol.</li><li>The <code>RoomRegistry</code> is in-memory; on server restart, rooms are rebuilt lazily as clients reconnect. Snapshots from Postgres provide continuity.</li><li><code>zstd</code> level 6 is the empirical sweet spot: level 3 = 30% larger, level 9 = 5% smaller (not worth 4× CPU).</li><li>The 5 MB IndexedDB cap is enforced by y-indexeddb's <code>gcFilter</code>; we configure it explicitly.</li><li>WebSocket close codes 4001/4002/4003 are app-defined (4xxx range is application-specific per RFC 6455 §7.4.2).</li><li>Snapshot prune query: <code>DELETE FROM yjs_state_snapshots WHERE doc_id = $1 AND version &lt; (SELECT version FROM yjs_state_snapshots WHERE doc_id = $1 ORDER BY version DESC LIMIT 1 OFFSET 49)</code> — keeps 50 most recent.</li><li>The <code>applies_count</code> metric in snapshot rows is a sanity check: very low values (1-2) suggest snapshots too frequent for doc; very high (10000+) suggest snapshots too rare.</li><li><code>current_setting('app.tenant_id')::uuid</code> is set by the connection pool's <code>SET LOCAL</code> before each request, per TASK-AUTH-003.</li><li>Awareness state heartbeat is sent by y-protocols automatically every ~15s; expiry is configured to 30s in the server.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-003.</em></p>";
