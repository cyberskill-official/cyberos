window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOCS-002"]="<h1>FR-DOCS-002 - Documentation single source of truth</h1>\n<h2>Context (the architecture decision)</h2>\n<p>The public wiki lives in <code>website/</code> as hand-authored HTML that duplicates and drifts from the real sources. Decision (operator 2026-07-10, format delegated): documentation moves to ONE source of truth, authored in Markdown with a separated <code>assets/</code> folder per scope, and every display surface (public website, web console, desktop app) renders from that source. HTML is an OUTPUT of the site build, never an authoring format. Ownership follows the code: each module owns its own documentation and step-by-step guides next to its code; only genuinely global artifacts stay in <code>docs/</code>.</p>\n<p>Why markdown over authored HTML: agents and humans author it natively, it diffs cleanly in review, one source renders to all three platforms, and the site build already generates its reference pages (FR/NFR catalogs, changelogs) from repo data - this extends that proven pattern to the doctrine pages instead of maintaining a parallel hand-written HTML tree.</p>\n<h2>1. Normative clauses</h2>\n<ol><li>Module-owned docs MUST live at <code>modules/&lt;m&gt;/docs/</code> — from day one, including modules that have no code yet (the folder IS the module's home; no interim location and no later move). Modules implemented as services carry their docs next to that code at <code>services/&lt;s&gt;/docs/</code> instead. Each scope contains <code>index.md</code> (the module page), optional <code>guides/*.md</code> (step-by-step guidelines), optional <code>appendices.md</code>, and an <code>assets/</code> folder for that module's images/files. Global artifacts (architecture, ADRs, deploy, strategy, feature-requests, glossary-level reference) MUST stay under <code>docs/</code> with <code>docs/assets/</code>.</li></ol>\n<p>1a. A doc page MUST describe only the current state of its subject. Historical narrative, superseded designs, and change records belong in the changelog (each module page ends with a <code>## Changelog</code> section linking its generated changelog page, built from the CHANGELOG.md files). Migrated legacy prose that describes past states is refined toward this rule as each module's docs are touched.</p>\n<ol><li>The website MUST be generated: a deterministic builder (<code>tools/docs-site/render-docs.mjs</code>, wired into <code>tools/docs-site/build.sh</code>) walks the global docs set and every module/service <code>docs/</code> folder, renders markdown into the existing site chrome (nav/styles/tokens), copies each scope's <code>assets/</code>, and writes under gitignored <code>dist/website/</code> — no generated file is ever committed. Same input MUST produce byte-identical output (matches FR-DOCS-001 §1 #3).</li><li>The markdown renderer MUST be dependency-free (no node_modules), supporting the documented subset: headings, paragraphs, bold/italic/inline code, links, images, fenced code blocks, ordered/unordered lists, tables, blockquotes, and horizontal rules. Unsupported constructs MUST pass through as escaped text, never break the build.</li><li>Every hand-authored HTML doctrine page currently in <code>website/docs/</code> (architecture pages, module index/appendices pages, getting-started, glossary, risk-register) MUST be migrated to a markdown source at its owning scope, and the hand-authored file is deleted; the page exists only as build output. Already-generated pages (fr-catalog, nfr-catalog, changelogs) keep their builders, relocated to <code>tools/docs-site/</code>.</li><li>A <code>docs-manifest</code> per scope is NOT required: the builder MUST derive the nav from the filesystem (module list + each scope's files), so adding a doc is adding a file.</li><li>The migration MUST NOT lose content: each migrated page's headings and body text are preserved (markdown conversion), with assets relocated to the owning scope's <code>assets/</code>.</li><li><code>cyberos doctor</code>-style honesty: the builder MUST fail non-zero on a missing referenced asset or unreadable source file.</li></ol>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] <code>tools/docs-site/build.sh</code> regenerates the whole docs site from md sources into <code>dist/website</code>; running twice yields byte-identical output.</li><li>[ ] All previously hand-authored doctrine pages exist as md under their owning scope; the <code>website/</code> folder is gone from version control entirely.</li><li>[ ] Site renders with the existing chrome (nav, styles) and working asset links.</li><li>[ ] Web console and desktop app can consume the same md sources (path contract documented in this FR; their viewers are follow-up FRs).</li></ul>\n<h2>3. Gate</h2>\n<p>Machine: build twice + diff (determinism), link/asset check pass, <code>bash tools/docs-site/build.sh</code> exit 0. Review + final acceptance: HITL per STATUS-REFERENCE §1.4.</p>\n<h2>Ship record (2026-07-12)</h2>\n<ul><li>Parked at reviewing from its own wave; gate cleared today. Fresh verification: render-docs.mjs + dependency-free md.mjs + relocated builders present; website/docs fully migrated (deleted); build green, 69 pages, byte-identical double-build (determinism), dist/ gitignored.</li><li>Review + final acceptance: APPROVE + pre-authorize done (Stephen Cheng, in-chat, 2026-07-12).</li></ul>\n<p><em>End of FR-DOCS-002.</em></p>";
