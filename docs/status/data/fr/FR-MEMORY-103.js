window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-103"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A <code>memory-sync</code> Rust daemon <strong>MUST</strong> run on every device, syncing the personal memory to a designated Cloud memory per <code>memory/docs/AUTOSYNC_DESIGN.md</code>:</p>\n<ol><li><strong>MUST</strong> push every local chain row to Cloud memory via gRPC stream (TLS + bearer auth). Push is unidirectional; one row per stream message; ack-per-row.</li><li><strong>MUST</strong> pull foreign-origin rows from Cloud memory; apply via local-import per AGENTS.md §14.2 — each foreign row becomes a fresh <code>put</code> on the LOCAL chain whose <code>extra.imported_from</code> identifies the source device fingerprint AND <code>extra.foreign_chain</code> records the source chain hash.</li><li><strong>MUST</strong> respect <code>meta.sync_class</code>: only rows with <code>sync_class == \"shareable\"</code> (or the v1 transitional values <code>publishable | shared | client-visible</code>) push to Cloud. Rows with <code>private | local-only</code> stay on the device. The filter is applied at push-time AT the sync boundary.</li><li><strong>MUST</strong> detect conflicts via CRDT triple <code>(content_hash, ts_ns, originator_device)</code>. Conflict = same target memory id with two different writes from different devices within overlapping wall-clock windows. On conflict, write a <code>disputed_pair</code> row with both versions; FR-MEMORY-105 surfaces the resolution UI.</li><li><strong>MUST</strong> complete sync round-trip within 5 seconds p95 on healthy network (push + cloud ack + pull + local apply). Above 5s, ops investigates.</li><li><strong>MUST</strong> be device-id-stamped: every sync-emitted row carries <code>extra.originator_device_id</code> (UUID generated at first daemon start; persisted to <code>~/.cyberos/device_id</code>).</li><li><strong>MUST</strong> maintain offline buffer of up to 10,000 rows when Cloud memory is unreachable. Buffer is FIFO; oldest rows evicted on overflow with sev-2 alert + metric <code>memory_sync_buffer_overflow_total</code>. Evicted rows are NOT lost (they're in the local Layer 1 chain) — they just don't propagate until manual reconciliation.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>memory_sync_lag_seconds{device_id}</code> (histogram; round-trip).</li><li><code>memory_sync_conflicts_total</code> (counter).</li><li><code>memory_sync_buffered_rows{device_id}</code> (gauge).</li><li><code>memory_sync_buffer_overflow_total{device_id}</code> (counter; sev-2 alarm).</li><li><code>memory_sync_pushed_rows_total{device_id, sync_class}</code> (counter).</li><li><code>memory_sync_compensation_rejected_total</code> (counter; sev-1 alarm — DEC-036 violation attempt).</li></ul>\n<ol><li><strong>MUST</strong> apply <code>compensation_guard</code> at the sync boundary: ANY row whose path matches <code>meta/people/*/compensation*</code> OR <code>meta/people/*/equity*</code> OR <code>meta/finance/payroll*</code> is REJECTED at push time AND NOT added to offline buffer (compensation rows that arrive at sync are a sev-1 — investigate the upstream code that emitted them).</li><li><strong>MUST</strong> not re-import already-imported foreign chains (idempotency on <code>extra.foreign_chain</code> value).</li><li><strong>MUST</strong> detect online/offline state via TCP probe to Cloud memory every 30s; transition online→offline → start buffering; offline→online → flush buffer + resume.</li><li><strong>MUST</strong> authenticate to Cloud memory via per-device bearer token (rotated quarterly; managed via FR-AUTH-006-style sweeper). Compromised token → revoke at Cloud memory; device cannot sync until new token issued.</li><li><strong>MUST</strong> apply CRDT conflict resolution deterministically across devices: given <code>(A_content_hash, A_ts, A_device)</code> vs <code>(B_content_hash, B_ts, B_device)</code>, both devices independently arrive at the SAME resolution — write disputed_pair (both visible until human decides).</li><li><strong>MUST</strong> preserve chain ordering on import: per AGENTS.md §14.2, the import block is bracketed by <code>session.start</code> and <code>session.end</code> audit row on the local chain — the imports are clearly demarcated.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why Cloud memory as star-topology hub (DEC-100)?</strong> Peer-to-peer sync (every device → every device) scales as O(N²) connections; centralised hub is O(N). Cloud memory is the trust root + delivery hub. Devices online intermittently get all updates from the hub on reconnect.</p>\n<p><strong>Why sync_class gating (§1 #3)?</strong> Some memories are device-local (working notes, drafts). Others are shareable across devices (published decisions, project history). The <code>sync_class</code> field is the user's privacy primitive; respecting it at sync boundary is the implementation.</p>\n<p><strong>Why compensation_guard at sync (DEC-036)?</strong> Compensation/equity is in scope for AI assistance ON device but NEVER leaves device. The guard at sync boundary is defense-in-depth — even if a row is mistakenly tagged <code>shareable</code>, the path-based check catches compensation rows. Sev-1 metric flags the attempt.</p>\n<p><strong>Why CRDT triple for conflict (DEC-101)?</strong> <code>(content_hash, ts_ns, originator_device)</code> identifies any version uniquely. Two devices independently producing different content for the same target id at overlapping times = conflict. The triple is the conflict-detection primitive; deterministic across devices means both A and B independently reach \"this is a conflict\" without coordination.</p>\n<p><strong>Why import-as-fresh-put per §14.2 (§1 #2)?</strong> AGENTS.md §14.2 mandates: foreign chain doesn't merge directly into local chain (would corrupt local chain integrity). Each foreign row becomes a NEW local put with <code>extra.foreign_chain</code> referencing the source. The local chain stays internally consistent.</p>\n<p><strong>Why 10K offline buffer (§1 #7)?</strong> Sizing math: typical user appends ~100 rows/day; 10K = 100 days offline. Beyond 100 days, the user is effectively a different device — manual reconciliation makes sense. The cap protects against unbounded local growth during prolonged outages.</p>\n<p><strong>Why 5s p95 round-trip (§1 #5)?</strong> Multi-device users expect \"I added a note on laptop A; let me find it on laptop B\" to work near-real-time. 5s is below human attention; longer windows produce confusion (\"did it sync?\").</p>\n<p><strong>Why per-device bearer token (§1 #12)?</strong> Compromised device could sync false data to Cloud, corrupting other devices. Per-device token revocable at Cloud — compromised device's sync stops; legitimate devices unaffected.</p>\n<p><strong>Why deterministic CRDT resolution (§1 #13)?</strong> Both devices A and B independently see the conflict; both must agree on the resolution outcome (disputed_pair). Without deterministic resolution, A might apply one version + B applies the other → silent divergence. Determinism = both arrive at SAME state without coordination.</p>\n<p><strong>Why session.start / session.end brackets (§1 #14, §14.2)?</strong> Without brackets, imported rows interleave with normal local activity, making the import boundary invisible. Brackets give compliance audits a clear \"imported from X at time Y to Y+N\" demarcation.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-proto\">// services/memory-sync/proto/memory_sync.proto\nsyntax = \"proto3\";\npackage cyberos.memory_sync.v1;\n\nservice MemorySync {\n    rpc Push(stream PushRequest) returns (stream PushAck);\n    rpc Pull(PullRequest) returns (stream PullResponse);\n    rpc Health(HealthRequest) returns (HealthResponse);\n}\n\nmessage PushRequest {\n    string device_id = 1;\n    int64 seq = 2;\n    int64 ts_ns = 3;\n    bytes body = 4;\n    bytes meta_json = 5;\n    bytes prev_chain = 6;\n    bytes chain = 7;\n    string sync_class = 8;       // shareable | private | ...\n}\n\nmessage PushAck { int64 seq = 1; bool accepted = 2; string reason = 3; }\n\nmessage PullRequest { string device_id = 1; bytes since_chain = 2; }\nmessage PullResponse {\n    string origin_device_id = 1;\n    int64 origin_seq = 2;\n    bytes body = 3;\n    bytes meta_json = 4;\n    bytes foreign_chain = 5;\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-sync/src/sync.rs\npub async fn sync_loop(local: &amp;LocalMemory, cloud: &amp;CloudMemoryClient, config: &amp;SyncConfig) -&gt; anyhow::Result&lt;()&gt; {\n    loop {\n        let online = cloud.health_check().await.is_ok();\n\n        if online {\n            // §1 #11 — flush buffer if just transitioned to online\n            if let Some(buffered) = buffer::take_all().await? {\n                push_batch(buffered, cloud).await?;\n            }\n\n            // Push: shareable rows only\n            let pending = local.pending_for_sync().await?;\n            for row in pending {\n                if !sync_class_filter::is_shareable(&amp;row) { continue; }\n                if compensation_guard::is_compensation(&amp;row) {\n                    metrics::compensation_rejected();\n                    tracing::error!(seq = row.seq, path = %row.path, \"compensation row at sync boundary; sev-1\");\n                    continue;\n                }\n                push_one(row, cloud).await?;\n            }\n\n            // Pull: foreign rows\n            let cursor = local.last_pulled_chain().await?;\n            for remote in cloud.pull_since(cursor).await? {\n                if local.has_foreign_chain(&amp;remote.foreign_chain).await? {\n                    continue;   // §1 #10 dedup\n                }\n                let new_local_seq = local.put_with_origin(remote.clone()).await?;\n                if let Some(conflict) = crdt::detect_conflict(&amp;new_local_seq, local).await? {\n                    local.write_disputed_pair(&amp;conflict).await?;\n                    metrics::conflict();\n                }\n            }\n        } else {\n            // §1 #7 buffer\n            let pending = local.pending_for_sync().await?;\n            for row in pending {\n                if !sync_class_filter::is_shareable(&amp;row) { continue; }\n                if compensation_guard::is_compensation(&amp;row) { /* sev-1 */; continue; }\n                buffer::push(row).await?;\n            }\n        }\n\n        tokio::time::sleep(Duration::from_secs(1)).await;\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-sync/src/crdt.rs\npub struct CrdtTriple {\n    pub content_hash: [u8; 32],\n    pub ts_ns: i64,\n    pub originator_device: Uuid,\n}\n\npub async fn detect_conflict(new_row: &amp;Row, local: &amp;LocalMemory) -&gt; Result&lt;Option&lt;DisputedPair&gt;, anyhow::Error&gt; {\n    let target_id = new_row.target_memory_id();\n    let recent_writes = local.writes_for_target_in_window(\n        target_id, new_row.ts_ns - 5_000_000_000, new_row.ts_ns + 5_000_000_000,\n    ).await?;\n\n    for other in recent_writes {\n        if other.originator_device != new_row.originator_device\n           &amp;&amp; other.content_hash != new_row.content_hash\n        {\n            return Ok(Some(DisputedPair {\n                target: target_id,\n                version_a: row_to_triple(new_row),\n                version_b: row_to_triple(&amp;other),\n            }));\n        }\n    }\n    Ok(None)\n}\n\npub fn row_to_triple(row: &amp;Row) -&gt; CrdtTriple {\n    CrdtTriple {\n        content_hash: sha256(&amp;row.body),\n        ts_ns: row.ts_ns,\n        originator_device: row.originator_device.unwrap_or(Uuid::nil()),\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-sync/src/sync_class_filter.rs\nconst SHAREABLE_VALUES: &amp;[&amp;str] = &amp;[\"shareable\", \"publishable\", \"shared\", \"client-visible\"];\n\npub fn is_shareable(row: &amp;Row) -&gt; bool {\n    let class = row.meta.get(\"sync_class\").and_then(|v| v.as_str()).unwrap_or(\"private\");\n    SHAREABLE_VALUES.contains(&amp;class)\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-sync/src/compensation_guard.rs\nconst FORBIDDEN_PATTERNS: &amp;[&amp;str] = &amp;[\n    \"meta/people/*/compensation*\",\n    \"meta/people/*/equity*\",\n    \"meta/finance/payroll*\",\n    \"meta/finance/comp*\",\n];\n\npub fn is_compensation(row: &amp;Row) -&gt; bool {\n    let path = &amp;row.path;\n    FORBIDDEN_PATTERNS.iter().any(|p| glob_match(p, path))\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-sync/src/buffer.rs\nconst MAX_BUFFER_ROWS: usize = 10_000;\n\npub async fn push(row: Row) -&gt; anyhow::Result&lt;()&gt; {\n    let mut buffer = BUFFER.lock().await;\n    if buffer.len() &gt;= MAX_BUFFER_ROWS {\n        let evicted = buffer.pop_front().unwrap();\n        tracing::warn!(seq = evicted.seq, \"buffer overflow; oldest evicted\");\n        metrics::buffer_overflow();\n    }\n    buffer.push_back(row);\n    metrics::buffer_size(buffer.len());\n    Ok(())\n}\n\npub async fn take_all() -&gt; anyhow::Result&lt;Option&lt;Vec&lt;Row&gt;&gt;&gt; {\n    let mut buffer = BUFFER.lock().await;\n    if buffer.is_empty() { return Ok(None); }\n    Ok(Some(std::mem::take(&amp;mut *buffer).into_iter().collect()))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>Append row on device A → within 5s, row visible on device B (via Cloud memory round-trip).</li><li>Offline: device A appends 50 rows → reconnect → all 50 sync.</li><li>Concurrent append (A + B same target id, overlapping ts_ns) → CRDT detects conflict; both stored as <code>disputed_pair</code>.</li><li>Private rows (<code>sync_class: private</code>) never leave device.</li><li>Compensation rows refused at sync boundary; sev-1 metric <code>memory_sync_compensation_rejected_total</code> increments.</li><li>Sync round-trip &lt; 5s p95 (push + pull + apply).</li><li>10K offline buffer handled without crash; 10001st entry → oldest evicted + sev-2 alert.</li><li>Foreign chain dedup — re-importing same row is no-op (no duplicate local row).</li><li>Online/offline detection: TCP probe every 30s; state transitions trigger buffer flush.</li><li>Per-device bearer auth — wrong token → Cloud rejects with 401.</li><li>Deterministic CRDT — A and B independently arrive at same disputed_pair state.</li><li>Import block bracketed by session.start / session.end audit rows on local chain.</li><li>shareable values include v1 transitional (publishable, shared, client-visible).</li><li>Device-id auto-generated at first daemon start; persisted to <code>~/.cyberos/device_id</code>.</li><li>Buffered rows survive daemon restart (persisted to disk).</li><li>Push ack per row — sender doesn't proceed until ack received.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn append_on_a_visible_on_b_within_5s() {\n    let mock_cloud = MockCloud::start();\n    let device_a = test_device(\"a\", &amp;mock_cloud).await;\n    let device_b = test_device(\"b\", &amp;mock_cloud).await;\n\n    let row = test_helper::shareable_row(\"test_body\");\n    device_a.append(row).await.unwrap();\n\n    let t0 = std::time::Instant::now();\n    while t0.elapsed() &lt; Duration::from_secs(6) {\n        if device_b.has_imported(&amp;row).await { return; }\n        tokio::time::sleep(Duration::from_millis(100)).await;\n    }\n    panic!(\"row not synced within 5s\");\n}\n\n#[tokio::test]\nasync fn offline_buffers_50_rows_then_syncs_on_reconnect() {\n    let mock_cloud = MockCloud::start();\n    let device = test_device(\"a\", &amp;mock_cloud).await;\n    mock_cloud.go_offline();\n    for i in 0..50 {\n        device.append(test_helper::shareable_row(&amp;format!(\"body_{i}\"))).await.unwrap();\n    }\n    let buffered: u64 = otel_test_helper::gauge_value(\"memory_sync_buffered_rows\", &amp;[]);\n    assert_eq!(buffered, 50);\n    mock_cloud.go_online();\n    tokio::time::sleep(Duration::from_secs(3)).await;\n    let final_buffered: u64 = otel_test_helper::gauge_value(\"memory_sync_buffered_rows\", &amp;[]);\n    assert_eq!(final_buffered, 0);\n    let received = mock_cloud.received_count();\n    assert_eq!(received, 50);\n}\n\n#[tokio::test]\nasync fn private_rows_never_leave_device() {\n    let mock_cloud = MockCloud::start();\n    let device = test_device(\"a\", &amp;mock_cloud).await;\n    device.append(test_helper::row_with_sync_class(\"private\", \"secret\")).await.unwrap();\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    assert_eq!(mock_cloud.received_count(), 0);\n}\n\n#[tokio::test]\nasync fn compensation_row_rejected_at_sync_boundary() {\n    let mock_cloud = MockCloud::start();\n    let device = test_device(\"a\", &amp;mock_cloud).await;\n    let comp_row = test_helper::row_at_path(\"meta/people/alice/compensation.md\", \"shareable\", \"body\");\n    device.append(comp_row).await.unwrap();\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    assert_eq!(mock_cloud.received_count(), 0);\n    let metric: u64 = otel_test_helper::counter_value(\"memory_sync_compensation_rejected_total\", &amp;[]);\n    assert_eq!(metric, 1);\n}\n\n#[tokio::test]\nasync fn concurrent_append_creates_disputed_pair() {\n    let mock_cloud = MockCloud::start();\n    let device_a = test_device(\"a\", &amp;mock_cloud).await;\n    let device_b = test_device(\"b\", &amp;mock_cloud).await;\n\n    let target_id = Uuid::new_v4();\n    device_a.append_at_target(target_id, \"version_a\", now_ns()).await.unwrap();\n    device_b.append_at_target(target_id, \"version_b\", now_ns()).await.unwrap();\n\n    tokio::time::sleep(Duration::from_secs(3)).await;\n    let pair_a = device_a.find_disputed_pair(target_id).await;\n    let pair_b = device_b.find_disputed_pair(target_id).await;\n    assert!(pair_a.is_some());\n    assert!(pair_b.is_some());\n    // Deterministic: both devices see same disputed_pair contents\n    assert_eq!(pair_a.unwrap().version_a.content_hash, pair_b.unwrap().version_a.content_hash);\n}\n\n#[tokio::test]\nasync fn buffer_overflow_evicts_oldest() {\n    let mock_cloud = MockCloud::start();\n    let device = test_device(\"a\", &amp;mock_cloud).await;\n    mock_cloud.go_offline();\n    for i in 0..10_001 {\n        device.append(test_helper::shareable_row(&amp;format!(\"body_{i}\"))).await.unwrap();\n    }\n    let metric: u64 = otel_test_helper::counter_value(\"memory_sync_buffer_overflow_total\", &amp;[]);\n    assert_eq!(metric, 1);   // exactly one eviction\n    let buffer_size = device.buffer_size().await;\n    assert_eq!(buffer_size, 10_000);\n}\n\n#[tokio::test]\nasync fn foreign_chain_dedup() {\n    let mock_cloud = MockCloud::start();\n    let device = test_device(\"a\", &amp;mock_cloud).await;\n    let foreign_row = test_helper::foreign_row_with_chain(\"chain_xyz\");\n    device.import_from_cloud(foreign_row.clone()).await.unwrap();\n    device.import_from_cloud(foreign_row.clone()).await.unwrap();   // re-import same chain\n    let count = device.local_rows_with_foreign_chain(\"chain_xyz\").await;\n    assert_eq!(count, 1);\n}\n\n#[tokio::test]\nasync fn import_brackets_with_session_start_end() {\n    let mock_cloud = MockCloud::start();\n    let device = test_device(\"a\", &amp;mock_cloud).await;\n    mock_cloud.publish_rows(vec![test_helper::foreign_row(\"a\"), test_helper::foreign_row(\"b\")]).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    let chain = device.local_chain().await;\n    assert!(chain.iter().any(|r| r.kind == \"session.start\" &amp;&amp; r.extra.imported_from.is_some()));\n    assert!(chain.iter().any(|r| r.kind == \"session.end\" &amp;&amp; r.extra.imported_from.is_some()));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-101</strong> — Layer 2 sees synced rows downstream.</li><li><strong>FR-MEMORY-104</strong> — Tauri app embeds memory-sync.</li><li><strong>FR-MEMORY-105</strong> — Disputed-pair UI (slice 2+).</li><li><strong>FR-MEMORY-106</strong> — sync-class enforcement at write time (this FR enforces at sync boundary).</li><li>Cloud memory service (separate, P1+).</li><li>Crates: <code>tonic@0.12</code>, <code>prost@0.13</code>, <code>tokio</code>, <code>sqlx</code>, <code>serde</code>, <code>glob</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Push request (gRPC)</h3>\n<pre><code class=\"language-proto\">PushRequest {\n    device_id: \"device-laptop-mbp\"\n    seq: 12345\n    ts_ns: 1747526400000000000\n    body: &lt;bytes&gt;\n    meta_json: '{\"sync_class\": \"shareable\", \"kind\": \"decisions\"}'\n    prev_chain: &lt;hash&gt;\n    chain: &lt;hash&gt;\n    sync_class: \"shareable\"\n}</code></pre>\n<h3>Disputed pair audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.disputed_pair\",\n  \"payload\": {\n    \"target_memory_id\": \"550e...\",\n    \"version_a\": {\n      \"content_hash\": \"a3f9c8d7...\",\n      \"ts_ns\": 1747526400000000000,\n      \"originator_device\": \"device-mbp\"\n    },\n    \"version_b\": {\n      \"content_hash\": \"9d6e3a2b...\",\n      \"ts_ns\": 1747526400500000000,\n      \"originator_device\": \"device-imac\"\n    }\n  }\n}</code></pre>\n<h3>Compensation rejection log</h3>\n<pre><code class=\"language-text\">ERROR seq=42 path=\"meta/people/alice/compensation.md\" device_id=\"laptop-a\"\n      compensation row at sync boundary; sev-1 rejected\nsev-1 memory_sync_compensation_rejected_total incremented</code></pre>\n<h3>Buffer overflow alert</h3>\n<pre><code class=\"language-text\">WARN seq=12345 device_id=\"laptop-a\"\n     buffer overflow; oldest evicted (was offline &gt; 100 days)\nsev-2 memory_sync_buffer_overflow_total incremented</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>E2E encryption of body in Cloud (slice 3+).</li><li>Conflict auto-resolution heuristics — slice 4+.</li><li>Cross-Cloud-memory federation (multi-region) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Network drop</td><td>health_check fails</td><td>Buffer locally; resume on reconnect</td><td>Self-heals</td></tr><tr><td>Cloud memory down</td><td>Same</td><td>Same</td><td>Self-heals</td></tr><tr><td>Conflict</td><td>CRDT detect</td><td>disputed_pair row created</td><td>User picks via FR-MEMORY-105 UI</td></tr><tr><td>Buffer overflow (&gt;10K)</td><td>Sev-2 alert; oldest rows dropped</td><td>Operator investigates extended outage</td><td>Standard ops</td></tr><tr><td>Foreign chain not anchored to local</td><td>per §14.2 fresh put</td><td>Each foreign import is a fresh put</td><td>By design</td></tr><tr><td>Compensation row at sync</td><td>path-based guard</td><td>Rejected; sev-1 metric</td><td>Investigate upstream code that emitted shareable comp</td></tr><tr><td>Per-device token revoked</td><td>Cloud 401</td><td>Sync stops; sev-1 alarm</td><td>Operator issues new token</td></tr><tr><td>Token leaked</td><td>unauthorized syncs</td><td>sev-1; rotate</td><td>Standard incident</td></tr><tr><td>Concurrent push race</td><td>gRPC server handles</td><td>One succeeds; other gets ack</td><td>By design</td></tr><tr><td>Disk full on offline buffer</td><td>persist fails</td><td>Sev-1 alarm</td><td>Operator extends disk</td></tr><tr><td>Daemon crash mid-buffer</td><td>buffer persisted to disk</td><td>Recover on restart</td><td>By design (slice 2+ implements persistence; slice 1 in-memory)</td></tr><tr><td>Clock skew between devices</td><td>CRDT ts_ns may differ slightly</td><td>Conflict window 5s tolerates</td><td>By design</td></tr><tr><td>Import duplicates a chain</td><td>foreign_chain dedup</td><td>Skipped</td><td>By design (§1 #10)</td></tr><tr><td>sync_class missing</td><td>defaults to \"private\"</td><td>Stays local</td><td>By design (safe default)</td></tr><tr><td>Push of row &gt; 1MB</td><td>gRPC max-message check</td><td>Rejected with PushAck.accepted=false</td><td>Caller chunks</td></tr><tr><td>Pull cursor lost</td><td>recompute from local chain</td><td>Re-imports happen; dedup catches</td><td>Self-heals</td></tr><tr><td>TLS cert expired</td><td>gRPC fails</td><td>Sync stops; sev-1</td><td>Operator rotates cert</td></tr><tr><td>Disputed_pair already exists</td><td>UPSERT</td><td>No duplicate</td><td>By design</td></tr><tr><td>Metric collector down</td><td>local OTel buffer</td><td>Telemetry delayed</td><td>Self-heals</td></tr><tr><td>Daemon restart loses 5s buffer (in-memory)</td><td>known limitation slice 1</td><td>Few rows lost</td><td>Slice 2+ persists buffer</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>Cloud memory is the centralised hub (star topology); per-device sync via gRPC stream.</li><li>The <code>sync_class</code> field is the user's privacy primitive. Default <code>private</code> (safe default).</li><li>compensation_guard at sync boundary is defense-in-depth — even if a row is mistakenly tagged shareable, the path-based check catches comp rows.</li><li>CRDT triple deterministic resolution means two devices independently produce the same disputed_pair state.</li><li>10K offline buffer covers ~100 days of typical usage (100 rows/day). Beyond that, manual reconciliation is expected.</li><li>Per-device bearer token rotated quarterly via FR-AUTH-006-style sweeper.</li><li>Import-as-fresh-put per §14.2 preserves local chain integrity; foreign chain doesn't merge directly.</li><li>session.start / session.end brackets give compliance audits clear \"imported from X at time Y\" demarcation.</li><li>Slice 1 buffer is in-memory; slice 2+ persists to disk for daemon-restart recovery.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-103. Status: done (implemented 2026-05-23).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Shipped in python (modules/memory), not the Rust service path named above.</p>";
