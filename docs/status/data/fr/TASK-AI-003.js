window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> provide a <code>memory_writer</code> Rust module that emits chained, content-addressed audit rows to the local memory via the canonical Writer subprocess. The module exposes one public emission function and a typed set of row kinds; every other AI Gateway path that needs to write to memory MUST route through this module.</p>\n<p>The <code>memory_writer::emit()</code> function:</p>\n<ol><li><strong>MUST</strong> accept a typed <code>MemoryEmit</code> struct describing the row (kind, path, extra payload).</li><li><strong>MUST</strong> spawn <code>python3 -m cyberos.writer put</code> as a subprocess, pipe the canonical-JSON payload on stdin, wait for the subprocess to exit, and read the resulting <code>{seq, chain, ts_ns}</code> triple on stdout.</li><li><strong>MUST</strong> treat a non-zero subprocess exit code as a hard failure (<code>Err(MemoryWriterError::WriterFailed)</code>); callers MUST NOT proceed with the action the row was guarding.</li><li><strong>MUST</strong> complete within 50ms p95 for a single row; budget includes process spawn (~15ms), file lock acquisition (~3ms), durable-sync (~10ms), stdout read (~2ms). The remaining 20ms is the canonical-JSON serialisation + the Writer's invariant checks.</li><li><strong>MUST</strong> be re-entrant: multiple AI Gateway tokio tasks calling <code>emit()</code> concurrently MUST serialise through the Writer's <code>.lock</code> (POSIX <code>LOCK_EX</code>) — no parallel chain advances are permitted.</li><li><strong>MUST</strong> validate the payload's canonical form before invoking the subprocess: NFC-normalised UTF-8, sorted keys, no insignificant whitespace, integers in their natural form. Per <code>AGENTS.md §6.2</code>.</li><li><strong>MUST</strong> verify the returned <code>chain</code> hash equals <code>SHA-256(canonical(record_minus_chain) || prev_chain)</code>; refuse to return <code>Ok</code> to the caller if the verification fails (catches a corrupted Writer).</li><li><strong>MUST</strong> support the following closed set of <code>ai.*</code> row kinds (updated 2026-05-16 as additional tasks were specified):</li></ol>\n<ul><li>Slice-1 initial: <code>ai.precheck</code>, <code>ai.invocation</code>, <code>ai.invocation_failed</code>, <code>ai.hold_expired</code>, <code>ai.persona_loaded</code>.</li><li>Added by TASK-AI-009 (circuit breaker): <code>ai.failover_triggered</code>.</li><li>Added by TASK-AI-015 (ZDR enforcement): <code>ai.zdr_violation</code>.</li><li>Added by TASK-AI-016 (residency pinning): <code>ai.residency_violation</code>.</li><li>Added by TASK-AI-017 (per-tenant cache): <code>ai.cache_hit</code>.</li><li>Added by TASK-AI-021 (operator CLI) — operator-action rows in <code>ai.cli_*</code> sub-namespace: <code>ai.cli_policy_updated</code>, <code>ai.cli_failover_drill</code>, <code>ai.cli_invoice_exported</code>, <code>ai.cli_breaker_reset</code>, <code>ai.cli_expiry_repaired</code>, <code>ai.cli_memory_emitted</code>.</li><li>Cross-module rows in adjacent namespaces (NOT owned by this bridge but emitted via the module's own writer; registered here per task-audit skill §3.2 rule 8 for the canonical closed-set): <code>auth.*</code> per TASK-AUTH-001/002/004/006; <code>memory.sync_row_filtered</code> per TASK-MEMORY-106; <code>skill.invoked_started</code> + <code>skill.invoked_completed</code> per TASK-SKILL-101; <code>obs.*</code> per task-OBS module.</li><li><strong>CHAT module rows</strong> (added via the strict-redo 2026-05-16 P.M. expansion of TASK-CHAT-002..012; emitted by services/chat/*, services/chat-memory-bridge/*, services/chat-importer/*, services/chat-lumi/*, services/chat-push/*, services/chat-dsar/*):</li><li>TASK-CHAT-002 (authbridge): <code>chat.session_started</code>.</li><li>TASK-CHAT-003 (Fargate deployment): <code>chat.deployment_provisioned</code>, <code>chat.deployment_inventory</code>, <code>chat.deployment_drift_detected</code>, <code>chat.tier_upgraded</code>, <code>chat.pitr_test_passed</code>, <code>chat.deployment_warning</code>, <code>chat.maintenance_started</code>, <code>chat.deployment_blocked</code>, <code>chat.tf_state_recovered</code>, <code>chat.module_deprecated</code>.</li><li>TASK-CHAT-005 (memory bridge): <code>chat.message</code>, <code>chat.message_edited</code>, <code>chat.message_deleted</code>, <code>chat.channel_created</code>, <code>chat.channel_archived</code>, <code>chat.message_reacted</code>, <code>chat.message_unreacted</code>, <code>chat.user_joined_channel</code>, <code>chat.user_left_channel</code>, <code>chat.message_attachment</code>, <code>chat.bridge_heartbeat</code>, <code>chat.bridge_lag_alert</code>, <code>chat.bridge_misconfigured</code>, <code>chat.bridge_shutdown</code>, <code>chat.bridge_redaction_failed</code>.</li><li>TASK-CHAT-006 (Slack import): <code>chat.import_started</code>, <code>chat.import_step_completed</code>, <code>chat.import_finished</code>, <code>chat.import_warning</code>, <code>chat.import_verification_failed</code>, <code>chat.import_aborted</code>, <code>chat.import_failed</code>, <code>chat.import_rate_limit_exhausted</code>.</li><li>TASK-CHAT-007 (Zalo import): reuses the TASK-CHAT-006 <code>chat.import_*</code> family AND adds <code>chat.import_unsupported_zalo_version</code>, <code>chat.import_timestamp_ambiguous</code>, <code>chat.import_conversation_completed</code>.</li><li>TASK-CHAT-008 (Lumi mention): <code>chat.lumi_invoked</code>, <code>chat.lumi_skipped</code>, <code>chat.lumi_error</code>.</li><li>TASK-CHAT-009 (retro capture): <code>chat.retro_capture_started</code>, <code>chat.retro_capture_completed</code>, <code>chat.retro_capture_cancelled</code>, <code>chat.retro_capture_expired</code>, <code>chat.retro_capture_truncated</code>.</li><li>TASK-CHAT-010 (decommission signal): <code>chat.decommission_signal</code>, <code>chat.decommission_state_changed</code>, <code>chat.decommission_snoozed</code>, <code>chat.decommission_unsnoozed</code>.</li><li>TASK-CHAT-011 (mobile push): <code>chat.push_delivered</code>, <code>chat.push_failed</code>, <code>chat.push_dnd_dropped</code>.</li><li>TASK-CHAT-012 (DSAR export): <code>chat.dsar_requested</code>, <code>chat.dsar_exporting</code>, <code>chat.dsar_delivered</code>, <code>chat.dsar_fully_delivered</code>, <code>chat.dsar_acknowledged</code>, <code>chat.dsar_failed</code>, <code>chat.dsar_expired</code>, <code>chat.dsar_url_reused</code>.</li><li><strong>PROJ module rows</strong> (added via the strict-redo 2026-05-16 P.M. expansion of TASK-PROJ-002, 005..018; emitted by services/proj-sync/* and web/proj-client/*):</li><li>TASK-PROJ-001 (issue schema baseline): <code>proj.issue_status_changed</code> (superseded by TASK-PROJ-002's <code>proj.decision</code> for status transitions; status_changed retained for non-decision metadata updates).</li><li>TASK-PROJ-002 (memory-anchored decision): <code>proj.decision</code>, <code>proj.decision_retracted</code>.</li><li>TASK-PROJ-005 (rate-card schema): <code>proj.rate_card_created</code>, <code>proj.rate_card_superseded</code>, <code>proj.rate_card_billable_default_changed</code>, <code>proj.rate_card_corrected</code>, <code>proj.rate_card_currency_mismatch</code>.</li><li>TASK-PROJ-006 (billable cascade): <code>proj.billable_resolved</code>, <code>proj.member_billable_override_set</code>, <code>proj.task_class_billable_set</code>.</li><li>TASK-PROJ-007 (billing modes): <code>proj.billing_mode_set</code>, <code>proj.billing_mode_changed</code>, <code>proj.milestone_invoiced</code>, <code>proj.milestone_cancelled</code>, <code>proj.milestone_added</code>, <code>proj.retainer_overage_emitted</code>, <code>proj.retainer_overage_streak</code>, <code>proj.retainer_rollover_carry_forward</code>, <code>proj.billing_mixed_currency_period</code>.</li><li>TASK-PROJ-008 (history events): <code>proj.issue_mutated</code>, <code>proj.chain_tampered</code>.</li><li>TASK-PROJ-009 (memory links): <code>proj.memory_link_created</code>, <code>proj.memory_link_removed</code>, <code>proj.memory_link_traversed</code>.</li><li>TASK-PROJ-010 (citation drift): <code>proj.citation_drift_detected</code>, <code>proj.drift_remediated</code>.</li><li>TASK-PROJ-011 (blocker detector): <code>proj.blocker_detected</code>, <code>proj.blocker_resolved</code>, <code>proj.blocker_resolved_diff</code>, <code>proj.blocker_stale</code>, <code>proj.blocker_escalated</code>, <code>proj.blocker_cycle_detected</code>.</li><li>TASK-PROJ-012 (cycle review): <code>proj.cycle_review_drafted</code>, <code>proj.cycle_review_accepted</code>, <code>proj.cycle_review_iterated</code>, <code>proj.cycle_review_skipped</code>, <code>proj.cycle_goal_updated</code>.</li><li>TASK-PROJ-013 (estimate calibration): <code>proj.estimate_calibration_computed</code>, <code>proj.calibration_drift_alert</code>, <code>proj.calibration_backfilled</code>.</li><li>TASK-PROJ-014 (Kanban): <code>proj.kanban_card_moved</code>, <code>proj.kanban_card_move_undone</code>, <code>proj.wip_limit_overridden</code>.</li><li>TASK-PROJ-015 (Timeline): <code>proj.timeline_bar_moved</code>.</li><li>TASK-PROJ-016 (Gantt): <code>proj.dependency_added</code>, <code>proj.dependency_removed</code>, <code>proj.dependency_near_cycle</code>, <code>proj.critical_path_recomputed</code>.</li><li>TASK-PROJ-017 (Brief Modal): <code>proj.brief_modal_opened</code>.</li><li>TASK-PROJ-018 (design tokens): n/a — visual/CI task; no audit rows. Adding a new <code>ai.*</code> kind requires (a) a Rust enum variant, (b) a serde-derive update, (c) a unit test, (d) a docs cross-reference to the kind's purpose, (e) a one-line update to this clause. Adding a new <code>chat.*</code> or <code>proj.*</code> kind requires the same plus a one-line append to the per-task sublist above so this clause stays the closed-set source of truth across the platform.</li></ul>\n<ol><li><strong>MUST</strong> NOT bypass the Writer to write directly to <code>&lt;memory-root&gt;/audit/*.binlog</code> or <code>&lt;memory-root&gt;/HEAD</code>. The Writer is the only legal mutator (per <code>AGENTS.md §14.1</code>).</li><li><strong>MUST</strong> run a startup health check via <code>python3 -m cyberos.writer --version</code> as a synchronous subprocess call during AI Gateway init. Non-zero exit MUST cause the gateway to exit 1 with stderr <code>memory_writer: Writer subprocess unavailable: &lt;reason&gt;</code>. This catches misconfigured deploys at deploy time, not first-request time.</li><li><strong>MUST NOT</strong> expose <code>memory_writer::emit_batch()</code> in slice 1. Callers needing N rows MUST call <code>emit()</code> N times (or use <code>futures::future::join_all</code>). TASK-AI-008 (slice 2 PyO3 spike) re-introduces a real batching API.</li><li><strong>MUST</strong> log <code>ChainHashMismatch</code> errors at sev-1 via <code>tracing::error!</code> with structured fields: <code>expected_chain</code> (hex), <code>actual_chain</code> (hex), <code>seq</code>, <code>tenant_id</code>, <code>payload_canonical_hash</code>. The OBS dashboard (TASK-OBS-007) routes sev-1 chain events to PagerDuty until TASK-OBS-007 lands; until then, <code>tracing::error!</code> lines surface via journald.</li><li><strong>SHOULD</strong> keep chain-hash verification ON by default in both dev and prod. The 5µs per emit cost is negligible compared to the 30ms subprocess fork. A feature flag <code>memory_writer/skip-verify</code> exists for ad-hoc debugging but MUST NOT be enabled in prod (CI gate enforces).</li><li><strong>SHOULD</strong> emit OTel metrics: <code>memory_writer_emit_calls_total{kind,outcome}</code> (counter; outcome ∈ ok/err), <code>memory_writer_emit_latency_ms</code> (histogram), <code>memory_writer_chain_mismatches_total</code> (counter; sev-1 dashboard alarm at &gt; 0), <code>memory_writer_writer_unavailable_total</code> (counter), <code>memory_writer_path_rejections_total{reason}</code> (counter).</li><li><strong>MUST NOT</strong> expose a <code>dry_run</code> mode in slice 1. TASK-AI-021 (operator CLI) adds a <code>cyberos-ai memory emit --dry-run</code> subcommand that validates payload without emission; the bridge itself stays single-purpose.</li><li><strong>MUST</strong> emit history events as pairs per task-audit skill §3.8 rule 26: every <code>ai.precheck_started</code> row MUST be followed by either <code>ai.precheck_completed</code> OR <code>ai.precheck_failed</code> within 30s (the precheck deadline). The same pairing rule applies to <code>ai.reconcile_started</code> / <code>ai.reconcile_completed | ai.reconcile_failed</code>. Standalone <code>*_started</code> rows are crash signals — an OBS Grafana lint alerts when <code>count(*_started) - count(*_completed) - count(*_failed) &gt; 0</code> over any 5-minute window for any (tenant, module).</li><li><strong>MUST</strong> include <code>extra.trace_id: String</code> (32-char lower-hex W3C <code>trace-id</code> form per task-audit skill §3.7 rule 23) in every emitted <code>MemoryRow</code>. The format MUST be produced via OTel <code>TraceId</code> Display (NOT Debug — Debug yields <code>TraceId(0af7…)</code> while Display yields the 32-char hex per task-audit skill §3.7 rule 24). AC #17 verifies via regex <code>^[0-9a-f]{32}$</code> against every emitted row's <code>extra.trace_id</code> in the round-trip test.</li></ol>\n<p>This task is the load-bearing piece beneath every other AI Gateway audit emission. It is also the slowest part of the call hot path (subprocess fork dominates the precheck's 50ms budget) — getting it right matters for both correctness and throughput.</p>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a subprocess and not an in-process Python binding (PyO3)?</strong> Three reasons. (1) The canonical Writer is the <em>only</em> code authorised to mutate <code>&lt;memory-root&gt;/</code>; running it as a separate process gives us strong isolation and a single, auditable code path for every memory write across the whole platform. (2) PyO3 adds a build-time dependency on a specific Python version and a runtime dependency on the GIL; both make the AI Gateway harder to ship and harder to test. (3) The 30ms subprocess overhead is acceptable for slice 1; TASK-AI-008 (slice 2 spike) revisits PyO3 if real-world traffic justifies it.</p>\n<p><strong>Why call it synchronously on the precheck/reconcile hot path?</strong> The audit-before-action invariant (<code>AGENTS.md §11</code>, research review §2.4) requires that the audit row reach the chain <em>before</em> the action it guards. An async-spawn-and-return pattern would let a precheck <code>Allow</code> return before its <code>ai.precheck</code> row lands; a process crash between those two events would leave us with a chain that doesn't match what the gateway actually did. Synchronous emission costs us 30ms; async emission would cost us the integrity of the chain. Pick correctness.</p>\n<p><strong>Why is the chain-hash verification step needed?</strong> The canonical Writer is trusted, but verification is cheap (one SHA-256 over ~600 bytes ≈ 5µs) and catches an entire class of bugs: a Writer that silently drops fields, a Writer that injects fields, a Writer whose canonical-JSON serialiser diverges from the AI Gateway's. The mismatch would silently break the chain; better to refuse the row and fail loudly.</p>\n<p><strong>Why route every kind through one bridge instead of one bridge per kind?</strong> Future kinds will land (<code>ai.cache_hit</code>, <code>ai.failover_triggered</code>, <code>ai.persona_stamp_mismatch</code>). One bridge with an enum gives us a single audit-emission entry point that every CI gate, every integration test, and every prod observability dashboard can hook into. A bridge-per-kind ecology fragments faster than a Vietnamese coffee shop's signage.</p>\n<p><strong>Why does the bridge not own retries?</strong> Because the only retryable failures (<code>.lock</code> contention, Writer subprocess transient SIGTERM) are already retried inside the Writer itself. From the AI Gateway's perspective, a <code>MemoryWriterError</code> is final: either the row landed (Ok) or it didn't (Err); there is no \"maybe\". The caller's retry policy lives in the caller's domain (e.g., precheck retries the entire <code>precheck()</code> call, not just the audit emission).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Public function signatures</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/memory_writer.rs\npub async fn emit(emit_request: MemoryEmit) -&gt; Result&lt;EmittedRow, MemoryWriterError&gt;;\n\n// Slice-1 startup health check\npub async fn check_writer_available() -&gt; Result&lt;WriterVersion, MemoryWriterError&gt;;\n\npub struct MemoryEmit {\n    pub kind: AiInvocationKind,\n    pub path: String,                  // memory file path under &lt;memory-root&gt;/, validated by Writer\n    pub extra: serde_json::Value,      // closed schema per kind; validated by the typed builder below\n    // NOTE: dedup_key is deferred to TASK-AI-008 (slice 2 PyO3 spike). Slice-1 callers must\n    // tolerate duplicate audit rows on crash recovery; TASK-AI-021 operator CLI provides a\n    // `cyberos-ai expiry repair` command to dedupe after the fact.\n}\n\npub struct WriterVersion {\n    pub semver: String,\n    pub commit: String,\n    pub schema_version: u32,\n}\n\npub enum AiInvocationKind {\n    Precheck,           // emitted by TASK-AI-001\n    Invocation,         // emitted by TASK-AI-002 (success path)\n    InvocationFailed,   // emitted by TASK-AI-002 (refund path)\n    HoldExpired,        // emitted by TASK-AI-004 (cleanup job)\n    PersonaLoaded,      // emitted by TASK-AI-014 (persona stamping)\n}\n\npub struct EmittedRow {\n    pub seq: u64,\n    pub ts_ns: u64,\n    pub chain: [u8; 32],\n    pub path: String,\n}\n\npub enum MemoryWriterError {\n    WriterFailed { exit_code: i32, stderr: String },\n    WriterUnreachable { reason: String },\n    CanonicalisationFailed { reason: String },\n    ChainHashMismatch { expected: [u8; 32], got: [u8; 32] },\n    PathRejected { path: String, reason: String },  // path traversal, invalid kind, etc.\n    Timeout { waited_ms: u32 },                     // subprocess hung &gt; 5s\n}</code></pre>\n<h3>Typed builders (closed schema per kind)</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/memory_writer/canonical.rs\n\npub fn precheck(\n    tenant_id: &amp;str,\n    agent_persona: &amp;str,\n    model_alias: &amp;str,\n    resolved_provider: &amp;str,\n    resolved_model: &amp;str,\n    estimated_usd: Decimal,\n    current_spent_usd: Decimal,\n    idempotency_key: &amp;str,\n) -&gt; MemoryEmit { /* … */ }\n\npub fn invocation(\n    tenant_id: &amp;str,\n    agent_persona: &amp;str,\n    model_alias: &amp;str,\n    resolved_provider: &amp;str,\n    resolved_model: &amp;str,\n    prompt_tokens: u32,\n    completion_tokens: u32,\n    actual_usd: Decimal,\n    hold_id: Uuid,\n    latency_ms: u32,\n    cache_state: CacheState,\n    provider_request_id: &amp;str,\n    new_spent_total_usd: Decimal,\n    warn_crossed: bool,\n    cancelled: bool,\n) -&gt; MemoryEmit { /* … */ }\n\npub fn invocation_failed(\n    tenant_id: &amp;str,\n    agent_persona: &amp;str,\n    resolved_provider: &amp;str,\n    resolved_model: &amp;str,\n    http_status: u16,\n    retryable: bool,\n    provider_error_message: &amp;str,\n    hold_id: Uuid,\n    refund_amount_usd: Decimal,\n) -&gt; MemoryEmit { /* … */ }\n\npub fn hold_expired(\n    tenant_id: &amp;str,\n    hold_id: Uuid,\n    expired_at: DateTime&lt;Utc&gt;,\n    refund_amount_usd: Decimal,\n) -&gt; MemoryEmit { /* … */ }\n\npub fn persona_loaded(\n    tenant_id: &amp;str,\n    persona_id: &amp;str,\n    persona_version: &amp;str,\n    source_path: &amp;str,\n    source_hash: [u8; 32],\n) -&gt; MemoryEmit { /* … */ }</code></pre>\n<p>The typed builders prevent any AI Gateway code from emitting a row with the wrong shape — the call-site is statically constrained.</p>\n<h3>Subprocess handshake</h3>\n<pre><code class=\"language-text\">spawn:        python3 -m cyberos.writer put\nstdin:        { \"path\": \"...\", \"body\": \"...\", \"meta\": { \"kind\": \"...\", ... } }\n              (single line of canonical JSON, terminated by '\\n')\nexpected exit: 0 on success, 1 on schema rejection, 2 on lock contention, 3 on path traversal\nstdout (on 0): { \"seq\": &lt;u64&gt;, \"ts_ns\": &lt;u64&gt;, \"chain\": \"&lt;hex&gt;\" }\n              (single line, terminated by '\\n')\nstderr (on 0): empty (debug logs go to syslog via the Writer's own logging)\nstderr (on &gt;0): JSON error: { \"code\": \"&lt;id&gt;\", \"detail\": \"&lt;text&gt;\" }\ntimeout:      5 seconds; if exceeded, kill -SIGTERM, then kill -SIGKILL after 1s grace</code></pre>\n<h3>Canonical JSON shape per AGENTS.md §6.2</h3>\n<pre><code class=\"language-json\">{\n  \"path\": \"memories/decisions/ai-invocations/1763112131000_org-cyberskill_01HZK9R7A2B4C8D6.md\",\n  \"body\": \"---\\nkind: ai.invocation\\ntenant_id: org:cyberskill\\n…\\n---\\n\",\n  \"meta\": {\n    \"kind\": \"ai.invocation\",\n    \"actor\": \"agent:cyberos-ai-gateway\",\n    \"actor_version\": \"0.1.0\",\n    \"extra\": {\n      \"tenant_id\": \"org:cyberskill\",\n      \"agent_persona\": \"cuo-cpo@0.4.1\",\n      \"actual_usd\": 0.0078,\n      \"prompt_tokens\": 120,\n      \"completion_tokens\": 450,\n      \"hold_id\": \"01HZK9R8M3X5C8Q4\"\n    }\n  }\n}</code></pre>\n<p>(Sorted keys; NFC UTF-8; no insignificant whitespace; integers natural form.)</p>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>Happy path (precheck row)</strong> — <code>memory_writer::emit(canonical::precheck(...))</code> MUST return <code>Ok(EmittedRow { seq, ts_ns, chain })</code>. The row MUST appear at <code>&lt;memory-root&gt;/memories/ai-invocations/&lt;ts_ns&gt;_&lt;tenant&gt;_&lt;key&gt;.md</code>. The returned <code>seq</code> MUST equal the memory's HEAD seq counter immediately after emission. The chain hash MUST equal SHA-256(canonical(row_minus_chain) ‖ prev_chain).</li><li><strong>Concurrent emission serialises</strong> — 16 tokio tasks calling <code>emit()</code> concurrently MUST result in 16 distinct rows on the chain, with monotonically increasing seq numbers and <code>chain</code> linkage preserved end-to-end. Test: parallel <code>for i in 0..16 { tokio::spawn(emit(...)) }</code>; assert seq is contiguous and chain verification passes for all 16 rows.</li><li><strong>Writer non-zero exit → Err</strong> — When the subprocess returns exit code <code>3</code> (path traversal), <code>emit()</code> MUST return <code>Err(MemoryWriterError::PathRejected)</code> carrying the stderr's <code>detail</code> field; MUST NOT emit a row; MUST NOT mutate <code>&lt;memory-root&gt;/</code>.</li><li><strong>Writer timeout → kill + Err</strong> — When the subprocess hangs for &gt;5s (test injects a <code>time.sleep(10)</code> via a Writer fixture), <code>emit()</code> MUST send SIGTERM, wait 1s, send SIGKILL, and return <code>Err(MemoryWriterError::Timeout { waited_ms: 5000 })</code>. MUST NOT leave a zombie process; MUST NOT leave a partial row on disk.</li><li><strong>Chain-hash mismatch → Err</strong> — When the Writer returns a <code>chain</code> that does NOT match the AI Gateway's recomputation (test injects a corrupted-Writer fixture), <code>emit()</code> MUST return <code>Err(MemoryWriterError::ChainHashMismatch)</code>; MUST log a sev-1 OBS event; MUST NOT return <code>Ok</code>.</li><li><strong>Canonical-JSON divergence → Err</strong> — When the payload contains non-NFC UTF-8 (test injects <code>\"café\"</code> with combining-acute instead of pre-composed), <code>emit()</code> MUST normalise to NFC before sending. Round-trip: payload-in → canonical → payload-out → assert NFC-form equality.</li><li><strong>Path validation</strong> — <code>MemoryEmit { path: \"../escape.md\", ... }</code> MUST be rejected at the bridge boundary (BEFORE subprocess spawn) with <code>Err(MemoryWriterError::PathRejected { reason: \"traversal\" })</code>. Same for absolute paths and paths under reserved directories (<code>audit/</code>, <code>index/</code>).</li><li><strong>Latency budget</strong> — <code>emit()</code> MUST complete within 50ms p95 over a 1000-call integration test against a real memory store on tmpfs. Per-call breakdown logged via <code>tracing</code> spans for TASK-AI-022.</li><li><strong>Typed-builder fidelity</strong> — Each of the 5 typed builders, when emitted, MUST produce a row whose <code>extra</code> matches the builder's input args 1:1 (no field renames, no silent type coercions). Test: round-trip <code>canonical::invocation(...)</code> → emit → re-read the row from disk → assert field equality.</li><li><strong>No-leak under panic</strong> — If the caller's tokio task is cancelled mid-emit, the subprocess MUST still be reaped (Drop impl on <code>EmitGuard</code>). Test: spawn 100 emits, cancel half at random points; assert <code>ps -ef | grep cyberos.writer</code> returns 0 after a 1s settling period.</li></ol>\n<hr>\n<h2>§5 — Verification method</h2>\n<p><strong>Unit tests:</strong> <code>services/ai-gateway/src/memory_writer/canonical.rs</code> has 5 typed-builder unit tests (one per kind) verifying that the builder produces the documented JSON shape with sorted keys.</p>\n<p><strong>Integration tests:</strong> <code>services/ai-gateway/tests/memory_writer_test.rs</code></p>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn emit_precheck_happy_path() {\n    let env = TestMemory::new_tmpfs().await;\n    let outcome = memory_writer::emit(\n        canonical::precheck(\n            \"org:test-a\", \"cuo-cpo@0.4.1\", \"chat.smart\",\n            \"bedrock\", \"anthropic.claude-3.5-sonnet\",\n            dec!(0.0085), dec!(47.23), \"01HZK9R7A2B4C8D6\",\n        )\n    ).await.unwrap();\n\n    assert!(outcome.seq &gt; 0);\n    assert!(env.row_exists_at(&amp;outcome.path).await);\n    let on_disk = env.read_row(outcome.seq).await;\n    assert_eq!(on_disk.chain, outcome.chain);\n    assert!(env.verify_chain_from_genesis().await);\n}\n\n#[tokio::test]\nasync fn emit_concurrent_serialises() {\n    let env = TestMemory::new_tmpfs().await;\n    let handles: Vec&lt;_&gt; = (0..16).map(|i| {\n        let path = format!(\"test_concurrent_{}\", i);\n        tokio::spawn(memory_writer::emit(test_emit_with_path(&amp;path)))\n    }).collect();\n\n    let outcomes: Vec&lt;_&gt; = futures::future::join_all(handles).await\n        .into_iter().map(|r| r.unwrap().unwrap()).collect();\n\n    let mut seqs: Vec&lt;u64&gt; = outcomes.iter().map(|o| o.seq).collect();\n    seqs.sort();\n    for w in seqs.windows(2) { assert_eq!(w[1], w[0] + 1, \"seq gap\"); }\n    assert!(env.verify_chain_from_genesis().await);\n}\n\n#[tokio::test]\nasync fn emit_writer_timeout_returns_err() {\n    let env = TestMemory::with_hung_writer().await;\n    let outcome = memory_writer::emit(test_emit()).await;\n    assert!(matches!(outcome, Err(MemoryWriterError::Timeout { .. })));\n    assert_no_zombie_writer().await;\n}\n\n#[tokio::test]\nasync fn emit_chain_hash_mismatch_rejects() {\n    let env = TestMemory::with_corrupted_writer().await;\n    let outcome = memory_writer::emit(test_emit()).await;\n    assert!(matches!(outcome, Err(MemoryWriterError::ChainHashMismatch { .. })));\n}\n\n#[tokio::test]\nasync fn emit_path_traversal_rejects_before_spawn() {\n    let outcome = memory_writer::emit(MemoryEmit {\n        kind: AiInvocationKind::Precheck,\n        path: \"../../etc/passwd\".to_string(),\n        extra: json!({}),\n    }).await;\n    assert!(matches!(outcome, Err(MemoryWriterError::PathRejected { .. })));\n    // Writer subprocess MUST NOT have been spawned\n    assert_eq!(WRITER_SPAWN_COUNT.load(Ordering::Relaxed), 0);\n}\n\n#[tokio::test]\nasync fn emit_normalises_nfc() {\n    let env = TestMemory::new_tmpfs().await;\n    // \"café\" with COMBINING ACUTE (U+0065 U+0301), not pre-composed (U+00E9)\n    let payload_with_decomposed = \"cafe\\u{0301}\";\n    let outcome = memory_writer::emit(test_emit_with_extra_string(payload_with_decomposed)).await.unwrap();\n    let on_disk = env.read_row(outcome.seq).await;\n    // After NFC normalisation, the body bytes should contain U+00E9 (0xC3 0xA9 in UTF-8)\n    assert!(on_disk.body_bytes.windows(2).any(|w| w == [0xC3, 0xA9]),\n        \"expected NFC-normalised pre-composed é\");\n    assert!(!on_disk.body_bytes.windows(2).any(|w| w == [0xCC, 0x81]),\n        \"expected combining-acute U+0301 to be normalised away\");\n}</code></pre>\n<p>Run via:</p>\n<pre><code class=\"language-bash\">cd /Users/stephencheng/Projects/CyberSkill/cyberos\ncargo test -p cyberos-ai-gateway memory_writer</code></pre>\n<p><strong>Property test:</strong> chain integrity property — for any sequence of N emits (N in 1..50), the chain hash at row N MUST equal SHA-256(canonical(row_N_without_chain) ‖ chain_{N-1}). Run via <code>cargo proptest</code>.</p>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/memory_writer.rs\n\nuse std::process::Stdio;\nuse tokio::io::{AsyncReadExt, AsyncWriteExt};\nuse tokio::process::Command;\nuse tokio::time::{timeout, Duration};\nuse sha2::{Sha256, Digest};\n\nconst WRITER_BIN: &amp;str = \"python3\";\nconst WRITER_ARGS: &amp;[&amp;str] = &amp;[\"-m\", \"cyberos.writer\", \"put\"];\nconst WRITER_TIMEOUT: Duration = Duration::from_secs(5);\n\npub async fn emit(req: MemoryEmit) -&gt; Result&lt;EmittedRow, MemoryWriterError&gt; {\n    // 1. Validate path before spawning\n    validate_path(&amp;req.path).map_err(|reason| MemoryWriterError::PathRejected {\n        path: req.path.clone(), reason,\n    })?;\n\n    // 2. Canonicalise payload\n    let body = build_body_markdown(&amp;req)?;\n    let payload = canonical::serialise(&amp;CanonicalPayload {\n        path: &amp;req.path,\n        body: &amp;body,\n        meta: &amp;Meta::for_kind(&amp;req.kind, &amp;req.extra),\n    }).map_err(|e| MemoryWriterError::CanonicalisationFailed { reason: e })?;\n\n    // 3. Spawn Writer\n    let mut child = Command::new(WRITER_BIN)\n        .args(WRITER_ARGS)\n        .stdin(Stdio::piped())\n        .stdout(Stdio::piped())\n        .stderr(Stdio::piped())\n        .spawn()\n        .map_err(|e| MemoryWriterError::WriterUnreachable { reason: e.to_string() })?;\n\n    let stdin = child.stdin.take().unwrap();\n    let stdout = child.stdout.take().unwrap();\n    let stderr = child.stderr.take().unwrap();\n\n    // 4. Pipe payload + drop stdin to signal EOF\n    let payload_bytes = format!(\"{}\\n\", payload).into_bytes();\n    let write_fut = async move {\n        let mut stdin = stdin;\n        stdin.write_all(&amp;payload_bytes).await?;\n        stdin.shutdown().await?;\n        Ok::&lt;_, std::io::Error&gt;(())\n    };\n\n    // 5. Read stdout + stderr concurrently with timeout\n    let (write_res, exit_res, stdout_buf, stderr_buf) = match timeout(WRITER_TIMEOUT, async {\n        let (w, e, so, se) = tokio::join!(\n            write_fut,\n            child.wait(),\n            read_all(stdout),\n            read_all(stderr),\n        );\n        (w, e, so, se)\n    }).await {\n        Ok(t) =&gt; t,\n        Err(_) =&gt; {\n            // Kill subprocess on timeout\n            let _ = child.start_kill();\n            tokio::time::sleep(Duration::from_secs(1)).await;\n            return Err(MemoryWriterError::Timeout { waited_ms: 5000 });\n        }\n    };\n\n    write_res.map_err(|e| MemoryWriterError::WriterUnreachable { reason: e.to_string() })?;\n    let exit = exit_res.map_err(|e| MemoryWriterError::WriterUnreachable { reason: e.to_string() })?;\n\n    if !exit.success() {\n        return Err(MemoryWriterError::WriterFailed {\n            exit_code: exit.code().unwrap_or(-1),\n            stderr: String::from_utf8_lossy(&amp;stderr_buf?).to_string(),\n        });\n    }\n\n    // 6. Parse stdout → EmittedRow\n    let row: WriterStdout = serde_json::from_slice(&amp;stdout_buf?)\n        .map_err(|e| MemoryWriterError::WriterFailed {\n            exit_code: 0, stderr: format!(\"stdout parse: {}\", e),\n        })?;\n\n    // 7. Verify chain hash\n    let expected = compute_expected_chain(&amp;payload, &amp;row.prev_chain);\n    let actual = hex::decode(&amp;row.chain).map_err(|_| MemoryWriterError::ChainHashMismatch {\n        expected, got: [0u8; 32],\n    })?;\n    if expected[..] != actual[..] {\n        let mut got = [0u8; 32];\n        got.copy_from_slice(&amp;actual);\n        return Err(MemoryWriterError::ChainHashMismatch { expected, got });\n    }\n\n    Ok(EmittedRow { seq: row.seq, ts_ns: row.ts_ns, chain: expected, path: req.path })\n}\n\nfn validate_path(path: &amp;str) -&gt; Result&lt;(), String&gt; {\n    if path.starts_with('/') || path.starts_with('\\\\') { return Err(\"absolute\".into()); }\n    if path.contains(\"..\") { return Err(\"traversal\".into()); }\n    for reserved in [\"audit/\", \"index/\", \"HEAD\", \".lock\"] {\n        if path.starts_with(reserved) { return Err(format!(\"reserved: {}\", reserved)); }\n    }\n    Ok(())\n}\n\nfn compute_expected_chain(canonical_payload: &amp;str, prev_chain_hex: &amp;str) -&gt; [u8; 32] {\n    let prev = hex::decode(prev_chain_hex).unwrap_or_default();\n    let mut hasher = Sha256::new();\n    hasher.update(canonical_payload.as_bytes());\n    hasher.update(&amp;prev);\n    hasher.finalize().into()\n}</code></pre>\n<p><em>Scaffold above is suggestive. AC §4 is the contract.</em></p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Code dependencies:</strong></p>\n<ul><li><code>cyberos.writer</code> Python module — already shipped as part of memory Layer 1. The AI Gateway invokes it as a subprocess.</li><li><code>cyberos.canonical</code> Python serialiser — used by the Writer; the AI Gateway side has its own Rust impl that MUST produce byte-identical output.</li></ul>\n<p><strong>Concept dependencies:</strong></p>\n<ul><li><code>AGENTS.md §3, §4, §6</code> — file ops, atomic write, audit ledger format.</li><li>Memory path schema (<code>memory.schema.json#/definitions/MemoryPath</code>).</li></ul>\n<p><strong>Operational dependencies:</strong></p>\n<ul><li><code>python3</code> ≥ 3.11 on the PATH at runtime (containers ship it; CI ensures it).</li><li>A live <code>&lt;memory-root&gt;/</code> (test fixtures use tmpfs).</li></ul>\n<p><strong>Versioning:</strong> the Writer's CLI contract is pinned. If the Writer changes its stdin/stdout shape, this task must be re-audited. The contract version travels in the memory manifest under <code>manifest.writer_cli_version</code>.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Caller in TASK-AI-001 (precheck)</h3>\n<pre><code class=\"language-rust\">memory_writer::emit(\n    canonical::precheck(\n        &amp;req.tenant_id,\n        &amp;req.agent_persona,\n        &amp;req.model_alias,\n        &amp;resolved_provider,\n        &amp;resolved_model,\n        estimated_usd,\n        current.spent_usd,\n        &amp;req.idempotency_key,\n    )\n).await.map_err(|e| PrecheckError::MemoryWriterFailed { stderr: e.to_string() })?;</code></pre>\n<h3>Subprocess stdin (canonical JSON, single line, NL-terminated)</h3>\n<pre><code>{\"meta\":{\"actor\":\"agent:cyberos-ai-gateway\",\"actor_version\":\"0.1.0\",\"extra\":{\"agent_persona\":\"cuo-cpo@0.4.1\",\"current_spent_usd\":47.23,\"estimated_usd\":0.0085,\"idempotency_key\":\"01HZK9R7A2B4C8D6\",\"model_alias\":\"chat.smart\",\"resolved_model\":\"anthropic.claude-3.5-sonnet\",\"resolved_provider\":\"bedrock\",\"tenant_id\":\"org:cyberskill\"},\"kind\":\"ai.precheck\"},\"body\":\"---\\nkind: ai.precheck\\nactor: agent:cyberos-ai-gateway\\ntenant_id: org:cyberskill\\n…\\n---\\n\",\"path\":\"memories/ai-invocations/1763112131000_org-cyberskill_01HZK9R7A2B4C8D6.md\"}</code></pre>\n<h3>Subprocess stdout (canonical JSON, single line, NL-terminated)</h3>\n<pre><code>{\"chain\":\"a3f9c8d7e6b5a4f3e2d1c0b9a8f7e6d5c4b3a2f1e0d9c8b7a6f5e4d3c2b1a0f9\",\"prev_chain\":\"7f8e9d0c1b2a3940516273849506a7b8c9dae0f12345678987654321fedcba98\",\"seq\":18421,\"ts_ns\":1763112131000000000}</code></pre>\n<h3>EmittedRow returned to caller</h3>\n<pre><code class=\"language-rust\">EmittedRow {\n    seq: 18421,\n    ts_ns: 1763112131000000000,\n    chain: [0xa3, 0xf9, /* … 30 more bytes */ 0xf9],\n    path: \"memories/ai-invocations/1763112131000_org-cyberskill_01HZK9R7A2B4C8D6.md\".to_string(),\n}</code></pre>\n<h3>Subprocess error case (exit code 3, path traversal)</h3>\n<pre><code>stderr: {\"code\":\"path_traversal\",\"detail\":\"path '../escape.md' resolves outside &lt;memory-root&gt;\"}</code></pre>\n<p>The bridge surfaces this as:</p>\n<pre><code class=\"language-rust\">Err(MemoryWriterError::PathRejected { path: \"../escape.md\".to_string(), reason: \"path_traversal: …\".to_string() })</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved 2026-05-15 (round 2). Promoted to §1 normative clauses:</p>\n<ol><li><strong>~~emit_batch() in slice 1~~</strong> → §1 #11 (NOT exposed; callers loop). Was Q1.</li><li><strong>~~Chain-hash verification cost~~</strong> → §1 #13 (ON by default; feature flag for dev only). Was Q2.</li><li><strong>~~python3 missing at startup~~</strong> → §1 #10 (startup health check). Was Q3.</li><li><strong>~~OBS event on ChainHashMismatch~~</strong> → §1 #12 (sev-1 <code>tracing::error!</code>). Was Q4.</li><li><strong>~~dry_run mode~~</strong> → §1 #15 (NOT in bridge; TASK-AI-021 CLI subcommand). Was Q5.</li><li><strong>Chain-hash verification cost</strong> — 5µs per emit, but 16 concurrent emits could compound. Profile in §5 integration test; if it pushes p95 over 50ms, gate behind a feature flag (<code>memory_writer/verify-chain</code>) that defaults on in dev and off in prod. Decision: leave on by default, profile, revise if needed.</li><li><strong>What if <code>python3</code> isn't on the PATH?</strong> — Slice-1 proposal: hard-fail at AI Gateway startup with a clear error message. The runtime container MUST ship Python; the dev environment MUST install it. This is enforced at deploy time, not runtime.</li><li><strong>OBS event emission on ChainHashMismatch</strong> — Sev-1 (\"chain corruption\") is the right severity, but TASK-OBS-003 isn't shipped yet at this task's build time. Proposal: log a structured <code>chain_hash_mismatch</code> event via <code>tracing::error!</code>; OBS slice 1 (TASK-OBS-007) will route those to PagerDuty when it lands.</li><li><strong>Should the bridge expose a <code>dry_run</code> mode?</strong> — Useful for TASK-AI-021 (operator CLI) to validate a payload without emitting. Slice-1 proposal: yes, but as an opt-in flag on <code>emit_batch</code>, not on <code>emit</code>; keeps the hot path simple.</li></ol>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>HTTP / Return</th><th>Recovery</th></tr></thead><tbody><tr><td>Writer subprocess not on PATH at startup</td><td><code>check_writer_available()</code> non-zero exit</td><td>Gateway exits 1 with stderr</td><td>Operator installs python3; redeploys</td></tr><tr><td>Writer subprocess hangs (&gt; 5s)</td><td>tokio timeout fires</td><td><code>Err(MemoryWriterError::Timeout)</code>; SIGTERM then SIGKILL</td><td>Caller retries; if persistent, OBS sev-2 alert</td></tr><tr><td>Writer subprocess non-zero exit</td><td><code>child.wait()</code> returns non-success</td><td><code>Err(WriterFailed { exit_code, stderr })</code></td><td>Caller bubbles up; TASK-AI-021 <code>cyberos-ai memory doctor</code> diagnoses</td></tr><tr><td>Path traversal in caller-supplied path</td><td><code>validate_path</code> regex check</td><td><code>Err(PathRejected)</code> BEFORE spawn</td><td>Caller fixes; this is a programmer error</td></tr><tr><td>Reserved path (<code>audit/</code>, <code>index/</code>, <code>HEAD</code>, <code>.lock</code>)</td><td><code>validate_path</code> substring check</td><td><code>Err(PathRejected)</code> BEFORE spawn</td><td>Same as above</td></tr><tr><td>Canonical-JSON serialisation failure</td><td><code>canonical::serialise</code> returns Err</td><td><code>Err(CanonicalisationFailed)</code> BEFORE spawn</td><td>Caller fixes payload (most likely non-NFC UTF-8)</td></tr><tr><td>Chain-hash mismatch</td><td>Local recomputation diverges from Writer stdout</td><td><code>Err(ChainHashMismatch)</code> + sev-1 log</td><td>Sev-1 OBS event; investigate Writer; do NOT trust the row</td></tr><tr><td><code>python3</code> missing at runtime</td><td>Subprocess spawn errors <code>ENOENT</code></td><td><code>Err(WriterUnreachable)</code></td><td>Gateway should have caught at startup (§1 #10); if not, sev-1</td></tr><tr><td>Concurrent .lock contention</td><td>Writer subprocess waits on lock; latency increases</td><td>Eventually succeeds</td><td>Normal; no action needed unless p99 exceeds budget</td></tr><tr><td>Writer subprocess killed mid-write</td><td>child exits non-zero with partial row on disk</td><td><code>Err(WriterFailed)</code></td><td>Writer's own recovery handles partial row; bridge surfaces the error</td></tr><tr><td>OOM during subprocess</td><td>child SIGKILL'd by kernel</td><td><code>Err(WriterFailed { exit_code: -9 })</code></td><td>OBS sev-2; capacity review</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>This task is the single biggest latency contributor on the AI Gateway hot path (50ms of the gateway's ~200ms p95 budget). PyO3 (TASK-AI-008) could cut that to &lt;10ms; we defer the optimisation until real traffic confirms the win.</li><li>The Writer subprocess is the only path that holds <code>&lt;memory-root&gt;/.lock</code>. The AI Gateway never touches the lock directly — that's the rule from <code>AGENTS.md §14.1</code> made into code.</li><li>The five row kinds defined here (<code>ai.precheck</code>, <code>ai.invocation</code>, <code>ai.invocation_failed</code>, <code>ai.hold_expired</code>, <code>ai.persona_loaded</code>) are the closed initial set for slice 1. Subsequent tasks extend the closed set explicitly via §1 #8: <code>ai.failover_triggered</code> (TASK-AI-009), <code>ai.zdr_violation</code> (TASK-AI-015), <code>ai.residency_violation</code> (TASK-AI-016), <code>ai.cache_hit</code> (TASK-AI-017), and the <code>ai.cli_*</code> family (TASK-AI-021). Every new kind needs the same minimal PR shape — enum variant + typed builder + unit test + update to §1 #8.</li><li>The <code>actor</code> field is <code>agent:cyberos-ai-gateway</code> (not a human subject id). This matches <code>AGENTS.md</code> §11's distinction between trusted and untrusted authors: only the canonical Writer can write to the chain, but the <em>originating actor</em> on the row is the AI Gateway service.</li><li>This task is the first non-memory module to depend on the canonical Writer subprocess. Once it lands, the pattern can be lifted into other modules (CHAT, CUO, EMAIL) by duplicating the Rust module into each service crate. A later refactor will extract it into a shared <code>cyberos-memory-client</code> crate.</li></ul>\n<hr>\n<p><em>End of TASK-AI-003. Run <code>task-audit</code> next: <code>cargo run -p cyberos-skill-cli -- run task-audit --input '{\"fr_path\": \"docs/tasks/ai/TASK-AI-003-memory-audit-bridge/spec.md\"}'</code></em></p>";
