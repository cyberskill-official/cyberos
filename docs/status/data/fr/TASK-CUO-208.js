window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-208"]="<h1>TASK-CUO-208: task template profile</h1>\n<h2>§1 - Description</h2>\n<p>Make the template decision explicit, per repo, in one place - instead of a plugin command, an author skill, and a rubric each implying a different answer.</p>\n<p>Normative clauses:</p>\n<ol><li>A reference <code>TEMPLATE_PROFILES.md</code> MUST define both templates normatively side by side: <code>engineering-spec@1</code> (frontmatter field set; <code>## §1 - Description</code> .. <code>## §11 - Implementation notes</code> section grammar; end marker) and <code>task@1</code> (its frontmatter incl. <code>template:</code> key; <code>## Summary</code> .. <code>## Dependencies</code> + conditional sections). Each profile lists its applicable audit rule families.</li><li><code>/create-tasks</code> MUST resolve the active template as: explicit per-invocation operator override, else <code>.cyberos/config.yaml</code> <code>fr_template</code> (TASK-CUO-207), else default <code>engineering-spec@1</code> - and MUST echo the resolved template in its PLAN so the operator approves template + content together.</li><li><code>task-author</code> MUST accept the template as an input-envelope field and emit the selected profile faithfully; its §12 authoring rules apply to engineering-spec@1, and TEMPLATE_PROFILES.md carries the equivalent authoring rules for task@1.</li><li><code>task-audit</code> MUST select rule families by detected template: task@1 -&gt; FM + SEC + COND + QA + SAFE (+ TRACE only where the grafted §4/§5 sections are present, as RUBRIC §9 already states); engineering-spec@1 -&gt; the §12 sub-rule set + TRACE-001..005 + QA + SAFE. The 10/10 verdict bar and needs_human semantics MUST be identical across templates. Template detection MUST come from the file itself (<code>template:</code> key present -&gt; task@1; §-section grammar -&gt; engineering-spec@1) and a file matching neither or both MUST be needs_human.</li><li>The plugin command doc MUST stop naming task@1 as THE format (current step-1 wording) and instead name the resolution chain from #2.</li><li>Mixed-template repos MUST be supported: the template is resolved per invocation batch, and the audit judges each file by its own detected template regardless of the repo default.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The two templates serve different consumers - engineering-spec@1 is the build-grade contract with traceability into tests; task@1 is the lighter product/compliance shape - so deleting either would break real users of the other. A per-repo default with per-file detection keeps the common path one-line simple while making the audit honest about what it is scoring. Defaulting to engineering-spec@1 follows the repo's own revealed preference (470:6) and the ship workflow's TRACE dependency.</p>\n<h2>§3 - Contract</h2>\n<p>Author input envelope gains: <code>\"template\": \"engineering-spec@1\" | \"task@1\"</code> (optional; default per §1 #2 chain). Audit output gains: <code>\"template_detected\"</code> echoing the per-file detection. PLAN echo line: <code>template: engineering-spec@1 (source: config)</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Profiles are complete and normative</strong> (§1 #1) - TEMPLATE_PROFILES.md defines both frontmatter sets, both section grammars, end markers, and each profile's rule-family list; the repo's newest exemplar of each template validates against its profile as written.</li><li><strong>Resolution chain honored</strong> (§1 #2) - fixtures: invocation override beats config; config beats default; absent both -&gt; engineering-spec@1; the PLAN echo names value + source in every case.</li><li><strong>Author emits both faithfully</strong> (§1 #3) - one sample task authored per template from the same interview fixture carries the correct sections and frontmatter for its profile (acceptance fixtures under the author skill).</li><li><strong>Audit families switch on detection</strong> (§1 #4) - a task@1 fixture missing <code>## Alternatives Considered</code> fails SEC-004; an engineering-spec@1 fixture missing §10 fails the §12 structural rule; the same 10/10 bar gates both.</li><li><strong>Ambiguity is needs_human</strong> (§1 #4) - a fixture with a <code>template: task@1</code> key AND §1..§11 sections routes to needs_human naming the conflict.</li><li><strong>Command doc updated</strong> (§1 #5) - create-tasks.md names the chain and no longer asserts a single format.</li><li><strong>Per-file judgment in mixed repos</strong> (§1 #6) - a batch containing one file of each template audits each against its own families (fixture pair, both pass).</li></ol>\n<h2>§5 - Verification</h2>\n<p>Acceptance-driven (contract work):</p>\n<ul><li><code>modules/skill/task-author/references/TEMPLATE_PROFILES.md</code> - carries its own \"verify this document\" preamble: the two exemplar-validation checklists used by AC 1.</li><li>Author acceptance fixtures (extend <code>modules/skill/task-author/acceptance/TRIGGER_TESTS.md</code> with the template-resolution cases) - AC 2, 3.</li><li>Audit acceptance fixtures (extend <code>modules/skill/task-audit/acceptance/TRIGGER_TESTS.md</code> with the four detection/family cases) - AC 4, 5, 7.</li><li>Doc assertion for AC 6: create-tasks.md contains the resolution chain wording; <code>grep -c \"task@1 task markdowns\"</code> returns 0.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<p>TEMPLATE_PROFILES.md: two mirrored halves + a comparison table + the detection rules. SKILL.md diffs: input-envelope field, PLAN echo, pointer to profiles. RUBRIC.md: a short \"family selection by template\" preamble above the existing families (no rule rewrites - FM-004 stays as-is for task@1; engineering-spec files are simply not subject to FM-004).</p>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-CUO-207 (<code>fr_template</code> config key). Composes with TASK-CUO-205 (same command doc, different step - #5 here touches step 1, 205 touches step 3).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>PLAN (4 tasks) - template: engineering-spec@1 (source: default)\n  TASK-ACME-001-payment-webhooks (product)\n  ...\napprove to write files.</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. A third template slot (client-specific house style) is deliberately out: profiles are code-reviewed contracts, not config-invented shapes; a new template means a new TEMPLATE_PROFILES.md entry by task.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Config says task@1 but the repo's existing tasks are engineering-spec - per-file detection (#6) keeps audits honest; the PLAN echo warns when resolved template differs from the majority of existing tasks (informational line, not a block).</li><li>Author asked to CONVERT between templates - out of scope; the audit's needs_human on hybrids prevents silent half-conversions.</li><li>Rubric drift (a rule added to one family list but not the profile doc) - TEMPLATE_PROFILES.md's family lists cite RUBRIC.md family names, not rule IDs, so rule additions inherit automatically; family additions require touching both files and AC 1's checklist catches a miss.</li><li>Detection false-positive on prose containing \"## Summary\" - detection requires the frontmatter <code>template:</code> key for task@1, not the section alone (§1 #4); the section grammar is the tiebreaker only for the engineering-spec side.</li><li>External repo with zero config and zero existing tasks - default engineering-spec@1 + PLAN echo makes the choice visible at the approval gate where it is cheapest to change.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep FM-004 untouched (it correctly binds <code>template: task@1</code> files). The engineering-spec §12 sub-rules stay in the author SKILL.md as today; TEMPLATE_PROFILES.md references them rather than duplicating, so there is exactly one normative home per rule set.</p>\n<p><em>End of TASK-CUO-208.</em></p>";
