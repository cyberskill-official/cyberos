window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-259"]="<h1>FR-MEMORY-259: brain_common test harness fix so the brain DB suite runs (raw_sql + once-per-process migrate)</h1>\n<h2>1. Description</h2>\n<p>brain_common test harness fix so the brain DB suite runs (raw_sql + once-per-process migrate). Implemented on branch <code>auto/memory-enterprise</code> (commit 9808d0b). Filed during the memory hardening session as <code>MEM-059</code>, which post-dated the backlog migration and so had no migrated FR. Author the normative clauses from the implementation before this FR leaves draft.</p>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] the fix is covered by a passing test (see the implementing commit)</li></ul>";
