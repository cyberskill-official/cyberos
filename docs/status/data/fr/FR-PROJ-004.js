window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PROJ-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The issue lifecycle <strong>MUST</strong> be a strict FSM with the following 5 states and 13 legal transitions:</p>\n<pre><code>   ┌────────┐   ┌──────┐   ┌─────────────┐   ┌───────────┐   ┌─────┐\n   │backlog ├──▶│ todo ├──▶│ in_progress ├──▶│ in_review ├──▶│done │\n   └────────┘   └──────┘   └─────────────┘   └───────────┘   └─────┘\n       │           │              │                 │            ▲\n       │           │              │                 │            │\n       │           │              ▼                 ▼            │\n       │           │      ┌─────────────┐    ┌─────────────┐     │\n       └──────────────────┤  cancelled  │◀───┤             │     │\n                          └─────────────┘    └─────────────┘     │\n                                                  │              │\n                                  (re-open with reason; ──┐      │\n                                   from done|cancelled    │      │\n                                   to in_progress)        └──────┘</code></pre>\n<ol><li><strong>MUST</strong> define <code>IssueStatus</code> enum with exactly 6 variants: <code>Backlog</code>, <code>Todo</code>, <code>InProgress</code>, <code>InReview</code>, <code>Done</code>, <code>Cancelled</code>. No other variants permitted in v1.</li><li><strong>MUST</strong> allow ONLY these transitions:</li></ol>\n<ul><li><strong>Forward</strong>: <code>Backlog → Todo</code>, <code>Backlog → InProgress</code>, <code>Todo → InProgress</code>, <code>Todo → Cancelled</code>, <code>InProgress → InReview</code>, <code>InProgress → Cancelled</code>, <code>InProgress → Done</code>, <code>InReview → Done</code>, <code>InReview → InProgress</code> (revisions), <code>InReview → Cancelled</code>.</li><li><strong>Re-open</strong>: <code>Done → InProgress</code>, <code>Cancelled → InProgress</code>. BOTH require non-empty <code>reason</code> field (per DEC-253).</li><li><strong>No-op</strong>: same-state → same-state (allowed; treated as <code>Idempotent</code> not Transition). All other pairs MUST be rejected with <code>FsmError::IllegalTransition { from, to, allowed_from }</code> where <code>allowed_from</code> is the list of legal targets from current state.</li></ul>\n<ol><li><strong>MUST</strong> validate transitions server-side BEFORE the LWW scalar write. Validation order:</li><li>FSM check (this FR).</li><li>LWW timestamp check (FR-PROJ-003 §1 #6).</li><li>Apply. FSM failure → 422 UNPROCESSABLE_ENTITY with body <code>{\"error\":\"illegal_transition\",\"from\":&lt;s&gt;,\"to\":&lt;s&gt;,\"allowed_to\":[...]}</code>.</li><li><strong>MUST</strong> persist transition history in <code>issue_status_history</code> (per-tenant, append-only):</li></ol>\n<ul><li><code>(issue_id, transition_seq, from_status, to_status, by_subject_id, reason, transitioned_at_ns)</code>.</li><li><code>transition_seq</code> is monotonic per issue.</li><li><code>reason</code> REQUIRED for re-open transitions (Done|Cancelled → InProgress); SHOULD-be-empty otherwise (operators MAY annotate non-re-open transitions for context).</li></ul>\n<ol><li><strong>MUST</strong> emit <code>proj.issue_status_changed</code> memory audit row per accepted transition with payload <code>{issue_id, from, to, by_subject_id, reason, transition_seq, transitioned_at_ns, trace_id}</code>.</li><li><strong>MUST</strong> support <code>cyberos issue history &lt;id&gt;</code> CLI returning the full transition log for an issue (operator + auditor view).</li><li><strong>MUST</strong> expose REST endpoint <code>POST /api/proj/issues/:id/transition</code> with body <code>{to: IssueStatus, reason?: string}</code>. Returns 200 with <code>{transition_seq, applied_at_ns, current_status}</code> or 422 with FSM error.</li><li><strong>MUST</strong> share the FSM definition between Rust + TypeScript via build-time codegen:</li></ol>\n<ul><li><code>build.rs</code> writes <code>web/proj-client/src/lifecycle/allowed_transitions.ts</code> from the Rust source-of-truth.</li><li>CI gate (<code>ts-fsm-fresh</code>) asserts the generated file matches <code>cargo build</code>'s output (no drift).</li></ul>\n<ol><li><strong>MUST</strong> track per-status time-in-state for analytics (downstream of FR-PROJ-013 estimate calibration):</li></ol>\n<ul><li>On transition out of status X, compute <code>elapsed_ns = transitioned_at_ns - prior_transitioned_at_ns_for_this_issue</code>.</li><li>Emit metric <code>proj_issue_time_in_status_seconds{from_status}</code> (histogram).</li></ul>\n<ol><li><strong>MUST</strong> emit OTel span <code>proj.issue.transition</code> with attributes <code>issue_id</code>, <code>from</code>, <code>to</code>, <code>had_reason</code>, <code>transition_seq</code>, <code>duration_ms</code>.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_issue_transitions_total{from, to}</code> (counter; bounded cardinality 5×6=30).</li><li><code>proj_issue_transitions_rejected_total{reason}</code> (counter; reason ∈ illegal_transition | reason_required | stale_write | unauthorised).</li><li><code>proj_issue_reopens_total</code> (counter — operator visibility into re-open frequency per tenant).</li></ul>\n<ol><li><strong>MUST</strong> emit <code>proj.issue_reopened</code> row (separate kind from generic status_changed) on every Done|Cancelled → InProgress transition, with payload <code>{issue_id, from, reason, by_subject_id, reopen_seq, trace_id}</code> so dashboards can pivot on re-open patterns.</li><li><strong>SHOULD</strong> support per-engagement extended states via <code>state_groups[]</code> config (slice-3+; placeholder). v1 only ships the canonical 6.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why FSM not free-form (§1 #2, DEC-250)?</strong> Free-form status = chaos. Velocity dashboards assume <code>backlog → todo → in_progress → done</code> progression; arbitrary jumps break them. Audit trails need to answer \"did this issue go through review?\" — if jumps are allowed, the answer is non-deterministic. FSM enforces the workflow at the data layer.</p>\n<p><strong>Why these 6 states specifically (§1 #1)?</strong> Linear/Notion/Jira all converge on this pattern (renamed: <code>unstarted → started → completed</code>, etc.). Our names are operator-readable. 6 states is the empirical sweet spot — 4 is too coarse (where does \"ready for review\" go?); 8+ is over-engineered for most teams.</p>\n<p><strong>Why <code>cancelled</code> from todo OR in_progress, but not backlog (§1 #2)?</strong> Backlog items haven't been committed-to yet; deleting &gt; cancelling. Cancellation is a meaningful operator decision once work has started.</p>\n<p><strong>Why re-open requires reason (§1 #2 + #4, DEC-253)?</strong> Re-opens are exception cases — the team thought they were done; something changed. Auditors and managers ask \"why\" immediately. Capturing the reason at transition time = no later interview required.</p>\n<p><strong>Why <code>InReview → InProgress</code> is legal (§1 #2)?</strong> Reviewer requests changes; work returns to in_progress. Without this transition, reviewers have no way to send feedback; they'd flip Cancelled (wrong semantic).</p>\n<p><strong>Why pre-validate before LWW (§1 #3)?</strong> A client sending <code>from: todo, to: done</code> should fail FAST (semantic error). Going through LWW first means an old client with stale timestamp gets a 409 (stale_write) instead of the more informative 422 (illegal_transition). Order matters for UX.</p>\n<p><strong>Why per-issue transition_seq (§1 #4)?</strong> Monotonic seq lets dashboards query \"give me the 5th transition of this issue\" without timestamp math. Also: detects gaps (audit trail corruption).</p>\n<p><strong>Why separate <code>proj.issue_reopened</code> row (§1 #12)?</strong> Re-open frequency is a leading indicator of estimate quality + reviewer-completeness. Dashboarding it as a distinct event (vs filtering generic status_changed) makes the metric trivially queryable.</p>\n<p><strong>Why TS↔Rust codegen (§1 #8)?</strong> Two-language hardcoding drifts. Adding a state to Rust + forgetting to update TS = UI shows option, server rejects. Codegen + CI gate makes drift impossible.</p>\n<p><strong>Why time-in-status metric (§1 #9)?</strong> \"Average time in <code>in_review</code>\" is the leading indicator of review-team throughput. Captured at transition-out time = exact + cheap. Downstream of FR-PROJ-013 calibration.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>IssueStatus enum</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/lifecycle/mod.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, Serialize, Deserialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum IssueStatus {\n    Backlog,\n    Todo,\n    InProgress,\n    InReview,\n    Done,\n    Cancelled,\n}\n\nimpl IssueStatus {\n    pub fn all() -&gt; [Self; 6] {\n        use IssueStatus::*;\n        [Backlog, Todo, InProgress, InReview, Done, Cancelled]\n    }\n    pub fn is_terminal(self) -&gt; bool {\n        matches!(self, IssueStatus::Done | IssueStatus::Cancelled)\n    }\n}</code></pre>\n<h3>FSM</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/lifecycle/fsm.rs\nuse crate::lifecycle::IssueStatus;\nuse IssueStatus::*;\n\n/// Source of truth for legal transitions. CI gate generates TS mirror from this.\npub const LEGAL_TRANSITIONS: &amp;[(IssueStatus, IssueStatus)] = &amp;[\n    // Forward\n    (Backlog,    Todo),\n    (Backlog,    InProgress),       // skip Todo (urgent tasks)\n    (Todo,       InProgress),\n    (Todo,       Cancelled),\n    (InProgress, InReview),\n    (InProgress, Cancelled),\n    (InProgress, Done),              // skip review (small tasks)\n    (InReview,   Done),\n    (InReview,   InProgress),        // reviewer requests changes\n    (InReview,   Cancelled),\n    // Re-open (requires reason)\n    (Done,       InProgress),\n    (Cancelled,  InProgress),\n];\n\npub fn is_legal(from: IssueStatus, to: IssueStatus) -&gt; bool {\n    if from == to { return true; }   // no-op\n    LEGAL_TRANSITIONS.contains(&amp;(from, to))\n}\n\npub fn requires_reason(from: IssueStatus, to: IssueStatus) -&gt; bool {\n    from.is_terminal() &amp;&amp; to == IssueStatus::InProgress\n}\n\npub fn allowed_targets(from: IssueStatus) -&gt; Vec&lt;IssueStatus&gt; {\n    LEGAL_TRANSITIONS.iter()\n        .filter_map(|&amp;(f, t)| if f == from { Some(t) } else { None })\n        .collect()\n}</code></pre>\n<h3>Transition handler</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/lifecycle/transitions.rs\nuse crate::lifecycle::{IssueStatus, fsm};\n\n#[derive(Debug, thiserror::Error)]\npub enum FsmError {\n    #[error(\"illegal transition: {from:?} → {to:?}; allowed: {allowed_to:?}\")]\n    IllegalTransition { from: IssueStatus, to: IssueStatus, allowed_to: Vec&lt;IssueStatus&gt; },\n    #[error(\"reason required for re-open ({from:?} → {to:?})\")]\n    ReasonRequired { from: IssueStatus, to: IssueStatus },\n    #[error(\"stale write (LWW)\")]\n    StaleWrite,\n    #[error(\"database error: {0}\")]\n    Db(String),\n}\n\n#[derive(Clone, Debug, Serialize)]\npub struct TransitionApplied {\n    pub transition_seq:        i64,\n    pub applied_at_ns:         i64,\n    pub current_status:        IssueStatus,\n}\n\npub async fn apply_transition(\n    pool: &amp;sqlx::PgPool,\n    issue_id: uuid::Uuid,\n    requested_to: IssueStatus,\n    by_subject_id: uuid::Uuid,\n    reason: Option&lt;String&gt;,\n) -&gt; Result&lt;TransitionApplied, FsmError&gt; {\n    // 1. Read current state (with row lock for the duration of transaction)\n    let mut tx = pool.begin().await.map_err(|e| FsmError::Db(e.to_string()))?;\n    let row = sqlx::query!(\n        \"SELECT status as \\\"status: IssueStatus\\\",\n                status_updated_at_ns,\n                status_updated_by_subject_id\n         FROM issues WHERE id = $1 FOR UPDATE\",\n        issue_id\n    ).fetch_one(&amp;mut *tx).await.map_err(|e| FsmError::Db(e.to_string()))?;\n\n    let from = row.status;\n\n    // 2. FSM check\n    if !fsm::is_legal(from, requested_to) {\n        return Err(FsmError::IllegalTransition {\n            from, to: requested_to,\n            allowed_to: fsm::allowed_targets(from),\n        });\n    }\n\n    // 3. Reason check\n    if fsm::requires_reason(from, requested_to) &amp;&amp; reason.as_deref().map(str::trim).unwrap_or(\"\").is_empty() {\n        return Err(FsmError::ReasonRequired { from, to: requested_to });\n    }\n\n    // 4. No-op short-circuit\n    if from == requested_to {\n        return Ok(TransitionApplied {\n            transition_seq:  row.transition_seq.unwrap_or(0),\n            applied_at_ns:   row.status_updated_at_ns,\n            current_status:  from,\n        });\n    }\n\n    // 5. Compute time-in-state BEFORE update\n    let now_ns = chrono::Utc::now().timestamp_nanos_opt().unwrap();\n    let time_in_state_ns = now_ns - row.status_updated_at_ns;\n    metrics::histogram!(\"proj_issue_time_in_status_seconds\", \"from_status\" =&gt; format!(\"{from:?}\"))\n        .record(time_in_state_ns as f64 / 1e9);\n\n    // 6. Insert into history; update issue\n    let transition_seq: i64 = sqlx::query_scalar!(\n        \"INSERT INTO issue_status_history\n            (issue_id, transition_seq, from_status, to_status, by_subject_id, reason, transitioned_at_ns, tenant_id)\n         SELECT $1,\n                COALESCE((SELECT MAX(transition_seq) FROM issue_status_history WHERE issue_id = $1), 0) + 1,\n                $2, $3, $4, $5, $6, current_setting('app.tenant_id')::uuid\n         RETURNING transition_seq\",\n        issue_id, from as IssueStatus, requested_to as IssueStatus,\n        by_subject_id, reason, now_ns\n    ).fetch_one(&amp;mut *tx).await.map_err(|e| FsmError::Db(e.to_string()))?;\n\n    sqlx::query!(\n        \"UPDATE issues SET status = $1, status_updated_at_ns = $2, status_updated_by_subject_id = $3 WHERE id = $4\",\n        requested_to as IssueStatus, now_ns, by_subject_id, issue_id\n    ).execute(&amp;mut *tx).await.map_err(|e| FsmError::Db(e.to_string()))?;\n\n    tx.commit().await.map_err(|e| FsmError::Db(e.to_string()))?;\n\n    // 7. Emit audit rows\n    emit_memory_row(\"proj.issue_status_changed\", serde_json::json!({\n        \"issue_id\": issue_id, \"from\": from, \"to\": requested_to,\n        \"by_subject_id\": by_subject_id, \"reason\": reason,\n        \"transition_seq\": transition_seq,\n        \"transitioned_at_ns\": now_ns,\n        \"trace_id\": current_trace_id(),\n    })).await;\n    if from.is_terminal() &amp;&amp; requested_to == IssueStatus::InProgress {\n        emit_memory_row(\"proj.issue_reopened\", serde_json::json!({\n            \"issue_id\": issue_id, \"from\": from, \"reason\": reason,\n            \"by_subject_id\": by_subject_id,\n            \"reopen_seq\": transition_seq, \"trace_id\": current_trace_id(),\n        })).await;\n        metrics::counter!(\"proj_issue_reopens_total\").increment(1);\n    }\n    metrics::counter!(\"proj_issue_transitions_total\",\n        \"from\" =&gt; format!(\"{from:?}\"), \"to\" =&gt; format!(\"{requested_to:?}\")).increment(1);\n\n    Ok(TransitionApplied {\n        transition_seq, applied_at_ns: now_ns, current_status: requested_to,\n    })\n}</code></pre>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0004_issue_status_history.sql\n\nCREATE TABLE issue_status_history (\n    issue_id            UUID NOT NULL,\n    transition_seq      BIGINT NOT NULL,\n    from_status         TEXT NOT NULL,\n    to_status           TEXT NOT NULL,\n    by_subject_id       UUID NOT NULL,\n    reason              TEXT,\n    transitioned_at_ns  BIGINT NOT NULL,\n    tenant_id           UUID NOT NULL,\n    PRIMARY KEY (issue_id, transition_seq)\n);\nCREATE INDEX idx_issue_status_history_recent ON issue_status_history (issue_id, transition_seq DESC);\n\nCREATE POLICY issue_status_history_tenant_isolation ON issue_status_history\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>TypeScript mirror (codegen output)</h3>\n<pre><code class=\"language-typescript\">// web/proj-client/src/lifecycle/allowed_transitions.ts (GENERATED — do not edit)\n// regenerate via `cd services/proj-sync &amp;&amp; cargo build`\n\nexport type IssueStatus = 'backlog' | 'todo' | 'in_progress' | 'in_review' | 'done' | 'cancelled';\n\nexport const LEGAL_TRANSITIONS: readonly [IssueStatus, IssueStatus][] = [\n  ['backlog',    'todo'],\n  ['backlog',    'in_progress'],\n  ['todo',       'in_progress'],\n  ['todo',       'cancelled'],\n  ['in_progress','in_review'],\n  ['in_progress','cancelled'],\n  ['in_progress','done'],\n  ['in_review',  'done'],\n  ['in_review',  'in_progress'],\n  ['in_review',  'cancelled'],\n  ['done',       'in_progress'],\n  ['cancelled',  'in_progress'],\n] as const;\n\nexport function isLegal(from: IssueStatus, to: IssueStatus): boolean {\n  if (from === to) return true;\n  return LEGAL_TRANSITIONS.some(([f, t]) =&gt; f === from &amp;&amp; t === to);\n}\n\nexport function requiresReason(from: IssueStatus, to: IssueStatus): boolean {\n  return (from === 'done' || from === 'cancelled') &amp;&amp; to === 'in_progress';\n}\n\nexport function allowedTargets(from: IssueStatus): readonly IssueStatus[] {\n  return LEGAL_TRANSITIONS.filter(([f]) =&gt; f === from).map(([, t]) =&gt; t);\n}</code></pre>\n<h3>Status picker UI</h3>\n<pre><code class=\"language-typescript\">// web/proj-client/src/lifecycle/StatusPicker.tsx\nimport * as React from 'react';\nimport { IssueStatus, allowedTargets, requiresReason, isLegal } from './allowed_transitions';\n\ninterface Props {\n  issueId:    string;\n  current:    IssueStatus;\n  onChange:   (to: IssueStatus, reason?: string) =&gt; Promise&lt;void&gt;;\n}\n\nexport function StatusPicker({ issueId, current, onChange }: Props) {\n  const [pending, setPending] = React.useState&lt;IssueStatus | null&gt;(null);\n  const [reasonText, setReasonText] = React.useState('');\n\n  const handleClick = async (to: IssueStatus) =&gt; {\n    if (!isLegal(current, to)) return;\n    if (requiresReason(current, to)) {\n      setPending(to);\n      return;\n    }\n    await onChange(to);\n  };\n\n  return (\n    &lt;div className=\"status-picker\"&gt;\n      {(['backlog','todo','in_progress','in_review','done','cancelled'] as IssueStatus[]).map(s =&gt; (\n        &lt;button key={s}\n                disabled={!isLegal(current, s) &amp;&amp; s !== current}\n                aria-current={s === current}\n                onClick={() =&gt; handleClick(s)}&gt;\n          {s}\n        &lt;/button&gt;\n      ))}\n      {pending &amp;&amp; (\n        &lt;div className=\"reason-prompt\"&gt;\n          &lt;label&gt;Re-open reason (required):&lt;/label&gt;\n          &lt;textarea value={reasonText} onChange={e =&gt; setReasonText(e.target.value)} /&gt;\n          &lt;button disabled={!reasonText.trim()} onClick={async () =&gt; {\n            await onChange(pending, reasonText.trim());\n            setPending(null); setReasonText('');\n          }}&gt;Re-open&lt;/button&gt;\n          &lt;button onClick={() =&gt; { setPending(null); setReasonText(''); }}&gt;Cancel&lt;/button&gt;\n        &lt;/div&gt;\n      )}\n    &lt;/div&gt;\n  );\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Every legal transition succeeds</strong> — fixture: each of 12 forward + 2 re-open transitions → 200; transition_seq increments.</li><li><strong>Every illegal transition rejected</strong> — fixture: all 18 illegal pairs (out of 30 total) → 422 with <code>allowed_to</code> list.</li><li><strong>No-op same-state allowed</strong> — current=todo, request todo → 200; <code>transition_seq</code> unchanged.</li><li><strong>Re-open requires reason</strong> — done → in_progress with <code>reason: null</code> → 422 <code>reason_required</code>; with <code>reason: \"scope creep\"</code> → 200.</li><li><strong>Re-open with empty reason rejected</strong> — <code>reason: \"   \"</code> → 422 (trimmed empty).</li><li><strong>history table populated</strong> — after 5 transitions on issue X → 5 rows in <code>issue_status_history</code> with consecutive <code>transition_seq</code> 1..5.</li><li><strong>Companion columns updated</strong> — <code>status_updated_at_ns</code> + <code>status_updated_by_subject_id</code> match the transition row.</li><li><strong>memory audit row per transition</strong> — every legal transition → <code>proj.issue_status_changed</code> row.</li><li><strong>memory audit <code>proj.issue_reopened</code> on re-open</strong> — done → in_progress → both <code>status_changed</code> AND <code>issue_reopened</code> rows.</li><li><strong>time_in_status metric recorded</strong> — issue X transitions todo → in_progress at T+10s → histogram records 10s for <code>from_status=\"todo\"</code>.</li><li><strong>TS mirror matches Rust</strong> — CI <code>ts-fsm-fresh</code> runs codegen + diffs; PR rejected if drift.</li><li><strong>StatusPicker disables illegal options</strong> — current=backlog → in_review button is disabled.</li><li><strong>StatusPicker prompts for reason on re-open</strong> — current=done, click in_progress → reason textarea appears; submit button disabled until non-empty.</li><li><strong><code>cyberos issue history &lt;id&gt;</code> CLI</strong> — returns transition log JSON; ordered ascending by transition_seq.</li><li><strong>Concurrent transitions serialised</strong> — two clients send transitions on same issue concurrently → second client gets stale_write (FR-PROJ-003 LWW); history reflects single transition.</li><li><strong>OTel span emitted</strong> — span <code>proj.issue.transition</code> with <code>from</code>, <code>to</code>, <code>had_reason</code>, <code>transition_seq</code>.</li><li><strong>OTel metric <code>proj_issue_transitions_total</code></strong> — labels <code>from</code> + <code>to</code> populated correctly per call.</li><li><strong>OTel metric <code>proj_issue_transitions_rejected_total{reason=\"illegal_transition\"}</code></strong> — increments on illegal transition.</li><li><strong>OTel metric <code>proj_issue_reopens_total</code></strong> — increments on re-open only.</li><li><strong>RLS enforces tenant isolation</strong> — tenant A cannot read/write tenant B's <code>issue_status_history</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/proj/tests/status_fsm_test.rs\n\n#[test]\nfn legal_transitions_table() {\n    use IssueStatus::*;\n    let expected_legal: HashSet&lt;_&gt; = [\n        (Backlog, Todo), (Backlog, InProgress),\n        (Todo, InProgress), (Todo, Cancelled),\n        (InProgress, InReview), (InProgress, Cancelled), (InProgress, Done),\n        (InReview, Done), (InReview, InProgress), (InReview, Cancelled),\n        (Done, InProgress), (Cancelled, InProgress),\n    ].into_iter().collect();\n    for &amp;(f, t) in expected_legal.iter() {\n        assert!(fsm::is_legal(f, t), \"expected legal: {f:?} → {t:?}\");\n    }\n}\n\n#[test]\nfn all_illegal_pairs_rejected() {\n    use IssueStatus::*;\n    let mut count_illegal = 0;\n    for f in IssueStatus::all() {\n        for t in IssueStatus::all() {\n            if f == t { continue; }\n            let in_legal = matches!((f, t),\n                (Backlog, Todo)        | (Backlog, InProgress)  |\n                (Todo, InProgress)     | (Todo, Cancelled)      |\n                (InProgress, InReview) | (InProgress, Cancelled)| (InProgress, Done) |\n                (InReview, Done)       | (InReview, InProgress) | (InReview, Cancelled) |\n                (Done, InProgress)     | (Cancelled, InProgress));\n            if !in_legal {\n                assert!(!fsm::is_legal(f, t), \"expected illegal: {f:?} → {t:?}\");\n                count_illegal += 1;\n            }\n        }\n    }\n    assert_eq!(count_illegal, 18);\n}\n\n#[test]\nfn no_op_same_state_legal() {\n    for s in IssueStatus::all() {\n        assert!(fsm::is_legal(s, s));\n    }\n}\n\n#[test]\nfn requires_reason_only_for_terminal_to_inprogress() {\n    use IssueStatus::*;\n    assert!(fsm::requires_reason(Done, InProgress));\n    assert!(fsm::requires_reason(Cancelled, InProgress));\n    assert!(!fsm::requires_reason(Done, Cancelled));   // not allowed; would short-circuit at fsm::is_legal\n    assert!(!fsm::requires_reason(Backlog, Todo));\n}\n\n#[tokio::test]\nasync fn re_open_without_reason_rejected() {\n    let env = TestEnv::new().await;\n    let issue = env.create_issue_in_status(IssueStatus::Done).await;\n    let res = apply_transition(&amp;env.pool, issue, IssueStatus::InProgress, env.alice(), None).await;\n    assert!(matches!(res, Err(FsmError::ReasonRequired { .. })));\n}\n\n#[tokio::test]\nasync fn re_open_with_reason_succeeds_and_emits_reopen_row() {\n    let env = TestEnv::new().await;\n    let issue = env.create_issue_in_status(IssueStatus::Done).await;\n    let res = apply_transition(&amp;env.pool, issue, IssueStatus::InProgress, env.alice(), Some(\"scope creep\".into())).await.unwrap();\n    assert_eq!(res.current_status, IssueStatus::InProgress);\n    let reopen_row = env.memory.latest(\"proj.issue_reopened\").await;\n    assert_eq!(reopen_row[\"payload\"][\"reason\"], \"scope creep\");\n}\n\n#[tokio::test]\nasync fn time_in_status_metric_recorded() {\n    let env = TestEnv::with_paused_time().await;\n    let issue = env.create_issue_in_status(IssueStatus::Todo).await;\n    tokio::time::advance(Duration::from_secs(10)).await;\n    let _ = apply_transition(&amp;env.pool, issue, IssueStatus::InProgress, env.alice(), None).await.unwrap();\n    let metric = env.metrics.histogram_values(\"proj_issue_time_in_status_seconds\", &amp;[(\"from_status\", \"Todo\")]).await;\n    assert!(metric.iter().any(|&amp;v| (v - 10.0).abs() &lt; 0.5));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-PROJ-001</strong> — <code>issues</code> table; we add <code>status_updated_at_ns</code> + <code>status_updated_by_subject_id</code> columns + new <code>issue_status_history</code> table.</li><li><strong>FR-PROJ-002</strong> — WebSocket sync; transitions broadcast as scalar LWW updates per FR-PROJ-003.</li><li><strong>FR-PROJ-003</strong> — LWW companion-column pattern.</li><li><strong>FR-PROJ-008 (downstream)</strong> — memory audit row pattern reused.</li><li><strong>FR-AUTH-003</strong> — RLS on <code>issue_status_history</code>.</li><li><strong>FR-MEMORY-101</strong> — audit emission.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>proj.issue_status_changed</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.issue_status_changed\",\n  \"payload\": {\n    \"issue_id\":             \"iss-01HZK9R8M3X5C8Q4\",\n    \"from\":                 \"in_progress\",\n    \"to\":                   \"in_review\",\n    \"by_subject_id\":        \"7e57c0de-1234-...\",\n    \"reason\":               null,\n    \"transition_seq\":       4,\n    \"transitioned_at_ns\":   1747407137483000000,\n    \"trace_id\":             \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<h3><code>proj.issue_reopened</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.issue_reopened\",\n  \"payload\": {\n    \"issue_id\":       \"iss-01HZK9R8M3X5C8Q4\",\n    \"from\":           \"done\",\n    \"reason\":         \"scope expanded by stakeholder; need additional logging\",\n    \"by_subject_id\":  \"7e57c0de-...\",\n    \"reopen_seq\":     7,\n    \"trace_id\":       \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<h3>422 illegal transition response</h3>\n<pre><code class=\"language-json\">{\n  \"error\":     \"illegal_transition\",\n  \"from\":      \"backlog\",\n  \"to\":        \"in_review\",\n  \"allowed_to\": [\"todo\", \"in_progress\"]\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Per-engagement extended states (state_groups) — slice 3+ (per §1 #13).</li><li>Status presets per project template — slice 4+.</li><li>Bulk transition API (move 50 issues at once) — slice 3+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Illegal transition</td><td>fsm::is_legal</td><td>422 <code>illegal_transition</code> with allowed_to</td><td>Client retries with legal target</td></tr><tr><td>Reason missing on re-open</td><td>trim check</td><td>422 <code>reason_required</code></td><td>Client prompts user</td></tr><tr><td>Concurrent transition race</td><td>row-level lock + LWW</td><td>Second caller gets stale_write OR transition_seq collision (PK)</td><td>Client retries</td></tr><tr><td>Postgres tx commit fails</td><td>sqlx Err</td><td>500; no audit row</td><td>Operator restores DB; client retries</td></tr><tr><td>Audit row emit fails</td><td>MemoryWriter Err</td><td>Transition succeeded; audit lost; sev-2 alarm</td><td>Operator restores memory</td></tr><tr><td>Codegen drift (TS missing new state)</td><td>CI <code>ts-fsm-fresh</code></td><td>PR blocked</td><td>Author re-runs <code>cargo build</code></td></tr><tr><td>Operator manually inserts row with skip</td><td>RLS doesn't prevent; FSM only checked at handler</td><td>Skips audit trail; out-of-band edit</td><td>Operator avoids manual SQL</td></tr><tr><td>Issue deleted mid-transition</td><td>SELECT … FOR UPDATE returns 0 rows</td><td>sqlx Err → 500</td><td>Client retries (will get 404)</td></tr><tr><td>Subject_id not in tenant</td><td>RLS prevents at issue read</td><td>404</td><td>Caller fixes auth</td></tr><tr><td>Time-in-state computed across DB clock skew</td><td>should not happen on single node</td><td>Slight imprecision in histogram</td><td>Operator monitors</td></tr><tr><td>Transition_seq gap (manual delete from history)</td><td>analytic query detects</td><td>Auditor flag; operator investigates</td><td>Manual reconciliation</td></tr><tr><td>Same-state no-op produces transition_seq</td><td>code short-circuits</td><td>No new row; correct</td><td>None</td></tr><tr><td>Re-open from non-terminal state</td><td>fsm rejects</td><td>422</td><td>None — not a legal transition</td></tr><tr><td><code>reason</code> is 1 GB</td><td>DB column TEXT (no length limit)</td><td>Stored; UI may truncate</td><td>Slice-3+ cap at 4 KB</td></tr><tr><td>RLS bypass attempt (cross-tenant transition)</td><td>RLS catches</td><td>RLS returns 0 rows → 500 / 404</td><td>None automatic</td></tr><tr><td>Time-in-state metric cardinality</td><td><code>from_status</code> has 6 values</td><td>Bounded; safe</td><td>None</td></tr><tr><td>Issue created with status != backlog</td><td>FR-PROJ-001 default; bypassed by direct SQL</td><td>Allowed; history starts mid-FSM</td><td>Operator audits</td></tr><tr><td>TS picker shows transitions the server later rejects</td><td>only happens if drift; CI prevents</td><td>UX-degrading</td><td>CI catches</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The Rust enum is <code>IssueStatus</code>; sqlx maps to text via <code>#[derive(sqlx::Type)] #[sqlx(type_name = \"TEXT\")]</code>. We use TEXT not Postgres enum because we may need to add states without migration (per §1 #13 placeholder).</li><li><code>LEGAL_TRANSITIONS</code> is a <code>&amp;[(IssueStatus, IssueStatus)]</code> slice — compile-time constant; no allocation.</li><li>The <code>build.rs</code> script reads <code>lifecycle/fsm.rs</code>, parses the LEGAL_TRANSITIONS table, and writes the TS mirror with a <code>// GENERATED</code> header. The CI gate diffs the file in repo against fresh codegen output.</li><li><code>requires_reason</code> is intentionally narrow: only for re-opens. Non-re-open transitions MAY carry <code>reason</code> (operator annotation) but it's not required.</li><li>The <code>apply_transition</code> function uses <code>SELECT … FOR UPDATE</code> to serialise concurrent transitions on the same issue — prevents the FSM check from being read-skewed.</li><li>The audit row's <code>reason</code> field is <code>null</code> for non-re-open transitions (rather than empty string) so consumers can distinguish \"no annotation\" from \"explicit empty annotation.\"</li><li>The time-in-state histogram has bounded label cardinality (6 from-statuses) — Prometheus-safe.</li><li>The TS picker's <code>disabled={!isLegal(current, s) &amp;&amp; s !== current}</code> keeps the current state button enabled but inert (good UX — visible self-reference).</li><li>Re-open reason prompt is a modal/textarea in the picker; FR-PROJ-017 (Brief modal) provides the more polished version.</li></ul>\n<hr>\n<p><em>End of FR-PROJ-004.</em></p>";
