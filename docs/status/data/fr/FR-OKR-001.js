window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OKR-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The OKR service <strong>MUST</strong> ship the Cycle + Team + Objective + KeyResult schema as the quarterly strategy operating loop primitive. Each requirement:</p>\n<ol><li><strong>MUST</strong> define the <code>cycles</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 100)</code> (e.g. \"Q3 2026\"), <code>cycle_kind cycle_kind NOT NULL DEFAULT 'quarterly'</code>, <code>start_date DATE NOT NULL</code>, <code>end_date DATE NOT NULL CHECK (end_date &gt; start_date)</code>, <code>status cycle_status NOT NULL DEFAULT 'planning'</code>, <code>theme TEXT</code> (nullable; optional theme for the cycle), <code>created_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID NOT NULL</code>. UNIQUE <code>(tenant_id, name)</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>cycle_kind</code> Postgres enum with exactly 3 values (per DEC-360): <code>'quarterly'</code>, <code>'monthly'</code>, <code>'trimester'</code>. Adding a 4th is an ADR. Default is quarterly.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>cycle_status</code> Postgres enum with exactly 4 values (per DEC-366): <code>'planning'</code>, <code>'active'</code>, <code>'closing'</code>, <code>'closed'</code>. Transitions are unidirectional: <code>planning → active → closing → closed</code>. Backward transitions are forbidden.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>teams</code> table (per DEC-372) with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 100)</code>, <code>parent_team_id UUID REFERENCES teams(id) ON DELETE RESTRICT</code>, <code>lead_subject_id UUID REFERENCES auth.subjects(id)</code>, <code>created_at TIMESTAMPTZ</code>. UNIQUE <code>(tenant_id, name)</code>. Teams form a hierarchy (Company → Eng → Backend → Auth); used by Team-scope objectives.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>objectives</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>cycle_id UUID NOT NULL REFERENCES cycles(id) ON DELETE CASCADE</code>, <code>scope okr_scope NOT NULL</code> (closed 3-value enum), <code>name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 300)</code>, <code>description TEXT</code>, <code>parent_objective_id UUID REFERENCES objectives(id) ON DELETE RESTRICT</code> (alignment FK — nullable for Company scope; required for Team + Member per FSM), <code>team_id UUID REFERENCES teams(id)</code> (required when scope='team'), <code>owner_subject_id UUID REFERENCES auth.subjects(id)</code> (required when scope='member'), <code>status objective_status NOT NULL DEFAULT 'planning'</code>, <code>created_at TIMESTAMPTZ</code>, <code>updated_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID NOT NULL</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>okr_scope</code> Postgres enum with exactly 3 values (per DEC-361): <code>'company'</code>, <code>'team'</code>, <code>'member'</code>. Adding a 4th tier (e.g. <code>department</code>) is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>objective_status</code> Postgres enum with 4 face-saving values: <code>'planning'</code>, <code>'active'</code>, <code>'closed_achieved'</code>, <code>'closed_learned'</code>. The terminal states <code>closed_achieved</code> (KRs met) and <code>closed_learned</code> (KRs not met — face-saving framing per DEC-364) replace conventional \"completed/failed\".</li></ol>\n<ol><li><strong>MUST</strong> ship the alignment-tree FSM validator (per DEC-365) at <code>services/okr/src/alignment/validator.rs</code>. Rules:</li></ol>\n<ul><li><code>scope='company'</code> → <code>parent_objective_id MUST BE NULL</code>.</li><li><code>scope='team'</code> → <code>parent_objective_id MUST reference an objective with scope='company'</code> AND <code>team_id MUST be set</code>.</li><li><code>scope='member'</code> → <code>parent_objective_id MUST reference an objective with scope='team'</code> AND <code>owner_subject_id MUST be set</code>.</li><li>Cross-cascade (e.g. member → company) is forbidden.</li><li>The parent objective MUST be in the same cycle (<code>cycle_id</code>). Validated at handler boundary AND by trigger <code>enforce_alignment_tree</code>.</li></ul>\n<ol><li><strong>MUST</strong> define the <code>key_results</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>objective_id UUID NOT NULL REFERENCES objectives(id) ON DELETE CASCADE</code>, <code>name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 300)</code>, <code>kr_type kr_type NOT NULL</code> (closed 3-value placeholder; full type validation ships in FR-OKR-002), <code>start_value_numeric BIGINT</code> (nullable for milestone), <code>target_value_numeric BIGINT NOT NULL</code>, <code>current_value_numeric BIGINT NOT NULL DEFAULT 0</code>, <code>unit TEXT NOT NULL DEFAULT ''</code> (e.g. \"VND\", \"%\", \"count\", \"\"), <code>status kr_status NOT NULL DEFAULT 'on_track'</code> (closed 5-value face-saving enum per DEC-364), <code>progress_source_query TEXT</code> (nullable; FR-OKR-003 ships the DSL), <code>last_progress_at TIMESTAMPTZ</code>, <code>created_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID NOT NULL</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>kr_type</code> Postgres enum with exactly 3 values (per DEC-363): <code>'hit_target'</code>, <code>'improvement'</code>, <code>'milestone'</code>. Adding a 4th is an ADR; full per-type validation ships in FR-OKR-002.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>kr_status</code> Postgres enum with exactly 5 <strong>face-saving</strong> values (per DEC-364): <code>'on_track'</code>, <code>'at_risk'</code>, <code>'learned'</code>, <code>'achieved'</code>, <code>'cycled_forward'</code>. The terms \"missed\", \"failed\", \"behind\", \"delayed\" MUST NOT appear in any enum value, error message, or UI string — enforced by the <code>face_saving_terminology_test</code> CI lint.</li></ol>\n<ol><li><strong>MUST</strong> enforce <strong>3-5 KRs per Objective</strong> at handler boundary (per DEC-362). POST <code>/objectives</code> with <code>initial_key_results</code> length &lt; 3 or &gt; 5 → 400 <code>kr_count_out_of_range</code>. Adding a KR that would bring count above 5 → 409 <code>objective_at_kr_limit</code>. Removing a KR that would drop count below 3 → 409 <code>objective_below_kr_min</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship the <code>kr_progress_log</code> append-only table (per DEC-367) with: <code>id BIGSERIAL PRIMARY KEY</code>, <code>kr_id UUID NOT NULL REFERENCES key_results(id) ON DELETE RESTRICT</code>, <code>tenant_id UUID NOT NULL</code>, <code>value_numeric BIGINT NOT NULL</code>, <code>source TEXT NOT NULL CHECK (source IN ('manual','auto','check_in'))</code>, <code>rationale TEXT CHECK (rationale IS NULL OR length(rationale) BETWEEN 1 AND 1000)</code>, <code>recorded_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>recorded_by_subject_id UUID NOT NULL</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>objective_status_history</code> append-only table with: <code>(id BIGSERIAL, objective_id UUID, tenant_id UUID, from_status objective_status, to_status objective_status, changed_at TIMESTAMPTZ, changed_by_subject_id UUID, reason TEXT)</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code>.</li></ol>\n<ol><li><strong>MUST</strong> enforce <strong>face-saving terminology</strong> via CI test (per DEC-364). <code>face_saving_terminology_test</code> scans <code>types.rs</code>, all migration SQL, all error messages, and the OpenAPI spec for the forbidden word list <code>[\"missed\", \"failed\", \"behind\", \"delayed\", \"fail\", \"miss\", \"behind schedule\"]</code>. Any occurrence → CI fails. Vietnamese equivalents <code>[\"thất bại\", \"trễ\", \"không đạt\"]</code> also forbidden.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> on cycles, teams, objectives, key_results, kr_progress_log, objective_status_history. Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship REST handlers:</li></ol>\n<ul><li><code>POST /v1/okr/cycles</code> — create cycle (status=planning).</li><li><code>POST /v1/okr/cycles/{id}/transition</code> — transition status (planning → active → closing → closed).</li><li><code>GET /v1/okr/cycles?status=&lt;&gt;</code> — list with filter.</li><li><code>POST /v1/okr/teams</code> — create team.</li><li><code>GET /v1/okr/teams</code> — list.</li><li><code>POST /v1/okr/objectives</code> — create objective + initial KRs (3-5).</li><li><code>PATCH /v1/okr/objectives/{id}</code> — update non-cascade fields.</li><li><code>POST /v1/okr/objectives/{id}/transition</code> — status transition.</li><li><code>POST /v1/okr/key_results/{id}/progress</code> — record progress (manual or check_in).</li><li><code>POST /v1/okr/objectives/{id}/key_results</code> — add KR (must stay ≤ 5).</li><li><code>DELETE /v1/okr/objectives/{id}/key_results/{kr_id}</code> — remove KR (must stay ≥ 3).</li><li><code>GET /v1/okr/objectives?cycle_id=&lt;&gt;&amp;scope=&lt;&gt;</code> — list with filters.</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit rows for the 8 kinds (per DEC-368):</li></ol>\n<ul><li><code>okr.cycle_opened</code> (cycle status → active).</li><li><code>okr.cycle_closed</code> (cycle status → closed).</li><li><code>okr.objective_created</code> (POST /objectives).</li><li><code>okr.objective_updated</code> (PATCH /objectives).</li><li><code>okr.kr_progress_recorded</code> (POST /key_results/{id}/progress).</li><li><code>okr.kr_status_changed</code> (KR status transition).</li><li><code>okr.alignment_created</code> (Team or Member objective with parent set).</li><li><code>okr.cycle_retro_recorded</code> (FR-OKR-007 retro entries; placeholder kind at slice 1).</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>description</code>, <code>rationale</code>, and <code>name</code> fields via FR-MEMORY-111 before chain commit. Tenant-scoped Postgres rows retain raw; memory chain holds scrubbed.</li></ol>\n<ol><li><strong>MUST</strong> complete handlers in ≤ 100 ms p95. <code>okr_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>okr.{cycle,team,objective,kr,progress}.{create,update,transition,...}</code> with <code>outcome</code> attribute (success | invalid_alignment | kr_count_out_of_range | invalid_status_transition | not_found | permission_denied | forbidden_terminology).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>okr_cycle_count{status, tenant_id}</code> (gauge).</li><li><code>okr_objective_count{scope, status, tenant_id}</code> (gauge).</li><li><code>okr_kr_count{status, kr_type, tenant_id}</code> (gauge).</li><li><code>okr_kr_progress_records_total{source}</code> (counter).</li><li><code>okr_objective_status_transitions_total{from_status, to_status, scope}</code> (counter).</li><li><code>okr_alignment_violations_total{kind}</code> (counter — should remain 0).</li></ul>\n<ol><li><strong>MUST</strong> ship cascading delete: deleting a <code>cycles</code> row CASCADES to objectives + key_results + history rows (per DEC-371). Deleting a <code>key_results</code> row with progress_log entries is RESTRICTED — preserve audit history. Cycles older than 5 years MAY be archived (not deleted) via a separate handler (out of scope for slice 1).</li></ol>\n<ol><li><strong>MUST</strong> record an <code>okr.alignment_created</code> memory row on every Team/Member objective creation, carrying <code>{objective_id, parent_objective_id, scope, alignment_depth}</code> where depth is 1 (team→company) or 2 (member→team→company).</li></ol>\n<ol><li><strong>MUST</strong> include an EU AI Act Art. 14 acknowledgement in the OpenAPI spec for endpoints that downstream HR/REW/LEARN modules will consume for employment decisions: every response includes a <code>_compliance_note</code> field stating \"OKR data is informational only; employment decisions require human approval per EU AI Act Art. 14 + Annex III §4\".</li></ol>\n<ol><li><strong>MUST</strong> validate parent objective is in same cycle on Team/Member objective creation. Cross-cycle parent → 400 <code>cross_cycle_alignment_forbidden</code>.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why 3-tier scope (company/team/member) closed (DEC-361)?</strong> Doerr/Grove canonical. A 4th tier (department/division) creates organizational ambiguity — is \"Backend Engineering\" a team or a department? Forcing the 3-tier model collapses sub-divisions into the team hierarchy via <code>parent_team_id</code>. The 3-tier scope drives the alignment FSM cleanly; adding a 4th would require complex parent-rules.</p>\n<p><strong>Why quarterly default + alternatives (DEC-360)?</strong> Quarterly is the standard Doerr/Grove cadence; allows enough time for meaningful KRs while staying tight. Monthly cadence suits high-velocity teams in early-stage; trimester (4-month) suits enterprises with slower release cycles. The closed enum prevents drift to weird cadences (\"hexamonthly\" etc.).</p>\n<p><strong>Why face-saving status enum (DEC-364, §1 #11, §1 #15)?</strong> Vietnamese cultural norm prefers \"what did we learn?\" over \"what did you miss?\". The schema bakes this in: status <code>'learned'</code> replaces \"failed\"; <code>'cycled_forward'</code> replaces \"deferred to next quarter\". The CI lint enforces no English forbidden terms anywhere — preventing well-meaning developers from re-introducing \"missed\" via error messages. This is the \"Vietnamese-cultural-fit\" design assertion documented in <code>(Vietnamese-cultural fit).md</code> (TODO in source_pages).</p>\n<p><strong>Why strict alignment tree (DEC-365, §1 #8)?</strong> Cross-cascade alignments (e.g. a Member OKR directly under a Company OKR, skipping the Team) defeat the cascade's purpose. Forcing the strict Company → Team → Member tree maintains the \"every member OKR rolls up to a team OKR rolls up to a company OKR\" guarantee — and the rollup is the foundation for FR-OKR-006's Monday digest.</p>\n<p><strong>Why 3-5 KRs per Objective (DEC-362, §1 #12)?</strong> Doerr/Grove industry rule of thumb — fewer than 3 means the Objective isn't operationally meaningful (too few measures); more than 5 means the team can't focus. Enforcing at handler keeps the rule explicit + visible; using a DB CHECK constraint would block transactional bootstrap (create objective + 3 KRs in one tx — Postgres deferred constraints could work but add complexity).</p>\n<p><strong>Why parent_objective_id NULLABLE on Company scope (§1 #5)?</strong> Company OKRs have no parent — they're the top of the cascade. Allowing NULL with the FSM rule (<code>scope='company' → parent IS NULL</code>) is cleaner than synthetic root entity. Trigger enforces.</p>\n<p><strong>Why team_id on Team objectives + owner_subject_id on Member objectives (§1 #5, §1 #8)?</strong> Scope alone is insufficient — we need to know WHICH team / WHICH member. Per-scope conditional required field at handler validation. The team_id references the tenant-local <code>teams</code> table; owner_subject_id references AUTH.</p>\n<p><strong>Why teams as a tenant-local primitive (DEC-372, §1 #4)?</strong> HR (FR-HR-001) ships Member records but doesn't ship a Team primitive (deferred to FR-HR-2xx). OKR needs teams now for the Team-scope objectives. Shipping the teams table here (in OKR's schema) is the pragmatic answer; future HR Team primitive can either supersede or join.</p>\n<p><strong>Why append-only kr_progress_log (DEC-367, §1 #13)?</strong> Quarterly retros depend on \"what was the KR's progression over time?\" — answerable only with a chained history. UPDATE in place loses prior recordings; the log preserves them. The <code>source</code> field (manual | auto | check_in) lets FR-OKR-003's auto-progress batch distinguish its own writes from operator manual recordings.</p>\n<p><strong>Why cycle_status unidirectional (DEC-366, §1 #3)?</strong> Cycles flow <code>planning → active → closing → closed</code>. Backward transitions (\"reopen a closed cycle\") would corrupt historical OKR analyses. The 1% case requiring reopen routes through ADR + manual SQL with audit trail.</p>\n<p><strong>Why face-saving terminology CI lint (§1 #15, DEC-364)?</strong> Well-meaning developers writing error messages like \"missed deadline\" reintroduce the cultural anti-pattern. The CI lint catches at build time before merge. Enforces the design assertion mechanically rather than via review vigilance.</p>\n<p><strong>Why 8 memory audit kinds split by lifecycle event (DEC-368, §1 #18)?</strong> Operators query specific events: \"show me all cycle closes this year\" vs \"show me all KR progress recordings this week\". Split kinds give selectivity benefits at query time.</p>\n<p><strong>Why EU AI Act Art. 14 acknowledgement in OpenAPI (§1 #25, DEC-369)?</strong> OKR data drives employment decisions (promotion, performance review) downstream in HR/REW. The Act requires human-in-loop for high-risk decisions. Embedding the compliance note in every response ensures consumers can't claim they didn't know — the gate is explicit at the API contract level.</p>\n<p><strong>Why parent objective MUST be in same cycle (§1 #26)?</strong> Cross-cycle alignment (a Q3 Member OKR aligned to a Q2 Team OKR) is semantically meaningless. The alignment is \"this Member OKR rolls up to this Team OKR THIS QUARTER\". Enforcing at trigger prevents accidental cross-cycle setups.</p>\n<p><strong>Why milestone KR uses boolean target_value_numeric (DEC-370)?</strong> Milestones are binary (delivered or not). Storing as <code>target=1, current=0|1</code> uses the same BIGINT column for all KR types — simplifies the schema. FR-OKR-002 ships the per-type validation that enforces this convention.</p>\n<p><strong>Why cascading delete Cycle → Objectives → KRs but RESTRICT on KR if progress_log exists (DEC-371, §1 #23)?</strong> Cycle deletion is an operator-explicit destructive action; cascading to dependent rows is expected. But KR progress_log is forensic — if you've recorded KR progress, the KR row must persist for the log to make sense. RESTRICT forces operators to either keep the KR or explicitly clear the log first (which itself requires elevated permission).</p>\n<p><strong>Why <code>description</code>, <code>rationale</code>, <code>name</code> PII-scrubbed (§1 #19)?</strong> Objective descriptions may contain employee names (\"Improve Alice's onboarding time\"); KR rationale during weekly check-ins may carry personal context. FR-MEMORY-111 scrubs before memory chain commit; Postgres retains raw for in-tenant queries.</p>\n<p><strong>Why slice 1 ships only the schema + handlers, not the progress DSL or check-in flow?</strong> Split: FR-OKR-001 = data model; FR-OKR-003 = progress source DSL (substantial — queries against PROJ/INV/HR/LEARN); FR-OKR-005 = weekly check-in handler. Splitting keeps this FR focused on the foundational schema.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0001 — cycles</h3>\n<pre><code class=\"language-sql\">-- services/okr/migrations/0001_cycles.sql\n\nBEGIN;\n\nCREATE TYPE cycle_kind   AS ENUM ('quarterly', 'monthly', 'trimester');\nCREATE TYPE cycle_status AS ENUM ('planning', 'active', 'closing', 'closed');\n\nCREATE TABLE cycles (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    name                   TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 100),\n    cycle_kind             cycle_kind   NOT NULL DEFAULT 'quarterly',\n    start_date             DATE         NOT NULL,\n    end_date               DATE         NOT NULL CHECK (end_date &gt; start_date),\n    status                 cycle_status NOT NULL DEFAULT 'planning',\n    theme                  TEXT,\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL\n);\n\nCREATE UNIQUE INDEX uniq_cycle_name ON cycles (tenant_id, name);\nCREATE INDEX cycles_status_idx ON cycles (tenant_id, status);\n\nALTER TABLE cycles ENABLE ROW LEVEL SECURITY;\nCREATE POLICY cycles_tenant_isolation ON cycles\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Unidirectional status FSM\nCREATE OR REPLACE FUNCTION enforce_cycle_status_fsm() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.status = OLD.status THEN RETURN NEW; END IF;\n    IF NOT (\n        (OLD.status = 'planning' AND NEW.status = 'active')\n        OR (OLD.status = 'active'   AND NEW.status = 'closing')\n        OR (OLD.status = 'closing'  AND NEW.status = 'closed')\n    ) THEN\n        RAISE EXCEPTION 'invalid_cycle_status_transition: % -&gt; %', OLD.status, NEW.status USING ERRCODE = 'P0060';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_cycle_status_fsm BEFORE UPDATE ON cycles\n    FOR EACH ROW EXECUTE FUNCTION enforce_cycle_status_fsm();\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0002 — teams</h3>\n<pre><code class=\"language-sql\">-- services/okr/migrations/0002_teams.sql\n\nBEGIN;\n\nCREATE TABLE teams (\n    id                UUID         PRIMARY KEY,\n    tenant_id         UUID         NOT NULL,\n    name              TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 100),\n    parent_team_id    UUID         REFERENCES teams(id) ON DELETE RESTRICT,\n    lead_subject_id   UUID         REFERENCES auth.subjects(id),\n    created_at        TIMESTAMPTZ  NOT NULL DEFAULT now()\n);\n\nCREATE UNIQUE INDEX uniq_team_name ON teams (tenant_id, name);\nCREATE INDEX teams_parent_idx ON teams (parent_team_id) WHERE parent_team_id IS NOT NULL;\n\nALTER TABLE teams ENABLE ROW LEVEL SECURITY;\nCREATE POLICY teams_tenant_isolation ON teams\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.3 — Migration 0003 — objectives</h3>\n<pre><code class=\"language-sql\">-- services/okr/migrations/0003_objectives.sql\n\nBEGIN;\n\nCREATE TYPE okr_scope        AS ENUM ('company', 'team', 'member');\nCREATE TYPE objective_status AS ENUM ('planning', 'active', 'closed_achieved', 'closed_learned');\n\nCREATE TABLE objectives (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    cycle_id               UUID         NOT NULL REFERENCES cycles(id) ON DELETE CASCADE,\n    scope                  okr_scope    NOT NULL,\n    name                   TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 300),\n    description            TEXT,\n    parent_objective_id    UUID         REFERENCES objectives(id) ON DELETE RESTRICT,\n    team_id                UUID         REFERENCES teams(id),\n    owner_subject_id       UUID         REFERENCES auth.subjects(id),\n    status                 objective_status NOT NULL DEFAULT 'planning',\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    updated_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL\n);\n\nCREATE INDEX objectives_cycle_scope_idx ON objectives (tenant_id, cycle_id, scope);\nCREATE INDEX objectives_parent_idx ON objectives (parent_objective_id) WHERE parent_objective_id IS NOT NULL;\nCREATE INDEX objectives_team_idx ON objectives (team_id) WHERE team_id IS NOT NULL;\nCREATE INDEX objectives_owner_idx ON objectives (owner_subject_id) WHERE owner_subject_id IS NOT NULL;\n\nALTER TABLE objectives ENABLE ROW LEVEL SECURITY;\nCREATE POLICY objectives_tenant_isolation ON objectives\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Alignment tree FSM (DEC-365)\nCREATE OR REPLACE FUNCTION enforce_alignment_tree() RETURNS TRIGGER AS $$\nDECLARE parent RECORD;\nBEGIN\n    -- Company scope: no parent + no team + no owner\n    IF NEW.scope = 'company' THEN\n        IF NEW.parent_objective_id IS NOT NULL THEN\n            RAISE EXCEPTION 'company_objective_has_no_parent' USING ERRCODE = 'P0070';\n        END IF;\n        RETURN NEW;\n    END IF;\n    -- Team scope: must have parent (must be company scope) + must have team_id\n    IF NEW.scope = 'team' THEN\n        IF NEW.parent_objective_id IS NULL THEN\n            RAISE EXCEPTION 'team_objective_requires_parent' USING ERRCODE = 'P0071';\n        END IF;\n        IF NEW.team_id IS NULL THEN\n            RAISE EXCEPTION 'team_objective_requires_team_id' USING ERRCODE = 'P0072';\n        END IF;\n        SELECT scope, cycle_id INTO parent FROM objectives WHERE id = NEW.parent_objective_id;\n        IF NOT FOUND OR parent.scope != 'company' THEN\n            RAISE EXCEPTION 'team_objective_parent_must_be_company' USING ERRCODE = 'P0073';\n        END IF;\n        IF parent.cycle_id != NEW.cycle_id THEN\n            RAISE EXCEPTION 'cross_cycle_alignment_forbidden' USING ERRCODE = 'P0074';\n        END IF;\n        RETURN NEW;\n    END IF;\n    -- Member scope: must have parent (must be team scope) + must have owner_subject_id\n    IF NEW.scope = 'member' THEN\n        IF NEW.parent_objective_id IS NULL THEN\n            RAISE EXCEPTION 'member_objective_requires_parent' USING ERRCODE = 'P0075';\n        END IF;\n        IF NEW.owner_subject_id IS NULL THEN\n            RAISE EXCEPTION 'member_objective_requires_owner' USING ERRCODE = 'P0076';\n        END IF;\n        SELECT scope, cycle_id INTO parent FROM objectives WHERE id = NEW.parent_objective_id;\n        IF NOT FOUND OR parent.scope != 'team' THEN\n            RAISE EXCEPTION 'member_objective_parent_must_be_team' USING ERRCODE = 'P0077';\n        END IF;\n        IF parent.cycle_id != NEW.cycle_id THEN\n            RAISE EXCEPTION 'cross_cycle_alignment_forbidden' USING ERRCODE = 'P0074';\n        END IF;\n        RETURN NEW;\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_objectives_alignment BEFORE INSERT OR UPDATE ON objectives\n    FOR EACH ROW EXECUTE FUNCTION enforce_alignment_tree();\n\nCOMMIT;</code></pre>\n<h3>3.4 — Migration 0004 — key_results</h3>\n<pre><code class=\"language-sql\">-- services/okr/migrations/0004_key_results.sql\n\nBEGIN;\n\nCREATE TYPE kr_type   AS ENUM ('hit_target', 'improvement', 'milestone');\nCREATE TYPE kr_status AS ENUM ('on_track', 'at_risk', 'learned', 'achieved', 'cycled_forward');\n\nCREATE TABLE key_results (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    objective_id           UUID         NOT NULL REFERENCES objectives(id) ON DELETE CASCADE,\n    name                   TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 300),\n    kr_type                kr_type      NOT NULL,\n    start_value_numeric    BIGINT,\n    target_value_numeric   BIGINT       NOT NULL,\n    current_value_numeric  BIGINT       NOT NULL DEFAULT 0,\n    unit                   TEXT         NOT NULL DEFAULT '',\n    status                 kr_status    NOT NULL DEFAULT 'on_track',\n    progress_source_query  TEXT,\n    last_progress_at       TIMESTAMPTZ,\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL\n);\n\nCREATE INDEX key_results_obj_idx ON key_results (objective_id);\nCREATE INDEX key_results_status_idx ON key_results (tenant_id, status);\n\nALTER TABLE key_results ENABLE ROW LEVEL SECURITY;\nCREATE POLICY key_results_tenant_isolation ON key_results\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.5 — Migration 0005 — append-only progress log</h3>\n<pre><code class=\"language-sql\">-- services/okr/migrations/0005_progress_log.sql\n\nBEGIN;\n\nCREATE TABLE kr_progress_log (\n    id                     BIGSERIAL    PRIMARY KEY,\n    kr_id                  UUID         NOT NULL REFERENCES key_results(id) ON DELETE RESTRICT,\n    tenant_id              UUID         NOT NULL,\n    value_numeric          BIGINT       NOT NULL,\n    source                 TEXT         NOT NULL CHECK (source IN ('manual','auto','check_in')),\n    rationale              TEXT         CHECK (rationale IS NULL OR length(rationale) BETWEEN 1 AND 1000),\n    recorded_at            TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    recorded_by_subject_id UUID         NOT NULL\n);\n\nCREATE INDEX progress_log_kr_idx ON kr_progress_log (kr_id, recorded_at DESC);\nCREATE INDEX progress_log_tenant_recorded_idx ON kr_progress_log (tenant_id, recorded_at DESC);\n\nALTER TABLE kr_progress_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY progress_log_tenant_isolation ON kr_progress_log\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON kr_progress_log FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.6 — Rust types</h3>\n<pre><code class=\"language-rust\">// services/okr/src/types.rs\nuse chrono::{DateTime, NaiveDate, Utc};\nuse serde::{Deserialize, Serialize};\nuse sqlx::{FromRow, Type};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"okr_scope\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum OkrScope { Company, Team, Member }\n\nimpl OkrScope { pub const ALL: &amp;'static [OkrScope] = &amp;[OkrScope::Company, OkrScope::Team, OkrScope::Member]; }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"cycle_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum CycleStatus { Planning, Active, Closing, Closed }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"objective_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum ObjectiveStatus { Planning, Active, ClosedAchieved, ClosedLearned }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"kr_type\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum KrType { HitTarget, Improvement, Milestone }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"kr_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum KrStatus { OnTrack, AtRisk, Learned, Achieved, CycledForward }\n\nimpl KrStatus {\n    pub const ALL: &amp;'static [KrStatus] = &amp;[\n        KrStatus::OnTrack, KrStatus::AtRisk, KrStatus::Learned, KrStatus::Achieved, KrStatus::CycledForward,\n    ];\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct Cycle {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub name: String,\n    pub cycle_kind: String,\n    pub start_date: NaiveDate,\n    pub end_date: NaiveDate,\n    pub status: CycleStatus,\n    pub theme: Option&lt;String&gt;,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct Objective {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub cycle_id: Uuid,\n    pub scope: OkrScope,\n    pub name: String,\n    pub description: Option&lt;String&gt;,\n    pub parent_objective_id: Option&lt;Uuid&gt;,\n    pub team_id: Option&lt;Uuid&gt;,\n    pub owner_subject_id: Option&lt;Uuid&gt;,\n    pub status: ObjectiveStatus,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub updated_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct KeyResult {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub objective_id: Uuid,\n    pub name: String,\n    pub kr_type: KrType,\n    pub start_value_numeric: Option&lt;i64&gt;,\n    pub target_value_numeric: i64,\n    pub current_value_numeric: i64,\n    pub unit: String,\n    pub status: KrStatus,\n    pub progress_source_query: Option&lt;String&gt;,\n    pub last_progress_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}</code></pre>\n<h3>3.7 — Face-saving terminology lint</h3>\n<pre><code class=\"language-rust\">// services/okr/tests/face_saving_terminology_test.rs\nuse std::path::Path;\nuse regex::Regex;\n\nconst FORBIDDEN_TERMS: &amp;[&amp;str] = &amp;[\n    \"missed\", \"failed\", \"behind\", \"delayed\", \" miss \", \" fail \",\n    \"thất bại\", \"trễ\", \"không đạt\",\n];\n\n#[test]\nfn no_forbidden_terminology_in_module() {\n    let scan_dirs = [\n        \"src\", \"migrations\", \"tests/fixtures\",\n    ];\n    let mut violations: Vec&lt;(String, String)&gt; = Vec::new();\n    for dir in scan_dirs {\n        let root = Path::new(dir);\n        for entry in walkdir::WalkDir::new(root).into_iter().filter_map(Result::ok) {\n            if entry.file_type().is_file() {\n                let content = std::fs::read_to_string(entry.path()).unwrap_or_default();\n                let lower = content.to_lowercase();\n                for term in FORBIDDEN_TERMS {\n                    if lower.contains(&amp;term.to_lowercase()) {\n                        violations.push((entry.path().display().to_string(), term.to_string()));\n                    }\n                }\n            }\n        }\n    }\n    assert!(violations.is_empty(),\n        \"Face-saving terminology violated; reintroduces blame language:\\n{:#?}\", violations);\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>All 5 OKR enums closed at stated cardinalities</strong> — OkrScope (3), CycleStatus (4), ObjectiveStatus (4), KrType (3), KrStatus (5).</li><li><strong>RLS isolates by tenant</strong> — cross-tenant queries return 0 rows.</li><li><strong>POST cycle happy path</strong> — status=planning created; UNIQUE on <code>(tenant_id, name)</code>.</li><li><strong>Cycle backward transition rejected</strong> — <code>active → planning</code> raises invalid_cycle_status_transition.</li><li><strong>Cycle status forward transitions allowed</strong> — planning → active → closing → closed.</li><li><strong>Company objective without parent</strong> — accepted.</li><li><strong>Company objective with parent</strong> → 400 company_objective_has_no_parent.</li><li><strong>Team objective without parent</strong> → 400 team_objective_requires_parent.</li><li><strong>Team objective without team_id</strong> → 400 team_objective_requires_team_id.</li><li><strong>Team objective with member-scope parent</strong> → 400 team_objective_parent_must_be_company.</li><li><strong>Member objective without owner_subject_id</strong> → 400 member_objective_requires_owner.</li><li><strong>Cross-cycle alignment</strong> → 400 cross_cycle_alignment_forbidden.</li><li><strong>Objective with 2 KRs</strong> → 400 kr_count_out_of_range.</li><li><strong>Objective with 6 KRs</strong> → 400 kr_count_out_of_range.</li><li><strong>Adding 6th KR to objective at limit</strong> → 409 objective_at_kr_limit.</li><li><strong>Removing 3rd KR (down to 2)</strong> → 409 objective_below_kr_min.</li><li><strong>KR status closed at exactly 5 face-saving values</strong> — <code>KrStatus::ALL.len() == 5</code>; no \"missed/failed/behind\".</li><li><strong>Face-saving terminology CI lint</strong> — adding \"missed\" anywhere in src/migrations/tests → CI fails.</li><li><strong>kr_progress_log append-only</strong> — UPDATE/DELETE blocked from cyberos_app.</li><li><strong>objective_status_history append-only</strong> — same.</li><li><strong>POST progress emits <code>okr.kr_progress_recorded</code> memory row</strong>.</li><li><strong>Cycle delete cascades</strong> — deleting cycle deletes all child objectives + KRs.</li><li><strong>KR delete with progress_log entries</strong> → RESTRICT.</li><li><strong>OTel span <code>okr.objective.create</code> emitted</strong> — outcome=success.</li><li><strong>OTel counter <code>okr_objective_count{scope=team}</code> increments</strong>.</li><li><strong>OTel counter <code>okr_alignment_violations_total</code> is 0 on clean tenant</strong> — should never increment in normal use.</li><li><strong>OpenAPI compliance note</strong> — every endpoint response includes <code>_compliance_note</code> per EU AI Act Art. 14.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/okr/tests/alignment_tree_test.rs\n#[sqlx::test]\nasync fn company_with_parent_rejected(pool: sqlx::PgPool) {\n    let cycle = seed_cycle(&amp;pool).await;\n    let company = seed_company_objective(&amp;pool, cycle).await;\n    // Attempt: create another \"company\" objective pointing at the first\n    let err = sqlx::query(\"INSERT INTO objectives (id, tenant_id, cycle_id, scope, name, parent_objective_id, created_by_subject_id) VALUES ($1, $2, $3, 'company'::okr_scope, 'Bad', $4, $5)\")\n        .bind(Uuid::new_v4()).bind(test_tenant()).bind(cycle).bind(company).bind(test_subject())\n        .execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"company_objective_has_no_parent\"));\n}\n\n#[sqlx::test]\nasync fn team_parent_must_be_company(pool: sqlx::PgPool) {\n    let cycle = seed_cycle(&amp;pool).await;\n    let company = seed_company_objective(&amp;pool, cycle).await;\n    let team_team = seed_team(&amp;pool).await;\n    let team_obj = seed_team_objective(&amp;pool, cycle, team_team, company).await;\n    // Attempt: another team objective pointing at the team (not company) parent\n    let err = sqlx::query(\"INSERT INTO objectives (id, tenant_id, cycle_id, scope, name, parent_objective_id, team_id, created_by_subject_id) VALUES ($1, $2, $3, 'team'::okr_scope, 'Bad', $4, $5, $6)\")\n        .bind(Uuid::new_v4()).bind(test_tenant()).bind(cycle).bind(team_obj).bind(team_team).bind(test_subject())\n        .execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"team_objective_parent_must_be_company\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/okr/tests/kr_count_bounds_test.rs\n#[tokio::test]\nasync fn objective_with_2_krs_rejected(ctx: TestCtx) {\n    let resp = ctx.post_objective_with_n_krs(2).await;\n    assert_eq!(resp.status(), 400);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"error\"], \"kr_count_out_of_range\");\n}\n\n#[tokio::test]\nasync fn objective_with_6_krs_rejected(ctx: TestCtx) {\n    let resp = ctx.post_objective_with_n_krs(6).await;\n    assert_eq!(resp.status(), 400);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/okr/tests/cycle_status_fsm_test.rs\n#[sqlx::test]\nasync fn backward_transition_rejected(pool: sqlx::PgPool) {\n    let cycle = seed_cycle_at_status(&amp;pool, \"active\").await;\n    let err = sqlx::query(\"UPDATE cycles SET status = 'planning'::cycle_status WHERE id = $1\")\n        .bind(cycle).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"invalid_cycle_status_transition\"));\n}\n\n#[sqlx::test]\nasync fn forward_transition_accepted(pool: sqlx::PgPool) {\n    let cycle = seed_cycle_at_status(&amp;pool, \"planning\").await;\n    sqlx::query(\"UPDATE cycles SET status = 'active'::cycle_status WHERE id = $1\").bind(cycle).execute(&amp;pool).await.unwrap();\n    sqlx::query(\"UPDATE cycles SET status = 'closing'::cycle_status WHERE id = $1\").bind(cycle).execute(&amp;pool).await.unwrap();\n    sqlx::query(\"UPDATE cycles SET status = 'closed'::cycle_status WHERE id = $1\").bind(cycle).execute(&amp;pool).await.unwrap();\n}</code></pre>\n<pre><code class=\"language-rust\">// services/okr/tests/append_only_progress_test.rs\n#[sqlx::test]\nasync fn progress_log_immutable_from_app(pool: sqlx::PgPool) {\n    set_role_app(&amp;pool).await;\n    let row_id = seed_progress(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE kr_progress_log SET value_numeric = 999 WHERE id = $1\")\n        .bind(row_id).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"permission denied\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 8 memory row builders follow the canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>FR-AUTH-003</strong> — RLS enforcement.</li><li><strong>FR-AUTH-101</strong> — RBAC (<code>Resource::OkrObjective</code>, <code>OkrKr</code>).</li></ul>\n<p><strong>Downstream (3 placeholders):</strong></p>\n<ul><li><strong>FR-OKR-002</strong> — full KR type validation (per-type rules for hit_target / improvement / milestone).</li><li><strong>FR-OKR-003</strong> — progress_source DSL.</li><li><strong>FR-OKR-005</strong> — weekly check-in handler (writes to kr_progress_log with source='check_in').</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>FR-AI-003</strong> — memory audit bridge.</li><li><strong>FR-MEMORY-111</strong> — PII scrubbing.</li><li><strong>FR-HR-001</strong> — Member subject_id referenced by Member-scope objectives.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/okr/cycles</h3>\n<pre><code class=\"language-json\">{\n  \"name\": \"Q3 2026\",\n  \"cycle_kind\": \"quarterly\",\n  \"start_date\": \"2026-07-01\",\n  \"end_date\": \"2026-09-30\",\n  \"theme\": \"International expansion\"\n}</code></pre>\n<h3>8.2 — POST /v1/okr/objectives (Team scope with 3 KRs)</h3>\n<pre><code class=\"language-json\">{\n  \"cycle_id\": \"&lt;cycle uuid&gt;\",\n  \"scope\": \"team\",\n  \"name\": \"Backend Engineering — ship multi-region by end of Q3\",\n  \"parent_objective_id\": \"&lt;company-objective uuid&gt;\",\n  \"team_id\": \"&lt;backend-team uuid&gt;\",\n  \"initial_key_results\": [\n    {\"name\": \"Deploy to SG-1 region\", \"kr_type\": \"milestone\", \"target_value_numeric\": 1, \"unit\": \"\"},\n    {\"name\": \"Region failover RTO ≤ 60s\", \"kr_type\": \"hit_target\", \"target_value_numeric\": 60, \"unit\": \"seconds\"},\n    {\"name\": \"Move p95 cross-region latency from 200ms to 50ms\", \"kr_type\": \"improvement\", \"start_value_numeric\": 200, \"target_value_numeric\": 50, \"unit\": \"ms\"}\n  ]\n}</code></pre>\n<h3>8.3 — okr.objective_created memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"okr.objective_created\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"objective_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"cycle_id\": \"&lt;uuid&gt;\",\n  \"scope\": \"team\",\n  \"name_scrubbed\": \"Backend Engineering — [REDACTED-PROJECT]\",\n  \"parent_objective_id\": \"&lt;uuid&gt;\",\n  \"team_id\": \"&lt;uuid&gt;\",\n  \"kr_count\": 3,\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — POST /v1/okr/key_results/{id}/progress</h3>\n<pre><code class=\"language-json\">{\n  \"value_numeric\": 75,\n  \"source\": \"manual\",\n  \"rationale\": \"Closed 25 of 30 issues in proposal; remaining 5 in review\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Full KR per-type validation</strong> — FR-OKR-002 (hit_target ≥ baseline; improvement target ≠ start; milestone target_value = 1).</li><li><strong>Progress source DSL</strong> — FR-OKR-003 (queries against PROJ/INV/HR/LEARN).</li><li><strong>Auto-progress nightly batch</strong> — FR-OKR-004.</li><li><strong>Weekly check-in handler</strong> — FR-OKR-005.</li><li><strong>Monday digest</strong> — FR-OKR-006.</li><li><strong>Quarterly retro draft</strong> — FR-OKR-007.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>RLS bypass</td><td><code>USING</code> predicate</td><td>0 rows</td><td>None — designed</td></tr><tr><td>Cycle backward transition</td><td>trigger</td><td>400 invalid_cycle_status_transition</td><td>Designed</td></tr><tr><td>Company with parent</td><td>trigger</td><td>400 company_objective_has_no_parent</td><td>Designed</td></tr><tr><td>Team without parent</td><td>trigger</td><td>400 team_objective_requires_parent</td><td>Designed</td></tr><tr><td>Team without team_id</td><td>trigger</td><td>400 team_objective_requires_team_id</td><td>Designed</td></tr><tr><td>Team with non-company parent</td><td>trigger</td><td>400 team_objective_parent_must_be_company</td><td>Designed</td></tr><tr><td>Member without owner</td><td>trigger</td><td>400 member_objective_requires_owner</td><td>Designed</td></tr><tr><td>Member with non-team parent</td><td>trigger</td><td>400 member_objective_parent_must_be_team</td><td>Designed</td></tr><tr><td>Cross-cycle alignment</td><td>trigger</td><td>400 cross_cycle_alignment_forbidden</td><td>Designed</td></tr><tr><td>KR count &lt; 3</td><td>handler</td><td>400 kr_count_out_of_range</td><td>Add KRs</td></tr><tr><td>KR count &gt; 5</td><td>handler</td><td>400 kr_count_out_of_range</td><td>Remove KRs</td></tr><tr><td>Add 6th KR</td><td>handler</td><td>409 objective_at_kr_limit</td><td>Designed</td></tr><tr><td>Remove last 3rd KR</td><td>handler</td><td>409 objective_below_kr_min</td><td>Designed</td></tr><tr><td>Face-saving violation introduced</td><td>CI lint</td><td>Build fails</td><td>Use approved terminology</td></tr><tr><td>kr_progress_log UPDATE/DELETE from app</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>objective_status_history UPDATE/DELETE</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>Cycle delete cascades to objectives + KRs</td><td>ON DELETE CASCADE</td><td>Designed</td><td>None</td></tr><tr><td>KR delete with progress_log entries</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Clear progress_log first (elevated perm)</td></tr><tr><td>Duplicate cycle name</td><td>UNIQUE</td><td>INSERT fails</td><td>Use different name</td></tr><tr><td>Duplicate team name</td><td>UNIQUE</td><td>INSERT fails</td><td>Use different name</td></tr><tr><td>end_date &lt;= start_date</td><td>DB CHECK</td><td>INSERT fails</td><td>Use correct dates</td></tr><tr><td>memory audit fail mid-tx</td><td>rollback</td><td>500 audit_failed</td><td>memory_writer health</td></tr><tr><td>PII not scrubbed</td><td>FR-MEMORY-111 + CI test</td><td>Pre-commit failure</td><td>Add rule</td></tr><tr><td>Parent objective deleted while children exist</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Reassign children first</td></tr><tr><td>Team deleted while objectives reference</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Reassign</td></tr><tr><td>Owner subject deleted while objectives reference</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Reassign owner</td></tr><tr><td>Concurrent KR add/remove on same objective</td><td>Postgres serialisable</td><td>One wins; second sees 409</td><td>Caller refetches</td></tr><tr><td>Progress recorded with bad source value</td><td>DB CHECK</td><td>INSERT fails</td><td>Use manual/auto/check_in</td></tr><tr><td>Rationale &gt; 1000 chars</td><td>DB CHECK</td><td>INSERT fails</td><td>Shorten</td></tr><tr><td>Cycle name &gt; 100 chars</td><td>DB CHECK</td><td>INSERT fails</td><td>Shorten</td></tr><tr><td>OpenAPI compliance note missing</td><td>spec lint</td><td>CI fails</td><td>Add note</td></tr><tr><td>Trigger error code drift</td><td>tests assert specific codes</td><td>CI fails</td><td>Restore codes</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Doerr/Grove canonical</strong> — 3-tier cascade + 3-5 KRs + quarterly cycles. Closed enums prevent organisational drift.</li><li><strong>Face-saving terminology baked into schema + CI lint</strong> — Vietnamese cultural adaptation enforced mechanically.</li><li><strong>Alignment tree at trigger</strong> — defense in depth; handler validates too.</li><li><strong>Tenant-local teams primitive</strong> — FR-HR ships Members; OKR ships Teams (no HR Team primitive yet).</li><li><strong>Append-only progress_log via SQL grant</strong> — quarterly retros depend on the full history.</li><li><strong>EU AI Act Art. 14 acknowledgement in OpenAPI</strong> — high-risk-adjacent module; human-in-loop is the contract.</li><li><strong>Cascading delete Cycle → Objectives → KRs</strong> — operator-explicit destructive action.</li><li><strong>KR RESTRICT on progress_log existence</strong> — preserve audit history.</li><li><strong>Milestone as boolean (target=1)</strong> — same column for all KR types; per-type rules in FR-OKR-002.</li><li><strong>Cross-cycle alignment forbidden at trigger</strong> — semantic correctness.</li><li><strong><code>source</code> enum {manual, auto, check_in}</strong> — distinguishes auto-progress batch writes (FR-OKR-004) from operator + check-in writes.</li><li><strong>PII scrubbing on description + rationale + name</strong> — quarterly retros may carry personal context.</li><li><strong>3 closed kr_type enum</strong> — hit_target / improvement / milestone (Doerr canonical).</li><li><strong>5 closed kr_status face-saving values</strong> — on_track / at_risk / learned / achieved / cycled_forward.</li><li><strong>Cycle status unidirectional</strong> — forward only.</li><li><strong>8 memory audit kinds split by lifecycle event</strong> — selectivity at query time.</li><li><strong>Forbidden terminology list bilingual</strong> — Vietnamese equivalents covered.</li><li><strong>OpenAPI <code>_compliance_note</code> mandatory</strong> — embedded in every response for AI Act discoverability.</li></ul>\n<hr>\n<p><em>End of FR-OKR-001.</em></p>";
