window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INV-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The INV service <strong>MUST</strong> accept Wise webhook events at <code>POST /v1/webhooks/wise/{profile_id}</code>, verify the RSA-SHA256 signature against the per-profile public key, decode the event body, persist append-only, and dispatch to FR-INV-006 cash application or hold for CFO review on mismatch.</p>\n<ol><li><strong>MUST</strong> verify the <code>X-Signature-SHA256</code> header on every incoming webhook before any processing (DEC-840). Wise signs the request body with RSA-SHA256 using a per-profile private key; the public key is fetched from <code>/v1/profiles/{profile_id}/webhook-subscriptions/{subscription_id}/public-key</code> and cached. Verification failure returns <code>401 invalid_signature</code> + sev-2 memory audit <code>inv.wise_signature_invalid</code> (NEVER tells Wise it was invalid — return 401 generically).</li></ol>\n<ol><li><strong>MUST</strong> fetch + cache the Wise public key in-process for 24 hours (DEC-841). On the first webhook for a profile, the handler fetches the key and stores <code>(profile_id, pem_key, fetched_at, expires_at)</code> in process memory. Subsequent webhooks reuse the cache.</li></ol>\n<ol><li><strong>MUST</strong> support public-key rotation (DEC-848). On signature-verification failure, the handler re-fetches the public key once, retries verification, and accepts if the new key validates. The 24h overlap window covers Wise's key-rotation contract. Persistent failure after re-fetch is treated per clause #1.</li></ol>\n<ol><li><strong>MUST</strong> define a closed 3-value <code>wise_event_type</code> enum (<code>transfers_state_change</code>, <code>balances_credit</code>, <code>balances_update</code>) per DEC-842. Event types outside this enum are persisted to dead-letter queue with reason <code>unknown_event_type</code> + sev-2 audit. CI cardinality test asserts exactly 3.</li></ol>\n<ol><li><strong>MUST</strong> enforce idempotency via UNIQUE constraint on <code>(profile_id, event_id)</code> (DEC-843). The Wise <code>event_id</code> is a 36-char UUID. Duplicate event MUST return success without re-processing AND without emitting a duplicate memory audit row.</li></ol>\n<ol><li><strong>MUST</strong> reject events with <code>data.occurred_at</code> more than 5 days in the past (DEC-844). Wise's retry window is 5 days; older events are presumed stale. Stale event returns <code>200 OK</code> (Wise's retry stops) + sev-2 audit <code>inv.wise_stale_event</code>. The body is logged for forensic but no processing happens.</li></ol>\n<ol><li><strong>MUST</strong> persist every received event (valid + invalid) to the append-only <code>wise_webhook_events</code> table (DEC-845). The table has REVOKE UPDATE, DELETE FROM cyberos_app; only <code>inv_wise_writer</code> role holds INSERT.</li></ol>\n<ol><li><strong>MUST</strong> return <code>200 OK</code> with empty body within 5 seconds (DEC-850 + Wise SLA). The handler performs signature verification + persistence + WAL-queue push, then returns. Heavy processing (invoice matching, memory emission) happens in a background processor reading from the WAL queue.</li></ol>\n<ol><li><strong>MUST</strong> define a closed 5-value <code>wise_receipt_state</code> enum (<code>received</code>, <code>matched</code>, <code>currency_mismatch</code>, <code>dead_lettered</code>, <code>manually_resolved</code>) per DEC-853. CI cardinality test asserts exactly 5.</li></ol>\n<ol><li><strong>MUST</strong> match incoming transfers to invoices by exact-currency comparison (DEC-846 + DEC-852). The processor delegates to FR-INV-006 cash-application cascade ONLY when the transfer currency matches an outstanding invoice in the same currency. Currency mismatch → state <code>currency_mismatch</code>, row written to <code>unmatched_receipts</code> table, CFO surfaces in dashboard. Never auto-convert.</li></ol>\n<ol><li><strong>MUST</strong> verify the tenant binding before processing. The webhook URL carries <code>profile_id</code>; the processor looks up <code>tenants.wise_profile_id = $1</code> (DEC-847). Profile_id not found → dead-letter + sev-1 audit <code>inv.wise_profile_unknown</code> (this should never happen for legitimate Wise traffic; signature already passed).</li></ol>\n<ol><li><strong>MUST</strong> route failed-3-times events to the dead-letter queue (DEC-851). The processor tracks retry count per event; on 3rd failure, the event moves to <code>dead_lettered</code> state + sev-1 audit <code>inv.wise_dead_lettered</code> + ops alarm. Manual restore is available via <code>POST /v1/admin/wise-events/{id}/restore</code> for CFO role.</li></ol>\n<ol><li><strong>MUST</strong> scrub all Wise event body fields containing customer names, account numbers, or bank references through FR-MEMORY-111 before memory audit emission (DEC-849). Postgres holds raw (RLS-scoped); chain holds scrubbed.</li></ol>\n<ol><li><strong>MUST</strong> emit 7 closed memory audit kinds:</li></ol>\n<ul><li><code>inv.wise_received</code> (sev-3, per valid event)</li><li><code>inv.wise_matched</code> (sev-3, on successful cash-app match)</li><li><code>inv.wise_signature_invalid</code> (sev-2, per verification fail)</li><li><code>inv.wise_stale_event</code> (sev-2, per occurred_at &gt; 5d)</li><li><code>inv.wise_currency_mismatch</code> (sev-2, per mismatch)</li><li><code>inv.wise_dead_lettered</code> (sev-1, per 3-fail)</li><li><code>inv.wise_profile_unknown</code> (sev-1, per unmapped profile_id)</li><li><code>inv.wise_key_rotated</code> (sev-2, per cache refresh due to verification failure)</li></ul>\n<ol><li><strong>MUST</strong> support concurrent webhook delivery without deadlock. Each request opens a short transaction (insert + WAL push). The background processor uses <code>pg_advisory_xact_lock(event_id)</code> to prevent double-processing of the same event from a retry storm.</li></ol>\n<ol><li><strong>MUST</strong> validate request body schema against the Wise event structure for each event_type. Missing required fields (e.g., <code>data.resource.id</code> for transfers#state-change) → 400 Bad Request + sev-2 audit <code>inv.wise_schema_invalid</code>.</li></ol>\n<ol><li><strong>MUST</strong> support per-tenant Wise profile rotation (e.g., tenant changes their Wise account). The handler reads the active <code>wise_profile_id</code> from <code>tenants</code> at request time; the previous profile's webhook URL gracefully returns 410 GONE after a 7-day deprecation window managed via <code>tenants.wise_profile_deprecated_at</code> column.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/admin/wise-events</code> for CFO role to list events with filters (state, tenant_id, date range). RLS-scoped to caller's tenant unless CyberSkill founder.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/admin/wise-events/{id}</code> for CFO to inspect a single event including raw body (Postgres-side, RLS-scoped). The handler scrubs the body through FR-MEMORY-111 only for chain-bound audit rows; the API response shows the raw row content to the authorized CFO.</li></ol>\n<ol><li><strong>MUST</strong> record currency-mismatch events in <code>unmatched_receipts (id, tenant_id, source, source_event_id, currency, amount_minor, occurred_at, notes, resolved_at, resolved_by)</code>. The CFO has a <code>POST /v1/admin/unmatched-receipts/{id}/resolve</code> endpoint to mark resolution + record a note (≥ 10 chars).</li></ol>\n<ol><li><strong>MUST</strong> validate the per-tenant <code>wise_profile_id</code> at tenant provisioning (FR-TEN-001 integration). The profile_id is a 12-digit integer per Wise; format-validated at policy save.</li></ol>\n<ol><li><strong>MUST</strong> reject webhook URLs where the path's <code>profile_id</code> does not match the body's <code>data.resource.profile_id</code> for transfer events. URL-vs-body profile_id mismatch returns <code>400 profile_id_mismatch</code> + sev-2 audit <code>inv.wise_profile_mismatch</code>.</li></ol>\n<ol><li><strong>MUST</strong> enforce TLS 1.3 on the webhook endpoint (no TLS 1.2 fallback). Wise sends from documented IP ranges; we additionally allowlist the Wise IP ranges at the Cloudflare/edge layer as a defense-in-depth bonus (out of scope for this FR, listed in implementation notes for downstream wiring).</li></ol>\n<ol><li><strong>MUST</strong> apply rate limiting per profile_id: max 100 webhooks per second per profile. Burst beyond returns <code>429 TOO_MANY_REQUESTS</code> with <code>Retry-After: 1</code> header. Wise's documented peak rate is well under 100/s; the limit defends against malformed retries or hostile traffic.</li></ol>\n<ol><li><strong>MUST</strong> record a <code>webhook_received_at</code> and <code>webhook_processed_at</code> timestamp pair per event for SLA observability. The pair lets operators identify processing-lag incidents distinct from delivery delays.</li></ol>\n<hr>\n<h2>§2 — Rationale (informative — preserve all 22 paragraphs)</h2>\n<p><strong>§2.1  Why RSA-SHA256 signature and not HMAC.</strong> DEC-840. Wise's design choice. Unlike Stripe (HMAC-SHA256 with shared secret) or Napas247 (HMAC-SHA256 too), Wise uses asymmetric signing — Wise holds the private key, we hold the public key. The benefit: a leaked secret on our side cannot forge signatures. The cost: we have to fetch + rotate public keys. The ring crate handles RSA-SHA256 verification safely.</p>\n<p><strong>§2.2  Why fetch + cache the public key.</strong> DEC-841 + clause #2. Each profile_id has a distinct public key. Caching means we don't fetch on every webhook (Wise's API rate limits + our latency budget). 24h TTL is generous given Wise rotates yearly; the refresh-on-failure pattern (clause #3) covers in-window rotation.</p>\n<p><strong>§2.3  Why retry verification with re-fetched key on failure.</strong> DEC-848 + clause #3. Wise's documented key rotation gives a 24h overlap window. If the cached key fails verification, the most likely cause is rotation. Re-fetching and retrying gracefully handles the rotation without ops intervention.</p>\n<p><strong>§2.4  Why closed event-type enum.</strong> DEC-842 + clause #4. Wise's API has a fixed set of event types we care about. Hardcoding the closed set ensures the handler can't be tricked into running unknown-type processing logic. New event types from Wise upgrades require a code change + DEC entry.</p>\n<p><strong>§2.5  Why idempotency on (profile_id, event_id).</strong> DEC-843 + clause #5. Wise retries delivery up to 5 days on 5xx responses. Without idempotency, every retry would re-process the same event. The UNIQUE constraint at INSERT is the cheapest enforcement; the handler treats duplicate-key as success.</p>\n<p><strong>§2.6  Why 5-day staleness rejection.</strong> DEC-844 + clause #6. Wise's documented retry window. Events older than 5 days won't be retried by Wise; if we receive one, it's either a misconfiguration or an attacker replaying. Either way, processing it serves no legitimate purpose; sev-2 audit makes the rejection visible.</p>\n<p><strong>§2.7  Why background processor not synchronous.</strong> DEC-850 + clause #8. Wise SLA: 5-second response window or webhook is retried (which then deduplicates but adds load). Synchronous processing (signature → DB → match → audit) risks the 5-second budget under DB latency or audit subprocess hangs. Fast 200 + background processing eliminates the timeout vector.</p>\n<p><strong>§2.8  Why currency-mismatch held for CFO review.</strong> DEC-852 + clause #10. A USD wire to a VND invoice is a real situation: an international customer pays in their currency for an invoice we issued in VND. We could auto-convert at the receipt time, but the FX rate used at conversion is policy-significant (SBV daily vs spot vs T+1). Holding for CFO review keeps the call human + auditable.</p>\n<p><strong>§2.9  Why append-only events.</strong> DEC-845 + clause #7. Webhook events are the wire-level forensic record. Any UPDATE/DELETE would create a \"did we receive that event?\" gap. Append-only + REVOKE GRANT is the defense.</p>\n<p><strong>§2.10  Why dead-letter queue at 3 failures.</strong> DEC-851 + clause #12. Transient failures (DB blip, FR-INV-006 race) resolve with retry. Persistent failures (corrupted event, downstream bug) need human attention; auto-retrying forever consumes capacity without resolving. 3 attempts is the standard \"transient vs persistent\" boundary.</p>\n<p><strong>§2.11  Why per-profile rate limit.</strong> Clause #24. A misconfigured Wise customer or an attacker could try to bury us under spurious webhooks. Per-profile (not global) means one bad actor doesn't block legitimate traffic from other profiles. 100/s is well above Wise's documented peak.</p>\n<p><strong>§2.12  Why URL-vs-body profile_id cross-check.</strong> Clause #22. The URL's profile_id is part of our routing; the body's profile_id is part of the signed payload. Mismatch indicates a forged URL or a confused integration. Reject + audit.</p>\n<p><strong>§2.13  Why 7-day deprecation for profile rotation.</strong> Clause #17. Wise profile changes are operational events. 7 days lets the tenant complete in-flight transfers under the old profile before the URL goes 410. After 7 days, the old URL is hard-removed; any straggler webhooks are dead-lettered.</p>\n<p><strong>§2.14  Why CFO surface for unmatched receipts.</strong> Clause #20. Currency-mismatch is a business decision (which invoice does this USD receipt go against?). The CFO has the visibility into the customer relationship needed to make the call. Sales/AR roles see the receipt but cannot resolve it without CFO sign-off (separation of duty).</p>\n<p><strong>§2.15  Why we don't auto-convert FX at receipt time.</strong> DEC-846 + clause #10. FX timing matters for revenue recognition. Receiving USD on day 1, posting against a VND invoice on day 5: which day's FX rate? Auto-conversion forces an answer; CFO review lets the answer match the company's policy (FR-INV-002 SBV daily snapshot, contract terms, or spot-at-receipt).</p>\n<p><strong>§2.16  Why we never log full event body at any level.</strong> Build-envelope <code>disallowed_tools</code>. Wise event bodies contain customer names, bank reference, sometimes invoice references. Bulk logging would create a PII-rich log corpus we don't want. Postgres-side raw storage is RLS-scoped; FR-MEMORY-111 scrubs before chain emission; the API response surface for raw body is CFO-gated.</p>\n<p><strong>§2.17  Why webhook returns 401 generically on signature failure.</strong> Clause #1. Returning a more specific error (\"signature mismatch\", \"expired key\", \"wrong profile\") would give probing attackers feedback. Generic 401 + sev-2 memory audit (operator-visible) is the right split.</p>\n<p><strong>§2.18  Why we don't share-credential authenticate (basic-auth, etc).</strong> Wise's signed-webhook contract is sufficient; layering basic-auth would be redundant and complicate Wise's configuration. The signed body already proves origin + integrity.</p>\n<p><strong>§2.19  Why per-tenant wise_profile_id and not platform-wide.</strong> DEC-847 + clause #11. Tenants control their own Wise accounts; the webhook needs to route to the right tenant's invoices. Per-tenant profile mapping is the natural pattern.</p>\n<p><strong>§2.20  Why we use <code>ring</code> or <code>rsa</code> crate not hand-rolled RSA.</strong> Build-envelope <code>disallowed_tools</code>. RSA signature verification has subtle pitfalls (padding scheme, hash function, key parsing). Battle-tested crates avoid PKCS#1 v1.5 pitfalls and side-channel issues.</p>\n<p><strong>§2.21  Why TLS 1.3 minimum.</strong> Clause #23. TLS 1.2 is acceptable for many integrations but 1.3 is faster + has fewer cipher suite vulnerabilities. Wise supports 1.3; mandating it costs us nothing.</p>\n<p><strong>§2.22  Why we don't store the raw body indefinitely.</strong> The <code>wise_webhook_events.body</code> column carries the full event body in JSONB. Retention policy: 2 years (matches typical financial-record retention). After 2 years, a periodic job purges body to NULL while preserving the audit metadata (profile_id, event_id, event_type, occurred_at, state). The memory chain row stays forever — the chain is the long-term forensic record.</p>\n<hr>\n<h2>§3 — API &amp; schema</h2>\n<h3>§3.1 — Migration 0020: wise_webhook_events</h3>\n<pre><code class=\"language-sql\">-- services/inv/migrations/0020_wise_webhook_events.sql\n\nCREATE TYPE wise_event_type AS ENUM ('transfers_state_change', 'balances_credit', 'balances_update');\nCREATE TYPE wise_receipt_state AS ENUM ('received', 'matched', 'currency_mismatch', 'dead_lettered', 'manually_resolved');\n\nCREATE TABLE wise_webhook_events (\n    id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    profile_id            BIGINT NOT NULL,\n    event_id              CHAR(36) NOT NULL,  -- Wise UUID\n    event_type            wise_event_type NOT NULL,\n    tenant_id             UUID REFERENCES tenants(id),\n    received_at           TIMESTAMPTZ NOT NULL DEFAULT now(),\n    processed_at          TIMESTAMPTZ,\n    occurred_at           TIMESTAMPTZ NOT NULL,\n    signature             TEXT NOT NULL,\n    body                  JSONB NOT NULL,\n    state                 wise_receipt_state NOT NULL DEFAULT 'received',\n    retry_count           INTEGER NOT NULL DEFAULT 0,\n    matched_invoice_id    UUID,\n    matched_allocation_id UUID,\n    error_reason          TEXT,\n    memory_chain_hash      CHAR(64) NOT NULL CHECK (memory_chain_hash ~ '^[0-9a-f]{64}$'),\n    CONSTRAINT idempotent UNIQUE (profile_id, event_id)\n);\n\nCREATE INDEX wise_events_state ON wise_webhook_events (state, received_at) WHERE state IN ('received', 'currency_mismatch');\nCREATE INDEX wise_events_tenant ON wise_webhook_events (tenant_id, received_at DESC);\n\nREVOKE UPDATE, DELETE ON wise_webhook_events FROM cyberos_app;\nGRANT INSERT, SELECT, UPDATE(processed_at, state, retry_count, matched_invoice_id, matched_allocation_id, error_reason)\n    ON wise_webhook_events TO inv_wise_writer;\nGRANT SELECT ON wise_webhook_events TO inv_wise_reader, cfo;\n\nALTER TABLE wise_webhook_events ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON wise_webhook_events\n    USING (tenant_id = current_setting('cyberos.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('cyberos.tenant_id')::uuid);\n\n-- Per-tenant wise profile mapping\nALTER TABLE tenants\n    ADD COLUMN IF NOT EXISTS wise_profile_id BIGINT UNIQUE,\n    ADD COLUMN IF NOT EXISTS wise_profile_deprecated_at TIMESTAMPTZ;</code></pre>\n<h3>§3.2 — Migration 0021: unmatched_receipts</h3>\n<pre><code class=\"language-sql\">-- services/inv/migrations/0021_wise_unmatched_receipts.sql\n\nCREATE TABLE unmatched_receipts (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id       UUID NOT NULL REFERENCES tenants(id),\n    source          TEXT NOT NULL CHECK (source IN ('wise', 'vietqr', 'stripe', 'manual')),\n    source_event_id TEXT NOT NULL,\n    currency        CHAR(3) NOT NULL,\n    amount_minor    BIGINT NOT NULL CHECK (amount_minor &gt; 0),\n    occurred_at     TIMESTAMPTZ NOT NULL,\n    received_at     TIMESTAMPTZ NOT NULL DEFAULT now(),\n    notes           TEXT,\n    resolved_at     TIMESTAMPTZ,\n    resolved_by     UUID REFERENCES subjects(id),\n    resolution_notes TEXT CHECK (resolution_notes IS NULL OR length(resolution_notes) &gt;= 10),\n    memory_chain_hash CHAR(64) NOT NULL\n);\n\nCREATE INDEX unmatched_receipts_tenant ON unmatched_receipts (tenant_id, resolved_at) WHERE resolved_at IS NULL;\n\nREVOKE UPDATE, DELETE ON unmatched_receipts FROM cyberos_app;\nGRANT INSERT, SELECT, UPDATE(resolved_at, resolved_by, resolution_notes) ON unmatched_receipts TO inv_wise_writer;\nGRANT SELECT ON unmatched_receipts TO cfo;\n\nALTER TABLE unmatched_receipts ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON unmatched_receipts\n    USING (tenant_id = current_setting('cyberos.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('cyberos.tenant_id')::uuid);</code></pre>\n<h3>§3.3 — Signature verification</h3>\n<pre><code class=\"language-rust\">// services/inv/src/wise/signature.rs\n\nuse ring::signature::{UnparsedPublicKey, RSA_PKCS1_2048_8192_SHA256};\nuse base64::{engine::general_purpose::STANDARD, Engine};\n\npub fn verify_signature(public_key_pem: &amp;[u8], body: &amp;[u8], signature_b64: &amp;str) -&gt; Result&lt;(), SignatureError&gt; {\n    let signature = STANDARD.decode(signature_b64).map_err(|_| SignatureError::InvalidEncoding)?;\n    let der_key = pem_to_der(public_key_pem)?;\n    let key = UnparsedPublicKey::new(&amp;RSA_PKCS1_2048_8192_SHA256, &amp;der_key);\n    key.verify(body, &amp;signature).map_err(|_| SignatureError::VerifyFailed)\n}</code></pre>\n<h3>§3.4 — Webhook handler</h3>\n<pre><code class=\"language-rust\">// services/inv/src/wise/handler.rs\n\npub async fn handle_wise_webhook(\n    state: State&lt;AppState&gt;,\n    Path(profile_id): Path&lt;u64&gt;,\n    headers: HeaderMap,\n    body: Bytes,\n) -&gt; impl IntoResponse {\n    let started = Instant::now();\n    let sig_header = headers.get(\"X-Signature-SHA256\")\n        .and_then(|v| v.to_str().ok())\n        .unwrap_or(\"\");\n\n    // §1 #2-3 fetch + verify with re-try on failure\n    let public_key = state.wise_keys.get_or_fetch(profile_id).await?;\n    if let Err(_) = signature::verify_signature(&amp;public_key, &amp;body, sig_header) {\n        let refreshed = state.wise_keys.force_refresh(profile_id).await?;\n        if signature::verify_signature(&amp;refreshed, &amp;body, sig_header).is_err() {\n            emit_memory_audit(MemoryKind::WiseSignatureInvalid, profile_id).await;\n            return StatusCode::UNAUTHORIZED;\n        }\n        emit_memory_audit(MemoryKind::WiseKeyRotated, profile_id).await;\n    }\n\n    // Parse + idempotency-check at INSERT\n    let event: WiseEvent = match serde_json::from_slice(&amp;body) {\n        Ok(e) =&gt; e,\n        Err(_) =&gt; {\n            emit_memory_audit(MemoryKind::WiseSchemaInvalid, profile_id).await;\n            return StatusCode::BAD_REQUEST;\n        }\n    };\n    // §1 #6 staleness check\n    if (Utc::now() - event.data.occurred_at).num_days() &gt; 5 {\n        emit_memory_audit(MemoryKind::WiseStaleEvent, profile_id).await;\n        return StatusCode::OK;\n    }\n    // §1 #22 URL-vs-body profile_id cross-check\n    if let Some(body_profile_id) = event.data.resource.profile_id {\n        if body_profile_id != profile_id {\n            emit_memory_audit(MemoryKind::WiseProfileMismatch, profile_id).await;\n            return StatusCode::BAD_REQUEST;\n        }\n    }\n\n    // Persist; INSERT race wins via UNIQUE\n    let tenant_id = state.pool.tenant_for_profile(profile_id).await?;\n    let insert_result = sqlx::query!(\n        r#\"INSERT INTO wise_webhook_events\n              (profile_id, event_id, event_type, tenant_id, occurred_at, signature, body, memory_chain_hash)\n           VALUES ($1, $2, $3, $4, $5, $6, $7, $8)\n           ON CONFLICT (profile_id, event_id) DO NOTHING\"#,\n        profile_id as i64, &amp;event.id, event.event_type as _, tenant_id, event.data.occurred_at, sig_header, body_as_jsonb(&amp;body), &amp;memory_hash\n    ).execute(&amp;state.pool).await;\n\n    // §1 #8 WAL push then fast 200\n    state.wal.push_for_processor(profile_id, event.id.clone()).await;\n    if started.elapsed() &gt; Duration::from_secs(4) {\n        // SLA warning — log but still 200\n        tracing::warn!(elapsed = ?started.elapsed(), \"wise webhook approaching 5s SLA\");\n    }\n    StatusCode::OK\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><code>wise_event_type</code> enum exactly 3 values.</li><li><code>wise_receipt_state</code> enum exactly 5 values.</li><li>Webhook with invalid signature returns 401 + sev-2 audit; no row written.</li><li>Webhook with valid signature returns 200 within 5s + row inserted.</li><li>Duplicate (profile_id, event_id) returns 200 + no second row + no second audit.</li><li>Event with <code>occurred_at</code> 6+ days ago returns 200 + sev-2 stale audit + no processing.</li><li>Event_type outside enum → dead-lettered + sev-2 audit <code>unknown_event_type</code>.</li><li>URL profile_id ≠ body profile_id → 400 + sev-2 audit.</li><li>Body schema invalid → 400 + sev-2 audit.</li><li>Public key cache TTL = 24h; refresh on signature failure once.</li><li>Signature retry after re-fetched key succeeds → sev-2 audit <code>wise_key_rotated</code>.</li><li>Persistent signature failure after re-fetch → 401 + sev-2.</li><li>Currency mismatch (USD receipt to VND invoice) → state <code>currency_mismatch</code> + unmatched_receipts row + sev-2 audit.</li><li>Currency match → delegates to FR-INV-006 cash-app → sev-3 <code>wise_matched</code> audit on success.</li><li>3rd processing failure → state <code>dead_lettered</code> + sev-1 audit + ops alarm.</li><li>CFO restore via <code>POST /v1/admin/wise-events/{id}/restore</code> resets state to <code>received</code> + new processing attempt.</li><li><code>GET /v1/admin/wise-events</code> RLS-scoped to caller's tenant.</li><li><code>wise_webhook_events</code> REVOKE UPDATE/DELETE confirmed at <code>\\dp</code>.</li><li>Cross-tenant SELECT returns empty (RLS).</li><li>Per-tenant <code>wise_profile_id</code> validated at provisioning (12-digit integer).</li><li>Profile deprecation: 7-day window then URL returns 410 GONE.</li><li>Background processor uses <code>pg_advisory_xact_lock(event_id)</code> to prevent double-processing.</li><li>Per-profile rate limit: 101st request/second returns 429 with Retry-After=1.</li><li>TLS 1.3 enforced; TLS 1.2 client gets connection refused.</li><li>CFO can read raw event body via <code>GET /v1/admin/wise-events/{id}</code> (RLS-scoped); other roles cannot.</li><li><code>unmatched_receipts.resolution_notes</code> ≥ 10 chars enforced; resolve handler requires CFO role.</li><li>memory audit row emitted per 8 closed kinds.</li><li>All scrubbable text routed through FR-MEMORY-111 before chain emission.</li><li>Webhook latency p99 &lt; 4s (signature verify + DB insert + WAL push).</li><li>Body retention 2 years; cleanup job NULLs body column while preserving metadata.</li></ol>\n<hr>\n<h2>§5 — Verification (CI tests)</h2>\n<ul><li><code>cardinality_test_event_type</code> — 3.</li><li><code>cardinality_test_receipt_state</code> — 5.</li><li><code>signature_valid_test</code> — fixture event + correct key → 200 + row.</li><li><code>signature_invalid_test</code> — wrong key → 401 + sev-2.</li><li><code>signature_rotated_test</code> — old cached key fails + new key succeeds → 200 + sev-2.</li><li><code>idempotency_test</code> — second POST with same event_id → 200 + no second row.</li><li><code>staleness_test</code> — occurred_at = -6d → 200 + sev-2 stale + no processing.</li><li><code>unknown_event_type_test</code> — <code>transfers#unsupported</code> → dead-lettered + sev-2.</li><li><code>profile_mismatch_test</code> — URL profile != body profile → 400.</li><li><code>schema_invalid_test</code> — missing required field → 400.</li><li><code>currency_mismatch_test</code> — USD receipt + VND invoice → unmatched_receipts row + sev-2.</li><li><code>currency_match_test</code> — delegates to FR-INV-006 → matched + sev-3.</li><li><code>dead_letter_test</code> — fail 3× → dead_lettered + sev-1.</li><li><code>restore_acl_test</code> — non-CFO restore → 403; CFO restore → 200 + state=received.</li><li><code>rls_isolation_test</code> — two tenants, cross-query empty.</li><li><code>append_only_test</code> — REVOKE inspection.</li><li><code>profile_id_format_test</code> — non-12-digit profile_id at provisioning → 400.</li><li><code>deprecation_window_test</code> — at +7d, URL returns 410.</li><li><code>advisory_lock_test</code> — concurrent processor invocations → no double-processing.</li><li><code>rate_limit_test</code> — 101st request/sec → 429.</li><li><code>tls_1_3_test</code> — TLS 1.2 client → connection refused.</li><li><code>cfo_raw_body_test</code> — CFO sees full body; other roles get 403.</li><li><code>resolution_notes_length_test</code> — &lt; 10 chars → 400.</li><li><code>latency_test</code> — p99 &lt; 4s under sustained load.</li></ul>\n<hr>\n<h2>§6 — File skeleton</h2>\n<pre><code>services/inv/\n├── src/\n│   ├── wise/\n│   │   ├── mod.rs              # pub re-exports\n│   │   ├── handler.rs          # POST /v1/webhooks/wise/{profile_id} (§3.4)\n│   │   ├── signature.rs        # RSA-SHA256 verify (§3.3)\n│   │   ├── parser.rs           # event_type discriminated parser\n│   │   ├── public_key.rs       # fetch + cache + rotation\n│   │   ├── processor.rs        # background WAL consumer + match delegate\n│   │   ├── unmatched.rs        # unmatched_receipts persistence\n│   │   ├── audit.rs            # 8 memory kinds\n│   │   └── error.rs            # SignatureError + ProcessError\n│   └── admin/\n│       ├── wise_list.rs        # GET /v1/admin/wise-events\n│       ├── wise_show.rs        # GET /v1/admin/wise-events/{id}\n│       ├── wise_restore.rs     # POST /v1/admin/wise-events/{id}/restore\n│       └── unmatched_resolve.rs # POST /v1/admin/unmatched-receipts/{id}/resolve\n├── migrations/\n│   ├── 0020_wise_webhook_events.sql\n│   └── 0021_wise_unmatched_receipts.sql\n└── tests/\n    ├── wise_signature_test.rs\n    ├── wise_idempotency_test.rs\n    ├── wise_currency_mismatch_test.rs\n    ├── wise_replay_test.rs\n    └── wise_key_rotation_test.rs</code></pre>\n<hr>\n<h2>§7 — Dependencies &amp; blast-radius</h2>\n<p><strong>Depends on</strong>: FR-AUTH-101 (RBAC for CFO role + tenant_admin gates).</p>\n<p><strong>Blocks</strong>: FR-INV-006 (cash-application — wise receipts feed the cascade as one source).</p>\n<p><strong>Blast radius if broken</strong>:</p>\n<ul><li><strong>Signature bypass</strong>: forged transfer events credit unearned invoices; sev-1.</li><li><strong>Idempotency bug</strong>: same payment counted twice on retry; CFO catches in reconciliation.</li><li><strong>Currency auto-convert at receipt time</strong>: FX timing controversy + revenue misstatement.</li><li><strong>Dead-letter accumulation</strong>: queue grows unbounded; ops alarm + manual review.</li></ul>\n<hr>\n<h2>§8 — Payload examples</h2>\n<h3>§8.1 — Webhook delivery</h3>\n<pre><code>POST /v1/webhooks/wise/12345678\nX-Signature-SHA256: BASE64_RSA_SIGNATURE\nContent-Type: application/json\n\n{\n  \"data\": {\n    \"resource\": {\n      \"id\": 90019283,\n      \"profile_id\": 12345678,\n      \"type\": \"transfer\"\n    },\n    \"current_state\": \"outgoing_payment_sent\",\n    \"previous_state\": \"processing\",\n    \"occurred_at\": \"2026-05-16T10:30:00Z\"\n  },\n  \"subscription_id\": \"sub-abc\",\n  \"event_type\": \"transfers#state-change\",\n  \"schema_version\": \"2.0.0\",\n  \"sent_at\": \"2026-05-16T10:30:01Z\"\n}\n\n200 OK</code></pre>\n<h3>§8.2 — Invalid signature</h3>\n<pre><code>POST /v1/webhooks/wise/12345678\nX-Signature-SHA256: WRONG\n\n401 Unauthorized</code></pre>\n<h3>§8.3 — Unmatched receipt list</h3>\n<pre><code>GET /v1/admin/unmatched-receipts\n\n200 OK\n[\n  {\n    \"id\": \"ur_xyz\",\n    \"source\": \"wise\",\n    \"currency\": \"USD\",\n    \"amount_minor\": 50000,  // $500\n    \"occurred_at\": \"2026-05-16T10:30:00Z\",\n    \"resolved_at\": null\n  }\n]</code></pre>\n<h3>§8.4 — Restore dead-lettered event</h3>\n<pre><code>POST /v1/admin/wise-events/{id}/restore\nAuthorization: Bearer &lt;cfo&gt;\n\n200 OK\n{ \"state\": \"received\", \"next_attempt_at\": \"2026-05-16T11:00:00Z\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<ul><li><strong>OQ-1</strong> (closed by DEC-846): no auto-convert at receipt time.</li><li><strong>OQ-2</strong> (closed by DEC-852): currency mismatch held for CFO.</li><li><strong>OQ-3</strong> (open): support Wise's <code>balances#statement-update</code> event-type? Not in scope for initial slice; revisit when CFO requests balance-reconciliation tooling.</li><li><strong>OQ-4</strong> (open): support multi-profile per tenant (e.g., one Wise profile per legal entity)? Currently one per tenant; the legal-entity workflow would need additional schema work.</li></ul>\n<hr>\n<h2>§10 — Failure modes (32 rows)</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Sev</th><th>Handler</th></tr></thead><tbody><tr><td>1</td><td>Signature verification failure</td><td>RSA verify</td><td>2</td><td>401 + sev-2</td></tr><tr><td>2</td><td>Public key fetch fails</td><td>HTTP error</td><td>2</td><td>Use cached key if still valid; sev-2 audit</td></tr><tr><td>3</td><td>Public key expired + fetch fails</td><td>both stale</td><td>1</td><td>401 + sev-1; ops alarm</td></tr><tr><td>4</td><td>Event body not valid JSON</td><td>parser error</td><td>2</td><td>400 + sev-2</td></tr><tr><td>5</td><td>Event body schema mismatch</td><td>required field missing</td><td>2</td><td>400 + sev-2</td></tr><tr><td>6</td><td>URL profile_id ≠ body profile_id</td><td>cross-check</td><td>2</td><td>400 + sev-2</td></tr><tr><td>7</td><td>event_type outside closed enum</td><td>match</td><td>2</td><td>Dead-lettered + sev-2</td></tr><tr><td>8</td><td>Idempotency collision (legitimate retry)</td><td>UNIQUE</td><td>3</td><td>200 + no second row</td></tr><tr><td>9</td><td>occurred_at &gt; 5 days old</td><td>timestamp</td><td>2</td><td>200 + sev-2 stale audit</td></tr><tr><td>10</td><td>Currency mismatch (USD→VND)</td><td>currency compare</td><td>2</td><td>unmatched_receipts + sev-2</td></tr><tr><td>11</td><td>FR-INV-006 cash-app match fails</td><td>downstream error</td><td>2</td><td>retry_count++; eventual dead-letter</td></tr><tr><td>12</td><td>3rd cash-app failure → dead_lettered</td><td>retry counter</td><td>1</td><td>sev-1 + ops alarm</td></tr><tr><td>13</td><td>profile_id not in tenants table</td><td>lookup miss</td><td>1</td><td>Dead-letter + sev-1 (should never happen post-sig)</td></tr><tr><td>14</td><td>Webhook latency &gt; 5s</td><td>SLA monitor</td><td>2</td><td>Wise retries; idempotency covers</td></tr><tr><td>15</td><td>Wise key rotation mid-delivery</td><td>sig fail → re-fetch</td><td>2</td><td>Re-fetch + retry; sev-2 audit</td></tr><tr><td>16</td><td>TLS 1.2 client</td><td>TLS handshake</td><td>3</td><td>Connection refused</td></tr><tr><td>17</td><td>Rate limit exceeded (&gt;100/s/profile)</td><td>rate limiter</td><td>3</td><td>429 + Retry-After</td></tr><tr><td>18</td><td>DB outage during INSERT</td><td>sqlx error</td><td>1</td><td>500; Wise retries within 5d</td></tr><tr><td>19</td><td>memory audit subprocess fails</td><td>timeout</td><td>1</td><td>WAL retry; sev-1 if exhausted</td></tr><tr><td>20</td><td>Background processor lock contention</td><td>pg_advisory_xact_lock</td><td>3</td><td>Skip + retry next tick</td></tr><tr><td>21</td><td>Restore by non-CFO role</td><td>RBAC</td><td>2</td><td>403 + sev-2</td></tr><tr><td>22</td><td>Restore with no pending event</td><td>application check</td><td>3</td><td>404</td></tr><tr><td>23</td><td>unmatched_receipts.resolution_notes &lt; 10 chars</td><td>CHECK</td><td>3</td><td>400</td></tr><tr><td>24</td><td>Cross-tenant RLS leak</td><td>rls_isolation_test</td><td>1</td><td>CI blocks</td></tr><tr><td>25</td><td>wise_profile_id format invalid</td><td>format check</td><td>3</td><td>400 at provisioning</td></tr><tr><td>26</td><td>wise_profile_deprecated_at expired</td><td>timestamp + middleware</td><td>3</td><td>410 GONE</td></tr><tr><td>27</td><td>Duplicate UNIQUE INSERT race</td><td>ON CONFLICT DO NOTHING</td><td>3</td><td>Treated as duplicate; no error</td></tr><tr><td>28</td><td>Background processor dies mid-event</td><td>WAL replay on restart</td><td>2</td><td>Resume from advisory lock release</td></tr><tr><td>29</td><td>memory_chain_hash regex fails</td><td>CHECK</td><td>1</td><td>INSERT rejected</td></tr><tr><td>30</td><td>Public-key cache size unbounded</td><td>LRU max 10000</td><td>3</td><td>Eviction</td></tr><tr><td>31</td><td>Body &gt; 1 MiB (Wise unlikely but defensive)</td><td>request body limit</td><td>3</td><td>413 Payload Too Large</td></tr><tr><td>32</td><td>Resolved unmatched receipt re-resolved</td><td>resolved_at check</td><td>3</td><td>409 already_resolved</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> RSA verification uses the <code>ring</code> crate (<code>RSA_PKCS1_2048_8192_SHA256</code> algorithm). The PEM-to-DER conversion happens once per key fetch.</p>\n<p><strong>§11.2</strong> The public key cache is <code>Arc&lt;RwLock&lt;HashMap&lt;u64, (Vec&lt;u8&gt;, Instant)&gt;&gt;&gt;</code> keyed on profile_id; the per-profile 24h TTL is per-entry.</p>\n<p><strong>§11.3</strong> The Wise API endpoint to fetch the public key requires authentication with a tenant-scoped Wise API token, stored encrypted in <code>tenants.wise_api_token_kms_blob</code> (analogous to FR-AUTH-103 SP signing key). KMS decryption happens once at cache populate.</p>\n<p><strong>§11.4</strong> The webhook handler is mounted at <code>POST /v1/webhooks/wise/{profile_id}</code> with no auth middleware — Wise authenticates via signature. The request size limit is 1 MiB (defensive; Wise events are typically &lt; 4 KiB).</p>\n<p><strong>§11.5</strong> The background processor is a single tokio task per gateway process consuming the WAL channel; on event arrival, it acquires <code>pg_advisory_xact_lock(event_id)</code> and processes.</p>\n<p><strong>§11.6</strong> The processor's retry policy: exponential backoff (1m, 5m, 30m); 3rd failure → dead_lettered.</p>\n<p><strong>§11.7</strong> The dead-letter restore endpoint resets retry_count to 0 + state to <code>received</code> + emits sev-2 <code>wise_restored</code> audit (under <code>wise_dead_lettered</code> kind with action=restored sub-field).</p>\n<p><strong>§11.8</strong> The <code>unmatched_receipts</code> table is shared across sources (wise, vietqr, stripe, manual); the <code>source</code> column discriminates.</p>\n<p><strong>§11.9</strong> The CFO's <code>wise_show</code> endpoint that surfaces raw body has a sev-3 <code>wise_raw_body_viewed</code> audit emission (operator-introspection visibility).</p>\n<p><strong>§11.10</strong> The 24h public-key cache TTL + the 1-hour overlap window cover ~99% of Wise rotation events without intervention.</p>\n<p><strong>§11.11</strong> Tests use a fixture RSA key pair (generated at test setup) and fixture event payloads from Wise documentation.</p>\n<p><strong>§11.12</strong> The advisory lock key is <code>hashtext(event_id)::bigint</code>; collisions are bounded by the 64-bit hash space.</p>\n<p><strong>§11.13</strong> The processor uses the <code>inv_wise_writer</code> role (not the public <code>cyberos_app</code>) so it can UPDATE the state column.</p>\n<p><strong>§11.14</strong> Rate limiting is implemented via <code>governor</code> crate per-profile token bucket; 100/s burst with 100/s refill.</p>\n<p><strong>§11.15</strong> TLS 1.3 enforcement is at the gateway/edge layer (Cloudflare); the application doesn't re-enforce — it trusts the edge for transport. Documented for ops in the deployment runbook.</p>\n<p><strong>§11.16</strong> The <code>wise_webhook_events.body</code> column has a periodic NULL-out job at 2 years (matches financial-record retention); the cleanup preserves all non-body columns.</p>\n<p><strong>§11.17</strong> The 5-day staleness check uses <code>Utc::now() - event.data.occurred_at</code> (not <code>received_at - occurred_at</code>) so a delayed delivery that was Wise-side queued isn't penalized.</p>\n<p><strong>§11.18</strong> Wise profile rotation: the previous URL stays alive for 7 days returning 410. After 7 days, the URL returns 404. The deprecation window is a tenant-side operational signal.</p>\n<p><strong>§11.19</strong> The processor delegates the cash-application match to FR-INV-006 via a function call (not HTTP) — same Rust binary; no network hop.</p>\n<p><strong>§11.20</strong> The <code>processed_at</code> timestamp is set when the processor finishes (success or dead-letter). The pair (received_at, processed_at) computes per-event processing latency.</p>\n<p><strong>§11.21</strong> Cross-tenant CFO view requires <code>cyberskill_founder</code> role + a SECURITY DEFINER function that sets tenant_id to a sentinel value, emitting a sev-3 audit per call.</p>\n<p><strong>§11.22</strong> The webhook URL pattern (<code>/v1/webhooks/wise/{profile_id}</code>) is configured at Wise side per profile. Changing the URL requires updating the Wise subscription (out-of-band ops task).</p>\n<hr>\n<p><em>End of FR-INV-004 spec.</em></p>";
