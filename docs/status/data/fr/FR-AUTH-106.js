window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-106"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> evaluate every successful credential verification against the subject's recent login geography and require an adaptive MFA challenge when the apparent travel speed since the prior login exceeds the per-tenant impossible-travel threshold.</p>\n<ol><li><strong>MUST</strong> invoke <code>travel::evaluate(subject_id, ip, occurred_at)</code> after successful credential verification (password + Argon2 match passes) but BEFORE issuing the AUTH session JWT. The evaluator returns one of: <code>Allowed</code>, <code>ChallengeRequired</code>, <code>Blocked</code> (per per-tenant action policy in DEC-746).</li></ol>\n<ol><li><strong>MUST</strong> geolocate the source IP using the locally-bundled MaxMind GeoLite2 City database (DEC-745). The lookup returns <code>(latitude, longitude, country_iso2, asn, is_anonymous_proxy)</code>. No external geolocation API call MUST be made — every login's IP must be resolvable from the local DB.</li></ol>\n<ol><li><strong>MUST</strong> compute the great-circle distance between the current login's geo coordinates and the most-recent prior login's coordinates using the Haversine formula on the WGS-84 sphere. The Haversine formula is sufficient (accuracy ≤ 0.5%); we don't need Vincenty's full ellipsoid math.</li></ol>\n<ol><li><strong>MUST</strong> compute the apparent travel speed as <code>distance_km / hours_since_prior_login</code>. Speeds with <code>hours_since_prior_login &lt; 0.01</code> (&lt; 36 seconds) are treated as the same-session — no travel evaluation, the result is <code>Allowed</code>.</li></ol>\n<ol><li><strong>MUST</strong> flag the login as impossible-travel when the apparent speed exceeds <code>tenants.impossible_travel_speed_kmh</code> (default 900 per DEC-741, range [200, 5000] per DEC-742).</li></ol>\n<ol><li><strong>MUST</strong> apply the per-tenant <code>impossible_travel_action</code> policy (DEC-746):</li></ol>\n<ul><li><code>challenge</code>: return <code>200 OK { result: \"challenge_required\", challenge_id, methods: [...] }</code> (mfa challenge linkage); the session JWT is NOT issued until challenge-completion endpoint succeeds (FR-AUTH-102 challenge flow).</li><li><code>block</code>: return <code>403 FORBIDDEN { error: \"impossible_travel_blocked\", prior_country, current_country, speed_kmh, threshold_kmh }</code>. The session JWT is NOT issued; the subject must contact ops.</li><li><code>warn_only</code>: issue the session JWT but emit a sev-2 memory audit row <code>auth.travel_warn_only</code>. No user friction.</li></ul>\n<ol><li><strong>MUST</strong> bypass the impossible-travel check when the same-subject's prior login was within 24h AND same ISO country AND same ASN (DEC-749). This guards against carrier-IP roaming false positives (e.g., a Vietnamese subject's mobile session switches from Viettel cell tower at HCMC to one near the Cambodian border — different geo by 100km, same country same ASN, well under 24h, real human movement).</li></ol>\n<ol><li><strong>MUST</strong> bypass the impossible-travel check when the current IP matches one of the per-tenant <code>impossible_travel_ip_allowlist_cidrs</code> entries (DEC-755). The list is a JSON array of CIDR strings (e.g., <code>[\"203.0.113.0/24\", \"198.51.100.42/32\"]</code>), validated at policy save (each entry MUST parse as <code>IpNet</code>; total list ≤ 64 entries). Bypassed logins emit a sev-3 audit <code>auth.travel_allowlist_bypass</code>.</li></ol>\n<ol><li><strong>MUST</strong> define a closed 4-value <code>travel_decision</code> Postgres enum (<code>allowed</code>, <code>challenged</code>, <code>allowed_after_challenge</code>, <code>blocked</code>). CI cardinality test asserts exactly 4 (DEC-744).</li></ol>\n<ol><li><strong>MUST</strong> define a closed 3-value <code>impossible_travel_action</code> Postgres enum (<code>challenge</code>, <code>block</code>, <code>warn_only</code>). CI cardinality test asserts exactly 3 (DEC-746).</li></ol>\n<ol><li><strong>MUST</strong> persist every login (challenged + allowed + blocked) to the <code>login_history_geo</code> append-only table per FR-AUTH-103's existing login_history pattern, extended with <code>(latitude, longitude, country_iso2, asn, is_anonymous_proxy, travel_decision, speed_kmh_vs_prior)</code>. The table is REVOKE UPDATE, DELETE FROM cyberos_app + privileged auth_writer role.</li></ol>\n<ol><li><strong>MUST</strong> maintain a 24-hour sliding window for prior-login lookup (DEC-747). The query: <code>SELECT latitude, longitude, occurred_at, country_iso2, asn FROM login_history_geo WHERE subject_id = $1 AND travel_decision IN ('allowed', 'allowed_after_challenge') AND occurred_at &gt; now() - INTERVAL '24 hours' ORDER BY occurred_at DESC LIMIT 1</code>. Older entries are not considered (a login 25h ago from Paris doesn't trigger when the next login is from New York 24h+1min later — but new-subject + sparse-login users are normal and should not be flagged).</li></ol>\n<ol><li><strong>MUST</strong> detect anonymous IP networks (VPN, Tor exit, proxy) via the MaxMind anonymous-IP database (DEC-750). When <code>is_anonymous_proxy = true</code>, the evaluator emits a sev-2 memory audit row <code>auth.travel_anonymous_ip</code> regardless of speed. By default, anonymous-IP logins ARE allowed (we don't auto-block — some users legitimately use VPNs). The per-tenant policy <code>block_anonymous_ip</code> (boolean, default false) can flip the default to block.</li></ol>\n<ol><li><strong>MUST</strong> validate the MaxMind DB freshness at process startup. If <code>mmdb.metadata().build_timestamp</code> is more than 30 days old, emit a sev-2 memory audit row <code>auth.travel_geoip_stale</code> (DEC-751). The service still starts and uses the stale DB — the staleness audit is the operational signal, not a hard block.</li></ol>\n<ol><li><strong>MUST</strong> route the adaptive challenge through FR-AUTH-102 (the existing MFA challenge issuer). The travel-evaluator creates a challenge with <code>kind = \"impossible_travel\"</code>, returns the challenge_id to the client. The client submits MFA proof via <code>POST /v1/auth/mfa/challenge/{id}/verify</code> per FR-AUTH-102. On verify-success, the AUTH session JWT is finally issued and a <code>login_history_geo</code> row is recorded with <code>travel_decision = 'allowed_after_challenge'</code>.</li></ol>\n<ol><li><strong>MUST</strong> suppress repeat challenges within 30 minutes from the same subject + same IP (DEC-753). This avoids friction for a user who completes an impossible-travel challenge from a coffee-shop wifi then immediately re-authenticates after a session timeout. The 30-min window emits a sev-3 audit <code>auth.travel_challenge_suppressed_repeat</code>.</li></ol>\n<ol><li><strong>MUST</strong> restrict policy mutation (<code>impossible_travel_speed_kmh</code>, <code>impossible_travel_action</code>, <code>block_anonymous_ip</code>, <code>impossible_travel_ip_allowlist_cidrs</code>) to the <code>security_admin</code> role per FR-AUTH-101 + a sev-2 memory audit row with reason ≥10 chars.</li></ol>\n<ol><li><strong>MUST</strong> validate per-tenant <code>impossible_travel_speed_kmh</code> at policy write time. Range [200, 5000]. Out-of-range returns <code>400 BAD_REQUEST</code> with <code>{error: \"speed_threshold_out_of_range\", min: 200, max: 5000}</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate per-tenant <code>impossible_travel_ip_allowlist_cidrs</code> at policy write time. Each CIDR parses via Rust <code>ipnetwork::IpNet</code>; invalid entries return <code>400</code>. List length ≤ 64. Per-entry max-prefix-length: IPv4 /0 forbidden, /8 forbidden (too broad); IPv6 /0 forbidden, /16 forbidden. Tightest acceptable prefix is /9 for IPv4 and /17 for IPv6.</li></ol>\n<ol><li><strong>MUST</strong> scrub all reason-bearing audit text (policy reason, block error message extensions) via FR-MEMORY-111 before memory chain emission. Geo coordinates + country + ASN are operational metadata, not PII; they pass through.</li></ol>\n<ol><li><strong>MUST</strong> emit 6 closed memory audit kinds (DEC-754):</li></ol>\n<ul><li><code>auth.travel_allowed</code> (sev-3, every login under threshold)</li><li><code>auth.travel_challenged</code> (sev-2, every login over threshold that triggered challenge)</li><li><code>auth.travel_allowed_after_challenge</code> (sev-2, challenge-completion success)</li><li><code>auth.travel_blocked</code> (sev-2, action=block tenant policy)</li><li><code>auth.travel_anonymous_ip</code> (sev-2, VPN/Tor/proxy)</li><li><code>auth.travel_geoip_stale</code> (sev-2, MaxMind DB &gt; 30d old at startup)</li><li><code>auth.travel_policy_changed</code> (sev-2, per security_admin mutation)</li><li><code>auth.travel_allowlist_bypass</code> (sev-3, allowlisted IP bypass)</li><li><code>auth.travel_challenge_suppressed_repeat</code> (sev-3, 30-min window suppression)</li><li><code>auth.travel_warn_only</code> (sev-2, action=warn_only would-have-triggered)</li></ul>\n<ol><li><strong>MUST</strong> treat IPv4 and IPv6 uniformly — both are geolocated via the same MaxMind DB. NAT64 / dual-stack edge cases that yield different geo for v4 vs v6 from the same client are accepted as-is; the evaluator uses whichever IP the request arrived on.</li></ol>\n<ol><li><strong>MUST</strong> support a <code>dry_run</code> mode on the policy update endpoint: <code>dry_run=true</code> returns the proposed effective config + a list of last-7-days logins that would have been challenged/blocked under the proposed policy. No DB mutation, no audit row emitted.</li></ol>\n<ol><li><strong>MUST</strong> maintain a per-subject <code>recent_login_geo_cache</code> in-process LRU (Arc&lt;Mutex&lt;lru::LruCache&lt;SubjectId, (lat, lon, occurred_at, country, asn)&gt;&gt;&gt;&gt;) sized to 50000 entries. On evaluator call, check the cache first; cache hit short-circuits the Postgres SELECT. Cache miss falls back to the Postgres query in clause #12. The cache is per-AUTH-process and is invalidated on every successful login (write-through).</li></ol>\n<ol><li><strong>MUST</strong> treat any IP that fails geolocation (private network, reserved space, unallocated v6) as <code>travel_decision = 'allowed'</code> with a sev-3 audit row <code>auth.travel_geoip_unresolvable</code>. This avoids false positives for development environments + reserved corporate networks.</li></ol>\n<ol><li><strong>MUST</strong> emit the prior-login geo coordinates in the memory audit chain row (not in the user-facing API response). The chain row carries <code>{subject_id, current_lat, current_lon, current_country, current_asn, prior_lat, prior_lon, prior_country, prior_asn, hours_delta, distance_km, speed_kmh, threshold_kmh, decision}</code>. The user-facing response on <code>block</code> includes only <code>{current_country, prior_country, speed_kmh, threshold_kmh}</code> — not the exact coordinates (the user knows where they are; the operator gets the precise geo in the chain).</li></ol>\n<ol><li><strong>MUST</strong> define a stale-cache invalidation pathway: when a security_admin updates the per-tenant policy, the in-process cache for that tenant's subjects is NOT flushed (the cache holds last-login geo, which is not policy-dependent). Policy changes take effect on the next login per subject. This is intentional: re-evaluating historical decisions under new policy is out of scope (it would require replaying every login row, an expensive operation we accept as future work).</li></ol>\n<hr>\n<h2>§2 — Rationale (informative — preserve all 22 paragraphs)</h2>\n<p><strong>§2.1  Why Haversine and not Vincenty.</strong> Haversine on a sphere is accurate to ~0.5% for distances &lt; 10000 km (most logins are within this range). Vincenty's iterative ellipsoid method is accurate to ~0.5 mm but adds CPU cycles for nothing — we're comparing against an 900 km/h threshold; sub-percent accuracy in distance contributes &lt; 1 km/h to the speed calculation, far below the threshold's noise floor.</p>\n<p><strong>§2.2  Why 900 km/h default threshold.</strong> DEC-741. Commercial airliners cruise at 800-900 km/h. Setting the threshold below cruise speed would false-positive on every long-haul flight. Setting it above means we miss in-flight credential abuse (impossible at our scale). 900 km/h is the right number for the kinds of users we expect (knowledge workers commuting locally or flying intercontinental).</p>\n<p><strong>§2.3  Why challenge, not block, by default.</strong> DEC-743 + DEC-752 + clause #6 (<code>challenge</code> is default action). Blocking outright punishes a legitimate user who took a red-eye flight to a conference and tries to log in upon landing. The challenge path requires fresh MFA — defeating credential-stuffing attackers (who don't have the second factor) without breaking the legitimate user. The <code>block</code> action exists for high-security tenants who explicitly opt in.</p>\n<p><strong>§2.4  Why local MaxMind DB and not a third-party API.</strong> DEC-745. Third-party geo APIs (ipinfo, ipstack, MaxMind's hosted) add a network round-trip per login + a third-party data-flow + a cost. Local DB is a one-time-per-week refresh, free, and the lookup is a 1-microsecond mmap. The accuracy difference vs hosted is negligible.</p>\n<p><strong>§2.5  Why 24-hour sliding window.</strong> DEC-747. Beyond 24h the prior login is too stale to draw conclusions — most users have multiple logins per day, so the most-recent-within-24h is the right comparison point. For users who log in once a week (some admins) the absence of a within-24h prior login means the current login can't be \"impossible\" relative to a stale comparison; treat as a first-login of the session.</p>\n<p><strong>§2.6  Why ASN bypass for carrier roaming.</strong> DEC-749 + clause #7. Mobile carriers assign IPs dynamically across geographic cell towers. A single subject's mobile session can traverse 200km in 30 minutes (e.g., driving from HCMC to Phan Thiet). The ASN-and-country-match within 24h heuristic suppresses these false positives without weakening the cross-country defense.</p>\n<p><strong>§2.7  Why VPN flagged not blocked by default.</strong> DEC-750 + clause #13. Legitimate users use VPNs (corporate, privacy-conscious, expats accessing home-country services). Auto-blocking would punish them. Flagging at sev-2 lets operators investigate suspicious VPN+travel combinations without breaking normal users. Per-tenant <code>block_anonymous_ip</code> lets paranoid tenants opt in.</p>\n<p><strong>§2.8  Why 30-day MaxMind staleness threshold.</strong> DEC-751 + clause #14. MaxMind publishes weekly. 4-week (30-day) gap means we missed 3+ updates — likely a CI failure. The sev-2 audit at startup is the operational signal; we don't auto-block startup because a stale DB still works (geo data doesn't shift wildly week-over-week).</p>\n<p><strong>§2.9  Why CIDR allowlist per tenant.</strong> DEC-755 + clause #8. Office IPs (corporate egress NAT) are the legitimate \"known safe\" geos. A subject logging in from the office + then from their hotel in the same day should not be challenged. The allowlist lets the tenant configure this without a backdoor.</p>\n<p><strong>§2.10  Why CIDR list capped at 64 entries.</strong> Clause #19. Office IPs are few (CIDR aggregation handles most corp networks). A 64-entry cap prevents operators from accidentally allowlisting entire countries (e.g., /8 of US). The /9-minimum prefix is the defense.</p>\n<p><strong>§2.11  Why challenge suppression at 30min.</strong> DEC-753 + clause #16. A coffee-shop user who completes MFA for impossible-travel then session-timeouts 25min later should not be re-challenged on the next login from the same shop. 30min is the user's tolerance for \"I just did this 5 minutes ago, why again\". Beyond 30min, the challenge re-arms (defense-in-depth against extended sessions on hostile networks).</p>\n<p><strong>§2.12  Why dry-run on policy update.</strong> Clause #23. Security_admins want to see \"how many of my recent logins would have been challenged if I tightened the threshold to 500 km/h?\" before flipping the policy live. Dry-run returns the count + a sample list. No DB mutation; no audit row.</p>\n<p><strong>§2.13  Why per-subject in-process cache.</strong> Clause #24. The Postgres SELECT for prior login is a few ms — but multiplied by every login across many AUTH processes, it adds up. The 50000-entry LRU caches the hot subjects; on cache miss, the SELECT runs and the result is cached. Cache invalidation on every login (write-through) keeps it correct.</p>\n<p><strong>§2.14  Why we don't store full IP history.</strong> The <code>login_history_geo</code> carries one row per login with coordinates — that's the historical record. We don't store every IP indefinitely as a separate table because (a) coordinates + country + ASN are sufficient for forensics, (b) raw IPs are mildly PII-sensitive and we want to scrub-on-export. The geo row IS the forensic record.</p>\n<p><strong>§2.15  Why we redact exact coordinates from the user-facing block response.</strong> Clause #26. A user who sees their exact prior coordinates in an error message might feel surveilled. The chain row (operator-visible) carries the precise geo; the user response shows only country + speed. This is the same pattern as the FR-AUTH-107 audit: operator sees count, user sees vague reject.</p>\n<p><strong>§2.16  Why the MaxMind DB is vendored, not pulled at runtime.</strong> Pulling at runtime means an unreachable MaxMind CDN bricks new AUTH process starts. Vendoring (committing the .mmdb file to a release artifact, refreshed weekly via CI) makes startup deterministic.</p>\n<p><strong>§2.17  Why we don't auto-challenge on country-change without speed-based reasoning.</strong> A user living in San Francisco who logs in from London 24h later has traveled by airliner — legitimate. A user in San Francisco who logs in from London 30 minutes later did NOT travel — that's the impossible-travel signal. Country-change alone false-positives too often (border-region users, dual-citizenship workers); speed-based is the right metric.</p>\n<p><strong>§2.18  Why first-login of a new subject is always allowed.</strong> A new subject has no prior login in the 24h sliding window. The evaluator returns <code>Allowed</code> and the audit row marks it as a first-login. This is the correct semantic: we can't reason about travel from no data.</p>\n<p><strong>§2.19  Why we run the evaluator AFTER credential check.</strong> Clause #1. The credential check is fast (Argon2 = 100ms). The travel evaluator is faster (mmap + cache lookup = sub-ms). Running travel first would burn travel-eval cycles on every credential-stuffing attempt; running travel after credentials means only successful credentials get the geo evaluation. This is cheap by ordering.</p>\n<p><strong>§2.20  Why per-tenant action and not global.</strong> DEC-746 + clause #6. Tenant security postures vary: a healthcare customer wants <code>block</code> for compliance; a startup wants <code>warn_only</code> for low friction. Global action would force one shape; per-tenant respects the customer's risk tolerance.</p>\n<p><strong>§2.21  Why we don't expose travel decisions via REST to tenants.</strong> Operators see chain audit rows; the tenant's subjects see only the challenge prompt or block error. We don't expose a \"your last 100 login geos\" API to the subject — that's an attack surface (account takeover sees where the legitimate user was, useful for spear-phishing). The tenant admin's audit-export (FR-DOC-009 future) is the right surface for operator review.</p>\n<p><strong>§2.22  Why the evaluator is in-process and not a separate microservice.</strong> A separate service would add a network hop on the hot login path. The evaluator's footprint (MaxMind DB + cache + Postgres SELECT) is small enough to colocate with AUTH. We revisit if AUTH becomes CPU-bound on travel evaluation.</p>\n<hr>\n<h2>§3 — API &amp; schema</h2>\n<h3>§3.1 — Migration 0016: login_history_geo</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0016_login_history_geo.sql\n\nCREATE TYPE travel_decision AS ENUM ('allowed', 'challenged', 'allowed_after_challenge', 'blocked');\nCREATE TYPE impossible_travel_action AS ENUM ('challenge', 'block', 'warn_only');\n\nCREATE TABLE login_history_geo (\n    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    subject_id          UUID NOT NULL REFERENCES subjects(id),\n    tenant_id           UUID NOT NULL REFERENCES tenants(id),\n    occurred_at         TIMESTAMPTZ NOT NULL DEFAULT now(),\n    ip                  INET NOT NULL,\n    latitude            DOUBLE PRECISION,\n    longitude           DOUBLE PRECISION,\n    country_iso2        CHAR(2),\n    asn                 BIGINT,\n    is_anonymous_proxy  BOOLEAN NOT NULL DEFAULT false,\n    travel_decision     travel_decision NOT NULL,\n    speed_kmh_vs_prior  DOUBLE PRECISION,  -- nullable when no prior login\n    challenge_id        UUID REFERENCES mfa_challenges(id),\n    memory_chain_hash    CHAR(64) NOT NULL CHECK (memory_chain_hash ~ '^[0-9a-f]{64}$')\n);\n\nCREATE INDEX login_geo_subject_time ON login_history_geo (subject_id, occurred_at DESC);\nCREATE INDEX login_geo_tenant_time ON login_history_geo (tenant_id, occurred_at DESC);\nCREATE INDEX login_geo_decision ON login_history_geo (travel_decision) WHERE travel_decision != 'allowed';\n\nREVOKE UPDATE, DELETE ON login_history_geo FROM cyberos_app;\nGRANT INSERT, SELECT ON login_history_geo TO auth_writer;\nGRANT SELECT ON login_history_geo TO security_admin;\n\nALTER TABLE login_history_geo ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON login_history_geo\n    USING (tenant_id = current_setting('cyberos.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('cyberos.tenant_id')::uuid);</code></pre>\n<h3>§3.2 — Migration 0017: per-tenant policy columns</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0017_travel_audit.sql\n\nALTER TABLE tenants\n    ADD COLUMN IF NOT EXISTS impossible_travel_speed_kmh INTEGER NOT NULL DEFAULT 900\n        CHECK (impossible_travel_speed_kmh BETWEEN 200 AND 5000),\n    ADD COLUMN IF NOT EXISTS impossible_travel_action impossible_travel_action NOT NULL DEFAULT 'challenge',\n    ADD COLUMN IF NOT EXISTS block_anonymous_ip BOOLEAN NOT NULL DEFAULT false,\n    ADD COLUMN IF NOT EXISTS impossible_travel_ip_allowlist_cidrs JSONB NOT NULL DEFAULT '[]'::jsonb;\n\n-- Audit table for policy changes (separate from login_history_geo)\nCREATE TABLE travel_policy_audit (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id       UUID NOT NULL REFERENCES tenants(id),\n    actor_id        UUID NOT NULL REFERENCES subjects(id),\n    occurred_at     TIMESTAMPTZ NOT NULL DEFAULT now(),\n    old_config      JSONB NOT NULL,\n    new_config      JSONB NOT NULL,\n    reason          TEXT NOT NULL CHECK (length(reason) &gt;= 10 AND length(reason) &lt;= 1000),\n    memory_chain_hash CHAR(64) NOT NULL\n);\n\nREVOKE UPDATE, DELETE ON travel_policy_audit FROM cyberos_app;\nGRANT INSERT, SELECT ON travel_policy_audit TO auth_writer;\nGRANT SELECT ON travel_policy_audit TO security_admin;\n\nALTER TABLE travel_policy_audit ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON travel_policy_audit\n    USING (tenant_id = current_setting('cyberos.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('cyberos.tenant_id')::uuid);</code></pre>\n<h3>§3.3 — Detector with Haversine + policy enforcement</h3>\n<pre><code class=\"language-rust\">// services/auth/src/travel/detector.rs\n\nuse maxminddb::geoip2;\nuse std::net::IpAddr;\n\npub struct GeoLookup {\n    pub latitude: f64,\n    pub longitude: f64,\n    pub country_iso2: String,\n    pub asn: u64,\n    pub is_anonymous_proxy: bool,\n}\n\npub enum TravelResult {\n    Allowed,\n    AllowlistBypass,\n    AsnRoamBypass,\n    Challenge { challenge_id: Uuid, prior: GeoLookup, current: GeoLookup, speed_kmh: f64 },\n    Block { prior: GeoLookup, current: GeoLookup, speed_kmh: f64 },\n    WarnOnly { speed_kmh: f64 },\n    FirstLogin,\n    Unresolvable,\n}\n\npub async fn evaluate(\n    pool: &amp;PgPool,\n    geoip: &amp;GeoIp,\n    subject_id: Uuid,\n    tenant_id: Uuid,\n    ip: IpAddr,\n    occurred_at: DateTime&lt;Utc&gt;,\n) -&gt; Result&lt;TravelResult, EvalError&gt; {\n    // Policy\n    let policy: TravelPolicy = sqlx::query_as!(\n        TravelPolicy,\n        r#\"SELECT impossible_travel_speed_kmh AS speed_kmh,\n                  impossible_travel_action AS \"action: ImpossibleTravelAction\",\n                  block_anonymous_ip,\n                  impossible_travel_ip_allowlist_cidrs AS allowlist\n           FROM tenants WHERE id = $1\"#,\n        tenant_id\n    ).fetch_one(pool).await?;\n\n    // Allowlist bypass\n    if cidr_match(&amp;policy.allowlist, ip) {\n        return Ok(TravelResult::AllowlistBypass);\n    }\n\n    // Geo-lookup\n    let current = match geoip.lookup(ip) {\n        Ok(c) =&gt; c,\n        Err(_) =&gt; return Ok(TravelResult::Unresolvable),\n    };\n\n    // Anonymous proxy\n    if current.is_anonymous_proxy &amp;&amp; policy.block_anonymous_ip {\n        return Ok(TravelResult::Block { prior: current.clone(), current, speed_kmh: 0.0 });\n    }\n\n    // Prior login lookup (cache → DB)\n    let prior = match prior_login_within_24h(pool, subject_id).await? {\n        Some(p) =&gt; p,\n        None =&gt; return Ok(TravelResult::FirstLogin),\n    };\n\n    // ASN + country bypass\n    if prior.country_iso2 == current.country_iso2 &amp;&amp; prior.asn == current.asn {\n        return Ok(TravelResult::AsnRoamBypass);\n    }\n\n    // Haversine speed\n    let distance_km = haversine_km(prior.latitude, prior.longitude, current.latitude, current.longitude);\n    let hours = (occurred_at - prior.occurred_at).num_milliseconds() as f64 / 3_600_000.0;\n    if hours &lt; 0.01 {\n        return Ok(TravelResult::Allowed);\n    }\n    let speed_kmh = distance_km / hours;\n\n    if speed_kmh &lt; policy.speed_kmh as f64 {\n        return Ok(TravelResult::Allowed);\n    }\n\n    // Threshold exceeded — apply action\n    match policy.action {\n        ImpossibleTravelAction::Challenge =&gt; {\n            let challenge_id = mfa::issue_challenge(pool, subject_id, \"impossible_travel\").await?;\n            Ok(TravelResult::Challenge { challenge_id, prior, current, speed_kmh })\n        }\n        ImpossibleTravelAction::Block =&gt; Ok(TravelResult::Block { prior, current, speed_kmh }),\n        ImpossibleTravelAction::WarnOnly =&gt; Ok(TravelResult::WarnOnly { speed_kmh }),\n    }\n}\n\nfn haversine_km(lat1: f64, lon1: f64, lat2: f64, lon2: f64) -&gt; f64 {\n    let r = 6371.0;\n    let dlat = (lat2 - lat1).to_radians();\n    let dlon = (lon2 - lon1).to_radians();\n    let a = (dlat / 2.0).sin().powi(2)\n        + lat1.to_radians().cos() * lat2.to_radians().cos() * (dlon / 2.0).sin().powi(2);\n    let c = 2.0 * a.sqrt().asin();\n    r * c\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><code>travel_decision</code> enum exactly 4 values.</li><li><code>impossible_travel_action</code> enum exactly 3 values.</li><li>Login from NYC then login from Tokyo 30min later (with default 900 km/h) triggers <code>challenged</code>.</li><li>Same login pair under <code>block</code> action returns <code>403 impossible_travel_blocked</code> with prior_country + current_country + speed_kmh.</li><li>Same login pair under <code>warn_only</code> action issues session JWT + emits <code>auth.travel_warn_only</code> sev-2 audit.</li><li>NYC → Tokyo within 30min under <code>challenge</code> action returns <code>200 { challenge_required: true, challenge_id }</code>.</li><li>Subsequent <code>POST /v1/auth/mfa/challenge/{id}/verify</code> with valid MFA proof issues session JWT + records <code>allowed_after_challenge</code>.</li><li>Same-country same-ASN within 24h bypasses with sev-3 audit <code>auth.travel_asn_roam_bypass</code> (not explicitly listed but covered by suppression).</li><li>First-login (no prior within 24h) returns <code>Allowed</code> + audit row records <code>allowed</code> with <code>speed_kmh_vs_prior = NULL</code>.</li><li>Allowlisted CIDR bypass emits sev-3 <code>auth.travel_allowlist_bypass</code>.</li><li>Anonymous-IP login emits sev-2 <code>auth.travel_anonymous_ip</code>; default does NOT block; tenant policy <code>block_anonymous_ip = true</code> blocks.</li><li>MaxMind DB &gt; 30 days old at startup emits sev-2 <code>auth.travel_geoip_stale</code>.</li><li>Repeat challenge within 30min from same subject + same IP suppressed; sev-3 <code>auth.travel_challenge_suppressed_repeat</code>.</li><li>Policy mutation requires <code>security_admin</code> role + reason ≥ 10 chars + sev-2 <code>auth.travel_policy_changed</code>.</li><li>Speed threshold &lt; 200 or &gt; 5000 returns <code>400 speed_threshold_out_of_range</code>.</li><li>CIDR list with /0 prefix or /8 IPv4 returns <code>400</code>.</li><li>CIDR list &gt; 64 entries returns <code>400</code>.</li><li>CIDR list with invalid CIDR string returns <code>400</code>.</li><li>Login never blocked outright on impossible-travel under default <code>challenge</code> action.</li><li>User-facing block response includes only country + speed; never lat/lon.</li><li>memory audit chain row includes full geo coordinates + ASN + country for both prior and current.</li><li>Postgres <code>login_history_geo</code> REVOKE UPDATE/DELETE confirmed at <code>\\dp</code>.</li><li>RLS prevents cross-tenant SELECT on <code>login_history_geo</code>.</li><li>Per-subject in-process cache reduces Postgres SELECTs (verified by query log count = 1 for 10 consecutive logins of same subject).</li><li>Cache invalidation on every successful login (write-through) — next login sees fresh prior data.</li><li>IPv4 and IPv6 geolocate via same DB; mixed-stack subjects handled uniformly.</li><li>Unresolvable IP (e.g., 10.0.0.1 private) returns <code>Allowed</code> + sev-3 <code>auth.travel_geoip_unresolvable</code>.</li><li>Dry-run policy update returns affected-login count for last 7 days; no DB write; no audit row.</li><li>CI cardinality tests assert 4 + 3 enum sizes.</li><li>Travel evaluator runs AFTER credential check (instrumentation hook).</li><li>Geo lookup latency p99 &lt; 1ms (in-memory MaxMind mmap).</li><li>NYC → Tokyo within 30min reports speed ≈ 21500 km/h (well above 900 km/h threshold).</li></ol>\n<hr>\n<h2>§5 — Verification (CI tests)</h2>\n<ul><li><code>cardinality_test_decision</code> — 4.</li><li><code>cardinality_test_action</code> — 3.</li><li><code>nyc_to_tokyo_30min_test</code> — challenge under default action.</li><li><code>nyc_to_tokyo_block_test</code> — block action returns 403.</li><li><code>nyc_to_tokyo_warn_test</code> — warn_only issues JWT + sev-2 audit.</li><li><code>challenge_then_verify_test</code> — challenge → MFA verify → JWT issued + <code>allowed_after_challenge</code>.</li><li><code>asn_roam_bypass_test</code> — same country + ASN within 24h, 300km distance → bypass.</li><li><code>first_login_test</code> — no prior login → <code>Allowed</code> + <code>speed_kmh_vs_prior = NULL</code>.</li><li><code>allowlist_bypass_test</code> — office CIDR → bypass + sev-3 audit.</li><li><code>anonymous_ip_test</code> — Tor exit IP → sev-2; default allow; with <code>block_anonymous_ip</code> → block.</li><li><code>geoip_stale_test</code> — mock DB metadata 31 days old → sev-2 startup audit.</li><li><code>challenge_suppression_test</code> — challenge completed at T; new login at T+25min from same IP → suppressed.</li><li><code>challenge_re-arm_test</code> — challenge completed at T; new login at T+31min → re-challenge fires.</li><li><code>policy_mutation_acl_test</code> — non-security_admin → 403.</li><li><code>policy_threshold_range_test</code> — 199 and 5001 → 400.</li><li><code>policy_cidr_validation_test</code> — invalid CIDR strings → 400; /0 + /8 IPv4 → 400.</li><li><code>policy_cidr_count_test</code> — 65-entry list → 400.</li><li><code>block_response_redaction_test</code> — assert response body has no <code>latitude</code>/<code>longitude</code> fields.</li><li><code>chain_audit_full_geo_test</code> — assert memory chain row carries prior + current lat/lon.</li><li><code>rls_isolation_test</code> — two tenants, cross-query empty.</li><li><code>cache_invalidation_test</code> — login → cache hit on next; insert direct → next login sees the inserted row.</li><li><code>mixed_stack_test</code> — IPv4 then IPv6 from same client; same evaluator behavior.</li><li><code>unresolvable_ip_test</code> — 10.0.0.1 → allowed + sev-3.</li><li><code>dry_run_test</code> — policy update with dry_run=true → preview + no DB write.</li><li><code>evaluator_after_credential_test</code> — instrumentation asserts credential check fires before travel::evaluate.</li><li><code>latency_test</code> — 1000 random logins; p99 evaluator latency &lt; 5ms (1ms geo + DB SELECT).</li><li><code>nyc_tokyo_speed_math_test</code> — speed_kmh &gt; 20000.</li></ul>\n<hr>\n<h2>§6 — File skeleton</h2>\n<pre><code>services/auth/\n├── src/\n│   ├── travel/\n│   │   ├── mod.rs          # pub evaluate()\n│   │   ├── geoip.rs        # MaxMind mmap wrapper\n│   │   ├── detector.rs     # Haversine + policy enforcement (§3.3)\n│   │   ├── policy.rs       # per-tenant policy loader\n│   │   ├── asn_bypass.rs   # same-country+ASN check\n│   │   ├── anonymous_ip.rs # VPN/Tor flagging\n│   │   ├── cache.rs        # per-subject LRU\n│   │   ├── audit.rs        # memory audit emission for 10 kinds\n│   │   └── error.rs\n│   └── handlers/\n│       ├── login.rs              # MODIFIED: invoke travel::evaluate after credential check\n│       └── travel_policy.rs      # NEW: PUT /v1/admin/tenants/{id}/travel-policy\n├── migrations/\n│   ├── 0016_login_history_geo.sql\n│   └── 0017_travel_audit.sql\n└── tests/\n    ├── travel_normal_test.rs\n    ├── travel_impossible_test.rs\n    ├── travel_vpn_test.rs\n    ├── travel_allowlist_test.rs\n    └── travel_policy_test.rs\nvendor/maxmind/\n└── GeoLite2-City.mmdb   # refreshed weekly by CI</code></pre>\n<hr>\n<h2>§7 — Dependencies &amp; blast-radius</h2>\n<p><strong>Depends on</strong>: FR-AUTH-002 (subject_create — login flow), FR-AUTH-102 (MFA challenge — adaptive challenge issuer).</p>\n<p><strong>Blocks</strong>: nothing (terminal leaf in P2 auth hardening; complements FR-AUTH-107).</p>\n<p><strong>Blast radius if broken</strong>:</p>\n<ul><li><strong>False positive</strong>: legitimate user challenged unnecessarily; bounded by allowlist + ASN bypass.</li><li><strong>False negative</strong>: impossible-travel signal missed; defense-in-depth via FR-AUTH-102 MFA still active.</li><li><strong>GeoIP DB stale</strong>: gradual quality degradation; sev-2 audit at startup.</li><li><strong>MaxMind unreachable for CI refresh</strong>: weekly refresh fails silently; vendored DB stays as-is until manual intervention.</li></ul>\n<hr>\n<h2>§8 — Payload examples</h2>\n<h3>§8.1 — Normal login (clean travel)</h3>\n<pre><code>POST /v1/auth/login\n{ \"email\": \"...\", \"password\": \"...\" }\n\n200 OK\n{ \"session_token\": \"...\", \"expires_at\": \"...\" }</code></pre>\n<h3>§8.2 — Impossible-travel triggers challenge</h3>\n<pre><code>POST /v1/auth/login\n{ \"email\": \"...\", \"password\": \"...\" }\n\n200 OK\n{\n  \"result\": \"challenge_required\",\n  \"reason\": \"impossible_travel\",\n  \"challenge_id\": \"chl_xyz\",\n  \"methods\": [\"totp\", \"webauthn\"]\n}</code></pre>\n<h3>§8.3 — Block action</h3>\n<pre><code>POST /v1/auth/login\n{ \"email\": \"...\", \"password\": \"...\" }\n\n403 Forbidden\n{\n  \"error\": \"impossible_travel_blocked\",\n  \"prior_country\": \"US\",\n  \"current_country\": \"JP\",\n  \"speed_kmh\": 21450.7,\n  \"threshold_kmh\": 900,\n  \"contact\": \"security@your-tenant.com\"\n}</code></pre>\n<h3>§8.4 — Policy mutation</h3>\n<pre><code>PUT /v1/admin/tenants/{id}/travel-policy\nAuthorization: Bearer &lt;security_admin&gt;\n{\n  \"impossible_travel_speed_kmh\": 500,\n  \"impossible_travel_action\": \"challenge\",\n  \"block_anonymous_ip\": false,\n  \"impossible_travel_ip_allowlist_cidrs\": [\"203.0.113.0/24\"],\n  \"reason\": \"Tightening threshold per audit finding\"\n}\n\n200 OK\n{ \"applied\": true }</code></pre>\n<h3>§8.5 — Dry-run policy preview</h3>\n<pre><code>PUT /v1/admin/tenants/{id}/travel-policy?dry_run=true\n{ \"impossible_travel_speed_kmh\": 500 }\n\n200 OK\n{\n  \"would_apply\": true,\n  \"affected_logins_last_7d\": 12,\n  \"sample\": [\n    { \"subject_id\": \"sub_abc\", \"speed_kmh\": 612.3, \"would_decision\": \"challenged\" }\n  ]\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<ul><li><strong>OQ-1</strong> (closed by DEC-741): 900 km/h threshold.</li><li><strong>OQ-2</strong> (closed by DEC-743): challenge not block by default.</li><li><strong>OQ-3</strong> (closed by DEC-745): local MaxMind DB.</li><li><strong>OQ-4</strong> (open): should we publish the user's \"last 5 login geos\" via subject-facing API for transparency? Currently no (§2.21). Revisit if customer demands.</li><li><strong>OQ-5</strong> (open): should we offer machine-learning-based anomaly detection beyond speed (e.g., device fingerprint, time-of-day, behavioral)? Not in scope; speed is the cheap operationally-understandable metric.</li></ul>\n<hr>\n<h2>§10 — Failure modes (32 rows)</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Sev</th><th>Handler</th></tr></thead><tbody><tr><td>1</td><td>GeoIP DB file missing at startup</td><td>mmap error</td><td>1</td><td>AUTH refuses to start</td></tr><tr><td>2</td><td>GeoIP DB corrupted</td><td>maxminddb parse error</td><td>1</td><td>AUTH refuses to start</td></tr><tr><td>3</td><td>GeoIP DB stale &gt; 30d</td><td>metadata check</td><td>2</td><td>sev-2 startup audit; service starts</td></tr><tr><td>4</td><td>IP geolocation lookup returns no city record</td><td>maxminddb None</td><td>3</td><td>Allowed + sev-3 unresolvable audit</td></tr><tr><td>5</td><td>Private/reserved IP space</td><td>RFC 1918 check</td><td>3</td><td>Allowed + sev-3 unresolvable audit</td></tr><tr><td>6</td><td>Postgres SELECT for prior login fails</td><td>sqlx error</td><td>1</td><td>Treat as first-login + sev-2 audit</td></tr><tr><td>7</td><td>Per-subject cache miss + Postgres slow</td><td>latency p99 alarm</td><td>2</td><td>Bounded by 60s SLA at sev-2</td></tr><tr><td>8</td><td>Policy load fails</td><td>sqlx error</td><td>1</td><td>Default policy used + sev-2 audit</td></tr><tr><td>9</td><td>CIDR allowlist malformed in DB</td><td>parse error at load</td><td>1</td><td>Treat as empty list + sev-2 audit</td></tr><tr><td>10</td><td>MFA challenge issuance fails (FR-AUTH-102 down)</td><td>upstream error</td><td>1</td><td>Login rejected with 503 + sev-1</td></tr><tr><td>11</td><td>Cross-tenant RLS misconfiguration</td><td>rls_isolation_test fails</td><td>1</td><td>CI blocks deploy</td></tr><tr><td>12</td><td>speed_kmh = NaN from same-instant logins</td><td>hours &lt; 0.01 guard</td><td>3</td><td>Allowed (clause #4)</td></tr><tr><td>13</td><td>Anonymous proxy DB missing</td><td>mmdb open error</td><td>2</td><td>Treat all IPs as non-anonymous + sev-2</td></tr><tr><td>14</td><td>Policy mutation by non-admin</td><td>RBAC</td><td>2</td><td>403 + sev-2</td></tr><tr><td>15</td><td>Policy threshold out of [200, 5000]</td><td>CHECK constraint</td><td>3</td><td>400</td></tr><tr><td>16</td><td>Policy action enum invalid</td><td>DB enum</td><td>3</td><td>400</td></tr><tr><td>17</td><td>CIDR list &gt; 64 entries</td><td>length validator</td><td>3</td><td>400</td></tr><tr><td>18</td><td>CIDR /0 or /8 IPv4</td><td>prefix validator</td><td>3</td><td>400</td></tr><tr><td>19</td><td>CIDR invalid string</td><td>ipnetwork parse</td><td>3</td><td>400</td></tr><tr><td>20</td><td>Reason for mutation &lt; 10 chars</td><td>CHECK</td><td>3</td><td>400</td></tr><tr><td>21</td><td>login_history_geo INSERT permission denied</td><td>GRANT misconfig</td><td>1</td><td>Login rejected; sev-1 audit</td></tr><tr><td>22</td><td>memory_chain_hash regex fails</td><td>CHECK constraint</td><td>1</td><td>Login rejected; sev-1 audit</td></tr><tr><td>23</td><td>Cache invalidation race (two concurrent logins)</td><td>LRU put serialization</td><td>3</td><td>Later-write-wins; bounded</td></tr><tr><td>24</td><td>Travel evaluator invoked before credential check</td><td>ordering test</td><td>2</td><td>CI blocks</td></tr><tr><td>25</td><td>Block response includes lat/lon</td><td>redaction test</td><td>1</td><td>CI blocks</td></tr><tr><td>26</td><td>Chain audit row missing geo fields</td><td>schema validation</td><td>1</td><td>CI blocks</td></tr><tr><td>27</td><td>Stale MaxMind DB in nightly CI</td><td>weekly refresh job</td><td>2</td><td>Sev-2 ops alarm</td></tr><tr><td>28</td><td>Repeat-challenge suppression bug (always suppresses)</td><td>challenge_re-arm_test</td><td>2</td><td>CI blocks</td></tr><tr><td>29</td><td>Dry-run mutates state</td><td>dry_run_test</td><td>2</td><td>CI blocks</td></tr><tr><td>30</td><td>Same IP both v4 and v6 yields different geo</td><td>mixed_stack_test</td><td>3</td><td>Use whichever arrived; sev-3 if speed-flag</td></tr><tr><td>31</td><td>Subject deleted between cache write and read</td><td>foreign key</td><td>3</td><td>Bounded by FR-AUTH-105 deletion ordering</td></tr><tr><td>32</td><td>Tenant terminated (FR-TEN-104) — old login geos still query</td><td>RLS + tenant state</td><td>3</td><td>Read-only after termination; allowed</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> The MaxMind GeoLite2-City.mmdb is committed as a release artifact, not as source code. Weekly CI refreshes it via <code>wget https://download.maxmind.com/...</code> (license key in CI secret).</p>\n<p><strong>§11.2</strong> The anonymous-IP DB is a separate <code>.mmdb</code> file (<code>GeoIP2-Anonymous-IP.mmdb</code>); both are mmap'd at startup.</p>\n<p><strong>§11.3</strong> Haversine constants: Earth radius = 6371 km. The formula uses <code>sin/cos/asin</code> which are fast enough that we don't memoize.</p>\n<p><strong>§11.4</strong> The per-subject cache is keyed on subject_id only (not tenant_id); the prior_login query is already tenant-scoped via RLS.</p>\n<p><strong>§11.5</strong> Cache invalidation is write-through: every successful login writes to both the in-process cache and Postgres in one async block. The Postgres write happens first (durable); the cache write happens after (on success).</p>\n<p><strong>§11.6</strong> The challenge_id returned in the <code>challenge_required</code> response is the same challenge type as FR-AUTH-102 user-initiated MFA — the verify endpoint is shared.</p>\n<p><strong>§11.7</strong> The MaxMind staleness check at startup runs once at process boot. Subsequent runtime staleness (e.g., a 60-day-running process) is detected on the next restart.</p>\n<p><strong>§11.8</strong> The 30-min challenge suppression is implemented via a small in-process map (subject_id + ip → last_challenge_at, ttl 30min). Cross-process visibility isn't needed because each AUTH process handles independent login streams.</p>\n<p><strong>§11.9</strong> The <code>block_anonymous_ip</code> policy is a simple boolean; we don't allow per-VPN-vendor whitelisting (that complicates the model and the tenant probably doesn't know their VPN vendor's IP ranges anyway — they'd use the CIDR allowlist).</p>\n<p><strong>§11.10</strong> ASN-and-country bypass is a single SQL row comparison (no aggregation). Latency is negligible.</p>\n<p><strong>§11.11</strong> The <code>is_anonymous_proxy</code> field combines four MaxMind categories (anonymous_vpn, public_proxy, tor_exit_node, hosting_provider). For our purposes any of these triggers the same audit kind; finer-grained handling is out of scope.</p>\n<p><strong>§11.12</strong> Tests use a fixture MaxMind DB with known IPs for repeatable assertions (no live MaxMind calls in CI).</p>\n<p><strong>§11.13</strong> The dry_run handler runs the same evaluator over the last 7 days of <code>login_history_geo</code> rows for the tenant. For tenants with &gt; 1M logins/week, the query is paginated; the sample in the response is the first 20 hits.</p>\n<p><strong>§11.14</strong> The <code>evaluator_after_credential_test</code> uses a code-instrumented hook (or a tracing span) to verify the ordering. If the test fails, the login handler was refactored incorrectly.</p>\n<p><strong>§11.15</strong> The <code>cache_invalidation_test</code> directly INSERTs a prior login row, then performs a login from a different geo, and asserts the evaluator sees the inserted row. This catches stale-cache bugs.</p>\n<p><strong>§11.16</strong> The <code>unresolvable_ip_test</code> covers RFC 1918 (10.0.0.0/8), 127.0.0.0/8, fe80::/10 link-local — common in development environments. The audit row makes the unresolvable case visible without breaking dev.</p>\n<p><strong>§11.17</strong> Tracing spans wrap each <code>travel::evaluate</code> call with the subject_id + decision. The span shows up in FR-OBS-001 OTEL traces, enabling forensic walks across login + travel + MFA spans.</p>\n<p><strong>§11.18</strong> The login handler emits the <code>auth.login_attempted</code> audit row before <code>travel::evaluate</code> and the <code>auth.travel_*</code> audit row after — both link to the same <code>trace_id</code>.</p>\n<p><strong>§11.19</strong> The CIDR allowlist is checked with the <code>ipnetwork::IpNetwork::contains(ip)</code> method; we don't roll our own bit manipulation. The list is loaded once per request from <code>tenants</code> (which is itself cached for 60s).</p>\n<p><strong>§11.20</strong> The MaxMind reader is <code>maxminddb::Reader&lt;Vec&lt;u8&gt;&gt;</code> (owned buffer) for thread-safety. The buffer is loaded once at startup and shared via <code>Arc</code>.</p>\n<p><strong>§11.21</strong> First-login of a new subject is identified by the absence of prior rows in the 24h window. The <code>FirstLogin</code> audit kind logs sev-3 — operationally interesting but expected.</p>\n<p><strong>§11.22</strong> Per-tenant policy cache (60s TTL) is invalidated via pg_notify on policy mutation, mirroring the FR-AUTH-107 pattern.</p>\n<hr>\n<p><em>End of FR-AUTH-106 spec.</em></p>";
