window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LEARN-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The LEARN service <strong>MUST</strong> ship VP rollup at <code>services/learn/src/vp/</code> aggregating PROJ + TIME + KB nightly, immutable per-week snapshots, versioned weights, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>vp_component</code> against closed enum per DEC-2101.</li></ol>\n<ol><li><strong>MUST</strong> aggregate at <code>aggregator.rs::aggregate(member, week, weights)</code>:</li></ol>\n<ul><li>proj_contribution = sum(issue.complexity × weight_proj) closed in week</li><li>time_billable = sum(billable_hours × weight_time) for week</li><li>kb_authorship = sum(doc.view_count × weight_kb) for docs authored before week</li><li>total_vp = sum of weighted components</li></ul>\n<ol><li><strong>MUST</strong> be deterministic per DEC-2103 — pure function; same data + same weights → same output.</li></ol>\n<ol><li><strong>MUST</strong> schedule nightly batch via FR-MCP-007 at 03:30 tenant_tz.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0003</code>: ```sql CREATE TABLE learn_vp_weights ( tenant_id UUID NOT NULL, version INT NOT NULL, weight_proj NUMERIC(5,4) NOT NULL, weight_time NUMERIC(5,4) NOT NULL, weight_kb NUMERIC(5,4) NOT NULL, effective_from DATE NOT NULL, set_by UUID NOT NULL, PRIMARY KEY (tenant_id, version) ); ALTER TABLE learn_vp_weights ENABLE ROW LEVEL SECURITY; CREATE POLICY weights_rls ON learn_vp_weights USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON learn_vp_weights FROM cyberos_app;</li></ol>\n<p>   CREATE TABLE learn_vp_snapshots (      snapshot_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      member_id UUID NOT NULL,      iso_week CHAR(8) NOT NULL,      proj_score NUMERIC(10,4) NOT NULL,      time_score NUMERIC(10,4) NOT NULL,      kb_score NUMERIC(10,4) NOT NULL,      total_vp NUMERIC(10,4) NOT NULL,      weights_version INT NOT NULL,      correction_of UUID REFERENCES learn_vp_snapshots(snapshot_id),      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now(),      UNIQUE (tenant_id, member_id, iso_week, weights_version)    );    CREATE INDEX vp_snap_member_idx ON learn_vp_snapshots(tenant_id, member_id, iso_week DESC);    ALTER TABLE learn_vp_snapshots ENABLE ROW LEVEL SECURITY;    CREATE POLICY vp_snap_rls ON learn_vp_snapshots      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON learn_vp_snapshots FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/learn/vp/weights         (CEO; new version) GET  /v1/learn/vp/weights         (latest) GET  /v1/learn/members/{id}/vp    (history) POST /v1/learn/vp/rollup/trigger  (CEO manual) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2104. PII per FR-MEMORY-111: scores SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from cron → aggregator → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior snapshot per DEC-2102 (correction = new row).</li></ol>\n<ol><li><strong>MUST NOT</strong> use non-deterministic inputs (no now(), no random).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why deterministic (DEC-2103)?</strong> VP drives governance + REW BP fund distribution; replay must yield same result for audit.</p>\n<p><strong>Why versioned weights (DEC-2103)?</strong> Weights change over time; snapshots must record which version applied.</p>\n<p><strong>Why immutable snapshots (DEC-2102)?</strong> Historical VP is institutional record; rewriting = governance failure.</p>\n<p><strong>Why 3 components (DEC-2101)?</strong> Captures core contribution dimensions; closed enum prevents add-hoc scope creep.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample VP snapshot:</p>\n<pre><code class=\"language-json\">{\n  \"member_id\": \"uuid\",\n  \"iso_week\": \"2026-W20\",\n  \"proj_score\": 45.5,\n  \"time_score\": 32.0,\n  \"kb_score\": 8.5,\n  \"total_vp\": 86.0,\n  \"weights_version\": 3\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>vp_component enum cardinality 3</strong>. 2. <strong>Deterministic (same input → same output)</strong>. 3. <strong>Snapshots immutable</strong>. 4. <strong>Weights versioned</strong>. 5. <strong>Nightly cron 03:30</strong>. 6. <strong>UNIQUE(tenant, member, week, weights_version) idempotency</strong>. 7. <strong>4 memory audit kinds emitted</strong>. 8. <strong>PII scrubbed (scores SHA256)</strong>. 9. <strong>RLS denies cross-tenant</strong>. 10. <strong>CEO-only weights + manual trigger</strong>. 11. <strong>Trace_id preserved</strong>. 12. <strong>rust_decimal precision (10,4)</strong>. 13. <strong>Correction via new snapshot with correction_of link</strong>. 14. <strong>Append-only via REVOKE</strong>. 15. <strong>Inactive member skipped</strong>. 16. <strong>0 active members skipped</strong>. 17. <strong>Weights version pinning enforced</strong>. 18. <strong>Backfill via manual trigger with iso_week</strong>. 19. <strong>History query desc time</strong>. 20. <strong>Per-component score visible</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn deterministic_replay() {\n    let ctx = TestContext::with_member_data().await;\n    let v1 = ctx.run_rollup(this_week()).await;\n    ctx.run_rollup(this_week()).await;\n    let v2 = ctx.fetch_snapshot(ctx.member_id, this_week()).await;\n    assert_eq!(v1.total_vp, v2.total_vp);\n}\n\n#[tokio::test]\nasync fn snapshot_immutable() {\n    let ctx = TestContext::with_vp_snapshot().await;\n    let r = ctx.try_mutate_snapshot(ctx.snapshot_id).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn correction_via_new_row() {\n    let ctx = TestContext::with_vp_snapshot().await;\n    ctx.run_correction_with_new_weights(ctx.snapshot_id).await;\n    let snaps = ctx.fetch_snapshots_for_week(ctx.iso_week).await;\n    assert_eq!(snaps.len(), 2);\n    let corrected = snaps.iter().find(|s| s.correction_of.is_some()).unwrap();\n    assert_eq!(corrected.correction_of, Some(ctx.snapshot_id));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-PROJ-013, FR-TIME-001. <strong>Cross-module:</strong> FR-LEARN-001 (member context), FR-KB-001 (doc authorship), FR-MCP-007 (cron), FR-AUTH-101 (CEO), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Source module fail</td><td>catch</td><td>sev-2; skip component</td><td>retry</td></tr><tr><td>Weights missing</td><td>catch</td><td>sev-1; halt</td><td>seed weights</td></tr><tr><td>Cron skipped</td><td>catch-up</td><td>inherent</td><td>inherent</td></tr><tr><td>Duplicate snapshot</td><td>UNIQUE</td><td>skip</td><td>inherent</td></tr><tr><td>Decimal precision drift</td><td>rust_decimal</td><td>inherent</td><td>inherent</td></tr><tr><td>Non-deterministic input</td><td>code review</td><td>inherent</td><td>bug fix</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Mid-rollup crash</td><td>resume</td><td>partial</td><td>retry</td></tr><tr><td>Weights mid-week change</td><td>use snapshot's weights_version</td><td>inherent</td><td>inherent</td></tr><tr><td>Inactive member</td><td>skip</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Cron via FR-MCP-007 <code>kind: 'learn.vp_rollup'</code>, daily 03:30.</li><li>§11.2 Aggregator pure: <code>(member_data, weights) → VpScores</code>.</li><li>§11.3 Snapshots stored per (member, week, weights_version) — weights change creates new snapshot.</li><li>§11.4 memory audit body: member_id, week, weights_version; scores SHA256.</li><li>§11.5 Manual trigger backfills via iso_week parameter; uses weights effective on week's Monday.</li></ul>\n<hr>\n<p><em>End of FR-LEARN-003 spec.</em></p>";
