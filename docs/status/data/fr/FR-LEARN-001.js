window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LEARN-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The LEARN service <strong>MUST</strong> ship skill tree at <code>services/learn/src/skill_tree/</code> with hierarchical skill graph + per-member mastery + append-only audit, 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>skill_domain</code> against closed enum per DEC-2081, <code>mastery_level</code> per DEC-2082.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0001</code>: ```sql CREATE TABLE learn_skills ( skill_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, name TEXT NOT NULL, description TEXT, domain TEXT NOT NULL CHECK (domain IN ('engineering','design','product','sales','finance','ops','legal','general')), parent_skill_id UUID REFERENCES learn_skills(skill_id), depth INT NOT NULL DEFAULT 0 CHECK (depth &gt;= 0 AND depth &lt;= 4), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (tenant_id, name, parent_skill_id) ); CREATE INDEX skills_parent_idx ON learn_skills(tenant_id, parent_skill_id) WHERE parent_skill_id IS NOT NULL; ALTER TABLE learn_skills ENABLE ROW LEVEL SECURITY; CREATE POLICY skills_rls ON learn_skills USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); GRANT UPDATE (name, description, parent_skill_id, depth) ON learn_skills TO cyberos_app;</li></ol>\n<p>   CREATE TABLE learn_member_mastery (      mastery_row_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      member_id UUID NOT NULL,      skill_id UUID NOT NULL REFERENCES learn_skills(skill_id),      mastery_level INT NOT NULL CHECK (mastery_level &gt;= 1 AND mastery_level &lt;= 5),      assessed_by UUID NOT NULL,      assessment_kind TEXT NOT NULL,  -- 'self' | 'peer' | 'council' | 'system'      valid_from DATE NOT NULL,      valid_to DATE,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    CREATE INDEX mastery_member_skill_idx ON learn_member_mastery(tenant_id, member_id, skill_id, valid_from DESC);    ALTER TABLE learn_member_mastery ENABLE ROW LEVEL SECURITY;    CREATE POLICY mastery_rls ON learn_member_mastery      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON learn_member_mastery FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> enforce nesting depth ≤4 per DEC-2080 at <code>validator.rs::validate(parent_skill_id)</code>.</li></ol>\n<ol><li><strong>MUST</strong> be append-only per DEC-2083 — corrections via new row with new valid_from; prior row's valid_to set.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/learn/skills                 (CHRO) POST /v1/learn/members/{id}/mastery   body: {skill_id, mastery_level, assessment_kind} GET  /v1/learn/members/{id}/mastery   (current per-skill) GET  /v1/learn/skills/tree            (hierarchical view) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-2084. PII per FR-MEMORY-111: skill names + descriptions text SHA-256 hashed; member_id + level ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from set → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior mastery row per DEC-2083.</li></ol>\n<ol><li><strong>MUST NOT</strong> create cycles in parent_skill_id (validator check).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 4-level depth (DEC-2080)?</strong> Bounded to prevent infinite trees; covers domain → discipline → skill → subskill.</p>\n<p><strong>Why 8 domains (DEC-2081)?</strong> Top-level taxonomy covers CyberSkill business; closed enum prevents sprawl.</p>\n<p><strong>Why 1-5 mastery (DEC-2082)?</strong> Industry-standard scale (Bloom + similar).</p>\n<p><strong>Why append-only (DEC-2083)?</strong> Audit lineage — promotion decisions reference mastery history.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample skill tree:</p>\n<pre><code class=\"language-json\">[\n  {\"skill_id\": \"uuid\", \"name\": \"Rust\", \"domain\": \"engineering\", \"depth\": 0,\n   \"children\": [{\"skill_id\": \"uuid\", \"name\": \"Async Rust\", \"depth\": 1}]}\n]</code></pre>\n<p>Sample mastery set:</p>\n<pre><code class=\"language-json\">POST /v1/learn/members/{id}/mastery\n{\n  \"skill_id\": \"uuid\",\n  \"mastery_level\": 3,\n  \"assessment_kind\": \"council\",\n  \"valid_from\": \"2026-06-01\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>skill_domain enum cardinality 8</strong>. 2. <strong>mastery_level CHECK 1-5</strong>. 3. <strong>parent depth ≤4</strong>. 4. <strong>Cycle prevention</strong>. 5. <strong>Append-only mastery</strong>. 6. <strong>UNIQUE(tenant, name, parent) on skills</strong>. 7. <strong>3 memory audit kinds emitted</strong>. 8. <strong>PII scrubbed (skill text SHA256)</strong>. 9. <strong>RLS denies cross-tenant</strong>. 10. <strong>CHRO-only skill create</strong>. 11. <strong>assessment_kind tagged (self/peer/council/system)</strong>. 12. <strong>valid_from + valid_to range</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Tree query recursive CTE</strong>. 15. <strong>Current mastery = max valid_from</strong>. 16. <strong>Self-reference parent rejected</strong>. 17. <strong>Skill rename via UPDATE OK</strong>. 18. <strong>Parent change via UPDATE OK (depth recomputed)</strong>. 19. <strong>Cross-tenant parent FK rejected</strong>. 20. <strong>Mastery FK to skill enforced</strong>. </li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn mastery_level_1_to_5_enforced() {\n    for level in 1..=5 {\n        let r = ctx.set_mastery(ctx.member_id, ctx.skill_id, level).await;\n        assert!(r.is_ok());\n    }\n    for bad in [0, 6, 100] {\n        let r = ctx.set_mastery(ctx.member_id, ctx.skill_id, bad).await;\n        assert!(r.is_err());\n    }\n}\n\n#[tokio::test]\nasync fn parent_depth_5_rejected() {\n    let chain = ctx.build_skill_chain(5).await;  // chain of 5 = depth 4\n    let r = ctx.add_skill_under(chain.last(), \"depth5\").await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn append_only_no_update() {\n    let ctx = TestContext::with_mastery_row().await;\n    let r = ctx.try_update_mastery_row(ctx.mastery_row_id).await;\n    assert!(r.is_err());\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-HR-001. <strong>Downstream:</strong> FR-LEARN-002 (degrees+certs), FR-LEARN-003 (VP rollup), FR-LEARN-004 (Council). <strong>Cross-module:</strong> FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Domain not in enum</td><td>CHECK</td><td>400</td><td>use valid</td></tr><tr><td>Depth &gt; 4</td><td>validator</td><td>reject</td><td>restructure tree</td></tr><tr><td>Cycle</td><td>validator</td><td>reject</td><td>inherent</td></tr><tr><td>Mastery out of range</td><td>CHECK</td><td>400</td><td>use 1-5</td></tr><tr><td>Duplicate skill name</td><td>UNIQUE</td><td>409</td><td>rename</td></tr><tr><td>Mastery on deleted skill</td><td>FK</td><td>404</td><td>reactivate skill</td></tr><tr><td>Cross-tenant parent</td><td>FK + RLS</td><td>404</td><td>inherent</td></tr><tr><td>Decimal precision</td><td>not applicable</td><td>inherent</td><td>inherent</td></tr><tr><td>Mastery row append race</td><td>inherent</td><td>both append</td><td>inherent</td></tr><tr><td>Self-ref parent</td><td>validator</td><td>reject</td><td>use different parent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Validator recursive depth check: walk parent chain; reject if depth &gt; 4 or cycle.</li><li>§11.2 Current mastery query: <code>SELECT * FROM mastery WHERE member=$1 AND skill=$2 AND valid_from &lt;= today AND (valid_to IS NULL OR valid_to &gt; today) ORDER BY valid_from DESC LIMIT 1</code>.</li><li>§11.3 memory audit body: member_id, skill_id, mastery_level, assessment_kind; skill text SHA256.</li><li>§11.4 Tree view via recursive CTE.</li><li>§11.5 Assessment_kind drives FR-LEARN-004 council vs self vs peer flows.</li></ul>\n<hr>\n<p><em>End of FR-LEARN-001 spec.</em></p>";
