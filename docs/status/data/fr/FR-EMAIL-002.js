window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EMAIL-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The EMAIL service <strong>MUST</strong> ship Stalwart authbridge plugin at <code>services/email/src/authbridge/</code> validating FR-AUTH-004 JWTs against per-tenant mailbox scope, with 60s Redis cache, FR-PORTAL-004 SCIM revoke cascade, 6-outcome enum, and 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>auth_outcome</code> enum: <code>('success','jwt_invalid','jwt_expired','jwt_wrong_audience','subject_revoked','mailbox_unauthorized')</code> per DEC-1463. Cardinality 6.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/email/auth</code> (Stalwart HTTP auth backend) body <code>{ username, password, protocol }</code>. Handler:</li></ol>\n<ul><li>Treats <code>password</code> field as JWT per DEC-1461.</li><li>Validates JWT against FR-AUTH-004 issuer + JWKS.</li><li>Validates <code>username</code> matches JWT's subject_id @ tenant.</li><li>Checks Redis cache; hit → return cached outcome.</li><li>Else verify + cache + return.</li><li>Returns 200 + <code>{ outcome, mailbox_path, permissions }</code> for Stalwart enforcement.</li></ul>\n<ol><li><strong>MUST</strong> resolve mailbox path per DEC-1462: <code>subject_id@tenant_slug.cyberos.world</code>. Per-Engagement aliases possible via FR-PORTAL-002 CNAME mapping.</li></ol>\n<ol><li><strong>MUST</strong> cache validated JWTs 60s per DEC-1464. Redis key = <code>email_auth:{jti}</code>; value = <code>{ outcome, mailbox_path, permissions, exp }</code>.</li></ol>\n<ol><li><strong>MUST</strong> subscribe to FR-PORTAL-004 SCIM revoke NATS events per DEC-1465. Consumer invalidates Redis cache for subject + emits <code>email.auth_failed</code> reason='subject_revoked' for next attempt.</li></ol>\n<ol><li><strong>MUST</strong> define <code>email_auth_log</code> at migration <code>0001</code>: <code>(id BIGSERIAL, tenant_id UUID, subject_id UUID, protocol TEXT, outcome auth_outcome, source_ip_hash16 TEXT, ts TIMESTAMPTZ DEFAULT now())</code>. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1466. PII-scrub source_ip via FR-MEMORY-111.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> cache JWT &gt; 60s per DEC-1464.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow cross-tenant mailbox per DEC-1462.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why JWT-as-password (DEC-1461)?</strong> IMAP/SMTP clients expect username/password; JWT in password field = client-transparent SSO without new protocols. Industry pattern (Google App Passwords replaced by OAuth, but base auth still username/password mechanism).</p>\n<p><strong>Why 60s cache (DEC-1464)?</strong> IMAP IDLE keeps connection open + re-auths frequently. Per-request JWT verify = ~5ms × 100 req/s = 500ms/s CPU; 60s cache = ~99% hit rate.</p>\n<p><strong>Why SCIM cascade (DEC-1465)?</strong> Without it, JWT exp window (8h IdP-auth) is the only revocation timeline. Cascade brings it to &lt;60s.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0001_email_auth_log.sql\nCREATE TYPE auth_outcome AS ENUM ('success','jwt_invalid','jwt_expired','jwt_wrong_audience','subject_revoked','mailbox_unauthorized');\n\nCREATE TABLE email_auth_log (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID,\n  subject_id UUID,\n  protocol TEXT NOT NULL CHECK (protocol IN ('imap','smtp','jmap','managesieve')),\n  outcome auth_outcome NOT NULL,\n  source_ip_hash16 TEXT,\n  ts TIMESTAMPTZ NOT NULL DEFAULT now(),\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_auth_log_subject ON email_auth_log(subject_id, ts DESC);\nALTER TABLE email_auth_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY email_auth_log_rls ON email_auth_log\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON email_auth_log FROM cyberos_app;</code></pre>\n<p>Stalwart backend HTTP contract: <code>POST /v1/email/auth</code> returns <code>{ outcome, mailbox_path, permissions: [...] }</code>.</p>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>auth_outcome cardinality 6</strong>.</li><li><strong>Valid JWT</strong> → success + mailbox_path returned.</li><li><strong>Expired JWT</strong> → jwt_expired + no cache.</li><li><strong>Wrong audience</strong> → jwt_wrong_audience.</li><li><strong>Username mismatch</strong> → mailbox_unauthorized.</li><li><strong>60s cache hit</strong> — second request within 60s returns cached.</li><li><strong>Cache TTL respected</strong> — at T+61s, re-validates.</li><li><strong>SCIM revoke invalidates cache</strong> — NATS event → next request 'subject_revoked'.</li><li><strong>4 memory audit kinds emitted</strong>.</li><li><strong>Per-tenant mailbox path correct</strong> — <code>subj@tenant.cyberos.world</code> format.</li><li><strong>Cross-tenant attempt denied</strong> — subject from tenant A trying tenant B mailbox → mailbox_unauthorized.</li><li><strong>Source IP PII-scrubbed</strong>.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>Stalwart integration smoke</strong> — real IMAP login via Stalwart succeeds.</li><li><strong>Redis unavailable fallback</strong> — falls through to per-request JWT verify.</li><li><strong>JWKS rotation handled</strong> — FR-AUTH-004 key rotation propagates.</li><li><strong>Protocol enum validated</strong> — unknown protocol → 400.</li><li><strong>Non-JWT password rejected</strong> — random string → jwt_invalid.</li><li><strong>RLS denies cross-tenant log read</strong>.</li><li><strong>Audit on every outcome</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn valid_jwt_returns_success() {\n    let ctx = TestContext::new().await;\n    let jwt = ctx.mint_jwt(ctx.subject_id, ctx.tenant_id).await;\n    let r = ctx.post_auth(\"alice@acme.cyberos.world\", &amp;jwt, \"imap\").await;\n    assert_eq!(r.status(), 200);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"outcome\"], \"success\");\n    assert_eq!(body[\"mailbox_path\"], \"alice@acme.cyberos.world\");\n}\n\n#[tokio::test]\nasync fn cache_hit_within_60s() {\n    let ctx = TestContext::new().await;\n    let jwt = ctx.mint_jwt(ctx.subject_id, ctx.tenant_id).await;\n    ctx.post_auth(\"alice@acme.cyberos.world\", &amp;jwt, \"imap\").await;\n    let metrics_before = ctx.jwt_verify_count();\n    ctx.post_auth(\"alice@acme.cyberos.world\", &amp;jwt, \"imap\").await;\n    let metrics_after = ctx.jwt_verify_count();\n    assert_eq!(metrics_after, metrics_before);  // cached, no re-verify\n}\n\n#[tokio::test]\nasync fn scim_revoke_invalidates() {\n    let ctx = TestContext::new().await;\n    let jwt = ctx.mint_jwt(ctx.subject_id, ctx.tenant_id).await;\n    ctx.post_auth(\"alice@acme.cyberos.world\", &amp;jwt, \"imap\").await;\n    ctx.publish_scim_revoke_event(ctx.subject_id).await;\n    tokio::time::sleep(Duration::from_millis(200)).await;\n    let r = ctx.post_auth(\"alice@acme.cyberos.world\", &amp;jwt, \"imap\").await;\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"outcome\"], \"subject_revoked\");\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-EMAIL-001, FR-AUTH-004. <strong>Cross-module:</strong> FR-PORTAL-004 (SCIM cascade), FR-AI-003, FR-MEMORY-111.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>JWKS unreachable</td><td>timeout 5s</td><td>Sev-2; serve from cached JWKS</td><td>AUTH recovery</td></tr><tr><td>Redis unavailable</td><td>error</td><td>Fallback per-request verify; sev-3</td><td>Redis recovery</td></tr><tr><td>JWT clock skew</td><td>exp check tolerance ±60s</td><td>Inherent</td><td>NTP</td></tr><tr><td>Stalwart auth timeout</td><td>10s default</td><td>Sev-2</td><td>Stalwart investigation</td></tr><tr><td>Cross-tenant attempt</td><td>username check</td><td>mailbox_unauthorized</td><td>Inherent</td></tr><tr><td>SCIM cascade delay</td><td>NATS lag</td><td>Sev-3; cache TTL covers worst case</td><td>NATS recovery</td></tr><tr><td>Same JWT replay &gt; 60s past exp</td><td>exp check</td><td>jwt_expired</td><td>Inherent</td></tr><tr><td>Cache poisoning</td><td>Redis ACL</td><td>Inherent</td><td>Redis isolation</td></tr><tr><td>Protocol enum unknown</td><td>check</td><td>400</td><td>Stalwart config fix</td></tr><tr><td>Per-request rate spike</td><td>rate limit</td><td>429</td><td>Inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Stalwart <code>auth.backend.type = http</code> configured to point at <code>/v1/email/auth</code>. <strong>§11.2</strong> JWT-as-password tested with real IMAP clients (Apple Mail, Thunderbird). <strong>§11.3</strong> Per-tenant mailbox isolation enforced by Stalwart given correct mailbox_path return. <strong>§11.4</strong> Redis cache pruned at TTL automatically. <strong>§11.5</strong> Source IP hashed before persist; never raw IP in audit chain.</p>\n<hr>\n<p><em>End of FR-EMAIL-002 spec.</em></p>";
