window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CRM-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CRM service <strong>MUST</strong> ship the Account / Contact / Deal Postgres schema with custom pipelines and stages as the foundational sales-pipeline primitive. Each requirement:</p>\n<ol><li><strong>MUST</strong> define the <code>accounts</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>legal_name TEXT NOT NULL CHECK (length(legal_name) BETWEEN 1 AND 200)</code>, <code>display_name TEXT</code>, <code>account_type account_type NOT NULL DEFAULT 'unknown'</code> (full enum ships in TASK-CRM-003 per DEC-343), <code>mst TEXT</code> (VN tax code; validation ships in TASK-CRM-003 + TASK-CRM-008), <code>website TEXT</code>, <code>industry TEXT</code>, <code>headquarters_country TEXT</code>, <code>owner_subject_id UUID NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT</code>, <code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>updated_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>created_by_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>contacts</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>full_name TEXT NOT NULL CHECK (length(full_name) BETWEEN 1 AND 200)</code>, <code>display_name TEXT</code>, <code>email TEXT</code> (nullable; PII), <code>phone TEXT</code> (nullable; PII), <code>title TEXT</code>, <code>language_code language_code</code> (vi/en per TASK-KB-001's enum reuse), <code>owner_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>, <code>created_at TIMESTAMPTZ</code>, <code>updated_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID</code>. Email + phone uniqueness is NOT enforced (same person may appear with the same email twice in different accounts).</li></ol>\n<ol><li><strong>MUST</strong> define the <code>contact_account_membership</code> JOIN table (per DEC-348) with: <code>contact_id UUID NOT NULL REFERENCES contacts(id) ON DELETE RESTRICT</code>, <code>account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT</code>, <code>tenant_id UUID NOT NULL</code>, <code>role TEXT NOT NULL DEFAULT 'general'</code> (closed enum-ish — common values: <code>decision_maker</code>, <code>technical</code>, <code>billing</code>, <code>legal</code>, <code>general</code>), <code>is_primary BOOLEAN NOT NULL DEFAULT false</code>, <code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>PRIMARY KEY (contact_id, account_id)</code>. A contact MUST have at least one membership (enforced by handler; not by DB constraint to allow transactional bootstrap).</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>pipeline_shape</code> Postgres enum with exactly 4 values (per DEC-341): <code>'sales'</code>, <code>'partner'</code>, <code>'inbound'</code>, <code>'outbound'</code>. Adding a 5th shape is an ADR. The shape gates the TASK-CRM-005 next-action skill's applicability.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>pipelines</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 100)</code>, <code>shape pipeline_shape NOT NULL</code>, <code>is_default BOOLEAN NOT NULL DEFAULT false</code>, <code>created_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID</code>. UNIQUE <code>(tenant_id, name)</code>; at most one pipeline per tenant per shape may be <code>is_default = true</code> (partial unique index).</li></ol>\n<ol><li><strong>MUST</strong> define the <code>pipeline_stages</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>pipeline_id UUID NOT NULL REFERENCES pipelines(id) ON DELETE RESTRICT</code>, <code>name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 50)</code>, <code>position INT NOT NULL CHECK (position BETWEEN 1 AND 50)</code>, <code>probability_default INT NOT NULL CHECK (probability_default BETWEEN 0 AND 100) DEFAULT 50</code> (per DEC-351), <code>is_open BOOLEAN NOT NULL DEFAULT true</code>, <code>is_won BOOLEAN NOT NULL DEFAULT false</code>, <code>is_lost BOOLEAN NOT NULL DEFAULT false</code>, <code>created_at TIMESTAMPTZ</code>. Stage tags <code>is_open/is_won/is_lost</code> are mutually exclusive (DB CHECK: exactly one true). UNIQUE <code>(pipeline_id, position)</code> and UNIQUE <code>(pipeline_id, name)</code>.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>deals</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 200)</code>, <code>account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT</code>, <code>primary_contact_id UUID REFERENCES contacts(id)</code> (nullable; one specific contact this deal is anchored to), <code>pipeline_id UUID NOT NULL REFERENCES pipelines(id)</code>, <code>current_stage_id UUID NOT NULL REFERENCES pipeline_stages(id)</code>, <code>status deal_status NOT NULL DEFAULT 'open'</code> (per DEC-344), <code>probability INT NOT NULL CHECK (probability BETWEEN 0 AND 100)</code>, <code>amount_minor BIGINT NOT NULL CHECK (amount_minor &gt;= 0)</code> (per task-audit skill rule 11 + DEC-345), <code>amount_currency CHAR(3) NOT NULL CHECK (amount_currency ~ '^[A-Z]{3}$')</code>, <code>expected_close_date DATE NOT NULL</code>, <code>actual_close_date DATE</code>, <code>owner_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>, <code>created_at TIMESTAMPTZ</code>, <code>updated_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID NOT NULL</code>, <code>won_at TIMESTAMPTZ</code>, <code>lost_reason TEXT</code> (nullable; required when status=lost), <code>cancelled_reason TEXT</code> (nullable; required when status=cancelled).</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>deal_status</code> Postgres enum with exactly 4 values (per DEC-344): <code>'open'</code>, <code>'won'</code>, <code>'lost'</code>, <code>'cancelled'</code>. Adding a 5th status is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> ship the deal-status FSM transition validator at <code>services/crm/src/fsm/deal_status.rs</code> as a closed lookup table (per DEC-347). Valid transitions:</li></ol>\n<ul><li><code>open → won</code> (requires current_stage to have <code>is_won = true</code>).</li><li><code>open → lost</code> (requires current_stage to have <code>is_lost = true</code> AND <code>lost_reason</code> non-empty).</li><li><code>open → cancelled</code> (requires <code>cancelled_reason</code> non-empty).</li><li><code>won → cancelled</code> (rare; refund/clawback; requires <code>cancelled_reason</code>). All other transitions return <code>INVALID_STATUS_TRANSITION</code> and are rejected at the API boundary AND by the trigger <code>enforce_deal_status_fsm</code>.</li></ul>\n<ol><li><strong>MUST</strong> record every deal status transition as an append-only row in <code>deal_status_history</code> (per DEC-346): <code>(id BIGSERIAL, deal_id UUID, tenant_id UUID, from_status deal_status, to_status deal_status, from_stage_id UUID, to_stage_id UUID, changed_at TIMESTAMPTZ, changed_by_subject_id UUID, reason TEXT)</code>. <code>REVOKE UPDATE, DELETE ON deal_status_history FROM cyberos_app</code>.</li></ol>\n<ol><li><strong>MUST</strong> record every deal stage transition (within <code>status=open</code>) as an append-only row in <code>deal_stage_history</code>: <code>(id BIGSERIAL, deal_id UUID, tenant_id UUID, from_stage_id UUID, to_stage_id UUID, from_probability INT, to_probability INT, changed_at TIMESTAMPTZ, changed_by_subject_id UUID, reason TEXT)</code>. Append-only via SQL grant.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> AND <code>WITH CHECK</code> clauses on <code>accounts</code>, <code>contacts</code>, <code>contact_account_membership</code>, <code>pipelines</code>, <code>pipeline_stages</code>, <code>deals</code>, <code>deal_status_history</code>, <code>deal_stage_history</code>. Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li></ol>\n<ol><li><strong>MUST</strong> store money as BIGINT minor units (per task-audit skill rule 11 + DEC-345). <code>amount_minor</code> is the smallest currency unit (e.g. cents for USD, đồng for VND — VND has no minor unit so 1 VND = 1 minor). <code>amount_currency</code> is the ISO-4217 code. The conversion to major units for display uses the <code>Currency::decimals()</code> helper.</li></ol>\n<ol><li><strong>MUST</strong> seed 4 default pipelines per tenant at tenant provisioning (per DEC-341, via TASK-TEN-001's hook). The seed function <code>seed_default_pipelines(tenant_id, created_by)</code> creates:</li></ol>\n<ul><li>Sales pipeline: stages <code>Lead → Qualified → Proposal → Negotiation → Won | Lost</code>.</li><li>Partner pipeline: stages <code>Identified → Engaged → Co-pitching → Signed → Active | Disengaged</code>.</li><li>Inbound pipeline: stages <code>New → Triaged → Demo → Trial → Converted | Disqualified</code>.</li><li>Outbound pipeline: stages <code>Researched → Contacted → Replied → Demo → Won | Lost</code>. Each pipeline has <code>is_default = true</code> for its shape; tenants can add custom stages or rename.</li></ul>\n<ol><li><strong>MUST</strong> ship REST handlers:</li></ol>\n<ul><li><code>POST /v1/crm/accounts</code> — create account; idempotent via Idempotency-Key.</li><li><code>GET /v1/crm/accounts/{id}</code> — fetch account + linked contacts.</li><li><code>PATCH /v1/crm/accounts/{id}</code> — update non-immutable fields.</li><li><code>POST /v1/crm/contacts</code> — create contact + initial membership(s).</li><li><code>GET /v1/crm/contacts/{id}</code> — fetch contact + memberships.</li><li><code>POST /v1/crm/contacts/{id}/memberships</code> — add account membership.</li><li><code>DELETE /v1/crm/contacts/{id}/memberships/{account_id}</code> — remove (requires ≥1 remaining).</li><li><code>POST /v1/crm/pipelines</code> — create pipeline (with initial stages).</li><li><code>GET /v1/crm/pipelines?shape=&lt;&gt;</code> — list.</li><li><code>POST /v1/crm/pipelines/{id}/stages</code> — add stage.</li><li><code>POST /v1/crm/deals</code> — create deal.</li><li><code>GET /v1/crm/deals/{id}</code> — fetch.</li><li><code>PATCH /v1/crm/deals/{id}</code> — update non-status non-stage fields.</li><li><code>POST /v1/crm/deals/{id}/stage</code> — transition stage (within open).</li><li><code>POST /v1/crm/deals/{id}/status</code> — transition status (open → won/lost/cancelled).</li></ul>\n<ol><li><strong>MUST</strong> validate that <strong>contact has ≥1 membership</strong> at handler boundary. POST <code>/contacts</code> with empty <code>initial_memberships</code> → 400 <code>contact_must_have_membership</code>. DELETE last membership → 409 <code>cannot_remove_last_membership</code> (suggest delete contact instead).</li></ol>\n<ol><li><strong>MUST</strong> emit memory audit rows for the 8 kinds (per DEC-349):</li></ol>\n<ul><li><code>crm.account_created</code>, <code>crm.account_updated</code> on account writes.</li><li><code>crm.contact_created</code> on contact creation.</li><li><code>crm.deal_created</code> on deal creation.</li><li><code>crm.deal_stage_changed</code> on stage transition.</li><li><code>crm.deal_won</code> on status → won.</li><li><code>crm.deal_lost</code> on status → lost (carries <code>lost_reason</code>).</li><li><code>crm.deal_cancelled</code> on status → cancelled. All audit-before-action per task-audit skill rule 25 (emitted within the same transaction as the DB write).</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>email</code>, <code>phone</code>, <code>full_name</code> via TASK-MEMORY-111 BEFORE chain commit. The PostgreSQL rows retain raw values (tenant-scoped + RLS-protected); memory audit chain holds only <code>subject_id_hash16</code> + <code>email_hash16</code> + redacted forms.</li></ol>\n<ol><li><strong>MUST</strong> require <code>expected_close_date &gt;= CURRENT_DATE</code> at deal creation AND on transitions into <code>is_open</code> stages (per DEC-352). Past-dated expected_close → 400 <code>expected_close_in_past</code>.</li></ol>\n<ol><li><strong>MUST</strong> set <code>won_at = now()</code> automatically on status → won; set <code>actual_close_date = now()</code> on status → won/lost/cancelled. The trigger <code>set_close_dates</code> handles this.</li></ol>\n<ol><li><strong>MUST</strong> require <code>lost_reason</code> (1–500 chars) when status → lost; require <code>cancelled_reason</code> (1–500 chars) when status → cancelled. Missing reason → 400 <code>reason_required</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship probability override semantics (per DEC-351): on stage transition, probability defaults to the new stage's <code>probability_default</code>; explicit <code>probability</code> in the request overrides. Outside [0, 100] → 400 <code>probability_out_of_range</code>.</li></ol>\n<ol><li><strong>MUST</strong> complete handlers in ≤ 100 ms p95. <code>crm_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>crm.{account,contact,pipeline,deal}.{create,update,stage,status,...}</code> with attributes: <code>tenant_id</code>, <code>entity_id</code>, <code>outcome</code> (success | invalid_status_transition | invalid_stage_transition | reason_required | expected_close_in_past | permission_denied | not_found).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>crm_account_create_total{outcome}</code> (counter).</li><li><code>crm_contact_create_total{outcome}</code> (counter).</li><li><code>crm_deal_create_total{outcome, pipeline_shape}</code> (counter).</li><li><code>crm_deal_status_transitions_total{from_status, to_status, outcome}</code> (counter).</li><li><code>crm_deal_stage_transitions_total{pipeline_shape, outcome}</code> (counter).</li><li><code>crm_deal_amount_minor{tenant_id, currency, status}</code> (gauge — periodic sum compute).</li><li><code>crm_open_deal_count{tenant_id, pipeline_id}</code> (gauge).</li></ul>\n<ol><li><strong>MUST</strong> support cursor pagination on list handlers (max 200 default 50; cursor on <code>(updated_at DESC, id)</code>).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why closed 3-primitive entity model (DEC-340)?</strong> Account → Contact → Deal is the Salesforce-flavoured industry standard. Adding a 4th primitive (Lead, Opportunity, Quote, Order) duplicates concerns: a Lead is \"an unqualified Contact\"; an Opportunity is \"an early-stage Deal\". The closed 3-primitive model forces operators to use the existing entities + stage transitions instead of inventing parallel hierarchies. ADR-required additions force the cross-module impact analysis.</p>\n<p><strong>Why 4 closed pipeline shapes (DEC-341)?</strong> Each shape has different optimal next-action heuristics (TASK-CRM-005's CUO skill consults the shape). Sales = \"AM nudge\"; Partner = \"exec sponsor activation\"; Inbound = \"speed-to-first-response\"; Outbound = \"personalisation\". A free-form 5th shape would force the skill to fall back to a generic heuristic — worse than picking one of the 4 deliberately. Adding a 5th shape ADR requires updating the skill's logic too.</p>\n<p><strong>Why tenant-defined stages per pipeline (DEC-342, §1 #6)?</strong> Different industries have different sales motions. SaaS sales: <code>Lead → Qualified → Demo → Trial → Closed</code>. Consulting: <code>Lead → Scoping → Proposal → Won</code>. Recurring services: <code>Lead → Discovery → Pilot → MSA → Renewal</code>. Forcing a global stage set would break ~half of tenants. Letting tenants define stages per pipeline + tag with <code>is_won/is_lost/is_open</code> keeps the downstream behaviour predictable.</p>\n<p><strong>Why deal_status independent from stage (DEC-344, §1 #8)?</strong> Stage = \"where in the pipeline?\" (can vary across pipelines). Status = \"what's the outcome?\" (universal: open/won/lost/cancelled). Treating them as one would duplicate the closed status enum across every tenant's stage list — drift inevitable. Splitting keeps the universal forecast logic clean (filter <code>status='open'</code> for pipeline; <code>status='won'</code> for revenue).</p>\n<p><strong>Why deal_status FSM at trigger AND handler (DEC-347, §1 #9)?</strong> Defense in depth. Handler validates at API boundary (good UX); trigger catches direct SQL access (e.g. operator psql session). The trigger checks: (a) transition is in the allowed set; (b) <code>current_stage.is_won/is_lost</code> matches the transition; (c) required reason fields are present.</p>\n<p><strong>Why won/lost terminal vs cancelled allows won → cancelled (DEC-347, §1 #9)?</strong> <code>lost</code> is final — \"this deal will not close\". <code>cancelled</code> is administrative — \"even though we won, refund or clawback\". The <code>won → cancelled</code> transition handles rare cases (customer backs out within return window). <code>lost → cancelled</code> makes no sense — a lost deal is already terminal.</p>\n<p><strong>Why money as BIGINT minor (DEC-345, §1 #13)?</strong> task-audit skill rule 11 — financial precision. Storing <code>1500000</code> minor units for 1.5M VND is exact; <code>1.5e6</code> FLOAT introduces rounding. Downstream TASK-INV-001 invoice math depends on exact addition; FLOAT drift produces invoices that don't sum to the deal amount.</p>\n<p><strong>Why append-only history on status + stage (DEC-346, §1 #10, #11)?</strong> Forecast accuracy + win/loss attribution depend on \"what was the stage on 2026-05-15?\" — questions only answerable from a chained history. UPDATE in place would lose the prior values. SQL grants make it audit-grade (operator typo can't rewrite); handler discipline alone is insufficient.</p>\n<p><strong>Why many-to-many contact-account membership (DEC-348, §1 #3)?</strong> Common cases: a CFO splits time between two subsidiary entities; an external consultant has billing relationships with multiple parent accounts; a lawyer represents multiple companies. Forcing one-account-per-contact creates duplicate contacts (the same human as 3 rows) — duplicates break dedup, break activity feeds (which appear under each duplicate), break next-action ranking. Many-to-many is the canonical shape.</p>\n<p><strong>Why contact must have ≥1 membership (§1 #16)?</strong> A contact with no membership is orphan data — no account to roll up under, no pipeline to track against. Enforcing at handler (not DB constraint) allows transactional bootstrap (create contact + initial membership in one tx).</p>\n<p><strong>Why probability override (DEC-351, §1 #22)?</strong> Stage defaults capture \"typical probability at this stage\"; per-deal override captures \"this specific deal is unusually strong (or weak)\". Salespeople know context the stage doesn't. The default-but-override pattern preserves the forecast-accuracy benefit of stage defaults while allowing per-deal calibration.</p>\n<p><strong>Why expected_close_date required + future (§1 #19, DEC-352)?</strong> Forecast accuracy demands a date. Allowing past-dated values (other than <code>actual_close_date</code>) corrupts pipeline forecast queries. Default to +30 days (slice 1 default) is a sane starting point; salesperson refines per deal.</p>\n<p><strong>Why won_at automatic + actual_close_date automatic (§1 #20)?</strong> Operators forget to set these manually; the trigger handles it. The cost is \"operator can't backdate\" (acceptable — historical correction is a rare ADR-driven path).</p>\n<p><strong>Why language_code on contact (§1 #2)?</strong> Vietnamese-aware salutations + outreach templates need to know the contact's preferred language. Reusing TASK-KB-001's <code>language_code</code> enum keeps consistency.</p>\n<p><strong>Why owner_subject_id required on deal + account (§1 #1, #7)?</strong> Ownership drives forecast attribution (CFO dashboard rolls up by owner) and CUO next-action skill (suggests moves for THIS owner's deals). Ownerless deals are forecast noise — make it a required field.</p>\n<p><strong>Why deal close emits 3 distinct audit kinds (deal_won, deal_lost, deal_cancelled) instead of one (DEC-349, §1 #17)?</strong> Different operator queries: \"show me wins this quarter\" filters <code>crm.deal_won</code>; \"show me losses to investigate root causes\" filters <code>crm.deal_lost</code>. One kind with a status field would force every consumer to filter — fewer selectivity benefits.</p>\n<p><strong>Why MST field present but validation deferred (DEC-343, §1 #1)?</strong> The column needs to exist now so deals can be created with the field; TASK-CRM-003 ships the validation (calls <code>vietnam-mst-validate</code> skill on write). Slice 1 stores any 10–13 char string; slice 2 enforces format + GDT registry check.</p>\n<p><strong>Why pipeline_stages position 1-50 (§1 #6)?</strong> Reasonable practical bound — pipelines deeper than 50 stages are pathological (operators lose track). 1-indexed for natural display order.</p>\n<p><strong>Why <code>is_open/is_won/is_lost</code> mutually exclusive (§1 #6)?</strong> A stage is exactly one of the three classifications. Allowing multiple (e.g. is_open + is_won) makes the deal_status FSM ambiguous. DB CHECK enforces.</p>\n<p><strong>Why no PATCH for <code>current_stage_id</code> (§1 #15)?</strong> Stage changes have side effects (probability update, history row, memory audit). PATCH would have to duplicate the orchestration; dedicated handler <code>POST /deals/{id}/stage</code> is the contract.</p>\n<p><strong>Why one default pipeline per (tenant, shape) but not strict (§1 #5)?</strong> Most tenants use one pipeline per shape; allowing multiples (partial unique index) supports edge cases (e.g. \"Sales-EU\" vs \"Sales-US\" pipelines). The <code>is_default</code> flag picks the canonical one for <code>vn-create-deal</code> skill auto-pipeline selection.</p>\n<p><strong>Why phone/email nullable on contact (§1 #2)?</strong> Real CRM data is incomplete; forcing both required would block import + bulk add. Nullable lets operators progressively enrich.</p>\n<p><strong>Why account.legal_name vs display_name (§1 #1)?</strong> Legal name is the regulatory entity (\"ACME Corporation Joint Stock Company\"); display_name is the brand (\"ACME\"). Reports use legal_name; UI uses display_name. Both nullable-with-fallback simplifies (display_name defaults to legal_name).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0001 — accounts</h3>\n<pre><code class=\"language-sql\">-- services/crm/migrations/0001_accounts.sql\n\nBEGIN;\n\n-- Placeholder enum at slice 1; full VN-specific enum ships in TASK-CRM-003.\nCREATE TYPE account_type AS ENUM ('unknown');\n\nCREATE TABLE accounts (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    legal_name             TEXT         NOT NULL CHECK (length(legal_name) BETWEEN 1 AND 200),\n    display_name           TEXT         CHECK (display_name IS NULL OR length(display_name) BETWEEN 1 AND 200),\n    account_type           account_type NOT NULL DEFAULT 'unknown',\n    mst                    TEXT,\n    website                TEXT,\n    industry               TEXT,\n    headquarters_country   TEXT,\n    owner_subject_id       UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    updated_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL REFERENCES auth.subjects(id)\n);\n\nCREATE INDEX accounts_tenant_owner_idx ON accounts (tenant_id, owner_subject_id);\nCREATE INDEX accounts_tenant_name_idx ON accounts (tenant_id, legal_name);\n\nALTER TABLE accounts ENABLE ROW LEVEL SECURITY;\nCREATE POLICY accounts_tenant_isolation ON accounts\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0002 — contacts + membership</h3>\n<pre><code class=\"language-sql\">-- services/crm/migrations/0002_contacts.sql\n\nBEGIN;\n\nCREATE TABLE contacts (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    full_name              TEXT         NOT NULL CHECK (length(full_name) BETWEEN 1 AND 200),\n    display_name           TEXT,\n    email                  TEXT,\n    phone                  TEXT,\n    title                  TEXT,\n    language_code          language_code,\n    owner_subject_id       UUID         NOT NULL REFERENCES auth.subjects(id),\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    updated_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL\n);\n\nCREATE TABLE contact_account_membership (\n    contact_id   UUID         NOT NULL REFERENCES contacts(id) ON DELETE RESTRICT,\n    account_id   UUID         NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,\n    tenant_id    UUID         NOT NULL,\n    role         TEXT         NOT NULL DEFAULT 'general',\n    is_primary   BOOLEAN      NOT NULL DEFAULT false,\n    created_at   TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    PRIMARY KEY (contact_id, account_id)\n);\n\nCREATE INDEX contacts_tenant_name_idx ON contacts (tenant_id, full_name);\nCREATE INDEX contacts_tenant_email_idx ON contacts (tenant_id, email) WHERE email IS NOT NULL;\nCREATE INDEX cam_account_idx ON contact_account_membership (tenant_id, account_id);\n\nALTER TABLE contacts ENABLE ROW LEVEL SECURITY;\nALTER TABLE contact_account_membership ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY contacts_tenant_isolation ON contacts\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCREATE POLICY cam_tenant_isolation ON contact_account_membership\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.3 — Migration 0003 — pipelines + stages</h3>\n<pre><code class=\"language-sql\">-- services/crm/migrations/0003_pipelines_stages.sql\n\nBEGIN;\n\nCREATE TYPE pipeline_shape AS ENUM ('sales', 'partner', 'inbound', 'outbound');\n\nCREATE TABLE pipelines (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    name                   TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 100),\n    shape                  pipeline_shape NOT NULL,\n    is_default             BOOLEAN      NOT NULL DEFAULT false,\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL\n);\n\nCREATE UNIQUE INDEX uniq_pipeline_tenant_name ON pipelines (tenant_id, name);\nCREATE UNIQUE INDEX uniq_pipeline_tenant_shape_default ON pipelines (tenant_id, shape) WHERE is_default = true;\n\nCREATE TABLE pipeline_stages (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    pipeline_id            UUID         NOT NULL REFERENCES pipelines(id) ON DELETE RESTRICT,\n    name                   TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 50),\n    position               INT          NOT NULL CHECK (position BETWEEN 1 AND 50),\n    probability_default    INT          NOT NULL CHECK (probability_default BETWEEN 0 AND 100) DEFAULT 50,\n    is_open                BOOLEAN      NOT NULL DEFAULT true,\n    is_won                 BOOLEAN      NOT NULL DEFAULT false,\n    is_lost                BOOLEAN      NOT NULL DEFAULT false,\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    CHECK ((is_open::int + is_won::int + is_lost::int) = 1)\n);\n\nCREATE UNIQUE INDEX uniq_stage_pipeline_position ON pipeline_stages (pipeline_id, position);\nCREATE UNIQUE INDEX uniq_stage_pipeline_name ON pipeline_stages (pipeline_id, name);\n\nALTER TABLE pipelines ENABLE ROW LEVEL SECURITY;\nALTER TABLE pipeline_stages ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY pipelines_tenant_isolation ON pipelines\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCREATE POLICY pipeline_stages_tenant_isolation ON pipeline_stages\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.4 — Migration 0004 — deals</h3>\n<pre><code class=\"language-sql\">-- services/crm/migrations/0004_deals.sql\n\nBEGIN;\n\nCREATE TYPE deal_status AS ENUM ('open', 'won', 'lost', 'cancelled');\n\nCREATE TABLE deals (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    name                   TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 200),\n    account_id             UUID         NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,\n    primary_contact_id     UUID         REFERENCES contacts(id),\n    pipeline_id            UUID         NOT NULL REFERENCES pipelines(id),\n    current_stage_id       UUID         NOT NULL REFERENCES pipeline_stages(id),\n    status                 deal_status  NOT NULL DEFAULT 'open',\n    probability            INT          NOT NULL CHECK (probability BETWEEN 0 AND 100),\n    amount_minor           BIGINT       NOT NULL CHECK (amount_minor &gt;= 0),\n    amount_currency        CHAR(3)      NOT NULL CHECK (amount_currency ~ '^[A-Z]{3}$'),\n    expected_close_date    DATE         NOT NULL,\n    actual_close_date      DATE,\n    won_at                 TIMESTAMPTZ,\n    lost_reason            TEXT         CHECK (lost_reason IS NULL OR length(lost_reason) BETWEEN 1 AND 500),\n    cancelled_reason       TEXT         CHECK (cancelled_reason IS NULL OR length(cancelled_reason) BETWEEN 1 AND 500),\n    owner_subject_id       UUID         NOT NULL REFERENCES auth.subjects(id),\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    updated_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL\n);\n\nCREATE INDEX deals_tenant_owner_idx ON deals (tenant_id, owner_subject_id) WHERE status = 'open';\nCREATE INDEX deals_tenant_pipeline_idx ON deals (tenant_id, pipeline_id, current_stage_id) WHERE status = 'open';\nCREATE INDEX deals_tenant_account_idx ON deals (tenant_id, account_id);\nCREATE INDEX deals_close_date_idx ON deals (tenant_id, expected_close_date) WHERE status = 'open';\n\nALTER TABLE deals ENABLE ROW LEVEL SECURITY;\nCREATE POLICY deals_tenant_isolation ON deals\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- FSM transition trigger (DEC-347)\nCREATE OR REPLACE FUNCTION enforce_deal_status_fsm() RETURNS TRIGGER AS $$\nDECLARE\n    stage_is_won BOOLEAN; stage_is_lost BOOLEAN;\nBEGIN\n    IF NEW.status = OLD.status THEN RETURN NEW; END IF;\n\n    -- Allowed: open → won/lost/cancelled, won → cancelled\n    IF NOT (\n        (OLD.status = 'open' AND NEW.status IN ('won', 'lost', 'cancelled'))\n        OR (OLD.status = 'won' AND NEW.status = 'cancelled')\n    ) THEN\n        RAISE EXCEPTION 'invalid_deal_status_transition: % -&gt; %', OLD.status, NEW.status USING ERRCODE = 'P0050';\n    END IF;\n\n    -- Stage gate: won requires is_won; lost requires is_lost\n    IF NEW.status = 'won' THEN\n        SELECT is_won INTO stage_is_won FROM pipeline_stages WHERE id = NEW.current_stage_id;\n        IF NOT stage_is_won THEN\n            RAISE EXCEPTION 'cannot_mark_won_from_non_won_stage' USING ERRCODE = 'P0051';\n        END IF;\n        NEW.won_at := now();\n        NEW.actual_close_date := CURRENT_DATE;\n    ELSIF NEW.status = 'lost' THEN\n        SELECT is_lost INTO stage_is_lost FROM pipeline_stages WHERE id = NEW.current_stage_id;\n        IF NOT stage_is_lost THEN\n            RAISE EXCEPTION 'cannot_mark_lost_from_non_lost_stage' USING ERRCODE = 'P0052';\n        END IF;\n        IF NEW.lost_reason IS NULL OR length(NEW.lost_reason) = 0 THEN\n            RAISE EXCEPTION 'lost_reason_required' USING ERRCODE = 'P0053';\n        END IF;\n        NEW.actual_close_date := CURRENT_DATE;\n    ELSIF NEW.status = 'cancelled' THEN\n        IF NEW.cancelled_reason IS NULL OR length(NEW.cancelled_reason) = 0 THEN\n            RAISE EXCEPTION 'cancelled_reason_required' USING ERRCODE = 'P0054';\n        END IF;\n        NEW.actual_close_date := CURRENT_DATE;\n    END IF;\n\n    NEW.updated_at := now();\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_deals_status_fsm BEFORE UPDATE ON deals\n    FOR EACH ROW EXECUTE FUNCTION enforce_deal_status_fsm();\n\n-- Expected-close future check on insert + stage-change-to-open\nCREATE OR REPLACE FUNCTION enforce_expected_close_future() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.status = 'open' AND NEW.expected_close_date &lt; CURRENT_DATE THEN\n        RAISE EXCEPTION 'expected_close_in_past' USING ERRCODE = 'P0055';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_deals_expected_close BEFORE INSERT ON deals\n    FOR EACH ROW EXECUTE FUNCTION enforce_expected_close_future();\n\nCOMMIT;</code></pre>\n<h3>3.5 — Migration 0005 — status + stage history</h3>\n<pre><code class=\"language-sql\">-- services/crm/migrations/0005_deal_status_history.sql\n\nBEGIN;\n\nCREATE TABLE deal_status_history (\n    id                     BIGSERIAL    PRIMARY KEY,\n    deal_id                UUID         NOT NULL REFERENCES deals(id),\n    tenant_id              UUID         NOT NULL,\n    from_status            deal_status,\n    to_status              deal_status  NOT NULL,\n    from_stage_id          UUID         REFERENCES pipeline_stages(id),\n    to_stage_id            UUID         REFERENCES pipeline_stages(id),\n    changed_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    changed_by_subject_id  UUID         NOT NULL,\n    reason                 TEXT\n);\n\nCREATE TABLE deal_stage_history (\n    id                     BIGSERIAL    PRIMARY KEY,\n    deal_id                UUID         NOT NULL REFERENCES deals(id),\n    tenant_id              UUID         NOT NULL,\n    from_stage_id          UUID         REFERENCES pipeline_stages(id),\n    to_stage_id            UUID         NOT NULL REFERENCES pipeline_stages(id),\n    from_probability       INT,\n    to_probability         INT          NOT NULL,\n    changed_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    changed_by_subject_id  UUID         NOT NULL,\n    reason                 TEXT\n);\n\nCREATE INDEX dsh_deal_idx ON deal_status_history (deal_id, changed_at DESC);\nCREATE INDEX dgh_deal_idx ON deal_stage_history (deal_id, changed_at DESC);\n\nALTER TABLE deal_status_history ENABLE ROW LEVEL SECURITY;\nALTER TABLE deal_stage_history ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY dsh_tenant_isolation ON deal_status_history\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCREATE POLICY dgh_tenant_isolation ON deal_stage_history\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON deal_status_history FROM cyberos_app;\nREVOKE UPDATE, DELETE ON deal_stage_history FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.6 — Default-pipelines seed function</h3>\n<pre><code class=\"language-sql\">-- services/crm/migrations/0006_seed_pipelines.sql\n\nBEGIN;\n\nCREATE OR REPLACE FUNCTION seed_default_pipelines(p_tenant_id UUID, p_created_by UUID) RETURNS VOID AS $$\nDECLARE\n    pid UUID;\nBEGIN\n    -- Sales pipeline\n    INSERT INTO pipelines (id, tenant_id, name, shape, is_default, created_by_subject_id)\n    VALUES (gen_random_uuid(), p_tenant_id, 'Sales', 'sales'::pipeline_shape, true, p_created_by)\n    RETURNING id INTO pid;\n    INSERT INTO pipeline_stages (id, tenant_id, pipeline_id, name, position, probability_default, is_open, is_won, is_lost) VALUES\n        (gen_random_uuid(), p_tenant_id, pid, 'Lead',         1, 10, true,  false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Qualified',    2, 25, true,  false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Proposal',     3, 50, true,  false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Negotiation',  4, 75, true,  false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Won',          5, 100, false, true,  false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Lost',         6, 0,   false, false, true);\n\n    -- Partner pipeline\n    INSERT INTO pipelines (id, tenant_id, name, shape, is_default, created_by_subject_id)\n    VALUES (gen_random_uuid(), p_tenant_id, 'Partner', 'partner'::pipeline_shape, true, p_created_by)\n    RETURNING id INTO pid;\n    INSERT INTO pipeline_stages (id, tenant_id, pipeline_id, name, position, probability_default, is_open, is_won, is_lost) VALUES\n        (gen_random_uuid(), p_tenant_id, pid, 'Identified',  1, 10,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Engaged',     2, 30,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Co-pitching', 3, 60,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Signed',      4, 90,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Active',      5, 100, false, true,  false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Disengaged',  6, 0,   false, false, true);\n\n    -- Inbound pipeline\n    INSERT INTO pipelines (id, tenant_id, name, shape, is_default, created_by_subject_id)\n    VALUES (gen_random_uuid(), p_tenant_id, 'Inbound', 'inbound'::pipeline_shape, true, p_created_by)\n    RETURNING id INTO pid;\n    INSERT INTO pipeline_stages (id, tenant_id, pipeline_id, name, position, probability_default, is_open, is_won, is_lost) VALUES\n        (gen_random_uuid(), p_tenant_id, pid, 'New',           1, 15,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Triaged',       2, 30,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Demo',          3, 55,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Trial',         4, 80,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Converted',     5, 100, false, true, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Disqualified',  6, 0,   false, false, true);\n\n    -- Outbound pipeline\n    INSERT INTO pipelines (id, tenant_id, name, shape, is_default, created_by_subject_id)\n    VALUES (gen_random_uuid(), p_tenant_id, 'Outbound', 'outbound'::pipeline_shape, true, p_created_by)\n    RETURNING id INTO pid;\n    INSERT INTO pipeline_stages (id, tenant_id, pipeline_id, name, position, probability_default, is_open, is_won, is_lost) VALUES\n        (gen_random_uuid(), p_tenant_id, pid, 'Researched', 1, 5,   true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Contacted',  2, 15,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Replied',    3, 35,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Demo',       4, 60,  true, false, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Won',        5, 100, false, true, false),\n        (gen_random_uuid(), p_tenant_id, pid, 'Lost',       6, 0,   false, false, true);\nEND;\n$$ LANGUAGE plpgsql;\n\nCOMMIT;</code></pre>\n<h3>3.7 — Rust types</h3>\n<pre><code class=\"language-rust\">// services/crm/src/types.rs\nuse chrono::{DateTime, NaiveDate, Utc};\nuse serde::{Deserialize, Serialize};\nuse sqlx::{FromRow, Type};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"deal_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum DealStatus { Open, Won, Lost, Cancelled }\n\nimpl DealStatus {\n    pub const ALL: &amp;'static [DealStatus] = &amp;[DealStatus::Open, DealStatus::Won, DealStatus::Lost, DealStatus::Cancelled];\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"pipeline_shape\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum PipelineShape { Sales, Partner, Inbound, Outbound }\n\nimpl PipelineShape {\n    pub const ALL: &amp;'static [PipelineShape] = &amp;[PipelineShape::Sales, PipelineShape::Partner, PipelineShape::Inbound, PipelineShape::Outbound];\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct Account {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub legal_name: String,\n    pub display_name: Option&lt;String&gt;,\n    pub account_type: String,   // closed enum expansion in TASK-CRM-003\n    pub mst: Option&lt;String&gt;,\n    pub website: Option&lt;String&gt;,\n    pub industry: Option&lt;String&gt;,\n    pub headquarters_country: Option&lt;String&gt;,\n    pub owner_subject_id: Uuid,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub updated_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct Deal {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub name: String,\n    pub account_id: Uuid,\n    pub primary_contact_id: Option&lt;Uuid&gt;,\n    pub pipeline_id: Uuid,\n    pub current_stage_id: Uuid,\n    pub status: DealStatus,\n    pub probability: i32,\n    pub amount_minor: i64,\n    pub amount_currency: String,\n    pub expected_close_date: NaiveDate,\n    pub actual_close_date: Option&lt;NaiveDate&gt;,\n    pub won_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    pub lost_reason: Option&lt;String&gt;,\n    pub cancelled_reason: Option&lt;String&gt;,\n    pub owner_subject_id: Uuid,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub updated_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}</code></pre>\n<h3>3.8 — Status FSM validator</h3>\n<pre><code class=\"language-rust\">// services/crm/src/fsm/deal_status.rs\nuse crate::types::DealStatus;\n\npub fn is_valid_transition(from: DealStatus, to: DealStatus) -&gt; bool {\n    use DealStatus::*;\n    matches!((from, to),\n        (Open, Won) | (Open, Lost) | (Open, Cancelled) | (Won, Cancelled)\n    )\n}\n\n#[derive(Debug, thiserror::Error)]\n#[error(\"invalid_deal_status_transition: {from:?} -&gt; {to:?}\")]\npub struct InvalidStatusTransition { pub from: DealStatus, pub to: DealStatus }\n\npub fn validate_transition(from: DealStatus, to: DealStatus) -&gt; Result&lt;(), InvalidStatusTransition&gt; {\n    if from == to { return Ok(()); }\n    if is_valid_transition(from, to) { Ok(()) } else { Err(InvalidStatusTransition { from, to }) }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>DealStatus enum closed at 4</strong> — <code>DealStatus::ALL.len() == 4</code>; Postgres enum has exactly 4 labels.</li><li><strong>PipelineShape enum closed at 4</strong> — same.</li><li><strong>RLS isolates by tenant</strong> — query as tenant-A returns 0 rows of tenant-B.</li><li><strong>POST account happy path</strong> — 201 + <code>crm.account_created</code> memory row.</li><li><strong>POST contact with ≥1 membership</strong> — 201 + <code>crm.contact_created</code> row.</li><li><strong>POST contact with 0 memberships</strong> → 400 <code>contact_must_have_membership</code>.</li><li><strong>DELETE last membership</strong> → 409 <code>cannot_remove_last_membership</code>.</li><li><strong>POST deal happy path</strong> — 201 + <code>crm.deal_created</code> row; default probability matches stage.</li><li><strong>POST deal past expected_close_date</strong> → 400 <code>expected_close_in_past</code>.</li><li><strong>Stage transition emits deal_stage_history row</strong> + <code>crm.deal_stage_changed</code> memory row.</li><li><strong>Status open → won at non-won stage</strong> → 400 <code>cannot_mark_won_from_non_won_stage</code>.</li><li><strong>Status open → won at won-stage</strong> → 200; <code>won_at</code> set; <code>actual_close_date</code> set; <code>crm.deal_won</code> memory row.</li><li><strong>Status open → lost without reason</strong> → 400 <code>lost_reason_required</code>.</li><li><strong>Status open → lost with reason</strong> → 200; <code>crm.deal_lost</code> memory row carries reason.</li><li><strong>Status open → cancelled with reason</strong> → 200; <code>crm.deal_cancelled</code> memory row.</li><li><strong>Status lost → won rejected</strong> — invalid_deal_status_transition.</li><li><strong>Status won → cancelled allowed</strong> (clawback scenario).</li><li><strong>Money stored as BIGINT minor</strong> — 1500000 stored; rendered as 1.5M VND via Currency helper.</li><li><strong>deal_status_history append-only</strong> — UPDATE/DELETE rejected by SQL grant.</li><li><strong>deal_stage_history append-only</strong> — same.</li><li><strong>Pipeline stage mutual exclusion</strong> — <code>is_open=true, is_won=true</code> insert → DB CHECK fails.</li><li><strong>One default pipeline per (tenant, shape)</strong> — second pipeline with <code>is_default=true</code> for same shape → UNIQUE violation.</li><li><strong>seed_default_pipelines creates 4 pipelines</strong> — called per TASK-TEN-001 hook; 4 pipelines × 6 stages each = 24 stages seeded.</li><li><strong>Probability 0-100 enforced</strong> — outside range → 400.</li><li><strong>OTel span emitted</strong> — <code>crm.deal.create</code> with <code>outcome=success</code>.</li><li><strong>Counters increment</strong> per audit row kind.</li><li><strong>Perf &lt; 100ms p95</strong> — <code>crm_perf_test</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/crm/tests/deal_status_fsm_test.rs\nuse cyberos_crm::fsm::deal_status::{is_valid_transition, validate_transition};\nuse cyberos_crm::types::DealStatus::*;\n\n#[test]\nfn valid_transitions() {\n    for (from, to) in [(Open, Won), (Open, Lost), (Open, Cancelled), (Won, Cancelled)] {\n        assert!(is_valid_transition(from, to));\n    }\n}\n\n#[test]\nfn invalid_transitions_rejected() {\n    for (from, to) in [(Lost, Won), (Won, Open), (Cancelled, Open), (Cancelled, Won)] {\n        assert!(!is_valid_transition(from, to));\n        assert!(validate_transition(from, to).is_err());\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/crm/tests/pipeline_stages_test.rs\n#[sqlx::test]\nasync fn stage_mutual_exclusion(pool: sqlx::PgPool) {\n    let (tenant, pipeline) = seed_pipeline(&amp;pool, PipelineShape::Sales).await;\n    let err = sqlx::query(\"INSERT INTO pipeline_stages (id, tenant_id, pipeline_id, name, position, probability_default, is_open, is_won, is_lost) VALUES ($1, $2, $3, 'BadStage', 99, 50, true, true, false)\")\n        .bind(Uuid::new_v4()).bind(tenant).bind(pipeline).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"violates check constraint\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/crm/tests/contacts_membership_test.rs\n#[tokio::test]\nasync fn contact_without_membership_rejected(ctx: TestCtx) {\n    let resp = ctx.post_contact_with_zero_memberships().await;\n    assert_eq!(resp.status(), 400);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"error\"], \"contact_must_have_membership\");\n}\n\n#[tokio::test]\nasync fn cannot_remove_last_membership(ctx: TestCtx) {\n    let contact = ctx.create_contact_with_one_membership().await;\n    let resp = ctx.delete_membership(contact.id, contact.account_id).await;\n    assert_eq!(resp.status(), 409);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"error\"], \"cannot_remove_last_membership\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/crm/tests/deals_create_test.rs\n#[tokio::test]\nasync fn deal_won_at_lost_stage_rejected(ctx: TestCtx) {\n    let stage = ctx.create_stage_with_lost_flag().await;\n    let deal = ctx.create_deal_at_stage(stage.id).await;\n    let resp = ctx.transition_status(deal.id, json!({\"to\": \"won\"})).await;\n    assert_eq!(resp.status(), 400);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"error\"], \"cannot_mark_won_from_non_won_stage\");\n}\n\n#[tokio::test]\nasync fn deal_won_emits_memory_row(ctx: TestCtx) {\n    let stage = ctx.create_won_stage().await;\n    let deal = ctx.create_deal_at_stage(stage.id).await;\n    ctx.transition_status(deal.id, json!({\"to\": \"won\"})).await;\n    let rows = ctx.memory_audit_rows(\"crm.deal_won\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"deal_id\"], deal.id.to_string());\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 8 memory row builders follow the canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-AUTH-003</strong> — RLS enforcement.</li><li><strong>TASK-AUTH-101</strong> — RBAC catalogue (<code>Resource::CrmAccount</code>, <code>CrmContact</code>, <code>CrmDeal</code>).</li></ul>\n<p><strong>Downstream (3 placeholders):</strong></p>\n<ul><li><strong>TASK-CRM-002</strong> — activity feed auto-log from EMAIL/CHAT/Calendar.</li><li><strong>TASK-CRM-003</strong> — VN-specific account_type enum + MST validation.</li><li><strong>TASK-CRM-004</strong> — Convert-to-Engagement workflow (consumes deals + emits to PROJ-005).</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AI-003</strong> — memory audit bridge.</li><li><strong>TASK-MEMORY-111</strong> — PII scrubbing for email, phone, full_name.</li><li><strong>TASK-TEN-001</strong> — seeds default pipelines via the seed_default_pipelines function during tenant provisioning.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/crm/deals request</h3>\n<pre><code class=\"language-json\">{\n  \"name\": \"ACME — CyberOS Annual Subscription\",\n  \"account_id\": \"9b1deb4d-...\",\n  \"primary_contact_id\": \"0a1deb4d-...\",\n  \"pipeline_id\": \"&lt;default sales pipeline&gt;\",\n  \"current_stage_id\": \"&lt;Qualified stage&gt;\",\n  \"amount_minor\": 12000000000,\n  \"amount_currency\": \"VND\",\n  \"expected_close_date\": \"2026-07-15\"\n}</code></pre>\n<h3>8.2 — crm.deal_created memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"crm.deal_created\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"deal_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"account_id\": \"9b1deb4d-...\",\n  \"pipeline_id\": \"&lt;uuid&gt;\",\n  \"stage_id\": \"&lt;uuid&gt;\",\n  \"amount_minor\": 12000000000,\n  \"amount_currency\": \"VND\",\n  \"expected_close_date\": \"2026-07-15\",\n  \"owner_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.3 — POST /v1/crm/deals/{id}/status (won)</h3>\n<pre><code class=\"language-json\">{ \"to\": \"won\", \"reason\": \"Customer signed MSA; kickoff Monday\" }</code></pre>\n<h3>8.4 — crm.deal_won memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"crm.deal_won\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"deal_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"amount_minor\": 12000000000,\n  \"amount_currency\": \"VND\",\n  \"won_at\": \"2026-07-15T14:00:00Z\",\n  \"changed_by_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>VN-specific account_type enum (Sole / LLC / JSC / FDI)</strong> — TASK-CRM-003.</li><li><strong>MST format + GDT registry validation</strong> — TASK-CRM-003 + TASK-CRM-008.</li><li><strong>Activity feed auto-log</strong> — TASK-CRM-002.</li><li><strong>Convert-to-Engagement workflow</strong> — TASK-CRM-004.</li><li><strong>Next-action CUO skill</strong> — TASK-CRM-005.</li><li><strong>AI lead scoring</strong> — TASK-CRM-006.</li><li><strong>Win/loss analysis CUO draft</strong> — TASK-CRM-007.</li><li><strong>VietQR invoice collection</strong> — TASK-CRM-009.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>RLS bypass attempt</td><td><code>USING</code> predicate</td><td>0 rows</td><td>None — designed</td></tr><tr><td>Cross-tenant FK</td><td>RLS</td><td>permission_denied</td><td>None — designed</td></tr><tr><td>Contact with 0 memberships</td><td>handler</td><td>400 contact_must_have_membership</td><td>Add membership in same tx</td></tr><tr><td>Delete last membership</td><td>handler</td><td>409 cannot_remove_last_membership</td><td>Delete contact instead</td></tr><tr><td>Stage <code>is_open + is_won</code> both true</td><td>DB CHECK</td><td>INSERT fails</td><td>Fix stage definition</td></tr><tr><td>Two defaults per (tenant, shape)</td><td>UNIQUE partial index</td><td>INSERT fails</td><td>Unset existing default first</td></tr><tr><td>Status transition lost → won</td><td>trigger FSM</td><td>400 invalid_deal_status_transition</td><td>Designed</td></tr><tr><td>Status open → won at non-won stage</td><td>trigger</td><td>400 cannot_mark_won_from_non_won_stage</td><td>Move to won stage first</td></tr><tr><td>Status open → lost at non-lost stage</td><td>trigger</td><td>400 cannot_mark_lost_from_non_lost_stage</td><td>Move to lost stage first</td></tr><tr><td>Status open → lost without reason</td><td>trigger</td><td>400 lost_reason_required</td><td>Provide reason</td></tr><tr><td>Status open → cancelled without reason</td><td>trigger</td><td>400 cancelled_reason_required</td><td>Provide reason</td></tr><tr><td>Past expected_close_date</td><td>trigger</td><td>400 expected_close_in_past</td><td>Use ≥ today</td></tr><tr><td>Probability out of [0, 100]</td><td>DB CHECK</td><td>INSERT/UPDATE fails</td><td>Use valid range</td></tr><tr><td>Pipeline stage position &gt; 50</td><td>DB CHECK</td><td>INSERT fails</td><td>Use ≤ 50</td></tr><tr><td>Amount_minor negative</td><td>DB CHECK</td><td>INSERT/UPDATE fails</td><td>Use ≥ 0</td></tr><tr><td>Currency not ISO-4217</td><td>DB CHECK</td><td>INSERT/UPDATE fails</td><td>Use valid 3-letter code</td></tr><tr><td>memory row commit fails mid-tx</td><td>rollback</td><td>500 audit_failed</td><td>memory_writer health</td></tr><tr><td>Audit row contains unscrubbed PII</td><td>TASK-MEMORY-111 + pre-commit test</td><td>CI fails</td><td>Add PII rule</td></tr><tr><td>Duplicate stage position in same pipeline</td><td>UNIQUE</td><td>INSERT fails</td><td>Adjust positions</td></tr><tr><td>Duplicate stage name in same pipeline</td><td>UNIQUE</td><td>INSERT fails</td><td>Use different name</td></tr><tr><td>Deal account deleted while deal exists</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Cancel deal first</td></tr><tr><td>Stage transition skips multiple positions</td><td>allowed (deals can jump stages)</td><td>None — designed</td><td>None</td></tr><tr><td>Owner subject deleted while deals exist</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Transfer ownership first</td></tr><tr><td>Pipeline deleted while deals reference it</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Migrate deals first</td></tr><tr><td>Idempotency-Key collision different body</td><td>409</td><td>Caller fixes</td><td>None</td></tr><tr><td>won_at retroactive set</td><td>not exposed via API</td><td>None — automatic</td><td>None</td></tr><tr><td>Status transition concurrent (two operators)</td><td>Postgres serialisable</td><td>One wins</td><td>Caller refetches</td></tr><tr><td>seed_default_pipelines called twice</td><td>UNIQUE constraint on name</td><td>Second call fails</td><td>Already seeded; OK</td></tr><tr><td>Stage rename mid-deal</td><td>UPDATE allowed; deal still points by id</td><td>None — designed</td><td>None</td></tr><tr><td>Negative probability_default</td><td>DB CHECK</td><td>INSERT fails</td><td>Use 0-100</td></tr><tr><td>Deal close date in the past at creation</td><td>trigger</td><td>400 expected_close_in_past</td><td>Use future date</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Account → Contact → Deal is the canonical 3-primitive shape</strong> — Salesforce-flavoured; adding 4th primitive is ADR.</li><li><strong>Closed pipeline_shape enum drives TASK-CRM-005 next-action skill heuristics</strong> — each shape has different optimal moves.</li><li><strong>Tenant-defined stages per pipeline + <code>is_open/is_won/is_lost</code> classification</strong> — universal forecast logic + per-tenant flexibility.</li><li><strong>deal_status independent from stage</strong> — universal 4-value enum; stage gates the won/lost transitions.</li><li><strong>FSM at trigger AND handler</strong> — defense in depth; trigger catches direct SQL.</li><li><strong>Money as BIGINT minor + CHAR(3) currency</strong> — task-audit skill rule 11; FLOAT forbidden.</li><li><strong>Append-only history at SQL grant</strong> — <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> on deal_status_history + deal_stage_history.</li><li><strong>Many-to-many contact-account membership</strong> — supports cross-account contacts (CFO of subsidiary parent).</li><li><strong>Contact must have ≥1 membership</strong> — enforced at handler (DB allows zero for transactional bootstrap).</li><li><strong>Probability override semantics</strong> — stage default suggested; per-deal override allowed.</li><li><strong>expected_close_date required + future on <code>is_open</code> stages</strong> — forecast hygiene.</li><li><strong>won_at + actual_close_date automatic via trigger</strong> — operator doesn't need to remember.</li><li><strong>language_code reused from TASK-KB-001</strong> — consistency across modules for VN/EN.</li><li><strong>PII scrubbing applies to email + phone + full_name</strong> — memory audit chain holds hashed forms only.</li><li><strong>seed_default_pipelines invoked by TASK-TEN-001's hook</strong> — every tenant gets 4 default pipelines on provisioning.</li><li><strong>3 separate audit kinds for close (won/lost/cancelled)</strong> — selectivity benefits at query time.</li><li><strong>owner_subject_id required on deal + account</strong> — forecast attribution depends on it.</li><li><strong>No PATCH on current_stage_id</strong> — dedicated stage handler ensures side-effects fire.</li><li><strong>Pipeline + stage UNIQUE constraints</strong> — <code>(pipeline_id, position)</code> + <code>(pipeline_id, name)</code>.</li><li><strong>deals indexes</strong> — owner + pipeline + close_date partial-WHERE-open indexes optimise the hot forecast queries.</li><li><strong><code>account_type</code> placeholder enum at slice 1</strong> — TASK-CRM-003 ships full VN-specific expansion.</li><li><strong>MST stored but unvalidated at slice 1</strong> — TASK-CRM-003 + TASK-CRM-008 ship validation against GDT.</li><li><strong>Bcc-like privacy on contact email</strong> — no UNIQUE constraint; same email may appear in two contacts (e.g. CFO same email different accounts).</li></ul>\n<hr>\n<p><em>End of TASK-CRM-001.</em></p>";
