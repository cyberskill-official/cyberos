window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOC-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The DOC service <strong>MUST</strong> ship the document repository as the foundational storage primitive — S3 Object-Lock Compliance bucket + per-tenant residency pinning + versioned + ACL'd + hash-chained audit log. Each requirement:</p>\n<ol><li><strong>MUST</strong> define the <code>document_metadata</code> table with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>bucket_scope bucket_scope NOT NULL</code>, <code>original_filename TEXT NOT NULL</code>, <code>mime_type TEXT NOT NULL</code>, <code>byte_size BIGINT NOT NULL CHECK (byte_size BETWEEN 1 AND 524288000)</code> (500 MB max), <code>sha256_hex CHAR(64) NOT NULL</code>, <code>status document_status NOT NULL DEFAULT 'draft'</code>, <code>s3_region TEXT NOT NULL</code>, <code>s3_bucket TEXT NOT NULL</code>, <code>s3_key TEXT NOT NULL</code>, <code>kms_key_id TEXT NOT NULL</code>, <code>current_version_id UUID NOT NULL REFERENCES document_versions(id) DEFERRABLE INITIALLY IMMEDIATE</code>, <code>legal_hold BOOLEAN NOT NULL DEFAULT false</code>, <code>retention_until DATE</code> (nullable until status=archived; immutable after), <code>parent_object_id UUID REFERENCES &lt;module&gt;(...)</code> (logical FK soft-typed by bucket_scope), <code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>updated_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>created_by_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>document_versions</code> with: <code>id UUID PRIMARY KEY</code>, <code>document_id UUID NOT NULL REFERENCES document_metadata(id) ON DELETE RESTRICT</code>, <code>tenant_id UUID NOT NULL</code>, <code>version_number INT NOT NULL</code>, <code>s3_key_versioned TEXT NOT NULL</code> (full S3 path with <code>?versionId=&lt;aws-version-id&gt;</code>), <code>sha256_hex CHAR(64) NOT NULL</code>, <code>byte_size BIGINT NOT NULL</code>, <code>mime_type TEXT NOT NULL</code>, <code>uploaded_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>uploaded_by_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>bucket_scope</code> Postgres enum with exactly 7 values (per DEC-282): <code>'hr_contracts'</code>, <code>'crm_contracts'</code>, <code>'esop_grants'</code>, <code>'kyc_docs'</code>, <code>'vendor_contracts'</code>, <code>'policies'</code>, <code>'generic'</code>. Adding an 8th is an ADR. Each scope maps to a distinct S3 bucket + KMS key (per DEC-291).</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>document_status</code> Postgres enum with exactly 4 values (per DEC-288): <code>'draft'</code>, <code>'in_signing'</code>, <code>'fully_signed'</code>, <code>'archived'</code>. Transition <code>fully_signed → archived</code> applies S3 Object-Lock retention (per §1 #7); other transitions leave the object mutable.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> clauses on <code>document_metadata</code> AND <code>document_versions</code>. Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>. Cross-tenant reads return 0 rows; cross-tenant writes fail <code>permission_denied</code>.</li></ol>\n<ol><li><strong>MUST</strong> be <strong>append-only</strong> on <code>document_versions</code> at the SQL-grant layer (per DEC-286 + feature-request-audit skill rule 12). <code>REVOKE UPDATE, DELETE ON document_versions FROM cyberos_app;</code>. Every update creates a new version row + new S3 object.</li></ol>\n<ol><li><strong>MUST</strong> apply S3 Object-Lock Compliance retention on transition to <code>status='archived'</code> (per DEC-280). The retention period is computed from the bucket_scope's policy (per DEC-287):</li></ol>\n<ul><li><code>hr_contracts</code> → 50 years (VN Labour Code Art. 161).</li><li><code>esop_grants</code> → 75 years (long-term employment-equivalent record).</li><li><code>kyc_docs</code> → 10 years (PDPL retention floor).</li><li><code>generic</code>, <code>crm_contracts</code>, <code>vendor_contracts</code>, <code>policies</code> → 10 years (eIDAS + US ESIGN floor). The retention is applied via <code>aws-sdk-s3 put_object_retention</code> with <code>Mode=COMPLIANCE</code> and <code>RetainUntilDate=&lt;computed&gt;</code>. Compliance mode means even the AWS root account cannot delete or shorten the retention.</li></ul>\n<ol><li><strong>MUST</strong> pin each tenant's documents to the residency region per FR-AI-016 (per DEC-281). Lookup function <code>residency::resolve(tenant_id) -&gt; (region, bucket, kms_key)</code>. The mapping:</li></ol>\n<ul><li>VN tenants → region <code>ap-southeast-1</code> + bucket <code>cyberos-doc-vn-1-&lt;scope&gt;</code> + KMS key alias <code>alias/cyberos-doc-vn-1-&lt;scope&gt;</code>.</li><li>EU tenants → region <code>eu-west-1</code> + bucket <code>cyberos-doc-eu-1-&lt;scope&gt;</code> + KMS key alias <code>alias/cyberos-doc-eu-1-&lt;scope&gt;</code>.</li><li>SG tenants → <code>ap-southeast-1</code> + bucket <code>cyberos-doc-sg-1-&lt;scope&gt;</code> (separate from VN bucket).</li><li>US tenants → <code>us-east-1</code> + bucket <code>cyberos-doc-us-1-&lt;scope&gt;</code>. The <code>s3_region</code>, <code>s3_bucket</code>, <code>kms_key_id</code> columns capture the resolution at upload time; downstream operations use these (not the residency resolver) to avoid races during tenant residency changes.</li></ul>\n<ol><li><strong>MUST</strong> support <strong>versioning at every modification</strong> (per DEC-284). Every upload of a new version of an existing document creates:</li></ol>\n<ul><li>A new row in <code>document_versions</code> with monotonic <code>version_number</code>.</li><li>A new S3 object stored at <code>&lt;s3_key&gt;?versionId=&lt;aws-version-id&gt;</code> (S3 Versioning enabled on the bucket).</li><li>An update to <code>document_metadata.current_version_id</code> pointing at the new version.</li><li>A <code>doc.versioned</code> memory audit row. All within one transaction.</li></ul>\n<ol><li><strong>MUST</strong> ship the <strong>hash-chained per-document audit log</strong> at <code>document_audit_log</code> table (per DEC-283). Schema: <code>id BIGSERIAL PRIMARY KEY</code>, <code>document_id UUID NOT NULL REFERENCES document_metadata(id)</code>, <code>tenant_id UUID NOT NULL</code>, <code>event_kind TEXT NOT NULL</code>, <code>event_payload JSONB NOT NULL</code>, <code>prev_hash CHAR(64)</code>, <code>chain_hash CHAR(64) NOT NULL</code>, <code>ts TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>actor_subject_id UUID NOT NULL</code>. The <code>chain_hash</code> is <code>SHA-256(canonical(prev_hash || event_kind || event_payload || ts || actor_subject_id))</code>. A <code>BEFORE INSERT</code> trigger validates <code>prev_hash</code> matches the prior row's <code>chain_hash</code>; tampered chain → reject. The log is queryable via <code>document_audit_chain(doc_id) RETURNS SETOF document_audit_log ORDER BY id ASC</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit the following 8 memory audit row kinds (per DEC-289):</li></ol>\n<ul><li><code>doc.uploaded</code> — first upload (status=draft → version 1 written).</li><li><code>doc.versioned</code> — new version of existing doc.</li><li><code>doc.acl_changed</code> — bucket_scope or RBAC scope change.</li><li><code>doc.signed</code> — signing event from FR-DOC-002/003/004 (placeholder kind at slice 1).</li><li><code>doc.archived</code> — status → archived; Object-Lock applied.</li><li><code>doc.legal_hold_applied</code> — LegalHold ON; CLO + CSO co-sign required (sev-1).</li><li><code>doc.legal_hold_released</code> — LegalHold OFF; same dual-signoff required (sev-1).</li><li><code>doc.access_audited</code> — every read of a document (per DEC-290; sev-2 — forensically relevant).</li></ul>\n<ol><li><strong>MUST</strong> enforce <strong>legal hold dual-signoff</strong> (per DEC-285). <code>POST /v1/doc/documents/{id}/legal-hold</code> accepts body <code>{\"apply\": true | false, \"primary_signer_subject_id\": &lt;uuid&gt;, \"secondary_signer_subject_id\": &lt;uuid&gt;, \"reason\": \"&lt;text&gt;\", \"case_reference\": \"&lt;text&gt;\"}</code>. Validation:</li></ol>\n<ul><li><code>primary_signer</code> MUST have role <code>clo</code> per FR-AUTH-101.</li><li><code>secondary_signer</code> MUST have role <code>cso</code> (security CSO, <code>cseco</code>) per FR-AUTH-101.</li><li><code>primary != secondary</code> (cannot self-co-sign).</li><li><code>case_reference</code> length 5–200 chars.</li><li>On apply: S3 <code>put_object_legal_hold</code> with <code>Status=ON</code>; <code>legal_hold=true</code> in metadata; <code>doc.legal_hold_applied</code> row.</li><li>On release: same shape; <code>Status=OFF</code>; <code>doc.legal_hold_released</code> row.</li></ul>\n<ol><li><strong>MUST</strong> block GDPR/PDPL erasure requests while <code>legal_hold=true</code> (per §1 #12 + GDPR Art. 17). Erasure handler (placeholder; slice 2) calls <code>is_erasure_blocked(doc_id) -&gt; bool</code> from this FR; true → erasure refused with reason <code>legal_hold_active</code>. This FR ships the predicate; the erasure handler ships in FR-DOC-2xx.</li></ol>\n<ol><li><strong>MUST</strong> forbid cross-bucket-scope moves (per DEC-292). A <code>BEFORE UPDATE</code> trigger on <code>document_metadata</code> rejects <code>UPDATE ... SET bucket_scope = &lt;different&gt;</code> with <code>cross_scope_move_forbidden</code>. Once uploaded to <code>hr_contracts</code>, the doc cannot be migrated to <code>generic</code>; the keyspace + retention policy + RBAC scope are bound.</li></ol>\n<ol><li><strong>MUST</strong> lock <code>retention_until</code> post-archive (per DEC-287). A <code>BEFORE UPDATE</code> trigger rejects mutation of <code>retention_until</code> when prior <code>status='archived'</code>. The retention period is set ONCE at archive transition, computed from the scope's policy.</li></ol>\n<ol><li><strong>MUST</strong> expose REST handlers:</li></ol>\n<ul><li><code>POST /v1/doc/documents</code> — initialise document metadata (no body upload yet); returns <code>{document_id, presigned_upload_url, presigned_form_fields}</code> for S3 direct-upload.</li><li><code>POST /v1/doc/documents/{id}/finalize</code> — called after S3 upload completes; validates SHA-256 server-side against S3 object's etag; creates version 1.</li><li><code>POST /v1/doc/documents/{id}/versions</code> — same flow for new versions.</li><li><code>GET /v1/doc/documents/{id}</code> — return metadata + presigned download URL.</li><li><code>GET /v1/doc/documents?bucket_scope=&lt;&gt;&amp;status=&lt;&gt;&amp;parent_object_id=&lt;&gt;</code> — list with cursor pagination.</li><li><code>PATCH /v1/doc/documents/{id}</code> — update non-scope-bound fields (original_filename, parent_object_id); rejects bucket_scope changes.</li><li><code>POST /v1/doc/documents/{id}/archive</code> — transition to archived; applies Object-Lock retention.</li><li><code>POST /v1/doc/documents/{id}/legal-hold</code> — dual-signoff legal hold workflow.</li></ul>\n<ol><li><strong>MUST</strong> verify SHA-256 integrity at every upload (per §1 #1 + S3 ETag check at finalize). Client computes locally + sends as <code>x-amz-content-sha256</code>; server validates against actual byte-stream + against S3 ETag. Mismatch → 409 <code>integrity_mismatch</code>.</li></ol>\n<ol><li><strong>MUST</strong> complete create + get + list handlers in ≤ 100 ms p95 (excluding the S3 round-trip; presigned URL generation is local). <code>documents_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> support idempotent creation via <code>Idempotency-Key</code> header (same semantics as FR-AUTH-002 §1 #6).</li></ol>\n<ol><li><strong>MUST</strong> scope RBAC permissions per bucket_scope (per FR-AUTH-101): caller MUST have <code>Resource::DocDocument + Action::Write</code> for the specific scope. Slice 1 ships scope as a single resource; per-scope refinement (HR vs CRM scope override) lands in FR-DOC-2xx via scope_grants.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>doc.{upload,version,archive,download,legal_hold,acl_change}</code> with attributes: <code>tenant_id</code>, <code>document_id</code>, <code>bucket_scope</code>, <code>status</code>, <code>outcome</code> (success | invalid_scope | residency_mismatch | integrity_mismatch | legal_hold_blocked | not_found | permission_denied | object_lock_apply_failed | etag_mismatch).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>doc_upload_total{outcome, bucket_scope}</code> (counter).</li><li><code>doc_archive_total{outcome, retention_years}</code> (counter).</li><li><code>doc_legal_hold_total{outcome, apply}</code> (counter; <code>apply</code> ∈ {true, false}).</li><li><code>doc_access_total{tenant_id, bucket_scope}</code> (counter; downloads emit sev-2 audit).</li><li><code>doc_bytes_stored{tenant_id, bucket_scope}</code> (gauge — periodic compute via S3 inventory; eventual consistency).</li><li><code>doc_count{tenant_id, bucket_scope, status}</code> (gauge).</li></ul>\n<ol><li><strong>MUST</strong> ensure access (GET /download) emits a <code>doc.access_audited</code> memory row at sev-2 per access (per DEC-290). The row carries <code>{document_id, accessed_by_subject_id_hash16, purpose, requesting_ip_hash16, ts_ns}</code>. <code>purpose</code> is supplied by the caller as a required field (<code>?purpose=&lt;text&gt;</code> query param, 1-200 chars); absent → 400.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>bucket_retention_policy(scope bucket_scope) RETURNS INT</code> SQL function returning the retention years per scope (50 for hr_contracts, 75 for esop_grants, 10 for others). Deterministic; same input → same output.</li></ol>\n<ol><li><strong>MUST</strong> validate residency match at upload: if the caller's tenant residency = <code>vn-1</code> and the resolved S3 bucket is <code>eu-1</code>, reject with 400 <code>residency_mismatch</code>. The check is at the handler boundary; the residency resolver should always return the correct match, but the validation is defence-in-depth.</li></ol>\n<ol><li><strong>MUST</strong> support cursor pagination on list with <code>?cursor=&lt;opaque&gt;&amp;limit=&lt;int&gt;</code> (max 200, default 50).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why S3 Object-Lock Compliance mode (DEC-280, §1 #7)?</strong> \"Retention is non-negotiable\" is the legal guarantee. Object-Lock Compliance mode means even AWS root credentials cannot delete or shorten the retention. Governance mode (the alternative) allows root override — defeats the guarantee. Compliance mode is the only path to eIDAS Art. 32 conformance for long-term electronic-signature preservation.</p>\n<p><strong>Why per-tenant residency pinning (DEC-281)?</strong> Three reasons converge: (1) PDPL + Decree 53/2022 require VN-citizen personal data to remain in-country; (2) GDPR + Schrems II require EU-citizen data to remain in qualified jurisdictions; (3) US ESIGN doesn't restrict location but auditability is regional. The resolver (joined with FR-AI-016) picks the bucket + KMS at upload time and records the resolution on the row — so future operations don't have to re-resolve (and can't race against tenant residency changes).</p>\n<p><strong>Why closed bucket_scope enum (DEC-282, §1 #3)?</strong> Scope drives the RBAC role, the retention period, the KMS keyspace. Free-form scopes would proliferate (\"<code>hr-contracts-2026</code>\", \"<code>hr_contracts</code>\", \"<code>hrcontracts</code>\") and break the downstream cascade. 7 values cover the major buckets; adding an 8th is an ADR that forces consideration of: (a) what's the retention period? (b) which KMS keyspace? (c) which RBAC role gates it? — the questions that prevent silent over-permissioning.</p>\n<p><strong>Why hash-chained per-document audit log (DEC-283, §1 #10)?</strong> Signed documents are court-admissible; the chain-of-custody must be verifiable. Hash chaining (<code>chain_hash = SHA-256(prev_hash || event)</code>) makes tamper detectable: if any row is modified or removed, every subsequent <code>chain_hash</code> breaks. The chain replays end-to-end via <code>document_audit_chain(doc_id)</code>. The trigger validates at INSERT — operators inserting backdated rows cannot fool the chain.</p>\n<p><strong>Why immutable versions (DEC-284, §1 #6, §1 #9)?</strong> Signatures bind to specific bytes; \"the signed document\" is <code>v2</code>, not \"the current state of the doc\". Editing in place would mean the signed bytes no longer match the stored bytes — signature verification fails. Every save creates a new version with its own SHA-256; the signature ties to a specific version's hash.</p>\n<p><strong>Why retention varies by scope (DEC-287, §1 #7)?</strong> Different document types have different legal retention floors. VN Labour Code Art. 161 requires 50-year retention for employment contracts. US ERISA-equivalent practices retain ESOP records for 75 years. Generic + most commercial contracts: 10 years (eIDAS + ESIGN floor). The bucket_retention_policy function encodes the lookup; the migration's CHECK constraint enforces <code>retention_until &gt;= now() + bucket_retention_policy(scope) years</code>.</p>\n<p><strong>Why dual-signoff for legal hold (DEC-285, §1 #12)?</strong> Legal hold is \"this document cannot be deleted, even if retention expires, because litigation is anticipated\". Misuse (operator applies hold to suppress a document indefinitely) is a real risk. Dual-signoff (CLO + CSO) creates a \"two trusted operators agree\" gate; the memory audit row at sev-1 alerts other operators. No auto-apply; never single-signer.</p>\n<p><strong>Why GDPR/PDPL erasure blocked under hold (§1 #13)?</strong> Legal hold supersedes erasure rights under most jurisdictions — if there's ongoing litigation requiring document preservation, the regulator wouldn't honour erasure. The predicate <code>is_erasure_blocked</code> returns true when <code>legal_hold=true</code>; the erasure handler refuses with a reason that makes it visible (so the data subject knows why their request was deferred).</p>\n<p><strong>Why cross-bucket-scope moves forbidden (DEC-292, §1 #14)?</strong> Scope binds retention + KMS + RBAC. Moving an HR contract to generic would (a) shorten retention from 50y to 10y, (b) switch KMS keyspace (encryption boundary breaks), (c) widen RBAC access. Forbidding moves at the trigger forces operators to re-upload under the new scope — making the boundary-cross visible.</p>\n<p><strong>Why retention_until immutable post-archive (DEC-287, §1 #15)?</strong> Object-Lock Compliance binds retention to the date the object was put-locked; the Postgres metadata mirrors that. Allowing UPDATE on <code>retention_until</code> post-archive would make the metadata diverge from S3 truth — operators would believe retention was shortened when S3 won't allow deletion. Lock the metadata to match S3.</p>\n<p><strong>Why presigned URLs for upload (§1 #16)?</strong> S3 direct-upload from the client (browser, mobile) avoids streaming bytes through our service. Presigned URLs include the bucket, key, content-type, and a signature; client uploads directly. Our service emits the URL + form fields + records pending metadata; on <code>finalize</code>, we verify the SHA-256 + ETag.</p>\n<p><strong>Why finalize step + ETag check (§1 #17)?</strong> Direct-upload presigned URLs don't prevent a client from uploading any bytes (within size + type limits). The finalize step compares (a) the client-supplied SHA-256 against the actually-uploaded ETag (which S3 computes from the upload); (b) the byte count. Mismatch → object is orphaned (S3 lifecycle cleanup after 24h) + 409 error.</p>\n<p><strong>Why access emits sev-2 audit (DEC-290, §1 #23, §1 #11)?</strong> Document access is a forensically-relevant event — \"did Person A read Person B's contract on Date X?\". Sev-2 means it goes through FR-OBS-007's operator digest. Routine high-volume access (e.g. operations dashboard auto-fetching every contract) would create noise; the <code>purpose</code> requirement filters trivial cases (caller must supply a purpose; routine systems use a stable purpose code).</p>\n<p><strong>Why <code>parent_object_id</code> is a soft-typed FK (§1 #1)?</strong> The doc may belong to an HR contract (FR-HR-002), a CRM deal (FR-CRM-004), an ESOP grant (FR-ESOP-001), a vendor agreement (FR-OPS-vendor-2xx). Cross-service FKs are complex; soft-typing on <code>parent_object_id</code> + <code>bucket_scope</code> together identifies the owner, queried via the appropriate module's API.</p>\n<p><strong>Why max 500 MB per document (§1 #1)?</strong> Above this size the upload latency makes the UX intolerable; the use cases (PDF contracts, scanned KYC docs, signed images) fit well below 500 MB. Larger media goes to a different path (FR-MEDIA-* — out of scope). The CHECK constraint prevents the table from accidentally accepting GB-sized uploads.</p>\n<p><strong>Why <code>s3_key</code> includes the document UUID (§1 #1, implicit in s3 upload)?</strong> Stable + collision-free + opaque. The key shape is <code>&lt;tenant_uuid&gt;/&lt;scope&gt;/&lt;doc_uuid&gt;</code> (e.g. <code>5e8f1d2a-.../hr_contracts/01HG7V8...</code>). Listing operations are filtered server-side by tenant_uuid prefix; collisions are impossible (UUID v4).</p>\n<p><strong>Why explicit <code>kms_key_id</code> on the row (§1 #1)?</strong> Encryption boundary is intrinsic to the scope. Recording the KMS key id at upload prevents operators from accidentally re-encrypting under a different key (which would invalidate prior signed-document references). The key id is set ONCE at upload; never changes.</p>\n<p><strong>Why versioned audit log + S3 versioning together?</strong> Defense in depth — the Postgres audit log captures the operation; the S3 versioning preserves the bytes. If one fails, the other catches it. The hash chain makes the audit log's integrity verifiable; Object-Lock makes the S3 side immutable.</p>\n<p><strong>Why no DELETE handler (§1 #16)?</strong> Documents enter <code>archived</code> state and are retained for the retention period; they never get deleted. The DELETE operation simply doesn't exist in the API; legal-hold + Object-Lock + SQL grant all combine to make deletion impossible.</p>\n<p><strong>Why <code>purpose</code> required on GET (§1 #23)?</strong> Forces the caller to declare why they're reading the document. Operators see \"Person A read Person B's contract for purpose: kyc_review\" — actionable; without purpose, the audit row would say \"Person A read Person B's contract\" — context-free.</p>\n<p><strong>Why slice 1 ships only the storage primitive (not signing, not lifecycle)?</strong> Storage is the foundation; signing (FR-DOC-002/003/004) and lifecycle (FR-DOC-007) build on it. Splitting concerns keeps each FR focused. The storage primitive alone is useful: it can hold HR contracts, CRM contracts, vendor agreements as draft/in_signing/archived documents — signing comes later but the storage works without it.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0001 — document_metadata + document_versions</h3>\n<pre><code class=\"language-sql\">-- services/doc/migrations/0001_document_metadata.sql\n\nBEGIN;\n\nCREATE TYPE bucket_scope AS ENUM (\n    'hr_contracts', 'crm_contracts', 'esop_grants', 'kyc_docs',\n    'vendor_contracts', 'policies', 'generic'\n);\n\nCREATE TYPE document_status AS ENUM ('draft', 'in_signing', 'fully_signed', 'archived');\n\nCREATE TABLE document_versions (\n    id                     UUID         PRIMARY KEY,\n    document_id            UUID         NOT NULL,\n    tenant_id              UUID         NOT NULL,\n    version_number         INT          NOT NULL,\n    s3_key_versioned       TEXT         NOT NULL,\n    sha256_hex             CHAR(64)     NOT NULL CHECK (sha256_hex ~ '^[0-9a-f]{64}$'),\n    byte_size              BIGINT       NOT NULL CHECK (byte_size BETWEEN 1 AND 524288000),\n    mime_type              TEXT         NOT NULL,\n    uploaded_at            TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    uploaded_by_subject_id UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nCREATE TABLE document_metadata (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    bucket_scope           bucket_scope NOT NULL,\n    original_filename      TEXT         NOT NULL CHECK (length(original_filename) BETWEEN 1 AND 255),\n    mime_type              TEXT         NOT NULL,\n    byte_size              BIGINT       NOT NULL CHECK (byte_size BETWEEN 1 AND 524288000),\n    sha256_hex             CHAR(64)     NOT NULL CHECK (sha256_hex ~ '^[0-9a-f]{64}$'),\n    status                 document_status NOT NULL DEFAULT 'draft',\n    s3_region              TEXT         NOT NULL,\n    s3_bucket              TEXT         NOT NULL,\n    s3_key                 TEXT         NOT NULL,\n    kms_key_id             TEXT         NOT NULL,\n    current_version_id     UUID         NOT NULL REFERENCES document_versions(id) DEFERRABLE INITIALLY IMMEDIATE,\n    legal_hold             BOOLEAN      NOT NULL DEFAULT false,\n    retention_until        DATE,\n    parent_object_id       UUID,                                  -- soft FK per bucket_scope; logical only\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    updated_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nALTER TABLE document_versions ADD CONSTRAINT document_versions_doc_fk\n    FOREIGN KEY (document_id) REFERENCES document_metadata(id) ON DELETE RESTRICT;\n\nCREATE INDEX document_metadata_tenant_scope_idx ON document_metadata (tenant_id, bucket_scope, status);\nCREATE INDEX document_metadata_tenant_parent_idx ON document_metadata (tenant_id, parent_object_id) WHERE parent_object_id IS NOT NULL;\nCREATE INDEX document_versions_doc_version_idx ON document_versions (document_id, version_number DESC);\n\nALTER TABLE document_metadata ENABLE ROW LEVEL SECURITY;\nALTER TABLE document_versions ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY document_metadata_tenant_isolation ON document_metadata\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCREATE POLICY document_versions_tenant_isolation ON document_versions\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Append-only on versions (DEC-286)\nREVOKE UPDATE, DELETE ON document_versions FROM cyberos_app;\n\n-- Retention policy lookup\nCREATE OR REPLACE FUNCTION bucket_retention_policy(scope bucket_scope) RETURNS INT AS $$\nBEGIN\n    RETURN CASE scope\n        WHEN 'hr_contracts'      THEN 50\n        WHEN 'esop_grants'       THEN 75\n        WHEN 'kyc_docs'          THEN 10\n        WHEN 'crm_contracts'     THEN 10\n        WHEN 'vendor_contracts'  THEN 10\n        WHEN 'policies'          THEN 10\n        WHEN 'generic'           THEN 10\n    END;\nEND;\n$$ LANGUAGE plpgsql IMMUTABLE;\n\n-- Cross-scope move rejection (DEC-292)\nCREATE OR REPLACE FUNCTION enforce_no_cross_scope_move() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.bucket_scope IS DISTINCT FROM OLD.bucket_scope THEN\n        RAISE EXCEPTION 'cross_scope_move_forbidden' USING ERRCODE = 'P0030';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_metadata_no_cross_scope BEFORE UPDATE ON document_metadata\n    FOR EACH ROW EXECUTE FUNCTION enforce_no_cross_scope_move();\n\n-- Retention immutability post-archive (DEC-287)\nCREATE OR REPLACE FUNCTION enforce_retention_immutable_post_archive() RETURNS TRIGGER AS $$\nBEGIN\n    IF OLD.status = 'archived' AND NEW.retention_until IS DISTINCT FROM OLD.retention_until THEN\n        RAISE EXCEPTION 'retention_immutable_post_archive' USING ERRCODE = 'P0031';\n    END IF;\n    NEW.updated_at := now();\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_metadata_retention_lock BEFORE UPDATE ON document_metadata\n    FOR EACH ROW EXECUTE FUNCTION enforce_retention_immutable_post_archive();\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0002 — hash-chained audit log</h3>\n<pre><code class=\"language-sql\">-- services/doc/migrations/0002_document_audit_log.sql\n\nBEGIN;\n\nCREATE TABLE document_audit_log (\n    id                     BIGSERIAL    PRIMARY KEY,\n    document_id            UUID         NOT NULL REFERENCES document_metadata(id) ON DELETE RESTRICT,\n    tenant_id              UUID         NOT NULL,\n    event_kind             TEXT         NOT NULL,\n    event_payload          JSONB        NOT NULL,\n    prev_hash              CHAR(64),\n    chain_hash             CHAR(64)     NOT NULL CHECK (chain_hash ~ '^[0-9a-f]{64}$'),\n    ts                     TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    actor_subject_id       UUID         NOT NULL REFERENCES auth.subjects(id)\n);\n\nCREATE INDEX doc_audit_log_doc_idx ON document_audit_log (document_id, id ASC);\n\nALTER TABLE document_audit_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY doc_audit_log_tenant_isolation ON document_audit_log\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON document_audit_log FROM cyberos_app;\n\n-- Chain integrity: NEW.prev_hash MUST equal previous row's chain_hash (DEC-283)\nCREATE OR REPLACE FUNCTION enforce_audit_chain_integrity() RETURNS TRIGGER AS $$\nDECLARE expected_prev CHAR(64);\nBEGIN\n    SELECT chain_hash INTO expected_prev FROM document_audit_log\n        WHERE document_id = NEW.document_id ORDER BY id DESC LIMIT 1;\n    IF expected_prev IS NULL AND NEW.prev_hash IS NOT NULL THEN\n        RAISE EXCEPTION 'audit_chain_bootstrap_violation' USING ERRCODE = 'P0040';\n    END IF;\n    IF expected_prev IS NOT NULL AND NEW.prev_hash IS DISTINCT FROM expected_prev THEN\n        RAISE EXCEPTION 'audit_chain_break' USING ERRCODE = 'P0041';\n    END IF;\n    -- chain_hash itself is computed by the application; trigger validates the link\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_doc_audit_chain BEFORE INSERT ON document_audit_log\n    FOR EACH ROW EXECUTE FUNCTION enforce_audit_chain_integrity();\n\n-- Chain replayer\nCREATE OR REPLACE FUNCTION document_audit_chain(p_doc_id UUID)\nRETURNS SETOF document_audit_log AS $$\n    SELECT * FROM document_audit_log WHERE document_id = p_doc_id ORDER BY id ASC\n$$ LANGUAGE sql STABLE;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Rust types</h3>\n<pre><code class=\"language-rust\">// services/doc/src/types.rs\nuse chrono::{DateTime, NaiveDate, Utc};\nuse serde::{Deserialize, Serialize};\nuse sqlx::{FromRow, Type};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"bucket_scope\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum BucketScope {\n    HrContracts, CrmContracts, EsopGrants, KycDocs,\n    VendorContracts, Policies, Generic,\n}\n\nimpl BucketScope {\n    pub const ALL: &amp;'static [BucketScope] = &amp;[\n        BucketScope::HrContracts, BucketScope::CrmContracts, BucketScope::EsopGrants,\n        BucketScope::KycDocs, BucketScope::VendorContracts, BucketScope::Policies, BucketScope::Generic,\n    ];\n\n    pub fn retention_years(self) -&gt; u32 {\n        match self {\n            BucketScope::HrContracts =&gt; 50,\n            BucketScope::EsopGrants =&gt; 75,\n            _ =&gt; 10,\n        }\n    }\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"document_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum DocumentStatus { Draft, InSigning, FullySigned, Archived }\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct DocumentMetadata {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub bucket_scope: BucketScope,\n    pub original_filename: String,\n    pub mime_type: String,\n    pub byte_size: i64,\n    pub sha256_hex: String,\n    pub status: DocumentStatus,\n    pub s3_region: String,\n    pub s3_bucket: String,\n    pub s3_key: String,\n    pub kms_key_id: String,\n    pub current_version_id: Uuid,\n    pub legal_hold: bool,\n    pub retention_until: Option&lt;NaiveDate&gt;,\n    pub parent_object_id: Option&lt;Uuid&gt;,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub updated_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}</code></pre>\n<h3>3.4 — Residency resolver</h3>\n<pre><code class=\"language-rust\">// services/doc/src/residency.rs\nuse uuid::Uuid;\nuse crate::types::BucketScope;\n\n#[derive(Debug, Clone)]\npub struct StorageBinding {\n    pub region: String,\n    pub bucket: String,\n    pub kms_key_id: String,\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum ResidencyError {\n    #[error(\"unknown_tenant_residency: {0}\")]\n    Unknown(Uuid),\n    #[error(\"residency_disabled_for_scope: {scope:?} in {residency}\")]\n    ScopeDisabled { scope: BucketScope, residency: String },\n}\n\n/// Resolve tenant's residency tag and produce the (region, bucket, KMS key) triple\n/// for the given bucket_scope. Consumes FR-AI-016's residency policy.\npub async fn resolve(tenant_id: Uuid, scope: BucketScope, db: &amp;sqlx::PgPool) -&gt; Result&lt;StorageBinding, ResidencyError&gt; {\n    let residency: String = sqlx::query_scalar(\"SELECT residency FROM tenant_residency WHERE tenant_id = $1\")\n        .bind(tenant_id).fetch_optional(db).await.unwrap().ok_or(ResidencyError::Unknown(tenant_id))?;\n\n    let (region, bucket_prefix) = match residency.as_str() {\n        \"vn-1\" =&gt; (\"ap-southeast-1\", \"cyberos-doc-vn-1\"),\n        \"sg-1\" =&gt; (\"ap-southeast-1\", \"cyberos-doc-sg-1\"),\n        \"eu-1\" =&gt; (\"eu-west-1\",      \"cyberos-doc-eu-1\"),\n        \"us-1\" =&gt; (\"us-east-1\",      \"cyberos-doc-us-1\"),\n        _ =&gt; return Err(ResidencyError::Unknown(tenant_id)),\n    };\n\n    let scope_str: &amp;str = serde_plain::to_string(&amp;scope).unwrap_or_default();\n    let bucket = format!(\"{bucket_prefix}-{scope_str}\");\n    let kms_key_id = format!(\"alias/{bucket}\");\n\n    Ok(StorageBinding { region: region.into(), bucket, kms_key_id })\n}</code></pre>\n<h3>3.5 — Object Lock application</h3>\n<pre><code class=\"language-rust\">// services/doc/src/s3/object_lock.rs\nuse aws_sdk_s3::types::{ObjectLockMode, ObjectLockLegalHoldStatus};\nuse chrono::{Duration, Utc};\nuse crate::types::{BucketScope, DocumentMetadata};\n\npub async fn apply_archive_retention(\n    s3: &amp;aws_sdk_s3::Client,\n    metadata: &amp;DocumentMetadata,\n) -&gt; Result&lt;chrono::NaiveDate, anyhow::Error&gt; {\n    let years = metadata.bucket_scope.retention_years();\n    let until = (Utc::now() + Duration::days(365 * years as i64 + (years / 4) as i64)).naive_utc().date();\n    s3.put_object_retention()\n        .bucket(&amp;metadata.s3_bucket)\n        .key(&amp;metadata.s3_key)\n        .version_id(metadata.current_version_id.to_string())   // pin to current version\n        .retention(aws_sdk_s3::types::ObjectLockRetention::builder()\n            .mode(ObjectLockMode::Compliance)\n            .retain_until_date(aws_smithy_types::DateTime::from_secs(until.and_hms_opt(0,0,0).unwrap().and_utc().timestamp()))\n            .build())\n        .send().await?;\n    Ok(until)\n}\n\npub async fn apply_legal_hold(\n    s3: &amp;aws_sdk_s3::Client,\n    metadata: &amp;DocumentMetadata,\n    on: bool,\n) -&gt; Result&lt;(), anyhow::Error&gt; {\n    let status = if on { ObjectLockLegalHoldStatus::On } else { ObjectLockLegalHoldStatus::Off };\n    s3.put_object_legal_hold()\n        .bucket(&amp;metadata.s3_bucket)\n        .key(&amp;metadata.s3_key)\n        .legal_hold(aws_sdk_s3::types::ObjectLockLegalHold::builder().status(status).build())\n        .send().await?;\n    Ok(())\n}</code></pre>\n<h3>3.6 — Legal hold handler</h3>\n<pre><code class=\"language-rust\">// services/doc/src/handlers/legal_hold.rs\nuse axum::{Json, extract::{Path, State}, http::StatusCode};\nuse cyberos_auth::rbac::{Resource, Action, Role};\nuse crate::audit::doc_events;\n\n#[derive(Deserialize)]\npub struct LegalHoldRequest {\n    pub apply: bool,\n    pub primary_signer_subject_id: Uuid,\n    pub secondary_signer_subject_id: Uuid,\n    pub reason: String,\n    pub case_reference: String,\n}\n\npub async fn legal_hold(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Path(doc_id): Path&lt;Uuid&gt;,\n    Json(req): Json&lt;LegalHoldRequest&gt;,\n) -&gt; Result&lt;StatusCode, ApiError&gt; {\n    // Caller permission\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::DocDocument, Action::Admin)?;\n\n    if req.primary_signer_subject_id == req.secondary_signer_subject_id {\n        return Err(ApiError::SelfCoSign);\n    }\n    if req.case_reference.len() &lt; 5 || req.case_reference.len() &gt; 200 {\n        return Err(ApiError::CaseReferenceInvalid);\n    }\n\n    // Validate signers' roles\n    let primary_roles = state.repo.subject_roles(req.primary_signer_subject_id).await?;\n    if !primary_roles.contains(&amp;Role::Clo) {\n        return Err(ApiError::SignerRoleMismatch { required: Role::Clo, found: primary_roles });\n    }\n    let secondary_roles = state.repo.subject_roles(req.secondary_signer_subject_id).await?;\n    if !secondary_roles.contains(&amp;Role::Cseco) {\n        return Err(ApiError::SignerRoleMismatch { required: Role::Cseco, found: secondary_roles });\n    }\n\n    let mut tx = state.db.begin().await?;\n    let metadata = state.repo.get_metadata(doc_id, &amp;mut tx).await?;\n\n    // Apply at S3\n    crate::s3::object_lock::apply_legal_hold(&amp;state.s3, &amp;metadata, req.apply).await?;\n\n    // Update metadata\n    sqlx::query(\"UPDATE document_metadata SET legal_hold = $2 WHERE id = $1\")\n        .bind(doc_id).bind(req.apply).execute(&amp;mut *tx).await?;\n\n    // Append audit log\n    if req.apply {\n        doc_events::emit_legal_hold_applied(&amp;mut tx, &amp;metadata, &amp;req, claims.subject_id()).await?;\n    } else {\n        doc_events::emit_legal_hold_released(&amp;mut tx, &amp;metadata, &amp;req, claims.subject_id()).await?;\n    }\n\n    tx.commit().await?;\n    Ok(StatusCode::OK)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>BucketScope enum closed at 7</strong> — <code>BucketScope::ALL.len() == 7</code>; Postgres enum has exactly 7 labels.</li><li><strong>DocumentStatus enum closed at 4</strong> — same shape.</li><li><strong>RLS isolates by tenant</strong> — query as tenant-A returns 0 docs of tenant-B.</li><li><strong>POST + finalize creates document + version 1</strong> — happy path returns metadata + version row; <code>doc.uploaded</code> memory row emitted.</li><li><strong>Cross-scope move rejected</strong> — <code>UPDATE document_metadata SET bucket_scope = 'generic' WHERE id = $1</code> (originally hr_contracts) → trigger <code>cross_scope_move_forbidden</code>.</li><li><strong>UPDATE document_versions blocked</strong> — <code>UPDATE document_versions ...</code> as cyberos_app → permission denied.</li><li><strong>DELETE document_versions blocked</strong> — same.</li><li><strong>Residency pin: VN tenant uploads to vn-1 bucket</strong> — resolver returns <code>s3_region='ap-southeast-1'</code>, <code>bucket='cyberos-doc-vn-1-hr_contracts'</code>.</li><li><strong>Residency mismatch caught</strong> — handler with wrong region → 400 <code>residency_mismatch</code>.</li><li><strong>Object-Lock retention applied on archive</strong> — POST /archive → S3 mock receives <code>PutObjectRetention</code> with <code>Mode=COMPLIANCE</code> and <code>RetainUntilDate</code> matching scope policy.</li><li><strong>Retention immutable post-archive</strong> — UPDATE retention_until after status=archived → trigger <code>retention_immutable_post_archive</code>.</li><li><strong>Retention by scope: hr_contracts=50y</strong> — <code>SELECT bucket_retention_policy('hr_contracts')</code> returns 50.</li><li><strong>Retention by scope: esop_grants=75y</strong> — returns 75.</li><li><strong>Retention by scope: generic=10y</strong> — returns 10.</li><li><strong>Legal hold dual-signoff</strong> — CLO + CSO co-sign → 200; CLO alone → 403 <code>signer_role_mismatch</code>.</li><li><strong>Legal hold self-co-sign rejected</strong> — primary_signer == secondary_signer → 400 <code>self_co_sign</code>.</li><li><strong>GDPR erasure blocked under hold</strong> — <code>is_erasure_blocked(doc_id)</code> returns true; erasure handler refuses with <code>legal_hold_active</code>.</li><li><strong>doc.access_audited emitted on GET /download</strong> — sev-2 memory row with purpose; missing purpose → 400.</li><li><strong>Audit log chain integrity</strong> — INSERT with mismatched <code>prev_hash</code> → trigger <code>audit_chain_break</code>.</li><li><strong>Audit log replay end-to-end</strong> — <code>document_audit_chain(doc_id)</code> returns rows in order; SHA-256 chain verified end-to-end.</li><li><strong>Idempotent create</strong> — same Idempotency-Key + same body → same document.</li><li><strong>SHA-256 mismatch on finalize</strong> — client-supplied vs S3-actual ETag differ → 409 <code>integrity_mismatch</code>.</li><li><strong>OTel span <code>doc.upload</code> emitted</strong> — with <code>outcome=success</code>.</li><li><strong>Counter <code>doc_upload_total{outcome=success, bucket_scope=hr_contracts}</code> increments</strong> — per upload.</li><li><strong>Counter <code>doc_legal_hold_total{outcome=success, apply=true}</code> increments</strong> — per hold apply.</li><li><strong>Counter <code>doc_access_total{tenant_id, bucket_scope}</code> increments</strong> — per GET.</li><li><strong>Subject FK ON DELETE RESTRICT</strong> — DELETE auth.subjects fails if documents reference.</li><li><strong>Perf budget &lt; 100ms p95</strong> — <code>documents_perf_test</code> 1000 iterations.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/doc/tests/bucket_scope_enum_test.rs\n#[test]\nfn bucket_scope_has_exactly_7_values() {\n    assert_eq!(BucketScope::ALL.len(), 7);\n}\n\n#[sqlx::test]\nasync fn pg_bucket_scope_enum_matches_rust(pool: sqlx::PgPool) {\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::bucket_scope))::text ORDER BY 1\"\n    ).fetch_all(&amp;pool).await.unwrap();\n    let expected: Vec&lt;String&gt; = vec![\n        \"crm_contracts\",\"esop_grants\",\"generic\",\"hr_contracts\",\"kyc_docs\",\"policies\",\"vendor_contracts\"\n    ].into_iter().map(String::from).collect();\n    assert_eq!(labels, expected);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/doc/tests/retention_period_test.rs\n#[sqlx::test]\nasync fn retention_periods_per_scope(pool: sqlx::PgPool) {\n    let cases = [\n        (\"hr_contracts\", 50), (\"esop_grants\", 75), (\"kyc_docs\", 10),\n        (\"crm_contracts\", 10), (\"vendor_contracts\", 10), (\"policies\", 10), (\"generic\", 10),\n    ];\n    for (scope, expected) in cases {\n        let got: i32 = sqlx::query_scalar(&amp;format!(\"SELECT bucket_retention_policy('{scope}')\"))\n            .fetch_one(&amp;pool).await.unwrap();\n        assert_eq!(got, expected, \"scope = {scope}\");\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/doc/tests/legal_hold_dual_signoff_test.rs\n#[tokio::test]\nasync fn single_signer_rejected(ctx: TestCtx) {\n    let doc = ctx.create_doc(BucketScope::HrContracts).await;\n    let clo = ctx.subject_with_role(Role::Clo).await;\n    let other = ctx.subject_with_role(Role::Cfo).await;   // not cseco\n    let err = ctx.post_legal_hold(doc.id, json!({\n        \"apply\": true,\n        \"primary_signer_subject_id\": clo,\n        \"secondary_signer_subject_id\": other,\n        \"reason\":\"litigation x\",\n        \"case_reference\":\"CASE-2026-Q3-001\"\n    })).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"signer_role_mismatch\"));\n}\n\n#[tokio::test]\nasync fn clo_plus_cseco_accepted(ctx: TestCtx) {\n    let doc = ctx.create_doc(BucketScope::HrContracts).await;\n    let clo = ctx.subject_with_role(Role::Clo).await;\n    let cseco = ctx.subject_with_role(Role::Cseco).await;\n    let resp = ctx.post_legal_hold(doc.id, json!({\n        \"apply\": true,\n        \"primary_signer_subject_id\": clo,\n        \"secondary_signer_subject_id\": cseco,\n        \"reason\":\"litigation x\",\n        \"case_reference\":\"CASE-2026-Q3-001\"\n    })).await.unwrap();\n    assert_eq!(resp.status(), 200);\n    let rows = ctx.memory_audit_rows(\"doc.legal_hold_applied\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"severity\"], \"sev-1\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/doc/tests/cross_scope_move_rejected_test.rs\n#[sqlx::test]\nasync fn cross_scope_update_rejected(pool: sqlx::PgPool) {\n    let doc = seed_doc_with_scope(&amp;pool, BucketScope::HrContracts).await;\n    let err = sqlx::query(\"UPDATE document_metadata SET bucket_scope = 'generic'::bucket_scope WHERE id = $1\")\n        .bind(doc).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"cross_scope_move_forbidden\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/doc/tests/audit_log_chain_test.rs\n#[sqlx::test]\nasync fn chain_integrity_enforced(pool: sqlx::PgPool) {\n    let doc_id = seed_doc(&amp;pool).await;\n    let row1 = insert_audit_event(&amp;pool, doc_id, \"doc.uploaded\", None).await;\n    // Attempt to insert with mismatched prev_hash\n    let err = sqlx::query(\"INSERT INTO document_audit_log (document_id, tenant_id, event_kind, event_payload, prev_hash, chain_hash, actor_subject_id) VALUES ($1, $2, 'doc.versioned', '{}'::jsonb, $3, $4, $5)\")\n        .bind(doc_id).bind(test_tenant()).bind(\"bad_prev_hash_64chars_00000000000000000000000000000000000000000000\")\n        .bind(\"any_chain_hash_64chars_0000000000000000000000000000000000000000\")\n        .bind(test_subject())\n        .execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"audit_chain_break\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton. The 8 memory row builders in <code>audit/doc_events.rs</code> follow the canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>FR-AUTH-101</strong> — RBAC; <code>Resource::DocDocument + Action::Write/Read/Admin</code>; signer-role validation against the closed enum.</li></ul>\n<p><strong>Downstream (6 placeholders):</strong></p>\n<ul><li><strong>FR-DOC-002</strong> — eIDAS QTSP partner integration (EU residency).</li><li><strong>FR-DOC-003</strong> — AATL CA integration (US/non-EU enterprise).</li><li><strong>FR-DOC-004</strong> — VNeID + VN CA chain (VN tenants).</li><li><strong>FR-DOC-005</strong> — multi-party signing workflow.</li><li><strong>FR-DOC-007</strong> — lifecycle metadata (parties, dates, renewal).</li><li><strong>FR-DOC-010</strong> — DocuSign / Adobe Sign / HelloSign import with LTV preservation.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>FR-AUTH-003</strong> — RLS enforcement.</li><li><strong>FR-AI-003</strong> — memory audit bridge; receives the 8 <code>doc.*</code> audit row kinds.</li><li><strong>FR-AI-016</strong> — residency policy; this FR consumes the per-tenant residency tag.</li><li><strong>FR-MEMORY-111</strong> — PII scrubbing on <code>event_payload</code> fields containing PII (e.g. names in reason).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/doc/documents request (initiate)</h3>\n<pre><code class=\"language-json\">{\n  \"bucket_scope\": \"hr_contracts\",\n  \"original_filename\": \"employment_contract_2026.pdf\",\n  \"mime_type\": \"application/pdf\",\n  \"byte_size\": 245760,\n  \"sha256_hex\": \"b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9\",\n  \"parent_object_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\"\n}</code></pre>\n<h3>8.2 — 201 CREATED response (with presigned upload URL)</h3>\n<pre><code class=\"language-json\">{\n  \"document_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"presigned_upload_url\": \"https://cyberos-doc-vn-1-hr_contracts.s3.ap-southeast-1.amazonaws.com/5e8f.../hr_contracts/01HG7V8B...?X-Amz-Signature=...\",\n  \"presigned_form_fields\": {\"x-amz-server-side-encryption\": \"aws:kms\", \"x-amz-server-side-encryption-aws-kms-key-id\": \"alias/cyberos-doc-vn-1-hr_contracts\"},\n  \"expires_at\": \"2026-05-16T11:00:00Z\"\n}</code></pre>\n<h3>8.3 — doc.uploaded memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"doc.uploaded\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"document_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"bucket_scope\": \"hr_contracts\",\n  \"byte_size\": 245760,\n  \"mime_type\": \"application/pdf\",\n  \"sha256_hex\": \"b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9\",\n  \"s3_region\": \"ap-southeast-1\",\n  \"s3_bucket\": \"cyberos-doc-vn-1-hr_contracts\",\n  \"uploaded_by_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — POST /v1/doc/documents/{id}/legal-hold request</h3>\n<pre><code class=\"language-json\">{\n  \"apply\": true,\n  \"primary_signer_subject_id\": \"&lt;clo-uuid&gt;\",\n  \"secondary_signer_subject_id\": \"&lt;cseco-uuid&gt;\",\n  \"reason\": \"Ongoing wrongful-termination claim filed 2026-09-15; preserve all related employment records\",\n  \"case_reference\": \"CASE-2026-Q3-001\"\n}</code></pre>\n<h3>8.5 — doc.legal_hold_applied memory row (sev-1)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"doc.legal_hold_applied\",\n  \"severity\": \"sev-1\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"document_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"primary_signer_subject_id_hash16\": \"abcdef0123456789\",\n  \"secondary_signer_subject_id_hash16\": \"0987654321fedcba\",\n  \"case_reference\": \"CASE-2026-Q3-001\",\n  \"reason_scrubbed\": \"Ongoing wrongful-termination claim filed [DATE]; preserve all related employment records\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.6 — doc.archived memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"doc.archived\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"document_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"bucket_scope\": \"hr_contracts\",\n  \"retention_until\": \"2076-05-16\",\n  \"object_lock_mode\": \"COMPLIANCE\",\n  \"archived_by_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>eIDAS QTSP / AATL / VNeID signing flows</strong> — FR-DOC-002/003/004.</li><li><strong>Multi-party signing workflow</strong> — FR-DOC-005.</li><li><strong>Lifecycle metadata (parties, dates, renewal)</strong> — FR-DOC-007.</li><li><strong>Expiry alert cascade</strong> — FR-DOC-008.</li><li><strong>Renewal proposal CUO draft</strong> — FR-DOC-009.</li><li><strong>DocuSign / Adobe Sign / HelloSign import</strong> — FR-DOC-010.</li><li><strong>PAdES-B-LT format with re-stamping</strong> — FR-DOC-011.</li><li><strong>GDPR erasure handler</strong> — FR-DOC-2xx; this FR ships the <code>is_erasure_blocked</code> predicate.</li><li><strong>Per-scope RBAC refinement via scope_grants</strong> — FR-DOC-2xx.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>UPDATE on document_versions</td><td>SQL grant</td><td>Permission denied</td><td>None — designed</td></tr><tr><td>DELETE on document_versions</td><td>SQL grant</td><td>Permission denied</td><td>None — designed</td></tr><tr><td>Cross-scope move attempted</td><td>trigger <code>cross_scope_move_forbidden</code></td><td>400</td><td>Re-upload under correct scope</td></tr><tr><td>Retention mutation post-archive</td><td>trigger <code>retention_immutable_post_archive</code></td><td>400</td><td>None — designed</td></tr><tr><td>Object-Lock S3 API failure</td><td>aws-sdk error</td><td>500 <code>object_lock_apply_failed</code> + sev-1 alarm</td><td>Retry; if persistent, investigate IAM permission</td></tr><tr><td>Residency resolver returns unknown tenant</td><td><code>ResidencyError::Unknown</code></td><td>500</td><td>Provision tenant residency</td></tr><tr><td>Residency mismatch handler-side</td><td>mismatch check</td><td>400 <code>residency_mismatch</code></td><td>None — designed</td></tr><tr><td>SHA-256 mismatch at finalize</td><td>client supplied vs S3 ETag</td><td>409 <code>integrity_mismatch</code></td><td>Re-upload</td></tr><tr><td>Byte size &gt; 500MB</td><td>DB CHECK</td><td>400</td><td>Use FR-MEDIA-* path</td></tr><tr><td>Legal hold without dual-signoff</td><td>handler validates roles</td><td>400 <code>signer_role_mismatch</code></td><td>Add appropriate signer</td></tr><tr><td>Legal hold self-co-sign</td><td>handler check</td><td>400 <code>self_co_sign</code></td><td>Use distinct signers</td></tr><tr><td>Case reference too short</td><td>handler validation</td><td>400 <code>case_reference_invalid</code></td><td>Provide 5–200 char ref</td></tr><tr><td>Audit chain break (tampered prev_hash)</td><td>trigger <code>audit_chain_break</code></td><td>500</td><td>Investigate tamper; chain must be repaired manually + sev-1 alarm</td></tr><tr><td>memory row commit fails</td><td>transaction rolls back</td><td>500 <code>audit_failed</code></td><td>memory_writer diagnosis</td></tr><tr><td>Missing purpose on GET /download</td><td>handler</td><td>400 <code>purpose_required</code></td><td>Add purpose query param</td></tr><tr><td>Cross-tenant FK violation</td><td>RLS</td><td>0 rows / permission_denied</td><td>None — designed</td></tr><tr><td>Subject deleted while metadata references created_by</td><td>FK RESTRICT</td><td>DELETE auth.subjects fails</td><td>Designed</td></tr><tr><td>S3 PutObject failed mid-upload</td><td>client gets 5xx from S3</td><td>Document stuck in draft</td><td>24h cleanup via S3 lifecycle</td></tr><tr><td>AWS region not available (outage)</td><td>aws-sdk timeout</td><td>500 with outage info</td><td>Wait + retry</td></tr><tr><td>KMS key disabled</td><td>aws-sdk error on PUT</td><td>500 <code>kms_disabled</code></td><td>Re-enable key or rotate per FR-AUTH-2xx</td></tr><tr><td>Audit log replay reveals broken chain</td><td>application chain re-hash</td><td>sev-1 alarm</td><td>Investigate; manual repair</td></tr><tr><td>Object-Lock Compliance applied incorrectly (Governance instead)</td><td>aws-sdk response check</td><td>sev-1 alarm</td><td>Replay with correct mode</td></tr><tr><td>GDPR erasure with active hold</td><td><code>is_erasure_blocked</code> returns true</td><td>403 <code>legal_hold_active</code></td><td>Defer erasure; document the deferral</td></tr><tr><td>Concurrent finalize</td><td>INSERT version_number conflict</td><td>409 retry</td><td>Designed</td></tr><tr><td>Storage cost unexpectedly high (large bucket)</td><td>S3 inventory + alarm</td><td>sev-3</td><td>Operator review</td></tr><tr><td>Versioning disabled on bucket</td><td>startup check fails</td><td>Service refuses to start</td><td>Re-enable + restart</td></tr><tr><td>KMS key rotated without metadata update</td><td>next read uses stale alias</td><td>None (alias resolves to current)</td><td>Designed (alias indirection)</td></tr><tr><td>presigned URL expired before upload</td><td>S3 returns 403</td><td>Client retries with new presigned URL</td><td>Designed</td></tr><tr><td>Mime type spoofing</td><td>mime validation at finalize via libmagic</td><td>400 <code>mime_mismatch</code></td><td>Use correct content-type</td></tr><tr><td>LegalHold release without dual-signoff</td><td>handler validates roles same as apply</td><td>400</td><td>Provide CLO + CSO</td></tr><tr><td>Audit log filesize growth</td><td>partition by month</td><td>OK</td><td>None</td></tr><tr><td>RLS bypass attempt</td><td><code>USING</code> predicate</td><td>0 rows</td><td>None — designed</td></tr><tr><td><code>parent_object_id</code> references deleted object</td><td>soft FK; no enforcement</td><td>Stale reference</td><td>Periodic cleanup job (FR-DOC-2xx)</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>S3 Object-Lock Compliance mode is the legal anchor</strong> — once retention is set, no root admin can delete or shorten. Governance mode allows root override — defeats the purpose.</li><li><strong>Per-tenant residency pinning recorded on the row</strong> — future operations don't re-resolve; protects against tenant residency changes mid-flight.</li><li><strong>Closed <code>bucket_scope</code> enum drives 3 things</strong>: retention policy, KMS keyspace, RBAC scope. Each change is ADR-worthy.</li><li><strong>Hash-chained audit log is per-document</strong> — chain integrity scoped to one doc; no cross-doc dependencies; replay is one-table-scan.</li><li><strong><code>chain_hash</code> computed by application, validated by trigger</strong> — application computes via SHA-256 over canonical JSON; trigger validates <code>prev_hash</code> matches prior row's <code>chain_hash</code>.</li><li><strong>Versioning at S3 + Postgres</strong> — defense in depth. S3 has versioning enabled; each upload creates a new S3 version + Postgres row.</li><li><strong><code>s3_key_versioned</code> includes the AWS version id</strong> — for direct download of a specific version; the metadata <code>current_version_id</code> points at the Postgres row, which points at the S3 version.</li><li><strong>Legal hold dual-signoff at CLO + CSO</strong> — both legal AND security operator agree. Single signer would be too easy to misuse.</li><li><strong>GDPR/PDPL erasure blocked under hold</strong> — this is the legal trump (litigation &gt; data minimisation under most jurisdictions). The <code>is_erasure_blocked</code> predicate is the contract for the erasure handler.</li><li><strong>Access audit at sev-2</strong> — every read is forensically relevant; the <code>purpose</code> requirement filters trivial cases.</li><li><strong><code>parent_object_id</code> soft-typed</strong> — bucket_scope identifies the owner module; cross-service FK enforcement is operationally complex.</li><li><strong>500 MB max</strong> — covers PDFs + scanned images + signed documents. Larger media (video, audio attachments) goes through FR-MEDIA-*.</li><li><strong>S3 lifecycle cleanup of orphaned drafts at 24h</strong> — uploads that never finalize get garbage-collected; metadata stays as draft until finalize or 24h expiry.</li><li><strong>Retention computation uses <code>INTERVAL '&lt;years&gt;' year</code></strong> — leap-year aware via Postgres date math; deterministic.</li><li><strong><code>alias/&lt;bucket-name&gt;</code></strong> is the KMS alias convention — operations on the alias resolve to the current key version (supports rotation without app changes).</li><li><strong><code>PutObjectRetention</code> pins to a specific version_id</strong> — locks the bytes of <code>current_version_id</code> only; future versions get their own retention via their own archive transitions.</li><li><strong><code>document_audit_chain(doc_id)</code> SQL function</strong> — single replay query; useful for forensic + audit-replay tooling.</li><li><strong>8 memory audit kinds</strong> — recorded, versioned, acl_changed, signed, archived, legal_hold_applied, legal_hold_released, access_audited. Sev assignments: signed/archived = sev-3 (informational); acl_changed/access_audited = sev-2 (operator review); legal_hold_applied/released = sev-1 (high attention).</li><li><strong><code>reason</code> field PII-scrubbed in memory row</strong> — operators may write sensitive case details; FR-MEMORY-111 strips before chain commit.</li><li><strong><code>case_reference</code> 5–200 chars</strong> — long enough for internal case numbers, short enough to discourage prose narrative.</li><li><strong><code>OBS sev-1 alarm on PutObjectRetention failure</code></strong> — Object-Lock-Compliance is critical; failure to apply is a sev-1 (the doc was supposed to be locked).</li><li><strong><code>Mode=COMPLIANCE</code> is the only valid mode</strong> — Governance mode is forbidden by spec; if a future migration changes it, the CI gate should fail.</li><li><strong><code>signature_event_log</code> (FR-DOC-002/003/004)</strong> — those FRs add their own per-signature audit kinds; this FR ships only <code>doc.signed</code> as a placeholder.</li><li><strong><code>document_metadata.kms_key_id</code></strong> is the actual key id used at write — recorded for forensic verification + rotation-safety.</li></ul>\n<hr>\n<p><em>End of FR-DOC-001.</em></p>";
