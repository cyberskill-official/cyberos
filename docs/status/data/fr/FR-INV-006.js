window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INV-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The INV service <strong>MUST</strong> ship cash application as a closed 4-step matching cascade with atomic allocation + over-allocation block + reversal pattern. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>payment_allocations</code> table: <code>(id UUID PRIMARY KEY, tenant_id UUID NOT NULL, receipt_id UUID NOT NULL REFERENCES payment_receipts(id), invoice_id UUID NOT NULL REFERENCES invoices(id), amount_minor BIGINT NOT NULL, allocation_source allocation_source NOT NULL, allocated_by_subject_id UUID NOT NULL, reverses_allocation_id UUID REFERENCES payment_allocations(id), allocated_at TIMESTAMPTZ NOT NULL DEFAULT now(), notes TEXT)</code>. <code>amount_minor</code> MAY be negative iff <code>reverses_allocation_id</code> is set.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>allocation_source</code> Postgres enum with exactly 5 values (per DEC-566): <code>'memo_parser'</code>, <code>'amount_date'</code>, <code>'fuzzy'</code>, <code>'manual'</code>, <code>'reversal'</code>. Adding a 6th is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with <code>USING + WITH CHECK</code> on <code>payment_allocations</code>. Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li></ol>\n<ol><li><strong>MUST</strong> be <strong>append-only</strong> on <code>payment_allocations</code> (per DEC-564). <code>REVOKE UPDATE, DELETE FROM cyberos_app</code>. Reversals create new rows with negative <code>amount_minor</code> + <code>reverses_allocation_id</code> link.</li></ol>\n<ol><li><strong>MUST</strong> enforce <strong>over-allocation block</strong> via DB trigger (per DEC-563). On INSERT or UPDATE on <code>payment_allocations</code>:</li></ol>\n<ul><li>Compute <code>SUM(amount_minor) WHERE invoice_id = NEW.invoice_id</code> (positive + negative reversals net).</li><li>If <code>SUM &gt; invoices.amount_minor</code> for that invoice → RAISE <code>over_allocation_blocked</code> ERRCODE P0100.</li><li>Compute <code>SUM(amount_minor) WHERE receipt_id = NEW.receipt_id</code>.</li><li>If <code>SUM &gt; payment_receipts.amount_minor</code> for that receipt → RAISE <code>receipt_over_allocated</code> ERRCODE P0101.</li></ul>\n<ol><li><strong>MUST</strong> ship the <code>inv_cash_app_job</code> scheduled job running every 5 minutes (per DEC-561 + DEC-571):</li></ol>\n<ul><li>Query: <code>SELECT id, amount_minor, currency, transfer_memo FROM payment_receipts WHERE invoice_id IS NULL AND received_at &gt; now() - interval '90 days' FOR UPDATE SKIP LOCKED</code>.</li><li>For each receipt: invoke <code>cascade::try_match(receipt)</code> running steps 1 → 2 → 3 sequentially; step 4 is operator-driven (never auto-run).</li><li>Each step returns <code>Match { invoice_id, amount_minor_allocated, source }</code> or <code>NoMatch</code>.</li><li>On Match: INSERT <code>payment_allocations</code> row + UPDATE <code>payment_receipts.invoice_id</code> + emit step-specific memory row.</li><li>On NoMatch after step 3: emit <code>inv.cash_app_no_match</code> memory row sev-3.</li></ul>\n<ol><li><strong>MUST</strong> implement <strong>step 1 — exact memo reference match</strong> (per DEC-560). Reuses FR-INV-005's <code>extract_invoice_id</code> parser on <code>transfer_memo</code>. Match → INSERT allocation with full receipt amount; <code>allocation_source='memo_parser'</code>. Already handled at webhook time for ~80% of cases; this step catches receipts where memo parser deferred (e.g. when <code>payment_intent.metadata.invoice_id</code> was absent on Stripe path).</li></ol>\n<ol><li><strong>MUST</strong> implement <strong>step 2 — amount + close-date match</strong> (per DEC-560). Query: invoices with <code>tenant_id = receipt.tenant_id AND amount_minor = receipt.amount_minor AND currency = receipt.currency AND status = 'open' AND due_date BETWEEN receipt.received_at - interval '7 days' AND receipt.received_at + interval '7 days'</code>. Match exactly one → INSERT allocation; <code>allocation_source='amount_date'</code>. Multiple candidates → skip to step 3 (ambiguous).</li></ol>\n<ol><li><strong>MUST</strong> implement <strong>step 3 — fuzzy partial match</strong> (per DEC-560 + DEC-567 + DEC-570). Per-tenant <code>cash_app_fuzzy_threshold_pct</code> lookup (default 5%, range 1-20%). Query: invoices where <code>ABS(invoice.amount_minor - receipt.amount_minor) / invoice.amount_minor &lt;= threshold/100 AND status = 'open'</code>. Match exactly one → INSERT allocation; allocation amount = MIN(receipt.amount_minor, invoice.amount_minor_outstanding); <code>allocation_source='fuzzy'</code>. Multiple candidates → skip to manual step.</li></ol>\n<ol><li><strong>MUST</strong> implement <strong>step 4 — manual operator match</strong> (per DEC-568). NOT auto-invoked. <code>POST /v1/inv/cash-app/allocate-manual</code> handler:</li></ol>\n<ul><li>Caller MUST have role <code>cfo</code> per FR-AUTH-101.</li><li>Body: <code>{receipt_id, invoice_id, amount_minor, notes}</code>.</li><li>Validates <code>amount_minor &gt; 0 AND &lt;= receipt.amount_minor_remaining AND &lt;= invoice.amount_minor_outstanding</code>.</li><li>INSERT allocation with <code>allocation_source='manual'</code>.</li><li>Emit <code>inv.cash_app_matched_manual</code> memory row sev-2 (every manual ledger touch is forensically critical).</li></ul>\n<ol><li><strong>MUST</strong> support <strong>partial allocation</strong> (per DEC-562). One receipt MAY split across N invoices; one invoice MAY receive M receipts. Constraint enforced at trigger:</li></ol>\n<ul><li>SUM(allocations.amount_minor WHERE receipt_id = R) ≤ receipt.amount_minor.</li><li>SUM(allocations.amount_minor WHERE invoice_id = I) ≤ invoice.amount_minor.</li><li>Within-receipt overflow → 409 receipt_over_allocated; within-invoice overflow → 409 over_allocation_blocked.</li></ul>\n<ol><li><strong>MUST</strong> support <strong>reversal</strong> (per DEC-569). <code>POST /v1/inv/cash-app/reverse</code> handler:</li></ol>\n<ul><li>Caller MUST have role <code>cfo</code>.</li><li>Body: <code>{allocation_id, reason}</code>.</li><li>INSERT new row with <code>amount_minor = -original.amount_minor</code>, <code>reverses_allocation_id = original.id</code>, <code>allocation_source='reversal'</code>, <code>notes = reason</code>.</li><li>Constraint trigger handles — net sums adjust.</li><li>Emit <code>inv.cash_app_allocation_reversed</code> memory row sev-2.</li></ul>\n<ol><li><strong>MUST</strong> ship <code>invoice_outstanding_view</code> SQL view (per DEC-572): ``<code>sql SELECT i.id AS invoice_id, i.tenant_id, i.amount_minor, i.currency, COALESCE(SUM(a.amount_minor), 0) AS amount_allocated_minor, i.amount_minor - COALESCE(SUM(a.amount_minor), 0) AS amount_outstanding_minor, CASE WHEN i.amount_minor - COALESCE(SUM(a.amount_minor), 0) &lt;= 0 THEN 'paid' ELSE i.status END AS effective_status FROM invoices i LEFT JOIN payment_allocations a ON a.invoice_id = i.id GROUP BY i.id; </code>`<code> Downstream consumers (FR-INV-009 AR aging, FR-INV-010 dunning) use this view, NOT raw </code>invoices.status`.</li></ol>\n<ol><li><strong>MUST</strong> auto-mark invoice <code>paid</code> via trigger when <code>SUM(allocations.amount_minor) &gt;= invoice.amount_minor</code> (per DEC-572). The trigger fires on <code>payment_allocations</code> AFTER INSERT and runs <code>UPDATE invoices SET status='paid', paid_at=now() WHERE id = NEW.invoice_id AND ... &lt;= SUM(allocations)</code>. Idempotent — already-paid stays paid.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>GET /v1/inv/cash-app/unmatched?from=&lt;ts&gt;&amp;to=&lt;ts&gt;</code> handler (per DEC-573). Returns unmatched receipts (<code>invoice_id IS NULL</code> after step 3 ran). Caller MUST have role <code>cfo</code>. Body: <code>[{receipt_id, amount_minor, currency, transfer_memo, received_at, suggested_matches: [...top 3 fuzzy candidates...]}]</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit 9 memory audit row kinds (per DEC-565):</li></ol>\n<ul><li><code>inv.cash_app_match_attempted</code> — every job-run on a receipt (sampled 10%).</li><li><code>inv.cash_app_matched_step1</code> — memo_parser hit during scheduled job.</li><li><code>inv.cash_app_matched_step2</code> — amount_date hit.</li><li><code>inv.cash_app_matched_step3</code> — fuzzy hit.</li><li><code>inv.cash_app_matched_manual</code> — CFO manual commit (sev-2).</li><li><code>inv.cash_app_no_match</code> — all auto-steps failed (sev-3; sev-2 if unmatched &gt; 24h).</li><li><code>inv.cash_app_over_allocation_blocked</code> — trigger fired (sev-1; alarm).</li><li><code>inv.cash_app_partial_allocated</code> — receipt split across multiple invoices.</li><li><code>inv.cash_app_allocation_reversed</code> — reversal recorded (sev-2).</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>notes</code> + <code>transfer_memo</code> via FR-MEMORY-111 before chain commit.</li></ol>\n<ol><li><strong>MUST</strong> sev-2 escalate when receipts remain unmatched &gt; 24h (per DEC-573). OBS rule: <code>inv.cash_app_no_match</code> count over last hour &gt; 10 → sev-2 alarm to CFO Slack channel.</li></ol>\n<ol><li><strong>MUST</strong> complete cascade per-receipt in ≤ 200 ms p95 (3 DB queries + 1 INSERT). <code>cash_app_perf_test</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>inv.cash_app.{cascade,allocate_manual,reverse,unmatched_query}</code> with attributes: <code>tenant_id</code>, <code>receipt_id</code>, <code>invoice_id</code>, <code>allocation_source</code>, <code>outcome</code> (matched_step1 | matched_step2 | matched_step3 | matched_manual | no_match | over_allocation | receipt_over_allocated | invalid_amount | not_found | permission_denied).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>inv_cash_app_match_total{tenant_id, source, outcome}</code> (counter).</li><li><code>inv_cash_app_unmatched_count{tenant_id}</code> (gauge — receipts with invoice_id NULL after step 3).</li><li><code>inv_cash_app_partial_allocations_total{tenant_id}</code> (counter — receipts allocated across &gt; 1 invoice).</li><li><code>inv_cash_app_reversals_total{tenant_id}</code> (counter — sev-2 alarm at &gt; 3/h).</li><li><code>inv_cash_app_over_allocation_blocked_total{tenant_id}</code> (counter — sev-1 alarm always).</li><li><code>inv_cash_app_cascade_latency_ms{step}</code> (histogram).</li></ul>\n<ol><li><strong>MUST</strong> ship <code>GET /v1/inv/cash-app/allocations?invoice_id=&lt;&gt;</code> for operator visibility. Returns chronological list including reversals. Caller MUST have role <code>cfo</code> or <code>cdo</code> (financial visibility roles).</li></ol>\n<ol><li><strong>MUST</strong> use the <code>inv_cash_applier</code> SQL role (defined in FR-INV-005) to INSERT into <code>payment_allocations</code> + UPDATE <code>payment_receipts.invoice_id</code> (per DEC-574). cyberos_app role gets SELECT only.</li></ol>\n<ol><li><strong>MUST</strong> advisory-lock each receipt during cascade (per DEC-571). <code>SELECT pg_advisory_xact_lock(hashtext(receipt_id::text))</code> at top of cascade — prevents concurrent job runs from double-processing the same receipt.</li></ol>\n<ol><li><strong>MUST</strong> detect + skip currency-mismatched candidates at every step. Receipt in USD MUST NOT match invoice in VND even if amount-minor happens to match. Currency mismatch is an explicit skip condition in step 2 + step 3 queries.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/inv/cash-app/dry-run</code> (caller role <code>cfo</code> or <code>cdo</code>). Body <code>{receipt_id}</code>. Runs cascade WITHOUT writing allocations + returns the would-be match (or no_match). Used by operators to preview before committing manual matches.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why closed 4-step cascade (DEC-560)?</strong> Each step is a distinct matching strategy with distinct false-positive risk. Step 1 (exact ref) = near-zero false positives. Step 2 (amount+date) = low false positives. Step 3 (fuzzy) = some false positives — acceptable for low-value partial payments. Step 4 (manual) = operator judgment, always correct. The cascade order is by descending confidence + ascending operator effort.</p>\n<p><strong>Why async via scheduled job (DEC-561)?</strong> Webhook handlers must ack within 5s (Stripe + Napas247 SLAs). Matching takes 50-200ms per receipt × thousands of receipts = unworkable synchronously. Async job decouples: webhook persists receipt fast; job retroactively matches when capacity allows.</p>\n<p><strong>Why 5-min cadence (DEC-561)?</strong> Operator-acceptable latency for \"I received a payment; when does my invoice show paid?\" is &lt; 10 minutes. 5-min cadence + &lt; 200ms per receipt cascade = &lt; 6 minutes worst case from receipt to allocation.</p>\n<p><strong>Why partial allocation (DEC-562)?</strong> Real-world: VN B2B customers often pay invoices in 2-3 installments (30/30/40 or 50/50). Forcing 1:1 receipt-to-invoice = these installments stay unallocated indefinitely. M:N allocation matches the financial reality.</p>\n<p><strong>Why over-allocation blocked at trigger (DEC-563)?</strong> Defense in depth: handler validates; trigger catches direct SQL + concurrent inserts. Sev-1 audit row + alarm = operator immediately notified (over-allocation = ledger corruption; must be investigated).</p>\n<p><strong>Why append-only via SQL grant (DEC-564)?</strong> Ledger integrity is the cardinal AR/AP property. UPDATE/DELETE on allocations would let operators silently rewrite history. Reversal-row pattern preserves the audit trail (original allocation + reversal both present in chain).</p>\n<p><strong>Why CFO-only manual match (DEC-568)?</strong> Manual matching = direct ledger touch = segregation-of-duties concern. Accountant prepares (via dry-run); CFO commits. Tenants without CFO role need ADR + alternate role assignment in attribute_mapping_yaml. Sev-2 audit on every manual match means every CFO touch is forensically visible.</p>\n<p><strong>Why 5% fuzzy threshold default (DEC-567 + DEC-570)?</strong> Industry standard for partial-payment tolerance. Below 1% = nearly exact; above 20% = false-positive risk too high. Tenant override [1, 20]% gives flexibility; &gt; 20% requires ADR.</p>\n<p><strong>Why reversal as new row not UPDATE (DEC-569)?</strong> Same as TIME-001 correction_to pattern. Preserves the original record + makes \"what was the original allocation?\" trivially queryable. Net sums via aggregation respect both rows.</p>\n<p><strong>Why <code>inv.cash_app_no_match</code> sev-3 initially + sev-2 after 24h (DEC-573)?</strong> Some receipts genuinely defer to manual review (e.g. wrong customer, refund pending). Sev-3 = inform; sev-2 after 24h = action required. Two-tier escalation matches operator workflow.</p>\n<p><strong>Why generated outstanding view (DEC-572)?</strong> Computing outstanding inline in every consumer query would proliferate. View centralises; consumers query view + trust the math. Trigger auto-marks paid status — invoices stay in sync without batch jobs.</p>\n<p><strong>Why advisory lock per receipt (DEC-571)?</strong> SKIP LOCKED on the receipt query handles row-level concurrency. Advisory lock additionally guards the multi-statement cascade (read invoice + INSERT allocation must be atomic per receipt). Tx-scope ensures lock releases at commit/rollback.</p>\n<p><strong>Why dry-run handler (§1 #26)?</strong> Operator wants to preview \"if I assigned this receipt to invoice X, would the ledger accept it?\" before committing. Dry-run runs cascade + returns hypothetical outcome — no writes. Reduces error rate on manual matches.</p>\n<p><strong>Why currency-mismatch hard skip (§1 #25)?</strong> Receipt in USD matching invoice in VND would be cents-vs-đồng confusion — financially catastrophic. Currency equality is non-negotiable.</p>\n<p><strong>Why 90-day receipt-age window in scheduler (§1 #6)?</strong> Receipts older than 90 days without auto-match = manual review backlog. Scheduler stops auto-trying after 90d to avoid pointless retries; manual handler still works for older receipts.</p>\n<p><strong>Why allocation_source enum closed at 5 (DEC-566)?</strong> Downstream analytics (\"what % of receipts matched at step 1?\") need a fixed dimension. Adding a 6th (e.g. <code>ml_predicted</code>) is an ADR — needs accuracy validation before contributing to ledger.</p>\n<p><strong>Why operator role check on dry-run (§1 #26)?</strong> Even read-only operations expose customer + invoice data. CFO/CDO are financial visibility roles; tenant-admin doesn't need ledger detail.</p>\n<p><strong>Why partial-allocation counter as adoption metric (§1 #21)?</strong> Tracks \"how often do we split receipts?\" — useful for understanding customer payment behaviour + sizing operator workflows.</p>\n<p><strong>Why over-allocation sev-1 alarm (§1 #21)?</strong> Over-allocation = ledger corruption attempt. Either bug or attack; either way, page-on-call equivalent. Routine matching never hits this — every trigger is a real problem.</p>\n<p><strong>Why memo_parser as a cascade step (step 1) when FR-INV-005 already does it?</strong> FR-INV-005's memo parser runs at webhook time, but the Stripe path may have empty metadata.invoice_id (operator didn't set it during invoice creation). Step 1 re-runs the parser on transfer_memo + Stripe description fields — catches the deferred 5-10%.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0014 — payment_allocations</h3>\n<pre><code class=\"language-sql\">-- services/inv/migrations/0014_payment_allocations.sql\n\nBEGIN;\n\nCREATE TYPE allocation_source AS ENUM ('memo_parser', 'amount_date', 'fuzzy', 'manual', 'reversal');\n\nCREATE TABLE payment_allocations (\n    id                          UUID         PRIMARY KEY,\n    tenant_id                   UUID         NOT NULL,\n    receipt_id                  UUID         NOT NULL REFERENCES payment_receipts(id) ON DELETE RESTRICT,\n    invoice_id                  UUID         NOT NULL REFERENCES invoices(id) ON DELETE RESTRICT,\n    amount_minor                BIGINT       NOT NULL,\n    allocation_source           allocation_source NOT NULL,\n    allocated_by_subject_id     UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    reverses_allocation_id      UUID         REFERENCES payment_allocations(id),\n    allocated_at                TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    notes                       TEXT         CHECK (notes IS NULL OR length(notes) BETWEEN 1 AND 1000),\n    CHECK (amount_minor != 0),\n    CHECK ((amount_minor &lt; 0) = (reverses_allocation_id IS NOT NULL))\n);\n\nCREATE INDEX payment_allocations_receipt_idx ON payment_allocations (receipt_id);\nCREATE INDEX payment_allocations_invoice_idx ON payment_allocations (invoice_id);\nCREATE INDEX payment_allocations_tenant_allocated_idx ON payment_allocations (tenant_id, allocated_at DESC);\n\nALTER TABLE payment_allocations ENABLE ROW LEVEL SECURITY;\nCREATE POLICY payment_allocations_tenant_iso ON payment_allocations\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Append-only (DEC-564)\nREVOKE UPDATE, DELETE ON payment_allocations FROM cyberos_app;\nGRANT INSERT ON payment_allocations TO inv_cash_applier;\nGRANT SELECT ON payment_allocations TO cyberos_app, inv_cash_applier;\n\n-- Over-allocation block (DEC-563)\nCREATE OR REPLACE FUNCTION enforce_allocation_sums() RETURNS TRIGGER AS $$\nDECLARE\n    receipt_amount      BIGINT;\n    invoice_amount      BIGINT;\n    sum_for_receipt     BIGINT;\n    sum_for_invoice     BIGINT;\nBEGIN\n    SELECT amount_minor INTO receipt_amount FROM payment_receipts WHERE id = NEW.receipt_id;\n    SELECT amount_minor INTO invoice_amount FROM invoices WHERE id = NEW.invoice_id;\n\n    SELECT COALESCE(SUM(amount_minor), 0) INTO sum_for_receipt\n      FROM payment_allocations WHERE receipt_id = NEW.receipt_id;\n    sum_for_receipt := sum_for_receipt + NEW.amount_minor;\n    IF sum_for_receipt &gt; receipt_amount THEN\n        RAISE EXCEPTION 'receipt_over_allocated: % &gt; %', sum_for_receipt, receipt_amount USING ERRCODE = 'P0101';\n    END IF;\n\n    SELECT COALESCE(SUM(amount_minor), 0) INTO sum_for_invoice\n      FROM payment_allocations WHERE invoice_id = NEW.invoice_id;\n    sum_for_invoice := sum_for_invoice + NEW.amount_minor;\n    IF sum_for_invoice &gt; invoice_amount THEN\n        RAISE EXCEPTION 'over_allocation_blocked: % &gt; %', sum_for_invoice, invoice_amount USING ERRCODE = 'P0100';\n    END IF;\n\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_payment_allocations_sum_check BEFORE INSERT ON payment_allocations\n    FOR EACH ROW EXECUTE FUNCTION enforce_allocation_sums();\n\n-- Auto-mark invoice paid (DEC-572)\nCREATE OR REPLACE FUNCTION mark_invoice_paid_if_settled() RETURNS TRIGGER AS $$\nDECLARE\n    invoice_amount  BIGINT;\n    total_allocated BIGINT;\nBEGIN\n    SELECT amount_minor INTO invoice_amount FROM invoices WHERE id = NEW.invoice_id;\n    SELECT COALESCE(SUM(amount_minor), 0) INTO total_allocated\n      FROM payment_allocations WHERE invoice_id = NEW.invoice_id;\n    IF total_allocated &gt;= invoice_amount THEN\n        UPDATE invoices SET status = 'paid', paid_at = COALESCE(paid_at, now())\n         WHERE id = NEW.invoice_id AND status != 'paid';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_payment_allocations_mark_paid AFTER INSERT ON payment_allocations\n    FOR EACH ROW EXECUTE FUNCTION mark_invoice_paid_if_settled();\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0015 — outstanding view</h3>\n<pre><code class=\"language-sql\">-- services/inv/migrations/0015_invoice_outstanding_view.sql\n\nBEGIN;\n\nCREATE VIEW invoice_outstanding_view AS\nSELECT\n    i.id                                                                 AS invoice_id,\n    i.tenant_id,\n    i.amount_minor,\n    i.currency,\n    i.status                                                             AS recorded_status,\n    COALESCE(SUM(a.amount_minor), 0)                                     AS amount_allocated_minor,\n    i.amount_minor - COALESCE(SUM(a.amount_minor), 0)                    AS amount_outstanding_minor,\n    CASE\n        WHEN i.amount_minor - COALESCE(SUM(a.amount_minor), 0) &lt;= 0     THEN 'paid'\n        WHEN i.due_date &lt; CURRENT_DATE                                   THEN 'overdue'\n        ELSE i.status\n    END                                                                  AS effective_status\nFROM invoices i\nLEFT JOIN payment_allocations a ON a.invoice_id = i.id\nGROUP BY i.id;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Cascade dispatcher</h3>\n<pre><code class=\"language-rust\">// services/inv/src/cash_app/cascade.rs\nuse sqlx::PgPool;\nuse uuid::Uuid;\nuse crate::cash_app::{step1_exact_ref, step2_amount_date, step3_fuzzy};\nuse crate::types::AllocationSource;\n\n#[derive(Debug, Clone)]\npub enum CascadeResult {\n    Matched { invoice_id: Uuid, amount_minor: i64, source: AllocationSource },\n    NoMatch,\n}\n\npub async fn try_match(pool: &amp;PgPool, receipt_id: Uuid) -&gt; anyhow::Result&lt;CascadeResult&gt; {\n    // Advisory lock per receipt for atomic cascade\n    sqlx::query(\"SELECT pg_advisory_xact_lock(hashtext($1::text))\")\n        .bind(receipt_id.to_string()).execute(pool).await?;\n\n    if let Some(r) = step1_exact_ref::try_match(pool, receipt_id).await? {\n        return Ok(CascadeResult::Matched { invoice_id: r, amount_minor: 0 /* full receipt */, source: AllocationSource::MemoParser });\n    }\n    if let Some(r) = step2_amount_date::try_match(pool, receipt_id).await? {\n        return Ok(CascadeResult::Matched { invoice_id: r.invoice_id, amount_minor: r.amount, source: AllocationSource::AmountDate });\n    }\n    if let Some(r) = step3_fuzzy::try_match(pool, receipt_id).await? {\n        return Ok(CascadeResult::Matched { invoice_id: r.invoice_id, amount_minor: r.amount, source: AllocationSource::Fuzzy });\n    }\n    Ok(CascadeResult::NoMatch)\n}</code></pre>\n<h3>3.4 — Step 2 — amount + date</h3>\n<pre><code class=\"language-rust\">// services/inv/src/cash_app/step2_amount_date.rs\nuse sqlx::PgPool;\nuse uuid::Uuid;\n\npub struct Step2Match { pub invoice_id: Uuid, pub amount: i64 }\n\npub async fn try_match(pool: &amp;PgPool, receipt_id: Uuid) -&gt; anyhow::Result&lt;Option&lt;Step2Match&gt;&gt; {\n    // Load receipt\n    let (receipt_amount, receipt_currency, received_at): (i64, String, chrono::DateTime&lt;chrono::Utc&gt;) =\n        sqlx::query_as(\"SELECT amount_minor, currency, received_at FROM payment_receipts WHERE id = $1\")\n            .bind(receipt_id).fetch_one(pool).await?;\n\n    // Find candidate invoices: exact amount + matching currency + within ±7-day due-date window + status='open'\n    let candidates: Vec&lt;(Uuid,)&gt; = sqlx::query_as(r#\"\n        SELECT i.id FROM invoices i\n        WHERE i.amount_minor = $1\n          AND i.currency = $2\n          AND i.status = 'open'\n          AND i.due_date BETWEEN $3 - interval '7 days' AND $3 + interval '7 days'\n          AND i.id NOT IN (SELECT invoice_id FROM payment_allocations WHERE invoice_id IS NOT NULL)\n        LIMIT 2\n    \"#)\n    .bind(receipt_amount).bind(&amp;receipt_currency).bind(received_at.date_naive())\n    .fetch_all(pool).await?;\n\n    // Match only if exactly one candidate (ambiguous → skip to step 3)\n    if candidates.len() == 1 {\n        Ok(Some(Step2Match { invoice_id: candidates[0].0, amount: receipt_amount }))\n    } else {\n        Ok(None)\n    }\n}</code></pre>\n<h3>3.5 — Step 3 — fuzzy match</h3>\n<pre><code class=\"language-rust\">// services/inv/src/cash_app/step3_fuzzy.rs\nuse sqlx::PgPool;\nuse uuid::Uuid;\n\npub struct Step3Match { pub invoice_id: Uuid, pub amount: i64 }\n\npub const DEFAULT_FUZZY_THRESHOLD_PCT: i32 = 5;\npub const FUZZY_MIN_PCT: i32 = 1;\npub const FUZZY_MAX_PCT: i32 = 20;\n\npub async fn try_match(pool: &amp;PgPool, receipt_id: Uuid) -&gt; anyhow::Result&lt;Option&lt;Step3Match&gt;&gt; {\n    let (receipt_amount, receipt_currency, tenant_id): (i64, String, Uuid) =\n        sqlx::query_as(\"SELECT amount_minor, currency, tenant_id FROM payment_receipts WHERE id = $1\")\n            .bind(receipt_id).fetch_one(pool).await?;\n\n    // Per-tenant fuzzy threshold (with bounds check)\n    let raw_pct: Option&lt;i32&gt; = sqlx::query_scalar(\n        \"SELECT cash_app_fuzzy_threshold_pct FROM tenant_policy WHERE tenant_id = $1\"\n    ).bind(tenant_id).fetch_optional(pool).await?;\n    let pct = raw_pct.unwrap_or(DEFAULT_FUZZY_THRESHOLD_PCT)\n        .clamp(FUZZY_MIN_PCT, FUZZY_MAX_PCT);\n\n    // Find candidates within ±pct%\n    let candidates: Vec&lt;(Uuid, i64)&gt; = sqlx::query_as(r#\"\n        SELECT i.id, i.amount_minor FROM invoices i\n        LEFT JOIN payment_allocations a ON a.invoice_id = i.id\n        WHERE i.currency = $1\n          AND i.status = 'open'\n          AND i.tenant_id = $2\n          AND ABS(i.amount_minor - $3) * 100 &lt;= i.amount_minor * $4\n        GROUP BY i.id\n        HAVING COALESCE(SUM(a.amount_minor), 0) &lt; i.amount_minor\n        LIMIT 2\n    \"#)\n    .bind(&amp;receipt_currency).bind(tenant_id).bind(receipt_amount).bind(pct as i64)\n    .fetch_all(pool).await?;\n\n    if candidates.len() == 1 {\n        let (invoice_id, invoice_amount) = candidates[0];\n        let allocation_amount = receipt_amount.min(invoice_amount);\n        Ok(Some(Step3Match { invoice_id, amount: allocation_amount }))\n    } else {\n        Ok(None)\n    }\n}</code></pre>\n<h3>3.6 — Scheduler</h3>\n<pre><code class=\"language-rust\">// services/inv/src/cash_app/scheduler.rs\nuse sqlx::PgPool;\nuse std::time::Duration;\nuse uuid::Uuid;\nuse crate::cash_app::{cascade, audit, allocator};\n\npub async fn run_loop(pool: PgPool) {\n    let mut tick = tokio::time::interval(Duration::from_secs(5 * 60));\n    loop {\n        tick.tick().await;\n        if let Err(e) = run_once(&amp;pool).await {\n            tracing::error!(\"cash_app scheduler iter error: {e:?}\");\n        }\n    }\n}\n\nasync fn run_once(pool: &amp;PgPool) -&gt; anyhow::Result&lt;()&gt; {\n    // Find unmatched receipts within 90-day window\n    let receipts: Vec&lt;(Uuid, Uuid)&gt; = sqlx::query_as(r#\"\n        SELECT id, tenant_id FROM payment_receipts\n        WHERE invoice_id IS NULL\n          AND received_at &gt; now() - interval '90 days'\n        FOR UPDATE SKIP LOCKED\n        LIMIT 100\n    \"#).fetch_all(pool).await?;\n\n    for (receipt_id, tenant_id) in receipts {\n        match cascade::try_match(pool, receipt_id).await {\n            Ok(cascade::CascadeResult::Matched { invoice_id, amount_minor, source }) =&gt; {\n                allocator::allocate(pool, tenant_id, receipt_id, invoice_id, amount_minor, source, system_subject_id()).await?;\n                audit::emit_matched(pool, tenant_id, receipt_id, invoice_id, source).await?;\n            }\n            Ok(cascade::CascadeResult::NoMatch) =&gt; {\n                audit::emit_no_match(pool, tenant_id, receipt_id).await?;\n            }\n            Err(e) =&gt; tracing::warn!(\"cascade error on {receipt_id}: {e:?}\"),\n        }\n    }\n    Ok(())\n}\n\nfn system_subject_id() -&gt; Uuid { Uuid::nil() }</code></pre>\n<h3>3.7 — Allocator</h3>\n<pre><code class=\"language-rust\">// services/inv/src/cash_app/allocator.rs\nuse sqlx::PgPool;\nuse uuid::Uuid;\nuse crate::types::AllocationSource;\n\npub async fn allocate(\n    pool: &amp;PgPool,\n    tenant_id: Uuid,\n    receipt_id: Uuid,\n    invoice_id: Uuid,\n    amount_minor: i64,\n    source: AllocationSource,\n    allocated_by: Uuid,\n) -&gt; anyhow::Result&lt;Uuid&gt; {\n    let allocation_id = Uuid::new_v4();\n    let mut tx = pool.begin().await?;\n    sqlx::query(r#\"\n        INSERT INTO payment_allocations (id, tenant_id, receipt_id, invoice_id, amount_minor, allocation_source, allocated_by_subject_id)\n        VALUES ($1, $2, $3, $4, $5, $6::allocation_source, $7)\n    \"#)\n    .bind(allocation_id).bind(tenant_id).bind(receipt_id).bind(invoice_id)\n    .bind(amount_minor).bind(format!(\"{source:?}\").to_lowercase()).bind(allocated_by)\n    .execute(&amp;mut *tx).await?;\n\n    // If full receipt allocated, set invoice_id on payment_receipts for fast lookup\n    let receipt_sum: i64 = sqlx::query_scalar(\n        \"SELECT COALESCE(SUM(amount_minor), 0) FROM payment_allocations WHERE receipt_id = $1\"\n    ).bind(receipt_id).fetch_one(&amp;mut *tx).await?;\n    let receipt_total: i64 = sqlx::query_scalar(\n        \"SELECT amount_minor FROM payment_receipts WHERE id = $1\"\n    ).bind(receipt_id).fetch_one(&amp;mut *tx).await?;\n    if receipt_sum &gt;= receipt_total {\n        sqlx::query(\"UPDATE payment_receipts SET invoice_id = $2 WHERE id = $1\")\n            .bind(receipt_id).bind(invoice_id).execute(&amp;mut *tx).await?;\n    }\n    tx.commit().await?;\n    Ok(allocation_id)\n}\n\npub async fn reverse(\n    pool: &amp;PgPool,\n    original_id: Uuid,\n    reversed_by: Uuid,\n    reason: &amp;str,\n) -&gt; anyhow::Result&lt;Uuid&gt; {\n    let original: (Uuid, Uuid, Uuid, i64, String) = sqlx::query_as(\n        \"SELECT tenant_id, receipt_id, invoice_id, amount_minor, allocation_source::text FROM payment_allocations WHERE id = $1\"\n    ).bind(original_id).fetch_one(pool).await?;\n\n    let reversal_id = Uuid::new_v4();\n    sqlx::query(r#\"\n        INSERT INTO payment_allocations (id, tenant_id, receipt_id, invoice_id, amount_minor, allocation_source, allocated_by_subject_id, reverses_allocation_id, notes)\n        VALUES ($1, $2, $3, $4, $5, 'reversal'::allocation_source, $6, $7, $8)\n    \"#)\n    .bind(reversal_id).bind(original.0).bind(original.1).bind(original.2)\n    .bind(-original.3).bind(reversed_by).bind(original_id).bind(reason)\n    .execute(pool).await?;\n    Ok(reversal_id)\n}</code></pre>\n<h3>3.8 — Manual handler</h3>\n<pre><code class=\"language-rust\">// services/inv/src/handlers/cash_app.rs (excerpt)\nuse axum::{Json, extract::State, http::StatusCode};\nuse cyberos_auth::rbac::Role;\nuse crate::cash_app::{allocator, audit};\nuse uuid::Uuid;\n\n#[derive(Deserialize)]\npub struct AllocateManualRequest {\n    pub receipt_id: Uuid,\n    pub invoice_id: Uuid,\n    pub amount_minor: i64,\n    pub notes: String,\n}\n\npub async fn allocate_manual(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Json(req): Json&lt;AllocateManualRequest&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;AllocateResponse&gt;), ApiError&gt; {\n    if !claims.roles().contains(&amp;Role::Cfo) {\n        return Err(ApiError::PermissionDenied);\n    }\n    if req.amount_minor &lt;= 0 || req.notes.is_empty() || req.notes.len() &gt; 1000 {\n        return Err(ApiError::InvalidAmountOrNotes);\n    }\n    // Currency match\n    let receipt_currency: String = sqlx::query_scalar(\n        \"SELECT currency FROM payment_receipts WHERE id = $1 AND tenant_id = $2\"\n    ).bind(req.receipt_id).bind(claims.tenant_id()).fetch_one(&amp;state.db).await?;\n    let invoice_currency: String = sqlx::query_scalar(\n        \"SELECT currency FROM invoices WHERE id = $1 AND tenant_id = $2\"\n    ).bind(req.invoice_id).bind(claims.tenant_id()).fetch_one(&amp;state.db).await?;\n    if receipt_currency != invoice_currency {\n        return Err(ApiError::CurrencyMismatch);\n    }\n\n    let allocation_id = allocator::allocate(\n        &amp;state.db, claims.tenant_id(), req.receipt_id, req.invoice_id, req.amount_minor,\n        crate::types::AllocationSource::Manual, claims.subject_id(),\n    ).await?;\n\n    audit::emit_matched_manual(\n        &amp;state.db, claims.tenant_id(), req.receipt_id, req.invoice_id, allocation_id,\n        claims.subject_id(), &amp;req.notes,\n    ).await?;\n\n    Ok((StatusCode::CREATED, Json(AllocateResponse { allocation_id })))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>AllocationSource enum closed at 5</strong> — memo_parser, amount_date, fuzzy, manual, reversal.</li><li><strong>RLS isolates by tenant</strong> — cross-tenant queries return 0 rows.</li><li><strong>Step 1 memo_parser match</strong> — receipt with HD123456 memo → invoice with number HD123456 matched.</li><li><strong>Step 2 amount+date match</strong> — receipt amount + date within ±7d of due_date + currency match → matched.</li><li><strong>Step 2 ambiguous (&gt;1 candidate) skips to step 3</strong> — multiple matching invoices → no step 2 match.</li><li><strong>Step 3 fuzzy 5% match</strong> — receipt amount within ±5% of invoice → matched at allocation = min(receipt, outstanding).</li><li><strong>Step 3 per-tenant override</strong> — <code>cash_app_fuzzy_threshold_pct=10</code> → 10% tolerance.</li><li><strong>Step 3 outside threshold rejected</strong> — receipt 6% off invoice (default 5%) → no match.</li><li><strong>Currency mismatch hard skip</strong> — receipt USD vs invoice VND → no match at any step.</li><li><strong>No match emits sev-3 audit</strong> — 4-step cascade fails → <code>inv.cash_app_no_match</code> row.</li><li><strong>Manual match by CFO</strong> → 201 + <code>inv.cash_app_matched_manual</code> sev-2 row.</li><li><strong>Manual match by non-CFO</strong> → 403.</li><li><strong>Manual match with invalid amount</strong> → 400.</li><li><strong>Currency mismatch on manual</strong> → 400.</li><li><strong>Over-allocation blocked at trigger</strong> — INSERT exceeding invoice outstanding → P0100 + sev-1 audit.</li><li><strong>Receipt over-allocation blocked</strong> — INSERT exceeding receipt amount → P0101.</li><li><strong>Partial allocation supported</strong> — one receipt → 2 invoices with split amounts.</li><li><strong>Reversal via <code>POST /reverse</code></strong> — original allocation + negative-amount reversal coexist.</li><li><strong>Reversal by non-CFO</strong> → 403.</li><li><strong>Net sums respect reversals</strong> — original +500 + reversal -500 → invoice outstanding restored.</li><li><strong><code>UPDATE/DELETE</code> blocked from cyberos_app</strong> — only <code>inv_cash_applier</code> may INSERT.</li><li><strong>Invoice auto-marked <code>paid</code></strong> — full allocation triggers status=paid + paid_at set.</li><li><strong>Outstanding view reflects allocations</strong> — invoice with $500 + allocation $200 → outstanding $300.</li><li><strong>Scheduler advisory-locks per receipt</strong> — concurrent runs don't double-allocate.</li><li><strong>Scheduler skips receipts &gt; 90 days old</strong> — only manual path remaining.</li><li><strong><code>GET /unmatched</code> returns receipts</strong> — caller MUST have role cfo or cdo.</li><li><strong><code>POST /dry-run</code> returns hypothetical match</strong> — no allocation written.</li><li><strong>Cascade p95 &lt; 200 ms</strong> — perf test.</li><li><strong>OTel span <code>inv.cash_app.cascade</code> emitted</strong> — outcome attribute.</li><li><strong>Counter <code>inv_cash_app_match_total{source=memo_parser, outcome=matched}</code> increments</strong>.</li><li><strong>Counter <code>inv_cash_app_over_allocation_blocked_total</code> sev-1 alarm always</strong>.</li><li><strong>9 memory audit kinds emit correctly</strong> — one per scenario.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/inv/tests/cash_app_step3_fuzzy_5pct_test.rs\n#[sqlx::test]\nasync fn fuzzy_within_5pct_matches(pool: sqlx::PgPool) {\n    let tenant = ctx_seed_tenant(&amp;pool).await;\n    let invoice = seed_invoice(&amp;pool, tenant, /* amount_minor */ 1_000_000, /* currency */ \"VND\").await;\n    let receipt = seed_receipt(&amp;pool, tenant, 970_000, \"VND\", \"\").await;  // 3% short\n    let r = cyberos_inv::cash_app::cascade::try_match(&amp;pool, receipt).await.unwrap();\n    assert!(matches!(r, cyberos_inv::cash_app::cascade::CascadeResult::Matched { ref source, .. }\n        if matches!(source, cyberos_inv::types::AllocationSource::Fuzzy)));\n}\n\n#[sqlx::test]\nasync fn fuzzy_outside_5pct_no_match(pool: sqlx::PgPool) {\n    let tenant = ctx_seed_tenant(&amp;pool).await;\n    let _invoice = seed_invoice(&amp;pool, tenant, 1_000_000, \"VND\").await;\n    let receipt = seed_receipt(&amp;pool, tenant, 930_000, \"VND\", \"\").await;  // 7% short\n    let r = cyberos_inv::cash_app::cascade::try_match(&amp;pool, receipt).await.unwrap();\n    assert!(matches!(r, cyberos_inv::cash_app::cascade::CascadeResult::NoMatch));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/inv/tests/cash_app_over_allocation_blocked_test.rs\n#[sqlx::test]\nasync fn over_allocation_trigger_blocks(pool: sqlx::PgPool) {\n    set_role_cash_applier(&amp;pool).await;\n    let invoice = seed_invoice(&amp;pool, 1_000_000).await;\n    let receipt = seed_receipt(&amp;pool, 1_500_000).await;\n    // First allocation 800,000 OK\n    let _ = sqlx::query(\"INSERT INTO payment_allocations (id, tenant_id, receipt_id, invoice_id, amount_minor, allocation_source, allocated_by_subject_id) VALUES ($1, $2, $3, $4, 800000, 'amount_date'::allocation_source, $5)\")\n        .bind(uuid::Uuid::new_v4()).bind(tenant()).bind(receipt).bind(invoice).bind(subject()).execute(&amp;pool).await.unwrap();\n    // Second allocation 300,000 → total 1,100,000 &gt; 1,000,000 → must fail\n    let err = sqlx::query(\"INSERT INTO payment_allocations (id, tenant_id, receipt_id, invoice_id, amount_minor, allocation_source, allocated_by_subject_id) VALUES ($1, $2, $3, $4, 300000, 'amount_date'::allocation_source, $5)\")\n        .bind(uuid::Uuid::new_v4()).bind(tenant()).bind(receipt).bind(invoice).bind(subject()).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"over_allocation_blocked\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/inv/tests/cash_app_reversal_test.rs\n#[sqlx::test]\nasync fn reversal_creates_negative_row_and_nets_to_zero(pool: sqlx::PgPool) {\n    let invoice = seed_invoice(&amp;pool, 1_000_000).await;\n    let receipt = seed_receipt(&amp;pool, 1_000_000).await;\n    let original = cyberos_inv::cash_app::allocator::allocate(\n        &amp;pool, tenant(), receipt, invoice, 1_000_000,\n        cyberos_inv::types::AllocationSource::Manual, subject(),\n    ).await.unwrap();\n    let _reversal = cyberos_inv::cash_app::allocator::reverse(&amp;pool, original, subject(), \"operator error\").await.unwrap();\n\n    let total: i64 = sqlx::query_scalar(\n        \"SELECT COALESCE(SUM(amount_minor), 0) FROM payment_allocations WHERE invoice_id = $1\"\n    ).bind(invoice).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(total, 0, \"reversal must net to zero\");\n    let outstanding: i64 = sqlx::query_scalar(\n        \"SELECT amount_outstanding_minor FROM invoice_outstanding_view WHERE invoice_id = $1\"\n    ).bind(invoice).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(outstanding, 1_000_000, \"invoice fully outstanding again after reversal\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/inv/tests/cash_app_partial_allocation_test.rs\n#[sqlx::test]\nasync fn one_receipt_splits_across_two_invoices(pool: sqlx::PgPool) {\n    let inv_a = seed_invoice(&amp;pool, 600_000).await;\n    let inv_b = seed_invoice(&amp;pool, 400_000).await;\n    let receipt = seed_receipt(&amp;pool, 1_000_000).await;\n    cyberos_inv::cash_app::allocator::allocate(&amp;pool, tenant(), receipt, inv_a, 600_000,\n        cyberos_inv::types::AllocationSource::Manual, subject()).await.unwrap();\n    cyberos_inv::cash_app::allocator::allocate(&amp;pool, tenant(), receipt, inv_b, 400_000,\n        cyberos_inv::types::AllocationSource::Manual, subject()).await.unwrap();\n\n    let inv_a_status: String = sqlx::query_scalar(\n        \"SELECT effective_status FROM invoice_outstanding_view WHERE invoice_id = $1\"\n    ).bind(inv_a).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(inv_a_status, \"paid\");\n    let inv_b_status: String = sqlx::query_scalar(\n        \"SELECT effective_status FROM invoice_outstanding_view WHERE invoice_id = $1\"\n    ).bind(inv_b).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(inv_b_status, \"paid\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/inv/tests/cash_app_step4_manual_cfo_test.rs\n#[tokio::test]\nasync fn manual_match_by_cfo_succeeds(ctx: TestCtx) {\n    let invoice = ctx.create_invoice(1_000_000, \"VND\").await;\n    let receipt = ctx.create_unmatched_receipt(1_000_000, \"VND\").await;\n    let resp = ctx.post_as_cfo(\"/v1/inv/cash-app/allocate-manual\", json!({\n        \"receipt_id\": receipt.id, \"invoice_id\": invoice.id, \"amount_minor\": 1_000_000,\n        \"notes\": \"Verified payment by phone confirmation 2026-05-15\"\n    })).await.unwrap();\n    assert_eq!(resp.status(), 201);\n    let rows = ctx.memory_audit_rows(\"inv.cash_app_matched_manual\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"severity\"], \"sev-2\");\n}\n\n#[tokio::test]\nasync fn manual_match_by_non_cfo_rejected(ctx: TestCtx) {\n    let invoice = ctx.create_invoice(1_000_000, \"VND\").await;\n    let receipt = ctx.create_unmatched_receipt(1_000_000, \"VND\").await;\n    let err = ctx.post_as_tenant_admin(\"/v1/inv/cash-app/allocate-manual\", json!({\n        \"receipt_id\": receipt.id, \"invoice_id\": invoice.id, \"amount_minor\": 1_000_000, \"notes\": \"test\"\n    })).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"permission_denied\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 9 memory row builders follow canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>FR-INV-003</strong> — Stripe webhook (provides receipts).</li><li><strong>FR-INV-005</strong> — VietQR webhook (provides receipts + memo parser).</li></ul>\n<p><strong>Downstream:</strong> none at slice 2.</p>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>FR-INV-001</strong> — invoices table (FK target + currency + amount_minor).</li><li><strong>FR-AUTH-101</strong> — CFO role gate.</li><li><strong>FR-AI-003</strong> — memory audit bridge.</li><li><strong>FR-MEMORY-111</strong> — PII scrubbing.</li><li><strong>FR-OBS-007</strong> — sev-1 alarm on over-allocation + sev-2 on no-match &gt; 24h.</li><li><strong>FR-AI-005</strong> — per-tenant fuzzy threshold policy.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/inv/cash-app/allocate-manual</h3>\n<pre><code class=\"language-json\">{\n  \"receipt_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"invoice_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"amount_minor\": 4500000,\n  \"notes\": \"Customer paid via WeChat instead of VietQR; manual link approved by CFO Q3-2026-042\"\n}</code></pre>\n<h3>8.2 — POST /v1/inv/cash-app/dry-run</h3>\n<pre><code class=\"language-json\">{ \"receipt_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\" }</code></pre>\n<p>Response:</p>\n<pre><code class=\"language-json\">{\n  \"would_match\": \"step3_fuzzy\",\n  \"invoice_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"amount_minor\": 970000,\n  \"fuzzy_percent_difference\": 3.0,\n  \"outstanding_after_match\": 30000\n}</code></pre>\n<h3>8.3 — GET /v1/inv/cash-app/unmatched response</h3>\n<pre><code class=\"language-json\">{\n  \"unmatched\": [\n    {\n      \"receipt_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n      \"amount_minor\": 4500000,\n      \"currency\": \"VND\",\n      \"transfer_memo\": \"Thanh toan thang 5 - ACME\",\n      \"received_at\": \"2026-05-15T10:00:00Z\",\n      \"suggested_matches\": [\n        { \"invoice_id\": \"9b1deb4d-...\", \"amount_minor\": 4500000, \"score\": 0.95, \"reason\": \"exact amount match outside date window\" },\n        { \"invoice_id\": \"8a7c8c80-...\", \"amount_minor\": 4525000, \"score\": 0.80, \"reason\": \"0.6% fuzzy match\" }\n      ]\n    }\n  ]\n}</code></pre>\n<h3>8.4 — inv.cash_app_matched_step3 memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"inv.cash_app_matched_step3\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"receipt_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"invoice_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"allocation_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8N\",\n  \"allocation_amount_minor\": 970000,\n  \"fuzzy_pct_used\": 5,\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.5 — inv.cash_app_over_allocation_blocked memory row (sev-1)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"inv.cash_app_over_allocation_blocked\",\n  \"severity\": \"sev-1\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"invoice_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"invoice_amount_minor\": 1000000,\n  \"attempted_sum_minor\": 1100000,\n  \"attempted_by_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.6 — inv.cash_app_allocation_reversed memory row (sev-2)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"inv.cash_app_allocation_reversed\",\n  \"severity\": \"sev-2\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"original_allocation_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"reversal_allocation_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8N\",\n  \"reason_scrubbed\": \"Operator linked wrong invoice; corrected via CFO desk 2026-05-16\",\n  \"reversed_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Multi-currency cross-allocation (e.g. USD receipt against VND invoice via FX)</strong> — FR-INV-002 ships SBV FX snapshot; cross-currency cash app is FR-INV-2xx.</li><li><strong>ML-suggested matches</strong> — slice 3 augmentation of step 4 manual.</li><li><strong>Bulk import historical allocations</strong> — FR-INV-2xx migration tool.</li><li><strong>Customer-side allocation hint via API</strong> — FR-PORTAL-008.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Over-allocation attempted</td><td>trigger</td><td>P0100 + sev-1 audit</td><td>Designed</td></tr><tr><td>Receipt over-allocated</td><td>trigger</td><td>P0101</td><td>Designed</td></tr><tr><td>Manual by non-CFO</td><td>role check</td><td>403</td><td>Designed</td></tr><tr><td>Currency mismatch on manual</td><td>handler</td><td>400 currency_mismatch</td><td>Designed</td></tr><tr><td>Invalid amount on manual</td><td>handler</td><td>400</td><td>Designed</td></tr><tr><td>Reversal by non-CFO</td><td>role check</td><td>403</td><td>Designed</td></tr><tr><td>Step 2 ambiguous (multiple candidates)</td><td>LIMIT 2 check</td><td>NoMatch from step 2; cascades to step 3</td><td>Designed</td></tr><tr><td>Step 3 ambiguous</td><td>same</td><td>NoMatch from step 3; falls to manual</td><td>Designed</td></tr><tr><td>Step 3 fuzzy threshold out of bounds</td><td>clamp</td><td>Threshold capped at [1,20]</td><td>Designed</td></tr><tr><td>Per-tenant policy missing</td><td>DEFAULT used</td><td>5%</td><td>Designed</td></tr><tr><td>Receipt &gt; 90 days old</td><td>scheduler skip</td><td>Only manual handler</td><td>None — designed</td></tr><tr><td>Concurrent scheduler runs</td><td>SKIP LOCKED + advisory lock</td><td>One wins per receipt</td><td>Designed</td></tr><tr><td>Append-only UPDATE/DELETE from app</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>Currency-mismatch at step 2</td><td>query filter</td><td>Skip to step 3</td><td>Designed</td></tr><tr><td>Currency-mismatch at step 3</td><td>query filter</td><td>NoMatch</td><td>Designed</td></tr><tr><td>Invoice auto-mark paid race</td><td>UPDATE WHERE status != 'paid'</td><td>Idempotent</td><td>Designed</td></tr><tr><td>memory audit emit fail mid-tx</td><td>rollback</td><td>500 + retry</td><td>memory_writer health</td></tr><tr><td>OTel span attribute missing</td><td>otel_test</td><td>CI fails</td><td>Fix</td></tr><tr><td>&gt; 10 unmatched at 24h</td><td>OBS rule</td><td>sev-2 alarm</td><td>Operator review</td></tr><tr><td>Reversal of already-reversed</td><td>sum trigger</td><td>OK (net zero is fine; sum stays bounded)</td><td>Designed</td></tr><tr><td>Reversal exceeding original</td><td>trigger</td><td>P0101 (receipt) or P0100 (invoice)</td><td>Designed</td></tr><tr><td>RLS bypass</td><td>USING</td><td>0 rows</td><td>Designed</td></tr><tr><td>Subject deleted while allocations exist</td><td>FK RESTRICT</td><td>DELETE auth.subjects fails</td><td>Soft-delete</td></tr><tr><td><code>cyberos_app</code> INSERT into allocations</td><td>grant absent</td><td>permission denied</td><td>Use inv_cash_applier role</td></tr><tr><td>Dry-run side-effect leak</td><td>test asserts</td><td>CI fails</td><td>Fix branching</td></tr><tr><td>Allocation 0 amount_minor</td><td>DB CHECK != 0</td><td>INSERT fails</td><td>Designed</td></tr><tr><td>Positive amount with reverses_allocation_id set</td><td>DB CHECK XOR</td><td>INSERT fails</td><td>Designed</td></tr><tr><td>Negative amount without reverses_allocation_id</td><td>DB CHECK XOR</td><td>INSERT fails</td><td>Designed</td></tr><tr><td>Notes &gt; 1000 chars</td><td>DB CHECK</td><td>INSERT fails</td><td>Shorten</td></tr><tr><td>Memo parser HD-prefix false positive</td><td>manual-reversal handler</td><td>Operator reverses + re-allocates</td><td>Designed</td></tr><tr><td>Step 1 hit for already-allocated invoice</td><td>over-allocation trigger</td><td>P0100</td><td>Designed</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Closed 4-step cascade</strong> — descending confidence, ascending operator effort.</li><li><strong>Async via 5-min scheduler</strong> — webhook stays fast; matching catches up.</li><li><strong>Advisory lock per receipt</strong> — multi-statement cascade atomic per receipt.</li><li><strong>Partial allocation M:N</strong> — VN B2B installment payments reality.</li><li><strong>Over-allocation block at trigger</strong> — defense in depth.</li><li><strong>Append-only via SQL grant</strong> — ledger integrity.</li><li><strong>Reversal as new row</strong> — same correction_to pattern as FR-TIME-001.</li><li><strong>CFO-only manual</strong> — segregation of duties.</li><li><strong>5% fuzzy default</strong> — industry standard.</li><li><strong>Per-tenant fuzzy override [1,20]%</strong> — flexibility with bounds.</li><li><strong>9 memory audit kinds</strong> — granular operator queries.</li><li><strong>Sev-1 alarm on over-allocation</strong> — ledger-corruption signal.</li><li><strong>Sev-2 escalation on &gt; 10 no-match at 24h</strong> — actionable backlog.</li><li><strong>Auto-mark <code>paid</code> via trigger</strong> — invoice status stays consistent.</li><li><strong>Outstanding view</strong> — centralised math.</li><li><strong>Dry-run handler</strong> — pre-commit preview.</li><li><strong>Currency-mismatch hard skip</strong> — cents-vs-đồng confusion defense.</li><li><strong>90-day window in scheduler</strong> — backlog avoidance.</li><li><strong><code>inv_cash_applier</code> SQL role</strong> — defense in depth (reuses FR-INV-005 role split).</li><li><strong>DB CHECK on amount_minor signs</strong> — positive iff not reversal.</li><li><strong>PII scrub notes + memo</strong> — chain holds scrubbed.</li><li><strong>OBS sev-2 on &gt; 3/h reversals</strong> — high reversal rate signals operator-error or attack.</li></ul>\n<hr>\n<p><em>End of FR-INV-006.</em></p>";
