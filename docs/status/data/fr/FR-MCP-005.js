window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MCP-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The MCP service <strong>MUST</strong> expose RFC 9728 Protected Resource Metadata at <code>/.well-known/oauth-protected-resource</code> (gateway-aggregate) and <code>/.well-known/oauth-protected-resource/cyberos.{module}</code> (per-module), generated from the FR-MCP-002 registration record, with ETag-based caching, rate limiting, and 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> expose <code>GET /.well-known/oauth-protected-resource</code> returning the gateway-aggregate PRM as <code>application/json; charset=utf-8</code> per RFC 9728 §3.1. Unauthenticated per DEC-896.</li></ol>\n<ol><li><strong>MUST</strong> include in the gateway-aggregate PRM the following fields per RFC 9728 §2:</li></ol>\n<ul><li><code>resource</code>: <code>https://api.cyberos.world/mcp/v1</code> (canonical gateway URI per DEC-898).</li><li><code>authorization_servers</code>: array of per-residency AUTH issuer URLs per DEC-899 (4 entries: us-1, eu-1, sg-1, vn-1).</li><li><code>bearer_methods_supported</code>: <code>[\"header\"]</code> per DEC-900 (header-only; body/query forbidden).</li><li><code>resource_signing_alg_values_supported</code>: <code>[\"RS256\", \"EdDSA\"]</code> per DEC-901.</li><li><code>resource_documentation</code>: <code>https://api.cyberos.world/mcp/v1/docs</code> per DEC-902.</li></ul>\n<ol><li><strong>MUST</strong> expose per-module PRM at <code>GET /.well-known/oauth-protected-resource/cyberos.{module}</code> for each module registered via FR-MCP-002 per DEC-897. The per-module PRM differs from the aggregate by:</li></ol>\n<ul><li><code>resource</code>: <code>https://api.cyberos.world/mcp/v1/{module}</code> per DEC-898.</li><li><code>scopes_supported</code>: array of tool-level scopes the module exposes (derived from FR-MCP-002 registration record per DEC-907 + DEC-905).</li><li>All other fields identical to aggregate (same auth servers, same bearer methods, same signing algs).</li></ul>\n<ol><li><strong>MUST</strong> GENERATE per-module PRMs from the FR-MCP-002 registration record per DEC-907. The <code>services/mcp/src/server_registry.rs</code> ModuleRegistration struct is extended with PRM-relevant fields (<code>exposed_scopes: Vec&lt;String&gt;</code>) populated at registration. The per-module PRM handler reads from this registry — never from a hand-written JSON file. If the registry has no entry for the requested <code>{module}</code>, return 404 + emit <code>mcp.prm_unknown_module_requested</code> informational row.</li></ol>\n<ol><li><strong>MUST NOT</strong> require authentication on PRM endpoints per RFC 9728 §3.1 + DEC-896 — metadata is public by spec. Other gateway protection layers (rate limit + Turnstile for non-discovery endpoints) DO NOT apply.</li></ol>\n<ol><li><strong>MUST</strong> include <code>ETag</code> header on PRM responses per DEC-903 + RFC 7232 §2.3. ETag is the SHA-256-truncated hex of the canonical-JSON body (first 16 hex chars). Client requests with <code>If-None-Match: &lt;etag&gt;</code> matching the current ETag receive <code>304 Not Modified</code> with no body.</li></ol>\n<ol><li><strong>MUST</strong> include <code>Cache-Control: max-age=3600</code> per DEC-903 (RFC 9728 §3.4 recommends caching but does not pin a value; 1h matches our hot-reload SLA for upstream config changes).</li></ol>\n<ol><li><strong>MUST</strong> detect drift between gateway-aggregate and per-module PRMs per DEC-906. The drift detector runs on every PRM serve: if the aggregate's <code>authorization_servers</code> or <code>resource_signing_alg_values_supported</code> differs from any per-module's (which should never happen — derived from same registry), emit <code>mcp.prm_aggregate_drift</code> sev-2 memory row + log to <code>prm_drift_log</code> table.</li></ol>\n<ol><li><strong>MUST</strong> define <code>prm_drift_log</code> table at migration <code>0005</code>: <code>(id BIGSERIAL PRIMARY KEY, detected_at TIMESTAMPTZ NOT NULL DEFAULT now(), aggregate_sha256 CHAR(64) NOT NULL, module_name TEXT NOT NULL, module_sha256 CHAR(64) NOT NULL, drift_fields TEXT[] NOT NULL)</code>. Append-only via REVOKE per feature-request-audit skill rule 12. RLS not required (system-tenant scope only — drift is global, not per-tenant).</li></ol>\n<ol><li><strong>MUST</strong> rate-limit PRM endpoints at 600 req/min/IP per DEC-908 (clients should cache; high rate signals misbehaving client). Excess returns <code>429</code> + <code>Retry-After</code>.</li></ol>\n<ol><li><strong>MUST</strong> complete PRM response in &lt; 50 ms p95 per DEC-909. The PRM bodies are small (&lt;2 KB) and built from in-memory registry data; sub-50ms is straightforward. OTel histogram <code>mcp_prm_serve_duration_seconds</code>; alarm sev-3 if p95 &gt; 200 ms sustained 5 min.</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit row kinds (DEC-906 + feature-request-audit skill rule 6 namespace pattern):</li></ol>\n<ul><li><code>mcp.prm_served</code> (sev-3 — high-volume; sampled at 1% per feature-request-audit skill tail-sampling pattern via FR-OBS-006)</li><li><code>mcp.prm_unknown_module_requested</code> (sev-3 — informational; could indicate client misconfig or scanner)</li><li><code>mcp.prm_aggregate_drift</code> (sev-2 — should never happen; indicates registry corruption)</li></ul>\n<ol><li><strong>MUST</strong> be Content-Type <code>application/json</code> with <code>charset=utf-8</code> per DEC-904 + RFC 9728 §3.1.</li></ol>\n<ol><li><strong>MUST</strong> include the <code>X-Robots-Tag: noindex</code> header on PRM responses to prevent search-engine indexing (defense-in-depth — PRM is metadata for OAuth clients, not for general web crawling).</li></ol>\n<ol><li><strong>MUST</strong> support CORS for cross-origin client fetches: <code>Access-Control-Allow-Origin: *</code> (per RFC 9728 — metadata is public + same content for every requester), <code>Access-Control-Allow-Methods: GET, HEAD</code>, <code>Access-Control-Max-Age: 3600</code>.</li></ol>\n<ol><li><strong>MUST</strong> respond to HEAD requests with the same headers as GET (status, ETag, Cache-Control, Content-Length) but empty body. Standard HTTP behaviour required by CORS preflight + some HTTP clients.</li></ol>\n<ol><li><strong>MUST</strong> invalidate the cached PRM body + ETag when the FR-MCP-002 registry changes (module register / heartbeat / unregister). Implementation: registry change publishes to NATS subject <code>cyberos.mcp.registry.changed</code>; PRM cache subscriber rebuilds + bumps ETag.</li></ol>\n<ol><li><strong>MUST</strong> route per-module PRM 404 (unknown module) via the same RLS/rate-limit middleware as the gateway-aggregate path — do not branch the middleware. Single ingress.</li></ol>\n<ol><li><strong>MUST</strong> emit the <code>WWW-Authenticate</code> resource-metadata header on 401 responses from FR-MCP-001 endpoints per MCP spec §2.1, pointing at this FR's PRM URL. Implementation note for FR-MCP-001 integration (this FR provides the endpoint; FR-MCP-001 references it).</li></ol>\n<ol><li><strong>SHOULD</strong> include <code>client_uri</code> field in the PRM pointing at <code>https://cyberos.world/mcp/clients</code> (a public landing page documenting registered MCP clients per DEC-902 derivative). OPTIONAL; included for client discoverability.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why PRM at all (§1 #1)?</strong> RFC 9728 is the OAuth 2.0 protected-resource discovery standard. MCP spec 2025-11-25 §2.1 mandates it: when an MCP client receives a 401, the response includes <code>WWW-Authenticate: Bearer resource_metadata=&lt;url&gt;</code>, the client fetches that URL, gets the PRM, and learns (a) which authorization server to talk to, (b) which signing algorithms are accepted, (c) which scopes exist. Without PRM, every MCP client requires hand-configuration per CyberOS deployment — non-scalable.</p>\n<p><strong>Why per-residency authorization_servers list (§1 #2, DEC-899)?</strong> FR-AUTH-004 mints residency-pinned JWTs (eu-1 tenants get tokens from auth.eu-1; us-1 from auth.us-1). PRM advertises all 4 issuers; the client picks the one matching their tenant's residency. Wrong-issuer token = 401 at the gateway with a clearer error message.</p>\n<p><strong>Why generate per-module PRMs from registry (§1 #4, DEC-907)?</strong> Single source of truth. Hand-writing per-module PRM JSON means drift — a module ships a new tool with a new scope, the PRM doesn't update, clients can't discover the scope, the tool is silently un-callable. Generating from FR-MCP-002 registration ensures advertised scopes always match registered tools.</p>\n<p><strong>Why ETag + 1h cache (§1 #6, §1 #7, DEC-903)?</strong> PRM is high-traffic (every MCP client fetches at startup + periodically refreshes). ETag enables <code>If-None-Match</code> 304 responses — 100 bytes of header instead of 2 KB of body. 1h cache reduces server load while matching our hot-reload SLA (registry changes propagate within 1h worst-case; faster via NATS invalidation per §1 #17).</p>\n<p><strong>Why bearer method header-only (§1 #2, DEC-900)?</strong> OAuth 2.1 + MCP spec both deprecate <code>body</code> and <code>query</code> bearer methods (token in URL = log leakage; token in body = breaks idempotent GETs). Header-only is the modern standard. Advertising other methods would invite client implementations that we'd then need to reject.</p>\n<p><strong>Why <code>noindex</code> (§1 #14)?</strong> PRM is metadata for OAuth clients, not for search engines. While the content isn't sensitive, indexing it adds noise to web search results pointing at API endpoints (confusing for non-developers who Google \"cyberos api\"). Defense-in-depth on metadata exposure.</p>\n<p><strong>Why drift detection (§1 #8, DEC-906)?</strong> The aggregate-PRM and per-module-PRMs SHOULD always agree on shared fields (auth servers, signing algs). If they don't, the registry is corrupted or a code change introduced a divergence path. Emitting sev-2 alerts forces operator review before clients see inconsistent metadata.</p>\n<p><strong>Why CORS <code>*</code> (§1 #15)?</strong> PRM is public + identical for every requester (no per-client variance). Restricting CORS gains no security and breaks cross-origin client implementations (browser-based MCP clients) which are common.</p>\n<p><strong>Why rate-limit at 600/min (§1 #10, DEC-908)?</strong> Generous for legitimate clients (cache hit ratio ~99%; misses on 1h boundaries means ~1 req/h/client). 600/min = 1 req per 100ms which only a misbehaving client (no cache, retry loop) would hit. Block sooner = false positives on legitimate clients during configuration changes.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0005_prm_drift_log.sql\nCREATE TABLE prm_drift_log (\n  id BIGSERIAL PRIMARY KEY,\n  detected_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  aggregate_sha256 CHAR(64) NOT NULL,\n  module_name TEXT NOT NULL,\n  module_sha256 CHAR(64) NOT NULL,\n  drift_fields TEXT[] NOT NULL,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_prm_drift_detected_at ON prm_drift_log(detected_at);\nREVOKE UPDATE, DELETE ON prm_drift_log FROM cyberos_app;\n-- RLS not needed: system-tenant scope only (drift is a global system event).</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/mcp/src/prm/mod.rs\n#[derive(serde::Serialize, Debug)]\npub struct PrmDocument {\n    pub resource: String,\n    pub authorization_servers: Vec&lt;String&gt;,\n    pub bearer_methods_supported: Vec&lt;String&gt;,\n    pub resource_signing_alg_values_supported: Vec&lt;String&gt;,\n    pub resource_documentation: String,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub scopes_supported: Option&lt;Vec&lt;String&gt;&gt;,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub client_uri: Option&lt;String&gt;,\n}\n\nimpl PrmDocument {\n    pub fn canonical_etag(&amp;self) -&gt; String {\n        let body = serde_json::to_vec(self).unwrap();  // serde sorts keys\n        let digest = sha2::Sha256::digest(&amp;body);\n        hex::encode(&amp;digest[..8])  // 16-char hex\n    }\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">GET    /.well-known/oauth-protected-resource                    (public, gateway-aggregate)\nGET    /.well-known/oauth-protected-resource/cyberos.{module}   (public, per-module)\nHEAD   /.well-known/oauth-protected-resource                    (same as GET, empty body)\nHEAD   /.well-known/oauth-protected-resource/cyberos.{module}\nOPTIONS /.well-known/oauth-protected-resource(/cyberos.{module})  (CORS preflight)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Gateway PRM unauthenticated</strong> — GET without Authorization header returns 200 + valid PRM JSON.</li><li><strong>PRM JSON conforms to RFC 9728</strong> — body matches RFC 9728 §2 schema; fields <code>resource</code>, <code>authorization_servers</code>, <code>bearer_methods_supported</code>, <code>resource_signing_alg_values_supported</code> present.</li><li><strong>Per-module PRM derived from registry</strong> — module registered with <code>exposed_scopes=[\"projects.read\",\"projects.write\"]</code> returns PRM with that exact <code>scopes_supported</code> array.</li><li><strong>Unknown module 404</strong> — GET <code>/.well-known/oauth-protected-resource/cyberos.bogus</code> returns 404 + emits <code>mcp.prm_unknown_module_requested</code>.</li><li><strong>ETag returned + 304 on match</strong> — first GET returns 200 + ETag; second GET with <code>If-None-Match: &lt;etag&gt;</code> returns 304 + no body.</li><li><strong>Cache-Control: max-age=3600</strong> — header present on every 200 response.</li><li><strong>Bearer methods header-only</strong> — PRM <code>bearer_methods_supported</code> = <code>[\"header\"]</code> exactly.</li><li><strong>Signing algs RS256+EdDSA</strong> — PRM <code>resource_signing_alg_values_supported</code> = <code>[\"RS256\",\"EdDSA\"]</code> exactly.</li><li><strong>CORS headers present</strong> — <code>Access-Control-Allow-Origin: *</code> + <code>Methods: GET, HEAD</code> + <code>Max-Age: 3600</code>.</li><li><strong>HEAD returns headers + empty body</strong> — HEAD response Content-Length matches GET body length but body is empty.</li><li><strong>noindex header</strong> — <code>X-Robots-Tag: noindex</code> on every PRM response.</li><li><strong>Drift detected when registry divergence injected</strong> — manual test injects divergent module PRM → <code>mcp.prm_aggregate_drift</code> sev-2 emitted + <code>prm_drift_log</code> row written.</li><li><strong>Rate limit at 600/min/IP</strong> — 601st request in 60s returns 429.</li><li><strong>P95 latency &lt; 50ms</strong> — synthetic load test with 1000 requests; p95 &lt; 50ms.</li><li><strong>PRM cache invalidated on registry change</strong> — module register/unregister event triggers ETag bump within 1 s.</li><li><strong><code>mcp.prm_served</code> row emitted at 1% sample</strong> — 1000 sampled requests produces ~10 audit rows (FR-OBS-006 tail-sampling).</li><li><strong>OAuth issuer URL list matches FR-AUTH-004 residency map</strong> — assertion against AUTH-004 <code>RESIDENCY_ISSUERS</code> const.</li><li><strong><code>resource</code> URI matches FR-MCP-004 audience-binding</strong> — assertion against FR-MCP-004's audience-bound JWT <code>aud</code> claim.</li><li><strong>Content-Type application/json; charset=utf-8</strong> — header present + correct.</li><li><strong>Registry-write fails if registration lacks <code>exposed_scopes</code></strong> — FR-MCP-002 ModuleRegistration validation rejects missing field.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>prm_gateway_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn gateway_prm_unauth_and_well_formed() {\n    let ctx = TestContext::new().await;\n    let r = ctx.get(\"/.well-known/oauth-protected-resource\").send().await.unwrap();\n    assert_eq!(r.status(), 200);\n    assert_eq!(r.headers()[\"content-type\"], \"application/json; charset=utf-8\");\n    assert!(r.headers().contains_key(\"etag\"));\n    assert_eq!(r.headers()[\"cache-control\"], \"max-age=3600\");\n    assert_eq!(r.headers()[\"x-robots-tag\"], \"noindex\");\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"resource\"], \"https://api.cyberos.world/mcp/v1\");\n    assert_eq!(body[\"bearer_methods_supported\"], serde_json::json!([\"header\"]));\n    assert_eq!(body[\"resource_signing_alg_values_supported\"], serde_json::json!([\"RS256\",\"EdDSA\"]));\n    let issuers = body[\"authorization_servers\"].as_array().unwrap();\n    assert_eq!(issuers.len(), 4);\n}</code></pre>\n<h3>5.2 <code>prm_per_module_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn per_module_prm_derived_from_registry() {\n    let ctx = TestContext::new().await;\n    ctx.register_module(\"projects\", vec![\"projects.read\",\"projects.write\",\"projects.delete\"]).await;\n    let r = ctx.get(\"/.well-known/oauth-protected-resource/cyberos.projects\").send().await.unwrap();\n    assert_eq!(r.status(), 200);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"resource\"], \"https://api.cyberos.world/mcp/v1/projects\");\n    assert_eq!(body[\"scopes_supported\"], serde_json::json!([\"projects.read\",\"projects.write\",\"projects.delete\"]));\n}</code></pre>\n<h3>5.3 <code>prm_etag_cache_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn etag_returns_304_on_match() {\n    let ctx = TestContext::new().await;\n    let r1 = ctx.get(\"/.well-known/oauth-protected-resource\").send().await.unwrap();\n    let etag = r1.headers()[\"etag\"].to_str().unwrap().to_owned();\n    let r2 = ctx.get(\"/.well-known/oauth-protected-resource\")\n        .header(\"if-none-match\", &amp;etag).send().await.unwrap();\n    assert_eq!(r2.status(), 304);\n    assert_eq!(r2.bytes().await.unwrap().len(), 0);\n}</code></pre>\n<h3>5.4 <code>prm_unknown_module_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn unknown_module_404_audited() {\n    let ctx = TestContext::new().await;\n    let r = ctx.get(\"/.well-known/oauth-protected-resource/cyberos.bogus\").send().await.unwrap();\n    assert_eq!(r.status(), 404);\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"mcp.prm_unknown_module_requested\"));\n}</code></pre>\n<h3>5.5 <code>prm_drift_detection_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn drift_emits_sev2_alert() {\n    let ctx = TestContext::new().await;\n    ctx.register_module(\"projects\", vec![\"projects.read\"]).await;\n    ctx.inject_per_module_signing_alg_drift(\"projects\", vec![\"HS256\"]).await;  // bad: HS256\n    let _ = ctx.get(\"/.well-known/oauth-protected-resource/cyberos.projects\").send().await.unwrap();\n    let audit = ctx.memory_rows().await;\n    let drift = audit.iter().find(|r| r.kind == \"mcp.prm_aggregate_drift\").unwrap();\n    assert_eq!(drift.severity, 2);\n    assert!(drift.payload[\"drift_fields\"].as_array().unwrap()\n        .iter().any(|f| f == \"resource_signing_alg_values_supported\"));\n\n    let drift_count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM prm_drift_log\").fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(drift_count, 1);\n}</code></pre>\n<h3>5.6 <code>prm_rate_limit_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn rate_limit_at_600_per_min() {\n    let ctx = TestContext::new().await;\n    for _ in 0..600 {\n        let r = ctx.get(\"/.well-known/oauth-protected-resource\").send().await.unwrap();\n        assert_eq!(r.status(), 200);\n    }\n    let r = ctx.get(\"/.well-known/oauth-protected-resource\").send().await.unwrap();\n    assert_eq!(r.status(), 429);\n}</code></pre>\n<h3>5.7 <code>prm_unauth_public_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn prm_responds_without_authentication() {\n    let ctx = TestContext::new().await;\n    // No Authorization header\n    let r = ctx.get(\"/.well-known/oauth-protected-resource\").send().await.unwrap();\n    assert_eq!(r.status(), 200);\n    // CORS preflight\n    let opt = ctx.request(reqwest::Method::OPTIONS, \"/.well-known/oauth-protected-resource\")\n        .header(\"origin\", \"https://example.com\").send().await.unwrap();\n    assert_eq!(opt.headers()[\"access-control-allow-origin\"], \"*\");\n}</code></pre>\n<h3>5.8 <code>prm_audit_emission_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn prm_served_sampled_at_1_percent() {\n    let ctx = TestContext::with_tail_sampling(1.0).await;  // 100% in test for determinism\n    for _ in 0..10 {\n        ctx.get(\"/.well-known/oauth-protected-resource\").send().await.unwrap();\n    }\n    let audit = ctx.memory_rows().await;\n    assert_eq!(audit.iter().filter(|r| r.kind == \"mcp.prm_served\").count(), 10);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract in §3 is the skeleton. The handler is trivially short.)</p>\n<pre><code class=\"language-rust\">// services/mcp/src/prm/gateway.rs\npub async fn build_gateway_prm(ctx: &amp;AppCtx) -&gt; PrmDocument {\n    PrmDocument {\n        resource: \"https://api.cyberos.world/mcp/v1\".to_string(),\n        authorization_servers: ctx.auth.residency_issuers(),  // 4 URLs from FR-AUTH-004\n        bearer_methods_supported: vec![\"header\".into()],\n        resource_signing_alg_values_supported: vec![\"RS256\".into(), \"EdDSA\".into()],\n        resource_documentation: \"https://api.cyberos.world/mcp/v1/docs\".into(),\n        scopes_supported: None,\n        client_uri: Some(\"https://cyberos.world/mcp/clients\".into()),\n    }\n}\n\n// services/mcp/src/prm/per_module.rs\npub async fn build_per_module_prm(ctx: &amp;AppCtx, module: &amp;str) -&gt; Option&lt;PrmDocument&gt; {\n    let reg = ctx.registry.get_module(module).await?;\n    Some(PrmDocument {\n        resource: format!(\"https://api.cyberos.world/mcp/v1/{module}\"),\n        authorization_servers: ctx.auth.residency_issuers(),\n        bearer_methods_supported: vec![\"header\".into()],\n        resource_signing_alg_values_supported: vec![\"RS256\".into(), \"EdDSA\".into()],\n        resource_documentation: \"https://api.cyberos.world/mcp/v1/docs\".into(),\n        scopes_supported: Some(reg.exposed_scopes),\n        client_uri: Some(\"https://cyberos.world/mcp/clients\".into()),\n    })\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>FR-MCP-004</strong> OAuth 2.1 PKCE — audience-bound tokens require this discovery endpoint to be useful.</li></ul>\n<p><strong>Cross-module (related_frs):</strong></p>\n<ul><li><strong>FR-MCP-001</strong> spec compliance — <code>WWW-Authenticate: Bearer resource_metadata=&lt;this-FR's-URL&gt;</code> referenced from 401 responses.</li><li><strong>FR-MCP-002</strong> per-module registration — registry feeds per-module PRM <code>scopes_supported</code>.</li><li><strong>FR-MCP-006</strong> Tool-annotation gating — scopes advertised here reflect the tool-annotation set.</li><li><strong>FR-MCP-007</strong> Tasks primitive — tasks tools' scopes appear in per-module PRM.</li><li><strong>FR-AUTH-004</strong> JWT mint — <code>authorization_servers</code> list matches FR-AUTH-004 residency issuers.</li><li><strong>FR-AUTH-104</strong> OIDC SSO — IdP discovery follows analogous well-known pattern.</li><li><strong>FR-AI-003</strong> memory audit-row bridge — 3 new kinds register.</li><li><strong>FR-OBS-005</strong> Trace correlation — <code>mcp.prm_served</code> sampled via OBS tail-sampling.</li></ul>\n<p><strong>Downstream (blocks):</strong> None at this slice. Future per-tool authz hooks (FR-MCP-2xx) may consume PRM.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 Gateway PRM response</h3>\n<pre><code class=\"language-http\">HTTP/1.1 200 OK\nContent-Type: application/json; charset=utf-8\nETag: \"f8a1b2c3d4e5f607\"\nCache-Control: max-age=3600\nX-Robots-Tag: noindex\nAccess-Control-Allow-Origin: *\nAccess-Control-Allow-Methods: GET, HEAD\nAccess-Control-Max-Age: 3600\n\n{\n  \"resource\": \"https://api.cyberos.world/mcp/v1\",\n  \"authorization_servers\": [\n    \"https://auth.us-1.cyberos.world\",\n    \"https://auth.eu-1.cyberos.world\",\n    \"https://auth.sg-1.cyberos.world\",\n    \"https://auth.vn-1.cyberos.world\"\n  ],\n  \"bearer_methods_supported\": [\"header\"],\n  \"resource_signing_alg_values_supported\": [\"RS256\", \"EdDSA\"],\n  \"resource_documentation\": \"https://api.cyberos.world/mcp/v1/docs\",\n  \"client_uri\": \"https://cyberos.world/mcp/clients\"\n}</code></pre>\n<h3>8.2 Per-module PRM response</h3>\n<pre><code class=\"language-http\">HTTP/1.1 200 OK\nContent-Type: application/json; charset=utf-8\nETag: \"9c4e7a8b6d2f1e3a\"\n...\n\n{\n  \"resource\": \"https://api.cyberos.world/mcp/v1/projects\",\n  \"authorization_servers\": [\"https://auth.us-1.cyberos.world\", ...],\n  \"bearer_methods_supported\": [\"header\"],\n  \"resource_signing_alg_values_supported\": [\"RS256\", \"EdDSA\"],\n  \"resource_documentation\": \"https://api.cyberos.world/mcp/v1/docs\",\n  \"scopes_supported\": [\"projects.read\", \"projects.write\", \"projects.delete\"],\n  \"client_uri\": \"https://cyberos.world/mcp/clients\"\n}</code></pre>\n<h3>8.3 <code>mcp.prm_aggregate_drift</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"mcp.prm_aggregate_drift\",\n  \"severity\": 2,\n  \"tenant_id\": \"00000000-0000-0000-0000-000000000001\",\n  \"actor_id\": \"system.mcp.prm\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"module_name\": \"projects\",\n    \"aggregate_sha256\": \"ab12...\",\n    \"module_sha256\": \"cd34...\",\n    \"drift_fields\": [\"resource_signing_alg_values_supported\"]\n  }\n}</code></pre>\n<h3>8.4 401 from FR-MCP-001 endpoint pointing at PRM</h3>\n<pre><code class=\"language-http\">HTTP/1.1 401 Unauthorized\nWWW-Authenticate: Bearer realm=\"cyberos-mcp\", resource_metadata=\"https://api.cyberos.world/.well-known/oauth-protected-resource\"\nContent-Type: application/json\n\n{ \"error\": \"invalid_token\", \"error_description\": \"Token expired or wrong audience\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Per-tenant PRM override (vanity URLs at <code>&lt;slug&gt;.cyberos.world/.well-known/...</code>) — slice 3, FR-MCP-2xx.</li><li><strong>Deferred:</strong> Multi-language <code>resource_documentation</code> URLs — slice 3.</li><li><strong>Deferred:</strong> Localised PRM scope descriptions (<code>scopes_supported_descriptions</code>) — slice 3.</li><li><strong>Deferred:</strong> PRM signing per RFC 9728 §3.5 (signed metadata) — slice 3; current trust model is HTTPS-only.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Unknown module requested</td><td>registry lookup miss</td><td>404 + <code>mcp.prm_unknown_module_requested</code></td><td>Client refreshes their module list via gateway-aggregate PRM (which lists modules)</td></tr><tr><td>Drift between aggregate + per-module</td><td>drift detector cmp at serve</td><td>sev-2 <code>mcp.prm_aggregate_drift</code> + <code>prm_drift_log</code> row</td><td>Operator investigates registry corruption; manual config rebuild</td></tr><tr><td>PRM cache stale after registry change</td><td>NATS invalidation event missed</td><td>Up to 1h staleness (Cache-Control)</td><td>NATS-side replay; manual cache flush via <code>cyberos-mcp prm-cache-flush</code> CLI</td></tr><tr><td>Rate limit hit</td><td>counter ≥ 600/min</td><td>429 + <code>Retry-After: 60</code></td><td>Client backs off; investigate cache misconfig</td></tr><tr><td>Invalid <code>If-None-Match</code> header (malformed)</td><td>parse fails</td><td>Treated as no-If-None-Match; serve 200 + body</td><td>Inherent — robust to malformed clients</td></tr><tr><td>Registry returns empty <code>authorization_servers</code></td><td>empty list</td><td>503 + sev-1 alert <code>mcp.prm_no_auth_servers</code></td><td>Operator verifies FR-AUTH-004 residency config</td></tr><tr><td>Disk I/O failure when writing <code>prm_drift_log</code></td><td>Postgres write error</td><td>sev-2 alert; PRM still served (don't fail request on audit failure)</td><td>Postgres recovers; manual drift re-emit via CLI</td></tr><tr><td>OPTIONS request with disallowed Method header</td><td>CORS handler check</td><td>200 + allowed-Methods header (no error; explicit reject not needed)</td><td>Inherent CORS conformance</td></tr><tr><td>ETag collision (same SHA-256 across different bodies — astronomically unlikely)</td><td>client receives 304 with wrong body</td><td>Client sees stale PRM until cache expiry</td><td>Inherent — 64-bit truncation enough for 1h cache scope</td></tr><tr><td>Module registers with malformed scope name (invalid chars)</td><td>FR-MCP-002 validation</td><td>Registration rejected upstream; PRM never sees bad data</td><td>FR-MCP-002 owns validation</td></tr><tr><td>HEAD request fails to set Content-Length</td><td>handler bug</td><td>Test asserts Content-Length matches GET body</td><td>CI catches; fix</td></tr><tr><td><code>X-Robots-Tag</code> header missing</td><td>regression</td><td>Test asserts presence</td><td>CI catches</td></tr><tr><td>FR-AUTH-004 residency issuer list mid-rotation</td><td>aggregate PRM advertises stale issuer</td><td>Client gets 401 from old issuer; falls back to next</td><td>Inherent — multi-issuer list provides resilience</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> PRM body is canonical JSON (serde_json with sort_keys via custom serializer) so ETag SHA-256 is deterministic. Without sorting, different request handlers' bodies hash differently.</p>\n<p><strong>§11.2</strong> ETag truncation to 16 hex chars (64 bits) is enough for 1h-cache scope; risk of collision = ~10⁻¹⁰ per cache lifetime. Full SHA-256 hex (64 chars) is overkill.</p>\n<p><strong>§11.3</strong> The <code>Cache-Control: max-age=3600</code> is independent of ETag — clients can cache for 1h, then revalidate with ETag. Aggressive clients hit our endpoint once an hour; conservative clients hit only on miss.</p>\n<p><strong>§11.4</strong> The CORS preflight OPTIONS response is cached by browsers for 3600s (matching <code>Access-Control-Max-Age</code>). Eliminates preflight chatter on subsequent requests.</p>\n<p><strong>§11.5</strong> <code>Access-Control-Allow-Headers</code> is intentionally NOT set — PRM responses don't require any non-standard request headers (Authorization is optional + safelisted via CORS spec).</p>\n<p><strong>§11.6</strong> Per-module PRM Content-Type is identical to gateway. Content negotiation (Accept header) not supported — RFC 9728 mandates application/json.</p>\n<p><strong>§11.7</strong> Drift detection compares canonical-JSON SHA-256 of the SHARED fields only (not the full body — <code>scopes_supported</code> differs by design). Implementation: extract shared field subset, canonical-serialize, hash.</p>\n<p><strong>§11.8</strong> The <code>cyberos-mcp prm-cache-flush</code> CLI (slice 3 future) clears the in-memory cache + bumps the ETag base. Slice 2 has no CLI — NATS invalidation is the only flush mechanism.</p>\n<p><strong>§11.9</strong> Per-module PRM at unknown module returns 404, NOT 200 with empty <code>scopes_supported</code>. Differentiation lets clients distinguish \"module exists but has no scopes\" (empty list) from \"module doesn't exist\" (404).</p>\n<p><strong>§11.10</strong> The <code>prm_drift_log</code> table has no RLS (system-tenant scope only) because PRM is a system-level concept — no tenant owns metadata drift. Operator-only access via separate audit role.</p>\n<p><strong>§11.11</strong> Server registration records (<code>ModuleRegistration</code>) extended with <code>exposed_scopes: Vec&lt;String&gt;</code>; FR-MCP-002 modified_files lists the type update.</p>\n<p><strong>§11.12</strong> Tail-sampling of <code>mcp.prm_served</code> rows (1% per feature-request-audit skill / FR-OBS-006) keeps the memory chain manageable at high request volume. The two other kinds are always emitted (low volume by design).</p>\n<p><strong>§11.13</strong> The <code>X-Robots-Tag: noindex</code> header also includes <code>, nofollow, nosnippet</code> per Google's documented best-practice for metadata endpoints.</p>\n<p><strong>§11.14</strong> Registry change events on NATS subject <code>cyberos.mcp.registry.changed</code> carry the changed module name; PRM cache subscriber rebuilds just that module's PRM (not all) for efficiency at scale.</p>\n<p><strong>§11.15</strong> The empty body on HEAD is enforced by axum middleware — Content-Length is set from a dry-run serialize without writing the body.</p>\n<hr>\n<p><em>End of FR-MCP-005 spec.</em></p>";
