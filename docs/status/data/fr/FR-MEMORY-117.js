window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-117"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The store-ACL layer <strong>MUST</strong> sit between the canonical <code>cyberos.core.writer.Writer</code> and the filesystem. Every <code>put</code> / <code>move</code> / <code>delete</code> operation is checked against the relevant <code>STORE.yaml</code> before the row is appended. The contract:</p>\n<ol><li><strong>MUST</strong> enforce ACLs on every write through <code>Writer</code>. The check runs after path validation (§3.3 of AGENTS.md) and before the audit-chain append.</li><li><strong>MUST NOT</strong> enforce ACLs on reads. Reads are unrestricted to all local processes that can read the filesystem (DEC-232). OS file permissions are the operator's responsibility, not the protocol's.</li><li><strong>MUST</strong> locate the active <code>STORE.yaml</code> by walking from the target path UP toward <code>&lt;memory-root&gt;/</code>. The first <code>STORE.yaml</code> encountered governs; further-up <code>STORE.yaml</code> files are ignored. This lets <code>memories/episodes/STORE.yaml</code> override a hypothetical <code>memories/STORE.yaml</code>. If no <code>STORE.yaml</code> is found anywhere up to the root, the default is permissive (<code>read-write</code> for all actors) — preserving back-compat for stores predating this FR.</li><li><strong>MUST</strong> define <code>STORE.yaml</code> shape: ```yaml store_id: org-wide-knowledge default_mode: read        # read | read-write | deny ; applied when no acl entry matches acl:</li></ol>\n<ul><li>{actor: \"*\",                  mode: \"read\"}</li><li>{actor: \"stephen@*\",          mode: \"read-write\"}</li><li>{actor: \"dream-runner\",       mode: \"read-write\"}</li><li>{actor: \"scheduled-importer\", mode: \"deny\"} ```</li></ul>\n<ol><li><strong>MUST</strong> resolve <code>actor</code> matches with glob-style matching against the active actor identity (<code>--actor &lt;name&gt;</code> flag on CLI, or <code>Writer(actor=...)</code> arg). First-match-wins, IN ORDER as written in the YAML. An explicit <code>deny</code> always blocks regardless of <code>default_mode</code>.</li><li><strong>MUST</strong> support these built-in actor literals (no glob needed):</li></ol>\n<ul><li><code>*</code> — wildcard (matches any actor)</li><li><code>dream-runner</code> — FR-MEMORY-115's dream runner identity</li><li><code>dream-applier</code> — FR-MEMORY-115's dream applier identity</li><li><code>scheduled-importer</code> — reserved for cron-driven <code>cyberos import</code> invocations</li><li><code>claude-code-hook</code> — FR-MEMORY-109's hook identity</li><li>any literal string matching <code>^[a-z][a-z0-9_-]*$</code> for custom actor ids</li><li>glob form <code>user@*</code> or <code>*@example.com</code> matches the literal \"@\" delimiter</li></ul>\n<ol><li><strong>MUST</strong> reject writes that resolve to <code>deny</code> or that resolve to <code>read</code> with structured error <code>{outcome: \"rejected\", reason: \"acl_denied\", actor: ..., path: ..., store_id: ...}</code>. Rejected writes do NOT advance HEAD and do NOT emit <code>put</code>/<code>move</code>/<code>delete</code> rows — but they MUST emit a <code>memory.acl_denied</code> aux audit row capturing the attempt. The aux row's payload is the same struct as the error.</li><li><strong>MUST</strong> handle <code>move(src, dst)</code> by checking ACL on BOTH paths — write capability on <code>src</code> (to delete the old location) AND write capability on <code>dst</code> (to create the new location). Either failing blocks the move.</li><li><strong>MUST</strong> lazily load <code>STORE.yaml</code> on first write to its subtree and cache the parsed result in-memory keyed by <code>(store_dir, mtime_ns)</code>. On <code>STORE.yaml</code> mtime change (or content change detected by <code>cyberos doctor</code>), the cache entry is invalidated automatically on next access.</li><li><strong>MUST</strong> ship a migration script <code>scripts/migrate-store-acl.py</code> that walks <code>&lt;memory-root&gt;/</code> and writes a default permissive <code>STORE.yaml</code> for every top-level dir that doesn't have one. Migration is idempotent — existing <code>STORE.yaml</code> files are never overwritten. Supports <code>--dry-run</code> (prints planned writes) and <code>--scope &lt;dir&gt;</code> (limit to one subtree).</li><li><strong>MUST</strong> validate <code>STORE.yaml</code> shape against <code>memory.schema.json#/definitions/StoreAcl</code> at parse time. Invalid YAML or invalid shape → walker invariant <code>store-yaml-acl-valid</code> fails with structured error naming the file and the violation.</li><li><strong>MUST</strong> add <code>cyberos acl show [--store &lt;id&gt;]</code> (lists STORE.yaml content per store), <code>cyberos acl validate</code> (re-validates all STORE.yaml files), and <code>cyberos acl explain &lt;path&gt;</code> (resolves the effective mode for the active actor on a given path, with the matching ACL entry highlighted).</li><li><strong>MUST</strong> require the AGENTS.md §14.4 amendment APPROVED via <code>APPROVE protocol change P20 §14.4</code> chat-turn before runtime ACL enforcement is enabled. Same runtime-check pattern as FR-MEMORY-115's §7.7: the writer checks for the §14.4 anchor in AGENTS.md at construction; absent → ACL enforcement is in WARN-ONLY mode (rejections logged + aux row emitted, but writes proceed). Once §14.4 is anchored, full enforcement is active.</li><li><strong>MUST</strong> emit the <code>memory.acl_denied</code> aux row even in WARN-ONLY mode so operators can audit \"what would have been blocked once §14.4 lands.\"</li><li><strong>SHOULD</strong> support per-store <code>default_mode: deny</code> with explicit-allow ACL entries — useful for highly-sensitive stores (e.g. a <code>secrets/</code> subtree where only one actor is permitted). Slice-4 stretch — slice-3 ships <code>default_mode: read</code> and <code>read-write</code> only.</li><li><strong>SHOULD</strong> support <code>cyberos acl audit --since 24h</code> — surfaces all <code>memory.acl_denied</code> rows in the window with a summary of which actors attempted what. Slice-4 polish.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why subtree-rooted, not root-only (§1 #3).</strong> The talk's three-store example demonstrates the load-bearing property: different subtrees need different rules. A single root-level ACL conflates the rules; one operator typo cascades across the memory. Subtree-rooted means a typo in <code>memories/episodes/STORE.yaml</code> affects episodes only. Search is upward — the closest <code>STORE.yaml</code> wins, which makes operator mental model match filesystem mental model.</p>\n<p><strong>Why permissive default (§1 #3, DEC-230).</strong> Back-compat with existing memories that predate this FR. An operator upgrading should see zero behaviour change until they edit or migrate. The migration script (§1 #10) writes the default permissive <code>STORE.yaml</code> so the explicit shape is materialised for future edits, but the semantic remains \"everything works as before.\"</p>\n<p><strong>Why first-match-wins, with explicit-deny overriding (§1 #5, DEC-231).</strong> Iptables-style rules are operator-intuitive; ordered evaluation matches how operators read the file. Explicit <code>deny</code> is special-cased because \"first I want to block X, then permit everyone else\" is a common pattern that's awkward in pure first-match (it forces putting deny entries first AND repeating allow patterns).</p>\n<p><strong>Why writes-only enforcement (§1 #2, DEC-232).</strong> Two reasons. (a) Read enforcement requires kernel-level filesystem permissions, which conflate \"OS user\" with \"agent actor\" — the agent identity is a logical concept, not a unix uid. (b) The Anthropic talk frames <code>read-only</code> as \"the agent's writer rejects writes,\" not as \"the agent can't see the bytes.\" A separate file-permissions layer can be applied by the operator if they need uid-level isolation; that's out of scope for the memory protocol.</p>\n<p><strong>Why WARN-ONLY mode pre-amendment (§1 #13).</strong> Anti-footgun. An operator pulling the FR code but not running the APPROVE chat-turn shouldn't have their writes silently blocked overnight. WARN-ONLY mode is the \"ask forgiveness, not permission\" pattern adapted to protocol amendments: the code path exists, it logs what it would have done, the operator gets time to APPROVE before enforcement bites. Once §14.4 is anchored, enforcement is real.</p>\n<p><strong>Why <code>memory.acl_denied</code> aux row even on WARN-ONLY (§1 #14).</strong> The aux row is the audit signal. Operators inspecting \"what would have been denied?\" need a queryable log, not just stderr noise. Once enforcement bites, the aux rows are still useful — they record blocked attempts for post-hoc analysis.</p>\n<p><strong>Why <code>move</code> checks both paths (§1 #8).</strong> A <code>move(src, dst)</code> is conceptually a <code>delete(src) + put(dst)</code>. If <code>src</code> is in a read-only store, the delete half should fail; if <code>dst</code> is in a deny store, the put half should fail. Both checks catch operators who try to \"smuggle\" content across stores via move. The error names the failing side.</p>\n<p><strong>Why mtime-keyed cache (§1 #9).</strong> STORE.yaml files are rarely changed but frequently consulted. In-memory cache avoids re-parsing YAML on every write. mtime-based invalidation matches how <code>cyberos doctor</code> and FR-MEMORY-107 (FS watcher) detect external mutations.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Schema</h3>\n<pre><code class=\"language-json\">{\n  \"$defs\": {\n    \"StoreAclMode\": {\n      \"type\": \"string\",\n      \"enum\": [\"read\", \"read-write\", \"deny\"]\n    },\n    \"StoreAclEntry\": {\n      \"type\": \"object\",\n      \"required\": [\"actor\", \"mode\"],\n      \"properties\": {\n        \"actor\": {\"type\": \"string\", \"minLength\": 1},\n        \"mode\":  {\"$ref\": \"#/$defs/StoreAclMode\"}\n      }\n    },\n    \"StoreAcl\": {\n      \"type\": \"object\",\n      \"required\": [\"store_id\", \"acl\"],\n      \"properties\": {\n        \"store_id\":     {\"type\": \"string\", \"pattern\": \"^[a-z][a-z0-9_-]{0,63}$\"},\n        \"default_mode\": {\"$ref\": \"#/$defs/StoreAclMode\", \"default\": \"read-write\"},\n        \"acl\":          {\"type\": \"array\", \"items\": {\"$ref\": \"#/$defs/StoreAclEntry\"}}\n      }\n    }\n  }\n}</code></pre>\n<h3>Resolver</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/store_acl.py\nfrom __future__ import annotations\nimport fnmatch\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Literal, Optional\nimport yaml\n\nMode = Literal[\"read\", \"read-write\", \"deny\"]\n\n\n@dataclass(frozen=True)\nclass StoreAcl:\n    store_id:     str\n    default_mode: Mode\n    acl:          tuple[tuple[str, Mode], ...]    # ordered, first-match-wins\n\n    @classmethod\n    def from_yaml(cls, path: Path) -&gt; \"StoreAcl\":\n        raw = yaml.safe_load(path.read_text())\n        if not isinstance(raw, dict):\n            raise ValueError(f\"{path}: must be a YAML object\")\n        store_id     = raw[\"store_id\"]\n        default_mode = raw.get(\"default_mode\", \"read-write\")\n        entries      = raw.get(\"acl\", [])\n        return cls(\n            store_id=store_id, default_mode=default_mode,\n            acl=tuple((e[\"actor\"], e[\"mode\"]) for e in entries),\n        )\n\n    def resolve_mode(self, actor: str) -&gt; Mode:\n        for pattern, mode in self.acl:\n            if pattern == \"*\" or pattern == actor or fnmatch.fnmatchcase(actor, pattern):\n                return mode\n        return self.default_mode\n\n\ndef find_governing_store_yaml(memory_root: Path, target: Path) -&gt; Optional[Path]:\n    \"\"\"Walk UP from target until we find STORE.yaml or hit memory_root.\"\"\"\n    current = (memory_root / target).resolve().parent\n    memory_root = memory_root.resolve()\n    while current &gt;= memory_root and current.is_relative_to(memory_root):\n        candidate = current / \"STORE.yaml\"\n        if candidate.exists():\n            return candidate\n        if current == memory_root:\n            break\n        current = current.parent\n    return None\n\n\n@dataclass(frozen=True)\nclass AclResult:\n    allowed:        bool\n    mode:           Mode\n    store_id:       Optional[str]\n    yaml_path:      Optional[str]\n    matched_entry:  Optional[str]      # e.g. \"actor=stephen@*\"\n    reason:         Optional[str]\n\n\ndef check_write(memory_root: Path, target: Path, actor: str, warn_only: bool) -&gt; AclResult:\n    yml = find_governing_store_yaml(memory_root, target)\n    if yml is None:\n        return AclResult(allowed=True, mode=\"read-write\", store_id=None,\n                         yaml_path=None, matched_entry=None,\n                         reason=\"no STORE.yaml — permissive default\")\n    acl = StoreAcl.from_yaml(yml)\n    mode = acl.resolve_mode(actor)\n    if mode == \"read-write\":\n        return AclResult(allowed=True, mode=mode, store_id=acl.store_id,\n                         yaml_path=str(yml.relative_to(memory_root)),\n                         matched_entry=f\"actor matched against acl\",\n                         reason=None)\n    return AclResult(\n        allowed=warn_only,           # WARN-ONLY → \"allowed=True with reason\"\n        mode=mode,\n        store_id=acl.store_id,\n        yaml_path=str(yml.relative_to(memory_root)),\n        matched_entry=f\"actor={actor!r}\",\n        reason=f\"acl_denied:mode={mode}\" if not warn_only else f\"warn_only:mode={mode}\",\n    )</code></pre>\n<h3>AGENTS.md §14.4 amendment text (proposed)</h3>\n<pre><code class=\"language-text\">## §14.4  Store-level ACL (added by P20 — requires APPROVE chat-turn per §0.2)\n\n§14.4.1  Each subtree of `&lt;memory-root&gt;/` MAY declare a `STORE.yaml` file\nat its root. The file's shape is normative in `memory.schema.json#/$defs/StoreAcl`.\nSubtrees without a `STORE.yaml` inherit the default permissive policy\n(`{actor: \"*\", mode: \"read-write\"}`).\n\n§14.4.2  The canonical writer (`cyberos.core.writer.Writer`) MUST enforce\nACLs on every `put` / `move` / `delete` operation. ACL is resolved by\nwalking up from the target path to the nearest `STORE.yaml`. First-match-wins\non the `acl` list. Explicit `deny` always blocks regardless of subsequent\nallow patterns.\n\n§14.4.3  Reads are NOT subject to ACL enforcement at the protocol level.\nRead isolation is the operator's responsibility via OS filesystem permissions.\n\n§14.4.4  Rejected writes MUST emit a `memory.acl_denied` aux audit row with\npayload `{actor, target_path, store_id, yaml_path, mode, matched_entry,\nattempt_kind: \"put\"|\"move\"|\"delete\"}`. The audit row is emitted even in\nWARN-ONLY mode (where rejection is logged but write proceeds).\n\n§14.4.5  `move(src, dst)` MUST check ACL on BOTH `src` and `dst`. Either\nside failing blocks the operation.\n\n§14.4.6  The `INTEROP.md` consumer subset MUST honour `STORE.yaml` `acl`\nfor writes; reads MAY ignore.</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Permissive default when no STORE.yaml</strong> — write to <code>memories/x.md</code> with no STORE.yaml anywhere → succeeds; AclResult <code>allowed: true</code>. <em>(traces_to: §1 #3, DEC-230)</em></li><li><strong>Wildcard actor <code>*</code> matches</strong> — STORE.yaml <code>[{actor: \"*\", mode: \"read-write\"}]</code>; any actor succeeds. <em>(traces_to: §1 #5, §1 #6)</em></li><li><strong>Read-only mode rejects write</strong> — STORE.yaml <code>[{actor: \"*\", mode: \"read\"}]</code>; write attempt → AclResult <code>allowed: false</code>, mode <code>read</code>; <code>memory.acl_denied</code> aux row emitted. <em>(traces_to: §1 #7)</em></li><li><strong>Explicit deny overrides allow</strong> — <code>[{actor: \"scheduled-importer\", mode: \"deny\"}, {actor: \"*\", mode: \"read-write\"}]</code>; actor=<code>scheduled-importer</code> rejected. <em>(traces_to: §1 #5)</em></li><li><strong>Glob actor matching</strong> — <code>[{actor: \"stephen@*\", mode: \"read-write\"}, ...]</code>; actor=<code>stephen@example.com</code> matches; actor=<code>alice@example.com</code> doesn't. <em>(traces_to: §1 #5, §1 #6)</em></li><li><strong>Closest STORE.yaml wins</strong> — <code>memories/STORE.yaml</code> denies; <code>memories/episodes/STORE.yaml</code> allows; write to <code>memories/episodes/x.md</code> → allowed (closest wins). <em>(traces_to: §1 #3)</em></li><li><strong>First-match-wins on acl list</strong> — <code>[{actor: \"*\", mode: \"read\"}, {actor: \"stephen\", mode: \"read-write\"}]</code>; actor=stephen rejected (first wildcard matches first). <em>(traces_to: §1 #5)</em></li><li><strong>Default_mode applied when no entry matches</strong> — <code>default_mode: deny</code>; acl <code>[{actor: \"stephen\", mode: \"read-write\"}]</code>; actor=alice → deny. <em>(traces_to: §1 #4)</em></li><li><strong>Reject emits aux row</strong> — every denied write produces exactly one <code>memory.acl_denied</code> row with the documented payload shape. <em>(traces_to: §1 #7)</em></li><li><strong>Move respects both paths</strong> — src in read-only store, dst in read-write store → reject (src side fails); src in r-w, dst in deny → reject (dst side fails). <em>(traces_to: §1 #8)</em></li><li><strong>STORE.yaml mtime cache invalidation</strong> — write succeeds; STORE.yaml edited to deny; next write fails (cache invalidated on mtime change). <em>(traces_to: §1 #9)</em></li><li><strong>Migration script idempotent</strong> — existing STORE.yaml not overwritten; missing ones populated with permissive default. Re-run is no-op. <em>(traces_to: §1 #10)</em></li><li><strong>Migration --dry-run</strong> — <code>migrate-store-acl.py --dry-run</code> prints planned writes but creates no files. <em>(traces_to: §1 #10)</em></li><li><strong>STORE.yaml shape invalid → walker error</strong> — handcraft a malformed yaml → <code>cyberos doctor</code> fails with <code>store-yaml-acl-valid</code>. <em>(traces_to: §1 #11)</em></li><li><strong><code>cyberos acl show</code></strong> — lists store_id + acl for each STORE.yaml in the memory. <em>(traces_to: §1 #12)</em></li><li><strong><code>cyberos acl explain &lt;path&gt;</code></strong> — for a given path + active actor, outputs the resolved mode + matched entry. <em>(traces_to: §1 #12)</em></li><li><strong>WARN-ONLY without §14.4 amendment</strong> — AGENTS.md lacks §14.4; write that would be denied → writes proceed (allowed=True due to warn_only); <code>memory.acl_denied</code> aux row still emitted with <code>reason: \"warn_only:...\"</code>. <em>(traces_to: §1 #13, §1 #14)</em></li><li><strong>Enforcement when §14.4 present</strong> — AGENTS.md has §14.4 anchor; same denied write → writes refused. <em>(traces_to: §1 #13)</em></li><li><strong>Built-in actor literals</strong> — <code>dream-runner</code>, <code>dream-applier</code>, <code>scheduled-importer</code>, <code>claude-code-hook</code> all match by literal name without glob. <em>(traces_to: §1 #6)</em></li><li><strong>Symlinked memory_root</strong> — STORE.yaml lookup follows symlinks to find the real path. <em>(traces_to: §1 #3)</em></li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_store_acl.py\nimport pytest, yaml\nfrom pathlib import Path\nfrom cyberos.core.store_acl import StoreAcl, check_write, find_governing_store_yaml\n\n\ndef write_store_yaml(p: Path, acl, default=\"read-write\", store_id=\"test\"):\n    p.parent.mkdir(parents=True, exist_ok=True)\n    p.write_text(yaml.safe_dump({\"store_id\": store_id, \"default_mode\": default, \"acl\": acl}))\n\n\ndef test_permissive_default(tmp_memory):\n    \"\"\"AC #1\"\"\"\n    res = check_write(tmp_memory, Path(\"memories/x.md\"), actor=\"stephen\", warn_only=False)\n    assert res.allowed is True\n    assert res.yaml_path is None\n\n\ndef test_wildcard_actor(tmp_memory):\n    \"\"\"AC #2\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\",\n                     [{\"actor\": \"*\", \"mode\": \"read-write\"}])\n    res = check_write(tmp_memory, Path(\"memories/x.md\"), actor=\"stephen\", warn_only=False)\n    assert res.allowed is True\n\n\ndef test_read_only_blocks_write(tmp_memory):\n    \"\"\"AC #3\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\",\n                     [{\"actor\": \"*\", \"mode\": \"read\"}])\n    res = check_write(tmp_memory, Path(\"memories/x.md\"), actor=\"stephen\", warn_only=False)\n    assert res.allowed is False\n    assert res.mode == \"read\"\n\n\ndef test_explicit_deny_overrides_allow(tmp_memory):\n    \"\"\"AC #4\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\", [\n        {\"actor\": \"scheduled-importer\", \"mode\": \"deny\"},\n        {\"actor\": \"*\",                  \"mode\": \"read-write\"},\n    ])\n    res = check_write(tmp_memory, Path(\"memories/x.md\"),\n                      actor=\"scheduled-importer\", warn_only=False)\n    assert res.allowed is False\n    assert res.mode == \"deny\"\n\n\ndef test_glob_actor_matching(tmp_memory):\n    \"\"\"AC #5\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\", [\n        {\"actor\": \"stephen@*\", \"mode\": \"read-write\"},\n        {\"actor\": \"*\",         \"mode\": \"read\"},\n    ])\n    assert check_write(tmp_memory, Path(\"memories/x.md\"),\n                       actor=\"stephen@example.com\", warn_only=False).allowed\n    assert not check_write(tmp_memory, Path(\"memories/x.md\"),\n                            actor=\"alice@example.com\", warn_only=False).allowed\n\n\ndef test_closest_store_yaml_wins(tmp_memory):\n    \"\"\"AC #6\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\",\n                     [{\"actor\": \"*\", \"mode\": \"read\"}])           # outer = read\n    write_store_yaml(tmp_memory / \"memories/episodes/STORE.yaml\",\n                     [{\"actor\": \"*\", \"mode\": \"read-write\"}])     # inner = r-w\n    res = check_write(tmp_memory, Path(\"memories/episodes/x.md\"),\n                      actor=\"stephen\", warn_only=False)\n    assert res.allowed is True\n    assert \"episodes\" in res.yaml_path\n\n\ndef test_first_match_wins(tmp_memory):\n    \"\"\"AC #7\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\", [\n        {\"actor\": \"*\",       \"mode\": \"read\"},\n        {\"actor\": \"stephen\", \"mode\": \"read-write\"},\n    ])\n    res = check_write(tmp_memory, Path(\"memories/x.md\"), actor=\"stephen\", warn_only=False)\n    # First entry (wildcard read) wins, not the more-specific stephen entry\n    assert res.allowed is False\n\n\ndef test_default_mode_applied(tmp_memory):\n    \"\"\"AC #8\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\",\n                     [{\"actor\": \"stephen\", \"mode\": \"read-write\"}],\n                     default=\"deny\")\n    res = check_write(tmp_memory, Path(\"memories/x.md\"), actor=\"alice\", warn_only=False)\n    assert res.allowed is False\n    assert res.mode == \"deny\"\n\n\ndef test_warn_only_mode_allows_but_emits_aux(tmp_memory):\n    \"\"\"AC #17\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\",\n                     [{\"actor\": \"*\", \"mode\": \"read\"}])\n    res = check_write(tmp_memory, Path(\"memories/x.md\"), actor=\"stephen\", warn_only=True)\n    # In WARN-ONLY mode, allowed=True even when mode is read; reason names warn_only\n    assert res.allowed is True\n    assert res.reason.startswith(\"warn_only:\")\n\n\ndef test_built_in_actors_match_literally(tmp_memory):\n    \"\"\"AC #19\"\"\"\n    write_store_yaml(tmp_memory / \"memories/STORE.yaml\", [\n        {\"actor\": \"dream-runner\", \"mode\": \"read-write\"},\n        {\"actor\": \"*\", \"mode\": \"read\"},\n    ])\n    res = check_write(tmp_memory, Path(\"memories/x.md\"),\n                      actor=\"dream-runner\", warn_only=False)\n    assert res.allowed is True</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_store_acl.py\nimport pytest, yaml, subprocess\nfrom pathlib import Path\n\n\ndef test_migration_creates_permissive_default(tmp_memory):\n    \"\"\"AC #12\"\"\"\n    (tmp_memory / \"memories\").mkdir(exist_ok=True)\n    (tmp_memory / \"meta\").mkdir(exist_ok=True)\n    subprocess.run([\"python\", \"scripts/migrate-store-acl.py\",\n                    \"--store\", str(tmp_memory)], check=True)\n    for sub in (\"memories\", \"meta\"):\n        yml = tmp_memory / sub / \"STORE.yaml\"\n        assert yml.exists()\n        body = yaml.safe_load(yml.read_text())\n        assert body[\"acl\"][0][\"mode\"] == \"read-write\"\n\n\ndef test_migration_idempotent(tmp_memory):\n    \"\"\"AC #12\"\"\"\n    (tmp_memory / \"memories\").mkdir(exist_ok=True)\n    (tmp_memory / \"memories/STORE.yaml\").write_text(yaml.safe_dump({\n        \"store_id\": \"custom\",\n        \"default_mode\": \"deny\",\n        \"acl\": [{\"actor\": \"stephen\", \"mode\": \"read-write\"}],\n    }))\n    subprocess.run([\"python\", \"scripts/migrate-store-acl.py\",\n                    \"--store\", str(tmp_memory)], check=True)\n    body = yaml.safe_load((tmp_memory / \"memories/STORE.yaml\").read_text())\n    # Existing config NOT overwritten\n    assert body[\"store_id\"] == \"custom\"\n    assert body[\"default_mode\"] == \"deny\"\n\n\ndef test_migration_dry_run(tmp_memory):\n    \"\"\"AC #13\"\"\"\n    (tmp_memory / \"memories\").mkdir(exist_ok=True)\n    result = subprocess.run([\"python\", \"scripts/migrate-store-acl.py\",\n                             \"--store\", str(tmp_memory), \"--dry-run\"],\n                            capture_output=True, text=True, check=True)\n    assert not (tmp_memory / \"memories/STORE.yaml\").exists()\n    assert \"would create\" in result.stdout\n\n\ndef test_invalid_store_yaml_walker_error(tmp_memory):\n    \"\"\"AC #14\"\"\"\n    (tmp_memory / \"memories\").mkdir(exist_ok=True)\n    (tmp_memory / \"memories/STORE.yaml\").write_text(\":::not valid yaml:::\")\n    result = subprocess.run([\"python\", \"-m\", \"cyberos\", \"--store\", str(tmp_memory),\n                             \"doctor\"], capture_output=True, text=True)\n    assert result.returncode != 0\n    assert \"store-yaml-acl-valid\" in (result.stderr + result.stdout)</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>API contracts above are the skeleton. Order:</p>\n<ol><li>AGENTS.md §14.4 amendment text (DO NOT commit until APPROVE chat-turn).</li><li>Schema (<code>memory.schema.json</code>).</li><li>Walker invariant.</li><li><code>cyberos/core/store_acl.py</code> resolver.</li><li><code>cyberos/core/writer.py</code> integration with WARN-ONLY mode toggle on §14.4 anchor check.</li><li>Migration script.</li><li><code>cyberos acl</code> CLI subcommands.</li><li>Tests.</li><li>INTEROP.md one-liner.</li><li>CHANGELOG.</li></ol>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-115 (related)</strong> — dream rows respect store ACL; dream-runner / dream-applier are built-in actor literals.</li><li><strong>FR-MEMORY-118 (this FR blocks)</strong> — <code>put_if</code> precondition-hash operates within ACL constraints (rejected writes don't proceed regardless of precondition match).</li><li><strong>FR-MEMORY-103 (transitively related)</strong> — multi-device sync respects ACL on import; foreign-chain rows arriving via sync get the <code>imported</code> actor literal.</li><li><strong>FR-MEMORY-106 (related)</strong> — sync_class is orthogonal to ACL; both can be active simultaneously.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>STORE.yaml</code> for SRE-style demo</h3>\n<pre><code class=\"language-yaml\"># memories/org-wide-knowledge/STORE.yaml\nstore_id: org-wide-knowledge\ndefault_mode: read\nacl:\n  - {actor: \"stephen@cyberskill.world\", mode: \"read-write\"}\n  - {actor: \"dream-applier\",            mode: \"read-write\"}\n  - {actor: \"*\",                        mode: \"read\"}</code></pre>\n<pre><code class=\"language-yaml\"># memories/sre/STORE.yaml\nstore_id: sre-working\ndefault_mode: read-write\nacl:\n  - {actor: \"scheduled-importer\", mode: \"deny\"}\n  - {actor: \"*\",                  mode: \"read-write\"}</code></pre>\n<h3><code>memory.acl_denied</code> aux row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.acl_denied\",\n  \"payload\": {\n    \"actor\":         \"scheduled-importer\",\n    \"target_path\":   \"memories/sre/dispatch-1.md\",\n    \"store_id\":      \"sre-working\",\n    \"yaml_path\":     \"memories/sre/STORE.yaml\",\n    \"mode\":          \"deny\",\n    \"matched_entry\": \"actor='scheduled-importer'\",\n    \"attempt_kind\":  \"put\",\n    \"warn_only\":     false\n  }\n}</code></pre>\n<h3><code>cyberos acl explain</code> output</h3>\n<pre><code class=\"language-text\">$ cyberos acl explain memories/sre/dispatch-1.md --actor scheduled-importer\nPath:             memories/sre/dispatch-1.md\nGoverning yaml:   memories/sre/STORE.yaml (store_id: sre-working)\nEffective mode:   deny\nMatched entry:    {actor: \"scheduled-importer\", mode: \"deny\"} [first match]\nResult:           writes WILL be rejected</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li><code>default_mode: deny</code> general support — §1 #15; slice 4 (requires more design around the \"secrets/\" subtree pattern).</li><li><code>cyberos acl audit --since 24h</code> — §1 #16; slice 4 polish.</li><li>Per-store rate limits — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>§14.4 missing</td><td>Writer construction check</td><td>WARN-ONLY mode active</td><td>Operator runs APPROVE chat-turn</td></tr><tr><td>STORE.yaml malformed</td><td>walker invariant</td><td><code>cyberos doctor</code> non-zero</td><td>Operator fixes file</td></tr><tr><td>Glob pattern catastrophic</td><td>fnmatch is bounded</td><td>normal speed</td><td>None — by design</td></tr><tr><td>Actor not matching any entry, no default_mode</td><td>schema default = \"read-write\"</td><td>permissive default</td><td>None</td></tr><tr><td>Migration script crash mid-pass</td><td>per-file write is atomic</td><td>partial migration; re-run picks up remaining</td><td>None — idempotent</td></tr><tr><td>Symlinked memory_root</td><td><code>resolve()</code> follows symlinks</td><td>works</td><td>None</td></tr><tr><td>STORE.yaml present but no <code>acl</code> array</td><td>jsonschema rejects</td><td>walker invariant fails</td><td>Operator adds at least one entry</td></tr><tr><td>Concurrent edit to STORE.yaml during write</td><td>mtime cache invalidates on next access</td><td>new policy applied to next write</td><td>None</td></tr><tr><td>ACL entry with mode outside enum</td><td>jsonschema rejects</td><td>walker fails</td><td>Operator fixes</td></tr><tr><td>Empty <code>acl: []</code> with default_mode</td><td>resolver returns default_mode</td><td>works</td><td>None</td></tr><tr><td>Recursive symlink in <code>&lt;memory-root&gt;/</code></td><td><code>resolve()</code> raises</td><td>walker fails</td><td>Operator fixes filesystem</td></tr><tr><td>Operator forgets actor flag → defaults to \"agent\"</td><td>normal resolution</td><td>matches against \"agent\" pattern in ACL</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>fnmatch with <code>case</code>-sensitive matching</strong> — actor strings are case-sensitive by spec. <code>fnmatchcase</code> (not <code>fnmatch</code>) enforces.</li><li><strong>Cache invalidation via mtime</strong> — simple; the FS watcher in FR-MEMORY-107 doesn't need to know about ACL caching.</li><li><strong>Migration script lives at <code>scripts/</code> not <code>cyberos/</code></strong> — it's an operator tool, not part of the runtime.</li><li><strong>WARN-ONLY mode is the bridge</strong> — operators can install this FR before the APPROVE chat-turn and see what would happen; once they're satisfied, they APPROVE and enforcement engages.</li><li><strong>Reads NOT enforced — by design.</strong> This is the most surprising design choice. Operator wanting read isolation uses OS file permissions. The protocol's contract: \"writes are protocol-controlled; reads are filesystem-controlled.\"</li><li><strong>No new audit kind for ACL grants/revokes</strong> — STORE.yaml edits are just file edits; FR-MEMORY-107's FS watcher already captures them as <code>put</code> rows on the path. The walker invariant validates the resulting shape.</li><li><strong><code>Writer.__init__</code> reads AGENTS.md once at construction</strong> to set <code>warn_only</code>. Re-reading on every write would be wasteful; restart-after-APPROVE is the contract.</li><li><strong>The migration script writes one STORE.yaml per top-level directory existing at migration time, NOT per any-depth directory.</strong> This matches DEC-230's intent — operators want a small finite set of stores, not \"every leaf has its own.\"</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-117.</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>ACL logic spans services/memory + modules/memory as consolidated.</p>";
