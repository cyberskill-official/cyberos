window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MCP-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The MCP Gateway service <strong>MUST</strong> implement OAuth 2.1 + PKCE per the MCP 2025-11-25 auth profile, issuing audience-bound JWT access tokens that MCP resource servers verify before dispatching tool calls.</p>\n<ol><li><strong>MUST</strong> implement exactly two grant types: <code>authorization_code</code> and <code>refresh_token</code> (DEC-800 + DEC-807). Implicit, resource-owner-password, and client_credentials are NOT in scope for this slice (client_credentials is deferred to FR-MCP-007). Unsupported grant_type returns <code>400 unsupported_grant_type</code> per RFC 6749 §5.2.</li></ol>\n<ol><li><strong>MUST</strong> require PKCE on every authorization request from a <code>public</code> client (DEC-803). The authorization request MUST include <code>code_challenge</code> (43-128 char base64url) and <code>code_challenge_method</code> (S256 only per DEC-801). Missing PKCE on a public client returns <code>400 invalid_request</code> with <code>error_description: \"pkce_required_for_public_client\"</code>. Plain code_challenge_method returns <code>400 invalid_request</code> with <code>error_description: \"pkce_method_must_be_s256\"</code>.</li></ol>\n<ol><li><strong>MUST</strong> verify PKCE at the token endpoint: SHA-256(code_verifier) base64url-encoded MUST equal the stored code_challenge. Mismatch returns <code>400 invalid_grant</code>. The code_verifier MUST be 43-128 chars per RFC 7636.</li></ol>\n<ol><li><strong>MUST</strong> define a closed 2-value <code>client_type</code> Postgres enum (<code>public</code>, <code>confidential</code>) per DEC-808. Public clients have no client_secret and rely on PKCE for proof. Confidential clients MUST authenticate at the token endpoint via Basic Auth (client_id:client_secret) per RFC 6749 §2.3.1 OR via private_key_jwt (RFC 7523). Both MUST also use PKCE if exposed to a browser context.</li></ol>\n<ol><li><strong>MUST</strong> define a closed 2-value <code>oauth_grant_type</code> Postgres enum (<code>authorization_code</code>, <code>refresh_token</code>). CI cardinality test asserts exactly 2 (DEC-807).</li></ol>\n<ol><li><strong>MUST</strong> define a closed 6-value <code>oauth_error_code</code> Postgres enum (<code>invalid_request</code>, <code>invalid_client</code>, <code>invalid_grant</code>, <code>unauthorized_client</code>, <code>unsupported_grant_type</code>, <code>invalid_scope</code>) per DEC-820 + RFC 6749 §5.2. CI cardinality test asserts exactly 6.</li></ol>\n<ol><li><strong>MUST</strong> issue access tokens as signed JWTs using the FR-AUTH-004 JWKS keys (RS256 or ES256). The token claims MUST include:</li></ol>\n<ul><li><code>iss</code>: the MCP-gateway URL.</li><li><code>aud</code>: the canonical URL of the MCP resource server (DEC-802 + RFC 8707).</li><li><code>sub</code>: the authorizing subject's UUID (FR-AUTH-002).</li><li><code>scope</code>: space-separated scope strings from the client's authorization grant (DEC-813).</li><li><code>nonce</code>: 16-byte random hex.</li><li><code>iat</code>, <code>exp</code>: standard timestamps; exp = iat + 3600 (DEC-805).</li><li><code>jti</code>: UUIDv4 — used for revocation list lookup (DEC-817).</li><li><code>client_id</code>: the requesting client's UUID.</li><li><code>tenant_id</code>: the tenant binding (per FR-AUTH-108 Lumi pattern).</li></ul>\n<ol><li><strong>MUST</strong> issue refresh tokens as opaque 256-bit random base64url strings (NOT JWTs). The refresh token is stored hashed (Argon2 or SHA-256) in <code>oauth_refresh_families</code> table with <code>(family_id, token_hash, issued_at, expires_at, parent_token_hash)</code>. TTL = 30 days (DEC-805).</li></ol>\n<ol><li><strong>MUST</strong> enforce refresh-token rotation (DEC-806). On <code>grant_type=refresh_token</code>, the server:</li></ol>\n<ul><li>Verifies the presented refresh_token matches an active row in <code>oauth_refresh_families</code>.</li><li>Marks the presented row as <code>state = used</code>.</li><li>Issues a new refresh_token in the same family with <code>parent_token_hash</code> pointing to the just-used row.</li><li>Returns the new access + refresh pair. If the presented refresh_token is found but <code>state = used</code> already, this is a REUSE — mark the entire family as <code>state = compromised</code>, invalidate all descendants, refuse issuance, emit a sev-1 memory audit row <code>mcp.oauth_refresh_reuse_detected</code>.</li></ul>\n<ol><li><strong>MUST</strong> verify <code>redirect_uri</code> via exact-match comparison against the pre-registered URI (DEC-809). The authorization request's <code>redirect_uri</code> MUST match one of <code>oauth_clients.redirect_uris</code> entries character-for-character. No substring, no regex, no trailing-slash normalization. Mismatch returns <code>400 invalid_request</code> with <code>error_description: \"redirect_uri_mismatch\"</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>redirect_uri</code> host on registration (DEC-816). The per-tenant policy <code>mcp_oauth_allowlist_redirect_hosts</code> (JSON array of hostnames or wildcards like <code>*.example.com</code>) restricts which hosts may appear in registered redirect URIs. Default policy: allow any HTTPS host (no host restriction). Tenants opt into stricter policies; tenant_admin role required for policy mutation + sev-2 memory audit.</li></ol>\n<ol><li><strong>MUST</strong> require <code>https</code> scheme on all redirect URIs in production. The only exception is <code>http://localhost[:port]/...</code> and <code>http://127.0.0.1[:port]/...</code> for native CLI clients (OAuth 2.1 §10.3.3). HTTP redirect to a non-loopback host returns <code>400 invalid_request</code> at registration.</li></ol>\n<ol><li><strong>MUST</strong> require the <code>state</code> parameter on every authorization request (DEC-810). Missing <code>state</code> returns <code>400 invalid_request</code> with <code>error_description: \"state_required\"</code>. The state is opaque to the gateway and returned unchanged on the redirect (CSRF defense).</li></ol>\n<ol><li><strong>MUST</strong> enforce authorization code TTL = 30 seconds (DEC-811). The <code>oauth_codes</code> row carries <code>expires_at = issued_at + INTERVAL '30 seconds'</code>. An expired code at token exchange returns <code>400 invalid_grant</code> with <code>error_description: \"code_expired\"</code>.</li></ol>\n<ol><li><strong>MUST</strong> enforce authorization code one-time-use (DEC-812). On token exchange, the handler:</li></ol>\n<ul><li>SELECTs the row FOR UPDATE.</li><li>Verifies <code>consumed_at IS NULL</code>.</li><li>UPDATEs <code>consumed_at = now()</code>.</li><li>If <code>consumed_at IS NOT NULL</code> (replay), marks the client's most recent refresh family as compromised + emits sev-2 memory audit <code>mcp.oauth_code_reuse_detected</code>.</li></ul>\n<ol><li><strong>MUST</strong> define a closed 3-value <code>oauth_code_state</code> enum (<code>active</code>, <code>consumed</code>, <code>expired</code>). CI cardinality test asserts exactly 3.</li></ol>\n<ol><li><strong>MUST</strong> define a closed 3-value <code>oauth_refresh_state</code> enum (<code>active</code>, <code>used</code>, <code>compromised</code>). CI cardinality test asserts exactly 3.</li></ol>\n<ol><li><strong>MUST</strong> implement RFC 7591 Dynamic Client Registration at <code>POST /register</code>. Request body:</li></ol>\n<ul><li><code>client_type</code> (required, public | confidential)</li><li><code>redirect_uris</code> (required, array of URIs; max 5 per client)</li><li><code>client_name</code> (optional, ≤ 64 chars)</li><li><code>scope</code> (required, space-separated list of MCP scopes from the registry) Response body:</li><li><code>client_id</code></li><li><code>client_secret</code> (only for confidential; cryptographically random 256-bit base64url)</li><li><code>client_id_issued_at</code> (unix ts) Public client registration is open (no caller auth); confidential client registration requires <code>tenant_admin</code> role per FR-AUTH-101.</li></ul>\n<ol><li><strong>MUST</strong> scope confidential client registration to the requesting subject's tenant. The client row carries <code>tenant_id</code> and the issued access tokens carry the same tenant_id. Cross-tenant client lookup returns 404.</li></ol>\n<ol><li><strong>MUST</strong> implement RFC 8414 Discovery at <code>GET /.well-known/oauth-authorization-server</code>. Response includes: ``<code>json { \"issuer\": \"https://mcp.cyberos.world\", \"authorization_endpoint\": \"https://mcp.cyberos.world/authorize\", \"token_endpoint\": \"https://mcp.cyberos.world/token\", \"registration_endpoint\": \"https://mcp.cyberos.world/register\", \"revocation_endpoint\": \"https://mcp.cyberos.world/revoke\", \"introspection_endpoint\": \"https://mcp.cyberos.world/introspect\", \"jwks_uri\": \"https://mcp.cyberos.world/.well-known/jwks.json\", \"response_types_supported\": [\"code\"], \"grant_types_supported\": [\"authorization_code\", \"refresh_token\"], \"code_challenge_methods_supported\": [\"S256\"], \"token_endpoint_auth_methods_supported\": [\"none\", \"client_secret_basic\", \"private_key_jwt\"], \"scopes_supported\": [&lt;from FR-MCP-001 registry&gt;] } </code>``</li></ol>\n<ol><li><strong>MUST</strong> implement RFC 7009 token revocation at <code>POST /revoke</code>. Body: <code>token=&lt;access_or_refresh&gt;&amp;token_type_hint=...</code>. The handler:</li></ol>\n<ul><li>For access_token: adds the JWT <code>jti</code> to a revocation list cached for 1 hour (matches access_token TTL).</li><li>For refresh_token: marks the row + family <code>state = compromised</code>.</li><li>Always returns 200 OK regardless of whether token was active (RFC 7009 §2.2 — prevents probing).</li></ul>\n<ol><li><strong>MUST</strong> implement RFC 7662 introspection at <code>POST /introspect</code> for resource servers only (DEC-818). The endpoint requires the caller to authenticate as a registered confidential client with the <code>mcp_introspect</code> scope. Public clients receive 401. Response shape per RFC 7662 §2.2. Introspection enables resource servers to verify the token's audience, scope, subject, tenant — without re-implementing JWT validation.</li></ol>\n<ol><li><strong>MUST</strong> verify audience binding at every MCP resource server hot path (DEC-802). The MCP gateway's <code>tools/call</code> handler MUST:</li></ol>\n<ul><li>Decode the bearer JWT via FR-AUTH-004 JWKS.</li><li>Assert <code>aud</code> exactly equals this resource server's canonical URL.</li><li>Mismatched aud returns <code>401 invalid_token</code> with <code>WWW-Authenticate: Bearer error=\"invalid_token\", error_description=\"audience_mismatch\"</code>.</li></ul>\n<ol><li><strong>MUST</strong> verify the JWT signature, exp, and revocation list on every request. Cache the revocation list in-process with 60s TTL; misses fetch from Postgres. Defense-in-depth: signature + exp + jti revocation check + audience check.</li></ol>\n<ol><li><strong>MUST</strong> emit 8 closed memory audit kinds:</li></ol>\n<ul><li><code>mcp.oauth_authorize_started</code> (sev-3, per /authorize redirect issued)</li><li><code>mcp.oauth_token_issued</code> (sev-3, per access_token issued)</li><li><code>mcp.oauth_token_refreshed</code> (sev-3, per refresh)</li><li><code>mcp.oauth_token_revoked</code> (sev-2, per revocation)</li><li><code>mcp.oauth_refresh_reuse_detected</code> (sev-1, family compromise)</li><li><code>mcp.oauth_code_reuse_detected</code> (sev-2, code replay)</li><li><code>mcp.oauth_audience_mismatch</code> (sev-2, per /tools/call rejection)</li><li><code>mcp.oauth_client_registered</code> (sev-2, per DCR call)</li></ul>\n<ol><li><strong>MUST</strong> route all reason-bearing audit text (error_description, client_name in registration audit) through FR-MEMORY-111 PII scrubbing.</li></ol>\n<ol><li><strong>MUST</strong> persist authorization codes in the <code>oauth_codes</code> table with <code>(code, client_id, subject_id, redirect_uri, code_challenge, scope, nonce, state, issued_at, expires_at, consumed_at)</code>. The table is append-only via SQL grant + UPDATE limited to <code>consumed_at = now()</code> only via a privileged role <code>oauth_code_consumer</code>.</li></ol>\n<ol><li><strong>MUST</strong> support <code>prompt=none</code> parameter at the authorize endpoint for silent re-authorization when the subject is already logged in (existing AUTH session). On success, the gateway redirects to redirect_uri with a fresh code immediately. On failure (subject not logged in OR consent not previously granted), redirect to redirect_uri with <code>error=login_required</code> per OIDC §3.1.2.6.</li></ol>\n<ol><li><strong>MUST</strong> support a <code>consent_screen</code> flow for the first time a subject grants scopes to a client. The consent record is persisted to <code>oauth_consents (subject_id, client_id, scopes, granted_at)</code>. Subsequent authorizations with the same or subset scopes skip the consent screen. New scope set requires re-consent.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>scope</code> against the MCP-server's registered scope set (FR-MCP-001 <code>tools/list</code> registry — DEC-813). Unknown scopes return <code>400 invalid_scope</code>. Scope strings are case-sensitive and conform to RFC 6749 §3.3 syntax (visible printable, no whitespace beyond the space separator).</li></ol>\n<hr>\n<h2>§2 — Rationale (informative — preserve all 22 paragraphs)</h2>\n<p><strong>§2.1  Why OAuth 2.1 and not OAuth 2.0.</strong> DEC-800. OAuth 2.1 is the consolidation IETF draft that removes the unsafe parts of 2.0 (implicit grant, password grant) and codifies the safe practices (PKCE everywhere, exact-match redirect_uri, refresh rotation). The MCP 2025-11-25 auth profile explicitly cites 2.1. Implementing 2.0 with all its options would multiply attack surface; 2.1 is the right baseline.</p>\n<p><strong>§2.2  Why PKCE on confidential clients too.</strong> Clause #4. OAuth 2.1 §4.1.3.2: \"MUST use PKCE if the authorization code flow is initiated in a browser context.\" A confidential client running a browser-based authorize step is functionally a public client during that step; PKCE protects against the authorization code interception attack regardless of how the client authenticates later.</p>\n<p><strong>§2.3  Why S256 only.</strong> DEC-801. The S256 code_challenge_method uses SHA-256 to bind code_verifier to the request; the plain method just sends the verifier in clear. Plain offers no protection against an attacker who intercepts the authorization request. OAuth 2.1 §4.1.4: \"the plain method MUST NOT be used\".</p>\n<p><strong>§2.4  Why audience-bound tokens.</strong> DEC-802 + RFC 8707. Without audience binding, an access token issued for <code>mcp-a.cyberos.world</code> could be presented at <code>mcp-b.cyberos.world</code> — full cross-server replay. The <code>aud</code> claim + the resource server's exact-match check eliminates this; an attacker who steals a token for one MCP server cannot use it elsewhere.</p>\n<p><strong>§2.5  Why refresh-token rotation.</strong> DEC-806. A long-lived refresh token is the highest-value credential in OAuth — stolen, it gives the attacker indefinite access. Rotation means every refresh produces a new token and invalidates the old one; an attacker who steals an old refresh token is detected the moment the legitimate client refreshes (the old token's REUSE triggers family compromise). This is the standard \"reuse detection\" pattern from OAuth 2.1 §6.1.</p>\n<p><strong>§2.6  Why we mark the entire family on reuse, not just one token.</strong> Clause #9 (refresh) + clause #15 (code). If an attacker has stolen one refresh token, they may have a chain of subsequent refresh tokens too. Invalidating just the reused one leaves the rest of the chain valid. Family compromise revokes the entire descendant chain in one operation.</p>\n<p><strong>§2.7  Why 30-second authorization code TTL.</strong> DEC-811. The code is meant to be exchanged immediately at the token endpoint (typically ≤ 1 second in practice). OAuth 2.1 recommends ≤10 minutes; we tighten to 30 seconds. This narrows the window where a stolen authorization code could be exchanged. Legitimate clients exchange within sub-second; the 30-second buffer accommodates network jitter.</p>\n<p><strong>§2.8  Why one-time-use authorization codes.</strong> DEC-812 + clause #15. A code that can be exchanged twice would let an attacker who intercepts the redirect (e.g., via URL-history snooping) silently issue themselves a token while the legitimate user's session also succeeds. One-time-use + reuse-detection makes intercepts visible immediately.</p>\n<p><strong>§2.9  Why exact-match redirect_uri.</strong> DEC-809 + clause #10. Substring matching enables open-redirect attacks (e.g., registered <code>https://app.example.com</code> matches attacker-controlled <code>https://app.example.com.attacker.com</code>). Regex matching has similar pitfalls. Exact match is the only safe comparison — the cost is operator discipline (register every variant explicitly), the benefit is no class of redirect-attack works.</p>\n<p><strong>§2.10  Why per-tenant redirect_uri host allowlist.</strong> DEC-816 + clause #11. Even with exact-match, a misconfigured DCR endpoint could allow registration of arbitrary HTTPS URLs. Per-tenant allowlists let tenant_admins lock down what hosts are registrable. The default (no restriction) keeps the gateway open for development; tenants opt in to strictness for production.</p>\n<p><strong>§2.11  Why JWT access tokens and opaque refresh tokens.</strong> Clauses #7 + #8. JWTs let resource servers verify access tokens without an introspection round-trip (fast hot path). Refresh tokens are presented only at the token endpoint; opaqueness lets us rotate and revoke without leaking structure to clients. The hybrid is standard practice.</p>\n<p><strong>§2.12  Why TTL = 1h for access, 30d for refresh.</strong> DEC-805. Access TTL of 1h means a stolen token's blast radius is bounded to 1h max (or until JWKS rotation, whichever first). Refresh TTL of 30d covers the common \"I open my IDE every couple weeks\" pattern without forcing weekly re-auth. Tighter access TTLs increase refresh traffic; looser refresh TTLs increase stolen-credential risk. 1h + 30d is the standard balance.</p>\n<p><strong>§2.13  Why DCR for public clients with no caller auth.</strong> DEC-804 + clause #18. Public clients (CLIs, desktops) have no pre-existing tenant binding when they first appear; requiring caller auth would create a chicken-and-egg. Open DCR for public clients is safe because (a) public clients can only use the authorization_code flow which requires user interaction, (b) they hold no client_secret, (c) PKCE binds the registered client to the specific authorization request.</p>\n<p><strong>§2.14  Why confidential clients require tenant_admin.</strong> DEC-804 + clause #18. A confidential client gets a client_secret that authenticates to the token endpoint without user interaction (in client_credentials, deferred to FR-MCP-007). Issuing such credentials needs tenant accountability; tenant_admin is the right role.</p>\n<p><strong>§2.15  Why scopes from the MCP server registry.</strong> DEC-813 + clause #30. Scopes are not free-form; they map to specific tool capabilities the MCP server exposes. The FR-MCP-001 <code>tools/list</code> registry IS the scope authority; the OAuth server validates against it. This keeps the scope vocabulary closed and machine-checkable.</p>\n<p><strong>§2.16  Why discovery via RFC 8414.</strong> DEC-819 + clause #20. Discovery lets clients learn endpoints + capabilities without hardcoding. RFC 8414 is the standard; MCP profile cites it. The endpoint is at <code>/.well-known/oauth-authorization-server</code> (separate from FR-MCP-005's Protected Resource Metadata at <code>/.well-known/oauth-protected-resource</code>).</p>\n<p><strong>§2.17  Why introspection is closed-network only.</strong> DEC-818 + clause #22. Introspection lets a caller learn token validity + claims. Exposing it publicly lets attackers probe valid token shapes. Restricting to authenticated resource servers (with <code>mcp_introspect</code> scope) eliminates the probing surface.</p>\n<p><strong>§2.18  Why we revoke via short-lived JWKS keys + revocation list.</strong> Clauses #21 + #24 + DEC-817. JWTs are inherently bearer credentials — once issued, they're valid until exp. To revoke before exp, we need either (a) check a revocation list on every verify (small cost; gives true revocation), or (b) rotate JWKS keys so all issued JWTs invalidate together (cheap; coarse). We do both: per-token revocation via jti list + key rotation as a periodic refresh. Revocation list is cached 60s for hot-path performance.</p>\n<p><strong>§2.19  Why prompt=none for silent re-auth.</strong> Clause #28. CLIs and IDEs that re-authorize on token expiry shouldn't bounce the user through a browser prompt every hour. If the subject has a valid AUTH session and previous consent for this client, prompt=none redirects with a fresh code immediately. This matches OIDC §3.1.2.6 semantics.</p>\n<p><strong>§2.20  Why consent screen on first authorization.</strong> Clause #29. The subject should knowingly grant scopes to a client. The first authorization shows the scope list + the client name; subsequent authorizations within the same scope set skip. This is the GDPR-aligned consent model adapted to OAuth.</p>\n<p><strong>§2.21  Why 8 closed audit kinds.</strong> Clause #25. Each kind is a distinct operational signal: authorize-started (intent), token-issued (success), refreshed (renewal), revoked (deliberate end), refresh-reuse (compromise detection), code-reuse (compromise detection), audience-mismatch (cross-server probe), client-registered (DCR). Operators query on kind; collapsing would force free-text parsing.</p>\n<p><strong>§2.22  Why we don't support DPoP at this slice.</strong> DPoP (RFC 9449) sender-constrains access tokens via per-request signatures. It's the next step after audience binding. We defer it because (a) MCP 2025-11-25 doesn't mandate DPoP, (b) audience + JWKS rotation + revocation list cover the bulk of the threat model, (c) DPoP requires every client to manage signing keys. Revisit in P3 if audit findings demand it.</p>\n<hr>\n<h2>§3 — API &amp; schema</h2>\n<h3>§3.1 — Migration 0010: oauth_clients + closed enums</h3>\n<pre><code class=\"language-sql\">-- services/mcp-gateway/migrations/0010_oauth_clients.sql\n\nCREATE TYPE client_type AS ENUM ('public', 'confidential');\nCREATE TYPE oauth_grant_type AS ENUM ('authorization_code', 'refresh_token');\nCREATE TYPE oauth_error_code AS ENUM (\n    'invalid_request',\n    'invalid_client',\n    'invalid_grant',\n    'unauthorized_client',\n    'unsupported_grant_type',\n    'invalid_scope'\n);\n\nCREATE TABLE oauth_clients (\n    id                   UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id            UUID REFERENCES tenants(id),   -- NULL for public CLI clients\n    client_type          client_type NOT NULL,\n    client_secret_hash   TEXT,                          -- NULL for public; Argon2 for confidential\n    redirect_uris        JSONB NOT NULL,\n    client_name          TEXT CHECK (client_name IS NULL OR length(client_name) &lt;= 64),\n    scope                TEXT NOT NULL CHECK (length(scope) BETWEEN 1 AND 1024),\n    created_at           TIMESTAMPTZ NOT NULL DEFAULT now(),\n    revoked_at           TIMESTAMPTZ,\n    CONSTRAINT confidential_has_secret CHECK (\n        (client_type = 'confidential' AND client_secret_hash IS NOT NULL)\n     OR (client_type = 'public' AND client_secret_hash IS NULL)\n    ),\n    CONSTRAINT redirect_uris_max_5 CHECK (jsonb_array_length(redirect_uris) BETWEEN 1 AND 5),\n    CONSTRAINT confidential_has_tenant CHECK (\n        client_type = 'public' OR tenant_id IS NOT NULL\n    )\n);\n\nCREATE INDEX oauth_clients_tenant ON oauth_clients (tenant_id) WHERE revoked_at IS NULL;\n\nREVOKE UPDATE, DELETE ON oauth_clients FROM cyberos_app;\nGRANT INSERT, SELECT, UPDATE(revoked_at) ON oauth_clients TO oauth_writer;\nGRANT SELECT ON oauth_clients TO oauth_reader;</code></pre>\n<h3>§3.2 — Migration 0011: oauth_codes</h3>\n<pre><code class=\"language-sql\">-- services/mcp-gateway/migrations/0011_oauth_codes.sql\n\nCREATE TYPE oauth_code_state AS ENUM ('active', 'consumed', 'expired');\n\nCREATE TABLE oauth_codes (\n    code                TEXT PRIMARY KEY CHECK (length(code) = 43),  -- 256-bit base64url\n    client_id           UUID NOT NULL REFERENCES oauth_clients(id),\n    subject_id          UUID NOT NULL REFERENCES subjects(id),\n    tenant_id           UUID NOT NULL REFERENCES tenants(id),\n    redirect_uri        TEXT NOT NULL,\n    code_challenge      TEXT NOT NULL CHECK (length(code_challenge) BETWEEN 43 AND 128),\n    code_challenge_method TEXT NOT NULL CHECK (code_challenge_method = 'S256'),\n    scope               TEXT NOT NULL,\n    audience            TEXT NOT NULL,  -- target resource server URL\n    nonce               TEXT NOT NULL,\n    state               TEXT NOT NULL,  -- client-supplied CSRF state\n    issued_at           TIMESTAMPTZ NOT NULL DEFAULT now(),\n    expires_at          TIMESTAMPTZ NOT NULL,\n    consumed_at         TIMESTAMPTZ,\n    memory_chain_hash    CHAR(64) NOT NULL\n);\n\nCREATE INDEX oauth_codes_expires ON oauth_codes (expires_at);\n\nREVOKE UPDATE, DELETE ON oauth_codes FROM cyberos_app;\nGRANT INSERT, SELECT, UPDATE(consumed_at) ON oauth_codes TO oauth_code_consumer;</code></pre>\n<h3>§3.3 — Migration 0012: oauth_refresh_families + revocation_list</h3>\n<pre><code class=\"language-sql\">-- services/mcp-gateway/migrations/0012_oauth_refresh_families.sql\n\nCREATE TYPE oauth_refresh_state AS ENUM ('active', 'used', 'compromised');\n\nCREATE TABLE oauth_refresh_families (\n    id                     UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    family_id              UUID NOT NULL,\n    client_id              UUID NOT NULL REFERENCES oauth_clients(id),\n    subject_id             UUID NOT NULL REFERENCES subjects(id),\n    tenant_id              UUID NOT NULL REFERENCES tenants(id),\n    audience               TEXT NOT NULL,\n    scope                  TEXT NOT NULL,\n    token_hash             CHAR(64) NOT NULL,  -- SHA-256 hex\n    parent_token_hash      CHAR(64),\n    issued_at              TIMESTAMPTZ NOT NULL DEFAULT now(),\n    expires_at             TIMESTAMPTZ NOT NULL,\n    state                  oauth_refresh_state NOT NULL DEFAULT 'active',\n    state_changed_at       TIMESTAMPTZ,\n    memory_chain_hash       CHAR(64) NOT NULL\n);\n\nCREATE UNIQUE INDEX oauth_refresh_token_hash ON oauth_refresh_families (token_hash);\nCREATE INDEX oauth_refresh_family_active ON oauth_refresh_families (family_id) WHERE state = 'active';\n\nREVOKE UPDATE, DELETE ON oauth_refresh_families FROM cyberos_app;\nGRANT INSERT, SELECT, UPDATE(state, state_changed_at) ON oauth_refresh_families TO oauth_refresh_writer;\n\nCREATE TABLE oauth_revocation_list (\n    jti           UUID PRIMARY KEY,\n    revoked_at    TIMESTAMPTZ NOT NULL DEFAULT now(),\n    expires_at    TIMESTAMPTZ NOT NULL,  -- matches the JWT exp; row TTL-eviction\n    reason        TEXT\n);\n\nCREATE INDEX oauth_revocation_expires ON oauth_revocation_list (expires_at);\n\nREVOKE UPDATE, DELETE ON oauth_revocation_list FROM cyberos_app;\nGRANT INSERT, SELECT ON oauth_revocation_list TO oauth_writer;\n\nCREATE TABLE oauth_consents (\n    subject_id   UUID NOT NULL REFERENCES subjects(id),\n    client_id    UUID NOT NULL REFERENCES oauth_clients(id),\n    scopes       TEXT NOT NULL,\n    granted_at   TIMESTAMPTZ NOT NULL DEFAULT now(),\n    PRIMARY KEY (subject_id, client_id)\n);</code></pre>\n<h3>§3.4 — PKCE verification</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/oauth/pkce.rs\n\nuse sha2::{Sha256, Digest};\nuse base64::{engine::general_purpose::URL_SAFE_NO_PAD, Engine as _};\n\npub fn verify_pkce(code_verifier: &amp;str, stored_code_challenge: &amp;str) -&gt; bool {\n    if code_verifier.len() &lt; 43 || code_verifier.len() &gt; 128 {\n        return false;\n    }\n    let computed = URL_SAFE_NO_PAD.encode(Sha256::digest(code_verifier.as_bytes()));\n    constant_time_eq::constant_time_eq(computed.as_bytes(), stored_code_challenge.as_bytes())\n}</code></pre>\n<h3>§3.5 — Token endpoint (handle authorization_code grant)</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/oauth/token.rs\n\npub async fn token_handler(\n    pool: &amp;PgPool,\n    body: TokenRequest,\n    client_authn: Option&lt;ClientAuth&gt;,\n) -&gt; Result&lt;TokenResponse, OAuthError&gt; {\n    let client: OAuthClient = load_client(pool, &amp;body.client_id).await?;\n    if client.is_confidential() {\n        verify_client_authn(&amp;client, &amp;client_authn)?;\n    }\n    match body.grant_type {\n        OAuthGrantType::AuthorizationCode =&gt; handle_authz_code(pool, &amp;client, body).await,\n        OAuthGrantType::RefreshToken =&gt; handle_refresh(pool, &amp;client, body).await,\n    }\n}\n\nasync fn handle_authz_code(pool: &amp;PgPool, client: &amp;OAuthClient, body: TokenRequest)\n    -&gt; Result&lt;TokenResponse, OAuthError&gt;\n{\n    let mut tx = pool.begin().await?;\n\n    // §1 #15 SELECT FOR UPDATE + one-time-use\n    let code_row: OAuthCodeRow = sqlx::query_as!(\n        OAuthCodeRow,\n        r#\"SELECT code, client_id, subject_id, tenant_id, redirect_uri,\n                  code_challenge, scope, audience, nonce, state,\n                  issued_at, expires_at, consumed_at\n           FROM oauth_codes WHERE code = $1 FOR UPDATE\"#,\n        body.code\n    ).fetch_optional(&amp;mut *tx).await?.ok_or(OAuthError::InvalidGrant(\"code_not_found\"))?;\n\n    if code_row.client_id != client.id {\n        return Err(OAuthError::InvalidGrant(\"code_client_mismatch\"));\n    }\n    if code_row.expires_at &lt; Utc::now() {\n        return Err(OAuthError::InvalidGrant(\"code_expired\"));\n    }\n    if code_row.consumed_at.is_some() {\n        // §1 #15 REUSE detected — mark refresh family compromised\n        mark_family_compromised(pool, &amp;client.id, &amp;code_row.subject_id, \"code_reuse\").await?;\n        emit_memory_audit(MemoryKind::OauthCodeReuseDetected, ...).await?;\n        return Err(OAuthError::InvalidGrant(\"code_already_used\"));\n    }\n    if code_row.redirect_uri != body.redirect_uri {\n        return Err(OAuthError::InvalidGrant(\"redirect_uri_mismatch\"));\n    }\n\n    // PKCE\n    if !pkce::verify_pkce(&amp;body.code_verifier, &amp;code_row.code_challenge) {\n        return Err(OAuthError::InvalidGrant(\"pkce_verification_failed\"));\n    }\n\n    sqlx::query!(\n        \"UPDATE oauth_codes SET consumed_at = now() WHERE code = $1\",\n        body.code\n    ).execute(&amp;mut *tx).await?;\n\n    // Issue access + refresh\n    let access = issue_access_jwt(\n        &amp;code_row.subject_id, &amp;code_row.tenant_id, &amp;client.id,\n        &amp;code_row.audience, &amp;code_row.scope\n    ).await?;\n    let refresh = issue_refresh(pool, &amp;client.id, &amp;code_row.subject_id, &amp;code_row.tenant_id,\n                                &amp;code_row.audience, &amp;code_row.scope, None).await?;\n\n    emit_memory_audit(MemoryKind::OauthTokenIssued, ...).await?;\n    tx.commit().await?;\n\n    Ok(TokenResponse {\n        access_token: access.token,\n        token_type: \"Bearer\".to_string(),\n        expires_in: 3600,\n        refresh_token: Some(refresh.token),\n        scope: code_row.scope,\n    })\n}</code></pre>\n<h3>§3.6 — Refresh rotation with reuse-detection</h3>\n<pre><code class=\"language-rust\">async fn handle_refresh(pool: &amp;PgPool, client: &amp;OAuthClient, body: TokenRequest)\n    -&gt; Result&lt;TokenResponse, OAuthError&gt;\n{\n    let mut tx = pool.begin().await?;\n    let token_hash = sha256_hex(&amp;body.refresh_token);\n\n    let row: OAuthRefreshRow = sqlx::query_as!(\n        OAuthRefreshRow,\n        r#\"SELECT id, family_id, client_id, subject_id, tenant_id,\n                  audience, scope, token_hash, state AS \"state: OauthRefreshState\", expires_at\n           FROM oauth_refresh_families WHERE token_hash = $1 FOR UPDATE\"#,\n        &amp;token_hash\n    ).fetch_optional(&amp;mut *tx).await?.ok_or(OAuthError::InvalidGrant(\"refresh_not_found\"))?;\n\n    if row.client_id != client.id {\n        return Err(OAuthError::InvalidGrant(\"refresh_client_mismatch\"));\n    }\n    if row.expires_at &lt; Utc::now() {\n        return Err(OAuthError::InvalidGrant(\"refresh_expired\"));\n    }\n\n    match row.state {\n        OauthRefreshState::Active =&gt; {\n            // §1 #9 mark this row as used; issue child\n            sqlx::query!(\n                \"UPDATE oauth_refresh_families SET state = 'used', state_changed_at = now() WHERE id = $1\",\n                row.id\n            ).execute(&amp;mut *tx).await?;\n\n            let access = issue_access_jwt(&amp;row.subject_id, &amp;row.tenant_id, &amp;client.id,\n                                          &amp;row.audience, &amp;row.scope).await?;\n            let new_refresh = issue_refresh(pool, &amp;client.id, &amp;row.subject_id, &amp;row.tenant_id,\n                                           &amp;row.audience, &amp;row.scope, Some(&amp;token_hash)).await?;\n            emit_memory_audit(MemoryKind::OauthTokenRefreshed, ...).await?;\n            tx.commit().await?;\n\n            Ok(TokenResponse {\n                access_token: access.token,\n                token_type: \"Bearer\".to_string(),\n                expires_in: 3600,\n                refresh_token: Some(new_refresh.token),\n                scope: row.scope,\n            })\n        }\n        OauthRefreshState::Used | OauthRefreshState::Compromised =&gt; {\n            // §1 #9 REUSE — mark entire family compromised\n            sqlx::query!(\n                r#\"UPDATE oauth_refresh_families\n                      SET state = 'compromised', state_changed_at = now()\n                      WHERE family_id = $1 AND state != 'compromised'\"#,\n                row.family_id\n            ).execute(&amp;mut *tx).await?;\n            emit_memory_audit(MemoryKind::OauthRefreshReuseDetected, ...).await?;\n            tx.commit().await?;\n            Err(OAuthError::InvalidGrant(\"refresh_reuse_detected\"))\n        }\n    }\n}</code></pre>\n<h3>§3.7 — Audience verification at MCP resource server</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/handlers/tools_call.rs (modified)\n\npub async fn tools_call(\n    auth_header: HeaderValue,\n    body: ToolsCallRequest,\n) -&gt; Result&lt;ToolsCallResponse, McpError&gt; {\n    let bearer = parse_bearer(auth_header)?;\n    let claims = jwt::verify(&amp;bearer, &amp;JWKS).await?;\n\n    let expected_aud = std::env::var(\"MCP_RESOURCE_SERVER_URL\")?;\n    if claims.aud != expected_aud {\n        emit_memory_audit(MemoryKind::OauthAudienceMismatch, &amp;claims, &amp;expected_aud).await?;\n        return Err(McpError::InvalidToken(\"audience_mismatch\"));\n    }\n\n    if is_revoked(&amp;claims.jti).await? {\n        return Err(McpError::InvalidToken(\"token_revoked\"));\n    }\n\n    // Tenant + scope authorized — dispatch\n    dispatch_tool(&amp;body, &amp;claims).await\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>CI cardinality tests assert <code>client_type</code> (2), <code>oauth_grant_type</code> (2), <code>oauth_error_code</code> (6), <code>oauth_code_state</code> (3), <code>oauth_refresh_state</code> (3).</li><li>Public client DCR: POST /register with <code>client_type=public</code> succeeds without caller auth.</li><li>Confidential client DCR: requires <code>tenant_admin</code> role.</li><li>DCR registers redirect_uris (1-5 URIs); 6th rejected with 400.</li><li>Authorization request without <code>code_challenge</code> from public client → 400 <code>pkce_required_for_public_client</code>.</li><li>Authorization request with <code>code_challenge_method=plain</code> → 400 <code>pkce_method_must_be_s256</code>.</li><li>Authorization request without <code>state</code> → 400 <code>state_required</code>.</li><li>Authorization request with substring-matching <code>redirect_uri</code> (not exact) → 400 <code>redirect_uri_mismatch</code>.</li><li>Authorization request with HTTP non-loopback redirect → 400 at registration (not at authorize — caught earlier).</li><li>Authorization code TTL = 30s; exchange at +31s returns <code>invalid_grant code_expired</code>.</li><li>Authorization code one-time-use: second exchange returns <code>invalid_grant code_already_used</code> + marks refresh family compromised + sev-2 audit.</li><li>PKCE verification: code_verifier mismatch returns <code>invalid_grant pkce_verification_failed</code>.</li><li>Token endpoint issues JWT with claims iss + aud + sub + scope + nonce + iat + exp + jti + client_id + tenant_id.</li><li>Access token TTL = 3600s (1h); refresh TTL = 30 days.</li><li>Refresh rotation: each refresh returns new refresh_token; old marked <code>used</code>.</li><li>Refresh reuse: presenting <code>used</code> refresh_token marks family compromised + invalidates all descendants + sev-1 audit <code>mcp.oauth_refresh_reuse_detected</code>.</li><li>Audience mismatch at MCP resource server: returns 401 <code>invalid_token audience_mismatch</code> + sev-2 audit.</li><li>Revoked JWT (jti in revocation_list) at /tools/call returns 401 <code>token_revoked</code>.</li><li>Revocation list cached in-process with 60s TTL.</li><li>Revocation endpoint: POST /revoke with valid token returns 200; same for invalid token (no probing surface).</li><li>Introspection endpoint requires <code>mcp_introspect</code> scope; public client request returns 401.</li><li>Discovery endpoint (GET /.well-known/oauth-authorization-server) returns RFC 8414 JSON with all required fields.</li><li>prompt=none with logged-in subject + previous consent redirects with fresh code immediately.</li><li>prompt=none without logged-in subject redirects with <code>error=login_required</code>.</li><li>First-time scope grant shows consent screen; subsequent same-scope grants skip.</li><li>Scope outside FR-MCP-001 registry returns <code>400 invalid_scope</code>.</li><li>JWT signed with FR-AUTH-004 JWKS (RS256 or ES256); signature verified on every request.</li><li>oauth_codes / oauth_refresh_families REVOKE UPDATE/DELETE from cyberos_app confirmed; only privileged roles can mutate specific columns.</li><li>Per-tenant redirect_uri host allowlist enforced at registration; mutation requires tenant_admin + sev-2 audit.</li><li>memory audit row emitted per 8 closed kinds; all reason text scrubbed via FR-MEMORY-111.</li><li>Confidential client authentication: client_secret_basic AND private_key_jwt both supported.</li><li>Cross-tenant client lookup returns 404 (tenant isolation at confidential client level).</li></ol>\n<hr>\n<h2>§5 — Verification (CI tests)</h2>\n<ul><li><code>cardinality_test_all</code> — 5 enums.</li><li><code>dcr_public_test</code> — POST /register without auth → success.</li><li><code>dcr_confidential_acl_test</code> — non-tenant_admin → 403.</li><li><code>dcr_redirect_uri_max_test</code> — 6 URIs → 400.</li><li><code>authorize_pkce_required_test</code> — public client w/o PKCE → 400.</li><li><code>authorize_pkce_plain_test</code> — plain method → 400.</li><li><code>authorize_state_required_test</code> — missing state → 400.</li><li><code>authorize_redirect_substring_test</code> — substring not match → 400.</li><li><code>register_http_non_loopback_test</code> — <code>http://example.com/cb</code> → 400.</li><li><code>register_http_loopback_test</code> — <code>http://localhost:8080/cb</code> → succeed.</li><li><code>code_ttl_test</code> — exchange at +31s → invalid_grant.</li><li><code>code_one_time_test</code> — second exchange → family compromised + sev-2.</li><li><code>pkce_verification_test</code> — wrong verifier → 400.</li><li><code>jwt_claims_test</code> — assert iss + aud + sub + scope + nonce + iat + exp + jti + client_id + tenant_id present.</li><li><code>access_ttl_test</code> — exp - iat = 3600.</li><li><code>refresh_ttl_test</code> — expires_at - issued_at = 30 days.</li><li><code>refresh_rotation_test</code> — old marked used; new active.</li><li><code>refresh_reuse_test</code> — present used refresh → compromised + sev-1.</li><li><code>audience_mismatch_test</code> — JWT aud != server aud → 401.</li><li><code>revoked_jti_test</code> — revocation_list hit → 401.</li><li><code>revocation_cache_test</code> — 60s TTL.</li><li><code>revoke_endpoint_test</code> — POST /revoke valid + invalid both return 200.</li><li><code>introspect_acl_test</code> — public client → 401.</li><li><code>discovery_test</code> — JSON matches RFC 8414 fields.</li><li><code>prompt_none_logged_in_test</code> — fresh code immediately.</li><li><code>prompt_none_logged_out_test</code> — error=login_required.</li><li><code>consent_first_test</code> — first auth shows consent.</li><li><code>consent_skip_test</code> — subsequent same scope skips.</li><li><code>scope_unknown_test</code> — not in registry → 400.</li><li><code>jwt_signature_test</code> — JWKS verify.</li><li><code>append_only_test</code> — REVOKE inspection.</li><li><code>redirect_host_allowlist_test</code> — host not in tenant policy → 400 at registration.</li><li><code>confidential_basic_test</code> — Basic auth → success.</li><li><code>confidential_private_key_jwt_test</code> — private_key_jwt → success.</li><li><code>cross_tenant_client_404_test</code> — wrong tenant returns 404.</li></ul>\n<hr>\n<h2>§6 — File skeleton</h2>\n<pre><code>services/mcp-gateway/\n├── src/\n│   ├── oauth/\n│   │   ├── mod.rs          # pub re-exports\n│   │   ├── authorize.rs    # GET /authorize handler\n│   │   ├── token.rs        # POST /token (§3.5 + §3.6)\n│   │   ├── pkce.rs         # §3.4 verify\n│   │   ├── refresh.rs      # rotation + family-compromise\n│   │   ├── dcr.rs          # POST /register\n│   │   ├── revoke.rs       # POST /revoke\n│   │   ├── introspect.rs   # POST /introspect\n│   │   ├── discovery.rs    # GET /.well-known/oauth-authorization-server\n│   │   ├── audience.rs     # audience verification helper\n│   │   ├── scope.rs        # scope validation against FR-MCP-001 registry\n│   │   ├── jwt.rs          # access JWT mint/verify (uses FR-AUTH-004 JWKS)\n│   │   ├── consent.rs      # consent screen handler + persisted record\n│   │   ├── audit.rs        # 8 memory audit kinds\n│   │   └── error.rs        # OAuthError + RFC 6749 §5.2 mapping\n│   └── handlers/\n│       └── tools_call.rs   # MODIFIED: verify audience + revocation list\n├── migrations/\n│   ├── 0010_oauth_clients.sql\n│   ├── 0011_oauth_codes.sql\n│   └── 0012_oauth_refresh_families.sql\n└── tests/\n    ├── oauth_authorize_test.rs\n    ├── oauth_token_test.rs\n    ├── oauth_refresh_rotation_test.rs\n    ├── oauth_pkce_test.rs\n    ├── oauth_audience_test.rs\n    ├── oauth_dcr_test.rs\n    ├── oauth_revoke_test.rs\n    └── oauth_discovery_test.rs</code></pre>\n<hr>\n<h2>§7 — Dependencies &amp; blast-radius</h2>\n<p><strong>Depends on</strong>: FR-AUTH-004 (JWKS for JWT signing), FR-MCP-001 (scope registry from tools/list).</p>\n<p><strong>Blocks</strong>: FR-MCP-005 (Protected Resource Metadata), FR-MCP-006 (resource indicators), FR-MCP-007 (client_credentials grant), FR-MCP-008 (tenant-admin token revocation UI).</p>\n<p><strong>Blast radius if broken</strong>:</p>\n<ul><li><strong>Audience-binding bypass</strong>: cross-server token replay — any token works at any MCP server.</li><li><strong>Refresh-reuse not detected</strong>: stolen refresh = indefinite access.</li><li><strong>PKCE bypass</strong>: authorization code interception attacks viable on public clients.</li><li><strong>Redirect_uri substring matching</strong>: open-redirect → token leakage.</li><li><strong>Code TTL too loose</strong>: stolen code window widens.</li></ul>\n<hr>\n<h2>§8 — Payload examples</h2>\n<h3>§8.1 — Authorization request</h3>\n<pre><code>GET /authorize?\n  response_type=code&amp;\n  client_id=cli_abc&amp;\n  redirect_uri=http://localhost:8080/cb&amp;\n  scope=mcp.tools.list%20mcp.tools.call&amp;\n  state=xyz&amp;\n  code_challenge=E9Melh...&amp;\n  code_challenge_method=S256&amp;\n  audience=https://mcp-a.cyberos.world\n\n302 → http://localhost:8080/cb?code=AcDe...&amp;state=xyz</code></pre>\n<h3>§8.2 — Token request</h3>\n<pre><code>POST /token\nContent-Type: application/x-www-form-urlencoded\n\ngrant_type=authorization_code&amp;\ncode=AcDe...&amp;\nredirect_uri=http://localhost:8080/cb&amp;\nclient_id=cli_abc&amp;\ncode_verifier=dBjftJeZ...\n\n200 OK\n{\n  \"access_token\": \"eyJhbGc...\",\n  \"token_type\": \"Bearer\",\n  \"expires_in\": 3600,\n  \"refresh_token\": \"vF7-...\",\n  \"scope\": \"mcp.tools.list mcp.tools.call\"\n}</code></pre>\n<h3>§8.3 — Refresh request</h3>\n<pre><code>POST /token\ngrant_type=refresh_token&amp;refresh_token=vF7-...&amp;client_id=cli_abc\n\n200 OK\n{\n  \"access_token\": \"eyJhbGc... (new)\",\n  \"token_type\": \"Bearer\",\n  \"expires_in\": 3600,\n  \"refresh_token\": \"Ka9Lm... (new)\",\n  \"scope\": \"mcp.tools.list mcp.tools.call\"\n}</code></pre>\n<h3>§8.4 — Refresh reuse rejection</h3>\n<pre><code>POST /token\ngrant_type=refresh_token&amp;refresh_token=vF7-... (already used)\n\n400 Bad Request\n{ \"error\": \"invalid_grant\", \"error_description\": \"refresh_reuse_detected\" }</code></pre>\n<h3>§8.5 — Audience mismatch at /tools/call</h3>\n<pre><code>POST /tools/call (at mcp-b.cyberos.world)\nAuthorization: Bearer eyJ... (aud=mcp-a.cyberos.world)\n\n401 Unauthorized\nWWW-Authenticate: Bearer error=\"invalid_token\", error_description=\"audience_mismatch\"</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<ul><li><strong>OQ-1</strong> (closed by DEC-800): OAuth 2.1 baseline.</li><li><strong>OQ-2</strong> (closed by DEC-806): refresh rotation mandatory.</li><li><strong>OQ-3</strong> (open): DPoP support — defer to P3 per §2.22.</li><li><strong>OQ-4</strong> (open): mTLS client authentication (RFC 8705) as alternative to client_secret_basic — out of scope for this slice.</li><li><strong>OQ-5</strong> (open): JAR (RFC 9101) signed request objects — defer to P3.</li></ul>\n<hr>\n<h2>§10 — Failure modes (33 rows)</h2>\n<table><thead><tr><th>#</th><th>Failure</th><th>Detection</th><th>Sev</th><th>Handler</th></tr></thead><tbody><tr><td>1</td><td>PKCE missing on public client</td><td>request validation</td><td>3</td><td>400 + sev-3</td></tr><tr><td>2</td><td>PKCE method = plain</td><td>request validation</td><td>3</td><td>400 + sev-3</td></tr><tr><td>3</td><td>code_challenge length out of [43, 128]</td><td>CHECK</td><td>3</td><td>400</td></tr><tr><td>4</td><td>code_verifier mismatch</td><td>PKCE verify</td><td>3</td><td>invalid_grant + sev-3</td></tr><tr><td>5</td><td>Authorization code TTL expired</td><td>timestamp</td><td>3</td><td>invalid_grant code_expired</td></tr><tr><td>6</td><td>Authorization code reuse</td><td>consumed_at != NULL</td><td>2</td><td>invalid_grant + family compromised + sev-2</td></tr><tr><td>7</td><td>Refresh-token reuse</td><td>state = used</td><td>1</td><td>family compromised + sev-1 audit</td></tr><tr><td>8</td><td>Refresh-token compromised family</td><td>state = compromised</td><td>1</td><td>invalid_grant + sev-1</td></tr><tr><td>9</td><td>Redirect_uri substring (non-exact)</td><td>string compare</td><td>3</td><td>400 redirect_uri_mismatch</td></tr><tr><td>10</td><td>HTTP non-loopback redirect at registration</td><td>URL parser</td><td>3</td><td>400 invalid_redirect</td></tr><tr><td>11</td><td>State parameter missing</td><td>request validation</td><td>3</td><td>400 state_required</td></tr><tr><td>12</td><td>Client_id unknown</td><td>DB lookup</td><td>3</td><td>400 invalid_client</td></tr><tr><td>13</td><td>Confidential client missing client_secret</td><td>authn check</td><td>2</td><td>401 invalid_client</td></tr><tr><td>14</td><td>Audience mismatch at resource server</td><td>aud claim compare</td><td>2</td><td>401 + sev-2</td></tr><tr><td>15</td><td>JWT signature invalid</td><td>JWKS verify</td><td>1</td><td>401 + sev-1</td></tr><tr><td>16</td><td>JWT exp passed</td><td>exp claim</td><td>3</td><td>401</td></tr><tr><td>17</td><td>JWT jti in revocation list</td><td>revocation list check</td><td>3</td><td>401 token_revoked</td></tr><tr><td>18</td><td>Revocation list cache stale &gt; 60s</td><td>TTL refresh</td><td>3</td><td>Refresh from DB</td></tr><tr><td>19</td><td>Scope outside MCP-001 registry</td><td>scope validation</td><td>3</td><td>400 invalid_scope</td></tr><tr><td>20</td><td>DCR public with confidential body</td><td>constraint violation</td><td>3</td><td>400</td></tr><tr><td>21</td><td>DCR &gt; 5 redirect_uris</td><td>CHECK constraint</td><td>3</td><td>400</td></tr><tr><td>22</td><td>DCR client_name &gt; 64 chars</td><td>CHECK</td><td>3</td><td>400</td></tr><tr><td>23</td><td>DCR confidential without tenant_admin role</td><td>RBAC</td><td>2</td><td>403 + sev-2</td></tr><tr><td>24</td><td>Redirect host not in tenant allowlist</td><td>policy check</td><td>3</td><td>400</td></tr><tr><td>25</td><td>Tenant allowlist mutation by non-admin</td><td>RBAC</td><td>2</td><td>403 + sev-2</td></tr><tr><td>26</td><td>prompt=none without active session</td><td>session check</td><td>3</td><td>redirect with error=login_required</td></tr><tr><td>27</td><td>Consent scope wider than previously granted</td><td>scope diff</td><td>3</td><td>Show consent screen</td></tr><tr><td>28</td><td>Discovery endpoint omits required field</td><td>response shape test</td><td>1</td><td>CI blocks</td></tr><tr><td>29</td><td>Introspection by public client</td><td>RBAC</td><td>2</td><td>401 + sev-2</td></tr><tr><td>30</td><td>Cross-tenant client lookup</td><td>RLS / tenant context</td><td>2</td><td>404 + sev-2</td></tr><tr><td>31</td><td>JWT issued without aud claim</td><td>code review + test</td><td>1</td><td>CI blocks</td></tr><tr><td>32</td><td>Audit emission fails</td><td>subprocess error</td><td>1</td><td>Retry via WAL; sev-1 if exhausted</td></tr><tr><td>33</td><td>Code expired but cleanup job fails</td><td>TTL cron</td><td>3</td><td>Periodic cleanup; sev-3</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> All OAuth endpoints serve under a single base path; the Discovery doc lists the absolute URLs.</p>\n<p><strong>§11.2</strong> Refresh tokens are stored as SHA-256 hashes (32 bytes), not Argon2 (we need fast lookup; collision-resistance of SHA-256 is sufficient since the token is 256-bit random).</p>\n<p><strong>§11.3</strong> Access JWT signing uses the FR-AUTH-004 active signing key from JWKS. Key rotation invalidates all in-flight access tokens (acceptable — they re-issue via refresh).</p>\n<p><strong>§11.4</strong> The revocation list is held in-process per gateway as <code>lru::LruCache&lt;Uuid, Instant&gt;</code> with 60s TTL. On cache miss, the gateway queries <code>oauth_revocation_list</code> and inserts.</p>\n<p><strong>§11.5</strong> Authorization codes are cleaned up by a daily cron that DELETEs rows where <code>expires_at &lt; now() - INTERVAL '1 day'</code>. The DELETE goes through <code>oauth_code_cleaner</code> privileged role since the table is REVOKE'd from cyberos_app.</p>\n<p><strong>§11.6</strong> The Discovery endpoint is publicly accessible (no auth) — RFC 8414 §3.</p>\n<p><strong>§11.7</strong> The Introspection endpoint requires a confidential client with <code>mcp_introspect</code> scope (a system scope reserved for resource servers).</p>\n<p><strong>§11.8</strong> Per-tenant redirect host allowlist supports wildcard <code>*.example.com</code> matching the leftmost label; <code>*</code> alone is forbidden.</p>\n<p><strong>§11.9</strong> Consent records are scoped to (subject, client) — not (subject, tenant). A subject who consents in one tenant context doesn't automatically consent in another.</p>\n<p><strong>§11.10</strong> The audience verification at /tools/call reads the expected canonical URL from env (<code>MCP_RESOURCE_SERVER_URL</code>). The env is set at deploy time per resource-server instance.</p>\n<p><strong>§11.11</strong> Tests use the FR-AUTH-004 testcontainers JWKS fixture for sign + verify.</p>\n<p><strong>§11.12</strong> The token endpoint supports both <code>client_secret_basic</code> (RFC 6749 §2.3.1) and <code>private_key_jwt</code> (RFC 7523). private_key_jwt requires the confidential client to have registered a JWKS endpoint at DCR time.</p>\n<p><strong>§11.13</strong> Refresh-token rotation: the new token is issued in the same <code>family_id</code>; the chain of <code>parent_token_hash</code> forms a linked list per family. Family compromise sweeps all rows with that family_id.</p>\n<p><strong>§11.14</strong> The DCR endpoint emits client credentials in the response once. Lost client_secret cannot be recovered; the tenant must DELETE and re-register.</p>\n<p><strong>§11.15</strong> The closed <code>oauth_error_code</code> enum maps to RFC 6749 §5.2's exact strings; the JSON response field is <code>\"error\"</code> (not <code>\"error_code\"</code>).</p>\n<p><strong>§11.16</strong> Authorization codes use 256-bit base64url-no-pad (43 chars); refresh tokens use the same shape. Both random via OS RNG.</p>\n<p><strong>§11.17</strong> The <code>consent_screen</code> is HTML rendered by the gateway with the client name + scope descriptions; the descriptions come from the FR-MCP-001 registry.</p>\n<p><strong>§11.18</strong> Token endpoint failure responses include <code>Cache-Control: no-store</code> and <code>Pragma: no-cache</code> per RFC 6749 §5.2.</p>\n<p><strong>§11.19</strong> The revocation endpoint returns 200 OK regardless of whether the token existed, to prevent probing.</p>\n<p><strong>§11.20</strong> The audience parameter on /authorize defaults to the MCP server origin (<code>https://&lt;host&gt;</code>) when not specified. Explicit specification is required when one client targets multiple resource servers within the same tenant.</p>\n<p><strong>§11.21</strong> PKCE constant-time equality prevents timing-channel leakage of the verifier hash.</p>\n<p><strong>§11.22</strong> The implementation uses the <code>oauth2</code> Rust crate as a foundation but layers our audience binding + reuse detection + audit emission on top.</p>\n<hr>\n<p><em>End of FR-MCP-004 spec.</em></p>";
