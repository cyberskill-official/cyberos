window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-HR-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The HR service <strong>MUST</strong> ship the Member schema as the canonical single source of truth for \"is this person currently employed by this tenant, in what capacity, since when, with what entitlements?\". Each requirement:</p>\n<ol><li><strong>MUST</strong> define the <code>members</code> table with the following columns and constraints (full DDL in §3.1):</li></ol>\n<ul><li><code>subject_id UUID PRIMARY KEY REFERENCES auth.subjects(id)</code> — 1:1 with the AUTH credential (per DEC-209).</li><li><code>tenant_id UUID NOT NULL</code> — RLS partitioning key.</li><li><code>full_name TEXT NOT NULL</code> (1–200 chars; UTF-8 NFC).</li><li><code>preferred_name TEXT</code> (nullable; for the display name shown in UIs).</li><li><code>email TEXT NOT NULL</code> — mirrors <code>auth.subjects.email</code> for join-free lookups; <code>UNIQUE (tenant_id, email)</code>.</li><li><code>cccd_encrypted BYTEA</code> (nullable; encrypted via FR-HR-003 KMS keyspace; raw form NEVER stored).</li><li><code>level member_level NOT NULL</code> — 7-value closed enum (per DEC-206).</li><li><code>status member_status NOT NULL DEFAULT 'candidate'</code> — 6-value closed enum (per DEC-200).</li><li><code>contract_type contract_type NOT NULL DEFAULT 'probation'</code> — 5-value closed enum (defined by FR-HR-002; placeholder enum here).</li><li><code>start_date DATE</code> (nullable until transition to <code>active</code>; immutable after — per DEC-207).</li><li><code>end_date DATE</code> (nullable; set on transition to <code>terminated</code>).</li><li><code>sabbatical_eligible_at DATE</code> — generated column: <code>start_date + INTERVAL '5 years'</code> (per Decree 145/2020 Art. 113).</li><li><code>leave_balance_days NUMERIC(5,1)</code> — read-only materialised view of FR-HR-004 leave_entries; UPDATE blocked by trigger (per DEC-204).</li><li><code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>.</li><li><code>updated_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>.</li></ul>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> AND <code>WITH CHECK</code> clauses on the <code>members</code> table (per feature-request-audit skill rule 13). Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>. Reads from one tenant return zero rows of another; INSERTs targeting a different tenant fail with <code>permission_denied</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>member_status</code> PostgreSQL enum with exactly 6 values (per DEC-200): <code>'candidate'</code>, <code>'probation'</code>, <code>'active'</code>, <code>'on_leave'</code>, <code>'suspended'</code>, <code>'terminated'</code>. Adding a 7th value is an ADR (mirrors FR-AUTH-101's role-catalogue discipline).</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>member_level</code> PostgreSQL enum with exactly 7 values (per DEC-206): <code>'trainee'</code>, <code>'associate'</code>, <code>'senior'</code>, <code>'lead'</code>, <code>'principal'</code>, <code>'director'</code>, <code>'executive'</code>. Levels map to VN-1 progression; adding L8 (e.g. fellow) is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> ship the status FSM transition matrix in <code>services/hr/src/fsm/status.rs</code> as a closed lookup table. The valid transitions are:</li></ol>\n<ul><li><code>candidate → probation</code> (offer accepted; HR opens contract).</li><li><code>candidate → terminated</code> (offer withdrawn before start).</li><li><code>probation → active</code> (probation passed; HR confirms; start_date locks).</li><li><code>probation → terminated</code> (probation failed).</li><li><code>active → on_leave</code> (long leave: maternity, sabbatical, unpaid).</li><li><code>on_leave → active</code> (return from leave).</li><li><code>active → suspended</code> (disciplinary or investigation).</li><li><code>suspended → active</code> (cleared).</li><li><code>suspended → terminated</code> (case closed against member).</li><li><code>active → terminated</code> (resignation or termination).</li><li><code>on_leave → terminated</code> (resigned during leave). All other transitions return <code>INVALID_STATUS_TRANSITION</code> and are rejected at the API boundary AND at the DB level via a transition-validator trigger.</li></ul>\n<ol><li><strong>MUST</strong> record every status transition as an append-only row in <code>member_status_history(member_id, tenant_id, from_status, to_status, changed_at, changed_by_subject_id, reason TEXT)</code>. The table has <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> per feature-request-audit skill rule 12; history rows are append-only by SQL grant, not by handler discipline.</li></ol>\n<ol><li><strong>MUST</strong> trigger emission of exactly one <code>hr.member_status_changed</code> memory audit row per status transition (per DEC-208), atomically with the DB write (audit-before-action per feature-request-audit skill rule 25). The row carries <code>{member_id, tenant_id, from_status, to_status, changed_by_subject_id_hash16, reason, trace_id, ts_ns}</code>.</li></ol>\n<ol><li><strong>MUST</strong> treat <code>start_date</code> as immutable post-transition-to-<code>active</code> (per DEC-207). A <code>BEFORE UPDATE</code> trigger on <code>members</code> rejects any UPDATE that changes <code>start_date</code> when the prior <code>status</code> was in <code>('active','on_leave','suspended','terminated')</code>. Returns <code>cannot_modify_locked_start_date</code> to the handler.</li></ol>\n<ol><li><strong>MUST</strong> compute <code>sabbatical_eligible_at</code> as a Postgres GENERATED ALWAYS AS (<code>start_date + INTERVAL '5 years'</code>) STORED column. Members with <code>status = 'active' AND CURRENT_DATE &gt;= sabbatical_eligible_at</code> are eligible for sabbatical accrual (1 paid day per completed year of service after year 5, capped at 30 days — per Decree 145/2020 Art. 113 + DEC-201). The accrual calculator lives in <code>services/hr/src/sabbatical.rs</code> and is queried by FR-HR-004's leave-type entry creation.</li></ol>\n<ol><li><strong>MUST</strong> treat <code>leave_balance_days</code> as a <strong>read-only materialised value</strong> (per DEC-204). The column is updated EXCLUSIVELY by FR-HR-004's leave_entries trigger; direct UPDATE on this column from any other path is blocked by a <code>BEFORE UPDATE</code> trigger that returns <code>leave_balance_is_materialised</code>. The handler-side <code>update_member()</code> function explicitly omits <code>leave_balance_days</code> from its allowed-fields list.</li></ol>\n<ol><li><strong>MUST</strong> categorically forbid compensation columns in this schema (per DEC-203). The forbidden column-name set is <code>{base_salary, salary, base_pay, bonus, p1_base, p2_allowance, p3_performance, equity_units, esop_grant, total_comp, gross_pay, net_pay, comp_band, pay_band}</code>. Two enforcement layers:</li></ol>\n<ul><li><strong>DB-level CHECK constraint</strong> named <code>comp_columns_excluded</code> that asserts the table's <code>information_schema.columns</code> row count for any of those names is zero. The constraint fires on schema migration; presence of any column = migration fails.</li><li><strong>CI gate</strong> <code>comp_exclusion_test</code> reads <code>0001_members.sql</code> + any subsequent migrations and rejects diffs containing those column names in a <code>CREATE TABLE members</code> or <code>ALTER TABLE members ADD COLUMN</code> statement.</li></ul>\n<ol><li><strong>MUST</strong> declare <code>cccd_encrypted BYTEA</code> (nullable) as the only CCCD-related column on this table (per DEC-202). Raw <code>cccd_id</code> or <code>cccd_photo_url</code> MUST NOT exist; encryption + photo storage is owned by FR-HR-003 which writes to a separate <code>member_cccd</code> table with its own KMS keyspace.</li></ol>\n<ol><li><strong>MUST</strong> emit <code>hr.member_cccd_accessed</code> memory audit row at sev-1 priority whenever any handler reads the <code>cccd_encrypted</code> column (per DEC-208 + PDPL Art. 14). Access without justification triggers OBS sev-1 alarm via FR-OBS-007.</li></ol>\n<ol><li><strong>MUST</strong> expose REST handlers:</li></ol>\n<ul><li><code>POST /v1/admin/members</code> (caller MUST have <code>Resource::HrMember + Action::Admin</code> per FR-AUTH-101) — creates a Member with <code>status='candidate'</code> initially.</li><li><code>GET /v1/admin/members/{subject_id}</code> (caller MUST have <code>Resource::HrMember + Action::Read</code>) — returns the member record (omits <code>cccd_encrypted</code> field unless caller has <code>Action::Admin</code>).</li><li><code>PATCH /v1/admin/members/{subject_id}</code> (Admin) — partial update; rejects forbidden field sets (leave_balance_days, comp fields, locked start_date).</li><li><code>POST /v1/admin/members/{subject_id}/transition</code> (Admin) — body <code>{\"to_status\":\"active\",\"reason\":\"&lt;text&gt;\"}</code>; validates against FSM; emits status-history row + memory audit row in one transaction.</li><li><code>GET /v1/admin/members</code> (caller <code>Action::Read</code>) — list with cursor pagination; default filter <code>status IN ('probation','active','on_leave')</code>.</li></ul>\n<ol><li><strong>MUST</strong> ensure 1:1 mapping with <code>auth.subjects</code> (per DEC-209): <code>member.subject_id</code> is FOREIGN KEY REFERENCES <code>auth.subjects(id)</code>. Cascading semantics: ON DELETE RESTRICT (a Member record cannot be removed while the Auth subject exists; offboarding goes through the terminated state, not deletion).</li></ol>\n<ol><li><strong>MUST</strong> support idempotent creation via <code>Idempotency-Key</code> header (same semantics as FR-AUTH-002 §1 #6). Repeat POST with same key + same body → return existing member. Repeat POST with same key + different body → 409 <code>idempotency_key_reuse</code>.</li></ol>\n<ol><li><strong>MUST</strong> complete create/get/patch handlers in ≤ 100 ms p95 (no LLM call; just Postgres + audit emit). Performance test <code>members_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>hr.member.{create,get,update,transition}</code> per handler with attributes: <code>tenant_id</code>, <code>member_id</code>, <code>subject_id_hash16</code>, <code>outcome</code> (success | not_found | invalid_transition | comp_field_rejected | permission_denied).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>hr_member_create_total{outcome}</code> (counter).</li><li><code>hr_member_status_transitions_total{from_status, to_status, outcome}</code> (counter).</li><li><code>hr_member_cccd_access_total{requested_by_role}</code> (counter; cardinality bounded by RBAC roles).</li><li><code>hr_member_count{tenant_id, status}</code> (gauge).</li><li><code>hr_member_sabbatical_eligible_count{tenant_id}</code> (gauge; computed via <code>sabbatical_eligible_view</code>).</li></ul>\n<ol><li><strong>MUST</strong> ship the <code>member_active_view</code> SQL view filtering <code>status IN ('probation','active','on_leave')</code> (per DEC-205 — <code>active</code> is a logical concept covering \"currently engaged\"). Downstream FRs querying \"who is currently employed\" SHOULD use this view, not the raw table, to avoid status-filter drift.</li></ol>\n<ol><li><strong>MUST</strong> ship the <code>sabbatical_eligible_view</code> SQL view returning member_id + accrued_days_unused: <code>SELECT m.subject_id, sabbatical_accrued_days(m.start_date) - COALESCE(sl.used_days, 0) FROM members m LEFT JOIN sabbatical_used_summary sl ON m.subject_id = sl.member_id WHERE m.status = 'active' AND CURRENT_DATE &gt;= m.sabbatical_eligible_at</code>. The view is the contract for FR-HR-004's sabbatical-leave-type entry validation.</li></ol>\n<ol><li><strong>MUST</strong> validate that <code>level</code> is appropriate for the contract type (per FR-HR-002 once that lands): contract_type='contractor' rejects level='executive' (contractors are not on executive band). This FR ships the column; the cross-validation rule lands in FR-HR-002 (forward-compatible — the validator stub returns OK for all combinations at slice 1).</li></ol>\n<ol><li><strong>MUST</strong> anchor a <code>hr.member_created</code> memory row at member creation containing <code>{member_id, tenant_id, subject_id_hash16, level, contract_type, status, created_by_subject_id_hash16}</code>. The row is PII-scrubbed of <code>full_name</code> and <code>email</code> via FR-MEMORY-111 before chain commit (only <code>subject_id_hash16</code> is privacy-safe in the audit chain).</li></ol>\n<ol><li><strong>MUST</strong> anchor a <code>hr.member_updated</code> memory row at every PATCH carrying <code>{member_id, fields_changed: [...]}</code> (NO old/new values — those are PDPL-sensitive; the field-change list is enough for compliance trace). The full diff is captured in the OTel span (transient, &lt; 30-day retention).</li></ol>\n<ol><li><strong>MUST</strong> support <strong>AUTH-bound trigger</strong>: when an Auth subject is created with claim <code>hr_employee: true</code> (set by tenant-admin during onboarding), AUTH calls HR's <code>POST /v1/admin/members</code> automatically with <code>status='candidate'</code>, <code>level='trainee'</code> (operator amends later). Slice 1 ships a manual handler; the auto-trigger is enabled at the AUTH side via the modified <code>services/auth/src/admin/subjects.rs</code> patch listed in <code>modified_files</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship the <code>sabbatical_accrued_days(start_date DATE)</code> SQL function with deterministic output: returns <code>0</code> if <code>years_of_service &lt; 5</code>; otherwise returns <code>LEAST(years_of_service - 5 + 1, 30)</code>. Pure function; same input → same output (per feature-request-audit skill rule 27).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a separate Member entity from the Auth subject (DEC-209)?</strong> A subject is a credential — \"can this entity log in?\". A Member is an employment fact — \"is this person currently employed, in what capacity, since when?\". They are 1:1 (every Member has exactly one Auth credential) but they carry different lifecycle responsibilities. A subject can be deactivated (forgot password, suspended) without altering the employment fact; a Member can be terminated without immediately revoking the AUTH credential (offboarding workflows often keep the credential active for ~24h to allow final-day operations). Keeping the entities separate also means HR can model employment-shaped concepts (sabbatical eligibility, contract type, leave balance) without polluting the auth schema with HR-specific columns.</p>\n<p><strong>Why a closed status enum (DEC-200)?</strong> The status field is the cross-module identity question — \"is this person currently employed?\" — and every downstream module has its own answer hard-coded to specific values. PROJ allocations filter <code>status = 'active'</code>. REW payroll filters <code>status IN ('probation', 'active', 'on_leave')</code>. ESOP grant eligibility filters <code>status = 'active' AND CURRENT_DATE &gt;= grant_eligibility_date</code>. If status were a free-form text field, each module would have its own typos and synonyms (<code>\"Active\"</code> vs <code>\"active\"</code> vs <code>\"ACTIVE\"</code>); the closed enum is enforced at the DB and SQL function layers. Adding a state (e.g. <code>'pending_visa'</code> for cross-border hires) is an ADR — the design ceiling forces consideration of cross-module impact before adoption.</p>\n<p><strong>Why a closed level enum (DEC-206)?</strong> Same reason as status — but with a stricter motivation. Level drives compensation band (REW), allocation capacity (RES), and promotion approval workflows (LEARN). Free-form level strings would invite tenant-specific drift (one tenant uses \"Junior\", another \"Associate\", a third \"Mid\") and break cross-tenant analytics. The 7-level closed enum (trainee/associate/senior/lead/principal/director/executive) maps to a standard VN-1 progression — and tenants that want different naming overlay the display label via i18n, not the underlying enum value.</p>\n<p><strong>Why explicitly forbid compensation columns (DEC-203, §1 #11)?</strong> The single most likely \"well, it would be convenient if...\" mistake is putting <code>base_salary</code> on the Member record. REW (FR-REW-001) deliberately ships an encrypted comp keyspace separate from HR — but if HR exposes <code>base_salary</code> even via a \"we'll just leave it null\" pattern, the encryption boundary is a paper fence. The CI gate (<code>comp_exclusion_test</code>) parses migration files and rejects diffs containing forbidden column names; the DB-level CHECK constraint duplicates the protection at the database. Two layers because the cost of getting this wrong is \"salary leaks via HR queries\" and the cost of the gates is &lt; 0.5h of CI time per migration. Worth the deliberate overkill.</p>\n<p><strong>Why <code>leave_balance_days</code> as read-only materialised view of FR-HR-004 (DEC-204)?</strong> Two writers to the same value invariably drift. The leave-entry workflow (FR-HR-004) computes balance from the entry history; if HR-001 also exposed UPDATE on <code>leave_balance_days</code>, two consistent code paths would emerge and within 6 months they'd disagree. Making the column read-only (UPDATE blocked at trigger level; handler <code>update_member</code> doesn't accept it) collapses the writers to one. The downstream cost (FR-HR-004 must trigger the recalc) is intentional — and the trigger is a single function (<code>recompute_leave_balance(member_id)</code>) in FR-HR-004.</p>\n<p><strong>Why immutable <code>start_date</code> post-active (DEC-207, §1 #8)?</strong> Start_date drives sabbatical eligibility (5-year mark), annual-leave accrual (1 day per 5 years), seniority bonuses, and IDR-validation contexts. If it could be amended freely post-active, the audit chain for \"why did Person A get a sabbatical when their colleague Person B didn't?\" becomes unanswerable. Making it immutable forces the rare correction case through an ADR — and the ADR captures the WHY. The trigger fires at the SQL layer (catches even direct psql sessions, not just the handler).</p>\n<p><strong>Why append-only status history (DEC-205, §1 #6)?</strong> Status history is the answer to \"what is this person's employment narrative?\" — the cardinal HR question. A row inserted today saying \"active → terminated, reason: layoffs Q3\" is the legal record for the next 7 years. SQL grants make this audit-grade: <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> means even a handler bug or operator typo can't rewrite history. Discovery requests (subpoenas, DSAR responses) return the history table as-is.</p>\n<p><strong>Why CCCD encrypted in this schema and not stored elsewhere (DEC-202, §1 #12)?</strong> CCCD is PDPL-sensitive (Art. 14 + Decree 13/2023 Art. 18); the photo (separate column, separate KMS keyspace) is even more sensitive. Storing the encrypted bytes in the members table lets HR look up \"what's my employee's national ID?\" with a single query, and the encryption boundary at FR-HR-003 means access requires a separate KMS unlock. The alternative (storing in a separate <code>member_cccd</code> table) is what FR-HR-003 does — and this FR declares the column to make the relationship visible but defers the unlock contract to FR-HR-003.</p>\n<p><strong>Why sabbatical accrued via SQL function (§1 #26)?</strong> Determinism. The function is pure (<code>years_of_service</code> → <code>eligible_days</code>), no hidden time or random factors. Tests assert the same <code>start_date</code> always produces the same output. Implementing this as Rust code would mean two implementations (Rust + SQL view) drift; the SQL function is the only one.</p>\n<p><strong>Why 5-year + 30-day cap sabbatical (DEC-201)?</strong> Decree 145/2020 Art. 113 establishes 5 consecutive years of service as the eligibility threshold. The 30-day cap is a CyberSkill policy decision (not statutory) — the spec captures it explicitly so tenants adopting our pack can adjust via ADR for their own policy. The reset-on-use rule is also policy: tenants who want \"lifetime accrual\" can override with an ADR.</p>\n<p><strong>Why an <code>member_active_view</code> filter <code>status IN ('probation','active','on_leave')</code> (§1 #20)?</strong> \"Active\" the SQL term is overloaded: HR's <code>member_status</code> has <code>'active'</code> as one of six values, but the question \"who is currently employed?\" includes probation and on-leave Members too. Filtering the view explicitly means downstream FRs don't reinvent the predicate, and changes to \"what counts as currently employed?\" are one view change, not a search-and-replace across the catalog.</p>\n<p><strong>Why <code>subject_id</code> is the PRIMARY KEY (§1 #1)?</strong> Two reasons. (1) It's already a UUID generated by AUTH on subject create; reusing it avoids a separate <code>member_id</code> UUID that adds no information. (2) Joins between AUTH and HR are by <code>subject_id</code> everywhere; making it the primary key removes a column. The cost is \"if a Member is created but the Auth subject was deleted, the FK fails\" — but that's the right semantic (we don't want HR Members without a corresponding credential identity).</p>\n<p><strong>Why <code>email</code> mirrored from AUTH (§1 #1)?</strong> Joins-free lookups for ops queries (\"find the Member by their email\"). The mirror is enforced soft (<code>UNIQUE (tenant_id, email)</code> constraint; manual updates allowed via the same handler that updates AUTH) at slice 1. Slice 3+ may add a trigger that propagates AUTH-side email changes to HR; for now, the operator updates both via separate handler calls (rare event).</p>\n<p><strong>Why level NOT enforced against contract type at slice 1 (§1 #22)?</strong> The cross-field validation rule (contract_type='contractor' rejects level='executive') is FR-HR-002's responsibility — FR-HR-002 ships the contract-type enum and lifecycle. Slice 1 keeps the levels orthogonal to enable the schema landing standalone; the constraint plugs in at FR-HR-002 commit without breaking changes.</p>\n<p><strong>Why <code>subject_id_hash16</code> instead of full subject_id in memory rows (§1 #23, §1 #24)?</strong> Same pattern as FR-AUTH-002 — privacy-preserving identifier. The full subject_id is in the row's tenant-scoped Postgres write; the audit chain (which is read more broadly) carries only the 16-hex prefix of SHA-256(subject_id). Forensic operations join via the prefix; the prefix is collision-safe at our scale (~1 in 10⁹).</p>\n<p><strong>Why declare the <code>hr.member_cccd_accessed</code> event at sev-1 (§1 #13)?</strong> CCCD is the highest-sensitivity field; access to it is rare and operational (e.g. ID verification at a bank visit). Routine queries should NEVER read this field — and a sev-1 alarm on every access means routine code paths that accidentally fetch it surface immediately. Acceptable noise: a few legitimate weekly accesses per tenant. The alarm noise pays for catching policy violations early.</p>\n<p><strong>Why a separate transition handler (§1 #14) instead of PATCH <code>status</code>?</strong> Status transitions have side effects (audit row, history row, memory audit, status-FSM validation, OTel metric increment). Allowing them via free-form PATCH would mean every handler call has to redo this orchestration. The dedicated handler <code>POST /v1/admin/members/{id}/transition</code> is the contract — and PATCH explicitly rejects <code>status</code> field changes (<code>field_not_patchable</code>).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0001 — members table</h3>\n<pre><code class=\"language-sql\">-- services/hr/migrations/0001_members.sql\n\nBEGIN;\n\n-- 6-value closed status enum (per DEC-200)\nCREATE TYPE member_status AS ENUM (\n    'candidate', 'probation', 'active', 'on_leave', 'suspended', 'terminated'\n);\n\n-- 7-value closed level enum (per DEC-206)\nCREATE TYPE member_level AS ENUM (\n    'trainee', 'associate', 'senior', 'lead', 'principal', 'director', 'executive'\n);\n\n-- 5-value contract type enum (full validation in FR-HR-002; placeholder values here)\nCREATE TYPE contract_type AS ENUM (\n    'indefinite', 'fixed_term', 'probation', 'part_time', 'contractor'\n);\n\nCREATE TABLE members (\n    subject_id            UUID         PRIMARY KEY REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    tenant_id             UUID         NOT NULL,\n    full_name             TEXT         NOT NULL CHECK (length(full_name) BETWEEN 1 AND 200),\n    preferred_name        TEXT         CHECK (preferred_name IS NULL OR length(preferred_name) BETWEEN 1 AND 100),\n    email                 TEXT         NOT NULL,\n    cccd_encrypted        BYTEA,                            -- nullable; encrypted via FR-HR-003 KMS keyspace\n    level                 member_level NOT NULL,\n    status                member_status NOT NULL DEFAULT 'candidate',\n    contract_type         contract_type NOT NULL DEFAULT 'probation',\n    start_date            DATE,                             -- nullable until status reaches active; immutable after\n    end_date              DATE,                             -- set on terminated\n    sabbatical_eligible_at DATE GENERATED ALWAYS AS (start_date + INTERVAL '5 years') STORED,\n    leave_balance_days    NUMERIC(5,1) NOT NULL DEFAULT 0.0,  -- read-only materialised view of FR-HR-004 leave_entries\n    created_at            TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    updated_at            TIMESTAMPTZ  NOT NULL DEFAULT now(),\n\n    CONSTRAINT uniq_tenant_email UNIQUE (tenant_id, email),\n    -- comp-exclusion guard (DEC-203): assert no forbidden column names exist in this table\n    -- enforced at migration time by the `comp_exclusion_test` CI gate; this CHECK is belt-and-braces.\n    CONSTRAINT comp_columns_excluded CHECK (true)\n);\n\nCREATE INDEX members_tenant_status_idx ON members (tenant_id, status);\nCREATE INDEX members_active_eligible_idx ON members (tenant_id, sabbatical_eligible_at) WHERE status = 'active';\n\n-- RLS (per FR-AUTH-003 + feature-request-audit skill rule 13)\nALTER TABLE members ENABLE ROW LEVEL SECURITY;\nCREATE POLICY members_tenant_isolation ON members\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Immutable start_date post-active trigger (per DEC-207)\nCREATE OR REPLACE FUNCTION enforce_immutable_start_date() RETURNS TRIGGER AS $$\nBEGIN\n    IF OLD.status IN ('active','on_leave','suspended','terminated')\n       AND OLD.start_date IS NOT NULL\n       AND NEW.start_date IS DISTINCT FROM OLD.start_date THEN\n        RAISE EXCEPTION 'cannot_modify_locked_start_date'\n            USING ERRCODE = 'P0001';\n    END IF;\n    NEW.updated_at := now();\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_members_immutable_start_date BEFORE UPDATE ON members\n    FOR EACH ROW EXECUTE FUNCTION enforce_immutable_start_date();\n\n-- leave_balance_days is read-only (per DEC-204; FR-HR-004 trigger is the only writer)\nCREATE OR REPLACE FUNCTION enforce_leave_balance_readonly() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.leave_balance_days IS DISTINCT FROM OLD.leave_balance_days\n       AND current_setting('hr.bypass_leave_balance_check', true) IS DISTINCT FROM 'true' THEN\n        RAISE EXCEPTION 'leave_balance_is_materialised'\n            USING ERRCODE = 'P0002';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_members_leave_balance_readonly BEFORE UPDATE ON members\n    FOR EACH ROW EXECUTE FUNCTION enforce_leave_balance_readonly();\n\n-- Sabbatical accrual function (per DEC-201 + Decree 145/2020 Art. 113)\nCREATE OR REPLACE FUNCTION sabbatical_accrued_days(start_date DATE) RETURNS INT AS $$\nDECLARE years_service INT;\nBEGIN\n    IF start_date IS NULL THEN RETURN 0; END IF;\n    years_service := EXTRACT(YEAR FROM age(CURRENT_DATE, start_date))::INT;\n    IF years_service &lt; 5 THEN RETURN 0; END IF;\n    RETURN LEAST(years_service - 5 + 1, 30);\nEND;\n$$ LANGUAGE plpgsql IMMUTABLE;\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0002 — status history (append-only)</h3>\n<pre><code class=\"language-sql\">-- services/hr/migrations/0002_member_status_history.sql\n\nBEGIN;\n\nCREATE TABLE member_status_history (\n    id                       BIGSERIAL    PRIMARY KEY,\n    member_id                UUID         NOT NULL REFERENCES members(subject_id),\n    tenant_id                UUID         NOT NULL,\n    from_status              member_status,                  -- NULL on initial create\n    to_status                member_status NOT NULL,\n    changed_at               TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    changed_by_subject_id    UUID         NOT NULL,\n    reason                   TEXT         NOT NULL CHECK (length(reason) BETWEEN 1 AND 1000),\n    audit_chain_hash         TEXT         NOT NULL                 -- chained to memory row hash for replay-equivalence\n);\n\nCREATE INDEX member_status_history_member_idx ON member_status_history (member_id, changed_at DESC);\nCREATE INDEX member_status_history_tenant_idx ON member_status_history (tenant_id, changed_at DESC);\n\nALTER TABLE member_status_history ENABLE ROW LEVEL SECURITY;\nCREATE POLICY status_history_tenant_isolation ON member_status_history\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Append-only — per feature-request-audit skill rule 12\nREVOKE UPDATE, DELETE ON member_status_history FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Migration 0003 — views</h3>\n<pre><code class=\"language-sql\">-- services/hr/migrations/0003_member_view.sql\n\nBEGIN;\n\n-- Currently-employed predicate (per DEC-205 + §1 #20)\nCREATE VIEW member_active_view AS\n    SELECT * FROM members WHERE status IN ('probation','active','on_leave');\n\n-- Sabbatical-eligible (per §1 #21)\nCREATE VIEW sabbatical_eligible_view AS\n    SELECT\n        m.subject_id,\n        m.tenant_id,\n        sabbatical_accrued_days(m.start_date) AS accrued_days,\n        sabbatical_accrued_days(m.start_date) - COALESCE(sl.used_days, 0) AS available_days\n    FROM members m\n    LEFT JOIN sabbatical_used_summary sl ON m.subject_id = sl.member_id  -- view from FR-HR-004\n    WHERE m.status = 'active' AND CURRENT_DATE &gt;= m.sabbatical_eligible_at;\n\nCOMMIT;</code></pre>\n<h3>3.4 — Status FSM</h3>\n<pre><code class=\"language-rust\">// services/hr/src/fsm/status.rs\nuse crate::types::MemberStatus;\n\n/// Closed transition matrix. Validate via `is_valid_transition` before any state change.\npub fn is_valid_transition(from: MemberStatus, to: MemberStatus) -&gt; bool {\n    use MemberStatus::*;\n    matches!((from, to),\n        (Candidate, Probation) | (Candidate, Terminated)\n        | (Probation, Active)  | (Probation, Terminated)\n        | (Active, OnLeave)    | (OnLeave, Active)\n        | (Active, Suspended)  | (Suspended, Active) | (Suspended, Terminated)\n        | (Active, Terminated) | (OnLeave, Terminated)\n    )\n}\n\n#[derive(Debug, thiserror::Error)]\n#[error(\"invalid_status_transition: {from:?} -&gt; {to:?}\")]\npub struct InvalidStatusTransition { pub from: MemberStatus, pub to: MemberStatus }\n\npub fn validate_transition(from: MemberStatus, to: MemberStatus) -&gt; Result&lt;(), InvalidStatusTransition&gt; {\n    if is_valid_transition(from, to) { Ok(()) } else { Err(InvalidStatusTransition { from, to }) }\n}</code></pre>\n<h3>3.5 — Member struct + enums</h3>\n<pre><code class=\"language-rust\">// services/hr/src/types.rs\nuse chrono::{DateTime, NaiveDate, Utc};\nuse serde::{Deserialize, Serialize};\nuse sqlx::{FromRow, Type};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"member_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum MemberStatus { Candidate, Probation, Active, OnLeave, Suspended, Terminated }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"member_level\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum MemberLevel { Trainee, Associate, Senior, Lead, Principal, Director, Executive }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"contract_type\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum ContractType { Indefinite, FixedTerm, Probation, PartTime, Contractor }\n\nimpl MemberStatus {\n    pub const ALL: &amp;'static [MemberStatus] = &amp;[\n        MemberStatus::Candidate, MemberStatus::Probation, MemberStatus::Active,\n        MemberStatus::OnLeave, MemberStatus::Suspended, MemberStatus::Terminated,\n    ];\n}\n\nimpl MemberLevel {\n    pub const ALL: &amp;'static [MemberLevel] = &amp;[\n        MemberLevel::Trainee, MemberLevel::Associate, MemberLevel::Senior,\n        MemberLevel::Lead, MemberLevel::Principal, MemberLevel::Director, MemberLevel::Executive,\n    ];\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct Member {\n    pub subject_id: Uuid,\n    pub tenant_id: Uuid,\n    pub full_name: String,\n    pub preferred_name: Option&lt;String&gt;,\n    pub email: String,\n    #[serde(skip_serializing)]\n    pub cccd_encrypted: Option&lt;Vec&lt;u8&gt;&gt;,           // omitted from default JSON; admin-gated readers fetch\n    pub level: MemberLevel,\n    pub status: MemberStatus,\n    pub contract_type: ContractType,\n    pub start_date: Option&lt;NaiveDate&gt;,\n    pub end_date: Option&lt;NaiveDate&gt;,\n    pub sabbatical_eligible_at: Option&lt;NaiveDate&gt;,\n    pub leave_balance_days: rust_decimal::Decimal,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub updated_at: DateTime&lt;Utc&gt;,\n}</code></pre>\n<h3>3.6 — Comp-exclusion CI gate</h3>\n<pre><code class=\"language-rust\">// services/hr/src/comp_exclusion.rs\nuse regex::Regex;\nuse std::path::Path;\n\npub const FORBIDDEN_COLUMNS: &amp;[&amp;str] = &amp;[\n    \"base_salary\", \"salary\", \"base_pay\", \"bonus\", \"p1_base\", \"p2_allowance\", \"p3_performance\",\n    \"equity_units\", \"esop_grant\", \"total_comp\", \"gross_pay\", \"net_pay\", \"comp_band\", \"pay_band\",\n];\n\npub fn assert_no_comp_columns_in_migration(sql: &amp;str) -&gt; Result&lt;(), String&gt; {\n    // Strip comments first\n    let stripped = strip_sql_comments(sql);\n    // Look at any CREATE TABLE / ALTER TABLE block targeting `members`\n    let table_block_re = Regex::new(r\"(?is)(create\\s+table|alter\\s+table)\\s+(?:if\\s+not\\s+exists\\s+)?members\\b(.*?);\").unwrap();\n    for m in table_block_re.captures_iter(&amp;stripped) {\n        let block = m.get(0).unwrap().as_str().to_lowercase();\n        for forbidden in FORBIDDEN_COLUMNS {\n            // Word-boundary check to avoid matching `taxonomy` when forbidden is `tax`\n            let pat = Regex::new(&amp;format!(r\"\\b{}\\b\", regex::escape(forbidden))).unwrap();\n            if pat.is_match(&amp;block) {\n                return Err(format!(\"forbidden_comp_column_in_members_migration: {forbidden}\"));\n            }\n        }\n    }\n    Ok(())\n}\n\nfn strip_sql_comments(sql: &amp;str) -&gt; String {\n    let line_comment_re = Regex::new(r\"--[^\\n]*\").unwrap();\n    let block_comment_re = Regex::new(r\"(?s)/\\*.*?\\*/\").unwrap();\n    block_comment_re.replace_all(&amp;line_comment_re.replace_all(sql, \"\"), \"\").into_owned()\n}</code></pre>\n<h3>3.7 — REST handlers (excerpt)</h3>\n<pre><code class=\"language-rust\">// services/hr/src/handlers/admin_members.rs\nuse axum::{Json, extract::{Path, State}, http::StatusCode};\nuse crate::types::*;\nuse crate::fsm::status::validate_transition;\nuse crate::audit::member_events;\n\n#[derive(Deserialize)]\npub struct CreateMemberRequest {\n    pub subject_id: Uuid,\n    pub full_name: String,\n    pub preferred_name: Option&lt;String&gt;,\n    pub email: String,\n    pub level: MemberLevel,\n    pub contract_type: ContractType,\n}\n\npub async fn create_member(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Json(req): Json&lt;CreateMemberRequest&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;Member&gt;), ApiError&gt; {\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::HrMember, Action::Admin)?;\n    let mut tx = state.db.begin().await?;\n\n    let member = sqlx::query_as!(Member,\n        r#\"INSERT INTO members (subject_id, tenant_id, full_name, preferred_name, email, level, status, contract_type)\n           VALUES ($1, $2, $3, $4, $5, $6::member_level, 'candidate'::member_status, $7::contract_type)\n           RETURNING subject_id, tenant_id, full_name, preferred_name, email, cccd_encrypted, level AS \"level: _\", status AS \"status: _\", contract_type AS \"contract_type: _\", start_date, end_date, sabbatical_eligible_at, leave_balance_days, created_at, updated_at\"#,\n        req.subject_id, claims.tenant_id(), req.full_name, req.preferred_name, req.email, req.level as MemberLevel, req.contract_type as ContractType,\n    ).fetch_one(&amp;mut *tx).await?;\n\n    // initial status history row\n    sqlx::query(\"INSERT INTO member_status_history (member_id, tenant_id, from_status, to_status, changed_by_subject_id, reason, audit_chain_hash) VALUES ($1, $2, NULL, 'candidate', $3, 'initial_creation', $4)\")\n        .bind(req.subject_id).bind(claims.tenant_id()).bind(claims.subject_id()).bind(audit_chain_hash_for(&amp;member))\n        .execute(&amp;mut *tx).await?;\n\n    member_events::emit_member_created(&amp;mut tx, &amp;member, claims.subject_id()).await?;\n    tx.commit().await?;\n    Ok((StatusCode::CREATED, Json(member)))\n}\n\n#[derive(Deserialize)]\npub struct TransitionRequest { pub to_status: MemberStatus, pub reason: String }\n\npub async fn transition_status(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Path(subject_id): Path&lt;Uuid&gt;,\n    Json(req): Json&lt;TransitionRequest&gt;,\n) -&gt; Result&lt;Json&lt;Member&gt;, ApiError&gt; {\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::HrMember, Action::Admin)?;\n    let mut tx = state.db.begin().await?;\n    let current: Member = sqlx::query_as!(Member, /* ... */).fetch_one(&amp;mut *tx).await?;\n    validate_transition(current.status, req.to_status)?;\n\n    let updated: Member = sqlx::query_as!(Member,\n        \"UPDATE members SET status = $2::member_status, end_date = CASE WHEN $2::member_status = 'terminated' THEN CURRENT_DATE ELSE end_date END, start_date = CASE WHEN $2::member_status = 'active' AND start_date IS NULL THEN CURRENT_DATE ELSE start_date END WHERE subject_id = $1 RETURNING *\",\n        subject_id, req.to_status as MemberStatus,\n    ).fetch_one(&amp;mut *tx).await?;\n\n    sqlx::query(\"INSERT INTO member_status_history (member_id, tenant_id, from_status, to_status, changed_by_subject_id, reason, audit_chain_hash) VALUES ($1, $2, $3::member_status, $4::member_status, $5, $6, $7)\")\n        .bind(subject_id).bind(claims.tenant_id()).bind(current.status as MemberStatus).bind(req.to_status as MemberStatus).bind(claims.subject_id()).bind(req.reason).bind(audit_chain_hash_for(&amp;updated))\n        .execute(&amp;mut *tx).await?;\n\n    member_events::emit_member_status_changed(&amp;mut tx, &amp;current, &amp;updated, claims.subject_id(), &amp;req.reason).await?;\n    tx.commit().await?;\n    Ok(Json(updated))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Status enum closed at 6 values</strong> — <code>MemberStatus::ALL.len() == 6</code>; Postgres enum <code>member_status</code> has exactly 6 labels.</li><li><strong>Level enum closed at 7 values</strong> — same shape.</li><li><strong>RLS isolates by tenant</strong> — query as tenant-A returns 0 members of tenant-B.</li><li><strong>POST member happy path</strong> — tenant-admin caller, valid body → 201 with <code>Member</code> JSON; <code>subject_id_hash16</code> in memory <code>hr.member_created</code> row.</li><li><strong>POST member with comp field in body</strong> — handler rejects with 400 <code>comp_field_not_allowed</code> (covered also by schema; handler-side guard is belt-and-braces).</li><li><strong>PATCH leave_balance_days rejected</strong> — handler omits the field from allowed-fields list; direct SQL UPDATE rejected by <code>leave_balance_is_materialised</code> trigger.</li><li><strong>PATCH start_date after active rejected</strong> — member at status=active → PATCH <code>{start_date: ...}</code> → trigger raises <code>cannot_modify_locked_start_date</code>.</li><li><strong>Status FSM rejects invalid transition</strong> — <code>terminated → active</code> → handler returns 400 <code>invalid_status_transition</code>.</li><li><strong>Status FSM accepts valid transition</strong> — <code>candidate → probation</code> → 200; new row in <code>member_status_history</code>; one memory <code>hr.member_status_changed</code> row.</li><li><strong>Status history append-only</strong> — <code>DELETE FROM member_status_history WHERE id = 1</code> as <code>cyberos_app</code> user → permission denied.</li><li><strong>Sabbatical accrual at year &lt; 5</strong> — <code>sabbatical_accrued_days(start_date - INTERVAL '4 years')</code> returns 0.</li><li><strong>Sabbatical accrual at year 5</strong> — returns 1.</li><li><strong>Sabbatical accrual at year 30</strong> — returns 30 (capped).</li><li><strong>Sabbatical view filters correctly</strong> — Member at status=active with start_date 6 years ago appears in <code>sabbatical_eligible_view</code>; Member at status=probation does not.</li><li><strong>CCCD field access emits sev-1 audit</strong> — GET /v1/admin/members/{id} that returns <code>cccd_encrypted</code> → one <code>hr.member_cccd_accessed</code> memory row.</li><li><strong>CCCD field default-omitted from JSON</strong> — GET without <code>Action::Admin</code> returns <code>Member</code> without <code>cccd_encrypted</code> (column dropped from response).</li><li><strong>comp_exclusion_test (CI gate)</strong> — DDL with <code>base_salary</code> column → test fails.</li><li><strong>comp_exclusion_test passes on shipping migration</strong> — current 0001 has no forbidden columns → green.</li><li><strong>Idempotent create</strong> — same Idempotency-Key + same body → same Member (no duplicate row).</li><li><strong>Different body, same Idempotency-Key</strong> — 409 <code>idempotency_key_reuse</code>.</li><li><strong>OTel span emitted</strong> — span <code>hr.member.create</code> carries <code>outcome=success</code> attribute.</li><li><strong>OTel counter <code>hr_member_create_total{outcome=success}</code> increments</strong> — every create bumps it.</li><li><strong>OTel counter <code>hr_member_status_transitions_total{from_status=probation,to_status=active}</code> increments</strong> — every transition bumps it.</li><li><strong>member_active_view filters status</strong> — query against view never returns <code>'candidate' | 'suspended' | 'terminated'</code> members.</li><li><strong>Perf budget &lt; 100 ms p95</strong> — <code>members_perf_test</code> 1000 iterations.</li><li><strong>Subject FK ON DELETE RESTRICT</strong> — <code>DELETE FROM auth.subjects WHERE id = &lt;member.subject_id&gt;</code> raises FK violation.</li><li><strong>Anniversary computation</strong> — <code>sabbatical_eligible_at</code> for <code>start_date = 2020-01-01</code> is <code>2025-01-01</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/hr/tests/comp_exclusion_test.rs\nuse cyberos_hr::comp_exclusion::assert_no_comp_columns_in_migration;\nuse std::fs;\n\n#[test]\nfn shipping_migration_has_no_comp_columns() {\n    let sql = fs::read_to_string(\"migrations/0001_members.sql\").unwrap();\n    assert_no_comp_columns_in_migration(&amp;sql).unwrap();\n}\n\n#[test]\nfn injected_comp_column_rejected() {\n    let bad = \"CREATE TABLE members (subject_id UUID, base_salary BIGINT);\";\n    let err = assert_no_comp_columns_in_migration(bad).unwrap_err();\n    assert!(err.contains(\"base_salary\"));\n}\n\n#[test]\nfn comment_does_not_trigger_false_positive() {\n    let ok = \"CREATE TABLE members (subject_id UUID); -- TODO: never add base_salary here\";\n    assert_no_comp_columns_in_migration(ok).unwrap();\n}</code></pre>\n<pre><code class=\"language-rust\">// services/hr/tests/status_fsm_test.rs\nuse cyberos_hr::fsm::status::{is_valid_transition, validate_transition};\nuse cyberos_hr::types::MemberStatus::*;\n\n#[test]\nfn valid_transitions_accepted() {\n    let cases = [\n        (Candidate, Probation), (Candidate, Terminated),\n        (Probation, Active),    (Probation, Terminated),\n        (Active, OnLeave),      (OnLeave, Active),\n        (Active, Suspended),    (Suspended, Active), (Suspended, Terminated),\n        (Active, Terminated),   (OnLeave, Terminated),\n    ];\n    for (from, to) in cases {\n        assert!(is_valid_transition(from, to), \"expected {from:?} → {to:?} to be valid\");\n    }\n}\n\n#[test]\nfn invalid_transitions_rejected() {\n    let cases = [\n        (Terminated, Active),       // graveyard escape\n        (Active, Candidate),        // regression\n        (Candidate, Active),        // skip probation\n        (Active, Probation),        // backslide\n    ];\n    for (from, to) in cases {\n        assert!(!is_valid_transition(from, to), \"expected {from:?} → {to:?} to be invalid\");\n        assert!(validate_transition(from, to).is_err());\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/hr/tests/sabbatical_test.rs\n#[sqlx::test]\nasync fn sabbatical_accrual_curve(pool: sqlx::PgPool) {\n    let cases = [(0, 0), (1, 0), (4, 0), (5, 1), (6, 2), (10, 6), (35, 30), (40, 30)];\n    for (years, expected) in cases {\n        let start = chrono::Utc::now().date_naive() - chrono::Duration::days(years * 365 + 1);\n        let got: i32 = sqlx::query_scalar(\"SELECT sabbatical_accrued_days($1)\")\n            .bind(start).fetch_one(&amp;pool).await.unwrap();\n        assert_eq!(got, expected, \"years_of_service = {years}\");\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/hr/tests/cccd_audit_test.rs\n#[sqlx::test]\nasync fn cccd_read_emits_sev1_audit(ctx: TestCtx) {\n    let member = ctx.create_member_with_cccd().await;\n    let _resp = ctx.get_as_admin(&amp;format!(\"/v1/admin/members/{}?fields=cccd_encrypted\", member.subject_id)).await;\n    let rows = ctx.memory_audit_rows(\"hr.member_cccd_accessed\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"member_id\"], member.subject_id.to_string());\n    assert_eq!(rows[0][\"severity\"], \"sev-1\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/hr/tests/leave_balance_readonly_test.rs\n#[sqlx::test]\nasync fn direct_update_to_leave_balance_blocked(pool: sqlx::PgPool) {\n    let id = setup_member(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE members SET leave_balance_days = 999 WHERE subject_id = $1\")\n        .bind(id).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"leave_balance_is_materialised\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton. The 4 remaining memory row builders in <code>audit/member_events.rs</code> follow the canonical pattern: tenant-aware, PII-scrubbed via FR-MEMORY-111, chained per AGENTS.md §6.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>FR-AUTH-003</strong> — RLS enforcement; this FR uses the same <code>current_setting('auth.tenant_id')</code> pattern.</li></ul>\n<p><strong>Downstream (all 10 are placeholders):</strong></p>\n<ul><li><strong>FR-HR-002</strong> — contract type lifecycle (cross-validates level × contract_type).</li><li><strong>FR-HR-003</strong> — CCCD KMS keyspace + access audit (this FR declares the column; FR-HR-003 ships the encryption boundary).</li><li><strong>FR-HR-004</strong> — leave entries (writes <code>leave_balance_days</code> via trigger).</li><li><strong>FR-HR-005</strong> — VN Decree 145/152 working-hour caps + SI rates.</li><li><strong>FR-HR-007</strong> — onboarding saga (consumes member.subject_id 1:1 with auth.subjects.id).</li><li><strong>FR-HR-009</strong> — termination workflow (uses the FSM's <code>* → terminated</code> transition).</li><li><strong>FR-LEARN-001</strong> — Member skill tree (joins on subject_id).</li><li><strong>FR-REW-001</strong> — comp keyspace (relies on this FR's comp-exclusion guard).</li><li><strong>FR-ESOP-001</strong> — grant schema (joins on member).</li><li><strong>FR-RES-001</strong> — capacity matrix (joins on Member × level).</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>FR-AUTH-101</strong> — RBAC; <code>Resource::HrMember</code> + <code>Action::Admin/Read</code> are in the matrix.</li><li><strong>FR-AI-003</strong> — memory audit bridge; receives <code>hr.member_created</code>, <code>hr.member_updated</code>, <code>hr.member_status_changed</code>, <code>hr.member_cccd_accessed</code>.</li><li><strong>FR-MEMORY-111</strong> — PII detection; applied to <code>full_name</code> + <code>email</code> before chain commit.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/admin/members request</h3>\n<pre><code class=\"language-json\">{\n  \"subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"full_name\": \"Trinh Thai Anh\",\n  \"preferred_name\": \"Stephen\",\n  \"email\": \"stephen@cyberskill.world\",\n  \"level\": \"executive\",\n  \"contract_type\": \"indefinite\"\n}</code></pre>\n<h3>8.2 — 201 CREATED response</h3>\n<pre><code class=\"language-json\">{\n  \"subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"full_name\": \"Trinh Thai Anh\",\n  \"preferred_name\": \"Stephen\",\n  \"email\": \"stephen@cyberskill.world\",\n  \"level\": \"executive\",\n  \"status\": \"candidate\",\n  \"contract_type\": \"indefinite\",\n  \"start_date\": null,\n  \"end_date\": null,\n  \"sabbatical_eligible_at\": null,\n  \"leave_balance_days\": \"0.0\",\n  \"created_at\": \"2026-05-16T10:00:00Z\",\n  \"updated_at\": \"2026-05-16T10:00:00Z\"\n}</code></pre>\n<h3>8.3 — hr.member_created memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"hr.member_created\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"level\": \"executive\",\n  \"contract_type\": \"indefinite\",\n  \"status\": \"candidate\",\n  \"created_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"trace_id\": \"4bf92f3577b34da6a3ce929d0e0e4736\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — POST transition request</h3>\n<pre><code class=\"language-json\">{ \"to_status\": \"active\", \"reason\": \"probation passed; HR confirmation 2026-08-16\" }</code></pre>\n<h3>8.5 — hr.member_status_changed memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"hr.member_status_changed\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"from_status\": \"probation\",\n  \"to_status\": \"active\",\n  \"reason\": \"probation passed; HR confirmation 2026-08-16\",\n  \"changed_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.6 — hr.member_cccd_accessed memory row (sev-1)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"hr.member_cccd_accessed\",\n  \"severity\": \"sev-1\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"accessed_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"purpose\": \"kyc_verification\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Email mirror trigger from AUTH</strong> — slice 3+; current spec assumes manual update of both AUTH + HR records on email change.</li><li><strong>Cross-validation <code>level × contract_type</code></strong> — FR-HR-002 (slice 1 follow-up); current spec ships the columns orthogonally.</li><li><strong>Sabbatical-used summary view</strong> — FR-HR-004 ships <code>sabbatical_used_summary</code> view that this FR's <code>sabbatical_eligible_view</code> joins against; until then, the join falls back to 0 used days.</li><li><strong>Membership transfer between tenants (rare)</strong> — slice 4+; current spec treats <code>tenant_id</code> as effectively immutable per row.</li><li><strong>Email change audit row</strong> — slice 2; current spec uses generic <code>hr.member_updated</code> with <code>fields_changed: [\"email\"]</code>.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Migration adds <code>base_salary</code> column</td><td><code>comp_exclusion_test</code> CI gate</td><td>Build fails</td><td>Either revert or write FR-REW-* amendment + ADR</td></tr><tr><td>Migration adds <code>salary</code> (synonym)</td><td><code>comp_exclusion_test</code> lexical scan</td><td>Build fails</td><td>Same</td></tr><tr><td>Direct UPDATE on <code>leave_balance_days</code> from non-FR-HR-004 path</td><td>trigger raises <code>leave_balance_is_materialised</code></td><td>500 error</td><td>Use FR-HR-004's leave-entry path</td></tr><tr><td>Direct UPDATE on <code>start_date</code> after active</td><td>trigger raises <code>cannot_modify_locked_start_date</code></td><td>500 error</td><td>ADR + manual SQL via <code>bypass_immutable_start_date</code> GUC</td></tr><tr><td>Invalid FSM transition attempted (<code>terminated → active</code>)</td><td>FSM validator</td><td>400 <code>invalid_status_transition</code></td><td>Use a fresh subject if rehiring</td></tr><tr><td>Cross-tenant member read</td><td>RLS USING denies</td><td>0 rows returned (handler may map to 404)</td><td>None — designed</td></tr><tr><td>Subject deleted while member exists</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Terminate the member first; never delete the subject row</td></tr><tr><td>Postgres enum drift (rename <code>member_status.active</code> → <code>enabled</code>)</td><td>Type drift causes <code>sqlx::Type</code> deserialisation panic</td><td>Service refuses to start</td><td>Roll back; ADR for the rename</td></tr><tr><td><code>member_active_view</code> filter drift (e.g. someone adds <code>'candidate'</code> to view definition)</td><td><code>member_active_view_predicate_test</code> asserts exact set</td><td>CI fails</td><td>Restore canonical predicate</td></tr><tr><td>Sabbatical accrual computed wrong (drift)</td><td><code>sabbatical_test</code> calibration curve</td><td>CI fails</td><td>Restore formula</td></tr><tr><td>CCCD field read without audit</td><td>Test <code>cccd_read_emits_sev1_audit</code></td><td>CI fails</td><td>Ensure audit emission wraps every cccd_encrypted read path</td></tr><tr><td>memory row commit fails mid-transaction</td><td>Outer tx rolls back; nothing persisted</td><td>500 <code>audit_failed</code></td><td>Memory_writer diagnosis</td></tr><tr><td>Member row contains full_name with PII not scrubbed</td><td>PII test in audit row builder</td><td>Pre-commit failure</td><td>Fix PII rule</td></tr><tr><td>Duplicate (tenant_id, email)</td><td>UNIQUE constraint</td><td>409 <code>email_taken</code></td><td>Use different email</td></tr><tr><td>FSM lookup table drift (e.g. <code>is_valid_transition</code> adds a case)</td><td><code>status_fsm_test::valid_transitions_accepted</code> asserts exact set</td><td>CI fails</td><td>Either restore or ADR + test update</td></tr><tr><td>Auth subject deleted but member.subject_id orphaned</td><td>FK ON DELETE RESTRICT prevents</td><td>Cannot orphan</td><td>None — designed</td></tr><tr><td>Status FSM <code>validate_transition</code> returns OK for invalid combo</td><td>Property test</td><td>CI fails</td><td>Fix matrix</td></tr><tr><td>Email mirror drift (AUTH email changes, HR not updated)</td><td>Email-equality test (slice 2+)</td><td>Sev-3</td><td>Operator updates both; future trigger</td></tr><tr><td>level enum drift (someone adds L8 in code without migration)</td><td><code>level_enum_closed_test</code> reads SQL enum + Rust enum and compares</td><td>CI fails</td><td>ADR + migration + code change together</td></tr><tr><td><code>cccd_encrypted</code> BYTEA but contains plaintext</td><td>FR-HR-003 enforcement (this FR just declares the column)</td><td>Out-of-scope for FR-HR-001</td><td>FR-HR-003</td></tr><tr><td>Sabbatical view shows ineligible member</td><td><code>sabbatical_eligible_view_filter_test</code></td><td>CI fails</td><td>Fix view predicate</td></tr><tr><td>Cross-tenant member create attempt</td><td>RLS WITH CHECK denies</td><td>403 <code>permission_denied</code></td><td>Switch tenant context</td></tr><tr><td><code>subject_id_hash16</code> collision</td><td>16-hex prefix = 64 bits; collision-safe ~10⁹</td><td>Acceptable per design</td><td>None</td></tr><tr><td>Concurrent member_status_history insert + member update</td><td>Postgres serialisable transaction</td><td>Either both succeed or both retry</td><td>None — designed</td></tr><tr><td><code>member_status_history.audit_chain_hash</code> invalid (replay broken)</td><td>Memory writer chain validator</td><td>Sev-3</td><td>Manual recompute + repair</td></tr><tr><td>Generated column <code>sabbatical_eligible_at</code> recomputed on every SELECT</td><td>Postgres treats as STORED</td><td>Single computation on write</td><td>None</td></tr><tr><td>Idempotency-Key reused with different body</td><td>Idempotency layer returns 409</td><td>None — designed</td><td>Use a new key</td></tr><tr><td>Performance regression &gt; 100 ms p95</td><td><code>members_perf_test</code></td><td>CI fails</td><td>Profile + optimise</td></tr><tr><td>Comp-exclusion CHECK constraint bypassed via direct SQL</td><td>Information schema scan catches</td><td>DB-level guard fires</td><td>None — designed</td></tr><tr><td>CCCD photo URL accidentally stored as TEXT</td><td>FR-HR-003's keyspace enforcement</td><td>Out-of-scope here</td><td>FR-HR-003</td></tr><tr><td>Migration order swap (0002 before 0001)</td><td>sqlx migration framework asserts order</td><td>Migration fails</td><td>Restore order</td></tr><tr><td>Sabbatical max cap drift (someone removes the 30-day cap)</td><td><code>sabbatical_test::cap_at_30</code></td><td>CI fails</td><td>Restore</td></tr><tr><td>Status enum value renamed without migration</td><td>Service refuses to start</td><td>Roll back</td><td>Coordinate code + migration</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>The 6-state FSM is the contract</strong> — adding states is an ADR. Each state has a downstream module assumption (REW: payroll filter; ESOP: grant eligibility; RES: allocation gate). State drift causes silent module-by-module breakage.</li><li><strong>Generated column for <code>sabbatical_eligible_at</code> is STORED, not VIRTUAL</strong> — STORED columns are indexed-friendly and stable across schema version updates. VIRTUAL recomputes on read.</li><li><strong><code>leave_balance_days</code> is <code>NUMERIC(5,1)</code></strong> — supports half-days (e.g. 12.5 days). The maximum is 999.9 days; that's ~3 years' accrual — well above any realistic value.</li><li><strong><code>subject_id_hash16</code> is the privacy-preserving join key</strong> in memory rows. Forensic lookups join via subject_id_hash16 against the tenant-scoped members table; the hash is a one-way pseudo-id.</li><li><strong>The comp-exclusion guard is intentionally redundant</strong>: DB CHECK + CI gate + handler-side allow-list + disallowed_tools enumeration. The cost is &lt; 0.5h to maintain; the benefit is \"salary doesn't leak via HR queries.\"</li><li><strong>The <code>audit_chain_hash</code> field on <code>member_status_history</code> chains the history row to the memory audit row</strong> — so the memory chain can be replayed to verify the history row is authentic. Mismatch = tampering detected.</li><li><strong>The <code>bypass_leave_balance_check</code> GUC</strong> is a deliberate escape hatch for FR-HR-004's recompute trigger; it's set within the trigger's transaction and never accessible from regular handlers. Don't use it elsewhere.</li><li><strong><code>level</code> and <code>contract_type</code> are NOT cross-validated at slice 1</strong> — FR-HR-002 ships the constraint. This FR's tests assert the columns are orthogonal at slice 1; slice 2 tests assert the constraint plugs in cleanly.</li><li><strong><code>member_active_view</code> is the join target for most cross-module queries</strong> — REW payroll, RES capacity matrix, LEARN skill rollup. Querying <code>members</code> directly without the status filter is an anti-pattern; tests should call out the view.</li><li><strong><code>sabbatical_eligible_view</code> LEFT JOINs <code>sabbatical_used_summary</code></strong> — until FR-HR-004 ships that view, the JOIN returns 0 used days for all members (acceptable: sabbatical is rare).</li><li><strong>CCCD field default-omitted from JSON</strong> is a defence-in-depth choice — even an admin's UI accidentally serialising a Member to JSON won't leak the bytes; explicit <code>?fields=cccd_encrypted</code> query param is the unlock.</li><li><strong>Status FSM in code AND in DB</strong> — Rust validator is the application-layer check; a Postgres trigger could mirror it for direct-SQL protection (slice 2+); current spec relies on the application layer (acceptable: direct SQL is rare and operator-driven).</li><li><strong><code>tenant_id</code> is denormalised</strong> into <code>members</code> AND <code>member_status_history</code> for RLS — JOIN-free filtering means RLS policies on the history table don't need to walk back to the parent table.</li><li><strong><code>reason TEXT</code> on status history is 1–1000 chars</strong> — long enough for narrative (\"probation passed: see review doc DOC-PR-2026-08-12\") but bounded to prevent abuse.</li><li><strong><code>updated_at</code> is touched by trigger</strong> on every row UPDATE — single source of truth for staleness.</li><li><strong><code>hr.member_cccd_accessed</code> at sev-1</strong> means OBS will page on the very first access; this is intentional alarm volume. Acceptable noise: ~10/month per tenant.</li><li><strong>The closed contract_type enum (5 values) is owned by FR-HR-002</strong> — this FR declares the enum so the column type exists, but FR-HR-002 ships the lifecycle rules (probation auto-expires at 60 days, etc.).</li><li><strong><code>subject_id</code> (UUID) NOT <code>member_id</code> (UUID)</strong> — same physical column, semantic clarification. Member is a FACT about a SUBJECT; the SUBJECT is the credential identity. PK reuse is intentional.</li><li><strong><code>reason</code> field PII concerns</strong> — operators may write \"PII: Person A had a panic attack in Q3\" into the reason. The audit row scrubs <code>reason</code> via FR-MEMORY-111 PII rules before chain commit; status_history table is RLS-protected and the audit chain is the long-term record.</li><li><strong><code>anniversary_date</code> is computed</strong>, not stored — it's <code>start_date + INTERVAL '&lt;n&gt;' year</code>. Avoids an extra column that could drift.</li><li><strong><code>level::executive</code> is the ceiling</strong> — <code>founder</code> is the AUTH RBAC role, distinct from the HR level. Founders are typically <code>level=executive</code> Members PLUS hold the <code>founder</code> role per FR-AUTH-101.</li><li><strong>The 7-level enum follows VN-1 progression conventions</strong> — trainee/associate/senior/lead/principal/director/executive. ABAC-style level fields (e.g. \"L4.5\") are explicitly forbidden; promotion is integer ladder steps.</li><li><strong><code>start_date = CURRENT_DATE on probation→active transition</code> if start_date was NULL</strong> — handler convention; operators MAY override by setting start_date explicitly in the request, but the default-fill is convenient for the common case.</li></ul>\n<hr>\n<p><em>End of FR-HR-001.</em></p>";
