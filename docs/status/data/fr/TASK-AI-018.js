window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-018"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway crate <strong>MUST</strong> include a property-based test asserting <strong>zero cross-tenant cache reads</strong> under any randomised input, plus enumerated regression scenarios, adversarial inputs, and concurrent-load testing. The test suite obeys the following:</p>\n<ol><li><strong>MUST</strong> use the <code>proptest</code> crate as the property-test framework. The crate's shrinking discipline produces minimal failing cases for human review on regression; alternative frameworks (quickcheck) lack equivalent shrinking.</li><li><strong>MUST</strong> generate at least <strong>200 random tenant pairs × 1000 cache operations per pair = 200,000 randomised operations per test run</strong>. Mechanically: <code>ProptestConfig::with_cases(256)</code> (proptest default) × <code>prop::collection::vec(any_cache_op(), 800..1200)</code> (each case fans out to ~1000 ops) → ~256K ops per CI run. The 200K floor catches cache-key collision bugs at ~1-in-10K frequency with ≥99.9% probability.</li><li><strong>MUST</strong> assert: for any sequence of <code>cache::insert(tenant_a, ...)</code> followed by <code>cache::lookup(tenant_b, ...)</code> where <code>tenant_a ≠ tenant_b</code>, the lookup MUST return <code>CacheLookupOutcome::Miss</code> even if all other key components (redacted prompt, model, persona_handle) are byte-identical. The assertion is at the API surface (matching the production cache call shape), not at the internal hash function.</li><li><strong>MUST</strong> include a second property test at the <strong>key-derivation</strong> level: for any <code>(A, B)</code> with <code>A ≠ B</code>, <code>CacheKey::derive(A, prompt, model, persona) ≠ CacheKey::derive(B, prompt, model, persona)</code> (byte-distinct hashes). This catches collisions BEFORE they manifest as Redis-level leaks; the dual-layer testing (derivation + end-to-end) is defence-in-depth.</li><li><strong>MUST</strong> include a third property test at the <strong>Redis-key namespace</strong> level: scanning <code>KEYS ai_cache:v1:tenant_a:*</code> after inserting under both <code>tenant_a</code> and <code>tenant_b</code> MUST return only tenant_a's entries. This validates the structural prefix invariant (TASK-AI-017 §1 #2).</li><li><strong>MUST</strong> include exactly <strong>7 enumerated regression scenarios</strong> in <code>cache_isolation_regression_scenarios.rs</code>, each documenting a historical near-miss bug. The seven (each gets one <code>#[test]</code> function with a comment block citing the original incident date + root cause):</li></ol>\n<ul><li><code>regression_001_underscore_collision</code> — <code>\"tenant_a\"</code> and <code>\"tenanta\"</code> previously collided due to insufficient input separation.</li><li><code>regression_002_unicode_normalization</code> — <code>\"tenant_é\"</code> (NFC) and <code>\"tenant_é\"</code> (NFD) hashed differently → race condition exposed; reverse case.</li><li><code>regression_003_trailing_whitespace</code> — <code>\"tenant_a\"</code> and <code>\"tenant_a \"</code> should be DIFFERENT tenants (whitespace is a valid id character).</li><li><code>regression_004_case_folding</code> — <code>\"Tenant_A\"</code> and <code>\"tenant_a\"</code> previously folded; tenants are case-sensitive.</li><li><code>regression_005_empty_prompt</code> — <code>tenant_a</code> + empty prompt should NOT collide with <code>tenant_a</code> + single-space prompt.</li><li><code>regression_006_persona_omitted</code> — Pre-TASK-AI-017-ISS-001 keys omitted persona; this scenario asserts persona-handle inclusion.</li><li><code>regression_007_model_substring</code> — <code>model = \"chat.smart\"</code> and <code>model = \"chat.smartx\"</code> previously hashed similarly under poor concat.</li></ul>\n<ol><li><strong>MUST</strong> include an adversarial-input test (<code>cache_isolation_adversarial_test.rs</code>) covering: null bytes (<code>\"\\x00\"</code> in tenant id), unit separators (<code>\"\\x1f\"</code> — the field separator we use internally; an attacker injecting it shouldn't break key uniqueness), control characters, RTL Unicode (<code>\"‮\"</code> reversal), zero-width joiners, very long strings (10KB tenant id), empty strings, and SQL-injection-shaped payloads (<code>\"tenant'; DROP TABLE--\"</code>).</li><li><strong>MUST</strong> include a concurrent-load test (<code>cache_isolation_concurrent_test.rs</code>) — 100 tokio tasks racing <code>insert</code>/<code>lookup</code> across 50 tenant pairs simultaneously. The test asserts: across 10,000 concurrent operations, ZERO cross-tenant reads. Concurrency catches race conditions that sequential tests miss (e.g., a non-atomic insert path that briefly exposes the wrong tenant's data).</li><li><strong>MUST</strong> complete the entire suite (property + regression + adversarial + concurrent) in ≤ 90 seconds on a standard <code>ubuntu-22.04</code> GitHub Actions runner. Budget breakdown: 60s for the main property test, 10s for regression, 10s for adversarial, 10s for concurrent.</li><li><strong>MUST</strong> emit per-failure detail with the FULL cross-tenant context: <code>tenant_a</code>, <code>tenant_b</code>, <code>prompt</code>, <code>model</code>, <code>persona_handle</code>, <code>step_in_sequence</code>, AND the <code>CacheKey::derive</code> hash for each side. The hash inclusion is critical for triaging \"is this a key-derivation bug or a Redis-level leak?\". On proptest shrinking, the minimal failing case is also reported.</li><li><strong>MUST</strong> isolate Redis state per test case via a per-case prefix <code>test_&lt;uuid&gt;_</code> injected into the tenant_id input. The <code>redis_isolation_helper</code> cleans up keys matching the prefix in <code>Drop</code>. This prevents inter-case state pollution that would produce flaky failures (one case inserting; the next case lookup-ing the same key).</li><li><strong>MUST</strong> be a CI gate on every PR via <code>.github/workflows/cache-isolation-gate.yml</code>. The workflow MUST trigger on changes to <code>services/ai-gateway/src/cache/**</code> AND <code>services/ai-gateway/tests/cache_isolation*.rs</code> AND the workflow file itself (self-gate per TASK-AI-013 §1 #8 pattern).</li><li><strong>MUST</strong> enforce non-skip in CI: the workflow parses pytest output for <code>skipped</code> count and fails if any skip markers appear. This prevents the operational failure mode of a developer adding <code>#[ignore]</code> to silence a flaky test.</li><li><strong>SHOULD</strong> emit a JSON artefact <code>cache_isolation_report_&lt;sha&gt;.json</code> with the case count, regression-scenario pass-list, adversarial-pass-list, concurrent-test wall-clock, and any failure detail. This is the audit primitive: when MoPS A05 asks \"show me the proof of cross-tenant isolation as of date X,\" the artefact for that date's CI run is the answer.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a property test instead of enumerated test cases?</strong> Property tests scale much better than enumerated tests for invariants like \"zero cross-tenant leaks.\" A traditional test suite might cover 10 hand-picked scenarios; a property test covers thousands of randomised scenarios that the engineer didn't think of. The cost is one-time test-strategy setup; the benefit is sustained for every PR. proptest's shrinking discipline produces minimal failing cases that humans can read — turning a 1000-op failure into a 2-op explanation.</p>\n<p><strong>Why 200K operations as the floor (§1 #2)?</strong> Cache-key collision bugs (the most likely class for cross-tenant leaks) tend to manifest at frequencies in the 1-in-10K to 1-in-1M range — too rare for a 100-case test, too common for a billion-case test. 200K ops sits at the sweet spot: ~99.9% probability of catching 1-in-10K bugs, runtime budget under 60 seconds. The number is calibrated, not arbitrary.</p>\n<p><strong>Why dual-layer testing (key-derivation + end-to-end, §1 #4 + #5)?</strong> A bug at the key-derivation level (<code>CacheKey::derive</code> produces same hash for different tenants) is also catchable at the end-to-end level (insert+lookup observes the leak). But the inverse isn't true: a bug in the Redis backend (e.g., a SCAN-pattern bug that returns wrong-tenant keys) wouldn't manifest at the derivation level. Testing both layers means a regression caught at one layer is also visible at the other — making triage much faster (the failure pattern tells you which layer regressed).</p>\n<p><strong>Why a Redis-namespace test (§1 #5)?</strong> TASK-AI-017 §1 #2 makes per-tenant prefix a STRUCTURAL invariant. The Redis-namespace test is the assertion of that structure — without it, \"structural\" is just a word. The KEYS pattern test would catch a backend bug like \"SCAN ignores tenant prefix\" that key-derivation tests would miss.</p>\n<p><strong>Why 7 enumerated regression scenarios (§1 #6)?</strong> Property tests catch novel bugs; regression tests document known bugs. The seven scenarios were each a real near-miss in development — naming them and pinning them to specific test functions means a future engineer who breaks one of these patterns sees an immediately diagnostic test name (<code>regression_001_underscore_collision</code>) rather than a generic \"property test failed at minimal case (..., ..., ...)\". The comment block in each test cites the incident date and root cause; the test IS the post-mortem documentation.</p>\n<p><strong>Why adversarial input testing (§1 #7)?</strong> Multi-tenancy boundaries are attack surfaces. A malicious tenant might choose a tenant_id like <code>\"tenant_a\\x1ftenant_b\"</code> (containing the unit separator we use internally) hoping it'd parse as a different key. The adversarial inputs are NOT testing for production behaviour (those tenant_ids would be rejected at policy load by TASK-AI-005's validators) — they're testing that the cache key derivation is robust EVEN IF the tenant_id validator fails. Defence in depth.</p>\n<p><strong>Why concurrent testing (§1 #8)?</strong> Sequential tests can't catch race conditions. A non-atomic insert path (e.g., write-then-read in a non-transactional manner) might briefly expose the wrong tenant's data. Concurrent testing — 100 tasks racing across 50 tenant pairs — exercises the path under load. The assertion is the same (zero cross-tenant reads) but the discovery surface is different.</p>\n<p><strong>Why per-case Redis isolation (§1 #11)?</strong> Without isolation, test case N's leftover entries pollute test case N+1's lookup space. A flaky failure pattern would be: case A inserts under <code>(tenant_x, prompt_y)</code>; case B (with different randomly-generated tenant) accidentally generates <code>tenant_x</code> again and looks up <code>prompt_y</code> — gets a hit from case A's residue. The flakiness is hard to reproduce. The per-case prefix (<code>test_&lt;uuid&gt;_&lt;original_tenant&gt;</code>) makes every case operate in its own namespace; cleanup in Drop ensures no residue.</p>\n<p><strong>Why non-skip enforcement (§1 #13)?</strong> The most common failure mode for compliance-gate tests is silent disablement: a developer hits a flaky failure, adds <code>#[ignore]</code>, the test stops protecting the invariant, nobody notices for months. Parsing CI output for skip count and failing on any skip catches this pattern. The trade-off is operational rigidity — but for a hard-zero invariant, rigidity is the point.</p>\n<p><strong>Why a JSON artefact (§1 #14)?</strong> MoPS A05 (Vietnam's Department of Cyber Security) asks during audits: \"how do you prove no cross-tenant data exposure?\" Without an artefact, the answer is \"we have a test in our codebase\" — non-evidential. With the artefact, the answer is \"this JSON file from CI run X on date Y proves the property held at that point.\" The artefact is a one-shot regulatory primitive.</p>\n<p><strong>Why this is its own task (TASK-AI-018), not a test in TASK-AI-017?</strong> TASK-AI-017 has its own property test (cross-tenant isolation at 1000-trial level) as a baseline. TASK-AI-018 is the dedicated, exhaustive treatment with 200K ops + 7 regressions + adversarial + concurrent + CI-artefact emission. Two reasons for the split: (a) the test surface is large enough to warrant its own owner; (b) future hardening (10M-op runs, mutation testing of the cache code, fuzzing) extends TASK-AI-018 without blowing up TASK-AI-017's scope.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<p>This task ships only test code; no production API. The test imports <code>cache::lookup</code>, <code>cache::insert</code>, <code>CacheKey::derive</code> from TASK-AI-017.</p>\n<h3>Test helpers</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/support/redis_isolation_helper.rs\n\nuse uuid::Uuid;\n\npub struct RedisTestNamespace {\n    pub prefix: String,      // e.g. \"test_a1b2c3d4_\"\n}\n\nimpl RedisTestNamespace {\n    pub fn new() -&gt; Self {\n        let prefix = format!(\"test_{}_\", Uuid::new_v4().simple());\n        Self { prefix }\n    }\n\n    /// Wraps a tenant_id with the per-case prefix so test cases don't pollute each other.\n    pub fn tenant(&amp;self, original: &amp;str) -&gt; String {\n        format!(\"{}{}\", self.prefix, original)\n    }\n}\n\nimpl Drop for RedisTestNamespace {\n    fn drop(&amp;mut self) {\n        // Clean up all keys matching `ai_cache:v1:{prefix}*`.\n        let pattern = format!(\"ai_cache:v1:{}*\", self.prefix);\n        let _ = redis_helper::delete_keys_matching(&amp;pattern);\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/support/proptest_strategies.rs\n\nuse proptest::prelude::*;\n\npub fn any_tenant_id() -&gt; impl Strategy&lt;Value = String&gt; {\n    \"[a-zA-Z0-9_:.\\\\-]{1,32}\".prop_map(String::from)\n}\n\npub fn any_tenant_pair() -&gt; impl Strategy&lt;Value = (String, String)&gt; {\n    (any_tenant_id(), any_tenant_id())\n        .prop_filter(\"tenants must differ\", |(a, b)| a != b)\n}\n\npub fn any_prompt() -&gt; impl Strategy&lt;Value = String&gt; {\n    \"[a-zA-Z0-9 ?!.,]{0,200}\".prop_map(String::from)\n}\n\npub fn any_model() -&gt; impl Strategy&lt;Value = String&gt; {\n    prop_oneof![\n        Just(\"chat.fast\".to_string()),\n        Just(\"chat.smart\".to_string()),\n        Just(\"embed.standard\".to_string()),\n        \"[a-z]{4,12}\\\\.[a-z]{4,8}\".prop_map(String::from),\n    ]\n}\n\npub fn any_persona_handle() -&gt; impl Strategy&lt;Value = String&gt; {\n    \"[a-z\\\\-]{4,16}@\\\\d+\\\\.\\\\d+\\\\.\\\\d+\".prop_map(String::from)\n}\n\npub fn any_cache_op() -&gt; impl Strategy&lt;Value = (String, String, String)&gt; {\n    (any_prompt(), any_model(), any_persona_handle())\n}\n\npub fn adversarial_tenant_strings() -&gt; Vec&lt;&amp;'static str&gt; {\n    vec![\n        \"\",                                             // empty\n        \"\\x00\",                                         // null byte\n        \"\\x1ftenant\",                                   // unit separator (our internal field sep)\n        \"tenant\\x1fid\",                                 // unit separator mid-string\n        \"tenant\\u{202E}id\",                             // RTL override\n        \"tenant\\u{200D}id\",                             // zero-width joiner\n        \"tenant'; DROP TABLE--\",                        // SQL-injection shape\n        \"../../../etc/passwd\",                          // path traversal\n        \"\\u{FEFF}tenant\",                               // BOM-prefixed\n        \"TENANT\",                                       // case variant (cross-test with \"tenant\")\n    ]\n}</code></pre>\n<h3>CI workflow</h3>\n<pre><code class=\"language-yaml\"># .github/workflows/cache-isolation-gate.yml\nname: Cache Cross-Tenant Isolation Gate\non:\n  pull_request:\n    paths:\n      - 'services/ai-gateway/src/cache/**'\n      - 'services/ai-gateway/tests/cache_isolation*'\n      - 'services/ai-gateway/tests/support/**'\n      - '.github/workflows/cache-isolation-gate.yml'\n\njobs:\n  isolation-gate:\n    runs-on: ubuntu-22.04\n    timeout-minutes: 5\n    services:\n      redis:\n        image: redis:7\n        ports: ['6379:6379']\n        options: --health-cmd \"redis-cli ping\" --health-interval 5s\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions-rust-lang/setup-rust-toolchain@v1\n      - name: Run cache isolation suite\n        working-directory: services/ai-gateway\n        env:\n          REDIS_URL: redis://localhost:6379\n          PROPTEST_CASES: \"256\"   # explicit; non-default raises a CI WARN\n        run: cargo test --release cache_isolation -- --test-threads=1 --format=json | tee report.json\n      - name: Enforce no skipped tests (§1 #13)\n        working-directory: services/ai-gateway\n        run: |\n          SKIP_COUNT=$(grep -c '\"event\":\"ignored\"' report.json || echo 0)\n          if [ \"$SKIP_COUNT\" -gt 0 ]; then\n            echo \"❌ $SKIP_COUNT cache_isolation test(s) skipped — refusing to merge\"\n            grep '\"event\":\"ignored\"' report.json\n            exit 1\n          fi\n      - name: Upload JSON artefact (§1 #14)\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: cache_isolation_report_${{ github.sha }}\n          path: services/ai-gateway/report.json</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>Test compiles and runs in CI</strong> — <code>cargo test cache_isolation</code> exits 0 in the GitHub Actions runner.</li><li><strong>Property test runs ≥256 cases × ~1000 ops</strong> — Verified by counting <code>proptest case</code> log output; total ops ≥ 200K per run.</li><li><strong>Zero cross-tenant reads (end-to-end)</strong> — <code>no_cross_tenant_cache_reads</code> proptest passes; assertion never fails across 200K+ ops.</li><li><strong>Zero cross-tenant key collisions (derivation level)</strong> — <code>no_cross_tenant_key_collisions</code> proptest: <code>CacheKey::derive(A,...) ≠ CacheKey::derive(B,...)</code> for any <code>A ≠ B</code>.</li><li><strong>Redis namespace is tenant-isolated</strong> — <code>redis_keys_scan_is_tenant_isolated</code> proptest: scanning <code>ai_cache:v1:tenant_a:*</code> after inserting under both tenants returns only tenant_a entries.</li><li><strong>All 7 regression scenarios pass</strong> — Each named test in <code>cache_isolation_regression_scenarios.rs</code> passes individually.</li><li><strong>Adversarial inputs handled</strong> — <code>cache_isolation_adversarial_test</code> covers all 10 enumerated adversarial tenant strings; each case asserts no cross-leak when paired with a benign tenant_id.</li><li><strong>Concurrent test passes</strong> — 100 tasks × 50 tenant pairs × 10,000 total ops; zero cross-tenant reads.</li><li><strong>Suite completes in ≤ 90 seconds</strong> — Wall-clock from CI start to CI end (excluding setup); enforced by GitHub Actions <code>timeout-minutes: 5</code>.</li><li><strong>Failure detail is actionable</strong> — On any failure, the message includes <code>tenant_a</code>, <code>tenant_b</code>, <code>prompt</code>, <code>model</code>, <code>persona_handle</code>, step number, AND both <code>CacheKey::derive</code> hashes.</li><li><strong>Per-case Redis isolation prevents pollution</strong> — Sequential test runs with the same RNG seed produce the same outcome (no flakiness from inter-case state).</li><li><strong>CI gate blocks merge on failure</strong> — A PR introducing a leaking change is rejected by the workflow with non-zero exit.</li><li><strong>No-skip enforcement fires</strong> — A PR that adds <code>#[ignore]</code> to any cache_isolation test is rejected by the workflow's skip-count check.</li><li><strong>Workflow self-gate</strong> — A PR that ONLY edits <code>.github/workflows/cache-isolation-gate.yml</code> triggers the gate against the modified workflow.</li><li><strong>JSON artefact emitted on every run</strong> — <code>cache_isolation_report_&lt;sha&gt;.json</code> uploaded as CI artefact on both pass and fail.</li><li><strong>Proptest shrinking produces minimal failing case on regression</strong> — A planted leak (test fixture that intentionally collides) reports a 2-3 op minimal case after shrinking, not the original 1000-op sequence.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>Property test #1: end-to-end isolation</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/cache_isolation_property_test.rs\nuse proptest::prelude::*;\nuse cyberos_ai_gateway::cache::{self, CacheKey, CacheLookupOutcome};\n\nmod support;\nuse support::{proptest_strategies::*, redis_isolation_helper::RedisTestNamespace};\n\nproptest! {\n    #![proptest_config(ProptestConfig::with_cases(256))]\n\n    #[test]\n    fn no_cross_tenant_cache_reads(\n        (tenant_a, tenant_b) in any_tenant_pair(),\n        ops in prop::collection::vec(any_cache_op(), 800..1200),\n    ) {\n        let ns = RedisTestNamespace::new();\n        let t_a = ns.tenant(&amp;tenant_a);\n        let t_b = ns.tenant(&amp;tenant_b);\n\n        let rt = tokio::runtime::Runtime::new().unwrap();\n        rt.block_on(async {\n            for (prompt, model, persona) in &amp;ops {\n                let k = CacheKey::derive(&amp;t_a, prompt, model, persona);\n                let _ = cache::insert(&amp;k, &amp;test_provider_response(), model).await;\n            }\n            for (prompt, model, persona) in &amp;ops {\n                let k = CacheKey::derive(&amp;t_b, prompt, model, persona);\n                match cache::lookup(&amp;k).await {\n                    CacheLookupOutcome::Miss =&gt; {}\n                    other =&gt; prop_assert!(false,\n                        \"cross-tenant leak: t_a={} t_b={} prompt={:?} model={} persona={} \\\n                         k_a_hash={} k_b_hash={} outcome={:?}\",\n                        t_a, t_b, prompt, model, persona,\n                        hex::encode(CacheKey::derive(&amp;t_a, prompt, model, persona).prompt_hash),\n                        hex::encode(k.prompt_hash), other,\n                    ),\n                }\n            }\n            Ok(())\n        }).unwrap();\n    }\n}</code></pre>\n<h3>Property test #2: key-derivation level</h3>\n<pre><code class=\"language-rust\">proptest! {\n    #![proptest_config(ProptestConfig::with_cases(1000))]\n\n    #[test]\n    fn no_cross_tenant_key_collisions(\n        (a, b) in any_tenant_pair(),\n        prompt in any_prompt(),\n        model in any_model(),\n        persona in any_persona_handle(),\n    ) {\n        let k_a = CacheKey::derive(&amp;a, &amp;prompt, &amp;model, &amp;persona);\n        let k_b = CacheKey::derive(&amp;b, &amp;prompt, &amp;model, &amp;persona);\n        prop_assert_ne!(k_a.prompt_hash, k_b.prompt_hash,\n            \"cache-key collision: tenant_a={a} tenant_b={b} prompt={prompt:?}\");\n    }\n}</code></pre>\n<h3>Property test #3: Redis namespace</h3>\n<pre><code class=\"language-rust\">proptest! {\n    #![proptest_config(ProptestConfig::with_cases(50))]\n\n    #[test]\n    fn redis_keys_scan_is_tenant_isolated(\n        (a, b) in any_tenant_pair(),\n        n_ops in 10..100u32,\n    ) {\n        let ns = RedisTestNamespace::new();\n        let t_a = ns.tenant(&amp;a);\n        let t_b = ns.tenant(&amp;b);\n        let rt = tokio::runtime::Runtime::new().unwrap();\n        rt.block_on(async {\n            for i in 0..n_ops {\n                let k_a = CacheKey::derive(&amp;t_a, &amp;format!(\"p{i}\"), \"chat.smart\", \"p@1.0.0\");\n                let k_b = CacheKey::derive(&amp;t_b, &amp;format!(\"p{i}\"), \"chat.smart\", \"p@1.0.0\");\n                let _ = cache::insert(&amp;k_a, &amp;test_provider_response(), \"chat.fast\").await;\n                let _ = cache::insert(&amp;k_b, &amp;test_provider_response(), \"chat.fast\").await;\n            }\n            let scan = redis_helper::keys(&amp;format!(\"ai_cache:v1:{}:*\", t_a));\n            for key in &amp;scan {\n                prop_assert!(key.starts_with(&amp;format!(\"ai_cache:v1:{}:\", t_a)),\n                    \"namespace leak: scan returned {key} when filtering for {t_a}\");\n            }\n            prop_assert_eq!(scan.len(), n_ops as usize);\n            Ok(())\n        }).unwrap();\n    }\n}</code></pre>\n<h3>7 enumerated regression scenarios</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/cache_isolation_regression_scenarios.rs\nuse cyberos_ai_gateway::cache::{self, CacheKey, CacheLookupOutcome};\n\nmod support;\nuse support::redis_isolation_helper::RedisTestNamespace;\n\n/// REGRESSION-001 (incident 2026-04-12, pre-fix):\n/// `tenant_a` and `tenanta` previously collided due to insufficient input separation\n/// when the key derivation used naive concat without unit-separator.\n/// Root cause: `concat(tenant, model)` → `concat(tenant_a, model_x)` and\n///             `concat(tenanta, _model_x)` produced same hash.\n#[tokio::test]\nasync fn regression_001_underscore_collision() {\n    let ns = RedisTestNamespace::new();\n    let k1 = CacheKey::derive(&amp;ns.tenant(\"tenant_a\"), \"prompt\", \"chat.smart\", \"p@1.0.0\");\n    let k2 = CacheKey::derive(&amp;ns.tenant(\"tenanta\"), \"prompt\", \"chat.smart\", \"p@1.0.0\");\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n\n    let _ = cache::insert(&amp;k1, &amp;test_provider_response(), \"chat.fast\").await;\n    assert!(matches!(cache::lookup(&amp;k2).await, CacheLookupOutcome::Miss));\n}\n\n/// REGRESSION-002 (incident 2026-04-15):\n/// Unicode normalisation form differences (\"é\" precomposed vs. e + combining acute)\n/// produced different hashes, but tenant policy parser later normalised both to NFC,\n/// causing a brief race-window leak.\n#[tokio::test]\nasync fn regression_002_unicode_normalization() {\n    let ns = RedisTestNamespace::new();\n    let nfc = ns.tenant(\"tenant_é\");                  // precomposed\n    let nfd = ns.tenant(\"tenant_é\");                  // e + combining acute (depending on source encoding)\n    let k1 = CacheKey::derive(&amp;nfc, \"p\", \"m\", \"p@1.0.0\");\n    let k2 = CacheKey::derive(&amp;nfd, \"p\", \"m\", \"p@1.0.0\");\n    // The cache treats them as distinct (current behaviour); TASK-AI-005 enforces NFC at policy load.\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n\n    let _ = cache::insert(&amp;k1, &amp;test_provider_response(), \"chat.fast\").await;\n    assert!(matches!(cache::lookup(&amp;k2).await, CacheLookupOutcome::Miss));\n}\n\n/// REGRESSION-003: trailing whitespace must NOT collapse two distinct tenant ids.\n#[tokio::test]\nasync fn regression_003_trailing_whitespace() {\n    let ns = RedisTestNamespace::new();\n    let a = ns.tenant(\"tenant_a\");\n    let b = ns.tenant(\"tenant_a \");                   // trailing space\n    let k1 = CacheKey::derive(&amp;a, \"p\", \"m\", \"p@1.0.0\");\n    let k2 = CacheKey::derive(&amp;b, \"p\", \"m\", \"p@1.0.0\");\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n}\n\n/// REGRESSION-004: tenant ids are case-sensitive; \"Tenant_A\" ≠ \"tenant_a\".\n#[tokio::test]\nasync fn regression_004_case_folding() {\n    let ns = RedisTestNamespace::new();\n    let upper = ns.tenant(\"Tenant_A\");\n    let lower = ns.tenant(\"tenant_a\");\n    let k1 = CacheKey::derive(&amp;upper, \"p\", \"m\", \"p@1.0.0\");\n    let k2 = CacheKey::derive(&amp;lower, \"p\", \"m\", \"p@1.0.0\");\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n}\n\n/// REGRESSION-005: empty prompt vs. single-space prompt must NOT collide.\n#[tokio::test]\nasync fn regression_005_empty_prompt() {\n    let ns = RedisTestNamespace::new();\n    let t = ns.tenant(\"tenant_a\");\n    let k_empty = CacheKey::derive(&amp;t, \"\", \"chat.smart\", \"p@1.0.0\");\n    let k_space = CacheKey::derive(&amp;t, \" \", \"chat.smart\", \"p@1.0.0\");\n    assert_ne!(k_empty.prompt_hash, k_space.prompt_hash);\n}\n\n/// REGRESSION-006: persona-handle MUST be in the key (TASK-AI-017 ISS-001 fix verification).\n/// Pre-fix, persona changes didn't invalidate cache; this scenario asserts they do.\n#[tokio::test]\nasync fn regression_006_persona_omitted() {\n    let ns = RedisTestNamespace::new();\n    let t = ns.tenant(\"tenant_a\");\n    let k1 = CacheKey::derive(&amp;t, \"p\", \"chat.smart\", \"cuo-cpo@0.4.1\");\n    let k2 = CacheKey::derive(&amp;t, \"p\", \"chat.smart\", \"cuo-cpo@0.4.2\");\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n}\n\n/// REGRESSION-007: model substring \"chat.smart\" vs. \"chat.smartx\" must NOT collide.\n#[tokio::test]\nasync fn regression_007_model_substring() {\n    let ns = RedisTestNamespace::new();\n    let t = ns.tenant(\"tenant_a\");\n    let k1 = CacheKey::derive(&amp;t, \"p\", \"chat.smart\", \"p@1.0.0\");\n    let k2 = CacheKey::derive(&amp;t, \"p\", \"chat.smartx\", \"p@1.0.0\");\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n}</code></pre>\n<h3>Adversarial inputs test</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/cache_isolation_adversarial_test.rs\nuse support::proptest_strategies::adversarial_tenant_strings;\n\n#[tokio::test]\nasync fn adversarial_tenant_strings_dont_leak() {\n    let ns = RedisTestNamespace::new();\n    let benign = ns.tenant(\"benign_tenant\");\n    let benign_key = CacheKey::derive(&amp;benign, \"p\", \"chat.smart\", \"p@1.0.0\");\n    let _ = cache::insert(&amp;benign_key, &amp;test_provider_response(), \"chat.fast\").await;\n\n    for adv in adversarial_tenant_strings() {\n        let adv_namespaced = ns.tenant(adv);\n        let adv_key = CacheKey::derive(&amp;adv_namespaced, \"p\", \"chat.smart\", \"p@1.0.0\");\n        match cache::lookup(&amp;adv_key).await {\n            CacheLookupOutcome::Miss =&gt; {}\n            other =&gt; panic!(\"adversarial leak: adv={adv:?} outcome={other:?}\"),\n        }\n    }\n}\n\n#[tokio::test]\nasync fn unit_separator_in_tenant_id_is_distinct() {\n    let ns = RedisTestNamespace::new();\n    let t1 = ns.tenant(\"tenant\\x1fa\");                // unit separator inside\n    let t2 = ns.tenant(\"tenant_a\");\n    let k1 = CacheKey::derive(&amp;t1, \"p\", \"m\", \"p@1.0.0\");\n    let k2 = CacheKey::derive(&amp;t2, \"p\", \"m\", \"p@1.0.0\");\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n}\n\n#[tokio::test]\nasync fn very_long_tenant_id_distinct_from_short() {\n    let ns = RedisTestNamespace::new();\n    let short = ns.tenant(\"t\");\n    let long  = ns.tenant(&amp;\"a\".repeat(10_000));\n    let k1 = CacheKey::derive(&amp;short, \"p\", \"m\", \"p@1.0.0\");\n    let k2 = CacheKey::derive(&amp;long, \"p\", \"m\", \"p@1.0.0\");\n    assert_ne!(k1.prompt_hash, k2.prompt_hash);\n}</code></pre>\n<h3>Concurrent test</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/cache_isolation_concurrent_test.rs\n#[tokio::test(flavor = \"multi_thread\", worker_threads = 8)]\nasync fn one_hundred_tasks_racing_no_cross_tenant_reads() {\n    let ns = std::sync::Arc::new(RedisTestNamespace::new());\n    let tenants: Vec&lt;String&gt; = (0..50).map(|i| ns.tenant(&amp;format!(\"t{i}\"))).collect();\n    let mut joinset = tokio::task::JoinSet::new();\n\n    for task_id in 0..100 {\n        let tenants = tenants.clone();\n        joinset.spawn(async move {\n            let owner = &amp;tenants[task_id % 50];\n            let other = &amp;tenants[(task_id + 1) % 50];\n            for op in 0..100 {\n                let k_owner = CacheKey::derive(owner, &amp;format!(\"p{op}\"), \"chat.smart\", \"p@1.0.0\");\n                let _ = cache::insert(&amp;k_owner, &amp;test_provider_response(), \"chat.fast\").await;\n                // Same prompt+model+persona, different tenant — must miss.\n                let k_other = CacheKey::derive(other, &amp;format!(\"p{op}\"), \"chat.smart\", \"p@1.0.0\");\n                let outcome = cache::lookup(&amp;k_other).await;\n                assert!(matches!(outcome, CacheLookupOutcome::Miss),\n                    \"concurrent leak: task={task_id} owner={owner} other={other} outcome={outcome:?}\");\n            }\n        });\n    }\n    while let Some(r) = joinset.join_next().await { r.unwrap(); }\n}</code></pre>\n<pre><code class=\"language-bash\">docker run -d --name test-redis -p 6379:6379 redis:7\ncd services/ai-gateway\ncargo test --release cache_isolation -- --test-threads=1</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3 (test helpers + workflow) and §5 (full test bodies). Boot order in CI workflow: Redis container starts → cargo test runs the four test files → JSON artefact uploaded.</p>\n<p>The test files reference <code>support::</code> modules; the support directory is shared test infrastructure (per <code>/tests/support/</code> Rust test convention).</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<h3>Code dependencies (other tasks/modules)</h3>\n<ul><li><strong>TASK-AI-017</strong> — <code>cache::lookup</code>, <code>cache::insert</code>, <code>CacheKey::derive</code>, <code>CacheLookupOutcome</code> are all consumed; this task is the dedicated leak-test for TASK-AI-017's per-tenant isolation invariant.</li><li><strong>TASK-AI-005</strong> — Tenant-id validation rules (the adversarial inputs assume some inputs would be rejected by TASK-AI-005's policy parser; this task tests the cache layer assuming validation has failed).</li><li><strong>TASK-AI-014</strong> — Persona handle format (<code>&lt;id&gt;@&lt;version&gt;</code>) is one of the cache key inputs; the regression test #6 asserts persona-handle inclusion.</li></ul>\n<h3>Concept dependencies (shared types)</h3>\n<ul><li><code>CacheKey::derive(tenant, prompt, model, persona)</code> is the cryptographic primitive under test.</li><li>The per-tenant Redis-key prefix (<code>ai_cache:v1:{tenant_id}:</code>) is the structural invariant under test.</li><li><code>CacheLookupOutcome</code> enum (Hit/Miss/SchemaMismatch/Error) is the assertion target — Miss is the only acceptable outcome for cross-tenant lookups.</li><li>Adversarial tenant strings list is a maintained corpus; new attack vectors discovered in production should be added to <code>proptest_strategies::adversarial_tenant_strings()</code>.</li></ul>\n<h3>Operational / external</h3>\n<ul><li>Rust crates: <code>proptest@1</code>, <code>tokio@1</code> with <code>rt-multi-thread</code> feature, <code>uuid@1</code>, <code>hex@0.4</code>.</li><li>Redis 7.x (test infrastructure; same as TASK-AI-017).</li><li>GitHub Actions runner with <code>services.redis</code> declared.</li><li><code>actions/upload-artifact@v4</code> for the JSON artefact.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>CI output on pass</h3>\n<pre><code class=\"language-text\">running 4 test files\ntest no_cross_tenant_cache_reads ... ok (256 cases × ~1000 ops = 256,000 ops)\ntest no_cross_tenant_key_collisions ... ok (1000 cases)\ntest redis_keys_scan_is_tenant_isolated ... ok (50 cases × ~50 ops)\ntest regression_001_underscore_collision ... ok\ntest regression_002_unicode_normalization ... ok\ntest regression_003_trailing_whitespace ... ok\ntest regression_004_case_folding ... ok\ntest regression_005_empty_prompt ... ok\ntest regression_006_persona_omitted ... ok\ntest regression_007_model_substring ... ok\ntest adversarial_tenant_strings_dont_leak ... ok\ntest unit_separator_in_tenant_id_is_distinct ... ok\ntest very_long_tenant_id_distinct_from_short ... ok\ntest one_hundred_tasks_racing_no_cross_tenant_reads ... ok\n\ncache_isolation suite passed in 73.2s (budget: 90s)</code></pre>\n<h3>CI output on failure (planted leak)</h3>\n<pre><code class=\"language-text\">test no_cross_tenant_cache_reads ... FAILED\n\nfailed: cross-tenant leak: t_a=\"tenant_x\" t_b=\"tenant_y\" prompt=\"hello\"\n        model=\"chat.smart\" persona=\"cuo-cpo@0.4.1\"\n        k_a_hash=a1b2c3...d4e5f6  k_b_hash=a1b2c3...d4e5f6  outcome=Hit(...)\n   at services/ai-gateway/tests/cache_isolation_property_test.rs:25\n   minimal failing case after shrinking:\n     tenant_a=\"x\"  tenant_b=\"y\"  ops=[(\"p\", \"chat.smart\", \"p@1.0.0\")]</code></pre>\n<h3>JSON artefact (success)</h3>\n<pre><code class=\"language-json\">{\n  \"sha\": \"0123abc...\",\n  \"suite\": \"cache_isolation\",\n  \"passed\": true,\n  \"case_counts\": {\n    \"no_cross_tenant_cache_reads\": 256,\n    \"no_cross_tenant_key_collisions\": 1000,\n    \"redis_keys_scan_is_tenant_isolated\": 50\n  },\n  \"regression_scenarios_passed\": [\n    \"regression_001_underscore_collision\",\n    \"regression_002_unicode_normalization\",\n    \"regression_003_trailing_whitespace\",\n    \"regression_004_case_folding\",\n    \"regression_005_empty_prompt\",\n    \"regression_006_persona_omitted\",\n    \"regression_007_model_substring\"\n  ],\n  \"adversarial_inputs_passed\": [\n    \"adversarial_tenant_strings_dont_leak\",\n    \"unit_separator_in_tenant_id_is_distinct\",\n    \"very_long_tenant_id_distinct_from_short\"\n  ],\n  \"concurrent_test_passed\": true,\n  \"wall_clock_seconds\": 73.2,\n  \"skipped_count\": 0\n}</code></pre>\n<h3>Skip-detection failure</h3>\n<pre><code class=\"language-text\">❌ 1 cache_isolation test(s) skipped — refusing to merge\n{\"event\":\"ignored\",\"name\":\"regression_002_unicode_normalization\",\"reason\":\"flaky\"}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. Items deferred to later tasks:</p>\n<ul><li>Mutation testing (deliberately mutate <code>CacheKey::derive</code> and assert the test catches it) — TASK-AI-022 area; current property test is structural defence.</li><li>Fuzz testing with libFuzzer over the cache key derivation — out of scope; proptest with adversarial inputs covers the current threat model.</li><li>Cross-region cache replication leak testing — TASK-AI-016 area; current tests are single-region.</li><li>10M-op extended runs in nightly CI — TASK-AI-022 follow-up; adds another 9-of-nines confidence.</li><li>Tenant-id whitelist validation as a separate compile-time check — out of scope; defended-in-depth at policy-load time (TASK-AI-005) and via the cache key derivation.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Property test finds leak (real bug in <code>CacheKey::derive</code>)</td><td>proptest panics with shrunk minimal case</td><td>CI blocks PR; engineer reads minimal case + hashes</td><td>Fix key derivation; add as new regression scenario</td></tr><tr><td>Property test finds leak (Redis backend bug)</td><td>Same panic with key_a_hash ≠ key_b_hash but Redis returns Hit</td><td>CI blocks PR</td><td>Investigate Redis connection-pool / SCAN behaviour</td></tr><tr><td>Test runs &gt; 90 sec</td><td>GitHub Actions <code>timeout-minutes: 5</code> fires</td><td>CI fails on timeout</td><td>Reduce per-case ops OR optimise hot path; investigate Redis latency</td></tr><tr><td>Flaky test (Redis state pollution between cases)</td><td>Intermittent fails with same RNG seed</td><td>sev-2 CI alarm</td><td>Investigate RedisTestNamespace cleanup; add explicit Drop assertion</td></tr><tr><td>Test skip slipped past CI</td><td><code>cargo test</code> <code>--format=json</code> parsing detects</td><td>CI fails on skip-count check (§1 #13)</td><td>Engineer removes <code>#[ignore]</code>; if test is genuinely flaky, investigate root cause not silence</td></tr><tr><td>Regression scenario removed/renamed</td><td>Test count drops; CI compares against expected list</td><td>CI fails on missing-test detection</td><td>Re-add; if intentional removal, requires task amendment</td></tr><tr><td>Adversarial input list shortened</td><td>Test count check</td><td>CI fails</td><td>Re-add; new adversarial inputs require addition not subtraction</td></tr><tr><td>Concurrent test hangs (deadlock in cache code)</td><td><code>tokio::test</code> 90s timeout</td><td>Test fails</td><td>Investigate cache::insert/lookup for blocking calls</td></tr><tr><td>Concurrent test detects race</td><td>Atomic-Bool failure flag; assertion in any task fires</td><td>Test fails with task_id, owner, other</td><td>Investigate cache layer for non-atomic write paths</td></tr><tr><td>Per-case Redis namespace leaked (Drop didn't run)</td><td>Subsequent test run sees stale <code>test_*</code> keys</td><td>sev-2 CI alarm</td><td>Investigate Drop chain; ensure tokio runtime allows Drop to run</td></tr><tr><td>New attack vector discovered in production</td><td>Post-mortem analysis</td><td>Add to adversarial inputs corpus + regression scenarios</td><td>Standard process; <code>proptest_strategies::adversarial_tenant_strings()</code> is appended</td></tr><tr><td>Workflow self-gate disabled</td><td><code>.github/workflows/cache-isolation-gate.yml</code> change without trigger</td><td>PR review catches; workflow file is in <code>paths:</code> so the gate fires against itself</td><td>Revert</td></tr><tr><td>JSON artefact upload fails</td><td><code>actions/upload-artifact@v4</code> step fails</td><td>CI marks step as failed but main test result still authoritative</td><td>Investigate Actions API; manually re-run job</td></tr><tr><td><code>RedisTestNamespace::new()</code> UUID collision (probability ~10⁻³⁰)</td><td>None practical</td><td>Self-resolves on next run</td><td>N/A</td></tr><tr><td>Property test triggers proptest deterministic-shrinking bug</td><td>Shrinker produces wrong minimal case</td><td>Diagnostic confusion</td><td>Update proptest crate; report upstream</td></tr><tr><td><code>--release</code> flag missing (test runs in debug mode)</td><td>Wall-clock &gt; 5 minutes</td><td>Suite times out</td><td>CI must use <code>cargo test --release</code>; documented in <code>.github/workflows/cache-isolation-gate.yml</code></td></tr><tr><td>Redis container not ready when test starts</td><td>First test errors with \"connection refused\"</td><td>Health-check <code>--health-cmd \"redis-cli ping\"</code> ensures Redis is ready</td><td>By design (workflow YAML)</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>This is the gold-standard invariant test for multi-tenancy in CyberOS. Every cache-touching change goes through this gate. The compliance value is \"we can prove the invariant holds across 200K+ randomised operations on every PR\" — much stronger than \"we have unit tests covering scenarios we thought of.\"</li><li>The 200K-operation budget catches 1-in-10K bugs at ≥99.9% probability per CI run. For 1-in-100K bugs, increase to <code>ProptestConfig::with_cases(2560)</code> (~2M ops) — currently out of scope but trivially extensible.</li><li>The 7 enumerated regression scenarios serve a different purpose from the property test. The property test catches novel bugs; the regressions document KNOWN bugs so they can never recur silently. Each scenario's comment block IS the post-mortem for that specific bug.</li><li>The adversarial inputs corpus (§3) is maintained — new attack vectors discovered in production are appended, never removed. The corpus is a collective hardening artefact across the lifetime of the codebase.</li><li>The concurrent test uses <code>flavor = \"multi_thread\", worker_threads = 8</code> to genuinely exercise concurrency. A single-threaded tokio runtime would interleave operations cooperatively; multi-threaded actually races them. Race-detection is the ONLY mode that catches non-atomic insert-path bugs.</li><li>The per-case Redis isolation (§1 #11) is the difference between \"this test is authoritative\" and \"this test is flaky.\" Without it, intermittent failures from inter-case pollution would degrade developer trust until the test is silently disabled.</li><li>The non-skip enforcement (§1 #13) is paranoid by design. The most common compliance-test-erosion pattern is silent disablement; making the CI workflow refuse to merge a PR with any cache_isolation skip stops that pattern at the gate.</li><li>The Vietnamese regulator (MoPS A05) specifically asks during audits: \"how do you prove no cross-tenant data exposure?\" The JSON artefact (§1 #14) is the answer — durable, cryptographically-tied to the SHA, durable across Github Actions log retention.</li><li>This task is downstream of TASK-AI-017 because it CONSUMES TASK-AI-017's API surface. But it's also UPSTREAM of any future task that touches the cache (e.g., TASK-AI-022's cache warming, TASK-AI-024's semantic cache) — those tasks will inherit this gate automatically because they touch <code>services/ai-gateway/src/cache/**</code>.</li><li>The workflow file (<code>cache-isolation-gate.yml</code>) is in its own <code>paths:</code> for self-gate (matching TASK-AI-013's pattern). A PR loosening the gate gets caught by the gate against itself.</li></ul>\n<hr>\n<p><em>End of TASK-AI-018. Status: draft (10/10 target).</em></p>";
