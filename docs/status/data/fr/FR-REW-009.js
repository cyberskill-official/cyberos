window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-REW-009"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The REW service <strong>MUST</strong> ship VietQR payroll batch at <code>services/rew/src/batch/</code> generating bulk transfer file + CFO manual confirm + FR-INV-005 reconciliation, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>batch_status</code> against closed enum per DEC-2231.</li></ol>\n<ol><li><strong>MUST</strong> generate file at <code>file_generator.rs::generate(payroll_run)</code> per DEC-2230:</li></ol>\n<ul><li>For each member: account_number + amount + memo (template per FR-CRM-009 convention)</li><li>Output Napas-bulk-transfer XML or CSV format</li><li>SHA256 the file for verification</li></ul>\n<ol><li><strong>MUST</strong> require CFO manual confirm at <code>POST .../confirm-submitted</code> per DEC-2232 — system updates status; no auto-submit.</li></ol>\n<ol><li><strong>MUST</strong> reconcile per DEC-2233 — match FR-INV-005 inbound webhook acks to batch entries via memo.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0009</code>: ```sql CREATE TABLE rew_payroll_batches ( batch_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, payroll_run_id UUID NOT NULL REFERENCES rew_payroll_runs(run_id), status TEXT NOT NULL DEFAULT 'generated' CHECK (status IN ('generated','downloaded','cfo_confirmed_submitted','paid_acked','partial_failed','failed')), file_sha256 CHAR(64) NOT NULL, file_doc_id UUID NOT NULL, total_amount_vnd BIGINT NOT NULL, members_count INT NOT NULL, generated_at TIMESTAMPTZ NOT NULL DEFAULT now(), downloaded_at TIMESTAMPTZ, downloaded_by UUID, cfo_confirmed_at TIMESTAMPTZ, cfo_confirmed_by UUID, trace_id CHAR(32), UNIQUE (payroll_run_id) ); ALTER TABLE rew_payroll_batches ENABLE ROW LEVEL SECURITY; CREATE POLICY batches_rls ON rew_payroll_batches USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON rew_payroll_batches FROM cyberos_app; GRANT UPDATE (status, downloaded_at, downloaded_by, cfo_confirmed_at, cfo_confirmed_by) ON rew_payroll_batches TO cyberos_app;</li></ol>\n<p>   CREATE TABLE rew_batch_member_acks (      ack_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      batch_id UUID NOT NULL REFERENCES rew_payroll_batches(batch_id),      member_id UUID NOT NULL,      amount_vnd BIGINT NOT NULL,      memo TEXT NOT NULL,      status TEXT NOT NULL DEFAULT 'pending'        CHECK (status IN ('pending','acked','failed')),      acked_at TIMESTAMPTZ,      UNIQUE (batch_id, member_id)    );    ALTER TABLE rew_batch_member_acks ENABLE ROW LEVEL SECURITY;    CREATE POLICY acks_rls ON rew_batch_member_acks      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON rew_batch_member_acks FROM cyberos_app;    GRANT UPDATE (status, acked_at) ON rew_batch_member_acks TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/rew/payroll/runs/{id}/batch        (generate after commit) GET  /v1/rew/payroll/batches/{id}/file      (CFO downloads; logs) POST /v1/rew/payroll/batches/{id}/confirm   (CFO manual confirm submitted) GET  /v1/rew/payroll/batches/{id}           (status + acks) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-2234. PII per FR-MEMORY-111: amounts SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from generate → download → confirm → ack → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> auto-submit to bank per DEC-2232.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip CFO confirm per DEC-2232.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why CFO manual submit (DEC-2232)?</strong> Banks don't yet have API for VN payroll; CFO uses bank portal directly. Manual confirm avoids state ambiguity.</p>\n<p><strong>Why FR-INV-005 reconciliation (DEC-2233)?</strong> Inbound VietQR webhook tracks individual transfers; matching by memo enables auto-status.</p>\n<p><strong>Why per-member ack rows (DEC-2233)?</strong> Bank may fail one member's transfer (insufficient funds, closed account); per-row status tracks partial failures.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample batch file (CSV format):</p>\n<pre><code class=\"language-csv\">account_number,amount_vnd,memo\n1234567890,25000000,REW-2026-06-uuid-a\n0987654321,30000000,REW-2026-06-uuid-b</code></pre>\n<p>Sample status:</p>\n<pre><code class=\"language-json\">{\n  \"batch_id\": \"uuid\",\n  \"status\": \"paid_acked\",\n  \"total_amount_vnd\": 825000000,\n  \"members_count\": 30,\n  \"acked_count\": 30,\n  \"failed_count\": 0\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>batch_status enum cardinality 6</strong>. 2. <strong>Napas-spec file format</strong>. 3. <strong>Memo template per FR-CRM-009</strong>. 4. <strong>SHA256 file integrity</strong>. 5. <strong>FR-DOC-001 storage</strong>. 6. <strong>CFO-only download</strong>. 7. <strong>CFO-only confirm</strong>. 8. <strong>NEVER auto-submit</strong>. 9. <strong>5 memory audit kinds emitted</strong>. 10. <strong>PII scrubbed (amounts SHA256)</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>Append-only via REVOKE except status cols</strong>. 14. <strong>UNIQUE(payroll_run_id) — one batch per run</strong>. 15. <strong>Per-member ack tracking</strong>. 16. <strong>Partial failure → status=partial_failed</strong>. 17. <strong>All acked → status=paid_acked</strong>. 18. <strong>bigint VND</strong>. 19. <strong>FR-INV-005 reconciliation hooks</strong>. 20. <strong>Re-confirm idempotent</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn never_auto_submits() {\n    let ctx = TestContext::with_committed_payroll().await;\n    ctx.generate_batch(ctx.run_id).await;\n    let bank_calls = ctx.bank_api_call_count().await;\n    assert_eq!(bank_calls, 0);\n    let batch = ctx.fetch_batch(ctx.run_id).await;\n    assert_eq!(batch.status, \"generated\");\n}\n\n#[tokio::test]\nasync fn cfo_confirm_updates_status() {\n    let ctx = TestContext::with_generated_batch().await;\n    ctx.download_as_cfo(ctx.batch_id).await;\n    ctx.cfo_confirm_submitted(ctx.batch_id).await;\n    let batch = ctx.fetch_batch(ctx.batch_id).await;\n    assert_eq!(batch.status, \"cfo_confirmed_submitted\");\n}\n\n#[tokio::test]\nasync fn reconciliation_matches_acks() {\n    let ctx = TestContext::with_cfo_confirmed_batch().await;\n    ctx.simulate_inv_005_webhook_acks(ctx.batch_id).await;\n    let batch = ctx.fetch_batch(ctx.batch_id).await;\n    assert_eq!(batch.status, \"paid_acked\");\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-INV-005. <strong>Cross-module:</strong> FR-REW-005 (payroll source), FR-CRM-009 (memo format), FR-DOC-001 (file storage), FR-AUTH-101 (CFO role), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>File generation fail</td><td>catch</td><td>status=failed</td><td>retry</td></tr><tr><td>Bank submission fail (CFO reports)</td><td>CFO marks failed</td><td>status=failed</td><td>retry batch</td></tr><tr><td>Partial member transfer fail</td><td>ack tracking</td><td>status=partial_failed</td><td>per-member retry</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Duplicate batch per run</td><td>UNIQUE</td><td>409</td><td>inherent</td></tr><tr><td>Memo format mismatch (FR-CRM-009 drift)</td><td>reconciliation fail</td><td>sev-2</td><td>data fix</td></tr><tr><td>Decimal precision</td><td>bigint VND</td><td>inherent</td><td>inherent</td></tr><tr><td>CFO confirms before submit</td><td>timing assumption</td><td>CFO discipline</td><td>inherent</td></tr><tr><td>Re-confirm</td><td>idempotent</td><td>inherent</td><td>inherent</td></tr><tr><td>File format invalid (bank rejects)</td><td>CFO reports</td><td>sev-1</td><td>regenerate</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 File format: Napas bulk transfer XML or CSV per bank's accepted spec.</li><li>§11.2 Memo template: <code>REW-{period_yyyymm}-{member_id_8}</code> matches FR-INV-005 reconciliation regex.</li><li>§11.3 memory audit body: batch_id, payroll_run_id, status, counts; amounts SHA256.</li><li>§11.4 Reconciliation cron via FR-MCP-007 polls every 15min during pay-day window.</li><li>§11.5 Future: bank API integration eliminates manual confirm step.</li></ul>\n<hr>\n<p><em>End of FR-REW-009 spec.</em></p>";
