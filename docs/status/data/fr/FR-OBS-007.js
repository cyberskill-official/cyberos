window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OBS-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A Rust HTTP service <code>obs-router</code> <strong>MUST</strong> accept Alertmanager webhook fires and route them through CUO's <code>obs.triage-alert@1</code> skill:</p>\n<ol><li><strong>MUST</strong> accept Alertmanager v2 webhook on <code>:7777/alert</code> with payload schema per Alertmanager docs.</li><li><strong>MUST</strong> invoke CUO <code>obs.triage-alert@1</code> skill with the alert payload as input. The skill returns <code>{ confidence: f64, summary: String, suggested_runbook: Option&lt;RunbookRef&gt;, suspected_cause: String }</code>.</li><li><strong>MUST</strong> route based on (severity, confidence):</li></ol>\n<ul><li>sev-1: route to BOTH CHAT and PagerDuty regardless of confidence.</li><li>sev-2..sev-4 + confidence ≥ 0.70: post triage summary to CHAT (<code>#oncall</code> channel).</li><li>sev-2..sev-4 + confidence &lt; 0.70: trigger PagerDuty.</li></ul>\n<ol><li><strong>MUST</strong> include in CHAT post:</li></ol>\n<ul><li>Alert name + severity (visual badge).</li><li>CUO triage summary (the skill's output).</li><li>Suspected cause.</li><li>Suggested runbook (link to KB if present).</li><li>Trace_id link (jumps to Tempo).</li><li>Ack button (sends POST to <code>obs-router:7777/ack/&lt;alert_id&gt;</code>).</li><li>Escalate-to-PagerDuty button (sends POST to escalate).</li></ul>\n<ol><li><strong>MUST</strong> route sev-1 alerts to BOTH CHAT and PagerDuty (no triage trust at sev-1 — DEC-171).</li><li><strong>MUST</strong> emit memory audit row <code>obs.alert_triaged</code> per alert with payload: <code>alert_name</code>, <code>severity</code>, <code>cuo_confidence</code>, <code>route</code> (chat | pagerduty | both), <code>suggested_runbook</code>, <code>trace_id</code>, <code>request_id</code>.</li><li><strong>MUST</strong> preserve <code>trace_id</code> from alert labels (<code>trace_id</code> exemplar from FR-OBS-005). The CHAT post + audit row carry the trace_id; investigators click → jump to Tempo.</li><li><strong>MUST</strong> complete triage + route within 10s p95. Slow triage means the alert is invisible to ops during the lag.</li><li><strong>MUST</strong> handle CUO skill timeout (5s budget): on timeout, route as if confidence == 0 (PagerDuty fallback). The metric <code>obs_router_cuo_timeouts_total</code> increments; sev-2 alarm if rate &gt; 5%.</li><li><strong>MUST</strong> handle ack from CHAT: when operator clicks ack-button, POST to <code>/ack/&lt;alert_id&gt;</code> MUST (a) update the CHAT post to show \"acked by @user at &lt;time&gt;\", (b) close the PagerDuty incident if dual-routed (sev-1), (c) emit <code>obs.alert_acked</code> memory row.</li><li><strong>MUST NOT</strong> silent-drop any alert. Every alert MUST route somewhere — CUO failure → PagerDuty fallback; CHAT failure → PagerDuty fallback; PagerDuty failure → log sev-1 + try CHAT as last resort.</li><li><strong>MUST</strong> support deduplication: alerts with identical <code>alert_fingerprint</code> arriving within 5 minutes are deduplicated to a single CHAT post (with a counter \"fired N times in last 5m\"). PagerDuty has its own dedup; we don't double-up.</li><li><strong>MUST</strong> authenticate Alertmanager via shared secret (<code>X-CyberOS-Webhook-Secret</code> header). Unauthenticated webhooks → 401.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>obs_router_alerts_received_total{severity}</code> (counter).</li><li><code>obs_router_alerts_routed_total{route, severity, outcome}</code> (counter; outcome ∈ ok | chat_failed | pagerduty_failed | cuo_failed | dropped).</li><li><code>obs_triage_confidence</code> (histogram).</li><li><code>obs_router_triage_latency_ms</code> (histogram; SLO p95 &lt; 10s).</li><li><code>obs_router_acks_total{ack_source}</code> (counter).</li><li><code>obs_router_dedup_total</code> (counter).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why CUO triage at all?</strong> Alerts are the firehose. CUO with <code>obs.triage-alert@1</code> skill (markdown + RAG over KB runbooks) reads the alert, queries similar past incidents, and produces a triage summary. ~80% of alerts have a clear root cause; CUO surfaces it; ops gets the answer in CHAT instead of getting paged at 3am.</p>\n<p><strong>Why 0.70 confidence threshold (DEC-170)?</strong> Conservative. Below 0.70, CUO is uncertain enough that page-on-call is the safe choice. Above 0.70, CUO is confident enough that CHAT triage is appropriate. The number is calibrated against the obs.triage-alert@1 skill's calibration curve; future tuning via FR-CUO-104.</p>\n<p><strong>Why sev-1 always pages BOTH (DEC-171)?</strong> Sev-1 = customer-facing outage OR security incident. The cost of missing one is too high to trust triage. Page-AND-CHAT means ops sees both immediately; CHAT post provides context while pager wakes them up.</p>\n<p><strong>Why ack-button + auto-close-PagerDuty (§1 #10)?</strong> When ops acks in CHAT, PagerDuty incident should auto-close — otherwise pager keeps escalating (\"acknowledge in PagerDuty too\"). The auto-close eliminates the double-action.</p>\n<p><strong>Why dedup at 5m window (§1 #12)?</strong> Many alert rules fire repeatedly while the underlying issue persists (e.g., latency spike for 30 minutes generates 30 fires). 30 CHAT posts is noise. Dedup to 1 post + counter is signal.</p>\n<p><strong>Why never-silent-drop (§1 #11)?</strong> Silent drops are catastrophic — a real incident invisible to ops. Cascade fallback (CUO → PagerDuty → CHAT-as-last-resort) ensures every alert reaches a human.</p>\n<p><strong>Why webhook secret (§1 #13)?</strong> Open webhook ingress lets attackers fake alerts. Operator dashboards become poisonable. Shared secret limits to legitimate Alertmanager.</p>\n<p><strong>Why 10s p95 budget (§1 #8)?</strong> Slow triage = alert invisible to ops. 5s for CUO + 5s for CHAT/PagerDuty post is the budget. Above 10s, ops investigates \"why didn't I see the alert?\"</p>\n<p><strong>Why escalate-to-PagerDuty button (§1 #4)?</strong> Sometimes triage looks good in CHAT but ops realises it's actually severe. One-click escalation to PagerDuty without re-triaging.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/obs-router/src/alertmanager_webhook.rs\n#[derive(Deserialize)]\npub struct AlertmanagerWebhook {\n    pub version: String,\n    pub group_key: String,\n    pub status: String,                       // \"firing\" | \"resolved\"\n    pub receiver: String,\n    pub group_labels: HashMap&lt;String, String&gt;,\n    pub common_labels: HashMap&lt;String, String&gt;,\n    pub alerts: Vec&lt;Alert&gt;,\n}\n\n#[derive(Deserialize)]\npub struct Alert {\n    pub status: String,\n    pub labels: HashMap&lt;String, String&gt;,      // includes severity, alert_name, tenant_id, trace_id\n    pub annotations: HashMap&lt;String, String&gt;, // includes runbook_url, summary\n    pub starts_at: DateTime&lt;Utc&gt;,\n    pub ends_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    pub fingerprint: String,                  // dedup key\n}\n\n// services/obs-router/src/cuo_triage.rs\n#[derive(Deserialize)]\npub struct TriageResult {\n    pub confidence: f64,\n    pub summary: String,\n    pub suspected_cause: String,\n    pub suggested_runbook: Option&lt;RunbookRef&gt;,\n}\n\n#[derive(Deserialize)]\npub struct RunbookRef {\n    pub kb_article_id: String,\n    pub title: String,\n    pub url: String,\n}\n\npub async fn invoke_triage_skill(alert: &amp;Alert) -&gt; Result&lt;TriageResult, CuoError&gt; {\n    let timeout = Duration::from_secs(5);\n    let payload = serde_json::json!({ \"alert\": alert });\n    tokio::time::timeout(timeout, cuo_client::invoke_skill(\"obs.triage-alert@1\", &amp;payload))\n        .await\n        .map_err(|_| CuoError::Timeout)?\n}\n\n// services/obs-router/src/severity.rs\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum Severity { P1, P2, P3, P4 }\n\npub fn parse_severity(label: &amp;str) -&gt; Severity {\n    match label.to_uppercase().as_str() {\n        \"P1\" | \"SEV-1\" | \"CRITICAL\" =&gt; Severity::P1,\n        \"P2\" | \"SEV-2\" | \"ERROR\"    =&gt; Severity::P2,\n        \"P3\" | \"SEV-3\" | \"WARNING\"  =&gt; Severity::P3,\n        _                            =&gt; Severity::P4,\n    }\n}\n\n#[derive(Debug, Clone, Copy)]\npub enum Route { Chat, PagerDuty, Both }\n\npub fn decide_route(severity: Severity, confidence: f64) -&gt; Route {\n    if severity == Severity::P1 { return Route::Both; }\n    if confidence &gt;= 0.70 { Route::Chat } else { Route::PagerDuty }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/obs-router/src/main.rs (handler)\npub async fn handle_alert(payload: AlertmanagerWebhook) -&gt; Result&lt;(), RouterError&gt; {\n    let alert = &amp;payload.alerts[0];   // assume one per webhook (Alertmanager grouping)\n    let severity = severity::parse_severity(alert.labels.get(\"severity\").map(String::as_str).unwrap_or(\"\"));\n\n    // §1 #12 dedup\n    if dedup::seen_within(alert.fingerprint, Duration::from_secs(300)) {\n        metrics::dedup();\n        return update_existing_chat_post_counter(alert).await;\n    }\n\n    let triage = match cuo_triage::invoke_triage_skill(alert).await {\n        Ok(t) =&gt; t,\n        Err(_) =&gt; TriageResult { confidence: 0.0, summary: \"CUO unavailable; review alert manually.\".into(),\n                                  suspected_cause: \"unknown\".into(), suggested_runbook: None },\n    };\n\n    let route = severity::decide_route(severity, triage.confidence);\n\n    let result = match route {\n        Route::Chat =&gt; chat_post::post(alert, &amp;triage, severity).await\n            .map(|_| ())\n            .or_else(|_| pagerduty::trigger(alert, &amp;triage, severity)),\n        Route::PagerDuty =&gt; pagerduty::trigger(alert, &amp;triage, severity).await,\n        Route::Both =&gt; {\n            let chat_result = chat_post::post(alert, &amp;triage, severity).await;\n            let pd_result = pagerduty::trigger(alert, &amp;triage, severity).await;\n            chat_result.or(pd_result)\n        }\n    };\n\n    if result.is_err() {\n        // §1 #11 last-resort fallback\n        let _ = chat_post::post_emergency(alert).await;\n    }\n\n    memory::emit(canonical::alert_triaged(alert, severity, triage.confidence, route)).await?;\n    metrics::routed(route, severity, result.is_ok());\n    Ok(())\n}\n\n// services/obs-router/src/chat_post.rs\npub async fn post(alert: &amp;Alert, triage: &amp;TriageResult, severity: Severity) -&gt; Result&lt;MessageId, ChatError&gt; {\n    let trace_id = alert.labels.get(\"trace_id\").cloned();\n    let blocks = json!([\n        { \"type\": \"header\", \"text\": { \"type\": \"plain_text\", \"text\": format!(\"[{severity:?}] {}\", alert.labels[\"alertname\"]) }},\n        { \"type\": \"section\", \"text\": { \"type\": \"mrkdwn\", \"text\": triage.summary }},\n        { \"type\": \"section\", \"text\": { \"type\": \"mrkdwn\", \"text\": format!(\"*Suspected cause:* {}\", triage.suspected_cause) }},\n        triage.suggested_runbook.as_ref().map(|r| json!({ \"type\": \"section\", \"text\": { \"type\": \"mrkdwn\",\n            \"text\": format!(\"*Runbook:* &lt;{}|{}&gt;\", r.url, r.title) }})),\n        trace_id.as_ref().map(|t| json!({ \"type\": \"section\", \"text\": { \"type\": \"mrkdwn\",\n            \"text\": format!(\"*Trace:* &lt;https://grafana.cyberos.world/trace/{t}|{t}&gt;\") }})),\n        { \"type\": \"actions\", \"elements\": [\n            { \"type\": \"button\", \"text\": { \"type\": \"plain_text\", \"text\": \"Ack\" },\n              \"action_id\": \"ack\", \"value\": alert.fingerprint.clone() },\n            { \"type\": \"button\", \"text\": { \"type\": \"plain_text\", \"text\": \"Escalate to PD\" },\n              \"action_id\": \"escalate\", \"value\": alert.fingerprint.clone() }\n        ]}\n    ]);\n    chat_client::post_blocks(\"#oncall\", blocks).await\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>Alertmanager fire → 200 OK from obs-router within 10s p95.</li><li>High-confidence (≥0.70) sev-2 → CHAT post created in <code>#oncall</code> (no PagerDuty).</li><li>Low-confidence (&lt;0.70) sev-2 → PagerDuty trigger (no CHAT).</li><li>Sev-1 → BOTH CHAT + PagerDuty regardless of confidence.</li><li>CHAT post contains: alert name, severity badge, summary, suspected cause, runbook link, trace_id link, ack button, escalate button.</li><li>CUO timeout → confidence 0 → PagerDuty (graceful fallback).</li><li>CUO failure (non-timeout) → confidence 0 → PagerDuty.</li><li>CHAT failure → fallback to PagerDuty.</li><li>PagerDuty failure → emergency CHAT post; sev-1 log.</li><li>memory audit row <code>obs.alert_triaged</code> emitted per alert.</li><li>Trace_id preserved end-to-end (CHAT link works).</li><li>Ack button: clicked → CHAT post updates \"acked by @user at &lt;ts&gt;\"; PagerDuty incident closed if dual-routed; <code>obs.alert_acked</code> row emitted.</li><li>Escalate button: clicked → PagerDuty triggered post-hoc; row carries <code>escalated_from_chat: true</code>.</li><li>Dedup: same fingerprint within 5min → existing CHAT post counter +1; no new post.</li><li>Webhook secret enforced: missing → 401; correct → process.</li><li>p95 triage+route latency &lt; 10s.</li><li>CUO timeout &gt; 5% sustained → sev-2 alarm.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn high_confidence_p2_routes_to_chat_only() {\n    let mock_cuo = MockCuo::returning_confidence(0.85);\n    let mock_chat = MockChat::start();\n    let mock_pd = MockPagerDuty::start();\n    let _ = handle_alert(test_webhook(P2, \"MemorySearchLatencyHigh\")).await.unwrap();\n    assert_eq!(mock_chat.posts().len(), 1);\n    assert_eq!(mock_pd.incidents().len(), 0);\n}\n\n#[tokio::test]\nasync fn low_confidence_routes_to_pagerduty() {\n    let _ = MockCuo::returning_confidence(0.40);\n    let mock_pd = MockPagerDuty::start();\n    let mock_chat = MockChat::start();\n    let _ = handle_alert(test_webhook(P3, \"x\")).await.unwrap();\n    assert_eq!(mock_pd.incidents().len(), 1);\n    assert_eq!(mock_chat.posts().len(), 0);\n}\n\n#[tokio::test]\nasync fn sev1_routes_to_both_regardless_of_confidence() {\n    let _ = MockCuo::returning_confidence(0.99);\n    let mock_chat = MockChat::start();\n    let mock_pd = MockPagerDuty::start();\n    let _ = handle_alert(test_webhook(P1, \"DBdown\")).await.unwrap();\n    assert_eq!(mock_chat.posts().len(), 1);\n    assert_eq!(mock_pd.incidents().len(), 1);\n}\n\n#[tokio::test]\nasync fn cuo_timeout_falls_back_to_pagerduty() {\n    let _ = MockCuo::timing_out();\n    let mock_pd = MockPagerDuty::start();\n    let _ = handle_alert(test_webhook(P2, \"x\")).await.unwrap();\n    assert_eq!(mock_pd.incidents().len(), 1);\n    let timeouts: u64 = otel_test_helper::counter_value(\"obs_router_cuo_timeouts_total\", &amp;[]);\n    assert_eq!(timeouts, 1);\n}\n\n#[tokio::test]\nasync fn ack_button_closes_pagerduty_for_sev1() {\n    let _ = handle_alert(test_webhook(P1, \"x\")).await.unwrap();\n    let alert_id = \"test_fingerprint\";\n    let _ = ack_handler::handle_ack(alert_id, \"alice@cyberos.world\").await.unwrap();\n    let mock_pd = MockPagerDuty::singleton();\n    let last_action = mock_pd.last_action(alert_id).await;\n    assert_eq!(last_action, \"resolved\");\n    assert!(memory_test_helper::has_row(\"obs.alert_acked\", alert_id));\n}\n\n#[tokio::test]\nasync fn dedup_within_5min_window() {\n    let mock_chat = MockChat::start();\n    let _ = handle_alert(test_webhook_with_fingerprint(\"fp1\", P3)).await.unwrap();\n    let _ = handle_alert(test_webhook_with_fingerprint(\"fp1\", P3)).await.unwrap();\n    let _ = handle_alert(test_webhook_with_fingerprint(\"fp1\", P3)).await.unwrap();\n    assert_eq!(mock_chat.posts().len(), 1);   // single post\n    let counter: u64 = otel_test_helper::counter_value(\"obs_router_dedup_total\", &amp;[]);\n    assert_eq!(counter, 2);\n}\n\n#[tokio::test]\nasync fn webhook_secret_enforced() {\n    let resp = post_webhook_without_secret(test_webhook(P2, \"x\")).await;\n    assert_eq!(resp.status(), 401);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-markdown\">&lt;!-- skills/obs.triage-alert/SKILL.md --&gt;\n---\nid: obs.triage-alert\nversion: 1.0.0\ndescription: Triage Alertmanager alerts using KB runbook RAG; emit confidence + summary + suggested runbook\ntools: [kb_search]\noutput_schema: { confidence: number, summary: string, suspected_cause: string, suggested_runbook: object? }\n---\n\n# obs.triage-alert@1\n\nGiven an Alertmanager alert, search the KB runbook corpus for similar past incidents,\nsynthesise a triage summary, and assess confidence.\n\n## Procedure\n1. Extract alert_name + labels + annotations.\n2. Search KB (`kb_search`) for runbooks matching alert_name or annotations.summary.\n3. Read top-3 matched runbooks.\n4. Synthesise: \"what happened, what to do, which runbook applies.\"\n5. Confidence = function of (runbook match quality, alert clarity).\n\n## Output\nJSON object per `output_schema`.</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-OBS-003</strong> — RED metrics drive most alert rules.</li><li><strong>FR-OBS-005</strong> — trace_id propagation; alerts carry trace_id from exemplars.</li><li><strong>FR-CUO-101</strong> — CUO Phase-2 LLM cascade. This FR can ship before Phase-2 with a Phase-1 rule-based fallback.</li><li><strong>FR-KB-008</strong> — KB runbook corpus that the skill RAG-searches.</li><li>Crates: <code>axum</code>, <code>reqwest</code>, <code>tokio</code>, <code>serde</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Alertmanager webhook</h3>\n<pre><code class=\"language-json\">{\n  \"version\": \"4\",\n  \"group_key\": \"...\",\n  \"status\": \"firing\",\n  \"alerts\": [{\n    \"status\": \"firing\",\n    \"labels\": { \"alertname\": \"MemorySearchLatencyHigh\", \"severity\": \"P2\", \"tenant_id\": \"550e...\", \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\" },\n    \"annotations\": { \"summary\": \"p99 &gt; 500ms\", \"runbook_url\": \"https://kb.cyberos.world/runbooks/memory-latency\" },\n    \"starts_at\": \"2026-05-15T14:00:00Z\",\n    \"fingerprint\": \"abc123\"\n  }]\n}</code></pre>\n<h3>CHAT post (high-confidence triage)</h3>\n<pre><code class=\"language-text\">[P2] MemorySearchLatencyHigh\n\nCUO triage (confidence 0.85): Recent surge in queries against tenant org:cyberskill's KB.\nIndex size has grown 30% in past hour. p99 spike correlates with FR-MEMORY-101 ingest job.\n\nSuspected cause: index-rebalancing during ingest\nRunbook: &lt;https://kb.cyberos.world/runbooks/memory-ingest-pause|Pause ingest temporarily&gt;\nTrace: &lt;https://grafana.cyberos.world/trace/0af7651...|view in Tempo&gt;\n\n[Ack] [Escalate to PD]</code></pre>\n<h3><code>obs.alert_triaged</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"obs.alert_triaged\",\n  \"payload\": {\n    \"alert_name\": \"MemorySearchLatencyHigh\",\n    \"severity\": \"P2\",\n    \"cuo_confidence\": 0.85,\n    \"route\": \"chat\",\n    \"suggested_runbook\": \"kb.cyberos.world/runbooks/memory-ingest-pause\",\n    \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n    \"request_id\": \"obs_router_...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Confidence-calibration tuning (FR-CUO-104) — slice 4+.</li><li>Per-tenant alert routing (different ops teams per tenant) — slice 5+.</li><li>Alert auto-resolve when underlying metric recovers — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>CUO skill timeout (5s)</td><td>tokio timeout</td><td>confidence=0 → PagerDuty</td><td>Investigate CUO</td></tr><tr><td>CUO skill error</td><td>catch-all</td><td>confidence=0 → PagerDuty</td><td>Investigate CUO</td></tr><tr><td>CHAT post fails</td><td>http error</td><td>Fallback to PagerDuty</td><td>Self-resolves</td></tr><tr><td>PagerDuty unreachable</td><td>http error</td><td>Last-resort emergency CHAT post</td><td>Sev-1 log</td></tr><tr><td>Sev-1 misclassified by sender</td><td>severity check</td><td>sev-1 always pages regardless</td><td>By design</td></tr><tr><td>Webhook secret missing</td><td>auth check</td><td>401</td><td>Operator fixes Alertmanager config</td></tr><tr><td>Webhook secret leaked</td><td>unauthorized webhooks</td><td>sev-1 alarm</td><td>Rotate secret</td></tr><tr><td>Triage &gt; 10s</td><td>OTel histogram</td><td>sev-3 alarm</td><td>Investigate CUO</td></tr><tr><td>Dedup window too short (legitimate re-fire missed)</td><td>metric anomaly</td><td>Investigate; tune window</td><td>Config</td></tr><tr><td>Ack button URL unreachable</td><td>callback fails</td><td>CHAT post stays \"unacked\"</td><td>Operator manually acks via PagerDuty</td></tr><tr><td>Escalate button → already-PD-routed alert</td><td>dedup check</td><td>No-op</td><td>By design</td></tr><tr><td>memory audit emit fails</td><td>memory_writer error</td><td>Sev-1 log; route still completes</td><td>Operator investigates memory</td></tr><tr><td>CUO returns confidence &gt; 1.0 (skill bug)</td><td>clamp at parse</td><td>Treat as 1.0</td><td>Investigate skill</td></tr><tr><td>Alertmanager webhook payload schema change</td><td>parse fails</td><td>Sev-1; Alertmanager retries</td><td>Update parser to handle new schema</td></tr><tr><td>Multiple alerts in one webhook</td><td>iterate per alert</td><td>Each routed independently</td><td>By design</td></tr><tr><td>CHAT channel <code>#oncall</code> archived/deleted</td><td>post fails</td><td>Fallback to PagerDuty</td><td>Operator restores channel</td></tr><tr><td>Suggested runbook URL fabricated/unverified</td><td>not an exact match in OBS_RUNBOOK_ALLOWLIST (the KB index)</td><td>Dropped; CHAT shows \"Runbook: none\" (fail-closed)</td><td>Operator adds the runbook to the KB index / allowlist</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The <code>obs.triage-alert@1</code> skill is a markdown-only skill (no executable code) with RAG over the KB runbook corpus (FR-KB-008). The skill's confidence comes from runbook-match quality.</li><li>Sev-1 always pages — never trust triage at the highest severity. Even confidence 0.99 sev-1 routes to BOTH.</li><li>CUO skill failure (timeout OR error) → confidence=0 → PagerDuty fallback. Never silent-drop.</li><li>Dedup window 5min collapses repeated fires of the same alert. The counter \"fired N times in last 5m\" tells ops the persistence.</li><li>Ack button + auto-close-PagerDuty eliminates the dual-action problem (ack in CHAT AND ack in PagerDuty).</li><li>Escalate button gives ops one-click PagerDuty escalation when CHAT triage doesn't capture the severity.</li><li>Webhook secret prevents alert poisoning. Rotation cadence quarterly.</li><li>The 10s triage+route p95 budget is the user-experience floor. Above 10s, ops doesn't see alerts in their natural workflow.</li><li>As built (2026-07-02, P2 hardening): a suggested runbook is surfaced only if its URL is EXACTLY in the KB runbook allowlist (env <code>OBS_RUNBOOK_ALLOWLIST</code>, comma- or whitespace-separated). Anything else - including the SKILL.md example URL a local model may echo, or a fabricated slug on the real KB host - is dropped (fail-closed when the allowlist is empty). Enforced in <code>services/obs-router/src/runbook.rs</code> (<code>sanitize_runbook</code>) at <code>route_alert</code>, so both the CHAT post and the <code>obs.alert_triaged</code> audit only carry a verified runbook. DEPLOY: set <code>OBS_RUNBOOK_ALLOWLIST</code> to the live KB runbook URLs before obs-router serves traffic.</li></ul>\n<hr>\n<p><em>End of FR-OBS-007. Status: draft (10/10 target).</em></p>";
