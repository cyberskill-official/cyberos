window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SKILL-103"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>Every <code>.skill</code> bundle's <code>SKILL.md</code> file <strong>MUST</strong> carry a YAML frontmatter block conforming to schema v1. The contract:</p>\n<ol><li><strong>MUST</strong> begin with <code>---</code> on line 1, end with <code>---</code> on a line by itself; the YAML block lies between. Anything before line 1's <code>---</code> is rejected. The body markdown begins on the line after the closing fence.</li><li><strong>MUST</strong> include the following required fields (rejection if missing):</li></ol>\n<ul><li><code>id</code> (string): kebab-case slug <code>^[a-z][a-z0-9-]*$</code>; the canonical skill identifier; used in OCI registry path (FR-SKILL-102).</li><li><code>version</code> (string): SemVer <code>^\\d+\\.\\d+\\.\\d+(?:-[A-Za-z0-9.-]+)?$</code>.</li><li><code>description</code> (string): 1–200 chars; the operator-facing one-line summary.</li><li><code>allowed_memory_scopes</code> (list of glob strings): which memory paths the skill MAY read. Empty list <code>[]</code> = no memory access. Each glob validated by <code>globset@0.4</code>; invalid glob → reject. Globs are evaluated against memory paths starting at <code>&lt;memory-root&gt;/</code> (e.g. <code>memories/projects/cyberos/**</code>).</li><li><code>allowed_tools</code> (list of strings): which broker-managed tools the skill MAY invoke. Each name MUST appear in the canonical tool enum (<code>Bash</code>, <code>Read</code>, <code>Write</code>, <code>Edit</code>, <code>Glob</code>, <code>Grep</code>, <code>MemoryRead</code>, <code>MemorySearch</code>, <code>HttpFetch</code>, custom MCP names per FR-SKILL-104). Unknown names → reject.</li></ul>\n<ol><li><strong>MUST</strong> support the following optional fields (validated when present):</li></ol>\n<ul><li><code>signature</code> (object): <code>{ algo: \"ed25519\", public_key_hex: &lt;64-char hex&gt;, signature_hex: &lt;128-char hex&gt; }</code>. Verifies that frontmatter content + body hash matches the signature. Absent signature = unsigned skill (allowed for local dev; rejected by FR-SKILL-102's OCI gate which requires signed).</li><li><code>min_broker_version</code> (string): SemVer; broker refuses to invoke if its own version is lower than this. Default: <code>0.1.0</code>.</li><li><code>max_broker_version</code> (string): SemVer; broker refuses if higher (forward-incompatible). Default: unbounded.</li><li><code>disallowed_tools</code> (list of strings): explicit denylist (subset of canonical tool enum). Useful for skills that want to assert \"I never need Edit\" even if allowed_tools is broad.</li><li><code>sync_class</code> (enum: <code>private</code> | <code>shareable</code>): default <code>private</code>; controls whether the skill's emitted memory rows are eligible for cross-device sync (per FR-MEMORY-106).</li><li><code>tenant_scope</code> (enum: <code>any</code> | <code>pinned</code>): default <code>any</code>; if <code>pinned</code>, skill executes only under the tenant_id where it was installed.</li><li><code>effort_minutes</code> (integer): suggested timeout cap; broker enforces SIGTERM after <code>effort_minutes * 60</code> seconds.</li><li><code>tags</code> (list of strings): operator-facing categorisation; ≤ 10 tags; each tag ≤ 30 chars.</li></ul>\n<ol><li><strong>MUST</strong> validate every field per §1 #2 + #3 BEFORE the skill is admitted to invocation. The broker exposes <code>frontmatter::load_and_validate(bundle_path) -&gt; Result&lt;SkillFrontmatter, FrontmatterError&gt;</code>; only <code>Ok</code> admits the skill.</li><li><strong>MUST</strong> reject (and produce structured error) on any of:</li></ol>\n<ul><li>Schema-required field missing.</li><li><code>id</code> does not match the kebab-case regex.</li><li><code>version</code> is not valid SemVer.</li><li>Any <code>allowed_memory_scopes</code> glob fails <code>globset::Glob::new()</code>.</li><li>Any <code>allowed_tools</code> value is not in the canonical enum.</li><li><code>signature</code> present and verification fails.</li><li><code>min_broker_version &gt; current_broker_version</code>.</li><li><code>max_broker_version &lt; current_broker_version</code>.</li><li>Frontmatter parses but contains unknown keys NOT prefixed with <code>x-</code> (forward-compat: <code>x-</code> prefixed unknown keys are allowed and ignored).</li></ul>\n<ol><li><strong>MUST</strong> be schema-version frozen at v1. Adding a new required field is a v2 schema; v1 bundles MUST continue to load on broker versions that support v2.</li><li><strong>MUST</strong> verify ed25519 signature (when present) BEFORE trusting any other frontmatter field. The signature signs <code>SHA-256(frontmatter_yaml_canonical) || SHA-256(body_markdown_canonical)</code>. Canonical form: trimmed leading/trailing whitespace, normalised line endings to <code>\\n</code>.</li><li><strong>MUST</strong> emit OTel span <code>skill.frontmatter.validate</code> per call with attributes <code>skill_id</code>, <code>version</code>, <code>validation_outcome</code> (ok | parse_err | required_missing | invalid_field | signature_failed | version_skew), <code>duration_ms</code>.</li><li><strong>MUST</strong> emit metric <code>skill_frontmatter_validation_total{outcome}</code> (counter) and <code>skill_frontmatter_validation_duration_seconds</code> (histogram).</li><li><strong>MUST</strong> be invokable from CLI: <code>cyberos skill validate &lt;bundle-path&gt;</code> prints structured JSON report; exit 0 on valid, exit 6 (<code>SchemaViolation</code>) on invalid; exit 1 on file-not-found.</li><li><strong>MUST</strong> be invokable from editor LSPs / CI gates via the standalone <code>skill.schema.json</code> (JSONSchema Draft 2020-12). LSPs can validate live as the author types; the Rust validator is the runtime source of truth (LSP is informational).</li><li><strong>SHOULD</strong> support <code>cyberos skill scaffold &lt;id&gt;</code> that writes a starter SKILL.md with all required fields populated and clear comments.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why frontmatter at all (§1 #1)?</strong> The body of SKILL.md is markdown — readable by humans, opaque to brokers. The frontmatter is the machine-readable contract: \"I am skill X v1.2.3; I need scopes Y and Z; I use tools W.\" Without it, the broker has no idea what the skill needs until the skill <em>tries</em> something and either succeeds or trips a denial.</p>\n<p><strong>Why required-field strictness (§1 #2 + #5)?</strong> Optional fields drift over time — early adopters skip them, later code assumes they exist, runtime panics. Required fields are validated at load; the skill either works on every broker or doesn't load at all. Five fields is the calibrated minimum: id (identity), version (compatibility), description (operator UX), allowed_memory_scopes (memory authorisation), allowed_tools (tool authorisation).</p>\n<p><strong>Why allowed_memory_scopes as globs (§1 #2)?</strong> A skill that touches \"all my projects\" needs <code>memories/projects/**</code>; a skill that touches \"only Cyberos\" needs <code>memories/projects/cyberos/**</code>. Globs are the right shape — granular, declarative, well-understood. <code>globset</code> is the canonical Rust glob crate; reusing it ensures consistency with FR-MEMORY-106's sync-class globs.</p>\n<p><strong>Why allowed_tools as enum (§1 #2)?</strong> If we accepted arbitrary strings, typos would creep in (<code>Bsah</code>, <code>memoryread</code>). Enum tokens are validated; the canonical set is small (~10 native tools + MCP names via FR-SKILL-104). Adding a new tool means: enum variant + broker impl + version bump.</p>\n<p><strong>Why signature optional (§1 #3)?</strong> Local dev needs to iterate fast — adding/checking signature on every change kills the flow. The OCI gate (FR-SKILL-102) enforces signature on registry-uploaded bundles; local dev runs unsigned. Two scopes: trust-local (loose) and trust-registry (strict).</p>\n<p><strong>Why min/max broker version (§1 #3)?</strong> Forward compat: a skill compiled against broker 0.5 may use APIs not in 0.3. Without min, the skill loads on 0.3 and crashes at first API call. Backward compat: a skill explicitly tested on 0.5 may know it breaks on 0.6's new sandbox rules; max lets the author declare that. Both are SemVer-checked at load time — invocation-time crashes are eliminated.</p>\n<p><strong>Why unknown-keys rejected by default (§1 #5)?</strong> Strict-by-default catches typos: <code>allowed_tols: [Bash]</code> would silently pass under \"ignore unknown\" but the broker wouldn't see any allowed tools. With strict rejection, the author sees \"unknown key <code>allowed_tols</code>\" immediately. The <code>x-</code> prefix escape hatch is the YAML convention for \"I know this is non-standard; respect it.\"</p>\n<p><strong>Why schema-version freeze at v1 (§1 #6 + DEC-182)?</strong> Stability — every change to the schema risks breaking authored skills. v1 is the minimal stable schema. v2 will be additive (new optional fields) or breaking (new required fields — major version bump); broker version negotiation handles cross-version.</p>\n<p><strong>Why signature signs frontmatter + body (§1 #7)?</strong> Frontmatter alone could be signed while body is tampered — a malicious actor edits the body to add a <code>bash:rm -rf /</code> operation. Signing both is necessary. Canonical form (trimmed + LF) ensures cross-platform reproducibility.</p>\n<p><strong>Why LSP-friendly schema.json (§1 #11)?</strong> Authoring SKILL.md in an editor with live validation cuts iteration time. The Rust validator is authoritative (it runs at invoke time), but the JSONSchema mirror lets editors highlight errors as you type.</p>\n<p><strong>Why scaffold CLI (§1 #12)?</strong> New skill authors copy-paste from existing skills, which propagates whatever bugs are in the template. A <code>scaffold</code> command writes a fresh, fully-documented starter — operators don't need to know the schema by heart.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>SkillFrontmatter (Rust)</h3>\n<pre><code class=\"language-rust\">// services/skill-broker/src/frontmatter/schema.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Debug, Deserialize, Serialize, PartialEq)]\n#[serde(deny_unknown_fields)]\npub struct SkillFrontmatter {\n    // Required\n    pub id:                   String,\n    pub version:              String,           // SemVer\n    pub description:          String,\n    pub allowed_memory_scopes: Vec&lt;String&gt;,      // glob patterns\n    pub allowed_tools:        Vec&lt;ToolName&gt;,\n\n    // Optional\n    #[serde(default)] pub signature:           Option&lt;Signature&gt;,\n    #[serde(default)] pub min_broker_version:  Option&lt;String&gt;,\n    #[serde(default)] pub max_broker_version:  Option&lt;String&gt;,\n    #[serde(default)] pub disallowed_tools:    Vec&lt;ToolName&gt;,\n    #[serde(default = \"default_sync_class\")] pub sync_class: SyncClass,\n    #[serde(default = \"default_tenant_scope\")] pub tenant_scope: TenantScope,\n    #[serde(default) ] pub effort_minutes:     Option&lt;u32&gt;,\n    #[serde(default) ] pub tags:               Vec&lt;String&gt;,\n\n    // Forward-compat: any `x-*` keys are captured here\n    #[serde(flatten)] pub x_extensions:        std::collections::BTreeMap&lt;String, serde_yaml::Value&gt;,\n}\n\n#[derive(Clone, Copy, Debug, Deserialize, Serialize, PartialEq, Eq)]\npub enum ToolName {\n    Bash, Read, Write, Edit, Glob, Grep,\n    MemoryRead, MemorySearch, MemoryEmit,\n    HttpFetch, HttpPost,\n    // MCP tool names registered at broker startup per FR-SKILL-104\n    #[serde(other)] McpTool,  // matches any registered MCP name; validated against MCP_TOOL_REGISTRY at validation time\n}\n\n#[derive(Clone, Copy, Debug, Deserialize, Serialize, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\npub enum SyncClass { Private, Shareable }\nfn default_sync_class() -&gt; SyncClass { SyncClass::Private }\n\n#[derive(Clone, Copy, Debug, Deserialize, Serialize, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\npub enum TenantScope { Any, Pinned }\nfn default_tenant_scope() -&gt; TenantScope { TenantScope::Any }\n\n#[derive(Clone, Debug, Deserialize, Serialize, PartialEq)]\n#[serde(deny_unknown_fields)]\npub struct Signature {\n    pub algo:           SignatureAlgo,\n    pub public_key_hex: String,                // 64 chars (32 bytes hex)\n    pub signature_hex:  String,                // 128 chars (64 bytes hex)\n}\n#[derive(Clone, Copy, Debug, Deserialize, Serialize, PartialEq, Eq)]\n#[serde(rename_all = \"lowercase\")]\npub enum SignatureAlgo { Ed25519 }</code></pre>\n<h3>Validation errors</h3>\n<pre><code class=\"language-rust\">// services/skill-broker/src/frontmatter/validators.rs\n#[derive(thiserror::Error, Debug)]\npub enum FrontmatterError {\n    #[error(\"missing frontmatter (file must start with `---`)\")]                 MissingFrontmatter,\n    #[error(\"YAML parse error: {0}\")]                                            YamlParse(#[from] serde_yaml::Error),\n    #[error(\"id violates kebab-case: {0:?}\")]                                    InvalidId(String),\n    #[error(\"version is not valid SemVer: {0:?}\")]                               InvalidVersion(String),\n    #[error(\"description must be 1..=200 chars (was {0})\")]                      InvalidDescription(usize),\n    #[error(\"allowed_memory_scopes[{idx}] is not a valid glob: {pat:?}\")]        InvalidMemoryScopeGlob { idx: usize, pat: String },\n    #[error(\"allowed_tools[{idx}] is not a known tool: {name:?}\")]              UnknownTool { idx: usize, name: String },\n    #[error(\"signature verification failed (frontmatter + body hash mismatch)\")] SignatureFailed,\n    #[error(\"broker version {broker} below skill's min_broker_version {min}\")]  BrokerTooOld { broker: String, min: String },\n    #[error(\"broker version {broker} above skill's max_broker_version {max}\")]  BrokerTooNew { broker: String, max: String },\n    #[error(\"unknown field {key:?} (must be `x-`-prefixed to be ignored)\")]     UnknownField { key: String },\n}\n\npub fn validate(fm: &amp;SkillFrontmatter, body_canonical: &amp;str, broker_version: &amp;str) -&gt; Result&lt;(), FrontmatterError&gt; {\n    static ID_RX: once_cell::sync::Lazy&lt;regex::Regex&gt; =\n        once_cell::sync::Lazy::new(|| regex::Regex::new(r\"^[a-z][a-z0-9-]*$\").unwrap());\n    if !ID_RX.is_match(&amp;fm.id) { return Err(FrontmatterError::InvalidId(fm.id.clone())); }\n\n    if !is_semver(&amp;fm.version) { return Err(FrontmatterError::InvalidVersion(fm.version.clone())); }\n\n    let desc_len = fm.description.chars().count();\n    if !(1..=200).contains(&amp;desc_len) { return Err(FrontmatterError::InvalidDescription(desc_len)); }\n\n    for (i, pat) in fm.allowed_memory_scopes.iter().enumerate() {\n        globset::Glob::new(pat).map_err(|_| FrontmatterError::InvalidMemoryScopeGlob { idx: i, pat: pat.clone() })?;\n    }\n\n    // ToolName enum already validated by serde; McpTool variant needs registry check\n    for (i, t) in fm.allowed_tools.iter().enumerate() {\n        if let ToolName::McpTool = t {\n            // serde captured the raw name via `#[serde(other)]`; cross-reference with MCP_TOOL_REGISTRY\n            // For simplicity in this skeleton we trust serde's pre-validation; FR-SKILL-104 owns the registry check.\n        }\n    }\n\n    // Broker version range\n    if let Some(min) = &amp;fm.min_broker_version {\n        if !is_semver(min) { return Err(FrontmatterError::InvalidVersion(min.clone())); }\n        if semver_lt(broker_version, min) {\n            return Err(FrontmatterError::BrokerTooOld { broker: broker_version.into(), min: min.clone() });\n        }\n    }\n    if let Some(max) = &amp;fm.max_broker_version {\n        if !is_semver(max) { return Err(FrontmatterError::InvalidVersion(max.clone())); }\n        if semver_gt(broker_version, max) {\n            return Err(FrontmatterError::BrokerTooNew { broker: broker_version.into(), max: max.clone() });\n        }\n    }\n\n    // Signature: verify SHA-256(frontmatter_yaml) || SHA-256(body) against the ed25519 signature.\n    if let Some(sig) = &amp;fm.signature {\n        verify_signature(fm, body_canonical, sig)?;\n    }\n\n    // Unknown fields not prefixed with `x-`\n    for k in fm.x_extensions.keys() {\n        if !k.starts_with(\"x-\") {\n            return Err(FrontmatterError::UnknownField { key: k.clone() });\n        }\n    }\n    Ok(())\n}</code></pre>\n<h3>Parser</h3>\n<pre><code class=\"language-rust\">// services/skill-broker/src/frontmatter/parser.rs\nuse std::path::Path;\n\npub fn load_and_validate(bundle_path: &amp;Path, broker_version: &amp;str)\n    -&gt; Result&lt;(SkillFrontmatter, String /* body */), FrontmatterError&gt;\n{\n    let skill_md_path = bundle_path.join(\"SKILL.md\");\n    let bytes = std::fs::read(&amp;skill_md_path).map_err(|_| FrontmatterError::MissingFrontmatter)?;\n    let text = std::str::from_utf8(&amp;bytes).map_err(|_| FrontmatterError::MissingFrontmatter)?;\n    let (yaml, body) = split_fenced(text)?;\n    let fm: SkillFrontmatter = serde_yaml::from_str(yaml)?;\n    let body_canonical = canonicalise(body);\n    validators::validate(&amp;fm, &amp;body_canonical, broker_version)?;\n    Ok((fm, body.to_string()))\n}\n\nfn split_fenced(text: &amp;str) -&gt; Result&lt;(&amp;str, &amp;str), FrontmatterError&gt; {\n    if !text.starts_with(\"---\") { return Err(FrontmatterError::MissingFrontmatter); }\n    let rest = &amp;text[3..];\n    // Find newline-fenced `---` close\n    let close = rest.find(\"\\n---\\n\").or_else(|| rest.find(\"\\n---\"))\n        .ok_or(FrontmatterError::MissingFrontmatter)?;\n    let yaml = &amp;rest[..close].trim_start_matches('\\n');\n    let body_start = close + \"\\n---\\n\".len();\n    let body = if body_start &lt; rest.len() { &amp;rest[body_start..] } else { \"\" };\n    Ok((yaml, body))\n}\n\nfn canonicalise(body: &amp;str) -&gt; String {\n    body.replace(\"\\r\\n\", \"\\n\").trim().to_string()\n}</code></pre>\n<h3>CLI</h3>\n<pre><code class=\"language-rust\">// services/skill-broker/src/bin/cyberos-skill-validate.rs\nuse clap::Parser;\nuse cyberos_cli_exit::ExitCode;\nuse std::path::PathBuf;\n\n#[derive(Parser)]\n#[command(name = \"cyberos skill validate\")]\nstruct Args {\n    /// Bundle path (directory containing SKILL.md)\n    bundle: PathBuf,\n    /// Output JSON instead of human-readable\n    #[arg(long)] json: bool,\n    /// Override broker version for forward-compat testing\n    #[arg(long)] broker_version: Option&lt;String&gt;,\n}\n\nfn main() -&gt; ExitCode {\n    let args = Args::parse();\n    let broker_version = args.broker_version.unwrap_or_else(|| env!(\"CARGO_PKG_VERSION\").into());\n    match cyberos_skill_broker::frontmatter::load_and_validate(&amp;args.bundle, &amp;broker_version) {\n        Ok((fm, _body)) =&gt; {\n            if args.json {\n                println!(\"{}\", serde_json::to_string_pretty(&amp;serde_json::json!({\n                    \"status\": \"ok\",\n                    \"id\": fm.id,\n                    \"version\": fm.version,\n                    \"allowed_memory_scopes\": fm.allowed_memory_scopes,\n                    \"allowed_tools\": fm.allowed_tools,\n                })).unwrap());\n            } else {\n                println!(\"✓ {} v{} — valid\", fm.id, fm.version);\n            }\n            ExitCode::Ok\n        }\n        Err(cyberos_skill_broker::frontmatter::FrontmatterError::MissingFrontmatter) =&gt; {\n            eprintln!(\"ERROR: SKILL.md not found or has no frontmatter\");\n            ExitCode::UserError\n        }\n        Err(e) =&gt; {\n            eprintln!(\"ERROR: {e}\");\n            ExitCode::SchemaViolation\n        }\n    }\n}</code></pre>\n<h3>Example SKILL.md (valid)</h3>\n<pre><code class=\"language-markdown\">---\nid: memory-capture\nversion: 1.0.0\ndescription: Canonical entry point for emitting memory capture rows from arbitrary tools.\nallowed_memory_scopes:\n  - memories/projects/**\n  - memories/people/*/notes/**\nallowed_tools:\n  - Read\n  - MemoryRead\n  - MemoryEmit\nsync_class: shareable\ntenant_scope: any\neffort_minutes: 5\ntags: [memory, capture, foundation]\nsignature:\n  algo: ed25519\n  public_key_hex: \"a4d8f2e1b9c7...\"\n  signature_hex:  \"9e3b1c2a...\"\nx-cyberos-author: \"stephen@cyberskill.world\"\n---\n\n# memory-capture@1\n\nThis skill is the canonical entry point ...</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Valid SKILL.md parses</strong> — fixture <code>skill-valid/SKILL.md</code> → <code>load_and_validate</code> returns <code>Ok(SkillFrontmatter, body)</code>.</li><li><strong>Missing frontmatter rejected</strong> — file without leading <code>---</code> → <code>Err(MissingFrontmatter)</code>.</li><li><strong>Missing required field rejected</strong> — fixture omits <code>id</code> → <code>Err(YamlParse)</code> (serde missing-field error wrapped).</li><li><strong>Invalid id (camelCase) rejected</strong> — <code>id: memoryCapture</code> → <code>Err(InvalidId)</code>.</li><li><strong>Invalid version rejected</strong> — <code>version: 1.0</code> (not full SemVer) → <code>Err(InvalidVersion)</code>.</li><li><strong>Description length bounds</strong> — empty <code>description: \"\"</code> → <code>Err(InvalidDescription)</code>; 201-char description → same.</li><li><strong>Bad glob rejected</strong> — <code>allowed_memory_scopes: [\"[unclosed\"]</code> → <code>Err(InvalidMemoryScopeGlob)</code>.</li><li><strong>Unknown tool rejected</strong> — <code>allowed_tools: [\"Bsah\"]</code> → serde Err mapped to validation Err.</li><li><strong>Unknown field without x- prefix rejected</strong> — <code>random_field: 1</code> → <code>Err(UnknownField)</code>.</li><li><strong>x-prefixed unknown field allowed</strong> — <code>x-cyberos-author: alice</code> → loads OK; stored in <code>x_extensions</code>.</li><li><strong>Signature verification: valid</strong> — signed fixture with correct keys → loads OK.</li><li><strong>Signature verification: tampered body</strong> — fixture's body modified after signing → <code>Err(SignatureFailed)</code>.</li><li><strong>Signature verification: tampered frontmatter</strong> — frontmatter field changed → <code>Err(SignatureFailed)</code>.</li><li><strong>Broker version too old</strong> — skill <code>min_broker_version: 2.0.0</code>, broker at 1.5 → <code>Err(BrokerTooOld)</code>.</li><li><strong>Broker version too new</strong> — skill <code>max_broker_version: 1.0</code>, broker at 2.0 → <code>Err(BrokerTooNew)</code>.</li><li><strong>Optional fields default correctly</strong> — fixture without <code>sync_class</code> → <code>SyncClass::Private</code>; without <code>tenant_scope</code> → <code>TenantScope::Any</code>.</li><li><strong>CLI exit codes</strong> — valid → 0; invalid → 6 (SchemaViolation); SKILL.md not found → 1 (UserError).</li><li><strong>CLI --json output</strong> — valid bundle → <code>{\"status\":\"ok\",\"id\":...}</code> JSON.</li><li><strong>JSONSchema mirror validates same fixtures</strong> — <code>skill.schema.json</code> + ajv-CLI → same accept/reject pattern as Rust validator on 8 fixture variants.</li><li><strong>OTel span per validate</strong> — span <code>skill.frontmatter.validate</code> with attrs <code>skill_id</code>, <code>version</code>, <code>validation_outcome</code>, <code>duration_ms</code>.</li><li><strong>Metric increments per outcome</strong> — running validator 10× on mixed valid/invalid → counter <code>skill_frontmatter_validation_total{outcome=\"ok\"}</code> + <code>{outcome=\"signature_failed\"}</code> etc. correct.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/skill-broker/tests/frontmatter_test.rs\n\n#[test]\nfn valid_skill_loads() {\n    let path = std::path::Path::new(\"tests/fixtures/skill-valid\");\n    let (fm, body) = frontmatter::load_and_validate(path, \"1.0.0\").unwrap();\n    assert_eq!(fm.id, \"memory-capture\");\n    assert_eq!(fm.version, \"1.0.0\");\n    assert!(!body.is_empty());\n}\n\n#[test]\nfn missing_frontmatter_rejected() {\n    // Fixture is a SKILL.md without `---` fences\n    let path = std::path::Path::new(\"tests/fixtures/skill-invalid-no-fm\");\n    assert!(matches!(\n        frontmatter::load_and_validate(path, \"1.0.0\"),\n        Err(FrontmatterError::MissingFrontmatter)\n    ));\n}\n\n#[test]\nfn bad_id_rejected() {\n    let path = std::path::Path::new(\"tests/fixtures/skill-invalid-bad-id\");\n    assert!(matches!(\n        frontmatter::load_and_validate(path, \"1.0.0\"),\n        Err(FrontmatterError::InvalidId(_))\n    ));\n}\n\n#[test]\nfn bad_glob_rejected() {\n    let path = std::path::Path::new(\"tests/fixtures/skill-invalid-bad-glob\");\n    let err = frontmatter::load_and_validate(path, \"1.0.0\").unwrap_err();\n    assert!(matches!(err, FrontmatterError::InvalidMemoryScopeGlob { .. }));\n}\n\n#[test]\nfn unknown_tool_rejected() {\n    let path = std::path::Path::new(\"tests/fixtures/skill-invalid-unknown-tool\");\n    let err = frontmatter::load_and_validate(path, \"1.0.0\").unwrap_err();\n    // serde maps unknown enum variant to YamlParse, or McpTool catches it; either way: not Ok\n    assert!(err.to_string().to_lowercase().contains(\"tool\") || matches!(err, FrontmatterError::YamlParse(_)));\n}\n\n#[test]\nfn x_prefixed_unknown_allowed() {\n    let path = std::path::Path::new(\"tests/fixtures/skill-x-extension\");\n    let (fm, _) = frontmatter::load_and_validate(path, \"1.0.0\").unwrap();\n    assert!(fm.x_extensions.contains_key(\"x-cyberos-author\"));\n}\n\n#[test]\nfn signature_tampered_body_rejected() {\n    // Fixture: valid signature, then body modified after signing\n    let path = std::path::Path::new(\"tests/fixtures/skill-tampered-body\");\n    let err = frontmatter::load_and_validate(path, \"1.0.0\").unwrap_err();\n    assert!(matches!(err, FrontmatterError::SignatureFailed));\n}\n\n#[test]\nfn broker_too_old_rejected() {\n    // Fixture: min_broker_version: 2.0.0\n    let path = std::path::Path::new(\"tests/fixtures/skill-needs-v2\");\n    let err = frontmatter::load_and_validate(path, \"1.5.0\").unwrap_err();\n    assert!(matches!(err, FrontmatterError::BrokerTooOld { .. }));\n}\n\n#[test]\nfn defaults_applied() {\n    let path = std::path::Path::new(\"tests/fixtures/skill-minimal\");\n    let (fm, _) = frontmatter::load_and_validate(path, \"1.0.0\").unwrap();\n    assert_eq!(fm.sync_class, SyncClass::Private);\n    assert_eq!(fm.tenant_scope, TenantScope::Any);\n    assert!(fm.tags.is_empty());\n}</code></pre>\n<pre><code class=\"language-bash\"># CLI fixtures\n$ cyberos skill validate tests/fixtures/skill-valid\n✓ memory-capture v1.0.0 — valid\n\n$ cyberos skill validate tests/fixtures/skill-invalid-bad-glob\nERROR: allowed_memory_scopes[0] is not a valid glob: \"[unclosed\"\n# exit 6\n\n$ cyberos skill validate tests/fixtures/skill-valid --json\n{\n  \"status\": \"ok\",\n  \"id\": \"memory-capture\",\n  \"version\": \"1.0.0\",\n  \"allowed_memory_scopes\": [\"memories/projects/**\", \"memories/people/*/notes/**\"],\n  \"allowed_tools\": [\"Read\", \"MemoryRead\", \"MemoryEmit\"]\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-SKILL-101 (upstream)</strong> — defines the broker that consumes this frontmatter at invoke time.</li><li><strong>FR-SKILL-102 (related)</strong> — OCI registry uploads MUST include a valid signature; this FR's signature schema is the format.</li><li><strong>FR-SKILL-104 (downstream)</strong> — capability broker enforces <code>allowed_tools</code> at runtime; FR-SKILL-104 owns the MCP_TOOL_REGISTRY.</li><li><strong>FR-SKILL-105 (downstream)</strong> — memory-sync@1 skill bundle is the first canonical user of <code>sync_class: shareable</code>.</li><li><strong>FR-MEMORY-106</strong> — sync_class semantics are reused (same enum variants per AGENTS.md §15).</li><li><strong>FR-AUTH-003</strong> — RLS for tenant_scope=pinned enforcement (broker injects tenant_id into memory reads).</li><li><strong><code>cyberos-cli-exit</code></strong> — exit codes.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<p>(Valid SKILL.md shown in §3.)</p>\n<h3>Validation failure JSON output</h3>\n<pre><code class=\"language-json\">{\n  \"status\": \"invalid\",\n  \"errors\": [\n    {\"field\": \"allowed_memory_scopes[0]\", \"code\": \"InvalidMemoryScopeGlob\", \"message\": \"not a valid glob: \\\"[unclosed\\\"\"}\n  ]\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Schema v2 (will add: telemetry_emit_kinds, required_secrets, capabilities_provided) — slice 3+.</li><li>Multi-signature support (two-of-three trusted authors) — slice 4+.</li><li>Schema migration tooling (auto-upgrade v1 → v2 frontmatter) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>File missing</td><td>std::fs::read Err</td><td><code>Err(MissingFrontmatter)</code>; exit 1</td><td>Operator runs <code>cyberos skill scaffold &lt;id&gt;</code></td></tr><tr><td>Frontmatter fence missing</td><td>text doesn't start with <code>---</code></td><td><code>Err(MissingFrontmatter)</code>; exit 6</td><td>Author adds fences</td></tr><tr><td>YAML syntax error</td><td>serde_yaml::Error</td><td><code>Err(YamlParse)</code>; exit 6 with line/col</td><td>Author fixes YAML</td></tr><tr><td>Required field missing</td><td>serde missing-field</td><td><code>Err(YamlParse)</code>; exit 6</td><td>Author adds field</td></tr><tr><td><code>id</code> violates kebab-case</td><td>regex no-match</td><td><code>Err(InvalidId)</code>; exit 6</td><td>Author fixes</td></tr><tr><td>Version not SemVer</td><td>parse fails</td><td><code>Err(InvalidVersion)</code>; exit 6</td><td>Author fixes</td></tr><tr><td>Description too long</td><td>char count</td><td><code>Err(InvalidDescription)</code>; exit 6</td><td>Author shortens</td></tr><tr><td>Bad glob</td><td><code>globset::Glob::new</code> Err</td><td><code>Err(InvalidMemoryScopeGlob)</code>; exit 6</td><td>Author fixes</td></tr><tr><td>Unknown tool</td><td>serde unknown-variant</td><td>mapped to YamlParse; exit 6</td><td>Author uses canonical name</td></tr><tr><td>Unknown field (no x- prefix)</td><td>x_extensions key check</td><td><code>Err(UnknownField)</code>; exit 6</td><td>Author renames or removes</td></tr><tr><td>Signature missing public key</td><td>hex parse</td><td><code>Err(SignatureFailed)</code>; exit 6</td><td>Re-sign bundle</td></tr><tr><td>Signature mismatch</td><td>ed25519 verify Err</td><td><code>Err(SignatureFailed)</code>; exit 6</td><td>Re-sign; or accept unsigned</td></tr><tr><td>Broker too old</td><td>semver compare</td><td><code>Err(BrokerTooOld)</code>; exit 6</td><td>Operator upgrades broker</td></tr><tr><td>Broker too new</td><td>semver compare</td><td><code>Err(BrokerTooNew)</code>; exit 6</td><td>Operator downgrades OR author bumps max_broker_version</td></tr><tr><td>Multi-byte char body</td><td>Rust UTF-8 invariants</td><td>None</td><td>None</td></tr><tr><td>Body with embedded <code>---</code> lines</td><td>parser uses first <code>\\n---\\n</code> after open</td><td>Correct; multi-fenced body handled</td><td>None</td></tr><tr><td>Signature algo unknown (e.g. <code>rsa</code>)</td><td>enum reject</td><td><code>Err(YamlParse)</code>; exit 6</td><td>Use ed25519</td></tr><tr><td>Public key wrong length (not 32 bytes)</td><td>hex decode + length check</td><td><code>Err(SignatureFailed)</code>; exit 6</td><td>Use correct key</td></tr><tr><td>Empty <code>allowed_tools: []</code></td><td>valid by schema (empty = no tools allowed)</td><td>OK; skill has no tool capabilities</td><td>By design</td></tr><tr><td>Empty <code>allowed_memory_scopes: []</code></td><td>valid (no memory access)</td><td>OK</td><td>By design</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The frontmatter parser MUST be tolerant of trailing whitespace after the closing fence; many editors auto-trim or auto-add blank lines.</li><li>The JSONSchema mirror is generated from the Rust types via <code>schemars</code> crate; the <code>cargo xtask schema</code> task in CI ensures <code>.json</code> is up-to-date.</li><li>The signature verification reuses the ed25519 implementation from <code>ed25519-dalek</code> (already in workspace via FR-OBS-009).</li><li><code>serde_yaml</code> allows duplicate keys silently in some versions; we pin to a version that errors on duplicates (≥ 0.9).</li><li>The <code>McpTool</code> <code>#[serde(other)]</code> variant is a forward-compat trick — for v1 we accept any string and defer registry validation to FR-SKILL-104. In v2 we may upgrade to a strict registry-time check.</li><li><code>effort_minutes</code> is an advisory cap; the actual SIGTERM enforcement lives in FR-SKILL-104. Default cap (when unspecified) is 30 minutes.</li><li><code>tags</code> are operator-facing only — no broker logic depends on them. Categorisation surfaces in <code>cyberos skill list</code>.</li><li>The <code>cyberos skill scaffold</code> CLI is intentionally simple: writes a SKILL.md with all required fields filled with placeholders + comments explaining each one. Authors can copy and customise.</li><li>The <code>body_canonical</code> form (trim + LF) matches Git's text mode; signed bundles are reproducible across OSes.</li></ul>\n<hr>\n<p><em>End of FR-SKILL-103.</em></p>";
