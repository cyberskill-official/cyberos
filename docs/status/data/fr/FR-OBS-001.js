window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OBS-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The observability plane <strong>MUST</strong> deploy a self-hosted OpenTelemetry Collector receiving OTLP from all CyberOS services, routing to LGTM backends (Loki for logs, Prometheus for metrics, Tempo for traces). Each piece:</p>\n<ol><li><strong>MUST</strong> accept OTLP/gRPC on <code>:4317</code> and OTLP/HTTP on <code>:4318</code> from all CyberOS services. Both protocols required (services may pick); both configured identically (same processors, same exporters).</li><li><strong>MUST</strong> authenticate ingress via per-service bearer token. Tokens live in <code>/etc/otelcol/auth.tokens</code> (mounted from the operator's secret store; NEVER committed to git). One token per service (<code>ai-gateway</code>, <code>auth-service</code>, <code>chat-service</code>, etc.); rotation cadence 90 days via <code>scripts/rotate_tokens.sh</code> (FR-AUTH-006-style sweeper). Missing or invalid token → ingress returns <code>401 UNAUTHORIZED</code>.</li><li><strong>MUST</strong> retain backend data per slice-1 floors:</li></ol>\n<ul><li>Loki: 30 days (P2 extends to 90 with S3 backend).</li><li>Prometheus: 90 days raw (P2 extends to 1 year with downsampling via Mimir).</li><li>Tempo full traces: 7 days.</li><li>Tempo sampled traces (FR-OBS-006 tail-sampled): 30 days.</li></ul>\n<ol><li><strong>MUST</strong> label every observation with <code>tenant_id</code> (from caller's resource attributes). FR-OBS-002's tenant-aware proxy enforces tenant-scoped queries downstream; this FR ensures the label IS present at ingress.</li><li><strong>MUST</strong> ship Grafana with provisioned datasources for all 3 backends (Loki, Prometheus, Tempo) AND a placeholder dashboards directory that FR-OBS-002 populates.</li><li><strong>MUST</strong> expose <code>/ready</code> health endpoints on all 4 services (collector + Loki + Prometheus + Tempo). Docker-compose healthchecks consume these; failure → container restart.</li><li><strong>MUST</strong> survive single-pod restarts without losing buffered data via OTel Collector's <code>file_storage</code> extension (1GB on-disk buffer at <code>/var/lib/otelcol</code>). Buffer is replayed on restart; data emitted during the restart window is delivered eventually.</li><li><strong>MUST</strong> apply OTel semantic conventions for resource attributes:</li></ol>\n<ul><li><code>service.name</code> (e.g., <code>\"ai-gateway\"</code>)</li><li><code>service.version</code> (semver)</li><li><code>deployment.environment</code> (<code>development | staging | production</code>)</li><li><code>tenant_id</code> (UUID) — populated by service per request</li><li><code>host.name</code></li></ul>\n<ol><li><strong>MUST</strong> enforce per-pipeline batch + resource processors:</li></ol>\n<ul><li><code>batch</code>: timeout 10s, send_batch_size 1024 (efficient backend writes).</li><li><code>resource</code>: upserts <code>deployment.environment</code> from <code>${ENV}</code> (defence against missing env vars).</li><li><code>attributes/pii_scrub</code>: drops attributes matching PII regex AS A SAFETY NET (caller-side discipline per FR-AI-022 §1 #15 is the primary control).</li></ul>\n<ol><li><strong>MUST</strong> emit collector self-telemetry (collector's own logs + metrics about throughput, drops, errors) to the same backends — operators see \"is the collector itself healthy?\" via Grafana.</li><li><strong>MUST</strong> apply PII-scrub processor at the collector as defence-in-depth: regex against email patterns + Vietnamese PII patterns (CCCD, MST, phone) + arbitrary <code>password=</code> substring. Matched attributes are DROPPED (not redacted to placeholder) AND emit <code>obs_collector_pii_scrub_total{pattern}</code> counter. Sustained &gt; 0/min triggers sev-1 (PII slipping past caller-side prevention).</li><li><strong>MUST</strong> be horizontally scalable: collector instances behind a load balancer; Loki/Prometheus/Tempo each support replication (slice 1 = single-instance per backend; slice 5 = HA with peering).</li><li><strong>MUST</strong> size at slice-1 baseline:</li></ol>\n<ul><li>Collector: 1 vCPU, 1GB RAM.</li><li>Loki: 2 vCPU, 4GB RAM.</li><li>Prometheus: 2 vCPU, 4GB RAM.</li><li>Tempo: 1 vCPU, 2GB RAM.</li><li>Grafana: 0.5 vCPU, 512MB RAM.</li><li>Total: 6.5 vCPU, 11.5GB RAM, 100GB disk.</li></ul>\n<ol><li><strong>SHOULD</strong> emit OTel SELF-metrics:</li></ol>\n<ul><li><code>obs_collector_received_spans_total{service}</code> (counter).</li><li><code>obs_collector_received_logs_total{service}</code> (counter).</li><li><code>obs_collector_received_metrics_total{service}</code> (counter).</li><li><code>obs_collector_dropped_total{reason}</code> (counter; reason ∈ auth | pii_scrub | backend_error | buffer_full).</li><li><code>obs_collector_buffer_bytes</code> (gauge; file_storage usage).</li><li><code>obs_collector_export_latency_ms{backend}</code> (histogram).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why LGTM stack (DEC-140)?</strong> Self-hosted, vendor-neutral, no per-host licensing fees. Critical for PDPL/GDPR data residency: SaaS observability (Datadog, New Relic) means tenant telemetry leaves Vietnam/EU borders. LGTM keeps everything in-region. Operational cost: ~$50/month for a slice-1 deployment (compute + disk).</p>\n<p><strong>Why per-service bearer tokens (§1 #2)?</strong> Ingress without auth lets anyone on the network inject fake telemetry — poisoning dashboards, creating alarm fatigue, providing cover for actual attacks. Per-service tokens scope the blast radius (compromised AI Gateway can't impersonate AUTH telemetry). Rotation prevents long-lived credential abuse.</p>\n<p><strong>Why file_storage extension (§1 #7)?</strong> Without persistent buffer, collector restart loses 5-30s of in-flight telemetry — invisible gaps in dashboards that look like outages or hide real outages. file_storage persists the buffer to disk; on restart, replay continues. The 1GB cap protects against unbounded growth during prolonged backend outages.</p>\n<p><strong>Why caller-side PII prevention is primary (§1 #11)?</strong> FR-AI-022 §1 #15 enforces typed attribute keys + AST lint at the source — PII never enters spans. The collector-side scrub is defence-in-depth: catches the cases where the caller-side prevention fails (e.g., a rare service that doesn't use the typed-key pattern, a regression in the lint). Detection at the collector means investigating + fixing the caller; the scrub also drops the data so it doesn't persist.</p>\n<p><strong>Why batch processor at 10s + 1024 (§1 #9)?</strong> Backend writes are more efficient batched. 10s is the latency-vs-efficiency floor; longer = laggier dashboards. 1024 is a per-batch cardinality floor — fewer round-trips at high volume.</p>\n<p><strong>Why slice-1 single-instance with HA deferred to slice 5 (§1 #12)?</strong> HA adds operational complexity (peering, leader election, split-memory). At slice-1 scale (50 tenants × ~5 services × ~100 spans/sec each = 25K spans/sec), a single collector instance + single backend per signal handles the load. HA earns its complexity at slice 5+ scale.</p>\n<p><strong>Why retention floors at 30d/90d/7d (§1 #3)?</strong> Logs (30d) cover most operational investigations + a typical compliance-audit lookback window. Metrics (90d) cover quarter-over-quarter trending. Traces (7d) are expensive (higher cardinality); 7d covers immediate investigations. Sampled traces (30d via FR-OBS-006) extend the trace lookback for regulatory needs at lower cost.</p>\n<p><strong>Why dedicated PII-scrub METRIC + sev-1 alert (§1 #11)?</strong> A scrub that matches PII is a SIGNAL — caller-side prevention failed. Sustained scrub-rate &gt; 0 means a service is leaking PII into telemetry; that's a code bug AND a compliance event. Sev-1 forces immediate investigation; the scrub itself prevents persistent storage of the leak.</p>\n<p><strong>Why grafana provisioned with placeholder dashboards (§1 #5)?</strong> FR-OBS-002 populates dashboards. Provisioning the datasources at THIS FR means dashboards land cleanly without manual datasource setup. The empty placeholder dir is a hook FR-OBS-002 fills.</p>\n<p><strong>Why OTel semantic conventions (§1 #8)?</strong> Standard attribute names mean Grafana dashboards (FR-OBS-002), correlation rules (FR-OBS-005), and tail-sampling policies (FR-OBS-006) all work without per-service translation. The convention is the contract; deviating means rewriting all consumers.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Collector config</h3>\n<pre><code class=\"language-yaml\"># deploy/obs/otel-collector-config.yaml\nreceivers:\n  otlp:\n    protocols:\n      grpc:\n        endpoint: 0.0.0.0:4317\n        auth: { authenticator: bearertokenauth }\n      http:\n        endpoint: 0.0.0.0:4318\n        auth: { authenticator: bearertokenauth }\n\nprocessors:\n  resource:\n    attributes:\n      - key: deployment.environment\n        value: ${ENV}\n        action: upsert\n  attributes/pii_scrub:\n    actions:\n      - key: prompt_text\n        action: delete\n      - key: response_text\n        action: delete\n      - key: user_email\n        action: delete\n      - key: cccd\n        action: delete\n      - pattern: \"(?i)(password|secret|api_?key)\\\\s*[:=]\"\n        action: delete\n  batch:\n    timeout: 10s\n    send_batch_size: 1024\n\nexporters:\n  loki:\n    endpoint: http://loki:3100/loki/api/v1/push\n    labels:\n      attributes:\n        service.name: \"service_name\"\n        tenant_id: \"tenant_id\"\n        deployment.environment: \"env\"\n  prometheusremotewrite:\n    endpoint: http://prometheus:9090/api/v1/write\n    external_labels: { source: \"otelcol\" }\n  otlp/tempo:\n    endpoint: tempo:4317\n    tls: { insecure: true }\n\nextensions:\n  file_storage:\n    directory: /var/lib/otelcol/file_storage\n    timeout: 1s\n  bearertokenauth:\n    scheme: \"Bearer\"\n    filename: /etc/otelcol/auth.tokens\n  health_check: { endpoint: 0.0.0.0:13133 }\n\nservice:\n  extensions: [file_storage, bearertokenauth, health_check]\n  pipelines:\n    logs:    { receivers: [otlp], processors: [resource, attributes/pii_scrub, batch], exporters: [loki] }\n    metrics: { receivers: [otlp], processors: [resource, batch], exporters: [prometheusremotewrite] }\n    traces:  { receivers: [otlp], processors: [resource, attributes/pii_scrub, batch], exporters: [otlp/tempo] }\n  telemetry:\n    metrics: { address: 0.0.0.0:8888 }   # collector self-metrics\n    logs: { level: info }</code></pre>\n<h3>Token file format</h3>\n<pre><code class=\"language-text\"># deploy/obs/auth/tokens.example  (committed; real tokens.live mounted separately)\nai-gateway   &lt;token-from-secret-store&gt;\nauth-service &lt;token-from-secret-store&gt;\nchat-service &lt;token-from-secret-store&gt;\nmemory-writer &lt;token-from-secret-store&gt;\nmcp-router   &lt;token-from-secret-store&gt;\n# rotate via scripts/rotate_tokens.sh quarterly</code></pre>\n<h3>docker-compose</h3>\n<pre><code class=\"language-yaml\"># deploy/obs/docker-compose.yml\nservices:\n  collector:\n    image: otel/opentelemetry-collector-contrib:0.110.0\n    command: [\"--config=/etc/otelcol/config.yaml\"]\n    ports: [\"4317:4317\", \"4318:4318\", \"13133:13133\", \"8888:8888\"]\n    environment: { ENV: \"${DEPLOYMENT_ENV:-production}\" }\n    volumes:\n      - ./otel-collector-config.yaml:/etc/otelcol/config.yaml:ro\n      - ./auth/tokens.live:/etc/otelcol/auth.tokens:ro\n      - otel-buffer:/var/lib/otelcol\n    depends_on:\n      loki:       { condition: service_healthy }\n      prometheus: { condition: service_healthy }\n      tempo:      { condition: service_healthy }\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:13133\"]\n      interval: 10s\n      timeout: 3s\n      retries: 3\n    restart: always\n    deploy:\n      resources:\n        limits: { cpus: \"1\", memory: \"1G\" }\n\n  loki:\n    image: grafana/loki:3.0.0\n    volumes:\n      - ./loki-config.yaml:/etc/loki/config.yaml:ro\n      - loki-data:/loki\n    healthcheck: { test: [\"CMD\", \"wget\", \"-q\", \"-O\", \"-\", \"http://localhost:3100/ready\"], interval: 10s }\n    restart: always\n    deploy: { resources: { limits: { cpus: \"2\", memory: \"4G\" } } }\n\n  prometheus:\n    image: prom/prometheus:v2.55.0\n    command:\n      - \"--config.file=/etc/prometheus/prometheus.yml\"\n      - \"--web.enable-remote-write-receiver\"\n      - \"--storage.tsdb.retention.time=90d\"\n    volumes:\n      - ./prometheus-config.yaml:/etc/prometheus/prometheus.yml:ro\n      - prom-data:/prometheus\n    healthcheck: { test: [\"CMD\", \"wget\", \"-q\", \"-O\", \"-\", \"http://localhost:9090/-/ready\"], interval: 10s }\n    restart: always\n    deploy: { resources: { limits: { cpus: \"2\", memory: \"4G\" } } }\n\n  tempo:\n    image: grafana/tempo:2.6.0\n    command: [\"-config.file=/etc/tempo.yaml\"]\n    volumes:\n      - ./tempo-config.yaml:/etc/tempo.yaml:ro\n      - tempo-data:/var/tempo\n    healthcheck: { test: [\"CMD\", \"wget\", \"-q\", \"-O\", \"-\", \"http://localhost:3200/ready\"], interval: 10s }\n    restart: always\n    deploy: { resources: { limits: { cpus: \"1\", memory: \"2G\" } } }\n\n  grafana:\n    image: grafana/grafana:11.3.0\n    ports: [\"3000:3000\"]\n    environment:\n      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}\n      GF_AUTH_ANONYMOUS_ENABLED: \"false\"\n    volumes:\n      - ./grafana/datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro\n      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro\n      - grafana-data:/var/lib/grafana\n    healthcheck: { test: [\"CMD\", \"wget\", \"-q\", \"-O\", \"-\", \"http://localhost:3000/api/health\"], interval: 10s }\n    restart: always\n    depends_on: [loki, prometheus, tempo]\n\nvolumes: { otel-buffer:, loki-data:, prom-data:, tempo-data:, grafana-data: }</code></pre>\n<h3>Loki retention</h3>\n<pre><code class=\"language-yaml\"># deploy/obs/loki-config.yaml (excerpt)\nlimits_config:\n  retention_period: 720h   # 30 days\ntable_manager:\n  retention_deletes_enabled: true\n  retention_period: 720h</code></pre>\n<h3>Tempo retention</h3>\n<pre><code class=\"language-yaml\"># deploy/obs/tempo-config.yaml (excerpt)\ncompactor:\n  compaction:\n    block_retention: 168h   # 7 days</code></pre>\n<h3>Grafana datasources</h3>\n<pre><code class=\"language-yaml\"># deploy/obs/grafana/datasources.yaml\napiVersion: 1\ndatasources:\n  - name: Loki\n    type: loki\n    url: http://loki:3100\n  - name: Prometheus\n    type: prometheus\n    url: http://prometheus:9090\n  - name: Tempo\n    type: tempo\n    url: http://tempo:3200</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Stack starts cleanly</strong> — <code>docker compose up -d</code> succeeds; all 5 containers reach healthy state in &lt;30s; smoke_test.sh passes.</li><li><strong>OTLP/gRPC accepts</strong> — Test client sending span to <code>:4317</code> produces stored trace in Tempo within 10s.</li><li><strong>OTLP/HTTP accepts</strong> — Same test via <code>:4318</code> produces stored trace.</li><li><strong>Bearer auth enforced</strong> — Test client without <code>Authorization: Bearer &lt;token&gt;</code> gets <code>401</code>.</li><li><strong>Per-service tokens distinct</strong> — <code>ai-gateway</code> token rejected by <code>auth-service</code> ingress (each token authorises one service.name).</li><li><strong>Logs route to Loki</strong> — Test log line via OTLP appears in Loki within 5s; queryable via Grafana.</li><li><strong>Metrics route to Prometheus</strong> — Test counter appears in Prometheus query within 30s.</li><li><strong>Traces route to Tempo</strong> — Test span appears in Tempo within 10s.</li><li><strong>Buffer survives restart</strong> — Kill collector mid-flush; restart; in-flight data not lost (file_storage replay verified).</li><li><strong>PII-scrub drops <code>prompt_text</code> attribute</strong> — Span emitted with <code>prompt_text=\"hello\"</code> arrives at Tempo WITHOUT that attribute; metric <code>obs_collector_pii_scrub_total{pattern=\"prompt_text\"}</code> increments; sev-1 alarm fires.</li><li><strong>Tenant_id label preserved</strong> — Span with <code>tenant_id</code> resource attr → Loki/Tempo records carry the label.</li><li><strong>Grafana datasources green</strong> — Grafana home page shows 3 datasources all live.</li><li><strong>Retention configs verified</strong> — Tempo deletes traces older than 7d; Loki rotates logs older than 30d; Prometheus retains 90d raw.</li><li><strong>Collector self-metrics emit</strong> — Prometheus query <code>obs_collector_received_spans_total</code> returns non-zero after test traffic.</li><li><strong>Healthchecks pass</strong> — All 4 backends + collector report ready; container restart on health failure.</li><li><strong>Resource limits enforced</strong> — <code>docker stats</code> shows containers within configured cpu/memory limits.</li><li><strong>Token rotation safe</strong> — <code>scripts/rotate_tokens.sh</code> rotates without service disruption (collector reloads file).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># deploy/obs/tests/smoke_test.sh\n#!/usr/bin/env bash\nset -euo pipefail\ncd \"$(dirname \"$0\")/..\"\n\ndocker compose up -d\nsleep 30\n\necho \"Checking healthchecks...\"\nfor svc in collector loki prometheus tempo grafana; do\n    status=$(docker compose ps --format json $svc | jq -r '.Health')\n    if [ \"$status\" != \"healthy\" ]; then echo \"FAIL: $svc not healthy\"; exit 1; fi\ndone\n\necho \"Sending test span via OTLP/HTTP...\"\nTOKEN=$(grep \"^ai-gateway \" auth/tokens.live | awk '{print $2}')\nTRACE_ID=$(uuidgen | tr -d '-' | head -c 32)\ncurl -X POST http://localhost:4318/v1/traces \\\n    -H \"Content-Type: application/json\" \\\n    -H \"Authorization: Bearer $TOKEN\" \\\n    -d \"{\\\"resourceSpans\\\":[{\\\"resource\\\":{\\\"attributes\\\":[{\\\"key\\\":\\\"service.name\\\",\\\"value\\\":{\\\"stringValue\\\":\\\"ai-gateway\\\"}}]},\\\"scopeSpans\\\":[{\\\"spans\\\":[{\\\"traceId\\\":\\\"$TRACE_ID\\\",\\\"spanId\\\":\\\"0011223344556677\\\",\\\"name\\\":\\\"test\\\",\\\"kind\\\":1,\\\"startTimeUnixNano\\\":\\\"1747526400000000000\\\",\\\"endTimeUnixNano\\\":\\\"1747526401000000000\\\"}]}]}]}\"\n\nsleep 10\necho \"Verifying span in Tempo...\"\nstatus=$(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3200/api/traces/$TRACE_ID)\nif [ \"$status\" != \"200\" ]; then echo \"FAIL: trace not found in Tempo\"; exit 1; fi\necho \"✅ smoke_test passed\"</code></pre>\n<pre><code class=\"language-bash\"># deploy/obs/tests/auth_required_test.sh\n#!/usr/bin/env bash\nstatus=$(curl -s -o /dev/null -w \"%{http_code}\" -X POST http://localhost:4318/v1/traces \\\n    -H \"Content-Type: application/json\" \\\n    -d '{\"resourceSpans\":[]}')\nif [ \"$status\" != \"401\" ]; then echo \"FAIL: ingress accepted unauthenticated request\"; exit 1; fi\necho \"✅ auth_required_test passed\"</code></pre>\n<pre><code class=\"language-bash\"># deploy/obs/tests/buffer_survives_restart_test.sh\n#!/usr/bin/env bash\n# Send 100 spans, kill collector mid-flush, restart, verify all 100 in Tempo\nTOKEN=$(grep \"^ai-gateway \" auth/tokens.live | awk '{print $2}')\nfor i in {1..100}; do\n    curl -X POST http://localhost:4318/v1/traces -H \"Authorization: Bearer $TOKEN\" -d @\"test_span_$i.json\" &amp;\ndone\nsleep 1\ndocker compose kill collector\nsleep 5\ndocker compose up -d collector\nsleep 15\n\nfound=$(curl -s \"http://localhost:3200/api/search?tags=test_batch:restart_test\" | jq '.traces | length')\nif [ \"$found\" -lt 95 ]; then echo \"FAIL: only $found/100 spans recovered\"; exit 1; fi\necho \"✅ buffer_survives_restart_test passed ($found/100)\"</code></pre>\n<pre><code class=\"language-bash\">docker compose -f deploy/obs/docker-compose.yml up -d\n./deploy/obs/tests/smoke_test.sh\n./deploy/obs/tests/auth_required_test.sh\n./deploy/obs/tests/buffer_survives_restart_test.sh</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-bash\"># deploy/obs/scripts/rotate_tokens.sh\n#!/usr/bin/env bash\nset -euo pipefail\nTOKENS_FILE=/etc/otelcol/auth.tokens\nNEW_TOKENS_FILE=$(mktemp)\nfor service in ai-gateway auth-service chat-service memory-writer mcp-router; do\n    new_token=$(openssl rand -hex 32)\n    echo \"$service $new_token\" &gt;&gt; \"$NEW_TOKENS_FILE\"\n    # TODO: push new token to each service's secret store via secret-manager API\ndone\nmv \"$NEW_TOKENS_FILE\" \"$TOKENS_FILE\"\ndocker compose kill -s SIGHUP collector   # OTel collector reloads on SIGHUP\necho \"✅ Tokens rotated\"</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li>Docker Compose v2.0+ (k8s + Helm at slice 5+).</li><li>6.5 vCPU + 11.5GB RAM + 100GB disk (slice 1 baseline; scales linearly).</li><li>Operator's secret store (1Password Connect, AWS Secrets Manager, etc.) for <code>auth.tokens.live</code>.</li><li>Ports: 4317, 4318 (collector), 3000 (Grafana), 13133 (collector health), 8888 (collector self-metrics).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>OTLP/HTTP ingress</h3>\n<pre><code class=\"language-http\">POST /v1/traces HTTP/1.1\nHost: otel.cyberos.world:4318\nAuthorization: Bearer &lt;ai-gateway-token&gt;\nContent-Type: application/json\n\n{\n  \"resourceSpans\": [{\n    \"resource\": { \"attributes\": [\n      { \"key\": \"service.name\",        \"value\": { \"stringValue\": \"ai-gateway\" } },\n      { \"key\": \"service.version\",     \"value\": { \"stringValue\": \"0.4.1\" } },\n      { \"key\": \"deployment.environment\", \"value\": { \"stringValue\": \"production\" } },\n      { \"key\": \"tenant_id\",           \"value\": { \"stringValue\": \"550e...\" } }\n    ]},\n    \"scopeSpans\": [{ \"spans\": [{ \"traceId\": \"...\", \"spanId\": \"...\", \"name\": \"ai_gateway.chat_completion\", ... }] }]\n  }]\n}</code></pre>\n<h3>PII-scrub event (sev-1)</h3>\n<pre><code class=\"language-text\">WARN  pattern=user_email service=chat-service\n      obs_collector_pii_scrub: dropped attribute \"user_email\" — caller-side prevention failed\nsev-1 obs_collector_pii_scrub_total{pattern=\"user_email\"} incremented</code></pre>\n<h3>Token rotation</h3>\n<pre><code class=\"language-text\">$ ./deploy/obs/scripts/rotate_tokens.sh\nRotating ai-gateway token...\nRotating auth-service token...\n...\nReloading collector via SIGHUP...\n✅ Tokens rotated</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>HA peering (Loki + Prometheus + Tempo replication) — slice 5+.</li><li>S3 backend for long-term retention — P2.</li><li>Mimir replacement for Prometheus (longer retention + downsampling) — P2.</li><li>Cross-region collector federation — slice 6+.</li><li>mTLS instead of bearer tokens — slice 4+ (when service-mesh lands).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Collector OOM</td><td>docker healthcheck fails</td><td>Restart policy: always</td><td>Operator scales memory limit</td></tr><tr><td>Loki backend full (&gt;90% disk)</td><td>disk-usage metric</td><td>Logs dropped; retention shortened automatically</td><td>Operator extends disk OR moves to S3 (P2)</td></tr><tr><td>Prometheus backend full</td><td>same</td><td>Metrics dropped</td><td>Same</td></tr><tr><td>Tempo backend full</td><td>same</td><td>Traces dropped</td><td>Same</td></tr><tr><td>Auth token leaked</td><td>Unknown caller successfully emits + sev-1 alarm</td><td>Sev-1 OBS event</td><td>Rotate via scripts/rotate_tokens.sh; investigate</td></tr><tr><td>Backend (Tempo) restart</td><td>healthcheck 503</td><td>Collector buffers via file_storage; resumes when backend up</td><td>Self-healing</td></tr><tr><td>Cross-service trace context broken</td><td>Tempo shows orphan spans</td><td>Investigate W3C traceparent propagation (FR-AI-022)</td><td>Engineer fixes propagation gap</td></tr><tr><td>PII detected by scrub</td><td>metric <code>obs_collector_pii_scrub_total</code> increment</td><td>Sev-1 + dropped from spans</td><td>Investigate caller; add typed-key per FR-AI-022 §1 #15</td></tr><tr><td>Collector buffer full (&gt;1GB)</td><td>metric <code>obs_collector_buffer_bytes</code> near cap</td><td>Spans dropped (oldest first); metric <code>obs_collector_dropped_total{reason=buffer_full}</code></td><td>Operator extends backend OR scales collector</td></tr><tr><td>Token rotation breaks service</td><td>Service ingress 401 spike</td><td>Sev-2 alarm; rollback rotation</td><td>Operator restores prior tokens</td></tr><tr><td>Grafana password missing</td><td>Boot fails</td><td>Grafana doesn't start</td><td>Operator sets GF_SECURITY_ADMIN_PASSWORD</td></tr><tr><td><code>tenant_id</code> resource attr missing</td><td>Span ingested without label</td><td>Logs/traces unsearchable by tenant</td><td>Caller MUST set resource attr (FR-AI-022)</td></tr><tr><td>File_storage corruption</td><td>Replay fails</td><td>Buffered data lost</td><td>Operator deletes corrupt buffer; restart</td></tr><tr><td>Collector config syntax error</td><td>Boot fails</td><td>Container restart loop</td><td>Operator fixes YAML; redeploy</td></tr><tr><td>Backend version drift (Loki upgraded but config incompatible)</td><td>Boot fails on backend</td><td>Backend down; collector buffers</td><td>Operator pins versions in compose</td></tr><tr><td>Network partition between collector and backend</td><td>Export retries; eventually drops</td><td>Sev-2 alarm</td><td>Operator investigates network</td></tr><tr><td>Disk-full on otel-buffer volume</td><td>file_storage write fails</td><td>Data dropped</td><td>Extend volume</td></tr><tr><td>Large span (&gt; default 1MB)</td><td>Collector truncates</td><td>Truncation logged</td><td>Caller checks span size; chunk if needed</td></tr><tr><td>Slow backend write</td><td>Export latency histogram alarm</td><td>Sev-3</td><td>Operator investigates backend; consider scaling</td></tr><tr><td>Rate of spans &gt; collector throughput</td><td>Backpressure to OTLP receivers</td><td>Senders block briefly</td><td>Auto-scale collector OR reduce sample rate</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>Use OTel Collector Contrib (not core) for the Loki + Tempo exporters + bearertokenauth + file_storage extensions.</li><li>Auth tokens are per-service. Rotation cadence 90 days via <code>scripts/rotate_tokens.sh</code>. The script pushes new tokens to each service's secret store; services pick up via standard env-var/secret reload.</li><li>Grafana admin password is set via env var <code>GRAFANA_ADMIN_PASSWORD</code>; never committed to git. Initial password set during operator's first deploy.</li><li>The PII-scrub processor is defence-in-depth. Caller-side prevention (FR-AI-022 §1 #15 typed attribute keys + AST lint) is the primary control. The scrub catches regressions; sev-1 alarm forces investigation.</li><li>File_storage extension persists collector buffer to disk (1GB cap). Survives restart; tradeoff is disk I/O on every batch (mitigated by 1s timeout). Without it, restart loses 5-30s of in-flight telemetry.</li><li>Slice 1 = single-instance backends. HA (peering for Loki/Prometheus/Tempo) is slice 5+ work. At our scale (~25K spans/sec), single instance is fine.</li><li>Retention floors (30d/90d/7d) reflect operational + compliance lookback windows. P2 extends with S3 backend (Loki) + Mimir (Prometheus) + sampled Tempo (FR-OBS-006).</li><li>Tenant_id resource attribute is the FR-OBS-002 query primitive — without it, tenant-aware filtering is impossible. FR-AI-022 + every CyberOS service MUST emit it.</li><li>The OTel collector is itself observable — its self-metrics + self-logs go to Prometheus + Loki via the same pipeline. Operators monitor \"is the collector dropping?\" via Grafana dashboard backed by <code>obs_collector_dropped_total</code>.</li></ul>\n<hr>\n<p><em>End of FR-OBS-001. Status: draft (10/10 target).</em></p>";
