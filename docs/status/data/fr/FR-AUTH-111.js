window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-111"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<ol><li>The OIDC callback <strong>MUST</strong> parse the person's name from the verified ID token, using the standard OpenID Connect claims: <code>name</code>, and failing that <code>given_name</code> + <code>family_name</code>, and failing that <code>preferred_username</code>.</li></ol>\n<ol><li>At JIT provisioning the service <strong>MUST</strong> bind <code>subjects.display_name</code> from that resolution chain, falling back in order to: the ID token's <code>name</code>; <code>given_name family_name</code> joined; <code>preferred_username</code>; the local-part of the email; and only then the full email address. It <strong>MUST NOT</strong> bind <code>display_name</code> directly from the email, which is what it does today (<code>oidc.rs</code> binds <code>idp_email.unwrap_or(\"\")</code> into the <code>display_name</code> column - the bug this FR exists to remove).</li></ol>\n<ol><li>The service <strong>MUST NOT</strong> invent, prettify, title-case, or otherwise transform a name it was given. If the IdP says the person is called <code>nguyenvana</code>, that is their name. Guessing at capitalisation or splitting on separators is how a product mangles Vietnamese names.</li></ol>\n<ol><li>On the <code>ON CONFLICT (tenant_id, handle) DO UPDATE</code> path - a returning SSO user - the service <strong>MUST</strong> refresh <code>display_name</code> <strong>only</strong> when the stored value was never really a name: that is, when it is <code>NULL</code>, empty, or byte-equal to the subject's email. It <strong>MUST NOT</strong> overwrite a <code>display_name</code> that differs from the email, because that value was set deliberately - by an administrator, by a future profile editor, or by a data repair - and silently reverting it on next login is worse than the original bug.</li></ol>\n<ol><li>Consequently the fix <strong>MUST</strong> be self-healing: every existing subject whose <code>display_name</code> is currently their email address acquires their real name the next time they sign in, with no migration and no administrator action.</li></ol>\n<ol><li>The same defect exists in <code>saml.rs</code>, which binds <code>display_name</code> the same way. It <strong>MUST</strong> be fixed in the same change, with the same resolution chain and the same no-clobber rule. Fixing one and not the other leaves a second door open and guarantees the bug is rediscovered.</li></ol>\n<ol><li>The migration <strong>MUST NOT</strong> attempt to reconstruct names for existing rows. There is no source of truth for them in our database - the ID token was discarded - and inventing a name from an email local-part would produce <code>van-anh.vu</code> where the person is called <code>Vũ Vân Anh</code>. §1 #5's self-healing path is the correct repair.</li></ol>\n<ol><li>The resolution <strong>MUST</strong> be logged at <code>debug</code> with the <em>rung of the chain that matched</em>, never with the name itself. <code>tracing::debug!(rung = \"given_name+family_name\")</code> is a diagnostic; <code>tracing::debug!(?name)</code> puts personal data in the log stream, which the privacy policy says we do not do.</li></ol>\n<ol><li>No claim beyond those in §1 #1 <strong>MAY</strong> be persisted. In particular the <code>picture</code> claim <strong>MUST NOT</strong> be stored: the published privacy policy enumerates what we collect, and adding a field to <code>subjects</code> is a change to that enumeration, not an implementation detail. If we want avatars, that is a separate FR <em>and</em> a privacy-policy revision, in that order.</li></ol>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why this is a defect and not a cosmetic issue.</strong> <code>oidc.rs</code> line ~965 binds the email into the <code>display_name</code> column. Every human provisioned by Google SSO therefore renders as <code>van-anh.vu@cyberskill.world</code> wherever a name belongs: in the channel list, above every message they have ever sent, in mentions, in the member picker, and in any screenshot or export. It reads as broken software. It also means the address is displayed in contexts where only a name was intended - a small but real leak, and one that has already forced a manual <code>UPDATE</code> against production to make a store screenshot presentable.</p>\n<p><strong>Why the code contradicts the published policy.</strong> <code>cyberskill.world/en/cyberos/privacy</code> says, in terms: \"When you sign in with Google we receive your name, your email address, and your Google account identifier.\" We do receive the name. We then drop it on the floor and store the email in its place. The policy is not wrong about what Google sends us; the code is wrong about what we do with it. Aligning them is the whole of this FR.</p>\n<p><strong>Why the no-clobber rule (§1 #4) is a MUST and not a nicety.</strong> The naive fix - always refresh <code>display_name</code> from the ID token on login - would work today and break the moment anything else can set a name. An administrator who fixes a colleague's name by hand, or a future profile editor, would watch their change silently revert on that person's next sign-in, with no error and no trace. Refreshing <em>only</em> the sentinel value (null, empty, or exactly the email) is the narrow rule that repairs the damage without acquiring the authority to undo a human's decision.</p>\n<p><strong>Why not backfill (§1 #7).</strong> Because the information is gone. We can see that <code>display_name = email</code>, so we can see <em>which</em> rows are wrong, but we cannot see what they should say - the ID token that carried the name was never persisted. The only honest sources are the IdP (which means a login) or a human (which means typing). Deriving <code>Van Anh Vu</code> from <code>van-anh.vu@</code> looks like a fix and is a guess, and it guesses wrong on exactly the names that matter most to a Vietnamese company. Self-healing on next login is slower and correct.</p>\n<p><strong>Why <code>picture</code> is explicitly excluded (§1 #9).</strong> It is right there in the ID token, it would make the product nicer, and adding it would quietly make the published Data Safety declaration false. The store form and the policy both enumerate what we collect. Widening that set is a decision with paperwork attached, not a line of code.</p>\n<h2>§3 - API contract</h2>\n<h3>The claims we read</h3>\n<pre><code class=\"language-rust\">// services/auth/src/oidc.rs\n\n/// Standard OIDC claims. We read only what §1 #1 names. `picture` is deliberately absent:\n/// adding it here would silently widen the Data Safety declaration (§1 #9).\n#[derive(Debug, Deserialize)]\nstruct IdTokenProfile {\n    #[serde(default)] name: Option&lt;String&gt;,\n    #[serde(default)] given_name: Option&lt;String&gt;,\n    #[serde(default)] family_name: Option&lt;String&gt;,\n    #[serde(default)] preferred_username: Option&lt;String&gt;,\n}</code></pre>\n<h3>The resolution chain</h3>\n<pre><code class=\"language-rust\">/// §1 #2. Ordered, total, and deliberately dumb: no transformation of any value it is handed (§1 #3).\n/// Returns the rung that matched alongside the value, so the caller can log the rung without logging\n/// the name (§1 #8).\nfn resolve_display_name&lt;'a&gt;(\n    p: &amp;'a IdTokenProfile,\n    email: Option&lt;&amp;'a str&gt;,\n) -&gt; (&amp;'static str, String) {\n    if let Some(n) = p.name.as_deref().map(str::trim).filter(|s| !s.is_empty()) {\n        return (\"name\", n.to_string());\n    }\n    let given  = p.given_name.as_deref().map(str::trim).filter(|s| !s.is_empty());\n    let family = p.family_name.as_deref().map(str::trim).filter(|s| !s.is_empty());\n    match (given, family) {\n        (Some(g), Some(f)) =&gt; return (\"given_name+family_name\", format!(\"{g} {f}\")),\n        (Some(g), None)    =&gt; return (\"given_name\", g.to_string()),\n        (None, Some(f))    =&gt; return (\"family_name\", f.to_string()),\n        (None, None)       =&gt; {}\n    }\n    if let Some(u) = p.preferred_username.as_deref().map(str::trim).filter(|s| !s.is_empty()) {\n        return (\"preferred_username\", u.to_string());\n    }\n    match email {\n        Some(e) if e.contains('@') =&gt; (\"email_local_part\", e.split('@').next().unwrap().to_string()),\n        Some(e)                    =&gt; (\"email\", e.to_string()),\n        None                       =&gt; (\"handle\", String::new()),   // caller substitutes the handle\n    }\n}</code></pre>\n<h3>The JIT insert</h3>\n<pre><code class=\"language-rust\">let (rung, display_name) = resolve_display_name(&amp;profile, idp_email);\ntracing::debug!(target: \"cyberos_auth::oidc\", rung, \"resolved display_name\");   // §1 #8 - rung, not name\n\nlet row: (Uuid,) = sqlx::query_as(\n    \"INSERT INTO subjects (tenant_id, handle, display_name, email, kind, status, password_hash, roles)\n          VALUES ($1, $2, $3, $4, 'human', 'active', $5, $6)\n     ON CONFLICT (tenant_id, handle) DO UPDATE\n        SET email = COALESCE(EXCLUDED.email, subjects.email),\n            -- §1 #4: refresh ONLY a display_name that was never really set. A value that differs\n            -- from the email was put there on purpose; do not silently revert someone's decision.\n            display_name = CASE\n                WHEN subjects.display_name IS NULL\n                  OR subjects.display_name = ''\n                  OR subjects.display_name = subjects.email\n                THEN EXCLUDED.display_name\n                ELSE subjects.display_name\n            END,\n            updated_at = NOW()\n   RETURNING id\",\n)\n.bind(idp.tenant_id)\n.bind(&amp;handle)\n.bind(&amp;display_name)          // &lt;- was: idp_email.unwrap_or(\"\")   THE BUG\n.bind(idp_email)\n.bind(&amp;sso_password_hash)\n.bind(&amp;idp.default_roles)\n.fetch_one(&amp;mut *tx)\n.await\n.map_err(internal)?;</code></pre>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0026_sso_display_name_backfill.sql\n-- FR-AUTH-111. There is deliberately NO backfill of display_name here (§1 #7): the ID token that\n-- carried the person's name was never persisted, so we can see WHICH rows are wrong but not what\n-- they should say. Deriving \"Van Anh Vu\" from \"van-anh.vu@\" is a guess, and it guesses wrong on\n-- exactly the names that matter most to a Vietnamese company. Every affected subject self-heals on\n-- their next sign-in via the ON CONFLICT rule.\n--\n-- What this migration DOES is make the damage visible and measurable, so we can watch it drain.\n\nCREATE OR REPLACE VIEW subjects_display_name_unset AS\n    SELECT id, tenant_id, handle, email, created_at\n      FROM subjects\n     WHERE kind = 'human'\n       AND email IS NOT NULL\n       AND (display_name IS NULL OR display_name = '' OR display_name = email);\n\nCOMMENT ON VIEW subjects_display_name_unset IS\n    'FR-AUTH-111: humans still wearing their email as a display name. Drains to zero as people sign in.';\n\nGRANT SELECT ON subjects_display_name_unset TO cyberos_ro;</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong><code>name</code> wins</strong> - an ID token carrying <code>name: \"Trịnh Thái Anh\"</code> provisions a subject whose <code>display_name</code> is exactly <code>Trịnh Thái Anh</code>, diacritics intact, untransformed.</li><li><strong><code>given_name</code> + <code>family_name</code> is the second rung</strong> - a token with no <code>name</code> but both parts provisions <code>\"&lt;given&gt; &lt;family&gt;\"</code>.</li><li><strong>A single part is used alone</strong> - a token with only <code>given_name</code> provisions that value, not an empty-padded join.</li><li><strong><code>preferred_username</code> is the third rung</strong> - used when no name claims are present.</li><li><strong>The email local-part is the fourth rung</strong> - a token with no name claims at all and <code>email: a.b@c.com</code> provisions <code>a.b</code>, never <code>a.b@c.com</code>.</li><li><strong><code>display_name</code> is never the full email</strong> - across every rung, no provisioned subject has <code>display_name = email</code>.</li><li><strong>A returning user with an email as their name is repaired</strong> - a subject stored with <code>display_name = email</code> acquires their real name on the next sign-in, with no migration.</li><li><strong>A deliberately set name is never clobbered</strong> - a subject whose <code>display_name</code> was set to <code>\"Play Review\"</code> by hand keeps it across sign-ins, even though the ID token would resolve to something else.</li><li><strong>A blank ID-token name does not blank the stored name</strong> - a token whose <code>name</code> is <code>\"\"</code> or whitespace falls through the chain and never writes an empty <code>display_name</code>.</li><li><strong>The name is never logged</strong> - no log line at any level contains the resolved name; the <code>debug</code> line contains only the rung that matched.</li><li><strong><code>picture</code> is not persisted</strong> - no column receives it, and the <code>subjects</code> row after a Google sign-in has no avatar data.</li><li><strong>SAML is fixed identically</strong> - the same eight assertions hold against <code>saml.rs</code>'s provisioning path.</li><li><strong>The visibility view drains</strong> - <code>subjects_display_name_unset</code> returns a row for an affected subject before their next sign-in and no row after it.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/oidc_jit.rs\n\n#[tokio::test]\nasync fn the_resolution_chain_walks_in_order() {                     // AC 1-5, 9\n    assert_eq!(resolve(tok().name(\"Trịnh Thái Anh\")).1, \"Trịnh Thái Anh\");\n    assert_eq!(resolve(tok().given(\"Thái Anh\").family(\"Trịnh\")).1, \"Thái Anh Trịnh\");\n    assert_eq!(resolve(tok().given(\"Thái Anh\")).1, \"Thái Anh\");\n    assert_eq!(resolve(tok().preferred(\"stephen\")).1, \"stephen\");\n    assert_eq!(resolve(tok().email(\"van-anh.vu@cyberskill.world\")).1, \"van-anh.vu\");\n    // whitespace-only claims fall through rather than writing a blank\n    assert_eq!(resolve(tok().name(\"   \").email(\"a.b@c.com\")).1, \"a.b\");\n}\n\n#[tokio::test]\nasync fn display_name_is_never_the_full_email() {                    // AC 6 - the bug, pinned\n    let app = harness().await;\n    for tok in [tok().name(\"A B\"), tok().preferred(\"ab\"), tok().email(\"a.b@c.com\")] {\n        let s = app.sso_login(tok.email(\"a.b@c.com\")).await;\n        assert_ne!(s.display_name, s.email.unwrap(),\n                   \"display_name must never be bound from the email column\");\n    }\n}\n\n#[tokio::test]\nasync fn a_returning_user_self_heals_but_a_set_name_survives() {     // AC 7, 8 - the no-clobber rule\n    let app = harness().await;\n\n    // Damaged row, exactly as production has them today.\n    let s = app.seed_subject(display_name = \"van-anh.vu@cyberskill.world\",\n                             email        = \"van-anh.vu@cyberskill.world\").await;\n    app.sso_login(tok().sub(s.idp_sub).name(\"Vũ Vân Anh\")).await;\n    assert_eq!(app.subject(s.id).await.display_name, \"Vũ Vân Anh\");   // repaired\n\n    // Deliberately set row: an admin fixed this by hand. It must survive.\n    let p = app.seed_subject(display_name = \"Play Review\",\n                             email        = \"play-review@cyberskill.world\").await;\n    app.sso_login(tok().sub(p.idp_sub).name(\"play-review\")).await;\n    assert_eq!(app.subject(p.id).await.display_name, \"Play Review\");  // NOT clobbered\n}\n\n#[tokio::test]\nasync fn the_name_never_reaches_the_log_stream() {                   // AC 10\n    let logs = capture_logs(Level::DEBUG, || async {\n        harness().await.sso_login(tok().name(\"Trịnh Thái Anh\")).await;\n    }).await;\n    assert!(logs.contains(r#\"rung=\"name\"\"#));\n    assert!(!logs.contains(\"Trịnh Thái Anh\"), \"personal data must not reach the log stream\");\n}\n\n#[tokio::test]\nasync fn picture_is_not_persisted() {                                // AC 11\n    let app = harness().await;\n    let s = app.sso_login(tok().name(\"A B\").picture(\"https://…/photo.jpg\")).await;\n    let raw = app.raw_subject_row(s.id).await;\n    assert!(!raw.to_string().contains(\"photo.jpg\"));\n    assert!(!raw.to_string().contains(\"picture\"));\n}\n\n#[tokio::test]\nasync fn saml_is_fixed_too() {                                       // AC 12\n    let app = harness().await;\n    let s = app.saml_login(assertion().display_name(\"Trịnh Thái Anh\")\n                                      .email(\"thai-anh.trinh@cyberskill.world\")).await;\n    assert_eq!(s.display_name, \"Trịnh Thái Anh\");\n}\n\n#[tokio::test]\nasync fn the_visibility_view_drains() {                              // AC 13\n    let app = harness().await;\n    let s = app.seed_subject(display_name = \"a.b@c.com\", email = \"a.b@c.com\").await;\n    assert_eq!(app.view_rows(\"subjects_display_name_unset\").await.len(), 1);\n    app.sso_login(tok().sub(s.idp_sub).name(\"A B\")).await;\n    assert_eq!(app.view_rows(\"subjects_display_name_unset\").await.len(), 0);\n}</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>(§3 is the skeleton. The change is three lines of binding, one <code>CASE</code> in the upsert, one helper, and the same again in <code>saml.rs</code>.)</p>\n<h2>§7 - Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none. <code>oidc.rs</code> already verifies the ID token before this code runs; we are reading claims from an already-verified token, not adding a trust boundary.</li><li><strong>Downstream:</strong> FR-CHAT-269's moderation queue renders reporter and reported names; it inherits the fix for free.</li><li><strong>Policy:</strong> <code>cyberskill.world/en/cyberos/privacy</code> already declares that we receive the person's name. This FR makes the code match the declaration. Any <em>widening</em> of what we persist - <code>picture</code>, <code>locale</code>, <code>hd</code> - is a policy change first and a code change second (§1 #9).</li></ul>\n<h2>§8 - Example payloads</h2>\n<p>The Google ID token, as verified today:</p>\n<pre><code class=\"language-json\">{\n  \"iss\": \"https://accounts.google.com\",\n  \"sub\": \"104…\",\n  \"email\": \"thai-anh.trinh@cyberskill.world\",\n  \"email_verified\": true,\n  \"name\": \"Trịnh Thái Anh\",\n  \"given_name\": \"Thái Anh\",\n  \"family_name\": \"Trịnh\",\n  \"picture\": \"https://lh3.googleusercontent.com/…\"\n}</code></pre>\n<p>The subject row today (the bug):</p>\n<pre><code class=\"language-json\">{ \"handle\": \"@thai-anh.trinh\",\n  \"display_name\": \"thai-anh.trinh@cyberskill.world\",\n  \"email\": \"thai-anh.trinh@cyberskill.world\" }</code></pre>\n<p>The subject row after this FR:</p>\n<pre><code class=\"language-json\">{ \"handle\": \"@thai-anh.trinh\",\n  \"display_name\": \"Trịnh Thái Anh\",\n  \"email\": \"thai-anh.trinh@cyberskill.world\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<p><strong>Deferred:</strong></p>\n<ul><li><em>Avatars.</em> The <code>picture</code> claim is right there and the product would look better for it. It is deliberately out of scope (§1 #9): persisting it widens the published Data Safety declaration, so it needs a privacy-policy revision and a Play Data Safety update before a line of code. Its own FR.</li><li><em>A profile editor.</em> Once people can set their own display name, §1 #4's no-clobber rule is what stops SSO from reverting it. The rule is written now precisely so the editor is additive later.</li><li><em>Handle derivation.</em> <code>@oidc-&lt;sub prefix&gt;</code> for a subject with no email is unpleasant but out of scope here; this FR touches <code>display_name</code> only.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>display_name</code> bound from the email column</td><td>AC 6 asserts <code>display_name != email</code> on every rung</td><td>The defect in production today</td><td>The bind is changed; the AC fails if anyone reverts it</td></tr><tr><td>SSO overwrites a hand-set name on next login</td><td>AC 8 seeds a deliberately set name and signs in</td><td>An admin's repair silently reverts, with no error</td><td><code>CASE</code> refreshes only null / empty / equal-to-email</td></tr><tr><td>A blank <code>name</code> claim writes an empty display name</td><td>AC 9; every rung trims and filters empty</td><td>A person renders as nothing at all</td><td>The chain falls through on whitespace</td></tr><tr><td>Backfill invents a wrong name</td><td>§1 #7; no backfill exists</td><td><code>Van Anh Vu</code> where the person is <code>Vũ Vân Anh</code></td><td>The migration creates a <em>view</em>, not an <code>UPDATE</code></td></tr><tr><td>Name written to the log stream</td><td>AC 10 greps captured logs for the name</td><td>Personal data in logs, contradicting the privacy policy</td><td>The <code>debug</code> line carries the rung only</td></tr><tr><td><code>picture</code> quietly persisted</td><td>AC 11 greps the raw row</td><td>The published Data Safety declaration becomes false</td><td><code>IdTokenProfile</code> has no <code>picture</code> field to deserialise into</td></tr><tr><td>SAML left broken</td><td>AC 12 runs the same assertions against <code>saml.rs</code></td><td>The bug is rediscovered through the other door</td><td>Both call sites are changed in one commit</td></tr><tr><td>Diacritics mangled by a \"helpful\" transformation</td><td>AC 1 asserts byte equality on <code>Trịnh Thái Anh</code></td><td>Vietnamese names corrupted - the worst possible failure for this company</td><td>§1 #3 forbids transformation; the resolver copies</td></tr><tr><td>A future contributor \"simplifies\" the CASE to an unconditional SET</td><td>AC 8 fails</td><td>The no-clobber guarantee is lost</td><td>Pinned by an assertion, not a comment</td></tr><tr><td>Subject has no email and no name claims</td><td>Chain's final rung returns empty; caller substitutes the handle</td><td>The person renders as their handle, not as blank</td><td>Explicit final arm in <code>resolve_display_name</code></td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li><strong>Why the resolver returns the rung as well as the value.</strong> So the caller can log <em>how</em> it resolved without logging <em>what</em> it resolved. Observability into a name-resolution bug is close to worthless without knowing which rung fired, and a <code>?name</code> in a debug line is a privacy incident. Returning <code>(&amp;'static str, String)</code> gets both properties for free.</li></ul>\n<ul><li><strong>Why <code>subjects.display_name = subjects.email</code> is the sentinel for \"never set\".</strong> Because it is exactly the value the bug wrote, and no human would ever choose it. It is a slightly ugly heuristic and it is the right one: the alternative - a nullable <code>display_name_source</code> column - is a schema change to fix a three-line bug, and it would need its own backfill.</li></ul>\n<ul><li><strong>Why the migration ships a view rather than doing nothing.</strong> The self-healing repair is invisible: there is no way to know whether it worked, or how many people are still affected, without a query. <code>subjects_display_name_unset</code> makes the damage countable, so the repair can be watched draining to zero rather than assumed.</li></ul>\n<ul><li><strong><code>format!(\"{g} {f}\")</code> and not a locale-aware ordering.</strong> Vietnamese convention puts the family name first, and <code>given_name</code>/<code>family_name</code> from Google are already ordered per the account's own locale in the <code>name</code> claim - which is why <code>name</code> is the first rung and the join is only a fallback. Building a locale-aware name formatter to serve the fallback path would be a large amount of code guarding an edge, and it would still be wrong for someone whose IdP disagrees with their culture. The <code>name</code> claim is the person's own answer; prefer it.</li></ul>\n<p><em>End of FR-AUTH-111.</em></p>";
