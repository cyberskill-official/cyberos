window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EVAL-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The rubric layer <strong>MUST</strong> turn the three signed employment documents into a structured, versioned, clause-cited framework that FR-EVAL-003 can evaluate recorded evidence against, with a human approving every item before it is effective. The contract:</p>\n<ol><li><strong>MUST</strong> define three tables — <code>rubric</code>, <code>rubric_version</code>, <code>rubric_item</code> — in <code>services/eval/migrations/0002_rubric.sql</code>, all tenant-scoped (<code>tenant_id UUID NOT NULL</code>) and all under RLS (FR-AUTH-003). A <code>rubric</code> is the named framework for a tenant (e.g. \"CyberSkill employment rubric\"); a <code>rubric_version</code> is one immutable, effective-dated published cut of it; a <code>rubric_item</code> is one checkable obligation, working term, KPI, or milestone within a version.</li></ol>\n<ol><li><strong>MUST</strong> make every <code>rubric_item</code> cite its exact source in the signed documents (DEC-2600):</li></ol>\n<ul><li><code>source_doc TEXT NOT NULL CHECK (source_doc IN ('labor_contract','nda_ip','total_rewards_appendix'))</code> — the closed set of the three documents and nothing else.</li><li><code>clause_ref TEXT NOT NULL</code> — the exact clause/article identifier within that document (e.g. <code>art.3.1</code>, <code>§5(b)</code>, <code>appendix-B.KPI-2</code>).</li><li><code>source_quote_vi TEXT</code> and <code>source_quote_en TEXT</code> — the verbatim clause text in each language, so a reviewer and the employee can read the standard at its source.</li><li>An INSERT or update that leaves <code>source_doc</code> or <code>clause_ref</code> null <strong>MUST</strong> be rejected (<code>422 rubric_item_uncited</code>); an item that cannot name its clause is not a rubric item.</li></ul>\n<ol><li><strong>MUST</strong> classify each item by what kind of thing it checks:</li></ol>\n<ul><li><code>item_kind TEXT NOT NULL CHECK (item_kind IN ('obligation','working_term','kpi','career_milestone'))</code>.</li><li>For <code>item_kind='obligation'</code>, a further <code>obligation_kind TEXT CHECK (obligation_kind IN ('confidentiality','non_compete','ip_assignment'))</code> — the three NDA obligation families called out in the brain-evaluation plan. Non-obligation items leave <code>obligation_kind</code> null.</li></ul>\n<ol><li><strong>MUST</strong> carry, per item, a machine-usable check descriptor that FR-EVAL-003 consumes:</li></ol>\n<ul><li><code>check_type TEXT NOT NULL CHECK (check_type IN ('evidence_presence','threshold_numeric','attestation','periodic_review','milestone_reached'))</code> — how the item is evaluated (is there evidence of X; is a number at/above a threshold; is there a signed attestation; was a review done on cadence; was a milestone reached).</li><li><code>check_params JSONB NOT NULL DEFAULT '{}'</code> — typed parameters for the check (e.g. <code>{\"metric\":\"on_time_delivery\",\"operator\":\"&gt;=\",\"target\":0.9}</code> for a <code>threshold_numeric</code> KPI). The schema of <code>check_params</code> is keyed by <code>check_type</code>; an unknown shape for a given <code>check_type</code> <strong>MUST</strong> be rejected at write time.</li><li><code>weight NUMERIC(5,2) NOT NULL CHECK (weight &gt;= 0)</code> — the item's contribution to its section's roll-up. Weights are relative within a version, not a promise of a global 0..100.</li></ul>\n<ol><li><strong>MUST</strong> be fully bilingual VN/EN (the documents are bilingual, dated 2026-01-01, under Labor Code 45/2019/QH14 + Decree 145/2020). Every human-facing item field <strong>MUST</strong> exist as a <code>_vi</code> / <code>_en</code> pair: <code>title_vi</code>/<code>title_en</code>, <code>description_vi</code>/<code>description_en</code>, plus the <code>source_quote_vi</code>/<code>source_quote_en</code> of clause 2. Vietnamese is the primary, legally-operative text; English is the working translation. An item missing the <code>_vi</code> side <strong>MUST</strong> be rejected (<code>422 rubric_item_missing_vi</code>).</li></ol>\n<ol><li><strong>MUST</strong> version the rubric and treat a published version as immutable (DEC-2603):</li></ol>\n<ul><li><code>rubric_version</code> carries <code>version_no INT</code>, <code>state TEXT CHECK (state IN ('draft','approved','published','superseded'))</code>, <code>effective_from DATE</code>, <code>effective_to DATE</code> (null = open-ended), <code>published_at_ns BIGINT</code>, <code>published_by_subject_id UUID</code>.</li><li>Once <code>state='published'</code>, the version's items and its <code>effective_from</code> <strong>MUST NOT</strong> be mutated. Re-curation (a contract amendment, a corrected reading) creates a NEW <code>rubric_version</code> with <code>version_no + 1</code>; the prior version is moved to <code>superseded</code> and its <code>effective_to</code> is set. A migration <strong>MUST</strong> <code>REVOKE UPDATE, DELETE</code> on published <code>rubric_version</code> / <code>rubric_item</code> rows from the <code>cyberos_app</code> role.</li></ul>\n<ol><li><strong>MUST</strong> resolve, for any date, the single version that was effective then: <code>resolve_effective(rubric_id, at_date) → rubric_version</code> returns the published version whose <code>[effective_from, effective_to)</code> contains <code>at_date</code>. FR-EVAL-003 calls this so an assessment for a period uses the standard that was actually in force then, not whatever is current.</li></ol>\n<ol><li><strong>MUST</strong> enforce the human-approval (HITL) gate before any item or version becomes effective (DEC-2602):</li></ol>\n<ul><li>GENIE/Lumi MAY create items in <code>state='draft'</code> only (clause 9), and MAY suggest edits, but <strong>MUST NOT</strong> approve or publish.</li><li>A <code>rubric_version</code> transition to <code>approved</code> or <code>published</code> <strong>MUST</strong> record a human <code>approver_subject_id UUID NOT NULL</code> and an <code>approved_at_ns</code>. A transition with a null or service-account approver <strong>MUST</strong> be rejected (<code>403 rubric_requires_human_approver</code>).</li><li>Only an item that belongs to a <code>published</code> version is visible to FR-EVAL-003; a <code>draft</code>/<code>approved</code>-but-unpublished item <strong>MUST NOT</strong> be evaluable.</li></ul>\n<ol><li><strong>MUST</strong> support a GENIE DRAFT path that proposes items from the three documents without ever setting the standard (DEC-2602):</li></ol>\n<ul><li><code>draft_genie.rs</code> calls Lumi (the FR-EVAL-001-governed analysis path) to read the three documents and propose <code>rubric_item</code> rows — each pre-filled with a candidate <code>source_doc</code>, <code>clause_ref</code>, <code>item_kind</code>, <code>check_type</code>, and bilingual title/description — into <code>state='draft'</code>.</li><li>Every GENIE-drafted item <strong>MUST</strong> carry <code>authored_by = 'genie'</code> and a <code>genie_confidence NUMERIC(4,3)</code>; a human-authored item carries <code>authored_by = 'human'</code>. The provenance is never erased on later human edit (an <code>edited_by_subject_id</code> is added; <code>authored_by</code> stays).</li><li>A GENIE draft that cannot ground an item in a specific clause <strong>MUST</strong> leave it flagged <code>needs_clause_ref</code> rather than inventing a citation; the human supplies the clause before approval. The model <strong>MUST NOT</strong> fabricate a <code>clause_ref</code> that is not in the document.</li></ul>\n<ol><li><strong>MUST</strong> be access-gated by the FR-EVAL-001 governance layer, not by an access rule this FR invents (DEC-2601). Authoring (create/edit/approve/publish) <strong>MUST</strong> require the FR-EVAL-001 grant for rubric administration (founder + designated rubric admins); reading the rubric <strong>MUST</strong> require a valid grant. The rubric tables <strong>MUST NOT</strong> ship before FR-EVAL-001 exists.</li></ol>\n<ol><li><strong>MUST</strong> emit one hash-chained memory audit row per rubric mutation through <code>services/eval/src/audit.rs</code> (DEC-2604): <code>eval.rubric_drafted</code>, <code>eval.rubric_edited</code>, <code>eval.rubric_approved</code>, <code>eval.rubric_published</code>, <code>eval.rubric_superseded</code>. Each payload carries <code>{rubric_id, rubric_version_id, version_no, item_id?, actor_subject_id, authored_by?, source_doc?, clause_ref?, trace_id}</code>. These chain into the same <code>l1_audit_log</code> the rest of CyberOS uses (FR-PROJ-008 / FR-MEMORY-123 pattern), so the rubric's full curation history is tamper-evident.</li></ol>\n<ol><li><strong>MUST</strong> expose a minimal authoring + read API, tenant-scoped and FR-EVAL-001-gated:</li></ol>\n<ul><li><code>POST /v1/eval/rubrics/{id}/versions</code> — open a new draft version.</li><li><code>POST /v1/eval/rubrics/{id}/versions/{vid}/items</code> — add/edit a draft item (clause-citation + bilingual + check-shape validated).</li><li><code>POST /v1/eval/rubrics/{id}/versions/{vid}/draft-from-documents</code> — trigger the GENIE draft path (clause 9), returns the proposed items in <code>draft</code>.</li><li><code>POST /v1/eval/rubrics/{id}/versions/{vid}/approve</code> and <code>.../publish</code> — the HITL transitions (human approver required).</li><li><code>GET /v1/eval/rubrics/{id}?at=&lt;date&gt;</code> — read the version effective on a date (defaults to today), with all items and their citations.</li></ul>\n<ol><li><strong>MUST</strong> validate, at publish time, that the version is coherent before it can become the standard:</li></ol>\n<ul><li>Every item is clause-cited (clause 2), bilingual (clause 5), and has a check shape valid for its <code>check_type</code> (clause 4).</li><li><code>effective_from</code> does not overlap an existing published version's open interval for the same <code>rubric</code> (no two versions effective on the same day).</li><li>At least one item exists. Publishing an empty version <strong>MUST</strong> be rejected (<code>422 rubric_version_empty</code>). A publish that fails any check <strong>MUST</strong> return the specific failure and leave the version unpublished.</li></ul>\n<ol><li><strong>MUST</strong> keep the rubric independent of any one person — a rubric and its versions describe the <em>standard</em>, not an assessment of an individual. Per-person scoring lives in FR-EVAL-003 and references <code>rubric_version_id</code>; this FR <strong>MUST NOT</strong> store any per-employee score, evidence, or assessment. (Separation of the standard from the judgement is what lets the same version fairly measure everyone.)</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics: <code>eval_rubric_items_total{item_kind,source_doc}</code> (gauge of items in the current published version), <code>eval_rubric_publishes_total{result}</code> (counter; result ∈ ok | rejected), and <code>eval_rubric_genie_drafts_total{outcome}</code> (counter; outcome ∈ proposed | needs_clause_ref).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why clause citation per item (DEC-2600, §1 #2)?</strong> The whole point of building the rubric from the three signed documents is that an evaluation can be defended by pointing at the exact clause it measured against. A rubric item that says \"maintains confidentiality\" with no <code>source_doc</code>/<code>clause_ref</code> is an opinion; one that cites <code>nda_ip / §4.2</code> with the verbatim text is a measurable obligation the employee agreed to. Rejecting an uncited item at write time keeps the rubric honest — it can only ever assert what a signed document says.</p>\n<p><strong>Why versioned + immutable + effective-dated (DEC-2603, §1 #6 #7)?</strong> Contracts change. If the rubric were mutable config, amending the Total Rewards appendix in July would silently rewrite the standard that the January–June assessments were measured against, and last quarter's review would no longer mean what it said. Immutable published versions plus <code>resolve_effective(at)</code> mean every assessment is anchored to the standard that was actually in force then, and the history stays interpretable forever.</p>\n<p><strong>Why the human-approval gate, with GENIE only drafting (DEC-2602, §1 #8 #9)?</strong> Lumi reading a legal clause is a fast first draft, not a ruling. Letting the model's reading become the operative standard with no human sign-off would mean the system sets the bar it later scores people against — the exact thing the brain-evaluation plan's \"human in the loop\" principle forbids. GENIE proposes into <code>draft</code>; a human approves into <code>published</code>. The model accelerates the curation; it never owns it.</p>\n<p><strong>Why GENIE must not fabricate a clause_ref (§1 #9)?</strong> A model asked to cite a clause will, if unconstrained, produce a plausible-looking citation that may not exist in the document — the same failure mode seen in the obs-triage local-model proof (it invented a runbook URL by copying an example). The <code>needs_clause_ref</code> flag forces the human to supply the real citation rather than rubber-stamping an invented one. A rubric grounded in fabricated citations is worse than no rubric.</p>\n<p><strong>Why bilingual VN/EN with Vietnamese primary (§1 #5)?</strong> The employment documents are bilingual and governed by Vietnamese law; the Vietnamese text is the legally-operative one. Storing both, with <code>_vi</code> required, means the standard is readable to the Vietnamese-speaking team in the authoritative language and to others in the working translation, and an assessment can show the employee the clause in the language they signed.</p>\n<p><strong>Why separate the standard from the judgement (§1 #14)?</strong> A rubric describes what \"good\" and \"compliant\" mean for everyone; an assessment is one person measured against it. Keeping per-person scoring out of this FR (it lives in FR-EVAL-003, referencing <code>rubric_version_id</code>) is what makes the rubric fair — the same version measures everyone, and changing the standard for one person is structurally impossible.</p>\n<p><strong>Why governance-first dependency on FR-EVAL-001 (DEC-2601, §1 #10)?</strong> The rubric is the spine of a system that affects pay and progression. Building it before the consent, access, and retention layer exists would mean authoring the most sensitive artifact in the platform with no access control and no disclosed basis. FR-EVAL-001 is a hard <code>depends_on</code>, and rubric reads/writes are gated by its grants, so the rubric never exists outside the governance frame.</p>\n<p><strong>Why audit every mutation (DEC-2604, §1 #11)?</strong> The rubric's curation history is itself evidence — who proposed an item, who approved it, when it was published, when it was superseded. Hash-chaining each mutation into <code>l1_audit_log</code> (the FR-PROJ-008 / FR-MEMORY-123 pattern) means a reviewer can later reconstruct exactly how the standard came to be and prove it was not quietly altered.</p>\n<p><strong>Why publish-time coherence checks (§1 #13)?</strong> A version becomes the standard the moment it is published; a half-finished version (an uncited item, a missing translation, an overlapping effective date) would corrupt every assessment that runs against it. Validating coherence at the publish boundary — not at every keystroke — lets authors work freely in <code>draft</code> but guarantees that what becomes operative is complete.</p>\n<p><strong>Why a closed <code>check_type</code> enum (§1 #4)?</strong> FR-EVAL-003 has to know how to evaluate each item mechanically. A free-form check would push interpretation into the evaluation engine per item. A small closed set (evidence presence, numeric threshold, attestation, periodic review, milestone reached) covers the obligations, working terms, KPIs, and milestones in the three documents and keeps the evaluation engine's logic bounded and testable.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/eval/migrations/0002_rubric.sql\n\nCREATE TABLE rubric (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id       UUID NOT NULL,\n    name            TEXT NOT NULL,\n    created_at_ns   BIGINT NOT NULL,\n    UNIQUE (tenant_id, name)\n);\n\nCREATE TABLE rubric_version (\n    id                      UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    rubric_id               UUID NOT NULL REFERENCES rubric(id),\n    tenant_id               UUID NOT NULL,\n    version_no              INT  NOT NULL,\n    state                   TEXT NOT NULL CHECK (state IN ('draft','approved','published','superseded')),\n    effective_from          DATE,\n    effective_to            DATE,\n    approver_subject_id     UUID,                 -- human; required for approved/published\n    approved_at_ns          BIGINT,\n    published_by_subject_id UUID,\n    published_at_ns         BIGINT,\n    UNIQUE (rubric_id, version_no)\n);\n\nCREATE TABLE rubric_item (\n    id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    rubric_version_id UUID NOT NULL REFERENCES rubric_version(id),\n    tenant_id         UUID NOT NULL,\n    -- citation (DEC-2600): every item names its clause\n    source_doc        TEXT NOT NULL CHECK (source_doc IN ('labor_contract','nda_ip','total_rewards_appendix')),\n    clause_ref        TEXT NOT NULL,\n    source_quote_vi   TEXT,\n    source_quote_en   TEXT,\n    -- classification\n    item_kind         TEXT NOT NULL CHECK (item_kind IN ('obligation','working_term','kpi','career_milestone')),\n    obligation_kind   TEXT CHECK (obligation_kind IN ('confidentiality','non_compete','ip_assignment')),\n    -- check descriptor consumed by FR-EVAL-003\n    check_type        TEXT NOT NULL CHECK (check_type IN\n                      ('evidence_presence','threshold_numeric','attestation','periodic_review','milestone_reached')),\n    check_params      JSONB NOT NULL DEFAULT '{}',\n    weight            NUMERIC(5,2) NOT NULL CHECK (weight &gt;= 0),\n    -- bilingual (DEC, §1 #5): _vi is required, _en is the working translation\n    title_vi          TEXT NOT NULL,\n    title_en          TEXT,\n    description_vi    TEXT,\n    description_en    TEXT,\n    -- provenance (HITL, §1 #9)\n    authored_by       TEXT NOT NULL CHECK (authored_by IN ('human','genie')),\n    genie_confidence  NUMERIC(4,3),\n    needs_clause_ref  BOOLEAN NOT NULL DEFAULT false,\n    edited_by_subject_id UUID\n);\n\nCREATE INDEX idx_rubric_version_effective ON rubric_version (rubric_id, effective_from, effective_to);\nCREATE INDEX idx_rubric_item_version       ON rubric_item (rubric_version_id);\n\nALTER TABLE rubric          ENABLE ROW LEVEL SECURITY;\nALTER TABLE rubric_version  ENABLE ROW LEVEL SECURITY;\nALTER TABLE rubric_item     ENABLE ROW LEVEL SECURITY;\nCREATE POLICY rubric_tenant_iso         ON rubric         USING (tenant_id = current_setting('app.tenant_id')::uuid);\nCREATE POLICY rubric_version_tenant_iso ON rubric_version USING (tenant_id = current_setting('app.tenant_id')::uuid);\nCREATE POLICY rubric_item_tenant_iso    ON rubric_item    USING (tenant_id = current_setting('app.tenant_id')::uuid);\n\n-- §1 #6: published versions + their items are immutable to the app role.\n-- (Re-curation makes a new version; the cyberos_ops admin role bypasses for corrections-of-record.)\nREVOKE UPDATE, DELETE ON rubric_version FROM cyberos_app;\nREVOKE UPDATE, DELETE ON rubric_item    FROM cyberos_app;</code></pre>\n<h3>Rust model</h3>\n<pre><code class=\"language-rust\">// services/eval/src/rubric/model.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum SourceDoc { LaborContract, NdaIp, TotalRewardsAppendix }\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum ItemKind { Obligation, WorkingTerm, Kpi, CareerMilestone }\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum ObligationKind { Confidentiality, NonCompete, IpAssignment }\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum CheckType { EvidencePresence, ThresholdNumeric, Attestation, PeriodicReview, MilestoneReached }\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum VersionState { Draft, Approved, Published, Superseded }\n\n#[derive(Clone, Debug, Serialize, sqlx::FromRow)]\npub struct RubricItem {\n    pub id:                uuid::Uuid,\n    pub rubric_version_id: uuid::Uuid,\n    pub source_doc:        SourceDoc,\n    pub clause_ref:        String,\n    pub source_quote_vi:   Option&lt;String&gt;,\n    pub source_quote_en:   Option&lt;String&gt;,\n    pub item_kind:         ItemKind,\n    pub obligation_kind:   Option&lt;ObligationKind&gt;,\n    pub check_type:        CheckType,\n    pub check_params:      serde_json::Value,\n    pub weight:            rust_decimal::Decimal,\n    pub title_vi:          String,\n    pub title_en:          Option&lt;String&gt;,\n    pub description_vi:    Option&lt;String&gt;,\n    pub description_en:    Option&lt;String&gt;,\n    pub authored_by:       String,           // \"human\" | \"genie\"\n    pub genie_confidence:  Option&lt;rust_decimal::Decimal&gt;,\n    pub needs_clause_ref:  bool,\n}\n\n/// Validate an item before it may be written into a draft version (§1 #2 #4 #5).\npub fn validate_item(it: &amp;RubricItemDraft) -&gt; Result&lt;(), RubricError&gt; {\n    if it.clause_ref.trim().is_empty() {\n        return Err(RubricError::Uncited);                 // 422 rubric_item_uncited\n    }\n    if it.title_vi.trim().is_empty() {\n        return Err(RubricError::MissingVi);               // 422 rubric_item_missing_vi\n    }\n    check_params_shape_for(it.check_type, &amp;it.check_params)?; // 422 on unknown shape\n    if let ItemKind::Obligation = it.item_kind {\n        if it.obligation_kind.is_none() {\n            return Err(RubricError::ObligationKindRequired);\n        }\n    }\n    Ok(())\n}</code></pre>\n<h3>Versioning + HITL transitions</h3>\n<pre><code class=\"language-rust\">// services/eval/src/rubric/versioning.rs\n\n/// The published version effective on `at` (§1 #7). FR-EVAL-003 anchors an\n/// assessment to whatever standard was actually in force for the period.\npub async fn resolve_effective(\n    pool: &amp;sqlx::PgPool, rubric_id: uuid::Uuid, at: chrono::NaiveDate,\n) -&gt; Result&lt;RubricVersion, RubricError&gt; {\n    sqlx::query_as::&lt;_, RubricVersion&gt;(\n        \"SELECT * FROM rubric_version\n          WHERE rubric_id = $1 AND state = 'published'\n            AND effective_from &lt;= $2\n            AND (effective_to IS NULL OR effective_to &gt; $2)\n          ORDER BY version_no DESC LIMIT 1\"\n    ).bind(rubric_id).bind(at).fetch_optional(pool).await?\n     .ok_or(RubricError::NoEffectiveVersion)\n}\n\n/// HITL publish (§1 #8 #13). A human approver is mandatory; the version must be\n/// coherent; the effective interval must not overlap a live published version.\npub async fn publish_version(\n    tx: &amp;mut sqlx::PgTransaction&lt;'_&gt;,\n    version_id: uuid::Uuid,\n    approver_subject_id: uuid::Uuid,        // MUST be a human subject, not a service account\n    effective_from: chrono::NaiveDate,\n) -&gt; Result&lt;(), RubricError&gt; {\n    if is_service_account(approver_subject_id).await? {\n        return Err(RubricError::RequiresHumanApprover);   // 403 rubric_requires_human_approver\n    }\n    assert_version_coherent(tx, version_id).await?;       // every item cited, bilingual, check-shape ok, non-empty\n    assert_no_effective_overlap(tx, version_id, effective_from).await?;\n\n    // supersede whatever is currently open-ended for this rubric, then publish this one\n    supersede_current_open(tx, version_id, effective_from).await?;\n    sqlx::query(\n        \"UPDATE rubric_version\n            SET state='published', effective_from=$2,\n                approver_subject_id=$3, approved_at_ns=$4,\n                published_by_subject_id=$3, published_at_ns=$4\n          WHERE id=$1 AND state IN ('draft','approved')\"\n    ).bind(version_id).bind(effective_from).bind(approver_subject_id)\n     .bind(now_ns()).execute(&amp;mut **tx).await?;\n\n    audit::emit_tx(tx, \"eval.rubric_published\", json!({\n        \"rubric_version_id\": version_id, \"actor_subject_id\": approver_subject_id,\n        \"effective_from\": effective_from, \"trace_id\": current_trace_id(),\n    })).await?;\n    Ok(())\n}</code></pre>\n<h3>GENIE draft path (proposes only)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/rubric/draft_genie.rs\n\n/// Lumi reads the three signed documents and PROPOSES items into state='draft'\n/// (§1 #9). It never approves, never publishes, and never fabricates a clause_ref:\n/// an item it cannot ground is flagged needs_clause_ref for a human to fill.\npub async fn draft_from_documents(\n    tx: &amp;mut sqlx::PgTransaction&lt;'_&gt;,\n    version_id: uuid::Uuid,\n    docs: &amp;SignedDocSet,                    // the 3 governed documents (FR-EVAL-001 access)\n) -&gt; Result&lt;Vec&lt;RubricItem&gt;, RubricError&gt; {\n    let proposed = lumi::propose_rubric_items(docs).await?;   // FR-EVAL-001-governed analysis call\n    let mut out = Vec::new();\n    for p in proposed {\n        let needs_ref = p.clause_ref.trim().is_empty();       // model could not ground it -&gt; human supplies\n        let item = insert_draft_item(tx, version_id, RubricItemDraft {\n            source_doc:       p.source_doc,\n            clause_ref:       p.clause_ref,                    // verbatim from the doc, or \"\" -&gt; needs_clause_ref\n            item_kind:        p.item_kind,\n            check_type:       p.check_type,\n            check_params:     p.check_params,\n            title_vi:         p.title_vi,  title_en: p.title_en,\n            authored_by:      \"genie\".into(),\n            genie_confidence: Some(p.confidence),\n            needs_clause_ref: needs_ref,\n            ..Default::default()\n        }).await?;\n        audit::emit_tx(tx, \"eval.rubric_drafted\", json!({\n            \"rubric_version_id\": version_id, \"item_id\": item.id,\n            \"authored_by\": \"genie\", \"source_doc\": item.source_doc,\n            \"clause_ref\": item.clause_ref, \"needs_clause_ref\": needs_ref,\n        })).await?;\n        out.push(item);\n    }\n    Ok(out)   // all in 'draft'; a human approves + publishes before FR-EVAL-003 ever sees them\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Tables created, tenant-scoped, RLS on</strong> — migration creates <code>rubric</code>, <code>rubric_version</code>, <code>rubric_item</code>; each has a tenant-isolation policy; tenant A's rubric is invisible to tenant B (AC for §1 #1).</li><li><strong>Uncited item rejected</strong> — POST a <code>rubric_item</code> with empty <code>clause_ref</code> → <code>422 rubric_item_uncited</code>; with a <code>clause_ref</code> → accepted (AC for §1 #2).</li><li><strong>Source doc constrained to the three</strong> — <code>source_doc='handbook'</code> → CHECK violation; <code>source_doc='nda_ip'</code> → accepted (AC for §1 #2).</li><li><strong>Obligation requires obligation_kind</strong> — <code>item_kind='obligation'</code> with null <code>obligation_kind</code> → rejected; with <code>confidentiality</code> → accepted (AC for §1 #3).</li><li><strong>Check shape validated per check_type</strong> — <code>threshold_numeric</code> with <code>{\"target\":0.9,\"operator\":\"&gt;=\"}</code> accepted; <code>threshold_numeric</code> with <code>{}</code> → 422 (AC for §1 #4).</li><li><strong><code>_vi</code> required</strong> — item with <code>title_vi=\"\"</code> → <code>422 rubric_item_missing_vi</code>; <code>_en</code> absent is allowed (AC for §1 #5).</li><li><strong>Published version immutable</strong> — attempt UPDATE on a published <code>rubric_item</code> as <code>cyberos_app</code> → permission denied (AC for §1 #6).</li><li><strong>Re-curation makes a new version</strong> — edit-after-publish creates <code>version_no + 1</code>, prior version → <code>superseded</code> with <code>effective_to</code> set (AC for §1 #6).</li><li><strong><code>resolve_effective</code> returns the in-force version</strong> — two published versions (Jan–Jun, Jul–open); <code>resolve_effective(_, 2026-03-15)</code> returns the Jan–Jun version (AC for §1 #7).</li><li><strong>Human approver mandatory</strong> — <code>publish_version</code> with a service-account approver → <code>403 rubric_requires_human_approver</code>; with a human subject → published (AC for §1 #8).</li><li><strong>Draft/unpublished item not evaluable</strong> — FR-EVAL-003's effective-version read returns only items of a <code>published</code> version; a <code>draft</code> item is absent (AC for §1 #8).</li><li><strong>GENIE drafts into draft only</strong> — <code>draft-from-documents</code> inserts items with <code>authored_by='genie'</code>, <code>state</code> of the version stays <code>draft</code>; no item becomes effective without a later human publish (AC for §1 #9).</li><li><strong>GENIE never fabricates a clause_ref</strong> — a proposed item with no grounding is stored <code>needs_clause_ref=true</code> with empty <code>clause_ref</code>, not an invented citation; publish is blocked until a human fills it (AC for §1 #9 #13).</li><li><strong>Access-gated by FR-EVAL-001</strong> — a caller without the rubric-admin grant gets <code>403</code> on authoring; without any grant gets <code>403</code> on read (AC for §1 #10).</li><li><strong>Audit row per mutation</strong> — draft, edit, approve, publish, supersede each emit the matching <code>eval.rubric_*</code> row, chained into <code>l1_audit_log</code> with the actor and citation (AC for §1 #11).</li><li><strong>Publish blocks on incoherence</strong> — a version with one uncited item → publish <code>422</code> naming the item; an empty version → <code>422 rubric_version_empty</code>; overlapping <code>effective_from</code> → <code>409</code> (AC for §1 #13).</li><li><strong>No per-person data stored</strong> — schema inspection confirms no employee/score/evidence column on any rubric table; per-person scoring is FR-EVAL-003's (AC for §1 #14).</li><li><strong>OTel metrics emitted</strong> — publishing a 4-item version sets <code>eval_rubric_items_total</code>; a rejected publish increments <code>eval_rubric_publishes_total{result=\"rejected\"}</code> (AC for §1 #15).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn uncited_item_is_rejected() {\n    let env = EvalTestEnv::new().await;\n    let (rid, vid) = env.new_draft_version().await;\n    let res = env.post_item(vid, ItemDraft { clause_ref: \"\".into(), ..good_obligation() }).await;\n    assert_eq!(res.status_code(), 422);\n    assert_eq!(res.error_code(), \"rubric_item_uncited\");\n\n    let ok = env.post_item(vid, ItemDraft { clause_ref: \"§4.2\".into(), ..good_obligation() }).await;\n    assert!(ok.is_ok());\n}\n\n#[tokio::test]\nasync fn published_version_is_immutable_to_app_role() {\n    let env = EvalTestEnv::new().await;\n    let vid = env.publish_minimal_version(env.founder()).await;\n    let res: Result&lt;_, _&gt; = sqlx::query(\"UPDATE rubric_item SET weight = 99 WHERE rubric_version_id = $1\")\n        .bind(vid).execute(env.pool_as(\"cyberos_app\")).await;\n    assert!(res.is_err());   // REVOKE UPDATE denies it\n}\n\n#[tokio::test]\nasync fn resolve_effective_picks_the_in_force_version() {\n    let env = EvalTestEnv::new().await;\n    let rid = env.rubric_with_versions(&amp;[\n        (\"2026-01-01\", Some(\"2026-07-01\")),   // v1\n        (\"2026-07-01\", None),                 // v2 open-ended\n    ]).await;\n    let v = resolve_effective(&amp;env.pool, rid, \"2026-03-15\".parse().unwrap()).await.unwrap();\n    assert_eq!(v.version_no, 1);\n    let v2 = resolve_effective(&amp;env.pool, rid, \"2026-09-01\".parse().unwrap()).await.unwrap();\n    assert_eq!(v2.version_no, 2);\n}\n\n#[tokio::test]\nasync fn publish_requires_a_human_approver() {\n    let env = EvalTestEnv::new().await;\n    let vid = env.new_coherent_draft_version().await;\n    let svc = env.service_account_subject();\n    let err = publish_version_tx(&amp;env.pool, vid, svc, \"2026-01-01\".parse().unwrap()).await.unwrap_err();\n    assert!(matches!(err, RubricError::RequiresHumanApprover));\n\n    let human = env.founder();\n    assert!(publish_version_tx(&amp;env.pool, vid, human, \"2026-01-01\".parse().unwrap()).await.is_ok());\n}\n\n#[tokio::test]\nasync fn genie_draft_never_fabricates_a_clause_ref() {\n    let env = EvalTestEnv::new().await;\n    let (_, vid) = env.new_draft_version().await;\n    env.lumi.stub_proposal_without_clause();   // model returns an item it cannot ground\n    let items = draft_from_documents_tx(&amp;env.pool, vid, &amp;env.signed_docs()).await.unwrap();\n    let ungrounded = items.iter().find(|i| i.title_vi.contains(\"ungrounded\")).unwrap();\n    assert!(ungrounded.needs_clause_ref);\n    assert!(ungrounded.clause_ref.is_empty());                 // not an invented citation\n    // publish is blocked while a needs_clause_ref item remains\n    let err = env.publish(vid, env.founder()).await.unwrap_err();\n    assert_eq!(err.error_code(), \"rubric_item_uncited\");\n}\n\n#[tokio::test]\nasync fn rubric_is_tenant_isolated() {\n    let env = EvalTestEnv::new().await;\n    let vid_a = env.as_tenant(\"A\").publish_minimal_version(env.founder()).await;\n    let rows = env.as_tenant(\"B\").list_rubric_items(vid_a).await.unwrap();\n    assert!(rows.is_empty());   // RLS hides tenant A's rubric from tenant B\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Model, versioning, GENIE draft path, and migration above. <code>authoring.rs</code> wires the create/edit/approve flow over <code>model::validate_item</code>; <code>lib.rs</code> mounts the routes behind the FR-EVAL-001 access guard.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-EVAL-001</strong> — governance, consent, access grants, retention. Hard dependency: the rubric tables ship only after it exists, and rubric authoring/reads are gated by its grants (DEC-2601).</li><li><strong>FR-EVAL-003</strong> — the evaluation engine that consumes the published rubric: it calls <code>resolve_effective(at)</code> and reads <code>rubric_item</code> rows to score evidence. This FR <code>blocks</code> it.</li><li><strong>FR-MEMORY-123</strong> — brain recall; the same audit-chain + memory substrate the rubric mutations chain into.</li><li><strong>FR-AUTH-003</strong> — RLS / roles; the tenant isolation and the <code>cyberos_app</code> vs <code>cyberos_ops</code> role split the immutability REVOKE relies on.</li><li><strong>FR-PROJ-008 / FR-MEMORY-123</strong> — the established hash-chained audit-row pattern the <code>eval.rubric_*</code> rows follow.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.rubric_published\",\n  \"payload\": {\n    \"rubric_id\":          \"rub-...\",\n    \"rubric_version_id\":  \"rv-...\",\n    \"version_no\":         2,\n    \"actor_subject_id\":   \"stephen-...\",\n    \"effective_from\":     \"2026-07-01\",\n    \"trace_id\":           \"0af...\"\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"item\": {\n    \"source_doc\":     \"nda_ip\",\n    \"clause_ref\":     \"§4.2\",\n    \"source_quote_vi\":\"Người lao động phải giữ bí mật...\",\n    \"source_quote_en\":\"The employee shall keep confidential...\",\n    \"item_kind\":      \"obligation\",\n    \"obligation_kind\":\"confidentiality\",\n    \"check_type\":     \"evidence_presence\",\n    \"check_params\":   { \"signal\": \"no_unauthorized_disclosure\" },\n    \"weight\":         10.0,\n    \"title_vi\":       \"Bảo mật thông tin\",\n    \"title_en\":       \"Confidentiality\",\n    \"authored_by\":    \"human\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Resolved by Stephen's 2026-06-29 decisions (governance first; GENIE drafts, human approves; versioned + effective-dated; clause-cited). Deferred:</p>\n<ul><li>Whether <code>check_params</code> schemas get a formal JSON-Schema registry per <code>check_type</code> (slice 2) — this FR validates shape by <code>check_type</code> in code; a declared schema registry is additive.</li><li>A diff view between two rubric versions (which clause changed, which item was added) for the FR-EVAL-004 console — surfacing only, a later additive screen.</li><li>Multi-document rubrics beyond the three signed documents (e.g. a code-of-conduct addendum) — out of scope until such a document is signed; the <code>source_doc</code> enum is deliberately closed to the three for now.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Item written with no <code>clause_ref</code></td><td><code>validate_item</code></td><td>422 rubric_item_uncited</td><td>Author cites the clause</td></tr><tr><td><code>source_doc</code> outside the three</td><td>CHECK constraint</td><td>422 / constraint error</td><td>Author uses one of the three docs</td></tr><tr><td>Obligation item without <code>obligation_kind</code></td><td><code>validate_item</code></td><td>rejected</td><td>Author sets confidentiality/non_compete/ip_assignment</td></tr><tr><td><code>check_params</code> shape wrong for <code>check_type</code></td><td><code>check_params_shape_for</code></td><td>422</td><td>Author fixes the params</td></tr><tr><td><code>title_vi</code> missing</td><td><code>validate_item</code></td><td>422 rubric_item_missing_vi</td><td>Author supplies Vietnamese title</td></tr><tr><td>Publish with a service-account approver</td><td><code>is_service_account</code> guard</td><td>403 rubric_requires_human_approver</td><td>A human approves</td></tr><tr><td>Publish an empty version</td><td><code>assert_version_coherent</code></td><td>422 rubric_version_empty</td><td>Add at least one item</td></tr><tr><td>Publish a version with an uncited item</td><td>coherence check</td><td>422 naming the item</td><td>Cite or remove the item</td></tr><tr><td>Overlapping <code>effective_from</code></td><td><code>assert_no_effective_overlap</code></td><td>409</td><td>Adjust the effective date</td></tr><tr><td>UPDATE on a published row by app role</td><td>REVOKE / permission</td><td>denied</td><td>Make a new version instead</td></tr><tr><td>GENIE fabricates a clause_ref</td><td><code>needs_clause_ref</code> flag + publish coherence</td><td>item flagged; publish blocked</td><td>Human supplies the real clause</td></tr><tr><td>GENIE proposes an item for a non-existent doc</td><td><code>source_doc</code> CHECK</td><td>rejected on insert</td><td>Human corrects/drops it</td></tr><tr><td>Rubric authored before FR-EVAL-001 exists</td><td>dependency gate (DEC-2601)</td><td>FR not shippable</td><td>Ship FR-EVAL-001 first</td></tr><tr><td>Read by a caller without a grant</td><td>FR-EVAL-001 access guard</td><td>403</td><td>Grant access per governance</td></tr><tr><td><code>resolve_effective</code> finds no in-force version</td><td>NoEffectiveVersion</td><td>error to FR-EVAL-003</td><td>Publish a version covering the period</td></tr><tr><td>Two published versions overlap (data drift)</td><td>publish-time overlap guard prevents</td><td>n/a at write; sweep flags if introduced by admin</td><td>Admin corrects-of-record</td></tr><tr><td>Audit emit fails mid-tx</td><td>sqlx tx rollback</td><td>mutation not committed</td><td>Caller retries</td></tr><tr><td>Cross-tenant read attempt</td><td>RLS</td><td>0 rows</td><td>None — by design</td></tr><tr><td><code>weight</code> negative</td><td>CHECK (weight &gt;= 0)</td><td>constraint error</td><td>Author fixes weight</td></tr><tr><td>Bilingual drift (<code>_en</code> says something <code>_vi</code> does not)</td><td>human review at approve</td><td>caught by approver</td><td>Approver corrects translation</td></tr><tr><td>OTel exporter down</td><td>buffered then dropped</td><td>logged</td><td>Restore FR-OBS-001</td></tr><tr><td>Admin (<code>cyberos_ops</code>) edits a published row</td><td>allowed by design for corrections-of-record</td><td>emits <code>eval.rubric_edited</code> audit</td><td>Audited; reviewable</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The three tables separate the framework (<code>rubric</code>), the immutable published cut (<code>rubric_version</code>), and the checkable items (<code>rubric_item</code>) so that re-curation is \"new version\", not \"mutate rows\" — the same append-only discipline as the FR-PROJ-008 history layer.</li><li><code>resolve_effective(at)</code> is the single seam FR-EVAL-003 uses; everything else about versioning is internal. Anchoring an assessment to the version in force on its period date is what keeps a past assessment interpretable after a contract amendment.</li><li>The HITL gate is enforced at two places: the model can only ever write <code>state='draft'</code> items (it has no path to <code>publish</code>), and <code>publish_version</code> rejects a non-human approver. Defence in depth: even a mis-wired caller cannot publish without a human subject.</li><li><code>authored_by</code> provenance is sticky — a later human edit adds <code>edited_by_subject_id</code> but never rewrites <code>authored_by</code>, so \"this item started as a GENIE draft\" stays visible in the audit trail forever.</li><li>The <code>needs_clause_ref</code> flag is the anti-fabrication mechanism. The model is instructed to leave <code>clause_ref</code> empty when it cannot ground an item, and publish coherence (clause 13) refuses to let an uncited item become operative. A grounded-looking but invented citation is the failure we design against; an honest gap that a human fills is the intended path.</li><li>Vietnamese is primary because the documents are governed by Vietnamese law and the Vietnamese text is the operative one; <code>_vi NOT NULL</code>, <code>_en</code> optional captures that without forcing a translation to exist before the standard can.</li><li>The <code>cyberos_app</code> REVOKE makes published rows immutable to the runtime; <code>cyberos_ops</code> retains the ability to correct a record (rare, audited via <code>eval.rubric_edited</code>). This mirrors the FR-PROJ-008 role split.</li><li><code>check_type</code> is intentionally a small closed enum so FR-EVAL-003's evaluation logic is bounded: five evaluation shapes cover obligations (evidence presence / attestation), working terms (periodic review), KPIs (numeric threshold), and career milestones (milestone reached). A new shape is a deliberate schema change, not a free-form field.</li><li><code>weight</code> is relative within a version; the roll-up math (how item weights compose into a section and an overall draft score) belongs to FR-EVAL-003, not here. This FR stores the weight; it does not compute a score.</li><li>No per-person column exists anywhere in this schema by design (clause 14). If a future need tempts adding one, that is the signal to put it in FR-EVAL-003 against <code>rubric_version_id</code>, keeping the standard and the judgement separate.</li><li>The audit rows reuse the <code>services/eval/src/audit.rs</code> emitter (the FR-MEMORY-123 chain), so the rubric's curation history lives in the same tamper-evident ledger as every other CyberOS event — a reviewer reconstructs how the standard was set from one place.</li><li>Effective-dating uses <code>[effective_from, effective_to)</code> half-open intervals so adjacent versions meet exactly on a boundary date with no gap and no overlap; the publish-time overlap guard enforces it.</li><li>This is a high-risk FR under the EU AI Act framing (the rubric feeds an employment-evaluation system); see the AI Risk Assessment section. The model's role is bounded to drafting, every operative item is human-approved, and the standard is clause-grounded and auditable.</li></ul>\n<hr>\n<h2>AI Risk Assessment</h2>\n<h3>Risk classification</h3>\n<p>High-risk under the EU AI Act framing: the rubric is the standard a later system (FR-EVAL-003) uses to assess employees, and employment evaluation that can influence pay and progression is a high-risk use. This FR does not itself score anyone, but it defines what \"good\" and \"compliant\" mean, so it is held to the high-risk bar: human oversight, traceability, and grounding in the signed source.</p>\n<h3>Data sources</h3>\n<p>The rubric is built only from the three signed employment documents — the Labor Contract, the NDA/non-compete/IP agreement, and the Total Rewards &amp; Career Path Appendix (bilingual VN/EN, dated 2026-01-01, under Labor Code 45/2019/QH14 and Decree 145/2020). It stores no per-employee data, no evidence, and no scores. Reading the documents to draft items happens through the FR-EVAL-001-governed analysis path, under that FR's consent and access basis.</p>\n<h3>Human oversight</h3>\n<p>GENIE/Lumi MAY draft rubric items but MUST NOT approve or publish them (EU AI Act Article 14). A human approver — the founder or a designated rubric admin holding the FR-EVAL-001 grant — must approve and publish before any item becomes the operative standard; a service-account approver is rejected (clause 8). Every item is clause-cited so the human can check the model's reading against the exact signed text, and an item the model cannot ground is flagged <code>needs_clause_ref</code> for the human to fill rather than published on an invented citation.</p>\n<h3>Traceability</h3>\n<p>Every rubric mutation — draft, edit, approve, publish, supersede — emits a hash-chained memory audit row (<code>eval.rubric_*</code>) carrying the actor, the version, and the citation, into the same tamper-evident <code>l1_audit_log</code> as the rest of CyberOS. Published versions are immutable and effective-dated, so the exact standard in force on any date, and the full history of how it was curated, is reconstructable and provable. The <code>authored_by</code> provenance keeps GENIE-drafted vs human-authored items distinguishable forever.</p>\n<h3>Failure modes</h3>\n<p>The model fabricating a clause citation is caught by <code>needs_clause_ref</code> plus the publish-time coherence check, which refuses to make an uncited item operative (clause 13). The model's reading becoming the standard without a human is prevented structurally — it has no path past <code>draft</code> (clause 8). A contract amendment silently rewriting the past is prevented by immutable, effective-dated versions (clause 6, clause 7). In every case the safe state is \"no operative standard changed without a human grounding it in the signed text\".</p>\n<hr>\n<p><em>End of FR-EVAL-002.</em></p>";
