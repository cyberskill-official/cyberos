window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOC-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The DOC service <strong>MUST</strong> extend FR-DOC-001 documents with lifecycle metadata at <code>services/doc/src/lifecycle/</code> — parties, dates, renewal terms, parent chain, auto-status, 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define table extension at migration <code>0002</code>: ``<code>sql ALTER TABLE doc_documents ADD COLUMN parties JSONB; ALTER TABLE doc_documents ADD COLUMN effective_date DATE; ALTER TABLE doc_documents ADD COLUMN expiry_date DATE; ALTER TABLE doc_documents ADD COLUMN renewal_terms JSONB; ALTER TABLE doc_documents ADD COLUMN parent_contract_id UUID REFERENCES doc_documents(document_id); ALTER TABLE doc_documents ADD COLUMN lifecycle_status TEXT CHECK (lifecycle_status IS NULL OR lifecycle_status IN ('draft','active','expiring','expired','terminated','renewed')); ALTER TABLE doc_documents ADD COLUMN status_computed_at TIMESTAMPTZ; CREATE INDEX docs_expiry_idx ON doc_documents(tenant_id, expiry_date) WHERE expiry_date IS NOT NULL; CREATE INDEX docs_parent_idx ON doc_documents(tenant_id, parent_contract_id) WHERE parent_contract_id IS NOT NULL; GRANT UPDATE (parties, effective_date, expiry_date, renewal_terms, parent_contract_id, lifecycle_status, status_computed_at) ON doc_documents TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> validate <code>lifecycle_status</code> against closed enum per DEC-1711.</li></ol>\n<ol><li><strong>MUST</strong> validate parties JSONB structure per DEC-1713 — array of <code>{party_id, party_type, role}</code>. party_type ∈ {tenant, customer, vendor, employee, authority}.</li></ol>\n<ol><li><strong>MUST</strong> compute status at <code>status_computer.rs::compute(doc, now)</code> per DEC-1712:</li></ol>\n<ul><li>Triggered on field change + nightly cron (FR-MCP-007).</li><li>Updates <code>lifecycle_status</code> + <code>status_computed_at</code>.</li></ul>\n<ol><li><strong>MUST</strong> support parent chain per DEC-1714 — amendments link via <code>parent_contract_id</code>; expose tree query endpoint.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text PUT    /v1/doc/documents/{id}/lifecycle    body: {parties, effective_date, expiry_date, renewal_terms, parent_contract_id?} GET    /v1/doc/documents/{id}/lifecycle GET    /v1/doc/documents/{id}/parent-chain   (returns ancestor tree) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-1715. PII per FR-MEMORY-111: parties JSON hashed; dates and status enum ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from set → compute → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior <code>parent_contract_id</code> (parent re-link rare; if needed, new row).</li></ol>\n<ol><li><strong>MUST NOT</strong> skip auto-status on field change per DEC-1712.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why first-class columns (DEC-1710)?</strong> JSONB-only would force every query to parse; columnar = indexable + queryable for FR-DOC-008.</p>\n<p><strong>Why auto-status (DEC-1712)?</strong> Manual updates drift; computed status reflects truth at query time.</p>\n<p><strong>Why party type enum (DEC-1713)?</strong> Roles drive notification routing (employees ≠ customers ≠ authorities).</p>\n<p><strong>Why parent chain (DEC-1714)?</strong> Amendments form contract lineage; legal needs to see full tree.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample lifecycle:</p>\n<pre><code class=\"language-json\">{\n  \"document_id\": \"uuid\",\n  \"parties\": [\n    {\"party_id\": \"uuid-tenant\", \"party_type\": \"tenant\", \"role\": \"service_provider\"},\n    {\"party_id\": \"uuid-acme\", \"party_type\": \"customer\", \"role\": \"client\"}\n  ],\n  \"effective_date\": \"2026-01-01\",\n  \"expiry_date\": \"2027-12-31\",\n  \"renewal_terms\": {\"auto_renew\": true, \"notice_days\": 60, \"term_months\": 24},\n  \"parent_contract_id\": null,\n  \"lifecycle_status\": \"active\",\n  \"status_computed_at\": \"2026-05-17T02:00:00Z\"\n}</code></pre>\n<p>Parent chain:</p>\n<pre><code class=\"language-json\">[\n  {\"document_id\": \"uuid-amendment-2\", \"level\": 0},\n  {\"document_id\": \"uuid-amendment-1\", \"level\": 1},\n  {\"document_id\": \"uuid-original-msa\", \"level\": 2}\n]</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>All lifecycle fields settable</strong>. 2. <strong>6-status enum + cardinality test</strong>. 3. <strong>Auto-status compute on field change</strong>. 4. <strong>Nightly cron refresh</strong>. 5. <strong>Parties JSONB structure validated</strong>. 6. <strong>Party type enum (5: tenant/customer/vendor/employee/authority)</strong>. 7. <strong>Parent chain query returns ancestors</strong>. 8. <strong>Indexed on expiry_date for FR-DOC-008</strong>. 9. <strong>Indexed on parent_contract_id for tree query</strong>. 10. <strong>3 memory audit kinds emitted</strong>. 11. <strong>PII scrubbed (parties JSONB SHA256)</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Status thresholds correct (90d = expiring)</strong>. 15. <strong>Terminated status manual-only (CLO action)</strong>. 16. <strong>Renewed status set on renewal contract creation</strong>. 17. <strong>Append-only via REVOKE UPDATE except 7 cols</strong>. 18. <strong>NULL allowed for legacy docs</strong>. 19. <strong>Self-referential FK enforced</strong>. 20. <strong>Status compute idempotent (same input → same result)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn status_active_within_window() {\n    let ctx = TestContext::doc_with_dates(\"2026-01-01\", \"2027-12-31\").await;\n    ctx.compute_status(ctx.doc_id, \"2026-06-01\").await;\n    let d = ctx.fetch_doc(ctx.doc_id).await;\n    assert_eq!(d.lifecycle_status.as_deref(), Some(\"active\"));\n}\n\n#[tokio::test]\nasync fn status_expiring_within_90d() {\n    let ctx = TestContext::doc_with_dates(\"2026-01-01\", \"2026-06-30\").await;\n    ctx.compute_status(ctx.doc_id, \"2026-04-15\").await;\n    let d = ctx.fetch_doc(ctx.doc_id).await;\n    assert_eq!(d.lifecycle_status.as_deref(), Some(\"expiring\"));\n}\n\n#[tokio::test]\nasync fn parent_chain_returns_ancestors() {\n    let ctx = TestContext::with_amendment_chain(3).await;\n    let chain = ctx.fetch_parent_chain(ctx.leaf_id).await;\n    assert_eq!(chain.len(), 3);\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-DOC-001. <strong>Downstream:</strong> FR-DOC-008 (expiry alerts), FR-DOC-009 (renewal proposals). <strong>Cross-module:</strong> FR-MCP-007 (cron), FR-AI-003 (parties extraction skill — future), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid party_type</td><td>validate</td><td>reject 400</td><td>use valid</td></tr><tr><td>Expiry before effective</td><td>validate</td><td>reject 400</td><td>fix dates</td></tr><tr><td>Self-referential parent</td><td>validate</td><td>reject (cycle)</td><td>fix link</td></tr><tr><td>Parent chain &gt;20 deep</td><td>sanity warn</td><td>sev-3; allow</td><td>inherent</td></tr><tr><td>Status compute fails on bad date</td><td>catch</td><td>NULL + sev-2</td><td>fix data</td></tr><tr><td>JSONB schema mismatch</td><td>parse</td><td>reject 400</td><td>fix structure</td></tr><tr><td>Renewal_terms unparseable</td><td>validate</td><td>reject 400</td><td>fix</td></tr><tr><td>Cross-tenant parent FK</td><td>RLS</td><td>404 (treat as missing)</td><td>inherent</td></tr><tr><td>Concurrent status compute</td><td>last-writer-wins</td><td>inherent</td><td>inherent</td></tr><tr><td>Cron skipped</td><td>catch on next</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Status computer pure function: <code>(effective, expiry, now) → status</code>.</li><li>§11.2 Nightly cron runs at 03:00 tenant_tz, recomputes all docs with expiry_date set.</li><li>§11.3 Parent chain query recursive CTE: <code>WITH RECURSIVE chain AS (...)</code>.</li><li>§11.4 memory audit body: doc_id, status, status_computed_at; parties JSONB hashed.</li><li>§11.5 Future FR can ingest contract PDFs and auto-extract parties via FR-AI-003 — this FR provides the schema.</li></ul>\n<hr>\n<p><em>End of FR-DOC-007 spec.</em></p>";
