window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-IMP-068"]="<h1>FR-IMP-068: Payload-version drift gate</h1>\n<h2>§1 - Description</h2>\n<p>The root <code>VERSION</code> file is the single platform version, auto-bumped in CI by <code>version.yml</code>. The distributable payload (<code>dist/cyberos</code>) is stamped from <code>VERSION</code> by <code>tools/cyberos-init/build.sh</code>, but only when a human runs the build. Nothing compares the two, so the payload and every installed plugin silently lag (observed: 1.2.0 vs 1.7.0). This FR adds the missing comparison and makes it enforceable in CI and locally.</p>\n<p>Normative clauses:</p>\n<ol><li>A script <code>tools/cyberos-init/check-version-sync.sh &lt;payload-dir&gt;</code> MUST compare root <code>VERSION</code> against every stamped artifact in the payload: <code>&lt;payload&gt;/VERSION</code>, <code>&lt;payload&gt;/plugin/.claude-plugin/plugin.json</code> <code>.version</code>, <code>&lt;payload&gt;/.claude-plugin/marketplace.json</code> <code>metadata.version</code>, <code>&lt;payload&gt;/mcp/package.json</code> <code>.version</code>, <code>&lt;payload&gt;/manifest.yaml</code> <code>cyberos_version</code>, and the <code>plugin.json</code> sealed inside <code>&lt;payload&gt;/cyberos.plugin</code> (read via <code>unzip -p</code>, no extraction to disk). It MUST exit 0 when all six match, exit 10 on any mismatch printing one line per drifted artifact in the form <code>DRIFT &lt;relative-path&gt;: &lt;found&gt; != &lt;expected&gt;</code>, and exit 2 when root <code>VERSION</code> is missing or not <code>X.Y.Z</code> semver.</li><li>A workflow <code>.github/workflows/payload-gate.yml</code> MUST run on push and pull_request to <code>main</code> when any of <code>tools/cyberos-init/**</code>, <code>modules/skill/**</code>, <code>modules/cuo/**</code>, or <code>VERSION</code> changes. It MUST build the payload into a temporary directory with <code>build.sh &lt;tmpdir&gt;</code> and run <code>check-version-sync.sh &lt;tmpdir&gt;</code>; a non-zero exit from either MUST fail the workflow.</li><li><code>build.sh</code> MUST exit non-zero with an explicit error when root <code>VERSION</code> is missing or not <code>X.Y.Z</code> semver. The current silent fallback (<code>|| echo 0.0.0</code>) MUST be removed; a payload stamped <code>0.0.0</code> MUST be impossible to produce.</li><li>A <code>.githooks/pre-commit</code> hook MUST be added (the repo's <code>core.hooksPath</code> is <code>.githooks</code>, so hooks placed only in the pre-commit framework never fire for contributors who skipped <code>pre-commit install</code>). When staged paths match the existing trigger list in <code>.pre-commit-hooks/cyberos-payload-build.sh</code> (<code>modules/cuo/ | modules/skill/ | tools/cyberos-init/ | VERSION</code>), the hook MUST invoke that script to refresh <code>dist/cyberos</code> and then run <code>check-version-sync.sh dist/cyberos</code>. Rebuild or check failure MUST abort the commit; a clean run MUST NOT block it. Non-matching commits MUST be a no-op.</li><li><code>docs/deploy/RELEASE.md</code> MUST describe the invariant as enforced (CI gate + wired hook), replacing the current aspirational claim that the pre-commit hook keeps the payload matched.</li><li>The CI gate MUST NOT require network access beyond checkout and MUST complete its build+check steps in under 3 minutes (the build is file copies plus sed).</li><li>Because the bot's bump commit carries <code>[skip ci]</code> (so <code>payload-gate.yml</code> never sees it), <code>version.yml</code> MUST run the build+check inline in its own job, immediately after <code>cyberos-version.mjs --apply</code> and before pushing - the bump and the proof that a payload builds clean at the new version land together.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The stamping logic in <code>build.sh</code> is already correct and single-source; duplicating version propagation elsewhere would create a second drift surface. The gate therefore re-uses the real build and only adds a read-only comparator. Building into a temp dir in CI keeps <code>dist/</code> gitignored (operator decision) while still proving that a build at this commit yields stamps equal to <code>VERSION</code>. The comparator is a standalone script so the CI gate, the git hook, FR-IMP-069's release job, and the desktop Ops tab (FR-APP-001) all share one implementation.</p>\n<h2>§3 - Contract</h2>\n<h3>check-version-sync.sh</h3>\n<pre><code>usage: check-version-sync.sh [payload-dir]   # default: &lt;repo&gt;/dist/cyberos\nexit 0   in sync (prints \"sync OK &lt;version&gt; across 6 artifacts\")\nexit 10  drift (one \"DRIFT &lt;path&gt;: &lt;found&gt; != &lt;expected&gt;\" line per artifact)\nexit 2   root VERSION missing/invalid, payload dir missing, or artifact unreadable</code></pre>\n<p>Artifact readers: <code>VERSION</code> = trimmed file; JSON fields via <code>node -p</code> (node is already a build dependency); <code>manifest.yaml</code> via grep of the <code>cyberos_version:</code> line; sealed plugin.json via <code>unzip -p &lt;payload&gt;/cyberos.plugin .claude-plugin/plugin.json</code>.</p>\n<h3>payload-gate.yml</h3>\n<pre><code class=\"language-yaml\">name: payload-gate\non:\n  push: { branches: [main], paths: [tools/cyberos-init/**, modules/skill/**, modules/cuo/**, VERSION] }\n  pull_request: { paths: [tools/cyberos-init/**, modules/skill/**, modules/cuo/**, VERSION] }\njobs:\n  build-and-check:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: bash tools/cyberos-init/build.sh \"$RUNNER_TEMP/payload\"\n      - run: bash tools/cyberos-init/check-version-sync.sh \"$RUNNER_TEMP/payload\"</code></pre>\n<h3>.githooks/pre-commit</h3>\n<p>Delegates to <code>.pre-commit-hooks/cyberos-payload-build.sh</code> (trigger-list match) then <code>check-version-sync.sh dist/cyberos</code>; both under <code>set -euo pipefail</code> so failure aborts the commit.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Sync passes on a fresh build</strong> (§1 #1) - immediately after <code>build.sh</code>, <code>check-version-sync.sh</code> exits 0 and reports the version and artifact count.</li><li><strong>Each artifact is individually guarded</strong> (§1 #1) - tampering any one of the six stamps (payload VERSION, plugin.json, marketplace.json, mcp/package.json, manifest.yaml, sealed plugin.json) makes the check exit 10 and name exactly that artifact.</li><li><strong>The sealed bundle is checked without extraction</strong> (§1 #1) - re-zipping <code>cyberos.plugin</code> with a stale inner plugin.json while all on-disk files match still exits 10.</li><li><strong>Invalid VERSION cannot stamp</strong> (§1 #3) - with <code>VERSION</code> containing <code>banana</code>, <code>build.sh</code> exits non-zero and writes no payload; with <code>VERSION</code> deleted, same.</li><li><strong>No 0.0.0 escape hatch remains</strong> (§1 #3) - <code>grep -n \"0\\.0\\.0\" tools/cyberos-init/build.sh</code> shows only the placeholder-substitution seds, no fallback echo.</li><li><strong>CI gate is wired</strong> (§1 #2, #6) - <code>payload-gate.yml</code> exists with the four path filters on both push and pull_request, parses as valid YAML, and its steps are exactly build-into-temp + check.</li><li><strong>Hook fires on trigger paths</strong> (§1 #4) - in a fixture repo with <code>core.hooksPath=.githooks</code>, committing a change under <code>modules/skill/</code> runs the rebuild and check; committing a change under <code>docs/</code> runs neither.</li><li><strong>Hook failure blocks the commit</strong> (§1 #4) - with <code>build.sh</code> forced to fail, the commit aborts with the build error visible.</li><li><strong>RELEASE.md tells the truth</strong> (§1 #5) - the doc names <code>payload-gate.yml</code> and <code>.githooks/pre-commit</code> as the enforcement points; the old aspirational sentence is gone.</li><li><strong>Bump commits are self-proving</strong> (§1 #7) - <code>version.yml</code> contains the inline build+check steps between apply and push; removing them makes t10's structural assertion fail.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/cyberos-init/tests/test_check_version_sync.sh\n# Harness: builds a scratch payload into $TMP/payload from a scratch VERSION file,\n# then mutates artifacts one at a time. Run: bash tools/cyberos-init/tests/test_check_version_sync.sh\n\nt01_fresh_build_syncs()          # AC 1\nt02_each_artifact_guarded()      # AC 2  (loop over the 6 artifacts, expect exit 10 + the right DRIFT line)\nt03_sealed_zip_checked()         # AC 3  (zip -j a stale plugin.json into cyberos.plugin)\nt04_invalid_version_refused()    # AC 4  (VERSION=banana and VERSION absent -&gt; build.sh non-zero)\nt05_no_fallback_left()           # AC 5  (static grep assertion)\nt06_workflow_shape()             # AC 6  (node yaml-less structural greps: name, both triggers, 4 path filters)\nt07_hook_trigger_matrix()        # AC 7  (git init fixture, core.hooksPath=.githooks, two commits)\nt08_hook_blocks_on_failure()     # AC 8  (PATH-shadowed failing build.sh -&gt; commit exits non-zero)\nt09_release_md_updated()         # AC 9  (grep RELEASE.md for payload-gate.yml + .githooks/pre-commit)\nt10_version_yml_inline_check()   # AC 10 (structural greps: build.sh + check-version-sync.sh steps between apply and push)</code></pre>\n<p>All nine cases green = §5 pass. The suite is plain bash with <code>set -euo pipefail</code>, no framework, runnable in CI and locally.</p>\n<h2>§6 - Implementation skeleton</h2>\n<p><code>check-version-sync.sh</code>: read+validate root VERSION; declare the six readers; accumulate drift lines; print and exit per contract. <code>build.sh</code> diff: replace <code>cyver=\"$(tr -d ' \\n\\r' &lt; \"$repo/VERSION\" 2&gt;/dev/null || echo 0.0.0)\"</code> with an existence+regex guard that errors out. Hook: 15-line bash wrapper as in §3.</p>\n<h2>§7 - Dependencies</h2>\n<p>None upstream. Blocks FR-IMP-069 (the release publisher reuses <code>check-version-sync.sh</code> as its pre-upload gate). FR-APP-001's desktop Ops tab can surface the same check verbatim.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ bash tools/cyberos-init/check-version-sync.sh dist/cyberos\nDRIFT dist/cyberos/VERSION: 1.2.0 != 1.7.0\nDRIFT dist/cyberos/plugin/.claude-plugin/plugin.json: 1.2.0 != 1.7.0\nDRIFT dist/cyberos/cyberos.plugin!.claude-plugin/plugin.json: 1.2.0 != 1.7.0\n$ echo $?\n10</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Whether <code>payload-gate</code> becomes a required check in the branch ruleset is an operator toggle after one week of green runs.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>CI runner lacks <code>zip</code>/<code>unzip</code> - <code>build.sh</code> already requires zip; the gate installs nothing, so the check script MUST fail with exit 2 and a \"unzip missing\" message rather than a false pass. Covered by t02 harness precondition.</li><li>Commit bypasses the hook (<code>--no-verify</code>) - accepted; <code>payload-gate.yml</code> is the backstop on push/PR.</li><li>Bot bump commit is invisible to path-triggered workflows (<code>[skip ci]</code>) - closed by §1 #7: the bump job itself proves the build before pushing, so no commit lands on main with an unprovable payload state.</li><li>Interrupted local build leaves a half-written dist - the check reads six artifacts; any missing file is exit 2 (unreadable), never a false 0.</li><li>manifest.yaml format change breaks the grep reader - t02 pins the reader against the generated manifest; a format change fails the suite at the same commit that changes the generator.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the job name <code>payload-gate / build-and-check</code> stable so it can be added to the ruleset's required checks. The check script must not import from <code>build.sh</code> (read-only comparator; zero side effects). Reuse in FR-IMP-069 and FR-APP-001 is by invocation, not by copy.</p>\n<p><strong>Post-ship amendment (2026-07-12, FR-IMP-071):</strong> §1 #7's premise - bump commits carry [skip ci] so payload-gate never sees them - is retired; bump commits are now plain <code>chore(release):</code> and payload-gate runs on them too. The inline proof in version.yml stays as belt-and-suspenders.</p>\n<p><em>End of FR-IMP-068.</em></p>";
