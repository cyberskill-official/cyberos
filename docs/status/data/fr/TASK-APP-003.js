window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-APP-003"]="<h2>§1 — Description</h2>\n<ol><li>CyberOS's Tauri desktop build <strong>MUST</strong> gain a second, App Store–specific build target (<code>tauri.mas.conf.json</code>, an overlay merged over <code>tauri.conf.json</code>) that is fully independent of the existing Developer ID / GitHub Releases build target. The existing <code>apps/desktop/src-tauri/tauri.conf.json</code> <strong>MUST NOT</strong> be mutated in a way that changes the Developer ID build's behavior — the two targets diverge only in <code>bundle.macOS.entitlements</code>, <code>bundle.macOS.signingIdentity</code>, and <code>bundle.macOS.provider Short Name</code> [sic — see §11 note on case], and in the packaging step (<code>.dmg</code> vs <code>.pkg</code>).</li></ol>\n<ol><li>The Mac App Store build <strong>MUST</strong> run inside the macOS App Sandbox (<code>com.apple.security.app-sandbox = true</code> in <code>Entitlements.mas.plist</code>). Every filesystem path, network destination, and IPC surface CyberOS's Rust backend touches <strong>MUST</strong> be enumerated (§3) and either (a) fall inside the sandbox container automatically, (b) be covered by an explicit entitlement, or (c) be removed/gated behind a \"Mac App Store build\" feature flag if no sandbox-compatible equivalent exists.</li></ol>\n<ol><li><code>apps/desktop/src-tauri/Entitlements.mas.inherit.plist</code> <strong>MUST</strong> exist and be attached to every Tauri-spawned child process (Tauri's sidecar / shell-plugin binaries, if CyberOS uses any) via the <code>com.apple.security.inherit</code> entitlement, per Apple's requirement that sandboxed apps propagate sandboxing to their own subprocesses.</li></ol>\n<ol><li>The build <strong>MUST</strong> produce a signed, App Store–distribution <code>.pkg</code> installer (via <code>productbuild</code>/<code>pkgbuild</code>), not the <code>.dmg</code> used by the Developer ID channel. App Store Connect ingestion (<code>xcrun altool --upload-app</code> or Transporter) accepts <code>.pkg</code> only for macOS.</li></ol>\n<ol><li>CI <strong>MUST</strong> gate the Mac App Store build behind a repo variable <code>MAS_RELEASE=true</code> (mirroring the existing <code>MOBILE_RELEASE</code> pattern used for Capacitor mobile jobs — see <code>apps/web/capacitor.config.ts</code>), defaulting to off, so this task ships inert until Stephen has an Apple Developer Program enrollment, a macOS App Store Connect app record, and a \"3rd Party Mac Developer Application\" + \"3rd Party Mac Developer Installer\" signing identity pair.</li></ol>\n<ol><li>Signing for this channel <strong>MUST</strong> use certificate types distinct from the Developer ID certificate already used for the GitHub Releases DMG (<code>Developer ID Application: &lt;team&gt;</code>). Mac App Store distribution requires <code>3rd Party Mac Developer Application</code> (app signing) and <code>3rd Party Mac Developer Installer</code> (pkg signing) — using a Developer ID cert on an App Store submission is rejected by App Store Connect at ingestion.</li></ol>\n<ol><li>This task <strong>MUST NOT</strong> attempt to acquire, request, or enter Apple Developer Program enrollment, App Store Connect API keys, or any signing certificate — those are Stephen's account-creation and credential-entry actions per standing operating constraints. The task's own §9 Open Questions records exactly which prerequisites block <code>MAS_RELEASE=true</code> from ever being flippable.</li></ol>\n<ol><li>A <code>docs/deploy/mac-app-store-submission.md</code> answer sheet <strong>MUST</strong> be authored capturing every App Store Connect macOS submission field that requires a human decision (export compliance / encryption declaration, content rights, macOS-specific privacy usage strings such as <code>NSCameraUsageDescription</code> if CyberOS ever requests camera access, age rating questionnaire) — mirroring the existing <code>docs/deploy/play-store-submission.md</code> pattern already in the repo for Android.</li></ol>\n<h2>§2 — Why this design</h2>\n<p><strong>Why a separate <code>tauri.mas.conf.json</code> overlay instead of conditionally branching the existing config (§1 #1)?</strong> Tauri's config merge model (<code>--config &lt;path&gt;</code> flag deep-merges a second JSON file over the base) is the tool's own supported mechanism for exactly this \"same source, two distribution channels\" case — Tauri's own documentation examples use this pattern for Developer-ID-vs-App-Store macOS builds. Branching logic inside a single <code>tauri.conf.json</code> would require templating (Tauri config is static JSON, no conditionals), which is more fragile and harder to diff in code review than two files.</p>\n<p><strong>Why full sandbox-compatibility audit before writing any entitlement (§1 #2)?</strong> App Sandbox entitlements are an allowlist, not a denylist — every capability CyberOS's Rust backend uses that isn't automatically inside the sandbox container (temp dir, app's own Application Support dir) needs an explicit, minimal entitlement. Guessing at entitlements and iterating via App Store rejection is slow (each Apple review cycle is 24–48h); enumerating actual IPC/filesystem/network usage up front against the source is faster and produces a minimal, review-friendly entitlement set (Apple review scrutinizes entitlement scope-creep, e.g. requesting <code>com.apple.security.files.all</code> gets challenged).</p>\n<p><strong>Why gate on a NEW repo variable <code>MAS_RELEASE</code> rather than reusing <code>MOBILE_RELEASE</code> (§1 #5)?</strong> <code>MOBILE_RELEASE</code> governs the Capacitor iOS/Android jobs, which are architecturally unrelated to the Tauri desktop build this task extends. Coupling them would mean flipping mobile release on/off also toggles the (unrelated, prerequisite-gated) Mac App Store job, which is exactly the kind of accidental coupling TASK-IMP-071's \"durable release trigger\" work (see <code>related_tasks</code> — not formally linked here since it predates this task and isn't itself gating this work) was designed to avoid.</p>\n<p><strong>Why does this task explicitly refuse to acquire Apple Developer Program access (§1 #7)?</strong> This mirrors the standing constraint that account creation and credential entry are not actions an agent performs on the user's behalf. Recording this explicitly in the task (rather than silently omitting it) means a future implementer reading this spec in isolation — without the chat history that produced it — still knows exactly what's blocked and why, rather than discovering the blocker mid-implementation.</p>\n<h2>§3 — API contract</h2>\n<p><code>apps/desktop/src-tauri/tauri.mas.conf.json</code> (overlay, deep-merged over <code>tauri.conf.json</code> at build time via <code>tauri build --config tauri.mas.conf.json</code>):</p>\n<pre><code class=\"language-json\">{\n  \"bundle\": {\n    \"macOS\": {\n      \"entitlements\": \"Entitlements.mas.plist\",\n      \"signingIdentity\": \"3rd Party Mac Developer Application\",\n      \"providerShortName\": null,\n      \"hardenedRuntime\": false\n    }\n  }\n}</code></pre>\n<p><code>apps/desktop/src-tauri/Entitlements.mas.plist</code> (initial minimal set — expanded only per §6 audit findings, never speculatively):</p>\n<pre><code class=\"language-xml\">&lt;?xml version=\"1.0\" encoding=\"UTF-8\"?&gt;\n&lt;!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\" \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\"&gt;\n&lt;plist version=\"1.0\"&gt;\n&lt;dict&gt;\n    &lt;key&gt;com.apple.security.app-sandbox&lt;/key&gt;\n    &lt;true/&gt;\n    &lt;key&gt;com.apple.security.network.client&lt;/key&gt;\n    &lt;true/&gt;\n    &lt;key&gt;com.apple.security.files.user-selected.read-write&lt;/key&gt;\n    &lt;true/&gt;\n&lt;/dict&gt;\n&lt;/plist&gt;</code></pre>\n<p><code>apps/desktop/src-tauri/Entitlements.mas.inherit.plist</code> (attached to sidecar/child processes only — Tauri config field <code>bundle.macOS.entitlements</code> covers the main app binary; child-process entitlements are applied via a post-build <code>codesign --entitlements Entitlements.mas.inherit.plist</code> pass wired into the CI packaging step, since Tauri v2 does not expose a first-class config field for child-process entitlements as of this writing):</p>\n<pre><code class=\"language-xml\">&lt;?xml version=\"1.0\" encoding=\"UTF-8\"?&gt;\n&lt;!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\" \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\"&gt;\n&lt;plist version=\"1.0\"&gt;\n&lt;dict&gt;\n    &lt;key&gt;com.apple.security.app-sandbox&lt;/key&gt;\n    &lt;true/&gt;\n    &lt;key&gt;com.apple.security.inherit&lt;/key&gt;\n    &lt;true/&gt;\n&lt;/dict&gt;\n&lt;/plist&gt;</code></pre>\n<p><code>.github/workflows/release-mas.yml</code> (new, gated job — skeleton showing the CI contract, not the full workflow file):</p>\n<pre><code class=\"language-yaml\">name: release-mas\non:\n  workflow_dispatch:\njobs:\n  assert-mas-gate-inert:\n    # Runs UNCONDITIONALLY (no `if:`), so AC #6 has something to assert against —\n    # this job's own success is meaningless in isolation; its purpose is to give\n    # `gh run view` a completed workflow run in which build-and-submit-mas's\n    # conclusion (skipped vs ran) can be inspected regardless of MAS_RELEASE state.\n    runs-on: ubuntu-latest\n    steps:\n      - run: echo \"workflow executed; see build-and-submit-mas conclusion for gate state\"\n\n  build-and-submit-mas:\n    if: vars.MAS_RELEASE == 'true'\n    runs-on: macos-14\n    steps:\n      - uses: actions/checkout@v4\n      - name: Import MAS signing certificates into CI keychain\n        env:\n          MAS_APP_CERT_P12_BASE64: ${{ secrets.MAS_APP_CERT_P12_BASE64 }}\n          MAS_INSTALLER_CERT_P12_BASE64: ${{ secrets.MAS_INSTALLER_CERT_P12_BASE64 }}\n          MAS_CERT_PASSWORD: ${{ secrets.MAS_CERT_PASSWORD }}\n          MAS_KEYCHAIN_PASSWORD: ${{ secrets.MAS_KEYCHAIN_PASSWORD }}\n        run: |\n          security create-keychain -p \"$MAS_KEYCHAIN_PASSWORD\" mas-build.keychain\n          security set-keychain-settings -lut 21600 mas-build.keychain\n          security unlock-keychain -p \"$MAS_KEYCHAIN_PASSWORD\" mas-build.keychain\n          for cert_var in MAS_APP_CERT_P12_BASE64 MAS_INSTALLER_CERT_P12_BASE64; do\n            echo \"${!cert_var}\" | base64 --decode &gt; /tmp/\"$cert_var\".p12\n            security import /tmp/\"$cert_var\".p12 -k mas-build.keychain -P \"$MAS_CERT_PASSWORD\" \\\n              -T /usr/bin/codesign -T /usr/bin/productbuild\n            rm /tmp/\"$cert_var\".p12\n          done\n          security list-keychains -d user -s mas-build.keychain login.keychain\n          security set-key-partition-list -S apple-tool:,apple: -s -k \"$MAS_KEYCHAIN_PASSWORD\" mas-build.keychain\n      - name: Build MAS bundle\n        run: |\n          cd apps/desktop\n          npx tauri build --config src-tauri/tauri.mas.conf.json --bundles app\n      - name: Sign child-process entitlements\n        env:\n          MAS_APP_SIGNING_IDENTITY: ${{ secrets.MAS_APP_SIGNING_IDENTITY }}   # e.g. \"3rd Party Mac Developer Application: CyberSkill Software Solutions Consultancy and Development Joint Stock Company (TEAMID)\"\n        run: |\n          codesign --force --deep --entitlements src-tauri/Entitlements.mas.inherit.plist \\\n            --sign \"$MAS_APP_SIGNING_IDENTITY\" \\\n            src-tauri/target/release/bundle/macos/CyberOS.app/Contents/MacOS/*\n      - name: Build pkg installer\n        env:\n          MAS_INSTALLER_SIGNING_IDENTITY: ${{ secrets.MAS_INSTALLER_SIGNING_IDENTITY }}   # e.g. \"3rd Party Mac Developer Installer: &lt;same org, different cert type&gt;\"\n        run: |\n          productbuild --component src-tauri/target/release/bundle/macos/CyberOS.app /Applications \\\n            --sign \"$MAS_INSTALLER_SIGNING_IDENTITY\" \\\n            CyberOS.pkg\n      - name: Upload to App Store Connect\n        env:\n          APP_STORE_CONNECT_KEY: ${{ secrets.ASC_API_KEY }}\n          APP_STORE_CONNECT_KEY_ID: ${{ secrets.ASC_KEY_ID }}\n          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}\n        run: |\n          xcrun altool --upload-app -f CyberOS.pkg -t macos \\\n            --apiKey \"$APP_STORE_CONNECT_KEY_ID\" --apiIssuer \"$APP_STORE_CONNECT_ISSUER_ID\"\n      - name: Clean up keychain\n        if: always()\n        run: security delete-keychain mas-build.keychain || true</code></pre>\n<p>Two distinct certificate types are deliberately kept as two distinct secrets (<code>MAS_APP_SIGNING_IDENTITY</code> for <code>codesign</code>, <code>MAS_INSTALLER_SIGNING_IDENTITY</code> for <code>productbuild</code>) rather than one team-name secret interpolated into two identity strings — see §11 for why collapsing these into a single secret is the most common first-time mistake in Mac App Store CI pipelines.</p>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Sandbox audit is exhaustive</strong> — every <code>#[tauri::command]</code> handler and every direct filesystem/network/process call in <code>apps/desktop/src-tauri/src/</code> is listed in a table (new file <code>docs/deploy/mac-app-store-submission.md</code> §\"Sandbox surface audit\") with a disposition of <code>sandbox-native</code>, <code>entitlement:&lt;name&gt;</code>, or <code>gated-behind-MAS-feature-flag</code>.</li><li><strong>Entitlements are minimal</strong> — <code>Entitlements.mas.plist</code> contains no entitlement without a corresponding row in the sandbox surface audit table justifying it; a CI lint (new script, <code>tools/mas-entitlement-lint.sh</code>) fails the build if an entitlement key exists in the plist with no matching audit-table row.</li><li><strong>Two build targets stay independent</strong> — running <code>tauri build</code> (no <code>--config</code> flag, the existing Developer ID path) produces byte-for-byte the same <code>bundle.macOS</code> output as before this task merged, verified by a CI diff step comparing <code>tauri.conf.json</code>'s resolved <code>bundle.macOS</code> block pre/post this task.</li><li><strong>Pkg is correctly signed</strong> — <code>pkgutil --check-signature CyberOS.pkg</code> reports a valid \"3rd Party Mac Developer Installer\" signature chain in the CI job's log, when <code>MAS_RELEASE=true</code> and real certificates are present.</li><li><strong>Sandbox validity check passes locally</strong> — <code>codesign --verify --deep --strict CyberOS.app</code> and <code>spctl -a -t install --context context:primary-signature CyberOS.app</code> both exit 0 against a locally-built MAS bundle before any CI wiring is trusted.</li><li><strong>CI job is inert by default</strong> — with <code>MAS_RELEASE</code> unset or <code>false</code> (the repo's current state), <code>release-mas.yml</code>'s <code>build-and-submit-mas</code> job is skipped entirely (verified via a CI run showing the job as <code>skipped</code>, not <code>failed</code>).</li><li><strong>Answer sheet is complete</strong> — <code>docs/deploy/mac-app-store-submission.md</code> has a filled-in row for every App Store Connect macOS submission field enumerated in Apple's macOS app submission checklist (export compliance, content rights, age rating, macOS privacy usage strings actually requested by CyberOS's entitlements from AC #1's audit table), each marked <code>human-confirmed</code> (Stephen) or <code>not-applicable</code> with a one-sentence reason.</li><li><strong>No credential material committed</strong> — a CI secret-scan step (reusing the existing repo secret-scanning gate referenced in <code>docs/deploy/web-and-desktop-deploy.md</code>) passes against every file this task adds.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># AC #3 — Developer ID build target unaffected.\n# Uses a disposable worktree checked out at the pre-task commit rather than\n# `git stash`, so the comparison can never lose or clobber uncommitted work\n# in the primary working tree.\ncd /sessions/ecstatic-sharp-lovelace/mnt/cyberos\nPRE_FR_SHA=$(git merge-base HEAD \"$(git log --diff-filter=A -- \\\n  apps/desktop/src-tauri/tauri.mas.conf.json | tail -1)^\")\ngit worktree add /tmp/pre-fr-worktree \"$PRE_FR_SHA\"\n( cd /tmp/pre-fr-worktree/apps/desktop/src-tauri &amp;&amp; \\\n  npx tauri build --bundles app --ci --config /dev/null &gt; /tmp/pre-fr-bundle.json )\n( cd apps/desktop/src-tauri &amp;&amp; \\\n  npx tauri build --bundles app --ci --config /dev/null &gt; /tmp/post-fr-bundle.json )\ndiff /tmp/pre-fr-bundle.json /tmp/post-fr-bundle.json  # MUST be empty\ngit worktree remove /tmp/pre-fr-worktree\n\n# AC #5 — local sandbox validity (macOS runner only)\ncodesign --verify --deep --strict target/release/bundle/macos/CyberOS.app\necho \"exit: $?\"  # MUST be 0\nspctl -a -t install --context context:primary-signature target/release/bundle/macos/CyberOS.app\necho \"exit: $?\"  # MUST be 0 once real MAS certs are present; documented as expected-fail with reason otherwise\n\n# AC #2 — entitlement lint\ntools/mas-entitlement-lint.sh apps/desktop/src-tauri/Entitlements.mas.plist \\\n  docs/deploy/mac-app-store-submission.md\n# exits 1 and prints the unjustified entitlement key if audit-table coverage is incomplete</code></pre>\n<pre><code class=\"language-yaml\"># AC #6 — CI job inert-by-default, as a workflow assertion (excerpt from a test workflow run)\n- name: Assert MAS job skipped when MAS_RELEASE unset\n  run: |\n    gh run view ${{ github.run_id }} --json jobs -q '.jobs[] | select(.name==\"build-and-submit-mas\") | .conclusion' | grep -q skipped</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton — the two config files, the CI job YAML, and the entitlement-lint script are the entirety of the net-new surface. No new Rust code is required by this task; the sandbox audit in AC #1 may surface follow-up tasks if a specific IPC command turns out to need refactoring for sandbox compatibility, but that refactor is explicitly out of scope here — see §9.)</p>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none — this task reads the existing <code>apps/desktop/src-tauri/tauri.conf.json</code> and <code>.github/workflows/release.yml</code> but does not require any other task to land first.</li><li><strong>Downstream:</strong> none currently — a future task to fix any sandbox-incompatible IPC command found by AC #1's audit would depend on this task's audit table existing, but no such task is drafted yet (§9).</li><li><strong>Cross-module:</strong> none.</li><li><strong>Human/account prerequisites (not task dependencies, but hard blockers on <code>MAS_RELEASE=true</code> ever being flippable):</strong> Apple Developer Program enrollment (Stephen), macOS platform added to the CyberOS App Store Connect app record (Stephen), \"3rd Party Mac Developer Application\" + \"3rd Party Mac Developer Installer\" certificates issued and stored as CI secrets (Stephen).</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>Sandbox surface audit table (excerpt, illustrative — the real table is populated during implementation from the actual source tree, not fabricated here per anti-fabrication discipline):</p>\n<pre><code class=\"language-markdown\">| Symbol | File:line | Capability used | Disposition |\n|---|---|---|---|\n| `&lt;pending — populated during implementation from actual src/ audit&gt;` | | | |</code></pre>\n<p><code>mas-entitlement-lint.sh</code> failure output shape:</p>\n<pre><code>ERROR: Entitlements.mas.plist declares \"com.apple.security.files.downloads.read-write\"\n  but no row in docs/deploy/mac-app-store-submission.md §\"Sandbox surface audit\"\n  justifies it. Add a row or remove the entitlement.\nexit 1</code></pre>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Apple Developer Program enrollment status</strong> — deferred to Stephen; blocks <code>MAS_RELEASE=true</code> entirely. <code>human-confirmed</code> marker required before this flag can ever be set.</li><li><strong>Whether CyberOS's Rust backend spawns any child processes today</strong> (relevant to whether <code>Entitlements.mas.inherit.plist</code> is load-bearing or defensive-only) — deferred to the AC #1 sandbox audit, to be resolved during implementation by reading <code>apps/desktop/src-tauri/src/</code> rather than guessed here.</li><li><strong>Whether any sandbox-incompatible capability is found by the AC #1 audit that has no entitlement equivalent</strong> (e.g. arbitrary-path filesystem access outside the sandbox container, if CyberOS's desktop app does anything like reading files a user hasn't explicitly selected via an Open panel) — if found, this becomes a follow-up task to gate that capability behind a \"MAS build\" feature flag; not resolved here since it depends on the audit's outcome.</li><li><strong>App Store Connect macOS app record bundle ID</strong> — whether it reuses <code>os.cyberskill.world.desktop</code> (the existing Tauri identifier) or requires a distinct one is an App Store Connect account-setup decision for Stephen, not an engineering decision this spec can make.</li><li><strong><code>related_tasks</code> forward-references</strong> — this task lists TASK-APP-004/005/006 in <code>related_tasks</code> even though they are drafted in the same authoring batch immediately after this one (all five approved together in a single PLAN). This is a deliberate same-batch forward reference, not a placeholder-task situation under §3.1 rule #3 (that rule scopes to <code>depends_on:</code>/<code>blocks:</code>, both empty here) — resolved once the batch completes and all referenced tasks exist on disk.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Sandbox audit misses an IPC command that touches an un-entitled path</td><td><code>spctl -a -t install</code> fails at local verification (AC #5) with a sandbox violation in <code>log show --predicate 'subsystem == \"com.apple.sandbox\"'</code></td><td>Local build verification catches it before any CI/App Store submission</td><td>Add the missing entitlement or gate the command behind a MAS feature flag; re-run AC #1 audit</td></tr><tr><td>Entitlement lint passes locally but a new IPC command is added later without updating the audit table</td><td>CI entitlement-lint step (wired into the same PR gate as other repo lints) fails on the PR that added the new command, if the PR also touches <code>Entitlements.mas.plist</code> — otherwise silently drifts</td><td>Entitlement stays broader than actual usage (over-permissioned, App Store review risk) until caught</td><td>Periodic re-audit; add entitlement-lint as a required check on any PR touching <code>src-tauri/src/</code> per §11</td></tr><tr><td>Developer ID and MAS builds silently diverge because someone edits <code>tauri.conf.json</code> assuming the MAS overlay auto-inherits changes</td><td>AC #3's diff check fails in CI</td><td>MAS build could ship stale config (e.g. old <code>productName</code>)</td><td>CI gate blocks merge; developer manually reconciles <code>tauri.mas.conf.json</code></td></tr><tr><td><code>codesign --deep</code> re-signs child-process binaries with the wrong entitlements because Tauri's build output layout changes in a future Tauri version</td><td>AC #5 local verification fails after a <code>tauri</code> CLI upgrade</td><td>MAS build silently ships without sandbox inheritance on subprocesses</td><td>Pin Tauri CLI version in CI; re-verify <code>Entitlements.mas.inherit.plist</code> application path after any Tauri upgrade</td></tr><tr><td>Apple rejects the submission for entitlement over-scoping despite the lint passing (lint only checks \"is it justified in our own docs\", not \"does Apple agree it's minimal\")</td><td>App Store Connect review rejection email (external signal, not a repo-internal detection)</td><td>Submission delayed 24–48h per review cycle</td><td>Narrow the entitlement per Apple's specific rejection reason; this is expected iteration, not a design flaw</td></tr><tr><td><code>MAS_RELEASE=true</code> is flipped before certificates exist in CI secrets</td><td>CI job fails at the <code>codesign</code> step with \"no identity found\"</td><td>Job fails loudly, no partial/corrupt artifact produced</td><td>AC #6 explicitly tests the <code>false</code>/unset state; the <code>true</code>-but-no-certs state is an operator error, not a spec gap — job failure is the correct behavior</td></tr><tr><td>The <code>.pkg</code> installer's component plist references the wrong install location (not <code>/Applications</code>)</td><td><code>productbuild</code> step produces a pkg that fails <code>pkgutil --check-signature</code> or installs to an unexpected path in manual QA</td><td>User-facing install failure or wrong install location if ever manually distributed outside App Store Connect</td><td>Component plist is generated by <code>productbuild --component &lt;app&gt; /Applications</code> per §3 contract — path is hardcoded correctly in the CI step, verified by AC #4</td></tr><tr><td>Two build targets' <code>productName</code>/<code>version</code> drift because <code>tauri.mas.conf.json</code> doesn't override <code>version</code> and the base <code>tauri.conf.json</code>'s version bump (via the repo's stamper, TASK-IMP-072) isn't picked up by the overlay merge</td><td>Version mismatch visible in App Store Connect build processing vs. the GitHub Release tag</td><td>Confusing version numbers across distribution channels for end users comparing \"which build is newer\"</td><td><code>tauri.mas.conf.json</code> MUST NOT declare its own <code>version</code> field — Tauri's config merge takes the base file's <code>version</code> when the overlay omits it, so this is a \"don't add the field\" discipline, verified by AC #3's diff check implicitly (adding a version field to the overlay would show up as a diff)</td></tr><tr><td>Secret scan (AC #8) has a false negative because App Store Connect API key JSON is base64-encoded inline in a workflow file instead of referenced via <code>secrets.*</code></td><td>Manual code review catches it, or it ships to a public repo</td><td>Credential leak</td><td>This task's <code>disallowed_tools</code> explicitly forbids entering credential material anywhere non-secret-manager; the <code>release-mas.yml</code> skeleton in §3 uses <code>${{ secrets.* }}</code> exclusively as the only sanctioned pattern</td></tr><tr><td><code>MOBILE_RELEASE</code> and <code>MAS_RELEASE</code> get accidentally coupled in a future refactor of <code>release.yml</code> that \"simplifies\" the release gating</td><td>Any future PR touching release gating that doesn't preserve independent boolean gates would need to pass AC #6's inert-by-default test for <code>MAS_RELEASE</code> specifically</td><td>Mac App Store job runs unexpectedly when mobile release is flipped on, before certs exist</td><td>AC #6's CI assertion is a standing regression test, not a one-time check — it runs on every workflow change</td></tr><tr><td>The ephemeral <code>mas-build.keychain</code> created per-CI-run isn't deleted if the job fails before the cleanup step, leaving orphaned keychains on a (hypothetically) reused self-hosted runner</td><td>Not detectable on GitHub-hosted <code>macos-14</code> runners (fresh VM per run, so this is moot there); would surface as keychain-list pollution if ever migrated to a self-hosted runner</td><td>No functional impact on GitHub-hosted runners; latent risk only if the runner strategy changes</td><td><code>if: always()</code> on the cleanup step (§3) covers the common failure case; a self-hosted-runner migration would need an additional pre-job keychain-sweep step, out of scope while runners stay GitHub-hosted</td></tr><tr><td><code>security import</code> succeeds but <code>security set-key-partition-list</code> fails silently on a keychain ACL edge case (documented macOS <code>security</code> CLI quirk on certain Xcode/macOS version combinations)</td><td><code>codesign</code> step fails immediately after with a keychain-access prompt hang or <code>errSecInternalComponent</code>, since CI has no interactive session to approve the ACL prompt</td><td>Build fails at the signing step, no partial artifact produced</td><td>Pin the <code>macos-14</code> runner image version in <code>release-mas.yml</code>; this is a known class of CI flake independent of this task's design, mitigated by image pinning rather than a code fix</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong><code>providerShortName</code> casing:</strong> Tauri v2's <code>bundle.macOS</code> config key is <code>providerShortName</code> (camelCase) even though this task's §1 #1 prose used a space for readability — the §3 JSON contract uses the correct camelCase key. This is a real Tauri config field, used when the Apple Developer Team has multiple providers associated (agencies signing on behalf of multiple clients); CyberSkill likely doesn't need it (<code>null</code> is valid and means \"use the default provider\"), but the field is included in the contract for completeness since getting it wrong silently produces the wrong provider in App Store Connect for teams that do need it.</li><li><strong>Why <code>hardenedRuntime: false</code> in the MAS overlay:</strong> the Hardened Runtime entitlement is for Developer-ID-distributed, notarized apps outside the App Store — Mac App Store apps use App Sandbox instead, and enabling both simultaneously is redundant/conflicting per Apple's own guidance. The Developer ID build (unaffected by this task) presumably already has <code>hardenedRuntime: true</code> for notarization; this overlay explicitly turns it off for the MAS target rather than leaving it ambiguous.</li><li><strong>Two-cert signing (app cert + separate installer cert) is not optional</strong> — this is the single most common first-time mistake in Mac App Store CI pipelines (using one cert for both <code>codesign</code> and <code>productbuild</code>). The §3 CI skeleton uses two distinct <code>secrets.MAS_TEAM_NAME</code>-scoped identity strings deliberately to make this explicit in the contract, even though both resolve to the same team name string in practice — the identity <em>type prefix</em> (<code>3rd Party Mac Developer Application</code> vs <code>3rd Party Mac Developer Installer</code>) is what differs and what a copy-paste error would most likely get wrong.</li><li><strong>This task deliberately does not attempt to resolve the sandbox-audit findings</strong> — AC #1 requires the audit table to exist and be exhaustive, but any IPC command found to be sandbox-incompatible is explicitly out of scope for <em>this</em> task to fix (§9). This keeps the task's own scope bounded to \"can we build and submit a sandboxed bundle at all,\" with any deeper Rust refactoring work spun into its own future task once the audit reveals whether such work is even needed — writing that follow-up task speculatively now would violate the anti-fabrication discipline (inventing scope for work whose necessity isn't yet known).</li></ul>\n<p><em>End of TASK-APP-003.</em></p>";
