window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EVAL-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The evaluation engine <strong>MUST</strong> turn captured platform evidence into an <em>evidence-linked, human-approved</em> assessment of a consented subject against the FR-EVAL-002 rubric. GENIE (Lumi, on the ai-gateway) drafts; a human decides. The model assists a decision — it never makes one that affects a person's pay, progression, or employment. The contract:</p>\n<ol><li><strong>MUST</strong> run on a cadence AND on demand. A scheduled run (default: per review cycle, configurable per tenant) and an explicit <code>POST /v1/eval/assessments</code> MUST both produce the same artifact through the same path. The cadence MUST NOT bypass any gate in this section.</li></ol>\n<ol><li><strong>MUST</strong> gate every draft on FR-EVAL-001 consent. Before GENIE is invoked for a subject, the engine MUST verify the subject has an <em>acknowledged</em> FR-EVAL-001 monitoring notice on record (<code>access::has_acknowledged_notice(subject_id, tenant_id)</code>). No acknowledgement → <code>403 not_consented</code>, no recall, no draft, and a <code>eval.draft_refused_no_consent</code> audit row. This is <strong>ACCESS-RESTRICTED + CONTRACT-DISCLOSED</strong> (DEC-2510).</li></ol>\n<ol><li><strong>MUST</strong> retrieve evidence via FR-MEMORY-123 brain recall only. For a consented subject, the engine MUST pull relevant work-interaction events (with provenance: event id, kind, occurred-at, source module, tenant) from MEMORY recall. It MUST NOT read private-life data, keystroke/screen surveillance, or any non-platform source. Recall scope MUST be the subject's own platform interactions within the review window.</li></ol>\n<ol><li><strong>MUST</strong> map evidence to rubric items and draft an assessment. GENIE MUST, per rubric clause from the active FR-EVAL-002 rubric version, select the supporting evidence events and draft a score with a rationale that quotes/refers to those events. The output is a set of <code>ScoreRow</code>s, one per scored rubric clause.</li></ol>\n<ol><li><strong>MUST</strong> enforce citation coverage at the boundary (DEC-2513). Every <code>ScoreRow</code> MUST carry: (a) <code>rubric_clause_id</code> (exactly one clause, from the pinned <code>rubric_version</code>), and (b) <code>evidence_refs</code> (≥ 1 <code>EvidenceRef</code>, each a real MEMORY event id resolvable in the subject's recall set). A <code>ScoreRow</code> with zero evidence refs or no rubric clause MUST be rejected with <code>422 uncited_score</code> and MUST NOT persist. There are no unsourced scores.</li></ol>\n<ol><li><strong>MUST</strong> produce a DRAFT only — never a final result. A GENIE run creates an <code>assessment</code> in state <code>draft</code>, then transitions it to <code>pending_review</code>. The legal states are exactly: <code>draft → pending_review → {approved | changed | rejected}</code> (DEC-2511). No other terminal state exists. <code>approved</code>, <code>changed</code>, and <code>rejected</code> are the only states reachable solely by a human reviewer.</li></ol>\n<ol><li><strong>MUST</strong> enforce a MANDATORY human-in-the-loop gate (DEC-2511, DEC-2512). An assessment MUST NOT become final (<code>approved</code> or <code>changed</code>) without a <code>reviewer_subject_id</code> distinct from the subject, recorded on the transition. The transition into any final state MUST be initiated by a human actor through <code>POST /v1/eval/assessments/:id/{approve|change|reject}</code>; the GENIE service identity MUST NOT be accepted as the reviewer. There is no configuration, flag, or fast-path that finalizes an assessment without a human.</li></ol>\n<ol><li><strong>MUST</strong> treat consequential outcomes as a HARD GATE (DEC-2512). Any field of the assessment marked <code>consequential</code> (a recommendation that affects pay, progression, level, bonus, PIP, or continued employment) MUST be <code>null</code>/inert until a human reviewer sets it on an <code>approve</code>/<code>change</code> transition with <code>consequential_ack = true</code>. The engine MUST NOT auto-apply a consequential outcome to any downstream system (payroll, HRIS, level/title), and MUST NOT expose a consequential recommendation as actionable while the assessment is <code>draft</code> or <code>pending_review</code>. The model proposes; only an approving human's explicit act makes it real.</li></ol>\n<ol><li><strong>MUST</strong> let the subject view and respond to their own assessment. Once an assessment is <code>pending_review</code> or later, the subject MUST be able to read it (their own, in full, with evidence and rationale) and submit a <code>rebuttal</code> (free text + optional clause-level disagreements) via <code>POST /v1/eval/assessments/:id/rebut</code>. The rebuttal MUST be captured immutably, surfaced to the reviewer before they finalize, and recorded in the audit chain. A pending rebuttal MUST block transition to <code>approved</code> until the reviewer has seen it (reviewer sets <code>rebuttal_considered = true</code>).</li></ol>\n<ol><li><strong>MUST</strong> write the final result AND every state change to <code>l1_audit_log</code> (DEC-2515) via the FR-MEMORY chain. Each transition emits one append-only audit row: <code>eval.assessment_drafted</code>, <code>eval.assessment_pending_review</code>, <code>eval.assessment_approved</code>, <code>eval.assessment_changed</code>, <code>eval.assessment_rejected</code>, <code>eval.assessment_rebutted</code>, <code>eval.fallback_no_score</code>, <code>eval.draft_refused_no_consent</code>. Each row MUST carry <code>{assessment_id, subject_id, actor_subject_id, rubric_version, prior_state, new_state, evidence_event_ids, score_digest, change_summary, trace_id}</code>. The chain is the defensible record.</li></ol>\n<ol><li><strong>MUST</strong> run GENIE via the ai-gateway with spend caps + residency + ZDR (DEC-2515). All model calls MUST route through the ai-gateway <code>eval.score</code> policy: per-tenant spend cap enforced, data residency honored (no cross-region egress of personal data), zero-data-retention (ZDR) on so the provider does not retain or train on the evidence. A direct provider call bypassing the gateway MUST be impossible from this service (no provider keys in the eval service).</li></ol>\n<ol><li><strong>MUST</strong> fall back to \"no score, flag for full human review\" on any model failure (DEC-2514). If GENIE is offline, times out, returns malformed output, or returns a score for a clause it cannot ground in evidence, the engine MUST NOT fabricate or default a number. It MUST mark the affected clause(s) <code>score = null, status = needs_human_review</code>, emit <code>eval.fallback_no_score</code>, and route the whole assessment to a human with the failure reason. The safe state is \"no score\", reached by flagging, never by guessing.</li></ol>\n<ol><li><strong>MUST</strong> refuse-to-score at the prompt boundary. The <code>score_v1</code> prompt MUST instruct GENIE to return <code>unsupported</code> for any rubric clause it cannot ground in the recalled evidence, rather than inferring a score from absence of evidence or from the subject's identity. An <code>unsupported</code> return maps to clause status <code>needs_human_review</code>, not to a low score.</li></ol>\n<ol><li><strong>MUST</strong> apply bias/fairness guardrails. Before scoring, <code>fairness.py</code> MUST blind protected attributes (name, gender, age, ethnicity, nationality, disability, and any field marked protected in the tenant config) from the evidence text passed to the model, scoring on behavior and output, not identity. After a batch, a group-disparity check MUST compare score distributions across protected groups and, on a configured threshold breach, flag the batch for human fairness review (<code>eval.disparity_flagged</code>) — the flag informs a human; it does not auto-adjust scores.</li></ol>\n<ol><li><strong>MUST</strong> provide an appeal path beyond the rebuttal. A subject MUST be able to escalate a finalized assessment to an appeal (<code>POST /v1/eval/assessments/:id/appeal</code>) that routes to a second human reviewer (not the original reviewer) and is recorded as <code>eval.assessment_appealed</code>. An appeal MUST be possible on any <code>approved</code>/<code>changed</code> assessment within the tenant-configured appeal window.</li></ol>\n<ol><li><strong>MUST</strong> RLS-enforce access (FR-AUTH-003 + FR-EVAL-001 access rules). An assessment row MUST be visible only to: the founder, the subject's manager chain, and the subject themselves (their own). No peer, no unrelated manager, no cross-tenant read. RLS MUST be the spine; the <code>access.rs</code> visibility check is defense-in-depth on top. Consequential recommendations MUST be visible to reviewers/founder only until finalized.</li></ol>\n<ol><li><strong>MUST</strong> pin the rubric version per assessment. Each assessment records the exact <code>rubric_version</code> (FR-EVAL-002) it was scored against, so re-reading an old assessment shows what \"good\" meant at scoring time. A new rubric version MUST NOT silently re-interpret past scores.</li></ol>\n<ol><li><strong>MUST</strong> make the score store append-only-on-finalize. <code>assessment_score</code> rows for a finalized assessment MUST NOT be mutated; a reviewer \"change\" creates a new score-revision linked to the prior, preserving the GENIE draft alongside the human-changed value (the diff is the point — \"GENIE said 3, manager changed to 4 because …\").</li></ol>\n<ol><li><strong>MUST</strong> record reviewer rationale on <code>change</code>/<code>reject</code>. A <code>change</code> or <code>reject</code> transition MUST carry a human <code>reviewer_rationale</code> (non-empty); a finalization that overrides GENIE without a stated reason MUST be refused with <code>422 rationale_required</code>. The override rate and its reasons are a first-class fairness signal.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics: <code>eval_assessments_drafted_total{tenant}</code>, <code>eval_assessments_finalized_total{outcome}</code> (outcome ∈ approved | changed | rejected), <code>eval_override_rate</code> (changed ÷ finalized), <code>eval_citation_coverage_ratio</code> (scores-with-valid-citations ÷ total scores; MUST be 1.0 by invariant — any dip is an alarm), <code>eval_fallback_no_score_total</code>, <code>eval_disparity_flagged_total</code>, <code>eval_rebuttals_total</code>, <code>eval_appeals_total</code>.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a draft-only model output with a mandatory human gate (DEC-2511, DEC-2512)?</strong> This is the line between a useful assistant and an unaccountable automated decision about a person's livelihood. The governance plan (Phase 4, \"Doing the monitoring responsibly\") is explicit: <em>\"Lumi drafts and surfaces evidence; a person decides anything that affects pay, progression, or employment. Never let the model auto-decide those.\"</em> The state machine encodes that as structure, not policy: there is literally no transition into a final state that the GENIE identity can take, and consequential fields are inert until a human acts. A reviewer can't be skipped because there's no edge that skips them.</p>\n<p><strong>Why citation coverage as a boundary invariant (DEC-2513)?</strong> An evaluation a person cannot contest is not an evaluation — it's an accusation. Every score that cites the specific events and the specific rubric clause is falsifiable: the subject can look at the same events and argue, the reviewer can check the inference, and an appeal has something concrete to examine. Rejecting an uncited score at the write boundary (not \"warning\" on it) is what makes \"no unsourced scores\" a guarantee rather than an aspiration. It also defangs the model's biggest failure mode — confident assertion without basis — because the structure won't store it.</p>\n<p><strong>Why the consent gate before recall (DEC-2510)?</strong> Disclosed monitoring is the defensible path; covert monitoring is the risky one. Vietnam's PDPD (13/2023/ND-CP) expects a lawful basis, notice, and purpose limitation, and the Labor Code (45/2019/QH14) governs the relationship the monitoring sits inside. Gating <em>recall itself</em> on an acknowledged notice means the system structurally cannot evaluate someone who was never told — the notice isn't a checkbox after the fact, it's the precondition for the pipeline running at all.</p>\n<p><strong>Why the \"no score, flag for human\" fallback (DEC-2514)?</strong> A fabricated score is worse than no score because it looks authoritative. When the model is offline or can't ground a clause, the only safe output is an explicit gap routed to a person — the same philosophy as FR-CUO-204's \"the safe state is 'no change applied'.\" Defaulting to a low score on missing evidence would punish people for the model's blind spots; defaulting to a high score would launder a non-answer. Both are wrong; <code>needs_human_review</code> is the truthful state.</p>\n<p><strong>Why route exclusively through the ai-gateway with ZDR (DEC-2515)?</strong> The evidence is sensitive employment data about real people. ZDR-on means the model provider doesn't retain or train on it; residency means it doesn't leave the permitted region; the spend cap means a runaway batch can't burn the budget. Keeping provider keys out of the eval service makes the gateway the only door — there's no side channel for a future change to accidentally leak the evidence to an unbounded provider.</p>\n<p><strong>Why the subject sees their record and can rebut + appeal (§1 #9, #15)?</strong> Transparency here is a feature, not an afterthought (governance plan, Phase 5). A person who can read the evidence behind their score, disagree in writing before it's finalized, and escalate to a second reviewer afterward is a person who trusts the system enough to keep using it — which raises data quality, which makes the evaluations better. The rebuttal blocking finalization-until-seen guarantees the reviewer can't rubber-stamp around the disagreement.</p>\n<p><strong>Why blind protected attributes + a disparity check (§1 #14)?</strong> The contracts define performance and compliance; identity is not one of the inputs. Blinding name/gender/age/ethnicity before the model scores keeps the assessment on behavior and output. The post-hoc disparity check is a smoke detector: if approved scores skew across protected groups beyond a threshold, a human looks — the system flags, it never silently \"corrects\" scores (which would be its own bias).</p>\n<p><strong>Why append-only-on-finalize with revisions, not in-place edits (§1 #18)?</strong> The diff between what GENIE drafted and what the human decided is the most valuable artifact for fairness auditing and for improving the rubric. \"GENIE said 3, manager changed to 4 because the client email shows the deadline moved\" is a record that teaches. Mutating in place would destroy exactly the signal that makes the human-in-the-loop loop auditable.</p>\n<p><strong>Why reviewer rationale is mandatory on override (§1 #19)?</strong> A high override rate without reasons could mean the rubric is wrong, the model is wrong, or a reviewer is rubber-stamping or freelancing. Forcing a stated reason on every <code>change</code>/<code>reject</code> turns the override rate into a diagnosable signal and creates the defensible \"why\" a grievance review will ask for.</p>\n<p><strong>Why pin the rubric version (§1 #17)?</strong> Rubrics evolve. An assessment scored against v3 must keep meaning v3 forever, or every historical review silently rewrites itself when the rubric changes. Pinning makes the record stable and the comparison honest.</p>\n<p><strong>Why a dedicated <code>services/eval</code> Rust+Python split?</strong> The state machine, the citation invariant, the access/consent gate, and the audit emit are correctness-critical and benefit from Rust's type system and the existing per-tenant RLS+sqlx patterns (mirrors FR-PROJ-008, FR-CHAT). The GENIE inference orchestration (recall → prompt → parse) is Python because that's where the ai-gateway client and the prompt live (mirrors the CUO/modules layout). The boundary is clean: Python proposes scores, Rust validates and persists them — and Rust is where every gate lives, so a Python bug can't finalize anything.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- services/eval/migrations/0001_assessments.sql\n\nCREATE TYPE assessment_state AS ENUM\n    ('draft', 'pending_review', 'approved', 'changed', 'rejected');\n\nCREATE TABLE assessment (\n    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id           UUID NOT NULL,\n    subject_id          UUID NOT NULL,                       -- the evaluated person\n    review_cycle        TEXT NOT NULL,                       -- e.g. '2026-H1'\n    rubric_version      TEXT NOT NULL,                       -- pinned FR-EVAL-002 version (§1 #17)\n    state               assessment_state NOT NULL DEFAULT 'draft',\n    drafted_by          TEXT NOT NULL,                       -- always the GENIE service identity for the draft\n    reviewer_subject_id UUID,                                -- the human reviewer; NULL until pending_review is acted on\n    consequential_ack   BOOLEAN NOT NULL DEFAULT FALSE,      -- §1 #8 hard gate\n    rebuttal_considered  BOOLEAN NOT NULL DEFAULT FALSE,     -- §1 #9 block\n    created_at_ns       BIGINT NOT NULL,\n    finalized_at_ns     BIGINT,\n    CHECK (subject_id &lt;&gt; reviewer_subject_id)                -- a person cannot review themselves (§1 #7)\n);\nCREATE INDEX idx_assessment_subject ON assessment (subject_id, review_cycle);\nCREATE INDEX idx_assessment_state   ON assessment (tenant_id, state);\n\nALTER TABLE assessment ENABLE ROW LEVEL SECURITY;\n-- §1 #16 — visible to founder, the subject's manager chain, and the subject.\nCREATE POLICY assessment_visibility ON assessment\n    USING (\n        tenant_id = current_setting('app.tenant_id')::uuid\n        AND (\n            current_setting('app.role') = 'founder'\n            OR subject_id = current_setting('app.subject_id')::uuid\n            OR eval_is_manager_of(current_setting('app.subject_id')::uuid, subject_id)\n        )\n    );</code></pre>\n<pre><code class=\"language-sql\">-- services/eval/migrations/0002_assessment_scores.sql\n\nCREATE TABLE assessment_score (\n    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    assessment_id       UUID NOT NULL REFERENCES assessment(id),\n    rubric_clause_id    TEXT NOT NULL,                       -- exactly one clause (§1 #5)\n    score               NUMERIC,                             -- NULL == needs_human_review (§1 #12)\n    clause_status       TEXT NOT NULL DEFAULT 'scored'\n                          CHECK (clause_status IN ('scored','needs_human_review','unsupported')),\n    genie_score         NUMERIC,                             -- the model's original draft (§1 #18)\n    rationale           TEXT NOT NULL,\n    evidence_event_ids  UUID[] NOT NULL,                     -- ≥1 real MEMORY event id (§1 #5)\n    revision_of         UUID REFERENCES assessment_score(id),-- human \"change\" links to the prior (§1 #18)\n    reviewer_rationale  TEXT,                                -- required on a human change/reject (§1 #19)\n    is_consequential    BOOLEAN NOT NULL DEFAULT FALSE,      -- §1 #8\n    tenant_id           UUID NOT NULL,\n    -- the boundary invariant, enforced in the DB too: a scored clause must cite evidence.\n    CHECK (clause_status &lt;&gt; 'scored' OR array_length(evidence_event_ids, 1) &gt;= 1)\n);\nCREATE INDEX idx_score_assessment ON assessment_score (assessment_id);\n\nALTER TABLE assessment_score ENABLE ROW LEVEL SECURITY;\nCREATE POLICY score_tenant_isolation ON assessment_score\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);\n-- finalized scores are immutable; a change writes a new revision row (§1 #18).\nREVOKE UPDATE, DELETE ON assessment_score FROM cyberos_app;</code></pre>\n<pre><code class=\"language-sql\">-- services/eval/migrations/0003_assessment_state_events.sql  (append-only audit spine, §1 #10)\nCREATE TABLE assessment_state_event (\n    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    assessment_id       UUID NOT NULL REFERENCES assessment(id),\n    seq                 BIGSERIAL NOT NULL,\n    prior_state         assessment_state,\n    new_state           assessment_state NOT NULL,\n    actor_subject_id    UUID,                                -- NULL only for the GENIE draft; never NULL on a finalize\n    actor_kind          TEXT NOT NULL CHECK (actor_kind IN ('genie','human','system')),\n    reason              TEXT,\n    memory_row_id       TEXT NOT NULL,                       -- l1_audit_log chain anchor (FR-PROJ-008 pattern)\n    chain_anchor        TEXT NOT NULL,\n    occurred_at_ns      BIGINT NOT NULL,\n    tenant_id           UUID NOT NULL,\n    -- a finalize MUST be human-driven (§1 #7): the type system + this check both guard it.\n    CHECK (new_state NOT IN ('approved','changed','rejected') OR actor_kind = 'human')\n);\nCREATE UNIQUE INDEX idx_state_event_assessment_seq ON assessment_state_event (assessment_id, seq);\nREVOKE UPDATE, DELETE ON assessment_state_event FROM cyberos_app;</code></pre>\n<pre><code class=\"language-sql\">-- services/eval/migrations/0004_assessment_rebuttals.sql  (§1 #9 / #15)\nCREATE TABLE assessment_rebuttal (\n    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    assessment_id       UUID NOT NULL REFERENCES assessment(id),\n    subject_id          UUID NOT NULL,                       -- must equal assessment.subject_id\n    kind                TEXT NOT NULL CHECK (kind IN ('rebuttal','appeal')),\n    body                TEXT NOT NULL,\n    clause_disputes     JSONB,                               -- optional clause-level disagreements\n    created_at_ns       BIGINT NOT NULL,\n    tenant_id           UUID NOT NULL\n);\nCREATE INDEX idx_rebuttal_assessment ON assessment_rebuttal (assessment_id);\nREVOKE UPDATE, DELETE ON assessment_rebuttal FROM cyberos_app;   -- immutable (§1 #9)</code></pre>\n<h3>Rust — the state machine (the gate lives here)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/state.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[sqlx(type_name = \"assessment_state\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum AssessmentState {\n    Draft, PendingReview, Approved, Changed, Rejected,\n}\n\n#[derive(Clone, Copy, Debug, PartialEq, Eq)]\npub enum ActorKind { Genie, Human, System }\n\n/// The ONLY legal transitions. There is no edge into a final state for `Genie`.\npub fn legal_transition(from: AssessmentState, to: AssessmentState, actor: ActorKind) -&gt; bool {\n    use AssessmentState::*;\n    use ActorKind::*;\n    match (from, to, actor) {\n        // GENIE drafts, then hands to humans — and stops there.\n        (Draft, PendingReview, Genie) =&gt; true,\n        (Draft, PendingReview, System) =&gt; true,         // cadence runner\n        // Only a human can finalize (§1 #7, DEC-2511/2512).\n        (PendingReview, Approved, Human) =&gt; true,\n        (PendingReview, Changed,  Human) =&gt; true,\n        (PendingReview, Rejected, Human) =&gt; true,\n        _ =&gt; false,\n    }\n}\n\npub struct FinalizeGuard;\nimpl FinalizeGuard {\n    /// Hard gate (§1 #7 #8). Refuses to finalize without a distinct human reviewer,\n    /// and refuses to apply a consequential outcome without an explicit ack.\n    pub fn check(\n        actor: ActorKind,\n        reviewer_subject_id: Option&lt;uuid::Uuid&gt;,\n        subject_id: uuid::Uuid,\n        has_consequential: bool,\n        consequential_ack: bool,\n        reviewer_rationale: Option&lt;&amp;str&gt;,\n        to: AssessmentState,\n    ) -&gt; Result&lt;(), EvalError&gt; {\n        if matches!(to, AssessmentState::Approved | AssessmentState::Changed | AssessmentState::Rejected) {\n            if actor != ActorKind::Human {\n                return Err(EvalError::HumanRequired);          // GENIE may never finalize.\n            }\n            match reviewer_subject_id {\n                Some(r) if r != subject_id =&gt; {}\n                _ =&gt; return Err(EvalError::ReviewerRequired),  // distinct human reviewer mandatory.\n            }\n            if has_consequential &amp;&amp; !consequential_ack {\n                return Err(EvalError::ConsequentialAckRequired); // §1 #8 hard gate.\n            }\n            if matches!(to, AssessmentState::Changed | AssessmentState::Rejected)\n                &amp;&amp; reviewer_rationale.map(str::trim).unwrap_or(\"\").is_empty() {\n                return Err(EvalError::RationaleRequired);        // §1 #19.\n            }\n        }\n        Ok(())\n    }\n}</code></pre>\n<h3>Rust — citation coverage (the no-unsourced-score boundary)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/scoring/citation.rs\npub struct ScoreRow {\n    pub rubric_clause_id: String,\n    pub score: Option&lt;f64&gt;,               // None == needs_human_review\n    pub clause_status: ClauseStatus,      // Scored | NeedsHumanReview | Unsupported\n    pub rationale: String,\n    pub evidence_refs: Vec&lt;EvidenceRef&gt;,  // each is a real MEMORY event id\n}\n\npub struct EvidenceRef { pub event_id: uuid::Uuid, pub kind: String, pub occurred_at_ns: i64 }\n\n/// DEC-2513: a *scored* clause must cite exactly one rubric clause and ≥1 grounded evidence event.\n/// `unsupported` / `needs_human_review` are allowed to carry no score, but never a fabricated one.\npub fn validate_citation(row: &amp;ScoreRow, recall_set: &amp;RecallSet) -&gt; Result&lt;(), EvalError&gt; {\n    if row.rubric_clause_id.trim().is_empty() {\n        return Err(EvalError::UncitedScore(\"missing rubric_clause_id\"));\n    }\n    match row.clause_status {\n        ClauseStatus::Scored =&gt; {\n            if row.score.is_none() {\n                return Err(EvalError::UncitedScore(\"scored clause with null score\"));\n            }\n            if row.evidence_refs.is_empty() {\n                return Err(EvalError::UncitedScore(\"scored clause cites no evidence\"));\n            }\n            // every cited event must actually be in the subject's recall set (§1 #5, no hallucinated ids).\n            for e in &amp;row.evidence_refs {\n                if !recall_set.contains(e.event_id) {\n                    return Err(EvalError::UncitedScore(\"evidence id not in recall set (hallucinated citation)\"));\n                }\n            }\n            Ok(())\n        }\n        // a non-scored clause must NOT carry a number (§1 #12 #13) — that would be a smuggled guess.\n        ClauseStatus::NeedsHumanReview | ClauseStatus::Unsupported =&gt; {\n            if row.score.is_some() {\n                return Err(EvalError::UncitedScore(\"unsupported clause must not carry a score\"));\n            }\n            Ok(())\n        }\n    }\n}</code></pre>\n<h3>Python — GENIE orchestration (drafts only; every model call via the gateway)</h3>\n<pre><code class=\"language-python\"># services/eval/genie/score_subject.py\nasync def score_subject(subject_id: str, tenant_id: str, cycle: str, rubric_version: str) -&gt; DraftAssessment:\n    # 1. consent gate is enforced upstream in Rust (access.rs); GENIE is only reached for consented subjects.\n    # 2. recall evidence with provenance (FR-MEMORY-123). Platform interactions only.\n    recall = await memory_recall(subject_id, tenant_id, window=cycle)        # [{event_id, kind, text, occurred_at, src}]\n    rubric = await load_rubric(tenant_id, rubric_version)                    # FR-EVAL-002 clauses\n\n    # 3. blind protected attributes before the model ever sees the text (§1 #14).\n    blinded = fairness.blind_protected_attributes(recall, tenant_id)\n\n    rows: list[ScoreRow] = []\n    for clause in rubric.clauses:\n        # 4. score one clause, grounded ONLY in the recalled evidence.\n        out = await ai_gateway.complete(\n            route=\"eval.score\",            # spend cap + residency + ZDR=on enforced by the gateway (§1 #11)\n            prompt=render(SCORE_V1, clause=clause, evidence=blinded),\n            tenant_id=tenant_id,\n        )\n        parsed = parse_score(out)\n        if parsed is None or parsed.unsupported:\n            # 5. refuse-to-score → needs_human_review, NEVER a fabricated number (§1 #12 #13, DEC-2514).\n            rows.append(ScoreRow.needs_human_review(clause.id, reason=parsed.reason if parsed else \"unparseable\"))\n            await emit_audit(\"eval.fallback_no_score\", subject_id, clause_id=clause.id, reason=\"unsupported_or_offline\")\n            continue\n        # the model returns the evidence ids it used; Rust re-validates them against the recall set (§1 #5).\n        rows.append(ScoreRow.scored(clause.id, parsed.score, parsed.rationale, parsed.evidence_event_ids))\n\n    return DraftAssessment(subject_id, cycle, rubric_version, rows)   # persisted by Rust in state=draft → pending_review</code></pre>\n<p>The Python side never writes a final state and never persists a score directly — it returns a draft to the Rust service, which runs <code>validate_citation</code> on every row and <code>FinalizeGuard</code> on every transition before anything is stored. A model failure or a parser failure yields <code>needs_human_review</code>, not an exception that could be swallowed into a default.</p>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Consent gate blocks unconsented draft</strong> — subject with no acknowledged FR-EVAL-001 notice → <code>POST /assessments</code> returns <code>403 not_consented</code>; no recall call made; <code>eval.draft_refused_no_consent</code> audit row written. (§1 #2)</li><li><strong>Consented draft produces pending_review</strong> — consented subject → GENIE drafts → assessment lands in <code>pending_review</code> with ≥1 <code>ScoreRow</code>. (§1 #4 #6)</li><li><strong>Every scored clause cites evidence + a rubric clause</strong> — each <code>clause_status='scored'</code> row has <code>rubric_clause_id</code> set and <code>array_length(evidence_event_ids) &gt;= 1</code>. (§1 #5)</li><li><strong>Uncited score rejected at boundary</strong> — a <code>ScoreRow</code> with empty <code>evidence_refs</code> and <code>clause_status='scored'</code> → <code>422 uncited_score</code>; row not persisted. (§1 #5)</li><li><strong>Hallucinated citation rejected</strong> — a <code>ScoreRow</code> citing an <code>event_id</code> not in the recall set → <code>422 uncited_score</code>. (§1 #5)</li><li><strong>GENIE cannot finalize</strong> — a transition <code>pending_review → approved</code> attempted with <code>actor_kind='genie'</code> → <code>EvalError::HumanRequired</code>; state unchanged. (§1 #7)</li><li><strong>Reviewer must be distinct from subject</strong> — <code>approve</code> with <code>reviewer_subject_id == subject_id</code> → <code>EvalError::ReviewerRequired</code>; DB CHECK also rejects. (§1 #7)</li><li><strong>Mandatory reviewer recorded on finalize</strong> — successful <code>approve</code> records a non-null <code>reviewer_subject_id</code> and an <code>assessment_state_event</code> with <code>actor_kind='human'</code>. (§1 #7)</li><li><strong>Consequential hard gate</strong> — assessment with <code>is_consequential=true</code> score → <code>approve</code> with <code>consequential_ack=false</code> → <code>EvalError::ConsequentialAckRequired</code>; the consequential field stays inert and is not emitted to any downstream. (§1 #8)</li><li><strong>No auto-apply of consequential outcome</strong> — at no state does the engine call a payroll/HRIS/level downstream; verified by asserting no such egress on any transition. (§1 #8)</li><li><strong>Subject can read own assessment + rebut</strong> — subject GETs their own <code>pending_review</code> assessment in full; <code>POST /rebut</code> stores an immutable <code>assessment_rebuttal(kind='rebuttal')</code>; <code>eval.assessment_rebutted</code> audit row. (§1 #9)</li><li><strong>Pending rebuttal blocks approval until seen</strong> — <code>approve</code> while an unconsidered rebuttal exists and <code>rebuttal_considered=false</code> → refused; succeeds after reviewer sets it true. (§1 #9)</li><li><strong>Every state change audit-chained</strong> — draft, pending_review, approved/changed/rejected each emit an <code>assessment_state_event</code> with a valid <code>chain_anchor</code> resolvable in <code>l1_audit_log</code>. (§1 #10)</li><li><strong>All model calls route via the gateway</strong> — eval service holds no provider keys; <code>score_subject</code> calls only <code>ai_gateway.complete(route='eval.score', ...)</code>; a direct provider call path does not exist. (§1 #11)</li><li><strong>ZDR + residency + cap enforced on eval.score</strong> — the <code>eval.score</code> policy in <code>ai-gateway/src/policy.rs</code> sets ZDR on, residency pinned, spend cap present; a call over cap is rejected by the gateway. (§1 #11)</li><li><strong>Offline model → no-score fallback</strong> — GENIE unreachable → affected clauses <code>score=null, clause_status='needs_human_review'</code>; <code>eval.fallback_no_score</code> row; assessment routed to human; no number fabricated. (§1 #12, DEC-2514)</li><li><strong>Unsupported clause carries no score</strong> — GENIE returns <code>unsupported</code> for a clause → <code>clause_status='unsupported'</code>, <code>score=null</code>; <code>validate_citation</code> rejects any attempt to attach a number. (§1 #13)</li><li><strong>Protected attributes blinded pre-scoring</strong> — evidence text passed to the model has name/gender/age/ethnicity redacted per tenant config; verified by inspecting the gateway request payload. (§1 #14)</li><li><strong>Disparity check flags, never adjusts</strong> — a batch breaching the disparity threshold emits <code>eval.disparity_flagged</code> and routes to human review; no score is auto-changed by the check. (§1 #14)</li><li><strong>Appeal routes to a second reviewer</strong> — <code>POST /appeal</code> on an <code>approved</code> assessment creates <code>assessment_rebuttal(kind='appeal')</code>, <code>eval.assessment_appealed</code>, and assigns a reviewer ≠ original. (§1 #15)</li><li><strong>RLS visibility</strong> — assessment visible to founder, subject, and subject's manager; a peer/unrelated-manager GET returns 0 rows; cross-tenant returns 0 rows. (§1 #16)</li><li><strong>Rubric version pinned</strong> — assessment records <code>rubric_version</code>; bumping the live FR-EVAL-002 rubric does not change a finalized assessment's scores. (§1 #17)</li><li><strong>Change writes a revision, not an in-place edit</strong> — reviewer <code>change</code> of a score creates a new <code>assessment_score</code> with <code>revision_of</code> set and <code>genie_score</code> preserving the original draft value. (§1 #18)</li><li><strong>Override without rationale refused</strong> — <code>change</code>/<code>reject</code> with empty <code>reviewer_rationale</code> → <code>422 rationale_required</code>. (§1 #19)</li><li><strong>Metrics emitted</strong> — <code>eval_citation_coverage_ratio == 1.0</code> on a healthy batch; <code>eval_override_rate</code>, <code>eval_fallback_no_score_total</code>, <code>eval_disparity_flagged_total</code> increment as exercised. (§1 #20)</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn genie_cannot_finalize_an_assessment() {\n    let env = TestEnv::new().await;\n    let a = env.draft_assessment_pending_review().await;     // GENIE-drafted, in pending_review\n    let res = transition(&amp;env.pool, a.id, AssessmentState::Approved, ActorKind::Genie, /*reviewer*/ None, None).await;\n    assert!(matches!(res, Err(EvalError::HumanRequired)));    // the gate holds\n    assert_eq!(env.state_of(a.id).await, AssessmentState::PendingReview);  // unchanged\n}\n\n#[tokio::test]\nasync fn scored_clause_without_evidence_is_rejected() {\n    let env = TestEnv::new().await;\n    let recall = env.recall_set_with(&amp;[\"e1\",\"e2\"]).await;\n    let row = ScoreRow::scored(\"clause.duty.1\", 3.0, \"looks fine\", vec![]);  // zero evidence\n    assert!(matches!(validate_citation(&amp;row, &amp;recall), Err(EvalError::UncitedScore(_))));\n}\n\n#[tokio::test]\nasync fn hallucinated_evidence_id_is_rejected() {\n    let env = TestEnv::new().await;\n    let recall = env.recall_set_with(&amp;[\"e1\",\"e2\"]).await;\n    let ghost = uuid::Uuid::new_v4();\n    let row = ScoreRow::scored(\"clause.duty.1\", 4.0, \"cited a ghost\", vec![EvidenceRef::just(ghost)]);\n    assert!(matches!(validate_citation(&amp;row, &amp;recall), Err(EvalError::UncitedScore(_))));\n}\n\n#[tokio::test]\nasync fn consequential_outcome_needs_explicit_human_ack() {\n    let env = TestEnv::new().await;\n    let a = env.pending_review_with_consequential().await;\n    // approve without the ack -&gt; refused; the consequential field stays inert.\n    let res = FinalizeGuard::check(ActorKind::Human, Some(env.manager()), a.subject_id,\n                                   true, /*ack*/ false, Some(\"ok\"), AssessmentState::Approved);\n    assert!(matches!(res, Err(EvalError::ConsequentialAckRequired)));\n}\n\n#[tokio::test]\nasync fn unconsented_subject_is_never_drafted() {\n    let env = TestEnv::new().await;\n    let subj = env.subject_without_notice().await;\n    let res = draft_assessment(&amp;env, subj).await;\n    assert!(matches!(res, Err(EvalError::NotConsented)));\n    assert_eq!(env.recall_call_count().await, 0);            // recall never happened\n    assert!(env.audit_has(\"eval.draft_refused_no_consent\", subj).await);\n}</code></pre>\n<pre><code class=\"language-python\"># services/eval/genie/tests/test_fallback_no_score.py\nasync def test_offline_model_yields_needs_human_review_not_a_number(monkeypatch):\n    monkeypatch.setattr(ai_gateway, \"complete\", _raise_offline)   # GENIE unreachable\n    draft = await score_subject(\"subj-1\", \"ten-1\", \"2026-H1\", \"rubric-v3\")\n    assert all(r.score is None for r in draft.rows)               # NO fabricated score\n    assert all(r.clause_status == \"needs_human_review\" for r in draft.rows)\n    assert audit_emitted(\"eval.fallback_no_score\")\n\nasync def test_unsupported_clause_never_carries_a_score():\n    out = parse_score('{\"unsupported\": true, \"reason\": \"no evidence for this duty\"}')\n    assert out.unsupported and out.score is None</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Migrations, state machine, citation boundary, GENIE orchestration above. The Rust service owns every gate; Python only drafts.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-123</strong> — brain recall supplies the evidence events with provenance; the recall set is the universe a score may cite (§1 #3 #5). <em>(depends_on)</em></li><li><strong>FR-EVAL-002</strong> — the rubric (clauses + source contract clause) the engine scores against; <code>rubric_version</code> is pinned per assessment (§1 #4 #17). <em>(depends_on)</em></li><li><strong>FR-EVAL-001</strong> — governance/consent/access/retention; the consent gate (§1 #2) and the founder|manager|self visibility (§1 #16) are this FR's enforcement of FR-EVAL-001's rules.</li><li><strong>FR-CUO-204 / the ai-gateway</strong> — GENIE (Lumi) is the analysis engine; the <code>eval.score</code> route reuses the gateway's spend cap + residency + ZDR machinery (§1 #11).</li><li><strong>FR-AUTH-003</strong> — per-tenant RLS spine for assessment visibility (§1 #16).</li><li><strong>FR-PROJ-008</strong> — the chain_anchor / memory-row linkage pattern reused for <code>assessment_state_event</code> (§1 #10).</li><li><strong>FR-EVAL-004</strong> — manager/employee views consume this engine's output. <em>(blocks)</em></li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.assessment_changed\",\n  \"payload\": {\n    \"assessment_id\":      \"asm-7e57...\",\n    \"subject_id\":         \"usr-alice-...\",\n    \"actor_subject_id\":   \"usr-manager-bob-...\",\n    \"actor_kind\":         \"human\",\n    \"rubric_version\":     \"rubric-v3\",\n    \"prior_state\":        \"pending_review\",\n    \"new_state\":          \"changed\",\n    \"evidence_event_ids\": [\"evt-0a3...\", \"evt-9c1...\"],\n    \"score_digest\":       \"sha256:1b9e...\",\n    \"change_summary\":     \"clause duty.3: GENIE drafted 3, reviewer changed to 4 — client email evt-9c1 shows the deadline moved, so the slip was external\",\n    \"reviewer_rationale\": \"Deadline change was the client's, not Alice's; the duty was met under the revised date.\",\n    \"consequential_ack\":  false,\n    \"trace_id\":           \"0af...\"\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.fallback_no_score\",\n  \"payload\": {\n    \"assessment_id\":   \"asm-7e57...\",\n    \"subject_id\":      \"usr-alice-...\",\n    \"rubric_clause_id\":\"compliance.ip.2\",\n    \"reason\":          \"ai_gateway_unreachable\",\n    \"resolution\":      \"routed_to_human_review\",\n    \"trace_id\":        \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred (do not block slice 1):</p>\n<ul><li>Cadence scheduler wiring (reuse the scheduled-task runner that wakes FR-CUO-204) — slice 2.</li><li>Calibration across reviewers (normalizing two managers who score differently) — a fairness concern for slice 3; out of scope here beyond the disparity flag.</li><li>Multi-language rationale (Vietnamese + English) for the subject view — FR-EVAL-004's surface concern.</li><li>Evidence \"freshness\" weighting (recency-decay from FR-MEMORY-113) as a recall input — additive, slice 2.</li></ul>\n<p>Resolved in-spec: HITL is mandatory and structural (DEC-2511/2512); the fallback is no-score (DEC-2514); citation coverage is a boundary invariant (DEC-2513); consent gates recall (DEC-2510).</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>GENIE offline / timeout</td><td>gateway error / deadline</td><td>clauses → <code>needs_human_review</code>; <code>eval.fallback_no_score</code>; routed to human</td><td>Human scores manually; retry when gateway back</td></tr><tr><td>GENIE returns a score it can't ground</td><td>prompt returns <code>unsupported</code>; <code>validate_citation</code></td><td>clause <code>unsupported</code>, no number stored</td><td>Human reviews the clause</td></tr><tr><td>GENIE cites an event id not in recall</td><td><code>validate_citation</code> recall-set check</td><td><code>422 uncited_score</code>; row dropped</td><td>Re-draft; flag possible prompt drift</td></tr><tr><td>GENIE identity attempts a finalize</td><td><code>FinalizeGuard</code> + DB CHECK (<code>actor_kind</code>)</td><td><code>HumanRequired</code>; state unchanged</td><td>None — by design; a human must act</td></tr><tr><td>Reviewer == subject</td><td><code>FinalizeGuard</code> + DB CHECK (<code>subject_id &lt;&gt; reviewer</code>)</td><td><code>ReviewerRequired</code></td><td>Assign a different reviewer</td></tr><tr><td>Consequential outcome without ack</td><td><code>FinalizeGuard</code></td><td><code>ConsequentialAckRequired</code>; field inert</td><td>Reviewer sets <code>consequential_ack</code> deliberately</td></tr><tr><td>Override with no rationale</td><td><code>FinalizeGuard</code></td><td><code>422 rationale_required</code></td><td>Reviewer states a reason</td></tr><tr><td>Unconsented subject drafted</td><td><code>access::has_acknowledged_notice</code></td><td><code>403 not_consented</code>; no recall; audit row</td><td>Obtain FR-EVAL-001 acknowledgement first</td></tr><tr><td>Subject's rebuttal ignored</td><td><code>rebuttal_considered</code> gate</td><td><code>approve</code> blocked until reviewer sees it</td><td>Reviewer reads rebuttal, sets flag</td></tr><tr><td>Disparity across protected groups</td><td>post-hoc <code>fairness.py</code> check</td><td><code>eval.disparity_flagged</code>; human fairness review</td><td>Human investigates; rubric/model adjusted by humans</td></tr><tr><td>Provider key leaks into eval service</td><td>code review + no-key invariant</td><td>build/config check fails</td><td>Remove key; all calls go via gateway only</td></tr><tr><td>Cross-region egress of evidence</td><td>gateway residency policy</td><td>call rejected at gateway</td><td>Fix residency config; never bypass gateway</td></tr><tr><td>Spend cap exceeded mid-batch</td><td>gateway cost ledger</td><td>over-cap call rejected</td><td>Resume next window; raise cap deliberately</td></tr><tr><td>In-place edit of a finalized score</td><td><code>REVOKE UPDATE/DELETE</code> on <code>assessment_score</code></td><td>permission denied</td><td>Change writes a revision row instead</td></tr><tr><td>Audit emit fails mid-transition</td><td>sqlx tx rollback</td><td>state change not committed</td><td>Caller retries; no orphan state</td></tr><tr><td>RLS bypass attempt</td><td>RLS policy</td><td>0 rows</td><td>None — by design</td></tr><tr><td>Peer reads someone's assessment</td><td>RLS visibility policy</td><td>0 rows</td><td>None</td></tr><tr><td>Rubric bumped after finalize</td><td>pinned <code>rubric_version</code></td><td>old assessment unchanged</td><td>None — by design</td></tr><tr><td>Malformed GENIE JSON</td><td><code>parse_score</code> returns None</td><td><code>needs_human_review</code>; <code>eval.fallback_no_score</code></td><td>Human scores; fix prompt/parser</td></tr><tr><td>Two reviewers act concurrently</td><td>first transition wins; second sees non-<code>pending_review</code></td><td>second gets 409</td><td>Second reviewer re-reads current state</td></tr><tr><td>Appeal assigned to original reviewer</td><td>second-reviewer constraint</td><td>rejected; reassigned</td><td>System picks a different reviewer</td></tr><tr><td>Recall returns private-life data (capture bug)</td><td>platform-only recall scope + provenance filter</td><td>non-platform events excluded before scoring</td><td>Fix capture (FR-EVAL-001 scope)</td></tr><tr><td>Consequential field exposed pre-finalize</td><td>visibility rule (reviewer/founder only)</td><td>not shown to subject while draft</td><td>None — by design</td></tr><tr><td>Subject denied view of own record</td><td>RLS self-visibility</td><td>subject always sees own</td><td>Fix RLS if regressed</td></tr><tr><td>OTel exporter down</td><td>buffered then dropped</td><td>metrics gap logged</td><td>Restore FR-OBS-001</td></tr><tr><td><code>eval_citation_coverage_ratio</code> &lt; 1.0</td><td>metric invariant breach</td><td>SEV-1 alarm (an uncited score escaped)</td><td>Investigate boundary bypass immediately</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The state machine is the load-bearing safety control: <code>legal_transition</code> has <strong>no edge</strong> into <code>approved</code>/<code>changed</code>/<code>rejected</code> for <code>ActorKind::Genie</code>, and the DB <code>CHECK (new_state NOT IN (...) OR actor_kind='human')</code> enforces the same thing one layer down. A bug in one layer cannot finalize an assessment because the other layer still refuses.</li><li><code>FinalizeGuard::check</code> is pure and exhaustively unit-tested; every finalize path calls it, so the consequential-ack and rationale-required gates can't be forgotten by a new handler.</li><li>Citation coverage is enforced in three places on purpose: the prompt (<code>score_v1</code> refuses to guess), the Rust boundary (<code>validate_citation</code> rejects), and the DB (<code>CHECK (clause_status &lt;&gt; 'scored' OR array_length(evidence_event_ids,1) &gt;= 1)</code>). Defense in depth because an uncited score is the failure that most undermines trust.</li><li>\"No score, flag for human\" is the single fallback for every model failure mode — offline, timeout, malformed output, unsupported clause. The engine has no code path that writes a numeric score the model did not ground; a <code>None</code> score is a first-class value, not an error to be defaulted away.</li><li>GENIE never persists. Python returns a <code>DraftAssessment</code>; Rust validates and writes it. This keeps every gate (consent, citation, finalize, audit) in the typed Rust service where it's testable, and means a Python exception degrades to <code>needs_human_review</code> rather than corrupting state.</li><li>The <code>eval.score</code> gateway route reuses the existing AI-gateway policy machinery (spend cap, residency, ZDR) rather than re-implementing it — this FR adds the route and its policy, not a second cost/residency engine. ZDR-on is non-negotiable for employment evidence; the route refuses to dispatch to a provider that doesn't honor it.</li><li><code>genie_score</code> on <code>assessment_score</code> always preserves the model's original draft even after a human <code>change</code>, so the GENIE-vs-human diff is queryable forever. The override rate and its rationales are the primary fairness telemetry; a high rate is a prompt/rubric signal, not a reviewer failing.</li><li>Protected-attribute blinding happens in Python before the gateway call so the model literally never receives identity. The blind list is per-tenant (jurisdictions differ on what's protected) and auditable. The disparity check is post-hoc and advisory: it flags for a human, never mutates a score, because an auto-correcting fairness pass is just a different bias.</li><li>The rebuttal is immutable and must be seen before approval (<code>rebuttal_considered</code>). This is deliberately a hard block, not a soft nudge: a person's written disagreement cannot be approved around without a reviewer acknowledging it.</li><li>The appeal path requires a <em>second</em> human reviewer, distinct from the one who finalized, mirroring a normal grievance escalation. It's the human safety net beyond the in-cycle rebuttal.</li><li>This is an EU-AI-Act high-risk system (employment evaluation): the conformance posture is disclosed-purpose (FR-EVAL-001 notice), human oversight (Article 14 — the mandatory reviewer and the appeal), transparency to the subject (own-record view + rebuttal), data governance (recall provenance, ZDR, residency), and logging (the audit chain). Stephen reviews and signs off before this FR leaves <code>draft</code>, per the high-risk rule.</li><li><code>eval_is_manager_of</code> is a tenant-scoped helper over the org/manager-chain data (FR-AUTH directory); it backs the RLS visibility policy and the appeal-reviewer selection. If the org graph is unavailable, the safe default is <em>deny</em> (founder + self only), never <em>allow</em>.</li><li>Cadence and on-demand drafts share one code path (§1 #1) so a gate can never be present on one and missing on the other; the scheduler is just a different caller of the same <code>draft_assessment</code> entrypoint.</li></ul>\n<hr>\n<h2>AI Risk Assessment</h2>\n<p>Required and central — this is an EU-AI-Act <strong>high-risk</strong> system (Annex III: AI used in employment, for evaluating people). EU AI Act Articles 5–7 and 14. All three subsections are load-bearing, not perfunctory.</p>\n<h3>Data Sources</h3>\n<p>The engine grounds its scores in two governed sources and nothing else:</p>\n<ul><li><strong>Captured platform work-interactions</strong>, retrieved via FR-MEMORY-123 brain recall, each carrying provenance (event id, kind, occurred-at, source module, tenant). Scope is the subject's own platform activity within the review window — chat, module usage, task/project/document activity, sign-ins. It explicitly <strong>excludes</strong> private life, keystroke logging, and screen surveillance (governance plan, \"proportionality and minimization\"). Recall only runs for a subject who has acknowledged the FR-EVAL-001 monitoring notice (§1 #2).</li><li><strong>The three signed documents</strong> — the labor contract, the NDA / non-compete / IP agreement, and the total-rewards and career-path appendix — but only as encoded into the FR-EVAL-002 rubric (the clauses + their source contract clause). The engine scores <em>against</em> this rubric; it does not read the raw contracts at inference time, and the <code>rubric_version</code> is pinned per assessment (§1 #17).</li></ul>\n<p>No model weights are trained or fine-tuned on this data. Personal-data handling: evidence reaches a model only via the ai-gateway <code>eval.score</code> route with zero-data-retention on and data residency pinned, so the provider neither retains nor trains on it and it does not leave the permitted region (§1 #11). Protected attributes (name, gender, age, ethnicity, nationality, disability) are blinded from the evidence text before the model ever receives it (§1 #14).</p>\n<h3>Human Oversight</h3>\n<p>A human is the decision-maker at every consequential point (EU AI Act Article 14):</p>\n<ul><li><strong>Mandatory reviewer.</strong> No assessment becomes final without a distinct human <code>reviewer_subject_id</code>; the GENIE service identity has no transition into <code>approved</code>/<code>changed</code>/<code>rejected</code>, enforced by the state-machine edge table, a DB <code>CHECK</code>, and <code>FinalizeGuard</code> (§1 #6 #7).</li><li><strong>Consequential hard gate.</strong> Anything affecting pay, progression, level, bonus, PIP, or continued employment stays inert until a human sets it with <code>consequential_ack = true</code>; the engine never auto-applies it to payroll/HRIS/level (§1 #8). The model assists; only an approving human's explicit act makes it real.</li><li><strong>Employee rebuttal.</strong> The subject reads their own assessment in full (evidence + rationale) and can rebut in writing; a pending rebuttal hard-blocks approval until the reviewer has seen it (§1 #9).</li><li><strong>Appeal.</strong> A finalized assessment can be escalated to a second human reviewer, distinct from the first (§1 #15).</li><li><strong>Override accountability + audit.</strong> A human <code>change</code>/<code>reject</code> requires a stated rationale (§1 #19); every state change is written append-only to <code>l1_audit_log</code> with actor, evidence, rubric version, and change summary (§1 #10), so who decided what on what evidence is always reconstructable. Stephen signs off before this FR leaves <code>draft</code>, per the high-risk rule.</li></ul>\n<h3>Failure Modes</h3>\n<p>Every model failure resolves to a human, never to a fabricated or silent score — the safe state is \"no score\", reached by flagging (the same discipline as FR-CUO-204's \"no change applied\"):</p>\n<ul><li><strong>Model wrong / hallucinated.</strong> The prompt returns <code>unsupported</code> for any clause it cannot ground (§1 #13); <code>validate_citation</code> rejects a score citing an evidence id not in the recall set and rejects any number on a non-scored clause (§1 #5 #12). A wrong-but-grounded score is contestable by the subject's rebuttal and correctable by the reviewer, whose change preserves the GENIE original as a diff (§1 #18).</li><li><strong>Model offline / timeout / malformed output.</strong> The affected clauses become <code>score = null, needs_human_review</code>, <code>eval.fallback_no_score</code> is emitted, and the assessment is routed to a human (§1 #12, DEC-2514). No code path defaults a number.</li><li><strong>Bias / unfairness.</strong> Protected attributes are blinded pre-scoring; a post-hoc group-disparity check flags a batch for human fairness review on a threshold breach — it flags, it never auto-adjusts a score, because an auto-correcting pass is its own bias (§1 #14).</li><li><strong>Governance failures.</strong> An unconsented subject is never drafted (§1 #2); evidence cannot egress to an unbounded or cross-region provider (§1 #11); a peer cannot read an assessment (RLS, §1 #16). In each case the system refuses rather than proceeds.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Cowork), authoring this FR + its paired audit from Stephen's 2026-06-29 decisions and the BRAIN/EVAL strategy note (<code>docs/strategy/cyberos-brain-evaluation-plan.md</code>, Phase 4 + \"Doing the monitoring responsibly\"), in the repo's engineering-spec@1 house style.</li><li><strong>Scope:</strong> full draft of this specification — the normative clauses, the migrations and Rust/Python sketches, the AI Risk Assessment, the acceptance criteria, and the failure-mode inventory.</li><li><strong>Human review:</strong> Stephen reviews and approves before status moves past <code>draft</code>. This is a high-risk FR, so the mandatory-HITL design, the consent gate, and the no-score fallback need his explicit sign-off; the paired audit plus the CAF/AWH gate validate before any implementation merges.</li></ul>\n<hr>\n<p><em>End of FR-EVAL-003.</em></p>";
