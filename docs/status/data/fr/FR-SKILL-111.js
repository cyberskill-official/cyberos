window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SKILL-111"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>This FR establishes the rules for the <code>description:</code> field of every <code>SKILL.md</code> frontmatter so that Anthropic-style hosts (and any host whose progressive-disclosure level 1 reads only frontmatter) can correctly route user requests to CyberOS skills.</p>\n<ol><li>The <code>description:</code> field <strong>MUST</strong> carry three structural elements in any order: <strong>(a) WHAT</strong> the skill does (one verb phrase, present tense, third-person), <strong>(b) WHEN</strong> the host should route to it (≥2 distinct trigger-phrase forms quoted in the description prose; trigger phrases mirror natural-language fragments a user might type), and <strong>(c) KEY VALUE</strong> (what the user gets — one outcome phrase). Order is recommended <code>WHAT + WHEN + KEY VALUE</code> but not enforced.</li><li>The <code>description:</code> field's character length <strong>MUST</strong> be <strong>≥ 80 chars</strong> AND <strong>≤ 1024 chars</strong> (raised from FR-SKILL-103's baseline of 200; aligns with the Anthropic guide's published cap per Reference B p. 31). Below 80 chars cannot carry both WHAT and ≥2 trigger phrases; above 1024 violates the host system-prompt budget.</li><li>Trigger phrases <strong>MUST</strong> be quoted in standard double-quotes (<code>\"...\"</code>) inside the description prose. Two acceptable forms:</li></ol>\n<ul><li><strong>Use-when form</strong> — <code>Use when user asks to \"&lt;verb phrase&gt;\"</code> or <code>Use when user mentions \"&lt;noun&gt;\"</code>. Example: <code>Use when user asks to \"audit this FR\" or \"check the rubric\"</code>.</li><li><strong>Triggers-on form</strong> — <code>Triggers on \"&lt;phrase&gt;\" or \"&lt;phrase&gt;\"</code>. Example: <code>Triggers on \"draft a PRD\" or \"outline the requirements\"</code>.</li></ul>\n<ol><li>The description <strong>MUST NOT</strong> contain XML angle brackets <code>&lt;</code> or <code>&gt;</code>. (Restates FR-SKILL-103 §1 #2 and FR-SKILL-113 sketch; included here so FM-112 is self-contained for the auditor — the validator runs the bracket check before the trigger-phrase check so the user gets the more diagnostic error first.)</li><li>The description <strong>MAY</strong> contain negative triggers in the form <code>Do NOT use for \"&lt;phrase&gt;\" (use &lt;other-skill&gt; instead)</code>. When present, the validator increments the trigger-phrase count for the positive triggers but not the negative ones (negatives are disambiguators, not triggers).</li><li>The description <strong>MUST</strong> reference file types or input artefacts by their canonical name when the skill consumes a specific format. Examples: <code>PRD/spec/SRS documents</code>, <code>.fig files</code>, <code>Postman collections</code>. This mirrors the Anthropic guide Chapter 2 p. 10 \"Mention file types if relevant\".</li><li>The description <strong>MUST NOT</strong> be a single trailing word like <code>Helps with X.</code> or <code>Manages Y.</code> — these were the canonical bad examples in the Anthropic guide p. 12. The validator rejects any description that fails the WHAT-detection regex (<code>\\b(generate|author|audit|review|draft|emit|build|propose|render|extract|classify|tag|score|track|enforce|validate|orchestrate|chain|select|pin|halt|resume|escalate|wrap|publish|deliver|test|simulate)\\b</code> — verb stems that indicate concrete action).</li><li>The description <strong>SHOULD</strong> name the skill's principal output artefact when the skill is artefact-producing (i.e. <code>produces.output_kind: artefact</code>). Example: <code>Generates a versioned feature_request@1 markdown</code>. This anchors trust because the user can verify the artefact appears in the output.</li><li>The description <strong>MAY</strong> be a multi-line YAML block (using <code>|</code> or <code>&gt;-</code> folding) for readability. The 80-1024 char budget applies to the <strong>flattened single-line equivalent</strong> (after YAML folding resolves whitespace).</li><li>Description <strong>MUST</strong> be locale-default English. Localised variants for VN-locale skills live in <code>description_localized.&lt;lang&gt;</code> under <code>metadata:</code> (out of scope for this FR; documented as future v0.3.0 work in §9).</li><li>The auditor rule <strong>MUST</strong> be <code>FM-112 description-format</code> with severity <code>error</code> for production skills (<code>status: accepted</code> or higher); <code>severity: warning</code> for <code>status: draft</code> skills (so authors get feedback during authoring but the rule doesn't block draft work).</li><li>The auditor rule <strong>MUST NOT</strong> auto-fix the description. Description text reflects human intent; auto-edit would silently rewrite trigger phrases the author chose deliberately. Verdict on description issues is always <code>needs_human</code> per <code>_template/audit/REPORT_FORMAT.md</code>.</li><li>The CLI <code>cyberos skill validate</code> (FR-SKILL-103 #10) <strong>MUST</strong> report description-format violations with a structured error message naming the missing element (<code>missing_what</code> | <code>missing_triggers</code> | <code>missing_value_phrase</code> | <code>too_short</code> | <code>too_long</code> | <code>forbidden_brackets</code>). The host shim per Part 9 of <code>modules/skill/README.md</code> MUST surface the message identically across hosts.</li><li>Existing skills that pre-date this FR <strong>MUST</strong> be backfilled lazily — the rule fires at <code>status: accepted</code> or higher; scaffold/<code>status: draft</code> skills get a grace window. The auditor rule fires only on fields that were touched by a fine-tune commit after this FR ships (per <code>human_fine_tune.signals_to_initiate</code> — fine-tune session brings the skill into compliance as a side effect).</li><li>The <code>## When to invoke this skill</code> body section <strong>MUST NOT</strong> be deleted; it remains the long-form companion to the frontmatter trigger phrases. The body section serves three purposes the description cannot: (a) negative triggers with prose explanations, (b) disambiguation cross-links to sibling skills, (c) handoff guidance to other personas. The frontmatter triggers are the host's level-1 hook; the body section is level-2 detail. <strong>Both must be kept in sync</strong> — when the description's triggers are updated, the body section MUST be re-audited for consistency (auditor rule SEC-007 enforces).</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why mandate trigger phrases in description rather than relying on the body (§1 #1)?</strong> Anthropic's progressive-disclosure model (guide Chapter 1 p. 5) loads frontmatter into every system prompt but reads the body only after a skill is matched. The host's relevance classifier sees the description and nothing else. If trigger phrases live only in the body, the classifier never sees them; the skill never matches; the body never loads. Inside CyberOS the supervisor's <code>classify_act</code> node reads the body (because we control both ends), but outside CyberOS — where Phase-B transpilers will ship plugins to Claude.ai, Codex, Cursor, etc. — the host follows Anthropic's contract. Trigger phrases MUST be in the description for the contract to hold.</p>\n<p><strong>Why raise the budget from 200 to 1024 chars (§1 #2)?</strong> The 200-char limit in FR-SKILL-103 baseline was inherited from an earlier draft that prioritised terseness. Real CyberOS skill descriptions already exceed 200 chars (e.g. <code>feature-request-author/SKILL.md</code> line 4-9 is ~470 chars in single-line equivalent). The Anthropic guide explicitly permits 1024 (Reference B p. 31). Raising the cap is a SemVer-compatible field-loosening — existing descriptions stay valid; the new cap admits enriched descriptions without forcing a v2 frontmatter schema. The 80-char floor is empirical: below 80 chars cannot fit WHAT (≥20 chars), 2 trigger phrases (≥30 chars combined), and a value phrase (≥20 chars) with quotes and separators.</p>\n<p><strong>Why two trigger-phrase forms rather than one (§1 #3)?</strong> Single-trigger descriptions over-fit one user phrasing. Two distinct forms increase classifier robustness — if the user says \"draft a PRD\" and only <code>\"author a PRD\"</code> is registered, the synonym mismatch breaks routing. Two forms cover the common paraphrase delta. More than two is welcome but not required. The Anthropic guide examples (p. 11) all carry 2-4 forms, validating the two-floor as the right minimum.</p>\n<p><strong>Why both <code>Use when</code> and <code>Triggers on</code> forms (§1 #3)?</strong> Different skill authors prefer different prose styles; both forms appear in the Anthropic guide examples. The validator accepts either. What matters is that the trigger phrases are <em>quoted</em> — the validator's regex looks for <code>\"&lt;phrase&gt;\"</code> to detect triggers; unquoted natural-language sentences pass freely without being recognised as triggers.</p>\n<p><strong>Why explicit XML-bracket rejection (§1 #4)?</strong> The Anthropic guide Reference B p. 31 forbids <code>&lt;</code> and <code>&gt;</code> in frontmatter for system-prompt injection reasons. Even though FR-SKILL-103 already validates this at the frontmatter level, FM-112's own check provides the more diagnostic error (\"found <code>&lt;</code> in description text\" vs the generic FR-SKILL-103 \"InvalidDescription\") — operator UX matters.</p>\n<p><strong>Why allow negative triggers (§1 #5)?</strong> The Anthropic guide Chapter 5 p. 25 shows the disambiguation pattern: <code>Do NOT use for simple data exploration (use data-viz skill instead)</code>. CyberOS body sections already do this in <code>## When to invoke this skill</code>; moving the negative-trigger sentence into the description gives the host classifier the same disambiguation power. Counting them separately prevents the validator from double-counting (a negative trigger isn't a positive trigger).</p>\n<p><strong>Why require file-type references (§1 #6)?</strong> Anthropic guide Chapter 2 p. 10 calls this out specifically: \"Mention file types if relevant\". A skill that reads .fig files should say <code>.fig files</code>; a skill that reads PRDs should say <code>PRD/spec/SRS documents</code>. The classifier weights file-extension matches heavily — without them, the skill doesn't trigger on prompts like \"I have a Figma file...\".</p>\n<p><strong>Why reject single-word-action descriptions (§1 #7)?</strong> The Anthropic guide p. 12 lists \"Helps with projects\" as the canonical bad description. The verb-stem regex catches the same anti-pattern: descriptions that don't name an action (\"Helps with X\", \"Manages Y\", \"Implements Z\") never match concrete user requests. The verb list is short (29 verb stems) and represents the actual surface of CyberOS skill actions.</p>\n<p><strong>Why name the output artefact (§1 #8)?</strong> Anchors trust. A user reading the description sees what they'll get (<code>feature_request@1 markdown</code>); the audit row's <code>payload_hash</code> (per <code>audit:</code> block) verifies the artefact exists; the round-trip is closed. Without the artefact name, the user can't tell what to expect, and the audit row carries less semantic meaning.</p>\n<p><strong>Why warning on draft, error on accepted (§1 #11)?</strong> Drafting is exploratory; forcing the trigger-phrase format on every draft commit would slow first-pass authoring. Production skills (<code>status: accepted</code> or higher) are routed by the supervisor and shipped to hosts — they MUST conform. The two-severity scheme lets authors iterate while keeping production hygienic.</p>\n<p><strong>Why no auto-fix (§1 #12)?</strong> Trigger phrases reflect human intent. An auto-edit might rewrite <code>\"audit this FR\"</code> as <code>\"check the FR\"</code> and silently miss the user-visible verb the author chose. Description issues always require human review.</p>\n<p><strong>Why the body <code>## When to invoke this skill</code> stays (§1 #15)?</strong> Three irreducible functions: (a) negative triggers with prose explanations (e.g. \"If the user wants <code>&lt;other skill&gt;</code>, route to <code>&lt;other skill&gt;</code> instead\"); (b) disambiguation cross-links to sibling skills that the description's character budget can't accommodate; (c) handoff guidance describing chains. Frontmatter is for the host classifier; body is for the supervisor + human reader. Keeping both keeps the cross-host port honest while preserving the rich-body audit surface.</p>\n<h2>§3 — API contract</h2>\n<h3>Rust types (added to <code>services/skill-broker/src/frontmatter/schema.rs</code>)</h3>\n<pre><code class=\"language-rust\">// Raised from prior 200-char cap. SemVer-compatible loosen.\npub const DESCRIPTION_MIN_LEN: usize = 80;\npub const DESCRIPTION_MAX_LEN: usize = 1024;\n\n// Inside SkillFrontmatter (FR-SKILL-103 §3):\n//   pub description: String,    // unchanged shape — just new validator</code></pre>\n<h3>Rust validator (<code>services/skill-broker/src/frontmatter/description_validator.rs</code>)</h3>\n<pre><code class=\"language-rust\">use crate::frontmatter::FrontmatterError;\nuse regex::Regex;\nuse once_cell::sync::Lazy;\n\n/// Verb stems that indicate concrete action. Conservative list — operator\n/// extends as needed via PR + RUBRIC update.\nstatic VERB_STEMS: Lazy&lt;Regex&gt; = Lazy::new(|| {\n    Regex::new(r\"(?i)\\b(generate|author|audit|review|draft|emit|build|propose|render|extract|classify|tag|score|track|enforce|validate|orchestrate|chain|select|pin|halt|resume|escalate|wrap|publish|deliver|test|simulate)\\b\")\n        .unwrap()\n});\n\n/// Quoted trigger phrase: `\"...\"` with non-empty body.\nstatic QUOTED_TRIGGER: Lazy&lt;Regex&gt; = Lazy::new(|| {\n    Regex::new(r#\"\"([^\"]{1,80})\"\"#).unwrap()\n});\n\n/// Negative-trigger preamble: matches \"Do NOT use for ...\" prefix.\nstatic NEGATIVE_PREFIX: Lazy&lt;Regex&gt; = Lazy::new(|| {\n    Regex::new(r\"(?i)\\bdo\\s+not\\s+use\\s+(for|when|with)\\b\").unwrap()\n});\n\n#[derive(Debug, PartialEq)]\npub enum DescriptionViolation {\n    TooShort { len: usize },\n    TooLong  { len: usize },\n    ForbiddenBrackets,\n    MissingWhat,\n    InsufficientTriggers { found: usize, needed: usize },\n}\n\npub fn validate(description: &amp;str) -&gt; Result&lt;(), DescriptionViolation&gt; {\n    // 0. Flatten YAML-folded multi-line into single line for measurement.\n    let flat = description.replace('\\n', \" \").trim().to_string();\n    let len  = flat.chars().count();\n    if len &lt; DESCRIPTION_MIN_LEN { return Err(DescriptionViolation::TooShort { len }); }\n    if len &gt; DESCRIPTION_MAX_LEN { return Err(DescriptionViolation::TooLong  { len }); }\n\n    // 1. Bracket-free (defensive duplicate of FR-SKILL-103 check).\n    if flat.contains('&lt;') || flat.contains('&gt;') {\n        return Err(DescriptionViolation::ForbiddenBrackets);\n    }\n\n    // 2. WHAT detection — at least one verb stem.\n    if !VERB_STEMS.is_match(&amp;flat) {\n        return Err(DescriptionViolation::MissingWhat);\n    }\n\n    // 3. Trigger phrases — count quoted phrases, excluding those preceded\n    //    by \"Do NOT use for\" (negative triggers).\n    let mut positive_triggers = 0usize;\n    for m in QUOTED_TRIGGER.find_iter(&amp;flat) {\n        let preceding = &amp;flat[..m.start()];\n        // If a negative-prefix appears in the 40 chars preceding the\n        // quoted phrase, treat it as a negative trigger (don't count).\n        let window_start = preceding.len().saturating_sub(40);\n        let window       = &amp;preceding[window_start..];\n        if NEGATIVE_PREFIX.is_match(window) { continue; }\n        positive_triggers += 1;\n    }\n    if positive_triggers &lt; 2 {\n        return Err(DescriptionViolation::InsufficientTriggers {\n            found: positive_triggers, needed: 2,\n        });\n    }\n    Ok(())\n}\n\nimpl From&lt;DescriptionViolation&gt; for FrontmatterError {\n    fn from(v: DescriptionViolation) -&gt; Self {\n        FrontmatterError::InvalidDescription(format!(\"{v:?}\"))\n    }\n}</code></pre>\n<h3>JSONSchema mirror (<code>services/skill-broker/skill.schema.json</code> — diff)</h3>\n<pre><code class=\"language-diff\">   \"description\": {\n     \"type\": \"string\",\n-    \"maxLength\": 200\n+    \"minLength\": 80,\n+    \"maxLength\": 1024,\n+    \"pattern\": \"(?s)^(?!.*[&lt;&gt;]).*$\"\n   }</code></pre>\n<p>(JSONSchema can express min/max length and the bracket-absence guard via pattern; the verb-stem + trigger-count checks live in the Rust validator only — JSONSchema regex can't express \"≥2 quoted substrings excluding those after a Do-NOT preamble\" cleanly. The mirror documents this in a <code>$comment</code> field.)</p>\n<h3>Auditor rule (added to <code>modules/skill/feature-request-audit/RUBRIC.md</code>)</h3>\n<pre><code class=\"language-markdown\">### FM-112 — description-format\n\n**Statement:** SKILL.md frontmatter `description:` field MUST carry WHAT (verb-stem action) + WHEN (≥2 quoted trigger phrases) + KEY VALUE; 80-1024 chars flattened; no XML brackets; per FR-SKILL-111 §1.\n\n**Severity:** error on `status: accepted | building | shipped`; warning on `status: draft`.\n\n**Auto-fix:** never (description reflects human intent — verdict `needs_human`).\n\n**Check (deterministic):** invoke `cyberos skill validate &lt;bundle&gt;`; if exit code 6 with `validation_outcome: description-format`, the rule fails. Specific sub-codes: `too_short` | `too_long` | `forbidden_brackets` | `missing_what` | `insufficient_triggers`.\n\n**Issue template:**\n</code></pre>\n<p>ISSUE id:              ISS-NNN rule_id:         FM-112 severity:        error|warning category:        description_format location:        frontmatter \"description:\" field evidence:        \"&lt;the description text, truncated to 200 chars&gt;\" description:     \"Description fails FM-112: &lt;sub-code&gt;. Detail: &lt;validator output&gt;.\" suggestion:      \"Rewrite description to include WHAT + WHEN (≥2 quoted triggers like \\\"&lt;phrase&gt;\\\") + KEY VALUE. See feature-request-audit skill §3.13 for examples.\" auto_fix_applied: false resolution:      null</p>\n<pre><code></code></pre>\n<h3>Updated <code>_template/author/SKILL.md</code> description block (diff)</h3>\n<pre><code class=\"language-diff\">-description: |\n-  Author a &lt;ARTIFACT&gt; markdown from &lt;input artefact(s)&gt;. Generates a\n-  versioned &lt;artefact&gt;@1 file under output_dir, with per-claim authority\n-  markers and provenance to the source. Chains naturally into\n-  &lt;artifact&gt;-audit by default. Refuses to author when upstream artefact\n-  is in non-pass state.\n+description: &gt;-\n+  Generate a versioned &lt;artefact&gt;@1 markdown from one or more &lt;input\n+  artefact(s)&gt;. Use when user asks to \"draft a &lt;ARTIFACT&gt;\" or \"turn\n+  this &lt;input&gt; into a &lt;ARTIFACT&gt;\". Halts at PLAN approval and HITL\n+  gates; resumable from manifest.json state. Chains into\n+  &lt;artifact&gt;-audit by default. Outputs feature_request@1-style\n+  markdowns with per-claim authority markers and source provenance.</code></pre>\n<p>(The folded scalar <code>&gt;-</code> flattens newlines to spaces for the validator's length count.)</p>\n<h3>Operator-facing CLI</h3>\n<pre><code class=\"language-bash\"># Validate a single skill's description (calls into FR-SKILL-103's broker)\ncyberos skill validate modules/skill/feature-request-author/\n\n# Validate every production skill — used by CI gate\ncyberos skill validate-all --status-min accepted\n\n# Output (human form):\n✓ modules/skill/feature-request-author/         — valid (description: 487 chars, 3 triggers)\n✗ modules/skill/closure-author/                 — InvalidDescription: insufficient_triggers (found: 1, needed: 2)\n✗ modules/skill/stage-gate-author/              — InvalidDescription: missing_what\n\n# Exit code: 0 if all pass, 6 (SchemaViolation) on any failure.</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Valid description with 2 triggers loads</strong> — fixture <code>description-valid/SKILL.md</code> with <code>description: \"Generate a feature_request@1 markdown from PRDs. Use when user asks to \\\"draft an FR\\\" or \\\"turn this PRD into a backlog\\\". Outputs versioned FR-NNN-slug.md files.\"</code> → <code>description_validator::validate</code> returns <code>Ok(())</code>.</li><li><strong>Description too short rejected</strong> — 70-char description → <code>Err(TooShort { len: 70 })</code>.</li><li><strong>Description too long rejected</strong> — 1100-char description → <code>Err(TooLong { len: 1100 })</code>.</li><li><strong>XML brackets in description rejected</strong> — description containing <code>&lt;untrusted&gt;</code> → <code>Err(ForbiddenBrackets)</code>.</li><li><strong>Description missing verb stem rejected</strong> — <code>\"Helps with FRs. Use when user says \\\"FR\\\" or \\\"backlog\\\".\"</code> → <code>Err(MissingWhat)</code>.</li><li><strong>Description with only 1 trigger rejected</strong> — <code>\"Generate FRs. Use when user asks to \\\"draft an FR\\\".\"</code> (single quoted phrase) → <code>Err(InsufficientTriggers { found: 1, needed: 2 })</code>.</li><li><strong>Description with 1 positive + 1 negative trigger rejected</strong> — <code>\"Generate FRs. Use when user asks to \\\"draft an FR\\\". Do NOT use for \\\"audit existing FRs\\\".\"</code> → <code>Err(InsufficientTriggers { found: 1, needed: 2 })</code>. (Negative trigger doesn't count.)</li><li><strong>Description with 2 positive + 1 negative trigger accepts</strong> — <code>\"Generate FRs. Use when user asks to \\\"draft an FR\\\" or \\\"turn this PRD into a backlog\\\". Do NOT use for \\\"audit existing FRs\\\".\"</code> → <code>Ok(())</code>.</li><li><strong>Folded YAML multi-line description flattened correctly</strong> — <code>description: &gt;-\\n  Line one with first trigger \\\"phrase one\\\".\\n  Line two with second trigger \\\"phrase two\\\".\\n  Outputs X.\\n</code> → flattened to single line and validated as if one-line; → <code>Ok(())</code>.</li><li><strong>JSONSchema mirror agrees on length bounds</strong> — running <code>ajv validate -s skill.schema.json -d &lt;fixture&gt;</code> against the same 4 length-test fixtures yields the same accept/reject pattern as the Rust validator.</li><li><strong>CLI exit codes</strong> — <code>cyberos skill validate &lt;valid&gt;</code> → 0; <code>cyberos skill validate &lt;missing-triggers&gt;</code> → 6 (SchemaViolation); <code>cyberos skill validate &lt;too-short&gt;</code> → 6.</li><li><strong>Auditor rule FM-112 fires on production skill with bad description</strong> — running <code>feature-request-audit</code> against an <code>accepted</code>-status skill whose description has 1 trigger → audit report contains one <code>rule_id: FM-112, severity: error, status: needs_human</code> issue.</li><li><strong>Auditor rule FM-112 fires as warning on draft skill</strong> — same input but <code>status: draft</code> → audit report contains one <code>rule_id: FM-112, severity: warning, status: needs_human</code> issue.</li><li><strong>Auditor never auto-fixes description</strong> — even when <code>auto_fix_applied</code> could be true in principle, the rule's <code>auto_fix_applied: false</code> is preserved across all runs.</li><li><strong>Backfill exemplar — feature-request-author</strong> — after FR-SKILL-111 ships, <code>modules/skill/feature-request-author/SKILL.md</code> description carries ≥2 trigger phrases; running <code>cyberos skill validate</code> on it returns Ok; running the auditor returns no FM-112 issues.</li><li><strong>Backfill exemplar — feature-request-audit</strong> — analogous; description carries ≥2 trigger phrases including ones distinct from the author's so the classifier disambiguates.</li><li><strong>Backfill exemplar — prd-author</strong> — analogous, completing the 3 exemplars cited in the §6.1 of <code>modules/skill/ANTHROPIC_GUIDE_DIGEST.md</code>.</li><li><strong>README Part 2.1 row updated</strong> — the description-field row in Part 2.1 of <code>modules/skill/README.md</code> shows new min/max + cross-references FR-SKILL-111.</li><li><strong>feature-request-audit skill §3.13 entry added</strong> — new sub-rule \"Description format\" with example good + bad descriptions.</li><li><strong>Cross-FR reciprocity preserved</strong> — FR-SKILL-103's <code>blocks:</code> list updated to include FR-SKILL-111 (since 103 is the parent frontmatter spec).</li><li><strong>OTel span emitted</strong> — every validate call emits <code>skill.description.validate</code> with attributes <code>skill_id</code>, <code>outcome</code> (ok | too_short | too_long | forbidden_brackets | missing_what | insufficient_triggers), <code>length_chars</code>, <code>trigger_count</code>, <code>duration_ms</code>.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/skill-broker/tests/description_validator_test.rs\n\nuse cyberos_skill_broker::frontmatter::description_validator::{validate, DescriptionViolation};\n\n#[test]\nfn valid_description_with_two_triggers() {\n    let d = r#\"Generate a feature_request@1 markdown from PRDs. Use when user asks to \"draft an FR\" or \"turn this PRD into a backlog\". Outputs versioned FR-NNN-slug.md files with anti-fabrication discipline.\"#;\n    assert!(validate(d).is_ok());\n}\n\n#[test]\nfn too_short() {\n    let d = r#\"Generate FRs. Use \"draft\" or \"audit\".\"#; // 38 chars\n    assert_eq!(validate(d).unwrap_err(), DescriptionViolation::TooShort { len: 38 });\n}\n\n#[test]\nfn too_long() {\n    let d = \"A\".repeat(1100);\n    let d = format!(r#\"Generate FRs. Use when user asks to \"draft\" or \"audit\". {d}\"#);\n    let len = d.chars().count();\n    assert_eq!(validate(&amp;d).unwrap_err(), DescriptionViolation::TooLong { len });\n}\n\n#[test]\nfn forbidden_brackets() {\n    let d = r#\"Generate &lt;FR&gt; markdowns. Use when user asks to \"draft\" or \"audit\".\"#;\n    assert_eq!(validate(d).unwrap_err(), DescriptionViolation::ForbiddenBrackets);\n}\n\n#[test]\nfn missing_what_verb() {\n    let d = r#\"Helps with FRs in the backlog. Useful when user says \"FR\" or \"backlog\" or \"story\". Returns markdown.\"#;\n    assert_eq!(validate(d).unwrap_err(), DescriptionViolation::MissingWhat);\n}\n\n#[test]\nfn insufficient_triggers_single_positive() {\n    let d = r#\"Generate FRs from a PRD source. Use when user asks to \"draft an FR\". Outputs versioned files in a structured backlog directory under output_dir.\"#;\n    assert_eq!(\n        validate(d).unwrap_err(),\n        DescriptionViolation::InsufficientTriggers { found: 1, needed: 2 }\n    );\n}\n\n#[test]\nfn negative_trigger_does_not_count() {\n    let d = r#\"Generate FRs from a PRD source. Use when user asks to \"draft an FR\". Do NOT use for \"audit existing FRs\". Outputs versioned FR-NNN-slug.md files.\"#;\n    assert_eq!(\n        validate(d).unwrap_err(),\n        DescriptionViolation::InsufficientTriggers { found: 1, needed: 2 }\n    );\n}\n\n#[test]\nfn two_positive_plus_one_negative_accepts() {\n    let d = r#\"Generate FRs from a PRD source. Use when user asks to \"draft an FR\" or \"turn this PRD into a backlog\". Do NOT use for \"audit existing FRs\". Outputs versioned files.\"#;\n    assert!(validate(d).is_ok());\n}\n\n#[test]\nfn folded_yaml_multiline_flattens() {\n    // Simulate what serde_yaml emits after parsing a `description: &gt;-` block.\n    let d = \"Generate FRs from a PRD source. Use when user asks to\\n\\\"draft an FR\\\" or \\\"turn this PRD into a backlog\\\".\\nOutputs versioned files.\";\n    assert!(validate(d).is_ok());\n}\n\n#[test]\nfn exact_floor_80_chars_with_two_triggers() {\n    // Smallest valid string: exactly 80 chars including 2 triggers + verb.\n    let d = r#\"Audit FRs. Use when user asks to \"audit FR\" or \"check FR\". Reports issues.\"#;\n    assert_eq!(d.chars().count(), 74); // sanity — below floor, should fail\n    assert_eq!(validate(d).unwrap_err(), DescriptionViolation::TooShort { len: 74 });\n\n    let d = r#\"Audit feature_request@1 markdowns. Use when user asks to \"audit an FR\" or \"check rubric\". Reports per-rule verdicts.\"#;\n    assert!(d.chars().count() &gt;= 80);\n    assert!(validate(d).is_ok());\n}\n\n#[test]\nfn cli_exit_code_on_violation() {\n    use assert_cmd::Command;\n    let bundle = tempfile::tempdir().unwrap();\n    // ... write a SKILL.md with too-short description ...\n    let mut cmd = Command::cargo_bin(\"cyberos-skill-validate\").unwrap();\n    cmd.arg(bundle.path()).assert().failure().code(6);\n}\n\n#[test]\nfn jsonschema_mirror_agrees_on_length_bounds() {\n    // Run ajv-CLI against the 4 length-test fixtures and assert\n    // accept/reject pattern matches Rust validator.\n    use std::process::Command;\n    let outputs = [\"description-valid\", \"description-too-short\", \"description-too-long\", \"description-missing-triggers\"]\n        .iter()\n        .map(|fix| {\n            Command::new(\"ajv\")\n                .args([\"validate\", \"-s\", \"skill.schema.json\", \"-d\", &amp;format!(\"tests/fixtures/{fix}/skill.json\")])\n                .status()\n                .unwrap()\n                .success()\n        })\n        .collect::&lt;Vec&lt;_&gt;&gt;();\n    // valid + too-long-by-pattern → ajv catches length; missing-triggers ajv ignores (rust-only).\n    assert_eq!(outputs, vec![true, false, false, true /* ajv can't see triggers */]);\n}</code></pre>\n<h3>Auditor regression fixtures (added to <code>modules/skill/feature-request-audit/acceptance/</code>)</h3>\n<pre><code class=\"language-bash\"># Three new golden fixtures:\nacceptance/regression-2026-05-19-fm112-missing-triggers/\n  golden-input.json    # an FR-style fixture with frontmatter that fails FM-112\n  golden-output.audit.md   # expected audit report with one FM-112 issue</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>Most of the surface is in §3 (Rust types + validator + JSONSchema diff + auditor rule + template diff). The remaining orchestration is small:</p>\n<ol><li><strong>Validator wiring</strong> — <code>services/skill-broker/src/frontmatter/validators.rs</code>'s top-level <code>validate(fm, body, broker_version)</code> calls <code>description_validator::validate(&amp;fm.description)?</code> as the first check.</li><li><strong>CLI integration</strong> — <code>cyberos skill validate</code> already exists (FR-SKILL-103 §3 CLI block). No change needed in the CLI itself — the new validator surfaces through the same <code>Err</code> path.</li><li><strong>Auditor integration</strong> — <code>feature-request-audit/RUBRIC.md</code> is the lookup table; the auditor's 8-step loop (per <code>_template/audit/AUDIT_LOOP.md</code>) runs every rule including FM-112.</li><li><strong>Lazy-backfill mechanic</strong> — no automation. The new RUBRIC rule fires on <code>status: accepted</code> skills; first audit cycle after FR ship surfaces the issue; author addresses during normal fine-tune flow. No bulk sweep.</li></ol>\n<h2>§7 — Dependencies</h2>\n<p><strong>Depends on:</strong></p>\n<ul><li><strong>FR-SKILL-103</strong> (frontmatter-extension) — provides the broker, frontmatter schema, validator framework, CLI binary, JSONSchema mirror, and <code>InvalidDescription</code> error variant. FR-SKILL-111 extends FR-SKILL-103's <code>description</code> field rules.</li></ul>\n<p><strong>Blocks:</strong> none (independent of FR-SKILL-112 and FR-SKILL-113).</p>\n<p><strong>Related:</strong></p>\n<ul><li><strong>FR-SKILL-112</strong> (trigger-tests-fixtures) — defines positive + negative trigger phrases per skill in <code>acceptance/TRIGGER_TESTS.md</code>. The two FRs are complementary: 111 puts triggers in the description; 112 validates the description's triggers against actual classifier behaviour. Either can ship first.</li><li><strong>FR-SKILL-113</strong> (XML-tag-free frontmatter — sketched only, not authored) — both 111 and 113 strengthen the host-portability surface. If 113 ships first, FR-SKILL-111's §1 #4 becomes a duplicate check (acceptable; the duplicate is defensive). If 111 ships first, FR-SKILL-113 still provides the broader sweep across <code>wrap_in:</code> and other fields.</li><li><strong>FR-SKILL-101</strong> (memory integration) — orthogonal; runs in a separate slice.</li><li><strong>AGENTS.md §15</strong> — the SKILL.md scope contract referenced from the broker.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>OBS module</strong> (FR-OBS-001..009) — when OBS ships, the <code>skill.description.validate</code> OTel span feeds the dashboard; per-skill description-format violation rates are visible.</li></ul>\n<h2>§8 — Example payloads</h2>\n<h3>Example 1 — valid description (the canonical good)</h3>\n<pre><code class=\"language-yaml\">description: &gt;-\n  Generate a versioned feature_request@1 markdown from one or more PRD/spec/SRS\n  documents. Use when user asks to \"draft an FR\", \"turn this PRD into a backlog\",\n  or \"expand this spec into FRs\". Halts at PLAN approval and HITL gates;\n  resumable from manifest.json. Chains into feature-request-audit by default.\n  Outputs FR-NNN-slug.md files with per-claim authority markers + provenance.\n  Do NOT use for \"audit existing FRs\" (use feature-request-audit instead).</code></pre>\n<p>Flattened length: ~478 chars. Triggers detected: 3 positive (<code>\"draft an FR\"</code>, <code>\"turn this PRD into a backlog\"</code>, <code>\"expand this spec into FRs\"</code>) + 1 negative (<code>\"audit existing FRs\"</code> after <code>Do NOT use for</code>). Verb stems: <code>generate</code>, <code>chain</code> (multiple). Verdict: <strong>OK</strong>.</p>\n<h3>Example 2 — auditor issue block (FM-112 firing)</h3>\n<pre><code>ISSUE\nid:              ISS-007\nrule_id:         FM-112\nseverity:        error\ncategory:        description_format\nlocation:        frontmatter \"description:\" field\nevidence:        \"Author a closure markdown from project state. Halts at HITL gates. Outputs versioned closure@1 files.\"\ndescription:     \"Description fails FM-112: insufficient_triggers (found: 0, needed: 2). The description has no quoted trigger phrases — the classifier sees only WHAT, not WHEN.\"\nsuggestion:      \"Add ≥2 quoted trigger phrases. Example: 'Use when user asks to \\\"close the project\\\" or \\\"draft the closure report\\\"'. See feature-request-audit skill §3.13.\"\nauto_fix_applied: false\nresolution:      null\nopened_at:       \"2026-05-19T14:00:00Z\"\nupdated_at:      \"2026-05-19T14:00:00Z\"</code></pre>\n<h3>Example 3 — OTel span</h3>\n<pre><code class=\"language-json\">{\n  \"name\": \"skill.description.validate\",\n  \"attributes\": {\n    \"skill_id\": \"feature-request-author\",\n    \"outcome\": \"ok\",\n    \"length_chars\": 487,\n    \"trigger_count\": 3,\n    \"duration_ms\": 0.4\n  }\n}</code></pre>\n<h3>Example 4 — CLI JSON output</h3>\n<pre><code class=\"language-json\">{\n  \"status\": \"fail\",\n  \"skill_id\": \"closure-author\",\n  \"violation\": {\n    \"kind\": \"description_format\",\n    \"sub_code\": \"insufficient_triggers\",\n    \"found\": 0,\n    \"needed\": 2,\n    \"description_length\": 97\n  }\n}</code></pre>\n<h2>§9 — Open questions</h2>\n<p><strong>All resolved during authoring.</strong></p>\n<p>Deferred to follow-up FRs (out of scope here):</p>\n<ul><li><strong>FR-SKILL-115</strong> (placeholder — not yet specified): localised descriptions (<code>description_localized.&lt;lang&gt;</code>) for VN-locale skills. Per §1 #10. Phase P2+.</li><li><strong>FR-SKILL-116</strong> (placeholder — not yet specified): classifier-feedback loop — feed real user phrasings that triggered (or failed to trigger) each skill back into the description's trigger phrases. Phase P2+ when OBS dashboards have enough volume.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Author writes description ≤80 chars</td><td><code>cyberos skill validate</code> → <code>TooShort</code>; CI gate fails</td><td>Skill not promoted to <code>accepted</code></td><td>Author rewrites; re-runs validate</td></tr><tr><td>Author writes description &gt;1024 chars</td><td><code>cyberos skill validate</code> → <code>TooLong</code>; CI gate fails</td><td>Skill not promoted</td><td>Author trims; re-runs validate</td></tr><tr><td>Author embeds <code>&lt;some-marker/&gt;</code> in description</td><td>Validator → <code>ForbiddenBrackets</code>; CI gate fails</td><td>Skill not promoted</td><td>Author rewrites without brackets; if a marker is needed, move it to <code>metadata:</code></td></tr><tr><td>Author writes <code>Helps with X</code> (no verb stem)</td><td>Validator → <code>MissingWhat</code>; CI gate fails</td><td>Skill not promoted</td><td>Author rewrites with concrete action verb</td></tr><tr><td>Author writes single quoted trigger</td><td>Validator → <code>InsufficientTriggers { found: 1, needed: 2 }</code></td><td>Skill not promoted</td><td>Author adds second trigger phrase</td></tr><tr><td>Author writes 2 negatives + 0 positives</td><td>Validator → <code>InsufficientTriggers { found: 0, needed: 2 }</code></td><td>Skill not promoted</td><td>Author adds positive trigger forms</td></tr><tr><td>Production skill backfilled wrong — trigger phrase doesn't match classifier behaviour</td><td>OBS reports <code>acceptance_rate</code> drop on that skill; manual fine-tune triggered (per <code>human_fine_tune.signals_to_initiate</code>)</td><td>Auto-pause at &lt;40% per DEC-055</td><td>Fine-tune cycle: update description triggers; re-test against TRIGGER_TESTS.md (FR-SKILL-112 when shipped)</td></tr><tr><td>Two skills' descriptions overlap in trigger phrases</td><td>Supervisor classifier returns ambiguous routing; surfaces Question primitive (\"which workflow do you mean?\")</td><td>User clarifies</td><td>Fine-tune both skills: differentiate triggers; cross-reference in <code>## When to invoke this skill</code> body section</td></tr><tr><td>Description triggers stale (skill behaviour changed but description didn't)</td><td>Auditor rule FM-112 still passes (triggers exist); but OBS shows <code>acceptance_rate</code> drop</td><td>Operator-initiated fine-tune</td><td>Update description AND body section together (§1 #15 keeps them in sync)</td></tr><tr><td>Author flattens YAML multi-line wrong (e.g. `</td><td>-<code> instead of </code>&gt;-`)</td><td>YAML parser preserves newlines; flattened length still under cap; tests still pass — but the host's prompt receives literal <code>\\n</code> characters</td><td>Cosmetic issue only; no functional break</td><td>Documentation note in feature-request-audit skill §3.13 prefers <code>&gt;-</code> (folded)</td></tr><tr><td>JSONSchema mirror drifts from Rust validator</td><td>CI gate runs both validators against fixtures; mismatch on any one fixture → CI fails</td><td>Build broken</td><td>Sync schema.rs ↔ skill.schema.json via <code>cargo xtask schema</code> (FR-SKILL-103 §11)</td></tr><tr><td>OTel span attribute schema drifts</td><td>Dashboard panels expecting <code>outcome</code> attribute see <code>validation_outcome</code> after a rename → no data</td><td>Visible in OBS dashboard panels</td><td>Rename only via MINOR-bump + dashboard migration; never silently</td></tr><tr><td>FM-112 rule mis-classifies a verb stem (e.g. <code>track</code> matches but author meant another sense)</td><td>Rule auto-fix never fires (always <code>needs_human</code>); operator reviews and either accepts the suggestion or extends the verb-stem regex via a future PATCH</td><td>No silent damage</td><td>Extend <code>VERB_STEMS</code> regex via PR; bump RUBRIC PATCH version</td></tr><tr><td>Backfill exemplar (feature-request-author) inadvertently breaks a working chain</td><td>Per-skill acceptance fixture catches it on next CI run</td><td>Chain works on golden fixture but fails on a real PRD</td><td>Rollback description change; re-author with the existing trigger phrases preserved</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why not enforce trigger-phrase format at YAML-parse time?</strong> Could be a JSONSchema regex, but that regex would be unwieldy (<code>(?s)^.*\"[^\"]+\".*\"[^\"]+\".*$</code> works for two but breaks on negative-trigger detection). Pushing this to the Rust validator gives us precise sub-code errors and keeps JSONSchema simple. The JSONSchema mirror handles length + bracket guard only; full semantic in Rust.</li><li><strong>Why 80-char floor and not 100?</strong> Empirical. 74 chars (<code>\"Audit FRs. Use when user asks to \\\"audit FR\\\" or \\\"check FR\\\". Reports issues.\"</code>) hits 2 trigger phrases + WHAT verb + brief value — but feels tight. 80 gives slack for a slightly longer outcome phrase. Below 80 the descriptions read as cryptic; above 80 they breathe.</li><li><strong>Why verb-stem regex and not a model-based check?</strong> Determinism. CI gates need byte-stable outcomes. A model-based \"is this a verb-action description?\" check would drift between model versions and break reproducibility. The regex is conservative (29 verb stems); operators extend it via PR when a legitimate new verb appears. False negatives are caught at audit (verdict: <code>needs_human</code> — operator can override).</li><li><strong>Why preserve <code>## When to invoke this skill</code> body section?</strong> Three reasons in §1 #15 + §2 — but the deeper one is <strong>audit-trail integrity</strong>. The body is what the supervisor reads at classify_act; the frontmatter is what hosts read at level-1 discovery. The two serve different layers; merging them would force one or the other to compromise. Keeping both is a small cost.</li><li><strong>Lazy backfill is the right strategy.</strong> A 104-pair sweep in one batch would consume ~30 hours and inject inconsistency into the audit ledger (every skill bumps <code>skill_version</code> in one commit batch). Lazy backfill — fire FM-112 only on <code>status: accepted</code>+ skills, surface during natural fine-tune — spreads the fix over 4-6 weeks of normal cadence. No big-bang risk.</li><li><strong>Two trigger-phrase forms is a floor, not a ceiling.</strong> Skills with broad surface (e.g. <code>chain-selector</code>) may want 5+ triggers. The validator never complains about &gt;2; it only complains about &lt;2.</li><li><strong>Authors who hate the verb-stem regex</strong> can propose extensions via PR + RUBRIC bump. The list is intentionally conservative — adding 50 verbs would let <code>Helps with X</code> slip through. A tight regex with a clear extension protocol is healthier than a permissive regex that admits anti-patterns.</li><li><strong>Cross-host portability is the load-bearing reason for this FR.</strong> Inside CyberOS, the supervisor reads the body, and trigger-phrase placement doesn't matter. Outside CyberOS — Phase B transpilers, partner connectors, the eventual OCI registry per FR-SKILL-102 — the host follows Anthropic's contract. FR-SKILL-111 makes CyberOS skills portable on day one of Phase B without an emergency port-surface sweep.</li><li><strong>FR-SKILL-112's TRIGGER_TESTS.md complements this FR</strong>. Once both ship, the workflow becomes: description carries triggers (111) → TRIGGER_TESTS.md asserts the classifier matches those triggers (112) → CI catches drift. The two FRs are independent but together close the routing gap entirely.</li></ul>\n<hr>\n<p><em>End of FR-SKILL-111.</em></p>";
