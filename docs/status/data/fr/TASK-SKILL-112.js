window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-112"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>This task establishes the <code>acceptance/TRIGGER_TESTS.md</code> convention — a per-skill fixture listing positive and negative trigger phrases that the supervisor classifier MUST match correctly. It closes the third Anthropic-guide test layer (triggering tests) that CyberOS currently lacks (per <code>modules/skill/ANTHROPIC_GUIDE_DIGEST.md</code> §5.1 Gap 2).</p>\n<ol><li>Every production skill (<code>status: accepted</code> or higher) <strong>MUST</strong> carry an <code>acceptance/TRIGGER_TESTS.md</code> file at <code>&lt;skill-folder&gt;/acceptance/TRIGGER_TESTS.md</code>. The file declares positive trigger phrases the supervisor classifier MUST route to this skill, and negative phrases the classifier MUST NOT route here.</li><li>The file's frontmatter <strong>MUST</strong> declare <code>skill_id</code> (kebab-case folder name) + <code>min_confidence: &lt;float in [0.0, 1.0]&gt;</code> (lowest classifier confidence accepted as a positive match — default <code>0.7</code>) + <code>classifier_version: &lt;semver&gt;</code> (the CUO router version the fixtures were authored against — protects against silent classifier regressions).</li><li>The file's body <strong>MUST</strong> contain two sections — <code>## Positive triggers (MUST route here)</code> and <code>## Negative triggers (MUST NOT route here)</code> — each containing a bulleted list of natural-language phrases (one phrase per bullet, ≤120 chars per phrase, in human-natural form).</li><li>Every production skill <strong>MUST</strong> carry <strong>≥ 3 positive triggers</strong> AND <strong>≥ 3 negative triggers</strong>. The 3-floor balances coverage against authoring cost. Skills with broader surface SHOULD scale up — <code>chain-selector</code> may carry 8-12 positive; <code>task-audit</code> may carry 5-8.</li><li>Positive triggers <strong>MUST</strong> be distinct <strong>paraphrases</strong> — not lexical variants. <code>\"draft a task\"</code> and <code>\"author a task\"</code> are duplicates (same verb meaning, same noun); <code>\"draft a task\"</code> and <code>\"turn this PRD into a backlog\"</code> are paraphrases (different surface, same intent). The validator rejects positive-trigger lists where any two phrases have edit distance ≤3 (single-character variants).</li><li>Negative triggers <strong>MUST</strong> be drawn from one of three pools: (a) phrases that route to a sibling skill in the same persona (e.g. <code>task-author</code> → <code>task-audit</code>), (b) phrases that route to a different persona (cross-persona disambiguation), (c) phrases that should NOT route to any skill (the supervisor returns \"I'm not sure which workflow\"). Each negative trigger SHOULD carry an inline <code>→</code> annotation pointing to the <em>expected</em> target skill or <code>→ none</code> for the \"no workflow\" case.</li><li>The CUO supervisor's classifier <strong>MUST</strong> be invocable via a Python entry point <code>cyberos.cuo.trigger_tests.run_for_skill(skill_path: Path) -&gt; TriggerTestResult</code>. The function reads <code>acceptance/TRIGGER_TESTS.md</code>, invokes <code>cyberos.cuo.router.classify</code> per the existing v3 supervisor (TASK-CUO-101 / TASK-CUO-103) for each phrase, asserts the routing matches the fixture, and returns a <code>TriggerTestResult</code> with per-phrase verdicts.</li><li>The CI gate <strong>MUST</strong> run <code>cyberos.cuo.trigger_tests.run_all()</code> as part of every <code>cuo</code> test invocation. Failures block merge for any skill at <code>status: accepted</code> or higher. Skills at <code>status: draft</code> produce CI warnings only.</li><li>The CUO supervisor's boot path <strong>MUST NOT</strong> fail if a skill lacks <code>TRIGGER_TESTS.md</code>; the file is a build-time + CI-time artefact, not runtime. Missing file → supervisor logs <code>WARNING: skill &lt;id&gt; has no TRIGGER_TESTS.md (rule FM-113 fires only at audit time)</code> and continues. Graceful degradation is required so a partially-backfilled catalog doesn't break runtime.</li><li>The auditor rule <strong>MUST</strong> be <code>FM-113 trigger-tests-present</code> with severity <code>error</code> for production skills (<code>status: accepted</code> or higher); severity <code>warning</code> for <code>status: draft</code>. Issue verdict <code>needs_human</code> — the auditor never auto-generates trigger phrases (they require human knowledge of how users actually phrase requests).</li><li>Existing skills <strong>MUST</strong> be backfilled lazily — the rule fires only on <code>status: accepted</code> or higher; scaffold/draft skills are exempt. The next fine-tune cycle for each production skill brings it into compliance as a normal artefact-add (per <code>human_fine_tune.required_artifacts</code> extension).</li><li>The fixture file <strong>SHOULD</strong> carry a section <code>## Authoring notes</code> explaining where the trigger phrases came from. Three acceptable sources: (a) OBS-mined real user phrasings (when available), (b) Anthropic guide examples paraphrased for the skill, (c) author's a-priori intuition documented as such. Phrases sourced from (a) carry higher trust than (c).</li><li>When TASK-SKILL-111's description-format check passes (skill carries ≥2 trigger phrases in description) AND TASK-SKILL-112's TRIGGER_TESTS.md passes (classifier actually routes those phrases), the skill is <strong>routing-stable</strong>. The README Part 13.1 validation pyramid grows a new explicit layer between Layer 1 (mechanical) and Layer 2 (functional) — call it <strong>Layer 1.5: triggering</strong>. CI gates run Layer 1 → Layer 1.5 → Layer 2 in order.</li><li>The fixture <strong>MUST</strong> be byte-stable across runs against the same classifier_version. Re-running the same TRIGGER_TESTS.md against the same router version MUST produce identical pass/fail verdicts. (Non-determinism in the classifier is a separate concern surfaced by <code>deterministic_drift</code> anomaly signal per <code>_template/author/SKILL.md</code> line 105.)</li><li>The fixture's <code>min_confidence</code> floor <strong>MUST</strong> be ≥ the skill's <code>confidence_band.defer_below</code> (per <code>_template/author/SKILL.md</code> line 92). A skill that defers below 0.5 confidence cannot meaningfully be tested with <code>min_confidence: 0.3</code> — the test would accept a result the skill itself would reject. The validator enforces the relationship.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a separate file rather than extending TASK-SKILL-111's description-format rule (§1 #1)?</strong> Two different concerns. TASK-SKILL-111 enforces <em>what the description says</em>; TASK-SKILL-112 enforces <em>what the classifier actually does</em>. The description can carry the right phrases and the classifier can still route wrong (e.g. a recently-updated sibling skill's description now overlaps; the classifier's softmax tips the scales the wrong way). Separating them gives orthogonal regression-catch: 111 catches description regressions; 112 catches classifier regressions.</p>\n<p><strong>Why ≥3 positive + ≥3 negative as the floor (§1 #4)?</strong> Below 3 doesn't catch the paraphrase variance space (Anthropic's guide p. 15 example test suites have 3-5 each). Above 5 inflates authoring cost. The 3-floor is the smallest number that catches typical user phrasing variations without padding the fixture with near-duplicates.</p>\n<p><strong>Why mandate paraphrase distinctness (§1 #5)?</strong> A fixture with <code>\"draft a task\"</code> + <code>\"author a task\"</code> + <code>\"write a task\"</code> tests one phrasing three times — the classifier either matches all three or none. A fixture with <code>\"draft a task\"</code> + <code>\"turn this PRD into a backlog\"</code> + <code>\"expand the spec into tasks\"</code> tests three different paraphrases — the classifier might match all three (good — wide trigger surface) or only one (signals the description is too narrow). Edit-distance ≤3 catches single-character drift (<code>\"audit\"</code> vs <code>\"audits\"</code>); larger differences are accepted.</p>\n<p><strong>Why three pools for negative triggers (§1 #6)?</strong> Each pool catches a different failure class:</p>\n<ul><li>Pool (a) — sibling routing: catches when a description widens and now overlaps a sister skill.</li><li>Pool (b) — cross-persona: catches when a description's domain language overlaps another persona's surface (e.g. <code>chro</code> and <code>chief-people-officer</code> overlap).</li><li>Pool (c) — no-skill: catches over-trigger on completely unrelated phrases (\"What's the weather?\").</li></ul>\n<p>The inline <code>→</code> annotation makes the expected target explicit and human-readable; without it, \"negative trigger\" is ambiguous.</p>\n<p><strong>Why an inline <code>→ &lt;skill&gt;</code> annotation (§1 #6)?</strong> Documentation as test. A reader sees <code>- \"audit my task collection\" → task-audit</code> and immediately understands \"this skill must NOT match this phrase; the right skill is task-audit\". The annotation is also machine-readable: the validator can assert the classifier <em>did</em> route to the named target, not just <em>didn't</em> route here. This catches false-positives where the classifier happened to route nowhere for an unrelated reason.</p>\n<p><strong>Why a Python entry point in the CUO module rather than the broker (§1 #7)?</strong> The classifier lives in CUO v3 (TASK-CUO-101). The classifier's stage-1 (filesystem-catalog domain-language fallback) and stage-2 (LLM router) both run in Python. Adding a sibling module <code>cyberos.cuo.trigger_tests</code> keeps the test fixture close to the thing under test. The broker (Rust) is the wrong layer for classifier testing — the broker validates frontmatter shape, not classifier behaviour.</p>\n<p><strong>Why CI integration via <code>cuo</code> test suite (§1 #8)?</strong> The CUO v3 test suite already exists (per memory <code>cuo Phase 4</code> — 49/50 tests pass). Adding <code>test_trigger_tests.py</code> to that suite keeps the gate close to the code being tested. The alternative — a standalone CI gate — would split the test infrastructure.</p>\n<p><strong>Why graceful degradation on missing file (§1 #9)?</strong> Lazy backfill means production skills accumulate TRIGGER_TESTS.md over weeks. If the supervisor refused to boot without 100% coverage, the rollout couldn't begin. Boot-time logging + audit-time enforcement is the same pattern AGENTS.md uses for memory files (warn + continue, audit later).</p>\n<p><strong>Why severity scheme matches TASK-SKILL-111 (§1 #10)?</strong> Both rules are about portability + classifier robustness; both protect production skills; both allow draft skills to iterate without friction. Consistent severity makes the auditor's behaviour predictable.</p>\n<p><strong>Why no auto-generation of trigger phrases (§1 #10)?</strong> Trigger phrases require <em>human</em> knowledge: what does this user demographic actually type? An LLM could synthesise plausible phrases but would systematically miss domain-specific jargon, regional phrasing (VN-locale users), and inside-baseball terminology. The auditor verdict is always <code>needs_human</code> for this reason.</p>\n<p><strong>Why an <code>## Authoring notes</code> source-attribution section (§1 #12)?</strong> Trust calibration. Phrases mined from real OBS logs are higher-trust than author intuition. When a fixture fails CI months later, the source-attribution tells the operator whether to update the fixture (intuition was wrong) or the description (real users have moved on). Without source-attribution, every CI failure looks the same.</p>\n<p><strong>Why the new \"Layer 1.5: triggering\" tier in the validation pyramid (§1 #13)?</strong> Anthropic's guide Chapter 3 lists three test areas (triggering / functional / performance). CyberOS's current pyramid has 1 (mechanical) / 2 (functional) / 3 (operational). Inserting 1.5 between 1 and 2 honours the Anthropic structure while keeping CyberOS's existing layers. The numbering 1.5 signals \"between\" — Layer 1 is structure, Layer 1.5 is routing, Layer 2 is behaviour.</p>\n<p><strong>Why byte-stable verdicts across runs (§1 #14)?</strong> Reproducibility is a CyberOS invariant (per <code>_template/audit/SKILL.md</code> line 136 <code>determinism.reproducible: true</code> for auditor skills). The trigger-test fixture inherits the same invariant: re-running against the same classifier_version produces identical results. Drift across runs surfaces <code>deterministic_drift</code> per the existing anomaly-signal framework.</p>\n<p><strong>Why <code>min_confidence ≥ defer_below</code> (§1 #15)?</strong> The skill's own confidence band declares what it considers \"I'm sure enough to act\"; testing against a lower bar would let phrases pass that the skill itself would refuse to act on. The validator enforces the relationship so authors can't accidentally weaken the test.</p>\n<h2>§3 — API contract</h2>\n<h3>Fixture format — <code>acceptance/TRIGGER_TESTS.md</code></h3>\n<pre><code class=\"language-markdown\">---\nskill_id: task-author\nmin_confidence: 0.7\nclassifier_version: 3.0.0-a4\n---\n\n# TRIGGER_TESTS for task-author\n\n## Positive triggers (MUST route here)\n\n- \"Turn this PRD into a backlog of tasks\"\n- \"Draft a task for the new email-bounce handling\"\n- \"Expand the spec into task markdowns\"\n- \"Generate the task backlog from these source docs\"\n\n## Negative triggers (MUST NOT route here)\n\n- \"Audit this existing task for completeness\" → task-audit\n- \"Has TASK-007 changed since the last audit?\" → task-audit\n- \"Draft a tech spec from this task\" → fr-to-tech-spec\n- \"What's our company holiday schedule?\" → none\n\n## Authoring notes\n\n- Positive triggers 1-3 derived from real OBS user phrasings observed during the v0.2.0 pilot (week 2026-W18).\n- Positive trigger 4 is author intuition (canonical CyberOS phrasing).\n- Negative triggers 1-2 derived from common confusion observed in pilot (users confused the author/audit pair).\n- Negative trigger 3 derived from the planned fr-to-tech-spec routing (TASK-SKILL-111 description-format makes this trigger unambiguous).\n- Negative trigger 4 is a canonical \"no skill\" sanity case.</code></pre>\n<h3>Python entry point — <code>modules/cuo/cuo/trigger_tests.py</code></h3>\n<pre><code class=\"language-python\">\"\"\"Trigger-test runner for TASK-SKILL-112.\n\nLoads TRIGGER_TESTS.md fixtures and runs each phrase against the CUO router.\nAsserts the routing matches the fixture's expectations.\n\"\"\"\n\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Iterable\n\nimport yaml\n\nfrom cyberos.cuo.router import classify, ClassificationResult\n\n\n@dataclass(frozen=True)\nclass TriggerTestRow:\n    phrase: str\n    expected_skill: str | None  # None = MUST NOT match this skill; \"&lt;skill_id&gt;\" = MUST route to this; \"none\" = MUST route nowhere\n    is_positive: bool\n\n\n@dataclass(frozen=True)\nclass TriggerTestResult:\n    skill_id: str\n    classifier_version: str\n    rows: list[tuple[TriggerTestRow, ClassificationResult, bool]]  # (input, output, passed)\n\n    @property\n    def passed(self) -&gt; bool:\n        return all(passed for _, _, passed in self.rows)\n\n    @property\n    def failures(self) -&gt; list[tuple[TriggerTestRow, ClassificationResult]]:\n        return [(r, c) for r, c, p in self.rows if not p]\n\n\ndef load_fixture(path: Path) -&gt; tuple[dict, list[TriggerTestRow]]:\n    \"\"\"Parse TRIGGER_TESTS.md into (frontmatter, rows).\"\"\"\n    text = path.read_text(encoding=\"utf-8\")\n    # Split frontmatter\n    if not text.startswith(\"---\\n\"):\n        raise ValueError(f\"{path}: missing frontmatter\")\n    end = text.index(\"\\n---\\n\", 4)\n    fm = yaml.safe_load(text[4:end])\n    body = text[end + 5:]\n\n    rows: list[TriggerTestRow] = []\n    section = None\n    for line in body.splitlines():\n        stripped = line.strip()\n        if stripped.startswith(\"## Positive\"):\n            section = \"positive\"\n        elif stripped.startswith(\"## Negative\"):\n            section = \"negative\"\n        elif stripped.startswith(\"## \"):\n            section = None\n        elif stripped.startswith(\"- \") and section in (\"positive\", \"negative\"):\n            content = stripped[2:].strip()\n            # Parse: `\"&lt;phrase&gt;\"` or `\"&lt;phrase&gt;\" → &lt;target&gt;`\n            if section == \"positive\":\n                phrase = content.strip('\"').rstrip()\n                # Strip trailing quote variants\n                if phrase.endswith('\"'):\n                    phrase = phrase[:-1]\n                rows.append(TriggerTestRow(\n                    phrase=phrase,\n                    expected_skill=fm[\"skill_id\"],\n                    is_positive=True,\n                ))\n            else:  # negative\n                if \"→\" in content:\n                    phrase_part, target_part = content.rsplit(\"→\", 1)\n                    phrase = phrase_part.strip().strip('\"').strip()\n                    target = target_part.strip()\n                    expected = None if target == \"none\" else target\n                else:\n                    phrase = content.strip('\"').strip()\n                    expected = None\n                rows.append(TriggerTestRow(\n                    phrase=phrase,\n                    expected_skill=expected,\n                    is_positive=False,\n                ))\n    return fm, rows\n\n\ndef run_for_skill(skill_path: Path) -&gt; TriggerTestResult:\n    \"\"\"Run TRIGGER_TESTS.md fixture for one skill against the classifier.\"\"\"\n    fixture_path = skill_path / \"acceptance\" / \"TRIGGER_TESTS.md\"\n    if not fixture_path.exists():\n        raise FileNotFoundError(f\"No TRIGGER_TESTS.md at {fixture_path}\")\n    fm, rows = load_fixture(fixture_path)\n    skill_id = fm[\"skill_id\"]\n    min_confidence = float(fm.get(\"min_confidence\", 0.7))\n    classifier_version = fm.get(\"classifier_version\", \"unknown\")\n\n    out_rows: list[tuple[TriggerTestRow, ClassificationResult, bool]] = []\n    for row in rows:\n        result = classify(row.phrase)\n        if row.is_positive:\n            # MUST route to this skill with confidence ≥ min_confidence\n            passed = (result.skill_id == skill_id and result.confidence &gt;= min_confidence)\n        else:\n            # MUST NOT route to this skill; SHOULD route to expected_skill (if specified)\n            if row.expected_skill is None:\n                passed = (result.skill_id != skill_id)\n            else:\n                passed = (result.skill_id == row.expected_skill and result.confidence &gt;= min_confidence)\n        out_rows.append((row, result, passed))\n\n    return TriggerTestResult(\n        skill_id=skill_id,\n        classifier_version=classifier_version,\n        rows=out_rows,\n    )\n\n\ndef run_all(catalog_root: Path) -&gt; dict[str, TriggerTestResult]:\n    \"\"\"Run every TRIGGER_TESTS.md in the catalog and return per-skill results.\"\"\"\n    results: dict[str, TriggerTestResult] = {}\n    for fixture in catalog_root.glob(\"**/acceptance/TRIGGER_TESTS.md\"):\n        skill_path = fixture.parent.parent\n        try:\n            results[skill_path.name] = run_for_skill(skill_path)\n        except FileNotFoundError:\n            continue\n    return results</code></pre>\n<h3>Auditor rule (added to <code>modules/skill/task-audit/RUBRIC.md</code>)</h3>\n<pre><code class=\"language-markdown\">### FM-113 — trigger-tests-present\n\n**Statement:** Every production skill (`status: accepted` or higher) MUST carry `acceptance/TRIGGER_TESTS.md` with ≥3 positive + ≥3 negative triggers conforming to TASK-SKILL-112 §1. Phrases MUST be paraphrase-distinct (edit-distance &gt; 3).\n\n**Severity:** error on `status: accepted | building | shipped`; warning on `status: draft`.\n\n**Auto-fix:** never (trigger phrases require human authorship — verdict `needs_human`).\n\n**Check (deterministic):** invoke `python -m cyberos.cuo.trigger_tests &lt;skill_path&gt;`; if exit code non-zero OR the result's `.passed` is False, the rule fails. Specific sub-codes via process stderr: `fixture_missing` | `insufficient_positive` | `insufficient_negative` | `paraphrase_duplicate` | `classifier_routing_mismatch`.</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Fixture parses</strong> — a well-formed <code>TRIGGER_TESTS.md</code> with 4 positive + 4 negative triggers → <code>load_fixture</code> returns <code>(frontmatter_dict, list[TriggerTestRow])</code> with 8 rows.</li><li><strong>Fixture rejected if missing frontmatter</strong> — file without leading <code>---</code> → <code>ValueError</code>.</li><li><strong>Fixture rejected if &lt; 3 positive triggers</strong> — fixture with 2 positive → auditor rule FM-113 fires <code>insufficient_positive</code>.</li><li><strong>Fixture rejected if &lt; 3 negative triggers</strong> — fixture with 2 negative → auditor rule FM-113 fires <code>insufficient_negative</code>.</li><li><strong>Paraphrase-distinct check</strong> — fixture with <code>\"draft a task\"</code> + <code>\"draft a task\"</code> (edit-distance 2) → FM-113 fires <code>paraphrase_duplicate</code>.</li><li><strong>Positive trigger routing PASS</strong> — classifier returns this skill with confidence ≥ min_confidence → row pass.</li><li><strong>Positive trigger routing FAIL</strong> — classifier returns different skill OR confidence &lt; min_confidence → row fail.</li><li><strong>Negative trigger routing PASS — no expected target</strong> — classifier returns NOT this skill (anything else) → row pass.</li><li><strong>Negative trigger routing PASS — expected target named</strong> — classifier returns the named expected skill with confidence ≥ min_confidence → row pass; if classifier routes nowhere or to a third skill → row fail.</li><li><strong>Negative trigger routing FAIL</strong> — classifier returns this skill (the negative trigger leaked) → row fail.</li><li><strong><code>run_for_skill</code> integration</strong> — given a valid skill folder, the function returns a <code>TriggerTestResult</code> whose <code>.passed</code> is True if all rows pass, False otherwise.</li><li><strong><code>run_all</code> catalog walk</strong> — given the catalog root, returns one <code>TriggerTestResult</code> per skill that has a <code>TRIGGER_TESTS.md</code>; skills without the file are skipped (not failures).</li><li><strong>Missing fixture on production skill triggers FM-113 audit issue</strong> — <code>status: accepted</code> skill without <code>TRIGGER_TESTS.md</code> → auditor reports one FM-113 issue with severity <code>error</code>.</li><li><strong>Missing fixture on draft skill triggers FM-113 warning</strong> — <code>status: draft</code> skill without <code>TRIGGER_TESTS.md</code> → auditor reports one FM-113 issue with severity <code>warning</code>.</li><li><strong>Backfill exemplar — task-author</strong> — <code>modules/skill/task-author/acceptance/TRIGGER_TESTS.md</code> exists, 4 positive + 4 negative; <code>run_for_skill</code> returns <code>.passed = True</code> against current classifier (3.0.0-a4).</li><li><strong>Backfill exemplar — task-audit</strong> — analogous; positive triggers ARE the negative triggers of the author exemplar (cross-reference verified).</li><li><strong>Backfill exemplar — prd-author</strong> — analogous; positive triggers anchored on PRD-specific phrasings.</li><li><strong><code>min_confidence ≥ defer_below</code> validator enforced</strong> — fixture with <code>min_confidence: 0.3</code> for a skill with <code>confidence_band.defer_below: 0.5</code> → auditor reports FM-113 with <code>confidence_relationship_invalid</code>.</li><li><strong>Graceful degradation on supervisor boot</strong> — CUO supervisor starts with one skill missing TRIGGER_TESTS.md → boots successfully with one WARNING log line; no crash.</li><li><strong>CI gate exit code</strong> — <code>python -m cyberos.cuo.trigger_tests --catalog modules/skill/</code> → exit 0 if all production skills pass, exit 1 if any fail.</li><li><strong>Reproducibility</strong> — running <code>run_all</code> twice against the same classifier_version → byte-identical results (allowing for the test execution's own ordering nondeterminism, which the test sorts).</li><li><strong>README Part 13.2 validation pyramid updated</strong> — the diagram + table reflect the new Layer 1.5 (triggering).</li><li><strong>task-audit skill §3.10 entry added</strong> — rule 41 references TASK-SKILL-112 and the TRIGGER_TESTS.md convention.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/cuo/tests/test_trigger_tests.py\nimport pytest\nfrom pathlib import Path\nfrom cyberos.cuo.trigger_tests import (\n    load_fixture, run_for_skill, run_all, TriggerTestRow, TriggerTestResult,\n)\n\nFIXTURES = Path(__file__).parent / \"fixtures\" / \"trigger_tests\"\n\n\ndef test_load_well_formed_fixture(tmp_path: Path):\n    fixture = tmp_path / \"TRIGGER_TESTS.md\"\n    fixture.write_text(\"\"\"---\nskill_id: foo-author\nmin_confidence: 0.7\nclassifier_version: 3.0.0-a4\n---\n\n# TRIGGER_TESTS for foo-author\n\n## Positive triggers (MUST route here)\n\n- \"draft a foo\"\n- \"turn this bar into a foo\"\n- \"generate the foo backlog\"\n\n## Negative triggers (MUST NOT route here)\n\n- \"audit my foo\" → foo-audit\n- \"draft a quux\" → quux-author\n- \"what is the weather?\" → none\n\"\"\", encoding=\"utf-8\")\n    fm, rows = load_fixture(fixture)\n    assert fm[\"skill_id\"] == \"foo-author\"\n    assert fm[\"min_confidence\"] == 0.7\n    assert len(rows) == 6\n    positives = [r for r in rows if r.is_positive]\n    negatives = [r for r in rows if not r.is_positive]\n    assert len(positives) == 3\n    assert len(negatives) == 3\n    # Negative annotations parsed\n    assert negatives[0].expected_skill == \"foo-audit\"\n    assert negatives[2].expected_skill is None  # \"→ none\"\n\n\ndef test_load_missing_frontmatter(tmp_path: Path):\n    fixture = tmp_path / \"TRIGGER_TESTS.md\"\n    fixture.write_text(\"# No frontmatter here\\n\", encoding=\"utf-8\")\n    with pytest.raises(ValueError, match=\"missing frontmatter\"):\n        load_fixture(fixture)\n\n\ndef test_run_for_skill_all_pass(monkeypatch, tmp_path: Path):\n    # Stub classify to return foo-author for positive phrases, foo-audit for negatives.\n    from cyberos.cuo import router\n\n    def fake_classify(phrase: str):\n        if \"audit\" in phrase:\n            return router.ClassificationResult(skill_id=\"foo-audit\", confidence=0.9)\n        if \"quux\" in phrase:\n            return router.ClassificationResult(skill_id=\"quux-author\", confidence=0.85)\n        if \"weather\" in phrase:\n            return router.ClassificationResult(skill_id=None, confidence=0.0)\n        return router.ClassificationResult(skill_id=\"foo-author\", confidence=0.85)\n\n    monkeypatch.setattr(router, \"classify\", fake_classify)\n    monkeypatch.setattr(\"cyberos.cuo.trigger_tests.classify\", fake_classify)\n\n    skill_dir = tmp_path / \"foo-author\"\n    accept_dir = skill_dir / \"acceptance\"\n    accept_dir.mkdir(parents=True)\n    (accept_dir / \"TRIGGER_TESTS.md\").write_text(\"\"\"---\nskill_id: foo-author\nmin_confidence: 0.7\nclassifier_version: 3.0.0-a4\n---\n\n## Positive triggers (MUST route here)\n\n- \"draft a foo\"\n- \"turn this bar into a foo\"\n- \"generate the foo backlog\"\n\n## Negative triggers (MUST NOT route here)\n\n- \"audit my foo\" → foo-audit\n- \"draft a quux\" → quux-author\n- \"what is the weather?\" → none\n\"\"\", encoding=\"utf-8\")\n\n    result = run_for_skill(skill_dir)\n    assert result.passed is True\n    assert len(result.failures) == 0\n\n\ndef test_run_for_skill_positive_misroute(monkeypatch, tmp_path: Path):\n    from cyberos.cuo import router\n\n    def fake_classify(phrase: str):\n        # Bug: positive phrase routes to wrong skill\n        return router.ClassificationResult(skill_id=\"bar-author\", confidence=0.95)\n\n    monkeypatch.setattr(\"cyberos.cuo.trigger_tests.classify\", fake_classify)\n\n    skill_dir = tmp_path / \"foo-author\"\n    accept_dir = skill_dir / \"acceptance\"\n    accept_dir.mkdir(parents=True)\n    (accept_dir / \"TRIGGER_TESTS.md\").write_text(\"\"\"---\nskill_id: foo-author\nmin_confidence: 0.7\nclassifier_version: 3.0.0-a4\n---\n\n## Positive triggers (MUST route here)\n\n- \"draft a foo\"\n- \"turn this bar into a foo\"\n- \"generate the foo backlog\"\n\n## Negative triggers (MUST NOT route here)\n\n- \"irrelevant\" → none\n\"\"\", encoding=\"utf-8\")\n\n    result = run_for_skill(skill_dir)\n    assert result.passed is False\n    assert len(result.failures) &gt;= 3   # all positives fail\n\n\ndef test_run_for_skill_negative_leak(monkeypatch, tmp_path: Path):\n    from cyberos.cuo import router\n\n    def fake_classify(phrase: str):\n        # Bug: negative phrase leaks into this skill\n        return router.ClassificationResult(skill_id=\"foo-author\", confidence=0.95)\n\n    monkeypatch.setattr(\"cyberos.cuo.trigger_tests.classify\", fake_classify)\n\n    skill_dir = tmp_path / \"foo-author\"\n    accept_dir = skill_dir / \"acceptance\"\n    accept_dir.mkdir(parents=True)\n    (accept_dir / \"TRIGGER_TESTS.md\").write_text(\"\"\"---\nskill_id: foo-author\nmin_confidence: 0.7\nclassifier_version: 3.0.0-a4\n---\n\n## Positive triggers (MUST route here)\n\n- \"draft a foo\"\n- \"another\"\n- \"third\"\n\n## Negative triggers (MUST NOT route here)\n\n- \"audit my foo\" → foo-audit\n- \"draft a quux\" → quux-author\n- \"weather?\" → none\n\"\"\", encoding=\"utf-8\")\n\n    result = run_for_skill(skill_dir)\n    assert result.passed is False\n    # Negative rows leak into this skill — all 3 negatives fail\n    failures = [r for r, _, _ in result.failures]\n    assert all(not r.is_positive for r in failures)\n\n\ndef test_missing_fixture_raises(tmp_path: Path):\n    skill_dir = tmp_path / \"foo-author\"\n    skill_dir.mkdir()\n    with pytest.raises(FileNotFoundError):\n        run_for_skill(skill_dir)\n\n\ndef test_run_all_walks_catalog(monkeypatch, tmp_path: Path):\n    from cyberos.cuo import router\n\n    def fake_classify(phrase: str):\n        return router.ClassificationResult(skill_id=\"foo-author\", confidence=0.85)\n\n    monkeypatch.setattr(\"cyberos.cuo.trigger_tests.classify\", fake_classify)\n\n    # Two skills, one with TRIGGER_TESTS.md and one without\n    (tmp_path / \"foo-author\" / \"acceptance\").mkdir(parents=True)\n    (tmp_path / \"foo-author\" / \"acceptance\" / \"TRIGGER_TESTS.md\").write_text(\"\"\"---\nskill_id: foo-author\nmin_confidence: 0.7\nclassifier_version: 3.0.0-a4\n---\n\n## Positive triggers (MUST route here)\n- \"a\"\n- \"b\"\n- \"c\"\n\n## Negative triggers (MUST NOT route here)\n- \"x\" → none\n- \"y\" → none\n- \"z\" → none\n\"\"\", encoding=\"utf-8\")\n    (tmp_path / \"bar-author\").mkdir()\n\n    results = run_all(tmp_path)\n    assert \"foo-author\" in results\n    assert \"bar-author\" not in results  # gracefully skipped\n\n\ndef test_paraphrase_distinct_check():\n    # Edit-distance ≤3 between \"draft a task\" and \"draft a task\" → duplicate\n    from cyberos.cuo.trigger_tests import are_paraphrase_distinct\n    assert not are_paraphrase_distinct(\"draft a task\", \"draft a task\")\n    assert are_paraphrase_distinct(\"draft a task\", \"turn this PRD into a backlog\")\n\n\ndef test_confidence_relationship_validator():\n    # min_confidence ≥ defer_below required\n    from cyberos.cuo.trigger_tests import validate_confidence_relationship\n    # Skill with defer_below 0.5 — min_confidence must be ≥ 0.5\n    assert validate_confidence_relationship(min_confidence=0.5, defer_below=0.5) is True\n    assert validate_confidence_relationship(min_confidence=0.7, defer_below=0.5) is True\n    assert validate_confidence_relationship(min_confidence=0.3, defer_below=0.5) is False</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>§3 covers the new files. Wiring:</p>\n<ol><li><code>modules/cuo/cuo/trigger_tests.py</code> is added as a new module; re-exported from <code>modules/cuo/cuo/__init__.py</code>.</li><li><code>modules/cuo/cli.py</code> gains a <code>trigger-tests &lt;skill_path&gt;</code> subcommand that wraps <code>run_for_skill</code>.</li><li><code>modules/cuo/cli.py</code> gains a <code>trigger-tests-all</code> subcommand that wraps <code>run_all</code> and exits 0/1 based on catalog-wide pass.</li><li><code>modules/skill/task-audit/RUBRIC.md</code> gains FM-113 entry; the auditor's 8-step loop (per <code>_template/audit/AUDIT_LOOP.md</code>) loads the rubric at start and runs FM-113 along with every other FM-NNN rule.</li><li>CI integration: <code>cd modules/cuo &amp;&amp; python -m pytest tests/test_trigger_tests.py</code> is added to the existing pytest invocation (the test suite already runs on every PR — adding 7-9 new tests is additive).</li><li>Lazy backfill: as each production skill's next fine-tune cycle fires (via <code>human_fine_tune.signals_to_initiate</code>), the operator authors <code>acceptance/TRIGGER_TESTS.md</code> as part of the cycle's <code>required_artifacts</code> extension.</li></ol>\n<h2>§7 — Dependencies</h2>\n<p><strong>Depends on:</strong></p>\n<ul><li><strong>TASK-SKILL-103</strong> (frontmatter-extension) — provides the broker, <code>cyberos skill validate</code> CLI, and the structured-error machinery.</li><li><strong>TASK-CUO-101</strong> (CUO catalog scanner + 2-stage router) — provides <code>cyberos.cuo.router.classify</code> which the trigger-tests runner calls.</li></ul>\n<p><strong>Blocks:</strong> none.</p>\n<p><strong>Related:</strong></p>\n<ul><li><strong>TASK-SKILL-111</strong> (description-format enrichment) — complementary; 111 puts triggers in the description, 112 validates the classifier routes those triggers correctly.</li><li><strong>TASK-CUO-103</strong> (LLM router / Anthropic Messages API integration) — when the real LLM router is wired, the trigger-tests fixtures gate every classifier change.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>OBS module</strong> (TASK-OBS-001..009) — when OBS ships, the per-phrase routing telemetry feeds back as candidate positive-trigger additions to TRIGGER_TESTS.md (future v0.3.0 feedback loop, sketched in TASK-SKILL-116).</li></ul>\n<h2>§8 — Example payloads</h2>\n<h3>Example 1 — fixture (task-audit) with full source attribution</h3>\n<pre><code class=\"language-markdown\">---\nskill_id: task-audit\nmin_confidence: 0.7\nclassifier_version: 3.0.0-a4\n---\n\n# TRIGGER_TESTS for task-audit\n\n## Positive triggers (MUST route here)\n\n- \"Audit this task for completeness\"\n- \"Has TASK-007 changed since the last audit?\"\n- \"Tell me which tasks would fail acceptance today\"\n- \"Re-run the rubric against this task collection\"\n\n## Negative triggers (MUST NOT route here)\n\n- \"Turn this PRD into a backlog of tasks\" → task-author\n- \"Generate tasks from this spec\" → task-author\n- \"Draft a tech spec from this task\" → fr-to-tech-spec\n- \"What's the team's holiday schedule?\" → none\n\n## Authoring notes\n\n- Positive 1-3 derived from OBS week 2026-W18 (real user phrasings).\n- Positive 4 is author intuition — covers the \"re-audit\" repeat case.\n- Negative 1-2 from common author/audit confusion in pilot.\n- Negative 3 from the planned fr-to-tech-spec routing.\n- Negative 4 is the canonical \"no skill\" sanity case.</code></pre>\n<h3>Example 2 — TriggerTestResult JSON (CI output)</h3>\n<pre><code class=\"language-json\">{\n  \"skill_id\": \"task-audit\",\n  \"classifier_version\": \"3.0.0-a4\",\n  \"passed\": true,\n  \"rows\": [\n    {\n      \"phrase\": \"Audit this task for completeness\",\n      \"expected_skill\": \"task-audit\",\n      \"is_positive\": true,\n      \"classifier_result\": {\"skill_id\": \"task-audit\", \"confidence\": 0.91},\n      \"passed\": true\n    },\n    {\n      \"phrase\": \"Turn this PRD into a backlog of tasks\",\n      \"expected_skill\": \"task-author\",\n      \"is_positive\": false,\n      \"classifier_result\": {\"skill_id\": \"task-author\", \"confidence\": 0.88},\n      \"passed\": true\n    }\n  ]\n}</code></pre>\n<h3>Example 3 — audit issue block (FM-113 firing — fixture missing)</h3>\n<pre><code>ISSUE\nid:              ISS-009\nrule_id:         FM-113\nseverity:        error\ncategory:        trigger_tests_fixture\nlocation:        acceptance/TRIGGER_TESTS.md\nevidence:        \"file does not exist\"\ndescription:     \"Production skill (status: accepted) MUST carry acceptance/TRIGGER_TESTS.md with ≥3 positive + ≥3 negative triggers per TASK-SKILL-112 §1. The auditor cannot proceed without it.\"\nsuggestion:      \"Author acceptance/TRIGGER_TESTS.md. See modules/skill/_template/author/acceptance/TRIGGER_TESTS.md for the scaffold. Mine OBS logs (week-window) or use author intuition (document as such in ## Authoring notes).\"\nauto_fix_applied: false\nresolution:      null\nopened_at:       \"2026-05-19T14:00:00Z\"\nupdated_at:      \"2026-05-19T14:00:00Z\"</code></pre>\n<h3>Example 4 — audit issue block (FM-113 firing — paraphrase duplicate)</h3>\n<pre><code>ISSUE\nid:              ISS-010\nrule_id:         FM-113\nseverity:        error\ncategory:        paraphrase_duplicate\nlocation:        acceptance/TRIGGER_TESTS.md\nevidence:        \"Positive triggers: \\\"draft a task\\\" and \\\"draft a task\\\" (edit-distance 2)\"\ndescription:     \"Positive triggers must be paraphrase-distinct (edit-distance &gt; 3 per TASK-SKILL-112 §1 #5). \\\"draft a task\\\" and \\\"draft a task\\\" differ by 1 character — same surface, same intent. Replace one with a distinct paraphrase.\"\nsuggestion:      \"Replace \\\"draft a task\\\" with a paraphrase like \\\"turn this PRD into a backlog\\\" or \\\"author the task from this spec\\\". Aim for verb-or-noun substitution, not lexical variant.\"\nauto_fix_applied: false\nresolution:      null\nopened_at:       \"2026-05-19T14:00:00Z\"\nupdated_at:      \"2026-05-19T14:00:00Z\"</code></pre>\n<h2>§9 — Open questions</h2>\n<p><strong>All resolved during authoring.</strong></p>\n<p>Deferred to follow-up tasks:</p>\n<ul><li><strong>TASK-SKILL-116</strong> (placeholder — not yet specified): OBS-driven candidate trigger-phrase suggestions — mine real user phrasings; auto-propose additions to TRIGGER_TESTS.md as <code>refinement_proposal</code> envelopes for human review. Phase P2+, requires OBS to be live.</li><li><strong>TASK-SKILL-117</strong> (placeholder — not yet specified): trigger-test localisation — VN-locale users' phrasings drive a parallel <code>TRIGGER_TESTS.vi.md</code>. Phase P2+.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Production skill ships without TRIGGER_TESTS.md</td><td>Auditor rule FM-113 fires <code>fixture_missing</code> at next audit cycle</td><td>Skill cannot transition to <code>building</code> or <code>shipped</code></td><td>Author the fixture; re-run audit</td></tr><tr><td>Fixture has &lt; 3 positive triggers</td><td>FM-113 <code>insufficient_positive</code></td><td>Audit issue; skill stays in current status</td><td>Author additional triggers</td></tr><tr><td>Fixture has &lt; 3 negative triggers</td><td>FM-113 <code>insufficient_negative</code></td><td>Audit issue</td><td>Author additional negatives across the 3 pools</td></tr><tr><td>Paraphrase duplicate in positive list</td><td>FM-113 <code>paraphrase_duplicate</code></td><td>Audit issue</td><td>Replace one with a paraphrase-distinct alternative</td></tr><tr><td>Positive trigger misroutes (classifier picks another skill)</td><td><code>run_for_skill</code> returns <code>passed=False</code>; CI gate fails</td><td>Block merge</td><td>Investigate: was the description widened (TASK-SKILL-111)? Is the sibling skill's description overlapping? Adjust descriptions or trigger phrases</td></tr><tr><td>Negative trigger leaks (classifier picks this skill)</td><td><code>run_for_skill</code> returns <code>passed=False</code>; CI gate fails</td><td>Block merge</td><td>Same root cause as misroute; classifier overlap between siblings</td></tr><tr><td>Negative trigger expected target wrong</td><td><code>run_for_skill</code> reports row failure; CI gate fails</td><td>Block merge</td><td>Either update fixture expectation OR fix the target skill's description (depends on which is wrong)</td></tr><tr><td>Classifier version mismatch (fixture authored against v3.0.0-a4; CI runs v3.0.0-a5)</td><td>Mismatch logged as warning; tests still run</td><td>Fixture stays valid until classifier behaviour changes</td><td>When classifier MAJOR-bumps, sweep all TRIGGER_TESTS.md; update <code>classifier_version</code>; re-verify</td></tr><tr><td>min_confidence too low (&lt; defer_below)</td><td>Auditor rule FM-113 fires <code>confidence_relationship_invalid</code></td><td>Audit issue</td><td>Raise min_confidence to ≥ defer_below</td></tr><tr><td>Supervisor boots without TRIGGER_TESTS.md for some skills</td><td>Supervisor logs WARNING and continues per §1 #9</td><td>No runtime crash; audit catches it later</td><td>Lazy backfill via fine-tune cycle</td></tr><tr><td>Test execution non-determinism (parallel tests racing)</td><td><code>run_all</code> produces different orderings → flaky CI</td><td>Sort results by skill_id before assertion</td><td>Test code uses <code>sorted(...)</code> on iteration</td></tr><tr><td>Author copy-pastes positive triggers from a sibling skill</td><td>Both skills' fixtures pass individually but classify wrong in production (the same phrase MUST positively route to one skill — the other's \"MUST NOT\" fails)</td><td>CI gate catches: the sibling's negative trigger row fails because the classifier picks this skill</td><td>Author differentiates trigger phrasing across siblings</td></tr><tr><td>Real OBS phrasing doesn't appear in TRIGGER_TESTS.md (gap coverage)</td><td>OBS tracking metric <code>untested_trigger_rate</code> shows phrasings the classifier sees but no fixture covers</td><td>Coverage gap; not a failure per se</td><td>Lazy backfill: add the phrase to TRIGGER_TESTS.md in next fine-tune cycle</td></tr><tr><td>Test execution timeout (classify is slow for some phrase)</td><td>pytest timeout marker fires; test fails as timeout</td><td>CI flag</td><td>Increase pytest timeout for the trigger-tests test class; investigate router slowness</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why not auto-generate trigger phrases from descriptions?</strong> Two reasons. (1) Trigger phrases reflect real user phrasings, not the author's idealised description prose; auto-generation from the description would systematically miss the gap that this task is designed to surface. (2) Auto-generated fixtures would silently pass even when the description is wrong — the test would only verify \"the classifier matches what the description says\", not \"the classifier matches what real users type\". Human authorship of trigger phrases is the load-bearing design choice.</li><li><strong>Why edit-distance 3 for paraphrase-distinctness?</strong> Empirical. Edit-distance 1-2 catches single-character drift; edit-distance 3 catches one short word change (<code>a</code> → <code>the</code>); edit-distance &gt;3 catches genuine paraphrasing (different verbs, different nouns). The threshold can be tuned in a future PATCH — currently 3 is conservative.</li><li><strong>Why ≥3 floor for both positive and negative?</strong> Below 3 doesn't test the paraphrase-variance space. The fixture's value comes from covering distinct phrasings; 3 is the smallest set that demonstrates coverage. Above 5 inflates authoring cost; the rule never complains about &gt;5.</li><li><strong>Why three negative-trigger pools (sibling / cross-persona / none)?</strong> Each pool catches a different failure class. A fixture with all three pools represented makes the test resilient against multiple regression types — a description widening (sibling pool), a persona surface expansion (cross-persona pool), or an overall classifier softening (none pool).</li><li><strong>Why the <code>→ &lt;target&gt;</code> annotation on negative triggers?</strong> Documentation + machine-readable assertion. The annotation lets the test verify the classifier routes to the <em>expected</em> target, not just <em>doesn't</em> route here. This catches false-positives where the classifier happens to return nowhere for unrelated reasons.</li><li><strong>Why a Python module rather than a Rust crate?</strong> The classifier lives in CUO (Python per TASK-CUO-101). Keeping the test infrastructure in the same language as the thing under test keeps the type contracts honest. The broker (Rust per TASK-SKILL-103) handles frontmatter validation; CUO handles classifier behaviour. Both are correct layers for their respective concerns.</li><li><strong>Why graceful degradation on boot (§1 #9)?</strong> Lazy backfill needs a long ramp. If the supervisor refused to boot without 100% TRIGGER_TESTS.md coverage, the first deploy would fail at the first un-backfilled skill. WARN + continue lets the catalog evolve organically; the auditor catches gaps at the right cadence (per-skill fine-tune).</li><li><strong>Why CI integration via the existing CUO test suite rather than a new gate?</strong> Two reasons. (1) The CUO test suite already runs on every PR (49/50 tests pass per the v3.0.0-a4 memory entry); adding 7-9 trigger-tests tests is additive. (2) A separate gate would mean two CI infrastructures to maintain; consolidation is cheaper.</li><li><strong>The \"Layer 1.5: triggering\" naming is deliberate.</strong> Inserting between existing Layer 1 (mechanical) and Layer 2 (functional) signals \"this is between structure and behaviour — it's about *routing*\". The half-step naming makes it clear this is an addition, not a renumbering of existing layers. README.md Part 13.1's validation pyramid diagram absorbs the new layer at the right height.</li><li><strong>TRIGGER_TESTS.md vs trigger_tests.json — why Markdown?</strong> Three reasons. (1) Authors write in Markdown for everything else (SKILL.md, INVARIANTS.md, RUBRIC.md); a JSON fixture would be the only non-Markdown artefact in a skill folder. (2) The <code>## Authoring notes</code> section is prose — JSON would force comments-as-key-values or YAML embedding. (3) The fixture is human-read more often than machine-read; readability matters. The parser strips Markdown lightly (just <code>## Section</code> headers + <code>- </code> bullets), so the format is trivially machine-parseable too.</li><li><strong>Why fix-attribution per phrase in <code>## Authoring notes</code>?</strong> Trust calibration. A failing CI gate on a phrase sourced from \"real OBS log week 2026-W18\" is high-signal — real users typed this; if it's failing, the description widened past the safe envelope. A failing gate on a phrase sourced from \"author intuition\" is lower-signal — the author may have been wrong about how users phrase requests. The source-attribution lets the fine-tune cycle prioritise.</li><li><strong>Cross-task coupling with TASK-SKILL-111</strong>: TASK-SKILL-111 enforces description format (≥2 trigger phrases in frontmatter); TASK-SKILL-112 enforces classifier behaviour (those phrases actually route). Either can ship first. Once both are live, the contract is: description carries triggers, fixture asserts triggers route. If only 111 ships, classifier regressions still slip through. If only 112 ships, the description can ship with no triggers and the fixture still passes (because the body's <code>## When to invoke</code> triggers the supervisor). Together they close the loop.</li></ul>\n<hr>\n<p><em>End of TASK-SKILL-112.</em></p>";
