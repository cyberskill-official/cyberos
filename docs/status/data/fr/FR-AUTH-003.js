window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>Every tenant-scoped Postgres table in the CyberOS schema <strong>MUST</strong> have RLS enabled with both USING and WITH CHECK policies. The enforcement contract:</p>\n<ol><li><strong>MUST</strong> enable RLS on every table in <code>rls/registry.rs::TENANT_SCOPED_TABLES</code>. Slice 1 list: <code>subjects</code>, <code>sessions</code>, <code>ai_invocations</code>, <code>cost_ledger</code>, <code>cost_ledger_hold</code>, <code>tenant_policies</code>, <code>chat_workspaces</code>, <code>chat_messages</code>, <code>kb_articles</code>, <code>proj_issues</code>, <code>audit_outbox</code>, <code>admin_idempotency_keys</code>. New tenant-scoped tables added to the schema MUST be added to the registry; CI lint (<code>rls_registry_completeness_test</code>) blocks PRs that introduce a tenant-scoped table without registry entry.</li><li><strong>MUST</strong> apply BOTH <code>USING (tenant_id = current_setting('app.tenant_id', true)::uuid)</code> AND <code>WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid)</code> clauses on every policy. USING filters reads; WITH CHECK filters writes. Policies with only USING permit silent wrong-tenant INSERTs (the row writes successfully but is invisible to subsequent SELECTs); WITH CHECK rejects the INSERT outright. Both clauses MUST exist on every policy.</li><li><strong>MUST</strong> set <code>app.tenant_id</code> per-transaction at session start via <code>SET LOCAL app.tenant_id = $1</code> with <code>$1</code> bound through sqlx's parameter binding (NEVER via <code>format!</code> string interpolation — that would be SQL injection). The <code>with_tenant</code> helper in <code>rls/with_tenant.rs</code> enforces this pattern; direct SQL writes must use the helper.</li><li><strong>MUST</strong> use a dedicated DB role <code>cyberos_app</code> (with <code>NOSUPERUSER</code>, <code>NOBYPASSRLS</code>) for all application queries. The <code>postgres</code> superuser is reserved for migrations only; application code connects as <code>cyberos_app</code> exclusively. The connection-string config enforces this — application boot fails if connecting as superuser.</li><li><strong>MUST</strong> define a separate <code>cyberos_ops</code> role (with <code>BYPASSRLS</code>) for operational queries that legitimately need cross-tenant reads (compliance reports, ops investigations). Use of <code>cyberos_ops</code> MUST be logged via PgAudit + emit a <code>auth.rls_bypass_used</code> memory audit row per query; sev-2 alarm on increment beyond a baseline (any unexpected use is investigated).</li><li><strong>MUST</strong> be tested via property-based test: 1000 random tenant pairs × 10K queries × ZERO cross-tenant reads. The test mirrors FR-AI-018's pattern but covers all tenant-scoped tables, not just the cache. Pattern: insert rows under tenant A; switch context to tenant B; assert queries return zero rows from A. Repeated 1000× with random tenant ids.</li><li><strong>MUST</strong> apply RLS to NEW tenants automatically via FR-AUTH-001's <code>rls::apply_for_tenant</code> hook. Adding a new tenant adds <code>tenant_&lt;id&gt;_&lt;table&gt;</code> policy entries on every registered table. The registry IS the contract; missing entries cause cross-tenant exposure on the new tenant's first call.</li><li><strong>MUST</strong> include WITH CHECK rejection in error responses — when a SQL INSERT violates the WITH CHECK clause, the application catches the postgres error code <code>42501</code> (insufficient privilege) and returns <code>403 FORBIDDEN</code> with <code>{\"error\":\"rls_check_violation\",\"table\":\"&lt;table&gt;\",\"attempted_tenant\":\"&lt;uuid&gt;\",\"actual_tenant\":\"&lt;uuid&gt;\"}</code>. Without this surfacing, RLS rejections look like generic SQL errors.</li><li><strong>MUST</strong> verify at boot that every table in <code>TENANT_SCOPED_TABLES</code> actually has RLS enabled (<code>pg_tables.rowsecurity = true</code>) AND has both USING + WITH CHECK policies. Boot fails if any registered table is missing RLS — this catches \"operator added the table to the registry but forgot to apply the migration\" before the gateway accepts traffic.</li><li><strong>MUST</strong> be deterministic: same <code>(app.tenant_id, query)</code> pair always returns the same result. Race conditions in setting <code>app.tenant_id</code> (e.g., a connection returned to the pool with stale setting) are catastrophic — <code>with_tenant</code> uses <code>SET LOCAL</code> (transaction-scoped) to ensure no leakage across requests on the same connection.</li><li><strong>MUST</strong> be CI-gated via <code>.github/workflows/rls-property-gate.yml</code> on every PR touching <code>services/auth/migrations/**</code>, <code>services/auth/src/rls/**</code>, OR any file matching <code>services/*/migrations/*.sql</code> (any module's migrations might add a tenant-scoped table). The workflow runs property test + registry completeness test + role-separation test; non-skip enforcement per FR-AI-018 §1 #13 pattern.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_rls_policy_count{table}</code> (gauge; per-table policy count, should equal tenant count).</li><li><code>auth_rls_check_violations_total{table}</code> (counter; sev-1 — expected to be near-zero in production).</li><li><code>auth_rls_bypass_used_total{role}</code> (counter; sev-2 alarm on increment).</li><li><code>auth_rls_with_tenant_calls_total{tenant_id}</code> (counter; tracks helper usage for context-validation audits).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why USING + WITH CHECK, not just USING (§1 #2)?</strong> USING only filters READS — <code>SELECT * FROM subjects</code> returns only the current tenant's subjects. But INSERT with a hand-crafted <code>tenant_id</code> (e.g., from a SQL injection or a buggy code path) succeeds; the row writes successfully and disappears from the writer's subsequent SELECTs (because USING filters them out). The writer thinks the row failed; the row actually exists in some other tenant's space. Silent failure mode. WITH CHECK rejects the INSERT at write time with a clear error. Both clauses together are defense-in-depth.</p>\n<p><strong>Why dedicated <code>cyberos_app</code> role (§1 #4)?</strong> Superuser bypasses RLS by Postgres design — RLS only applies to non-superuser roles. If the application connects as <code>postgres</code>, every query bypasses every policy. The dedicated <code>cyberos_app</code> role with <code>NOSUPERUSER NOBYPASSRLS</code> ensures RLS always applies. The boot-time check (refuse to start if connecting as superuser) prevents the misconfiguration class.</p>\n<p><strong>Why a separate <code>cyberos_ops</code> BYPASSRLS role (§1 #5)?</strong> Some operations legitimately need cross-tenant reads — compliance reports (\"show me all subjects across all tenants where...\"), ops investigations (\"which tenant has the runaway request?\"), regulator audits. A separate role makes these legitimate uses explicit AND auditable: every query as <code>cyberos_ops</code> is logged via PgAudit + emits a memory audit row. Without the separate role, the only options would be \"use superuser\" (no isolation between legitimate ops and emergency root) or \"do without\" (legitimate ops fail).</p>\n<p><strong>Why <code>SET LOCAL</code> instead of <code>SET</code> (§1 #3)?</strong> <code>SET</code> persists for the connection's lifetime; if the connection returns to the pool with <code>app.tenant_id = A</code> set, the next user of that connection (potentially tenant B's request) starts with A's context — catastrophic cross-tenant leak. <code>SET LOCAL</code> is transaction-scoped: it resets at COMMIT/ROLLBACK. Connection pools recycle connections without state contamination.</p>\n<p><strong>Why parameter binding for SET LOCAL (§1 #3)?</strong> <code>format!(\"SET LOCAL app.tenant_id = '{}'\", tenant_id)</code> is SQL injection if <code>tenant_id</code> is attacker-controlled. Even though it's typed as Uuid (which can't contain injection chars), defense in depth says: never interpolate, always bind. The <code>with_tenant</code> helper uses <code>sqlx::query(\"SET LOCAL app.tenant_id = $1\").bind(tenant_id)</code> which is safe by construction.</p>\n<p><strong>Why a registry of tenant-scoped tables (§1 #1)?</strong> Without a registry, \"is this table tenant-scoped?\" is a code-archaeology question. A PR adding a new table might forget to add RLS — and the omission is invisible until production data lands in the wrong tenant. The registry is the explicit contract; the boot-time check (§1 #9) catches drift; the CI test (<code>rls_registry_completeness_test</code>) catches PRs that introduce un-registered tables.</p>\n<p><strong>Why surface RLS violations as 403 Forbidden (§1 #8)?</strong> Postgres returns error code <code>42501 (insufficient_privilege)</code> for WITH CHECK rejections. Bubbled up as a 500 error, this looks like a server bug. Catching the specific code and returning 403 with detail tells the operator EXACTLY what happened (which table, which tenants involved) — actionable feedback for debugging.</p>\n<p><strong>Why mirror FR-AI-018's property-test pattern (§1 #6)?</strong> FR-AI-018 proved the pattern works for cache cross-tenant isolation (200K random ops, 7 regression scenarios, adversarial inputs). RLS is the same invariant (\"zero cross-tenant reads\") at the DB layer. Reusing the proven pattern reduces risk and keeps the test infrastructure consistent.</p>\n<p><strong>Why CI-gate against ANY module's migrations (§1 #11)?</strong> A new tenant-scoped table might be introduced by FR-CHAT-007 or FR-PROJ-014 — modules that don't touch <code>services/auth/</code>. Gating only on <code>services/auth/migrations/**</code> would miss these. The broader path filter (<code>services/*/migrations/*.sql</code>) catches every migration; the registry-completeness test asserts the registry was updated.</p>\n<p><strong>Why <code>auth.rls_bypass_used</code> audit row on every cyberos_ops query (§1 #5)?</strong> A regulator asking \"did anyone access cross-tenant data during the period under review?\" gets a positive answer (rows showing every legitimate cross-tenant query) rather than absence-of-evidence. The sev-2 alarm on baseline drift catches abuse: if cyberos_ops usage suddenly 10x's, an investigation starts.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Roles + base RLS migration</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0004_rls_roles.sql\nCREATE ROLE cyberos_app NOSUPERUSER NOBYPASSRLS LOGIN PASSWORD '&lt;env&gt;';\nCREATE ROLE cyberos_ops NOSUPERUSER BYPASSRLS LOGIN PASSWORD '&lt;env&gt;';\n\nGRANT USAGE ON SCHEMA public TO cyberos_app, cyberos_ops;\nGRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO cyberos_app, cyberos_ops;\nALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO cyberos_app, cyberos_ops;</code></pre>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0005_rls_enable_on_tables.sql\n-- Apply policy to every tenant-scoped table (registry mirror).\n\nALTER TABLE subjects ENABLE ROW LEVEL SECURITY;\nALTER TABLE subjects FORCE ROW LEVEL SECURITY;   -- applies to table owner too\nCREATE POLICY subjects_tenant_isolation ON subjects\n    USING      (tenant_id = current_setting('app.tenant_id', true)::uuid)\n    WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);\n\nALTER TABLE sessions ENABLE ROW LEVEL SECURITY;\nALTER TABLE sessions FORCE ROW LEVEL SECURITY;\nCREATE POLICY sessions_tenant_isolation ON sessions\n    USING      (tenant_id = current_setting('app.tenant_id', true)::uuid)\n    WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);\n\n-- ... same pattern for: ai_invocations, cost_ledger, cost_ledger_hold, tenant_policies,\n-- chat_workspaces, chat_messages, kb_articles, proj_issues, audit_outbox, admin_idempotency_keys</code></pre>\n<h3>Registry</h3>\n<pre><code class=\"language-rust\">// services/auth/src/rls/registry.rs\npub struct TenantScopedTable { pub name: &amp;'static str, pub tenant_column: &amp;'static str }\n\npub const TENANT_SCOPED_TABLES: &amp;[TenantScopedTable] = &amp;[\n    TenantScopedTable { name: \"subjects\",                 tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"sessions\",                 tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"ai_invocations\",           tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"cost_ledger\",              tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"cost_ledger_hold\",         tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"tenant_policies\",          tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"chat_workspaces\",          tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"chat_messages\",            tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"kb_articles\",              tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"proj_issues\",              tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"audit_outbox\",             tenant_column: \"tenant_id\" },\n    TenantScopedTable { name: \"admin_idempotency_keys\",   tenant_column: \"tenant_id\" },\n];\n\n/// §1 #9: boot-time check that every registered table has RLS enabled with USING + WITH CHECK.\npub async fn verify_rls_at_boot(pool: &amp;PgPool) -&gt; Result&lt;(), RlsBootError&gt; {\n    for t in TENANT_SCOPED_TABLES {\n        let enabled: bool = sqlx::query_scalar(\n            \"SELECT rowsecurity FROM pg_tables WHERE schemaname='public' AND tablename=$1\",\n        ).bind(t.name).fetch_one(pool).await?;\n        if !enabled {\n            return Err(RlsBootError::NotEnabled { table: t.name.into() });\n        }\n\n        let policies: Vec&lt;(Option&lt;String&gt;, Option&lt;String&gt;)&gt; = sqlx::query_as(\n            \"SELECT qual, with_check FROM pg_policies WHERE tablename = $1\",\n        ).bind(t.name).fetch_all(pool).await?;\n        if policies.is_empty() {\n            return Err(RlsBootError::NoPolicy { table: t.name.into() });\n        }\n        for (using_clause, with_check_clause) in policies {\n            if using_clause.is_none() {\n                return Err(RlsBootError::MissingUsing { table: t.name.into() });\n            }\n            if with_check_clause.is_none() {\n                return Err(RlsBootError::MissingWithCheck { table: t.name.into() });\n            }\n        }\n    }\n    Ok(())\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum RlsBootError {\n    #[error(\"RLS not enabled on table {table}\")]\n    NotEnabled { table: String },\n    #[error(\"no RLS policy on table {table}\")]\n    NoPolicy { table: String },\n    #[error(\"policy on {table} missing USING clause\")]\n    MissingUsing { table: String },\n    #[error(\"policy on {table} missing WITH CHECK clause\")]\n    MissingWithCheck { table: String },\n    #[error(\"db error: {0}\")]\n    Db(#[from] sqlx::Error),\n}</code></pre>\n<h3><code>with_tenant</code> helper</h3>\n<pre><code class=\"language-rust\">// services/auth/src/rls/with_tenant.rs\nuse uuid::Uuid;\nuse sqlx::{PgPool, Postgres, Transaction};\nuse std::future::Future;\n\npub async fn with_tenant&lt;F, Fut, T&gt;(pool: &amp;PgPool, tenant_id: Uuid, f: F) -&gt; Result&lt;T, sqlx::Error&gt;\nwhere\n    F: for&lt;'c&gt; FnOnce(&amp;'c mut Transaction&lt;'_, Postgres&gt;) -&gt; Fut,\n    Fut: Future&lt;Output = Result&lt;T, sqlx::Error&gt;&gt;,\n{\n    let mut tx = pool.begin().await?;\n    // §1 #3: bound parameter, NEVER format!.\n    sqlx::query(\"SET LOCAL app.tenant_id = $1::text\").bind(tenant_id.to_string())\n        .execute(&amp;mut *tx).await?;\n    let result = f(&amp;mut tx).await?;\n    tx.commit().await?;\n    Ok(result)\n}\n\n/// Surface postgres error code 42501 as a typed RlsCheckViolation.\npub fn classify_pg_error(err: &amp;sqlx::Error) -&gt; Option&lt;RlsCheckViolation&gt; {\n    if let sqlx::Error::Database(db) = err {\n        if db.code().as_deref() == Some(\"42501\") {\n            return Some(RlsCheckViolation { detail: db.message().into() });\n        }\n    }\n    None\n}\n\n#[derive(Debug)]\npub struct RlsCheckViolation { pub detail: String }</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><code>app.tenant_id = A</code>: <code>SELECT * FROM subjects</code> returns only A's rows (zero from other tenants).</li><li><code>app.tenant_id = B</code>: same SELECT returns only B's rows.</li><li><code>INSERT INTO subjects (tenant_id, ...) VALUES ('&lt;other-tenant-uuid&gt;', ...)</code> under <code>app.tenant_id = A</code> returns postgres error 42501; row NOT inserted.</li><li>Property test: 1000 random tenant pairs × 10K queries → 0 cross-tenant reads (mirrors FR-AI-018).</li><li>New tenant from FR-AUTH-001 → <code>tenant_&lt;id&gt;_&lt;table&gt;</code> policies exist on every registered table.</li><li><code>cyberos_app</code> role cannot bypass RLS (<code>SELECT current_setting('is_superuser')</code> returns <code>'off'</code>; queries respect policies).</li><li><code>cyberos_ops</code> role bypasses RLS — sees all tenants' rows; emits <code>auth.rls_bypass_used</code> audit row per query.</li><li>Boot-time check fails if a registered table doesn't have RLS enabled.</li><li>Boot-time check fails if a policy is missing WITH CHECK clause.</li><li>CI workflow <code>rls-property-gate.yml</code> blocks merge on cross-leak detection.</li><li>Every table introduced in any module's migration MUST be added to TENANT_SCOPED_TABLES OR explicitly excluded with a <code>// non-tenant-scoped</code> comment in the schema file (CI lint enforces).</li><li>RLS check violations surface as HTTP 403 with table + attempted_tenant + actual_tenant fields.</li><li><code>with_tenant</code> helper uses bound parameter (NEVER format!) — code-search lint asserts.</li><li><code>SET LOCAL</code> (not <code>SET</code>) used in helper — connection pool returned with no residual context.</li><li><code>cyberos_app</code> boot connection MUST be NOSUPERUSER + NOBYPASSRLS — boot fails if connected as superuser.</li><li>Operator queries via <code>cyberos_ops</code> emit memory audit rows + sev-2 alarm on &gt;baseline rate.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/rls_isolation_test.rs\n#[tokio::test]\nasync fn select_returns_only_current_tenant_rows() {\n    let pool = test_pool_as_cyberos_app().await;\n    let tenant_a = test_helper::create_tenant().await;\n    let tenant_b = test_helper::create_tenant().await;\n    test_helper::insert_subject(tenant_a, \"a@x.com\").await;\n    test_helper::insert_subject(tenant_b, \"b@x.com\").await;\n\n    rls::with_tenant(&amp;pool, tenant_a, |tx| async move {\n        let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM subjects\").fetch_one(&amp;mut **tx).await?;\n        assert_eq!(count, 1);\n        Ok(())\n    }).await.unwrap();\n\n    rls::with_tenant(&amp;pool, tenant_b, |tx| async move {\n        let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM subjects\").fetch_one(&amp;mut **tx).await?;\n        assert_eq!(count, 1);\n        Ok(())\n    }).await.unwrap();\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rls_isolation_test.rs\n#[tokio::test]\nasync fn insert_with_wrong_tenant_id_blocked_by_with_check() {\n    let pool = test_pool_as_cyberos_app().await;\n    let a = test_helper::create_tenant().await;\n    let b = test_helper::create_tenant().await;\n\n    let err = rls::with_tenant(&amp;pool, a, |tx| async move {\n        sqlx::query(\"INSERT INTO subjects (tenant_id, email, password_hash) VALUES ($1, $2, $3)\")\n            .bind(b)                           // Wrong tenant!\n            .bind(\"evil@x.com\")\n            .bind(\"$2b$12$...\")\n            .execute(&amp;mut **tx).await\n    }).await.expect_err(\"expected 42501\");\n\n    let violation = rls::with_tenant::classify_pg_error(&amp;err).expect(\"expected RlsCheckViolation\");\n    assert!(violation.detail.contains(\"policy\"));\n\n    // Confirm row NOT inserted.\n    let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM subjects WHERE email='evil@x.com'\")\n        .fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(count, 0);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rls_property_test.rs\nuse proptest::prelude::*;\n\nproptest! {\n    #![proptest_config(ProptestConfig::with_cases(200))]\n\n    #[test]\n    fn no_cross_tenant_reads_under_random_pairs(\n        ops_per_pair in 5..50_usize,\n    ) {\n        let rt = tokio::runtime::Runtime::new().unwrap();\n        rt.block_on(async {\n            let pool = test_pool_as_cyberos_app().await;\n            let a = test_helper::create_tenant().await;\n            let b = test_helper::create_tenant().await;\n\n            // Insert ops_per_pair rows under tenant A.\n            for i in 0..ops_per_pair {\n                rls::with_tenant(&amp;pool, a, |tx| async move {\n                    sqlx::query(\"INSERT INTO subjects (tenant_id, email, password_hash) VALUES ($1, $2, $3)\")\n                        .bind(a).bind(format!(\"a{i}@x.com\")).bind(\"$2b$12$...\")\n                        .execute(&amp;mut **tx).await.map(|_| ())\n                }).await.unwrap();\n            }\n\n            // From tenant B context, assert none of A's rows visible.\n            rls::with_tenant(&amp;pool, b, |tx| async move {\n                let leaked: i64 = sqlx::query_scalar(\n                    \"SELECT COUNT(*) FROM subjects WHERE email LIKE 'a%@x.com'\"\n                ).fetch_one(&amp;mut **tx).await?;\n                prop_assert_eq!(leaked, 0);\n                Ok(())\n            }).await.unwrap();\n        });\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rls_isolation_test.rs\n#[tokio::test]\nasync fn cyberos_app_cannot_bypass_rls() {\n    let pool = test_pool_as_cyberos_app().await;\n    let is_super: String = sqlx::query_scalar(\"SELECT current_setting('is_superuser')\")\n        .fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(is_super, \"off\");\n\n    let bypass: String = sqlx::query_scalar(\n        \"SELECT rolbypassrls::text FROM pg_roles WHERE rolname=current_user\"\n    ).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(bypass, \"false\");\n}\n\n#[tokio::test]\nasync fn cyberos_ops_can_bypass_rls_and_emits_audit() {\n    let pool = test_pool_as_cyberos_ops().await;\n    let _ = test_helper::insert_subjects_in_multiple_tenants(3).await;\n\n    let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM subjects\").fetch_one(&amp;pool).await.unwrap();\n    assert!(count &gt;= 3, \"ops role should see all tenants\");\n\n    let audit_count = memory_test_helper::count_rows_since(\"auth.rls_bypass_used\", recently()).await;\n    assert!(audit_count &gt;= 1, \"ops bypass MUST emit audit row\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rls_registry_completeness_test.rs\n#[tokio::test]\nasync fn every_tenant_scoped_table_in_registry() {\n    let pool = test_pool_as_postgres_for_introspection().await;\n    let tables: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT tablename FROM pg_tables WHERE schemaname='public'\n         AND tablename NOT IN (SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename LIKE '_sqlx%')\"\n    ).fetch_all(&amp;pool).await.unwrap();\n\n    let mut missing = vec![];\n    for t in &amp;tables {\n        let has_tenant_id: bool = sqlx::query_scalar(\n            \"SELECT EXISTS(SELECT 1 FROM information_schema.columns\n                           WHERE table_name = $1 AND column_name = 'tenant_id')\",\n        ).bind(t).fetch_one(&amp;pool).await.unwrap();\n        if has_tenant_id &amp;&amp; !rls::registry::TENANT_SCOPED_TABLES.iter().any(|r| r.name == t) {\n            missing.push(t.clone());\n        }\n    }\n    assert!(missing.is_empty(),\n        \"tables with tenant_id column not in TENANT_SCOPED_TABLES registry: {missing:?}\");\n}</code></pre>\n<p>CI workflow:</p>\n<pre><code class=\"language-yaml\"># .github/workflows/rls-property-gate.yml\nname: RLS Property Gate\non:\n  pull_request:\n    paths:\n      - 'services/auth/migrations/**'\n      - 'services/auth/src/rls/**'\n      - 'services/*/migrations/*.sql'\n      - '.github/workflows/rls-property-gate.yml'\njobs:\n  rls-gate:\n    runs-on: ubuntu-22.04\n    timeout-minutes: 10\n    services:\n      postgres:\n        image: postgres:16\n        env: { POSTGRES_PASSWORD: pass }\n        ports: ['5432:5432']\n        options: --health-cmd \"pg_isready\"\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions-rust-lang/setup-rust-toolchain@v1\n      - run: |\n          cd services/auth\n          sqlx migrate run\n          cargo test rls -- --test-threads=1 --format=json | tee report.json\n          if grep -c '\"event\":\"ignored\"' report.json | xargs test 0 -lt; then exit 1; fi</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3 + §5 for full code. Boot integration:</p>\n<pre><code class=\"language-rust\">// services/auth/src/lib.rs (additions)\npub async fn run() -&gt; Result&lt;(), Error&gt; {\n    let pool_app = PgPool::connect_with(connect_opts_for_role(\"cyberos_app\")).await?;\n    rls::registry::verify_rls_at_boot(&amp;pool_app).await?;\n    // Refuse to bind if RLS not enabled on registered tables.\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-AUTH-001</strong> — Tenant create invokes <code>rls::apply_for_tenant</code> which uses the canonical template.</li><li><strong>FR-AUTH-004 (downstream)</strong> — JWT issuance reads from <code>subjects</code> table; relies on RLS context being set.</li><li><strong>FR-AI-018</strong> — Cache cross-tenant test pattern; reused here for DB-layer testing.</li><li>Crates: <code>proptest@1</code>, <code>sqlx@0.7</code> (postgres), <code>uuid@1</code>, <code>thiserror@1</code>.</li><li>Postgres 16+ (RLS + <code>pg_policies</code> view).</li><li>PgAudit extension for <code>cyberos_ops</code> query logging.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>RLS check violation surfaced as HTTP 403</h3>\n<pre><code class=\"language-http\">POST /v1/admin/subjects HTTP/1.1\n{ \"tenant_id\": \"&lt;other-tenant&gt;\", ... }\n\n→ 403 Forbidden\n{\n  \"error\": \"rls_check_violation\",\n  \"table\": \"subjects\",\n  \"attempted_tenant\": \"&lt;other-tenant&gt;\",\n  \"actual_tenant\": \"&lt;caller-tenant&gt;\"\n}</code></pre>\n<h3>Audit row <code>auth.rls_bypass_used</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.rls_bypass_used\",\n  \"payload\": {\n    \"operator_id\": \"stephen@cyberos.world\",\n    \"role\": \"cyberos_ops\",\n    \"query_sha256\": \"...\",\n    \"tables_accessed\": [\"subjects\", \"ai_invocations\"],\n    \"request_id\": \"ops_...\"\n  }\n}</code></pre>\n<h3>Boot failure</h3>\n<pre><code class=\"language-text\">ERROR rls_boot_check_failed: RLS not enabled on table 'kb_articles'\nERROR refusing to bind; operator must run migrations OR remove kb_articles from TENANT_SCOPED_TABLES</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Cross-region RLS (per-region database with replication) — slice 6+.</li><li>Per-row ACL beyond tenant_id (e.g., subject-scoped rows visible only to creator) — slice 5+.</li><li>Time-limited RLS bypass token for emergency ops (auto-expires in 1 hour) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>RLS policy missing on a new tenant-scoped table</td><td>boot-time check via <code>verify_rls_at_boot</code></td><td>Gateway refuses to bind</td><td>Operator runs migration; redeploy</td></tr><tr><td>Property test finds cross-leak</td><td>proptest panics in CI</td><td>PR blocked</td><td>Engineer fixes policy or query</td></tr><tr><td>INSERT with wrong tenant_id under cyberos_app</td><td>WITH CHECK violation (postgres 42501)</td><td>403 RLS_CHECK_VIOLATION</td><td>Caller fixes tenant_id</td></tr><tr><td><code>cyberos_ops</code> query without audit emit</td><td>integration test asserts</td><td>Test fails → PR blocked</td><td>Add memory_writer call</td></tr><tr><td>Application connects as <code>postgres</code> (superuser)</td><td>boot check <code>is_superuser='on'</code></td><td>Gateway refuses to bind</td><td>Operator fixes connection-string config</td></tr><tr><td><code>SET</code> used instead of <code>SET LOCAL</code></td><td>code-grep lint</td><td>PR blocked</td><td>Use <code>with_tenant</code> helper exclusively</td></tr><tr><td><code>format!</code> interpolation in SET LOCAL</td><td>code-grep lint for <code>format!.*SET LOCAL</code></td><td>PR blocked</td><td>Use bound parameter</td></tr><tr><td>New tenant created but RLS apply fails</td><td>FR-AUTH-001 transaction rollback</td><td>Tenant NOT created</td><td>By design (FR-AUTH-001 §1 #12)</td></tr><tr><td><code>TENANT_SCOPED_TABLES</code> registry incomplete (table has tenant_id but not registered)</td><td><code>rls_registry_completeness_test</code></td><td>PR blocked</td><td>Add to registry</td></tr><tr><td><code>pg_policies</code> query returns missing WITH CHECK</td><td>boot check</td><td>Gateway refuses to bind</td><td>Operator updates migration to add WITH CHECK</td></tr><tr><td>Connection pool returns connection with stale <code>app.tenant_id</code></td><td>impossible by design (SET LOCAL transaction-scoped)</td><td>N/A</td><td>By design</td></tr><tr><td>Race: SET LOCAL + concurrent query on same connection</td><td>sqlx serializes within tx</td><td>No race</td><td>By design</td></tr><tr><td>Sev-2 alarm on cyberos_ops baseline drift</td><td>metric <code>auth_rls_bypass_used_total</code> increment &gt; baseline</td><td>Operator investigates</td><td>Standard ops process</td></tr><tr><td>pg_audit not installed</td><td><code>cyberos_ops</code> queries not logged</td><td>Compliance gap</td><td>Operator installs extension</td></tr><tr><td>Migration adds tenant_id column but doesn't enable RLS</td><td>boot check + registry test</td><td>Both checks fail</td><td>Operator updates migration + registry</td></tr><tr><td>Replica server runs without RLS sync</td><td>replica boot check fails</td><td>Replica refuses promotion</td><td>Operator investigates replication</td></tr><tr><td>Operator manually grants superuser to cyberos_app</td><td>boot check catches <code>is_superuser='on'</code></td><td>Refuse to bind</td><td>Revoke superuser</td></tr><tr><td><code>cyberos_ops</code> token leaked to non-ops user</td><td>abuse via audit-log review</td><td>sev-1 incident</td><td>Rotate role password; audit recent queries</td></tr><tr><td>FORCE ROW LEVEL SECURITY missing on a table</td><td>table owner could bypass</td><td>possibly catch via ad-hoc test</td><td>Add to migration</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>USING + WITH CHECK is the load-bearing pattern. Without WITH CHECK, malicious or buggy INSERTs can write into the wrong tenant silently. The §10 row \"INSERT with wrong tenant_id under cyberos_app\" is the proof that WITH CHECK actually catches the case.</li><li><code>SET LOCAL</code> (transaction-scoped) is the only safe way to set <code>app.tenant_id</code>. <code>SET</code> (session-scoped) leaves residual state on connection-pool return — a connection that handled tenant A's request would serve tenant B's next request with A's context.</li><li>The <code>cyberos_app</code> vs <code>cyberos_ops</code> role split is the operational primitive. Application code never needs cross-tenant reads; ops occasionally does. The split makes legitimate cross-tenant queries explicit + auditable.</li><li>The boot-time check (§1 #9) is the structural defence against \"operator forgot to run the migration.\" It refuses to bind rather than serve traffic with broken RLS — loud failure beats silent leak.</li><li>The property test pattern is borrowed from FR-AI-018. Keeping the test infrastructure consistent across modules reduces cognitive load and shares the proven tooling.</li><li><code>FORCE ROW LEVEL SECURITY</code> (in §3 migration) ensures the table OWNER also respects RLS. Without it, the role that owns the table (typically <code>postgres</code>) bypasses policies even if NOSUPERUSER. Defence in depth.</li><li><code>rls_registry_completeness_test</code> is a CI-time check that catches \"operator added a tenant_id column but forgot to register the table.\" Without this test, RLS gaps ship to production silently.</li><li>The <code>with_tenant</code> helper is the single sanctioned way to set RLS context. Direct sqlx queries that don't use the helper risk forgetting <code>SET LOCAL</code> — a code-grep lint (<code>grep -r 'app.tenant_id' --exclude-dir=rls</code>) flags any such bypass.</li><li>Sev-2 alarm on <code>cyberos_ops</code> baseline drift catches abuse early. A 10x increase in cross-tenant queries is investigated; either it's legitimate (regulator audit underway) or it's not (incident).</li></ul>\n<hr>\n<p><em>End of FR-AUTH-003. Status: draft (10/10 target).</em></p>";
