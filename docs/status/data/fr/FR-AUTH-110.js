window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-110"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> ship a first-party OIDC provider (authorization server) so that CyberOS first-party apps - CHAT (Mattermost), PORTAL, and future ones - authenticate users against one CyberOS identity via OIDC Authorization Code + PKCE. This is the inverse of FR-AUTH-104: there AUTH is the OIDC client of Google; here AUTH is the OIDC provider that downstream apps consume. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>auth_oidc_rp_clients</code>: <code>(id UUID PRIMARY KEY, tenant_id UUID NOT NULL, name TEXT NOT NULL, client_id TEXT NOT NULL UNIQUE, client_secret_kms_blob BYTEA NOT NULL, kms_key_id TEXT NOT NULL, redirect_uris TEXT[] NOT NULL, post_logout_redirect_uris TEXT[] NOT NULL DEFAULT '{}', allow_refresh BOOLEAN NOT NULL DEFAULT false, is_active BOOLEAN NOT NULL DEFAULT true, created_at TIMESTAMPTZ, created_by_subject_id UUID NOT NULL)</code>. First-party relying parties are admin-registered confidential clients (DEC-2483). UNIQUE <code>(tenant_id, name)</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_oidc_auth_codes</code>: <code>(code_hash TEXT PRIMARY KEY, tenant_id UUID, rp_client_id TEXT NOT NULL, subject_id UUID NOT NULL, redirect_uri TEXT NOT NULL, code_challenge TEXT NOT NULL, nonce TEXT, scope TEXT NOT NULL, sso_session_id UUID NOT NULL, issued_at TIMESTAMPTZ, expires_at TIMESTAMPTZ NOT NULL, consumed_at TIMESTAMPTZ)</code>. The stored value is a hash of the code, not the code (DB-dump safety). Single-use, 60-second TTL (DEC-2490).</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_sso_sessions</code>: <code>(id UUID PRIMARY KEY, tenant_id UUID NOT NULL, subject_id UUID NOT NULL, created_at TIMESTAMPTZ, last_seen_at TIMESTAMPTZ, absolute_expiry TIMESTAMPTZ NOT NULL, revoked_at TIMESTAMPTZ)</code>. The browser holds a <code>__Host-cyberos_sso</code> cookie carrying the session id; the table is the source of truth so the session is revocable (DEC-2489).</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_op_login_history</code>: <code>(id BIGSERIAL, tenant_id UUID, rp_client_id TEXT, subject_id UUID, outcome TEXT NOT NULL CHECK (outcome IN ('authorize_issued','token_issued','denied')), reason TEXT, source_ip_hash16 TEXT, ts TIMESTAMPTZ)</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> (DEC-2495).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> on all four tables: <code>tenant_id = current_setting('app.current_tenant_id')::uuid</code>. (Note: AUTH sets the GUC via <code>SELECT set_config('app.current_tenant_id', $1, true)</code> inside a transaction - the bind form <code>SET LOCAL ... = $1</code> is invalid Postgres and was the class of bug fixed across the SSO path on 2026-06-29.)</li></ol>\n<ol><li><strong>MUST</strong> serve OIDC + RFC 8414 discovery at <code>GET /.well-known/openid-configuration</code> (DEC-2492) advertising: <code>issuer</code> (the configured canonical AUTH URL, DEC-2498), <code>authorization_endpoint</code> (<code>/v1/auth/op/authorize</code>), <code>token_endpoint</code> (<code>/v1/auth/op/token</code>), <code>userinfo_endpoint</code> (<code>/v1/auth/op/userinfo</code>), <code>jwks_uri</code> (the existing <code>/.well-known/jwks.json</code>), <code>end_session_endpoint</code> (slice 2, advertised as present), <code>response_types_supported: [\"code\"]</code>, <code>grant_types_supported: [\"authorization_code\"]</code> (plus <code>refresh_token</code> when any active RP has <code>allow_refresh</code>), <code>code_challenge_methods_supported: [\"S256\"]</code>, <code>scopes_supported: [\"openid\",\"profile\",\"email\",\"offline_access\"]</code>, <code>id_token_signing_alg_values_supported: [\"RS256\"]</code>, <code>subject_types_supported: [\"public\"]</code>.</li></ol>\n<ol><li><strong>MUST</strong> implement <code>GET /v1/auth/op/authorize</code> (DEC-2482, DEC-2484):</li></ol>\n<ul><li>Validate <code>client_id</code> is an active registered RP; unknown → render error, do NOT redirect.</li><li>Validate <code>redirect_uri</code> is an exact match of a registered <code>redirect_uris</code> entry (DEC-2491); mismatch → render error, do NOT redirect (open-redirect defense).</li><li>Require <code>response_type=code</code>, <code>scope</code> containing <code>openid</code>, <code>code_challenge</code> + <code>code_challenge_method=S256</code>, <code>state</code> (echoed), optional <code>nonce</code>. Missing PKCE → redirect back with <code>error=invalid_request</code>.</li><li>Establish the human: if a valid <code>__Host-cyberos_sso</code> cookie maps to a non-revoked <code>auth_sso_sessions</code> row, use its <code>subject_id</code> (silent SSO). Else broker to the tenant's primary upstream IdP via FR-AUTH-104 <code>initiate</code>, preserving the original authorize request; on the FR-AUTH-104 callback, create an SSO session (§1 #9) and resume this authorize.</li><li>Revoke gate (DEC-2488): if the resolved <code>subject_id</code> is revoked per FR-AUTH-005 (deny-list / <code>subjects.status='revoked'</code>), deny with <code>access_denied</code> + emit <code>auth.op_authorize_denied</code>; the person cannot obtain a code.</li><li>Issue a single-use code: 32 random bytes base64url; store <code>code_hash</code>, bind <code>rp_client_id</code>, <code>subject_id</code>, <code>redirect_uri</code>, <code>code_challenge</code>, <code>nonce</code>, <code>scope</code>, <code>sso_session_id</code>, <code>expires_at = now + 60s</code>; redirect to <code>redirect_uri?code=&lt;&gt;&amp;state=&lt;&gt;</code>.</li></ul>\n<ol><li><strong>MUST</strong> implement <code>POST /v1/auth/op/token</code> (DEC-2490, DEC-2486, DEC-2487):</li></ol>\n<ul><li>Authenticate the RP (<code>client_secret_basic</code> or POST body <code>client_id</code>+<code>client_secret</code>); bad secret → 401 <code>invalid_client</code>.</li><li><code>grant_type=authorization_code</code>: look up <code>code_hash</code>; missing/expired/consumed → <code>invalid_grant</code>. On a consumed-code replay, ALSO revoke any token already minted from it (RFC 6749 §4.1.2). Verify <code>redirect_uri</code> equals the stored value and <code>code_verifier</code> satisfies the stored <code>code_challenge</code> (S256). Re-check the revoke gate (DEC-2488).</li><li>Mint: <code>access_token</code> = FR-AUTH-004 RS256 JWT, scope <code>openid</code> + granted, <code>aud</code> includes the RP; <code>id_token</code> = RS256 JWT per §1 #10 with <code>nonce</code> echoed; <code>token_type=Bearer</code>, <code>expires_in</code>. Mark the code consumed.</li><li><code>grant_type=refresh_token</code>: only if the RP has <code>allow_refresh</code> (DEC-2499); rotate per FR-AUTH-004.</li><li>Emit <code>auth.op_token_issued</code>.</li></ul>\n<ol><li><strong>MUST</strong> manage the AUTH SSO browser session (DEC-2489, §1 #3): after any successful human authentication at authorize (silent reuse OR fresh upstream login), ensure an <code>auth_sso_sessions</code> row + set <code>__Host-cyberos_sso</code> (Secure, HttpOnly, SameSite=Lax, Path=/, no Domain - the <code>__Host-</code> prefix forbids Domain and requires Secure+Path=/). Sliding TTL 8h on <code>last_seen_at</code>, absolute 24h. This is what makes the second app a one-click login. Emit <code>auth.op_sso_session_started</code> on creation.</li></ol>\n<ol><li><strong>MUST</strong> build the id_token (DEC-2486): claims <code>iss</code> (= configured issuer, DEC-2498), <code>sub</code> (= CyberOS <code>subject_id</code>), <code>aud</code> (= RP <code>client_id</code>), <code>exp</code> (+1h), <code>iat</code>, <code>auth_time</code>, <code>nonce</code> (echoed if supplied), <code>email</code>, <code>email_verified</code>, <code>name</code>, <code>preferred_username</code>, <code>tenant_id</code>, <code>roles</code> (FR-AUTH-101 array). Signed RS256 against <code>auth_signing_keys</code> with the current <code>kid</code> (DEC-2481). NO second key system.</li></ol>\n<ol><li><strong>MUST</strong> implement <code>GET /v1/auth/op/userinfo</code> (DEC-2487): validate the bearer access_token (RS256 against the same JWKS, <code>aud</code> + <code>exp</code> + revoke gate); return <code>sub</code>, <code>email</code>, <code>email_verified</code>, <code>name</code>, <code>preferred_username</code>, <code>tenant_id</code>, <code>roles</code>. Emit <code>auth.op_userinfo_served</code>. Revoked subject → 401.</li></ol>\n<ol><li><strong>MUST</strong> enforce exact redirect_uri matching (DEC-2491) at BOTH authorize (against the registered set) and token (against the code-bound value). No wildcard, no path-prefix, no query-param laxness. The comparison is byte-exact after WHATWG URL normalization of the registered values at write time.</li></ol>\n<ol><li><strong>MUST</strong> revoke-gate every credential issuance (DEC-2488): authorize, token, and userinfo all re-check that <code>subject_id</code> is not revoked. A revoke (FR-AUTH-005) therefore blocks re-authentication into every first-party app immediately. (Killing already-live downstream sessions in real time is the slice-2 back-channel-logout follow-up, §9.)</li></ol>\n<ol><li><strong>MUST</strong> skip consent for first-party clients (DEC-2485): registered RPs are trusted internal apps; authorize issues the code without a consent prompt. A <code>first_party</code> flag is implicit (all slice-1 RPs are first-party). Third-party consent UI is slice 3.</li></ol>\n<ol><li><strong>MUST</strong> restrict RP registration to tenant-admin (DEC-2497): <code>POST /v1/auth/op/rp-clients</code> (create), <code>PATCH .../{id}</code>, <code>DELETE .../{id}</code> (soft-delete <code>is_active=false</code>) require role <code>tenant-admin</code> per FR-AUTH-101. The plaintext <code>client_secret</code> is returned exactly once at create; never again. Emit <code>auth.op_rp_client_changed</code> (excluding the secret value).</li></ol>\n<ol><li><strong>MUST</strong> store auth codes hashed + single-use + 60s TTL (DEC-2490): the table key is <code>sha256(code)</code>; lookups hash the presented code; first successful exchange sets <code>consumed_at</code>; any later use → <code>invalid_grant</code>.</li></ol>\n<ol><li><strong>MUST</strong> treat <code>state</code> as opaque RP CSRF (DEC-2496): echo it back verbatim on the redirect, never parse or store it beyond the round trip. <code>nonce</code> is bound into the id_token.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub <code>email</code>, <code>subject_id</code>, <code>reason</code> via the memory PII layer before chain commit; audit rows carry hash16 forms (mirrors FR-AUTH-104 §1 #15).</li></ol>\n<ol><li><strong>MUST</strong> complete the token exchange in ≤ 200 ms p95 (no upstream network call on the token path - JWKS + signing keys are local; the upstream-IdP latency lives only on the cold authorize path). <code>op_token_perf_test</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel spans <code>auth.op.{authorize,token,userinfo,rp_client_change,sso_session}</code> with <code>outcome</code> attribute (issued | denied_revoked | unknown_client | redirect_mismatch | invalid_grant | pkce_fail | silent_sso | upstream_broker).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics: <code>auth_op_authorize_total{tenant_id, rp_client_id, outcome}</code>, <code>auth_op_token_total{outcome}</code>, <code>auth_op_active_sso_sessions</code> (gauge), <code>auth_op_authorize_denied_revoked_total</code> (counter - a spike means many revoked users still trying, expected right after a layoff), <code>auth_op_token_latency_ms</code> (histogram, SLO p95 &lt; 200ms).</li></ol>\n<ol><li><strong>MUST</strong> emit memory audit rows for 7 kinds (DEC-2494): <code>auth.op_authorize_issued</code>, <code>auth.op_authorize_denied</code>, <code>auth.op_token_issued</code>, <code>auth.op_userinfo_served</code>, <code>auth.op_rp_client_changed</code>, <code>auth.op_sso_session_started</code>, <code>auth.op_sso_session_revoked</code>.</li></ol>\n<ol><li><strong>MUST</strong> reject implicit + hybrid flow (DEC-2484): authorize hard-requires <code>response_type=code</code>; any other value → <code>unsupported_response_type</code>.</li></ol>\n<ol><li><strong>MUST</strong> pin the issuer (DEC-2498): a single configured canonical AUTH URL is the <code>iss</code> in every id_token AND the <code>issuer</code> in discovery; they MUST be byte-identical so RPs validate consistently. Misconfiguration (mismatch) → startup refuses to boot the provider routes.</li></ol>\n<ol><li><strong>MUST</strong> make refresh tokens opt-in per RP (DEC-2499): <code>allow_refresh=false</code> by default; CHAT/Mattermost does not need it (Mattermost mints its own session post-login). When true, <code>offline_access</code> scope grants a refresh_token rotated per FR-AUTH-004.</li></ol>\n<ol><li><strong>MUST</strong> revoke SSO sessions on subject revoke: when FR-AUTH-005 revokes a subject, all that subject's <code>auth_sso_sessions</code> rows are marked <code>revoked_at</code> (so silent SSO stops too, not only new logins). Emit <code>auth.op_sso_session_revoked</code>. (This is the slice-1 reach of the kick into the broker; the reach into already-issued downstream app sessions is slice 2.)</li></ol>\n<hr>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Why AUTH becomes the provider, not Mattermost trusting Google directly (DEC-2480). Stephen chose the unified path: one CyberOS identity behind every surface. If Mattermost trusted Google directly, chat would have its own identity island - no shared roles, no central revoke, and the AuthBridge plugin we inspected cannot bridge it (a Mattermost plugin cannot replace the login route, and the shipped one is a simulation: in-memory users, a fabricated session string, no Mattermost SDK). Making AUTH the broker means every first-party app federates to the same subject, and a single revoke locks the person out of all of them.</p>\n<p>Why reuse the FR-AUTH-004 keys and JWKS (DEC-2481). The platform already signs RS256 and publishes one JWKS. A second key system for the provider would fragment verification and drift on rotation. One issuer, one key set, one place to rotate.</p>\n<p>Why broker through the existing session or Google, not a new password prompt (DEC-2482). The human already proves who they are to CyberOS via Google (FR-AUTH-104). The provider should not re-ask. If the user has an AUTH SSO session, authorize is silent. If not, it sends them through the same Google flow that already works, then issues the downstream code. Google stays the root authenticator; AUTH is the broker that turns that into a CyberOS-shaped identity every app understands.</p>\n<p>Why first-party clients are admin-registered, not self-registering (DEC-2483). The relying parties are a small, known set we operate (chat, portal). Open Dynamic Client Registration is for ecosystems of third-party apps - it is the MCP gateway's job for agents, not this. Admin registration with locked redirect_uris keeps the trust boundary tight.</p>\n<p>Why PKCE mandatory and implicit forbidden (DEC-2484). Same reasoning as FR-AUTH-104: authorization-code interception is the classic attack, PKCE binds the code to the initiator, and implicit/hybrid leak tokens through the URL. OAuth 2.1 deprecates them. We only ship the safe profile.</p>\n<p>Why skip consent for first-party apps (DEC-2485). A consent screen exists to protect a user from a third-party app over-asking. For our own chat and portal there is nothing to consent to - the apps are us. Forcing a consent click on every internal login is friction with no security gain. Third-party consent comes when we actually have third-party RPs (slice 3).</p>\n<p>Why the id_token carries roles and tenant (DEC-2486). Mattermost and portal need more than \"who\" - they need \"which tenant\" and \"what role\" to place the user correctly. Putting roles in the id_token lets the RP map a CyberOS admin to a Mattermost admin without a second call. userinfo returns the same for RPs that prefer the call (DEC-2487).</p>\n<p>Why the authorize endpoint is revoke-gated (DEC-2488). This is the heart of kick-by-revoke. The moment a subject is revoked, authorize refuses to mint a code, so they cannot get a fresh session in any first-party app. We are honest about the limit: an already-open Mattermost session is not killed by this gate alone, because Mattermost holds its own session after login. Killing live sessions in real time needs back-channel logout, which is the named slice-2 follow-up. Slice 1 guarantees they cannot get back in; slice 2 throws them out instantly.</p>\n<p>Why an AUTH SSO session cookie (DEC-2489). Without it, every app would re-run the full Google round trip - the opposite of unified. The cookie is the \"logged into CyberOS\" fact that all first-party apps borrow. It is a server-side session (the table is truth) so it is revocable, with a sliding 8h / absolute 24h window. The <code>__Host-</code> prefix is the strict cookie form: Secure, Path=/, no Domain - the hardest to fixate or scope-leak.</p>\n<p>Why codes are hashed, single-use, 60 seconds (DEC-2490). A code is a one-time bearer of a login. Storing only its hash means a DB dump does not leak live codes. Single-use plus the RFC 6749 rule (replay of a consumed code revokes its tokens) closes the replay window. 60 seconds is ample for an immediate redirect-and-exchange and short enough to bound theft.</p>\n<p>Why exact redirect_uri matching, reject-without-redirect (DEC-2491). Loose redirect matching is how open-redirect and token-leak attacks happen. Exact match against the registered set, and when it fails we render an error rather than redirecting anywhere - because redirecting to an unverified URI is itself the vulnerability.</p>\n<p>Why pin the issuer (DEC-2498). Downstream validators compare the id_token <code>iss</code> to the discovery <code>issuer</code>. If those differ by even a trailing slash, every RP rejects every token. One configured canonical value, checked at boot, removes a whole class of \"it works on my machine\" failures.</p>\n<p>Why refresh tokens are opt-in per RP (DEC-2499). Mattermost mints its own session after the OIDC handshake, so it never needs our refresh token; handing one out anyway is needless standing credential. Portal or a SPA might want offline_access. Default off, opt-in per client.</p>\n<p>Why reuse the MCP OAuth substrate (DEC-2493). FR-MCP-004 already built and proved authorize, token, PKCE, JWT-minting against <code>auth_signing_keys</code>, the code/token store, and the RFC 8414 discovery shape. The honest move is to lift that substrate (extract a shared crate or re-implement the thin parts in auth) and spend the budget on what is genuinely new here: the OIDC layer (id_token, userinfo, openid-configuration), the human-auth brokering, and the SSO cookie. The audit should decide extract-vs-reimplement.</p>\n<hr>\n<h2>§3 - API contract</h2>\n<h3>3.1 - Migration 0013 - rp_clients</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0013_oidc_rp_clients.sql\nBEGIN;\n\nCREATE TABLE auth_oidc_rp_clients (\n    id                         UUID        PRIMARY KEY,\n    tenant_id                  UUID        NOT NULL,\n    name                       TEXT        NOT NULL CHECK (length(name) BETWEEN 1 AND 100),\n    client_id                  TEXT        NOT NULL UNIQUE,\n    client_secret_kms_blob     BYTEA       NOT NULL,\n    kms_key_id                 TEXT        NOT NULL,\n    redirect_uris              TEXT[]      NOT NULL CHECK (cardinality(redirect_uris) BETWEEN 1 AND 10),\n    post_logout_redirect_uris  TEXT[]      NOT NULL DEFAULT '{}',\n    allow_refresh              BOOLEAN     NOT NULL DEFAULT false,\n    is_active                  BOOLEAN     NOT NULL DEFAULT true,\n    created_at                 TIMESTAMPTZ NOT NULL DEFAULT now(),\n    created_by_subject_id      UUID        NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nCREATE UNIQUE INDEX uniq_rp_client_name ON auth_oidc_rp_clients (tenant_id, name);\n\nALTER TABLE auth_oidc_rp_clients ENABLE ROW LEVEL SECURITY;\nALTER TABLE auth_oidc_rp_clients FORCE ROW LEVEL SECURITY;\nCREATE POLICY rp_clients_tenant_iso ON auth_oidc_rp_clients\n    USING (tenant_id = current_setting('app.current_tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('app.current_tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.2 - Migration 0014 - auth_codes</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0014_oidc_auth_codes.sql\nBEGIN;\n\nCREATE TABLE auth_oidc_auth_codes (\n    code_hash       TEXT         PRIMARY KEY,            -- sha256(code), never the code\n    tenant_id       UUID         NOT NULL,\n    rp_client_id    TEXT         NOT NULL,\n    subject_id      UUID         NOT NULL REFERENCES auth.subjects(id),\n    redirect_uri    TEXT         NOT NULL,\n    code_challenge  TEXT         NOT NULL,               -- S256\n    nonce           TEXT,\n    scope           TEXT         NOT NULL,\n    sso_session_id  UUID         NOT NULL,\n    issued_at       TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    expires_at      TIMESTAMPTZ  NOT NULL,               -- issued_at + 60s\n    consumed_at     TIMESTAMPTZ\n);\n\nCREATE INDEX auth_codes_expiry_idx ON auth_oidc_auth_codes (expires_at);\n\nALTER TABLE auth_oidc_auth_codes ENABLE ROW LEVEL SECURITY;\nALTER TABLE auth_oidc_auth_codes FORCE ROW LEVEL SECURITY;\nCREATE POLICY auth_codes_tenant_iso ON auth_oidc_auth_codes\n    USING (tenant_id = current_setting('app.current_tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('app.current_tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON auth_oidc_auth_codes FROM cyberos_app;  -- consume via consumed_at set in one UPDATE-as-INSERT-guard; see note\n\nCOMMIT;</code></pre>\n<p>Note on consume: because UPDATE is revoked, \"consume\" is a <code>... WHERE consumed_at IS NULL</code> guarded update is not available; instead the consume is modeled as an insert into a sibling <code>auth_oidc_code_consumptions(code_hash PK)</code> - the first insert wins, a second raises a unique violation = replay. The audit should confirm this pattern vs granting a narrow UPDATE.</p>\n<h3>3.3 - Migration 0015 - sso_sessions</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0015_sso_sessions.sql\nBEGIN;\n\nCREATE TABLE auth_sso_sessions (\n    id                UUID         PRIMARY KEY,\n    tenant_id         UUID         NOT NULL,\n    subject_id        UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE CASCADE,\n    created_at        TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    last_seen_at      TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    absolute_expiry   TIMESTAMPTZ  NOT NULL,              -- created_at + 24h\n    revoked_at        TIMESTAMPTZ\n);\n\nCREATE INDEX sso_sessions_subject_idx ON auth_sso_sessions (subject_id) WHERE revoked_at IS NULL;\n\nALTER TABLE auth_sso_sessions ENABLE ROW LEVEL SECURITY;\nALTER TABLE auth_sso_sessions FORCE ROW LEVEL SECURITY;\nCREATE POLICY sso_sessions_tenant_iso ON auth_sso_sessions\n    USING (tenant_id = current_setting('app.current_tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('app.current_tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.4 - Discovery document</h3>\n<pre><code class=\"language-rust\">// services/auth/src/op/discovery.rs\nuse serde_json::{json, Value};\n\n/// OIDC + RFC 8414 provider metadata. `issuer` is the single configured canonical AUTH URL\n/// (DEC-2498); `jwks_uri` is the existing FR-AUTH-004 /.well-known/jwks.json (DEC-2481).\npub fn openid_configuration(issuer: &amp;str, jwks_uri: &amp;str, grant_types: &amp;[&amp;str]) -&gt; Value {\n    let base = issuer.trim_end_matches('/');\n    json!({\n        \"issuer\": base,\n        \"authorization_endpoint\": format!(\"{base}/v1/auth/op/authorize\"),\n        \"token_endpoint\":         format!(\"{base}/v1/auth/op/token\"),\n        \"userinfo_endpoint\":      format!(\"{base}/v1/auth/op/userinfo\"),\n        \"end_session_endpoint\":   format!(\"{base}/v1/auth/op/logout\"),   // slice 2\n        \"jwks_uri\": jwks_uri,\n        \"response_types_supported\": [\"code\"],\n        \"grant_types_supported\": grant_types,                            // [\"authorization_code\"] (+ \"refresh_token\")\n        \"subject_types_supported\": [\"public\"],\n        \"id_token_signing_alg_values_supported\": [\"RS256\"],\n        \"code_challenge_methods_supported\": [\"S256\"],\n        \"scopes_supported\": [\"openid\", \"profile\", \"email\", \"offline_access\"],\n        \"token_endpoint_auth_methods_supported\": [\"client_secret_basic\", \"client_secret_post\"],\n        \"claims_supported\": [\"sub\",\"email\",\"email_verified\",\"name\",\"preferred_username\",\"tenant_id\",\"roles\"],\n    })\n}</code></pre>\n<h3>3.5 - id_token builder</h3>\n<pre><code class=\"language-rust\">// services/auth/src/op/id_token.rs\nuse serde::Serialize;\n\n#[derive(Debug, Serialize)]\npub struct IdTokenClaims {\n    pub iss: String,                 // configured issuer (DEC-2498)\n    pub sub: String,                 // CyberOS subject_id\n    pub aud: String,                 // RP client_id\n    pub exp: i64,\n    pub iat: i64,\n    pub auth_time: i64,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub nonce: Option&lt;String&gt;,\n    pub email: String,\n    pub email_verified: bool,\n    pub name: String,\n    pub preferred_username: String,\n    pub tenant_id: String,\n    pub roles: Vec&lt;String&gt;,\n}\n\n/// Signs with the current FR-AUTH-004 signing key (RS256). The `kid` header MUST match a key\n/// published in /.well-known/jwks.json so the RP can verify. No second key system (DEC-2481).\npub fn sign_id_token(claims: &amp;IdTokenClaims, signer: &amp;cyberos_auth::signing::ActiveKey)\n    -&gt; Result&lt;String, super::errors::OpError&gt; { /* jsonwebtoken encode with header.kid = signer.kid */ unimplemented!() }</code></pre>\n<h3>3.6 - authorize (control flow, normative skeleton)</h3>\n<pre><code class=\"language-rust\">// services/auth/src/op/authorize.rs  (shape, not full body)\n// 1. parse + validate client_id (active RP)            -&gt; else render_error(\"unknown_client\")  [no redirect]\n// 2. exact-match redirect_uri against rp.redirect_uris  -&gt; else render_error(\"redirect_mismatch\") [no redirect]  (DEC-2491)\n// 3. require response_type=code, scope∋openid, S256 code_challenge, state; nonce optional\n//      -&gt; else redirect(redirect_uri, error=invalid_request|unsupported_response_type, state)\n// 4. resolve subject:\n//      if valid __Host-cyberos_sso cookie -&gt; sso row (not revoked) -&gt; subject_id        (silent_sso)\n//      else -&gt; stash this authorize request, 302 into FR-AUTH-104 initiate (upstream broker)\n//              on FR-AUTH-104 callback: create sso session (DEC-2489), resume here        (upstream_broker)\n// 5. REVOKE GATE (DEC-2488): subject revoked? -&gt; redirect(error=access_denied) + audit op_authorize_denied\n// 6. mint single-use code (60s, PKCE-bound, sso_session_id) -&gt; redirect(redirect_uri, code, state)\n//    + audit op_authorize_issued</code></pre>\n<hr>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Discovery served</strong> - <code>/.well-known/openid-configuration</code> returns the pinned OIDC profile; <code>issuer</code> equals the configured value; <code>jwks_uri</code> is the existing FR-AUTH-004 JWKS.</li><li><strong>Unknown client rejected without redirect</strong> - bad <code>client_id</code> → error page, no Location header.</li><li><strong>redirect_uri exact match</strong> - registered <code>https://chat.cyberos.world/signin/oidc/complete</code> accepted; any variant (extra slash, query, subpath) → error page, no redirect.</li><li><strong>PKCE required</strong> - authorize without <code>code_challenge</code> → redirect <code>error=invalid_request</code>.</li><li><strong>Implicit/hybrid rejected</strong> - <code>response_type=token</code> or <code>code id_token</code> → <code>unsupported_response_type</code>.</li><li><strong>Silent SSO</strong> - with a valid <code>__Host-cyberos_sso</code> cookie, authorize issues a code with no upstream round trip (<code>outcome=silent_sso</code>).</li><li><strong>Upstream broker</strong> - with no cookie, authorize drives FR-AUTH-104 Google login, creates an SSO session, then issues the code.</li><li><strong>Revoke gate at authorize</strong> - a revoked subject → <code>access_denied</code>, no code, <code>auth.op_authorize_denied</code> row.</li><li><strong>Code single-use</strong> - first <code>/token</code> exchange succeeds; replay of the same code → <code>invalid_grant</code> AND any token from it is revoked.</li><li><strong>Code TTL</strong> - exchange after 60s → <code>invalid_grant</code>.</li><li><strong>PKCE verifier checked</strong> - wrong <code>code_verifier</code> → <code>invalid_grant</code>.</li><li><strong>id_token claims</strong> - <code>iss</code>/<code>sub</code>/<code>aud</code>/<code>exp</code>/<code>nonce</code>/<code>email</code>/<code>name</code>/<code>tenant_id</code>/<code>roles</code> present; signed RS256; <code>kid</code> resolves in JWKS; nonce echoed.</li><li><strong>access_token + userinfo</strong> - <code>/userinfo</code> with the access_token returns the same identity; revoked subject → 401.</li><li><strong>RP auth</strong> - <code>/token</code> with wrong <code>client_secret</code> → 401 <code>invalid_client</code>.</li><li><strong>RP registry admin-only</strong> - non-admin create → 403; <code>client_secret</code> returned once at create, never on read/update.</li><li><strong>SSO revoke cascade</strong> - revoking the subject marks <code>auth_sso_sessions.revoked_at</code>; subsequent silent SSO fails → re-auth required.</li><li><strong>Issuer pinned</strong> - booting with discovery <code>issuer</code> != id_token <code>iss</code> config → provider routes refuse to start.</li><li><strong>Append-only login history</strong> - UPDATE/DELETE on <code>auth_op_login_history</code> from <code>cyberos_app</code> → permission denied.</li><li><strong>Token exchange p95 &lt; 200 ms</strong> - <code>op_token_perf_test</code>.</li><li><strong>OTel span <code>auth.op.token</code></strong> - <code>outcome</code> attribute present.</li><li><strong>7 audit kinds emitted</strong> - one test asserts each row shape.</li></ol>\n<hr>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/op_authorize_revoke_gate_test.rs\n#[tokio::test]\nasync fn revoked_subject_cannot_get_code(ctx: TestCtx) {\n    let rp  = ctx.register_rp(\"chat\", &amp;[\"https://chat.cyberos.world/signin/oidc/complete\"]).await;\n    let sub = ctx.seed_subject_with_sso_session(\"[email protected]\").await;\n    ctx.revoke_subject(sub.id).await;                       // FR-AUTH-005\n    let resp = ctx.authorize(&amp;rp, &amp;sub.sso_cookie, \"S256-challenge\", \"state123\").await;\n    assert_eq!(resp.status(), 302);\n    let loc = resp.headers()[\"Location\"].to_str().unwrap();\n    assert!(loc.contains(\"error=access_denied\"));\n    assert!(!loc.contains(\"code=\"));\n    assert_eq!(ctx.memory_rows(\"auth.op_authorize_denied\").await.len(), 1);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/op_code_single_use_test.rs\n#[tokio::test]\nasync fn code_replay_revokes_issued_tokens(ctx: TestCtx) {\n    let (rp, code, verifier) = ctx.happy_authorize().await;\n    let first = ctx.token_exchange(&amp;rp, &amp;code, &amp;verifier).await.unwrap();      // 200\n    assert!(first.id_token.split('.').count() == 3);\n    let replay = ctx.token_exchange(&amp;rp, &amp;code, &amp;verifier).await.unwrap_err();\n    assert!(format!(\"{replay:?}\").contains(\"invalid_grant\"));\n    assert!(ctx.access_token_is_revoked(&amp;first.access_token).await);          // RFC 6749 §4.1.2\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/op_redirect_uri_exact_match_test.rs\n#[tokio::test]\nasync fn redirect_uri_must_match_exactly(ctx: TestCtx) {\n    let rp = ctx.register_rp(\"chat\", &amp;[\"https://chat.cyberos.world/signin/oidc/complete\"]).await;\n    for bad in [\"https://chat.cyberos.world/signin/oidc/complete/\",\n                \"https://chat.cyberos.world/signin/oidc/complete?x=1\",\n                \"https://evil.example/signin/oidc/complete\"] {\n        let resp = ctx.authorize_with_redirect(&amp;rp, bad).await;\n        assert!(resp.is_error_page());        // rendered, NOT redirected\n        assert!(resp.headers().get(\"Location\").is_none());\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/op_id_token_claims_test.rs\n#[tokio::test]\nasync fn id_token_carries_roles_and_tenant_signed_by_jwks(ctx: TestCtx) {\n    let (rp, code, verifier) = ctx.happy_authorize_with_roles(&amp;[\"tenant-admin\"]).await;\n    let tok = ctx.token_exchange(&amp;rp, &amp;code, &amp;verifier).await.unwrap();\n    let claims = ctx.verify_against_jwks(&amp;tok.id_token).await.unwrap();   // verifies kid ∈ JWKS\n    assert_eq!(claims[\"iss\"], ctx.configured_issuer());\n    assert_eq!(claims[\"aud\"], rp.client_id);\n    assert_eq!(claims[\"roles\"], serde_json::json!([\"tenant-admin\"]));\n    assert!(claims[\"tenant_id\"].is_string());\n}</code></pre>\n<hr>\n<h2>§6 - Implementation skeleton</h2>\n<p>§3 is the skeleton. The provider reuses the FR-MCP-004 <code>oauth</code> substrate (pkce, jwt-minting against <code>auth_signing_keys</code>, code/token store shape, discovery shape, error enum) per DEC-2493; the net-new is the OIDC layer (id_token, userinfo, openid-configuration), the authorize human-auth brokering into FR-AUTH-104, and the SSO cookie/session. The 7 memory row builders follow the canonical pattern.</p>\n<hr>\n<h2>§7 - Dependencies</h2>\n<p>Upstream:</p>\n<ul><li>FR-AUTH-004 - signing keys + JWKS (id_token + access_token signed against these; one key system).</li><li>FR-AUTH-104 - OIDC client (the upstream Google login that authorize brokers to when there is no SSO session).</li><li>FR-AUTH-005 - revoke / deny-list (the kick the authorize/token/userinfo gates enforce).</li><li>FR-AUTH-101 - role enum (roles claim in the id_token).</li><li>FR-MCP-004 - the OAuth-server substrate reused (DEC-2493).</li></ul>\n<p>Downstream:</p>\n<ul><li>CHAT SSO (the Mattermost OIDC connector) consumes this provider - the reachable unified-path login (the AuthBridge plugin is a non-working simulation and is dropped; Mattermost's native OIDC connector points at this provider's discovery URL). The exact CHAT FR id is reconciled in the backlog.</li><li>FR-PORTAL-003 - external IdP / portal login federates here too.</li><li>FR-PORTAL-004 - SCIM auto-deprovision complements the revoke gate for instant cross-app deprovisioning.</li></ul>\n<p>Cross-module:</p>\n<ul><li>FR-AUTH-002 - subject lookup for claims.</li><li>FR-AI-003 / memory - audit bridge.</li><li>FR-OBS-007 - alarm on a denied-revoked spike (expected post-layoff; sustained from one IP may be probing).</li></ul>\n<hr>\n<h2>§8 - Example payloads</h2>\n<h3>8.1 - POST /v1/auth/op/rp-clients (create, admin-only)</h3>\n<pre><code class=\"language-json\">{\n  \"name\": \"CyberOS Chat\",\n  \"redirect_uris\": [\"https://chat.cyberos.world/signin/oidc/complete\"],\n  \"post_logout_redirect_uris\": [\"https://chat.cyberos.world/login\"],\n  \"allow_refresh\": false\n}</code></pre>\n<p>Response (the only time the secret is shown):</p>\n<pre><code class=\"language-json\">{\n  \"client_id\": \"cyberos-chat\",\n  \"client_secret\": \"ONE_TIME_REVEAL_VALUE\",\n  \"redirect_uris\": [\"https://chat.cyberos.world/signin/oidc/complete\"]\n}</code></pre>\n<h3>8.2 - auth.op_token_issued memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.op_token_issued\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"rp_client_id\": \"cyberos-chat\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"scope\": \"openid profile email\",\n  \"ts_ns\": 1751155200000000000\n}</code></pre>\n<h3>8.3 - auth.op_authorize_denied memory row (revoke gate)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.op_authorize_denied\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"rp_client_id\": \"cyberos-chat\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"reason\": \"subject_revoked\",\n  \"ts_ns\": 1751155200000000000\n}</code></pre>\n<hr>\n<h2>§9 - Open questions</h2>\n<p>Deferred (named slices, not gaps):</p>\n<ul><li><strong>Back-channel + RP-initiated logout</strong> (OIDC logout specs) - slice 2. This is what kills a live Mattermost session the instant a subject is revoked; slice 1 already blocks re-authentication and revokes the SSO session.</li><li><strong>Refresh-token rotation for RPs</strong> - slice 2 (the FR-AUTH-004 rotation already exists; this wires it per RP).</li><li><strong>Third-party consent UI + RFC 7591 Dynamic Client Registration</strong> - slice 3 (first-party RPs are admin-registered).</li><li><strong>SCIM auto-deprovision</strong> - FR-PORTAL-004 (instant cross-app removal beyond the revoke gate).</li><li><strong>Extract-vs-reimplement the MCP oauth substrate</strong> - the audit decides whether to lift <code>services/mcp-gateway/src/oauth/</code> into a shared crate or re-implement the thin parts in auth (DEC-2493).</li></ul>\n<hr>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Unknown client_id</td><td>registry lookup</td><td>error page, no redirect</td><td>Register RP</td></tr><tr><td>redirect_uri mismatch</td><td>exact match</td><td>error page, no redirect</td><td>Fix RP config</td></tr><tr><td>Missing PKCE</td><td>param check</td><td>redirect error=invalid_request</td><td>RP sends S256</td></tr><tr><td>Implicit/hybrid response_type</td><td>param check</td><td>unsupported_response_type</td><td>Use code flow</td></tr><tr><td>No SSO session, no cookie</td><td>cookie absent</td><td>broker to FR-AUTH-104</td><td>Designed</td></tr><tr><td>Revoked subject at authorize</td><td>revoke gate</td><td>access_denied + audit</td><td>Designed (the kick)</td></tr><tr><td>Revoked subject at token</td><td>revoke gate</td><td>invalid_grant</td><td>Designed</td></tr><tr><td>Revoked subject at userinfo</td><td>revoke gate</td><td>401</td><td>Designed</td></tr><tr><td>Code expired (&gt;60s)</td><td>expires_at</td><td>invalid_grant</td><td>Re-initiate</td></tr><tr><td>Code replay</td><td>consume guard</td><td>invalid_grant + token revoke</td><td>Designed</td></tr><tr><td>Wrong code_verifier</td><td>S256 check</td><td>invalid_grant</td><td>Designed</td></tr><tr><td>Wrong client_secret</td><td>client auth</td><td>401 invalid_client</td><td>Fix secret</td></tr><tr><td>issuer mismatch (config)</td><td>boot check</td><td>provider refuses to start</td><td>Fix config</td></tr><tr><td>JWKS/signing key missing</td><td>FR-AUTH-004</td><td>500 + sev-1</td><td>Key health</td></tr><tr><td>SSO cookie fixation</td><td>__Host- prefix + server table</td><td>rejected</td><td>Designed</td></tr><tr><td>SSO session past absolute expiry</td><td>absolute_expiry</td><td>re-auth</td><td>Designed</td></tr><tr><td>Append-only history UPDATE/DELETE</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>RLS bypass</td><td>USING + FORCE</td><td>0 rows</td><td>Designed</td></tr><tr><td>client_secret leaked in response</td><td>one-time reveal only</td><td>never re-exposed</td><td>Designed</td></tr><tr><td>Live downstream session after revoke</td><td>(slice 2)</td><td>not killed in slice 1</td><td>Back-channel logout (slice 2)</td></tr></tbody></table>\n<hr>\n<h2>§11 - Implementation notes</h2>\n<ul><li>One issuer, one JWKS - reuse FR-AUTH-004; never a second key system.</li><li>authorize brokers: silent via the SSO cookie, else through FR-AUTH-104 Google, then resume.</li><li>PKCE S256 mandatory; implicit/hybrid forbidden.</li><li>redirect_uri exact match; on mismatch render an error, never redirect.</li><li>Codes hashed + single-use + 60s; replay revokes issued tokens.</li><li>Revoke gate on authorize + token + userinfo = kick-by-revoke into every first-party app.</li><li>SSO session is server-side truth (revocable); <code>__Host-</code> cookie is the strict form.</li><li>id_token carries sub + email + name + tenant_id + roles; signed RS256 with a JWKS kid.</li><li>First-party RPs admin-registered, confidential, locked redirect_uris, secret one-time reveal.</li><li>Consent skipped for first-party; third-party consent is slice 3.</li><li>Refresh tokens opt-in per RP (off for Mattermost).</li><li>Reuse the FR-MCP-004 oauth substrate; spend the budget on the OIDC layer + brokering + SSO cookie.</li><li>Honest kick boundary: slice 1 blocks re-auth + revokes SSO; slice 2 back-channel logout kills live app sessions.</li><li>7 memory audit kinds; PII-scrubbed; append-only history.</li></ul>\n<hr>\n<p><em>End of FR-AUTH-110.</em></p>";
