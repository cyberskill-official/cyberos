window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PROJ-014"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The Kanban Board <strong>MUST</strong> present issues grouped by status with drag/drop transitions + keyboard parity. The contract:</p>\n<ol><li><strong>MUST</strong> render 6 columns matching FR-PROJ-004 status enum: Backlog, Todo, InProgress, InReview, Done, Cancelled.</li><li><strong>MUST</strong> subscribe to FR-PROJ-002 WebSocket for live updates; YjsProvider for description/comments.</li><li><strong>MUST</strong> support drag-and-drop between columns via @dnd-kit/core:</li></ol>\n<ul><li>Drop on column → POST <code>/api/proj/issues/:id/transition</code> with <code>to: &lt;new_status&gt;</code>.</li><li>Illegal transition (422 from FR-PROJ-004) → snap card back to original column with 250ms spring animation.</li><li>Successful transition → emit toast + audit-trail link.</li><li>Optimistic UI: card visually moves immediately; reconciled on server confirmation.</li></ul>\n<ol><li><strong>MUST</strong> support keyboard navigation parity:</li></ol>\n<ul><li>Tab/Shift-Tab cycles focus through columns + cards.</li><li>J / K / Down / Up move focus within column.</li><li>H / L / Left / Right move focus between columns.</li><li>Enter opens FR-PROJ-017 Brief Modal for focused card.</li><li>Cmd/Ctrl + Shift + → moves focused card to next-rightward column (if legal); ← to next-leftward.</li><li>Esc dismisses any open transient UI.</li></ul>\n<ol><li><strong>MUST</strong> virtualise long columns via <code>react-window FixedSizeList</code> when &gt; 200 items; 60fps maintained.</li><li><strong>MUST</strong> render IssueCard with: title (≤ 2 lines truncated), assignee avatar, estimate badge, priority indicator, blocker count badge (FR-PROJ-011), labels.</li><li><strong>MUST</strong> show real-time presence indicators (FR-PROJ-003 awareness): cursor + selection of other users editing the same issue (when in Brief Modal).</li><li><strong>MUST</strong> emit memory audit row <code>proj.kanban_card_moved</code> per drag-induced transition; payload <code>{issue_id, from_status, to_status, by_subject_id, was_keyboard: bool, trace_id}</code>.</li><li><strong>MUST</strong> emit OTel client-side metrics via <code>web-vitals</code>:</li></ol>\n<ul><li><code>proj_kanban_render_p95_ms</code> (LCP for board page).</li><li><code>proj_kanban_drag_latency_ms</code> (drag start → drop reflected).</li><li><code>proj_kanban_transitions_total{outcome}</code> (counter; outcome ∈ accepted | rejected | optimistic_rollback).</li></ul>\n<ol><li><strong>MUST</strong> pass axe-core a11y audit: no critical/serious violations; ARIA roles for <code>application</code>/<code>group</code>/<code>listitem</code> correct; keyboard-only test passes.</li><li><strong>MUST</strong> handle WebSocket disconnect gracefully: banner \"offline; changes will sync when connected\"; drags queued in offline buffer (FR-PROJ-003 §1 #8).</li><li><strong>MUST</strong> support <code>?member=&lt;uuid&gt;</code> and <code>?label=&lt;id&gt;</code> URL query filters; updates URL on filter change for shareability.</li><li><strong>MUST</strong> support WIP (Work-In-Progress) limits per column when configured: <code>cyberos_proj_engagement_settings.wip_limits = {in_progress: 5, in_review: 3}</code>. Drag into a column at-or-above limit → warning banner + soft block (user can confirm to proceed; emit <code>proj.wip_limit_overridden</code> audit).</li><li><strong>MUST</strong> support card minification: at zoom level &lt; 75% OR per-engagement preference, render compact cards (title only, no badges). Reduces visual noise on dense boards.</li><li><strong>MUST</strong> support bulk operations via multi-select: Shift-click selects range; Cmd/Ctrl-click toggles; bulk drag moves all selected. Bulk transitions emit one memory row per issue (per FR-PROJ-002 §1 #16).</li><li><strong>MUST</strong> support quick-add via keyboard: pressing <code>c</code> from column-focused state opens an inline issue creator at the top of that column with status=column's status. ESC cancels; Enter creates.</li><li><strong>MUST</strong> support swimlanes by assignee: optional view mode <code>?swimlanes=assignee</code> renders rows per assignee with status columns. Helps team standups.</li><li><strong>MUST</strong> maintain scroll position across re-renders (e.g. WebSocket update doesn't reset scroll). Test: scroll column to 50%, receive update, scroll position preserved.</li><li><strong>MUST</strong> support undo for the last drag operation via Cmd/Ctrl+Z within 5 seconds; emits <code>proj.kanban_card_move_undone</code> audit; reverses the transition.</li><li><strong>MUST</strong> show drag-target preview: ghost card appears in target column at expected insert position during drag; users see exactly where it'll land.</li><li><strong>MUST</strong> support keyboard-driven card reorder within column: J/K with <code>Shift</code> held reorders the card (vs. moving focus); persists card order in <code>cyberos_proj_kanban_order</code> table.</li><li><strong>MUST</strong> include real-time issue count + WIP overflow indicators in column headers: \"InProgress 7/5 ⚠\" when over WIP limit.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why @dnd-kit (DEC-351 enabling)?</strong> Provides keyboard parity out of the box (focus-management hooks); react-dnd is mouse-first. WCAG AA mandates parity; @dnd-kit accelerates compliance.</p>\n<p><strong>Why optimistic UI (§1 #3)?</strong> 200ms reconciliation gap feels janky; optimistic move + rollback-on-error matches expected drag latency.</p>\n<p><strong>Why snap-back animation (§1 #3)?</strong> Without animation, rejection is invisible; user assumes drag succeeded. Spring animation signals \"rejected; try again.\"</p>\n<p><strong>Why react-window FixedSizeList (DEC-352)?</strong> Variable size = layout recalculation thrash; fixed size = O(1) per scroll frame. Issue cards are visually uniform anyway.</p>\n<p><strong>Why keyboard shortcut for cross-column move (§1 #4)?</strong> Power users move many issues quickly; mouse drag for 50 cards is friction. Cmd+Shift+→ is the discoverable shortcut.</p>\n<p><strong>Why URL filter sync (§1 #12)?</strong> Operators share \"look at Alice's bug backlog\" via URL — board state must be URL-encodable.</p>\n<p><strong>Why audit was_keyboard flag (§1 #8)?</strong> Distinguishes user behaviour patterns; informs UX research (\"kb users move 3× faster but make 30% more illegal-attempts\").</p>\n<p><strong>Why soft WIP limit (§1 #13)?</strong> Hard block frustrates operators; soft warn-and-confirm respects autonomy while flagging overflow.</p>\n<p><strong>Why card minification (§1 #14)?</strong> Dense boards (50+ cards visible) overload visual processing; compact mode trades detail for density.</p>\n<p><strong>Why bulk operations (§1 #15)?</strong> Bulk status moves at sprint planning are common; single-card drag for 30 cards is friction.</p>\n<p><strong>Why quick-add <code>c</code> (§1 #16)?</strong> Keyboard-driven workflows need card creation without context switching. <code>c</code> is the standard issue-create keystroke in Linear / GitHub Issues.</p>\n<p><strong>Why swimlanes (§1 #17)?</strong> Standup view: \"what is each person doing\" maps naturally to rows-per-person.</p>\n<p><strong>Why scroll preservation (§1 #18)?</strong> WebSocket updates re-render; scrolling-to-the-top is jarring during browsing.</p>\n<p><strong>Why undo (§1 #19)?</strong> Accidental drag is the most common kanban error; undo within 5s = forgiveness without overhead.</p>\n<p><strong>Why drag-target preview (§1 #20)?</strong> Card order matters within column; preview removes guesswork about insert position.</p>\n<p><strong>Why keyboard reorder (§1 #21)?</strong> Operators reordering for prioritisation need keyboard parity; Shift+J/K is the discoverable pattern.</p>\n<p><strong>Why WIP overflow indicator (§1 #22)?</strong> Operators glancing at the board see overflow immediately; \"5/3 ⚠\" is visual quick-glance signal.</p>\n<hr>\n<h2>§3 — API contract (component sketches)</h2>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/Kanban/Board.tsx\nexport function Board({ cycleId }: { cycleId: string }) {\n  const issues = useIssuesForCycle(cycleId);\n  const [filter, setFilter] = useUrlFilter();\n  const sensors = useSensors(\n    useSensor(PointerSensor),\n    useSensor(KeyboardSensor, { coordinateGetter: kbdCoordinateGetter }),\n  );\n  return (\n    &lt;DndContext sensors={sensors} onDragEnd={handleDragEnd}&gt;\n      &lt;div role=\"application\" aria-label=\"Kanban board\"&gt;\n        {COLUMNS.map(status =&gt; (\n          &lt;Column key={status} status={status}\n                  items={issues.filter(i =&gt; i.status === status &amp;&amp; matchesFilter(i, filter))} /&gt;\n        ))}\n        &lt;DragLayer /&gt;\n      &lt;/div&gt;\n    &lt;/DndContext&gt;\n  );\n\n  async function handleDragEnd(ev: DragEndEvent) {\n    const issueId = ev.active.id as string;\n    const newStatus = ev.over?.id as IssueStatus | undefined;\n    if (!newStatus) return;\n    const issue = issues.find(i =&gt; i.id === issueId)!;\n    if (!isLegal(issue.status, newStatus)) {\n      toast('Cannot move there', 'error');\n      return;   // dnd-kit snaps back automatically\n    }\n    optimisticMove(issueId, newStatus);\n    try {\n      const reason = requiresReason(issue.status, newStatus) ? await promptReason() : undefined;\n      await postTransition(issueId, newStatus, reason);\n      emitMemory('proj.kanban_card_moved', { issueId, from: issue.status, to: newStatus,\n                                              wasKeyboard: ev.activatorEvent instanceof KeyboardEvent });\n    } catch (e) {\n      rollbackMove(issueId);\n      toast('Transition rejected', 'error');\n    }\n  }\n}</code></pre>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/Kanban/Column.tsx\nimport { FixedSizeList as List } from 'react-window';\n\nexport function Column({ status, items }: { status: IssueStatus; items: Issue[] }) {\n  const VIRTUALISE_THRESHOLD = 200;\n  return (\n    &lt;div className=\"kanban-column\" role=\"group\" aria-label={status}&gt;\n      &lt;header&gt;&lt;h2&gt;{status}&lt;/h2&gt;&lt;span&gt;{items.length}&lt;/span&gt;&lt;/header&gt;\n      {items.length &gt; VIRTUALISE_THRESHOLD ? (\n        &lt;List height={600} itemCount={items.length} itemSize={92} width=\"100%\"&gt;\n          {({ index, style }) =&gt; (\n            &lt;div style={style}&gt;&lt;IssueCard issue={items[index]} /&gt;&lt;/div&gt;\n          )}\n        &lt;/List&gt;\n      ) : (\n        items.map(i =&gt; &lt;IssueCard key={i.id} issue={i} /&gt;)\n      )}\n    &lt;/div&gt;\n  );\n}</code></pre>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/Kanban/KeyboardNav.tsx\nfunction kbdCoordinateGetter(event: KeyboardEvent, args: any) {\n  // J/K = down/up, H/L = left/right\n  // Cmd+Shift+→ = move card to next-rightward legal column\n  // Implementation per @dnd-kit/sortable docs\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>6 columns render</strong> — board mounts with all 6 status columns.</li><li><strong>Drag valid → server accepts</strong> — drag Todo→InProgress → POST returns 200; card stays in InProgress.</li><li><strong>Drag illegal → snap back</strong> — drag Backlog→InReview → 422; card snaps back with animation; toast shown.</li><li><strong>Optimistic UI</strong> — drag → card visually moves before server response (verified via mock latency).</li><li><strong>Reason prompt on re-open</strong> — drag Done→InProgress → reason modal appears; submit moves card.</li><li><strong>Keyboard nav: Tab moves between columns</strong> — Tab cycles through column headers + cards.</li><li><strong>Keyboard nav: J/K within column</strong> — focus moves down/up.</li><li><strong>Keyboard nav: Cmd+Shift+→ moves to next legal column</strong> — works only when target is legal.</li><li><strong>Enter opens Brief Modal</strong> — focused card + Enter → FR-PROJ-017 modal opens.</li><li><strong>Esc dismisses overlays</strong> — Esc closes modal / toast / reason prompt.</li><li><strong>Virtualised at &gt; 200 items</strong> — fixture with 500 items → only ~30 rendered DOM nodes.</li><li><strong>60fps maintained on scroll</strong> — Chrome devtools performance trace shows no frame &gt; 16ms during scroll.</li><li><strong>Awareness indicator visible</strong> — second user opens same Brief Modal → cursor indicator shows on first user's view.</li><li><strong>Offline banner appears</strong> — disconnect WebSocket → banner; drags queued.</li><li><strong>URL filter sync</strong> — filter by member → URL updates; reload preserves filter.</li><li><strong>axe-core passes</strong> — no critical/serious violations on board page.</li><li><strong>memory audit row per move</strong> — every drag-induced transition → row.</li><li><strong>was_keyboard flag</strong> — keyboard-triggered move → flag true; mouse → false.</li><li><strong>Optimistic rollback metric</strong> — server reject → counter <code>optimistic_rollback</code> increments.</li><li><strong>LCP p95 &lt; 2.5s</strong> — empirical on 1000-issue board.</li><li><strong>WIP soft block</strong> — drag into column at WIP limit → confirm dialog; confirm proceeds + audit (AC for §1 #13).</li><li><strong>Compact card at low zoom</strong> — set zoom 50% → cards render compact (no badges) (AC for §1 #14).</li><li><strong>Bulk-select via Shift-click</strong> — Shift-click two cards → range selected; bulk drag moves both (AC for §1 #15).</li><li><strong>Quick-add <code>c</code> opens creator</strong> — Tab to column header + <code>c</code> → inline creator (AC for §1 #16).</li><li><strong>Swimlanes by assignee</strong> — <code>?swimlanes=assignee</code> → rows per assignee (AC for §1 #17).</li><li><strong>Scroll preserved across update</strong> — scroll to 50%, receive WS update → scroll preserved (AC for §1 #18).</li><li><strong>Undo within 5s</strong> — drag card; Cmd+Z within 5s → card reverts; <code>proj.kanban_card_move_undone</code> row (AC for §1 #19).</li><li><strong>Drag-target preview shows position</strong> — ghost card appears in target at insert position (AC for §1 #20).</li><li><strong>Shift+J/K reorders</strong> — focused card + Shift+J → moves down within column; persists to DB (AC for §1 #21).</li><li><strong>WIP overflow indicator in header</strong> — column with 7 cards + WIP=5 → header shows \"7/5 ⚠\" (AC for §1 #22).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// kanban_test.tsx\ntest('drag valid status accepted', async () =&gt; {\n  const { user } = render(&lt;Board cycleId={cycle.id} /&gt;);\n  const card = screen.getByText(/My Issue/);\n  await user.drag(card, screen.getByRole('group', { name: 'in_progress' }));\n  await waitFor(() =&gt; expect(card).toHaveAttribute('data-status', 'in_progress'));\n});\n\ntest('drag illegal status snaps back', async () =&gt; {\n  const { user } = render(&lt;Board cycleId={cycle.id} /&gt;);\n  mockTransition.fail422();\n  const card = screen.getByText(/Backlog Issue/);\n  await user.drag(card, screen.getByRole('group', { name: 'in_review' }));\n  await waitFor(() =&gt; expect(card).toHaveAttribute('data-status', 'backlog'));\n  expect(screen.getByRole('alert')).toHaveTextContent(/cannot move/i);\n});\n\ntest('keyboard nav: J moves focus down', async () =&gt; {\n  const { user } = render(&lt;Board cycleId={cycle.id} /&gt;);\n  const first = screen.getAllByTestId('issue-card')[0];\n  first.focus();\n  await user.keyboard('j');\n  expect(screen.getAllByTestId('issue-card')[1]).toHaveFocus();\n});\n\ntest('virtualised at 500 items', async () =&gt; {\n  setupCycle({ issueCount: 500 });\n  render(&lt;Board cycleId={cycle.id} /&gt;);\n  const cards = screen.queryAllByTestId('issue-card');\n  expect(cards.length).toBeLessThan(50);\n});</code></pre>\n<pre><code class=\"language-typescript\">// kanban_a11y_test.tsx\ntest('axe-core passes', async () =&gt; {\n  const { container } = render(&lt;Board cycleId={cycle.id} /&gt;);\n  const results = await axe(container);\n  expect(results).toHaveNoViolations();\n});\n\ntest('keyboard-only workflow', async () =&gt; {\n  const { user } = render(&lt;Board cycleId={cycle.id} /&gt;);\n  await user.tab();   // first column\n  await user.tab();   // first card\n  await user.keyboard('{Enter}');   // Brief Modal opens\n  expect(screen.getByRole('dialog')).toBeVisible();\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Component sketches above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-PROJ-002</strong> — WebSocket sync.</li><li><strong>FR-PROJ-003</strong> — Y.Doc + LWW + awareness.</li><li><strong>FR-PROJ-004</strong> — status FSM + transition endpoint.</li><li><strong>FR-PROJ-017 (sibling)</strong> — Brief Modal opened on Enter.</li><li><strong>FR-PROJ-018 (downstream)</strong> — design tokens + a11y CI.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.kanban_card_moved\",\n  \"payload\": {\n    \"issue_id\":    \"iss-...\",\n    \"from_status\": \"todo\",\n    \"to_status\":   \"in_progress\",\n    \"by_subject_id\": \"7e57c0de-...\",\n    \"was_keyboard\": false,\n    \"trace_id\":   \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>WIP limits per column — slice 4+; ops-configurable.</li><li>Multi-cycle board (cross-cycle view) — slice 4+.</li><li>Swimlanes by assignee — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Server rejects transition</td><td>422</td><td>Snap back + toast</td><td>User retries</td></tr><tr><td>WS disconnect mid-drag</td><td>useWebSocketStatus</td><td>Queue in offline buffer</td><td>Reconnect drains</td></tr><tr><td>1000+ items in one column</td><td>virtualisation</td><td>60fps; no jank</td><td>None</td></tr><tr><td>Keyboard nav loses focus</td><td>focus-trap fallback</td><td>Re-focus first column</td><td>None</td></tr><tr><td>Drag from filtered view</td><td>filter still applies; underlying issue moves</td><td>Card disappears from view post-drag</td><td>By design</td></tr><tr><td>Awareness flood (10 users)</td><td>YjsProvider throttle</td><td>30Hz cap</td><td>None</td></tr><tr><td>LCP regression</td><td>web-vitals reporting</td><td>sev-2 alarm</td><td>Investigate via Lighthouse</td></tr><tr><td>Browser without IntersectionObserver</td><td>polyfill via npm</td><td>None</td><td>None</td></tr><tr><td>Touch device</td><td>@dnd-kit TouchSensor</td><td>Drag works on iPad</td><td>None</td></tr><tr><td>Browser zoom &gt; 200%</td><td>reflow gracefully; no overlap</td><td>Usable</td><td>None</td></tr><tr><td>Cross-tab sync via BroadcastChannel</td><td>OPTIONAL slice 4+</td><td>None</td><td>None</td></tr><tr><td>RTL locale</td><td>flex direction reversed</td><td>Columns flow right-to-left</td><td>None</td></tr><tr><td>Issue deleted during drag</td><td>server returns 404; toast</td><td>Card removed</td><td>None</td></tr><tr><td>Optimistic move + rollback ordering</td><td>UI state machine</td><td>Last-write-wins on local state</td><td>None</td></tr><tr><td>WIP soft block but operator overrides</td><td>audit emitted</td><td>None</td><td>None</td></tr><tr><td>WIP limits not configured</td><td>feature absent</td><td>works without WIP</td><td>None</td></tr><tr><td>Compact card cuts off important info</td><td>hover shows tooltip</td><td>None</td><td>Operator can re-enlarge</td></tr><tr><td>Bulk drag with &gt; 50 cards</td><td>bounded; warn at 30</td><td>bulk transition slow but completes</td><td>None</td></tr><tr><td>Quick-add with invalid title</td><td>inline validation</td><td>error shown; not created</td><td>None</td></tr><tr><td>Swimlanes with many assignees (50+)</td><td>virtualised; collapsible</td><td>None</td><td>None</td></tr><tr><td>Scroll preservation breaks on layout change</td><td>property test</td><td>None</td><td>Author fixes</td></tr><tr><td>Undo after 5s</td><td>rejected</td><td>None</td><td>None</td></tr><tr><td>Drag preview lag at 60fps boundary</td><td>RAF-throttled</td><td>smooth</td><td>None</td></tr><tr><td>Shift+J reorder race with WS update</td><td>conflict resolution via LWW</td><td>None</td><td>None</td></tr><tr><td>WIP overflow indicator wraps</td><td>CSS layout</td><td>None</td><td>None</td></tr><tr><td>Quick-add <code>c</code> triggers in input field</td><td>event filter (no in inputs)</td><td>None</td><td>None</td></tr><tr><td>Touch device drag-and-drop with bulk select</td><td>partial support; warn</td><td>bulk single-card only on touch</td><td>None</td></tr><tr><td>Locale RTL with swimlanes</td><td>reflow correctly</td><td>None</td><td>None</td></tr><tr><td>WS connection flapping mid-undo</td><td>preserves intent; reapplies on reconnect</td><td>None</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>@dnd-kit/core provides <code>useSensor(KeyboardSensor)</code>; we override <code>coordinateGetter</code> to map J/K/H/L to coordinate changes.</li><li><code>react-window</code> requires fixed <code>itemSize</code>; we measure once at mount and assume cards are uniform height.</li><li>Optimistic state uses zustand store with rollback function returned at action time.</li><li>Awareness rendering uses YjsProvider's <code>awarenessChange</code> event; throttled by 30Hz cap.</li><li>The <code>was_keyboard</code> flag detects via <code>event instanceof KeyboardEvent</code> on dnd-kit's activator event.</li><li>Color tokens for column headers come from FR-PROJ-018.</li><li>Brief Modal is lazy-loaded via React.lazy to keep Board bundle ≤ 200 KB gzip.</li><li>Filter URL serialization: <code>?member=&lt;uuid&gt;&amp;label=&lt;id&gt;</code> — simple flat schema.</li><li>WIP limits enforce soft-block via dialog rather than hard reject because: enforcing too strictly demotivates flow; soft signal preserves autonomy.</li><li>Compact card threshold is browser zoom % AND per-engagement preference; either triggers.</li><li>Bulk operations use a temporary <code>selected_ids: Set&lt;string&gt;</code> in zustand; cleared on action complete.</li><li>Quick-add <code>c</code> is the canonical issue-create shortcut across modern tools (Linear, GitHub); user familiarity drives adoption.</li><li>Swimlanes by assignee are an additional axis (rows) layered onto columns; virtualised per-row for performance.</li><li>Scroll position preservation uses <code>ScrollRestoration</code> from react-router or manual <code>scrollTop</code> cache; tested across WS updates.</li><li>Undo within 5s uses a debounced timer; after 5s, undo is unavailable. Why 5s: balances forgiveness against operator clarity (\"the move stuck after a moment\").</li><li>Drag-target preview is provided by @dnd-kit's <code>useDroppable</code> + custom ghost overlay; performance is GPU-accelerated transform.</li><li>Keyboard reorder (Shift+J/K) updates the <code>kanban_order</code> column; persisted to enable cross-device order consistency.</li><li>WIP overflow indicator uses <code>aria-live=\"polite\"</code> so screen readers announce the overflow.</li><li>Quick-add <code>c</code> event listener filters out typing in input/textarea elements; only triggers when focus is on column / card.</li><li>We considered animated transitions for swimlane rearrange but kept simple to maintain 60fps on low-end devices.</li><li>Touch device support for bulk select is limited (no Shift key); users tap individual cards then drag. Future slice 4+ adds long-press multi-select.</li><li>RTL locale handling: column flex-direction reverses; J/K maps to logical direction (J=down regardless of LTR/RTL).</li><li>WebSocket reconnect after undo: the undo operation is the persisted state; reconnect resyncs.</li><li>Drag preview rendering uses <code>pointer-events: none</code> ghost element to avoid interfering with hit-testing.</li><li>The 5-second undo window emits a small toast \"Press Cmd+Z to undo\" so operators discover the feature.</li><li>WIP limits per column are stored in JSONB; operators set via engagement settings UI.</li><li>Compact card mode preserves keyboard navigation; only the visual presentation changes.</li><li>Multi-select keyboard support (slice 4+): Shift+Arrow extends selection.</li></ul>\n<hr>\n<p><em>End of FR-PROJ-014.</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Client code lives under apps/web/src (there is no web/proj-client/).</p>";
