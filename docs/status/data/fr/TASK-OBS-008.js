window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A read-only HTTP service <code>obs-compliance-view</code> <strong>MUST</strong> expose pre-built compliance views over the memory audit chain. Each view:</p>\n<ol><li><strong>MUST</strong> be one of 4 endpoints: <code>GET /eu-ai-act/</code>, <code>GET /pdpl/</code>, <code>GET /soc2/</code>, <code>GET /iso27001/</code>. Each scoped to that regulation's audit requirements (per-view rows defined in §1 #11 below).</li><li><strong>MUST</strong> require <code>role: external_auditor</code> in the JWT for access. Operators access via Grafana (which uses the same backend). Tenant-admin role does NOT grant compliance-view access — auditor is a distinct role provisioned per engagement.</li><li><strong>MUST</strong> be tenant-scoped: every query carries <code>tenant_id</code> from the JWT; auditor sees ONLY their assigned tenant's rows. RLS at the memory query layer is the enforcement. Cross-tenant <code>?tenant_id=</code> query parameter is rejected with 403.</li><li><strong>MUST</strong> query the memory audit chain (binlog rows) READ-ONLY; never mutate. The query API is the existing <code>memory::query(tenant_id, kinds, since, until)</code>.</li><li><strong>MUST</strong> emit Ed25519 hash-chain proof on every view's response footer. The proof signs the response contents (canonical JSON of rows + summary) using the deployment's signing key (loaded from secret store). Auditors can independently verify the signature against the published public key.</li><li><strong>MUST</strong> support time-range filtering via <code>?since=&lt;ISO8601&gt;&amp;until=&lt;ISO8601&gt;</code> query parameters. Window &gt; 365 days is rejected with 400 (auditor must paginate).</li><li><strong>MUST</strong> export to PDF + JSON formats via <code>?format=pdf|json</code> (default json).</li><li><strong>MUST</strong> complete view rendering within 30s p95 for 90-day windows. Above 30s, response is <code>429 TOO_LARGE</code> with suggestion to reduce window.</li><li><strong>MUST</strong> include a summary block per view (counts, key metrics) + the row list. Auditors typically read the summary first; rows are evidence.</li><li><strong>MUST</strong> emit memory audit row <code>obs.compliance_view_accessed</code> per query (auditor self-audit) with payload: <code>auditor_subject_id</code>, <code>tenant_id</code>, <code>view</code>, <code>time_range_days</code>, <code>request_id</code>.</li><li><strong>Per-view content (slice-3 scope):</strong></li></ol>\n<ul><li><strong>EU AI Act</strong> (Art. 12 record-keeping):</li><li><code>ai.invocation</code>, <code>ai.persona_loaded</code>, <code>ai.zdr_violation</code>, <code>ai.residency_violation</code> rows.</li><li>Persona-version stamps + EU AI Act Art. 50 <code>made_by_genie</code> attributions.</li><li>Oversight log: every CLI mutation (<code>cli.*</code>) for AI infra.</li><li><strong>PDPL</strong> (VN data residency):</li><li>DSAR fulfilment rows (delete-data, export-data requests).</li><li>Cross-border transfer log (<code>ai.residency_violation</code> + <code>obs.langsmith_export_*</code>).</li><li>Consent records (langsmith opt-in/out audit).</li><li>PII redaction stats (TASK-AI-011 metrics aggregated).</li><li><strong>SOC 2</strong> (trust services criteria):</li><li>Access logs (<code>auth.token_issued</code> + <code>auth.token_failed</code>).</li><li>Configuration changes (<code>ai.cli_policy_updated</code> + <code>ai.cli_breaker_reset</code>).</li><li>Backup attestations (TASK-OBS-009).</li><li>Incident response (<code>obs.alert_triaged</code> + <code>obs.alert_acked</code>).</li><li><strong>ISO 27001:2022</strong> (Annex A):</li><li>Asset inventory (tenant + subject lists).</li><li>Risk assessments (manual ops uploads).</li><li>Access control reviews (subject-role audits).</li></ul>\n<ol><li><strong>MUST</strong> enforce PII-placeholder discipline: rows returned MUST NOT contain raw PII. The audit chain itself uses placeholders (<code>email_hash16</code>, <code>&lt;VN_CCCD_1&gt;</code>); this view inherits. Defence-in-depth: response body is regex-scanned for raw PII patterns before serving; matches → sev-1 + 500.</li><li><strong>MUST</strong> authenticate via TASK-AUTH-004 JWT verification; auditor JWT issued per engagement (typically 30-day TTL, longer than standard) via <code>cyberos-auth issue-auditor-token</code>.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>obs_compliance_view_requests_total{view, format, outcome}</code> (counter).</li><li><code>obs_compliance_view_latency_ms{view}</code> (histogram).</li><li><code>obs_compliance_view_rows_returned{view}</code> (histogram).</li><li><code>obs_compliance_pii_leak_attempted_total</code> (counter; sev-1 alarm).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why pre-built views (DEC-175)?</strong> Compliance audits are time-consuming because auditors ask questions the team has never anticipated. Pre-building the 4 most common views (per regulation) turns audit prep from days to hours. Auditors recognise the format; we save engagement time.</p>\n<p><strong>Why Ed25519 chain proof (DEC-176)?</strong> Hash-chain proof is the differentiator vs SaaS observability (Datadog, Splunk). The auditor can independently verify \"this view is derived from an immutable chain; the signature is valid.\" Without proof, auditors must trust the source — taking longer to gain comfort.</p>\n<p><strong>Why read-only (§1 #4)?</strong> Compliance views derive FROM the audit chain. Mutating from the view would be self-defeating (\"the audit log shows I edited the audit log\"). Read-only by construction.</p>\n<p><strong>Why separate <code>external_auditor</code> role (DEC-178)?</strong> Operators have broader access (CLI mutations, ops tooling); auditors should have narrower access (read-only views, scoped to assigned tenant). Separate role + per-engagement JWT TTL prevents permanent auditor access.</p>\n<p><strong>Why 30s p95 budget (§1 #8)?</strong> Auditors expect responsive tooling. Above 30s, the audit experience degrades; auditors workaround by exporting raw chain data (which compromises the view's value-add).</p>\n<p><strong>Why 365-day max window (§1 #6)?</strong> Multi-year queries balloon response size + render time. 365 days covers annual audit cycles; longer queries paginate. The cap is conservative; raising requires explicit task amendment.</p>\n<p><strong>Why per-engagement JWT TTL (§1 #13)?</strong> Standard 1-hour JWTs would force auditors to refresh constantly. 30-day TTL covers a typical engagement window. Per-engagement issuance + rotation prevents stale auditor access.</p>\n<p><strong>Why PII-placeholder defence-in-depth (§1 #12)?</strong> Audit chain SHOULD contain only placeholders (TASK-AUTH-002 + TASK-AUTH-004 + TASK-AI-014 enforce). But regression-bugs could leak. Regex scan at response time catches the regression before the auditor sees raw PII.</p>\n<p><strong>Why memory audit row of view access (§1 #10)?</strong> The auditor's own access is auditable. Compliance reviews of the auditor (yes, this happens) need to know \"what did auditor X look at during their engagement?\"</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/obs-compliance-view/src/main.rs\nuse axum::{Router, routing::get, Json, Query};\n\npub fn router() -&gt; Router {\n    Router::new()\n        .route(\"/eu-ai-act/\", get(views::eu_ai_act::handle))\n        .route(\"/pdpl/\", get(views::pdpl::handle))\n        .route(\"/soc2/\", get(views::soc2::handle))\n        .route(\"/iso27001/\", get(views::iso27001::handle))\n}\n\n#[derive(Deserialize)]\npub struct ViewQuery {\n    pub since: DateTime&lt;Utc&gt;,\n    pub until: DateTime&lt;Utc&gt;,\n    #[serde(default = \"default_format\")]\n    pub format: Format,\n}\n\n#[derive(Deserialize)]\n#[serde(rename_all = \"lowercase\")]\npub enum Format { Json, Pdf }\n\n#[derive(Serialize)]\npub struct ViewResponse {\n    pub regulation: String,\n    pub tenant_id: Uuid,\n    pub time_range: (DateTime&lt;Utc&gt;, DateTime&lt;Utc&gt;),\n    pub rows: Vec&lt;AuditRow&gt;,\n    pub summary: serde_json::Value,\n    pub chain_proof: ChainProof,\n}\n\n#[derive(Serialize)]\npub struct ChainProof {\n    pub algorithm: String,        // \"Ed25519\"\n    pub public_key_id: String,    // identifier for verifier\n    pub sth_at_query_time: String, // signed tree head from memory\n    pub signature: String,        // base64-encoded over canonical(response_minus_chain_proof)\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum ViewError {\n    #[error(\"auth failed\")]\n    AuthFailed,\n    #[error(\"forbidden: needs external_auditor role\")]\n    Forbidden,\n    #[error(\"time range &gt; 365 days\")]\n    TimeRangeTooLarge,\n    #[error(\"tenant_id query param not allowed; tenant from JWT\")]\n    TenantIdInQuery,\n    #[error(\"query timeout (&gt; 30s)\")]\n    QueryTimeout,\n    #[error(\"pii leak attempted in response (sev-1)\")]\n    PiiLeakAttempt,\n    #[error(\"memory query failed: {0}\")]\n    MemoryQueryFailed(String),\n}</code></pre>\n<pre><code class=\"language-rust\">// services/obs-compliance-view/src/views/eu_ai_act.rs\npub async fn handle(query: ViewQuery, claims: Claims) -&gt; Result&lt;ViewResponse, ViewError&gt; {\n    auth::require_auditor_role(&amp;claims)?;\n    validate_time_range(query.since, query.until)?;\n\n    let rows = memory::query(claims.tenant_id, EU_AI_ACT_KINDS, query.since, query.until).await?;\n    pii_scrub::reject_if_raw_pii(&amp;rows)?;\n\n    let summary = serde_json::json!({\n        \"total_calls\": rows.iter().filter(|r| r.kind == \"ai.invocation\").count(),\n        \"unique_personas\": rows.iter().filter(|r| r.kind == \"ai.persona_loaded\").map(|r| r.payload[\"persona_handle\"].clone()).collect::&lt;HashSet&lt;_&gt;&gt;().len(),\n        \"zdr_violations\": rows.iter().filter(|r| r.kind == \"ai.zdr_violation\").count(),\n        \"residency_violations\": rows.iter().filter(|r| r.kind == \"ai.residency_violation\").count(),\n    });\n\n    let chain_proof = chain_proof::sign_response(&amp;rows, &amp;summary).await?;\n\n    let response = ViewResponse {\n        regulation: \"EU AI Act\".into(),\n        tenant_id: claims.tenant_id,\n        time_range: (query.since, query.until),\n        rows, summary, chain_proof,\n    };\n\n    memory::emit(canonical::compliance_view_accessed(\n        claims.subject_id, claims.tenant_id, \"eu-ai-act\",\n        (query.until - query.since).num_days(), &amp;claims.request_id,\n    )).await?;\n\n    Ok(response)\n}\n\nconst EU_AI_ACT_KINDS: &amp;[&amp;str] = &amp;[\n    \"ai.invocation\", \"ai.persona_loaded\",\n    \"ai.zdr_violation\", \"ai.residency_violation\",\n    \"ai.cli_policy_updated\", \"ai.cli_failover_drill\", \"ai.cli_memory_emitted\",\n];</code></pre>\n<pre><code class=\"language-rust\">// services/obs-compliance-view/src/chain_proof.rs\nuse ed25519_dalek::{Signer, SigningKey};\n\npub async fn sign_response(rows: &amp;[AuditRow], summary: &amp;serde_json::Value) -&gt; Result&lt;ChainProof, ViewError&gt; {\n    let key = load_signing_key().await?;\n    let canonical_bytes = canonicalise(rows, summary)?;\n    let sig = key.sign(&amp;canonical_bytes);\n    let sth = memory::current_signed_tree_head().await?;\n    Ok(ChainProof {\n        algorithm: \"Ed25519\".into(),\n        public_key_id: key.verifying_key().to_bytes()[..8].iter().map(|b| format!(\"{:02x}\", b)).collect(),\n        sth_at_query_time: hex::encode(sth),\n        signature: base64::encode(sig.to_bytes()),\n    })\n}\n\nfn canonicalise(rows: &amp;[AuditRow], summary: &amp;serde_json::Value) -&gt; Result&lt;Vec&lt;u8&gt;, ViewError&gt; {\n    // Deterministic canonical form: rows sorted by (ts_ns, kind, payload_hash); summary by sorted keys\n    let payload = serde_json::json!({ \"rows\": rows, \"summary\": summary });\n    Ok(serde_json::to_vec(&amp;payload).unwrap())\n}</code></pre>\n<pre><code class=\"language-rust\">// services/obs-compliance-view/src/export/pdf.rs\npub async fn render_pdf(view: &amp;ViewResponse) -&gt; Result&lt;Vec&lt;u8&gt;, ViewError&gt; {\n    let html = render_html_template(view)?;\n    let pdf = wkhtmltopdf::convert(&amp;html).map_err(|e| ViewError::MemoryQueryFailed(e.to_string()))?;\n    Ok(pdf)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>EU AI Act view returns all <code>ai.*</code> rows in time range.</li><li>PDPL view returns DSAR + cross-border + consent + PII redaction stats.</li><li>SOC 2 view returns access + config + backup + incident-response rows.</li><li>ISO 27001 view returns asset inventory + risk + access-control rows.</li><li>Hash-chain proof verifies — auditor independently runs <code>ed25519_verify(public_key, signature, canonical_bytes)</code> and gets true.</li><li>Cross-tenant attempt (<code>?tenant_id=other</code>) → 403.</li><li>Tenant_id query param rejected → 403 with <code>TenantIdInQuery</code>.</li><li>PDF export renders correctly (no missing rows; tables formatted).</li><li>JSON export is canonical (sorted keys, deterministic ordering).</li><li>90-day query under 30s p95.</li><li>&gt;365-day query → 400 with <code>TimeRangeTooLarge</code>.</li><li>JWT without <code>external_auditor</code> role → 403.</li><li>JWT missing → 401.</li><li>memory audit row <code>obs.compliance_view_accessed</code> emitted per query.</li><li>Raw PII in response → 500 + sev-1 metric increment.</li><li>Summary block populated correctly per view.</li><li>Per-engagement JWT TTL (30-day) supported.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn eu_ai_act_view_returns_relevant_rows() {\n    let pool = test_pool().await;\n    test_helper::insert_audit_rows(vec![\n        (\"ai.invocation\", json!({\"tenant_id\": \"t1\"})),\n        (\"ai.persona_loaded\", json!({\"tenant_id\": \"t1\"})),\n        (\"auth.token_issued\", json!({\"tenant_id\": \"t1\"})),   // NOT in EU AI Act view\n    ]).await;\n\n    let claims = auditor_claims_for(\"t1\");\n    let resp = views::eu_ai_act::handle(test_query_30d(), claims).await.unwrap();\n    let kinds: HashSet&lt;_&gt; = resp.rows.iter().map(|r| r.kind.clone()).collect();\n    assert!(kinds.contains(\"ai.invocation\"));\n    assert!(kinds.contains(\"ai.persona_loaded\"));\n    assert!(!kinds.contains(\"auth.token_issued\"));\n}\n\n#[tokio::test]\nasync fn cross_tenant_query_param_rejected() {\n    let claims = auditor_claims_for(\"t1\");\n    let mut query = test_query_30d();\n    // attempt to query t2 from t1's auditor\n    let err = views::eu_ai_act::handle_with_tenant_param(query, claims, \"t2\").await.expect_err(\"expected Forbidden\");\n    assert!(matches!(err, ViewError::TenantIdInQuery));\n}\n\n#[tokio::test]\nasync fn chain_proof_verifies() {\n    let claims = auditor_claims_for(\"t1\");\n    let resp = views::eu_ai_act::handle(test_query_30d(), claims).await.unwrap();\n    let pubkey = test_helper::deployment_public_key();\n    let canonical = canonicalise(&amp;resp.rows, &amp;resp.summary).unwrap();\n    let sig = base64::decode(&amp;resp.chain_proof.signature).unwrap();\n    let result = pubkey.verify(&amp;canonical, &amp;Signature::from_bytes(&amp;sig).unwrap());\n    assert!(result.is_ok());\n}\n\n#[tokio::test]\nasync fn pii_in_response_500s() {\n    let pool = test_pool().await;\n    test_helper::insert_audit_rows(vec![\n        (\"ai.invocation\", json!({\"tenant_id\": \"t1\", \"leaked_email\": \"alice@cyberos.world\"})),   // bug: raw email\n    ]).await;\n    let err = views::eu_ai_act::handle(test_query_30d(), auditor_claims_for(\"t1\")).await.expect_err(\"expected PiiLeakAttempt\");\n    assert!(matches!(err, ViewError::PiiLeakAttempt));\n}\n\n#[tokio::test]\nasync fn time_range_over_365_days_rejected() {\n    let mut query = test_query_30d();\n    query.since = chrono::Utc::now() - chrono::Duration::days(400);\n    let err = views::eu_ai_act::handle(query, auditor_claims_for(\"t1\")).await.expect_err(\"expected TimeRangeTooLarge\");\n    assert!(matches!(err, ViewError::TimeRangeTooLarge));\n}\n\n#[tokio::test]\nasync fn pdf_export_includes_all_rows() {\n    let resp = views::eu_ai_act::handle(test_query_30d(), auditor_claims_for(\"t1\")).await.unwrap();\n    let pdf = export::pdf::render_pdf(&amp;resp).await.unwrap();\n    assert!(pdf.starts_with(b\"%PDF\"));\n    let extracted_text = pdf_extract::extract_text_from_mem(&amp;pdf).unwrap();\n    for row in &amp;resp.rows {\n        assert!(extracted_text.contains(&amp;row.kind));\n    }\n}\n\n#[tokio::test]\nasync fn audit_row_per_query() {\n    let claims = auditor_claims_for(\"t1\");\n    let _ = views::eu_ai_act::handle(test_query_30d(), claims).await.unwrap();\n    let row = memory_test_helper::find_latest(\"obs.compliance_view_accessed\").unwrap();\n    assert_eq!(row.payload[\"view\"], \"eu-ai-act\");\n    assert_eq!(row.payload[\"tenant_id\"], \"t1\");\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-json\">// deploy/obs/grafana/dashboards/compliance.json (excerpt)\n{\n  \"title\": \"Compliance — EU AI Act\",\n  \"panels\": [{\n    \"type\": \"table\",\n    \"datasource\": \"obs-compliance-view\",\n    \"targets\": [{ \"url\": \"/eu-ai-act/?since=$__from&amp;until=$__to\" }]\n  }]\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-OBS-002</strong> — Tenant-aware query proxy (this task's tenant scoping inherits the pattern).</li><li><strong>TASK-AUTH-004</strong> — JWT verification.</li><li><strong>TASK-OBS-009</strong> — chain-of-custody manifest (TASK-OBS-008 surfaces backup attestations from this task).</li><li>memory module's <code>query</code> API + <code>current_signed_tree_head</code> API.</li><li>Crates: <code>axum</code>, <code>ed25519-dalek@2</code>, <code>wkhtmltopdf</code> (or <code>weasyprint</code> Python binding), <code>serde</code>, <code>chrono</code>, <code>base64</code>, <code>hex</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>EU AI Act view request</h3>\n<pre><code class=\"language-http\">GET /eu-ai-act/?since=2026-01-01T00:00:00Z&amp;until=2026-05-31T23:59:59Z&amp;format=json HTTP/1.1\nAuthorization: Bearer &lt;auditor-jwt&gt;\n\n→ 200 OK\n{\n  \"regulation\": \"EU AI Act\",\n  \"tenant_id\": \"550e...\",\n  \"time_range\": [\"2026-01-01T00:00:00Z\", \"2026-05-31T23:59:59Z\"],\n  \"rows\": [\n    { \"ts\": \"2026-02-15T...\", \"kind\": \"ai.invocation\", \"persona_version\": \"cuo-cpo@0.4.1\", \"...\" },\n    ...\n  ],\n  \"summary\": {\n    \"total_calls\": 12450,\n    \"unique_personas\": 3,\n    \"zdr_violations\": 2,\n    \"residency_violations\": 0\n  },\n  \"chain_proof\": {\n    \"algorithm\": \"Ed25519\",\n    \"public_key_id\": \"4b8c0d2f1a7e9c3b\",\n    \"sth_at_query_time\": \"...\",\n    \"signature\": \"base64-encoded-signature...\"\n  }\n}</code></pre>\n<h3>Cross-tenant attempt response</h3>\n<pre><code class=\"language-http\">HTTP/1.1 403 Forbidden\n{ \"error\": \"tenant_id_in_query\", \"reason\": \"tenant_id is derived from JWT; do not include as query param\" }</code></pre>\n<h3><code>obs.compliance_view_accessed</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"obs.compliance_view_accessed\",\n  \"payload\": {\n    \"auditor_subject_id\": \"auditor-...\",\n    \"tenant_id\": \"550e...\",\n    \"view\": \"eu-ai-act\",\n    \"time_range_days\": 151,\n    \"request_id\": \"compliance_view_...\"\n  }\n}</code></pre>\n<h3>PII-leak attempted response</h3>\n<pre><code class=\"language-http\">HTTP/1.1 500 Internal Server Error\n{ \"error\": \"pii_leak_attempted\", \"reason\": \"raw PII detected in response; sev-1 emitted\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>PCI DSS view — slice 5+.</li><li>HIPAA view (US healthcare tenants) — slice 5+.</li><li>Tenant-self-service compliance view (tenant-admin sees own data) — slice 4+.</li><li>Auditor query history (so engagement can resume) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>memory unavailable</td><td>memory query error</td><td>503</td><td>Self-resolves</td></tr><tr><td>Time range too large (&gt;365d)</td><td>size check</td><td>400 TimeRangeTooLarge</td><td>Auditor paginates</td></tr><tr><td>Signature key missing</td><td>load fails</td><td>500 + sev-1</td><td>Operator restores key</td></tr><tr><td>Auditor JWT invalid</td><td>JWT verify fails</td><td>401</td><td>Refresh per-engagement JWT</td></tr><tr><td>JWT lacks external_auditor role</td><td>role check</td><td>403</td><td>Operator grants role</td></tr><tr><td>Cross-tenant attempt via query param</td><td>param check</td><td>403</td><td>By design</td></tr><tr><td>Raw PII detected in response</td><td>regex scan</td><td>500 + sev-1</td><td>Investigate audit-chain regression</td></tr><tr><td>Query &gt; 30s</td><td>timeout</td><td>429</td><td>Auditor reduces window</td></tr><tr><td>PDF render fails</td><td>wkhtmltopdf error</td><td>500</td><td>Operator investigates wkhtmltopdf</td></tr><tr><td>Ed25519 verify fails (operator-side)</td><td>signature verify</td><td>Auditor flags</td><td>Investigate chain integrity</td></tr><tr><td>Per-engagement JWT not rotated</td><td>TTL expiry alert</td><td>Operator rotates</td><td>Standard cadence</td></tr><tr><td>Auditor accesses outside engagement window</td><td>row carries timestamp; manual review</td><td>Compliance review</td><td>Standard process</td></tr><tr><td>memory sth missing (no signed tree head)</td><td><code>current_signed_tree_head</code> returns None</td><td>500 + sev-1</td><td>Operator investigates memory</td></tr><tr><td>Compliance row schema drift</td><td>view tests fail</td><td>PR blocked</td><td>Update view per new schema</td></tr><tr><td><code>obs.compliance_view_accessed</code> emit fails</td><td>memory_writer error</td><td>View still served; sev-1 for missing audit</td><td>Operator investigates memory</td></tr><tr><td>Auditor JWT leaked</td><td>unknown access</td><td>sev-1; rotate JWT</td><td>Standard incident response</td></tr><tr><td>Wrong audit row included in view</td><td>view test asserts</td><td>PR blocked</td><td>Update view filter</td></tr><tr><td>Wrong audit row excluded from view</td><td>manual auditor review</td><td>Update view to include</td><td>Standard fix</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The 4 views map 1:1 to the most-common audit regulations. Adding a 5th (PCI DSS) is TASK-OBS-010 placeholder.</li><li>Hash-chain proof (Ed25519) is the differentiator vs SaaS observability — provably immutable, auditor-verifiable independently.</li><li>Read-only by construction — compliance views derive from the chain; mutating would defeat the purpose.</li><li>Per-engagement auditor JWT (30-day TTL) covers typical engagement; rotation prevents stale access.</li><li>Tenant-scoping at the query layer (RLS) + 403 on tenant_id query param = defense in depth.</li><li>PII placeholders in audit chain (TASK-AUTH-002 + TASK-AI-014) + regex scan at view-time = defense in depth.</li><li>365-day max window is conservative; larger requires explicit task amendment.</li><li>The auditor self-audit (<code>obs.compliance_view_accessed</code>) means even compliance reviews of the auditor have evidence.</li><li>PDF export uses wkhtmltopdf — battle-tested; alternative weasyprint (Python) also viable. Pin version.</li></ul>\n<hr>\n<p><em>End of TASK-OBS-008. Status: draft (10/10 target).</em></p>";
