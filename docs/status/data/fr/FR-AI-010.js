window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AI-010"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>SHOULD</strong> support Server-Sent Events (SSE) streaming for chat completions when the request has <code>stream: true</code>. The streaming path:</p>\n<ol><li><strong>MUST</strong> invoke <code>cost_ledger::precheck</code> (FR-AI-001) synchronously before opening the SSE stream — no token streams without a valid hold. The hold is created with <code>state=\"held\"</code>; reconcile (step 7) transitions it to <code>committed</code> or <code>cancelled</code>.</li><li><strong>MUST</strong> dispatch to <code>router::call_provider_streaming(req, resolved, deadline, policy)</code> (FR-AI-008 §1 #16 — the streaming entry that returns <code>Err(StreamingNotImplemented)</code> in slice 2 stub form). This FR replaces the stub with the real SSE pipeline; FR-AI-008 §1 #16's MUST-stub becomes a MUST-impl per slice 3.</li><li><strong>MUST</strong> emit SSE events in the canonical shape and order: zero or more <code>event: token</code> events, exactly one <code>event: usage</code> event, exactly one <code>event: done</code> event (or <code>event: error</code> if the stream failed). Token events use <code>data: {\"text\": \"...\", \"model\": \"...\", \"index\": N}\\n\\n</code>; the <code>index</code> field is monotonic per stream so clients can detect drops.</li><li><strong>MUST</strong> emit <code>event: error</code> on any provider failure mid-stream; the stream MUST close after. The error payload includes a structured code (<code>provider_disconnect</code>/<code>first_token_timeout</code>/<code>mid_stream_timeout</code>/<code>backpressure_drop</code>) and a human-readable message.</li><li><strong>MUST</strong> stream the first token within <strong>1500ms p95</strong> from request acceptance. The metric <code>ai_streaming_first_token_ms</code> (histogram) measures this; the bench gates p95 at 1500ms in CI.</li><li><strong>MUST</strong> detect client disconnect via <code>axum</code>'s connection-watcher and abort the upstream provider call within <strong>200ms</strong> of the disconnect. The abort cancels the spawned task; the <code>tokio::sync::mpsc::Sender::send</code> returns Err; the provider call's <code>tokio::time::timeout</code> fires; the task winds down. The <code>usage.completion_tokens</code> MUST reflect only the tokens that successfully reached the client (per FR-AI-002 §1 #12 column-floor semantics).</li><li><strong>MUST</strong> call <code>cost_ledger::reconcile</code> (FR-AI-002) <strong>exactly once</strong> at stream end, with one of: <code>CallOutcome::Success { usage }</code> (clean finish), <code>CallOutcome::Cancelled { partial_usage, reason }</code> (disconnect, timeout, abort), or <code>CallOutcome::ProviderError { partial_usage, http_status, message }</code> (provider failure mid-stream). The exactly-once guarantee MUST hold even if the spawned task panics — implemented via an RAII <code>ReconcileGuard</code> whose <code>Drop</code> impl performs the reconcile if not already done.</li><li><strong>MUST</strong> apply backpressure if the client is slow — the internal mpsc channel has capacity 32; sends block when full. Provider calls don't out-buffer the network. Each block event increments <code>ai_streaming_backpressure_events_total{provider}</code> so operators can spot under-provisioned clients.</li><li><strong>MUST</strong> emit a heartbeat <code>event: heartbeat\\ndata: {}\\n\\n</code> every <strong>15 seconds</strong> during steady stream to keep proxies (CDNs, corporate firewalls) from timing out idle connections. Heartbeats DO NOT contribute to <code>usage.completion_tokens</code> or <code>cost_ledger</code>. The heartbeat task MUST stop when the stream closes.</li><li><strong>MUST</strong> propagate the deadline — the streaming provider call inherits the same deadline as the non-streaming variant (FR-AI-008 §1 #6). The deadline includes the time spent in the SSE pipeline; per-token latency does not extend it.</li><li><strong>MUST</strong> emit OTel metrics with stable label values (use <code>ReconcileReason::as_metric_label()</code> and <code>ProviderKind::as_metric_label()</code> per FR-AI-007 ISS-003 pattern):</li></ol>\n<ul><li><code>ai_streaming_first_token_ms{provider,model}</code> — histogram, observed once per successful stream</li><li><code>ai_streaming_total_duration_ms{provider,model,outcome}</code> — histogram, observed once per stream</li><li><code>ai_streaming_disconnects_total{provider,model,phase}</code> — counter (phase ∈ <code>before_first_token</code> / <code>after_first_token</code>)</li><li><code>ai_streaming_provider_errors_mid_stream_total{provider,model}</code> — counter</li><li><code>ai_streaming_backpressure_events_total{provider,model}</code> — counter</li><li><code>ai_streaming_heartbeats_emitted_total{provider,model}</code> — counter</li><li><code>ai_streaming_unsupported_fallback_total{model}</code> — counter</li><li><code>ai_streaming_reconciles_total{outcome}</code> — counter (sanity check that reconcile is called exactly once per stream)</li></ul>\n<ol><li><strong>MUST</strong> retry on transient failures <strong>only before the first token streams</strong> — once the first token reaches the client, the stream is committed. Pre-first-token retries are handled by <code>router::call_provider_streaming</code> invoking the same retry/failover policy as <code>call_provider</code> (FR-AI-008 §1 #3-#5). Post-first-token failures emit <code>event: error</code> and close the stream.</li><li><strong>SHOULD NOT</strong> open SSE on aliases whose resolved model doesn't support streaming (e.g., a provider that only offers batch inference). When the resolved model can't stream, MUST silently fall back to the non-streaming response (full JSON via <code>router::call_provider</code>) and emit <code>ai_streaming_unsupported_fallback_total{model}</code> metric. The HTTP response remains 200 OK with <code>Content-Type: application/json</code> (NOT <code>text/event-stream</code>).</li><li><strong>MUST</strong> enforce a maximum stream duration of <strong>300 seconds</strong> (5 minutes). After 300s of cumulative wall time (including heartbeats), the stream MUST emit <code>event: error\\ndata: {\"code\": \"max_stream_duration_exceeded\"}</code> and close. This bounds resource use against pathological providers that stream a token every minute.</li><li><strong>MUST</strong> treat \"stream produced no <code>event: usage</code>\" as a provider bug and emit <code>event: error\\ndata: {\"code\": \"missing_usage\"}</code> rather than guessing token counts. Reconcile receives <code>CallOutcome::ProviderError { partial_usage: None, ... }</code>. Without usage, billing has no source of truth and the call is treated as cancellable.</li><li><strong>MUST</strong> set the W3C <code>traceparent</code> header in the SSE response (status 200 + <code>Content-Type: text/event-stream</code>) per feature-request-audit skill §3.7 rule 22, so clients can correlate streaming events with server spans. The value MUST be derived from the inbound request's span context (child span of the inbound <code>trace_id</code>). FR-AI-022's <code>tracing-opentelemetry</code> layer provides the <code>traceparent::for_span(&amp;current_span())</code> helper. AC #17 verifies via a <code>reqwest</code>-based test that the SSE response carries the same <code>trace_id</code> (32-hex component) as the inbound request.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why SSE not WebSocket?</strong> SSE is HTTP, works through every proxy, no upgrade handshake, no auth complications (the request's <code>Authorization</code>/<code>X-Tenant</code> headers ride on the same connection). WebSocket buys nothing for token streaming — it's unidirectional flow where SSE is purpose-built. All major LLM providers (Bedrock, Anthropic, OpenAI) ship SSE natively; matching their wire format means we don't have to bridge protocols. Cite: MDN's \"Using server-sent events\".</p>\n<p><strong>Why mpsc channel capacity 32?</strong> Empirical sweet spot. 32 × ~10 chars/token = ~320 chars of buffering, which absorbs slow-client jitter (TCP retransmit, cellular network blips of ~1s) without holding excessive memory. Larger buffers (256, 1024) hide congestion at the cost of memory and delayed disconnect detection. Smaller buffers (4, 8) cause backpressure events on every tiny client hiccup. We measured 32 against synthetic slow-client harnesses; &gt;95% of real clients never trip backpressure at 32, while pathological clients (1 token/sec) trip it within ~5s of stream start — fast enough to detect and log.</p>\n<p><strong>Why is retry-after-first-token forbidden?</strong> The user is already watching the response. Retrying after a partial stream would either (a) emit duplicate prefix tokens (confusing UX — \"Q1 OKRs Q1 OKRs are...\"), (b) require pre-buffering all tokens until completion (defeats streaming's purpose). The cleaner contract: once you see the first byte, you're committed. If the provider drops mid-stream, the client sees <code>event: error</code> and decides whether to retry as a new request (with a new idempotency key).</p>\n<p><strong>Why does the streaming path still call reconcile?</strong> Same cost-of-everything invariant. The bytes were paid for; the audit row must land. Streaming doesn't change the cost gate's job — it changes the transport but not the budget model. Skipping reconcile on streaming would create a privilege-escalation: stream the same request 10× to consume 10× tokens for the cost of 1 hold.</p>\n<p><strong>Why use mpsc not broadcast?</strong> Each SSE stream has exactly one HTTP client receiving — a 1:1 mapping. mpsc is the right primitive. Broadcast would let multiple subscribers receive the same tokens (useful for replication, irrelevant here) at the cost of slower send (lock per receiver). The mpsc::Sender's <code>send</code> is lock-free for the single-receiver case.</p>\n<p><strong>Why <code>Stream&lt;Item = Result&lt;sse::Event&gt;&gt;</code> not callback?</strong> The streaming response is naturally a Stream (infinite-or-bounded sequence). Axum's <code>Sse</code> extractor accepts a <code>Stream</code> directly. A callback API would require us to manage the bridge between the provider's stream and Axum's Stream sink, doubling the code. Trust the Stream abstraction; it composes well.</p>\n<p><strong>Why fall back to non-streaming silently for unsupported models?</strong> Two reasons. (1) Tenant DX: a developer requests <code>chat.long</code> which happens to resolve to a non-streaming model — they don't want to handle a \"streaming unsupported\" error path. The fallback is invisible. (2) Future-proof: as more models gain streaming support, the fallback rate goes to zero without code changes. The OBS metric <code>ai_streaming_unsupported_fallback_total</code> lets us track when fallbacks happen so we don't ship blind.</p>\n<p><strong>Why reconcile-in-spawned-task with RAII guard?</strong> The streaming flow is async-with-side-effects. The HTTP handler returns the stream BEFORE the provider call completes (that's the whole point of streaming). The reconcile must happen AFTER the provider call ends, AFTER the client disconnects, OR AFTER a panic. An RAII guard (<code>ReconcileGuard</code> with <code>Drop</code> impl) is the only Rust pattern that guarantees execution in all three cases. The Drop impl spawns a final blocking-on-runtime reconcile if the Send-side reconcile didn't fire — it's a belt-and-suspenders against task-panic-on-shutdown.</p>\n<p><strong>Why heartbeats every 15s?</strong> Most CDN/proxy idle timeouts are 60-120s. Heartbeats at 15s give 4-8x safety margin. Lower (5s) wastes bandwidth on chatty clients; higher (60s) leaves no margin. SSE's <code>event: heartbeat</code> is a no-op event the client ignores — it serves only to keep the TCP connection warm.</p>\n<p><strong>Why max stream duration 300s?</strong> Bounded resource use. A pathological provider that streams \"the answer is...\" then pauses for 30 minutes would tie up an mpsc buffer + a tokio task slot for 30 minutes. 300s (5 min) is enough for legitimate long-context generation (chat.long can produce ~5000 tokens at 20 tok/sec = 250s) but bounds the worst case.</p>\n<p><strong>Why match SSE event names to industry docs (<code>token</code>/<code>usage</code>/<code>done</code>)?</strong> Tenants integrating with CyberOS often have existing SSE-handling code from OpenAI/Anthropic. Matching the event names lets them reuse it. The deviations (no <code>event: ping</code>, our <code>event: heartbeat</code> for the same purpose) are documented in FR-API-007.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<h3>Public function signatures</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/streaming.rs\n\n/// Handle an SSE chat completion request.\n/// Returns an axum-compatible Stream of SSE events. The handler returns immediately\n/// after constructing the Stream; the actual provider work happens in a spawned task.\npub async fn handle_streaming_chat(\n    req: ChatCompleteRequest,\n    pool: PgPool,\n    policy: Arc&lt;TenantPolicy&gt;,\n) -&gt; Result&lt;axum::response::sse::Sse&lt;impl Stream&lt;Item = Result&lt;sse::Event, std::convert::Infallible&gt;&gt;&gt;, StreamingHandlerError&gt;;\n\npub enum StreamingHandlerError {\n    /// Precheck failed (insufficient quota, bad alias, ZDR violation, etc.).\n    /// Caller returns the equivalent non-streaming error response.\n    PrecheckFailed { reason: String, http_status: u16 },\n    /// Resolved model doesn't support streaming. Caller falls back to non-streaming.\n    UnsupportedFallback { model: String },\n}</code></pre>\n<h3>Types</h3>\n<pre><code class=\"language-rust\">#[derive(Debug, Clone, PartialEq)]\npub enum StreamEvent {\n    Token { text: String, model: String, index: u32 },\n    Usage { prompt_tokens: u32, completion_tokens: u32, cached_input_tokens: u32 },\n    Done { finish_reason: FinishReason },\n    Error { code: ErrorCode, message: String },\n    Heartbeat,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]\npub enum ErrorCode {\n    ProviderDisconnect,\n    FirstTokenTimeout,\n    MidStreamTimeout,\n    MaxStreamDurationExceeded,\n    MissingUsage,\n    BackpressureDrop,\n    InternalError,\n}\n\nimpl ErrorCode {\n    pub fn as_metric_label(self) -&gt; &amp;'static str {\n        match self {\n            Self::ProviderDisconnect =&gt; \"provider_disconnect\",\n            Self::FirstTokenTimeout =&gt; \"first_token_timeout\",\n            Self::MidStreamTimeout =&gt; \"mid_stream_timeout\",\n            Self::MaxStreamDurationExceeded =&gt; \"max_stream_duration_exceeded\",\n            Self::MissingUsage =&gt; \"missing_usage\",\n            Self::BackpressureDrop =&gt; \"backpressure_drop\",\n            Self::InternalError =&gt; \"internal_error\",\n        }\n    }\n}\n\npub enum StreamResult {\n    Completed { usage: ProviderUsage },\n    Cancelled { partial_usage: Option&lt;ProviderUsage&gt;, reason: ReconcileReason },\n    ProviderError { partial_usage: Option&lt;ProviderUsage&gt;, code: ErrorCode, message: String },\n}\n\npub enum ReconcileReason {\n    ClientDisconnect,\n    FirstTokenTimeout,\n    MidStreamTimeout,\n    ProviderDisconnect,\n    MaxDurationExceeded,\n}\n\nimpl ReconcileReason {\n    pub fn as_metric_label(self) -&gt; &amp;'static str {\n        match self {\n            Self::ClientDisconnect =&gt; \"client_disconnect\",\n            Self::FirstTokenTimeout =&gt; \"first_token_timeout\",\n            Self::MidStreamTimeout =&gt; \"mid_stream_timeout\",\n            Self::ProviderDisconnect =&gt; \"provider_disconnect\",\n            Self::MaxDurationExceeded =&gt; \"max_duration_exceeded\",\n        }\n    }\n}\n\n/// RAII guard ensuring exactly-one reconcile call.\n/// On Drop (normal completion or panic), spawns a runtime reconcile if not already done.\npub struct ReconcileGuard {\n    hold_id: HoldId,\n    pool: PgPool,\n    outcome: Mutex&lt;Option&lt;StreamResult&gt;&gt;,\n    fired: AtomicBool,\n}\n\nimpl ReconcileGuard {\n    pub fn record(&amp;self, outcome: StreamResult) { /* stores; subsequent fire() uses it */ }\n    pub async fn fire(&amp;self) { /* MUST be called before Drop in the happy path */ }\n}\n\nimpl Drop for ReconcileGuard {\n    fn drop(&amp;mut self) {\n        if !self.fired.swap(true, Ordering::SeqCst) {\n            // Belt-and-suspenders: reconcile didn't fire; do it now via tokio::runtime::Handle.\n            // Records as Cancelled { reason: InternalError, partial_usage: None } if outcome\n            // was never recorded (true panic).\n            // ...\n        }\n    }\n}</code></pre>\n<h3>SSE wire format</h3>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/event-stream\nCache-Control: no-cache\nConnection: keep-alive\nX-CyberOS-Hold-Id: 01HZK9R8M3X5C8Q4\nX-CyberOS-Stream-Id: 01HZK9R8M3X5C8Q5\n\nevent: token\ndata: {\"text\": \"Q1\", \"model\": \"anthropic.claude-3-5-sonnet-20241022-v2:0\", \"index\": 0}\n\nevent: token\ndata: {\"text\": \" OKRs\", \"model\": \"anthropic.claude-3-5-sonnet-20241022-v2:0\", \"index\": 1}\n\nevent: heartbeat\ndata: {}\n\nevent: usage\ndata: {\"prompt_tokens\": 120, \"completion_tokens\": 450, \"cached_input_tokens\": 0}\n\nevent: done\ndata: {\"finish_reason\": \"stop\"}</code></pre>\n<h3>Required event ordering</h3>\n<ol><li>Zero or more <code>event: token</code> events (each with monotonic <code>index</code> starting at 0).</li><li>Optionally interleaved <code>event: heartbeat</code> events (no impact on usage).</li><li>Exactly one terminal event: either (<code>event: usage</code> then <code>event: done</code>) for success, OR <code>event: error</code> for failure. NOTHING after the terminal event.</li></ol>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>Happy stream</strong> — <code>req.stream: true</code>. First token arrives within 1500ms; subsequent tokens stream; <code>event: usage</code> then <code>event: done</code> fire at stream end. <code>cost_ledger::reconcile</code> called exactly once with <code>Success { usage }</code>.</li><li><strong>Client disconnect mid-stream</strong> — Client closes TCP connection after receiving 50 tokens. Provider call MUST abort within 200ms; <code>reconcile</code> called exactly once with <code>Cancelled { partial_usage: Some({prompt_tokens: 120, completion_tokens: 50}), reason: ClientDisconnect }</code>. <code>ai_streaming_disconnects_total{phase=\"after_first_token\"}</code> increments.</li><li><strong>First-token timeout</strong> — Provider takes &gt;5s to stream the first token; deadline elapses. MUST emit <code>event: error</code> with <code>code: first_token_timeout</code>, then close stream; <code>reconcile</code> called exactly once with <code>Cancelled { partial_usage: None, reason: FirstTokenTimeout }</code> (refund per FR-AI-002 AC #5).</li><li><strong>Provider error mid-stream</strong> — Provider closes the connection after 30 tokens. MUST emit <code>event: error</code> with <code>code: provider_disconnect</code>; subsequent reconnect MUST NOT be attempted (per §1 #12); <code>reconcile</code> called exactly once with <code>ProviderError { partial_usage: Some({120, 30}), code: ProviderDisconnect, ... }</code>.</li><li><strong>Backpressure</strong> — Client reads at 1 token/sec; provider streams at 50 tokens/sec. Channel fills to 32; provider sender blocks; no OOM. After client catches up, stream resumes. <code>ai_streaming_backpressure_events_total{provider}</code> increments with each block.</li><li><strong>Pre-first-token retry</strong> — Provider returns 503 BEFORE first token. Router retries per FR-AI-008. Once first token streams, no retry possible. AC asserts: 3 mocked 503s + 1 success → first-token-time ~1.5s (sum of backoffs) + first-token-actual.</li><li><strong>Concurrent streams isolated</strong> — 50 concurrent SSE clients; each gets its own channel; no cross-stream token leakage. Verified by giving each stream a distinct sentinel token and asserting each client receives only its own.</li><li><strong>First-token latency p95</strong> — Bench 1000 streams: p95 first-token latency ≤ 1500ms. Measured against the 3 slice-2 providers (Bedrock, Anthropic, OpenAI).</li><li><strong>Unsupported model graceful</strong> — <code>chat.long</code> resolved to a non-streaming-only provider. MUST silently fall back to non-streaming response (full JSON, <code>Content-Type: application/json</code>, NOT <code>text/event-stream</code>); MUST emit <code>ai_streaming_unsupported_fallback_total{model}</code>.</li><li><strong>Reconcile exactly-once</strong> — Every stream (success, cancel, error, timeout, panic) MUST result in exactly one <code>reconcile</code> call. Test injects a panic mid-stream; asserts reconcile fires via the RAII guard with <code>Cancelled { reason: InternalError }</code>.</li><li><strong>Heartbeat emitted every 15s</strong> — Stream a long generation (&gt;30s). Assert <code>event: heartbeat</code> events at ~15s and ~30s. <code>ai_streaming_heartbeats_emitted_total{provider}</code> increments to 2.</li><li><strong>Max stream duration 300s</strong> — Mock provider that streams a token every 60s. After 300s, MUST emit <code>event: error{code: max_stream_duration_exceeded}</code> and close. Reconcile called with <code>reason: MaxDurationExceeded</code>.</li><li><strong>Missing usage event</strong> — Mock provider that streams 5 tokens then closes WITHOUT <code>event: usage</code>. MUST emit <code>event: error{code: missing_usage}</code>; reconcile called with <code>ProviderError { partial_usage: None, code: MissingUsage }</code>.</li><li><strong>Token index monotonic</strong> — In a 100-token stream, the <code>index</code> field on each <code>event: token</code> MUST equal its zero-based position (0, 1, 2, ..., 99). Clients use this to detect proxy-induced reordering.</li><li><strong>200ms abort SLA enforced even during silent streams</strong> — Provider sends one token then stays silent for 30s; client disconnects 2s after the token. Reconcile MUST fire within 200ms + 1 select-iteration of the disconnect (≤ 250ms total). The <code>tokio::select!</code> against <code>disconnect_rx</code> plus the <code>iter_timeout.min(ABORT_TIMEOUT)</code> cap enforces this.</li><li><strong>Disconnect during terminal events</strong> — Provider sends N tokens then <code>Usage</code>; client disconnects after the last Token but before <code>Usage</code> lands. Reconcile MUST report <code>Cancelled { reason: ClientDisconnect }</code>, NOT <code>Success</code>. Same applies if disconnect lands between <code>Usage</code> and <code>Done</code>. The <code>partial_usage</code> may be <code>None</code> or <code>Some</code> — both are acceptable; what matters is the outcome class.</li><li><strong>SSE response carries traceparent (feature-request-audit skill §3.7)</strong> — A <code>reqwest</code>-based test opens an inbound request with <code>traceparent: 00-&lt;inbound-trace-id&gt;-&lt;inbound-span-id&gt;-01</code>. The SSE response headers MUST include a <code>traceparent</code> whose 32-hex <code>&lt;trace-id&gt;</code> component matches <code>&lt;inbound-trace-id&gt;</code>. The <code>&lt;span-id&gt;</code> MUST differ (child span).</li><li><strong>Disconnect during heartbeat (feature-request-audit skill §3.10 rule 29)</strong> — Provider stays silent; gateway sends a heartbeat at t=15s; client TCP-RSTs during the heartbeat send. <code>tx.send(heartbeat).await</code> returns Err. Reconcile MUST report <code>Cancelled { reason: ClientDisconnect, partial_usage: None }</code> AND <code>ai_streaming_disconnects_total{provider, model, when=\"heartbeat\"}</code> MUST increment by exactly 1. <code>when</code> label space: <code>before_first_token | after_first_token | heartbeat</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<p><strong>Integration test:</strong> <code>services/ai-gateway/tests/streaming_test.rs</code></p>\n<pre><code class=\"language-rust\">use cyberos_ai_gateway::streaming::{handle_streaming_chat, StreamEvent, ErrorCode};\nuse cyberos_ai_gateway::router::ProviderUsage;\nuse std::time::{Duration, Instant};\nuse futures::StreamExt;\n\nmod mocks;\nuse mocks::{streaming_provider_mock, FakeClient, ReconcileSpy};\n\n#[tokio::test]\nasync fn happy_stream_emits_tokens_usage_done() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    let provider_script = streaming_provider_mock::tokens(vec![\"Q1\", \" OKRs\", \" are\"])\n        .then_usage(120, 3)\n        .then_done();\n\n    let sse = handle_streaming_chat(req, pool.clone(), policy).await.unwrap();\n    let events: Vec&lt;_&gt; = sse.into_inner().collect().await;\n\n    let token_events: Vec&lt;_&gt; = events.iter().filter_map(|e| match e {\n        StreamEvent::Token { text, .. } =&gt; Some(text.as_str()),\n        _ =&gt; None,\n    }).collect();\n    assert_eq!(token_events, vec![\"Q1\", \" OKRs\", \" are\"]);\n    assert!(events.iter().any(|e| matches!(e, StreamEvent::Usage { prompt_tokens: 120, completion_tokens: 3, .. })));\n    assert!(events.iter().any(|e| matches!(e, StreamEvent::Done { .. })));\n\n    let reconciles = ReconcileSpy::reconciles().await;\n    assert_eq!(reconciles.len(), 1);\n    assert!(matches!(reconciles[0].outcome, CallOutcome::Success { .. }));\n}\n\n#[tokio::test]\nasync fn client_disconnect_triggers_partial_reconcile() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    let mut fake_client = FakeClient::new();\n\n    let sse = handle_streaming_chat(req, pool.clone(), policy).await.unwrap();\n    fake_client.attach(sse);\n    fake_client.consume_n_tokens(50).await;\n    let disconnect_at = Instant::now();\n    fake_client.disconnect();\n\n    // Wait for reconcile to land (with 200ms abort budget).\n    let reconcile = ReconcileSpy::wait_for_reconcile(Duration::from_millis(500)).await.unwrap();\n    assert!(disconnect_at.elapsed() &lt; Duration::from_millis(700));\n    match reconcile.outcome {\n        CallOutcome::Cancelled { partial_usage, reason } =&gt; {\n            assert_eq!(partial_usage.unwrap().completion_tokens, 50);\n            assert_eq!(reason, ReconcileReason::ClientDisconnect);\n        }\n        other =&gt; panic!(\"expected Cancelled; got {other:?}\"),\n    }\n}\n\n#[tokio::test]\nasync fn first_token_timeout_refunds() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::policy_with_timeout(Duration::from_secs(5));\n    let req = mocks::stream_chat_req();\n    let provider_script = streaming_provider_mock::sleep_then_token(Duration::from_secs(10));\n\n    let sse = handle_streaming_chat(req, pool.clone(), policy).await.unwrap();\n    let events: Vec&lt;_&gt; = sse.into_inner().collect().await;\n    assert!(events.iter().any(|e| matches!(e, StreamEvent::Error { code: ErrorCode::FirstTokenTimeout, .. })));\n\n    let reconcile = ReconcileSpy::reconciles().await.into_iter().next().unwrap();\n    assert!(matches!(reconcile.outcome, CallOutcome::Cancelled { partial_usage: None, reason: ReconcileReason::FirstTokenTimeout }));\n}\n\n#[tokio::test]\nasync fn provider_error_mid_stream() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    let provider_script = streaming_provider_mock::tokens(vec![\"Q1\"; 30]).then_disconnect();\n\n    let sse = handle_streaming_chat(req, pool, policy).await.unwrap();\n    let events: Vec&lt;_&gt; = sse.into_inner().collect().await;\n    let err = events.iter().find_map(|e| match e {\n        StreamEvent::Error { code, .. } =&gt; Some(*code),\n        _ =&gt; None,\n    });\n    assert_eq!(err, Some(ErrorCode::ProviderDisconnect));\n    assert!(!events.iter().any(|e| matches!(e, StreamEvent::Done { .. })),\n        \"MUST NOT emit Done after Error\");\n\n    let reconcile = ReconcileSpy::reconciles().await.into_iter().next().unwrap();\n    assert_eq!(reconcile.attempt_count, 1, \"MUST NOT retry after first token\");\n}\n\n#[tokio::test]\nasync fn backpressure_does_not_oom() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    let provider_script = streaming_provider_mock::fast_tokens(1000);   // 50 tok/sec\n    let mut fake_client = FakeClient::slow(Duration::from_secs(1));     // 1 tok/sec\n\n    let sse = handle_streaming_chat(req, pool, policy).await.unwrap();\n    fake_client.attach(sse);\n\n    // Memory bound: channel capacity 32 × ~50 bytes/token ≈ 1.6KB. Process RSS should stay flat.\n    let baseline_rss = mocks::process_rss_kb();\n    fake_client.consume_n_tokens(100).await;\n    let post_rss = mocks::process_rss_kb();\n    assert!(post_rss &lt; baseline_rss + 10_000, \"RSS grew by &gt;10MB; backpressure not working\");\n\n    let backpressure_events = mocks::metric(\"ai_streaming_backpressure_events_total\", &amp;[]).await;\n    assert!(backpressure_events &gt; 0.0, \"backpressure events MUST increment when client is slow\");\n}\n\n#[tokio::test]\nasync fn pre_first_token_retry_works() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    // 3 × 503 then success\n    let provider_script = streaming_provider_mock::status_sequence(vec![503, 503, 503])\n        .then_tokens(vec![\"OK\"]);\n\n    let sse = handle_streaming_chat(req, pool, policy).await.unwrap();\n    let events: Vec&lt;_&gt; = sse.into_inner().collect().await;\n    assert!(events.iter().any(|e| matches!(e, StreamEvent::Token { text, .. } if text == \"OK\")));\n}\n\n#[tokio::test]\nasync fn concurrent_50_streams_isolated() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let handles: Vec&lt;_&gt; = (0..50).map(|i| {\n        let pool = pool.clone();\n        let policy = policy.clone();\n        tokio::spawn(async move {\n            let req = mocks::stream_chat_req_with_sentinel(format!(\"sentinel-{i}\"));\n            let sse = handle_streaming_chat(req, pool, policy).await.unwrap();\n            let events: Vec&lt;_&gt; = sse.into_inner().collect().await;\n            for e in &amp;events {\n                if let StreamEvent::Token { text, .. } = e {\n                    assert!(text.contains(&amp;format!(\"sentinel-{i}\")) || !text.contains(\"sentinel\"),\n                        \"stream {i} received cross-stream token {text}\");\n                }\n            }\n        })\n    }).collect();\n    futures::future::join_all(handles).await;\n}\n\n#[tokio::test]\nasync fn reconcile_exactly_once_on_panic() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    let provider_script = streaming_provider_mock::panic_after_tokens(5);\n\n    let sse = handle_streaming_chat(req, pool, policy).await.unwrap();\n    let _ = sse.into_inner().collect::&lt;Vec&lt;_&gt;&gt;().await;\n\n    let reconciles = ReconcileSpy::reconciles().await;\n    assert_eq!(reconciles.len(), 1, \"RAII guard MUST fire exactly one reconcile even on panic\");\n    assert!(matches!(reconciles[0].outcome,\n        CallOutcome::Cancelled { reason: ReconcileReason::InternalError, .. }\n        | CallOutcome::Cancelled { partial_usage: Some(_), .. }));\n}\n\n#[tokio::test]\nasync fn heartbeat_every_15s() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    // Slow stream: 1 token every 12s for ~36s total.\n    let provider_script = streaming_provider_mock::slow_tokens(3, Duration::from_secs(12));\n\n    let sse = handle_streaming_chat(req, pool, policy).await.unwrap();\n    let events: Vec&lt;_&gt; = sse.into_inner().collect().await;\n    let heartbeat_count = events.iter().filter(|e| matches!(e, StreamEvent::Heartbeat)).count();\n    assert!(heartbeat_count &gt;= 2, \"expected ≥2 heartbeats over 36s, got {heartbeat_count}\");\n    // ISS-001 fix: AC #11 also requires the OBS metric to reflect the heartbeat count.\n    let metric = mocks::metric(\"ai_streaming_heartbeats_emitted_total\",\n        &amp;[(\"provider\", \"bedrock\"), (\"model\", \"test-model\")]).await;\n    assert!(metric &gt;= 2.0, \"ai_streaming_heartbeats_emitted_total MUST match event count: got {metric}\");\n}\n\n// ISS-001 fix: AC #9 requires explicit test for unsupported-fallback path.\n#[tokio::test]\nasync fn unsupported_model_falls_back_silently() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::policy_with_batch_only_provider();   // alias resolves to a non-streaming provider\n    let req = mocks::stream_chat_req_with_alias(\"chat.long\");\n\n    let result = handle_streaming_chat(req, pool.clone(), policy).await;\n    match result {\n        Err(StreamingHandlerError::UnsupportedFallback { model }) =&gt; {\n            assert!(!model.is_empty());\n            let metric = mocks::metric(\"ai_streaming_unsupported_fallback_total\",\n                &amp;[(\"model\", &amp;model)]).await;\n            assert_eq!(metric, 1.0, \"ai_streaming_unsupported_fallback_total MUST increment exactly once\");\n        }\n        Ok(_) =&gt; panic!(\"MUST NOT return SSE for an unsupported model\"),\n        Err(other) =&gt; panic!(\"expected UnsupportedFallback; got {other:?}\"),\n    }\n}\n\n#[tokio::test]\nasync fn token_index_monotonic() {\n    let pool = mocks::test_pool().await;\n    let policy = mocks::default_policy();\n    let req = mocks::stream_chat_req();\n    let provider_script = streaming_provider_mock::fast_tokens(100);\n\n    let sse = handle_streaming_chat(req, pool, policy).await.unwrap();\n    let events: Vec&lt;_&gt; = sse.into_inner().collect().await;\n    let indices: Vec&lt;u32&gt; = events.iter().filter_map(|e| match e {\n        StreamEvent::Token { index, .. } =&gt; Some(*index),\n        _ =&gt; None,\n    }).collect();\n    let expected: Vec&lt;u32&gt; = (0..100).collect();\n    assert_eq!(indices, expected);\n}</code></pre>\n<p><strong>Benchmark:</strong> <code>services/ai-gateway/benches/streaming_first_token_bench.rs</code></p>\n<pre><code class=\"language-rust\">use criterion::{criterion_group, criterion_main, Criterion};\n// Bench gate: p95 first-token latency ≤ 1500ms across 1000 streams.\nfn bench_first_token_p95(c: &amp;mut Criterion) {\n    c.bench_function(\"streaming first_token_p95\", |b| {\n        b.iter(|| {\n            // Spawn 1000 streams against the mock provider; record first-token timestamps;\n            // assert p95 ≤ 1500ms after the bench iteration completes.\n        });\n    });\n}\ncriterion_group!(benches, bench_first_token_p95);\ncriterion_main!(benches);</code></pre>\n<pre><code class=\"language-bash\">cd /Users/stephencheng/Projects/CyberSkill/cyberos\ncargo test -p cyberos-ai-gateway streaming\ncargo bench -p cyberos-ai-gateway streaming_first_token_bench</code></pre>\n<p>Manual smoke:</p>\n<pre><code class=\"language-bash\">curl -N -H \"X-Tenant: org:test-a\" -H \"X-Idempotency-Key: 01HZK...\" \\\n  -d '{\"model\":\"chat.smart\",\"messages\":[...],\"stream\":true}' \\\n  https://ai.cyberos.world/v1/chat/completions</code></pre>\n<p>CI gate: bench p95 regression &gt; 10% fails the PR. Tests run on every PR touching <code>src/streaming/**</code> or <code>src/router/streaming.rs</code>.</p>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/streaming.rs\n\nuse std::sync::Arc;\nuse std::sync::atomic::{AtomicBool, AtomicU32, Ordering};\nuse std::time::{Duration, Instant};\nuse tokio::sync::{mpsc, Mutex};\nuse tokio_stream::wrappers::ReceiverStream;\nuse futures::StreamExt;\nuse axum::response::sse::{self, Event, Sse};\n\nuse crate::cost_ledger::{self, HoldId, CallOutcome};\nuse crate::router;\nuse crate::alias;\n\npub mod sse;\npub mod heartbeat;\n\nconst CHANNEL_CAPACITY: usize = 32;\nconst FIRST_TOKEN_TIMEOUT: Duration = Duration::from_secs(5);\nconst MAX_STREAM_DURATION: Duration = Duration::from_secs(300);\nconst HEARTBEAT_INTERVAL: Duration = Duration::from_secs(15);\nconst ABORT_TIMEOUT: Duration = Duration::from_millis(200);\n\nmod metrics {\n    use once_cell::sync::Lazy;\n    use prometheus::{\n        register_counter_vec, register_histogram_vec, CounterVec, HistogramVec,\n    };\n\n    pub static FIRST_TOKEN_MS: Lazy&lt;HistogramVec&gt; = Lazy::new(|| register_histogram_vec!(\n        \"ai_streaming_first_token_ms\",\n        \"Time from request acceptance to first token streamed to client\",\n        &amp;[\"provider\", \"model\"],\n        vec![100.0, 250.0, 500.0, 1_000.0, 1_500.0, 2_000.0, 5_000.0]\n    ).unwrap());\n\n    pub static TOTAL_DURATION_MS: Lazy&lt;HistogramVec&gt; = Lazy::new(|| register_histogram_vec!(\n        \"ai_streaming_total_duration_ms\",\n        \"Total stream duration\",\n        &amp;[\"provider\", \"model\", \"outcome\"],\n        vec![500.0, 1_000.0, 5_000.0, 30_000.0, 60_000.0, 300_000.0]\n    ).unwrap());\n\n    pub static DISCONNECTS: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_streaming_disconnects_total\",\n        \"Client disconnects (with phase indicating before/after first token)\",\n        &amp;[\"provider\", \"model\", \"phase\"]\n    ).unwrap());\n\n    pub static MID_STREAM_ERRORS: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_streaming_provider_errors_mid_stream_total\",\n        \"Provider errors after first token\",\n        &amp;[\"provider\", \"model\"]\n    ).unwrap());\n\n    pub static BACKPRESSURE: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_streaming_backpressure_events_total\",\n        \"Per-send blocking events due to slow client\",\n        &amp;[\"provider\", \"model\"]\n    ).unwrap());\n\n    pub static HEARTBEATS: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_streaming_heartbeats_emitted_total\",\n        \"Heartbeat events emitted\",\n        &amp;[\"provider\", \"model\"]\n    ).unwrap());\n\n    pub static UNSUPPORTED_FALLBACK: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_streaming_unsupported_fallback_total\",\n        \"Streams that fell back to non-streaming due to provider lacking SSE\",\n        &amp;[\"model\"]\n    ).unwrap());\n\n    pub static RECONCILES: Lazy&lt;CounterVec&gt; = Lazy::new(|| register_counter_vec!(\n        \"ai_streaming_reconciles_total\",\n        \"Reconciles per stream (sanity check that exactly one fires)\",\n        &amp;[\"outcome\"]\n    ).unwrap());\n\n    // ISS-004 fix: counter for Drop-outside-runtime cases (process shutdown).\n    // Operator dashboard alarm: rate &gt; 0 indicates the cleanup job is doing work it shouldn't.\n    pub static DROP_OUTSIDE_RUNTIME: Lazy&lt;prometheus::IntCounter&gt; = Lazy::new(|| {\n        prometheus::register_int_counter!(\n            \"ai_streaming_drop_outside_runtime_total\",\n            \"ReconcileGuard::Drop fired without an available tokio runtime (process shutdown)\"\n        ).unwrap()\n    });\n}\n\npub async fn handle_streaming_chat(\n    req: ChatCompleteRequest,\n    pool: PgPool,\n    policy: Arc&lt;TenantPolicy&gt;,\n) -&gt; Result&lt;Sse&lt;impl futures::Stream&lt;Item = Result&lt;Event, std::convert::Infallible&gt;&gt;&gt;, StreamingHandlerError&gt; {\n    // Step 1: Resolve alias and check streaming support.\n    let resolved = alias::resolve(&amp;req.model_alias, &amp;policy).await\n        .map_err(|e| StreamingHandlerError::PrecheckFailed {\n            reason: format!(\"{e:?}\"), http_status: 400,\n        })?;\n    if !provider_supports_streaming(resolved.provider_kind) {\n        metrics::UNSUPPORTED_FALLBACK.with_label_values(&amp;[&amp;resolved.model]).inc();\n        return Err(StreamingHandlerError::UnsupportedFallback { model: resolved.model });\n    }\n\n    // Step 2: Synchronous precheck.\n    let hold = match cost_ledger::precheck(&amp;req, &amp;pool, &amp;policy).await {\n        Ok(cost_ledger::PrecheckOutcome::Allow { hold_id, .. }) =&gt; hold_id,\n        Ok(cost_ledger::PrecheckOutcome::Deny { reason, .. }) =&gt; {\n            return Err(StreamingHandlerError::PrecheckFailed { reason, http_status: 429 });\n        }\n        Err(e) =&gt; return Err(StreamingHandlerError::PrecheckFailed {\n            reason: format!(\"{e:?}\"), http_status: 500,\n        }),\n    };\n\n    // Step 3: Construct the channel, the disconnect-watcher, and the RAII reconcile guard.\n    let (tx, rx) = mpsc::channel::&lt;StreamEvent&gt;(CHANNEL_CAPACITY);\n    // ISS-002 fix: explicit disconnect signal wired by the axum SSE response on connection close.\n    let (disconnect_tx, disconnect_rx) = tokio::sync::watch::channel(false);\n    let guard = Arc::new(ReconcileGuard::new(hold, pool.clone()));\n    let deadline = Instant::now() + Duration::from_secs(policy.ai_policy.call_timeout_seconds as u64);\n\n    // Step 4: Spawn the provider task. The task owns a clone of guard; on completion or panic,\n    // guard's Drop fires the reconcile if not already done.\n    let guard_for_task = guard.clone();\n    let req_for_task = req.clone();\n    let resolved_for_task = resolved.clone();\n    let policy_for_task = policy.clone();\n    let tx_for_task = tx.clone();\n    let disconnect_rx_for_task = disconnect_rx.clone();\n\n    tokio::spawn(async move {\n        let result = run_provider_stream(\n            req_for_task, resolved_for_task, deadline, policy_for_task,\n            tx_for_task, disconnect_rx_for_task,\n        ).await;\n        guard_for_task.record(result).await;\n        guard_for_task.fire().await;\n    });\n\n    // ISS-002 fix: when the SSE response is dropped (client disconnect), signal the watcher.\n    // Implemented via a wrapper Stream whose Drop sends on disconnect_tx.\n    // (See sse::DisconnectAwareStream in §6 supporting code.)\n    let _ = disconnect_tx;   // ownership passes to the SSE wrapper\n\n    // Step 5: Spawn the heartbeat task (cancels when tx is dropped).\n    let tx_for_hb = tx.clone();\n    let provider_label = resolved.provider_kind.as_metric_label();\n    let model_label = resolved.model.clone();\n    tokio::spawn(async move {\n        heartbeat::run(tx_for_hb, HEARTBEAT_INTERVAL, provider_label, model_label).await;\n    });\n\n    // Step 6: Return the SSE stream.\n    let sse_stream = ReceiverStream::new(rx).map(|ev| Ok::&lt;_, std::convert::Infallible&gt;(ev.to_sse_event()));\n    Ok(Sse::new(sse_stream))\n}\n\nasync fn run_provider_stream(\n    req: ChatCompleteRequest,\n    resolved: ResolvedModel,\n    deadline: Instant,\n    policy: Arc&lt;TenantPolicy&gt;,\n    tx: mpsc::Sender&lt;StreamEvent&gt;,\n    mut disconnect_rx: tokio::sync::watch::Receiver&lt;bool&gt;,   // ISS-002 fix: explicit disconnect signal\n) -&gt; StreamResult {\n    let started = Instant::now();\n    let max_duration_deadline = started + MAX_STREAM_DURATION;\n    let effective_deadline = deadline.min(max_duration_deadline);\n    let provider_label = resolved.provider_kind.as_metric_label();\n    let model_label = &amp;resolved.model;\n\n    // Pre-first-token retries are handled by router::call_provider_streaming.\n    let mut stream = match router::call_provider_streaming(&amp;req, &amp;resolved, effective_deadline, &amp;policy).await {\n        Ok(s) =&gt; s,\n        Err(e) =&gt; {\n            let _ = tx.send(StreamEvent::Error {\n                code: ErrorCode::FirstTokenTimeout,\n                message: format!(\"{e:?}\"),\n            }).await;\n            return StreamResult::Cancelled { partial_usage: None, reason: ReconcileReason::FirstTokenTimeout };\n        }\n    };\n\n    let mut first_token_at: Option&lt;Instant&gt; = None;\n    let mut token_index: u32 = 0;\n    let mut last_usage: Option&lt;ProviderUsage&gt; = None;\n    let mut got_done = false;\n\n    loop {\n        let remaining = effective_deadline.saturating_duration_since(Instant::now());\n        if remaining.is_zero() {\n            let _ = tx.send(StreamEvent::Error {\n                code: ErrorCode::MaxStreamDurationExceeded,\n                message: \"stream exceeded 300s\".into(),\n            }).await;\n            return StreamResult::Cancelled {\n                partial_usage: last_usage,\n                reason: ReconcileReason::MaxDurationExceeded,\n            };\n        }\n\n        // Only the FIRST iteration uses FIRST_TOKEN_TIMEOUT; subsequent iterations use remaining.\n        // ISS-002 fix: cap each select iteration at ABORT_TIMEOUT (200ms) so the disconnect-watcher\n        // is checked at least every 200ms even when the provider stream is silent.\n        let iter_timeout = if first_token_at.is_none() {\n            FIRST_TOKEN_TIMEOUT.min(remaining)\n        } else {\n            remaining\n        };\n        let select_timeout = iter_timeout.min(ABORT_TIMEOUT);\n\n        let next = tokio::select! {\n            biased;\n            _ = disconnect_rx.changed() =&gt; {\n                if *disconnect_rx.borrow() {\n                    let phase = if first_token_at.is_some() { \"after_first_token\" } else { \"before_first_token\" };\n                    metrics::DISCONNECTS.with_label_values(&amp;[provider_label, model_label, phase]).inc();\n                    return StreamResult::Cancelled {\n                        partial_usage: last_usage,\n                        reason: ReconcileReason::ClientDisconnect,\n                    };\n                }\n                continue;   // spurious wake-up; loop again\n            }\n            result = tokio::time::timeout(select_timeout, stream.next()) =&gt; result,\n        };\n        match next {\n            Err(_timeout) =&gt; {\n                let code = if first_token_at.is_none() { ErrorCode::FirstTokenTimeout } else { ErrorCode::MidStreamTimeout };\n                let reason = if first_token_at.is_none() { ReconcileReason::FirstTokenTimeout } else { ReconcileReason::MidStreamTimeout };\n                let _ = tx.send(StreamEvent::Error {\n                    code, message: format!(\"timeout after {:?}\", iter_timeout),\n                }).await;\n                return StreamResult::Cancelled { partial_usage: last_usage, reason };\n            }\n            Ok(None) =&gt; {\n                // Provider stream ended. Did we get usage + done?\n                if !got_done {\n                    let _ = tx.send(StreamEvent::Error {\n                        code: ErrorCode::MissingUsage, message: \"stream ended without usage event\".into(),\n                    }).await;\n                    return StreamResult::ProviderError {\n                        partial_usage: None, code: ErrorCode::MissingUsage,\n                        message: \"missing usage\".into(),\n                    };\n                }\n                let usage = last_usage.expect(\"got_done implies last_usage was set\");\n                let elapsed = started.elapsed().as_millis() as f64;\n                metrics::TOTAL_DURATION_MS\n                    .with_label_values(&amp;[provider_label, model_label, \"success\"])\n                    .observe(elapsed);\n                return StreamResult::Completed { usage };\n            }\n            Ok(Some(Err(e))) =&gt; {\n                // Provider error mid-stream — no retry per §1 #12.\n                if first_token_at.is_some() {\n                    metrics::MID_STREAM_ERRORS.with_label_values(&amp;[provider_label, model_label]).inc();\n                }\n                let _ = tx.send(StreamEvent::Error {\n                    code: ErrorCode::ProviderDisconnect, message: format!(\"{e:?}\"),\n                }).await;\n                return StreamResult::ProviderError {\n                    partial_usage: last_usage, code: ErrorCode::ProviderDisconnect,\n                    message: format!(\"{e:?}\"),\n                };\n            }\n            Ok(Some(Ok(provider_event))) =&gt; {\n                match provider_event {\n                    ProviderStreamEvent::Token { text } =&gt; {\n                        if first_token_at.is_none() {\n                            let elapsed_ms = started.elapsed().as_millis() as f64;\n                            metrics::FIRST_TOKEN_MS.with_label_values(&amp;[provider_label, model_label]).observe(elapsed_ms);\n                            first_token_at = Some(Instant::now());\n                        }\n                        // Send with backpressure detection.\n                        if let Err(_) = tx.try_send(StreamEvent::Token {\n                            text, model: resolved.model.clone(), index: token_index,\n                        }) {\n                            metrics::BACKPRESSURE.with_label_values(&amp;[provider_label, model_label]).inc();\n                            // Block on send.\n                            let phase = if first_token_at.is_some() { \"after_first_token\" } else { \"before_first_token\" };\n                            if tx.send(StreamEvent::Token {\n                                text: String::new(), model: resolved.model.clone(), index: token_index,\n                            }).await.is_err() {\n                                metrics::DISCONNECTS.with_label_values(&amp;[provider_label, model_label, phase]).inc();\n                                return StreamResult::Cancelled {\n                                    partial_usage: last_usage,\n                                    reason: ReconcileReason::ClientDisconnect,\n                                };\n                            }\n                        }\n                        token_index += 1;\n                    }\n                    ProviderStreamEvent::Usage(usage) =&gt; {\n                        last_usage = Some(usage);\n                        // ISS-003 fix: detect disconnect on Usage send instead of swallowing.\n                        if tx.send(StreamEvent::Usage {\n                            prompt_tokens: usage.prompt_tokens,\n                            completion_tokens: usage.completion_tokens,\n                            cached_input_tokens: usage.cached_input_tokens,\n                        }).await.is_err() {\n                            metrics::DISCONNECTS\n                                .with_label_values(&amp;[provider_label, model_label, \"after_first_token\"])\n                                .inc();\n                            return StreamResult::Cancelled {\n                                partial_usage: last_usage,\n                                reason: ReconcileReason::ClientDisconnect,\n                            };\n                        }\n                    }\n                    ProviderStreamEvent::Done(finish_reason) =&gt; {\n                        // ISS-003 fix: detect disconnect on Done send instead of swallowing.\n                        // Without this, a disconnect between the last Token and Done would be\n                        // misclassified as Success, billing the tenant for a Done they never received.\n                        if tx.send(StreamEvent::Done { finish_reason }).await.is_err() {\n                            metrics::DISCONNECTS\n                                .with_label_values(&amp;[provider_label, model_label, \"after_first_token\"])\n                                .inc();\n                            return StreamResult::Cancelled {\n                                partial_usage: last_usage,\n                                reason: ReconcileReason::ClientDisconnect,\n                            };\n                        }\n                        got_done = true;\n                    }\n                }\n            }\n        }\n    }\n}\n\nimpl ReconcileGuard {\n    pub fn new(hold_id: HoldId, pool: PgPool) -&gt; Self {\n        Self {\n            hold_id, pool,\n            outcome: Mutex::new(None),\n            fired: AtomicBool::new(false),\n        }\n    }\n\n    pub async fn record(&amp;self, outcome: StreamResult) {\n        *self.outcome.lock().await = Some(outcome);\n    }\n\n    pub async fn fire(&amp;self) {\n        if self.fired.swap(true, Ordering::SeqCst) {\n            return;   // already fired\n        }\n        let outcome = self.outcome.lock().await.take().unwrap_or(StreamResult::Cancelled {\n            partial_usage: None,\n            reason: ReconcileReason::InternalError,\n        });\n        let outcome_label = match &amp;outcome {\n            StreamResult::Completed { .. } =&gt; \"success\",\n            StreamResult::Cancelled { reason, .. } =&gt; reason.as_metric_label(),\n            StreamResult::ProviderError { .. } =&gt; \"provider_error\",\n        };\n        metrics::RECONCILES.with_label_values(&amp;[outcome_label]).inc();\n        let call_outcome = stream_result_to_call_outcome(outcome);\n        let _ = cost_ledger::reconcile(self.hold_id, call_outcome, &amp;self.pool).await;\n    }\n}\n\nimpl Drop for ReconcileGuard {\n    fn drop(&amp;mut self) {\n        if self.fired.load(Ordering::SeqCst) { return; }\n\n        // ISS-004 fix: branch on runtime availability. In-runtime → spawn recovery.\n        // Out-of-runtime (process shutdown, runtime timeout) → log loudly so operators\n        // know the cleanup job (FR-AI-001 §1 #14) will sweep the held entry within ≤60s.\n        if let Ok(handle) = tokio::runtime::Handle::try_current() {\n            let hold = self.hold_id;\n            let pool = self.pool.clone();\n            handle.spawn(async move {\n                metrics::RECONCILES.with_label_values(&amp;[\"panic_recovery\"]).inc();\n                let _ = cost_ledger::reconcile(hold, CallOutcome::Cancelled {\n                    partial_usage: None,\n                    reason: ReconcileReason::InternalError,\n                }, &amp;pool).await;\n            });\n        } else {\n            // Process is exiting; runtime is unavailable. We can't reconcile from here.\n            // Increment the dedicated counter so operators can alarm on non-zero rate.\n            metrics::DROP_OUTSIDE_RUNTIME.inc();\n            tracing::error!(\n                hold_id = ?self.hold_id,\n                severity = \"sev-2\",\n                \"reconcile_guard_drop_outside_runtime; FR-AI-001 cleanup job will sweep within 60s\"\n            );\n        }\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/streaming/heartbeat.rs\n\nuse std::time::Duration;\nuse tokio::sync::mpsc;\nuse super::StreamEvent;\n\npub async fn run(\n    tx: mpsc::Sender&lt;StreamEvent&gt;,\n    interval: Duration,\n    provider_label: &amp;'static str,\n    model_label: String,\n) {\n    let mut tick = tokio::time::interval(interval);\n    tick.tick().await;   // skip the immediate first tick\n    loop {\n        tick.tick().await;\n        if tx.send(StreamEvent::Heartbeat).await.is_err() {\n            return;   // receiver dropped (stream ended); stop heartbeat task\n        }\n        super::metrics::HEARTBEATS.with_label_values(&amp;[provider_label, &amp;model_label]).inc();\n    }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<h3>Code dependencies (other FRs/modules)</h3>\n<ul><li><strong>FR-AI-001 / FR-AI-002</strong> — <code>cost_ledger::precheck</code> and <code>cost_ledger::reconcile</code> (unchanged contracts; streaming uses the same calls).</li><li><strong>FR-AI-006</strong> — <code>alias::resolve</code> resolves the model before the stream opens.</li><li><strong>FR-AI-008</strong> — <code>router::call_provider_streaming</code> is the entry that this FR replaces (FR-AI-008 §1 #16 ships a stub returning <code>StreamingNotImplemented</code>; FR-AI-010 ships the real impl).</li><li><strong>FR-AI-009</strong> — Circuit breaker is consulted by <code>router::call_provider_streaming</code> before each provider attempt (same as the non-streaming path).</li></ul>\n<h3>Concept dependencies (shared types)</h3>\n<ul><li><code>ProviderUsage</code>, <code>FinishReason</code> from <code>crate::router::normalize</code> (FR-AI-008 §3).</li><li><code>CallOutcome</code> from <code>crate::cost_ledger</code> (FR-AI-002).</li><li><code>ProviderKind::as_metric_label()</code> and the <code>ReconcileReason::as_metric_label()</code> (added in this FR's §3) — both follow the FR-AI-007 ISS-003 stable-string-label pattern.</li><li><code>tokio::sync::mpsc</code> for the in-process channel.</li><li><code>axum::response::sse::{Sse, Event}</code> for the HTTP-level SSE serialization.</li></ul>\n<h3>Operational / external</h3>\n<ul><li><code>axum</code> v0.7 (the SSE response type).</li><li><code>tokio_stream</code> v0.1 for <code>ReceiverStream</code>.</li><li><code>futures</code> v0.3 for <code>Stream</code> combinators.</li><li><code>prometheus</code> v0.13 for OBS metrics.</li><li><code>tokio</code> v1 with the <code>time</code> and <code>sync</code> features.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Happy streamed response</h3>\n<pre><code class=\"language-text\">HTTP/1.1 200 OK\nContent-Type: text/event-stream\nCache-Control: no-cache\nConnection: keep-alive\nX-CyberOS-Hold-Id: 01HZK9R8M3X5C8Q4\nX-CyberOS-Stream-Id: 01HZK9R8M3X5C8Q5\n\nevent: token\ndata: {\"text\":\"Q1\",\"model\":\"anthropic.claude-3-5-sonnet-20241022-v2:0\",\"index\":0}\n\nevent: token\ndata: {\"text\":\" OKRs\",\"model\":\"anthropic.claude-3-5-sonnet-20241022-v2:0\",\"index\":1}\n\nevent: token\ndata: {\"text\":\" are\",\"model\":\"anthropic.claude-3-5-sonnet-20241022-v2:0\",\"index\":2}\n\nevent: usage\ndata: {\"prompt_tokens\":120,\"completion_tokens\":3,\"cached_input_tokens\":0}\n\nevent: done\ndata: {\"finish_reason\":\"stop\"}</code></pre>\n<h3>Error mid-stream (provider disconnect)</h3>\n<pre><code class=\"language-text\">event: token\ndata: {\"text\":\"Q1\",\"model\":\"...\",\"index\":0}\n\nevent: token\ndata: {\"text\":\" OKRs\",\"model\":\"...\",\"index\":1}\n\nevent: error\ndata: {\"code\":\"provider_disconnect\",\"message\":\"upstream closed connection after 30 tokens\"}</code></pre>\n<h3>Heartbeat during slow generation</h3>\n<pre><code class=\"language-text\">event: token\ndata: {\"text\":\"The\",\"index\":0}\n\nevent: heartbeat\ndata: {}\n\nevent: token\ndata: {\"text\":\" answer\",\"index\":1}\n\nevent: heartbeat\ndata: {}\n\nevent: usage\ndata: {\"prompt_tokens\":100,\"completion_tokens\":2,\"cached_input_tokens\":0}\n\nevent: done\ndata: {\"finish_reason\":\"stop\"}</code></pre>\n<h3>First-token timeout</h3>\n<pre><code class=\"language-text\">event: error\ndata: {\"code\":\"first_token_timeout\",\"message\":\"timeout after 5s\"}</code></pre>\n<h3>Unsupported model fallback</h3>\n<pre><code class=\"language-text\">HTTP/1.1 200 OK\nContent-Type: application/json\n\n{\n  \"id\": \"...\",\n  \"model\": \"some-batch-only-model\",\n  \"choices\": [...],\n  \"usage\": {...},\n  \"_cyberos_streaming\": \"fell_back_unsupported\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. Items deferred to later FRs:</p>\n<ul><li>WebSocket variant for bidirectional flows (FR-AI-022, P3 — not on the slice-2 roadmap).</li><li>Last-Event-Id resume semantics — explicitly NOT supported (a partial stream is committed; restart with a new request).</li><li>Server-side stream merging (combining multiple provider streams) — out of scope.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Client disconnects mid-stream</td><td><code>tx.send().await</code> returns Err (receiver dropped)</td><td>Provider task aborts; reconcile records <code>Cancelled { reason: ClientDisconnect, partial_usage }</code></td><td>FR-AI-002 §1 #12 column-floor billing</td></tr><tr><td>Provider closes mid-stream</td><td>EOF on response body / Err from <code>stream.next()</code></td><td>Error event <code>code: provider_disconnect</code>; reconcile records <code>ProviderError</code></td><td>Caller may retry as a NEW request (new idempotency_key)</td></tr><tr><td>First-token timeout</td><td><code>tokio::time::timeout(FIRST_TOKEN_TIMEOUT, ...)</code> fires</td><td>Error event <code>code: first_token_timeout</code>; reconcile refund per FR-AI-002 AC #5</td><td>Caller retries</td></tr><tr><td>Mid-stream timeout</td><td><code>tokio::time::timeout(remaining, ...)</code> fires after first token</td><td>Error event <code>code: mid_stream_timeout</code>; reconcile partial</td><td>Caller retries from current state if desired</td></tr><tr><td>Backpressure (slow client)</td><td><code>tx.try_send</code> returns Full → fall through to <code>tx.send().await</code></td><td>Provider sender awaits; OBS counter increments</td><td>Self-resolves when client catches up</td></tr><tr><td>Memory pressure (too many concurrent streams)</td><td>OS-level backpressure (axum connection limit)</td><td>New SSE connections rejected with 503</td><td>Auto-scale; or operator action</td></tr><tr><td>Provider doesn't support streaming for model</td><td><code>provider_supports_streaming(kind) == false</code></td><td>Return <code>StreamingHandlerError::UnsupportedFallback</code>; caller falls back to non-streaming</td><td>Caller still gets response; just not streamed</td></tr><tr><td>Hold expires during slow stream (&gt;60s)</td><td>reconcile sees <code>state='expired'</code></td><td>Stream completes but reconcile fails; OBS sev-2</td><td>Investigate why stream took &gt;60s; raise hold TTL if needed</td></tr><tr><td>Spawned task panics before reconcile</td><td><code>ReconcileGuard::Drop</code> fires</td><td>Drop spawns final reconcile with <code>Cancelled { reason: InternalError }</code></td><td>Investigate the panic; reconcile is recorded as panic_recovery in metrics</td></tr><tr><td>Stream exceeds 300s max duration</td><td><code>Instant::now() &gt;= max_duration_deadline</code></td><td>Error event <code>code: max_stream_duration_exceeded</code>; reconcile <code>MaxDurationExceeded</code></td><td>Provider investigation — why is generation so slow?</td></tr><tr><td>Provider stream ends without <code>usage</code> event</td><td><code>got_done == false</code> after <code>stream.next() == None</code></td><td>Error event <code>code: missing_usage</code>; reconcile <code>ProviderError { partial_usage: None }</code></td><td>Provider impl bug — file ticket; NEVER guess token counts</td></tr><tr><td>Heartbeat task fails to send (receiver dropped)</td><td><code>tx.send().await.is_err()</code> in heartbeat loop</td><td>Heartbeat task exits cleanly; no impact on reconcile</td><td>By design — heartbeat is best-effort</td></tr><tr><td>Token index drift (provider streams out of order)</td><td>Caller-side check on monotonic <code>index</code></td><td>Caller may surface to user; gateway doesn't reorder</td><td>Provider impl bug</td></tr><tr><td>Backpressure exceeds reasonable bound</td><td>Backpressure event count &gt; 100 in 30s</td><td>OBS alarm fires; operator investigates client</td><td>Operator action — kill or rate-limit the client</td></tr><tr><td>Concurrent streams exceed connection budget</td><td>axum <code>MaxConnections</code> middleware</td><td>New requests get 503</td><td>Auto-scale</td></tr><tr><td><code>provider_supports_streaming</code> returns wrong value</td><td>Manual table maintenance</td><td>Either an unsupported provider tries to stream (fails fast), OR a streaming-capable provider falls back unnecessarily (works but logs)</td><td>CI test asserts the table matches each provider impl</td></tr><tr><td><code>ReconcileGuard::Drop</code> fires outside tokio runtime (process shutdown)</td><td><code>Handle::try_current()</code> returns Err</td><td>Sev-2 log emitted; <code>ai_streaming_drop_outside_runtime_total</code> increments; no immediate reconcile</td><td>FR-AI-001 cleanup job sweeps held entries within ≤60s — ISS-004 fix</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The 1500ms first-token target is provider-dependent. Bedrock typically delivers in &lt;800ms; Anthropic native in &lt;1000ms; OpenAI in &lt;600ms. Hitting 1500ms p95 across all three provides headroom for cold starts, retries on the first attempt, and SSE serialization.</li><li>Streaming is <code>SHOULD</code> not <code>MUST</code> for slice 2 because the non-streaming path (FR-AI-008) is the workhorse. Streaming polish can land iteratively without blocking slice-2 release.</li><li>The cost-ledger semantics are identical to non-streaming: hold-precheck → call → reconcile. Streaming changes only the <em>transport</em>, not the <em>budget</em> model. The <code>ReconcileGuard</code> is the only structural difference — required because the spawned task may complete out of band with the HTTP handler.</li><li>Tests use <code>ReconcileSpy</code> (a test double) to assert reconcile call shape. Production code never imports it; it lives in <code>services/ai-gateway/tests/cost_reconcile_test.rs</code> behind <code>#[cfg(test)]</code>.</li><li>The <code>provider_supports_streaming</code> table is a static lookup keyed on <code>ProviderKind</code>. In slice 2 it returns <code>true</code> for Bedrock/Anthropic/OpenAI/Vertex (when added), <code>false</code> for batch-only providers. A CI test asserts every <code>ProviderKind</code> variant is covered.</li><li>Heartbeats are sent via <code>tx.send</code> (NOT <code>try_send</code>) — if the channel is full, heartbeats wait their turn. This means a heavily backpressured stream may skip a heartbeat tick; that's fine because tokens themselves keep the connection alive.</li><li>The <code>RECONCILES</code> counter is the canary — if <code>sum(rate(ai_streaming_reconciles_total[5m]))</code> doesn't match <code>rate(ai_streaming_starts_total[5m])</code>, we have a leak. Operator dashboard alerts on &gt;0.1% mismatch.</li><li>Future work (FR-AI-022): Last-Event-Id resume semantics. The current contract is \"no resume\" — partial streams are committed; clients restart with a new request. Adding resume requires a server-side buffer that we explicitly chose not to ship in slice 2.</li><li>The <code>index</code> field on <code>event: token</code> exists specifically to detect proxy-induced reordering. SSE specifies in-order delivery, but cellular networks and corporate proxies have occasionally been observed to reorder events in real deployments; the index lets clients catch the bug.</li></ul>\n<hr>\n<p><em>End of FR-AI-010. Status: draft (10/10 target).</em></p>";
