window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MCP-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The MCP service <strong>MUST</strong> ship tool-annotation gating at <code>services/mcp/src/gating/</code> enforcing MCP 2025-11-25 spec hint semantics (<code>readOnlyHint</code> / <code>destructiveHint</code> / <code>idempotentHint</code> / <code>openWorldHint</code>) at gateway entry, with per-tenant policy override, confirm-mode ack store, elicit-mode delegation to FR-MCP-008, bypass-token path, audit-only transition mode, nightly annotation drift detection, and 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define the closed <code>mcp_gating_mode</code> enum at migration <code>0006</code>: <code>('auto_confirm','confirm','elicit')</code> per DEC-1043. CI cardinality test asserts 3.</li></ol>\n<ol><li><strong>MUST</strong> define the closed <code>mcp_gating_decision</code> enum at migration <code>0006</code>: <code>('auto_allowed','confirmed','elicited','bypassed','rejected_no_confirmation','rejected_expired_ack')</code> per DEC-1049. CI cardinality test asserts 6.</li></ol>\n<ol><li><strong>MUST</strong> define <code>mcp_gating_policy</code> table at migration <code>0006</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, policy_yaml TEXT NOT NULL, audit_only BOOLEAN NOT NULL DEFAULT false, version INT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), created_by_subject_id UUID NOT NULL, UNIQUE (tenant_id, version))</code>. Per-tenant versioned policy.</li></ol>\n<ol><li><strong>MUST</strong> define <code>mcp_pending_confirmations</code> table at migration <code>0007</code>: <code>(id BIGSERIAL PRIMARY KEY, caller_subject_id UUID NOT NULL, tool_id TEXT NOT NULL, tenant_id UUID NOT NULL, request_payload_sha256 CHAR(64) NOT NULL, expires_at TIMESTAMPTZ NOT NULL, consumed_at TIMESTAMPTZ, UNIQUE(caller_subject_id, tool_id, request_payload_sha256))</code>. Single-use ack store per DEC-1054.</li></ol>\n<ol><li><strong>MUST</strong> define <code>mcp_gating_decisions_log</code> table at migration <code>0008</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, caller_subject_id UUID NOT NULL, tool_id TEXT NOT NULL, decision mcp_gating_decision NOT NULL, annotations JSONB NOT NULL, gating_mode mcp_gating_mode NOT NULL, audit_only BOOLEAN NOT NULL, decided_at TIMESTAMPTZ NOT NULL DEFAULT now(), trace_id CHAR(32))</code>. Append-only per feature-request-audit skill rule 12.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both USING and WITH CHECK on all 3 gating tables. Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li></ol>\n<ol><li><strong>MUST</strong> intercept every <code>tools/call</code> MCP request at the gateway entry (FR-MCP-001 handler modification) BEFORE dispatching to the per-module server. The <code>tools_call</code> handler invokes <code>gating::decision::decide(caller, tool, payload)</code> first; on <code>rejected_*</code> decisions, returns 403 + <code>confirm_required</code> (with confirm endpoint URL) + emits <code>mcp.tool_gating_decision</code>. On <code>auto_allowed | confirmed | elicited | bypassed</code>, proceeds to dispatch.</li></ol>\n<ol><li><strong>MUST</strong> resolve gating mode per DEC-1053 annotation precedence (most restrictive wins):</li></ol>\n<ul><li>If tool has <code>destructiveHint=true</code> OR <code>openWorldHint=true</code> → requires confirmation (mode from policy: <code>confirm</code> or <code>elicit</code>).</li><li>Else if <code>readOnlyHint=true</code> AND <code>destructiveHint=false</code> AND <code>openWorldHint=false</code> → <code>auto_confirm</code> (fast path).</li><li>Otherwise → policy-default (which itself defaults to <code>confirm</code>).</li><li><code>idempotentHint</code> is informational only (does not change gating; surfaces in audit log).</li></ul>\n<ol><li><strong>MUST</strong> support per-tenant gating policy YAML per DEC-1044. Schema: ``<code>yaml version: 1 default_mode: confirm audit_only: false overrides: \"cyberos.docs.delete_document\": mode: elicit \"cyberos.projects.read_issues\": mode: auto_confirm # explicit override of annotation-derived default </code>`<code> Policy stored in </code>mcp_gating_policy.policy_yaml<code>; loaded into memory at handler startup + hot-reloaded on NATS subject </code>cyberos.mcp.gating_policy.updated.&lt;tenant_slug&gt;`.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tenant_id}/mcp/gating-policy</code> per DEC-1058. Caller has <code>tenant_admin</code> role. Body: <code>{ policy_yaml }</code>. Handler:</li></ol>\n<ul><li>Validates YAML schema (tool_id format <code>cyberos.&lt;module&gt;.&lt;verb&gt;_&lt;noun&gt;</code> per FR-MCP-003 + SEP-986).</li><li>INSERTs new policy row at version=max(version)+1.</li><li>Publishes NATS reload event.</li><li>Emits <code>mcp.tool_gating_policy_updated</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/mcp/tools/{tool_id}/confirm</code> for confirm-mode ack. Body: <code>{ tenant_id, tool_id, request_payload_sha256, signed_payload? }</code>. Handler:</li></ol>\n<ul><li>Validates caller JWT (FR-AUTH-004).</li><li>Validates <code>request_payload_sha256</code> matches a recently-attempted tool call by this caller (matched within 60s — denial-of-future-action mitigation).</li><li>INSERTs <code>mcp_pending_confirmations</code> row with <code>expires_at = now() + 5 min</code> per DEC-1047.</li><li>Returns 201 + <code>{ confirm_token, expires_at }</code>. Caller re-issues tools/call within 5 min; confirm row consumed atomically.</li></ul>\n<ol><li><strong>MUST</strong> consume confirm token atomically per DEC-1054. The tools/call handler's gating decision:</li></ol>\n<ul><li>Compute <code>request_payload_sha256</code>.</li><li><code>SELECT ... FROM mcp_pending_confirmations WHERE caller_subject_id=$1 AND tool_id=$2 AND request_payload_sha256=$3 AND consumed_at IS NULL AND expires_at &gt; now() FOR UPDATE</code>.</li><li>If row found: UPDATE <code>consumed_at=now()</code> + proceed.</li><li>If not found: return 403 + <code>confirm_required</code>.</li><li><code>UNIQUE(caller_subject_id, tool_id, request_payload_sha256)</code> prevents double-use.</li></ul>\n<ol><li><strong>MUST</strong> delegate elicit mode to FR-MCP-008 per DEC-1055. At slice 2 of this FR (FR-MCP-008 not yet shipped), <code>elicit</code> mode returns <code>503 + { error: \"elicitation_not_yet_supported\", retry_when: \"FR-MCP-008 ships in next slice\" }</code>. When FR-MCP-008 lands, the elicit handler invokes <code>mcp::elicitation::request(caller, tool, prompt)</code> + awaits the response inline.</li></ol>\n<ol><li><strong>MUST</strong> support bypass token per DEC-1045 + DEC-1057. Callers with JWT <code>scope_grants</code> containing <code>mcp_gating_bypass</code> skip gating + record <code>bypassed</code> decision in audit log. The scope is granted ONLY to system-tenant callers (per FR-AUTH-004 + DEC-1045); cross-tenant grant attempt is rejected at JWT mint. Every bypass invocation emits <code>mcp.tool_gating_bypass_used</code> sev-2.</li></ol>\n<ol><li><strong>MUST</strong> support audit-only mode per DEC-1050. Tenant sets <code>mcp_gating_policy.audit_only=true</code>; gating decisions are computed + logged but tools always proceed (no 403). Surface mode via <code>decision='audit_only_allowed'</code> in audit log + sev-2 row <code>mcp.tool_gating_audit_only_mode_set</code> when policy is updated to enable audit-only.</li></ol>\n<ol><li><strong>MUST</strong> detect annotation drift nightly per DEC-1059. The <code>drift_detector.rs</code> scheduled job:</li></ol>\n<ul><li>Loads each module's currently-registered annotations from <code>server_registry</code>.</li><li>Queries each module's <code>tools/list</code> endpoint to fetch the live annotations.</li><li>For each tool: compares declared annotations (registry) vs live annotations (server).</li><li>Drift detected → INSERTs <code>mcp_gating_drift_log</code> row + emits <code>mcp.tool_gating_annotation_drift</code> sev-1 with <code>(module, tool_id, registry_annotations, live_annotations, diff_fields)</code>.</li></ul>\n<ol><li><strong>MUST</strong> rate-limit confirmation requests at 100 confirm-mode requests/min/caller per DEC-1056. Excess returns <code>429 + Retry-After</code>.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub audit rows per DEC-1060 + feature-request-audit skill rule 18. <code>request_payload_sha256</code> only in memory chain; raw payload in MCP gateway logs (RLS-scoped, 7-day retention).</li></ol>\n<ol><li><strong>MUST</strong> thread W3C <code>traceparent</code> across <code>tools/call</code> → gating decision → confirm-ack → dispatch → response (feature-request-audit skill rule 22-24).</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit row kinds per DEC-1051:</li></ol>\n<ul><li><code>mcp.tool_gating_decision</code> (sev-3 high-volume; sampled at 1% via FR-OBS-006 tail-sampling)</li><li><code>mcp.tool_gating_policy_updated</code> (sev-1)</li><li><code>mcp.tool_gating_annotation_drift</code> (sev-1)</li><li><code>mcp.tool_gating_bypass_used</code> (sev-2)</li><li><code>mcp.tool_gating_audit_only_mode_set</code> (sev-2)</li></ul>\n<ol><li><strong>MUST</strong> preserve MCP-spec response shape on rejection. The 403 response body includes: ``<code>json { \"jsonrpc\": \"2.0\", \"error\": { \"code\": -32000, \"message\": \"confirm_required\", \"data\": { \"tool_id\": \"...\", \"confirm_endpoint\": \"https://...\", \"expires_at_after_confirm\": \"...\" } }, \"id\": &lt;request_id&gt; } </code>``</li></ol>\n<ol><li><strong>MUST</strong> support gating for FR-MCP-007 Tasks primitive — long-running tasks with <code>destructiveHint=true</code> require confirmation at task START; subsequent task status polls do NOT require re-confirmation (the consent is to the task as a unit). Documented as cross-FR contract.</li></ol>\n<ol><li><strong>MUST NOT</strong> cache gating decisions cross-tenant. Each <code>mcp_gating_policy</code> is per-tenant; per-tenant in-memory cache; tenant_id-scoped cache key.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow bypass-token scope grant outside system-tenant per DEC-1045. FR-AUTH-004 JWT mint guard: if <code>tenant_id != system_tenant</code> AND <code>mcp_gating_bypass</code> in scope_grants → reject mint with <code>invalid_scope_for_tenant</code>.</li></ol>\n<ol><li><strong>SHOULD</strong> observe per-tenant gating-decision distribution via OTel histogram <code>mcp_gating_decision_total</code> labelled <code>(tenant_id, decision, tool_id)</code> for operator visibility on policy effectiveness.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why gateway-side gating (§1 #7, DEC-1052)?</strong> Defense-in-depth. MCP clients (Claude Desktop, ChatGPT) MAY surface destructive-tool UX prompts, but they're not REQUIRED to. Server-side gating ensures the safety net exists regardless of client implementation quality. Single ingress point at the gateway means per-module servers don't each re-implement gating logic.</p>\n<p><strong>Why annotation precedence (§1 #8, DEC-1053)?</strong> MCP 2025-11-25 spec defines 5 hints; combining them ambiguously is bug-prone. Documenting \"most restrictive wins\" makes the decision algorithm deterministic. <code>readOnlyHint=true</code> AND <code>destructiveHint=true</code> together is contradictory (spec says don't); we err on the safe side (require confirmation).</p>\n<p><strong>Why per-tenant policy (§1 #9, DEC-1044)?</strong> Default conservative gating (confirm everything destructive) is right for most tenants. But sandbox tenants (development) need looser policy; regulated tenants (PII-heavy) need stricter (elicit instead of confirm). One global policy can't serve both. Per-tenant YAML lets each tenant_admin tune.</p>\n<p><strong>Why confirm-token TTL of 5 min (§1 #12, DEC-1047)?</strong> Confirmation is \"user intends to do X now\"; intent expires quickly. 5 min is long enough for the user to read the prompt + decide; short enough that an attacker who steals the confirm token has minimal window. Industry convention (Stripe payment intent confirm tokens: 5 min default).</p>\n<p><strong>Why bypass-token forensic-loud (§1 #14, DEC-1057)?</strong> Bypass is rare-by-design. Sev-2 audit on every bypass invocation makes \"who is using the bypass and why\" answerable in 30 seconds via dashboard. Silent bypass = forensic blindness.</p>\n<p><strong>Why audit-only transition mode (§1 #15, DEC-1050)?</strong> Switching from un-gated to gated breaks running workflows that didn't expect 403s. Audit-only mode runs the decision algorithm in shadow: produces audit rows showing what WOULD happen if gating were enforced. Tenant_admin reviews, tunes policy, then flips to enforced mode. Reduces rollout blast radius from \"everything breaks\" to \"everything works, but now we know what would have been blocked\".</p>\n<p><strong>Why annotation drift detection nightly (§1 #16, DEC-1059)?</strong> Tool annotations are forensic-critical (they drive gating decisions). A module that changes a tool from <code>readOnlyHint=true</code> to <code>destructiveHint=true</code> without updating the registry = silent removal of safety. Nightly comparison catches drift before it bites.</p>\n<p><strong>Why confirm-token request_payload_sha256 matching (§1 #11)?</strong> Without payload binding, an attacker who tricks the user into confirming \"delete_project(123)\" could re-use the confirm token for \"delete_project(456)\". Binding the ack to the payload hash means the confirmation is for THAT specific call, not for that tool generally.</p>\n<p><strong>Why elicit-mode placeholder via 503 at slice 2 (§1 #13, DEC-1055)?</strong> FR-MCP-008 (Elicitation) is on the roadmap but not yet shipped. Returning 503 instead of silently degrading to confirm mode (a) makes the missing capability visible, (b) doesn't violate the tenant_admin's policy choice (they picked elicit; we don't pretend confirm is equivalent), (c) gives operators a metric to track when FR-MCP-008 demand materialises.</p>\n<p><strong>Why scope-claim approach for bypass (§1 #14, DEC-1045)?</strong> Scope claims are issued at JWT mint (FR-AUTH-004 controlled); they're tamper-evident (signed); they're auditable (scope grants logged at mint time). Alternative (separate bypass-token endpoint) duplicates auth surface. Scope claim integrates with existing FR-AUTH-004 + FR-AUTH-101 RBAC.</p>\n<p><strong>Why versioned policy (§1 #3)?</strong> Same rationale as FR-PORTAL-002 brand pack versioning — tenant_admin wants rollback safety on commercial-grade config changes. Versioning is cheap (one row per save) + valuable (undo button).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0006_mcp_gating_policy.sql\nCREATE TYPE mcp_gating_mode AS ENUM ('auto_confirm','confirm','elicit');\nCREATE TYPE mcp_gating_decision AS ENUM (\n  'auto_allowed','confirmed','elicited','bypassed',\n  'rejected_no_confirmation','rejected_expired_ack'\n);\n\nCREATE TABLE mcp_gating_policy (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  policy_yaml TEXT NOT NULL,\n  audit_only BOOLEAN NOT NULL DEFAULT false,\n  version INT NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  created_by_subject_id UUID NOT NULL,\n  UNIQUE (tenant_id, version)\n);\nCREATE TABLE mcp_gating_policy_active (\n  tenant_id UUID PRIMARY KEY,\n  active_policy_id BIGINT NOT NULL REFERENCES mcp_gating_policy(id),\n  activated_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nALTER TABLE mcp_gating_policy ENABLE ROW LEVEL SECURITY;\nCREATE POLICY mcp_gating_policy_rls ON mcp_gating_policy\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nALTER TABLE mcp_gating_policy_active ENABLE ROW LEVEL SECURITY;\nCREATE POLICY mcp_gating_policy_active_rls ON mcp_gating_policy_active\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON mcp_gating_policy FROM cyberos_app;\nGRANT UPDATE (active_policy_id, activated_at) ON mcp_gating_policy_active TO cyberos_app;\n\n-- 0007_mcp_pending_confirmations.sql\nCREATE TABLE mcp_pending_confirmations (\n  id BIGSERIAL PRIMARY KEY,\n  caller_subject_id UUID NOT NULL,\n  tool_id TEXT NOT NULL,\n  tenant_id UUID NOT NULL,\n  request_payload_sha256 CHAR(64) NOT NULL,\n  expires_at TIMESTAMPTZ NOT NULL,\n  consumed_at TIMESTAMPTZ,\n  UNIQUE (caller_subject_id, tool_id, request_payload_sha256)\n);\nCREATE INDEX idx_mcp_confirm_lookup\n  ON mcp_pending_confirmations(caller_subject_id, tool_id, request_payload_sha256)\n  WHERE consumed_at IS NULL;\nCREATE INDEX idx_mcp_confirm_expiry\n  ON mcp_pending_confirmations(expires_at) WHERE consumed_at IS NULL;\nALTER TABLE mcp_pending_confirmations ENABLE ROW LEVEL SECURITY;\nCREATE POLICY mcp_pending_confirmations_rls ON mcp_pending_confirmations\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON mcp_pending_confirmations FROM cyberos_app;\nGRANT UPDATE (consumed_at) ON mcp_pending_confirmations TO cyberos_app;\nGRANT DELETE ON mcp_pending_confirmations TO cyberos_pruner;\n\n-- 0008_mcp_gating_decisions_log.sql\nCREATE TABLE mcp_gating_decisions_log (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  caller_subject_id UUID NOT NULL,\n  tool_id TEXT NOT NULL,\n  decision mcp_gating_decision NOT NULL,\n  annotations JSONB NOT NULL,\n  gating_mode mcp_gating_mode NOT NULL,\n  audit_only BOOLEAN NOT NULL,\n  decided_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_gating_decisions_caller ON mcp_gating_decisions_log(caller_subject_id, decided_at DESC);\nALTER TABLE mcp_gating_decisions_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY mcp_gating_decisions_log_rls ON mcp_gating_decisions_log\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON mcp_gating_decisions_log FROM cyberos_app;</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/mcp/src/gating/mod.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug, sqlx::Type, serde::Serialize)]\n#[sqlx(type_name = \"mcp_gating_decision\", rename_all = \"snake_case\")]\npub enum GatingDecision {\n    AutoAllowed, Confirmed, Elicited, Bypassed,\n    RejectedNoConfirmation, RejectedExpiredAck,\n}\n\n#[derive(Debug)]\npub struct ToolAnnotations {\n    pub read_only_hint: bool,\n    pub destructive_hint: bool,\n    pub idempotent_hint: bool,\n    pub open_world_hint: bool,\n}\n\npub async fn decide(ctx: &amp;AppCtx, caller: &amp;JwtClaims, tool_id: &amp;str, payload: &amp;[u8])\n    -&gt; Result&lt;GatingDecision, GatingError&gt;\n{\n    if caller.has_scope(\"mcp_gating_bypass\") {\n        audit_bypass(ctx, caller, tool_id).await;\n        return Ok(GatingDecision::Bypassed);\n    }\n    let policy = ctx.gating.policy_for(caller.tenant_id).await?;\n    let annotations = ctx.registry.annotations_for(tool_id).await?;\n    let mode = policy.resolve_mode(tool_id, &amp;annotations);\n    let payload_sha = sha256_hex(payload);\n    match mode {\n        GatingMode::AutoConfirm =&gt; Ok(GatingDecision::AutoAllowed),\n        GatingMode::Confirm =&gt; {\n            if confirm_token_consume(ctx, caller, tool_id, &amp;payload_sha).await? {\n                Ok(GatingDecision::Confirmed)\n            } else {\n                Err(GatingError::ConfirmRequired { confirm_endpoint: confirm_url(tool_id) })\n            }\n        }\n        GatingMode::Elicit =&gt; elicit::request(ctx, caller, tool_id, payload).await,\n    }\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">POST   /v1/admin/tenants/{tenant_id}/mcp/gating-policy             (tenant_admin)\nPOST   /v1/admin/tenants/{tenant_id}/mcp/gating-policy/activate    (tenant_admin)\nPOST   /v1/mcp/tools/{tool_id}/confirm                              (any authenticated caller)\nGET    /v1/admin/tenants/{tenant_id}/mcp/gating-decisions          (tenant_admin or system)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Annotation precedence</strong> — tool with <code>destructiveHint=true AND readOnlyHint=true</code> → requires confirm (most restrictive wins).</li><li><strong>Read-only fast path</strong> — tool with <code>readOnlyHint=true, destructiveHint=false, openWorldHint=false</code> + policy default <code>confirm</code> → <code>auto_allowed</code> (annotation hint wins for read-only safety).</li><li><strong>Destructive requires confirm</strong> — tool with <code>destructiveHint=true</code> invoked without prior ack → 403 + <code>confirm_required</code> + endpoint URL.</li><li><strong>Confirm ack consumed atomically</strong> — same confirm token used twice → second invocation 403 <code>rejected_expired_ack</code>.</li><li><strong>Confirm TTL</strong> — confirm with <code>expires_at=now() - 1min</code> (expired) → 403 + <code>rejected_expired_ack</code>.</li><li><strong>Bypass token</strong> — caller with <code>mcp_gating_bypass</code> scope → <code>bypassed</code> decision; sev-2 memory row emitted.</li><li><strong>Bypass for non-system tenant rejected at mint</strong> — JWT mint with <code>mcp_gating_bypass</code> for non-system tenant → mint rejected <code>invalid_scope_for_tenant</code>.</li><li><strong>Audit-only mode</strong> — <code>audit_only=true</code> policy + destructive tool → call proceeds; audit row <code>decision='audit_only_allowed'</code> + <code>mcp.tool_gating_audit_only_mode_set</code> emitted on policy save.</li><li><strong>mcp_gating_decision enum cardinality</strong> — 6 values exactly: <code>{auto_allowed, confirmed, elicited, bypassed, rejected_no_confirmation, rejected_expired_ack}</code>.</li><li><strong>mcp_gating_mode enum cardinality</strong> — 3 values exactly: <code>{auto_confirm, confirm, elicit}</code>.</li><li><strong>Policy update tenant_admin only</strong> — engagement_admin POST → 403; tenant_admin → 201 + sev-1 memory row.</li><li><strong>Elicit mode 503 placeholder</strong> — policy with <code>mode: elicit</code> on a destructive tool → 503 <code>elicitation_not_yet_supported</code>.</li><li><strong>Rate limit on confirm endpoint</strong> — 101st confirm in 60s → 429.</li><li><strong>Annotation drift detection</strong> — registry says <code>readOnlyHint=true</code> but live tools/list says <code>destructiveHint=true</code> → drift row + sev-1 audit.</li><li><strong>JSON-RPC error shape</strong> — 403 response body conforms to MCP JSON-RPC error shape with <code>confirm_endpoint</code> in <code>data</code>.</li><li><strong>Per-tenant policy isolation</strong> — tenant A's policy doesn't affect tenant B's gating decisions.</li><li><strong>Bypass audit always emitted</strong> — every bypass invocation → exactly one <code>mcp.tool_gating_bypass_used</code> row (no batching).</li><li><strong>PII scrub</strong> — audit row carries <code>request_payload_sha256</code> only; raw payload not in memory chain.</li><li><strong>Trace_id threaded</strong> — single trace_id present in tools/call span + gating decision row + downstream dispatch.</li><li><strong>Idempotent hint informational</strong> — tool with <code>idempotentHint=true, destructiveHint=true</code> still requires confirm; idempotent hint surfaces in audit <code>annotations</code> JSONB but doesn't relax gating.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>gating_annotation_precedence_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn destructive_overrides_readonly_when_both_true() {\n    let ctx = TestContext::new().await;\n    ctx.register_tool(\"cyberos.docs.weird_tool\", ToolAnnotations {\n        read_only_hint: true, destructive_hint: true, idempotent_hint: false, open_world_hint: false,\n    }).await;\n    let r = ctx.invoke_tool(\"cyberos.docs.weird_tool\", json!({})).await;\n    assert_eq!(r.status(), 403);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"][\"message\"], \"confirm_required\");\n}</code></pre>\n<h3>5.2 <code>gating_readonly_fast_path_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn readonly_tool_bypasses_confirmation() {\n    let ctx = TestContext::with_default_policy_mode(GatingMode::Confirm).await;\n    ctx.register_tool(\"cyberos.projects.list\", ToolAnnotations {\n        read_only_hint: true, destructive_hint: false, idempotent_hint: true, open_world_hint: false,\n    }).await;\n    let r = ctx.invoke_tool(\"cyberos.projects.list\", json!({})).await;\n    assert_eq!(r.status(), 200);\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"mcp.tool_gating_decision\"\n        &amp;&amp; r.payload[\"decision\"] == \"auto_allowed\"));\n}</code></pre>\n<h3>5.3 <code>gating_destructive_requires_confirm_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn destructive_tool_requires_confirm_then_proceeds() {\n    let ctx = TestContext::new().await;\n    ctx.register_tool(\"cyberos.docs.delete\", ToolAnnotations {\n        read_only_hint: false, destructive_hint: true, idempotent_hint: false, open_world_hint: false,\n    }).await;\n    let payload = json!({\"doc_id\": \"abc\"});\n\n    // First call: 403 + confirm endpoint\n    let r1 = ctx.invoke_tool(\"cyberos.docs.delete\", payload.clone()).await;\n    assert_eq!(r1.status(), 403);\n\n    // Confirm\n    let payload_sha = sha256_hex(&amp;serde_json::to_vec(&amp;payload).unwrap());\n    let confirm = ctx.post_confirm(\"cyberos.docs.delete\", &amp;payload_sha).await;\n    assert_eq!(confirm.status(), 201);\n\n    // Re-invoke: 200\n    let r2 = ctx.invoke_tool(\"cyberos.docs.delete\", payload.clone()).await;\n    assert_eq!(r2.status(), 200);\n\n    // Re-use ack: 403\n    let r3 = ctx.invoke_tool(\"cyberos.docs.delete\", payload).await;\n    assert_eq!(r3.status(), 403);\n}</code></pre>\n<h3>5.4 <code>gating_confirm_ttl_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn expired_confirm_token_rejected() {\n    let ctx = TestContext::new().await;\n    ctx.register_tool(\"cyberos.docs.delete\", destructive_annotations()).await;\n    let payload = json!({\"doc_id\": \"abc\"});\n    let payload_sha = sha256_hex(&amp;serde_json::to_vec(&amp;payload).unwrap());\n    ctx.insert_pending_confirmation_expiring(\n        ctx.caller_subject_id, \"cyberos.docs.delete\", &amp;payload_sha,\n        Utc::now() - Duration::minutes(1)\n    ).await;\n\n    let r = ctx.invoke_tool(\"cyberos.docs.delete\", payload).await;\n    assert_eq!(r.status(), 403);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"][\"message\"], \"rejected_expired_ack\");\n}</code></pre>\n<h3>5.5 <code>gating_bypass_token_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn bypass_scope_skips_gating_and_audits() {\n    let ctx = TestContext::with_system_tenant_caller_scope(\"mcp_gating_bypass\").await;\n    ctx.register_tool(\"cyberos.docs.delete\", destructive_annotations()).await;\n    let r = ctx.invoke_tool(\"cyberos.docs.delete\", json!({\"doc_id\": \"x\"})).await;\n    assert_eq!(r.status(), 200);\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"mcp.tool_gating_bypass_used\" &amp;&amp; r.severity == 2));\n}\n\n#[tokio::test]\nasync fn non_system_tenant_cannot_grant_bypass_scope() {\n    let ctx = TestContext::new().await;\n    let result = ctx.auth.mint_jwt_for(ctx.regular_tenant, vec![\"mcp_gating_bypass\"]).await;\n    assert!(matches!(result, Err(AuthError::InvalidScopeForTenant)));\n}</code></pre>\n<h3>5.6 <code>gating_audit_only_mode_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn audit_only_mode_allows_destructive_but_logs() {\n    let ctx = TestContext::with_audit_only_policy().await;\n    ctx.register_tool(\"cyberos.docs.delete\", destructive_annotations()).await;\n    let r = ctx.invoke_tool(\"cyberos.docs.delete\", json!({\"doc_id\": \"x\"})).await;\n    assert_eq!(r.status(), 200);\n\n    let audit = ctx.memory_rows().await;\n    let dec = audit.iter().find(|r| r.kind == \"mcp.tool_gating_decision\").unwrap();\n    assert_eq!(dec.payload[\"audit_only\"], true);\n}</code></pre>\n<h3>5.7 <code>gating_decision_enum_cardinality_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn mcp_gating_decision_has_6_values() {\n    let ctx = TestContext::new().await;\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::mcp_gating_decision))::text\"\n    ).fetch_all(&amp;ctx.pool).await.unwrap();\n    let mut labels = labels; labels.sort();\n    assert_eq!(labels, vec![\n        \"auto_allowed\",\"bypassed\",\"confirmed\",\"elicited\",\n        \"rejected_expired_ack\",\"rejected_no_confirmation\"\n    ]);\n}</code></pre>\n<h3>5.8 <code>gating_drift_detection_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn drift_between_registry_and_live_detected() {\n    let ctx = TestContext::new().await;\n    ctx.register_tool_in_registry(\"cyberos.docs.weird\", readonly_annotations()).await;\n    ctx.set_live_tool_annotations(\"cyberos.docs.weird\", destructive_annotations()).await;\n    ctx.run_drift_detector().await;\n\n    let audit = ctx.memory_rows().await;\n    let drift = audit.iter().find(|r| r.kind == \"mcp.tool_gating_annotation_drift\").unwrap();\n    assert_eq!(drift.severity, 1);\n    assert!(drift.payload[\"diff_fields\"].as_array().unwrap()\n        .iter().any(|f| f == \"destructiveHint\"));\n}</code></pre>\n<h3>5.9 <code>gating_policy_tenant_admin_only_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn engagement_admin_cannot_set_gating_policy() {\n    let ctx = TestContext::new().await;\n    let eng_admin_token = ctx.mint_jwt_with_role(ctx.tenant, \"engagement_admin\");\n    let r = ctx.post(\"/v1/admin/tenants/{tenant}/mcp/gating-policy\").bearer_auth(eng_admin_token)\n        .body(\"default_mode: confirm\").send().await.unwrap();\n    assert_eq!(r.status(), 403);\n}</code></pre>\n<h3>5.10 <code>gating_audit_emission_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn full_lifecycle_emits_5_kinds() {\n    let ctx = TestContext::new().await;\n    ctx.tenant_admin_set_policy_with_audit_only(true).await;     // audit_only_mode_set\n    ctx.tenant_admin_set_policy_with_audit_only(false).await;    // policy_updated\n    ctx.register_tool(\"cyberos.docs.weird\", readonly_annotations()).await;\n    ctx.set_live_tool_annotations(\"cyberos.docs.weird\", destructive_annotations()).await;\n    ctx.run_drift_detector().await;                              // annotation_drift\n    ctx.invoke_tool_with_bypass(\"cyberos.docs.weird\").await;     // bypass_used + decision\n    let kinds: Vec&lt;&amp;str&gt; = ctx.memory_rows().await.iter().map(|r| r.kind.as_str()).collect();\n    assert!(kinds.contains(&amp;\"mcp.tool_gating_policy_updated\"));\n    assert!(kinds.contains(&amp;\"mcp.tool_gating_audit_only_mode_set\"));\n    assert!(kinds.contains(&amp;\"mcp.tool_gating_annotation_drift\"));\n    assert!(kinds.contains(&amp;\"mcp.tool_gating_bypass_used\"));\n    assert!(kinds.contains(&amp;\"mcp.tool_gating_decision\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<h3>6.1 Policy mode resolver</h3>\n<pre><code class=\"language-rust\">// services/mcp/src/gating/policy.rs\nimpl GatingPolicy {\n    pub fn resolve_mode(&amp;self, tool_id: &amp;str, ann: &amp;ToolAnnotations) -&gt; GatingMode {\n        if let Some(override_mode) = self.overrides.get(tool_id) {\n            return *override_mode;\n        }\n        if ann.destructive_hint || ann.open_world_hint {\n            return self.default_destructive_mode.unwrap_or(GatingMode::Confirm);\n        }\n        if ann.read_only_hint &amp;&amp; !ann.destructive_hint &amp;&amp; !ann.open_world_hint {\n            return GatingMode::AutoConfirm;\n        }\n        self.default_mode\n    }\n}</code></pre>\n<h3>6.2 Confirm consumption (atomic)</h3>\n<pre><code class=\"language-rust\">// services/mcp/src/gating/confirm.rs\npub async fn consume_pending(\n    pool: &amp;PgPool, caller: Uuid, tool_id: &amp;str, payload_sha: &amp;str\n) -&gt; Result&lt;bool, sqlx::Error&gt; {\n    let result = sqlx::query(\n        \"UPDATE mcp_pending_confirmations\n         SET consumed_at = now()\n         WHERE caller_subject_id = $1 AND tool_id = $2 AND request_payload_sha256 = $3\n               AND consumed_at IS NULL AND expires_at &gt; now()\n         RETURNING id\"\n    ).bind(caller).bind(tool_id).bind(payload_sha).fetch_optional(pool).await?;\n    Ok(result.is_some())\n}</code></pre>\n<h3>6.3 Drift detector</h3>\n<pre><code class=\"language-rust\">pub async fn detect_drift(ctx: &amp;AppCtx) -&gt; Result&lt;Vec&lt;DriftEvent&gt;, DriftError&gt; {\n    let modules = ctx.registry.list_modules().await?;\n    let mut drifts = Vec::new();\n    for module in modules {\n        let registered = ctx.registry.annotations_by_module(&amp;module).await?;\n        let live = ctx.fetch_live_tools_list(&amp;module).await?;\n        for tool in registered.tools {\n            let live_ann = live.iter().find(|t| t.tool_id == tool.tool_id);\n            if let Some(live_ann) = live_ann {\n                let diff = compare_annotations(&amp;tool.annotations, &amp;live_ann.annotations);\n                if !diff.is_empty() {\n                    drifts.push(DriftEvent { module: module.clone(), tool_id: tool.tool_id.clone(), diff });\n                }\n            }\n        }\n    }\n    for d in &amp;drifts {\n        emit_audit(ctx, \"mcp.tool_gating_annotation_drift\", json!(d)).await;\n    }\n    Ok(drifts)\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>FR-MCP-001</strong> spec compliance — tools/call dispatch path; gating intercepts here.</li></ul>\n<p><strong>Cross-module (related_frs):</strong></p>\n<ul><li><strong>FR-MCP-002</strong> Per-module registration — annotations stored in registry.</li><li><strong>FR-MCP-003</strong> SEP-986 naming — tool_id format validated in policy.</li><li><strong>FR-MCP-004</strong> OAuth 2.1 PKCE — JWT bearer with scope_grants for bypass.</li><li><strong>FR-MCP-005</strong> PRM — gating-supported tool scopes appear in per-module PRM.</li><li><strong>FR-MCP-007</strong> Tasks primitive — destructive tasks confirm at start; subsequent polls don't re-confirm.</li><li><strong>FR-MCP-008</strong> Elicitation — <code>elicit</code> mode delegates here (slice-2 placeholder).</li><li><strong>FR-AUTH-004</strong> JWT mint — bypass-scope guard at mint.</li><li><strong>FR-AI-003</strong> memory audit-row bridge — 5 new kinds.</li><li><strong>FR-MEMORY-111</strong> PII scrubbing — payload SHA only in chain.</li><li><strong>FR-OBS-007</strong> Auto-runbook — sev-1 drift + policy events route to CHAT.</li></ul>\n<p><strong>Downstream (blocks):</strong> None at this slice.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>mcp.tool_gating_decision</code> memory row (rejected)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"mcp.tool_gating_decision\",\n  \"severity\": 3,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.mcp.gating\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"caller_subject_id\": \"7c4e...\",\n    \"tool_id\": \"cyberos.docs.delete_document\",\n    \"decision\": \"rejected_no_confirmation\",\n    \"annotations\": {\n      \"destructiveHint\": true, \"openWorldHint\": false,\n      \"readOnlyHint\": false, \"idempotentHint\": false\n    },\n    \"gating_mode\": \"confirm\",\n    \"audit_only\": false,\n    \"request_payload_sha256\": \"9c4e7a8b6d2f1e3a...\"\n  }\n}</code></pre>\n<h3>8.2 403 confirm-required response</h3>\n<pre><code class=\"language-json\">{\n  \"jsonrpc\": \"2.0\",\n  \"error\": {\n    \"code\": -32000,\n    \"message\": \"confirm_required\",\n    \"data\": {\n      \"tool_id\": \"cyberos.docs.delete_document\",\n      \"confirm_endpoint\": \"https://api.cyberos.world/v1/mcp/tools/cyberos.docs.delete_document/confirm\",\n      \"expires_after_confirm_seconds\": 300\n    }\n  },\n  \"id\": 42\n}</code></pre>\n<h3>8.3 Policy YAML</h3>\n<pre><code class=\"language-yaml\">version: 1\ndefault_mode: confirm\naudit_only: false\noverrides:\n  \"cyberos.docs.delete_document\":\n    mode: elicit\n  \"cyberos.projects.list_issues\":\n    mode: auto_confirm\n  \"cyberos.crm.bulk_email\":\n    mode: elicit</code></pre>\n<h3>8.4 <code>mcp.tool_gating_annotation_drift</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"mcp.tool_gating_annotation_drift\",\n  \"severity\": 1,\n  \"tenant_id\": \"00000000-0000-0000-0000-000000000001\",\n  \"actor_id\": \"system.mcp.drift_detector\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T03:00:12.118Z\",\n  \"payload\": {\n    \"module\": \"docs\",\n    \"tool_id\": \"cyberos.docs.export_audit\",\n    \"registry_annotations\": {\n      \"readOnlyHint\": true, \"destructiveHint\": false,\n      \"idempotentHint\": true, \"openWorldHint\": false\n    },\n    \"live_annotations\": {\n      \"readOnlyHint\": false, \"destructiveHint\": false,\n      \"idempotentHint\": true, \"openWorldHint\": true\n    },\n    \"diff_fields\": [\"readOnlyHint\", \"openWorldHint\"]\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 2. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Per-caller policy overrides (one user trusted, another not) — slice 3.</li><li><strong>Deferred:</strong> Time-bounded bypass scopes (bypass for next 1h) — slice 3.</li><li><strong>Deferred:</strong> Policy templates (compliance presets: HIPAA-strict, SOC2-balanced) — slice 3.</li><li><strong>Deferred:</strong> Confirmation via Out-Of-Band channel (Slack approval) — slice 3, FR-MCP-2xx.</li><li><strong>Deferred:</strong> Multi-actor confirmation (M-of-N approval for destructive ops) — slice 3.</li><li><strong>Deferred:</strong> Dry-run mode (show what gating WOULD do without actually invoking) — slice 3.</li><li><strong>Deferred:</strong> UI for policy editing (vs YAML) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Tool registered without annotations</td><td>registry validation</td><td>Reject registration with <code>missing_annotations</code></td><td>Module fixes registration</td></tr><tr><td>Tool annotations malformed (non-boolean values)</td><td>schema validation</td><td>Reject with <code>invalid_annotation_value</code></td><td>Module fixes</td></tr><tr><td>Confirm token consumed by different caller</td><td>UNIQUE constraint scoped to caller_subject_id</td><td>Original caller's confirm not affected; cross-caller confirm impossible</td><td>Inherent isolation</td></tr><tr><td>Pending confirmations table grows unbounded</td><td>scheduled prune at expires_at</td><td>TTL prune via cyberos_pruner role daily</td><td>Inherent — prune job</td></tr><tr><td>Policy YAML parse error</td><td>YAML parser</td><td>400 + <code>policy_yaml_invalid</code> + line/column hint</td><td>tenant_admin fixes YAML</td></tr><tr><td>Policy YAML references unknown tool_id</td><td>validation against registry</td><td>400 + <code>unknown_tool_id</code> + tool_id</td><td>Use valid tool_id from registry list</td></tr><tr><td>Bypass scope granted to non-system tenant</td><td>JWT mint guard</td><td>mint rejected <code>invalid_scope_for_tenant</code></td><td>Caller's tenant cannot have bypass; admin re-mints without</td></tr><tr><td>Audit-only mode set + later policy change forgets to disable</td><td>activation pointer + audit</td><td>Tenant_admin sees <code>audit_only=true</code> indicator in dashboard</td><td>Tenant_admin reviews; flips to enforced</td></tr><tr><td>Elicit mode requested but FR-MCP-008 not shipped</td><td>mode resolver</td><td>503 + <code>elicitation_not_yet_supported</code></td><td>Tenant_admin temporarily falls back to confirm mode</td></tr><tr><td>Drift detector finds widespread drift (module re-registered)</td><td>drift event count threshold</td><td>sev-1 alert; ops review</td><td>Module-side fix + re-register</td></tr><tr><td>Tool removed from registry but cached gating decision references it</td><td>cache invalidation on registry change</td><td>Cached decision rejected on next use; re-resolved</td><td>Inherent — cache TTL</td></tr><tr><td>Confirm endpoint rate-limit hit</td><td>sliding-window</td><td>429 + Retry-After</td><td>Caller backs off; investigate misbehaving client</td></tr><tr><td>JWT scope_grants tampered (impossible with signed JWTs)</td><td>signature verify</td><td>401 at JWT validate</td><td>Inherent JWT security</td></tr><tr><td>Same tool registered with different annotations across module instances</td><td>drift detector</td><td>sev-1 with all instances' annotations</td><td>Module deployment process audit</td></tr><tr><td><code>mcp_pending_confirmations</code> table missing index</td><td>slow query OBS alert</td><td>sev-3; query degraded</td><td>Migration adds index</td></tr><tr><td>Cross-tenant policy lookup attempt</td><td>RLS scope</td><td>Returns 0 rows; falls through to default global policy</td><td>Inherent isolation</td></tr><tr><td>Empty annotations on tool (all false)</td><td>resolver</td><td>Default to policy <code>confirm</code> mode (conservative)</td><td>Module should explicitly set hints</td></tr><tr><td>Tenant has no policy row</td><td>resolver</td><td>Falls through to platform-default <code>confirm</code></td><td>tenant_admin creates policy</td></tr><tr><td>Policy version rollback to a previous version</td><td>activate handler</td><td>UPSERT active_policy_id; old version still in table</td><td>Inherent versioning</td></tr><tr><td>Audit log row insert fails post-decision</td><td>Postgres error</td><td>Sev-2 alert; decision proceeds (FAIL-OPEN on audit — alternative is FAIL-CLOSED which denies all tool calls on audit issue)</td><td>Operator investigates Postgres</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> The decision algorithm is pure (no I/O); separable for testing without mocks. Side effects (audit emit, consume row) factored into outer orchestrator.</p>\n<p><strong>§11.2</strong> Pending confirmations are pruned by <code>cyberos_pruner</code> role's DELETE grant + scheduled job (same pattern as FR-TEN-003 <code>stripe_api_calls</code>).</p>\n<p><strong>§11.3</strong> Bypass scope is granted ONLY via FR-AUTH-004's admin mint endpoint with explicit operator approval; not a self-service grant.</p>\n<p><strong>§11.4</strong> Policy YAML validation includes tool_id existence check against current registry; future tools added after policy save are subject to default policy until policy updated.</p>\n<p><strong>§11.5</strong> Drift detector schedule: nightly at 03:00 UTC (low-traffic window); runs &lt;2 min for 100 modules × 50 tools.</p>\n<p><strong>§11.6</strong> The <code>idempotentHint</code> annotation is informational only per spec. Documented in audit row's <code>annotations</code> JSONB so operators see the full picture.</p>\n<p><strong>§11.7</strong> Annotation-precedence algorithm: documented + tested + reviewed — the 4-line resolver in §6.1 is THE algorithm; no alternative paths.</p>\n<p><strong>§11.8</strong> Confirm token request_payload_sha256 binding uses SHA-256 of canonical-JSON of payload; ensures key-order-independent matching.</p>\n<p><strong>§11.9</strong> Per-tenant policy hot-reload via NATS subject <code>cyberos.mcp.gating_policy.updated.&lt;tenant_slug&gt;</code>; in-memory cache invalidated on receipt + reload from Postgres.</p>\n<p><strong>§11.10</strong> Decision audit at sev-3 sampled at 1% via FR-OBS-006 tail-sampling for routine flow; rejected decisions emitted at 100% (security-relevant).</p>\n<p><strong>§11.11</strong> The <code>confirm_token</code> returned by POST confirm is the row's <code>id</code>; opaque to callers; not interpretable.</p>\n<p><strong>§11.12</strong> Elicit mode at slice 2 returns 503 not 501 (501 \"Not Implemented\" would be lying — it IS implemented as a placeholder); 503 \"Service Unavailable\" matches the \"elicitation server not yet running\" semantic.</p>\n<p><strong>§11.13</strong> Per-tenant audit-only mode is a transition path; the audit-only setting itself has a <code>set_at</code> column (not added at slice 2; add at slice 3 if audit-only durations need tracking).</p>\n<p><strong>§11.14</strong> The drift detector compares JSONB annotations field-by-field; nullable fields (e.g., <code>idempotentHint</code> may be omitted in some clients) treated as <code>false</code> for diff purposes.</p>\n<p><strong>§11.15</strong> Bypass invocations are NOT rate-limited (system-tenant callers may legitimately invoke many gated tools); the audit row volume is the limit.</p>\n<p><strong>§11.16</strong> Policy YAML schema versioning: slice 2 <code>version: 1</code>; slice 3 may add <code>version: 2</code> with backward-compat parser.</p>\n<p><strong>§11.17</strong> The MCP JSON-RPC error code <code>-32000</code> is \"server error\" per JSON-RPC 2.0 spec §5.1; chosen for confirm_required because it's a server-side policy enforcement.</p>\n<p><strong>§11.18</strong> Annotation hint defaults per MCP spec: all 4 hints default <code>false</code>; omission of a hint = explicit <code>false</code>. Our resolver treats undefined as <code>false</code> consistently.</p>\n<p><strong>§11.19</strong> Per-tenant audit log retention: 90 days (matches FR-TEN-101 signup_sessions pattern); after 90d, summary statistics retained, individual rows pruned.</p>\n<hr>\n<p><em>End of FR-MCP-006 spec.</em></p>";
