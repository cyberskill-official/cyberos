window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-106"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The memory-sync daemon <strong>MUST</strong> enforce <code>meta.sync_class</code> filtering on every sync direction (push + pull + import + cross-memory merge). Each filter call:</p>\n<ol><li><strong>MUST</strong> push only <code>sync_class == \"shareable\"</code> rows to Cloud memory. Rows with <code>private | local-only</code> are filtered at push.</li><li><strong>MUST</strong> refuse to ingest a foreign row whose <code>sync_class != \"shareable\"</code> at pull time (defensive — Cloud memory should already filter; double-check at receive boundary).</li><li><strong>MUST</strong> consult <code>meta.acl[]</code> (allow-list of actor IDs) for shareable rows; if <code>acl</code> is non-empty AND current actor not listed, refuse. Empty <code>acl</code> = unrestricted (any actor authorised by Cloud sees it).</li><li><strong>MUST</strong> maintain transitional support for v1 <code>sync_class</code> values per AGENTS.md §15:</li></ol>\n<ul><li><code>local-only</code> → treat as <code>private</code> (refuse).</li><li><code>publishable | shared | client-visible</code> → treat as <code>shareable</code> (allow, subject to ACL). Transitional mapping logs INFO with the v1 value seen (operators monitor adoption); metric <code>memory_sync_v1_transitional_total{value}</code> counts.</li></ul>\n<ol><li><strong>MUST</strong> reject DEC-036 (compensation) and equity rows at sync boundary REGARDLESS of sync_class (structural exclusion as defense-in-depth — even mistakenly-tagged shareable comp rows are caught). Path-based detection on <code>meta/people/*/compensation*</code>, <code>meta/people/*/equity*</code>, <code>meta/finance/payroll*</code>, <code>meta/finance/comp*</code>.</li><li><strong>MUST</strong> emit memory audit row <code>memory.sync_row_filtered</code> per filter decision (push or pull) with payload: <code>direction</code> (push|pull), <code>seq</code>, <code>path</code>, <code>sync_class</code>, <code>reason</code> (enum: Private | AclMismatch | StructuralExclusion | V1TransitionalRefuse), <code>actor_id</code> (current syncing device).</li><li><strong>MUST</strong> be deterministic — same row + same actor + same time = same decision. No clock-dependent or RNG inputs.</li><li><strong>MUST</strong> be testable via property test: 10K random rows × 100 random actors × verify (a) zero compensation/equity ever crosses boundary; (b) zero private rows pushed; (c) ACL respected.</li><li><strong>MUST</strong> integrate into <code>services/memory-sync/src/sync.rs</code>'s push and pull loops; calls <code>should_sync()</code> BEFORE network IO.</li><li><strong>SHOULD</strong> support a <code>cyberos-memory validate-sync-class</code> CLI for operators: takes a row file as input; reports the SyncDecision; helps debug filter decisions.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>memory_sync_filter_decisions_total{direction, reason}</code> (counter).</li><li><code>memory_sync_v1_transitional_total{value}</code> (counter; track v1 adoption).</li><li><code>memory_sync_compensation_excluded_total</code> (counter; sev-1 alarm if &gt; 0/day; investigate upstream).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why structural compensation exclusion (DEC-036)?</strong> Compensation/equity is the highest-stakes data class. A single leak ends careers + companies. The <code>sync_class</code> field is the user's primary control, but bugs happen — a misclassified comp row tagged \"shareable\" would propagate. The structural-path-check is defense-in-depth: even if every other safeguard fails, the path-based reject catches comp rows.</p>\n<p><strong>Why filter at EVERY boundary (§1 #1 + #2 + #5)?</strong> Push filter prevents Cloud receiving private rows. Pull filter prevents local importing private rows. Both are necessary because Cloud memory's filtering bug could push private rows TO us — pull filter catches that. Defense-in-depth at every direction.</p>\n<p><strong>Why ACL on shareable rows (§1 #3)?</strong> Some shareable rows are tenant-wide (any actor with tenant access sees them). Others are role-scoped (only finance team, only legal team). The ACL allow-list lets the user be specific: <code>acl: [\"@alice\", \"@bob\"]</code> = only these actors. Empty acl = no restriction.</p>\n<p><strong>Why v1 transitional mapping (§1 #4)?</strong> Existing memories tagged with v1 values shouldn't break. Mapping <code>local-only → private</code>, <code>publishable|shared|client-visible → shareable</code> preserves intent. The INFO log + metric track v1 usage; eventual deprecation when v1 usage drops to zero.</p>\n<p><strong>Why audit-row per filter (§1 #6)?</strong> Compliance audits need to answer \"what was filtered out + why?\" Without the audit row, filter decisions are invisible. With the row, ops + auditors can answer the question via standard memory query.</p>\n<p><strong>Why property test (§1 #8)?</strong> Filter logic is the kind of thing that breaks silently. Compensation row with shareable tag + filter has a bug = leak. Property test 10K random rows × 100 actors × invariant assertions = high-confidence detection.</p>\n<p><strong>Why pre-network-IO call (§1 #9)?</strong> Filtering before network IO means private rows never leave the device. Filtering after would mean rows briefly transit before being rejected — observable to network sniffers, longer attack window.</p>\n<p><strong>Why dedicated <code>compensation_excluded</code> metric (§1 #11)?</strong> A compensation row arriving at sync boundary indicates upstream code emitted shareable comp — that's a bug. Sev-1 alarm forces investigation. Trending the metric quantifies upstream-code health.</p>\n<p><strong>Why CLI for ops dry-run (§1 #10)?</strong> Operators debugging \"why didn't this row sync?\" need a way to ask the filter directly. CLI takes a row, returns SyncDecision + reason + audit-row preview. Faster than reading logs.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/memory-sync/src/sync_class.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Deserialize, Serialize)]\n#[serde(rename_all = \"kebab-case\")]\npub enum SyncClass {\n    // Canonical (AGENTS.md §15) — the only two values new writers should emit:\n    Private,    // never leaves the local store\n    Shareable,  // MAY be exported; ACL field carries explicit allow-list\n\n    // v1 transitional — preserved for one release cycle for tooling that has\n    // not migrated (AGENTS.md §15). New writers MUST NOT emit these. Readers\n    // map them per §1 #4: local-only → Private; publishable | shared |\n    // client-visible → Shareable. Each occurrence is logged + counted so\n    // operators can track when v1 usage reaches zero and these variants\n    // can be dropped.\n    #[serde(rename = \"local-only\")]    LocalOnly,\n    Publishable,\n    Shared,\n    #[serde(rename = \"client-visible\")] ClientVisible,\n}\n\n#[derive(Debug, Clone, PartialEq)]\npub enum SyncDecision {\n    Allow,\n    Refuse(Reason),\n}\n\n#[derive(Debug, Clone, PartialEq, Serialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum Reason {\n    Private,\n    AclMismatch { actor: String, allowed: Vec&lt;String&gt; },\n    StructuralExclusion { path: String },\n    V1TransitionalRefuse,\n}\n\npub fn should_sync(row: &amp;MemoryRow, current_actor: &amp;str, direction: Direction) -&gt; SyncDecision {\n    // §1 #5 structural exclusion (defense in depth)\n    if structural_exclusion::is_compensation_or_equity(&amp;row.path) {\n        metrics::compensation_excluded(direction);\n        return SyncDecision::Refuse(Reason::StructuralExclusion { path: row.path.clone() });\n    }\n\n    let class = row.meta.get(\"sync_class\")\n        .and_then(|v| serde_yaml::from_value(v.clone()).ok())\n        .unwrap_or(SyncClass::Private);   // safe default\n\n    match class {\n        SyncClass::Private | SyncClass::LocalOnly =&gt; {\n            if class == SyncClass::LocalOnly {\n                tracing::info!(value = \"local-only\", \"v1 transitional sync_class\");\n                metrics::v1_transitional(\"local-only\");\n            }\n            SyncDecision::Refuse(Reason::Private)\n        }\n        SyncClass::Shareable | SyncClass::Publishable | SyncClass::Shared | SyncClass::ClientVisible =&gt; {\n            if !matches!(class, SyncClass::Shareable) {\n                tracing::info!(value = ?class, \"v1 transitional sync_class\");\n                metrics::v1_transitional(&amp;format!(\"{:?}\", class).to_lowercase());\n            }\n            // §1 #3 ACL evaluation\n            let acl: Vec&lt;String&gt; = row.meta.get(\"acl\")\n                .and_then(|v| serde_yaml::from_value(v.clone()).ok())\n                .unwrap_or_default();\n            if acl.is_empty() || acl.contains(&amp;current_actor.to_string()) {\n                SyncDecision::Allow\n            } else {\n                SyncDecision::Refuse(Reason::AclMismatch {\n                    actor: current_actor.into(), allowed: acl,\n                })\n            }\n        }\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-sync/src/structural_exclusion.rs\nconst FORBIDDEN_PATTERNS: &amp;[&amp;str] = &amp;[\n    \"meta/people/*/compensation*\",\n    \"meta/people/*/equity*\",\n    \"meta/finance/payroll*\",\n    \"meta/finance/comp*\",\n    \"memories/people/*/compensation*\",\n    \"memories/finance/equity*\",\n];\n\npub fn is_compensation_or_equity(path: &amp;str) -&gt; bool {\n    FORBIDDEN_PATTERNS.iter().any(|p| glob::Pattern::new(p).unwrap().matches(path))\n}</code></pre>\n<p>Integration in <code>sync.rs</code>:</p>\n<pre><code class=\"language-rust\">// Push path\nfor row in pending {\n    let decision = sync_class::should_sync(&amp;row, &amp;device_id, Direction::Push);\n    match decision {\n        SyncDecision::Allow =&gt; push_to_cloud(row).await?,\n        SyncDecision::Refuse(reason) =&gt; {\n            memory::emit(canonical::sync_row_filtered(&amp;row, Direction::Push, reason)).await?;\n            metrics::filter_decision(Direction::Push, &amp;reason);\n        }\n    }\n}\n\n// Pull path (defensive)\nfor remote in pulled {\n    let decision = sync_class::should_sync(&amp;remote, &amp;device_id, Direction::Pull);\n    match decision {\n        SyncDecision::Allow =&gt; import_to_local(remote).await?,\n        SyncDecision::Refuse(reason) =&gt; {\n            memory::emit(canonical::sync_row_filtered(&amp;remote, Direction::Pull, reason)).await?;\n            metrics::filter_decision(Direction::Pull, &amp;reason);\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><code>sync_class: private</code> row never pushed.</li><li><code>sync_class: shareable, acl: []</code> row pushed to Cloud.</li><li><code>sync_class: shareable, acl: [@alice]</code> row not pushed when current actor is @bob.</li><li><code>sync_class: shareable, acl: [@alice]</code> pushed when current actor is @alice.</li><li>Compensation path (<code>memories/people/alice/compensation.md</code>) refused regardless of sync_class.</li><li>Equity path refused regardless of sync_class.</li><li>v1 <code>local-only</code> treated as private (refuse).</li><li>v1 <code>publishable</code> treated as shareable (allow if ACL passes).</li><li>v1 <code>shared</code> treated as shareable (allow if ACL passes).</li><li>v1 <code>client-visible</code> treated as shareable.</li><li>v1 transitional values logged at INFO + metric increments.</li><li>Pull-side defensive: foreign row with <code>private</code> from Cloud → refused at pull.</li><li>Filter decisions emit <code>memory.sync_row_filtered</code> audit row with reason.</li><li>Property test: 10K random rows × 100 actors → 0 compensation/equity, 0 private pushed, ACL respected.</li><li>Deterministic: same row + same actor → same decision (run twice, assert equal).</li><li>CLI <code>cyberos-memory validate-sync-class --file row.yaml --actor @alice</code> returns SyncDecision JSON.</li><li>Sev-1 alarm on <code>memory_sync_compensation_excluded_total &gt; 0/day</code> (upstream bug).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[test]\nfn private_row_refused() {\n    let row = test_row_with_sync_class(\"private\");\n    let dec = sync_class::should_sync(&amp;row, \"@anyone\", Direction::Push);\n    assert!(matches!(dec, SyncDecision::Refuse(Reason::Private)));\n}\n\n#[test]\nfn shareable_empty_acl_allowed() {\n    let row = test_row_with_sync_class_and_acl(\"shareable\", &amp;[]);\n    let dec = sync_class::should_sync(&amp;row, \"@anyone\", Direction::Push);\n    assert_eq!(dec, SyncDecision::Allow);\n}\n\n#[test]\nfn shareable_with_acl_actor_in_list() {\n    let row = test_row_with_sync_class_and_acl(\"shareable\", &amp;[\"@alice\", \"@bob\"]);\n    let dec = sync_class::should_sync(&amp;row, \"@alice\", Direction::Push);\n    assert_eq!(dec, SyncDecision::Allow);\n}\n\n#[test]\nfn shareable_with_acl_actor_not_in_list() {\n    let row = test_row_with_sync_class_and_acl(\"shareable\", &amp;[\"@alice\"]);\n    let dec = sync_class::should_sync(&amp;row, \"@bob\", Direction::Push);\n    assert!(matches!(dec, SyncDecision::Refuse(Reason::AclMismatch { .. })));\n}\n\n#[test]\nfn compensation_path_refused_regardless_of_sync_class() {\n    let row = test_row_at_path(\"memories/people/alice/compensation.md\", \"shareable\");\n    let dec = sync_class::should_sync(&amp;row, \"@anyone\", Direction::Push);\n    assert!(matches!(dec, SyncDecision::Refuse(Reason::StructuralExclusion { .. })));\n}\n\n#[test]\nfn equity_path_refused_regardless_of_sync_class() {\n    let row = test_row_at_path(\"memories/finance/equity_grant_2026.md\", \"shareable\");\n    let dec = sync_class::should_sync(&amp;row, \"@anyone\", Direction::Push);\n    assert!(matches!(dec, SyncDecision::Refuse(Reason::StructuralExclusion { .. })));\n}\n\n#[test]\nfn v1_local_only_refused() {\n    let row = test_row_with_sync_class(\"local-only\");\n    let dec = sync_class::should_sync(&amp;row, \"@anyone\", Direction::Push);\n    assert!(matches!(dec, SyncDecision::Refuse(Reason::Private)));\n}\n\n#[test]\nfn v1_publishable_allowed() {\n    let row = test_row_with_sync_class(\"publishable\");\n    let dec = sync_class::should_sync(&amp;row, \"@anyone\", Direction::Push);\n    assert_eq!(dec, SyncDecision::Allow);\n}\n\n#[test]\nfn deterministic_same_inputs_same_decision() {\n    let row = test_row_with_sync_class(\"shareable\");\n    let d1 = sync_class::should_sync(&amp;row, \"@alice\", Direction::Push);\n    let d2 = sync_class::should_sync(&amp;row, \"@alice\", Direction::Push);\n    assert_eq!(d1, d2);\n}\n\n#[test]\nfn v1_transitional_metric_increments() {\n    let row = test_row_with_sync_class(\"publishable\");\n    let _ = sync_class::should_sync(&amp;row, \"@a\", Direction::Push);\n    let metric: u64 = otel_test_helper::counter_value(\"memory_sync_v1_transitional_total\", &amp;[(\"value\", \"publishable\")]);\n    assert_eq!(metric, 1);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-sync/tests/sync_class_property_test.rs\nuse proptest::prelude::*;\n\nproptest! {\n    #![proptest_config(ProptestConfig::with_cases(1000))]\n\n    #[test]\n    fn no_compensation_or_equity_ever_synced(\n        path in \"[a-z/_]{10,80}\",\n        sync_class in prop_oneof![\"private\", \"shareable\", \"local-only\", \"publishable\", \"shared\", \"client-visible\"],\n        actor in \"@[a-z]{3,12}\",\n    ) {\n        let row = test_row(path.clone(), &amp;sync_class, vec![]);\n        let dec = sync_class::should_sync(&amp;row, &amp;actor, Direction::Push);\n        if structural_exclusion::is_compensation_or_equity(&amp;path) {\n            prop_assert!(matches!(dec, SyncDecision::Refuse(Reason::StructuralExclusion { .. })));\n        }\n    }\n\n    #[test]\n    fn no_private_pushed(sync_class in prop_oneof![\"private\", \"local-only\"]) {\n        let row = test_row(\"memories/test.md\".into(), &amp;sync_class, vec![]);\n        let dec = sync_class::should_sync(&amp;row, \"@anyone\", Direction::Push);\n        prop_assert!(matches!(dec, SyncDecision::Refuse(Reason::Private)));\n    }\n\n    #[test]\n    fn acl_respected(\n        actor in \"@[a-z]{3,12}\",\n        allowed in prop::collection::vec(\"@[a-z]{3,12}\", 0..5),\n    ) {\n        let row = test_row(\"memories/test.md\".into(), \"shareable\", allowed.clone());\n        let dec = sync_class::should_sync(&amp;row, &amp;actor, Direction::Push);\n        if allowed.is_empty() || allowed.contains(&amp;actor) {\n            prop_assert_eq!(dec, SyncDecision::Allow);\n        } else {\n            prop_assert!(matches!(dec, SyncDecision::Refuse(Reason::AclMismatch { .. })));\n        }\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// CLI test\n#[test]\nfn cli_validate_sync_class_returns_json() {\n    let row_file = \"/tmp/test_row.yaml\";\n    std::fs::write(row_file, \"path: memories/test.md\\nmeta:\\n  sync_class: shareable\\n\").unwrap();\n    let output = std::process::Command::new(env!(\"CARGO_BIN_EXE_cyberos_memory\"))\n        .args(&amp;[\"validate-sync-class\", \"--file\", row_file, \"--actor\", \"@alice\"])\n        .output().unwrap();\n    let stdout = String::from_utf8_lossy(&amp;output.stdout);\n    let json: serde_json::Value = serde_json::from_str(&amp;stdout).unwrap();\n    assert_eq!(json[\"decision\"], \"allow\");\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-103</strong> — sync.rs integration point.</li><li><strong>FR-MEMORY-107 (downstream)</strong> — read-time ACL filtering uses similar logic.</li><li>Crates: <code>glob@0.3</code>, <code>serde</code>, <code>serde_yaml</code>, <code>proptest@1</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Filter decision audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.sync_row_filtered\",\n  \"payload\": {\n    \"direction\": \"push\",\n    \"seq\": 12345,\n    \"path\": \"memories/people/alice/compensation.md\",\n    \"sync_class\": \"shareable\",\n    \"reason\": { \"structural_exclusion\": { \"path\": \"memories/people/alice/compensation.md\" } },\n    \"actor_id\": \"@stephen\"\n  }\n}</code></pre>\n<h3>CLI validate</h3>\n<pre><code class=\"language-text\">$ cyberos-memory validate-sync-class --file row.yaml --actor @alice\n{\n  \"decision\": \"refuse\",\n  \"reason\": {\n    \"acl_mismatch\": {\n      \"actor\": \"@alice\",\n      \"allowed\": [\"@bob\"]\n    }\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Per-row encryption (slice 3+).</li><li>Time-bounded shareability (<code>shareable_until: 2026-12-31</code>) — slice 4+.</li><li>Group ACLs (<code>acl: [@team-finance]</code>) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Misclassified row (compensation marked shareable)</td><td>Structural exclusion catches</td><td>Refused; sev-1 metric</td><td>Engineer fixes upstream classifier</td></tr><tr><td>ACL list contains invalid actor</td><td>Treat as no-match (refuse)</td><td>Refused for that actor</td><td>Operator fixes ACL</td></tr><tr><td>Foreign row with v1 sync_class</td><td>Transition mapping applies</td><td>Allow/refuse per mapping</td><td>By design</td></tr><tr><td>sync_class missing</td><td>Default to private</td><td>Refused (safe default)</td><td>By design</td></tr><tr><td>ACL value malformed (not a list)</td><td>Parse error</td><td>Treat as empty (no restriction; row syncs)</td><td>Operator fixes ACL syntax</td></tr><tr><td>Property test finds violation</td><td>proptest panics in CI</td><td>PR blocked</td><td>Engineer fixes filter</td></tr><tr><td>should_sync called with wrong direction</td><td>wrong metric label</td><td>Sev-3 (incorrect attribution)</td><td>Engineer fixes call site</td></tr><tr><td>Glob pattern bug allows comp through</td><td>property test catches</td><td>PR blocked</td><td>Fix pattern</td></tr><tr><td>Path with unicode normalisation diff</td><td>structural exclusion misses</td><td>Sev-1 (compensation leak)</td><td>Add NFC normalisation pre-check</td></tr><tr><td>Compensation excluded sustained &gt; 0</td><td>sev-1 alarm</td><td>Investigate upstream</td><td>Standard process</td></tr><tr><td>Audit row emit fails</td><td>memory_writer error</td><td>Sev-2; row still filtered</td><td>Operator investigates memory</td></tr><tr><td>should_sync slow (regex compilation)</td><td>OTel histogram</td><td>Optimise: use lazy_static</td><td>Standard optimisation</td></tr><tr><td>New v1 transitional value introduced</td><td>Falls into default (private)</td><td>Refused; metric <code>unknown_value</code></td><td>Operator adds mapping</td></tr><tr><td>Future sync_class value (e.g., \"team-only\")</td><td>Falls into default</td><td>Refused</td><td>Add to enum + filter</td></tr><tr><td>Test fixture mismatched</td><td>unit test fails</td><td>PR blocked</td><td>Update fixture</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The structural exclusion is the load-bearing primitive. Without it, comp rows tagged shareable would propagate. With it, every sync direction has the path-based safety net.</li><li>v1 transitional support exists for migration; eventual deprecation via FR-MEMORY-204 (after v1 usage drops to &lt;1% of rows).</li><li>ACL evaluation is allow-list semantics: empty = unrestricted; non-empty = whitelist. No deny-list semantics (would be confusing alongside the existing reject-by-default model).</li><li>The <code>memory.sync_row_filtered</code> audit row is the compliance primitive. Auditors can answer \"what was filtered out?\" via standard memory query.</li><li>Property test 10K random rows × 100 actors ensures the filter logic is robust across the input space.</li><li>Default sync_class is <code>private</code> (safe default); operators must explicitly opt-in to sharing.</li><li>Sev-1 on compensation_excluded &gt; 0/day is conservative — even one such event is investigated.</li><li>The CLI dry-run helps operators debug filter decisions without poking at logs.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-106. Status: done (implemented 2026-05-23).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Shipped in python (modules/memory/core/sync_class.py).</p>";
