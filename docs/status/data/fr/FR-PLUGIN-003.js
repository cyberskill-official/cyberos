window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLUGIN-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PLUGIN module <strong>MUST</strong> ship 4 canonical slash-commands at <code>modules/plugin/commands/&lt;name&gt;.md</code>. Each command is a markdown file with YAML frontmatter binding the command to one or more MCP tools exposed by FR-PLUGIN-002.</p>\n<ol><li><strong>MUST</strong> ship exactly 4 commands in v1 per DEC-2421:</li></ol>\n<ul><li><code>/cyberos-run &lt;persona&gt; &lt;workflow&gt;</code> — execute a CUO workflow chain (calls <code>cyberos.cuo.execute_workflow</code>)</li><li><code>/cyberos-memory &lt;read|append&gt; ...</code> — read or append memory audit rows (calls <code>cyberos.memory.read_audit</code> or <code>cyberos.memory.append_audit</code>)</li><li><code>/cyberos-skill-list [filter]</code> — list available skills from catalog (calls <code>cyberos.skill.list_catalog</code>)</li><li><code>/cyberos-route &lt;query&gt;</code> — natural-language routing to persona+workflow (calls <code>cyberos.cuo.route</code>)</li></ul>\n<ol><li><strong>MUST</strong> use the frontmatter schema documented at <code>modules/plugin/commands/SCHEMA.md</code> (clause 6 below for the schema itself).</li></ol>\n<ol><li><strong>MUST</strong> mirror the underlying MCP tool's <code>input_schema</code> exactly per DEC-2423. A command argument list is derived from the tool's <code>properties</code> object; required-argument list is derived from the tool's <code>required</code> array. Authors MUST NOT inline alternative schemas.</li></ol>\n<ol><li><strong>MUST</strong> declare 4 example trigger phrases per command per FR-SKILL-111 + DEC-2424. Phrases live in the frontmatter <code>triggers:</code> array. Hosts that route by description match (Claude Code's skill router) use these to disambiguate.</li></ol>\n<ol><li><strong>MUST</strong> include a <code>description:</code> field of 60-480 characters per FR-SKILL-111. This is the host-rendered command summary. The 60-char floor forces meaningful copy; the 480-char ceiling forces conciseness.</li></ol>\n<ol><li><strong>MUST</strong> carry frontmatter conforming to this YAML shape: ```yaml --- name: &lt;slash-command-name&gt;           # without leading /, kebab-case description: &lt;60-480 char summary&gt; binds_to:</li></ol>\n<ul><li>tool: cyberos.&lt;module&gt;.&lt;verb&gt;_&lt;noun&gt;   # SEP-986 from FR-PLUGIN-001 when: &lt;natural-language disambiguator OR \"always\"&gt; arguments:</li><li>name: &lt;arg-name&gt; description: &lt;human-friendly&gt; required: &lt;true|false&gt; type: &lt;string|number|boolean|object|array&gt; triggers:</li><li>\"&lt;example user input that would invoke this command&gt;\"</li><li>\"&lt;another example&gt;\"</li><li>\"&lt;third example&gt;\"</li><li>\"&lt;fourth example, including edge-case phrasing&gt;\" destructive: &lt;true|false&gt;            # if any bound tool is destructive (per FR-MCP-006) --- ```</li></ul>\n<ol><li><strong>MUST</strong> validate via <code>modules/plugin/tests/test_commands_*.py</code> that:</li></ol>\n<ul><li>Every command file parses as YAML frontmatter + markdown body</li><li>Every <code>binds_to[*].tool</code> matches a tool name registered in FR-PLUGIN-002's static registry</li><li>Every command's <code>arguments[]</code> is a subset (by name + type) of the bound tool's input_schema</li><li>Every command has <code>len(description) ∈ [60, 480]</code></li><li>Every command has <code>len(triggers) == 4</code></li></ul>\n<ol><li><strong>MUST</strong> include a body section per command that explains: when to invoke, what scopes are required, what side-effects occur, and a worked example. Body is rendered in the host's command-detail view (Claude Code's <code>?</code> button on a command).</li></ol>\n<ol><li><strong>MUST NOT</strong> declare a tool binding to a non-existent tool (validated by test_commands_bind_to_valid_tools).</li></ol>\n<ol><li><strong>MUST NOT</strong> add new commands without a successor FR (FR-PLUGIN-003a, etc.) per DEC-2421.</li></ol>\n<ol><li><strong>MUST NOT</strong> rename a command between v1.x.y releases — rename = breaking change, requires major bump.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why markdown + frontmatter (clause 2)?</strong> Markdown commands are the standard format Claude Code expects (per Anthropic Agent Skills spec) and Cowork rendering uses. YAML frontmatter gives structured metadata (bindings, triggers) while keeping the body rich for human readers.</p>\n<p><strong>Why exactly 4 commands in v1 (DEC-2421)?</strong> Each command is a learnable UI primitive — too many and users can't remember what's available. The 4 cover orchestration (run + route), memory (memory read/append), and discovery (skill-list). Future commands land via successor FRs after usage data.</p>\n<p><strong>Why mirror tool input_schema (DEC-2423, clause 3)?</strong> Two sources of truth for the same shape drift. The manifest schema (FR-PLUGIN-001) is canonical. Commands MUST derive from it.</p>\n<p><strong>Why 4 trigger phrases (DEC-2424, clause 4)?</strong> FR-SKILL-111 calibrated 4 as the minimum count that gives the description-match router enough fingerprint to disambiguate. Two phrases is too noisy; eight is overkill for a slash command that the user already typed.</p>\n<p><strong>Why 60-480 char description (clause 5)?</strong> Same as FR-SKILL-111 description-enrichment range. Below 60 = unhelpful blurb; above 480 = wall-of-text in cramped UI panels.</p>\n<p><strong>Why destructive flag in frontmatter (clause 6)?</strong> Hosts surface destructive-command warnings differently. The flag is the host's hook to render that UI. Without it, every command looks the same and users invoke destructive ops without warning.</p>\n<p><strong>Why body section with worked example (clause 8)?</strong> Slash commands are user-facing. The body teaches the user how to use the command — required scopes, expected behaviour, sample output. Without the body, hosts surface a stub that says \"no description provided\" and adoption stalls.</p>\n<p><strong>Why no command renames within v1.x.y (clause 11)?</strong> Renames break user muscle memory and any scripts that invoke commands by name. Rename = major version bump.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3><code>commands/SCHEMA.md</code> (excerpt)</h3>\n<pre><code class=\"language-yaml\"># CyberOS slash-command frontmatter — v1 contract\nname: string                          # required, kebab-case, no leading /\ndescription: string                   # required, 60-480 chars\nbinds_to:                             # required, ≥1 entry\n  - tool: string                      # required, SEP-986 pattern\n    when: string                      # optional; \"always\" if omitted\narguments:                            # optional; subset of bound tool's input_schema\n  - name: string\n    description: string\n    required: boolean\n    type: enum[string,number,boolean,object,array]\ntriggers:                             # required, exactly 4 entries\n  - string\ndestructive: boolean                  # required; true if any bound tool has destructive: true</code></pre>\n<h3><code>commands/cyberos-run.md</code> (excerpt)</h3>\n<pre><code class=\"language-markdown\">---\nname: cyberos-run\ndescription: Execute a CyberOS workflow chain for a chosen persona. Used when the user wants to run a structured multi-step process (e.g. architect a system, prepare an investor update) and benefit from CUO's persona-aware orchestration with memory audit emission.\nbinds_to:\n  - tool: cyberos.cuo.execute_workflow\n    when: always\narguments:\n  - name: persona\n    description: Persona slug (e.g. chief-technology-officer)\n    required: true\n    type: string\n  - name: workflow\n    description: Workflow slug (e.g. architect-new-system)\n    required: true\n    type: string\n  - name: inputs\n    description: Workflow-specific input parameters as JSON\n    required: false\n    type: object\ntriggers:\n  - \"/cyberos-run chief-technology-officer architect-new-system\"\n  - \"Run the architect-new-system workflow for the CTO\"\n  - \"Execute CUO workflow\"\n  - \"Kick off the ADR-quick-capture flow\"\ndestructive: false\n---\n\n## When to use\n\nUse this command when you have a CUO-defined workflow ready to execute end-to-end. Workflows are\nmulti-step chains that run through the CyberOS supervisor and emit memory audit rows for every step.\n\n## Required scopes\n\n- `cyberos:cuo:execute`\n- `cyberos:memory:write` (for audit emission)\n\n## Side effects\n\n- Spawns one Task per invocation (long-running, async)\n- Emits 1× `plugin.invoked` audit row at task start\n- Emits N× `cuo.step_completed` audit rows during execution\n- Emits 1× `cuo.workflow_completed` audit row at finish\n\n## Example\n\n```text\nYou: /cyberos-run chief-technology-officer adr-quick-capture\nPlugin: Started task t-abc123. Polling status...\nPlugin: Step 1/3 complete — issue_authored\nPlugin: Step 2/3 complete — adr_drafted\nPlugin: Step 3/3 complete — adr_published\nPlugin: Workflow completed in 4.2s. ADR-2402 published.</code></pre>\n<pre><code>\n### Frontmatter validators (Python pseudocode)\n\n```python\ndef validate_command_file(path: Path) -&gt; List[ValidationError]:\n    fm, body = parse_frontmatter(path.read_text())\n    errors = []\n    if not 60 &lt;= len(fm[\"description\"]) &lt;= 480:\n        errors.append((\"description\", \"length out of [60,480]\"))\n    if len(fm[\"triggers\"]) != 4:\n        errors.append((\"triggers\", f\"expected 4, got {len(fm['triggers'])}\"))\n    for bind in fm[\"binds_to\"]:\n        if bind[\"tool\"] not in REGISTERED_TOOL_NAMES:\n            errors.append((\"binds_to\", f\"tool '{bind['tool']}' not in FR-PLUGIN-002 registry\"))\n    return errors</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Exactly 4 command files exist</strong> — <code>ls modules/plugin/commands/cyberos-*.md | wc -l</code> → 4.</li><li><strong>Every command has parseable YAML frontmatter</strong> — test parses each file.</li><li><strong>Every command's description is 60-480 chars</strong> — test asserts length.</li><li><strong>Every command has exactly 4 triggers</strong> — test asserts <code>len(triggers) == 4</code>.</li><li><strong>Every binds_to.tool exists in FR-PLUGIN-002 registry</strong> — test loads tool list, checks each bind.</li><li><strong>Every argument name + type is a subset of bound tool's input_schema</strong> — test loads manifest, compares.</li><li><strong>/cyberos-run binds to cyberos.cuo.execute_workflow</strong> — explicit fixture check.</li><li><strong>/cyberos-memory binds to cyberos.memory.read_audit AND cyberos.memory.append_audit</strong> — explicit fixture check (2 bindings with <code>when</code> disambiguators).</li><li><strong>/cyberos-skill-list binds to cyberos.skill.list_catalog</strong> — explicit fixture check.</li><li><strong>/cyberos-route binds to cyberos.cuo.route</strong> — explicit fixture check.</li><li><strong>/cyberos-memory append variant has destructive: true</strong> — test asserts.</li><li><strong>Other 3 commands have destructive: false</strong> — test asserts.</li><li><strong>Frontmatter validator rejects missing description</strong> — fixture file fails validation.</li><li><strong>Frontmatter validator rejects 3 triggers</strong> — fixture file fails validation.</li><li><strong>Frontmatter validator rejects unknown tool binding</strong> — fixture with <code>tool: foo.bar.baz</code> fails.</li><li><strong>Body section has worked example</strong> — test grep-asserts <code>## Example</code> appears in every command body.</li><li><strong>Body section lists required scopes</strong> — test grep-asserts <code>## Required scopes</code> appears in every command body.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/plugin/tests/test_commands_have_frontmatter.py\nfrom pathlib import Path\nimport yaml\n\nCOMMANDS_DIR = Path(__file__).parent.parent / \"commands\"\n\ndef test_exactly_four_commands():\n    files = sorted(COMMANDS_DIR.glob(\"cyberos-*.md\"))\n    assert len(files) == 4, f\"expected 4, got {len(files)}: {[f.name for f in files]}\"\n\ndef test_each_has_yaml_frontmatter():\n    for f in COMMANDS_DIR.glob(\"cyberos-*.md\"):\n        raw = f.read_text()\n        assert raw.startswith(\"---\\n\"), f\"{f.name} missing frontmatter\"\n        fm_end = raw.find(\"\\n---\\n\", 4)\n        fm = yaml.safe_load(raw[4:fm_end])\n        assert \"name\" in fm and \"description\" in fm and \"binds_to\" in fm</code></pre>\n<pre><code class=\"language-python\"># modules/plugin/tests/test_commands_bind_to_valid_tools.py\nREGISTERED = {\n    \"cyberos.cuo.list_personas\", \"cyberos.cuo.list_workflows\",\n    \"cyberos.cuo.route\", \"cyberos.cuo.execute_workflow\",\n    \"cyberos.memory.read_audit\", \"cyberos.memory.append_audit\",\n    \"cyberos.skill.list_catalog\", \"cyberos.skill.invoke_skill\",\n}\n\ndef test_all_bindings_exist():\n    for f in COMMANDS_DIR.glob(\"cyberos-*.md\"):\n        fm = load_frontmatter(f)\n        for bind in fm[\"binds_to\"]:\n            assert bind[\"tool\"] in REGISTERED, \\\n                f\"{f.name}: tool '{bind['tool']}' not in FR-PLUGIN-002 registry\"</code></pre>\n<pre><code class=\"language-python\"># modules/plugin/tests/test_commands_description_length.py\ndef test_description_length_in_range():\n    for f in COMMANDS_DIR.glob(\"cyberos-*.md\"):\n        fm = load_frontmatter(f)\n        n = len(fm[\"description\"])\n        assert 60 &lt;= n &lt;= 480, f\"{f.name}: description length {n} not in [60,480]\"</code></pre>\n<pre><code class=\"language-python\"># modules/plugin/tests/test_commands_trigger_count.py\ndef test_each_has_four_triggers():\n    for f in COMMANDS_DIR.glob(\"cyberos-*.md\"):\n        fm = load_frontmatter(f)\n        assert len(fm[\"triggers\"]) == 4, \\\n            f\"{f.name}: expected 4 triggers, got {len(fm['triggers'])}\"\n        for t in fm[\"triggers\"]:\n            assert isinstance(t, str) and len(t) &gt; 0</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>Markdown commands are content, not code. The skeleton is: write each of 4 files following the SCHEMA.md frontmatter contract; write 4 validator tests; wire commands into the manifest's <code>commands[]</code> array.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> FR-PLUGIN-001 (manifest schema declares <code>commands</code> array shape), FR-PLUGIN-002 (provides the bound tools).</li><li><strong>Downstream:</strong> FR-PLUGIN-007 (adapters export commands to per-runtime format — Claude Code reads <code>.md</code> directly; Cursor doesn't render commands but ignores them).</li><li><strong>Cross-module:</strong> FR-SKILL-111 (description enrichment discipline, shipped), FR-SKILL-112 (TRIGGER_TESTS.md pattern — the <code>triggers:</code> array follows the same shape); FR-MCP-006 (destructive flag mirrors tool annotation).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<p>(See §3 for full /cyberos-run example.)</p>\n<p>Manifest <code>commands</code> array entry:</p>\n<pre><code class=\"language-json\">{\n  \"name\": \"/cyberos-run\",\n  \"file\": \"commands/cyberos-run.md\",\n  \"description\": \"Execute a CyberOS workflow chain for a chosen persona.\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<ul><li>~~Should commands support typed argument completion?~~ → Yes, via <code>arguments[].type</code> per clause 6. Hosts that support tab-complete (Claude Code) use the type hint.</li><li>~~Should /cyberos-memory be one command or split into /cyberos-memory-read and /cyberos-memory-append?~~ → One command with mode prefix per clause 1. Symmetric with how /cyberos-route handles both reads and writes via inputs.</li><li>~~Should we add /cyberos-doctor for the bundle health check?~~ → No, FR-PLUGIN-001 ships <code>cyberos-plugin doctor</code> as a CLI; not surfaced as a slash command. Successor FR may add if user demand emerges.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Command file missing frontmatter</td><td>parser raises</td><td>validator test fails</td><td>Author adds <code>---</code> fences</td></tr><tr><td>Frontmatter YAML invalid</td><td>yaml.safe_load raises</td><td>validator test fails</td><td>Author fixes YAML</td></tr><tr><td>Description too short</td><td>length check</td><td>validator test fails</td><td>Author expands to ≥60 chars</td></tr><tr><td>Description too long</td><td>length check</td><td>validator test fails</td><td>Author trims to ≤480 chars</td></tr><tr><td>Triggers count != 4</td><td>length check</td><td>validator test fails</td><td>Author adds/removes triggers</td></tr><tr><td>Tool binding doesn't exist</td><td>set membership</td><td>validator test fails</td><td>Author fixes tool name OR ships FR-PLUGIN-002a to add the tool</td></tr><tr><td>Argument type mismatch</td><td>type check vs input_schema</td><td>validator test fails</td><td>Author fixes argument type</td></tr><tr><td>Destructive flag missing on append-memory command</td><td>explicit fixture assertion</td><td>validator test fails</td><td>Author adds <code>destructive: true</code></td></tr><tr><td>New command added without FR</td><td>manifest commands[] grows</td><td>validator test fails (5 instead of 4)</td><td>Author rolls back OR ships FR-PLUGIN-003a</td></tr><tr><td>Command rename within v1</td><td>git diff on filename</td><td>manual review caught at PR</td><td>Revert or bump to v2</td></tr><tr><td>Host renders empty command body</td><td>manual smoke test</td><td>UX bug surfaces in install</td><td>Author adds body</td></tr><tr><td>Body missing worked example</td><td>grep test for <code>## Example</code></td><td>validator test fails</td><td>Author adds Example section</td></tr><tr><td>Two commands with same name</td><td>filesystem uniqueness</td><td>install fails</td><td>inherent — filenames unique</td></tr><tr><td>Trigger phrase duplicate across commands</td><td>not detected (acceptable)</td><td>host router may misroute</td><td>Authors choose distinct phrasings</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 <strong>Why no slash-command schema in <code>manifest.schema.json</code> is needed.</strong> The manifest just lists <code>{name, file, description}</code> per command. The full frontmatter contract lives in the markdown file itself and is validated by the tests in this FR.</li></ul>\n<ul><li>§11.2 <strong>Why <code>name</code> in frontmatter doesn't include leading <code>/</code>.</strong> Hosts that render commands prepend the <code>/</code> themselves. Storing it without the slash keeps the value usable as a slug in URLs and filesystem paths.</li></ul>\n<ul><li>§11.3 <strong>Why <code>when:</code> field on <code>binds_to[*]</code>.</strong> A command can bind to multiple tools (e.g. /cyberos-memory binds to both read and append). The <code>when:</code> natural-language disambiguator helps the host route to the right tool based on the user's argument shape (<code>when: arguments.mode == \"append\"</code>).</li></ul>\n<ul><li>§11.4 <strong>Why triggers in YAML, not in body.</strong> Hosts that match descriptions extract triggers from frontmatter — body content is markdown for humans and isn't parsed for routing. Keeping triggers in frontmatter makes them machine-readable.</li></ul>\n<ul><li>§11.5 <strong>Test fixture for invalid commands.</strong> <code>modules/plugin/tests/fixtures/invalid_*.md</code> files exercise each rejection path so the validator suite covers happy + failure cases.</li></ul>\n<ul><li>§11.6 <strong>Cursor doesn't render slash commands.</strong> Cursor's MCP integration surfaces tools but not commands. The FR-PLUGIN-007 Cursor adapter omits the <code>commands/</code> directory from the Cursor bundle. The canonical command files still ship in the canonical bundle for hosts that do render them.</li></ul>\n<ul><li>§11.7 <strong>Claude Code custom command rendering.</strong> Claude Code reads markdown from a <code>.claude/commands/</code> directory (relative to project). The Claude Code adapter (FR-PLUGIN-007) copies <code>modules/plugin/commands/*.md</code> to <code>dist/.claude/commands/</code> in the packed bundle.</li></ul>\n<ul><li>§11.8 <strong>Future command expansion path.</strong> When usage shows demand for additional commands, ship FR-PLUGIN-003a covering each new command at 10/10 with the same validator coverage. Don't accumulate command sprawl.</li></ul>\n<hr>\n<p><em>End of FR-PLUGIN-003 spec.</em></p>";
