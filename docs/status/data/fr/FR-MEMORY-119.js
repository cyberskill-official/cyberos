window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-119"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The session-transcript ledger is an <strong>opt-in turn-level audit trail</strong> for agent-user conversations. Sessions are stored independently from the main audit chain (separate <code>sessions/</code> directory) but emit summary rows on the main chain so downstream consumers (FR-MEMORY-115 dream) can discover them. The contract:</p>\n<ol><li><strong>MUST</strong> support three CLI lifecycle commands:</li></ol>\n<ul><li><code>cyberos session start --id &lt;slug&gt; [--classification confidential|restricted] [--retention-days &lt;N&gt;]</code> — emits <code>session.start</code> row on the main chain + creates <code>sessions/&lt;YYYY-MM-DD&gt;/&lt;id&gt;.binlog.zst</code></li><li><code>cyberos session append --id &lt;slug&gt; --role {user|assistant|system|tool} --content &lt;text&gt;</code> — emits <code>session.turn</code> row into the session's own binlog</li><li><code>cyberos session end --id &lt;slug&gt; [--reason &lt;text&gt;]</code> — emits <code>session.end</code> row on the main chain + seals the session's binlog (compresses, computes final chain hash, marks immutable)</li></ul>\n<ol><li><strong>MUST</strong> store session bodies at <code>&lt;memory-root&gt;/sessions/&lt;YYYY-MM-DD&gt;/&lt;id&gt;.binlog.zst</code> — date-partitioned for easy retention purge. The binlog format mirrors §6.2 (length-prefixed framed records) so the existing reader code paths can parse it.</li><li><strong>MUST</strong> default <code>classification: confidential</code> per DEC-251. Operators MAY override per-session with <code>--classification restricted</code> to force encryption envelope per §5.4. The <code>public</code> and <code>internal</code> classifications are NOT permitted on sessions (they'd undermine the whole purpose of the transcript being sensitive).</li><li><strong>MUST</strong> when <code>classification: restricted</code>, encrypt every <code>session.turn</code> payload's <code>content</code> field via the §5.4 envelope. The meta-frame (role, ts, turn_seq) stays plaintext for fast filtering without decryption.</li><li><strong>MUST</strong> validate session lifecycle invariants via walker rule <code>session-lifecycle-well-formed</code>:</li></ol>\n<ul><li>Every <code>session.start</code> matched by 0 or 1 <code>session.end</code> (in-flight sessions are valid; double-end is not)</li><li>Within one session's binlog: <code>turn_seq</code> strictly monotonically increasing from 0 to N</li><li>No <code>session.turn</code> row precedes its <code>session.start</code> or follows its <code>session.end</code></li></ul>\n<ol><li><strong>MUST</strong> reject <code>append</code> for a session id that has no preceding <code>start</code> OR has a preceding <code>end</code>. Error message names the violated state.</li><li><strong>MUST</strong> support <code>cyberos session read --id &lt;slug&gt; [--decrypt]</code> to render a session's transcript as a sequence of turn entries. <code>--decrypt</code> is required for <code>restricted</code> sessions; without the flag, restricted sessions display turn metadata only (\"[encrypted content; --decrypt to read]\").</li><li><strong>MUST</strong> support <code>cyberos session list [--since 24h] [--classification ...] [--ended {true|false|all}]</code> to enumerate sessions. Default returns last 24h of all sessions.</li><li><strong>MUST</strong> support retention via <code>manifest.json:sessions.retention_days</code> (default = 30 per DEC-252). After expiry, <code>cyberos session purge-expired</code> (run on cron) drops the body via <code>delete(purge)</code> semantics — the session.start / session.end summary rows on the main chain remain (with the body's hash as proof-of-content-having-existed); the session's <code>.binlog.zst</code> file is overwritten with a tombstone manifest.</li><li><strong>MUST</strong> emit <code>session.purged</code> audit row on the main chain when a session's body is dropped. Payload: <code>{session_id, original_started_at, original_ended_at, turns_count, purged_at, reason: \"retention_expired\"|\"manual\"}</code>.</li><li><strong>MUST</strong> honour FR-MEMORY-117 store-ACL for the <code>sessions/</code> subtree — operators can set a <code>sessions/STORE.yaml</code> to restrict which actors can <code>start</code>/<code>append</code>/<code>end</code> sessions. Reads remain unrestricted at the protocol level (same DEC-232).</li><li><strong>MUST</strong> require the AGENTS.md §18 amendment APPROVED via <code>APPROVE protocol change P22 §18</code> chat-turn before sessions can be started. Writer checks for the §18 anchor at construction; absent → <code>cyberos session start</code> raises with the structured error.</li><li><strong>MUST</strong> include a <code>session_id</code> field on every memory write that originated during an active session (i.e. the agent was in a session AND made memory writes during it). The writer reads the current active session id from <code>&lt;memory-root&gt;/sessions/.active</code> (a lock-file-style pointer) and attaches it as <code>extra.session_id</code> on the put/move/delete row. This binds memory writes to the transcript that produced them.</li><li><strong>MUST</strong> allow <code>cyberos session append</code> to flag specific turn content as needing PII redaction by setting <code>--redactions-applied true</code>; the FR-MEMORY-111 PII detector is invoked on the content before storage. The redaction outcome is recorded in the turn payload's <code>redactions_applied</code> field.</li><li><strong>SHOULD</strong> support <code>cyberos session export --id &lt;slug&gt; --format jsonl</code> for cross-system handoff. Slice-4 stretch.</li><li><strong>SHOULD</strong> support multi-session correlation via <code>--correlates-with &lt;other-id&gt;</code> to link related sessions (e.g. a multi-step task spans two sessions). Slice-4+ stretch.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why opt-in, not always-on (§1 #1, DEC-250).</strong> Sessions add storage cost and operator complexity. Most cyberos invocations (single <code>put</code>, doctor checks, exports) are NOT conversations — there's nothing to transcribe. Forcing session lifecycle on every invocation is overhead the operator didn't ask for. Opt-in keeps the simple case simple; conversation-aware agents that NEED transcripts call <code>session start</code> explicitly.</p>\n<p><strong>Why default classification: confidential (§1 #3, DEC-251).</strong> Stephen explicitly chose this on 2026-05-19. Sessions contain raw user-agent dialogue; even when the topic is mundane, the dialogue itself is private context (who asked, when, what they wanted). <code>confidential</code> strikes the right balance: encryption envelope RECOMMENDED (§5.4) but not REQUIRED — operators can encrypt for high-stakes deployments, leave plaintext for casual local use. <code>restricted</code> (force-encrypt) is one flag away for sensitive deployments.</p>\n<p><strong>Why date-partitioned sessions directory (§1 #2).</strong> Three reasons. (a) Retention purge is <code>rm -rf sessions/2026-04-19/</code> once a date's retention expires — atomic and fast. (b) Disk usage by date is the natural operator question. (c) The session id is unique within a date; multiple operators on the same machine can't collide unless they pick the same id on the same day.</p>\n<p><strong>Why separate binlog instead of inlining into main audit (§1 #2, §1 #5).</strong> Two reasons. (a) Cardinality — a 1-hour conversation can produce 100+ turn rows; inlining would balloon the main chain and slow walker. (b) Privacy — sessions are confidential by default; the main chain might be shared (via <code>cyberos export</code>) while sessions are kept local. Separation enables differential sharing.</p>\n<p><strong>Why summary rows on main chain (§1 #1, §1 #10).</strong> FR-MEMORY-115 dream needs to <em>discover</em> sessions — it can't scan <code>sessions/</code> directly because it operates on chain rows. The summary rows (<code>session.start</code>, <code>session.end</code>, <code>session.purged</code>) live on the main chain and carry enough metadata (id, classification, turn count) for dream to decide whether to dive into a session's binlog for context.</p>\n<p><strong>Why lifecycle invariants enforced by walker (§1 #5, §1 #6, DEC-253).</strong> Append-without-start would create orphaned transcript fragments — no way to know whose conversation they came from. Double-end would be ambiguous about which <code>end</code> row \"won.\" Walker enforcement catches operator typos at <code>cyberos doctor</code> time rather than at dream-runner time (when the failure mode would be obscure).</p>\n<p><strong>Why <code>extra.session_id</code> on memory writes during active session (§1 #13).</strong> Closes the loop between transcript and memory. When the agent makes a <code>put</code> mid-conversation, the resulting audit row carries the session id; FR-MEMORY-120's <code>cyberos history &lt;path&gt;</code> can then say \"this memory was written during session X turn 17\" — directly answering \"what was the agent thinking when it wrote this?\" The pointer file <code>sessions/.active</code> is the simplest implementation; the alternative (passing session_id through every CLI call) is fragile.</p>\n<p><strong>Why retention default 30 days (DEC-252).</strong> Long enough for FR-MEMORY-115 dream to consume sessions on a daily cadence + retrospective debugging up to a month back. Short enough to limit privacy exposure. Operators can override per-store via manifest; high-compliance deployments set 7 days, research deployments set 365.</p>\n<p><strong>Why <code>session.purged</code> row on main chain (§1 #10).</strong> Audit-trail completeness. Operators reviewing \"did the agent have access to private user info on date X?\" need to answer \"is the transcript still readable?\" Even after purge, the <em>fact</em> of purge is on the chain — same pattern as <code>delete(purge)</code> from §3.6.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Schema fragment</h3>\n<pre><code class=\"language-json\">{\n  \"$defs\": {\n    \"SessionClassification\": {\n      \"type\": \"string\",\n      \"enum\": [\"confidential\", \"restricted\"]\n    },\n    \"SessionFrontmatter\": {\n      \"type\": \"object\",\n      \"required\": [\"id\", \"started_at\", \"classification\"],\n      \"properties\": {\n        \"id\":              {\"type\": \"string\", \"pattern\": \"^[a-z][a-z0-9_-]{0,63}$\"},\n        \"started_at\":      {\"type\": \"string\", \"format\": \"date-time\"},\n        \"ended_at\":        {\"type\": \"string\", \"format\": \"date-time\"},\n        \"ended_reason\":    {\"type\": \"string\"},\n        \"classification\":  {\"$ref\": \"#/$defs/SessionClassification\"},\n        \"retention_days\":  {\"type\": \"integer\", \"minimum\": 1, \"default\": 30},\n        \"actor\":           {\"type\": \"string\"},\n        \"correlates_with\": {\"type\": \"string\"}\n      }\n    },\n    \"SessionTurnPayload\": {\n      \"type\": \"object\",\n      \"required\": [\"session_id\", \"role\", \"turn_seq\", \"ts\"],\n      \"properties\": {\n        \"session_id\":         {\"type\": \"string\"},\n        \"role\":               {\"type\": \"string\", \"enum\": [\"user\", \"assistant\", \"system\", \"tool\"]},\n        \"content\":            {\"type\": \"string\"},\n        \"content_cipher\":     {\"$ref\": \"#/$defs/Envelope\"},\n        \"turn_seq\":           {\"type\": \"integer\", \"minimum\": 0},\n        \"ts\":                 {\"type\": \"string\", \"format\": \"date-time\"},\n        \"redactions_applied\": {\"type\": \"boolean\"}\n      },\n      \"oneOf\": [\n        {\"required\": [\"content\"]},\n        {\"required\": [\"content_cipher\"]}\n      ]\n    },\n    \"SessionAuditKind\": {\n      \"type\": \"string\",\n      \"enum\": [\"session.start\", \"session.end\", \"session.purged\"]\n    }\n  }\n}</code></pre>\n<h3>Session module</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/session.py\nfrom __future__ import annotations\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Literal, Optional\nimport zstandard\n\nfrom cyberos.core.writer import Writer\n\nClassification = Literal[\"confidential\", \"restricted\"]\nRole = Literal[\"user\", \"assistant\", \"system\", \"tool\"]\n\n\n@dataclass\nclass Session:\n    id:             str\n    started_at:     datetime\n    classification: Classification\n    retention_days: int          = 30\n    actor:          str          = \"agent\"\n    ended_at:       Optional[datetime] = None\n    correlates_with: Optional[str] = None\n\n\ndef start(writer: Writer, id: str, classification: Classification = \"confidential\",\n          retention_days: int = 30, actor: str = \"agent\") -&gt; Session:\n    writer._require_protocol_amendment_p22()          # §1 #12\n    if (writer.store_path / \"sessions\" / \".active\").exists():\n        raise RuntimeError(\"a session is already active; end it before starting another\")\n\n    s = Session(id=id, started_at=datetime.now(timezone.utc),\n                classification=classification, retention_days=retention_days,\n                actor=actor)\n\n    # date-partitioned storage\n    date_dir = writer.store_path / \"sessions\" / s.started_at.strftime(\"%Y-%m-%d\")\n    date_dir.mkdir(parents=True, exist_ok=True)\n    binlog_path = date_dir / f\"{id}.binlog.zst\"\n    if binlog_path.exists():\n        raise RuntimeError(f\"session id {id!r} already exists on {s.started_at.date()}\")\n\n    # Empty binlog (will be populated by append)\n    binlog_path.write_bytes(b\"\")\n\n    # Pointer to active session\n    (writer.store_path / \"sessions\" / \".active\").write_text(id)\n\n    # Summary row on main chain\n    writer.emit_aux(\n        kind=\"session.start\",\n        payload={\n            \"session_id\":     id,\n            \"started_at\":     s.started_at.isoformat(),\n            \"classification\": classification,\n            \"retention_days\": retention_days,\n            \"actor\":          actor,\n        },\n        actor=actor,\n    )\n    return s\n\n\ndef append(writer: Writer, id: str, role: Role, content: str,\n           redactions_applied: Optional[bool] = None) -&gt; int:\n    active = (writer.store_path / \"sessions\" / \".active\")\n    if not active.exists():\n        raise RuntimeError(\"no active session; start one first\")\n    if active.read_text() != id:\n        raise RuntimeError(f\"active session is {active.read_text()!r}, not {id!r}\")\n\n    s = _load_session_state(writer, id)\n    if s.ended_at is not None:\n        raise RuntimeError(f\"session {id!r} has ended\")\n\n    turn_seq = _next_turn_seq(writer, id)\n    ts = datetime.now(timezone.utc).isoformat()\n\n    payload = {\n        \"session_id\": id, \"role\": role, \"turn_seq\": turn_seq, \"ts\": ts,\n    }\n    if s.classification == \"restricted\":\n        payload[\"content_cipher\"] = _encrypt(content, writer)\n    else:\n        payload[\"content\"] = content\n    if redactions_applied is not None:\n        payload[\"redactions_applied\"] = redactions_applied\n\n    binlog = _binlog_path(writer, id)\n    _append_framed(binlog, _canonical_json(payload).encode(\"utf-8\"))\n    return turn_seq\n\n\ndef end(writer: Writer, id: str, reason: Optional[str] = None) -&gt; Session:\n    active = writer.store_path / \"sessions\" / \".active\"\n    if not active.exists() or active.read_text() != id:\n        raise RuntimeError(f\"session {id!r} is not active\")\n\n    s = _load_session_state(writer, id)\n    s.ended_at = datetime.now(timezone.utc)\n    active.unlink()\n\n    # Seal the binlog (compute final chain tip; mark immutable per FS perms if desired)\n    binlog = _binlog_path(writer, id)\n    final_hash = _sha256_file(binlog)\n\n    writer.emit_aux(\n        kind=\"session.end\",\n        payload={\n            \"session_id\":   id,\n            \"ended_at\":     s.ended_at.isoformat(),\n            \"ended_reason\": reason or \"explicit\",\n            \"turns_count\":  _turns_count(writer, id),\n            \"binlog_hash\":  final_hash,\n        },\n        actor=s.actor,\n    )\n    return s\n\n\ndef purge_expired(writer: Writer, dry_run: bool = False) -&gt; dict:\n    retention = writer.config().sessions.retention_days\n    now = datetime.now(timezone.utc)\n    purged = []\n    for binlog in (writer.store_path / \"sessions\").rglob(\"*.binlog.zst\"):\n        date_str = binlog.parent.name\n        try:\n            date = datetime.strptime(date_str, \"%Y-%m-%d\").replace(tzinfo=timezone.utc)\n        except ValueError:\n            continue\n        age_days = (now - date).days\n        if age_days &lt;= retention:\n            continue\n        session_id = binlog.stem.replace(\".binlog\", \"\")\n        purged.append({\"session_id\": session_id, \"date\": date_str, \"age_days\": age_days})\n        if not dry_run:\n            # tombstone marker replaces the body\n            binlog.write_text(_tombstone_manifest(session_id, date))\n            writer.emit_aux(\n                kind=\"session.purged\",\n                payload={\n                    \"session_id\":  session_id,\n                    \"original_started_at\": date.isoformat(),\n                    \"purged_at\":   now.isoformat(),\n                    \"reason\":      \"retention_expired\",\n                },\n                actor=\"retention-purger\",\n            )\n    return {\"purged_count\": len(purged), \"dry_run\": dry_run, \"purged\": purged}\n\n\ndef _binlog_path(writer: Writer, session_id: str) -&gt; Path:\n    # find the binlog by scanning today's and yesterday's dirs (sessions can span midnight)\n    for date_dir in sorted((writer.store_path / \"sessions\").iterdir(), reverse=True):\n        if date_dir.is_dir() and date_dir.name not in (\".active\",):\n            candidate = date_dir / f\"{session_id}.binlog.zst\"\n            if candidate.exists():\n                return candidate\n    raise FileNotFoundError(f\"session binlog for {session_id!r} not found\")\n\n\ndef _next_turn_seq(writer: Writer, session_id: str) -&gt; int:\n    binlog = _binlog_path(writer, session_id)\n    # Count existing frames; turn_seq starts at 0\n    return _count_frames(binlog)\n\n\n# Helpers (zstd framing, encryption, hashing) — implemented mirroring §6.2 patterns</code></pre>\n<h3>AGENTS.md §18 amendment text (proposed)</h3>\n<pre><code class=\"language-text\">## §18  Session transcript ledger (added by P22 — requires APPROVE chat-turn per §0.2)\n\n§18.1  Sessions are an OPTIONAL turn-level audit trail for agent-user\nconversations. Operators OPT IN per conversation via `cyberos session start`.\n\n§18.2  Session bodies live at\n`&lt;memory-root&gt;/sessions/&lt;YYYY-MM-DD&gt;/&lt;id&gt;.binlog.zst` and are independent\nfrom the main audit chain. The framed binlog format mirrors §6.2.\n\n§18.3  Sessions MUST carry a `classification` of either `confidential`\n(default) or `restricted`. The classifications `public` and `internal`\nare NOT permitted on sessions.\n\n§18.4  When `classification: restricted`, every session.turn payload's\n`content` field MUST be encrypted via the §5.4 envelope. The meta-frame\n(role, ts, turn_seq) remains plaintext.\n\n§18.5  Sessions emit summary rows on the main audit chain:\n- `session.start` at lifecycle start\n- `session.end`   at lifecycle end (or, on retention purge, NEVER — see §18.6)\n- `session.purged` when a session's body is dropped per retention\n\n§18.6  Retention is configured via `manifest.json:sessions.retention_days`\n(default 30 days). Purge replaces the session body with a tombstone\nmanifest; the summary rows on the main chain remain. The fact of purge is\nitself a chain leaf and is not erasable.\n\n§18.7  Memory writes that occur during an active session MUST carry\n`extra.session_id` on the put/move/delete row. The active session is\nindicated by `&lt;memory-root&gt;/sessions/.active`; only one session may be\nactive at a time per memory.\n\n§18.8  Lifecycle invariants enforced by the walker:\n- Every `session.start` has 0 or 1 `session.end` for the same session_id\n- Within a session's binlog, turn_seq is strictly monotonically increasing\n- No `session.turn` precedes session.start or follows session.end</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Full lifecycle round-trip</strong> — start → append × 3 → end → read returns the 3 turns in order. <em>(traces_to: §1 #1, §1 #7)</em></li><li><strong>Default classification is confidential</strong> — <code>cyberos session start --id x</code> → frontmatter <code>classification: confidential</code>. <em>(traces_to: §1 #3, DEC-251)</em></li><li><strong>Restricted forces encryption</strong> — <code>--classification restricted</code> → every turn's <code>content</code> is in <code>content_cipher</code>, not <code>content</code>. <em>(traces_to: §1 #4)</em></li><li><strong><code>public</code> and <code>internal</code> classifications rejected</strong> — <code>--classification public</code> → CLI parse error or schema rejection. <em>(traces_to: §1 #3)</em></li><li><strong>Append without start rejected</strong> — <code>cyberos session append --id never-started ...</code> → error naming the violated state. <em>(traces_to: §1 #6)</em></li><li><strong>Append after end rejected</strong> — start → end → append → error. <em>(traces_to: §1 #6)</em></li><li><strong>Double end rejected</strong> — start → end → end → second end raises. <em>(traces_to: §1 #5)</em></li><li><strong>Two active sessions rejected</strong> — start one, attempt to start another without ending → raises. <em>(traces_to: §1 #1)</em></li><li><strong><code>session.start</code> row on main chain</strong> — after <code>cyberos session start --id x</code>, HEAD has a new aux row of kind <code>session.start</code> with payload matching the schema. <em>(traces_to: §1 #1, §1 #5)</em></li><li><strong><code>session.end</code> row on main chain</strong> — same for end. <em>(traces_to: §1 #1)</em></li><li><strong><code>session_id</code> attached to memory writes during active session</strong> — start → put memories/x.md → end → the put row's <code>extra.session_id</code> equals the session id. <em>(traces_to: §1 #13)</em></li><li><strong><code>session_id</code> absent when no active session</strong> — put outside session → no <code>session_id</code> in extra. <em>(traces_to: §1 #13)</em></li><li><strong>Walker catches lifecycle violation</strong> — handcraft an end without start in the binlog → <code>cyberos doctor</code> fails with <code>session-lifecycle-well-formed</code>. <em>(traces_to: §1 #5)</em></li><li><strong>Walker catches turn_seq non-monotone</strong> — handcraft turn_seq 0, 1, 1 in binlog → walker fails. <em>(traces_to: §1 #5)</em></li><li><strong>Read without decrypt for restricted shows placeholder</strong> — <code>cyberos session read --id x</code> (no --decrypt) on restricted session → turn metadata visible, content shown as <code>[encrypted content; --decrypt to read]</code>. <em>(traces_to: §1 #7)</em></li><li><strong>Read with decrypt for restricted shows plaintext</strong> — <code>--decrypt</code> flag on restricted session → content visible. <em>(traces_to: §1 #7)</em></li><li><strong><code>session list --since 24h</code></strong> — lists sessions started in last 24h with their classification + state (active|ended|purged). <em>(traces_to: §1 #8)</em></li><li><strong>Retention purge expired</strong> — fixture with sessions dated 35 days ago + retention=30 → <code>cyberos session purge-expired</code> produces <code>session.purged</code> rows; binlog bodies replaced by tombstone manifests. <em>(traces_to: §1 #9, §1 #10)</em></li><li><strong>Retention purge <code>--dry-run</code></strong> — dry-run prints what would be purged; no binlog modified. <em>(traces_to: §1 #9)</em></li><li><strong><code>session.purged</code> payload shape</strong> — payload matches §1 #10 schema (session_id, original_started_at, purged_at, reason). <em>(traces_to: §1 #10)</em></li><li><strong>ACL on sessions/ subtree</strong> — <code>sessions/STORE.yaml</code> denies <code>scheduled-importer</code> → <code>cyberos session start --actor scheduled-importer</code> rejected. <em>(traces_to: §1 #11)</em></li><li><strong>§18 anchor required</strong> — AGENTS.md lacks §18 → <code>cyberos session start</code> raises with structured message naming APPROVE phrase. <em>(traces_to: §1 #12)</em></li><li><strong>PII redaction applied on opt-in</strong> — <code>cyberos session append --redactions-applied true</code> → content runs through FR-MEMORY-111 PII detector; <code>redactions_applied: true</code> set in payload. <em>(traces_to: §1 #14)</em></li><li><strong>Storage path date-partitioned</strong> — session started 2026-05-19 → binlog at <code>sessions/2026-05-19/&lt;id&gt;.binlog.zst</code>. <em>(traces_to: §1 #2)</em></li><li><strong>Spanning midnight</strong> — session started 23:55 UTC continues to receive turns at 00:05 UTC the next day; binlog remains in the start-date directory. <em>(traces_to: §1 #2)</em></li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_transcript.py\nimport pytest, json\nfrom datetime import datetime, timedelta, timezone\nfrom pathlib import Path\n\nfrom cyberos.core.session import start, append, end, purge_expired\n\n\ndef test_full_lifecycle(empty_memory, ensure_section_18):\n    \"\"\"AC #1\"\"\"\n    s = start(empty_memory, \"sess-1\")\n    append(empty_memory, \"sess-1\", role=\"user\",      content=\"hello\")\n    append(empty_memory, \"sess-1\", role=\"assistant\", content=\"hi\")\n    append(empty_memory, \"sess-1\", role=\"user\",      content=\"how are you\")\n    end(empty_memory, \"sess-1\")\n    binlog = empty_memory.store_path / \"sessions\" / s.started_at.strftime(\"%Y-%m-%d\") / \"sess-1.binlog.zst\"\n    assert binlog.exists()\n    # Read back via cyberos.core.session.read (not shown here for brevity)\n\n\ndef test_default_classification(empty_memory, ensure_section_18):\n    \"\"\"AC #2\"\"\"\n    s = start(empty_memory, \"sess-x\")\n    assert s.classification == \"confidential\"\n\n\ndef test_restricted_classification_encrypts(empty_memory, ensure_section_18, monkeypatch):\n    \"\"\"AC #3\"\"\"\n    s = start(empty_memory, \"sess-y\", classification=\"restricted\")\n    append(empty_memory, \"sess-y\", role=\"user\", content=\"secret hello\")\n    # Inspect raw binlog frame and confirm `content_cipher` present, `content` absent\n    binlog = empty_memory.store_path / \"sessions\" / s.started_at.strftime(\"%Y-%m-%d\") / \"sess-y.binlog.zst\"\n    frame = _read_first_frame(binlog)\n    payload = json.loads(frame)\n    assert \"content_cipher\" in payload\n    assert \"content\" not in payload\n\n\ndef test_invalid_classification_rejected(empty_memory, ensure_section_18):\n    \"\"\"AC #4\"\"\"\n    with pytest.raises((ValueError, RuntimeError)):\n        start(empty_memory, \"sess-z\", classification=\"public\")\n\n\ndef test_append_without_start_rejected(empty_memory, ensure_section_18):\n    \"\"\"AC #5\"\"\"\n    with pytest.raises(RuntimeError, match=\"no active session\"):\n        append(empty_memory, \"ghost\", role=\"user\", content=\"hello\")\n\n\ndef test_append_after_end_rejected(empty_memory, ensure_section_18):\n    \"\"\"AC #6\"\"\"\n    start(empty_memory, \"sess-a\")\n    end(empty_memory, \"sess-a\")\n    with pytest.raises(RuntimeError):\n        append(empty_memory, \"sess-a\", role=\"user\", content=\"too late\")\n\n\ndef test_double_end_rejected(empty_memory, ensure_section_18):\n    \"\"\"AC #7\"\"\"\n    start(empty_memory, \"sess-b\")\n    end(empty_memory, \"sess-b\")\n    with pytest.raises(RuntimeError):\n        end(empty_memory, \"sess-b\")\n\n\ndef test_two_active_sessions_rejected(empty_memory, ensure_section_18):\n    \"\"\"AC #8\"\"\"\n    start(empty_memory, \"sess-c\")\n    with pytest.raises(RuntimeError, match=\"already active\"):\n        start(empty_memory, \"sess-d\")\n\n\ndef test_session_id_attached_to_memory_writes(empty_memory, ensure_section_18):\n    \"\"\"AC #11\"\"\"\n    s = start(empty_memory, \"sess-e\")\n    head_before = empty_memory.head_seq()\n    empty_memory.put(\"memories/facts/x.md\", \"body\", meta={})\n    row = empty_memory.read_audit_row(head_before + 1)\n    assert row[\"extra\"][\"session_id\"] == \"sess-e\"\n\n\ndef test_session_id_absent_outside_session(empty_memory, ensure_section_18):\n    \"\"\"AC #12\"\"\"\n    head_before = empty_memory.head_seq()\n    empty_memory.put(\"memories/facts/y.md\", \"body\", meta={})\n    row = empty_memory.read_audit_row(head_before + 1)\n    assert \"session_id\" not in row.get(\"extra\", {})\n\n\ndef test_protocol_amendment_required(memory_without_section_18):\n    \"\"\"AC #22\"\"\"\n    with pytest.raises(Exception, match=r\"APPROVE protocol change P22 §18\"):\n        start(memory_without_section_18, \"sess-x\")\n\n\ndef test_storage_date_partitioned(empty_memory, ensure_section_18):\n    \"\"\"AC #24\"\"\"\n    s = start(empty_memory, \"sess-dated\")\n    expected = empty_memory.store_path / \"sessions\" / s.started_at.strftime(\"%Y-%m-%d\") / \"sess-dated.binlog.zst\"\n    assert expected.exists()\n\n\ndef test_walker_catches_lifecycle_violation(memory_with_orphan_end, ensure_section_18):\n    \"\"\"AC #13\"\"\"\n    import subprocess\n    result = subprocess.run([\"python\", \"-m\", \"cyberos\", \"--store\",\n                             str(memory_with_orphan_end.store_path), \"doctor\"],\n                            capture_output=True, text=True)\n    assert result.returncode != 0\n    assert \"session-lifecycle-well-formed\" in result.stderr + result.stdout</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_session.py\nimport pytest\nfrom datetime import datetime, timedelta, timezone\nfrom cyberos.core.session import purge_expired\n\n\ndef test_purge_expired_removes_old(memory_with_expired_sessions, ensure_section_18):\n    \"\"\"AC #18 + #20\"\"\"\n    result = purge_expired(memory_with_expired_sessions)\n    assert result[\"purged_count\"] &gt; 0\n    # Inspect main chain for session.purged row\n    rows = memory_with_expired_sessions.read_recent_audit_rows(20)\n    assert any(r[\"kind\"] == \"session.purged\" for r in rows)\n\n\ndef test_dry_run_does_not_purge(memory_with_expired_sessions, ensure_section_18):\n    \"\"\"AC #19\"\"\"\n    result = purge_expired(memory_with_expired_sessions, dry_run=True)\n    assert result[\"dry_run\"] is True\n    # No binlog modified\n    for binlog in (memory_with_expired_sessions.store_path / \"sessions\").rglob(\"*.binlog.zst\"):\n        assert binlog.stat().st_size &gt; 0  # original content preserved</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>API contracts above. Order:</p>\n<ol><li>AGENTS.md §18 amendment text (DO NOT commit until APPROVE chat-turn).</li><li>Schema (<code>memory.schema.json</code>).</li><li>Walker invariants.</li><li><code>cyberos/core/session.py</code>.</li><li><code>cyberos/cli/session.py</code>.</li><li><code>cyberos/__main__.py</code> wiring.</li><li>Writer: <code>extra.session_id</code> injection + <code>_require_protocol_amendment_p22</code>.</li><li>Tests.</li><li>CHANGELOG.</li></ol>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-111 (related)</strong> — PII detector invoked on <code>--redactions-applied true</code>.</li><li><strong>FR-MEMORY-115 (this FR blocks)</strong> — dream's patterns detector consumes <code>session.start</code> / <code>session.end</code> rows + (optionally) decrypts session bodies for finer-grained pattern detection.</li><li><strong>FR-MEMORY-117 (related)</strong> — ACL on <code>sessions/</code> subtree controls who can start/append/end sessions.</li><li><strong>FR-MEMORY-109 (related)</strong> — Claude Code hook can call <code>cyberos session append</code> to record real conversation turns; the hook can pre-redact via FR-MEMORY-111 then set <code>redactions_applied: true</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>session.start</code> aux row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"session.start\",\n  \"payload\": {\n    \"session_id\":     \"sre-investigation-2026-05-19\",\n    \"started_at\":     \"2026-05-19T08:00:00Z\",\n    \"classification\": \"confidential\",\n    \"retention_days\": 30,\n    \"actor\":          \"stephen\"\n  }\n}</code></pre>\n<h3><code>session.end</code> aux row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"session.end\",\n  \"payload\": {\n    \"session_id\":   \"sre-investigation-2026-05-19\",\n    \"ended_at\":     \"2026-05-19T08:42:13Z\",\n    \"ended_reason\": \"explicit\",\n    \"turns_count\":  47,\n    \"binlog_hash\":  \"f3a9b2c1d4e5f6789a0b1c2d3e4f56789a0b1c2d3e4f56789a0b1c2d3e4f5678\"\n  }\n}</code></pre>\n<h3>Encrypted turn payload (restricted)</h3>\n<pre><code class=\"language-json\">{\n  \"session_id\":     \"private-1\",\n  \"role\":           \"user\",\n  \"turn_seq\":       17,\n  \"ts\":             \"2026-05-19T08:23:11Z\",\n  \"content_cipher\": {\n    \"alg\":   \"aes-256-gcm\",\n    \"key_ref\": \"default\",\n    \"nonce\": \"abc123…\",\n    \"ct\":    \"f3a9b2…\"\n  }\n}</code></pre>\n<h3><code>session.purged</code> aux row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"session.purged\",\n  \"payload\": {\n    \"session_id\":          \"old-conversation-2026-04-15\",\n    \"original_started_at\": \"2026-04-15T00:00:00Z\",\n    \"purged_at\":           \"2026-05-19T03:00:00Z\",\n    \"reason\":              \"retention_expired\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li><code>cyberos session export --format jsonl</code> — §1 #15; slice 4.</li><li><code>--correlates-with &lt;other-id&gt;</code> linkage — §1 #16; slice 4+.</li><li>Real-time tail (<code>cyberos session tail --id x</code>) — slice 4+.</li><li>Multi-tenant per-session retention overrides — slice 4+ once FR-MEMORY-117 lands.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>§18 missing</td><td>constructor check</td><td>session.start raises</td><td>APPROVE chat-turn</td></tr><tr><td>Two <code>start</code> for same id</td><td>path-exists check</td><td>second raises</td><td>Operator picks unique id</td></tr><tr><td>Two active sessions</td><td>.active file check</td><td>second start raises</td><td>End current first</td></tr><tr><td>Append without start</td><td>.active file absent</td><td>raises</td><td>Start session first</td></tr><tr><td>Crash between start and end</td><td>.active file persists</td><td>next start fails</td><td>Operator deletes <code>.active</code> manually (audit row exists)</td></tr><tr><td>Disk full during append</td><td>write fails</td><td>binlog frame partial; CRC32C catches on next read</td><td>walker repairs by truncating to last intact frame</td></tr><tr><td>Encryption key missing</td><td>encryption call fails</td><td>start raises</td><td>Operator provisions key</td></tr><tr><td>Walker invariant violation</td><td>doctor</td><td>non-zero exit</td><td>Operator fixes binlog or rolls back</td></tr><tr><td>Restricted session read without --decrypt</td><td>placeholder shown</td><td>works</td><td>Pass --decrypt</td></tr><tr><td>Session retained past expiry by mistake</td><td>purge-expired catches on next cron</td><td>scheduled retroactive purge</td><td>None</td></tr><tr><td>Manual purge of active session</td><td>reject</td><td>raises</td><td>End session first</td></tr><tr><td>Cross-day session (midnight rollover)</td><td>binlog stays in original date dir</td><td>works</td><td>None</td></tr><tr><td>ACL denies session start</td><td>ACL check before path-exists</td><td>raises</td><td>Adjust ACL</td></tr><tr><td>Concurrent appends from two processes</td><td>only one .active session at a time</td><td>second process sees same active</td><td>Sequential by design</td></tr><tr><td><code>.active</code> file corrupted (empty / nonexistent session_id)</td><td>read returns empty/missing</td><td>start raises ambiguous</td><td>Operator deletes .active manually</td></tr><tr><td>Old session's binlog file deleted manually</td><td>next purge can't find it</td><td>warned in stderr; chain-rows remain</td><td>Operator restores from backup if needed</td></tr><tr><td>Tombstone manifest hand-edited</td><td>walker validates schema</td><td>doctor fails</td><td>Restore tombstone</td></tr><tr><td>1MB turn content</td><td>normal zstd compression</td><td>works</td><td>None</td></tr><tr><td>Restricted session purged → encrypted blob gone</td><td>already encrypted; purge removes ciphertext + IV</td><td>irretrievable per design</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong><code>.active</code> pointer file</strong> — single-process-active is enforced by file presence. Two-active is a deliberate choice; supporting concurrent sessions on one memory is slice-4+ and would require per-PID session pointers.</li><li><strong>Turn sequencing within a session</strong> — <code>_next_turn_seq()</code> counts existing frames. Atomic per <code>.lock</code> (same exclusive lock as memory writes); no race.</li><li><strong>Date-partitioning at start-date</strong> — sessions spanning midnight stay in their start date's directory. Retention purges by start date, which is the natural mental model.</li><li><strong>Tombstone manifest</strong> — replaces the binlog body with a JSON document declaring the session id, original metadata, purge timestamp. Walker can still find the session pointer.</li><li><strong>Encryption envelope reuses §5.4</strong> — same <code>Envelope</code> definition as memory files; key management is operator's responsibility (slice-4 KMS integration).</li><li><strong><code>session.purged</code> is the load-bearing chain leaf</strong> — keeps the <em>fact</em> of purge auditable forever, even after the body is gone. Matches the §3.6 <code>delete(purge)</code> semantics for memory files.</li><li><strong><code>--actor</code> flag for sessions</strong> — recorded in session frontmatter + every aux row; ACL check uses this actor against <code>sessions/STORE.yaml</code>.</li><li><strong>Session list output</strong> — JSON line per session for piping into tools. <code>--format pretty</code> for human-readable; default JSONL for scripting.</li><li><strong>The <code>ensure_section_18</code> test fixture</strong> — symlinks AGENTS.md to a fixture that includes the §18 amendment text. Lets tests run end-to-end as if the APPROVE chat-turn had already happened.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-119.</em></p>";
