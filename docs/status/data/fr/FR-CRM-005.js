window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CRM-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CRM service <strong>MUST</strong> ship <code>crm.next-action@1</code> skill at <code>services/crm/src/next_action/</code> returning top-3 AI-ranked actions per deal, grounded in deal context, rate-limited, 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> register skill at CUO via <code>skill_handler.rs::register()</code> per DEC-1650 — invoked via CUO <code>crm.next-action@1</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>next_action_kind</code> against closed enum per DEC-1651.</li></ol>\n<ol><li><strong>MUST</strong> build context at <code>context_builder.rs::build(deal_id)</code>:</li></ol>\n<ul><li>Deal record (stage, value, age, owner)</li><li>Last 20 activities from FR-CRM-002</li><li>Contact engagement (last email reply gap, etc.)</li><li>Account history (won/lost recent deals)</li></ul>\n<ol><li><strong>MUST</strong> rank at <code>ranker.rs::rank(context)</code> via FR-AI-003 with structured prompt:</li></ol>\n<ul><li>Output JSON array of 3 entries, each <code>{kind, summary, rationale, confidence_score, deep_link}</code> per DEC-1653.</li><li>Validation: each kind in enum; confidence 0-1; deep_link non-empty.</li></ul>\n<ol><li><strong>MUST</strong> enforce rate limit per DEC-1654 — 100 calls/user/day; return 429 when exceeded.</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0005</code>: ``<code>sql CREATE TABLE crm_next_action_suggestions ( suggestion_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, deal_id UUID NOT NULL, suggestions JSONB NOT NULL, requested_by UUID NOT NULL, status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','executed','dismissed','expired')), executed_kind TEXT CHECK (executed_kind IS NULL OR executed_kind IN ('send_email','schedule_call','share_proposal','request_intro','escalate_to_decision_maker','follow_up','mark_lost')), executed_at TIMESTAMPTZ, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); CREATE INDEX next_action_user_day_idx ON crm_next_action_suggestions(tenant_id, requested_by, created_at DESC); ALTER TABLE crm_next_action_suggestions ENABLE ROW LEVEL SECURITY; CREATE POLICY next_action_rls ON crm_next_action_suggestions USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON crm_next_action_suggestions FROM cyberos_app; GRANT UPDATE (status, executed_kind, executed_at) ON crm_next_action_suggestions TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> auto-expire pending suggestions after 7 days (cron via FR-MCP-007).</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-1655. PII per FR-MEMORY-111: rationale text SHA-256 hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from CUO call → context → AI → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> return &gt;3 suggestions per DEC-1650.</li></ol>\n<ol><li><strong>MUST NOT</strong> exceed user rate limit per DEC-1654.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why top-3 (DEC-1650)?</strong> Cognitive load research: &gt;5 options paralyzes; 3 optimizes selection.</p>\n<p><strong>Why rationale required (DEC-1653)?</strong> CDO won't follow black-box AI; explainability drives adoption.</p>\n<p><strong>Why rate limit (DEC-1654)?</strong> AI cost per call is non-trivial; runaway usage breaks budget.</p>\n<p><strong>Why context from FR-CRM-002 (DEC-1652)?</strong> Activity feed is the truth source; without it AI hallucinates.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST   /v1/crm/next-action       body: {deal_id}\nPOST   /v1/crm/next-action/{id}/execute    body: {kind}  (records executed_kind)\nPOST   /v1/crm/next-action/{id}/dismiss</code></pre>\n<p>Sample response:</p>\n<pre><code class=\"language-json\">{\n  \"suggestion_id\": \"uuid\",\n  \"deal_id\": \"uuid\",\n  \"suggestions\": [\n    {\n      \"kind\": \"send_email\",\n      \"summary\": \"Follow up on proposal — no reply in 5 days\",\n      \"rationale\": \"Last email Jun 1 unanswered; account history shows 7-day response pattern.\",\n      \"confidence_score\": 0.85,\n      \"deep_link\": \"/email/threads/abc-123\"\n    },\n    {\n      \"kind\": \"schedule_call\",\n      \"summary\": \"Push for decision call this week\",\n      \"rationale\": \"Deal age 45d, stage 'proposal' for 14d; similar deals close after exec call.\",\n      \"confidence_score\": 0.75,\n      \"deep_link\": \"/calendar/new?contact_id=...\"\n    },\n    {\n      \"kind\": \"request_intro\",\n      \"summary\": \"Ask current contact to introduce CFO\",\n      \"rationale\": \"Decision-maker not yet engaged; CFO buyer signals from similar deals.\",\n      \"confidence_score\": 0.65,\n      \"deep_link\": \"/crm/contacts/...../add-stakeholder\"\n    }\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>CUO skill registered as crm.next-action@1</strong>. 2. <strong>Returns exactly 3 suggestions (or fewer if AI can't fill)</strong>. 3. <strong>Enum 7 + cardinality test</strong>. 4. <strong>Each suggestion has all 5 fields</strong>. 5. <strong>Rationale 1-2 sentences</strong>. 6. <strong>Confidence_score 0-1</strong>. 7. <strong>Deep_link non-empty</strong>. 8. <strong>Context built from FR-CRM-002 activities</strong>. 9. <strong>Rate limit 100/user/day</strong>. 10. <strong>3 memory audit kinds emitted</strong>. 11. <strong>PII scrubbed (rationale SHA256)</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Execute records executed_kind</strong>. 15. <strong>Dismiss → status=dismissed</strong>. 16. <strong>7-day expiry via cron</strong>. 17. <strong>Append-only suggestions table</strong>. 18. <strong>AI returns invalid JSON → sev-2 + retry once</strong>. 19. <strong>No deal_id (closed deal) → 404</strong>. 20. <strong>CDO/CRO role required</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn returns_top_3() {\n    let ctx = TestContext::with_open_deal_and_activities(20).await;\n    let r = ctx.next_action(ctx.deal_id).await;\n    assert_eq!(r.suggestions.len(), 3);\n    for s in &amp;r.suggestions {\n        assert!(!s.rationale.is_empty());\n        assert!(s.confidence_score &gt;= 0.0 &amp;&amp; s.confidence_score &lt;= 1.0);\n    }\n}\n\n#[tokio::test]\nasync fn rate_limit_enforced() {\n    let ctx = TestContext::with_user().await;\n    for _ in 0..100 {\n        ctx.next_action_for_random_deal().await;\n    }\n    let r = ctx.next_action_for_random_deal().await;\n    assert_eq!(r.status_code, 429);\n}\n\n#[tokio::test]\nasync fn execute_records_kind() {\n    let ctx = TestContext::with_suggestion().await;\n    ctx.execute_suggestion(ctx.suggestion_id, \"send_email\").await;\n    let row = ctx.fetch_suggestion(ctx.suggestion_id).await;\n    assert_eq!(row.status, \"executed\");\n    assert_eq!(row.executed_kind.as_deref(), Some(\"send_email\"));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-CRM-001, FR-CUO-101. <strong>Cross-module:</strong> FR-CRM-002 (activity context), FR-AI-003 (LLM), FR-MCP-007 (expiry cron), FR-AUTH-101 (role), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>AI returns &lt;3 suggestions</td><td>accept fewer</td><td>use what AI returned</td><td>inherent</td></tr><tr><td>AI invalid JSON</td><td>parse error</td><td>retry 1x, fallback sev-2</td><td>inherent</td></tr><tr><td>AI hallucinated kind (not in enum)</td><td>filter</td><td>drop, fewer suggestions</td><td>inherent</td></tr><tr><td>Deal closed (won/lost)</td><td>check stage</td><td>404</td><td>inherent</td></tr><tr><td>Rate limit window edge</td><td>day boundary check</td><td>rolling 24h or calendar day</td><td>inherent</td></tr><tr><td>Context too large (&gt;50k tokens)</td><td>truncate to last 10 acts</td><td>inherent</td><td>inherent</td></tr><tr><td>AI provider quota</td><td>rate limit upstream</td><td>sev-2; degrade gracefully</td><td>inherent</td></tr><tr><td>Expiry cron skipped</td><td>next run catches</td><td>inherent</td><td>manual run</td></tr><tr><td>Execute kind not in original suggestions</td><td>accept (user override)</td><td>record anyway</td><td>inherent</td></tr><tr><td>Cross-tenant suggestion lookup</td><td>RLS</td><td>404</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 AI prompt: includes deal context as structured JSON, asks for <code>[{kind, summary, rationale, confidence_score, deep_link}]</code>.</li><li>§11.2 Rate limit via Redis sliding-window: 100 ops per 24h per user.</li><li>§11.3 memory audit body: deal_id, kinds[]; rationale SHA256.</li><li>§11.4 Expiry cron at 02:00 tenant_tz: <code>UPDATE suggestions SET status='expired' WHERE status='pending' AND created_at &lt; now() - interval '7 days'</code>.</li><li>§11.5 Deep_link templates per kind: send_email → /email/compose?to=..., schedule_call → /calendar/new?contact=..., etc.</li></ul>\n<hr>\n<p><em>End of FR-CRM-005 spec.</em></p>";
