window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TEN-105"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship signed-bundle export at <code>services/ten/src/bundle/</code> per tenant offboarding (FR-TEN-104 termination) producing deterministic ZIP + Ed25519-signed manifest + memory chain anchor, with 30-day signed URL delivery, async build via FR-MCP-007 Tasks, 3-per-year rate limit, pre-deletion gate dependency, and 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>bundle_status</code> enum: <code>('pending','building','ready','delivered','expired','failed')</code> per DEC-1326. Cardinality asserts 6.</li></ol>\n<ol><li><strong>MUST</strong> define <code>tenant_bundle_exports</code> at migration <code>0026</code>: <code>(bundle_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, status bundle_status NOT NULL DEFAULT 'pending', requested_at TIMESTAMPTZ NOT NULL DEFAULT now(), requested_by_subject_id UUID NOT NULL, build_started_at TIMESTAMPTZ, ready_at TIMESTAMPTZ, delivered_at TIMESTAMPTZ, expires_at TIMESTAMPTZ, s3_key TEXT, bundle_size_bytes BIGINT, file_count INT, manifest_sha256 CHAR(64), manifest_signature_kms_blob BYTEA, source_chain_head BIGINT, trace_id CHAR(32))</code>. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> define <code>tenant_signing_keys</code> at migration <code>0027</code>: <code>(tenant_id UUID PRIMARY KEY, public_key_pem TEXT NOT NULL, private_key_kms_blob BYTEA NOT NULL, kms_key_id TEXT NOT NULL, generated_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. One key per tenant; generated at FR-TEN-001 provisioning per DEC-1322.</li></ol>\n<ol><li><strong>MUST</strong> generate Ed25519 keypair at tenant provisioning per DEC-1322. Modification to FR-TEN-001 orchestrator: after tenant row created, generate keypair via <code>ed25519-dalek</code>, KMS-encrypt private key, insert <code>tenant_signing_keys</code> row.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tid}/bundle/export</code> body <code>{ reason }</code>. Caller has <code>tenant_admin</code> OR <code>cso</code> role. Handler:</li></ol>\n<ul><li>Rate-limit check per DEC-1329 (3 per year per tenant).</li><li>INSERTs bundle row with status='pending'.</li><li>Enqueues build task via FR-MCP-007.</li><li>Emit <code>ten.bundle_export_initiated</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> build bundle per DEC-1323 via <code>bundle/builder.rs</code>:</li></ol>\n<ul><li>Per-source JSON: PROJ (all projects + sub-resources), INV (invoices + lines), DOC (metadata + S3 refs), CHAT (channels + messages), AUDIT_CHAIN (full memory segment for tenant), METERING (FR-TEN-004 history), USERS (FR-AUTH-002 subjects).</li><li>Per-source CSV (human-readable; same data).</li><li>signed-manifest.json (Ed25519-signed file inventory).</li><li>public-key.pem (verifier reference).</li><li>README.md (verification instructions).</li><li>Deterministic ZIP per §1 #8.</li><li>Computes SHA-256, file count, total size.</li></ul>\n<ol><li><strong>MUST</strong> include full memory audit chain segment per DEC-1331. Chain segment = all rows where tenant_id matches; chain hashes intact; verifier can replay locally.</li></ol>\n<ol><li><strong>MUST</strong> produce deterministic ZIP per DEC-1321 + feature-request-audit skill rule 27-28:</li></ol>\n<ul><li>Sorted entries (alphabetic by path).</li><li>Fixed mtime <code>2000-01-01T00:00:00Z</code>.</li><li>Fixed mode <code>0o644</code>.</li><li>ZIP_DEFLATED compression level 6.</li><li>No extra/optional ZIP fields (mac extras, unicode extras).</li><li>Two builds of same source data produce byte-identical zip.</li></ul>\n<ol><li><strong>MUST</strong> sign manifest with per-tenant Ed25519 key per DEC-1324. The <code>bundle/signer.rs::sign(manifest_bytes, tenant_id)</code>:</li></ol>\n<ul><li>Loads private key from <code>tenant_signing_keys</code> (KMS-decrypt).</li><li>Signs <code>SHA-256(manifest_bytes)</code> per Ed25519 spec.</li><li>Returns signature bytes (64 bytes).</li><li>Persists <code>manifest_signature_kms_blob</code> on bundle row.</li><li>Emits <code>ten.bundle_signature_generated</code>.</li></ul>\n<ol><li><strong>MUST</strong> anchor in memory per DEC-1325. After build completes:</li></ol>\n<ul><li>INSERT memory row <code>ten.bundle_built</code> with payload containing <code>bundle_sha256</code> + <code>manifest_sha256</code> + <code>tenant_id</code> + <code>file_count</code>.</li><li>This row's chain hash becomes the anchor for FR-OBS-009 chain-of-custody per DEC-1333.</li></ul>\n<ol><li><strong>MUST</strong> upload to S3 with 30-day signed URL per DEC-1327. Handler:</li></ol>\n<ul><li>Uploads to <code>cyberos-bundles/{tenant_id}/{bundle_id}/bundle.zip</code> per residency S3.</li><li>Generates presigned URL with 30-day TTL.</li><li>Status → 'ready'; expires_at set.</li><li>Emit <code>ten.bundle_built</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> deliver via <code>GET /v1/admin/tenants/{tid}/bundle/{bundle_id}/download</code> returning the signed URL (URL itself not stored client-side; fetched per request). On first delivery: status → 'delivered'; emit <code>ten.bundle_delivered</code>.</li></ol>\n<ol><li><strong>MUST</strong> rate-limit per DEC-1329 — 3 bundle requests per tenant per 365 days. Excess → 429 + <code>bundle_rate_limit_exceeded</code>.</li></ol>\n<ol><li><strong>MUST</strong> enforce pre-deletion gate per DEC-1330. FR-TEN-106 attestation handler checks:</li></ol>\n<ul><li>SELECT bundle WHERE tenant_id=$1 AND status IN ('ready','delivered') AND ready_at &gt; now() - interval '90 days'.</li><li>No row → 412 PRECONDITION_FAILED + <code>bundle_export_required_before_attestation</code>.</li></ul>\n<ol><li><strong>MUST</strong> expire bundle at T+30d per DEC-1327. Scheduled job:</li></ol>\n<ul><li>status='expired' + bundle s3 deleted + signed URL invalid.</li><li>Emit <code>ten.bundle_expired</code>.</li></ul>\n<ol><li><strong>MUST</strong> emit 6 memory audit kinds per DEC-1332: export_initiated (sev-1), built (sev-1), delivered (sev-1), failed (sev-1), expired (sev-2), signature_generated (sev-2). All sev-1 except expired (sev-2 ops event) + signature_generated (sev-2 internal).</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub via FR-MEMORY-111 — <code>reason</code> text hashed; tenant_id retained for chain correlation.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id across request → task → build → S3 upload → audit.</li></ol>\n<ol><li><strong>MUST</strong> support manifest verification at <code>GET /v1/admin/tenants/{tid}/bundle/{bundle_id}/verify</code> returning <code>{ manifest_sha256, signature, public_key_pem, verifier_instructions_url }</code> — operator OR external auditor can verify offline.</li></ol>\n<ol><li><strong>MUST NOT</strong> sign bundle with shared key (per DEC-1322 — per-tenant only).</li></ol>\n<ol><li><strong>MUST NOT</strong> allow non-deterministic ZIP entries (per DEC-1321) — two builds MUST produce byte-identical output.</li></ol>\n<ol><li><strong>MUST NOT</strong> permanent-delete tenant without valid recent bundle (per DEC-1330 gate enforced in FR-TEN-106).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why per-tenant Ed25519 keys (§1 #3-4, DEC-1322)?</strong> Single global key = single compromise vector. Per-tenant key compromise scopes to one tenant. Standard cryptographic hygiene.</p>\n<p><strong>Why deterministic ZIP (§1 #8, DEC-1321)?</strong> Auditor reproducibility — \"did the bundle change between my download and yours?\" Deterministic = answer is yes (different hash) or no (same hash) with certainty.</p>\n<p><strong>Why include memory chain segment (§1 #7, DEC-1331)?</strong> The audit chain IS the legal evidence. Customer departing on bad terms → CyberSkill needs to prove every action. Chain segment with signed bundle = unforgeable.</p>\n<p><strong>Why 3-per-year rate limit (§1 #13, DEC-1329)?</strong> Bundle building is expensive (cross-source aggregation, S3 upload, GBs of data). Operationally tenant requests bundle once at termination + maybe annual compliance archive + once-in-a-while audit support = 3.</p>\n<p><strong>Why 30-day TTL (§1 #11, DEC-1327)?</strong> Legal evidence may need multi-week review by counsel; 7d (DSAR) too short. 30d covers typical legal cycles.</p>\n<p><strong>Why pre-deletion gate (§1 #14, DEC-1330)?</strong> Permanent-delete is irreversible. Requiring recent bundle = \"we always have a recovery path within the past 90 days\" — operational safety net.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0026_tenant_bundle_exports.sql\nCREATE TYPE bundle_status AS ENUM ('pending','building','ready','delivered','expired','failed');\n\nCREATE TABLE tenant_bundle_exports (\n  bundle_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  status bundle_status NOT NULL DEFAULT 'pending',\n  requested_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  requested_by_subject_id UUID NOT NULL,\n  reason TEXT,\n  build_started_at TIMESTAMPTZ,\n  ready_at TIMESTAMPTZ,\n  delivered_at TIMESTAMPTZ,\n  expires_at TIMESTAMPTZ,\n  s3_key TEXT,\n  bundle_size_bytes BIGINT,\n  file_count INT,\n  manifest_sha256 CHAR(64),\n  manifest_signature_kms_blob BYTEA,\n  source_chain_head BIGINT,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_bundle_tenant_year ON tenant_bundle_exports(tenant_id, requested_at DESC);\nALTER TABLE tenant_bundle_exports ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_bundle_exports_rls ON tenant_bundle_exports\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON tenant_bundle_exports FROM cyberos_app;\nGRANT UPDATE (status, build_started_at, ready_at, delivered_at, expires_at,\n              s3_key, bundle_size_bytes, file_count, manifest_sha256,\n              manifest_signature_kms_blob, source_chain_head) ON tenant_bundle_exports TO cyberos_app;\n\n-- 0027_tenant_signing_keys.sql\nCREATE TABLE tenant_signing_keys (\n  tenant_id UUID PRIMARY KEY,\n  public_key_pem TEXT NOT NULL,\n  private_key_kms_blob BYTEA NOT NULL,\n  kms_key_id TEXT NOT NULL,\n  generated_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nALTER TABLE tenant_signing_keys ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_signing_keys_rls ON tenant_signing_keys\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON tenant_signing_keys FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/admin/tenants/{tid}/bundle/export                  (tenant_admin or cso)\nGET    /v1/admin/tenants/{tid}/bundle                          (list bundles for tenant)\nGET    /v1/admin/tenants/{tid}/bundle/{bundle_id}/download    (signed URL retrieval)\nGET    /v1/admin/tenants/{tid}/bundle/{bundle_id}/verify      (public key + signature for verification)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>bundle_status cardinality 6</strong>.</li><li><strong>Keypair generated at provisioning</strong> — every new tenant has <code>tenant_signing_keys</code> row.</li><li><strong>Bundle build async</strong> — request returns 202 + bundle_id; task processes; status transitions pending→building→ready.</li><li><strong>Deterministic zip</strong> — two builds of same source data → byte-identical ZIP.</li><li><strong>Manifest signed</strong> — Ed25519 signature on manifest_sha256; verifies with public key.</li><li><strong>memory chain anchor</strong> — <code>ten.bundle_built</code> row contains bundle_sha256 + manifest_sha256.</li><li><strong>30-day signed URL</strong> — URL valid for 30d; T+31d → 403.</li><li><strong>Pre-deletion gate</strong> — FR-TEN-106 attestation without recent bundle → 412.</li><li><strong>3-per-year rate limit</strong> — 4th request in 12 months → 429.</li><li><strong>Audit chain in bundle</strong> — bundle's <code>audit_chain.json</code> contains all rows for tenant.</li><li><strong>Per-source files present</strong> — bundle contains projects.json, invoices.json, documents.json, chat.json, audit_chain.json, metering.json, users.json + .csv variants.</li><li><strong>Manifest verification endpoint</strong> — returns public_key_pem + signature.</li><li><strong>Bundle expiry T+30d</strong> — scheduled job marks expired + deletes S3 object.</li><li><strong>6 memory audit kinds emitted</strong> — full lifecycle.</li><li><strong>Signed manifest matches files</strong> — recompute SHA-256 of each file → matches manifest entries.</li><li><strong>Cross-tenant bundle invisible</strong> — RLS prevents tenant A from seeing tenant B's bundles.</li><li><strong>Per-tenant private key not exfiltrable</strong> — KMS-encrypted; raw private key never in DB.</li><li><strong>Bundle build failure</strong> — fixture broken source → status='failed' + audit + retry option.</li><li><strong>Trace_id end-to-end</strong> — request → task → audit all share trace_id.</li><li><strong>CSV-JSON parity</strong> — every JSON row also appears in CSV (1:1 mapping).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn bundle_is_deterministic() {\n    let ctx = TestContext::with_seeded_tenant().await;\n    let b1 = ctx.request_bundle().await;\n    ctx.wait_ready(b1).await;\n    let bytes1 = ctx.download_bundle(b1).await;\n\n    // Re-build same tenant data\n    let b2 = ctx.request_bundle().await;\n    ctx.wait_ready(b2).await;\n    let bytes2 = ctx.download_bundle(b2).await;\n\n    assert_eq!(sha256(&amp;bytes1), sha256(&amp;bytes2), \"bundles differ\");\n}\n\n#[tokio::test]\nasync fn manifest_signature_verifies() {\n    let ctx = TestContext::with_seeded_tenant().await;\n    let bundle_id = ctx.request_and_wait_bundle().await;\n    let verify_resp = ctx.get_verify_info(bundle_id).await;\n    let public_key = verify_resp[\"public_key_pem\"].as_str().unwrap();\n    let signature = base64::decode(verify_resp[\"signature\"].as_str().unwrap()).unwrap();\n    let manifest_sha256 = verify_resp[\"manifest_sha256\"].as_str().unwrap();\n\n    let pk = ed25519_dalek::PublicKey::from_pem(public_key).unwrap();\n    let sig = ed25519_dalek::Signature::from_bytes(&amp;signature).unwrap();\n    let msg = hex::decode(manifest_sha256).unwrap();\n    assert!(pk.verify(&amp;msg, &amp;sig).is_ok());\n}\n\n#[tokio::test]\nasync fn pre_deletion_gate_blocks_attestation_without_bundle() {\n    let ctx = TestContext::new().await;\n    let tid = ctx.provision_tenant().await;\n    // No bundle exported\n    let r = ctx.post_ten_106_attestation(tid, \"test\").await;\n    assert_eq!(r.status(), 412);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"bundle_export_required_before_attestation\");\n}\n\n#[tokio::test]\nasync fn three_per_year_rate_limit() {\n    let ctx = TestContext::new().await;\n    for _ in 0..3 { ctx.request_bundle().await; }\n    let r = ctx.request_bundle_raw().await;\n    assert_eq!(r.status(), 429);\n}\n\n#[tokio::test]\nasync fn memory_chain_anchored() {\n    let ctx = TestContext::with_seeded_tenant().await;\n    let b = ctx.request_and_wait_bundle().await;\n    let audit = ctx.memory_rows().await;\n    let built = audit.iter().find(|r| r.kind == \"ten.bundle_built\").unwrap();\n    assert!(built.payload[\"bundle_sha256\"].is_string());\n    assert!(built.payload[\"manifest_sha256\"].is_string());\n}\n\n// 5.6 30d expiry\n// 5.7 cross-tenant invisible\n// 5.8 audit chain completeness\n// 5.9 csv-json parity\n// 5.10 audit kinds emitted</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-TEN-104 (lifecycle — bundle as part of termination flow). <strong>Cross-module:</strong> FR-TEN-106 (pre-deletion gate), FR-AUTH-101 (cso + tenant_admin roles), FR-MEMORY-101 (audit chain source), FR-DOC-001 (S3 storage), FR-MCP-007 (async build via Tasks), FR-AI-003 (audit kinds), FR-MEMORY-111 (PII scrub), FR-OBS-007 (sev-1 routing), FR-OBS-009 (chain-of-custody anchor). <strong>Downstream:</strong> None.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<p><code>ten.bundle_built</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.bundle_built\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.ten.bundle\",\n  \"trace_id\": \"0af76519...\",\n  \"occurred_at\": \"2026-05-17T...\",\n  \"payload\": {\n    \"bundle_id\": \"0190...\",\n    \"bundle_sha256\": \"9c4e7a8b...\",\n    \"manifest_sha256\": \"f8a1b2c3...\",\n    \"file_count\": 12,\n    \"bundle_size_bytes\": 487293104,\n    \"source_chain_head\": 348201,\n    \"expires_at\": \"2026-06-16T...\"\n  }\n}</code></pre>\n<p>Manifest format:</p>\n<pre><code class=\"language-json\">{\n  \"version\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"exported_at\": \"2026-05-17T09:14:32.847Z\",\n  \"source_chain_head\": 348201,\n  \"file_count\": 12,\n  \"files\": [\n    { \"path\": \"audit_chain.json\", \"sha256\": \"...\", \"size_bytes\": 2837492 },\n    { \"path\": \"audit_chain.csv\", \"sha256\": \"...\", \"size_bytes\": 1928374 },\n    { \"path\": \"projects.json\", \"sha256\": \"...\", \"size_bytes\": 84729 },\n    { \"path\": \"projects.csv\", \"sha256\": \"...\", \"size_bytes\": 41928 }\n  ],\n  \"signature_algorithm\": \"Ed25519\",\n  \"public_key_pem\": \"-----BEGIN PUBLIC KEY-----...-----END PUBLIC KEY-----\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-region bundle replication (slice 3).</li><li><strong>Deferred:</strong> Cold-storage archive for long-term retention (slice 3).</li><li><strong>Deferred:</strong> Bundle GPG-signing variant for paranoid customers (slice 3).</li><li><strong>Deferred:</strong> Differential exports (only changes since last bundle) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Build task fails</td><td>FR-MCP-007 error</td><td>status='failed' + sev-1 audit</td><td>Operator retries</td></tr><tr><td>KMS unavailable for signing</td><td>KMS error</td><td>Bundle status='failed'; sev-1</td><td>KMS recovery</td></tr><tr><td>S3 upload fails</td><td>API error</td><td>Status='failed'; retry path</td><td>Inherent</td></tr><tr><td>Non-deterministic zip detected</td><td>hash mismatch on test</td><td>CI fails the deploy</td><td>Fix ZIP library config</td></tr><tr><td>Pre-deletion gate triggered with old bundle (&gt; 90d)</td><td>check fails</td><td>412; operator requests new bundle</td><td>Re-request</td></tr><tr><td>3-per-year hit</td><td>counter</td><td>429</td><td>Operator escalates if legitimate need</td></tr><tr><td>Bundle exceeds 50 GB</td><td>size check</td><td>sev-2; build proceeds but slow</td><td>Future: split into multi-volume</td></tr><tr><td>Manifest signature corruption</td><td>verify endpoint</td><td>sev-1; bundle marked suspect</td><td>Re-issue bundle</td></tr><tr><td>Cross-tenant bundle leak via misconfigured RLS</td><td>RLS test</td><td>Periodic check</td><td>Inherent</td></tr><tr><td>Bundle ID collision (astronomical)</td><td>UUID partial unique</td><td>Inherent</td><td>None needed</td></tr><tr><td>Public key drift (regenerated after compromise)</td><td>key rotation table</td><td>Old bundles use old key; new use new key</td><td>Public key archive</td></tr><tr><td>Tenant data deleted before bundle build</td><td>source-empty bundle</td><td>Bundle empty; audit row notes</td><td>Pre-bundle data freeze (slice 3)</td></tr><tr><td>Audit chain segment &gt; 1M rows</td><td>size check</td><td>Bundle build slow; sev-3 log</td><td>Async pagination in builder</td></tr><tr><td>Build interrupted by deploy</td><td>task retry via FR-MCP-007 checkpoint</td><td>Resumes from last checkpoint</td><td>Inherent</td></tr><tr><td>Signed URL leaked via screen-share</td><td>inherent</td><td>30d window for download; passphrase optional add-on</td><td>Inherent UX warning</td></tr><tr><td>Cross-residency tenant export</td><td>residency-router consumed</td><td>Bundle stored in tenant's residency S3</td><td>Inherent</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> <code>ed25519-dalek</code> crate for keygen + sign + verify.</p>\n<p><strong>§11.2</strong> Deterministic ZIP via <code>zip</code> crate with explicit ZIP64 disable + fixed timestamp + sorted entries.</p>\n<p><strong>§11.3</strong> Manifest canonical JSON via sorted keys.</p>\n<p><strong>§11.4</strong> Per-tenant Ed25519 key never re-generated (would invalidate prior signatures); rotation = new keypair stored alongside old; old bundles verify with old key.</p>\n<p><strong>§11.5</strong> S3 lifecycle policy auto-deletes bundle objects at T+30d (defense-in-depth vs application cleanup).</p>\n<p><strong>§11.6</strong> Build task uses FR-MCP-007 worker pool; checkpoints per-source so crash mid-build resumes.</p>\n<p><strong>§11.7</strong> README.md in bundle includes verification instructions in English + Vietnamese.</p>\n<p><strong>§11.8</strong> Audit chain segment serialised as JSONL (one row per line) for streaming-friendly format.</p>\n<p><strong>§11.9</strong> Per-tenant residency: bundle stored in tenant's residency S3 per FR-TEN-103 trip-wire.</p>\n<p><strong>§11.10</strong> Chain-of-custody anchor at FR-OBS-009 cross-system; future-proofs auditor questions.</p>\n<hr>\n<p><em>End of FR-TEN-105 spec.</em></p>";
