window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TIME-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TIME service <strong>MUST</strong> ship VN Labour Code Art. 107 OT cap enforcement at <code>services/time/src/vn_labour/</code> with 4-tier hard-blocks (monthly + yearly + daily + weekly), per-Member approval tier flag, 80% soft warnings, vn-1-residency scoping, and 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>ot_breach_kind</code> enum: <code>('monthly_40h_breach','yearly_200h_breach','yearly_300h_breach_no_approval','daily_4h_breach','weekly_12h_breach')</code> per DEC-1392. Cardinality 5.</li></ol>\n<ol><li><strong>MUST</strong> apply caps ONLY when <code>Member's tenant.residency = 'vn-1'</code> per DEC-1394. Non-VN Members skip cap check entirely (other labour-law FRs handle).</li></ol>\n<ol><li><strong>MUST</strong> compute OT = <code>MAX(0, daily_total_hours - 8)</code> per Art. 107. Standard workday baseline 8h; everything beyond is OT.</li></ol>\n<ol><li><strong>MUST</strong> aggregate OT at 4 granularities via <code>vn_labour/aggregator.rs</code>:</li></ol>\n<ul><li><strong>Daily</strong>: SUM(OT hours) WHERE entry_date = $today.</li><li><strong>Weekly</strong>: SUM(OT hours) WHERE entry_date &gt;= $week_start (Monday-Sunday).</li><li><strong>Monthly</strong>: SUM(OT hours) WHERE entry_month = $current_month.</li><li><strong>Yearly</strong>: SUM(OT hours) WHERE entry_year = $current_year.</li></ul>\n<ol><li><strong>MUST</strong> check 4 hard caps pre-entry-write per DEC-1391 + DEC-1395:</li></ol>\n<ul><li><strong>Daily</strong>: new_daily_ot &gt; 4h → <code>daily_4h_breach</code>.</li><li><strong>Weekly</strong>: new_weekly_ot &gt; 12h → <code>weekly_12h_breach</code>.</li><li><strong>Monthly</strong>: new_monthly_ot &gt; 40h → <code>monthly_40h_breach</code>.</li><li><strong>Yearly (standard tier)</strong>: new_yearly_ot &gt; 200h AND approval='standard' → <code>yearly_200h_breach</code>.</li><li><strong>Yearly (extended tier)</strong>: new_yearly_ot &gt; 300h → <code>yearly_300h_breach_no_approval</code> (regardless of approval; 300h is absolute cap).</li><li>First-matching breach returned; entry write blocked with 412 + breach kind.</li></ul>\n<ol><li><strong>MUST</strong> emit 80% soft warning per DEC-1391. If new_total &gt; 0.80 × cap AND new_total ≤ cap: write proceeds + emits <code>time.ot_warning_issued</code> sev-3 + UI banner.</li></ol>\n<ol><li><strong>MUST</strong> consume per-Member <code>vn_ot_approval</code> flag per DEC-1393 from <code>hr_members.vn_ot_approval</code>. FR-HR-001 owns this field; CLO sets after MOLISA approval received.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vn_ot_tracking</code> table at migration <code>0003</code> for materialised aggregates: <code>(member_subject_id UUID, year INT, month INT, day DATE, week_start DATE, daily_ot_seconds INT, weekly_ot_seconds INT, monthly_ot_seconds INT, yearly_ot_seconds INT, updated_at TIMESTAMPTZ, PRIMARY KEY (member_subject_id, day))</code>. Updated transactionally on each TIME entry write. RLS scoped.</li></ol>\n<ol><li><strong>MUST</strong> expose approval-change endpoint <code>POST /v1/admin/members/{member_id}/vn-ot-approval</code> body <code>{ tier, molisa_doc_ref, expires_at }</code>. Caller has <code>clo</code> role. Updates <code>hr_members.vn_ot_approval</code> + emits <code>time.ot_approval_changed</code> sev-1.</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-1396:</li></ol>\n<ul><li><code>time.ot_warning_issued</code> (sev-3)</li><li><code>time.ot_breach_blocked</code> (sev-2 — material entry rejection)</li><li><code>time.ot_approval_changed</code> (sev-1 — compliance event)</li></ul>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow entry write that breaches per DEC-1391.</li></ol>\n<ol><li><strong>MUST NOT</strong> apply VN caps to non-vn-1 Members per DEC-1394.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why hard-block (§1 #5, DEC-1391)?</strong> Soft warnings get ignored. Hard-block forces conversation: either Member legitimately stops, or admin investigates rate-card/staffing issue, or extended approval requested.</p>\n<p><strong>Why 4 cap granularities (§1 #4-5)?</strong> Art. 107 specifies all 4 (daily, weekly, monthly, yearly). Single yearly aggregate would let Member burn 300h in January and crash; granular limits enforce smooth distribution.</p>\n<p><strong>Why approval tier flag (§1 #7, DEC-1393)?</strong> MOLISA-approved 300h tier is per-Member (not org-wide). Per-Member flag lets CLO grant on case-by-case basis without org-wide policy change.</p>\n<p><strong>Why residency scoping (§1 #2, DEC-1394)?</strong> Non-VN Members have their own labour laws (EU 48h/week, US no federal cap, etc.). Applying VN caps to non-VN Members is wrong; future FRs cover other jurisdictions.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0003_vn_ot_tracking.sql\nCREATE TYPE ot_breach_kind AS ENUM ('monthly_40h_breach','yearly_200h_breach','yearly_300h_breach_no_approval','daily_4h_breach','weekly_12h_breach');\n\nCREATE TABLE vn_ot_tracking (\n  member_subject_id UUID NOT NULL,\n  tenant_id UUID NOT NULL,\n  day DATE NOT NULL,\n  week_start DATE NOT NULL,\n  year INT NOT NULL,\n  month INT NOT NULL,\n  daily_ot_seconds INT NOT NULL DEFAULT 0,\n  weekly_ot_seconds INT NOT NULL DEFAULT 0,\n  monthly_ot_seconds INT NOT NULL DEFAULT 0,\n  yearly_ot_seconds INT NOT NULL DEFAULT 0,\n  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  PRIMARY KEY (member_subject_id, day)\n);\nALTER TABLE vn_ot_tracking ENABLE ROW LEVEL SECURITY;\nCREATE POLICY vn_ot_tracking_rls ON vn_ot_tracking\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON vn_ot_tracking FROM cyberos_app;\nGRANT UPDATE (daily_ot_seconds, weekly_ot_seconds, monthly_ot_seconds, yearly_ot_seconds, updated_at)\n  ON vn_ot_tracking TO cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/admin/members/{id}/vn-ot-approval                  (clo)\nGET    /v1/time/vn-ot/status?member_id=...                     (member own; or hr_admin)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>ot_breach_kind cardinality 5</strong>.</li><li><strong>Monthly 40h hard-block</strong> — 40h+1min entry → 412 + monthly_40h_breach.</li><li><strong>Yearly 200h block (standard)</strong> — Member at 200h0min, +1min entry → 412 + yearly_200h_breach.</li><li><strong>300h with approval</strong> — Member with tier='extended', at 250h, +30min entry → succeeds.</li><li><strong>300h absolute cap</strong> — Member with tier='extended', at 300h0min, +1min → 412 + yearly_300h_breach_no_approval.</li><li><strong>Daily 4h breach</strong> — single day 4h+1min OT → 412.</li><li><strong>Weekly 12h breach</strong> — sum across week 12h+1min → 412.</li><li><strong>80% warning emitted</strong> — 32h monthly (80% of 40h) → entry succeeds + audit warning.</li><li><strong>Non-VN Member skipped</strong> — sg-1 Member 50h monthly OT → succeeds without check.</li><li><strong>Approval change requires CLO</strong> — non-CLO call → 403.</li><li><strong>Approval change emits sev-1 audit</strong> — CLO updates tier → <code>time.ot_approval_changed</code>.</li><li><strong>OT calculation correct</strong> — 9h day = 1h OT.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>PII scrub</strong> — audit row carries Member hash only.</li><li><strong>Aggregator transactional</strong> — entry insert + tracking update atomic.</li><li><strong>3 memory audit kinds emitted</strong> in full lifecycle.</li><li><strong>RLS isolation</strong> — cross-tenant tracking invisible.</li><li><strong>Concurrent entries respect cap</strong> — race-safe via SELECT FOR UPDATE on tracking row.</li><li><strong>Week boundary correctly Monday</strong> — entry on Sunday counts to that week.</li><li><strong>Year rollover</strong> — Jan 1 entry counts to new year; Dec 31 to old.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn monthly_40h_hard_blocked() {\n    let ctx = TestContext::with_vn_member().await;\n    ctx.seed_ot_for_month(ctx.member_id, 40 * 3600).await;  // exactly 40h\n    let r = ctx.create_entry_with_ot(ctx.member_id, 60).await;  // +1min\n    assert_eq!(r.status(), 412);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"breach_kind\"], \"monthly_40h_breach\");\n}\n\n#[tokio::test]\nasync fn extended_approval_300h_works() {\n    let ctx = TestContext::with_vn_member_with_extended_approval().await;\n    ctx.seed_ot_for_year(ctx.member_id, 250 * 3600).await;\n    let r = ctx.create_entry_with_ot(ctx.member_id, 30 * 60).await;\n    assert_eq!(r.status(), 201);\n}\n\n#[tokio::test]\nasync fn non_vn_member_skipped() {\n    let ctx = TestContext::with_sg_member().await;\n    ctx.seed_ot_for_month(ctx.member_id, 50 * 3600).await;\n    let r = ctx.create_entry_with_ot(ctx.member_id, 8 * 3600).await;\n    assert_eq!(r.status(), 201);\n}\n\n#[tokio::test]\nasync fn 80pct_warning_emitted() {\n    let ctx = TestContext::with_vn_member().await;\n    ctx.seed_ot_for_month(ctx.member_id, 30 * 3600).await;  // 75% of 40h\n    let r = ctx.create_entry_with_ot(ctx.member_id, 3 * 3600).await;  // pushes to 33h (82.5%)\n    assert_eq!(r.status(), 201);\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"time.ot_warning_issued\"));\n}\n\n// 5.5..5.10: daily/weekly breaches, approval gate, aggregator transactional, race-safety</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-TIME-001 (entry write path). <strong>Cross-module:</strong> FR-HR-001 (vn_ot_approval flag), FR-TIME-002 (timer integration), FR-AUTH-101 (clo role), FR-AI-003, FR-MEMORY-111.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>time.ot_breach_blocked</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.ot_breach_blocked\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.member.456\",\n  \"trace_id\": \"...\",\n  \"payload\": {\n    \"member_subject_id_hash16\": \"f8a1...\",\n    \"breach_kind\": \"monthly_40h_breach\",\n    \"attempted_ot_seconds\": 60,\n    \"current_monthly_ot_seconds\": 144000,\n    \"cap_seconds\": 144000\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Non-VN labour codes (EU 48h, US OSHA) — slice 2 / per-jurisdiction FRs.</li><li><strong>Deferred:</strong> Tet/National holiday triple-pay flagging — slice 2.</li><li><strong>Deferred:</strong> Per-engagement OT cap (some clients want stricter) — slice 2.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Aggregator out-of-date</td><td>SELECT FOR UPDATE locks tracking row</td><td>Inherent consistency</td><td>None needed</td></tr><tr><td>Approval expired mid-year</td><td>check expires_at</td><td>Treated as 'standard' tier</td><td>CLO renews</td></tr><tr><td>Concurrent entry race</td><td>row lock</td><td>First wins</td><td>Inherent</td></tr><tr><td>Bulk import bypasses check</td><td>import handler must invoke same check</td><td>Sev-2 if missing</td><td>Code review</td></tr><tr><td>Cap reached exactly</td><td>inclusive vs exclusive</td><td>412 at +1s past cap</td><td>Inherent</td></tr><tr><td>Member transferred to vn-1 mid-year</td><td>check at write-time only</td><td>Subsequent entries enforced</td><td>History grandfathered</td></tr><tr><td>Daily cap reached but week not</td><td>order of checks</td><td>Daily wins (more granular)</td><td>Inherent</td></tr><tr><td>Year-end rollover entry</td><td>timestamp check</td><td>Counts to entry's date year</td><td>Inherent</td></tr><tr><td>Negative OT (correction reduces total)</td><td>aggregator handles signed</td><td>Subtracts correctly</td><td>Inherent</td></tr><tr><td>Tracking table desync vs entries</td><td>nightly reconciliation job</td><td>Sev-2 if drift detected</td><td>Rebuild from entries</td></tr><tr><td>Cross-tenant attempt</td><td>RLS</td><td>0 rows</td><td>Inherent</td></tr><tr><td>Approval ref doc lost</td><td>TEXT field; no FK</td><td>Soft loss; CLO re-attaches</td><td>Inherent</td></tr><tr><td>Member changes residency from vn-1 to sg-1</td><td>residency immutable per FR-TEN-103</td><td>Cannot happen</td><td>Inherent</td></tr><tr><td>Bulk entry retroactively breaches</td><td>check at write per entry</td><td>Each entry checked individually</td><td>Inherent</td></tr><tr><td>Approval tier changed mid-write</td><td>tx isolation</td><td>Last-write semantics</td><td>Race rare</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Aggregator uses materialized table for O(1) cap lookup; transactional update on every TIME entry write.</p>\n<p><strong>§11.2</strong> Week boundary: Monday 00:00 tenant timezone (default Asia/Ho_Chi_Minh).</p>\n<p><strong>§11.3</strong> Year boundary: Jan 1 00:00 tenant timezone.</p>\n<p><strong>§11.4</strong> Cap order checked: daily → weekly → monthly → yearly (most granular first).</p>\n<p><strong>§11.5</strong> OT computed per entry: <code>OT = MAX(0, entry_seconds_in_day - (8*3600 - prior_today_seconds))</code>.</p>\n<p><strong>§11.6</strong> Concurrent insert race: <code>SELECT ... FROM vn_ot_tracking WHERE member_id=$1 AND day=$2 FOR UPDATE</code> before write.</p>\n<p><strong>§11.7</strong> Approval expires_at check: if expired, fallback to 'standard' tier silently + emit sev-3 warning.</p>\n<p><strong>§11.8</strong> Bulk import handler MUST invoke same cap_check per entry; CI lint enforces.</p>\n<p><strong>§11.9</strong> Nightly reconciliation: SUM(entries.duration) vs tracking.daily_ot_seconds; sev-2 alert on drift &gt; 60s.</p>\n<p><strong>§11.10</strong> PII: Member identifier hashed to 16-hex in chain; raw retained in DB.</p>\n<hr>\n<p><em>End of FR-TIME-007 spec.</em></p>";
