window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOC-009"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The DOC service <strong>MUST</strong> ship renewal proposal generation at <code>services/doc/src/renewal/</code> triggered at FR-DOC-008 d90 alert or manual, draft via FR-AI-003, AM review required, creates child doc with parent link, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> trigger at d90 alert via FR-DOC-008 hook OR manual <code>POST /v1/doc/documents/{id}/draft-renewal</code> per DEC-1730. Auto-trigger ONLY if <code>renewal_terms.auto_renew = true</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>renewal_recommendation</code> against closed enum per DEC-1732.</li></ol>\n<ol><li><strong>MUST</strong> generate draft via <code>draft_generator.rs::generate(parent_doc)</code>:</li></ol>\n<ul><li>Pull parent terms (effective_date, expiry_date, renewal_terms.term_months).</li><li>Compute new dates: new_effective = old_expiry + 1d; new_expiry = new_effective + term_months.</li><li>CPI adjust price per <code>cpi_adjuster.rs::adjust(old_price, residency, since_date)</code>.</li><li>AI summarize scope-change recommendation.</li></ul>\n<ol><li><strong>MUST</strong> queue for AM review per DEC-1733 — NEVER auto-send to customer.</li></ol>\n<ol><li><strong>MUST</strong> on approval per DEC-1734:</li></ol>\n<ul><li>Create new doc row, <code>parent_contract_id = original.document_id</code>.</li><li>Status='draft'.</li><li>Lifecycle status compute via FR-DOC-007 (will be 'draft' if effective &gt; now).</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0004</code>: ``<code>sql CREATE TABLE doc_renewal_drafts ( draft_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, parent_document_id UUID NOT NULL UNIQUE,  -- one active draft per parent recommendation TEXT NOT NULL CHECK (recommendation IN ('auto_renew_as_is','renew_with_price_adj','renew_with_scope_change','do_not_renew')), draft_terms JSONB NOT NULL, ai_rationale TEXT, status TEXT NOT NULL DEFAULT 'pending_review' CHECK (status IN ('pending_review','approved','dismissed','sent','signed')), reviewed_by UUID, reviewed_at TIMESTAMPTZ, child_document_id UUID, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); ALTER TABLE doc_renewal_drafts ENABLE ROW LEVEL SECURITY; CREATE POLICY renewal_drafts_rls ON doc_renewal_drafts USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON doc_renewal_drafts FROM cyberos_app; GRANT UPDATE (status, reviewed_by, reviewed_at, child_document_id) ON doc_renewal_drafts TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST   /v1/doc/documents/{id}/draft-renewal   (manual trigger, AM/CLO) GET    /v1/doc/renewal-drafts                 (list pending review) POST   /v1/doc/renewal-drafts/{id}/approve    (creates child doc) POST   /v1/doc/renewal-drafts/{id}/dismiss POST   /v1/doc/renewal-drafts/{id}/send       (FR-EMAIL-009 send after approval) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1735. PII per FR-MEMORY-111: terms+rationale SHA-256 hashed; ids ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from trigger → AI → AM review → child create → send → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> auto-send per DEC-1733.</li></ol>\n<ol><li><strong>MUST NOT</strong> create child doc without parent_contract_id per DEC-1734.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why d90 trigger (DEC-1730)?</strong> Aligns with FR-DOC-008 first alert; gives full quarter for negotiation.</p>\n<p><strong>Why manual approval (DEC-1733)?</strong> Price changes + scope changes are commercial decisions; AM owns relationship.</p>\n<p><strong>Why parent link (DEC-1734)?</strong> Lineage required for audit + legal traceability (\"which contract supersedes which\").</p>\n<p><strong>Why CPI adjustment default (DEC-1731)?</strong> Industry-standard escalation; AM overrides per relationship.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample draft:</p>\n<pre><code class=\"language-json\">{\n  \"draft_id\": \"uuid\",\n  \"parent_document_id\": \"uuid\",\n  \"recommendation\": \"renew_with_price_adj\",\n  \"draft_terms\": {\n    \"new_effective_date\": \"2028-01-01\",\n    \"new_expiry_date\": \"2029-12-31\",\n    \"new_monthly_fee_vnd\": 11000000,\n    \"old_monthly_fee_vnd\": 10000000,\n    \"cpi_adjustment_pct\": 10.0,\n    \"scope_changes\": []\n  },\n  \"ai_rationale\": \"Standard CPI-indexed renewal; no scope changes detected; original account in good standing.\",\n  \"status\": \"pending_review\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Auto-trigger at d90 with auto_renew=true</strong>. 2. <strong>Manual trigger via POST</strong>. 3. <strong>No trigger when auto_renew=false (manual only)</strong>. 4. <strong>AM review required (no auto-send)</strong>. 5. <strong>Approve creates child doc with parent link</strong>. 6. <strong>Status enum 4 + cardinality test</strong>. 7. <strong>CPI adjustment computed</strong>. 8. <strong>4 memory audit kinds emitted</strong>. 9. <strong>PII scrubbed (terms+rationale SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>AM/CLO role only</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>UNIQUE on parent_document_id (one active draft)</strong>. 14. <strong>Send endpoint requires status=approved</strong>. 15. <strong>Append-only via REVOKE except 4 cols</strong>. 16. <strong>Dismiss → status=dismissed</strong>. 17. <strong>Recommendation enum 4 values</strong>. 18. <strong>AI failure → status=failed + sev-2 + retry</strong>. 19. <strong>Child doc inherits parties (with refresh prompt)</strong>. 20. <strong>Send via FR-EMAIL-009 with renewal template</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn triggers_at_d90_with_auto_renew() {\n    let ctx = TestContext::doc_expires_in_90d_with_auto_renew().await;\n    ctx.run_d90_alert(ctx.doc_id).await;\n    tokio::time::sleep(Duration::from_secs(1)).await;\n    let drafts = ctx.fetch_renewal_drafts(ctx.doc_id).await;\n    assert_eq!(drafts.len(), 1);\n}\n\n#[tokio::test]\nasync fn never_auto_sends() {\n    let ctx = TestContext::with_renewal_draft().await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    let sent_count = ctx.email_send_count().await;\n    assert_eq!(sent_count, 0);\n}\n\n#[tokio::test]\nasync fn approve_creates_child_doc() {\n    let ctx = TestContext::with_renewal_draft().await;\n    ctx.approve_draft(ctx.draft_id).await;\n    let row = ctx.fetch_draft(ctx.draft_id).await;\n    assert!(row.child_document_id.is_some());\n    let child = ctx.fetch_doc(row.child_document_id.unwrap()).await;\n    assert_eq!(child.parent_contract_id, Some(ctx.parent_doc_id));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-DOC-007, FR-CUO-101. <strong>Cross-module:</strong> FR-DOC-008 (d90 trigger), FR-EMAIL-009 (send), FR-AI-003 (draft+rationale), FR-AUTH-101 (AM/CLO role), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>AI timeout</td><td>retry 1x</td><td>sev-2 fallback minimal draft</td><td>AM completes manually</td></tr><tr><td>CPI source unavailable</td><td>use 5% default</td><td>sev-2</td><td>inherent</td></tr><tr><td>Duplicate draft race</td><td>UNIQUE on parent_doc_id</td><td>second skipped</td><td>inherent</td></tr><tr><td>Auto_renew=false unexpected</td><td>check</td><td>no auto trigger</td><td>inherent</td></tr><tr><td>Approve race</td><td>UPDATE WHERE pending</td><td>first wins</td><td>inherent</td></tr><tr><td>Child doc create fails</td><td>rollback approval</td><td>sev-1</td><td>retry</td></tr><tr><td>Parent already renewed</td><td>check existing chain</td><td>reject</td><td>inherent</td></tr><tr><td>AM not assigned</td><td>warn + queue</td><td>inherent</td><td>reassign</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Send fails</td><td>retry</td><td>sev-2</td><td>manual resend</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 CPI: per-residency lookup table (vn-1: VN CPI, sg-1: SG CPI, eu-1: EU HICP, us-1: US CPI-U).</li><li>§11.2 AI rationale prompt: \"Summarize this renewal proposal (scope/price changes) in 2-3 sentences.\"</li><li>§11.3 Child doc inherits parties array; AM prompted to confirm/update before send.</li><li>§11.4 memory audit body: parent_doc_id, recommendation; draft_terms SHA256.</li><li>§11.5 Send templates: branded per tenant (FR-PORTAL-002 brand pack).</li></ul>\n<hr>\n<p><em>End of FR-DOC-009 spec.</em></p>";
