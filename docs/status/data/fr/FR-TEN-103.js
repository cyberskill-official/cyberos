window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TEN-103"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship 4-residency provisioning (<code>sg-1</code>, <code>eu-1</code>, <code>us-1</code>, <code>vn-1</code>) with per-residency Postgres + S3 + NATS + KMS + Stripe + AUTH-issuer separation, defense-in-depth cross-residency-write trip-wires, atomic-at-residency provisioning, per-residency memory chain partitioning, residency-aware logging context, 6-component health check + CLI, and 8 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define the closed <code>residency</code> Postgres enum at migration <code>0015</code>: <code>('sg-1','eu-1','us-1','vn-1')</code>. CI cardinality test asserts exactly 4 per DEC-920. Adding a fifth requires schema migration + DEC entry.</li></ol>\n<ol><li><strong>MUST</strong> change <code>tenant_residency_map.residency</code> column type from TEXT to <code>residency</code> enum via migration <code>0003</code> modification (single source of truth — no free-text residency anywhere). Existing rows are validated against the enum at migration; non-conforming rows reject migration (fail-fast).</li></ol>\n<ol><li><strong>MUST</strong> enforce closed currency → residency mapping per DEC-922 (consistent with FR-TEN-003 DEC-785 + FR-TEN-101 DEC-825):</li></ol>\n<ul><li><code>VND → vn-1</code></li><li><code>SGD → sg-1</code></li><li><code>EUR → eu-1</code></li><li><code>GBP → eu-1</code></li><li><code>USD → us-1</code> The mapping is a <code>const fn</code> in <code>services/ten/src/residency/mod.rs::derive_residency(billing_currency)</code>; no other code path may compute the mapping (single source of truth).</li></ul>\n<ol><li><strong>MUST</strong> lock <code>tenant.residency</code> post-provisioning per DEC-925. Migration <code>0016</code> includes a trigger <code>trg_residency_immutable()</code> that RAISEs on any UPDATE attempting to change <code>tenants.residency</code> (analogous to FR-TEN-003 <code>trg_billing_currency_immutable</code> per DEC-798). Mutation requires new tenant + manual data migration.</li></ol>\n<ol><li><strong>MUST</strong> provision per-residency infrastructure via Terraform modules at <code>infra/terraform/residency/{residency}/main.tf</code>. Each residency stands up:</li></ol>\n<ul><li>One Aurora PostgreSQL cluster in a private VPC (no peering per DEC-928).</li><li>One S3 bucket pair: <code>cyberos-{residency}-tenants</code> (data) + <code>cyberos-{residency}-audit</code> (audit archive); cross-region replication DISABLED per DEC-933.</li><li>One NATS cluster (3-node JetStream); subjects local per DEC-932.</li><li>One AWS KMS key for envelope encryption (DEC-929: region-pinned).</li><li>One OBS stack subset (Loki + Tempo + Prometheus regional shards) per DEC-934.</li><li>One Stripe account binding per DEC-935 (FR-TEN-003 DEC-801 already provisioned; this FR consumes).</li><li>One AUTH issuer URL per DEC-931 (<code>https://auth.&lt;residency&gt;.cyberos.world</code>).</li></ul>\n<ol><li><strong>MUST</strong> provide a residency router in <code>services/ten/src/residency/</code>:</li></ol>\n<ul><li><code>pool_router.rs</code>: <code>Map&lt;Residency, PgPool&gt;</code> with per-residency connection strings loaded from KMS-encrypted secrets.</li><li><code>s3_router.rs</code>: <code>Map&lt;Residency, aws_sdk_s3::Client&gt;</code> configured to the correct AWS region per DEC-929.</li><li><code>nats_router.rs</code>: <code>Map&lt;Residency, async_nats::Client&gt;</code> connected to per-residency NATS clusters.</li><li><code>kms_router.rs</code>: <code>Map&lt;Residency, aws_sdk_kms::Client&gt;</code> for envelope decrypt operations.</li><li><code>issuer_map.rs</code>: <code>Map&lt;Residency, IssuerConfig&gt;</code> mapping residency → AUTH issuer URL + JWKS URL.</li></ul>\n<ol><li><strong>MUST</strong> route every tenant-scoped operation through the residency router. Service-level handlers MUST:</li></ol>\n<ul><li>Receive <code>tenant_id</code> from request context.</li><li>Lookup <code>tenants.residency</code> via a per-residency-fanout query (one cross-residency lookup is permitted at the entry: the JWT carries <code>residency</code> claim per FR-AUTH-004 + this FR's modification, so the lookup is JWT-only in 99% of cases).</li><li>Select the correct pool/client from the router.</li><li>Issue the operation against that pool only.</li><li>Mis-route (e.g., handler uses sg-1 pool for an eu-1 tenant) is caught by the trip-wire trigger + emits <code>ten.residency_pool_misroute</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> install a cross-residency-write trip-wire trigger on EVERY tenant-scoped Postgres table per DEC-924 + DEC-939 + feature-request-audit skill rule 13 derivative. The trigger function <code>trg_cross_residency_write_block()</code>: ``<code>sql CREATE OR REPLACE FUNCTION trg_cross_residency_write_block() RETURNS trigger AS $$ DECLARE expected_residency TEXT := current_setting('auth.residency', true); BEGIN IF expected_residency IS NULL OR expected_residency = '' THEN RAISE EXCEPTION 'cross_residency_write_blocked: auth.residency session var not set'; END IF; IF EXISTS (SELECT 1 FROM tenants WHERE id = NEW.tenant_id AND residency::text != expected_residency) THEN RAISE EXCEPTION 'cross_residency_write_blocked: tenant residency != session residency'; END IF; RETURN NEW; END $$ LANGUAGE plpgsql; </code>`<code> Applied via cursor loop in migration </code>0016` to all 28 tenant-scoped tables identified at migration time. New tables added in future FRs MUST include this trigger.</li></ol>\n<ol><li><strong>MUST</strong> set the session-local <code>auth.residency</code> Postgres setting at handler entry from the validated JWT's <code>residency</code> claim. The FR-AUTH-004 JWT mint is extended to include this claim; FR-AUTH-004's <code>services/auth/src/handlers/login.rs</code> modification covers the wiring.</li></ol>\n<ol><li><strong>MUST</strong> enforce per-residency JWT issuer validation per DEC-931. The AUTH issuer URL embedded in the JWT (<code>iss</code> claim) MUST match the residency-derived expected issuer; mismatch returns <code>401 wrong_residency_token</code> + emits <code>ten.cross_residency_access_attempt</code> sev-1.</li></ol>\n<ol><li><strong>MUST</strong> partition the memory audit chain per residency per DEC-926. Each residency's services append to that residency's chain only; the chain head is stored in that residency's Aurora <code>memory.chain_state</code> table. Cross-residency memory events are FORBIDDEN — attempting to append a chain row for a tenant in a different residency than the current handler's residency raises <code>cross_residency_memory_event_blocked</code> + emits the corresponding memory row in the LOCAL chain.</li></ol>\n<ol><li><strong>MUST</strong> provision atomically per residency per DEC-936. The <code>services/ten/src/provisioning/orchestrator.rs</code> (modified per FR-TEN-001 build envelope) runs:</li><li>Open tx in target residency's Aurora.</li><li>Insert tenant row + tenant_residency_map row.</li><li>Create S3 prefix in target residency's bucket (idempotent marker object).</li><li>Register NATS subject in target residency's cluster.</li><li>Verify AUTH bootstrap in target residency.</li><li>Commit tx. On any step failure: ROLLBACK tx + manual cleanup of any partial S3/NATS state via <code>cyberos-ten residency-cleanup-orphan</code> (slice 3 CLI; slice 2 = operator runs cleanup manually after sev-1 alert per DEC-944).</li></ol>\n<ol><li><strong>MUST</strong> add <code>auth.residency</code> to AUTH JWT claims per #9 + DEC-931. JWT shape post-this-FR: <code>{ sub, tenant_id, residency, exp, iat, iss, aud, scope_grants, persona }</code>. Tokens issued before this FR ships (legacy) lack the claim; transitional handler accepts them only for 24h post-deploy then enforces presence (fail closed).</li></ol>\n<ol><li><strong>MUST</strong> carry <code>residency=&lt;rid&gt;</code> field on every log line via <code>tracing::instrument(fields(residency = %ctx.residency))</code> per DEC-942. Missing field on any log line emitted from a tenant-scoped handler raises an OBS alarm sev-3 (<code>ten.residency_logging_context_missing</code> — informational, ops sweep).</li></ol>\n<ol><li><strong>MUST</strong> ship <code>cyberos-ten residency-status</code> CLI per DEC-943 — 6-component health check per residency:</li><li>Aurora connectivity (SELECT 1 + &lt; 100ms).</li><li>S3 reachability (PUT/DELETE marker object + &lt; 500ms).</li><li>NATS heartbeat (publish + subscribe round-trip + &lt; 200ms).</li><li>Stripe ping (GET /v1/account + &lt; 1s — only stripe-rail residencies).</li><li>KMS responsiveness (Encrypt 32 bytes + &lt; 200ms).</li><li>AUTH issuer responsiveness (GET JWKS + &lt; 500ms). Output: per-residency score 0-6 with timing per component; exit code 0 if all 4 residencies score ≥ 5, exit code 73 otherwise.</li></ol>\n<ol><li><strong>MUST</strong> emit 8 memory audit row kinds per DEC-941 (feature-request-audit skill rule 6 namespace pattern):</li></ol>\n<ul><li><code>ten.tenant_residency_assigned</code> (sev-2 — material commercial event)</li><li><code>ten.residency_provisioned</code> (sev-1 — infrastructure event; one per residency standup)</li><li><code>ten.residency_pool_misroute</code> (sev-1 — silent-leak prevented)</li><li><code>ten.cross_residency_access_attempt</code> (sev-1 — security signal)</li><li><code>ten.cross_residency_write_blocked</code> (sev-1 — trip-wire fired)</li><li><code>ten.cross_residency_memory_event_blocked</code> (sev-1 — chain pollution prevented)</li><li><code>ten.residency_health_degraded</code> (sev-2 — one component failing)</li><li><code>ten.residency_kms_unavailable</code> (sev-1 — encryption broken in one region)</li></ul>\n<ol><li><strong>MUST</strong> maintain <code>residency_health_log</code> table at migration <code>0017</code>: <code>(id BIGSERIAL PRIMARY KEY, residency residency NOT NULL, component TEXT NOT NULL CHECK (component IN ('aurora','s3','nats','stripe','kms','auth_issuer')), status TEXT NOT NULL CHECK (status IN ('healthy','degraded','down')), latency_ms INT, checked_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. Append-only via REVOKE per feature-request-audit skill rule 12. Per-residency append + global read (no RLS — health is system-tenant scope).</li></ol>\n<ol><li><strong>MUST</strong> map vn-1 to ap-southeast-1 physical region per DEC-930. The mapping is documented in <code>services/ten/src/residency/mod.rs</code> const + reflected in <code>infra/terraform/residency/vn-1/main.tf</code> as <code>provider \"aws\" { region = \"ap-southeast-1\" }</code>. PDPL Law 91/2025 §17 disclosure is presented at FR-TEN-101 signup for VN-residency tenants.</li></ol>\n<ol><li><strong>MUST</strong> require explicit <code>--residency</code> flag on FR-TEN-001's <code>cyberos-ten provision</code> CLI per DEC-945 (no auto-derive at slice 2). CCO process review checks consistency between provided <code>--billing-currency</code> and <code>--residency</code> against the DEC-922 mapping; mismatch is rejected with exit code 64 + message naming the expected residency.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub per-residency memory rows via FR-MEMORY-111 (feature-request-audit skill rule 18). Cross-residency event payloads carry <code>tenant_id_hash16</code> not the raw tenant_id (defense-in-depth: even if a cross-residency audit row leaks across residencies due to incident, no PII flows).</li></ol>\n<ol><li><strong>MUST</strong> thread W3C <code>traceparent</code> across the residency-fanout entry (single-cross-residency lookup) + per-residency operation (feature-request-audit skill rule 22 + 23 + 24). Trace_id present on every memory row + every log line.</li></ol>\n<ol><li><strong>MUST NOT</strong> support automatic failover between residencies at slice 2 per DEC-940. Cross-region DR is out-of-scope; per-residency multi-AZ Aurora is the DR primitive at slice 2.</li></ol>\n<ol><li><strong>MUST NOT</strong> share KMS keys across residencies per DEC-929. Per-residency keys are separate AWS KMS aliases; cross-region key replication NOT enabled (consistent with DEC-921 no-shared-infra).</li></ol>\n<ol><li><strong>MUST NOT</strong> allow <code>tenant_id</code> collision across residencies per DEC-927. UUIDv7 generation is collision-free across regions via the residency-prefix nibble pattern: high nibble of byte 6 encodes residency (<code>0=sg-1, 1=eu-1, 2=us-1, 3=vn-1</code>). This is reserved encoding; FR-TEN-001 modified_files includes the generator update.</li></ol>\n<ol><li><strong>SHOULD</strong> observe per-residency p95 latency for tenant-scoped operations via OTel histogram <code>tenant_op_duration_seconds_by_residency</code>. Alarms route to FR-OBS-007 runbook router per residency.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why no-shared-infra per residency (§1 #5, DEC-921)?</strong> Compliance regulators (GDPR Art. 44, PDPA §26, PDPL §17) treat \"shared\" as \"transferred.\" Sharing an Aurora cluster across residencies means EU customer data is technically in scope of US-jurisdiction queries if a US handler executes against it. Zero-sharing is the only defensible architecture for regulated markets.</p>\n<p><strong>Why defense-in-depth trip-wires (§1 #8, DEC-924)?</strong> Cross-residency leaks are silent failures — the data just moves to the wrong region and we don't know until audit (typically years later). Three layers (pool router + trigger + Postgres FDW absence) means catching the bug at the first of: bad code (router catches), bad config (trigger catches), bad infra (no FDW to traverse). Each layer can fail; three together approach zero leakage probability.</p>\n<p><strong>Why memory chain per-residency (§1 #11, DEC-926)?</strong> Audit chains are subpoena-able. A single global chain means subpoena for tenant A's audit history forces production of ALL tenants' chain rows (the chain is a Merkle structure — you can't redact). Per-residency chains scope the subpoena response to one residency's tenants; cross-residency tenants are out of scope.</p>\n<p><strong>Why per-residency KMS keys (§1 #5, DEC-929)?</strong> Compromise scoping. One leaked KMS key = one residency's data decryptable. Cross-residency key sharing turns one compromise into four residencies' data exposed. Aligns with NIST SP 800-57 key-scoping guidance.</p>\n<p><strong>Why vn-1 in ap-southeast-1 physical (§1 #18, DEC-930)?</strong> AWS has no Vietnam region (Q2 2026 unchanged). Self-hosting in VN is infeasible at our scale. The PDPL §17 contract-residency clause permits this with explicit customer disclosure — the FR-TEN-101 signup consent flow handles the disclosure. When AWS opens a VN region, vn-1 migrates physically (data + Terraform); the residency identifier stays <code>vn-1</code> so customers don't see a label change.</p>\n<p><strong>Why JWT-carries-residency (§1 #9, DEC-931)?</strong> The JWT is presented at every request; reading the residency from the JWT is free (already validated). Looking up <code>tenants.residency</code> per request would require a cross-residency lookup — defeats the whole architecture. JWT-carries-residency means the per-request residency is signed by the AUTH issuer + tamper-evident.</p>\n<p><strong>Why immutable residency (§1 #4, DEC-925)?</strong> Changing residency = physically moving data across regions = a migration project (Aurora dump + restore + verify + cutover + back-out plan). Allowing UPDATE on <code>tenants.residency</code> would silently corrupt — the row would say \"eu-1\" but the data is still in us-1's bucket/cluster. Lock at the schema level, force migration through ops review.</p>\n<p><strong>Why atomic provisioning per-residency (§1 #12, DEC-936)?</strong> Half-provisioned tenants accumulate (tenants table row but no S3 prefix). At scale, ops alerts about \"missing S3 prefix\" become noise. Atomic transitions ensure tenant either exists fully or not at all in each residency. Cross-residency atomicity is intentionally NOT attempted (would require 2-phase commit across AWS regions — operational nightmare).</p>\n<p><strong>Why session-local <code>auth.residency</code> Postgres setting (§1 #9)?</strong> RLS evaluates <code>current_setting('auth.residency')</code> per row at query time. Setting it per-request via <code>SET LOCAL</code> makes it visible to RLS predicates + trip-wire triggers without polluting connection state. This is standard Postgres RLS pattern.</p>\n<p><strong>Why explicit --residency flag (§1 #19, DEC-945)?</strong> Auto-derivation hides operator intent. If billing_currency is wrong, auto-derive propagates the error. Explicit flag forces operator to commit to a residency separately from currency; CCO process review catches mismatches before provisioning.</p>\n<p><strong>Why 8 audit kinds with heavy sev-1 weighting (§1 #16, DEC-941)?</strong> Every kind in this FR is a cross-residency event — by definition unusual + forensically critical. Sev-1 routes to FR-OBS-007's CHAT/PagerDuty path for immediate response. Cross-residency leakage is a regulatory-reportable event; sev-1 catches it before any breach-notification window closes.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0015_residency_enum.sql\nCREATE TYPE residency AS ENUM ('sg-1','eu-1','us-1','vn-1');\n\n-- Modification to 0003_tenant_residency_map.sql (referenced in modified_files):\nALTER TABLE tenant_residency_map ALTER COLUMN residency TYPE residency USING residency::residency;\nALTER TABLE tenants ADD COLUMN residency residency;  -- nullable until backfill complete\nUPDATE tenants t SET residency = (SELECT residency FROM tenant_residency_map WHERE tenant_id=t.id);\nALTER TABLE tenants ALTER COLUMN residency SET NOT NULL;\n\n-- Immutability trigger (DEC-925)\nCREATE OR REPLACE FUNCTION trg_residency_immutable() RETURNS trigger AS $$\nBEGIN\n  IF OLD.residency IS DISTINCT FROM NEW.residency THEN\n    RAISE EXCEPTION 'residency_immutable: cannot change residency on existing tenant (DEC-925); create new tenant + manual data migration';\n  END IF;\n  RETURN NEW;\nEND $$ LANGUAGE plpgsql;\nCREATE TRIGGER tenants_residency_immutable\n  BEFORE UPDATE ON tenants\n  FOR EACH ROW EXECUTE FUNCTION trg_residency_immutable();\n\n-- 0016_residency_trip_wire.sql\nCREATE OR REPLACE FUNCTION trg_cross_residency_write_block() RETURNS trigger AS $$\nDECLARE expected_residency TEXT := current_setting('auth.residency', true);\nDECLARE row_tenant_id UUID;\nDECLARE row_residency TEXT;\nBEGIN\n  IF expected_residency IS NULL OR expected_residency = '' THEN\n    RAISE EXCEPTION 'cross_residency_write_blocked: auth.residency session var not set';\n  END IF;\n  -- NEW.tenant_id may be null on system-tenant rows (e.g., audit log); skip check\n  IF NEW.tenant_id IS NULL THEN RETURN NEW; END IF;\n  SELECT residency::text INTO row_residency FROM tenants WHERE id = NEW.tenant_id;\n  IF row_residency IS NULL THEN\n    RAISE EXCEPTION 'cross_residency_write_blocked: tenant % not found in this residency', NEW.tenant_id;\n  END IF;\n  IF row_residency != expected_residency THEN\n    RAISE EXCEPTION 'cross_residency_write_blocked: tenant % residency=% but session residency=%', NEW.tenant_id, row_residency, expected_residency;\n  END IF;\n  RETURN NEW;\nEND $$ LANGUAGE plpgsql;\n\n-- Apply to all tenant-scoped tables (cursor loop):\nDO $$\nDECLARE t RECORD;\nBEGIN\n  FOR t IN\n    SELECT c.table_name\n      FROM information_schema.columns c\n      WHERE c.column_name='tenant_id' AND c.table_schema='public'\n  LOOP\n    EXECUTE format(\n      'CREATE TRIGGER cross_residency_write_trigger BEFORE INSERT OR UPDATE ON %I FOR EACH ROW EXECUTE FUNCTION trg_cross_residency_write_block()',\n      t.table_name\n    );\n  END LOOP;\nEND $$;\n\n-- 0017_residency_health_log.sql\nCREATE TABLE residency_health_log (\n  id BIGSERIAL PRIMARY KEY,\n  residency residency NOT NULL,\n  component TEXT NOT NULL CHECK (component IN ('aurora','s3','nats','stripe','kms','auth_issuer')),\n  status TEXT NOT NULL CHECK (status IN ('healthy','degraded','down')),\n  latency_ms INT,\n  checked_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_residency_health_residency_checked ON residency_health_log(residency, checked_at DESC);\nREVOKE UPDATE, DELETE ON residency_health_log FROM cyberos_app;</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/ten/src/residency/mod.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug, sqlx::Type, serde::Serialize, serde::Deserialize)]\n#[sqlx(type_name = \"residency\", rename_all = \"lowercase\")]\npub enum Residency { Sg1, Eu1, Us1, Vn1 }\n\nimpl Residency {\n    pub const ALL: [Residency; 4] = [Residency::Sg1, Residency::Eu1, Residency::Us1, Residency::Vn1];\n\n    pub const fn as_str(self) -&gt; &amp;'static str {\n        match self {\n            Residency::Sg1 =&gt; \"sg-1\",\n            Residency::Eu1 =&gt; \"eu-1\",\n            Residency::Us1 =&gt; \"us-1\",\n            Residency::Vn1 =&gt; \"vn-1\",\n        }\n    }\n\n    pub const fn aws_region(self) -&gt; &amp;'static str {\n        match self {\n            Residency::Sg1 =&gt; \"ap-southeast-1\",\n            Residency::Eu1 =&gt; \"eu-west-1\",\n            Residency::Us1 =&gt; \"us-east-1\",\n            Residency::Vn1 =&gt; \"ap-southeast-1\",  // vn-1 physically in Singapore (DEC-930)\n        }\n    }\n}\n\npub const fn derive_residency(currency: BillingCurrency) -&gt; Residency {\n    match currency {\n        BillingCurrency::Vnd =&gt; Residency::Vn1,\n        BillingCurrency::Sgd =&gt; Residency::Sg1,\n        BillingCurrency::Eur =&gt; Residency::Eu1,\n        BillingCurrency::Gbp =&gt; Residency::Eu1,\n        BillingCurrency::Usd =&gt; Residency::Us1,\n    }\n}\n\n// services/ten/src/residency/pool_router.rs\npub struct PoolRouter {\n    pools: std::collections::HashMap&lt;Residency, sqlx::PgPool&gt;,\n}\n\nimpl PoolRouter {\n    pub fn pool_for(&amp;self, residency: Residency) -&gt; Result&lt;&amp;sqlx::PgPool, ResidencyError&gt; {\n        self.pools.get(&amp;residency).ok_or(ResidencyError::ResidencyUnavailable(residency))\n    }\n}</code></pre>\n<h3>3.3 CLI</h3>\n<pre><code class=\"language-text\">cyberos-ten residency-status [--residency sg-1|eu-1|us-1|vn-1] [--json]\ncyberos-ten residency-cleanup-orphan &lt;tenant_id&gt;     (slice 3)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Enum cardinality</strong> — <code>residency_enum_cardinality_test</code> asserts enum = exactly <code>{sg-1, eu-1, us-1, vn-1}</code>.</li><li><strong>Currency → residency mapping</strong> — <code>derive_residency(VND)=vn-1</code>, <code>(SGD)=sg-1</code>, <code>(EUR)=eu-1</code>, <code>(GBP)=eu-1</code>, <code>(USD)=us-1</code>.</li><li><strong>Residency immutable trigger</strong> — <code>UPDATE tenants SET residency='eu-1' WHERE id=&lt;sg-1 tenant&gt;</code> raises <code>residency_immutable</code>.</li><li><strong>Cross-residency-write trip-wire</strong> — handler with <code>SET LOCAL auth.residency='sg-1'</code> attempting INSERT into a tenant-scoped table with tenant in eu-1 raises <code>cross_residency_write_blocked</code>.</li><li><strong>Pool router selects correct pool</strong> — <code>pool_router.pool_for(Eu1)</code> returns the eu-1 pool; mis-call returns ResidencyError.</li><li><strong>Pool misroute detected</strong> — handler using sg-1 pool for eu-1 tenant operation triggers trip-wire → 500 + <code>ten.residency_pool_misroute</code> sev-1 emitted.</li><li><strong>JWT cross-residency rejection</strong> — request with JWT carrying <code>iss=https://auth.us-1.cyberos.world</code> against eu-1 endpoint returns 401 + <code>ten.cross_residency_access_attempt</code>.</li><li><strong>Atomic provisioning</strong> — provisioning failure at S3 step rolls back Aurora insert; no half-provisioned state in any residency.</li><li><strong>6-component health check</strong> — <code>cyberos-ten residency-status</code> reports all 6 components per residency with timing.</li><li><strong>memory chain partitioning</strong> — appending a chain row for an eu-1 tenant from a sg-1 handler raises <code>cross_residency_memory_event_blocked</code>.</li><li><strong>KMS per-residency</strong> — eu-1 encrypt uses eu-1 KMS key; sg-1 encrypt uses sg-1 KMS key; cross-region attempts fail.</li><li><strong>vn-1 physical region</strong> — Terraform plan for vn-1 targets ap-southeast-1; AWS resources are tagged with <code>cyberos_residency=vn-1</code> for clarity.</li><li><strong>UUIDv7 residency-prefix nibble</strong> — tenant_id high nibble of byte 6 = residency index; collision-free across residencies.</li><li><strong>Explicit --residency flag required</strong> — provision CLI without <code>--residency</code> exits 64 (invalid arg).</li><li><strong>Currency-residency mismatch rejected</strong> — <code>--billing-currency VND --residency us-1</code> exits 64 with explicit error.</li><li><strong>JWT carries residency claim</strong> — post-FR JWT has <code>residency</code> claim; pre-FR JWTs accepted for 24h transitional then 401.</li><li><strong>Logging carries <code>residency=&lt;rid&gt;</code> field</strong> — log scrape of tenant handler shows field on every line.</li><li><strong>AUTH issuer URLs per residency</strong> — <code>issuer_map</code> has 4 entries matching <code>https://auth.&lt;residency&gt;.cyberos.world</code>.</li><li><strong>No cross-residency NATS subscription</strong> — sg-1 NATS subscriber cannot subscribe to <code>tenant.&lt;slug&gt;.*</code> on eu-1 cluster (separate clusters per DEC-932).</li><li><strong><code>residency_health_log</code> append-only</strong> — UPDATE on the table raises permission-denied (REVOKE enforced).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>residency_enum_cardinality_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn residency_enum_has_exactly_4_values() {\n    let ctx = TestContext::new().await;\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::residency))::text\"\n    ).fetch_all(&amp;ctx.pool).await.unwrap();\n    let mut labels = labels;\n    labels.sort();\n    assert_eq!(labels, vec![\"eu-1\",\"sg-1\",\"us-1\",\"vn-1\"]);\n}</code></pre>\n<h3>5.2 <code>residency_trip_wire_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cross_residency_insert_blocked() {\n    let ctx = TestContext::with_residency(Residency::Eu1).await;\n    let sg_tenant = ctx.provision_tenant_in(Residency::Sg1, \"sg-tenant\").await;\n\n    sqlx::query(\"SET LOCAL auth.residency = 'eu-1'\").execute(&amp;ctx.pool).await.unwrap();\n    let err = sqlx::query(\"INSERT INTO projects (id, tenant_id, name) VALUES ($1, $2, 'x')\")\n        .bind(uuid::Uuid::new_v4()).bind(sg_tenant).execute(&amp;ctx.pool).await.unwrap_err();\n    assert!(err.to_string().contains(\"cross_residency_write_blocked\"));\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.cross_residency_write_blocked\"));\n}\n\n#[tokio::test]\nasync fn missing_session_residency_blocks_write() {\n    let ctx = TestContext::new().await;\n    let tenant = ctx.provision_tenant_in(Residency::Sg1, \"sg-tenant\").await;\n    // No SET LOCAL auth.residency\n    let err = sqlx::query(\"INSERT INTO projects (id, tenant_id, name) VALUES ($1, $2, 'x')\")\n        .bind(uuid::Uuid::new_v4()).bind(tenant).execute(&amp;ctx.pool).await.unwrap_err();\n    assert!(err.to_string().contains(\"auth.residency session var not set\"));\n}</code></pre>\n<h3>5.3 <code>residency_immutable_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn residency_cannot_change_post_provision() {\n    let ctx = TestContext::new().await;\n    let tenant = ctx.provision_tenant_in(Residency::Sg1, \"sg-tenant\").await;\n    let err = sqlx::query(\"UPDATE tenants SET residency='eu-1' WHERE id=$1\")\n        .bind(tenant).execute(&amp;ctx.pool).await.unwrap_err();\n    assert!(err.to_string().contains(\"residency_immutable\"));\n}</code></pre>\n<h3>5.4 <code>residency_pool_routing_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn pool_router_selects_correct_pool() {\n    let ctx = TestContext::with_all_residencies().await;\n    let sg_pool = ctx.pool_router.pool_for(Residency::Sg1).unwrap();\n    let eu_pool = ctx.pool_router.pool_for(Residency::Eu1).unwrap();\n    assert_ne!(sg_pool as *const _, eu_pool as *const _);  // different pools\n    let sg_db: String = sqlx::query_scalar(\"SELECT current_database()\").fetch_one(sg_pool).await.unwrap();\n    let eu_db: String = sqlx::query_scalar(\"SELECT current_database()\").fetch_one(eu_pool).await.unwrap();\n    assert_ne!(sg_db, eu_db);\n}</code></pre>\n<h3>5.5 <code>residency_jwt_cross_rejection_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn wrong_residency_jwt_rejected() {\n    let ctx = TestContext::with_residency(Residency::Eu1).await;\n    let us_tenant = ctx.provision_tenant_in(Residency::Us1, \"us-tenant\").await;\n    let us_jwt = ctx.mint_jwt_in_residency(us_tenant, Residency::Us1).await;\n\n    let r = ctx.get_in_residency(Residency::Eu1, \"/v1/projects\").bearer_auth(us_jwt).send().await.unwrap();\n    assert_eq!(r.status(), 401);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"wrong_residency_token\");\n}</code></pre>\n<h3>5.6 <code>residency_atomic_provisioning_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn provisioning_rollback_on_s3_failure() {\n    let ctx = TestContext::with_residency(Residency::Sg1).await;\n    ctx.simulate_s3_failure();\n    let result = ctx.provision_tenant_in(Residency::Sg1, \"atomic-test\").await;\n    assert!(result.is_err());\n\n    let count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM tenants WHERE slug='atomic-test'\")\n        .fetch_one(ctx.pool_router.pool_for(Residency::Sg1).unwrap()).await.unwrap();\n    assert_eq!(count, 0);\n}</code></pre>\n<h3>5.7 <code>residency_health_check_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn all_residencies_healthy() {\n    let ctx = TestContext::with_all_residencies().await;\n    let report = run_residency_status(&amp;ctx).await;\n    for residency in Residency::ALL {\n        let score = report.score_for(residency);\n        assert_eq!(score.aurora.status, \"healthy\");\n        assert!(score.aurora.latency_ms &lt; 100);\n        assert_eq!(score.s3.status, \"healthy\");\n        assert_eq!(score.nats.status, \"healthy\");\n        assert_eq!(score.kms.status, \"healthy\");\n        assert_eq!(score.auth_issuer.status, \"healthy\");\n    }\n}</code></pre>\n<h3>5.8 <code>residency_memory_chain_partitioning_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cross_residency_memory_append_blocked() {\n    let ctx = TestContext::with_all_residencies().await;\n    let eu_tenant = ctx.provision_tenant_in(Residency::Eu1, \"eu-tenant\").await;\n    let sg_handler_ctx = ctx.handler_ctx_in(Residency::Sg1);\n\n    let result = sg_handler_ctx.memory.append_row(\n        MemoryRow::new(\"test.event\", eu_tenant, json!({})).build()\n    ).await;\n    assert!(result.is_err());\n    assert!(result.unwrap_err().to_string().contains(\"cross_residency_memory_event_blocked\"));\n\n    let local_audit = sg_handler_ctx.memory.recent_rows().await;\n    assert!(local_audit.iter().any(|r| r.kind == \"ten.cross_residency_memory_event_blocked\"));\n}</code></pre>\n<h3>5.9 <code>residency_currency_mapping_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[test]\nfn currency_to_residency_mapping() {\n    assert_eq!(derive_residency(BillingCurrency::Vnd), Residency::Vn1);\n    assert_eq!(derive_residency(BillingCurrency::Sgd), Residency::Sg1);\n    assert_eq!(derive_residency(BillingCurrency::Eur), Residency::Eu1);\n    assert_eq!(derive_residency(BillingCurrency::Gbp), Residency::Eu1);\n    assert_eq!(derive_residency(BillingCurrency::Usd), Residency::Us1);\n}</code></pre>\n<h3>5.10 <code>residency_pool_misroute_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn pool_misroute_caught_by_trip_wire() {\n    let ctx = TestContext::with_all_residencies().await;\n    let eu_tenant = ctx.provision_tenant_in(Residency::Eu1, \"eu-tenant\").await;\n    // Simulate a buggy handler using the sg-1 pool for an eu-1 tenant\n    let sg_pool = ctx.pool_router.pool_for(Residency::Sg1).unwrap();\n    sqlx::query(\"SET LOCAL auth.residency = 'sg-1'\").execute(sg_pool).await.unwrap();\n    let err = sqlx::query(\"INSERT INTO projects (id, tenant_id, name) VALUES ($1, $2, 'x')\")\n        .bind(uuid::Uuid::new_v4()).bind(eu_tenant).execute(sg_pool).await.unwrap_err();\n    assert!(err.to_string().contains(\"cross_residency_write_blocked\"));\n\n    let audit = ctx.memory_rows_in(Residency::Sg1).await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.residency_pool_misroute\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract in §3 is the skeleton. Additional notes below.)</p>\n<h3>6.1 Handler-entry residency context setting</h3>\n<pre><code class=\"language-rust\">// At every tenant-scoped handler entry:\npub async fn require_residency(ctx: &amp;AppCtx, jwt: &amp;Jwt) -&gt; Result&lt;HandlerCtx, AuthError&gt; {\n    let residency = jwt.claims.residency.parse::&lt;Residency&gt;()?;\n    let expected_issuer = ctx.residency_issuer_map.issuer_for(residency);\n    if jwt.claims.iss != expected_issuer {\n        emit_audit(&amp;ctx, \"ten.cross_residency_access_attempt\", json!({\n            \"jwt_iss\": jwt.claims.iss, \"expected_iss\": expected_issuer,\n        })).await;\n        return Err(AuthError::WrongResidencyToken);\n    }\n    let pool = ctx.pool_router.pool_for(residency)?;\n    let mut conn = pool.acquire().await?;\n    sqlx::query(\"SET LOCAL auth.residency = $1\").bind(residency.as_str()).execute(&amp;mut *conn).await?;\n    sqlx::query(\"SET LOCAL auth.tenant_id = $1\").bind(jwt.claims.tenant_id).execute(&amp;mut *conn).await?;\n    Ok(HandlerCtx { conn, residency, tenant_id: jwt.claims.tenant_id })\n}</code></pre>\n<h3>6.2 Residency health check</h3>\n<pre><code class=\"language-rust\">pub async fn check_residency(ctx: &amp;AppCtx, residency: Residency) -&gt; ResidencyHealthReport {\n    let mut report = ResidencyHealthReport::new(residency);\n    report.aurora = check_aurora(ctx.pool_router.pool_for(residency)?).await;\n    report.s3 = check_s3(ctx.s3_router.client_for(residency)?, &amp;format!(\"cyberos-{}-tenants\", residency.as_str())).await;\n    report.nats = check_nats(ctx.nats_router.client_for(residency)?).await;\n    report.stripe = check_stripe(ctx.stripe_router.client_for(residency)?).await;\n    report.kms = check_kms(ctx.kms_router.client_for(residency)?).await;\n    report.auth_issuer = check_auth_issuer(ctx.residency_issuer_map.issuer_for(residency)).await;\n    persist_health_log(ctx, &amp;report).await;\n    report\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>FR-AI-016</strong> Residency pinning — establishes residency concept at AI layer; TEN-103 extends to data layer.</li><li><strong>FR-TEN-001</strong> Provisioning CLI — TEN-103 modifies the orchestrator to consume residency router.</li></ul>\n<p><strong>Cross-module (related_frs):</strong></p>\n<ul><li><strong>FR-TEN-003</strong> Stripe billing — per-residency Stripe account routing (DEC-801 consumed here).</li><li><strong>FR-TEN-004</strong> 4-axis metering — emits per-residency metering events; trip-wire applies.</li><li><strong>FR-TEN-101</strong> Self-serve signup — residency derivation at signup hand-off.</li><li><strong>FR-TEN-102</strong> VND domestic rail — vn-1 residency consumes this rail.</li><li><strong>FR-TEN-104</strong> Lifecycle — tenant termination per-residency.</li><li><strong>FR-AUTH-004</strong> JWT mint — <code>residency</code> claim added.</li><li><strong>FR-AI-003</strong> memory audit-row bridge — 8 new kinds register; chain partitioned per residency.</li><li><strong>FR-MEMORY-111</strong> PII scrubbing — tenant_id hash16 in cross-residency events.</li><li><strong>FR-DOC-001</strong> Documents — S3 bucket per-residency consumed.</li><li><strong>FR-EMAIL-001</strong> Email — SES region per-residency.</li><li><strong>FR-OBS-005</strong> Trace correlation — residency tag on every span.</li><li><strong>FR-OBS-007</strong> Auto-runbook — sev-1 alerts route per-residency.</li><li><strong>FR-OBS-008</strong> Compliance views — per-residency scoping consumed.</li></ul>\n<p><strong>Downstream (blocks):</strong> None (this FR is bottom-of-stack for residency infrastructure; other FRs consume but don't block on TEN-103 at slice 2).</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>ten.cross_residency_write_blocked</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.cross_residency_write_blocked\",\n  \"severity\": 1,\n  \"tenant_id\": \"00000000-0000-0000-0000-000000000001\",\n  \"actor_id\": \"system.ten.residency\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"expected_residency\": \"sg-1\",\n    \"blocked_tenant_residency\": \"eu-1\",\n    \"blocked_tenant_id_hash16\": \"f8a1b2c3d4e5f607\",\n    \"table_name\": \"projects\",\n    \"operation\": \"INSERT\",\n    \"service\": \"cyberos-projects-service\",\n    \"stack_trace_sha256\": \"9c4e7a8b...\"\n  }\n}</code></pre>\n<h3>8.2 <code>cyberos-ten residency-status --json</code> output</h3>\n<pre><code class=\"language-json\">{\n  \"checked_at\": \"2026-05-17T09:14:32.847Z\",\n  \"overall_score\": \"22/24\",\n  \"residencies\": {\n    \"sg-1\": {\n      \"score\": \"6/6\",\n      \"components\": {\n        \"aurora\":      {\"status\": \"healthy\",  \"latency_ms\": 12},\n        \"s3\":          {\"status\": \"healthy\",  \"latency_ms\": 84},\n        \"nats\":        {\"status\": \"healthy\",  \"latency_ms\": 7},\n        \"stripe\":      {\"status\": \"healthy\",  \"latency_ms\": 412},\n        \"kms\":         {\"status\": \"healthy\",  \"latency_ms\": 18},\n        \"auth_issuer\": {\"status\": \"healthy\",  \"latency_ms\": 89}\n      }\n    },\n    \"eu-1\": {\"score\": \"5/6\", \"components\": { \"nats\": {\"status\": \"degraded\", \"latency_ms\": 380}, \"...\": \"...\" }},\n    \"us-1\": {\"score\": \"6/6\", \"components\": \"...\"},\n    \"vn-1\": {\"score\": \"5/6\", \"components\": \"...\", \"note\": \"vn-1 physically hosted in ap-southeast-1 per DEC-930\"}\n  }\n}</code></pre>\n<h3>8.3 <code>ten.residency_provisioned</code> memory row (one per residency standup)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.residency_provisioned\",\n  \"severity\": 1,\n  \"tenant_id\": \"00000000-0000-0000-0000-000000000001\",\n  \"actor_id\": \"system.ten.residency\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"residency\": \"sg-1\",\n    \"aws_region\": \"ap-southeast-1\",\n    \"aurora_cluster_arn\": \"arn:aws:rds:ap-southeast-1:...\",\n    \"s3_bucket\": \"cyberos-sg-1-tenants\",\n    \"s3_audit_bucket\": \"cyberos-sg-1-audit\",\n    \"nats_endpoint\": \"nats.sg-1.cyberos.world:4222\",\n    \"kms_key_arn\": \"arn:aws:kms:ap-southeast-1:...\",\n    \"auth_issuer_url\": \"https://auth.sg-1.cyberos.world\",\n    \"terraform_apply_id\": \"tf_abc123...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 2. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Cross-region DR (failover from sg-1 → us-1 etc.) — slice 3, FR-TEN-2xx (placeholder).</li><li><strong>Deferred:</strong> vn-1 physical migration to AWS Vietnam region when it opens — slice 3, ops project (no code change required; Terraform plan-only).</li><li><strong>Deferred:</strong> Per-residency self-serve via signup form — FR-TEN-101 derives residency from billing_currency; explicit residency picker in UI is slice 3.</li><li><strong>Deferred:</strong> Cross-residency tenant migration (rare admin op) — slice 3, FR-TEN-2xx.</li><li><strong>Deferred:</strong> Residency-aware backup/restore CLI (<code>cyberos-ten residency-backup --to s3://...</code>) — slice 3.</li><li><strong>Deferred:</strong> <code>cyberos-ten residency-cleanup-orphan</code> CLI — slice 3 (slice 2 = manual ops cleanup on sev-1).</li><li><strong>Deferred:</strong> Per-residency rate limiting policy (different per residency) — slice 3.</li><li><strong>Deferred:</strong> Multi-region OBS query proxy (FR-OBS-002 enhancement for residency federation) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Cross-residency write attempt</td><td>trip-wire trigger raises</td><td>INSERT/UPDATE blocked + sev-1 <code>ten.cross_residency_write_blocked</code></td><td>Operator investigates handler bug + fixes pool routing</td></tr><tr><td>Cross-residency read attempt</td><td>RLS USING predicate <code>residency=current_setting('auth.residency')</code> rejects</td><td>0 rows returned (silent — RLS conformant); audited at handler if it expected ≥1</td><td>Same: handler bug investigation</td></tr><tr><td>Pool misroute (handler uses wrong pool)</td><td>trip-wire catches OR rows-returned=0</td><td>sev-1 <code>ten.residency_pool_misroute</code> + 500 to caller</td><td>Operator audits handler; fixes pool router consumption</td></tr><tr><td>JWT residency mismatch</td><td>issuer check at handler entry</td><td>401 + <code>wrong_residency_token</code> + <code>ten.cross_residency_access_attempt</code> sev-1</td><td>User signs in via correct-residency portal</td></tr><tr><td><code>auth.residency</code> session var missing</td><td>trip-wire raises</td><td>INSERT blocked + sev-1 alert; handler bug</td><td>Handler audited; missing <code>SET LOCAL</code> call added</td></tr><tr><td>Residency mutation attempt</td><td><code>trg_residency_immutable</code> raises</td><td>UPDATE blocked + audit</td><td>Manual data migration via cross-region tooling (slice 3)</td></tr><tr><td>Atomic provisioning rolls back</td><td>orchestrator step failure</td><td>Aurora row rolled back; S3/NATS partial cleanup needed via manual ops</td><td><code>cyberos-ten residency-cleanup-orphan &lt;slug&gt;</code> (slice 3)</td></tr><tr><td>KMS unavailable in one residency</td><td>health check 200ms timeout</td><td>sev-1 <code>ten.residency_kms_unavailable</code> + degraded encryption</td><td>AWS KMS incident; encrypt-fail-closed (no plaintext write) until recovery</td></tr><tr><td>NATS cluster down in one residency</td><td>health check fails</td><td>sev-1 + that residency's tenant events queue locally (FR-TEN-004 WAL queue)</td><td>NATS-side recovery; queue drains</td></tr><tr><td>Aurora primary failover within residency</td><td>RDS Multi-AZ event</td><td>&lt; 30s outage; handlers retry; sev-2 alert</td><td>Inherent — Multi-AZ DR primitive</td></tr><tr><td>Stripe US account credentials misconfig</td><td>Stripe API 401</td><td>sev-1 + stripe-rail temporarily fails for that residency</td><td>Operator rotates per DEC-801 / FR-TEN-003 ops</td></tr><tr><td>Per-residency Terraform drift</td><td>nightly tf plan drift detector</td><td>sev-2 alert with diff</td><td>Operator reviews + applies tf changes (with change-management)</td></tr><tr><td>Cross-residency memory append attempted</td><td>chain.append guard</td><td>sev-1 <code>ten.cross_residency_memory_event_blocked</code> + local-chain row</td><td>Inherent — chain-pollution prevented</td></tr><tr><td>Residency-aware logging missing field</td><td>OBS log-quality sweep</td><td>sev-3 informational</td><td>CI test enforces; PR rejected if missing</td></tr><tr><td>New tenant table added without trip-wire trigger</td><td>migration audit (CI)</td><td>CI fails the PR</td><td>PR adds trigger before merge</td></tr><tr><td>UUIDv7 high-nibble collision (astronomically improbable)</td><td>partial-unique constraint on <code>(tenant_id)</code> global</td><td>INSERT fails</td><td>Re-generate ID (1-in-2^60 chance)</td></tr><tr><td>Per-residency JWKS rotation mid-flight</td><td>FR-AUTH-004 JWKS cache stale</td><td>Token validation fails briefly; cache refresh triggered</td><td>Inherent</td></tr><tr><td>Health check timeout false positive</td><td>network blip</td><td>One degraded-status row in <code>residency_health_log</code>; next 5-min poll recovers</td><td>Inherent — degraded ≠ down</td></tr><tr><td><code>current_setting('auth.residency')</code> typo in handler</td><td>dev mistake</td><td>trip-wire raises; sev-1; handler ships broken</td><td>CI integration test catches</td></tr><tr><td>Migration <code>0016</code> cursor loop misses a table</td><td>check at CI</td><td>CI test asserts every <code>tenant_id</code>-bearing table has the trigger</td><td>CI catches; manual trigger add</td></tr><tr><td>Cross-residency NATS subject subscription attempt</td><td>NATS clusters are separate; connect to wrong cluster URL → connection refused</td><td>Connection-level failure; not a runtime risk</td><td>Inherent isolation</td></tr><tr><td>Per-residency KMS key revocation</td><td>AWS KMS audit</td><td>Encrypt operations fail in that residency; sev-1 alert</td><td>Operator restores key OR rotates to new key with re-encrypt sweep</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> The 28-tenant-scoped-tables count (§1 #8) is a deploy-time inventory; CI test re-counts via <code>information_schema.columns WHERE column_name='tenant_id' AND table_schema='public'</code> and asserts each has the trip-wire trigger.</p>\n<p><strong>§11.2</strong> The trip-wire trigger does ONE additional SELECT per write (<code>SELECT residency FROM tenants WHERE id=NEW.tenant_id</code>). At write-heavy workloads this is a ~5% overhead; acceptable cost for the defense.</p>\n<p><strong>§11.3</strong> The <code>residency_immutable</code> trigger uses <code>IS DISTINCT FROM</code> to handle NULL-vs-NULL correctly (residency is NOT NULL post-backfill, but the IS-DISTINCT-FROM is future-proof if NULL ever becomes valid).</p>\n<p><strong>§11.4</strong> UUIDv7 residency-nibble encoding: byte 6 high nibble = residency index (0/1/2/3). UUIDv7 already uses byte 6 for version field; we reserve the high nibble of byte 7 instead (compatible with RFC 4122 + RFC 9562 extension space). <code>services/ten/src/residency/uuid_gen.rs</code> implements + tests cross-residency collision-free.</p>\n<p><strong>§11.5</strong> AWS region mapping is <code>const fn</code> (compile-time); changing it requires recompile, which is desired (region mapping is forensic-critical).</p>\n<p><strong>§11.6</strong> Per-residency Terraform state is stored in per-residency S3 backend (each residency's tf state lives in that residency's S3) — no central tf state that becomes a cross-residency single point of failure.</p>\n<p><strong>§11.7</strong> The handler-entry <code>require_residency</code> function (§6.1) is mandatory at every tenant-scoped handler; CI rule via <code>clippy::custom_lint</code> enforces presence.</p>\n<p><strong>§11.8</strong> <code>pool_router</code> and friends are loaded at service startup from KMS-encrypted connection strings in AWS Secrets Manager. Boot fails fast if any residency's secrets unreachable; service does NOT serve partial (better: refuse to start than serve wrong residency).</p>\n<p><strong>§11.9</strong> The <code>cyberos-ten residency-status</code> CLI is operator-only; runs under <code>cyberos_ops</code> IAM role with read-only KMS/Stripe/AUTH access.</p>\n<p><strong>§11.10</strong> Health check latency thresholds are SLOs, not absolute caps; intermittent breach degrades the score but doesn't fire sev-1 (sustained breach via FR-OBS-007 alarm definition does).</p>\n<p><strong>§11.11</strong> The transitional 24h post-deploy window for residency-claim-missing JWTs uses a feature flag <code>auth.residency_claim_required=false</code> that flips to true via a scheduled job at deploy+24h.</p>\n<p><strong>§11.12</strong> Per-residency memory chain Merkle Mountain Range proof (FR-MEMORY-101 §6.4 PROPOSAL P2) is computed per-residency; cross-residency chain-of-custody verification is out-of-scope at slice 2.</p>\n<p><strong>§11.13</strong> The <code>Cross-Region-NATS-Sync</code> is intentionally absent — DEC-932 forbids; future cross-region event federation (slice 3) would require explicit consent mechanism per regulation.</p>\n<p><strong>§11.14</strong> Test fixtures for <code>with_all_residencies()</code> provision 4 in-memory Postgres instances + 4 LocalStack S3 buckets + 4 NATS clusters; integration tests against real AWS deploy nightly (not on every PR).</p>\n<p><strong>§11.15</strong> The <code>residency_health_log</code> table's <code>latency_ms INT</code> is nullable for <code>status='down'</code> (no latency observable when down); other statuses have non-null latency.</p>\n<p><strong>§11.16</strong> The 8 memory kinds (§1 #16) are all sev-1 or sev-2 because every kind by definition is unusual: routine residency operation does not emit (steady-state silent). FR-AI-003 closed-set extension adds 8.</p>\n<p><strong>§11.17</strong> Trip-wire trigger error messages include enough context to identify the buggy handler (table name, expected vs actual residency, tenant_id hash16). Operators can grep memory rows to find the offending service.</p>\n<p><strong>§11.18</strong> Per-residency Terraform modules share helper modules at <code>infra/terraform/residency/_shared/modules/</code> — DRY without sharing actual resources.</p>\n<p><strong>§11.19</strong> The vn-1 residency's PDPL-disclosure copy is at <code>services/ten/web/signup/consents/vi/pdpl-vn-residency-disclosure-v1.md</code> — FR-TEN-101 surfaces this at signup for VN-residency tenants.</p>\n<p><strong>§11.20</strong> Cross-region IAM role policies — each residency's services use IAM roles scoped to that residency's resources only; no cross-region role assumption permitted.</p>\n<p><strong>§11.21</strong> Per-residency Stripe account API keys (FR-TEN-003 DEC-801) are stored in that residency's AWS Secrets Manager only; no shared key store.</p>\n<p><strong>§11.22</strong> The <code>residency_currency_mapping_test</code> is a pure-Rust unit test (no DB); validates the const fn truth-table.</p>\n<hr>\n<p><em>End of FR-TEN-103 spec.</em></p>";
