window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SKILL-104"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The capability broker <strong>MUST</strong> be the sole tool dispatcher for skills. Skills communicate with the broker via JSON-RPC over a Unix socket; every tool call is authorised against the skill's frontmatter at request time. The contract:</p>\n<ol><li><strong>MUST</strong> listen on a per-skill Unix socket <code>/tmp/cyberos-skill-broker.&lt;skill_invocation_id&gt;.sock</code> created at invoke time and removed at termination. The socket is the ONLY IPC channel — the skill subprocess has no other access to broker state.</li><li><strong>MUST</strong> speak JSON-RPC 2.0 over length-prefixed framing (4-byte BE u32 length || JSON body). Methods: <code>tool.call</code>, <code>tool.list</code>, <code>broker.status</code>. Request size capped at 1 MB; response size capped at 16 MB.</li><li><strong>MUST</strong> enforce frontmatter at every <code>tool.call</code>:</li></ol>\n<ul><li><code>tool.method</code> MUST be in the skill's <code>allowed_tools</code> list AND in the global <code>MCP_TOOL_REGISTRY</code>.</li><li><code>tool.method</code> MUST NOT be in the skill's <code>disallowed_tools</code> list (denylist overrides).</li><li>For <code>MemoryRead</code> + <code>MemorySearch</code>: every requested <code>path</code> MUST match at least one glob in <code>allowed_memory_scopes</code>.</li><li>For <code>Read</code> + <code>Edit</code> + <code>Write</code>: paths MUST match <code>allowed_files</code> (<code>x-allowed-files: [...]</code> frontmatter extension; default deny).</li><li>For <code>HttpFetch</code> + <code>HttpPost</code>: hostnames MUST match <code>allowed_domains</code> (<code>x-allowed-domains: [...]</code> extension; default deny).</li><li>Violation → return JSON-RPC error <code>{\"code\": -32603, \"data\": {\"reason\": \"tool_not_allowed\" | \"scope_violation\" | \"domain_violation\" | \"file_violation\"}}</code>; broker emits <code>skill.tool_denied</code> audit row carrying the violation details.</li></ul>\n<ol><li><strong>MUST</strong> sandbox the skill subprocess:</li></ol>\n<ul><li><code>close_fds(3..MAX)</code> — close all inherited file descriptors except stdin/stdout/stderr.</li><li><code>env_clear()</code> then re-set only <code>CYBEROS_BROKER_SOCKET</code>, <code>CYBEROS_SKILL_ID</code>, <code>CYBEROS_INVOCATION_ID</code>, <code>CYBEROS_TENANT_ID</code>, <code>RUST_LOG=warn</code>.</li><li>Set <code>unshare(CLONE_NEWPID)</code> on Linux when broker has CAP_SYS_ADMIN; otherwise skip with WARN log.</li><li>Set <code>setrlimit(RLIMIT_AS, 512MB)</code>, <code>RLIMIT_CPU, 60s)</code> (override per frontmatter <code>effort_minutes * 60</code>), <code>RLIMIT_NPROC, 8)</code>.</li></ul>\n<ol><li><strong>MUST</strong> enforce <code>allowed_memory_scopes</code> path-glob check using <code>globset</code> (same crate FR-SKILL-103 uses for parse-time validation). Globs are evaluated against the post-canonicalisation memory path (per AGENTS.md §0.4). The check happens AT EVERY <code>MemoryRead</code>/<code>MemorySearch</code>/<code>MemoryEmit</code> call — even within the same invocation; this prevents glob bypass via mid-session reconfiguration.</li><li><strong>MUST</strong> enforce <code>effort_minutes</code> timeout:</li></ol>\n<ul><li>At T=0, start a tokio timer of <code>effort_minutes * 60 * 0.9</code> seconds.</li><li>At T=90%, send SIGTERM to subprocess; broker continues to accept the tool.call response on the socket for up to 10s.</li><li>At T=100%, send SIGKILL; broker closes socket; emits <code>skill.timeout</code> audit row.</li></ul>\n<p>6.5. <strong>MUST</strong> treat default <code>effort_minutes</code> as 30 minutes (1800s) when frontmatter doesn't specify.</p>\n<ol><li><strong>MUST</strong> emit memory audit rows for every tool dispatch:</li></ol>\n<ul><li><code>skill.tool_call_started</code> BEFORE dispatch with <code>{invocation_id, skill_id, tool_name, args_hash, trace_id}</code>.</li><li><code>skill.tool_call_completed</code> AFTER with <code>{invocation_id, tool_name, outcome (success|error|denied|timeout), duration_ms, result_hash, trace_id}</code>.</li><li><code>skill.tool_denied</code> on enforcement violation with <code>{invocation_id, skill_id, tool_name, reason, attempted_args_hash, trace_id}</code>.</li><li><code>skill.timeout</code> when SIGKILL fires with <code>{invocation_id, skill_id, elapsed_ms, last_tool_call, trace_id}</code>.</li></ul>\n<ol><li><strong>MUST</strong> propagate W3C TraceContext (per FR-AI-022) — every tool.call includes a <code>traceparent</code> field; the broker injects this into the underlying tool (e.g. <code>HttpFetch</code> adds it as a request header; <code>MemoryEmit</code> writes it into the row payload).</li><li><strong>MUST</strong> register the canonical tool set at broker startup. The registry is a static <code>MCP_TOOL_REGISTRY: BTreeMap&lt;ToolName, Box&lt;dyn Tool&gt;&gt;</code>. Native tools: <code>Bash</code>, <code>Read</code>, <code>Write</code>, <code>Edit</code>, <code>Glob</code>, <code>Grep</code>, <code>MemoryRead</code>, <code>MemorySearch</code>, <code>MemoryEmit</code>, <code>HttpFetch</code>, <code>HttpPost</code>. MCP-server-provided tools are registered dynamically when the broker connects to an MCP server (slice-3+).</li><li><strong>MUST</strong> emit OTel span <code>skill.broker.tool_call</code> per call with attributes <code>skill_id</code>, <code>tool_name</code>, <code>outcome</code>, <code>duration_ms</code>, <code>args_size_bytes</code>, <code>result_size_bytes</code>. Per-tool spans cascade — e.g. <code>Bash</code> further emits <code>skill.tool.bash.exec</code>.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>skill_broker_tool_calls_total{tool_name, outcome}</code> (counter; outcome ∈ success | error | denied | timeout).</li><li><code>skill_broker_tool_duration_seconds{tool_name}</code> (histogram).</li><li><code>skill_broker_active_invocations</code> (gauge).</li><li><code>skill_broker_socket_bytes_total{direction}</code> (counter; direction ∈ tx | rx).</li></ul>\n<ol><li><strong>MUST</strong> expose <code>cyberos skill broker status</code> showing active invocations, total tool calls today, denied calls today (alerting signal).</li><li><strong>MUST</strong> support <code>cyberos skill broker tail --invocation &lt;id&gt;</code> to live-stream tool calls for one invocation (operator debugging).</li><li><strong>SHOULD</strong> support <code>cyberos skill broker replay --invocation &lt;id&gt;</code> reconstructing a past invocation's tool-call sequence from memory audit rows (slice-3+; placeholder).</li><li><strong>SHOULD</strong> support per-skill rate limits (max tool calls per minute) — slice-3+; default unbounded.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a broker process at all (§1 #1)?</strong> Without a broker, every skill would need its own tool implementation. The broker centralises: tool implementations (one impl of <code>Bash</code>, not N), enforcement (one place to check <code>allowed_tools</code>), audit (one place to emit memory rows), observability (one place to attach OTel spans). Skills become thin wrappers around <code>tool.call</code> JSON-RPC.</p>\n<p><strong>Why Unix sockets (§1 #1)?</strong> Per-invocation socket means: (a) cleanup is trivial (delete file on exit), (b) skill subprocess can't talk to other invocations' brokers, (c) no network surface. The socket path embeds <code>invocation_id</code> so debugging is easy (<code>lsof | grep &lt;id&gt;</code>).</p>\n<p><strong>Why JSON-RPC 2.0 (§1 #2)?</strong> Standard protocol; widely supported by client libraries; clear error semantics (<code>{\"error\": {\"code\": N, \"data\": {...}}}</code>). Length-prefixed framing avoids JSON-parsing-state machines.</p>\n<p><strong>Why enforcement at EVERY tool call (§1 #3 + #5)?</strong> A skill granted broad scopes early in execution could narrow itself later (e.g. reading frontmatter from a different SKILL.md it just downloaded). Per-call checks prevent any drift: every call validated against the originally-loaded frontmatter, no mid-flight changes.</p>\n<p><strong>Why subprocess sandbox details (§1 #4)?</strong> Three threat models:</p>\n<ul><li><strong>FD leak</strong>: broker may have an open writer-process FD (per FR-MEMORY-101); skill could write to memory bypassing the broker. <code>close_fds</code> prevents.</li><li><strong>Env leak</strong>: broker's env contains <code>CYBEROS_JWT_SECRET</code> for signing tokens; skill could read &amp; exfiltrate. <code>env_clear</code> prevents.</li><li><strong>PID namespace</strong> isolates the skill from seeing other processes on the host (defense-in-depth; not a strong boundary alone).</li><li><strong>rlimit</strong> prevents fork bombs (<code>NPROC</code>) and memory exhaustion (<code>AS</code>).</li></ul>\n<p><strong>Why path canonicalisation before glob check (§1 #5)?</strong> A skill could try <code>MemoryRead(\"memories/projects/cyberos/../people/founders/compensation\")</code> — without canonicalisation, the glob <code>memories/projects/cyberos/**</code> might match (depending on glob library); with canonicalisation, the path normalises to <code>memories/people/founders/compensation</code> and the glob fails.</p>\n<p><strong>Why 90% / 100% timeout split (§1 #6)?</strong> SIGTERM lets the skill clean up (close files, flush buffers, exit gracefully). 10% grace period (e.g. 3 minutes on a 30-min budget) is enough for most graceful-shutdown patterns. SIGKILL after 100% guarantees the broker reclaims resources.</p>\n<p><strong>Why audit BOTH started AND completed (§1 #7)?</strong> \"Started without completed\" = crash; investigators see exactly where the failure occurred. \"Completed without started\" = impossible (broker writes started before dispatch); used as an integrity check.</p>\n<p><strong>Why args_hash, not args (§1 #7)?</strong> Same reason FR-MEMORY-109 hashes Claude Code tool args — secrets in args (Bash commands like <code>curl -H \"Bearer ...\"</code>). Hash for dedup + audit; never store raw.</p>\n<p><strong>Why default-deny everywhere (§1 #3)?</strong> Default-allow is unsafe by construction. A skill that declares no <code>allowed_memory_scopes</code> should have no memory access (not \"everything\"); a skill that declares no <code>x-allowed-files</code> should not be able to read arbitrary files. Frontmatter is opt-in for capabilities.</p>\n<p><strong>Why x-prefixed extensions for files + domains (§1 #3)?</strong> The v1 schema (FR-SKILL-103) is frozen at 5 required + 8 optional fields. Adding <code>allowed_files</code> + <code>allowed_domains</code> as required would break existing skills; making them v2 fields requires a major bump. The <code>x-</code> extension namespace (per FR-SKILL-103 §1 #5) is the right escape hatch — these are de facto required for skills that read files or call HTTP, but v1 schema doesn't enforce them.</p>\n<p><strong>Why per-tool span cascade (§1 #10)?</strong> Aggregated dashboards: \"how often does the <code>Bash</code> tool fail vs succeed?\", \"what's the p99 latency of MemorySearch in this skill?\" Per-call spans roll up into per-tool histograms.</p>\n<p><strong>Why <code>cyberos skill broker tail</code> (§1 #13)?</strong> Operators debugging a misbehaving skill need real-time tool-call visibility. Reading audit rows post-hoc has latency. Tail is the equivalent of <code>tail -f</code> for skill broker IPC.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>JSON-RPC over Unix socket</h3>\n<pre><code class=\"language-jsonc\">// Request: tool.call\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\":      \"call-123\",\n  \"method\":  \"tool.call\",\n  \"params\": {\n    \"tool\":  \"MemoryRead\",\n    \"args\":  {\"path\": \"memories/projects/cyberos/notes/auth-design.md\"},\n    \"traceparent\": \"00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01\"\n  }\n}\n\n// Response: success\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\":      \"call-123\",\n  \"result\":  {\"body\": \"...\", \"content_hash\": \"9b0e8c5...\"}\n}\n\n// Response: denied (enforcement violation)\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\":      \"call-123\",\n  \"error\": {\n    \"code\":    -32603,\n    \"message\": \"tool_not_allowed\",\n    \"data\":    {\"reason\": \"tool_not_in_allowed_tools\", \"tool\": \"Bash\", \"skill_id\": \"memory-capture\"}\n  }\n}</code></pre>\n<h3>Enforce module</h3>\n<pre><code class=\"language-rust\">// services/skill-broker/src/enforce.rs\nuse crate::frontmatter::SkillFrontmatter;\nuse globset::GlobSet;\n\n#[derive(thiserror::Error, Debug)]\npub enum EnforceError {\n    #[error(\"tool {tool:?} is not in skill's allowed_tools\")]\n    ToolNotAllowed { tool: String, allowed: Vec&lt;String&gt; },\n    #[error(\"tool {tool:?} is in skill's disallowed_tools\")]\n    ToolDenied   { tool: String },\n    #[error(\"memory scope violation: path {path:?} not in allowed_memory_scopes\")]\n    ScopeViolation { path: String },\n    #[error(\"file violation: path {path:?} not in x-allowed-files\")]\n    FileViolation { path: String },\n    #[error(\"domain violation: host {host:?} not in x-allowed-domains\")]\n    DomainViolation { host: String },\n    #[error(\"request body too large: {n} &gt; {cap}\")]\n    RequestTooLarge { n: usize, cap: usize },\n}\n\npub struct Enforcer {\n    allowed_tools:      std::collections::BTreeSet&lt;String&gt;,\n    disallowed_tools:   std::collections::BTreeSet&lt;String&gt;,\n    memory_scope_globs:  GlobSet,\n    file_globs:         GlobSet,\n    domain_globs:       GlobSet,\n}\n\nimpl Enforcer {\n    pub fn from_frontmatter(fm: &amp;SkillFrontmatter) -&gt; anyhow::Result&lt;Self&gt; {\n        let allowed_tools: std::collections::BTreeSet&lt;String&gt; =\n            fm.allowed_tools.iter().map(|t| format!(\"{t:?}\")).collect();\n        let disallowed_tools: std::collections::BTreeSet&lt;String&gt; =\n            fm.disallowed_tools.iter().map(|t| format!(\"{t:?}\")).collect();\n\n        let mut memory_b = globset::GlobSetBuilder::new();\n        for p in &amp;fm.allowed_memory_scopes { memory_b.add(globset::Glob::new(p)?); }\n        let memory_scope_globs = memory_b.build()?;\n\n        let mut file_b = globset::GlobSetBuilder::new();\n        if let Some(serde_yaml::Value::Sequence(seq)) = fm.x_extensions.get(\"x-allowed-files\") {\n            for v in seq { if let Some(s) = v.as_str() { file_b.add(globset::Glob::new(s)?); } }\n        }\n        let file_globs = file_b.build()?;\n\n        let mut domain_b = globset::GlobSetBuilder::new();\n        if let Some(serde_yaml::Value::Sequence(seq)) = fm.x_extensions.get(\"x-allowed-domains\") {\n            for v in seq { if let Some(s) = v.as_str() { domain_b.add(globset::Glob::new(s)?); } }\n        }\n        let domain_globs = domain_b.build()?;\n\n        Ok(Self { allowed_tools, disallowed_tools, memory_scope_globs, file_globs, domain_globs })\n    }\n\n    pub fn check_tool(&amp;self, tool: &amp;str) -&gt; Result&lt;(), EnforceError&gt; {\n        if self.disallowed_tools.contains(tool) { return Err(EnforceError::ToolDenied { tool: tool.into() }); }\n        if !self.allowed_tools.contains(tool)   { return Err(EnforceError::ToolNotAllowed {\n            tool: tool.into(),\n            allowed: self.allowed_tools.iter().cloned().collect(),\n        }); }\n        Ok(())\n    }\n\n    pub fn check_memory_path(&amp;self, path: &amp;str) -&gt; Result&lt;(), EnforceError&gt; {\n        // Path canonicalisation: handle `..` segments\n        let canonical = canonicalise_memory_path(path);\n        if !self.memory_scope_globs.is_match(&amp;canonical) {\n            return Err(EnforceError::ScopeViolation { path: canonical });\n        }\n        Ok(())\n    }\n\n    pub fn check_file_path(&amp;self, path: &amp;str) -&gt; Result&lt;(), EnforceError&gt; {\n        let canonical = std::fs::canonicalize(path).ok()\n            .and_then(|p| p.to_str().map(String::from))\n            .unwrap_or_else(|| path.into());\n        if !self.file_globs.is_match(&amp;canonical) {\n            return Err(EnforceError::FileViolation { path: canonical });\n        }\n        Ok(())\n    }\n\n    pub fn check_domain(&amp;self, url: &amp;str) -&gt; Result&lt;(), EnforceError&gt; {\n        let host = url::Url::parse(url).ok().and_then(|u| u.host_str().map(String::from)).unwrap_or_default();\n        if !self.domain_globs.is_match(&amp;host) {\n            return Err(EnforceError::DomainViolation { host });\n        }\n        Ok(())\n    }\n\n    pub fn check_request_size(&amp;self, body: &amp;[u8]) -&gt; Result&lt;(), EnforceError&gt; {\n        const CAP: usize = 1_048_576;  // 1 MB\n        if body.len() &gt; CAP { return Err(EnforceError::RequestTooLarge { n: body.len(), cap: CAP }); }\n        Ok(())\n    }\n}\n\nfn canonicalise_memory_path(path: &amp;str) -&gt; String {\n    let mut parts: Vec&lt;&amp;str&gt; = Vec::new();\n    for seg in path.split('/') {\n        match seg {\n            \"\" | \".\"  =&gt; continue,\n            \"..\"      =&gt; { parts.pop(); }\n            _         =&gt; parts.push(seg),\n        }\n    }\n    parts.join(\"/\")\n}</code></pre>\n<h3>Dispatcher</h3>\n<pre><code class=\"language-rust\">// services/skill-broker/src/dispatcher.rs\nuse crate::enforce::Enforcer;\nuse tokio::io::{AsyncReadExt, AsyncWriteExt};\nuse tokio::net::UnixListener;\nuse std::time::Duration;\nuse std::sync::Arc;\n\npub struct Dispatcher {\n    pub enforcer:     Arc&lt;Enforcer&gt;,\n    pub invocation_id: uuid::Uuid,\n    pub skill_id:     String,\n    pub trace_id:     String,\n    pub registry:     Arc&lt;crate::registry::ToolRegistry&gt;,\n    pub memory_writer: Arc&lt;cyberos_memory_writer::MemoryWriter&gt;,\n}\n\nimpl Dispatcher {\n    pub async fn run_one_invocation(&amp;self, socket_path: &amp;std::path::Path, deadline: tokio::time::Instant) {\n        let _ = std::fs::remove_file(socket_path);\n        let listener = UnixListener::bind(socket_path).expect(\"bind socket\");\n        let (mut stream, _) = listener.accept().await.expect(\"accept\");\n\n        loop {\n            tokio::select! {\n                _ = tokio::time::sleep_until(deadline) =&gt; { break; }\n                req = read_frame(&amp;mut stream) =&gt; {\n                    let req = match req { Ok(r) =&gt; r, Err(_) =&gt; break };\n                    if let Err(e) = self.enforcer.check_request_size(&amp;req) {\n                        let _ = write_frame(&amp;mut stream, error_response(e)).await;\n                        continue;\n                    }\n                    let response = self.dispatch_one(req).await;\n                    let _ = write_frame(&amp;mut stream, response).await;\n                }\n            }\n        }\n    }\n\n    async fn dispatch_one(&amp;self, req_bytes: Vec&lt;u8&gt;) -&gt; Vec&lt;u8&gt; {\n        let req: JsonRpcReq = match serde_json::from_slice(&amp;req_bytes) {\n            Ok(r) =&gt; r,\n            Err(_) =&gt; return error_response_str(\"invalid_json\"),\n        };\n        // Pre-dispatch enforcement\n        if let Err(e) = self.enforcer.check_tool(&amp;req.params.tool) {\n            self.emit_denied(&amp;req, &amp;e).await;\n            return error_response(e);\n        }\n        // Per-tool scope/file/domain check\n        if let Err(e) = self.check_args(&amp;req.params.tool, &amp;req.params.args) {\n            self.emit_denied(&amp;req, &amp;e).await;\n            return error_response(e);\n        }\n        // Audit: started\n        self.emit_started(&amp;req).await;\n        // Dispatch\n        let start = std::time::Instant::now();\n        let result = self.registry.get(&amp;req.params.tool)\n            .expect(\"tool exists; registry populated at startup\")\n            .invoke(&amp;req.params.args, &amp;self.trace_id).await;\n        // Audit: completed\n        let duration = start.elapsed();\n        self.emit_completed(&amp;req, &amp;result, duration).await;\n        serde_json::to_vec(&amp;JsonRpcResp { jsonrpc: \"2.0\".into(), id: req.id, result: Some(result.into()) , error: None }).unwrap()\n    }\n\n    fn check_args(&amp;self, tool: &amp;str, args: &amp;serde_json::Value) -&gt; Result&lt;(), crate::enforce::EnforceError&gt; {\n        match tool {\n            \"MemoryRead\" | \"MemorySearch\" | \"MemoryEmit\" =&gt; {\n                if let Some(p) = args.get(\"path\").and_then(|v| v.as_str()) {\n                    self.enforcer.check_memory_path(p)?;\n                }\n            }\n            \"Read\" | \"Write\" | \"Edit\" =&gt; {\n                if let Some(p) = args.get(\"path\").and_then(|v| v.as_str()) {\n                    self.enforcer.check_file_path(p)?;\n                }\n            }\n            \"HttpFetch\" | \"HttpPost\" =&gt; {\n                if let Some(u) = args.get(\"url\").and_then(|v| v.as_str()) {\n                    self.enforcer.check_domain(u)?;\n                }\n            }\n            _ =&gt; {}\n        }\n        Ok(())\n    }\n    // emit_* methods elided; they write canonical::skill_* audit rows via memory_writer\n}</code></pre>\n<h3>Subprocess spawn</h3>\n<pre><code class=\"language-rust\">// services/skill-broker/src/broker.rs (excerpt)\npub async fn spawn_skill_subprocess(\n    skill_id: &amp;str,\n    socket_path: &amp;std::path::Path,\n    tenant_id: uuid::Uuid,\n    effort_minutes: u32,\n) -&gt; anyhow::Result&lt;tokio::process::Child&gt; {\n    use std::os::unix::process::CommandExt;\n    use tokio::process::Command;\n\n    let mut cmd = Command::new(format!(\"/opt/cyberos/skills/{skill_id}/main\"));\n    cmd.env_clear();\n    cmd.env(\"CYBEROS_BROKER_SOCKET\",  socket_path);\n    cmd.env(\"CYBEROS_SKILL_ID\",       skill_id);\n    cmd.env(\"CYBEROS_INVOCATION_ID\",  uuid::Uuid::new_v4().to_string());\n    cmd.env(\"CYBEROS_TENANT_ID\",      tenant_id.to_string());\n    cmd.env(\"RUST_LOG\",               \"warn\");\n    // unsafe: pre_exec runs in the child after fork, before exec\n    unsafe { cmd.pre_exec(move || {\n        // close FDs 3..MAX (stdin=0, stdout=1, stderr=2 inherited)\n        for fd in 3..1024 { libc::close(fd); }\n        // rlimit\n        let cpu = libc::rlimit { rlim_cur: (effort_minutes as u64 * 60), rlim_max: (effort_minutes as u64 * 60) };\n        libc::setrlimit(libc::RLIMIT_CPU, &amp;cpu);\n        let mem = libc::rlimit { rlim_cur: 512 * 1024 * 1024, rlim_max: 512 * 1024 * 1024 };\n        libc::setrlimit(libc::RLIMIT_AS, &amp;mem);\n        let proc = libc::rlimit { rlim_cur: 8, rlim_max: 8 };\n        libc::setrlimit(libc::RLIMIT_NPROC, &amp;proc);\n        Ok(())\n    }); }\n    let child = cmd.spawn()?;\n    Ok(child)\n}</code></pre>\n<h3>Timeout enforcement</h3>\n<pre><code class=\"language-rust\">// Inside the invocation orchestrator\npub async fn enforce_timeout(child: &amp;mut tokio::process::Child, effort_minutes: u32) {\n    let total = std::time::Duration::from_secs((effort_minutes as u64) * 60);\n    let warn_at = total.mul_f32(0.9);\n    let sigterm_at = tokio::time::Instant::now() + warn_at;\n    let sigkill_at = tokio::time::Instant::now() + total;\n\n    tokio::select! {\n        _ = tokio::time::sleep_until(sigterm_at) =&gt; {\n            if let Some(pid) = child.id() {\n                tracing::warn!(pid, \"approaching deadline; sending SIGTERM\");\n                unsafe { libc::kill(pid as i32, libc::SIGTERM); }\n            }\n            tokio::time::sleep_until(sigkill_at).await;\n            if let Some(pid) = child.id() {\n                if child.try_wait().ok().flatten().is_none() {\n                    tracing::error!(pid, \"deadline exceeded; sending SIGKILL\");\n                    unsafe { libc::kill(pid as i32, libc::SIGKILL); }\n                }\n            }\n        }\n        result = child.wait() =&gt; {\n            // Normal exit\n            tracing::info!(?result, \"skill subprocess exited within deadline\");\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Tool call succeeds for allowed tool</strong> — skill with <code>allowed_tools: [MemoryRead]</code> calls <code>tool.call({tool: \"MemoryRead\", args: {path: \"memories/projects/cyberos/notes/x.md\"}})</code> → success response.</li><li><strong>Tool not in allowed_tools rejected</strong> — same skill calls <code>Bash</code> → JSON-RPC error <code>tool_not_in_allowed_tools</code>; <code>skill.tool_denied</code> audit row emitted.</li><li><strong>Tool in disallowed_tools rejected</strong> — skill with <code>disallowed_tools: [Bash]</code> calls Bash → rejected (denylist overrides allowlist).</li><li><strong>memory scope check works</strong> — skill with <code>allowed_memory_scopes: [\"memories/projects/cyberos/**\"]</code> reads <code>memories/projects/cyberos/x.md</code> → ok; reads <code>memories/people/y.md</code> → <code>scope_violation</code>.</li><li><strong>Path canonicalisation prevents glob bypass</strong> — skill tries <code>memories/projects/cyberos/../people/y.md</code> → canonicalises to <code>memories/people/y.md</code> → <code>scope_violation</code>.</li><li><strong>File path enforcement</strong> — skill with <code>x-allowed-files: [\"/tmp/**\"]</code> reads <code>/tmp/x.txt</code> → ok; reads <code>/etc/passwd</code> → <code>file_violation</code>.</li><li><strong>Domain enforcement</strong> — skill with <code>x-allowed-domains: [\"api.example.com\"]</code> fetches <code>https://api.example.com/x</code> → ok; fetches <code>https://evil.com/y</code> → <code>domain_violation</code>.</li><li><strong>Subprocess env cleared</strong> — skill prints <code>env | grep CYBEROS</code> → output contains only <code>BROKER_SOCKET</code>, <code>SKILL_ID</code>, <code>INVOCATION_ID</code>, <code>TENANT_ID</code>; no <code>JWT_SECRET</code>, <code>AWS_*</code>, etc.</li><li><strong>Subprocess FDs closed</strong> — skill checks <code>/proc/self/fd/</code> (Linux) → only fds 0/1/2 present; no leaked broker fds.</li><li><strong>rlimit enforced (memory)</strong> — skill mallocs 1 GB → SIGSEGV / process killed (RLIMIT_AS = 512 MB).</li><li><strong>rlimit enforced (fork)</strong> — skill spawns 16 children → fails after 8 (RLIMIT_NPROC).</li><li><strong>SIGTERM at 90%</strong> — skill with <code>effort_minutes: 1</code> (60s); broker sends SIGTERM at T=54s.</li><li><strong>SIGKILL at 100%</strong> — same skill ignores SIGTERM; broker SIGKILLs at T=60s; <code>skill.timeout</code> audit row emitted.</li><li><strong>Audit: started + completed pair</strong> — every successful tool call → exactly one <code>skill.tool_call_started</code> + one <code>skill.tool_call_completed</code> with matching invocation_id + trace_id.</li><li><strong>Audit: denied row</strong> — every enforcement violation → <code>skill.tool_denied</code> with <code>reason</code>, <code>attempted_args_hash</code>, <code>trace_id</code>.</li><li><strong>Request size cap</strong> — 2 MB request body → <code>request_too_large</code> error; broker continues to accept further requests on same socket.</li><li><strong>JSON-RPC error code -32603</strong> — every enforcement error uses code -32603 (Internal error) per JSON-RPC 2.0 conventions.</li><li><strong>W3C trace propagation: HttpFetch</strong> — tool.call carries <code>traceparent</code>; emitted HTTP request to backend has the same <code>traceparent</code> header.</li><li><strong>W3C trace propagation: MemoryEmit</strong> — emitted memory row's payload.trace_id matches.</li><li><strong>Broker status CLI</strong> — <code>cyberos skill broker status</code> → JSON with <code>active_invocations</code>, <code>tool_calls_today</code>, <code>denied_today</code>.</li><li><strong>Broker tail CLI</strong> — <code>cyberos skill broker tail --invocation &lt;id&gt;</code> → line per tool call as they occur.</li><li><strong>OTel span per tool call</strong> — exporter receives <code>skill.broker.tool_call</code> per call with <code>skill_id</code>, <code>tool_name</code>, <code>outcome</code>, <code>duration_ms</code>.</li><li><strong>Metric: per-outcome counter</strong> — fixture run → counters <code>tool_calls_total{outcome=\"success\"}</code>, <code>{outcome=\"denied\"}</code>, <code>{outcome=\"timeout\"}</code> correct.</li><li><strong>Two concurrent invocations don't share sockets</strong> — invocation A's socket at <code>/tmp/cyberos-skill-broker.&lt;A&gt;.sock</code>; invocation B at <code>&lt;B&gt;.sock</code>; A cannot connect to B's socket.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/skill-broker/tests/enforce_test.rs\n#[test]\nfn tool_not_allowed_rejected() {\n    let fm = SkillFrontmatter::test_with([\"Read\"]);\n    let enforcer = Enforcer::from_frontmatter(&amp;fm).unwrap();\n    assert!(matches!(enforcer.check_tool(\"Bash\"), Err(EnforceError::ToolNotAllowed { .. })));\n}\n\n#[test]\nfn memory_scope_glob_match() {\n    let mut fm = SkillFrontmatter::test_empty();\n    fm.allowed_memory_scopes = vec![\"memories/projects/cyberos/**\".into()];\n    let e = Enforcer::from_frontmatter(&amp;fm).unwrap();\n    assert!(e.check_memory_path(\"memories/projects/cyberos/notes/x.md\").is_ok());\n    assert!(matches!(e.check_memory_path(\"memories/people/y.md\"), Err(EnforceError::ScopeViolation { .. })));\n}\n\n#[test]\nfn path_traversal_caught() {\n    let mut fm = SkillFrontmatter::test_empty();\n    fm.allowed_memory_scopes = vec![\"memories/projects/cyberos/**\".into()];\n    let e = Enforcer::from_frontmatter(&amp;fm).unwrap();\n    let res = e.check_memory_path(\"memories/projects/cyberos/../people/y.md\");\n    assert!(matches!(res, Err(EnforceError::ScopeViolation { .. })));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/skill-broker/tests/broker_e2e_test.rs\n#[tokio::test]\nasync fn dispatch_memoryread_succeeds() {\n    let env = BrokerTestEnv::with_skill(\"test-read\", &amp;[\"MemoryRead\"], &amp;[\"memories/projects/cyberos/**\"]);\n    let client = env.client().await;\n    let resp = client.call(\"MemoryRead\", json!({\"path\": \"memories/projects/cyberos/x.md\"})).await.unwrap();\n    assert!(resp[\"body\"].is_string());\n}\n\n#[tokio::test]\nasync fn dispatch_bash_denied() {\n    let env = BrokerTestEnv::with_skill(\"test-no-bash\", &amp;[\"MemoryRead\"], &amp;[]);\n    let client = env.client().await;\n    let resp = client.call(\"Bash\", json!({\"cmd\": \"ls\"})).await;\n    assert!(resp.unwrap_err().to_string().contains(\"tool_not_in_allowed_tools\"));\n    let row = env.memory.latest(\"skill.tool_denied\").await;\n    assert_eq!(row[\"payload\"][\"reason\"], \"tool_not_in_allowed_tools\");\n}\n\n#[tokio::test]\nasync fn timeout_enforced() {\n    let env = BrokerTestEnv::with_skill(\"test-timeout\", &amp;[\"Bash\"], &amp;[]);\n    env.set_effort_minutes(1).await;  // 60s\n    // Skill that sleeps 120s — should be SIGKILLed at 60s\n    let start = std::time::Instant::now();\n    let exit = env.run_skill(\"sleep 120\").await;\n    let elapsed = start.elapsed();\n    assert!(elapsed &lt; std::time::Duration::from_secs(75));   // 60 + 10 grace + slack\n    assert!(elapsed &gt; std::time::Duration::from_secs(55));   // not premature\n    let timeout_row = env.memory.latest(\"skill.timeout\").await;\n    assert_eq!(timeout_row[\"payload\"][\"skill_id\"], \"test-timeout\");\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-SKILL-103 (upstream)</strong> — frontmatter parser; this FR consumes parsed <code>SkillFrontmatter</code>.</li><li><strong>FR-SKILL-101 (upstream)</strong> — memory integration (pre/post audit rows pattern).</li><li><strong>FR-SKILL-102 (related)</strong> — OCI registry distributes signed bundles; broker reads them.</li><li><strong>FR-SKILL-105 (downstream)</strong> — memory-capture@1 skill is the first canonical user.</li><li><strong>FR-MEMORY-101</strong> — MemoryWriter (used by broker to emit audit rows).</li><li><strong>FR-MEMORY-106</strong> — sync_class semantics.</li><li><strong>FR-AUTH-003</strong> — RLS on MemoryRead enforces tenant_id scope.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>skill.tool_call_started</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"skill.tool_call_started\",\n  \"payload\": {\n    \"invocation_id\": \"0e3b1a2c-4f5d-6789-abcd-ef0123456789\",\n    \"skill_id\":      \"memory-capture\",\n    \"tool_name\":     \"MemoryEmit\",\n    \"args_hash\":     \"9b0e8c5...\",\n    \"trace_id\":      \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<h3><code>skill.tool_denied</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"skill.tool_denied\",\n  \"payload\": {\n    \"invocation_id\":      \"0e3b1a2c-4f5d-6789-abcd-ef0123456789\",\n    \"skill_id\":           \"memory-capture\",\n    \"tool_name\":          \"Bash\",\n    \"reason\":             \"tool_not_in_allowed_tools\",\n    \"attempted_args_hash\": \"ab12cd...\",\n    \"trace_id\":           \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Cross-skill IPC (skill A calls skill B via broker) — slice 3+.</li><li>Per-skill rate limits — slice 3+.</li><li>Replay tool from memory audit rows — slice 3+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Skill subprocess crashes mid-dispatch</td><td>child.wait Err</td><td><code>skill.invoked_completed</code> with outcome=panic; broker socket cleanup</td><td>Operator inspects audit row</td></tr><tr><td>Skill never connects to socket</td><td>listener.accept timeout (60s)</td><td>broker times out; emits <code>skill.no_connect</code> row</td><td>Operator checks skill binary</td></tr><tr><td>Skill makes 1000 rapid tool calls</td><td>Per-call enforce check; high throughput</td><td>Each enforced; no special-case</td><td>Slice-3+: per-skill rate limit</td></tr><tr><td>Tool implementation throws</td><td>tool.invoke Err</td><td>error response; tool_call_completed with outcome=error</td><td>Operator investigates tool</td></tr><tr><td>MemoryWriter unavailable</td><td>emit Err</td><td>Audit row dropped (logged); tool call still completes</td><td>Operator restores writer</td></tr><tr><td>Invalid JSON-RPC on socket</td><td>parse Err</td><td>error response with code -32700</td><td>Skill should send valid JSON</td></tr><tr><td>Tool not in registry</td><td>registry.get None</td><td>error response with <code>unknown_tool</code></td><td>Author updates allowed_tools or skill code</td></tr><tr><td>Glob match on tampered path</td><td>canonicalisation catches</td><td>scope_violation</td><td>None — by design</td></tr><tr><td>OS lacks pid namespace (macOS)</td><td>unshare unavailable</td><td>WARN log; skip; rlimit still applied</td><td>Acceptable degradation</td></tr><tr><td>Socket path collision</td><td>listener bind Err</td><td>broker exits 1; logged</td><td>Operator runs cleanup script</td></tr><tr><td>Subprocess SIGKILL by OS (OOM, etc)</td><td>child wait</td><td><code>skill.timeout</code> not emitted; instead <code>skill.invoked_completed</code> outcome=killed</td><td>Operator investigates</td></tr><tr><td>Long-running tool (MemorySearch with 100k results)</td><td>timeout enforcement</td><td>SIGTERM at 90%; if tool yields response in grace, emit completed</td><td>Operator considers smaller queries</td></tr><tr><td>HTTP tool: TLS handshake fails</td><td>tool returns Err</td><td>tool_call_completed outcome=error; row carries error message</td><td>Operator checks network</td></tr><tr><td>Skill writes 16 MB+ response</td><td>response cap</td><td>broker truncates to 16 MB; logs WARN; metric increments</td><td>Operator considers chunked tools</td></tr><tr><td>Concurrent invocations of same skill</td><td>Different invocation_id + socket</td><td>Both run independently</td><td>By design</td></tr><tr><td>Frontmatter changed between invocations</td><td>Each invocation re-loads</td><td>Old invocation uses old frontmatter; new uses new</td><td>By design (no live-reload)</td></tr><tr><td>Broker process restart mid-invocation</td><td>child still running but socket gone</td><td>Skill connect fails; subprocess exits</td><td>Broker re-spawns on next invocation</td></tr><tr><td>Audit row write fails mid-dispatch</td><td>memory_writer Err</td><td>Row queued; emit in background; tool call still completes</td><td>Operator restores memory</td></tr><tr><td>Unknown tool name in MCP_TOOL_REGISTRY</td><td>startup validation</td><td>Broker refuses to start; sev-1 alarm</td><td>Operator fixes registry</td></tr><tr><td>Two skills want different versions of same tool</td><td>tool versioning v2+</td><td>v1: registry is single source; tools are global</td><td>v2+ design</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The Unix socket pattern means broker process must stay alive for the duration of invocation; it owns the socket file. On broker crash, the file is orphaned; FR-MEMORY-110's sweeper cleans <code>/tmp/cyberos-skill-broker.*.sock</code> files older than 5 minutes.</li><li><code>unsafe { cmd.pre_exec(...) }</code> runs in the child between fork and exec; the closure must be async-signal-safe. <code>libc::close</code> and <code>libc::setrlimit</code> are; <code>tracing::info!</code> is NOT (allocates).</li><li><code>RLIMIT_AS</code> (address space) is enforced by the kernel; <code>RLIMIT_DATA</code> is more granular but less reliably available across glibc versions.</li><li><code>unshare(CLONE_NEWPID)</code> requires CAP_SYS_ADMIN on Linux; in non-root cyberos installs, we skip with a WARN. Per-skill PID isolation is defense-in-depth, not the primary boundary.</li><li><code>globset</code> is used both at parse time (FR-SKILL-103) and at enforce time (this FR); single glob library = consistent semantics.</li><li>The dispatcher's <code>check_args</code> pattern-matches on tool name. For MCP tools (dynamic at startup), the registry entry carries an <code>arg_validator</code> fn that performs the equivalent check.</li><li>The trace_id is per-invocation, not per-tool-call. Tool calls within one invocation share trace_id; cascade spans connect them.</li><li><code>skill.tool_denied</code> rows include <code>attempted_args_hash</code> (NOT raw args) for the same reason FR-MEMORY-109 hashes Claude Code tool args: args may contain secrets.</li><li><code>cyberos skill broker tail</code> is implemented as a memory query subscription (the audit rows arrive in real-time); avoids needing a separate IPC channel.</li><li>Per-tool span cascade is OTel-native: child spans inherit parent's trace; each tool implementation calls <code>tracing::info_span!(\"skill.tool.bash.exec\", ...)</code> to create the child.</li></ul>\n<hr>\n<p><em>End of FR-SKILL-104.</em></p>";
