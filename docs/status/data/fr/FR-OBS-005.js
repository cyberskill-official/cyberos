window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-OBS-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>Every CyberOS service <strong>MUST</strong> propagate W3C TraceContext via HTTP headers AND embed <code>trace_id</code> + <code>span_id</code> into every structured log line + Prometheus histogram exemplar. Each piece:</p>\n<ol><li><strong>MUST</strong> parse incoming HTTP <code>traceparent</code> header per W3C spec at every service boundary; if absent or malformed, generate a new trace_id (NEVER reject the request — TraceContext is operational, not security). Edge services (CHAT, AUTH) typically generate; internal services (AI Gateway, memory) typically receive.</li><li><strong>MUST</strong> include <code>trace_id</code> + <code>span_id</code> + <code>tenant_id</code> fields in EVERY structured log line emitted by CyberOS services. Implementation: <code>tracing-subscriber</code> layer that pulls from current OTel context and adds the fields automatically. No manual <code>info!(trace_id = ..., ...)</code> boilerplate at call sites.</li><li><strong>MUST</strong> include <code>trace_id</code> as Prometheus exemplar on the <code>cyberos_duration_ms</code> histogram (FR-OBS-003). Exemplars let Grafana operators click a histogram bucket and jump directly to the offending trace in Tempo. The OTel SDK's <code>record</code> method supports exemplar injection natively.</li><li><strong>MUST</strong> forward <code>traceparent</code> (and <code>tracestate</code> + <code>baggage</code>) to downstream HTTP calls. The HTTP client wrapper auto-injects from the current span's context. Manual <code>req.headers_mut().insert(\"traceparent\", ...)</code> at call sites is forbidden — use the wrapper.</li><li><strong>MUST</strong> correlate LangSmith trace with operational trace via shared <code>trace_id</code> (FR-OBS-004 §1 #1 already requires this; this FR ensures the propagation chain ending at LangSmith preserves the value).</li><li><strong>MUST</strong> include <code>tenant_id</code> in every log line + every metric label (extracted from current request context). Without tenant_id, multi-tenant filtering at the OBS layer (FR-OBS-002) doesn't work.</li><li><strong>MUST</strong> be CI-gated by <code>obs-correlation-gate.yml</code>: the workflow runs <code>end_to_end_correlation_test.rs</code> which makes a synthetic AI call, waits for OTel batches to flush, then queries Loki + Tempo + LangSmith + Prometheus and asserts all 4 systems hold records for the same trace_id.</li><li><strong>MUST</strong> preserve trace_id through tokio tasks (e.g., async background work). The <code>tracing::Instrument</code> extension propagates the span across <code>tokio::spawn</code>; this FR requires its use throughout.</li><li><strong>MUST</strong> preserve trace_id through cross-process boundaries (subprocess spawns, e.g., memory_writer subprocess). The OTel context is serialised via env vars <code>OTEL_TRACE_ID</code> and <code>OTEL_SPAN_ID</code>; the subprocess restores at boot.</li><li><strong>MUST</strong> generate W3C-compliant trace_id (16 random bytes, hex-encoded as 32-char string) when no incoming header. The <code>opentelemetry::trace::TraceId::from_random()</code> produces this.</li><li><strong>MUST</strong> validate parsed <code>traceparent</code> strictly per W3C spec: <code>00-{32hex}-{16hex}-01</code> format. Malformed → generate new trace_id + log WARN with hash of bad value (NOT raw — bad values may be malicious).</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>obs_tracecontext_extracted_total{outcome}</code> (counter; outcome ∈ extracted | malformed | missing_generated_new).</li><li><code>obs_log_enrichment_total{service}</code> (counter; tracks how many logs were enriched).</li><li><code>obs_exemplar_emission_total</code> (counter; tracks histogram exemplar emissions).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why W3C TraceContext (DEC-160)?</strong> IETF-standardised. Broad support across SDKs (Rust, Python, Go, JS, Java). B3 (Zipkin) and Jaeger-native are widely used historically but their non-standard nature creates interop friction. Choosing W3C aligns with where the ecosystem is going.</p>\n<p><strong>Why generate new trace_id when traceparent absent (§1 #1)?</strong> TraceContext is an OPERATIONAL signal, not a SECURITY one. Rejecting requests without traceparent would break callers who don't emit it (early-stage clients, tools, ad-hoc curl). Generating new trace_id at the boundary preserves observability without sacrificing availability.</p>\n<p><strong>Why trace_id in EVERY log (§1 #2 + DEC-161)?</strong> The primary debug query is \"show me everything that happened during this call.\" Without trace_id in logs, that query becomes \"show me everything in time window X for tenant Y\" — noisy, slow, often wrong. With trace_id, it's <code>loki: {trace_id=\"abc\"}</code> — instant.</p>\n<p><strong>Why exemplars on histograms (§1 #3)?</strong> A Grafana latency spike (p99 jumped from 100ms to 2s) raises the question \"which call caused it?\" Without exemplars, ops manually narrows the time window then greps Tempo. With exemplars, click the bucket → jump to the trace. Reduces investigation time from minutes to seconds.</p>\n<p><strong>Why auto-inject traceparent at HTTP client (§1 #4)?</strong> Manual injection is error-prone — missing one downstream call breaks the chain. Auto-inject via wrapper means every outgoing HTTP call is instrumented; the chain is unbreakable.</p>\n<p><strong>Why CI gate on end-to-end correlation (§1 #7)?</strong> Correlation is the kind of property that breaks silently. A propagation gap doesn't produce errors; it produces \"the trace stops at service X.\" The CI test makes a synthetic call and verifies all 4 backends hold records for the same trace_id. Detection at PR time, not in production.</p>\n<p><strong>Why preserve trace_id through tokio::spawn (§1 #8)?</strong> Async background work (e.g., audit-row emission) is part of the same logical request. Without <code>Instrument</code>, the spawned task creates its own trace — the chain is broken; debugging \"did the audit row write?\" requires manual correlation.</p>\n<p><strong>Why preserve through cross-process boundaries (§1 #9)?</strong> memory_writer is a subprocess; without env-var propagation, the chain is broken at process boundaries. The serialisation via env vars is the standard pattern for OTel cross-process.</p>\n<p><strong>Why strict traceparent validation + WARN on malformed (§1 #11)?</strong> Malformed traceparent could be: (a) buggy client, (b) attempt to inject specific trace_id (security concern). Logging the bad value's hash (not raw) gives forensic capability without leaking attacker-controlled bytes.</p>\n<p><strong>Why generate, not preserve, on malformed (§1 #11)?</strong> Honoring an attacker-supplied trace_id would let them poison correlation (e.g., two unrelated requests with same trace_id appear linked in Tempo). Generating fresh ensures each request has an unbiased trace_id.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/src/tracecontext.rs\nuse axum::http::Request;\nuse opentelemetry::trace::TraceContextExt;\nuse tracing::Instrument;\n\n/// Wraps an axum handler: extracts trace_id from headers OR generates new; sets context for the duration of `f`.\npub async fn with_trace_context&lt;F, Fut, T&gt;(req: &amp;Request&lt;axum::body::Body&gt;, f: F) -&gt; T\nwhere\n    F: FnOnce() -&gt; Fut,\n    Fut: std::future::Future&lt;Output = T&gt;,\n{\n    let trace_id = match extract_traceparent(req.headers()) {\n        Ok(tc) =&gt; {\n            metrics::tracecontext_extracted(\"extracted\");\n            tc.trace_id\n        }\n        Err(ExtractError::Missing) =&gt; {\n            metrics::tracecontext_extracted(\"missing_generated_new\");\n            opentelemetry::trace::TraceId::from_random()\n        }\n        Err(ExtractError::Malformed(hash16)) =&gt; {\n            tracing::warn!(traceparent_hash16 = %hash16, \"malformed traceparent; generating new\");\n            metrics::tracecontext_extracted(\"malformed\");\n            opentelemetry::trace::TraceId::from_random()\n        }\n    };\n    let span = tracing::info_span!(\"request\", trace_id = %format!(\"{trace_id:032x}\"));\n    f().instrument(span).await\n}\n\npub fn extract_traceparent(headers: &amp;HeaderMap) -&gt; Result&lt;TraceContext, ExtractError&gt; {\n    let h = headers.get(\"traceparent\").ok_or(ExtractError::Missing)?;\n    let s = h.to_str().map_err(|_| ExtractError::Malformed(hash16(h.as_bytes())))?;\n    parse_w3c_traceparent(s).ok_or_else(|| ExtractError::Malformed(hash16(s.as_bytes())))\n}\n\npub fn inject_traceparent(headers: &amp;mut HeaderMap, ctx: &amp;opentelemetry::Context) {\n    let span_ctx = ctx.span().span_context();\n    let value = format!(\"00-{:032x}-{:016x}-01\", span_ctx.trace_id(), span_ctx.span_id());\n    headers.insert(\"traceparent\", value.parse().unwrap());\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum ExtractError {\n    #[error(\"traceparent header missing\")]\n    Missing,\n    #[error(\"traceparent header malformed (hash16: {0})\")]\n    Malformed(String),\n}</code></pre>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/src/logging.rs\nuse tracing_subscriber::Layer;\n\n/// tracing-subscriber layer that adds trace_id + span_id + tenant_id to every log record.\npub struct ObsContextLayer;\n\nimpl&lt;S: tracing::Subscriber&gt; Layer&lt;S&gt; for ObsContextLayer {\n    fn on_event(&amp;self, event: &amp;Event, ctx: Context&lt;S&gt;) {\n        let span_ctx = opentelemetry::Context::current().span().span_context();\n        let trace_id = format!(\"{:032x}\", span_ctx.trace_id());\n        let span_id  = format!(\"{:016x}\", span_ctx.span_id());\n        let tenant_id = current_tenant_id().unwrap_or_else(|| \"unknown\".into());\n\n        // Inject as event fields via a custom Visit that wraps the user's Visit\n        // (implementation specific to tracing-subscriber Layer trait)\n    }\n}\n\npub fn current_tenant_id() -&gt; Option&lt;String&gt; {\n    opentelemetry::Context::current()\n        .baggage()\n        .get(\"tenant_id\")\n        .map(|v| v.value().to_string())\n}</code></pre>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/src/exemplar.rs\nuse opentelemetry::metrics::Histogram;\nuse opentelemetry::KeyValue;\n\npub fn record_with_exemplar(histogram: &amp;Histogram&lt;f64&gt;, value: f64, labels: &amp;[KeyValue]) {\n    let trace_id = opentelemetry::Context::current().span().span_context().trace_id();\n    // OTel SDK 0.21 supports exemplar via `record` overload; embed trace_id as exemplar attribute\n    histogram.record(value, labels);\n    // Prometheus exemplar emission via the OTel-Prometheus exporter is automatic if trace_id is in current context\n}</code></pre>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/src/red.rs (modified per FR-OBS-003 §1 #3 hook)\npub fn record_request(...) {\n    // ... existing ...\n    exemplar::record_with_exemplar(&amp;DURATION.get().unwrap(), duration_ms as f64, &amp;labels);\n}</code></pre>\n<pre><code class=\"language-rust\">// HTTP client wrapper\npub struct InstrumentedClient(reqwest::Client);\n\nimpl InstrumentedClient {\n    pub async fn post&lt;B: Serialize&gt;(&amp;self, url: &amp;str, body: &amp;B) -&gt; Result&lt;reqwest::Response, reqwest::Error&gt; {\n        let mut req = self.0.post(url).json(body).build()?;\n        tracecontext::inject_traceparent(req.headers_mut(), &amp;opentelemetry::Context::current());\n        // also inject baggage for tenant_id propagation\n        let baggage = format!(\"tenant_id={}\", current_tenant_id().unwrap_or_default());\n        req.headers_mut().insert(\"baggage\", baggage.parse().unwrap());\n        self.0.execute(req).await\n    }\n}</code></pre>\n<p>CI workflow:</p>\n<pre><code class=\"language-yaml\"># .github/workflows/obs-correlation-gate.yml\nname: OBS End-to-End Correlation Gate\non:\n  pull_request:\n    paths:\n      - 'crates/cyberos-obs-sdk/**'\n      - 'services/**'\n      - 'deploy/obs/**'\n      - '.github/workflows/obs-correlation-gate.yml'\n\njobs:\n  correlation:\n    runs-on: ubuntu-22.04\n    timeout-minutes: 15\n    services:\n      otel-stack:\n        image: docker\n    steps:\n      - uses: actions/checkout@v4\n      - run: docker compose -f deploy/obs/docker-compose.yml up -d\n      - run: docker compose -f deploy/obs/langsmith-docker-compose.yml up -d\n      - run: sleep 30\n      - uses: actions-rust-lang/setup-rust-toolchain@v1\n      - run: cargo test --release end_to_end_correlation -- --test-threads=1</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Incoming traceparent preserved</strong> — Synthetic call with header <code>traceparent: 00-abc123...-def456...-01</code>; trace_id <code>abc123...</code> appears in all downstream services' logs + spans.</li><li><strong>Missing traceparent generates new trace_id</strong> — Synthetic call without header → new 32-char hex trace_id; metric <code>obs_tracecontext_extracted_total{outcome=missing_generated_new}</code> increments.</li><li><strong>Malformed traceparent generates new + WARN log</strong> — Header <code>traceparent: bad-value</code> → new trace_id; WARN log with hash16; metric <code>outcome=malformed</code>.</li><li><strong>Every log line in Loki carries trace_id</strong> — Synthetic call; query Loki for the trace_id; &gt;5 log lines returned across multiple services.</li><li><strong>Every log line carries tenant_id</strong> — Same query returns lines with <code>tenant_id=&lt;expected&gt;</code>.</li><li><strong>Prometheus histogram has exemplar</strong> — Query <code>cyberos_duration_ms_bucket{...}</code> with exemplars enabled; bucket has trace_id reference.</li><li><strong>Exemplar click in Grafana → Tempo trace</strong> — Manual UI test (or screenshot proof); the click navigates to the right Tempo trace.</li><li><strong>Outgoing HTTP carries traceparent</strong> — Test with mock downstream service; received request has <code>traceparent</code> header matching current trace_id.</li><li><strong>Outgoing HTTP carries baggage</strong> — Same test; <code>baggage: tenant_id=&lt;value&gt;</code> header present.</li><li><strong>LangSmith trace correlates</strong> — FR-OBS-004 export uses same trace_id; test queries LangSmith for trace_id and finds the AI call.</li><li><strong>End-to-end CI test passes</strong> — <code>end_to_end_correlation_test.rs</code>: synthetic call → assert log + span + langsmith + metric all share trace_id.</li><li><strong>tokio::spawn preserves trace_id</strong> — Background task spawned within request scope: log lines from the task carry the same trace_id.</li><li><strong>Subprocess preserves trace_id</strong> — memory_writer subprocess invocation sets <code>OTEL_TRACE_ID</code> env var; subprocess logs carry the same trace_id.</li><li><strong>W3C-compliant generated trace_id</strong> — <code>from_random()</code> produces 16 random bytes, 32 hex chars, all lowercase.</li><li><strong>Strict W3C parser rejects bad formats</strong> — <code>traceparent: 01-...-...-01</code> (wrong version) → malformed; <code>traceparent: 00-{31hex}-{16hex}-01</code> (wrong length) → malformed.</li><li><strong>Hash16 of bad traceparent NOT raw</strong> — WARN log contains 16-hex hash, not the raw value.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/tests/tracecontext_test.rs\n#[tokio::test]\nasync fn extracts_valid_traceparent() {\n    let mut headers = HeaderMap::new();\n    headers.insert(\"traceparent\", \"00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01\".parse().unwrap());\n    let ctx = tracecontext::extract_traceparent(&amp;headers).unwrap();\n    assert_eq!(format!(\"{:032x}\", ctx.trace_id), \"0af7651916cd43dd8448eb211c80319c\");\n}\n\n#[tokio::test]\nasync fn missing_traceparent_returns_missing() {\n    let headers = HeaderMap::new();\n    let err = tracecontext::extract_traceparent(&amp;headers).expect_err(\"expected Missing\");\n    assert!(matches!(err, tracecontext::ExtractError::Missing));\n}\n\n#[tokio::test]\nasync fn malformed_traceparent_returns_malformed_with_hash() {\n    let mut headers = HeaderMap::new();\n    headers.insert(\"traceparent\", \"totally-bogus\".parse().unwrap());\n    let err = tracecontext::extract_traceparent(&amp;headers).expect_err(\"expected Malformed\");\n    match err {\n        tracecontext::ExtractError::Malformed(hash) =&gt; assert_eq!(hash.len(), 16),\n        e =&gt; panic!(\"wrong: {e:?}\"),\n    }\n}\n\n#[tokio::test]\nasync fn injection_to_outgoing_request() {\n    let trace_id = TraceId::from_random();\n    let mut headers = HeaderMap::new();\n    let ctx = opentelemetry::Context::current_with_span_context(/* trace_id */);\n    tracecontext::inject_traceparent(&amp;mut headers, &amp;ctx);\n    let value = headers.get(\"traceparent\").unwrap().to_str().unwrap();\n    assert!(value.starts_with(\"00-\"));\n    assert!(value.contains(&amp;format!(\"{trace_id:032x}\")));\n}\n\n// crates/cyberos-obs-sdk/tests/log_enrichment_test.rs\n#[tokio::test]\nasync fn every_log_line_has_trace_id_and_tenant_id() {\n    let trace_id = TraceId::from_random();\n    let captured = test_helper::capture_logs(|| {\n        with_trace_context(&amp;test_request(trace_id, \"tenant_a\"), || async {\n            tracing::info!(\"test message\");\n            tracing::warn!(\"another\");\n        }).await;\n    }).await;\n    for line in captured {\n        assert!(line.contains(&amp;format!(\"trace_id={trace_id:032x}\")));\n        assert!(line.contains(\"tenant_id=tenant_a\"));\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/tests/end_to_end_correlation_test.rs\n#[tokio::test]\n#[ignore = \"requires full OTel stack; run with --ignored in obs-correlation-gate workflow\"]\nasync fn synthetic_call_correlates_across_loki_tempo_langsmith_prometheus() {\n    let trace_id = TraceId::from_random();\n    let trace_id_hex = format!(\"{trace_id:032x}\");\n\n    // Synthesise an AI call with the trace_id\n    let resp = test_helper::synthetic_ai_call(&amp;trace_id, &amp;test_tenant()).await;\n    assert!(resp.is_ok());\n\n    // Wait for OTel batches to flush\n    tokio::time::sleep(Duration::from_secs(15)).await;\n\n    // Query Loki\n    let loki_logs = loki_query(&amp;format!(r#\"{{trace_id=\"{trace_id_hex}\"}}\"#)).await.unwrap();\n    assert!(!loki_logs.is_empty(), \"expected logs in Loki for trace_id {trace_id_hex}\");\n\n    // Query Tempo\n    let tempo_trace = tempo_query(&amp;trace_id_hex).await.unwrap();\n    assert!(tempo_trace.spans.len() &gt; 1, \"expected multi-span trace\");\n\n    // Query LangSmith\n    let langsmith_trace = langsmith_query(&amp;trace_id_hex).await.unwrap();\n    assert_eq!(langsmith_trace.trace_id, trace_id_hex);\n\n    // Query Prometheus exemplars\n    let exemplars = prometheus_exemplar_query(\n        &amp;format!(\"cyberos_duration_ms_bucket{{tenant_id=\\\"{}\\\"}}\", test_tenant()),\n    ).await.unwrap();\n    let trace_ids_in_exemplars: HashSet&lt;String&gt; = exemplars.iter().filter_map(|e| e.trace_id.clone()).collect();\n    assert!(trace_ids_in_exemplars.contains(&amp;trace_id_hex), \"expected exemplar referencing {trace_id_hex}\");\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/main.rs (using the wrapper)\nuse axum::middleware;\nuse cyberos_obs_sdk::tracecontext::with_trace_context;\n\nlet app = Router::new()\n    .route(\"/v1/chat/completions\", post(handle_chat))\n    .layer(middleware::from_fn(|req, next| async move {\n        with_trace_context(&amp;req, || async { next.run(req).await }).await\n    }));</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-OBS-003</strong> — RED metrics; this FR adds exemplar emission to histograms.</li><li><strong>FR-OBS-004</strong> — LangSmith trace_id correlation.</li><li><strong>FR-AI-022</strong> — OTel trace emission; this FR ensures the trace_id is universal.</li><li>Crates: <code>opentelemetry@0.21</code>, <code>opentelemetry-otlp@0.14</code>, <code>opentelemetry_sdk@0.21</code>, <code>tracing@0.1</code>, <code>tracing-subscriber@0.3</code> with <code>Layer</code>, <code>tracing-opentelemetry</code>, <code>axum-tracing-opentelemetry@0.18</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Log line in Loki</h3>\n<pre><code class=\"language-json\">{\n  \"ts\": \"2026-05-15T14:00:00.123Z\",\n  \"level\": \"info\",\n  \"msg\": \"ai_gateway.precheck_allow\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"span_id\": \"b7ad6b7169203331\",\n  \"tenant_id\": \"org:cyberskill\",\n  \"estimated_usd\": 0.0085\n}</code></pre>\n<h3>Prometheus histogram with exemplar</h3>\n<pre><code class=\"language-text\">cyberos_duration_ms_bucket{service=\"ai-gateway\",route=\"/v1/chat/completions\",tenant_id=\"org:cyberskill\",le=\"500\"} 42 # {trace_id=\"0af7651916cd43dd8448eb211c80319c\"} 437 1747526400.123</code></pre>\n<h3>Outgoing HTTP with propagation</h3>\n<pre><code class=\"language-http\">POST /api/v1/messages HTTP/1.1\nHost: api.anthropic.com\nAuthorization: Bearer ...\ntraceparent: 00-0af7651916cd43dd8448eb211c80319c-c1d2e3f4a5b6c7d8-01\nbaggage: tenant_id=org:cyberskill</code></pre>\n<h3>Malformed traceparent log</h3>\n<pre><code class=\"language-text\">WARN  traceparent_hash16=4b8c0d2f1a7e9c3b\n      malformed traceparent; generating new</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Span links (correlate two unrelated traces) — slice 5+.</li><li>Probabilistic sampling decision propagation (FR-OBS-006 owns) — already covered.</li><li>Cross-region trace correlation (federation) — slice 6+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Service drops trace_id propagation</td><td>end_to_end_correlation_test fails</td><td>CI blocks PR</td><td>Engineer fixes propagation in that service</td></tr><tr><td>Invalid traceparent on incoming</td><td>strict W3C parser</td><td>Generate new + WARN with hash16</td><td>Self-resolves; investigate caller</td></tr><tr><td>Cross-call clock skew</td><td>trace_id correct but timestamps inconsistent</td><td>Trace UI may misorder spans</td><td>Tempo span_id-based ordering compensates</td></tr><tr><td>Missing tenant_id in baggage</td><td>log enrichment uses \"unknown\"</td><td>Sev-3 alarm if \"unknown\" &gt; 1%</td><td>Operator fixes upstream caller</td></tr><tr><td>Tokio task spawn without <code>Instrument</code></td><td>logs from task lack trace_id</td><td>Sev-3 detected via log search</td><td>Add <code>.instrument(Span::current())</code></td></tr><tr><td>Subprocess without env propagation</td><td>subprocess logs lack trace_id</td><td>Sev-3 alarm</td><td>Add OTEL_TRACE_ID to subprocess env</td></tr><tr><td>Exemplar emission breaks Prometheus</td><td>scrape error</td><td>Sev-3 alarm</td><td>Investigate exporter version</td></tr><tr><td>Bad traceparent floods malformed-counter</td><td>metric <code>outcome=malformed</code> spike</td><td>Sev-2 alarm; investigate caller</td><td>Possibly attack</td></tr><tr><td>Outgoing HTTP wrapper bypassed</td><td>Integration test asserts traceparent presence</td><td>Test fails → PR blocked</td><td>Use InstrumentedClient</td></tr><tr><td>Trace_id in logs but field name differs (e.g., trace_id vs traceID)</td><td>Loki query fails</td><td>Investigate field name</td><td>Standardise field name in <code>logging.rs</code></td></tr><tr><td>Exemplar UI broken (Grafana version)</td><td>Manual test fails</td><td>Fix Grafana datasource config</td><td>Pin Grafana version</td></tr><tr><td>Subprocess env clears at shell boundary</td><td>Tests assert env is set</td><td>Use <code>command.env(...)</code> not shell <code>export</code></td><td>By design</td></tr><tr><td>LangSmith trace_id format mismatch</td><td>Integration test asserts hex format</td><td>Test fails</td><td>FR-OBS-004 fix</td></tr><tr><td>Multiple trace_ids per request (regression)</td><td>end_to_end_correlation_test asserts unique</td><td>PR blocked</td><td>Investigate</td></tr><tr><td>Performance regression from logging overhead</td><td>Benchmark</td><td>Investigate; reduce per-line work</td><td>Profile</td></tr><tr><td>Span context lost across thread boundary</td><td>Integration test fails</td><td>Add <code>Instrument</code></td><td>By design</td></tr><tr><td>OTel SDK version mismatch</td><td>Compile error</td><td>Pin versions</td><td>By design</td></tr><tr><td>Header injection bypass</td><td>InstrumentedClient enforced</td><td>Code-review check</td><td>By design</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>W3C TraceContext is widely supported in OTel SDKs across languages — works seamlessly with Python (presidio sidecar, BGE sidecar) if those forward the header.</li><li>The <code>with_trace_context</code> wrapper is the entry point for every CyberOS service. Applying it as axum middleware ensures every handler runs in the right context.</li><li><code>tracing-subscriber</code> ObsContextLayer adds trace_id + span_id + tenant_id to every log record without per-call boilerplate. The layer reads from current OTel context; correctness depends on <code>with_trace_context</code> being applied.</li><li>Exemplar emission is automatic if the OTel-Prometheus exporter sees a trace_id in current context when <code>record</code> is called. The wrapper ensures the context is set.</li><li>The HTTP client wrapper (<code>InstrumentedClient</code>) is the only sanctioned way to make outgoing HTTP calls. Direct <code>reqwest::Client::post</code> calls would skip injection — code review enforces; future lint could check.</li><li>The end-to-end CI test is the structural enforcement of correlation. It runs the full OTel stack + LangSmith + a synthetic call, then verifies all 4 systems agree on the trace_id. Any propagation gap fails the test.</li><li>Subprocess propagation via env vars (<code>OTEL_TRACE_ID</code>, <code>OTEL_SPAN_ID</code>) is the standard pattern for OTel cross-process. memory_writer subprocess restores from env at boot.</li><li>Malformed traceparent generates new trace_id (not honors malformed) — security-aware default. Honoring would let attackers poison correlation.</li><li>The <code>unknown</code> tenant_id fallback is the operational signal for \"caller didn't set baggage.\" Sev-3 alarm if rate &gt; 1% — investigate which caller is missing the upstream call.</li></ul>\n<hr>\n<p><em>End of FR-OBS-005. Status: draft (10/10 target).</em></p>";
