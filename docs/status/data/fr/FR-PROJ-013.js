window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PROJ-013"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The calibration layer <strong>MUST</strong> compute nightly snapshots of estimate accuracy per (member, task_class). The contract:</p>\n<ol><li><strong>MUST</strong> define <code>estimate_calibration_snapshots</code> table: <code>id UUID PK</code>, <code>member_id UUID</code>, <code>task_class TEXT</code>, <code>snapshot_date DATE</code>, <code>posterior_mean f64</code>, <code>posterior_std f64</code>, <code>sample_size i32</code>, <code>effective_sample_size f64</code> (after exp decay), <code>recommended_multiplier f64</code>, <code>created_at</code>, <code>tenant_id</code>. Composite key <code>(member_id, task_class, snapshot_date)</code>.</li><li><strong>MUST</strong> schedule a nightly cron at 03:00 local time (after FR-PROJ-010 drift sweep).</li><li><strong>MUST</strong> compute per-cell as follows:</li></ol>\n<ul><li>Query all issues with <code>status = Done</code>, <code>cycle_id != NULL</code>, <code>assignee = member</code>, <code>task_class = class</code>, completed in last 365 days.</li><li>For each issue: <code>ratio = actual_hours / estimate_hours</code> where actual = sum of billable time entries; estimate from issue.estimate field. Skip issues without estimate or actual.</li><li>Apply exp-decay weight: <code>w_i = exp(-Δt / 90)</code> where Δt = days since completion.</li><li>Posterior mean = <code>Σ(w_i × ratio_i) / Σ(w_i)</code>.</li><li>Posterior std = weighted std of ratios.</li><li>Effective sample size = <code>Σ(w_i)</code>.</li><li>Recommended multiplier = posterior mean (rounded to 2 decimals).</li></ul>\n<ol><li><strong>MUST</strong> require minimum <code>effective_sample_size &gt;= 3</code> for a cell to publish a snapshot; below threshold → no row written (insufficient data; UI shows \"not enough data yet\").</li><li><strong>MUST</strong> persist snapshots APPEND-ONLY. Multiple snapshots per cell across days form a trend.</li><li><strong>MUST</strong> emit <code>proj.estimate_calibration_computed</code> memory audit row per published snapshot.</li><li><strong>MUST</strong> expose REST endpoints:</li></ol>\n<ul><li><code>GET /api/proj/calibration/:member_id/:task_class</code> — latest snapshot.</li><li><code>GET /api/proj/calibration/:member_id/:task_class/history?since=YYYY-MM-DD</code> — trend.</li><li><code>GET /api/proj/calibration/team?engagement_id=...</code> — aggregate trends per team.</li></ul>\n<ol><li><strong>MUST</strong> NEVER auto-apply multipliers to issue estimates — calibration is advisory. UI surfaces the recommended multiplier when creating new estimates.</li><li><strong>MUST</strong> RLS-enforce.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_calibration_cells_computed_total</code> (counter).</li><li><code>proj_calibration_cells_below_threshold_total</code> (counter).</li><li><code>proj_calibration_posterior_mean_drift{member_bucket, task_class}</code> (histogram — magnitude of change vs prior snapshot).</li></ul>\n<ol><li><strong>MUST</strong> support outlier filtering: data points with <code>ratio &gt; 5.0</code> OR <code>ratio &lt; 0.2</code> are excluded from the posterior compute by default (catches clear data-entry errors). Tenant policy <code>cyberos_proj_tenant_settings.calibration_outlier_threshold</code> overrides bounds.</li><li><strong>MUST</strong> include <code>posterior_ci_lower</code> + <code>posterior_ci_upper</code> (95% confidence interval) on each snapshot — UI shows \"recommended × 1.28 (CI: 1.10–1.46)\" so operators see uncertainty.</li><li><strong>MUST</strong> support per-engagement opt-out: <code>cyberos_proj_engagement_settings.calibration_enabled = false</code> excludes that engagement's issues from data points. Used for unusual engagements (R&amp;D experiments) that would bias calibration.</li><li><strong>MUST</strong> include <code>data_points_summary</code> JSONB on snapshot: <code>{ratio_p25, ratio_p50, ratio_p75, oldest_data_days, newest_data_days}</code> for operator inspection without re-fetching raw data.</li><li><strong>MUST</strong> support new-hire bootstrap: members with &lt; 3 data points use the team-average multiplier (per task_class) as a stub recommendation; <code>bootstrap = true</code> flag on the response indicates it's a stub.</li><li><strong>MUST</strong> compute team-level calibration: <code>GET /api/proj/calibration/team/:task_class</code> returns weighted average across all team members in the engagement.</li><li><strong>MUST</strong> emit <code>proj.calibration_drift_alert</code> SEV-3 when posterior_mean shifts &gt; 30% from prior snapshot (rapid skill change OR data corruption signal).</li><li><strong>MUST</strong> support backfill: <code>cyberos calibration backfill --member &lt;id&gt; --task-class &lt;c&gt; --since &lt;date&gt;</code> recomputes snapshots for past dates from historical data. Audit row <code>proj.calibration_backfilled</code>.</li><li><strong>MUST</strong> include <code>multiplier_applied_count</code> metric: how often operators actually used the recommended multiplier when creating new estimates. Adoption signal.</li><li><strong>MUST</strong> include <code>multiplier_acceptance_rate</code> per-member metric (rolling 30 days): how often the operator's estimate was within ±10% of <code>original × recommended_multiplier</code>. Calibration-effectiveness signal.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why Bayesian / exp-decay (DEC-341)?</strong> Simple averaging gives equal weight to 2-year-old and yesterday's data. Members' estimation skill changes (new hires improve; some plateau). Exp-decay with 90-day half-life means recent data dominates while history influences.</p>\n<p><strong>Why per-member per-task-class (DEC-340)?</strong> Individual estimation bias is the actionable signal. Team averages hide that some members are consistent and some erratic. Task-class separates \"bug fixes I do at 1.2× estimate\" from \"feature work I do at 0.9× estimate\" — different skill curves.</p>\n<p><strong>Why min sample size 3 (§1 #4)?</strong> Below 3 data points, posterior is dominated by prior — meaningless multiplier. 3 is the minimum useful sample for variance estimation.</p>\n<p><strong>Why never auto-apply (§1 #8)?</strong> Multipliers are mathematical artifacts; humans interpret context (vacation, illness, new tech stack). Auto-applying multipliers ignores context. Advisory UI = operator sees the trend, decides.</p>\n<p><strong>Why per-snapshot history (DEC-342)?</strong> \"Is Alice getting better at estimating?\" requires trend. Single-value-overwrite loses history. Append-only is the standard pattern.</p>\n<p><strong>Why outlier filtering (§1 #11)?</strong> Data-entry errors (typed 0.5h instead of 5h) skew posterior heavily; defaults of 5× / 0.2× catch the obvious cases without losing legitimate variance.</p>\n<p><strong>Why CI in snapshot (§1 #12)?</strong> Operators making decisions on point estimates without uncertainty miss \"this is 1.28 ± 0.05\" vs \"1.28 ± 0.50.\" Different actions.</p>\n<p><strong>Why per-engagement opt-out (§1 #13)?</strong> R&amp;D engagements have unpredictable scope; including them in calibration biases regular engineering work toward \"always slower than estimated.\"</p>\n<p><strong>Why data_points_summary (§1 #14)?</strong> Operators inspecting a snapshot want quick stats without re-running queries; pre-computed summary in the snapshot row.</p>\n<p><strong>Why new-hire bootstrap (§1 #15)?</strong> Below 3 data points = no snapshot per §1 #4; but the UI still needs to show <em>something</em>. Team average is a reasonable stub.</p>\n<p><strong>Why team-level calibration (§1 #16)?</strong> Operators planning by team need an aggregate (\"our team's bug-fix estimates average 1.2× actual\"); per-member is too granular for sprint planning.</p>\n<p><strong>Why drift alert (§1 #17)?</strong> A 30%+ shift signals: (a) member skill changed, (b) data corruption, (c) work-type shift. Either way, operator should know.</p>\n<p><strong>Why backfill (§1 #18)?</strong> New tenants joining mid-year have history; backfill computes snapshots so the trend is visible from day 1.</p>\n<p><strong>Why multiplier-applied count (§1 #19)?</strong> Calibration is advisory; if operators ignore it, the feature has no impact. Tracking adoption surfaces UX issues.</p>\n<p><strong>Why multiplier-acceptance rate (§1 #20)?</strong> Even with adoption, the recommendation may be wrong; tracking whether following it produced accurate estimates is the feedback loop.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0013_estimate_calibration.sql\n\nCREATE TABLE estimate_calibration_snapshots (\n    id                       UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    member_id                UUID NOT NULL,\n    task_class               TEXT NOT NULL,\n    snapshot_date            DATE NOT NULL,\n    posterior_mean           DOUBLE PRECISION NOT NULL,\n    posterior_std            DOUBLE PRECISION NOT NULL,\n    sample_size              INT NOT NULL,\n    effective_sample_size    DOUBLE PRECISION NOT NULL,\n    recommended_multiplier   DOUBLE PRECISION NOT NULL,\n    created_at               TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    tenant_id                UUID NOT NULL,\n    UNIQUE (member_id, task_class, snapshot_date)\n);\nCREATE INDEX idx_calibration_latest ON estimate_calibration_snapshots (member_id, task_class, snapshot_date DESC);\n\nALTER TABLE estimate_calibration_snapshots ENABLE ROW LEVEL SECURITY;\nCREATE POLICY calibration_tenant_iso ON estimate_calibration_snapshots\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Rust</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/calibration/bayes.rs\npub struct DataPoint { pub actual_hours: f64, pub estimate_hours: f64, pub days_ago: f64 }\n\npub fn compute_posterior(points: &amp;[DataPoint]) -&gt; Option&lt;Posterior&gt; {\n    if points.is_empty() { return None; }\n    let half_life_days = 90.0;\n    let decay = |days: f64| (-days * 2f64.ln() / half_life_days).exp();\n\n    let weighted_ratios: Vec&lt;(f64, f64)&gt; = points.iter()\n        .filter(|p| p.estimate_hours &gt; 0.0 &amp;&amp; p.actual_hours &gt; 0.0)\n        .map(|p| {\n            let ratio = p.actual_hours / p.estimate_hours;\n            let w = decay(p.days_ago);\n            (ratio, w)\n        }).collect();\n\n    if weighted_ratios.len() &lt; 3 { return None; }\n\n    let sum_w: f64 = weighted_ratios.iter().map(|(_, w)| w).sum();\n    let weighted_mean: f64 = weighted_ratios.iter().map(|(r, w)| r * w).sum::&lt;f64&gt;() / sum_w;\n    let weighted_var: f64 = weighted_ratios.iter()\n        .map(|(r, w)| w * (r - weighted_mean).powi(2))\n        .sum::&lt;f64&gt;() / sum_w;\n    let weighted_std = weighted_var.sqrt();\n\n    Some(Posterior {\n        mean: weighted_mean,\n        std: weighted_std,\n        sample_size: weighted_ratios.len() as i32,\n        effective_sample_size: sum_w,\n    })\n}\n\n#[derive(Clone, Debug)]\npub struct Posterior {\n    pub mean: f64,\n    pub std: f64,\n    pub sample_size: i32,\n    pub effective_sample_size: f64,\n}</code></pre>\n<h3>Nightly job</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/calibration/nightly.rs\npub async fn run_nightly(pool: &amp;sqlx::PgPool, tenant_id: uuid::Uuid) -&gt; anyhow::Result&lt;i32&gt; {\n    sqlx::query(\"SELECT set_config('app.tenant_id', $1, true)\")\n        .bind(tenant_id.to_string()).execute(pool).await?;\n\n    let cells: Vec&lt;(uuid::Uuid, String)&gt; = sqlx::query_as(\n        \"SELECT DISTINCT assignee_subject_id, task_class\n         FROM issues\n         WHERE assignee_subject_id IS NOT NULL AND task_class IS NOT NULL\n           AND status = 'done' AND status_updated_at_ns &gt; $1\",\n        (chrono::Utc::now() - chrono::Duration::days(365)).timestamp_nanos_opt().unwrap()\n    ).fetch_all(pool).await?;\n\n    let today = chrono::Utc::now().date_naive();\n    let mut published = 0;\n    for (member_id, task_class) in cells {\n        let points = fetch_data_points(pool, member_id, &amp;task_class).await?;\n        if let Some(post) = compute_posterior(&amp;points) {\n            sqlx::query(\n                \"INSERT INTO estimate_calibration_snapshots\n                  (member_id, task_class, snapshot_date,\n                   posterior_mean, posterior_std, sample_size, effective_sample_size,\n                   recommended_multiplier, tenant_id)\n                 VALUES ($1,$2,$3,$4,$5,$6,$7,$8, current_setting('app.tenant_id')::uuid)\n                 ON CONFLICT (member_id, task_class, snapshot_date) DO NOTHING\"\n            )\n            .bind(member_id).bind(&amp;task_class).bind(today)\n            .bind(post.mean).bind(post.std).bind(post.sample_size).bind(post.effective_sample_size)\n            .bind((post.mean * 100.0).round() / 100.0)\n            .execute(pool).await?;\n\n            emit_memory_row(\"proj.estimate_calibration_computed\", serde_json::json!({\n                \"member_id\": member_id, \"task_class\": task_class,\n                \"snapshot_date\": today, \"posterior_mean\": post.mean,\n                \"sample_size\": post.sample_size,\n                \"effective_sample_size\": post.effective_sample_size,\n                \"recommended_multiplier\": (post.mean * 100.0).round() / 100.0,\n            })).await;\n            metrics::counter!(\"proj_calibration_cells_computed_total\").increment(1);\n            published += 1;\n        } else {\n            metrics::counter!(\"proj_calibration_cells_below_threshold_total\").increment(1);\n        }\n    }\n    Ok(published)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Compute posterior for happy cell</strong> — 5 issues with ratios [1.0, 1.2, 1.5, 1.3, 1.1] → posterior_mean ≈ 1.22 (weighted by recency).</li><li><strong>Effective sample size from decay</strong> — 3 issues 1 day ago + 3 issues 180 days ago → effective_sample_size ≈ 3 + 3 × 0.25 = 3.75.</li><li><strong>Threshold of 3 enforced</strong> — only 2 issues completed → no snapshot written.</li><li><strong>Snapshot append-only</strong> — second nightly run same day → ON CONFLICT DO NOTHING; no row added.</li><li><strong>History endpoint returns trend</strong> — 10 days of snapshots → 10 rows ASC by snapshot_date.</li><li><strong>memory audit per published snapshot</strong> — <code>proj.estimate_calibration_computed</code> row appears.</li><li><strong>Recommended multiplier ≈ posterior mean</strong> — rounded to 2 decimals.</li><li><strong>Per-cell isolation</strong> — Alice + bug-fix has its own row distinct from Alice + feature-work.</li><li><strong>No auto-apply to estimates</strong> — UI surfaces multiplier; issue.estimate field unchanged.</li><li><strong>RLS isolates</strong> — tenant A invisible to tenant B.</li><li><strong>Skip issues without estimate</strong> — issue with estimate = null → skipped (not in posterior).</li><li><strong>Skip issues without actual</strong> — completed but 0 billable hours → skipped.</li><li><strong>Drift metric</strong> — second snapshot with mean differing from prior → histogram records magnitude.</li><li><strong>Counter per cell processed</strong> — even below-threshold cells counted.</li><li><strong>Outlier excluded</strong> — data point with ratio=10 → excluded from posterior; included in <code>data_points_summary.outlier_count</code> (AC for §1 #11).</li><li><strong>CI in snapshot</strong> — snapshot includes <code>posterior_ci_lower</code> + <code>posterior_ci_upper</code> 95% bounds (AC for §1 #12).</li><li><strong>Engagement opt-out excludes data</strong> — set <code>calibration_enabled=false</code>; that engagement's issues skipped (AC for §1 #13).</li><li><strong>data_points_summary populated</strong> — snapshot has p25/p50/p75/oldest/newest (AC for §1 #14).</li><li><strong>New-hire bootstrap returns team average</strong> — member with 1 data point → response has <code>bootstrap=true</code> + team-average multiplier (AC for §1 #15).</li><li><strong>Team-level calibration</strong> — GET /team/:task_class → weighted average across team (AC for §1 #16).</li><li><strong>Drift alert at &gt; 30% shift</strong> — fixture: posterior_mean shifts from 1.0 to 1.4 → <code>proj.calibration_drift_alert</code> SEV-3 (AC for §1 #17).</li><li><strong>Backfill produces past snapshots</strong> — <code>cyberos calibration backfill --since 2026-01-01</code> → snapshots for each prior day (AC for §1 #18).</li><li><strong>Multiplier-applied count metric</strong> — operator uses recommended → counter increments (AC for §1 #19).</li><li><strong>Multiplier acceptance rate</strong> — operator's estimate within 10% of recommended → counted in success (AC for §1 #20).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[test]\nfn posterior_with_uniform_ratios() {\n    let points = vec![\n        DataPoint { actual_hours: 1.0, estimate_hours: 1.0, days_ago: 0.0 },\n        DataPoint { actual_hours: 2.0, estimate_hours: 2.0, days_ago: 0.0 },\n        DataPoint { actual_hours: 3.0, estimate_hours: 3.0, days_ago: 0.0 },\n    ];\n    let p = compute_posterior(&amp;points).unwrap();\n    assert!((p.mean - 1.0).abs() &lt; 1e-6);\n    assert_eq!(p.sample_size, 3);\n}\n\n#[test]\nfn old_data_decays() {\n    let recent = vec![\n        DataPoint { actual_hours: 2.0, estimate_hours: 1.0, days_ago: 0.0 },\n        DataPoint { actual_hours: 2.0, estimate_hours: 1.0, days_ago: 0.0 },\n        DataPoint { actual_hours: 2.0, estimate_hours: 1.0, days_ago: 0.0 },\n    ];\n    let old = vec![\n        DataPoint { actual_hours: 1.0, estimate_hours: 1.0, days_ago: 180.0 },\n        DataPoint { actual_hours: 1.0, estimate_hours: 1.0, days_ago: 180.0 },\n        DataPoint { actual_hours: 1.0, estimate_hours: 1.0, days_ago: 180.0 },\n    ];\n    let mixed: Vec&lt;DataPoint&gt; = recent.iter().chain(old.iter()).cloned().collect();\n    let p = compute_posterior(&amp;mixed).unwrap();\n    assert!(p.mean &gt; 1.5, \"recent data (ratio 2.0) should dominate; got mean = {}\", p.mean);\n}\n\n#[test]\nfn below_threshold_returns_none() {\n    let points = vec![\n        DataPoint { actual_hours: 1.0, estimate_hours: 1.0, days_ago: 0.0 },\n        DataPoint { actual_hours: 1.0, estimate_hours: 1.0, days_ago: 0.0 },\n    ];\n    assert!(compute_posterior(&amp;points).is_none());\n}\n\n#[tokio::test]\nasync fn nightly_publishes_snapshot() {\n    let env = TestEnv::new().await;\n    let member = env.create_member().await;\n    for i in 0..5 {\n        env.create_completed_issue(member, \"bug_fix\", estimate=2.0, actual=2.5,\n                                    completed_days_ago=i*7).await;\n    }\n    let published = run_nightly(&amp;env.pool, env.tenant_id()).await.unwrap();\n    assert_eq!(published, 1);\n    let snap: CalibrationSnapshot = env.read_latest_snapshot(member, \"bug_fix\").await;\n    assert!((snap.recommended_multiplier - 1.25).abs() &lt; 0.05);\n}\n\n#[tokio::test]\nasync fn idempotent_same_day() {\n    let env = TestEnv::new().await;\n    let _ = env.bootstrap_calibration_data().await;\n    let r1 = run_nightly(&amp;env.pool, env.tenant_id()).await.unwrap();\n    let r2 = run_nightly(&amp;env.pool, env.tenant_id()).await.unwrap();\n    assert!(r1 &gt;= 1);\n    assert_eq!(r2, 0);   // ON CONFLICT DO NOTHING\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + DB above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-PROJ-001</strong> — issues schema (estimate, task_class, assignee).</li><li><strong>FR-PROJ-004</strong> — status FSM (Done detection).</li><li><strong>FR-TIME-001</strong> — billable hours source.</li><li><strong>FR-MEMORY-101</strong> — audit emission.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.estimate_calibration_computed\",\n  \"payload\": {\n    \"member_id\": \"mb-...\",\n    \"task_class\": \"bug_fix\",\n    \"snapshot_date\": \"2026-05-16\",\n    \"posterior_mean\": 1.28,\n    \"sample_size\": 12,\n    \"effective_sample_size\": 8.7,\n    \"recommended_multiplier\": 1.28\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Cell-aware new-hire bootstrap (use team average as prior until 3 data points) — slice 4+.</li><li>Variance-aware multiplier (recommend × 1.28 ± 0.15) — slice 4+; UI complexity.</li><li>Estimation method exploration (planning poker vs absolute) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Member has &lt; 3 cells</td><td>threshold check</td><td>No snapshot</td><td>None — gather more data</td></tr><tr><td>All issues have ratio = 0 or NaN</td><td>NaN-skip in compute_posterior</td><td>Cell skipped</td><td>Operator investigates time-tracking</td></tr><tr><td>Insert race (concurrent nightly)</td><td>ON CONFLICT DO NOTHING</td><td>One winner; idempotent</td><td>None</td></tr><tr><td>Daylight-saving day-counting</td><td>days_ago uses UTC</td><td>Consistent</td><td>None</td></tr><tr><td>Extreme outlier (ratio = 100)</td><td>included but std reflects</td><td>UI shows wide CI</td><td>Operator considers excluding</td></tr><tr><td>Member changes role</td><td>calibration carries on; new task_class cell starts fresh</td><td>Trend shows change</td><td>None</td></tr><tr><td>memory emit fails</td><td>snapshot stored; audit lost; sev-2</td><td>None</td><td>Operator restores memory</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>Aggregate query slow at 100K issues</td><td>nightly batch acceptable</td><td>None</td><td>Slice 4+ optimise</td></tr><tr><td>Issue without <code>estimate</code> field</td><td>filtered out</td><td>Doesn't affect computation</td><td>None</td></tr><tr><td>Multiple time entries same issue (split work)</td><td>sum gives true actual</td><td>Correct</td><td>None</td></tr><tr><td>memory-PII concerns (member name in audit)</td><td>member_id is UUID, not name</td><td>Safe</td><td>None</td></tr><tr><td>Tenant offboarding</td><td>their cell snapshots persist</td><td>Audit trail retained per legal</td><td>None until cleanup script</td></tr><tr><td>Outlier threshold misconfigured (negative)</td><td>startup validation</td><td>use default</td><td>Operator</td></tr><tr><td>CI computation produces NaN (zero variance)</td><td>guard: return CI = mean ± 0</td><td>None</td><td>None</td></tr><tr><td>Engagement opt-out toggled mid-cycle</td><td>next nightly excludes</td><td>snapshot uses snapshot-time state</td><td>None</td></tr><tr><td>data_points_summary &gt; 1KB</td><td>bounded</td><td>None</td><td>None</td></tr><tr><td>New-hire member with 0 team data either</td><td>bootstrap returns no recommendation</td><td>UI shows \"no data\"</td><td>None</td></tr><tr><td>Team-level calibration with single member</td><td>uses that member's data only</td><td>None</td><td>None</td></tr><tr><td>Drift alert false positive (new hire transition)</td><td>dedup if member changed task_class</td><td>None</td><td>None</td></tr><tr><td>Backfill on date with existing snapshot</td><td>ON CONFLICT DO NOTHING</td><td>None</td><td>None</td></tr><tr><td>Multiplier-applied tracking races create-issue</td><td>best-effort metric</td><td>None</td><td>None</td></tr><tr><td>Acceptance rate metric over short window</td><td>bounded; require min 5 issues</td><td>None</td><td>None</td></tr><tr><td>All cells below threshold</td><td>counter only</td><td>sweep completes</td><td>None</td></tr><tr><td>Posterior compute with all-zero weights (edge case)</td><td>division by zero guard</td><td>None</td><td>None</td></tr><tr><td>CI bounds clamped at [0.1, 10.0]</td><td>sanity clamp</td><td>None</td><td>None</td></tr><tr><td>Snapshot row size &gt; 10KB (huge summary)</td><td>bounded</td><td>None</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>90-day half-life chosen empirically: covers ~3 cycles for typical 2-3 week cycles; weighting goes ~0.5 at 90 days, ~0.25 at 180.</li><li>Exp-decay formula: <code>w = exp(-days × ln(2) / 90)</code>. At days=0 → w=1.0; days=90 → w=0.5; days=270 → w=0.125.</li><li>Posterior std is weighted variance (Bessel correction not applied because weighted; effective_sample_size accounts for it).</li><li>Cell-aware new-hire (use team-average prior) is slice 4+; current implementation just waits for 3 data points.</li><li>The UI hint in §1 #8 lives in FR-PROJ-001's issue create form: when assignee + task_class chosen, fetches latest snapshot and displays \"recommended × &lt;multiplier&gt;\".</li><li>Issue estimate units are points/hours depending on engagement convention; this FR treats them opaquely (just numbers; ratio is dimensionless).</li><li>Outlier thresholds (5×, 0.2×) were chosen empirically from observed data; data-entry errors typically cluster at integer ratios (5, 10, 100).</li><li>CI is computed using normal approximation (mean ± 1.96 × std / sqrt(effective_n)); good enough for the sample sizes typical.</li><li>Per-engagement opt-out is rare; R&amp;D engagements specifically. Most engagements should contribute.</li><li>data_points_summary is computed once per snapshot at compute time; not regenerated on read.</li><li>New-hire bootstrap uses team-average across same task_class for the engagement; if no team data either, returns no recommendation.</li><li>Team-level calibration weights members equally (could weight by sample size, slice 4+).</li><li>Drift alert threshold (30%) is conservative; tunable per tenant in slice 4+.</li><li>Backfill is single-tenant; running for all tenants requires explicit iteration.</li><li>Multiplier-applied count is incremented at issue.create when operator's estimate matches recommended × original (within 1%); approximate but useful.</li><li>Multiplier-acceptance rate is computed at issue.completion: if actual is within 10% of (original × recommended), counted as success.</li><li>The 95% CI bounds may be wide for small effective_sample_size; operators reading them know to discount.</li><li>We considered storing raw data points alongside snapshot but rejected: bloats storage; can re-fetch from issues if needed.</li><li>The snapshot is per-day; if no data changes in a day, no snapshot written (ON CONFLICT DO NOTHING + no-data-points returns None).</li><li>Calibration is per-tenant scope; cross-tenant member calibration not supported (member is unique within tenant).</li><li>We don't track per-engagement calibration because individual estimation skill is the actionable variable; per-engagement noise.</li><li>The recommended multiplier rounding (2 decimals) matches operator UI display; raw mean stored unrounded for trend computation.</li><li>Posterior std stored for CI computation + trend analysis; not surfaced in main UI.</li><li>The cell-level audit row emits even for cells that produce no snapshot (counter increments).</li></ul>\n<hr>\n<p><em>End of FR-PROJ-013.</em></p>";
