window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DOCS-006"]="<h1>TASK-DOCS-006: Status hub</h1>\n<h2>§1 - Description</h2>\n<p>One page answers \"where is the project\" completely: a persistent deck of headline numbers over three deep-linkable tabs.</p>\n<p>Normative clauses:</p>\n<ol><li>A builder <code>tools/docs-site/render-status-hub.mjs</code> MUST emit <code>dist/website/reference/status.html</code> through the <code>status-hub@1</code> template from the SAME three inputs as TASK-DOCS-003 (task frontmatter, CHANGELOG sections, VERSION) - node stdlib only, deterministic stamp (VERSION + commit, no wall clock).</li><li>The command deck MUST always show: VERSION, built-from commit, total tasks, per-status counts (10-value enum order), latest release (version + date), and module count - visible regardless of active tab.</li><li>Three tabs MUST render: <strong>Roadmap</strong> (default; the TASK-DOCS-003 board + module rollups, rows linking to task pages when TASK-DOCS-005 pages exist), <strong>Backlog</strong> (filterable table: id, title, module, class, priority, status - module/class/status/priority facets; BACKLOG.md stays the agents' write-path index, this tab is the human view), <strong>Changelog</strong> (the release timeline). Tab routing via URL hash (<code>#roadmap</code>, <code>#backlog</code>, <code>#changelog</code>) with hash-change handling; no JS -&gt; all three panels present in the DOM and the tab bar degrades to in-page anchor links.</li><li><code>roadmap.html</code> MUST become a redirect stub to <code>status.html#roadmap</code> (meta refresh + link, no JS required); the shared nav's entry MUST become \"Status\" pointing at status.html; TASK-DOCS-003's suite is repointed at the hub builder with its assertions preserved (board counts, determinism, honest failures, token-clean) - the superseding is recorded on TASK-DOCS-003 as a post-ship amendment.</li><li>Inline vanilla JS only, CDS tokens only (template rule), same honest failures as TASK-DOCS-003 §1 #5 (unparseable task names file; zero CHANGELOG sections fails).</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The deck answers 90% of \"how are we doing\" without a click; tabs keep each dense surface uncrowded; hash routing makes every view shareable. Superseding roadmap.html instead of keeping two pages avoids a split status story.</p>\n<h2>§3 - Contract</h2>\n<p>Summary stdout: <code>status-hub: N tasks, R releases, VERSION X (deck+3 tabs)</code>. Page: <code>reference/status.html</code>, template <code>status-hub@1</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Deck totals true</strong> (§1 #2) - fixture corpus: deck numbers equal frontmatter/CHANGELOG/VERSION-derived truth.</li><li><strong>Three tabs + routing + degrade</strong> (§1 #3) - emitted HTML carries three panels in DOM; hash script present; anchors work JS-free (structural asserts).</li><li><strong>Backlog tab facets</strong> (§1 #3) - rows carry data attrs for all four facets; filter script wired.</li><li><strong>Supersession clean</strong> (§1 #4) - roadmap.html is a stub pointing at status.html#roadmap; nav says Status; repointed legacy suite passes.</li><li><strong>Determinism + honesty + tokens</strong> (§1 #1, #5) - byte-identical rebuilds; failure fixtures fail loud; no hex outside token block.</li><li><strong>task-page links</strong> (§1 #3) - board/backlog rows href <code>frs/&lt;module&gt;/&lt;STEM&gt;/</code> when TASK-DOCS-005 is present in the build (fixture with pages asserts hrefs).</li></ol>\n<h2>§5 - Verification</h2>\n<p><code>tools/docs-site/tests/test_render_status_hub.sh</code>: t01_deck_true, t02_tabs_routing_degrade, t03_backlog_facets, t04_supersession, t05_deterministic_honest_tokens, t06_fr_links. (AC 1-6.)</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Port render-roadmap's parsers; add backlog-table + deck renderers; substitute status-hub.html slots; stub roadmap.html; nav refItems swap; repoint old suite.</p>\n<h2>§7 - Dependencies</h2>\n<p>TASK-TPL-001 (shell). Soft: TASK-DOCS-005 (links activate when pages exist; builder feature-detects the frs/ output dir).</p>\n<h2>§8 - Example payloads</h2>\n<p><code>status-hub: 486 tasks, 18 releases, VERSION 0.1.0 (deck+3 tabs)</code></p>\n<h2>§9 - Open questions</h2>\n<p>None blocking. A fourth \"Releases assets\" tab is future scope.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Tab state lost on reload - hash IS the state; default applied only when hash absent.</li><li>Deck/tab count drift - both derive from one parsed corpus object; single source in code.</li><li>roadmap.html bookmarks break - stub redirect keeps them alive indefinitely.</li><li>Backlog tab tempts editing-by-hand expectations - tab header states it is a generated view; BACKLOG.md remains the write path.</li><li>JS-off filtering absent - full corpus visible unfiltered; documented degrade per §1 #3.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep TASK-DOCS-003's greppable prefixes alive in the new summary line; deploy logs already watch them.</p>\n<p><em>End of TASK-DOCS-006.</em></p>";
