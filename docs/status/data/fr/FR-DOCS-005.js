window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOCS-005"]="<h1>FR-DOCS-005: Per-FR CDS HTML pages</h1>\n<h2>§1 - Description</h2>\n<p>Every FR folder renders to one CDS-styled page a human can read anywhere - spec body, audit verdict, media - while markdown stays the only authored source.</p>\n<p>Normative clauses:</p>\n<ol><li>A builder <code>tools/docs-site/render-fr-pages.mjs</code> MUST walk <code>docs/feature-requests/&lt;module&gt;/&lt;STEM&gt;/spec.md</code>, render markdown via the existing dependency-free <code>md.mjs</code>, and emit <code>dist/website/frs/&lt;module&gt;/&lt;STEM&gt;/index.html</code> through the <code>deliverable@1</code> template (FR-TPL-001), node stdlib only.</li><li>Each page MUST show: id, title, status badge, module + class + priority badges, key frontmatter (created/shipped/depends_on/blocks as links when those FRs have pages), the rendered spec body with heading anchors per §-section, and the audit verdict + score when <code>audit.md</code> exists (rendered below the spec, visually separated).</li><li>Assets MUST work: <code>&lt;STEM&gt;/assets/**</code> is copied beside the page; relative <code>assets/...</code> references in the markdown resolve unchanged; image links render as <code>&lt;img&gt;</code>, and links to video files (mp4/webm/mov) render as <code>&lt;video controls&gt;</code> - both capped to content width.</li><li>Self-containment (template rule): CDS tokens + shell styles inlined into each page; the only external references are the page's own relative assets - pages work from file://.</li><li><code>build.sh</code> MUST run the builder before nav generation; the FR catalog's cards MUST link to the pages (<code>render-fr-catalog.mjs</code> href swap); determinism and honesty per house rules: byte-identical rebuilds, unreadable spec fails naming the file, missing referenced asset fails non-zero (FR-DOCS-002 §1 #7 discipline).</li><li>Scale envelope: the full corpus (~486 pages) MUST build in under 30s on CI hardware and add no external asset weight beyond the corpus's own media.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Rendering through the templates module is what makes \"CDS everywhere\" a property instead of a habit - one shell, hundreds of pages. Reusing md.mjs keeps a single markdown dialect across docs and FRs.</p>\n<h2>§3 - Contract</h2>\n<p>Page path: <code>frs/&lt;module&gt;/&lt;STEM&gt;/index.html</code>; template <code>deliverable@1</code>; asset copy: sibling <code>assets/</code>. Build summary line: <code>fr-pages: N pages, M assets copied, K with audits</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Pages render for the corpus</strong> (§1 #1, #2) - fixture tree (spec+audit+asset mix) renders all pages; badges/anchors/audit block assert on emitted HTML.</li><li><strong>Media works</strong> (§1 #3) - fixture with png + mp4 emits <code>&lt;img&gt;</code> + <code>&lt;video controls&gt;</code>; asset files copied beside the page; relative hrefs unchanged.</li><li><strong>Self-contained</strong> (§1 #4) - a rendered page has zero http(s)/absolute asset references; tokens present inline.</li><li><strong>Wired + deterministic + honest</strong> (§1 #5) - build.sh invokes it; double build byte-identical; broken spec and missing asset each fail naming the file.</li><li><strong>Catalog links</strong> (§1 #5) - fr-catalog cards href the new pages (fixture assert).</li><li><strong>Envelope</strong> (§1 #6) - timed corpus build under the cap on the runner.</li></ol>\n<h2>§5 - Verification</h2>\n<p><code>tools/docs-site/tests/test_render_fr_pages.sh</code>: t01_corpus_renders, t02_media, t03_selfcontained, t04_wired_deterministic_honest, t05_catalog_links, t06_envelope. (AC 1-6.)</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Walk folders -&gt; frontmatter + md.mjs body -&gt; slot-substitute deliverable.html -&gt; write + copy assets; video extension map at top; audit.md rendered with the same md pipeline.</p>\n<h2>§7 - Dependencies</h2>\n<p>FR-TPL-001 (shell + tokens), FR-DOCS-004 (layout). FR-DOCS-006 links these pages from the board.</p>\n<h2>§8 - Example payloads</h2>\n<p><code>fr-pages: 486 pages, 12 assets copied, 209 with audits</code></p>\n<h2>§9 - Open questions</h2>\n<p>None blocking. PDF export of a page is future scope (print stylesheet suffices meanwhile).</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Giant video bloats the site - assets are copied as-is, never inlined; page weight = media weight, a content decision.</li><li>Spec with raw HTML - md.mjs escapes unsupported constructs (FR-DOCS-002 §1 #3), so injection dies at the renderer.</li><li>Broken relative asset ref - missing-asset build failure names spec + path.</li><li>486-page nav pollution - pages are NOT in the shared nav; entry via catalog, board, and direct links.</li><li>Template drift - data-template-id asserted in t03, so a fork or hand-edit surfaces in tests.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the summary line stable (deploy log health). Anchor slugs reuse check_doc_anchors' grammar so §-citations deep-link.</p>\n<p><em>End of FR-DOCS-005.</em></p>";
