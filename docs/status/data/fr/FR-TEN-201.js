window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TEN-201"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship HoldCo flip CLI at <code>services/ten/src/holdco/</code> + <code>services/ten/src/cli/holdco_flip.rs</code> orchestrating 6-step restructure with triple-sign + immutable checkpoints, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>holdco_flip_step</code> against closed enum per DEC-2401.</li></ol>\n<ol><li><strong>MUST</strong> require CEO + CFO + CLO triple-sign at initiation per DEC-2403 via <code>triple_sign_gate.rs::can_initiate(flip)</code>:</li></ol>\n<ul><li>All three signed</li><li>Same-person rejected across any two slots</li><li>Initiation proceeds only after all three</li></ul>\n<ol><li><strong>MUST</strong> orchestrate 6 steps via CLI per DEC-2400:</li></ol>\n<ul><li>sg_entity_form: BizFile API call to form Pte Ltd</li><li>acra_filings: generate Form 24 + ACRA submissions</li><li>shareholder_migration: VN shareholders → SG shareholders (signed agreements track)</li><li>esop_reissue: FR-ESOP-001 grants re-issued under SG entity</li><li>residency_migrated: FR-TEN-103 tenant residency → sg-1</li><li>completed: all steps done</li></ul>\n<ol><li><strong>MUST</strong> be resumable per DEC-2402 — each step writes checkpoint; restart resumes from last completed.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0010</code>: ```sql CREATE TABLE ten_holdco_flips ( flip_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','sg_entity_formed','acra_filings_prepared','shareholder_agreements_drafted','esop_reissued','residency_migrated','completed','failed')), ceo_signed_by UUID, ceo_signed_at TIMESTAMPTZ, cfo_signed_by UUID, cfo_signed_at TIMESTAMPTZ, clo_signed_by UUID, clo_signed_at TIMESTAMPTZ, initiated_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, failure_reason TEXT, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (tenant_id)  -- one flip per tenant ); ALTER TABLE ten_holdco_flips ENABLE ROW LEVEL SECURITY; CREATE POLICY flips_rls ON ten_holdco_flips USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON ten_holdco_flips FROM cyberos_app; GRANT UPDATE (status, ceo_signed_by, ceo_signed_at, cfo_signed_by, cfo_signed_at, clo_signed_by, clo_signed_at, initiated_at, completed_at, failure_reason) ON ten_holdco_flips TO cyberos_app;</li></ol>\n<p>   CREATE TABLE ten_holdco_flip_steps (      step_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      flip_id UUID NOT NULL REFERENCES ten_holdco_flips(flip_id),      step_name TEXT NOT NULL,      status TEXT NOT NULL DEFAULT 'pending'        CHECK (status IN ('pending','running','completed','failed')),      started_at TIMESTAMPTZ,      completed_at TIMESTAMPTZ,      output_jsonb JSONB,      failure_reason TEXT,      UNIQUE (flip_id, step_name)    );    ALTER TABLE ten_holdco_flip_steps ENABLE ROW LEVEL SECURITY;    CREATE POLICY flip_steps_rls ON ten_holdco_flip_steps      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON ten_holdco_flip_steps FROM cyberos_app;    GRANT UPDATE (status, started_at, completed_at, output_jsonb, failure_reason) ON ten_holdco_flip_steps TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> expose CLI: ``<code>text cyberos-ten holdco-flip init --ceo-sign --cfo-sign --clo-sign cyberos-ten holdco-flip resume --flip-id &lt;id&gt; cyberos-ten holdco-flip status --flip-id &lt;id&gt; </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2404. PII per FR-MEMORY-111: output_jsonb SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from CLI → orchestrator → step → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> initiate without triple-sign per DEC-2403.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior step per DEC-2402.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow same-person dual sign across slots per DEC-2403.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why CLI (DEC-2400)?</strong> HoldCo flip is rare + high-stakes; CLI provides reproducible scripting; full audit trail.</p>\n<p><strong>Why 8-state enum (DEC-2401)?</strong> Captures sequential restructure phases; status visible to ops.</p>\n<p><strong>Why triple-sign (DEC-2403)?</strong> Restructure involves $100k+ in legal + restructure costs; broad governance.</p>\n<p><strong>Why ACRA filings (DEC-2400)?</strong> SG company formation requires statutory submissions; CLI generates standard package.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>CLI usage:</p>\n<pre><code class=\"language-bash\">$ cyberos-ten holdco-flip init\n✓ CEO signature captured\n✓ CFO signature captured\n✓ CLO signature captured\n✓ Flip initiated (flip_id: abc-123)\nResume with: cyberos-ten holdco-flip resume --flip-id abc-123</code></pre>\n<pre><code class=\"language-bash\">$ cyberos-ten holdco-flip status --flip-id abc-123\nStep: sg_entity_formed ✓\nStep: acra_filings_prepared ✓\nStep: shareholder_agreements_drafted ⟳ in progress\nStep: esop_reissued ⏸ pending\nStep: residency_migrated ⏸ pending</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>holdco_flip_step enum cardinality 8</strong>. 2. <strong>CEO+CFO+CLO triple-sign required</strong>. 3. <strong>Same-person across slots rejected</strong>. 4. <strong>CLI commands (init/resume/status)</strong>. 5. <strong>6-step orchestration</strong>. 6. <strong>Resumable from last completed</strong>. 7. <strong>UNIQUE(tenant_id) — one flip per tenant</strong>. 8. <strong>4 memory audit kinds emitted</strong>. 9. <strong>PII scrubbed (output SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>Trace_id preserved</strong>. 12. <strong>Append-only via REVOKE except status cols</strong>. 13. <strong>Per-step checkpoint</strong>. 14. <strong>ACRA Form 24 generated</strong>. 15. <strong>ESOP re-issue under SG entity</strong>. 16. <strong>FR-TEN-103 residency → sg-1 on completion</strong>. 17. <strong>Failed step → flip status=failed; resumable</strong>. 18. <strong>CLI exits non-zero on failure</strong>. 19. <strong>Wet-signature docs tracked out-of-band</strong>. 20. <strong>CLO role required for legal sign</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn triple_sign_required() {\n    let ctx = TestContext::new_tenant().await;\n    let r = ctx.try_init_flip_with(ctx.ceo, ctx.cfo).await;\n    assert!(r.is_err());  // CLO missing\n    let r2 = ctx.init_flip_with(ctx.ceo, ctx.cfo, ctx.clo).await;\n    assert!(r2.is_ok());\n}\n\n#[tokio::test]\nasync fn resume_from_step() {\n    let ctx = TestContext::with_flip_at_step(\"acra_filings_prepared\").await;\n    ctx.resume_flip(ctx.flip_id).await;\n    let steps = ctx.fetch_steps(ctx.flip_id).await;\n    let completed: Vec&lt;_&gt; = steps.iter().filter(|s| s.status == \"completed\").collect();\n    assert!(completed.len() &gt;= 2);  // sg_entity + acra\n}\n\n#[tokio::test]\nasync fn esop_reissue_under_sg() {\n    let ctx = TestContext::with_flip_at_esop_reissue().await;\n    ctx.run_step(\"esop_reissued\").await;\n    let grants = ctx.fetch_grants(ctx.tenant_id).await;\n    assert!(grants.iter().all(|g| g.issuer_entity == \"sg_holdco\"));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-ESOP-001. <strong>Cross-module:</strong> FR-ESOP-006 (acceleration triggers may overlap), FR-TEN-103 (residency migration), FR-AUTH-101 (CEO+CFO+CLO roles), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>One sig missing</td><td>gate</td><td>reject init</td><td>get sig</td></tr><tr><td>Same-person dual</td><td>validate</td><td>403</td><td>different signer</td></tr><tr><td>ACRA API down</td><td>client err</td><td>step=failed</td><td>retry</td></tr><tr><td>ACRA rejects filing</td><td>response</td><td>step=failed</td><td>fix + resubmit</td></tr><tr><td>ESOP re-issue partial</td><td>atomic per grant</td><td>partial; resume</td><td>retry</td></tr><tr><td>Residency migration mid-fail</td><td>rollback</td><td>sev-1</td><td>manual fix</td></tr><tr><td>Wet-sig doc missing</td><td>track separately</td><td>warn + proceed</td><td>upload doc</td></tr><tr><td>Cross-tenant flip</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>Duplicate flip per tenant</td><td>UNIQUE</td><td>409</td><td>inherent</td></tr><tr><td>CLI interrupted</td><td>resume from checkpoint</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 BizFile API integration via Singapore gov OAuth; CTO obtains credentials.</li><li>§11.2 ACRA Form 24 template version-pinned; updates via FR-DOC-001 templates.</li><li>§11.3 ESOP re-issue creates new FR-ESOP-001 grants under SG entity with same vesting terms.</li><li>§11.4 memory audit body: flip_id, step_name, status; output SHA256.</li><li>§11.5 Triple-sign: CLI prompts for each signer's session token; backend verifies.</li></ul>\n<hr>\n<p><em>End of FR-TEN-201 spec.</em></p>";
