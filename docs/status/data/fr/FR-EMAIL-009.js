window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EMAIL-009"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The EMAIL service <strong>MUST</strong> ship outbound 1:1 send at <code>services/email/src/outbound/</code> with confirm-token gate, FR-EMAIL-004 DKIM signing, bounce + complaint handling, per-tenant suppression, rate limit, and 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>send_status</code> enum: <code>('drafting','queued','sent','bounced_hard','bounced_soft','complaint','suppressed')</code> per DEC-1481. Cardinality 7.</li></ol>\n<ol><li><strong>MUST</strong> expose compose <code>POST /v1/email/outbound/compose</code> body <code>{ to, cc, bcc, subject, body_html, body_text, in_reply_to? }</code>. Handler:</li></ol>\n<ul><li>Validates recipients not in suppression list per DEC-1483; suppressed → 412 + suppressed reason.</li><li>Generates <code>confirm_token</code> (UUIDv7) with 5-min TTL; cached in Redis.</li><li>Returns 201 + <code>{ message_id, confirm_token, expires_at }</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose send <code>POST /v1/email/outbound/send</code> body <code>{ message_id, confirm_token }</code>. Handler:</li></ol>\n<ul><li>Validates token + message ownership.</li><li>Rate-limit check per DEC-1485.</li><li>Invokes FR-EMAIL-004 DKIM signer.</li><li>Hands to Stalwart SMTP queue.</li><li>Persists status='queued'.</li><li>Emits <code>email.send_queued</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> handle bounce events from Stalwart per DEC-1481:</li></ol>\n<ul><li>Hard bounce (5xx permanent) → status='bounced_hard' + add recipient to suppression.</li><li>Soft bounce (4xx temporary) → status='bounced_soft' + Stalwart retries up to 3 days.</li><li>Emits respective audit kinds.</li></ul>\n<ol><li><strong>MUST</strong> handle complaint (Feedback Loop from Gmail/Outlook) per DEC-1481 → status='complaint' + add to suppression + emit <code>email.send_complaint</code> sev-1.</li></ol>\n<ol><li><strong>MUST</strong> maintain per-tenant suppression list per DEC-1483 with reasons (hard_bounce | complaint | manual). Manual unsuppress endpoint for engagement_admin.</li></ol>\n<ol><li><strong>MUST</strong> rate-limit 100 sends/hour/Member per DEC-1485 via Redis sliding-window. Excess → 429.</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1484.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> send without confirm_token (DEC-1482).</li></ol>\n<ol><li><strong>MUST NOT</strong> send to suppressed (DEC-1483).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why confirm token (DEC-1482)?</strong> Two-step gate prevents accidental sends. UI shows summary before commit.</p>\n<p><strong>Why suppression list (DEC-1483)?</strong> Repeated sends to hard-bounced addresses = spam-reputation damage. Persistent suppression protects sender reputation.</p>\n<p><strong>Why 100/hour/Member (DEC-1485)?</strong> Legitimate Member sends ~10-30/day. 100/hour catches compromised accounts before significant damage.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0004_outbound_messages.sql\nCREATE TYPE send_status AS ENUM ('drafting','queued','sent','bounced_hard','bounced_soft','complaint','suppressed');\n\nCREATE TABLE outbound_messages (\n  message_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  sender_subject_id UUID NOT NULL,\n  to_addrs TEXT[] NOT NULL,\n  cc_addrs TEXT[],\n  bcc_addrs TEXT[],\n  subject_sha256 CHAR(64) NOT NULL,\n  body_sha256 CHAR(64) NOT NULL,\n  in_reply_to TEXT,\n  status send_status NOT NULL DEFAULT 'drafting',\n  queued_at TIMESTAMPTZ,\n  sent_at TIMESTAMPTZ,\n  bounce_reason TEXT,\n  complaint_reason TEXT,\n  smtp_message_id TEXT,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_outbound_sender ON outbound_messages(sender_subject_id, queued_at DESC);\nALTER TABLE outbound_messages ENABLE ROW LEVEL SECURITY;\nCREATE POLICY outbound_messages_rls ON outbound_messages\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON outbound_messages FROM cyberos_app;\nGRANT UPDATE (status, queued_at, sent_at, bounce_reason, complaint_reason, smtp_message_id)\n  ON outbound_messages TO cyberos_app;\n\n-- 0005_suppression_list.sql\nCREATE TABLE email_suppression (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  recipient_addr_hash16 TEXT NOT NULL,\n  recipient_addr_kms_blob BYTEA NOT NULL,\n  reason TEXT NOT NULL CHECK (reason IN ('hard_bounce','complaint','manual')),\n  suppressed_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  suppressed_by_subject_id UUID,\n  unsuppressed_at TIMESTAMPTZ,\n  UNIQUE (tenant_id, recipient_addr_hash16)\n);\nALTER TABLE email_suppression ENABLE ROW LEVEL SECURITY;\nCREATE POLICY email_suppression_rls ON email_suppression\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON email_suppression FROM cyberos_app;\nGRANT UPDATE (unsuppressed_at) ON email_suppression TO cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/email/outbound/compose\nPOST   /v1/email/outbound/send\nPOST   /v1/admin/email/suppression/unsuppress     (engagement_admin)\nGET    /v1/email/outbound?status=...               (sender or admin)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>send_status cardinality 7</strong>.</li><li><strong>Compose returns confirm_token</strong> valid 5min.</li><li><strong>Send without confirm → 400</strong>.</li><li><strong>Send with valid confirm</strong> → queued.</li><li><strong>DKIM signed before queue</strong> — verified via FR-EMAIL-004.</li><li><strong>Hard bounce adds to suppression</strong> + status=bounced_hard.</li><li><strong>Soft bounce retried</strong> — Stalwart 3-day retry; status=bounced_soft.</li><li><strong>Complaint adds to suppression</strong> + sev-1 audit.</li><li><strong>Suppressed recipient blocked</strong> — compose to suppressed → 412.</li><li><strong>Rate limit 100/hour</strong> — 101st → 429.</li><li><strong>Manual unsuppress</strong> — engagement_admin can re-enable + audit.</li><li><strong>5 memory audit kinds emitted</strong>.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>PII scrub</strong> — subject + body sha256 in chain; recipient hash; raw KMS.</li><li><strong>Cross-tenant RLS denied</strong>.</li><li><strong>In-reply-to preserved</strong> — reply maintains thread.</li><li><strong>Bounce notification to sender</strong> — UI surfaces bounce.</li><li><strong>Confirm token expires 5min</strong> — past TTL → 412.</li><li><strong>Sender required to be Member of tenant</strong>.</li><li><strong>Audit on each transition</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn send_requires_confirm_token() {\n    let ctx = TestContext::with_member().await;\n    let compose = ctx.compose(\"to@example.com\", \"test\", \"body\").await;\n    let msg_id: Uuid = compose.json::&lt;serde_json::Value&gt;().await.unwrap()[\"message_id\"].as_str().unwrap().parse().unwrap();\n    let r = ctx.send_without_token(msg_id).await;\n    assert_eq!(r.status(), 400);\n}\n\n#[tokio::test]\nasync fn hard_bounce_adds_to_suppression() {\n    let ctx = TestContext::with_member().await;\n    let msg_id = ctx.compose_and_send(\"bouncing@example.com\").await;\n    ctx.simulate_hard_bounce(msg_id).await;\n    let suppressed: bool = sqlx::query_scalar(\n        \"SELECT EXISTS(SELECT 1 FROM email_suppression WHERE tenant_id=$1 AND recipient_addr_hash16=$2)\"\n    ).bind(ctx.tenant_id).bind(hash16(\"bouncing@example.com\")).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(suppressed);\n}\n\n#[tokio::test]\nasync fn suppressed_recipient_blocked() {\n    let ctx = TestContext::with_member().await;\n    ctx.add_suppression(ctx.tenant_id, \"blocked@example.com\", \"manual\").await;\n    let r = ctx.compose(\"blocked@example.com\", \"test\", \"body\").await;\n    assert_eq!(r.status(), 412);\n}\n\n#[tokio::test]\nasync fn rate_limit_100_per_hour() {\n    let ctx = TestContext::with_member().await;\n    for _ in 0..100 { ctx.compose_and_send_minimal().await; }\n    let r = ctx.compose(\"more@example.com\", \"test\", \"body\").await;\n    assert_eq!(r.status(), 429);\n}\n\n// 5.5..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-EMAIL-004. <strong>Cross-module:</strong> FR-AUTH-101 (engagement_admin), FR-AI-003, FR-MEMORY-111. <strong>Downstream:</strong> FR-EMAIL-010, FR-EMAIL-011.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Stalwart unavailable</td><td>SMTP error</td><td>Status remains queued; retry</td><td>Stalwart recovery</td></tr><tr><td>Confirm token expired</td><td>TTL check</td><td>412</td><td>Re-compose</td></tr><tr><td>Hard bounce</td><td>Stalwart event</td><td>Suppression + status</td><td>Inherent</td></tr><tr><td>Soft bounce 3-day exceeded</td><td>watchdog</td><td>Final status=bounced_hard</td><td>Inherent</td></tr><tr><td>Complaint via FBL</td><td>feedback consumer</td><td>Suppression + sev-1</td><td>Inherent</td></tr><tr><td>Rate limit</td><td>counter</td><td>429</td><td>Member waits</td></tr><tr><td>Cross-tenant via Member context</td><td>RLS</td><td>403</td><td>Inherent</td></tr><tr><td>DKIM sign fail</td><td>per FR-EMAIL-004</td><td>Status=queued but Stalwart rejects</td><td>Investigate KMS</td></tr><tr><td>Recipient address invalid</td><td>RFC 5321 check</td><td>400</td><td>Inherent</td></tr><tr><td>Body &gt; 25 MiB</td><td>size check</td><td>413</td><td>Inherent</td></tr><tr><td>Compromised Member account</td><td>rate-limit triggers</td><td>Auto-flagged</td><td>Sec team review</td></tr><tr><td>Reply-to thread broken</td><td>in_reply_to invalid</td><td>Allowed; client may not thread</td><td>Inherent</td></tr><tr><td>FBL not configured for ISP</td><td>Microsoft/Yahoo registration</td><td>Complaints not detected for those</td><td>Manual unsubscribe handling</td></tr><tr><td>Suppression list grows unbounded</td><td>tier review at 1M entries</td><td>Indexed lookup remains O(log n)</td><td>Inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Confirm token in Redis with 5-min TTL.</li><li>§11.2 Bounce parsing via <code>mail-parser</code> Rust crate.</li><li>§11.3 Suppression check at compose time (early reject).</li><li>§11.4 Rate limit Redis sliding-window per (sender_id, hour).</li><li>§11.5 In-reply-to header preserved through Stalwart for threading.</li></ul>\n<hr>\n<p><em>End of FR-EMAIL-009 spec.</em></p>";
