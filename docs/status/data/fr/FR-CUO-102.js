window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CUO-102"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CUO service <strong>MUST</strong> ship Postgres checkpointer at <code>services/cuo/src/checkpointer/</code> implementing LangGraph BaseCheckpointer + immutable persistence + EU AI Act Art. 12 logging, 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>checkpoint_kind</code> against closed enum per DEC-2321.</li></ol>\n<ol><li><strong>MUST</strong> persist at <code>postgres_writer.rs::write(run_id, kind, state)</code> per DEC-2320:</li></ol>\n<ul><li>Each node entered + completed; each edge traversed</li><li>Full state JSON serialized via <code>state_serializer.rs</code></li><li>Trace_id from FR-CUO-101 supervisor context</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0002</code>: ``<code>sql CREATE TABLE cuo_langgraph_checkpoints ( checkpoint_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, run_id UUID NOT NULL, parent_checkpoint_id UUID REFERENCES cuo_langgraph_checkpoints(checkpoint_id), node_name TEXT, edge_from TEXT, edge_to TEXT, kind TEXT NOT NULL CHECK (kind IN ('node_entered','node_completed','edge_traversed','run_started','run_completed','run_failed')), state_json JSONB NOT NULL, trace_id CHAR(32) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now() ) PARTITION BY RANGE (created_at); CREATE INDEX checkpoints_run_idx ON cuo_langgraph_checkpoints(tenant_id, run_id, created_at); ALTER TABLE cuo_langgraph_checkpoints ENABLE ROW LEVEL SECURITY; CREATE POLICY checkpoints_rls ON cuo_langgraph_checkpoints USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON cuo_langgraph_checkpoints FROM cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> archive monthly partitions per DEC-2323 — older than 7 years can be safely removed via DROP PARTITION.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text GET /v1/cuo/runs/{run_id}/checkpoints   (FR-CUO-103 replay reads these) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-2324. PII per FR-MEMORY-111: state_json hashed; ids ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from supervisor → writer → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior checkpoint per DEC-2323 (REVOKE UPDATE/DELETE).</li></ol>\n<ol><li><strong>MUST NOT</strong> skip persistence per DEC-2320 (Art. 12 compliance).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why Postgres (DEC-2320)?</strong> Already in stack; transactional; supports JSONB for state.</p>\n<p><strong>Why per-node checkpoint (DEC-2320)?</strong> Resume from last successful node on crash; replay to investigate failures.</p>\n<p><strong>Why partition (DEC-2323)?</strong> 7-year retention → millions of rows; partition by month enables fast drop.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample checkpoint:</p>\n<pre><code class=\"language-json\">{\n  \"checkpoint_id\": \"uuid\",\n  \"run_id\": \"uuid\",\n  \"node_name\": \"router\",\n  \"kind\": \"node_completed\",\n  \"state_json\": {\"selected_skill\": \"calendar.list_events\", ...},\n  \"trace_id\": \"abcdef...\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>checkpoint_kind enum cardinality 6</strong>. 2. <strong>LangGraph BaseCheckpointer interface implemented</strong>. 3. <strong>Per-node + per-edge persistence</strong>. 4. <strong>State JSON serialized</strong>. 5. <strong>trace_id captured</strong>. 6. <strong>3 memory audit kinds emitted</strong>. 7. <strong>PII scrubbed (state_json SHA256)</strong>. 8. <strong>RLS denies cross-tenant</strong>. 9. <strong>Trace_id preserved</strong>. 10. <strong>Append-only via REVOKE UPDATE/DELETE</strong>. 11. <strong>Monthly partitioning</strong>. 12. <strong>7-year retention</strong>. 13. <strong>Archive cron via DROP PARTITION</strong>. 14. <strong>Replay roundtrip OK</strong>. 15. <strong>Performance &lt; 5ms per checkpoint</strong>. 16. <strong>Run-scoped query indexed</strong>. 17. <strong>Parent_checkpoint_id forms DAG</strong>. 18. <strong>EU AI Act Art. 12 documented</strong>. 19. <strong>Concurrent checkpoint OK</strong>. 20. <strong>State size capped 5MB per checkpoint</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn checkpoint_immutable() {\n    let ctx = TestContext::with_checkpoint().await;\n    let r = ctx.try_update_checkpoint(ctx.cp_id).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn serialization_roundtrip() {\n    let state = json!({\"key\": \"value\", \"nested\": {\"a\": 1}});\n    let serialized = state_serializer::serialize(&amp;state);\n    let restored = state_serializer::deserialize(&amp;serialized);\n    assert_eq!(state, restored);\n}\n\n#[tokio::test]\nasync fn per_node_checkpoint_count() {\n    let ctx = TestContext::with_supervisor_run_5_nodes().await;\n    let cps = ctx.fetch_checkpoints(ctx.run_id).await;\n    assert!(cps.len() &gt;= 5 * 2);  // entered + completed per node\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-CUO-101. <strong>Downstream:</strong> FR-CUO-103 (replay uses checkpoints). <strong>Cross-module:</strong> FR-MEMORY-111 (PII), FR-MCP-007 (archive cron).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>State &gt; 5MB</td><td>validate</td><td>reject; sev-2</td><td>reduce state</td></tr><tr><td>Serialization fail</td><td>catch</td><td>sev-1; run halts</td><td>bug fix</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Mutation attempt</td><td>REVOKE</td><td>DB error</td><td>inherent</td></tr><tr><td>Partition table count grows</td><td>monthly partitions</td><td>OK</td><td>inherent</td></tr><tr><td>Archive cron fail</td><td>sev-2</td><td>data retained longer</td><td>retry</td></tr><tr><td>Concurrent checkpoint</td><td>UNIQUE on (run, time) NOT needed (append)</td><td>inherent</td><td>inherent</td></tr><tr><td>State JSON invalid</td><td>validate</td><td>reject; sev-1</td><td>bug fix</td></tr><tr><td>Trace_id missing</td><td>sev-2</td><td>use NIL_UUID</td><td>bug fix</td></tr><tr><td>Decimal precision N/A</td><td>inherent</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 BaseCheckpointer trait per LangGraph spec: <code>aput()</code>, <code>aget_tuple()</code>, <code>alist()</code>.</li><li>§11.2 Partition strategy: monthly RANGE on created_at; drop &gt; 84 months old (7 years).</li><li>§11.3 memory audit body: run_id, node_name, kind; state_json SHA256.</li><li>§11.4 Archive cron via FR-MCP-007 monthly; DROP PARTITION for oldest.</li><li>§11.5 EU AI Act Art. 12: high-risk AI systems must keep automatic logs; this satisfies it.</li></ul>\n<hr>\n<p><em>End of FR-CUO-102 spec.</em></p>";
