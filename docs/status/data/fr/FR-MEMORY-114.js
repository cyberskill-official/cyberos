window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-114"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The write-time importance scoring layer is an <strong>optional preprocessor</strong> sitting between <code>cyberos put</code> invocation and the canonical Writer. When opted-in via <code>--score-importance</code>, it asks a small/cheap LLM to rate the candidate content on a <code>[0.0, 1.0]</code> scale, merges the result into <code>meta.importance</code> on the frontmatter, then proceeds with the write. The contract:</p>\n<ol><li><strong>MUST</strong> be <strong>optional</strong>, opt-in via <code>cyberos put --score-importance</code>. Default <code>cyberos put</code> writes MUST NOT call any LLM and MUST NOT touch the network (DEC-200). This preserves the offline-first guarantee of the memory.</li><li><strong>MUST</strong> allow the operator to explicitly override the scored value via <code>--importance &lt;float&gt;</code> on the same <code>cyberos put</code> invocation. Both flags together: <code>--importance</code> wins; no LLM call is made (saves cost when the operator already knows).</li><li><strong>MUST</strong> select the active Invoker through the same priority chain as the CUO supervisor (Phase 3):</li><li>CLI flag <code>--invoker {mock|anthropic}</code> (highest priority)</li><li>Env var <code>CYBEROS_IMPORTANCE_INVOKER</code></li><li><code>manifest.json:importance.invoker</code> (string enum <code>mock | anthropic</code>)</li><li>Default <code>\"mock\"</code> when no API key is detected; <code>\"anthropic\"</code> when <code>ANTHROPIC_API_KEY</code> is set in env</li><li><strong>MUST</strong> treat <code>CYBEROS_DISABLE_LLM=1</code> (env) as a hard override forcing <code>MockInvoker</code> regardless of any other setting. This is the offline / CI / air-gapped escape hatch.</li><li><strong>MUST</strong> define <code>ImportanceInvoker</code> as a Protocol with method <code>async def score(content: str) -&gt; ScoreResult</code> returning the typed record: ``<code>python @dataclass(frozen=True) class ScoreResult: score:       float      # in [0.0, 1.0] latency_ms:  int model:       str        # \"mock\" or e.g. \"claude-haiku-4-5-20251001\" outcome:     Literal[\"ok\", \"fallback\"] reason:      Optional[str]   # populated when outcome == \"fallback\" </code>``</li><li><strong>MUST</strong> cache results keyed on <code>sha256(content)</code>. Cache hit ⇒ no invoker call; cache miss ⇒ invoker call ⇒ result stored. Cache lives at <code>&lt;memory-root&gt;/index/importance_cache.db</code> (SQLite). Cache invalidation is automatic via sha256 mismatch (DEC-203). The cache table schema and the <code>importance-cache-valid-sha256</code> invariant guard against partial writes.</li><li><strong>MUST</strong> clamp invoker output to <code>[0.0, 1.0]</code>. Out-of-range responses (e.g. LLM returns \"1.5\" or \"-0.1\") are clamped silently — they don't constitute a fallback, just bounded sanitisation. Logged at DEBUG level.</li><li><strong>MUST</strong> emit one <code>memory.importance_scored</code> audit row per scoring invocation (whether opt-in flag was set or not — even cache hits emit). Payload: ``<code>json { \"kind\": \"memory.importance_scored\", \"payload\": { \"path\":         \"memories/facts/x.md\", \"content_sha256\": \"abc123…\", \"score\":        0.72, \"model\":        \"claude-haiku-4-5-20251001\", \"outcome\":      \"ok\", \"reason\":       null, \"cache_hit\":    false, \"latency_ms\":   240 } } </code>``</li><li><strong>MUST</strong> fall back to <code>0.5</code> (neutral) on any of: invoker timeout (≥ 5 s), API error, response parse failure, missing API key (when <code>anthropic</code> invoker selected), exception in invoker. The fallback path emits <code>outcome: \"fallback\"</code> with a concrete <code>reason</code> string. Memory is still written with <code>meta.importance: 0.5</code> (so downstream ranking is non-degenerate).</li><li><strong>MUST</strong> support <code>--dry-run</code> on <code>cyberos put --score-importance</code>: print the would-be <code>meta.importance</code> value + the full frontmatter as it would be written, but do not actually write anything to disk. Useful for operators evaluating score quality on a single file.</li><li><strong>MUST</strong> use the exact prompt from Ramakrushna's article (verbatim) as the AnthropicInvoker's system prompt: ```text Rate the importance of saving this for future interactions. 0.0 = trivial (greeting) 0.5 = moderately useful 1.0 = critical (preferences, errors, decisions)</li></ol>\n<p>    Information: &lt;content&gt;     Reply with ONLY the number.     ``<code>     The response is parsed with regex </code>r\"[-+]?\\d*\\.\\d+|\\d+\"` → first match → float. If parse fails, fallback.</p>\n<ol><li><strong>MUST</strong> complete a single scoring call in ≤ 5 seconds (invoker timeout). On timeout, fallback. On AnthropicInvoker rate-limit (HTTP 429), retry once with 1-second jitter then fall back.</li><li><strong>MUST</strong> validate <code>manifest.json:importance</code> block at writer construction time (same fail-fast pattern as FR-MEMORY-113 §1 #9). Malformed → <code>ManifestError</code>.</li><li><strong>SHOULD</strong> offer a batch-scoring mode <code>cyberos importance score-all [--filter kind=facts] [--unscored-only]</code> that walks existing memories and scores any without <code>meta.importance</code>. Slice-4 stretch — not required for FR-MEMORY-115 to consume scores.</li><li><strong>SHOULD</strong> expose <code>cyberos importance stats</code> for operator introspection: histogram of scores over the store, % of memories with absent vs scored importance, cache hit rate, fallback rate. Slice-4 stretch.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why opt-in, not default-on (§1 #1, DEC-200).</strong> The memory is offline-first. A user dropping in <code>AGENTS.md</code> on a corporate laptop without API access must continue to work. Default-on importance scoring would silently fail or block writes for anyone without an API key. Opt-in keeps the simple case simple; sophisticated users who want quality scoring enable it explicitly.</p>\n<p><strong>Why CUO Phase-3 invoker pattern (§1 #3, DEC-201).</strong> The CUO supervisor already implements this exact pattern — <code>MockInvoker</code> for tests, <code>AnthropicInvoker</code> for prod, env-or-manifest selection, <code>CYBEROS_DISABLE_LLM</code> escape hatch. Reusing it: (a) one mental model across the codebase; (b) the test infrastructure already exists; (c) operators who know CUO know this; (d) future LLM providers (OpenAI, local Ollama, etc.) plug in as new Invoker classes without changing call sites.</p>\n<p><strong>Why <code>claude-haiku-4-5</code> default (DEC-202).</strong> Importance scoring is a one-token-out task. Haiku is the cheapest fast model that reliably outputs structured floats. Sonnet/Opus would work but cost 10-30× more per call. For a 1000-memory new project, this is the difference between a few cents and a dollar per scoring pass.</p>\n<p><strong>Why cache on <code>sha256(content)</code> (§1 #6, DEC-203).</strong> The same content scored twice is wasted spend. Cache eliminates duplicate calls on (a) idempotent re-writes (same put with same body), (b) the slice-4 <code>score-all</code> batch pass, (c) <code>--dry-run</code> invocations that the operator iterates on. SHA-256 keying makes content-equality the cache primitive — a one-character whitespace change is a different score (because the model might judge it differently), so we don't accidentally return stale scores.</p>\n<p><strong>Why clamp out-of-range silently (§1 #7).</strong> LLMs occasionally drift outside <code>[0, 1]</code> (especially with the literal \"Reply with ONLY the number\" prompt — Haiku might say \"0.95-1.0\" or \"around 0.6\"). Clamping recovers the signal without treating it as a fallback. Logging at DEBUG lets operators audit drift without polluting normal logs.</p>\n<p><strong>Why fallback to 0.5, not refuse to write (§1 #9, DEC-204).</strong> Refusing to write would conflate \"I can't score this\" with \"I can't store this\". The operator's intent is the write; importance is the optional metadata. 0.5 (neutral) lets the write proceed without falsely up- or down-ranking the memory in future recall. The <code>memory.importance_scored</code> audit row with <code>outcome: \"fallback\"</code> and a <code>reason</code> preserves the diagnostic signal for FR-MEMORY-115 dream to learn from (\"hey, 30% of imports last week were fallback — check API status\").</p>\n<p><strong>Why exact Ramakrushna prompt (§1 #11).</strong> The article's prompt is calibrated: 0.0 / 0.5 / 1.0 anchored to concrete examples. Reinventing the prompt is wasted effort and risks miscalibration vs. the published baseline. Keeping the prompt verbatim also means we can swap LLM providers (OpenAI, local) without changing the prompt — the prompt is the contract, not the model.</p>\n<p><strong>Why 5-second timeout (§1 #12).</strong> Haiku p95 is ~1.5 s. 5 s is 3× margin — catches network hiccups, rate-limit waits, etc. Beyond 5 s we fall back rather than block the write. Blocking puts on LLM latency would degrade the user's perceived memory responsiveness.</p>\n<p><strong>Why emit <code>memory.importance_scored</code> even on cache hits (§1 #8).</strong> FR-MEMORY-115's dream pipeline counts cache hit rate when evaluating \"is the importance signal actually being used?\". If we suppress cache-hit audit rows, dream can't tell whether a low-import store has 1000 misses or 1000 hits. Audit rows are cheap; the signal is valuable.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Invoker Protocol + implementations</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/invokers/base.py\nfrom __future__ import annotations\nfrom dataclasses import dataclass\nfrom typing import Literal, Optional, Protocol\n\n\n@dataclass(frozen=True)\nclass ScoreResult:\n    score:      float\n    latency_ms: int\n    model:      str\n    outcome:    Literal[\"ok\", \"fallback\"]\n    reason:     Optional[str] = None\n\n\nclass ImportanceInvoker(Protocol):\n    async def score(self, content: str) -&gt; ScoreResult: ...</code></pre>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/invokers/mock.py\nimport hashlib, time\nfrom .base import ImportanceInvoker, ScoreResult\n\n\nclass MockInvoker:\n    \"\"\"Deterministic, offline. Score derived from sha256(content) so the same\n    content always gets the same mock score (good for tests + cache validation).\"\"\"\n\n    async def score(self, content: str) -&gt; ScoreResult:\n        t0 = time.perf_counter()\n        h = hashlib.sha256(content.encode(\"utf-8\")).hexdigest()\n        raw = int(h[:8], 16) / 0xFFFFFFFF        # [0.0, 1.0]\n        score = 0.1 + raw * 0.85                  # clamp to [0.1, 0.95] so we never hit literal extremes\n        latency_ms = int((time.perf_counter() - t0) * 1000)\n        return ScoreResult(score=score, latency_ms=latency_ms, model=\"mock\", outcome=\"ok\", reason=None)</code></pre>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/invokers/anthropic_invoker.py\nimport os, re, time\nfrom .base import ImportanceInvoker, ScoreResult\n\nSYSTEM_PROMPT = \"\"\"Rate the importance of saving this for future interactions.\n0.0 = trivial (greeting)\n0.5 = moderately useful\n1.0 = critical (preferences, errors, decisions)\n\nInformation: {content}\nReply with ONLY the number.\"\"\"\n\n_FLOAT_RE = re.compile(r\"[-+]?\\d*\\.\\d+|\\d+\")\n\n\nclass AnthropicInvoker:\n    def __init__(self, model: str = \"claude-haiku-4-5\", timeout_s: float = 5.0) -&gt; None:\n        try:\n            import anthropic                                   # noqa: F401\n        except ImportError as e:                                # graceful per CUO Phase-3 pattern\n            raise RuntimeError(\n                \"AnthropicInvoker selected but the `anthropic` package is not installed.\\n\"\n                \"Install with `pip install anthropic` or set CYBEROS_IMPORTANCE_INVOKER=mock\"\n            ) from e\n        if not os.environ.get(\"ANTHROPIC_API_KEY\"):\n            raise RuntimeError(\n                \"AnthropicInvoker selected but ANTHROPIC_API_KEY is unset.\\n\"\n                \"Export the key or set CYBEROS_DISABLE_LLM=1\"\n            )\n        self._model = model\n        self._timeout_s = timeout_s\n\n    async def score(self, content: str) -&gt; ScoreResult:\n        import anthropic, asyncio\n        client = anthropic.AsyncAnthropic()\n        t0 = time.perf_counter()\n        try:\n            resp = await asyncio.wait_for(\n                client.messages.create(\n                    model=self._model,\n                    max_tokens=8,\n                    system=SYSTEM_PROMPT.format(content=content),\n                    messages=[{\"role\": \"user\", \"content\": \"Rate this.\"}],\n                ),\n                timeout=self._timeout_s,\n            )\n        except asyncio.TimeoutError:\n            return ScoreResult(score=0.5, latency_ms=int(self._timeout_s * 1000),\n                               model=self._model, outcome=\"fallback\", reason=\"timeout\")\n        except Exception as e:\n            return ScoreResult(score=0.5, latency_ms=int((time.perf_counter() - t0) * 1000),\n                               model=self._model, outcome=\"fallback\", reason=f\"api_error:{type(e).__name__}\")\n        text = resp.content[0].text.strip()\n        m = _FLOAT_RE.search(text)\n        if not m:\n            return ScoreResult(score=0.5, latency_ms=int((time.perf_counter() - t0) * 1000),\n                               model=self._model, outcome=\"fallback\", reason=f\"parse_error:{text!r}\")\n        try:\n            v = float(m.group())\n        except ValueError:\n            return ScoreResult(score=0.5, latency_ms=int((time.perf_counter() - t0) * 1000),\n                               model=self._model, outcome=\"fallback\", reason=f\"parse_error:{m.group()!r}\")\n        clamped = max(0.0, min(1.0, v))\n        return ScoreResult(score=clamped, latency_ms=int((time.perf_counter() - t0) * 1000),\n                           model=self._model, outcome=\"ok\", reason=None)</code></pre>\n<h3>Orchestrator + cache</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/importance.py\nimport hashlib, sqlite3, time\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Optional\n\nfrom cyberos.core.invokers.base import ImportanceInvoker, ScoreResult\n\n\ndef select_invoker(name: Optional[str] = None) -&gt; ImportanceInvoker:\n    import os\n    from cyberos.core.invokers.mock import MockInvoker\n\n    if os.environ.get(\"CYBEROS_DISABLE_LLM\") == \"1\":\n        return MockInvoker()                                   # §1 #4 escape hatch\n    name = name or os.environ.get(\"CYBEROS_IMPORTANCE_INVOKER\") or _default_from_env()\n    if name == \"mock\":\n        return MockInvoker()\n    if name == \"anthropic\":\n        from cyberos.core.invokers.anthropic_invoker import AnthropicInvoker\n        return AnthropicInvoker()\n    raise ValueError(f\"unknown invoker {name!r}; expected one of: mock, anthropic\")\n\n\ndef _default_from_env() -&gt; str:\n    import os\n    return \"anthropic\" if os.environ.get(\"ANTHROPIC_API_KEY\") else \"mock\"\n\n\nclass ImportanceCache:\n    def __init__(self, db_path: Path):\n        db_path.parent.mkdir(parents=True, exist_ok=True)\n        self._con = sqlite3.connect(str(db_path), isolation_level=None)\n        self._con.execute(\"\"\"\n            CREATE TABLE IF NOT EXISTS importance_cache (\n                content_sha256 BLOB PRIMARY KEY,\n                score          REAL NOT NULL,\n                model          TEXT NOT NULL,\n                scored_at_ns   INTEGER NOT NULL\n            )\n        \"\"\")\n\n    def get(self, content_sha256: bytes) -&gt; Optional[float]:\n        cur = self._con.execute(\n            \"SELECT score FROM importance_cache WHERE content_sha256 = ?\", (content_sha256,)\n        )\n        row = cur.fetchone()\n        return row[0] if row else None\n\n    def put(self, content_sha256: bytes, score: float, model: str) -&gt; None:\n        self._con.execute(\n            \"INSERT OR REPLACE INTO importance_cache (content_sha256, score, model, scored_at_ns) VALUES (?, ?, ?, ?)\",\n            (content_sha256, score, model, time.time_ns()),\n        )\n\n\nasync def score(content: str, invoker: ImportanceInvoker, cache: Optional[ImportanceCache] = None,\n                aux_emitter=None, path: str = \"\") -&gt; ScoreResult:\n    h = hashlib.sha256(content.encode(\"utf-8\")).digest()\n    if cache is not None:\n        cached = cache.get(h)\n        if cached is not None:\n            res = ScoreResult(score=cached, latency_ms=0, model=\"cache\", outcome=\"ok\", reason=None)\n            if aux_emitter:\n                aux_emitter(kind=\"memory.importance_scored\",\n                            payload={\"path\": path, \"content_sha256\": h.hex(),\n                                     \"score\": cached, \"model\": \"cache\",\n                                     \"outcome\": \"ok\", \"reason\": None, \"cache_hit\": True,\n                                     \"latency_ms\": 0})\n            return res\n    res = await invoker.score(content)\n    if cache is not None and res.outcome == \"ok\":\n        cache.put(h, res.score, res.model)\n    if aux_emitter:\n        aux_emitter(kind=\"memory.importance_scored\",\n                    payload={\"path\": path, \"content_sha256\": h.hex(),\n                             \"score\": res.score, \"model\": res.model,\n                             \"outcome\": res.outcome, \"reason\": res.reason,\n                             \"cache_hit\": False, \"latency_ms\": res.latency_ms})\n    return res</code></pre>\n<h3>Manifest</h3>\n<pre><code class=\"language-json\">// .cyberos/memory/store/manifest.json (excerpt)\n{\n  \"importance\": {\n    \"invoker\":  \"anthropic\",\n    \"model\":    \"claude-haiku-4-5\",\n    \"timeout_s\": 5.0\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Opt-in default-off</strong> — <code>cyberos put memories/x.md - &lt; file</code> does NOT call any invoker; <code>cache.get_call_count() == 0</code> after the call. <em>(traces_to: §1 #1, DEC-200)</em></li><li><strong>Opt-in flag works</strong> — <code>cyberos put memories/x.md - --score-importance &lt; file</code> produces a memory file with <code>meta.importance</code> set to a float in <code>[0.0, 1.0]</code>. <em>(traces_to: §1 #1)</em></li><li><strong>Explicit override beats LLM</strong> — <code>cyberos put memories/x.md - --score-importance --importance 0.9 &lt; file</code> produces <code>meta.importance: 0.9</code>; invoker was NOT called (call count 0). <em>(traces_to: §1 #2)</em></li><li><strong>Invoker selection — CLI flag wins</strong> — <code>--invoker anthropic</code> with no <code>CYBEROS_IMPORTANCE_INVOKER</code> set → AnthropicInvoker selected. <em>(traces_to: §1 #3)</em></li><li><strong>Invoker selection — env wins over manifest</strong> — <code>CYBEROS_IMPORTANCE_INVOKER=mock</code> with <code>manifest.importance.invoker=anthropic</code> → MockInvoker. <em>(traces_to: §1 #3)</em></li><li><strong>Invoker selection — manifest wins over default</strong> — manifest sets <code>anthropic</code>; no env, no CLI flag → AnthropicInvoker. <em>(traces_to: §1 #3)</em></li><li><strong><code>CYBEROS_DISABLE_LLM=1</code> forces mock</strong> — env set, manifest says anthropic, CLI says anthropic → MockInvoker still selected. <em>(traces_to: §1 #4, DEC-201)</em></li><li><strong>Default fallback when no API key</strong> — no env vars, no manifest, no key → <code>select_invoker()</code> returns <code>MockInvoker</code>. <em>(traces_to: §1 #3)</em></li><li><strong>MockInvoker deterministic</strong> — <code>MockInvoker().score(\"X\")</code> returns same score across two calls. <em>(traces_to: §1 #5)</em></li><li><strong>ScoreResult shape</strong> — <code>score()</code> returns <code>ScoreResult</code> with <code>score</code>, <code>latency_ms</code>, <code>model</code>, <code>outcome</code>, <code>reason</code> fields. <em>(traces_to: §1 #5)</em></li><li><strong>Cache hit returns cached score, no invoker call</strong> — <code>score(content, mock, cache)</code> twice → second call has <code>model: \"cache\"</code>; mock call count = 1. <em>(traces_to: §1 #6, DEC-203)</em></li><li><strong>Cache invalidates on content change</strong> — same path but different body → cache miss; invoker called. <em>(traces_to: §1 #6)</em></li><li><strong>Cache survives Writer restart</strong> — write twice with same content, restart Writer between → second call is cache hit. <em>(traces_to: §1 #6)</em></li><li><strong>Clamp out-of-range LLM output</strong> — AnthropicInvoker mocked to return \"1.5\" → returned <code>score == 1.0</code>; mocked to return \"-0.1\" → <code>score == 0.0</code>. <em>(traces_to: §1 #7)</em></li><li><strong><code>memory.importance_scored</code> audit row emitted</strong> — per call (incl. cache hits); payload matches schema in §1 #8. <em>(traces_to: §1 #8)</em></li><li><strong>Audit row <code>cache_hit</code> field accurate</strong> — cache miss → <code>cache_hit: false</code>; cache hit → <code>cache_hit: true</code>. <em>(traces_to: §1 #8)</em></li><li><strong>Fallback on timeout</strong> — AnthropicInvoker mocked to sleep 6s, timeout=5s → ScoreResult <code>score=0.5, outcome=\"fallback\", reason=\"timeout\"</code>. <em>(traces_to: §1 #9, DEC-204)</em></li><li><strong>Fallback on parse error</strong> — AnthropicInvoker mocked to return \"I think it's quite important\" (no float) → fallback. <em>(traces_to: §1 #9, §1 #11)</em></li><li><strong>Fallback on missing API key</strong> — AnthropicInvoker selected but <code>ANTHROPIC_API_KEY</code> unset → constructor raises with a structured message naming the env var. <em>(traces_to: §1 #3, §1 #9)</em></li><li><strong><code>--dry-run</code> writes nothing</strong> — <code>cyberos put ... --score-importance --dry-run</code> produces no audit row, no memory file, but prints the frontmatter to stdout. <em>(traces_to: §1 #10)</em></li><li><strong>Verbatim Ramakrushna prompt</strong> — AnthropicInvoker's system prompt text equals the literal §1 #11 string. <em>(traces_to: §1 #11)</em></li><li><strong>Timeout default = 5 s</strong> — <code>AnthropicInvoker()._timeout_s == 5.0</code>. <em>(traces_to: §1 #12)</em></li><li><strong>Manifest validation at construction</strong> — <code>manifest.importance.invoker = \"made_up\"</code> → <code>Writer(...)</code> raises ManifestError. <em>(traces_to: §1 #13)</em></li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_importance.py\nimport pytest, asyncio\nfrom unittest.mock import patch, AsyncMock, MagicMock\n\nfrom cyberos.core.invokers.mock import MockInvoker\nfrom cyberos.core.importance     import score\n\n\nasync def _run(coro): return await coro\n\n\ndef test_mock_invoker_deterministic():\n    \"\"\"AC #9\"\"\"\n    inv = MockInvoker()\n    r1 = asyncio.run(inv.score(\"X\"))\n    r2 = asyncio.run(inv.score(\"X\"))\n    assert r1.score == r2.score\n\n\ndef test_mock_score_in_clamped_range():\n    \"\"\"AC #10 — score in [0.1, 0.95] sane mock range\"\"\"\n    inv = MockInvoker()\n    r = asyncio.run(inv.score(\"anything\"))\n    assert 0.1 &lt;= r.score &lt;= 0.95\n\n\n@pytest.mark.parametrize(\"text,expected_score,outcome\", [\n    (\"0.85\",                 0.85, \"ok\"),\n    (\"0.85.\",                0.85, \"ok\"),\n    (\"1.5\",                  1.0,  \"ok\"),     # AC #14: clamp\n    (\"-0.1\",                 0.0,  \"ok\"),     # AC #14\n    (\"I think it's quite important\", 0.5, \"fallback\"),  # AC #18\n])\ndef test_anthropic_invoker_parse_paths(text, expected_score, outcome):\n    \"\"\"AC #14, #18\"\"\"\n    from cyberos.core.invokers.anthropic_invoker import AnthropicInvoker\n    inv = AnthropicInvoker.__new__(AnthropicInvoker)    # bypass __init__\n    inv._model = \"claude-haiku-4-5-test\"\n    inv._timeout_s = 5.0\n    fake_resp = MagicMock()\n    fake_resp.content = [MagicMock(text=text)]\n    with patch.object(AnthropicInvoker, \"score\",\n                     AsyncMock(side_effect=lambda content: AnthropicInvoker._real_score(inv, content, fake_resp))):\n        # ... (test scaffold simplified for readability; real test calls the real `score` with the mocked HTTP call)\n        pass\n\n\ndef test_fallback_on_timeout():\n    \"\"\"AC #17\"\"\"\n    from cyberos.core.invokers.anthropic_invoker import AnthropicInvoker\n    inv = AnthropicInvoker.__new__(AnthropicInvoker)\n    inv._model = \"claude-haiku-4-5-test\"\n    inv._timeout_s = 0.001     # ridiculous\n    # Patch asyncio.wait_for to raise TimeoutError\n    with patch(\"asyncio.wait_for\", side_effect=asyncio.TimeoutError):\n        r = asyncio.run(inv.score(\"X\"))\n    assert r.outcome == \"fallback\"\n    assert r.reason == \"timeout\"\n    assert r.score == 0.5\n\n\ndef test_audit_row_emitted_on_cache_hit(tmp_memory, capsys_emitter):\n    \"\"\"AC #15 + #16\"\"\"\n    from cyberos.core.importance import ImportanceCache\n    cache = ImportanceCache(tmp_memory.store_path / \"index/importance_cache.db\")\n    inv = MockInvoker()\n    rows = []\n    def emit(kind, payload): rows.append((kind, payload))\n    r1 = asyncio.run(score(\"X\", inv, cache, aux_emitter=emit, path=\"memories/x.md\"))\n    r2 = asyncio.run(score(\"X\", inv, cache, aux_emitter=emit, path=\"memories/x.md\"))\n    assert len(rows) == 2\n    assert rows[0][1][\"cache_hit\"] is False\n    assert rows[1][1][\"cache_hit\"] is True\n\n\ndef test_verbatim_ramakrushna_prompt():\n    \"\"\"AC #21\"\"\"\n    from cyberos.core.invokers.anthropic_invoker import SYSTEM_PROMPT\n    expected_prefix = \"Rate the importance of saving this for future interactions.\\n0.0 = trivial (greeting)\"\n    assert SYSTEM_PROMPT.startswith(expected_prefix)\n    assert \"Reply with ONLY the number.\" in SYSTEM_PROMPT\n\n\ndef test_default_timeout_5_seconds():\n    \"\"\"AC #22\"\"\"\n    from cyberos.core.invokers.anthropic_invoker import AnthropicInvoker\n    import os\n    os.environ.setdefault(\"ANTHROPIC_API_KEY\", \"test\")\n    inv = AnthropicInvoker()\n    assert inv._timeout_s == 5.0</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_importance.py\nimport pytest, os\nfrom cyberos.core.importance import select_invoker\n\n\ndef test_disable_llm_env_forces_mock(monkeypatch):\n    \"\"\"AC #7\"\"\"\n    monkeypatch.setenv(\"CYBEROS_DISABLE_LLM\", \"1\")\n    monkeypatch.setenv(\"CYBEROS_IMPORTANCE_INVOKER\", \"anthropic\")\n    inv = select_invoker(\"anthropic\")\n    assert inv.__class__.__name__ == \"MockInvoker\"\n\n\ndef test_cli_flag_wins(monkeypatch):\n    \"\"\"AC #4\"\"\"\n    monkeypatch.delenv(\"CYBEROS_DISABLE_LLM\", raising=False)\n    monkeypatch.delenv(\"CYBEROS_IMPORTANCE_INVOKER\", raising=False)\n    monkeypatch.setenv(\"ANTHROPIC_API_KEY\", \"test\")\n    inv = select_invoker(\"mock\")          # explicit override\n    assert inv.__class__.__name__ == \"MockInvoker\"\n\n\ndef test_env_var(monkeypatch):\n    \"\"\"AC #5\"\"\"\n    monkeypatch.delenv(\"CYBEROS_DISABLE_LLM\", raising=False)\n    monkeypatch.setenv(\"CYBEROS_IMPORTANCE_INVOKER\", \"mock\")\n    monkeypatch.setenv(\"ANTHROPIC_API_KEY\", \"test\")\n    inv = select_invoker()\n    assert inv.__class__.__name__ == \"MockInvoker\"\n\n\ndef test_default_without_api_key(monkeypatch):\n    \"\"\"AC #8\"\"\"\n    monkeypatch.delenv(\"CYBEROS_DISABLE_LLM\", raising=False)\n    monkeypatch.delenv(\"CYBEROS_IMPORTANCE_INVOKER\", raising=False)\n    monkeypatch.delenv(\"ANTHROPIC_API_KEY\", raising=False)\n    inv = select_invoker()\n    assert inv.__class__.__name__ == \"MockInvoker\"\n\n\ndef test_unknown_invoker_raises(monkeypatch):\n    monkeypatch.delenv(\"CYBEROS_DISABLE_LLM\", raising=False)\n    with pytest.raises(ValueError):\n        select_invoker(\"made_up\")\n\n\ndef test_missing_api_key_for_anthropic(monkeypatch):\n    \"\"\"AC #19\"\"\"\n    monkeypatch.delenv(\"CYBEROS_DISABLE_LLM\", raising=False)\n    monkeypatch.delenv(\"ANTHROPIC_API_KEY\", raising=False)\n    with pytest.raises(RuntimeError, match=\"ANTHROPIC_API_KEY\"):\n        select_invoker(\"anthropic\")</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_importance.py\nimport asyncio, hashlib\nfrom cyberos.core.importance import ImportanceCache, score\nfrom cyberos.core.invokers.mock import MockInvoker\n\n\ndef test_cache_hit_returns_same_score(tmp_path):\n    \"\"\"AC #11\"\"\"\n    cache = ImportanceCache(tmp_path / \"cache.db\")\n    inv = MockInvoker()\n    r1 = asyncio.run(score(\"X\", inv, cache))\n    r2 = asyncio.run(score(\"X\", inv, cache))\n    assert r1.score == r2.score\n    assert r2.model == \"cache\"\n\n\ndef test_cache_invalidates_on_content_change(tmp_path):\n    \"\"\"AC #12\"\"\"\n    cache = ImportanceCache(tmp_path / \"cache.db\")\n    inv = MockInvoker()\n    r1 = asyncio.run(score(\"X\", inv, cache))\n    r2 = asyncio.run(score(\"Y\", inv, cache))\n    assert r1.score != r2.score\n    assert r2.model != \"cache\"\n\n\ndef test_cache_survives_restart(tmp_path):\n    \"\"\"AC #13\"\"\"\n    cache1 = ImportanceCache(tmp_path / \"cache.db\")\n    inv = MockInvoker()\n    r1 = asyncio.run(score(\"X\", inv, cache1))\n    cache1 = None\n    cache2 = ImportanceCache(tmp_path / \"cache.db\")\n    r2 = asyncio.run(score(\"X\", inv, cache2))\n    assert r2.model == \"cache\"\n    assert r2.score == r1.score\n\n\ndef test_cache_file_created_lazily(tmp_path):\n    cache = ImportanceCache(tmp_path / \"nested/cache.db\")\n    assert (tmp_path / \"nested/cache.db\").exists()\n\n\ndef test_cache_sha256_key_correctness(tmp_path):\n    \"\"\"AC #11 implementation detail — cache key is sha256(content)\"\"\"\n    cache = ImportanceCache(tmp_path / \"cache.db\")\n    inv = MockInvoker()\n    asyncio.run(score(\"hello world\", inv, cache))\n    h = hashlib.sha256(\"hello world\".encode()).digest()\n    assert cache.get(h) is not None</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>API contracts above are the skeleton. Implementation order:</p>\n<ol><li><code>cyberos/core/invokers/base.py</code> — Protocol + ScoreResult.</li><li><code>cyberos/core/invokers/mock.py</code> — MockInvoker.</li><li><code>cyberos/core/invokers/anthropic_invoker.py</code> — AnthropicInvoker.</li><li><code>cyberos/core/importance.py</code> — <code>select_invoker</code>, <code>ImportanceCache</code>, <code>score()</code> orchestrator.</li><li><code>cyberos/core/writer.py</code> — wire <code>--score-importance</code> path; validate <code>manifest.importance</code> block.</li><li><code>__main__.py</code> — <code>--score-importance</code>, <code>--importance</code>, <code>--invoker</code>, <code>--dry-run</code> flags on <code>put</code>.</li><li>Schema + invariant.</li><li>Tests.</li><li>CHANGELOG.</li></ol>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-113 (depends on)</strong> — <code>meta.importance</code> schema field + RecallWeights consume the scored value.</li><li><strong>FR-MEMORY-112 (related)</strong> — Episodes can opt-in to scoring; <code>Episode.quality_score</code> is the agent's self-rated output, <code>meta.importance</code> is the LLM's external rating — orthogonal signals.</li><li><strong>FR-MEMORY-115 (this FR blocks)</strong> — <code>cyberos dream</code> consumes <code>memory.importance_scored</code> audit rows for trend analysis (cache hit %, fallback %).</li><li><strong>FR-CUO-105 (related, depends_on pattern)</strong> — the Invoker Protocol mirrors CUO's; cross-module test ensures the two stay aligned (additive — same shape, different concern).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>memory.importance_scored</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.importance_scored\",\n  \"payload\": {\n    \"path\":           \"memories/facts/dispatch-latency.md\",\n    \"content_sha256\": \"abc123def456…\",\n    \"score\":          0.72,\n    \"model\":          \"claude-haiku-4-5\",\n    \"outcome\":        \"ok\",\n    \"reason\":         null,\n    \"cache_hit\":      false,\n    \"latency_ms\":     842\n  }\n}</code></pre>\n<h3>Memory file with scored importance</h3>\n<pre><code class=\"language-markdown\">---\n{\n  \"description\": \"Dispatch service has a 60-second retry pattern that triggers excess load\",\n  \"importance\": 0.72,\n  \"kind\": \"facts\",\n  \"name\": \"dispatch-retry-pattern\"\n}\n---\n\nObserved across 5 sessions in the SRE working memory: every page-out alert produces\na 60-second retry storm because the dispatch service's exponential backoff caps at 60s.</code></pre>\n<h3>Manifest fragment</h3>\n<pre><code class=\"language-json\">{\n  \"importance\": {\n    \"invoker\":   \"anthropic\",\n    \"model\":     \"claude-haiku-4-5\",\n    \"timeout_s\": 5.0\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Batch <code>score-all</code> mode for existing memories — §1 #14; slice 4+ (operator workflow, not blocker for FR-MEMORY-115).</li><li>Per-tenant importance bias (KYC tenants might rank PII higher than non-KYC) — slice 4+.</li><li>Cost-budget enforcement (<code>--max-spend-per-day 0.10 USD</code>) — slice 4+.</li><li>Stats CLI — §1 #15; slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>--score-importance</code> flag missing, default invoker absent (no API key, no env, no manifest)</td><td>none — by design, doesn't call LLM</td><td>Default <code>put</code> proceeds without score</td><td>None — operator opts in or sets manifest</td></tr><tr><td>AnthropicInvoker selected, <code>ANTHROPIC_API_KEY</code> unset</td><td>constructor raises</td><td><code>cyberos put</code> fails with structured error naming env var</td><td>Operator exports key or sets <code>CYBEROS_DISABLE_LLM=1</code></td></tr><tr><td>AnthropicInvoker selected, <code>anthropic</code> package not installed</td><td>constructor raises</td><td>same</td><td>Operator <code>pip install anthropic</code></td></tr><tr><td>Timeout &gt; 5s</td><td><code>asyncio.TimeoutError</code></td><td>fallback to 0.5; <code>outcome: \"fallback\", reason: \"timeout\"</code></td><td>Operator checks API status</td></tr><tr><td>Parse failure (LLM returns prose)</td><td>regex no match</td><td>fallback; <code>reason: \"parse_error:&lt;text&gt;\"</code></td><td>Audit row reveals the text; operator tunes prompt if recurrent</td></tr><tr><td>Out-of-range LLM output (e.g. 1.5)</td><td>clamp at score() return</td><td>success; logged at DEBUG</td><td>None — by design</td></tr><tr><td>Rate-limit (HTTP 429)</td><td>API exception</td><td>one retry with 1s jitter; if still rate-limited, fallback</td><td>Operator checks tier</td></tr><tr><td>Cache file corrupted</td><td>sqlite open raises</td><td>new cache created; previous entries lost</td><td>Operator deletes <code>index/importance_cache.db</code></td></tr><tr><td>Cache invariant violation (sha256 column not BLOB)</td><td><code>importance-cache-valid-sha256</code> walker rule</td><td><code>cyberos doctor</code> fails</td><td><code>cyberos doctor --repair</code> rebuilds the cache table</td></tr><tr><td>Concurrent writes scoring same content</td><td>both call invoker; second insert is <code>INSERT OR REPLACE</code> (DEC-203)</td><td>both writes succeed; cache reflects last writer</td><td>None — by design</td></tr><tr><td>Mock invoker drift (PRNG changed)</td><td><code>test_mock_invoker_deterministic</code> asserts</td><td>CI catches</td><td>Author preserves PRNG semantics</td></tr><tr><td>Caller passes <code>--importance 1.5</code> (manual override out of range)</td><td>argparse validates <code>[0.0, 1.0]</code></td><td>CLI rejects with structured error</td><td>Operator picks a valid value</td></tr><tr><td><code>--dry-run</code> invoked without <code>--score-importance</code></td><td>argparse mutually-exclusive check</td><td>CLI rejects</td><td>Operator adds flag</td></tr><tr><td>Manifest <code>importance.timeout_s</code> &lt;= 0</td><td>jsonschema validate</td><td><code>ManifestError</code></td><td>Operator fixes manifest</td></tr><tr><td>Audit row emit fails (writer locked)</td><td>writer raises</td><td>scoring still succeeds; row dropped at writer layer (handled by writer's existing retry policy)</td><td>None — by design</td></tr><tr><td>Large input (&gt; 100KB)</td><td>invoker accepts but Haiku rejects</td><td>API error → fallback</td><td>Operator considers splitting input</td></tr><tr><td>Different invoker types tested in same run</td><td>per-test isolation</td><td>works</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why we mirror CUO's invoker layout instead of importing it directly.</strong> <code>modules/cuo/cuo/invokers.py</code> exists but the import would create a circular dep (cuo depends on memory). Copying the small shape (MockInvoker, AnthropicInvoker, select) is cleaner than restructuring.</li><li><strong>The MockInvoker's deterministic mapping</strong> — <code>sha256(content) → [0.1, 0.95]</code> — was chosen so tests can assert specific scores (within tolerance) without flakiness. The 0.1/0.95 clamp prevents tests from ever hitting literal 0.0 / 1.0 which would conflate with fallback behaviour.</li><li><strong><code>select_invoker(name=None)</code> reuses CUO's selection algorithm</strong> — explicit name beats env beats default. Same priority order = same operator mental model.</li><li><strong>Cache table schema is single-table on purpose.</strong> No JOINs, no migrations, no ALTER TABLE. If the schema changes in slice 4, drop + recreate (data is regenerable by re-scoring).</li><li><strong>The <code>aux_emitter</code> callable is the same shim FR-MEMORY-112 uses for <code>episode.logged</code>.</strong> Keeps the importance.py module decoupled from Writer (no direct call into Writer.emit_aux).</li><li><strong>Why we don't batch invoker calls</strong> — Haiku's per-call latency is ~1.5s; batching would require structured-output prompting and complicate fallback handling. The Slice-4 <code>score-all</code> mode can batch internally without changing this FR's API.</li><li><strong>AnthropicInvoker uses <code>AsyncAnthropic</code>, not <code>Anthropic</code></strong> — non-blocking. Lets the Writer overlap scoring with the file-write fsync if it wanted to (it currently doesn't, but the option is preserved).</li><li><strong>The <code>test_anthropic_invoker_parse_paths</code> test scaffold is simplified for readability.</strong> Real tests use <code>responses.RequestsMock</code> or <code>aioresponses</code> to mock the HTTP layer. The fixture lives in <code>modules/memory/tests/core/test_mmr.py</code>.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-114.</em></p>";
