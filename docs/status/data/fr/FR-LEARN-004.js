window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-LEARN-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The LEARN service <strong>MUST</strong> ship Hội đồng Chuyên môn at <code>services/learn/src/council/</code> with 3-5 judges + 5-dim scoring + median aggregation, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>score_dimension</code> against closed enum per DEC-2111, <code>council_status</code> per DEC-2112.</li></ol>\n<ol><li><strong>MUST</strong> enforce 3-5 judges per DEC-2110 at council convene.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0004</code>: ```sql CREATE TABLE learn_councils ( council_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, candidate_member_id UUID NOT NULL, skill_id UUID, promotion_target_level INT, status TEXT NOT NULL DEFAULT 'convened' CHECK (status IN ('convened','scoring','completed','dismissed')), convened_by UUID NOT NULL, convened_at TIMESTAMPTZ NOT NULL DEFAULT now(), completed_at TIMESTAMPTZ, trace_id CHAR(32) ); ALTER TABLE learn_councils ENABLE ROW LEVEL SECURITY; CREATE POLICY councils_rls ON learn_councils USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON learn_councils FROM cyberos_app; GRANT UPDATE (status, completed_at) ON learn_councils TO cyberos_app;</li></ol>\n<p>   CREATE TABLE learn_council_judges (      council_id UUID NOT NULL REFERENCES learn_councils(council_id),      judge_id UUID NOT NULL,      tenant_id UUID NOT NULL,      assigned_at TIMESTAMPTZ NOT NULL DEFAULT now(),      PRIMARY KEY (council_id, judge_id)    );    ALTER TABLE learn_council_judges ENABLE ROW LEVEL SECURITY;    CREATE POLICY judges_rls ON learn_council_judges      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON learn_council_judges FROM cyberos_app;</p>\n<p>   CREATE TABLE learn_council_scores (      score_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      council_id UUID NOT NULL REFERENCES learn_councils(council_id),      judge_id UUID NOT NULL,      dimension TEXT NOT NULL        CHECK (dimension IN ('technical','leadership','impact','collaboration','growth_potential')),      score INT NOT NULL CHECK (score &gt;= 1 AND score &lt;= 5),      rationale TEXT,      submitted_at TIMESTAMPTZ NOT NULL DEFAULT now(),      trace_id CHAR(32),      UNIQUE (council_id, judge_id, dimension)    );    ALTER TABLE learn_council_scores ENABLE ROW LEVEL SECURITY;    CREATE POLICY scores_rls ON learn_council_scores      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON learn_council_scores FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> aggregate at <code>aggregator.rs::aggregate(council)</code> per DEC-2114:</li></ol>\n<ul><li>For each dimension: median of judge scores</li><li>Overall recommendation: promote if median &gt;= 4 on technical + leadership; hold if median 3; decline if median &lt;3.</li></ul>\n<ol><li><strong>MUST</strong> mark complete only when all judges submitted all 5 dimensions.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/learn/councils                           (CHRO convenes) POST /v1/learn/councils/{id}/judges               (add judge — 3-5 enforced) POST /v1/learn/councils/{id}/scores               (judge submits) GET  /v1/learn/councils/{id}                      (status + aggregate if completed) POST /v1/learn/councils/{id}/dismiss </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-2115. PII per FR-MEMORY-111: rationale SHA256; scores ok (small integers).</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from convene → scoring → complete → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow &lt;3 or &gt;5 judges per DEC-2110.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior score per DEC-2113.</li></ol>\n<ol><li><strong>MUST NOT</strong> double-score same dimension by same judge (UNIQUE).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 3-5 judges (DEC-2110)?</strong> Odd numbers + bounded — prevents tie + cognitive overhead.</p>\n<p><strong>Why 5 dimensions (DEC-2111)?</strong> Industry-standard rubric (Google L-ladder pattern).</p>\n<p><strong>Why median (DEC-2114)?</strong> Robust to outlier judges; mean would let one extreme score sway.</p>\n<p><strong>Why per-judge anonymity (DEC-2113)?</strong> Reduces social pressure; FR-LEARN-005 enforces post-completion isolation.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample council convene:</p>\n<pre><code class=\"language-json\">POST /v1/learn/councils\n{\n  \"candidate_member_id\": \"uuid\",\n  \"skill_id\": \"uuid\",\n  \"promotion_target_level\": 4\n}</code></pre>\n<p>Sample judge score:</p>\n<pre><code class=\"language-json\">POST /v1/learn/councils/{id}/scores\n{\n  \"dimension\": \"technical\",\n  \"score\": 4,\n  \"rationale\": \"Strong system design; needs more breadth in distributed systems.\"\n}</code></pre>\n<p>Aggregate (when completed):</p>\n<pre><code class=\"language-json\">{\n  \"council_id\": \"uuid\",\n  \"status\": \"completed\",\n  \"aggregate\": {\n    \"technical\": 4,\n    \"leadership\": 3,\n    \"impact\": 4,\n    \"collaboration\": 4,\n    \"growth_potential\": 4,\n    \"overall_recommendation\": \"promote\"\n  },\n  \"judges_count\": 5\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>score_dimension enum cardinality 5</strong>. 2. <strong>council_status enum cardinality 4</strong>. 3. <strong>3-5 judges enforced</strong>. 4. <strong>Score CHECK 1-5</strong>. 5. <strong>UNIQUE(council, judge, dimension)</strong>. 6. <strong>Median aggregation</strong>. 7. <strong>Completed when all judges × all dims submitted</strong>. 8. <strong>5 memory audit kinds emitted</strong>. 9. <strong>PII scrubbed (rationale SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>CHRO-only convene</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>Append-only via REVOKE except status cols</strong>. 14. <strong>Dismiss endpoint allowed</strong>. 15. <strong>Judge cannot self-score (member ≠ judge)</strong>. 16. <strong>Same-tenant judges only</strong>. 17. <strong>promotion_target_level optional (general assessment OK)</strong>. 18. <strong>Aggregate hidden until completed</strong>. 19. <strong>History queryable per candidate</strong>. 20. <strong>Overall recommendation logic per spec</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn 3_judges_minimum() {\n    let ctx = TestContext::with_council_convened().await;\n    ctx.add_judge(ctx.judge1).await;\n    ctx.add_judge(ctx.judge2).await;\n    let r = ctx.try_start_scoring(ctx.council_id).await;\n    assert!(r.is_err());  // need 3+\n    ctx.add_judge(ctx.judge3).await;\n    let r2 = ctx.try_start_scoring(ctx.council_id).await;\n    assert!(r2.is_ok());\n}\n\n#[tokio::test]\nasync fn median_aggregation() {\n    let ctx = TestContext::with_5_judges_scored([3, 4, 4, 5, 5], \"technical\").await;\n    ctx.complete_council().await;\n    let agg = ctx.fetch_aggregate(ctx.council_id).await;\n    assert_eq!(agg.technical, 4);  // median of [3,4,4,5,5]\n}\n\n#[tokio::test]\nasync fn double_score_rejected() {\n    let ctx = TestContext::with_council_scoring().await;\n    ctx.submit_score(ctx.judge_id, \"technical\", 4).await;\n    let r = ctx.submit_score(ctx.judge_id, \"technical\", 5).await;\n    assert!(r.is_err());  // UNIQUE\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-LEARN-001. <strong>Downstream:</strong> FR-LEARN-005 (isolation), FR-LEARN-006 (promotion approval). <strong>Cross-module:</strong> FR-AUTH-101 (CHRO + judge roles), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Judge count &lt;3</td><td>validate</td><td>reject start</td><td>add judge</td></tr><tr><td>Judge count &gt;5</td><td>validate</td><td>reject add</td><td>inherent</td></tr><tr><td>Score out of range</td><td>CHECK</td><td>400</td><td>use 1-5</td></tr><tr><td>Double score</td><td>UNIQUE</td><td>409</td><td>inherent</td></tr><tr><td>Judge = candidate</td><td>validate</td><td>reject</td><td>use different judge</td></tr><tr><td>Cross-tenant judge</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Premature complete (missing scores)</td><td>check</td><td>reject</td><td>submit remaining</td></tr><tr><td>Judge withdraws mid-council</td><td>manual reassign</td><td>inherent</td><td>replacement judge</td></tr><tr><td>Decimal precision N/A</td><td>integers</td><td>inherent</td><td>inherent</td></tr><tr><td>Concurrent score</td><td>UNIQUE</td><td>first wins</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Median: even-count handled by floor(avg) for stability.</li><li>§11.2 Overall recommendation matrix configurable in future; v1 hardcoded.</li><li>§11.3 memory audit body: council_id, judge_id, dimension, score; rationale SHA256.</li><li>§11.4 Judge UI shows other judges' status (submitted/pending) but NOT their scores.</li><li>§11.5 FR-LEARN-005 enforces post-completion data isolation outside council.</li></ul>\n<hr>\n<p><em>End of FR-LEARN-004 spec.</em></p>";
