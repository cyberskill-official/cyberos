window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INV-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The INV service <strong>MUST</strong> ship invoice substrate at <code>services/inv/src/</code> with 5 migrations, 8-state status FSM, append-only line corrections, rate-card snapshot, per-engagement scoping, gap-free per-tenant numbering, CFO-gated write-off, and 9 memory audit kinds. Anchors all downstream INV FRs (002-011) and cross-module invoice references.</p>\n<ol><li><strong>MUST</strong> define closed <code>invoice_status</code> enum: <code>('draft','ready_for_review','approved','sent','partially_paid','paid','void','written_off')</code> per DEC-1361. Cardinality 8.</li></ol>\n<ol><li><strong>MUST</strong> define closed <code>invoice_line_kind</code> enum: <code>('time_entry','fixed_fee','expense_reimbursement','discount','tax','late_fee')</code> per DEC-1362. Cardinality 6.</li></ol>\n<ol><li><strong>MUST</strong> define <code>invoices</code> at migration <code>0001</code>: <code>(invoice_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, invoice_number TEXT UNIQUE NOT NULL, status invoice_status NOT NULL DEFAULT 'draft', billing_currency billing_currency_enum NOT NULL, issued_at TIMESTAMPTZ, due_at TIMESTAMPTZ, sent_at TIMESTAMPTZ, paid_at TIMESTAMPTZ, voided_at TIMESTAMPTZ, written_off_at TIMESTAMPTZ, billing_period_start TIMESTAMPTZ, billing_period_end TIMESTAMPTZ, client_subject_id UUID, rate_card_snapshot JSONB NOT NULL, total_pre_tax_minor BIGINT NOT NULL DEFAULT 0, total_tax_minor BIGINT NOT NULL DEFAULT 0, total_minor BIGINT NOT NULL DEFAULT 0, paid_minor BIGINT NOT NULL DEFAULT 0, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), created_by_subject_id UUID NOT NULL, trace_id CHAR(32))</code>. Append-only on mutation of lines (status, amount fields updated through state-machine handlers only).</li></ol>\n<ol><li><strong>MUST</strong> define <code>invoice_lines</code> at migration <code>0002</code>: <code>(line_id UUID PRIMARY KEY, invoice_id UUID NOT NULL REFERENCES invoices(invoice_id), line_kind invoice_line_kind NOT NULL, description TEXT NOT NULL, quantity NUMERIC(18,4) NOT NULL, unit_price_minor BIGINT NOT NULL, amount_minor BIGINT NOT NULL, vat_rate_pct NUMERIC(5,2) NOT NULL DEFAULT 0, source_kind TEXT NOT NULL CHECK (source_kind IN ('time_entry','expense','manual','correction','discount_policy')), source_ref UUID, sort_order INT NOT NULL, correction_of_line_id UUID REFERENCES invoice_lines(line_id), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), trace_id CHAR(32))</code>. Append-only via REVOKE UPDATE/DELETE per feature-request-audit skill rule 12 + DEC-1366; correction = new row referencing original.</li></ol>\n<ol><li><strong>MUST</strong> define <code>invoice_status_history</code> at migration <code>0003</code>: <code>(id BIGSERIAL PRIMARY KEY, invoice_id UUID NOT NULL REFERENCES invoices(invoice_id), from_status invoice_status, to_status invoice_status NOT NULL, transitioned_at TIMESTAMPTZ NOT NULL DEFAULT now(), transitioned_by_subject_id UUID NOT NULL, reason TEXT, trace_id CHAR(32))</code>. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> define <code>invoice_number_sequence</code> at migration <code>0004</code>: <code>(tenant_id UUID NOT NULL, year INT NOT NULL, last_sequence INT NOT NULL DEFAULT 0, notes JSONB NOT NULL DEFAULT '[]'::jsonb, PRIMARY KEY (tenant_id, year))</code>. Gap-free per-tenant annual; skipped sequences logged in <code>notes</code> for Decree-123 conformance (slice-2 hóa đơn integration).</li></ol>\n<ol><li><strong>MUST</strong> define <code>rate_card_snapshot</code> table at migration <code>0005</code> storing per-engagement rate-card versions: <code>(snapshot_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, version INT NOT NULL, rates_jsonb JSONB NOT NULL, effective_from TIMESTAMPTZ NOT NULL DEFAULT now(), created_by_subject_id UUID NOT NULL, UNIQUE(engagement_id, version))</code>. Versioned rate cards; invoices snapshot the latest at creation time.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with USING and WITH CHECK on all 5 tables: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li></ol>\n<ol><li><strong>MUST</strong> expose draft creation <code>POST /v1/inv/invoices/draft</code> body <code>{ engagement_id, billing_period_start, billing_period_end }</code>. Handler:</li></ol>\n<ul><li>Validates engagement_id in tenant scope.</li><li>Resolves billing currency from engagement.</li><li>Allocates invoice number per §1 #10.</li><li>Snapshots current rate card per §1 #11.</li><li>Aggregates unbilled TIME entries via FR-TIME-009 rollup into <code>invoice_lines</code> rows.</li><li>Computes totals.</li><li>Marks TIME entries <code>invoiced_at = now()</code> to prevent double-billing.</li><li>INSERTs invoices + invoice_lines + invoice_status_history (status='draft').</li><li>Emits <code>inv.draft_created</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> allocate invoice number per DEC-1367 via <code>numbering/mod.rs</code>:</li></ol>\n<ul><li><code>SELECT last_sequence FROM invoice_number_sequence WHERE tenant_id=$1 AND year=$2 FOR UPDATE</code>.</li><li><code>last_sequence + 1</code>.</li><li><code>INVOICE_NUMBER = \"INV-\" + tenant_slug + \"-\" + (year%100) + \"-\" + ZeroPad(seq, 6)</code>.</li><li>UPDATE sequence.</li><li>On rollback after allocation: record skipped number in <code>notes</code> JSONB for Decree-123 audit clarity (matches FR-TEN-102 §1 #17 pattern).</li></ul>\n<ol><li><strong>MUST</strong> snapshot rate card per DEC-1363. The <code>snapshot/rate_card.rs::snapshot_for_engagement(engagement_id)</code>:</li></ol>\n<ul><li>Resolves latest active rate-card version.</li><li>Deep-copies into <code>invoices.rate_card_snapshot</code> JSONB.</li><li>Records snapshot version reference for traceability.</li></ul>\n<ol><li><strong>MUST</strong> validate state-machine transitions per DEC-1361 via <code>status/state_machine.rs</code>. Allowed transitions:</li></ol>\n<ul><li><code>draft → ready_for_review</code> (any user can flip own draft).</li><li><code>ready_for_review → approved</code> (cfo OR engagement_admin only per DEC-1370).</li><li><code>approved → sent</code> (auto on send action OR manual).</li><li><code>sent → partially_paid</code> (auto on partial payment receipt via FR-INV-003/005).</li><li><code>sent | partially_paid → paid</code> (auto on full payment).</li><li><code>draft | ready_for_review | approved → void</code> (cfo only; with reason).</li><li><code>sent | partially_paid → written_off</code> (cfo only; with reason per DEC-1371). Invalid transition → 400 <code>invalid_status_transition</code>.</li></ul>\n<ol><li><strong>MUST</strong> support append-only corrections per DEC-1366. <code>POST /v1/inv/invoices/{id}/lines/correction</code> body <code>{ correction_of_line_id, line_kind, description, quantity, unit_price_minor, amount_minor, vat_rate_pct, reason }</code>. Handler:</li></ol>\n<ul><li>Validates <code>correction_of_line_id</code> exists + same invoice.</li><li>INSERTs new line with <code>source_kind='correction'</code> + <code>correction_of_line_id</code> populated.</li><li>Recomputes invoice totals.</li><li>Emits <code>inv.correction_added</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> support approve action <code>POST /v1/inv/invoices/{id}/approve</code>. Caller has <code>cfo</code> OR <code>engagement_admin</code>. Validates transition; INSERTs status history; UPDATEs status; emits <code>inv.approved</code> sev-1 (material commercial event).</li></ol>\n<ol><li><strong>MUST</strong> support send action <code>POST /v1/inv/invoices/{id}/send</code> body <code>{ delivery_method, delivery_target }</code>. Caller has <code>cfo</code> OR <code>engagement_admin</code>. Validates status='approved'; transitions to 'sent'; sets <code>sent_at</code>; emits <code>inv.sent</code> sev-1 (triggers FR-INV-007 VN hóa đơn for VN tenants).</li></ol>\n<ol><li><strong>MUST</strong> support write-off <code>POST /v1/inv/invoices/{id}/write-off</code> body <code>{ reason }</code>. Caller has <code>cfo</code> only. Records reason; transitions to 'written_off'; emits <code>inv.written_off</code> sev-1.</li></ol>\n<ol><li><strong>MUST</strong> support void <code>POST /v1/inv/invoices/{id}/void</code> body <code>{ reason }</code>. Caller has <code>cfo</code> only. Validates status NOT IN ('sent','partially_paid','paid','written_off'); transitions to 'void'; emits <code>inv.void</code> sev-1.</li></ol>\n<ol><li><strong>MUST</strong> auto-generate drafts at billing-cycle boundaries per DEC-1369 via <code>draft/scheduler.rs::run_for_engagement(engagement_id, cycle_end)</code>. Scheduled job runs hourly; selects engagements whose cycle ends in past hour; creates draft if unbilled TIME entries exist + engagement has <code>auto_draft_enabled=true</code>.</li></ol>\n<ol><li><strong>MUST</strong> scope invoice to ONE engagement per DEC-1368. Cross-engagement consolidation = anti-pattern; deferred.</li></ol>\n<ol><li><strong>MUST</strong> store amounts as BIGINT minor per feature-request-audit skill rule 11. NEVER FLOAT.</li></ol>\n<ol><li><strong>MUST</strong> mark TIME entries <code>invoiced_at</code> to prevent double-billing per DEC-1369 derivative. FR-TIME-009 rollup filter excludes entries with non-NULL <code>invoiced_at</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit 9 memory audit kinds per DEC-1374:</li></ol>\n<ul><li><code>inv.draft_created</code> (sev-2)</li><li><code>inv.lines_added</code> (sev-3)</li><li><code>inv.status_transitioned</code> (sev-2)</li><li><code>inv.approved</code> (sev-1)</li><li><code>inv.sent</code> (sev-1)</li><li><code>inv.paid</code> (sev-1)</li><li><code>inv.void</code> (sev-1)</li><li><code>inv.written_off</code> (sev-1)</li><li><code>inv.correction_added</code> (sev-2)</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub description + reason via FR-MEMORY-111 — SHA256 in chain.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate invoice_lines after approval per DEC-1366 — correction-only path.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow gap in invoice numbering per DEC-1367 — skipped numbers documented in notes JSONB.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow non-cfo write-off per DEC-1371.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why rate-card snapshot (§1 #11, DEC-1363)?</strong> Rates change. An invoice generated last quarter at $X/hr stays at $X/hr regardless of subsequent rate hikes. Live reference = re-invoicing legally indefensible (client paid based on a number we can no longer reproduce).</p>\n<p><strong>Why append-only corrections (§1 #13, DEC-1366)?</strong> Post-approval edits silently changing past invoices = fraud signal in any audit. Correction lines (with reason) maintain the original AND the fix AND the auditor's ability to see the delta.</p>\n<p><strong>Why gap-free numbering (§1 #10, DEC-1367)?</strong> Decree 123 + general accounting principle — gaps suggest hidden transactions. Skip-with-reason logged in notes satisfies tax-authority audit while permitting transaction rollback.</p>\n<p><strong>Why ONE engagement per invoice (§1 #19, DEC-1368)?</strong> Multi-engagement invoices conflate client billing relationships; \"Acme paid invoice X — for which engagement?\" becomes ambiguous. Engagement-scoped invoices = clean audit + clean reporting.</p>\n<p><strong>Why CFO-only write-off (§1 #16, DEC-1371)?</strong> Write-off = revenue erasure with tax implications. Requires CFO sign-off both for accounting control (prevent fraud) and for tax-treatment correctness.</p>\n<p><strong>Why 8-state FSM vs simpler 4-state (§1 #12)?</strong> Financial workflows need draft/review/approved/sent/partially_paid/paid as distinct states for status reporting + automation triggers. void + written_off cover the two distinct \"no payment received\" terminal cases (cancelled-by-us vs uncollectable).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0001_invoices.sql\nCREATE TYPE invoice_status AS ENUM ('draft','ready_for_review','approved','sent','partially_paid','paid','void','written_off');\n\nCREATE TABLE invoices (\n  invoice_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  invoice_number TEXT UNIQUE NOT NULL,\n  status invoice_status NOT NULL DEFAULT 'draft',\n  billing_currency billing_currency_enum NOT NULL,\n  issued_at TIMESTAMPTZ,\n  due_at TIMESTAMPTZ,\n  sent_at TIMESTAMPTZ,\n  paid_at TIMESTAMPTZ,\n  voided_at TIMESTAMPTZ,\n  written_off_at TIMESTAMPTZ,\n  billing_period_start TIMESTAMPTZ,\n  billing_period_end TIMESTAMPTZ,\n  client_subject_id UUID,\n  rate_card_snapshot JSONB NOT NULL,\n  total_pre_tax_minor BIGINT NOT NULL DEFAULT 0,\n  total_tax_minor BIGINT NOT NULL DEFAULT 0,\n  total_minor BIGINT NOT NULL DEFAULT 0,\n  paid_minor BIGINT NOT NULL DEFAULT 0,\n  notes TEXT,\n  internal_notes TEXT,\n  sync_class TEXT NOT NULL DEFAULT 'client-visible' CHECK (sync_class IN ('private','team-internal','client-visible','client-visible-redacted')),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  created_by_subject_id UUID NOT NULL,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_inv_engagement ON invoices(engagement_id, status);\nCREATE INDEX idx_inv_status_due ON invoices(status, due_at) WHERE status NOT IN ('paid','void','written_off');\nALTER TABLE invoices ENABLE ROW LEVEL SECURITY;\nCREATE POLICY invoices_rls ON invoices\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON invoices FROM cyberos_app;\nGRANT UPDATE (status, issued_at, due_at, sent_at, paid_at, voided_at, written_off_at,\n              total_pre_tax_minor, total_tax_minor, total_minor, paid_minor, notes) ON invoices TO cyberos_app;\n\n-- 0002_invoice_lines.sql\nCREATE TYPE invoice_line_kind AS ENUM ('time_entry','fixed_fee','expense_reimbursement','discount','tax','late_fee');\n\nCREATE TABLE invoice_lines (\n  line_id UUID PRIMARY KEY,\n  invoice_id UUID NOT NULL REFERENCES invoices(invoice_id),\n  line_kind invoice_line_kind NOT NULL,\n  description TEXT NOT NULL,\n  quantity NUMERIC(18,4) NOT NULL,\n  unit_price_minor BIGINT NOT NULL,\n  amount_minor BIGINT NOT NULL,\n  vat_rate_pct NUMERIC(5,2) NOT NULL DEFAULT 0,\n  source_kind TEXT NOT NULL CHECK (source_kind IN ('time_entry','expense','manual','correction','discount_policy')),\n  source_ref UUID,\n  sort_order INT NOT NULL,\n  correction_of_line_id UUID REFERENCES invoice_lines(line_id),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_inv_lines_invoice ON invoice_lines(invoice_id, sort_order);\nALTER TABLE invoice_lines ENABLE ROW LEVEL SECURITY;\nCREATE POLICY invoice_lines_rls ON invoice_lines\n  USING (invoice_id IN (SELECT invoice_id FROM invoices WHERE tenant_id = current_setting('auth.tenant_id')::uuid))\n  WITH CHECK (invoice_id IN (SELECT invoice_id FROM invoices WHERE tenant_id = current_setting('auth.tenant_id')::uuid));\nREVOKE UPDATE, DELETE ON invoice_lines FROM cyberos_app;\n\n-- 0003_invoice_status_history.sql\nCREATE TABLE invoice_status_history (\n  id BIGSERIAL PRIMARY KEY,\n  invoice_id UUID NOT NULL REFERENCES invoices(invoice_id),\n  from_status invoice_status,\n  to_status invoice_status NOT NULL,\n  transitioned_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  transitioned_by_subject_id UUID NOT NULL,\n  reason TEXT,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_inv_history_invoice ON invoice_status_history(invoice_id, transitioned_at);\nALTER TABLE invoice_status_history ENABLE ROW LEVEL SECURITY;\nCREATE POLICY invoice_status_history_rls ON invoice_status_history\n  USING (invoice_id IN (SELECT invoice_id FROM invoices WHERE tenant_id = current_setting('auth.tenant_id')::uuid))\n  WITH CHECK (invoice_id IN (SELECT invoice_id FROM invoices WHERE tenant_id = current_setting('auth.tenant_id')::uuid));\nREVOKE UPDATE, DELETE ON invoice_status_history FROM cyberos_app;\n\n-- 0004_invoice_number_sequence.sql\nCREATE TABLE invoice_number_sequence (\n  tenant_id UUID NOT NULL,\n  year INT NOT NULL,\n  last_sequence INT NOT NULL DEFAULT 0,\n  notes JSONB NOT NULL DEFAULT '[]'::jsonb,\n  PRIMARY KEY (tenant_id, year)\n);\nALTER TABLE invoice_number_sequence ENABLE ROW LEVEL SECURITY;\nCREATE POLICY invoice_number_sequence_rls ON invoice_number_sequence\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON invoice_number_sequence FROM cyberos_app;\nGRANT UPDATE (last_sequence, notes) ON invoice_number_sequence TO cyberos_app;\n\n-- 0005_rate_card_snapshot.sql\nCREATE TABLE rate_card_snapshot (\n  snapshot_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  version INT NOT NULL,\n  rates_jsonb JSONB NOT NULL,\n  effective_from TIMESTAMPTZ NOT NULL DEFAULT now(),\n  created_by_subject_id UUID NOT NULL,\n  UNIQUE (engagement_id, version)\n);\nALTER TABLE rate_card_snapshot ENABLE ROW LEVEL SECURITY;\nCREATE POLICY rate_card_snapshot_rls ON rate_card_snapshot\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON rate_card_snapshot FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/inv/invoices/draft                                     (engagement_admin or cfo)\nPOST   /v1/inv/invoices/{id}/lines/correction                     (cfo or engagement_admin)\nPOST   /v1/inv/invoices/{id}/approve                              (cfo or engagement_admin)\nPOST   /v1/inv/invoices/{id}/send                                 (cfo or engagement_admin)\nPOST   /v1/inv/invoices/{id}/void                                 (cfo)\nPOST   /v1/inv/invoices/{id}/write-off                            (cfo)\nGET    /v1/inv/invoices/{id}                                      (engagement_admin or cfo or client per sync_class)\nGET    /v1/inv/invoices?engagement_id=...&amp;status=...              (engagement_admin or cfo)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>invoice_status cardinality 8</strong>.</li><li><strong>invoice_line_kind cardinality 6</strong>.</li><li><strong>Draft from TIME rollup</strong> — unbilled TIME entries aggregated into invoice_lines; entries marked <code>invoiced_at</code>.</li><li><strong>Rate-card snapshot immutable</strong> — invoice created; rate card subsequently changed; invoice retains original rates in snapshot.</li><li><strong>Append-only lines</strong> — UPDATE on <code>invoice_lines</code> raises permission-denied.</li><li><strong>Correction line</strong> — POST correction creates new line with <code>correction_of_line_id</code> populated; totals recomputed.</li><li><strong>Status FSM transitions</strong> — <code>draft → ready_for_review → approved → sent → paid</code> legal; reverse illegal.</li><li><strong>Approval requires cfo or engagement_admin</strong> — tenant_admin → 403.</li><li><strong>Write-off requires cfo</strong> — engagement_admin write-off → 403.</li><li><strong>Write-off requires reason</strong> — empty reason → 400.</li><li><strong>Invoice number format</strong> — <code>INV-acme-26-000001</code> for first tenant=acme invoice in 2026.</li><li><strong>Gap-free numbering</strong> — 1000 concurrent draft creations → 1000 unique numbers no duplicates.</li><li><strong>Skipped numbers logged</strong> — rollback after allocation → skip recorded in notes.</li><li><strong>Per-engagement scope</strong> — invoice has exactly one engagement_id.</li><li><strong>TIME entries not double-billed</strong> — second draft generation on same period → 0 lines (all entries invoiced_at).</li><li><strong>Status history append-only</strong> — every transition recorded; UPDATE on history → permission-denied.</li><li><strong>9 memory audit kinds emitted</strong> — full lifecycle.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>BIGINT minor amounts</strong> — schema validation; never FLOAT.</li><li><strong>RLS cross-tenant denied</strong> — tenant A invoice invisible to tenant B.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn draft_from_time_rollup() {\n    let ctx = TestContext::with_engagement_and_time_entries().await;\n    let r = ctx.post_draft(ctx.eng_id, ctx.period_start, ctx.period_end).await;\n    assert_eq!(r.status(), 201);\n    let body: serde_json::Value = r.json().await.unwrap();\n    let inv_id: Uuid = body[\"invoice_id\"].as_str().unwrap().parse().unwrap();\n    let lines: Vec&lt;(String,)&gt; = sqlx::query_as(\"SELECT description FROM invoice_lines WHERE invoice_id=$1\")\n        .bind(inv_id).fetch_all(&amp;ctx.pool).await.unwrap();\n    assert!(lines.len() &gt;= 1);\n\n    let invoiced_count: i64 = sqlx::query_scalar(\n        \"SELECT count(*) FROM time_entries WHERE engagement_id=$1 AND invoiced_at IS NOT NULL\"\n    ).bind(ctx.eng_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(invoiced_count &gt; 0);\n}\n\n#[tokio::test]\nasync fn rate_card_snapshot_immutable() {\n    let ctx = TestContext::with_engagement_and_time_entries().await;\n    let inv_id = ctx.create_draft().await;\n    let snapshot1: serde_json::Value = sqlx::query_scalar(\n        \"SELECT rate_card_snapshot FROM invoices WHERE invoice_id=$1\"\n    ).bind(inv_id).fetch_one(&amp;ctx.pool).await.unwrap();\n\n    ctx.bump_rate_card(ctx.eng_id, /*new rate*/ 200_00).await;\n    let snapshot2: serde_json::Value = sqlx::query_scalar(\n        \"SELECT rate_card_snapshot FROM invoices WHERE invoice_id=$1\"\n    ).bind(inv_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(snapshot1, snapshot2);\n}\n\n#[tokio::test]\nasync fn append_only_corrections() {\n    let ctx = TestContext::with_invoice().await;\n    let r = sqlx::query(\"UPDATE invoice_lines SET amount_minor=999 WHERE invoice_id=$1\")\n        .bind(ctx.inv_id).execute(&amp;ctx.pool).await;\n    assert!(r.is_err());\n\n    let r = ctx.post_correction(ctx.inv_id, ctx.line_id, /*amount*/ 500_00, \"rate correction\").await;\n    assert_eq!(r.status(), 201);\n    let line_count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM invoice_lines WHERE invoice_id=$1\")\n        .bind(ctx.inv_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(line_count, 2);  // original + correction\n}\n\n#[tokio::test]\nasync fn status_fsm_enforces_transitions() {\n    let ctx = TestContext::with_invoice().await;\n    let r = ctx.transition_invoice(ctx.inv_id, \"paid\").await;  // draft → paid skip\n    assert_eq!(r.status(), 400);\n\n    ctx.transition_invoice(ctx.inv_id, \"ready_for_review\").await;\n    ctx.as_cfo().transition_invoice(ctx.inv_id, \"approved\").await;\n    ctx.transition_invoice(ctx.inv_id, \"sent\").await;\n    let r = ctx.transition_invoice(ctx.inv_id, \"paid\").await;\n    assert_eq!(r.status(), 200);\n}\n\n#[tokio::test]\nasync fn write_off_cfo_only_with_reason() {\n    let ctx = TestContext::with_sent_invoice().await;\n    let r = ctx.as_engagement_admin().post_write_off(ctx.inv_id, \"uncollectable\").await;\n    assert_eq!(r.status(), 403);\n    let r = ctx.as_cfo().post_write_off(ctx.inv_id, \"\").await;\n    assert_eq!(r.status(), 400);\n    let r = ctx.as_cfo().post_write_off(ctx.inv_id, \"uncollectable\").await;\n    assert_eq!(r.status(), 200);\n}\n\n#[tokio::test]\nasync fn invoice_number_gap_free() {\n    let ctx = TestContext::new().await;\n    let nums: Vec&lt;String&gt; = futures::stream::iter(0..100).then(|_| async {\n        let r = ctx.post_draft_minimal().await;\n        r.json::&lt;serde_json::Value&gt;().await.unwrap()[\"invoice_number\"].as_str().unwrap().to_owned()\n    }).collect::&lt;Vec&lt;_&gt;&gt;().await;\n    let unique: std::collections::HashSet&lt;_&gt; = nums.iter().cloned().collect();\n    assert_eq!(unique.len(), nums.len());\n}\n\n// 5.7..5.12: enum cardinality, per-engagement scope, TIME no-double-bill, status history, audit emission, RLS</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-TIME-009 (per-cycle rollup source). <strong>Cross-module:</strong> FR-INV-002 (multi-currency), FR-INV-003 (Stripe payment), FR-INV-005 (VietQR), FR-INV-006 (cash app), FR-INV-007 (VN hóa đơn), FR-TEN-003 (Stripe ref), FR-TEN-102 (VND ref), FR-PORTAL-001 (invoices view), FR-PORTAL-006 (billing_inquiry workflows), FR-CRM-001 (client subject), FR-AUTH-101 (cfo + engagement_admin roles), FR-AI-003, FR-MEMORY-111, FR-OBS-007. <strong>Downstream:</strong> FR-INV-002 through FR-INV-011.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>inv.draft_created</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"inv.draft_created\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.engagement_admin.456\",\n  \"trace_id\": \"...\",\n  \"occurred_at\": \"2026-05-17T...\",\n  \"payload\": {\n    \"invoice_id\": \"0190...\",\n    \"invoice_number\": \"INV-acme-26-000042\",\n    \"engagement_id\": \"0190...\",\n    \"billing_currency\": \"USD\",\n    \"billing_period_start\": \"2026-04-17T00:00:00Z\",\n    \"billing_period_end\": \"2026-05-17T00:00:00Z\",\n    \"line_count\": 12,\n    \"total_minor\": 875_000,\n    \"rate_card_version\": 3\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-currency per invoice (slice 2 / FR-INV-002).</li><li><strong>Deferred:</strong> Subscription-style recurring invoice templates (slice 3).</li><li><strong>Deferred:</strong> Customer-facing payment link in sent email (slice 2).</li><li><strong>Deferred:</strong> Late-fee auto-calc + late_fee line generation (slice 3).</li><li><strong>Deferred:</strong> Multi-engagement consolidated invoicing (anti-pattern; not planned).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>TIME entry already invoiced</td><td>filter excludes invoiced_at</td><td>Skipped from draft</td><td>Inherent</td></tr><tr><td>Rate card missing for engagement</td><td>snapshot fails</td><td>412 + <code>no_rate_card_configured</code></td><td>Engagement_admin configures</td></tr><tr><td>Invalid status transition</td><td>FSM check</td><td>400 + <code>invalid_status_transition</code></td><td>Caller fixes flow</td></tr><tr><td>Approve without chief-financial-officer/engagement_admin role</td><td>check</td><td>403</td><td>Inherent</td></tr><tr><td>Write-off without CFO</td><td>check</td><td>403</td><td>Inherent</td></tr><tr><td>Write-off without reason</td><td>validation</td><td>400</td><td>Inherent</td></tr><tr><td>Numbering rollback</td><td>exception in INSERT after sequence allocated</td><td>Skip logged in notes</td><td>Inherent forensic</td></tr><tr><td>Duplicate invoice_number (race)</td><td>UNIQUE constraint</td><td>Re-attempt with next sequence</td><td>Inherent retry</td></tr><tr><td>Correction reference invalid</td><td>FK check</td><td>400</td><td>Caller fixes line_id</td></tr><tr><td>Cross-tenant invoice access</td><td>RLS</td><td>0 rows</td><td>Inherent</td></tr><tr><td>Concurrent line addition pre-approval</td><td>tx isolation</td><td>Last writer wins</td><td>Inherent</td></tr><tr><td>Multi-currency on single invoice</td><td>engagement.billing_currency immutable</td><td>Inherent prevention</td><td>New engagement required</td></tr><tr><td>Send before approval</td><td>FSM check</td><td>400 + invalid transition</td><td>Approve first</td></tr><tr><td>Void after send</td><td>FSM check</td><td>400</td><td>Use write-off instead</td></tr><tr><td>Negative total computed</td><td>balance check</td><td>sev-2 alert</td><td>Operator review</td></tr><tr><td>Status history insert fails post-transition</td><td>tx isolation</td><td>Rollback; status unchanged</td><td>Inherent</td></tr><tr><td>TIME entry deleted after invoiced</td><td>source_ref orphan</td><td>Line retained with description; FR-TIME-009 prevents delete after invoiced_at</td><td>Inherent guard</td></tr><tr><td>Rate card snapshot JSONB &gt; 100 KB</td><td>size limit</td><td>sev-2; investigate rate-card explosion</td><td>Slim rate card</td></tr><tr><td>Approval after period close</td><td>period_close hooks</td><td>Allowed; backfills supported</td><td>Inherent</td></tr><tr><td>Invoice viewed by client with internal_notes leaked</td><td>sync_class filter via PORTAL-001</td><td>Internal hidden</td><td>PORTAL-001 enforces</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Rate-card snapshot deep-copy via <code>serde_json::to_value(&amp;rate_card)</code> ensures complete capture.</p>\n<p><strong>§11.2</strong> TIME-side <code>invoiced_at</code> column added via migration in this FR's <code>modified_files</code>.</p>\n<p><strong>§11.3</strong> Numbering sequence uses Postgres <code>FOR UPDATE</code> row lock to prevent race; skipped logged via post-rollback hook.</p>\n<p><strong>§11.4</strong> State machine encoded as Rust <code>match</code> table; CI test asserts every (from, to) pair documented.</p>\n<p><strong>§11.5</strong> Per-engagement billing currency consumed from <code>engagements.billing_currency</code>; immutable once set (FR-TEN-003 derivative).</p>\n<p><strong>§11.6</strong> Cross-line totals computed at write-time (not view-time) for query simplicity.</p>\n<p><strong>§11.7</strong> Append-only at SQL grant level prevents handler bypass.</p>\n<p><strong>§11.8</strong> Status_history <code>from_status</code> is NULL on initial create row.</p>\n<p><strong>§11.9</strong> PORTAL-001 view of invoices applies <code>sync_class</code> filter to hide internal fields.</p>\n<p><strong>§11.10</strong> Draft auto-scheduler runs hourly; engagement.auto_draft_enabled gates participation.</p>\n<hr>\n<p><em>End of FR-INV-001 spec.</em></p>";
