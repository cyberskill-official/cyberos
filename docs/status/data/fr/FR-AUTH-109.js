window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-109"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> enforce the FR-AUTH-101 stub → full migration via the 30-day grace window + rejection enforcer + per-tenant extension override. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>auth_migration_state</code> table per-tenant: <code>(tenant_id UUID PRIMARY KEY, cutover_at TIMESTAMPTZ NOT NULL, status TEXT NOT NULL CHECK (status IN ('grace_active','grace_extended','cutover_completed')), grace_extended_at TIMESTAMPTZ, grace_extension_reason TEXT, extension_count INT NOT NULL DEFAULT 0 CHECK (extension_count BETWEEN 0 AND 1), created_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. One row per tenant.</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_token_refresh_log</code> table: <code>(id BIGSERIAL, tenant_id UUID, subject_id UUID, prior_rbac_v_present BOOLEAN NOT NULL, new_rbac_v INT NOT NULL, refreshed_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> (per DEC-446).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> on <code>auth_migration_state</code> (root-admin only — same pattern as FR-TEN-001's tenants table; consumers read via internal helper) and <code>auth_token_refresh_log</code> (tenant-scoped).</li></ol>\n<ol><li><strong>MUST</strong> seed <code>auth_migration_state</code> for every existing tenant at FR-AUTH-101 ship time: cutover_at = ship_date + 30 days, status = <code>grace_active</code>. New tenants (provisioned after ship date) get cutover_at = now() + 30 days at provisioning time (FR-TEN-001 hook).</li></ol>\n<ol><li><strong>MUST</strong> ship the verifier hook (per DEC-442 + DEC-448). When FR-AUTH-004's verifier validates a JWT:</li></ol>\n<ul><li>If token's <code>rbac_v</code> claim is present → continue normal verification.</li><li>If <code>rbac_v</code> is absent → look up <code>auth_migration_state</code> for the token's <code>tid</code> claim.</li><li>If <code>cutover_at &gt; now()</code> → accept; emit <code>auth.stub_token_accepted</code> memory row (sampled 1%).</li><li>If <code>cutover_at &lt;= now()</code> → reject with 401 + body per DEC-448; emit <code>auth.stub_token_rejected</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> ship the refresh hook (per DEC-449). When FR-AUTH-004's refresh path issues a new access token:</li></ol>\n<ul><li>If prior token lacked <code>rbac_v</code> → log the refresh in <code>auth_token_refresh_log</code> with <code>prior_rbac_v_present=false</code>; new token MUST include <code>rbac_v</code> at current value.</li><li>If prior token had <code>rbac_v</code> → log with <code>prior_rbac_v_present=true</code>; new token includes current <code>rbac_v</code> (may differ from prior if catalogue version bumped).</li></ul>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/migration/extend-grace</code> handler (per DEC-445). Body: <code>{additional_days: 1-60, reason: \"&lt;text 1-500 chars&gt;\"}</code>. Caller MUST have role <code>root-admin</code> per FR-AUTH-101. Validates:</li></ol>\n<ul><li>Tenant's current status is <code>grace_active</code> (extended → 409 already_extended).</li><li>additional_days in [1, 60].</li><li>reason non-empty. On success: UPDATE <code>auth_migration_state</code> SET cutover_at = cutover_at + additional_days, status = <code>grace_extended</code>, grace_extended_at = now(), grace_extension_reason = reason, extension_count = 1. Emit <code>auth.grace_window_extended</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> ship <code>GET /v1/auth/migration/preview</code> returning <code>{tenant_id, cutover_at, status, days_remaining, stub_tokens_accepted_count_24h, stub_tokens_rejected_count_24h, refreshed_count_24h, extension_count, can_extend: bool}</code>. Caller MUST have role <code>root-admin</code> per FR-AUTH-101 OR <code>tenant-admin</code> (can see their own tenant's preview).</li></ol>\n<ol><li><strong>MUST</strong> ensure cutover_at is immutable post-grace-expiry (per DEC-442). A <code>BEFORE UPDATE</code> trigger on <code>auth_migration_state</code> rejects mutation of <code>cutover_at</code> when <code>status = 'cutover_completed'</code>. Cutover transition (grace_active → cutover_completed) happens automatically when verifier observes <code>cutover_at &lt;= now()</code> on first request; the trigger UPDATEs status atomically.</li></ol>\n<ol><li><strong>MUST</strong> transition status <code>grace_active</code> (or <code>grace_extended</code>) → <code>cutover_completed</code> exactly once per tenant — the first verifier request after <code>cutover_at &lt;= now()</code>. Emit <code>auth.cutover_completed</code> memory row.</li></ol>\n<ol><li><strong>MUST</strong> emit memory audit rows for 4 kinds (per DEC-443):</li></ol>\n<ul><li><code>auth.stub_token_accepted</code> — sampled 1% during grace; 100% near grace end (last 24h).</li><li><code>auth.stub_token_rejected</code> — every rejection; carries <code>tenant_id</code>, <code>subject_id_hash16</code>, <code>cutover_at</code>, <code>now</code>.</li><li><code>auth.grace_window_extended</code> — every extension; carries <code>tenant_id</code>, <code>prior_cutover_at</code>, <code>new_cutover_at</code>, <code>additional_days</code>, <code>reason_scrubbed</code>, <code>extended_by_subject_id_hash16</code>.</li><li><code>auth.cutover_completed</code> — single emission per tenant on transition; carries <code>tenant_id</code>, <code>final_cutover_at</code>, <code>total_stub_accepted</code>, <code>total_stub_rejected</code>.</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>grace_extension_reason</code> via FR-MEMORY-111 before chain commit.</li></ol>\n<ol><li><strong>MUST</strong> complete the verifier hook in ≤ 5 ms p99 (in-memory state cache; DB read only on cache miss). The verifier hook is on the hot path of every JWT verification — must be fast.</li></ol>\n<ol><li><strong>MUST</strong> cache <code>auth_migration_state</code> per-tenant in-memory with 60s TTL. Cache invalidated on extension or cutover-completion via explicit invalidation message.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>auth.migration.{verify_stub,refresh,extend_grace,cutover}</code> with attributes: <code>tenant_id</code>, <code>outcome</code> (accepted | rejected | extended | already_extended | cutover_triggered | not_root_admin).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_stub_token_accepted_total{tenant_id}</code> (counter — should approach zero post-grace).</li><li><code>auth_stub_token_rejected_total{tenant_id}</code> (counter — alarm sev-2 at sustained &gt; 100/h per DEC-444).</li><li><code>auth_token_refresh_total{tenant_id, prior_rbac_v_present}</code> (counter).</li><li><code>auth_grace_extensions_total{tenant_id}</code> (counter; max 1 per tenant).</li><li><code>auth_cutover_completed_total{tenant_id}</code> (counter; max 1 per tenant).</li><li><code>auth_migration_grace_days_remaining{tenant_id}</code> (gauge — periodic compute).</li></ul>\n<ol><li><strong>MUST</strong> support the <code>auth_provisioner</code> SQL role distinct from <code>cyberos_app</code> for INSERT into <code>auth_migration_state</code>. INSERT/UPDATE granted to <code>auth_provisioner</code>; UPDATE on <code>status</code> granted only to <code>auth_provisioner</code> (controlled cutover); REVOKE UPDATE, DELETE from cyberos_app.</li></ol>\n<ol><li><strong>MUST</strong> validate extension <code>additional_days</code> is integer in [1, 60]; outside → 400 <code>additional_days_out_of_range</code>.</li></ol>\n<ol><li><strong>MUST</strong> track in OBS sev-2 alarm when <code>auth_stub_token_rejected_total{tenant_id}</code> rate &gt; 100/h sustained over 1h. Rule lives in FR-OBS-007's rule set; this FR documents the alarm contract.</li></ol>\n<ol><li><strong>MUST</strong> persist the seed at FR-AUTH-101 ship time via a one-off migration that INSERTs <code>auth_migration_state</code> for every existing tenant. The migration is idempotent (uses <code>INSERT ... ON CONFLICT DO NOTHING</code>).</li></ol>\n<ol><li><strong>MUST</strong> make <code>days_remaining</code> calculation in <code>GET /preview</code> derive from <code>(cutover_at - now()).days</code> clipped at 0; reflect cutover_completed status correctly (returns 0 + status).</li></ol>\n<ol><li><strong>MUST</strong> emit <code>auth.cutover_completed</code> ONLY ONCE per tenant — the trigger uses <code>WHERE status != 'cutover_completed'</code> predicate to enforce. Subsequent requests after cutover see status=<code>cutover_completed</code> and the verifier rejects stub tokens immediately without re-emitting cutover row.</li></ol>\n<ol><li><strong>MUST</strong> keep cutover backward-flow forbidden — <code>cutover_completed → grace_active</code> rejection at trigger.</li></ol>\n<ol><li><strong>MUST</strong> allow operators to query <code>GET /v1/auth/migration/refresh-events?from=&lt;ts&gt;&amp;to=&lt;ts&gt;</code> for refresh-log inspection. Caller MUST have root-admin role.</li></ol>\n<ol><li><strong>MUST</strong> support env-driven <code>AUTH_MIGRATION_DEFAULT_GRACE_DAYS</code> (default 30; min 7; max 90 — enforced at startup).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why 30-day grace default (DEC-440)?</strong> Typical refresh-token cycle is 7 days; 30 days covers 4 refresh cycles + safety margin. Below 30 risks too many active stub tokens at cutover; above 30 delays the security benefit (rbac_v replay-resistance). 30 is the deliberate middle.</p>\n<p><strong>Why per-tenant cutover_at not global (DEC-442)?</strong> Different tenants ship at different dates. Existing tenants get cutover_at = ship_date + 30 days at FR-AUTH-101 ship; new tenants get cutover_at = provision_date + 30 days. Global cutover would force later-provisioned tenants into immediate cutover — operationally cruel.</p>\n<p><strong>Why one extension max per tenant (DEC-445)?</strong> Extension is escape valve for stuck client base, not a recurring policy lever. Allowing repeated extensions delays the security benefit indefinitely. One 60-day max extension = total 90 days possible — beyond that, the tenant needs to address whatever's blocking client refresh.</p>\n<p><strong>Why root-admin role for extension (DEC-445)?</strong> Cross-tenant operation (CyberSkill ops decides extension). Tenant-admin doesn't have authority — could grant indefinite stub-token validity for their own tenant.</p>\n<p><strong>Why clear 401 rejection body (DEC-448)?</strong> Clients seeing opaque 401 don't know to refresh. The body with <code>reason: \"stub_token_grace_window_expired\"</code> + <code>action_required: \"refresh\"</code> tells the client exactly what to do. Standard 401 alone leaves clients guessing.</p>\n<p><strong>Why refresh hook on FR-AUTH-004 path (DEC-449)?</strong> Refresh is the natural cutover point — old token returns access token + refresh token; new access token gets rbac_v injected. No client code change needed; refresh is opaque from client perspective.</p>\n<p><strong>Why 1% sampling on stub_token_accepted during grace + 100% near grace end (§1 #11)?</strong> Volume-vs-visibility tradeoff. Early in grace, expect millions of stub-token acceptances (cost issue if 100% logged). Near grace end, fewer remaining stub tokens — 100% gives operator visibility into which subjects are still stub.</p>\n<p><strong>Why cutover trigger on first post-grace request (§1 #10)?</strong> Lazy transition — no need for a scheduled job. The verifier path naturally observes <code>cutover_at &lt;= now()</code> + transitions status. Idempotent via the <code>status != 'cutover_completed'</code> predicate.</p>\n<p><strong>Why cache 60s TTL (§1 #14)?</strong> Verifier hot path can't afford per-request DB read. 60s cache means extension propagation has 60s lag (acceptable for ops events; not real-time).</p>\n<p><strong>Why preview API (§1 #8, DEC-447)?</strong> Operators decide whether to extend grace based on visibility. \"100 users still on stub tokens 7 days from cutover\" — extend. \"5 users on stub tokens\" — don't extend (those users need to refresh). Preview gives the data.</p>\n<p><strong>Why append-only migration_state (DEC-446)?</strong> Cutover history is forensic — \"when did this tenant's grace expire?\" is a compliance question. UPDATE on <code>status</code> is the only mutation needed; rest is INSERT-only (extension creates extension_count = 1 update + grace_extended_at update; both granted to auth_provisioner role).</p>\n<p><strong>Why sev-2 alarm at &gt; 100/h rejected (DEC-444)?</strong> Normal cutover should produce zero rejections (all subjects refreshed during grace). &gt; 100/h sustained signals (a) bulk client misbehaviour (didn't refresh), (b) operator missed the grace deadline, (c) deployment regression. Sev-2 = operator investigation.</p>\n<p><strong>Why <code>auth_provisioner</code> SQL role (§1 #17)?</strong> Same defense-in-depth pattern as FR-TEN-001. App code can't accidentally mutate migration state; only the explicit migration handler (with provisioner role) can.</p>\n<p><strong>Why grace bounds 7-90 days (DEC-440)?</strong> 7 days minimum covers one refresh cycle; below that some clients can't refresh in time. 90 days maximum prevents indefinite grace creep (defeats the security benefit). The env-driven default within these bounds is operator-tunable.</p>\n<p><strong>Why <code>extension_count</code> bounded at 1 (DEC-445)?</strong> SQL CHECK enforces; second extension attempt → 409. Hard cap.</p>\n<p><strong>Why PII-scrub <code>grace_extension_reason</code> (§1 #12)?</strong> Operator-supplied text may mention specific clients or stuck-user identifiers. Scrubbed in memory chain; raw in tenant-scoped Postgres.</p>\n<p><strong>Why <code>auth.cutover_completed</code> once per tenant (§1 #22, §1 #10)?</strong> Idempotent state transition. Trigger predicate <code>status != 'cutover_completed'</code> ensures single emission. Subsequent rejections continue to emit <code>auth.stub_token_rejected</code> for each event.</p>\n<p><strong>Why no automatic cutover scheduling job (§1 #10)?</strong> Verifier-driven lazy transition is simpler: no cron, no missed-window risk, transition happens on first stub-token request after grace expiry. The first rejection IS the cutover.</p>\n<p><strong>Why FR-AUTH-109 has no <code>blocks: []</code>?</strong> FR-AUTH-101's contract claims the grace window will be enforced; FR-AUTH-109 ships the enforcer. No downstream FR needs to know about FR-AUTH-109 — it's the implementation of an existing promise.</p>\n<p><strong>Why refresh log captures <code>prior_rbac_v_present</code>?</strong> Operator analytics: \"how many refreshes converted from stub to full?\" — the count of <code>prior_rbac_v_present=false</code> rows = exactly the number of stub-to-full conversions. Useful for predicting cutover readiness.</p>\n<p><strong>Why <code>additional_days</code> max 60 (DEC-445)?</strong> Beyond 60 days, the operator should be addressing root causes (client refresh broken, deployment stuck). 60-day cap prevents indefinite extension-as-policy.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0014 — auth_migration_state</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0014_auth_migration_state.sql\n\nBEGIN;\n\nCREATE TABLE auth_migration_state (\n    tenant_id                UUID         PRIMARY KEY,\n    cutover_at               TIMESTAMPTZ  NOT NULL,\n    status                   TEXT         NOT NULL CHECK (status IN ('grace_active','grace_extended','cutover_completed')),\n    grace_extended_at        TIMESTAMPTZ,\n    grace_extension_reason   TEXT         CHECK (grace_extension_reason IS NULL OR length(grace_extension_reason) BETWEEN 1 AND 500),\n    extension_count          INT          NOT NULL DEFAULT 0 CHECK (extension_count BETWEEN 0 AND 1),\n    created_at               TIMESTAMPTZ  NOT NULL DEFAULT now()\n);\n\nALTER TABLE auth_migration_state ENABLE ROW LEVEL SECURITY;\nCREATE POLICY auth_migration_state_root_admin ON auth_migration_state\n    USING (current_setting('auth.is_root_admin', true) = 'true'\n           OR tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (current_setting('auth.is_root_admin', true) = 'true');\n\nREVOKE UPDATE, DELETE ON auth_migration_state FROM cyberos_app;\n\n-- auth_provisioner role for cutover + extension flows\nCREATE ROLE auth_provisioner;\nGRANT INSERT ON auth_migration_state TO auth_provisioner;\nGRANT UPDATE (cutover_at, status, grace_extended_at, grace_extension_reason, extension_count) ON auth_migration_state TO auth_provisioner;\nGRANT SELECT ON auth_migration_state TO cyberos_app, auth_provisioner;\n\n-- Immutability post-cutover (DEC-442)\nCREATE OR REPLACE FUNCTION enforce_cutover_immutable() RETURNS TRIGGER AS $$\nBEGIN\n    IF OLD.status = 'cutover_completed' AND NEW.cutover_at IS DISTINCT FROM OLD.cutover_at THEN\n        RAISE EXCEPTION 'cutover_immutable_post_completion' USING ERRCODE = 'P0080';\n    END IF;\n    IF OLD.status = 'cutover_completed' AND NEW.status = 'grace_active' THEN\n        RAISE EXCEPTION 'cutover_status_backward_forbidden' USING ERRCODE = 'P0081';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_migration_state_immutable BEFORE UPDATE ON auth_migration_state\n    FOR EACH ROW EXECUTE FUNCTION enforce_cutover_immutable();\n\n-- Seed for existing tenants at ship time (idempotent)\nINSERT INTO auth_migration_state (tenant_id, cutover_at, status)\nSELECT id, now() + INTERVAL '30 days', 'grace_active' FROM tenants\nON CONFLICT (tenant_id) DO NOTHING;\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0015 — refresh_log</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0015_auth_token_refresh_log.sql\n\nBEGIN;\n\nCREATE TABLE auth_token_refresh_log (\n    id                       BIGSERIAL    PRIMARY KEY,\n    tenant_id                UUID         NOT NULL,\n    subject_id               UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    prior_rbac_v_present     BOOLEAN      NOT NULL,\n    new_rbac_v               INT          NOT NULL,\n    refreshed_at             TIMESTAMPTZ  NOT NULL DEFAULT now()\n);\n\nCREATE INDEX refresh_log_tenant_refreshed_idx ON auth_token_refresh_log (tenant_id, refreshed_at DESC);\nCREATE INDEX refresh_log_prior_present_idx ON auth_token_refresh_log (tenant_id, prior_rbac_v_present, refreshed_at DESC);\n\nALTER TABLE auth_token_refresh_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY refresh_log_tenant_iso ON auth_token_refresh_log\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid\n           OR current_setting('auth.is_root_admin', true) = 'true')\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON auth_token_refresh_log FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Migration state lookup</h3>\n<pre><code class=\"language-rust\">// services/auth/src/migration/state.rs\nuse chrono::{DateTime, Utc};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone)]\npub struct MigrationState {\n    pub tenant_id: Uuid,\n    pub cutover_at: DateTime&lt;Utc&gt;,\n    pub status: MigrationStatus,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum MigrationStatus { GraceActive, GraceExtended, CutoverCompleted }\n\npub struct MigrationStateCache {\n    inner: Arc&lt;RwLock&lt;HashMap&lt;Uuid, (MigrationState, Instant)&gt;&gt;&gt;,\n}\n\nimpl MigrationStateCache {\n    pub async fn get(&amp;self, tenant_id: Uuid, db: &amp;PgPool) -&gt; anyhow::Result&lt;MigrationState&gt; {\n        // Cache check\n        if let Some((state, t)) = self.inner.read().await.get(&amp;tenant_id) {\n            if t.elapsed() &lt; Duration::from_secs(60) {\n                return Ok(state.clone());\n            }\n        }\n        // DB load\n        let row: (Uuid, DateTime&lt;Utc&gt;, String) = sqlx::query_as(\n            \"SELECT tenant_id, cutover_at, status FROM auth_migration_state WHERE tenant_id = $1\"\n        ).bind(tenant_id).fetch_one(db).await?;\n        let state = MigrationState {\n            tenant_id: row.0,\n            cutover_at: row.1,\n            status: match row.2.as_str() {\n                \"grace_active\" =&gt; MigrationStatus::GraceActive,\n                \"grace_extended\" =&gt; MigrationStatus::GraceExtended,\n                \"cutover_completed\" =&gt; MigrationStatus::CutoverCompleted,\n                _ =&gt; anyhow::bail!(\"unknown_status\"),\n            },\n        };\n        self.inner.write().await.insert(tenant_id, (state.clone(), Instant::now()));\n        Ok(state)\n    }\n\n    pub async fn invalidate(&amp;self, tenant_id: Uuid) {\n        self.inner.write().await.remove(&amp;tenant_id);\n    }\n}</code></pre>\n<h3>3.4 — Verifier hook</h3>\n<pre><code class=\"language-rust\">// services/auth/src/migration/verifier_hook.rs\nuse chrono::Utc;\nuse crate::jwt::Claims;\nuse crate::migration::state::{MigrationState, MigrationStatus, MigrationStateCache};\n\n#[derive(Debug, thiserror::Error)]\npub enum StubTokenRejection {\n    #[error(\"stub_token_grace_window_expired\")]\n    GraceExpired { cutover_at: chrono::DateTime&lt;chrono::Utc&gt; },\n}\n\npub async fn check_stub_token(\n    claims: &amp;Claims,\n    cache: &amp;MigrationStateCache,\n    db: &amp;sqlx::PgPool,\n    audit: &amp;impl AuditEmitter,\n) -&gt; Result&lt;(), StubTokenRejection&gt; {\n    // If token has rbac_v → not a stub; nothing to check.\n    if claims.rbac_v.is_some() { return Ok(()); }\n\n    let state = cache.get(claims.tid, db).await\n        .expect(\"migration state must exist for every tenant\");\n\n    let now = Utc::now();\n    if state.status == MigrationStatus::CutoverCompleted || state.cutover_at &lt;= now {\n        // Reject + emit audit\n        audit.emit_stub_token_rejected(claims.tid, claims.sub, state.cutover_at).await;\n        // If we just crossed the threshold, transition status\n        if state.status != MigrationStatus::CutoverCompleted {\n            tokio::spawn(transition_to_cutover_completed(claims.tid, db.clone(), cache.clone()));\n        }\n        return Err(StubTokenRejection::GraceExpired { cutover_at: state.cutover_at });\n    }\n\n    // Still in grace — accept; sample audit at 1% (100% in last 24h)\n    let days_remaining = (state.cutover_at - now).num_days();\n    let should_audit = if days_remaining &lt;= 1 { true } else { rand::random::&lt;f32&gt;() &lt; 0.01 };\n    if should_audit {\n        audit.emit_stub_token_accepted(claims.tid, claims.sub, days_remaining).await;\n    }\n    Ok(())\n}\n\nasync fn transition_to_cutover_completed(\n    tenant_id: Uuid, db: sqlx::PgPool, cache: MigrationStateCache,\n) {\n    let rows = sqlx::query(r#\"\n        UPDATE auth_migration_state SET status = 'cutover_completed'\n        WHERE tenant_id = $1 AND status != 'cutover_completed'\n    \"#).bind(tenant_id).execute(&amp;db).await;\n    if let Ok(r) = rows {\n        if r.rows_affected() == 1 {\n            // First time crossing — emit cutover_completed audit row\n            cache.invalidate(tenant_id).await;\n            // ... emit auth.cutover_completed\n        }\n    }\n}</code></pre>\n<h3>3.5 — Refresh hook</h3>\n<pre><code class=\"language-rust\">// services/auth/src/migration/refresh_hook.rs\nuse crate::jwt::Claims;\n\npub async fn on_refresh(\n    prior_claims: &amp;Claims,\n    new_claims: &amp;mut Claims,\n    rbac_v: u32,\n    db: &amp;sqlx::PgPool,\n) -&gt; anyhow::Result&lt;()&gt; {\n    let prior_present = prior_claims.rbac_v.is_some();\n\n    // Always inject current rbac_v on refresh (DEC-449)\n    new_claims.rbac_v = Some(rbac_v);\n\n    sqlx::query(r#\"\n        INSERT INTO auth_token_refresh_log (tenant_id, subject_id, prior_rbac_v_present, new_rbac_v)\n        VALUES ($1, $2, $3, $4)\n    \"#).bind(new_claims.tid).bind(new_claims.sub).bind(prior_present).bind(rbac_v as i32)\n       .execute(db).await?;\n\n    Ok(())\n}</code></pre>\n<h3>3.6 — Extend grace handler</h3>\n<pre><code class=\"language-rust\">// services/auth/src/handlers/migration.rs\nuse axum::{Json, extract::State, http::StatusCode};\nuse cyberos_auth::rbac::Role;\n\n#[derive(Deserialize)]\npub struct ExtendGraceRequest {\n    pub additional_days: i32,\n    pub reason: String,\n}\n\npub async fn extend_grace(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Json(req): Json&lt;ExtendGraceRequest&gt;,\n) -&gt; Result&lt;StatusCode, ApiError&gt; {\n    // (1) Role check\n    if !claims.roles().contains(&amp;Role::RootAdmin) {\n        return Err(ApiError::PermissionDenied);\n    }\n    // (2) Validate\n    if !(1..=60).contains(&amp;req.additional_days) {\n        return Err(ApiError::AdditionalDaysOutOfRange);\n    }\n    if req.reason.is_empty() || req.reason.len() &gt; 500 {\n        return Err(ApiError::ReasonInvalid);\n    }\n    // (3) Apply with provisioner role\n    let mut tx = state.db.begin().await?;\n    let updated = sqlx::query(r#\"\n        UPDATE auth_migration_state\n        SET cutover_at = cutover_at + ($2 || ' days')::interval,\n            status = 'grace_extended',\n            grace_extended_at = now(),\n            grace_extension_reason = $3,\n            extension_count = extension_count + 1\n        WHERE tenant_id = $1\n          AND status = 'grace_active'\n          AND extension_count = 0\n        RETURNING cutover_at\n    \"#).bind(claims.tenant_id()).bind(req.additional_days as i64).bind(&amp;req.reason)\n       .execute(&amp;mut *tx).await?;\n    if updated.rows_affected() == 0 {\n        return Err(ApiError::AlreadyExtended);\n    }\n    audit::emit_grace_window_extended(&amp;mut tx, claims.tenant_id(), req.additional_days, &amp;req.reason, claims.subject_id()).await?;\n    tx.commit().await?;\n    state.cache.invalidate(claims.tenant_id()).await;\n    Ok(StatusCode::OK)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Migration state seeded on FR-AUTH-101 ship</strong> — every existing tenant gets cutover_at = now+30d.</li><li><strong>New tenants seeded on provisioning</strong> — cutover_at = provision_time + 30d via FR-TEN-001 hook.</li><li><strong>Stub token accepted during grace</strong> — verifier returns OK; <code>auth.stub_token_accepted</code> row sampled 1%.</li><li><strong>Stub token rejected post-grace</strong> — 401 with body per DEC-448; <code>auth.stub_token_rejected</code> memory row.</li><li><strong>rbac_v injected on refresh</strong> — refreshed token includes rbac_v; refresh log row with <code>prior_rbac_v_present=false</code>.</li><li><strong>Extension by root-admin</strong> — 200; status → grace_extended; extension_count=1.</li><li><strong>Extension by non-root-admin</strong> → 403.</li><li><strong>Second extension attempt</strong> → 409 already_extended.</li><li><strong>additional_days &lt; 1 or &gt; 60</strong> → 400 additional_days_out_of_range.</li><li><strong>Empty reason</strong> → 400 reason_invalid.</li><li><strong>cutover_completed status forbids cutover_at change</strong> → trigger raises cutover_immutable_post_completion.</li><li><strong>cutover_completed backward to grace_active forbidden</strong> → trigger raises cutover_status_backward_forbidden.</li><li><strong>Cutover triggered on first post-grace verification</strong> — status flips atomically; <code>auth.cutover_completed</code> row emitted once.</li><li><strong>Cutover NOT re-emitted on subsequent rejections</strong> — trigger predicate prevents.</li><li><strong>Preview API</strong> — returns counts + days_remaining + can_extend.</li><li><strong>Preview can_extend false after extension</strong> — extension_count=1.</li><li><strong>OBS sev-2 at &gt; 100/h rejections</strong> — alarm rule fires.</li><li><strong>append-only migration_state from cyberos_app</strong> — UPDATE blocked.</li><li><strong>append-only refresh_log from cyberos_app</strong> — UPDATE/DELETE blocked.</li><li><strong>State cache 60s TTL</strong> — invalidation on extension propagates within 60s.</li><li><strong>Verifier hook &lt; 5ms p99</strong> — perf test.</li><li><strong>OTel span <code>auth.migration.verify_stub</code> emitted</strong> with outcome.</li><li><strong>Counter <code>auth_stub_token_rejected_total{tenant_id}</code> increments</strong> on every rejection.</li><li><strong>PII-scrubbed reason in memory row</strong>.</li><li><strong>Env-driven default grace days</strong> — AUTH_MIGRATION_DEFAULT_GRACE_DAYS=14 → new tenants get 14d.</li><li><strong>Env value &lt; 7 or &gt; 90 rejected at startup</strong> — service refuses.</li><li><strong><code>GET /migration/refresh-events</code> returns log rows</strong> — root-admin only.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/jwt_token_endpoint_test.rs\n#[tokio::test]\nasync fn stub_token_accepted_with_5_days_remaining(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_with_cutover_in_days(5).await;\n    let stub_token = ctx.issue_stub_token_for(tenant).await;  // no rbac_v\n    let resp = ctx.get(\"/v1/some/protected-endpoint\").bearer(&amp;stub_token).await;\n    assert_eq!(resp.status(), 200);\n    let rows = ctx.memory_audit_rows(\"auth.stub_token_accepted\").await;\n    // 1% sampling — may or may not have the row; assert no rejection\n    let rejections = ctx.memory_audit_rows(\"auth.stub_token_rejected\").await;\n    assert_eq!(rejections.len(), 0);\n}\n\n#[tokio::test]\nasync fn stub_token_accepted_100pct_in_last_24h(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_with_cutover_in_hours(12).await;  // &lt; 24h remaining\n    for _ in 0..10 {\n        let stub_token = ctx.issue_stub_token_for(tenant).await;\n        let _ = ctx.get(\"/v1/some/protected-endpoint\").bearer(&amp;stub_token).await;\n    }\n    let rows = ctx.memory_audit_rows(\"auth.stub_token_accepted\").await;\n    assert_eq!(rows.len(), 10);  // 100% sampling near end\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/jwt_token_endpoint_test.rs\n#[tokio::test]\nasync fn post_grace_stub_token_rejected_with_clear_reason(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_with_past_cutover().await;\n    let stub_token = ctx.issue_stub_token_for(tenant).await;\n    let resp = ctx.get(\"/v1/some/protected-endpoint\").bearer(&amp;stub_token).await;\n    assert_eq!(resp.status(), 401);\n    let body: serde_json::Value = resp.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"rbac_version_required\");\n    assert_eq!(body[\"reason\"], \"stub_token_grace_window_expired\");\n    assert!(body[\"grace_expired_at\"].is_string());\n    assert_eq!(body[\"action_required\"], \"refresh\");\n}\n\n#[tokio::test]\nasync fn first_post_grace_request_triggers_cutover(ctx: TestCtx) {\n    let tenant = ctx.create_tenant_with_past_cutover_no_completion().await;\n    let stub_token = ctx.issue_stub_token_for(tenant).await;\n    let _ = ctx.get(\"/protected\").bearer(&amp;stub_token).await;\n    tokio::time::sleep(Duration::from_millis(100)).await;  // async transition\n    let state = ctx.fetch_migration_state(tenant).await;\n    assert_eq!(state.status, \"cutover_completed\");\n    let rows = ctx.memory_audit_rows(\"auth.cutover_completed\").await;\n    assert_eq!(rows.len(), 1);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/geoip_test.rs\n#[tokio::test]\nasync fn second_extension_rejected(ctx: TestCtx) {\n    let tenant = ctx.tenant_in_grace().await;\n    ctx.post_as_root_admin(\"/v1/auth/migration/extend-grace\",\n        json!({\"additional_days\": 30, \"reason\": \"first extension\"})).await.unwrap();\n    let err = ctx.post_as_root_admin(\"/v1/auth/migration/extend-grace\",\n        json!({\"additional_days\": 14, \"reason\": \"second extension\"})).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"already_extended\"));\n}\n\n#[tokio::test]\nasync fn tenant_admin_cannot_extend(ctx: TestCtx) {\n    let tenant = ctx.tenant_in_grace().await;\n    let err = ctx.post_as_tenant_admin(\"/v1/auth/migration/extend-grace\",\n        json!({\"additional_days\": 30, \"reason\": \"try\"})).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"permission_denied\"));\n}\n\n#[tokio::test]\nasync fn additional_days_out_of_range(ctx: TestCtx) {\n    for days in [0, 61, -5] {\n        let err = ctx.post_as_root_admin(\"/v1/auth/migration/extend-grace\",\n            json!({\"additional_days\": days, \"reason\": \"x\"})).await.unwrap_err();\n        assert!(format!(\"{err:?}\").contains(\"additional_days_out_of_range\"));\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/refresh_hook_injects_rbac_v_test.rs\n#[tokio::test]\nasync fn refresh_of_stub_token_injects_rbac_v(ctx: TestCtx) {\n    let stub_token = ctx.issue_stub_token().await;\n    let refresh_token = ctx.issue_refresh_token().await;\n    let resp = ctx.post(\"/v1/auth/refresh\", json!({\"refresh_token\": refresh_token})).await;\n    let new_access: String = resp.json().await.unwrap()[\"access_token\"].as_str().unwrap().into();\n    let claims = ctx.decode_claims(&amp;new_access);\n    assert!(claims.rbac_v.is_some());\n    let log = ctx.fetch_refresh_log_for_subject(ctx.test_subject()).await;\n    assert!(log.iter().any(|r| !r.prior_rbac_v_present));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/cutover_immutable_test.rs\n#[sqlx::test]\nasync fn cutover_at_immutable_post_completion(pool: sqlx::PgPool) {\n    let tid = seed_migration_state(&amp;pool, \"cutover_completed\", chrono::Utc::now() - chrono::Duration::days(5)).await;\n    set_role_provisioner(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE auth_migration_state SET cutover_at = now() + interval '30 days' WHERE tenant_id = $1\")\n        .bind(tid).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"cutover_immutable_post_completion\"));\n}\n\n#[sqlx::test]\nasync fn cutover_backward_forbidden(pool: sqlx::PgPool) {\n    let tid = seed_migration_state(&amp;pool, \"cutover_completed\", chrono::Utc::now() - chrono::Duration::days(5)).await;\n    set_role_provisioner(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE auth_migration_state SET status = 'grace_active' WHERE tenant_id = $1\")\n        .bind(tid).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"cutover_status_backward_forbidden\"));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 4 memory row builders follow the canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>FR-AUTH-101</strong> — RBAC catalogue; consumes the <code>rbac_v</code> claim contract from §1 #18 of that FR.</li></ul>\n<p><strong>Downstream:</strong> none — this FR is the implementation of FR-AUTH-101's grace-window promise.</p>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>FR-AUTH-004</strong> — JWT verifier + refresh path (this FR hooks both).</li><li><strong>FR-AI-003</strong> — memory audit bridge.</li><li><strong>FR-MEMORY-111</strong> — PII scrub of grace_extension_reason.</li><li><strong>FR-OBS-007</strong> — sev-2 alarm on &gt; 100/h rejections.</li><li><strong>FR-TEN-001</strong> — provisioning hook that seeds per-tenant cutover_at.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — 401 rejection response post-grace</h3>\n<pre><code class=\"language-json\">{\n  \"error\": \"rbac_version_required\",\n  \"reason\": \"stub_token_grace_window_expired\",\n  \"grace_expired_at\": \"2026-06-15T00:00:00Z\",\n  \"action_required\": \"refresh\"\n}</code></pre>\n<h3>8.2 — POST /v1/auth/migration/extend-grace</h3>\n<pre><code class=\"language-json\">{\n  \"additional_days\": 30,\n  \"reason\": \"Bulk client refresh stalled due to mobile-app store review delay\"\n}</code></pre>\n<h3>8.3 — GET /v1/auth/migration/preview response</h3>\n<pre><code class=\"language-json\">{\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"cutover_at\": \"2026-06-15T00:00:00Z\",\n  \"status\": \"grace_active\",\n  \"days_remaining\": 14,\n  \"stub_tokens_accepted_count_24h\": 1240,\n  \"stub_tokens_rejected_count_24h\": 0,\n  \"refreshed_count_24h\": 380,\n  \"extension_count\": 0,\n  \"can_extend\": true\n}</code></pre>\n<h3>8.4 — auth.stub_token_rejected memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.stub_token_rejected\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"cutover_at\": \"2026-06-15T00:00:00Z\",\n  \"now\": \"2026-06-16T10:30:00Z\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.5 — auth.grace_window_extended memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.grace_window_extended\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"prior_cutover_at\": \"2026-06-15T00:00:00Z\",\n  \"new_cutover_at\": \"2026-07-15T00:00:00Z\",\n  \"additional_days\": 30,\n  \"reason_scrubbed\": \"Bulk client refresh stalled due to [REDACTED-CONTEXT]\",\n  \"extended_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.6 — auth.cutover_completed memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.cutover_completed\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"final_cutover_at\": \"2026-06-15T00:00:00Z\",\n  \"total_stub_accepted\": 28400,\n  \"total_stub_rejected\": 0,\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Token revocation API</strong> — FR-AUTH-2xx; this FR's rejection doesn't revoke (token is rejected at verify but the token itself isn't on a CRL).</li><li><strong>Per-subject grace override</strong> — slice 4; useful for specific bot/service accounts.</li><li><strong>Automated extension recommendation</strong> — slice 4; preview API gives data; operator decides.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Stub token post-grace</td><td>verifier hook</td><td>401 + audit</td><td>Refresh</td></tr><tr><td>Stub token during grace</td><td>verifier hook</td><td>accept + sampled audit</td><td>Designed</td></tr><tr><td>Migration state missing for tenant</td><td>DB lookup fail</td><td>500 + sev-1</td><td>Seed via one-off SQL</td></tr><tr><td>Cache stale during extension</td><td>60s TTL + invalidate</td><td>Brief inconsistency</td><td>Designed</td></tr><tr><td>Trigger blocks legitimate UPDATE</td><td>test catches</td><td>CI fails</td><td>Fix predicate</td></tr><tr><td>Concurrent cutover transition</td><td>UPDATE WHERE status != 'completed'</td><td>One wins; idempotent</td><td>Designed</td></tr><tr><td>Extension &gt; 60 days</td><td>handler validate</td><td>400</td><td>Designed</td></tr><tr><td>Extension &lt; 1 day</td><td>handler validate</td><td>400</td><td>Designed</td></tr><tr><td>Second extension</td><td>UPDATE WHERE extension_count = 0</td><td>0 rows; 409</td><td>Designed</td></tr><tr><td>Reason &gt; 500 chars</td><td>handler validate + DB CHECK</td><td>400</td><td>Shorten</td></tr><tr><td>Non-root-admin extension</td><td>role check</td><td>403</td><td>Designed</td></tr><tr><td>Cutover_at backward UPDATE</td><td>trigger</td><td>rejected</td><td>Designed</td></tr><tr><td>Cutover_completed → grace_active</td><td>trigger</td><td>rejected</td><td>Designed</td></tr><tr><td>Refresh log UPDATE/DELETE from app</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>Migration state UPDATE from app</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>Env grace days &lt; 7 or &gt; 90</td><td>startup check</td><td>service refuses</td><td>Fix env</td></tr><tr><td>memory audit emit fails</td><td>tx rollback</td><td>500; verifier retries</td><td>memory_writer health</td></tr><tr><td>Preview can_extend wrong</td><td>unit test</td><td>CI fails</td><td>Fix logic</td></tr><tr><td>Refresh hook fails to inject rbac_v</td><td>unit test</td><td>CI fails</td><td>Fix hook</td></tr><tr><td>Cache invalidation race</td><td>next refresh observes</td><td>Brief stale</td><td>Designed</td></tr><tr><td>Cutover audit emitted twice</td><td>predicate check</td><td>UPDATE 0 rows on second</td><td>Designed</td></tr><tr><td>Concurrent extension attempts</td><td>first wins; second 409</td><td>Designed</td><td>None</td></tr><tr><td>Subject deleted while in refresh log</td><td>FK RESTRICT</td><td>DELETE auth.subjects fails</td><td>Soft-delete subject</td></tr><tr><td>RLS bypass</td><td>USING</td><td>0 rows</td><td>Designed</td></tr><tr><td>OTel span attribute missing</td><td>otel_test</td><td>CI fails</td><td>Fix</td></tr><tr><td>Counter <code>auth_stub_token_rejected_total</code> not incrementing</td><td>metric test</td><td>CI fails</td><td>Fix instrumentation</td></tr><tr><td>Sev-2 alarm threshold drift</td><td>OBS rule version</td><td>None</td><td>Coordinate with FR-OBS-007</td></tr><tr><td>Verifier hook latency spike</td><td>perf test</td><td>CI fails</td><td>Optimise cache path</td></tr><tr><td>Migration state insert fails on new tenant provisioning</td><td>hook fail</td><td>Tenant created but cutover_at NULL</td><td>Manual SQL seed + audit</td></tr><tr><td><code>additional_days</code> integer overflow</td><td>i32 + range check</td><td>None</td><td>Designed</td></tr><tr><td>Refresh log table size growth</td><td>partition (deferred)</td><td>None at slice 1</td><td>FR-OBS-006 retention rule</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>30-day default grace</strong> — covers 4 refresh cycles + safety margin.</li><li><strong>Per-tenant cutover_at</strong> — fairness for newly-provisioned tenants.</li><li><strong>One extension max per tenant</strong> — escape valve, not recurring policy.</li><li><strong>Root-admin only for extension</strong> — cross-tenant operation.</li><li><strong>Clear 401 rejection body</strong> — client knows to refresh.</li><li><strong>Refresh hook on FR-AUTH-004 path</strong> — transparent client-side cutover.</li><li><strong>1% sampling during grace + 100% in last 24h</strong> — volume-vs-visibility.</li><li><strong>Lazy cutover transition on first post-grace request</strong> — no cron needed.</li><li><strong>60s cache TTL</strong> — hot path performance; ops events tolerate lag.</li><li><strong>Preview API</strong> — operator decision support.</li><li><strong>Append-only migration_state via auth_provisioner role split</strong> — app code can't mutate.</li><li><strong>Sev-2 at &gt; 100/h rejected</strong> — operator investigation prompt.</li><li><strong>Idempotent cutover audit via UPDATE WHERE status !=</strong> — single emission per tenant.</li><li><strong>Cutover backward-flow forbidden</strong> — semantic correctness.</li><li><strong>Refresh log per-row analytics</strong> — \"how many stub → full conversions?\".</li><li><strong><code>additional_days</code> capped 1-60</strong> — bounded extension.</li><li><strong><code>extension_count</code> CHECK 0-1</strong> — DB-enforced max 1.</li><li><strong>PII scrub reason</strong> — operator may type sensitive context.</li><li><strong>Env-driven default with min 7 / max 90</strong> — operator tunability with bounds.</li><li><strong>Verifier hook &lt; 5ms p99</strong> — hot path budget.</li><li><strong><code>auth_provisioner</code> role split</strong> — defense in depth.</li></ul>\n<hr>\n<p><em>End of FR-AUTH-109.</em></p>";
