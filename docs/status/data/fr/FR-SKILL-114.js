window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SKILL-114"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>This FR introduces the <code>BASELINE.md</code> artefact — a per-skill design-time performance comparison written at v0.x → v1.0 promotion. It closes the Anthropic guide Chapter 2 \"performance comparison\" measurement layer (per <code>modules/skill/ANTHROPIC_GUIDE_DIGEST.md</code> §6.4) that CyberOS currently lacks at design time (it tracks the metrics in production via OBS but doesn't fossilise the promotion-time justification).</p>\n<ol><li>Every skill at <code>skill_version: 1.0.0</code> or higher <strong>MUST</strong> carry a <code>BASELINE.md</code> file at <code>&lt;skill-folder&gt;/BASELINE.md</code> (sibling of SKILL.md). The file documents the design-time performance comparison + the trust justification that earned the promotion.</li><li>The file's frontmatter <strong>MUST</strong> declare: <code>skill_id</code> (kebab-case folder name) + <code>baseline_version: &lt;semver&gt;</code> (the baseline doc's own version, independent of skill_version) + <code>baseline_measured_at: &lt;ISO 8601 with timezone&gt;</code> + <code>attested_by: &lt;persona-id | human:&lt;id&gt;&gt;</code> (who signed off) + <code>next_review_due: &lt;ISO 8601&gt;</code> (when this baseline should be re-measured — default +12 months).</li><li>The body <strong>MUST</strong> contain six sections in order: <code>## Workflow under test</code>, <code>## Without-skill baseline</code>, <code>## With-skill measurements</code>, <code>## Token-budget transparency</code>, <code>## Trust calibration</code>, <code>## Authoring notes</code>.</li><li><code>## Without-skill baseline</code> <strong>MUST</strong> contain three numbered measurements: (a) <strong>tool-call count</strong> (how many tool invocations the workflow takes when a human runs it without this skill), (b) <strong>token count</strong> (total prompt + completion tokens consumed), (c) <strong>failure rate</strong> (% of attempts that ended in user redirection or abandonment). Each MUST cite the measurement methodology + sample size (n=N) + measurement window (date range).</li><li><code>## With-skill measurements</code> <strong>MUST</strong> mirror the three measurements with the skill enabled, computed against the same sample set + window. Numbers MUST come from real measurements (not estimates); the <code>attested_by</code> field's signoff certifies this. Optionally a fourth measurement: <strong>iteration count</strong> (per-artefact auto-refinement iterations to convergence — relevant for audit-loop skills).</li><li>The skill is <strong>eligible for v1.0 promotion</strong> if all of: tool-call ratio (with / without) ≤ 0.7 (skill reduces tool calls by ≥30%); token ratio ≤ 0.7; failure-rate ratio ≤ 0.5 (skill cuts failure rate by ≥50%). These thresholds are calibrated to the Anthropic guide Chapter 2 p. 9 \"Quantitative metrics\" guidance (90% trigger rate, X tool calls, 0 failed API calls). Skills not meeting all three thresholds <strong>MAY</strong> still be promoted with explicit operator override + reason captured in <code>## Authoring notes</code>.</li><li><code>## Token-budget transparency</code> <strong>MUST</strong> declare the skill's expected token budget per invocation (prompt + completion bounded; mean + 95th percentile). Operators reading the budget can decide whether to enable the skill in token-constrained sessions.</li><li><code>## Trust calibration</code> <strong>MUST</strong> declare the skill's <code>confidence_band.default</code> value + the rationale (why 0.7 vs 0.9, etc.) + the <code>defer_below</code> trigger threshold + the empirical acceptance rate observed during the v0.x measurement window. This section is the audit anchor — six months later, when acceptance rate drifts, this section is the reference point.</li><li><code>## Authoring notes</code> <strong>MAY</strong> contain: measurement caveats, edge cases not covered, known failure modes deferred to a v1.1 follow-up, attestation chain (who reviewed the baseline before the operator attestation).</li><li>The auditor rule <strong>MUST</strong> be <code>FM-114 baseline-present-at-v1</code> with severity <code>error</code> for <code>skill_version &gt;= 1.0.0</code>; severity <code>info</code> (advisory) for v0.x. The rule fires at promotion-time CI gate, not at every fine-tune cycle.</li><li>The Rust broker (FR-SKILL-103) <strong>MUST</strong> check <code>BASELINE.md</code> presence when <code>skill_version &gt;= 1.0.0</code> AND <code>exposable_as.partner_connector: true</code>. Missing baseline on a partner-flagged skill rejects load with <code>FrontmatterError::PartnerWithoutBaseline</code>.</li><li>The <code>attested_by</code> field's identity <strong>MUST</strong> be either a CyberOS persona id (e.g. <code>cuo-cpo</code> for CPO-attested baselines) or a <code>human:&lt;id&gt;</code> form (e.g. <code>human:stephen-cheng</code>). The attestation chain is part of the audit log (per AGENTS.md §7) — the <code>op:\"baseline.attested\"</code> row records the signoff.</li><li><code>next_review_due</code> <strong>MUST</strong> be set; default +12 months. When the date passes, the auditor rule severity escalates from <code>info</code> (compliant) to <code>warning</code> (review overdue). After 24 months without re-measurement, the rule escalates to <code>error</code> (stale baseline).</li><li>Existing skills at v1.0+ (only <code>cuo/_shared/hello-world</code> at v1.0.0 today per <code>MODULE.md</code>) <strong>MUST</strong> be backfilled with a retrospective BASELINE.md as part of this FR's implementation. Future v1.0 promotions ship with BASELINE.md at the promotion-commit boundary; lazy backfill is not allowed (the artefact's whole point is to justify <em>this</em> promotion).</li><li>The validation pyramid in <code>modules/skill/README.md</code> Part 13.1 <strong>MUST</strong> be updated to reference BASELINE.md at the promotion gate (Layer 3 — operational telemetry now anchors against the design-time baseline). The pyramid diagram's third layer caption updates from \"production telemetry\" to \"production telemetry vs design-time baseline\".</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why one artefact per skill rather than a catalog-wide spreadsheet (§1 #1)?</strong> Per-skill BASELINE.md travels with the skill folder — when a skill is exported (per the AGENTS.md §11 plug-in contract), the baseline travels with it. Catalog-wide spreadsheets diverge from per-skill state over time. The single source of truth lives next to SKILL.md.</p>\n<p><strong>Why six body sections (§1 #3)?</strong> Each section answers a question a future operator asks: (a) what does this skill do? (Workflow under test), (b) what's the cost without it? (Without-skill), (c) what's the cost with it? (With-skill), (d) what's the runtime cost? (Token-budget), (e) why do we trust it? (Trust calibration), (f) what's not measured? (Authoring notes). Fewer sections lose context; more sections inflate authoring cost.</p>\n<p><strong>Why the 30% / 30% / 50% threshold split (§1 #6)?</strong> Empirical, derived from the Anthropic guide Chapter 3 baseline-comparison example (p. 16): \"15 back-and-forth messages → 2 clarifying questions; 12,000 tokens → 6,000 tokens; 3 failed API calls → 0 failed API calls\". The example shows ~87% reduction in tool calls, 50% in tokens, 100% in failures. CyberOS's thresholds (30% / 30% / 50%) are conservative — easier to meet, but still demonstrate real value. Stricter thresholds would reject legitimate-but-marginal skills; looser would let weak skills slip through. The thresholds are configurable per skill in §1 #6's override clause.</p>\n<p><strong>Why operator override is permitted (§1 #6)?</strong> Some skills earn promotion on dimensions the baseline doesn't quantify — for example, audit skills whose value is \"catches violations a human would miss\" rather than \"reduces tool calls\". The override captures the reason; the auditor records the override + reason in the audit chain.</p>\n<p><strong>Why token-budget transparency (§1 #7)?</strong> Operators running in token-constrained sessions (free tier, mobile contexts) need to know which skills are budget-aware. A skill claiming \"saves 50% tokens vs baseline\" might still consume 50,000 tokens — important to surface.</p>\n<p><strong>Why explicit trust calibration section (§1 #8)?</strong> This section answers \"why is <code>confidence_band.default: 0.7</code> the right value for this skill?\". Without it, six months later when acceptance drifts, the operator can't tell whether the original calibration was wrong or whether the underlying user behaviour changed. The section anchors the audit chain.</p>\n<p><strong>Why FM-114 fires at promotion-time CI gate, not every fine-tune (§1 #10)?</strong> Fine-tune is iterative; the baseline is a one-shot promotion artefact. Firing the rule on every fine-tune cycle would create noise (the baseline exists, the rule passes, every commit) without informational value. The rule fires when <code>skill_version</code> crosses from 0.x to 1.x — exactly the promotion event.</p>\n<p><strong>Why partner_connector skills carry a stricter requirement (§1 #11)?</strong> Per FR-SKILL-103's trust-exposability link, partner-connector skills face external scrutiny (partner SLAs, billing, tenancy). The baseline is the operator's defence: \"this skill earns its position because of X measurements; here's the attestation chain\". Without baseline, the trust-exposability gate is operator-vibes-based.</p>\n<p><strong>Why attestation by persona OR human (§1 #12)?</strong> Some baselines are attested by the responsible C-suite persona (<code>cuo-cpo</code> for product-owned skills); others by named humans (when persona attribution isn't clean). The two forms cover both governance models. The audit row records either form.</p>\n<p><strong>Why a 12-month review cadence (§1 #13)?</strong> Skill behaviour drifts over LLM model updates, training data shifts, and user-base evolution. Re-measurement every 12 months catches the drift. The warning/error escalation at 12/24 months gives operators a graceful runway: 12 months = \"schedule a re-measurement\"; 24 months = \"this baseline is stale enough to block promotion-derivative actions\".</p>\n<p><strong>Why backfill existing v1.0+ skills (§1 #14)?</strong> The artefact's value is <em>justifying this promotion</em>. A v1.0 skill without a baseline is a v1.0 skill that was never justified. Lazy backfill would defeat the purpose. Today only <code>cuo/_shared/hello-world</code> is at v1.0 (per MODULE.md) — the backfill is one skill, trivial cost.</p>\n<p><strong>Why update the validation pyramid (§1 #15)?</strong> Layer 3 (operational telemetry) now has a design-time anchor (BASELINE.md). The pyramid's caption update makes the dependency explicit — production metrics get their meaning by comparison to the baseline, not in isolation. Future operators reading README Part 13 understand the dependency without grepping.</p>\n<h2>§3 — API contract</h2>\n<h3>BASELINE.md frontmatter contract</h3>\n<pre><code class=\"language-yaml\">---\nskill_id: feature-request-author\nbaseline_version: 1.0.0\nbaseline_measured_at: 2026-05-19T15:30:00+07:00\nattested_by: cuo-cpo\nnext_review_due: 2027-05-19T00:00:00+07:00\n---</code></pre>\n<h3>Body section contract (worked example)</h3>\n<pre><code class=\"language-markdown\">## Workflow under test\n\nThe workflow is \"turn a PRD into a feature-request backlog\". Human-baseline:\nthe operator reads the PRD, identifies candidate FRs by hand, writes each\nFR markdown, runs the audit by hand. Skill-augmented: invoke\n`feature-request-author` with the PRD path; it generates a manifest +\nbacklog, halts at PLAN approval, the operator approves, it writes the FRs.\n\n## Without-skill baseline\n\n**Measurement window:** 2026-04-15 → 2026-05-15 (4 weeks). **Sample size:** n=12\nreal PRD-to-backlog flows across cpo/cto/cmo personas.\n\n| Measurement | Without skill (mean ± stddev) | Methodology |\n|---|---|---|\n| Tool-call count | 47 ± 12 | OBS `tool_call_count` per session; sessions filtered by `task_taxonomy: prd-to-backlog` |\n| Token count | 84,000 ± 18,000 | OBS `tokens_total` per session |\n| Failure rate | 33% | Sessions that ended with user \"abort\" or \"reformulate\" / total sessions (4 of 12) |\n\n## With-skill measurements\n\nSame window, same sample (n=12 the operators chose to re-run with the skill).\n\n| Measurement | With skill (mean ± stddev) | Ratio vs baseline | Pass threshold |\n|---|---|---|---|\n| Tool-call count | 11 ± 3 | 0.23 | ✓ (≤0.7) |\n| Token count | 22,000 ± 4,500 | 0.26 | ✓ (≤0.7) |\n| Failure rate | 8% | 0.24 | ✓ (≤0.5) |\n| Iteration count | 1.8 ± 0.6 | — | (audit-loop skill — info-only) |\n\n**Verdict:** all three thresholds passed. Skill is v1.0-eligible per FR-SKILL-114 §1 #6.\n\n## Token-budget transparency\n\nPer-invocation token budget for `feature-request-author`:\n\n- **Prompt tokens (mean):** 14,000\n- **Prompt tokens (95th percentile):** 19,500\n- **Completion tokens (mean):** 8,000\n- **Completion tokens (95th percentile):** 13,000\n- **Total ceiling guidance:** budget 40,000 tokens for a worst-case invocation.\n\nOperators in token-constrained sessions (free tier) should enable only when\na backlog is genuinely needed.\n\n## Trust calibration\n\n`confidence_band.default: 0.7` chosen because:\n\n- FR authoring is judgement work (`determinism.reproducible: false`).\n- The 0.7 default balances \"model is confident enough to act\" (0.7) against\n  \"model defers on ambiguity\" (≤0.5 triggers HITL).\n- During the v0.x measurement window, the empirical acceptance rate was **84%**\n  (10 of 12 batches accepted as-shipped). 84% &gt; the DEC-055 auto-pause threshold\n  of 40% by a wide margin; the skill is operationally stable.\n\n`defer_below: 0.5` chosen because:\n\n- Below 0.5, the model is genuinely uncertain about an FR claim.\n- HITL surfaces the question to the operator — the alternative is silent\n  fabrication (anti-pattern per `references/ANTI_FABRICATION.md`).\n\n## Authoring notes\n\n- Sample size n=12 is small. Caveats: the cpo persona contributed 8 of 12;\n  the cto and cmo personas contributed 4. Generalisation to less-represented\n  personas needs more data; recommend re-measuring in v1.1.\n- Baseline measurement excluded one PRD that was malformed (no `## Summary`\n  section); the skill correctly refused with `INPUTS_CHANGED`. Reported as\n  an FR-AI-XXX follow-up for the input-validation pipeline.\n- Attestation chain: measurement gathered by claude-opus-4-7 (this session)\n  during sweep; reviewed by cuo-cpo; signed off by stephen-cheng (human).</code></pre>\n<h3>Auditor rule — addition to <code>modules/skill/feature-request-audit/RUBRIC.md</code></h3>\n<pre><code class=\"language-markdown\">### FM-114 — baseline-present-at-v1\n\n**Statement:** Skills at `skill_version &gt;= 1.0.0` MUST carry `BASELINE.md` per FR-SKILL-114 §1. The artefact justifies the promotion + anchors future drift detection.\n\n**Severity:**\n- error on `skill_version &gt;= 1.0.0`\n- info on `skill_version &lt; 1.0.0` (advisory — pre-promotion drafts may add BASELINE.md early)\n- escalates to warning if `next_review_due` is overdue &gt;0 days\n- escalates to error if `next_review_due` is overdue &gt;365 days (stale baseline)\n\n**Auto-fix:** never (numbers must come from real measurements — verdict `needs_human`).\n\n**Check (deterministic):** invoke `python -m cyberos.cuo.baseline &lt;skill_path&gt;`; exits 0 if valid, exits 1 with structured error otherwise. Sub-codes: `file_missing` | `frontmatter_invalid` | `section_missing` | `numeric_out_of_range` | `attested_by_invalid` | `review_overdue`.</code></pre>\n<h3>Python validator — <code>modules/cuo/cuo/baseline.py</code></h3>\n<pre><code class=\"language-python\">\"\"\"Validate BASELINE.md artefacts per FR-SKILL-114.\"\"\"\n\nfrom dataclasses import dataclass\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nimport re\nimport yaml\n\n\nREQUIRED_SECTIONS = [\n    \"## Workflow under test\",\n    \"## Without-skill baseline\",\n    \"## With-skill measurements\",\n    \"## Token-budget transparency\",\n    \"## Trust calibration\",\n    \"## Authoring notes\",\n]\nREQUIRED_FM_KEYS = {\"skill_id\", \"baseline_version\", \"baseline_measured_at\", \"attested_by\", \"next_review_due\"}\nATTESTOR_RE = re.compile(r\"^(cuo-[a-z]+|human:[a-z][a-z0-9_-]*)$\")\n\n\n@dataclass(frozen=True)\nclass BaselineValidationResult:\n    skill_id: str\n    valid: bool\n    issues: list[str]\n\n\ndef validate(path: Path) -&gt; BaselineValidationResult:\n    if not path.exists():\n        return BaselineValidationResult(\"?\", False, [\"file_missing\"])\n    text = path.read_text(encoding=\"utf-8\")\n    issues: list[str] = []\n    skill_id = \"?\"\n\n    # 1. Parse frontmatter\n    if not text.startswith(\"---\\n\"):\n        return BaselineValidationResult(\"?\", False, [\"frontmatter_invalid: missing leading ---\"])\n    try:\n        end = text.index(\"\\n---\\n\", 4)\n    except ValueError:\n        return BaselineValidationResult(\"?\", False, [\"frontmatter_invalid: missing closing ---\"])\n    fm = yaml.safe_load(text[4:end])\n    body = text[end + 5:]\n    if not isinstance(fm, dict):\n        return BaselineValidationResult(\"?\", False, [\"frontmatter_invalid: not a dict\"])\n\n    skill_id = fm.get(\"skill_id\", \"?\")\n\n    # 2. Required keys present\n    missing = REQUIRED_FM_KEYS - set(fm.keys())\n    if missing:\n        issues.append(f\"frontmatter_invalid: missing keys {sorted(missing)}\")\n\n    # 3. attested_by format\n    attested_by = fm.get(\"attested_by\", \"\")\n    if attested_by and not ATTESTOR_RE.match(str(attested_by)):\n        issues.append(f\"attested_by_invalid: '{attested_by}'\")\n\n    # 4. next_review_due in future (warn) or past (escalate)\n    try:\n        due = datetime.fromisoformat(str(fm.get(\"next_review_due\", \"\")))\n        now = datetime.now(timezone.utc)\n        if due.tzinfo is None:\n            due = due.replace(tzinfo=timezone.utc)\n        days_overdue = (now - due).days\n        if days_overdue &gt; 365:\n            issues.append(\"review_overdue: stale baseline (&gt;365 days)\")\n        elif days_overdue &gt; 0:\n            issues.append(f\"review_overdue: {days_overdue} days\")\n    except (ValueError, TypeError):\n        issues.append(\"frontmatter_invalid: next_review_due is not ISO 8601\")\n\n    # 5. Required body sections\n    for section in REQUIRED_SECTIONS:\n        if section not in body:\n            issues.append(f\"section_missing: '{section}'\")\n\n    return BaselineValidationResult(skill_id, len(issues) == 0, issues)</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Valid BASELINE.md passes</strong> — fixture <code>baseline-valid.md</code> with all required frontmatter keys + 6 body sections + numeric measurements within thresholds → <code>validate</code> returns <code>valid=True</code>.</li><li><strong>Missing file rejected</strong> — non-existent path → <code>valid=False</code>, issues includes <code>\"file_missing\"</code>.</li><li><strong>Missing frontmatter rejected</strong> — file without <code>---</code> fences → <code>valid=False</code>, issues includes <code>frontmatter_invalid</code>.</li><li><strong>Missing required frontmatter key rejected</strong> — fixture without <code>attested_by:</code> → <code>valid=False</code>, issues mentions the missing key.</li><li><strong>Invalid attested_by form rejected</strong> — fixture with <code>attested_by: random-person</code> → <code>valid=False</code>, issues includes <code>attested_by_invalid</code>.</li><li><strong>Missing body section rejected</strong> — fixture without <code>## Token-budget transparency</code> → <code>valid=False</code>, issues includes <code>section_missing</code>.</li><li><strong>Review overdue (&gt;0 days) flagged as warning</strong> — fixture with <code>next_review_due: 2024-01-01</code> → <code>valid=False</code>, issues includes <code>review_overdue: N days</code>.</li><li><strong>Review stale (&gt;365 days) flagged as error</strong> — fixture with <code>next_review_due: 2023-01-01</code> → <code>valid=False</code>, issues includes <code>review_overdue: stale baseline</code>.</li><li><strong>Auditor rule FM-114 fires on v1.0+ skill without BASELINE.md</strong> — running auditor on a v1.0 skill missing BASELINE.md → audit reports one FM-114 issue (severity error, status needs_human).</li><li><strong>Auditor rule FM-114 fires as info on v0.x skill</strong> — same input but <code>skill_version: 0.2.0</code> → audit reports one FM-114 issue (severity info — advisory).</li><li><strong>Broker rejects partner_connector v1.0+ without baseline</strong> — load attempt → <code>FrontmatterError::PartnerWithoutBaseline</code>.</li><li><strong>Numeric threshold pass check</strong> — fixture with tool-call ratio 0.5 (≤0.7) + token ratio 0.5 + failure ratio 0.4 → meets thresholds; promotion eligible.</li><li><strong>Numeric threshold fail check</strong> — fixture with tool-call ratio 0.8 → does NOT meet threshold; promotion would require operator override + reason captured in Authoring notes.</li><li><strong>Backfill hello-world v1.0</strong> — <code>cuo/_shared/hello-world/BASELINE.md</code> exists post-FR-ship; passes <code>validate</code>.</li><li><strong>README Recipe 11 expanded</strong> — Recipe 11 in README.md mentions BASELINE.md + the 3-threshold check + the operator-override clause.</li><li><strong>feature-request-audit skill §3.11 entry added</strong> — new sub-rule references FR-SKILL-114.</li><li><strong>Validation pyramid updated</strong> — README.md Part 13 mentions BASELINE.md as the design-time anchor.</li><li><strong>Template scaffold present</strong> — <code>_template/author/BASELINE.md</code> is a complete scaffold an author can copy.</li><li><strong>CI integration</strong> — <code>python -m pytest modules/cuo/tests/test_baseline.py</code> runs as part of the existing CUO test suite.</li><li><strong>OTel span emitted</strong> — <code>skill.baseline.validate</code> with attributes <code>skill_id</code>, <code>outcome</code>, <code>issue_count</code>.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/cuo/tests/test_baseline.py\nimport pytest\nfrom datetime import datetime, timezone, timedelta\nfrom pathlib import Path\nfrom cyberos.cuo.baseline import validate, BaselineValidationResult\n\n\nFIXTURES = Path(__file__).parent / \"fixtures\"\n\n\ndef test_valid_baseline_passes():\n    result = validate(FIXTURES / \"baseline-valid.md\")\n    assert result.valid is True\n    assert result.issues == []\n\n\ndef test_missing_file_rejected(tmp_path):\n    result = validate(tmp_path / \"nonexistent.md\")\n    assert result.valid is False\n    assert \"file_missing\" in result.issues\n\n\ndef test_missing_frontmatter_rejected(tmp_path):\n    p = tmp_path / \"BASELINE.md\"\n    p.write_text(\"# No frontmatter here\\n\", encoding=\"utf-8\")\n    result = validate(p)\n    assert result.valid is False\n    assert any(\"frontmatter_invalid\" in i for i in result.issues)\n\n\ndef test_missing_required_key_rejected(tmp_path):\n    p = tmp_path / \"BASELINE.md\"\n    p.write_text(\"\"\"---\nskill_id: foo\nbaseline_version: 1.0.0\nbaseline_measured_at: 2026-05-19T15:30:00+07:00\nnext_review_due: 2027-05-19T00:00:00+07:00\n---\n\n## Workflow under test\n## Without-skill baseline\n## With-skill measurements\n## Token-budget transparency\n## Trust calibration\n## Authoring notes\n\"\"\", encoding=\"utf-8\")\n    # Missing: attested_by\n    result = validate(p)\n    assert result.valid is False\n    assert any(\"attested_by\" in i for i in result.issues)\n\n\ndef test_invalid_attested_by(tmp_path):\n    p = tmp_path / \"BASELINE.md\"\n    p.write_text(\"\"\"---\nskill_id: foo\nbaseline_version: 1.0.0\nbaseline_measured_at: 2026-05-19T15:30:00+07:00\nattested_by: random-person\nnext_review_due: 2027-05-19T00:00:00+07:00\n---\n\n## Workflow under test\n## Without-skill baseline\n## With-skill measurements\n## Token-budget transparency\n## Trust calibration\n## Authoring notes\n\"\"\", encoding=\"utf-8\")\n    result = validate(p)\n    assert result.valid is False\n    assert any(\"attested_by_invalid\" in i for i in result.issues)\n\n\ndef test_section_missing_rejected(tmp_path):\n    p = tmp_path / \"BASELINE.md\"\n    p.write_text(\"\"\"---\nskill_id: foo\nbaseline_version: 1.0.0\nbaseline_measured_at: 2026-05-19T15:30:00+07:00\nattested_by: cuo-cpo\nnext_review_due: 2027-05-19T00:00:00+07:00\n---\n\n## Workflow under test\n## Without-skill baseline\n## With-skill measurements\n## Trust calibration\n## Authoring notes\n\"\"\", encoding=\"utf-8\")\n    # Missing: ## Token-budget transparency\n    result = validate(p)\n    assert result.valid is False\n    assert any(\"Token-budget transparency\" in i for i in result.issues)\n\n\ndef test_review_overdue_flagged(tmp_path):\n    p = tmp_path / \"BASELINE.md\"\n    p.write_text(\"\"\"---\nskill_id: foo\nbaseline_version: 1.0.0\nbaseline_measured_at: 2024-01-01T00:00:00+07:00\nattested_by: cuo-cpo\nnext_review_due: 2024-06-01T00:00:00+07:00\n---\n\n## Workflow under test\n## Without-skill baseline\n## With-skill measurements\n## Token-budget transparency\n## Trust calibration\n## Authoring notes\n\"\"\", encoding=\"utf-8\")\n    result = validate(p)\n    assert result.valid is False\n    assert any(\"review_overdue\" in i for i in result.issues)\n\n\ndef test_human_attestation_accepted(tmp_path):\n    p = tmp_path / \"BASELINE.md\"\n    p.write_text(\"\"\"---\nskill_id: foo\nbaseline_version: 1.0.0\nbaseline_measured_at: 2026-05-19T15:30:00+07:00\nattested_by: human:stephen-cheng\nnext_review_due: 2027-05-19T00:00:00+07:00\n---\n\n## Workflow under test\n## Without-skill baseline\n## With-skill measurements\n## Token-budget transparency\n## Trust calibration\n## Authoring notes\n\"\"\", encoding=\"utf-8\")\n    result = validate(p)\n    assert result.valid is True</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>§3 covers the surface. Wiring:</p>\n<ol><li><code>modules/skill/_template/author/BASELINE.md</code> is a fully-populated scaffold an author copies + edits.</li><li><code>modules/cuo/cuo/baseline.py</code> is added as a new module; re-exported from <code>modules/cuo/cuo/__init__.py</code>.</li><li><code>modules/cuo/cli.py</code> gains a <code>validate-baseline &lt;skill_path&gt;</code> subcommand.</li><li>README Recipe 11 expanded from 3 paragraphs to 8 paragraphs covering the FR-SKILL-114 contract.</li><li>feature-request-audit skill §3.11 (documentation-discipline) adds the rule.</li><li>RUBRIC.md FM-114 added.</li></ol>\n<h2>§7 — Dependencies</h2>\n<p><strong>Depends on:</strong></p>\n<ul><li><strong>FR-SKILL-103</strong> (frontmatter-extension) — provides the broker that gates partner_connector skills on BASELINE.md presence.</li></ul>\n<p><strong>Blocks:</strong> none.</p>\n<p><strong>Related:</strong></p>\n<ul><li><strong>FR-SKILL-111</strong> (description trigger enrichment) — orthogonal; description is loaded by host classifier; baseline is read by promotion auditor.</li><li><strong>FR-SKILL-112</strong> (TRIGGER_TESTS.md) — orthogonal; routing test vs promotion artefact.</li><li><strong>FR-SKILL-113</strong> (XML-free frontmatter) — orthogonal; frontmatter shape vs new artefact.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>OBS module</strong> — when OBS ships, the production telemetry feeds back into the baseline at next review (12-month cadence).</li></ul>\n<h2>§8 — Example payloads</h2>\n<h3>Example 1 — valid BASELINE.md (worked example)</h3>\n<p>(See §3 body section contract — that's the worked example.)</p>\n<h3>Example 2 — audit issue (FM-114 firing on v1.0+ skill without baseline)</h3>\n<pre><code>ISSUE\nid:              ISS-013\nrule_id:         FM-114\nseverity:        error\ncategory:        baseline_missing\nlocation:        BASELINE.md (file does not exist)\nevidence:        \"Skill at skill_version: 1.0.0 has no BASELINE.md sibling file.\"\ndescription:     \"Skills at v1.0+ MUST carry BASELINE.md per FR-SKILL-114 §1. The artefact justifies the v0.x → v1.0 promotion.\"\nsuggestion:      \"Copy modules/skill/_template/author/BASELINE.md to &lt;skill_path&gt;/BASELINE.md; measure tool-call count / token count / failure rate without and with the skill enabled; populate the 6 body sections; attest by persona-id or human:&lt;id&gt;.\"\nauto_fix_applied: false\nresolution:      null\nopened_at:       \"2026-05-19T16:00:00Z\"\nupdated_at:      \"2026-05-19T16:00:00Z\"</code></pre>\n<h3>Example 3 — CLI output</h3>\n<pre><code class=\"language-bash\">$ python -m cyberos.cuo.baseline modules/skill/feature-request-author/\n✓ feature-request-author — baseline valid (all 3 thresholds met)\n\n$ python -m cyberos.cuo.baseline modules/skill/closure-author/\n✗ closure-author — baseline invalid:\n  - section_missing: '## Token-budget transparency'\n  - review_overdue: 47 days</code></pre>\n<h2>§9 — Open questions</h2>\n<p><strong>All resolved during authoring.</strong></p>\n<p>Deferred to follow-up FRs:</p>\n<ul><li><strong>FR-SKILL-118</strong> (placeholder — not yet specified): re-measurement automation — at the 12-month review-due date, automatically schedule a re-measurement run using OBS data + propose updated numbers via a <code>refinement_proposal</code> envelope. Phase P2+.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Author at v1.0 promotion skips BASELINE.md</td><td>FM-114 fires; CI gate fails</td><td>Promotion blocked</td><td>Measure baseline + author the artefact</td></tr><tr><td>Author writes BASELINE.md with estimated (not measured) numbers</td><td>Audit step has no detection at file-write time; trust gap surfaces in production when acceptance drifts</td><td>Skill might pass initial promotion but drift fast</td><td>Periodic re-measurement (12-month cadence) surfaces drift</td></tr><tr><td>Author writes BASELINE.md with cherry-picked sample</td><td>Same as above — undetectable at write time</td><td>Same as above</td><td>Audit attestation chain documents the methodology; reviewer should reject cherry-picking</td></tr><tr><td><code>next_review_due</code> set far in future (e.g. 2099)</td><td>Validator accepts (no upper bound)</td><td>Stale-by-default</td><td>feature-request-audit skill §3.11 prescribes default +12 months</td></tr><tr><td><code>attested_by</code> is a placeholder like <code>human:tbd</code></td><td>ATTESTOR_RE accepts (matches <code>human:</code> prefix pattern)</td><td>Defeats the attestation chain</td><td>Reviewer rejects on PR review (manual safeguard)</td></tr><tr><td>BASELINE.md present but numbers fail thresholds</td><td>Validator passes (numbers themselves aren't gated by validator); operator override reasoning in <code>## Authoring notes</code> should explain</td><td>Override is recorded; promotion proceeds with documented exception</td><td>Periodic review surfaces the exception's continued validity</td></tr><tr><td>Skill drops <code>skill_version</code> from 1.0.0 → 0.x (rare)</td><td>FM-114 severity drops from error to info</td><td>Baseline becomes advisory</td><td>Operator removes BASELINE.md if downgrade is intentional</td></tr><tr><td><code>next_review_due</code> in past at PR-merge time</td><td>Validator flags <code>review_overdue</code> — could block merge</td><td>Operator updates baseline + advances <code>next_review_due</code></td><td>Re-measurement cycle</td></tr><tr><td>Numbers fail 1-of-3 thresholds (e.g. tool-call good, token good, failure bad)</td><td>Override clause applies</td><td>Promotion proceeds with documented exception</td><td>Authoring notes explain why the failure-rate threshold doesn't apply (e.g. audit skill's \"failure\" semantically means \"found violations\")</td></tr><tr><td>Validator misclassifies an ISO 8601 timestamp</td><td>Test fixture catches</td><td>Type-system robust</td><td>Add test case</td></tr><tr><td>Token-budget section's numbers are stale</td><td>Authoring notes flag staleness</td><td>Operator decides</td><td>Re-measurement cycle</td></tr><tr><td>Backfill of hello-world is rejected because hello-world is \"teaching example\"</td><td>Operator-override clause applies</td><td>hello-world's BASELINE.md notes \"teaching example — thresholds not applicable\"</td><td>Document in Authoring notes</td></tr><tr><td>Migration mistake — BASELINE.md committed but skill_version not bumped to 1.0</td><td>FM-114 fires as info (≤v1) but is benign</td><td>Audit noise</td><td>Bump skill_version OR remove BASELINE.md until ready</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why per-skill rather than module-wide BASELINE?</strong> Skills export individually (per AGENTS.md §11 plug-in contract). A module-wide CSV would be lost on export. Per-skill BASELINE.md travels with the skill folder.</li><li><strong>Why human-attestation as <code>human:&lt;id&gt;</code> form?</strong> Distinguishes from persona-attestation. A persona attestation can be auto-generated by a workflow (e.g. cuo-cpo signs off on its own promotion via a chained skill); human attestation requires a real signature in the audit row (per AGENTS.md §7).</li><li><strong>Why ASCII-only sections rather than Vietnamese-localised?</strong> English is the engineering lingua franca (per README Part 17.2). VN-locale doesn't need a parallel BASELINE.md — the artefact is for internal governance, not end-user UX.</li><li><strong>Why 12-month review cadence rather than 6 or 24?</strong> Empirical. LLM model updates ship roughly every 6 months; user-behaviour shifts every 12-18 months. 12 months catches both; 6 months would be too frequent for stable-baseline skills; 24 months too lax.</li><li><strong>Operator override is the escape valve.</strong> Some skills add value in dimensions the baseline doesn't measure (audit skills, trust-calibration skills, governance skills). The override preserves the rigor while allowing legitimate exceptions. The audit row records the override + reason.</li><li><strong>Why FM-114 doesn't gate <code>status: accepted</code> (only v1.0+)?</strong> Most CyberOS skills will live at <code>skill_version: 0.x</code> for many cycles. Forcing a baseline at every accept-status promotion would inflate authoring cost. The artefact is for v1.0+ where partner-exposure + drift-anchor questions become real.</li><li><strong>The baseline numbers are operator-attested, not validator-checked.</strong> The validator confirms the file exists + has the right shape + has a valid attestation. It does NOT verify the numbers themselves (that would require running the actual workload). The operator-attestation chain (persona-id or human-id signoff) is the trust anchor; periodic re-measurement is the drift detector.</li><li><strong>Foundation-stage value:</strong> today only hello-world is v1.0; backfilling its baseline costs ~15 minutes. The infrastructure (template + validator + auditor rule + recipe) is what earns its place — when the first production skill promotes from v0.x to v1.0 (likely feature-request-author or feature-request-audit at a Phase 2+ milestone), the artefact is ready.</li></ul>\n<hr>\n<p><em>End of FR-SKILL-114.</em></p>";
