window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EMAIL-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The EMAIL service <strong>MUST</strong> ship DKIM signing + ARC chain forward + BIMI brand indicator at <code>services/email/src/{dkim,arc,bimi,dns}/</code>, per-tenant Ed25519 keypair generated at provisioning, DNS setup wizard, RFC-conformant signing, and 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>dkim_outcome</code> enum: <code>('signed_ed25519','signed_rsa','sign_failed_no_key','sign_failed_kms')</code> per DEC-1473. Cardinality 4.</li></ol>\n<ol><li><strong>MUST</strong> generate per-tenant DKIM keypair per DEC-1470 at FR-TEN-001 provisioning:</li></ol>\n<ul><li>Ed25519 primary per RFC 8463.</li><li>RSA-2048 fallback for legacy receivers.</li><li>Private keys KMS-wrapped.</li><li>Public keys serialized as DNS TXT format for publication.</li></ul>\n<ol><li><strong>MUST</strong> define <code>tenant_dkim_keys</code> table at migration <code>0002</code>: <code>(tenant_id UUID NOT NULL, key_kind TEXT NOT NULL CHECK (key_kind IN ('ed25519','rsa2048')), selector TEXT NOT NULL DEFAULT 'cyberos', private_key_kms_blob BYTEA NOT NULL, public_key_dns_txt TEXT NOT NULL, kms_key_id TEXT NOT NULL, generated_at TIMESTAMPTZ NOT NULL DEFAULT now(), revoked_at TIMESTAMPTZ, PRIMARY KEY (tenant_id, key_kind, selector))</code>.</li></ol>\n<ol><li><strong>MUST</strong> sign outbound messages via <code>dkim/signer.rs::sign(message, tenant_id)</code> per RFC 6376:</li></ol>\n<ul><li>Resolves private key from <code>tenant_dkim_keys</code> (Ed25519 primary).</li><li>KMS-decrypts.</li><li>Computes canonical body hash.</li><li>Signs over headers + hash.</li><li>Emits <code>DKIM-Signature</code> header.</li><li>Emits memory row <code>email.dkim_signed</code> with outcome.</li></ul>\n<ol><li><strong>MUST</strong> support ARC chain forward per DEC-1471 + RFC 8617 via <code>arc/chain_forward.rs</code>. For forwarded inbound mail:</li></ol>\n<ul><li>Verifies existing ARC chain (cv= verdict).</li><li>Appends new ARC-Authentication-Results + ARC-Message-Signature + ARC-Seal.</li><li>Preserves chain for downstream receivers.</li></ul>\n<ol><li><strong>MUST</strong> attach BIMI brand indicator per DEC-1472 via <code>bimi/mod.rs</code>:</li></ol>\n<ul><li>Requires tenant's DMARC at <code>p=quarantine</code> or stricter (verified via DNS check).</li><li>SVG logo from FR-PORTAL-002 brand pack → tinified (SVG Tiny PS) via <code>svg_tinifier.rs</code>.</li><li>Optional VMC certificate URL for verified-mark display.</li><li>Adds <code>BIMI-Selector</code> header + DNS BIMI TXT record.</li></ul>\n<ol><li><strong>MUST</strong> define <code>tenant_dns_setup</code> at migration <code>0003</code>: <code>(tenant_id UUID PRIMARY KEY, custom_domain TEXT, dkim_txt_published BOOLEAN, spf_txt_published BOOLEAN, dmarc_txt_published BOOLEAN, dmarc_policy TEXT, bimi_txt_published BOOLEAN, vmc_cert_url TEXT, last_verified_at TIMESTAMPTZ, verification_failures INT NOT NULL DEFAULT 0)</code>.</li></ol>\n<ol><li><strong>MUST</strong> expose DNS setup wizard <code>POST /v1/admin/tenants/{tid}/email/dns-setup</code>. Returns required TXT records (DKIM public key + SPF + DMARC + BIMI). Tenant admin publishes; verifier polls.</li></ol>\n<ol><li><strong>MUST</strong> verify DNS via <code>dns/verifier.rs::verify(tenant_id)</code>. Daily job:</li></ol>\n<ul><li>DNS resolve each expected TXT.</li><li>Compare with database expectation.</li><li>Mismatch → emit <code>email.dns_verification_failed</code> sev-2.</li><li>Success → <code>email.dns_verification_passed</code> sev-3.</li></ul>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1475.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> share keys across tenants (per DEC-1470).</li></ol>\n<ol><li><strong>MUST NOT</strong> attach BIMI without DMARC enforcement (per DEC-1472).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why per-tenant DKIM (DEC-1470)?</strong> Compromise scoping; shared key = single-failure compromise of every tenant.</p>\n<p><strong>Why Ed25519 + RSA dual (DEC-1470)?</strong> Legacy receivers (some enterprise mail servers) don't yet support Ed25519; RSA fallback ensures delivery.</p>\n<p><strong>Why ARC (DEC-1471)?</strong> Mailing-list forwards break SPF/DKIM; ARC preserves original auth verdict for receivers.</p>\n<p><strong>Why BIMI requires DMARC (DEC-1472)?</strong> BIMI spec mandates p=quarantine+ to prevent abuse — spoofers can't get brand-recognised inboxes.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0002_tenant_dkim_keys.sql\nCREATE TABLE tenant_dkim_keys (\n  tenant_id UUID NOT NULL,\n  key_kind TEXT NOT NULL CHECK (key_kind IN ('ed25519','rsa2048')),\n  selector TEXT NOT NULL DEFAULT 'cyberos',\n  private_key_kms_blob BYTEA NOT NULL,\n  public_key_dns_txt TEXT NOT NULL,\n  kms_key_id TEXT NOT NULL,\n  generated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  revoked_at TIMESTAMPTZ,\n  PRIMARY KEY (tenant_id, key_kind, selector)\n);\nALTER TABLE tenant_dkim_keys ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_dkim_keys_rls ON tenant_dkim_keys\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON tenant_dkim_keys FROM cyberos_app;\nGRANT UPDATE (revoked_at) ON tenant_dkim_keys TO cyberos_app;\n\n-- 0003_tenant_dns_setup.sql\nCREATE TABLE tenant_dns_setup (\n  tenant_id UUID PRIMARY KEY,\n  custom_domain TEXT,\n  dkim_txt_published BOOLEAN NOT NULL DEFAULT false,\n  spf_txt_published BOOLEAN NOT NULL DEFAULT false,\n  dmarc_txt_published BOOLEAN NOT NULL DEFAULT false,\n  dmarc_policy TEXT,\n  bimi_txt_published BOOLEAN NOT NULL DEFAULT false,\n  vmc_cert_url TEXT,\n  last_verified_at TIMESTAMPTZ,\n  verification_failures INT NOT NULL DEFAULT 0,\n  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nALTER TABLE tenant_dns_setup ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_dns_setup_rls ON tenant_dns_setup\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON tenant_dns_setup FROM cyberos_app;\nGRANT UPDATE (dkim_txt_published, spf_txt_published, dmarc_txt_published, dmarc_policy,\n              bimi_txt_published, vmc_cert_url, last_verified_at, verification_failures, custom_domain, updated_at)\n  ON tenant_dns_setup TO cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/admin/tenants/{tid}/email/dns-setup        (tenant_admin)\nPOST   /v1/admin/tenants/{tid}/email/dns-verify       (tenant_admin)\nPOST   /v1/admin/tenants/{tid}/email/bimi-enable      (tenant_admin)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>dkim_outcome cardinality 4</strong>.</li><li><strong>DKIM Ed25519 sign</strong> — outbound message gets <code>DKIM-Signature</code> header with a=ed25519-sha256.</li><li><strong>RSA fallback</strong> — legacy receiver fixture → RSA signature attached as additional header.</li><li><strong>Per-tenant key isolation</strong> — tenant A's key cannot sign for tenant B (RLS enforced).</li><li><strong>DNS wizard returns TXT records</strong> — POST returns expected DKIM/SPF/DMARC/BIMI TXT values.</li><li><strong>DNS verification daily</strong> — wizard records publication state; verifier polls + updates.</li><li><strong>ARC chain extended</strong> — forwarded message has ARC-Seal added.</li><li><strong>BIMI requires DMARC</strong> — bimi-enable without DMARC=quarantine → 412.</li><li><strong>SVG tinify</strong> — uploaded SVG processed to BIMI-compliant Tiny PS.</li><li><strong>5 memory audit kinds emitted</strong>.</li><li><strong>KMS unavailable</strong> → sign_failed_kms outcome + sev-1 audit.</li><li><strong>Key missing for tenant</strong> → sign_failed_no_key.</li><li><strong>DNS verification failure persisted</strong> — failures counter increments.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>Cross-tenant RLS denied</strong>.</li><li><strong>VMC cert URL optional</strong> — BIMI works without VMC (no verified mark badge).</li><li><strong>Selector configurable</strong> — default 'cyberos'; tenant can change.</li><li><strong>Provisioning integration</strong> — new tenant gets DKIM keys auto-generated.</li><li><strong>Revoked key not used for signing</strong> — revoked_at set → skipped.</li><li><strong>PII scrub</strong> — DNS records non-PII; raw IP not in audit.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn dkim_signs_outbound_message() {\n    let ctx = TestContext::with_provisioned_tenant().await;\n    let msg = ctx.outbound_message(\"alice@acme.cyberos.world\", \"bob@example.com\").await;\n    let signed = ctx.dkim_sign(ctx.tenant_id, msg).await;\n    assert!(signed.headers().contains_key(\"DKIM-Signature\"));\n    let sig: &amp;str = signed.headers().get(\"DKIM-Signature\").unwrap();\n    assert!(sig.contains(\"a=ed25519-sha256\"));\n    assert!(sig.contains(\"s=cyberos\"));\n}\n\n#[tokio::test]\nasync fn cross_tenant_key_isolation() {\n    let ctx = TestContext::with_two_tenants().await;\n    let r = ctx.as_tenant_a().dkim_sign_for_tenant(ctx.tenant_b_id, \"test\").await;\n    assert!(r.is_err());  // RLS rejects\n}\n\n#[tokio::test]\nasync fn bimi_requires_dmarc() {\n    let ctx = TestContext::with_provisioned_tenant().await;\n    let r = ctx.bimi_enable(ctx.tenant_id).await;\n    assert_eq!(r.status(), 412);  // DMARC not yet set\n    ctx.set_dmarc_policy(ctx.tenant_id, \"quarantine\").await;\n    let r = ctx.bimi_enable(ctx.tenant_id).await;\n    assert_eq!(r.status(), 200);\n}\n\n#[tokio::test]\nasync fn provisioning_generates_keys() {\n    let ctx = TestContext::new().await;\n    let tid = ctx.provision_tenant().await;\n    let count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM tenant_dkim_keys WHERE tenant_id=$1\")\n        .bind(tid).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(count, 2);  // ed25519 + rsa\n}\n\n// 5.5..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-EMAIL-001. <strong>Cross-module:</strong> FR-TEN-001 (keygen at provisioning), FR-PORTAL-002 (BIMI logo), FR-AI-003, FR-MEMORY-111. <strong>Downstream:</strong> FR-EMAIL-009 (outbound send).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>KMS unavailable</td><td>timeout</td><td>sign_failed_kms; sev-1</td><td>KMS recovery</td></tr><tr><td>Key missing</td><td>lookup miss</td><td>sign_failed_no_key</td><td>Regenerate via wizard</td></tr><tr><td>DNS not propagated</td><td>verifier poll</td><td>failures++; sev-2 at 5+ failures</td><td>Tenant admin fixes DNS</td></tr><tr><td>ARC chain corrupted upstream</td><td>verify fail</td><td>cv=fail; forward with verdict; sev-3</td><td>Inherent</td></tr><tr><td>BIMI SVG &gt; 32KB</td><td>tinifier limit</td><td>400</td><td>Tenant slims logo</td></tr><tr><td>VMC URL unreachable</td><td>fetch fail</td><td>BIMI works without verified mark; sev-3</td><td>Inherent</td></tr><tr><td>Cross-tenant signing attempt</td><td>RLS</td><td>Inherent</td><td>None</td></tr><tr><td>Revoked key signing attempt</td><td>revoked_at check</td><td>sign_failed_no_key</td><td>Generate new</td></tr><tr><td>DNS provider rate limit</td><td>poll backoff</td><td>sev-3</td><td>Inherent</td></tr><tr><td>Selector collision</td><td>partial unique</td><td>tenant chooses unique</td><td>Inherent</td></tr><tr><td>Custom domain mismatch</td><td>wizard</td><td>400</td><td>Fix domain config</td></tr><tr><td>KMS rotation breaks signing</td><td>key archive</td><td>Old signed msgs verify with old pub key</td><td>Inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Ed25519 key 32 bytes; RSA-2048 256 bytes; DNS TXT under 255-char limit handled via chunked TXT.</li><li>§11.2 BIMI SVG tinification via <code>usvg</code> Rust crate.</li><li>§11.3 ARC verification uses <code>mail-auth</code> Rust crate.</li><li>§11.4 DNS verifier uses <code>hickory-resolver</code>.</li><li>§11.5 Provisioning hook in FR-TEN-001 orchestrator generates both keypairs.</li></ul>\n<hr>\n<p><em>End of FR-EMAIL-004 spec.</em></p>";
