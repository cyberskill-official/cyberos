window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SKILL-119"]="<h1>FR-SKILL-119: Stale-reference sweep + doc-anchor checker</h1>\n<h2>§1 - Description</h2>\n<p>Skills are contracts; contracts that cite documents which no longer exist train agents to distrust citations. The SDP anchor died in the docs split and the ship workflow carries notes that predate its own children. This FR sweeps both and adds the checker that prevents recurrence.</p>\n<p>Normative clauses:</p>\n<ol><li>Every <code>modules/skill/*/SKILL.md</code> (and any other modules/skill markdown) citing <code>modules/cuo/README.md#software-development-process</code> - including the <code>SDP §2(a)..(g)</code> lettered forms - MUST be repointed to the live SDP location (<code>modules/cuo/docs/appendices.md</code> §13 stage mapping, or <code>modules/cuo/docs/module.md</code> where the prose form fits), preserving each citation's stage letter/number semantics unchanged.</li><li><code>ship-feature-requests.md</code> MUST drop the obsolete note (currently near line 199) claiming the code-review pair may not exist yet, replacing it with the current fact (pair exists, vendored); the Issue Request <code>TBD</code> (near line 137) MUST either point at a tracked FR id or be reworded as explicitly unscheduled future work - a bare <code>TBD</code> MUST NOT remain.</li><li>A script <code>scripts/check_doc_anchors.sh</code> MUST scan <code>modules/skill/**/*.md</code> and <code>modules/cuo/**/*.md</code> for repo-relative markdown links and inline path#anchor citations, and verify each target file exists and (when an anchor is given) the anchor resolves to a heading in that file (GitHub slug rules: lowercase, spaces to hyphens, punctuation stripped). Unresolved references exit 10 as <code>DEAD &lt;citing-file&gt;:&lt;line&gt; -&gt; &lt;target&gt;</code>; external URLs (http/https) MUST be skipped; a <code>--list</code> flag prints the would-be-swept set without failing.</li><li>The checker MUST run in CI on changes to <code>modules/skill/**</code> or <code>modules/cuo/**</code> (extend <code>payload-gate.yml</code> from FR-IMP-068 with a step, or the existing voice-and-consistency workflow - implementer's choice, documented in the workflow file).</li><li>The sweep MUST NOT alter any skill's trigger description, frontmatter, or artefact contract - citation strings only (same byte-stability discipline as FR-SKILL-118 §1 #7).</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Fixing the anchors without a checker just schedules the next rot; a checker without slug-aware anchor resolution would only catch deleted FILES, and the observed failure is a deleted SECTION HOST. Scanning both markdown link syntax and inline <code>path#anchor</code> prose covers how skills actually cite (they use both). CI placement rides existing gates rather than adding a new workflow.</p>\n<h2>§3 - Contract</h2>\n<pre><code>scripts/check_doc_anchors.sh [--list] [root]\n  exit 0   all repo-relative references resolve (prints \"anchors OK: N checked in M files\")\n  exit 10  DEAD &lt;file&gt;:&lt;line&gt; -&gt; &lt;target&gt;   (one per unresolved reference)\n  exit 2   root unreadable\n  --list   print each reference (resolved or not) as \"&lt;file&gt;:&lt;line&gt; &lt;target&gt; &lt;ok|DEAD&gt;\"</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Zero dead SDP anchors after the sweep</strong> (§1 #1) - <code>grep -rn \"modules/cuo/README.md\" modules/skill modules/cuo</code> returns nothing, and the checker exits 0 over both trees.</li><li><strong>Stage semantics preserved</strong> (§1 #1) - the two named SKILL.md files (implementation-plan-author, architecture-decision-record-author) still cite their respective SDP stages (implementation prep / architecture decision), now at the live location.</li><li><strong>Ship workflow notes are current</strong> (§1 #2) - line-199-class note gone, replaced by the present-tense fact; no bare <code>TBD</code> remains in the file (grep clean), the Issue Request mention names an FR or says \"future work, unscheduled\".</li><li><strong>Checker resolves anchors, not just files</strong> (§1 #3) - a fixture link to an existing file but nonexistent heading is reported DEAD with file:line; the same link with a valid heading passes.</li><li><strong>External URLs skipped, --list works</strong> (§1 #3) - an https link never fails the check; <code>--list</code> prints every reference with its status and exits 0.</li><li><strong>CI wired</strong> (§1 #4) - the chosen workflow runs the checker on the two path filters; the step is present and the workflow parses.</li><li><strong>Contracts byte-stable outside citations</strong> (§1 #5) - for every swept SKILL.md, the diff touches only citation strings (no frontmatter, no description, no artefact-section changes).</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># scripts/tests/test_check_doc_anchors.sh\nt01_sweep_leaves_zero_dead()     # AC 1\nt02_stage_semantics_kept()       # AC 2  (grep the two files for stage wording + new target)\nt03_ship_notes_current()         # AC 3\nt04_anchor_vs_file_resolution()  # AC 4  (fixture tree with good-file/bad-anchor case)\nt05_external_and_list()          # AC 5\nt06_ci_step_present()            # AC 6\nt07_citation_only_diffs()        # AC 7  (git diff --unified=0 scoped assertions on a sample)</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>Checker: extract candidates via two greps (<code>\\]\\([^)h][^)]*\\)</code> for md links; <code>[a-zA-Z0-9_./-]+\\.md(#[a-z0-9-]+)?</code> for inline paths), normalize against repo root, slugify headings of each target once into an assoc cache, compare. Sweep: run <code>--list</code>, sed the dead SDP form to the live target across the listed files, hand-fix the two workflow notes.</p>\n<h2>§7 - Dependencies</h2>\n<p>None upstream; FR-IMP-068's workflow is the preferred CI host but the voice-and-consistency workflow is an acceptable fallback (implementer documents the choice). Related: FR-SKILL-115 (prior sweep, done - different defect class), FR-DOCS-002 (the docs split that orphaned the anchor).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ bash scripts/check_doc_anchors.sh\nDEAD modules/skill/implementation-plan-author/SKILL.md:5 -&gt; modules/cuo/README.md#software-development-process\nDEAD modules/skill/architecture-decision-record-author/SKILL.md:5 -&gt; modules/cuo/README.md#software-development-process\n$ echo $?\n10</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Whether the checker later covers docs/feature-requests cross-references too is a cheap follow-up flag; scope here is the skill/cuo contract trees where agents read citations at run time.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Slug algorithm mismatch (unicode, duplicate headings with -1 suffixes) - implement GitHub's documented rules incl. duplicate suffixing; fixture covers a duplicated heading.</li><li>False positives on code blocks containing path-like strings - the scanner MUST skip fenced code blocks; fixture includes a fenced <code>modules/cuo/README.md</code> mention that must not fail.</li><li>Sweep sed over-matching (a skill legitimately discussing the OLD path as history) - the sweep list comes from the checker (resolution-based), not from a blind grep; historical mentions inside fenced blocks survive per #2.</li><li>Anchor exists only in the rendered site, not the markdown (generated pages) - scope is repo markdown; generated-site anchors are FR-DOCS-002's builder concern.</li><li>Checker runtime creep on big trees - single-pass with a per-file heading cache; budget &lt; 5s over modules/, asserted in t05.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep <code>DEAD </code> output grep-stable. The sweep commit should separate mechanical repoints (one commit) from the two hand-edited workflow notes (second commit) for reviewable diffs.</p>\n<p><em>End of FR-SKILL-119.</em></p>";
