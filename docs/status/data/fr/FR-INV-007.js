window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INV-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The INV service <strong>MUST</strong> ship VN hóa đơn auto-emit at <code>services/invoicing/src/hoadon/</code> triggered on first AM-send for VN tenants, XML-signed per Decree 123, transmitted to GDT via async task with verification-code polling, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> hook into <code>services/invoicing/src/handlers/invoice_send.rs</code> — on first send (AC: send_count → 1), if <code>tenant.residency='vn-1'</code> AND <code>invoice.currency='VND'</code>, enqueue hóa đơn emit task per DEC-1520. Non-VN tenants: skip silently.</li></ol>\n<ol><li><strong>MUST</strong> build XML at <code>xml_builder.rs::build(invoice)</code> per Decree 123 Annex 1 schema — root <code>&lt;HDon&gt;</code> with <code>&lt;DLHDon&gt;</code> (invoice data), <code>&lt;DSHHDVu&gt;</code> (line items), <code>&lt;TToan&gt;</code> (totals), <code>&lt;TTHDon&gt;</code> (transaction info). UTF-8, canonical form.</li></ol>\n<ol><li><strong>MUST</strong> sign XML at <code>signer.rs::sign(xml, tenant_id)</code> — load tenant's signing cert from KMS via <code>tenant.signing_cert_kms_arn</code>; XMLDSig per W3C spec embedded in <code>&lt;Signature&gt;</code> element. Sign canonical form per DEC-1521. KMS errors → fail emit (don't transmit unsigned).</li></ol>\n<ol><li><strong>MUST</strong> transmit at <code>gdt_client.rs::submit(signed_xml, tenant)</code> to <code>tenant.gdt_environment</code> URL — production https://hoadondientu.gdt.gov.vn or sandbox. Receive immediate ack with <code>hoadon_id</code>. Verification code arrives async (5min-2hr per DEC-1524).</li></ol>\n<ol><li><strong>MUST</strong> poll at <code>poller.rs::poll(hoadon_id, tenant)</code> via FR-MCP-007 task — every 5min for first hour, every 15min thereafter, max 24h. On verification_code receipt: update row to <code>accepted</code>, emit <code>inv.hoadon_verification_received</code>. On 24h timeout: status=<code>pending</code> (CFO investigates).</li></ol>\n<ol><li><strong>MUST</strong> define <code>vn_hoadon</code> table at migration <code>0007</code>: ``<code>sql CREATE TABLE vn_hoadon ( hoadon_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, invoice_id UUID NOT NULL UNIQUE, hoadon_status TEXT NOT NULL DEFAULT 'pending' CHECK (hoadon_status IN ('pending','signed','transmitted','accepted','rejected','cancelled')), gdt_invoice_number TEXT, verification_code TEXT, xml_signed BYTEA, gdt_response JSONB, submitted_at TIMESTAMPTZ, verified_at TIMESTAMPTZ, failure_reason TEXT, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now() ); CREATE UNIQUE INDEX vn_hoadon_invoice_idx ON vn_hoadon(invoice_id); ALTER TABLE vn_hoadon ENABLE ROW LEVEL SECURITY; CREATE POLICY vn_hoadon_rls ON vn_hoadon USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON vn_hoadon FROM cyberos_app; GRANT UPDATE (hoadon_status, gdt_invoice_number, verification_code, gdt_response, submitted_at, verified_at, failure_reason, updated_at) ON vn_hoadon TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> be idempotent per DEC-1522 — <code>UNIQUE(invoice_id)</code> constraint + ON CONFLICT DO NOTHING; duplicate emit attempt returns existing row.</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1526. PII scrub per FR-MEMORY-111 — invoice amounts SHA-256 hashed, only hoadon_id + status in chain.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from AM-send through emit + sign + transmit + poll.</li></ol>\n<ol><li><strong>MUST NOT</strong> transmit unsigned XML per DEC-1521.</li></ol>\n<ol><li><strong>MUST NOT</strong> emit for non-VN tenants per DEC-1520 — silent skip is the contract.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why auto-emit on send (DEC-1520)?</strong> Decree 123 requires hóa đơn at point-of-sale; \"send\" is the legal trigger. Manual emit doubles workflow.</p>\n<p><strong>Why idempotent (DEC-1522)?</strong> Retry storms must not produce duplicate hóa đơn — each duplicate is a legal violation requiring cancellation paperwork.</p>\n<p><strong>Why async polling (DEC-1524)?</strong> GDT verification code latency is 5min-2hr; sync would timeout. Polling is documented GDT-recommended pattern.</p>\n<p><strong>Why KMS-backed signing (DEC-1521)?</strong> Signing cert is tenant-specific GDT-issued; must not be checked into code or env vars. KMS provides audit trail per HSM compliance.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Endpoints (internal):</p>\n<pre><code class=\"language-text\">POST   /v1/inv/hoadon/emit            (called by invoice_send handler)\nGET    /v1/inv/hoadon/{invoice_id}    (status poll for CFO/UI)\nPOST   /v1/inv/hoadon/{id}/resubmit   (CFO-only, after rejection fix)</code></pre>\n<p>Sample XML (Decree 123 schema, abbreviated):</p>\n<pre><code class=\"language-xml\">&lt;HDon xmlns=\"http://kekhaithue.gdt.gov.vn/TKhaiHDon\"&gt;\n  &lt;DLHDon&gt;\n    &lt;TTChung&gt;\n      &lt;PBan&gt;2.0.0&lt;/PBan&gt;\n      &lt;THDon&gt;1&lt;/THDon&gt;\n      &lt;KHMSHDon&gt;1&lt;/KHMSHDon&gt;\n      &lt;KHHDon&gt;K24TAA&lt;/KHHDon&gt;\n      &lt;SHDon&gt;00000001&lt;/SHDon&gt;\n      &lt;NLap&gt;2026-05-17&lt;/NLap&gt;\n      &lt;DVTTe&gt;VND&lt;/DVTTe&gt;\n    &lt;/TTChung&gt;\n    &lt;NDHDon&gt;\n      &lt;NBan&gt;&lt;Ten&gt;{tenant_name}&lt;/Ten&gt;&lt;MST&gt;{tenant_tax_id}&lt;/MST&gt;&lt;/NBan&gt;\n      &lt;NMua&gt;&lt;Ten&gt;{customer_name}&lt;/Ten&gt;&lt;/NMua&gt;\n      &lt;DSHHDVu&gt;&lt;HHDVu&gt;...&lt;/HHDVu&gt;&lt;/DSHHDVu&gt;\n      &lt;TToan&gt;&lt;THTTLTSuat&gt;...&lt;/THTTLTSuat&gt;&lt;TgTCThue&gt;1000000&lt;/TgTCThue&gt;&lt;/TToan&gt;\n    &lt;/NDHDon&gt;\n  &lt;/DLHDon&gt;\n  &lt;DSCKS&gt;&lt;NBan&gt;&lt;Signature&gt;...&lt;/Signature&gt;&lt;/NBan&gt;&lt;/DSCKS&gt;\n&lt;/HDon&gt;</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Auto-emit on first send (VN)</strong>. 2. <strong>No emit on non-VN tenants</strong>. 3. <strong>No emit on USD/SGD/EUR invoices</strong>. 4. <strong>XML schema valid per Decree 123</strong>. 5. <strong>Signed via tenant KMS cert</strong>. 6. <strong>Idempotent (duplicate request = same hoadon_id)</strong>. 7. <strong>Status enum 6 values</strong>. 8. <strong>GDT transmission via FR-MCP-007 task</strong>. 9. <strong>Verification code polled async</strong>. 10. <strong>Failure → status=rejected + CFO notification</strong>. 11. <strong>5 memory audit kinds emitted</strong>. 12. <strong>PII scrubbed (amounts → SHA-256)</strong>. 13. <strong>RLS denies cross-tenant view</strong>. 14. <strong>KMS errors fail emit (not transmit)</strong>. 15. <strong>Retry on transient GDT 5xx</strong>. 16. <strong>Trace_id preserved</strong>. 17. <strong>Resubmit endpoint CFO-only</strong>. 18. <strong>24h poll timeout → pending+investigation</strong>. 19. <strong>GDT environment switchable per-tenant</strong>. 20. <strong>No duplicate UNIQUE invoice constraint</strong>. 21. <strong>Append-only (UPDATE on status only, no row delete)</strong>. 22. <strong>Hoadon_id stable across resubmits</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn auto_emits_on_first_vn_send() {\n    let ctx = TestContext::vn_tenant_with_signed_invoice().await;\n    ctx.send_invoice(ctx.invoice_id).await;\n    let row: VnHoadonRow = ctx.fetch_hoadon(ctx.invoice_id).await;\n    assert_eq!(row.hoadon_status, \"transmitted\");\n    assert!(row.xml_signed.is_some());\n}\n\n#[tokio::test]\nasync fn skips_non_vn_tenant() {\n    let ctx = TestContext::sg_tenant_with_invoice().await;\n    ctx.send_invoice(ctx.invoice_id).await;\n    let row = ctx.try_fetch_hoadon(ctx.invoice_id).await;\n    assert!(row.is_none());\n}\n\n#[tokio::test]\nasync fn idempotent_duplicate_emit() {\n    let ctx = TestContext::vn_tenant_with_signed_invoice().await;\n    let h1 = ctx.emit_hoadon(ctx.invoice_id).await;\n    let h2 = ctx.emit_hoadon(ctx.invoice_id).await;\n    assert_eq!(h1, h2);\n}\n\n// 5.4..5.10 — signing, retry, audit, RLS, resubmit, enum cardinality, PII scrub</code></pre>\n<hr>\n<h2>§6 — Skeleton</h2>\n<pre><code class=\"language-rust\">// services/invoicing/src/hoadon/mod.rs\npub async fn emit(invoice_id: Uuid, tenant: &amp;Tenant, db: &amp;Db) -&gt; Result&lt;HoadonId&gt; {\n    if tenant.residency != \"vn-1\" { return Err(Skip::NonVnTenant.into()); }\n    let invoice = db.fetch_invoice(invoice_id).await?;\n    if invoice.currency != \"VND\" { return Err(Skip::NonVndInvoice.into()); }\n    let existing = db.try_get_hoadon(invoice_id).await?;\n    if let Some(h) = existing { return Ok(h.hoadon_id); }\n    let trace = current_span_trace_id();\n    audit::emit(\"inv.hoadon_emit_started\", json!({\"invoice_id\": invoice_id}), trace).await?;\n    let xml = xml_builder::build(&amp;invoice, tenant)?;\n    let signed = signer::sign(&amp;xml, tenant).await?;\n    audit::emit(\"inv.hoadon_signed\", json!({\"invoice_id\": invoice_id}), trace).await?;\n    let row = db.insert_hoadon(invoice_id, &amp;signed, tenant).await?;\n    queue_transmission(row.hoadon_id, signed, tenant.clone()).await?;\n    Ok(row.hoadon_id)\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-INV-001. <strong>Cross-module:</strong> FR-MCP-007 (async task), FR-AUTH-101 (KMS), FR-MEMORY-111 (PII scrub), FR-INV-008 (cancellation flow). <strong>Tenant config:</strong> FR-SKILL-109 placeholder (signing_cert_kms_arn admin UI — created on first VN tenant signup).</p>\n<h2>§8 — Sample payloads</h2>\n<p>GDT acceptance response:</p>\n<pre><code class=\"language-json\">{\n  \"hoadon_id\": \"uuid\",\n  \"gdt_invoice_number\": \"K24TAA-00000001\",\n  \"verification_code\": \"ABCD1234EFGH\",\n  \"verified_at\": \"2026-05-17T15:00:00Z\",\n  \"status\": \"accepted\"\n}</code></pre>\n<h2>§9 — Open questions</h2>\n<p>None blocking — all per Decree 123 + GDT API docs.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>KMS cert missing</td><td>KMS 404</td><td>status=pending; sev-2 audit</td><td>CFO uploads cert</td></tr><tr><td>KMS sign error</td><td>sign() fail</td><td>status=pending; not transmitted</td><td>retry after KMS fix</td></tr><tr><td>GDT 5xx</td><td>client error</td><td>retry 3x w/ backoff</td><td>exponential 2/4/8min</td></tr><tr><td>GDT 4xx (XML invalid)</td><td>response code</td><td>status=rejected; sev-2</td><td>CFO fixes invoice → resubmit</td></tr><tr><td>Verification timeout 24h</td><td>poller expiry</td><td>status=pending; sev-1</td><td>CFO investigates GDT portal</td></tr><tr><td>Duplicate submit attempt</td><td>UNIQUE constraint</td><td>returns existing</td><td>inherent</td></tr><tr><td>Non-VN tenant call</td><td>early return</td><td>silent skip</td><td>inherent</td></tr><tr><td>Cert expired</td><td>KMS signal</td><td>fail emit; sev-1</td><td>CFO renews cert</td></tr><tr><td>Network partition mid-submit</td><td>client timeout</td><td>poll on reconnect</td><td>inherent (idempotent)</td></tr><tr><td>GDT environment misconfig</td><td>wrong URL</td><td>rejected by sandbox/prod</td><td>CFO fixes tenant config</td></tr><tr><td>Verification code already used</td><td>GDT 409</td><td>mark cancelled+reissue</td><td>CFO escalation</td></tr><tr><td>Tenant tax_id missing</td><td>XML build fail</td><td>status=pending; sev-2</td><td>onboarding fix</td></tr><tr><td>Audit chain pause</td><td>memory unavailable</td><td>retry emit</td><td>per FR-MEMORY-111</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 XML canonicalization per W3C Canonical XML 1.1 — required for valid XMLDSig.</li><li>§11.2 Signing cert held in AWS KMS as asymmetric RSA-2048 key; tenant's GDT registration ties public half.</li><li>§11.3 GDT environment URLs per tenant config — switchable test→prod without code deploy.</li><li>§11.4 Polling intervals tuned per GDT recommended pattern (5min × 12, then 15min × 92, total 24h).</li><li>§11.5 PII: amounts/customer info never in memory — only hoadon_id, status, gdt_invoice_number (public).</li><li>§11.6 Per Decree 123 Art. 19: hóa đơn must be transmitted within 60s of invoice issuance (async ok if reasonable effort).</li></ul>\n<hr>\n<p><em>End of FR-INV-007 spec.</em></p>";
