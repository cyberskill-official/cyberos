window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TIME-009"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TIME service <strong>MUST</strong> ship per-cycle billable rollup at <code>services/time/src/rollup/</code> producing per-Member × role × project × task aggregations consumed by FR-INV-001, with idempotency cache, rate-card snapshot, status/billable filtering, and 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/time/rollup</code> body <code>{ engagement_id, cycle_start_date, cycle_end_date }</code>. Caller has <code>cfo</code> OR <code>engagement_admin</code> role. Handler:</li></ol>\n<ul><li>Check cache by <code>(engagement_id, cycle_end_date)</code>; hit → return cached + emit <code>time.rollup_idempotent_hit</code>.</li><li>Else: invoke aggregator; cache result; emit <code>time.rollup_started</code> + <code>time.rollup_completed</code>.</li></ul>\n<ol><li><strong>MUST</strong> aggregate via <code>aggregator.rs::aggregate(engagement_id, start, end)</code>: ``<code>sql SELECT member_subject_id, role_on_engagement, project_id, task_id, SUM(duration_seconds) AS total_seconds, COUNT(*) AS entry_count FROM time_entries e JOIN timesheets t ON t.member_subject_id = e.member_subject_id AND t.engagement_id = e.engagement_id AND e.entry_date BETWEEN t.week_start_date AND t.week_end_date WHERE e.engagement_id = $1 AND e.entry_date BETWEEN $2 AND $3 AND e.is_billable = true AND t.status = 'locked' GROUP BY member_subject_id, role_on_engagement, project_id, task_id </code>``</li></ol>\n<ol><li><strong>MUST</strong> apply rate-card per DEC-1433 via <code>rate_card_apply.rs::apply(rollup_row, rate_card)</code>. For each aggregation row, look up rate by (role_on_engagement, project_kind) → produce <code>unit_price_minor</code> + computed <code>amount_minor = total_seconds / 3600 * unit_price_minor</code>.</li></ol>\n<ol><li><strong>MUST</strong> define rollup cache table at migration <code>0007</code>: <code>(engagement_id UUID NOT NULL, cycle_end_date DATE NOT NULL, result_jsonb JSONB NOT NULL, rate_card_snapshot JSONB NOT NULL, rolled_up_at TIMESTAMPTZ NOT NULL DEFAULT now(), rolled_up_by_subject_id UUID NOT NULL, trace_id CHAR(32), PRIMARY KEY (engagement_id, cycle_end_date))</code>. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS scoped to tenant_id.</li></ol>\n<ol><li><strong>MUST</strong> return result shape consumable by FR-INV-001: ``<code>json { \"engagement_id\": \"...\", \"cycle_start\": \"2026-04-17\", \"cycle_end\": \"2026-05-17\", \"rate_card_snapshot\": { /* full card */ }, \"lines\": [ { \"member_subject_id\": \"...\", \"role_on_engagement\": \"senior_consultant\", \"project_id\": \"...\", \"task_id\": \"...\", \"total_seconds\": 144000, \"unit_price_minor\": 250_00, \"amount_minor\": 10_000_00 } ], \"totals\": { \"total_seconds\": ..., \"amount_minor\": ... } } </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1434.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> include non-billable or unlocked entries.</li></ol>\n<ol><li><strong>MUST NOT</strong> re-run rollup for cached (engagement, cycle_end) — return cached.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why idempotent caching (§1 #1, DEC-1431)?</strong> Rollup is computationally expensive; multiple INV-001 retry attempts would re-aggregate. Cache eliminates waste + ensures determinism (re-run produces same numbers).</p>\n<p><strong>Why timesheets='locked' filter (§1 #2, DEC-1432)?</strong> Pre-approval entries are draft; including would create invoices on un-reviewed data → trust + audit gap.</p>\n<p><strong>Why rate-card snapshot in result (§1 #3, DEC-1433)?</strong> FR-INV-001 needs the snapshot to fulfill DEC-1363. This FR provides it ready-to-copy.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0007_rollup_cache.sql\nCREATE TABLE rollup_cache (\n  engagement_id UUID NOT NULL,\n  cycle_end_date DATE NOT NULL,\n  tenant_id UUID NOT NULL,\n  result_jsonb JSONB NOT NULL,\n  rate_card_snapshot JSONB NOT NULL,\n  rolled_up_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  rolled_up_by_subject_id UUID NOT NULL,\n  trace_id CHAR(32),\n  PRIMARY KEY (engagement_id, cycle_end_date)\n);\nALTER TABLE rollup_cache ENABLE ROW LEVEL SECURITY;\nCREATE POLICY rollup_cache_rls ON rollup_cache\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON rollup_cache FROM cyberos_app;</code></pre>\n<p>Endpoint: <code>POST /v1/time/rollup</code>.</p>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Rollup aggregates per Member/role/project/task</strong> — fixture entries produce expected groupby.</li><li><strong>Idempotent</strong> — second call returns cached + audit.</li><li><strong>Excludes non-billable</strong> — <code>is_billable=false</code> entries omitted.</li><li><strong>Excludes unlocked</strong> — <code>timesheets.status≠'locked'</code> entries omitted.</li><li><strong>Rate-card snapshot returned</strong> — <code>result.rate_card_snapshot</code> present.</li><li><strong>Amount = seconds/3600 × unit_price</strong> — math correct.</li><li><strong>4 memory audit kinds emitted</strong>.</li><li><strong>CFO or engagement_admin only</strong> — other roles 403.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>RLS cross-tenant denied</strong>.</li><li><strong>Empty rollup (no entries)</strong> — returns 0-line result; cached.</li><li><strong>Cycle dates inclusive</strong> — entries on cycle_end_date included.</li><li><strong>Multi-role same Member</strong> — different role_on_engagement → separate lines.</li><li><strong>Long-running rollup → task</strong> — &gt; 30s engagement uses FR-MCP-007 pattern (slice 2 enhancement).</li><li><strong>Cache PRIMARY KEY enforces idempotency</strong> — concurrent calls race-safe.</li><li><strong>Total seconds sum matches</strong> — Σ(lines) == result.totals.total_seconds.</li><li><strong>Rate card missing for role</strong> — line emitted with 0 unit_price + warning.</li><li><strong>Member without engagement role</strong> — skipped + sev-3 audit.</li><li><strong>Audit emit on idempotent hit</strong> — <code>time.rollup_idempotent_hit</code> row added.</li><li><strong>JSON shape stable</strong> — FR-INV-001 consumer tested against fixture.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn rollup_aggregates_per_member_role_project() {\n    let ctx = TestContext::with_engagement_and_locked_entries().await;\n    let r = ctx.post_rollup(ctx.eng_id, ctx.cycle_start, ctx.cycle_end).await;\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert!(body[\"lines\"].as_array().unwrap().len() &gt; 0);\n    let total_seconds: i64 = body[\"totals\"][\"total_seconds\"].as_i64().unwrap();\n    let line_sum: i64 = body[\"lines\"].as_array().unwrap().iter()\n        .map(|l| l[\"total_seconds\"].as_i64().unwrap()).sum();\n    assert_eq!(total_seconds, line_sum);\n}\n\n#[tokio::test]\nasync fn rollup_idempotent() {\n    let ctx = TestContext::with_engagement_and_locked_entries().await;\n    let r1 = ctx.post_rollup(ctx.eng_id, ctx.cycle_start, ctx.cycle_end).await;\n    let r2 = ctx.post_rollup(ctx.eng_id, ctx.cycle_start, ctx.cycle_end).await;\n    let b1: serde_json::Value = r1.json().await.unwrap();\n    let b2: serde_json::Value = r2.json().await.unwrap();\n    assert_eq!(b1, b2);\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"time.rollup_idempotent_hit\"));\n}\n\n#[tokio::test]\nasync fn rollup_excludes_non_billable_and_unlocked() {\n    let ctx = TestContext::new().await;\n    ctx.seed_entry(billable: true, status: \"locked\").await;\n    ctx.seed_entry(billable: false, status: \"locked\").await;\n    ctx.seed_entry(billable: true, status: \"submitted\").await;\n    let r = ctx.post_rollup_minimal().await;\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"lines\"].as_array().unwrap().len(), 1);\n}\n\n// 5.4..5.6: rate-card snapshot, role check, audit kinds</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-TIME-005 (billable flag); transitively FR-TIME-001 + FR-TIME-006 (locked status). <strong>Downstream:</strong> FR-INV-001 (consumes rollup result). <strong>Cross-module:</strong> FR-AUTH-101 (role gates), FR-AI-003, FR-MEMORY-111.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>time.rollup_completed</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.rollup_completed\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.cfo.789\",\n  \"trace_id\": \"...\",\n  \"payload\": {\n    \"engagement_id\": \"0190...\",\n    \"cycle_start\": \"2026-04-17\", \"cycle_end\": \"2026-05-17\",\n    \"line_count\": 12,\n    \"total_seconds\": 875000,\n    \"total_amount_minor\": 21_875_00\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-currency rollup (currently single-currency per engagement) — slice 2.</li><li><strong>Deferred:</strong> Per-task rate-card overrides — slice 2.</li><li><strong>Deferred:</strong> Async via FR-MCP-007 Tasks for huge engagements — slice 2.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Engagement has no rate card</td><td>resolver miss</td><td>412 + <code>no_rate_card</code></td><td>Engagement admin configures</td></tr><tr><td>Cache PRIMARY KEY race</td><td>concurrent INSERT</td><td>Second sees ON CONFLICT; returns cached</td><td>Inherent</td></tr><tr><td>Rate-card mid-cycle change</td><td>resolver uses cycle_end version</td><td>Stable across reruns</td><td>Inherent</td></tr><tr><td>Member transferred mid-cycle</td><td>role_on_engagement snapshot at entry time</td><td>Each line reflects role-at-time</td><td>Inherent</td></tr><tr><td>Cycle includes future date</td><td>date validation</td><td>400</td><td>Inherent</td></tr><tr><td>Engagement archived during rollup</td><td>status check</td><td>Sev-2 audit; rollup proceeds (historical data still billable)</td><td>Inherent</td></tr><tr><td>Rate-card missing for specific role</td><td>line with 0 unit_price + warning audit</td><td>Engagement_admin completes card</td><td>Inherent</td></tr><tr><td>Cache JSONB &gt; 100 KB</td><td>size limit</td><td>sev-2 alert</td><td>Investigate engagement size</td></tr><tr><td>Cross-tenant rollup</td><td>RLS</td><td>0 rows; empty result</td><td>Inherent</td></tr><tr><td>Member without locked timesheet for week</td><td>entry filtered</td><td>Inherent</td><td>Member submits + AM approves</td></tr><tr><td>Concurrent locked status change</td><td>tx isolation</td><td>Cycle uses status-at-rollup-time</td><td>Inherent</td></tr><tr><td>Database query timeout</td><td>timeout 30s</td><td>503</td><td>Slice 2 async via Tasks</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Aggregation uses single SQL GROUP BY for performance.</p>\n<p><strong>§11.2</strong> Rate card lookup per (role, project_kind); falls back to engagement default.</p>\n<p><strong>§11.3</strong> Cache PRIMARY KEY on (engagement_id, cycle_end_date) enforces idempotency at schema level.</p>\n<p><strong>§11.4</strong> Result JSONB serialized canonically (sorted keys) for deterministic hash.</p>\n<p><strong>§11.5</strong> FR-INV-001 invokes rollup synchronously then creates invoice with result.</p>\n<p><strong>§11.6</strong> Idempotent hit emits separate audit kind for forensic clarity.</p>\n<p><strong>§11.7</strong> Engagement timezone applied to cycle date interpretation.</p>\n<p><strong>§11.8</strong> Cache pruned at slice 3 (12-month retention).</p>\n<p><strong>§11.9</strong> Trace_id propagated from FR-INV-001 caller through rollup to FR-INV-001 invoice row.</p>\n<p><strong>§11.10</strong> PII: Member subject_id retained for traceability; description not in rollup payload (entries already filtered + aggregated).</p>\n<hr>\n<p><em>End of FR-TIME-009 spec.</em></p>";
