window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-103"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> ship SAML 2.0 SP-initiated SSO with per-tenant IdP config + XML signature verification + assertion validation + JIT provisioning + attribute mapping + replay defense. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>auth_saml_idp_configs</code> table: <code>(id UUID PRIMARY KEY, tenant_id UUID NOT NULL, name TEXT NOT NULL, entity_id_idp TEXT NOT NULL, sso_url TEXT NOT NULL, metadata_url TEXT, x509_cert_pem TEXT NOT NULL, sp_signing_key_kms_blob BYTEA NOT NULL, sp_signing_kms_key_id TEXT NOT NULL, sp_entity_id TEXT NOT NULL, acs_url TEXT NOT NULL, attribute_mapping_yaml TEXT NOT NULL, is_active BOOLEAN NOT NULL DEFAULT true, created_at TIMESTAMPTZ, created_by_subject_id UUID NOT NULL)</code>. UNIQUE <code>(tenant_id, name)</code>; partial unique <code>(tenant_id, entity_id_idp) WHERE is_active=true</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_saml_login_history</code> table: <code>(id BIGSERIAL, tenant_id UUID, idp_id UUID, subject_id UUID, nameid TEXT NOT NULL, nameid_format TEXT NOT NULL, outcome TEXT NOT NULL CHECK (outcome IN ('succeeded','failed','jit_provisioned')), failure_reason TEXT, source_ip_hash16 TEXT, ts TIMESTAMPTZ)</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_saml_authn_request_log</code> table for replay defense: <code>(id UUID PRIMARY KEY, tenant_id UUID, idp_id UUID, issued_at TIMESTAMPTZ, expires_at TIMESTAMPTZ NOT NULL, consumed BOOLEAN NOT NULL DEFAULT false, consumed_at TIMESTAMPTZ)</code>. UNIQUE on <code>id</code>; TTL 10 minutes.</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_saml_subject_link</code> table: <code>(idp_id UUID, nameid TEXT, tenant_id UUID, subject_id UUID NOT NULL REFERENCES auth.subjects(id), linked_at TIMESTAMPTZ, PRIMARY KEY (idp_id, nameid))</code>. Per-(idp_id, nameid) uniqueness (same shape as FR-AUTH-104 OIDC).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with <code>USING + WITH CHECK</code> on all 4 tables; root-admin escape clause.</li></ol>\n<ol><li><strong>MUST</strong> implement SP-initiated flow only (per DEC-520):</li></ol>\n<ul><li><code>GET /v1/auth/saml/initiate?idp_id=&lt;&gt;</code> — builds SAMLRequest XML, signs with SP signing key, base64-encodes, redirects to IdP's sso_url via HTTP Redirect binding.</li><li><code>POST /v1/auth/saml/acs</code> — receives SAMLResponse via HTTP POST binding; parses + verifies + provisions/links subject; redirects to tenant SPA on success. IdP-initiated unsolicited responses (POST to ACS without prior AuthnRequest) → 401 <code>unsolicited_response_rejected</code> + sev-2 audit per DEC-520.</li></ul>\n<ol><li><strong>MUST</strong> fetch IdP metadata at config save time (per DEC-523). Parse <code>EntityDescriptor</code> → extract <code>SingleSignOnService</code> URL + <code>X509Certificate</code>. Cache 24h with kid-style overlap on cert rotation (new cert accepted immediately; old cert accepted for 24h after observed rotation).</li></ol>\n<ol><li><strong>MUST</strong> require BOTH <code>WantAssertionsSigned=true</code> AND <code>AuthnRequestsSigned=true</code> (per DEC-522). Response-only-signature (Microsoft Azure AD default config) → reject with <code>assertion_signature_required</code> + emit <code>auth.saml_signature_invalid</code> memory row.</li></ol>\n<ol><li><strong>MUST</strong> enforce closed NameIDFormat (per DEC-524). Accepted formats: <code>urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress</code> + <code>urn:oasis:names:tc:SAML:2.0:nameid-format:persistent</code>. Others (transient, unspecified, x509SubjectName, etc.) → 401 <code>nameid_format_unsupported</code>.</li></ol>\n<ol><li><strong>MUST</strong> implement replay defense via InResponseTo (per DEC-525):</li></ol>\n<ul><li>AuthnRequest's ID stored in <code>auth_saml_authn_request_log</code> with 10-min TTL.</li><li>SAMLResponse's <code>InResponseTo</code> attribute MUST match a known unconsumed row.</li><li>Missing/unknown/expired/consumed → 401 <code>saml_replay_or_expired</code> + emit <code>auth.saml_assertion_replay_attempted</code> (sev-2) memory row.</li><li>On success, mark consumed.</li></ul>\n<ol><li><strong>MUST</strong> validate assertion conditions (per DEC-526):</li></ol>\n<ul><li><code>NotBefore</code> ≤ now ≤ <code>NotOnOrAfter</code> with ±60s clock skew tolerance.</li><li><code>SubjectConfirmation/SubjectConfirmationData.Recipient</code> MUST exactly match SP ACS URL.</li><li><code>SubjectConfirmation/SubjectConfirmationData.InResponseTo</code> MUST match outer InResponseTo.</li><li><code>Conditions/AudienceRestriction/Audience</code> MUST contain SP entity_id (per DEC-536). Any fail → 401 + <code>auth.saml_login_failed</code> + reason field.</li></ul>\n<ol><li><strong>MUST</strong> verify XML signature with restricted transforms (per DEC-531). Allowed transforms:</li></ol>\n<ul><li><code>http://www.w3.org/2001/10/xml-exc-c14n#</code> (Exclusive Canonicalization).</li><li><code>http://www.w3.org/2000/09/xmldsig#enveloped-signature</code>. Any other transform (especially <code>xpath-2.0</code>, <code>xslt</code>) → reject with <code>unsupported_transform</code> per XSW attack defense.</li></ul>\n<ol><li><strong>MUST</strong> require RSA-SHA256 minimum signature algorithm (per DEC-532). <code>SignatureMethod</code> must be one of:</li></ol>\n<ul><li><code>http://www.w3.org/2001/04/xmldsig-more#rsa-sha256</code> ✓</li><li><code>http://www.w3.org/2001/04/xmldsig-more#ecdsa-sha256</code> ✓</li><li><code>http://www.w3.org/2001/04/xmldsig-more#ecdsa-sha384</code> ✓ SHA-1 (<code>http://www.w3.org/2000/09/xmldsig#rsa-sha1</code>) → 401 <code>weak_signature_algorithm</code>.</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>nameid</code>, <code>failure_reason</code>, and the <code>AttributeStatement</code> value contents via FR-MEMORY-111 before chain commit.</li></ol>\n<ol><li><strong>MUST</strong> JIT-provision subject on first login (per DEC-528). Same shape as FR-AUTH-104:</li></ol>\n<ul><li>Lookup <code>auth_saml_subject_link WHERE idp_id=$1 AND nameid=$2</code> — found → use linked subject_id.</li><li>Not found → call FR-AUTH-002 internal helper with <code>email</code> from nameid (if emailAddress format) or AttributeStatement's <code>email</code> claim; assign role per attribute_mapping_yaml; INSERT link row; emit <code>auth.saml_jit_provisioned</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> apply per-tenant attribute → role mapping (per DEC-528). Reuses FR-AUTH-104 YAML shape but with SAML AttributeStatement claim names: ```yaml default_role: tenant-member claim_rules:</li></ol>\n<ul><li>attribute: \"http://schemas.xmlsoap.org/claims/Group\"   # AD-style contains: \"Domain Engineers\" grant_role: tenant-admin</li><li>attribute: \"groups\"   # generic contains: \"Finance\" grant_role: cfo ``<code> Validation: every </code>grant_role` MUST parse to FR-AUTH-101 closed Role enum at config save time.</li></ul>\n<ol><li><strong>MUST</strong> enforce max 2 active SAML IdP configs per tenant (per DEC-534). 3rd → 409 <code>idp_config_limit_exceeded</code>. ADR required.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/saml/idp-configs</code> handler for tenant-admin SAML IdP CRUD. Caller MUST have role <code>tenant-admin</code>. Validates:</li></ol>\n<ul><li>metadata_url fetch succeeds.</li><li>x509_cert_pem parses + matches metadata.</li><li>attribute_mapping_yaml parses + all roles valid.</li><li>SP signing key generated (RSA-2048) + KMS-encrypted.</li><li>Returns SP metadata for IdP-side configuration (entity_id, ACS URL, signing certificate).</li></ul>\n<ol><li><strong>MUST</strong> emit 7 memory audit row kinds (per DEC-529):</li></ol>\n<ul><li><code>auth.saml_login_succeeded</code> — full flow → AUTH JWT issued.</li><li><code>auth.saml_login_failed</code> — any failure with reason.</li><li><code>auth.saml_jit_provisioned</code> — new subject on first login.</li><li><code>auth.saml_metadata_refreshed</code> — IdP metadata fetch updated cert.</li><li><code>auth.saml_idp_config_changed</code> — POST/PATCH/DELETE on idp_configs.</li><li><code>auth.saml_signature_invalid</code> — sev-2 (may signal attack).</li><li><code>auth.saml_assertion_replay_attempted</code> — sev-2 InResponseTo violation.</li></ul>\n<ol><li><strong>MUST</strong> complete ACS callback handler in ≤ 500 ms p95 (XML signature verify is the dominant cost). <code>saml_perf_test</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>auth.saml.{initiate,acs,jit_provision,idp_config_change,metadata_refresh}</code> with <code>outcome</code> attribute (success | jit_provisioned | unsolicited_response | replay_or_expired | signature_invalid | assertion_signature_required | weak_signature_algorithm | unsupported_transform | audience_mismatch | recipient_mismatch | not_before_violated | not_on_or_after_violated | nameid_format_unsupported | unknown_role | metadata_fetch_failed | sub_already_linked).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_saml_login_total{tenant_id, idp_id, outcome}</code> (counter).</li><li><code>auth_saml_jit_provisioned_total{tenant_id}</code> (counter).</li><li><code>auth_saml_metadata_refreshes_total{tenant_id, idp_id}</code> (counter).</li><li><code>auth_saml_signature_failures_total{tenant_id, idp_id}</code> (counter — sev-2 alarm &gt; 5/h).</li><li><code>auth_saml_replay_attempts_total{tenant_id}</code> (counter — sev-2 alarm &gt; 3/h).</li><li><code>auth_saml_flow_latency_ms</code> (histogram; SLO p95 &lt; 500ms).</li></ul>\n<ol><li><strong>MUST</strong> ship the <code>samael</code> crate (or equivalent Rust SAML library audited for security) for XML parsing + signature verification + canonicalization. Hand-rolled XML signature is forbidden — too easy to misimplement (see XSW + XML signature attack literature).</li></ol>\n<ol><li><strong>MUST</strong> support per-tenant SP entity_id format <code>https://auth.&lt;tenant_slug&gt;.cyberos.world/saml/sp</code> and ACS URL format <code>https://auth.&lt;tenant_slug&gt;.cyberos.world/v1/auth/saml/acs</code>. Validated at IdP config save time.</li></ol>\n<ol><li><strong>MUST</strong> sign every outbound AuthnRequest with the per-tenant SP signing key (per DEC-522 <code>AuthnRequestsSigned=true</code>). KMS-decrypt the private key for signing; never expose plaintext.</li></ol>\n<ol><li><strong>MUST</strong> include <code>RequestedAuthnContext</code> with <code>AuthnContextClassRef = urn:oasis:names:tc:SAML:2.0:ac:classes:PasswordProtectedTransport</code> at minimum; tenants requiring MFA enforcement at IdP MAY configure <code>MultiFactor</code> via per-tenant override.</li></ol>\n<ol><li><strong>MUST</strong> support metadata XML download at <code>GET /v1/auth/saml/idp-configs/{id}/sp-metadata</code> — returns SP-side metadata XML for IdP operators to configure their end (entity_id, ACS URL, X.509 signing cert).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why SP-initiated only at slice 1 (DEC-520)?</strong> IdP-initiated unsolicited responses lack the InResponseTo binding to a known SP request — an attacker hosting their own IdP can mint assertions for arbitrary subjects (the \"evil IdP\" attack). OASIS recommends SP-initiated. Some legitimate use cases (bookmark-from-IdP-portal) need IdP-initiated; deferred to FR-AUTH-2xx with per-tenant explicit opt-in + additional defenses.</p>\n<p><strong>Why both AuthnRequestsSigned AND WantAssertionsSigned (DEC-522)?</strong> Signing AuthnRequests proves SP authenticity to IdP (prevents request forgery). Signing assertions proves IdP authenticity to SP (prevents assertion injection). Response-only signing (Azure AD default) is insufficient — the assertion within an unsigned envelope can be swapped via XSW. Both signatures together = full chain validated.</p>\n<p><strong>Why exc-c14n only + restricted transforms (DEC-531)?</strong> XML Signature Wrapping (XSW) attacks exploit canonicalization differences to validate a signature against one element while the application reads a different element. Restricting transforms to <code>exc-c14n + enveloped-signature</code> eliminates the attack surface; the alternative (xpath, xslt) lets attackers craft assertions that pass signature verification but contain attacker-controlled content.</p>\n<p><strong>Why RSA-SHA256 minimum (DEC-532)?</strong> SHA-1 collision attacks (SHAttered + Polonen 2020) make forged signatures feasible at nation-state level. NIST has deprecated SHA-1 for digital signatures. SHA-256 minimum is the current baseline; SHA-384 ECDSA accepted for high-security tenants.</p>\n<p><strong>Why InResponseTo validation (DEC-525)?</strong> Without it, an attacker captures a legitimate SAMLResponse + replays. With it, each response binds to a specific unconsumed AuthnRequest ID — replay fails. 10-min TTL covers slow IdP responses without leaving consumed requests usable indefinitely.</p>\n<p><strong>Why ±60s clock skew (DEC-526)?</strong> NTP drift between IdP + SP is typically &lt; 1s but can spike during NTP outages. ±60s tolerates outages without false-rejection; tighter would false-reject legitimate flows.</p>\n<p><strong>Why Audience + Recipient + InResponseTo checks (§1 #11, DEC-536)?</strong> Defense in depth: even if signature verification fails to detect XSW (subtle bug), the Audience + Recipient + InResponseTo checks add second + third + fourth gates. Any missed claim = reject.</p>\n<p><strong>Why closed NameIDFormat (DEC-524)?</strong> Transient nameids are session-scoped (different each login) — useless for our subject_link table. Unspecified is operator-discretion — could be anything. EmailAddress + persistent are the two formats with stable subject identity over time. Closing the set prevents IdP config drift.</p>\n<p><strong>Why per-tenant attribute mapping YAML (DEC-528, §1 #16)?</strong> Different IdPs use different attribute names: Okta uses <code>groups</code>; Azure AD uses <code>http://schemas.xmlsoap.org/claims/Group</code>; Google uses scope; ADFS uses claim types. Per-tenant YAML lets each tenant configure their IdP-specific mapping without code changes. Reuses FR-AUTH-104's YAML shape for consistency.</p>\n<p><strong>Why max 2 IdP configs per tenant (DEC-534)?</strong> Enterprise tenants typically have one primary IdP (corporate AD) and at most a fallback (e.g. during IdP migration). &gt; 2 suggests config sprawl; ADR-required to raise.</p>\n<p><strong>Why samael crate not hand-rolled (§1 #23)?</strong> SAML signature verification is famously bug-prone (10+ public CVEs from incorrect parser implementations in 2015-2020 alone). Battle-tested crate is the only safe path. Hand-rolled XML signature is forbidden at slice 1.</p>\n<p><strong>Why SLO deferred (DEC-535)?</strong> Single Logout (SLO) is operationally complex (cascading logout across multiple SPs; partial-failure handling; user-visible session inconsistency). Slice 1 covers login; tenant session expiry handles logout adequately for slice 1. SLO ships in FR-AUTH-2xx when enterprise tenants demand it.</p>\n<p><strong>Why KMS-encrypt SP signing key (§1 #25, DEC-527)?</strong> SP signing key proves to IdP that AuthnRequests are from us. Compromise = attacker forges AuthnRequests for any subject. KMS encryption requires KMS-decrypt permission to sign — meaningful additional barrier.</p>\n<p><strong>Why metadata XML download endpoint (§1 #27)?</strong> IdP operators (corporate IT) need our SP metadata (entity_id, ACS URL, signing cert) to configure their side. Standard SAML SP exposes this at a well-known URL. Per-tenant URL makes copy-paste setup easy.</p>\n<p><strong>Why min RequestedAuthnContext = PasswordProtectedTransport (§1 #26)?</strong> Baseline: IdP must authenticate user with at least password + TLS transport. Lower (e.g. <code>PreviousSession</code>) = relying on browser session — phishable. Tenants requiring MFA at IdP override via config; our slice 1 ships baseline + MFA-via-FR-AUTH-102 at SP side.</p>\n<p><strong>Why 7 memory audit kinds (DEC-529)?</strong> Different operator queries: \"show me all successful SAML logins this week\" → <code>auth.saml_login_succeeded</code>. \"Show me signature failures\" → <code>auth.saml_signature_invalid</code>. \"Show me replay attempts\" → <code>auth.saml_assertion_replay_attempted</code>. Selectivity benefits at query time.</p>\n<p><strong>Why metadata 24h cache + cert rotation overlap (§1 #7, DEC-523)?</strong> IdPs rotate signing certs periodically. 24h cache avoids hot-path metadata refetch. Overlap (old cert + new cert both valid) prevents flap during rotation. Mirrors FR-AUTH-104 JWKS pattern.</p>\n<p><strong>Why per-tenant <code>entity_id</code> format (§1 #24)?</strong> Distinct per-tenant entity_id lets IdP-side admins manage per-tenant SP registrations independently. Conflict with another tenant's entity_id is structurally impossible.</p>\n<p><strong>Why append-only login_history at SQL grant (§1 #2, DEC-530)?</strong> Forensic record of all login attempts including failures. UPDATE/DELETE blocked prevents tampering. Failed logins (with reason) help diagnose attacks + misconfigurations.</p>\n<p><strong>Why sev-2 alarm on &gt; 5/h signature failures + &gt; 3/h replay attempts (§1 #22)?</strong> Both indicate either (a) IdP misconfiguration + needs operator help, or (b) active attack. Sev-2 = operator investigates within an hour.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0021 — idp_configs</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0021_saml_idp_configs.sql\n\nBEGIN;\n\nCREATE TABLE auth_saml_idp_configs (\n    id                          UUID         PRIMARY KEY,\n    tenant_id                   UUID         NOT NULL,\n    name                        TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 100),\n    entity_id_idp               TEXT         NOT NULL,\n    sso_url                     TEXT         NOT NULL,\n    metadata_url                TEXT,\n    x509_cert_pem               TEXT         NOT NULL,\n    sp_signing_key_kms_blob     BYTEA        NOT NULL,\n    sp_signing_kms_key_id       TEXT         NOT NULL,\n    sp_entity_id                TEXT         NOT NULL,\n    acs_url                     TEXT         NOT NULL,\n    attribute_mapping_yaml      TEXT         NOT NULL,\n    is_active                   BOOLEAN      NOT NULL DEFAULT true,\n    created_at                  TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id       UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nCREATE UNIQUE INDEX uniq_saml_idp_name ON auth_saml_idp_configs (tenant_id, name);\nCREATE UNIQUE INDEX uniq_active_saml_idp_entity ON auth_saml_idp_configs (tenant_id, entity_id_idp) WHERE is_active = true;\n\nALTER TABLE auth_saml_idp_configs ENABLE ROW LEVEL SECURITY;\nCREATE POLICY saml_idp_configs_tenant_iso ON auth_saml_idp_configs\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0022 — login_history</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0022_saml_login_history.sql\n\nBEGIN;\n\nCREATE TABLE auth_saml_login_history (\n    id                  BIGSERIAL    PRIMARY KEY,\n    tenant_id           UUID         NOT NULL,\n    idp_id              UUID         NOT NULL REFERENCES auth_saml_idp_configs(id),\n    subject_id          UUID         REFERENCES auth.subjects(id),\n    nameid              TEXT         NOT NULL,\n    nameid_format       TEXT         NOT NULL,\n    outcome             TEXT         NOT NULL CHECK (outcome IN ('succeeded','failed','jit_provisioned')),\n    failure_reason      TEXT,\n    source_ip_hash16    TEXT,\n    ts                  TIMESTAMPTZ  NOT NULL DEFAULT now()\n);\n\nCREATE INDEX saml_login_history_tenant_ts_idx ON auth_saml_login_history (tenant_id, ts DESC);\nCREATE INDEX saml_login_history_subject_idx ON auth_saml_login_history (subject_id) WHERE subject_id IS NOT NULL;\n\nALTER TABLE auth_saml_login_history ENABLE ROW LEVEL SECURITY;\nCREATE POLICY saml_login_history_tenant_iso ON auth_saml_login_history\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON auth_saml_login_history FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Migration 0023 — authn_request_log (replay defense)</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0023_saml_authn_request_log.sql\n\nBEGIN;\n\nCREATE TABLE auth_saml_authn_request_log (\n    id              UUID         PRIMARY KEY,\n    tenant_id       UUID         NOT NULL,\n    idp_id          UUID         NOT NULL REFERENCES auth_saml_idp_configs(id),\n    issued_at       TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    expires_at      TIMESTAMPTZ  NOT NULL,\n    consumed        BOOLEAN      NOT NULL DEFAULT false,\n    consumed_at     TIMESTAMPTZ\n);\n\nCREATE INDEX saml_authn_request_expires_idx ON auth_saml_authn_request_log (expires_at) WHERE consumed = false;\n\nALTER TABLE auth_saml_authn_request_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY saml_authn_log_tenant_iso ON auth_saml_authn_request_log\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE DELETE ON auth_saml_authn_request_log FROM cyberos_app;\nGRANT UPDATE (consumed, consumed_at) ON auth_saml_authn_request_log TO cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.4 — Migration 0024 — subject_link</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0024_saml_subject_link.sql\n\nBEGIN;\n\nCREATE TABLE auth_saml_subject_link (\n    idp_id       UUID         NOT NULL REFERENCES auth_saml_idp_configs(id) ON DELETE RESTRICT,\n    nameid       TEXT         NOT NULL,\n    tenant_id    UUID         NOT NULL,\n    subject_id   UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    linked_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    PRIMARY KEY (idp_id, nameid)\n);\n\nCREATE INDEX saml_subject_link_subject_idx ON auth_saml_subject_link (tenant_id, subject_id);\n\nALTER TABLE auth_saml_subject_link ENABLE ROW LEVEL SECURITY;\nCREATE POLICY saml_subject_link_tenant_iso ON auth_saml_subject_link\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.5 — AuthnRequest builder</h3>\n<pre><code class=\"language-rust\">// services/auth/src/saml/authn_request.rs\nuse chrono::{DateTime, Utc};\nuse uuid::Uuid;\n\npub struct AuthnRequestParams {\n    pub id: Uuid,\n    pub issue_instant: DateTime&lt;Utc&gt;,\n    pub destination: String,        // IdP SSO URL\n    pub sp_entity_id: String,\n    pub acs_url: String,\n}\n\npub fn build_xml(p: &amp;AuthnRequestParams) -&gt; String {\n    format!(r#\"&lt;samlp:AuthnRequest xmlns:samlp=\"urn:oasis:names:tc:SAML:2.0:protocol\"\n    xmlns:saml=\"urn:oasis:names:tc:SAML:2.0:assertion\"\n    ID=\"_{id}\" Version=\"2.0\" IssueInstant=\"{ts}\" Destination=\"{dest}\"\n    AssertionConsumerServiceURL=\"{acs}\" ProtocolBinding=\"urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST\"&gt;\n  &lt;saml:Issuer&gt;{sp}&lt;/saml:Issuer&gt;\n  &lt;samlp:NameIDPolicy AllowCreate=\"true\" Format=\"urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress\"/&gt;\n  &lt;samlp:RequestedAuthnContext Comparison=\"minimum\"&gt;\n    &lt;saml:AuthnContextClassRef&gt;urn:oasis:names:tc:SAML:2.0:ac:classes:PasswordProtectedTransport&lt;/saml:AuthnContextClassRef&gt;\n  &lt;/samlp:RequestedAuthnContext&gt;\n&lt;/samlp:AuthnRequest&gt;\"#,\n        id = p.id.to_simple(), ts = p.issue_instant.to_rfc3339(),\n        dest = p.destination, acs = p.acs_url, sp = p.sp_entity_id,\n    )\n}</code></pre>\n<h3>3.6 — Response verifier (signature + assertion conditions)</h3>\n<pre><code class=\"language-rust\">// services/auth/src/saml/response_verifier.rs\nuse chrono::{DateTime, Duration, Utc};\nuse samael::{schema::Response, service_provider::ServiceProvider};\n\nconst CLOCK_SKEW_SECONDS: i64 = 60;\nconst ALLOWED_SIG_ALGS: &amp;[&amp;str] = &amp;[\n    \"http://www.w3.org/2001/04/xmldsig-more#rsa-sha256\",\n    \"http://www.w3.org/2001/04/xmldsig-more#ecdsa-sha256\",\n    \"http://www.w3.org/2001/04/xmldsig-more#ecdsa-sha384\",\n];\nconst ALLOWED_TRANSFORMS: &amp;[&amp;str] = &amp;[\n    \"http://www.w3.org/2001/10/xml-exc-c14n#\",\n    \"http://www.w3.org/2000/09/xmldsig#enveloped-signature\",\n];\n\n#[derive(Debug, thiserror::Error)]\npub enum SamlVerifyError {\n    #[error(\"response_signature_invalid\")]\n    ResponseSignatureInvalid,\n    #[error(\"assertion_signature_required\")]\n    AssertionSignatureRequired,\n    #[error(\"assertion_signature_invalid\")]\n    AssertionSignatureInvalid,\n    #[error(\"weak_signature_algorithm: {0}\")]\n    WeakSignatureAlgorithm(String),\n    #[error(\"unsupported_transform: {0}\")]\n    UnsupportedTransform(String),\n    #[error(\"audience_mismatch\")]\n    AudienceMismatch,\n    #[error(\"recipient_mismatch\")]\n    RecipientMismatch,\n    #[error(\"not_before_violated\")]\n    NotBeforeViolated,\n    #[error(\"not_on_or_after_violated\")]\n    NotOnOrAfterViolated,\n    #[error(\"nameid_format_unsupported: {0}\")]\n    NameidFormatUnsupported(String),\n    #[error(\"in_response_to_invalid\")]\n    InResponseToInvalid,\n    #[error(\"unsolicited_response_rejected\")]\n    UnsolicitedResponseRejected,\n}\n\npub fn verify_response(\n    response_xml: &amp;str,\n    idp_cert_pem: &amp;str,\n    sp_entity_id: &amp;str,\n    sp_acs_url: &amp;str,\n    expected_in_response_to: Option&lt;&amp;str&gt;,\n) -&gt; Result&lt;VerifiedAssertion, SamlVerifyError&gt; {\n    // Parse + signature verify via samael\n    let sp = ServiceProvider::default()\n        .with_idp_certificate_pem(idp_cert_pem)\n        .with_entity_id(sp_entity_id);\n    let parsed: Response = samael::traits::ToXml::from_xml(response_xml)\n        .map_err(|_| SamlVerifyError::ResponseSignatureInvalid)?;\n\n    // (1) Unsolicited check (DEC-520)\n    let in_response_to = parsed.in_response_to.as_deref().ok_or(SamlVerifyError::UnsolicitedResponseRejected)?;\n    let expected = expected_in_response_to.ok_or(SamlVerifyError::UnsolicitedResponseRejected)?;\n    if in_response_to != expected {\n        return Err(SamlVerifyError::InResponseToInvalid);\n    }\n\n    // (2) Signature algorithm whitelist (DEC-532)\n    let sig_alg = parsed.signature.as_ref()\n        .map(|s| s.signed_info.signature_method.algorithm.as_str())\n        .unwrap_or(\"\");\n    if !ALLOWED_SIG_ALGS.iter().any(|a| *a == sig_alg) {\n        return Err(SamlVerifyError::WeakSignatureAlgorithm(sig_alg.to_string()));\n    }\n\n    // (3) Transforms whitelist (DEC-531 — XSW defense)\n    for tr in parsed.signature.as_ref().map(|s| s.signed_info.references.iter()\n        .flat_map(|r| r.transforms.iter()\n            .flat_map(|t| t.transform.iter()\n                .map(|tr| tr.algorithm.as_str())))) {\n        for alg in tr {\n            if !ALLOWED_TRANSFORMS.iter().any(|a| *a == alg) {\n                return Err(SamlVerifyError::UnsupportedTransform(alg.to_string()));\n            }\n        }\n    }\n\n    // (4) Assertion signature required (DEC-522)\n    let assertion = parsed.assertions.into_iter().next()\n        .ok_or(SamlVerifyError::AssertionSignatureInvalid)?;\n    if assertion.signature.is_none() {\n        return Err(SamlVerifyError::AssertionSignatureRequired);\n    }\n\n    // (5) Audience check (DEC-536)\n    let audiences: Vec&lt;String&gt; = assertion.conditions.as_ref()\n        .into_iter()\n        .flat_map(|c| c.audience_restrictions.iter())\n        .flat_map(|ar| ar.audiences.iter().map(|a| a.audience.clone()))\n        .collect();\n    if !audiences.contains(&amp;sp_entity_id.to_string()) {\n        return Err(SamlVerifyError::AudienceMismatch);\n    }\n\n    // (6) Recipient check (per DEC-526)\n    let recipient_match = assertion.subject.as_ref()\n        .and_then(|s| s.subject_confirmations.iter().next())\n        .and_then(|sc| sc.subject_confirmation_data.as_ref())\n        .map(|d| d.recipient.as_deref() == Some(sp_acs_url))\n        .unwrap_or(false);\n    if !recipient_match {\n        return Err(SamlVerifyError::RecipientMismatch);\n    }\n\n    // (7) NotBefore + NotOnOrAfter (60s skew)\n    let now = Utc::now();\n    if let Some(conds) = assertion.conditions.as_ref() {\n        if let Some(nb) = conds.not_before {\n            if now + Duration::seconds(CLOCK_SKEW_SECONDS) &lt; nb {\n                return Err(SamlVerifyError::NotBeforeViolated);\n            }\n        }\n        if let Some(noa) = conds.not_on_or_after {\n            if now - Duration::seconds(CLOCK_SKEW_SECONDS) &gt;= noa {\n                return Err(SamlVerifyError::NotOnOrAfterViolated);\n            }\n        }\n    }\n\n    // (8) NameIDFormat closed set\n    let (nameid, nameid_format) = assertion.subject.as_ref()\n        .and_then(|s| s.name_id.as_ref())\n        .map(|n| (n.value.clone(), n.format.clone().unwrap_or_default()))\n        .ok_or(SamlVerifyError::NameidFormatUnsupported(\"missing\".into()))?;\n    const ALLOWED_FORMATS: &amp;[&amp;str] = &amp;[\n        \"urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress\",\n        \"urn:oasis:names:tc:SAML:2.0:nameid-format:persistent\",\n    ];\n    if !ALLOWED_FORMATS.iter().any(|f| *f == nameid_format) {\n        return Err(SamlVerifyError::NameidFormatUnsupported(nameid_format));\n    }\n\n    Ok(VerifiedAssertion {\n        nameid, nameid_format,\n        attributes: extract_attributes(&amp;assertion),\n    })\n}\n\npub struct VerifiedAssertion {\n    pub nameid: String,\n    pub nameid_format: String,\n    pub attributes: std::collections::HashMap&lt;String, Vec&lt;String&gt;&gt;,\n}\n\nfn extract_attributes(assertion: &amp;samael::schema::Assertion) -&gt; std::collections::HashMap&lt;String, Vec&lt;String&gt;&gt; {\n    let mut map = std::collections::HashMap::new();\n    for stmt in &amp;assertion.attribute_statements {\n        for attr in &amp;stmt.attributes {\n            let name = attr.name.clone();\n            let values: Vec&lt;String&gt; = attr.values.iter().filter_map(|v| v.value.clone()).collect();\n            map.insert(name, values);\n        }\n    }\n    map\n}</code></pre>\n<h3>3.7 — ACS handler</h3>\n<pre><code class=\"language-rust\">// services/auth/src/handlers/saml.rs (excerpt)\nuse axum::{Form, extract::State, http::StatusCode, response::Redirect};\nuse crate::saml::{response_verifier, attribute_mapper, jit_provision, audit};\nuse uuid::Uuid;\n\n#[derive(Deserialize)]\npub struct AcsForm {\n    #[serde(rename = \"SAMLResponse\")]\n    pub saml_response_b64: String,\n    #[serde(rename = \"RelayState\")]\n    pub relay_state: Option&lt;String&gt;,\n}\n\npub async fn acs_callback(\n    State(state): State&lt;AppState&gt;,\n    Form(form): Form&lt;AcsForm&gt;,\n) -&gt; Result&lt;Redirect, ApiError&gt; {\n    // (1) Base64-decode SAMLResponse\n    let response_xml = String::from_utf8(\n        base64::decode(&amp;form.saml_response_b64).map_err(|_| ApiError::MalformedSamlResponse)?\n    ).map_err(|_| ApiError::MalformedSamlResponse)?;\n\n    // (2) Parse InResponseTo to find IdP + AuthnRequest record\n    let in_response_to = quick_parse_in_response_to(&amp;response_xml)?;\n    let authn_log = state.repo.find_authn_request(in_response_to).await?\n        .ok_or(ApiError::SamlUnsolicited)?;\n    if authn_log.consumed || authn_log.expires_at &lt; chrono::Utc::now() {\n        audit::emit_assertion_replay_attempted(authn_log.tenant_id, in_response_to).await;\n        return Err(ApiError::SamlReplayOrExpired);\n    }\n\n    // (3) Load IdP config\n    let idp = state.repo.load_saml_idp_config(authn_log.idp_id).await?;\n\n    // (4) Verify\n    let assertion = match response_verifier::verify_response(\n        &amp;response_xml, &amp;idp.x509_cert_pem, &amp;idp.sp_entity_id, &amp;idp.acs_url,\n        Some(&amp;in_response_to.to_string()),\n    ) {\n        Ok(a) =&gt; a,\n        Err(e) =&gt; {\n            audit::emit_login_failed(authn_log.tenant_id, authn_log.idp_id, &amp;format!(\"{e:?}\")).await;\n            if matches!(e, response_verifier::SamlVerifyError::AssertionSignatureRequired\n                       | response_verifier::SamlVerifyError::ResponseSignatureInvalid\n                       | response_verifier::SamlVerifyError::AssertionSignatureInvalid\n                       | response_verifier::SamlVerifyError::WeakSignatureAlgorithm(_)\n                       | response_verifier::SamlVerifyError::UnsupportedTransform(_)) {\n                audit::emit_signature_invalid(authn_log.tenant_id, authn_log.idp_id, &amp;format!(\"{e:?}\")).await;\n            }\n            return Err(ApiError::from(e));\n        }\n    };\n\n    // (5) JIT provisioning + role mapping\n    let role = attribute_mapper::resolve_role(&amp;idp.attribute_mapping_yaml, &amp;assertion.attributes)?;\n    let (subject_id, was_jit) = jit_provision::provision_or_link(\n        &amp;state, authn_log.tenant_id, authn_log.idp_id, &amp;assertion, role,\n    ).await?;\n\n    // (6) Mark AuthnRequest consumed\n    state.repo.mark_authn_request_consumed(authn_log.id).await?;\n\n    // (7) Issue AUTH JWT\n    let token = state.jwt_issuer.issue_for_subject(subject_id, authn_log.tenant_id).await?;\n\n    // (8) Audit + redirect\n    audit::emit_login_succeeded(authn_log.tenant_id, authn_log.idp_id, subject_id, was_jit).await;\n    let redirect_url = form.relay_state.unwrap_or_else(|| state.config.default_post_login_url.clone());\n    Ok(Redirect::to(&amp;format!(\"{redirect_url}?access_token={token}\")))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>SP-initiated flow only</strong> — GET /initiate generates AuthnRequest; POST /acs without prior request → 401 unsolicited_response_rejected.</li><li><strong>AuthnRequest signed</strong> — outbound request XML carries signature.</li><li><strong>WantAssertionsSigned required</strong> — response with unsigned assertion → 401 assertion_signature_required.</li><li><strong>Response signature verified</strong> — invalid sig → 401.</li><li><strong>SHA-1 algorithm rejected</strong> → 401 weak_signature_algorithm.</li><li><strong>RSA-SHA256 accepted</strong> — happy path.</li><li><strong>ECDSA-SHA256 accepted</strong> — alternate alg.</li><li><strong>Unsupported transform rejected</strong> (XSW defense) — xpath transform → 401 unsupported_transform.</li><li><strong>InResponseTo replay rejected</strong> — second use of same ID → 401 + sev-2 audit.</li><li><strong>InResponseTo expired (&gt; 10min)</strong> → 401.</li><li><strong>Unknown InResponseTo</strong> → 401 unsolicited_response.</li><li><strong>Audience mismatch</strong> → 401.</li><li><strong>Recipient mismatch</strong> → 401.</li><li><strong>NotBefore in future &gt; 60s</strong> → 401.</li><li><strong>NotOnOrAfter past &gt; 60s</strong> → 401.</li><li><strong>Clock skew tolerated within 60s</strong> — happy path.</li><li><strong>NameIDFormat emailAddress accepted</strong> — happy path.</li><li><strong>NameIDFormat persistent accepted</strong> — happy path.</li><li><strong>NameIDFormat transient rejected</strong> → 401.</li><li><strong>JIT provisioning on first login</strong> — new subject created; <code>auth.saml_jit_provisioned</code> row.</li><li><strong>Subject reused on repeat login</strong> — link row hit.</li><li><strong>Attribute → role mapping applies</strong> — Group/groups attribute matches → granted role.</li><li><strong>Unknown role in attribute_mapping_yaml rejected</strong> at config save.</li><li><strong>Max 2 IdP configs per tenant</strong> — 3rd → 409.</li><li><strong>SP signing key KMS-encrypted</strong> — DB row carries BYTEA blob.</li><li><strong>append-only login_history</strong> — UPDATE/DELETE blocked.</li><li><strong>GET sp-metadata returns SP metadata XML</strong> — tenant-admin only.</li><li><strong>OTel span emitted</strong> — <code>auth.saml.acs</code> with outcome.</li><li><strong>Counter <code>auth_saml_login_total{outcome=succeeded}</code> increments</strong>.</li><li><strong>Counter <code>auth_saml_signature_failures_total</code> sev-2 at &gt; 5/h</strong>.</li><li><strong>Counter <code>auth_saml_replay_attempts_total</code> sev-2 at &gt; 3/h</strong>.</li><li><strong>ACS handler p95 &lt; 500ms</strong> — perf test.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/saml_xsw_attack_defense_test.rs\n#[test]\nfn xpath_transform_rejected() {\n    let xml_with_xpath_transform = r#\"&lt;samlp:Response xmlns:samlp=\"urn:oasis:names:tc:SAML:2.0:protocol\"&gt;\n        &lt;Signature xmlns=\"http://www.w3.org/2000/09/xmldsig#\"&gt;\n            &lt;SignedInfo&gt;&lt;Reference URI=\"\"&gt;\n                &lt;Transforms&gt;&lt;Transform Algorithm=\"http://www.w3.org/TR/1999/REC-xpath-19991116\"/&gt;&lt;/Transforms&gt;\n                ...\n            &lt;/Reference&gt;&lt;/SignedInfo&gt;\n        &lt;/Signature&gt;\n    &lt;/samlp:Response&gt;\"#;\n    let result = cyberos_auth::saml::response_verifier::verify_response(\n        xml_with_xpath_transform, mock_cert(), \"sp_entity\", \"https://acs\", Some(\"known_id\"),\n    );\n    assert!(matches!(result, Err(cyberos_auth::saml::response_verifier::SamlVerifyError::UnsupportedTransform(_))));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/admin_subject_create_test.rs\n#[test]\nfn sha1_signature_method_rejected() {\n    let xml_with_sha1 = mock_response_with_sig_method(\"http://www.w3.org/2000/09/xmldsig#rsa-sha1\");\n    let result = cyberos_auth::saml::response_verifier::verify_response(\n        &amp;xml_with_sha1, mock_cert(), \"sp_entity\", \"https://acs\", Some(\"known_id\"),\n    );\n    assert!(matches!(result, Err(cyberos_auth::saml::response_verifier::SamlVerifyError::WeakSignatureAlgorithm(_))));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/saml_assertion_signature_required_test.rs\n#[test]\nfn response_only_signed_rejected() {\n    let xml = mock_response_with_unsigned_assertion();\n    let result = cyberos_auth::saml::response_verifier::verify_response(\n        &amp;xml, mock_cert(), \"sp_entity\", \"https://acs\", Some(\"id\"),\n    );\n    assert!(matches!(result, Err(cyberos_auth::saml::response_verifier::SamlVerifyError::AssertionSignatureRequired)));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/admin_revoke_test.rs\n#[tokio::test]\nasync fn reused_in_response_to_rejected(ctx: TestCtx) {\n    let req_id = ctx.initiate_flow().await;\n    let response = ctx.build_idp_response_for(req_id).await;\n    ctx.acs_callback(&amp;response).await.unwrap();   // first use OK\n    let err = ctx.acs_callback(&amp;response).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"SamlReplayOrExpired\"));\n    let rows = ctx.memory_audit_rows(\"auth.saml_assertion_replay_attempted\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"severity\"], \"sev-2\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/saml_nameid_format_closed_test.rs\n#[test]\nfn transient_nameid_rejected() {\n    let xml = mock_response_with_nameid_format(\"urn:oasis:names:tc:SAML:2.0:nameid-format:transient\");\n    let result = cyberos_auth::saml::response_verifier::verify_response(\n        &amp;xml, mock_cert(), \"sp_entity\", \"https://acs\", Some(\"id\"),\n    );\n    assert!(matches!(result, Err(cyberos_auth::saml::response_verifier::SamlVerifyError::NameidFormatUnsupported(_))));\n}\n\n#[test]\nfn email_address_format_accepted() {\n    let xml = mock_response_with_nameid_format(\"urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress\");\n    let result = cyberos_auth::saml::response_verifier::verify_response(\n        &amp;xml, mock_cert(), \"sp_entity\", \"https://acs\", Some(\"id\"),\n    );\n    assert!(result.is_ok());\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; samael crate handles XML signature plumbing; 7 memory row builders follow canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>FR-AUTH-004</strong> — JWT issuer (post-SAML callback issues our token).</li></ul>\n<p><strong>Downstream (1 placeholder):</strong></p>\n<ul><li><strong>FR-PORTAL-003</strong> — external IdP SAML 2.0 + OIDC support; consumes both FR-AUTH-103 + FR-AUTH-104.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>FR-AUTH-002</strong> — subject create (JIT helper).</li><li><strong>FR-AUTH-101</strong> — RBAC; attribute_mapping_yaml validates against closed Role enum.</li><li><strong>FR-AI-003</strong> — memory audit bridge.</li><li><strong>FR-MEMORY-111</strong> — PII scrubbing.</li><li><strong>FR-OBS-007</strong> — sev-2 alarm on signature failures + replay attempts.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/auth/saml/idp-configs</h3>\n<pre><code class=\"language-json\">{\n  \"name\": \"Acme ADFS\",\n  \"entity_id_idp\": \"https://adfs.acme.example/adfs/services/trust\",\n  \"sso_url\": \"https://adfs.acme.example/adfs/ls/\",\n  \"metadata_url\": \"https://adfs.acme.example/FederationMetadata/2007-06/FederationMetadata.xml\",\n  \"x509_cert_pem\": \"-----BEGIN CERTIFICATE-----\\nMIID...==\\n-----END CERTIFICATE-----\",\n  \"attribute_mapping_yaml\": \"default_role: tenant-member\\nclaim_rules:\\n  - attribute: \\\"http://schemas.xmlsoap.org/claims/Group\\\"\\n    contains: \\\"Domain Engineers\\\"\\n    grant_role: tenant-admin\\n  - attribute: \\\"http://schemas.xmlsoap.org/claims/Group\\\"\\n    contains: \\\"CFO Office\\\"\\n    grant_role: cfo\\n\"\n}</code></pre>\n<h3>8.2 — auth.saml_login_succeeded memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.saml_login_succeeded\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"idp_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"nameid_hash16\": \"abc123def4567890\",\n  \"nameid_format\": \"urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress\",\n  \"granted_role\": \"chief-financial-officer\",\n  \"was_jit\": false,\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.3 — auth.saml_signature_invalid memory row (sev-2)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.saml_signature_invalid\",\n  \"severity\": \"sev-2\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"idp_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"reason\": \"WeakSignatureAlgorithm(\\\"http://www.w3.org/2000/09/xmldsig#rsa-sha1\\\")\",\n  \"source_ip_hash16\": \"fed0987654321abc\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — auth.saml_assertion_replay_attempted memory row (sev-2)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.saml_assertion_replay_attempted\",\n  \"severity\": \"sev-2\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"in_response_to\": \"_abc123-def456-...\",\n  \"source_ip_hash16\": \"fed0987654321abc\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>IdP-initiated unsolicited responses</strong> — FR-AUTH-2xx with per-tenant opt-in.</li><li><strong>Single Logout (SLO)</strong> — FR-AUTH-2xx.</li><li><strong>Assertion encryption (WantAssertionsEncrypted)</strong> — FR-AUTH-2xx for high-sec tenants.</li><li><strong>HTTP Artifact binding</strong> — out of scope; POST + Redirect cover modern enterprise.</li><li><strong>Dynamic SP metadata cert rotation</strong> — slice 2.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Unsolicited response (no InResponseTo)</td><td>check</td><td>401 unsolicited_response</td><td>Designed</td></tr><tr><td>InResponseTo replay</td><td>consumed/expired check</td><td>401 + sev-2 audit</td><td>Re-initiate</td></tr><tr><td>InResponseTo expired (&gt;10min)</td><td>TTL</td><td>401</td><td>Re-initiate</td></tr><tr><td>Response signature invalid</td><td>samael verify</td><td>401 + sev-2 audit</td><td>Investigate</td></tr><tr><td>Assertion signature missing</td><td>verifier</td><td>401 + sev-2</td><td>Configure IdP to sign assertions</td></tr><tr><td>SHA-1 algorithm</td><td>algorithm check</td><td>401 weak_signature_algorithm</td><td>Upgrade IdP signing config</td></tr><tr><td>xpath transform in signature</td><td>transform check</td><td>401 unsupported_transform</td><td>XSW attack defense</td></tr><tr><td>Audience mismatch</td><td>conditions check</td><td>401</td><td>Fix IdP config</td></tr><tr><td>Recipient mismatch</td><td>subject confirmation check</td><td>401</td><td>Fix IdP config</td></tr><tr><td>NotBefore in future</td><td>timestamp check</td><td>401</td><td>Clock sync</td></tr><tr><td>NotOnOrAfter past</td><td>timestamp check</td><td>401</td><td>Re-initiate</td></tr><tr><td>Clock skew &gt; 60s</td><td>window check</td><td>401</td><td>Sync clocks</td></tr><tr><td>Transient nameid</td><td>format check</td><td>401</td><td>Configure IdP for persistent/emailAddress</td></tr><tr><td>Unknown nameid format</td><td>format check</td><td>401</td><td>Configure</td></tr><tr><td>Unknown role in attribute_mapping_yaml</td><td>parse_config</td><td>400 at config save</td><td>Fix YAML</td></tr><tr><td>3rd IdP config</td><td>handler check</td><td>409</td><td>ADR + cap raise</td></tr><tr><td>KMS decrypt fail (SP signing key)</td><td>aws-sdk error</td><td>500 + sev-1</td><td>Rotate key</td></tr><tr><td>Metadata fetch fail</td><td>reqwest error</td><td>500 + sev-3</td><td>IdP health check</td></tr><tr><td>Cross-tenant subject conflict</td><td>UNIQUE (idp_id, nameid)</td><td>409</td><td>Designed</td></tr><tr><td>JIT subject create fail</td><td>FR-AUTH-002 error</td><td>500 + audit</td><td>Investigate</td></tr><tr><td>append-only log UPDATE from app</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>RLS bypass</td><td>USING</td><td>0 rows</td><td>Designed</td></tr><tr><td>OTel span attribute missing</td><td>otel_test</td><td>CI fails</td><td>Fix</td></tr><tr><td>memory audit emit fails</td><td>tx rollback</td><td>500</td><td>memory_writer health</td></tr><tr><td>Malformed XML</td><td>parse error</td><td>400</td><td>Designed</td></tr><tr><td>Cert rotation: old + new cache overlap</td><td>24h window</td><td>Designed</td><td>None</td></tr><tr><td>Signature verifies but XSW substituted element</td><td>samael's hardened verifier</td><td>None (defense in depth via Audience + Recipient + transform restrictions)</td><td>None</td></tr><tr><td>&gt; 5/h signature failures sustained</td><td>OBS rule</td><td>sev-2</td><td>Investigate</td></tr><tr><td>&gt; 3/h replay attempts</td><td>OBS rule</td><td>sev-2</td><td>Investigate</td></tr><tr><td>Disabled IdP login attempt</td><td>is_active check</td><td>401</td><td>Re-enable</td></tr><tr><td>client_secret leaked in API</td><td>handler omits</td><td>None</td><td>Designed</td></tr><tr><td>Concurrent ACS callbacks for same InResponseTo</td><td>row lock + consumed</td><td>One wins</td><td>Designed</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>SP-initiated only</strong> — IdP-initiated requires per-tenant opt-in (FR-AUTH-2xx).</li><li><strong>HTTP POST + Redirect bindings</strong> — modern enterprise standard.</li><li><strong>WantAssertionsSigned + AuthnRequestsSigned both required</strong> — XSW defense.</li><li><strong>exc-c14n + enveloped-signature only</strong> — XSW transform whitelist.</li><li><strong>RSA-SHA256 minimum</strong> — SHA-1 deprecated; ECDSA accepted.</li><li><strong>InResponseTo replay defense</strong> — 10-min TTL; consumed flag.</li><li><strong>±60s clock skew</strong> — NTP-drift tolerance.</li><li><strong>Closed NameIDFormat (email/persistent)</strong> — stable subject identity.</li><li><strong>Per-tenant attribute_mapping_yaml</strong> — same shape as FR-AUTH-104.</li><li><strong>Max 2 IdP configs per tenant</strong> — typical enterprise count.</li><li><strong>samael crate</strong> — battle-tested SAML library; hand-rolled forbidden.</li><li><strong>Per-tenant SP entity_id + ACS URL</strong> — IdP-side admin per-tenant.</li><li><strong>SP signing key KMS-encrypted</strong> — secret-at-rest hardening.</li><li><strong>24h metadata cache + rotation overlap</strong> — matches FR-AUTH-104 JWKS pattern.</li><li><strong>PII scrub nameid + attributes</strong> — chain holds hashed.</li><li><strong>7 memory audit kinds</strong> — selective queries.</li><li><strong>sev-2 alarms on signature failure + replay</strong> — attack signal.</li><li><strong>SP metadata download endpoint</strong> — IdP operator setup.</li><li><strong>RequestedAuthnContext PasswordProtectedTransport</strong> — baseline assurance.</li><li><strong>Append-only via SQL grant</strong> — forensic.</li><li><strong>SLO deferred</strong> — token expiry suffices at slice 1.</li><li><strong>Defense in depth</strong> — signature + Audience + Recipient + InResponseTo + transform whitelist all required.</li></ul>\n<hr>\n<p><em>End of FR-AUTH-103.</em></p>";
