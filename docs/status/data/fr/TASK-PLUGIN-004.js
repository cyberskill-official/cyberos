window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLUGIN-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PLUGIN module <strong>MUST</strong> ship 12 skill playbooks at <code>modules/plugin/skills/&lt;name&gt;/SKILL.md</code> following the Anthropic Agent Skills spec. Each playbook is a markdown file with YAML frontmatter teaching hosts WHEN and HOW to chain the MCP tools exposed in TASK-PLUGIN-002. Playbooks do NOT add new MCP tools — they are documentation that the host's skill router uses to inject just-in-time discipline into the model's prompt.</p>\n<ol><li><strong>MUST</strong> ship exactly 12 playbooks in v1 per DEC-2430 + DEC-2435, grouped by use-case:</li></ol>\n<ul><li><strong>Orchestration (3)</strong>: <code>run-cuo-workflow</code>, <code>route-natural-language</code>, <code>cross-workflow-chain</code></li><li><strong>Memory (3)</strong>: <code>audit-trail-query</code>, <code>audit-trail-append</code>, <code>memory-write-discipline</code></li><li><strong>Discovery (3)</strong>: <code>discover-skills</code>, <code>persona-discovery</code>, <code>workflow-inspection</code></li><li><strong>Governance (3)</strong>: <code>invoke-cyberos-skill</code>, <code>auth-and-scopes</code>, <code>audit-emission-discipline</code></li></ul>\n<ol><li><strong>MUST</strong> conform to SKILL_BUNDLE_RUBRIC SKB-020..023 per DEC-2432 + TASK-SKILL-111/113:</li></ol>\n<ul><li>SKB-020: description 60-480 chars</li><li>SKB-021: description contains ≥4 quoted trigger examples</li><li>SKB-022: description has no XML/HTML tags</li><li>SKB-023: description verb stems are recognised (router fingerprint)</li></ul>\n<ol><li><strong>MUST</strong> carry <code>acceptance/TRIGGER_TESTS.md</code> per DEC-2433 + TASK-SKILL-112 with at least:</li></ol>\n<ul><li>4 positive fixtures (user-input strings that SHOULD trigger this playbook)</li><li>4 negative fixtures (strings that should NOT trigger it, especially adjacent-skill confusables)</li></ul>\n<ol><li><strong>MUST</strong> reference only tools registered in TASK-PLUGIN-002 — validator test <code>test_playbooks_reference_valid_tools.py</code> checks every tool name mentioned in the playbook body against the 8-tool registry.</li></ol>\n<ol><li><strong>MUST</strong> explain in body sections: WHEN to use the playbook (one paragraph), WHICH tools it chains (bulleted), WHAT scopes are required (list), WHAT side effects occur (list), and a worked example (code block showing 2-4 sample tool calls).</li></ol>\n<ol><li><strong>MUST</strong> declare the playbook frontmatter shape per Anthropic Agent Skills spec: ``<code>yaml --- name: &lt;skill-id&gt;                       # kebab-case, matches folder name description: &gt; One paragraph (60-480 chars) describing what this skill is for AND 4 quoted trigger examples e.g. \"use this when the user asks 'run a workflow', 'execute a CUO chain', 'kick off the architect-new-system workflow', or 'start a persona workflow'\" license: Apache-2.0 --- </code>``</li></ol>\n<ol><li><strong>MUST</strong> pass TASK-SKILL-114 baseline check — once SKILL.md hits v1.0 maturity, an <code>acceptance/BASELINE.md</code> MUST be created freezing the trigger-test pass rate so regressions are detectable.</li></ol>\n<ol><li><strong>MUST</strong> be host-portable per DEC-2434 — same SKILL.md works in Claude Code, Cowork, Codex CLI without modification. Cursor doesn't render Skills (its MCP integration surfaces tools only); the Cursor adapter (TASK-PLUGIN-007) omits skills/.</li></ol>\n<ol><li><strong>MUST</strong> be subject to lazy-load discipline — host routers load only the SKILL.md description, not the body, at fingerprinting time. Body content is fetched only when the skill is triggered. Authors MUST treat the description as the load-bearing copy.</li></ol>\n<ol><li><strong>MUST NOT</strong> add new playbooks in v1.x.y without a successor task (task-PLUGIN-004a, etc.) per DEC-2435.</li></ol>\n<ol><li><strong>MUST NOT</strong> rename a playbook within v1.x.y — rename = breaking change requiring major bump (mirrors TASK-PLUGIN-003 clause 11).</li></ol>\n<ol><li><strong>MUST NOT</strong> declare destructive operations in a playbook body without flagging in the description's trigger examples — users routed to the playbook MUST see the side effects upfront.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why playbooks AND commands AND tools (clauses 1+)?</strong> Three layers of host-facing surface:</p>\n<ul><li><strong>Tools</strong> (TASK-PLUGIN-002) — <em>what</em> the plugin can do; called by the host's tool-calling subsystem.</li><li><strong>Commands</strong> (TASK-PLUGIN-003) — <em>how the user invokes</em> it explicitly; a UI affordance.</li><li><strong>Playbooks</strong> (this task) — <em>when and why</em> the host model should pick a tool; injected into the model's prompt when the description matches the user's input.</li></ul>\n<p>Without playbooks, the host model sees tool names + brief descriptions but has no discipline to follow. Playbooks add the \"use this tool ONLY when X\" prose that prevents misuse.</p>\n<p><strong>Why exactly 12 playbooks (DEC-2430)?</strong> Same logic as TASK-PLUGIN-003 — small, learnable, well-considered. 12 covers the major use-cases with room to teach edge-cases (memory-write-discipline, audit-emission-discipline). More playbooks dilute the router's discrimination.</p>\n<p><strong>Why conform to SKB-020..023 (DEC-2432)?</strong> Plugin playbooks SHIP THROUGH the same Anthropic Agent Skills surface as everything else in <code>modules/skill/</code>. The SKB rubric is how all skills get discovered correctly. Playbooks must clear the same bar.</p>\n<p><strong>Why TRIGGER_TESTS (DEC-2433)?</strong> TASK-SKILL-112 introduced TRIGGER_TESTS.md as the way to measure routing accuracy. Without it, a description that triggers spuriously (false positive) or fails to trigger (false negative) goes unnoticed. With TRIGGER_TESTS, CI catches the regression.</p>\n<p><strong>Why reference only registered tools (clause 4)?</strong> Hallucinated tool names in playbook bodies waste the model's call attempts and produce confusing error envelopes. Validator test catches the divergence at PR time.</p>\n<p><strong>Why required body sections (clause 5)?</strong> Same lazy-load discipline as TASK-PLUGIN-003 commands — the body teaches the model when triggered. Without explicit \"scopes required\" and \"side effects\" sections, the model uses the playbook without knowing the consequences.</p>\n<p><strong>Why mandatory BASELINE.md at v1.0 (clause 7)?</strong> TASK-SKILL-114 establishes this as the regression-prevention mechanism. Playbook descriptions drift as authors tune triggers; without a baseline pass-rate snapshot, the team can't tell if a tweak improved routing or broke it.</p>\n<p><strong>Why omit skills from Cursor adapter (clause 8)?</strong> Cursor's MCP integration surfaces tools to the model directly. Skills (Anthropic Agent Skills) are an Anthropic-spec concept not implemented by Cursor as of 2026. Shipping them adds bloat without benefit. The canonical skills still ship in the canonical bundle for hosts that do support.</p>\n<p><strong>Why lazy-load discipline (clause 9)?</strong> Anthropic Skills router loads descriptions into a fingerprint index at install; bodies are fetched on match. If author treats body as load-bearing for routing, the playbook never triggers. Description is the only path to triggering.</p>\n<p><strong>Why no playbook renames in v1.x.y (clause 11)?</strong> Same reason as command renames: user scripts and host caches reference by slug; renames break references silently.</p>\n<p><strong>Why flag destructive ops in description (clause 12)?</strong> Users routed by description never see the body before the model decides to act. If \"delete some memory rows\" is in the body but not the description, the model may invoke without the user's expected warning. Surfacing in description triggers gives the user a glance-able heads-up.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Playbook folder layout (per playbook)</h3>\n<pre><code>modules/plugin/skills/&lt;name&gt;/\n├── SKILL.md                       (frontmatter + body)\n└── acceptance/\n    ├── TRIGGER_TESTS.md           (4 positive + 4 negative fixtures)\n    └── BASELINE.md                (frozen pass rate at v1.0 promotion — added by TASK-SKILL-114)</code></pre>\n<h3>Sample playbook — <code>run-cuo-workflow/SKILL.md</code></h3>\n<pre><code class=\"language-markdown\">---\nname: run-cuo-workflow\ndescription: &gt;\n  Use this skill when the user wants to execute a CyberOS persona-aware workflow chain end-to-end.\n  Triggers on user requests like \"run a workflow\", \"execute the architect-new-system workflow\",\n  \"kick off CUO for the CTO\", or \"run the ADR quick-capture flow\". Routes the model to call\n  cyberos.cuo.execute_workflow with the right persona+workflow slugs and surfaces task status\n  back to the user as the long-running execution proceeds.\nlicense: Apache-2.0\n---\n\n## When to use\n\nUser wants to execute a CyberOS workflow they (or you) have identified. The workflow is a multi-step\nchain belonging to one of the 47 active personas. Workflows emit memory audit rows at every step.\n\nIf the user only knows the goal (\"architect a new payment system\") and not the workflow name,\nuse `route-natural-language` FIRST to translate the natural-language ask into a persona+workflow\npair, then come here.\n\n## Tools chained\n\n- `cyberos.cuo.execute_workflow` — primary call; returns task_id, status becomes \"running\"\n- `tasks/get` — poll for status (called by host's task subsystem, not directly by you)\n\n## Scopes required\n\n- `cyberos:cuo:execute`\n- `cyberos:memory:write` (for audit emission)\n\n## Side effects\n\n- Spawns one Task per invocation (long-running, 30 seconds – several minutes typical)\n- Emits 1× `plugin.invoked` audit row at task start\n- Emits N× `cuo.step_completed` audit rows during execution\n- Emits 1× `cuo.workflow_completed` audit row at finish\n\n## Worked example\n\n```text\nUser: Run the ADR quick-capture workflow for the CTO with title \"Adopt PostgreSQL 16\"\nYou: { tool: cyberos.cuo.execute_workflow,\n       args: { persona: \"chief-technology-officer\",\n               workflow: \"adr-quick-capture\",\n               inputs: { title: \"Adopt PostgreSQL 16\" } } }\nHost: { task_id: \"t-abc123\", status: \"running\" }\n[host polls via tasks/get; you tell user \"started, polling...\"]\nHost: { status: \"completed\", output: { adr_number: \"ADR-2402\", artifact_uri: \"...\" } }\nYou: \"ADR-2402 published: Adopt PostgreSQL 16. Took 4.2s.\"</code></pre>\n<pre><code>\n### Sample `acceptance/TRIGGER_TESTS.md`\n\n```markdown\n# Trigger tests — run-cuo-workflow\n\n## Positive (should trigger)\n\n- \"Run a CUO workflow\"\n- \"Execute the architect-new-system workflow for the CTO\"\n- \"Kick off ADR quick-capture for adopting PostgreSQL 16\"\n- \"Start a persona workflow\"\n\n## Negative (should NOT trigger)\n\n- \"Find the right workflow for me\" → routes to route-natural-language\n- \"Show me which workflows exist\" → routes to workflow-inspection\n- \"What did the workflow do last time?\" → routes to audit-trail-query\n- \"Cancel the running workflow\" → not a separate skill; this is a tasks/cancel call from host</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Exactly 12 playbook folders exist</strong> — <code>ls -d modules/plugin/skills/*/ | wc -l</code> → 12.</li><li><strong>Every folder has SKILL.md and acceptance/TRIGGER_TESTS.md</strong> — test asserts presence.</li><li><strong>Every SKILL.md has frontmatter with name + description + license</strong> — test loads YAML.</li><li><strong>Every description is 60-480 chars (SKB-020)</strong> — test asserts length.</li><li><strong>Every description has ≥4 quoted trigger examples (SKB-021)</strong> — test counts quotes.</li><li><strong>No description contains XML/HTML tags (SKB-022)</strong> — regex <code>&lt;[a-zA-Z]+</code> MUST NOT match.</li><li><strong>Every description's verb stems are recognised (SKB-023)</strong> — test runs against VERB_STEMS allowlist from TASK-SKILL-115 tooling.</li><li><strong>Every TRIGGER_TESTS.md has 4+ positive + 4+ negative fixtures</strong> — test parses lists.</li><li><strong>Every tool name referenced in body matches TASK-PLUGIN-002 registry</strong> — test greps <code>cyberos.&lt;...&gt;</code> patterns and validates.</li><li><strong>Body has \"When to use\" section</strong> — test grep.</li><li><strong>Body has \"Tools chained\" section</strong> — test grep.</li><li><strong>Body has \"Scopes required\" section</strong> — test grep.</li><li><strong>Body has \"Side effects\" section</strong> — test grep.</li><li><strong>Body has \"Worked example\" section with a code block</strong> — test grep + code-fence check.</li><li><strong>3 orchestration playbooks present</strong> — explicit check of folder names.</li><li><strong>3 memory playbooks present</strong> — explicit check.</li><li><strong>3 discovery playbooks present</strong> — explicit check.</li><li><strong>3 governance playbooks present</strong> — explicit check.</li><li><strong>Destructive playbook (audit-trail-append) flags side effect in description</strong> — test parses description, asserts \"write\" or \"append\" phrase.</li><li><strong>Manifest skills[] array references all 12</strong> — manifest validator check.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/plugin/tests/test_playbooks_conform_to_skb.py\nimport re, yaml\nfrom pathlib import Path\n\nSKILLS = Path(__file__).parent.parent / \"skills\"\n\ndef test_exactly_twelve_playbooks():\n    assert sum(1 for _ in SKILLS.iterdir() if _.is_dir()) == 12\n\ndef test_description_skb_020_023():\n    for skill_dir in SKILLS.iterdir():\n        fm = load_frontmatter(skill_dir / \"SKILL.md\")\n        desc = fm[\"description\"]\n        assert 60 &lt;= len(desc) &lt;= 480, f\"{skill_dir.name}: SKB-020 length\"\n        quote_count = desc.count('\"')\n        assert quote_count &gt;= 8, f\"{skill_dir.name}: SKB-021 needs ≥4 quoted examples (saw {quote_count // 2})\"\n        assert not re.search(r\"&lt;[a-zA-Z]+\", desc), f\"{skill_dir.name}: SKB-022 XML tag\"</code></pre>\n<pre><code class=\"language-python\"># modules/plugin/tests/test_playbooks_have_trigger_tests.py\ndef test_trigger_tests_present_and_balanced():\n    for skill_dir in SKILLS.iterdir():\n        tt = skill_dir / \"acceptance\" / \"TRIGGER_TESTS.md\"\n        assert tt.exists(), f\"{skill_dir.name}: missing TRIGGER_TESTS.md\"\n        text = tt.read_text()\n        pos_section = text.split(\"## Positive\")[1].split(\"## Negative\")[0]\n        neg_section = text.split(\"## Negative\")[1]\n        pos_count = sum(1 for line in pos_section.splitlines() if line.startswith(\"- \"))\n        neg_count = sum(1 for line in neg_section.splitlines() if line.startswith(\"- \"))\n        assert pos_count &gt;= 4 and neg_count &gt;= 4, \\\n            f\"{skill_dir.name}: need ≥4 positive + ≥4 negative (got {pos_count}/{neg_count})\"</code></pre>\n<pre><code class=\"language-python\"># modules/plugin/tests/test_playbooks_reference_valid_tools.py\nREGISTERED = {\n    \"cyberos.cuo.list_personas\", \"cyberos.cuo.list_workflows\",\n    \"cyberos.cuo.route\", \"cyberos.cuo.execute_workflow\",\n    \"cyberos.memory.read_audit\", \"cyberos.memory.append_audit\",\n    \"cyberos.skill.list_catalog\", \"cyberos.skill.invoke_skill\",\n}\n\ndef test_all_referenced_tools_exist():\n    pattern = re.compile(r\"\\bcyberos\\.[a-z][a-z0-9]*\\.[a-z][a-z0-9_]*\\b\")\n    for skill_dir in SKILLS.iterdir():\n        body = (skill_dir / \"SKILL.md\").read_text()\n        for match in pattern.findall(body):\n            assert match in REGISTERED, \\\n                f\"{skill_dir.name}: tool '{match}' not in TASK-PLUGIN-002 registry\"</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>Each of 12 playbooks follows the pattern in §3 sample. Author writes:</p>\n<ul><li>SKILL.md with frontmatter + 5 body sections (When to use / Tools chained / Scopes required / Side effects / Worked example)</li><li>acceptance/TRIGGER_TESTS.md with 4 positive + 4 negative fixtures</li><li>(later, at v1.0 promotion per TASK-SKILL-114) acceptance/BASELINE.md</li></ul>\n<p>Three validator tests enforce conformance.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> TASK-PLUGIN-002 (provides the 8 tools referenced); TASK-SKILL-111 (description enrichment discipline applied here); TASK-SKILL-113 (XML-free frontmatter — SKB-022).</li><li><strong>Downstream:</strong> TASK-PLUGIN-007 (multi-runtime adapters copy <code>skills/</code> into per-target bundle for hosts that support Skills).</li><li><strong>Cross-module:</strong> TASK-SKILL-112 (TRIGGER_TESTS.md format), TASK-SKILL-114 (BASELINE.md once v1.0), TASK-SKILL-115 (placeholder sweep tooling reused for verb-stem check).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<p>(See §3 for full sample playbook and TRIGGER_TESTS.md.)</p>\n<p>Manifest skills[] array entry:</p>\n<pre><code class=\"language-json\">{\n  \"id\": \"run-cuo-workflow\",\n  \"path\": \"skills/run-cuo-workflow/SKILL.md\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<ul><li>~~Should playbooks be merged into commands (single concept)?~~ → No. Commands are user-invoked UI; playbooks are host-router-injected discipline. Different consumers, different lifecycles. Keep separate.</li><li>~~Should we ship 6 playbooks now and grow to 12 in v1.1?~~ → No, ship 12 at once per DEC-2430. Half-coverage means routing misses obvious use-cases and adoption stalls.</li><li>~~Should BASELINE.md ship in v1 or wait for v1.0 promotion?~~ → Wait for v1.0 per clause 7 + TASK-SKILL-114 — baselines on pre-stable skills get bumped repeatedly and the discipline erodes.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Description too short</td><td>SKB-020 length check</td><td>test fails</td><td>Author expands ≥60</td></tr><tr><td>Description too long</td><td>SKB-020 length check</td><td>test fails</td><td>Author trims ≤480</td></tr><tr><td>Description missing trigger quotes</td><td>SKB-021 quote count</td><td>test fails</td><td>Author adds ≥4 quoted examples</td></tr><tr><td>Description has XML tag</td><td>SKB-022 regex</td><td>test fails</td><td>Author rewrites without tags</td></tr><tr><td>Verb stems unrecognised</td><td>SKB-023 allowlist</td><td>test fails</td><td>Author uses canonical verbs or extends VERB_STEMS via TASK-SKILL-115</td></tr><tr><td>Missing TRIGGER_TESTS.md</td><td>filesystem check</td><td>test fails</td><td>Author writes the file</td></tr><tr><td>TRIGGER_TESTS imbalanced (&lt;4 pos/neg)</td><td>line count</td><td>test fails</td><td>Author adds fixtures</td></tr><tr><td>Tool name typo in body</td><td>regex + registry check</td><td>test fails</td><td>Author corrects to SEP-986 name</td></tr><tr><td>Body missing required section</td><td>grep check</td><td>test fails</td><td>Author adds section</td></tr><tr><td>Playbook count != 12</td><td>folder count</td><td>test fails</td><td>Author adds/removes per task-PLUGIN-004a (additions need task)</td></tr><tr><td>Two playbooks with same name</td><td>filesystem</td><td>install fails</td><td>inherent</td></tr><tr><td>Playbook rename within v1</td><td>git diff</td><td>manual review</td><td>Revert or bump major</td></tr><tr><td>Worked example references missing tool</td><td>regex + registry</td><td>test fails</td><td>Author fixes</td></tr><tr><td>Description has unbalanced quotes</td><td>YAML parse fails</td><td>YAML loader raises</td><td>Author closes quotes</td></tr><tr><td>Side-effect missing from description for destructive playbook</td><td>manual review</td><td>reviewer flags</td><td>Author adds phrase like \"appends to memory\"</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 <strong>Why 12 not 10 or 14.</strong> 12 = 3 use-cases × 4 playbooks each, evenly balanced. 10 leaves discovery undercovered; 14 dilutes router discrimination. Sweet spot from TASK-SKILL-111 calibration data.</li></ul>\n<ul><li>§11.2 <strong>VERB_STEMS reuse.</strong> TASK-SKILL-115 shipped a verb-stem allowlist (<code>tools/sweep-placeholders/verb_stems.py</code>). The test imports it directly. Adding new verbs requires the TASK-SKILL-115 extension procedure.</li></ul>\n<ul><li>§11.3 <strong>Why description carries trigger quotes inline.</strong> Anthropic Skills router fingerprints on the description alone — body is loaded later. Quoted phrases inside the description directly contribute to router accuracy. Conventional \"see TRIGGER_TESTS.md for examples\" approach fails the router.</li></ul>\n<ul><li>§11.4 <strong>Cursor + Codex CLI compatibility.</strong> Cursor doesn't render Skills; the TASK-PLUGIN-007 Cursor adapter strips the skills/ directory from the bundle. Codex CLI reads SKILL.md as-is. Both behaviours are tested in TASK-PLUGIN-007.</li></ul>\n<ul><li>§11.5 <strong>Playbook-vs-command crossover.</strong> /cyberos-run command and run-cuo-workflow playbook overlap. By design: command is the explicit UI affordance; playbook is the model-side injection. Both reference the same underlying tool. Authors keep them consistent (description phrasing, scope list) but don't merge.</li></ul>\n<ul><li>§11.6 <strong>Triggering one playbook may suggest another.</strong> \"Worked example\" sections often suggest a follow-up playbook (\"use route-natural-language first\"). This is intentional — cross-playbook chaining is good prompt engineering.</li></ul>\n<ul><li>§11.7 <strong>Manifest skills[] is the source of distribution.</strong> Even though the playbooks live in <code>modules/plugin/skills/</code> on disk, the manifest's <code>skills[]</code> array is what the packer reads. Adding a playbook without updating the manifest = playbook ships but isn't advertised.</li></ul>\n<ul><li>§11.8 <strong>Avoid playbook author drift.</strong> Annual review: re-check every playbook's description against shipped TASK-PLUGIN-002 tool list. Add a CI annotation when a new tool lands without a playbook covering it.</li></ul>\n<hr>\n<p><em>End of TASK-PLUGIN-004 spec.</em></p>";
