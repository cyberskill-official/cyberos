window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EVAL-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The evaluation-views panel <strong>MUST</strong> surface FR-EVAL-003 assessments to a strictly-bounded audience, always show the auto-score as a clearly-marked draft a human must approve, and give the employee the right to see and respond to their own record, with every cross-person read audited. The contract:</p>\n<ol><li><strong>MUST</strong> be one panel in the same static single-page app as FR-APP-001, under <code>apps/console/</code>, mounted inside the FR-APP-001 shell and behind its auth gate, using CDS tokens and components (DEC-2624). It <strong>MUST NOT</strong> define its own shell, sign-in flow, or design language; it reuses what FR-APP-001 established (the FR-APP-005 pattern).</li></ol>\n<ol><li><strong>MUST</strong> be access-restricted to exactly three reader relationships (DEC-2620), enforced server-side in <code>services/eval/src/views/access.rs</code>, deny-by-default:</li></ol>\n<ul><li>the <strong>founder</strong> (the tenant owner role);</li><li>a <strong>manager</strong> viewing only an employee who is their own report, established through AUTH's manager-of relationship (FR-AUTH-003);</li><li>the <strong>employee themselves</strong>, viewing only their own record (the self-view). Any other caller — including a manager requesting a non-report, or any peer — <strong>MUST</strong> receive <code>403 eval_view_forbidden</code> and <strong>MUST NOT</strong> see any assessment content, not even existence/metadata.</li></ul>\n<ol><li><strong>MUST</strong> treat the access decision as the FR-EVAL-001 governance grant plus the AUTH relationship, not a rule this panel invents (DEC-2624). The check <strong>MUST</strong> consult the FR-EVAL-001 access grants (who may view evaluations at all) and then the manager-of relationship (which specific reports a manager may view). A caller lacking the FR-EVAL-001 grant is denied even for their own self-view path until governance has provisioned it.</li></ol>\n<ol><li><strong>MUST</strong> present a manager view (<code>apps/console/src/screens/eval_manager.ts</code> over <code>services/eval/src/views/manager.rs</code>) for an authorised manager/founder that shows, for one report: the assessment's rubric mapping (each FR-EVAL-002 rubric item, with its <code>source_doc</code>/<code>clause_ref</code>), the evidence FR-EVAL-003 linked per item, the <strong>auto-score draft</strong>, and the approve/override control. It <strong>MUST NOT</strong> show a report's assessment to a manager who is not that report's manager-of.</li></ol>\n<ol><li><strong>MUST</strong> render the FR-EVAL-003 auto-score as a clearly-marked DRAFT and <strong>MUST NEVER</strong> let it read as final (DEC-2621):</li></ol>\n<ul><li>the assessment carries an explicit <code>state</code> of <code>draft | approved | overridden</code> (sourced from FR-EVAL-003 / the response flow), and the panel <strong>MUST</strong> show a prominent DRAFT badge whenever <code>state='draft'</code>.</li><li>a <code>draft</code> assessment <strong>MUST</strong> be visually and textually distinguished from an <code>approved</code>/<code>overridden</code> one (badge, label, and a \"not yet reviewed by a human\" note); the auto-score <strong>MUST NOT</strong> be displayed as a decided result, a final grade, or anything that reads as an employment decision.</li><li>the panel <strong>MUST NOT</strong> offer any \"final\" or \"decided\" affordance for a <code>draft</code> assessment other than the human approve/override control.</li></ul>\n<ol><li><strong>MUST</strong> require a human reviewer's approval before an assessment is final (DEC-2621, EU AI Act Article 14):</li></ol>\n<ul><li>the approve/override control (<code>services/eval/src/views/response.rs</code>) lets the authorised manager/founder either <strong>approve</strong> the draft (state → <code>approved</code>, recording <code>reviewer_subject_id</code>) or <strong>override</strong> it (state → <code>overridden</code>, recording the reviewer's adjusted outcome + a required <code>override_reason</code>).</li><li>the transition out of <code>draft</code> <strong>MUST</strong> record a human <code>reviewer_subject_id</code>; a transition with a null or service-account reviewer <strong>MUST</strong> be rejected (<code>403 review_requires_human</code>).</li><li>the model's auto-score is never the final word: <code>approved</code> means a human endorsed it; <code>overridden</code> means a human replaced it; neither happens without a human acting.</li></ul>\n<ol><li><strong>MUST</strong> provide an employee self-view (<code>apps/console/src/screens/eval_self.ts</code> over <code>services/eval/src/views/employee.rs</code>) that shows the signed-in employee their own assessment — the same rubric mapping, evidence, score, and state — and <strong>MUST NOT</strong> expose any other person's record, any manager's private notes not meant for the employee, or any other employee's data through it.</li></ol>\n<ol><li><strong>MUST</strong> capture the employee's right to respond (DEC-2622):</li></ol>\n<ul><li>the self-view <strong>MUST</strong> let the employee record a free-text response to their own assessment (<code>POST .../response</code>), stored against the assessment with the author and timestamp.</li><li>the response <strong>MUST</strong> be shown alongside the assessment in both the self-view and the manager view (the manager sees the employee's response).</li><li>an assessment <strong>MUST NOT</strong> be marked <code>closed</code> until the employee has had the opportunity to respond — either the employee has recorded a response, or an explicit, audited \"response window elapsed / waived\" event has occurred. A close attempt before that <strong>MUST</strong> be rejected (<code>409 response_opportunity_pending</code>).</li></ul>\n<ol><li><strong>MUST</strong> audit every cross-person read (DEC-2623): when the founder or a manager opens an employee's assessment, the panel's backing endpoint <strong>MUST</strong> emit a hash-chained <code>eval.assessment_read</code> memory audit row naming <code>{reader_subject_id, subject_employee_id, assessment_id, relationship, trace_id}</code>. Self-views <strong>MUST</strong> also emit an <code>eval.assessment_read</code> row (reader == subject). Reads are first-class audited events, never silent.</li></ol>\n<ol><li><strong>MUST</strong> emit a hash-chained audit row for every state-affecting action through <code>services/eval/src/audit.rs</code>: <code>eval.assessment_approved</code>, <code>eval.assessment_overridden</code> (carrying <code>override_reason</code>), and <code>eval.assessment_response_recorded</code>. Each chains into the same <code>l1_audit_log</code> the rest of CyberOS uses (the FR-PROJ-008 / FR-MEMORY-123 pattern).</li></ol>\n<ol><li><strong>MUST</strong> be tenant-scoped through the session: every eval read/write <strong>MUST</strong> carry the FR-AUTH-004 session token the FR-APP-001 shell holds, and results <strong>MUST</strong> be the tenant scope the eval service's RLS applies to that token. The panel <strong>MUST NOT</strong> attempt to widen scope or pass a tenant other than the session's.</li></ol>\n<ol><li><strong>MUST</strong> add no backend beyond the eval service's own read/approve/respond endpoints (DEC-2624). The console is a front-end over those endpoints; a screen that appears to need a new endpoint is a signal to extend the eval service's FR, not to add a backend in <code>app</code>. The access check, the audit emission, and the state machine live in the eval service, not in the browser.</li></ol>\n<ol><li><strong>MUST</strong> fail visibly and safely on error: on a 401 the panel <strong>MUST</strong> defer to the FR-APP-001 auth gate and return to sign-in; on a 403 it <strong>MUST</strong> show an explicit access-denied state (not an empty panel that reads as \"no assessments\"); on any other non-2xx the affected screen <strong>MUST</strong> show a clear error and <strong>MUST NOT</strong> fabricate an assessment, a score, or a state. An empty-but-successful read (the employee has no assessment yet) <strong>MUST</strong> be shown as \"no assessment yet\", distinct from denied and from error.</li></ol>\n<ol><li><strong>MUST NOT</strong> let the browser compute or alter the score, the state, or the access decision. The panel renders the eval service's verdict — the score, the <code>draft</code>/<code>approved</code>/<code>overridden</code> state, and whether the caller is allowed — exactly as the service returns them; it never recomputes a score client-side, never up-grades a <code>draft</code> to look <code>approved</code>, and never makes its own allow/deny call.</li></ol>\n<ol><li><strong>MUST</strong> keep the API-client and render functions pure where possible and unit-tested without a live backend, against fixtures for the assessment-card shape, the DRAFT-vs-APPROVED badge, and the access-denied state. A live render against running eval + auth is an owner-run check, not a unit-test dependency.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics: <code>eval_assessment_reads_total{relationship}</code> (counter; relationship ∈ founder | manager | self), <code>eval_assessment_reviews_total{action}</code> (counter; action ∈ approved | overridden), and <code>eval_assessment_responses_total</code> (counter). The read counter by relationship makes \"who is reading evaluations\" observable, supporting the governance posture.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why server-side, deny-by-default access (DEC-2620, §1 #2 #3)?</strong> An evaluation record is the most sensitive data about a person on the platform. The access rule cannot live in the browser, where it is advisory and bypassable; it is enforced in the eval service, and the default is deny. Exactly three relationships can read an assessment — the founder, the report's own manager, and the employee themselves — and everyone else gets a 403 with no content, not even a hint that the record exists.</p>\n<p><strong>Why the access decision is FR-EVAL-001 grant + AUTH relationship, not a new rule (DEC-2624, §1 #3)?</strong> The governance layer already decides who may view evaluations and discloses that basis in the signed documents; AUTH already knows the manager-of relationship. Inventing a second access rule here would create two sources of truth that could drift, and would let the panel grant access the governance layer never sanctioned. The panel composes the two existing authorities; it does not add a third.</p>\n<p><strong>Why the auto-score is always a marked DRAFT (DEC-2621, §1 #5)?</strong> A number a model produced, shown plainly, reads as a verdict — and a verdict that affects pay or progression must come from a human, not a model (the brain-evaluation plan's core rule; EU AI Act Article 14). The persistent DRAFT badge and the \"not yet reviewed\" note make it impossible to mistake the auto-score for a decision. The draft is an input to a human's judgement, displayed as such.</p>\n<p><strong>Why a human must approve or override before final (DEC-2621, §1 #6)?</strong> Showing a draft is not enough; the system must make the human's act the thing that finalises an assessment. <code>approved</code> records that a human endorsed the draft; <code>overridden</code> records that a human replaced it with a reason. Either way a named human acted, and a service account cannot stand in for them. The model assists the decision; it never makes it.</p>\n<p><strong>Why the employee response and the \"not closed until they can respond\" rule (DEC-2622, §1 #8)?</strong> Transparency here is a feature, not an afterthought. An evaluation the subject cannot see or contest is surveillance; an evaluation they can read and respond to, on the record, is a fair process. Blocking <code>closed</code> until the employee has had the opportunity to respond turns \"right to respond\" from a slogan into an enforced state transition.</p>\n<p><strong>Why audit every read, including self-views (DEC-2623, §1 #9)?</strong> Who looks at whose evaluation is itself sensitive. Auditing every cross-person read — naming the reader, the subject, and the relationship — means a manager browsing a report's record leaves a trace, and the employee (and the founder) can later see who accessed their record. Auditing self-views too keeps the ledger complete and makes \"the employee read their own assessment\" a provable part of the response process.</p>\n<p><strong>Why no new backend, panel-in-the-console (DEC-2624, §1 #1 #12)?</strong> The founder's decision is one unified console with a panel per module (the FR-APP-001 / FR-APP-005 pattern). A separate evaluation app would duplicate the shell, the auth gate, and the CDS wiring, and split a sensitive surface across two front-ends. The access check, audit, and state machine belong in the eval service, where they are enforceable; the panel renders.</p>\n<p><strong>Why the browser must not compute the score, state, or access (§1 #14)?</strong> Each of these is security- or fairness-load-bearing. A client-side score could diverge from the service's; a client-side \"approved\" badge could lie about a draft; a client-side allow/deny could be bypassed by editing the page. The panel is a faithful renderer of the service's verdict on all three, never a second authority.</p>\n<p><strong>Why a 403 must look different from an empty result (§1 #13)?</strong> \"You are not allowed to see this\" and \"there is no assessment yet\" are completely different facts, and conflating them either leaks (an empty panel implying no record when access was actually denied) or confuses (a denied panel implying the person has no evaluation). The panel distinguishes denied, empty-but-allowed, and error explicitly.</p>\n<p><strong>Why the read-by-relationship metric (§1 #16)?</strong> The governance posture promises bounded access; making \"who reads evaluations, in what relationship\" observable lets the founder verify that promise is being kept and notice anomalies (e.g. a spike in cross-person reads) without reading the audit log row by row.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Access check (server-side, deny-by-default)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/views/access.rs\n\n#[derive(Clone, Copy, Debug, Serialize, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\npub enum Relationship { Founder, Manager, SelfView }\n\n/// The ONLY gate to an assessment (§1 #2 #3). Composes the FR-EVAL-001 grant with\n/// AUTH's manager-of relationship. Deny-by-default: anything not explicitly allowed\n/// returns Err(Forbidden) and the caller sees no content.\npub async fn authorize_read(\n    pool: &amp;sqlx::PgPool,\n    reader: &amp;Subject,                  // from the FR-AUTH-004 session\n    subject_employee_id: uuid::Uuid,   // whose assessment is being read\n) -&gt; Result&lt;Relationship, ViewError&gt; {\n    // 1. governance gate: may this caller view evaluations at all? (FR-EVAL-001)\n    if !eval_governance::has_view_grant(pool, reader).await? {\n        return Err(ViewError::Forbidden);            // 403 eval_view_forbidden\n    }\n    // 2. self-view\n    if reader.subject_id == subject_employee_id {\n        return Ok(Relationship::SelfView);\n    }\n    // 3. founder (tenant owner)\n    if reader.is_founder {\n        return Ok(Relationship::Founder);\n    }\n    // 4. manager-of: only this reader's own reports (FR-AUTH-003)\n    if auth::is_manager_of(pool, reader.subject_id, subject_employee_id).await? {\n        return Ok(Relationship::Manager);\n    }\n    Err(ViewError::Forbidden)                          // everyone else: denied, no content\n}</code></pre>\n<h3>Manager read (audits the cross-person read)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/views/manager.rs\n\npub async fn get_report_assessment(\n    pool: &amp;sqlx::PgPool,\n    reader: &amp;Subject,\n    subject_employee_id: uuid::Uuid,\n) -&gt; Result&lt;AssessmentView, ViewError&gt; {\n    let rel = access::authorize_read(pool, reader, subject_employee_id).await?;  // 403 if not allowed\n\n    let view = load_assessment_view(pool, subject_employee_id).await?;  // rubric items + evidence + score + state\n\n    // §1 #9: every cross-person read (and self-view) is a first-class audited event\n    audit::emit(pool, \"eval.assessment_read\", json!({\n        \"reader_subject_id\":   reader.subject_id,\n        \"subject_employee_id\": subject_employee_id,\n        \"assessment_id\":       view.assessment_id,\n        \"relationship\":        rel,\n        \"trace_id\":            current_trace_id(),\n    })).await?;\n    metrics::counter!(\"eval_assessment_reads_total\", \"relationship\" =&gt; rel.as_str()).increment(1);\n\n    Ok(view)   // carries state = draft | approved | overridden; the client renders the DRAFT badge on draft\n}</code></pre>\n<h3>Human approve / override + employee response (the HITL state machine)</h3>\n<pre><code class=\"language-rust\">// services/eval/src/views/response.rs\n\n/// Human review (§1 #6, EU AI Act Art. 14). A human reviewer endorses (approve)\n/// or replaces (override, with a reason) the draft. A service account cannot review.\npub async fn review_assessment(\n    tx: &amp;mut sqlx::PgTransaction&lt;'_&gt;,\n    reviewer: &amp;Subject,\n    assessment_id: uuid::Uuid,\n    decision: ReviewDecision,          // Approve | Override { outcome, reason }\n) -&gt; Result&lt;(), ViewError&gt; {\n    // reviewer must be the founder or the subject's manager-of, and must be human\n    let subj = subject_of(tx, assessment_id).await?;\n    let rel = access::authorize_read(&amp;tx_pool(tx), reviewer, subj).await?;\n    if rel == Relationship::SelfView { return Err(ViewError::Forbidden); }   // you cannot approve your own\n    if is_service_account(reviewer.subject_id).await? {\n        return Err(ViewError::ReviewRequiresHuman);     // 403 review_requires_human\n    }\n    match decision {\n        ReviewDecision::Approve =&gt; {\n            set_state(tx, assessment_id, \"approved\", reviewer.subject_id).await?;\n            audit::emit_tx(tx, \"eval.assessment_approved\", json!({\n                \"assessment_id\": assessment_id, \"reviewer_subject_id\": reviewer.subject_id })).await?;\n        }\n        ReviewDecision::Override { outcome, reason } =&gt; {\n            require_nonempty(&amp;reason)?;                  // override demands a reason\n            set_overridden(tx, assessment_id, outcome, &amp;reason, reviewer.subject_id).await?;\n            audit::emit_tx(tx, \"eval.assessment_overridden\", json!({\n                \"assessment_id\": assessment_id, \"reviewer_subject_id\": reviewer.subject_id,\n                \"override_reason\": reason })).await?;\n        }\n    }\n    Ok(())\n}\n\n/// Employee response (§1 #8). The subject — and only the subject — records a response\n/// to their own assessment. An assessment is not closeable until this opportunity is met.\npub async fn record_response(\n    tx: &amp;mut sqlx::PgTransaction&lt;'_&gt;,\n    author: &amp;Subject,\n    assessment_id: uuid::Uuid,\n    body: &amp;str,\n) -&gt; Result&lt;(), ViewError&gt; {\n    let subj = subject_of(tx, assessment_id).await?;\n    if author.subject_id != subj { return Err(ViewError::Forbidden); }   // only your own\n    insert_response(tx, assessment_id, author.subject_id, body).await?;\n    audit::emit_tx(tx, \"eval.assessment_response_recorded\", json!({\n        \"assessment_id\": assessment_id, \"author_subject_id\": author.subject_id })).await?;\n    Ok(())\n}\n\n/// §1 #8: close is blocked until the employee has had the opportunity to respond.\npub async fn close_assessment(\n    tx: &amp;mut sqlx::PgTransaction&lt;'_&gt;, closer: &amp;Subject, assessment_id: uuid::Uuid,\n) -&gt; Result&lt;(), ViewError&gt; {\n    if !response_opportunity_met(tx, assessment_id).await? {\n        return Err(ViewError::ResponseOpportunityPending);   // 409 response_opportunity_pending\n    }\n    set_state(tx, assessment_id, \"closed\", closer.subject_id).await?;\n    Ok(())\n}</code></pre>\n<h3>Console panel (renders the verdict; never computes it)</h3>\n<pre><code class=\"language-typescript\">// apps/console/src/render/assessment_card.ts\n// Pure render: the DRAFT badge is driven by the service's state, never by the client.\nexport function renderAssessmentCard(a: AssessmentView): HTMLElement {\n  const card = el(\"div\", \"cds-card eval-assessment\");\n  if (a.state === \"draft\") {\n    card.append(badge(\"DRAFT — auto-score, not yet reviewed by a human\", \"cds-badge-warning\"));\n  } else if (a.state === \"approved\") {\n    card.append(badge(\"APPROVED by \" + a.reviewer_display, \"cds-badge-success\"));\n  } else if (a.state === \"overridden\") {\n    card.append(badge(\"OVERRIDDEN by \" + a.reviewer_display, \"cds-badge-info\"));\n  }\n  card.append(rubricMapping(a.items));        // each item: source_doc / clause_ref + evidence + per-item result\n  card.append(scoreBlock(a.score, a.state));  // score is shown; on draft it is labelled provisional\n  if (a.response) card.append(responseBlock(a.response));   // the employee's response, shown to both sides\n  return card;\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Panel mounts in the FR-APP-001 shell</strong> — the eval panel appears in the console nav only when the session holds the FR-EVAL-001 view grant; it reuses the shell, auth gate, and CDS (AC for §1 #1 #3).</li><li><strong>Founder can read any report</strong> — founder requests employee X's assessment → 200 with the assessment view (AC for §1 #2).</li><li><strong>Manager can read only their own report</strong> — manager M (manager-of X) reads X → 200; manager M reads Y (not their report) → <code>403 eval_view_forbidden</code>, no content (AC for §1 #2 #4).</li><li><strong>Peer is denied</strong> — a peer with no relationship reads X → 403, no existence leak (AC for §1 #2).</li><li><strong>No grant → denied even for self</strong> — a caller without the FR-EVAL-001 grant reads their own record → 403 until governance provisions the grant (AC for §1 #3).</li><li><strong>DRAFT badge on auto-score</strong> — a <code>state='draft'</code> assessment renders the DRAFT badge and the \"not yet reviewed\" note; it does not render an approved/final label (AC for §1 #5).</li><li><strong>Draft is not offered as final</strong> — the only state-changing affordance on a draft is the human approve/override control; no \"mark final/decided\" action exists for a draft (AC for §1 #5).</li><li><strong>Human approval finalises</strong> — manager approves X's draft → state <code>approved</code>, <code>reviewer_subject_id</code> recorded; badge flips to APPROVED (AC for §1 #6).</li><li><strong>Override requires a reason</strong> — override with empty reason → rejected; with a reason → state <code>overridden</code>, reason stored + audited (AC for §1 #6).</li><li><strong>Service account cannot review</strong> — <code>review_assessment</code> with a service-account reviewer → <code>403 review_requires_human</code> (AC for §1 #6).</li><li><strong>Self cannot approve own</strong> — the subject calling <code>review_assessment</code> on their own assessment → 403 (AC for §1 #6).</li><li><strong>Employee self-view shows own record only</strong> — employee X's self-view returns X's assessment; it exposes no other person's data (AC for §1 #7).</li><li><strong>Employee can respond</strong> — X posts a response → stored against the assessment, shown in both the self-view and the manager view (AC for §1 #8).</li><li><strong>Close blocked before response opportunity</strong> — close before X has responded (and no waiver event) → <code>409 response_opportunity_pending</code>; after a response or an audited waiver → close succeeds (AC for §1 #8).</li><li><strong>Cross-person read audited</strong> — founder/manager opening X's assessment emits <code>eval.assessment_read</code> naming reader, subject, relationship; chained into <code>l1_audit_log</code> (AC for §1 #9).</li><li><strong>Self-view audited</strong> — X opening own assessment emits <code>eval.assessment_read</code> with reader == subject (AC for §1 #9).</li><li><strong>Approve/override/response audited</strong> — each emits its matching <code>eval.assessment_*</code> row; override row carries <code>override_reason</code> (AC for §1 #10).</li><li><strong>Tenant isolation</strong> — tenant A's assessments are unreachable from a tenant B session (AC for §1 #11).</li><li><strong>No new backend</strong> — the panel's <code>api/eval.ts</code> calls only eval service routes; review of the client against the eval route list finds no call to a route that does not exist (AC for §1 #12).</li><li><strong>403 distinct from empty distinct from error</strong> — denied shows access-denied; an allowed read of someone with no assessment shows \"no assessment yet\"; a 500 shows an error; none is fabricated (AC for §1 #13).</li><li><strong>Browser does not recompute</strong> — the rendered score and state come verbatim from the service; a client-tampered \"approved\" label cannot exist for a <code>draft</code> because the badge is driven by <code>state</code> (AC for §1 #14).</li><li><strong>Render units pass without backend</strong> — the DRAFT-vs-APPROVED badge and the access-denied state render correctly against fixtures (AC for §1 #15).</li><li><strong>OTel read-by-relationship metric</strong> — a founder read increments <code>eval_assessment_reads_total{relationship=\"founder\"}</code>; a self read increments <code>{relationship=\"self\"}</code> (AC for §1 #16).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn manager_reads_own_report_but_not_others() {\n    let env = EvalTestEnv::new().await;\n    let (m, x, y) = env.manager_with_report_and_a_stranger().await;  // m manages x, not y\n    assert!(env.as_subject(m).get_assessment(x).await.is_ok());\n\n    let denied = env.as_subject(m).get_assessment(y).await;\n    assert_eq!(denied.status_code(), 403);\n    assert_eq!(denied.error_code(), \"eval_view_forbidden\");\n    assert!(denied.body_is_empty_of_content());     // no existence leak\n}\n\n#[tokio::test]\nasync fn no_eval_grant_denies_even_self_view() {\n    let env = EvalTestEnv::new().await;\n    let x = env.employee_without_view_grant().await;\n    let res = env.as_subject(x).get_assessment(x).await;\n    assert_eq!(res.status_code(), 403);             // governance grant is a precondition\n}\n\n#[tokio::test]\nasync fn auto_score_is_marked_draft_until_human_approves() {\n    let env = EvalTestEnv::new().await;\n    let (m, x) = env.manager_with_drafted_assessment().await;  // FR-EVAL-003 produced a draft\n    let v = env.as_subject(m).get_assessment(x).await.unwrap();\n    assert_eq!(v.state, \"draft\");                   // not final\n\n    env.as_subject(m).approve(v.assessment_id).await.unwrap();\n    let v2 = env.as_subject(m).get_assessment(x).await.unwrap();\n    assert_eq!(v2.state, \"approved\");\n    assert_eq!(v2.reviewer_subject_id, Some(m));\n}\n\n#[tokio::test]\nasync fn service_account_cannot_review() {\n    let env = EvalTestEnv::new().await;\n    let (_, x) = env.manager_with_drafted_assessment().await;\n    let svc = env.service_account_subject();\n    let err = env.as_subject(svc).approve(env.assessment_of(x).await).await.unwrap_err();\n    assert_eq!(err.error_code(), \"review_requires_human\");\n}\n\n#[tokio::test]\nasync fn assessment_not_closeable_until_employee_can_respond() {\n    let env = EvalTestEnv::new().await;\n    let (m, x) = env.manager_with_approved_assessment().await;\n    let aid = env.assessment_of(x).await;\n    // no response yet, no waiver -&gt; close blocked\n    let blocked = env.as_subject(m).close(aid).await;\n    assert_eq!(blocked.error_code(), \"response_opportunity_pending\");\n\n    env.as_subject(x).respond(aid, \"I disagree with item 3; here is context.\").await.unwrap();\n    assert!(env.as_subject(m).close(aid).await.is_ok());\n}\n\n#[tokio::test]\nasync fn every_cross_person_read_is_audited() {\n    let env = EvalTestEnv::new().await;\n    let (m, x) = env.manager_with_drafted_assessment().await;\n    env.as_subject(m).get_assessment(x).await.unwrap();\n    let row = env.audit.last_of_kind(\"eval.assessment_read\").await.unwrap();\n    assert_eq!(row[\"payload\"][\"reader_subject_id\"], m.to_string());\n    assert_eq!(row[\"payload\"][\"subject_employee_id\"], x.to_string());\n    assert_eq!(row[\"payload\"][\"relationship\"], \"manager\");\n    assert!(!row[\"chain_anchor\"].as_str().unwrap().is_empty());   // chained\n}</code></pre>\n<pre><code class=\"language-typescript\">// apps/console/tests/eval_draft_badge_render.test.ts\ntest(\"draft assessment renders the DRAFT badge and no approved label\", () =&gt; {\n  const card = renderAssessmentCard(fixtureAssessment({ state: \"draft\" }));\n  expect(card.textContent).toContain(\"DRAFT\");\n  expect(card.textContent).not.toContain(\"APPROVED\");\n});\ntest(\"approved assessment renders APPROVED, not DRAFT\", () =&gt; {\n  const card = renderAssessmentCard(fixtureAssessment({ state: \"approved\", reviewer_display: \"Stephen\" }));\n  expect(card.textContent).toContain(\"APPROVED\");\n  expect(card.textContent).not.toContain(\"DRAFT\");\n});\ntest(\"access-denied renders the denied state, not an empty panel\", () =&gt; {\n  const panel = renderEvalPanel(fixtureDenied());\n  expect(panel.textContent).toContain(\"do not have access\");\n  expect(panel.textContent).not.toContain(\"no assessment\");\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Access check, manager read with read-audit, the approve/override + response state machine, and the pure render above. <code>lib.rs</code> mounts the views routes behind the FR-EVAL-001 guard; <code>main.ts</code> registers the panel in the FR-APP-001 nav only when the view grant is present.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-EVAL-003</strong> — the evaluation engine that produces the assessments (auto-score draft, rubric mapping, linked evidence, the <code>draft</code> state) this panel surfaces and a human approves/overrides. Hard <code>depends_on</code>.</li><li><strong>FR-APP-001</strong> — the CDS console shell, the auth gate, the CDS tokens, and the Caddy static-serving path this panel mounts into (the FR-APP-005 pattern). Hard <code>depends_on</code>.</li><li><strong>FR-EVAL-001</strong> — the governance/consent/access/retention layer; the view grant the access check consults and the disclosed basis that makes these reads lawful and non-covert.</li><li><strong>FR-EVAL-002</strong> — the rubric the assessment maps to; the panel shows each rubric item's <code>source_doc</code>/<code>clause_ref</code> so the manager and employee see what was measured and against which clause.</li><li><strong>FR-AUTH-003 / FR-AUTH-004</strong> — RLS + the manager-of relationship the access check uses, and the session token the panel carries on every call.</li><li><strong>FR-APP-005</strong> — the precedent for \"one more read-only panel in the console, no new backend, CDS, tenant-scoped through the session\".</li><li><strong>FR-MEMORY-123 / FR-PROJ-008</strong> — the hash-chained audit-row pattern the <code>eval.assessment_*</code> rows (including the read audit) follow.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.assessment_read\",\n  \"payload\": {\n    \"reader_subject_id\":   \"manager-...\",\n    \"subject_employee_id\": \"employee-...\",\n    \"assessment_id\":       \"asmt-...\",\n    \"relationship\":        \"manager\",\n    \"trace_id\":            \"0af...\"\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"kind\": \"eval.assessment_overridden\",\n  \"payload\": {\n    \"assessment_id\":      \"asmt-...\",\n    \"reviewer_subject_id\":\"stephen-...\",\n    \"override_reason\":    \"Q2 evidence for KPI-2 was logged late; adjusting up after manual review.\",\n    \"trace_id\":           \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Resolved by Stephen's 2026-06-29 decisions (access-restricted + contract-disclosed; both auto-score AND HITL with the draft clearly marked; employee right to respond; every cross-person read audited; panel in the console, no new backend). Deferred:</p>\n<ul><li>A formal response window (a duration after which \"opportunity to respond\" is auto-satisfied with an audited waiver) versus a purely event-driven close — this FR requires the opportunity but leaves the window-vs-event policy to FR-EVAL-001 governance config; the close gate honours whichever it sets.</li><li>An HR role distinct from the founder (the brain plan mentions HR) — folded into the founder/manager grants for now; a dedicated HR grant is an additive FR-EVAL-001 grant, not a change to this panel's access shape.</li><li>A diff/history view of how an assessment moved draft → approved/overridden over time, and a rubric-version diff — surfacing-only additive screens, later.</li><li>Notifications to the employee that a draft assessment exists — additive; the audit row and the self-view exist now, a push/email nudge is a later FR.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Peer reads someone's assessment</td><td><code>authorize_read</code> deny-by-default</td><td>403 eval_view_forbidden, no content</td><td>None — by design</td></tr><tr><td>Manager reads a non-report</td><td><code>is_manager_of</code> false</td><td>403, no existence leak</td><td>None — by design</td></tr><tr><td>Caller without FR-EVAL-001 grant</td><td><code>has_view_grant</code> false</td><td>403 even for self-view</td><td>Governance provisions the grant</td></tr><tr><td>Auto-score shown as final</td><td>client badge driven by <code>state</code></td><td>DRAFT badge forced on draft</td><td>None — structural</td></tr><tr><td>Service account approves</td><td><code>is_service_account</code> guard</td><td>403 review_requires_human</td><td>A human reviews</td></tr><tr><td>Subject approves own assessment</td><td>self-view relationship rejected</td><td>403</td><td>A manager/founder reviews</td></tr><tr><td>Override with no reason</td><td><code>require_nonempty</code></td><td>rejected</td><td>Reviewer supplies a reason</td></tr><tr><td>Close before employee can respond</td><td><code>response_opportunity_met</code> false</td><td>409 response_opportunity_pending</td><td>Employee responds or an audited waiver elapses</td></tr><tr><td>Cross-person read not audited</td><td>endpoint emits before returning</td><td>read blocked if audit emit fails (tx)</td><td>Retry; read is gated on the audit row</td></tr><tr><td>Self-view leaks another's data</td><td>self-view loads own subject only</td><td>only own record returned</td><td>None — by design</td></tr><tr><td>Browser recomputes the score</td><td>client renders service value only</td><td>no client score path exists</td><td>None — structural</td></tr><tr><td>Client forges an \"approved\" badge</td><td>badge bound to <code>state</code> from service</td><td>a <code>draft</code> cannot render approved</td><td>None — structural</td></tr><tr><td>403 rendered as empty panel</td><td>client distinguishes denied/empty/error</td><td>access-denied state shown</td><td>None — by design</td></tr><tr><td>Empty-but-allowed read</td><td>HTTP 200, no assessment</td><td>\"no assessment yet\" shown</td><td>None</td></tr><tr><td>401 expired session</td><td>FR-APP-001 auth gate</td><td>return to sign-in</td><td>Re-authenticate</td></tr><tr><td>Upstream eval unreachable</td><td>non-2xx</td><td>error state, no fabrication</td><td>Restore eval service</td></tr><tr><td>Cross-tenant read</td><td>RLS + session scope</td><td>0 rows / 403</td><td>None — by design</td></tr><tr><td>New endpoint appears needed</td><td>API-layer review (guardrail)</td><td>extend eval FR, not add backend</td><td>Add to the eval service's FR</td></tr><tr><td>Audit emit fails mid-review</td><td>sqlx tx rollback</td><td>state change not committed</td><td>Caller retries</td></tr><tr><td>OTel exporter down</td><td>buffered then dropped</td><td>logged</td><td>Restore FR-OBS-001</td></tr><tr><td>Manager loses manager-of mid-session</td><td>next read re-checks relationship</td><td>403 on next read</td><td>None — re-checked per call</td></tr><tr><td>Override outcome out of rubric range</td><td>validated against FR-EVAL-002 scale</td><td>422</td><td>Reviewer picks a valid outcome</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The access check (<code>authorize_read</code>) is the single gate and is deny-by-default: it returns one of three relationships or <code>Forbidden</code>, and every read path calls it first. There is no second access path and no client-side allow/deny, so the rule cannot be bypassed by editing the page.</li><li>The access decision composes two existing authorities — the FR-EVAL-001 view grant (may this caller see evaluations at all) and AUTH's manager-of (which specific reports a manager may see) — rather than inventing a third. This keeps a single source of truth for \"who may see what\" and prevents the panel from granting access governance never sanctioned.</li><li>The DRAFT badge is bound to the service-supplied <code>state</code>, never to a client flag. A <code>draft</code> assessment cannot be made to render as <code>approved</code> from the browser, because the only thing that flips the badge is the service returning <code>state='approved'</code> after a human acted. This is the structural enforcement of \"the model never decides\".</li><li>Approve vs override are both human acts that finalise; the difference is endorse vs replace. Override demands a reason and records the reviewer's adjusted outcome, so a human disagreeing with the auto-score leaves a defensible, audited record of why. A service account cannot stand in for the human reviewer.</li><li>The \"not closed until the employee can respond\" rule is enforced as a state-machine precondition (<code>response_opportunity_met</code>), not a UI nicety. Either the employee recorded a response, or an explicit audited waiver/window event occurred; absent both, <code>close</code> returns 409. This turns the right to respond into an enforced gate.</li><li>Every read — cross-person and self — emits <code>eval.assessment_read</code> before the content is returned, and the emit is on the request path (tx-gated for the review/response writes). Auditing self-views too keeps the ledger complete and makes \"the employee saw their assessment\" provable for the response process.</li><li>The panel adds no backend: the access check, the audit, and the state machine live in <code>services/eval/</code>, and the console is a faithful renderer (the FR-APP-005 discipline). A screen that seems to need a new endpoint is the signal to extend FR-EVAL-003, not to put logic in the browser.</li><li>The three render distinctions — denied (403), empty-but-allowed (200, no assessment), and error (other non-2xx) — are kept separate because conflating them either leaks or misleads. The audit row for a denied read is not written by this panel (the deny happens before content load), but the 403 itself is observable via the access metric.</li><li><code>eval_assessment_reads_total{relationship}</code> makes the governance promise auditable in aggregate: the founder can watch the balance of self vs manager vs founder reads and spot an anomaly without walking the audit log.</li><li>This is a high-risk FR under the EU AI Act framing because it surfaces an employment evaluation and is where human oversight is operationalised; see the AI Risk Assessment section. The human approve/override gate (Article 14) is the core of the FR, not an add-on.</li><li>The override-outcome validity is checked against the FR-EVAL-002 rubric version's scale, so a reviewer cannot record an outcome the rubric does not define; this keeps the human's adjustment inside the same standard the draft was scored against.</li><li>HR-as-a-distinct-role is deferred: the brain plan mentions HR, but for the first release the founder/manager grants cover it, and a dedicated HR grant is an additive FR-EVAL-001 grant rather than a change to this panel's three-relationship access shape.</li></ul>\n<hr>\n<h2>AI Risk Assessment</h2>\n<h3>Risk classification</h3>\n<p>High-risk under the EU AI Act framing: the panel surfaces an automated employment-evaluation output that can inform pay and progression, and it is the surface where human oversight of that output is exercised. Human oversight (Article 14) is the core of this FR, not a peripheral control.</p>\n<h3>Data sources</h3>\n<p>The panel reads only FR-EVAL-003 assessments for the people the caller is authorised to see — the founder for any report, a manager for their own reports, and the employee for their own record. It reads through the FR-AUTH-004 session, tenant-scoped by RLS, and it stores only the employee's response and the reviewer's approve/override decision. It computes nothing about a person itself; the score and state come from the eval service.</p>\n<h3>Human oversight</h3>\n<p>The FR-EVAL-003 auto-score is always shown as a clearly-marked DRAFT and can never read as final (clause 5). A human reviewer — the founder or the subject's manager, never a service account and never the subject for their own — must approve or override before an assessment is final (clause 6, EU AI Act Article 14); override demands a written reason. The model assists the decision and never makes it. The employee can see their own record and record a response, and the assessment is not closed until they have had the opportunity to (clause 8).</p>\n<h3>Traceability</h3>\n<p>Every read of an assessment, cross-person and self, emits a hash-chained <code>eval.assessment_read</code> audit row naming who read whose record and in what relationship (clause 9); approve, override (with reason), and response each emit their own chained row (clause 10), into the same tamper-evident <code>l1_audit_log</code> as the rest of CyberOS. Who accessed an evaluation, who finalised it and how, and what the employee said are all reconstructable and provable. A read-by-relationship metric (clause 16) makes the bounded-access promise auditable in aggregate.</p>\n<h3>Failure modes</h3>\n<p>Unauthorised access is denied by default with no content or existence leak (clause 2). A model score reading as a decision is prevented structurally — the DRAFT badge is bound to the service state and a draft has no \"final\" affordance (clause 5). A model or service account finalising an assessment is prevented by the human-reviewer requirement (clause 6). An evaluation the subject cannot contest is prevented by the response right and the close gate (clause 8). The browser cannot recompute or up-grade the score, state, or access (clause 14). In every case the safe state is \"no one sees what they may not, and nothing is final without a named human\".</p>\n<hr>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Claude (Cowork), authoring this FR from the brain-evaluation plan (Phase 5 surfacing), Stephen's 2026-06-29 decisions, and the existing FR-APP-001 / FR-APP-005 console pattern and the FR-EVAL-001..003 chain.</li><li>Scope: full draft of this specification — the normative clauses, the access model, the DRAFT-vs-approved state machine, the employee-response gate, the read-audit requirement, and the paired AI Risk Assessment. No console or service code is written by this FR; the panel and the views endpoints are built in a later session.</li><li>Human review: Stephen reviews and approves before status moves past draft; this is a high-risk FR, so the access restriction, the human-approval gate, and the read-audit need his explicit sign-off, and the paired audit plus the CAF gate validate before any implementation merges.</li></ul>\n<hr>\n<h2>Operating mode (founder decision, 2026-06-30): quiet / in-product-silent</h2>\n<p>In quiet mode the employee self-view is OFF by default (config <code>eval_employee_self_view = off</code>): the product surfaces evaluation data only to the founder and the responsible manager. An employee still has the right to see and respond to their own record, but that is served on request through HR and the access path in FR-EVAL-001, not as a default in-product screen. The auto-score is never shown to the employee by default, and where it is shown to a reviewer it is a clearly-marked DRAFT requiring human approval before it is final. Access stays founder + manager-of-report (+ self on request), deny-by-default, with every cross-person read audited. The lawful basis is the signed clause in <code>docs/legal/data-monitoring-and-evaluation-notice.md</code>.</p>\n<hr>\n<p><em>End of FR-EVAL-004.</em></p>";
