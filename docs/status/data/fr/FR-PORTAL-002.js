window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PORTAL-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship per-tenant brand pack (logo + colour palette + email overrides) and custom CNAME with ACME-issued TLS at <code>services/portal/src/brand/</code> + <code>services/portal/src/cname/</code>, with versioning + rollback, WCAG 2.1 AA contrast enforcement, magic-bytes asset validation, daily auto-renewal, and 8 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define <code>portal_brand_packs</code> (versioned, immutable rows) at migration <code>0005</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, version INT NOT NULL, palette JSONB NOT NULL, email_overrides JSONB NOT NULL DEFAULT '{}'::jsonb, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), created_by_subject_id UUID NOT NULL, asset_set_id BIGINT NOT NULL REFERENCES portal_brand_assets(asset_set_id))</code>. Per-tenant version is monotonic. REVOKE UPDATE, DELETE per feature-request-audit skill rule 12 (rollback = pointer change, not row mutation).</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_brand_pack_active</code> at migration <code>0006</code>: <code>(tenant_id UUID PRIMARY KEY, active_pack_id BIGINT NOT NULL REFERENCES portal_brand_packs(id), activated_at TIMESTAMPTZ NOT NULL DEFAULT now(), activated_by_subject_id UUID NOT NULL)</code>. One-row-per-tenant activation pointer.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_brand_assets</code> at migration <code>0007</code>: <code>(asset_set_id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, kind brand_asset_kind NOT NULL, mime_type TEXT NOT NULL CHECK (mime_type IN ('image/png','image/svg+xml')), content_kms_blob BYTEA NOT NULL, content_sha256 CHAR(64) NOT NULL, kms_key_id TEXT NOT NULL, content_length_bytes INT NOT NULL CHECK (content_length_bytes &lt;= 1048576), created_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. Closed <code>brand_asset_kind</code> enum per DEC-1013.</li></ol>\n<ol><li><strong>MUST</strong> define the closed <code>brand_asset_kind</code> enum at migration <code>0007</code>: <code>('favicon','header_logo','splash_logo','email_logo')</code>. CI cardinality test asserts 4.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_cname_configs</code> at migration <code>0008</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, cname TEXT NOT NULL, dns_verify_token TEXT NOT NULL, dns_verified_at TIMESTAMPTZ, tls_cert_pem_kms_blob BYTEA, tls_cert_chain_pem_kms_blob BYTEA, tls_key_pem_kms_blob BYTEA, tls_kms_key_id TEXT, tls_issued_at TIMESTAMPTZ, tls_expires_at TIMESTAMPTZ, tls_renewal_failures INT NOT NULL DEFAULT 0, status TEXT NOT NULL CHECK (status IN ('pending_dns','dns_verified','active','revoked')) DEFAULT 'pending_dns', last_renewal_attempt_at TIMESTAMPTZ)</code>. Partial unique <code>(tenant_id) WHERE status IN ('pending_dns','dns_verified','active')</code> — one active CNAME per tenant per DEC-1010.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both USING and WITH CHECK on all 4 PORTAL brand tables (per DEC-1014 + feature-request-audit skill rule 13). Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tenant_id}/brand-pack</code> for brand-pack creation. Caller has <code>tenant_admin</code> role. Body: <code>{ palette: {primary, secondary, accent, background, surface, error}, email_overrides?: {welcome?, magic_link?, password_reset?, invoice_receipt?}, assets: [{kind, base64_content}] }</code>. Handler:</li></ol>\n<ul><li>Validates palette: 6 named slots, each <code>#RRGGBB</code> hex.</li><li>Runs WCAG 2.1 AA contrast check (per §1 #11) — reject if non-conformant.</li><li>For each asset: magic-bytes validate (PNG or SVG); size ≤ 1 MiB; generates 3 size variants via image_pipeline.</li><li>Validates email overrides (sandboxed Tera per §1 #14).</li><li>INSERTs new <code>portal_brand_assets</code> row(s) + new <code>portal_brand_packs</code> row at version=max(version)+1.</li><li>DOES NOT activate (separate step per §1 #8).</li><li>Emits <code>portal.brand_pack_created</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tenant_id}/brand-pack/{pack_id}/activate</code>. Caller has <code>tenant_admin</code> role. Handler:</li></ol>\n<ul><li>UPSERT <code>portal_brand_pack_active</code> with the new pack_id.</li><li>Invalidate CDN cache for <code>&lt;tenant_slug&gt;</code> (publish to NATS <code>cyberos.portal.brand_cdn.invalidate.&lt;tenant_slug&gt;</code>).</li><li>Emit <code>portal.brand_pack_activated</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tenant_id}/brand-pack/rollback</code> per DEC-1007. Body: <code>{ target_pack_id }</code>. Handler re-points <code>portal_brand_pack_active.active_pack_id</code> to the named historic pack_id. Emits <code>portal.brand_pack_rolled_back</code>. Idempotent on <code>target_pack_id</code>.</li></ol>\n<ol><li><strong>MUST</strong> enforce per-tenant rate limit of 10 saves per day on <code>POST /brand-pack</code> per DEC-1018. Excess returns <code>429 + Retry-After: 86400</code> + emits <code>portal.brand_pack_validation_rejected</code> with reason='rate_limited'.</li></ol>\n<ol><li><strong>MUST</strong> enforce WCAG 2.1 AA contrast per DEC-1003 + W3C contrast formula:</li></ol>\n<ul><li><code>contrast_ratio(primary, background) ≥ 4.5</code></li><li><code>contrast_ratio(error, background) ≥ 4.5</code></li><li><code>contrast_ratio(primary, surface) ≥ 3.0</code> (text-on-card UX) Non-conformant submission returns <code>400 + { error: \"wcag_contrast_violation\", offending_pair: [\"primary\",\"background\"], actual_ratio: 3.2, required_ratio: 4.5 }</code> + emits <code>portal.brand_pack_validation_rejected</code>.</li></ul>\n<ol><li><strong>MUST</strong> validate uploaded asset content-type via magic-bytes per DEC-1019. PNG header = <code>89 50 4E 47 0D 0A 1A 0A</code> (8 bytes); SVG header = <code>&lt;?xml</code> OR <code>&lt;svg</code>. Mismatched magic-bytes vs claimed mime_type → 400 + <code>invalid_asset_content</code>. Never trust the Content-Type request header.</li></ol>\n<ol><li><strong>MUST</strong> generate 3 size variants from each uploaded logo per DEC-1001 via libvips:</li></ol>\n<ul><li><code>favicon</code>: 32×32 px (from any uploaded logo).</li><li><code>header_logo</code>: 200×60 px (preserve aspect; pad transparent).</li><li><code>splash_logo</code>: 800×240 px (preserve aspect; pad transparent). Variants stored as separate <code>portal_brand_assets</code> rows with <code>kind</code> differentiation; canonical upload stored at upload-supplied size.</li></ul>\n<ol><li><strong>MUST</strong> apply Tera sandboxing for email overrides per DEC-1016. Allowed Tera tags: <code>{{ var }}</code>, <code>{% if %}</code>, <code>{% for %}</code>, <code>{% include %}</code> (restricted to a per-tenant include path). DISALLOWED: <code>{% set %}</code> with file/env reads, <code>{% raw %}</code> with HTML injection, any <code>tera_*</code> registered functions that access I/O. Validation: parse Tera template + walk AST + reject if any disallowed tag detected.</li></ol>\n<ol><li><strong>MUST</strong> expose CNAME setup at <code>POST /v1/admin/tenants/{tenant_id}/cname</code> per DEC-1004 + DEC-1005. Caller has <code>tenant_admin</code> role at tenant level. Body: <code>{ cname: \"portal.acme.com\" }</code>. Handler:</li></ol>\n<ul><li>Generates a random 32-char <code>dns_verify_token</code>.</li><li>INSERTs <code>portal_cname_configs</code> row with <code>status='pending_dns'</code>.</li><li>Returns <code>201 + { cname, dns_verify_record: \"_cyberos-portal-verify TXT \\\"&lt;token&gt;\\\"\" }</code>. Tenant adds the TXT record at their DNS provider.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tenant_id}/cname/{id}/verify</code> per DEC-1004. Handler:</li></ol>\n<ul><li>Resolves the TXT record at <code>_cyberos-portal-verify.&lt;cname&gt;</code> via DNS lookup.</li><li>If TXT value matches <code>dns_verify_token</code>: UPDATE status='dns_verified' + <code>dns_verified_at=now()</code> + invoke ACME issuance per §1 #17 + emit <code>portal.cname_dns_verified</code>.</li><li>On miss: returns <code>424 + { error: \"dns_record_not_found_or_mismatched\" }</code>; client retries (DNS propagation can take 5min-1h).</li></ul>\n<ol><li><strong>MUST</strong> issue ACME TLS cert via <code>instant-acme</code> crate per DEC-1004 + RFC 8555:</li></ol>\n<ul><li>Use Let's Encrypt production endpoint (sandbox env uses staging).</li><li>HTTP-01 challenge served from <code>https://&lt;cname&gt;/.well-known/acme-challenge/&lt;token&gt;</code>.</li><li>On successful cert issuance: KMS-encrypt cert + key + chain into <code>portal_cname_configs.tls_*_kms_blob</code> columns + UPDATE status='active' + <code>tls_issued_at=now()</code> + <code>tls_expires_at=now()+90 days</code>.</li><li>Emit <code>portal.cname_tls_issued</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> run daily ACME renewal job per DEC-1011 + DEC-1004. The <code>cname_renewal_job.rs</code> scheduled job:</li></ol>\n<ul><li>Queries <code>portal_cname_configs WHERE status='active' AND tls_expires_at &lt; now() + interval '30 days' AND last_renewal_attempt_at &lt; now() - interval '4 hours'</code>.</li><li>For each: invoke ACME renewal (same flow as issuance but reusing the DNS-verified domain).</li><li>On success: UPDATE cert blob + <code>tls_expires_at</code> + reset <code>tls_renewal_failures=0</code> + emit <code>portal.cname_tls_renewed</code> sev-2.</li><li>On failure: increment <code>tls_renewal_failures</code> + emit <code>portal.cname_tls_renewal_failed</code> sev-2; after 3 consecutive failures → sev-1 alert; after 7 days at &lt; 30d expiry → page on-call.</li></ul>\n<ol><li><strong>MUST</strong> serve brand assets publicly at <code>GET https://cdn.cyberos.world/brand/{tenant_slug}/{kind}.{ext}?v={sha16}</code> per DEC-1008 + DEC-1009. Handler:</li></ol>\n<ul><li>Resolves tenant_slug → tenant_id.</li><li>Looks up active brand pack + asset by kind.</li><li>Returns asset binary with <code>Content-Type</code> from row + <code>ETag: \"&lt;sha16&gt;\"</code> + <code>Cache-Control: public, max-age=300</code> (5-min edge TTL per DEC-1008).</li><li>On <code>If-None-Match</code> match: 304.</li><li>Unknown slug or kind: 404.</li></ul>\n<ol><li><strong>MUST</strong> publish CDN cache-invalidation event on brand-pack activation per DEC-1008. NATS subject <code>cyberos.portal.brand_cdn.invalidate.&lt;tenant_slug&gt;</code>; downstream CloudFront/CDN edge consumer (slice 1 ops-managed) invalidates the cache. ETag in URL query (<code>?v=&lt;sha16&gt;</code>) means CDN cache hit ratio remains high after invalidation (only the changed URL invalidates).</li></ol>\n<ol><li><strong>MUST</strong> support brand-pack export per DEC-1012. <code>GET /v1/admin/tenants/{tenant_id}/brand-pack/{pack_id}/export</code> returns a deterministic .zip containing:</li></ol>\n<ul><li><code>pack.json</code> — canonical-JSON of the pack metadata (palette + email overrides + asset filenames).</li><li><code>assets/&lt;kind&gt;.&lt;ext&gt;</code> files for each asset. Deterministic per feature-request-audit skill rule 27-28: file order alphabetic; zip mtime = <code>2000-01-01T00:00:00Z</code>; mode 0o644.</li></ul>\n<ol><li><strong>MUST</strong> apply standard fallback chain for email overrides per DEC-1006. FR-EMAIL-001's template loader tries (in order):</li><li><code>services/email/templates/_overrides/&lt;tenant_slug&gt;/&lt;template&gt;.tera</code> (per-tenant override mounted from <code>portal_brand_packs.email_overrides</code>).</li><li><code>services/email/templates/&lt;template&gt;.tera</code> (CyberSkill default). Missing override silently falls through to default; no error.</li></ol>\n<ol><li><strong>MUST</strong> emit 8 memory audit row kinds per DEC-1017 (feature-request-audit skill rule 6 namespace):</li></ol>\n<ul><li><code>portal.brand_pack_created</code> (sev-2)</li><li><code>portal.brand_pack_activated</code> (sev-2)</li><li><code>portal.brand_pack_rolled_back</code> (sev-2)</li><li><code>portal.cname_dns_verified</code> (sev-2)</li><li><code>portal.cname_tls_issued</code> (sev-2)</li><li><code>portal.cname_tls_renewed</code> (sev-3 — routine)</li><li><code>portal.cname_tls_renewal_failed</code> (sev-2 → sev-1 after 3 consecutive)</li><li><code>portal.brand_pack_validation_rejected</code> (sev-3 — informational; high volume during onboarding)</li></ul>\n<ol><li><strong>MUST</strong> thread W3C <code>traceparent</code> across upload → validate → image_pipeline → INSERT → activate → CDN invalidate (feature-request-audit skill rule 22-24). Single trace_id per save operation.</li></ol>\n<ol><li><strong>MUST NOT</strong> persist plaintext TLS keys or plaintext assets — every blob in <code>portal_brand_assets.content_kms_blob</code> and <code>portal_cname_configs.tls_*_kms_blob</code> is KMS-encrypted at rest. Asset serve handler decrypts on-demand into a per-request buffer (no plaintext on disk).</li></ol>\n<ol><li><strong>MUST NOT</strong> allow &gt;1 active CNAME per tenant at slice 1 per DEC-1010. Adding a second active CNAME returns <code>409 + cname_already_configured</code>.</li></ol>\n<ol><li><strong>MUST</strong> be idempotent on brand-pack activation by <code>pack_id</code> — re-activating the currently-active pack returns 200 OK (no-op).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why immutable versioned rows + activation pointer (§1 #1, DEC-1007)?</strong> Brand-pack edits are commercial decisions — a CMO who wants to A/B test logos or roll back a bad change needs the prior version intact. Versioning + activation pointer is the standard pattern (Drupal/WordPress revisions, AWS launch templates). Mutating in-place forfeits rollback safety.</p>\n<p><strong>Why WCAG 2.1 AA enforcement at save (§1 #11, DEC-1003)?</strong> Inaccessible portals harm tenants' end-users + create AODA/ADA legal exposure for the tenants. Save-time enforcement (rather than warn-only) catches the problem before it reaches users; the explicit contrast-ratio feedback in the rejection lets the tenant pick conformant colours immediately.</p>\n<p><strong>Why magic-bytes validation (§1 #12, DEC-1019)?</strong> Content-Type headers are user-controlled — an attacker can claim <code>image/png</code> while uploading an executable. Magic-bytes (first 8 bytes for PNG, <code>&lt;?xml</code>/<code>&lt;svg</code> for SVG) are the actual file-format signature; trust them, not the header. Defends against downstream image-processor exploits (libvips CVE history is substantial).</p>\n<p><strong>Why ACME-issued TLS rather than operator-mediated (§1 #17, DEC-1004)?</strong> Operator-mediated cert issuance is ~24h-to-72h SLA at scale. For self-serve white-label, that latency kills the onboarding UX (\"set up your portal in &lt;1 hour\"). ACME via Let's Encrypt is automatic, free, RFC-conformant, and renewable — the operational cost is one daily job vs. one ops ticket per cert.</p>\n<p><strong>Why daily renewal vs continuous (§1 #18, DEC-1011)?</strong> Let's Encrypt certs are 90 days; renewing at T-30 days gives a 30-day buffer for renewal failures. Daily polling at T-30 vs continuous monitoring means the renewal job is one batch query + per-cert workflow — operationally simple. 4-hour cooldown between attempts (per row) prevents tight retry loops if ACME-side has transient issues.</p>\n<p><strong>Why public-no-auth CDN serve (§1 #19, DEC-1009)?</strong> Brand assets are PUBLIC by definition (logos appear on the tenant's landing page; anyone can scrape them with curl). Requiring auth on a CDN endpoint adds latency + complicates browser caching + doesn't add security. The PII-scrubbing validation at save-time ensures no PII enters the assets, so public serve is safe.</p>\n<p><strong>Why per-tenant Tera fragment sandboxing (§1 #14, DEC-1016)?</strong> Email templates with full Tera power are a remote-code-execution vector — a tenant who can <code>{% set x = read_file(\"/etc/passwd\") %}</code> reads the host. Whitelisting allowed tags + walking the AST is the standard mitigation. Allows useful customisation (variable interpolation, conditional sections) without the foot-gun.</p>\n<p><strong>Why 5-min CDN TTL + ETag (§1 #19, DEC-1008)?</strong> 5-min TTL means a brand-pack change propagates within 5 min worst-case (without manual cache invalidation). ETag means stale cached responses still serve correctly while the cache updates — eventually-consistent. The ?v=&lt;sha16&gt; URL query means each pack version has its own URL, so cache invalidation is automatic on activation (the URL changes; new fetches; no purge needed).</p>\n<p><strong>Why tenant_admin only for CNAME (§1 #15, DEC-1005)?</strong> DNS misconfig affects every user landing on <code>portal.&lt;tenant&gt;.com</code>; engagement_admin role's scope is one engagement, not the tenant-wide DNS. Mirrors FR-PORTAL-003 DEC-882 pattern. Reduces blast-radius surface.</p>\n<p><strong>Why 6-slot fixed palette vs free-form (§1 #11, DEC-1002)?</strong> Fixed slots map to the design-system tokens used across the PORTAL UI components. Free-form palette would force every UI component to invent its own colour-picking logic. 6 slots is the standard tier (Material Design uses 5; Apple HIG uses 6; we picked 6 for parity with the most common design-system patterns).</p>\n<p><strong>Why 1 MiB asset cap + 5 MiB total (§1 #3, DEC-1015)?</strong> 1 MiB PNG at 800×240 splash size is generous; &gt; 1 MiB usually indicates unoptimised export. 5 MiB total cap prevents storage exhaustion via 100s of large uploads. The image_pipeline produces 3 variants per upload, so the total stored ~3x cap.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema (key migrations)</h3>\n<pre><code class=\"language-sql\">-- 0005_portal_brand_packs.sql\nCREATE TABLE portal_brand_packs (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  version INT NOT NULL,\n  palette JSONB NOT NULL,\n  email_overrides JSONB NOT NULL DEFAULT '{}'::jsonb,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  created_by_subject_id UUID NOT NULL,\n  asset_set_id BIGINT NOT NULL,\n  UNIQUE (tenant_id, version)\n);\nALTER TABLE portal_brand_packs ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_brand_packs_rls ON portal_brand_packs\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_brand_packs FROM cyberos_app;\n\n-- 0006_portal_brand_pack_active.sql\nCREATE TABLE portal_brand_pack_active (\n  tenant_id UUID PRIMARY KEY,\n  active_pack_id BIGINT NOT NULL REFERENCES portal_brand_packs(id),\n  activated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  activated_by_subject_id UUID NOT NULL\n);\nALTER TABLE portal_brand_pack_active ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_brand_pack_active_rls ON portal_brand_pack_active\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON portal_brand_pack_active FROM cyberos_app;\nGRANT UPDATE (active_pack_id, activated_at, activated_by_subject_id) ON portal_brand_pack_active TO cyberos_app;\n\n-- 0007_portal_brand_assets.sql\nCREATE TYPE brand_asset_kind AS ENUM ('favicon','header_logo','splash_logo','email_logo');\nCREATE TABLE portal_brand_assets (\n  asset_set_id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  kind brand_asset_kind NOT NULL,\n  mime_type TEXT NOT NULL CHECK (mime_type IN ('image/png','image/svg+xml')),\n  content_kms_blob BYTEA NOT NULL,\n  content_sha256 CHAR(64) NOT NULL,\n  kms_key_id TEXT NOT NULL,\n  content_length_bytes INT NOT NULL CHECK (content_length_bytes &lt;= 1048576),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nALTER TABLE portal_brand_assets ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_brand_assets_rls ON portal_brand_assets\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_brand_assets FROM cyberos_app;\n\n-- 0008_portal_cname_configs.sql\nCREATE TABLE portal_cname_configs (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  cname TEXT NOT NULL,\n  dns_verify_token TEXT NOT NULL,\n  dns_verified_at TIMESTAMPTZ,\n  tls_cert_pem_kms_blob BYTEA,\n  tls_cert_chain_pem_kms_blob BYTEA,\n  tls_key_pem_kms_blob BYTEA,\n  tls_kms_key_id TEXT,\n  tls_issued_at TIMESTAMPTZ,\n  tls_expires_at TIMESTAMPTZ,\n  tls_renewal_failures INT NOT NULL DEFAULT 0,\n  last_renewal_attempt_at TIMESTAMPTZ,\n  status TEXT NOT NULL DEFAULT 'pending_dns'\n    CHECK (status IN ('pending_dns','dns_verified','active','revoked'))\n);\nCREATE UNIQUE INDEX uniq_cname_active_per_tenant ON portal_cname_configs(tenant_id)\n  WHERE status IN ('pending_dns','dns_verified','active');\nCREATE UNIQUE INDEX uniq_cname_global ON portal_cname_configs(cname)\n  WHERE status != 'revoked';\nALTER TABLE portal_cname_configs ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_cname_configs_rls ON portal_cname_configs\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_cname_configs FROM cyberos_app;\nGRANT UPDATE (dns_verified_at, tls_cert_pem_kms_blob, tls_cert_chain_pem_kms_blob, tls_key_pem_kms_blob,\n              tls_kms_key_id, tls_issued_at, tls_expires_at, tls_renewal_failures,\n              last_renewal_attempt_at, status) ON portal_cname_configs TO cyberos_app;</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/portal/src/brand/mod.rs\n#[derive(serde::Deserialize, serde::Serialize, Debug)]\npub struct Palette {\n    pub primary:    String,  // \"#RRGGBB\"\n    pub secondary:  String,\n    pub accent:     String,\n    pub background: String,\n    pub surface:    String,\n    pub error:      String,\n}\n\n#[derive(Copy, Clone, Debug, sqlx::Type)]\n#[sqlx(type_name = \"brand_asset_kind\", rename_all = \"snake_case\")]\npub enum BrandAssetKind { Favicon, HeaderLogo, SplashLogo, EmailLogo }\n\n#[derive(serde::Deserialize, Debug)]\npub struct BrandPackCreateReq {\n    pub palette: Palette,\n    pub email_overrides: Option&lt;EmailOverrides&gt;,\n    pub assets: Vec&lt;AssetUpload&gt;,\n}\n\n#[derive(serde::Deserialize, Debug)]\npub struct EmailOverrides {\n    pub welcome: Option&lt;String&gt;,\n    pub magic_link: Option&lt;String&gt;,\n    pub password_reset: Option&lt;String&gt;,\n    pub invoice_receipt: Option&lt;String&gt;,\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">POST   /v1/admin/tenants/{tenant_id}/brand-pack                    (tenant_admin)\nPOST   /v1/admin/tenants/{tenant_id}/brand-pack/{pack_id}/activate (tenant_admin)\nPOST   /v1/admin/tenants/{tenant_id}/brand-pack/rollback            (tenant_admin)\nGET    /v1/admin/tenants/{tenant_id}/brand-pack/{pack_id}/export    (tenant_admin)\nPOST   /v1/admin/tenants/{tenant_id}/cname                          (tenant_admin)\nPOST   /v1/admin/tenants/{tenant_id}/cname/{id}/verify              (tenant_admin)\nGET    https://cdn.cyberos.world/brand/{tenant_slug}/{kind}.{ext}?v={sha16}  (public)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Brand pack CRUD</strong> — POST creates version 1; second POST creates version 2; both immutable.</li><li><strong>Activation pointer update</strong> — POST activate sets <code>portal_brand_pack_active.active_pack_id</code>; subsequent GET serves the new pack.</li><li><strong>Rollback</strong> — POST rollback to v1 re-points the active pointer; v2 still exists as historic.</li><li><strong>WCAG contrast enforcement</strong> — palette with <code>primary=#888 background=#999</code> (contrast 1.1) → 400 + <code>wcag_contrast_violation</code> + offending_pair body.</li><li><strong>Magic-bytes validation</strong> — asset uploaded as <code>image/png</code> but with <code>&lt;html&gt;</code> content → 400 + <code>invalid_asset_content</code>.</li><li><strong>Image pipeline 3 variants</strong> — uploaded 800×600 PNG produces 32×32 favicon + 200×60 header + 800×240 splash rows in <code>portal_brand_assets</code>.</li><li><strong>CNAME DNS verify</strong> — POST CNAME returns verify token; manual TXT record set; POST verify resolves token → status='dns_verified'.</li><li><strong>ACME issuance</strong> — post-verify, ACME job runs HTTP-01 → cert issued + persisted KMS-encrypted + status='active'.</li><li><strong>Daily renewal</strong> — cert with <code>tls_expires_at = now() + 25 days</code> triggered by renewal job → cert re-issued + <code>tls_expires_at = now() + 90 days</code>.</li><li><strong>Email override sandbox</strong> — override template with <code>{% set x = read_file(\"/etc/passwd\") %}</code> → 400 + <code>tera_disallowed_tag</code>.</li><li><strong>CDN asset ETag</strong> — first GET returns 200 + ETag; second GET with If-None-Match → 304.</li><li><strong>CDN ETag invalidation on activation</strong> — activate new pack → URL ?v= changes → fresh fetch.</li><li><strong>Export deterministic</strong> — two exports of same pack produce byte-identical .zip.</li><li><strong>Size cap rejection</strong> — 2 MiB PNG upload → 413 + <code>asset_too_large</code>.</li><li><strong>brand_asset_kind cardinality</strong> — enum = exactly <code>{favicon, header_logo, splash_logo, email_logo}</code>.</li><li><strong>RLS isolation</strong> — tenant A's session cannot read tenant B's brand pack (RLS returns 0 rows).</li><li><strong>CNAME global uniqueness</strong> — second tenant claiming the same <code>cname</code> → 409 + <code>cname_taken</code>.</li><li><strong>Rate-limit 10/day</strong> — 11th brand-pack save in 24h → 429.</li><li><strong>Renewal failure escalation</strong> — 3 consecutive failures → sev-1 alert; row's <code>tls_renewal_failures=3</code>.</li><li><strong>8 memory audit kinds emitted</strong> — full lifecycle (create + activate + rollback + cname_verify + tls_issued + tls_renewed + tls_renewal_failed + validation_rejected) covered.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>brand_pack_create_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn brand_pack_create_versions_monotonic() {\n    let ctx = TestContext::new().await;\n    let v1 = ctx.create_brand_pack(default_palette(), valid_asset_png()).await.unwrap();\n    let v2 = ctx.create_brand_pack(default_palette(), valid_asset_png()).await.unwrap();\n    assert_eq!(v1.version, 1);\n    assert_eq!(v2.version, 2);\n}</code></pre>\n<h3>5.2 <code>wcag_contrast_enforcement_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn low_contrast_palette_rejected() {\n    let ctx = TestContext::new().await;\n    let palette = Palette {\n        primary: \"#888888\".into(), background: \"#999999\".into(),\n        secondary: \"#000\".into(), accent: \"#000\".into(),\n        surface: \"#fff\".into(), error: \"#f00\".into(),\n    };\n    let r = ctx.post_brand_pack(palette, valid_asset_png()).await;\n    assert_eq!(r.status(), 400);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"wcag_contrast_violation\");\n    assert_eq!(body[\"offending_pair\"][0], \"primary\");\n    assert_eq!(body[\"offending_pair\"][1], \"background\");\n}</code></pre>\n<h3>5.3 <code>magic_bytes_validation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn html_uploaded_as_png_rejected() {\n    let ctx = TestContext::new().await;\n    let fake_png_body = b\"&lt;html&gt;&lt;body&gt;not a png&lt;/body&gt;&lt;/html&gt;\".to_vec();\n    let r = ctx.upload_asset(BrandAssetKind::HeaderLogo, \"image/png\", fake_png_body).await;\n    assert_eq!(r.status(), 400);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"invalid_asset_content\");\n}</code></pre>\n<h3>5.4 <code>image_pipeline_variants_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn upload_generates_three_variants() {\n    let ctx = TestContext::new().await;\n    let png_800x600 = ctx.fixture_png(800, 600);\n    let _ = ctx.upload_asset(BrandAssetKind::HeaderLogo, \"image/png\", png_800x600).await;\n\n    let variants: Vec&lt;(String, i32)&gt; = sqlx::query_as(\n        \"SELECT kind::text, content_length_bytes FROM portal_brand_assets WHERE tenant_id=$1 ORDER BY kind\"\n    ).bind(ctx.tenant_id).fetch_all(&amp;ctx.pool).await.unwrap();\n    assert!(variants.iter().any(|(k,_)| k == \"favicon\"));\n    assert!(variants.iter().any(|(k,_)| k == \"header_logo\"));\n    assert!(variants.iter().any(|(k,_)| k == \"splash_logo\"));\n}</code></pre>\n<h3>5.5 <code>cname_acme_issuance_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn dns_verified_triggers_acme_issuance() {\n    let ctx = TestContext::with_acme_sandbox().await;\n    let cname_id = ctx.post_cname(\"portal-test.example.com\").await;\n    ctx.simulate_dns_txt_record_set(cname_id).await;\n    ctx.post_cname_verify(cname_id).await.expect_status(200);\n    ctx.wait_for_acme_complete(cname_id, Duration::from_secs(30)).await;\n\n    let row: (String, Option&lt;DateTime&lt;Utc&gt;&gt;) = sqlx::query_as(\n        \"SELECT status, tls_issued_at FROM portal_cname_configs WHERE id=$1\"\n    ).bind(cname_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(row.0, \"active\");\n    assert!(row.1.is_some());\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.cname_dns_verified\"));\n    assert!(audit.iter().any(|r| r.kind == \"portal.cname_tls_issued\"));\n}</code></pre>\n<h3>5.6 <code>cname_acme_renewal_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn renewal_job_renews_cert_under_30d() {\n    let ctx = TestContext::with_acme_sandbox().await;\n    let cname_id = ctx.seed_cname_with_cert_expiring_in(Duration::from_days(25)).await;\n    ctx.run_renewal_job().await;\n\n    let row: (Option&lt;DateTime&lt;Utc&gt;&gt;, i32) = sqlx::query_as(\n        \"SELECT tls_expires_at, tls_renewal_failures FROM portal_cname_configs WHERE id=$1\"\n    ).bind(cname_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(row.0.unwrap() &gt; Utc::now() + Duration::days(85));\n    assert_eq!(row.1, 0);\n}</code></pre>\n<h3>5.7 <code>email_override_sandbox_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn dangerous_tera_tag_rejected() {\n    let ctx = TestContext::new().await;\n    let bad_template = r#\"{% set leaked = read_file(\"/etc/passwd\") %}Hello {{ name }}\"#;\n    let r = ctx.post_brand_pack_with_overrides(EmailOverrides {\n        welcome: Some(bad_template.into()), ..Default::default()\n    }).await;\n    assert_eq!(r.status(), 400);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"tera_disallowed_tag\");\n}</code></pre>\n<h3>5.8 <code>brand_asset_etag_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cdn_asset_etag_304_on_match() {\n    let ctx = TestContext::new().await;\n    ctx.activate_brand_pack().await;\n    let r1 = ctx.get_cdn_asset(\"acme\", \"header_logo\", \"png\").send().await.unwrap();\n    let etag = r1.headers()[\"etag\"].to_str().unwrap().to_owned();\n    let r2 = ctx.get_cdn_asset(\"acme\", \"header_logo\", \"png\")\n        .header(\"if-none-match\", &amp;etag).send().await.unwrap();\n    assert_eq!(r2.status(), 304);\n}</code></pre>\n<h3>5.9 <code>brand_pack_export_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn export_is_deterministic() {\n    let ctx = TestContext::new().await;\n    let pack_id = ctx.create_brand_pack(default_palette(), valid_asset_png()).await.unwrap().id;\n    let z1 = ctx.export_brand_pack(pack_id).await;\n    let z2 = ctx.export_brand_pack(pack_id).await;\n    assert_eq!(sha256(&amp;z1), sha256(&amp;z2));\n}</code></pre>\n<h3>5.10 <code>brand_pack_rls_isolation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn tenant_a_cannot_read_tenant_b_brand_pack() {\n    let ctx = TestContext::with_two_tenants().await;\n    let pack_id = ctx.as_tenant(\"b\").create_brand_pack(default_palette(), valid_asset_png()).await.unwrap().id;\n    let rows: Vec&lt;(i64,)&gt; = sqlx::query_as(\"SELECT id FROM portal_brand_packs WHERE id=$1\")\n        .bind(pack_id).fetch_all(ctx.pool_as(\"a\")).await.unwrap_or_default();\n    assert_eq!(rows.len(), 0);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<h3>6.1 WCAG contrast formula</h3>\n<pre><code class=\"language-rust\">// services/portal/src/brand/validate.rs\npub fn relative_luminance(hex: &amp;str) -&gt; f64 {\n    let (r, g, b) = parse_hex(hex);\n    let lin = |c: f64| if c &lt;= 0.03928 { c / 12.92 } else { ((c + 0.055) / 1.055).powf(2.4) };\n    0.2126 * lin(r as f64 / 255.0) + 0.7152 * lin(g as f64 / 255.0) + 0.0722 * lin(b as f64 / 255.0)\n}\n\npub fn contrast_ratio(fg: &amp;str, bg: &amp;str) -&gt; f64 {\n    let l1 = relative_luminance(fg);\n    let l2 = relative_luminance(bg);\n    let (l, d) = if l1 &gt; l2 { (l1, l2) } else { (l2, l1) };\n    (l + 0.05) / (d + 0.05)\n}\n\npub fn validate_wcag_aa(palette: &amp;Palette) -&gt; Result&lt;(), ValidationError&gt; {\n    let pairs = [\n        (\"primary\", \"background\", 4.5),\n        (\"error\", \"background\", 4.5),\n        (\"primary\", \"surface\", 3.0),\n    ];\n    for (fg_name, bg_name, required) in pairs {\n        let ratio = contrast_ratio(palette.get(fg_name), palette.get(bg_name));\n        if ratio &lt; required {\n            return Err(ValidationError::WcagViolation {\n                offending_pair: [fg_name, bg_name],\n                actual_ratio: ratio,\n                required_ratio: required,\n            });\n        }\n    }\n    Ok(())\n}</code></pre>\n<h3>6.2 Magic-bytes check</h3>\n<pre><code class=\"language-rust\">pub fn validate_magic_bytes(claimed_mime: &amp;str, content: &amp;[u8]) -&gt; Result&lt;(), ValidationError&gt; {\n    match claimed_mime {\n        \"image/png\" =&gt; {\n            if content.len() &lt; 8 || &amp;content[0..8] != b\"\\x89PNG\\r\\n\\x1a\\n\" {\n                return Err(ValidationError::InvalidAssetContent { claimed: claimed_mime.into() });\n            }\n        }\n        \"image/svg+xml\" =&gt; {\n            let head = std::str::from_utf8(&amp;content[..content.len().min(256)]).unwrap_or(\"\");\n            if !head.trim_start().starts_with(\"&lt;?xml\") &amp;&amp; !head.trim_start().starts_with(\"&lt;svg\") {\n                return Err(ValidationError::InvalidAssetContent { claimed: claimed_mime.into() });\n            }\n        }\n        _ =&gt; return Err(ValidationError::UnsupportedMimeType(claimed_mime.into())),\n    }\n    Ok(())\n}</code></pre>\n<h3>6.3 ACME issuance via instant-acme</h3>\n<pre><code class=\"language-rust\">pub async fn issue_cert(ctx: &amp;AppCtx, cname: &amp;str) -&gt; Result&lt;IssuedCert, AcmeError&gt; {\n    let order = ctx.acme_account.new_order(&amp;[Identifier::Dns(cname.into())]).await?;\n    let challenges = order.authorizations().await?;\n    for auth in challenges {\n        let challenge = auth.find_http_01().ok_or(AcmeError::NoHttp01Challenge)?;\n        ctx.acme_challenge_server.serve(&amp;challenge.token, &amp;challenge.key_authorization()).await;\n        challenge.ready().await?;\n    }\n    order.poll_ready(Duration::from_secs(120)).await?;\n    let (cert_chain_pem, key_pem) = order.finalize().await?;\n    Ok(IssuedCert {\n        cert_pem: cert_chain_pem.first_cert_pem(),\n        chain_pem: cert_chain_pem.intermediate_pem(),\n        key_pem,\n        expires_at: parse_x509_expiry(&amp;cert_chain_pem)?,\n    })\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>FR-TEN-101</strong> Self-serve signup — tenant exists before brand pack can be created.</li></ul>\n<p><strong>Cross-module (related_frs):</strong></p>\n<ul><li><strong>FR-PORTAL-001</strong> Scoped read-only views — consumes brand pack at render time.</li><li><strong>FR-PORTAL-003</strong> External IdP — brand applied at IdP redirect login page.</li><li><strong>FR-PORTAL-005</strong> Branded Genie — brand pack applied to embedded chat.</li><li><strong>FR-TEN-103</strong> Residency provisioning — CDN edge selection per residency.</li><li><strong>FR-AUTH-101</strong> RBAC — <code>tenant_admin</code> role gate.</li><li><strong>FR-EMAIL-001</strong> Transactional email — consumes fallback chain for tenant overrides.</li><li><strong>FR-DOC-001</strong> Documents — brand pack may apply to PDF export headers.</li><li><strong>FR-AI-003</strong> memory audit-row bridge — 8 new kinds.</li><li><strong>FR-MEMORY-111</strong> PII scrubbing — validation that no PII enters assets.</li></ul>\n<p><strong>Downstream (blocks):</strong> None.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>POST /brand-pack</code> request</h3>\n<pre><code class=\"language-json\">{\n  \"palette\": {\n    \"primary\":    \"#1A73E8\",\n    \"secondary\":  \"#34A853\",\n    \"accent\":     \"#FBBC04\",\n    \"background\": \"#FFFFFF\",\n    \"surface\":    \"#F8F9FA\",\n    \"error\":      \"#D93025\"\n  },\n  \"email_overrides\": {\n    \"welcome\": \"Welcome to {{ tenant_name }}'s portal!\\n\\nClick {{ magic_link }} to sign in.\"\n  },\n  \"assets\": [\n    { \"kind\": \"header_logo\", \"mime_type\": \"image/png\", \"base64_content\": \"iVBORw0KGgo...\" }\n  ]\n}</code></pre>\n<h3>8.2 <code>portal.brand_pack_activated</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.brand_pack_activated\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.tenant_admin.789\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"pack_id\": 42,\n    \"version\": 3,\n    \"previous_pack_id\": 41\n  }\n}</code></pre>\n<h3>8.3 CNAME setup response (201)</h3>\n<pre><code class=\"language-json\">{\n  \"cname\": \"portal.acme.com\",\n  \"dns_verify_record\": \"_cyberos-portal-verify TXT \\\"abc123def456...\\\"\",\n  \"status\": \"pending_dns\",\n  \"next_step\": \"Add the TXT record above to your DNS provider, then POST /cname/{id}/verify\"\n}</code></pre>\n<h3>8.4 WCAG violation response (400)</h3>\n<pre><code class=\"language-json\">{\n  \"error\": \"wcag_contrast_violation\",\n  \"offending_pair\": [\"primary\", \"background\"],\n  \"actual_ratio\": 1.13,\n  \"required_ratio\": 4.5,\n  \"remediation_hint\": \"Try a darker primary or lighter background\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 1. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-CNAME per tenant (region-specific landing pages) — slice 2.</li><li><strong>Deferred:</strong> Email template GUI editor (vs raw Tera) — slice 2.</li><li><strong>Deferred:</strong> Brand-pack inheritance (parent tenant → child engagement) — slice 2.</li><li><strong>Deferred:</strong> Custom favicon variants beyond 32×32 (16×16, Apple touch, etc.) — slice 2.</li><li><strong>Deferred:</strong> SVG runtime sanitization (defang scripts inside SVG) — slice 2; for now SVG accepted as-is with magic-bytes only.</li><li><strong>Deferred:</strong> Brand-pack analytics (which version most-served) — slice 3.</li><li><strong>Deferred:</strong> Dark-mode palette variants — slice 2 (6 light-mode slots only at slice 1).</li><li><strong>Deferred:</strong> Custom CSS overrides beyond palette — slice 3 (security review needed).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>WCAG contrast violation</td><td>save-time check</td><td>400 + <code>wcag_contrast_violation</code> + remediation hint + <code>portal.brand_pack_validation_rejected</code></td><td>Tenant picks conformant colours</td></tr><tr><td>Magic-bytes mismatch (asset spoof)</td><td>upload validation</td><td>400 + <code>invalid_asset_content</code></td><td>Tenant re-uploads valid asset</td></tr><tr><td>Asset &gt; 1 MiB</td><td>size check</td><td>413 + <code>asset_too_large</code></td><td>Tenant optimises asset</td></tr><tr><td>Total brand pack &gt; 5 MiB</td><td>aggregate check</td><td>400 + <code>brand_pack_total_too_large</code></td><td>Tenant reduces asset sizes</td></tr><tr><td>Rate limit 10/day hit</td><td>per-tenant counter</td><td>429 + <code>Retry-After: 86400</code></td><td>Tenant waits 24h or contacts support</td></tr><tr><td>Tera template with disallowed tag</td><td>AST walk</td><td>400 + <code>tera_disallowed_tag</code> + tag name</td><td>Tenant removes the tag</td></tr><tr><td>DNS verify TXT not found</td><td>DNS resolver returns NXDOMAIN</td><td>424 + <code>dns_record_not_found</code></td><td>Tenant adds TXT + waits propagation; client retries</td></tr><tr><td>ACME HTTP-01 challenge fails (port 80 closed)</td><td>ACME poll returns invalid</td><td>sev-2 alert <code>portal.cname_tls_issuance_failed</code>; cname status remains <code>dns_verified</code></td><td>Tenant opens port 80; manual re-trigger</td></tr><tr><td>ACME rate-limited (LE rate limits)</td><td>ACME 429</td><td>Backoff 1h + retry; sev-2 if persistent</td><td>Inherent — wait + retry</td></tr><tr><td>Daily renewal job fails</td><td>row's <code>tls_renewal_failures</code> increments</td><td>After 3 consecutive → sev-1; &gt; 7 days at &lt; 30d expiry → page on-call</td><td>On-call investigates; manual renewal via CLI</td></tr><tr><td>Cert expired (renewal job didn't run)</td><td>renewal job's stale-job detector</td><td>sev-1 immediate</td><td>On-call runs manual ACME issuance</td></tr><tr><td>Tenant CNAME conflicts with existing</td><td>unique index <code>uniq_cname_global</code></td><td>409 + <code>cname_taken</code></td><td>Tenant picks alternate CNAME</td></tr><tr><td>KMS unavailable when decrypting asset</td><td>KMS timeout</td><td>503 + sev-2 <code>portal.kms_unavailable</code>; CDN serves stale-cached if available</td><td>AWS KMS recovers; cache rebuilds</td></tr><tr><td>CDN cache invalidation NATS publish fails</td><td>NATS error</td><td>Cache staleness up to 5min (TTL); sev-3 informational</td><td>Inherent — TTL eventual consistency</td></tr><tr><td>Image pipeline (vips) crash</td><td>vips returns error</td><td>500 + sev-2 alert; asset NOT persisted</td><td>Operator investigates; libvips upgrade may be needed</td></tr><tr><td>Rollback to non-existent pack_id</td><td>FK constraint fails</td><td>404 + <code>target_pack_not_found</code></td><td>Tenant chooses valid pack from list</td></tr><tr><td>Multiple concurrent activations (race)</td><td>UPSERT on portal_brand_pack_active is atomic</td><td>Last writer wins; previous activation memory row still exists for audit</td><td>Inherent — last-write-wins acceptable</td></tr><tr><td>Tenant uploads SVG with embedded <code>&lt;script&gt;</code></td><td>slice 1 accepts as-is</td><td>XSS risk if SVG served inline</td><td>Slice 2 adds SVG sanitization; slice 1 mitigates by serving SVG with <code>Content-Disposition: attachment</code> for now (downloaded, not inline)</td></tr><tr><td>Email override <code>{% include %}</code> outside per-tenant path</td><td>sandbox path constraint</td><td>400 + <code>tera_include_path_violation</code></td><td>Tenant uses relative path</td></tr><tr><td>Renewal job runs on revoked cert</td><td>status check</td><td>Skip + log informational; no audit row</td><td>Inherent — guard at job entry</td></tr><tr><td>Brand pack export size &gt; 100 MiB</td><td>size check before zip stream</td><td>413 + <code>export_too_large</code></td><td>Tenant exports per-asset instead</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> The W3C relative luminance formula (§6.1) is the standard WCAG 2.x algorithm; do not invent variations. The <code>lin</code> curve handles the sRGB gamma correction.</p>\n<p><strong>§11.2</strong> <code>csscolorparser</code> crate handles hex parsing + future RGB/HSL formats; slice 1 accepts only <code>#RRGGBB</code> hex.</p>\n<p><strong>§11.3</strong> <code>libvips</code> binding (<code>libvips-rs</code>) is a Rust wrapper around the C library; deployment requires <code>libvips-dev</code> package. Production image processing is faster + safer than alternatives (ImageMagick has historical CVE volume).</p>\n<p><strong>§11.4</strong> ACME via <code>instant-acme</code> crate is the recommended Rust ACME client. Account key persisted in <code>portal_cname_configs.acme_account_key_kms_blob</code> at deployment time (one global account; per-cname order).</p>\n<p><strong>§11.5</strong> HTTP-01 challenge server runs as a sub-handler at <code>/.well-known/acme-challenge/{token}</code> exposing the keyAuthorization for the active order. Multi-tenant: dispatch by <code>Host</code> header to the correct order's response.</p>\n<p><strong>§11.6</strong> Tera sandbox uses a custom <code>tera::Tera</code> instance with <code>register_function</code> calls limited to a whitelist; <code>register_filter</code> not exposed; <code>register_tester</code> not exposed. AST walk uses tera's <code>Context</code> introspection.</p>\n<p><strong>§11.7</strong> CDN edge: CloudFront in front of our origin; cache key includes <code>?v=&lt;sha16&gt;</code> query param so different versions never collide. 5-min TTL at edge.</p>\n<p><strong>§11.8</strong> ETag format: SHA-256-truncated 16 hex chars of the canonical asset bytes; matches FR-MCP-005's PRM ETag pattern.</p>\n<p><strong>§11.9</strong> Brand pack JSON export uses canonical-JSON (sorted keys) for deterministic byte equality across runs (feature-request-audit skill rule 27).</p>\n<p><strong>§11.10</strong> ZIP archive determinism: <code>ZIP_DEFLATED level 6 + fixed mtime 2000-01-01T00:00:00Z + mode 0o644 + sorted entries</code> (consistent with AGENTS.md §10 portability pattern).</p>\n<p><strong>§11.11</strong> The <code>created_by_subject_id</code> + <code>activated_by_subject_id</code> columns enable per-actor brand-change auditing (which CMO/admin made the change).</p>\n<p><strong>§11.12</strong> SVG sanitization deferred to slice 2: slice 1 serves SVG with <code>Content-Disposition: attachment</code> to prevent inline rendering + XSS. Tenants who want inline SVG can use PNG (which can't carry script).</p>\n<p><strong>§11.13</strong> ACME account renewal at year-7 (LE accounts expire in 10 years; renew at 7). Out-of-scope at slice 1 (next renewal is 2033).</p>\n<p><strong>§11.14</strong> The renewal job is idempotent on <code>(cname_id, run_date)</code> — running twice the same day is a no-op after first success.</p>\n<p><strong>§11.15</strong> Per-tenant rate limit (10 saves/day) uses Redis sliding-window like FR-TEN-101 §1 #5; same infrastructure.</p>\n<p><strong>§11.16</strong> Email-override fallback chain is FR-EMAIL-001 internal mechanism; this FR populates the override files via <code>portal_brand_packs.email_overrides</code> JSONB → on-disk Tera fragment.</p>\n<p><strong>§11.17</strong> CNAME global uniqueness (<code>uniq_cname_global</code>) prevents two tenants from claiming the same custom domain — first-come-first-served; second tenant gets <code>cname_taken</code> until first revokes.</p>\n<p><strong>§11.18</strong> Activation timestamp + actor_id retention: never delete activation history; rollback creates a new activation row pointing to the historic pack.</p>\n<p><strong>§11.19</strong> Brand pack JSON schema versioning: slice 1 implicit v1; if v2 changes shape, the loader uses <code>schema_version</code> field (added in slice 2).</p>\n<p><strong>§11.20</strong> Per-asset <code>content_sha256</code> enables cache-hit at the asset table level — if a tenant uploads the same logo twice (re-paste), the SHA dedup avoids re-storage (slice 2 enhancement; slice 1 stores duplicates).</p>\n<hr>\n<p><em>End of FR-PORTAL-002 spec.</em></p>";
