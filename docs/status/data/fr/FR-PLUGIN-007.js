window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLUGIN-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PLUGIN module <strong>MUST</strong> ship multi-runtime adapters at <code>services/plugin-host/src/adapters/&lt;target&gt;/</code>. Each adapter transforms the canonical CyberOS manifest (per FR-PLUGIN-001) into a target-runtime-native bundle. P1 targets: claude-code, cursor, cowork, codex-cli.</p>\n<ol><li><strong>MUST</strong> ship adapters for all four P1 targets per DEC-2460:</li></ol>\n<ul><li><strong><code>claude-code</code></strong> — produces <code>.plugin</code> zip with <code>plugin.json</code> at root + <code>commands/*.md</code> + <code>skills/*</code> + <code>bin/cyberos-mcp-bridge</code></li><li><strong><code>cursor</code></strong> — produces <code>.mcp.json</code> single-file config pointing at <code>cyberos-mcp-bridge</code> stdio transport; omits commands + skills per DEC-2465</li><li><strong><code>cowork</code></strong> — produces Customize-slot zip with <code>manifest.json</code> + <code>commands/*.md</code> + <code>skills/*</code> + HTTP-transport bridge config</li><li><strong><code>codex-cli</code></strong> — produces top-level <code>SKILL.md</code> + nested <code>skills/</code> subfolders + env var binding for MCP</li></ul>\n<ol><li><strong>MUST</strong> consume the SAME canonical manifest per DEC-2462. The CLI <code>cyberos-plugin pack --target &lt;name&gt;</code> reads <code>modules/plugin/manifests/&lt;id&gt;@&lt;version&gt;.plugin.json</code> once and dispatches to the target adapter. Authors do NOT write per-target manifests.</li></ol>\n<ol><li><strong>MUST</strong> be reproducible per DEC-2463. For a given canonical manifest + target, two pack invocations produce identical SHA-256 bundle hashes. Adapters MUST use the reproducibility helpers in <code>adapters/common.rs</code> (epoch mtimes, sorted entries, fixed permissions) — same as FR-PLUGIN-001 clause 10.</li></ol>\n<ol><li><strong>MUST</strong> include the MCP bridge binary <code>cyberos-mcp-bridge</code> inside the bundle for cursor + cowork + codex-cli per DEC-2464. Claude Code references the binary via cwd-relative path because Claude Code manages binary lifecycle separately. Each adapter MUST select the right architecture binary (<code>x86_64-linux-musl</code>, <code>aarch64-darwin</code>, <code>x86_64-windows-msvc</code>) from a CDN-published artefact set.</li></ol>\n<ol><li><strong>MUST</strong> omit <code>skills/</code> directory from the Cursor bundle per DEC-2465. Cursor's MCP integration surfaces tools but does NOT render Skills. Shipping skills/ bloats the bundle without benefit and may confuse users.</li></ol>\n<ol><li><strong>MUST</strong> emit bundles conforming to each target's published format per DEC-2466:</li></ol>\n<ul><li>claude-code: <code>.plugin</code> format per Anthropic Claude Code plugin spec (zip with manifest at root)</li><li>cursor: <code>.mcp.json</code> format per Cursor MCP integration docs</li><li>cowork: Customize slot format per Anthropic Cowork docs</li><li>codex-cli: Anthropic Agent Skills SKILL.md format (single SKILL.md at root + nested skills/)</li></ul>\n<ol><li><strong>MUST</strong> translate the canonical manifest's <code>tools[]</code> to target-appropriate shape:</li></ol>\n<ul><li>claude-code: tools nested under <code>mcp_servers[0].tools[]</code></li><li>cursor: tools live in <code>command</code> config; Cursor introspects via MCP <code>tools/list</code> at runtime</li><li>cowork: tools under <code>connectors[0].tools[]</code></li><li>codex-cli: tools surface via MCP server env-var; SKILL.md describes via prose</li></ul>\n<ol><li><strong>MUST</strong> translate <code>commands[]</code> and <code>skills[]</code> per target capability:</li></ol>\n<ul><li>claude-code: copy <code>commands/*.md</code> into <code>.claude/commands/</code>; copy <code>skills/*</code> into <code>.claude/skills/</code></li><li>cursor: omit both</li><li>cowork: copy commands into <code>commands/</code>; copy skills into <code>skills/</code></li><li>codex-cli: convert commands into supplementary SKILL.md files (one per command); copy skills as-is</li></ul>\n<ol><li><strong>MUST</strong> validate target name against the manifest's <code>targets[]</code> array — if author packs <code>--target cursor</code> but manifest doesn't list <code>cursor</code> in <code>targets[]</code>, the pack fails with a clear error. This prevents shipping bundles for targets the author didn't intend.</li></ol>\n<ol><li><strong>MUST</strong> emit a bundle-level Sigstore signature distinct per target per FR-PLUGIN-001 clause 8. The same canonical manifest produces 4 distinct signatures (one per target bundle).</li></ol>\n<ol><li><strong>MUST</strong> include adapter-specific test coverage at <code>services/plugin-host/tests/adapter_&lt;target&gt;_test.rs</code> validating: bundle structure, manifest translation, tools-list translation, reproducibility, and absence of disallowed content (e.g. skills in cursor bundle).</li></ol>\n<ol><li><strong>MUST NOT</strong> introduce proprietary extensions to target formats per DEC-2466. Adapter output MUST be loadable in the standard host without custom patches.</li></ol>\n<ol><li><strong>MUST NOT</strong> require authors to maintain per-target manifests per DEC-2462.</li></ol>\n<ol><li><strong>MUST NOT</strong> leak machine/runtime context (cwd, env vars, mtimes) into bundles per DEC-2463.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why exactly 4 P1 targets (DEC-2460)?</strong> Each P1 target reflects a real 2026 user base: Claude Code (Anthropic users), Cursor (developer base), Cowork (Anthropic non-developer base), Codex CLI (OpenAI users adopting SKILL.md format). Goose / Amp / Continue.dev are smaller, more niche; ship in P2 once core targets validate the adapter pattern.</p>\n<p><strong>Why single canonical manifest (DEC-2462, clause 2)?</strong> Drift between per-target manifests is the #1 source of bug reports in multi-runtime ecosystems. Single source of truth + adapter-time translation eliminates the drift class entirely. Authors edit one file; adapters compute the rest.</p>\n<p><strong>Why reproducibility per target (DEC-2463, clause 3)?</strong> Sigstore Rekor anchors the bundle hash. If the same source produces different bundles per build, the signature is unverifiable. Reproducibility makes \"build it yourself, verify the hash\" work — a credibility-builder for OSS distribution (Strategy §4 Level 1).</p>\n<p><strong>Why bundle the bridge for most targets (DEC-2464, clause 4)?</strong> Cursor + Codex CLI + Cowork users expect a one-step install. If they have to \"also install the bridge separately,\" adoption drops. Claude Code has its own binary lifecycle (<code>claude-code plugin install</code> handles dependencies), so bundling there would be wasteful.</p>\n<p><strong>Why omit playbooks from Cursor (DEC-2465, clause 5)?</strong> Cursor's MCP integration as of 2026 surfaces tools to its built-in agent (which has its own routing). Skills are an Anthropic-spec concept; Cursor doesn't read them. Shipping ~12 SKILL.md files in the Cursor bundle is bloat. The canonical manifest still has <code>skills[]</code>; the Cursor adapter drops them.</p>\n<p><strong>Why per-target signatures (clause 10)?</strong> Each bundle has a different byte stream → different hash → different signature. A single signature on the canonical manifest doesn't bind to the per-target bundle. Per-target signing is mandatory for verifier round-trip.</p>\n<p><strong>Why architecture-specific binaries (clause 4)?</strong> CyberOS users run macOS (Intel + ARM), Linux (x86_64), Windows (x86_64). Adapter picks the right binary at pack time based on the target architecture metadata. CDN-published artefacts let the adapter not bake-in the binary at compile time.</p>\n<p><strong>Why test per-target (clause 11)?</strong> Each target's format is the conformance bar. Generic tests can't catch \"Cursor rejected this because port field name was wrong.\" Per-target tests embed the target's expectations.</p>\n<p><strong>Why no proprietary extensions (DEC-2466, clause 12)?</strong> A bundle that needs a CyberOS-only patch is not really portable. Strategy §4 hinges on \"open-standard\" positioning. Extensions undermine the claim.</p>\n<p><strong>Why adapter-target-list cross-check (clause 9)?</strong> A bundle packed for a target not in the manifest's <code>targets[]</code> array is a manifest bug — author forgot to update. Catching at pack time saves a publish-side rejection.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Adapter trait</h3>\n<pre><code class=\"language-rust\">// services/plugin-host/src/adapters/mod.rs\n#[async_trait]\npub trait Adapter: Send + Sync {\n    fn target_name(&amp;self) -&gt; &amp;'static str;\n    fn pack(&amp;self, manifest: &amp;CanonicalManifest, out_dir: &amp;Path) -&gt; Result&lt;PackedBundle&gt;;\n}\n\npub struct PackedBundle {\n    pub target: &amp;'static str,\n    pub path: PathBuf,\n    pub sha256: String,\n    pub size_bytes: u64,\n    pub included_tools: u32,\n    pub included_commands: u32,\n    pub included_skills: u32,\n    pub binary_included: bool,\n}</code></pre>\n<h3>Adapter selection</h3>\n<pre><code class=\"language-rust\">// services/plugin-host/src/bin/cyberos-plugin.rs\nfn select_adapter(target: &amp;str) -&gt; Result&lt;Box&lt;dyn Adapter&gt;&gt; {\n    match target {\n        \"claude-code\" =&gt; Ok(Box::new(ClaudeCodeAdapter)),\n        \"cursor\"      =&gt; Ok(Box::new(CursorAdapter)),\n        \"cowork\"      =&gt; Ok(Box::new(CoworkAdapter)),\n        \"codex-cli\"   =&gt; Ok(Box::new(CodexCliAdapter)),\n        \"goose\"|\"amp\"|\"continue-dev\" =&gt; Err(Error::DeferredTarget(target.to_string())),\n        _ =&gt; Err(Error::UnknownTarget(target.to_string())),\n    }\n}</code></pre>\n<h3>Claude Code bundle layout</h3>\n<pre><code>cyberos-1.0.0.claude-code.plugin (zip)\n├── plugin.json                    (target-specific manifest)\n├── README.md                      (rendered in Claude Code's plugin detail view)\n├── .claude/\n│   ├── commands/                  (from manifest commands[])\n│   │   ├── cyberos-run.md\n│   │   ├── cyberos-memory.md\n│   │   ├── cyberos-skill-list.md\n│   │   └── cyberos-route.md\n│   └── skills/                    (from manifest skills[])\n│       ├── run-cuo-workflow/SKILL.md\n│       └── ... (11 more)\n└── (no bridge binary — claude-code manages separately)</code></pre>\n<h3>Cursor bundle layout</h3>\n<pre><code>cyberos-1.0.0.cursor.mcp.json (single file)\n{\n  \"mcpServers\": {\n    \"cyberos\": {\n      \"command\": \"/path/to/cyberos-mcp-bridge\",\n      \"args\": [\"--transport\", \"stdio\"],\n      \"env\": {\n        \"CYBEROS_AUTH_ENDPOINT\": \"https://auth.cyberskill.world\",\n        \"CYBEROS_MEMORY_ENDPOINT\": \"https://memory.cyberskill.world\"\n      }\n    }\n  }\n}</code></pre>\n<p>(Bridge binary shipped alongside as <code>cyberos-mcp-bridge.&lt;arch&gt;</code>; user moves to PATH.)</p>\n<h3>Cowork bundle layout</h3>\n<pre><code>cyberos-1.0.0.cowork.zip\n├── manifest.json                   (Customize slot manifest)\n├── README.md\n├── commands/\n│   └── ... (4 commands)\n├── skills/\n│   └── ... (12 skills)\n└── bin/\n    ├── cyberos-mcp-bridge.x86_64-linux-musl\n    ├── cyberos-mcp-bridge.aarch64-darwin\n    └── cyberos-mcp-bridge.x86_64-windows-msvc</code></pre>\n<h3>Codex CLI bundle layout</h3>\n<pre><code>cyberos-1.0.0.codex-cli/  (folder, not zipped)\n├── SKILL.md                        (top-level: routes to cyberos.* tools via MCP env var)\n├── README.md\n├── skills/\n│   ├── run-cuo-workflow/SKILL.md\n│   └── ... (11 more)\n└── bin/cyberos-mcp-bridge.&lt;arch&gt;</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong><code>pack --target claude-code</code> emits .plugin zip</strong> — output exists, valid zip.</li><li><strong>claude-code bundle has plugin.json at root</strong> — zip listing shows <code>plugin.json</code> entry.</li><li><strong>claude-code bundle has .claude/commands/ with 4 entries</strong> — zip listing.</li><li><strong>claude-code bundle has .claude/skills/ with 12 entries</strong> — zip listing.</li><li><strong>claude-code bundle does NOT include bridge binary</strong> — no <code>bin/cyberos-mcp-bridge*</code> entries.</li><li><strong><code>pack --target cursor</code> emits .mcp.json</strong> — output is single file, JSON.</li><li><strong>cursor .mcp.json validates against Cursor spec</strong> — schema check.</li><li><strong>cursor bundle does NOT include commands or skills</strong> — bundle is single file.</li><li><strong><code>pack --target cowork</code> emits zip with HTTP transport</strong> — manifest.json has <code>mcp_url: \"...\"</code> not stdio path.</li><li><strong>cowork bundle includes bridge binary</strong> — zip listing has <code>bin/cyberos-mcp-bridge.*</code>.</li><li><strong><code>pack --target codex-cli</code> emits folder with SKILL.md root</strong> — folder structure correct.</li><li><strong>codex-cli SKILL.md has frontmatter with <code>name: cyberos</code></strong> — frontmatter check.</li><li><strong>Reproducibility: two packs same target = same SHA-256</strong> — test runs pack twice, asserts equal.</li><li><strong>Cross-target SHA-256 differs</strong> — pack target A and target B; hashes differ (different content).</li><li><strong><code>pack --target goose</code> fails with DeferredTarget error</strong> — adapter selector explicit.</li><li><strong><code>pack --target bogus</code> fails with UnknownTarget error</strong> — adapter selector explicit.</li><li><strong><code>pack --target cursor</code> on manifest without cursor in targets[]` fails</strong> — clause 9 check.</li><li><strong>claude-code adapter copies commands verbatim</strong> — content hash equal to source.</li><li><strong>codex-cli adapter converts commands to supplementary SKILL.md files</strong> — count matches commands count.</li><li><strong>cowork adapter uses HTTP transport not stdio</strong> — manifest.json has HTTP URL.</li><li><strong>All bundles' Sigstore signatures verify</strong> — <code>cosign verify</code> on each bundle passes.</li><li><strong>Cursor bundle works in Cursor (smoke)</strong> — install Cursor adapter output → MCP tool list shows 8 tools.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/adapter_claude_code_test.rs\n#[test]\nfn claude_code_bundle_structure() {\n    let manifest = load_canonical_manifest(\"cyberos@1.0.0\");\n    let result = ClaudeCodeAdapter.pack(&amp;manifest, &amp;tmp_out_dir()).unwrap();\n    let zip = zip::ZipArchive::new(File::open(&amp;result.path).unwrap()).unwrap();\n    let names: Vec&lt;String&gt; = zip.file_names().map(String::from).collect();\n    assert!(names.contains(&amp;\"plugin.json\".to_string()));\n    let claude_cmds = names.iter().filter(|n| n.starts_with(\".claude/commands/\")).count();\n    assert_eq!(claude_cmds, 4);\n    let claude_skills = names.iter().filter(|n| n.starts_with(\".claude/skills/\")).count();\n    assert!(claude_skills &gt;= 12); // 12 SKILL.md + 12 TRIGGER_TESTS.md\n    let bin_entries = names.iter().filter(|n| n.starts_with(\"bin/\")).count();\n    assert_eq!(bin_entries, 0, \"claude-code should NOT include bridge binary\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/adapter_cursor_test.rs\n#[test]\nfn cursor_bundle_is_single_mcp_json() {\n    let manifest = load_canonical_manifest(\"cyberos@1.0.0\");\n    let result = CursorAdapter.pack(&amp;manifest, &amp;tmp_out_dir()).unwrap();\n    assert!(result.path.ends_with(\"cyberos-1.0.0.mcp.json\"));\n    assert_eq!(result.included_commands, 0);\n    assert_eq!(result.included_skills, 0);\n    let content: serde_json::Value = serde_json::from_str(&amp;fs::read_to_string(&amp;result.path).unwrap()).unwrap();\n    assert!(content[\"mcpServers\"][\"cyberos\"][\"command\"].is_string());\n    assert!(content[\"mcpServers\"][\"cyberos\"][\"args\"].as_array().unwrap().contains(&amp;json!(\"--transport\")));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/adapter_cowork_test.rs\n#[test]\nfn cowork_bundle_uses_http_transport() {\n    let manifest = load_canonical_manifest(\"cyberos@1.0.0\");\n    let result = CoworkAdapter.pack(&amp;manifest, &amp;tmp_out_dir()).unwrap();\n    let zip = zip::ZipArchive::new(File::open(&amp;result.path).unwrap()).unwrap();\n    let manifest_bytes = read_zip_file(&amp;zip, \"manifest.json\");\n    let m: serde_json::Value = serde_json::from_slice(&amp;manifest_bytes).unwrap();\n    assert!(m[\"connectors\"][0][\"mcp_url\"].as_str().unwrap().starts_with(\"http\"));\n}\n\n#[test]\nfn cowork_bundle_includes_bridge_binary() {\n    let result = CoworkAdapter.pack(&amp;load_canonical_manifest(\"cyberos@1.0.0\"), &amp;tmp_out_dir()).unwrap();\n    let zip = zip::ZipArchive::new(File::open(&amp;result.path).unwrap()).unwrap();\n    let names: Vec&lt;String&gt; = zip.file_names().map(String::from).collect();\n    assert!(names.iter().any(|n| n.starts_with(\"bin/cyberos-mcp-bridge\")));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/adapter_codex_cli_test.rs\n#[test]\nfn codex_cli_emits_top_level_skill_md() {\n    let result = CodexCliAdapter.pack(&amp;load_canonical_manifest(\"cyberos@1.0.0\"), &amp;tmp_out_dir()).unwrap();\n    let skill_md = result.path.join(\"SKILL.md\");\n    assert!(skill_md.exists());\n    let raw = fs::read_to_string(&amp;skill_md).unwrap();\n    assert!(raw.starts_with(\"---\\n\"));\n    let fm: serde_yaml::Value = serde_yaml::from_str(extract_frontmatter(&amp;raw)).unwrap();\n    assert_eq!(fm[\"name\"].as_str().unwrap(), \"cyberos\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/adapter_reproducibility_test.rs\n#[test]\nfn pack_is_reproducible_per_target() {\n    let manifest = load_canonical_manifest(\"cyberos@1.0.0\");\n    for adapter in [Box::new(ClaudeCodeAdapter) as Box&lt;dyn Adapter&gt;,\n                    Box::new(CursorAdapter), Box::new(CoworkAdapter), Box::new(CodexCliAdapter)] {\n        let r1 = adapter.pack(&amp;manifest, &amp;tmp_out_dir()).unwrap();\n        let r2 = adapter.pack(&amp;manifest, &amp;tmp_out_dir()).unwrap();\n        assert_eq!(r1.sha256, r2.sha256, \"target {} not reproducible\", adapter.target_name());\n    }\n}\n\n#[test]\nfn cross_target_hashes_differ() {\n    let manifest = load_canonical_manifest(\"cyberos@1.0.0\");\n    let cc = ClaudeCodeAdapter.pack(&amp;manifest, &amp;tmp_out_dir()).unwrap();\n    let cu = CursorAdapter.pack(&amp;manifest, &amp;tmp_out_dir()).unwrap();\n    assert_ne!(cc.sha256, cu.sha256);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Bundle layouts in §3 + adapter trait are the skeleton. Each adapter is ~150-300 Rust lines.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> FR-PLUGIN-002 (bridge binary referenced in 3 of 4 adapter bundles); FR-PLUGIN-004 (skills folder layout); FR-PLUGIN-005 (auth manifest section translation).</li><li><strong>Downstream:</strong> FR-PLUGIN-008 (marketplace publish reads target list per bundle).</li><li><strong>Cross-module:</strong> FR-PLUGIN-003 (commands surface per adapter); FR-PLUGIN-006 (audit endpoint per adapter target).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Claude Code <code>plugin.json</code> (after translation from canonical)</h3>\n<pre><code class=\"language-json\">{\n  \"name\": \"cyberos\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Persona-aware orchestration + memory + skills for any agentic IDE.\",\n  \"mcp_servers\": [{\n    \"name\": \"cyberos\",\n    \"command\": \"./bin/cyberos-mcp-bridge\",\n    \"args\": [\"--transport\", \"stdio\"],\n    \"tools\": [/* 8 tools verbatim */]\n  }]\n}</code></pre>\n<h3>Cowork Customize <code>manifest.json</code> (after translation)</h3>\n<pre><code class=\"language-json\">{\n  \"schema_version\": \"1.0.0\",\n  \"id\": \"cyberos\",\n  \"version\": \"1.0.0\",\n  \"name\": \"CyberOS\",\n  \"connectors\": [{\n    \"type\": \"mcp\",\n    \"mcp_url\": \"http://127.0.0.1:8082/mcp\",\n    \"transport\": \"http\"\n  }],\n  \"commands\": [/* 4 entries */],\n  \"skills\": [/* 12 entries */]\n}</code></pre>\n<h3>Codex CLI top-level <code>SKILL.md</code></h3>\n<pre><code class=\"language-markdown\">---\nname: cyberos\ndescription: &gt;\n  CyberOS persona-aware orchestration + memory + skills. Use when the user wants to \"execute a CUO workflow\",\n  \"look up the audit trail\", \"find a CyberOS skill\", or \"route a query to the right persona\".\nlicense: Apache-2.0\n---\n\nThis skill provides 8 CyberOS tools via MCP. Refer to skills/ for granular routing playbooks.\n\n## Setup\n\nEnsure `CYBEROS_MCP_BRIDGE_PATH` points at the bundled `bin/cyberos-mcp-bridge` binary.\nAuthenticate once via `cyberos-plugin auth login` (browser-based OAuth-PKCE).\n\n## Tools available\n\n(see skills/*/SKILL.md for the playbook map)</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<ul><li>~~Should the adapter chain produce a single multi-target bundle (one zip with all targets)?~~ → No, one bundle per target per clause 1. Multi-target zip complicates install and signature verification.</li><li>~~Should we ship adapter for VS Code's MCP integration?~~ → VS Code uses Continue.dev for MCP; deferred to P2 successor FR.</li><li>~~Should the codex-cli adapter convert each command into a separate skill (1:1)?~~ → Yes for now (per clause 8); revisit when Codex CLI's command surface ships.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Unknown target name</td><td>adapter selector</td><td>UnknownTarget error</td><td>Author uses one of 4 P1 targets</td></tr><tr><td>Deferred target name</td><td>adapter selector</td><td>DeferredTarget error</td><td>Wait for P2 successor FR or contribute</td></tr><tr><td>Target not in manifest.targets[]</td><td>pack-time cross-check</td><td>error: \"manifest doesn't include target\"</td><td>Author updates manifest</td></tr><tr><td>Bridge binary not found for target arch</td><td>filesystem check</td><td>error: \"binary for x86_64-linux-musl not in CDN cache\"</td><td>Run pre-fetch step OR fall back to download-at-pack-time</td></tr><tr><td>Reproducibility broken</td><td>adapter test</td><td>CI fails</td><td>Investigate which adapter helper leaks state</td></tr><tr><td>Bundle exceeds target size limit</td><td>adapter size guard</td><td>error</td><td>Trim large skills body content; image assets</td></tr><tr><td>Sigstore signing fails</td><td>sign step</td><td>adapter error</td><td>Operator inspects Sigstore credentials</td></tr><tr><td>Target adapter has bug</td><td>per-target test</td><td>CI fails</td><td>Author fixes adapter</td></tr><tr><td>Canonical manifest changes mid-pack</td><td>file modtime check</td><td>error: \"manifest changed during pack\"</td><td>Re-run pack</td></tr><tr><td>Cursor bundle includes skills</td><td>adapter test</td><td>CI fails</td><td>Adapter logic bug</td></tr><tr><td>Codex-cli bundle missing SKILL.md root</td><td>adapter test</td><td>CI fails</td><td>Adapter logic bug</td></tr><tr><td>Cowork bundle uses stdio</td><td>adapter test</td><td>CI fails</td><td>Adapter logic bug</td></tr><tr><td>Adapter omits commands when target supports them</td><td>adapter test</td><td>CI fails</td><td>Adapter logic bug</td></tr><tr><td>Adapter introduces proprietary field</td><td>manual code review at PR</td><td>reviewer rejects</td><td>Strip the extension</td></tr><tr><td>Per-target bundles have same hash</td><td>adapter test</td><td>CI fails</td><td>One of the adapters is broken</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 <strong>Adapter implementation pattern.</strong> Each adapter has 3 phases: (a) <code>prepare()</code> — read canonical manifest, validate target compatibility; (b) <code>emit()</code> — produce target-specific files in scratch dir; (c) <code>seal()</code> — zip + sign + compute hash. Common helpers in <code>adapters/common.rs::seal_reproducible_zip</code>.</li></ul>\n<ul><li>§11.2 <strong>Binary selection.</strong> Adapters that bundle binary use a <code>bin_cache</code> directory pre-populated by CI from CDN URLs <code>https://cdn.cyberskill.world/binaries/cyberos-mcp-bridge/{version}/{target}</code>. Pack failure if any required arch missing.</li></ul>\n<ul><li>§11.3 <strong>CDN binary cache.</strong> CI build job uploads built binaries to CDN at every release. Adapter downloads on demand if cache miss. SHA-256 of binary is pinned in canonical manifest's <code>binary_pins</code> map (added in FR-PLUGIN-007a if needed).</li></ul>\n<ul><li>§11.4 <strong>Claude Code bundle.</strong> <code>.plugin</code> files are zips per Anthropic Claude Code spec. Bundle root has <code>plugin.json</code>. Nested <code>.claude/commands/</code> and <code>.claude/skills/</code> are read by Claude Code at install.</li></ul>\n<ul><li>§11.5 <strong>Cursor <code>.mcp.json</code>.</strong> Single file written to user's <code>.cursor/mcp.json</code> location at install. Cursor reads at startup. The file has no commands or skills — Cursor doesn't render those. Tool list is discovered via MCP <code>tools/list</code> at runtime.</li></ul>\n<ul><li>§11.6 <strong>Cowork manifest.</strong> Customize slots accept zip uploads. Manifest at root has <code>connectors[]</code> array; each connector entry declares MCP transport URL. HTTP transport is used because Cowork runs in-cloud (no local stdio).</li></ul>\n<ul><li>§11.7 <strong>Codex CLI.</strong> As of late 2025, OpenAI's Codex CLI adopted Anthropic Agent Skills SKILL.md format. Bundle is a folder (not zip) per spec. Top-level SKILL.md is the entrypoint; nested skills/ are routing playbooks. Codex CLI auto-discovers via filesystem scan.</li></ul>\n<ul><li>§11.8 <strong>Why no zip for codex-cli.</strong> Codex CLI's discovery is filesystem-based — users symlink the bundle folder into their skills directory. Zipping would force an unzip step. Folder is the right unit.</li></ul>\n<ul><li>§11.9 <strong>Multi-arch single bundle.</strong> Cursor + Cowork + Codex bundles include 3 binaries (Linux x86_64, macOS arm64, Windows x86_64) by default. Adapter has a <code>--arch</code> flag to ship single-arch bundles for size-sensitive distribution. Default is multi-arch.</li></ul>\n<ul><li>§11.10 <strong>Why no goose / amp / continue-dev in v1.</strong> Each adds ~200 LoC + tests + format learning cost. Validating the 4-adapter pattern in v1 lets us evolve confidently. Successor FR (FR-PLUGIN-007a) covers P2 targets.</li></ul>\n<hr>\n<p><em>End of FR-PLUGIN-007 spec.</em></p>";
