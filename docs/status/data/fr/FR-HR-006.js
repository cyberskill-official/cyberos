window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-HR-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The HR service <strong>MUST</strong> ship leave accrual at <code>services/hr/src/accrual/</code> running nightly via FR-MCP-007 cron, computing base + seniority per Decree 145, immutable ledger, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> schedule batch at 02:00 tenant_tz per DEC-1850.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>accrual_kind</code> against closed enum per DEC-1852.</li></ol>\n<ol><li><strong>MUST</strong> compute per-month accrual at <code>monthly_batch.rs::accrue(member, year_month)</code>:</li></ol>\n<ul><li>base = 1.0d (Decree 145 Art. 65) × pro-rate (FR-HR-002 contract type override)</li><li>seniority = floor(years_of_service / 5) × 1.0d (Art. 66; default 0 unless industry special)</li><li>skip if member.is_active=false for that month</li></ul>\n<ol><li><strong>MUST</strong> be idempotent per DEC-1854 — UNIQUE on (member_id, year_month, kind); ON CONFLICT DO NOTHING.</li></ol>\n<ol><li><strong>MUST</strong> support correction at <code>correction_handler.rs::add_correction(member, year_month, days, reason)</code> per DEC-1853 — new row with kind='correction' + sign.</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0006</code>: ``<code>sql CREATE TABLE hr_leave_accrual_ledger ( ledger_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_id UUID NOT NULL, year_month CHAR(7) NOT NULL,  -- 'YYYY-MM' kind TEXT NOT NULL CHECK (kind IN ('monthly_base','seniority_bonus','correction','carryover')), days_added NUMERIC(5,2) NOT NULL, reason TEXT, applied_by UUID,  -- system or CHRO uuid trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (tenant_id, member_id, year_month, kind) ); CREATE INDEX accrual_member_year_idx ON hr_leave_accrual_ledger(tenant_id, member_id, year_month DESC); ALTER TABLE hr_leave_accrual_ledger ENABLE ROW LEVEL SECURITY; CREATE POLICY accrual_rls ON hr_leave_accrual_ledger USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON hr_leave_accrual_ledger FROM cyberos_app; -- Append-only per DEC-1853 </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST   /v1/hr/accrual/run-batch              (manual trigger, CHRO) POST   /v1/hr/accrual/corrections            (CHRO; new correction row) GET    /v1/hr/members/{id}/accrual-ledger    (history) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1855. PII per FR-MEMORY-111: reason SHA-256 hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from cron → batch → ledger insert → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior accrual per DEC-1853.</li></ol>\n<ol><li><strong>MUST NOT</strong> double-credit per DEC-1854 (UNIQUE).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why nightly cron (DEC-1850)?</strong> Daily run catches month-end transitions; weekly delay = members short on leave for 7d.</p>\n<p><strong>Why immutable ledger (DEC-1853)?</strong> Audit lineage; corrections via new row preserve \"who changed what when\".</p>\n<p><strong>Why idempotent (DEC-1854)?</strong> Cron may retry on failure; double-run must not double-credit.</p>\n<p><strong>Why seniority bonus (DEC-1851)?</strong> Decree 145 Art. 66 mandates for hazardous/heavy industries; default 0 for office, configurable per tenant.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample accrual ledger row:</p>\n<pre><code class=\"language-json\">{\n  \"ledger_id\": \"uuid\",\n  \"member_id\": \"uuid\",\n  \"year_month\": \"2026-06\",\n  \"kind\": \"monthly_base\",\n  \"days_added\": 1.00,\n  \"applied_by\": \"system\",\n  \"created_at\": \"2026-07-01T02:00:00Z\"\n}</code></pre>\n<p>Sample correction:</p>\n<pre><code class=\"language-json\">POST /v1/hr/accrual/corrections\n{\n  \"member_id\": \"uuid\",\n  \"year_month\": \"2026-05\",\n  \"days_added\": 0.5,\n  \"reason\": \"Late hire mid-May; pro-rate adjustment\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Nightly batch 02:00 tenant_tz</strong>. 2. <strong>1d/month base accrual</strong>. 3. <strong>Seniority bonus configurable per tenant (default 0)</strong>. 4. <strong>Pro-rate respects FR-HR-002 contract type</strong>. 5. <strong>Inactive members skipped</strong>. 6. <strong>Idempotent via UNIQUE</strong>. 7. <strong>Correction kind allows manual adj</strong>. 8. <strong>kind enum cardinality 4</strong>. 9. <strong>4 memory audit kinds emitted</strong>. 10. <strong>PII scrubbed (reason SHA256)</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>Append-only via REVOKE</strong>. 14. <strong>History query desc time</strong>. 15. <strong>CHRO-only manual trigger</strong>. 16. <strong>CHRO-only correction</strong>. 17. <strong>Year_month format 'YYYY-MM' enforced</strong>. 18. <strong>Cron skip if 0 active members</strong>. 19. <strong>Days_added precision 2 decimal (rust_decimal)</strong>. 20. <strong>Carryover kind for year-end roll</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn monthly_base_accrual() {\n    let ctx = TestContext::with_active_indefinite_member().await;\n    ctx.run_accrual_batch(ctx.tenant_id, \"2026-06\").await;\n    let ledger = ctx.fetch_ledger(ctx.member_id).await;\n    assert!(ledger.iter().any(|r| r.kind == \"monthly_base\" &amp;&amp; r.year_month == \"2026-06\" &amp;&amp; r.days_added == dec!(1.0)));\n}\n\n#[tokio::test]\nasync fn idempotent_double_run() {\n    let ctx = TestContext::with_active_member().await;\n    ctx.run_accrual_batch(ctx.tenant_id, \"2026-06\").await;\n    ctx.run_accrual_batch(ctx.tenant_id, \"2026-06\").await;\n    let ledger = ctx.fetch_ledger(ctx.member_id).await;\n    let count = ledger.iter().filter(|r| r.kind == \"monthly_base\" &amp;&amp; r.year_month == \"2026-06\").count();\n    assert_eq!(count, 1);\n}\n\n#[tokio::test]\nasync fn correction_creates_new_row() {\n    let ctx = TestContext::with_accrual_for_member(\"2026-05\").await;\n    ctx.add_correction(ctx.member_id, \"2026-05\", dec!(0.5), \"late hire\").await;\n    let ledger = ctx.fetch_ledger(ctx.member_id).await;\n    assert!(ledger.iter().any(|r| r.kind == \"correction\" &amp;&amp; r.days_added == dec!(0.5)));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-HR-004. <strong>Cross-module:</strong> FR-HR-002 (contract pro-rate), FR-MCP-007 (cron), FR-AUTH-101 (CHRO), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Batch fails mid-run</td><td>partial inserts</td><td>sev-2; resume from last successful</td><td>re-run safe</td></tr><tr><td>Duplicate cron instance</td><td>UNIQUE</td><td>second skipped</td><td>inherent</td></tr><tr><td>Member inactive mid-month</td><td>skip</td><td>inherent</td><td>data integrity</td></tr><tr><td>Year_month format wrong</td><td>validate</td><td>reject 400</td><td>use YYYY-MM</td></tr><tr><td>Correction without reason</td><td>warn but allow</td><td>inherent</td><td>audit visible</td></tr><tr><td>Cross-tenant correction</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>Days_added negative</td><td>allow (correction reduces)</td><td>inherent</td><td>inherent</td></tr><tr><td>Pro-rate computation wrong</td><td>unit tests</td><td>inherent</td><td>code fix</td></tr><tr><td>Cron skipped (system down)</td><td>catch-up next boot</td><td>inherent</td><td>inherent</td></tr><tr><td>Seniority bonus misconfigured</td><td>default 0</td><td>inherent</td><td>tenant config fix</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Cron via FR-MCP-007 <code>kind: 'hr.accrual_batch'</code>, daily 02:00 tenant_tz.</li><li>§11.2 Year-end carryover: separate cron at Jan 1, kind='carryover', moves unused annual days per policy (max 5d typically).</li><li>§11.3 memory audit body: tenant_id, year_month, members_count; reason SHA256.</li><li>§11.4 Manual trigger for backfill: CHRO specifies year_month range; iterates each month.</li><li>§11.5 Days_added uses rust_decimal (2 decimal places); never float.</li></ul>\n<hr>\n<p><em>End of FR-HR-006 spec.</em></p>";
