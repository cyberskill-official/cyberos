window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PORTAL-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship per-Engagement external IdP (SAML 2.0 + OIDC) sign-in for client-tenant users with SCIM 2.0 JIT provisioning, claim → role mapping, signed-attribute trust chain, per-Engagement isolation, and 7 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define the <code>portal_idp_configs</code> table at migration <code>0001</code>: <code>(id UUID PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, idp_kind portal_idp_kind NOT NULL, idp_name TEXT NOT NULL, idp_entity_id TEXT NOT NULL, idp_metadata_url TEXT, idp_signing_cert_kms_blob BYTEA NOT NULL, idp_signing_cert_thumbprint TEXT NOT NULL, idp_kms_key_id TEXT NOT NULL, enforcement TEXT NOT NULL CHECK (enforcement IN ('optional','required')) DEFAULT 'optional', email_domain_hint TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), rotated_at TIMESTAMPTZ, status TEXT NOT NULL CHECK (status IN ('active','rotated','revoked')) DEFAULT 'active')</code>. Partial unique <code>(tenant_id, engagement_id) WHERE status='active'</code> enforces one active IdP per Engagement.</li></ol>\n<ol><li><strong>MUST</strong> define the closed <code>portal_idp_kind</code> enum at migration <code>0001</code> with exactly 2 values: <code>saml, oidc</code> (DEC-869). CI cardinality test asserts 2; adding a third requires schema migration + DEC entry.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_idp_groups_map</code> at migration <code>0003</code>: <code>(idp_config_id UUID NOT NULL, idp_group_name TEXT NOT NULL, cyberos_role TEXT NOT NULL, PRIMARY KEY (idp_config_id, idp_group_name))</code>. Many-to-one (multiple IdP groups → one CyberOS role per DEC-885); one IdP group cannot map to multiple roles (composite-PK enforces).</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_scim_tokens</code> at migration <code>0004</code>: <code>(engagement_id UUID PRIMARY KEY, token_sha256 CHAR(64) NOT NULL, kms_key_id TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), rotated_at TIMESTAMPTZ, status TEXT NOT NULL CHECK (status IN ('active','rotated','revoked')))</code>. Bearer-token form per DEC-867; rotation per DEC-884.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_scim_audit_log</code> at migration <code>0002</code>: append-only event journal: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, scim_operation TEXT NOT NULL CHECK (scim_operation IN ('user_create','user_update','user_delete','group_create','group_update','group_delete','token_rotated')), external_id TEXT, subject_id UUID, request_sha256 CHAR(64) NOT NULL, response_status INT NOT NULL, ts TIMESTAMPTZ NOT NULL DEFAULT now())</code>. REVOKE UPDATE, DELETE from <code>cyberos_app</code> (feature-request-audit skill rule 12).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both USING and WITH CHECK on all 4 PORTAL tables (feature-request-audit skill rule 13). Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code> PLUS per-Engagement scope check for staff-level reads.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/engagements/{engagement_id}/idp</code> for IdP config creation. Caller MUST have role <code>tenant_admin</code> at the TENANT level per DEC-882 (not engagement_admin — IdP misconfig has cross-engagement blast radius). Body validates: <code>idp_kind</code>, <code>idp_entity_id</code>, <code>idp_metadata_url</code> (HTTPS-only) OR <code>idp_signing_cert_pem</code> (one of two; if URL provided, fetched + parsed inline), <code>enforcement</code>, <code>email_domain_hint</code>. Handler KMS-encrypts the signing cert + persists, emits <code>portal.idp_config_created</code>.</li></ol>\n<ol><li><strong>MUST</strong> support SAML 2.0 SP per OASIS SAML 2.0 + DEC-861 + DEC-880. The SP endpoint at <code>/saml/v2/{engagement_slug}/acs</code> (Assertion Consumer Service) accepts POST-binding Responses. Verification:</li></ol>\n<ul><li>Locate <code>idp_config</code> via <code>engagement_slug</code>; reject if no active config.</li><li>XML-parse the Response; reject if non-well-formed.</li><li>Verify the SAML Response signature against <code>idp_signing_cert_kms_blob</code> (xmldsig per W3C; cert chain validated against <code>idp_signing_cert_thumbprint</code>).</li><li>Verify <code>InResponseTo</code> matches a server-side pending RequestID (CSRF defense; 5-min TTL).</li><li>Verify <code>Conditions/NotBefore</code> and <code>Conditions/NotOnOrAfter</code> (replay window = 5 min per DEC-870).</li><li>Per DEC-864 + #15: only honour AttributeStatements whose containing Assertion is signed; unsigned attributes are dropped + emit <code>portal.signed_attr_trust_violation</code>.</li><li>Extract <code>NameID</code> (subject identifier) + signed AttributeStatement claims.</li><li>JIT-provision via #11.</li></ul>\n<ol><li><strong>MUST</strong> support OIDC RP per FR-AUTH-104 protocol layer + per-Engagement metadata at <code>/oidc/v1/{engagement_slug}/.well-known/openid-configuration</code>. The callback at <code>/oidc/v1/{engagement_slug}/callback</code> verifies:</li></ol>\n<ul><li>PKCE code_verifier matches the stored challenge (per FR-AUTH-104 DEC-401).</li><li><code>state</code> nonce matches the server-issued state (single-use, 5-min TTL).</li><li>ID token signature against IdP JWKS (cached 24h, kid-based rotation per FR-AUTH-104 DEC-402).</li><li><code>iat</code> skew ≤ 60 s per DEC-870.</li><li><code>aud</code> claim matches the per-Engagement client_id.</li><li>JIT-provision via #11.</li></ul>\n<ol><li><strong>MUST</strong> expose SAML SP metadata at <code>GET /saml/v2/{engagement_slug}/metadata</code> per DEC-874 — XML document with <code>&lt;EntityDescriptor&gt;</code> + <code>&lt;SPSSODescriptor&gt;</code> + AssertionConsumerService URL + signing cert (if encryption requested). IdP admins self-configure their IdP with this URL.</li></ol>\n<ol><li><strong>MUST</strong> JIT-provision via FR-AUTH-002 at first SSO sign-in per DEC-862. Flow:</li></ol>\n<ul><li>Lookup existing subject by <code>(tenant_id, engagement_id, external_id)</code> where <code>external_id</code> = SAML NameID OR OIDC <code>sub</code> claim.</li><li>If exists: load + update <code>last_sso_at</code> + sync claim-derived role per #12 + return.</li><li>If not exists: INSERT subject via FR-AUTH-002 with <code>auth_method='external_idp'</code>, <code>idp_config_id</code>, <code>external_id</code>, <code>email</code> (claim), <code>display_name</code> (claim), <code>role</code> (claim-mapped per #12).</li><li>Bind subject to Engagement via FR-AUTH-101 RBAC grant.</li><li>Emit <code>portal.scim_user_created</code> (sev-2 — material identity event).</li></ul>\n<ol><li><strong>MUST</strong> apply closed claim → role mapping per DEC-863 + DEC-885:</li></ol>\n<ul><li>Default role for any JIT-provisioned subject = <code>client_viewer</code>.</li><li>For each IdP group in the claim's <code>groups</code> array (SAML AttributeStatement <code>Group</code> attribute OR OIDC <code>groups</code> claim), lookup <code>portal_idp_groups_map(idp_config_id, idp_group_name)</code> → <code>cyberos_role</code>. Highest-privilege match wins (role ordinal: viewer &lt; editor &lt; admin).</li><li>Unrecognised groups silently ignored (no role elevation).</li><li>Role change at re-auth: if claim-mapped role differs from current, UPDATE subject + emit <code>portal.scim_user_updated</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose SCIM 2.0 endpoints per RFC 7643 + RFC 7644 + DEC-867:</li></ol>\n<ul><li><code>POST   /scim/v2/{engagement_slug}/Users</code> — create (idempotent on <code>externalId</code>).</li><li><code>GET    /scim/v2/{engagement_slug}/Users/{id}</code> — read.</li><li><code>PATCH  /scim/v2/{engagement_slug}/Users/{id}</code> — partial update per RFC 7644 §3.5.2.</li><li><code>PUT    /scim/v2/{engagement_slug}/Users/{id}</code> — full replace.</li><li><code>DELETE /scim/v2/{engagement_slug}/Users/{id}</code> — soft-tombstone (delegate to FR-PORTAL-004 for full session-invalidation flow).</li><li><code>POST   /scim/v2/{engagement_slug}/Groups</code> + analogous CRUD.</li><li>Bearer-token auth via Authorization header (DEC-867); token validated against <code>portal_scim_tokens.token_sha256</code> SHA-256-hashed lookup.</li></ul>\n<ol><li><strong>MUST</strong> enforce SCIM idempotency on <code>externalId</code> claim per DEC-876. Duplicate create with same externalId returns 409 + body <code>{ \"detail\": \"User with this externalId already exists\", \"existing_id\": \"&lt;id&gt;\" }</code>. RFC 7644 §3.3 conformant.</li></ol>\n<ol><li><strong>MUST</strong> drop unsigned AttributeStatement claims per DEC-864 + #8. The signed_attr.rs module walks the XML tree, identifies the enclosing <code>&lt;ds:Signature&gt;</code> for each <code>&lt;saml:AttributeStatement&gt;</code>, verifies the signature against the configured cert thumbprint, and includes ONLY signed attributes in the JIT claim set. Any unsigned attribute encountered emits a <code>portal.signed_attr_trust_violation</code> sev-2 memory row with <code>(engagement_id, attribute_name, idp_entity_id)</code>.</li></ol>\n<ol><li><strong>MUST</strong> support per-Engagement <code>enforcement: required</code> per DEC-871. When <code>required</code>, the standard email/password login endpoint (FR-AUTH-004) returns <code>403 + { error: \"sso_required\", idp_redirect_url: \"https://portal.cyberos.world/sso/&lt;engagement_slug&gt;\" }</code> for any user whose <code>auth_method='external_idp'</code> OR who belongs to a <code>required</code>-enforcement Engagement. The check is at JWT mint, before password verification.</li></ol>\n<ol><li><strong>MUST</strong> enforce JWT session lifetime 8h for IdP-authenticated subjects per DEC-879 + DEC-883. FR-AUTH-004 JWT mint reads <code>subject.auth_method</code> — if <code>external_idp</code>, sets <code>exp = now + 8h</code>. At any sensitive operation, the handler checks <code>last_sso_at</code>; if &gt; 8h ago AND <code>enforcement='required'</code>, redirect to IdP for re-authentication.</li></ol>\n<ol><li><strong>MUST</strong> support email-domain IdP discovery per DEC-865. The <code>/v1/portal/sign-in?email=alice@acme.com</code> endpoint extracts the email domain, matches against <code>portal_idp_configs.email_domain_hint</code>, and on match returns <code>{ redirect_url: \"https://portal.cyberos.world/sso/&lt;engagement_slug&gt;\", idp_name: \"Acme SSO\" }</code>. No match → <code>{ fallback: \"email_password\" }</code>.</li></ol>\n<ol><li><strong>MUST</strong> rotate SCIM tokens quarterly per DEC-884. The <code>POST /v1/admin/engagements/{id}/scim-token/rotate</code> endpoint generates a new 32-byte token (base64url-encoded), SHA-256-hashes for storage, marks the old token as <code>rotated</code>, sets a 60-second overlap window during which BOTH tokens are accepted. Old token cleanup after overlap. Emits <code>portal.scim_token_rotation</code> informational row (not in core 7-kind list per DEC-873).</li></ol>\n<ol><li><strong>MUST</strong> emit 7 memory audit row kinds per DEC-873 (feature-request-audit skill rule 6 namespace pattern <code>^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$</code>):</li></ol>\n<ul><li><code>portal.idp_sign_in</code> (sev-2)</li><li><code>portal.idp_sign_in_failed</code> (sev-2 — security signal)</li><li><code>portal.scim_user_created</code> (sev-2)</li><li><code>portal.scim_user_updated</code> (sev-3)</li><li><code>portal.idp_config_created</code> (sev-1 — material security event)</li><li><code>portal.idp_config_rotated</code> (sev-1)</li><li><code>portal.signed_attr_trust_violation</code> (sev-1)</li></ul>\n<p>    Plus 1 supporting kind: <code>portal.scim_token_rotation</code> (sev-3 — informational). PII-scrubbed via FR-MEMORY-111 per DEC-886.</p>\n<ol><li><strong>MUST</strong> support SP-initiated AND IdP-initiated SAML flows per DEC-881. IdP-initiated POSTs lacking <code>InResponseTo</code> are accepted ONLY when <code>portal_idp_configs.allow_idp_initiated=true</code> (defaults false; explicit opt-in due to CSRF risk). OIDC supports SP-initiated only per OAuth 2.1 PKCE requirement.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub all audit rows per DEC-886 + feature-request-audit skill rule 18. The 7 memory row kinds carry <code>email_hash16</code>, <code>name_hash16</code>, <code>external_id_hash16</code> — raw values retained in <code>subjects</code> (RLS-scoped to tenant) only.</li></ol>\n<ol><li><strong>MUST</strong> thread W3C <code>traceparent</code> across SAML/OIDC roundtrip + JIT provisioning + SCIM operations (feature-request-audit skill rule 22 + 23 + 24). Trace_id persisted in <code>portal_scim_audit_log.trace_id</code> column (added via 0002 migration).</li></ol>\n<ol><li><strong>MUST NOT</strong> persist IdP private keys (CyberOS doesn't hold IdP signing keys — verification uses IdP-published public certs only). IdP public certs ARE KMS-wrapped per DEC-866 because operational hardening (secret-of-secrets pattern); not because they're cryptographically private.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow a JIT-provisioned subject to elevate role beyond claim-mapped per DEC-863. Subsequent role mutations via FR-AUTH-101 admin UI are permitted (tenant_admin can override claim-mapping), but the JIT path itself is closed.</li></ol>\n<ol><li><strong>MUST</strong> be rate-limited at the SP endpoint (<code>/saml/v2/{eng}/acs</code>) — 100 req/min/IP (legitimate IdP traffic well under this); excess returns 429 + <code>Retry-After</code> (defends against credential-stuffing via SAML brute force).</li></ol>\n<ol><li><strong>MUST</strong> emit per-Engagement metric <code>portal_idp_sign_in_duration_seconds</code> to OBS (FR-OBS-005). Alarm sev-2 if p95 &gt; 3 s sustained 10 min (latency indicator for IdP-side issues).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why per-Engagement (not per-tenant) IdP binding (§1 #1, DEC-860)?</strong> A single CyberOS tenant frequently hosts many client Engagements — e.g., a consulting firm runs 50 client projects, each client having their own corporate IdP. Per-tenant IdP forces the consulting firm to pick one IdP for all clients — impossible. Per-Engagement matches the commercial reality.</p>\n<p><strong>Why signed-attribute trust chain (§1 #15, DEC-864)?</strong> SAML AttributeStatements are XML elements that can appear in unsigned form within a signed Response. Naively trusting all AttributeStatements lets an attacker (or compromised IdP intermediary) inject claims like <code>groups=[\"cyberos-admin\"]</code>. The mitigation: walk the XML, verify the signature scope, drop anything not within the signature's coverage. This is the OWASP-recommended pattern for SAML SP implementations.</p>\n<p><strong>Why claim → role mapping closed-set (§1 #12, DEC-863)?</strong> Open-set mapping (\"any group name in IdP → same-named role in CyberOS\") is a privilege-escalation vector — an IdP-side admin who can edit group names can grant themselves CyberOS admin by renaming a group. Closed-set mapping (\"only mappings explicitly configured in <code>portal_idp_groups_map</code> are honoured\") makes the privilege boundary explicit and audit-able.</p>\n<p><strong>Why SCIM 2.0 instead of custom user-sync API (§1 #13)?</strong> SCIM is the de facto enterprise standard (Okta, Azure AD, OneLogin, Google Workspace all speak it). Custom API forces every enterprise to write a custom connector. SCIM is straightforward to implement (RFC 7644 is ~80 pages of clear spec) and unlocks ~100% of enterprise customers without custom work.</p>\n<p><strong>Why 8h JWT lifetime for IdP-auth (§1 #17, DEC-879)?</strong> Industry convention. Internal users get 24h sessions (Okta/Azure recommendation). External-IdP users get 8h because (a) IdP-side session may be revoked by the customer's security team mid-day, and (b) the IdP may want to enforce re-auth for sensitive ops. 8h is a reasonable middle ground between security and UX.</p>\n<p><strong>Why required-enforcement mode (§1 #16, DEC-871)?</strong> Regulated industries (finance, healthcare) often have compliance requirements like \"all employees must SSO, no password fallback.\" Without <code>enforcement: required</code>, a tenant admin could enable IdP for convenience but leave password fallback open — defeating the compliance objective.</p>\n<p><strong>Why many-to-one group mapping (§1 #12, DEC-885)?</strong> Common enterprise pattern: \"Sales group + Marketing group + Customer Success group all get the <code>client_viewer</code> role.\" One-to-many (one group → multiple roles) creates ambiguity at resolution. Many-to-one (multiple groups → one role) is the simpler, less-error-prone shape.</p>\n<p><strong>Why tenant_admin (not engagement_admin) for IdP config (§1 #7, DEC-882)?</strong> IdP misconfig has cross-Engagement blast radius — a wrong cert at Engagement A doesn't break Engagement A alone; it can cause cascading auth failures across the tenant if the misconfig hits a shared cert store. Tenant_admin is the smallest scope that owns the blast radius.</p>\n<p><strong>Why 7 audit kinds with sev-1 weighting on config events (§1 #20, DEC-873)?</strong> Identity-system config changes are forensically critical. Sev-1 ensures they appear in compliance dashboards (FR-OBS-008) and trigger immediate operator review. Sign-in events are sev-2 (high-volume but important); transient sign-in failures are sev-2 (security signal); SCIM user updates are sev-3 (high-volume routine).</p>\n<p><strong>Why SP-metadata published at a per-Engagement URL (§1 #10, DEC-874)?</strong> IdP admins (Okta, Azure) self-configure by entering an SP metadata URL — they paste the URL, IdP fetches + parses. Per-Engagement URLs let each Engagement's IdP admin do this without operator hand-holding. Single global SP metadata wouldn't carry per-Engagement AssertionConsumerService URLs.</p>\n<p><strong>Why drop unsigned attributes silently into the audit rather than fail the sign-in (§1 #15)?</strong> Failing the sign-in on unsigned attributes would break IdPs that bundle some attributes unsigned (rare but real). The audit row surfaces the violation to operators; recurring violations from one IdP are a config issue to escalate. Mid-incident customer-facing breakage from an over-strict rejection is worse than the audit-row residue.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0001_portal_idp_configs.sql\nCREATE TYPE portal_idp_kind AS ENUM ('saml','oidc');\n\nCREATE TABLE portal_idp_configs (\n  id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  idp_kind portal_idp_kind NOT NULL,\n  idp_name TEXT NOT NULL,\n  idp_entity_id TEXT NOT NULL,\n  idp_metadata_url TEXT,\n  idp_signing_cert_kms_blob BYTEA NOT NULL,\n  idp_signing_cert_thumbprint TEXT NOT NULL,\n  idp_kms_key_id TEXT NOT NULL,\n  enforcement TEXT NOT NULL DEFAULT 'optional'\n    CHECK (enforcement IN ('optional','required')),\n  email_domain_hint TEXT,\n  allow_idp_initiated BOOLEAN NOT NULL DEFAULT false,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  rotated_at TIMESTAMPTZ,\n  status TEXT NOT NULL DEFAULT 'active'\n    CHECK (status IN ('active','rotated','revoked'))\n);\nCREATE UNIQUE INDEX uniq_idp_active_per_engagement\n  ON portal_idp_configs(tenant_id, engagement_id)\n  WHERE status='active';\nALTER TABLE portal_idp_configs ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_idp_configs_rls ON portal_idp_configs\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_idp_configs FROM cyberos_app;\nGRANT UPDATE (rotated_at, status) ON portal_idp_configs TO cyberos_app;\n\n-- 0002_portal_scim_audit_log.sql\nCREATE TABLE portal_scim_audit_log (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  scim_operation TEXT NOT NULL\n    CHECK (scim_operation IN ('user_create','user_update','user_delete','group_create','group_update','group_delete','token_rotated')),\n  external_id TEXT,\n  subject_id UUID,\n  request_sha256 CHAR(64) NOT NULL,\n  response_status INT NOT NULL,\n  trace_id CHAR(32),\n  ts TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nALTER TABLE portal_scim_audit_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_scim_audit_log_rls ON portal_scim_audit_log\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_scim_audit_log FROM cyberos_app;\n\n-- 0003_portal_idp_groups_map.sql\nCREATE TABLE portal_idp_groups_map (\n  idp_config_id UUID NOT NULL REFERENCES portal_idp_configs(id),\n  idp_group_name TEXT NOT NULL,\n  cyberos_role TEXT NOT NULL,\n  PRIMARY KEY (idp_config_id, idp_group_name)\n);\nALTER TABLE portal_idp_groups_map ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_idp_groups_map_rls ON portal_idp_groups_map\n  USING (idp_config_id IN (SELECT id FROM portal_idp_configs\n                            WHERE tenant_id = current_setting('auth.tenant_id')::uuid))\n  WITH CHECK (idp_config_id IN (SELECT id FROM portal_idp_configs\n                                  WHERE tenant_id = current_setting('auth.tenant_id')::uuid));\n\n-- 0004_portal_scim_tokens.sql\nCREATE TABLE portal_scim_tokens (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  token_sha256 CHAR(64) NOT NULL,\n  kms_key_id TEXT NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  rotated_at TIMESTAMPTZ,\n  status TEXT NOT NULL DEFAULT 'active'\n    CHECK (status IN ('active','rotated','revoked'))\n);\nCREATE UNIQUE INDEX uniq_active_scim_token\n  ON portal_scim_tokens(engagement_id) WHERE status='active';\nCREATE INDEX idx_scim_token_sha256 ON portal_scim_tokens(token_sha256);\nALTER TABLE portal_scim_tokens ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_scim_tokens_rls ON portal_scim_tokens\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_scim_tokens FROM cyberos_app;\nGRANT UPDATE (rotated_at, status) ON portal_scim_tokens TO cyberos_app;</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/portal/src/idp/mod.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug, sqlx::Type)]\npub enum PortalIdpKind { Saml, Oidc }\n\n#[derive(Debug)]\npub struct IdpConfig {\n    pub id: uuid::Uuid,\n    pub tenant_id: uuid::Uuid,\n    pub engagement_id: uuid::Uuid,\n    pub kind: PortalIdpKind,\n    pub entity_id: String,\n    pub signing_cert: openssl::x509::X509,  // KMS-decrypted at load\n    pub signing_cert_thumbprint: String,\n    pub enforcement: Enforcement,\n    pub allow_idp_initiated: bool,\n}\n\n#[derive(Copy, Clone, Eq, PartialEq, Debug)]\npub enum Enforcement { Optional, Required }\n\n#[derive(Debug)]\npub struct JitClaims {\n    pub external_id: String,    // SAML NameID or OIDC sub\n    pub email: String,\n    pub display_name: Option&lt;String&gt;,\n    pub groups: Vec&lt;String&gt;,\n    pub trace_id: String,\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\"># SAML SP\nPOST   /saml/v2/{engagement_slug}/acs               (IdP→SP Assertion Consumer)\nGET    /saml/v2/{engagement_slug}/metadata           (public SP metadata)\n\n# OIDC RP\nGET    /oidc/v1/{engagement_slug}/.well-known/openid-configuration  (public)\nPOST   /oidc/v1/{engagement_slug}/callback           (OIDC return)\n\n# Discovery\nGET    /v1/portal/sign-in                            (email-domain hint)\n\n# Admin (tenant_admin only)\nPOST   /v1/admin/engagements/{id}/idp                (create IdP config)\nPATCH  /v1/admin/engagements/{id}/idp                (rotate signing cert)\nPOST   /v1/admin/engagements/{id}/scim-token/rotate  (quarterly rotation)\nPOST   /v1/admin/engagements/{id}/idp/groups-map     (configure claim → role)\n\n# SCIM 2.0 (bearer token)\n{POST,GET,PATCH,PUT,DELETE} /scim/v2/{engagement_slug}/Users[/{id}]\n{POST,GET,PATCH,PUT,DELETE} /scim/v2/{engagement_slug}/Groups[/{id}]</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>SAML happy path</strong> — POST signed SAML Response → JIT subject created with claim-derived role → JWT minted with 8h TTL → <code>portal.idp_sign_in</code> memory row emitted.</li><li><strong>OIDC happy path</strong> — OIDC callback with valid ID token → JIT subject created → role from <code>groups</code> claim → 8h JWT.</li><li><strong>SAML replay window</strong> — Response with <code>NotOnOrAfter</code> 6 min ago → 401 + <code>portal.idp_sign_in_failed</code> with reason='replay_window_exceeded'.</li><li><strong>OIDC iat skew</strong> — ID token with <code>iat</code> 70 s in future → 401 + reason='iat_skew_exceeded'.</li><li><strong>Unsigned attribute dropped</strong> — Response with mixed signed + unsigned attributes → only signed in JIT claims; <code>portal.signed_attr_trust_violation</code> emitted; sign-in still succeeds.</li><li><strong>SCIM externalId idempotency</strong> — second SCIM CREATE with same externalId returns 409 + existing user id; no duplicate subject.</li><li><strong>portal_idp_kind cardinality</strong> — <code>idp_kind_enum_cardinality_test</code> asserts enum = exactly <code>{saml, oidc}</code>.</li><li><strong>Per-Engagement isolation</strong> — Engagement A's IdP config invisible to Engagement B; cross-Engagement lookup returns 404.</li><li><strong>Required enforcement disables password</strong> — user in <code>required</code>-enforcement Engagement attempts FR-AUTH-004 password login → 403 + <code>sso_required</code> + IdP redirect URL.</li><li><strong>8h re-auth trigger</strong> — subject with <code>last_sso_at</code> &gt; 8h ago hitting sensitive endpoint → redirect to IdP.</li><li><strong>Email-domain discovery hint</strong> — <code>/v1/portal/sign-in?email=alice@acme.com</code> where <code>acme.com</code> matches <code>email_domain_hint</code> → redirect_url returned.</li><li><strong>SCIM token rotation 60s overlap</strong> — both old + new token accepted for 60 s post-rotation; <code>portal.scim_token_rotation</code> emitted.</li><li><strong>Claim → role many-to-one</strong> — IdP groups <code>[\"Sales\",\"Marketing\"]</code> both mapped to <code>client_viewer</code> → JIT user gets <code>client_viewer</code> (single role, no conflict).</li><li><strong>Unrecognised group ignored</strong> — IdP claim group <code>Random</code> not in <code>portal_idp_groups_map</code> → default <code>client_viewer</code> role assigned (no elevation).</li><li><strong>tenant_admin role gate on IdP config</strong> — engagement_admin POSTing <code>/v1/admin/engagements/{id}/idp</code> → 403; tenant_admin → 201.</li><li><strong>PII scrubbed in audit rows</strong> — <code>portal.idp_sign_in</code> row carries <code>email_hash16</code> not raw email.</li><li><strong>W3C trace_id threaded</strong> — single trace_id present in SAML ACS span → JIT span → SCIM audit log row → memory row.</li><li><strong>IdP-initiated SAML allowed only when opted in</strong> — Response lacking InResponseTo with <code>allow_idp_initiated=false</code> → 401; with <code>true</code> → accepted.</li><li><strong>SCIM token in URL rejected</strong> — <code>?token=xxx</code> query param ignored; Authorization header required.</li><li><strong>IdP signing cert KMS-wrapped</strong> — <code>portal_idp_configs.idp_signing_cert_kms_blob</code> decryptable only via the configured KMS key; raw cert never written to disk.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>saml_happy_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn saml_signed_response_jit_provisions_subject() {\n    let ctx = TestContext::with_engagement_idp(\"eng-acme\", IdpKind::Saml).await;\n    let saml_response = ctx.fake_idp.signed_response(\"alice@acme.com\",\n        vec![(\"groups\", vec![\"Engineering\"])]).await;\n    let r = ctx.post(&amp;format!(\"/saml/v2/eng-acme/acs\")).body(saml_response).send().await.unwrap();\n    assert_eq!(r.status(), 302);  // redirect with JWT cookie set\n\n    let subj = ctx.repo.subjects.find_by_external_id(ctx.tenant_id, \"alice@acme.com\").await.unwrap();\n    assert_eq!(subj.auth_method, \"external_idp\");\n    assert_eq!(subj.role, \"client_viewer\");  // default; no group mapping yet\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.idp_sign_in\"));\n    assert!(audit.iter().any(|r| r.kind == \"portal.scim_user_created\"));\n}</code></pre>\n<h3>5.2 <code>saml_unsigned_attr_dropped_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn unsigned_attribute_dropped_and_audited() {\n    let ctx = TestContext::with_engagement_idp(\"eng-acme\", IdpKind::Saml).await;\n    let saml_response = ctx.fake_idp.signed_response_with_unsigned_attrs(\"alice@acme.com\",\n        signed=vec![(\"groups\", vec![\"Engineering\"])],\n        unsigned=vec![(\"role_override\", vec![\"super_admin\"])]).await;\n\n    let r = ctx.post(&amp;format!(\"/saml/v2/eng-acme/acs\")).body(saml_response).send().await.unwrap();\n    assert_eq!(r.status(), 302);  // sign-in still succeeds\n\n    let subj = ctx.repo.subjects.find_by_external_id(ctx.tenant_id, \"alice@acme.com\").await.unwrap();\n    assert_eq!(subj.role, \"client_viewer\");  // role_override IGNORED\n\n    let audit = ctx.memory_rows().await;\n    let violation = audit.iter().find(|r| r.kind == \"portal.signed_attr_trust_violation\").unwrap();\n    assert_eq!(violation.payload[\"attribute_name\"], \"role_override\");\n}</code></pre>\n<h3>5.3 <code>oidc_iat_skew_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn oidc_iat_in_future_rejected() {\n    let ctx = TestContext::with_engagement_idp(\"eng-acme\", IdpKind::Oidc).await;\n    let id_token = ctx.fake_idp.id_token_with_iat_offset(\"alice@acme.com\",\n        chrono::Duration::seconds(70)).await;\n    let r = ctx.post(&amp;format!(\"/oidc/v1/eng-acme/callback\"))\n        .json(&amp;json!({\"id_token\": id_token, \"state\": ctx.valid_state()})).send().await.unwrap();\n    assert_eq!(r.status(), 401);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"iat_skew_exceeded\");\n}</code></pre>\n<h3>5.4 <code>scim_create_idempotency_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn scim_create_idempotent_on_external_id() {\n    let ctx = TestContext::with_scim_token(\"eng-acme\").await;\n    let body = json!({\"externalId\": \"okta-12345\", \"userName\": \"alice\",\n                       \"emails\": [{\"value\": \"alice@acme.com\", \"primary\": true}]});\n\n    let r1 = ctx.scim_post(\"/scim/v2/eng-acme/Users\", &amp;body).await;\n    assert_eq!(r1.status(), 201);\n    let id1: String = r1.json::&lt;serde_json::Value&gt;().await.unwrap()[\"id\"].as_str().unwrap().into();\n\n    let r2 = ctx.scim_post(\"/scim/v2/eng-acme/Users\", &amp;body).await;\n    assert_eq!(r2.status(), 409);\n    let body2: serde_json::Value = r2.json().await.unwrap();\n    assert_eq!(body2[\"existing_id\"], id1);\n}</code></pre>\n<h3>5.5 <code>claim_to_role_mapping_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn many_to_one_group_mapping() {\n    let ctx = TestContext::with_engagement_idp(\"eng-acme\", IdpKind::Saml).await;\n    ctx.configure_group_map(vec![\n        (\"Sales\", \"client_viewer\"),\n        (\"Marketing\", \"client_viewer\"),\n        (\"Engineering\", \"client_editor\"),\n    ]).await;\n\n    let saml = ctx.fake_idp.signed_response(\"alice@acme.com\",\n        vec![(\"groups\", vec![\"Sales\",\"Marketing\"])]).await;\n    ctx.post(&amp;format!(\"/saml/v2/eng-acme/acs\")).body(saml).send().await.unwrap();\n    let subj = ctx.repo.subjects.find_by_external_id(ctx.tenant_id, \"alice@acme.com\").await.unwrap();\n    assert_eq!(subj.role, \"client_viewer\");  // both groups map same role\n\n    let bob_saml = ctx.fake_idp.signed_response(\"bob@acme.com\",\n        vec![(\"groups\", vec![\"Sales\",\"Engineering\"])]).await;\n    ctx.post(&amp;format!(\"/saml/v2/eng-acme/acs\")).body(bob_saml).send().await.unwrap();\n    let bob = ctx.repo.subjects.find_by_external_id(ctx.tenant_id, \"bob@acme.com\").await.unwrap();\n    assert_eq!(bob.role, \"client_editor\");  // highest-privilege wins\n}</code></pre>\n<h3>5.6 <code>sso_enforcement_required_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn required_enforcement_blocks_password_login() {\n    let ctx = TestContext::with_engagement_idp_enforcement(\"eng-acme\", Enforcement::Required).await;\n    let subj = ctx.jit_provision_via_saml(\"alice@acme.com\").await;\n\n    let r = ctx.post(\"/v1/auth/login\").json(&amp;json!({\n        \"email\": \"alice@acme.com\", \"password\": \"any-password\",\n    })).send().await.unwrap();\n    assert_eq!(r.status(), 403);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"sso_required\");\n    assert!(body[\"idp_redirect_url\"].as_str().unwrap().contains(\"/sso/eng-acme\"));\n}</code></pre>\n<h3>5.7 <code>8h_re_auth_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn last_sso_older_than_8h_triggers_re_auth() {\n    let ctx = TestContext::with_engagement_idp_enforcement(\"eng-acme\", Enforcement::Required).await;\n    let subj = ctx.jit_provision_via_saml(\"alice@acme.com\").await;\n    sqlx::query(\"UPDATE subjects SET last_sso_at = now() - interval '9 hours' WHERE id=$1\")\n        .bind(subj.id).execute(&amp;ctx.pool).await.unwrap();\n\n    let jwt = ctx.mint_jwt_for(subj.id).await;\n    let r = ctx.get(\"/v1/portal/sensitive-resource\").bearer_auth(jwt).send().await.unwrap();\n    assert_eq!(r.status(), 401);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"sso_re_auth_required\");\n}</code></pre>\n<h3>5.8 <code>scim_token_rotation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn scim_token_rotation_60s_overlap() {\n    let ctx = TestContext::with_scim_token(\"eng-acme\").await;\n    let old_token = ctx.current_scim_token().await;\n\n    let new_token = ctx.rotate_scim_token(\"eng-acme\").await;\n    for token in [&amp;old_token, &amp;new_token] {\n        let r = ctx.scim_post_with_token(\"/scim/v2/eng-acme/Users\",\n            &amp;json!({\"externalId\": format!(\"u-{}\", uuid::Uuid::new_v4()), \"userName\": \"x\"}), token).await;\n        assert!(r.status().is_success(), \"token {token} failed during overlap\");\n    }\n\n    tokio::time::sleep(Duration::from_secs(61)).await;\n    let r_old = ctx.scim_post_with_token(\"/scim/v2/eng-acme/Users\",\n        &amp;json!({\"externalId\": format!(\"u-{}\", uuid::Uuid::new_v4()), \"userName\": \"y\"}), &amp;old_token).await;\n    assert_eq!(r_old.status(), 401);\n}</code></pre>\n<h3>5.9 <code>per_engagement_isolation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn engagement_a_idp_invisible_to_engagement_b() {\n    let ctx = TestContext::with_engagements(vec![\"eng-a\", \"eng-b\"]).await;\n    ctx.configure_idp(\"eng-a\", IdpKind::Saml).await;\n\n    let r = ctx.get(\"/saml/v2/eng-b/metadata\").send().await.unwrap();\n    assert_eq!(r.status(), 404);\n}</code></pre>\n<h3>5.10 <code>idp_kind_enum_cardinality_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn portal_idp_kind_has_exactly_two_values() {\n    let ctx = TestContext::new().await;\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::portal_idp_kind))::text\"\n    ).fetch_all(&amp;ctx.pool).await.unwrap();\n    let mut labels = labels;\n    labels.sort();\n    assert_eq!(labels, vec![\"oidc\".to_string(), \"saml\".to_string()]);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract in §3 is the skeleton. Additional notes below.)</p>\n<h3>6.1 SAML SP Assertion Consumer (<code>services/portal/src/idp/saml.rs</code>)</h3>\n<pre><code class=\"language-rust\">pub async fn acs(ctx: AppCtx, eng_slug: String, body: Bytes) -&gt; Result&lt;Response, PortalError&gt; {\n    let config = ctx.repo.idp_configs.find_active_by_engagement_slug(&amp;eng_slug).await?\n        .ok_or(PortalError::NoActiveIdpConfig)?;\n    if config.kind != PortalIdpKind::Saml { return Err(PortalError::WrongIdpKind); }\n\n    let response_xml = saml::parse_xml(&amp;body)?;\n    saml::verify_response_signature(&amp;response_xml, &amp;config.signing_cert)?;\n    let in_response_to = saml::extract_in_response_to(&amp;response_xml);\n    if !config.allow_idp_initiated &amp;&amp; in_response_to.is_none() {\n        return Err(PortalError::IdpInitiatedNotAllowed);\n    }\n    if let Some(req_id) = &amp;in_response_to {\n        ctx.repo.saml_pending_requests.consume_one_shot(req_id).await?;\n    }\n    saml::verify_replay_window(&amp;response_xml, Duration::minutes(5))?;\n\n    let signed_attrs = signed_attr::extract_signed_attributes_only(&amp;response_xml, &amp;config.signing_cert);\n    let claims = JitClaims::from_saml_signed_attrs(&amp;signed_attrs)?;\n\n    let subject_id = jit_provision_or_update(&amp;ctx, &amp;config, &amp;claims).await?;\n    let jwt = ctx.auth.mint_jwt_for_external_idp(subject_id, config.tenant_id, &amp;claims).await?;\n    emit_audit(&amp;ctx, \"portal.idp_sign_in\", json!({\n        \"engagement_id\": config.engagement_id, \"subject_id\": subject_id,\n        \"external_id_hash16\": hash16(&amp;claims.external_id), \"idp_kind\": \"saml\",\n    })).await;\n\n    Ok(redirect_response(format!(\"https://portal.cyberos.world/onboarding?token={jwt}\")))\n}</code></pre>\n<h3>6.2 SCIM bearer-token middleware (<code>services/portal/src/scim/token_auth.rs</code>)</h3>\n<pre><code class=\"language-rust\">pub async fn scim_bearer_auth&lt;B&gt;(\n    ctx: AppCtx, eng_slug: String, req: Request&lt;B&gt;, next: Next&lt;B&gt;\n) -&gt; Result&lt;Response, StatusCode&gt; {\n    let token = req.headers().get(\"Authorization\")\n        .and_then(|h| h.to_str().ok())\n        .and_then(|h| h.strip_prefix(\"Bearer \"))\n        .ok_or(StatusCode::UNAUTHORIZED)?;\n    let token_sha256 = sha256_hex(token.as_bytes());\n    let valid = ctx.repo.scim_tokens.find_by_sha256_active_or_overlap(&amp;token_sha256, &amp;eng_slug).await\n        .map_err(|_| StatusCode::INTERNAL_SERVER_ERROR)?;\n    if !valid { return Err(StatusCode::UNAUTHORIZED); }\n    Ok(next.run(req).await)\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>FR-AUTH-103</strong> SAML SSO — SAML protocol primitives (Response parse, Assertion verify, cert chain).</li><li><strong>FR-AUTH-104</strong> OIDC SSO — OIDC RP primitives (ID token verify, JWKS rotation, PKCE).</li></ul>\n<p><strong>Cross-module (related_frs):</strong></p>\n<ul><li><strong>FR-AUTH-002</strong> Subject create — JIT calls into this.</li><li><strong>FR-AUTH-004</strong> JWT mint — first-login token after SSO.</li><li><strong>FR-AUTH-101</strong> RBAC — Engagement membership grants.</li><li><strong>FR-PORTAL-001</strong> Scoped read-only views — depends on subjects existing.</li><li><strong>FR-PORTAL-002</strong> Per-tenant brand pack — sign-in page brand applied.</li><li><strong>FR-PORTAL-004</strong> SCIM deprovision — full session-invalidation on SCIM DELETE.</li><li><strong>FR-PORTAL-005</strong> Branded Genie chat — uses external-IdP scope_grants.</li><li><strong>FR-TEN-101</strong> Self-serve signup — root-admin can configure IdP post-signup.</li><li><strong>FR-AI-003</strong> memory audit-row bridge — 7 + 1 supporting kinds register.</li><li><strong>FR-MEMORY-111</strong> PII scrubbing — email/name/external_id hashes.</li><li><strong>FR-OBS-007</strong> Auto-runbook — sev-1 IdP-config events trigger CHAT alerts.</li></ul>\n<p><strong>Downstream (blocks):</strong></p>\n<ul><li><strong>FR-PORTAL-004</strong> SCIM deprovision — needs SCIM endpoints + session linkage.</li><li><strong>FR-PORTAL-005</strong> Branded Genie — needs IdP-auth scope_grants.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>portal.idp_sign_in</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.idp_sign_in\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.portal.idp\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"engagement_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n    \"subject_id\": \"7c4e9a2b-1d3f-4e6a-bccc-000000000042\",\n    \"idp_kind\": \"saml\",\n    \"external_id_hash16\": \"f8a1b2c3d4e5f607\",\n    \"email_hash16\": \"9c4e7a8b6d2f1e3a\",\n    \"groups_count\": 2,\n    \"role_assigned\": \"client_viewer\"\n  }\n}</code></pre>\n<h3>8.2 SCIM Create request/response</h3>\n<pre><code class=\"language-json\">// POST /scim/v2/eng-acme/Users\n{\n  \"schemas\": [\"urn:ietf:params:scim:schemas:core:2.0:User\"],\n  \"externalId\": \"okta-12345\",\n  \"userName\": \"alice\",\n  \"emails\": [{\"value\": \"alice@acme.com\", \"primary\": true}],\n  \"name\": {\"givenName\": \"Alice\", \"familyName\": \"Smith\"},\n  \"groups\": [{\"value\": \"Engineering\"}]\n}\n\n// 201 response\n{\n  \"schemas\": [\"urn:ietf:params:scim:schemas:core:2.0:User\"],\n  \"id\": \"7c4e9a2b-1d3f-4e6a-bccc-000000000042\",\n  \"externalId\": \"okta-12345\",\n  \"userName\": \"alice\",\n  \"emails\": [{\"value\": \"alice@acme.com\", \"primary\": true}],\n  \"meta\": {\n    \"resourceType\": \"User\",\n    \"created\": \"2026-05-17T09:14:32.847Z\",\n    \"location\": \"https://portal.cyberos.world/scim/v2/eng-acme/Users/7c4e9a2b-...\"\n  }\n}</code></pre>\n<h3>8.3 IdP config admin POST</h3>\n<pre><code class=\"language-json\">{\n  \"idp_kind\": \"saml\",\n  \"idp_name\": \"Acme Corp Okta\",\n  \"idp_entity_id\": \"https://acme.okta.com/saml2/acme\",\n  \"idp_metadata_url\": \"https://acme.okta.com/app/abc123/sso/saml/metadata\",\n  \"enforcement\": \"required\",\n  \"email_domain_hint\": \"acme.com\",\n  \"allow_idp_initiated\": false\n}</code></pre>\n<h3>8.4 <code>portal.signed_attr_trust_violation</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.signed_attr_trust_violation\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.portal.idp\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.901Z\",\n  \"payload\": {\n    \"engagement_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n    \"idp_entity_id\": \"https://acme.okta.com/saml2/acme\",\n    \"attribute_name\": \"role_override\",\n    \"value_hash16\": \"a1b2c3d4e5f607c8\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 1. Deferred:</p>\n<ul><li><strong>Deferred:</strong> SCIM 2.0 Group sync DELETE → cascade subject-membership revoke — slice 2, FR-PORTAL-004 path.</li><li><strong>Deferred:</strong> eIDAS QES (Qualified Electronic Signature) integration for EU regulated tenants — slice 3, FR-AUTH-2xx (placeholder).</li><li><strong>Deferred:</strong> Multi-IdP per Engagement (e.g., Okta primary + Google fallback) — slice 2.</li><li><strong>Deferred:</strong> SCIM bulk operations (RFC 7644 §3.7) — slice 2 (single-user CRUD only at slice 1).</li><li><strong>Deferred:</strong> Custom claim mappers for non-<code>groups</code> claim sources (e.g., role inferred from <code>department</code>) — slice 2.</li><li><strong>Deferred:</strong> WebAuthn step-up at sensitive ops post-SSO (vs full re-auth) — slice 2, FR-AUTH-2xx.</li><li><strong>Deferred:</strong> Per-Engagement custom domain for SP endpoint (<code>sso.acme.com/saml2/acs</code>) — slice 2 + FR-PORTAL-002 brand pack integration.</li><li><strong>Deferred:</strong> SAML SLO (Single Log-Out) — slice 2; slice 1 has SCIM DELETE deprovision via FR-PORTAL-004.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>SAML Response signature invalid</td><td>xmldsig verify returns false</td><td>401 + <code>signature_invalid</code> + <code>portal.idp_sign_in_failed</code> sev-2</td><td>IdP cert rotated without our config update — admin re-imports IdP metadata</td></tr><tr><td>SAML Response InResponseTo missing (SP-initiated only flow)</td><td>absence of <code>InResponseTo</code> element</td><td>401 + <code>idp_initiated_not_allowed</code> unless <code>allow_idp_initiated=true</code></td><td>Admin opts in via config update OR user retries SP-initiated flow</td></tr><tr><td>SAML Response replay window exceeded</td><td><code>NotOnOrAfter &lt; now - 5min</code></td><td>401 + <code>replay_window_exceeded</code> + <code>portal.idp_sign_in_failed</code></td><td>User retries; clock-skew investigation if recurring</td></tr><tr><td>SAML Assertion unsigned attribute present</td><td>signed_attr.rs walk finds attr outside Signature scope</td><td>Attribute dropped silently; <code>portal.signed_attr_trust_violation</code> sev-1 emitted; sign-in still proceeds with signed-only attrs</td><td>Operator alerted; IdP-side admin reconfigures to sign all attrs</td></tr><tr><td>OIDC ID token signature invalid</td><td>JWKS verify fails</td><td>401 + <code>id_token_invalid</code> + <code>portal.idp_sign_in_failed</code></td><td>JWKS rotation drift — cache refresh triggered (24h max stale per FR-AUTH-104)</td></tr><tr><td>OIDC ID token iat skew exceeded</td><td>`</td><td>now - iat</td><td>&gt; 60s`</td><td>401 + <code>iat_skew_exceeded</code></td><td>User retries; if recurring, NTP investigation</td></tr><tr><td>OIDC state nonce mismatch</td><td>server-side <code>state</code> lookup miss</td><td>401 + <code>state_nonce_invalid</code></td><td>User restarts sign-in (CSRF protection working as intended)</td></tr><tr><td>OIDC PKCE code_verifier mismatch</td><td>hash compare fails</td><td>401 + <code>pkce_verify_failed</code></td><td>User restarts sign-in</td></tr><tr><td>SCIM bearer token invalid/expired</td><td>token_sha256 not in active or overlap set</td><td>401 + <code>WWW-Authenticate: Bearer error=\"invalid_token\"</code></td><td>IdP-side SCIM admin updates token; rotation flow used if mid-rotation</td></tr><tr><td>SCIM externalId collision</td><td>unique constraint hit on <code>subjects(tenant_id, engagement_id, external_id)</code></td><td>409 + <code>{detail, existing_id}</code></td><td>IdP-side admin investigates duplicate-user state</td></tr><tr><td>SCIM bulk DELETE attempted (not supported slice 1)</td><td>endpoint match</td><td>405 + <code>bulk_not_supported_slice_1</code></td><td>Use individual DELETE per RFC 7644 §3.6</td></tr><tr><td>IdP signing cert expired (validation fails)</td><td>X.509 NotAfter check</td><td>401 + <code>idp_cert_expired</code> + sev-1 alert <code>portal.idp_cert_expired</code></td><td>Tenant admin invokes IdP config rotation (<code>PATCH /v1/admin/engagements/{id}/idp</code>)</td></tr><tr><td>IdP metadata URL unreachable (during config create)</td><td>fetch timeout 10 s</td><td>503 + <code>idp_metadata_fetch_failed</code></td><td>Admin uploads cert manually instead OR retries when IdP-side network recovers</td></tr><tr><td>Per-Engagement IdP config inactive (status=revoked)</td><td>lookup filter <code>WHERE status='active'</code> returns nothing</td><td>404 + <code>no_active_idp_config</code></td><td>Admin re-activates or re-creates config</td></tr><tr><td><code>enforcement=required</code> user attempts password login</td><td>login handler check</td><td>403 + <code>sso_required</code> + IdP redirect URL</td><td>User follows redirect to IdP</td></tr><tr><td>8h re-auth required post-sensitive-op</td><td>last_sso_at check at handler entry</td><td>401 + <code>sso_re_auth_required</code> + IdP redirect URL</td><td>User completes IdP re-auth + retries</td></tr><tr><td>SCIM token rotation overlap expired before old IdP-side updated</td><td>old token used past 60s overlap</td><td>401 + <code>token_rotation_expired</code></td><td>Tenant admin re-rotates with longer notice OR rolls back to old token (revoked status)</td></tr><tr><td>Claim → role map missing for group present in IdP claim</td><td>lookup returns no match</td><td>Group silently ignored; default <code>client_viewer</code> role assigned</td><td>Admin configures <code>portal_idp_groups_map</code> entry</td></tr><tr><td>Cross-Engagement RLS bypass attempt</td><td>RLS policy USING clause rejects</td><td>404 (no rows returned; appears as not-found)</td><td>Inherent — RLS prevents enumeration</td></tr><tr><td>SAML XML signature wrap attack (XSW)</td><td>XML canonical form check + signature reference validation</td><td>401 + <code>xsw_detected</code> + sev-1 alert</td><td>Inherent — proper SAML lib (libxmlsec) prevents; logged for forensic</td></tr><tr><td>OIDC ID token aud mismatch (token for different RP)</td><td>aud claim check</td><td>401 + <code>aud_mismatch</code></td><td>User signs in via correct portal URL</td></tr><tr><td>Unrecognised IdP entity_id (Response from different IdP than configured)</td><td>issuer check</td><td>401 + <code>unexpected_issuer</code></td><td>Admin verifies IdP config; possible cert reuse across IdPs (bad practice)</td></tr><tr><td>KMS unavailable when loading idp_signing_cert</td><td>KMS decrypt timeout</td><td>503 + sev-2 alert <code>portal.kms_unavailable</code>; sign-in retries</td><td>KMS recovers; admin verifies IAM permissions</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> SAML SP implementation uses <code>samael</code> crate (active maintenance, libxmlsec bindings). OIDC RP uses <code>openidconnect</code> crate.</p>\n<p><strong>§11.2</strong> SCIM 2.0 conformance: implement Levels 1-2 per RFC 7644 (Users + Groups CRUD + idempotency); Level 3 (bulk, filter expressions) deferred to slice 2.</p>\n<p><strong>§11.3</strong> SCIM Filter expressions (RFC 7644 §3.4.2.2) for GET supported subset: <code>eq, ne, sw, ew, co, pr, and, or</code> — sufficient for most Okta/Azure use cases. Full grammar at slice 2.</p>\n<p><strong>§11.4</strong> IdP signing cert thumbprint = SHA-256 of DER-encoded cert (uppercase hex with colon separators, common convention).</p>\n<p><strong>§11.5</strong> Per-Engagement metadata URL endpoint is unauthenticated (per SAML/OIDC convention — metadata is public).</p>\n<p><strong>§11.6</strong> SCIM <code>meta.location</code> URL points to the resource's canonical URL; required by RFC 7644 §3.1.</p>\n<p><strong>§11.7</strong> Bearer-token format for SCIM is opaque (not JWT) — 32 random bytes base64url-encoded. Length 43 chars. Easy to rotate.</p>\n<p><strong>§11.8</strong> The <code>subjects.last_sso_at</code> column requires a migration to FR-AUTH-002's table — added via <code>services/auth/migrations/0XXX_subjects_last_sso_at.sql</code> referenced by this FR's modified_files.</p>\n<p><strong>§11.9</strong> <code>signed_attr.rs</code> is the security-critical core. Test coverage MUST include: signed-only attrs (happy), mixed signed+unsigned (drop unsigned), nested Assertions, attributes outside enclosed Signature scope. Library: <code>xmlsec-rs</code> with libxmlsec bindings.</p>\n<p><strong>§11.10</strong> SCIM <code>PATCH</code> uses JSON Patch (RFC 6902-ish) per RFC 7644 §3.5.2; partial updates avoid full-replace race conditions.</p>\n<p><strong>§11.11</strong> Per DEC-885, role ordinal: <code>client_viewer &lt; client_editor &lt; client_admin</code>. Resolver picks max.</p>\n<p><strong>§11.12</strong> The SP-metadata endpoint serves XML; Content-Type <code>application/samlmetadata+xml</code> per OASIS.</p>\n<p><strong>§11.13</strong> Per-Engagement OIDC metadata at <code>/oidc/v1/{eng}/.well-known/openid-configuration</code> differs from per-Tenant OIDC of FR-AUTH-104 — PORTAL is the RP-facing-OUTWARD endpoint (IdP-side configures against this), while FR-AUTH-104 is the AUTH server-as-RP for internal SSO.</p>\n<p><strong>§11.14</strong> <code>portal_scim_tokens</code> query path uses <code>idx_scim_token_sha256</code> for O(log n) lookup; bearer-auth middleware caches accepted-token results for 5 s to avoid hot-path DB hit (cache invalidated on rotation events).</p>\n<p><strong>§11.15</strong> Force re-auth (§1 #17) is implemented as middleware on routes tagged with <code>requires_fresh_sso</code> attribute; the JWT bearer middleware reads <code>subject.last_sso_at</code> and decides.</p>\n<p><strong>§11.16</strong> IdP metadata URL fetch (during config create) uses a 10s timeout + content-length cap of 1 MiB to defend against slow-loris + size-amplification attacks.</p>\n<p><strong>§11.17</strong> PII hashing: <code>email_hash16 = encode(substring(digest(coalesce(lower(email), '') || global_salt, 'sha256') from 1 for 8), 'hex')</code> — consistent with FR-TEN-101's hash function. Global salt rotates yearly; old hashes retained for forensic comparison.</p>\n<p><strong>§11.18</strong> SAML <code>NameID</code> formats supported: <code>emailAddress</code>, <code>persistent</code>, <code>transient</code> (per OASIS Section 8.3). The <code>external_id</code> field stores the NameID value AS-IS; format is informational.</p>\n<p><strong>§11.19</strong> OIDC <code>sub</code> claim stability: per OpenID Connect Core 1.0 §5.5, <code>sub</code> is stable per (issuer, audience). We treat <code>sub</code> as the canonical external_id and ignore claim instability.</p>\n<p><strong>§11.20</strong> The <code>cyberos_app</code> UPDATE grants on <code>portal_idp_configs</code> (rotated_at, status) and <code>portal_scim_tokens</code> (rotated_at, status) are intentionally narrow — only the rotation handler can update these columns.</p>\n<p><strong>§11.21</strong> Per-Engagement test fixtures use <code>wiremock</code> to simulate IdP endpoints; integration tests against real Okta/Azure dev tenants run nightly (not on every PR) to validate protocol-level conformance.</p>\n<p><strong>§11.22</strong> The 7-kind core list (§1 #20) + 1 supporting (<code>portal.scim_token_rotation</code>) totals 8 memory audit kinds; FR-AI-003 closed-set extension adds all 8.</p>\n<p><strong>§11.23</strong> Trace_id in memory rows uses the OTel TraceId Display form (32-char lower-hex) per feature-request-audit skill rule 24; never Debug form.</p>\n<p><strong>§11.24</strong> Slice-2 enhancement: emit OTel histogram per-IdP <code>portal_idp_sign_in_duration_seconds_by_idp_entity_id</code> for per-customer latency analysis.</p>\n<p><strong>§11.25</strong> SAML SP-metadata is regenerated on every request (not cached) because per-Engagement SP cert may rotate; cache invalidation complexity &gt; generation cost.</p>\n<p><strong>§11.26</strong> OIDC nonce + state are stored in Redis with 5-min TTL; one-shot consumption + auto-eviction.</p>\n<hr>\n<p><em>End of FR-PORTAL-003 spec.</em></p>";
