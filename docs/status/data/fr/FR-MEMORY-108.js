window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-108"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The memory service <strong>MUST</strong> expose <code>GET /v1/memory/search</code> returning ranked memories. Each request:</p>\n<ol><li><strong>MUST</strong> accept <code>?q=&lt;query&gt;&amp;kind=&lt;filter&gt;&amp;ts_since=&lt;ns&gt;&amp;ts_until=&lt;ns&gt;&amp;limit=&lt;int&gt;&amp;explain=&lt;bool&gt;</code>. Limit default 10, max 100.</li><li><strong>MUST</strong> dispatch to 3 backends in parallel via <code>tokio::try_join!</code>:</li></ol>\n<ul><li><strong>Vector</strong>: BGE-M3 embed query (FR-AI-019) → cosine similarity search in <code>layer2_memories</code> HNSW index.</li><li><strong>Graph</strong>: Cypher query on Apache AGE finding semantically-related entities (within 2 hops).</li><li><strong>Full-text</strong>: PGroonga search with Vietnamese-aware tokenisation (mecab + custom dictionary).</li></ul>\n<ol><li><strong>MUST</strong> combine via Reciprocal Rank Fusion (RRF) with k=60 (standard parameter); take top 50 candidates; rerank via FR-AI-020 BGE-reranker-v2-m3 → final top-N.</li><li><strong>MUST</strong> apply RLS at the DB layer — caller's tenant_id (from JWT) scopes <code>layer2_memories</code> queries via <code>current_setting('app.tenant_id')</code>. RLS USING clause filters; cross-tenant queries return 0 rows.</li><li><strong>MUST</strong> consult <code>meta.acl[]</code> per memory at result-filtering time — if non-empty AND caller's actor_id NOT in list, exclude from results. ACL applied AFTER RLS (defense-in-depth: RLS at DB, ACL at API).</li><li><strong>MUST</strong> return <code>[{id, kind, path, ts_ns, snippet, score, related_count}]</code>. Snippet is a 200-char excerpt around the match (full-text) OR a synthesized summary (vector); <code>related_count</code> = entities linked via graph.</li><li><strong>MUST</strong> complete p95 ≤ 250ms on 1M-chunk tenant fixture (NFR-PERF-01). Above 250ms, OBS sev-3 alarm.</li><li><strong>MUST</strong> fall back gracefully on backend failures:</li></ol>\n<ul><li>BGE sidecar down → vector backend skipped; results from graph + fulltext only.</li><li>AGE query timeout (5s) → graph skipped.</li><li>PGroonga error → fulltext skipped. When all 3 fail, return <code>503 SERVICE_UNAVAILABLE</code>. When at least one succeeds, return results + surface degradation in <code>?explain=true</code> response.</li></ul>\n<ol><li><strong>SHOULD</strong> support <code>?explain=true</code> returning per-backend scores + RRF computation + rerank input/output for debugging.</li><li><strong>MUST</strong> verify <code>chain_anchor</code> for each returned result against Layer 1 (per FR-MEMORY-101 §1 #4). Mismatch → drop the result + emit sev-1 OBS event <code>memory_search_chain_anchor_mismatch{tenant_id, seq}</code>.</li><li><strong>MUST</strong> authenticate via FR-AUTH-004 JWT; extract tenant_id + actor_id from claims.</li><li><strong>MUST</strong> support empty results — return <code>[]</code> with HTTP 200; NOT 404 (search semantics: \"no matches\" is normal, not an error).</li><li><strong>MUST</strong> support multi-language (Vietnamese + English) queries. PGroonga's VN tokenisation handles Vietnamese; BGE-M3 multilingual embedding handles cross-language.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>memory_search_requests_total{tenant_id, outcome}</code> (counter; outcome ∈ ok | partial_degrade | full_failure | empty).</li><li><code>memory_search_latency_ms{tenant_id}</code> (histogram; SLO p95 &lt; 250ms).</li><li><code>memory_search_backend_latency_ms{backend}</code> (histogram per vector/graph/fulltext).</li><li><code>memory_search_chain_anchor_mismatch_total{tenant_id}</code> (counter; sev-1 alarm).</li><li><code>memory_search_acl_filtered_total{tenant_id}</code> (counter; how many results filtered).</li><li><code>memory_search_rerank_improvement</code> (histogram; rerank-position-delta tracking).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why 3-way parallel (DEC-195)?</strong> Each backend has different recall/precision profiles:</p>\n<ul><li>Vector: semantically-similar but maybe topically-different (\"legal advice\" finds \"regulatory guidance\").</li><li>Graph: explicit relationships (\"Alice's projects\" finds projects she owns).</li><li>Full-text: lexically-similar (\"Decree 13\" finds documents containing those words).</li></ul>\n<p>Combining via RRF preserves diversity; reranking polishes precision.</p>\n<p><strong>Why RRF + rerank (DEC-195)?</strong> RRF is a robust ensemble method (no parameter tuning needed); BGE-reranker is the precision-polish on the top-50 candidates. Two-stage: ensemble for recall, rerank for precision.</p>\n<p><strong>Why chain_anchor verify (DEC-196)?</strong> Layer 2 is derived; if Layer 1 is tampered AND Layer 2 hasn't re-ingested, Layer 2 contains stale (pre-tamper) data. The chain_anchor stored in Layer 2 is <code>SHA-256(Layer 1 row at ingest time)</code>. At read, recompute from current Layer 1; mismatch → tampering detected.</p>\n<p><strong>Why graceful BGE degrade (DEC-197)?</strong> BGE outage shouldn't take down search. Falling back to fulltext (less semantic, still useful) preserves availability. The <code>explain</code> field surfaces the degradation so users know.</p>\n<p><strong>Why Vietnamese tokenisation (DEC-198)?</strong> Default PGroonga uses BLI tokenisation (good for English); Vietnamese has different word-segmentation rules. PGroonga's mecab support + custom Vietnamese dictionary gives proper VN segmentation.</p>\n<p><strong>Why 250ms p95 budget (§1 #7)?</strong> Search is interactive (user types → results appear). 250ms is at the edge of human perception; longer feels sluggish. Vector + graph + fulltext in parallel + rerank fits in 250ms on 1M-chunk Postgres + GPU rerank.</p>\n<p><strong>Why ACL at result-time, not query-time (§1 #5)?</strong> ACL is per-memory metadata; computing \"give me only memories where I'm in ACL\" at SQL-time would require GIN index on <code>acl</code> array. At search-time scale (top-50 candidates), in-memory filter is fast (~1ms). Trade-off: more rows fetched then filtered; manageable at scale.</p>\n<p><strong>Why empty results = 200 not 404 (§1 #12)?</strong> Search semantics: \"no matches\" is a valid answer, not an error. 404 implies \"this URL doesn't exist\"; 200 with <code>[]</code> says \"search ran; no matches.\" Standard search-API convention.</p>\n<p><strong>Why explain support (§1 #9)?</strong> Search relevance is hard to debug. <code>?explain=true</code> returns per-backend scores + RRF computation + rerank input/output — operators can answer \"why is X ranked higher than Y?\"</p>\n<p><strong>Why chain_anchor mismatch is sev-1 (§1 #10)?</strong> Layer 1 tampering is the highest-stakes failure: someone modified the chain. Even a single mismatch warrants immediate investigation.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/memory/src/search/mod.rs\nuse uuid::Uuid;\nuse chrono::{DateTime, Utc};\n\n#[derive(serde::Deserialize)]\npub struct SearchQuery {\n    pub q: String,\n    pub kind: Option&lt;String&gt;,\n    pub ts_since: Option&lt;i64&gt;,\n    pub ts_until: Option&lt;i64&gt;,\n    #[serde(default = \"default_limit\")]\n    pub limit: usize,\n    #[serde(default)]\n    pub explain: bool,\n}\nfn default_limit() -&gt; usize { 10 }\n\n#[derive(serde::Serialize)]\npub struct SearchResults {\n    pub items: Vec&lt;SearchResult&gt;,\n    pub explain: Option&lt;ExplainPayload&gt;,\n    pub degraded_backends: Vec&lt;String&gt;,\n}\n\n#[derive(serde::Serialize)]\npub struct SearchResult {\n    pub id: Uuid,\n    pub kind: String,\n    pub path: String,\n    pub ts_ns: i64,\n    pub snippet: String,\n    pub score: f32,\n    pub related_count: u32,\n}\n\n#[derive(serde::Serialize)]\npub struct ExplainPayload {\n    pub backend_scores: HashMap&lt;String, Vec&lt;(Uuid, f32)&gt;&gt;,\n    pub rrf_input: Vec&lt;Uuid&gt;,\n    pub rerank_pre: Vec&lt;Uuid&gt;,\n    pub rerank_post: Vec&lt;Uuid&gt;,\n    pub timings_ms: HashMap&lt;String, u32&gt;,\n}\n\n#[derive(thiserror::Error, Debug)]\npub enum SearchError {\n    #[error(\"auth failed\")]\n    AuthFailed,\n    #[error(\"all backends failed\")]\n    AllBackendsFailed,\n    #[error(\"limit exceeds max (100)\")]\n    LimitTooLarge,\n    #[error(\"db: {0}\")]\n    Db(#[from] sqlx::Error),\n}\n\npub async fn search(query: SearchQuery, tenant_id: Uuid, actor_id: &amp;str, pool: &amp;PgPool, bge_url: &amp;str, rerank_url: &amp;str)\n    -&gt; Result&lt;SearchResults, SearchError&gt;\n{\n    if query.limit &gt; 100 { return Err(SearchError::LimitTooLarge); }\n\n    let mut explain = if query.explain { Some(ExplainPayload::default()) } else { None };\n    let mut degraded = vec![];\n\n    // §1 #2: 3 backends in parallel; collect Result&lt;Vec&lt;_&gt;, _&gt; so failures don't poison the join\n    let (vec_res, graph_res, fts_res) = tokio::join!(\n        vector::search(&amp;query, tenant_id, pool, bge_url),\n        graph::search(&amp;query, tenant_id, pool),\n        fulltext::search(&amp;query, tenant_id, pool),\n    );\n\n    let mut backends: Vec&lt;Vec&lt;(Uuid, f32)&gt;&gt; = vec![];\n    match vec_res {\n        Ok(v) =&gt; backends.push(v),\n        Err(e) =&gt; { tracing::warn!(error = %e, \"vector search degraded\"); degraded.push(\"vector\".into()); }\n    }\n    match graph_res {\n        Ok(v) =&gt; backends.push(v),\n        Err(e) =&gt; { tracing::warn!(error = %e, \"graph search degraded\"); degraded.push(\"graph\".into()); }\n    }\n    match fts_res {\n        Ok(v) =&gt; backends.push(v),\n        Err(e) =&gt; { tracing::warn!(error = %e, \"fulltext search degraded\"); degraded.push(\"fulltext\".into()); }\n    }\n\n    if backends.is_empty() { return Err(SearchError::AllBackendsFailed); }\n\n    let fused = rrf::reciprocal_rank_fusion(&amp;backends, 60);\n    let candidates: Vec&lt;Uuid&gt; = fused.into_iter().map(|(id, _)| id).take(50).collect();\n    let reranked = rerank::with_bge(&amp;query.q, &amp;candidates, rerank_url, pool, tenant_id).await\n        .unwrap_or_else(|_| {\n            degraded.push(\"rerank\".into());\n            candidates.iter().enumerate().map(|(i, id)| (*id, 1.0 / (i + 1) as f32)).collect()\n        });\n\n    let mut items = vec![];\n    for (id, score) in reranked.iter().take(query.limit * 2) {   // overfetch for ACL filtering\n        let row = layer2_memories::fetch(pool, tenant_id, *id).await?;\n        if !chain_anchor_verify::matches_layer1(&amp;row, pool).await? {\n            metrics::chain_anchor_mismatch(tenant_id, row.seq);\n            tracing::error!(seq = row.seq, \"chain_anchor mismatch; sev-1\");\n            continue;\n        }\n        if !acl_filter::passes(&amp;row, actor_id) {\n            metrics::acl_filtered(tenant_id);\n            continue;\n        }\n        items.push(SearchResult {\n            id: row.id, kind: row.kind, path: row.path,\n            ts_ns: row.ts_ns, snippet: build_snippet(&amp;row, &amp;query.q),\n            score: *score, related_count: graph::related_count(&amp;row, pool).await.unwrap_or(0),\n        });\n        if items.len() &gt;= query.limit { break; }\n    }\n\n    Ok(SearchResults { items, explain, degraded_backends: degraded })\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory/src/search/rrf.rs\npub fn reciprocal_rank_fusion(backend_results: &amp;[Vec&lt;(Uuid, f32)&gt;], k: usize) -&gt; Vec&lt;(Uuid, f32)&gt; {\n    let mut scores: HashMap&lt;Uuid, f32&gt; = HashMap::new();\n    for backend in backend_results {\n        for (rank, (id, _)) in backend.iter().enumerate() {\n            *scores.entry(*id).or_insert(0.0) += 1.0 / (k as f32 + rank as f32 + 1.0);\n        }\n    }\n    let mut sorted: Vec&lt;_&gt; = scores.into_iter().collect();\n    sorted.sort_by(|a, b| b.1.partial_cmp(&amp;a.1).unwrap());\n    sorted\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory/src/search/chain_anchor_verify.rs\npub async fn matches_layer1(row: &amp;Layer2Row, pool: &amp;PgPool) -&gt; Result&lt;bool, SearchError&gt; {\n    let layer1_canonical = layer1::fetch_canonical_bytes(pool, row.tenant_id, row.seq).await?;\n    let recomputed = sha256(&amp;layer1_canonical);\n    Ok(recomputed == row.chain_anchor[..])\n}</code></pre>\n<pre><code class=\"language-sql\">-- services/memory/migrations/0003_pgroonga.sql\nCREATE EXTENSION IF NOT EXISTS pgroonga;\n\nALTER TABLE layer2_memories ADD COLUMN body_text TEXT;   -- denormalised for full-text\nCREATE INDEX layer2_memories_pgroonga ON layer2_memories\n    USING pgroonga (body_text)\n    WITH (tokenizer = 'TokenMecab');   -- VN-aware tokenisation</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>Vector + graph + full-text all contribute to results.</li><li>RRF fusion produces stable ordering — same query twice returns same ordering.</li><li>Rerank improves precision — known-relevance fixture has relevant docs in top-3 (cosine ≥ 0.5).</li><li>p95 &lt; 250ms on 1M chunks.</li><li>Cross-tenant: caller in tenant A never sees B's results (RLS enforces).</li><li>ACL filter: memories with <code>acl: [@alice]</code> invisible to @bob; metric <code>memory_search_acl_filtered_total</code> increments.</li><li>BGE sidecar down → fallback to graph + fulltext; surfaced in <code>explain.degraded_backends</code>.</li><li>AGE timeout → graph skipped; still returns from vec + fulltext.</li><li>PGroonga error → fulltext skipped.</li><li>All 3 backends fail → 503 with <code>all_backends_failed</code>.</li><li>Vietnamese query \"Nghị định 13\" returns Vietnamese-tagged docs (mecab tokenisation).</li><li>Empty results → 200 with <code>[]</code>.</li><li>Limit &gt; 100 → 400 with <code>limit_too_large</code>.</li><li><code>?explain=true</code> returns per-backend scores + RRF + rerank input/output.</li><li>Chain anchor mismatch → drop result + sev-1 metric.</li><li>JWT missing → 401.</li><li>Snippet returned per result (200 chars around match).</li><li><code>related_count</code> reflects graph linkage.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn three_backends_contribute_to_results() {\n    let (pool, tenant) = test_setup().await;\n    test_helper::insert_memories(&amp;pool, tenant, vec![\n        memory(\"decisions/decree-13.md\", \"Decree 13 PDPL\"),\n        memory(\"decisions/related.md\", \"regulatory guidance Vietnam\"),\n    ]).await;\n\n    let result = search(SearchQuery { q: \"Decree 13\".into(), explain: true, ..default_query() },\n                        tenant, \"@alice\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    let explain = result.explain.unwrap();\n    assert!(!explain.backend_scores[\"vector\"].is_empty());\n    assert!(!explain.backend_scores[\"fulltext\"].is_empty());\n}\n\n#[tokio::test]\nasync fn cross_tenant_rls_blocks() {\n    let (pool, t_a) = test_setup().await;\n    let t_b = test_helper::create_tenant().await;\n    test_helper::insert_memory(&amp;pool, t_a, \"secret\", \"secret data\").await;\n\n    let result = search(default_query(\"secret\"), t_b, \"@bob\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    assert!(result.items.is_empty(), \"tenant B saw tenant A's data\");\n}\n\n#[tokio::test]\nasync fn acl_excludes_unauthorised_actor() {\n    let (pool, tenant) = test_setup().await;\n    test_helper::insert_memory_with_acl(&amp;pool, tenant, \"private-x\", \"secret\", &amp;[\"@alice\"]).await;\n    let result_alice = search(default_query(\"private-x\"), tenant, \"@alice\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    assert_eq!(result_alice.items.len(), 1);\n    let result_bob = search(default_query(\"private-x\"), tenant, \"@bob\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    assert_eq!(result_bob.items.len(), 0);\n    let metric: u64 = otel_test_helper::counter_value(\"memory_search_acl_filtered_total\", &amp;[(\"tenant_id\", &amp;tenant.to_string())]);\n    assert!(metric &gt; 0);\n}\n\n#[tokio::test]\nasync fn bge_down_falls_back_to_fulltext() {\n    let (pool, tenant) = test_setup().await;\n    test_helper::insert_memory(&amp;pool, tenant, \"test\", \"Decree 13\").await;\n    bge_test_helper::stop();\n    let result = search(SearchQuery { q: \"Decree 13\".into(), explain: true, ..default_query() },\n                        tenant, \"@a\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    assert!(!result.items.is_empty());\n    assert!(result.degraded_backends.contains(&amp;\"vector\".to_string()));\n}\n\n#[tokio::test]\nasync fn all_backends_fail_503() {\n    bge_test_helper::stop();\n    age_test_helper::stop();\n    pgroonga_test_helper::stop();\n    let err = search(default_query(\"anything\"), test_tenant().await, \"@a\", &amp;test_pool().await, &amp;bge_url(), &amp;rerank_url()).await.expect_err(\"expected AllBackendsFailed\");\n    assert!(matches!(err, SearchError::AllBackendsFailed));\n}\n\n#[tokio::test]\nasync fn vietnamese_query_returns_vn_docs() {\n    let (pool, tenant) = test_setup().await;\n    test_helper::insert_memory(&amp;pool, tenant, \"nd13\", \"Nghị định 13/2023 về bảo vệ dữ liệu cá nhân\").await;\n    let result = search(default_query(\"Nghị định 13\"), tenant, \"@a\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    assert_eq!(result.items.len(), 1);\n}\n\n#[tokio::test]\nasync fn chain_anchor_mismatch_drops_result_and_sev1() {\n    let (pool, tenant) = test_setup().await;\n    let id = test_helper::insert_memory(&amp;pool, tenant, \"x\", \"body\").await;\n    test_helper::corrupt_layer1(&amp;pool, tenant, id).await;\n\n    let result = search(default_query(\"body\"), tenant, \"@a\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    assert_eq!(result.items.len(), 0);\n    let metric: u64 = otel_test_helper::counter_value(\"memory_search_chain_anchor_mismatch_total\", &amp;[(\"tenant_id\", &amp;tenant.to_string())]);\n    assert!(metric &gt; 0);\n}\n\n#[tokio::test]\n#[ignore = \"long-running benchmark\"]\nasync fn p95_under_250ms_on_1m_chunks() {\n    let (pool, tenant) = setup_with_1m_chunks().await;\n    let mut samples = vec![];\n    for _ in 0..200 {\n        let t0 = std::time::Instant::now();\n        let _ = search(default_query(\"test query\"), tenant, \"@a\", &amp;pool, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n        samples.push(t0.elapsed().as_millis() as u64);\n    }\n    samples.sort();\n    let p95 = samples[(samples.len() as f64 * 0.95) as usize];\n    assert!(p95 &lt; 250, \"p95 {p95}ms exceeds 250ms\");\n}\n\n#[tokio::test]\nasync fn explain_returns_full_breakdown() {\n    let result = search(SearchQuery { q: \"test\".into(), explain: true, ..default_query() }, test_tenant().await, \"@a\", &amp;test_pool().await, &amp;bge_url(), &amp;rerank_url()).await.unwrap();\n    assert!(result.explain.is_some());\n    let e = result.explain.unwrap();\n    assert!(!e.backend_scores.is_empty());\n    assert!(!e.timings_ms.is_empty());\n}\n\n#[tokio::test]\nasync fn limit_over_100_returns_400() {\n    let err = search(SearchQuery { limit: 101, ..default_query() }, test_tenant().await, \"@a\", &amp;test_pool().await, &amp;bge_url(), &amp;rerank_url()).await.expect_err(\"expected LimitTooLarge\");\n    assert!(matches!(err, SearchError::LimitTooLarge));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-101</strong> — Layer 2 + chain_anchor.</li><li><strong>FR-AI-019</strong> — BGE-M3 sidecar.</li><li><strong>FR-AI-020</strong> — BGE-rerank-v2-m3.</li><li><strong>FR-AUTH-003</strong> — RLS enforcement.</li><li><strong>FR-AUTH-004</strong> — JWT.</li><li><strong>FR-MEMORY-106</strong> — sync_class informs ACL filter (related).</li><li>Crates: <code>axum</code>, <code>sqlx</code>, <code>tokio</code>, <code>serde</code>, <code>reqwest</code>, <code>proptest@1</code> (test).</li><li>Postgres 16 + pgvector + AGE + PGroonga (with mecab for Vietnamese).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Search request</h3>\n<pre><code class=\"language-http\">GET /v1/memory/search?q=Decree%2013&amp;kind=decisions&amp;limit=10&amp;explain=true HTTP/1.1\nAuthorization: Bearer &lt;jwt&gt;</code></pre>\n<h3>Response</h3>\n<pre><code class=\"language-json\">{\n  \"items\": [\n    {\n      \"id\": \"550e...\", \"kind\": \"decisions\",\n      \"path\": \"memories/decisions/decree-13-pdpl.md\",\n      \"ts_ns\": 1747526400000000000,\n      \"snippet\": \"...Decree 13/2023 establishes Vietnam's personal data protection framework...\",\n      \"score\": 0.94,\n      \"related_count\": 5\n    }\n  ],\n  \"explain\": {\n    \"backend_scores\": {\n      \"vector\": [[\"550e...\", 0.85], [\"...\"]],\n      \"graph\":  [[\"550e...\", 1.0]],\n      \"fulltext\": [[\"550e...\", 0.9]]\n    },\n    \"rrf_input\": [\"550e...\", \"...\"],\n    \"rerank_pre\": [\"550e...\", \"...\"],\n    \"rerank_post\": [\"550e...\", \"...\"],\n    \"timings_ms\": { \"vector\": 45, \"graph\": 30, \"fulltext\": 60, \"rrf\": 1, \"rerank\": 80, \"acl_filter\": 5, \"chain_anchor_verify\": 3 }\n  },\n  \"degraded_backends\": []\n}</code></pre>\n<h3>Degraded response (BGE down)</h3>\n<pre><code class=\"language-json\">{\n  \"items\": [...],\n  \"degraded_backends\": [\"vector\"]\n}</code></pre>\n<h3>Chain-anchor mismatch sev-1</h3>\n<pre><code class=\"language-text\">ERROR seq=12345 stored_anchor=abc... recomputed=def...\n      memory_search_chain_anchor_mismatch; result dropped\nsev-1 memory_search_chain_anchor_mismatch_total{tenant_id=...} incremented</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Hybrid sparse+dense retrieval (BM25 + vector) — slice 4+.</li><li>Per-user search-history personalisation — slice 5+.</li><li>Query suggestions (autocomplete) — slice 4+.</li><li>Multi-modal search (images + audio) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>BGE down</td><td>reqwest error</td><td>Vector backend degraded; graph + fulltext continue</td><td>Self-heals when BGE up</td></tr><tr><td>AGE query timeout (5s)</td><td>tokio timeout</td><td>Skip graph; use vec + fulltext</td><td>Self-heals</td></tr><tr><td>PGroonga error</td><td>sqlx error</td><td>Skip fulltext; use vec + graph</td><td>Self-heals</td></tr><tr><td>All 3 backends fail</td><td>empty backends list</td><td>503</td><td>Operator investigates</td></tr><tr><td>Cross-tenant attempt</td><td>RLS blocks at DB</td><td>0 rows</td><td>By design</td></tr><tr><td>Empty results</td><td>normal</td><td>200 with <code>[]</code></td><td>By design</td></tr><tr><td>Chain anchor mismatch</td><td>per-result verify</td><td>Drop result + sev-1</td><td>Investigate Layer 1 corruption</td></tr><tr><td>ACL filter excludes all results</td><td>result-time filter</td><td>200 with <code>[]</code></td><td>By design</td></tr><tr><td>Vietnamese query no mecab match</td><td>fallback to BLI tokenisation</td><td>Lower precision but works</td><td>Update mecab dict</td></tr><tr><td>Limit &gt; 100</td><td>param check</td><td>400</td><td>Caller fixes limit</td></tr><tr><td>JWT missing</td><td>auth check</td><td>401</td><td>Caller obtains JWT</td></tr><tr><td>Slow query (&gt; 250ms p95)</td><td>OTel histogram</td><td>sev-3</td><td>Investigate hot path</td></tr><tr><td>Rerank fails</td><td>catch</td><td>Use unranked top-50</td><td>Self-heals</td></tr><tr><td>Snippet build fails</td><td>catch</td><td>Empty snippet</td><td>Self-heals</td></tr><tr><td>Concurrent search same query</td><td>tokio handles</td><td>Each independent</td><td>By design</td></tr><tr><td>Limit 0</td><td>param check</td><td>400</td><td>Caller fixes</td></tr><tr><td>ts_since &gt; ts_until</td><td>param check</td><td>400</td><td>Caller fixes</td></tr><tr><td>Empty query string</td><td>accept; returns broad results</td><td>200</td><td>By design</td></tr><tr><td>Backend latency outliers</td><td>histogram</td><td>Sev-3 alarm if backend p99 &gt; budget</td><td>Operator investigates</td></tr><tr><td>Index missing (pgvector or pgroonga)</td><td>startup check</td><td>Refuse to start</td><td>Operator runs migration</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>3-way parallel search exploits backend complementarity — vector/graph/fulltext catch different relevance signals.</li><li>RRF (k=60 standard) is robust to backend score-scale differences; doesn't need per-backend tuning.</li><li>BGE-reranker-v2-m3 polishes top-50 with cross-encoder precision (FR-AI-020).</li><li>Chain_anchor verify per result is the read-time tamper detection — slow but security-load-bearing.</li><li>ACL applied at result-time (in-memory) avoids GIN index complexity; performant for top-50.</li><li>Graceful degrade preserves availability during backend outages; surfaced in <code>explain.degraded_backends</code>.</li><li>Vietnamese tokenisation via PGroonga + mecab + custom dictionary handles VN word segmentation correctly.</li><li>250ms p95 budget covers vector (~50ms) + graph (~50ms) + fulltext (~50ms parallel) + rerank (~80ms) + chain_anchor verify (~20ms) + ACL filter (~5ms).</li><li>Empty query returns broad results (latest memories within filters); useful as \"browse\" mode.</li><li>Sev-1 chain_anchor mismatch is the highest-value security signal — Layer 1 tampering caught at query time.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-108. Status: done (implemented 2026-05-23).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Shipped as services/memory/src/search.rs (single module), not the search/{vector,graph,...} tree; AGE-era graph search is the relational l2_edge path.</p>";
