window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SKILL-102"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A self-hosted OCI-compliant registry <strong>MUST</strong> host <code>.skill</code> bundles. Each interaction:</p>\n<ol><li><strong>MUST</strong> speak OCI Distribution v1.1 — standard <code>/v2/&lt;name&gt;/manifests/&lt;reference&gt;</code> + <code>/v2/&lt;name&gt;/blobs/&lt;digest&gt;</code> endpoints. Backend: zot (recommended; OCI-native + small footprint).</li><li><strong>MUST</strong> require cosign signature on every push. Cosign uses Ed25519; per-publisher keypair (private key in publisher's secret store). Publisher signs bundle bytes; registry verifies before storing. Unsigned push → 401 with <code>signature_required</code>.</li><li><strong>MUST</strong> emit memory rows:</li></ol>\n<ul><li><code>skill.published</code> on successful push: <code>tenant_id</code>, <code>skill_id</code>, <code>version</code>, <code>digest</code>, <code>publisher_subject_id</code>, <code>signature_pubkey_id</code>, <code>bundle_size_bytes</code>, <code>request_id</code>.</li><li><code>skill.pulled</code> on successful pull: <code>tenant_id</code>, <code>skill_id</code>, <code>version</code>, <code>digest</code>, <code>puller_subject_id</code>, <code>request_id</code>.</li></ul>\n<ol><li><strong>MUST</strong> scope namespace by <code>tenant_id</code>: registry path is <code>/v2/&lt;tenant_id&gt;/&lt;skill_id&gt;/manifests/&lt;version&gt;</code>. Pulls require valid JWT (FR-AUTH-004) AND <code>claims.tenant_id == namespace tenant_id</code>. Cross-tenant attempts → 403 with <code>cross_tenant_blocked</code>.</li><li><strong>MUST</strong> support immutable tags — once <code>&lt;skill_id&gt;:&lt;version&gt;</code> is published, re-push same tag returns 409 with <code>immutable_tag</code>. Bug-fix releases require version bump (semver discipline).</li><li><strong>MUST</strong> support <code>cyberos-skill publish &lt;bundle.tar.zst&gt; --version &lt;semver&gt;</code> CLI for ergonomic pushes. Auto-signs via local cosign keypair.</li><li><strong>MUST</strong> support <code>cyberos-skill pull &lt;skill_id&gt;:&lt;version&gt;</code> CLI for ergonomic pulls. Auto-verifies signature; cached locally.</li><li><strong>MUST</strong> verify signature on EVERY pull (not just push). Tampered storage backend → pull fails with <code>signature_invalid</code>. Defense-in-depth.</li><li><strong>MUST</strong> authenticate via FR-AUTH-004 JWT with <code>claims.scope_grants</code> containing <code>skill:publish</code> or <code>skill:pull</code>. Missing scope → 403.</li><li><strong>MUST</strong> include <code>Idempotency-Key</code> header support on push (mirrors FR-AUTH-001 §1 #5). Repeat push with same key + same content → return existing manifest; same key + different content → 409 with <code>idempotency_key_reuse</code>.</li><li><strong>MUST</strong> enforce 100MB bundle size cap. Bundle &gt; 100MB → 413 PAYLOAD_TOO_LARGE with <code>bundle_too_large</code>.</li><li><strong>MUST</strong> support quota per tenant (10GB total bundle storage at slice 1; configurable). Above quota → 507 INSUFFICIENT_STORAGE.</li><li><strong>MUST</strong> complete push p95 &lt; 5s for 10MB bundle (typical size); pull p95 &lt; 2s.</li><li><strong>MUST</strong> publish manifest format matching OCI v1.1: ``<code>json { \"schemaVersion\": 2, \"mediaType\": \"application/vnd.cyberos.skill.v1+json\", \"config\": { \"mediaType\": \"application/vnd.cyberos.skill.config.v1+json\", \"digest\": \"sha256:...\", \"size\": ... }, \"layers\": [{ \"mediaType\": \"application/vnd.cyberos.skill.bundle.v1+tar+zst\", \"digest\": \"sha256:...\", \"size\": ... }], \"annotations\": { \"world.cyberos.skill.signature\": \"&lt;base64-cosign-sig&gt;\", \"world.cyberos.skill.publisher\": \"&lt;subject_id&gt;\", \"world.cyberos.skill.published_at\": \"&lt;iso8601&gt;\" } } </code>``</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>skill_registry_pushes_total{tenant_id, outcome}</code> (counter).</li><li><code>skill_registry_pulls_total{tenant_id, outcome}</code> (counter).</li><li><code>skill_registry_signature_failures_total{stage}</code> (counter; sev-1 alarm; stage ∈ push | pull).</li><li><code>skill_registry_bundle_size_bytes</code> (histogram).</li><li><code>skill_registry_storage_used_bytes{tenant_id}</code> (gauge).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why self-hosted (DEC-205)?</strong> SaaS registries (DockerHub, GHCR) leak skill metadata to the registry operator. Self-hosted keeps tenant-business semantics in-region. zot is OCI-native, small, well-maintained.</p>\n<p><strong>Why cosign on every push (DEC-206)?</strong> Tampered bundles installed in tenant infrastructure = code execution opportunity. Signature on push + verify on every pull = supply-chain integrity. Per-publisher keypair limits blast radius if one is compromised.</p>\n<p><strong>Why immutable tags (DEC-207)?</strong> Mutable tags allow silent content changes. Tenant A pulls <code>obs.triage-alert:1.0</code>; later, attacker re-publishes 1.0 with malicious content; tenant A's next pull picks it up. Immutable tags prevent this.</p>\n<p><strong>Why tenant-scoped namespace (DEC-208)?</strong> Tenant A's skills shouldn't be visible/installable by tenant B. Namespace scoping at registry layer = structural isolation. Cross-tenant attempts at API level → 403.</p>\n<p><strong>Why verify on EVERY pull, not just on push (§1 #8)?</strong> Storage backend compromise (zot DB tampering) could replace bundle content. Re-verifying on pull catches this. The cost is small (Ed25519 verify is microseconds); the security benefit is large.</p>\n<p><strong>Why 100MB bundle cap (§1 #11)?</strong> Skills are typically &lt;10MB. 100MB cap catches pathological bundles (huge embedded models, miscellaneous tarballs). Above 100MB, skill packaging probably has a bug.</p>\n<p><strong>Why idempotency on push (§1 #10)?</strong> Network retries during publish (slow upload, timeout) shouldn't produce 409 on retry. Idempotency-Key lets retry succeed AS the original.</p>\n<p><strong>Why p95 budgets (§1 #13)?</strong> CI/CD pipelines push skills frequently. 5s push budget keeps deploys fast. 2s pull keeps cold-start of skill execution fast.</p>\n<p><strong>Why cyberos-skill CLI (§1 #6 + #7)?</strong> Ergonomic UX. Without CLI, publishers use raw <code>oras</code> or <code>crane</code> commands — possible but error-prone. CLI auto-handles cosign + JWT + paths.</p>\n<p><strong>Why per-publisher cosign keypair?</strong> Publisher identity is part of audit chain. One keypair per publisher means \"who signed this\" is unambiguous + revocable independently.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Endpoints (OCI Distribution v1.1)</h3>\n<pre><code>POST /v2/&lt;tenant_id&gt;/&lt;skill_id&gt;/blobs/uploads/         # initiate blob upload\nPUT  /v2/&lt;tenant_id&gt;/&lt;skill_id&gt;/blobs/uploads/&lt;uuid&gt;?digest=sha256:&lt;&gt;\nPUT  /v2/&lt;tenant_id&gt;/&lt;skill_id&gt;/manifests/&lt;version&gt;    # push manifest\nGET  /v2/&lt;tenant_id&gt;/&lt;skill_id&gt;/manifests/&lt;version&gt;    # pull manifest\nGET  /v2/&lt;tenant_id&gt;/&lt;skill_id&gt;/blobs/sha256:&lt;&gt;        # pull blob\nHEAD /v2/&lt;tenant_id&gt;/&lt;skill_id&gt;/manifests/&lt;version&gt;    # check exists (immutability check)</code></pre>\n<h3>Push handler</h3>\n<pre><code class=\"language-rust\">// services/skill-registry/src/oci.rs\npub async fn push_manifest(\n    tenant_id: Uuid, skill_id: &amp;str, version: &amp;str,\n    body: Bytes, claims: &amp;Claims, idempotency_key: Option&lt;String&gt;,\n    storage: &amp;Storage, memory: &amp;MemoryBridge,\n) -&gt; Result&lt;PushResponse, RegistryError&gt; {\n    // §1 #4 tenant scope\n    if claims.tenant_id != tenant_id { return Err(RegistryError::CrossTenantBlocked); }\n    // §1 #9 scope grant\n    if !claims.scope_grants.iter().any(|g| g == \"skill:publish\" || g == \"*\") {\n        return Err(RegistryError::Forbidden { needed: \"skill:publish\".into() });\n    }\n\n    // §1 #5 immutability\n    if storage.manifest_exists(tenant_id, skill_id, version).await? {\n        return Err(RegistryError::ImmutableTag { skill_id: skill_id.into(), version: version.into() });\n    }\n\n    // §1 #10 idempotency\n    let body_hash = hex::encode(sha256(&amp;body));\n    if let Some(key) = &amp;idempotency_key {\n        if let Some(prior) = storage.idempotency_lookup(key).await? {\n            if prior.body_hash != body_hash {\n                return Err(RegistryError::IdempotencyKeyReuse);\n            }\n            return Ok(prior.response);\n        }\n    }\n\n    let manifest: SkillManifest = serde_json::from_slice(&amp;body)?;\n\n    // §1 #11 size cap\n    let total_size: u64 = manifest.layers.iter().map(|l| l.size).sum();\n    if total_size &gt; 100 * 1024 * 1024 {\n        return Err(RegistryError::BundleTooLarge { actual_bytes: total_size });\n    }\n\n    // §1 #2 cosign signature verify\n    let signature = manifest.annotations.get(\"world.cyberos.skill.signature\")\n        .ok_or(RegistryError::SignatureRequired)?;\n    let pubkey_id = claims.subject_id.to_string();\n    cosign_verify::verify_signature(&amp;body, signature, &amp;pubkey_id).await\n        .map_err(|e| RegistryError::SignatureInvalid(e.to_string()))?;\n\n    // §1 #12 quota\n    let used = storage.tenant_used_bytes(tenant_id).await?;\n    if used + total_size &gt; tenant_quota(tenant_id) {\n        return Err(RegistryError::QuotaExceeded);\n    }\n\n    storage.put_manifest(tenant_id, skill_id, version, &amp;body).await?;\n\n    let request_id = format!(\"registry_{}\", ulid::Ulid::new());\n    memory.emit(canonical::skill_published(\n        tenant_id, skill_id, version, &amp;body_hash, claims.subject_id,\n        &amp;pubkey_id, total_size, &amp;request_id,\n    )).await?;\n\n    if let Some(key) = idempotency_key {\n        storage.idempotency_insert(&amp;key, &amp;body_hash, &amp;response).await?;\n    }\n\n    metrics::push(tenant_id, \"ok\");\n    Ok(PushResponse { manifest_digest: body_hash, status: 201 })\n}</code></pre>\n<h3>Pull handler</h3>\n<pre><code class=\"language-rust\">pub async fn pull_manifest(\n    tenant_id: Uuid, skill_id: &amp;str, version: &amp;str, claims: &amp;Claims,\n    storage: &amp;Storage, memory: &amp;MemoryBridge,\n) -&gt; Result&lt;Bytes, RegistryError&gt; {\n    if claims.tenant_id != tenant_id { return Err(RegistryError::CrossTenantBlocked); }\n    if !claims.scope_grants.iter().any(|g| g == \"skill:pull\" || g == \"*\") {\n        return Err(RegistryError::Forbidden { needed: \"skill:pull\".into() });\n    }\n\n    let body = storage.get_manifest(tenant_id, skill_id, version).await?\n        .ok_or(RegistryError::NotFound)?;\n\n    // §1 #8 verify on pull (defense in depth)\n    let manifest: SkillManifest = serde_json::from_slice(&amp;body)?;\n    let signature = manifest.annotations.get(\"world.cyberos.skill.signature\")\n        .ok_or(RegistryError::SignatureRequired)?;\n    let publisher = manifest.annotations.get(\"world.cyberos.skill.publisher\").cloned().unwrap_or_default();\n    cosign_verify::verify_signature(&amp;body, signature, &amp;publisher).await\n        .map_err(|e| { metrics::signature_failure(\"pull\"); RegistryError::SignatureInvalid(e.to_string()) })?;\n\n    let request_id = format!(\"registry_{}\", ulid::Ulid::new());\n    memory.emit(canonical::skill_pulled(\n        tenant_id, skill_id, version, hex::encode(sha256(&amp;body)), claims.subject_id, &amp;request_id,\n    )).await?;\n\n    metrics::pull(tenant_id, \"ok\");\n    Ok(body)\n}</code></pre>\n<h3>CLI</h3>\n<pre><code class=\"language-rust\">// services/skill-registry/src/bin/cyberos_skill.rs\n#[derive(clap::Parser)]\nstruct Cli { #[command(subcommand)] cmd: Cmd }\n\n#[derive(clap::Subcommand)]\nenum Cmd {\n    Publish { #[arg(long)] bundle: PathBuf, #[arg(long)] version: String },\n    Pull    { skill_ref: String },   // \"obs.triage-alert:1.0.0\"\n}\n\n#[tokio::main]\nasync fn main() -&gt; anyhow::Result&lt;()&gt; {\n    let cli = Cli::parse();\n    match cli.cmd {\n        Cmd::Publish { bundle, version } =&gt; {\n            let bytes = std::fs::read(&amp;bundle)?;\n            let signature = cosign::sign_local(&amp;bytes, \"~/.cyberos/cosign.key\")?;\n            let manifest = build_manifest(&amp;bytes, &amp;signature, &amp;version)?;\n            let resp = http::push(&amp;manifest, &amp;load_jwt()).await?;\n            println!(\"✅ Published: {}@{}\", resp.skill_id, version);\n        }\n        Cmd::Pull { skill_ref } =&gt; {\n            let (skill_id, version) = parse_ref(&amp;skill_ref)?;\n            let bytes = http::pull(&amp;skill_id, &amp;version, &amp;load_jwt()).await?;\n            std::fs::write(format!(\"./{skill_id}-{version}.tar.zst\"), &amp;bytes)?;\n            println!(\"✅ Pulled: {skill_id}@{version}\");\n        }\n    }\n    Ok(())\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>Push <code>.skill</code> bundle with valid cosign sig → 201; manifest stored.</li><li>Push without signature → 401 <code>signature_required</code>.</li><li>Push with invalid signature → 401 <code>signature_invalid</code>.</li><li>Pull → verifies signature; emits skill.pulled audit row.</li><li>Re-push same tag → 409 <code>immutable_tag</code>.</li><li>Cross-tenant pull (tenant B pulling from tenant A namespace) → 403 <code>cross_tenant_blocked</code>.</li><li>Cross-tenant push → 403.</li><li>JWT lacks <code>skill:publish</code> → 403.</li><li>JWT lacks <code>skill:pull</code> → 403.</li><li>memory rows emitted on push (skill.published) and pull (skill.pulled).</li><li>Bundle &gt; 100MB → 413 <code>bundle_too_large</code>.</li><li>Tenant over 10GB quota → 507 <code>quota_exceeded</code>.</li><li>Idempotent push (same key + same content) → returns prior manifest.</li><li>Idempotent push (same key + different content) → 409 <code>idempotency_key_reuse</code>.</li><li>p95 push &lt; 5s for 10MB bundle.</li><li>p95 pull &lt; 2s.</li><li>CLI <code>cyberos-skill publish</code> works end-to-end.</li><li>CLI <code>cyberos-skill pull</code> works end-to-end.</li><li>Pull with tampered storage (manual byte change) → 401 <code>signature_invalid</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn push_with_valid_signature_succeeds() {\n    let bundle = test_helper::build_bundle(\"obs.triage-alert\");\n    let sig = cosign::sign(&amp;bundle, &amp;test_keypair());\n    let resp = push_manifest(test_tenant(), \"obs.triage-alert\", \"1.0.0\",\n                              build_manifest(&amp;bundle, &amp;sig), &amp;test_publisher_claims(), None,\n                              &amp;test_storage(), &amp;test_memory()).await.unwrap();\n    assert_eq!(resp.status, 201);\n    assert!(memory_test_helper::has_row(\"skill.published\", \"1.0.0\").is_some());\n}\n\n#[tokio::test]\nasync fn push_without_signature_returns_401() {\n    let bundle = test_helper::build_bundle(\"x\");\n    let manifest = build_manifest_unsigned(&amp;bundle);\n    let err = push_manifest(test_tenant(), \"x\", \"1.0.0\", manifest, &amp;test_publisher_claims(), None, &amp;test_storage(), &amp;test_memory()).await.expect_err(\"expected SignatureRequired\");\n    assert!(matches!(err, RegistryError::SignatureRequired));\n}\n\n#[tokio::test]\nasync fn push_with_invalid_signature_returns_401() {\n    let bundle = test_helper::build_bundle(\"x\");\n    let sig = \"TAMPERED_SIGNATURE_BASE64\";\n    let manifest = build_manifest(&amp;bundle, sig);\n    let err = push_manifest(test_tenant(), \"x\", \"1.0.0\", manifest, &amp;test_publisher_claims(), None, &amp;test_storage(), &amp;test_memory()).await.expect_err(\"expected SignatureInvalid\");\n    assert!(matches!(err, RegistryError::SignatureInvalid(_)));\n}\n\n#[tokio::test]\nasync fn re_push_same_tag_returns_409_immutable() {\n    let _ = push_test_manifest(\"obs.triage-alert\", \"1.0.0\").await.unwrap();\n    let err = push_test_manifest(\"obs.triage-alert\", \"1.0.0\").await.expect_err(\"expected ImmutableTag\");\n    assert!(matches!(err, RegistryError::ImmutableTag { .. }));\n}\n\n#[tokio::test]\nasync fn cross_tenant_pull_returns_403() {\n    let tenant_a = test_helper::create_tenant().await;\n    let tenant_b = test_helper::create_tenant().await;\n    let _ = push_with_tenant(tenant_a, \"x\", \"1.0.0\").await.unwrap();\n\n    let claims_b = claims_for(tenant_b);\n    let err = pull_manifest(tenant_a, \"x\", \"1.0.0\", &amp;claims_b, &amp;test_storage(), &amp;test_memory()).await.expect_err(\"expected CrossTenantBlocked\");\n    assert!(matches!(err, RegistryError::CrossTenantBlocked));\n}\n\n#[tokio::test]\nasync fn pull_emits_skill_pulled_audit_row() {\n    let _ = push_test_manifest(\"x\", \"1.0.0\").await.unwrap();\n    let _ = pull_manifest(test_tenant(), \"x\", \"1.0.0\", &amp;test_puller_claims(), &amp;test_storage(), &amp;test_memory()).await.unwrap();\n    let row = memory_test_helper::find_latest(\"skill.pulled\").unwrap();\n    assert_eq!(row.payload[\"skill_id\"], \"x\");\n    assert_eq!(row.payload[\"version\"], \"1.0.0\");\n}\n\n#[tokio::test]\nasync fn bundle_over_100mb_returns_413() {\n    let huge = vec![0u8; 110 * 1024 * 1024];\n    let manifest = build_manifest_with_layer_size(&amp;huge, 110 * 1024 * 1024);\n    let err = push_manifest(test_tenant(), \"x\", \"1.0.0\", manifest, &amp;test_publisher_claims(), None, &amp;test_storage(), &amp;test_memory()).await.expect_err(\"expected BundleTooLarge\");\n    assert!(matches!(err, RegistryError::BundleTooLarge { .. }));\n}\n\n#[tokio::test]\nasync fn tampered_storage_pull_fails_signature_verify() {\n    let _ = push_test_manifest(\"x\", \"1.0.0\").await.unwrap();\n    test_helper::tamper_storage_byte(test_tenant(), \"x\", \"1.0.0\", 100, 0xff).await;\n    let err = pull_manifest(test_tenant(), \"x\", \"1.0.0\", &amp;test_puller_claims(), &amp;test_storage(), &amp;test_memory()).await.expect_err(\"expected SignatureInvalid\");\n    assert!(matches!(err, RegistryError::SignatureInvalid(_)));\n}\n\n#[tokio::test]\nasync fn idempotent_push_returns_prior_manifest() {\n    let key = \"idem-001\".to_string();\n    let manifest = test_manifest(\"x\", \"1.0.0\");\n    let r1 = push_manifest(test_tenant(), \"x\", \"1.0.0\", manifest.clone(), &amp;test_publisher_claims(), Some(key.clone()), &amp;test_storage(), &amp;test_memory()).await.unwrap();\n    let r2 = push_manifest(test_tenant(), \"x\", \"1.0.0\", manifest, &amp;test_publisher_claims(), Some(key), &amp;test_storage(), &amp;test_memory()).await.unwrap();\n    assert_eq!(r1.manifest_digest, r2.manifest_digest);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-yaml\"># deploy/skill-registry/docker-compose.yml\nservices:\n  zot:\n    image: ghcr.io/project-zot/zot:v2.1.0\n    ports: [\"5000:5000\"]\n    volumes:\n      - ./zot-config.json:/etc/zot/config.json:ro\n      - zot-data:/var/lib/registry\n  skill-registry:\n    build: ../../services/skill-registry\n    ports: [\"7878:7878\"]\n    environment: { ZOT_URL: http://zot:5000, MEMORY_URL: http://memory:8080 }\n    depends_on: [zot]\nvolumes: { zot-data: }</code></pre>\n<pre><code class=\"language-json\">{\n  \"storage\": { \"rootDirectory\": \"/var/lib/registry\" },\n  \"http\": { \"address\": \"0.0.0.0\", \"port\": \"5000\",\n            \"auth\": { \"htpasswd\": { \"path\": \"/etc/zot/htpasswd\" }}},\n  \"extensions\": { \"search\": { \"enable\": true }}\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-SKILL-101</strong> — audit row pattern.</li><li><strong>FR-AUTH-004</strong> — JWT with scope_grants.</li><li><strong>FR-AUTH-003</strong> — RLS pattern (tenant scoping).</li><li><strong>FR-AI-003</strong> — memory_writer for audit emission.</li><li>Crates: <code>axum</code>, <code>reqwest</code>, <code>tonic</code> (zot client), <code>sigstore@0.10</code> (cosign), <code>clap@4</code>, <code>serde</code>, <code>tokio</code>.</li><li>zot OCI registry binary.</li><li>cosign CLI for publisher-side signing.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Push request (CLI)</h3>\n<pre><code class=\"language-bash\">$ cyberos-skill publish ./obs-triage-alert.tar.zst --version 1.0.0\n✅ Signed with cosign (key: ~/.cyberos/cosign.key)\n✅ Published: obs.triage-alert@1.0.0 (digest: sha256:abc123...)</code></pre>\n<h3>Push manifest</h3>\n<pre><code class=\"language-http\">PUT /v2/550e.../obs.triage-alert/manifests/1.0.0 HTTP/1.1\nAuthorization: Bearer &lt;jwt&gt;\nContent-Type: application/vnd.cyberos.skill.v1+json\nIdempotency-Key: pub-001\n\n{\n  \"schemaVersion\": 2,\n  \"mediaType\": \"application/vnd.cyberos.skill.v1+json\",\n  \"config\": { \"mediaType\": \"application/vnd.cyberos.skill.config.v1+json\", \"digest\": \"sha256:abc...\", \"size\": 512 },\n  \"layers\": [{ \"mediaType\": \"application/vnd.cyberos.skill.bundle.v1+tar+zst\", \"digest\": \"sha256:def...\", \"size\": 8192345 }],\n  \"annotations\": {\n    \"world.cyberos.skill.signature\": \"MEQCI...\",\n    \"world.cyberos.skill.publisher\": \"subject-stephen-...\",\n    \"world.cyberos.skill.published_at\": \"2026-05-15T14:00:00Z\"\n  }\n}</code></pre>\n<h3>Audit rows</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"skill.published\",\n  \"payload\": {\n    \"tenant_id\": \"550e...\", \"skill_id\": \"obs.triage-alert\", \"version\": \"1.0.0\",\n    \"digest\": \"sha256:abc...\", \"publisher_subject_id\": \"...\",\n    \"signature_pubkey_id\": \"subject-stephen-...\",\n    \"bundle_size_bytes\": 8192345, \"request_id\": \"registry_...\"\n  }\n}\n\n{\n  \"kind\": \"skill.pulled\",\n  \"payload\": {\n    \"tenant_id\": \"550e...\", \"skill_id\": \"obs.triage-alert\", \"version\": \"1.0.0\",\n    \"digest\": \"sha256:abc...\", \"puller_subject_id\": \"...\", \"request_id\": \"registry_...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Skill marketplace UI (browse cross-tenant skills) — slice 5+; current scope is intra-tenant.</li><li>Skill versioning policies (semver enforcement, deprecation) — slice 4+.</li><li>Skill dependencies (one skill requires another) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>cosign signature fails</td><td>sigstore verify</td><td>401 signature_invalid</td><td>Publisher fixes signing</td></tr><tr><td>Bundle too large (&gt;100MB)</td><td>size check</td><td>413 bundle_too_large</td><td>Reduce bundle</td></tr><tr><td>Immutable tag exists</td><td>manifest_exists check</td><td>409 immutable_tag</td><td>Bump version</td></tr><tr><td>Cross-tenant push</td><td>claims check</td><td>403 cross_tenant_blocked</td><td>Caller uses correct tenant JWT</td></tr><tr><td>Cross-tenant pull</td><td>claims check</td><td>403</td><td>Same</td></tr><tr><td>JWT lacks skill:publish</td><td>scope check</td><td>403 forbidden</td><td>Grant scope</td></tr><tr><td>Quota exceeded (&gt;10GB)</td><td>tenant_used check</td><td>507 quota_exceeded</td><td>Operator extends quota OR delete old</td></tr><tr><td>Storage backend error</td><td>zot 5xx</td><td>503</td><td>Operator investigates</td></tr><tr><td>Idempotent replay (same key + same content)</td><td>lookup</td><td>201 with prior digest</td><td>By design</td></tr><tr><td>Idempotency reuse (same key + different content)</td><td>hash mismatch</td><td>409 idempotency_key_reuse</td><td>Caller uses different key</td></tr><tr><td>Tampered storage (post-push)</td><td>pull-time verify catches</td><td>401 signature_invalid</td><td>Investigate storage integrity</td></tr><tr><td>memory audit emit fails</td><td>memory_writer error</td><td>Push succeeds; sev-1 log</td><td>Operator investigates</td></tr><tr><td>zot down</td><td>http error</td><td>503</td><td>Restart zot</td></tr><tr><td>Publisher key revoked</td><td>signature still verifies (key cached)</td><td>Future pushes fail</td><td>Update key allow-list</td></tr><tr><td>Bundle integrity corruption (during transfer)</td><td>digest mismatch</td><td>400</td><td>Caller retries</td></tr><tr><td>Unknown manifest reference (HEAD nonexistent)</td><td>404</td><td>Caller proceeds with PUT</td><td>By design</td></tr><tr><td>Concurrent push same version</td><td>DB unique constraint</td><td>One succeeds; other 409</td><td>By design</td></tr><tr><td>CLI cosign key missing</td><td>file not found</td><td>Exit 1 with clear message</td><td>User generates keypair</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>zot is the reference OCI implementation chosen for its small footprint + active maintenance + full v1.1 support. Alternative: Harbor (heavier; richer UI).</li><li>cosign signing on publisher side via <code>cosign sign-blob</code>; verification at registry uses sigstore-rs library.</li><li>Per-publisher cosign keypair stored in publisher's local secret store (<code>~/.cyberos/cosign.key</code>); rotation per FR-AUTH-006-style sweeper.</li><li>Immutable tags enforce semver discipline — bug fixes go to new patch version, not silent overwrite.</li><li>Tenant scope at registry layer prevents accidental cross-tenant skill installation.</li><li>100MB cap catches bundle bloat (typical skills are &lt;10MB; bundles approaching 100MB usually have a packaging bug).</li><li>10GB quota per tenant is slice-1 default; configurable via tenant policy.</li><li>Pull-side verify catches storage tampering — defense in depth.</li><li>Idempotency-Key matches FR-AUTH-001 pattern; standard retry-safety.</li><li>Audit rows on push + pull = compliance answer to \"who published/installed what when.\"</li></ul>\n<hr>\n<p><em>End of FR-SKILL-102. Status: draft (10/10 target).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>skill-registry was consolidated into services/skill-broker (src/oci.rs).</p>";
