window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-KB-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The KB service <strong>MUST</strong> ship BGE-M3 semantic search at <code>services/kb/src/semantic/</code> ingesting via FR-AI-019 Layer 2, chunked dense embedding, top-K retrieval, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>chunk_kind</code> against closed enum per DEC-1921.</li></ol>\n<ol><li><strong>MUST</strong> chunk via <code>chunker.rs::chunk(doc_plaintext)</code> per DEC-1922:</li></ol>\n<ul><li>Detect semantic boundaries (paragraphs, headings, code blocks).</li><li>Each chunk 256-512 tokens; overlap 64.</li><li>Tag with kind enum.</li></ul>\n<ol><li><strong>MUST</strong> embed via <code>bge_m3_client.rs::embed(text) → Vec&lt;f32; 1024&gt;</code> per DEC-1920.</li></ol>\n<ol><li><strong>MUST</strong> ingest via FR-AI-019 memory Layer 2 — call AI-019 ingest API with doc context + chunk embeddings.</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0005</code>: ``<code>sql CREATE EXTENSION IF NOT EXISTS vector; CREATE TABLE kb_semantic_chunks ( chunk_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, doc_id UUID NOT NULL, version_id UUID NOT NULL, chunk_kind TEXT NOT NULL CHECK (chunk_kind IN ('paragraph','section_heading','code_block','list_item','table_row')), chunk_text TEXT NOT NULL, embedding VECTOR(1024) NOT NULL, chunk_order INT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (tenant_id, doc_id, version_id, chunk_order) ); CREATE INDEX chunks_doc_idx ON kb_semantic_chunks(tenant_id, doc_id, version_id); CREATE INDEX chunks_embedding_idx ON kb_semantic_chunks USING ivfflat (embedding vector_cosine_ops); ALTER TABLE kb_semantic_chunks ENABLE ROW LEVEL SECURITY; CREATE POLICY chunks_rls ON kb_semantic_chunks USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON kb_semantic_chunks FROM cyberos_app; GRANT DELETE ON kb_semantic_chunks TO cyberos_app;  -- invalidation </code>``</li></ol>\n<ol><li><strong>MUST</strong> invalidate on new version per DEC-1923 — DELETE chunks for old version on new version commit.</li></ol>\n<ol><li><strong>MUST</strong> query at <code>vector_query.rs::search(tenant, query, top_k=20)</code> per DEC-1924:</li></ol>\n<ul><li>Embed query via BGE-M3.</li><li>Cosine similarity search against chunks_embedding_idx.</li><li>Return top-K with chunk + doc context.</li><li>Apply RLS tier filter.</li></ul>\n<ol><li><strong>MUST</strong> expose endpoint: ``<code>text POST /v1/kb/search/semantic   body: {query, top_k?: 20, filters?} </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1925. PII per FR-MEMORY-111: query + chunk text SHA-256 hashed; embedding never in chain (binary).</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from query → embed → search → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip memory Layer 2 ingest per DEC-1920.</li></ol>\n<ol><li><strong>MUST NOT</strong> split chunks mid-sentence per DEC-1922.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why BGE-M3 (DEC-1920)?</strong> Multi-lingual (VN+EN+more), 1024-dim balance accuracy/speed, open weights (no API lock-in).</p>\n<p><strong>Why chunking 256-512 (DEC-1922)?</strong> BGE-M3 context 8192 but embedding quality degrades at scale; smaller chunks = better recall.</p>\n<p><strong>Why top-K=20 (DEC-1924)?</strong> Reranker (FR-KB-006) needs candidates; 20 balances recall + rerank cost.</p>\n<p><strong>Why version-keyed invalidation (DEC-1923)?</strong> Doc updates change semantics; stale embeddings produce wrong results.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST /v1/kb/search/semantic</code></pre>\n<p>Sample request:</p>\n<pre><code class=\"language-json\">{\n  \"query\": \"how do I issue an invoice\",\n  \"top_k\": 20,\n  \"filters\": {\"category\": \"finance\"}\n}</code></pre>\n<p>Sample response:</p>\n<pre><code class=\"language-json\">{\n  \"results\": [\n    {\n      \"chunk_id\": \"uuid\",\n      \"doc_id\": \"uuid\",\n      \"doc_title\": \"Quy trình xuất hóa đơn\",\n      \"chunk_text\": \"Để xuất hóa đơn cho khách hàng VN, bạn cần...\",\n      \"chunk_kind\": \"paragraph\",\n      \"similarity\": 0.87\n    }\n  ],\n  \"total\": 20\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>chunk_kind enum cardinality 5</strong>. 2. <strong>BGE-M3 embedding 1024-dim</strong>. 3. <strong>Chunks 256-512 tokens</strong>. 4. <strong>Semantic boundary detection (no mid-sentence)</strong>. 5. <strong>Ingest via FR-AI-019</strong>. 6. <strong>pgvector ivfflat index</strong>. 7. <strong>Cosine similarity query</strong>. 8. <strong>Top-K=20 default</strong>. 9. <strong>Tier filter applied (RLS)</strong>. 10. <strong>Invalidation on new version (DELETE)</strong>. 11. <strong>4 memory audit kinds emitted</strong>. 12. <strong>PII scrubbed (query+chunk SHA256; embedding never in chain)</strong>. 13. <strong>RLS denies cross-tenant</strong>. 14. <strong>Trace_id preserved</strong>. 15. <strong>UNIQUE(doc, version, chunk_order)</strong>. 16. <strong>Append-only via REVOKE except DELETE</strong>. 17. <strong>Empty index returns empty array</strong>. 18. <strong>Query embedding cached 5min</strong>. 19. <strong>Bulk ingest async via FR-MCP-007 task</strong>. 20. <strong>Multilingual query (VN+EN mixed) supported</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn semantic_finds_paraphrase() {\n    let ctx = TestContext::with_doc(\"invoice issuance process\").await;\n    let r = ctx.semantic_search(\"how do I bill a customer\").await;\n    assert!(r.results.iter().any(|c| c.doc_id == ctx.doc_id));\n}\n\n#[tokio::test]\nasync fn invalidation_on_new_version() {\n    let ctx = TestContext::with_indexed_doc().await;\n    let original_chunks = ctx.fetch_chunks(ctx.doc_id).await;\n    ctx.create_new_version(ctx.doc_id, \"new content\").await;\n    ctx.run_ingest().await;\n    let chunks = ctx.fetch_chunks(ctx.doc_id).await;\n    let old_ones = chunks.iter().filter(|c| c.version_id == ctx.original_version).count();\n    assert_eq!(old_ones, 0);\n}\n\n#[tokio::test]\nasync fn top_k_returned() {\n    let ctx = TestContext::with_50_indexed_docs().await;\n    let r = ctx.semantic_search(\"test\").await;\n    assert_eq!(r.results.len(), 20);\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-AI-019 (memory Layer 2), FR-KB-001. <strong>Downstream:</strong> FR-KB-006 (rerank). <strong>Cross-module:</strong> FR-KB-002 (plaintext source), FR-MCP-007 (async ingest), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>BGE-M3 service down</td><td>retry</td><td>sev-2; fall back to lexical only</td><td>retry</td></tr><tr><td>Embedding dimension mismatch</td><td>validate</td><td>reject; sev-1</td><td>bug fix</td></tr><tr><td>pgvector extension missing</td><td>catch</td><td>sev-1</td><td>install</td></tr><tr><td>Ingest mid-doc fail</td><td>rollback</td><td>sev-2</td><td>re-ingest</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Large doc (&gt;100k tokens)</td><td>chunking handles</td><td>inherent</td><td>inherent</td></tr><tr><td>ivfflat index build slow</td><td>async</td><td>inherent</td><td>tune lists param</td></tr><tr><td>Query embedding cache miss</td><td>re-embed</td><td>inherent</td><td>inherent</td></tr><tr><td>Multilingual mixed query</td><td>BGE-M3 native</td><td>inherent</td><td>inherent</td></tr><tr><td>Index drift (stale chunks)</td><td>invalidation cron</td><td>sev-3</td><td>re-ingest</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 BGE-M3 model hosted via FR-AI-019 inference endpoint; service-side caching of embeddings.</li><li>§11.2 Chunker uses tree-sitter for code-aware splits; falls back to paragraph for prose.</li><li>§11.3 ivfflat: lists=floor(sqrt(row_count)); rebuilt monthly via maintenance cron.</li><li>§11.4 memory audit body: doc_id, version_id, chunk_count; query+text SHA256.</li><li>§11.5 Bulk ingest: large doc → chunked → batched embed calls (10 chunks per batch).</li></ul>\n<hr>\n<p><em>End of FR-KB-005 spec.</em></p>";
