window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EMAIL-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The EMAIL service <strong>MUST</strong> ship Genie-prefix routing at <code>services/email/src/genie/</code> matching subject prefix, calling FR-PORTAL-005 Branded Genie, proposing actions, queueing for user approval, 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> hook into <code>services/email/src/inbound_processor.rs</code> after message stored: call <code>genie::route(message)</code>.</li></ol>\n<ol><li><strong>MUST</strong> match prefix per DEC-1590 via <code>prefix_router.rs::matches(subject, tenant)</code>:</li></ol>\n<ul><li>Load <code>tenant.genie_prefix</code> (default \"Genie:\").</li><li>Case-insensitive starts-with match after stripping <code>Re:</code>/<code>Fwd:</code>.</li></ul>\n<ol><li><strong>MUST</strong> load context per DEC-1594 — brand pack (FR-PORTAL-002), CRM contact via FR-EMAIL-006 link, thread last-10-msgs, tenant tool list from FR-MCP-006.</li></ol>\n<ol><li><strong>MUST</strong> call Branded Genie at <code>portal_bridge.rs::propose(message, context)</code> — invokes FR-PORTAL-005 chat with system prompt: \"Read this email, propose 0-N actions. Output JSON [{kind, params, rationale}]\".</li></ol>\n<ol><li><strong>MUST</strong> validate proposed <code>genie_action_kind</code> against closed enum per DEC-1592.</li></ol>\n<ol><li><strong>MUST</strong> queue actions for user review per DEC-1593 — NEVER auto-execute.</li></ol>\n<ol><li><strong>MUST</strong> define <code>genie_sessions</code> and <code>genie_actions</code> tables at migration <code>0010</code>: ```sql CREATE TABLE genie_sessions ( session_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, message_id UUID NOT NULL, thread_id UUID NOT NULL, status TEXT NOT NULL DEFAULT 'proposing' CHECK (status IN ('proposing','awaiting_review','executing','completed','failed','dismissed')), started_at TIMESTAMPTZ NOT NULL DEFAULT now(), completed_at TIMESTAMPTZ, trace_id CHAR(32) ); ALTER TABLE genie_sessions ENABLE ROW LEVEL SECURITY; CREATE POLICY genie_sessions_rls ON genie_sessions USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON genie_sessions FROM cyberos_app; GRANT UPDATE (status, completed_at) ON genie_sessions TO cyberos_app;</li></ol>\n<p>   CREATE TABLE genie_actions (      action_id UUID PRIMARY KEY,      session_id UUID NOT NULL REFERENCES genie_sessions(session_id),      tenant_id UUID NOT NULL,      kind TEXT NOT NULL        CHECK (kind IN ('draft_reply','create_issue','summarize_thread','fetch_data','escalate_human','no_action')),      params JSONB NOT NULL,      rationale TEXT,      status TEXT NOT NULL DEFAULT 'proposed'        CHECK (status IN ('proposed','approved','executed','dismissed','failed')),      reviewed_by UUID,      reviewed_at TIMESTAMPTZ,      executed_at TIMESTAMPTZ,      result JSONB,      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    ALTER TABLE genie_actions ENABLE ROW LEVEL SECURITY;    CREATE POLICY genie_actions_rls ON genie_actions      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON genie_actions FROM cyberos_app;    GRANT UPDATE (status, reviewed_by, reviewed_at, executed_at, result) ON genie_actions TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> emit 6 memory audit kinds per DEC-1595. PII per FR-MEMORY-111: message body + AI output SHA-256 hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from inbound → prefix match → Genie → action queue → user approve → execute.</li></ol>\n<ol><li><strong>MUST</strong> add tenant config column to extend FR-EMAIL-001: ``<code>sql ALTER TABLE email_tenant_config ADD COLUMN genie_prefix TEXT DEFAULT 'Genie:'; ALTER TABLE email_tenant_config ADD COLUMN genie_enabled BOOLEAN DEFAULT false; </code>``</li></ol>\n<ol><li><strong>MUST NOT</strong> auto-execute action per DEC-1593.</li></ol>\n<ol><li><strong>MUST NOT</strong> bypass FR-MCP-006 tool gating when executing fetch_data action — only allowlisted tools per tenant.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why prefix routing (DEC-1590)?</strong> Lightweight opt-in per-message; doesn't intercept regular email flow.</p>\n<p><strong>Why action proposals not direct execution (DEC-1593)?</strong> AI hallucination risk on customer-facing sends; manual review is the gate.</p>\n<p><strong>Why 6 action kinds (DEC-1591)?</strong> Covers common Genie use cases — reply drafts, issue creation, thread summary, data lookup, human escalation, no-op (matches existing CDO workflows).</p>\n<p><strong>Why brand pack context (DEC-1594)?</strong> Branded Genie must speak in tenant voice; brand pack defines tone+language+disclaimers.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">GET    /v1/email/genie/sessions                  (list user's pending Genie reviews)\nGET    /v1/email/genie/sessions/{id}             (detail with proposed actions)\nPOST   /v1/email/genie/actions/{id}/approve      (execute)\nPOST   /v1/email/genie/actions/{id}/dismiss      (mark dismissed)\nPUT    /v1/email/genie/config                    (tenant prefix + enabled toggle)</code></pre>\n<p>Sample session detail:</p>\n<pre><code class=\"language-json\">{\n  \"session_id\": \"uuid\",\n  \"message_id\": \"uuid\",\n  \"status\": \"awaiting_review\",\n  \"actions\": [\n    {\n      \"action_id\": \"uuid\",\n      \"kind\": \"draft_reply\",\n      \"params\": {\"body\": \"Dear John, thanks for reaching out...\"},\n      \"rationale\": \"Customer asked about pricing; drafted standard response.\",\n      \"status\": \"proposed\"\n    },\n    {\n      \"action_id\": \"uuid\",\n      \"kind\": \"create_issue\",\n      \"params\": {\"project_id\": \"...\", \"title\": \"Schedule pricing demo for Acme\"},\n      \"rationale\": \"Follow-up needed in 24h.\",\n      \"status\": \"proposed\"\n    }\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Subject prefix triggers Genie</strong>. 2. <strong>Case-insensitive match</strong>. 3. <strong>Strips Re:/Fwd: before match</strong>. 4. <strong>Tenant prefix configurable</strong>. 5. <strong>Genie_enabled toggle respected (off → skip silently)</strong>. 6. <strong>Context loaded (brand + CRM + thread + tools)</strong>. 7. <strong>6 action kinds enum + cardinality test</strong>. 8. <strong>Actions queued, never auto-executed</strong>. 9. <strong>6 memory audit kinds emitted</strong>. 10. <strong>PII scrubbed (body/AI output SHA256)</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>fetch_data respects FR-MCP-006 gating</strong>. 14. <strong>Approve → execute (calls FR-EMAIL-009 send / FR-PROJ-001 create / etc.)</strong>. 15. <strong>Dismiss → status=dismissed (audit)</strong>. 16. <strong>AI failure → status=failed + sev-2</strong>. 17. <strong>Append-only sessions/actions tables</strong>. 18. <strong>Multiple actions per session executed in order</strong>. 19. <strong>Result of execution stored in actions.result</strong>. 20. <strong>Branded Genie call uses brand pack tone</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn prefix_triggers_genie() {\n    let ctx = TestContext::with_genie_enabled().await;\n    let msg = ctx.receive_inbound_with_subject(\"Genie: Draft a reply\").await;\n    let session = ctx.fetch_genie_session_for_msg(msg.id).await;\n    assert!(session.is_some());\n}\n\n#[tokio::test]\nasync fn never_auto_executes() {\n    let ctx = TestContext::with_genie_enabled().await;\n    let msg = ctx.receive_inbound_with_subject(\"Genie: Help me\").await;\n    let session = ctx.wait_for_session_proposing(msg.id).await;\n    let sent_emails = ctx.email_send_count().await;\n    let issues = ctx.issue_create_count().await;\n    assert_eq!(sent_emails, 0);\n    assert_eq!(issues, 0);\n    assert_eq!(session.status, \"awaiting_review\");\n}\n\n#[tokio::test]\nasync fn case_insensitive_prefix() {\n    let ctx = TestContext::with_genie_prefix(\"Genie:\").await;\n    let cases = [\"GENIE: help\", \"genie: help\", \"Genie: help\", \"Re: GENIE: reply\", \"Fwd: Genie: forward\"];\n    for subj in cases {\n        let msg = ctx.receive_inbound_with_subject(subj).await;\n        assert!(ctx.fetch_genie_session_for_msg(msg.id).await.is_some());\n    }\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-EMAIL-001, FR-PORTAL-005, FR-CUO-101. <strong>Cross-module:</strong> FR-PORTAL-002 (brand pack), FR-CRM-001 (contact), FR-MCP-006 (tool gating), FR-AI-003 (LLM), FR-MEMORY-111 (PII).</p>\n<h2>§8 — Sample payloads (see §3)</h2>\n<h2>§9 — Open questions</h2>\n<p>None blocking.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Genie disabled (tenant)</td><td>flag check</td><td>skip silently</td><td>inherent</td></tr><tr><td>Branded Genie unreachable</td><td>HTTP timeout</td><td>status=failed; sev-2</td><td>retry</td></tr><tr><td>AI returns invalid action kind</td><td>enum match</td><td>filter + sev-3 audit</td><td>inherent</td></tr><tr><td>User no email send permission</td><td>exec time</td><td>action fails, status=failed</td><td>request perm</td></tr><tr><td>Tool not in allowlist (fetch_data)</td><td>FR-MCP-006 gate</td><td>rejected</td><td>inherent</td></tr><tr><td>Prefix not configured</td><td>use default \"Genie:\"</td><td>inherent</td><td>inherent</td></tr><tr><td>Concurrent prefix match on same msg</td><td>UNIQUE on session_id+message_id</td><td>first wins</td><td>inherent</td></tr><tr><td>Approve already-executed action</td><td>status check</td><td>409</td><td>inherent</td></tr><tr><td>Brand pack missing</td><td>FR-PORTAL-002 fallback</td><td>use default tone</td><td>inherent</td></tr><tr><td>Genie context too large (&gt;50k tokens)</td><td>truncate</td><td>last 10 msgs only</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Prefix match regex: <code>^(re:|fwd:)?\\s*{escaped_prefix}\\s*</code> case-insensitive.</li><li>§11.2 Action proposer prompt includes JSON schema for output validation.</li><li>§11.3 Result column stores execution outcome (e.g. sent_message_id, created_issue_id).</li><li>§11.4 memory audit body: action kinds + counts; AI output SHA256.</li><li>§11.5 fetch_data executes via FR-MCP-006-gated MCP tools; tenant must have allowlisted them.</li></ul>\n<hr>\n<p><em>End of FR-EMAIL-008 spec.</em></p>";
