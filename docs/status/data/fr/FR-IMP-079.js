window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-IMP-079"]="<h2>§1</h2>\n<ol><li>Both docs jobs <strong>MUST</strong> ship through one implementation, <code>tools/docs-site/ship.sh</code> - the duplicated inline snippet is how the shared-staging race was born, and FR-IMP-074's rules-to-channels principle applies to the shippers themselves.</li><li>Staging <strong>MUST</strong> be per-run unique (<code>docs.new.${GITHUB_RUN_ID}.${GITHUB_RUN_ATTEMPT}</code>, <code>local$$</code> fallback): no shipper can ever name, and therefore never delete, another's in-flight staging.</li><li>The swap (<code>rm -rf docs &amp;&amp; mv &lt;stage&gt; docs</code>) <strong>MUST</strong> run under a remote <code>flock</code> on <code>~/cyberos/apps/console/.docs-ship.lock</code>: swaps serialize, the docs-absent window stays single-threaded and sub-second, last writer wins (all writers build the same main, so content is equivalent).</li><li>Abandoned staging dirs <strong>MUST</strong> be swept without endangering live ones: the flock'd section removes <code>docs.new*</code> entries untouched for 2h+ only - an in-flight extract continuously refreshes its dir mtime and completes in seconds. This also retires the legacy shared <code>docs.new</code> name on its first post-fix ship.</li><li>Transport invariants inherited unchanged: tar streamed over ssh (no runner tgz, no VPS /tmp - the scp era shipped a truncated archive), <code>set -euo pipefail</code>, size echo for the deploy log.</li></ol>\n<p><em>Lean profile: one script + two call-site swaps; the race, the fix, and the sweep semantics are all machine-verified in-session by a two-racer simulation of the exact remote command shape.</em></p>\n<h2>§5 (run 2026-07-13)</h2>\n<ul><li><code>bash -n ship.sh</code> PASS; both workflows YAML-parse post-edit. PASS</li><li>Two-racer simulation (exact remote command shape, tiny payloads, shared console dir): both exit 0, final <code>docs</code> = last swap's content, no cannot-stat. PASS (pre-fix design loses one racer by construction - observed live in #127)</li><li>Sweep semantics: pre-seeded stale <code>docs.new</code> (3h) swept; fresh foreign <code>docs.new.other.0</code> survived. PASS</li><li><code>flock</code> present in util-linux (verified in the ubuntu sandbox; the VPS is ubuntu - if ever absent, the step fails loud at \"command not found\", not silently).</li><li>Testing pass 2026-07-13 (post gate-1 \"approve all\"): two-racer simulation re-run green (both exit 0, last swap wins, sweep semantics intact); ship.sh bash -n + both workflows YAML re-verified.</li></ul>\n<h2>§9</h2>\n<ul><li>The docs-absent window during the swap (sub-second, now serialized) is accepted, as it was pre-fix. A symlink-flip scheme (docs -&gt; release dir, atomic <code>ln -sfn</code>) would remove it entirely; adopt only if a monitoring blip ever attributes to it.</li><li>deploy.yml's docs job and release.yml's docs job still both exist by design (FR-DOCS-003: tags refresh the site too); deduplicating the JOBS is out of scope - this FR dedupes the shipper.</li></ul>\n<h2>§10</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Recovery</th></tr></thead><tbody><tr><td>two shippers race (the #127 event)</td><td>none needed - per-run staging + flock make it a supported case</td><td>last swap wins; both green</td></tr><tr><td>shipper killed mid-extract</td><td>orphan staging dir on the VPS</td><td>swept by the next ship's 2h sweep</td></tr><tr><td>flock missing on a future VPS image</td><td>step fails loud: command not found</td><td>install util-linux / adjust script</td></tr><tr><td>sweep window vs a &gt;2h extract</td><td>impossible in practice (18M extracts in seconds); would surface as a swept-staging mv failure</td><td>raise -mmin threshold</td></tr><tr><td>release.yml tag ref predates ship.sh</td><td>docs job fails loud at \"no such file\"</td><td>re-tag current main (the standard sequence)</td></tr></tbody></table>\n<p><em>End of FR-IMP-079.</em></p>";
