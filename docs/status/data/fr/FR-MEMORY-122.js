window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-122"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>This FR makes day-1 wide capture real: it wires the first two live modules (AUTH and CHAT) to emit FR-MEMORY-121 interaction-events for every platform work-interaction, turns on the chat→brain audit link so chat activity chains into MEMORY, and defines the emitter contract so the remaining modules add emitters the same way. It introduces no new event shape — it produces events of the FR-MEMORY-121 shape. Each piece:</p>\n<ol><li><strong>MUST</strong> provide a shared emitter in a new crate <code>cyberos-capture</code>: a <code>Capturer</code> that holds the brain audit pool and a <code>capture()</code> method which builds an FR-MEMORY-121 <code>InteractionEvent</code> (via that FR's typed builder) and calls FR-MEMORY-121 <code>emit()</code>. Every module emits through this one path; no module constructs an <code>l1_audit_log</code> row directly. The <code>Capturer::capture</code> signature is best-effort: it returns the FR-MEMORY-121 <code>EmitOutcome</code>/<code>EmitError</code>, and callers log + swallow.</li><li><strong>MUST</strong> define a <code>CaptureEmitter</code> convention (DEC-2714): each module owns a thin <code>capture.rs</code> that exposes typed helpers (e.g. <code>emit_signed_in(...)</code>, <code>emit_message_created(...)</code>) translating that module's domain event into a built <code>InteractionEvent</code>. The helper set is the per-module surface; the crate is the shared mechanism. New modules add a <code>capture.rs</code>, not a new shape.</li><li><strong>MUST</strong> wire AUTH to emit, next to the existing <code>emit_token_issued</code> / <code>emit_token_failed</code> in <code>handlers.rs</code>:</li></ol>\n<ul><li><code>auth.signed_in</code> (<code>event_class: auth</code>, <code>source_channel</code> from the request, <code>target_ref: session{jti}</code>, <code>content_ref: none</code>, <code>attributes: {source_ip_hash16, method}</code> where method ∈ <code>password | oidc | passkey</code>) on a successful token issue.</li><li><code>auth.sign_in_failed</code> (<code>event_class: auth</code>, <code>content_ref: none</code>, <code>attributes: {reason, source_ip_hash16}</code>) on a failed attempt. <code>subject_id</code> is <code>null</code> when the email did not resolve (no person to attribute), mirroring <code>emit_token_failed</code>.</li><li>These reuse AUTH's existing audit pool (the same one <code>memory_bridge</code> writes to); no new connection.</li></ul>\n<ol><li><strong>MUST</strong> wire CHAT to emit, in <code>messages.rs</code> / <code>channels.rs</code> / <code>realtime.rs</code>:</li></ol>\n<ul><li><code>chat.message_created</code> (<code>event_class: content</code>, <code>target_ref: channel{id}</code> or <code>dm{id}</code>, <code>content_ref: pointer{store:\"chat_messages\", id:&lt;message_id&gt;}</code>, <code>attributes: {channel_kind, has_attachment}</code>).</li><li><code>chat.message_edited</code> (<code>content</code>, same target, <code>content_ref: pointer{chat_messages,id}</code>).</li><li><code>chat.message_deleted</code> (<code>content</code>, <code>content_ref: none</code> — the body is gone; the pointer would dangle).</li><li><code>chat.channel_created</code> (<code>admin</code>, <code>target_ref: channel{id}</code>).</li><li><code>chat.channel_joined</code> / <code>chat.channel_left</code> (<code>activity</code>, <code>target_ref: channel{id}</code>).</li><li><code>chat.dm_opened</code> (<code>activity</code>, <code>target_ref: dm{id}</code>).</li><li><code>chat.presence_changed</code> (<code>presence</code>, <code>target_ref: channel{id}</code>, <code>attributes: {state}</code> where state ∈ <code>online | offline</code>). Message bodies are NEVER inlined; the <code>content_ref</code> points at chat's own <code>chat_messages</code> row (migration 0005's attachment link is reused for the attachment flag only).</li></ul>\n<ol><li><strong>MUST</strong> turn ON the chat→brain audit link (DEC-2713). CHAT already reads <code>CHAT_AUDIT_DATABASE_URL</code> into <code>audit_pool</code> and logs <code>\"unset; chat audit events are logged, not chained\"</code> when absent (P0 left it unset). This FR sets that variable to the brain's audit DB (the MEMORY <code>l1_audit_log</code> database, Supabase Postgres in prod) so chat interaction-events chain into MEMORY, changes the <code>unset</code> log from <code>warn</code> to <code>info</code>, and documents the variable as required in production. The chat capture path routes through <code>cyberos-capture</code> → FR-MEMORY-121 <code>emit()</code> over that pool.</li><li><strong>MUST</strong> route every emitter through FR-MEMORY-121 <code>emit()</code> so the consent gate (FR-MEMORY-121 §1 #8) and validation apply uniformly. An emitter physically cannot skip the gate, because it has no other way to write the row. A subject who has not acknowledged the FR-EVAL-001 notice produces <code>Skipped</code> outcomes and zero rows, regardless of which module emitted.</li><li><strong>MUST</strong> keep capture best-effort and off the critical path: a capture failure (audit pool down, validation error, <code>Skipped</code>) MUST NOT fail or delay the underlying interaction — the sign-in still issues its token, the message still sends. Emitters call <code>capture()</code>, match the outcome for metrics, and swallow errors with a <code>tracing::warn!</code>, exactly as AUTH's <code>emit_token_issued</code> and chat's <code>audit::emit</code> already do.</li><li><strong>MUST</strong> dedup presence emits to the online/offline edges only: <code>chat.presence_changed{online}</code> is emitted when a subject's open-connection count for a channel goes 0→1, and <code>{offline}</code> when it goes 1→0 (the <code>Presence</code> map in <code>realtime.rs</code> already tracks the count). A subject opening a second tab does NOT emit another <code>online</code>. This keeps presence signal meaningful and the chain uncluttered.</li><li><strong>MUST</strong> provide a bounded backfill/replay path (<code>services/memory/src/interaction/backfill.rs</code>) that, for a subject who has since acknowledged the notice, replays recent chat history (a bounded window, default 30 days, operator-set) into <code>chat.message_created</code> interaction-events. Backfill is idempotent on <code>event_id</code> (deterministically derived from the source message id so a re-run is a no-op via FR-MEMORY-121 §1 #17), defaults to a dry-run that reports the count, and only writes on an explicit <code>--apply</code>. This recovers the pre-acknowledgment / pre-link history without double-counting.</li><li><strong>MUST</strong> derive backfill <code>event_id</code>s deterministically from the source row (e.g. UUIDv5 over <code>chat_messages.id</code>) so replaying the same message always yields the same <code>event_id</code>, and <code>occurred_at_ns</code> from the message's original timestamp (not replay time) so the backfilled event sits at the correct point in the person's history.</li><li><strong>MUST</strong> respect the consent gate in backfill too: a message is only backfilled for a subject who has acknowledged; messages authored by an unacknowledged subject are skipped (and counted), never written. Backfill calls the same <code>emit()</code> and so inherits the gate; it does not bypass it for historical data.</li><li><strong>MUST</strong> emit OTel metrics (in addition to the FR-MEMORY-121 counters): <code>memory_capture_emitter_calls_total{module, event_type, outcome}</code> (outcome ∈ <code>recorded | skipped_consent | invalid | emit_error</code>) and <code>memory_capture_backfill_events_total{outcome}</code>. These give a per-emitter view of live capture and of backfill progress.</li><li><strong>MUST</strong> tag <code>source_channel</code> from the real entry point: AUTH reads it from the request (web console vs API vs CLI), CHAT from the client (web/desktop/mobile/api), backfill emits <code>import</code>. This is what makes \"Daria messaged from mobile\" vs \"an API client posted\" distinguishable downstream.</li><li><strong>MUST</strong> set <code>trace_id</code> on emitted events when a request trace is in scope (FR-OBS-003 traceparent), so a captured interaction correlates with its request span and log line. When no trace is in scope (e.g. a websocket presence edge), <code>trace_id</code> is <code>null</code> — never fabricated.</li><li><strong>MUST</strong> document, in <code>docs/deploy/p0-google-chat-runbook.md</code>, that the chat→brain link is ON in production, the exact <code>CHAT_AUDIT_DATABASE_URL</code> it uses, the verify query (<code>SELECT count(*) FROM l1_audit_log WHERE event_type='memory.interaction_event' AND iev_module='chat'</code>), and the consent prerequisite (rows appear only for acknowledged subjects). Operators must be able to confirm capture is live and understand why an unacknowledged subject shows none.</li><li><strong>MUST</strong> keep AUTH and CHAT independent of MEMORY's HTTP liveness: capture writes go directly to the shared audit DB via <code>cyberos-capture</code> (the same direct-write pattern AUTH <code>memory_bridge</code> and the obs services already use), so capture does not couple a sign-in or a message send to the memory service being up.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a shared <code>cyberos-capture</code> crate (DEC-2714)?</strong> AUTH and CHAT (and soon PROJ, EMAIL, APP, MCP) all need to turn a domain event into the one interaction-event shape and write it the same way. Without a shared mechanism, each module reinvents the building + the best-effort write + the metric, and they drift. The crate holds the mechanism once; each module's <code>capture.rs</code> holds only the thin domain translation. A new module adds a <code>capture.rs</code>, not a new pattern.</p>\n<p><strong>Why wire AUTH and CHAT first?</strong> They are the two live modules in production today (P0 is sign-in + team chat). Sign-in and presence are the \"from the moment a person logs in\" signal the plan calls for, and chat is where most of the work conversation happens. Starting here makes capture real on day one with the modules that already have traffic, instead of waiting for modules that aren't built.</p>\n<p><strong>Why turn on the chat→brain link now (DEC-2713)?</strong> The brain plan names this exact gap: chat content is in chat's DB but, because P0 left <code>CHAT_AUDIT_DATABASE_URL</code> off, none of it is mirrored to the brain. The infrastructure is already there — chat reads the variable, holds an <code>audit_pool</code>, and writes through <code>cyberos-audit-chain</code> when it is set. Turning it on is a configuration flip plus routing chat's capture through the FR-MEMORY-121 emit path, not new plumbing. Until it is on, the brain has no chat signal at all.</p>\n<p><strong>Why route everything through <code>emit()</code> and forbid hand-built rows (DEC-2712)?</strong> The consent gate, the validation, and the schema only protect capture if there is no way around them. If an emitter could insert an <code>l1_audit_log</code> row directly, it could (by accident) capture an unacknowledged subject or write a malformed/raw-content row. Making <code>emit()</code> the only door means the governance property holds for every module by construction, not by everyone remembering.</p>\n<p><strong>Why best-effort, never blocking (§1 #7)?</strong> Capture must never be the reason a sign-in fails or a message is slow. The product comes first; the record is a side effect. AUTH and CHAT already treat their audit writes this way; this FR keeps that contract so capture is invisible to the user whose work it records.</p>\n<p><strong>Why dedup presence to edges (§1 #8)?</strong> Presence is useful signal (\"Stephen was online 09:00–18:00\") but a naive emit on every websocket open/close produces noise — a person with three tabs would emit three <code>online</code>s. Emitting only on the 0↔1 connection-count edge captures the meaningful transition and keeps the chain readable. The count is already tracked in <code>realtime.rs</code>.</p>\n<p><strong>Why backfill exists and why it is idempotent + consent-gated (§1 #9–#11)?</strong> Two reasons drive replay: chat has live production history from before the link was on, and a person who acknowledges the notice later should have their prior platform activity captured from the acknowledged window. Deterministic <code>event_id</code>s (UUIDv5 over the source message id) make a re-run a no-op, so backfill is safe to run repeatedly. Routing backfill through <code>emit()</code> means historical capture obeys the same consent rule as live capture — you do not get to capture someone's history just because it is in the past.</p>\n<p><strong>Why deterministic id + original timestamp in backfill (§1 #10)?</strong> If backfill used fresh ids and replay-time timestamps, a re-run would double-count and the events would sit at the wrong point in the person's timeline. Deriving the id from the message and the time from the original message keeps the backfilled history both idempotent and chronologically honest.</p>\n<p><strong>Why direct-write to the audit DB, not via the memory HTTP service (§1 #16)?</strong> AUTH <code>memory_bridge</code> and the obs services already write to <code>l1_audit_log</code> directly because the alternative — coupling a sign-in or a message send to memory's HTTP liveness — is worse. Capture follows the same rule: it shares the Postgres deployment and writes directly, so a memory-service blip never touches the critical path.</p>\n<p><strong>Why document the link + verify query in the runbook (§1 #15)?</strong> \"Is capture actually live?\" must be answerable in one query, and \"why does this person show no rows?\" must have an obvious answer (they have not acknowledged). The runbook is where the operator looks; the brain plan's whole trust argument depends on capture being visible and explainable, not a black box.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Shared crate</h3>\n<pre><code class=\"language-rust\">// services/shared/cyberos-capture/src/emitter.rs\nuse cyberos_memory::interaction::{emit, event::*, EmitOutcome, EmitError};\nuse sqlx::PgPool;\nuse uuid::Uuid;\n\n/// The shared capture mechanism. Holds the brain audit pool; turns a built InteractionEvent into a\n/// chained l1_audit_log row via FR-MEMORY-121 emit() (consent-gated, validated, best-effort).\n#[derive(Clone)]\npub struct Capturer {\n    pool: PgPool,\n}\n\nimpl Capturer {\n    pub fn new(pool: PgPool) -&gt; Self { Self { pool } }\n\n    /// Best-effort: returns the FR-MEMORY-121 outcome. Callers log + swallow on error;\n    /// the underlying interaction (sign-in, message send) MUST proceed regardless.\n    pub async fn capture(&amp;self, ev: &amp;InteractionEvent) -&gt; Result&lt;EmitOutcome, EmitError&gt; {\n        emit(&amp;self.pool, ev).await\n    }\n}\n\n/// A module's capture.rs implements typed helpers that build the event, then call Capturer::capture.\npub trait CaptureEmitter {\n    fn capturer(&amp;self) -&gt; &amp;Capturer;\n}</code></pre>\n<h3>AUTH emitter</h3>\n<pre><code class=\"language-rust\">// services/auth/src/capture.rs\nuse cyberos_capture::Capturer;\nuse cyberos_memory::interaction::event::*;\nuse uuid::Uuid;\n\npub async fn emit_signed_in(\n    cap: &amp;Capturer, tenant: Uuid, subject: Uuid, jti: &amp;str,\n    method: &amp;str, source: SourceChannel, ip_hash16: &amp;str, trace_id: Option&lt;String&gt;,\n) {\n    let ev = InteractionEvent::builder(Module::Auth, \"auth.signed_in\", EventClass::Auth)\n        .tenant(tenant).subject(subject).occurred_now()\n        .session(Uuid::nil())                              // jti string carried in target_ref/attributes\n        .target(TargetRef::Session { id: jti.to_string() })\n        .content(ContentRef::None)\n        .source(source).trace(trace_id)\n        .attr(\"method\", method).attr(\"source_ip_hash16\", ip_hash16)\n        .build();\n    match ev {\n        Ok(ev) =&gt; { let _ = cap.capture(&amp;ev).await; }       // best-effort\n        Err(e) =&gt; tracing::warn!(error = %e, \"auth.signed_in event build failed\"),\n    }\n}\n\npub async fn emit_sign_in_failed(\n    cap: &amp;Capturer, tenant: Uuid, subject: Option&lt;Uuid&gt;,\n    reason: &amp;str, source: SourceChannel, ip_hash16: &amp;str,\n) {\n    let mut b = InteractionEvent::builder(Module::Auth, \"auth.sign_in_failed\", EventClass::Auth)\n        .tenant(tenant).occurred_now()\n        .target(TargetRef::None).content(ContentRef::None)\n        .source(source).attr(\"reason\", reason).attr(\"source_ip_hash16\", ip_hash16);\n    if let Some(s) = subject { b = b.subject(s); }          // null when the email didn't resolve\n    if let Ok(ev) = b.build() { let _ = cap.capture(&amp;ev).await; }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/handlers.rs  (added next to the existing emit_token_issued call, ~line 2126)\n//\n// FR-MEMORY-122 §1 #3 — emit the auth.signed_in interaction-event alongside the audit token row.\n// Same pool as memory_bridge; best-effort; does not gate token issuance.\ncrate::capture::emit_signed_in(\n    &amp;state.capturer, tenant_id, sub_id, &amp;jti, method, source_channel,\n    &amp;source_ip_hash16, request_trace_id(),\n).await;</code></pre>\n<h3>CHAT emitter (message create)</h3>\n<pre><code class=\"language-rust\">// services/chat/src/capture.rs\nuse cyberos_capture::Capturer;\nuse cyberos_memory::interaction::event::*;\nuse uuid::Uuid;\n\npub async fn emit_message_created(\n    cap: &amp;Capturer, tenant: Uuid, author: Uuid, channel_id: &amp;str, channel_kind: &amp;str,\n    message_id: &amp;str, has_attachment: bool, source: SourceChannel, trace_id: Option&lt;String&gt;,\n) {\n    let target = match channel_kind {\n        \"dm\" =&gt; TargetRef::Dm { id: channel_id.to_string() },\n        _    =&gt; TargetRef::Channel { id: channel_id.to_string() },\n    };\n    let ev = InteractionEvent::builder(Module::Chat, \"chat.message_created\", EventClass::Content)\n        .tenant(tenant).subject(author).occurred_now()\n        .target(target)\n        // §1 #4: pointer to chat's own row, NEVER the raw body.\n        .content(ContentRef::Pointer { store: \"chat_messages\".into(), id: message_id.to_string() })\n        .source(source).trace(trace_id)\n        .attr(\"channel_kind\", channel_kind).attr(\"has_attachment\", has_attachment)\n        .build();\n    if let Ok(ev) = ev { let _ = cap.capture(&amp;ev).await; }   // best-effort; send already happened\n}</code></pre>\n<pre><code class=\"language-rust\">// services/chat/src/realtime.rs  (presence edge — §1 #8: emit only on 0↔1)\n// `presence.join` already returns whether this open brought the subject online (count 0→1).\nif st.presence.join(channel, me) {                            // returns true only on the 0→1 edge\n    chat_capture::emit_presence(&amp;st.capturer, tenant, me, channel, \"online\", None).await;\n    // ... existing ChatEvent::Presence broadcast ...\n}</code></pre>\n<h3>Turning ON the chat→brain link</h3>\n<pre><code class=\"language-rust\">// services/chat/src/main.rs  (the audit_pool already exists; DEC-2713 makes it the brain link)\nlet audit_pool = match std::env::var(\"CHAT_AUDIT_DATABASE_URL\") {\n    Ok(url) =&gt; Some(\n        sqlx::postgres::PgPoolOptions::new().max_connections(4).connect(&amp;url).await?,\n    ),\n    Err(_) =&gt; {\n        // Was warn! (\"logged, not chained\"); now info! — but REQUIRED in production (runbook §1 #15).\n        tracing::info!(\"CHAT_AUDIT_DATABASE_URL unset; chat interaction-events are logged, not chained \\\n                        (dev/local only — production MUST set this to the brain audit DB)\");\n        None\n    }\n};\n// AppState gains: capturer: audit_pool.clone().map(cyberos_capture::Capturer::new)</code></pre>\n<pre><code class=\"language-yaml\"># deploy/vps/docker-compose.yml  (chat service env — DEC-2713)\n    environment:\n      # ... existing ...\n      # The brain link: chat interaction-events chain into MEMORY's l1_audit_log (Supabase audit DB).\n      CHAT_AUDIT_DATABASE_URL: ${SUPABASE_AUDIT_DATABASE_URL}</code></pre>\n<h3>Backfill</h3>\n<pre><code class=\"language-rust\">// services/memory/src/interaction/backfill.rs\nuse cyberos_memory::interaction::{emit, event::*, EmitOutcome};\nuse sqlx::PgPool;\nuse uuid::Uuid;\n\n/// Replays recent chat history into chat.message_created interaction-events for acknowledged subjects.\n/// Idempotent (§1 #10: UUIDv5 event_id over the source message id); dry-run unless `apply`.\npub async fn backfill_chat(\n    chat_pool: &amp;PgPool, audit_pool: &amp;PgPool, tenant: Uuid, window_days: u32, apply: bool,\n) -&gt; anyhow::Result&lt;BackfillReport&gt; {\n    let rows = recent_messages(chat_pool, tenant, window_days).await?;   // (id, author, channel, kind, ts_ns, has_attachment)\n    let mut report = BackfillReport::default();\n    for m in rows {\n        let event_id = Uuid::new_v5(&amp;CAPTURE_NAMESPACE, m.id.as_bytes());  // deterministic\n        let ev = InteractionEvent {\n            schema_version: SCHEMA_VERSION, event_id, tenant_id: tenant,\n            subject_id: Some(m.author), occurred_at_ns: m.ts_ns,           // original time, not now\n            module: Module::Chat, event_type: \"chat.message_created\".into(),\n            event_class: EventClass::Content,\n            target_ref: TargetRef::Channel { id: m.channel.clone() },\n            content_ref: ContentRef::Pointer { store: \"chat_messages\".into(), id: m.id.clone() },\n            session_id: None, trace_id: None, source_channel: SourceChannel::Import,\n            attributes: Default::default(),\n        };\n        report.seen += 1;\n        if !apply { continue; }\n        match emit(audit_pool, &amp;ev).await {                  // consent-gated + idempotent (§1 #11)\n            Ok(EmitOutcome::Recorded { .. }) =&gt; report.recorded += 1,\n            Ok(EmitOutcome::Skipped { .. })  =&gt; report.skipped_consent += 1,\n            Err(_)                           =&gt; report.errors += 1,\n        }\n    }\n    Ok(report)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Shared emitter chains via emit()</strong> — <code>Capturer::capture</code> of a built event inserts one <code>l1_audit_log</code> interaction-event row; no module writes the row directly (AC for §1 #1).</li><li><strong>AUTH signed_in emitted</strong> — a successful token issue for an acknowledged subject produces one <code>auth.signed_in</code> interaction-event with <code>event_class='auth'</code> and <code>source_ip_hash16</code> in attributes (AC for §1 #3).</li><li><strong>AUTH sign_in_failed with null subject</strong> — a failed attempt for an unresolved email produces <code>auth.sign_in_failed</code> with <code>subject_id=null</code> (AC for §1 #3).</li><li><strong>CHAT message_created is a pointer, not raw</strong> — sending a message produces <code>chat.message_created</code> whose <code>content_ref</code> is <code>pointer{store:\"chat_messages\", id}</code>; the row contains no message text (AC for §1 #4).</li><li><strong>CHAT edit/delete emitted</strong> — editing emits <code>chat.message_edited</code> (pointer); deleting emits <code>chat.message_deleted</code> with <code>content_ref:none</code> (AC for §1 #4).</li><li><strong>Channel + DM activity emitted</strong> — creating/joining/leaving a channel and opening a DM each emit the matching event with the right <code>target_ref</code> (AC for §1 #4).</li><li><strong>Chat→brain link ON</strong> — with <code>CHAT_AUDIT_DATABASE_URL</code> set to the brain DB, chat interaction-events appear in <code>l1_audit_log</code>; with it unset, chat logs the info line and writes none (AC for §1 #5).</li><li><strong>Consent gate honored by emitters</strong> — an unacknowledged subject sending a message produces a <code>Skipped</code> outcome and zero rows; an acknowledged subject produces a row (AC for §1 #6).</li><li><strong>Capture never blocks the interaction</strong> — with the audit pool down, the sign-in still issues its token and the message still sends; capture logs a warn (AC for §1 #7).</li><li><strong>Presence dedup to edges</strong> — opening two tabs emits one <code>chat.presence_changed{online}</code>; closing the last emits one <code>{offline}</code>; the middle close emits none (AC for §1 #8).</li><li><strong>Backfill is idempotent</strong> — running backfill <code>--apply</code> twice over the same window yields one interaction-event per source message (AC for §1 #9/#10).</li><li><strong>Backfill uses original timestamp</strong> — a backfilled event's <code>occurred_at_ns</code> equals the source message's timestamp, not replay time (AC for §1 #10).</li><li><strong>Backfill respects consent</strong> — a message by an unacknowledged subject is counted but not written during backfill (AC for §1 #11).</li><li><strong>Emitter metrics</strong> — <code>memory_capture_emitter_calls_total{module=\"chat\", event_type=\"chat.message_created\", outcome=\"recorded\"}</code> increments on a recorded message (AC for §1 #12).</li><li><strong>source_channel reflects entry point</strong> — a web-console sign-in tags <code>web</code>; a backfilled event tags <code>import</code> (AC for §1 #13).</li><li><strong>trace_id correlation</strong> — a message sent within a traced request carries that <code>trace_id</code>; a presence edge carries <code>null</code> (AC for §1 #14).</li><li><strong>Runbook documents the link + verify query</strong> — <code>p0-google-chat-runbook.md</code> states the link is ON, the variable, the verify query, and the consent prerequisite (AC for §1 #15).</li><li><strong>Direct-write decoupling</strong> — capture writes succeed with the memory HTTP service stopped (it writes to the audit DB directly) (AC for §1 #16).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/capture_signin_test.rs\n#[tokio::test]\nasync fn signin_emits_interaction_event_for_acknowledged_subject() {\n    let env = AuthTestEnv::new().await;\n    env.ack_notice(env.tenant(), env.alice()).await;\n    let _token = env.password_grant(env.alice(), \"web\").await.unwrap();   // issues + captures\n    let row: (String, String) = sqlx::query_as(\n        \"SELECT iev_event_type, iev_event_class FROM l1_audit_log\n         WHERE event_type='memory.interaction_event' AND subject_id=$1\")\n        .bind(env.alice()).fetch_one(&amp;env.audit_pool).await.unwrap();\n    assert_eq!(row.0, \"auth.signed_in\");\n    assert_eq!(row.1, \"auth\");\n}\n\n#[tokio::test]\nasync fn signin_for_unacknowledged_subject_captures_nothing() {\n    let env = AuthTestEnv::new().await;                                   // no ack\n    let _token = env.password_grant(env.bob(), \"web\").await.unwrap();     // token still issues\n    let n: i64 = sqlx::query_scalar(\n        \"SELECT COUNT(*) FROM l1_audit_log WHERE event_type='memory.interaction_event' AND subject_id=$1\")\n        .bind(env.bob()).fetch_one(&amp;env.audit_pool).await.unwrap();\n    assert_eq!(n, 0);                                                     // gate held\n}\n\n#[tokio::test]\nasync fn signin_succeeds_even_when_audit_pool_down() {\n    let env = AuthTestEnv::new_with_dead_audit_pool().await;\n    env.ack_notice(env.tenant(), env.alice()).await;\n    let token = env.password_grant(env.alice(), \"web\").await;             // must still succeed\n    assert!(token.is_ok(), \"sign-in must not fail because capture failed\");\n}</code></pre>\n<pre><code class=\"language-python\"># services/chat/tests/smoke_capture.py  (live chat service, brain link ON)\ndef test_message_create_edit_delete_emit_interaction_events(chat, audit_db, alice_token):\n    ack_notice(audit_db, TENANT, ALICE)                       # pass the gate\n    ch = chat.create_channel(alice_token, \"general\")\n    msg = chat.send_message(alice_token, ch, \"hello team\")    # body stays in chat's DB\n    chat.edit_message(alice_token, msg, \"hello everyone\")\n    chat.delete_message(alice_token, msg)\n\n    rows = audit_db.query(\n        \"SELECT iev_event_type, body::jsonb-&gt;'payload'-&gt;'content_ref'-&gt;&gt;'kind' AS cref \"\n        \"FROM l1_audit_log WHERE event_type='memory.interaction_event' AND iev_module='chat' \"\n        \"AND subject_id=%s ORDER BY seq\", (ALICE,))\n    kinds = [r[0] for r in rows]\n    assert \"chat.channel_created\" in kinds\n    assert \"chat.message_created\" in kinds\n    assert \"chat.message_edited\"  in kinds\n    assert \"chat.message_deleted\" in kinds\n    # created/edited point at chat's row; deleted has no content_ref\n    created = next(r for r in rows if r[0] == \"chat.message_created\")\n    assert created[1] == \"pointer\"                            # never raw body\n    deleted = next(r for r in rows if r[0] == \"chat.message_deleted\")\n    assert deleted[1] == \"none\"\n\ndef test_presence_dedup_to_edges(chat, audit_db, alice_token):\n    ack_notice(audit_db, TENANT, ALICE)\n    ch = chat.create_channel(alice_token, \"general\")\n    ws1 = chat.connect_ws(alice_token, ch)                    # 0→1: emit online\n    ws2 = chat.connect_ws(alice_token, ch)                    # 1→2: emit nothing\n    ws2.close()                                               # 2→1: emit nothing\n    ws1.close()                                               # 1→0: emit offline\n    states = audit_db.query(\n        \"SELECT body::jsonb-&gt;'payload'-&gt;'attributes'-&gt;&gt;'state' FROM l1_audit_log \"\n        \"WHERE iev_event_type='chat.presence_changed' AND subject_id=%s ORDER BY seq\", (ALICE,))\n    assert [s[0] for s in states] == [\"online\", \"offline\"]</code></pre>\n<pre><code class=\"language-rust\">// services/memory/tests/interaction_backfill_test.rs\n#[tokio::test]\nasync fn backfill_is_idempotent_and_keeps_original_time() {\n    let env = BackfillTestEnv::new().await;\n    env.ack_notice(env.tenant(), env.alice()).await;\n    env.seed_chat_messages(env.alice(), 5, /*at*/ DAYS_AGO_3).await;\n\n    let r1 = backfill_chat(&amp;env.chat_pool, &amp;env.audit_pool, env.tenant(), 30, true).await.unwrap();\n    let r2 = backfill_chat(&amp;env.chat_pool, &amp;env.audit_pool, env.tenant(), 30, true).await.unwrap();\n    assert_eq!(r1.recorded, 5);\n    assert_eq!(r2.recorded, 0, \"re-run is a no-op (idempotent event_id)\");\n\n    let n: i64 = sqlx::query_scalar(\n        \"SELECT COUNT(*) FROM l1_audit_log WHERE iev_event_type='chat.message_created' AND subject_id=$1\")\n        .bind(env.alice()).fetch_one(&amp;env.audit_pool).await.unwrap();\n    assert_eq!(n, 5);\n    let ts: i64 = sqlx::query_scalar(\n        \"SELECT (body::jsonb-&gt;'payload'-&gt;&gt;'occurred_at_ns')::bigint FROM l1_audit_log\n         WHERE iev_event_type='chat.message_created' AND subject_id=$1 ORDER BY seq LIMIT 1\")\n        .bind(env.alice()).fetch_one(&amp;env.audit_pool).await.unwrap();\n    assert!(ts &lt; now_ns() - 2 * DAY_NS, \"backfilled at original time, not replay time\");\n}\n\n#[tokio::test]\nasync fn backfill_skips_unacknowledged_authors() {\n    let env = BackfillTestEnv::new().await;                   // bob never acknowledged\n    env.seed_chat_messages(env.bob(), 3, DAYS_AGO_3).await;\n    let r = backfill_chat(&amp;env.chat_pool, &amp;env.audit_pool, env.tenant(), 30, true).await.unwrap();\n    assert_eq!(r.seen, 3);\n    assert_eq!(r.recorded, 0);\n    assert_eq!(r.skipped_consent, 3);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3 (shared crate, AUTH + CHAT emitters, the link flip, backfill). Each module's <code>capture.rs</code> is the per-module surface; <code>cyberos-capture</code> is the shared mechanism; everything routes through FR-MEMORY-121 <code>emit()</code>.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-121</strong> — the event shape, the typed builder, <code>emit()</code>, the consent gate, and the contract these emitters produce/obey. This FR adds no shape; it produces FR-MEMORY-121 events.</li><li><strong>FR-AUTH-002 / FR-AUTH-004</strong> — the subjects + sessions AUTH attributes events to; the existing audit pool + <code>emit_token_issued</code>/<code>_failed</code> call sites the new emitters sit next to.</li><li><strong>FR-CHAT-101</strong> — the cyberos-chat service (channels/DMs/messages/attachments/presence) the chat emitters hook into; the existing <code>CHAT_AUDIT_DATABASE_URL</code> / <code>audit_pool</code> plumbing DEC-2713 turns on.</li><li><strong>FR-EVAL-001</strong> — owns the acknowledgment ledger the consent gate reads (via FR-MEMORY-121); capture for a subject begins only after acknowledgment.</li><li><strong>FR-OBS-003</strong> — the traceparent <code>trace_id</code> emitters attach when a request trace is in scope.</li><li><strong>Downstream:</strong> FR-MEMORY-123 (ingestion consumes the events these emitters produce); FR-APP-005 (the audit viewer surfaces them).</li><li>Crates: <code>sqlx@0.7</code>, <code>serde</code>, <code>uuid</code> (v5 + v7), <code>chrono</code>, <code>tracing</code>, <code>cyberos-memory</code> (interaction module), <code>cyberos-capture</code> (new).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>auth.signed_in (emitted by AUTH next to the token row)</h3>\n<pre><code class=\"language-json\">{\n  \"event_type\": \"memory.interaction_event\",\n  \"payload\": {\n    \"schema_version\": 1,\n    \"event_id\": \"018f9c2a-7e10-7c3b-9a44-6b1d2e3f4a55\",\n    \"tenant_id\": \"cyberskill-tenant-uuid\",\n    \"subject_id\": \"stephen-subject-uuid\",\n    \"occurred_at_ns\": 1782950400000000000,\n    \"module\": \"auth\",\n    \"event_type\": \"auth.signed_in\",\n    \"event_class\": \"auth\",\n    \"target_ref\": { \"kind\": \"session\", \"id\": \"jti-abc123\" },\n    \"content_ref\": { \"kind\": \"none\" },\n    \"session_id\": null,\n    \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n    \"source_channel\": \"web\",\n    \"attributes\": { \"method\": \"oidc\", \"source_ip_hash16\": \"9f86d081884c7d65\" }\n  }\n}</code></pre>\n<h3>chat.message_created (emitted by CHAT; body stays in chat's DB)</h3>\n<pre><code class=\"language-json\">{\n  \"event_type\": \"memory.interaction_event\",\n  \"payload\": {\n    \"schema_version\": 1,\n    \"event_id\": \"018f9c2a-9b22-7e88-8c01-11aa22bb33cc\",\n    \"tenant_id\": \"cyberskill-tenant-uuid\",\n    \"subject_id\": \"daria-subject-uuid\",\n    \"occurred_at_ns\": 1782950460000000000,\n    \"module\": \"chat\",\n    \"event_type\": \"chat.message_created\",\n    \"event_class\": \"content\",\n    \"target_ref\": { \"kind\": \"channel\", \"id\": \"general-channel-uuid\" },\n    \"content_ref\": { \"kind\": \"pointer\", \"store\": \"chat_messages\", \"id\": \"msg-7e57c0de\" },\n    \"session_id\": \"jti-def456\",\n    \"trace_id\": null,\n    \"source_channel\": \"web\",\n    \"attributes\": { \"channel_kind\": \"channel\", \"has_attachment\": false }\n  }\n}</code></pre>\n<h3>Backfill report (operator output)</h3>\n<pre><code class=\"language-text\">backfill_chat tenant=cyberskill window_days=30 apply=true\n  seen=412  recorded=388  skipped_consent=24  errors=0\n  (24 messages authored by subjects who have not acknowledged the notice — not captured)</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>PROJ / EMAIL / APP / MCP emitters — added as those modules come online, each via its own <code>capture.rs</code> against this FR's contract (the emitter convention is defined here; the wiring lands per module).</li><li>A batched/async emit channel for very high-frequency emitters — slice 3; the synchronous best-effort path is sufficient for AUTH + CHAT volumes at P0 scale.</li><li>Backfilling AUTH sign-in history — not meaningful (sign-ins were not durably recorded pre-link beyond the token rows); chat is the history worth replaying.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Brain audit pool unreachable</td><td>sqlx error in capture</td><td><code>EmitError</code>; emitter logs warn; sign-in/message proceed</td><td>Self-heals; OBS alarm on sustained drop</td></tr><tr><td><code>CHAT_AUDIT_DATABASE_URL</code> unset in prod</td><td>startup info line + runbook check</td><td>chat captures nothing; logged not chained</td><td>Operator sets the variable (runbook §1 #15)</td></tr><tr><td>Subject not acknowledged</td><td><code>Skipped</code> outcome</td><td>no row; counted in skipped_consent metric</td><td>By design; subject acknowledges, capture begins</td></tr><tr><td>Emitter inlines raw body (bug)</td><td>FR-MEMORY-121 size cap + body-bytes histogram</td><td><code>Invalid</code> at the limit; metric flags below</td><td>Author fixes emitter to use content_ref</td></tr><tr><td>Presence emit on every websocket (bug)</td><td>edge-dedup in realtime.rs</td><td>only 0↔1 edges emit</td><td>By design (§1 #8)</td></tr><tr><td>Backfill re-run</td><td>deterministic event_id unique index</td><td>second run no-op</td><td>By design (§1 #10)</td></tr><tr><td>Backfill with <code>--apply</code> on huge window</td><td>dry-run-first + bounded window</td><td>operator sees count before writing</td><td>By design; operator narrows window</td></tr><tr><td>Capture latency on hot path</td><td>best-effort, off critical path</td><td>interaction unaffected; capture may lag/drop</td><td>By design (§1 #7)</td></tr><tr><td>Message deleted then capture replays</td><td>message_deleted has content_ref:none</td><td>no dangling pointer</td><td>By design (§1 #4)</td></tr><tr><td>trace_id fabricated for ws events</td><td>null-when-absent rule</td><td>trace_id null, never fake</td><td>By design (§1 #14)</td></tr><tr><td>Two emitters for one domain event (bug)</td><td>FR-MEMORY-121 event_id unique index</td><td>one wins; SEV-3 warn</td><td>Author fixes call site</td></tr><tr><td>AUTH audit pool and brain pool differ</td><td>both are the same l1_audit_log DB</td><td>rows land together</td><td>By design (shared deployment)</td></tr><tr><td>Chat DB and audit DB out of sync (pointer dangles)</td><td>resolve-time check in consumer</td><td>consumer treats missing target as redacted</td><td>FR-MEMORY-123 handles gracefully</td></tr><tr><td>Source_channel unknown (legacy client)</td><td>default mapping</td><td>falls back to <code>api</code></td><td>Client sends explicit channel</td></tr><tr><td>Backfill author no longer a subject</td><td>subject FK</td><td>row skipped + counted</td><td>By design</td></tr><tr><td>Notice version bumped mid-backfill</td><td>gate re-reads</td><td>newly-unacknowledged authors skipped</td><td>By design (governance)</td></tr><tr><td>OBS exporter down</td><td>buffered then dropped</td><td>metric gap; rows still written</td><td>Restore FR-OBS-001</td></tr><tr><td>Websocket churn floods presence edges</td><td>each edge is one row</td><td>bounded by real online/offline transitions</td><td>By design</td></tr><tr><td>Capture added to a module without capture.rs</td><td>compile (no helper)</td><td>module simply emits nothing yet</td><td>Add the module's capture.rs</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>This FR produces FR-MEMORY-121 events; it defines no new shape. The shared <code>cyberos-capture</code> crate is the mechanism (build → <code>emit()</code>); each module's <code>capture.rs</code> is the thin domain translation. New modules add a <code>capture.rs</code>, not a new pattern — that is the whole DEC-2714 point.</li><li>AUTH reuses its existing audit pool (the one <code>memory_bridge</code> already writes to). The <code>auth.signed_in</code> / <code>auth.sign_in_failed</code> emits sit literally next to <code>emit_token_issued</code> / <code>emit_token_failed</code> so the same request data (jti, subject, ip hash, reason) is in hand; no new connection, no new request plumbing.</li><li>CHAT already had the brain link plumbing from P0 — it reads <code>CHAT_AUDIT_DATABASE_URL</code>, holds an <code>audit_pool</code>, and writes through <code>cyberos-audit-chain</code>. P0 deliberately left the variable unset (the warn line). DEC-2713 is therefore a configuration flip plus routing chat's capture through <code>emit()</code>, not new infrastructure. The deploy compose sets it to the Supabase audit DB.</li><li>Message bodies never leave chat's database. The emitter writes <code>content_ref: pointer{store:\"chat_messages\", id}</code>; the BRAIN holds the pointer, chat holds the content under chat's RLS. A consumer that needs the text resolves the pointer for a viewer who already has access. <code>message_deleted</code> uses <code>content_ref:none</code> so there is no dangling pointer to a removed row.</li><li>Presence is deduped to the online/offline edges in <code>realtime.rs</code>, where the <code>Presence</code> connection-count map already lives. <code>presence.join</code> returns true only on the 0→1 edge; the offline emit hangs off the symmetric 1→0 edge. A person with three tabs produces one <code>online</code>, not three.</li><li>Backfill exists because chat has live production history from before the link was on, and because a person who acknowledges later deserves their acknowledged-window activity captured. It is idempotent (UUIDv5 <code>event_id</code> over the source message id), defaults to a dry-run that reports the count, and only writes on <code>--apply</code>. It routes through <code>emit()</code>, so historical capture obeys the same consent gate as live capture — you cannot backfill someone who never acknowledged.</li><li>Capture is best-effort everywhere. A failed capture logs a <code>tracing::warn!</code> and is swallowed; the sign-in still issues its token, the message still sends. This is the same contract AUTH and CHAT already hold for their audit writes, kept so capture is invisible to the user whose work it records.</li><li>Direct-write to the audit DB (not via the memory HTTP service) follows the existing AUTH <code>memory_bridge</code> / obs-services pattern: the services share one Postgres deployment, so capture writes go straight to <code>l1_audit_log</code> and never couple a critical-path interaction to memory's HTTP liveness.</li><li>The runbook update is load-bearing for trust: \"is capture live?\" is one query (<code>count(*) ... WHERE iev_module='chat'</code>), and \"why does this person show nothing?\" has one answer (they have not acknowledged). The brain plan's trust argument depends on capture being visible and explainable.</li><li><code>eu_ai_act_risk_class: limited</code> and the personal-data handling are carried in the AI Risk Assessment below: this FR is where employee work-activity actually starts being recorded, so the privacy posture (platform-only, content_ref-not-raw, consent-gated, per-tenant RLS, no autonomous decision) is stated explicitly here, not only in the schema FR.</li></ul>\n<hr>\n<h2>AI Risk Assessment</h2>\n<ul><li><strong>Why limited:</strong> this FR is the point where real employee work-activity begins flowing into the BRAIN — sign-ins, presence, and chat activity for CyberSkill staff — which is the data foundation for the later AI-assisted evaluation (FR-EVAL-003/004). The emitters run no model and make no decision, but they feed a limited-risk AI use (workplace evaluation support), so they are classified and governed as part of that chain.</li><li><strong>Personal data captured, and the hard limits:</strong> who signed in (and from where, as a hashed IP prefix), who was online when, and who created/edited/deleted which message in which channel/DM. That is platform work-interaction metadata plus pointers to content — NOT keystroke logging, NOT screen capture, NOT anything from private life (DEC-2711). Message bodies stay in chat's own database; the captured row carries a <code>content_ref</code> pointer, never the text.</li><li><strong>Consent before capture (enforced, not promised):</strong> every emitter writes only through FR-MEMORY-121 <code>emit()</code>, which is hard-gated on the FR-EVAL-001 notice acknowledgment. There is no emit path that bypasses the gate (hand-built audit rows are forbidden by <code>disallowed_tools</code>), so no subject is captured before they have acknowledged the monitoring notice — live or in backfill. This is disclosed monitoring made structural.</li><li><strong>Minimisation + purpose limitation:</strong> emitters carry only the bounded attributes their event needs (e.g. <code>channel_kind</code>, <code>method</code>, <code>source_ip_hash16</code>), reference content rather than copying it, and inherit FR-MEMORY-121's 2 KiB attributes cap and body-bytes watch. The purpose is to record the work, not to surveil the person; the closed event vocabulary bounds what can be recorded.</li><li><strong>Access + tenancy:</strong> captured events are <code>l1_audit_log</code> rows and inherit the per-tenant RLS, so a tenant's records never cross to another. Who may read them (manager + HR + the employee for their own record) is enforced by the consuming surfaces (FR-EVAL-005), not loosened here.</li><li><strong>Operator transparency:</strong> the runbook documents that the chat→brain link is ON, the exact variable, the one-line verify query, and the consent prerequisite — so capture is auditable and explainable rather than a silent background process.</li><li><strong>No autonomous decision:</strong> these emitters record interactions; they never score, rank, or decide anything about a person. Every consequential use runs through the human-in-the-loop evaluation workflow (FR-EVAL-004).</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-122.</em></p>";
