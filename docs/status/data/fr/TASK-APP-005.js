window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-APP-005"]="<h2>§1 — Description</h2>\n<ol><li>CyberOS <strong>MUST</strong> gain two independent Linux app-store distribution paths — Snap Store and Flathub — layered on top of the existing <code>.deb</code>/<code>.rpm</code>/<code>.AppImage</code> GitHub-Releases artifacts (confirmed produced today by the <code>desktop</code> job's <code>ubuntu-latest</code> matrix leg in <code>.github/workflows/release.yml</code>, lines 69–139), not replacing them.</li></ol>\n<ol><li>The two paths <strong>MUST</strong> be treated as architecturally distinct, not parallel instances of the same pattern used in TASK-APP-003/TASK-APP-004, because their submission mechanics genuinely differ: <strong>Snap Store</strong> accepts a CI-built, CI-uploaded <code>.snap</code> artifact via <code>snapcraft upload</code> (a CI-automatable, credential-gated upload, matching the TASK-APP-003/004 pattern); <strong>Flathub</strong> does <strong>NOT</strong> accept a pre-built artifact upload at all — Flathub's own build infrastructure clones a submitted manifest repository and builds the Flatpak itself, so CyberOS's role is limited to maintaining a correct, buildable manifest and (with Stephen's explicit per-instance approval) opening a pull request against the external <code>flathub/flathub</code> repository.</li></ol>\n<ol><li><code>apps/desktop/src-tauri/snap/snapcraft.yaml</code> <strong>MUST</strong> declare <code>confinement: strict</code> (not <code>devmode</code> or <code>classic</code>) using the <code>gnome</code> extension for <code>base: core22</code>, which provisions the WebKitGTK/GTK runtime libraries a Tauri app needs without CyberOS having to stage them manually — this mirrors the exact library set the existing <code>Linux build deps</code> CI step already installs for the GitHub-Releases build (<code>libwebkit2gtk-4.1-dev</code>, <code>libappindicator3-dev</code>, <code>librsvg2-dev</code>), confirming <code>webkit2gtk-4.1</code> (not the older <code>4.0</code> API) is the correct runtime generation to target.</li></ol>\n<ol><li>Snap's <code>plugs</code> <strong>MUST</strong> be limited to what CyberOS's actual functionality needs: <code>desktop</code>, <code>desktop-legacy</code>, <code>wayland</code>, <code>x11</code> (display/input), <code>network</code> (the app is a thin shell loading <code>os.cyberskill.world/web</code>, confirmed by the existing release workflow's own comment: \"the desktop app is a thin shell that loads os.cyberskill.world/web, so there is no web build step here\" — meaning outbound network access is a hard functional requirement, not optional), and <code>opengl</code> (WebKitGTK's hardware-accelerated compositing). No broader plugs (e.g. <code>home</code>, <code>removable-media</code>, <code>raw-usb</code>) <strong>MUST</strong> be requested without a documented functional justification in §9, since over-broad plug requests are a common cause of manual Snap Store review delay.</li></ol>\n<ol><li>Flathub's manifest <strong>MUST</strong> use a reverse-DNS app ID distinct from (though may be textually similar to) the existing Tauri <code>identifier</code> field (<code>os.cyberskill.world.desktop</code>, confirmed in <code>tauri.conf.json</code>) — Flatpak app IDs have their own naming convention and validation rules (must match the <code>.desktop</code> file's basename, must be a syntactically valid reverse-DNS string under Flathub's ownership-verification model, which typically expects the domain segment to correspond to a domain CyberSkill controls) — this task <strong>MUST</strong> decide and document the actual app ID as an explicit choice, not silently reuse the Tauri identifier string without verifying it satisfies Flathub's conventions.</li></ol>\n<ol><li>CI <strong>MUST</strong> gate the Snap Store CI-automation job behind a repo variable <code>SNAP_RELEASE=true</code>, following the exact <code>vars.&lt;FLAG&gt; == 'true' &amp;&amp; secrets.&lt;X&gt; || ''</code> gating idiom already established in <code>release.yml</code>'s <code>desktop</code> job for <code>MACOS_SIGN</code>/<code>DESKTOP_UPDATER_SIGN</code>, defaulting to off, independent of <code>MOBILE_RELEASE</code>/<code>MAS_RELEASE</code>/<code>MSSTORE_RELEASE</code>. Flathub submission has <strong>no equivalent CI-gated build job</strong> (per §1 #2) — the closest analogue is a documented, Stephen-approved-per-instance process for opening the Flathub PR, not a flippable CI flag.</li></ol>\n<ol><li>This task <strong>MUST NOT</strong> attempt to register the <code>cyberos</code> snap name in the Snap Store namespace, run <code>snapcraft export-login</code> to mint store credentials, or open the Flathub submission PR without Stephen's explicit, fresh chat-turn confirmation at the time of submission — those are Stephen's account-setup and publishing actions respectively.</li></ol>\n<ol><li>A <code>docs/deploy/linux-store-submission.md</code> answer sheet <strong>MUST</strong> be authored covering the Snap Store submission metadata (summary, description, category) and, separately, Flathub's actual review checklist items (app-id conventions, sandbox <code>finish-args</code> justification, <code>.desktop</code>/AppStream metadata completeness), mirroring the <code>docs/deploy/play-store-submission.md</code> pattern but structured as two distinct sections given §1 #2's architectural split.</li></ol>\n<h2>§2 — Why this design</h2>\n<p><strong>Why is Flathub handled so differently from every other store task in this batch (§1 #2)?</strong> TASK-APP-003 (Mac App Store) and TASK-APP-004 (Microsoft Store) both follow the same shape: CyberOS's CI builds a signed package and pushes it to the store via that store's own submission API. Flathub does not offer an equivalent artifact-upload API for the initial or ongoing publishing flow — Flathub's documented, current process is: a developer submits a <em>manifest</em> (the recipe describing how to build the app from source, including which runtime/SDK and which upstream sources to fetch) as a pull request against <code>flathub/flathub</code>, and Flathub's own build infrastructure clones that manifest and builds the actual Flatpak on every update. Pretending this fits the same \"CI job with a <code>FLATHUB_RELEASE=true</code> gate that uploads a built artifact\" shape as the other three store tasks would misdescribe how Flathub actually works and would produce a CI job with no real function to perform. Treating it honestly as \"maintain a correct manifest, propose a PR when ready\" is the accurate design, even though it breaks the batch's otherwise-consistent CI-gate pattern.</p>\n<p><strong>Why strict confinement rather than classic (§1 #3)?</strong> <code>classic</code> confinement grants full system access with no sandbox and is reserved by the Snap Store for apps with a specific technical justification (typically dev tools needing arbitrary filesystem/process access) — approval for <code>classic</code> snaps requires a manual Snap Store review process that's slower and less certain than <code>strict</code>. CyberOS is a webview-based app loading a remote URL; it has no functional need for unsandboxed system access, so <code>strict</code> confinement with a minimal, justified plug list (§1 #4) is both the more secure choice and the faster review path.</p>\n<p><strong>Why the <code>gnome</code> extension instead of manually bundling WebKitGTK (§1 #3)?</strong> Manually staging every GTK/WebKitGTK shared library and its transitive dependencies inside a strictly-confined snap is exactly the kind of packaging complexity Canonical's platform <code>extensions</code> mechanism exists to eliminate — the <code>gnome</code> extension (for <code>core22</code>) provisions the GNOME/GTK/WebKitGTK runtime stack via a shared content snap, keeping <code>snapcraft.yaml</code> from needing to hand-declare CyberOS's own copies of libraries the existing GitHub-Releases Linux build already depends on (<code>libwebkit2gtk-4.1-dev</code> et al., confirmed in <code>release.yml</code>).</p>\n<p><strong>Why does the Flatpak app ID need explicit decision-making rather than reusing the Tauri <code>identifier</code> (§1 #5)?</strong> The two identifier systems serve different validation purposes — Tauri's <code>identifier</code> only needs to be a valid bundle identifier for OS-level app registration (no external ownership-verification step), while Flathub's app-id convention is tied to a domain-ownership model reviewers check during submission. Assuming they're interchangeable without verifying against Flathub's actual current documentation risks a rejected or delayed submission over a naming technicality that's cheap to get right up front and expensive to discover during review.</p>\n<h2>§3 — API contract</h2>\n<p><code>apps/desktop/src-tauri/snap/snapcraft.yaml</code> (structural skeleton):</p>\n<pre><code class=\"language-yaml\">name: cyberos\nbase: core22\nversion: '1.0.0'\nsummary: CyberOS — Turn Your Will Into Real\ndescription: |\n  CyberOS is CyberSkill's desktop shell for the CyberOS platform. This snap\n  packages the Tauri-based desktop client, a thin shell over os.cyberskill.world/web.\ngrade: stable\nconfinement: strict\n# `architectures:` syntax below (per-entry build-on objects) is Snapcraft's documented form as\n# of core22-era schemas; a simpler `architectures: [amd64, arm64]` list form also exists in\n# some schema versions. WORKER phase MUST confirm the exact accepted form against Snapcraft's\n# current schema for core22 before this file is treated as final — flagged here rather than\n# asserted with false confidence, since both forms are plausible and this task's authoring did\n# not execute `snapcraft pack` against a real Snapcraft installation to confirm.\narchitectures:\n  - build-on: amd64\n  - build-on: arm64\n\napps:\n  cyberos:\n    command: usr/bin/cyberos\n    extensions: [gnome]\n    plugs:\n      - desktop\n      - desktop-legacy\n      - wayland\n      - x11\n      - network\n      - opengl\n\nparts:\n  cyberos:\n    plugin: dump\n    source: dist/          # populated by the CI staging step from the existing `tauri build --bundles deb` .deb payload — see §3's CI skeleton, not a separate Cargo build.\n    organize:\n      usr/bin/cyberos: usr/bin/cyberos\n    stage-packages:\n      - libwebkit2gtk-4.1-0\n      - libayatana-appindicator3-1</code></pre>\n<p><code>flathub-manifest/os.cyberskill.world.desktop.yml</code> (structural skeleton — filename and app-id are the §9-tracked open decision, shown here with the current best candidate):</p>\n<pre><code class=\"language-yaml\">app-id: os.cyberskill.world.desktop\nruntime: org.freedesktop.Platform\nruntime-version: '23.08'\nsdk: org.freedesktop.Sdk\ncommand: cyberos\n\nfinish-args:\n  - --share=network      # thin shell loading os.cyberskill.world/web — hard functional requirement\n  - --socket=wayland\n  - --socket=fallback-x11\n  - --device=dri         # WebKitGTK hardware-accelerated compositing\n  - --share=ipc\n\nmodules:\n  - name: cyberos\n    buildsystem: simple\n    build-commands:\n      # Flathub's own build infra runs this from source per §2 — placeholder build-commands\n      # here are a structural skeleton only; the real commands depend on which upstream source\n      # archive/tag Flathub is instructed to fetch, decided during WORKER-phase submission prep,\n      # not fabricated here.\n      - install -Dm755 cyberos /app/bin/cyberos\n      - install -Dm644 os.cyberskill.world.desktop.desktop /app/share/applications/os.cyberskill.world.desktop.desktop\n      - install -Dm644 icons/128x128.png /app/share/icons/hicolor/128x128/apps/os.cyberskill.world.desktop.png\n    sources:\n      - type: git\n        url: https://github.com/cyberskill-official/cyberos.git\n        tag: v1.0.0  # pinned per release; Flathub rebuilds only when this manifest is updated</code></pre>\n<p><code>.github/workflows/release-snap.yml</code> (Snap Store CI skeleton — Flathub has no CI-upload equivalent, per §1 #2):</p>\n<pre><code class=\"language-yaml\">name: release-snap\non:\n  workflow_dispatch:\njobs:\n  build-and-upload-snap:\n    if: vars.SNAP_RELEASE == 'true'\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n        with: { ref: \"${{ github.event.inputs.tag || github.ref_name }}\" }\n      - name: Linux build deps\n        run: |\n          sudo apt-get update\n          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf\n      - name: Build .deb payload (source for the snap's dump part)\n        run: |\n          cd apps/desktop\n          npx tauri build --bundles deb\n      - name: Stage snap dump source from the .deb payload\n        run: |\n          mkdir -p apps/desktop/src-tauri/snap/dist\n          dpkg-deb -x apps/desktop/src-tauri/target/release/bundle/deb/*.deb apps/desktop/src-tauri/snap/dist\n      - uses: snapcore/action-build@v1\n        id: snapcraft\n        with:\n          path: apps/desktop/src-tauri\n      - uses: snapcore/action-publish@v1\n        env:\n          SNAPCRAFT_STORE_CREDENTIALS: ${{ secrets.SNAPCRAFT_STORE_CREDENTIALS }}\n        with:\n          snap: ${{ steps.snapcraft.outputs.snap }}\n          release: stable</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong><code>snapcraft.yaml</code> is schema-valid and packs locally</strong> — <code>snapcraft pack apps/desktop/src-tauri</code> exits 0 against a locally-staged <code>dist/</code> directory containing a stub <code>usr/bin/cyberos</code> binary, without requiring real Snap Store credentials.</li><li><strong>Strict confinement, no <code>classic</code>/<code>devmode</code></strong> — a CI lint (<code>grep -q \"^confinement: strict$\" apps/desktop/src-tauri/snap/snapcraft.yaml</code>) asserts the confinement level never silently regresses to a weaker mode.</li><li><strong>Plug list matches §1 #4 exactly, no silent additions</strong> — a CI lint asserts the <code>plugs:</code> block in <code>snapcraft.yaml</code> contains exactly the six-entry set from §1 #4 (<code>desktop</code>, <code>desktop-legacy</code>, <code>wayland</code>, <code>x11</code>, <code>network</code>, <code>opengl</code>) and no others, via a fixed-string comparison, not a fuzzy diff. Automated cross-checking that any <em>future</em> plug addition is accompanied by a §9 justification entry is a code-review norm (§11), not a CI-enforceable mechanism this task builds — a script that parses prose §9 entries and correlates them to YAML list changes would be real but disproportionate engineering for this task's scope, and asserting it exists without designing it would be exactly the kind of unspecified-but-claimed logic this authoring discipline forbids.</li><li><strong>Flatpak manifest validates structurally</strong> — a <code>flatpak-builder</code> invocation against <code>flathub-manifest/os.cyberskill.world.desktop.yml</code> completes manifest-parsing without a YAML/schema error (full build success is not required for this AC, since Flathub's own infra performs the authoritative build per §2 — this AC only guards against manifest syntax/schema errors CyberOS can catch before ever proposing a PR). The exact <code>flatpak-builder</code> flag combination for a parse-only/dry-run check (§5) <strong>MUST</strong> be confirmed against the installed <code>flatpak-builder</code> version's actual <code>--help</code> output at implementation time rather than assumed from this spec — flatpak-builder's CLI surface was not exercised during this task's authoring.</li><li><strong>CI job is inert by default</strong> — with <code>SNAP_RELEASE</code> unset or <code>false</code>, <code>release-snap.yml</code>'s <code>build-and-upload-snap</code> job is skipped (verified the same way as TASK-APP-003/004 — an unconditional anchor job plus a <code>gh run view</code> conclusion check).</li><li><strong>No Flathub PR is opened by CI or by this task's own tooling under any circumstance</strong> — there is no <code>FLATHUB_RELEASE</code> flag, no workflow step, and no script in this task's <code>new_files</code> that calls <code>gh pr create</code> against <code>flathub/flathub</code>; this is a structural, not merely a documented, guarantee — verified by <code>grep -r \"flathub/flathub\" .github/ tools/</code> returning zero matches (the answer sheet at <code>docs/deploy/linux-store-submission.md</code> lives under <code>docs/</code>, outside the two directories this check scans, so no exclusion pattern is needed).</li><li><strong>Answer sheet is complete</strong> — <code>docs/deploy/linux-store-submission.md</code> has a filled-in row for every Snap Store metadata field and every Flathub review-checklist item, each marked <code>human-confirmed</code> or <code>not-applicable</code> with a reason.</li><li><strong>No credential material committed</strong> — the repo's existing secret-scan gate passes against every file this task adds.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># AC #1 — snapcraft.yaml packs locally against a stub payload, no real credentials needed\nmkdir -p /tmp/snap-verify/dist/usr/bin\necho '#!/bin/sh\nexit 0' &gt; /tmp/snap-verify/dist/usr/bin/cyberos\nchmod +x /tmp/snap-verify/dist/usr/bin/cyberos\ncp apps/desktop/src-tauri/snap/snapcraft.yaml /tmp/snap-verify/snapcraft.yaml\n( cd /tmp/snap-verify &amp;&amp; snapcraft pack --destructive-mode )\necho \"exit: $?\"  # MUST be 0\n\n# AC #2 — confinement lint\ngrep -q \"^confinement: strict$\" apps/desktop/src-tauri/snap/snapcraft.yaml\necho \"exit: $?\"  # MUST be 0\n\n# AC #4 — Flatpak manifest structural validation. Exact flag TBD against the installed\n# flatpak-builder version's --help output (§4 AC #4) — a full, non-destructive local build\n# into a scratch repo is the fallback if no dedicated parse-only flag exists:\nflatpak-builder --force-clean --repo=/tmp/flathub-test-repo /tmp/flathub-test-build \\\n  flathub-manifest/os.cyberskill.world.desktop.yml\necho \"exit: $?\"  # MUST be 0 (or the confirmed parse-only flag's equivalent, per AC #4)\n\n# AC #6 — no Flathub PR automation exists anywhere in the repo's tooling. Scoped to .github/\n# and tools/ only — docs/deploy/linux-store-submission.md legitimately mentions\n# \"flathub/flathub\" in prose and lives outside both scanned directories, so no exclusion\n# pattern is needed (a prior draft of this check carried a now-removed, dead `grep -v`\n# exclusion for a path that was never in scope to begin with).\nMATCHES=$(grep -rl \"flathub/flathub\" .github/ tools/ 2&gt;/dev/null | wc -l)\necho \"match count: $MATCHES\"  # MUST be 0</code></pre>\n<pre><code class=\"language-yaml\"># AC #5 — CI job inert-by-default, mirroring TASK-APP-003/004's pattern\n- name: Assert Snap job skipped when SNAP_RELEASE unset\n  run: |\n    gh run view ${{ github.run_id }} --json jobs -q \\\n      '.jobs[] | select(.name==\"build-and-upload-snap\") | .conclusion' | grep -q skipped</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>The API contract in §3 covers <code>snapcraft.yaml</code>, the Flatpak manifest, and the Snap CI workflow. Two pieces of logic are intentionally deferred to WORKER phase rather than fully specified here, each for a distinct, stated reason (not scope-avoidance):</p>\n<ul><li><strong>The <code>.desktop</code> entry file's exact content</strong> (<code>os.cyberskill.world.desktop.desktop</code>, referenced by both the snap and the Flatpak manifest) — a small, mechanical XDG desktop-entry file whose exact <code>Exec=</code>/<code>Icon=</code>/<code>Categories=</code> values depend on the final app-id decision (§9), so it is authored once that decision lands rather than duplicated here with a value that might need to change.</li><li><strong>The exact <code>tag:</code> / source-archive pinning strategy in the Flatpak manifest's <code>sources:</code> block</strong> — Flathub's manifest-review process has specific, current expectations about source provenance (git tag vs. release tarball with checksum) that are best confirmed against Flathub's live submission documentation at proposal time rather than guessed here.</li></ul>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none. Reuses the existing <code>desktop</code> job's <code>ubuntu-latest</code> matrix leg build deps (<code>libwebkit2gtk-4.1-dev</code>, <code>libappindicator3-dev</code>, <code>librsvg2-dev</code>, <code>patchelf</code>) as the grounding for <code>snapcraft.yaml</code>'s <code>stage-packages</code>/extension choice, but does not modify that job.</li><li><strong>Downstream:</strong> none currently drafted.</li><li><strong>Cross-module:</strong> none.</li><li><strong>Human/account prerequisites:</strong> Snapcraft/Ubuntu One account + <code>cyberos</code> snap name registration (Stephen — free, no payment), <code>snapcraft export-login</code> to mint <code>SNAPCRAFT_STORE_CREDENTIALS</code> (Stephen). <strong>Hard blocker on ever opening the Flathub PR:</strong> the app-id/domain-ownership decision (§9) must resolve first — an unresolved app-id means the manifest's <code>app-id:</code> field, filename, and <code>.desktop</code> entry (§6) are all still provisional, and submitting a PR with a provisional app-id risks exactly the rejected-on-a-naming-technicality outcome §2 warns against. Layered on top of that: Stephen's explicit per-instance approval is required before this task's tooling (or a human directly) opens the Flathub submission PR regardless of app-id status (§1 #7, §4 AC #6 — structurally guaranteed, not just policy).</li></ul>\n<h2>§8 — Example payloads</h2>\n<p><code>snapcraft pack</code> local verification failure output shape (illustrative — a plausible, not fabricated-as-real, snapcraft error for a missing stage-package):</p>\n<pre><code>Failed to install snap 'gnome' extension content package.\nEnsure the snapcraft.yaml declares a supported `base` for the requested extension.</code></pre>\n<p>Flathub manifest structural-validation failure shape (<code>flatpak-builder --show-manifest</code> on a malformed manifest):</p>\n<pre><code>Failed to parse arguments: builddir must be an existing directory</code></pre>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Flathub app-id: exact reverse-DNS string</strong> — <code>os.cyberskill.world.desktop</code> is used as the working candidate throughout §3, matching the existing Tauri <code>identifier</code>, but this <strong>MUST</strong> be verified against Flathub's current domain-ownership submission requirements before the manifest is finalized — deferred to Stephen (may require confirming <code>cyberskill.world</code> DNS ownership through whatever mechanism Flathub currently documents).</li><li><strong>Snap Store channel strategy</strong> — §3's CI skeleton releases directly to the <code>stable</code> channel on every <code>SNAP_RELEASE=true</code> run; Stephen may prefer routing through <code>candidate</code>/<code>beta</code> first with manual promotion via <code>snapcraft release</code>, mirroring a staged-rollout posture. Deferred; the skeleton's <code>stable</code>-direct choice is a starting default, not a locked-in decision.</li><li><strong>Any future need for a broader Snap plug</strong> (e.g. <code>home</code> for local file access, if CyberOS ever gains local-file features) — per §1 #4, any such addition requires a documented functional justification added here at the time it's needed; none is required today because the app is a thin remote-URL shell.</li><li><strong><code>related_tasks</code> references TASK-APP-006, which does not yet exist on disk</strong> — deliberate same-batch forward reference (this task and TASK-APP-006 were approved together in the same PLAN and are being authored sequentially in one session; TASK-APP-003 and TASK-APP-004 are already landed). <code>depends_on</code>/<code>blocks</code> are both empty, so no inline placeholder annotation is mechanically required; documented here for the same disclosure reasons TASK-APP-003 §9 and TASK-APP-004 §9 recorded their own forward references.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>gnome</code> extension version mismatch with <code>base: core22</code> (Snapcraft extensions are versioned per base)</td><td><code>snapcraft pack</code> fails at extension-resolution time with an explicit error naming the unsupported combination</td><td>Local pack verification (AC #1) fails before any CI run is attempted</td><td>Confirm the <code>gnome</code> extension's currently-supported base list against Snapcraft's live documentation at implementation time; core22 was the most recent stable base as of this task's authoring but Snapcraft's supported-base list changes over time</td></tr><tr><td>Strict-confinement snap fails to launch WebKitGTK due to a missing plug not anticipated in §1 #4's minimal list (e.g. a missing <code>gsettings</code> or <code>password-manager-service</code> plug some GTK apps implicitly need)</td><td>Manual smoke-test of the packed <code>.snap</code> installed via <code>snap install --dangerous</code> fails to render the webview or crashes on launch</td><td>Snap builds successfully but doesn't run correctly for end users if shipped as-is</td><td>Documented as a required manual smoke-test step in <code>docs/deploy/linux-store-submission.md</code> before any <code>SNAP_RELEASE=true</code> promotion to <code>stable</code>; if a missing plug is discovered, it's added to §1 #4's list with the justification §3's AC #3 lint requires</td></tr><tr><td><code>dpkg-deb -x</code> staging step (§3's CI skeleton) extracts a <code>.deb</code> payload whose internal path layout doesn't match the <code>organize:</code> mapping in <code>snapcraft.yaml</code> (e.g. Tauri's <code>.deb</code> output nests the binary under a different path than assumed)</td><td><code>snapcraft pack</code> succeeds but the resulting snap's <code>apps.cyberos.command</code> points at a nonexistent file, caught by <code>snap install --dangerous</code> smoke-testing, not by <code>pack</code> itself</td><td>Snap builds \"successfully\" but is non-functional</td><td>This is exactly why AC #1's verification uses a stub binary at the assumed path rather than trusting the real <code>.deb</code> layout sight-unseen — the WORKER phase <strong>MUST</strong> confirm the actual <code>.deb</code> internal path against a real <code>tauri build --bundles deb</code> output before finalizing <code>organize:</code>, not assume the skeleton's guessed path is correct</td></tr><tr><td>Flathub reviewers reject the <code>--device=dri</code> or <code>--share=network</code> <code>finish-args</code> as insufficiently justified during manual review</td><td>Flathub PR review comments (external, human, asynchronous — not a CI-detectable failure)</td><td>Submission delayed pending manifest revision and re-review</td><td>Documented in the answer sheet (AC #7) with the functional justification each permission maps to (<code>--share=network</code> → thin remote-shell architecture, confirmed via <code>release.yml</code>'s own comment; <code>--device=dri</code> → WebKitGTK hardware compositing) so the justification is ready at submission time, not improvised during review</td></tr><tr><td>CyberOS's <code>identifier</code> (<code>os.cyberskill.world.desktop</code>) turns out not to satisfy Flathub's domain-ownership verification convention once actually checked against current documentation</td><td>Discovered during §9's deferred verification step, ideally before PR submission</td><td>App-id rename required, touching the manifest, the <code>.desktop</code> file, and icon install paths together</td><td>Because §1 #5 explicitly scoped this as a decision requiring verification rather than assuming reuse was safe, a rename at this stage touches only files this task itself introduces — no coupling to already-shipped identifiers elsewhere in the repo (Tauri's own <code>identifier</code> field is untouched by this task)</td></tr><tr><td><code>SNAPCRAFT_STORE_CREDENTIALS</code> macaroon expires (Snapcraft export-login credentials have a bounded validity period)</td><td><code>snapcore/action-publish@v1</code> step fails with an authentication error</td><td>CI job fails at the upload step; no partial/corrupt Snap Store revision created</td><td>Stephen re-runs <code>snapcraft export-login</code> and rotates the <code>SNAPCRAFT_STORE_CREDENTIALS</code> secret; expected periodic operational task, documented in the answer sheet</td></tr><tr><td>A future contributor adds a <code>FLATHUB_RELEASE=true</code>-style CI gate under the mistaken assumption Flathub works like the other three stores</td><td>Code review, or AC #6's structural grep-based guard, which would start failing the moment such a step appears</td><td>Would silently misrepresent how Flathub actually works if merged unnoticed</td><td>AC #6 makes this a standing, automatically-enforced regression test rather than relying on institutional memory of §2's architectural explanation</td></tr><tr><td>Snap's <code>stage-packages</code> list (<code>libwebkit2gtk-4.1-0</code>, <code>libayatana-appindicator3-1</code>) drifts out of sync with the actual <code>apt-get install</code> package list the existing GitHub-Releases Linux build depends on (<code>-dev</code> packages vs. runtime packages are named differently in Debian/Ubuntu, e.g. <code>libwebkit2gtk-4.1-dev</code> for building vs. <code>libwebkit2gtk-4.1-0</code> for running)</td><td>A snap built successfully but crashing at runtime due to a missing shared library, caught by the same manual smoke-test as the plug-list failure mode above</td><td>Non-functional snap shipped if not caught pre-release</td><td>Both failure modes route through the same manual smoke-test gate documented in the answer sheet; this is a known category of Snapcraft packaging mistake (confusing <code>-dev</code> build-time packages with runtime <code>-N</code> versioned packages) worth naming explicitly rather than leaving implicit</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>This task deliberately does not attempt to automate Flathub submission end-to-end</strong> — per §2's architectural explanation, doing so would misrepresent how Flathub's review-and-build model actually works. The manifest is prepared to a locally-verifiable standard (AC #4) and the PR-opening step remains a Stephen-gated action, structurally guaranteed by AC #6, not merely documented as a should-not.</li><li><strong>The <code>.deb</code>-payload-as-snap-source approach (§3's CI staging step) is chosen over a separate <code>rust</code>-plugin Cargo build inside the snap</strong> to avoid maintaining two independent build configurations for the same binary — the existing <code>tauri build --bundles deb</code> invocation is already exercised by the GitHub-Releases pipeline, so reusing its output as the snap's <code>dump</code>-plugin source means the snap always ships exactly what the GitHub-Releases <code>.deb</code> ships, with no drift risk between the two distribution channels.</li><li><strong>Snap Store and Flathub both require zero payment and zero paid developer account</strong>, unlike TASK-APP-003 (Apple Developer Program) and TASK-APP-004 (Microsoft Partner Center, though Partner Center itself is also free — the constraint there was Azure AD/EV-cert tooling, not a listing fee). This is noted in <code>risk_if_skipped</code> and worth keeping visible: the friction in this task is entirely process/verification friction (confinement review, manifest correctness, app-id conventions), not financial gatekeeping.</li><li><strong>The exact Flathub source-pinning strategy (§6) and the <code>.desktop</code> file's final content (§6) are named as explicitly deferred, not silently absent</strong> — both depend on the §9 app-id decision landing first, and authoring them against a placeholder app-id risks producing files that need rework the moment §9 resolves.</li></ul>\n<p><em>End of TASK-APP-005.</em></p>";
