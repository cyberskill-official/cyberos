window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PROJ-011"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The blocker detector <strong>MUST</strong> parse Issue comments for \"blocked by\" patterns and monitor dwell time. The contract:</p>\n<ol><li><strong>MUST</strong> parse comments on insert/edit (FR-PROJ-003 CRDT events) for the regex <code>(?i)\\b(block(?:ed|s|ing))\\s+by[:\\s]+([^\\n,;.]+)</code>. Captures: keyword (blocked|blocks|blocking) + target reference.</li><li><strong>MUST</strong> classify target as:</li></ol>\n<ul><li><code>IssueMention</code>: matches <code>#&lt;uuid&gt;</code> or <code>#&lt;readable-id&gt;</code>; resolved via FR-PROJ-001 issue table.</li><li><code>MemoryPath</code>: matches <code>memories/...</code>; resolved via FR-MEMORY-101.</li><li><code>FreeText</code>: anything else; stored verbatim (\"waiting on customer\", \"needs design review\").</li></ul>\n<ol><li><strong>MUST</strong> record blockers in <code>blocker_state</code> table: <code>(issue_id, comment_id, blocker_kind, target_ref, detected_at, detected_by, resolved_at, resolved_by_comment_id, tenant_id)</code>. Active = <code>resolved_at IS NULL</code>.</li><li><strong>MUST</strong> auto-resolve a blocker when:</li></ol>\n<ul><li>Target Issue transitions to <code>Done</code> (forward-direction unblock).</li><li>A subsequent comment on the blocking issue matches <code>(?i)\\bunblocked\\b</code> or <code>(?i)\\bresolved\\b</code>.</li><li>The issue itself transitions to Done | Cancelled (block becomes moot).</li></ul>\n<ol><li><strong>MUST</strong> compute <code>dwell_business_days</code> since <code>detected_at</code>, excluding Sat/Sun and configured VN public holidays (<code>CYBEROS_HOLIDAYS_VN</code> env: comma-separated YYYY-MM-DD). ≥ 3 business days → mark stale.</li><li><strong>MUST</strong> emit memory audit rows:</li></ol>\n<ul><li><code>proj.blocker_detected</code> on new blocker.</li><li><code>proj.blocker_resolved</code> on auto-resolution (carries reason).</li><li><code>proj.blocker_stale</code> once dwell ≥ 3 business days (idempotent — once per blocker).</li></ul>\n<ol><li><strong>MUST</strong> notify via FR-CUO-101 webhook on stale events (placeholder URL <code>https://cuo-internal.cyberos.world/notify</code>; OPSEC: not exposed publicly). Payload: <code>{tenant_id, issue_id, blocker_id, target_ref, dwell_business_days, assignee_subject_id}</code>.</li><li><strong>MUST</strong> run hourly dwell scan; on-demand via <code>cyberos blocker scan [--tenant &lt;uuid&gt;]</code> CLI.</li><li><strong>MUST</strong> RLS-enforce.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_blockers_active{tenant_id_bucket}</code> (gauge).</li><li><code>proj_blockers_detected_total{kind}</code> (counter).</li><li><code>proj_blockers_resolved_total{auto_reason}</code> (counter).</li><li><code>proj_blockers_stale_total</code> (counter).</li></ul>\n<ol><li><strong>MUST</strong> redact PII from <code>target_ref</code> (especially FreeText kind) before memory audit emit via FR-MEMORY-111 ruleset.</li><li><strong>MUST</strong> support manual operator override: <code>POST /api/proj/blockers/:id/resolve</code> with reason; emits <code>proj.blocker_resolved</code> with <code>auto_reason=\"manual\"</code>.</li><li><strong>MUST</strong> support manual creation: <code>POST /api/proj/issues/:id/blockers</code> for cases where parser missed (e.g. blocker discussed verbally). Tracks <code>detected_by_subject_id</code> as the operator.</li><li><strong>MUST</strong> track escalation: after <code>stale_notified_at + 7 business days</code> without resolution, emit <code>proj.blocker_escalated</code> SEV-2 + notify tenant admin (in addition to assignee).</li><li><strong>MUST</strong> support per-tenant dwell threshold override: <code>cyberos_proj_tenant_settings.blocker_stale_business_days</code> (default 3); SLA-heavy tenants may want stricter (1d).</li><li><strong>MUST</strong> include <code>cuo_notify_attempts INT</code> + <code>last_cuo_notify_at</code> columns for tracking notification reliability; if 3 consecutive notification failures, SEV-1 alert on CUO health.</li><li><strong>MUST</strong> support a \"dependency cycle\" detection: if Issue A is blocked by Issue B AND Issue B is blocked by Issue A, emit <code>proj.blocker_cycle_detected</code> SEV-2; do NOT auto-break.</li><li><strong>MUST</strong> include <code>blocker_age_distribution</code> histogram in metrics — bucketed by business days (0-1, 1-3, 3-7, 7-14, 14+).</li><li><strong>MUST</strong> support a \"snooze\" action: operator can snooze a blocker's stale notification for N days (max 14); <code>snoozed_until</code> column; scan skips snoozed blockers.</li><li><strong>MUST</strong> include <code>mentioned_users</code> (parsed @-mentions from the blocker comment) in audit payload — useful for \"who was tagged as the unblock owner.\"</li><li><strong>MUST</strong> emit <code>proj.blocker_resolved_diff</code> audit row containing the comment text that triggered auto-resolution (e.g. the \"unblocked\" comment). Operators reviewing resolution can see the context.</li><li><strong>MUST</strong> support cross-engagement blocker references: Issue X (engagement A) blocked by Issue Y (engagement B) — link is valid if caller has read scope on both engagements.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why comment-stream parsing (DEC-320)?</strong> Operators already write \"blocked by X\" in comments; structured-label workflows (drag to \"Blocked\" column) are friction. Parsing existing prose = zero friction adoption.</p>\n<p><strong>Why three target classes (§1 #2)?</strong> IssueMention is the strongest signal (machine-resolvable). MemoryPath catches memory-anchored decisions. FreeText handles human context (\"blocked by customer's response\") — can't auto-resolve but still timed.</p>\n<p><strong>Why business days not calendar (§1 #5)?</strong> A blocker filed Friday afternoon shouldn't ping Monday morning as \"3 days stale.\" Business-day math + VN holidays = culturally-correct.</p>\n<p><strong>Why CUO Notify routing (§1 #7, DEC-322)?</strong> Direct emails create email-noise that operators filter to a forgotten folder. CUO is the unified inbox; routing through it makes blockers visible alongside other surfaces.</p>\n<p><strong>Why hourly scan (§1 #8)?</strong> Dwell is wall-clock-driven; hourly granularity is the right resolution for \"3 business days = 72 working hours.\" More-frequent burns CPU; less-frequent delays signal.</p>\n<p><strong>Why audit per state transition (§1 #6)?</strong> Operators investigating \"did this issue ever get blocked\" need first-class events. Generic comment-history doesn't separate \"blocked\" from \"everything else.\"</p>\n<p><strong>Why redact target_ref (§1 #11)?</strong> FreeText blockers (\"waiting on alice@x.com response\") embed PII. Years of accumulated audit = PII bloat.</p>\n<p><strong>Why manual resolve (§1 #12)?</strong> Auto-resolve covers known patterns; real-world blockers resolve in ways the parser misses (operator agrees verbally to unblock). Manual override prevents indefinite-stale.</p>\n<p><strong>Why manual create (§1 #13)?</strong> Operators discussing blockers in meetings (not comments) want to record them in PROJ for tracking. Manual create closes that gap.</p>\n<p><strong>Why escalation at 7d (§1 #14)?</strong> 3-day stale = assignee should fix; 10-day total = needs management attention. Escalation forces visibility.</p>\n<p><strong>Why per-tenant dwell threshold (§1 #15)?</strong> Enterprise SLAs vary; some clients want next-day escalation, others week.</p>\n<p><strong>Why CUO notification health tracking (§1 #16)?</strong> Silent notification failures = operators don't know blockers exist. Tracking + SEV-1 alert ensures detection.</p>\n<p><strong>Why cycle detection (§1 #17)?</strong> A↔B blocker cycle = both stuck indefinitely; auto-break would lose data. Detection alerts operator to intervene.</p>\n<p><strong>Why age distribution histogram (§1 #18)?</strong> Operators tracking team health see \"we have 5 blockers in 7-14 day range\" — actionable.</p>\n<p><strong>Why snooze (§1 #19)?</strong> Some blockers are legitimately long-running (vendor contract negotiation); snooze prevents nagging.</p>\n<p><strong>Why mentioned_users in payload (§1 #20)?</strong> Comment \"blocked by Bob (@bob): waiting for design review\" tags Bob as the unblock owner. Capturing the @-mention provides actionability.</p>\n<p><strong>Why blocker_resolved_diff (§1 #21)?</strong> Operators reviewing the auto-resolution see \"Alice replied 'unblocked because customer approved'\" — full context.</p>\n<p><strong>Why cross-engagement blockers (§1 #22)?</strong> Real engagements have dependencies across project boundaries; restricting to same-engagement misses these.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0011_blocker_state.sql\n\nCREATE TABLE blocker_state (\n    id                   UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    issue_id             UUID NOT NULL,\n    comment_id           TEXT NOT NULL,\n    blocker_kind         TEXT NOT NULL CHECK (blocker_kind IN ('issue_mention','memory_path','free_text')),\n    target_ref           TEXT NOT NULL,\n    detected_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    detected_by_subject_id UUID NOT NULL,\n    resolved_at          TIMESTAMPTZ,\n    resolved_by_comment_id TEXT,\n    stale_notified_at    TIMESTAMPTZ,\n    tenant_id            UUID NOT NULL\n);\nCREATE INDEX idx_blocker_active ON blocker_state (issue_id) WHERE resolved_at IS NULL;\nCREATE INDEX idx_blocker_dwell  ON blocker_state (detected_at) WHERE resolved_at IS NULL AND stale_notified_at IS NULL;\n\nALTER TABLE blocker_state ENABLE ROW LEVEL SECURITY;\nCREATE POLICY blocker_tenant_iso ON blocker_state\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Rust API</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/blocker/mod.rs\nuse serde::Serialize;\n\n#[derive(Clone, Copy, Debug, Serialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum BlockerKind { IssueMention, MemoryPath, FreeText }\n\n#[derive(Clone, Debug, Serialize)]\npub struct Blocker {\n    pub id:                     uuid::Uuid,\n    pub issue_id:               uuid::Uuid,\n    pub comment_id:             String,\n    pub blocker_kind:           BlockerKind,\n    pub target_ref:             String,\n    pub detected_at:            chrono::DateTime&lt;chrono::Utc&gt;,\n    pub resolved_at:            Option&lt;chrono::DateTime&lt;chrono::Utc&gt;&gt;,\n    pub dwell_business_days:    Option&lt;i32&gt;,\n}</code></pre>\n<h3>Parser</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/blocker/parser.rs\nuse crate::blocker::BlockerKind;\nuse once_cell::sync::Lazy;\nuse regex::Regex;\n\nstatic BLOCKER_RX: Lazy&lt;Regex&gt; = Lazy::new(||\n    Regex::new(r\"(?i)\\b(?:block(?:ed|s|ing))\\s+by[:\\s]+([^\\n,;.]+)\").unwrap());\nstatic ISSUE_REF_RX: Lazy&lt;Regex&gt; = Lazy::new(||\n    Regex::new(r\"^\\s*#([a-zA-Z0-9-]+)\\s*$\").unwrap());\nstatic MEMORY_REF_RX: Lazy&lt;Regex&gt; = Lazy::new(||\n    Regex::new(r\"^\\s*(memories/[^\\s]+)\\s*$\").unwrap());\n\n#[derive(Clone, Debug)]\npub struct ParsedBlocker {\n    pub kind:        BlockerKind,\n    pub target_ref:  String,\n}\n\npub fn parse(comment_body: &amp;str) -&gt; Vec&lt;ParsedBlocker&gt; {\n    BLOCKER_RX.captures_iter(comment_body)\n        .filter_map(|cap| cap.get(1).map(|m| m.as_str().trim().to_string()))\n        .map(|raw| {\n            let kind = if ISSUE_REF_RX.is_match(&amp;raw) { BlockerKind::IssueMention }\n                       else if MEMORY_REF_RX.is_match(&amp;raw) { BlockerKind::MemoryPath }\n                       else { BlockerKind::FreeText };\n            ParsedBlocker { kind, target_ref: raw }\n        })\n        .collect()\n}</code></pre>\n<h3>Dwell scan</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/blocker/dwell.rs\npub async fn scan_stale(pool: &amp;sqlx::PgPool, tenant_id: uuid::Uuid) -&gt; anyhow::Result&lt;i32&gt; {\n    sqlx::query(\"SELECT set_config('app.tenant_id', $1, true)\")\n        .bind(tenant_id.to_string()).execute(pool).await?;\n\n    let active: Vec&lt;(uuid::Uuid, uuid::Uuid, chrono::DateTime&lt;chrono::Utc&gt;, String)&gt; = sqlx::query_as(\n        \"SELECT id, issue_id, detected_at, target_ref\n         FROM blocker_state\n         WHERE resolved_at IS NULL AND stale_notified_at IS NULL\"\n    ).fetch_all(pool).await?;\n\n    let now = chrono::Utc::now();\n    let mut notified = 0;\n    for (id, issue_id, detected_at, target_ref) in active {\n        let dwell = business_days_between(detected_at, now);\n        if dwell &gt;= 3 {\n            sqlx::query(\"UPDATE blocker_state SET stale_notified_at = NOW() WHERE id = $1\")\n                .bind(id).execute(pool).await?;\n            cuo_notify(tenant_id, issue_id, id, &amp;target_ref, dwell).await?;\n            emit_memory_row(\"proj.blocker_stale\", serde_json::json!({\n                \"blocker_id\": id, \"issue_id\": issue_id,\n                \"target_ref\": target_ref, \"dwell_business_days\": dwell,\n            })).await;\n            metrics::counter!(\"proj_blockers_stale_total\").increment(1);\n            notified += 1;\n        }\n    }\n    Ok(notified)\n}\n\nfn business_days_between(start: chrono::DateTime&lt;chrono::Utc&gt;, end: chrono::DateTime&lt;chrono::Utc&gt;) -&gt; i32 {\n    use chrono::Datelike;\n    let mut days = 0;\n    let mut d = start.date_naive();\n    let end_d = end.date_naive();\n    let holidays = parse_vn_holidays_env();\n    while d &lt; end_d {\n        d = d + chrono::Duration::days(1);\n        if matches!(d.weekday(), chrono::Weekday::Sat | chrono::Weekday::Sun) { continue; }\n        if holidays.contains(&amp;d) { continue; }\n        days += 1;\n    }\n    days\n}\n\nfn parse_vn_holidays_env() -&gt; std::collections::HashSet&lt;chrono::NaiveDate&gt; {\n    std::env::var(\"CYBEROS_HOLIDAYS_VN\").ok()\n        .map(|s| s.split(',').filter_map(|d| d.trim().parse().ok()).collect())\n        .unwrap_or_default()\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Parse \"blocked by #abc123\"</strong> → 1 ParsedBlocker, kind=IssueMention, target_ref=\"#abc123\".</li><li><strong>Parse \"blocks: memories/x/y.md\"</strong> → kind=MemoryPath.</li><li><strong>Parse \"blocked by customer feedback\"</strong> → kind=FreeText.</li><li><strong>Multiple blockers in one comment</strong> — \"blocked by #X, blocked by #Y\" → 2 parsed.</li><li><strong>Comment without blocker prose</strong> → 0 parsed.</li><li><strong>Insert detected blocker into blocker_state</strong> — on comment insert → row appears; <code>proj.blocker_detected</code> audit row.</li><li><strong>Auto-resolve on target Done</strong> — referenced issue transitions to Done → blocker row resolved_at set; <code>proj.blocker_resolved</code> row.</li><li><strong>Auto-resolve on <code>unblocked</code> comment</strong> — followup comment \"unblocked\" → resolved.</li><li><strong>Auto-resolve on issue cancellation</strong> — block-owning issue cancelled → all its blockers resolved (moot).</li><li><strong>Dwell scan ≥ 3 business days</strong> — fixture: detected_at = Mon 09:00; current = Thu 11:00 → dwell = 3; stale_notified_at set; CUO webhook fired.</li><li><strong>Weekend doesn't count</strong> — Fri detected → Mon = dwell 1 (not 3).</li><li><strong>VN holiday env honoured</strong> — config holiday 2026-09-02; Tue–Wed straddles it → dwell skips that day.</li><li><strong>Stale notification idempotent</strong> — second scan after notification → no duplicate webhook; metric unchanged.</li><li><strong>memory audit on detect/resolve/stale</strong> — all 3 kinds emitted at correct events.</li><li><strong>CUO Notify webhook payload schema</strong> — payload matches §1 #7 spec.</li><li><strong>RLS tenant isolation</strong> — tenant A's blockers invisible to tenant B.</li><li><strong>Gauge <code>proj_blockers_active</code></strong> — accurate count of unresolved blockers per tenant.</li><li><strong>target_ref PII redacted in audit</strong> — FreeText \"waiting on alice@x.com\" → audit row contains \"&lt;EMAIL&gt;\" (AC for §1 #11).</li><li><strong>Manual resolve emits proj.blocker_resolved with reason=manual</strong> — POST /:id/resolve → audit row (AC for §1 #12).</li><li><strong>Manual create works</strong> — POST /blockers with fields → row inserted; detected_by_subject_id = operator (AC for §1 #13).</li><li><strong>Escalation at 10 business days</strong> — stale + 7 more business days → <code>proj.blocker_escalated</code> SEV-2 (AC for §1 #14).</li><li><strong>Per-tenant dwell override</strong> — set blocker_stale_business_days=1; blocker after 1 day flagged stale (AC for §1 #15).</li><li><strong>CUO notification failure tracking</strong> — 3 consecutive failures → SEV-1 on CUO health (AC for §1 #16).</li><li><strong>Cycle detected</strong> — A blocked by B, B blocked by A → <code>proj.blocker_cycle_detected</code> SEV-2 (AC for §1 #17).</li><li><strong>Age distribution histogram populated</strong> — distinct blocker ages across buckets (AC for §1 #18).</li><li><strong>Snooze skips stale scan</strong> — operator snoozes for 7d; scan during snooze window → no notification (AC for §1 #19).</li><li><strong>mentioned_users captured</strong> — comment \"blocked by @bob waiting on review\" → audit payload mentioned_users=[\"bob\"] (AC for §1 #20).</li><li><strong>blocker_resolved_diff contains comment</strong> — auto-resolve via \"unblocked\" comment → audit row contains comment text (AC for §1 #21).</li><li><strong>Cross-engagement blocker validated</strong> — blocker referring to another engagement's issue allowed only with read scope (AC for §1 #22).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[test]\nfn parses_issue_mention() {\n    let p = parse(\"This is blocked by #ABC-123 right now.\");\n    assert_eq!(p.len(), 1);\n    assert_eq!(p[0].kind, BlockerKind::IssueMention);\n    assert_eq!(p[0].target_ref, \"#ABC-123\");\n}\n\n#[test]\nfn parses_memory_path() {\n    let p = parse(\"blocks: memories/projects/cyberos/decisions/DEC-300.md\");\n    assert_eq!(p[0].kind, BlockerKind::MemoryPath);\n}\n\n#[test]\nfn parses_free_text() {\n    let p = parse(\"blocked by customer response\");\n    assert_eq!(p[0].kind, BlockerKind::FreeText);\n}\n\n#[test]\nfn parses_multiple_per_comment() {\n    let p = parse(\"blocked by #X, blocked by #Y. blocking #Z\");\n    assert_eq!(p.len(), 3);\n}\n\n#[test]\nfn weekend_skipped() {\n    let mon = chrono::DateTime::parse_from_rfc3339(\"2026-05-11T09:00:00Z\").unwrap().with_timezone(&amp;chrono::Utc);\n    let next_mon = chrono::DateTime::parse_from_rfc3339(\"2026-05-18T09:00:00Z\").unwrap().with_timezone(&amp;chrono::Utc);\n    // Mon-Mon = 5 business days\n    assert_eq!(business_days_between(mon, next_mon), 5);\n}\n\n#[tokio::test]\nasync fn auto_resolve_on_target_done() {\n    let env = TestEnv::new().await;\n    let target = env.create_issue().await;\n    let blocker_issue = env.create_issue_with_blocker_comment(target).await;\n    apply_transition(&amp;env.pool, target, IssueStatus::Done, env.alice(), None).await.unwrap();\n    let blk: Blocker = env.read_blocker(blocker_issue).await;\n    assert!(blk.resolved_at.is_some());\n}\n\n#[tokio::test]\nasync fn stale_notifies_at_three_business_days() {\n    let env = TestEnv::with_paused_time().await;\n    let blocker_issue = env.create_issue_with_blocker_comment_at(\"monday 9am\").await;\n    env.advance_to(\"thursday 9am\").await;\n    let notified = scan_stale(&amp;env.pool, env.tenant_id()).await.unwrap();\n    assert_eq!(notified, 1);\n    let alert = env.cuo.latest_notification().await;\n    assert_eq!(alert[\"dwell_business_days\"], 3);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + DB above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-PROJ-003</strong> — comment events trigger parser.</li><li><strong>FR-PROJ-004</strong> — status transition triggers auto-resolve.</li><li><strong>FR-CUO-101 (placeholder)</strong> — notification webhook.</li><li><strong>FR-OBS-007</strong> — sev-3 alert (informational).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.blocker_detected\",\n  \"payload\": {\n    \"blocker_id\": \"blk-...\",\n    \"issue_id\": \"iss-...\",\n    \"comment_id\": \"cmt-...\",\n    \"blocker_kind\": \"issue_mention\",\n    \"target_ref\": \"#ABC-123\",\n    \"detected_at_ns\": 1747407137483000000,\n    \"trace_id\": \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>ML-based \"soft blocker\" detection (e.g. \"I'm waiting on...\" without explicit keyword) — slice 4+.</li><li>Per-tenant configurable dwell threshold — slice 4+.</li><li>Bidirectional blocker (issue X blocks issue Y AND issue Y is blocked by X auto-link) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Comment parse regex fails on edge case</td><td>unit test fixtures catch</td><td>False negative</td><td>Operator files bug</td></tr><tr><td>Target issue mentioned doesn't exist</td><td>resolution lookup Err</td><td>Blocker stored with kind=FreeText (degraded)</td><td>Operator inspects</td></tr><tr><td>CUO webhook timeout</td><td>retry once; fail</td><td>sev-3 alarm via FR-OBS-007</td><td>Operator restores CUO</td></tr><tr><td>Holiday env malformed</td><td>parse Err</td><td>Falls back to weekend-only skip</td><td>Operator fixes env</td></tr><tr><td>Stale_notified_at race (two scans concurrent)</td><td>UPDATE WHERE NULL → second sees row already notified</td><td>No duplicate</td><td>None</td></tr><tr><td>Issue transitions but blocker resolve fails</td><td>sqlx Err</td><td>Blocker stays active; next scan retries</td><td>Acceptable</td></tr><tr><td>Comment edited to remove \"blocked by\"</td><td>edit not handled in slice 3</td><td>Original blocker stays</td><td>Slice 4+</td></tr><tr><td>10K active blockers in one tenant</td><td>scan slow</td><td>sev-2 latency alarm</td><td>Slice 4+ paginate</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>Concurrent unblocked comment + Done transition</td><td>both resolve</td><td>Idempotent UPDATE</td><td>None</td></tr><tr><td>MemoryEmit fails</td><td>row created; audit lost</td><td>sev-2</td><td>Operator restores</td></tr><tr><td>Free-text blocker with secrets</td><td>redacted via FR-MEMORY-111 at audit emit</td><td>Safe</td><td>None</td></tr><tr><td>Manual resolve without reason</td><td>400</td><td>None</td><td>Caller</td></tr><tr><td>Manual create with invalid target</td><td>falls back to FreeText</td><td>None</td><td>None</td></tr><tr><td>Escalation fires before assignee fixes (race)</td><td>dedup at OBS</td><td>None</td><td>None</td></tr><tr><td>Per-tenant dwell = 0</td><td>every blocker immediately stale</td><td>as-configured</td><td>Operator</td></tr><tr><td>CUO notify down 3+ days</td><td>SEV-1 alert; blockers accumulate</td><td>operator restores</td><td>None</td></tr><tr><td>Cycle detection misses indirect cycle (A→B→C→A)</td><td>DFS depth 100 covers</td><td>detected</td><td>None</td></tr><tr><td>Histogram bucket out of range (&gt;14d)</td><td>overflow bucket counts</td><td>None</td><td>None</td></tr><tr><td>Snooze longer than allowed</td><td>rejected</td><td>400</td><td>Caller</td></tr><tr><td>Snoozed blocker resolves naturally</td><td>cancel-on-resolve cleans snooze</td><td>None</td><td>None</td></tr><tr><td>Mention contains non-existent user</td><td>stored verbatim; resolver returns null</td><td>None</td><td>None</td></tr><tr><td>Resolved_diff &gt; 5KB</td><td>truncated to 5KB</td><td>None</td><td>None</td></tr><tr><td>Cross-engagement target without scope</td><td>403</td><td>None</td><td>Caller</td></tr><tr><td>Engagement archived mid-blocker</td><td>resolve_on_archive auto-resolves</td><td>proj.blocker_resolved</td><td>None</td></tr><tr><td>Concurrent manual + auto resolve</td><td>last wins; idempotent</td><td>None</td><td>None</td></tr><tr><td>Two operators snooze same blocker</td><td>latest wins</td><td>None</td><td>None</td></tr><tr><td>Snooze on already-resolved blocker</td><td>rejected</td><td>409</td><td>Caller</td></tr><tr><td>Mentioned_users with &gt; 50 mentions</td><td>truncated to 50 + warning</td><td>None</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The regex avoids <code>blockchain</code> / <code>roadblock</code> etc. via <code>\\bblock</code> word-boundary; <code>blocked|blocks|blocking</code> covers tense.</li><li>Issue mention syntax (<code>#ABC-123</code> or <code>#&lt;uuid&gt;</code>) needs resolution against issues table; mismatches fall to FreeText.</li><li>VN holidays default to: Tết (Lunar New Year, variable), 30/4 + 1/5, 2/9 (National Day), Hùng King (variable). Env override allows custom.</li><li>CUO Notify URL is internal; production deployment sets via <code>CYBEROS_CUO_NOTIFY_URL</code> env.</li><li>The <code>dwell_business_days</code> field is computed at query time (not stored) for accuracy across server time vs. wall clock.</li><li>Hourly cron scan handles dwell threshold crossings; doesn't re-emit for already-notified blockers.</li><li>Free-text blockers can't auto-resolve via target Done; only via explicit unblocked/resolved comment or owner-issue Done|Cancelled.</li><li>PII redaction of target_ref runs at audit emit, not at storage — operator UI shows the original.</li><li>Manual create + manual resolve provide operator-controlled paths for cases the parser misses.</li><li>Escalation at 10 business days is calibrated: 3 days assignee fix + 7 days unattended = needs management.</li><li>Per-tenant dwell threshold is per-engagement override (slice 4+); current MVP is per-tenant only.</li><li>CUO notification health uses exponential backoff (1s, 2s, 4s); 3 failures = retry exhausted.</li><li>Cycle detection is O(N²) in worst case but bounded by blocker count per tenant (~hundreds typically); fast in practice.</li><li>Age distribution histogram updates on scan; buckets fixed for cross-tenant comparability.</li><li>Snooze uses <code>snoozed_until TIMESTAMPTZ</code>; expired snoozes are cleared at next scan.</li><li>Mentioned_users uses regex <code>@([a-zA-Z0-9_]+)</code> to extract; resolves against users table (best-effort).</li><li>Resolved_diff stores up to 5KB of the resolving comment text; longer truncated.</li><li>Cross-engagement blockers require the caller to have read scope on both engagements; enforced via RLS at insert.</li><li>We considered NLP-based blocker detection but rejected: false positives on metaphorical \"blocked\" mentions; regex is precise + auditable.</li><li>The <code>proj.blocker_escalated</code> SEV-2 notification is routed to tenant admin via FR-OBS-007 + the assignee via CUO.</li><li>Snooze max 14 days because: anything longer = blocker isn't really tracked; operator should remove or escalate.</li><li>The blocker comment that triggered detection is the canonical reference; comment edits don't re-trigger detection (slice 4+ feature).</li><li>Manual create + manual resolve emit memory audit rows with <code>auto_reason=\"manual\"</code> so analytics can distinguish from automated detection.</li><li>The mentioned_users extraction handles Vietnamese username conventions (with diacritics stripped for resolution).</li></ul>\n<hr>\n<p><em>End of FR-PROJ-011.</em></p>";
