window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AI-105"]="<h1>Feature Request</h1>\n<blockquote><p>Turn Your Will Into Real.</p></blockquote>\n<h2>Summary</h2>\n<p>CyberOS should route real model calls to local, no-key inference servers. Stephen runs models locally (LM Studio today, Ollama earlier), and both must work. This adds two provider adapters to the existing FR-AI-008 router: one for LM Studio and any OpenAI-compatible local server (POST to /v1/chat/completions, default http://localhost:1234), and one for Ollama-native (POST to /api/chat, default http://localhost:11434). Each is selectable per tenant through the existing FR-AI-006 alias map and FR-AI-008 fallback chain, holds no API key, and fails closed when the local server is down. The same change flips the HTTP serving path from the in-repo EchoBackend to the real router, so a tenant whose primary provider is local actually gets a model response rather than an echo.</p>\n<h2>Problem</h2>\n<p>The gateway already has the whole control plane: cost ledger (FR-AI-001/002), tenant policy (FR-AI-005), alias resolution (FR-AI-006), the multi-provider router with retry and failover (FR-AI-008), PII redaction (FR-AI-011/012), ZDR (FR-AI-015), residency (FR-AI-016), and per-tenant cache (FR-AI-017). But none of it drives a real model on the local-dev or self-hosted path. The serving endpoint answers through EchoBackend, and the Provider trait's adapters (Anthropic, OpenAI, Bedrock) are stubs that return an error. So today a chat call cannot reach a model without cloud keys, and even with keys the server does not call the router.</p>\n<p>Stephen's working setup is a local model server: LM Studio right now, Ollama before that. LM Studio exposes an OpenAI-compatible API; Ollama exposes its own /api/chat shape. CyberOS needs to talk to both, with no key, so the local path proves end to end and self-hosted deployments have a zero-cost inference option.</p>\n<h2>Proposed Solution</h2>\n<p>Two real adapters behind the existing <code>Provider</code> trait, plus the serving flip. User-visible behaviour: a tenant sets <code>primary_provider: { kind: local-openai }</code> or <code>{ kind: ollama }</code> in its policy, maps its aliases to local model ids, and chat calls return real completions from the local server. No key is configured. If the server is down, the call fails with a clear error instead of an echo.</p>\n<h3>Section 1 - normative requirements (BCP-14)</h3>\n<ol><li>The gateway MUST ship <code>Provider::LocalOpenai { model_alias_map }</code>: POST <code>{endpoint}/v1/chat/completions</code> with body <code>{model, messages, stream:false, temperature?, max_tokens?}</code>, parsing <code>choices[0].message.content</code>, <code>usage.prompt_tokens</code>, and <code>usage.completion_tokens</code>. This covers LM Studio and any OpenAI-compatible local runtime (llama.cpp server, vLLM, text-generation-webui).</li></ol>\n<ol><li>The gateway MUST ship <code>Provider::Ollama { model_alias_map }</code>: POST <code>{endpoint}/api/chat</code> with body <code>{model, messages, stream:false, options?}</code>, parsing <code>message.content</code>, <code>prompt_eval_count</code>, and <code>eval_count</code>.</li></ol>\n<ol><li>Endpoints MUST come from environment, not from tenant YAML: <code>LMSTUDIO_ENDPOINT</code> (default <code>http://localhost:1234</code>) and <code>OLLAMA_ENDPOINT</code> (default <code>http://localhost:11434</code>). One local server serves a whole deployment, so the endpoint is deployment-level, not tenant-level.</li></ol>\n<ol><li>Local providers MUST require no API key, and MUST fail closed: if the local server is unreachable or returns a non-2xx, the adapter MUST return a <code>RouterError</code> (never a fabricated or echoed completion). The router's existing retry and failover then apply.</li></ol>\n<ol><li>The provider taxonomy MUST register <code>ProviderKind::LocalOpenai</code> and <code>ProviderKind::Ollama</code> in every match site: the config <code>Provider</code> enum (kind/region/model_for_alias), the cost-table provider parser, the cost-reconcile parser, the ZDR parser, and the streaming-support table. Streaming-support MUST report false for both until a streaming adapter lands.</li></ol>\n<ol><li>The HTTP serving path MUST call the real router (<code>router::call_provider</code>) for tenants whose resolved provider is a real adapter, instead of EchoBackend. EchoBackend MAY remain available behind an explicit test-only flag, but MUST NOT be the default serving path.</li></ol>\n<ol><li>Local providers MUST be treated as zero-cost in the cost ledger (no per-token USD), while still recording token usage and emitting the post-call audit row. A missing cost row for a local provider MUST NOT block the call.</li></ol>\n<ol><li>Both local providers MUST be selectable concurrently: a tenant MAY set one as primary and the other (or a cloud provider) in the fallback chain, and FR-AI-008 failover MUST work across the mix.</li></ol>\n<ol><li>The adapters MUST emit memory audit kinds <code>ai.local_provider_invoked</code> and <code>ai.local_provider_unreachable</code> through the FR-AI-003 bridge, carrying tenant_id, provider kind, model, and outcome (prompts already hashed at the FR-AI-006 layer).</li></ol>\n<ol><li>The request-build and response-parse functions MUST be pure and unit-tested without a live server; the live round trip is covered by an owner-run integration test gated on a reachable local server.</li></ol>\n<h2>Alternatives Considered</h2>\n<p>Reuse the cloud OpenAI adapter pointed at localhost. Rejected: it covers LM Studio (OpenAI-compatible) but not Ollama-native (/api/chat is a different shape), and the cloud adapter assumes an API key and cloud base URL. The user needs both dialects and a no-key path.</p>\n<p>Put the endpoint and dialect in the tenant policy YAML (one generic HTTP provider with a <code>url</code> + <code>dialect</code> field). Rejected: infrastructure endpoints are a deployment concern, not a per-tenant one. Tenant policy stays about caps, residency, and aliases; the local server URL belongs in env. It also widens the policy schema's trust surface.</p>\n<p>Keep EchoBackend and add a separate \"local test\" endpoint. Rejected: it leaves the real router unexercised in production and forks the serving path. Flipping the dispatch once is cleaner and exercises retry, failover, cost, and audit on the real path.</p>\n<h2>Success Metrics</h2>\n<p>Primary metric - local round-trip success rate.</p>\n<ul><li>Definition: fraction of chat calls routed to a local provider that return a real (non-echo) completion while the local server is up.</li><li>Baseline: 0% (no local adapter exists; serving path is EchoBackend).</li><li>Target: at least 99% when the configured local server is reachable.</li><li>Measurement method: integration test plus the <code>ai.local_provider_invoked</code> audit count over total local-routed calls.</li><li>Source: memory audit rows (FR-AI-003) and the gateway request log.</li></ul>\n<p>Guardrail metric - key and cross-tenant leakage.</p>\n<ul><li>Definition: number of incidents where a local provider call carries or exposes an API key, or a response crosses tenants.</li><li>Baseline: not applicable (no local path today).</li><li>Target: zero. Local providers hold no key by construction, and FR-AI-017/018 cache isolation still applies.</li><li>Measurement method: the FR-AI-018 cross-tenant leak property test plus a static check that local adapters read no key material.</li><li>Source: FR-AI-018 test output and code review.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the LocalOpenai and Ollama adapters, env-driven endpoints, the no-key fail-closed contract, the provider-taxonomy registrations, the EchoBackend-to-router serving flip, zero-cost ledger handling for local providers, the two audit kinds, and pure unit tests plus one owner-run integration test.</p>\n<h3>Out of scope</h3>\n<ul><li>Streaming for local providers (SSE token-by-token). Deferred to a follow-up; the streaming-support table returns false for now.</li><li>Local embeddings (Ollama /api/embeddings, LM Studio /v1/embeddings). The embedding path stays on FR-AI-019 BGE; local embeddings are a separate FR.</li><li>Full KMS wiring of cloud-provider keys (Anthropic, OpenAI, Bedrock). This FR specs the seam and keeps those adapters on the same trait, but the secrets-management implementation is its own FR and stays behind the prohibited-secrets boundary.</li><li>GPU or model lifecycle management of the local server itself (loading, unloading, quantization).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>FR-AI-008 multi-provider router - the <code>Provider</code> trait, retry, and failover that the adapters plug into.</li><li>FR-AI-006 model-alias resolution - maps the tenant alias to a local model id and yields the <code>ResolvedModel</code> the router consumes.</li><li>FR-AI-005 tenant-policy loader - the policy schema gains the two local provider variants.</li><li>FR-AI-007 provider cost-table loader - zero-cost rows for local providers, and the loader must tolerate their absence.</li><li>FR-AI-015 ZDR enforcement - local providers are inherently zero-retention; the ZDR parser learns the new kinds.</li><li>FR-AI-003 memory audit bridge - the two new audit kinds.</li><li>Cross-cutting: the server-dispatch flip touches <code>services/ai-gateway/src/server.rs</code> (the HTTP handler), which is the one architecturally significant change in this FR.</li></ul>\n<h2>AI Risk Assessment</h2>\n<h3>Data sources</h3>\n<p>No training or fine-tuning happens here. The adapters forward already-redacted prompts (PII stripped at FR-AI-011/012 before the router) to a local model the operator controls. For local providers, prompt data never leaves the deployment host, which is the strongest residency and retention posture available.</p>\n<h3>Human oversight</h3>\n<p>Provider selection is set by the tenant operator in policy YAML and reviewed like any policy change. The serving flip is gated by the test suite and the CAF gate. A human (Stephen) approves the policy that points a tenant at a local model. There is no autonomous provider switching beyond the declared fallback chain.</p>\n<h3>Failure modes</h3>\n<p>If the local server is down, the adapter fails closed and the router falls over to the next provider in the chain, or returns a terminal error if none remain; the user sees an error, never an echo or a fabricated answer. If the local server returns malformed JSON, the parse returns a <code>RouterError::InvalidResponse</code>. If a local model id is misconfigured, the server's own error surfaces through the adapter. None of these can produce a silent wrong answer.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Claude (Cowork), authoring this FR from Stephen's capability request and the existing FR-AI-008 router code.</li><li>Scope: full draft of this specification, including the normative clauses and acceptance criteria. The LocalOpenai and Ollama adapter code is authored in the same session.</li><li>Human review: Stephen reviews and approves before status moves past draft; the paired audit (FR-AI-105.audit.md) and the CAF gate validate before merge.</li></ul>";
