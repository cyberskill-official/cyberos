window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INV-011"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The INV service <strong>MUST</strong> ship revenue recognition at <code>services/invoicing/src/recognition/</code> supporting 4 methods, monthly EOM rollforward, journal entry pairs, immutable snapshots, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> support 4 recognition methods per DEC-1560:</li></ol>\n<ul><li><code>time_based</code>: equal monthly amount over engagement.start_date → end_date</li><li><code>milestone_based</code>: recognize when milestone achieved (FR-PROJ-002 milestone marked complete)</li><li><code>pct_completion</code>: recognize by % of project hours billed (uses FR-TIME-002 time entries)</li><li><code>point_in_time</code>: full recognition on invoice send (one-off services)</li></ul>\n<ol><li><strong>MUST</strong> validate <code>recognition_method</code> against closed enum per DEC-1561.</li></ol>\n<ol><li><strong>MUST</strong> build schedule at engagement creation via <code>scheduler.rs::build(engagement)</code>:</li></ol>\n<ul><li>time_based → equal split across N months</li><li>milestone_based → per-milestone bucket</li><li>pct_completion → recompute on hours change</li><li>point_in_time → single bucket at send_date</li></ul>\n<ol><li><strong>MUST</strong> run rollforward at EOM tenant_timezone per DEC-1562 via FR-MCP-007 task:</li></ol>\n<ul><li>For each engagement, compute period revenue recognized</li><li>Emit journal entry pair per DEC-1563</li><li>Insert immutable snapshot row</li></ul>\n<ol><li><strong>MUST</strong> generate journal entry pairs per DEC-1563:</li></ol>\n<ul><li>On invoice issuance: <code>DR AR / CR Deferred Revenue</code></li><li>On recognition: <code>DR Deferred Revenue / CR Revenue</code></li><li>Pairs balance (sum DR == sum CR per entry)</li></ul>\n<ol><li><strong>MUST</strong> define tables at migration <code>0010</code>: ```sql CREATE TABLE recognition_schedules ( schedule_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, recognition_method TEXT NOT NULL CHECK (recognition_method IN ('time_based','milestone_based','pct_completion','point_in_time')), period_start DATE NOT NULL, period_end DATE NOT NULL, planned_amount NUMERIC(18,4) NOT NULL, recognized_amount NUMERIC(18,4) NOT NULL DEFAULT 0, currency TEXT NOT NULL, reconcile_status TEXT NOT NULL DEFAULT 'pending' CHECK (reconcile_status IN ('pending','partial','complete')), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now() ); CREATE UNIQUE INDEX recognition_schedule_period_idx ON recognition_schedules(tenant_id, engagement_id, period_start, period_end); ALTER TABLE recognition_schedules ENABLE ROW LEVEL SECURITY; CREATE POLICY rec_sched_rls ON recognition_schedules USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON recognition_schedules FROM cyberos_app; GRANT UPDATE (recognized_amount, reconcile_status, updated_at) ON recognition_schedules TO cyberos_app;</li></ol>\n<p>   CREATE TABLE recognition_snapshots (      snapshot_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      period_end DATE NOT NULL,      engagement_id UUID NOT NULL,      period_revenue NUMERIC(18,4) NOT NULL,      deferred_revenue_change NUMERIC(18,4) NOT NULL,      ending_deferred_revenue NUMERIC(18,4) NOT NULL,      currency TEXT NOT NULL,      journal_entry_id UUID,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    CREATE UNIQUE INDEX rec_snap_period_idx      ON recognition_snapshots(tenant_id, engagement_id, period_end);    ALTER TABLE recognition_snapshots ENABLE ROW LEVEL SECURITY;    CREATE POLICY rec_snap_rls ON recognition_snapshots      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON recognition_snapshots FROM cyberos_app;    -- No GRANT UPDATE — snapshots immutable per DEC-1566</p>\n<p>   CREATE TABLE journal_entries (      journal_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      entry_date DATE NOT NULL,      description TEXT NOT NULL,      debit_account TEXT NOT NULL,      credit_account TEXT NOT NULL,      amount NUMERIC(18,4) NOT NULL CHECK (amount &gt; 0),      currency TEXT NOT NULL,      engagement_id UUID,      invoice_id UUID,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    ALTER TABLE journal_entries ENABLE ROW LEVEL SECURITY;    CREATE POLICY journal_rls ON journal_entries      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON journal_entries FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> make <code>recognition_snapshots</code> immutable per DEC-1566 — corrections via NEW <code>prior_period_adjustment</code> snapshot, never UPDATE.</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1565. PII scrub per FR-MEMORY-111: amount/period_revenue SHA-256 hashed; engagement_id (uuid) ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from EOM cron → rollforward → schedule update → journal → snapshot.</li></ol>\n<ol><li><strong>MUST</strong> use <code>rust_decimal::Decimal</code> for ALL amounts — never f64.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior-period snapshot per DEC-1566.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip journal entry on any recognition event per DEC-1563.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 4 methods (DEC-1560)?</strong> Covers consultancy spectrum — subscription, project (milestone), T&amp;M consulting (pct-completion), one-off (point-in-time). ASC 606 enumerates these patterns.</p>\n<p><strong>Why immutable snapshots (DEC-1566)?</strong> Audit lineage requires period close to be unrevisable; restatements happen via prior-period adjustment journal entries.</p>\n<p><strong>Why monthly EOM (DEC-1562)?</strong> Aligns with month-end close cycle; weekly/quarterly are less common.</p>\n<p><strong>Why per-engagement schedule (DEC-1564)?</strong> Each engagement has independent contract terms; aggregation done at report time.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST   /v1/inv/recognition/schedules             (CFO assigns method to engagement)\nGET    /v1/inv/recognition/schedules/{eng_id}    (view schedule)\nGET    /v1/inv/recognition/snapshots/{period}    (period rollforward)\nPOST   /v1/inv/recognition/rollforward           (manual trigger, CFO)\nGET    /v1/inv/recognition/journal-entries       (audit query)</code></pre>\n<p>Sample schedule (time_based, 12-month $120k):</p>\n<pre><code class=\"language-json\">[\n  {\"period_end\": \"2026-01-31\", \"planned_amount\": 10000, \"recognized_amount\": 10000, \"status\": \"complete\"},\n  {\"period_end\": \"2026-02-28\", \"planned_amount\": 10000, \"recognized_amount\": 10000, \"status\": \"complete\"},\n  ...\n  {\"period_end\": \"2026-12-31\", \"planned_amount\": 10000, \"recognized_amount\": 0, \"status\": \"pending\"}\n]</code></pre>\n<p>Sample journal pair on rollforward:</p>\n<pre><code class=\"language-json\">[\n  {\"date\": \"2026-05-31\", \"description\": \"May revenue recognition\", \"DR\": \"Deferred Revenue\", \"CR\": \"Revenue\", \"amount\": 10000, \"engagement_id\": \"...\"},\n  {\"date\": \"2026-05-31\", \"description\": \"May AR offset (already booked at issue)\", \"DR\": \"AR\", \"CR\": \"Deferred Revenue\", \"amount\": 0}  // no-op for pre-billed\n]</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>4 methods supported + cardinality test</strong>. 2. <strong>Schedule built at engagement creation</strong>. 3. <strong>EOM rollforward via FR-MCP-007 cron</strong>. 4. <strong>Journal entries balance (DR sum = CR sum)</strong>. 5. <strong>Snapshots immutable (no UPDATE/DELETE grant)</strong>. 6. <strong>Prior-period adjustment via new snapshot</strong>. 7. <strong>rust_decimal for amounts (no f64)</strong>. 8. <strong>4 memory audit kinds</strong>. 9. <strong>PII scrubbed (amount SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>Trace_id preserved</strong>. 12. <strong>Multi-currency: schedule in engagement currency; report base via FR-INV-002</strong>. 13. <strong>time_based: equal monthly split</strong>. 14. <strong>milestone_based: recognize on milestone complete event</strong>. 15. <strong>pct_completion: recompute on FR-TIME-002 entry change</strong>. 16. <strong>point_in_time: full on invoice send</strong>. 17. <strong>Manual rollforward CFO-only</strong>. 18. <strong>Idempotent (UNIQUE on tenant+engagement+period)</strong>. 19. <strong>Failure mid-rollforward → snapshot=failed + sev-1</strong>. 20. <strong>Reconcile_status auto-updates (pending→partial→complete)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn time_based_equal_split() {\n    let ctx = TestContext::engagement_with_terms(120000, 12, \"time_based\").await;\n    let schedule = ctx.fetch_schedule(ctx.engagement_id).await;\n    assert_eq!(schedule.len(), 12);\n    for s in schedule { assert_eq!(s.planned_amount, dec!(10000)); }\n}\n\n#[tokio::test]\nasync fn snapshots_immutable() {\n    let ctx = TestContext::with_completed_period().await;\n    let snap = ctx.fetch_snapshot(ctx.period_end).await;\n    let result = ctx.try_mutate_snapshot(snap.snapshot_id).await;\n    assert!(result.is_err());  // RLS + REVOKE blocks\n}\n\n#[tokio::test]\nasync fn journal_entries_balance() {\n    let ctx = TestContext::with_rollforward_run().await;\n    let entries = ctx.fetch_journal_entries_for_period().await;\n    let total_dr: Decimal = entries.iter().map(|e| e.amount).sum();\n    let total_cr: Decimal = entries.iter().map(|e| e.amount).sum();\n    assert_eq!(total_dr, total_cr);\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§6 — Skeleton</h2>\n<pre><code class=\"language-rust\">pub async fn rollforward(period_end: NaiveDate, tenant: &amp;Tenant, db: &amp;Db) -&gt; Result&lt;RollforwardResult&gt; {\n    let engagements = db.fetch_active_engagements(tenant.id).await?;\n    let trace = current_span_trace_id();\n    for eng in engagements {\n        let period_revenue = match eng.recognition_method.as_str() {\n            \"time_based\" =&gt; time_based_amount(&amp;eng, period_end),\n            \"milestone_based\" =&gt; milestone_revenue(&amp;eng, period_end, db).await?,\n            \"pct_completion\" =&gt; pct_completion_revenue(&amp;eng, period_end, db).await?,\n            \"point_in_time\" =&gt; point_in_time_revenue(&amp;eng, period_end, db).await?,\n            _ =&gt; unreachable!()\n        };\n        let journal_id = journal_entry::create_recognition_pair(&amp;eng, period_revenue, period_end, db).await?;\n        db.insert_snapshot(eng.id, period_end, period_revenue, journal_id, trace).await?;\n        db.update_schedule_recognized(&amp;eng, period_end, period_revenue).await?;\n        audit::emit(\"inv.revenue_recognized\", json!({...}), trace).await?;\n    }\n    audit::emit(\"inv.revenue_rollforward_completed\", json!({\"period_end\": period_end}), trace).await?;\n    Ok(RollforwardResult{snapshots_created: engagements.len()})\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-INV-001. <strong>Cross-module:</strong> FR-MCP-007 (cron), FR-TIME-002 (hours for pct_completion), FR-PROJ-002 (milestones), FR-MEMORY-111 (PII), FR-AUTH-101 (CFO role).</p>\n<h2>§8 — Sample payloads (see §3)</h2>\n<h2>§9 — Open questions</h2>\n<p>None blocking — ASC 606 + IFRS 15 patterns well-established.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Engagement no method set</td><td>scheduler check</td><td>sev-2 + skip rollforward</td><td>CFO assigns method</td></tr><tr><td>Journal entries don't balance</td><td>post-condition check</td><td>rollback + sev-1</td><td>data investigation</td></tr><tr><td>Snapshot mutation attempt</td><td>RLS + REVOKE</td><td>DB error</td><td>inherent</td></tr><tr><td>pct_completion negative hours</td><td>scheduler validate</td><td>sev-2 + skip period</td><td>FR-TIME-002 fix</td></tr><tr><td>Milestone never completes</td><td>indefinite deferral</td><td>annual sweep alerts</td><td>CFO review</td></tr><tr><td>Mid-month engagement add</td><td>next EOM rollforward picks up</td><td>inherent</td><td>inherent</td></tr><tr><td>Mid-rollforward crash</td><td>partial snapshots</td><td>resume from last successful eng</td><td>re-run</td></tr><tr><td>Multi-currency engagement w/o FX</td><td>rollforward fail</td><td>sev-1</td><td>provide FX</td></tr><tr><td>Engagement modified mid-rollforward</td><td>snapshot SQL</td><td>uses snapshot row state</td><td>inherent</td></tr><tr><td>Decimal precision drift</td><td>rust_decimal</td><td>exact</td><td>inherent</td></tr><tr><td>Period not yet ended</td><td>guard</td><td>reject</td><td>wait</td></tr><tr><td>Duplicate rollforward run</td><td>UNIQUE constraint</td><td>ON CONFLICT DO NOTHING</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 EOM tenant_timezone: midnight of first of next month; jitter ±30min to avoid thundering herd.</li><li>§11.2 pct_completion formula: <code>recognized = (hours_completed / hours_estimated) * total_contract</code>; cap at 100%.</li><li>§11.3 memory audit body: engagement_id (uuid), period_end (date), period_revenue SHA256 hashed.</li><li>§11.4 Snapshots are reportable via FR-INV-009 + future financial-statements FR.</li><li>§11.5 Prior-period adjustment pattern: new snapshot with <code>description: 'PPA: &lt;reason&gt;'</code>; original snapshot unchanged.</li></ul>\n<hr>\n<p><em>End of FR-INV-011 spec.</em></p>";
