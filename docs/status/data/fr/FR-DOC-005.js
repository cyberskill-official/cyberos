window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOC-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The DOC service <strong>MUST</strong> ship multi-party signing at <code>services/doc/src/signing/</code> supporting 3 workflow types, FR-DOC-006 verification per signer, FR-DOC-002/003/004 CA routing per region, reminder cron, 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/doc/documents/{id}/signing-workflows</code> body <code>{ workflow_kind, signers: [{signer_id, region, required_assurance_level, position?}], expires_in_days?, reminder_cadence_override? }</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>workflow_kind</code> against closed enum per DEC-1751.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>signature_status</code> against closed enum per DEC-1752.</li></ol>\n<ol><li><strong>MUST</strong> dispatch per kind:</li></ol>\n<ul><li><code>ordered_runner.rs::run(workflow)</code> — invite signer[0], wait for sign, invite signer[1], etc.</li><li><code>parallel_runner.rs::run(workflow)</code> — invite all signers immediately.</li><li><code>counter_sign_runner.rs::run(workflow)</code> — initiator (position=0) signs first, then invites others in parallel.</li></ul>\n<ol><li><strong>MUST</strong> for each signer per DEC-1754:</li></ol>\n<ul><li>Invite via FR-EMAIL-009 with sign link.</li><li>On click: trigger FR-DOC-006 verification (method per signer's region + required_assurance_level).</li><li>Only if verified=verified: apply signature via FR-DOC-002/003/004 (routed per DEC-1755).</li><li>Set status=signed; emit audit.</li></ul>\n<ol><li><strong>MUST</strong> route CA per signer region per DEC-1755:</li></ol>\n<ul><li>region=vn → FR-DOC-004 (VN CA)</li><li>region=eu → FR-DOC-002 (eIDAS QTSP)</li><li>region=other → FR-DOC-003 (AATL)</li></ul>\n<ol><li><strong>MUST</strong> send reminders per DEC-1753 via FR-MCP-007 cron — at 24h, 72h, 7d after invite; configurable per tenant.</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0006</code>: ```sql CREATE TABLE doc_signing_workflows ( workflow_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, document_id UUID NOT NULL, workflow_kind TEXT NOT NULL CHECK (workflow_kind IN ('ordered','parallel','counter_sign')), status TEXT NOT NULL DEFAULT 'in_progress' CHECK (status IN ('in_progress','completed','withdrawn','expired','failed')), expires_at TIMESTAMPTZ NOT NULL, reminder_cadence_hours INT[] NOT NULL DEFAULT '{24,72,168}', created_by UUID NOT NULL, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), completed_at TIMESTAMPTZ ); ALTER TABLE doc_signing_workflows ENABLE ROW LEVEL SECURITY; CREATE POLICY workflows_rls ON doc_signing_workflows USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON doc_signing_workflows FROM cyberos_app; GRANT UPDATE (status, completed_at) ON doc_signing_workflows TO cyberos_app;</li></ol>\n<p>   CREATE TABLE doc_signers (      signer_row_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      workflow_id UUID NOT NULL REFERENCES doc_signing_workflows(workflow_id),      signer_id UUID NOT NULL,      region TEXT NOT NULL,      required_assurance_level TEXT NOT NULL CHECK (required_assurance_level IN ('low','substantial','high')),      position INT,  -- NULL for parallel; ordered/counter_sign use 0,1,2...      status TEXT NOT NULL DEFAULT 'pending'        CHECK (status IN ('pending','in_progress','signed','declined','expired','withdrawn')),      invited_at TIMESTAMPTZ,      signed_at TIMESTAMPTZ,      verification_id UUID,  -- FK to FR-DOC-006      ca_routing TEXT,      signature_payload BYTEA,      last_reminder_at TIMESTAMPTZ,      reminder_count INT NOT NULL DEFAULT 0,      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    CREATE INDEX signers_workflow_idx ON doc_signers(tenant_id, workflow_id, position);    ALTER TABLE doc_signers ENABLE ROW LEVEL SECURITY;    CREATE POLICY signers_rls ON doc_signers      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON doc_signers FROM cyberos_app;    GRANT UPDATE (status, invited_at, signed_at, verification_id, ca_routing,                  signature_payload, last_reminder_at, reminder_count) ON doc_signers TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> handle decline per DEC-1752 — signer.status=declined → block workflow completion → workflow.status=failed.</li></ol>\n<ol><li><strong>MUST</strong> emit 6 memory audit kinds per DEC-1756. PII per FR-MEMORY-111: signer_id (uuid) ok; signature_payload hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id through workflow → invite → verify → sign → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> apply signature without successful FR-DOC-006 verification per DEC-1754.</li></ol>\n<ol><li><strong>MUST NOT</strong> route signature to wrong CA per region per DEC-1755.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 3 kinds (DEC-1750)?</strong> Ordered = legal sign chain (junior → senior), parallel = NDA-style (all sign independently), counter_sign = our signature first then customers.</p>\n<p><strong>Why verify-before-sign (DEC-1754)?</strong> Signature attached to wrong identity = signing fraud → court-ineffective.</p>\n<p><strong>Why per-region CA (DEC-1755)?</strong> Regulatory: EU eIDAS, US AATL, VN Decree 130; cross-routing breaks legal validity.</p>\n<p><strong>Why declined blocks workflow (DEC-1752)?</strong> Without all signers, contract incomplete; workflow.status=failed lets AM restart cleanly.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST   /v1/doc/documents/{id}/signing-workflows\nGET    /v1/doc/signing-workflows/{id}            (status + signer list)\nPOST   /v1/doc/signing-workflows/{id}/withdraw   (CLO/AM only)\nPOST   /v1/doc/signers/{id}/sign                 (signer-authenticated callback)\nPOST   /v1/doc/signers/{id}/decline              (signer-authenticated)</code></pre>\n<p>Sample workflow:</p>\n<pre><code class=\"language-json\">{\n  \"workflow_kind\": \"ordered\",\n  \"signers\": [\n    {\"signer_id\": \"uuid-cto\", \"region\": \"vn\", \"required_assurance_level\": \"high\", \"position\": 0},\n    {\"signer_id\": \"uuid-customer\", \"region\": \"vn\", \"required_assurance_level\": \"substantial\", \"position\": 1}\n  ],\n  \"expires_in_days\": 14\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>3-kind workflow enum + cardinality test</strong>. 2. <strong>6-status enum + cardinality test</strong>. 3. <strong>Ordered: signer N+1 invited only after N signs</strong>. 4. <strong>Parallel: all invited at start</strong>. 5. <strong>Counter_sign: position-0 signs then others invited</strong>. 6. <strong>Verification required before sign</strong>. 7. <strong>CA routed per region (vn/eu/other)</strong>. 8. <strong>Reminder cron 24h/72h/7d</strong>. 9. <strong>Reminder cadence configurable per tenant</strong>. 10. <strong>Decline → workflow=failed</strong>. 11. <strong>All signed → workflow=completed</strong>. 12. <strong>Expires at expires_at → workflow=expired + remaining signers=expired</strong>. 13. <strong>6 memory audit kinds emitted</strong>. 14. <strong>PII scrubbed (signature payload SHA256)</strong>. 15. <strong>RLS denies cross-tenant</strong>. 16. <strong>Trace_id preserved</strong>. 17. <strong>Withdraw by initiator allowed</strong>. 18. <strong>Append-only via REVOKE UPDATE except status cols</strong>. 19. <strong>Verification level mismatch → cannot sign</strong>. 20. <strong>Multi-region workflow: each signer routes to correct CA</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn ordered_invites_sequentially() {\n    let ctx = TestContext::ordered_workflow(3_signers).await;\n    let s1 = ctx.signers()[0];\n    assert!(s1.invited_at.is_some());\n    let s2 = ctx.signers()[1];\n    assert!(s2.invited_at.is_none());\n    ctx.sign(s1.id).await;\n    let s2_after = ctx.signers()[1];\n    assert!(s2_after.invited_at.is_some());\n}\n\n#[tokio::test]\nasync fn parallel_all_invited_at_start() {\n    let ctx = TestContext::parallel_workflow(3_signers).await;\n    for s in ctx.signers() {\n        assert!(s.invited_at.is_some());\n    }\n}\n\n#[tokio::test]\nasync fn decline_fails_workflow() {\n    let ctx = TestContext::parallel_workflow(3_signers).await;\n    ctx.decline(ctx.signers()[0].id).await;\n    let wf = ctx.fetch_workflow(ctx.workflow_id).await;\n    assert_eq!(wf.status, \"failed\");\n}\n\n#[tokio::test]\nasync fn ca_routed_by_region() {\n    let ctx = TestContext::multi_region_workflow().await;\n    ctx.sign_all().await;\n    let signers = ctx.signers();\n    let vn = signers.iter().find(|s| s.region == \"vn\").unwrap();\n    assert!(vn.ca_routing.as_deref().unwrap().contains(\"vn-ca\"));\n    let eu = signers.iter().find(|s| s.region == \"eu\").unwrap();\n    assert!(eu.ca_routing.as_deref().unwrap().contains(\"qtsp\"));\n}\n\n// 5.5..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-DOC-001, FR-DOC-006. <strong>Cross-module:</strong> FR-DOC-002/003/004 (CA per region), FR-EMAIL-009 (invite + reminder), FR-MCP-007 (reminder cron), FR-AUTH-101 (initiator role), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Verification fails</td><td>check pre-sign</td><td>signer stays pending; no signature</td><td>retry</td></tr><tr><td>CA route fails</td><td>downstream err</td><td>signer status=failed; sev-1</td><td>retry</td></tr><tr><td>Signer email bounces</td><td>FR-EMAIL-009 status</td><td>sev-2 + reminder via SMS</td><td>manual outreach</td></tr><tr><td>Workflow expires</td><td>cron check</td><td>status=expired</td><td>restart</td></tr><tr><td>Ordered position skipped</td><td>check sequence</td><td>error if N+1 invited before N</td><td>inherent</td></tr><tr><td>Decline mid-ordered</td><td>block all subsequent</td><td>workflow=failed</td><td>restart</td></tr><tr><td>Withdraw during signing</td><td>reject signed signers</td><td>inherent (signed=immutable)</td><td>inherent</td></tr><tr><td>Multi-CA concurrent</td><td>per-signer call</td><td>independent</td><td>inherent</td></tr><tr><td>Region mismatch (signer.region invalid)</td><td>validate</td><td>reject 400</td><td>use valid</td></tr><tr><td>Cross-tenant workflow</td><td>RLS</td><td>403</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Sign link is signed token with workflow_id + signer_row_id; expires per workflow.</li><li>§11.2 Reminder cron: per workflow, computes elapsed since invited_at; sends if matches cadence_hours[i].</li><li>§11.3 Workflow engine state machine: in_progress → completed | failed | expired | withdrawn.</li><li>§11.4 memory audit body: workflow_id, signer_id, kind; signature payload SHA256.</li><li>§11.5 CA router maps region to FR-DOC-002/003/004 module; future regions extend the table.</li></ul>\n<hr>\n<p><em>End of FR-DOC-005 spec.</em></p>";
