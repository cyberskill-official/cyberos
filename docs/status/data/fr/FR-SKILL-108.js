window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-SKILL-108"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The <code>vietnam-mst-validate@1</code> skill <strong>MUST</strong> validate Vietnamese Tax IDs (MST) using a two-stage process: local checksum + remote GDT verification. The contract:</p>\n<ol><li><strong>MUST</strong> accept input as a 10-digit or 13-digit MST string. 10-digit = company head office; 13-digit = company branch (<code>AAAAAAAAAA-BBB</code> format where AAAAAAAAAA is the parent 10-digit and BBB is the branch suffix).</li><li><strong>MUST</strong> run local checksum FIRST per the official GDT algorithm:</li></ol>\n<ul><li>10-digit: <code>Sum = Σ digit[i] × weight[i]</code> where weights <code>= [31, 29, 23, 19, 17, 13, 7, 5, 3, 1]</code>. Check digit (last) = <code>10 - (Sum % 11) mod 10</code>, mod 10.</li><li>13-digit: validate the parent 10-digit first (same algorithm); branch suffix (BBB) is informational, no checksum.</li></ul>\n<ol><li><strong>MUST</strong> reject with <code>MstError::ChecksumFailed</code> if local checksum doesn't match. Network call NOT attempted on checksum failure (saves the round-trip).</li><li><strong>MUST</strong> call the GDT public API at <code>https://gdtapi.gdt.gov.vn/Service.asmx</code> with SOAP envelope (canonical method <code>ttin_NNT_DN</code>). Parse the XML response:</li></ol>\n<ul><li><code>valid: true</code> if response contains <code>&lt;TrangThai&gt;00&lt;/TrangThai&gt;</code> (active status).</li><li><code>name: &lt;DiaChiNNT&gt;</code> (registered tax name).</li><li><code>address: &lt;DiaChi&gt;</code> (registered address).</li><li><code>business_type: &lt;NganhNghe&gt;</code> (registered business sector).</li><li><code>valid_from: &lt;NgayCap&gt;</code> (registration date).</li><li><code>status: &lt;TenTrangThai&gt;</code> (human-readable status).</li></ul>\n<ol><li><strong>MUST</strong> treat the following GDT statuses as \"valid for transactions\":</li></ol>\n<ul><li><code>00</code> — đang hoạt động (active).</li><li><code>04</code> — chờ đóng cửa (closing; warn but accept). All others → reject with <code>MstError::Inactive { status, status_text }</code>.</li></ul>\n<ol><li><strong>MUST</strong> cache validation outcomes in sled-backed local cache with 24h TTL. Cache key: MST string. Cache hit within TTL → no GDT call; cache miss OR expired → fresh call.</li><li><strong>MUST</strong> support <code>force_refresh: true</code> flag to bypass cache (operator-controlled; e.g. when re-validating after a known status change).</li><li><strong>MUST</strong> retry GDT calls on transient failure with exp backoff: 3 retries at 500ms, 2s, 8s. Total max wait ~10s. Permanent failure → <code>MstError::GdtUnreachable</code>.</li><li><strong>MUST</strong> rate-limit GDT calls to 100 req/min globally (per GDT's published cap). Implemented via governor token bucket; over-limit returns <code>MstError::RateLimited</code> immediately (don't burn budget on already-rate-limited calls).</li><li><strong>MUST</strong> emit memory audit row <code>compliance.mst_validated</code> on EVERY validation (cache hit or fresh) with payload <code>{mst_redacted, valid, status, business_name, validated_at_ns, cache_hit, gdt_round_trip_ms, trace_id}</code>. <code>mst_redacted</code> is <code>XXXXXX{last_4}</code> (last 4 digits visible per PDPL).</li><li><strong>MUST</strong> log MSTs as redacted (<code>{first_2}*****{last_2}</code>) — never plain text.</li><li><strong>MUST</strong> offline-mode fallback: if <code>CYBEROS_OFFLINE=true</code> OR network unreachable, return last cached outcome (regardless of TTL) with <code>stale: true</code> flag. Caller decides whether to accept stale data.</li><li><strong>MUST</strong> emit OTel span <code>skill.vn_mst.validate</code> with attributes <code>mst_redacted</code>, <code>valid</code>, <code>cache_hit</code>, <code>gdt_round_trip_ms</code>, <code>duration_ms</code>.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>skill_vn_mst_validations_total{outcome}</code> (counter; outcome ∈ valid | checksum_failed | inactive | gdt_unreachable | rate_limited | offline_stale).</li><li><code>skill_vn_mst_gdt_round_trip_seconds</code> (histogram; only when cache_hit=false).</li><li><code>skill_vn_mst_cache_hit_ratio</code> (gauge).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why two-stage validation (§1 #2 + #4)?</strong> Local checksum catches 99% of typos at zero cost; GDT call catches the remaining 1% (validly-checksummed but registry-inactive). Skipping local would burn GDT quota on obvious typos; skipping GDT would miss the truly-invalid MSTs.</p>\n<p><strong>Why cache 24h (§1 #6, DEC-212)?</strong> Onboarding flows revalidate the same MST many times in a day (form submit + edit + retry). 24h cache catches same-day churn without staleness risk: MST status changes are rare, and FR-INV (Invoice module) re-validates before each hóa đơn anyway.</p>\n<p><strong>Why audit on cache hit too (§1 #10)?</strong> KYC compliance needs the trail. The auditor's question is \"did you check this MST before emitting hóa đơn?\" — answer = \"yes, validation row at &lt;time&gt; with status=&lt;x&gt;\". Cache hit doesn't mean \"we didn't check\"; it means \"we checked recently.\"</p>\n<p><strong>Why <code>04</code> accepted (§1 #5)?</strong> Status <code>04</code> is \"winding down\" — the company is still legally trading but in dissolution. Hóa đơn is legal during this period; rejecting it would block valid business. We warn the caller but accept.</p>\n<p><strong>Why redact MST in logs (§1 #11, DEC-213)?</strong> PDPL 2025 Art. 6 lists tax IDs as restricted personal data. Logs persist beyond the validation event; redacting (XXXXX1234) lets operators trace flows without exposing the full ID. The audit row (encrypted, retention-controlled) carries the redacted form too.</p>\n<p><strong>Why governor rate limit (§1 #9)?</strong> GDT publishes a 100-req/min cap. Exceeding triggers IP-level block (15 minutes). Better to fail fast (RateLimited) and let the caller queue or retry than to consume the quota and lock the platform.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>SKILL.md</h3>\n<pre><code class=\"language-markdown\">---\nid: vietnam-mst-validate\nversion: 1.0.0\ndescription: Validate Vietnamese Tax ID (MST) via GDT public API + local checksum.\nallowed_memory_scopes:\n  - memories/compliance/mst/**\nallowed_tools:\n  - MemoryEmit\n  - HttpFetch\nsync_class: private    # MST data is PDPL-restricted; never sync\ntenant_scope: any\neffort_minutes: 1\ntags: [vn, mst, compliance, kyc, gdt]\nsignature:\n  algo: ed25519\n  public_key_hex: \"&lt;release-populated&gt;\"\n  signature_hex:  \"&lt;release-populated&gt;\"\nx-allowed-domains:\n  - gdtapi.gdt.gov.vn\n---\n\n# vietnam-mst-validate@1\n\nVietnamese Tax ID (MST) validation skill.\n\n```rust\nuse cyberos_vn_mst_validate::{validate_mst, ValidateOptions};\n\nlet outcome = validate_mst(\"0312345678\", ValidateOptions::default()).await?;\nif outcome.valid {\n    println!(\"MST belongs to: {} ({})\", outcome.business_name, outcome.business_type);\n}</code></pre>\n<pre><code>\n### Rust API\n\n```rust\n// skills/vietnam-mst-validate/src/lib.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Debug, Serialize, Deserialize)]\npub struct ValidationOutcome {\n    pub mst:           String,         // original (plain) — caller holds; redacted in logs\n    pub valid:         bool,\n    pub status:        String,         // \"00\", \"04\", etc.\n    pub status_text:   String,         // \"đang hoạt động\"\n    pub business_name: Option&lt;String&gt;,\n    pub address:       Option&lt;String&gt;,\n    pub business_type: Option&lt;String&gt;,\n    pub valid_from:    Option&lt;String&gt;, // ISO date\n    pub validated_at:  i64,            // unix ms\n    pub cache_hit:     bool,\n    pub stale:         bool,           // true when offline-fallback used\n    pub trace_id:      String,\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum MstError {\n    #[error(\"MST must be 10 or 13 digits (got {0} chars)\")]    InvalidLength(usize),\n    #[error(\"MST checksum failed\")]                            ChecksumFailed,\n    #[error(\"MST inactive (status {status}: {status_text})\")]  Inactive { status: String, status_text: String },\n    #[error(\"GDT API unreachable after 3 retries\")]            GdtUnreachable,\n    #[error(\"rate limited; retry after Δ\")]                    RateLimited,\n    #[error(\"GDT response parse failed: {0}\")]                 ParseError(String),\n}\n\n#[derive(Default)]\npub struct ValidateOptions {\n    pub force_refresh: bool,\n}\n\npub async fn validate_mst(mst: &amp;str, opts: ValidateOptions) -&gt; Result&lt;ValidationOutcome, MstError&gt; {\n    let normalised = normalise_mst(mst)?;\n    checksum::verify(&amp;normalised)?;\n\n    // Cache lookup (unless force_refresh)\n    if !opts.force_refresh {\n        if let Some(cached) = cache::get(&amp;normalised).await {\n            return Ok(ValidationOutcome { cache_hit: true, ..cached });\n        }\n    }\n    // Offline mode → stale cache OR error\n    if std::env::var(\"CYBEROS_OFFLINE\").as_deref() == Ok(\"true\") {\n        return cache::get_stale(&amp;normalised).await\n            .map(|c| ValidationOutcome { stale: true, ..c })\n            .ok_or(MstError::GdtUnreachable);\n    }\n\n    // Rate limit\n    if !gdt_client::rate_limiter().check() { return Err(MstError::RateLimited); }\n\n    // GDT call with retry\n    let raw = gdt_client::fetch_with_retry(&amp;normalised).await?;\n    let outcome = gdt_client::parse_response(&amp;raw, &amp;normalised)?;\n\n    // Cache the result\n    cache::put(&amp;normalised, &amp;outcome).await;\n\n    // Emit memory audit row\n    emit_audit_row(&amp;outcome).await;\n\n    Ok(outcome)\n}\n\nfn normalise_mst(mst: &amp;str) -&gt; Result&lt;String, MstError&gt; {\n    let cleaned: String = mst.chars().filter(|c| c.is_ascii_digit() || *c == '-').collect();\n    let digits_only: String = cleaned.chars().filter(|c| c.is_ascii_digit()).collect();\n    match digits_only.len() {\n        10 =&gt; Ok(digits_only),\n        13 =&gt; Ok(format!(\"{}-{}\", &amp;digits_only[..10], &amp;digits_only[10..])),\n        n  =&gt; Err(MstError::InvalidLength(n)),\n    }\n}</code></pre>\n<h3>Checksum</h3>\n<pre><code class=\"language-rust\">// skills/vietnam-mst-validate/src/checksum.rs\nuse crate::MstError;\n\nconst WEIGHTS: [u32; 10] = [31, 29, 23, 19, 17, 13, 7, 5, 3, 1];\n\npub fn verify(normalised: &amp;str) -&gt; Result&lt;(), MstError&gt; {\n    // Strip branch suffix if present\n    let parent = if normalised.contains('-') {\n        normalised.split('-').next().unwrap()\n    } else { normalised };\n    if parent.len() != 10 { return Err(MstError::InvalidLength(parent.len())); }\n\n    let digits: Vec&lt;u32&gt; = parent.chars().map(|c| c.to_digit(10).unwrap()).collect();\n    let sum: u32 = digits.iter().zip(WEIGHTS.iter()).take(9).map(|(d, w)| d * w).sum();\n    let expected = (10 - (sum % 11)) % 10;\n    if digits[9] == expected { Ok(()) } else { Err(MstError::ChecksumFailed) }\n}</code></pre>\n<h3>GDT client (SOAP)</h3>\n<pre><code class=\"language-rust\">// skills/vietnam-mst-validate/src/gdt_client.rs\nuse std::time::Duration;\nuse governor::{Quota, RateLimiter};\nuse std::num::NonZeroU32;\nuse std::sync::OnceLock;\n\nconst GDT_URL:  &amp;str = \"https://gdtapi.gdt.gov.vn/Service.asmx\";\nconst RATE_LIMIT: NonZeroU32 = NonZeroU32::new(100).unwrap();\n\npub fn rate_limiter() -&gt; &amp;'static RateLimiter&lt;governor::state::NotKeyed, governor::state::InMemoryState, governor::clock::DefaultClock&gt; {\n    static R: OnceLock&lt;RateLimiter&lt;governor::state::NotKeyed, governor::state::InMemoryState, governor::clock::DefaultClock&gt;&gt; = OnceLock::new();\n    R.get_or_init(|| RateLimiter::direct(Quota::per_minute(RATE_LIMIT)))\n}\n\npub async fn fetch_with_retry(mst: &amp;str) -&gt; Result&lt;String, crate::MstError&gt; {\n    let body = format!(r#\"&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;soap:Envelope xmlns:soap=\"http://www.w3.org/2003/05/soap-envelope\"&gt;\n  &lt;soap:Body&gt;\n    &lt;ttin_NNT_DN xmlns=\"http://tempuri.org/\"&gt;\n      &lt;strMST&gt;{mst}&lt;/strMST&gt;\n    &lt;/ttin_NNT_DN&gt;\n  &lt;/soap:Body&gt;\n&lt;/soap:Envelope&gt;\"#);\n\n    let delays = [Duration::from_millis(500), Duration::from_secs(2), Duration::from_secs(8)];\n    for (i, delay) in delays.iter().enumerate() {\n        let start = std::time::Instant::now();\n        let resp = reqwest::Client::new()\n            .post(GDT_URL)\n            .header(\"Content-Type\", \"application/soap+xml; charset=utf-8\")\n            .body(body.clone())\n            .timeout(Duration::from_secs(5))\n            .send()\n            .await;\n        match resp {\n            Ok(r) if r.status().is_success() =&gt; {\n                let txt = r.text().await.map_err(|e| crate::MstError::ParseError(e.to_string()))?;\n                tracing::debug!(mst_redacted = redact(mst), round_trip_ms = start.elapsed().as_millis() as u64, \"gdt fetch ok\");\n                return Ok(txt);\n            }\n            Ok(r) =&gt; {\n                tracing::warn!(status = r.status().as_u16(), \"gdt non-success; retrying\");\n                if i + 1 == delays.len() { return Err(crate::MstError::GdtUnreachable); }\n            }\n            Err(e) =&gt; {\n                tracing::warn!(?e, \"gdt error; retrying\");\n                if i + 1 == delays.len() { return Err(crate::MstError::GdtUnreachable); }\n            }\n        }\n        tokio::time::sleep(*delay).await;\n    }\n    Err(crate::MstError::GdtUnreachable)\n}\n\npub fn parse_response(xml: &amp;str, mst: &amp;str) -&gt; Result&lt;crate::ValidationOutcome, crate::MstError&gt; {\n    // SOAP XML; parse via quick-xml + serde\n    let doc: GdtResponse = quick_xml::de::from_str(xml).map_err(|e| crate::MstError::ParseError(e.to_string()))?;\n    let valid = matches!(doc.status.as_str(), \"00\" | \"04\");\n    if !valid {\n        return Err(crate::MstError::Inactive { status: doc.status.clone(), status_text: doc.status_text.clone() });\n    }\n    Ok(crate::ValidationOutcome {\n        mst:           mst.into(),\n        valid:         true,\n        status_text:   doc.status_text,\n        business_name: Some(doc.tax_name),\n        address:       Some(doc.address),\n        business_type: Some(doc.business_sector),\n        valid_from:    Some(doc.registration_date),\n        validated_at:  chrono::Utc::now().timestamp_millis(),\n        cache_hit:     false,\n        stale:         false,\n        trace_id:      current_trace_id(),\n    })\n}\n\npub fn redact(mst: &amp;str) -&gt; String {\n    let n = mst.len();\n    if n &lt; 4 { return \"****\".into(); }\n    format!(\"{}{}\", &amp;mst[..2], \"*\".repeat(n - 4)) + &amp;mst[n-2..]\n}\n\n#[derive(serde::Deserialize)]\n#[serde(rename_all = \"PascalCase\")]\nstruct GdtResponse {\n    #[serde(rename = \"TrangThai\")] status:           String,\n    #[serde(rename = \"TenTrangThai\")] status_text:   String,\n    #[serde(rename = \"DiaChiNNT\")] tax_name:         String,\n    #[serde(rename = \"DiaChi\")] address:             String,\n    #[serde(rename = \"NganhNghe\")] business_sector:  String,\n    #[serde(rename = \"NgayCap\")] registration_date:  String,\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>10-digit MST: valid checksum</strong> — <code>0312345678</code> (real test vector) → checksum passes; GDT call proceeds.</li><li><strong>10-digit MST: invalid checksum</strong> — <code>1234567890</code> → <code>Err(ChecksumFailed)</code>; no GDT call made.</li><li><strong>13-digit MST normalised</strong> — <code>0312345678001</code> (no dash) → normalised to <code>0312345678-001</code>; parent checksum verified.</li><li><strong>Invalid length rejected</strong> — <code>123456789</code> (9 digits) → <code>Err(InvalidLength(9))</code>.</li><li><strong>GDT response parsed: valid (status 00)</strong> — mock GDT returns SOAP with <code>&lt;TrangThai&gt;00&lt;/TrangThai&gt;</code> → <code>valid: true</code>; business name populated.</li><li><strong>GDT response parsed: closing (status 04)</strong> — mock returns <code>04</code> → <code>valid: true</code> (per §1 #5); status_text set.</li><li><strong>GDT response parsed: inactive (status 03)</strong> — mock returns <code>03</code> → <code>Err(Inactive { status: \"03\", ... })</code>.</li><li><strong>GDT timeout retried 3×</strong> — mock returns 504 three times → SDK retries; 4th attempt unmocked succeeds; outcome ok.</li><li><strong>GDT unreachable after 3 retries</strong> — mock always 504 → <code>Err(GdtUnreachable)</code> after ~10s.</li><li><strong>Rate limit triggers RateLimited error</strong> — exhaust governor (101 calls in 60s) → next call immediate <code>Err(RateLimited)</code>.</li><li><strong>Cache hit within 24h</strong> — call twice within TTL → second call cache_hit: true; only 1 GDT request made.</li><li><strong>Cache miss after TTL</strong> — call once; advance time 25h; call again → cache miss; fresh GDT call.</li><li><strong>Force refresh bypasses cache</strong> — call once; <code>ValidateOptions { force_refresh: true }</code> → cache_hit: false; fresh GDT call.</li><li><strong>Offline mode returns stale cache</strong> — <code>CYBEROS_OFFLINE=true</code> → returns last cached outcome with <code>stale: true</code>.</li><li><strong>Offline + no cache → GdtUnreachable</strong> — <code>CYBEROS_OFFLINE=true</code> + cache empty → <code>Err(GdtUnreachable)</code>.</li><li><strong>memory audit row emitted</strong> — every successful validation → <code>compliance.mst_validated</code> row in memory; <code>mst_redacted</code> field redacts to <code>XX******1234</code> format.</li><li><strong>memory audit row on cache hit</strong> — cache hits also emit audit row (with <code>cache_hit: true</code>) for KYC trail.</li><li><strong>Logs redact MST</strong> — <code>tracing::info!(mst, ...)</code> in source → grep log file → no plain MST appears; redacted form (<code>03********78</code>) appears instead.</li><li><strong>OTel span emitted</strong> — <code>skill.vn_mst.validate</code> span per call with <code>mst_redacted</code>, <code>valid</code>, <code>cache_hit</code>, <code>gdt_round_trip_ms</code> attrs.</li><li><strong>Metrics increment</strong> — <code>skill_vn_mst_validations_total{outcome=\"valid\"}</code> non-zero after happy calls; <code>outcome=\"checksum_failed\"</code> non-zero after #2.</li><li><strong>SOAP XML parser robust to whitespace</strong> — varied whitespace in real GDT responses → parser succeeds.</li><li><strong>broker enforces allowed_tools</strong> — skill calls only MemoryEmit + HttpFetch; trying Bash within skill → broker denial (verified via fixture).</li><li><strong>domain enforcement</strong> — HttpFetch to non-<code>gdtapi.gdt.gov.vn</code> → broker denial.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// skills/vietnam-mst-validate/tests/checksum_test.rs\n#[test]\nfn valid_10_digit_passes() {\n    assert!(checksum::verify(\"0312345678\").is_ok());\n}\n#[test]\nfn invalid_checksum_fails() {\n    assert!(matches!(checksum::verify(\"1234567890\"), Err(MstError::ChecksumFailed)));\n}\n#[test]\nfn valid_13_digit_normalises() {\n    let n = normalise_mst(\"0312345678-001\").unwrap();\n    assert!(checksum::verify(&amp;n).is_ok());\n}\n#[test]\nfn invalid_length_rejected() {\n    assert!(matches!(normalise_mst(\"123\"), Err(MstError::InvalidLength(_))));\n}\n// ... 46 more test vectors (positive + negative, real-world MSTs from public registry)</code></pre>\n<pre><code class=\"language-rust\">// skills/vietnam-mst-validate/services/skill-broker/tests/integration.rs\n#[tokio::test]\nasync fn happy_path_against_mock_gdt() {\n    let mock = MockGdt::start_with(\"0312345678\", \"00\", \"CYBERSKILL JSC\").await;\n    let outcome = validate_mst(\"0312345678\", ValidateOptions::default()).await.unwrap();\n    assert!(outcome.valid);\n    assert_eq!(outcome.status, \"00\");\n    assert_eq!(outcome.business_name.as_deref(), Some(\"CYBERSKILL JSC\"));\n}\n\n#[tokio::test]\nasync fn cache_hit_skips_gdt() {\n    let mock = MockGdt::start_with(\"0312345678\", \"00\", \"X\").await;\n    let _ = validate_mst(\"0312345678\", ValidateOptions::default()).await.unwrap();\n    let second = validate_mst(\"0312345678\", ValidateOptions::default()).await.unwrap();\n    assert!(second.cache_hit);\n    assert_eq!(mock.call_count(), 1);\n}\n\n#[tokio::test]\nasync fn offline_returns_stale() {\n    let mock = MockGdt::start_with(\"0312345678\", \"00\", \"X\").await;\n    let _ = validate_mst(\"0312345678\", ValidateOptions::default()).await.unwrap();\n    std::env::set_var(\"CYBEROS_OFFLINE\", \"true\");\n    let stale = validate_mst(\"0312345678\", ValidateOptions::default()).await.unwrap();\n    assert!(stale.stale);\n    assert_eq!(stale.business_name.as_deref(), Some(\"X\"));\n}\n\n#[tokio::test]\nasync fn rate_limit_triggers_error() {\n    // Burn the governor budget\n    for _ in 0..100 { let _ = rate_limiter().check(); }\n    assert!(matches!(rate_limiter().check(), Err(_)));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-SKILL-103</strong> — frontmatter schema.</li><li><strong>FR-SKILL-104</strong> — capability broker enforces <code>allowed_tools: [MemoryEmit, HttpFetch]</code> + domain allowlist.</li><li><strong>FR-SKILL-105</strong> — memory-capture SDK (used internally for audit emit).</li><li><strong>FR-SKILL-109, 110</strong> — VAT invoice + bank transfer skills depend on validated MST.</li><li><strong>FR-MEMORY-111</strong> — PII detection; MST is a recognised PII tag (<code>VnMst</code>).</li><li><strong>FR-AI-016</strong> — residency pinning; GDT API call MUST originate from VN region (sg-1 fallback acceptable for slice-3).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>compliance.mst_validated</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"compliance.mst_validated\",\n  \"payload\": {\n    \"mst_redacted\":     \"03******78\",\n    \"valid\":            true,\n    \"status\":           \"00\",\n    \"status_text\":      \"đang hoạt động\",\n    \"business_name\":    \"CYBERSKILL JSC\",\n    \"validated_at_ns\":  1747407137483000000,\n    \"cache_hit\":        false,\n    \"stale\":            false,\n    \"gdt_round_trip_ms\": 247,\n    \"trace_id\":         \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>MST-to-business-info enrichment (full registered shareholders, legal representative) — slice 4+; GDT public API doesn't expose; need different data source.</li><li>Cross-reference with NBT (Ngân hàng Trung ương) for blacklist check — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Checksum failure</td><td>local check</td><td><code>ChecksumFailed</code>; no GDT call</td><td>Caller fixes typo</td></tr><tr><td>GDT unreachable</td><td>network err / 5xx</td><td>retry 3×; final <code>GdtUnreachable</code></td><td>Operator retries; uses offline cache</td></tr><tr><td>GDT returns inactive status</td><td><code>00</code>/<code>04</code> check fails</td><td><code>Inactive { status, text }</code></td><td>Caller surfaces to user; may stop transaction</td></tr><tr><td>Rate limit exceeded</td><td>governor.check Err</td><td><code>RateLimited</code></td><td>Caller backs off</td></tr><tr><td>Cache corruption</td><td>sled error</td><td>Treat as miss; fresh GDT call</td><td>Cache eventually recovers</td></tr><tr><td>Offline mode + no cache</td><td>env check + cache miss</td><td><code>GdtUnreachable</code></td><td>Operator brings network OR accepts no validation</td></tr><tr><td>MST not in GDT registry</td><td>response has <code>&lt;TrangThai&gt;05&lt;/TrangThai&gt;</code> (\"not found\")</td><td><code>Inactive { status: \"05\", ... }</code></td><td>Caller informs user</td></tr><tr><td>SOAP parse error</td><td>quick_xml Err</td><td><code>ParseError(detail)</code>; metric increments</td><td>Operator investigates GDT format change</td></tr><tr><td>GDT timeout (5s default)</td><td>reqwest timeout</td><td>retried as transient</td><td>None</td></tr><tr><td>Stale cache served (offline)</td><td>flag in outcome</td><td>Caller decides</td><td>None</td></tr><tr><td>Concurrent same-MST validations</td><td>lib is async-safe</td><td>Either races to populate cache; second call sees cache hit</td><td>By design</td></tr><tr><td>Multi-byte UTF-8 in business name</td><td>parse handles</td><td>Stored correctly</td><td>None</td></tr><tr><td>GDT IP-blocked us</td><td>403/blocked</td><td>Treated as transient; retry; final <code>GdtUnreachable</code></td><td>Operator contacts GDT</td></tr><tr><td>Cache file size grows</td><td>sled compaction; capped at 100 MB</td><td>Old entries evicted</td><td>None</td></tr><tr><td><code>force_refresh</code> racing with another caller</td><td>last writer wins</td><td>Slight inefficiency</td><td>None</td></tr><tr><td>MST normalisation strips letters</td><td><code>normalise_mst</code> filters non-digit/dash</td><td>Invalid input flagged as InvalidLength</td><td>Caller passes correct format</td></tr><tr><td>Audit row write fails</td><td>MemoryEmit Err</td><td>Validation still succeeds; audit lost; sev-2 alarm</td><td>Operator restores memory</td></tr><tr><td>OTel span buffered then dropped</td><td>exporter unavailable</td><td>Logged WARN</td><td>Operator restores collector</td></tr><tr><td>Tracing inadvertently logs raw MST</td><td><code>tracing::info!(mst, ...)</code> instead of redacted</td><td>PII leak</td><td>Author uses <code>redact(mst)</code> helper</td></tr><tr><td>Concurrent rate_limiter().check() race</td><td>governor handles</td><td>Either accept or reject; deterministic per call</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><code>quick-xml</code> (with serde feature) is the canonical SOAP parser; <code>xmlrs</code> was rejected for being heavier with no benefit on small responses.</li><li>The sled cache file lives at <code>~/.cyberos/cache/vn-mst.sled</code> (user-scope) or <code>/var/lib/cyberos/cache/vn-mst.sled</code> (system-scope); FR-MEMORY-110 sweeper doesn't touch it.</li><li><code>governor</code> rate limiter is process-local. Multiple cyberos instances on the same host could exceed global GDT rate; slice-3+ may add Redis-backed shared limit.</li><li>The <code>redact()</code> function is used everywhere except the audit row and the returned <code>ValidationOutcome.mst</code>; both intentional (audit needs the full thing; caller holds the original).</li><li>SOAP namespace <code>xmlns=\"http://tempuri.org/\"</code> is the actual GDT convention (legacy ASP.NET service); not a typo.</li><li>The 5s per-request timeout is empirical: 95th percentile GDT response is ~600ms; 5s is generous. Hard exp backoff handles longer tails.</li><li><code>valid_from</code> is parsed as ISO date but returned as String — different callers want different date types. Caller converts.</li><li>The 24h cache TTL covers both <code>valid</code> and <code>inactive</code> outcomes; caller checks <code>outcome.valid</code> before transacting.</li><li>Rate-limit recovery: governor restores tokens at 100/min linearly; after 1 minute of zero usage, full burst available.</li></ul>\n<hr>\n<p><em>End of FR-SKILL-108.</em></p>";
