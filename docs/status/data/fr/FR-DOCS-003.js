window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOCS-003"]="<h1>FR-DOCS-003: Release roadmap visualization</h1>\n<h2>§1 - Description</h2>\n<p>One generated page that answers \"where is the platform and what is coming\" from the data the repo already maintains: every FR's status, every version's changelog entries, and the current VERSION - refreshed automatically whenever a release is cut or the site deploys.</p>\n<p>Normative clauses:</p>\n<ol><li>A builder <code>tools/docs-site/render-roadmap.mjs</code> MUST generate <code>roadmap.html</code> into the site output dir from exactly three inputs: FR frontmatter across <code>docs/feature-requests/*/FR-*.md</code> (excluding <code>_audits/</code>, <code>_archive/</code>, <code>*.audit.md</code>), <code>CHANGELOG.md</code> version sections, and <code>VERSION</code>. It MUST use node stdlib only (no dependencies), matching the FR-DOCS-002 renderer constraint.</li><li>The page MUST contain four blocks: (a) a header stamp - VERSION + built-from commit; (b) a release timeline - one entry per CHANGELOG version section (version, date, entry lines), newest first; (c) a pipeline board - the 10 lifecycle statuses as columns in STATUS-REFERENCE order, each FR as a row item (id, title, module, class badge) with per-column counts; (d) module rollups - per module: counts by status and by class. Counts MUST be derived from frontmatter, never from BACKLOG.md.</li><li>Client-side filtering by module, class, and status MUST work with inline vanilla JS (no external assets, no CDN), degrading gracefully to the full unfiltered page when JS is off (content present in the DOM regardless of filter state).</li><li><code>tools/docs-site/build.sh</code> MUST invoke the roadmap builder on every site build, and BOTH <code>.github/workflows/deploy.yml</code> (docs job) and <code>.github/workflows/release.yml</code> MUST run the site build step so the published roadmap refreshes on every deploy AND every release tag - the release job publishing to the same docs target the deploy job uses.</li><li>Determinism and honesty per FR-DOCS-002: same inputs -&gt; byte-identical <code>roadmap.html</code> (the header stamp uses VERSION + commit, no wall-clock timestamp); an FR file whose frontmatter fails to parse MUST fail the build non-zero naming the file; a CHANGELOG that yields zero version sections MUST fail likewise (structure changed under the parser).</li><li>The generated site nav MUST include the roadmap page (via the FR-DOCS-002 filesystem-derived nav or an explicit nav hook, whichever that builder exposes).</li><li>The page MUST render acceptably in light and dark contexts using the site's existing chrome/tokens - no hardcoded colors outside the site's token set.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The roadmap is a VIEW, so it introduces no new records: FR frontmatter is already the declared source of truth, CHANGELOG already narrates releases, and VERSION already names \"now\". Rebuilding on deploy AND release satisfies the trigger requirement with the two hooks that already exist rather than a scheduler. Excluding wall-clock time from the stamp preserves FR-DOCS-002's byte-identical rebuild property, which is what makes \"did anything change?\" a diff instead of a guess.</p>\n<h2>§3 - Contract</h2>\n<pre><code>node tools/docs-site/render-roadmap.mjs &lt;repo-root&gt; &lt;out-dir&gt;\n  writes &lt;out-dir&gt;/roadmap.html\n  exit 0 ok | exit 1 unparseable FR frontmatter or empty CHANGELOG parse (file named on stderr)</code></pre>\n<p>Pipeline board column order = STATUS-REFERENCE §1 order: draft, ready_to_implement, implementing, ready_to_review, reviewing, ready_to_test, testing, done, on_hold, closed.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Three inputs only, stdlib only</strong> (§1 #1) - the builder reads nothing outside the three sources (strace-free proof: code review + a fixture tree where ONLY those inputs exist); <code>node -e \"require('tools/docs-site/render-roadmap.mjs')\"</code>-style import check shows no third-party requires.</li><li><strong>Four blocks render with true counts</strong> (§1 #2) - against a fixture FR tree (known status mix incl. improvement-class rows), the board's per-column counts equal the fixture's frontmatter tally, the timeline lists the fixture CHANGELOG versions newest-first, and the rollups match per module.</li><li><strong>Filters work and degrade</strong> (§1 #3) - with JS: selecting module+status hides non-matching rows (DOM assertions via node's parser on the emitted markup + the inline script's data attributes); without JS: all rows present.</li><li><strong>Wired into build, deploy, and release</strong> (§1 #4) - build.sh calls the builder; both workflow files contain the site-build step; the release workflow publishes to the same target (structural asserts on the three files).</li><li><strong>Deterministic</strong> (§1 #5) - two builds over the same fixture are byte-identical; adding one FR changes the output (sanity inverse).</li><li><strong>Honest failure</strong> (§1 #5) - a fixture FR with broken frontmatter fails the build naming the file; an empty CHANGELOG fixture fails.</li><li><strong>In the nav</strong> (§1 #6) - the generated site's nav includes the roadmap link (assert on the built fixture site).</li><li><strong>Token-clean styling</strong> (§1 #7) - the emitted HTML contains no hex colors outside the site token definitions (grep-level assert), so the page inherits the site theme.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/docs-site/tests/test_render_roadmap.sh\nt01_inputs_and_stdlib_only()     # AC 1\nt02_four_blocks_true_counts()    # AC 2\nt03_filters_and_nojs_degrade()   # AC 3\nt04_wired_build_deploy_release() # AC 4\nt05_byte_identical_rebuilds()    # AC 5\nt06_honest_failures()            # AC 6\nt07_nav_link_present()           # AC 7\nt08_token_clean_styles()         # AC 8</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>Frontmatter reader: reuse/port the minimal YAML-subset reader pattern from <code>scripts/migrate_improvement_to_fr.py</code> in ~60 lines of node (key: value scalars + flow lists suffice for the fields used). CHANGELOG parser: split on <code>^## </code> version headings. Emit: one template literal per block, data attributes (<code>data-module</code>, <code>data-status</code>, <code>data-class</code>) driving a ~30-line inline filter script.</p>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-DOCS-002 (site builder home, chrome/tokens, nav derivation - currently <code>reviewing</code>, so this FR queues right behind it). Related: FR-IMP-069's release job gives release.yml the natural place for the publish step; FR-DOCS-001's determinism rules are inherited via FR-DOCS-002.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ node tools/docs-site/render-roadmap.mjs . dist/website\nroadmap: 477 FRs (335 draft, 7 ready_to_implement, 14 implementing, 1 reviewing, 115 done, 1 on_hold), 12 releases, VERSION 1.7.0</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. A per-release \"which FRs shipped in vX.Y.Z\" join (matching FR ids inside changelog entries) renders when ids appear in the entry text and is omitted otherwise - a data-quality nudge, not a requirement, noted on the page.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>FR volume growth (477 -&gt; thousands) makes the page heavy - rows are plain elements with data attributes (~100 bytes each); at 5k FRs the page stays ~1 MB before gzip. Acceptable; a pagination follow-up is named in the page footer comment when count &gt; 2000.</li><li>Status vocabulary drift (a file with an invalid status like the fixed FR-EVAL-001 case) - unknown statuses render in an <code>invalid</code> bucket at the far right, loudly visible instead of dropped, and the build stderr lists them (warning, not failure - the FR audit pipeline owns enforcement).</li><li>CHANGELOG format evolution - parser requires <code>## X.Y.Z</code> heads; zero matches fails honestly per §1 #5.</li><li>Deploy and release racing (tag push + main push same minute) - both jobs regenerate from their own checkout; last writer wins with deterministic content from its commit; no shared mutable state.</li><li>Dark-mode regressions - token-only styling (AC 8) delegates theming to the site chrome; no page-local palette to drift.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the summary stdout line (§8) stable - it doubles as the deploy log's roadmap health line. The invalid-status bucket doubles as a live data-quality monitor for the whole FR corpus; keep it.</p>\n<p><strong>Post-ship amendment (2026-07-12, FR-DOCS-006):</strong> superseded by the status hub - render-roadmap.mjs retired; its parsers, assertions, and greppable summary conventions live on in render-status-hub.mjs; roadmap.html remains as a permanent redirect stub to status.html#roadmap (bookmarks preserved).</p>\n<p><em>End of FR-DOCS-003.</em></p>";
