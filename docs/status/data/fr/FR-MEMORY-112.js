window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-112"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A new memory kind <code>episode</code> is the <strong>agent's record of one completed task</strong>. It is structurally distinct from the four existing kinds (<code>decisions | facts | people | projects | preferences | drift | refinements</code>) which describe knowledge; an Episode describes an <em>event</em>. The Episode contract:</p>\n<ol><li><strong>MUST</strong> add <code>episode</code> to <code>memory.schema.json#/definitions/MemoryKind</code>. Files MUST live under <code>memories/episodes/&lt;hex&gt;/&lt;hex&gt;/&lt;slug&gt;.md</code> per the existing path convention; the writer enforces this layout via <code>memory.invariants.yaml#layout-kind-directory-match</code>.</li><li><strong>MUST</strong> validate against a per-kind frontmatter contract (<code>memory.schema.json#/definitions/EpisodeFrontmatter</code>) whose fields are:</li></ol>\n<table><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead><tbody><tr><td><code>task</code></td><td>string, ≥ 1 char after trim</td><td>yes</td><td>The task the agent was asked to do. Free-form.</td></tr><tr><td><code>approach</code></td><td>string, ≥ 1 char after trim</td><td>yes</td><td>One-line description of how the agent approached it.</td></tr><tr><td><code>outcome</code></td><td>enum `success \\</td><td>partial \\</td><td>failure`</td><td>yes</td><td>Closed enum (§1 #5).</td></tr><tr><td><code>duration_ms</code></td><td>integer ≥ 0</td><td>yes</td><td>Wall-clock duration.</td></tr><tr><td><code>token_cost</code></td><td>integer ≥ 0</td><td>optional</td><td>Input + output tokens combined; absent ≡ unknown.</td></tr><tr><td><code>quality_score</code></td><td>float <code>0.0..1.0</code></td><td>optional</td><td>Absent ≡ 0.5 (DEC-181).</td></tr><tr><td><code>notes</code></td><td>string</td><td>optional</td><td>Free-form observations.</td></tr><tr><td><code>error</code></td><td>string</td><td>optional, required iff <code>outcome != success</code></td><td>Concrete failure description.</td></tr></tbody></table>\n<ol><li><strong>MUST</strong> preserve the existing <code>MemoryFrontmatter</code> shape for non-episode kinds verbatim; the per-kind contract is <strong>additive</strong> — old memories continue to validate.</li><li><strong>MUST</strong> route every Episode write through <code>cyberos.core.writer.Writer</code> (the canonical writer). The new convenience function <code>cyberos.core.episode.log(store, episode)</code> constructs the body + frontmatter and then calls <code>Writer.put(...)</code>; it MUST NOT touch <code>audit/</code>, <code>HEAD</code>, or <code>.lock</code> directly (per AGENTS.md §14.1).</li><li><strong>MUST</strong> reject Episode rows whose <code>outcome</code> value is outside the closed enum. The walker's <code>episode-outcome-closed-enum</code> invariant runs on every <code>cyberos doctor</code>. Severity: error.</li><li><strong>MUST</strong> reject Episode rows whose <code>quality_score</code> is outside <code>[0.0, 1.0]</code> or <code>duration_ms</code> is negative. Same invariant pattern. Severity: error.</li><li><strong>MUST</strong> treat the searchable document for an Episode as <code>f\"Task: {task}\\nApproach: {approach}\\nOutcome: {outcome}\\nNotes: {notes}\"</code> (mirrors the article's Episode dataclass §3). This is what the FTS5 index sees and what sentence-transformers embeds.</li><li><strong>MUST</strong> add a new CLI subcommand <code>cyberos episode log</code> that wraps <code>cyberos.core.episode.log(...)</code>: ``<code> cyberos episode log \\ --task \"Audit MEMORY module for self-learning gaps\" \\ --approach \"MHTML + ffmpeg/whisper extraction; cross-ref vs AGENTS.md\" \\ --outcome success \\ --duration-ms 1847000 \\ --token-cost 145000 \\ --quality-score 0.92 </code>``</li><li><strong>MUST</strong> add a new CLI subcommand <code>cyberos recall-similar &lt;task-string&gt;</code> that:</li></ol>\n<ul><li>Runs semantic search filtered to <code>kind: episode</code> (semantic backend if <code>sentence-transformers</code> is installed; FTS5 fallback otherwise — same soft-dep pattern as FR-MEMORY-108).</li><li>Ranks results by the <strong>combined score</strong> <code>relevance · 0.4 + quality_score · 0.3 + recency · 0.3</code>. For FR-MEMORY-112's scope, <code>recency</code> is a placeholder mock that returns <code>1.0</code> (no decay); FR-MEMORY-113 plugs in the proper Park-et-al decay function.</li><li>Defaults: <code>--k 3</code>, <code>--min-relevance 0.65</code>.</li><li>Output: JSON list of <code>{path, task, approach, outcome, quality_score, relevance, combined_score, last_seen_at}</code> ordered by <code>combined_score</code> descending.</li></ul>\n<ol><li><strong>MUST</strong> treat absent <code>quality_score</code> as exactly <code>0.5</code> in the combined-score ranking (the \"no opinion\" midpoint, per DEC-181). Never silently boost or penalise.</li><li><strong>MUST</strong> preserve current <code>cyberos recall</code> (full-text) and <code>cyberos search</code> (semantic) semantics. The new <code>recall-similar</code> is a strictly additive subcommand; existing call sites in <code>cyberos.core.semantic.recall(...)</code> continue to work without the <code>memory_kind=</code> kwarg (defaults to <code>None</code> ≡ \"all kinds\").</li><li><strong>MUST</strong> emit one audit row per <code>episode log</code> invocation of kind <code>episode.logged</code> with payload <code>{path, outcome, duration_ms, token_cost, quality_score}</code>. The writer already emits the standard <code>put</code> row; the <code>episode.logged</code> row is a <strong>lightweight projection</strong> for FR-MEMORY-115 to consume without re-parsing every <code>put</code> payload.</li><li><strong>MUST</strong> ship a labelled fixture corpus (<code>tests/fixtures/episode_corpus.jsonl</code>, ≥ 40 rows) covering: 10 success + 10 partial + 10 failure + 10 edge cases (zero duration, quality 0.0 / 1.0, missing optional fields, multi-line task strings). CI gates on the fixture parsing cleanly through the schema.</li><li><strong>MUST</strong> complete <code>cyberos recall-similar</code> in ≤ 500 ms p95 on a memory with 10,000 Episodes (semantic backend) or ≤ 150 ms p95 (FTS5 fallback). Both budgets measured on commodity hardware against the fixture corpus expanded to 10K rows.</li><li><strong>SHOULD</strong> support <code>cyberos episode log --from-json &lt;file&gt;</code> for batch-loading Episodes from external systems (e.g. importing historical run data from a CI export). Slice-3 stretch; placeholder noted for FR-MEMORY-115 forward-compatibility.</li><li><strong>SHOULD</strong> surface \"no similar episodes found\" as a structured response rather than silent emptiness, so an agent calling <code>recall-similar</code> programmatically can branch: <code>{matches: [], reason: \"no_episodes_above_min_relevance\"}</code> (or <code>no_episodes_in_store</code> if the store has zero Episodes).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a new kind instead of overloading <code>facts</code> (§1 #1)?</strong> Facts describe what is true (durable state); Episodes describe what happened (events). Overloading would force the per-kind validator to special-case <code>meta.outcome</code> on <code>kind: facts</code>, which is exactly the kind of schema drift <code>memory.schema.json</code> is supposed to prevent. A new kind is cheap (additive enum value) and structurally correct.</p>\n<p><strong>Why a closed outcome enum (§1 #5)?</strong> The article (Ramakrushna §3) defines outcome as <code>success | partial | failure</code>. Open-text outcomes turn dashboards into NLP problems: counting \"partial\" vs \"partial success\" vs \"kinda worked\" vs \"almost-success\". Closed enum keeps aggregation arithmetic, not heuristic. Anything ambiguous gets rejected at write time so the agent has to pick a side.</p>\n<p><strong>Why default missing <code>quality_score</code> to 0.5 (§1 #10, DEC-181)?</strong> Three options were considered: (a) default to 1.0 (\"optimistic, treat unscored as good\"); (b) default to 0.0 (\"pessimistic, treat unscored as bad\"); (c) default to 0.5 (\"no opinion\"). The article's combined-score formula <code>relevance · 0.4 + importance · 0.3 + recency · 0.3</code> is sensitive to score weight; defaulting to either extreme would silently up-rank or down-rank unscored Episodes against scored ones. 0.5 is the only neutral value. Documented in the §3 type contract so downstream FRs (especially FR-MEMORY-113's recall ranking) don't surprise the operator.</p>\n<p><strong>Why ship <code>recall-similar</code> separately from <code>recall</code> (§1 #11)?</strong> Two reasons. First, <code>recall</code> is general (any <code>kind</code>) — adding kind-specific logic to it would push complexity into a path most callers don't need. Second, <code>recall-similar</code> is the load-bearing primitive for the reflection loop (Ramakrushna's §\"Reflection loop\"); separating it keeps the call site readable: \"give me episodes similar to this current task\" is one mental operation, \"search the store\" is another.</p>\n<p><strong>Why include <code>notes</code> in the searchable document (§1 #7)?</strong> The article's reference implementation joins <code>Task / Approach / Outcome / Notes</code> into the embedded text precisely because notes contain the \"what would I do differently next time?\" signal that makes episodes useful. Excluding notes would mean an episode whose task was identical to a current one but whose lesson is in the notes never surfaces.</p>\n<p><strong>Why a separate <code>episode.logged</code> audit row (§1 #12)?</strong> FR-MEMORY-115 (<code>cyberos dream</code>) consumes episodes when looking for cross-session patterns. The <code>put</code> row's payload contains the entire body, which means dream has to re-parse markdown frontmatter on every row scan — expensive. The <code>episode.logged</code> row is a lightweight projection that exposes outcome / duration / quality_score directly, so dream's pattern detector reads compact data. Cost: one extra audit row per episode log. Benefit: ~50× faster dream-time pattern scanning over 10K+ episode stores.</p>\n<p><strong>Why ≤ 500 ms p95 semantic / ≤ 150 ms FTS (§1 #14)?</strong> Agents calling <code>recall-similar</code> synchronously inside their task loop need this to feel cheap. 500 ms is the same ceiling FR-MEMORY-108 sets for <code>cyberos search --semantic</code>; piggybacking the budget keeps memory-side latency consistent. 150 ms FTS is a 3× margin — FTS5 is dramatically faster than vector for filtered queries.</p>\n<p><strong>Why an additive enum extension rather than an §0.2 protocol amendment?</strong> AGENTS.md §5.2 says: <em>\"The schema's <code>kind</code> field is closed; unknown values MUST be rejected.\"</em> Adding a new kind value extends the closed enum — that is exactly the schema-evolution path §5.2 names. It is not a behaviour change to the protocol; the protocol's invariant (\"kind is closed\") is preserved. Distinguished from FR-MEMORY-115 / 117 / 118 / 119 which DO require §0.2 chat-turns because they change writer behaviour, add new ops, or introduce new normative sections.</p>\n<p><strong>Why 12 hours of effort, not the 16 hours of FR-MEMORY-107?</strong> The schema change is mechanical (one enum value + one new struct). The CLI surface is two subcommands. The tests are mostly fixture-driven. No new daemon, no new threading model, no new network surface. The cost is in (a) the per-kind validator pattern (§1 #2) which establishes infrastructure FR-MEMORY-114 will reuse, and (b) the fixture corpus (§1 #13).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3><code>EpisodeFrontmatter</code> schema (memory.schema.json fragment)</h3>\n<pre><code class=\"language-json\">{\n  \"$defs\": {\n    \"MemoryKind\": {\n      \"type\": \"string\",\n      \"enum\": [\"decisions\", \"facts\", \"people\", \"projects\", \"preferences\", \"drift\", \"refinements\", \"episode\"]\n    },\n    \"EpisodeOutcome\": {\n      \"type\": \"string\",\n      \"enum\": [\"success\", \"partial\", \"failure\"]\n    },\n    \"EpisodeFrontmatter\": {\n      \"type\": \"object\",\n      \"required\": [\"kind\", \"task\", \"approach\", \"outcome\", \"duration_ms\"],\n      \"properties\": {\n        \"kind\":          {\"const\": \"episode\"},\n        \"task\":          {\"type\": \"string\", \"minLength\": 1},\n        \"approach\":      {\"type\": \"string\", \"minLength\": 1},\n        \"outcome\":       {\"$ref\": \"#/$defs/EpisodeOutcome\"},\n        \"duration_ms\":   {\"type\": \"integer\", \"minimum\": 0},\n        \"token_cost\":    {\"type\": \"integer\", \"minimum\": 0},\n        \"quality_score\": {\"type\": \"number\",  \"minimum\": 0.0, \"maximum\": 1.0},\n        \"notes\":         {\"type\": \"string\"},\n        \"error\":         {\"type\": \"string\"}\n      },\n      \"allOf\": [\n        {\n          \"if\":   {\"properties\": {\"outcome\": {\"const\": \"success\"}}},\n          \"then\": true,\n          \"else\": {\"required\": [\"error\"]}\n        }\n      ]\n    }\n  }\n}</code></pre>\n<h3><code>cyberos.core.episode</code> (Python)</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/episode.py\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Literal, Optional\nimport hashlib, json, uuid\n\nfrom cyberos.core.writer import Writer\n\nEpisodeOutcome = Literal[\"success\", \"partial\", \"failure\"]\n\n\n@dataclass\nclass Episode:\n    task:         str\n    approach:     str\n    outcome:      EpisodeOutcome\n    duration_ms:  int\n    token_cost:   Optional[int]   = None\n    quality_score: Optional[float] = None\n    notes:        str             = \"\"\n    error:        Optional[str]   = None\n\n    def __post_init__(self) -&gt; None:\n        if not self.task.strip():\n            raise ValueError(\"Episode.task must be non-empty after trim\")\n        if not self.approach.strip():\n            raise ValueError(\"Episode.approach must be non-empty after trim\")\n        if self.outcome not in (\"success\", \"partial\", \"failure\"):\n            raise ValueError(f\"Episode.outcome {self.outcome!r} not in closed enum\")\n        if self.duration_ms &lt; 0:\n            raise ValueError(\"Episode.duration_ms must be ≥ 0\")\n        if self.quality_score is not None and not (0.0 &lt;= self.quality_score &lt;= 1.0):\n            raise ValueError(\"Episode.quality_score must be in [0.0, 1.0]\")\n        if self.outcome != \"success\" and not (self.error or \"\").strip():\n            raise ValueError(f\"Episode.outcome={self.outcome!r} requires non-empty error\")\n\n    def searchable_document(self) -&gt; str:\n        \"\"\"The text the FTS / sentence-transformers index sees.\"\"\"\n        return (\n            f\"Task: {self.task}\\n\"\n            f\"Approach: {self.approach}\\n\"\n            f\"Outcome: {self.outcome}\\n\"\n            f\"Notes: {self.notes}\"\n        )\n\n    def frontmatter(self) -&gt; dict:\n        d: dict = {\n            \"kind\":        \"episode\",\n            \"task\":        self.task,\n            \"approach\":    self.approach,\n            \"outcome\":     self.outcome,\n            \"duration_ms\": self.duration_ms,\n        }\n        if self.token_cost is not None:    d[\"token_cost\"]    = self.token_cost\n        if self.quality_score is not None: d[\"quality_score\"] = self.quality_score\n        if self.notes:                     d[\"notes\"]         = self.notes\n        if self.error:                     d[\"error\"]         = self.error\n        return d\n\n\ndef log(writer: Writer, episode: Episode, actor: str = \"agent\") -&gt; Path:\n    \"\"\"Append an Episode to the memory. Returns the relative path under `&lt;memory-root&gt;/`.\"\"\"\n    short = hashlib.sha256(episode.task.encode(\"utf-8\")).hexdigest()\n    slug  = f\"{short[:8]}-{uuid.uuid4().hex[:6]}.md\"\n    path  = Path(\"memories\") / \"episodes\" / short[:2] / short[2:4] / slug\n\n    body  = \"---\\n\" + json.dumps(episode.frontmatter(), indent=2, sort_keys=True) + \"\\n---\\n\\n\" + episode.searchable_document() + \"\\n\"\n\n    # `put` emits the standard audit row; `episode.logged` is an additional lightweight projection\n    writer.put(str(path), body, actor=actor)\n    writer.emit_aux(\n        kind=\"episode.logged\",\n        payload={\n            \"path\":          str(path),\n            \"outcome\":       episode.outcome,\n            \"duration_ms\":   episode.duration_ms,\n            \"token_cost\":    episode.token_cost,\n            \"quality_score\": episode.quality_score if episode.quality_score is not None else 0.5,\n            \"logged_at\":     datetime.now(timezone.utc).isoformat(),\n        },\n        actor=actor,\n    )\n    return path</code></pre>\n<h3><code>cyberos.core.semantic.recall(..., memory_kind=)</code> extension</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/semantic.py — extension diff (additive)\n\ndef recall(\n    store_path: Path,\n    query:      str,\n    k:          int   = 5,\n    min_relevance: float = 0.65,\n    memory_kind:   Optional[str] = None,       # ← NEW; None ≡ all kinds (back-compat)\n) -&gt; list[RecallHit]:\n    ...\n    # filter step (added)\n    if memory_kind is not None:\n        hits = [h for h in hits if h.frontmatter.get(\"kind\") == memory_kind]\n    ...</code></pre>\n<h3><code>cyberos recall-similar</code> CLI</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/cli/recall.py\nimport argparse, json, time\nfrom pathlib import Path\nfrom cyberos.core.semantic import recall as semantic_recall\nfrom cyberos.core.semantic import available as semantic_available\nfrom cyberos.core.reader   import recall as fts_recall  # FTS5 fallback\n\n\ndef add_args(sub: argparse.ArgumentParser) -&gt; None:\n    sub.add_argument(\"task\", help=\"Task description to find similar episodes for\")\n    sub.add_argument(\"--k\",   type=int,   default=3)\n    sub.add_argument(\"--min-relevance\", type=float, default=0.65)\n    sub.add_argument(\"--json\", action=\"store_true\", help=\"Emit JSON instead of human-formatted output\")\n\n\ndef combined_score(relevance: float, quality_score: Optional[float], recency: float) -&gt; float:\n    qs = quality_score if quality_score is not None else 0.5     # DEC-181\n    return relevance * 0.4 + qs * 0.3 + recency * 0.3\n\n\ndef run(store: Path, args: argparse.Namespace) -&gt; dict:\n    backend = \"semantic\" if semantic_available() else \"fts5\"\n    hits = (semantic_recall if backend == \"semantic\" else fts_recall)(\n        store_path=store,\n        query=args.task,\n        k=args.k * 4,                                            # over-fetch then filter\n        min_relevance=args.min_relevance,\n        memory_kind=\"episode\",\n    )\n    enriched = []\n    for h in hits:\n        fm = h.frontmatter\n        enriched.append({\n            \"path\":           h.path,\n            \"task\":           fm[\"task\"],\n            \"approach\":       fm[\"approach\"],\n            \"outcome\":        fm[\"outcome\"],\n            \"quality_score\":  fm.get(\"quality_score\"),\n            \"relevance\":      round(h.relevance, 3),\n            \"recency\":        1.0,                               # FR-MEMORY-113 plugs in the real decay\n            \"combined_score\": round(combined_score(h.relevance, fm.get(\"quality_score\"), 1.0), 3),\n            \"last_seen_at\":   h.last_seen_at,\n        })\n    enriched.sort(key=lambda x: x[\"combined_score\"], reverse=True)\n    enriched = enriched[:args.k]\n    return {\n        \"backend\": backend,\n        \"matches\": enriched,\n        \"reason\":  None if enriched else (\n            \"no_episodes_in_store\"        if backend == \"semantic\" and not hits else\n            \"no_episodes_above_min_relevance\"\n        ),\n    }</code></pre>\n<h3><code>cyberos episode log</code> CLI</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/cli/episode.py\nimport argparse\nfrom cyberos.core.episode import Episode, log as episode_log\nfrom cyberos.core.writer  import Writer\n\n\ndef add_args(sub: argparse.ArgumentParser) -&gt; None:\n    sub.add_argument(\"--task\",          required=True)\n    sub.add_argument(\"--approach\",      required=True)\n    sub.add_argument(\"--outcome\",       required=True, choices=[\"success\", \"partial\", \"failure\"])\n    sub.add_argument(\"--duration-ms\",   required=True, type=int)\n    sub.add_argument(\"--token-cost\",    type=int)\n    sub.add_argument(\"--quality-score\", type=float)\n    sub.add_argument(\"--notes\",         default=\"\")\n    sub.add_argument(\"--error\",         default=None)\n\n\ndef run(writer: Writer, args: argparse.Namespace, actor: str) -&gt; str:\n    ep = Episode(\n        task=args.task, approach=args.approach, outcome=args.outcome,\n        duration_ms=args.duration_ms, token_cost=args.token_cost,\n        quality_score=args.quality_score, notes=args.notes, error=args.error,\n    )\n    return str(episode_log(writer, ep, actor=actor))</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Schema accepts new kind</strong> — <code>episode</code> is a valid <code>MemoryKind</code>; <code>jsonschema.validate(meta, MemoryKind)</code> passes for <code>episode</code> and continues to pass for every existing kind. <em>(traces_to: §1 #1)</em></li><li><strong>Episode frontmatter required fields</strong> — a memory file with <code>kind: episode</code> and any of <code>{task, approach, outcome, duration_ms}</code> missing → walker fails with rule id <code>episode-frontmatter-required-field</code>. <em>(traces_to: §1 #2)</em></li><li><strong>Existing memory kinds unchanged</strong> — every existing memory file under <code>memories/{decisions,facts,...}/</code> continues to validate against <code>MemoryFrontmatter</code> exactly as before the schema migration. Regression suite <code>modules/memory/tests/core/test_frontmatter.py</code> passes unchanged. <em>(traces_to: §1 #3)</em></li><li><strong>Writer routes Episode writes</strong> — <code>cyberos.core.episode.log(writer, ep)</code> results in one <code>put</code> audit row + one <code>episode.logged</code> audit row; HEAD advances by exactly 2 seq positions. No direct file write to <code>audit/</code>, <code>HEAD</code>, or <code>.lock</code>. <em>(traces_to: §1 #4, §1 #12)</em></li><li><strong>Outcome enum closed</strong> — <code>Episode(outcome=\"success\")</code> accepted; <code>Episode(outcome=\"kinda-worked\")</code> raises <code>ValueError</code>. Walker rejects file-level violations as error severity. <em>(traces_to: §1 #5)</em></li><li><strong>Quality_score range enforced</strong> — <code>quality_score=1.0</code> accepted; <code>quality_score=1.1</code> rejected at construction; <code>quality_score=-0.0001</code> rejected. Same checks at walker time. <em>(traces_to: §1 #6)</em></li><li><strong>Duration non-negative</strong> — <code>duration_ms=0</code> accepted; <code>duration_ms=-1</code> rejected at construction and at walker. <em>(traces_to: §1 #6)</em></li><li><strong>Error required on non-success</strong> — <code>Episode(outcome=\"failure\", error=\"\")</code> rejected at construction; <code>Episode(outcome=\"failure\", error=\"timeout in dispatcher\")</code> accepted. <em>(traces_to: §1 #2)</em></li><li><strong>Searchable document shape</strong> — <code>Episode(...).searchable_document()</code> matches the exact format <code>Task: X\\nApproach: Y\\nOutcome: Z\\nNotes: N</code> (newlines literal). <em>(traces_to: §1 #7)</em></li><li><strong><code>cyberos episode log</code> CLI</strong> — happy-path invocation returns a memory path under <code>memories/episodes/&lt;hex&gt;/&lt;hex&gt;/&lt;slug&gt;.md</code>; <code>cyberos read &lt;path&gt;</code> round-trips frontmatter exactly. <em>(traces_to: §1 #8)</em></li><li><strong><code>cyberos recall-similar</code> filters to episode kind</strong> — after writing 5 episodes + 5 facts under similar text, <code>cyberos recall-similar \"...\" --k 5</code> returns only Episodes (zero facts). <em>(traces_to: §1 #9, §1 #11)</em></li><li><strong>Combined-score ordering</strong> — two Episodes with relevance 0.80 each but quality_score 0.9 vs 0.4 → the 0.9 one ranks first. <em>(traces_to: §1 #9)</em></li><li><strong>Default quality_score = 0.5 for ranking</strong> — Episode without <code>quality_score</code> ranks identically to one with <code>quality_score=0.5</code>, lower than 0.9, higher than 0.4. <em>(traces_to: §1 #10, DEC-181)</em></li><li><strong>Min-relevance cutoff</strong> — three Episodes with relevances 0.80 / 0.70 / 0.60, <code>--min-relevance 0.65</code> → top-K returns 2 (the 0.60 is filtered out). <em>(traces_to: §1 #9)</em></li><li><strong>Top-K cap</strong> — 10 Episodes match, <code>--k 3</code> → exactly 3 returned. <em>(traces_to: §1 #9)</em></li><li><strong>FTS5 fallback when sentence-transformers absent</strong> — when <code>cyberos.core.semantic.available()</code> returns False, <code>cyberos recall-similar</code> runs the FTS5 path; response <code>backend: \"fts5\"</code>. <em>(traces_to: §1 #9, DEC-183)</em></li><li><strong><code>recall</code> back-compat</strong> — every existing call site to <code>cyberos.core.semantic.recall(...)</code> that does not pass <code>memory_kind=</code> continues to return the same set of hits it did before the migration. Regression covered by <code>modules/memory/tests/core/test_dream.py</code>. <em>(traces_to: §1 #11)</em></li><li><strong><code>episode.logged</code> projection row shape</strong> — the aux audit row's payload matches <code>{path, outcome, duration_ms, token_cost, quality_score, logged_at}</code>; <code>quality_score</code> is <code>0.5</code> literal when the source Episode had it absent. <em>(traces_to: §1 #12, DEC-181)</em></li><li><strong>Fixture corpus parses</strong> — <code>tests/fixtures/episode_corpus.jsonl</code> has ≥ 40 rows; every row constructs a valid <code>Episode</code> and round-trips through <code>frontmatter()</code> → schema-validate → reconstruct without loss. <em>(traces_to: §1 #13)</em></li><li><strong>Recall-similar latency p95</strong> — 10K-Episode store, 100 trial runs of <code>cyberos recall-similar</code> → p95 ≤ 500 ms (semantic) / ≤ 150 ms (FTS). <em>(traces_to: §1 #14)</em></li><li><strong>No similar found → structured response</strong> — empty store → <code>{matches: [], reason: \"no_episodes_in_store\"}</code>; non-empty but all below cutoff → <code>{matches: [], reason: \"no_episodes_above_min_relevance\"}</code>. <em>(traces_to: §1 #16)</em></li><li><strong>Invariant catches walker violations</strong> — manually inject an Episode file with <code>outcome: \"kinda-worked\"</code> via raw write → <code>cyberos doctor</code> reports invariant <code>episode-outcome-closed-enum</code> failed; exit code non-zero. <em>(traces_to: §1 #5, §1 #6)</em></li><li><strong>CHANGELOG entry exists</strong> — <code>modules/memory/CHANGELOG.md</code> has a new dated section for FR-MEMORY-112 referencing <code>kind: episode</code> and the two new CLIs. <em>(traces_to: §1 #2, §1 #8, §1 #9)</em></li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_episode.py\nimport json\nfrom pathlib import Path\n\nimport pytest\nfrom cyberos.core.episode  import Episode, log as episode_log\nfrom cyberos.core.semantic import recall as semantic_recall, available as semantic_available\nfrom cyberos.core.writer   import Writer\n\n\ndef test_episode_log_writes_put_and_aux_rows(tmp_memory: Writer):\n    \"\"\"AC #4 + #18 — both `put` and `episode.logged` rows emitted.\"\"\"\n    seq_before = tmp_memory.head_seq()\n    ep = Episode(task=\"ship FR\", approach=\"audit-revise loop\", outcome=\"success\",\n                 duration_ms=1800_000, quality_score=0.92)\n    path = episode_log(tmp_memory, ep, actor=\"stephen\")\n    assert tmp_memory.head_seq() == seq_before + 2\n    aux = tmp_memory.read_audit_row(seq_before + 2)\n    assert aux[\"kind\"] == \"episode.logged\"\n    assert aux[\"payload\"][\"outcome\"] == \"success\"\n    assert aux[\"payload\"][\"quality_score\"] == 0.92\n\n\ndef test_episode_log_defaults_quality_score_to_half_in_aux(tmp_memory: Writer):\n    \"\"\"AC #18 + DEC-181 — absent quality_score surfaces as literal 0.5 in aux row.\"\"\"\n    ep = Episode(task=\"t\", approach=\"a\", outcome=\"success\", duration_ms=10)\n    seq_before = tmp_memory.head_seq()\n    episode_log(tmp_memory, ep)\n    aux = tmp_memory.read_audit_row(seq_before + 2)\n    assert aux[\"payload\"][\"quality_score\"] == 0.5\n\n\n@pytest.mark.parametrize(\"outcome,error,ok\", [\n    (\"success\",  None,                  True),\n    (\"partial\",  \"dispatcher returned 502 once before recovering\", True),\n    (\"failure\",  \"tokens exhausted\",    True),\n    (\"failure\",  None,                  False),    # AC #8\n    (\"failure\",  \"\",                    False),\n    (\"kinda\",    None,                  False),    # AC #5\n])\ndef test_outcome_enum_and_error_requirement(outcome, error, ok):\n    \"\"\"AC #5 + #8 — closed enum + error required on non-success.\"\"\"\n    if ok:\n        Episode(task=\"t\", approach=\"a\", outcome=outcome, duration_ms=1, error=error)\n    else:\n        with pytest.raises(ValueError):\n            Episode(task=\"t\", approach=\"a\", outcome=outcome, duration_ms=1, error=error)\n\n\n@pytest.mark.parametrize(\"score,ok\", [\n    (0.0, True), (1.0, True), (0.5, True), (None, True),\n    (-0.0001, False), (1.0001, False),                   # AC #6\n])\ndef test_quality_score_range(score, ok):\n    if ok:\n        Episode(task=\"t\", approach=\"a\", outcome=\"success\", duration_ms=1, quality_score=score)\n    else:\n        with pytest.raises(ValueError):\n            Episode(task=\"t\", approach=\"a\", outcome=\"success\", duration_ms=1, quality_score=score)\n\n\ndef test_searchable_document_shape():\n    \"\"\"AC #9 — exact format.\"\"\"\n    ep = Episode(task=\"X\", approach=\"Y\", outcome=\"success\", duration_ms=10, notes=\"N\")\n    assert ep.searchable_document() == \"Task: X\\nApproach: Y\\nOutcome: success\\nNotes: N\"\n\n\ndef test_recall_similar_filters_to_episode_kind(seeded_memory):\n    \"\"\"AC #11 — recall-similar returns only episodes, even when facts share similar text.\"\"\"\n    hits = semantic_recall(seeded_memory.path, \"ship a feature request\",\n                           k=10, min_relevance=0.0, memory_kind=\"episode\")\n    assert all(h.frontmatter[\"kind\"] == \"episode\" for h in hits)\n\n\ndef test_recall_similar_combined_score_order(seeded_memory):\n    \"\"\"AC #12 — higher quality_score wins on tie-equivalent relevance.\"\"\"\n    from cyberos.cli.recall import run as recall_run\n    out = recall_run(seeded_memory.path, _ns(task=\"ship\", k=5, min_relevance=0.0))\n    qs = [m[\"quality_score\"] for m in out[\"matches\"]]\n    # Among first two hits, the higher quality_score must come first\n    assert qs[0] is None or qs[0] &gt;= (qs[1] if qs[1] is not None else 0.5)\n\n\ndef test_recall_similar_missing_quality_acts_as_half(seeded_memory):\n    \"\"\"AC #13 + DEC-181 — absent quality_score ranks like 0.5.\"\"\"\n    from cyberos.cli.recall import combined_score\n    s_absent = combined_score(0.8, None, 1.0)\n    s_half   = combined_score(0.8, 0.5,  1.0)\n    assert s_absent == pytest.approx(s_half)\n\n\ndef test_recall_similar_top_k_cap(seeded_memory):\n    \"\"\"AC #15 — --k 3 returns ≤ 3 hits.\"\"\"\n    from cyberos.cli.recall import run as recall_run\n    out = recall_run(seeded_memory.path, _ns(task=\"ship\", k=3, min_relevance=0.0))\n    assert len(out[\"matches\"]) &lt;= 3\n\n\ndef test_recall_similar_fts_fallback(monkeypatch, seeded_memory):\n    \"\"\"AC #16 — sentence-transformers absent → fts5 backend.\"\"\"\n    monkeypatch.setattr(\"cyberos.core.semantic.available\", lambda: False)\n    from cyberos.cli.recall import run as recall_run\n    out = recall_run(seeded_memory.path, _ns(task=\"ship\", k=3, min_relevance=0.0))\n    assert out[\"backend\"] == \"fts5\"\n\n\ndef test_no_episodes_structured_response(empty_memory):\n    \"\"\"AC #21 — empty store → reason: no_episodes_in_store.\"\"\"\n    from cyberos.cli.recall import run as recall_run\n    out = recall_run(empty_memory.path, _ns(task=\"x\", k=3, min_relevance=0.65))\n    assert out[\"matches\"] == []\n    assert out[\"reason\"] == \"no_episodes_in_store\"\n\n\ndef test_fixture_corpus_parses():\n    \"\"\"AC #19 — every fixture row constructs a valid Episode.\"\"\"\n    rows = [json.loads(line) for line in Path(\"tests/fixtures/episode_corpus.jsonl\").read_text().splitlines() if line.strip()]\n    assert len(rows) &gt;= 40\n    for r in rows:\n        Episode(**r)</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_episode.py\nimport jsonschema, json\nfrom pathlib import Path\n\nSCHEMA = json.loads(Path(\"modules/memory/memory.schema.json\").read_text())\n\n\ndef test_episode_kind_in_enum():\n    \"\"\"AC #1 — `episode` is a valid MemoryKind.\"\"\"\n    jsonschema.validate(\"episode\", SCHEMA[\"$defs\"][\"MemoryKind\"])\n\n\ndef test_legacy_kinds_still_valid():\n    \"\"\"AC #3 — every pre-existing kind continues to validate.\"\"\"\n    for kind in (\"decisions\", \"facts\", \"people\", \"projects\", \"preferences\", \"drift\", \"refinements\"):\n        jsonschema.validate(kind, SCHEMA[\"$defs\"][\"MemoryKind\"])\n\n\ndef test_episode_frontmatter_required_fields():\n    \"\"\"AC #2 — missing one of {task, approach, outcome, duration_ms} fails validation.\"\"\"\n    base = {\"kind\": \"episode\", \"task\": \"t\", \"approach\": \"a\", \"outcome\": \"success\", \"duration_ms\": 1}\n    for missing in (\"task\", \"approach\", \"outcome\", \"duration_ms\"):\n        bad = {k: v for k, v in base.items() if k != missing}\n        with pytest.raises(jsonschema.ValidationError):\n            jsonschema.validate(bad, SCHEMA[\"$defs\"][\"EpisodeFrontmatter\"])\n\n\ndef test_walker_rejects_outcome_outside_enum(broken_memory):\n    \"\"\"AC #22 — handcrafted outcome=`kinda` in a written file → doctor non-zero.\"\"\"\n    rc = run_subprocess([\"cyberos\", \"--store\", str(broken_memory), \"doctor\"])\n    assert rc != 0\n    assert \"episode-outcome-closed-enum\" in last_stderr()</code></pre>\n<h3>Fixture corpus (excerpt)</h3>\n<pre><code class=\"language-jsonl\">// modules/memory/tests/fixtures/episode_corpus.jsonl  (40+ rows)\n{\"task\": \"Ship FR-AUTH-003 RLS enforcement\", \"approach\": \"single-commit RLS GUC; per-tenant policies\", \"outcome\": \"success\", \"duration_ms\": 1800000, \"token_cost\": 120000, \"quality_score\": 0.9, \"notes\": \"8/9 gaps closed\"}\n{\"task\": \"Migrate Slack workspace to Mattermost\", \"approach\": \"Slack import API + decommission gate ≥ 0.95\", \"outcome\": \"partial\", \"duration_ms\": 5400000, \"token_cost\": 88000, \"quality_score\": 0.7, \"notes\": \"decommission gate at 0.91; held back for round 2\", \"error\": \"decommission signal below 0.95 threshold\"}\n{\"task\": \"Reduce capture daemon p95\", \"approach\": \"rate-limit + debounce\", \"outcome\": \"failure\", \"duration_ms\": 9_000_000, \"token_cost\": 200_000, \"quality_score\": 0.2, \"notes\": \"p95 went UP because debounce ate priority events\", \"error\": \"wrong invariant\"}\n{\"task\": \"Author FR with §1↔§4↔§5 traceability\", \"approach\": \"audit-revise loop\", \"outcome\": \"success\", \"duration_ms\": 1500000}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>API contracts above are the skeleton. Implementation order:</p>\n<ol><li>Schema (memory.schema.json) — additive enum + EpisodeFrontmatter.</li><li>Walker invariants (memory.invariants.yaml).</li><li><code>cyberos/core/episode.py</code> (Episode dataclass + <code>log()</code>).</li><li><code>cyberos/core/semantic.py</code> — add <code>memory_kind=</code> kwarg with default None (back-compat).</li><li><code>cyberos/cli/episode.py</code> + <code>cyberos/cli/recall.py</code>.</li><li>Wire subcommands into <code>__main__.py</code>.</li><li>Tests + fixture corpus.</li><li>AGENTS.md §2 layout line + CHANGELOG entry.</li></ol>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-108 (depends on, shipped or in flight)</strong> — semantic search engine: <code>cyberos.core.semantic.recall()</code> is the API we extend. If <code>sentence-transformers</code> not installed, FTS5 fallback is the same one FR-MEMORY-108 ships.</li><li><strong>FR-MEMORY-113 (this FR blocks)</strong> — recency-decay recall ranking: pluggable <code>recency</code> function will replace the constant <code>1.0</code> placeholder in <code>combined_score</code>.</li><li><strong>FR-MEMORY-114 (this FR blocks)</strong> — write-time importance scoring: optional <code>meta.importance</code> field reuses the same per-kind frontmatter contract pattern.</li><li><strong>FR-MEMORY-115 (this FR blocks)</strong> — <code>cyberos dream</code> consumes <code>episode.logged</code> aux rows for cross-session pattern detection.</li><li><strong>FR-MEMORY-120 (this FR enables)</strong> — <code>cyberos history</code> surfaces \"last time you tried this approach\" by walking Episodes for a given task fingerprint.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>episode.logged</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"episode.logged\",\n  \"payload\": {\n    \"path\":          \"memories/episodes/d4/12/d4127a3b-1f2c3d.md\",\n    \"outcome\":       \"success\",\n    \"duration_ms\":   1800000,\n    \"token_cost\":    120000,\n    \"quality_score\": 0.92,\n    \"logged_at\":     \"2026-05-19T14:23:11Z\"\n  }\n}</code></pre>\n<h3><code>cyberos recall-similar</code> response (--json)</h3>\n<pre><code class=\"language-json\">{\n  \"backend\": \"semantic\",\n  \"matches\": [\n    {\n      \"path\": \"memories/episodes/d4/12/d4127a3b-1f2c3d.md\",\n      \"task\": \"Ship FR-AUTH-003 RLS enforcement\",\n      \"approach\": \"single-commit RLS GUC; per-tenant policies\",\n      \"outcome\": \"success\",\n      \"quality_score\": 0.92,\n      \"relevance\": 0.84,\n      \"recency\": 1.0,\n      \"combined_score\": 0.910,\n      \"last_seen_at\": \"2026-05-18T20:55:13Z\"\n    }\n  ],\n  \"reason\": null\n}</code></pre>\n<h3>Memory file on disk (<code>memories/episodes/d4/12/d4127a3b-1f2c3d.md</code>)</h3>\n<pre><code class=\"language-markdown\">---\n{\n  \"approach\": \"single-commit RLS GUC; per-tenant policies\",\n  \"duration_ms\": 1800000,\n  \"kind\": \"episode\",\n  \"notes\": \"8/9 gaps closed\",\n  \"outcome\": \"success\",\n  \"quality_score\": 0.92,\n  \"task\": \"Ship FR-AUTH-003 RLS enforcement\",\n  \"token_cost\": 120000\n}\n---\n\nTask: Ship FR-AUTH-003 RLS enforcement\nApproach: single-commit RLS GUC; per-tenant policies\nOutcome: success\nNotes: 8/9 gaps closed</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Batch loading from external CI exports — §1 #15; slice 3+ stretch.</li><li>Per-tenant Episode allowlist (suppress recall of episodes from another tenant's memory even when imported) — slice 4+.</li><li><code>cyberos episode rescore --path ...</code> to update <code>quality_score</code> after the fact — emits a new <code>episode.rescored</code> row; deferred to slice 4 because it changes the recall surface.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>outcome</code> outside closed enum at construction</td><td><code>Episode.__post_init__</code> ValueError</td><td>Episode not created; caller sees exception</td><td>Caller picks one of {success, partial, failure}</td></tr><tr><td><code>outcome</code> outside enum in raw-written file</td><td>walker <code>episode-outcome-closed-enum</code> (error)</td><td><code>cyberos doctor</code> non-zero</td><td>Operator fixes file or <code>cyberos doctor --repair</code> if available</td></tr><tr><td><code>quality_score</code> &gt; 1.0</td><td><code>Episode.__post_init__</code> + walker</td><td>Same</td><td>Same</td></tr><tr><td><code>duration_ms</code> negative</td><td><code>Episode.__post_init__</code> + walker</td><td>Same</td><td>Same</td></tr><tr><td><code>error</code> empty on non-success outcome</td><td><code>Episode.__post_init__</code></td><td>Episode not created</td><td>Caller supplies a concrete failure description</td></tr><tr><td><code>task</code> empty after trim</td><td><code>Episode.__post_init__</code></td><td>Episode not created</td><td>Caller supplies non-empty task</td></tr><tr><td>Missing <code>sentence-transformers</code></td><td><code>semantic.available()</code> False</td><td><code>recall-similar</code> uses FTS5 backend; response includes <code>backend: \"fts5\"</code></td><td>Operator installs <code>pip install sentence-transformers</code> for semantic backend</td></tr><tr><td>10K-Episode store p95 &gt; 500 ms (semantic)</td><td>bench script asserts at CI</td><td>Latency alarm</td><td>Operator profiles + tunes batch size / vector quantization</td></tr><tr><td>Schema migration mid-flight (some files have new kind, some don't)</td><td>walker treats absent <code>kind</code> field as legacy; new kind validated against new schema</td><td>Both shapes coexist</td><td>None — migration is non-breaking by §1 #3</td></tr><tr><td>Concurrent <code>episode log</code> from two processes</td><td>existing <code>.lock</code> lease serialises</td><td>Both writes succeed sequentially; HEAD advances by 4 (2 writes × 2 rows each)</td><td>None — by design</td></tr><tr><td><code>episode.logged</code> aux row written but <code>put</code> row failed</td><td>impossible: writer emits both inside same transaction batch</td><td>If chain integrity broken, doctor catches via existing chain-verify</td><td><code>cyberos consolidate</code> (existing recovery path)</td></tr><tr><td>Caller passes both <code>quality_score=None</code> and <code>quality_score=0.5</code> (ambiguous intent)</td><td>only one kwarg; latest wins</td><td>No ambiguity in API</td><td>None</td></tr><tr><td>Schema enum extended in newer version, older walker can't parse</td><td>walker's enum is loaded from <code>memory.schema.json</code> at boot</td><td>Newer schema rejected by older walker → <code>cyberos doctor</code> fails with structured error</td><td>Operator upgrades the cyberos package</td></tr><tr><td>Token cost overflow (int &gt; 2^63)</td><td>jsonschema integer</td><td>rejected by schema validate</td><td>Caller sanity-checks; impossible in practice</td></tr><tr><td><code>notes</code> contains markdown that confuses the FTS tokenizer</td><td>FTS5 trigram tokenizer handles arbitrary UTF-8</td><td>Search ranks may shift but no error</td><td>None — by design</td></tr><tr><td><code>task</code> and <code>approach</code> identical (degenerate Episode)</td><td>Episode constructs successfully</td><td><code>recall-similar</code> ranks normally; doesn't filter dupes</td><td>If undesired, future dedup pass (FR-MEMORY-116) merges</td></tr><tr><td>Episode written by an actor without ACL on <code>memories/episodes/</code> (FR-MEMORY-117 era)</td><td>writer rejects per <code>STORE.yaml</code> check</td><td>Write fails with <code>acl_denied</code> audit row</td><td>Operator grants write scope or uses a different store</td></tr><tr><td>Caller misuses <code>recall-similar</code> for non-episode queries (e.g. recall recently-noted facts)</td><td>strict filter to <code>kind: episode</code></td><td>Returns no matches; reason: <code>no_episodes_above_min_relevance</code></td><td>Operator switches to <code>cyberos search</code> / <code>cyberos recall</code></td></tr><tr><td>Fixture corpus drift (new schema field added, fixture not updated)</td><td><code>test_fixture_corpus_parses</code> fails</td><td>CI blocked</td><td>Author updates fixture</td></tr><tr><td><code>combined_score</code> overflow / NaN</td><td>bounded inputs; deterministic arithmetic</td><td>Not possible by construction</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why frontmatter is JSON not YAML inside Episode files (§3 example).</strong> The walker's <code>frontmatter.py</code> already supports both YAML and JSON frontmatter. JSON is deterministic (no <code>null</code> vs absent ambiguity), and the existing writer's canonicalisation step produces JSON anyway. New Episodes emit JSON; legacy YAML Episodes (if anyone hand-rolls them) continue to parse.</li><li><strong>Why <code>episode.logged</code> is a separate aux audit kind, not a frontmatter field.</strong> A frontmatter field is part of the immutable body. The aux audit row is an indexable projection — FR-MEMORY-115 scans rows of kind <code>episode.logged</code> directly without re-parsing markdown. The same pattern is used elsewhere in CyberOS (<code>memory.imported</code>, <code>consolidation.published</code>).</li><li><strong><code>searchable_document()</code> excludes the <code>error</code> field deliberately.</strong> An error trace can contain stack-trace noise that pollutes the embedding. The error is preserved in the frontmatter for <code>cyberos history</code> and dream-pipeline introspection, but it's kept out of the FTS / semantic surface.</li><li><strong>Recall path is over-fetch + filter, not query-level kind filter at the engine.</strong> Reason: SQLite FTS5 doesn't natively filter on JSON metadata; vector store does but each backend has its own filter syntax. Over-fetching 4× the requested k and filtering in Python is portable + fast enough (vector retrieval is O(log N); filter is O(k·4) post-step).</li><li><strong>Fixture corpus is 40+ rows because that's the threshold below which CI flakes.</strong> Adding 10 more rows per quarter as the team observes new failure modes is the maintenance cadence (same as FR-MEMORY-111's PII corpus).</li><li><strong><code>memory_kind</code> is a string, not an enum, in the Python API.</strong> Enum would be cleaner but it forces every caller to import the enum; a string is easier for the CLI to pass through. The schema is the source of truth; runtime validation is on the schema side.</li><li><strong><code>tmp_memory</code> and <code>seeded_memory</code> pytest fixtures</strong> — defined in <code>modules/memory/tests/core/test_mmr.py</code>. <code>tmp_memory</code> is a fresh empty store; <code>seeded_memory</code> has 5 episodes + 5 facts pre-loaded for recall tests. Add <code>empty_memory</code> and <code>broken_memory</code> (with a hand-written invalid Episode) in this FR.</li><li><strong>Why we don't auto-set <code>last_seen_at</code> on the frontmatter.</strong> The audit chain already records when the put happened; duplicating it in the body would be drift-prone. FR-MEMORY-120 (<code>cyberos history</code>) projects <code>last_seen_at</code> from the latest audit row touching that path.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-112.</em></p>";
