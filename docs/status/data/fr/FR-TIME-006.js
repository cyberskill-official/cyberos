window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TIME-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TIME service <strong>MUST</strong> ship weekly approval flow at <code>services/time/src/timesheet/</code> with 5-state status FSM, AM review + reject, bulk approval, 14-day auto-lock, diff view on resubmission, email notifications, and 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>timesheet_status</code> enum: <code>('open','submitted','approved','rejected','locked')</code> per DEC-1421. Cardinality 5.</li></ol>\n<ol><li><strong>MUST</strong> define <code>timesheets</code> table at migration <code>0005</code>: <code>(timesheet_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_subject_id UUID NOT NULL, engagement_id UUID NOT NULL, week_start_date DATE NOT NULL, week_end_date DATE NOT NULL, status timesheet_status NOT NULL DEFAULT 'open', total_seconds INT NOT NULL DEFAULT 0, billable_seconds INT NOT NULL DEFAULT 0, submitted_at TIMESTAMPTZ, reviewed_at TIMESTAMPTZ, reviewed_by_subject_id UUID, locked_at TIMESTAMPTZ, lock_reason TEXT, trace_id CHAR(32))</code>. Partial unique <code>(member_subject_id, engagement_id, week_start_date)</code>. Append-only at status field via REVOKE per feature-request-audit skill rule 12.</li></ol>\n<ol><li><strong>MUST</strong> define <code>timesheet_reviews</code> table at migration <code>0006</code> for review history: <code>(id BIGSERIAL PRIMARY KEY, timesheet_id UUID NOT NULL REFERENCES timesheets(timesheet_id), reviewer_subject_id UUID NOT NULL, action TEXT NOT NULL CHECK (action IN ('approved','rejected')), reason TEXT, reviewed_at TIMESTAMPTZ NOT NULL DEFAULT now(), trace_id CHAR(32))</code>. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS scoped to tenant_id; Members see own timesheets, AMs see engagement-scoped, CFO sees all.</li></ol>\n<ol><li><strong>MUST</strong> expose Member submit <code>POST /v1/time/timesheets/{id}/submit</code>. Handler:</li></ol>\n<ul><li>Validates Member owns timesheet.</li><li>Validates status='open' or 'rejected'.</li><li>Aggregates entries for week → total_seconds, billable_seconds.</li><li>Transitions status='submitted' + submitted_at.</li><li>Triggers email to AM via FR-EMAIL-001.</li><li>Emits <code>time.timesheet_submitted</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> expose AM approve <code>POST /v1/time/timesheets/{id}/approve</code>. Caller has <code>engagement_admin</code> role. Handler:</li></ol>\n<ul><li>Validates status='submitted'.</li><li>Transitions status='approved' + reviewed_at + reviewed_by.</li><li>INSERTs review row.</li><li>Auto-transitions to 'locked' immediately per DEC-1422.</li><li>Emits <code>time.timesheet_approved</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> expose AM reject <code>POST /v1/time/timesheets/{id}/reject</code> body <code>{ reason }</code>. Handler:</li></ol>\n<ul><li>Transitions status='rejected'.</li><li>INSERTs review row with reason.</li><li>Triggers email to Member with reason.</li><li>Emits <code>time.timesheet_rejected</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> expose bulk approve <code>POST /v1/time/timesheets/bulk-approve</code> body <code>{ engagement_id, week_start_date }</code> per DEC-1423. AM scope. Approves all submitted timesheets matching filter; one memory row + per-timesheet audit. Emits <code>time.timesheet_bulk_approved</code> sev-2 + N individual <code>time.timesheet_approved</code>.</li></ol>\n<ol><li><strong>MUST</strong> expose diff view <code>GET /v1/time/timesheets/{id}/diff?since=&lt;submission_n&gt;</code> per DEC-1424. Returns added/removed/modified entries since prior submission for resubmission review.</li></ol>\n<ol><li><strong>MUST</strong> auto-lock 14d post-week-end per DEC-1425 via <code>auto_lock_job.rs</code>:</li></ol>\n<ul><li>Daily job: SELECT timesheets WHERE status='open' AND week_end_date &lt; now() - 14d.</li><li>Transition status='locked' + lock_reason='auto_lock_14d_no_submission'.</li><li>Triggers email to Member + AM.</li><li>Emits <code>time.timesheet_auto_locked</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> block entry writes for locked weeks per DEC-1422. FR-TIME-001 entry/create.rs modified to check <code>timesheets.status</code> for the week; locked → 412 + <code>week_locked</code>.</li></ol>\n<ol><li><strong>MUST</strong> trigger emails per DEC-1426 via FR-EMAIL-001:</li></ol>\n<ul><li>Monday 09:00: reminder to Members with unsubmitted previous week.</li><li>Wednesday 09:00: reminder to AMs with pending reviews.</li><li>On rejection: Member notified with reason.</li><li>On auto-lock: Member + AM notified.</li></ul>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1427. PII-scrub reason via FR-MEMORY-111.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow mutation of approved/locked timesheets per DEC-1422.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow non-AM approval per DEC-1420 (cfo has visibility only; no approval power).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why weekly cadence (§1 #1, DEC-1420)?</strong> Industry standard; matches client billing cadence; recent enough that Members remember what they did; not so frequent it's overhead.</p>\n<p><strong>Why immediate auto-lock on approve (§1 #6, DEC-1422)?</strong> Approved = ready-for-invoice. Lock prevents drift. Corrections via correction_to pattern (FR-TIME-001 derivative).</p>\n<p><strong>Why 14d auto-lock (§1 #10, DEC-1425)?</strong> Forgotten timesheets pollute reporting. 14 days = enough for Member to catch up; not so long that quarter-close blows up.</p>\n<p><strong>Why bulk approve (§1 #8, DEC-1423)?</strong> AM reviewing 20 Members weekly = 20 individual clicks. Bulk per-engagement reduces to 1 click + spot-check view.</p>\n<p><strong>Why diff view (§1 #9, DEC-1424)?</strong> Resubmission after rejection — AM needs to see what changed, not re-review from scratch. Standard pattern (Git PR diff).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0005_timesheets.sql\nCREATE TYPE timesheet_status AS ENUM ('open','submitted','approved','rejected','locked');\n\nCREATE TABLE timesheets (\n  timesheet_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  member_subject_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  week_start_date DATE NOT NULL,\n  week_end_date DATE NOT NULL,\n  status timesheet_status NOT NULL DEFAULT 'open',\n  total_seconds INT NOT NULL DEFAULT 0,\n  billable_seconds INT NOT NULL DEFAULT 0,\n  submitted_at TIMESTAMPTZ,\n  reviewed_at TIMESTAMPTZ,\n  reviewed_by_subject_id UUID,\n  locked_at TIMESTAMPTZ,\n  lock_reason TEXT,\n  trace_id CHAR(32)\n);\nCREATE UNIQUE INDEX uniq_timesheet_member_week\n  ON timesheets(member_subject_id, engagement_id, week_start_date);\nALTER TABLE timesheets ENABLE ROW LEVEL SECURITY;\nCREATE POLICY timesheets_rls ON timesheets\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON timesheets FROM cyberos_app;\nGRANT UPDATE (status, total_seconds, billable_seconds, submitted_at, reviewed_at,\n              reviewed_by_subject_id, locked_at, lock_reason) ON timesheets TO cyberos_app;\n\n-- 0006_timesheet_reviews.sql\nCREATE TABLE timesheet_reviews (\n  id BIGSERIAL PRIMARY KEY,\n  timesheet_id UUID NOT NULL REFERENCES timesheets(timesheet_id),\n  reviewer_subject_id UUID NOT NULL,\n  action TEXT NOT NULL CHECK (action IN ('approved','rejected')),\n  reason TEXT,\n  reviewed_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  trace_id CHAR(32)\n);\nALTER TABLE timesheet_reviews ENABLE ROW LEVEL SECURITY;\nCREATE POLICY timesheet_reviews_rls ON timesheet_reviews\n  USING (timesheet_id IN (SELECT timesheet_id FROM timesheets WHERE tenant_id = current_setting('auth.tenant_id')::uuid))\n  WITH CHECK (timesheet_id IN (SELECT timesheet_id FROM timesheets WHERE tenant_id = current_setting('auth.tenant_id')::uuid));\nREVOKE UPDATE, DELETE ON timesheet_reviews FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/time/timesheets/{id}/submit\nPOST   /v1/time/timesheets/{id}/approve              (engagement_admin)\nPOST   /v1/time/timesheets/{id}/reject               (engagement_admin)\nPOST   /v1/time/timesheets/bulk-approve              (engagement_admin)\nGET    /v1/time/timesheets/{id}/diff?since=N\nGET    /v1/time/timesheets/pending                    (engagement_admin)\nGET    /v1/time/timesheets/mine                       (member)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>timesheet_status cardinality 5</strong>.</li><li><strong>Submit transitions</strong> — open → submitted; email sent.</li><li><strong>Approve locks</strong> — submitted → approved → locked immediate.</li><li><strong>Reject + reason</strong> — submitted → rejected; Member emailed.</li><li><strong>Resubmit</strong> — rejected → submitted; diff view shows changes.</li><li><strong>Bulk approve</strong> — 5 submitted in engagement → all → approved.</li><li><strong>Auto-lock 14d</strong> — week_end+14d + status=open → locked.</li><li><strong>Locked entries blocked</strong> — entry write for locked week → 412.</li><li><strong>AM-only approval</strong> — Member tries to approve own → 403.</li><li><strong>5 memory audit kinds emitted</strong>.</li><li><strong>Email cadence Monday/Wednesday</strong> — scheduled jobs verified.</li><li><strong>CFO read-only</strong> — CFO can view but POST approve → 403.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>RLS cross-tenant denied</strong>.</li><li><strong>PII scrub reason</strong>.</li><li><strong>Concurrent submit race</strong> — first wins; second 409.</li><li><strong>Approved correction via correction_to</strong> — works (FR-TIME-001 path).</li><li><strong>Diff includes added + removed + modified</strong> — entry diffs accurate.</li><li><strong>Pending list ordered by week_end</strong> — oldest first.</li><li><strong>Bulk approve idempotent on already-approved</strong> — skips, no error.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn submit_then_approve_locks() {\n    let ctx = TestContext::with_timesheet().await;\n    ctx.submit_timesheet(ctx.ts_id).await;\n    ctx.as_am().approve_timesheet(ctx.ts_id).await;\n    let status: String = sqlx::query_scalar(\"SELECT status::text FROM timesheets WHERE timesheet_id=$1\")\n        .bind(ctx.ts_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(status, \"locked\");\n}\n\n#[tokio::test]\nasync fn reject_with_reason_emails_member() {\n    let ctx = TestContext::with_submitted_timesheet().await;\n    ctx.as_am().reject_timesheet(ctx.ts_id, \"missing project codes\").await;\n    let emails = ctx.sent_emails_to(ctx.member_id).await;\n    assert!(emails.iter().any(|e| e.body.contains(\"missing project codes\")));\n}\n\n#[tokio::test]\nasync fn auto_lock_after_14d() {\n    let ctx = TestContext::with_open_timesheet_week_ago(15).await;\n    ctx.run_auto_lock_job().await;\n    let status: String = sqlx::query_scalar(\"SELECT status::text FROM timesheets WHERE timesheet_id=$1\")\n        .bind(ctx.ts_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(status, \"locked\");\n}\n\n#[tokio::test]\nasync fn locked_week_blocks_entry_write() {\n    let ctx = TestContext::with_locked_timesheet().await;\n    let r = ctx.create_entry_for_week(ctx.member_id, ctx.locked_week_date).await;\n    assert_eq!(r.status(), 412);\n}\n\n// 5.5..5.10: bulk, diff, AM-only, audit, race, CFO read-only</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-TIME-001 (entries to aggregate). <strong>Cross-module:</strong> FR-AUTH-101 (engagement_admin role), FR-EMAIL-001 (notifications), FR-AI-003, FR-MEMORY-111.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>time.timesheet_approved</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.timesheet_approved\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.engagement_admin.789\",\n  \"trace_id\": \"...\",\n  \"payload\": {\n    \"timesheet_id\": \"0190...\",\n    \"member_subject_id_hash16\": \"f8a1...\",\n    \"engagement_id\": \"0190...\",\n    \"week_start_date\": \"2026-05-10\",\n    \"total_seconds\": 144000,\n    \"billable_seconds\": 130000\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-week submission (catch-up) — slice 2.</li><li><strong>Deferred:</strong> Auto-approve for trusted Members (zero-rejection track record) — slice 3.</li><li><strong>Deferred:</strong> Variance flags (week deviates &gt; 20% from history) — slice 3.</li><li><strong>Deferred:</strong> Mobile push notifications — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Submit before week-end</td><td>date check</td><td>400 + early_submission</td><td>Wait for week end</td></tr><tr><td>Approve without AM role</td><td>role check</td><td>403</td><td>Inherent</td></tr><tr><td>Reject without reason</td><td>validation</td><td>400</td><td>Inherent</td></tr><tr><td>Email send fails</td><td>FR-EMAIL-001 retry</td><td>Audit logged; user notified</td><td>Inherent retry</td></tr><tr><td>Concurrent submit + auto-lock race</td><td>tx isolation; partial unique catches</td><td>Submit wins if before lock</td><td>Inherent</td></tr><tr><td>Bulk approve includes non-submitted</td><td>filter on status='submitted'</td><td>Skipped silently</td><td>Inherent</td></tr><tr><td>Bulk approve cross-engagement</td><td>scope check</td><td>403 if non-AM</td><td>Inherent</td></tr><tr><td>Auto-lock job crashes</td><td>watchdog</td><td>Sev-2; manual lock CLI</td><td>Operator runs job manually</td></tr><tr><td>Locked week with pending corrections</td><td>correction_to path</td><td>Allowed; preserves audit</td><td>FR-TIME-001 derivative</td></tr><tr><td>Member resubmits after lock</td><td>status check</td><td>412 + week_locked</td><td>New entries via correction</td></tr><tr><td>AM tries to approve own timesheet</td><td>role + self check</td><td>403</td><td>Inherent</td></tr><tr><td>Submitted timesheet with 0 entries</td><td>allowed (zero-hours week)</td><td>Approved if AM ok</td><td>Inherent</td></tr><tr><td>Diff view across multiple resubmissions</td><td>versioned reviews</td><td>Last-submission diff</td><td>Inherent</td></tr><tr><td>Tenant timezone affects week boundary</td><td>per-tenant config</td><td>Monday 00:00 tenant TZ</td><td>Inherent</td></tr><tr><td>Email rate-limit hit</td><td>FR-EMAIL-001 queue</td><td>Delivery delayed</td><td>Inherent retry</td></tr><tr><td>Reviewer subject_id deleted post-approval</td><td>FK soft</td><td>Review row retained</td><td>Inherent forensic</td></tr><tr><td>Bulk approve &gt; 100 timesheets</td><td>size limit</td><td>413</td><td>Caller filters narrower</td></tr><tr><td>Auto-lock during Member typing</td><td>tx isolation</td><td>Member's submit may race with lock</td><td>First-wins</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Week boundary: Monday 00:00 tenant timezone.</p>\n<p><strong>§11.2</strong> Aggregation at submit reads entries for week × engagement × member.</p>\n<p><strong>§11.3</strong> Bulk approve uses single tx + N audit emits.</p>\n<p><strong>§11.4</strong> Auto-lock job runs daily 03:00; sweeps past-due.</p>\n<p><strong>§11.5</strong> Diff view compares two submission snapshots; each submission persists entry IDs.</p>\n<p><strong>§11.6</strong> Email templates use FR-PORTAL-002 brand pack overrides for tenant theming.</p>\n<p><strong>§11.7</strong> Locked-week entry block at FR-TIME-001 create.rs entry-point.</p>\n<p><strong>§11.8</strong> Concurrent submit race resolved by partial unique on (member, engagement, week).</p>\n<p><strong>§11.9</strong> Audit row carries member + AM subject IDs as hashes.</p>\n<p><strong>§11.10</strong> CFO visibility via separate read endpoint with <code>cfo</code> role; no write.</p>\n<hr>\n<p><em>End of FR-TIME-006 spec.</em></p>";
