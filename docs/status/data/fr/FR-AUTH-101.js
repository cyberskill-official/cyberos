window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-101"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> ship the closed 22-role RBAC catalogue, the permission matrix, the role-assignment REST API, the JWT role-claim shape, the ADR gate, and the stub→full migration path. Each component:</p>\n<ol><li><strong>MUST</strong> define exactly 22 roles in a closed Rust <code>enum Role</code> at <code>services/auth/src/rbac/catalogue.rs</code>. The variants are (in catalogue order, matching <code>website/docs/modules/auth.html#rbac-catalogue</code> and DEC-121): <code>RootAdmin</code>, <code>TenantAdmin</code>, <code>TenantMember</code>, <code>ServiceAccount</code>, <code>AgentPersona</code>, <code>Founder</code>, <code>Cfo</code>, <code>Cto</code>, <code>Coo</code>, <code>Chro</code>, <code>Cmo</code>, <code>Cpo</code>, <code>Cso</code>, <code>Cseco</code>, <code>Clo</code>, <code>Cdo</code>, <code>Dpo</code>, <code>Caio</code>, <code>ClientPortalUser</code>, <code>Auditor</code>, <code>Regulator</code>, <code>BillingSystem</code>. The string form is kebab-case (e.g. <code>tenant-admin</code>, <code>client-portal-user</code>). The enum <strong>MUST NOT</strong> carry a <code>_Other(String)</code> variant — unknown strings parse to <code>RoleParseError::UnknownRole(input)</code> and are rejected at every API boundary.</li></ol>\n<ol><li><strong>MUST</strong> define exactly <strong>40 resources</strong> in a closed <code>enum Resource</code> (one per cross-module surface: <code>subject</code>, <code>tenant</code>, <code>role_assignment</code>, <code>jwt_jwks</code>, <code>audit_row</code>, <code>crm_account</code>, <code>crm_contact</code>, <code>crm_deal</code>, <code>proj_issue</code>, <code>proj_engagement</code>, <code>proj_rate_card</code>, <code>proj_timeline</code>, <code>time_entry</code>, <code>time_expense</code>, <code>inv_invoice</code>, <code>inv_payment</code>, <code>inv_hoa_don</code>, <code>kb_document</code>, <code>kb_runbook</code>, <code>hr_member</code>, <code>hr_contract</code>, <code>hr_leave</code>, <code>hr_cccd_photo</code>, <code>rew_payslip</code>, <code>rew_bp_ledger</code>, <code>esop_grant</code>, <code>esop_valuation</code>, <code>learn_skill</code>, <code>learn_certification</code>, <code>okr_objective</code>, <code>okr_kr</code>, <code>res_allocation</code>, <code>doc_document</code>, <code>doc_signature</code>, <code>email_thread</code>, <code>chat_channel</code>, <code>chat_message</code>, <code>cuo_chain</code>, <code>memory_memory</code>, <code>obs_alert</code>). Adding a 41st resource is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> define exactly 5 actions in a closed <code>enum Action</code>: <code>Read</code>, <code>Write</code>, <code>Admin</code>, <code>Approve</code>, <code>Sign</code>. <code>Approve</code> is reserved for dual-signoff workflows (CFO+CEO co-sign, etc.); <code>Sign</code> is reserved for e-signature on DOC + hóa đơn emissions. Adding a 6th action is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> seed the permission matrix in migration <code>0005_roles_permissions.sql</code> with one row per <code>(role, resource, action)</code> tuple that is allowed. Disallowed tuples are absent — there is no explicit deny row. Total seeded rows: ~280 (varies by audit; the migration's row count is asserted in the migration test). The matrix is loaded into the <code>RoleMatrix</code> snapshot at service start and refreshed every 60 s by a tokio task.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/subjects/{subject_id}/roles</code> with body <code>{\"role\": \"&lt;kebab-name&gt;\"}</code> to grant a single role to a subject. The handler:</li></ol>\n<ul><li>Validates the caller has <code>Resource::RoleAssignment + Action::Admin</code> (typically <code>tenant-admin</code> or <code>root-admin</code>).</li><li>Parses the role string via <code>Role::from_str</code> — unknown name → <code>400 BAD_REQUEST {\"error\":\"unknown_role\",\"role\":\"&lt;x&gt;\",\"allowed\":[...22 names...]}</code>.</li><li>Refuses self-assignment of reserved roles (per §1 #11). Reserved-role assignment requires a dedicated elevated-privilege endpoint (out of scope for slice 1; see §9).</li><li>Refuses <code>founder</code> assignment to a subject without a registered WebAuthn factor (per DEC-128 + FR-AUTH-105). Missing factor → <code>409 CONFLICT {\"error\":\"webauthn_required\",\"role\":\"founder\"}</code>.</li><li>Inserts into <code>subject_roles</code> (tenant-scoped; RLS-protected) with <code>(subject_id, role, granted_by, granted_at)</code>. Duplicate grant → <code>409 CONFLICT {\"error\":\"already_granted\"}</code> (idempotent on the (subject_id, role) PK).</li><li>Emits exactly one <code>auth.role_assigned</code> memory audit row before commit (audit-before-action per feature-request-audit skill rule 25).</li><li>Returns <code>201 CREATED</code> with <code>{\"subject_id\", \"role\", \"granted_by\", \"granted_at\"}</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>DELETE /v1/admin/subjects/{subject_id}/roles/{role}</code> with the same caller-permission gate. Deletion:</li></ol>\n<ul><li>Is hard-delete (no soft-delete; the audit row is the record).</li><li>Emits exactly one <code>auth.role_revoked</code> memory audit row before commit.</li><li>Returns <code>204 NO CONTENT</code>. Already-absent → <code>204</code> (idempotent — <code>DELETE</code> matches REST semantics).</li></ul>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/admin/roles</code> returning the catalogue: <code>{\"version\": &lt;rbac_v&gt;, \"roles\": [{\"name\":\"&lt;kebab&gt;\",\"display\":\"&lt;Human&gt;\",\"reserved\":&lt;bool&gt;,\"requires_webauthn\":&lt;bool&gt;,\"scope_summary\":\"&lt;one-line&gt;\"}, ...]}</code>. The handler reads from the in-memory <code>RoleMatrix</code>; never hits the DB. RBAC-version-aware caching: ETag is <code>W/\"rbac-v&lt;n&gt;\"</code>.</li></ol>\n<ol><li><strong>MUST</strong> issue access tokens (FR-AUTH-004 path) with two new claims:</li></ol>\n<ul><li><code>roles: [\"&lt;role-name&gt;\", ...]</code> — array of kebab-case role names the subject holds at issuance time.</li><li><code>rbac_v: &lt;integer&gt;</code> — the catalogue version embedded for replay-resistance. Verifiers MAY compare against the live <code>RoleMatrix.version</code> and challenge tokens issued under stale catalogue versions (acceptable lag: 1 minor version; rejection threshold: &gt; 2 versions behind).</li></ul>\n<ol><li><strong>MUST</strong> check role membership via the in-memory <code>RoleMatrix</code> only. The matrix is loaded at boot from <code>subject_roles</code> join <code>role_permissions</code> and refreshed every 60 s. Per-request DB lookups for role/permission are <strong>MUST NOT</strong> (per DEC-126 — performance budget is &lt; 50 µs per check at p95).</li></ol>\n<ol><li><strong>MUST</strong> consult the <code>RoleMatrix</code> from RLS via the <code>auth.has_role(role_name)</code> SQL function (created in migration <code>0005_roles_permissions.sql</code>). The function reads the per-session GUC <code>auth.roles</code> (set by the JWT middleware on every connection). RLS policies for sensitive tables (<code>subjects</code>, <code>audit_row</code>, <code>billing_*</code>, <code>hr_cccd_photo</code>, <code>rew_payslip</code>) <strong>MUST</strong> include a <code>auth.has_role(&lt;role&gt;)</code> check in addition to the existing tenant-id RLS.</li></ol>\n<ol><li><strong>MUST</strong> classify the following 5 roles as <strong>reserved</strong> (<code>Role::is_reserved() == true</code>): <code>root-admin</code>, <code>client-portal-user</code>, <code>auditor</code>, <code>regulator</code>, <code>billing-system</code>. Reserved roles <strong>MUST NOT</strong> be assignable via the standard <code>POST /v1/admin/subjects/{id}/roles</code> endpoint. Attempts return <code>403 FORBIDDEN {\"error\":\"reserved_role\",\"role\":\"&lt;x&gt;\",\"required_endpoint\":\"&lt;path or 'not yet specified'&gt;\"}</code>. The dedicated reserved-role-assignment endpoints land in slice 2 (out of scope for this FR; see §9).</li></ol>\n<ol><li><strong>MUST</strong> classify <code>founder</code> as <strong>WebAuthn-required</strong> (<code>Role::requires_webauthn() == true</code>). All other roles return <code>false</code> from this method. The classification is intrinsic to the role (closed enum match), not driven by configuration — preventing accidental relaxation.</li></ol>\n<ol><li><strong>MUST</strong> reject any JWT whose <code>roles</code> claim contains a string that does not parse to a <code>Role</code> variant. The verifier (used by every consuming service per FR-AUTH-004 §1 #7) treats this as a tampered token: <code>401 UNAUTHORIZED {\"error\":\"invalid_token\",\"reason\":\"unknown_role_in_claim\"}</code>. The check is at the verifier, not downstream — failing closed.</li></ol>\n<ol><li><strong>MUST</strong> emit exactly one <code>auth.role_catalogue_changed</code> memory audit row whenever the catalogue version bumps (via the trigger on <code>role_catalogue_version</code>). The row carries <code>{old_version, new_version, changed_at, migration_id, adr_id}</code>. Catalogue version bumps without a recorded <code>adr_id</code> (e.g. ad-hoc INSERT in a manual psql session) fail the audit row's <code>NOT NULL adr_id</code> constraint and roll back the change.</li></ol>\n<ol><li><strong>MUST</strong> ship migration <code>0005_roles_permissions.sql</code> that creates:</li></ol>\n<ul><li><code>roles(name TEXT PRIMARY KEY, display TEXT NOT NULL, reserved BOOLEAN NOT NULL, requires_webauthn BOOLEAN NOT NULL, scope_summary TEXT NOT NULL, lands_in_slice INT NOT NULL)</code> — 22 seeded rows matching the closed enum.</li><li><code>resources(name TEXT PRIMARY KEY, module TEXT NOT NULL)</code> — 40 seeded rows.</li><li><code>actions(name TEXT PRIMARY KEY)</code> — 5 seeded rows.</li><li><code>role_permissions(role TEXT REFERENCES roles, resource TEXT REFERENCES resources, action TEXT REFERENCES actions, PRIMARY KEY (role, resource, action))</code> — ~280 seeded rows.</li><li><code>subject_roles(tenant_id UUID, subject_id UUID, role TEXT REFERENCES roles, granted_by UUID, granted_at TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY (subject_id, role))</code> — tenant-scoped; RLS enabled with <code>USING + WITH CHECK</code> on <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>.</li><li>SQL function <code>auth.has_role(role_name TEXT) RETURNS BOOLEAN</code> — reads session GUC <code>auth.roles</code> (comma-separated).</li></ul>\n<ol><li><strong>MUST</strong> ship migration <code>0006_role_catalogue_version.sql</code> that creates:</li></ol>\n<ul><li><code>role_catalogue_version(id INT PRIMARY KEY CHECK (id = 1), version INT NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), adr_id TEXT NOT NULL)</code> — singleton (id=1).</li><li>AFTER INSERT/UPDATE/DELETE trigger on <code>roles</code> + <code>role_permissions</code> that bumps <code>version</code> and inserts an <code>auth.role_catalogue_changed</code> memory audit row via the memory_writer bridge (FR-AI-003).</li></ul>\n<ol><li><strong>MUST</strong> ship the ADR gate at <code>services/auth/src/rbac/adr.rs</code> as a CI-callable validator. Invoked as <code>cargo test rbac_adr_gate_test</code>, it:</li></ol>\n<ul><li>Parses each migration file under <code>services/auth/migrations/</code> for changes to <code>roles</code> or <code>role_permissions</code>.</li><li>For each such migration, requires a matching <code>services/auth/adr/ADR-NNN-*.md</code> file referenced in a SQL comment <code>-- ADR: ADR-NNN</code>.</li><li>Fails the test if any role-touching migration lacks an ADR reference, OR if the referenced ADR-NNN.md file does not exist.</li></ul>\n<ol><li><strong>MUST</strong> support the 5-role stub→full migration path <strong>without invalidating existing access tokens issued under <code>rbac_v = 1</code></strong>. Existing tokens (issued by FR-AUTH-002's 5-role allow-list, before this FR ships) carry no <code>rbac_v</code> claim. The verifier treats the absence of <code>rbac_v</code> as implicit <code>rbac_v = 1</code> and accepts the token for a 30-day grace window. After grace, missing-claim tokens are rejected (<code>401 UNAUTHORIZED {\"error\":\"rbac_version_required\"}</code>). The grace-window enforcer is FR-AUTH-109.</li></ol>\n<ol><li><strong>MUST</strong> classify the 5 stub roles (root-admin, tenant-admin, tenant-member, service-account, agent-persona) as a strict prefix of the 22-role catalogue — same names, same string form, additive permissions only. The matrix migration MUST NOT change the permission matrix for any stub role except to add new (resource, action) tuples. A regression test (<code>rbac_stub_compat_test</code>) asserts that every (stub_role, resource, action) tuple present in the prior FR-AUTH-002 catalogue is still present after migration.</li></ol>\n<ol><li><strong>MUST</strong> support layered narrowing via the <code>scope_grant</code> table: <code>scope_grants(tenant_id UUID, subject_id UUID, resource TEXT, resource_id UUID, action TEXT, granted_by UUID, granted_at TIMESTAMPTZ, expires_at TIMESTAMPTZ)</code>. The grant <strong>NARROWS</strong> a role's base privilege to specific resource_ids. Example: <code>cfo</code> has <code>inv_invoice + Read</code> matrix-wide; a <code>scope_grants</code> row with <code>resource=inv_invoice, resource_id=&lt;invoice-A&gt;, action=Read, expires_at=2026-12-31</code> for a <code>tenant-member</code> subject grants read access to invoice-A only. The <code>HasPermission</code> extractor checks both layers: role matrix MUST permit OR scope-grant MUST cover. Grants are not standalone privileges — they only narrow/extend within a tenant's scope.</li></ol>\n<ol><li><strong>MUST</strong> complete role check (<code>has_role</code> / <code>has_any_role</code> / <code>has_permission</code>) in ≤ 50 µs p99 against an in-memory matrix of 22 roles × 200 permissions × 1000 active subjects. The performance test (<code>rbac_perf_test</code>) asserts this on every CI run.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>auth.rbac_check</code> with attributes <code>subject_id_hash16</code>, <code>role</code>, <code>resource</code>, <code>action</code>, <code>outcome</code> (allow | deny | reserved_role | webauthn_required | unknown_role) on every check. Sampling: 1% under steady state, 100% on <code>outcome != allow</code> (deny + error paths fully captured).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_rbac_check_total{outcome, role}</code> (counter).</li><li><code>auth_rbac_check_latency_us</code> (histogram; SLO p99 &lt; 50 µs).</li><li><code>auth_rbac_matrix_refresh_total{outcome}</code> (counter; outcome ∈ {success, db_unreachable, hash_unchanged}).</li><li><code>auth_rbac_subject_role_count{tenant_id}</code> (gauge — number of (subject, role) pairs).</li><li><code>auth_rbac_catalogue_version</code> (gauge — current <code>rbac_v</code>).</li></ul>\n<ol><li><strong>MUST</strong> include <code>roles</code> and <code>rbac_v</code> claims in every JWT issued by FR-AUTH-004 after this FR ships. The JWT header <code>typ</code> MUST remain <code>JWT</code>; the claim shape change is additive only (no breaking changes to existing claim names). FR-AUTH-004's verifier is updated to surface both claims via <code>Claims::roles()</code> and <code>Claims::rbac_v()</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship the ADR file <code>services/auth/adr/ADR-101-rbac-22-role-catalogue.md</code> as part of this FR. The ADR documents: business rationale (closed catalogue prevents ABAC slide), scope-creep risk assessment (each new role costs 1 ADR + DPO + CSEC review), deprecation policy (90-day shadow-monitoring window), audit-trail implications (every assignment + revocation chained into memory), and the explicit DPO + CSEC sign-off block.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a closed 22-role catalogue and not ABAC (§1 #1, DEC-121, DEC-122)?</strong> ABAC (attribute-based access control) sounds flexible — \"if subject.department == finance AND resource.classification &lt;= confidential, allow\" — but in practice it produces a debugging nightmare. Every access decision becomes a logic puzzle; tracing why a request was denied requires reconstructing the attribute graph at decision time. RBAC with a closed catalogue is auditable by construction: every subject's privileges are a finite, enumerable set, and changes are ADR-gated. The 22-role boundary is a deliberate ceiling — when we want a 23rd, we either (a) realise an existing role covers the case, or (b) write an ADR that forces explicit consideration of scope creep. The website docs (§2.6) describe this as \"a design assertion.\"</p>\n<p><strong>Why the 5-role stub is a strict prefix (§1 #19, DEC-123)?</strong> FR-AUTH-002 shipped a 2-name allow-list (<code>tenant-admin</code>, <code>tenant-member</code>); the wider stub used in FR-AUTH-005/006 included <code>root-admin</code>, <code>service-account</code>, <code>agent-persona</code>. Those 5 names are not \"temporary placeholder names\" — they ARE the production names. This FR adds 17 more without renaming any of the 5. The <code>rbac_stub_compat_test</code> regression test makes this guarantee enforceable: prior subjects holding <code>tenant-admin</code> keep exactly the same matrix-permission set + any new (resource, action) tuples that were absent before. This is the property that lets existing tokens (without <code>rbac_v</code>) keep working — the underlying matrix is additive only.</p>\n<p><strong>Why a permission matrix layer instead of role checks directly in code (§1 #4, DEC-122)?</strong> Role checks scattered across services produce drift: service A might use <code>caller.has_role(\"chief-financial-officer\")</code>, service B might write <code>caller.role == \"cfo\" || caller.role == \"founder\"</code>. The matrix centralises the truth — <code>caller.has_permission(Resource::InvInvoice, Action::Approve)</code> is the canonical check, and the matrix decides which roles satisfy it. Adding a new role only requires updating the matrix; consuming services don't change. This is the ISO 27001:2022 A.5.18 recommendation (access rights provisioned via documented roles), not via inline string comparisons.</p>\n<p><strong>Why role + scope-grant layered (§1 #20, DEC-124)?</strong> Pure RBAC over-grants: giving an external auditor the <code>auditor</code> role grants matrix-wide read; we only want them to see the specific audit window they were engaged for. The scope-grant layer narrows: <code>auditor</code> role provides base privilege; the <code>scope_grants</code> row narrows it to <code>resource_id IN (engagement-2026-q3)</code>. Without scope-grants, we'd be forced to invent per-audit roles (<code>auditor-2026-q3</code>) — and that's the ABAC slide the closed catalogue is designed to prevent. Scope grants are NOT standalone privileges (they cannot grant what the role does not already permit); they only narrow/restrict. This preserves the matrix as the source of truth for \"what is allowed in principle.\"</p>\n<p><strong>Why in-memory RoleMatrix with 60s refresh and not per-request DB lookup (§1 #9, §1 #21, DEC-126)?</strong> Role checks happen on every authenticated request — typically 5–20 per request lifetime (auth, RLS context, scope checks, audit-row emission). At 1k RPS with 10 checks each, that's 10k role lookups per second; at 1 ms per DB hop, that's 10 cores burning on RBAC alone. The in-memory matrix collapses this to a single hashmap lookup at &lt; 50 µs. The 60-second refresh tolerates revocation latency in exchange for performance: a revoked role is honoured for up to 60s after revocation; the OTel <code>auth_rbac_matrix_refresh_total{outcome=success}</code> metric tracks freshness. Time-critical revocations (e.g. terminated employee) go via the per-tenant CRL flush endpoint that targets the cache directly (out of scope for this FR; see FR-AUTH-111 placeholder).</p>\n<p><strong>Why reserved roles cannot be self-assigned (§1 #11, DEC-127)?</strong> <code>root-admin</code> is the cross-tenant superuser — its assignment is a CyberSkill operator action, not a tenant action. <code>client-portal-user</code> belongs to PORTAL's JIT provisioning flow (FR-PORTAL-003). <code>auditor</code> and <code>regulator</code> are external identities granted through a vetted intake — never by a tenant-admin clicking a UI button. <code>billing-system</code> is the Stripe/VietQR webhook identity — assigned via a setup script, not a REST call. Routing reserved-role assignment through the standard endpoint would invite operator mistakes; refusing at the API boundary makes the elevated path explicit.</p>\n<p><strong>Why founder requires WebAuthn (§1 #12, DEC-128)?</strong> The founder role grants cross-module privileged read (financial overview, OKR-cascade override, strategic-document signoff). A compromised founder credential is catastrophic — the entire company's executive view is exposed. WebAuthn (passkey) eliminates phishability and provides hardware-bound assurance. The check is intrinsic to the role (<code>Role::Founder.requires_webauthn() == true</code>) rather than configuration-driven because we never want this gate accidentally turned off via a config typo.</p>\n<p><strong>Why <code>rbac_v</code> in the JWT (§1 #8, DEC-129)?</strong> Without versioning, a long-lived access token issued before a role's permissions were tightened keeps the older, looser permissions until expiry. The <code>rbac_v</code> claim lets verifiers detect stale tokens and challenge them — the threshold (2 versions behind) tolerates normal refresh lag but catches significantly outdated tokens. Verifiers compare <code>claims.rbac_v</code> against <code>RoleMatrix.version</code>; &gt; 2 behind → reject with <code>rbac_version_stale</code>. This is replay-resistance against tokens issued before a role-tightening ADR landed.</p>\n<p><strong>Why the ADR gate at CI (§1 #17, §1 #25, DEC-121)?</strong> Without the gate, a developer can quietly add a 23rd role via a migration patch and the closed-catalogue design assertion silently dies. The CI test fails the build until ADR-NNN.md exists AND the migration's SQL comment references it. The cost is one ADR file per role change; the benefit is that the catalogue's boundary is enforced by tooling, not by reviewer vigilance.</p>\n<p><strong>Why classify exactly 40 resources and 5 actions (§1 #2, §1 #3)?</strong> Resources and actions are themselves closed enums for the same reason roles are: drift between services on what counts as a \"resource\" or an \"action\" produces unauditable matrices. 40 covers every cross-module surface in the planned BACKLOG; 5 actions (Read/Write/Admin/Approve/Sign) cover every business workflow without ABAC-style verb explosion. Adding the 41st resource or 6th action is an ADR — the same governance discipline as adding a role.</p>\n<p><strong>Why does the <code>roles</code> claim hold names only and not the matrix (§1 #8, PDPL Art. 6)?</strong> Embedding the full permission matrix in every JWT bloats tokens (typical ~2 KB → ~50 KB) and creates a data-minimisation problem: every service that logs the JWT now logs every permission. Names-only keeps the token small and the verifier-side matrix lookup fast (&lt; 50 µs). The verifier resolves names → permissions via the in-memory <code>RoleMatrix</code>; the token never carries the matrix itself.</p>\n<p><strong>Why <code>auth.has_role()</code> as a SQL function for RLS (§1 #10)?</strong> RLS policies need to evaluate at the database layer, not the application layer — application-layer checks can be bypassed by a SQL-injection or a misrouted query. The SQL function reads the per-session GUC (set by the JWT middleware at connection acquisition); RLS policies invoke it inline. This makes \"tenant-admin or above can read audit rows\" express as <code>USING (tenant_id = current_setting('auth.tenant_id')::uuid AND auth.has_role('tenant-admin'))</code> — the role check is in the policy, not behind it.</p>\n<p><strong>Why hard-delete role assignments instead of soft-delete (§1 #6)?</strong> The <code>auth.role_revoked</code> memory audit row IS the record. Soft-delete would create two records of truth (the row's <code>revoked_at</code> field plus the audit row) and invite \"is this revoked?\" ambiguity in queries. The audit row is unerasable (per AGENTS.md §6.5); soft-delete adds nothing the audit chain doesn't already provide.</p>\n<p><strong>Why a 30-day grace window for stub-era tokens (§1 #18, DEC-125)?</strong> Typical access-token lifetime in the FR-AUTH-004 design is 1 hour; refresh tokens 7 days. A 30-day grace covers every refresh-token cycle plus a safety margin — no production user is surprised by a forced re-auth. The grace-window enforcer (FR-AUTH-109) flips on <code>30d after this FR ships</code>; until then, missing-<code>rbac_v</code> tokens are accepted. After flip, missing-claim tokens are rejected — and the rejection metric <code>auth_rbac_check_total{outcome=stub_token_rejected}</code> tells operations how many tokens are still missing the claim.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Closed role enum</h3>\n<pre><code class=\"language-rust\">// services/auth/src/rbac/catalogue.rs\nuse std::str::FromStr;\nuse std::fmt;\nuse serde::{Deserialize, Serialize};\n\n/// The closed 22-role catalogue. Adding a variant requires ADR-NNN + matching migration.\n/// String form is kebab-case via Display + FromStr.\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]\n#[serde(try_from = \"&amp;str\", into = \"String\")]\npub enum Role {\n    RootAdmin,\n    TenantAdmin,\n    TenantMember,\n    ServiceAccount,\n    AgentPersona,\n    Founder,\n    Cfo,\n    Cto,\n    Coo,\n    Chro,\n    Cmo,\n    Cpo,\n    Cso,\n    Cseco,\n    Clo,\n    Cdo,\n    Dpo,\n    Caio,\n    ClientPortalUser,\n    Auditor,\n    Regulator,\n    BillingSystem,\n}\n\nimpl Role {\n    /// All 22 variants in catalogue order. Tests assert len() == 22.\n    pub const ALL: &amp;'static [Role] = &amp;[\n        Role::RootAdmin, Role::TenantAdmin, Role::TenantMember, Role::ServiceAccount,\n        Role::AgentPersona, Role::Founder, Role::Cfo, Role::Cto, Role::Coo, Role::Chro,\n        Role::Cmo, Role::Cpo, Role::Cso, Role::Cseco, Role::Clo, Role::Cdo, Role::Dpo,\n        Role::Caio, Role::ClientPortalUser, Role::Auditor, Role::Regulator, Role::BillingSystem,\n    ];\n\n    /// Kebab-case string form (canonical wire/storage representation).\n    pub fn as_str(self) -&gt; &amp;'static str {\n        match self {\n            Role::RootAdmin =&gt; \"root-admin\",\n            Role::TenantAdmin =&gt; \"tenant-admin\",\n            Role::TenantMember =&gt; \"tenant-member\",\n            Role::ServiceAccount =&gt; \"service-account\",\n            Role::AgentPersona =&gt; \"agent-persona\",\n            Role::Founder =&gt; \"founder\",\n            Role::Cfo =&gt; \"chief-financial-officer\",\n            Role::Cto =&gt; \"chief-technology-officer\",\n            Role::Coo =&gt; \"chief-operating-officer\",\n            Role::Chro =&gt; \"chief-human-resources-officer\",\n            Role::Cmo =&gt; \"chief-marketing-officer\",\n            Role::Cpo =&gt; \"cpo\",\n            Role::Cso =&gt; \"cso\",\n            Role::Cseco =&gt; \"cseco\",\n            Role::Clo =&gt; \"clo\",\n            Role::Cdo =&gt; \"cdo\",\n            Role::Dpo =&gt; \"dpo\",\n            Role::Caio =&gt; \"chief-ai-officer\",\n            Role::ClientPortalUser =&gt; \"client-portal-user\",\n            Role::Auditor =&gt; \"auditor\",\n            Role::Regulator =&gt; \"regulator\",\n            Role::BillingSystem =&gt; \"billing-system\",\n        }\n    }\n\n    /// Reserved roles (cannot be self-assigned via standard REST).\n    pub fn is_reserved(self) -&gt; bool {\n        matches!(self,\n            Role::RootAdmin | Role::ClientPortalUser | Role::Auditor\n            | Role::Regulator | Role::BillingSystem\n        )\n    }\n\n    /// WebAuthn-required roles (founder only at slice 1).\n    pub fn requires_webauthn(self) -&gt; bool {\n        matches!(self, Role::Founder)\n    }\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum RoleParseError {\n    #[error(\"unknown_role: {0}\")]\n    UnknownRole(String),\n}\n\nimpl FromStr for Role {\n    type Err = RoleParseError;\n    fn from_str(s: &amp;str) -&gt; Result&lt;Self, Self::Err&gt; {\n        for r in Role::ALL { if r.as_str() == s { return Ok(*r); } }\n        Err(RoleParseError::UnknownRole(s.to_string()))\n    }\n}\n\nimpl fmt::Display for Role {\n    fn fmt(&amp;self, f: &amp;mut fmt::Formatter&lt;'_&gt;) -&gt; fmt::Result { f.write_str(self.as_str()) }\n}\n\nimpl TryFrom&lt;&amp;str&gt; for Role {\n    type Error = RoleParseError;\n    fn try_from(s: &amp;str) -&gt; Result&lt;Self, Self::Error&gt; { Role::from_str(s) }\n}\n\nimpl From&lt;Role&gt; for String {\n    fn from(r: Role) -&gt; String { r.as_str().to_string() }\n}</code></pre>\n<h3>3.2 — Closed resource + action enums</h3>\n<pre><code class=\"language-rust\">// services/auth/src/rbac/permissions.rs\nuse serde::{Deserialize, Serialize};\nuse std::str::FromStr;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]\n#[serde(try_from = \"&amp;str\", into = \"&amp;'static str\")]\npub enum Resource {\n    Subject, Tenant, RoleAssignment, JwtJwks, AuditRow,\n    CrmAccount, CrmContact, CrmDeal,\n    ProjIssue, ProjEngagement, ProjRateCard, ProjTimeline,\n    TimeEntry, TimeExpense,\n    InvInvoice, InvPayment, InvHoaDon,\n    KbDocument, KbRunbook,\n    HrMember, HrContract, HrLeave, HrCccdPhoto,\n    RewPayslip, RewBpLedger,\n    EsopGrant, EsopValuation,\n    LearnSkill, LearnCertification,\n    OkrObjective, OkrKr,\n    ResAllocation,\n    DocDocument, DocSignature,\n    EmailThread, ChatChannel, ChatMessage,\n    CuoChain, MemoryMemory, ObsAlert,\n}\n\nimpl Resource {\n    pub const ALL: &amp;'static [Resource] = &amp;[ /* ...40 variants in order... */ ];\n    pub fn as_str(self) -&gt; &amp;'static str { /* kebab-snake match */ todo!() }\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]\n#[serde(try_from = \"&amp;str\", into = \"&amp;'static str\")]\npub enum Action { Read, Write, Admin, Approve, Sign }\n\nimpl Action {\n    pub const ALL: &amp;'static [Action] = &amp;[Action::Read, Action::Write, Action::Admin, Action::Approve, Action::Sign];\n}</code></pre>\n<h3>3.3 — In-memory RoleMatrix + 60s refresher</h3>\n<pre><code class=\"language-rust\">// services/auth/src/rbac/matrix.rs\nuse std::collections::{HashMap, HashSet};\nuse std::sync::Arc;\nuse arc_swap::ArcSwap;\nuse sha2::{Digest, Sha256};\n\nuse crate::rbac::catalogue::Role;\nuse crate::rbac::permissions::{Resource, Action};\n\n#[derive(Debug, Clone)]\npub struct RoleMatrix {\n    /// Catalogue version (bumped on every ADR-approved schema change).\n    pub version: u32,\n    /// (role, resource, action) → allowed.\n    pub allowed: HashSet&lt;(Role, Resource, Action)&gt;,\n    /// hash of the matrix for change detection during refresh.\n    pub hash: [u8; 32],\n}\n\nimpl RoleMatrix {\n    pub fn has_permission(&amp;self, role: Role, res: Resource, act: Action) -&gt; bool {\n        self.allowed.contains(&amp;(role, res, act))\n    }\n}\n\npub struct RoleMatrixHandle {\n    inner: Arc&lt;ArcSwap&lt;RoleMatrix&gt;&gt;,\n}\n\nimpl RoleMatrixHandle {\n    pub async fn load_from_db(pool: &amp;sqlx::PgPool) -&gt; anyhow::Result&lt;Self&gt; {\n        let m = load_matrix(pool).await?;\n        Ok(Self { inner: Arc::new(ArcSwap::from_pointee(m)) })\n    }\n\n    pub fn snapshot(&amp;self) -&gt; Arc&lt;RoleMatrix&gt; { self.inner.load_full() }\n\n    /// Spawn 60s refresher. Reloads matrix; only swaps if hash changed.\n    pub fn spawn_refresher(self: Arc&lt;Self&gt;, pool: sqlx::PgPool) {\n        tokio::spawn(async move {\n            let mut interval = tokio::time::interval(std::time::Duration::from_secs(60));\n            loop {\n                interval.tick().await;\n                match load_matrix(&amp;pool).await {\n                    Ok(fresh) =&gt; {\n                        let current = self.inner.load();\n                        if current.hash != fresh.hash {\n                            self.inner.store(Arc::new(fresh));\n                            metrics::counter!(\"auth_rbac_matrix_refresh_total\", \"outcome\" =&gt; \"success\").increment(1);\n                        } else {\n                            metrics::counter!(\"auth_rbac_matrix_refresh_total\", \"outcome\" =&gt; \"hash_unchanged\").increment(1);\n                        }\n                    }\n                    Err(_) =&gt; {\n                        metrics::counter!(\"auth_rbac_matrix_refresh_total\", \"outcome\" =&gt; \"db_unreachable\").increment(1);\n                        // Keep serving stale matrix; never panic the service.\n                    }\n                }\n            }\n        });\n    }\n}\n\nasync fn load_matrix(pool: &amp;sqlx::PgPool) -&gt; anyhow::Result&lt;RoleMatrix&gt; {\n    let version: i32 = sqlx::query_scalar(\"SELECT version FROM role_catalogue_version WHERE id = 1\")\n        .fetch_one(pool).await?;\n    let rows: Vec&lt;(String, String, String)&gt; = sqlx::query_as(\n        \"SELECT role, resource, action FROM role_permissions ORDER BY role, resource, action\"\n    ).fetch_all(pool).await?;\n    let mut allowed = HashSet::new();\n    let mut hasher = Sha256::new();\n    for (r, res, act) in &amp;rows {\n        hasher.update(r.as_bytes()); hasher.update(b\"\\0\");\n        hasher.update(res.as_bytes()); hasher.update(b\"\\0\");\n        hasher.update(act.as_bytes()); hasher.update(b\"\\n\");\n        let role = Role::from_str(r)?;\n        let resource = Resource::from_str(res)?;\n        let action = Action::from_str(act)?;\n        allowed.insert((role, resource, action));\n    }\n    Ok(RoleMatrix { version: version as u32, allowed, hash: hasher.finalize().into() })\n}</code></pre>\n<h3>3.4 — Axum extractors</h3>\n<pre><code class=\"language-rust\">// services/auth/src/rbac/check.rs\nuse axum::{async_trait, extract::FromRequestParts, http::request::Parts, http::StatusCode};\nuse crate::rbac::catalogue::Role;\nuse crate::rbac::permissions::{Resource, Action};\nuse crate::jwt::Claims;\n\npub struct HasRole(pub Role);\n\n#[async_trait]\nimpl&lt;S: Send + Sync&gt; FromRequestParts&lt;S&gt; for HasRole {\n    type Rejection = (StatusCode, &amp;'static str);\n    async fn from_request_parts(parts: &amp;mut Parts, _state: &amp;S) -&gt; Result&lt;Self, Self::Rejection&gt; {\n        let claims: &amp;Claims = parts.extensions.get()\n            .ok_or((StatusCode::UNAUTHORIZED, \"no_claims\"))?;\n        // Read required role from request annotation (set by route handler).\n        let required: Role = parts.extensions.get::&lt;Role&gt;()\n            .copied().ok_or((StatusCode::INTERNAL_SERVER_ERROR, \"no_role_annotation\"))?;\n        if claims.roles().contains(&amp;required) {\n            Ok(HasRole(required))\n        } else {\n            metrics::counter!(\"auth_rbac_check_total\", \"outcome\" =&gt; \"deny\", \"role\" =&gt; required.as_str()).increment(1);\n            Err((StatusCode::FORBIDDEN, \"insufficient_role\"))\n        }\n    }\n}\n\npub struct HasPermission(pub Resource, pub Action);\n\n#[async_trait]\nimpl&lt;S: Send + Sync&gt; FromRequestParts&lt;S&gt; for HasPermission {\n    type Rejection = (StatusCode, &amp;'static str);\n    async fn from_request_parts(parts: &amp;mut Parts, _state: &amp;S) -&gt; Result&lt;Self, Self::Rejection&gt; {\n        let claims: &amp;Claims = parts.extensions.get().ok_or((StatusCode::UNAUTHORIZED, \"no_claims\"))?;\n        let matrix: &amp;Arc&lt;crate::rbac::matrix::RoleMatrix&gt; = parts.extensions.get().ok_or((StatusCode::INTERNAL_SERVER_ERROR, \"no_matrix\"))?;\n        let (res, act) = parts.extensions.get::&lt;(Resource, Action)&gt;().copied().ok_or((StatusCode::INTERNAL_SERVER_ERROR, \"no_perm_annotation\"))?;\n        let allowed = claims.roles().iter().any(|r| matrix.has_permission(*r, res, act));\n        if allowed { Ok(HasPermission(res, act)) }\n        else {\n            metrics::counter!(\"auth_rbac_check_total\", \"outcome\" =&gt; \"deny\", \"role\" =&gt; \"agg\").increment(1);\n            Err((StatusCode::FORBIDDEN, \"insufficient_permission\"))\n        }\n    }\n}</code></pre>\n<h3>3.5 — Migration 0005</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0005_roles_permissions.sql\n-- ADR: ADR-101\n\nBEGIN;\n\nCREATE TABLE roles (\n    name              TEXT PRIMARY KEY,\n    display           TEXT NOT NULL,\n    reserved          BOOLEAN NOT NULL,\n    requires_webauthn BOOLEAN NOT NULL,\n    scope_summary     TEXT NOT NULL,\n    lands_in_slice    INT NOT NULL\n);\n\nINSERT INTO roles (name, display, reserved, requires_webauthn, scope_summary, lands_in_slice) VALUES\n  ('root-admin',         'Root Admin',         TRUE,  FALSE, 'Cross-tenant superuser; CyberSkill operators only',    1),\n  ('tenant-admin',       'Tenant Admin',       FALSE, FALSE, 'Full admin within one tenant',                          1),\n  ('tenant-member',      'Tenant Member',      FALSE, FALSE, 'Regular member; read shareable+, write own scopes',     1),\n  ('service-account',    'Service Account',    FALSE, FALSE, 'Non-human identity; module-to-module mTLS',             1),\n  ('agent-persona',      'Agent Persona',      FALSE, FALSE, 'Persona-versioned agent (CUO + sub-skills)',            1),\n  ('founder',            'Founder',            FALSE, TRUE,  'Founder-CEO equivalent; WebAuthn required',             3),\n  ('cfo',                'CFO',                FALSE, FALSE, 'Financial read + disbursement + ESOP signoff',          4),\n  ('cto',                'CTO',                FALSE, FALSE, 'Tech-debt + security advisory + OBS digest target',     4),\n  ('coo',                'COO',                FALSE, FALSE, 'Cross-module status + blocker triage + process',        4),\n  ('chro',               'CHRO',               FALSE, FALSE, 'HR records + onboarding + perf review + PII-elevated', 4),\n  ('cmo',                'CMO',                FALSE, FALSE, 'Campaign briefs + content calendars + comms approval',  4),\n  ('cpo',                'CPO',                FALSE, FALSE, 'Product brief + roadmap + feature-request-author canonical',         4),\n  ('cso',                'CSO (Strategy)',     FALSE, FALSE, 'OKR cascade + scenarios + competitive intel read',      4),\n  ('cseco',              'CSO (Security)',     FALSE, FALSE, 'Security review + key rotation + vuln triage',          4),\n  ('clo',                'CLO',                FALSE, FALSE, 'Contract redline + DSAR triage + regulatory signoff',   4),\n  ('cdo',                'CDO',                FALSE, FALSE, 'Data quality + lineage + residency + memory owner',      4),\n  ('dpo',                'DPO',                FALSE, FALSE, 'DSAR fulfilment + breach notification + purge approval',4),\n  ('caio',               'CAIO',               FALSE, FALSE, 'AI Gateway budget + synthesis sub-skill review',        5),\n  ('client-portal-user', 'Client Portal User', TRUE,  FALSE, 'External tenant user (PORTAL filter only)',             5),\n  ('auditor',            'Auditor',            TRUE,  FALSE, 'External auditor; read-only, time-bounded, scope-pinned',5),\n  ('regulator',          'Regulator',          TRUE,  FALSE, 'External regulatory authority; DSAR + breach scopes',   5),\n  ('billing-system',     'Billing System',     TRUE,  FALSE, 'Stripe/VietQR/Momo webhook identity; write-restricted', 5);\n\nCREATE TABLE resources (\n    name   TEXT PRIMARY KEY,\n    module TEXT NOT NULL\n);\n-- 40 rows inserted here, one per Resource enum variant (omitted for brevity in spec).\n\nCREATE TABLE actions (name TEXT PRIMARY KEY);\nINSERT INTO actions (name) VALUES ('read'), ('write'), ('admin'), ('approve'), ('sign');\n\nCREATE TABLE role_permissions (\n    role     TEXT NOT NULL REFERENCES roles(name),\n    resource TEXT NOT NULL REFERENCES resources(name),\n    action   TEXT NOT NULL REFERENCES actions(name),\n    PRIMARY KEY (role, resource, action)\n);\n-- ~280 INSERT rows here, generated from the matrix design in ADR-101 §3.\n\nCREATE TABLE subject_roles (\n    tenant_id  UUID NOT NULL,\n    subject_id UUID NOT NULL,\n    role       TEXT NOT NULL REFERENCES roles(name),\n    granted_by UUID NOT NULL,\n    granted_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n    PRIMARY KEY (subject_id, role)\n);\nALTER TABLE subject_roles ENABLE ROW LEVEL SECURITY;\nCREATE POLICY subject_roles_tenant ON subject_roles\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE INSERT, UPDATE, DELETE ON role_permissions FROM cyberos_app;  -- runtime cannot mutate matrix\nREVOKE INSERT, UPDATE, DELETE ON roles FROM cyberos_app;             -- runtime cannot add roles\nREVOKE INSERT, UPDATE, DELETE ON resources FROM cyberos_app;\nREVOKE INSERT, UPDATE, DELETE ON actions FROM cyberos_app;\n\nCREATE TABLE scope_grants (\n    tenant_id   UUID NOT NULL,\n    subject_id  UUID NOT NULL,\n    resource    TEXT NOT NULL REFERENCES resources(name),\n    resource_id UUID NOT NULL,\n    action      TEXT NOT NULL REFERENCES actions(name),\n    granted_by  UUID NOT NULL,\n    granted_at  TIMESTAMPTZ NOT NULL DEFAULT now(),\n    expires_at  TIMESTAMPTZ,\n    PRIMARY KEY (subject_id, resource, resource_id, action)\n);\nALTER TABLE scope_grants ENABLE ROW LEVEL SECURITY;\nCREATE POLICY scope_grants_tenant ON scope_grants\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCREATE OR REPLACE FUNCTION auth.has_role(role_name TEXT) RETURNS BOOLEAN AS $$\nDECLARE\n    roles_csv TEXT;\nBEGIN\n    roles_csv := current_setting('auth.roles', true);\n    IF roles_csv IS NULL THEN RETURN FALSE; END IF;\n    RETURN role_name = ANY(string_to_array(roles_csv, ','));\nEND;\n$$ LANGUAGE plpgsql STABLE;\n\nCOMMIT;</code></pre>\n<h3>3.6 — Migration 0006 (catalogue version + trigger)</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0006_role_catalogue_version.sql\n-- ADR: ADR-101\n\nBEGIN;\n\nCREATE TABLE role_catalogue_version (\n    id         INT PRIMARY KEY CHECK (id = 1),\n    version    INT NOT NULL,\n    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n    adr_id     TEXT NOT NULL\n);\nINSERT INTO role_catalogue_version (id, version, adr_id) VALUES (1, 2, 'ADR-101');\n-- v1 was the implicit pre-FR-AUTH-101 catalogue (5 roles); v2 ships here.\n\nCREATE OR REPLACE FUNCTION bump_catalogue_version() RETURNS TRIGGER AS $$\nBEGIN\n    UPDATE role_catalogue_version SET version = version + 1, updated_at = now() WHERE id = 1;\n    -- memory audit row emission via memory_writer bridge (FR-AI-003) is handled at the migration commit hook.\n    RETURN NULL;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_bump_on_roles AFTER INSERT OR UPDATE OR DELETE ON roles\n    EXECUTE FUNCTION bump_catalogue_version();\nCREATE TRIGGER trg_bump_on_perms AFTER INSERT OR UPDATE OR DELETE ON role_permissions\n    EXECUTE FUNCTION bump_catalogue_version();\n\nREVOKE INSERT, UPDATE, DELETE ON role_catalogue_version FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.7 — REST handlers</h3>\n<pre><code class=\"language-rust\">// services/auth/src/admin/roles_rest.rs\nuse axum::{Json, extract::{Path, State}, http::StatusCode};\nuse serde::{Deserialize, Serialize};\nuse uuid::Uuid;\nuse crate::rbac::catalogue::Role;\nuse crate::rbac::matrix::RoleMatrixHandle;\nuse crate::audit::role_events;\n\n#[derive(Deserialize)]\npub struct AssignRoleRequest { pub role: String }\n\n#[derive(Serialize)]\npub struct AssignRoleResponse {\n    pub subject_id: Uuid,\n    pub role: String,\n    pub granted_by: Uuid,\n    pub granted_at: chrono::DateTime&lt;chrono::Utc&gt;,\n}\n\npub async fn assign_role(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,                              // injected by FR-AUTH-004 middleware\n    Path(subject_id): Path&lt;Uuid&gt;,\n    Json(req): Json&lt;AssignRoleRequest&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;AssignRoleResponse&gt;), ApiError&gt; {\n    // 1. Permission check.\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::RoleAssignment, Action::Admin)?;\n    // 2. Parse role string against closed enum.\n    let role: Role = req.role.parse().map_err(|_| ApiError::UnknownRole(req.role.clone()))?;\n    // 3. Refuse reserved-role assignment via this endpoint.\n    if role.is_reserved() { return Err(ApiError::ReservedRole(role)); }\n    // 4. Founder requires WebAuthn enrolment.\n    if role.requires_webauthn() {\n        let has_passkey = state.webauthn.has_factor(claims.tenant_id(), subject_id).await?;\n        if !has_passkey { return Err(ApiError::WebAuthnRequired(role)); }\n    }\n    // 5. Insert + audit-before-action in one transaction.\n    let mut tx = state.db.begin().await?;\n    sqlx::query(\"INSERT INTO subject_roles (tenant_id, subject_id, role, granted_by) VALUES ($1, $2, $3, $4)\")\n        .bind(claims.tenant_id()).bind(subject_id).bind(role.as_str()).bind(claims.subject_id())\n        .execute(&amp;mut *tx).await\n        .map_err(|e| {\n            if let Some(db_err) = e.as_database_error() {\n                if db_err.code().as_deref() == Some(\"23505\") { return ApiError::AlreadyGranted; }\n            }\n            ApiError::Db(e)\n        })?;\n    role_events::emit_role_assigned(&amp;mut tx, claims.tenant_id(), subject_id, role, claims.subject_id()).await?;\n    tx.commit().await?;\n    Ok((StatusCode::CREATED, Json(AssignRoleResponse { subject_id, role: role.as_str().to_string(), granted_by: claims.subject_id(), granted_at: chrono::Utc::now() })))\n}\n\npub async fn revoke_role(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Path((subject_id, role_str)): Path&lt;(Uuid, String)&gt;,\n) -&gt; Result&lt;StatusCode, ApiError&gt; {\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::RoleAssignment, Action::Admin)?;\n    let role: Role = role_str.parse().map_err(|_| ApiError::UnknownRole(role_str.clone()))?;\n    let mut tx = state.db.begin().await?;\n    let rows = sqlx::query(\"DELETE FROM subject_roles WHERE tenant_id = $1 AND subject_id = $2 AND role = $3\")\n        .bind(claims.tenant_id()).bind(subject_id).bind(role.as_str())\n        .execute(&amp;mut *tx).await?.rows_affected();\n    if rows &gt; 0 {\n        role_events::emit_role_revoked(&amp;mut tx, claims.tenant_id(), subject_id, role, claims.subject_id()).await?;\n    }\n    tx.commit().await?;\n    Ok(StatusCode::NO_CONTENT)\n}\n\npub async fn list_roles(State(state): State&lt;AppState&gt;) -&gt; Json&lt;RoleCatalogueResponse&gt; {\n    let matrix = state.matrix.snapshot();\n    let roles: Vec&lt;RoleCatalogueEntry&gt; = Role::ALL.iter().map(|r| RoleCatalogueEntry {\n        name: r.as_str().into(),\n        display: r.display_name().into(),\n        reserved: r.is_reserved(),\n        requires_webauthn: r.requires_webauthn(),\n        scope_summary: r.scope_summary().into(),\n    }).collect();\n    Json(RoleCatalogueResponse { version: matrix.version, roles })\n}</code></pre>\n<h3>3.8 — Updated JWT claims</h3>\n<pre><code class=\"language-rust\">// services/auth/src/jwt.rs (delta)\n#[derive(Debug, Serialize, Deserialize)]\npub struct Claims {\n    pub sub: Uuid,               // subject_id\n    pub tid: Uuid,               // tenant_id\n    pub iss: String,\n    pub iat: i64,\n    pub exp: i64,\n    pub nbf: i64,\n    pub roles: Vec&lt;String&gt;,      // NEW — kebab-case role names\n    pub rbac_v: u32,             // NEW — catalogue version at issuance\n}\n\nimpl Claims {\n    pub fn roles(&amp;self) -&gt; Vec&lt;Role&gt; {\n        // Per §1 #13: refuse tokens with unknown role names.\n        self.roles.iter().map(|s| s.parse::&lt;Role&gt;()).collect::&lt;Result&lt;_, _&gt;&gt;()\n            .expect(\"verifier upstream already rejected unknown-role tokens\")\n    }\n    pub fn rbac_v(&amp;self) -&gt; u32 { self.rbac_v }\n}</code></pre>\n<h3>3.9 — ADR file</h3>\n<pre><code class=\"language-markdown\">&lt;!-- services/auth/adr/ADR-101-rbac-22-role-catalogue.md --&gt;\n# ADR-101 — 22-role closed RBAC catalogue\n\n**Status:** Accepted\n**Date:** 2026-05-16\n**Deciders:** Stephen Cheng (CTO), DPO sign-off (pending), CSEC sign-off (pending)\n\n## Context\nCyberOS needs a role-based access control model that scales from the initial 1-tenant deployment (CyberSkill itself) to multi-tenant agency operation. Two design options: (a) ABAC (attribute-based), (b) RBAC with a closed catalogue.\n\n## Decision\nRBAC with exactly 22 closed roles. Permission matrix is `roles × resources × actions`. Roles are not extensible at runtime — schema-defined only. Adding role 23 requires a new ADR; same for resource 41 or action 6.\n\n## Consequences\n**Positive:** every access decision is auditable; permission matrix is enumerable; compliance auditors (ISO 27001 A.5.18) accept by construction.\n**Negative:** narrowing per-resource access requires the scope-grants layer (DEC-124); pure RBAC over-grants.\n**Mitigation:** scope-grants narrow within a tenant's matrix; never grant what the role doesn't already permit.\n\n## Scope-creep risk assessment\nEvery new role costs: 1 ADR + DPO review + CSEC review + matrix migration + regression test. The cost is deliberate; it forces the question \"does an existing role cover this?\" before adding a new one.\n\n## DPO sign-off\nPDPL Art. 6 (data minimisation) is satisfied: the JWT carries role names only, not the permission matrix. The matrix never leaves the AUTH service.\n\n## CSEC sign-off\nReserved-role assignment (root-admin, auditor, regulator, billing-system, client-portal-user) requires dedicated elevated endpoints, not the standard REST. Founder requires WebAuthn factor presence.</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Closed enum invariants</strong> — <code>Role::ALL.len() == 22</code> AND every variant has unique <code>as_str()</code> AND <code>from_str(as_str()) == Ok(variant)</code> for all 22.</li><li><strong>Unknown role rejection</strong> — <code>\"super-admin\".parse::&lt;Role&gt;()</code> returns <code>Err(RoleParseError::UnknownRole(\"super-admin\"))</code>.</li><li><strong>Reserved role classification</strong> — exactly 5 roles return <code>true</code> from <code>is_reserved()</code>: root-admin, client-portal-user, auditor, regulator, billing-system.</li><li><strong>WebAuthn-required classification</strong> — exactly 1 role (<code>founder</code>) returns <code>true</code> from <code>requires_webauthn()</code>.</li><li><strong>Resource closed enum</strong> — <code>Resource::ALL.len() == 40</code> AND parse round-trip.</li><li><strong>Action closed enum</strong> — <code>Action::ALL.len() == 5</code> AND parse round-trip.</li><li><strong>Matrix seeded</strong> — <code>SELECT count(*) FROM role_permissions</code> returns the expected row count (matches ADR-101 §3 enumeration; assertion in migration test).</li><li><strong>Matrix in-memory load</strong> — service start loads matrix; <code>RoleMatrix::has_permission(TenantAdmin, Subject, Admin)</code> returns true.</li><li><strong>Matrix refresher hash-only-swap</strong> — running the refresher twice on unchanged matrix yields <code>auth_rbac_matrix_refresh_total{outcome=hash_unchanged}</code> increment 1, not a fresh allocation.</li><li><strong>POST role happy path</strong> — tenant-admin caller, valid target subject, valid role string → 201 with response shape; row appears in <code>subject_roles</code>; <code>auth.role_assigned</code> audit row emitted.</li><li><strong>POST role unknown</strong> — <code>{\"role\":\"super-admin\"}</code> → 400 with <code>{\"error\":\"unknown_role\"}</code> body.</li><li><strong>POST role reserved</strong> — <code>{\"role\":\"root-admin\"}</code> → 403 with <code>{\"error\":\"reserved_role\"}</code>.</li><li><strong>POST founder without passkey</strong> — target subject has no WebAuthn factor; <code>{\"role\":\"founder\"}</code> → 409 with <code>{\"error\":\"webauthn_required\"}</code>.</li><li><strong>POST role idempotent</strong> — re-POST same (subject_id, role) → 409 <code>{\"error\":\"already_granted\"}</code> (PK enforces).</li><li><strong>DELETE role happy</strong> — existing grant → 204; row removed; <code>auth.role_revoked</code> audit row emitted.</li><li><strong>DELETE role absent</strong> — non-existent grant → 204 (idempotent; matches REST DELETE semantics).</li><li><strong>GET /v1/admin/roles</strong> — returns version + 22 entries with stable order; ETag is <code>W/\"rbac-v&lt;n&gt;\"</code>.</li><li><strong>JWT carries roles + rbac_v</strong> — newly issued token (after FR-AUTH-101 ships) has both claims populated; older tokens (pre-FR-AUTH-101) lack <code>rbac_v</code> and are accepted during grace window.</li><li><strong>JWT unknown-role rejection</strong> — verifier given a token with <code>roles: [\"super-admin\"]</code> returns 401 <code>{\"error\":\"invalid_token\",\"reason\":\"unknown_role_in_claim\"}</code>.</li><li><strong>RLS consults role</strong> — query as a <code>tenant-member</code> subject against <code>audit_row</code> returns 0 rows; same query as <code>tenant-admin</code> returns all tenant-scoped rows. (<code>auth.has_role</code> SQL function in policy.)</li><li><strong>Stub compatibility</strong> — every (stub_role, resource, action) tuple from the pre-FR-AUTH-101 matrix is present in the new matrix; no removed tuples (additive only).</li><li><strong>ADR gate</strong> — migration adding a 23rd role without ADR-NNN comment → <code>cargo test rbac_adr_gate_test</code> fails.</li><li><strong>Catalogue-changed audit</strong> — INSERT into <code>roles</code> (via test migration with ADR) bumps <code>role_catalogue_version.version</code>; emits exactly one <code>auth.role_catalogue_changed</code> memory audit row with old/new version + adr_id.</li><li><strong>Scope-grant narrowing</strong> — <code>cfo</code> role + scope-grant <code>(inv_invoice, invoice-A, Read, expires=future)</code> → caller may read invoice-A; revoke grant → 403 on next request after 60s cache TTL.</li><li><strong>Perf budget</strong> — <code>rbac_perf_test</code>: 1k checks against 22-role × 40-resource × 5-action matrix complete in &lt; 50 µs p99 (in-process, no network).</li><li><strong>OTel span emission</strong> — every check emits <code>auth.rbac_check</code> span with <code>outcome</code> attribute; deny + error outcomes always sampled.</li><li><strong>OTel metrics emission</strong> — <code>auth_rbac_check_total</code>, <code>auth_rbac_check_latency_us</code>, <code>auth_rbac_matrix_refresh_total</code>, <code>auth_rbac_subject_role_count</code>, <code>auth_rbac_catalogue_version</code> all observable from <code>/metrics</code> endpoint.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/rbac_catalogue_test.rs\nuse cyberos_auth::rbac::catalogue::{Role, RoleParseError};\nuse std::collections::HashSet;\n\n#[test]\nfn all_22_roles_present() {\n    assert_eq!(Role::ALL.len(), 22);\n    let names: HashSet&lt;&amp;str&gt; = Role::ALL.iter().map(|r| r.as_str()).collect();\n    assert_eq!(names.len(), 22, \"duplicate role names\");\n}\n\n#[test]\nfn roundtrip_for_every_role() {\n    for r in Role::ALL {\n        let parsed: Role = r.as_str().parse().unwrap();\n        assert_eq!(*r, parsed);\n    }\n}\n\n#[test]\nfn unknown_role_rejected() {\n    let err = \"super-admin\".parse::&lt;Role&gt;().unwrap_err();\n    assert!(matches!(err, RoleParseError::UnknownRole(s) if s == \"super-admin\"));\n}\n\n#[test]\nfn reserved_roles_exact_set() {\n    let reserved: HashSet&lt;Role&gt; = Role::ALL.iter().copied().filter(|r| r.is_reserved()).collect();\n    let expected: HashSet&lt;Role&gt; = [Role::RootAdmin, Role::ClientPortalUser, Role::Auditor, Role::Regulator, Role::BillingSystem].into_iter().collect();\n    assert_eq!(reserved, expected);\n}\n\n#[test]\nfn webauthn_required_exactly_founder() {\n    let required: Vec&lt;Role&gt; = Role::ALL.iter().copied().filter(|r| r.requires_webauthn()).collect();\n    assert_eq!(required, vec![Role::Founder]);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rbac_adr_gate_test.rs (excerpt)\n#[tokio::test]\nasync fn assign_unknown_role_rejected() {\n    let ctx = TestCtx::new_with_seed(\"tenant-admin\").await;\n    let resp = ctx.post(&amp;format!(\"/v1/admin/subjects/{}/roles\", target_id), json!({\"role\":\"super-admin\"})).await;\n    assert_eq!(resp.status(), 400);\n    let body: serde_json::Value = resp.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"unknown_role\");\n    assert_eq!(body[\"role\"], \"super-admin\");\n    assert!(body[\"allowed\"].as_array().unwrap().len() == 22);\n}\n\n#[tokio::test]\nasync fn assign_reserved_role_rejected() {\n    let ctx = TestCtx::new_with_seed(\"tenant-admin\").await;\n    let resp = ctx.post(&amp;format!(\"/v1/admin/subjects/{}/roles\", target_id), json!({\"role\":\"root-admin\"})).await;\n    assert_eq!(resp.status(), 403);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"error\"], \"reserved_role\");\n}\n\n#[tokio::test]\nasync fn assign_founder_without_passkey_rejected() {\n    let ctx = TestCtx::new_with_seed(\"tenant-admin\").await;\n    let target_id = ctx.create_subject_without_passkey().await;\n    let resp = ctx.post(&amp;format!(\"/v1/admin/subjects/{}/roles\", target_id), json!({\"role\":\"founder\"})).await;\n    assert_eq!(resp.status(), 409);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"error\"], \"webauthn_required\");\n}\n\n#[tokio::test]\nasync fn assign_role_emits_audit_row() {\n    let ctx = TestCtx::new_with_seed(\"tenant-admin\").await;\n    let target_id = ctx.create_subject().await;\n    let resp = ctx.post(&amp;format!(\"/v1/admin/subjects/{}/roles\", target_id), json!({\"role\":\"cfo\"})).await;\n    assert_eq!(resp.status(), 201);\n    let rows = ctx.memory_audit_rows(\"auth.role_assigned\").await;\n    assert_eq!(rows.len(), 1);\n    assert_eq!(rows[0][\"subject_id\"], target_id.to_string());\n    assert_eq!(rows[0][\"role\"], \"chief-financial-officer\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rbac_adr_gate_test.rs\n#[test]\nfn matrix_check_under_50us_p99() {\n    let matrix = synthetic_matrix(22, 40, 5);\n    let mut times: Vec&lt;u64&gt; = Vec::with_capacity(10_000);\n    let start = std::time::Instant::now();\n    for _ in 0..10_000 {\n        let t0 = std::time::Instant::now();\n        std::hint::black_box(matrix.has_permission(Role::Cfo, Resource::InvInvoice, Action::Read));\n        times.push(t0.elapsed().as_nanos() as u64);\n    }\n    let elapsed = start.elapsed();\n    times.sort_unstable();\n    let p99_us = (times[(times.len() * 99) / 100] as f64) / 1000.0;\n    eprintln!(\"matrix check p99 = {p99_us:.3} µs (10k iters, total {elapsed:?})\");\n    assert!(p99_us &lt; 50.0, \"p99 = {p99_us}µs exceeds 50µs budget\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rbac_adr_gate_test.rs\n#[test]\nfn every_role_touching_migration_has_adr_reference() {\n    let walker = crate::rbac::adr::AdrGate::new(\"services/auth/migrations\", \"services/auth/adr\");\n    let report = walker.scan().expect(\"scan\");\n    assert!(report.violations.is_empty(),\n        \"migrations touch roles/role_permissions without ADR reference: {:#?}\", report.violations);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rls_isolation_test.rs\n#[tokio::test]\nasync fn stub_token_accepted_during_grace_window() {\n    let ctx = TestCtx::new().await;\n    let stub_token = ctx.issue_stub_token(/* no rbac_v claim */).await;\n    let resp = ctx.get(\"/v1/admin/roles\").header(\"Authorization\", format!(\"Bearer {stub_token}\")).await;\n    assert_eq!(resp.status(), 200);\n}\n\n#[tokio::test]\nasync fn stub_token_rejected_after_grace_window() {\n    let ctx = TestCtx::new_with_clock_skew_days(31).await;  // simulate post-grace\n    let stub_token = ctx.issue_stub_token().await;\n    let resp = ctx.get(\"/v1/admin/roles\").header(\"Authorization\", format!(\"Bearer {stub_token}\")).await;\n    assert_eq!(resp.status(), 401);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"reason\"], \"rbac_version_required\");\n}\n\n#[tokio::test]\nasync fn stub_role_permissions_strictly_additive() {\n    let pre_matrix = load_pre_fr_auth_101_matrix();\n    let post_matrix = load_matrix_after_migrations();\n    for (role, res, act) in &amp;pre_matrix.allowed {\n        assert!(post_matrix.allowed.contains(&amp;(*role, *res, *act)),\n            \"FR-AUTH-101 removed pre-existing tuple ({role:?}, {res:?}, {act:?})\");\n    }\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton — Resource enum's full 40-variant body is filled in during implementation; ADR-101 §3 enumerates the matrix rows that the migration seeds.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (this FR depends on):</strong></p>\n<ul><li><strong>FR-AUTH-005</strong> — admin REST router; this FR adds <code>/v1/admin/subjects/{id}/roles</code> and <code>/v1/admin/roles</code> to it.</li><li><strong>FR-AUTH-004</strong> — JWT issuance; this FR extends the claim shape.</li><li><strong>FR-AUTH-003</strong> — RLS enforcement; this FR adds role-aware policies on sensitive tables.</li><li><strong>FR-AI-003</strong> — memory-audit bridge; receives <code>auth.role_assigned / .role_revoked / .role_catalogue_changed</code> rows.</li></ul>\n<p><strong>Downstream (this FR blocks):</strong></p>\n<ul><li><strong>FR-AUTH-108</strong> — Lumi tenant-identity JWT shape (needs agent-persona role in production catalogue).</li><li><strong>FR-AUTH-109</strong> — stub→full migration enforcer (this FR ships the matrix it migrates to).</li><li><strong>FR-CRM-001, FR-DOC-001, FR-HR-001, FR-KB-001, FR-OKR-001, FR-REW-001</strong> — all need specialist roles (cfo, dpo, chro, founder).</li></ul>\n<p><strong>Cross-module (informational):</strong></p>\n<ul><li><strong>FR-AUTH-105 (placeholder)</strong> — passkey enrolment. The founder-WebAuthn gate (§1 #12) calls this FR's <code>webauthn::has_factor</code>. Until FR-AUTH-105 ships, the gate returns <code>false</code> for all subjects (founder assignment effectively disabled).</li><li><strong>FR-AUTH-111 (placeholder)</strong> — per-tenant CRL flush for instant revocation. Until FR-AUTH-111 ships, revocations are honoured at 60s latency.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/admin/subjects/{id}/roles request</h3>\n<pre><code class=\"language-json\">{\n  \"role\": \"cfo\"\n}</code></pre>\n<h3>8.2 — 201 CREATED response</h3>\n<pre><code class=\"language-json\">{\n  \"subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"role\": \"chief-financial-officer\",\n  \"granted_by\": \"8a7c8c80-1234-4567-89ab-cdef01234567\",\n  \"granted_at\": \"2026-05-16T14:32:11Z\"\n}</code></pre>\n<h3>8.3 — auth.role_assigned memory audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.role_assigned\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"role\": \"chief-financial-officer\",\n  \"granted_by\": \"8a7c8c80-1234-4567-89ab-cdef01234567\",\n  \"rbac_v\": 2,\n  \"trace_id\": \"4bf92f3577b34da6a3ce929d0e0e4736\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — GET /v1/admin/roles response (truncated)</h3>\n<pre><code class=\"language-json\">{\n  \"version\": 2,\n  \"roles\": [\n    {\"name\":\"root-admin\",\"display\":\"Root Admin\",\"reserved\":true,\"requires_webauthn\":false,\"scope_summary\":\"Cross-tenant superuser; CyberSkill operators only\"},\n    {\"name\":\"tenant-admin\",\"display\":\"Tenant Admin\",\"reserved\":false,\"requires_webauthn\":false,\"scope_summary\":\"Full admin within one tenant\"},\n    {\"name\":\"founder\",\"display\":\"Founder\",\"reserved\":false,\"requires_webauthn\":true,\"scope_summary\":\"Founder-CEO equivalent; WebAuthn required\"}\n  ]\n}</code></pre>\n<h3>8.5 — JWT claims (post-FR-AUTH-101)</h3>\n<pre><code class=\"language-json\">{\n  \"sub\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"tid\": \"5e8f1d2a-...\",\n  \"iss\": \"https://auth.cyberskill.world\",\n  \"iat\": 1747920731,\n  \"exp\": 1747924331,\n  \"nbf\": 1747920731,\n  \"roles\": [\"tenant-member\", \"chief-financial-officer\"],\n  \"rbac_v\": 2\n}</code></pre>\n<h3>8.6 — auth.role_catalogue_changed memory audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.role_catalogue_changed\",\n  \"old_version\": 2,\n  \"new_version\": 3,\n  \"migration_id\": \"0007_add_role_caso.sql\",\n  \"adr_id\": \"ADR-115\",\n  \"changed_at\": \"2026-08-01T10:00:00Z\",\n  \"ts_ns\": 1754049600000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Per-tenant CRL flush endpoint</strong> for sub-60s revocation latency — deferred to FR-AUTH-111 (slice 2).</li><li><strong>Reserved-role assignment endpoints</strong> — root-admin assignment via cyberos-ten bootstrap CLI (FR-TEN-001); auditor/regulator/billing-system via dedicated intake flow; client-portal-user via PORTAL JIT provisioning (FR-PORTAL-003). Each of those FRs documents its own reserved-role grant path.</li><li><strong>Argon2id migration</strong> — FR-AUTH-114 (slice 4); orthogonal to RBAC.</li><li><strong>Scope-grant TTL enforcement</strong> — slice 2; current spec allows <code>expires_at</code> but does not enforce expiry cleanup (relies on the <code>has_permission</code> checker to ignore expired rows).</li><li><strong>Role-impact diff in PR review</strong> — a tool that surfaces \"this migration changes which roles can do X\" — slice 3; would be a nice-to-have for ADR reviewers.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>Role::ALL</code> length drifts from 22 (developer adds variant without updating ALL)</td><td><code>rbac_catalogue_test::all_22_roles_present</code></td><td>CI fails before merge</td><td>Add the variant to <code>ALL</code> array</td></tr><tr><td>Two roles share the same <code>as_str()</code></td><td><code>rbac_catalogue_test::all_22_roles_present</code> (HashSet len check)</td><td>CI fails</td><td>Fix duplicate string in <code>as_str()</code> match</td></tr><tr><td>Migration 0005 row count drifts</td><td><code>rbac_permission_matrix_test</code> (asserts seeded row count == ADR-101 expectation)</td><td>CI fails</td><td>Either fix migration OR update ADR + test together</td></tr><tr><td>Matrix loader hits malformed <code>role_permissions</code> row (NULL role)</td><td><code>load_matrix</code> returns Err; service stays on prior snapshot; <code>auth_rbac_matrix_refresh_total{outcome=db_unreachable}</code> increments</td><td>Service serves stale matrix; OBS alarm at 5min sustained</td><td>Operator inspects DB; rolls back bad migration</td></tr><tr><td>RoleMatrix refresher panics</td><td>Tokio task supervisor restarts; OBS alarm sev-2 on panic count &gt; 0</td><td>Brief refresh gap (60s); checks still served from stale snapshot</td><td>Investigate panic via tracing; ship patch</td></tr><tr><td>JWT contains <code>roles: [\"super-admin\"]</code> (tampered token)</td><td><code>Claims::roles()</code> parse fails at verifier</td><td>401 <code>unknown_role_in_claim</code>; OBS sev-3 if pattern repeats</td><td>Token is invalid; user re-auths</td></tr><tr><td>JWT missing <code>rbac_v</code> after grace window</td><td>Verifier check against clock</td><td>401 <code>rbac_version_required</code></td><td>User re-auths and gets a fresh token with <code>rbac_v</code></td></tr><tr><td>JWT carries <code>rbac_v: 1</code> but live version is 4</td><td>Verifier compares; &gt; 2 stale → reject</td><td>401 <code>rbac_version_stale</code></td><td>Refresh-token flow issues fresh access token</td></tr><tr><td>POST role with body <code>{\"role\":\"super-admin\"}</code></td><td>Handler parse</td><td>400 <code>unknown_role</code> with <code>allowed</code> array of 22</td><td>Caller picks valid role</td></tr><tr><td>POST role for reserved role (<code>root-admin</code>)</td><td>Handler <code>is_reserved()</code> check</td><td>403 <code>reserved_role</code></td><td>Caller routes to dedicated reserved-role endpoint</td></tr><tr><td>POST founder for subject without passkey</td><td>Handler <code>webauthn::has_factor</code> check</td><td>409 <code>webauthn_required</code></td><td>Enrol passkey first (FR-AUTH-105)</td></tr><tr><td>POST role duplicate (race-condition double-click)</td><td>Postgres PK violation (23505)</td><td>409 <code>already_granted</code> (idempotent on subject_id+role PK)</td><td>No action; request is no-op</td></tr><tr><td>DELETE absent role</td><td>Handler returns 204 anyway</td><td>204 NO CONTENT (REST idempotency)</td><td>No action</td></tr><tr><td>Audit row emission fails inside grant tx</td><td>Postgres transaction rolls back</td><td>500 <code>audit_failed</code>; subject_roles INSERT also reverts</td><td>Operator inspects memory_writer health</td></tr><tr><td>Matrix <code>auth.has_role</code> SQL function evaluates wrong (RLS bypass risk)</td><td>Negative test <code>rls_audit_row_member_zero_rows</code></td><td>Test fails</td><td>Fix function or policy</td></tr><tr><td>Two services see different matrix versions briefly</td><td>Expected — 60s eventual consistency</td><td><code>rbac_v</code> claim catches stale token verification</td><td>Refresher catches up; OTel <code>auth_rbac_catalogue_version</code> gauge shows version per service</td></tr><tr><td>Migration adds 23rd role with no ADR file</td><td><code>rbac_adr_gate_test</code> CI gate</td><td>Build fails</td><td>Either write ADR or revert migration</td></tr><tr><td>ADR file referenced but doesn't exist</td><td><code>rbac_adr_gate_test</code> checks path exists</td><td>Build fails</td><td>Create ADR or fix path</td></tr><tr><td>Catalogue version bump trigger fails silently</td><td>Test <code>rbac_catalogue_version_bump</code> asserts increment</td><td>CI fails before merge</td><td>Investigate trigger function body</td></tr><tr><td>Scope-grant points at deleted resource (<code>resource_id</code> no longer exists)</td><td>Periodic FK-check job (out of scope here)</td><td>Stale grant is no-op (resource not found at access time)</td><td>Future cleanup job in slice 2</td></tr><tr><td>Subject holds 100+ roles (pathological)</td><td>Test <code>rbac_subject_role_count_gauge</code> warns at &gt; 10</td><td>OBS sev-4; not blocking</td><td>Review with tenant-admin; revoke unnecessary</td></tr><tr><td>RLS policy on <code>subjects</code> denies tenant-admin (regression)</td><td><code>rbac_rls_integration_test</code></td><td>Test fails</td><td>Fix policy USING clause</td></tr><tr><td>Bcrypt path changed (FR-AUTH-114 lands) and role gate breaks for founder</td><td>Test <code>rbac_founder_webauthn_gate_test</code> after bcrypt swap</td><td>CI fails</td><td>Coordinate FR-AUTH-114 + FR-AUTH-101 changes</td></tr><tr><td>60s refresher leaks task on shutdown</td><td>Service shutdown handler cancels token</td><td>Tokio drop drains</td><td>None (defensive)</td></tr><tr><td>In-memory matrix grows unbounded (large tenant)</td><td>Matrix size = roles × resources × actions = 4400 rows max</td><td>Bounded by closed enums</td><td>None — bound by design</td></tr><tr><td>Subject's role-cache out of sync briefly after grant</td><td>Expected 60s window; OTel <code>auth_rbac_subject_role_count</code> gauge</td><td>Caller waits one refresh cycle</td><td>Acceptable per DEC-126</td></tr><tr><td>Concurrent grant + revoke on same (subject_id, role)</td><td>Postgres serialisation</td><td>Either grant-then-revoke (clean) OR revoke-on-nothing (204)</td><td>None — serialisable transactions handle it</td></tr><tr><td>Audit row chain head drift across services</td><td>Per-row <code>prev_chain</code> validates at memory_writer</td><td>Bridge rejects out-of-order rows</td><td>Operator runs <code>cyberos doctor --repair</code></td></tr><tr><td>Test fixtures call <code>Role::Other(s)</code></td><td>Compile error (variant doesn't exist)</td><td>Build fails</td><td>Fix test to use valid Role</td></tr><tr><td>RBAC service started without role-catalogue table</td><td>Migration not applied</td><td>Service refuses to start (config validation)</td><td>Apply migrations</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>The closed enum is the design</strong>: introducing <code>Role::Custom(String)</code> or <code>Resource::Other(String)</code> would silently kill the catalogue's value. Any temptation to add it should route through ADR-101 instead — the answer should be \"no\" until ABAC is unavoidable, which the design assertion says it never is.</li><li><strong>40 resources / 5 actions / ~280 matrix rows</strong>: the row count is bounded by <code>roles × resources × actions = 4400</code>. Most matrix cells are denied (no row). The seeded set covers documented access patterns from website docs §2.6 + ADR-101 §3.</li><li><strong><code>arc_swap</code> for the matrix snapshot</strong>: lock-free read path; writers swap a fresh <code>Arc&lt;RoleMatrix&gt;</code> after refresh. Readers see a consistent snapshot for the duration of their request. The 60-second swap is the only mutation; serialised through the tokio task.</li><li><strong><code>sha2::Sha256</code> for matrix hash</strong>: deterministic over a sorted query (<code>ORDER BY role, resource, action</code>); hash unchanged → skip swap allocation. Avoids 4 KB allocations per refresh when nothing changed (typical case).</li><li><strong><code>auth.has_role()</code> SQL function reads session GUC</strong>: the JWT middleware sets <code>auth.roles</code> GUC on connection acquisition via <code>SET LOCAL auth.roles = $1</code>. RLS policies invoke <code>auth.has_role('tenant-admin')</code>. The GUC is per-transaction; never leaks across connections.</li><li><strong><code>rbac_v</code> design rationale</strong>: comparing token-issued version vs live version is a soft-reject (2-version tolerance) rather than a hard match. Strict-match would force re-auth every time any role's matrix tweaked; tolerance preserves UX while catching meaningful drift.</li><li><strong>Founder-WebAuthn check is intrinsic to the role</strong>: not \"configured as required\" — preventing operator from accidentally turning the gate off via env var. The cost is one extra match arm in <code>requires_webauthn()</code>; the safety is \"this is the kind of mistake that won't happen.\"</li><li><strong>Reserved-role routing</strong>: standard endpoint refuses; dedicated endpoints (out of scope) handle reserved-role assignment with their own elevated-privilege gates. Until those FRs ship, reserved roles can only be assigned via direct SQL (operator action); the memory audit row carries <code>granted_by</code> to attribute the action.</li><li><strong>30-day grace window</strong>: longer than 7-day refresh-token max (FR-AUTH-004) plus 23-day safety margin. After grace, missing-claim tokens are rejected and the rejection metric tells ops whether to push a re-auth notice.</li><li><strong>Matrix-version mismatch tolerance</strong> (2 versions): tolerates one missed refresh cycle (60s × 2 minutes) on a single auth-server instance during catalogue change; rejects tokens issued 3+ versions ago (typically &gt; 1 day stale).</li><li><strong>ADR validator scope</strong>: only checks migrations that <em>modify</em> <code>roles</code> or <code>role_permissions</code> tables; pure DDL on unrelated tables doesn't require ADR. Detection regex: <code>(INSERT|UPDATE|DELETE).*(roles|role_permissions)</code>. Bypasses include schema-only changes (column comments, etc.) — see <code>services/auth/src/rbac/adr.rs</code> heuristic.</li><li><strong><code>subject_roles</code> PK on <code>(subject_id, role)</code> not <code>(tenant_id, subject_id, role)</code></strong>: subject_id is globally unique (UUIDv4); same subject_id appearing in two tenants would be a deeper invariant violation (subject_id is tenant-scoped in subjects table). The (subject_id, role) PK is sufficient and avoids the redundant tenant_id column in the PK.</li><li><strong>Scope-grant intersection rule</strong>: role check passes OR scope-grant covers. Never AND. Rationale: scope-grants are <em>narrowing for restricted subjects</em> (e.g. external auditor) and <em>expanding for low-role subjects</em> (e.g. tenant-member with one-off invoice access). Neither pattern requires AND semantics.</li><li><strong>The matrix migration is data, not schema</strong>: future role additions are pure INSERT migrations + ADR-NNN.md. Schema changes (adding a resource enum variant) are rare and ADR-gated.</li><li><strong><code>auth_rbac_subject_role_count</code> gauge</strong>: high cardinality on tenant_id label; aggregate by tenant_id only (no subject_id label). The gauge is for capacity planning, not per-subject debugging.</li><li><strong>PostgreSQL <code>current_setting('auth.roles', true)</code> second arg</strong>: <code>true</code> means \"return NULL if not set\" instead of erroring. The SQL function handles NULL with an early FALSE return — preventing accidental role-check passes from un-initialised connections.</li><li><strong>JWT claim ordering</strong>: <code>roles</code> array order is insertion order (DB returns sorted by <code>(subject_id, role)</code> ASC). Verifiers MUST NOT depend on ordering — they should treat <code>roles</code> as a set. The <code>Claims::roles()</code> helper returns <code>Vec&lt;Role&gt;</code> for ergonomics but downstream uses <code>.contains()</code>.</li><li><strong>ADR-101 itself is shipped in this FR</strong>: per feature-request-audit skill the spec must be self-contained; the ADR is part of the spec's deliverables, not a separately negotiable artifact.</li><li><strong>CHANGELOG note</strong>: this FR closes the 17-role gap referenced in <code>CHANGELOG.md</code> line 338 (\"the remaining 17 land across slices 3–5\"); after this FR, all 22 are seeded — the slice number per role only documents intent for SCOPE-CREEP audits, not gating.</li></ul>\n<hr>\n<p><em>End of FR-AUTH-101.</em></p>";
