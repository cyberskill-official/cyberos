window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DOCS-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CyberOS documentation build pipeline <strong>MUST</strong> server-render the three reference catalog pages at build time, producing static HTML rows visible at first paint AND indexed by Pagefind. Each piece:</p>\n<ol><li><strong>MUST</strong> parse the data arrays (<code>NFR_DATA</code>, <code>RISKS</code>, <code>TASK_CATALOG</code>) at build time. Source-of-truth becomes JSON files at <code>website/build/data/{nfrs,risks,frs}.json</code> (DEC-221); both the build script AND the Alpine reactive layer consume from JSON.</li><li><strong>MUST</strong> emit a <code>&lt;section data-prerendered=\"true\"&gt;</code> block per page, containing the same <code>&lt;article&gt;</code> cards Alpine would render at runtime. Prerendered cards include all card content (title, IDs, descriptions, badges) — visible without JS.</li><li><strong>MUST</strong> be deterministic — running the build twice on the same JSON inputs produces byte-identical HTML output. No Date.now(), no RNG, no unsorted iteration. CI gate (§1 #11) enforces.</li><li><strong>MUST</strong> preserve Alpine reactive behaviour: on JS hydration, <code>hydrated=true</code> is set; CSS <code>[x-cloak] { display: none }</code> hides prerendered AND Alpine takes over. No double-render flash.</li><li><strong>MUST</strong> preserve filter/sort UX from current Alpine implementation. Post-hydrate clicks still filter/sort the (now-Alpine-rendered) cards.</li><li><strong>MUST</strong> include all metadata fields per card type:</li></ol>\n<ul><li><strong>NFR</strong>: id, category, target, phase, modules, references, description.</li><li><strong>Risk</strong>: id, severity, likelihood, mitigation, owner, status.</li><li><strong>task</strong>: id, module, priority, status, slice, owner, depends_on, blocks.</li></ul>\n<ol><li><strong>MUST</strong> generate stable IDs/anchors so external links (e.g., <code>https://docs.cyberos.world/reference/nfr-catalog.html#NFR-PERF-01</code>) work. Each card has <code>id=\"&lt;NFR-ID&gt;\"</code> attribute.</li><li><strong>MUST</strong> integrate with Cloudflare Pages build (or other CI). The build script runs as a step BEFORE deploy; failure aborts deploy.</li><li><strong>MUST</strong> be Pagefind-indexable — re-running <code>pagefind --site docs/</code> after prerender produces an index that returns hits for <code>NFR-PERF-01</code>, <code>RSK-09</code>, <code>TASK-AI-001</code> queries.</li><li><strong>MUST</strong> support both build-time AND watch-mode runs:</li></ol>\n<ul><li><code>node build/render-reference-pages.mjs</code> (one-shot).</li><li><code>node build/render-reference-pages.mjs --watch</code> (re-renders on JSON change; for local dev).</li></ul>\n<ol><li><strong>MUST</strong> be CI-gated by <code>docs-prerender-gate.yml</code>: on every PR touching <code>website/**</code>, the workflow runs the build + asserts the output matches the committed HTML (drift = build fail). Prevents the JSON-vs-HTML drift class.</li><li><strong>SHOULD</strong> emit a build report at <code>website/build/last-build-report.json</code> with stats: number of NFRs/RSKs/tasks rendered, build duration, output bytes per page. Visible in CI logs for operator review.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p>Per research review §4 #1: \"Tables render client-side; first paint shows empty scaffold with 'of NFRs match current filters' placeholder text.\" Per §5.4: \"task catalog is empty. NFR catalog renders empty without JS. Risk register relies on client-side rendering.\"</p>\n<p>The current architecture is a hard procurement and accessibility failure:</p>\n<ul><li>An auditor running a Pagefind query for <code>NFR-PERF-01</code> gets zero hits even though the NFR exists in the page's JS array.</li><li>A regulator reading the Risk Register page in screenshot mode sees an empty table.</li><li>A crawler indexing the page extracts no data because everything is loaded by JS.</li></ul>\n<p>The fix is <strong>prerendering at build time</strong> so the static HTML and the dynamic UX coexist.</p>\n<p><strong>Why JSON as source of truth (DEC-221)?</strong> Currently the data arrays are embedded as inline JavaScript in the HTML. To prerender, the build script needs the data — extracting from inline JS is fragile. Moving to JSON files = one parse, two consumers (build script + Alpine), no ambiguity.</p>\n<p><strong>Why deterministic build (DEC-220)?</strong> Without determinism, two CI runs on the same input produce different outputs — diff drift, false-positive failures. Stable sort + no time-dependent values + ordered iteration ensures determinism.</p>\n<p><strong>Why coexistence not replacement (§1 #4)?</strong> Alpine UX (filter, sort, hover effects) works post-hydrate. Replacing entirely would lose those interactions. Prerender for first-paint + crawlers; Alpine for interactivity. CSS <code>x-cloak</code> ensures no flash.</p>\n<p><strong>Why CI gate on drift (§1 #11)?</strong> Operators editing JSON without re-running the build = JSON-HTML drift. The CI gate runs the build + diffs; drift fails the build, forcing the operator to commit the regenerated HTML.</p>\n<p><strong>Why Pagefind re-index step (§1 #9)?</strong> Static index on stale HTML returns wrong results. Re-running <code>pagefind --site docs/</code> after prerender ensures the index reflects the current data.</p>\n<p><strong>Why &lt;noscript&gt; would fail (rejection rationale)?</strong> Pagefind doesn't index <code>&lt;noscript&gt;</code> content. Browsers with JS see duplicate content. Better to prerender + hide on hydrate.</p>\n<p><strong>Why per-card IDs/anchors (§1 #7)?</strong> Cross-reference URLs (<code>#NFR-PERF-01</code>) work today only because Alpine generates them. Prerender preserves them at first paint, so deep links work for crawlers + screenshot tooling.</p>\n<p><strong>Why watch mode (§1 #10)?</strong> Local dev iteration needs fast feedback. Watch mode re-renders on JSON change; eliminates manual rebuild step.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Build script</h3>\n<pre><code class=\"language-javascript\">// website/build/render-reference-pages.mjs\nimport * as cheerio from 'cheerio';\nimport Handlebars from 'handlebars';\nimport { readFileSync, writeFileSync, watch } from 'fs';\nimport path from 'path';\n\nconst PAGES = [\n  { html: 'docs/reference/nfr-catalog.html', data: 'build/data/nfrs.json',  template: 'build/templates/nfr-catalog.hbs', selector: '#nfr-list' },\n  { html: 'docs/reference/risk-register.html', data: 'build/data/risks.json', template: 'build/templates/risk-register.hbs', selector: '#risk-list' },\n  { html: 'docs/reference/fr-catalog.html',  data: 'build/data/frs.json',   template: 'build/templates/fr-catalog.hbs', selector: '#fr-list' },\n];\n\nasync function renderPage({ html, data, template, selector }) {\n  const json = JSON.parse(readFileSync(data, 'utf8'));\n  // §1 #3: deterministic — sort by id ascending\n  json.sort((a, b) =&gt; a.id.localeCompare(b.id));\n\n  const tmpl = Handlebars.compile(readFileSync(template, 'utf8'));\n  const rendered = tmpl({ items: json });\n\n  const $ = cheerio.load(readFileSync(html, 'utf8'));\n  $(selector).empty();   // remove existing prerendered (if any)\n  $(selector).append(rendered);\n\n  writeFileSync(html, $.html());\n  return { rendered_count: json.length, output_bytes: $.html().length };\n}\n\nasync function main() {\n  const watchMode = process.argv.includes('--watch');\n  const buildOnce = async () =&gt; {\n    const report = { pages: [], started_at: new Date().toISOString(), duration_ms: 0 };\n    const t0 = Date.now();\n    for (const page of PAGES) {\n      const stat = await renderPage(page);\n      report.pages.push({ html: page.html, ...stat });\n    }\n    report.duration_ms = Date.now() - t0;\n    writeFileSync('build/last-build-report.json', JSON.stringify(report, null, 2));\n    console.log(`✅ Rendered ${report.pages.length} pages in ${report.duration_ms}ms`);\n  };\n\n  await buildOnce();\n\n  if (watchMode) {\n    console.log('Watching for changes...');\n    for (const page of PAGES) {\n      watch(page.data, async () =&gt; {\n        console.log(`Change detected in ${page.data}; rebuilding...`);\n        await buildOnce();\n      });\n    }\n  }\n}\n\nmain().catch(e =&gt; { console.error(e); process.exit(1); });</code></pre>\n<h3>Handlebars template (NFR card)</h3>\n<pre><code class=\"language-handlebars\">{{!-- website/build/templates/nfr-catalog.hbs --}}\n&lt;section data-prerendered=\"true\" x-cloak&gt;\n  {{#each items}}\n  &lt;article class=\"bbg-card nfr-card\" id=\"{{id}}\" data-nfr-id=\"{{id}}\" data-category=\"{{category}}\"&gt;\n    &lt;header&gt;\n      &lt;span class=\"nfr-id nfr-{{category}}\"&gt;{{id}}&lt;/span&gt;\n      &lt;span class=\"target-pill\"&gt;{{target}}&lt;/span&gt;\n      &lt;span class=\"phase-chip phase-{{phase}}\"&gt;{{phase}}&lt;/span&gt;\n    &lt;/header&gt;\n    &lt;h3&gt;{{title}}&lt;/h3&gt;\n    &lt;p class=\"description\"&gt;{{description}}&lt;/p&gt;\n    &lt;dl class=\"grid-info\"&gt;\n      &lt;dt&gt;Measurement&lt;/dt&gt;&lt;dd&gt;{{measurement}}&lt;/dd&gt;\n      &lt;dt&gt;Modules&lt;/dt&gt;&lt;dd&gt;{{#each modules}}&lt;span class=\"module-chip\"&gt;{{this}}&lt;/span&gt;{{/each}}&lt;/dd&gt;\n      {{#if references}}\n      &lt;dt&gt;Reference&lt;/dt&gt;&lt;dd&gt;{{#each references}}&lt;a href=\"#{{this}}\"&gt;{{this}}&lt;/a&gt;{{#unless @last}} · {{/unless}}{{/each}}&lt;/dd&gt;\n      {{/if}}\n    &lt;/dl&gt;\n  &lt;/article&gt;\n  {{/each}}\n&lt;/section&gt;</code></pre>\n<h3>Data shape (NFR JSON)</h3>\n<pre><code class=\"language-json\">[\n  {\n    \"id\": \"NFR-PERF-01\",\n    \"category\": \"perf\",\n    \"title\": \"memory search p95 latency\",\n    \"target\": \"p95 ≤ 250ms\",\n    \"phase\": \"P0\",\n    \"description\": \"memory search MUST return within 250ms p95...\",\n    \"measurement\": \"1M chunks fixture / 1000 random queries\",\n    \"modules\": [\"memory\"],\n    \"references\": [\"DEC-070\"]\n  }\n]</code></pre>\n<h3>Alpine init modification</h3>\n<pre><code class=\"language-javascript\">// website/docs/reference/_alpine-init.js (modified)\ndocument.addEventListener('alpine:init', () =&gt; {\n    Alpine.data('referenceCatalog', () =&gt; ({\n        nfrs: [],\n        hydrated: false,\n        async init() {\n            const response = await fetch('/build/data/nfrs.json');\n            this.nfrs = await response.json();\n            this.hydrated = true;\n            // Hide prerendered after hydration — Alpine reactive takes over\n            document.querySelectorAll('[data-prerendered]').forEach(el =&gt; el.style.display = 'none');\n        },\n        // ... existing filter/sort methods unchanged ...\n    }));\n});</code></pre>\n<h3>CSS</h3>\n<pre><code class=\"language-css\">[x-cloak] { display: block; }   /* prerendered visible at first paint */\n.hydrated [x-cloak] { display: none; }   /* hidden once Alpine ready */</code></pre>\n<h3>CI workflow</h3>\n<pre><code class=\"language-yaml\"># .github/workflows/docs-prerender-gate.yml\nname: Docs Prerender Gate\non:\n  pull_request:\n    paths:\n      - 'website/**'\n      - '.github/workflows/docs-prerender-gate.yml'\n\njobs:\n  prerender-gate:\n    runs-on: ubuntu-22.04\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: '20' }\n      - run: cd website &amp;&amp; npm ci\n      - name: Run prerender\n        run: cd website &amp;&amp; node build/render-reference-pages.mjs\n      - name: Assert no drift\n        run: |\n          if ! git diff --exit-code website/docs/reference/; then\n            echo \"❌ Prerender drift detected — commit the regenerated HTML\"\n            exit 1\n          fi\n      - name: Pagefind index test\n        run: |\n          cd website/docs &amp;&amp; npx pagefind --site .\n          npx pagefind search \"NFR-PERF-01\" | jq -e '.results | length &gt; 0'\n          npx pagefind search \"RSK-09\" | jq -e '.results | length &gt; 0'\n          npx pagefind search \"TASK-AI-001\" | jq -e '.results | length &gt; 0'</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>First paint shows data</strong> — <code>curl https://docs.cyberos.world/reference/nfr-catalog.html | grep \"NFR-PERF-01\"</code> returns matching <code>&lt;article&gt;</code> line. No JS execution required.</li><li><strong>Pagefind indexes the data</strong> — <code>pagefind search \"NFR-PERF-01\"</code> returns the nfr-catalog page. Same for <code>RSK-09</code> and <code>TASK-AI-001</code>.</li><li><strong>JS-on UX unchanged</strong> — on a browser with Alpine loaded, page looks identical to current. No double-render flash on hydration.</li><li><strong>Filter/sort work post-hydration</strong> — clicking a filter chip shows/hides the right cards.</li><li><strong>Determinism</strong> — running the build twice on same JSON produces byte-identical HTML output (<code>git diff --exit-code</code> = clean).</li><li><strong>CI gate</strong> — drift between JSON and HTML fails the PR.</li><li><strong>Per-card anchors work</strong> — <code>#NFR-PERF-01</code> URL fragment scrolls to the card without JS.</li><li><strong>All metadata fields rendered</strong> — verifying NFR cards have id/category/target/phase/modules/references; RSK cards have id/severity/likelihood/mitigation/owner/status; task cards have id/module/priority/status/slice/owner/depends_on/blocks.</li><li><strong>Cloudflare Pages build runs prerender</strong> — <code>deploy.yml</code> includes the step; failure aborts deploy.</li><li><strong>Watch mode works</strong> — <code>--watch</code> flag re-renders on JSON change.</li><li><strong>Build report emitted</strong> — <code>last-build-report.json</code> contains per-page stats.</li><li><strong>Empty array doesn't crash</strong> — JSON with empty array → empty <code>&lt;section&gt;</code> rendered (no crash).</li><li><strong>Special characters escaped</strong> — NFR description with <code>&lt;script&gt;</code> is HTML-escaped (Handlebars default).</li><li><strong>Stable iteration order</strong> — items sorted by <code>id</code> lexicographically.</li><li><strong>Hydration sets <code>hydrated</code> class on body</strong> — for CSS to hide prerendered.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># Manual smoke\ncd cyberos/website\nnode build/render-reference-pages.mjs\ngit diff --exit-code docs/reference/    # determinism check (should be clean if regen of unchanged)\n\n# Pagefind\ncd docs &amp;&amp; npx pagefind --site .\nnpx pagefind search \"NFR-PERF-01\" | jq '.results | length'   # expect &gt; 0\nnpx pagefind search \"RSK-09\" | jq '.results | length'        # expect &gt; 0\nnpx pagefind search \"TASK-AI-001\" | jq '.results | length'     # expect &gt; 0\n\n# First-paint check (no JS)\ncurl -s https://docs.cyberos.world/reference/nfr-catalog.html | grep -c 'data-nfr-id=\"NFR-'   # expect &gt; 50\n\n# Watch mode\nnode build/render-reference-pages.mjs --watch &amp;\necho \"test change\" &gt;&gt; build/data/nfrs.json\n# Expect rebuild log within 1s\nkill %1</code></pre>\n<pre><code class=\"language-javascript\">// website/build/tests/render_test.mjs\nimport { execSync } from 'child_process';\nimport { readFileSync } from 'fs';\nimport { test } from 'node:test';\nimport assert from 'node:assert/strict';\n\ntest('render produces NFR cards', async () =&gt; {\n    execSync('node build/render-reference-pages.mjs', { cwd: process.cwd() });\n    const html = readFileSync('docs/reference/nfr-catalog.html', 'utf8');\n    assert.ok(html.includes('data-nfr-id=\"NFR-PERF-01\"'), 'expected NFR-PERF-01 in output');\n    assert.ok(html.includes('&lt;section data-prerendered=\"true\"'), 'expected prerendered section');\n});\n\ntest('render produces task cards with all metadata', async () =&gt; {\n    execSync('node build/render-reference-pages.mjs', { cwd: process.cwd() });\n    const html = readFileSync('docs/reference/fr-catalog.html', 'utf8');\n    assert.ok(html.includes('TASK-AI-001'));\n    assert.ok(html.includes('depends_on'));\n});</code></pre>\n<pre><code class=\"language-javascript\">// website/build/tests/determinism_test.mjs\nimport { execSync } from 'child_process';\nimport { readFileSync } from 'fs';\nimport { test } from 'node:test';\nimport assert from 'node:assert/strict';\n\ntest('two consecutive builds produce identical HTML', async () =&gt; {\n    execSync('node build/render-reference-pages.mjs');\n    const html1 = readFileSync('docs/reference/nfr-catalog.html');\n    execSync('node build/render-reference-pages.mjs');\n    const html2 = readFileSync('docs/reference/nfr-catalog.html');\n    assert.deepEqual(html1, html2, 'HTML output not deterministic');\n});</code></pre>\n<pre><code class=\"language-javascript\">// website/build/tests/pagefind_index_test.mjs\nimport { execSync } from 'child_process';\nimport { test } from 'node:test';\nimport assert from 'node:assert/strict';\n\ntest('pagefind indexes prerendered NFR-IDs', async () =&gt; {\n    execSync('node build/render-reference-pages.mjs');\n    execSync('npx pagefind --site docs/');\n    const result = JSON.parse(execSync('npx pagefind search \"NFR-PERF-01\"').toString());\n    assert.ok(result.results.length &gt; 0, 'pagefind did not index NFR-PERF-01');\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-javascript\">// website/build/data-extract.mjs (one-time migration: extract from existing inline JS)\nimport * as cheerio from 'cheerio';\nimport { readFileSync, writeFileSync } from 'fs';\nimport vm from 'vm';\n\nconst FILES = [\n  { html: 'docs/reference/nfr-catalog.html', varName: 'NFR_DATA', out: 'build/data/nfrs.json' },\n  { html: 'docs/reference/risk-register.html', varName: 'RISKS', out: 'build/data/risks.json' },\n  { html: 'docs/reference/fr-catalog.html', varName: 'TASK_CATALOG', out: 'build/data/frs.json' },\n];\n\nfor (const { html, varName, out } of FILES) {\n  const $ = cheerio.load(readFileSync(html, 'utf8'));\n  const script = $('script').filter((_, el) =&gt; $(el).text().includes(`const ${varName}`)).text();\n  const ctx = { window: {}, document: {} };\n  vm.createContext(ctx);\n  vm.runInContext(script + `; this.${varName} = ${varName};`, ctx);\n  writeFileSync(out, JSON.stringify(ctx[varName], null, 2));\n  console.log(`✅ Extracted ${varName} → ${out}`);\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li>Cloudflare Pages build env has Node 20 (or any CI runner with Node).</li><li>npm packages: <code>cheerio@1</code>, <code>handlebars@4</code>.</li><li>Pagefind binary (or @pagefind/default-ui as npm dep).</li><li>One-time migration step: extract inline JS arrays to JSON via <code>data-extract.mjs</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Build report</h3>\n<pre><code class=\"language-json\">{\n  \"started_at\": \"2026-05-15T14:00:00Z\",\n  \"duration_ms\": 245,\n  \"pages\": [\n    { \"html\": \"docs/reference/nfr-catalog.html\", \"rendered_count\": 76, \"output_bytes\": 89234 },\n    { \"html\": \"docs/reference/risk-register.html\", \"rendered_count\": 22, \"output_bytes\": 31872 },\n    { \"html\": \"docs/reference/fr-catalog.html\", \"rendered_count\": 64, \"output_bytes\": 124398 }\n  ]\n}</code></pre>\n<h3>Pagefind search response</h3>\n<pre><code class=\"language-json\">{\n  \"results\": [\n    {\n      \"id\": \"...\",\n      \"data\": \"/reference/nfr-catalog.html#NFR-PERF-01\",\n      \"title\": \"memory search p95 latency\",\n      \"excerpt\": \"memory search MUST return within 250ms p95...\"\n    }\n  ]\n}</code></pre>\n<h3>CI drift failure</h3>\n<pre><code class=\"language-text\">❌ Prerender drift detected — commit the regenerated HTML\ndiff --git a/website/docs/reference/nfr-catalog.html b/website/docs/reference/nfr-catalog.html\n+ &lt;article class=\"bbg-card nfr-card\" id=\"NFR-NEW-01\"&gt;...&lt;/article&gt;</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Per-language prerender (Vietnamese reference pages) — slice 3+ when i18n lands.</li><li>Differential rebuild (only re-render pages whose JSON changed) — slice 4+ optimisation.</li><li>HTMX progressive enhancement (replace Alpine entirely) — out of scope; Alpine works.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>JSON parse error</td><td>Build fails with line number</td><td>Operator fixes JSON</td></tr><tr><td>Handlebars template syntax error</td><td>Build fails</td><td>Operator fixes template</td></tr><tr><td>Cheerio fails to parse HTML</td><td>Build fails</td><td>Operator fixes HTML scaffold</td></tr><tr><td>JSON-HTML drift in PR</td><td>CI fails (<code>git diff --exit-code</code>)</td><td>Operator runs build + commits</td></tr><tr><td>Pagefind index returns 0 hits</td><td>Test fails</td><td>Investigate prerender output</td></tr><tr><td>First paint still empty (post-deploy)</td><td>Manual smoke check fails</td><td>Check deploy step ran prerender</td></tr><tr><td>Double-render flash on hydration</td><td>Manual UX check</td><td>Add <code>[x-cloak]</code> to template + CSS</td></tr><tr><td>Filter/sort broken post-hydrate</td><td>Manual UX check</td><td>Investigate Alpine init order</td></tr><tr><td>Special characters not escaped</td><td>XSS in description field</td><td>Handlebars default escapes; verify</td></tr><tr><td>Determinism violated (sort order)</td><td>determinism_test fails</td><td>Add explicit <code>localeCompare</code> sort</td></tr><tr><td>Watch mode doesn't trigger</td><td>local dev annoyance</td><td>Check <code>fs.watch</code> permissions</td></tr><tr><td>Build report missing</td><td>CI artefact missing</td><td>Always write before exit</td></tr><tr><td>Cloudflare Pages build doesn't run prerender</td><td>First paint empty post-deploy</td><td>Update deploy.yml</td></tr><tr><td>Per-card anchor missing</td><td>deep-link scroll fails</td><td>Verify <code>id=\"&lt;card-id&gt;\"</code> attribute</td></tr><tr><td>Empty JSON array</td><td>empty section rendered</td><td>By design</td></tr><tr><td>Migration extracts wrong data</td><td>data-extract.mjs bug</td><td>Manually verify JSON post-extraction</td></tr><tr><td>Cheerio version mismatch</td><td>Build fails on different runner</td><td>Pin in package.json</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>This task closes UX defects #1 and #2 from <code>docs/archive/2026-05-14/RESEARCH_REVIEW.md §4</code>. Load-bearing for procurement-story for auditors.</li><li>Deferred to P0 polish slice; estimated land date late P0.</li><li>JSON-as-source-of-truth + watch mode + CI gate together prevent JSON-HTML drift.</li><li>Alpine reactive coexists; UX preserved.</li><li>Pagefind re-indexes on every deploy.</li><li>Per-card anchors enable deep linking from external sources (e.g., regulator citing <code>https://docs.cyberos.world/reference/nfr-catalog.html#NFR-PERF-01</code>).</li><li>Determinism via stable sort + no time-dependent values catches accidental non-determinism in CI.</li><li>Build report enables operator review of \"did all 76 NFRs render?\" without inspecting HTML.</li><li>Watch mode improves local dev iteration; not run in CI.</li></ul>\n<hr>\n<p><em>End of TASK-DOCS-001. Status: planned (10/10 target).</em></p>\n<hr>\n<p><strong>Supersession record (2026-07-12, conflict-scan doctrine: newest wins).</strong> The live intent of this task shipped through the TASK-DOCS-002/005/006 pipeline: data extraction to JSON (tools/docs-site/data-extract.mjs -&gt; data/frs.json, nfrs), prerendered <code>&lt;section data-prerendered=\"true\"&gt;</code> catalog cards (render-task-catalog / render-nfr-catalog carry <code>TASK-DOCS-001 §1 #2</code> citations), deterministic builds, stable per-card anchors, and last-build-report.json. The remaining clauses are obsolete by later approved doctrine: #1's website/build paths and #8's Cloudflare Pages (replaced by tools/docs-site + VPS deploy), #4/#5 Alpine hydration (vanilla JS since the rebuild), #11's committed-HTML drift gate (FORBIDDEN by TASK-DOCS-002: generated output is never committed), #9 Pagefind and #10 watch mode (never built). Client-side search of the docs site is genuinely undelivered and is queued as a fresh-task candidate for the next intake batch rather than resurrecting this spec's mechanics. Status: closed (superseded), not done - several clauses as written are permanently false.</p>";
