window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PROJ-010"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The drift detector <strong>MUST</strong> run a nightly sweep over all active memory_links and flag stale ones. The contract:</p>\n<ol><li><strong>MUST</strong> schedule a nightly cron task at 02:00 local time (configurable per <code>CYBEROS_DRIFT_SWEEP_CRON</code> env var; default <code>0 2 * * *</code>).</li><li><strong>MUST</strong> also support on-demand sweep via <code>cyberos drift sweep [--tenant-id &lt;uuid&gt;]</code> CLI.</li><li><strong>MUST</strong> detect three drift kinds:</li></ol>\n<ul><li><code>TargetMissing</code>: linked memory_path no longer exists in memory (deletion / unwatched folder).</li><li><code>TargetSuperseded</code>: linked memory has been superseded by a newer memory (FR-AGENTS §8 correction_to row); the link points at the older revision.</li><li><code>ScopeRevoked</code>: the issue's tenant lost read scope to the memory (tenant permission change since link was created).</li></ul>\n<ol><li><strong>MUST</strong> emit <code>proj.citation_drift_detected</code> memory audit row PER detected drift with payload <code>{link_id, issue_id, memory_path, drift_kind, detected_at_ns, prior_check_at_ns, trace_id}</code>.</li><li><strong>MUST</strong> record sweep state in <code>drift_state</code> table: per-tenant <code>last_sweep_at</code>, <code>last_total_links_checked</code>, <code>last_drift_count_by_kind</code>. Operators query for \"when did the last sweep run.\"</li><li><strong>MUST</strong> notify via FR-OBS-007:</li></ol>\n<ul><li>If <code>drift_count &gt;= 10</code> in one sweep for one tenant → sev-2 alert.</li><li>Otherwise → sev-3 (informational; surfaces in daily digest).</li></ul>\n<ol><li><strong>MUST</strong> NOT auto-remove stale links. Drift is informational; the operator decides whether to remove or accept the stale state.</li><li><strong>MUST</strong> expose REST <code>GET /api/proj/drift?tenant_id=...&amp;since=...</code> returning detected drifts (paginated).</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_drift_sweep_duration_seconds</code> (histogram).</li><li><code>proj_drift_links_checked_total</code> (counter).</li><li><code>proj_drift_detected_total{kind}</code> (counter).</li></ul>\n<ol><li><strong>MUST</strong> be deterministic given fixed memory state: same input = same drift report (no Date.now()-keyed randomness).</li><li><strong>MUST</strong> complete within 5 minutes for a tenant with ≤ 10K active links; exceeded → sev-2 latency alarm.</li><li><strong>MUST</strong> support delta sweeps: <code>cyberos drift sweep --since &lt;timestamp&gt;</code> only re-checks links touched after <code>&lt;timestamp&gt;</code>. Used by operators iterating fixes without re-checking everything.</li><li><strong>MUST</strong> track per-link drift status separately from the audit row: <code>memory_links.drift_status</code> column (<code>healthy | target_missing | target_superseded | scope_revoked | unchecked</code>) updated after each sweep. UI uses this to render \"stale link\" badges.</li><li><strong>MUST</strong> notify assignees (not just admins) via FR-OBS-007: each drift event creates a per-assignee notification (queued via CUO triage per DEC-312). Assignee = current <code>assignee_subject_id</code> on the linked issue.</li><li><strong>MUST</strong> support \"suppress\" workflow: operators can suppress a known-stale drift (<code>POST /api/proj/drift/:link_id/suppress</code> with reason) so it doesn't re-alert. Suppression expires after 90 days unless renewed.</li><li><strong>MUST</strong> include a \"drift severity hierarchy\": within a sweep, <code>TargetMissing &gt; ScopeRevoked &gt; TargetSuperseded</code> (highest to lowest priority). Notifications group by severity; SEV-2 fires only on highest-severity counts.</li><li><strong>MUST</strong> support per-tenant config override: <code>cyberos_proj_tenant_settings.drift_sweep_cron</code> overrides the default cron; <code>drift_sev2_threshold</code> overrides the 10-drift threshold.</li><li><strong>MUST</strong> include suggested remediation in each drift event: for TargetSuperseded → suggest the successor memory_path; for TargetMissing → suggest similar memories via FR-MEMORY-108 fuzzy search; for ScopeRevoked → suggest contacting the memory owner.</li><li><strong>MUST</strong> support a \"dry-run\" mode: <code>cyberos drift sweep --dry-run</code> performs the check but skips audit row emission + notifications. Used for operator preview before committing to a sweep.</li><li><strong>MUST</strong> include a <code>proj.drift_remediated</code> audit row when a stale link is removed OR retargeted; tracks remediation rate per tenant.</li><li><strong>MUST</strong> support drift-trend metric: <code>proj_drift_trend_total{tenant_id, kind, direction}</code> where direction ∈ <code>increasing | stable | decreasing</code> based on 7-day rolling comparison.</li><li><strong>MUST</strong> complete within 1 minute for tenants with ≤ 1K active links (smaller-tenant fast path); larger tenants get the 5-min budget. Smaller tenants get faster feedback.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why nightly + on-demand (DEC-310)?</strong> Drift accumulates slowly (memories aren't deleted often). Nightly cadence catches the bulk; on-demand for \"we just did a memory purge; please re-check now.\"</p>\n<p><strong>Why not inline (DEC-310)?</strong> Inline drift checks on every link query would burn memory-read budget for negligible benefit (drift detection at query-time finds the same drift drift sweep finds — but slower per-call). Async batch is the right model.</p>\n<p><strong>Why flag-only (DEC-310, §1 #7)?</strong> Auto-removing stale links is dangerous: a temporary memory outage during sweep would mass-remove valid links. Flag-only = operator-controlled remediation.</p>\n<p><strong>Why three drift kinds (DEC-311)?</strong> <code>TargetMissing</code> is the obvious one. <code>TargetSuperseded</code> is value-add: the link is technically valid but stale; operator should retarget to the newer memory. <code>ScopeRevoked</code> catches the rare-but-meaningful case where a memory was demoted from shareable to private.</p>\n<p><strong>Why sev-2 at ≥ 10 drifts (§1 #6)?</strong> Empirical: 1-2 drifts per tenant per sweep is normal background (operators delete obsolete memories). 10+ is a signal of bulk action (purge sweep, scope policy change) that operator should review.</p>\n<p><strong>Why deterministic (§1 #10)?</strong> Operator reruns sweep to confirm fix → must get the same answer. Non-determinism (e.g. random sampling) defeats the verification workflow.</p>\n<p><strong>Why delta sweeps (§1 #12)?</strong> Operators iterating on fixes don't want to re-check thousands of healthy links. <code>--since</code> confines the check to recently-touched links.</p>\n<p><strong>Why drift_status column (§1 #13)?</strong> UI rendering \"stale link\" badges per row needs O(1) lookup; without column, every row render = re-running sweep. Materialised column is the cache.</p>\n<p><strong>Why notify assignees (§1 #14)?</strong> Admins see drift counts; the actual operator with context to fix is the issue assignee. Per-assignee notification ensures the right person gets the alert.</p>\n<p><strong>Why suppress workflow (§1 #15)?</strong> Some drifts are known-and-accepted (linked memory archived intentionally; no fix planned). Suppression silences repeat alerts. 90-day expiry forces periodic re-evaluation.</p>\n<p><strong>Why drift severity hierarchy (§1 #16)?</strong> A tenant with 5 TargetMissing + 100 TargetSuperseded shouldn't fire SEV-2 on the latter; missing is far worse than superseded.</p>\n<p><strong>Why per-tenant config (§1 #17)?</strong> Different tenants have different drift tolerance (legal-compliance tenant wants daily sweep; SMB tenant fine with weekly). Per-tenant flexibility.</p>\n<p><strong>Why suggested remediation (§1 #18)?</strong> Operators receiving an alert want to know what to do; \"memory missing — here are 3 similar paths\" cuts triage time.</p>\n<p><strong>Why dry-run (§1 #19)?</strong> Operator running a sweep at non-cron time may want preview without spamming notifications.</p>\n<p><strong>Why drift_remediated audit (§1 #20)?</strong> Tracks operational health: increasing remediation rate = ops responding; flat rate with rising detection = backlog growing.</p>\n<p><strong>Why trend metric (§1 #21)?</strong> Single-day drift counts are noisy; week-over-week trend tells operators whether they're keeping up.</p>\n<p><strong>Why fast-path for small tenants (§1 #22)?</strong> Quick feedback on small tenants (1K links: 6s instead of 60s); large tenants tolerate longer.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0010_drift_state.sql\n\nCREATE TABLE drift_state (\n    tenant_id                    UUID PRIMARY KEY,\n    last_sweep_at                TIMESTAMPTZ NOT NULL,\n    last_total_links_checked     INT NOT NULL,\n    last_drift_count_target_missing      INT NOT NULL DEFAULT 0,\n    last_drift_count_target_superseded   INT NOT NULL DEFAULT 0,\n    last_drift_count_scope_revoked       INT NOT NULL DEFAULT 0,\n    last_sweep_duration_ms       BIGINT NOT NULL\n);</code></pre>\n<h3>Rust API</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/drift/mod.rs\nuse serde::Serialize;\n\n#[derive(Clone, Copy, Debug, Serialize, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\npub enum DriftKind { TargetMissing, TargetSuperseded, ScopeRevoked }\n\n#[derive(Clone, Debug, Serialize)]\npub struct DriftReport {\n    pub tenant_id:      uuid::Uuid,\n    pub swept_at_ns:    i64,\n    pub links_checked:  i32,\n    pub drifts:         Vec&lt;DriftEvent&gt;,\n    pub duration_ms:    i64,\n}\n\n#[derive(Clone, Debug, Serialize)]\npub struct DriftEvent {\n    pub link_id:       uuid::Uuid,\n    pub issue_id:      uuid::Uuid,\n    pub memory_path:   String,\n    pub drift_kind:    DriftKind,\n    pub detected_at_ns: i64,\n}\n\npub async fn sweep_tenant(\n    pool: &amp;sqlx::PgPool,\n    tenant_id: uuid::Uuid,\n) -&gt; anyhow::Result&lt;DriftReport&gt; {\n    let start = std::time::Instant::now();\n    let swept_at_ns = chrono::Utc::now().timestamp_nanos_opt().unwrap();\n\n    // Set tenant_id context for RLS\n    sqlx::query(\"SELECT set_config('app.tenant_id', $1, true)\")\n        .bind(tenant_id.to_string()).execute(pool).await?;\n\n    let links: Vec&lt;crate::memory_link::MemoryLink&gt; = sqlx::query_as(\n        \"SELECT * FROM memory_links WHERE removed_at IS NULL\"\n    ).fetch_all(pool).await?;\n\n    let mut drifts = Vec::new();\n    for link in &amp;links {\n        let drift = detect_drift_for_link(link).await;\n        if let Some(kind) = drift {\n            let ev = DriftEvent {\n                link_id: link.id, issue_id: link.issue_id,\n                memory_path: link.memory_path.clone(),\n                drift_kind: kind, detected_at_ns: swept_at_ns,\n            };\n            emit_memory_row(\"proj.citation_drift_detected\", serde_json::json!({\n                \"link_id\": ev.link_id, \"issue_id\": ev.issue_id,\n                \"memory_path\": ev.memory_path, \"drift_kind\": ev.drift_kind,\n                \"detected_at_ns\": ev.detected_at_ns,\n                \"trace_id\": current_trace_id(),\n            })).await;\n            metrics::counter!(\"proj_drift_detected_total\", \"kind\" =&gt; format!(\"{:?}\", kind)).increment(1);\n            drifts.push(ev);\n        }\n    }\n\n    let duration_ms = start.elapsed().as_millis() as i64;\n    sqlx::query(\n        \"INSERT INTO drift_state\n           (tenant_id, last_sweep_at, last_total_links_checked,\n            last_drift_count_target_missing, last_drift_count_target_superseded,\n            last_drift_count_scope_revoked, last_sweep_duration_ms)\n         VALUES ($1, NOW(), $2, $3, $4, $5, $6)\n         ON CONFLICT (tenant_id) DO UPDATE SET\n           last_sweep_at = EXCLUDED.last_sweep_at,\n           last_total_links_checked = EXCLUDED.last_total_links_checked,\n           last_drift_count_target_missing = EXCLUDED.last_drift_count_target_missing,\n           last_drift_count_target_superseded = EXCLUDED.last_drift_count_target_superseded,\n           last_drift_count_scope_revoked = EXCLUDED.last_drift_count_scope_revoked,\n           last_sweep_duration_ms = EXCLUDED.last_sweep_duration_ms\"\n    )\n    .bind(tenant_id).bind(links.len() as i32)\n    .bind(drifts.iter().filter(|d| d.drift_kind == DriftKind::TargetMissing).count() as i32)\n    .bind(drifts.iter().filter(|d| d.drift_kind == DriftKind::TargetSuperseded).count() as i32)\n    .bind(drifts.iter().filter(|d| d.drift_kind == DriftKind::ScopeRevoked).count() as i32)\n    .bind(duration_ms).execute(pool).await?;\n\n    metrics::histogram!(\"proj_drift_sweep_duration_seconds\").record(start.elapsed().as_secs_f64());\n    metrics::counter!(\"proj_drift_links_checked_total\").increment(links.len() as u64);\n\n    // Notification\n    if drifts.len() &gt;= 10 {\n        obs::alert(obs::Severity::Sev2, \"citation_drift_high\",\n            serde_json::json!({\"tenant_id\": tenant_id, \"drift_count\": drifts.len()})).await;\n    } else if !drifts.is_empty() {\n        obs::alert(obs::Severity::Sev3, \"citation_drift_detected\",\n            serde_json::json!({\"tenant_id\": tenant_id, \"drift_count\": drifts.len()})).await;\n    }\n\n    Ok(DriftReport { tenant_id, swept_at_ns, links_checked: links.len() as i32, drifts, duration_ms })\n}\n\nasync fn detect_drift_for_link(link: &amp;crate::memory_link::MemoryLink) -&gt; Option&lt;DriftKind&gt; {\n    // 1. TargetMissing: memory doesn't exist\n    let memory = match memory_reader::find_memory(&amp;link.memory_path).await {\n        Some(m) =&gt; m,\n        None    =&gt; return Some(DriftKind::TargetMissing),\n    };\n\n    // 2. ScopeRevoked: memory's sync_class changed to private (was shareable when linked)\n    if memory.sync_class == memory::SyncClass::Private {\n        // Compare to historical scope; if previously shareable and now private → drift\n        if was_shareable_at_link_create(&amp;memory, &amp;link.created_at).await {\n            return Some(DriftKind::ScopeRevoked);\n        }\n    }\n\n    // 3. TargetSuperseded: a newer memory exists with `correction_to` pointing at this one\n    if memory_reader::has_successor(&amp;memory.row_id).await {\n        return Some(DriftKind::TargetSuperseded);\n    }\n\n    None\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>TargetMissing detected</strong> — link to deleted memory → DriftKind::TargetMissing reported.</li><li><strong>TargetSuperseded detected</strong> — newer memory with correction_to → DriftKind::TargetSuperseded.</li><li><strong>ScopeRevoked detected</strong> — memory flipped shareable → private after link → DriftKind::ScopeRevoked.</li><li><strong>Healthy links not flagged</strong> — happy memory + healthy link → no drift events.</li><li><strong>Sweep deterministic</strong> — same DB state → same report on rerun.</li><li><strong>drift_state row updated</strong> — sweep completion → drift_state has row with counts.</li><li><strong>memory audit per drift</strong> — N drifts → N <code>proj.citation_drift_detected</code> rows.</li><li><strong>Sev-2 alert at ≥ 10</strong> — fixture with 10 drifts → FR-OBS-007 sev-2 alert fired.</li><li><strong>Sev-3 alert at &lt; 10</strong> — fixture with 3 drifts → sev-3 alert.</li><li><strong>No alert on zero drifts</strong> — happy sweep → no alert.</li><li><strong>CLI works</strong> — <code>cyberos drift sweep --tenant-id &lt;uuid&gt;</code> → exit 0 + report JSON.</li><li><strong>REST GET filters by tenant</strong> — tenant A's report invisible to tenant B.</li><li><strong>RLS isolates drift_state</strong> — tenant A cannot read tenant B's drift_state.</li><li><strong>Auto-removal NOT performed</strong> — sweep run + drift detected → memory_links row unchanged (still active).</li><li><strong>Sweep latency ≤ 5min for 10K links</strong> — fixture; assert duration_ms &lt; 300000.</li><li><strong>OTel metrics emitted</strong> — histogram + counters populated.</li><li><strong>Delta sweep checks only recent links</strong> — <code>--since &lt;ts&gt;</code> → only links touched after ts re-checked (AC for §1 #12).</li><li><strong>drift_status updated post-sweep</strong> — links marked appropriately (AC for §1 #13).</li><li><strong>Assignee notifications fired</strong> — drift on issue with assignee → notification routed to assignee (AC for §1 #14).</li><li><strong>Suppress silences alert</strong> — suppressed link doesn't re-alert; 90d expiry then re-alerts (AC for §1 #15).</li><li><strong>Severity hierarchy gates SEV-2</strong> — 5 TargetMissing + 100 TargetSuperseded → SEV-2 fires for missing not superseded (AC for §1 #16).</li><li><strong>Tenant config override</strong> — set tenant <code>drift_sev2_threshold=20</code>; 10 drifts no longer triggers SEV-2 (AC for §1 #17).</li><li><strong>Suggested remediation included</strong> — TargetMissing event has <code>suggested_paths</code> array (AC for §1 #18).</li><li><strong>Dry-run skips audit + notification</strong> — <code>--dry-run</code> → no audit row, no alert (AC for §1 #19).</li><li><strong>drift_remediated audit on operator fix</strong> — operator removes stale link → audit row emitted (AC for §1 #20).</li><li><strong>Trend metric reflects 7d direction</strong> — 7 days of increasing drifts → direction=increasing (AC for §1 #21).</li><li><strong>Small-tenant fast path</strong> — tenant with 500 links → sweep completes in &lt;60s (AC for §1 #22).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn target_missing_detected() {\n    let env = TestEnv::new().await;\n    let (issue, mem) = env.setup_link().await;\n    env.delete_memory(&amp;mem).await;   // make target missing\n    let report = sweep_tenant(&amp;env.pool, env.tenant_id()).await.unwrap();\n    let drift = report.drifts.iter().find(|d| d.memory_path == mem).unwrap();\n    assert_eq!(drift.drift_kind, DriftKind::TargetMissing);\n}\n\n#[tokio::test]\nasync fn superseded_detected() {\n    let env = TestEnv::new().await;\n    let mem_v1 = env.create_memory_v1().await;\n    let _ = env.create_link_to(mem_v1.clone()).await;\n    env.create_correction_memory(&amp;mem_v1).await;\n    let report = sweep_tenant(&amp;env.pool, env.tenant_id()).await.unwrap();\n    assert!(report.drifts.iter().any(|d| d.drift_kind == DriftKind::TargetSuperseded));\n}\n\n#[tokio::test]\nasync fn deterministic_rerun_same_report() {\n    let env = TestEnv::new().await;\n    env.setup_two_drifts().await;\n    let r1 = sweep_tenant(&amp;env.pool, env.tenant_id()).await.unwrap();\n    let r2 = sweep_tenant(&amp;env.pool, env.tenant_id()).await.unwrap();\n    let kinds1: Vec&lt;_&gt; = r1.drifts.iter().map(|d| (d.link_id, d.drift_kind)).collect();\n    let kinds2: Vec&lt;_&gt; = r2.drifts.iter().map(|d| (d.link_id, d.drift_kind)).collect();\n    assert_eq!(kinds1, kinds2);\n}\n\n#[tokio::test]\nasync fn no_auto_remove() {\n    let env = TestEnv::new().await;\n    let (link, _) = env.setup_drift().await;\n    let _ = sweep_tenant(&amp;env.pool, env.tenant_id()).await.unwrap();\n    let still_active: bool = sqlx::query_scalar(\n        \"SELECT removed_at IS NULL FROM memory_links WHERE id = $1\"\n    ).bind(link).fetch_one(&amp;env.pool).await.unwrap();\n    assert!(still_active);\n}\n\n#[tokio::test]\nasync fn sev_2_alert_at_high_drift() {\n    let env = TestEnv::new().await;\n    env.setup_n_drifts(12).await;\n    let _ = sweep_tenant(&amp;env.pool, env.tenant_id()).await.unwrap();\n    let alert = env.obs.latest_alert().await;\n    assert_eq!(alert.severity, \"sev-2\");\n    assert_eq!(alert.kind, \"citation_drift_high\");\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + DB above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-PROJ-009</strong> — memory_links table consumed.</li><li><strong>FR-MEMORY-101</strong> — MemoryReader for find_memory + has_successor.</li><li><strong>FR-MEMORY-108</strong> — search API.</li><li><strong>FR-OBS-007</strong> — alert routing.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.citation_drift_detected\",\n  \"payload\": {\n    \"link_id\": \"lk-...\",\n    \"issue_id\": \"iss-...\",\n    \"memory_path\": \"memories/projects/cyberos/decisions/DEC-220.md\",\n    \"drift_kind\": \"target_superseded\",\n    \"detected_at_ns\": 1747407137483000000,\n    \"prior_check_at_ns\": 1747320737483000000,\n    \"trace_id\": \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Periodic sub-tenant sweep (per-engagement) — slice 4+.</li><li>Drift remediation workflow (auto-retarget to successor) — slice 4+; risky.</li><li>Real-time drift detection via memory chain hooks — slice 4+; more complex.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>memory reader unavailable</td><td>find_memory Err</td><td>Sweep aborts; sev-1 alarm</td><td>Operator restores memory</td></tr><tr><td>Tenant has 100K+ links</td><td>sweep exceeds 5min</td><td>sev-2 latency alarm</td><td>Slice 4+ paginate sweep</td></tr><tr><td>drift_state INSERT fails</td><td>sqlx Err</td><td>Sweep result lost; sev-2</td><td>Operator investigates DB</td></tr><tr><td>Concurrent sweep + new link write</td><td>RLS isolates per-tenant; race tolerable</td><td>Either link checked or skipped (consistent next sweep)</td><td>None</td></tr><tr><td>memory search transient error</td><td>retry 3×</td><td>Drift report unreliable for that link</td><td>Re-run sweep</td></tr><tr><td>Massive supersession event (1000 memories)</td><td>drifts spike</td><td>sev-2 alert fires; ops notified</td><td>Operator reviews</td></tr><tr><td>Sweep starts but server restart</td><td>partial state</td><td>drift_state shows old sweep; next sweep covers</td><td>None</td></tr><tr><td>Drift detection itself bugged (false positives)</td><td>property tests catch</td><td>CI blocked</td><td>Author fixes</td></tr><tr><td>Operator deletes drift_state</td><td>recreated on next sweep</td><td>Historical sweep info lost</td><td>Acceptable</td></tr><tr><td>OBS alert exporter down</td><td>metric buffered</td><td>Notification delayed</td><td>Operator restores FR-OBS-001</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>Memory path encoding inconsistencies</td><td>normalise at FR-MEMORY-108</td><td>Consistent</td><td>None</td></tr><tr><td>Delta sweep with invalid <code>--since</code></td><td>rejects</td><td>400</td><td>Caller</td></tr><tr><td>drift_status column out of sync (race)</td><td>next sweep corrects</td><td>brief inconsistency</td><td>None</td></tr><tr><td>Assignee subject_id stale (deleted user)</td><td>notification falls back to admin</td><td>None</td><td>None</td></tr><tr><td>Suppress expires mid-sweep</td><td>counted as un-suppressed</td><td>next sweep alerts</td><td>None</td></tr><tr><td>Multiple suppressions for one link</td><td>latest wins</td><td>None</td><td>None</td></tr><tr><td>Severity hierarchy disabled (config error)</td><td>all drifts contribute</td><td>SEV-2 may fire on superseded only</td><td>Operator</td></tr><tr><td>Per-tenant cron config invalid (e.g. <code>0 99 * * *</code>)</td><td>startup rejects</td><td>log + tenant uses default</td><td>None</td></tr><tr><td>Remediation suggestion fuzzy-search fails</td><td>fallback to empty</td><td>drift event still emitted</td><td>Operator</td></tr><tr><td>Dry-run leaks audit (bug)</td><td>property test catches</td><td>None</td><td>Author fixes</td></tr><tr><td>drift_remediated audit on no-op (false detection)</td><td>rare; matched against state</td><td>None</td><td>None</td></tr><tr><td>Trend metric direction calculation off-by-one</td><td>tested via fixtures</td><td>None</td><td>Author</td></tr><tr><td>Small-tenant fast-path crosses threshold</td><td>switches to slow path</td><td>brief reclassification</td><td>None</td></tr><tr><td>Suppress without reason</td><td>400</td><td>None</td><td>Caller</td></tr><tr><td>Tenant deleted mid-sweep</td><td>sweep aborts gracefully</td><td>None</td><td>None</td></tr><tr><td>Concurrent drift sweep + link create</td><td>new link unchecked this run</td><td>next sweep covers</td><td>None</td></tr><tr><td>Drift on archived issue</td><td>still counted (issue closed but link active)</td><td>operator reviews</td><td>None</td></tr><tr><td>memory reader 5xx burst</td><td>exp backoff retry</td><td>sweep slower; eventually completes</td><td>Operator</td></tr><tr><td>Suggested remediation with PII in path</td><td>should be redacted</td><td>event emit safe</td><td>None</td></tr><tr><td>Operator suppresses then deletes link</td><td>suppression auto-cleared</td><td>None</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>Cron is implemented via tokio-cron-scheduler in the proj-sync main loop.</li><li><code>was_shareable_at_link_create</code> queries memory audit chain for <code>meta.sync_class</code> history at the link's <code>created_at</code> timestamp.</li><li><code>has_successor</code> queries memory for rows with <code>correction_to = &lt;row_id&gt;</code>.</li><li>The 5-minute latency budget for 10K links assumes memory search latency p99 ≤ 30ms — borderline; slice 4+ may need pagination + parallel sweep.</li><li>Multi-tenant sweep iterates tenants serially to avoid memory-side rate limits.</li><li>The <code>proj.citation_drift_detected</code> audit row's <code>prior_check_at_ns</code> field is the prior sweep's timestamp from drift_state; null if no prior sweep.</li><li>Drift remediation is intentionally manual; auto-remediation pattern reserved for slice 4+.</li><li>Delta sweep uses <code>memory_links.updated_at</code> (touched on any mutation including drift_status update); <code>--since</code> filters by that.</li><li>drift_status column materialises the latest sweep result; UI badge rendering reads it in O(1).</li><li>Assignee notification queues via FR-OBS-007's CUO triage; the notification appears in the assignee's daily digest.</li><li>Suppression rows live in <code>memory_link_drift_suppressions</code> table with link_id + suppressed_at + reason + expires_at; sweep joins this table.</li><li>Severity hierarchy is implemented in the alert-decision function; not a column.</li><li>Per-tenant config lives in <code>cyberos_proj_tenant_settings</code> (extended); cron validates at startup.</li><li>Suggested remediation for TargetMissing uses FR-MEMORY-108 <code>search(path_fragments)</code> with edit-distance scoring; returns top 3.</li><li>Dry-run is a CLI flag wired through to the sweep function; toggled before audit emit + alert.</li><li><code>proj.drift_remediated</code> is emitted by the <code>remove_link</code> and (slice 4+) <code>retarget_link</code> handlers; carries <code>prior_drift_kind</code>.</li><li>Trend metric uses a 7-day rolling window stored in <code>drift_trend_state</code> table; comparison is rate-of-change.</li><li>Small-tenant fast path is selected based on <code>last_total_links_checked</code> at sweep start; threshold 1K.</li><li>Suppression auto-expiry runs in a separate cleanup task hourly.</li><li>We considered triggering sweep on every link deletion in memory but rejected: link write rate is high; sweep on every event = O(N²) churn.</li><li>Drift detection ON_DEMAND CLI is intended for operators investigating an issue (\"did this drift since last sweep?\"); per-link granular check.</li><li>The drift_state row updates atomically with the sweep completion; no partial rows.</li><li>Assignee notification dedups within 24h: one drift event per (link, assignee) per day, no spam.</li><li>The remediation <code>suggested_paths</code> array is bounded at 3; UI shows them as quick-action buttons.</li><li>We rejected real-time drift detection via memory chain hooks because: (a) added complexity; (b) batch sweep finds the same drift; (c) memory event firehose would need filtering anyway.</li></ul>\n<hr>\n<p><em>End of FR-PROJ-010.</em></p>";
