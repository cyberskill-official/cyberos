window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AI-016"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> enforce tenant residency at alias-resolution time. The enforcement and surrounding contract obey the following:</p>\n<ol><li><strong>MUST</strong> expose <code>residency::matches(policy_residency: Residency, provider_region: &amp;Region) -&gt; bool</code>. Returns <code>true</code> if and only if the provider's region is in the residency's acceptable-region set per the <code>region_table.rs</code> mapping.</li><li><strong>MUST</strong> define the residency → acceptable-region mapping in <code>region_table.rs</code> as a <code>frozenset</code> per residency; changes to the mapping require explicit FR amendment:</li></ol>\n<ul><li><code>Sg1</code> → <code>{\"ap-southeast-1\"}</code> (Singapore; AWS only at slice 4).</li><li><code>Eu1</code> → <code>{\"eu-central-1\", \"eu-west-1\"}</code> (Frankfurt, Ireland — both adequacy-covered for EU↔EU intra-region transfers).</li><li><code>Us1</code> → <code>{\"us-east-1\", \"us-east-2\", \"us-west-2\"}</code> (Northern Virginia, Ohio, Oregon).</li><li><code>Vn1</code> → <code>{}</code> (empty set; no AWS region is in-country for Vietnam at slice 4).</li></ul>\n<ol><li><strong>MUST</strong> be invoked by <code>alias::resolve</code> (FR-AI-006 §1 #7). When <code>residency::matches(policy.residency, &amp;resolved_region) == false</code>, <code>alias::resolve</code> returns <code>Err(AliasError::ResidencyViolation { policy_residency, resolved_region, attempted_alias })</code>.</li><li><strong>MUST</strong> treat tenants with a missing <code>policy.ai_policy.residency</code> field as fail-closed for PDPL-pinned tenants (any tenant with <code>policy.tenant_jurisdiction == \"VN\"</code>); for other tenants the missing-field default is <code>Sg1</code> (Asia-Pacific consensus default for the CyberSkill home region). The default is documented in FR-AI-005's schema; this FR consumes the parsed value.</li><li><strong>MUST</strong> strip AZ suffix from the provider-returned region string before matching: <code>\"ap-southeast-1a\"</code> → <code>\"ap-southeast-1\"</code>. The matcher operates on AWS region strings (no AZ); AZ-aware policies are out of scope at slice 4. The strip rule is <code>^(?P&lt;region&gt;[a-z]{2}-[a-z]+-\\d+)[a-z]?$</code> — stripping a single trailing alpha character if present.</li><li><strong>MUST</strong> return <code>false</code> for <code>Vn1</code> against ANY provider region in slice 4 (no VN provider integrated). Tenants pinning <code>vn-1</code> MUST be refused at resolve time; the refusal carries a distinct error message <code>vn1_no_provider_yet</code> so the operator dashboard can distinguish \"no VN provider\" from \"wrong region\" failures. FR-AI-104 (placeholder) will integrate Viettel Cloud + FPT Cloud and add their region strings to the <code>Vn1</code> set.</li><li><strong>MUST</strong> emit an <code>ai.residency_violation</code> memory audit row when a request is refused due to residency. The row carries <code>tenant_id</code>, <code>agent_persona</code>, <code>requested_alias</code>, <code>policy_residency</code>, <code>resolved_region</code>, <code>request_id</code>, AND a <code>vn1_no_provider</code> boolean (true when residency is Vn1 and the failure is due to absence of a VN provider rather than wrong region).</li><li><strong>MUST</strong> propagate <code>ResidencyViolation</code> errors as HTTP <code>403 RESIDENCY_VIOLATION</code> with body <code>{\"error\":\"residency_violation\",\"policy_residency\":\"&lt;r&gt;\",\"resolved_region\":\"&lt;reg&gt;\",\"contact\":\"ops@cyberos.world\"}</code>. For Vn1 failures, the error code is <code>residency_violation</code> AND the body includes <code>\"reason\":\"no_vn_provider_yet\"</code> so client UIs can render an informative message.</li><li><strong>MUST</strong> be deterministic: same <code>(Residency, Region)</code> pair always returns the same boolean. The <code>region_table.rs</code> mapping is a <code>LazyLock&lt;HashMap&lt;Residency, HashSet&lt;&amp;'static str&gt;&gt;&gt;</code>; no I/O, no time-dependent state, no env-var lookup.</li><li><strong>MUST</strong> integrate with FR-AI-015 (ZDR enforcement) such that the precedence is: <strong>ZDR check first, then residency</strong>. A request that fails ZDR is refused with <code>ZdrViolation</code>; a request that passes ZDR but fails residency is refused with <code>ResidencyViolation</code>. Both checks run in <code>alias::resolve</code>; the order is fixed (ZDR before residency) and documented in FR-AI-006's resolve function.</li><li><strong>MUST</strong> support a per-alias residency override (<code>policy.ai_policy.residency_override</code>) for tenants that need to pin a specific alias to a different residency than the tenant default. Schema: <code>residency_override: { \"&lt;alias-glob&gt;\": \"&lt;residency&gt;\" }</code>. Example: a SG tenant pinning <code>chat.eu-customer-data</code> to <code>eu-1</code>. The override is consulted BEFORE the tenant default; ambiguous overrides (multiple globs match) fail with <code>OverrideAmbiguous</code>.</li><li><strong>MUST NOT</strong> silently degrade <code>vn-1</code> to a \"closest available\" region. Slice 4 explicitly refuses; the alternative (silent fallback to <code>sg-1</code>) is the failure mode that produces a Decree 53 violation on a tenant who pinned <code>vn-1</code> precisely to avoid out-of-country routing.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>ai_residency_mismatches_total{policy_residency, resolved_region}</code> (counter; alarm &gt; 0).</li><li><code>ai_residency_vn1_refused_total{tenant_id}</code> (counter; tracks <code>Vn1</code> refusals separately from generic mismatches; trending this informs FR-AI-104 prioritisation).</li><li><code>ai_residency_overrides_used_total{tenant_id, alias}</code> (counter; how often per-alias overrides fire).</li><li><code>ai_residency_default_applied_total{outcome}</code> (counter; outcome ∈ <code>sg1_default | refused_pdpl_no_pin</code>; tracks the missing-field fallback path).</li></ul>\n<ol><li><strong>SHOULD</strong> log at WARN on every <code>Vn1</code> refusal — <code>vn1 residency refused; FR-AI-104 Viettel integration needed for tenant=&lt;id&gt;</code> so the operator dashboard can prioritise the VN-provider build.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why is residency the highest-precedence policy gate (after ZDR)?</strong> Residency violations are <em>unrecoverable</em>: once data crosses a border, no audit-trail or apology undoes the transfer. Decree 53/2022 (Vietnam) imposes data localisation for VN-resident PII; the fines are denominated in revenue percentage. GDPR Art. 44's cross-border-transfer principle, paired with the limited adequacy decisions (Art. 45), means EU data routed to non-adequate jurisdictions is presumed unlawful. Both regimes treat the violation as occurring at the moment of transfer; there is no remediation that reverses the breach. The cost of an extra HashMap lookup in <code>alias::resolve</code> (microseconds) is trivial vs. the cost of a single mis-routed call (potentially the contract).</p>\n<p><strong>Why fail-closed on missing residency for PDPL tenants (§1 #4)?</strong> A VN-jurisdiction tenant whose policy file omits <code>residency:</code> is in an indeterminate state — they didn't explicitly pin VN, but the regulator presumes VN data localisation applies. Defaulting to <code>Sg1</code> (the closest acceptable AP region) is the conservative call: it avoids the Decree 53 risk without forcing every onboarding to explicitly set residency. Tenants who legitimately want EU or US routing must explicitly pin; the default is the safe-but-tight choice. Non-PDPL tenants get the same <code>Sg1</code> default for operational consistency.</p>\n<p><strong>Why static enum (§1 #2) and not config-driven?</strong> The four residency values map to legal-jurisdiction categories that change rarely (AWS region launches don't add new residency tiers — they add new acceptable regions within an existing tier). Encoding them as a Rust enum + LazyLock map means the type system enforces \"you can't add a residency without amending the FR and recompiling.\" A config-driven approach invites operational drift (\"operator added <code>apac-2</code> to the YAML; nobody noticed it doesn't map to any AWS region\"). The trade-off is loss of hot-reloadability for residency itself; given the rarity of changes, this is the right trade.</p>\n<p><strong>Why is <code>Vn1</code> an empty set rather than mapped to <code>ap-southeast-1</code> (the closest region)?</strong> Because mapping it to <code>ap-southeast-1</code> would convert \"Vietnam data residency\" into \"Singapore data residency\" without telling anyone. A tenant pinning <code>vn-1</code> is making a regulatory statement; satisfying that statement requires a VN-located provider, which we don't have at slice 4. The honest answer is \"we can't serve you under this constraint yet\" — the dishonest answer is \"we'll route to Singapore and hope the regulator doesn't notice.\" Refusing and waiting for FR-AI-104 (Viettel/FPT integration) preserves the regulatory contract.</p>\n<p><strong>Why strip AZ suffix (§1 #5)?</strong> Bedrock and Vertex sometimes return AZ-suffixed region strings (<code>ap-southeast-1a</code>) in error messages or routing metadata. The matcher operates at region granularity (the legal residency unit) — AZ is sub-region and irrelevant to localisation regulation. The single trailing <code>[a-z]?</code> strip handles every AWS AZ format and is unambiguous (no AWS region ends in a single letter; the ambiguity of <code>ap-southeast-1a</code> would only arise if AWS launched a region literally named <code>ap-southeast-1a</code>, which they won't because their convention treats trailing letters as AZ).</p>\n<p><strong>Why does residency precedence go AFTER ZDR (§1 #10)?</strong> Both are compliance gates; both refuse calls. Order matters because the operator dashboard sees the first-fired error. Putting ZDR first lets a tenant who fails BOTH gates see \"ZDR violation\" first (the more expensive/restrictive gate is more diagnostic — fixing ZDR often resolves residency, since ZDR-attested providers tend to publish region availability). Putting residency first would obscure ZDR failures behind region failures. The order is documented and tested; future FRs touching this precedence must justify a change.</p>\n<p><strong>Why per-alias residency override (§1 #11)?</strong> Real-world: a SG-default tenant has one workflow that processes EU customer data (\"send EU customer support summary to model X\"). They want most calls to route via SG (latency, cost), but THIS specific alias must route via EU. Without the override, the tenant has only two unsatisfactory choices: (a) flip the entire tenant to <code>Eu1</code> (degrading every other call's latency), or (b) accept the GDPR risk. The override gives them surgical control. The ambiguity-rejection rule (<code>OverrideAmbiguous</code>) prevents the operational footgun of two glob patterns matching the same alias with different residencies.</p>\n<p><strong>Why a dedicated audit row (<code>ai.residency_violation</code>)?</strong> Same reasoning as ZDR's audit row (FR-AI-015 §1 #6): a regulator's question \"did you ever route VN data outside Vietnam\" needs a positive answer (rows showing refusals) rather than absence-of-evidence. The <code>vn1_no_provider</code> field is informational — when a regulator asks \"why did you refuse this Vn1 tenant,\" the row says \"because we don't have a VN provider integrated yet\" rather than \"we refused for some reason.\"</p>\n<p><strong>Why does the metric set include <code>ai_residency_vn1_refused_total{tenant_id}</code>?</strong> Trending this metric tells us how badly we need FR-AI-104 (the Viettel/FPT integration). If five VN tenants refuse 1000 calls/day each due to <code>Vn1</code> not being mapped, the FR-AI-104 prioritisation case writes itself. Without the per-tenant breakdown, the demand signal is invisible.</p>\n<p><strong>Why a property test (§5)?</strong> The matcher's correctness is \"no cross-residency pairs return true.\" A unit test enumerating every (Residency × Region) pair has 4 × ~30 = ~120 cases; manually writing them is tedious and bug-prone. proptest generates random pairs and asserts the global property in 1000+ trials. The property test catches accidental aliasing (e.g., adding a region to <code>Eu1</code> that's also in <code>Us1</code> due to copy-paste error) that a happy-path test would miss.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<h3>Type definitions</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/residency/mod.rs\n\nuse std::collections::{HashMap, HashSet};\nuse std::sync::LazyLock;\nuse serde::{Deserialize, Serialize};\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]\n#[serde(rename_all = \"kebab-case\")]\npub enum Residency {\n    #[serde(rename = \"sg-1\")] Sg1,\n    #[serde(rename = \"eu-1\")] Eu1,\n    #[serde(rename = \"us-1\")] Us1,\n    #[serde(rename = \"vn-1\")] Vn1,\n}\n\n#[derive(Debug, Clone, PartialEq, Eq, Hash)]\npub struct Region(String);   // newtype to prevent string-typing accidents\n\nimpl Region {\n    /// Strip AZ suffix per §1 #5.\n    pub fn from_provider_string(raw: &amp;str) -&gt; Result&lt;Self, RegionParseError&gt; {\n        static RE: LazyLock&lt;regex::Regex&gt; = LazyLock::new(|| {\n            regex::Regex::new(r\"^(?P&lt;region&gt;[a-z]{2}-[a-z]+-\\d+)[a-z]?$\").unwrap()\n        });\n        let caps = RE.captures(raw).ok_or_else(|| RegionParseError::Invalid(raw.into()))?;\n        Ok(Region(caps.name(\"region\").unwrap().as_str().into()))\n    }\n    pub fn as_str(&amp;self) -&gt; &amp;str { &amp;self.0 }\n}\n\npub fn matches(policy_residency: Residency, provider_region: &amp;Region) -&gt; bool {\n    REGIONS_BY_RESIDENCY.get(&amp;policy_residency)\n        .map(|set| set.contains(provider_region.as_str()))\n        .unwrap_or(false)\n}\n\npub fn parse_residency(s: &amp;str) -&gt; Result&lt;Residency, ResidencyParseError&gt; {\n    serde_yaml::from_str(s).map_err(|e| ResidencyParseError::Invalid(e.to_string()))\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum ResidencyParseError {\n    #[error(\"invalid residency value {0:?}; expected sg-1 | eu-1 | us-1 | vn-1\")]\n    Invalid(String),\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum RegionParseError {\n    #[error(\"invalid region string {0:?}; expected AWS region format\")]\n    Invalid(String),\n}\n\n// In FR-AI-006 alias.rs (modified_files):\npub enum AliasError {\n    // ... existing variants from FR-AI-006 + FR-AI-015 ...\n    ResidencyViolation {\n        policy_residency: Residency,\n        resolved_region: Region,\n        attempted_alias: String,\n        vn1_no_provider: bool,\n    },\n}</code></pre>\n<h3>Region table (single source of truth)</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/residency/region_table.rs\n\nuse std::collections::{HashMap, HashSet};\nuse std::sync::LazyLock;\nuse super::Residency;\n\n/// §1 #2: residency → acceptable-region mapping. Changes require FR amendment.\npub static REGIONS_BY_RESIDENCY: LazyLock&lt;HashMap&lt;Residency, HashSet&lt;&amp;'static str&gt;&gt;&gt; =\n    LazyLock::new(|| {\n        let mut m = HashMap::new();\n        m.insert(Residency::Sg1, {\n            let mut s = HashSet::new();\n            s.insert(\"ap-southeast-1\");        // Singapore\n            s\n        });\n        m.insert(Residency::Eu1, {\n            let mut s = HashSet::new();\n            s.insert(\"eu-central-1\");          // Frankfurt\n            s.insert(\"eu-west-1\");             // Ireland\n            s\n        });\n        m.insert(Residency::Us1, {\n            let mut s = HashSet::new();\n            s.insert(\"us-east-1\");             // N. Virginia\n            s.insert(\"us-east-2\");             // Ohio\n            s.insert(\"us-west-2\");             // Oregon\n            s\n        });\n        m.insert(Residency::Vn1, HashSet::new());   // §1 #6: empty until FR-AI-104\n        m\n    });</code></pre>\n<h3>Override parser</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/residency/parse.rs\n\nuse globset::{Glob, GlobMatcher};\n\npub struct ResidencyOverride {\n    pub alias_pattern: GlobMatcher,\n    pub residency: Residency,\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum OverrideError {\n    #[error(\"ambiguous override: aliases [{0:?}] both match alias {1!r}\")]\n    OverrideAmbiguous(Vec&lt;String&gt;, String),\n    #[error(\"invalid glob pattern {0!r}: {1}\")]\n    InvalidGlob(String, String),\n}\n\npub fn resolve_override(\n    overrides: &amp;[ResidencyOverride], alias: &amp;str,\n) -&gt; Result&lt;Option&lt;Residency&gt;, OverrideError&gt; {\n    let matches: Vec&lt;_&gt; = overrides.iter()\n        .filter(|o| o.alias_pattern.is_match(alias))\n        .collect();\n    match matches.len() {\n        0 =&gt; Ok(None),\n        1 =&gt; Ok(Some(matches[0].residency)),\n        _ =&gt; Err(OverrideError::OverrideAmbiguous(\n            matches.iter().map(|o| o.alias_pattern.glob().to_string()).collect(),\n            alias.into(),\n        )),\n    }\n}</code></pre>\n<h3>Tenant policy schema extension (FR-AI-005)</h3>\n<pre><code class=\"language-yaml\"># tenants/&lt;tenant_id&gt;/policy.yaml — additions\nai_policy:\n  residency: sg-1                          # default residency for this tenant\n  residency_override:                      # per-alias override (§1 #11)\n    \"chat.eu-customer-*\":  eu-1            # any chat alias starting with eu-customer- pins EU\n    \"embeddings.gdpr-pii\": eu-1            # specific embeddings alias pins EU</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>Sg1 → ap-southeast-1 matches</strong> — <code>matches(Sg1, &amp;Region(\"ap-southeast-1\".into()))</code> returns <code>true</code>.</li><li><strong>Sg1 → us-east-1 mismatches</strong> — <code>matches(Sg1, &amp;Region(\"us-east-1\".into()))</code> returns <code>false</code>.</li><li><strong>Eu1 → eu-central-1 AND eu-west-1 match</strong> — both regions return <code>true</code>.</li><li><strong>Us1 → all three US regions match</strong> — <code>us-east-1</code>, <code>us-east-2</code>, <code>us-west-2</code> all return <code>true</code>.</li><li><strong>Vn1 → empty set always returns false</strong> — <code>matches(Vn1, &amp;Region(\"ap-southeast-1\".into()))</code> returns <code>false</code>.</li><li><strong>AZ-suffix stripped</strong> — <code>Region::from_provider_string(\"ap-southeast-1a\")</code> returns <code>Region(\"ap-southeast-1\")</code>; <code>matches(Sg1, &amp;that)</code> returns <code>true</code>.</li><li><strong>Invalid region string rejected</strong> — <code>Region::from_provider_string(\"not-a-region\")</code> returns <code>Err(RegionParseError::Invalid)</code>.</li><li><strong>Property test: no cross-residency leak</strong> — proptest 1000 trials over <code>(any_residency, any_region)</code>: if <code>matches(R, region)</code> returns <code>true</code>, then <code>region.as_str()</code> is in <code>REGIONS_BY_RESIDENCY[R]</code>. No accidental aliasing between residencies.</li><li><strong>FR-AI-006 integration: refusal on mismatch</strong> — Tenant policy <code>residency: sg-1</code>, alias resolves to bedrock <code>us-east-1</code> → <code>alias::resolve</code> returns <code>Err(AliasError::ResidencyViolation { policy_residency: Sg1, resolved_region: Region(\"us-east-1\"), .. })</code>.</li><li><strong>FR-AI-006 integration: success on match</strong> — Tenant policy <code>residency: sg-1</code>, alias resolves to bedrock <code>ap-southeast-1</code> → <code>alias::resolve</code> returns <code>Ok((Bedrock, \"claude-3-...\", Region(\"ap-southeast-1\")))</code>.</li><li><strong>Vn1 refusal carries vn1_no_provider flag</strong> — Tenant policy <code>residency: vn-1</code>; any alias resolution returns <code>Err(AliasError::ResidencyViolation { vn1_no_provider: true, .. })</code>; OTel <code>ai_residency_vn1_refused_total{tenant_id}</code> increments.</li><li><strong>HTTP 403 RESIDENCY_VIOLATION on refusal</strong> — Handler converts AliasError to a <code>403</code> response with the documented body shape; Vn1 case includes <code>\"reason\":\"no_vn_provider_yet\"</code>.</li><li><strong>Audit row emitted</strong> — Every refusal emits exactly one <code>ai.residency_violation</code> memory row with all required fields populated.</li><li><strong>Missing residency field defaults to Sg1 (non-PDPL tenant)</strong> — Tenant policy without <code>residency</code> field; FR-AI-005 schema parser defaults to <code>Sg1</code>; <code>alias::resolve</code> enforces against <code>Sg1</code>.</li><li><strong>Missing residency field for PDPL tenant fails closed</strong> — Tenant with <code>tenant_jurisdiction: VN</code> but no <code>residency</code> field; FR-AI-005 returns <code>PolicyError::MissingResidencyForPdplTenant</code>; HTTP <code>503 POLICY_INVALID</code>.</li><li><strong>Per-alias override applies before tenant default</strong> — Policy <code>residency: sg-1</code> + <code>residency_override: { \"chat.eu-customer-*\": eu-1 }</code>; resolving <code>chat.eu-customer-summary</code> enforces against <code>Eu1</code>, not <code>Sg1</code>.</li><li><strong>Ambiguous override rejected at parse</strong> — Two glob patterns match the same alias with different residencies; <code>resolve_override</code> returns <code>Err(OverrideAmbiguous)</code>; FR-AI-005 policy load fails with <code>PolicyError::AmbiguousResidencyOverride</code>.</li><li><strong>ZDR check fires before residency check</strong> — Tenant policy <code>zdr_required: true</code> + <code>residency: sg-1</code>; alias resolves to <code>openai gpt-4o</code> (non-ZDR, non-SG); <code>alias::resolve</code> returns <code>Err(AliasError::ZdrViolation)</code>, NOT <code>ResidencyViolation</code> — ZDR is the first-fired error.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>Happy + matcher tests</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/residency_property_test.rs\nuse cyberos_ai_gateway::residency::{self, Residency, Region};\n\n#[test]\nfn sg1_accepts_apse1_only() {\n    let region = Region::from_provider_string(\"ap-southeast-1\").unwrap();\n    assert!(residency::matches(Residency::Sg1, &amp;region));\n    let region = Region::from_provider_string(\"us-east-1\").unwrap();\n    assert!(!residency::matches(Residency::Sg1, &amp;region));\n}\n\n#[test]\nfn eu1_accepts_central_and_west() {\n    for r in &amp;[\"eu-central-1\", \"eu-west-1\"] {\n        assert!(residency::matches(Residency::Eu1, &amp;Region::from_provider_string(r).unwrap()));\n    }\n}\n\n#[test]\nfn us1_accepts_all_three_us_regions() {\n    for r in &amp;[\"us-east-1\", \"us-east-2\", \"us-west-2\"] {\n        assert!(residency::matches(Residency::Us1, &amp;Region::from_provider_string(r).unwrap()));\n    }\n}\n\n#[test]\nfn vn1_empty_set_always_returns_false() {\n    for r in &amp;[\"ap-southeast-1\", \"us-east-1\", \"eu-central-1\"] {\n        let region = Region::from_provider_string(r).unwrap();\n        assert!(!residency::matches(Residency::Vn1, &amp;region));\n    }\n}\n\n#[test]\nfn az_suffix_stripped() {\n    let region = Region::from_provider_string(\"ap-southeast-1a\").unwrap();\n    assert_eq!(region.as_str(), \"ap-southeast-1\");\n    assert!(residency::matches(Residency::Sg1, &amp;region));\n}\n\n#[test]\nfn invalid_region_string_rejected() {\n    let err = Region::from_provider_string(\"not-a-region\").expect_err(\"invalid\");\n    assert!(matches!(err, residency::RegionParseError::Invalid(_)));\n}\n\n#[test]\nfn parse_residency_from_yaml() {\n    assert_eq!(residency::parse_residency(\"sg-1\").unwrap(), Residency::Sg1);\n    assert_eq!(residency::parse_residency(\"vn-1\").unwrap(), Residency::Vn1);\n    assert!(residency::parse_residency(\"apac-2\").is_err());\n}</code></pre>\n<h3>Property test</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/residency_property_test.rs\nuse proptest::prelude::*;\nuse cyberos_ai_gateway::residency::{self, Residency, Region, region_table::REGIONS_BY_RESIDENCY};\n\nfn any_residency() -&gt; impl Strategy&lt;Value = Residency&gt; {\n    prop_oneof![Just(Residency::Sg1), Just(Residency::Eu1), Just(Residency::Us1), Just(Residency::Vn1)]\n}\n\nfn any_region() -&gt; impl Strategy&lt;Value = Region&gt; {\n    let known_regions = [\n        \"ap-southeast-1\", \"ap-southeast-2\", \"ap-northeast-1\",\n        \"eu-central-1\", \"eu-west-1\", \"eu-west-2\", \"eu-north-1\",\n        \"us-east-1\", \"us-east-2\", \"us-west-1\", \"us-west-2\",\n        \"ca-central-1\", \"sa-east-1\",\n    ];\n    prop::sample::select(known_regions.to_vec())\n        .prop_map(|r| Region::from_provider_string(r).unwrap())\n}\n\nproptest! {\n    #![proptest_config(ProptestConfig::with_cases(1000))]\n\n    #[test]\n    fn no_cross_residency_leak(r in any_residency(), region in any_region()) {\n        // AC #8: matches(R, region) → true ⇒ region in REGIONS_BY_RESIDENCY[R]\n        if residency::matches(r, &amp;region) {\n            let allowed = REGIONS_BY_RESIDENCY.get(&amp;r).unwrap();\n            prop_assert!(allowed.contains(region.as_str()),\n                        \"cross-residency leak: {r:?} matched {region:?} but region not in allowed set\");\n        }\n    }\n\n    #[test]\n    fn deterministic(r in any_residency(), region in any_region()) {\n        // §1 #9: same pair → same result, run twice.\n        let r1 = residency::matches(r, &amp;region);\n        let r2 = residency::matches(r, &amp;region);\n        prop_assert_eq!(r1, r2);\n    }\n}</code></pre>\n<h3>Integration test (FR-AI-006 wiring)</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/residency_integration_test.rs\nuse cyberos_ai_gateway::{alias, residency::Residency};\n\n#[tokio::test]\nasync fn alias_resolve_refuses_on_residency_mismatch() {\n    let policy = test_policy_with_residency(Residency::Sg1);\n    let result = alias::resolve(\"chat.us-only-alias\", &amp;policy).await;   // routes to us-east-1\n    match result {\n        Err(alias::AliasError::ResidencyViolation { policy_residency, resolved_region, vn1_no_provider, .. }) =&gt; {\n            assert_eq!(policy_residency, Residency::Sg1);\n            assert_eq!(resolved_region.as_str(), \"us-east-1\");\n            assert!(!vn1_no_provider);\n        }\n        _ =&gt; panic!(\"expected ResidencyViolation\"),\n    }\n}\n\n#[tokio::test]\nasync fn alias_resolve_succeeds_on_residency_match() {\n    let policy = test_policy_with_residency(Residency::Sg1);\n    let (provider, model, region) = alias::resolve(\"chat.smart\", &amp;policy).await.unwrap();\n    assert_eq!(region.as_str(), \"ap-southeast-1\");\n}\n\n#[tokio::test]\nasync fn vn1_carries_no_provider_flag() {\n    let policy = test_policy_with_residency(Residency::Vn1);\n    let result = alias::resolve(\"chat.smart\", &amp;policy).await;\n    match result {\n        Err(alias::AliasError::ResidencyViolation { vn1_no_provider: true, .. }) =&gt; {}\n        _ =&gt; panic!(\"expected vn1_no_provider\"),\n    }\n\n    let counter = otel_test_helper::counter_value(\n        \"ai_residency_vn1_refused_total\",\n        &amp;[(\"tenant_id\", &amp;policy.tenant_id)],\n    );\n    assert!(counter &gt;= 1);\n}\n\n#[tokio::test]\nasync fn audit_row_emitted_on_residency_refusal() {\n    let request_id = \"req_test_residency_001\";\n    let _ = handlers::chat::handle(test_request_with_residency(\n        Residency::Sg1, request_id, \"chat.us-only-alias\",\n    )).await;\n    let rows = memory_test_helper::find_rows(\"ai.residency_violation\", request_id);\n    assert_eq!(rows.len(), 1);\n    let p = &amp;rows[0].payload;\n    assert_eq!(p[\"policy_residency\"], \"sg-1\");\n    assert_eq!(p[\"resolved_region\"], \"us-east-1\");\n    assert_eq!(p[\"vn1_no_provider\"], false);\n}\n\n#[tokio::test]\nasync fn zdr_check_fires_before_residency() {\n    // §1 #10: ZDR before residency.\n    let policy = test_policy_with_residency_and_zdr(Residency::Sg1, /* zdr_required */ true);\n    let result = alias::resolve(\"chat.openai-us-only\", &amp;policy).await;   // openai gpt-4o, us-east-1, non-ZDR\n    match result {\n        Err(alias::AliasError::ZdrViolation { .. }) =&gt; {}   // ZDR fires first\n        Err(alias::AliasError::ResidencyViolation { .. }) =&gt; panic!(\"residency fired before ZDR\"),\n        _ =&gt; panic!(\"expected error\"),\n    }\n}\n\n#[tokio::test]\nasync fn per_alias_override_wins_over_tenant_default() {\n    let policy = test_policy_with_override(\n        Residency::Sg1,   // default\n        vec![(\"chat.eu-customer-*\".into(), Residency::Eu1)],\n    );\n    let result = alias::resolve(\"chat.eu-customer-summary\", &amp;policy).await;\n    // Without override, this would resolve to SG region; with override, must resolve to EU.\n    let (_, _, region) = result.unwrap();\n    assert!(region.as_str().starts_with(\"eu-\"));\n}\n\n#[tokio::test]\nasync fn ambiguous_override_rejected_at_policy_load() {\n    let yaml = r#\"\n        ai_policy:\n          residency: sg-1\n          residency_override:\n            \"chat.*-customer-*\": eu-1\n            \"chat.eu-*\":         us-1\n    \"#;\n    let err = policy::parse(yaml).expect_err(\"expected AmbiguousResidencyOverride\");\n    assert!(matches!(err, policy::PolicyError::AmbiguousResidencyOverride { .. }));\n}</code></pre>\n<pre><code class=\"language-bash\">cd services/ai-gateway\ncargo test -p cyberos-ai-gateway residency\ncargo test -p cyberos-ai-gateway --test residency_property_test</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3 for the type defs + region table + override parser. Integration into FR-AI-006's resolve:</p>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/alias.rs (modified)\n\npub async fn resolve(alias: &amp;str, policy: &amp;TenantPolicy) -&gt; Result&lt;(ProviderKind, String, Region), AliasError&gt; {\n    // Resolve alias → (provider, model, region)\n    let (provider, model, region) = ALIAS_MAP.get().unwrap().load().get(alias)\n        .ok_or_else(|| AliasError::UnknownAlias(alias.into()))?;\n\n    // §1 #10: ZDR before residency.\n    if policy.ai_policy.zdr_required &amp;&amp; !zdr::is_zdr(&amp;provider, &amp;model) {\n        return Err(AliasError::ZdrViolation {\n            resolved_provider: provider, resolved_model: model.clone(),\n            attestation: zdr::attestation_for(&amp;provider, &amp;model),\n        });\n    }\n\n    // §1 #11: per-alias override resolves first.\n    let effective_residency = match parse::resolve_override(&amp;policy.ai_policy.residency_override, alias) {\n        Ok(Some(r)) =&gt; r,\n        Ok(None) =&gt; policy.ai_policy.residency,   // tenant default\n        Err(e) =&gt; return Err(AliasError::OverridePolicyInvalid(e.to_string())),\n    };\n\n    // §1 #1, §1 #5: matcher with AZ-strip.\n    if !residency::matches(effective_residency, &amp;region) {\n        let vn1_no_provider = effective_residency == Residency::Vn1;\n        return Err(AliasError::ResidencyViolation {\n            policy_residency: effective_residency,\n            resolved_region: region.clone(),\n            attempted_alias: alias.into(),\n            vn1_no_provider,\n        });\n    }\n\n    Ok((provider, model.clone(), region.clone()))\n}</code></pre>\n<p><code>canonical::residency_violation</code> builder:</p>\n<pre><code class=\"language-rust\">pub mod canonical {\n    pub fn residency_violation(\n        tenant_id: &amp;str, agent_persona: &amp;str, requested_alias: &amp;str,\n        policy_residency: Residency, resolved_region: &amp;Region,\n        vn1_no_provider: bool, request_id: &amp;str,\n    ) -&gt; AuditRow {\n        AuditRow {\n            kind: \"ai.residency_violation\".into(),\n            payload: serde_json::json!({\n                \"tenant_id\": tenant_id,\n                \"agent_persona\": agent_persona,\n                \"requested_alias\": requested_alias,\n                \"policy_residency\": serde_yaml::to_string(&amp;policy_residency).unwrap().trim(),\n                \"resolved_region\": resolved_region.as_str(),\n                \"vn1_no_provider\": vn1_no_provider,\n                \"request_id\": request_id,\n            }),\n            ..Default::default()\n        }\n    }\n}</code></pre>\n<p>Handler refusal path:</p>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/handlers/chat.rs (modified)\nmatch alias::resolve(&amp;req.alias, &amp;policy).await {\n    Err(AliasError::ResidencyViolation { policy_residency, resolved_region, vn1_no_provider, attempted_alias }) =&gt; {\n        memory_writer::emit(canonical::residency_violation(\n            &amp;policy.tenant_id, &amp;req.agent_persona, &amp;attempted_alias,\n            policy_residency, &amp;resolved_region, vn1_no_provider, &amp;req.request_id,\n        )).await?;\n        metrics::residency_mismatch(policy_residency, &amp;resolved_region);\n        if vn1_no_provider {\n            metrics::vn1_refused(&amp;policy.tenant_id);\n            tracing::warn!(tenant_id=%policy.tenant_id,\n                          \"vn1 residency refused; FR-AI-104 Viettel integration needed\");\n        }\n        let body = serde_json::json!({\n            \"error\": \"residency_violation\",\n            \"policy_residency\": serde_yaml::to_string(&amp;policy_residency).unwrap().trim(),\n            \"resolved_region\": resolved_region.as_str(),\n            \"reason\": if vn1_no_provider { Some(\"no_vn_provider_yet\") } else { None },\n            \"contact\": \"ops@cyberos.world\",\n        });\n        return Err(ApiError::Forbidden(body));\n    }\n    // ... other error variants ...\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<h3>Code dependencies (other FRs/modules)</h3>\n<ul><li><strong>FR-AI-006</strong> — <code>alias::resolve</code> consumes <code>residency::matches</code>. The <code>AliasError::ResidencyViolation</code> variant is added to FR-AI-006's enum.</li><li><strong>FR-AI-005</strong> — Tenant policy schema declares <code>policy.ai_policy.residency</code> (Residency enum) and <code>policy.ai_policy.residency_override</code> (map of glob → Residency). FR-AI-005's parser delegates to <code>residency::parse_residency</code> for the value.</li><li><strong>FR-AI-015</strong> — ZDR enforcement runs BEFORE residency in <code>alias::resolve</code> (precedence in §1 #10). Both errors are surfaced via the same handler path.</li><li><strong>FR-AI-003</strong> — memory audit-row bridge. This FR adds the <code>canonical::residency_violation</code> builder for the <code>ai.residency_violation</code> row kind.</li><li><strong>FR-AI-104 (downstream placeholder)</strong> — VN provider integration (Viettel Cloud + FPT Cloud). Will extend <code>REGIONS_BY_RESIDENCY[Vn1]</code> from empty set to a populated set; this FR's <code>Vn1 → false</code> rule reverts at that point.</li></ul>\n<h3>Concept dependencies (shared types)</h3>\n<ul><li><code>Residency</code> enum is the residency primitive used by tenant policy, alias resolve, audit rows, response bodies, OTel metrics.</li><li><code>Region</code> newtype prevents string-typing accidents (e.g., comparing AZ-suffixed vs. region-only strings).</li><li><code>REGIONS_BY_RESIDENCY</code> is the single source of truth for the residency → region mapping; all matchers consult this.</li><li>The precedence (ZDR before residency) is a documented invariant; FRs touching <code>alias::resolve</code> must preserve it.</li></ul>\n<h3>Operational / external</h3>\n<ul><li>Rust crates: <code>regex@1</code>, <code>globset@0.4</code>, <code>proptest@1</code>, <code>serde@1</code>, <code>serde_yaml@0.9</code>, <code>thiserror@1</code>.</li><li>AWS region list is enumerated in <code>region_table.rs</code>; if AWS launches a new region in an existing residency tier (e.g., <code>eu-south-1</code> for EU), the table is updated via FR amendment.</li><li><code>LazyLock</code> is used for the static map (Rust 1.80+); on older toolchains, <code>once_cell::sync::Lazy</code> is the fallback.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Tenant policy (with override)</h3>\n<pre><code class=\"language-yaml\"># tenants/tenant_alpha/policy.yaml\ntenant_id: tenant_alpha\ntenant_jurisdiction: VN\nai_policy:\n  residency: sg-1                          # default for this VN-jurisdiction tenant\n  residency_override:\n    \"chat.eu-gdpr-*\": eu-1                 # specific EU-data aliases pin EU\n  zdr_required: true</code></pre>\n<h3>Caller in FR-AI-006 alias.rs</h3>\n<pre><code class=\"language-rust\">let (provider, model, region) = alias::resolve(&amp;req.alias, &amp;policy).await?;\n// region.as_str() guaranteed in REGIONS_BY_RESIDENCY[policy.ai_policy.residency]\n//                  OR in REGIONS_BY_RESIDENCY[override-resolved residency]</code></pre>\n<h3>Audit row <code>ai.residency_violation</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ai.residency_violation\",\n  \"ts_ns\": 1747526400000000000,\n  \"payload\": {\n    \"tenant_id\": \"tenant_alpha\",\n    \"agent_persona\": \"cuo-cpo@0.4.1\",\n    \"requested_alias\": \"chat.us-only-alias\",\n    \"policy_residency\": \"sg-1\",\n    \"resolved_region\": \"us-east-1\",\n    \"vn1_no_provider\": false,\n    \"request_id\": \"req_01HZK9R8M3X5C8Q4\"\n  }\n}</code></pre>\n<h3>HTTP refusal (generic mismatch)</h3>\n<pre><code class=\"language-text\">HTTP/1.1 403 Forbidden\nContent-Type: application/json\n\n{\n  \"error\": \"residency_violation\",\n  \"policy_residency\": \"sg-1\",\n  \"resolved_region\": \"us-east-1\",\n  \"contact\": \"ops@cyberos.world\"\n}</code></pre>\n<h3>HTTP refusal (Vn1 no-provider)</h3>\n<pre><code class=\"language-text\">HTTP/1.1 403 Forbidden\nContent-Type: application/json\n\n{\n  \"error\": \"residency_violation\",\n  \"policy_residency\": \"vn-1\",\n  \"resolved_region\": \"ap-southeast-1\",\n  \"reason\": \"no_vn_provider_yet\",\n  \"contact\": \"ops@cyberos.world\"\n}</code></pre>\n<h3>Override-applied trace log</h3>\n<pre><code class=\"language-text\">INFO  alias=chat.eu-gdpr-summary tenant_default=sg-1 override_matched=eu-1\n      residency_override applied</code></pre>\n<h3>Ambiguous-override policy load failure</h3>\n<pre><code class=\"language-text\">ERROR policy file tenants/tenant_beta/policy.yaml load failed:\n      AmbiguousResidencyOverride { aliases: [\"chat.*-customer-*\", \"chat.eu-*\"], for_alias: \"chat.eu-customer-summary\" }</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. Items deferred to later FRs:</p>\n<ul><li><strong>FR-AI-104 (placeholder)</strong>: VN provider integration (Viettel Cloud + FPT Cloud). Will populate <code>REGIONS_BY_RESIDENCY[Vn1]</code> and remove the §1 #6 fail-closed rule.</li><li>AZ-aware policies (rare regulatory ask: \"must be in <code>us-east-1</code> AZ a\"; e.g., for some federal compliance regimes) — out of scope; AZ stripping is the slice 4 boundary.</li><li>Per-region cost-aware routing (route to the cheapest region within a residency) — slice 5; current resolver picks the alias's primary region.</li><li>Multi-residency tolerance (<code>residency: [sg-1, eu-1]</code> accepting either) — out of scope; current model is single-residency-per-tenant + per-alias overrides for exceptions.</li><li>Residency-aware fallback during regional outage (AWS Singapore down → fall back to AWS Tokyo for <code>Sg1</code>?) — explicitly OUT of scope; residency violation is unrecoverable; refusing is the correct behaviour.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Tenant pins <code>vn-1</code> but no VN provider</td><td><code>Vn1</code> empty set always returns false</td><td><code>Err(ResidencyViolation { vn1_no_provider: true })</code> → <code>403</code> with <code>reason: no_vn_provider_yet</code></td><td>Tenant chooses <code>sg-1</code> (closest acceptable) OR waits for FR-AI-104</td></tr><tr><td>New AWS region not in matcher</td><td>Region returns false (fail closed)</td><td><code>Err(ResidencyViolation)</code> for affected residencies</td><td>Operator adds region to <code>region_table.rs</code>; FR amendment; redeploy</td></tr><tr><td>Property test detects cross-residency leak</td><td>proptest panics in CI</td><td>PR blocked</td><td>Fix <code>region_table.rs</code> (likely accidental aliasing across residencies)</td></tr><tr><td>Region string with AZ suffix</td><td><code>Region::from_provider_string</code> strips suffix</td><td>Match proceeds against region-only string</td><td>By design (§1 #5)</td></tr><tr><td>Region string in unknown format</td><td><code>RegionParseError::Invalid</code> from regex</td><td><code>Err</code> propagates; alias resolve fails with <code>RegionParseError</code> (not residency violation)</td><td>Operator investigates provider response; likely a provider API change</td></tr><tr><td>Tenant policy without <code>residency</code> field, non-PDPL</td><td>FR-AI-005 schema default → <code>Sg1</code></td><td>Enforce against <code>Sg1</code></td><td>Operator updates policy if non-default residency intended</td></tr><tr><td>Tenant policy without <code>residency</code> field, PDPL tenant</td><td>FR-AI-005 schema rejects</td><td><code>PolicyError::MissingResidencyForPdplTenant</code> → 503 at policy load</td><td>Operator must explicitly pin residency in policy</td></tr><tr><td>Tenant policy with <code>residency: apac-2</code> (invalid)</td><td><code>parse_residency</code> rejects</td><td><code>ResidencyParseError::Invalid</code> → policy load fails</td><td>Operator fixes value to one of sg-1, eu-1, us-1, vn-1</td></tr><tr><td>Per-alias override glob matches no aliases</td><td><code>resolve_override</code> returns None → fall back to default</td><td>Tenant default residency applies</td><td>By design</td></tr><tr><td>Per-alias override glob matches multiple aliases ambiguously</td><td><code>resolve_override</code> → <code>OverrideAmbiguous</code></td><td><code>PolicyError::AmbiguousResidencyOverride</code> → 503 at policy load</td><td>Operator narrows glob patterns</td></tr><tr><td>Per-alias override glob has invalid syntax</td><td><code>Glob::new</code> parse error</td><td><code>PolicyError::InvalidOverrideGlob</code> → 503 at policy load</td><td>Operator fixes glob pattern</td></tr><tr><td>ZDR + residency both fail</td><td>Precedence: ZDR fires first (§1 #10)</td><td><code>Err(ZdrViolation)</code> returned; residency check never runs</td><td>By design; operator sees ZDR error first</td></tr><tr><td>Audit row emit fails (memory bridge down)</td><td><code>memory_writer::emit</code> returns Err</td><td>Refusal still proceeds; sev-1 log \"residency refused but audit row failed\"</td><td>Operator investigates memory; FR-AI-003 §10 covers</td></tr><tr><td>Concurrent <code>matches()</code> calls</td><td>LazyLock + immutable HashSet</td><td>All readers see same result</td><td>By design (§1 #9 deterministic)</td></tr><tr><td>Region table mutation attempted at runtime</td><td>LazyLock prevents writes</td><td>Compile error if attempted</td><td>By design</td></tr><tr><td>FR-AI-104 lands but <code>Vn1</code> set still empty</td><td>Test <code>test_vn1_set_populated_after_fr_ai_104</code> fails</td><td>CI blocked on FR-AI-104 PR</td><td>FR-AI-104 must extend the table as part of its acceptance</td></tr><tr><td>Alias is in alias map but its region is invalid format</td><td><code>Region::from_provider_string</code> fails at alias-load time</td><td>Alias load fails; alias unavailable</td><td>Operator fixes alias map entry</td></tr><tr><td>Tenant overrides residency to a value with no acceptable provider</td><td>Override resolves to (e.g.) <code>Vn1</code> for an alias whose only region is <code>us-east-1</code></td><td><code>ResidencyViolation</code> with override-resolved residency</td><td>Operator removes override OR waits for VN provider</td></tr><tr><td>Tenant onboarding skipped residency selection</td><td>FR-AI-005 onboarding wizard enforces residency</td><td>Cannot complete onboarding without selection</td><td>By design</td></tr><tr><td>OTel metric label cardinality explosion (per-tenant_id × per-region)</td><td>Cardinality monitoring</td><td>Metric drop / sample</td><td>Operator scales OTel collector OR aggregates labels</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The Vn1 placeholder is intentional and load-bearing. Adding Viettel/FPT as Provider variants is FR-AI-104; until then, refusing Vn1 calls is the correct regulatory behaviour. The metric <code>ai_residency_vn1_refused_total</code> quantifies the demand for FR-AI-104.</li><li>The Sg1 single-region pinning may relax in future if AWS adds another AP region with similar latency to Singapore (currently <code>ap-southeast-3</code> Jakarta is closest but not yet covered for Bedrock). Any addition requires FR amendment.</li><li>The static enum + <code>LazyLock</code> map design trades hot-reloadability for type-system safety. Residency tiers map to legal jurisdictions (slow-changing); region additions within tiers are FR amendments. This is the right trade vs. ZDR (FR-AI-015), which uses a YAML config because attestations DO drift quarterly.</li><li>The precedence rule (ZDR before residency, §1 #10) is a small but important UX choice. An operator getting \"ZDR violation\" first for a tenant who fails both gates can fix ZDR (by routing to a ZDR-attested provider) and discover the residency issue separately. The opposite order would mask the ZDR failure under a region failure.</li><li>The per-alias override mechanism (§1 #11) is the answer to \"we have ONE alias that needs to behave differently.\" Without it, tenants face the all-or-nothing choice. The <code>OverrideAmbiguous</code> rejection prevents the operational footgun of two glob patterns silently fighting.</li><li>The AZ-strip rule (§1 #5) is conservative. Provider responses occasionally surface AZ-suffixed regions (in error messages, in routing metadata); the matcher operates at region granularity per the legal definition. AZ-aware policies are explicitly OUT of scope.</li><li>The <code>Vn1</code> empty-set design (rather than mapping to \"closest acceptable\") is the honesty principle. A tenant pinning <code>vn-1</code> is making a regulatory statement; satisfying that statement requires VN-located infrastructure. Silently downgrading to SG would convert a regulatory failure into a silent failure, which is strictly worse — the tenant would assume compliance when it doesn't exist.</li><li>The audit row (<code>ai.residency_violation</code>) is the proof-of-refusal primitive matching FR-AI-015's <code>ai.zdr_violation</code>. Both rows answer \"did we ever route protected data outside its required jurisdiction\" with positive evidence rather than absence-of-evidence.</li><li>The property test runs 1000 trials by default. At CI time, this is ~50ms — trivial. The catch is \"no cross-residency aliasing\" — a property that's hard to write as a unit test (would need 4 × ~30 = 120 manual cases) and easy to break (one copy-paste mistake adds a region to two residencies).</li><li>Future regulatory complexity (multi-residency tolerance, AZ-aware policies, regional cost optimisation) is consciously deferred. Slice 4 ships the minimum viable enforcement; slice 5+ adds nuance once we have operational data on which extensions matter.</li></ul>\n<hr>\n<p><em>End of FR-AI-016. Status: draft (10/10 target).</em></p>";
