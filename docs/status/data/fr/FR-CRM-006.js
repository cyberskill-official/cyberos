window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CRM-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CRM service <strong>MUST</strong> ship lead scoring at <code>services/crm/src/scoring/</code> computing 0-100 score + tier on contact-create + nightly refresh, immutable snapshots, 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> hook into contact creation (<code>services/crm/src/contacts.rs</code>): on insert, call <code>scoring::score_initial(contact_id)</code> asynchronously per DEC-1660.</li></ol>\n<ol><li><strong>MUST</strong> run nightly refresh per DEC-1660 at <code>nightly_refresh.rs::refresh_all(tenant)</code> triggered by FR-MCP-007 cron at 02:00 tenant_tz — refresh all active contacts.</li></ol>\n<ol><li><strong>MUST</strong> build signals at <code>signal_builder.rs::build(contact_id)</code> per DEC-1662:</li></ol>\n<ul><li>Title seniority (Director / VP / C-suite weight higher)</li><li>Engagement count last 30d (from FR-CRM-002 activity feed)</li><li>Response rate (replies / sends ratio)</li><li>Account tier match (account-level signal)</li><li>Deal-history similarity (similar past contacts that won)</li></ul>\n<ol><li><strong>MUST</strong> score at <code>scorer.rs::score(signals, weights)</code> returning 0-100 int + tier per DEC-1661.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>lead_score_tier</code> against closed enum (cardinality 4).</li></ol>\n<ol><li><strong>MUST</strong> define tables at migration <code>0006</code>: ```sql ALTER TABLE crm_contacts ADD COLUMN current_score INT CHECK (current_score IS NULL OR (current_score &gt;= 0 AND current_score &lt;= 100)); ALTER TABLE crm_contacts ADD COLUMN current_tier TEXT CHECK (current_tier IS NULL OR current_tier IN ('cold','warm','hot','qualified')); ALTER TABLE crm_contacts ADD COLUMN scored_at TIMESTAMPTZ; GRANT UPDATE (current_score, current_tier, scored_at) ON crm_contacts TO cyberos_app;</li></ol>\n<p>   CREATE TABLE crm_score_snapshots (      snapshot_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      contact_id UUID NOT NULL,      score INT NOT NULL CHECK (score &gt;= 0 AND score &lt;= 100),      tier TEXT NOT NULL CHECK (tier IN ('cold','warm','hot','qualified')),      signals JSONB NOT NULL,      weights_version TEXT NOT NULL,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    CREATE INDEX score_snapshots_contact_time_idx      ON crm_score_snapshots(tenant_id, contact_id, created_at DESC);    ALTER TABLE crm_score_snapshots ENABLE ROW LEVEL SECURITY;    CREATE POLICY snap_rls ON crm_score_snapshots      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON crm_score_snapshots FROM cyberos_app;    -- No GRANT UPDATE — snapshots immutable per DEC-1663</p>\n<p>   CREATE TABLE crm_scoring_weights (      tenant_id UUID PRIMARY KEY,      weights JSONB NOT NULL,      version TEXT NOT NULL,      updated_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    ALTER TABLE crm_scoring_weights ENABLE ROW LEVEL SECURITY;    CREATE POLICY weights_rls ON crm_scoring_weights      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    GRANT UPDATE (weights, version, updated_at) ON crm_scoring_weights TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> support per-tenant weight override via PUT endpoint (CDO/CRO only); default weights ship in code.</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-1665. PII per FR-MEMORY-111: signals JSONB hashed (may contain title text); ids ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from create/cron → builder → scorer → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior snapshot per DEC-1663 — append-only.</li></ol>\n<ol><li><strong>MUST NOT</strong> score without all required signals — return error.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 0-100 + 4 tiers (DEC-1661)?</strong> Numeric continuous + categorical band — UX shows badge color (tier) + number for fine-grained filter.</p>\n<p><strong>Why snapshots (DEC-1663)?</strong> Score history = analytical truth (\"scored 75 last week, 40 today — what changed?\"). Mutation loses lineage.</p>\n<p><strong>Why nightly refresh (DEC-1660)?</strong> Signals decay (engagement count rolls), score must follow.</p>\n<p><strong>Why per-tenant weights (DEC-1664)?</strong> B2B SaaS values title seniority; agency values response rate.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST   /v1/crm/contacts/{id}/rescore          (manual rescore, CDO)\nGET    /v1/crm/contacts/{id}/score-history    (snapshot list)\nPUT    /v1/crm/scoring/weights                (CDO/CRO only)\nGET    /v1/crm/scoring/weights</code></pre>\n<p>Sample score:</p>\n<pre><code class=\"language-json\">{\n  \"contact_id\": \"uuid\",\n  \"current_score\": 72,\n  \"current_tier\": \"hot\",\n  \"scored_at\": \"2026-05-17T02:00:00Z\",\n  \"signals\": {\n    \"title_seniority\": 8,\n    \"engagement_count_30d\": 5,\n    \"response_rate\": 0.6,\n    \"account_tier_match\": \"strategic\",\n    \"similar_deal_win_rate\": 0.4\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Initial score on contact create</strong>. 2. <strong>Nightly refresh via cron 02:00</strong>. 3. <strong>Score 0-100 range enforced (CHECK)</strong>. 4. <strong>Tier enum 4 + cardinality test</strong>. 5. <strong>Tier thresholds applied (0-24=cold, 25-49=warm, 50-74=hot, 75+=qualified)</strong>. 6. <strong>All signals present required</strong>. 7. <strong>Snapshot immutable (no UPDATE/DELETE grant)</strong>. 8. <strong>Per-tenant weights respected</strong>. 9. <strong>Default weights ship in code</strong>. 10. <strong>3 memory audit kinds emitted</strong>. 11. <strong>PII scrubbed (signals JSON hashed)</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Manual rescore via POST</strong>. 15. <strong>History GET returns desc time</strong>. 16. <strong>Append-only snapshots</strong>. 17. <strong>Weight update increments version field</strong>. 18. <strong>Cron skip when tenant has 0 active contacts</strong>. 19. <strong>Score 100 cap (not 101)</strong>. 20. <strong>Signal missing → score=null + sev-2 audit (don't lie)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn initial_score_on_create() {\n    let ctx = TestContext::with_account_signals().await;\n    let contact = ctx.create_contact(\"john@acme.com\").await;\n    tokio::time::sleep(Duration::from_secs(1)).await;\n    let c: Contact = ctx.fetch_contact(contact.id).await;\n    assert!(c.current_score.is_some());\n    assert!(c.current_tier.is_some());\n}\n\n#[tokio::test]\nasync fn snapshots_immutable() {\n    let ctx = TestContext::with_scored_contact().await;\n    let snap = ctx.fetch_latest_snapshot(ctx.contact_id).await;\n    let r = ctx.try_mutate_snapshot(snap.snapshot_id, 0).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn nightly_refresh_updates_all() {\n    let ctx = TestContext::with_3_contacts().await;\n    ctx.simulate_time_passage_signal_drift().await;\n    ctx.run_nightly_scoring(ctx.tenant_id).await;\n    for cid in ctx.contact_ids() {\n        let history = ctx.fetch_score_history(cid).await;\n        assert!(history.len() &gt;= 2);  // initial + refresh\n    }\n}\n\n// 5.4..5.10 — tier thresholds, weight version, manual rescore</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-CRM-001, FR-CUO-101. <strong>Cross-module:</strong> FR-CRM-002 (signals), FR-AI-003 (LLM scoring), FR-MCP-007 (cron), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Signals missing</td><td>early validate</td><td>score=null + sev-2 audit</td><td>rescore later</td></tr><tr><td>AI scoring timeout</td><td>retry 1x</td><td>sev-2; previous score retained</td><td>next cron</td></tr><tr><td>Score out of range</td><td>CHECK constraint</td><td>DB rejects</td><td>bug fix</td></tr><tr><td>Tier mismatch with score</td><td>validation</td><td>reject + sev-1</td><td>bug fix</td></tr><tr><td>Snapshot table grows large</td><td>partition by month</td><td>inherent</td><td>maintenance</td></tr><tr><td>Weight update mid-refresh</td><td>version pinning</td><td>inherent</td><td>inherent</td></tr><tr><td>Default weights miss tenant nuance</td><td>per-tenant override</td><td>manual tune</td><td>inherent</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Cron skipped</td><td>next run catches</td><td>inherent</td><td>inherent</td></tr><tr><td>0 contacts tenant</td><td>skip silently</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Scorer uses FR-AI-003 with structured prompt: \"Score this contact 0-100 based on signals. Output JSON {score, tier, rationale}.\"</li><li>§11.2 Default weights: title 30%, engagement 25%, response_rate 20%, account_tier 15%, deal_similarity 10%.</li><li>§11.3 Snapshots persisted per refresh; useful for trend analysis.</li><li>§11.4 memory audit body: contact_id, score, tier; signals SHA256.</li><li>§11.5 Nightly cron via FR-MCP-007 <code>kind: 'crm.lead_scoring_refresh'</code>, per-tenant fanout.</li></ul>\n<hr>\n<p><em>End of FR-CRM-006 spec.</em></p>";
