window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-INV-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The INV service <strong>MUST</strong> ship VN hóa đơn cancellation at <code>services/invoicing/src/hoadon/cancel.rs</code> supporting replace + cancel flows, customer agreement upload, GDT form 04/SS-HDDT submission, replacement-pointer audit trail, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/inv/hoadon/{id}/cancel</code> body <code>{ reason, customer_agreement_doc_id, replacement_invoice_id?, notes }</code> — CFO-role only via FR-AUTH-101.</li></ol>\n<ol><li><strong>MUST</strong> validate reason against closed enum per DEC-1532; reject invalid values 400.</li></ol>\n<ol><li><strong>MUST</strong> require <code>customer_agreement_doc_id</code> (FR-DOC-001 reference) per DEC-1530 — without it, return 422.</li></ol>\n<ol><li><strong>MUST</strong> build form 04/SS-HDDT at <code>form_04_builder.rs::build(cancellation)</code> per Decree 123 Annex 1 — root <code>&lt;TBao&gt;</code> with <code>&lt;DLTBao&gt;</code> (notification data) + <code>&lt;DSHDon&gt;</code> (cancelled hóa đơn list).</li></ol>\n<ol><li><strong>MUST</strong> submit form to GDT within 24h of decision per DEC-1533 — via FR-MCP-007 task (GDT may ack async).</li></ol>\n<ol><li><strong>MUST</strong> update original <code>vn_hoadon</code> row to <code>hoadon_status='cancelled'</code>, set <code>replacement_hoadon_id</code> if replace flow per DEC-1534. Never delete row.</li></ol>\n<ol><li><strong>MUST</strong> define table extension at migration <code>0008</code>: ```sql ALTER TABLE vn_hoadon ADD COLUMN replacement_hoadon_id UUID REFERENCES vn_hoadon(hoadon_id); ALTER TABLE vn_hoadon ADD COLUMN cancel_reason TEXT CHECK (cancel_reason IS NULL OR cancel_reason IN ('error_correction','customer_dispute','engagement_terminated','duplicate_emission')); ALTER TABLE vn_hoadon ADD COLUMN cancellation_form_id UUID; ALTER TABLE vn_hoadon ADD COLUMN customer_agreement_doc_id UUID; ALTER TABLE vn_hoadon ADD COLUMN cancelled_at TIMESTAMPTZ; GRANT UPDATE (replacement_hoadon_id, cancel_reason, cancellation_form_id, customer_agreement_doc_id, cancelled_at, hoadon_status, updated_at) ON vn_hoadon TO cyberos_app;</li></ol>\n<p>   CREATE TABLE vn_hoadon_cancellation_forms (      form_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      cancelled_hoadon_ids UUID[] NOT NULL,      reason TEXT NOT NULL,      form_xml BYTEA NOT NULL,      gdt_response JSONB,      status TEXT NOT NULL DEFAULT 'pending'        CHECK (status IN ('pending','submitted','accepted','rejected')),      submitted_at TIMESTAMPTZ,      accepted_at TIMESTAMPTZ,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    ALTER TABLE vn_hoadon_cancellation_forms ENABLE ROW LEVEL SECURITY;    CREATE POLICY cancel_forms_rls ON vn_hoadon_cancellation_forms      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON vn_hoadon_cancellation_forms FROM cyberos_app;    GRANT UPDATE (status, gdt_response, submitted_at, accepted_at) ON vn_hoadon_cancellation_forms TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> support replace flow: when <code>replacement_invoice_id</code> supplied, trigger FR-INV-007 emit for replacement BEFORE marking original cancelled; ensures continuity.</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1535. PII per FR-MEMORY-111 (reason text scrubbed of customer names → SHA256 hash of full notes).</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from CFO action → cancel → form submit → GDT response.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow cancel of <code>accepted</code> hóa đơn without customer agreement (DEC-1530); <code>pending</code> or <code>rejected</code> may cancel without it (admin discretion).</li></ol>\n<ol><li><strong>MUST NOT</strong> delete the original row per DEC-1534.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why customer agreement required (DEC-1530)?</strong> Per Decree 123 Art. 19, cancellation without customer biên bản is legally void; GDT will reject form 04.</p>\n<p><strong>Why replace + cancel separate flows (DEC-1531)?</strong> Replace preserves AR continuity (new invoice issued); cancel terminates. Distinct legal treatments.</p>\n<p><strong>Why append-only with replacement_hoadon_id (DEC-1534)?</strong> Audit lineage — accountant must trace original → replacement chain for VAT reconciliation.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/inv/hoadon/{id}/cancel       (CFO-only)\nGET    /v1/inv/hoadon/{id}/cancellation  (status + form)\nGET    /v1/inv/cancellation-forms        (list pending/submitted/accepted)</code></pre>\n<p>Sample request:</p>\n<pre><code class=\"language-json\">{\n  \"reason\": \"error_correction\",\n  \"customer_agreement_doc_id\": \"uuid-of-uploaded-biên-bản\",\n  \"replacement_invoice_id\": \"uuid-of-new-invoice\",\n  \"notes\": \"Customer disputed line item 3 quantity; corrected from 100 to 80\"\n}</code></pre>\n<p>Form 04/SS-HDDT (Decree 123 Annex 1):</p>\n<pre><code class=\"language-xml\">&lt;TBao xmlns=\"http://kekhaithue.gdt.gov.vn/TBaoSaiSot\"&gt;\n  &lt;DLTBao&gt;\n    &lt;TTChung&gt;&lt;MCQT&gt;{tax_id}&lt;/MCQT&gt;&lt;MLTBao&gt;1&lt;/MLTBao&gt;&lt;/TTChung&gt;\n    &lt;NDTBao&gt;\n      &lt;DSHDon&gt;\n        &lt;HDon&gt;&lt;MSHDon&gt;K24TAA-00000001&lt;/MSHDon&gt;&lt;LDo&gt;1&lt;/LDo&gt;&lt;/HDon&gt;\n      &lt;/DSHDon&gt;\n    &lt;/NDTBao&gt;\n  &lt;/DLTBao&gt;\n  &lt;DSCKS&gt;...&lt;/DSCKS&gt;\n&lt;/TBao&gt;</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>CFO-only access</strong> (FR-AUTH-101 enforced). 2. <strong>Reason enum 4 values + cardinality test</strong>. 3. <strong>Customer agreement required for accepted hóa đơn</strong>. 4. <strong>Replace flow issues new hóa đơn first</strong>. 5. <strong>Original row updated, never deleted</strong>. 6. <strong>Form 04 schema valid per Decree 123 Annex 1</strong>. 7. <strong>Form submitted to GDT within 24h</strong>. 8. <strong>GDT async via FR-MCP-007</strong>. 9. <strong>4 memory audit kinds emitted</strong>. 10. <strong>PII scrubbed (notes → SHA256)</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>Replacement chain pointer queryable</strong>. 13. <strong>Append-only on form table</strong>. 14. <strong>GDT rejection → form status=rejected + CFO notification</strong>. 15. <strong>Cancellation of pending/rejected hóa đơn allowed without agreement</strong>. 16. <strong>Trace_id propagated</strong>. 17. <strong>Duplicate cancel attempt rejected (already cancelled)</strong>. 18. <strong>Form xml signed via tenant KMS cert</strong>. 19. <strong>24h window enforced (audit if past)</strong>. 20. <strong>Cancellation event broadcast to FR-CHAT-010 if customer-facing channel exists</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn replace_flow_issues_new_then_cancels_original() {\n    let ctx = TestContext::vn_tenant_with_accepted_hoadon().await;\n    let new_inv = ctx.create_replacement_invoice().await;\n    let resp = ctx.cancel_hoadon(ctx.original_id, \"error_correction\",\n        ctx.agreement_doc, Some(new_inv)).await;\n    assert_eq!(resp.status, 202);\n    let original = ctx.fetch_hoadon(ctx.original_id).await;\n    assert_eq!(original.hoadon_status, \"cancelled\");\n    let new_h = ctx.fetch_hoadon_by_invoice(new_inv).await;\n    assert_eq!(new_h.hoadon_status, \"transmitted\");\n    assert_eq!(original.replacement_hoadon_id, Some(new_h.hoadon_id));\n}\n\n#[tokio::test]\nasync fn cancel_accepted_requires_agreement() {\n    let ctx = TestContext::vn_tenant_with_accepted_hoadon().await;\n    let resp = ctx.cancel_hoadon_without_agreement(ctx.original_id, \"customer_dispute\").await;\n    assert_eq!(resp.status, 422);\n}\n\n#[tokio::test]\nasync fn form_submitted_within_24h() {\n    let ctx = TestContext::vn_tenant_with_cancellation().await;\n    let form: VnHoadonCancelForm = ctx.fetch_form(ctx.form_id).await;\n    let elapsed = form.submitted_at.unwrap() - form.created_at;\n    assert!(elapsed &lt; Duration::hours(24));\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§6 — Skeleton</h2>\n<pre><code class=\"language-rust\">pub async fn cancel(req: CancelRequest, actor: &amp;CfoActor, db: &amp;Db) -&gt; Result&lt;CancelResponse&gt; {\n    let original = db.fetch_hoadon(req.hoadon_id).await?;\n    if original.hoadon_status == \"accepted\" &amp;&amp; req.customer_agreement_doc_id.is_none() {\n        return Err(CancelError::AgreementRequired.into());\n    }\n    if let Some(replacement_invoice_id) = req.replacement_invoice_id {\n        let new_hoadon = super::emit(replacement_invoice_id, actor.tenant(), db).await?;\n        db.set_replacement_pointer(req.hoadon_id, new_hoadon).await?;\n    }\n    let form_xml = form_04_builder::build(&amp;original, &amp;req)?;\n    let form_id = db.insert_cancellation_form(&amp;original, &amp;req, &amp;form_xml).await?;\n    let trace = current_span_trace_id();\n    audit::emit(\"inv.hoadon_cancel_initiated\", json!({\"hoadon_id\": req.hoadon_id, \"reason\": req.reason}), trace).await?;\n    queue_gdt_submission(form_id, form_xml, actor.tenant().clone()).await?;\n    db.mark_hoadon_cancelled(req.hoadon_id, form_id, &amp;req).await?;\n    Ok(CancelResponse{form_id, ..})\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-INV-007. <strong>Cross-module:</strong> FR-AUTH-101 (CFO role), FR-DOC-001 (agreement upload), FR-MCP-007 (async), FR-MEMORY-111 (PII), FR-CHAT-010 (customer notification).</p>\n<h2>§8 — Sample payloads (see §3)</h2>\n<h2>§9 — Open questions</h2>\n<p>None blocking.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Agreement doc missing</td><td>early validate</td><td>422</td><td>CFO uploads doc</td></tr><tr><td>Invalid reason enum</td><td>CHECK constraint</td><td>400</td><td>use valid enum</td></tr><tr><td>Original not cancellable (already cancelled)</td><td>state check</td><td>409</td><td>inherent</td></tr><tr><td>GDT 4xx on form</td><td>response code</td><td>status=rejected; sev-2</td><td>CFO fixes + resubmit</td></tr><tr><td>GDT 5xx</td><td>retry 3x w/ backoff</td><td>retry</td><td>inherent</td></tr><tr><td>Replacement invoice doesn't exist</td><td>FK violation</td><td>404</td><td>provide valid id</td></tr><tr><td>Form &gt; 24h window</td><td>audit</td><td>sev-2 + submit anyway</td><td>post-hoc explanation</td></tr><tr><td>KMS sign fail on form</td><td>sign error</td><td>status=pending; sev-1</td><td>KMS recovery</td></tr><tr><td>Concurrent cancel attempts</td><td>UNIQUE on form for hoadon</td><td>second 409</td><td>inherent</td></tr><tr><td>Customer changes mind mid-flow</td><td>manual CFO action</td><td>revert via new emit</td><td>CFO escalation</td></tr><tr><td>GDT acceptance race vs replacement</td><td>sequence</td><td>replacement first, then cancel</td><td>inherent (order in skeleton)</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Form 04/SS-HDDT signed with same tenant KMS cert as original emission.</li><li>§11.2 GDT acknowledges form ~5min; CFO sees status update on dashboard.</li><li>§11.3 Customer agreement doc retention: 10 years per VN accounting law.</li><li>§11.4 Replace flow ordering — new emit BEFORE cancel ensures we never leave gap in VAT period.</li><li>§11.5 memory audit: cancel_reason in chain (it's enum, no PII); notes hashed.</li></ul>\n<hr>\n<p><em>End of FR-INV-008 spec.</em></p>";
