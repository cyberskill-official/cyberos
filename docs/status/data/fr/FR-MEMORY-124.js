window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-124"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>The awh out-of-band gate (ship-feature-requests workflow step 28) reruns an FR's §1 cited tests plus its module suite against a sealed, read-only baseline and returns GREEN or RED. This FR records that verdict as an aux audit row on the memory chain, so the chain tells the full story of why an FR reached <code>done</code> (independent verification), not just that an agent claimed it.</p>\n<p>This FR is gated on <code>APPROVE protocol change P23 §6</code>, which adds the row kind to the audit ledger. Until P23 is approved and this FR ships, the gate writes verdicts to a side log (<code>.awh/gate-results.jsonl</code>) and emits no chain row.</p>\n<ol><li>The system MUST define a new aux audit row kind <code>memory.awh_gate_result</code>, enumerated in <code>memory.schema.json#/definitions/AuditRecord</code> alongside the existing aux kinds (<code>memory.precondition_failed</code>, <code>memory.acl_denied</code>, <code>memory.status_overridden</code>).</li><li>The row MUST carry payload <code>{fr_id, module, outcome, weighted_pass, harness_version, sealed_acceptance_hash, tasks}</code> where <code>outcome</code> is the closed enum <code>GREEN | RED</code>, <code>weighted_pass</code> is the awh eval weighted pass@1 in <code>[0.0, 1.0]</code>, <code>harness_version</code> is the vendored awh source sha, and <code>sealed_acceptance_hash</code> is the hash of the locked golden set plus baseline that the FR was graded against.</li><li>The row MUST be written through the canonical memory writer (AGENTS.md §14.1), never by touching <code>audit/</code> directly. The awh-gate workflow step shells to <code>cyberos</code> to emit it.</li><li>On RED, the row MUST be emitted before the FR routes back to <code>ready_to_implement</code> (STATUS-REFERENCE §1.3), so the failure is on the chain even though the FR did not ship.</li><li>The row is a pure record. It MUST NOT change any memory file and MUST NOT gate any read.</li></ol>\n<h2>§2 - Rationale</h2>\n<p>The whole point of absorbing awh is that agent self-certification is not trust. Recording the gate verdict on the immutable chain makes the trust boundary auditable: every <code>done</code> carries a <code>memory.awh_gate_result{outcome: GREEN}</code> with the harness version and the sealed-acceptance hash, and every route-back carries a RED row. This FR is the dogfood: it is the first FR shipped through the awh-gated workflow, so the gate proves itself by gating its own audit row.</p>\n<h2>§3 - Cited tests (held-out)</h2>\n<ul><li><code>modules/memory/tests/core/test_awh_gate_result_row.py</code> - row shape, enum validation, writer integration, RED-before-route-back ordering, read-only invariant.</li></ul>\n<p>Acceptance command (sealed via <code>awh lock modules/memory/tests</code>):</p>\n<pre><code>cd modules/memory &amp;&amp; python -m pytest tests/core/test_awh_gate_result_row.py -q</code></pre>\n<h2>§10 - File writes</h2>\n<ul><li><code>modules/memory/memory.schema.json</code> - add the row kind to the AuditRecord enum + payload.</li><li><code>modules/memory/cyberos/core/writer.py</code> - typed builder for the row.</li><li><code>modules/memory/cyberos/data/AGENTS.md</code> - document the row kind under §6 (the P23 amendment).</li><li><code>modules/memory/tests/core/test_awh_gate_result_row.py</code> - the cited test.</li></ul>\n<h2>§11 - Notes</h2>\n<p>This is a <code>draft</code>. It enters the normal <code>draft -&gt; ready_to_implement</code> audit chain (<code>feature-request-author</code> / <code>feature-request-audit</code>) to reach 10/10 before the <code>ship-feature-requests</code> workflow picks it up. It is intentionally the first FR routed through the new awh gate.</p>";
