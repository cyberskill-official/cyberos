window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-104"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> ship OIDC SSO with RFC 8414 discovery + RFC 7517 JWKS rotation + PKCE Authorisation Code flow + per-tenant IdP config + JIT subject provisioning + claim → role mapping. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>auth_oidc_idp_configs</code> table: <code>(id UUID PRIMARY KEY, tenant_id UUID NOT NULL, name TEXT NOT NULL, issuer_url TEXT NOT NULL, client_id TEXT NOT NULL, client_secret_kms_blob BYTEA NOT NULL, kms_key_id TEXT NOT NULL, redirect_uri TEXT NOT NULL, claim_mapping_yaml TEXT NOT NULL, is_active BOOLEAN NOT NULL DEFAULT true, created_at TIMESTAMPTZ, created_by_subject_id UUID NOT NULL)</code>. UNIQUE <code>(tenant_id, name)</code>; partial unique <code>(tenant_id, issuer_url) WHERE is_active=true</code>. Max 3 active per tenant (handler check per DEC-412).</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_oidc_login_history</code> table: <code>(id BIGSERIAL, tenant_id UUID, idp_id UUID, subject_id UUID (nullable until JIT), sub_claim TEXT NOT NULL, outcome TEXT NOT NULL CHECK (outcome IN ('succeeded','failed','jit_provisioned')), failure_reason TEXT, source_ip_hash16 TEXT, ts TIMESTAMPTZ)</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>auth_oidc_subject_link</code> table: <code>(idp_id UUID, sub_claim TEXT, tenant_id UUID, subject_id UUID NOT NULL REFERENCES auth.subjects(id), linked_at TIMESTAMPTZ, PRIMARY KEY (idp_id, sub_claim))</code>. Per DEC-410, sub uniqueness is per (idp_id, tenant_id); cross-IdP linking is opt-in at slice 3.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> on all 3 OIDC tables. Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>. Discovery + JWKS fetches at gateway are tenant-scoped via the IdP config lookup.</li></ol>\n<ol><li><strong>MUST</strong> implement RFC 8414 discovery (per DEC-400). Fetch <code>&lt;issuer&gt;/.well-known/openid-configuration</code> once per tenant + cache 1h. Parse <code>authorization_endpoint</code>, <code>token_endpoint</code>, <code>jwks_uri</code>, <code>issuer</code>, <code>response_types_supported</code>, <code>grant_types_supported</code>, <code>code_challenge_methods_supported</code>. Validate: <code>code</code> in response_types, <code>authorization_code</code> in grant_types, <code>S256</code> in code_challenge_methods. Missing → reject IdP config with <code>oidc_pkce_unsupported</code>.</li></ol>\n<ol><li><strong>MUST</strong> implement RFC 7517 JWKS fetch + rotation (per DEC-402). Fetch <code>jwks_uri</code> once per kid + cache 24h. On id_token with unknown kid → refetch JWKS; if still unknown → 401 <code>unknown_kid</code>. Old kids accepted for 24h overlap (graceful rotation); new kids accepted immediately.</li></ol>\n<ol><li><strong>MUST</strong> implement the Authorisation Code flow with PKCE (RFC 7636 + DEC-401):</li></ol>\n<ul><li><code>GET /v1/auth/oidc/initiate?idp_id=&lt;&gt;&amp;tenant_slug=&lt;&gt;</code> — generates <code>state</code> (32 random bytes hex) + <code>nonce</code> (32 random bytes hex) + <code>code_verifier</code> (43-128 char base64url) + <code>code_challenge = SHA-256(code_verifier)</code>; stores <code>(state, nonce, code_verifier, idp_id, tenant_id, expires_at = now + 10min)</code> in Redis; redirects to <code>authorization_endpoint?response_type=code&amp;client_id=&lt;&gt;&amp;redirect_uri=&lt;&gt;&amp;scope=openid profile email&amp;state=&lt;&gt;&amp;nonce=&lt;&gt;&amp;code_challenge=&lt;&gt;&amp;code_challenge_method=S256</code>.</li><li><code>GET /v1/auth/oidc/callback?code=&lt;&gt;&amp;state=&lt;&gt;</code> — looks up <code>state</code> in Redis; expired or missing → 401 <code>oidc_flow_expired</code>; validates <code>state</code> + recovers <code>code_verifier</code>; POSTs to <code>token_endpoint</code> with <code>grant_type=authorization_code&amp;code=&lt;&gt;&amp;client_id=&lt;&gt;&amp;client_secret=&lt;&gt;&amp;redirect_uri=&lt;&gt;&amp;code_verifier=&lt;&gt;</code>; receives <code>id_token</code>; verifies per §1 #8; provisions JIT subject per §1 #9; issues FR-AUTH-004 JWT; redirects to tenant SPA.</li></ul>\n<ol><li><strong>MUST</strong> validate id_token signature + claims (per DEC-411 + §1 #8):</li></ol>\n<ul><li>Signature: HS/RS/ES algorithms per JWKS <code>alg</code>; verify against the matching <code>kid</code>.</li><li><code>iss</code> claim MUST equal <code>idp_config.issuer_url</code>.</li><li><code>aud</code> claim MUST contain <code>client_id</code>.</li><li><code>exp</code> claim MUST be in the future (60s skew tolerance).</li><li><code>nbf</code> claim MUST be in the past (60s skew).</li><li><code>nonce</code> claim MUST match the stored nonce.</li><li>Any fail → 401 + emit <code>auth.oidc_login_failed</code> + <code>auth.oidc_signature_invalid</code> memory rows.</li></ul>\n<ol><li><strong>MUST</strong> JIT-provision a subject on first login (per DEC-403). If <code>auth_oidc_subject_link WHERE idp_id=$1 AND sub_claim=$2</code> returns nothing:</li></ol>\n<ul><li>Extract <code>email</code> claim (fall back to <code>preferred_username</code> if email absent).</li><li>Call <code>FR-AUTH-002</code>'s <code>create_subject</code> internal helper with <code>email</code>, JIT-generated password (zeroised; subject MUST set password reset on first session OR rely on SSO subsequently), default role per claim mapping (§1 #10).</li><li>INSERT <code>auth_oidc_subject_link</code> row.</li><li>Emit <code>auth.oidc_jit_provisioned</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> apply per-tenant claim → role mapping (per DEC-404). The IdP config's <code>claim_mapping_yaml</code> is a closed-shape YAML: ```yaml default_role: tenant-member claim_rules:</li></ol>\n<ul><li>claim: groups        # one of: groups | roles | role | scope contains: \"Engineering\" grant_role: tenant-admin</li><li>claim: groups contains: \"Finance\" grant_role: cfo      # per FR-AUTH-101 enum ``<code> Rules evaluated top-down; first match wins. Unmatched claim values → use </code>default_role<code>. Unknown role in mapping → log + fall back to </code>default_role`.</li></ul>\n<ol><li><strong>MUST</strong> enforce the closed FR-AUTH-101 role enum on JIT provisioning. claim_mapping_yaml referencing a role not in the catalogue → IdP config save fails with <code>unknown_role: &lt;name&gt;</code>.</li></ol>\n<ol><li><strong>MUST</strong> support state + nonce replay protection (per DEC-408). Redis stores each <code>state</code> with 10-minute TTL; reuse → 401 <code>oidc_state_reused</code>; expired → 401 <code>oidc_flow_expired</code>. Nonce checked against id_token.</li></ol>\n<ol><li><strong>MUST</strong> rotate JWKS via 24-hour key-cache overlap (per DEC-402). On id_token with unknown <code>kid</code>, refetch JWKS once; emit <code>auth.oidc_jwks_rotated</code> memory row when new kid first observed.</li></ol>\n<ol><li><strong>MUST</strong> enforce a per-tenant max of 3 active IdP configs (per DEC-412). Attempt to create a 4th → 409 <code>idp_config_limit_exceeded</code>. ADR required to raise the cap.</li></ol>\n<ol><li><strong>MUST</strong> emit memory audit rows for 6 kinds (per DEC-406):</li></ol>\n<ul><li><code>auth.oidc_login_succeeded</code> (full login flow → AUTH JWT issued).</li><li><code>auth.oidc_login_failed</code> (any callback failure).</li><li><code>auth.oidc_jit_provisioned</code> (new subject created on first login).</li><li><code>auth.oidc_jwks_rotated</code> (new kid observed in JWKS).</li><li><code>auth.oidc_idp_config_changed</code> (POST/PATCH/DELETE on idp_configs).</li><li><code>auth.oidc_signature_invalid</code> (id_token verification failed; sev-2 — may signal attack).</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>sub_claim</code>, <code>email</code>, <code>failure_reason</code> via FR-MEMORY-111 before chain commit.</li></ol>\n<ol><li><strong>MUST</strong> complete the OIDC callback handler in ≤ 500 ms p95 (most latency is IdP token endpoint + JWKS fetch; PKCE + claim validation &lt; 50 ms server-side). <code>oidc_perf_test</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>auth.oidc.{initiate,callback,jit_provision,idp_config_change}</code> with <code>outcome</code> attribute (success | jit_provisioned | state_reused | flow_expired | signature_invalid | sub_mismatch | unknown_kid | idp_unreachable | claim_mapping_unknown_role).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_oidc_login_total{tenant_id, idp_id, outcome}</code> (counter).</li><li><code>auth_oidc_jit_provisioned_total{tenant_id}</code> (counter).</li><li><code>auth_oidc_jwks_rotations_total{tenant_id, idp_id}</code> (counter).</li><li><code>auth_oidc_signature_failures_total{tenant_id, idp_id}</code> (counter — sev-2 if sustained &gt; 5/h).</li><li><code>auth_oidc_flow_latency_ms</code> (histogram; SLO p95 &lt; 500ms).</li></ul>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/oidc/idp-configs</code> for tenant-admin IdP config CRUD. Caller MUST have role <code>tenant-admin</code> per FR-AUTH-101. Validates claim_mapping_yaml + roles + discovery succeeds before persisting.</li></ol>\n<ol><li><strong>MUST</strong> support <code>PATCH /v1/auth/oidc/idp-configs/{id}</code> (update non-immutable fields) + <code>DELETE</code> (soft-delete via <code>is_active=false</code>); deletion of active IdP with linked subjects requires explicit confirm header <code>X-Confirm-Subject-Deactivate: yes</code>.</li></ol>\n<ol><li><strong>MUST</strong> reject implicit + hybrid flow (per DEC-401). The initiate handler hard-codes <code>response_type=code</code>; the IdP config validator rejects discovery payloads missing <code>code</code> in <code>response_types_supported</code>.</li></ol>\n<ol><li><strong>MUST</strong> ensure 60-second clock skew tolerance on exp + nbf claims (per DEC-411 + §1 #8). Wider tolerance → replay risk; tighter → false-reject of legitimate tokens.</li></ol>\n<ol><li><strong>MUST</strong> support the standard scopes: <code>openid</code> (required), <code>profile</code>, <code>email</code>, plus per-IdP custom scopes specified in idp_config. Unknown IdP-claimed scopes → ignore (not an error).</li></ol>\n<ol><li><strong>MUST</strong> validate sub_claim uniqueness per (idp_id, tenant_id) (per DEC-410). Same sub appearing twice for different subject_id values → 409 <code>oidc_sub_already_linked</code>. Cross-IdP same sub → distinct subjects (DEC-410).</li></ol>\n<ol><li><strong>MUST</strong> emit <code>auth.oidc_idp_config_changed</code> memory row with the diff (which fields changed) BUT NOT the new client_secret value (PII-grade secret).</li></ol>\n<ol><li><strong>MUST</strong> alarm at sev-2 on sustained signature failures &gt; 5/h per tenant (per §1 #18 metric) — suggests IdP key rotation issue OR attack attempt.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why RFC 8414 discovery (DEC-400, §1 #5)?</strong> Discovery is the well-known endpoint that publishes the IdP's metadata (token endpoint, JWKS URI, supported algs). Without it, every IdP integration becomes manual config. RFC 8414 standardises the shape; OIDC providers publish at <code>/.well-known/openid-configuration</code>. Caching 1h limits hot-path latency without losing rotation freshness.</p>\n<p><strong>Why PKCE mandatory (DEC-401, §1 #6)?</strong> Authorization-code interception is the classic OAuth attack — attacker intercepts the redirect_uri callback containing <code>code</code>, exchanges it for token. PKCE binds the code to the original initiator via <code>code_verifier</code> (only the initiator knows the verifier matching the published code_challenge). RFC 7636 mandatory for public clients; we use it for all clients (defense in depth).</p>\n<p><strong>Why implicit + hybrid flow forbidden (DEC-401, §1 #21)?</strong> Implicit flow returns id_token in the URL fragment — leaks via browser history, referrer headers. Hybrid flow has similar leakage. OAuth 2.1 (draft) deprecates both. Standard auth code with PKCE is the canonical secure pattern.</p>\n<p><strong>Why JWKS 24h cache + overlap (DEC-402, §1 #6, §1 #12)?</strong> IdPs rotate signing keys periodically. The 24h cache avoids refetching JWKS on every token verification (cost). Overlap means old kids (still in cache) AND new kids (refetched on unknown-kid) both work — graceful rotation. Without overlap, key rotation = forced re-auth for everyone.</p>\n<p><strong>Why state + nonce mandatory (DEC-408, §1 #11)?</strong> State prevents CSRF (attacker initiates flow + tricks victim to complete callback with attacker's code). Nonce prevents id_token replay (same token can't be reused across login attempts). Both are spec-required; making them ours-required closes the gap if IdP misimplements.</p>\n<p><strong>Why 10-minute state TTL (DEC-409)?</strong> Login flow should complete within seconds; 10 minutes covers slow IdPs + user delay (e.g. MFA prompt). Past 10 minutes the user has abandoned — force re-initiate.</p>\n<p><strong>Why JIT provisioning (DEC-403, §1 #9)?</strong> Enterprise SSO works only if user provisioning is automatic. First login → IdP authenticates → AUTH creates the subject from id_token claims. The alternative (require pre-provisioning via admin) defeats the SSO value prop. JIT keeps user lifecycle synced with IdP.</p>\n<p><strong>Why claim → role mapping per-tenant YAML (DEC-404, §1 #10)?</strong> Different tenants use different IdP claim shapes. Okta uses <code>groups</code>; Azure AD uses <code>roles</code>; Google Workspace uses scope claims. Per-tenant YAML lets each tenant configure their mapping without code changes. Closed-shape YAML (4 supported claim names) prevents drift; explicit <code>default_role</code> covers unmatched cases.</p>\n<p><strong>Why max 3 active IdP configs per tenant (DEC-412, §1 #13)?</strong> Each active IdP shows up in the login selector UI; &gt; 3 becomes a UX problem. Multi-IdP scenarios (employee + contractor + partner) typically fit ≤ 3. ADR-raise forces operator consideration of UX cost.</p>\n<p><strong>Why per-(idp_id, sub_claim) uniqueness, not global sub (DEC-410, §1 #24)?</strong> Same <code>sub</code> value from different IdPs are different users (e.g. Google <code>sub=12345</code> and Okta <code>sub=12345</code> are unrelated). Scoping uniqueness per IdP prevents accidental cross-linking. Cross-IdP linking (same human via two IdPs) is opt-in at slice 3.</p>\n<p><strong>Why append-only login_history (DEC-407)?</strong> Login attempts (success + fail) are forensic events. UPDATE/DELETE blocked at SQL grant prevents post-hoc rewriting of \"who logged in when\".</p>\n<p><strong>Why 60s clock skew tolerance (DEC-411, §1 #22)?</strong> NTP drift between IdP + our server is typically &lt; 1s but can spike to ~10-30s on misconfigured systems. 60s is generous + below replay window. Tighter would false-reject legitimate tokens.</p>\n<p><strong>Why sub_claim PII-scrubbed in memory chain (§1 #15)?</strong> sub is typically an opaque IdP-assigned id (e.g. UUID), but some IdPs use email-derived subs. Scrubbing prevents inadvertent PII in audit chain.</p>\n<p><strong>Why client_secret KMS-encrypted (§1 #1)?</strong> Client secret authenticates the AUTH service to the IdP. Plaintext at rest = compromise on DB dump. KMS encryption requires KMS key permissions for decrypt — meaningful additional barrier.</p>\n<p><strong>Why redirect_uri in IdP config not request-supplied (§1 #1, §1 #6)?</strong> Redirect_uri is a per-tenant fixed value (e.g. <code>https://auth.cyberos.world/v1/auth/oidc/callback</code>). Allowing request-supplied redirects opens open-redirect attacks. Config-locked redirect prevents.</p>\n<p><strong>Why JIT-generated password (subject MUST reset OR rely on SSO) (§1 #9)?</strong> Subject needs a password hash to satisfy FR-AUTH-002's schema. JIT generates a strong random password (zeroised immediately); subject either (a) sets a real password via reset flow if SSO becomes unavailable, OR (b) rely on SSO indefinitely. Common case = (b).</p>\n<p><strong>Why sev-2 alarm on signature failures &gt; 5/h (§1 #26)?</strong> Normal operation produces zero signature failures. A burst means IdP key rotation not propagated OR active attack (forged tokens). Sev-2 = operator investigation.</p>\n<p><strong>Why <code>claim_mapping_yaml</code> rather than JSON (§1 #10)?</strong> YAML is human-readable; operators edit by hand at slice 1 (slice 3 may add UI). YAML supports comments — operators can document why a mapping rule exists.</p>\n<p><strong>Why slice 1 doesn't ship cross-IdP linking (DEC-410)?</strong> Cross-IdP same-human resolution requires identity-matching logic (email match? phone match?) — substantial. Slice 1 keeps it simple: same human via two IdPs = two AUTH subjects. Slice 3 ships the link flow.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0010 — idp_configs</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0010_oidc_idp_configs.sql\n\nBEGIN;\n\nCREATE TABLE auth_oidc_idp_configs (\n    id                      UUID         PRIMARY KEY,\n    tenant_id               UUID         NOT NULL,\n    name                    TEXT         NOT NULL CHECK (length(name) BETWEEN 1 AND 100),\n    issuer_url              TEXT         NOT NULL,\n    client_id               TEXT         NOT NULL,\n    client_secret_kms_blob  BYTEA        NOT NULL,\n    kms_key_id              TEXT         NOT NULL,\n    redirect_uri            TEXT         NOT NULL,\n    claim_mapping_yaml      TEXT         NOT NULL,\n    is_active               BOOLEAN      NOT NULL DEFAULT true,\n    created_at              TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id   UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nCREATE UNIQUE INDEX uniq_idp_name ON auth_oidc_idp_configs (tenant_id, name);\nCREATE UNIQUE INDEX uniq_active_idp_issuer ON auth_oidc_idp_configs (tenant_id, issuer_url) WHERE is_active = true;\n\nALTER TABLE auth_oidc_idp_configs ENABLE ROW LEVEL SECURITY;\nCREATE POLICY auth_oidc_idp_configs_tenant_iso ON auth_oidc_idp_configs\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0011 — login_history</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0011_oidc_login_history.sql\n\nBEGIN;\n\nCREATE TABLE auth_oidc_login_history (\n    id                  BIGSERIAL    PRIMARY KEY,\n    tenant_id           UUID         NOT NULL,\n    idp_id              UUID         NOT NULL REFERENCES auth_oidc_idp_configs(id),\n    subject_id          UUID         REFERENCES auth.subjects(id),\n    sub_claim           TEXT         NOT NULL,\n    outcome             TEXT         NOT NULL CHECK (outcome IN ('succeeded','failed','jit_provisioned')),\n    failure_reason      TEXT,\n    source_ip_hash16    TEXT,\n    ts                  TIMESTAMPTZ  NOT NULL DEFAULT now()\n);\n\nCREATE INDEX login_history_tenant_ts_idx ON auth_oidc_login_history (tenant_id, ts DESC);\nCREATE INDEX login_history_subject_idx ON auth_oidc_login_history (subject_id) WHERE subject_id IS NOT NULL;\n\nALTER TABLE auth_oidc_login_history ENABLE ROW LEVEL SECURITY;\nCREATE POLICY login_history_tenant_iso ON auth_oidc_login_history\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON auth_oidc_login_history FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Migration 0012 — subject_link</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0012_oidc_subject_link.sql\n\nBEGIN;\n\nCREATE TABLE auth_oidc_subject_link (\n    idp_id       UUID         NOT NULL REFERENCES auth_oidc_idp_configs(id) ON DELETE RESTRICT,\n    sub_claim    TEXT         NOT NULL,\n    tenant_id    UUID         NOT NULL,\n    subject_id   UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    linked_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    PRIMARY KEY (idp_id, sub_claim)\n);\n\nCREATE INDEX subject_link_subject_idx ON auth_oidc_subject_link (tenant_id, subject_id);\n\nALTER TABLE auth_oidc_subject_link ENABLE ROW LEVEL SECURITY;\nCREATE POLICY subject_link_tenant_iso ON auth_oidc_subject_link\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCOMMIT;</code></pre>\n<h3>3.4 — Discovery + JWKS</h3>\n<pre><code class=\"language-rust\">// services/auth/src/oidc/discovery.rs\nuse serde::Deserialize;\n\n#[derive(Debug, Clone, Deserialize)]\npub struct OidcDiscovery {\n    pub issuer: String,\n    pub authorization_endpoint: String,\n    pub token_endpoint: String,\n    pub jwks_uri: String,\n    #[serde(default)]\n    pub response_types_supported: Vec&lt;String&gt;,\n    #[serde(default)]\n    pub grant_types_supported: Vec&lt;String&gt;,\n    #[serde(default)]\n    pub code_challenge_methods_supported: Vec&lt;String&gt;,\n}\n\npub async fn fetch_discovery(issuer_url: &amp;str) -&gt; anyhow::Result&lt;OidcDiscovery&gt; {\n    let url = format!(\"{issuer_url}/.well-known/openid-configuration\");\n    let resp = reqwest::get(&amp;url).await?.error_for_status()?;\n    let disco: OidcDiscovery = resp.json().await?;\n    validate_pkce_support(&amp;disco)?;\n    Ok(disco)\n}\n\nfn validate_pkce_support(disco: &amp;OidcDiscovery) -&gt; anyhow::Result&lt;()&gt; {\n    if !disco.response_types_supported.iter().any(|t| t == \"code\") {\n        anyhow::bail!(\"oidc_response_type_code_not_supported\");\n    }\n    if !disco.grant_types_supported.iter().any(|g| g == \"authorization_code\") {\n        anyhow::bail!(\"oidc_auth_code_grant_not_supported\");\n    }\n    if !disco.code_challenge_methods_supported.iter().any(|m| m == \"S256\") {\n        anyhow::bail!(\"oidc_pkce_s256_unsupported\");\n    }\n    Ok(())\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/oidc/jwks.rs\nuse serde::Deserialize;\nuse std::collections::HashMap;\nuse std::sync::Arc;\nuse arc_swap::ArcSwap;\n\n#[derive(Debug, Clone, Deserialize)]\npub struct Jwk {\n    pub kid: String,\n    pub kty: String,\n    pub alg: String,\n    pub n: Option&lt;String&gt;,    // RSA modulus\n    pub e: Option&lt;String&gt;,    // RSA exponent\n    pub crv: Option&lt;String&gt;,  // EC curve\n    pub x: Option&lt;String&gt;,    // EC x\n    pub y: Option&lt;String&gt;,    // EC y\n    pub k: Option&lt;String&gt;,    // HMAC key (base64url)\n}\n\npub struct JwksCache {\n    inner: Arc&lt;ArcSwap&lt;HashMap&lt;String, Jwk&gt;&gt;&gt;,   // kid → Jwk\n}\n\nimpl JwksCache {\n    pub fn new() -&gt; Self {\n        Self { inner: Arc::new(ArcSwap::from_pointee(HashMap::new())) }\n    }\n\n    pub async fn refetch(&amp;self, jwks_uri: &amp;str) -&gt; anyhow::Result&lt;()&gt; {\n        #[derive(Deserialize)] struct JwksDoc { keys: Vec&lt;Jwk&gt; }\n        let doc: JwksDoc = reqwest::get(jwks_uri).await?.error_for_status()?.json().await?;\n        let map: HashMap&lt;String, Jwk&gt; = doc.keys.into_iter().map(|k| (k.kid.clone(), k)).collect();\n        self.inner.store(Arc::new(map));\n        Ok(())\n    }\n\n    pub fn get(&amp;self, kid: &amp;str) -&gt; Option&lt;Jwk&gt; {\n        self.inner.load().get(kid).cloned()\n    }\n}</code></pre>\n<h3>3.5 — Authorisation code + PKCE flow</h3>\n<pre><code class=\"language-rust\">// services/auth/src/oidc/flow.rs\nuse rand::RngCore;\nuse sha2::{Digest, Sha256};\nuse base64::{Engine, engine::general_purpose};\n\n#[derive(Debug, Clone)]\npub struct FlowState {\n    pub state: String,\n    pub nonce: String,\n    pub code_verifier: String,\n    pub idp_id: uuid::Uuid,\n    pub tenant_id: uuid::Uuid,\n    pub expires_at: chrono::DateTime&lt;chrono::Utc&gt;,\n}\n\npub fn generate_flow_state(idp_id: uuid::Uuid, tenant_id: uuid::Uuid) -&gt; FlowState {\n    let mut rng = rand::thread_rng();\n    let state = random_hex(32, &amp;mut rng);\n    let nonce = random_hex(32, &amp;mut rng);\n    let mut verifier_bytes = [0u8; 32];\n    rng.fill_bytes(&amp;mut verifier_bytes);\n    let code_verifier = general_purpose::URL_SAFE_NO_PAD.encode(verifier_bytes);\n    FlowState {\n        state, nonce, code_verifier, idp_id, tenant_id,\n        expires_at: chrono::Utc::now() + chrono::Duration::minutes(10),\n    }\n}\n\npub fn code_challenge_from_verifier(verifier: &amp;str) -&gt; String {\n    let hash = Sha256::digest(verifier.as_bytes());\n    general_purpose::URL_SAFE_NO_PAD.encode(hash)\n}\n\nfn random_hex(n_bytes: usize, rng: &amp;mut impl RngCore) -&gt; String {\n    let mut buf = vec![0u8; n_bytes];\n    rng.fill_bytes(&amp;mut buf);\n    hex::encode(buf)\n}\n\npub fn build_auth_url(disco: &amp;super::discovery::OidcDiscovery, idp: &amp;IdpConfig, state: &amp;FlowState) -&gt; String {\n    let challenge = code_challenge_from_verifier(&amp;state.code_verifier);\n    format!(\n        \"{auth}?response_type=code&amp;client_id={cid}&amp;redirect_uri={ru}&amp;scope={scope}&amp;state={s}&amp;nonce={n}&amp;code_challenge={c}&amp;code_challenge_method=S256\",\n        auth = disco.authorization_endpoint,\n        cid = urlencoding::encode(&amp;idp.client_id),\n        ru = urlencoding::encode(&amp;idp.redirect_uri),\n        scope = urlencoding::encode(\"openid profile email\"),\n        s = state.state, n = state.nonce, c = challenge,\n    )\n}</code></pre>\n<h3>3.6 — id_token verification</h3>\n<pre><code class=\"language-rust\">// services/auth/src/oidc/id_token.rs\nuse jsonwebtoken::{Algorithm, DecodingKey, Validation, decode_header, decode};\nuse serde::Deserialize;\n\n#[derive(Debug, Deserialize)]\npub struct IdTokenClaims {\n    pub iss: String,\n    pub sub: String,\n    pub aud: serde_json::Value,    // string or array\n    pub exp: i64,\n    pub nbf: Option&lt;i64&gt;,\n    pub iat: i64,\n    pub nonce: Option&lt;String&gt;,\n    pub email: Option&lt;String&gt;,\n    pub preferred_username: Option&lt;String&gt;,\n    pub groups: Option&lt;Vec&lt;String&gt;&gt;,\n    pub roles: Option&lt;Vec&lt;String&gt;&gt;,\n    pub role: Option&lt;String&gt;,\n    pub scope: Option&lt;String&gt;,\n}\n\npub fn verify_id_token(\n    token: &amp;str,\n    jwks: &amp;super::jwks::JwksCache,\n    expected_issuer: &amp;str,\n    expected_client_id: &amp;str,\n    expected_nonce: &amp;str,\n) -&gt; Result&lt;IdTokenClaims, OidcError&gt; {\n    let header = decode_header(token).map_err(|_| OidcError::TokenMalformed)?;\n    let kid = header.kid.as_deref().ok_or(OidcError::TokenKidMissing)?;\n    let jwk = jwks.get(kid).ok_or(OidcError::UnknownKid)?;\n\n    let alg = match jwk.alg.as_str() {\n        \"RS256\" =&gt; Algorithm::RS256, \"ES256\" =&gt; Algorithm::ES256,\n        _ =&gt; return Err(OidcError::AlgorithmUnsupported(jwk.alg)),\n    };\n    let key = make_decoding_key(&amp;jwk, alg)?;\n    let mut validation = Validation::new(alg);\n    validation.set_issuer(&amp;[expected_issuer]);\n    validation.set_audience(&amp;[expected_client_id]);\n    validation.leeway = 60;   // 60-second clock skew per DEC-411\n\n    let decoded = decode::&lt;IdTokenClaims&gt;(token, &amp;key, &amp;validation)\n        .map_err(|e| OidcError::IdTokenValidation(format!(\"{e:?}\")))?;\n\n    if decoded.claims.nonce.as_deref() != Some(expected_nonce) {\n        return Err(OidcError::NonceMismatch);\n    }\n    Ok(decoded.claims)\n}\n\nfn make_decoding_key(jwk: &amp;super::jwks::Jwk, alg: Algorithm) -&gt; Result&lt;DecodingKey, OidcError&gt; {\n    match (jwk.kty.as_str(), alg) {\n        (\"RSA\", Algorithm::RS256) =&gt; {\n            let n = jwk.n.as_deref().ok_or(OidcError::JwkMalformed)?;\n            let e = jwk.e.as_deref().ok_or(OidcError::JwkMalformed)?;\n            DecodingKey::from_rsa_components(n, e).map_err(|_| OidcError::JwkMalformed)\n        }\n        (\"EC\", Algorithm::ES256) =&gt; {\n            let x = jwk.x.as_deref().ok_or(OidcError::JwkMalformed)?;\n            let y = jwk.y.as_deref().ok_or(OidcError::JwkMalformed)?;\n            DecodingKey::from_ec_components(x, y).map_err(|_| OidcError::JwkMalformed)\n        }\n        _ =&gt; Err(OidcError::JwkKtyAlgMismatch),\n    }\n}</code></pre>\n<h3>3.7 — Claim mapper</h3>\n<pre><code class=\"language-rust\">// services/auth/src/oidc/claim_mapper.rs\nuse serde::Deserialize;\nuse cyberos_auth::rbac::Role;\n\n#[derive(Debug, Deserialize)]\npub struct ClaimMappingConfig {\n    pub default_role: String,\n    pub claim_rules: Vec&lt;ClaimRule&gt;,\n}\n\n#[derive(Debug, Deserialize)]\npub struct ClaimRule {\n    pub claim: ClaimName,          // groups | roles | role | scope\n    pub contains: String,\n    pub grant_role: String,\n}\n\n#[derive(Debug, Deserialize)]\n#[serde(rename_all = \"lowercase\")]\npub enum ClaimName { Groups, Roles, Role, Scope }\n\npub fn parse_config(yaml: &amp;str) -&gt; anyhow::Result&lt;ClaimMappingConfig&gt; {\n    let cfg: ClaimMappingConfig = serde_yaml::from_str(yaml)?;\n    // Validate all referenced roles against FR-AUTH-101 closed enum\n    cfg.default_role.parse::&lt;Role&gt;()\n        .map_err(|_| anyhow::anyhow!(\"unknown_role: {}\", cfg.default_role))?;\n    for rule in &amp;cfg.claim_rules {\n        rule.grant_role.parse::&lt;Role&gt;()\n            .map_err(|_| anyhow::anyhow!(\"unknown_role: {}\", rule.grant_role))?;\n    }\n    Ok(cfg)\n}\n\npub fn resolve_role(cfg: &amp;ClaimMappingConfig, claims: &amp;super::id_token::IdTokenClaims) -&gt; Role {\n    for rule in &amp;cfg.claim_rules {\n        let claim_values: Vec&lt;String&gt; = match rule.claim {\n            ClaimName::Groups =&gt; claims.groups.clone().unwrap_or_default(),\n            ClaimName::Roles  =&gt; claims.roles.clone().unwrap_or_default(),\n            ClaimName::Role   =&gt; claims.role.clone().map(|r| vec![r]).unwrap_or_default(),\n            ClaimName::Scope  =&gt; claims.scope.clone().map(|s| s.split(' ').map(String::from).collect()).unwrap_or_default(),\n        };\n        if claim_values.iter().any(|v| v == &amp;rule.contains) {\n            if let Ok(role) = rule.grant_role.parse::&lt;Role&gt;() {\n                return role;\n            }\n        }\n    }\n    cfg.default_role.parse::&lt;Role&gt;().expect(\"validated at parse_config\")\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Discovery fetch + cache</strong> — fresh fetch on first IdP config; cached 1h subsequent.</li><li><strong>PKCE S256 challenge generated</strong> — code_verifier 43-128 chars; challenge = base64url(SHA256(verifier)).</li><li><strong>Implicit flow IdP config rejected</strong> — discovery missing <code>code</code> in response_types → <code>oidc_response_type_code_not_supported</code>.</li><li><strong>Hybrid flow rejected</strong> — same path.</li><li><strong>Authorisation URL contains required params</strong> — response_type, client_id, redirect_uri, scope, state, nonce, code_challenge, code_challenge_method=S256.</li><li><strong>State + nonce stored 10-min TTL</strong> — Redis lookup recovers code_verifier within window.</li><li><strong>Expired state rejected</strong> — past TTL → 401 <code>oidc_flow_expired</code>.</li><li><strong>Reused state rejected</strong> — second callback with same state → 401 <code>oidc_state_reused</code>.</li><li><strong>id_token signature verified</strong> — happy path: 200; invalid sig: 401 + <code>auth.oidc_signature_invalid</code> memory row.</li><li><strong>Unknown kid triggers JWKS refetch</strong> — JWKS cache updates; <code>auth.oidc_jwks_rotated</code> row emitted.</li><li><strong>iss mismatch rejected</strong> — 401 <code>oidc_iss_mismatch</code>.</li><li><strong>aud mismatch rejected</strong> — 401 <code>oidc_aud_mismatch</code>.</li><li><strong>nonce mismatch rejected</strong> — 401 <code>oidc_nonce_mismatch</code>.</li><li><strong>exp expired (past + 60s skew)</strong> → 401 <code>oidc_expired</code>.</li><li><strong>JIT provisioning on first login</strong> — new sub → subject created + link row; <code>auth.oidc_jit_provisioned</code> memory row.</li><li><strong>JIT skipped on repeat login</strong> — existing link → subject reused.</li><li><strong>Claim mapping grants role</strong> — groups: [\"Engineering\"] → tenant-admin per config.</li><li><strong>Default role used on no match</strong> — claim mapping has no rule matching → default_role applied.</li><li><strong>Unknown role in claim_mapping_yaml rejected at config save</strong> — <code>unknown_role: &lt;name&gt;</code> 400.</li><li><strong>Max 3 IdP configs per tenant</strong> — 4th create → 409 <code>idp_config_limit_exceeded</code>.</li><li><strong>append-only login_history</strong> — UPDATE/DELETE from cyberos_app rejected.</li><li><strong>client_secret KMS-encrypted at rest</strong> — DB row contains BYTEA blob, not plaintext.</li><li><strong>redirect_uri locked in config</strong> — request-supplied redirect_uri ignored.</li><li><strong>OIDC callback p95 &lt; 500 ms</strong> — <code>oidc_perf_test</code>.</li><li><strong>5/h signature failure alarm</strong> — OBS fires sev-2.</li><li><strong>OTel span <code>auth.oidc.callback</code> emitted</strong> — outcome attr.</li><li><strong>Counter <code>auth_oidc_login_total{outcome=succeeded}</code> increments</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/geoip_test.rs\n#[tokio::test]\nasync fn pkce_full_flow(ctx: TestCtx) {\n    let idp = ctx.seed_idp_config_with_mock_provider().await;\n    // 1. Initiate\n    let init_resp = ctx.get(&amp;format!(\"/v1/auth/oidc/initiate?idp_id={}&amp;tenant_slug=acme\", idp.id)).await;\n    let auth_url = init_resp.headers()[\"Location\"].to_str().unwrap();\n    assert!(auth_url.contains(\"response_type=code\"));\n    assert!(auth_url.contains(\"code_challenge=\"));\n    assert!(auth_url.contains(\"code_challenge_method=S256\"));\n    // 2. Simulate IdP redirect with code\n    let state_param = extract_query_param(auth_url, \"state\");\n    ctx.mock_idp_provider().expect_token_exchange().return_id_token(/* signed by mock JWKS */);\n    let cb_resp = ctx.get(&amp;format!(\"/v1/auth/oidc/callback?code=mock_code&amp;state={state_param}\")).await;\n    assert_eq!(cb_resp.status(), 302);\n    // 3. Subject created (JIT)\n    let subjects = ctx.list_subjects().await;\n    assert_eq!(subjects.len(), 1);\n    let rows = ctx.memory_audit_rows(\"auth.oidc_jit_provisioned\").await;\n    assert_eq!(rows.len(), 1);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rbac_catalogue_test.rs\n#[tokio::test]\nasync fn reused_state_rejected(ctx: TestCtx) {\n    let idp = ctx.seed_idp_config().await;\n    let state = ctx.initiate_and_get_state(idp.id).await;\n    let _first = ctx.complete_callback(&amp;state, \"code1\").await.unwrap();\n    let second = ctx.complete_callback(&amp;state, \"code2\").await.unwrap_err();\n    assert!(format!(\"{second:?}\").contains(\"oidc_state_reused\"));\n}\n\n#[tokio::test]\nasync fn expired_state_rejected(ctx: TestCtx) {\n    let idp = ctx.seed_idp_config().await;\n    let state = ctx.initiate_and_get_state(idp.id).await;\n    ctx.advance_clock_minutes(11).await;\n    let err = ctx.complete_callback(&amp;state, \"code1\").await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"oidc_flow_expired\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/oidc_claim_mapping_test.rs\n#[test]\nfn unknown_role_in_yaml_rejected() {\n    let yaml = \"default_role: tenant-member\\nclaim_rules:\\n  - claim: groups\\n    contains: Eng\\n    grant_role: super-admin\\n\";\n    let err = cyberos_auth::oidc::claim_mapper::parse_config(yaml).unwrap_err();\n    assert!(format!(\"{err}\").contains(\"unknown_role: super-admin\"));\n}\n\n#[test]\nfn groups_claim_maps_to_role() {\n    let yaml = \"default_role: tenant-member\\nclaim_rules:\\n  - claim: groups\\n    contains: Engineering\\n    grant_role: tenant-admin\\n\";\n    let cfg = cyberos_auth::oidc::claim_mapper::parse_config(yaml).unwrap();\n    let claims = IdTokenClaims { groups: Some(vec![\"Engineering\".into()]), /* ... */ };\n    assert_eq!(cyberos_auth::oidc::claim_mapper::resolve_role(&amp;cfg, &amp;claims), Role::TenantAdmin);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/rbac_adr_gate_test.rs\n#[tokio::test]\nasync fn unknown_kid_triggers_jwks_refetch(ctx: TestCtx) {\n    let idp = ctx.seed_idp_config_with_mock_jwks().await;\n    ctx.warmup_jwks(idp.id).await;\n    let cached_kids_before = ctx.jwks_cache_kids(idp.id).await;\n    // IdP rotates: emits new kid\n    ctx.mock_provider().rotate_kid(\"new-kid-001\").await;\n    let token = ctx.mock_provider().sign_token_with_kid(\"new-kid-001\").await;\n    let resp = ctx.callback_with_token(idp.id, &amp;token).await;\n    assert_eq!(resp.status(), 302);\n    let cached_kids_after = ctx.jwks_cache_kids(idp.id).await;\n    assert!(cached_kids_after.contains(&amp;\"new-kid-001\".to_string()));\n    let rotation_rows = ctx.memory_audit_rows(\"auth.oidc_jwks_rotated\").await;\n    assert_eq!(rotation_rows.len(), 1);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 6 memory row builders follow the canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>FR-AUTH-004</strong> — JWT issuance + JWKS (used after OIDC callback for our own JWT).</li><li><strong>FR-AUTH-101</strong> — RBAC role enum (claim mapper validates against).</li></ul>\n<p><strong>Downstream (2 placeholders):</strong></p>\n<ul><li><strong>FR-TEN-101</strong> — self-serve signup form (consumes OIDC for federated identity).</li><li><strong>FR-PORTAL-003</strong> — external IdP integration with JIT user provisioning.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>FR-AUTH-002</strong> — subject create (JIT provisioning calls internal helper).</li><li><strong>FR-AI-003</strong> — memory audit bridge.</li><li><strong>FR-MEMORY-111</strong> — PII scrubbing for sub_claim + email.</li><li><strong>FR-OBS-007</strong> — sev-2 alarm on signature failures &gt; 5/h.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/auth/oidc/idp-configs</h3>\n<pre><code class=\"language-json\">{\n  \"name\": \"Acme Okta\",\n  \"issuer_url\": \"https://acme.okta.com\",\n  \"client_id\": \"0oa3xyz...\",\n  \"client_secret\": \"REDACTED_SECRET\",\n  \"redirect_uri\": \"https://auth.cyberos.world/v1/auth/oidc/callback\",\n  \"claim_mapping_yaml\": \"default_role: tenant-member\\nclaim_rules:\\n  - claim: groups\\n    contains: Engineering\\n    grant_role: tenant-admin\\n  - claim: groups\\n    contains: Finance\\n    grant_role: cfo\\n\"\n}</code></pre>\n<h3>8.2 — auth.oidc_login_succeeded memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.oidc_login_succeeded\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"idp_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"sub_claim_hash16\": \"abc123def4567890\",\n  \"granted_role\": \"tenant-admin\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.3 — auth.oidc_jit_provisioned memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.oidc_jit_provisioned\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"idp_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"new_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"sub_claim_hash16\": \"abc123def4567890\",\n  \"email_hash16\": \"def4567890123456\",\n  \"granted_role\": \"tenant-member\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — auth.oidc_signature_invalid memory row (sev-2)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.oidc_signature_invalid\",\n  \"severity\": \"sev-2\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"idp_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"source_ip_hash16\": \"fed0987654321abc\",\n  \"reason\": \"unknown_kid\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Cross-IdP subject linking</strong> — slice 3.</li><li><strong>OIDC dynamic client registration (RFC 7591)</strong> — slice 3.</li><li><strong>Refresh token rotation</strong> — handled by FR-AUTH-004 separately.</li><li><strong>SCIM provisioning</strong> — FR-PORTAL-004.</li><li><strong>SAML 2.0 SSO</strong> — FR-AUTH-103 (sibling).</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>IdP discovery unreachable</td><td>reqwest timeout</td><td>500 + sev-3</td><td>IdP health investigation</td></tr><tr><td>Discovery payload lacks PKCE</td><td>validator</td><td>IdP config save fails</td><td>Use PKCE-supporting IdP</td></tr><tr><td>Implicit flow IdP config</td><td>validator</td><td>400</td><td>Use auth code flow</td></tr><tr><td>JWKS unreachable</td><td>reqwest fail</td><td>401 + sev-3</td><td>IdP key endpoint check</td></tr><tr><td>Unknown kid (rotation)</td><td>refetch + retry</td><td>Success or 401 if still unknown</td><td>Designed</td></tr><tr><td>id_token signature invalid</td><td>jsonwebtoken</td><td>401 + sev-2 audit</td><td>Investigate</td></tr><tr><td>iss mismatch</td><td>validation.set_issuer</td><td>401</td><td>Designed</td></tr><tr><td>aud mismatch</td><td>validation.set_audience</td><td>401</td><td>Designed</td></tr><tr><td>exp expired</td><td>validation + leeway</td><td>401</td><td>Re-auth</td></tr><tr><td>nbf future</td><td>validation + leeway</td><td>401</td><td>Clock sync</td></tr><tr><td>nonce mismatch</td><td>post-decode check</td><td>401</td><td>CSRF defense</td></tr><tr><td>State reused</td><td>Redis lookup</td><td>401</td><td>Designed</td></tr><tr><td>State expired</td><td>TTL</td><td>401</td><td>Restart flow</td></tr><tr><td>Unknown role in claim_mapping_yaml</td><td>parse_config</td><td>400 at config save</td><td>Fix YAML</td></tr><tr><td>Claim mapping no match</td><td>resolve_role fallback</td><td>default_role applied</td><td>Designed</td></tr><tr><td>4th IdP config</td><td>handler check</td><td>409</td><td>ADR + cap raise</td></tr><tr><td>Cross-tenant sub conflict</td><td>UNIQUE (idp_id, sub_claim)</td><td>409</td><td>Designed</td></tr><tr><td>KMS decrypt fail (client_secret)</td><td>aws-sdk error</td><td>500 + sev-1</td><td>Rotate secret</td></tr><tr><td>JIT subject create fail</td><td>FR-AUTH-002 error</td><td>500 + audit</td><td>Investigate</td></tr><tr><td>Append-only login_history UPDATE</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>Append-only login_history DELETE</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>RLS bypass</td><td>USING</td><td>0 rows</td><td>Designed</td></tr><tr><td>OTel span attribute missing</td><td>otel_test</td><td>CI fails</td><td>Fix</td></tr><tr><td>Redis unreachable (state store)</td><td>redis error</td><td>500</td><td>Redis health</td></tr><tr><td>client_secret leaked in API response</td><td>handler omits</td><td>Never exposed</td><td>Designed</td></tr><tr><td>Concurrent state insert</td><td>Redis SETNX</td><td>One wins</td><td>Designed</td></tr><tr><td>Disabled IdP login attempt</td><td>is_active check</td><td>401</td><td>Re-enable or use other</td></tr><tr><td>Claim mapping YAML malformed</td><td>serde_yaml error</td><td>400</td><td>Fix YAML</td></tr><tr><td>&gt; 5/h signature failures</td><td>OBS rule</td><td>sev-2</td><td>Investigate IdP/attack</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>RFC 8414 discovery + 1h cache</strong> — fast hot path; refresh on cache miss.</li><li><strong>JWKS 24h cache + per-kid lookup</strong> — old kids accepted during overlap; new kids refetched on unknown.</li><li><strong>PKCE S256 mandatory</strong> — auth code interception defense.</li><li><strong>Implicit + hybrid flow forbidden</strong> — modern best practice.</li><li><strong>State + nonce mandatory</strong> — CSRF + replay defense.</li><li><strong>10-minute state TTL</strong> — covers slow IdP + user delay.</li><li><strong>60-second clock skew</strong> — generous + replay-safe.</li><li><strong>JIT provisioning per first login</strong> — auto-create AUTH subject.</li><li><strong>Per-tenant claim mapping YAML</strong> — closed shape; default_role fallback.</li><li><strong>Max 3 IdP configs per tenant</strong> — UX bound; ADR to raise.</li><li><strong>Per-(idp_id, sub) uniqueness</strong> — different IdPs = different subjects at slice 1.</li><li><strong>Append-only login_history at SQL grant</strong> — forensic record.</li><li><strong>client_secret KMS-encrypted</strong> — secret-at-rest hardening.</li><li><strong>redirect_uri locked in config</strong> — open-redirect defense.</li><li><strong>6 memory audit kinds</strong> — succeeded/failed/jit/jwks-rotated/config-changed/signature-invalid.</li><li><strong>PII scrubbing of sub + email</strong> — chain holds hashed forms.</li><li><strong>Sev-2 alarm on &gt; 5/h signature failures</strong> — operator investigation prompt.</li><li><strong>claim_mapping_yaml validates roles at save</strong> — closed FR-AUTH-101 enum.</li><li><strong><code>auth.oidc_idp_config_changed</code> row excludes client_secret</strong> — PII-grade secret never logged.</li><li><strong>OIDC scopes: openid (required) + profile + email</strong> — standard set.</li></ul>\n<hr>\n<p><em>End of FR-AUTH-104.</em></p>";
