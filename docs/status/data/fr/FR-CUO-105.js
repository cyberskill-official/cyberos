window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-CUO-105"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CUO service <strong>MUST</strong> ship per-step rollback at <code>services/cuo/src/rollback/</code> triggered by FR-CUO-104 step failure, compensations in reverse order, immutable audit, 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>rollback_step_status</code> against closed enum per DEC-2371.</li></ol>\n<ol><li><strong>MUST</strong> trigger on FR-CUO-104 step failure (chain status=failed) per DEC-2370.</li></ol>\n<ol><li><strong>MUST</strong> execute at <code>executor.rs::rollback(chain_id)</code> per DEC-2370:</li></ol>\n<ul><li>Fetch completed steps in REVERSE order</li><li>For each: look up compensating action via FR-SKILL-001 registry</li><li>If registered: invoke compensation; mark compensated</li><li>If not registered: mark no_compensation_registered + audit (don't fail rollback)</li></ul>\n<ol><li><strong>MUST</strong> lookup compensation at <code>compensation_registry.rs::get(skill_id)</code> per DEC-2372 — returns Option&lt;compensating_skill_id&gt;.</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0005</code>: ``<code>sql CREATE TABLE cuo_chain_rollbacks ( rollback_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, chain_id UUID NOT NULL REFERENCES cuo_chain_walks(chain_id), step_id UUID NOT NULL REFERENCES cuo_chain_steps(step_id), compensating_skill_id TEXT, status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','compensating','compensated','no_compensation_registered','compensation_failed')), started_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, failure_reason TEXT, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (chain_id, step_id) ); CREATE INDEX rollbacks_chain_idx ON cuo_chain_rollbacks(tenant_id, chain_id); ALTER TABLE cuo_chain_rollbacks ENABLE ROW LEVEL SECURITY; CREATE POLICY rollbacks_rls ON cuo_chain_rollbacks USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON cuo_chain_rollbacks FROM cyberos_app; GRANT UPDATE (status, started_at, completed_at, failure_reason) ON cuo_chain_rollbacks TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> update FR-CUO-104 chain status to <code>partial_rolled_back</code> post-completion per DEC-2373.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/cuo/chains/{id}/rollback         (auto on step failure; CDO manual trigger) GET  /v1/cuo/chains/{id}/rollback-status  (per-step status) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 6 memory audit kinds per DEC-2374. PII per FR-MEMORY-111: failure_reason SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from chain failure → rollback → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip compensation if registered per DEC-2370.</li></ol>\n<ol><li><strong>MUST NOT</strong> rollback in forward order per DEC-2370 (always reverse).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why reverse order (DEC-2370)?</strong> Compensations undo state changes; must unwind in reverse to handle dependencies between steps.</p>\n<p><strong>Why no-compensation preservation (DEC-2372)?</strong> Some operations (sending email, charging card) have no safe undo; preserving + auditing is the only correct option.</p>\n<p><strong>Why immutable rollback rows (DEC-2373)?</strong> Audit lineage; rollback is a financial-grade operation.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample rollback status:</p>\n<pre><code class=\"language-json\">{\n  \"rollback_id\": \"uuid\",\n  \"chain_id\": \"uuid\",\n  \"steps\": [\n    {\"step_order\": 2, \"skill_id\": \"email.send\", \"status\": \"no_compensation_registered\"},\n    {\"step_order\": 1, \"skill_id\": \"calendar.book\", \"compensating_skill_id\": \"calendar.cancel\", \"status\": \"compensated\"},\n    {\"step_order\": 0, \"skill_id\": \"auth.create_invite\", \"compensating_skill_id\": \"auth.revoke_invite\", \"status\": \"compensated\"}\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>rollback_step_status enum cardinality 5</strong>. 2. <strong>Triggered on FR-CUO-104 step failure</strong>. 3. <strong>Reverse order execution</strong>. 4. <strong>Compensation looked up from FR-SKILL-001 registry</strong>. 5. <strong>Missing compensation preserved with audit (not failure)</strong>. 6. <strong>6 memory audit kinds emitted</strong>. 7. <strong>PII scrubbed (failure_reason SHA256)</strong>. 8. <strong>RLS denies cross-tenant</strong>. 9. <strong>Trace_id preserved</strong>. 10. <strong>UNIQUE(chain_id, step_id)</strong>. 11. <strong>Chain status → partial_rolled_back post</strong>. 12. <strong>Append-only via REVOKE except status cols</strong>. 13. <strong>CDO manual trigger allowed</strong>. 14. <strong>Per-step compensation isolated (one failure doesn't halt rollback)</strong>. 15. <strong>compensation_failed audited sev-2</strong>. 16. <strong>Idempotent (re-trigger uses existing rollback_id per step)</strong>. 17. <strong>Rollback of failed step itself skipped (not completed)</strong>. 18. <strong>Skipped steps (FR-CUO-104) also no-rollback</strong>. 19. <strong>Order matches inverse of step_order</strong>. 20. <strong>Rollback timeout per step 30s</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn rollback_in_reverse_order() {\n    let ctx = TestContext::with_3_step_chain_step_3_fails().await;\n    ctx.run_chain(ctx.chain_id).await;\n    let rollbacks = ctx.fetch_rollbacks(ctx.chain_id).await;\n    let order: Vec&lt;i32&gt; = rollbacks.iter().map(|r| r.step_order).collect();\n    assert_eq!(order, vec![2, 1, 0]);  // reverse\n}\n\n#[tokio::test]\nasync fn no_compensation_preserved() {\n    let ctx = TestContext::with_chain_step_no_compensation_registered().await;\n    ctx.run_chain(ctx.chain_id).await;\n    let r = ctx.fetch_rollback_for_step(ctx.step_id).await;\n    assert_eq!(r.status, \"no_compensation_registered\");\n}\n\n#[tokio::test]\nasync fn compensation_failure_isolated() {\n    let ctx = TestContext::with_3_step_chain_step2_compensation_fails().await;\n    ctx.run_chain(ctx.chain_id).await;\n    let rollbacks = ctx.fetch_rollbacks(ctx.chain_id).await;\n    // step 2 compensation failed; step 1 still compensated\n    assert_eq!(rollbacks.iter().find(|r| r.step_order == 2).unwrap().status, \"compensation_failed\");\n    assert_eq!(rollbacks.iter().find(|r| r.step_order == 1).unwrap().status, \"compensated\");\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-CUO-104. <strong>Cross-module:</strong> FR-SKILL-001 (compensation registry), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>No compensation registered</td><td>lookup miss</td><td>preserve + sev-3 audit</td><td>manual cleanup</td></tr><tr><td>Compensation execution fails</td><td>catch</td><td>compensation_failed sev-2</td><td>manual or retry</td></tr><tr><td>Cross-tenant rollback</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>Concurrent rollback</td><td>UNIQUE on (chain, step)</td><td>second skipped</td><td>inherent</td></tr><tr><td>Mid-rollback crash</td><td>resume from last completed</td><td>partial</td><td>retry</td></tr><tr><td>Step still running on trigger</td><td>wait or abort</td><td>sev-2</td><td>manual</td></tr><tr><td>Compensation skill missing</td><td>lookup fail</td><td>sev-2; treat as no-compensation</td><td>data fix</td></tr><tr><td>Rollback of skipped step</td><td>filter</td><td>no-op</td><td>inherent</td></tr><tr><td>Trace_id missing</td><td>sev-3</td><td>use NIL_UUID</td><td>bug fix</td></tr><tr><td>Decimal precision N/A</td><td>inherent</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Compensation registered at FR-SKILL-001 via optional <code>compensating_skill</code> field.</li><li>§11.2 Walker invokes rollback via async task; allows main chain to mark failed quickly.</li><li>§11.3 memory audit body: chain_id, step_id, status; reason SHA256.</li><li>§11.4 Per-step timeout 30s; longer → mark compensation_failed.</li><li>§11.5 Idempotency: re-trigger checks existing rollback row; only retries failed compensations.</li></ul>\n<hr>\n<p><em>End of FR-CUO-105 spec.</em></p>";
