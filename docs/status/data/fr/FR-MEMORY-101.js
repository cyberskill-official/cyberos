window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-101"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A long-lived Rust process <strong>MUST</strong> tail every <code>&lt;memory-root&gt;/audit/*.binlog</code> and ingest each row into pgvector (embeddings) + Apache AGE (graph) within ≤ 1 second of binlog append. Each piece:</p>\n<ol><li><strong>MUST</strong> consume from binlog via per-tenant offset cursor; persist last-consumed seq to Postgres for restart resume. Cursor table <code>layer2_ingest_cursor (tenant_id PK, last_seq, updated_at)</code>.</li><li><strong>MUST</strong> compute BGE-M3 embedding via FR-AI-019 sidecar for every memory-body row; insert into pgvector with <code>(tenant_id, seq, id, kind, path, ts_ns, embedding 1024-dim, chain_anchor 32-bytes)</code>.</li><li><strong>MUST</strong> extract entities (PERSON, ORG, PLACE, CONCEPT) + relations via spaCy NER + custom recognizers; insert into AGE graph as nodes + edges. Failure to extract entities does NOT block pgvector insertion (per §1 #11 fallback).</li><li><strong>MUST</strong> tag every Layer 2 row with <code>chain_anchor = SHA-256(canonical(Layer 1 row at seq N))</code> for tamper detection. Read paths (FR-MEMORY-108) verify chain_anchor matches Layer 1 before returning results; mismatch → sev-1 + drop the result.</li><li><strong>MUST</strong> be idempotent — re-ingesting the same seq is a no-op (UPSERT on <code>(tenant_id, seq)</code>). Restart-mid-batch can re-process some rows; the UPSERT prevents duplicates.</li><li><strong>MUST</strong> target ingest staleness ≤ 1 second p95 from binlog append to Layer 2 visibility (DEC-074). Steady-state SLO measured via <code>memory_layer2_ingest_lag_seconds</code> histogram.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>memory_layer2_ingest_lag_seconds{tenant_id}</code> (histogram; SLO p95 &lt; 1s).</li><li><code>memory_layer2_ingest_failures_total{tenant_id, reason}</code> (counter; reason ∈ bge_down | age_fail | postgres_error | chain_anchor_mismatch).</li><li><code>memory_layer2_throughput_rows_per_sec{tenant_id}</code> (gauge).</li><li><code>memory_layer2_cursor_advance_total{tenant_id}</code> (counter).</li><li><code>memory_layer2_age_pending_retry_total{tenant_id}</code> (gauge; entries needing AGE backfill).</li></ul>\n<ol><li><strong>MUST</strong> support tenant isolation via <code>tenant_id</code> column + RLS (FR-AUTH-003 pattern). The <code>layer2_memories</code> table is in <code>TENANT_SCOPED_TABLES</code> registry; RLS USING + WITH CHECK clauses applied per FR-AUTH-003 §1 #2.</li><li><strong>MUST NOT</strong> be the source of truth — DEC-070 invariant: Layer 1 wins on any conflict. Read paths comparing Layer 1 vs Layer 2 (e.g., on chain_anchor mismatch) MUST trust Layer 1 + flag Layer 2 row as <code>stale</code>.</li><li><strong>MUST</strong> retry BGE-M3 sidecar failures with exponential backoff (100ms, 250ms, 500ms, 1s, 2s) up to 5 attempts before marking the row as <code>pending_embed_retry</code>. Retry job (slice 2 follow-up) reprocesses such rows.</li><li><strong>MUST</strong> apply graceful AGE-entity-extraction fallback: if spaCy/AGE call fails, write the pgvector row (with embedding) AND insert an AGE-pending row marked <code>state: pending_retry</code>. The pgvector row is queryable; AGE backfill runs in a separate job.</li><li><strong>MUST</strong> validate Layer 1 row's structural integrity before ingestion: parse the binlog frame; check seq monotonicity (next seq = previous + 1 per tenant); chain_anchor recompute (frames carry their own hash). Frame-level validation failure → log sev-2 + skip + advance cursor (alternative would block ingest indefinitely on a corrupted row).</li><li><strong>MUST</strong> support concurrent multi-tenant ingestion via tokio task per tenant. Each tenant's ingest runs independently; one tenant's BGE saturation doesn't starve another. Task scheduling round-robin per CPU core.</li><li><strong>MUST</strong> support graceful shutdown: on SIGTERM, current in-flight transaction completes; cursor saved; tasks drain; service exits cleanly. No partial commits.</li><li><strong>SHOULD</strong> support a <code>--rebuild</code> flag for FR-MEMORY-102 CI gate: starts from seq 0 for all tenants; ignores existing cursor; re-ingests everything from Layer 1.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why per-tenant cursor (DEC-073)?</strong> Global cursor would let one tenant's slow ingest stall everyone. Per-tenant + tokio task per tenant means tenant A can be 10s behind without affecting tenant B. Also: tenant-scoped restart resume is more granular.</p>\n<p><strong>Why chain_anchor (DEC-072)?</strong> Layer 1 is the source of truth, but read paths hit Layer 2 for performance. If Layer 1 is corrupted (e.g., disk bit-flip, malicious mutation), Layer 2 contains the pre-corruption state. The chain_anchor lets read paths detect drift: recompute SHA-256 of Layer 1 row at seq N; compare to stored chain_anchor; mismatch → flag as sev-1 + drop the result.</p>\n<p><strong>Why 1s p95 lag (DEC-074)?</strong> Users expect \"I just saved this; let me search for it\" to work. 1s is below human perception of staleness; longer windows produce \"where did my data go?\" complaints. The 1s budget covers BGE embedding (~50ms on GPU), entity extraction (~30ms), pgvector + AGE writes (~10ms) with healthy margin.</p>\n<p><strong>Why graceful AGE fallback (§1 #11)?</strong> Entity extraction is the slowest part of the pipeline; a spaCy hang would block ALL ingestion. Writing pgvector + marking AGE pending preserves the searchability (vector search works) while AGE catches up via background job.</p>\n<p><strong>Why idempotent UPSERT (§1 #5)?</strong> Restart-mid-batch is the recovery scenario. After SIGTERM, the cursor is saved at seq N; some rows up to N+1 might have been written with the old transaction not yet committed. On restart, ingest re-processes from cursor; UPSERT ensures no duplicates.</p>\n<p><strong>Why frame-level validation skip on corruption (§1 #12)?</strong> A single corrupted frame in binlog should NOT block ingest indefinitely. Skip + sev-2 + advance cursor lets ingest continue; the corrupted row is investigated separately. Without skip, one bad frame stalls the entire tenant's ingest.</p>\n<p><strong>Why concurrent multi-tenant (§1 #13)?</strong> Single-threaded ingest scales linearly with tenant count. At 100 tenants × 100 rows/sec, single thread saturates. Per-tenant tasks distribute across CPU cores.</p>\n<p><strong>Why DEC-070 Layer 1 wins (§1 #9)?</strong> Layer 2 is derived. If Layer 2 says X but Layer 1 says Y, Y is correct (Layer 2 derivation must have a bug). Read paths trust Layer 1 + flag Layer 2 for re-ingest.</p>\n<p><strong>Why per-tenant tokio task (§1 #13)?</strong> Better than one big task with internal queue: per-tenant task has natural backpressure (the task either keeps up or doesn't); no need for a separate scheduler. Failure mode is contained: one task panicking only affects one tenant.</p>\n<p><strong>Why graceful shutdown (§1 #14)?</strong> Mid-transaction abort would leave pgvector + AGE inconsistent. Drain + commit ensures atomicity — either all 3 inserts (pgvector, AGE, cursor) succeed or none.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- services/memory/migrations/0001_layer2.sql\nCREATE EXTENSION IF NOT EXISTS vector;\nCREATE EXTENSION IF NOT EXISTS age;\nLOAD 'age';\nSET search_path = ag_catalog, \"$user\", public;\nSELECT create_graph('cyberos_layer2');\n\nCREATE TABLE layer2_memories (\n    tenant_id     UUID NOT NULL,\n    seq           BIGINT NOT NULL,\n    id            UUID NOT NULL,\n    kind          TEXT NOT NULL,\n    path          TEXT NOT NULL,\n    ts_ns         BIGINT NOT NULL,\n    embedding     vector(1024) NOT NULL,\n    chain_anchor  BYTEA NOT NULL,\n    age_state     TEXT NOT NULL DEFAULT 'complete' CHECK (age_state IN ('complete', 'pending_retry')),\n    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    PRIMARY KEY (tenant_id, seq)\n);\n\nCREATE INDEX layer2_memories_hnsw ON layer2_memories USING hnsw (embedding vector_cosine_ops);\nCREATE INDEX layer2_memories_kind_idx ON layer2_memories (tenant_id, kind);\nCREATE INDEX layer2_memories_age_pending_idx ON layer2_memories (tenant_id) WHERE age_state = 'pending_retry';\n\nALTER TABLE layer2_memories ENABLE ROW LEVEL SECURITY;\nALTER TABLE layer2_memories FORCE ROW LEVEL SECURITY;\nCREATE POLICY layer2_isolation ON layer2_memories\n    USING      (tenant_id = current_setting('app.tenant_id', true)::uuid)\n    WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid);</code></pre>\n<pre><code class=\"language-sql\">-- services/memory/migrations/0002_layer2_cursor.sql\nCREATE TABLE layer2_ingest_cursor (\n    tenant_id  UUID PRIMARY KEY,\n    last_seq   BIGINT NOT NULL,\n    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);</code></pre>\n<pre><code class=\"language-rust\">// services/memory/src/layer2/ingest.rs\nuse std::sync::Arc;\n\npub struct IngestConfig {\n    pub bge_url: String,\n    pub spacy_url: String,\n    pub poll_interval: Duration,\n}\n\npub async fn run_ingest_loop(pool: Arc&lt;PgPool&gt;, config: IngestConfig) -&gt; anyhow::Result&lt;()&gt; {\n    loop {\n        let tenants = active_tenants(&amp;pool).await?;\n        let mut joinset = tokio::task::JoinSet::new();\n        for tenant_id in tenants {\n            let pool = pool.clone();\n            let config = config.clone();\n            joinset.spawn(async move {\n                ingest_one_tenant(tenant_id, &amp;pool, &amp;config).await\n            });\n        }\n        while let Some(_) = joinset.join_next().await {}\n        tokio::time::sleep(config.poll_interval).await;\n    }\n}\n\nasync fn ingest_one_tenant(tenant_id: Uuid, pool: &amp;PgPool, config: &amp;IngestConfig) -&gt; anyhow::Result&lt;()&gt; {\n    let cursor = cursor::get(pool, tenant_id).await?;\n    let frames = binlog_tail::read_frames_after(tenant_id, cursor).await?;\n\n    for frame in frames {\n        // §1 #12: structural validation\n        if !chain_anchor::validate_frame(&amp;frame) {\n            tracing::warn!(tenant_id = %tenant_id, seq = frame.seq, \"corrupted frame; skipping\");\n            cursor::advance(pool, tenant_id, frame.seq).await?;\n            metrics::ingest_failure(tenant_id, \"frame_corrupted\");\n            continue;\n        }\n\n        let embedding_result = bge::embed_with_retry(&amp;config.bge_url, &amp;frame.body).await;\n        let entity_result = entity_extract::run(&amp;config.spacy_url, &amp;frame.body).await;\n\n        let mut tx = pool.begin().await?;\n        rls_set_tenant(&amp;mut tx, tenant_id).await?;\n\n        let embedding = match embedding_result {\n            Ok(e) =&gt; e,\n            Err(_) =&gt; {\n                metrics::ingest_failure(tenant_id, \"bge_down\");\n                continue;   // skip; retry job picks up later\n            }\n        };\n\n        let chain_anchor = chain_anchor::compute(&amp;frame);\n        sqlx::query(\"INSERT INTO layer2_memories (tenant_id, seq, id, kind, path, ts_ns, embedding, chain_anchor, age_state)\n                     VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)\n                     ON CONFLICT (tenant_id, seq) DO NOTHING\")\n            .bind(tenant_id).bind(frame.seq).bind(frame.id)\n            .bind(&amp;frame.kind).bind(&amp;frame.path).bind(frame.ts_ns)\n            .bind(embedding).bind(&amp;chain_anchor)\n            .bind(if entity_result.is_ok() { \"complete\" } else { \"pending_retry\" })\n            .execute(&amp;mut *tx).await?;\n\n        if let Ok(entities) = entity_result {\n            age::insert_entities_and_relations(&amp;mut tx, tenant_id, frame.seq, &amp;entities).await?;\n        } else {\n            metrics::ingest_failure(tenant_id, \"age_fail\");\n        }\n\n        cursor::advance_in_tx(&amp;mut tx, tenant_id, frame.seq).await?;\n        tx.commit().await?;\n\n        metrics::lag_observed(tenant_id, (Utc::now().timestamp_nanos() - frame.ts_ns) / 1_000_000_000);\n        metrics::cursor_advanced(tenant_id);\n    }\n    Ok(())\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory/src/layer2/chain_anchor.rs\npub fn compute(frame: &amp;BinlogFrame) -&gt; [u8; 32] {\n    let canonical = canonicalise_layer1_row(frame);\n    sha256(&amp;canonical)\n}\n\npub fn verify(stored_anchor: &amp;[u8; 32], current_layer1_row: &amp;Layer1Row) -&gt; bool {\n    let recomputed = sha256(&amp;canonicalise_layer1_row_from(current_layer1_row));\n    recomputed == *stored_anchor\n}\n\npub fn validate_frame(frame: &amp;BinlogFrame) -&gt; bool {\n    // CRC check + format validation\n    let computed_crc = crc32c::crc32c(&amp;frame.payload);\n    computed_crc == frame.crc\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Append a Layer 1 row → within 1s, Layer 2 row exists</strong> — synthetic write to binlog; query layer2_memories within 1s; row present.</li><li><strong>Restart ingest process → resumes from cursor</strong> (no re-ingest of prior rows; no duplicate INSERT).</li><li><strong>Cross-tenant query: tenant A's ingest doesn't write to tenant B's index</strong> — RLS enforces; tenant B SELECT from layer2_memories returns 0 of A's rows.</li><li><strong>Idempotent: re-ingest same seq → UPSERT, single row</strong> in layer2_memories.</li><li><strong>Lag metric &lt; 1s p95 under steady-state load</strong> — emit 1000 frames over 100s; histogram p95 &lt; 1s.</li><li><strong>Embedding sidecar (FR-AI-019) failure → ingest retries with backoff; doesn't crash</strong> — kill BGE; ingest queues retries; continues.</li><li><strong>AGE entity-extraction failure → pgvector row written; AGE row marked <code>state: pending_retry</code></strong> — kill spaCy; pgvector still inserts; age_state='pending_retry'.</li><li><strong>CI rebuild test (FR-MEMORY-102) recreates Layer 2 from Layer 1 in &lt;30min</strong> for slice-1 data volume.</li><li><strong>Chain_anchor verifies post-write</strong> — Layer 1 row at seq N; recompute chain_anchor; equals stored value.</li><li><strong>Chain_anchor mismatch flagged sev-1</strong> — manually corrupt Layer 1; read path detects mismatch; sev-1 OBS event.</li><li><strong>Frame-level validation skip on CRC failure</strong> — inject CRC-corrupt frame; ingest skips + advances cursor + sev-2 log.</li><li><strong>Concurrent multi-tenant ingest</strong> — 10 tenants × 100 rows; all 10 progress concurrently; per-tenant lag &lt; 2s.</li><li><strong>Graceful shutdown</strong> — SIGTERM; in-flight tx commits; cursor saved; restart resumes correctly.</li><li><strong>--rebuild flag re-ingests from seq 0</strong> — flag passed; cursor reset; full re-ingest.</li><li><strong>OTel metrics emit per ingest event</strong>.</li><li><strong>RLS on layer2_memories — INSERT with wrong tenant_id rejected (42501)</strong>.</li><li><strong>HNSW index used for vector search</strong> — EXPLAIN shows <code>Index Scan using layer2_memories_hnsw</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn append_to_layer1_visible_in_layer2_within_1s() {\n    let pool = test_pool().await;\n    let tenant = test_tenant().await;\n    spawn_ingest_loop(pool.clone()).await;\n\n    let seq = test_helper::append_layer1_row(tenant, \"decisions/test.md\", \"test body\").await;\n    let t0 = std::time::Instant::now();\n\n    while t0.elapsed() &lt; Duration::from_secs(2) {\n        let row = sqlx::query(\"SELECT * FROM layer2_memories WHERE tenant_id = $1 AND seq = $2\")\n            .bind(tenant).bind(seq).fetch_optional(&amp;pool).await.unwrap();\n        if row.is_some() { return; }\n        tokio::time::sleep(Duration::from_millis(50)).await;\n    }\n    panic!(\"Layer 2 row not visible within 2s\");\n}\n\n#[tokio::test]\nasync fn restart_resumes_from_cursor() {\n    let pool = test_pool().await;\n    let tenant = test_tenant().await;\n    let handle = spawn_ingest_loop(pool.clone()).await;\n    test_helper::append_layer1_rows(tenant, 100).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    let count_before: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM layer2_memories\").fetch_one(&amp;pool).await.unwrap();\n\n    handle.shutdown().await;\n    let _ = spawn_ingest_loop(pool.clone()).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    let count_after: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM layer2_memories\").fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(count_before, count_after, \"no duplicate rows after restart\");\n}\n\n#[tokio::test]\nasync fn cross_tenant_isolation() {\n    let pool = test_pool().await;\n    let a = test_tenant().await;\n    let b = test_tenant().await;\n    test_helper::append_layer1_row(a, \"decisions/a.md\", \"tenant A\").await;\n    test_helper::append_layer1_row(b, \"decisions/b.md\", \"tenant B\").await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    rls::with_tenant(&amp;pool, a, |tx| async move {\n        let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM layer2_memories\").fetch_one(&amp;mut **tx).await.unwrap();\n        assert_eq!(count, 1, \"tenant A should see only their row\");\n    }).await;\n}\n\n#[tokio::test]\nasync fn bge_down_triggers_retry_no_crash() {\n    bge_test_helper::stop();\n    let pool = test_pool().await;\n    let tenant = test_tenant().await;\n    spawn_ingest_loop(pool.clone()).await;\n    test_helper::append_layer1_row(tenant, \"decisions/x.md\", \"body\").await;\n    tokio::time::sleep(Duration::from_secs(3)).await;\n    let metric: u64 = otel_test_helper::counter_value(\"memory_layer2_ingest_failures_total\", &amp;[(\"reason\", \"bge_down\")]);\n    assert!(metric &gt; 0);\n    // ingest still alive\n    bge_test_helper::start();\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    let row = sqlx::query(\"SELECT 1 FROM layer2_memories WHERE tenant_id = $1\").bind(tenant).fetch_optional(&amp;pool).await.unwrap();\n    assert!(row.is_some(), \"ingest recovered after BGE restart\");\n}\n\n#[tokio::test]\nasync fn age_failure_pgvector_still_writes() {\n    spacy_test_helper::stop();\n    let pool = test_pool().await;\n    let tenant = test_tenant().await;\n    spawn_ingest_loop(pool.clone()).await;\n    test_helper::append_layer1_row(tenant, \"decisions/x.md\", \"body\").await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    let row: (String,) = sqlx::query_as(\"SELECT age_state FROM layer2_memories WHERE tenant_id = $1\").bind(tenant).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(row.0, \"pending_retry\");\n}\n\n#[tokio::test]\nasync fn chain_anchor_mismatch_sev1() {\n    let pool = test_pool().await;\n    let tenant = test_tenant().await;\n    test_helper::append_layer1_row(tenant, \"decisions/x.md\", \"body\").await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    test_helper::corrupt_layer1_row_at_seq(tenant, 1, \"TAMPERED\").await;\n\n    // Read path queries chain_anchor\n    let result = layer2_search::query_with_anchor_check(tenant, \"body\").await;\n    assert!(matches!(result, Err(SearchError::ChainAnchorMismatch)));\n    let metric: u64 = otel_test_helper::counter_value(\"memory_layer2_ingest_failures_total\", &amp;[(\"reason\", \"chain_anchor_mismatch\")]);\n    assert!(metric &gt; 0);\n}\n\n#[tokio::test]\nasync fn idempotent_upsert() {\n    let pool = test_pool().await;\n    let tenant = test_tenant().await;\n    spawn_ingest_loop(pool.clone()).await;\n    let seq = test_helper::append_layer1_row(tenant, \"decisions/x.md\", \"body\").await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    test_helper::reset_cursor(tenant, seq - 1).await;   // simulate restart-mid-batch\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM layer2_memories WHERE tenant_id = $1 AND seq = $2\")\n        .bind(tenant).bind(seq).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(count, 1);\n}\n\n#[tokio::test]\nasync fn rls_blocks_wrong_tenant_insert() {\n    let pool = test_pool_as_cyberos_app().await;\n    let a = test_tenant().await;\n    let b = test_tenant().await;\n    let err = rls::with_tenant(&amp;pool, a, |tx| async move {\n        sqlx::query(\"INSERT INTO layer2_memories (tenant_id, seq, id, kind, path, ts_ns, embedding, chain_anchor) VALUES ($1, 1, gen_random_uuid(), 'k', 'p', 0, $2, $3)\")\n            .bind(b)   // wrong tenant\n            .bind(vec![0.0f32; 1024])\n            .bind(vec![0u8; 32])\n            .execute(&amp;mut **tx).await\n    }).await.expect_err(\"expected RLS violation\");\n    let violation = rls::classify_pg_error(&amp;err).expect(\"expected RlsCheckViolation\");\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-AI-019</strong> — BGE-M3 sidecar (embeddings).</li><li><strong>FR-AUTH-003</strong> — RLS pattern + TENANT_SCOPED_TABLES registry (add <code>layer2_memories</code>).</li><li><strong>FR-OBS-001</strong> — OTel metrics.</li><li><strong>FR-MEMORY-102 (downstream)</strong> — rebuild CI gate uses <code>--rebuild</code> flag.</li><li>Crates: <code>sqlx@0.7</code> (postgres + vector), <code>tokio</code>, <code>reqwest</code>, <code>serde</code>, <code>chrono</code>, <code>crc32c</code>, <code>sha2</code>, <code>apache-age</code> (or psql via sqlx).</li><li>Postgres 16 + pgvector 0.7 + Apache AGE 1.5.</li><li>spaCy + Vietnamese model (vi_core_news_lg) for NER.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Layer 2 row (selected fields)</h3>\n<pre><code class=\"language-text\">tenant_id:    550e8400-...\nseq:          12345\nid:           7e57c0de-...\nkind:         decisions\npath:         memories/decisions/2026-05-15-revoke-policy.md\nts_ns:        1747526400000000000\nembedding:    [0.012, -0.034, ...]   (1024 dims)\nchain_anchor: a3f9c8d7e6b5a4f3...\nage_state:    complete</code></pre>\n<h3>Cursor row</h3>\n<pre><code class=\"language-text\">tenant_id:  550e8400-...\nlast_seq:   12345\nupdated_at: 2026-05-15T14:00:00.500Z</code></pre>\n<h3>Sev-1 chain_anchor mismatch event</h3>\n<pre><code class=\"language-text\">sev-1  memory_layer2_chain_anchor_mismatch\n       tenant_id=550e... seq=12345 stored=a3f9c8d7... recomputed=ff00aa11...\n       Layer 1 may be tampered; halting reads against this seq</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Real-time streaming (binlog-tail via inotify instead of poll) — slice 2.</li><li>AGE backfill job for <code>pending_retry</code> rows — slice 2.</li><li>Embedding model upgrade (BGE-M4 when available) — slice 4+.</li><li>Per-tenant ingest priority (paid vs free) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>BGE sidecar down</td><td>reqwest error</td><td>Ingest retries with backoff (5 attempts) then <code>pending_embed_retry</code>; metric <code>bge_down</code> increments</td><td>Self-heals when sidecar up; backfill job processes pending</td></tr><tr><td>AGE entity extraction fails</td><td>spaCy error</td><td>pgvector row written with <code>age_state=pending_retry</code>; AGE backfill runs separately</td><td>Self-heals; backfill job</td></tr><tr><td>Postgres deadlock</td><td>sqlx error</td><td>tx rollback + retry once</td><td>Self-heals</td></tr><tr><td>Layer 1 row corruption (frame CRC)</td><td><code>validate_frame</code> fails</td><td>Skip + advance cursor + sev-2 log</td><td>Engineer investigates</td></tr><tr><td>Layer 1 row corruption (chain_anchor mismatch on read)</td><td>Read-path verify</td><td>sev-1 + drop result</td><td>Engineer investigates</td></tr><tr><td>Cursor table corruption</td><td>sqlx error</td><td>Restart fails</td><td>Operator restores from backup</td></tr><tr><td>Concurrent ingest race (same tenant, two processes)</td><td>UPSERT idempotent</td><td>No duplicates</td><td>By design</td></tr><tr><td>Postgres unavailable</td><td>sqlx connect</td><td>Ingest waits + retries</td><td>Self-heals</td></tr><tr><td>RLS violation (cross-tenant write)</td><td>postgres 42501</td><td>Sev-1 alarm</td><td>Investigate ingest code</td></tr><tr><td>Restart mid-batch</td><td>UPSERT idempotent</td><td>No duplicates; cursor advances correctly</td><td>By design</td></tr><tr><td>HNSW index fragmentation</td><td>Slow vector queries</td><td>sev-3 alarm; REINDEX</td><td>Operator action</td></tr><tr><td>Tenant deleted while ingest running</td><td>tenants table FK violation</td><td>Tenant ingest task exits cleanly; metric</td><td>By design</td></tr><tr><td>Embedding dim mismatch (BGE returns wrong dim)</td><td>sqlx schema check</td><td>INSERT fails; sev-2</td><td>Investigate FR-AI-019</td></tr><tr><td>Frame deserialise fails (msgpack error)</td><td>parse error</td><td>Skip + sev-2</td><td>Engineer investigates Layer 1 writer</td></tr><tr><td>Disk full on layer2 partition</td><td>INSERT fails</td><td>sev-1; ingest pauses</td><td>Operator extends disk</td></tr><tr><td>AGE extension missing</td><td>startup CREATE GRAPH fails</td><td>Service refuses to start</td><td>Operator runs migration</td></tr><tr><td>pgvector extension missing</td><td>startup CREATE EXTENSION fails</td><td>Service refuses to start</td><td>Operator runs migration</td></tr><tr><td>Tokio task panic</td><td>observability via tracing</td><td>Other tenants unaffected; restart task</td><td>Investigate</td></tr><tr><td>Lag &gt; 1s p95 sustained</td><td>OTel histogram alarm</td><td>sev-3</td><td>Investigate BGE OR DB load</td></tr><tr><td>--rebuild on production</td><td>flag check + confirmation</td><td>Operator must explicitly confirm in production</td><td>By design (slice 2)</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>Per-tenant tokio task = natural backpressure + failure isolation. One slow tenant doesn't starve others.</li><li>Chain_anchor is the read-path safety check. Layer 2 read returns + chain_anchor verified vs Layer 1 = trust.</li><li>AGE pending_retry is the graceful-degradation primitive. pgvector search works even when AGE is down.</li><li>BGE retry budget (5 attempts × exponential = ~4s total) covers transient sidecar issues without indefinite queueing.</li><li>HNSW index for cosine similarity is the slice-1 default. Slice 4+ may add IVFFlat as alternative.</li><li>spaCy NER + custom recognizers extract PERSON/ORG/PLACE/CONCEPT. Custom recognizers reuse FR-AI-012's VN_* set for Vietnamese-specific entities.</li><li>The <code>--rebuild</code> flag is the FR-MEMORY-102 hook. CI rebuilds Layer 2 from Layer 1 to ensure derivability.</li><li>Concurrent multi-tenant via tokio tasks scales across CPU cores. At 100 tenants × 8 cores, each core handles ~12 tenants.</li><li>Graceful shutdown drains in-flight tx; cursor saved at last completed seq. Restart resumes correctly.</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-101. Status: done (implemented 2026-05-23).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Apache AGE was removed; the layer-2 graph is the relational l2_edge table + pgvector. Any AGE/CREATE GRAPH references above are historical.</p>";
