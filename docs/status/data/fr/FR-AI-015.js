window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AI-015"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> maintain an authoritative ZDR (Zero Data Retention) attestation table and enforce it at alias-resolution time when tenant policy demands ZDR. The table and enforcement together obey the following:</p>\n<ol><li><strong>MUST</strong> load <code>services/ai-gateway/config/zdr_attestations.yaml</code> at gateway startup (boot order: after FR-AI-007 cost-table loader, before binding the HTTP server). Every (provider, model) combination MUST be explicitly enumerated; the file is the single source of truth.</li><li><strong>MUST</strong> expose <code>zdr::is_zdr(provider: &amp;ProviderKind, model: &amp;str) -&gt; bool</code> and <code>zdr::attestation_for(provider: &amp;ProviderKind, model: &amp;str) -&gt; Option&lt;ZdrAttestation&gt;</code>. The boolean is the gate; the attestation struct is the audit-trail primitive.</li><li><strong>MUST</strong> default <code>is_zdr</code> to <code>false</code> for any (provider, model) NOT in the table — fail closed. The lookup must never return <code>true</code> based on a missing entry, a parse fallback, or a \"trust the provider\" heuristic. A non-attested call when policy requires ZDR is a refusal, not an unknown.</li><li><strong>MUST</strong> require every YAML entry to carry <code>is_zdr</code> (bool), <code>verified_at</code> (ISO date), <code>source_url</code> (HTTPS URL — HTTP rejected at parse), and <code>attested_by</code> (kebab-case email or username). <code>notes</code> is optional. An entry missing any required field MUST fail <code>init_zdr_table</code> with <code>LoaderInitError::Schema { reason }</code>. ZDR claims without source = audit failure; the parser blocks them at boot.</li><li><strong>MUST</strong> be consulted by <code>alias::resolve</code> (FR-AI-006 §1 #6 invokes this FR) BEFORE returning a resolved (provider, model). When <code>policy.ai_policy.zdr_required == true</code> AND <code>zdr::is_zdr(&amp;resolved_provider, &amp;resolved_model) == false</code>, <code>alias::resolve</code> MUST return <code>Err(AliasError::ZdrViolation { resolved_provider, resolved_model, attestation: &lt;option&gt; })</code>. The <code>attestation</code> field surfaces the documented status (e.g., a known-non-ZDR provider returns the entry with <code>is_zdr: false</code>; a missing entry returns <code>None</code>) so the operator can distinguish \"we know this isn't ZDR\" from \"we have no record.\"</li><li><strong>MUST</strong> emit an <code>ai.zdr_violation</code> memory audit row when a request is refused due to ZDR. The row carries <code>tenant_id</code>, <code>agent_persona</code>, <code>requested_alias</code>, <code>resolved_provider</code>, <code>resolved_model</code>, <code>policy_requires_zdr: true</code>, <code>attestation_present: bool</code>, <code>request_id</code>. The audit-before-refusal invariant from FR-AI-001 §1 #6 applies.</li><li><strong>MUST</strong> be hot-reloadable via the <code>notify</code> crate's file-watch with a 250ms debounce (same machinery as FR-AI-005, FR-AI-007, FR-AI-014). On reload-success, the new attestations replace the cache via <code>ArcSwap::store</code>.</li><li><strong>MUST</strong> detect ZDR-status revocations on hot-reload: for every (provider, model) where the previous cache entry had <code>is_zdr == true</code> AND the new entry has <code>is_zdr == false</code>, emit <code>tracing::warn!</code> AND increment <code>ai_zdr_attestations_revoked_total{provider, model}</code>. Operators MUST be alerted (the metric is a sev-2 alarm trigger).</li><li><strong>MUST</strong> implement staleness handling at two thresholds. (a) <strong>Soft stale</strong> at <code>verified_at + 90 days</code>: weekly CI cron (<code>.github/workflows/zdr-staleness-check.yml</code>) flags the entry; PR-time validation logs WARN; metric <code>ai_zdr_attestations_stale_total{provider, model}</code> increments. (b) <strong>Hard stale</strong> at <code>verified_at + 365 days</code>: <code>is_zdr</code> MUST return <code>false</code> regardless of the table's recorded value (defensive override; an attestation no one has reverified for a year is no longer trustworthy). The hard-stale path also logs <code>tracing::error!</code> and increments <code>ai_zdr_attestations_expired_total</code>.</li><li><strong>MUST</strong> validate <code>source_url</code> at parse time: scheme MUST be <code>https</code>, host MUST be a valid DNS name (or known provider domain — <code>aws.amazon.com</code>, <code>anthropic.com</code>, <code>platform.openai.com</code>, <code>cloud.google.com</code>, etc.). HTTP URLs and bare paths are rejected with <code>LoaderInitError::InvalidSourceUrl</code>. The validator does NOT fetch the URL (no network call at parse time); it only validates syntax + scheme.</li><li><strong>MUST</strong> validate <code>attested_by</code> at parse time as either <code>&lt;localpart&gt;@cyberos.world</code> (CyberSkill staff) OR <code>&lt;auditor-id&gt;@&lt;approved-auditor-domain&gt;</code> (third-party auditor on a maintained allow-list). Random strings are rejected with <code>LoaderInitError::InvalidAttestor</code>. The allow-list lives in <code>parse.rs</code> constants for slice 3; FR-AI-022 will move it to a separate config.</li><li><strong>MUST</strong> integrate with <code>policy.ai_policy.zdr_required</code> from FR-AI-005's tenant policy schema. The flag is read once per request via FR-AI-001's policy load; this FR reads it through the request context, never directly from disk.</li><li><strong>MUST</strong> propagate <code>ZdrViolation</code> errors as HTTP <code>403 ZDR_VIOLATION</code> with body <code>{\"error\":\"zdr_violation\",\"resolved_provider\":\"&lt;p&gt;\",\"resolved_model\":\"&lt;m&gt;\",\"policy_requires_zdr\":true,\"contact\":\"ops@cyberos.world\"}</code>. The body MUST NOT echo the attestation's <code>notes</code> field (operator-facing explanation, not customer-facing).</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>ai_zdr_lookups_total{provider, model, outcome}</code> (counter; outcome ∈ <code>attested | missing | revoked | expired</code>).</li><li><code>ai_zdr_violations_total{tenant_id}</code> (counter; alarm threshold &gt; 0 over a 5-minute window).</li><li><code>ai_zdr_attestations_revoked_total{provider, model}</code> (counter; sev-2 alert on increment).</li><li><code>ai_zdr_attestations_stale_total{provider, model}</code> (counter; weekly evaluated).</li><li><code>ai_zdr_attestations_expired_total{provider, model}</code> (counter; sev-1 alert).</li><li><code>ai_zdr_table_size</code> (gauge; current attestation count).</li></ul>\n<ol><li><strong>SHOULD</strong> log at INFO level on every successful hot-reload: <code>zdr_table_reloaded count=&lt;N&gt; sources={hash16}</code> so operators can verify \"did my edit actually load?\".</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a separate YAML, not inline in the cost-table or alias map?</strong> ZDR is a legal-compliance assertion that drifts independently from cost. Bedrock might be ZDR today but a new model launched on Bedrock might not initially carry the same attestation — even though the cost-table entry exists for billing. Coupling ZDR to cost or alias creates two failure modes: (a) updating cost flips ZDR by accident; (b) reviewing ZDR requires reading three files. Keeping ZDR in its own file with its own owner means the audit conversation (\"show me the ZDR attestations for the period under review\") is one file, one history.</p>\n<p><strong>Why fail closed (default false, §1 #3)?</strong> Same reasoning as FR-AI-001 §1 #9 (policy-missing fails closed): silent defaults bury compliance failures. Missing entry = treat as non-ZDR = refuse if policy demands ZDR. The alternative — defaulting to <code>true</code> for \"providers we trust\" — is exactly the failure mode that produces a \"we promised ZDR and didn't enforce it\" headline.</p>\n<p><strong>Why does each row carry <code>verified_at</code> + <code>source_url</code> + <code>attested_by</code> (§1 #4)?</strong> When an auditor asks \"prove you only sent PDPL-protected data to ZDR-attested providers,\" the chain of evidence is:</p>\n<ol><li>Tenant policy says <code>zdr_required: true</code> (memory row + YAML).</li><li>Request was routed to (provider, model).</li><li><code>zdr_attestations.yaml</code> shows that (provider, model) had <code>is_zdr: true</code> at the time of the request, citing a published source URL.</li><li>The attestor (<code>attested_by</code>) is identifiable; if questioned, they can produce the source they read.</li></ol>\n<p>Each piece of provenance is load-bearing; an attestation without <code>source_url</code> is a claim, not evidence. The parse-time enforcement of all four required fields is the discipline that keeps the table audit-grade.</p>\n<p><strong>Why HTTPS-only source URLs (§1 #10)?</strong> A source URL on HTTP can be MITMed — an attacker between the auditor and the documentation could serve a forged \"we have ZDR\" page. HTTPS isn't perfect but it's the floor. Rejecting HTTP at parse time is free and prevents accidentally-shipped HTTP citations from a copy-paste error.</p>\n<p><strong>Why two-tier staleness (90d soft, 365d hard, §1 #9)?</strong> Vendor policies change. SOC 2 cadence is annual reassessment for vendor controls; we mirror that as the hard floor. Soft stale at 90d (matching standard quarterly vendor-review cycles) gives operators a reminder before things go critical. Hard stale at 365d is the defensive override: an attestation no human has reverified for a year cannot be trusted to still describe reality. The enforcement is automatic — not \"we should reverify\" but \"the bool returns false until reverified\" — so the failure mode is loud (a request refusal) not silent (a stale attestation continuing to gate calls open).</p>\n<p><strong>Why does revocation produce a WARN log + metric, not an automatic refusal (§1 #8)?</strong> Revocation is an operational signal: \"the attestor changed <code>is_zdr: true → false</code> deliberately, probably because the vendor changed their policy.\" The flip itself is the new authoritative state — calls to that (provider, model) start being refused immediately. The WARN + metric exist so operators <em>know</em> it happened (so they can email affected tenants) — not to undo the flip.</p>\n<p><strong>Why a separate <code>attested_by</code> field rather than git blame?</strong> Git blame works for slice 3 (small team, all attestations from <code>stephen@cyberos.world</code>). At scale (multi-attestor org, third-party auditors), the attestor is operationally relevant: \"who do I email to question this attestation?\" An auditor's id (e.g., a third-party SOC 2 firm) is not in our git history. Surfacing <code>attested_by</code> in the YAML AND in the audit row makes the attribution explicit.</p>\n<p><strong>Why is the enforcement at <code>alias::resolve</code> time, not at the precheck (FR-AI-001) time?</strong> The precheck has the alias name (<code>chat.smart</code>), not the resolved (provider, model). Resolution happens later; that's where the (provider, model) is known. Enforcing at resolution means the gate fires at the right point with the right data, AND we get exactly one enforcement site (no duplication, no risk of one path checking and another not). The cost is one extra error class (<code>ZdrViolation</code>) propagating from <code>alias::resolve</code> to the handler — small.</p>\n<p><strong>Why a separate memory audit row (<code>ai.zdr_violation</code>, §1 #6)?</strong> A regulator's audit of \"did we ever send PDPL data to a non-ZDR provider\" needs a search target. Without the dedicated row, the evidence is \"absence of evidence\" (no request went through to the non-ZDR provider, but how do we prove we didn't try?). The dedicated row converts the question to \"show me all <code>ai.zdr_violation</code> rows for tenant X\" — a positive answer, not an absence. The row is the proof we <em>did</em> refuse.</p>\n<p><strong>Why doesn't <code>LoaderInitError::Schema</code> fail-closed-on-init?</strong> It does (<code>Result&lt;(), LoaderInitError&gt;</code> propagates up through the boot path; the gateway refuses to bind). The §10 inventory makes this explicit. Inability to load the ZDR table is treated as severe as inability to load the cost table — both are compliance-load-bearing primitives.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<h3>Type definitions</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/zdr/mod.rs\n\nuse std::sync::Arc;\nuse std::path::Path;\nuse arc_swap::ArcSwap;\nuse chrono::NaiveDate;\nuse once_cell::sync::OnceCell;\nuse std::collections::HashMap;\n\n#[derive(Debug, Clone, PartialEq)]\npub struct ZdrAttestation {\n    pub is_zdr: bool,\n    pub verified_at: NaiveDate,\n    pub source_url: String,                // HTTPS-validated at parse\n    pub attested_by: String,               // &lt;localpart&gt;@cyberos.world OR approved-auditor\n    pub notes: Option&lt;String&gt;,\n}\n\npub fn is_zdr(provider: &amp;ProviderKind, model: &amp;str) -&gt; bool;\npub fn attestation_for(provider: &amp;ProviderKind, model: &amp;str) -&gt; Option&lt;ZdrAttestation&gt;;\npub async fn init_zdr_table(config_path: &amp;Path) -&gt; Result&lt;(), LoaderInitError&gt;;\n\n#[derive(Debug, thiserror::Error)]\npub enum LoaderInitError {\n    #[error(\"zdr_attestations.yaml malformed: {reason}\")]\n    Schema { reason: String },\n    #[error(\"invalid source_url at {provider}/{model}: must be https://, got {url}\")]\n    InvalidSourceUrl { provider: String, model: String, url: String },\n    #[error(\"invalid attested_by at {provider}/{model}: {value}\")]\n    InvalidAttestor { provider: String, model: String, value: String },\n    #[error(\"zdr table already initialised; init_zdr_table called twice\")]\n    AlreadyInitialised,\n    #[error(\"io error reading config: {0}\")]\n    Io(#[from] std::io::Error),\n}\n\n// In FR-AI-006 alias.rs (modified_files):\npub enum AliasError {\n    // ... existing variants ...\n    ZdrViolation {\n        resolved_provider: ProviderKind,\n        resolved_model: String,\n        attestation: Option&lt;ZdrAttestation&gt;,   // None ⇒ no entry; Some(a) ⇒ entry shows is_zdr=false\n    },\n}\n\nstatic TABLE: OnceCell&lt;ArcSwap&lt;HashMap&lt;(ProviderKind, String), ZdrAttestation&gt;&gt;&gt; = OnceCell::new();</code></pre>\n<h3>YAML schema (with all required fields enforced at parse)</h3>\n<pre><code class=\"language-yaml\"># services/ai-gateway/config/zdr_attestations.yaml\nversion: 1\nlast_updated: 2026-05-15\n\n# Each entry MUST cite a source. ZDR claims without source = audit failure.\n# Entries lacking is_zdr / verified_at / source_url / attested_by FAIL init.\nattestations:\n  bedrock:\n    \"anthropic.claude-3-5-sonnet-20241022-v2:0\":\n      is_zdr: true\n      verified_at: 2026-05-15\n      source_url: \"https://aws.amazon.com/bedrock/data-privacy/\"\n      attested_by: \"stephen@cyberos.world\"\n      notes: \"Bedrock guarantees no-data-retention per their data privacy whitepaper\"\n\n    \"anthropic.claude-3-haiku-20240307-v1:0\":\n      is_zdr: true\n      verified_at: 2026-05-15\n      source_url: \"https://aws.amazon.com/bedrock/data-privacy/\"\n      attested_by: \"stephen@cyberos.world\"\n\n  anthropic:\n    \"claude-3-5-sonnet-20241022\":\n      is_zdr: true\n      verified_at: 2026-05-15\n      source_url: \"https://www.anthropic.com/legal/zero-data-retention\"\n      attested_by: \"stephen@cyberos.world\"\n      notes: \"ZDR available on Enterprise plan only; runtime tenant-tier check is FR-AI-022 follow-up\"\n\n  openai:\n    \"gpt-4o\":\n      is_zdr: false\n      verified_at: 2026-05-15\n      source_url: \"https://platform.openai.com/docs/models#data-policy\"\n      attested_by: \"stephen@cyberos.world\"\n      notes: |\n        Standard OpenAI retains data 30 days by default. ZDR requires the\n        zero-data-retention org policy enabled per OpenAI's documentation;\n        CyberOS has not validated this. Treat as non-ZDR until policy enabled.\n\n  vertex:\n    \"gemini-2.0-pro\":\n      is_zdr: false\n      verified_at: 2026-05-15\n      source_url: \"https://cloud.google.com/vertex-ai/docs/general/data-governance\"\n      attested_by: \"stephen@cyberos.world\"\n      notes: \"Vertex retention is region-dependent; ZDR not validated for VN tenants\"</code></pre>\n<h3>Parser contract</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/zdr/parse.rs\n\nuse url::Url;\n\nconst APPROVED_AUDITOR_DOMAINS: &amp;[&amp;str] = &amp;[\n    \"cyberos.world\",\n    // Third-party auditor allow-list:\n    \"kpmg.com.vn\",\n    \"ey.com\",\n    \"deloitte.com\",\n    // ... maintained by ops; FR-AI-022 moves to separate config\n];\n\npub fn parse_attestations(yaml: &amp;str) -&gt; Result&lt;HashMap&lt;(ProviderKind, String), ZdrAttestation&gt;, LoaderInitError&gt; {\n    let raw: serde_yaml::Value = serde_yaml::from_str(yaml)\n        .map_err(|e| LoaderInitError::Schema { reason: e.to_string() })?;\n\n    let attestations = raw.get(\"attestations\")\n        .ok_or_else(|| LoaderInitError::Schema { reason: \"missing 'attestations' root key\".into() })?;\n\n    let mut out = HashMap::new();\n    for (provider_str, models) in attestations.as_mapping().unwrap() {\n        let provider = ProviderKind::from_str(provider_str.as_str().unwrap())\n            .map_err(|e| LoaderInitError::Schema { reason: format!(\"unknown provider {provider_str:?}: {e}\") })?;\n        for (model_str, fields) in models.as_mapping().unwrap() {\n            let model = model_str.as_str().unwrap().to_string();\n            let att = parse_one_attestation(&amp;provider, &amp;model, fields)?;\n            out.insert((provider, model), att);\n        }\n    }\n    Ok(out)\n}\n\nfn parse_one_attestation(\n    provider: &amp;ProviderKind, model: &amp;str, fields: &amp;serde_yaml::Value,\n) -&gt; Result&lt;ZdrAttestation, LoaderInitError&gt; {\n    let map = fields.as_mapping().ok_or_else(|| LoaderInitError::Schema {\n        reason: format!(\"{provider:?}/{model}: not a mapping\"),\n    })?;\n\n    let is_zdr = map.get(&amp;\"is_zdr\".into()).and_then(|v| v.as_bool())\n        .ok_or_else(|| LoaderInitError::Schema { reason: format!(\"{provider:?}/{model}: missing is_zdr\") })?;\n\n    let verified_at_s = map.get(&amp;\"verified_at\".into()).and_then(|v| v.as_str())\n        .ok_or_else(|| LoaderInitError::Schema { reason: format!(\"{provider:?}/{model}: missing verified_at\") })?;\n    let verified_at = NaiveDate::parse_from_str(verified_at_s, \"%Y-%m-%d\")\n        .map_err(|e| LoaderInitError::Schema { reason: format!(\"{provider:?}/{model}: bad verified_at: {e}\") })?;\n\n    let source_url = map.get(&amp;\"source_url\".into()).and_then(|v| v.as_str())\n        .ok_or_else(|| LoaderInitError::Schema { reason: format!(\"{provider:?}/{model}: missing source_url\") })?\n        .to_string();\n    validate_source_url(provider, model, &amp;source_url)?;\n\n    let attested_by = map.get(&amp;\"attested_by\".into()).and_then(|v| v.as_str())\n        .ok_or_else(|| LoaderInitError::Schema { reason: format!(\"{provider:?}/{model}: missing attested_by\") })?\n        .to_string();\n    validate_attested_by(provider, model, &amp;attested_by)?;\n\n    let notes = map.get(&amp;\"notes\".into()).and_then(|v| v.as_str()).map(|s| s.to_string());\n\n    Ok(ZdrAttestation { is_zdr, verified_at, source_url, attested_by, notes })\n}\n\nfn validate_source_url(provider: &amp;ProviderKind, model: &amp;str, url: &amp;str) -&gt; Result&lt;(), LoaderInitError&gt; {\n    let parsed = Url::parse(url).map_err(|_| LoaderInitError::InvalidSourceUrl {\n        provider: format!(\"{provider:?}\"), model: model.into(), url: url.into(),\n    })?;\n    if parsed.scheme() != \"https\" {\n        return Err(LoaderInitError::InvalidSourceUrl {\n            provider: format!(\"{provider:?}\"), model: model.into(), url: url.into(),\n        });\n    }\n    Ok(())\n}\n\nfn validate_attested_by(provider: &amp;ProviderKind, model: &amp;str, value: &amp;str) -&gt; Result&lt;(), LoaderInitError&gt; {\n    let Some((_local, domain)) = value.split_once('@') else {\n        return Err(LoaderInitError::InvalidAttestor {\n            provider: format!(\"{provider:?}\"), model: model.into(), value: value.into(),\n        });\n    };\n    if !APPROVED_AUDITOR_DOMAINS.contains(&amp;domain) {\n        return Err(LoaderInitError::InvalidAttestor {\n            provider: format!(\"{provider:?}\"), model: model.into(), value: value.into(),\n        });\n    }\n    Ok(())\n}</code></pre>\n<h3>Staleness check</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/zdr/staleness.rs\n\nuse chrono::{Duration, Utc};\n\npub const SOFT_STALE_DAYS: i64 = 90;\npub const HARD_STALE_DAYS: i64 = 365;\n\npub fn is_soft_stale(att: &amp;ZdrAttestation) -&gt; bool {\n    Utc::now().date_naive() - att.verified_at &gt; Duration::days(SOFT_STALE_DAYS)\n}\n\npub fn is_hard_stale(att: &amp;ZdrAttestation) -&gt; bool {\n    Utc::now().date_naive() - att.verified_at &gt; Duration::days(HARD_STALE_DAYS)\n}</code></pre>\n<pre><code class=\"language-rust\">// In zdr/mod.rs::is_zdr — apply hard-stale override per §1 #9.\npub fn is_zdr(provider: &amp;ProviderKind, model: &amp;str) -&gt; bool {\n    let table = match TABLE.get() {\n        Some(t) =&gt; t.load(),\n        None =&gt; return false,\n    };\n    let key = (*provider, model.to_string());\n    match table.get(&amp;key) {\n        None =&gt; {\n            metrics::lookup(&amp;key, \"missing\");\n            false\n        }\n        Some(att) if staleness::is_hard_stale(att) =&gt; {\n            metrics::lookup(&amp;key, \"expired\");\n            tracing::error!(provider=?provider, model=%model, verified_at=%att.verified_at,\n                            \"zdr attestation HARD-stale (&gt;365d); forcing is_zdr=false\");\n            false\n        }\n        Some(att) =&gt; {\n            if staleness::is_soft_stale(att) {\n                metrics::soft_stale(&amp;key);\n            }\n            metrics::lookup(&amp;key, if att.is_zdr { \"attested\" } else { \"missing\" });\n            att.is_zdr\n        }\n    }\n}</code></pre>\n<h3>CI staleness workflow</h3>\n<pre><code class=\"language-yaml\"># .github/workflows/zdr-staleness-check.yml\nname: ZDR Attestation Staleness Check\non:\n  schedule:\n    - cron: '0 0 * * 1'   # every Monday 00:00 UTC\n  workflow_dispatch: {}\n\njobs:\n  check-staleness:\n    runs-on: ubuntu-22.04\n    permissions:\n      issues: write\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions-rust-lang/setup-rust-toolchain@v1\n      - name: Run staleness check\n        working-directory: services/ai-gateway\n        run: cargo run --bin zdr-staleness-check -- config/zdr_attestations.yaml\n      - name: Open issue if soft-stale entries found\n        if: failure()\n        uses: actions/github-script@v7\n        with:\n          script: |\n            github.rest.issues.create({\n              owner: context.repo.owner,\n              repo: context.repo.repo,\n              title: `ZDR attestations soft-stale (&gt;90 days) — refresh due`,\n              labels: ['compliance', 'zdr', 'staleness'],\n              body: `Per FR-AI-015 §1 #9, one or more ZDR attestations exceed 90 days.\\n\\nRefresh by visiting each provider's published policy and bumping verified_at.`\n            });</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>ZDR-attested provider passes</strong> — <code>is_zdr(&amp;Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\")</code> returns <code>true</code>.</li><li><strong>Documented non-ZDR fails</strong> — <code>is_zdr(&amp;OpenAI, \"gpt-4o\")</code> returns <code>false</code>.</li><li><strong>Missing entry fails closed</strong> — <code>is_zdr(&amp;Vertex, \"gemini-9.9.9\")</code> (not in table) returns <code>false</code>; metric <code>ai_zdr_lookups_total{outcome=\"missing\"}</code> increments.</li><li><strong>FR-AI-006 integration: refusal when policy requires</strong> — Tenant policy <code>zdr_required: true</code>; alias resolves to <code>openai:gpt-4o</code>; <code>alias::resolve</code> returns <code>Err(AliasError::ZdrViolation { attestation: Some(att_with_is_zdr_false), .. })</code>.</li><li><strong>FR-AI-006 integration: refusal when entry missing</strong> — Tenant policy <code>zdr_required: true</code>; alias resolves to a (provider, model) not in the table; <code>alias::resolve</code> returns <code>Err(AliasError::ZdrViolation { attestation: None, .. })</code>.</li><li><strong>HTTP 403 ZDR_VIOLATION on refusal</strong> — Handler converts the AliasError to a <code>403</code> response with the documented body shape; <code>notes</code> field NOT echoed in response body.</li><li><strong>Audit row emitted</strong> — Every <code>ZdrViolation</code> refusal emits exactly one <code>ai.zdr_violation</code> memory row carrying tenant_id, resolved_provider, resolved_model, policy_requires_zdr, attestation_present, request_id.</li><li><strong>Hot reload picks up new attestation</strong> — Adding a new entry to YAML; within 500ms <code>is_zdr</code> returns the new value.</li><li><strong>Revocation warns and metricises</strong> — Changing an entry from <code>is_zdr: true</code> to <code>is_zdr: false</code> triggers <code>tracing::warn!</code> AND increments <code>ai_zdr_attestations_revoked_total{provider, model}</code>.</li><li><strong>Soft staleness flagged in CI</strong> — <code>verified_at = today - 91 days</code>; <code>cargo run --bin zdr-staleness-check</code> exits non-zero with the entry listed; weekly cron opens an issue.</li><li><strong>Hard staleness forces is_zdr=false</strong> — <code>verified_at = today - 366 days</code>; <code>is_zdr</code> returns <code>false</code> regardless of the YAML's recorded value; <code>tracing::error!</code> fired; metric <code>ai_zdr_attestations_expired_total</code> incremented.</li><li><strong>HTTP source_url rejected at parse</strong> — A YAML entry with <code>source_url: \"http://...\"</code> fails <code>init_zdr_table</code> with <code>InvalidSourceUrl</code>.</li><li><strong>Bare-string attestor rejected at parse</strong> — A YAML entry with <code>attested_by: \"alice\"</code> (no <code>@</code>) fails with <code>InvalidAttestor</code>.</li><li><strong>Out-of-domain attestor rejected at parse</strong> — A YAML entry with <code>attested_by: \"alice@gmail.com\"</code> fails with <code>InvalidAttestor</code> (domain not in approved list).</li><li><strong>Required-field validation: missing source_url</strong> — A YAML entry without <code>source_url</code> fails <code>init_zdr_table</code> with <code>Schema</code>.</li><li><strong>Required-field validation: missing attested_by</strong> — A YAML entry without <code>attested_by</code> fails <code>init_zdr_table</code> with <code>Schema</code>.</li><li><strong>Attestation provenance retrievable</strong> — <code>attestation_for(&amp;Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\")</code> returns a <code>ZdrAttestation</code> with all five fields populated.</li><li><strong>Concurrent lookups + hot-reload safe</strong> — 100 tokio tasks calling <code>is_zdr</code> concurrently while a hot-reload runs see either old or new state, never torn or panicking.</li><li><strong>Double-init rejected</strong> — <code>init_zdr_table</code> then second <code>init_zdr_table</code> returns <code>LoaderInitError::AlreadyInitialised</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/zdr_test.rs\nuse cyberos_ai_gateway::zdr::{self, init_zdr_table};\nuse cyberos_ai_gateway::providers::ProviderKind;\nuse std::path::Path;\n\n#[tokio::test]\nasync fn zdr_attested_passes_and_non_attested_fails_closed() {\n    init_zdr_table(Path::new(\"config/zdr_attestations.yaml\")).await.unwrap();\n\n    // AC #1\n    assert!(zdr::is_zdr(&amp;ProviderKind::Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\"));\n    // AC #2\n    assert!(!zdr::is_zdr(&amp;ProviderKind::OpenAI, \"gpt-4o\"));\n    // AC #3\n    assert!(!zdr::is_zdr(&amp;ProviderKind::Vertex, \"gemini-9.9.9\"));\n}\n\n#[tokio::test]\nasync fn alias_resolve_refuses_when_policy_requires_zdr() {\n    init_zdr_table(Path::new(\"config/zdr_attestations.yaml\")).await.unwrap();\n    let policy = test_policy_with_zdr_required(true);\n    let result = alias::resolve(\"chat.smart-non-zdr\", &amp;policy);   // alias maps to openai:gpt-4o\n    match result {\n        Err(AliasError::ZdrViolation { resolved_provider, attestation, .. }) =&gt; {\n            assert_eq!(resolved_provider, ProviderKind::OpenAI);\n            assert!(matches!(attestation, Some(a) if !a.is_zdr));\n        }\n        _ =&gt; panic!(\"expected ZdrViolation\"),\n    }\n}\n\n#[tokio::test]\nasync fn audit_row_emitted_on_zdr_refusal() {\n    let request_id = \"req_test_zdr_001\";\n    let tenant_id = \"tenant_alpha\";\n    let _ = handlers::chat::handle(test_request(tenant_id, request_id, \"chat.smart-non-zdr\")).await;\n    let rows = memory_test_helper::find_rows(\"ai.zdr_violation\", request_id);\n    assert_eq!(rows.len(), 1);\n    let p = &amp;rows[0].payload;\n    assert_eq!(p[\"tenant_id\"], tenant_id);\n    assert_eq!(p[\"resolved_provider\"], \"openai\");\n    assert_eq!(p[\"resolved_model\"], \"gpt-4o\");\n    assert_eq!(p[\"policy_requires_zdr\"], true);\n    assert_eq!(p[\"attestation_present\"], true);\n}\n\n#[tokio::test]\nasync fn http_source_url_rejected() {\n    let yaml = r#\"\n        version: 1\n        attestations:\n          openai:\n            \"gpt-4o\":\n              is_zdr: false\n              verified_at: \"2026-05-15\"\n              source_url: \"http://platform.openai.com/policy\"\n              attested_by: \"stephen@cyberos.world\"\n    \"#;\n    let err = zdr::parse::parse_attestations(yaml).expect_err(\"expected InvalidSourceUrl\");\n    assert!(matches!(err, LoaderInitError::InvalidSourceUrl { .. }));\n}\n\n#[tokio::test]\nasync fn bare_string_attestor_rejected() {\n    let yaml = r#\"\n        version: 1\n        attestations:\n          openai:\n            \"gpt-4o\":\n              is_zdr: false\n              verified_at: \"2026-05-15\"\n              source_url: \"https://platform.openai.com/policy\"\n              attested_by: \"alice\"\n    \"#;\n    let err = zdr::parse::parse_attestations(yaml).expect_err(\"expected InvalidAttestor\");\n    assert!(matches!(err, LoaderInitError::InvalidAttestor { .. }));\n}\n\n#[tokio::test]\nasync fn missing_source_url_rejected() {\n    let yaml = r#\"\n        version: 1\n        attestations:\n          openai:\n            \"gpt-4o\":\n              is_zdr: false\n              verified_at: \"2026-05-15\"\n              attested_by: \"stephen@cyberos.world\"\n    \"#;\n    let err = zdr::parse::parse_attestations(yaml).expect_err(\"expected Schema\");\n    match err {\n        LoaderInitError::Schema { reason } =&gt; assert!(reason.contains(\"source_url\")),\n        e =&gt; panic!(\"wrong variant: {e:?}\"),\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/alias_resolution_test.rs\n#[tokio::test]\nasync fn revocation_warns_and_metricises() {\n    init_zdr_table(Path::new(\"config/zdr_attestations.yaml\")).await.unwrap();\n    let path = \"config/zdr_attestations.yaml\";\n    let original = std::fs::read_to_string(path).unwrap();\n    let revoked = original.replace(\n        \"is_zdr: true\\n      verified_at: 2026-05-15\\n      source_url: \\\"https://aws.amazon.com/bedrock/data-privacy/\\\"\",\n        \"is_zdr: false\\n      verified_at: 2026-05-15\\n      source_url: \\\"https://aws.amazon.com/bedrock/data-privacy/\\\"\",\n    );\n    std::fs::write(path, &amp;revoked).unwrap();\n    tokio::time::sleep(std::time::Duration::from_millis(500)).await;\n\n    assert!(!zdr::is_zdr(&amp;ProviderKind::Bedrock, \"anthropic.claude-3-5-sonnet-20241022-v2:0\"));\n    let counter = otel_test_helper::counter_value(\n        \"ai_zdr_attestations_revoked_total\",\n        &amp;[(\"provider\", \"bedrock\"), (\"model\", \"anthropic.claude-3-5-sonnet-20241022-v2:0\")],\n    );\n    assert!(counter &gt;= 1, \"revocation counter not incremented\");\n\n    std::fs::write(path, &amp;original).unwrap();\n}</code></pre>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/zdr_test.rs\n#[test]\nfn soft_stale_at_91_days() {\n    let att = ZdrAttestation {\n        is_zdr: true,\n        verified_at: chrono::Utc::now().date_naive() - chrono::Duration::days(91),\n        source_url: \"https://x\".into(), attested_by: \"stephen@cyberos.world\".into(), notes: None,\n    };\n    assert!(zdr::staleness::is_soft_stale(&amp;att));\n    assert!(!zdr::staleness::is_hard_stale(&amp;att));\n}\n\n#[test]\nfn hard_stale_at_366_days_overrides_is_zdr() {\n    let att = ZdrAttestation {\n        is_zdr: true,\n        verified_at: chrono::Utc::now().date_naive() - chrono::Duration::days(366),\n        source_url: \"https://x\".into(), attested_by: \"stephen@cyberos.world\".into(), notes: None,\n    };\n    assert!(zdr::staleness::is_hard_stale(&amp;att));\n\n    // Force-inject att into table; is_zdr MUST return false.\n    test_helper::inject_attestation(&amp;ProviderKind::Bedrock, \"test-model\", att);\n    assert!(!zdr::is_zdr(&amp;ProviderKind::Bedrock, \"test-model\"));\n}</code></pre>\n<pre><code class=\"language-bash\">cd services/ai-gateway\ncargo test -p cyberos-ai-gateway zdr</code></pre>\n<p>CI gate: cargo-test runs on every PR touching <code>services/ai-gateway/src/zdr/**</code>, <code>services/ai-gateway/config/zdr_attestations.yaml</code>, or <code>services/ai-gateway/src/alias.rs</code> (FR-AI-006 wiring).</p>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3 for type defs + parser + staleness module. Hot-reload follows FR-AI-014's pattern (250ms debounce, ArcSwap pointer-swap, parse-error keeps cache):</p>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/zdr/watch.rs\npub fn spawn_watcher_with_revocation_detection(config_path: &amp;Path) {\n    let path = config_path.to_path_buf();\n    std::thread::spawn(move || {\n        let (tx, rx) = std::sync::mpsc::channel();\n        let mut watcher = notify::recommended_watcher(tx).unwrap();\n        watcher.watch(&amp;path, notify::RecursiveMode::NonRecursive).unwrap();\n\n        for ev in rx {\n            if ev.is_err() { continue; }\n            std::thread::sleep(std::time::Duration::from_millis(250));\n            // Drain debounce window.\n            while rx.try_recv().is_ok() {}\n\n            match reload_with_diff(&amp;path) {\n                Ok((new_size, revocations)) =&gt; {\n                    tracing::info!(count = new_size, \"zdr_table_reloaded\");\n                    for (key, prev_is_zdr) in revocations {\n                        if prev_is_zdr {\n                            tracing::warn!(provider = ?key.0, model = %key.1,\n                                          \"zdr attestation REVOKED (was true, now false or missing)\");\n                            metrics::revocation(&amp;key);\n                        }\n                    }\n                }\n                Err(e) =&gt; {\n                    tracing::warn!(error = %e, \"zdr table reload failed; cache unchanged\");\n                    metrics::reload_failure(&amp;e);\n                }\n            }\n        }\n    });\n}\n\nfn reload_with_diff(path: &amp;Path) -&gt; Result&lt;(usize, Vec&lt;((ProviderKind, String), bool)&gt;), LoaderInitError&gt; {\n    let yaml = std::fs::read_to_string(path)?;\n    let new = parse::parse_attestations(&amp;yaml)?;\n    let old = TABLE.get().unwrap().load();\n    let mut revocations = vec![];\n    for (key, old_att) in old.iter() {\n        let new_is_zdr = new.get(key).map(|a| a.is_zdr).unwrap_or(false);\n        if old_att.is_zdr &amp;&amp; !new_is_zdr {\n            revocations.push((key.clone(), old_att.is_zdr));\n        }\n    }\n    TABLE.get().unwrap().store(Arc::new(new.clone()));\n    Ok((new.len(), revocations))\n}</code></pre>\n<p><code>canonical::zdr_violation</code> builder (added to FR-AI-003's memory bridge):</p>\n<pre><code class=\"language-rust\">pub mod canonical {\n    pub fn zdr_violation(\n        tenant_id: &amp;str, agent_persona: &amp;str, requested_alias: &amp;str,\n        resolved_provider: &amp;ProviderKind, resolved_model: &amp;str,\n        attestation_present: bool, request_id: &amp;str,\n    ) -&gt; AuditRow {\n        AuditRow {\n            kind: \"ai.zdr_violation\".into(),\n            payload: serde_json::json!({\n                \"tenant_id\": tenant_id,\n                \"agent_persona\": agent_persona,\n                \"requested_alias\": requested_alias,\n                \"resolved_provider\": format!(\"{resolved_provider:?}\").to_lowercase(),\n                \"resolved_model\": resolved_model,\n                \"policy_requires_zdr\": true,\n                \"attestation_present\": attestation_present,\n                \"request_id\": request_id,\n            }),\n            ..Default::default()\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<h3>Code dependencies (other FRs/modules)</h3>\n<ul><li><strong>FR-AI-006</strong> — <code>alias::resolve</code> invokes <code>zdr::is_zdr</code> (declared in FR-AI-006 §1 #6); this FR provides the implementation. The <code>AliasError::ZdrViolation</code> variant is added to FR-AI-006's enum.</li><li><strong>FR-AI-001</strong> — Cost ledger precheck routes the request; <code>ZdrViolation</code> errors propagate through the precheck path to the handler.</li><li><strong>FR-AI-005</strong> — Tenant policy schema declares <code>policy.ai_policy.zdr_required</code>; this FR consumes it via the request context.</li><li><strong>FR-AI-003</strong> — memory audit-row bridge. This FR adds the <code>canonical::zdr_violation</code> builder for the <code>ai.zdr_violation</code> row kind (declared in FR-AI-003 §3).</li><li><strong>FR-AI-016 (downstream)</strong> — Residency pinning is the natural pair of ZDR; both gates live behind tenant policy. FR-AI-016 may extend this FR's table with regional-attestation columns.</li><li><strong>FR-AI-022 (downstream)</strong> — OTel trace emission consumes <code>ai_zdr_*</code> metrics.</li></ul>\n<h3>Concept dependencies (shared types)</h3>\n<ul><li><code>ZdrAttestation</code> is the single attestation primitive used in lookups, audit rows, and operator queries. The five required fields (is_zdr, verified_at, source_url, attested_by, notes) are immutable across slice 3.</li><li><code>(ProviderKind, String)</code> is the attestation key. ProviderKind enum is declared in FR-AI-008's multi-provider router; this FR uses the same enum.</li><li><code>APPROVED_AUDITOR_DOMAINS</code> is the attestor allow-list (cyberos.world + third-party SOC 2 firms). Maintained in <code>parse.rs</code> constants for slice 3; FR-AI-022 moves to a separate config.</li><li>Soft/hard staleness thresholds (90d / 365d) are baked-in constants matching SOC 2 cadence; changes require explicit FR amendment.</li></ul>\n<h3>Operational / external</h3>\n<ul><li>Rust crates: <code>arc-swap@1</code>, <code>notify@6</code>, <code>chrono@0.4</code>, <code>serde_yaml@0.9</code>, <code>url@2</code>, <code>once_cell@1</code>, <code>thiserror@1</code>.</li><li>The <code>url</code> crate parses + validates <code>source_url</code> syntax (HTTPS-only enforced by us).</li><li>GitHub Actions for the weekly staleness cron.</li><li>Initial entries cite Bedrock, Anthropic, OpenAI, Vertex documentation pages; these are evergreen URLs maintained by each vendor.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Caller in FR-AI-006 alias.rs</h3>\n<pre><code class=\"language-rust\">// FR-AI-006 §1 #6 (modified to call zdr::is_zdr)\npub fn resolve(alias: &amp;str, policy: &amp;TenantPolicy) -&gt; Result&lt;(ProviderKind, String), AliasError&gt; {\n    let (provider, model) = ALIAS_MAP.get().unwrap().load().get(alias)\n        .ok_or_else(|| AliasError::UnknownAlias(alias.into()))?;\n\n    if policy.ai_policy.zdr_required &amp;&amp; !zdr::is_zdr(&amp;provider, &amp;model) {\n        let attestation = zdr::attestation_for(&amp;provider, &amp;model);\n        return Err(AliasError::ZdrViolation {\n            resolved_provider: provider, resolved_model: model.clone(), attestation,\n        });\n    }\n\n    Ok((provider, model.clone()))\n}</code></pre>\n<h3>Audit row <code>ai.zdr_violation</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ai.zdr_violation\",\n  \"ts_ns\": 1747526400000000000,\n  \"payload\": {\n    \"tenant_id\": \"tenant_alpha\",\n    \"agent_persona\": \"cuo-cpo@0.4.1\",\n    \"requested_alias\": \"chat.smart\",\n    \"resolved_provider\": \"openai\",\n    \"resolved_model\": \"gpt-4o\",\n    \"policy_requires_zdr\": true,\n    \"attestation_present\": true,\n    \"request_id\": \"req_01HZK9R8M3X5C8Q4\"\n  }\n}</code></pre>\n<h3>HTTP refusal</h3>\n<pre><code class=\"language-text\">HTTP/1.1 403 Forbidden\nContent-Type: application/json\n\n{\n  \"error\": \"zdr_violation\",\n  \"resolved_provider\": \"openai\",\n  \"resolved_model\": \"gpt-4o\",\n  \"policy_requires_zdr\": true,\n  \"contact\": \"ops@cyberos.world\"\n}</code></pre>\n<h3>Attestation lookup for audit reporting</h3>\n<pre><code class=\"language-rust\">let att = zdr::attestation_for(&amp;ProviderKind::Bedrock,\n                              \"anthropic.claude-3-5-sonnet-20241022-v2:0\").unwrap();\nprintln!(\"ZDR: {} | source: {} | attested: {} on {}\",\n    att.is_zdr, att.source_url, att.attested_by, att.verified_at);\n// =&gt; ZDR: true | source: https://aws.amazon.com/bedrock/data-privacy/\n//    attested: stephen@cyberos.world on 2026-05-15</code></pre>\n<h3>Hot-reload INFO log (success)</h3>\n<pre><code class=\"language-text\">INFO  zdr_table_reloaded count=12</code></pre>\n<h3>Hot-reload WARN log (revocation)</h3>\n<pre><code class=\"language-text\">WARN  provider=Bedrock model=anthropic.claude-3-5-sonnet-20241022-v2:0\n      zdr attestation REVOKED (was true, now false or missing)</code></pre>\n<h3>Hard-stale ERROR log</h3>\n<pre><code class=\"language-text\">ERROR provider=Vertex model=gemini-2.0-pro verified_at=2025-04-15\n      zdr attestation HARD-stale (&gt;365d); forcing is_zdr=false</code></pre>\n<h3>Weekly staleness CI output</h3>\n<pre><code class=\"language-text\">$ cargo run --bin zdr-staleness-check -- config/zdr_attestations.yaml\nSoft-stale entries (&gt;90 days; refresh due):\n  vertex/gemini-2.0-pro          verified_at=2026-02-10 (95 days old)\n  openai/gpt-4o                  verified_at=2026-02-13 (92 days old)\n2 entries flagged. Exiting non-zero.</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. Items deferred to later FRs:</p>\n<ul><li>Enterprise-plan tenant-tier check (Anthropic direct API requires Enterprise plan for ZDR; current FR ships the note-as-documentation but doesn't runtime-validate the tenant tier) — FR-AI-022 follow-up.</li><li>Per-region ZDR attestation (Vertex retention is region-dependent; current table is provider+model granularity) — FR-AI-016 (residency pinning) area.</li><li>Auto-refresh attestations from provider APIs (programmatic scrape of provider policy pages) — out of scope; the human-in-the-loop verification step is the audit primitive.</li><li>Signed attestations (cryptographic signature from the attestor) — out of scope; <code>attested_by</code> + git-blame is sufficient evidence.</li><li>Multi-attestor rows (e.g., both <code>stephen@cyberos.world</code> AND <code>auditor@kpmg.com.vn</code> co-attest) — slice 5; current schema is one attestor per entry.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Missing ZDR attestation for (provider, model)</td><td>HashMap miss in <code>is_zdr</code></td><td>Returns false (fail closed); metric <code>lookups_total{outcome=missing}</code></td><td>Operator adds entry to YAML</td></tr><tr><td>Provider revokes ZDR (true→false on hot-reload)</td><td>Diff-detect in <code>reload_with_diff</code></td><td><code>tracing::warn!</code> + metric <code>attestations_revoked_total</code> increment; subsequent lookups return false</td><td>Operator notifies affected tenants</td></tr><tr><td>Provider entry deleted from YAML on reload</td><td>Diff sees old entry true, new entry absent</td><td>Counts as revocation; same WARN+metric path</td><td>Operator confirms intent</td></tr><tr><td>YAML parse error at init</td><td><code>LoaderInitError::Schema</code></td><td>Gateway exits 1 (refuses to bind)</td><td>Operator fixes YAML; redeploy</td></tr><tr><td>YAML parse error at hot-reload</td><td>Reload fails; cache unchanged</td><td>INFO log \"reload failed\"; metric <code>reload_failure_total</code></td><td>Operator fixes YAML; next file-watch event triggers retry</td></tr><tr><td>Concurrent lookup + hot-reload</td><td>ArcSwap atomic</td><td>Reader sees old or new state, never torn</td><td>By design (§1 #7)</td></tr><tr><td>Concurrent hot-reload + reload (rapid edits)</td><td>250ms debounce</td><td>One reparse runs; events queue</td><td>By design (§1 #7)</td></tr><tr><td><code>source_url</code> is HTTP not HTTPS</td><td><code>validate_source_url</code> parser check</td><td><code>LoaderInitError::InvalidSourceUrl</code> → init fails</td><td>Operator changes URL to HTTPS</td></tr><tr><td><code>attested_by</code> is bare string (no <code>@</code>)</td><td><code>validate_attested_by</code> parser check</td><td><code>LoaderInitError::InvalidAttestor</code> → init fails</td><td>Operator uses <code>&lt;localpart&gt;@cyberos.world</code></td></tr><tr><td><code>attested_by</code> domain not in approved list</td><td><code>validate_attested_by</code> allow-list check</td><td><code>LoaderInitError::InvalidAttestor</code> → init fails</td><td>Operator uses approved domain or extends allow-list (PR review)</td></tr><tr><td>Required field missing (is_zdr, verified_at, source_url, attested_by)</td><td><code>parse_one_attestation</code> field checks</td><td><code>LoaderInitError::Schema</code> → init fails</td><td>Operator fills missing field</td></tr><tr><td>Soft-stale entry (&gt;90d)</td><td>Weekly CI cron check; <code>is_soft_stale</code></td><td>CI fails non-zero; GitHub issue opened; metric <code>attestations_stale_total</code> increments</td><td>Operator reviews provider policy; bumps verified_at</td></tr><tr><td>Hard-stale entry (&gt;365d)</td><td><code>is_hard_stale</code> check in <code>is_zdr</code></td><td><code>is_zdr</code> forced to false regardless of recorded value; ERROR log + metric <code>attestations_expired_total</code></td><td>Operator reverifies attestation immediately; if confirmed still ZDR, bumps verified_at</td></tr><tr><td>Tenant policy <code>zdr_required</code> missing</td><td>FR-AI-005 schema default</td><td>Defaults to false (no enforcement for that tenant)</td><td>Operator updates tenant policy YAML if ZDR is required</td></tr><tr><td>Alias resolves to provider not in table</td><td><code>is_zdr</code> returns false; <code>attestation_for</code> returns None</td><td><code>ZdrViolation { attestation: None }</code></td><td>Operator adds attestation OR removes the alias mapping</td></tr><tr><td>Audit row emit fails (memory bridge down)</td><td><code>memory_writer::emit</code> returns Err</td><td>Refusal still proceeds; sev-1 log (\"ZDR refused but audit row failed\")</td><td>Operator investigates memory; FR-AI-003 §10 covers</td></tr><tr><td>Revocation notification missed (operator absent)</td><td>OTel alarm on <code>attestations_revoked_total</code></td><td>Alarm pages on-call</td><td>Standard incident response</td></tr><tr><td><code>notes</code> field accidentally echoed in 403 response body</td><td>Integration test asserts <code>notes</code> absent</td><td>Test fails → PR blocked</td><td>Handler MUST scrub <code>notes</code> before serialising response</td></tr><tr><td>New provider added without entry</td><td><code>is_zdr</code> returns false → calls refused if policy requires ZDR</td><td>New-provider rollout PR includes attestation entry</td><td>Standard PR process</td></tr><tr><td><code>notify</code> watcher thread panics</td><td>tokio observability</td><td>Watcher dies; hot-reload stops; cache continues serving old state</td><td>sev-2 alert; restart gateway</td></tr><tr><td>Double init (test re-entry)</td><td><code>OnceCell::set</code> returns Err</td><td><code>LoaderInitError::AlreadyInitialised</code></td><td>Tests use <code>reset_for_tests()</code></td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The OpenAI <code>gpt-4o</code> entry intentionally records <code>is_zdr: false</code> with notes — many readers assume OpenAI defaults to ZDR; they don't (30-day retention by default; ZDR requires the org-level zero-data-retention policy). Documenting it inline prevents copy-paste errors.</li><li>The 90-day soft-stale + 365-day hard-stale cadence matches industry SOC 2 expectations for vendor-control reassessment. The hard-stale auto-override is the defence-in-depth: even if the operator misses every weekly reminder, the 365th day automatically force-fails the gate.</li><li>The <code>attested_by</code> allow-list is small and PR-reviewed. Adding an auditor domain (e.g., a new SOC 2 firm) requires a code change with reviewer approval — keeping the attestation surface small and known.</li><li>HTTPS-only source URLs is a small but real defence against documentation tampering. A copy-pasted <code>http://</code> URL might survive operator review by accident; the parser-level rejection makes accidents impossible.</li><li>The hard-stale override (§1 #9) is the single most important defensive property here. Without it, a benign \"we forgot to refresh the attestation\" becomes a silent compliance failure that an auditor might catch a year later. With it, the failure is loud (calls refused) the day after the 365-day mark — operators get pinged immediately.</li><li>The audit row (<code>ai.zdr_violation</code>) is the proof-of-refusal primitive. Regulators investigating \"did you ever route PDPL data to a non-ZDR provider for tenant X\" can search the chain for <code>ai.zdr_violation</code> rows scoped to that tenant; positive results prove the gate fired.</li><li>The Anthropic \"Enterprise plan only\" caveat in the YAML is documentation-only for slice 3. A tenant on a non-Enterprise Anthropic plan whose calls route to Anthropic direct will get ZDR-attested results from this gate but might NOT actually have ZDR coverage (Anthropic's enforcement is at the API key tier). FR-AI-022 will add the runtime tenant-tier check; until then, ops manually validates Enterprise tier per tenant during onboarding.</li><li>The OTel metric set (<code>ai_zdr_*</code>) is designed for direct dashboarding. The <code>attestations_expired_total</code> metric should pager-alert (sev-1) on any non-zero increment because it indicates the hard-stale safety net engaged — operators should reverify within hours, not days.</li><li>Future expansion (FR-AI-016): regional ZDR attestation. Vertex's \"is_zdr\" depends on which region the call routes to; current schema collapses all regions into one boolean. The residency-pinning FR will likely extend this schema with <code>regions: { vn: true, eu: true, us: false }</code> per attestation. Until then, the conservative approach is to mark Vertex as <code>is_zdr: false</code> (current state) until per-region attestation lands.</li></ul>\n<hr>\n<p><em>End of FR-AI-015. Status: draft (10/10 target).</em></p>";
