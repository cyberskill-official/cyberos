window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TIME-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TIME service <strong>MUST</strong> ship auto-detect proposal engine at <code>services/time/src/proposals/</code> watching PROJ activity, proposing TIME entries with confidence scores, requiring Member confirmation (no auto-create), 7-day expiry, and 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>proposal_source</code> enum: <code>('proj_status_change','proj_comment_burst','proj_attachment_added','git_commit','calendar_event')</code> per DEC-1441. Cardinality 5. Slice 2 active: proj_* (3 of 5); git_commit + calendar_event slice 3.</li></ol>\n<ol><li><strong>MUST</strong> define closed <code>proposal_state</code> enum: <code>('pending','accepted','rejected','expired')</code> per DEC-1442. Cardinality 4.</li></ol>\n<ol><li><strong>MUST</strong> define <code>time_proposals</code> table at migration <code>0008</code>: <code>(proposal_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_subject_id UUID NOT NULL, engagement_id UUID NOT NULL, project_id UUID, task_id UUID, source proposal_source NOT NULL, source_ref UUID NOT NULL, suggested_start TIMESTAMPTZ NOT NULL, suggested_end TIMESTAMPTZ NOT NULL, suggested_description TEXT, confidence_score INT NOT NULL CHECK (confidence_score BETWEEN 50 AND 100), state proposal_state NOT NULL DEFAULT 'pending', created_at TIMESTAMPTZ NOT NULL DEFAULT now(), expires_at TIMESTAMPTZ NOT NULL, accepted_entry_id UUID, trace_id CHAR(32))</code>. RLS scoped to Member.</li></ol>\n<ol><li><strong>MUST</strong> subscribe to PROJ NATS events via <code>proj_activity_watcher.rs</code>:</li></ol>\n<ul><li><code>proj.issue.status_changed</code>: fires <code>proj_status_change</code> proposal evaluator.</li><li><code>proj.comment.created</code>: per-issue rolling count → <code>proj_comment_burst</code> if &gt; 5 comments in 1h.</li><li><code>proj.attachment.added</code>: fires <code>proj_attachment_added</code>.</li></ul>\n<ol><li><strong>MUST</strong> compute confidence via <code>confidence_score.rs::score(source, context)</code>:</li></ol>\n<ul><li>Status change from In-Progress → Done: ~85 (likely user did work).</li><li>Comment burst: ~70 (probably discussion + work).</li><li>Attachment added: ~65 (artifact suggests work).</li><li>Multiple signals same hour: aggregate +10 each (cap 100).</li><li>User's role on engagement matches issue assignee: +5.</li><li>Issue assignee = Member: required (else skip).</li></ul>\n<ol><li><strong>MUST</strong> filter proposals &lt; 50 confidence per DEC-1444 — never persist.</li></ol>\n<ol><li><strong>MUST</strong> propose duration from event timestamps. For status_change: <code>now() - prior_status_change_at</code> (capped at 4h). For comment_burst: span of comments (capped at 2h). For attachment: 30min default.</li></ol>\n<ol><li>**MUST NEVER auto-create TIME entry per DEC-1445. Always requires <code>POST /v1/time/proposals/{id}/accept</code> from Member with body <code>{ duration_seconds_override?, description_override? }</code>. Handler:</li></ol>\n<ul><li>Validates Member owns proposal.</li><li>Creates TIME entry via FR-TIME-001 with Member's overrides applied.</li><li>Transitions state='accepted'.</li><li>Emits <code>time.proposal_accepted</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> support reject <code>POST /v1/time/proposals/{id}/reject</code> body <code>{ reason? }</code>. Transitions state='rejected'. Emits <code>time.proposal_rejected</code> sev-3.</li></ol>\n<ol><li><strong>MUST</strong> expire pending proposals past <code>expires_at</code> per DEC-1443 via daily job. Transition state='expired'. Emit <code>time.proposal_expired</code> sev-3.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/time/proposals?state=pending</code> for Member's pending list.</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1446. PII-scrub description via FR-MEMORY-111.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from PROJ event through proposal creation.</li></ol>\n<ol><li><strong>MUST NOT</strong> auto-create entry (DEC-1445).</li></ol>\n<ol><li><strong>MUST NOT</strong> emit &lt; 50 confidence (DEC-1444).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why never auto-create (§1 #8, DEC-1445)?</strong> AI-proposed wrong entries bill clients — fraud risk. Member confirmation is the security gate.</p>\n<p><strong>Why confidence filtering (§1 #6, DEC-1444)?</strong> Low-confidence proposals are noise; Members tune out → all proposals ignored → primitive worthless. 50% threshold = quality filter.</p>\n<p><strong>Why 7-day expiry (§1 #10, DEC-1443)?</strong> Older than a week, Member forgot; proposal is stale; clean up.</p>\n<p><strong>Why slice 2 not slice 1 (SHOULD)?</strong> Not required for billing pipeline; quality-of-life UX. Build after core TIME flow works.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0008_time_proposals.sql\nCREATE TYPE proposal_source AS ENUM ('proj_status_change','proj_comment_burst','proj_attachment_added','git_commit','calendar_event');\nCREATE TYPE proposal_state AS ENUM ('pending','accepted','rejected','expired');\n\nCREATE TABLE time_proposals (\n  proposal_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  member_subject_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  project_id UUID,\n  task_id UUID,\n  source proposal_source NOT NULL,\n  source_ref UUID NOT NULL,\n  suggested_start TIMESTAMPTZ NOT NULL,\n  suggested_end TIMESTAMPTZ NOT NULL,\n  suggested_description TEXT,\n  confidence_score INT NOT NULL CHECK (confidence_score BETWEEN 50 AND 100),\n  state proposal_state NOT NULL DEFAULT 'pending',\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  expires_at TIMESTAMPTZ NOT NULL,\n  accepted_entry_id UUID,\n  reject_reason TEXT,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_proposals_member_pending\n  ON time_proposals(member_subject_id, created_at DESC)\n  WHERE state = 'pending';\nALTER TABLE time_proposals ENABLE ROW LEVEL SECURITY;\nCREATE POLICY time_proposals_rls ON time_proposals\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND member_subject_id = current_setting('auth.subject_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND member_subject_id = current_setting('auth.subject_id')::uuid);\nREVOKE DELETE ON time_proposals FROM cyberos_app;\nGRANT UPDATE (state, accepted_entry_id, reject_reason) ON time_proposals TO cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">GET    /v1/time/proposals?state=pending\nPOST   /v1/time/proposals/{id}/accept\nPOST   /v1/time/proposals/{id}/reject</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>proposal_source cardinality 5</strong>.</li><li><strong>proposal_state cardinality 4</strong>.</li><li><strong>Status change generates proposal</strong> — issue done → proposal created with &gt; 50 confidence.</li><li><strong>Comment burst threshold</strong> — 6 comments in 1h → proposal.</li><li><strong>Accept creates TIME entry</strong> — accept → TIME entry persisted + state=accepted.</li><li><strong>Reject transitions</strong> — reject → state=rejected.</li><li><strong>Expire 7d</strong> — pending past 7d → expired.</li><li><strong>No auto-create</strong> — proposal exists but no TIME entry until accept.</li><li><strong>&lt; 50 confidence filtered</strong> — heuristic produces 40 → no row persisted.</li><li><strong>Accept respects override</strong> — duration_seconds_override applied to TIME entry.</li><li><strong>4 memory audit kinds emitted</strong>.</li><li><strong>RLS Member-scoped</strong>.</li><li><strong>PII scrub description</strong>.</li><li><strong>Trace_id from PROJ event preserved</strong>.</li><li><strong>Non-assigned Member skipped</strong> — issue assignee != Member → no proposal.</li><li><strong>Multi-source same hour aggregates</strong> — status + comment + attachment within 1h → single high-confidence proposal.</li><li><strong>Pending list sorted by created_at desc</strong>.</li><li><strong>Slice 2 sources only</strong> — git/calendar sources rejected at insert.</li><li><strong>Concurrent accept race</strong> — first wins; second sees state≠pending → 409.</li><li><strong>Audit on each transition</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn status_change_to_done_creates_proposal() {\n    let ctx = TestContext::with_member_assigned_to_issue().await;\n    ctx.publish_proj_event(\"issue.status_changed\", json!({\n        \"issue_id\": ctx.issue_id, \"from\": \"in_progress\", \"to\": \"done\",\n        \"assignee_id\": ctx.member_id\n    })).await;\n    tokio::time::sleep(Duration::from_millis(500)).await;\n\n    let count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM time_proposals WHERE member_subject_id=$1 AND state='pending'\")\n        .bind(ctx.member_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(count &gt;= 1);\n}\n\n#[tokio::test]\nasync fn accept_creates_time_entry() {\n    let ctx = TestContext::with_pending_proposal().await;\n    let r = ctx.accept_proposal(ctx.proposal_id, json!({})).await;\n    assert_eq!(r.status(), 201);\n    let entry_id: Uuid = r.json::&lt;serde_json::Value&gt;().await.unwrap()[\"entry_id\"].as_str().unwrap().parse().unwrap();\n    let exists: bool = sqlx::query_scalar(\"SELECT EXISTS(SELECT 1 FROM time_entries WHERE entry_id=$1)\")\n        .bind(entry_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(exists);\n}\n\n#[tokio::test]\nasync fn no_auto_create_without_accept() {\n    let ctx = TestContext::with_pending_proposal().await;\n    let count: i64 = sqlx::query_scalar(\"SELECT count(*) FROM time_entries WHERE member_subject_id=$1\")\n        .bind(ctx.member_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(count, 0);\n}\n\n#[tokio::test]\nasync fn expire_after_7d() {\n    let ctx = TestContext::with_old_pending_proposal(8).await;\n    ctx.run_expire_job().await;\n    let state: String = sqlx::query_scalar(\"SELECT state::text FROM time_proposals WHERE proposal_id=$1\")\n        .bind(ctx.proposal_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(state, \"expired\");\n}\n\n// 5.5..5.10: confidence filter, assignee match, audit, cardinality</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-PROJ-002 (NATS events to subscribe). <strong>Cross-module:</strong> FR-TIME-001 (entry create), FR-AI-003, FR-MEMORY-111.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>time.proposal_generated</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.proposal_generated\",\n  \"severity\": 3,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.time.detector\",\n  \"trace_id\": \"...\",\n  \"payload\": {\n    \"proposal_id\": \"0190...\",\n    \"member_subject_id_hash16\": \"f8a1...\",\n    \"engagement_id\": \"0190...\",\n    \"source\": \"proj_status_change\",\n    \"confidence_score\": 85,\n    \"expires_at\": \"2026-05-24T...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Git commit detection — slice 3.</li><li><strong>Deferred:</strong> Calendar event detection — slice 3.</li><li><strong>Deferred:</strong> ML confidence scoring (vs heuristics) — slice 3.</li><li><strong>Deferred:</strong> Bulk-accept (Monday morning batch confirm) — slice 3.</li><li><strong>Deferred:</strong> Decline-pattern learning (Member rejects X repeatedly → stop proposing) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>PROJ NATS event delivery failure</td><td>NATS retry</td><td>Proposal not created; sev-3</td><td>Inherent NATS retry</td></tr><tr><td>Confidence score &lt; 50</td><td>filter</td><td>Not persisted</td><td>Inherent</td></tr><tr><td>Member not assignee</td><td>check</td><td>Skipped</td><td>Inherent</td></tr><tr><td>Concurrent accept race</td><td>state check</td><td>Second 409</td><td>Inherent</td></tr><tr><td>Accepted proposal entry creation fails</td><td>rollback</td><td>State remains pending</td><td>Member retries</td></tr><tr><td>Expired proposal accept attempt</td><td>state check</td><td>409 + expired</td><td>Inherent</td></tr><tr><td>Proposal for non-existent issue</td><td>FK soft</td><td>Created; FK check at accept</td><td>Inherent</td></tr><tr><td>Member rejected then status revives</td><td>new proposal generated</td><td>Treated independently</td><td>Inherent</td></tr><tr><td>Proposal description PII not scrubbed</td><td>FR-MEMORY-111</td><td>Audit dropped; sev-3</td><td>Inherent</td></tr><tr><td>Confidence &gt; 100 from heuristic bug</td><td>CHECK constraint</td><td>INSERT fails</td><td>Bug fix</td></tr><tr><td>Multi-source proposals overlap timewise</td><td>each independent</td><td>Member chooses which to accept</td><td>Inherent</td></tr><tr><td>Pending list grows unbounded</td><td>expire job</td><td>Daily cleanup</td><td>Inherent</td></tr><tr><td>Trace_id from NATS dropped</td><td>propagation guard</td><td>Sev-3 if missing</td><td>Inherent</td></tr><tr><td>Slice-3 source attempted</td><td>CHECK CHECK on source enum</td><td>INSERT rejected</td><td>Inherent</td></tr><tr><td>Cross-tenant proposal injection</td><td>RLS</td><td>0 rows visible</td><td>Inherent</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> NATS subscriber consumes PROJ events as fire-and-forget; failures don't block PROJ.</p>\n<p><strong>§11.2</strong> Confidence heuristics tunable via tenant config (slice 3 enhancement).</p>\n<p><strong>§11.3</strong> Expire job runs daily 04:00.</p>\n<p><strong>§11.4</strong> Member assignee check prevents proposals for delegated work.</p>\n<p><strong>§11.5</strong> Accept handler creates entry via FR-TIME-001 standard path — same caps + validations.</p>\n<p><strong>§11.6</strong> Proposal description hashed via FR-MEMORY-111 in audit.</p>\n<p><strong>§11.7</strong> Multi-signal aggregation uses time-window join (1h).</p>\n<p><strong>§11.8</strong> Trace_id forwarded from PROJ event to proposal to accepted entry.</p>\n<p><strong>§11.9</strong> SHOULD priority — graceful degradation; if engine down, manual entry still works.</p>\n<p><strong>§11.10</strong> Per-Member pending count exposed via UI badge.</p>\n<hr>\n<p><em>End of FR-TIME-004 spec.</em></p>";
