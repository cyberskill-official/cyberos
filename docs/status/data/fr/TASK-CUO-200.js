window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-200"]="<h2>Summary</h2>\n<p>Build the read-only foundation of the CyberOS continuous-improvement harness: a daemon-or-CLI tool that walks the memory audit chain, computes per-skill and per-workflow anomaly metrics over a configurable window, and emits a daily <code>harness-report.md</code> listing which <code>self_audit.anomaly_signals</code> (declared in every SKILL.md frontmatter) tripped which thresholds. <strong>No mutation, no proposals, no auto-bumps — just visibility.</strong> This is the foundation Waves 2/3/4 (TASK-CUO-201/202/203) build on.</p>\n<h2>Problem</h2>\n<p>Every SKILL.md already declares <code>self_audit.anomaly_signals</code> (e.g. <code>confidence_low_streak</code>, <code>user_correction_streak</code>, <code>needs_human_rate_above</code>, <code>deterministic_drift</code>) and <code>human_fine_tune.signals_to_initiate</code> (acceptance-rate thresholds, HITL-pause rates, drift signals). The policy is fully specified. <strong>But nothing reads those signals.</strong> Operators have no way to know which skills are struggling, drifting, or repeatedly needing HITL until they manually scan audit logs — which doesn't happen at scale.</p>\n<p>Without a harness, the entire self-evolution architecture lives in frontmatter as dormant intent. Wave 1 lights it up by surfacing the signals deterministically, daily, in a markdown report Stephen can read in 5 minutes.</p>\n<h2>Proposed Solution</h2>\n<h3>§1 Normative requirements</h3>\n<ol><li><strong>MUST</strong> ship <code>cuo/core/harness.py</code> with a <code>compute_report(audit_dir, window: timedelta, skill_root) -&gt; HarnessReport</code> API that walks the audit chain and returns a structured report.</li><li><strong>MUST</strong> ship <code>cuo/core/harness_signals.py</code> with one Python function per declared signal type (<code>confidence_low_streak</code>, <code>user_correction_streak</code>, <code>rule_reversal_streak</code>, <code>needs_human_rate_above</code>, <code>deterministic_drift</code>, plus the <code>human_fine_tune.signals_to_initiate</code> set: <code>acceptance_rate_below</code>, <code>hitl_pause_rate_above</code>, <code>drift_signal_count_above</code>). Each signal function takes the windowed audit rows + the skill's threshold dict and returns <code>(tripped: bool, value: float, evidence_rows: list[dict])</code>.</li><li><strong>MUST</strong> support window arguments via duration strings (<code>24h</code>, <code>7d</code>, <code>30d</code>) parsed once and applied per-skill. <em>(traces_to: §1 #3 → AC #1)</em></li><li><strong>MUST</strong> read each skill's <code>self_audit.anomaly_signals</code> and <code>human_fine_tune.signals_to_initiate</code> blocks from its SKILL.md frontmatter at report time — no hard-coded threshold lists. <em>(traces_to: §1 #4 → AC #2)</em></li><li><strong>MUST</strong> emit a markdown report at <code>docs/harness/harness-report-&lt;YYYY-MM-DD&gt;.md</code> with sections: (a) skills with tripped signals (sorted by severity), (b) workflows with elevated <code>ROUTED_BACK</code> rates, (c) per-task routed-back history with <code>routed_back_count</code> over the window, (d) summary stats (total runs, total HITL pauses, total rework events).</li><li><strong>MUST</strong> include the matching audit-row IDs as <code>evidence:</code> cells in the report so operators can drill in via <code>cyberos history --row &lt;id&gt;</code>.</li><li><strong>MUST NOT</strong> mutate any skill, RUBRIC, contract, or workflow file. <strong>Read-only.</strong> Wave 2 (TASK-CUO-201) introduces proposal authoring.</li><li><strong>MUST</strong> add CLI subcommand <code>cyberos-cuo harness report --since &lt;window&gt; [--skill &lt;name&gt;] [--workflow &lt;id&gt;] [--out &lt;path&gt;]</code>. Default window <code>24h</code>; default path computed from date.</li><li><strong>SHOULD</strong> include a <code>--watch</code> mode that re-runs every N minutes and writes the latest report atomically, for use under a cron / systemd timer.</li><li><strong>MUST</strong> emit one <code>harness.report_emitted</code> memory audit row per run, with payload <code>{report_path, window, skills_with_signals, workflows_with_signals, evidence_row_count}</code>.</li></ol>\n<h3>§2 Out of scope (deferred to Waves 2/3/4)</h3>\n<ul><li>Authoring <code>refinement_proposal</code> artefacts (TASK-CUO-201)</li><li>Auto-applying minor bumps (TASK-CUO-202)</li><li>Workflow-level chain evolution (TASK-CUO-203)</li><li>Multi-tenant aggregation across BRAINs (long-term)</li></ul>\n<h2>Alternatives Considered</h2>\n<ol><li><strong>Grafana dashboard</strong> — over-engineered for a single-operator use case. The audit chain isn't yet in Prometheus/OTel (TASK-OBS-001 ships that). Markdown report is the fastest path.</li><li><strong>Inline in <code>cyberos verify</code></strong> — would muddle the doctor invariants (which check structural integrity) with the policy-anomaly signals (which check evolution-worthiness). Keep them separate.</li><li><strong>Per-skill <code>self-audit run</code> subcommand</strong> — useful but doesn't give cross-skill visibility. The aggregate report subsumes per-skill checks (which can still be added later).</li></ol>\n<h2>Success Metrics</h2>\n<table><thead><tr><th>metric</th><th>baseline</th><th>target</th><th>deadline</th></tr></thead><tbody><tr><td>Time to identify a struggling skill (manual today)</td><td>~30 min (grep audit logs)</td><td>&lt; 1 min (open report)</td><td>2026-05-31</td></tr><tr><td>Operator-perceived signal density of report</td><td>n/a (no report)</td><td>\"actionable\" rated ≥ 4/5 in first 4 reports</td><td>2026-06-15</td></tr><tr><td>Skills with at least one threshold declared in frontmatter</td><td>100% (already done)</td><td>100%</td><td>already met</td></tr></tbody></table>\n<h2>Scope</h2>\n<p>In scope: <code>cuo/core/harness.py</code>, <code>cuo/core/harness_signals.py</code>, signal functions for every declared signal type, markdown report formatter, CLI subcommand, audit row emission.</p>\n<h3>Out of scope</h3>\n<ul><li>Proposal authoring (Wave 2)</li><li>Auto-bumps (Wave 3)</li><li>Workflow chain evolution (Wave 4)</li><li>Per-workflow custom signals beyond the standard set</li></ul>\n<h2>Dependencies</h2>\n<ul><li><strong>TASK-MEMORY-112</strong> — episodic memory (provides <code>episode.logged</code> rows that some signals aggregate over)</li><li><strong>TASK-MEMORY-114</strong> — write-time importance (provides <code>memory.importance_scored</code> rows for <code>confidence_low_streak</code>)</li><li><strong>TASK-MEMORY-117</strong> — per-store ACL (provides <code>memory.acl_denied</code> rows that contribute to drift detection)</li><li><strong>TASK-MEMORY-120</strong> — <code>cyberos history</code> (provides the path-set + row-filter machinery the harness reuses for evidence linking)</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Anthropic Claude (the assistant authored the task body via the standard <code>task-author</code> chain).</li><li><strong>Scope:</strong> §1 normative clauses, §4 ACs, §5 test entries, alternatives section — all draft-generated and then revised by the operator.</li><li><strong>Human review:</strong> Stephen Cheng reviewed the spec end-to-end before audit; revisions are tracked in the audit-fix log per §10 of the sibling .audit.md.</li></ul>\n<h2>§4 Acceptance Criteria</h2>\n<ol><li><code>cyberos-cuo harness report --since 7d</code> produces a non-empty markdown file at <code>docs/harness/harness-report-YYYY-MM-DD.md</code>. <em>(traces_to: §1 #5, #8)</em></li><li>The report's \"Skills with tripped signals\" section lists at least one entry when running against a seeded chain with 11 <code>memory.fr_routed_back</code> rows for one task (above <code>acceptance_rate_below: 0.6</code> if any forward-runs exist). <em>(traces_to: §1 #2, #5)</em></li><li>Each tripped signal's row carries the skill name, signal id, observed value, threshold, and at least one evidence row ID. <em>(traces_to: §1 #6)</em></li><li>The report includes a \"Workflows with elevated rework\" section sorting workflows by <code>routed_back_count / total_runs</code> descending. <em>(traces_to: §1 #5)</em></li><li>Re-running the same command in <code>--watch</code> mode after one new event writes a new report atomically (write-to-temp then rename) without truncation. <em>(traces_to: §1 #9)</em></li><li>Per run, exactly one <code>harness.report_emitted</code> memory aux row is appended; payload validates against §1 #10. <em>(traces_to: §1 #10)</em></li><li>Running the harness on an audit chain that contains zero qualifying rows produces a report with all sections present but empty (no crash, no missing-key errors). <em>(traces_to: §1 #5)</em></li></ol>\n<h2>§5 Verification</h2>\n<ul><li><code>modules/cuo/tests/test_harness_report.py::test_report_emits_markdown</code> (covers AC #1)</li><li><code>modules/cuo/tests/test_harness_report.py::test_signal_thresholds_trip_correctly</code> (AC #2, #3)</li><li><code>modules/cuo/tests/test_harness_report.py::test_workflow_rework_rate</code> (AC #4)</li><li><code>modules/cuo/tests/test_harness_report.py::test_watch_mode_atomic_write</code> (AC #5)</li><li><code>modules/cuo/tests/test_harness_report.py::test_emits_audit_row</code> (AC #6)</li><li><code>modules/cuo/tests/test_harness_report.py::test_empty_chain_clean_exit</code> (AC #7)</li></ul>";
