window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DOC-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The DOC service <strong>MUST</strong> ship expiry alert cascade at <code>services/doc/src/expiry/</code> triggered daily, sending at 90/30/7-day thresholds via FR-EMAIL-009 + FR-CHAT-005, deduplicated, snooze-able, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> schedule daily scan at 06:00 tenant_tz per DEC-1720 via FR-MCP-007 cron.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>alert_threshold</code> against closed enum per DEC-1721.</li></ol>\n<ol><li><strong>MUST</strong> scan at <code>scanner.rs::scan(tenant, today)</code>:</li></ol>\n<ul><li>SELECT documents WHERE expiry_date IS NOT NULL AND lifecycle_status NOT IN ('expired','terminated').</li><li>Check if <code>expiry_date - today</code> matches d90/d30/d7 ±0d.</li><li>Filter snoozed docs per DEC-1724.</li></ul>\n<ol><li><strong>MUST</strong> dedup per DEC-1722 via UNIQUE constraint — skip if <code>(document_id, threshold)</code> row exists.</li></ol>\n<ol><li><strong>MUST</strong> dispatch at <code>notifier.rs::notify(doc, threshold, parties)</code>:</li></ol>\n<ul><li>For each party with email → FR-EMAIL-009 send</li><li>Tenant CLO → FR-CHAT-005 message</li><li>Use template: \"Contract {title} expires in {days}d. Renew or terminate?\"</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0003</code>: ```sql CREATE TABLE doc_expiry_alerts ( alert_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, document_id UUID NOT NULL, threshold TEXT NOT NULL CHECK (threshold IN ('d90','d30','d7')), sent_at TIMESTAMPTZ NOT NULL DEFAULT now(), recipients_count INT NOT NULL, trace_id CHAR(32), UNIQUE (tenant_id, document_id, threshold) ); ALTER TABLE doc_expiry_alerts ENABLE ROW LEVEL SECURITY; CREATE POLICY alerts_rls ON doc_expiry_alerts USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON doc_expiry_alerts FROM cyberos_app;</li></ol>\n<p>   CREATE TABLE doc_expiry_snoozes (      snooze_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      document_id UUID NOT NULL,      snoozed_until TIMESTAMPTZ NOT NULL,      reason TEXT,      snoozed_by UUID NOT NULL,      created_at TIMESTAMPTZ NOT NULL DEFAULT now(),      UNIQUE (tenant_id, document_id)    );    ALTER TABLE doc_expiry_snoozes ENABLE ROW LEVEL SECURITY;    CREATE POLICY snoozes_rls ON doc_expiry_snoozes      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    GRANT DELETE ON doc_expiry_snoozes TO cyberos_app;  -- CLO can un-snooze    ```</p>\n<ol><li><strong>MUST</strong> support CLO snooze per DEC-1724: ``<code>text POST   /v1/doc/documents/{id}/snooze-alerts   body: {until, reason} DELETE /v1/doc/documents/{id}/snooze-alerts   (un-snooze) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1725. PII per FR-MEMORY-111: title hashed; threshold + counts ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from cron → scanner → notifier → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> send duplicate alert per DEC-1722.</li></ol>\n<ol><li><strong>MUST NOT</strong> ignore active snooze per DEC-1724.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 90/30/7 (DEC-1720)?</strong> Industry standard escalation cadence; gives renewal negotiation runway.</p>\n<p><strong>Why dedup (DEC-1722)?</strong> Daily scan re-checks; without UNIQUE, same threshold fires repeatedly.</p>\n<p><strong>Why snooze (DEC-1724)?</strong> Active renewal negotiation makes alerts noise; CLO suppresses until they need them again.</p>\n<p><strong>Why both email + chat (DEC-1723)?</strong> Parties (external) need email; CLO (internal) needs chat.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST   /v1/doc/documents/{id}/snooze-alerts   body: {until: ISO8601, reason}\nDELETE /v1/doc/documents/{id}/snooze-alerts\nGET    /v1/doc/expiry-alerts                  (list sent + scheduled)\nPOST   /v1/doc/expiry-scan                    (CLO manual trigger)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Daily scan at 06:00 tenant_tz</strong>. 2. <strong>3-threshold enum + cardinality test</strong>. 3. <strong>Dedup via UNIQUE constraint</strong>. 4. <strong>Snooze suppresses all thresholds until snoozed_until</strong>. 5. <strong>Un-snooze (DELETE) re-enables</strong>. 6. <strong>Email to each party with email field</strong>. 7. <strong>Chat to tenant CLO</strong>. 8. <strong>4 memory audit kinds emitted</strong>. 9. <strong>PII scrubbed (title SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>Trace_id preserved</strong>. 12. <strong>Expired docs excluded from scan</strong>. 13. <strong>Terminated docs excluded from scan</strong>. 14. <strong>Boundary day exact match (90d = scan day matches)</strong>. 15. <strong>CLO manual trigger via POST</strong>. 16. <strong>Append-only alerts table</strong>. 17. <strong>Snooze can be re-set (UNIQUE on doc_id replaces)</strong>. 18. <strong>Failed send → status=failed; retry</strong>. 19. <strong>High-volume tenant (1000+ docs/day) handled</strong>. 20. <strong>No alert if expiry=null (legacy docs)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn alerts_at_exact_thresholds() {\n    let ctx = TestContext::doc_expires_in_days(90).await;\n    ctx.run_expiry_scan(today()).await;\n    let alerts = ctx.fetch_alerts(ctx.doc_id).await;\n    assert_eq!(alerts.len(), 1);\n    assert_eq!(alerts[0].threshold, \"d90\");\n}\n\n#[tokio::test]\nasync fn dedup_skips_second_scan() {\n    let ctx = TestContext::doc_expires_in_days(90).await;\n    ctx.run_expiry_scan(today()).await;\n    ctx.run_expiry_scan(today()).await;  // same day, same threshold\n    let alerts = ctx.fetch_alerts(ctx.doc_id).await;\n    assert_eq!(alerts.len(), 1);\n}\n\n#[tokio::test]\nasync fn snooze_suppresses_alerts() {\n    let ctx = TestContext::doc_expires_in_days(30).await;\n    ctx.snooze_alerts(ctx.doc_id, today() + Duration::days(60)).await;\n    ctx.run_expiry_scan(today()).await;\n    let alerts = ctx.fetch_alerts(ctx.doc_id).await;\n    assert_eq!(alerts.len(), 0);\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> FR-DOC-007. <strong>Cross-module:</strong> FR-EMAIL-009 (email send), FR-CHAT-005 (chat notify), FR-MCP-007 (cron), FR-AUTH-101 (CLO role), FR-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Email send fails</td><td>retry 3x</td><td>failed audit</td><td>next scan retries</td></tr><tr><td>Chat send fails</td><td>retry</td><td>failed audit</td><td>next scan retries</td></tr><tr><td>No party emails</td><td>warn</td><td>CLO chat only</td><td>inherent</td></tr><tr><td>Scanner missed run</td><td>last_run check</td><td>catch-up next boot</td><td>inherent</td></tr><tr><td>High doc volume</td><td>pagination</td><td>batched</td><td>inherent</td></tr><tr><td>Snooze past expiry</td><td>OK</td><td>won't alert post-expiry</td><td>inherent</td></tr><tr><td>Duplicate snooze race</td><td>UNIQUE</td><td>second wins (replace)</td><td>inherent</td></tr><tr><td>Doc has no expiry_date</td><td>filter</td><td>excluded</td><td>inherent</td></tr><tr><td>Doc status terminated</td><td>filter</td><td>excluded</td><td>inherent</td></tr><tr><td>Cross-tenant snooze</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Cron via FR-MCP-007 <code>kind: 'doc.expiry_scan'</code>, daily 06:00.</li><li>§11.2 Threshold check: <code>(expiry_date - today) IN (90,30,7)</code> (exact match — runs daily so each day-X-from-expiry triggers once).</li><li>§11.3 Notifier batches per-doc: all parties get same email body in one call.</li><li>§11.4 memory audit body: doc_id, threshold, recipients_count; title SHA256.</li><li>§11.5 Snooze: stored row with snoozed_until; scanner filters where snooze IS NULL OR snoozed_until &lt; now().</li></ul>\n<hr>\n<p><em>End of FR-DOC-008 spec.</em></p>";
