window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-IMP-073"]="<h2>§1 — Description</h2>\n<ol><li>CyberOS's Capacitor-based mobile shells (<code>apps/web/android</code>, <code>apps/web/ios</code>) <strong>MUST</strong> ship the CyberSkill brand icon in every app-icon density/size Android and iOS require, not Capacitor's generic default template icon left over from the one-time <code>npx cap add ios &amp;&amp; npx cap add android</code> scaffold step.</li></ol>\n<ol><li>Root cause, confirmed by direct comparison: <code>npx cap add android</code>/<code>npx cap add ios</code> (documented as the one-time init step in <code>capacitor.config.ts</code>'s own header comment) stamps each native project from Capacitor's own default project template, which includes a <strong>generic placeholder icon</strong> — confirmed at HEAD via <code>git show HEAD:&lt;path&gt;</code> to be a 48x48 blank/template PNG for every Android mipmap density, and Capacitor's equivalent generic default for the single iOS <code>AppIcon-512@2x.png</code> universal icon. Nothing in this repo's tooling ever wired icon generation into that scaffold step or into the release pipeline's <code>npx cap sync android</code>/<code>npx cap sync ios</code> calls (confirmed: <code>capacitor.config.ts</code> has no <code>@capacitor/assets</code> reference, and neither <code>apps/web/package.json</code> nor the root <code>package.json</code> declares that or any equivalent icon-generation dependency) — so the placeholder simply persisted, uncaught, from the moment the mobile shells were scaffolded through the current multi-platform store-submission push.</li></ol>\n<ol><li>Separately, and not by coincidence: <code>apps/desktop/src-tauri/icons/</code> already contains a <strong>correctly-branded, byte-verifiable icon set</strong> for both Android (<code>icons/android/mipmap-*/ic_launcher*.png</code>) and iOS (<code>icons/ios/AppIcon-*.png</code>) — a side effect of Tauri's own <code>tauri icon</code> generation tooling, which emits Android- and iOS-shaped asset variants even though CyberOS's actual mobile distribution channel is Capacitor, not Tauri's (still-experimental, unused-by-this-repo) mobile target. Tauri's <code>tauri icon</code> command is a general-purpose multi-platform icon generator bundled with the Tauri CLI: given one source image, it produces the full output set for every platform Tauri <em>can</em> target — desktop (<code>.icns</code>, <code>.ico</code>, PNG set), Windows Store tiles (<code>Square*Logo.png</code>, <code>StoreLogo.png</code>), and both mobile platforms — regardless of which targets a given <code>tauri.conf.json</code> actually enables for that project. CyberOS's <code>tauri.conf.json</code> only declares desktop bundle targets, but running <code>tauri icon</code> against the brand source image still wrote the Android/iOS variants to disk as a byproduct of the tool's normal behavior, which is how a correctly-branded mobile icon set ended up sitting unused in the repo the whole time this defect existed. The 15 Android files and 1 iOS file this FR's <code>modified_files</code> list touches are, file-for-file, the exact paths Capacitor's own project layout expects.</li></ol>\n<ol><li>The fix <strong>MUST</strong> be, and already has been (applied directly to the working tree in this session, per <code>source_decisions</code>), a straight filesystem copy of those 16 files from <code>apps/desktop/src-tauri/icons/{android,ios}/</code> into their <code>apps/web/{android,ios}/</code> equivalents — confirmed byte-identical post-copy via <code>sha256sum</code>/<code>shasum -a 256</code> (§5), not a re-generation, re-export, or any transformation that could introduce a visual mismatch against the desktop build's already-shipped icon.</li></ol>\n<ol><li>This FR <strong>MUST NOT</strong> be read as introducing a new brand asset — the source PNGs already exist in the repo, already ship in the signed/notarized desktop <code>.dmg</code>/NSIS installer today, and this FR's only job is making the mobile shells consistent with an icon CyberSkill has already approved and shipped elsewhere.</li></ol>\n<ol><li>A regression guard <strong>SHOULD</strong> exist (§6, deferred to implementation) so that a future <code>npx cap add</code>/<code>npx cap sync</code> re-scaffold, or a future rebrand of the desktop icon set without a corresponding mobile-icon update, does not silently reintroduce a placeholder-vs-brand mismatch that nothing in CI would catch today.</li></ol>\n<ol><li>The copied Android adaptive-icon foreground layer's internal safe-zone padding <strong>SHOULD</strong> be visually confirmed against a circular/squircle launcher mask (§9) before this fix is treated as fully closed, since neither this FR's hash-based verification (§5) nor a successful build (AC #4) can detect a sub-icon composition defect — only human visual inspection can.</li></ol>\n<ol><li>This FR <strong>MUST NOT</strong> modify <code>apps/web/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml</code> or any other adaptive-icon XML wiring — those files were confirmed already correct at HEAD (§3), and touching them is explicitly out of scope; only the 16 raster PNG files named in <code>modified_files</code> (§0) are in scope for this fix.</li></ol>\n<h2>§2 — Why this design</h2>\n<p><strong>Why fix this by copying an existing asset rather than treating it as a design task?</strong> The correct icon already exists, is already brand-approved (it ships in the desktop build today), and is already present in the exact pixel dimensions Android/iOS require, generated by Tauri's own tooling rather than a lossy manual re-export. Treating this as a design or asset-generation task would be solving a problem that doesn't exist — the actual defect is a missing wiring step (nothing ever copied the already-correct asset into the Capacitor project paths), not a missing asset.</p>\n<p><strong>Why does this matter enough to be <code>priority: MUST</code> rather than a cosmetic <code>SHOULD</code>, given the rest of this batch (FR-APP-003 through FR-APP-006) is <code>SHOULD</code>?</strong> Those four FRs extend CyberOS's distribution <em>surface area</em> (new stores, new package managers) — deferring any of them costs discoverability, not functionality, exactly as their own <code>risk_if_skipped</code> fields state. This FR is different: it sits directly upstream of task #6 (TestFlight/Play Console internal testing, already in progress) and any eventual production mobile release. A placeholder icon is the kind of defect that both platforms' review guidelines treat as release-blocking, and shipping it to real testers or reviewers — even internally — would be a visible, avoidable embarrassment for CyberSkill's own product, not a deferrable nice-to-have.</p>\n<p><strong>Why does this FR include an acceptance criterion (AC #4) it cannot itself verify as passing today, since <code>ANDROID_RELEASE</code>/<code>IOS_RELEASE</code> are both off?</strong> The alternative — dropping AC #4 because it can't be exercised right now — would let this FR reach <code>ready_to_implement</code> (or, worse, a future <code>done</code>) without ever committing to the one check that catches the failure-mode class §10 flags as most consequential (a build-time resource-linking failure that only a real <code>gradlew bundleRelease</code>/Fastlane archive run can surface). This mirrors FR-APP-003 through FR-APP-006's own pattern of listing acceptance criteria that depend on a currently-off gate (<code>MAS_RELEASE</code>, <code>MSSTORE_RELEASE</code>, <code>SNAP_RELEASE</code>, <code>PKGMGR_*_RELEASE</code>) being turned on for a real run — the criterion is real and will be checked, just not by this FR's own authoring-time verification pass (§5's explicit caveat under AC #3/#4).</p>\n<p><strong>Why <code>module: improvement</code> / <code>class: improvement</code> rather than <code>module: app</code> alongside FR-APP-003 through FR-APP-006?</strong> Those four FRs each add a genuinely new distribution <em>channel</em> — a new store, a new package registry, a new signing/submission surface — which is squarely <code>app</code> module territory by this repo's own convention (every FR in <code>docs/feature-requests/app/</code> adds or changes where/how CyberOS ships). This FR adds no new channel; it repairs a defect in a channel (Capacitor mobile) that already exists and was already scaffolded before this session began. That places it with the rest of the hardening/correctness work already living in <code>docs/feature-requests/improvement/</code> (FR-IMP-068 through FR-IMP-072, all bug-fix- or correctness-flavored, all landed in the same \"Wave E - 1.0.0 hardening closeout\" vein this FR's <code>phase</code> field echoes) rather than alongside net-new distribution-channel FRs.</p>\n<p><strong>Why is this FR's line count and section depth lighter than FR-APP-003 through FR-APP-006?</strong> Those four FRs each stood up a genuinely new distribution mechanism from a from-scratch design (new CI jobs, new external-repo relationships, new signing surfaces, multiple open architectural questions). This FR fixes one already-diagnosed, already-fixed, already-hash-verified defect in an existing mechanism — there is no architecture to design, no external review process to reason about, and no signing/submission-safety surface this FR touches. Per the authoring discipline's own stub/pure-infra exception, a narrowly-scoped, already-grounded bug-fix FR earns a leaner bar than a from-scratch distribution-channel FR; padding this document to match FR-APP-003's length would not make the fix more correct, only harder to review.</p>\n<p><strong>How does this FR relate to the icon-asset uncertainty already documented elsewhere in this session's batch?</strong> Two of this batch's other FRs flagged their own icon-completeness gaps rather than asserting confidence they hadn't earned: FR-APP-004's audit (ISS-001) caught a Microsoft Store manifest referencing an unconfirmed <code>Wide310x150Logo.png</code> asset and resolved it by removing the unconfirmed reference rather than guessing; FR-APP-006's §1 #9 and §10 flag the Homebrew Cask <code>zap trash:</code> uninstall-cleanup paths as unverified guesses requiring a real test before being treated as final. This FR is the same underlying theme playing out in the opposite direction — not an unconfirmed reference to something that might not exist, but a <em>confirmed</em> defect (the placeholder icon) with a <em>confirmed, hash-verified</em> fix already available in the repo. Read together, all three findings point at the same practical lesson for this codebase: icon/asset correctness across CyberOS's many build targets (desktop, MAS, MSIX, Snap, Flathub, Cask, winget, and now Capacitor mobile) is not something any one FR's authoring can assume from a file merely existing at the expected path — each target's actual rendered result needs its own confirmation, exactly as AC #3 and §9's safe-zone open question insist on here.</p>\n<p><strong>Why are <code>depends_on</code> and <code>blocks</code> both empty arrays despite <code>related_frs</code> naming FR-IMP-065 and FR-APP-001?</strong> This FR authoring deliberately kept the reciprocal dependency fields empty rather than populating them with a soft, non-binding relationship: FR-IMP-065 is an unauthored draft stub (§7's cross-module note) with no normative clauses yet to depend on or block, and FR-APP-001 (the CyberOS 1.0.0 initial release FR, predating this batch) is complete and shipped, so there is nothing live for this fix to formally gate. Populating either field with a relationship that isn't a real precondition or a real blocker would misrepresent the FR-dependency graph to any tooling or reviewer that treats those fields as load-bearing scheduling constraints rather than prose color — <code>related_frs</code> exists precisely to carry that softer, non-blocking kind of connection instead.</p>\n<p><strong>How does this FR close out the batch?</strong> FR-APP-003 through FR-APP-006 (Mac App Store, Microsoft Store, Linux stores, package managers) are all distribution-surface-area FRs sharing one design shape — a new CI job, a new signing/submission surface, a gate currently off by default. This FR is the batch's outlier by design: it is the one correctness fix rather than a new channel, its <code>priority</code> is <code>MUST</code> rather than <code>SHOULD</code>, and its fix predates its own documentation rather than the reverse. Ending the batch with a release-blocking correctness fix, after five FRs that each expand where CyberOS ships, reflects the actual order of operations a real release readiness review would follow: new channels are worth little if the app users actually download carries a broken icon.</p>\n<h2>§3 — API contract</h2>\n<p>Not applicable in the usual sense — this FR ships no new command, endpoint, or CI job surface. The \"contract\" is the fix itself: a deterministic, file-for-file mapping from the already-correct source paths to the already-known-defective destination paths.</p>\n<pre><code>apps/desktop/src-tauri/icons/android/mipmap-hdpi/ic_launcher.png            → apps/web/android/app/src/main/res/mipmap-hdpi/ic_launcher.png\napps/desktop/src-tauri/icons/android/mipmap-hdpi/ic_launcher_foreground.png → apps/web/android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png\napps/desktop/src-tauri/icons/android/mipmap-hdpi/ic_launcher_round.png      → apps/web/android/app/src/main/res/mipmap-hdpi/ic_launcher_round.png\n# ... identical pattern repeats for mipmap-mdpi, mipmap-xhdpi, mipmap-xxhdpi, mipmap-xxxhdpi\napps/desktop/src-tauri/icons/ios/AppIcon-512@2x.png                        → apps/web/ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png</code></pre>\n<p><code>apps/desktop/src-tauri/icons/android/mipmap-anydpi-v26/ic_launcher.xml</code> and <code>apps/desktop/src-tauri/icons/android/values/ic_launcher_background.xml</code> are <strong>not</strong> part of this mapping: Capacitor's own default Android project already ships an equivalent adaptive-icon XML wiring at <code>apps/web/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml</code>, and this FR does not disturb it — only the raster PNG layers underneath it need replacing, confirmed by inspecting that both trees' adaptive-icon XML already reference the same <code>@mipmap/ic_launcher_foreground</code>/<code>@mipmap/ic_launcher_background</code>/<code>@color/ic_launcher_background</code> symbol names.</p>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>All 16 files match their <code>apps/desktop/src-tauri/icons/{android,ios}/</code> source, byte-for-byte</strong> — <code>sha256sum</code> (or <code>shasum -a 256</code>) of every file in this FR's <code>modified_files</code> list is identical to its corresponding source-tree file. Already confirmed true for the working-tree state as of this FR's authoring (§5).</li><li><strong>No file outside this FR's <code>modified_files</code> list changed</strong> — <code>git diff --stat</code> against the commit containing this fix touches exactly these 16 paths and no others (no incidental <code>cap sync</code> regeneration of unrelated Capacitor project files bundled into the same commit).</li><li><strong>Visual sanity check passes</strong> — a human (Stephen) confirms all of the following, none of which this agent can verify itself (no image-rendering capability):</li></ol>\n<ul><li>At least one Android mipmap PNG per density and the iOS <code>AppIcon-512@2x.png</code> show the CyberSkill brand mark, correctly oriented, not a corrupted or off-center copy.</li><li>The Android adaptive-icon foreground layer's safe-zone padding (§9) looks correct when previewed under a circular or squircle launcher mask, not just a square one.</li><li>The optional contact-sheet aid (§6) may be used to speed this up, but is not itself a substitute for the check — a generated contact sheet nobody actually looks at does not satisfy this criterion.</li><li>None of the 16 files is visibly truncated, stretched, or shows compression artifacts inconsistent with the rest of the set — a quick outlier check across all 16 rather than a single-file spot-check, since §5's hash comparison cannot distinguish \"correct\" from \"consistently wrong.\"</li></ul>\n<ol><li><strong>The fix builds a valid Android bundle and iOS archive</strong> — the existing <code>android</code>/<code>ios</code> CI jobs in <code>release.yml</code> (gated on <code>ANDROID_RELEASE</code>/<code>IOS_RELEASE</code>, currently off) succeed through <code>npx cap sync android</code>/<code>npx cap sync ios</code> and the subsequent <code>gradlew bundleRelease</code>/Fastlane archive step without any icon-related build failure, once either gate is turned on for a real test run.</li><li><strong>Regression guard exists</strong> (§6) — some automated check (CI step, pre-commit hook, or documented manual checklist item in <code>docs/deploy/RELEASE.md</code>) exists so a future re-scaffold or desktop-icon rebrand cannot silently reintroduce this defect without at least a visible warning.</li><li><strong>This FR's own documentation is internally consistent</strong> — <code>modified_files</code> (§0), §3's mapping table, §5's verification script, and §8's per-density breakdown table all name the exact same 16 paths with no drift between them; a reviewer cross-checking any two of these four should never find a mismatch.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># AC #1 — every modified file matches its source byte-for-byte (already run and confirmed true\n# during this FR's authoring; re-run here as the standing verification a reviewer/CI can repeat)\nset -e\npairs=(\n  \"mipmap-hdpi/ic_launcher.png\" \"mipmap-hdpi/ic_launcher_foreground.png\" \"mipmap-hdpi/ic_launcher_round.png\"\n  \"mipmap-mdpi/ic_launcher.png\" \"mipmap-mdpi/ic_launcher_foreground.png\" \"mipmap-mdpi/ic_launcher_round.png\"\n  \"mipmap-xhdpi/ic_launcher.png\" \"mipmap-xhdpi/ic_launcher_foreground.png\" \"mipmap-xhdpi/ic_launcher_round.png\"\n  \"mipmap-xxhdpi/ic_launcher.png\" \"mipmap-xxhdpi/ic_launcher_foreground.png\" \"mipmap-xxhdpi/ic_launcher_round.png\"\n  \"mipmap-xxxhdpi/ic_launcher.png\" \"mipmap-xxxhdpi/ic_launcher_foreground.png\" \"mipmap-xxxhdpi/ic_launcher_round.png\"\n)\nfor p in \"${pairs[@]}\"; do\n  a=\"apps/desktop/src-tauri/icons/android/$p\"\n  b=\"apps/web/android/app/src/main/res/$p\"\n  ha=$(shasum -a 256 \"$a\" | cut -d' ' -f1)\n  hb=$(shasum -a 256 \"$b\" | cut -d' ' -f1)\n  if [ \"$ha\" != \"$hb\" ]; then echo \"MISMATCH: $p\"; exit 1; fi\ndone\nha=$(shasum -a 256 apps/desktop/src-tauri/icons/ios/AppIcon-512@2x.png | cut -d' ' -f1)\nhb=$(shasum -a 256 apps/web/ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png | cut -d' ' -f1)\nif [ \"$ha\" != \"$hb\" ]; then echo \"MISMATCH: ios AppIcon-512@2x.png\"; exit 1; fi\necho \"all 16 files verified byte-identical to source\"\n\n# AC #2 — no unrelated files in the same change\ngit diff --stat HEAD -- apps/web/android apps/web/ios | grep -v \"16 files changed\" || true</code></pre>\n<p>AC #3 (visual sanity check) and AC #4 (a real gated CI run) are not scriptable within this agent's own verification and are recorded here as required human/CI steps rather than asserted as already passing. AC #6 (internal document consistency) was checked manually during this FR's authoring by comparing <code>modified_files</code>, §3's table, §5's script above, and §8's breakdown table side-by-side — all four independently list the same 16 paths.</p>\n<h2>§6 — Implementation skeleton</h2>\n<p>The fix itself (§3's mapping) is already applied to the working tree; nothing further to implement there. The one deferred piece is the regression guard (§1 #6, AC #5), intentionally left unspecified in detail rather than guessed at:</p>\n<ul><li><strong>Option A (cheapest): a documentation checklist item</strong> in <code>docs/deploy/RELEASE.md</code> under the existing mobile-release one-time-setup section, instructing whoever next runs <code>npx cap add</code>/re-scaffolds either native project to re-copy the icon set from <code>apps/desktop/src-tauri/icons/{android,ios}/</code> before the next release build.</li><li><strong>Option B (stronger): a CI assertion step</strong> added to the <code>android</code>/<code>ios</code> jobs in <code>release.yml</code>, run immediately after <code>npx cap sync</code>, that hash-compares the same 16 files this FR's §5 script checks and fails the job loudly if they've drifted from the desktop icon source — catching both an accidental re-scaffold and a future desktop-icon rebrand that forgot to update the mobile copies.</li><li>The choice between A and B, and the exact CI step wording if B is chosen, is deferred to whoever implements this FR's remaining sub-task (§0 <code>sub_tasks</code>), since it is a policy decision (how much CI weight to add to two currently-off-by-default mobile jobs) rather than a fact this FR's own research can settle unilaterally.</li></ul>\n<p>Trade-offs to weigh when making that choice (recorded here so the decision isn't made blind, without dictating the outcome):</p>\n<ul><li>Option A costs nothing in CI time or maintenance surface, but relies entirely on a human remembering to follow a documentation checklist — the exact kind of manual step that this defect's own root cause (§1 #2) shows can be silently skipped for an extended period without anyone noticing.</li><li>Option B adds a small, fixed amount of CI time (16 hash comparisons, sub-second) to two jobs that are currently off by default (<code>ANDROID_RELEASE</code>/<code>IOS_RELEASE</code>) and therefore rarely run today, meaning its ongoing cost is close to zero until mobile release cadence increases — but it does add one more CI step someone has to understand and maintain going forward.</li><li>Option B is strictly stronger at catching the specific regression this FR fixes (a silent re-scaffold or rebrand), since it converts a \"someone has to remember and notice\" failure mode into a \"the build fails loudly\" one — directly closing the gap §10's first two failure-mode rows describe.</li><li>Neither option addresses the AC #3 human-visual-check class of defect (corrupted-but-hash-consistent source, safe-zone padding) — that risk exists regardless of which regression guard is chosen, since both options are hash-based, not visual.</li></ul>\n<p><strong>Optional aid for AC #3's visual check:</strong> rather than opening 16 individual PNGs one at a time, a single contact-sheet image makes the human visual check (and the §9 safe-zone check) faster and less error-prone. Illustrative only — exact tool availability (ImageMagick's <code>montage</code>, or a macOS-native equivalent via <code>sips</code>) not asserted as pre-installed, and not required for AC #3 to be satisfied by other means:</p>\n<pre><code class=\"language-bash\"># Illustrative helper, not a required part of this FR's own verification (§5) — produces one\n# contact-sheet PNG so AC #3's human visual check can review all 16 files at a glance.\nmontage \\\n  apps/web/android/app/src/main/res/mipmap-*/ic_launcher.png \\\n  apps/web/android/app/src/main/res/mipmap-*/ic_launcher_foreground.png \\\n  apps/web/android/app/src/main/res/mipmap-*/ic_launcher_round.png \\\n  apps/web/ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png \\\n  -tile 4x4 -geometry 128x128+4+4 -label '%f' \\\n  /tmp/cyberos-mobile-icon-review.png\nopen /tmp/cyberos-mobile-icon-review.png   # macOS; Stephen's actual review happens here</code></pre>\n<p>If Option B is chosen, a plausible (illustrative, not final — exact placement within the existing <code>android</code>/<code>ios</code> jobs to be confirmed against <code>release.yml</code>'s current step ordering at implementation time) shape for the guard:</p>\n<pre><code class=\"language-yaml\"># Illustrative only — insert immediately after \"npx cap sync android\" in the existing `android` job\n# (and the iOS equivalent after \"npx cap sync ios\" in the `ios` job); exact step name/placement is\n# this FR's §6 Option B, deferred to implementation rather than asserted as the final wording here.\n- name: Assert mobile app icons still match the desktop brand source\n  run: |\n    fail=0\n    for p in mipmap-hdpi/ic_launcher.png mipmap-hdpi/ic_launcher_foreground.png mipmap-hdpi/ic_launcher_round.png \\\n             mipmap-mdpi/ic_launcher.png mipmap-mdpi/ic_launcher_foreground.png mipmap-mdpi/ic_launcher_round.png \\\n             mipmap-xhdpi/ic_launcher.png mipmap-xhdpi/ic_launcher_foreground.png mipmap-xhdpi/ic_launcher_round.png \\\n             mipmap-xxhdpi/ic_launcher.png mipmap-xxhdpi/ic_launcher_foreground.png mipmap-xxhdpi/ic_launcher_round.png \\\n             mipmap-xxxhdpi/ic_launcher.png mipmap-xxxhdpi/ic_launcher_foreground.png mipmap-xxxhdpi/ic_launcher_round.png; do\n      a=\"apps/desktop/src-tauri/icons/android/$p\"\n      b=\"apps/web/android/app/src/main/res/$p\"\n      [ \"$(sha256sum \"$a\" | cut -d' ' -f1)\" = \"$(sha256sum \"$b\" | cut -d' ' -f1)\" ] || { echo \"DRIFT: $p\"; fail=1; }\n    done\n    [ \"$fail\" -eq 0 ] || exit 1</code></pre>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none — this fix touches only already-existing, already-committed source assets on both sides of the copy.</li><li><strong>Blocking on this FR from other in-flight work:</strong> none identified during this authoring pass; no other open FR in this repo's <code>docs/feature-requests/</code> tree was found (via the checks in <code>source_pages</code>/§2) to have a hard dependency on the Capacitor mobile shells' icon state specifically.</li><li><strong>This FR blocking other future work:</strong> none named explicitly; a future FR authoring Capacitor mobile-shell store submissions (the still-unauthored FR-IMP-065 scope) would reasonably want this fix landed first as a precondition, but that dependency does not exist yet since FR-IMP-065 has no normative clauses to depend on this fix.</li><li><strong>Downstream:</strong> task #6 (adding 14 testers to TestFlight/Play Console internal testing) and any eventual <code>ANDROID_RELEASE=true</code>/<code>IOS_RELEASE=true</code> production run both benefit from this landing first, though neither is technically blocked at the tooling level — Play/App Store review is what would actually reject a placeholder-icon build, not anything in this repo's own CI.</li><li><strong>Cross-module:</strong> <code>FR-IMP-065</code> (\"Track B: mobile shells and store release pipeline,\" currently an unauthored stub) is the broader track this fix's icon-quality concern eventually belongs under; this FR is deliberately kept independent of FR-IMP-065's own authoring status since the bug and its fix are self-contained and don't need FR-IMP-065's broader scope resolved first.</li><li><strong>Sibling FRs in this batch:</strong> none of FR-APP-003 through FR-APP-006 depend on or are depended on by this fix — they extend desktop, Windows, and Linux distribution respectively, none of which touch the Capacitor mobile shells this FR corrects. The only connection is thematic (§2's closing paragraph), not a scheduling dependency.</li><li><strong>Batch authoring order:</strong> this FR was authored fifth and last in the session's batch, after FR-APP-003 through FR-APP-006, per the topological-order continuation the FR-authoring skill's own policy requires — no other FR in this batch was left pending or skipped ahead of this one.</li><li><strong>No user chat message occurred between the batch's APPROVE and this FR's completion</strong> — all five FRs, including this one, were authored under the single standing approval per the skill's continuation-policy discipline, without a further per-FR confirmation gate.</li><li><strong>Human/account prerequisites:</strong> none for the fix itself (no account, no paid enrollment, no external credential). Three concrete actions remain Stephen's, consistent with this agent's standing constraint against committing/merging on its own initiative and against modifying CI-gating repo variables:</li><li>Commit the fix (§0 <code>sub_tasks</code>), ideally with a message that references this FR's id and root-cause summary (§1 #2) for a clean audit trail.</li><li>Perform AC #3's visual check (including the safe-zone sub-item, §9), optionally using §6's contact-sheet aid.</li><li>At whatever point Android/iOS mobile testing is ready to proceed (task #6 in the tracker), turn on <code>ANDROID_RELEASE</code>/<code>IOS_RELEASE</code> for a real gated run that exercises AC #4 — this FR does not itself flip either flag, consistent with FR-APP-003 through FR-APP-006's own pattern of never self-enabling their own release gates.</li><li>Resolve §6's Option A/B regression-guard decision and implement whichever is chosen, since this authoring pass deliberately left that choice open rather than picking on Stephen's behalf.</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>Confirmed pre-fix state (Capacitor's default template, HEAD): a 48×48 RGBA PNG, <code>sha256</code> <code>27ed3603010ebc278f64f8645741ab132ff517abb5308eb9df6c8e42a48956b2</code> for <code>mipmap-mdpi/ic_launcher.png</code> — visibly different in both dimensions and content from any brand asset.</p>\n<p>Confirmed post-fix state (working tree, this session), a representative sample of the 16-file set (all 16 were verified per §5's script; these three are cited directly as evidence a reviewer can spot-check without re-running the full script):</p>\n<table><thead><tr><th>File</th><th>sha256 (source == destination)</th></tr></thead><tbody><tr><td><code>mipmap-mdpi/ic_launcher.png</code></td><td><code>bd102ab991c5a3fc2b59599e67faf60f2b6d5258d5485e47b38109b52149f995</code></td></tr><tr><td><code>mipmap-hdpi/ic_launcher_foreground.png</code></td><td><code>3a9c47a9bf575377f82cc4c1237329307577d25a6c722683d8369399f3b277bf</code></td></tr><tr><td><code>mipmap-xxxhdpi/ic_launcher_round.png</code></td><td><code>80e589e040d6352d2504e5efcdb4925b0852da572cbd59cbb4604ed55453cbd7</code></td></tr><tr><td><code>ios/AppIcon-512@2x.png</code></td><td><code>b778be9ffce98100378f3f5ee0de9c5305810133b02f47f2184c70eaec15e322</code></td></tr></tbody></table>\n<p>Full 16-file breakdown by Android density bucket and iOS, for a reviewer who wants to confirm this FR's <code>modified_files</code> list (§0) against the actual per-density file count Android's mipmap convention expects (3 files per density × 5 densities = 15, plus 1 iOS universal icon = 16 total):</p>\n<table><thead><tr><th>Directory / target</th><th>Files</th><th>Count</th></tr></thead><tbody><tr><td><code>mipmap-mdpi</code> (~1x baseline density)</td><td><code>ic_launcher.png</code>, <code>ic_launcher_foreground.png</code>, <code>ic_launcher_round.png</code></td><td>3</td></tr><tr><td><code>mipmap-hdpi</code> (~1.5x)</td><td><code>ic_launcher.png</code>, <code>ic_launcher_foreground.png</code>, <code>ic_launcher_round.png</code></td><td>3</td></tr><tr><td><code>mipmap-xhdpi</code> (~2x)</td><td><code>ic_launcher.png</code>, <code>ic_launcher_foreground.png</code>, <code>ic_launcher_round.png</code></td><td>3</td></tr><tr><td><code>mipmap-xxhdpi</code> (~3x)</td><td><code>ic_launcher.png</code>, <code>ic_launcher_foreground.png</code>, <code>ic_launcher_round.png</code></td><td>3</td></tr><tr><td><code>mipmap-xxxhdpi</code> (~4x)</td><td><code>ic_launcher.png</code>, <code>ic_launcher_foreground.png</code>, <code>ic_launcher_round.png</code></td><td>3</td></tr><tr><td><code>ios/App/App/Assets.xcassets/AppIcon.appiconset</code></td><td><code>AppIcon-512@2x.png</code> (single universal 1024×1024 catalog entry)</td><td>1</td></tr><tr><td><strong>Total</strong></td><td></td><td><strong>16</strong></td></tr></tbody></table>\n<p>Each density's three-file pattern (<code>ic_launcher.png</code> legacy square, <code>ic_launcher_foreground.png</code> adaptive-icon foreground layer, <code>ic_launcher_round.png</code> legacy round variant) exists for Android API-level backward compatibility: <code>ic_launcher</code>/<code>ic_launcher_round</code> serve pre-Android-8.0 launchers that predate the adaptive-icon system, while <code>ic_launcher_foreground</code> (paired with the untouched <code>ic_launcher_background</code>/<code>ic_launcher.xml</code> wiring per §3) serves API 26+ launchers that compose the final masked icon at render time. This FR's copy-only fix replaces all three per density uniformly — there is no case where only one of the three needed replacing, since Capacitor's default template was equally generic across all three legacy/adaptive variants.</p>\n<p>AC #2's <code>git diff --stat</code> shape once this fix is committed (illustrative — exact byte counts vary by PNG content, shown here only to confirm the expected file count and paths, not exact sizes):</p>\n<pre><code> apps/web/android/app/src/main/res/mipmap-hdpi/ic_launcher.png            | Bin 1234 -&gt; 5678 bytes\n apps/web/android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png | Bin 1234 -&gt; 5678 bytes\n ...\n apps/web/ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png | Bin 1234 -&gt; 5678 bytes\n 16 files changed, 0 insertions(+), 0 deletions(-)</code></pre>\n<h2>§9 — Open questions</h2>\n<ul><li><strong>Option A vs. Option B for the regression guard (§6)</strong> — deferred to implementation as a policy/cost decision, not a fact this FR's research could settle.</li><li><strong>Whether Tauri's mobile target will ever become CyberOS's actual mobile distribution mechanism</strong>, which would make <code>apps/desktop/src-tauri/icons/{android,ios}/</code> the canonical source by design rather than by side-effect-of-tooling coincidence — out of scope for this FR; today it is Capacitor, and this fix is correct under that fact regardless of how that question eventually resolves.</li><li><strong>Whether the Tauri-generated Android adaptive-icon foreground layer respects Android's conventional ~66% safe-zone padding</strong> (the center region a launcher actually keeps visible when masking the icon into a circle, squircle, or other shape) — this FR's authoring confirmed the foreground/background PNG pair is wired correctly (§3) but did not independently re-verify the foreground layer's internal padding against Android's safe-zone guidance, since that padding was baked in whenever <code>tauri icon</code> originally generated these files for the desktop build, not something this FR's copy-only fix could introduce or correct. Worth a visual check (folds into AC #3) on a real device/emulator with a circular launcher icon shape enabled, not assumed correct from the file existing.</li><li><strong>Whether §6's optional <code>montage</code> contact-sheet helper is worth promoting into a small, committed repo script</strong> (e.g. <code>tools/mobile-icon-contact-sheet.sh</code>) rather than living only as an illustrative snippet inside this FR's own §6 — a promoted, real script would be independently discoverable and reusable the next time any mobile-icon set needs a human visual pass (not just this one fix), but promoting it also means committing to maintaining a small utility script indefinitely for a check that, per §10's last failure-mode row, is already non-blocking and gracefully degrades to opening files individually if the tool is missing. Deferred to implementation as a judgment call about how much permanent tooling this one-off fix should leave behind, not a fact this FR's research could resolve unilaterally.</li><li><strong>Whether this fix should also be cross-referenced from task #12's Mac App Store submission work</strong> — the Mac App Store build (FR-APP-003) packages <code>apps/desktop</code>'s own already-correct icon set, not the Capacitor mobile shells this FR touches, so there is no direct file-level overlap; the connection is only the same-batch, same-session thematic one already drawn out in §2's closing paragraph (icon/asset correctness needing per-target confirmation, not assumption). No action item results from this open question — it is recorded here only so a future reader doesn't have to re-derive why FR-APP-003 and this FR don't share a dependency edge despite both concerning app icons.</li><li><strong>Whether the iOS <code>Contents.json</code> catalog's single-1024×1024-entry format is itself confirmed sufficient</strong> for every current App Store Connect submission path, or whether some legacy tooling in the release chain still expects the older multi-size icon set — flagged separately in §10's iOS-catalog-format row as a pre-existing question this FR's copy-only fix neither introduces nor resolves, but recorded here too since it is the one open question in this FR that isn't fully self-contained to the Capacitor mobile shells alone.</li><li><strong>Whether <code>language: n/a</code> (§0) is the right frontmatter convention for a binary-asset-only fix</strong>, versus some other value a backlog-tooling script might expect — flagged in §10's corresponding failure-mode row as a low-probability, easily-correctable edge case; this open question exists only to record that the choice was deliberate (no source-code language applies here) rather than an oversight, not because any specific tooling failure has actually been observed.</li><li><strong>Whether the case-insensitive-vs-case-sensitive filesystem risk (§10) is worth a dedicated CI check of its own</strong>, separate from §6's Option A/B regression guard which only checks content hashes, not path casing — deferred, since no actual casing drift has been observed in this fix and adding a dedicated check for a risk that hasn't materialized would be speculative engineering ahead of any evidence it's needed.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>A future <code>npx cap add</code>/re-scaffold of either native project resets the icon files back to Capacitor's default, undoing this fix</td><td>Not caught automatically today (§1 #6, §9's Option A/B decision is exactly why) — only caught by a human noticing the icon looks wrong again, or by the CI assertion if Option B is implemented</td><td>Same defect this FR fixes reappears, silently, with no build failure</td><td>§6's regression guard (once implemented) turns this from a silent regression into either a documented manual checklist step or a hard CI failure</td></tr><tr><td>The desktop icon set (<code>apps/desktop/src-tauri/icons/</code>) is rebranded in the future without someone remembering to re-run this FR's copy mapping (§3) for the mobile shells</td><td>Same as above — no automatic detection without §6's guard</td><td>Desktop and mobile builds ship visually inconsistent brand icons</td><td>Same recovery path as the row above; this is the other concrete scenario §6 exists to catch</td></tr><tr><td>AC #3's visual sanity check is skipped and one of the 16 copied files is subtly corrupted (e.g. a partial copy, truncated PNG) despite passing the hash check because the hash check itself was run against an already-corrupted source</td><td>The hash-comparison verification (§5) only proves destination matches source — it cannot catch a source file that was itself wrong to begin with</td><td>A wrong-but-hash-consistent icon ships</td><td>AC #3's human visual check exists specifically because §5's automated check has this structural blind spot; it is listed as a required, non-optional step for exactly this reason</td></tr><tr><td>Android's adaptive-icon XML wiring (<code>mipmap-anydpi-v26/ic_launcher.xml</code>, deliberately excluded from this FR's copy mapping per §3) references a <code>@mipmap/ic_launcher_foreground</code>/<code>_background</code> symbol name that doesn't actually match between the two trees, despite this FR's authoring having visually confirmed a match</td><td>Would surface as a build-time resource-linking error in AC #4's gated CI run (Android's <code>aapt</code>/Gradle resource compiler fails loudly on an unresolved mipmap reference), not silently</td><td>Android build fails, caught before any store submission, not after</td><td>AC #4 exists precisely to catch this class of error with a real build rather than trusting the confirmed-by-inspection symbol-name match alone</td></tr><tr><td>AC #4 cannot actually be exercised today — both <code>ANDROID_RELEASE</code> and <code>IOS_RELEASE</code> are currently <code>false</code>/unset, so the gated CI jobs this FR's fix needs to build-verify against don't run on an ordinary push</td><td>Not caught by this FR's own verification (§5 only proves file-level correctness, not a successful build) — only closed when Stephen turns on either flag for a real test run, which is outside this FR's own scope to trigger</td><td>This FR's <code>verify: T</code> status rests partly on a build-level check that is deferred, not yet executed, as of this FR reaching <code>ready_to_implement</code></td><td>Documented here rather than hidden — AC #4 stays open until a real gated run happens; this FR does not claim AC #4 is satisfied today, only that AC #1/#2 are</td></tr><tr><td>The Tauri-generated Android adaptive-icon foreground layer's internal safe-zone padding (§9) turns out to be insufficient for Android's circular/squircle launcher masks, clipping part of the CyberSkill mark when a user's launcher applies a non-square icon shape</td><td>Only caught by a real-device or emulator visual check with a circular icon shape enabled — not caught by any hash comparison (§5) or build step (AC #4), both of which are blind to sub-icon visual composition</td><td>Icon looks correct in a plain square launcher but visibly clipped in circular/squircle launcher themes, a partial-severity version of the defect this FR otherwise fully fixes</td><td>Folded into AC #3's visual check as an explicit sub-item (§9); if padding is found insufficient, the fix would need to trace back to the desktop <code>tauri icon</code> generation step, not this FR's copy-only mapping</td></tr><tr><td>§6's optional contact-sheet aid (<code>montage</code>) isn't installed in Stephen's review environment, or the command's exact flags don't match the ImageMagick version actually present</td><td>Immediate, loud (<code>montage: command not found</code> or a flag-parsing error) — not a silent failure</td><td>AC #3's review takes longer (16 files opened individually instead of one contact sheet) but is not blocked, since §6 explicitly marks this aid as optional</td><td>No recovery needed beyond falling back to opening files individually; the aid was never load-bearing for AC #3, only a convenience</td></tr><tr><td>The 16-file copy is performed on a case-insensitive filesystem (macOS default, this session's development environment) and one of the paths' casing drifts when the same operation is later repeated on a case-sensitive CI runner (Linux, <code>release.yml</code>'s actual build environment)</td><td>Would surface as a missing-file error in AC #4's gated CI run rather than silently — Android's resource compiler and Xcode's asset catalog tooling both fail loudly on a path that doesn't resolve exactly</td><td>AC #4's gated build fails on a path-casing mismatch that never appeared locally on macOS</td><td>§5's verification script uses the exact paths from §0's <code>modified_files</code> list as the source of truth, not a re-derived glob, which limits (but does not eliminate) the risk of a silent casing drift; AC #4's real CI run is the actual backstop</td></tr><tr><td>iOS's <code>Contents.json</code> asset-catalog format (confirmed single-entry, 1024×1024 universal icon per <code>source_pages</code>) is a modern format that some older Xcode/App Store Connect tooling versions do not accept, expecting the legacy multi-size icon set instead</td><td>Would surface as an App Store Connect upload rejection at submission time, not at this FR's own build or verification stage</td><td>A correctly-copied, byte-verified icon could still be rejected at submission if the <em>catalog format itself</em> (not the pixel content this FR touches) is outdated for the Xcode/tooling version in use</td><td>Out of scope for this FR's copy-only fix — <code>Contents.json</code> was not modified and was already present in this shape before this fix; if this risk materializes it is a separate, pre-existing catalog-format question, not a regression this FR introduced</td></tr><tr><td>This FR's <code>language: n/a</code> frontmatter value (§0), chosen because the fix is a binary-asset replacement with no source code changed, doesn't match any value a language-aware backlog-tooling script might expect if such a script exists elsewhere in the repo and was never audited as part of this FR's authoring</td><td>Would surface only if and when such tooling is run against this FR's frontmatter — not caught by this FR's own verification (§5), which only checks the icon files themselves</td><td>A hypothetical downstream tooling failure this FR's authoring cannot rule out without auditing every consumer of FR frontmatter across the repo, which is out of this FR's own scope</td><td>If this ever surfaces, the fix is a frontmatter-only correction to this FR's <code>language</code> field, not a change to the icon fix itself — recorded here as a known, low-probability, easily-correctable edge case rather than a blocking concern</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>This FR documents a fix already applied to the working tree</strong>, not a fix yet to be written — its purpose is to make that fix reviewable, testable, and regression-guarded rather than an unreviewed direct edit with no root-cause record. The remaining implementation work (§0 <code>sub_tasks</code>) is committing it and closing §6's open regression-guard decision, not writing new code.</li><li><strong>The byte-identical hash match between <code>apps/desktop/src-tauri/icons/{android,ios}/</code> and the fixed <code>apps/web/{android,ios}/</code> files is not incidental</strong> — it is the actual mechanism of the fix (a verbatim copy), and is restated in §5/§8 specifically so a reviewer doesn't need to re-derive that fact from first principles.</li><li><strong>No AI image generation, re-export, or any lossy transformation was used anywhere in this fix</strong> — consistent with this batch's anti-fabrication discipline, an already-correct, already-approved asset was reused verbatim rather than regenerated, eliminating any risk of a visually-different result from what the desktop build already ships.</li><li><strong>This fix directly unblocks task #6</strong> (adding testers to TestFlight/Play Console internal testing, already in progress as of this FR's authoring) — a placeholder-icon build would have been a poor first impression for the very testers that task is inviting, even before any public release consideration.</li><li><strong>If Option A (documentation-only guard, §6) is chosen</strong>, the specific insertion point is the mobile-release one-time-setup section already referenced by <code>capacitor.config.ts</code>'s own header comment (<code>cd apps/web</code>, <code>npm i -D @capacitor/core ...</code>, <code>npx cap add ios &amp;&amp; npx cap add android</code>) — appending the icon-recopy instruction immediately after that block keeps the one-time-setup instructions co-located rather than scattered across the docs tree.</li><li><strong>This FR's tracker entry corresponds to task #16</strong> (\"Fix Capacitor mobile app icon (iOS + Android placeholder)\") in the batch task list this session has maintained alongside FR-APP-003 through FR-APP-006 (tasks #12–#15); the two remaining task-list items this FR does not itself resolve — #6 (tester onboarding) and #12 (Mac App Store submission) — are cross-referenced in §7 and §9 respectively, but neither is blocked by this FR at the tooling level, only benefited by it landing first.</li><li><strong>This FR's <code>effort_hours: 2</code> estimate reflects work already substantially performed</strong>, not work yet to be scoped from scratch: 0.5h root-cause confirmation (done), 0.5h fix application (done), 0.5h commit-and-review (Stephen-gated, not yet done), 0.5h regression-guard implementation once §6's Option A/B decision is made (not yet done) — a materially different effort profile from FR-APP-003 through FR-APP-006's <code>effort_hours</code> estimates, which reflect from-scratch design and CI-integration work rather than a fix already sitting in the working tree.</li><li><strong>This agent has no image-rendering capability</strong>, which is why AC #3's visual sanity check is explicitly delegated to Stephen throughout this FR (§4, §7) rather than claimed as something this authoring pass already confirmed — every claim in this document about the icon files' <em>content</em> is grounded in hash comparison (§5, §8) and file-shape metadata (dimensions, format, byte size), never in an actual visual inspection this agent performed itself.</li><li><strong>This FR's self-audit (sibling <code>audit.md</code>) is written after this document reaches its final form</strong>, per the master rule's author-then-audit-then-revise-to-10/10 sequence already applied to every other FR in this batch — the findings recorded there reflect gaps identified during that structured review pass, not gaps this document was already known to have while still being drafted.</li><li><strong>This is the fifth and final FR in this session's batch</strong>, following FR-APP-003, FR-APP-004, FR-APP-005, and FR-APP-006 in authoring order — once this document and its sibling <code>audit.md</code> both reach <code>score_post_revision: 10/10</code>, the remaining batch-closing steps are the backlog insert-rows for all five FRs and a final summary report, not further FR authoring.</li><li><strong><code>class: improvement</code> and <code>module: improvement</code> (§0) intentionally match</strong>, consistent with §2's rationale for placing this fix in <code>docs/feature-requests/improvement/</code> rather than <code>docs/feature-requests/app/</code> alongside FR-APP-003 through FR-APP-006.</li><li><strong><code>status: ready_to_implement</code> is set from the start of this FR's authoring</strong>, not flipped from <code>draft</code> partway through as with FR-APP-006 — this reflects that the underlying fix was already applied and hash-verified before this document existed, so there was never a period during authoring where the fix itself was still undecided.</li></ul>\n<p><em>End of FR-IMP-073.</em></p>";
