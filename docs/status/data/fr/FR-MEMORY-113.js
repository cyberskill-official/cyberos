window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-MEMORY-113"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The recall ranking layer <strong>MUST</strong> replace the existing \"sort by relevance descending\" behaviour in <code>cyberos.core.semantic.recall(...)</code> and <code>cyberos.core.reader.recall(...)</code> with a <strong>combined score</strong> that blends semantic relevance, memory importance, and recency. The contract:</p>\n<ol><li><strong>MUST</strong> define the combined score as <code>combined_score = relevance · w_r + importance · w_i + recency · w_t</code> where the weights <code>(w_r, w_i, w_t)</code> are loaded from <code>manifest.json:recall_weights</code> (defaults <code>(0.4, 0.3, 0.3)</code> per DEC-190 and Park et al. 2023). Sum of weights MUST equal 1.0 within ±1e-6; the walker invariant <code>manifest-recall-weights-sum-to-one</code> rejects malformed manifests.</li><li><strong>MUST</strong> treat <code>relevance</code> as the cosine similarity (semantic backend) or BM25-normalised score scaled to <code>[0.0, 1.0]</code> (FTS5 backend) returned by the underlying engine. Engines that emit out-of-range values (e.g. SQLite FTS5 raw BM25 returns negative scores) are normalised by the engine adapter, not the ranking layer.</li><li><strong>MUST</strong> treat <code>importance</code> as the float <code>meta.importance ∈ [0.0, 1.0]</code> declared in the memory file's frontmatter. Absence is exactly 0.5 (DEC-192). The walker invariant <code>importance-range</code> rejects out-of-range values (error severity).</li><li><strong>MUST</strong> treat <code>recency</code> as the value returned by the active <strong>decay profile</strong> evaluated at <code>now - last_seen_at</code> (delta in hours). Two built-in profiles MUST be supported:</li></ol>\n<ul><li><strong>Exponential</strong> (default): <code>recency = decay_factor^hours_old</code>, default <code>decay_factor = 0.995</code> ⇒ half-life ≈ 138 hours ≈ 5.75 days. Park et al. 2023 form. (Note: the proposal earlier estimated 4 days; the precise math is <code>t_½ = ln(0.5) / ln(0.995) ≈ 138.3h ≈ 5.76 days</code>.)</li><li><strong>Ebbinghaus</strong>: <code>recency = exp(-hours_old / strength)</code>, default <code>strength = 240</code> (i.e. ~10-day characteristic time). MARS framework alignment.</li></ul>\n<ol><li><strong>MUST</strong> select the active decay profile via <code>manifest.json:recall_weights.decay_profile</code> (string enum <code>exponential | ebbinghaus</code>, default <code>exponential</code>) with <code>manifest.json:recall_weights.decay_params</code> (dict of profile-specific params). CLI override <code>--decay-profile {exponential|ebbinghaus}</code> MUST work without modifying the manifest (per-query override is non-persistent).</li><li><strong>MUST</strong> fall back to <code>recency = 1.0</code> when the hit's <code>last_seen_at</code> is absent or unparseable. This treats unknown-age memories as \"as fresh as possible\" — the conservative direction, since the alternative (treating them as ancient) would silently suppress legitimate hits.</li><li><strong>MUST</strong> preserve the existing <code>recall(...)</code> Python signature except for one new keyword argument <code>weights: Optional[RecallWeights] = None</code>. Callers that pass <code>weights=None</code> (legacy callers) get the ranking-applied result; callers that explicitly want raw relevance order set <code>weights=RecallWeights.relevance_only()</code> (programmatic, not a CLI flag — used only by internal tools that need to inspect raw similarity).</li><li><strong>MUST</strong> annotate every <code>RecallHit</code> with the four scalars (<code>relevance</code>, <code>importance</code>, <code>recency</code>, <code>combined_score</code>) so downstream consumers (CLI <code>--json</code>, FR-MEMORY-115 dream pipeline) can inspect the score derivation.</li><li><strong>MUST</strong> validate <code>manifest.json:recall_weights</code> at writer construction time (<code>Writer.__init__</code>), raising <code>ManifestError</code> on:</li></ol>\n<ul><li>sum of weights outside <code>[1.0 - 1e-6, 1.0 + 1e-6]</code></li><li>any weight outside <code>[0.0, 1.0]</code></li><li>unknown decay profile name</li><li>decay params that fail the profile's validator (e.g. <code>decay_factor ∉ (0.0, 1.0)</code> for exponential) Fail-fast means an operator typo doesn't silently degrade ranking quality on every recall.</li></ul>\n<ol><li><strong>MUST</strong> make <code>cyberos.core.ranking.score_hits()</code> a <strong>pure function</strong> (no I/O, no time.now() inside) — <code>now</code> is injected so tests are deterministic and downstream callers (FR-MEMORY-115 batch dream) can score against a snapshot timestamp.</li><li><strong>MUST</strong> add a benchmark <code>bench/bench_recall_latency.py</code> asserting that the ranking step adds ≤ 10% overhead over the underlying engine's retrieval cost on a 10,000-Episode store (e.g. semantic retrieval ~450 ms p95 → ranked recall ≤ 495 ms p95).</li><li><strong>MUST</strong> add <code>meta.importance</code> to the <code>MemoryFrontmatter</code> schema (general, not just Episode). The field is optional; semantics same as DEC-192.</li><li><strong>SHOULD</strong> support a third pluggable decay profile slot via <code>entry_points</code> so third parties / future research can register additional curves (<code>power_law</code>, <code>linear</code>, <code>mars_three_phase</code>) without modifying the core. Slice-4+ stretch.</li><li><strong>SHOULD</strong> emit an OTel span <code>memory.recall.scored</code> per recall invocation with attributes <code>hit_count</code>, <code>decay_profile</code>, <code>weights_hash</code>, <code>duration_ms</code>. Slice-4 stretch — gated by FR-OBS-001.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why 0.4 / 0.3 / 0.3 as defaults (§1 #1, DEC-190).</strong> Park et al. 2023 (\"Generative Agents\") publishes a calibrated weight triple on a benchmark generative-agents simulation. The weights aren't arbitrary — they were tuned so that semantic relevance dominates (40%) but importance and recency get meaningful weight (30% each). Adopting their numbers gives us a reasonable starting line; per-store override via manifest lets each memory tune. The constraint that they sum to 1.0 (§1 #1) keeps <code>combined_score</code> itself in <code>[0.0, 1.0]</code> and makes the relative weights interpretable as percentages.</p>\n<p><strong>Why exponential default + Ebbinghaus alternative (§1 #4, DEC-191).</strong> Two production-validated forgetting curves exist in the literature: simple exponential (used in Park et al. and most recsys) and Ebbinghaus (used in MARS [1] and the spaced-repetition tradition). They have meaningfully different shapes — exponential is \"smooth fade\", Ebbinghaus has a sharper early drop with a long tail. Some workloads favour one over the other (e.g. CRM-style \"this fact is current OR forgotten\" prefers Ebbinghaus). Defaulting to exponential matches the article's design and most RAG systems; offering Ebbinghaus opt-in covers the MARS-alignment use case.</p>\n<p><strong>Why <code>last_seen_at</code> absent → recency=1.0 (§1 #6).</strong> Two options: (a) treat absent as \"ancient\" (recency ≈ 0); (b) treat absent as \"fresh\" (recency = 1.0). Option (a) silently down-ranks memories that haven't been tagged with seen-timestamps — most likely freshly-imported memories from <code>cyberos import</code>. Option (b) treats them as \"I don't know how old this is, don't penalise it.\" We chose (b) because the failure mode of (a) (silent down-ranking) is invisible to the operator until they investigate \"why isn't memory X showing up in recall?\"; the failure mode of (b) (slight up-ranking of unstamped memories) is detectable by inspecting <code>combined_score</code> annotations (§1 #8). Visible failures &gt; invisible ones.</p>\n<p><strong>Why fail-fast at writer construction (§1 #9).</strong> Operator typos in <code>manifest.json</code> are common (we've seen weights <code>[0.5, 0.5, 0.5]</code> and <code>decay_factor: 1.5</code>). If we lazy-validate at first recall, the memory starts up cleanly and silently mis-ranks until someone notices. Fail-fast means <code>cyberos doctor</code> and <code>Writer.__init__</code> both refuse to operate on malformed config; the operator sees the error immediately.</p>\n<p><strong>Why pure-function <code>score_hits()</code> (§1 #10).</strong> Two callers will use this: (a) live <code>recall(...)</code> paths that score at query time with <code>now = datetime.utcnow()</code>, and (b) FR-MEMORY-115's batch dream pipeline that scores against a fixed snapshot timestamp (so multiple agents dreaming over the same window get identical scores). Injecting <code>now</code> keeps the function deterministic + makes both callers trivially testable.</p>\n<p><strong>Why a separate <code>decay.py</code> module (§1 #4).</strong> Decay profiles are plug-points. Keeping them in a dedicated module with a clear <code>DecayProfile</code> protocol (<code>__call__(hours_old: float) -&gt; float</code>) means slice-4 can add <code>power_law</code>, <code>mars_three_phase</code>, etc. without touching the ranking layer. The two-callers split keeps ranking and decay independently testable.</p>\n<p><strong>Why ≤ 10% ranking overhead budget (§1 #11).</strong> Ranking is a per-hit operation (constant time in the hit count). On a 10K-Episode store, recall returns ≤ 20 hits typically; 20 × small-float-arithmetic is dominated by retrieval cost. The 10% budget is a soft ceiling that catches accidental quadratic patterns (e.g. someone re-fetching <code>last_seen_at</code> from disk per-hit).</p>\n<p><strong>Why add <code>meta.importance</code> to general MemoryFrontmatter, not just Episode (§1 #12).</strong> The Park et al. formula applies to all memory kinds (a <code>decisions</code> entry is more important than a passing <code>facts</code> observation). Putting <code>importance</code> on the general frontmatter lets FR-MEMORY-114 (write-time importance scoring) target any kind, not just Episode. Forward-compatibility for FR-MEMORY-114 at zero cost.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Schema fragment</h3>\n<pre><code class=\"language-json\">{\n  \"$defs\": {\n    \"Importance\": {\n      \"type\": \"number\",\n      \"minimum\": 0.0,\n      \"maximum\": 1.0,\n      \"description\": \"Memory importance in [0.0, 1.0]; absent ≡ 0.5 (neutral).\"\n    },\n    \"MemoryFrontmatter\": {\n      \"properties\": {\n        \"importance\": {\"$ref\": \"#/$defs/Importance\"}\n      }\n    },\n    \"RecallWeights\": {\n      \"type\": \"object\",\n      \"required\": [\"relevance\", \"importance\", \"recency\"],\n      \"properties\": {\n        \"relevance\":     {\"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0},\n        \"importance\":    {\"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0},\n        \"recency\":       {\"type\": \"number\", \"minimum\": 0.0, \"maximum\": 1.0},\n        \"decay_profile\": {\"type\": \"string\", \"enum\": [\"exponential\", \"ebbinghaus\"], \"default\": \"exponential\"},\n        \"decay_params\":  {\"type\": \"object\", \"default\": {}}\n      }\n    }\n  }\n}</code></pre>\n<h3><code>cyberos.core.decay</code></h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/decay.py\nfrom __future__ import annotations\nfrom dataclasses import dataclass\nfrom datetime import datetime, timezone\nfrom math import exp\nfrom typing import Protocol\n\n\nclass DecayProfile(Protocol):\n    \"\"\"recency(hours_old) -&gt; float in [0.0, 1.0]; monotonically non-increasing.\"\"\"\n    def __call__(self, hours_old: float) -&gt; float: ...\n\n\n@dataclass(frozen=True)\nclass Exponential:\n    decay_factor: float = 0.995\n\n    def __post_init__(self) -&gt; None:\n        if not (0.0 &lt; self.decay_factor &lt; 1.0):\n            raise ValueError(f\"decay_factor must be in (0.0, 1.0); got {self.decay_factor}\")\n\n    def __call__(self, hours_old: float) -&gt; float:\n        if hours_old &lt; 0:                # future-dated last_seen → cap at 1.0\n            return 1.0\n        return self.decay_factor ** hours_old\n\n    @property\n    def half_life_hours(self) -&gt; float:\n        from math import log\n        return log(0.5) / log(self.decay_factor)\n\n\n@dataclass(frozen=True)\nclass Ebbinghaus:\n    strength: float = 240.0     # characteristic time in hours\n\n    def __post_init__(self) -&gt; None:\n        if self.strength &lt;= 0:\n            raise ValueError(f\"strength must be &gt; 0; got {self.strength}\")\n\n    def __call__(self, hours_old: float) -&gt; float:\n        if hours_old &lt; 0:\n            return 1.0\n        return exp(-hours_old / self.strength)\n\n\ndef build_profile(name: str, params: dict | None = None) -&gt; DecayProfile:\n    params = params or {}\n    if name == \"exponential\":\n        return Exponential(decay_factor=params.get(\"decay_factor\", 0.995))\n    if name == \"ebbinghaus\":\n        return Ebbinghaus(strength=params.get(\"strength\", 240.0))\n    raise ValueError(f\"unknown decay profile {name!r}\")\n\n\ndef hours_between(now: datetime, last_seen: datetime | None) -&gt; float | None:\n    if last_seen is None:\n        return None\n    if last_seen.tzinfo is None:\n        last_seen = last_seen.replace(tzinfo=timezone.utc)\n    if now.tzinfo is None:\n        now = now.replace(tzinfo=timezone.utc)\n    return (now - last_seen).total_seconds() / 3600.0</code></pre>\n<h3><code>cyberos.core.ranking</code></h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/ranking.py\nfrom __future__ import annotations\nfrom dataclasses import dataclass, replace\nfrom datetime import datetime, timezone\nfrom typing import Iterable, Optional\n\nfrom cyberos.core.decay import DecayProfile, hours_between\n\n\n@dataclass(frozen=True)\nclass RecallWeights:\n    relevance:  float = 0.4\n    importance: float = 0.3\n    recency:    float = 0.3\n\n    def __post_init__(self) -&gt; None:\n        for name, v in ((\"relevance\", self.relevance), (\"importance\", self.importance), (\"recency\", self.recency)):\n            if not (0.0 &lt;= v &lt;= 1.0):\n                raise ValueError(f\"{name} must be in [0.0, 1.0]; got {v}\")\n        if abs(self.relevance + self.importance + self.recency - 1.0) &gt; 1e-6:\n            raise ValueError(\n                f\"weights must sum to 1.0; got {self.relevance + self.importance + self.recency}\"\n            )\n\n    @classmethod\n    def relevance_only(cls) -&gt; \"RecallWeights\":\n        return cls(1.0, 0.0, 0.0)\n\n\n@dataclass\nclass ScoredHit:\n    path:          str\n    relevance:     float\n    importance:    float    # 0.5 if absent on the frontmatter\n    recency:       float    # 1.0 if last_seen unknown\n    combined_score: float\n    frontmatter:   dict\n    last_seen_at:  Optional[datetime]\n\n\ndef score_hits(\n    hits:    Iterable[\"RecallHit\"],\n    weights: RecallWeights,\n    decay:   DecayProfile,\n    now:     Optional[datetime] = None,\n) -&gt; list[ScoredHit]:\n    now = now or datetime.now(timezone.utc)\n    out: list[ScoredHit] = []\n    for h in hits:\n        imp = h.frontmatter.get(\"importance\", 0.5)         # DEC-192\n        h_old = hours_between(now, h.last_seen_at)\n        rec = decay(h_old) if h_old is not None else 1.0    # §1 #6\n        combined = h.relevance * weights.relevance + imp * weights.importance + rec * weights.recency\n        out.append(ScoredHit(\n            path=h.path, relevance=h.relevance, importance=imp, recency=rec,\n            combined_score=combined, frontmatter=h.frontmatter,\n            last_seen_at=h.last_seen_at,\n        ))\n    return sorted(out, key=lambda s: s.combined_score, reverse=True)</code></pre>\n<h3>Manifest fragment</h3>\n<pre><code class=\"language-json\">// .cyberos/memory/store/manifest.json (excerpt)\n{\n  \"store_version\": \"2.0.0\",\n  \"recall_weights\": {\n    \"relevance\":     0.4,\n    \"importance\":    0.3,\n    \"recency\":       0.3,\n    \"decay_profile\": \"exponential\",\n    \"decay_params\":  {\"decay_factor\": 0.995}\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Default weights</strong> — <code>RecallWeights()</code> constructs to <code>(0.4, 0.3, 0.3)</code> with <code>decay_profile=\"exponential\"</code>. <em>(traces_to: §1 #1)</em></li><li><strong>Weights sum-to-1.0 constraint</strong> — <code>RecallWeights(0.5, 0.3, 0.3)</code> (sum 1.1) raises <code>ValueError</code>; <code>RecallWeights(0.4, 0.3, 0.3)</code> succeeds. <em>(traces_to: §1 #1)</em></li><li><strong>Weights bounded to [0, 1]</strong> — <code>RecallWeights(-0.1, 0.55, 0.55)</code> raises (negative + still sums to 1.0); <code>RecallWeights(1.1, -0.05, -0.05)</code> raises. <em>(traces_to: §1 #1)</em></li><li><strong>Relevance passes through unchanged</strong> — <code>score_hits([Hit(relevance=0.84, importance=None, last_seen_at=now)])</code> annotates <code>relevance=0.84</code>. <em>(traces_to: §1 #2)</em></li><li><strong>Absent importance defaults to 0.5</strong> — Hit with no <code>importance</code> in frontmatter → <code>ScoredHit.importance == 0.5</code>. <em>(traces_to: §1 #3, DEC-192)</em></li><li><strong>Importance out-of-range rejected at walker</strong> — handcraft a memory file with <code>importance: 1.5</code> → <code>cyberos doctor</code> fails with <code>importance-range</code>. <em>(traces_to: §1 #3, §1 #12)</em></li><li><strong>Exponential half-life ≈ 138 h</strong> — <code>Exponential(0.995).half_life_hours</code> is within <code>[137.8, 138.5]</code>. <em>(traces_to: §1 #4)</em></li><li><strong>Exponential at hours_old=0 → 1.0</strong> — <code>Exponential()(0.0) == 1.0</code>. <em>(traces_to: §1 #4)</em></li><li><strong>Exponential monotonic decreasing</strong> — for <code>h1 &lt; h2</code>, <code>Exponential()(h1) &gt; Exponential()(h2)</code>. <em>(traces_to: §1 #4)</em></li><li><strong>Ebbinghaus at 0 → 1.0; finite for large h</strong> — <code>Ebbinghaus()(0) == 1.0</code>; <code>Ebbinghaus()(10000) &gt; 0</code>. <em>(traces_to: §1 #4)</em></li><li><strong>Decay parameter validation</strong> — <code>Exponential(decay_factor=1.5)</code> raises; <code>Exponential(decay_factor=-0.1)</code> raises; <code>Ebbinghaus(strength=0)</code> raises. <em>(traces_to: §1 #4, §1 #9)</em></li><li><strong>last_seen_at absent → recency=1.0</strong> — <code>score_hits([Hit(last_seen_at=None)], ...)</code> annotates <code>recency == 1.0</code>. <em>(traces_to: §1 #6)</em></li><li><strong>last_seen_at in the future → recency=1.0</strong> — caps at 1.0 rather than &gt; 1 or NaN. <em>(traces_to: §1 #4)</em></li><li><strong>Combined-score formula</strong> — <code>Hit(relevance=0.8, importance=0.6, recency=0.7)</code> with weights <code>(0.4, 0.3, 0.3)</code> → <code>combined == 0.8·0.4 + 0.6·0.3 + 0.7·0.3 == 0.71</code>. <em>(traces_to: §1 #1)</em></li><li><strong>Ordering</strong> — three hits with combined 0.91, 0.50, 0.70 → sorted output is <code>[0.91, 0.70, 0.50]</code>. <em>(traces_to: §1 #1)</em></li><li><strong>Profile override at CLI</strong> — <code>cyberos recall-similar X --decay-profile ebbinghaus</code> produces different <code>recency</code> than default <code>exponential</code> on the same hit. <em>(traces_to: §1 #5)</em></li><li><strong>Manifest weight fail-fast at construction</strong> — <code>manifest.json</code> with <code>recall_weights: {relevance: 0.5, importance: 0.5, recency: 0.5}</code> → <code>Writer(...)</code> raises <code>ManifestError</code> with a structured message naming <code>recall_weights.sum</code>. <em>(traces_to: §1 #9)</em></li><li><strong>Manifest unknown profile fail-fast</strong> — <code>decay_profile: \"made_up\"</code> → <code>ManifestError</code>. <em>(traces_to: §1 #9)</em></li><li><strong>Pure function — <code>now</code> injected</strong> — <code>score_hits(..., now=fixed_dt)</code> returns the same scores on two consecutive calls; no <code>datetime.now()</code> inside. <em>(traces_to: §1 #10)</em></li><li><strong>RecallHit annotations preserved</strong> — <code>ScoredHit</code> exposes <code>relevance</code>, <code>importance</code>, <code>recency</code>, <code>combined_score</code>. <em>(traces_to: §1 #8)</em></li><li><strong>Back-compat — relevance-only mode</strong> — <code>score_hits(hits, RecallWeights.relevance_only(), ...)</code> orders identically to pre-FR sort-by-relevance. <em>(traces_to: §1 #7)</em></li><li><strong>Bench latency budget</strong> — <code>bench_recall_latency.py --episodes 10000 --trials 100</code> records ranking-overhead p95 ≤ 10% of retrieval p95. <em>(traces_to: §1 #11)</em></li><li><strong><code>meta.importance</code> on a <code>facts</code> memory</strong> — non-Episode kind accepts the optional <code>importance</code> field; round-trips through reader. <em>(traces_to: §1 #12)</em></li><li><strong>FR-MEMORY-112 CLI calls real ranking</strong> — after migration, <code>cyberos recall-similar</code> returns identical <code>combined_score</code> to <code>score_hits()</code> invoked directly with same inputs (regression vs the FR-MEMORY-112 stub). <em>(traces_to: §1 #1)</em></li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_ranking_and_decay.py\nimport pytest\nfrom datetime import datetime, timedelta, timezone\nfrom cyberos.core.ranking import RecallWeights, ScoredHit, score_hits\nfrom cyberos.core.decay   import Exponential, Ebbinghaus\n\n\ndef make_hit(relevance, importance=None, last_seen_at=None, path=\"x\"):\n    \"\"\"Shim — real test conftest provides RecallHit factory.\"\"\"\n    from types import SimpleNamespace\n    fm = {} if importance is None else {\"importance\": importance}\n    return SimpleNamespace(path=path, relevance=relevance, frontmatter=fm, last_seen_at=last_seen_at)\n\n\ndef test_default_weights():\n    \"\"\"AC #1\"\"\"\n    w = RecallWeights()\n    assert (w.relevance, w.importance, w.recency) == (0.4, 0.3, 0.3)\n\n\n@pytest.mark.parametrize(\"w,err\", [\n    ((0.5, 0.3, 0.3), True),     # sum 1.1\n    ((0.4, 0.3, 0.3), False),\n    ((-0.1, 0.55, 0.55), True),  # negative\n    ((1.1, -0.05, -0.05), True), # out of range\n])\ndef test_weight_validation(w, err):\n    \"\"\"AC #2 + #3\"\"\"\n    if err:\n        with pytest.raises(ValueError):\n            RecallWeights(*w)\n    else:\n        RecallWeights(*w)\n\n\ndef test_importance_defaults_to_half():\n    \"\"\"AC #5\"\"\"\n    now = datetime.now(timezone.utc)\n    h = make_hit(relevance=0.8, importance=None, last_seen_at=now)\n    s = score_hits([h], RecallWeights(), Exponential(), now=now)\n    assert s[0].importance == 0.5\n\n\ndef test_last_seen_none_recency_one():\n    \"\"\"AC #12\"\"\"\n    now = datetime.now(timezone.utc)\n    h = make_hit(relevance=0.5, last_seen_at=None)\n    s = score_hits([h], RecallWeights(), Exponential(), now=now)\n    assert s[0].recency == 1.0\n\n\ndef test_last_seen_future_recency_one():\n    \"\"\"AC #13\"\"\"\n    now = datetime.now(timezone.utc)\n    future = now + timedelta(hours=24)\n    h = make_hit(relevance=0.5, last_seen_at=future)\n    s = score_hits([h], RecallWeights(), Exponential(), now=now)\n    assert s[0].recency == 1.0\n\n\ndef test_combined_score_formula():\n    \"\"\"AC #14\"\"\"\n    now = datetime.now(timezone.utc)\n    h = make_hit(relevance=0.8, importance=0.6, last_seen_at=now)\n    # last_seen_at == now ⇒ recency = exponential(0) = 1.0; we want 0.7 instead\n    s = score_hits([h], RecallWeights(0.4, 0.3, 0.3), Exponential(), now=now)\n    # With recency=1.0 the answer is 0.8*0.4 + 0.6*0.3 + 1.0*0.3 = 0.80\n    assert s[0].combined_score == pytest.approx(0.8)\n\n\ndef test_ordering_descending():\n    \"\"\"AC #15\"\"\"\n    now = datetime.now(timezone.utc)\n    hits = [\n        make_hit(relevance=0.1, importance=0.1, last_seen_at=now, path=\"low\"),\n        make_hit(relevance=0.9, importance=0.9, last_seen_at=now, path=\"high\"),\n        make_hit(relevance=0.5, importance=0.5, last_seen_at=now, path=\"mid\"),\n    ]\n    s = score_hits(hits, RecallWeights(), Exponential(), now=now)\n    assert [h.path for h in s] == [\"high\", \"mid\", \"low\"]\n\n\ndef test_pure_function_no_now_internal():\n    \"\"\"AC #19\"\"\"\n    now = datetime(2026, 5, 19, tzinfo=timezone.utc)\n    hit = make_hit(relevance=0.5, last_seen_at=now - timedelta(hours=10))\n    s1 = score_hits([hit], RecallWeights(), Exponential(), now=now)\n    s2 = score_hits([hit], RecallWeights(), Exponential(), now=now)\n    assert s1[0].combined_score == s2[0].combined_score\n\n\ndef test_relevance_only_mode():\n    \"\"\"AC #21\"\"\"\n    now = datetime.now(timezone.utc)\n    hits = [make_hit(relevance=0.3, importance=0.9, last_seen_at=now),\n            make_hit(relevance=0.7, importance=0.1, last_seen_at=now)]\n    s = score_hits(hits, RecallWeights.relevance_only(), Exponential(), now=now)\n    assert s[0].relevance &gt; s[1].relevance\n\n\ndef test_scored_hit_annotations():\n    \"\"\"AC #20\"\"\"\n    now = datetime.now(timezone.utc)\n    h = make_hit(relevance=0.5, importance=0.7, last_seen_at=now)\n    s = score_hits([h], RecallWeights(), Exponential(), now=now)\n    assert hasattr(s[0], \"relevance\")\n    assert hasattr(s[0], \"importance\")\n    assert hasattr(s[0], \"recency\")\n    assert hasattr(s[0], \"combined_score\")</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_digest.py\nimport pytest\nfrom cyberos.core.decay import Exponential, Ebbinghaus, build_profile\n\n\ndef test_exponential_half_life():\n    \"\"\"AC #7\"\"\"\n    e = Exponential(0.995)\n    assert 137.8 &lt; e.half_life_hours &lt; 138.5\n\n\ndef test_exponential_zero():\n    \"\"\"AC #8\"\"\"\n    assert Exponential()(0.0) == 1.0\n\n\ndef test_exponential_monotonic():\n    \"\"\"AC #9\"\"\"\n    e = Exponential()\n    assert e(1.0) &gt; e(2.0) &gt; e(3.0) &gt; e(10.0)\n\n\ndef test_exponential_negative_hours_caps_one():\n    \"\"\"AC #13\"\"\"\n    assert Exponential()(-10.0) == 1.0\n\n\ndef test_ebbinghaus_zero_one():\n    \"\"\"AC #10\"\"\"\n    eb = Ebbinghaus()\n    assert eb(0.0) == 1.0\n\n\ndef test_ebbinghaus_large_finite():\n    \"\"\"AC #10\"\"\"\n    assert 0 &lt; Ebbinghaus()(10000.0) &lt; 1.0\n\n\n@pytest.mark.parametrize(\"kls,kwargs\", [\n    (Exponential, {\"decay_factor\": 1.5}),\n    (Exponential, {\"decay_factor\": -0.1}),\n    (Exponential, {\"decay_factor\": 0.0}),\n    (Exponential, {\"decay_factor\": 1.0}),\n    (Ebbinghaus, {\"strength\": 0}),\n    (Ebbinghaus, {\"strength\": -1.0}),\n])\ndef test_decay_param_validation(kls, kwargs):\n    \"\"\"AC #11\"\"\"\n    with pytest.raises(ValueError):\n        kls(**kwargs)\n\n\ndef test_build_profile_unknown_name():\n    with pytest.raises(ValueError):\n        build_profile(\"made_up\", {})\n\n\ndef test_build_profile_known_names():\n    e1 = build_profile(\"exponential\", {\"decay_factor\": 0.99})\n    e2 = build_profile(\"ebbinghaus\",  {\"strength\": 100})\n    assert e1(0) == 1.0\n    assert e2(0) == 1.0</code></pre>\n<pre><code class=\"language-python\"># modules/memory/bench/bench_recall_latency.py\nimport argparse, time, statistics\nfrom cyberos.core.ranking import RecallWeights, score_hits\nfrom cyberos.core.decay   import Exponential\n\n\ndef main():\n    p = argparse.ArgumentParser()\n    p.add_argument(\"--episodes\", type=int, default=10000)\n    p.add_argument(\"--trials\",   type=int, default=100)\n    args = p.parse_args()\n    # Fixture: 10K mock hits with random relevance / importance / last_seen\n    from tests.fixtures.hit_factory import build_hits\n    hits = build_hits(args.episodes)\n    decay = Exponential()\n    weights = RecallWeights()\n    durations = []\n    for _ in range(args.trials):\n        t0 = time.perf_counter()\n        score_hits(hits[:20], weights, decay)        # 20-hit recall is the realistic shape\n        durations.append((time.perf_counter() - t0) * 1000)\n    p95 = statistics.quantiles(durations, n=20)[18]  # 95th percentile\n    print(f\"ranking-only p95 = {p95:.2f} ms\")\n    assert p95 &lt; 5.0, f\"ranking overhead p95 {p95:.2f}ms above 5ms budget\"\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>API contracts above are the skeleton. Implementation order:</p>\n<ol><li>Schema (memory.schema.json): add <code>Importance</code>, <code>RecallWeights</code>, extend <code>MemoryFrontmatter</code>.</li><li>Walker invariants: <code>manifest-recall-weights-sum-to-one</code>, <code>importance-range</code>, <code>decay-factor-range</code>.</li><li><code>cyberos/core/decay.py</code>.</li><li><code>cyberos/core/ranking.py</code>.</li><li><code>cyberos/core/writer.py</code>: load + validate <code>manifest.recall_weights</code> in <code>__init__</code>.</li><li>Swap <code>cyberos/core/semantic.py</code> + <code>cyberos/core/reader.py</code> to call <code>score_hits()</code>.</li><li>Update <code>cyberos/cli/recall.py</code> to delegate to <code>ranking</code>.</li><li>Tests + bench.</li><li>CHANGELOG entry.</li></ol>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-MEMORY-108 (depends on)</strong> — semantic search; we extend its <code>recall()</code> not replace it.</li><li><strong>FR-MEMORY-112 (depends on)</strong> — Episodes are the highest-stakes consumers of this ranking; the FR-MEMORY-112 stub <code>combined_score</code> is replaced by <code>ranking.score_hits</code>.</li><li><strong>FR-MEMORY-114 (this FR enables)</strong> — write-time importance scoring writes the <code>meta.importance</code> field that this FR consumes.</li><li><strong>FR-MEMORY-115 (this FR blocks)</strong> — <code>cyberos dream</code> calls <code>score_hits()</code> against a fixed snapshot timestamp to score candidate episodes in batch.</li><li><strong>FR-MEMORY-120 (this FR enables)</strong> — <code>cyberos history</code> surfaces the four scalar annotations so the agent can explain \"why did this rank first?\".</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Manifest</h3>\n<pre><code class=\"language-json\">// .cyberos/memory/store/manifest.json (excerpt)\n{\n  \"recall_weights\": {\n    \"relevance\":     0.4,\n    \"importance\":    0.3,\n    \"recency\":       0.3,\n    \"decay_profile\": \"exponential\",\n    \"decay_params\":  {\"decay_factor\": 0.995}\n  }\n}</code></pre>\n<h3>Recall response with ranking annotations</h3>\n<pre><code class=\"language-json\">{\n  \"backend\": \"semantic\",\n  \"weights\": {\"relevance\": 0.4, \"importance\": 0.3, \"recency\": 0.3},\n  \"decay\":   {\"profile\": \"exponential\", \"decay_factor\": 0.995},\n  \"matches\": [\n    {\n      \"path\": \"memories/episodes/d4/12/d4127a3b-1f2c3d.md\",\n      \"task\": \"Ship FR-AUTH-003 RLS enforcement\",\n      \"relevance\":      0.84,\n      \"importance\":     0.92,\n      \"recency\":        0.998,\n      \"combined_score\": 0.913,\n      \"last_seen_at\": \"2026-05-18T20:55:13Z\"\n    }\n  ]\n}</code></pre>\n<h3>Walker error on bad manifest</h3>\n<pre><code class=\"language-text\">ManifestError: recall_weights.sum != 1.0 (relevance=0.5 importance=0.5 recency=0.5 sum=1.5);\n  fix manifest.json:recall_weights or set CYBEROS_RECALL_WEIGHTS=auto to use defaults</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Third-party decay profiles via <code>entry_points</code> — §1 #13; slice 4+.</li><li><code>memory.recall.scored</code> OTel span — §1 #14; gated by FR-OBS-001.</li><li>Per-kind weight overrides (e.g. <code>decisions</code> ranks recency higher than <code>facts</code>) — slice 4+; needs schema extension.</li><li>A/B comparison harness that runs same query under multiple weight triples — slice 4+; ops tool, not a runtime feature.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Manifest weights sum != 1.0</td><td><code>Writer.__init__</code> ManifestError</td><td>Daemon refuses to start</td><td>Operator fixes manifest</td></tr><tr><td>Manifest unknown profile name</td><td>same</td><td>same</td><td>Operator names a supported profile</td></tr><tr><td>Decay param out of range in manifest</td><td>profile constructor raises</td><td>same</td><td>Operator fixes param</td></tr><tr><td>Memory file <code>importance</code> field out of range</td><td>walker <code>importance-range</code></td><td><code>cyberos doctor</code> non-zero</td><td>Operator fixes file (or <code>--repair</code> clamps to nearest valid)</td></tr><tr><td><code>last_seen_at</code> missing</td><td>recency fallback to 1.0</td><td>Memory ranks as fresh</td><td>None — by design</td></tr><tr><td><code>last_seen_at</code> unparseable timestamp</td><td>reader skips → None</td><td>recency fallback to 1.0</td><td>Operator fixes timestamp</td></tr><tr><td><code>now</code> in past relative to <code>last_seen_at</code> (clock skew)</td><td>profile receives negative hours_old; caps at recency=1.0</td><td>Memory ranks as fresh</td><td>None — by design</td></tr><tr><td>Pure-function violation (someone adds time.now() in score_hits)</td><td>unit test <code>test_pure_function_no_now_internal</code></td><td>CI blocked</td><td>Author removes the call</td></tr><tr><td>Ranking overhead exceeds 10% budget</td><td>bench <code>bench_recall_latency.py</code> asserts</td><td>CI blocked</td><td>Profile + optimise; likely cause is unnecessary I/O per-hit</td></tr><tr><td>Two hits with identical combined_score</td><td>Python sort is stable</td><td>Insertion order preserved</td><td>None — by design (acceptable non-determinism)</td></tr><tr><td>Hit with <code>importance</code> defined but as a string (\"0.5\")</td><td>jsonschema fails</td><td>walker invariant <code>importance-range</code> (with type check)</td><td>Operator fixes file</td></tr><tr><td>Default-decay user wants Ebbinghaus</td><td>manifest update</td><td>next recall uses new profile</td><td>None</td></tr><tr><td><code>decay_params</code> provided for wrong profile (e.g. <code>strength</code> on exponential)</td><td>profile constructor ignores unknown kwargs</td><td>exponential uses default decay_factor</td><td>If operator-intended, surface via doctor warning</td></tr><tr><td>Catastrophic decay (decay_factor → 0)</td><td>profile constructor allows</td><td>every hit gets recency ≈ 0; combined_score ≈ relevance * w_r</td><td>Operator picks a reasonable profile</td></tr><tr><td>Ranking applied to legacy code path that expected RecallHit shape</td><td>type-checked at score_hits call</td><td>TypeError; CI catches</td><td>Author updates call site</td></tr><tr><td>Bench fixture build_hits drifts from real Hit shape</td><td>bench test parametrizes against current Hit</td><td>CI catches</td><td>Author updates fixture</td></tr><tr><td>Weight tuple loaded from manifest with extra keys</td><td>jsonschema strict mode rejects</td><td><code>ManifestError</code></td><td>Operator removes extra keys</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why we don't memoize decay results.</strong> The decay function is so cheap (one <code>pow</code> or <code>exp</code> call) that memoization adds more overhead than it saves on the typical 20-hit recall.</li><li><strong><code>Exponential.half_life_hours</code> property</strong> — exposed because operators want to reason about decay in human terms (\"a memory at 1 week old has X% recency\"). The math is in the type so the operator doesn't have to redo it.</li><li><strong>Why <code>recall_weights</code> lives on <code>manifest.json</code>, not a separate config file.</strong> Manifest is already loaded at writer construction; reusing it keeps the failure surface narrow. A separate config means one more file to manage + one more invariant.</li><li><strong><code>build_profile()</code> is the slice-4 plug-point</strong> — when third-party profiles land via <code>entry_points</code>, <code>build_profile()</code> becomes the dispatcher. For now it's a stub if/elif because the slice-3 surface is exactly two profiles.</li><li><strong>Bench fixture lives in <code>modules/memory/tests/core/test_history.py</code></strong> — generates deterministic hits given a seed so the bench is reproducible across runs. Seed: <code>42</code>.</li><li><strong>The <code>test_pure_function_no_now_internal</code> test is the load-bearing invariant</strong> — without it, someone will eventually add <code>datetime.now()</code> inside <code>score_hits</code> and FR-MEMORY-115's batch dream will start producing non-deterministic scores. The unit test prevents that regression.</li><li><strong>Ranking is applied AFTER the engine's k-limit, not before.</strong> Engines retrieve top-k by their own internal score (relevance for vector, BM25 for FTS); we then re-rank with the combined score. This means the very top of the underlying engine's rank survives — the post-ranking is a fine-grained reordering, not a wholesale reordering.</li><li><strong>The 10% overhead budget is generous</strong> — empirically the operation is sub-millisecond for typical k=20. The budget exists to catch accidental regressions (e.g. someone forgetting to call <code>last_seen_at</code> from a derived index and going back to the filesystem).</li></ul>\n<hr>\n<p><em>End of FR-MEMORY-113.</em></p>";
