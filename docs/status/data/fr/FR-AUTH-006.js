window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> ship a <code>cyberos-auth</code> CLI binary providing bootstrap + ongoing operational tasks. Each subcommand:</p>\n<ol><li><strong>MUST</strong> support <code>bootstrap</code> to initialise tenant 0 + root-admin subject + initial signing key in a single Postgres transaction. Tenant 0 has UUID <code>00000000-0000-0000-0000-000000000000</code> (nil UUID) and slug <code>\"root\"</code>.</li><li><strong>MUST</strong> prompt for root-admin email + password interactively OR accept via env vars <code>CYBEROS_BOOTSTRAP_EMAIL</code> and <code>CYBEROS_BOOTSTRAP_PASSWORD</code> (for CI / Terraform). Email must pass FR-AUTH-002 §1 #2 validation; password must pass §1 #4 complexity rules. Interactive password input is masked via <code>rpassword</code> crate.</li><li><strong>MUST</strong> use bcrypt cost 12 for password hashing (matches FR-AUTH-002 §1 #3).</li><li><strong>MUST</strong> emit memory row <code>auth.bootstrap_completed</code> with payload: <code>tenant_0_id</code>, <code>root_admin_subject_id</code>, <code>initial_signing_key_kid</code>, <code>bootstrap_environment</code> (development | staging | production), <code>bootstrapped_by</code> (system user running CLI). The memory write is in the same transaction; rollback rolls back both.</li><li><strong>MUST NOT</strong> echo plaintext password in CLI output, memory row, OR any logs. The <code>Zeroizing&lt;String&gt;</code> discipline from FR-AUTH-002 applies; the password is hashed and dropped immediately.</li><li><strong>MUST</strong> create the initial RSA-2048 signing key during bootstrap by invoking FR-AUTH-004's <code>rotation::generate_new_signing_key</code>. Without this, FR-AUTH-004's <code>/v1/auth/token</code> cannot issue tokens (no active key exists). The kid is recorded in the bootstrap audit row.</li><li><strong>MUST</strong> be idempotent: running <code>cyberos-auth bootstrap</code> after success exits with code 5 (<code>AlreadyInitialised</code>) and message <code>\"Tenant 0 + root-admin + signing key already exist. Use --reset --confirm to recreate (destructive).\"</code>. The check is <code>SELECT EXISTS FROM tenants WHERE id = nil_uuid</code>.</li><li><strong>MUST</strong> support <code>cyberos-auth rotate-keys</code> to manually trigger FR-AUTH-004's quarterly rotation (the same function that the cron runs). Useful for emergency rotation (suspected key compromise).</li><li><strong>MUST</strong> support <code>cyberos-auth sweepers</code> to manually trigger cleanup of:</li></ol>\n<ul><li>Expired <code>sessions</code> rows (where <code>expires_at &lt; NOW()</code>).</li><li>Old <code>admin_idempotency_keys</code> rows (where <code>created_at &lt; NOW() - INTERVAL '24 hours'</code>).</li><li>Retired <code>signing_keys</code> rows (where <code>status='retired'</code> AND <code>retired_at &lt; NOW()</code>). Output reports counts deleted per table. Cron runs hourly; manual invocation is for ops investigation.</li></ul>\n<ol><li><strong>MUST</strong> support <code>--reset --confirm</code> for destructive re-bootstrap (drops tenant 0 + cascades). BOTH flags required; either alone exits with code 4.</li><li><strong>MUST</strong> refuse <code>--reset</code> in production environment (<code>CYBEROS_DEPLOYMENT_TIER=production</code>) unless <code>--force-prod-reset</code> is ALSO passed AND an interactive Y/N prompt confirms (with the deployment tier displayed). Non-tty input in production exits 4 unconditionally.</li><li><strong>MUST</strong> use standardised exit codes from the shared <strong><code>cyberos-cli-exit</code></strong> crate (single source of truth across CyberOS CLIs — see also FR-AI-021):</li></ol>\n<ul><li><code>0</code> Ok</li><li><code>1</code> UserError (bad args, validation failure)</li><li><code>2</code> AuthFailed (reserved — not raised by bootstrap, which is pre-auth)</li><li><code>3</code> RemoteUnreachable (DB or memory)</li><li><code>4</code> DestructiveWithoutConfirm OR ProductionResetBlocked</li><li><code>5</code> AlreadyInitialised (bootstrap rerun — distinct from UserError so CI scripts can detect \"already done, no action needed\" vs \"bad input, fix the call\")</li><li><code>6</code> SchemaViolation (password complexity, email format)</li><li><code>7</code> InternalError These numerical values are a stable cross-CLI contract; any module-specific extensions begin at <code>200</code> (AUTH module range).</li></ul>\n<ol><li><strong>SHOULD</strong> print summary to stdout on success: tenant 0 id, root-admin id (NOT email — privacy; operator knows what they typed), initial signing key kid, memory audit row request_id.</li><li><strong>SHOULD</strong> emit OTel span <code>auth.cli.bootstrap</code> (and <code>auth.cli.sweepers</code>, <code>auth.cli.rotate_keys</code>) with attributes <code>subcommand</code>, <code>outcome</code>, <code>deployment_tier</code>, <code>operator_id</code> (if from env, else <code>\"interactive\"</code>).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a CLI not an API endpoint (§1 #1)?</strong> The chicken-and-egg problem: creating tenant 0 + root-admin requires <code>POST /v1/admin/tenants</code> which requires a root-admin token which requires a root-admin subject which doesn't exist yet. The CLI breaks the cycle by writing directly to the DB (skipping the API layer). The trade-off is a separate code path; mitigated by reusing FR-AUTH-002's password-hash + email-validation logic.</p>\n<p><strong>Why initial signing key in bootstrap (§1 #6)?</strong> FR-AUTH-004's <code>/v1/auth/token</code> looks up the active signing key from <code>signing_keys</code> table. Without bootstrap creating one, the first token-issue request 500s with \"no active key.\" Bundling it into bootstrap means \"after <code>cyberos-auth bootstrap</code>, the system is fully operational\" — no second step required.</p>\n<p><strong>Why interactive password input + env-var fallback (§1 #2)?</strong> Interactive is for humans (operator running bootstrap on a fresh deployment). Env-var is for CI/CD (Terraform, Ansible). Both work; choose based on context. The Zeroizing wrapper applies to both paths — password bytes are overwritten after hashing regardless of source.</p>\n<p><strong>Why exit code 5 for AlreadyInitialised (§1 #7)?</strong> Distinct from \"user error\" (exit 1) so CI scripts can detect \"this was a re-run, no action needed\" vs \"bad input, fix the call.\" Stripe's CLI uses similar patterns. Code 5 is the shared <code>cyberos-cli-exit::ExitCode::AlreadyInitialised</code> constant — uniform across every CyberOS CLI.</p>\n<p><strong>Why double-flag for --reset (§1 #10)?</strong> Single-flag destructive operations are footguns. <code>--reset</code> looks scary but operators get habituated; the second <code>--confirm</code> flag is the deliberate-this-time gate. Terraform's <code>-destroy</code> requires similar interactive confirmation.</p>\n<p><strong>Why production-reset extra guard (§1 #11)?</strong> Resetting tenant 0 in production wipes EVERY tenant + EVERY subject + EVERY signing key. The destructive blast radius is total. The triple gate (<code>--reset</code> + <code>--confirm</code> + <code>--force-prod-reset</code> + interactive Y) makes accidental production destruction structurally impossible. Even malicious destruction requires deliberate steps that leave audit trail.</p>\n<p><strong>Why sweepers in this CLI (§1 #9)?</strong> Three tables grow unbounded without sweeping: <code>sessions</code>, <code>admin_idempotency_keys</code>, <code>signing_keys</code> (retired ones). Each FR's responsibility was \"the data exists for N hours/days then becomes stale\"; the sweeper is the implementation of \"becomes stale = deleted.\" Centralising in this CLI gives one place to audit + one cron to schedule.</p>\n<p><strong>Why <code>bootstrapped_by</code> in audit row (§1 #4)?</strong> Forensic question: \"who bootstrapped this deployment?\" The CLI captures the system user (from <code>whoami</code> or <code>USER</code> env) — not perfect identity (no JWT here; we're pre-auth) but better than nothing. Combined with the deployment_tier, gives a useful audit trail.</p>\n<p><strong>Why rotate-keys as a manual subcommand (§1 #8)?</strong> Quarterly cron is the routine; manual invocation is for emergency rotation (suspected key compromise). The same function backs both — cron just calls the CLI binary with <code>rotate-keys</code> argument. Operators familiar with the manual command know what cron does.</p>\n<p><strong>Why no email in summary output (§1 #13)?</strong> The operator just typed the email; echoing it in stdout adds nothing AND risks landing in logs. The id is sufficient for ops correlation; the operator knows what email they used.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-bash\">cyberos-auth --version\ncyberos-auth bootstrap [--email &lt;e&gt;] [--password-from-env]\n                       [--reset --confirm [--force-prod-reset]]\ncyberos-auth rotate-keys\ncyberos-auth sweepers</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/cli/bootstrap.rs\n#[derive(clap::Args)]\npub struct BootstrapArgs {\n    #[arg(long)] pub email: Option&lt;String&gt;,\n    #[arg(long)] pub password_from_env: bool,\n    #[arg(long)] pub reset: bool,\n    #[arg(long)] pub confirm: bool,\n    #[arg(long)] pub force_prod_reset: bool,\n}\n\npub async fn run(args: BootstrapArgs, pool: &amp;PgPool) -&gt; Result&lt;BootstrapResult, BootstrapError&gt; {\n    let env_tier = std::env::var(\"CYBEROS_DEPLOYMENT_TIER\").unwrap_or_else(|_| \"development\".into());\n\n    let exists: bool = sqlx::query_scalar(\n        \"SELECT EXISTS(SELECT 1 FROM tenants WHERE id = '00000000-0000-0000-0000-000000000000'::uuid)\"\n    ).fetch_one(pool).await?;\n\n    if exists &amp;&amp; !args.reset {\n        return Err(BootstrapError::AlreadyInitialised);\n    }\n    if args.reset &amp;&amp; !args.confirm {\n        return Err(BootstrapError::DestructiveWithoutConfirm);\n    }\n    if args.reset &amp;&amp; env_tier == \"production\" {\n        if !args.force_prod_reset {\n            return Err(BootstrapError::ProductionResetBlocked { reason: \"missing --force-prod-reset\".into() });\n        }\n        if !is_tty() {\n            return Err(BootstrapError::ProductionResetBlocked { reason: \"non-tty input in production\".into() });\n        }\n        prompt_interactive_y_n(\"Reset production tenant 0? This wipes all tenants/subjects/keys. [y/N] \")?;\n    }\n\n    let email = args.email.or_else(|| std::env::var(\"CYBEROS_BOOTSTRAP_EMAIL\").ok())\n                          .unwrap_or_else(|| prompt_email());\n    validate_email(&amp;email)?;\n    let password = if args.password_from_env {\n        Zeroizing::new(std::env::var(\"CYBEROS_BOOTSTRAP_PASSWORD\")\n            .map_err(|_| BootstrapError::UserError(\"CYBEROS_BOOTSTRAP_PASSWORD not set\".into()))?)\n    } else {\n        prompt_password_masked()\n    };\n    password::validate_complexity(&amp;password, &amp;email)?;\n    let hash = bcrypt::hash(&amp;password, 12)?;\n\n    let mut tx = pool.begin().await?;\n    if args.reset {\n        sqlx::query(\"DELETE FROM tenants WHERE id = '00000000-0000-0000-0000-000000000000'::uuid\").execute(&amp;mut *tx).await?;\n        // CASCADE removes subjects, sessions, etc.\n    }\n\n    let tenant_0_id = Uuid::nil();\n    sqlx::query(\"INSERT INTO tenants (id, slug, name) VALUES ($1, 'root', 'Root Tenant')\")\n        .bind(tenant_0_id).execute(&amp;mut *tx).await?;\n\n    rls::apply_for_tenant_in_tx(&amp;mut *tx, tenant_0_id).await?;\n\n    let subject_id = Uuid::new_v4();\n    sqlx::query(\"INSERT INTO subjects (id, tenant_id, email, password_hash, roles) VALUES ($1, $2, $3, $4, ARRAY['root-admin'])\")\n        .bind(subject_id).bind(tenant_0_id).bind(&amp;email).bind(&amp;hash).execute(&amp;mut *tx).await?;\n\n    let kid = jwks::rotation::generate_new_signing_key_in_tx(&amp;mut *tx).await?;\n\n    let request_id = format!(\"bootstrap_{}\", ulid::Ulid::new());\n    let bootstrapped_by = std::env::var(\"USER\").unwrap_or_else(|_| \"unknown\".into());\n    memory::emit_in_tx(&amp;mut tx, memory::canonical::bootstrap_completed(\n        tenant_0_id, subject_id, &amp;kid, &amp;env_tier, &amp;bootstrapped_by, &amp;request_id,\n    )).await?;\n\n    tx.commit().await?;\n\n    Ok(BootstrapResult {\n        tenant_0_id, root_admin_subject_id: subject_id,\n        initial_signing_key_kid: kid, request_id,\n    })\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/cli/sweepers.rs\npub async fn run(pool: &amp;PgPool) -&gt; Result&lt;SweepersResult, SweepersError&gt; {\n    let sessions: u64 = sqlx::query(\"DELETE FROM sessions WHERE expires_at &lt; NOW()\")\n        .execute(pool).await?.rows_affected();\n    let idempotency: u64 = sqlx::query(\"DELETE FROM admin_idempotency_keys WHERE created_at &lt; NOW() - INTERVAL '24 hours'\")\n        .execute(pool).await?.rows_affected();\n    let signing_keys: u64 = sqlx::query(\"DELETE FROM signing_keys WHERE status='retired' OR (status='retiring' AND retired_at &lt; NOW())\")\n        .execute(pool).await?.rows_affected();\n    Ok(SweepersResult { sessions, idempotency, signing_keys })\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/src/cli/rotate_keys.rs\npub async fn run(pool: &amp;PgPool) -&gt; Result&lt;String, anyhow::Error&gt; {\n    let kid = jwks::rotation::generate_new_signing_key(pool).await?;\n    Ok(kid)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Fresh DB: bootstrap succeeds</strong> — Tenant 0 created with nil UUID + slug <code>\"root\"</code> + name <code>\"Root Tenant\"</code>; root-admin subject created with role <code>[\"root-admin\"]</code>; initial signing key created with status <code>active</code>.</li><li><strong>memory row emitted</strong> — <code>auth.bootstrap_completed</code> row in chain with all 6 payload fields populated.</li><li><strong>Re-run after success: exit 5</strong> — Output includes <code>\"already initialised\"</code>; no second tenant 0 created.</li><li><strong><code>--reset</code> without <code>--confirm</code> exit 4</strong> — Output requires both flags.</li><li><strong><code>--reset --confirm</code> in non-prod recreates</strong> — Drops + recreates tenant 0; new signing key kid; new memory row.</li><li><strong><code>--reset --confirm</code> in production WITHOUT <code>--force-prod-reset</code> exit 4</strong> — Production-reset blocked.</li><li><strong><code>--reset --confirm --force-prod-reset</code> in production with interactive N exit 4</strong> — User typed N at prompt.</li><li><strong><code>--reset --confirm --force-prod-reset</code> in production with interactive Y succeeds</strong> — Resets + requires tty.</li><li><strong>Production reset with non-tty input exit 4</strong> — Pipe <code>echo y</code> doesn't bypass tty check.</li><li><strong>Email from env var <code>CYBEROS_BOOTSTRAP_EMAIL</code></strong> — CI path works without prompting.</li><li><strong>Password from env var <code>CYBEROS_BOOTSTRAP_PASSWORD</code> with <code>--password-from-env</code></strong> — CI path works.</li><li><strong>Interactive password input is masked</strong> — <code>rpassword</code> shows no characters.</li><li><strong>Plaintext password NOT in stdout, memory row, OR audit log</strong>.</li><li><strong>Initial signing key in JWKS</strong> — After bootstrap, <code>/.well-known/jwks.json</code> returns the new kid.</li><li><strong><code>cyberos-auth rotate-keys</code> generates new active key</strong> — Prior active becomes retiring; new key is active.</li><li><strong><code>cyberos-auth sweepers</code> deletes expired rows</strong> — Reports <code>{sessions: N1, idempotency: N2, signing_keys: N3}</code>.</li><li><strong>Sweepers idempotent</strong> — Re-running with no expired rows returns <code>{0, 0, 0}</code> and exit 0.</li><li><strong>Email validation: bad email exits 5</strong> — <code>bootstrap --email \"not-an-email\"</code> exits SchemaViolation.</li><li><strong>Weak password exits 5</strong> — <code>password.rs::validate_complexity</code> rejects.</li><li><strong>DB unreachable exits 3</strong> — Wrong connection-string returns RemoteUnreachable.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn fresh_bootstrap_creates_tenant_0_and_root_admin() {\n    let pool = test_pool_blank().await;\n    let result = bootstrap::run(BootstrapArgs {\n        email: Some(\"admin@cyberos.world\".into()),\n        password_from_env: true,\n        reset: false, confirm: false, force_prod_reset: false,\n    }, &amp;pool).await.unwrap();\n\n    assert_eq!(result.tenant_0_id, Uuid::nil());\n    assert!(memory_test_helper::has_row(\"auth.bootstrap_completed\", &amp;result.request_id).await);\n\n    let count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM signing_keys WHERE status='active'\").fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(count, 1);\n}\n\n#[tokio::test]\nasync fn rerun_returns_already_initialised() {\n    let pool = test_pool_blank().await;\n    bootstrap::run(default_args_env(), &amp;pool).await.unwrap();\n    let err = bootstrap::run(default_args_env(), &amp;pool).await.expect_err(\"expected AlreadyInitialised\");\n    assert!(matches!(err, BootstrapError::AlreadyInitialised));\n}\n\n#[tokio::test]\nasync fn reset_without_confirm_exits_3() {\n    let pool = test_pool().await;\n    let err = bootstrap::run(BootstrapArgs { reset: true, confirm: false, ..default_args_env() }, &amp;pool).await.expect_err(\"expected DestructiveWithoutConfirm\");\n    assert!(matches!(err, BootstrapError::DestructiveWithoutConfirm));\n}\n\n#[tokio::test]\nasync fn production_reset_without_force_prod_reset_blocked() {\n    std::env::set_var(\"CYBEROS_DEPLOYMENT_TIER\", \"production\");\n    let pool = test_pool().await;\n    let err = bootstrap::run(BootstrapArgs { reset: true, confirm: true, force_prod_reset: false, ..default_args_env() }, &amp;pool).await.expect_err(\"expected ProductionResetBlocked\");\n    assert!(matches!(err, BootstrapError::ProductionResetBlocked { .. }));\n}\n\n#[tokio::test]\nasync fn weak_password_exits_5() {\n    let pool = test_pool_blank().await;\n    std::env::set_var(\"CYBEROS_BOOTSTRAP_PASSWORD\", \"weak\");\n    let err = bootstrap::run(BootstrapArgs {\n        email: Some(\"a@b.com\".into()), password_from_env: true,\n        reset: false, confirm: false, force_prod_reset: false,\n    }, &amp;pool).await.expect_err(\"expected WeakPassword\");\n    // Maps to SchemaViolation exit code in main.\n}\n\n#[tokio::test]\nasync fn sweepers_deletes_expired() {\n    let pool = test_pool().await;\n    test_helper::insert_expired_sessions(10).await;\n    test_helper::insert_old_idempotency_keys(5).await;\n\n    let result = sweepers::run(&amp;pool).await.unwrap();\n    assert_eq!(result.sessions, 10);\n    assert_eq!(result.idempotency, 5);\n}\n\n#[tokio::test]\nasync fn bootstrap_audit_row_has_no_password() {\n    let pool = test_pool_blank().await;\n    let result = bootstrap::run(default_args_env(), &amp;pool).await.unwrap();\n    let row = memory_test_helper::find_row(\"auth.bootstrap_completed\", &amp;result.request_id).unwrap();\n    let s = serde_json::to_string(&amp;row.payload).unwrap();\n    assert!(!s.contains(\"CorrectHorse\"));   // the test password\n}\n\n#[tokio::test]\nasync fn rotate_keys_creates_new_active() {\n    let pool = test_pool().await;\n    bootstrap::run(default_args_env(), &amp;pool).await.unwrap();\n    let new_kid = rotate_keys::run(&amp;pool).await.unwrap();\n    let active_kid: String = sqlx::query_scalar(\"SELECT kid FROM signing_keys WHERE status='active'\").fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(active_kid, new_kid);\n    let retiring_count: i64 = sqlx::query_scalar(\"SELECT COUNT(*) FROM signing_keys WHERE status='retiring'\").fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(retiring_count, 1);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/auth/src/bin/cyberos_auth.rs\nuse clap::{Parser, Subcommand};\nuse cyberos_auth::cli::{bootstrap, sweepers, rotate_keys};\nuse cyberos_cli_exit::ExitCode;  // shared: 0=Ok, 1=UserError, 2=AuthFailed, 3=RemoteUnreachable, 4=DestructiveWithoutConfirm, 5=AlreadyInitialised, 6=SchemaViolation, 7=InternalError\n\n#[derive(Parser)]\n#[command(name = \"cyberos-auth\", version)]\nstruct Cli { #[command(subcommand)] cmd: Cmd }\n\n#[derive(Subcommand)]\nenum Cmd { Bootstrap(bootstrap::BootstrapArgs), RotateKeys, Sweepers }\n\n#[tokio::main]\nasync fn main() {\n    let cli = Cli::parse();\n    let pool = build_pool().await.unwrap_or_else(|e| {\n        eprintln!(\"remote_unreachable: {e}\");\n        std::process::exit(ExitCode::RemoteUnreachable as i32);\n    });\n    let result = match cli.cmd {\n        Cmd::Bootstrap(args) =&gt; bootstrap::run(args, &amp;pool).await.map(|r| {\n            println!(\"✅ Bootstrap complete:\\n  tenant_0_id: {}\\n  root-admin subject id: {}\\n  signing key kid: {}\",\n                     r.tenant_0_id, r.root_admin_subject_id, r.initial_signing_key_kid);\n        }).map_err(BootstrapError::into_exit_code),\n        Cmd::RotateKeys =&gt; rotate_keys::run(&amp;pool).await.map(|kid| println!(\"✅ New active key: {kid}\")).map_err(|_| ExitCode::InternalError),\n        Cmd::Sweepers   =&gt; sweepers::run(&amp;pool).await.map(|r| println!(\"✅ Swept: sessions={} idempotency={} signing_keys={}\", r.sessions, r.idempotency, r.signing_keys)).map_err(|_| ExitCode::InternalError),\n    };\n    match result { Ok(()) =&gt; std::process::exit(0), Err(code) =&gt; std::process::exit(code as i32) }\n}</code></pre>\n<p>Cron schedule (FR-AUTH-006 ships the binary; ops team configures cron):</p>\n<pre><code># /etc/cron.d/cyberos-auth\n0 *    * * * cyberos cyberos-auth sweepers\n0 3 1 1,4,7,10 * cyberos cyberos-auth rotate-keys   # quarterly, 03:00 UTC</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>FR-AUTH-001..005</strong> — All AUTH FRs use what bootstrap creates.</li><li>Crates: <code>clap@4</code>, <code>rpassword@7</code>, <code>zeroize@1</code>, <code>bcrypt@0.15</code>, <code>sqlx</code>, <code>tokio</code>, <code>anyhow</code>.</li><li><code>whoami</code> for <code>bootstrapped_by</code> field.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Bootstrap (interactive)</h3>\n<pre><code class=\"language-text\">$ cyberos-auth bootstrap\nEmail: admin@cyberos.world\nPassword (hidden): **********\nConfirm password: **********\n✅ Bootstrap complete:\n  tenant_0_id: 00000000-0000-0000-0000-000000000000\n  root-admin subject id: 7e57c0de-...\n  signing key kid: 01HZK9R8M3X5C8Q4\n  request_id: bootstrap_01HZK...</code></pre>\n<h3>Bootstrap (CI/env)</h3>\n<pre><code class=\"language-bash\">export CYBEROS_BOOTSTRAP_EMAIL=admin@cyberos.world\nexport CYBEROS_BOOTSTRAP_PASSWORD='CorrectHorseBatteryStaple9!'\ncyberos-auth bootstrap --password-from-env</code></pre>\n<h3>Already initialised</h3>\n<pre><code class=\"language-text\">$ cyberos-auth bootstrap\nTenant 0 + root-admin + signing key already exist.\nUse --reset --confirm to recreate (destructive).\nexit 5</code></pre>\n<h3>Production reset blocked</h3>\n<pre><code class=\"language-text\">$ CYBEROS_DEPLOYMENT_TIER=production cyberos-auth bootstrap --reset --confirm\nERROR: production reset requires --force-prod-reset AND interactive Y confirmation\nexit 4</code></pre>\n<h3>Sweepers</h3>\n<pre><code class=\"language-text\">$ cyberos-auth sweepers\n✅ Swept: sessions=42 idempotency=18 signing_keys=2</code></pre>\n<h3>Audit row <code>auth.bootstrap_completed</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.bootstrap_completed\",\n  \"payload\": {\n    \"tenant_0_id\": \"00000000-0000-0000-0000-000000000000\",\n    \"root_admin_subject_id\": \"7e57c0de-...\",\n    \"initial_signing_key_kid\": \"01HZK9R8M3X5C8Q4\",\n    \"bootstrap_environment\": \"production\",\n    \"bootstrapped_by\": \"deploy-bot\",\n    \"request_id\": \"bootstrap_...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Multi-region bootstrap (each region needs its own bootstrap) — slice 6+.</li><li>Bootstrap from existing-credentials backup (disaster recovery) — slice 5+.</li><li>Web UI for bootstrap (deliberately deferred — CLI is the only path).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Already bootstrapped (no --reset)</td><td>DB exists check</td><td>Exit 5</td><td>Op aware; no action needed OR use --reset</td></tr><tr><td>--reset without --confirm</td><td>flag check</td><td>Exit 4</td><td>Op adds --confirm</td></tr><tr><td>--reset in production without --force-prod-reset</td><td>env tier check</td><td>Exit 4</td><td>Op adds flag (deliberate)</td></tr><tr><td>--reset in production with non-tty</td><td>tty check</td><td>Exit 4</td><td>Op runs from interactive shell</td></tr><tr><td>--reset in production interactive N</td><td>Y/N prompt</td><td>Exit 4</td><td>Op confirmed cancellation</td></tr><tr><td>memory unavailable</td><td>memory_writer error</td><td>tx rollback; exit 3</td><td>Op fixes memory; rerun</td></tr><tr><td>DB constraint violation</td><td>sqlx error</td><td>tx rollback; exit 7</td><td>Op investigates</td></tr><tr><td>Password complexity fails</td><td>password.rs check</td><td>Exit 6 + reasons</td><td>Op picks stronger</td></tr><tr><td>Email validation fails</td><td>regex</td><td>Exit 6</td><td>Op fixes email</td></tr><tr><td><code>CYBEROS_BOOTSTRAP_PASSWORD</code> not set with --password-from-env</td><td>env check</td><td>Exit 1 + clear msg</td><td>Op sets env var</td></tr><tr><td>Interactive prompt skipped (non-tty)</td><td>tty check on interactive path</td><td>Exit 1</td><td>Op runs interactively OR uses env vars</td></tr><tr><td>Initial signing key generation fails</td><td>RSA gen error</td><td>tx rollback; exit 7</td><td>Op investigates entropy / openssl</td></tr><tr><td>Sweepers find nothing</td><td>normal</td><td>Exit 0 with zeroes</td><td>By design</td></tr><tr><td>Rotate-keys fails (DB error)</td><td>sqlx</td><td>Exit 7</td><td>Op investigates</td></tr><tr><td>Plaintext password in stdout (regression)</td><td>§5 grep test</td><td>PR blocked</td><td>By design</td></tr><tr><td>Plaintext password in memory row (regression)</td><td>§5 audit-row inspection</td><td>PR blocked</td><td>By design</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The CLI is the chicken-and-egg solver. Every subsequent admin operation goes through the API; bootstrap is the one DB-direct path.</li><li>The triple gate for production reset (--reset + --confirm + --force-prod-reset + interactive Y) is deliberate paranoia. Production reset wipes EVERYTHING; making accidental destruction structurally impossible is the right trade-off vs operator inconvenience.</li><li>Sweepers run hourly via cron. Manual invocation via this CLI is for ops investigation (e.g., \"did the sweeper run? Let me check now\"). Both paths use identical code; the cron just calls the binary.</li><li>Quarterly key rotation runs Jan 1 / Apr 1 / Jul 1 / Oct 1 at 03:00 UTC. Calendar-aligned for audit-narrative clarity. Ad-hoc rotation (suspected compromise) uses <code>cyberos-auth rotate-keys</code> directly.</li><li>Password input via <code>rpassword</code> masks characters. Env-var fallback exists for CI; both paths apply Zeroizing&lt;String&gt; discipline.</li><li>The <code>bootstrapped_by</code> field captures <code>$USER</code> as a forensic primitive. Not a strong identity (no auth here), but better than null. Combined with deployment_tier + signing key kid + tenant_0_id, gives a useful audit trail.</li><li>Email NOT echoed in success summary because it adds nothing (operator just typed it) AND risks leaking into operator-shell logs. The subject_id is sufficient correlation.</li><li>The CLI binary is shipped as part of the AUTH service Docker image. Operators run it via <code>docker exec auth-service cyberos-auth bootstrap</code>.</li><li>Future multi-region bootstrap is non-trivial — each region has its own DB; rotation is per-region; tenant 0 is global. Slice 6+ work.</li></ul>\n<hr>\n<p><em>End of FR-AUTH-006. Status: draft (10/10 target).</em></p>";
