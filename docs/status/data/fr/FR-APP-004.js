window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-APP-004"]="<h2>§1 — Description</h2>\n<ol><li>CyberOS's Tauri Windows build <strong>MUST</strong> gain an MSIX packaging path layered on top of the existing NSIS installer output, not a replacement for it. Tauri v2's built-in <code>bundle.windows</code> config produces <code>.msi</code> (via WiX) or <code>.exe</code> (via NSIS) targets natively; it does <strong>NOT</strong> produce <code>.msix</code> as a first-class bundle target as of this writing — MSIX packaging for a Tauri app requires a post-build wrapping step using the Windows SDK's <code>makeappx.exe</code> against a manually authored <code>AppxManifest.xml</code> and a staged file layout, which this FR <strong>MUST</strong> implement explicitly rather than assume Tauri handles.</li></ol>\n<ol><li><code>apps/desktop/src-tauri/AppxManifest.xml</code> <strong>MUST</strong> declare <code>&lt;Identity Name=\"...\" Publisher=\"...\" Version=\"...\" /&gt;</code> matching the app identity Stephen reserves in Partner Center (§9 — this FR authors the manifest with clearly-marked placeholder values that a human swaps in once the reservation exists; it does not fabricate a plausible-looking real identity string).</li></ol>\n<ol><li>The manifest's <code>&lt;uap:VisualElements&gt;</code> <strong>MUST</strong> reference the Windows Store tile icon set already committed at <code>apps/desktop/src-tauri/icons/</code> (<code>Square44x44Logo.png</code> for the app-list icon, <code>Square150x150Logo.png</code> for the medium tile, <code>Square310x310Logo.png</code> for the large tile, <code>StoreLogo.png</code> for the Store listing icon) — these files exist in the repo already (generated by a prior <code>tauri icon</code> run) and <strong>MUST NOT</strong> be regenerated or replaced by this FR; this FR only wires the manifest to point at them.</li></ol>\n<ol><li>CI <strong>MUST</strong> gate the Microsoft Store build behind a repo variable <code>MSSTORE_RELEASE=true</code>, mirroring the <code>MOBILE_RELEASE</code> / <code>MAS_RELEASE</code> (FR-APP-003) pattern, defaulting to off, independent of both. A second, independent repo variable, <code>MSSTORE_SIGNING_MODE</code> (<code>store-managed</code> default vs <code>self-managed</code>), <strong>MUST</strong> control only which signing step runs (§1 #5) — it <strong>MUST NOT</strong> be conflated with <code>MSSTORE_RELEASE</code>, which controls whether the job runs at all.</li></ol>\n<ol><li>Package signing <strong>MUST</strong> follow whichever of the two Microsoft Store–supported paths Stephen selects during implementation (§9 records this as an open decision, not a design default this FR imposes): <strong>(a) Store-managed signing</strong> — submit an unsigned or dev-signed MSIX to Partner Center, which re-signs it with a Microsoft Store certificate at ingestion; no EV code-signing certificate purchase required; or <strong>(b) self-managed signing</strong> — sign the MSIX locally/in-CI with Stephen's own EV code-signing certificate before submission, required only if CyberOS also wants to sideload the same signed package outside the Store (e.g. enterprise MDM/Intune distribution).</li></ol>\n<ol><li>Submission to Partner Center <strong>MUST</strong> use the Microsoft Store Submission API (REST, Azure AD app-registration OAuth2 client-credentials flow) for CI automation, rather than requiring Stephen to manually upload via the Partner Center web UI on every release — but the CI job <strong>MUST</strong> remain inert (<code>MSSTORE_RELEASE</code> unset/false) until Stephen has completed the Azure AD app registration and associated it with the Partner Center account, which this FR cannot do on his behalf.</li></ol>\n<ol><li>This FR <strong>MUST NOT</strong> attempt to reserve the app name/identity in Partner Center, purchase an EV code-signing certificate, or create the Azure AD app registration — those are Stephen's account-setup actions. §9 records exactly which prerequisites block <code>MSSTORE_RELEASE=true</code> from ever being flippable.</li></ol>\n<ol><li>A <code>docs/deploy/microsoft-store-submission.md</code> answer sheet <strong>MUST</strong> be authored covering every Partner Center submission field requiring a human decision (age rating questionnaire — Microsoft's own IARC-based system, distinct from Apple's and Google's — privacy policy URL, package family name, submission API scope/permissions), mirroring the existing <code>docs/deploy/play-store-submission.md</code> pattern.</li></ol>\n<h2>§2 — Why this design</h2>\n<p><strong>Why MSIX-wrap the NSIS output rather than the MSI output (§1 #1)?</strong> Tauri's <code>bundle.windows.nsis</code> target produces a single self-contained <code>.exe</code> installer; <code>bundle.windows.wix</code> produces a <code>.msi</code>. <code>makeappx.exe pack</code> wraps neither installer artifact directly — it wraps a <em>staged file layout directory</em> containing the raw compiled application binary (<code>cargo</code>/<code>tauri build</code> always emits this at <code>target/release/&lt;binary-name&gt;.exe</code> regardless of which installer bundle target is also requested), the manifest, and the icon assets. \"Wrap the NSIS output\" in this FR's title/description means \"reuse the same <code>tauri build</code> invocation and Cargo profile that produces CyberOS's primary NSIS-based GitHub-Releases artifact\" — not \"unpack the NSIS installer's contents\" — the MSIX layout is staged directly from the raw binary, never by extracting anything from the <code>.exe</code> installer wrapper. This choice is made because NSIS is already CyberOS's primary GitHub-Releases Windows distribution format (per the existing <code>bundle.targets: \"all\"</code> in <code>tauri.conf.json</code>, which Tauri resolves to platform-appropriate defaults, historically NSIS-first on Windows) — reusing the already-tested Cargo build profile minimizes new build-config surface, versus also standing up and testing a parallel WiX/MSI pipeline that nothing else in the repo currently exercises.</p>\n<p><strong>Why does the manifest ship with placeholder identity values instead of blocking spec-writing until Stephen has a reservation (§1 #2)?</strong> The anti-fabrication discipline (§11 of the authoring rules) forbids inventing plausible-looking real values, but the manifest file itself — including the <em>shape</em> of what a completed one looks like — is legitimate engineering scaffolding that can exist before the account-side reservation does. Blocking the entire FR on an account action would mean no engineering progress is possible until Stephen manually completes a Partner Center flow; instead, the placeholder is unambiguous (<code>CHANGEME-PENDING-PARTNER-CENTER-RESERVATION</code>) so no one mistakes it for a real value, and the manifest's structural correctness (XML schema validity, icon references, capability declarations) can be verified independently of the identity string.</p>\n<p><strong>Why leave the signing-path decision (§1 #5) open rather than defaulting to Store-managed signing?</strong> Store-managed signing is simpler for a Store-only distribution, but if CyberOS ever wants the <em>same</em> signed MSIX to be sideloadable via enterprise MDM (a plausible future need for a B2B-leaning product like CyberOS/CyberSkill selling to Vietnamese SMBs who may want IT-managed rollout), self-managed signing with an owned EV cert is required — that's a product-strategy question, not something this spec should silently pre-decide. Recording it as an explicit open question keeps the decision visible rather than baked into code that would need rework if the answer turns out to be \"we need our own cert after all.\"</p>\n<h2>§3 — API contract</h2>\n<p><code>apps/desktop/src-tauri/AppxManifest.xml</code> (structural skeleton — Identity values are explicit placeholders pending Partner Center reservation per §9; icon references point at real, already-committed files):</p>\n<pre><code class=\"language-xml\">&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;Package\n    xmlns=\"http://schemas.microsoft.com/appx/manifest/foundation/windows10\"\n    xmlns:uap=\"http://schemas.microsoft.com/appx/manifest/uap/windows10\"\n    xmlns:rescap=\"http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities\"\n    IgnorableNamespaces=\"uap rescap\"&gt;\n\n  &lt;Identity\n      Name=\"CHANGEME-PENDING-PARTNER-CENTER-RESERVATION\"\n      Publisher=\"CHANGEME-PENDING-PARTNER-CENTER-RESERVATION\"\n      Version=\"1.0.0.0\" /&gt;\n\n  &lt;Properties&gt;\n    &lt;DisplayName&gt;CyberOS&lt;/DisplayName&gt;\n    &lt;PublisherDisplayName&gt;CyberSkill Software Solutions Consultancy and Development Joint Stock Company&lt;/PublisherDisplayName&gt;\n    &lt;Logo&gt;Assets\\StoreLogo.png&lt;/Logo&gt;\n  &lt;/Properties&gt;\n\n  &lt;Dependencies&gt;\n    &lt;TargetDeviceFamily Name=\"Windows.Desktop\" MinVersion=\"10.0.17763.0\" MaxVersionTested=\"10.0.22621.0\" /&gt;\n  &lt;/Dependencies&gt;\n\n  &lt;Resources&gt;\n    &lt;Resource Language=\"en-us\" /&gt;\n  &lt;/Resources&gt;\n\n  &lt;Applications&gt;\n    &lt;Application Id=\"CyberOS\" Executable=\"CyberOS.exe\" EntryPoint=\"Windows.FullTrustApplication\"&gt;\n      &lt;uap:VisualElements\n          DisplayName=\"CyberOS\"\n          Description=\"CyberOS — Turn Your Will Into Real\"\n          BackgroundColor=\"transparent\"\n          Square150x150Logo=\"Assets\\Square150x150Logo.png\"\n          Square44x44Logo=\"Assets\\Square44x44Logo.png\"&gt;\n        &lt;!-- No Wide310x150Logo: not present in the confirmed Tauri-generated icon set (§10, §11).\n             DefaultTile omits the optional wide-tile attribute entirely rather than reference an\n             unconfirmed file; a wide tile can be added once that asset exists. --&gt;\n        &lt;uap:DefaultTile Square310x310Logo=\"Assets\\Square310x310Logo.png\" /&gt;\n      &lt;/uap:VisualElements&gt;\n    &lt;/Application&gt;\n  &lt;/Applications&gt;\n\n  &lt;Capabilities&gt;\n    &lt;rescap:Capability Name=\"runFullTrust\" /&gt;\n  &lt;/Capabilities&gt;\n&lt;/Package&gt;</code></pre>\n<p><code>apps/desktop/src-tauri/tauri.msix.conf.json</code> (overlay — narrows the bundle target to the NSIS output this FR wraps):</p>\n<pre><code class=\"language-json\">{\n  \"bundle\": {\n    \"targets\": [\"nsis\"]\n  }\n}</code></pre>\n<p><code>.github/workflows/release-msstore.yml</code> (skeleton):</p>\n<pre><code class=\"language-yaml\">name: release-msstore\non:\n  workflow_dispatch:\njobs:\n  build-and-submit-msstore:\n    if: vars.MSSTORE_RELEASE == 'true'\n    runs-on: windows-2022\n    steps:\n      - uses: actions/checkout@v4\n      - name: Build NSIS bundle\n        run: |\n          cd apps/desktop\n          npx tauri build --config src-tauri/tauri.msix.conf.json --bundles nsis\n      - name: Locate Windows SDK tools (version-independent — the exact SDK build installed\n          on the windows-2022 runner image drifts over time; hardcoding a version string\n          here would silently break on the next runner-image update)\n        id: sdk\n        shell: pwsh\n        run: |\n          $kitsRoot = \"${env:ProgramFiles(x86)}\\Windows Kits\\10\\bin\"\n          $sdkDir = Get-ChildItem $kitsRoot -Directory | Where-Object { $_.Name -match '^\\d+\\.\\d+\\.\\d+\\.\\d+$' } |\n            Sort-Object Name -Descending | Select-Object -First 1\n          if (-not $sdkDir) { throw \"No Windows SDK bin directory found under $kitsRoot\" }\n          $makeappx = Join-Path $sdkDir.FullName \"x64\\makeappx.exe\"\n          $signtool = Join-Path $sdkDir.FullName \"x64\\signtool.exe\"\n          if (-not (Test-Path $makeappx)) { throw \"makeappx.exe not found at $makeappx\" }\n          \"makeappx=$makeappx\" &gt;&gt; $env:GITHUB_OUTPUT\n          \"signtool=$signtool\" &gt;&gt; $env:GITHUB_OUTPUT\n      - name: Stage MSIX layout directory\n        shell: pwsh\n        run: |\n          New-Item -ItemType Directory -Force -Path staging\\Assets\n          Copy-Item src-tauri\\target\\release\\CyberOS.exe staging\\\n          Copy-Item src-tauri\\AppxManifest.xml staging\\\n          Copy-Item src-tauri\\icons\\Square150x150Logo.png,src-tauri\\icons\\Square44x44Logo.png,`\n            src-tauri\\icons\\Square310x310Logo.png,src-tauri\\icons\\StoreLogo.png staging\\Assets\\\n          # AC #2 pre-pack existence check: fail loudly with the missing filename, not a\n          # silent makeappx.exe schema error, if any referenced asset didn't copy.\n          foreach ($f in @(\"Square150x150Logo.png\",\"Square44x44Logo.png\",\"Square310x310Logo.png\",\"StoreLogo.png\")) {\n            if (-not (Test-Path \"staging\\Assets\\$f\")) { throw \"Missing staged asset: $f\" }\n          }\n      - name: Pack MSIX\n        shell: pwsh\n        run: |\n          &amp; \"${{ steps.sdk.outputs.makeappx }}\" pack /d staging /p CyberOS.msix /o\n      - name: Import EV signing certificate (self-managed path only)\n        if: vars.MSSTORE_SIGNING_MODE == 'self-managed'\n        shell: pwsh\n        env:\n          MSSTORE_EV_CERT_PFX_BASE64: ${{ secrets.MSSTORE_EV_CERT_PFX_BASE64 }}\n          MSSTORE_EV_CERT_PFX_PASSWORD: ${{ secrets.MSSTORE_EV_CERT_PFX_PASSWORD }}\n        run: |\n          # windows-2022 runners do not ship with any CyberOS-owned certificate pre-installed\n          # (mirrors FR-APP-003 ISS-002's macOS keychain-import lesson for the Windows side).\n          $pfxBytes = [Convert]::FromBase64String($env:MSSTORE_EV_CERT_PFX_BASE64)\n          $pfxPath = Join-Path $env:RUNNER_TEMP \"mstore-ev-cert.pfx\"\n          [IO.File]::WriteAllBytes($pfxPath, $pfxBytes)\n          $securePw = ConvertTo-SecureString $env:MSSTORE_EV_CERT_PFX_PASSWORD -AsPlainText -Force\n          Import-PfxCertificate -FilePath $pfxPath -CertStoreLocation Cert:\\CurrentUser\\My -Password $securePw\n          Remove-Item $pfxPath -Force\n      - name: Sign (self-managed path only — skipped if Store-managed signing is selected)\n        if: vars.MSSTORE_SIGNING_MODE == 'self-managed'\n        shell: pwsh\n        env:\n          MSSTORE_EV_CERT_THUMBPRINT: ${{ secrets.MSSTORE_EV_CERT_THUMBPRINT }}\n        run: |\n          &amp; \"${{ steps.sdk.outputs.signtool }}\" sign /fd SHA256 /sha1 \"$env:MSSTORE_EV_CERT_THUMBPRINT\" `\n            /tr http://timestamp.digicert.com /td SHA256 CyberOS.msix\n      - name: Submit to Partner Center via Store Submission API\n        shell: pwsh\n        env:\n          MSSTORE_TENANT_ID: ${{ secrets.MSSTORE_TENANT_ID }}\n          MSSTORE_CLIENT_ID: ${{ secrets.MSSTORE_CLIENT_ID }}\n          MSSTORE_CLIENT_SECRET: ${{ secrets.MSSTORE_CLIENT_SECRET }}\n          MSSTORE_APP_ID: ${{ secrets.MSSTORE_APP_ID }}\n        run: |\n          $token = (Invoke-RestMethod -Method Post `\n            -Uri \"https://login.microsoftonline.com/$env:MSSTORE_TENANT_ID/oauth2/v2.0/token\" `\n            -Body @{ grant_type=\"client_credentials\"; client_id=$env:MSSTORE_CLIENT_ID; `\n                     client_secret=$env:MSSTORE_CLIENT_SECRET; `\n                     scope=\"https://manage.devcenter.microsoft.com/.default\" }).access_token\n          # POST /v1.0/my/applications/{applicationId}/submissions, upload CyberOS.msix per\n          # Microsoft's documented multi-step submission flow (create draft -&gt; upload package\n          # via the returned Azure Blob SAS URL -&gt; commit submission); full flow implemented\n          # during WORKER phase, not fabricated here.\n          Write-Host \"auth token acquired; submission flow proceeds per Microsoft Store Submission API docs\"</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Manifest is schema-valid</strong> — <code>AppxManifest.xml</code> validates against the Windows App Package Manifest schema (<code>makeappx.exe</code> itself rejects malformed manifests at pack time; a CI step asserts <code>makeappx.exe pack</code> exits 0 against a locally-buildable test layout).</li><li><strong>Icon references resolve</strong> — every <code>Assets\\*.png</code> path referenced in <code>&lt;uap:VisualElements&gt;</code> corresponds to a file actually copied into the staging layout by the \"Stage MSIX layout directory\" step, verified by a pre-pack existence check in that same step (fail loudly with a listed missing filename, not a silent <code>makeappx.exe</code> schema error).</li><li><strong>Identity placeholder is unambiguous</strong> — <code>Identity Name</code>/<code>Publisher</code> values contain the literal string <code>CHANGEME-PENDING-PARTNER-CENTER-RESERVATION</code>, and a CI lint (new script <code>tools/msix-identity-lint.sh</code>) fails any build where <code>MSSTORE_RELEASE=true</code> is set while the placeholder is still present — this is the mechanism that keeps the inert-by-default guarantee honest even after <code>MSSTORE_RELEASE</code> is flipped on, since the placeholder itself is a second, independent gate.</li><li><strong>CI job is inert by default</strong> — with <code>MSSTORE_RELEASE</code> unset or <code>false</code>, <code>release-msstore.yml</code>'s <code>build-and-submit-msstore</code> job is skipped (verified the same way as FR-APP-003 AC #6 — an unconditional anchor job plus a <code>gh run view</code> conclusion check).</li><li><strong>Signing-mode selection is explicit and defaults safe</strong> — the self-managed signing step only runs when <code>vars.MSSTORE_SIGNING_MODE == 'self-managed'</code>; any other value (including unset) skips it, meaning Store-managed signing is the safe default that requires no additional CI secret to function.</li><li><strong>Answer sheet is complete</strong> — <code>docs/deploy/microsoft-store-submission.md</code> has a filled-in row for every Partner Center submission field, each marked <code>human-confirmed</code> or <code>not-applicable</code> with a reason.</li><li><strong>No credential material committed</strong> — the repo's existing secret-scan gate passes against every file this FR adds.</li><li><strong>Tile icon set is untouched</strong> — <code>git diff --stat</code> against <code>apps/desktop/src-tauri/icons/Square*.png</code> and <code>StoreLogo.png</code> is empty after this FR lands (this FR references, never regenerates or edits, those assets).</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># AC #1, #2 — manifest schema + icon-reference validity, runnable without real Partner Center creds\nmkdir -p /tmp/msix-staging/Assets\ncp apps/desktop/src-tauri/AppxManifest.xml /tmp/msix-staging/\ncp apps/desktop/src-tauri/icons/{Square150x150Logo.png,Square44x44Logo.png,Square310x310Logo.png,StoreLogo.png} \\\n  /tmp/msix-staging/Assets/\necho \"stub CyberOS.exe for pack-only validation\" &gt; /tmp/msix-staging/CyberOS.exe\nmakeappx.exe pack /d /tmp/msix-staging /p /tmp/CyberOS.test.msix /o\necho \"exit: $?\"  # MUST be 0\n\n# AC #3 — placeholder lint\ntools/msix-identity-lint.sh apps/desktop/src-tauri/AppxManifest.xml\n# exits 0 while MSSTORE_RELEASE is unset/false regardless of placeholder state;\n# exits 1 if MSSTORE_RELEASE=true AND the placeholder string is still present\n\n# AC #8 — tile assets untouched\ngit diff --stat -- apps/desktop/src-tauri/icons/Square150x150Logo.png \\\n  apps/desktop/src-tauri/icons/Square44x44Logo.png \\\n  apps/desktop/src-tauri/icons/Square310x310Logo.png \\\n  apps/desktop/src-tauri/icons/StoreLogo.png\n# MUST print nothing</code></pre>\n<pre><code class=\"language-yaml\"># AC #4 — CI job inert-by-default, mirroring FR-APP-003's pattern\n- name: Assert MSStore job skipped when MSSTORE_RELEASE unset\n  run: |\n    gh run view ${{ github.run_id }} --json jobs -q \\\n      '.jobs[] | select(.name==\"build-and-submit-msstore\") | .conclusion' | grep -q skipped</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>The API contract in §3 covers the manifest, build overlay, and CI workflow. One piece of CyberOS-specific logic gating AC #3 is not yet shown — <code>tools/msix-identity-lint.sh</code>, the placeholder-lint script:</p>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\nset -euo pipefail\n\nMANIFEST=\"${1:?usage: msix-identity-lint.sh &lt;path-to-AppxManifest.xml&gt;}\"\nPLACEHOLDER=\"CHANGEME-PENDING-PARTNER-CENTER-RESERVATION\"\n\n# Inert outside a real Store release attempt — AC #3 only requires this to be a hard\n# gate when MSSTORE_RELEASE=true; running it standalone (e.g. in AC #1's local verification)\n# must never fail a build that isn't actually attempting Store submission.\nif [[ \"${MSSTORE_RELEASE:-false}\" != \"true\" ]]; then\n  exit 0\nfi\n\nif grep -q \"$PLACEHOLDER\" \"$MANIFEST\"; then\n  echo \"ERROR: MSSTORE_RELEASE=true but AppxManifest.xml Identity Name/Publisher\" &gt;&amp;2\n  echo \"  still contain the placeholder \\\"$PLACEHOLDER\\\".\" &gt;&amp;2\n  echo \"  Update Identity values from the Partner Center app identity reservation\" &gt;&amp;2\n  echo \"  before enabling MSSTORE_RELEASE.\" &gt;&amp;2\n  exit 1\nfi\n\nexit 0</code></pre>\n<p>The one piece of net-new logic <em>not</em> spelled out here is the Store Submission API's full multi-step upload flow — create draft submission, obtain an Azure Blob SAS URL, upload the MSIX to that URL, commit the submission, poll for validation status. This is a well-documented Microsoft REST API contract; implementing it in full here would mean transcribing Microsoft's own API reference into this spec, which adds length without adding CyberOS-specific design decisions — the WORKER phase implements it directly against Microsoft's current API docs at build time, per §11.</p>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none.</li><li><strong>Downstream:</strong> none currently drafted.</li><li><strong>Cross-module:</strong> none.</li><li><strong>Human/account prerequisites (hard blockers on <code>MSSTORE_RELEASE=true</code>):</strong> Partner Center app identity reservation (Stephen — he has already created the Partner Center account per this session's context, but has not yet reserved an app name/identity), Azure AD app registration associated with the Partner Center account (Stephen), signing-mode decision (§9) and, if self-managed is chosen, an EV code-signing certificate purchase (Stephen).</li></ul>\n<h2>§8 — Example payloads</h2>\n<p><code>tools/msix-identity-lint.sh</code> failure output shape:</p>\n<pre><code>ERROR: MSSTORE_RELEASE=true but AppxManifest.xml Identity Name/Publisher\n  still contain the placeholder \"CHANGEME-PENDING-PARTNER-CENTER-RESERVATION\".\n  Update Identity values from the Partner Center app identity reservation\n  before enabling MSSTORE_RELEASE.\nexit 1</code></pre>\n<p>Partner Center Store Submission API response shape (illustrative, per Microsoft's public API documentation — not fabricated CyberOS data):</p>\n<pre><code class=\"language-json\">{\n  \"id\": \"1152921504621259678\",\n  \"status\": \"PendingCommit\",\n  \"fileUploadUrl\": \"https://productingestionbin1.blob.core.windows.net/...(SAS token)...\"\n}</code></pre>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Signing mode: Store-managed vs self-managed EV certificate</strong> — deferred to Stephen. Blocks whether <code>MSSTORE_SIGNING_MODE=self-managed</code> is ever set, and whether an EV cert purchase is needed. Default assumption for <code>MSSTORE_RELEASE=true</code> with <code>MSSTORE_SIGNING_MODE</code> unset is Store-managed signing (no CyberOS-owned cert required).</li><li><strong>Partner Center app identity reservation</strong> — deferred to Stephen; blocks the <code>AppxManifest.xml</code> <code>Identity</code> fields from ever leaving placeholder state, and therefore blocks <code>MSSTORE_RELEASE=true</code> per AC #3's lint.</li><li><strong>Azure AD app registration for Store Submission API auth</strong> — deferred to Stephen; blocks CI-automated submission entirely. Manual Partner Center web UI upload remains available to Stephen as a fallback that doesn't require this FR's CI automation at all, if he prefers not to set up the Azure AD registration.</li><li><strong>Whether CyberOS needs <code>runFullTrust</code> capability long-term</strong> — the manifest declares it (§3) because Tauri/WebView2-based desktop apps require full-trust execution (they're not UWP-sandboxed apps); this is a structural requirement of packaging any Win32/Tauri app as MSIX via the Desktop Bridge, not a per-CyberOS design choice, so it is not treated as an open question requiring Stephen's input — recorded here only for completeness/traceability.</li><li><strong><code>related_frs</code> references FR-APP-005 and FR-APP-006, which do not yet exist on disk</strong> — deliberate same-batch forward reference (this FR, FR-APP-005, and FR-APP-006 were all approved together in the same PLAN and are being authored sequentially in one session; FR-APP-003 is already landed). No inline placeholder annotation is mechanically required since <code>depends_on</code>/<code>blocks</code> — the fields that rule actually scopes to — are both empty here; documented explicitly so a reader encountering this file before the batch completes understands why the cross-references resolve only once FR-APP-005/006 land, matching the same disclosure FR-APP-003 §9 made about this FR before it existed.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>makeappx.exe</code>/<code>signtool.exe</code> absolute path changes across Windows SDK point releases baked into future <code>windows-2022</code> runner-image updates</td><td>The \"Locate Windows SDK tools\" step's <code>Test-Path</code> check throws explicitly (\"makeappx.exe not found at …\") rather than the pack step failing with an opaque error</td><td>Build fails loudly, early, with a clear diagnostic — before any staging or submission work</td><td>Resolved in §3: the SDK bin directory is discovered dynamically via <code>Get-ChildItem</code>/regex match + <code>Sort-Object -Descending</code> (newest installed SDK wins) rather than hardcoded, so this failure mode now only triggers if the runner image ships with no Windows SDK at all, not merely a different SDK version</td></tr><tr><td>Self-managed signing step references a certificate thumbprint (<code>MSSTORE_EV_CERT_THUMBPRINT</code>) that was never imported into the <code>windows-2022</code> runner's certificate store</td><td><code>signtool.exe sign</code> fails with \"No certificates were found that met all the given criteria\"</td><td>Signing step fails; MSIX remains unsigned, submission does not proceed</td><td>Resolved in §3: an explicit \"Import EV signing certificate\" step decodes a base64-encoded PFX secret and imports it via <code>Import-PfxCertificate</code> before the sign step runs, mirroring FR-APP-003 ISS-002's macOS keychain-import fix for the Windows side of this FR</td></tr><tr><td>Store-managed signing is assumed but Stephen later decides self-managed is needed after <code>AppxManifest.xml</code>/CI are already built assuming the simpler path</td><td>Product requirement surfaces after implementation (e.g. an enterprise customer needs a self-signed sideload build)</td><td>Rework of the signing step, not a design flaw — §1 #5/§9 explicitly scoped this as an open decision, not a default</td><td>Flip <code>MSSTORE_SIGNING_MODE=self-managed</code>, provision <code>MSSTORE_EV_CERT_THUMBPRINT</code> secret, no manifest changes required since signing is orthogonal to the manifest's identity declaration</td></tr><tr><td>Azure AD client-credentials token acquisition fails due to expired/rotated client secret</td><td>Submission step's <code>Invoke-RestMethod</code> call to the token endpoint returns 401/400</td><td>CI job fails at the auth step, no partial/corrupt submission created</td><td>Rotate <code>MSSTORE_CLIENT_SECRET</code>; Azure AD client secrets have finite expiry (commonly 6–24 months) and this is an expected operational task, not a design gap — documented in the answer sheet (AC #6)</td></tr><tr><td>Store Submission API's multi-step flow (create draft → upload via SAS URL → commit) is interrupted mid-flow by a CI timeout</td><td>Partner Center shows a stale \"PendingCommit\" submission with no further CI action</td><td>Manual cleanup required in Partner Center web UI to discard the stale draft before retrying</td><td>Out of scope for this FR to build full idempotent-resume logic for the submission flow (would require Store Submission API session-state tracking) — documented as a known operational risk in the answer sheet; Stephen resolves via the Partner Center UI if it occurs</td></tr><tr><td>CI runner's Windows SDK version doesn't match the <code>MaxVersionTested</code> declared in <code>AppxManifest.xml</code>'s <code>TargetDeviceFamily</code>, causing a compatibility warning at Partner Center ingestion (not a hard failure)</td><td>Partner Center's automated pre-review compatibility check flags it</td><td>Submission proceeds but with a warning; not blocking</td><td><code>MaxVersionTested</code> is a soft declaration Microsoft uses for compatibility warnings, not a hard gate — update it periodically as Windows versions advance; not an urgent fix</td></tr><tr><td>Tile icon PNGs committed to the repo are RGB without alpha transparency where the Store expects transparent PNGs for tile backgrounds, producing a visible background-color box around the logo in Store listings</td><td>Visual inspection of the Partner Center submission preview (manual QA step, not automatable in CI)</td><td>Cosmetic issue only, not a submission blocker</td><td>Documented as a manual QA checklist item in <code>docs/deploy/microsoft-store-submission.md</code>; regenerating the icon set with correct alpha channels (if needed) is out of scope for this FR since it would touch already-committed, already-correct-for-other-purposes assets shared with the desktop build</td></tr><tr><td><code>MOBILE_RELEASE</code>, <code>MAS_RELEASE</code>, and <code>MSSTORE_RELEASE</code> gates get coupled in a future <code>release.yml</code> refactor</td><td>Regression caught only if a future PR's tests specifically re-verify all three gates independently</td><td>Wrong release channel fires unexpectedly</td><td>Same mitigation pattern as FR-APP-003 ISS/failure-mode: each gate's inert-by-default assertion (AC #4 here) is a standing regression test that runs on every workflow change, not a one-time check</td></tr><tr><td>Windows Defender SmartScreen flags the unsigned test MSIX during local <code>makeappx.exe pack</code> verification (AC #1), confusing a developer running the verification script locally</td><td>Local developer sees a SmartScreen warning when double-clicking the test <code>.msix</code> output</td><td>No functional impact — the verification script only checks <code>makeappx.exe pack</code>'s exit code, never installs/runs the test package</td><td>Documented in §11 as expected behavior for an intentionally-unsigned local test artifact; not a CI failure since CI never attempts to install the package, only pack it</td></tr><tr><td>Secret scan has a false negative because an Azure AD client secret is pasted directly into a workflow YAML during manual debugging and not cleaned up before commit</td><td>Manual code review or the repo's secret-scan gate (AC #7)</td><td>Credential leak</td><td>This FR's <code>disallowed_tools</code> explicitly forbids entering credential material anywhere non-secret-manager; the <code>release-msstore.yml</code> skeleton uses <code>${{ secrets.* }}</code> exclusively</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong><code>Wide310x150Logo.png</code> is not part of the confirmed-committed Tauri-generated icon set</strong> — rather than reference an unconfirmed file in the manifest (§3) or silently drop the whole <code>&lt;uap:DefaultTile&gt;</code> element, the manifest omits only the optional wide-tile attribute and keeps the square tile, which is the schema-valid minimum. Adding a wide tile later is a pure asset-addition task (generate or hand-author <code>Wide310x150Logo.png</code>, then add the attribute back) with zero manifest-structure rework — recorded here so a future reader understands why the wide tile is absent rather than assuming it was overlooked.</li><li><strong>Microsoft's age-rating system (IARC) is genuinely distinct from Apple's and Google Play's</strong> — this is why the answer sheet (AC #6) is a <em>new</em> document rather than a reference to the existing <code>docs/deploy/play-store-submission.md</code>; despite surface similarity (\"fill out an age rating questionnaire for a store\"), the underlying rating bodies and question sets differ enough that copy-adapting the Play Store answer sheet would risk transcription errors on a compliance-relevant field.</li><li><strong>The Store Submission API's full request/response contract is intentionally not transcribed into §6</strong> — Microsoft's own REST API reference is the authoritative, versioned source; duplicating it here would create a second copy that drifts out of sync as Microsoft revises the API. The WORKER phase implements directly against Microsoft's live documentation at build time.</li><li><strong>This FR does not attempt to automate the age-rating questionnaire or privacy-policy-URL submission fields via the Submission API</strong> — those are one-time-per-listing metadata fields best set once via the Partner Center web UI (or the API's listing-metadata endpoints, out of this FR's CI-automation scope, which is focused on <em>package</em> upload/submission per release, not listing metadata management) and then left stable across releases.</li></ul>\n<p><em>End of FR-APP-004.</em></p>";
