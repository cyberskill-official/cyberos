window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-111"]="<h1>TASK-IMP-111: The plan workflow</h1>\n<h2>Summary</h2>\n<p><code>create-tasks</code> cannot take an idea. Its command promises a standalone interview for the no-document case, but the interview's required field is <code>source_files</code> - hand it an idea and it asks for a document. Add <code>plan</code>: a workflow that turns an idea (greenfield) or an idea plus a deep repo scan (brownfield) into a <code>plan@1</code> document whose proposed task set is exactly what create-tasks already consumes.</p>\n<h2>Problem</h2>\n<p>Two verified gaps.</p>\n<p><strong>The command promises what the skill cannot do.</strong> <code>create-tasks.md</code> says: \"If given only an idea (no document), use the skill's standalone interview to elicit scope.\" <code>task-author/STANDALONE_INTERVIEW.md</code>'s required field is <code>source_files</code>: \"Which source file(s) should I read to draft the task? ... The skill confirms each path resolves.\" It is file plumbing, not elicitation. The brand-new-project case is unreachable today. This is the fourth instance this run of a spec promise with nothing under it.</p>\n<p><strong>The deep scan runs too late and too narrow.</strong> <code>repo-context-map-author</code> extracts patterns, schemas, blast radius, and a module-misplacement flag - but its inputs are <code>{repo_root, task_id}</code> and it runs at ship-tasks step 1, AFTER tasks exist, scoped to one task. Planning against an existing repo needs a repo-wide scan BEFORE any task exists.</p>\n<h2>Proposed Solution</h2>\n<p><code>plan</code> emits <code>plan@1</code> at <code>docs/plans/PLAN-&lt;slug&gt;-&lt;date&gt;/plan.md</code>: intent, context, options with checkable evidence, one decision with confidence, scope, a proposed task set, risks, and the BRAIN rows emitted. Section 6 (the proposed task set) is the input contract - a document with a proposed task set is exactly the \"PRD or spec\" task-author already consumes, so create-tasks needs no new input shape.</p>\n<p>Roughly 70 % already exists. <code>repo-context-map-author</code> gains <code>scope: repo | task</code> (task = today's behavior, byte-for-byte). The PLAN gate's state machine, the spike's option grammar, <code>memory-append</code>, and the author/audit pair shape are all reused rather than reinvented.</p>\n<p>Mode detection: no <code>.cyberos/</code> and no git HEAD is greenfield; commits or a corpus is brownfield with a MANDATORY scan; ambiguous ASKS, because guessing greenfield on a live repo plans against a codebase that exists.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Fix <code>STANDALONE_INTERVIEW.md</code> to elicit ideas, no new workflow. Rejected: it would put option-weighing, repo scanning, and decision-recording inside task-author, which authors tasks. The interview is the symptom; the missing front door is the defect.</li><li>Build IMP-21 (four-label intake triage) separately. Rejected explicitly: <code>plan</code> subsumes it. Triage's <code>park</code> and <code>needs_info</code> become plan outcomes and <code>needs_spike</code> is already a step. Two front doors is one too many.</li><li>Let plan write tasks directly. Rejected: create-tasks owns the audited write path, and a second writer to <code>docs/tasks/**</code> re-opens the 086 class.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: an idea with no document produces a <code>plan@1</code> that create-tasks consumes unmodified - the case that is unreachable today. Baseline: the interview asks for a file.</li><li>Guardrail: brownfield never plans without a scan, and <code>repo-context-map-author --scope task</code> behaves byte-identically to today (the ship-tasks path must not move).</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>plan-author</code>, <code>plan-audit</code>, <code>plan_rubric@1.0</code>, the <code>scope</code> input on repo-context-map, the <code>/cyberos:plan</code> command, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Writing <code>docs/tasks/**</code> or any BACKLOG row - create-tasks does that.</li><li>Writing code. The blast radius is one directory of markdown.</li><li>Setting any task status - plan produces no tasks.</li><li>Changing <code>repo-context-map-author</code>'s existing task-scoped behavior.</li><li>A second HITL gate: create-tasks already has its own PLAN gate, and two approvals of the same content in five minutes is how a gate becomes a rubber stamp.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Reuses skills that exist.</p>\n<p><strong>Serialisation note:</strong> touches <code>build.sh</code> (shared with TASK-IMP-110 - both add a vendored skill to VENDORED_SKILLS, and chain-coverage fails closed if either lands half-applied). Parent-serialised per §11a.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §12, written after checking create-tasks for reusable mechanism as the operator instructed; the two gaps were verified against the live skills on merged main.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>plan</code> MUST detect mode: greenfield (no <code>.cyberos/</code> AND no git HEAD), brownfield (commits and/or <code>docs/tasks/</code>), or ambiguous - and ambiguous MUST halt and ask rather than guess.</li><li>1.2 In brownfield mode the repo-wide scan MUST run before the interview, and <code>plan</code> MUST NOT emit a decision without it.</li><li>1.3 <code>repo-context-map-author</code> MUST accept <code>scope: repo | task</code>; <code>task</code> MUST behave exactly as today (the ship-tasks path is unchanged).</li><li>1.4 <code>plan-author</code> MUST emit <code>plan@1</code> carrying: intent, context, &gt;=2 options each with checkable evidence, exactly one decision with a confidence grade, scope with a non-empty out list, a proposed task set, risks, and the BRAIN rows emitted.</li><li>1.5 <code>plan</code> MUST HALT at one operator gate on the decision, before emitting.</li><li>1.6 <code>plan-audit</code> MUST refuse below 10/10 against <code>plan_rubric@1.0</code>.</li><li>1.7 <code>plan</code> MUST NOT write <code>docs/tasks/**</code>, MUST NOT write BACKLOG rows, MUST NOT write code, and MUST NOT set any task status.</li><li>1.8 The <code>plan@1</code> proposed task set MUST be consumable by create-tasks with no change to task-author's input contract.</li><li>1.9 <code>plan</code> MUST emit its decision and context to BRAIN via <code>memory-append</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - greenfield, brownfield, and ambiguous fixtures each route correctly; ambiguous halts - test: <code>tools/install/tests/test_plan_workflow.sh::t01_mode_detect</code></li><li>[ ] AC 2 (traces_to: #1.2, #1.3) - brownfield runs the repo-scoped scan before the interview; <code>--scope task</code> output is byte-identical to today's - test: <code>tools/install/tests/test_plan_workflow.sh::t02_scan_first_task_scope_unchanged</code></li><li>[ ] AC 3 (traces_to: #1.4, #1.6) - a plan missing an option, a decision, or the out list reds at audit - test: <code>tools/install/tests/test_plan_workflow.sh::t03_rubric_refuses_incomplete</code></li><li>[ ] AC 4 (traces_to: #1.7) - a plan run leaves <code>docs/tasks/**</code> and BACKLOG.md byte-identical - test: <code>tools/install/tests/test_plan_workflow.sh::t04_never_writes_tasks</code></li><li>[ ] AC 5 (traces_to: #1.8) - a <code>plan@1</code> from an idea-only greenfield run is consumed by create-tasks unmodified - test: <code>tools/install/tests/test_plan_workflow.sh::t05_output_feeds_create_tasks</code></li><li>[ ] AC 6 (traces_to: #1.9) - the run appends the decision to BRAIN and the chain verifies - test: <code>tools/install/tests/test_plan_workflow.sh::t06_brain_rows_chain</code></li><li>[ ] AC 7 (traces_to: #1.5) - the decision gate halts and no artefact is emitted without a verdict - verify: recorded gate-log transcript (a HITL halt cannot be asserted by a suite without simulating the human; same rationale as the existing gate arms).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Brownfield repo with 100k+ lines: the scan must bound itself (module inventory and conventions, not every file) or it exceeds the sandbox cap. The scan reports what it sampled rather than implying exhaustiveness.</li><li>Greenfield where the operator has an idea but no opinion on stack: options carry the evidence and the decision names a confidence of <code>low</code> - honest, and a low-confidence decision is what a spike is for.</li><li>A repo with <code>.cyberos/</code> but no commits (installed, never committed): brownfield - the machine's presence means someone intends to work here.</li><li>An idea that is already a task in the corpus: the brownfield scan surfaces it and the plan's option set MUST include \"this exists\" rather than proposing a duplicate.</li><li>Operator points <code>plan</code> at a document (not an idea): legal - it plans from the document. <code>plan</code> is not restricted to ideas; it is the front door.</li><li>Security-class: the scan reads repo files and the interview reads operator text. Neither is executed. The scan MUST confine under the repo root; a plan document is a proposal and is never a command source.</li></ul>";
