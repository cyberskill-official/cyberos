window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-107"]="<h1>TASK-IMP-107: End-to-end mechanical smoke test</h1>\n<h2>Summary</h2>\n<p>Twenty-five suites cover install hygiene, channels, payload shape, the helper CLIs, the renderer, and workflow doctrine. None runs the three workflows end to end, because the middle two need a model. Add a suite that exercises the MECHANICAL spine - install, lint, insert, every lifecycle flip, coverage-scope, reconcile, uninstall - on a scratch repo with no model in the loop.</p>\n<h2>Problem</h2>\n<p>The proof that the loop works is a session transcript. That is evidence, but it is not a gate: nothing re-runs it, and nothing fails when a helper's contract drifts from the workflow that calls it. Each helper is tested in isolation and the workflow doctrine is tested as prose; the seam between them is tested by hand, once, by whoever happened to run the loop that day.</p>\n<p>The batch-4 seed-shape break is the shape of the gap: it was caught by the awh gate, one layer late and only because a goldenset existed. A mechanical spine test would have caught it earlier and without one.</p>\n<h2>Proposed Solution</h2>\n<p><code>test_e2e_skeleton.sh</code>: create a scratch repo, install the built payload, write a fixture <code>task@1</code> spec by hand, lint it clean, insert its row via <code>backlog-mutate</code>, flip it through every lifecycle status with the real helper, run <code>coverage-scope</code> against a stub report, run <code>task-reconcile</code> and assert the expected recommendation, uninstall, and assert the corpus survives. No model, no LLM, no network; target under 30 s to stay inside the sandbox cap. It tests the plumbing, not the judgment - which is 100 % more than zero.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>A model-in-the-loop e2e test. Rejected: non-deterministic, expensive, and it would fail for reasons unrelated to the plumbing - the definition of a flaky gate.</li><li>Extending each helper's own suite. Rejected: they already pass individually; the untested thing is specifically the seam between them.</li><li>Recording a transcript as a golden. Rejected: a transcript of a model's output is not a contract, and pinning it would make every prompt improvement a test failure.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: the spine suite passes on a scratch repo in under 30 s with no network and no model - suite-asserted every run. Baseline: no end-to-end coverage exists.</li><li>Guardrail: it asserts the corpus survives uninstall, closing the loop on the one thing an operator cannot recover from.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the new suite and its wiring into <code>run_all.sh</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Testing model judgment, prompt quality, or audit verdict correctness.</li><li>Replacing any existing suite.</li><li>Testing the plugin host or any agent harness.</li></ul>\n<h2>Dependencies</h2>\n<p>None - every helper it drives exists on main.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md IMP-28; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The suite MUST run on a scratch repo it creates and removes, and MUST NOT touch the working repo's corpus.</li><li>1.2 It MUST exercise, in order: install the built payload, write a fixture <code>task@1</code> spec, <code>task-lint</code> clean, <code>backlog-mutate</code> insert, a flip through every lifecycle status in <code>STATUS-REFERENCE.md</code> §1.1, <code>coverage-scope</code> against a stub report, <code>task-reconcile</code>, uninstall.</li><li>1.3 It MUST assert <code>task-reconcile</code>'s recommendation matches the state it constructed, not merely that it exits 0.</li><li>1.4 It MUST assert the corpus survives uninstall.</li><li>1.5 It MUST require no model, no network, and no credentials, and MUST complete within 30 s on the reference sandbox.</li><li>1.6 It MUST be wired into <code>scripts/tests/run_all.sh</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.2, #1.5) - the full spine runs green on a scratch repo, offline, under 30 s - test: <code>tools/install/tests/test_e2e_skeleton.sh::t01_spine_green</code></li><li>[ ] AC 2 (traces_to: #1.3) - a deliberately drifted state yields the expected reconcile recommendation, not just exit 0 - test: <code>tools/install/tests/test_e2e_skeleton.sh::t02_reconcile_recommendation_asserted</code></li><li>[ ] AC 3 (traces_to: #1.4) - the fixture corpus is present and byte-identical after uninstall - test: <code>tools/install/tests/test_e2e_skeleton.sh::t03_corpus_survives_uninstall</code></li><li>[ ] AC 4 (traces_to: #1.1) - the working repo's <code>docs/tasks/</code> is untouched by a suite run - test: <code>tools/install/tests/test_e2e_skeleton.sh::t04_scratch_isolation</code></li><li>[ ] AC 5 (traces_to: #1.6) - <code>run_all.sh</code> invokes the suite - test: <code>scripts/tests/run_all.sh</code> (the suite appears in its output; asserted by the runner's own count)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Scratch repo on a filesystem without <code>git</code>: the suite <code>git init</code>s its own scratch; if git is absent it MUST skip with a named reason rather than fail (the same skip discipline the existing suites use).</li><li>The 45 s sandbox cap: 1.5's 30 s target leaves headroom; if the spine grows past it, split by phase rather than raise the cap - a suite that cannot finish is a suite that gets disabled.</li><li>A helper that legitimately changes its output shape: this suite WILL fail, and that is the point - it is the seam test, and the fix is to update the assertion deliberately, not to loosen it.</li><li>Leftover scratch dirs from an interrupted run: the suite MUST clean up on trap, and MUST tolerate a stale scratch from a previous kill.</li><li>Security-class: writes only inside its own scratch directory; runs only repo-tracked helpers with fixed arguments; no untrusted input reaches a command.</li></ul>";
