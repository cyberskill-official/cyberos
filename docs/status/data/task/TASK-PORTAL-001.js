window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PORTAL-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship scoped read-only views at <code>services/portal/src/views/</code> over the PROJ/INV/DOC/CHAT modules, filtered by Engagement membership AND sync_class='client-visible' per TASK-MEMORY-106, with per-row redaction, field projection, cursor pagination, full-text search, CSV/XLSX export, and 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define the closed <code>portal_view_kind</code> enum at migration <code>0014</code>: <code>('projects','invoices','documents','channels','calendar')</code> per DEC-1201. CI cardinality test asserts 5. The <code>calendar</code> view is a slice-2 stub returning 501.</li></ol>\n<ol><li><strong>MUST</strong> create SQL views per <code>portal_view_kind</code> via migration <code>0014</code>. Each view is a <code>CREATE VIEW portal_view_&lt;kind&gt; AS SELECT ... FROM &lt;source_table&gt; WHERE sync_class IN ('client-visible','client-visible-redacted') AND tenant_id = current_setting('auth.tenant_id')::uuid AND engagement_id IN (SELECT engagement_id FROM engagement_memberships WHERE subject_id = current_setting('auth.subject_id')::uuid)</code>. RLS predicate inherits from each source table's RLS plus the engagement-membership join.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_view_read_log</code> at migration <code>0015</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, caller_subject_id UUID NOT NULL, view_kind portal_view_kind NOT NULL, resource_id UUID, action TEXT NOT NULL CHECK (action IN ('list','detail','search','export_initiated','export_completed')), filter_hash16 TEXT, result_count INT, trace_id CHAR(32), occurred_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. Append-only per task-audit skill rule 12. RLS-scoped.</li></ol>\n<ol><li><strong>MUST</strong> expose list endpoint <code>GET /v1/portal/views/{view_kind}?engagement_id=...&amp;filters=...&amp;fields=...&amp;cursor=...&amp;limit=...</code> per DEC-1205. Handler:</li></ol>\n<ul><li>Validates JWT + extracts caller_subject_id + tenant_id.</li><li>Validates engagement_id is in caller's <code>engagement_memberships</code>; else 403 + <code>engagement_access_denied</code>.</li><li>Validates <code>view_kind</code> is in closed enum.</li><li>Applies per-view-kind filters per DEC-1211.</li><li>Applies field projection per §1 #10.</li><li>Returns rows from the SQL view (RLS auto-applies sync_class + engagement filter).</li><li>Paginate via cursor (default limit 50; max 200).</li><li>Emit <code>portal.view_read</code> (sev-3 sampled 1%).</li><li>ETag + Cache-Control per §1 #13.</li></ul>\n<ol><li><strong>MUST</strong> expose detail endpoint <code>GET /v1/portal/views/{view_kind}/{id}</code> per DEC-1207. Handler:</li></ol>\n<ul><li>Same auth + engagement check.</li><li>Returns single row PLUS related sub-resources:</li><li><code>projects/{id}</code> → includes tasks, comments, status_history (all filtered by sync_class).</li><li><code>invoices/{id}</code> → includes line_items, payment_history.</li><li><code>documents/{id}</code> → includes versions, comments.</li><li><code>channels/{id}</code> → includes recent messages (last 50, also sync_class filtered).</li><li>Emit <code>portal.view_detail_read</code> (sev-2 — always, not sampled).</li></ul>\n<ol><li><strong>MUST</strong> expose search endpoint <code>POST /v1/portal/views/{view_kind}/search</code> per DEC-1206 with body <code>{ query: &lt;string&gt;, filters?: {...}, fields?: [...], cursor?: ... }</code>. Handler:</li></ol>\n<ul><li>Uses PostgreSQL <code>tsvector</code> index on per-view searchable columns (e.g. project name + description).</li><li>Combines with view's existing RLS filters.</li><li>Returns ranked results.</li><li>Emit <code>portal.view_search_executed</code> (sev-3 — sampled).</li></ul>\n<ol><li><strong>MUST</strong> expose export endpoint <code>GET /v1/portal/views/{view_kind}/export?format=csv|xlsx&amp;filters=...&amp;fields=...</code> per DEC-1213. Handler:</li></ol>\n<ul><li>Same auth + filter.</li><li>Streams response (chunked transfer encoding); rows fetched + serialised in 100-row batches.</li><li>Caps at 10,000 rows; over-cap returns 413 + <code>export_too_large; use_dsar</code>.</li><li>Content-Type <code>text/csv</code> or <code>application/vnd.openxmlformats-officedocument.spreadsheetml.sheet</code>.</li><li>Emit <code>portal.view_export_initiated</code> at start + <code>portal.view_export_completed</code> at end (both sev-2 — material data movement).</li></ul>\n<ol><li><strong>MUST</strong> apply per-row redaction per DEC-1203 + DEC-1215. Rows with <code>sync_class='client-visible-redacted'</code> returned with internal-only fields blanked:</li></ol>\n<ul><li><code>projects</code>: blank <code>internal_notes</code>, <code>assignee_internal_id</code>, <code>internal_priority</code>.</li><li><code>invoices</code>: blank <code>internal_notes</code>, <code>cost_breakdown</code>.</li><li><code>documents</code>: blank <code>internal_summary</code>, <code>internal_classification</code>.</li><li><code>channels</code>: blank <code>internal_pin_count</code>, <code>internal_archive_reason</code>. Redaction tracked: each redacted row emits sampled <code>portal.view_redaction_applied</code> informational row (sev-3, sampled 5%) so operators can monitor what's being redacted.</li></ul>\n<ol><li><strong>MUST</strong> support per-view kind-specific filters per DEC-1211. Filter param format <code>?filters=&lt;base64-JSON&gt;</code> decodes to:</li></ol>\n<ul><li><code>projects</code>: <code>{ status: [\"active\",\"pending\"], assignee_external_id: \"...\", date_range: {from: \"...\", to: \"...\"} }</code>.</li><li><code>invoices</code>: <code>{ status: [\"sent\",\"paid\",\"overdue\"], date_range: {...}, amount_range: {min: ..., max: ...} }</code>.</li><li><code>documents</code>: <code>{ tag: [\"contract\",\"report\"], type: [\"pdf\",\"docx\"], date_range: {...} }</code>.</li><li><code>channels</code>: <code>{ tag: [\"project-X\"], last_message_date_range: {...} }</code>. Invalid filter keys for the view kind → 400 + <code>invalid_filter_key</code> + valid_keys array.</li></ul>\n<ol><li><strong>MUST</strong> apply GraphQL-style field projection per DEC-1204. The <code>fields</code> query param decodes to a comma-separated list (or JSON array via base64). Handler:</li></ol>\n<ul><li>If absent: returns the default safe set per view kind.</li><li>If present: validates each field is in the allowed set; rejects unknown fields with 400 + <code>unknown_field</code>.</li><li>SELECT only those fields from the view (avoids SELECT * cost on wide tables).</li><li>Default safe sets:</li><li><code>projects</code>: <code>id, title, status, owner_external_id, created_at, last_activity_at</code>.</li><li><code>invoices</code>: <code>id, invoice_number, status, amount, currency, issued_at, due_at</code>.</li><li><code>documents</code>: <code>id, title, type, size_bytes, tag, created_at, updated_at</code>.</li><li><code>channels</code>: <code>id, name, tag, last_message_at, member_count</code>.</li></ul>\n<ol><li><strong>MUST</strong> enforce per-Engagement context per DEC-1209. <code>engagement_id</code> query param REQUIRED on every endpoint. Without it → 400 + <code>engagement_id_required</code>. Caller member of N Engagements switches via UI engagement-picker; views never aggregate.</li></ol>\n<ol><li><strong>MUST</strong> apply cursor pagination per DEC-1219. The cursor is base64(JSON <code>{last_id: uuid, last_sort_value: &lt;value&gt;}</code>). Pagination handler:</li></ol>\n<ul><li>Validates cursor signature (HMAC-signed to prevent forgery).</li><li>Applies <code>WHERE (sort_value, id) &gt; (cursor.last_sort_value, cursor.last_id)</code> (keyset pagination per task-audit skill rule 16 derivative).</li><li>Returns <code>{ rows: [...], next_cursor: &lt;base64&gt; | null }</code>.</li></ul>\n<ol><li><strong>MUST</strong> include ETag + Cache-Control headers per DEC-1217 + DEC-1218. ETag = SHA-256 truncated 16 hex of canonical-JSON response body; <code>If-None-Match</code> match → 304. Cache-Control <code>private, max-age=30</code> allows browser-side cache; per-user RLS prevents shared-cache leak.</li></ol>\n<ol><li><strong>MUST</strong> rate-limit at 600 reads/min/caller per DEC-1210 + task-audit skill §8.2d derivative. Exceeded → 429 + Retry-After.</li></ol>\n<ol><li><strong>MUST</strong> emit 6 memory audit row kinds per DEC-1214:</li></ol>\n<ul><li><code>portal.view_read</code> (sev-3 — high-volume; sampled 1%)</li><li><code>portal.view_detail_read</code> (sev-2 — material; always emitted)</li><li><code>portal.view_search_executed</code> (sev-3 — sampled 5%)</li><li><code>portal.view_export_initiated</code> (sev-2 — material data movement)</li><li><code>portal.view_export_completed</code> (sev-2 — paired with initiated)</li><li><code>portal.view_redaction_applied</code> (sev-3 — sampled 5%)</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub per task-audit skill rule 18. Audit rows carry <code>filter_hash16</code> + <code>resource_id</code> (UUID; non-PII per TASK-PORTAL-004 §1 #18 rationale); raw filter values + result content NOT in chain.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end per task-audit skill rule 22-24.</li></ol>\n<ol><li><strong>MUST</strong> stream exports in chunked-transfer encoding per §1 #7. Memory footprint bounded — no full-result-set buffering.</li></ol>\n<ol><li><strong>MUST</strong> enforce read-only — view endpoints never accept POST/PUT/PATCH/DELETE (returns 405). Writes route through TASK-PORTAL-006 workflows.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>sync_class</code> filter is enforced at the SQL view level per DEC-1202 + TASK-MEMORY-106. Handler does NOT trust client-supplied sync_class filter; the view's predicate is the gate.</li></ol>\n<ol><li><strong>MUST NOT</strong> auto-aggregate across Engagements per DEC-1209. Each request scoped to one engagement_id.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow shared HTTP cache (proxies, CDNs) per DEC-1217. <code>Cache-Control: private</code> mandatory; CDN edges MUST NOT cache.</li></ol>\n<ol><li><strong>MUST NOT</strong> return rows with <code>sync_class IN ('private','team-internal')</code> per TASK-MEMORY-106. View definition enforces; handler-side check defense-in-depth.</li></ol>\n<ol><li><strong>SHOULD</strong> observe per-Engagement read volume via OTel histogram <code>portal_view_read_total{engagement_id, view_kind}</code>.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why read-only at slice 1 (§1 #19, DEC-1200)?</strong> Read-only is simpler to make correct: no transaction complexity, no validation, no cascade revocation. Writes via PORTAL-006 workflows give us audit-trail control (client-initiated request → CHAT thread → CyberOS-side action). Read-only views + write workflows = clean separation of read scale from write trust.</p>\n<p><strong>Why sync_class='client-visible' enforced at SQL view (§1 #2, DEC-1202)?</strong> Defense-in-depth — handler-only enforcement leaks if handler bug. SQL view enforcement is the floor; handler is the ceiling. Both must agree; either alone catches bugs in the other.</p>\n<p><strong>Why per-row redaction (vs row omission) (§1 #8, DEC-1215)?</strong> Omitting a row \"blinds\" the client — they don't know the row exists. Showing a redacted row is honest: \"we have data on X but you can't see all of it\". Better UX + same data security.</p>\n<p><strong>Why per-Engagement context required (§1 #11, DEC-1209)?</strong> Multi-Engagement caller seeing one mega-list is confusing UX (which client is which?) + auditor's \"who saw what\" becomes unclear (one query → 50 Engagement rows = which one was the user actually looking at?). Forcing per-Engagement scope makes the audit answerable.</p>\n<p><strong>Why CSV + XLSX export but capped 10k (§1 #7, DEC-1213)?</strong> Clients legitimately want offline copies of their data (compliance backup, accounting reconciliation). CSV/XLSX are universal formats. 10k cap balances UX (covers 95% of legitimate needs) vs DOS prevention. DSAR (TASK-PORTAL-008) handles full-archive exports.</p>\n<p><strong>Why GraphQL-style field projection (§1 #10, DEC-1204)?</strong> Mobile clients on metered connections want minimal payload. Full row = 5-20 KB; projected row = 200-500 bytes. 90%+ bandwidth saving. Standard pattern (GraphQL, Sparse Fieldsets in JSON:API).</p>\n<p><strong>Why cursor pagination over offset (§1 #12)?</strong> Offset breaks at scale (slow scan past <code>LIMIT offset N</code>); cursor uses keyset (index lookup). Standard pattern for large datasets.</p>\n<p><strong>Why per-Engagement filter at SQL view + RLS (§1 #2)?</strong> Combined enforcement: tenant_id from current_setting (RLS) + engagement_id from join. Single-condition would leak if either piece is misconfigured.</p>\n<p><strong>Why default sync_class='private' (DEC-1216)?</strong> Default-deny is the security default. A new row appearing in a source module is invisible to clients until explicitly promoted. Default-allow would leak every internal note.</p>\n<p><strong>Why sampled view-read audit (§1 #15, DEC-1208)?</strong> Browsing = high-volume reads (every page render = 10s of view calls). Sampling at 1% gives statistical observability without exploding the chain. Detail reads (always emitted) + search (sampled 5%) provide the audit signal for specific resource access.</p>\n<p><strong>Why detail reads always emitted (§1 #15)?</strong> Detail = \"user saw row X\". Forensic: \"did the client view document 12345 before suing?\" must be answerable. Always-emit guarantees.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0014_portal_view_definitions.sql\nCREATE TYPE portal_view_kind AS ENUM ('projects','invoices','documents','channels','calendar');\n\n-- Example: projects view\nCREATE VIEW portal_view_projects AS\nSELECT p.id, p.tenant_id, p.engagement_id, p.title, p.status,\n       p.owner_external_id, p.created_at, p.last_activity_at,\n       CASE WHEN p.sync_class = 'client-visible' THEN p.internal_notes ELSE NULL END AS internal_notes,\n       CASE WHEN p.sync_class = 'client-visible' THEN p.assignee_internal_id ELSE NULL END AS assignee_internal_id,\n       p.sync_class\nFROM projects p\nWHERE p.sync_class IN ('client-visible','client-visible-redacted')\n  AND p.tenant_id = current_setting('auth.tenant_id')::uuid\n  AND p.engagement_id IN (\n    SELECT engagement_id FROM engagement_memberships\n    WHERE subject_id = current_setting('auth.subject_id')::uuid\n  );\n\n-- Analogous CREATE VIEW for invoices, documents, channels.\n-- All views are RLS-aware via the WHERE clauses (no separate RLS policy needed; the view IS the policy).\n\n-- 0015_portal_view_read_log.sql\nCREATE TABLE portal_view_read_log (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  caller_subject_id UUID NOT NULL,\n  view_kind portal_view_kind NOT NULL,\n  resource_id UUID,\n  action TEXT NOT NULL CHECK (action IN ('list','detail','search','export_initiated','export_completed')),\n  filter_hash16 TEXT,\n  result_count INT,\n  trace_id CHAR(32),\n  occurred_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nCREATE INDEX idx_view_read_caller ON portal_view_read_log(caller_subject_id, occurred_at DESC);\nALTER TABLE portal_view_read_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_view_read_log_rls ON portal_view_read_log\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_view_read_log FROM cyberos_app;</code></pre>\n<h3>3.2 REST endpoints</h3>\n<pre><code class=\"language-text\">GET    /v1/portal/views/{view_kind}                              (list; cursor pagination)\nGET    /v1/portal/views/{view_kind}/{id}                          (detail + sub-resources)\nPOST   /v1/portal/views/{view_kind}/search                        (search; cursor pagination)\nGET    /v1/portal/views/{view_kind}/export?format=csv|xlsx        (streaming; 10k cap)</code></pre>\n<p>All require <code>engagement_id</code> query param.</p>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>portal_view_kind cardinality</strong> — enum = exactly <code>{projects, invoices, documents, channels, calendar}</code>.</li><li><strong>List filtered by sync_class</strong> — projects with sync_class='private' NOT in response; only 'client-visible' or 'client-visible-redacted'.</li><li><strong>Engagement isolation</strong> — caller member of Eng X + Y; list query for X returns ONLY X's rows; Y rows absent.</li><li><strong>Field projection</strong> — <code>?fields=id,title</code> returns only those fields; <code>?fields=internal_notes</code> rejected with 400 if not in safe set.</li><li><strong>Per-row redaction</strong> — row with <code>sync_class='client-visible-redacted'</code> returns with <code>internal_notes=NULL</code>.</li><li><strong>Cursor pagination</strong> — first page returns next_cursor; cursor returns next page; round-trip stable.</li><li><strong>Search</strong> — POST search returns ranked results scoped by RLS.</li><li><strong>CSV export</strong> — <code>?format=csv</code> returns text/csv stream with headers row.</li><li><strong>XLSX export</strong> — <code>?format=xlsx</code> returns valid xlsx binary.</li><li><strong>Export 10k cap</strong> — 10001 rows → 413 + <code>export_too_large</code>.</li><li><strong>Detail with sub-resources</strong> — <code>projects/{id}</code> returns row + tasks + comments + status_history.</li><li><strong>engagement_id required</strong> — list without <code>engagement_id</code> → 400 + <code>engagement_id_required</code>.</li><li><strong>Cross-Engagement engagement_id</strong> — list with engagement_id NOT in membership → 403.</li><li><strong>ETag 304</strong> — second list with <code>If-None-Match</code> → 304.</li><li><strong>Cache-Control private</strong> — header on every 200 response.</li><li><strong>Rate limit 600/min</strong> — 601st read → 429.</li><li><strong>Read-only</strong> — POST/PUT/DELETE on view endpoint → 405.</li><li><strong>Calendar slice-2 stub</strong> — <code>/v1/portal/views/calendar</code> → 501.</li><li><strong>6 memory audit kinds emitted</strong> — full browse + detail + search + export + redaction lifecycle covers all 6.</li><li><strong>Filter hash in audit</strong> — filter_hash16 = SHA256-16 of canonical filter JSON; raw filter NOT in chain.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>view_projects_list_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn list_returns_only_client_visible() {\n    let ctx = TestContext::with_engagement_subject().await;\n    ctx.seed_project(SyncClass::Private, \"private-project\").await;\n    ctx.seed_project(SyncClass::ClientVisible, \"shared-project\").await;\n\n    let r = ctx.get_view(\"projects\", ctx.eng_id).await;\n    let body: serde_json::Value = r.json().await.unwrap();\n    let titles: Vec&lt;&amp;str&gt; = body[\"rows\"].as_array().unwrap().iter().filter_map(|r| r[\"title\"].as_str()).collect();\n    assert!(titles.contains(&amp;\"shared-project\"));\n    assert!(!titles.contains(&amp;\"private-project\"));\n}</code></pre>\n<h3>5.2 <code>view_engagement_isolation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn engagement_y_rows_not_returned_when_filtering_x() {\n    let ctx = TestContext::with_subject_in_two_engagements().await;\n    ctx.seed_project_in_eng(ctx.eng_x, SyncClass::ClientVisible, \"x-proj\").await;\n    ctx.seed_project_in_eng(ctx.eng_y, SyncClass::ClientVisible, \"y-proj\").await;\n\n    let r = ctx.get_view(\"projects\", ctx.eng_x).await;\n    let titles: Vec&lt;String&gt; = ctx.extract_titles(r).await;\n    assert!(titles.contains(&amp;\"x-proj\".into()));\n    assert!(!titles.contains(&amp;\"y-proj\".into()));\n}</code></pre>\n<h3>5.3 <code>view_per_row_redaction_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn redacted_rows_have_internal_fields_nulled() {\n    let ctx = TestContext::with_engagement_subject().await;\n    ctx.seed_project_with_redacted_class(\"redacted-proj\", \"secret note\").await;\n    let r = ctx.get_view(\"projects\", ctx.eng_id).await;\n    let row = ctx.find_row(r, \"title\", \"redacted-proj\").await;\n    assert!(row[\"internal_notes\"].is_null());\n    assert_eq!(row[\"title\"], \"redacted-proj\");\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.view_redaction_applied\"));\n}</code></pre>\n<h3>5.4 <code>view_field_projection_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn fields_param_limits_response_shape() {\n    let ctx = TestContext::with_engagement_subject().await;\n    ctx.seed_project(SyncClass::ClientVisible, \"p1\").await;\n    let r = ctx.get_view_with_fields(\"projects\", ctx.eng_id, \"id,title\").await;\n    let row = ctx.first_row(r).await;\n    assert!(row.get(\"id\").is_some());\n    assert!(row.get(\"title\").is_some());\n    assert!(row.get(\"status\").is_none());\n\n    let r2 = ctx.get_view_with_fields(\"projects\", ctx.eng_id, \"internal_notes\").await;\n    assert_eq!(r2.status(), 400);\n}</code></pre>\n<h3>5.5 <code>view_pagination_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cursor_round_trip_stable() {\n    let ctx = TestContext::with_engagement_subject().await;\n    for i in 0..150 {\n        ctx.seed_project(SyncClass::ClientVisible, &amp;format!(\"p{i}\")).await;\n    }\n    let r1 = ctx.get_view_limited(\"projects\", ctx.eng_id, 50).await;\n    let body1: serde_json::Value = r1.json().await.unwrap();\n    assert_eq!(body1[\"rows\"].as_array().unwrap().len(), 50);\n    let cursor = body1[\"next_cursor\"].as_str().unwrap();\n\n    let r2 = ctx.get_view_cursor(\"projects\", ctx.eng_id, cursor).await;\n    let body2: serde_json::Value = r2.json().await.unwrap();\n    assert_eq!(body2[\"rows\"].as_array().unwrap().len(), 50);\n}</code></pre>\n<h3>5.6 <code>view_export_csv_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn csv_export_streams_with_headers() {\n    let ctx = TestContext::with_engagement_subject().await;\n    for i in 0..100 { ctx.seed_project(SyncClass::ClientVisible, &amp;format!(\"p{i}\")).await; }\n    let r = ctx.get_export(\"projects\", ctx.eng_id, \"csv\").await;\n    assert_eq!(r.headers()[\"content-type\"].to_str().unwrap(), \"text/csv\");\n    let body = r.text().await.unwrap();\n    let lines: Vec&lt;&amp;str&gt; = body.lines().collect();\n    assert!(lines[0].contains(\"id,title,status\"));\n    assert_eq!(lines.len(), 101);  // header + 100 rows\n}</code></pre>\n<h3>5.7 <code>view_export_size_cap_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn export_over_10k_rejected() {\n    let ctx = TestContext::with_engagement_subject().await;\n    for i in 0..10001 { ctx.seed_project_fast(SyncClass::ClientVisible, &amp;format!(\"p{i}\")).await; }\n    let r = ctx.get_export(\"projects\", ctx.eng_id, \"csv\").await;\n    assert_eq!(r.status(), 413);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"export_too_large\");\n    assert_eq!(body[\"use_dsar\"], true);\n}</code></pre>\n<h3>5.8 <code>view_detail_with_subresources_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn detail_includes_tasks_and_comments() {\n    let ctx = TestContext::with_engagement_subject().await;\n    let proj_id = ctx.seed_project(SyncClass::ClientVisible, \"p1\").await;\n    ctx.seed_task_in_project(proj_id, SyncClass::ClientVisible, \"t1\").await;\n    ctx.seed_comment_in_project(proj_id, SyncClass::ClientVisible, \"c1\").await;\n\n    let r = ctx.get_view_detail(\"projects\", proj_id, ctx.eng_id).await;\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"title\"], \"p1\");\n    assert_eq!(body[\"tasks\"].as_array().unwrap().len(), 1);\n    assert_eq!(body[\"comments\"].as_array().unwrap().len(), 1);\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.view_detail_read\" &amp;&amp; r.severity == 2));\n}</code></pre>\n<h3>5.9 <code>view_kind_enum_cardinality_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn view_kind_has_5_values() {\n    let ctx = TestContext::new().await;\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::portal_view_kind))::text\"\n    ).fetch_all(&amp;ctx.pool).await.unwrap();\n    let mut labels = labels; labels.sort();\n    assert_eq!(labels, vec![\"calendar\",\"channels\",\"documents\",\"invoices\",\"projects\"]);\n}</code></pre>\n<h3>5.10 <code>view_etag_caching_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn etag_304_on_match() {\n    let ctx = TestContext::with_engagement_subject().await;\n    ctx.seed_project(SyncClass::ClientVisible, \"p1\").await;\n    let r1 = ctx.get_view(\"projects\", ctx.eng_id).await;\n    let etag = r1.headers()[\"etag\"].to_str().unwrap().to_owned();\n    let r2 = ctx.get_view_with_if_none_match(\"projects\", ctx.eng_id, &amp;etag).await;\n    assert_eq!(r2.status(), 304);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/portal/src/views/mod.rs\npub async fn list_handler(ctx: AppCtx, jwt: JwtClaims, view_kind: PortalViewKind,\n                          engagement_id: Uuid, filters: Filters, fields: FieldSet,\n                          cursor: Option&lt;Cursor&gt;, limit: u32) -&gt; Result&lt;ListResp, ViewError&gt; {\n    // Validations\n    require_engagement_membership(&amp;ctx, jwt.subject_id, engagement_id).await?;\n    rate_limit_check(&amp;ctx, jwt.subject_id).await?;\n    filters.validate_for_kind(view_kind)?;\n    fields.validate_for_kind(view_kind)?;\n\n    // Set session vars for RLS\n    let mut conn = ctx.pool.acquire().await?;\n    sqlx::query(\"SET LOCAL auth.subject_id = $1\").bind(jwt.subject_id).execute(&amp;mut *conn).await?;\n    sqlx::query(\"SET LOCAL auth.tenant_id = $1\").bind(jwt.tenant_id).execute(&amp;mut *conn).await?;\n\n    let (rows, next_cursor) = match view_kind {\n        PortalViewKind::Projects =&gt; projects::list(&amp;mut conn, engagement_id, &amp;filters, &amp;fields, cursor, limit).await?,\n        PortalViewKind::Invoices =&gt; invoices::list(/* ... */).await?,\n        /* ... */\n        PortalViewKind::Calendar =&gt; return Err(ViewError::SliceUnavailable),\n    };\n\n    // Apply per-row redaction\n    let redacted_rows: Vec&lt;_&gt; = rows.into_iter()\n        .map(|r| redaction::apply(view_kind, r))\n        .collect();\n    if redacted_rows.iter().any(|r| r.was_redacted) {\n        emit_audit(&amp;ctx, \"portal.view_redaction_applied\", json!({/*sampled*/})).await;\n    }\n\n    // Audit + ETag\n    emit_audit(&amp;ctx, \"portal.view_read\", json!({\n        \"engagement_id\": engagement_id,\n        \"view_kind\": view_kind,\n        \"filter_hash16\": filters.hash16(),\n        \"result_count\": redacted_rows.len(),\n    })).await;\n\n    let resp = ListResp { rows: redacted_rows, next_cursor };\n    let etag = sha256_hex(&amp;serde_json::to_vec(&amp;resp)?)[..16].to_owned();\n    Ok(ListResp { /* ... */ }.with_etag(etag).with_cache_control(\"private, max-age=30\"))\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>TASK-TEN-101</strong> Self-serve signup — tenant + Engagement exists before views.</li></ul>\n<p><strong>Cross-module (related_tasks):</strong></p>\n<ul><li><strong>TASK-PORTAL-002</strong> Brand pack — view UI inherits brand.</li><li><strong>TASK-PORTAL-003</strong> External IdP — caller_subject_id from IdP-auth JWT.</li><li><strong>TASK-PORTAL-004</strong> SCIM deprovision — revoked sessions return 401.</li><li><strong>TASK-PORTAL-005</strong> Branded Genie — uses same scope_grants pattern.</li><li><strong>TASK-PORTAL-006</strong> Client-initiated workflows — write counterpart to read views.</li><li><strong>TASK-PORTAL-008</strong> DSAR — full data export when &gt; 10k rows needed.</li><li><strong>TASK-PROJ-001</strong> Projects — source schema; sync_class filter joins here.</li><li><strong>TASK-INV-001</strong> Invoices — source schema.</li><li><strong>TASK-DOC-001</strong> Documents — source schema.</li><li><strong>TASK-CHAT-005</strong> Channels — source schema; messages too.</li><li><strong>TASK-AUTH-101</strong> RBAC — engagement_memberships table consumed.</li><li><strong>TASK-MEMORY-106</strong> sync_class enforcement — view definitions consume.</li><li><strong>TASK-AI-003</strong> memory audit — 6 new kinds.</li><li><strong>TASK-MEMORY-111</strong> PII scrubbing — filter hash only in chain.</li></ul>\n<p><strong>Downstream (blocks):</strong></p>\n<ul><li><strong>TASK-PORTAL-007</strong> PWA — needs views to display.</li><li><strong>TASK-PORTAL-008</strong> DSAR — references view shape for export.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 List response</h3>\n<pre><code class=\"language-json\">{\n  \"rows\": [\n    { \"id\": \"0190f7c0-...\", \"title\": \"Q2 audit\", \"status\": \"in_progress\",\n      \"owner_external_id\": \"alice@acme.com\", \"created_at\": \"2026-05-01T...\",\n      \"last_activity_at\": \"2026-05-17T...\" }\n  ],\n  \"next_cursor\": \"eyJsYXN0X2lkIjoiMDE5MGY3YzAtLi4uIiwibGFzdF9zb3J0X3ZhbHVlIjoiMjAyNi0wNS0xNyJ9\"\n}</code></pre>\n<h3>8.2 <code>portal.view_detail_read</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.view_detail_read\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.subject.456\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"engagement_id\": \"0190...\",\n    \"view_kind\": \"projects\",\n    \"resource_id\": \"0190f7c0-...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 1. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Calendar view (slice 2).</li><li><strong>Deferred:</strong> Real-time view updates via SSE (slice 2).</li><li><strong>Deferred:</strong> Saved filter presets per user (slice 2).</li><li><strong>Deferred:</strong> Custom field configuration per tenant (slice 3).</li><li><strong>Deferred:</strong> Bulk operations (multi-row actions) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Engagement not in caller membership</td><td>join returns 0</td><td>403 + <code>engagement_access_denied</code></td><td>Caller switches engagement</td></tr><tr><td>Unknown view_kind</td><td>enum validation</td><td>400 + <code>unknown_view_kind</code></td><td>Caller fixes URL</td></tr><tr><td>Unknown filter key</td><td>per-kind validation</td><td>400 + <code>invalid_filter_key</code> + valid_keys</td><td>Caller fixes filter</td></tr><tr><td>Unknown field in projection</td><td>safe-set check</td><td>400 + <code>unknown_field</code></td><td>Caller fixes fields param</td></tr><tr><td>Cursor signature invalid (tampered)</td><td>HMAC verify</td><td>400 + <code>cursor_invalid</code></td><td>Caller starts fresh</td></tr><tr><td>Export &gt; 10k rows</td><td>count pre-check</td><td>413 + <code>export_too_large; use_dsar</code></td><td>DSAR via TASK-PORTAL-008</td></tr><tr><td>Rate limit hit</td><td>counter</td><td>429 + Retry-After</td><td>Caller backs off</td></tr><tr><td>Sync_class column missing on source table</td><td>view DDL fails at migration</td><td>Migration error; rollback</td><td>Source module adds sync_class column</td></tr><tr><td>RLS bypassed (auth.subject_id not set)</td><td>view returns 0 rows</td><td>Empty list response</td><td>Handler bug; fix session var</td></tr><tr><td>Calendar view requested at slice 1</td><td>enum match → SliceUnavailable</td><td>501 + <code>slice_2_coming</code></td><td>Wait for slice 2</td></tr><tr><td>Detail row not found</td><td>view returns 0</td><td>404 + <code>not_found</code></td><td>Inherent</td></tr><tr><td>ETag collision (rare)</td><td>client sees stale 304</td><td>Up to 30s staleness</td><td>Cache TTL eventual consistency</td></tr><tr><td>Slow query on large filter</td><td>OBS p95 latency alarm</td><td>sev-3; investigate index</td><td>Add index per filter pattern</td></tr><tr><td>Export stream cancelled mid-way</td><td>client disconnect</td><td>Partial CSV written + audit <code>export_completed</code> with partial count</td><td>Client retries</td></tr><tr><td>Cross-tenant view via session var hack</td><td>RLS rejects via current_setting</td><td>0 rows; appears as empty list</td><td>Inherent isolation</td></tr><tr><td>Sub-resource missing for detail row</td><td>source table doesn't have row</td><td>Sub-resource field is empty array</td><td>Inherent</td></tr><tr><td>Search returns no results</td><td>tsvector miss</td><td>200 + empty rows + next_cursor null</td><td>Inherent</td></tr><tr><td>Field projection on sub-resource field</td><td>not in safe set</td><td>Rejected upfront</td><td>Caller uses detail endpoint</td></tr><tr><td>Filter date_range invalid format</td><td>parse error</td><td>400 + <code>invalid_date_format</code></td><td>Caller fixes</td></tr><tr><td>Postgres view definition out of sync with source table schema</td><td>migration test</td><td>CI fails</td><td>Update view + re-migrate</td></tr><tr><td>ETag computation overhead on huge response</td><td>OTel latency</td><td>sev-3 if p95 &gt; 500ms</td><td>Cache ETag in Redis (slice 2)</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> SQL views chosen over materialised views at slice 1 — correctness &gt; performance; slice 2 may add materialised view + refresh-on-source-change pattern.</p>\n<p><strong>§11.2</strong> Cursor HMAC uses server-side secret rotated quarterly; key in KMS.</p>\n<p><strong>§11.3</strong> ETag computation = SHA-256 truncated 16 hex; matches TASK-MCP-005 PRM pattern.</p>\n<p><strong>§11.4</strong> Sub-resource queries (detail endpoint) hit additional tables; each requires its own RLS path. Slice 1 = sequential queries; slice 2 = parallel via <code>tokio::join!</code>.</p>\n<p><strong>§11.5</strong> Field projection at SELECT level (not post-fetch filter) reduces row size + network bandwidth.</p>\n<p><strong>§11.6</strong> Export streaming via <code>axum::body::Body::from_stream</code>; row batches of 100 to bound memory.</p>\n<p><strong>§11.7</strong> XLSX export uses <code>rust_xlsxwriter</code> crate; streaming write to bytes buffer.</p>\n<p><strong>§11.8</strong> Rate limit per-caller via Redis sliding-window (consistent with all other PORTAL tasks).</p>\n<p><strong>§11.9</strong> SQL view RLS via WHERE clause + current_setting — not separate POLICY because views inherit base-table policies but adding view-specific RLS predicate is clearer.</p>\n<p><strong>§11.10</strong> Cache-Control: private + max-age=30 is intentional — 30s allows fast navigation between pages without re-querying; per-user privacy maintained.</p>\n<p><strong>§11.11</strong> The <code>portal.view_redaction_applied</code> audit is sampled 5% (higher than view_read's 1%) because redaction is forensically relevant — operators may want to see \"is the tenant correctly classifying sync_class\" patterns.</p>\n<p><strong>§11.12</strong> ETag is computed over the canonical-JSON of the response (sorted keys) for determinism — same data on same request returns same ETag.</p>\n<p><strong>§11.13</strong> The view definition aliases internal fields with CASE WHEN to NULL-out for redacted rows; client always sees the same JSON shape (avoids \"this row has fewer fields\" UX confusion).</p>\n<p><strong>§11.14</strong> Calendar view stub at slice 1 returns 501 immediately; logged but no audit row (informational only).</p>\n<p><strong>§11.15</strong> Export 10k cap is per-export; multiple exports can accumulate. Rate limit + DSAR alternative.</p>\n<hr>\n<p><em>End of TASK-PORTAL-001 spec.</em></p>";
