window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PORTAL-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship branded Genie chat at <code>services/portal/src/genie/</code> integrating TASK-CUO-101 with PORTAL's IdP-auth subjects, scope_grants narrowing per JWT, TASK-PORTAL-002 brand application, cross-tenant boundary enforcement, SCIM-cascade revocation per TASK-PORTAL-004, SSE streaming, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define the closed <code>cuo_scope_resource_type</code> enum at migration <code>0012</code>: <code>('project','document','invoice','channel','engagement')</code> per DEC-1172. CI cardinality test asserts 5.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_genie_sessions</code> table at migration <code>0012</code>: <code>(session_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, caller_subject_id UUID NOT NULL, persona_override TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), last_activity_at TIMESTAMPTZ NOT NULL DEFAULT now(), archived_at TIMESTAMPTZ, revoked_at TIMESTAMPTZ, revoked_reason TEXT)</code>. Partial unique <code>(engagement_id, caller_subject_id) WHERE archived_at IS NULL AND revoked_at IS NULL</code> — one active session per (caller, engagement).</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_genie_messages</code> table at migration <code>0013</code>: <code>(id BIGSERIAL PRIMARY KEY, session_id UUID NOT NULL REFERENCES portal_genie_sessions(session_id), role TEXT NOT NULL CHECK (role IN ('user','assistant','system')), content_kms_blob BYTEA NOT NULL, content_sha256 CHAR(64) NOT NULL, scope_grants_snapshot JSONB NOT NULL, boundary_violations JSONB DEFAULT '[]'::jsonb, created_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. Append-only; raw content KMS-encrypted; 90-day retention via scheduled prune.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with USING and WITH CHECK on both tables: <code>tenant_id = current_setting('auth.tenant_id')::uuid AND caller_subject_id = current_setting('auth.subject_id')::uuid</code> (per-caller scope).</li></ol>\n<ol><li><strong>MUST</strong> extend TASK-AUTH-004 JWT mint with <code>scope_grants</code> claim per DEC-1171. Claim shape: ``<code>json \"scope_grants\": [ { \"resource_type\": \"project\",    \"resource_ids\": [\"uuid1\",\"uuid2\"], \"permissions\": [\"read\"] }, { \"resource_type\": \"document\",   \"resource_ids\": [\"uuid3\"],         \"permissions\": [\"read\",\"comment\"] }, { \"resource_type\": \"engagement\", \"resource_ids\": [\"uuid_eng\"],      \"permissions\": [\"read\"] } ] </code>``</li></ol>\n<p>JWT mint resolves the caller's TASK-AUTH-101 RBAC role → scope_grants from <code>engagement_memberships</code> table.</p>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/portal/genie/query</code> per DEC-1173. Body: <code>{ session_id?, message }</code>. Handler:</li></ol>\n<ul><li>Validates JWT + extracts scope_grants.</li><li>Creates session if <code>session_id</code> absent (UUIDv7) or loads existing.</li><li>Rate-limit check per DEC-1179 (60/min/caller).</li><li>Pre-flight boundary check per §1 #7.</li><li>Invokes CUO with <code>{ message, scope_grants, persona }</code> context.</li><li>Post-flight boundary check on CUO response per §1 #8.</li><li>Returns SSE stream per DEC-1181; on success persists user + assistant messages.</li></ul>\n<ol><li><strong>MUST</strong> perform pre-flight boundary check at <code>services/portal/src/genie/boundary_check.rs::pre_flight(scope_grants, message)</code>:</li></ol>\n<ul><li>Heuristic check on message intent (regex + keyword match for known cross-tenant query patterns like \"all clients\", \"other engagements\").</li><li>If suspicious: don't block; flag for stricter post-flight check.</li><li>Always: ensure CUO query context is scoped to <code>(tenant_id, engagement_id from JWT)</code>.</li></ul>\n<ol><li><strong>MUST</strong> perform post-flight boundary check on CUO response per DEC-1174:</li></ol>\n<ul><li>CUO returns <code>retrieval_sources: [{type, id, tenant_id}]</code> metadata.</li><li>For each source: verify <code>source.tenant_id == jwt.tenant_id</code> AND <code>source.id ∈ scope_grants[source.type].resource_ids</code>.</li><li>Violation found: replace response with user-friendly message \"I can only see resources in this engagement\"; emit <code>portal.genie_boundary_violation_detected</code> sev-1 (security-critical); persist violation details in <code>portal_genie_messages.boundary_violations</code> JSONB.</li></ul>\n<ol><li><strong>MUST</strong> apply per-Engagement brand from TASK-PORTAL-002 per DEC-1175. The Genie UI inlines per-Engagement brand colours + logo via the <code>portal_brand_pack_active</code> lookup; default pack if none configured.</li></ol>\n<ol><li><strong>MUST</strong> apply per-Engagement persona override per DEC-1177. <code>portal_genie_sessions.persona_override</code> populated from <code>engagements.genie_persona</code> (slice 3 extension; slice 2 reads from in-memory config). Persona name passed to CUO as context modifier.</li></ol>\n<ol><li><strong>MUST</strong> stream SSE response per DEC-1181. Server-Sent Events at <code>/v1/portal/genie/query</code> with <code>Content-Type: text/event-stream</code>. Each event = one token. Client opens persistent connection; CUO emits tokens; server forwards as SSE events. Final event = <code>{ done: true, message_id, final_text }</code>. Non-SSE clients (Accept: application/json) get full response after CUO completes.</li></ol>\n<ol><li><strong>MUST</strong> invalidate Genie sessions on TASK-PORTAL-004 SCIM deprovision per DEC-1176. The cascade target <code>Genie sessions</code> from TASK-PORTAL-004 §1 #6 UPDATE <code>portal_genie_sessions SET revoked_at=now(), revoked_reason='scim_deprovision' WHERE caller_subject_id=$1 AND engagement_id=$2</code>. Active SSE connections close per TASK-PORTAL-004 §1 #5 (WebSocket close 4001 equivalent for SSE = abort stream + emit <code>error</code> event).</li></ol>\n<ol><li><strong>MUST</strong> rate-limit at 60 messages/min/caller per DEC-1179. Sliding-window via Redis. Excess returns SSE event <code>{ error: \"rate_limited\", retry_after_seconds: 60 }</code> + closes stream.</li></ol>\n<ol><li><strong>MUST</strong> persist conversation history per DEC-1178. Messages stored with role (user/assistant/system), KMS-encrypted content, scope_grants snapshot (audit forensic — what permissions did the caller have when this answer was generated). Default 90-day retention via daily prune job.</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit row kinds per DEC-1180:</li></ol>\n<ul><li><code>portal.genie_query_issued</code> (sev-3 — high-volume; sampled at 1% via TASK-OBS-006)</li><li><code>portal.genie_answer_emitted</code> (sev-3 — sampled)</li><li><code>portal.genie_boundary_violation_detected</code> (sev-1 — security-critical, ALWAYS emitted not sampled)</li><li><code>portal.genie_session_created</code> (sev-3)</li><li><code>portal.genie_session_archived</code> (sev-3)</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub per DEC-1183. Query + answer SHA256 hashes only in memory chain; raw text in <code>portal_genie_messages.content_kms_blob</code> (RLS-scoped, 90-day retention).</li></ol>\n<ol><li><strong>MUST</strong> enforce cross-tenant boundary at CUO invocation per DEC-1182. CUO orchestrator receives <code>scope_grants</code> context + <code>tenant_id</code> from JWT; CUO MUST NOT retrieve from any other tenant_id. Verified by <code>cuo.boundary_test</code> doctor invariant (TASK-CUO-101 derivative). Violation = <code>boundary_violation_detected</code> audit row + caller sees safe error.</li></ol>\n<ol><li><strong>MUST</strong> thread W3C trace_id end-to-end per task-audit skill rule 22-24. Genie query trace_id propagates through CUO invocation + audit rows + SSE events.</li></ol>\n<ol><li><strong>MUST</strong> expose session list at <code>GET /v1/portal/genie/sessions?engagement_id=...</code> per caller. Returns recent sessions (last 30 days; max 50). Tenant_admin sees all sessions for the Engagement via separate handler.</li></ol>\n<ol><li><strong>MUST</strong> support session archival at <code>POST /v1/portal/genie/sessions/{id}/archive</code>. Sets <code>archived_at=now()</code>; conversation history retained per 90-day retention but no longer surfaced in default list.</li></ol>\n<ol><li><strong>MUST</strong> detect and refuse cross-tenant queries at the boundary check per DEC-1182 + §1 #8. Test fixture: client user in tenant X attempts to query about tenant Y's data → CUO retrieval blocked (returns 0 sources from tenant Y) + boundary_violation_detected audit.</li></ol>\n<ol><li><strong>SHOULD</strong> observe per-Engagement query volume + latency via OTel histogram <code>portal_genie_query_duration_seconds{engagement_id}</code> for operator visibility.</li></ol>\n<ol><li><strong>SHOULD</strong> auto-archive idle sessions after 30 days of no activity (daily job) per DEC-1178 implementation note.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why scope_grants in JWT (§1 #5, DEC-1171)?</strong> The JWT is presented at every Genie query; reading scope_grants is free. Database lookup per query = latency + DB load. JWT mint computes scope_grants once from RBAC + Engagement membership; CUO consumes signed scope without re-verification. Standard JWT-as-credential pattern.</p>\n<p><strong>Why closed enum cuo_scope_resource_type (§1 #1, DEC-1172)?</strong> Free-form types invite mis-spellings + scope-creep. 5 resource types cover the slice-2 use cases (project, document, invoice, channel, engagement). Adding a 6th = schema migration + audit. Forces deliberate scope expansion.</p>\n<p><strong>Why pre-flight + post-flight boundary checks (§1 #7 + §1 #8)?</strong> Defense-in-depth. Pre-flight catches obvious cross-tenant intent (\"show me all clients\"); post-flight catches CUO over-retrieval (a CUO bug that touches disallowed data). Either alone misses cases; both together approach zero leakage.</p>\n<p><strong>Why \"I can only see X\" user-friendly message (§1 #8, DEC-1174)?</strong> Raw errors (\"403 boundary_violation_detected\") confuse end-users. Friendly framing avoids \"support escalation about confusing error\" while still security-correct (we don't reveal what they're not allowed to see).</p>\n<p><strong>Why per-caller session scope (§1 #2 partial unique, DEC-1178)?</strong> Multi-session per caller per engagement = confusion (which conversation is the user in?). One active session = clean UX; user can explicitly archive to start fresh.</p>\n<p><strong>Why SSE streaming (§1 #11, DEC-1181)?</strong> Perceived latency dominates UX; first-token latency &lt; 1s feels instant; 5s feels slow. CUO can take 5-15s for complex answers; SSE shows tokens as they're generated. Standard pattern (ChatGPT, Claude.ai, Copilot Chat all use SSE).</p>\n<p><strong>Why 60 msg/min rate limit (§1 #13, DEC-1179)?</strong> Genie is a chat — typing 60 msgs/min = ~1/sec which is faster than humans type. Above = bot or stuck retry loop; bound the abuse.</p>\n<p><strong>Why brand pack inheritance from TASK-PORTAL-002 (§1 #9, DEC-1175)?</strong> Single source of truth for tenant branding; Genie shouldn't have its own brand config. PORTAL-002 brand pack applies everywhere PORTAL renders, including Genie.</p>\n<p><strong>Why SCIM-cascade (§1 #12, DEC-1176)?</strong> Active Genie chat to a deprovisioned user is a 8h window of unauthorized chat. PORTAL-004 cascade target keeps the deprovision SLO tight (30s) across Genie too.</p>\n<p><strong>Why 90-day retention on messages (§1 #14, DEC-1178)?</strong> Long enough for the user to come back next sprint + re-find a useful answer; bounded to limit storage growth. Matches TASK-TEN-101 signup_sessions pattern.</p>\n<p><strong>Why scope_grants_snapshot per message (§1 #3, DEC-1183 derivative)?</strong> Forensic-critical. Audit question: \"Did this user have permission to ask about X at the time the answer was generated?\" Snapshot answers it exactly; reconstructing post-hoc (RBAC + Engagement membership changes over time) is unreliable.</p>\n<p><strong>Why cross-tenant boundary at CUO level (§1 #17, DEC-1182)?</strong> PORTAL is one layer; CUO is the retrieval layer. Defense-in-depth means BOTH must enforce. CUO's <code>cuo.boundary_test</code> doctor invariant validates this regardless of which surface (PORTAL, CHAT, CLI) invokes CUO.</p>\n<p><strong>Why per-Engagement persona override (§1 #10, DEC-1177)?</strong> Different client tones — a law firm wants formal Genie; a marketing agency wants friendly. One-Genie-fits-all = brand-mismatch for at least half the tenants.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0012_portal_genie_sessions.sql\nCREATE TYPE cuo_scope_resource_type AS ENUM ('project','document','invoice','channel','engagement');\n\nCREATE TABLE portal_genie_sessions (\n  session_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  caller_subject_id UUID NOT NULL,\n  persona_override TEXT,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  last_activity_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  archived_at TIMESTAMPTZ,\n  revoked_at TIMESTAMPTZ,\n  revoked_reason TEXT\n);\nCREATE UNIQUE INDEX uniq_active_genie_session\n  ON portal_genie_sessions(engagement_id, caller_subject_id)\n  WHERE archived_at IS NULL AND revoked_at IS NULL;\nCREATE INDEX idx_genie_sessions_caller ON portal_genie_sessions(caller_subject_id, last_activity_at DESC);\nALTER TABLE portal_genie_sessions ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_genie_sessions_rls ON portal_genie_sessions\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND caller_subject_id = current_setting('auth.subject_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND caller_subject_id = current_setting('auth.subject_id')::uuid);\nREVOKE DELETE ON portal_genie_sessions FROM cyberos_app;\nGRANT UPDATE (last_activity_at, archived_at, revoked_at, revoked_reason, persona_override)\n  ON portal_genie_sessions TO cyberos_app;\n\n-- 0013_portal_genie_messages.sql\nCREATE TABLE portal_genie_messages (\n  id BIGSERIAL PRIMARY KEY,\n  session_id UUID NOT NULL REFERENCES portal_genie_sessions(session_id),\n  role TEXT NOT NULL CHECK (role IN ('user','assistant','system')),\n  content_kms_blob BYTEA NOT NULL,\n  content_sha256 CHAR(64) NOT NULL,\n  kms_key_id TEXT NOT NULL,\n  scope_grants_snapshot JSONB NOT NULL,\n  boundary_violations JSONB NOT NULL DEFAULT '[]'::jsonb,\n  trace_id CHAR(32),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nCREATE INDEX idx_genie_messages_session ON portal_genie_messages(session_id, created_at);\nALTER TABLE portal_genie_messages ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_genie_messages_rls ON portal_genie_messages\n  USING (session_id IN (SELECT session_id FROM portal_genie_sessions\n                          WHERE caller_subject_id = current_setting('auth.subject_id')::uuid))\n  WITH CHECK (session_id IN (SELECT session_id FROM portal_genie_sessions\n                               WHERE caller_subject_id = current_setting('auth.subject_id')::uuid));\nREVOKE UPDATE, DELETE ON portal_genie_messages FROM cyberos_app;\nGRANT DELETE ON portal_genie_messages TO cyberos_pruner;  -- 90-day retention</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/portal/src/genie/scope_narrowing.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug, sqlx::Type, serde::Serialize, serde::Deserialize)]\n#[sqlx(type_name = \"cuo_scope_resource_type\", rename_all = \"snake_case\")]\npub enum CuoScopeResourceType { Project, Document, Invoice, Channel, Engagement }\n\n#[derive(serde::Serialize, serde::Deserialize, Debug, Clone)]\npub struct ScopeGrant {\n    pub resource_type: CuoScopeResourceType,\n    pub resource_ids: Vec&lt;Uuid&gt;,\n    pub permissions: Vec&lt;String&gt;,\n}\n\n// Tool-side CUO invocation context\n#[derive(serde::Serialize, Debug)]\npub struct CuoQueryContext {\n    pub tenant_id: Uuid,\n    pub engagement_id: Uuid,\n    pub caller_subject_id: Uuid,\n    pub scope_grants: Vec&lt;ScopeGrant&gt;,\n    pub persona: Option&lt;String&gt;,\n    pub trace_id: String,\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">POST   /v1/portal/genie/query                                (SSE or JSON; caller-owned)\nGET    /v1/portal/genie/sessions?engagement_id=...           (caller-owned list)\nGET    /v1/portal/genie/sessions/{id}/messages               (caller-owned history)\nPOST   /v1/portal/genie/sessions/{id}/archive                (caller-owned)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>cuo_scope_resource_type cardinality</strong> — enum = exactly <code>{project, document, invoice, channel, engagement}</code>.</li><li><strong>Happy query path</strong> — POST <code>{message: \"show my projects\"}</code> → CUO returns scoped list → SSE stream tokens → final message persisted.</li><li><strong>Scope narrowing</strong> — JWT scope_grants = <code>[{type:'project', ids:[uuid1]}]</code>; CUO retrieval returns ONLY uuid1; uuid2 (different project) NOT in retrieval.</li><li><strong>Boundary violation</strong> — CUO retrieves a document not in scope_grants → post-flight detects + replaces answer with safe message + sev-1 audit.</li><li><strong>Cross-tenant refused</strong> — caller in tenant X queries; CUO retrieval attempts tenant Y document → 0 results from tenant Y + boundary_violation_detected audit.</li><li><strong>Session persistence</strong> — second query with <code>session_id</code> from first → same session row updated, conversation history accumulates.</li><li><strong>Per-Engagement persona override</strong> — engagement A configured persona \"FormalLegal\"; tenant_admin sees Genie response styled accordingly.</li><li><strong>SSE streaming</strong> — Accept: text/event-stream → response is SSE; tokens stream as generated; final event has <code>done:true</code>.</li><li><strong>Non-SSE JSON fallback</strong> — Accept: application/json → full response after CUO completes.</li><li><strong>Brand pack applied</strong> — Genie UI fetches brand pack from TASK-PORTAL-002; falls back to default if none.</li><li><strong>Rate limit 60/min</strong> — 61st query in 60s → SSE event <code>{error: \"rate_limited\"}</code>.</li><li><strong>SCIM-cascade revoke</strong> — TASK-PORTAL-004 SCIM DELETE cascades to active Genie session → <code>revoked_at</code> set + active SSE aborted.</li><li><strong>Conversation history view</strong> — GET <code>/sessions/{id}/messages</code> returns decrypted messages in order.</li><li><strong>Session archive</strong> — POST archive → <code>archived_at</code> set + no longer in default list.</li><li><strong>Idle session auto-archive</strong> — fixture session with <code>last_activity_at</code> 31 days ago → daily job archives.</li><li><strong>Trace_id threaded</strong> — single trace_id across query + CUO invocation + audit rows + SSE events.</li><li><strong>5 memory audit kinds emitted</strong> — happy path covers query_issued + answer_emitted + session_created; failure paths cover boundary_violation + session_archived.</li><li><strong>PII scrub</strong> — audit row carries <code>content_sha256</code> only; raw text in DB.</li><li><strong>scope_grants in JWT</strong> — minted JWT contains scope_grants claim; validator passes through to handler.</li><li><strong>90-day retention prune</strong> — fixture message &gt; 90d old → daily prune sets content_kms_blob=NULL.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>genie_query_happy_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn happy_query_returns_scoped_response() {\n    let ctx = TestContext::with_engagement_subject_with_scope(vec![\n        ScopeGrant { resource_type: CuoScopeResourceType::Project, resource_ids: vec![ctx.proj_a], permissions: vec![\"read\".into()] }\n    ]).await;\n    let r = ctx.post_genie_query(\"show my projects\").await;\n    let messages = ctx.collect_sse_messages(r).await;\n    assert!(messages.iter().any(|m| m.contains(&amp;ctx.proj_a.to_string())));\n    assert!(!messages.iter().any(|m| m.contains(&amp;ctx.proj_b.to_string())));\n}</code></pre>\n<h3>5.2 <code>genie_scope_narrowing_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cuo_retrieval_narrowed_to_jwt_scope() {\n    let ctx = TestContext::with_scoped_subject(vec![ScopeGrant {\n        resource_type: CuoScopeResourceType::Document, resource_ids: vec![ctx.doc1], permissions: vec![\"read\".into()]\n    }]).await;\n    let r = ctx.post_genie_query(\"summarize all documents\").await;\n    let cuo_call = ctx.last_cuo_invocation().await;\n    assert_eq!(cuo_call.context.scope_grants.len(), 1);\n    assert_eq!(cuo_call.context.scope_grants[0].resource_ids, vec![ctx.doc1]);\n    assert!(!cuo_call.retrieval_sources.iter().any(|s| s.id == ctx.doc2));\n}</code></pre>\n<h3>5.3 <code>genie_boundary_violation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn boundary_violation_replaces_response() {\n    let ctx = TestContext::with_scoped_subject(vec![]).await;\n    ctx.force_cuo_to_retrieve_unauthorized_doc(ctx.unauthorized_doc).await;\n    let r = ctx.post_genie_query(\"explain x\").await;\n    let msgs = ctx.collect_sse_messages(r).await;\n    let full = msgs.join(\"\");\n    assert!(full.contains(\"I can only see resources in this engagement\"));\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.genie_boundary_violation_detected\" &amp;&amp; r.severity == 1));\n}</code></pre>\n<h3>5.4 <code>genie_cross_tenant_refused_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cross_tenant_query_blocked_at_cuo() {\n    let ctx = TestContext::with_two_tenants().await;\n    let r = ctx.as_caller(\"a\").post_genie_query(\"what does tenant b have\").await;\n    let _ = ctx.collect_sse_messages(r).await;\n\n    let cuo_call = ctx.last_cuo_invocation().await;\n    assert_eq!(cuo_call.context.tenant_id, ctx.tenant_a_id);\n    assert!(cuo_call.retrieval_sources.iter().all(|s| s.tenant_id == ctx.tenant_a_id));\n}</code></pre>\n<h3>5.5 <code>genie_session_persistence_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn second_query_continues_session() {\n    let ctx = TestContext::new().await;\n    let r1 = ctx.post_genie_query_new_session(\"hello\").await;\n    let session_id: Uuid = r1.json::&lt;serde_json::Value&gt;().await.unwrap()[\"session_id\"].as_str().unwrap().parse().unwrap();\n\n    let r2 = ctx.post_genie_query_in_session(session_id, \"follow up\").await;\n    let messages: Vec&lt;(String,String)&gt; = sqlx::query_as(\n        \"SELECT role, encode(content_sha256, 'escape') FROM portal_genie_messages WHERE session_id=$1 ORDER BY id\"\n    ).bind(session_id).fetch_all(&amp;ctx.pool).await.unwrap();\n    assert!(messages.len() &gt;= 4);  // 2 user + 2 assistant\n}</code></pre>\n<h3>5.6 <code>genie_scim_deprovision_cascade_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn scim_delete_revokes_active_genie_session() {\n    let ctx = TestContext::with_engagement_subject_with_jwt(\"alice@acme.com\").await;\n    let session_id = ctx.create_active_genie_session().await;\n    ctx.scim_delete_user(ctx.subject_id).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;  // async cascade\n\n    let revoked: Option&lt;DateTime&lt;Utc&gt;&gt; = sqlx::query_scalar(\n        \"SELECT revoked_at FROM portal_genie_sessions WHERE session_id=$1\"\n    ).bind(session_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(revoked.is_some());\n}</code></pre>\n<h3>5.7 <code>genie_persona_override_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn engagement_persona_passed_to_cuo() {\n    let ctx = TestContext::new().await;\n    ctx.set_engagement_persona(ctx.eng_id, \"FormalLegal\").await;\n    let r = ctx.post_genie_query(\"hello\").await;\n    let _ = ctx.collect_sse_messages(r).await;\n    let cuo_call = ctx.last_cuo_invocation().await;\n    assert_eq!(cuo_call.context.persona, Some(\"FormalLegal\".into()));\n}</code></pre>\n<h3>5.8 <code>genie_sse_streaming_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn sse_streams_tokens_progressively() {\n    let ctx = TestContext::new().await;\n    let r = ctx.post_genie_query_sse(\"explain x\").await;\n    let mut events = ctx.sse_event_iter(r).await;\n    let first_token = events.next().await.unwrap();\n    assert!(first_token.data.contains(\"data:\"));\n    let final_event = events.last().await.unwrap();\n    assert!(final_event.data.contains(\"\\\"done\\\":true\"));\n}</code></pre>\n<h3>5.9 <code>genie_rate_limit_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn rate_limit_at_60_per_min() {\n    let ctx = TestContext::new().await;\n    for _ in 0..60 {\n        let _ = ctx.post_genie_query(\"hi\").await;\n    }\n    let r = ctx.post_genie_query(\"one more\").await;\n    let msgs = ctx.collect_sse_messages(r).await;\n    assert!(msgs.iter().any(|m| m.contains(\"rate_limited\")));\n}</code></pre>\n<h3>5.10 <code>genie_scope_resource_type_enum_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cuo_scope_resource_type_has_5_values() {\n    let ctx = TestContext::new().await;\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::cuo_scope_resource_type))::text\"\n    ).fetch_all(&amp;ctx.pool).await.unwrap();\n    let mut labels = labels; labels.sort();\n    assert_eq!(labels, vec![\"channel\",\"document\",\"engagement\",\"invoice\",\"project\"]);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<h3>6.1 Genie query orchestrator</h3>\n<pre><code class=\"language-rust\">// services/portal/src/genie/query_handler.rs\npub async fn query(ctx: AppCtx, jwt: JwtClaims, req: GenieQueryReq) -&gt; Result&lt;SseResponse, GenieError&gt; {\n    // Rate limit\n    if !rate_limit_check(&amp;ctx, jwt.subject_id).await? { return Err(GenieError::RateLimited); }\n\n    // Load or create session\n    let session_id = match req.session_id {\n        Some(id) =&gt; { ctx.repo.genie_sessions.touch(id, jwt.subject_id).await?; id }\n        None =&gt; create_session(&amp;ctx, &amp;jwt, &amp;req.engagement_id).await?,\n    };\n\n    // Persona resolution\n    let persona = persona::resolve(&amp;ctx, jwt.engagement_id).await?;\n\n    // CUO context\n    let cuo_ctx = CuoQueryContext {\n        tenant_id: jwt.tenant_id,\n        engagement_id: jwt.engagement_id,\n        caller_subject_id: jwt.subject_id,\n        scope_grants: jwt.scope_grants.clone(),\n        persona,\n        trace_id: ctx.trace_id.clone(),\n    };\n\n    // Persist user message\n    let scope_snapshot = serde_json::to_value(&amp;jwt.scope_grants)?;\n    let user_msg_id = persist_message(&amp;ctx, session_id, \"user\", &amp;req.message, &amp;scope_snapshot, vec![]).await?;\n\n    emit_audit(&amp;ctx, \"portal.genie_query_issued\", json!({\n        \"session_id\": session_id, \"message_sha256\": sha256_hex(req.message.as_bytes()),\n    })).await;\n\n    // Invoke CUO; stream response\n    let (stream, retrieval_sources_handle) = ctx.cuo.invoke_streaming(cuo_ctx, req.message.clone()).await?;\n    let (tx, rx) = tokio::sync::mpsc::channel(32);\n    let ctx_clone = ctx.clone();\n    tokio::spawn(async move {\n        let mut accumulated = String::new();\n        while let Some(token) = stream.next().await {\n            accumulated.push_str(&amp;token);\n            let _ = tx.send(SseEvent::data(json!({\"token\": token}))).await;\n        }\n\n        // Post-flight boundary check on retrieval_sources\n        let sources = retrieval_sources_handle.await.unwrap_or_default();\n        let violations = boundary_check::post_flight(&amp;ctx_clone, &amp;jwt, &amp;sources);\n        if !violations.is_empty() {\n            accumulated = \"I can only see resources in this engagement. If you need broader access, please contact your administrator.\".into();\n            emit_audit(&amp;ctx_clone, \"portal.genie_boundary_violation_detected\", json!({\n                \"session_id\": session_id, \"violations\": violations,\n            })).await;\n        }\n\n        let assistant_scope_snapshot = serde_json::to_value(&amp;jwt.scope_grants).unwrap();\n        let assistant_msg_id = persist_message(&amp;ctx_clone, session_id, \"assistant\", &amp;accumulated, &amp;assistant_scope_snapshot, violations).await.ok();\n\n        emit_audit(&amp;ctx_clone, \"portal.genie_answer_emitted\", json!({\n            \"session_id\": session_id, \"message_id\": assistant_msg_id,\n            \"answer_sha256\": sha256_hex(accumulated.as_bytes()),\n        })).await;\n\n        let _ = tx.send(SseEvent::data(json!({\"done\": true, \"message_id\": assistant_msg_id}))).await;\n    });\n\n    Ok(SseResponse::from_rx(rx))\n}</code></pre>\n<h3>6.2 Post-flight boundary check</h3>\n<pre><code class=\"language-rust\">// services/portal/src/genie/boundary_check.rs\npub fn post_flight(ctx: &amp;AppCtx, jwt: &amp;JwtClaims, sources: &amp;[RetrievalSource]) -&gt; Vec&lt;BoundaryViolation&gt; {\n    let mut violations = Vec::new();\n    for source in sources {\n        if source.tenant_id != jwt.tenant_id {\n            violations.push(BoundaryViolation::CrossTenant {\n                expected: jwt.tenant_id, actual: source.tenant_id, source_id: source.id,\n            });\n            continue;\n        }\n        let allowed = jwt.scope_grants.iter().any(|g| {\n            g.resource_type == source.resource_type &amp;&amp; g.resource_ids.contains(&amp;source.id)\n        });\n        if !allowed {\n            violations.push(BoundaryViolation::OutOfScope {\n                resource_type: source.resource_type, resource_id: source.id,\n            });\n        }\n    }\n    violations\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>TASK-PORTAL-003</strong> External IdP + SCIM JIT — IdP-auth subjects + JWT scope_grants pattern.</li><li><strong>TASK-CUO-101</strong> LangGraph supervisor — CUO orchestrator invoked here; receives scope_grants context + enforces boundary via doctor invariant.</li></ul>\n<p><strong>Cross-module (related_tasks):</strong></p>\n<ul><li><strong>TASK-PORTAL-001</strong> Scoped read-only views — same scope_grants apply.</li><li><strong>TASK-PORTAL-002</strong> Brand pack — Genie UI inherits brand.</li><li><strong>TASK-PORTAL-004</strong> SCIM deprovision — cascade target.</li><li><strong>TASK-AUTH-004</strong> JWT mint — scope_grants claim extension here.</li><li><strong>TASK-AUTH-101</strong> RBAC — scope_grants derived from RBAC + Engagement membership.</li><li><strong>TASK-CHAT-005</strong> CHAT — Genie may surface inline in chat.</li><li><strong>TASK-AI-003</strong> memory audit-row bridge — 5 new kinds.</li><li><strong>TASK-MEMORY-111</strong> PII scrubbing — content SHA only.</li><li><strong>TASK-OBS-005</strong> Trace correlation — trace_id end-to-end.</li></ul>\n<p><strong>Downstream (blocks):</strong> None.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 Genie query request</h3>\n<pre><code class=\"language-json\">{\n  \"session_id\": null,\n  \"engagement_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n  \"message\": \"Summarize the documents for the Q2 review\"\n}</code></pre>\n<h3>8.2 SSE event stream</h3>\n<pre><code class=\"language-text\">event: message\ndata: {\"token\": \"Based\"}\n\nevent: message\ndata: {\"token\": \" on\"}\n\nevent: message\ndata: {\"token\": \" the\"}\n...\nevent: message\ndata: {\"done\": true, \"message_id\": 12345, \"final_text\": \"Based on the 3 documents in your scope...\"}</code></pre>\n<h3>8.3 <code>portal.genie_boundary_violation_detected</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.genie_boundary_violation_detected\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.subject.456\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"session_id\": \"0190f7c0-...\",\n    \"engagement_id\": \"0190f7c0-...\",\n    \"caller_subject_id_hash16\": \"f8a1b2c3d4e5f607\",\n    \"violations\": [\n      { \"type\": \"OutOfScope\", \"resource_type\": \"document\", \"resource_id\": \"9c4e7a8b-...\" }\n    ],\n    \"user_message_sha256\": \"ab12cd34...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 2. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-modal Genie input (image upload, file attach) — slice 3.</li><li><strong>Deferred:</strong> Voice input/output for Genie — slice 4.</li><li><strong>Deferred:</strong> Conversation export (PDF transcript) — slice 3.</li><li><strong>Deferred:</strong> Per-message reaction (thumbs up/down for feedback) — slice 3.</li><li><strong>Deferred:</strong> Cross-session search (\"find when I asked about X\") — slice 3.</li><li><strong>Deferred:</strong> Genie-to-tool action (Genie suggesting tool call requiring user confirm) — slice 4, requires TASK-MCP-008 integration.</li><li><strong>Deferred:</strong> Tenant_admin view of all caller sessions — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>CUO retrieves cross-tenant data</td><td>post-flight check</td><td>Response replaced with safe message; sev-1 audit</td><td>Operator investigates CUO; TASK-CUO-101 doctor invariant should have caught</td></tr><tr><td>CUO retrieves out-of-scope (same tenant, wrong resource)</td><td>post-flight</td><td>Same: safe message + audit</td><td>Operator reviews CUO's retrieval logic</td></tr><tr><td>Pre-flight heuristic false-positive (legit query flagged)</td><td>not blocked; only flag</td><td>Post-flight still runs; query proceeds</td><td>Inherent — pre-flight is hint not gate</td></tr><tr><td>Rate limit hit</td><td>Redis counter</td><td>SSE event rate_limited; stream closes</td><td>Caller backs off</td></tr><tr><td>CUO error (model unavailable)</td><td>CUO returns Err</td><td>SSE event error; session message logged with error</td><td>Caller retries</td></tr><tr><td>SSE connection drops mid-stream</td><td>TCP reset</td><td>Client sees partial response; reconnect with session_id loads accumulated history</td><td>Inherent</td></tr><tr><td>SCIM deprovision mid-query</td><td>revoked_at set during query</td><td>Active SSE aborted (error event); subsequent queries 401</td><td>Inherent cascade</td></tr><tr><td>Brand pack lookup fails</td><td>KMS or DB error</td><td>Default pack used + sev-3 log</td><td>Inherent fallback</td></tr><tr><td>Persona override invalid</td><td>unknown persona name</td><td>CUO uses default persona + sev-3 log</td><td>Admin sets valid persona</td></tr><tr><td>Conversation history &gt; 1000 messages</td><td>length cap</td><td>Truncate context window to recent 50 + summary</td><td>Inherent</td></tr><tr><td>Cross-tenant subject ID in scope_grants</td><td>RLS rejects</td><td>Query fails with 403 + sev-1 audit</td><td>Investigate JWT mint bug</td></tr><tr><td>KMS unavailable for message decrypt (history view)</td><td>KMS timeout</td><td>503 + temp unavailable</td><td>AWS KMS recovery</td></tr><tr><td>Idle session &gt; 30 days</td><td>daily archive job</td><td>session.archived_at set</td><td>Inherent</td></tr><tr><td>90-day message retention prune</td><td>daily job</td><td>content_kms_blob set NULL; metadata retained</td><td>Inherent</td></tr><tr><td>Session_id provided but belongs to different caller</td><td>RLS rejects</td><td>403</td><td>Caller uses own session_id</td></tr><tr><td>CUO emits sources without tenant_id metadata</td><td>post-flight needs the field</td><td>Sev-1 audit; conservative reject (assume violation)</td><td>TASK-CUO-101 schema fix</td></tr><tr><td>Scope_grants empty (no permissions)</td><td>JWT mint produces empty array</td><td>All queries return safe message (\"no access\")</td><td>Tenant_admin assigns membership</td></tr><tr><td>SSE backpressure from slow client</td><td>tokio channel full</td><td>CUO stream pauses; client should catch up</td><td>Inherent</td></tr><tr><td>Multiple concurrent queries on same session</td><td>last writer wins on last_activity_at</td><td>UX may show interleaved responses; client should serialize</td><td>UI-side serialisation</td></tr><tr><td>Boundary violation in pre-flight (cross-tenant intent keyword)</td><td>regex hit</td><td>Not blocked at pre-flight; post-flight still runs</td><td>Inherent — heuristic</td></tr><tr><td>Genie answer contains hallucinated tenant boundary leak (model says \"client B did X\")</td><td>not detectable at retrieval layer</td><td>Sev-3 informational; future enhancement: answer-content filter</td><td>Slice 4 LLM-side hallucination check</td></tr><tr><td>Persona prompt injection by malicious user message</td><td>prompt-injection guard</td><td>Stripped from context before CUO; sev-3 log</td><td>Inherent CUO defense</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> SSE Content-Type <code>text/event-stream</code>; uses <code>axum::response::sse::Sse</code> wrapper.</p>\n<p><strong>§11.2</strong> Rate limiter is sliding-window Redis (same pattern as TASK-TEN-101 + TASK-MCP-007).</p>\n<p><strong>§11.3</strong> scope_grants in JWT may grow large at high-RBAC users (tenant_admin with 50 projects). JWT size budget: keep &lt; 8 KB to fit cookie limits. Mitigation: tenant_admin scopes use wildcard <code>resource_ids: [\"*\"]</code> semantic (slice 3); slice 2 enumerates IDs (acceptable for typical 5-20 projects).</p>\n<p><strong>§11.4</strong> Wildcard <code>*</code> in resource_ids interpretation deferred to slice 3; slice 2 = literal UUID list only.</p>\n<p><strong>§11.5</strong> SSE event format follows W3C spec: <code>event:</code> + <code>data:</code> + blank line per event.</p>\n<p><strong>§11.6</strong> Pre-flight heuristic keywords: deny-list of intent phrases (\"all clients\", \"other engagements\", \"tenant X\"); future ML-based intent classifier (slice 4).</p>\n<p><strong>§11.7</strong> Post-flight enforcement: defense-in-depth — CUO's <code>cuo.boundary_test</code> invariant SHOULD prevent cross-tenant retrieval; this task catches CUO bugs.</p>\n<p><strong>§11.8</strong> Persona resolution: per-Engagement override &gt; tenant default &gt; CyberOS default. Cached in memory at handler startup; reload on engagement config change.</p>\n<p><strong>§11.9</strong> Conversation history retention: 90 days; daily prune via <code>cyberos_pruner</code> role (same as TASK-MCP-007 pattern).</p>\n<p><strong>§11.10</strong> scope_grants_snapshot per message critical for audit: \"what permissions did caller have when answering at T?\" reconstruct-from-snapshot only.</p>\n<p><strong>§11.11</strong> Idle session archive: 30 days no activity; daily job; caller can resume by creating new session_id (history view via <code>?include_archived=true</code>).</p>\n<p><strong>§11.12</strong> Boundary-violation audit always sev-1 + ALWAYS emitted (not sampled per OBS tail-sampling); security-critical.</p>\n<p><strong>§11.13</strong> Cross-session search (slice 3) requires full-text index on <code>portal_genie_messages.content</code> — gated on KMS-decrypt cost.</p>\n<p><strong>§11.14</strong> Genie response streaming uses CUO's streaming endpoint (TASK-CUO-101 derivative); non-streaming CUO models accumulate then emit single-event.</p>\n<p><strong>§11.15</strong> UI integration: <code>/v1/portal/genie/widget.html</code> (slice 3) serves the chat widget; slice 2 = API-only, frontend integrated by PORTAL-002 brand pack templates.</p>\n<p><strong>§11.16</strong> Cross-caller session_id access blocked by RLS; cross-Engagement same-caller blocked by <code>(engagement_id, caller_subject_id)</code> partial unique + the engagement_id check at handler.</p>\n<p><strong>§11.17</strong> Per-tool MCP integration via TASK-MCP-008 elicitation (slice 4): Genie suggests tool calls; user confirms via elicitation; tool runs; response continued.</p>\n<p><strong>§11.18</strong> OBS metric <code>portal_genie_query_duration_seconds</code> includes label <code>tenant_id</code> (cardinality OK at 100s of tenants).</p>\n<p><strong>§11.19</strong> SSE keepalive: send <code>: keepalive</code> comment every 30s to prevent intermediate proxy timeout.</p>\n<p><strong>§11.20</strong> Boundary violation kind always at sev-1 because security-critical; even sampled-OBS doesn't drop.</p>\n<hr>\n<p><em>End of TASK-PORTAL-005 spec.</em></p>";
