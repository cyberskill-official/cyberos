window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MCP-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The MCP service <strong>MUST</strong> ship Elicitation primitive at <code>services/mcp/src/elicitation/</code> per MCP 2025-11-25 spec — server-initiated structured prompts mid-tool-execution, response polling + cancellation + timeout, JSON Schema validation, 5 type-specific schemas, TASK-MCP-006 confirmation integration, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define the closed <code>elicitation_type</code> enum at migration <code>0012</code>: <code>('string_input','single_choice','multi_choice','confirmation','file_upload')</code> per DEC-1141. CI cardinality test asserts 5.</li></ol>\n<ol><li><strong>MUST</strong> define the closed <code>elicitation_status</code> enum: <code>('pending','responded','expired','cancelled','validation_failed')</code>. CI cardinality test asserts 5.</li></ol>\n<ol><li><strong>MUST</strong> define <code>mcp_elicitations</code> table at migration <code>0012</code>: <code>(elicitation_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, task_id UUID, caller_subject_id UUID NOT NULL, tool_id TEXT NOT NULL, elicitation_type elicitation_type NOT NULL, status elicitation_status NOT NULL DEFAULT 'pending', prompt JSONB NOT NULL, response_schema JSONB NOT NULL, response_payload_kms_blob BYTEA, response_payload_sha256 CHAR(64), validation_errors JSONB, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), responded_at TIMESTAMPTZ, timeout_seconds INT NOT NULL CHECK (timeout_seconds BETWEEN 1 AND 1800), expires_at TIMESTAMPTZ NOT NULL, trace_id CHAR(32), retry_count INT NOT NULL DEFAULT 0 CHECK (retry_count BETWEEN 0 AND 3))</code>. Per-task elicitations linked via task_id (NULL for sync-tool elicitations which are forbidden per DEC-1147 — defensive column).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with USING and WITH CHECK on the table: <code>tenant_id = current_setting('auth.tenant_id')::uuid AND caller_subject_id = current_setting('auth.subject_id')::uuid</code> (per-caller scope; tenant_admin can see all via separate handler).</li></ol>\n<ol><li><strong>MUST</strong> expose tool-side API at <code>services/mcp/src/elicitation/request.rs::TaskCtx::elicit(elicitation_type, prompt, response_schema, timeout_seconds) -&gt; Result&lt;ElicitationResponse, ElicitationError&gt;</code> per DEC-1140. The API:</li></ol>\n<ul><li>Validates tool's <code>supports_elicitation=true</code> per DEC-1146.</li><li>Validates <code>timeout_seconds ≤ 1800</code> (30 min max) per DEC-1143.</li><li>Generates UUIDv7 elicitation_id.</li><li>INSERTs <code>mcp_elicitations</code> row with status='pending', <code>expires_at=now()+timeout_seconds</code>.</li><li>Publishes to NATS <code>tenant.&lt;slug&gt;.mcp.elicitations.&lt;elicitation_id&gt;.requested</code> per DEC-1145 (push transport).</li><li>Awaits caller response via Postgres LISTEN/NOTIFY on <code>elicitation_responded:&lt;elicitation_id&gt;</code> channel with timeout.</li><li>On NOTIFY: loads row, returns response per type.</li><li>On timeout: transitions status='expired', emits <code>mcp.elicitation_timeout</code>, returns Err.</li><li>On cancellation: transitions status='cancelled', emits <code>mcp.elicitation_cancelled</code>, returns Err.</li></ul>\n<ol><li><strong>MUST</strong> expose caller polling at <code>GET /v1/mcp/elicitations?task_id=...&amp;status=pending</code> per DEC-1145. Returns array of pending elicitations for the caller's session. Rate-limit 60/min/caller (1/sec sufficient).</li></ol>\n<ol><li><strong>MUST</strong> expose caller response at <code>POST /v1/mcp/elicitations/{elicitation_id}/respond</code> with body <code>{ response_payload }</code>. Handler:</li></ol>\n<ul><li>Validates RLS + caller ownership.</li><li>Validates <code>response_payload</code> against <code>mcp_elicitations.response_schema</code> via JSON Schema validator per DEC-1142 + DEC-1150.</li><li>On validation success: KMS-encrypt + persist <code>response_payload_kms_blob</code>; transition status='responded'; <code>NOTIFY elicitation_responded:&lt;elicitation_id&gt;</code> to wake the tool.</li><li>On validation failure: increment <code>retry_count</code>; if <code>retry_count &gt; 3</code> → status='validation_failed' (terminal); else return 422 + validation_errors array (caller re-submits within the same elicitation_id).</li><li>Emit <code>mcp.elicitation_responded</code> (on success) or <code>mcp.elicitation_validation_failed</code> (on failure).</li></ul>\n<ol><li><strong>MUST</strong> expose cancellation at <code>POST /v1/mcp/elicitations/{id}/cancel</code> per DEC-1148. Handler:</li></ol>\n<ul><li>Transitions status='cancelled'; NOTIFY to wake the tool with cancellation signal.</li><li>Tool's <code>elicit()</code> returns Err(<code>elicitation_cancelled</code>); tool MAY decide to fail-task or retry-elicit.</li></ul>\n<ol><li><strong>MUST</strong> apply per-type fixed schemas per DEC-1141 + DEC-1152 + DEC-1160:</li></ol>\n<ul><li><strong>string_input</strong>: schema <code>{ type: \"object\", properties: { value: { type: \"string\", maxLength: 4096 } }, required: [\"value\"] }</code>.</li><li><strong>single_choice</strong>: schema <code>{ type: \"object\", properties: { value: { enum: [...prompt.choices.map(c =&gt; c.value)] } }, required: [\"value\"] }</code> (choices declared in prompt).</li><li><strong>multi_choice</strong>: schema <code>{ type: \"object\", properties: { values: { type: \"array\", items: { enum: [...] }, uniqueItems: true } }, required: [\"values\"] }</code>.</li><li><strong>confirmation</strong>: schema <code>{ type: \"object\", properties: { confirmed: { type: \"boolean\" }, reason: { type: \"string\", maxLength: 512 } }, required: [\"confirmed\"] }</code>.</li><li><strong>file_upload</strong>: schema <code>{ type: \"object\", properties: { s3_key: { type: \"string\", pattern: \"^elicitations/[a-f0-9-]{36}/[a-zA-Z0-9_.-]+$\" }, sha256: { type: \"string\", pattern: \"^[a-f0-9]{64}$\" }, size_bytes: { type: \"integer\", minimum: 1, maximum: 104857600 } }, required: [\"s3_key\", \"sha256\", \"size_bytes\"] }</code>.</li></ul>\n<ol><li><strong>MUST</strong> support file_upload elicitation per DEC-1153 via TASK-DOC-001 presigned S3 URLs. Handler at elicit creation for file_upload type:</li></ol>\n<ul><li>Generates per-elicitation S3 key <code>elicitations/{elicitation_id}/{filename}</code>.</li><li>Generates presigned PUT URL with <code>expires_at</code> matching elicitation timeout.</li><li>Returns URL in elicitation prompt payload (<code>prompt.upload_url</code>).</li><li>Caller uploads; then POSTs <code>respond</code> with <code>{s3_key, sha256, size_bytes}</code> per the schema.</li><li>Server verifies S3 object exists + size matches + SHA256 matches the uploaded object's ETag (or computed); on mismatch → 422.</li></ul>\n<ol><li><strong>MUST</strong> integrate with TASK-MCP-006 gating per DEC-1151. When TASK-MCP-006 policy says <code>mode=elicit</code> for a tool:</li></ol>\n<ul><li>Gating layer creates a <code>confirmation</code> elicitation with prompt describing the action.</li><li>Tool execution blocks until elicitation resolved.</li><li><code>confirmed=true</code> → tool proceeds; <code>confirmed=false</code> → tool fails with <code>user_rejected</code>.</li><li>De-stubs TASK-MCP-006's 503 placeholder (<code>elicitation_not_yet_supported</code>); TASK-MCP-006 <code>services/mcp/src/gating/elicit.rs</code> modified here.</li></ul>\n<ol><li><strong>MUST</strong> enforce 5-min default timeout + 30-min max per DEC-1143. Tool API enforces upper bound at create time. Daily timeout job per DEC-1144 sweeps expired pending elicitations.</li></ol>\n<ol><li><strong>MUST</strong> rate-limit elicitation creation at 100/min/task per DEC-1154. Excess returns Err(<code>elicitation_rate_limited</code>) to the tool.</li></ol>\n<ol><li><strong>MUST</strong> be idempotent on response submission per DEC-1156. Re-POSTing <code>respond</code> with same <code>elicitation_id</code> + <code>response_payload_sha256</code> returns cached response (200 + existing state); avoids double-side-effect on tool resume.</li></ol>\n<ol><li><strong>MUST</strong> apply re-elicit retry cap per DEC-1150. The <code>retry_count</code> column tracks retries; CHECK constraint enforces ≤ 3. Tool can call <code>ctx.elicit()</code> after a validation_failed elicitation but only 3 times for the same logical prompt (tool determines uniqueness; framework tracks count per elicitation_id chain).</li></ol>\n<ol><li><strong>MUST</strong> scope elicitation to caller_subject_id per DEC-1159. Cross-caller-subject GET/POST returns 403 + <code>cross_caller_access_denied</code> + sev-1 audit.</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit row kinds per DEC-1149 (task-audit skill rule 6):</li></ol>\n<ul><li><code>mcp.elicitation_requested</code> (sev-3 — informational; can be high-volume)</li><li><code>mcp.elicitation_responded</code> (sev-3)</li><li><code>mcp.elicitation_timeout</code> (sev-3)</li><li><code>mcp.elicitation_cancelled</code> (sev-3)</li><li><code>mcp.elicitation_validation_failed</code> (sev-2 — indicates bad tool design or compromised client)</li></ul>\n<ol><li><strong>MUST</strong> preserve trace_id end-to-end per DEC-1158. Parent task trace_id propagates to elicitation row + caller poll responses + audit rows + NATS publishes.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub per DEC-1157 + task-audit skill rule 18: <code>response_payload_sha256</code> only in memory chain; raw payload in <code>mcp_elicitations.response_payload_kms_blob</code> (RLS-scoped, 30-day retention post-completion).</li></ol>\n<ol><li><strong>MUST</strong> auto-clean responded elicitations at T+30 days post-completion. Daily job UPDATE <code>response_payload_kms_blob=NULL WHERE status IN ('responded','expired','cancelled','validation_failed') AND COALESCE(responded_at, expires_at) &lt; now() - interval '30 days'</code>. Metadata retained for forensic.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow elicitation from sync tools at slice 3 per DEC-1147. Tool's elicit() API checks calling context; if not within a task (long_running tool execution), returns Err(<code>sync_elicit_forbidden_slice_3</code>).</li></ol>\n<ol><li><strong>MUST NOT</strong> allow cross-tenant elicitation access per DEC-1159. RLS + explicit check both enforce.</li></ol>\n<ol><li><strong>MUST NOT</strong> persist responses past 30 days post-completion per DEC-1157 + §1 #20. Pruning auto-runs daily.</li></ol>\n<ol><li><strong>SHOULD</strong> observe per-tool elicitation latency (request → response) via OTel histogram <code>mcp_elicitation_duration_seconds{tool_id, type}</code> for UX visibility.</li></ol>\n<ol><li><strong>SHOULD</strong> support NATS push transport in addition to polling per DEC-1145; clients with NATS access get sub-second wake-up; polling clients see 1-sec lag.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why server-initiated prompts (§1 #5, DEC-1140)?</strong> Many tools need a clarification or confirmation that depends on partial work (e.g., \"found 3 candidate documents — which one?\"). Without elicitation, tool errors with missing-arg + caller retries from scratch; with elicitation, tool pauses + asks + resumes — much better UX.</p>\n<p><strong>Why JSON Schema validation (§1 #7, DEC-1142)?</strong> Free-form responses crash tool handlers (type errors, missing keys). JSON Schema declarative validation = tool implementer specifies the shape once; framework rejects malformed responses without tool code seeing them.</p>\n<p><strong>Why per-type fixed schemas (§1 #9, DEC-1141 + DEC-1152 + DEC-1160)?</strong> Common patterns (single_choice, confirmation) shouldn't require tool implementers to hand-craft JSON schemas every time. Fixed type-specific schemas = consistent UX across tools + less boilerplate.</p>\n<p><strong>Why ban elicitation in sync tools at slice 3 (§1 #21, DEC-1147)?</strong> Sync tools are 30s-bounded by gateway; blocking on a 5-min elicitation = guaranteed timeout. Long-running tasks (TASK-MCP-007) are the natural home for elicitation; banning sync usage prevents footgun. Slice 4 may revisit with shorter sync-elicit pattern.</p>\n<p><strong>Why re-elicit retry cap of 3 (§1 #15, DEC-1150)?</strong> Bug-defense — a tool that elicits with a too-strict schema could elicit-fail-elicit-fail forever if the caller can't satisfy it. 3 retries = generous (covers genuine typos); cap prevents infinite loops.</p>\n<p><strong>Why polling required + NATS push optional (§1 #6, DEC-1145)?</strong> Mirrors TASK-MCP-007 pattern: polling universal, push for high-volume clients. CHAT consumes elicitations via NATS push for sub-second UX; CLI tools poll.</p>\n<p><strong>Why file_upload via presigned S3 (§1 #10, DEC-1153)?</strong> Large file uploads through the MCP gateway bloat the request path + tie up memory. Presigned S3 = direct client-to-S3 upload; gateway only sees the reference. Standard pattern for file upload at scale.</p>\n<p><strong>Why TASK-MCP-006 confirm-mode satisfied by confirmation elicitation (§1 #11, DEC-1151)?</strong> Both patterns ask \"user, please confirm X\". Unifying them avoids the gating-layer reinventing prompt UX. The MCP-006 <code>elicit</code> mode (previously placeholder per TASK-MCP-006 DEC-1055) de-stubs by routing to this task's confirmation elicitation.</p>\n<p><strong>Why per-task elicitation scoping (§1 #3, schema task_id column)?</strong> Elicitations belong to a tool invocation. Listing pending elicitations by task_id (or session_id) gives the UI a clean per-task action panel. Cross-task elicitations would require additional UX state.</p>\n<p><strong>Why 30-min max timeout (§1 #12, DEC-1143)?</strong> Beyond 30 min, the user has likely closed the tab / context-switched / forgotten. Timeout-and-resume-with-error is the right semantic; longer timeouts waste worker capacity (worker holding the task slot).</p>\n<p><strong>Why audit at sev-3 for routine events (§1 #17)?</strong> Elicitation traffic is high-volume in interactive scenarios. Sev-3 = informational, sampled at the OBS layer per TASK-OBS-006. Sev-2 reserved for validation_failed (forensic signal for bad client behaviour or tool bugs).</p>\n<p><strong>Why presigned URL ttl matches elicitation timeout (§1 #10)?</strong> Two timeouts (URL vs elicitation) creates failure-mode confusion. Same TTL = single failure boundary.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0012_mcp_elicitations.sql\nCREATE TYPE elicitation_type AS ENUM ('string_input','single_choice','multi_choice','confirmation','file_upload');\nCREATE TYPE elicitation_status AS ENUM ('pending','responded','expired','cancelled','validation_failed');\n\nCREATE TABLE mcp_elicitations (\n  elicitation_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  task_id UUID,                                       -- nullable for defensive logging (sync tools forbidden)\n  caller_subject_id UUID NOT NULL,\n  tool_id TEXT NOT NULL,\n  elicitation_type elicitation_type NOT NULL,\n  status elicitation_status NOT NULL DEFAULT 'pending',\n  prompt JSONB NOT NULL,\n  response_schema JSONB NOT NULL,\n  response_payload_kms_blob BYTEA,\n  response_payload_sha256 CHAR(64),\n  validation_errors JSONB,\n  retry_count INT NOT NULL DEFAULT 0 CHECK (retry_count BETWEEN 0 AND 3),\n  timeout_seconds INT NOT NULL CHECK (timeout_seconds BETWEEN 1 AND 1800),\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  responded_at TIMESTAMPTZ,\n  expires_at TIMESTAMPTZ NOT NULL,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_elicit_caller_pending\n  ON mcp_elicitations(caller_subject_id, status, created_at DESC)\n  WHERE status = 'pending';\nCREATE INDEX idx_elicit_task ON mcp_elicitations(task_id) WHERE task_id IS NOT NULL;\nCREATE INDEX idx_elicit_expiry ON mcp_elicitations(expires_at) WHERE status = 'pending';\nALTER TABLE mcp_elicitations ENABLE ROW LEVEL SECURITY;\nCREATE POLICY mcp_elicitations_rls ON mcp_elicitations\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND caller_subject_id = current_setting('auth.subject_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND caller_subject_id = current_setting('auth.subject_id')::uuid);\nREVOKE DELETE ON mcp_elicitations FROM cyberos_app;\nGRANT UPDATE (status, response_payload_kms_blob, response_payload_sha256, validation_errors, retry_count, responded_at)\n  ON mcp_elicitations TO cyberos_app;\nGRANT DELETE ON mcp_elicitations TO cyberos_pruner;</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/mcp/src/elicitation/mod.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug, sqlx::Type, serde::Serialize)]\n#[sqlx(type_name = \"elicitation_type\", rename_all = \"snake_case\")]\npub enum ElicitationType { StringInput, SingleChoice, MultiChoice, Confirmation, FileUpload }\n\n#[derive(Copy, Clone, Eq, PartialEq, Debug, sqlx::Type, serde::Serialize)]\n#[sqlx(type_name = \"elicitation_status\", rename_all = \"snake_case\")]\npub enum ElicitationStatus { Pending, Responded, Expired, Cancelled, ValidationFailed }\n\n#[derive(Debug, serde::Serialize)]\npub struct ElicitationRequest {\n    pub elicitation_id: Uuid,\n    pub task_id: Option&lt;Uuid&gt;,\n    pub tool_id: String,\n    pub elicitation_type: ElicitationType,\n    pub prompt: serde_json::Value,\n    pub response_schema: serde_json::Value,\n    pub expires_at: DateTime&lt;Utc&gt;,\n}\n\n#[derive(Debug, serde::Deserialize)]\npub struct ElicitationRespondReq { pub response_payload: serde_json::Value }</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">GET    /v1/mcp/elicitations?task_id=...&amp;status=pending    (caller-owned polling)\nPOST   /v1/mcp/elicitations/{id}/respond                   (caller submits response)\nPOST   /v1/mcp/elicitations/{id}/cancel                    (caller cancels)</code></pre>\n<h3>3.4 Tool-side API</h3>\n<pre><code class=\"language-rust\">// available within TaskCtx (TASK-MCP-007 task execution context)\nasync fn elicit&lt;R: DeserializeOwned&gt;(\n    &amp;self,\n    elicitation_type: ElicitationType,\n    prompt: serde_json::Value,\n    response_schema: serde_json::Value,\n    timeout: Duration,\n) -&gt; Result&lt;R, ElicitationError&gt;;</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>elicitation_type cardinality</strong> — enum = exactly <code>{string_input, single_choice, multi_choice, confirmation, file_upload}</code>.</li><li><strong>elicitation_status cardinality</strong> — enum = exactly <code>{pending, responded, expired, cancelled, validation_failed}</code>.</li><li><strong>Request + response happy path</strong> — tool emits elicitation, caller polls, sees pending, POSTs response, tool resumes with parsed response.</li><li><strong>Timeout expires</strong> — elicitation with <code>timeout=2s</code>; no response in 3s → status='expired'; tool receives <code>elicitation_timeout</code> error; audit row emitted.</li><li><strong>Cancellation</strong> — caller cancels pending elicitation → status='cancelled'; tool receives <code>elicitation_cancelled</code>.</li><li><strong>Schema validation rejection</strong> — response payload missing required field → 422 + validation_errors; retry_count++.</li><li><strong>Re-elicit cap 3 retries</strong> — 4th attempt at re-submission for same <code>elicitation_id</code> → status='validation_failed' terminal.</li><li><strong>Confirmation type happy</strong> — tool emits <code>confirmation</code> elicit with prompt \"delete X?\"; caller responds <code>{confirmed: true}</code>; tool proceeds.</li><li><strong>File-upload presigned URL</strong> — tool emits <code>file_upload</code>; prompt response includes <code>upload_url</code> (presigned S3); caller PUTs file + POSTs response with s3_key.</li><li><strong>TASK-MCP-006 integration</strong> — TASK-MCP-006 policy <code>mode=elicit</code> on destructive tool routes through this task's confirmation elicit; previous 503 placeholder de-stubbed.</li><li><strong>Sync-tool elicit forbidden</strong> — sync tool calling <code>elicit()</code> returns Err <code>sync_elicit_forbidden_slice_3</code>.</li><li><strong>Cross-caller access denied</strong> — caller A polling caller B's elicitation_id → 403 + sev-1 audit.</li><li><strong>Cross-tenant access denied</strong> — RLS prevents cross-tenant read; verified via two-tenant test.</li><li><strong>Rate limit 100/min/task</strong> — 101st elicit() within 60s on same task → Err <code>elicitation_rate_limited</code>.</li><li><strong>Idempotent response submission</strong> — re-POST same response → 200 with existing state.</li><li><strong>NATS push transport</strong> — subscriber on <code>tenant.&lt;slug&gt;.mcp.elicitations.&lt;id&gt;.requested</code> receives push within 100ms of tool-side elicit().</li><li><strong>Trace_id preserved</strong> — single trace_id across tool's elicit() span + caller poll response + audit rows + NATS payload.</li><li><strong>30-day retention pruning</strong> — responded elicitation &gt; 30d post-completion → <code>response_payload_kms_blob=NULL</code> after prune; metadata retained.</li><li><strong>PII scrub</strong> — audit row carries <code>response_payload_sha256</code> only; raw payload in DB only.</li><li><strong>5 memory audit kinds emitted</strong> — full lifecycle covers requested + responded + timeout + cancelled + validation_failed.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>elicitation_request_response_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn happy_path_request_then_respond() {\n    let ctx = TestContext::new().await;\n    let task = ctx.spawn_long_running_tool_that_elicits().await;\n    let pending = ctx.poll_pending_elicitations_until_nonempty(task).await;\n    assert_eq!(pending.len(), 1);\n    let eid = pending[0].elicitation_id;\n\n    let r = ctx.respond_to_elicitation(eid, json!({\"value\": \"answer\"})).await;\n    assert_eq!(r.status(), 200);\n\n    let result = ctx.await_task_completion(task, Duration::from_secs(5)).await;\n    assert_eq!(result[\"received_value\"], \"answer\");\n}</code></pre>\n<h3>5.2 <code>elicitation_timeout_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn elicitation_expires_after_timeout() {\n    let ctx = TestContext::new().await;\n    let task = ctx.spawn_tool_that_elicits_with_timeout(2).await;\n    tokio::time::sleep(Duration::from_secs(3)).await;\n    ctx.run_elicitation_timeout_job().await;\n\n    let row: (String,) = sqlx::query_as(\"SELECT status::text FROM mcp_elicitations WHERE task_id=$1\")\n        .bind(task).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(row.0, \"expired\");\n\n    let task_result = ctx.poll_task(task).await;\n    assert!(task_result.error.is_some());\n    assert_eq!(task_result.error.unwrap().message, \"elicitation_timeout\");\n}</code></pre>\n<h3>5.3 <code>elicitation_schema_validation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn missing_required_field_rejected_with_422() {\n    let ctx = TestContext::new().await;\n    let task = ctx.spawn_tool_that_elicits_string_input().await;\n    let pending = ctx.poll_pending_elicitations_until_nonempty(task).await;\n    let eid = pending[0].elicitation_id;\n\n    let r = ctx.respond_to_elicitation(eid, json!({})).await;  // missing \"value\"\n    assert_eq!(r.status(), 422);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert!(body[\"validation_errors\"].as_array().unwrap().len() &gt; 0);\n}</code></pre>\n<h3>5.4 <code>elicitation_re_elicit_after_invalid_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn three_retries_then_terminal() {\n    let ctx = TestContext::new().await;\n    let task = ctx.spawn_tool_that_elicits_string_input().await;\n    let eid = ctx.poll_pending_elicitations_until_nonempty(task).await[0].elicitation_id;\n\n    for _ in 0..3 {\n        let r = ctx.respond_to_elicitation(eid, json!({})).await;\n        assert_eq!(r.status(), 422);\n    }\n    let r4 = ctx.respond_to_elicitation(eid, json!({})).await;\n    assert_eq!(r4.status(), 422);\n\n    let row: (String,) = sqlx::query_as(\"SELECT status::text FROM mcp_elicitations WHERE elicitation_id=$1\")\n        .bind(eid).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(row.0, \"validation_failed\");\n}</code></pre>\n<h3>5.5 <code>elicitation_confirmation_integration_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn mcp006_elicit_mode_routes_through_elicitation() {\n    let ctx = TestContext::with_policy_mode(\"elicit\").await;\n    ctx.register_tool(\"cyberos.docs.delete\", destructive_long_running_annotations()).await;\n    let r = ctx.invoke_tool(\"cyberos.docs.delete\", json!({\"doc_id\": \"x\"})).await;\n    let task_id: Uuid = r.json::&lt;serde_json::Value&gt;().await.unwrap()[\"task_id\"].as_str().unwrap().parse().unwrap();\n\n    let pending = ctx.poll_pending_elicitations_until_nonempty(task_id).await;\n    assert_eq!(pending[0].elicitation_type, ElicitationType::Confirmation);\n\n    ctx.respond_to_elicitation(pending[0].elicitation_id, json!({\"confirmed\": true})).await;\n    let result = ctx.await_task_completion(task_id, Duration::from_secs(5)).await;\n    assert_eq!(result[\"deleted\"], true);\n}</code></pre>\n<h3>5.6 <code>elicitation_cancellation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cancel_transitions_and_unblocks_tool() {\n    let ctx = TestContext::new().await;\n    let task = ctx.spawn_tool_that_elicits_string_input().await;\n    let eid = ctx.poll_pending_elicitations_until_nonempty(task).await[0].elicitation_id;\n    let r = ctx.cancel_elicitation(eid).await;\n    assert_eq!(r.status(), 200);\n    let task_result = ctx.await_task_completion(task, Duration::from_secs(3)).await;\n    assert!(task_result.error.is_some());\n    assert_eq!(task_result.error.unwrap().message, \"elicitation_cancelled\");\n}</code></pre>\n<h3>5.7 <code>elicitation_type_enum_cardinality_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn elicitation_type_has_5_values() {\n    let ctx = TestContext::new().await;\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::elicitation_type))::text\"\n    ).fetch_all(&amp;ctx.pool).await.unwrap();\n    let mut labels = labels; labels.sort();\n    assert_eq!(labels, vec![\"confirmation\",\"file_upload\",\"multi_choice\",\"single_choice\",\"string_input\"]);\n}</code></pre>\n<h3>5.8 <code>elicitation_cross_tenant_denied_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn caller_b_cannot_access_caller_a_elicitation() {\n    let ctx = TestContext::with_two_callers().await;\n    let task_a = ctx.as_caller(\"a\").spawn_tool_that_elicits_string_input().await;\n    let eid_a = ctx.as_caller(\"a\").poll_pending_elicitations_until_nonempty(task_a).await[0].elicitation_id;\n\n    let r = ctx.as_caller(\"b\").respond_to_elicitation(eid_a, json!({\"value\": \"x\"})).await;\n    assert_eq!(r.status(), 403);\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"mcp.elicitation_validation_failed\"\n        || r.kind == \"mcp.cross_caller_access_denied\"));  // either security audit\n}</code></pre>\n<h3>5.9 <code>elicitation_idempotency_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn duplicate_response_idempotent() {\n    let ctx = TestContext::new().await;\n    let task = ctx.spawn_tool_that_elicits_string_input().await;\n    let eid = ctx.poll_pending_elicitations_until_nonempty(task).await[0].elicitation_id;\n\n    let r1 = ctx.respond_to_elicitation(eid, json!({\"value\": \"answer\"})).await;\n    assert_eq!(r1.status(), 200);\n    let r2 = ctx.respond_to_elicitation(eid, json!({\"value\": \"answer\"})).await;\n    assert_eq!(r2.status(), 200);  // idempotent\n\n    let count: i64 = sqlx::query_scalar(\n        \"SELECT count(*) FROM mcp_elicitations WHERE elicitation_id=$1 AND status='responded'\"\n    ).bind(eid).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(count, 1);\n}</code></pre>\n<h3>5.10 <code>elicitation_file_upload_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn file_upload_via_presigned_url() {\n    let ctx = TestContext::with_s3_mock().await;\n    let task = ctx.spawn_tool_that_elicits_file_upload().await;\n    let pending = ctx.poll_pending_elicitations_until_nonempty(task).await;\n    let upload_url = pending[0].prompt[\"upload_url\"].as_str().unwrap();\n\n    let bytes = b\"test file contents\".to_vec();\n    ctx.put_to_presigned(upload_url, &amp;bytes).await;\n\n    let sha256 = sha256_hex(&amp;bytes);\n    let s3_key = ctx.extract_s3_key_from_url(upload_url);\n    let r = ctx.respond_to_elicitation(pending[0].elicitation_id, json!({\n        \"s3_key\": s3_key, \"sha256\": sha256, \"size_bytes\": bytes.len()\n    })).await;\n    assert_eq!(r.status(), 200);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<h3>6.1 Tool-side elicit API</h3>\n<pre><code class=\"language-rust\">// services/mcp/src/elicitation/request.rs\nimpl TaskCtx {\n    pub async fn elicit&lt;R: DeserializeOwned&gt;(\n        &amp;self,\n        elicit_type: ElicitationType,\n        prompt: serde_json::Value,\n        schema: serde_json::Value,\n        timeout: Duration,\n    ) -&gt; Result&lt;R, ElicitationError&gt; {\n        // Check sync-tool ban\n        if self.task_id.is_none() {\n            return Err(ElicitationError::SyncElicitForbiddenSlice3);\n        }\n        // Rate limit\n        if !self.elicit_rate_limit_check().await? { return Err(ElicitationError::RateLimited); }\n        // Validate timeout\n        if timeout &gt; Duration::from_secs(1800) { return Err(ElicitationError::TimeoutTooLong); }\n\n        let elicit_id = uuid7();\n        let expires_at = Utc::now() + chrono::Duration::from_std(timeout)?;\n        let trace_id = self.trace_id.clone();\n\n        // INSERT pending row\n        sqlx::query(/* ... */)\n            .bind(elicit_id).bind(self.tenant_id).bind(self.task_id)\n            .bind(self.caller_subject_id).bind(self.tool_id)\n            .bind(elicit_type).bind(prompt).bind(schema)\n            .bind(timeout.as_secs() as i32).bind(expires_at).bind(&amp;trace_id)\n            .execute(&amp;self.pool).await?;\n\n        // NATS push\n        self.nats.publish(\n            format!(\"tenant.{}.mcp.elicitations.{}.requested\", self.tenant_slug, elicit_id),\n            serde_json::to_vec(&amp;ElicitationRequest {\n                elicitation_id: elicit_id, task_id: self.task_id, tool_id: self.tool_id.clone(),\n                elicitation_type: elicit_type, prompt: prompt.clone(), response_schema: schema.clone(),\n                expires_at,\n            })?\n        ).await;\n\n        emit_audit(&amp;self.ctx, \"mcp.elicitation_requested\", json!({/*...*/})).await;\n\n        // Await response via LISTEN/NOTIFY\n        let mut listener = sqlx::postgres::PgListener::connect_with(&amp;self.pool).await?;\n        listener.listen(&amp;format!(\"elicitation_responded:{elicit_id}\")).await?;\n        let signal = tokio::time::timeout(timeout, listener.recv()).await;\n\n        match signal {\n            Err(_) =&gt; {  // timeout\n                let _ = sqlx::query(\"UPDATE mcp_elicitations SET status='expired' WHERE elicitation_id=$1 AND status='pending'\")\n                    .bind(elicit_id).execute(&amp;self.pool).await;\n                emit_audit(&amp;self.ctx, \"mcp.elicitation_timeout\", json!({/*...*/})).await;\n                Err(ElicitationError::Timeout)\n            }\n            Ok(Ok(_)) =&gt; {\n                let row: (String, Option&lt;Vec&lt;u8&gt;&gt;) = sqlx::query_as(\n                    \"SELECT status::text, response_payload_kms_blob FROM mcp_elicitations WHERE elicitation_id=$1\"\n                ).bind(elicit_id).fetch_one(&amp;self.pool).await?;\n                match row.0.as_str() {\n                    \"responded\" =&gt; {\n                        let decrypted = kms_decrypt(&amp;row.1.unwrap()).await?;\n                        let parsed: R = serde_json::from_slice(&amp;decrypted)?;\n                        Ok(parsed)\n                    }\n                    \"cancelled\" =&gt; Err(ElicitationError::Cancelled),\n                    \"validation_failed\" =&gt; Err(ElicitationError::ValidationFailed),\n                    _ =&gt; Err(ElicitationError::UnexpectedState(row.0)),\n                }\n            }\n            Ok(Err(e)) =&gt; Err(ElicitationError::ListenerError(e.to_string())),\n        }\n    }\n}</code></pre>\n<h3>6.2 Response handler</h3>\n<pre><code class=\"language-rust\">pub async fn respond(ctx: &amp;AppCtx, elicit_id: Uuid, req: ElicitationRespondReq) -&gt; Result&lt;Response, RespondError&gt; {\n    let elicit_row = ctx.repo.elicitations.find_pending(elicit_id).await?\n        .ok_or(RespondError::NotFound)?;\n\n    // RLS already verified caller; explicit re-check\n    if elicit_row.caller_subject_id != ctx.caller_subject_id {\n        emit_audit(ctx, \"mcp.cross_caller_access_denied\", json!({/*...*/})).await;\n        return Err(RespondError::Forbidden);\n    }\n\n    // Schema validation\n    let validator = jsonschema::JSONSchema::compile(&amp;elicit_row.response_schema)?;\n    if let Err(errs) = validator.validate(&amp;req.response_payload) {\n        let errors_json: Vec&lt;serde_json::Value&gt; = errs.map(|e| json!({\"path\": e.instance_path.to_string(), \"msg\": e.to_string()})).collect();\n        let next_retry = elicit_row.retry_count + 1;\n        if next_retry &gt; 3 {\n            sqlx::query(\"UPDATE mcp_elicitations SET status='validation_failed', validation_errors=$1, retry_count=$2 WHERE elicitation_id=$3\")\n                .bind(serde_json::Value::Array(errors_json.clone())).bind(next_retry).bind(elicit_id).execute(&amp;ctx.pool).await?;\n            emit_audit(ctx, \"mcp.elicitation_validation_failed\", json!({/*...*/})).await;\n            ctx.notify(&amp;format!(\"elicitation_responded:{elicit_id}\")).await;\n        } else {\n            sqlx::query(\"UPDATE mcp_elicitations SET validation_errors=$1, retry_count=$2 WHERE elicitation_id=$3\")\n                .bind(serde_json::Value::Array(errors_json.clone())).bind(next_retry).bind(elicit_id).execute(&amp;ctx.pool).await?;\n        }\n        return Ok(Response::status(422).json(json!({\"validation_errors\": errors_json})));\n    }\n\n    // Persist response\n    let bytes = serde_json::to_vec(&amp;req.response_payload)?;\n    let sha256 = sha256_hex(&amp;bytes);\n    let encrypted = kms_encrypt(&amp;bytes).await?;\n    sqlx::query(\"UPDATE mcp_elicitations SET status='responded', response_payload_kms_blob=$1,\n                 response_payload_sha256=$2, responded_at=now() WHERE elicitation_id=$3 AND status='pending'\")\n        .bind(encrypted.blob).bind(&amp;sha256).bind(elicit_id).execute(&amp;ctx.pool).await?;\n    ctx.notify(&amp;format!(\"elicitation_responded:{elicit_id}\")).await;\n    emit_audit(ctx, \"mcp.elicitation_responded\", json!({/*...*/})).await;\n\n    Ok(Response::status(200))\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>TASK-MCP-001</strong> spec compliance — elicitation defined per MCP 2025-11-25 spec.</li><li><strong>TASK-MCP-004</strong> OAuth 2.1 PKCE — JWT bearer auth for poll/respond/cancel.</li></ul>\n<p><strong>Cross-module (related_tasks):</strong></p>\n<ul><li><strong>TASK-MCP-006</strong> Gating — confirmation elicitation satisfies MCP-006 <code>mode=elicit</code> policy; de-stubs MCP-006's 503 placeholder.</li><li><strong>TASK-MCP-007</strong> Tasks primitive — elicit() is exposed on TaskCtx; only callable from within a long-running task.</li><li><strong>TASK-AUTH-004</strong> JWT validate — caller_subject_id verification.</li><li><strong>TASK-AI-003</strong> memory audit-row bridge — 5 new kinds.</li><li><strong>TASK-MEMORY-111</strong> PII scrubbing — response payload SHA only.</li><li><strong>TASK-CHAT-005</strong> CHAT — surfaces elicitations inline in conversation; NATS push consumer.</li><li><strong>TASK-OBS-005</strong> Trace correlation — trace_id end-to-end.</li><li><strong>TASK-OBS-007</strong> Auto-runbook — sev-2 validation_failed alerts.</li></ul>\n<p><strong>Downstream (blocks):</strong> None at this slice.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 Elicitation request (NATS payload + caller poll response)</h3>\n<pre><code class=\"language-json\">{\n  \"elicitation_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n  \"task_id\": \"0190f7c0-8b3c-7a4f-bbbb-000000000042\",\n  \"tool_id\": \"cyberos.kb.bulk_delete\",\n  \"elicitation_type\": \"confirmation\",\n  \"prompt\": {\n    \"title\": \"Confirm bulk delete\",\n    \"description\": \"About to delete 247 documents matching 'archived'. This cannot be undone.\",\n    \"action_summary\": \"Delete 247 archived documents\"\n  },\n  \"response_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"confirmed\": { \"type\": \"boolean\" },\n      \"reason\": { \"type\": \"string\", \"maxLength\": 512 }\n    },\n    \"required\": [\"confirmed\"]\n  },\n  \"expires_at\": \"2026-05-17T09:19:32.847Z\"\n}</code></pre>\n<h3>8.2 Caller response</h3>\n<pre><code class=\"language-json\">{\n  \"response_payload\": {\n    \"confirmed\": true,\n    \"reason\": \"Verified with team; archived docs cleanup approved\"\n  }\n}</code></pre>\n<h3>8.3 <code>mcp.elicitation_requested</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"mcp.elicitation_requested\",\n  \"severity\": 3,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.mcp.elicitation\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"elicitation_id\": \"0190f7c0-...\",\n    \"task_id\": \"0190f7c0-...\",\n    \"caller_subject_id\": \"7c4e...\",\n    \"tool_id\": \"cyberos.kb.bulk_delete\",\n    \"elicitation_type\": \"confirmation\",\n    \"timeout_seconds\": 300\n  }\n}</code></pre>\n<h3>8.4 File-upload elicitation prompt</h3>\n<pre><code class=\"language-json\">{\n  \"title\": \"Upload signed contract\",\n  \"description\": \"Please upload the signed PDF (max 100 MiB)\",\n  \"upload_url\": \"https://s3.amazonaws.com/cyberos-elicit/elicitations/0190.../contract.pdf?X-Amz-Signature=...\",\n  \"expires_at\": \"2026-05-17T09:19:32.847Z\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 3. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Sync-tool short-elicit pattern (&lt; 10s timeout, blocking the sync response) — slice 4.</li><li><strong>Deferred:</strong> Elicitation chaining (response to one elicit triggers another) — slice 4.</li><li><strong>Deferred:</strong> Voice/audio elicitation type — slice 4.</li><li><strong>Deferred:</strong> Webhook-delivered elicitations (vs polling/push) — slice 4.</li><li><strong>Deferred:</strong> Multi-recipient elicitation (any-of-N teammates can answer) — slice 4.</li><li><strong>Deferred:</strong> Elicitation templates (named pre-configured prompts) — slice 4.</li><li><strong>Deferred:</strong> Per-tool elicitation rate limit override — slice 4.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Elicitation timeout</td><td>NOTIFY recv timeout</td><td>status='expired'; tool gets <code>Timeout</code> Err</td><td>Tool decides retry-or-fail; daily sweep ensures status update if NOTIFY missed</td></tr><tr><td>Response schema validation fails</td><td>jsonschema validate</td><td>422 + validation_errors; retry_count++; capped at 3</td><td>Caller fixes payload + re-submits</td></tr><tr><td>4th retry on validation_failed</td><td>retry cap check</td><td>status='validation_failed' terminal; tool gets <code>ValidationFailed</code> Err</td><td>Tool decides retry-or-fail</td></tr><tr><td>Cancellation</td><td>caller POST cancel</td><td>status='cancelled'; tool gets <code>Cancelled</code> Err</td><td>Tool decides response</td></tr><tr><td>Cross-caller access</td><td>RLS + explicit caller_subject_id check</td><td>403 + sev-1 audit</td><td>Investigate compromised JWT</td></tr><tr><td>Cross-tenant access</td><td>RLS rejects</td><td>0 rows; appears as 404</td><td>Inherent</td></tr><tr><td>Sync tool calls elicit()</td><td>task_id=None check</td><td>Err <code>sync_elicit_forbidden_slice_3</code></td><td>Tool ported to long_running pattern</td></tr><tr><td>Rate limit hit (100/min/task)</td><td>counter</td><td>Err <code>elicitation_rate_limited</code></td><td>Tool reduces elicit volume; possibly batch prompts</td></tr><tr><td>KMS unavailable for response decrypt</td><td>KMS timeout</td><td>Err <code>kms_unavailable</code>; tool gets error</td><td>AWS KMS recovery</td></tr><tr><td>Tool process crashes during NOTIFY wait</td><td>listener dies</td><td>Tool restarted; new instance picks up task; elicitation status still 'pending' or 'responded'</td><td>TASK-MCP-007 task resume covers; new tool instance polls elicitation status</td></tr><tr><td>Duplicate response submission</td><td>UPDATE WHERE status='pending' returns 0 rows on 2nd</td><td>200 OK with existing state (idempotent)</td><td>Inherent</td></tr><tr><td>Presigned S3 URL expired before upload</td><td>S3 returns 403 on upload</td><td>Caller sees S3 error; elicitation_timeout eventually fires</td><td>Caller restarts upload; tool re-elicits</td></tr><tr><td>File upload exceeds 100 MiB</td><td>S3 PUT size check</td><td>S3 rejects; caller sees error</td><td>Caller compresses or splits</td></tr><tr><td>File SHA256 mismatch (corruption)</td><td>server compares</td><td>422 + <code>sha256_mismatch</code></td><td>Caller re-uploads</td></tr><tr><td>Tool elicits more than 3 retries same logical prompt</td><td>retry_count check</td><td>Returns <code>ValidationFailed</code> after 3rd terminal</td><td>Tool changes prompt or schema</td></tr><tr><td>Tenant_admin viewing elicitations across callers</td><td>separate handler with admin role</td><td>List view shows all; per-row audit on access</td><td>OK with audit visibility</td></tr><tr><td>Prompt JSONB &gt; 1 MiB (huge schema)</td><td>size check</td><td>400 + <code>prompt_too_large</code></td><td>Tool slims prompt</td></tr><tr><td>Postgres LISTEN/NOTIFY drops (connection reset)</td><td>listener error</td><td>Tool restarts listener; checks DB for status</td><td>Inherent reconnect logic</td></tr><tr><td>Pending elicitation orphaned (task crashed without resolving)</td><td>daily expiry sweep</td><td>status='expired' at timeout</td><td>Inherent</td></tr><tr><td>Multiple workers respond to same NOTIFY</td><td>first writer wins UPDATE</td><td>Other workers see status≠'pending'; no-op</td><td>Inherent CAS</td></tr><tr><td>Elicitation_id collision (UUIDv7)</td><td>primary key constraint</td><td>INSERT fails; retry with new ID</td><td>Astronomical odds</td></tr><tr><td>Caller polls deleted task's elicitations</td><td>task_id check</td><td>Returns 404</td><td>Inherent</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> JSON Schema validator: <code>jsonschema</code> crate; compile schemas once + cache; instances reused per elicitation.</p>\n<p><strong>§11.2</strong> LISTEN/NOTIFY connection requires a dedicated pool connection (not from main pool — long-lived). The <code>tasks/worker_pool.rs</code> pre-allocates a notify-listener connection per worker.</p>\n<p><strong>§11.3</strong> NATS push subject pattern: <code>tenant.&lt;slug&gt;.mcp.elicitations.&lt;id&gt;.&lt;event&gt;</code> where event in {requested, responded, expired, cancelled}.</p>\n<p><strong>§11.4</strong> Presigned S3 URL TTL matches elicitation timeout exactly; TASK-DOC-001 provides the presigning helper.</p>\n<p><strong>§11.5</strong> Cross-caller audit uses a distinct kind (<code>mcp.cross_caller_access_denied</code>) outside the 5-kind core list per task-audit skill §8.1d (security signal needs distinct kind).</p>\n<p><strong>§11.6</strong> UUIDv7 generation: same <code>services/ten/src/residency/uuid_gen.rs</code> per TASK-TEN-103.</p>\n<p><strong>§11.7</strong> Rate-limit counter: Redis sliding-window per task_id; TASK-MCP-007 ctx exposes the limiter.</p>\n<p><strong>§11.8</strong> Prompt and response_schema stored as JSONB for flexibility; future indexed queries on prompt content (slice 4) trivial.</p>\n<p><strong>§11.9</strong> The 30-day pruning happens via daily <code>cyberos_pruner</code> job (same role as TASK-TEN-003 + TASK-MCP-007 patterns).</p>\n<p><strong>§11.10</strong> Elicitation status transitions are LINEAR: <code>pending → {responded | expired | cancelled | validation_failed}</code>. No reverse transitions. Tool's retry creates NEW elicitation row, not status reversal on existing.</p>\n<p><strong>§11.11</strong> Confirmation elicitation has a default 5-min timeout (matching TASK-MCP-006 confirm-token TTL for consistency); tools can override.</p>\n<p><strong>§11.12</strong> Per-type prompt JSONB schemas are described in implementer-docs (not normative spec); tools build prompts per type-specific UX guidelines.</p>\n<p><strong>§11.13</strong> The <code>task_id IS NULL</code> defensive column allows future sync-tool elicitation without schema migration; slice 3 enforces non-null via runtime check, not constraint.</p>\n<p><strong>§11.14</strong> Caller polling query uses <code>idx_elicit_caller_pending</code> index; sub-ms response.</p>\n<p><strong>§11.15</strong> NATS push payload is full ElicitationRequest struct; clients can react immediately without polling.</p>\n<p><strong>§11.16</strong> Cross-caller audit is sev-1 but uses a kind outside the 5-core list — operators see in security dashboards alongside TASK-TEN-103 cross-residency events.</p>\n<p><strong>§11.17</strong> Tool implementer responsibility: validate schema is reasonable (not too strict, not too permissive); poor schema = bad UX or security hole.</p>\n<p><strong>§11.18</strong> The elicit() API is awaitable inside async tool implementations; non-blocking from worker pool perspective (semaphore released? no — worker holds slot while waiting for human response, which is correct given task-bounded concurrency model).</p>\n<p><strong>§11.19</strong> Caller poll endpoint scope: <code>caller_subject_id = jwt.subject_id</code> (own elicitations only); tenant_admin has separate list endpoint at slice 4.</p>\n<p><strong>§11.20</strong> Idempotency on response submission is per <code>(elicitation_id, response_payload_sha256)</code> — different payload → different submission, not idempotent.</p>\n<hr>\n<p><em>End of TASK-MCP-008 spec.</em></p>";
