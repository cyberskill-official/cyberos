window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-061"]="<h1>TASK-IMP-061: BRAIN Phase 0 consent completion</h1>\n<h2>Summary</h2>\n<p>Layer-1 BRAIN already <em>declares</em> consent on personnel memories (PERSON template, gateguard checklist, audit <code>consent_event_id</code>, fixture 21), but the protocol has no normative consent section, no consent-record store path, no walker invariant, and the install payload can vendor the thin root <code>AGENTS.md</code> into <code>.cyberos/memory/AGENTS.md</code> instead of the dense Layer-1 protocol. This task completes Phase 0 for the <strong>agent BRAIN</strong> (markdown store): protocol §19, <code>meta/consent/</code> scaffolding, <code>personnel-requires-consent</code> invariant + tests, and a build fix so the protocol (including §19) actually ships.</p>\n<h2>Problem</h2>\n<p>Deep-audit Stage 5 and the brain-evaluation plan both order <strong>governance before capture</strong>. For the product BRAIN/EVAL path that means TASK-EVAL-001 notice + acknowledgment (and counsel clearance of <code>docs/legal/data-monitoring-and-evaluation-notice.md</code>). That product path is <strong>not</strong> this task: Track C (IMP-066) is closed won't-do for 1.x, and EVAL-001 remains <code>on_hold</code>.</p>\n<p>For the <strong>Layer-1 agent BRAIN</strong> the gap is smaller and already named in-tree: fixture 21 says \"validator currently doesn't enforce consent\". PERSON writes require a consent event id, but there is no <code>CONSENT</code> template, no <code>meta/consent/</code> home, no <code>cyberos doctor</code> check, and no AGENTS.md clause. Agents can invent <code>has_consent: true</code> with a null event and stay doctor-green. Separately, <code>build.sh</code> materialises root thin <code>AGENTS.md</code> into the payload memory tree, so even a correct protocol update may not reach consumer installs.</p>\n<h2>Proposed Solution</h2>\n<ol><li>Add <strong>AGENTS.md §19 (Phase 0 consent)</strong> — personnel-classified / person-kind memories MUST carry <code>consent.has_consent: true</code> and a non-null <code>consent.consent_event</code> that resolves to <code>meta/consent/&lt;id&gt;.md</code> (or a matching audit row id).</li><li>Scaffold <strong>consent records</strong> under <code>meta/consent/</code> (starter README + <code>CONSENT.md</code> template); install creates the directory on fresh BRAIN scaffold.</li><li>Add walker invariant <strong><code>personnel-requires-consent</code></strong> (error level) + pytest coverage for pass/fail/resolve paths.</li><li>Flip fixture 21's expected critical code to <code>personnel-requires-consent</code> (forward-compat corpus + generator comment).</li><li>Fix <strong><code>build.sh</code></strong> to materialise <code>modules/memory/cyberos/data/AGENTS.md</code> into <code>$out/memory/AGENTS.md</code> (IMP-138 Branch A normative home).</li><li>Document explicit <strong>non-goals</strong>: counsel clearance of the employment monitoring notice; EVAL-001 / capture activation; Track C rollout.</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Close as won't-do / blocked on counsel.</strong> Rejected for the Layer-1 half: fixture 21 and gateguard already commit to enforceable consent; leaving it forever soft is the opposite of Phase 0. Counsel clearance remains a separate operator/legal gate documented in <code>docs/deploy/brain-capture-activation.md</code>.</li><li><strong>Implement full EVAL-001 activation in this PR.</strong> Rejected: IMP-066 closed won't-do for 1.x; EVAL-001 is <code>on_hold</code> pending AUTH; scope would be a product rewrite, not payload scaffolding.</li><li><strong>Only docs, no invariant.</strong> Rejected: without a walker check, Phase 0 is aspirational and fixture 21 stays a lie.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a store with <code>classification: personnel</code> and no resolvable <code>consent.consent_event</code> fails <code>personnel-requires-consent</code>; a store with a matching <code>meta/consent/&lt;id&gt;.md</code> passes. Suite-asserted.</li><li>Guardrail: payload <code>memory/AGENTS.md</code> after <code>build.sh</code> matches the dense protocol (contains <code>§19</code> / <code>Phase 0 consent</code>), not only the thin spine.</li></ul>\n<h2>Scope</h2>\n<p>In scope: Layer-1 protocol AGENTS.md §19; consent templates + starter/install scaffolding; invariant + tests; fixture 21 expectation; build.sh AGENTS vendor path; task lifecycle + batch evidence.</p>\n<p>Out of scope / Non-Goals:</p>\n<ul><li>Publishing or counsel-clearing <code>docs/legal/data-monitoring-and-evaluation-notice.md</code>.</li><li>Wiring <code>SqlConsentGate</code> / TASK-EVAL-001 acknowledgment ledger / <code>CAPTURE_ENABLED</code>.</li><li>Track C brain activation rollout (IMP-066 closed).</li><li>Restoring a separate workbench <code>cyberos_validate.py</code> binary (living check is <code>cyberos.core.invariants</code>).</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Related: EVAL-001 (product gate, separate), IMP-138 (Branch A already decided; this task finishes the build vendor path for the protocol file), IMP-066 (closed; depended on this task for Track C which will not ship in 1.x).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Composer (Cursor agent) under CyberOS ship-tasks / session HITL for batch/12f.</li><li><strong>Scope:</strong> Layer-1 BRAIN Phase 0 consent scaffolding only. Re-derived and CONFIRMED: fixture 21 previously expected no critical codes and documented the missing rule (<code>generate_vectors.py</code>); gateguard PERSON path already required consent_event; PERSON template used <code>${CONSENT_EVENT_ID}</code> with no CONSENT template; build.sh materialised root thin AGENTS.md into memory/. Re-derived and CORRECTED: build vendor path root <code>AGENTS.md</code> -&gt; <code>modules/memory/cyberos/data/AGENTS.md</code> (IMP-138 Branch A home). Measured and ADDED: AGENTS.md §19, <code>personnel-requires-consent</code> invariant, <code>meta/consent/</code> scaffold, <code>CONSENT.md</code> template, <code>test_personnel_consent.py</code> (8 cases). Product EVAL notice clearance and capture activation excluded.</li><li><strong>Human review:</strong> session HITL override for batch/12f (operator Stephen Cheng); Gate-1 and Gate-2 evidence under docs/batches/batch-12f-*.</li></ul>\n<h2>1. Description (BCP-14 normative)</h2>\n<ul><li>1.1 The protocol MUST add AGENTS.md §19 Phase 0 consent stating that any memory whose frontmatter has <code>classification: personnel</code> OR <code>kind: person</code> OR a <code>scope</code> containing <code>people</code> MUST include a <code>consent</code> object with <code>has_consent: true</code> and a non-null <code>consent_event</code> string.</li><li>1.2 <code>consent.consent_event</code> MUST resolve to either (a) a file <code>meta/consent/&lt;consent_event&gt;.md</code> under the store, or (b) an <code>audit_id</code> present in a legacy <code>audit/*.jsonl</code> row. Unresolved ids MUST fail the doctor walk.</li><li>1.3 Consent records under <code>meta/consent/</code> MUST be documented as append-only disclosures and MUST NOT be treated as a substitute for the product EVAL acknowledgment ledger.</li><li>1.4 The repo MUST ship starter template <code>CONSENT.md</code> and a <code>meta/consent/README.md</code> in the cyberos-starter tree.</li><li>1.5 <code>install.sh</code> MUST create <code>meta/consent/</code> when scaffolding a fresh BRAIN store.</li><li>1.6 Invariant <code>personnel-requires-consent</code> (level: error) MUST exist with check <code>cyberos.core.invariants.check_personnel_requires_consent</code>, registered identically in both invariants.yaml copies.</li><li>1.7 Tests MUST prove: clean store passes; personnel without consent fails; unresolved event fails; resolvable <code>meta/consent/&lt;id&gt;.md</code> passes.</li><li>1.8 Vector fixture <code>21-personnel-no-consent</code> MUST expect critical code <code>personnel-requires-consent</code>.</li><li>1.9 <code>build.sh</code> MUST materialise <code>modules/memory/cyberos/data/AGENTS.md</code> (not root thin <code>AGENTS.md</code>) into the install payload at <code>memory/AGENTS.md</code>.</li><li>1.10 This task MUST NOT claim counsel clearance of the employment monitoring notice, flip <code>CAPTURE_ENABLED</code>, or implement EVAL notice/ack APIs.</li><li>1.11 Session HITL evidence MUST be recorded under <code>docs/batches/batch-12f-*</code>; <code>done</code> requires recorded human verdicts (session override allowed).</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[x] AC 1 (traces_to: #1.1) - AGENTS.md §19 present with BCP-14 MUST clauses for personnel consent + resolution rules - verify: <code>grep -n '§19' modules/memory/cyberos/data/AGENTS.md</code></li><li>[x] AC 2 (traces_to: #1.2,#1.4,#1.5) - <code>meta/consent/</code> scaffolded in starter + fresh install; consent_event resolution documented; <code>CONSENT.md</code> template exists - verify: paths exist on disk</li><li>[x] AC 3 (traces_to: #1.6,#1.7) - <code>personnel-requires-consent</code> fails open violations and passes resolved consent - test: <code>modules/memory/tests/test_personnel_consent.py</code></li><li>[x] AC 4 (traces_to: #1.7) - personnel consent suite green (8 cases) - test: <code>modules/memory/tests/test_personnel_consent.py</code></li><li>[x] AC 5 (traces_to: #1.8) - fixture 21 expects <code>personnel-requires-consent</code> - verify: <code>modules/memory/tools/tests/vectors/21-personnel-no-consent/expected.json</code></li><li>[x] AC 6 (traces_to: #1.9) - <code>build.sh</code> vendors dense protocol; payload <code>memory/AGENTS.md</code> contains <code>§19</code> - verify: <code>grep data/AGENTS.md tools/install/build.sh</code></li><li>[x] AC 7 (traces_to: #1.3,#1.10) - Spec non-goals exclude notice clearance / EVAL activation; no capture env flips - verify: recorded diff review</li><li>[x] AC 8 (traces_to: #1.11) - Task reaches <code>done</code> with Gate-1 + Gate-2 session HITL evidence under <code>docs/batches/</code> - verify: <code>docs/batches/batch-12f-gate1-acceptance.md</code></li></ul>\n<h2>Verification</h2>\n<ol><li><code>cd modules/memory &amp;&amp; PYTHONPATH=cyberos:../.. pytest tests/test_personnel_consent.py -q</code></li><li><code>bash tools/install/build.sh</code> (or repo equivalent) and <code>grep -n '§19' dist/cyberos/memory/AGENTS.md</code></li><li>Manual: construct temp store with personnel md lacking consent → <code>python -c 'from cyberos.core.invariants import check_personnel_requires_consent; ...'</code> returns failed.</li></ol>\n<h2>Edge cases</h2>\n<table><thead><tr><th>Case</th><th>Expected</th></tr></thead><tbody><tr><td>No memories/ yet</td><td>Pass (nothing to gate)</td></tr><tr><td>Operational classification, no consent block</td><td>Pass</td></tr><tr><td><code>has_consent: true</code>, <code>consent_event: null</code></td><td>Fail</td></tr><tr><td><code>has_consent: false</code> on personnel</td><td>Fail</td></tr><tr><td>Event id file missing</td><td>Fail</td></tr><tr><td>Event id matches <code>meta/consent/&lt;id&gt;.md</code></td><td>Pass</td></tr><tr><td>README.md under meta/consent/</td><td>Ignored as a consent event</td></tr><tr><td>Product notice still DRAFT</td><td>Out of scope — documented, not blocked</td></tr></tbody></table>\n<h2>Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Mitigation</th></tr></thead><tbody><tr><td>Protocol edited but thin AGENTS still vendored</td><td>AC6 grep</td><td>build.sh path fix</td></tr><tr><td>Invariant yaml drift between two copies</td><td>byte-identical check in review</td><td>edit both / cp</td></tr><tr><td>Agents set has_consent without event</td><td>AC3</td><td>invariant</td></tr></tbody></table>\n<h2>Implementation notes</h2>\n<ul><li>Prefer extending existing frontmatter <code>consent:</code> shape; do not invent a second schema.</li><li>Keep resolution file-based first; jsonl audit_id match is back-compat only.</li><li>Cite TASK-IMP-061 as the §0.2 approval channel for §19 (operator ship session 2026-07-25), same pattern as P19/P20/P22 section headers.</li></ul>";
