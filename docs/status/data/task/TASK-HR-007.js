window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-HR-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The HR service <strong>MUST</strong> ship onboarding saga at <code>services/hr/src/onboarding/</code> orchestrating 6 module provisions in order with compensating rollback, immutable saga state, 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> trigger on <code>member.status</code> transition to 'active' per DEC-1883 — hook at <code>services/hr/src/members.rs</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>saga_step</code> per DEC-1881, <code>saga_status</code> per DEC-1882.</li></ol>\n<ol><li><strong>MUST</strong> execute steps in fixed order per DEC-1880 at <code>saga_orchestrator.rs::run(member)</code>:</li><li>auth_provision — TASK-AUTH-101 create user with role per contract type</li><li>time_init — TASK-TIME-001 create member time profile</li><li>learn_assign_starter — TASK-LEARN-001 assign starter pack</li><li>kb_grant_scope — TASK-KB-001 grant team scope</li><li>chat_create_channel — TASK-CHAT-005 add to team channels</li><li>rew_init_baseline — TASK-REW-001 init comp record</li></ol>\n<ol><li><strong>MUST</strong> be idempotent per DEC-1880 — each step checks \"already done\" before acting.</li></ol>\n<ol><li><strong>MUST</strong> compensate per DEC-1884 on any failure at <code>compensation.rs::compensate(saga, failed_step)</code>:</li></ol>\n<ul><li>Run reverse ops in REVERSE order of completed steps.</li><li>E.g. if rew_init fails, compensate chat → kb → learn → time → auth.</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0009</code>: ``<code>sql CREATE TABLE hr_onboarding_sagas ( saga_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_id UUID NOT NULL UNIQUE, current_step TEXT CHECK (current_step IS NULL OR current_step IN ('auth_provision','time_init','learn_assign_starter','kb_grant_scope','chat_create_channel','rew_init_baseline')), completed_steps TEXT[] NOT NULL DEFAULT '{}', status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','in_progress','completed','failed','compensating','compensated')), failed_step TEXT, failure_reason TEXT, compensation_log JSONB, trace_id CHAR(32), started_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); ALTER TABLE hr_onboarding_sagas ENABLE ROW LEVEL SECURITY; CREATE POLICY saga_rls ON hr_onboarding_sagas USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON hr_onboarding_sagas FROM cyberos_app; GRANT UPDATE (current_step, completed_steps, status, failed_step, failure_reason, compensation_log, started_at, completed_at) ON hr_onboarding_sagas TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST   /v1/hr/onboarding/start                (CHRO; manual trigger if hook missed) POST   /v1/hr/onboarding/{saga_id}/retry      (CHRO; resume failed at step) POST   /v1/hr/onboarding/{saga_id}/compensate (CHRO; force rollback) GET    /v1/hr/onboarding/sagas/{id}            (status) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 6 memory audit kinds per DEC-1885. PII per TASK-MEMORY-111: member_id (uuid) ok; failure_reason hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id across all 6 steps + compensation; visible in each module's audit chain.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip compensation on failure per DEC-1884 — must execute completed_steps in reverse.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip steps per DEC-1880 — ordering matters (chat needs auth user).</li></ol>\n<ol><li><strong>MUST NOT</strong> double-onboard same member (UNIQUE on member_id).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why saga pattern (DEC-1880)?</strong> Cross-module orchestration without 2PC; each step transactional + compensable.</p>\n<p><strong>Why fixed order (DEC-1880)?</strong> Dependencies: chat needs auth user; learn needs member profile; rew needs contract type. Order is contract.</p>\n<p><strong>Why compensation (DEC-1884)?</strong> Partial state = worse than no state; rollback ensures clean retry.</p>\n<p><strong>Why idempotency (DEC-1880)?</strong> Saga retries on transient failure; double-execution must be safe.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample saga state:</p>\n<pre><code class=\"language-json\">{\n  \"saga_id\": \"uuid\",\n  \"member_id\": \"uuid\",\n  \"current_step\": \"kb_grant_scope\",\n  \"completed_steps\": [\"auth_provision\", \"time_init\", \"learn_assign_starter\"],\n  \"status\": \"in_progress\",\n  \"started_at\": \"2026-05-17T10:00:00Z\"\n}</code></pre>\n<p>Failure state:</p>\n<pre><code class=\"language-json\">{\n  \"saga_id\": \"uuid\",\n  \"current_step\": \"rew_init_baseline\",\n  \"completed_steps\": [\"auth_provision\", \"time_init\", \"learn_assign_starter\", \"kb_grant_scope\", \"chat_create_channel\"],\n  \"status\": \"compensating\",\n  \"failed_step\": \"rew_init_baseline\",\n  \"failure_reason\": \"REW partner API timeout\",\n  \"compensation_log\": [{\"step\": \"chat_create_channel\", \"compensated_at\": \"...\"}]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>6-step enum + cardinality test</strong>. 2. <strong>6-status enum + cardinality test</strong>. 3. <strong>Triggered on member.status → active</strong>. 4. <strong>Steps execute in order</strong>. 5. <strong>Idempotent steps</strong>. 6. <strong>Compensation on failure (reverse order)</strong>. 7. <strong>UNIQUE on member_id</strong>. 8. <strong>6 memory audit kinds emitted</strong>. 9. <strong>PII scrubbed (failure_reason SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>Trace_id preserved across modules</strong>. 12. <strong>CHRO-only manual trigger/retry/compensate</strong>. 13. <strong>Append-only via REVOKE except status cols</strong>. 14. <strong>Retry resumes from failed step</strong>. 15. <strong>Compensation log JSONB tracks each reverse op</strong>. 16. <strong>Saga state queryable</strong>. 17. <strong>Contract type required (else error)</strong>. 18. <strong>Saga timeout 30min (sev-1 + compensate)</strong>. 19. <strong>Concurrent triggers UNIQUE-rejected</strong>. 20. <strong>All 6 modules return success on completion</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn full_flow_completes() {\n    let ctx = TestContext::with_new_member_and_contract().await;\n    ctx.activate_member(ctx.member_id).await;\n    let saga = ctx.wait_for_saga_complete(ctx.member_id).await;\n    assert_eq!(saga.status, \"completed\");\n    assert_eq!(saga.completed_steps.len(), 6);\n}\n\n#[tokio::test]\nasync fn step_failure_triggers_compensation() {\n    let ctx = TestContext::with_new_member_rew_will_fail().await;\n    ctx.activate_member(ctx.member_id).await;\n    let saga = ctx.wait_for_saga_state(ctx.member_id, \"compensated\").await;\n    let auth_status = ctx.fetch_auth(ctx.member_id).await;\n    assert!(auth_status.is_none());  // compensated back to nothing\n}\n\n#[tokio::test]\nasync fn idempotent_double_trigger() {\n    let ctx = TestContext::with_new_member().await;\n    ctx.activate_member(ctx.member_id).await;\n    let r = ctx.try_activate_again(ctx.member_id).await;\n    let sagas = ctx.fetch_sagas(ctx.member_id).await;\n    assert_eq!(sagas.len(), 1);\n}\n\n#[tokio::test]\nasync fn trace_id_propagated() {\n    let ctx = TestContext::with_traceable_activation().await;\n    ctx.activate_member(ctx.member_id).await;\n    let saga = ctx.wait_complete(ctx.member_id).await;\n    let auth_audit = ctx.fetch_memory_audit(\"auth.user_created\", saga.member_id).await;\n    assert_eq!(auth_audit.trace_id, saga.trace_id);\n}\n\n// 5.5..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-HR-001. <strong>Cross-module:</strong> TASK-AUTH-101 (provision), TASK-TIME-001 (init), TASK-LEARN-001 (starter pack), TASK-KB-001 (scope grant), TASK-CHAT-005 (channel), TASK-REW-001 (comp baseline), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Step API timeout</td><td>retry 3x</td><td>failed; sev-1; compensate</td><td>manual retry</td></tr><tr><td>Step idempotency violated</td><td>step handler</td><td>sev-1; investigate</td><td>bug fix</td></tr><tr><td>Compensation step fails</td><td>sev-1 alert</td><td>partial compensation logged</td><td>manual intervention</td></tr><tr><td>Saga timeout 30min</td><td>cron check</td><td>sev-1; compensate</td><td>inherent</td></tr><tr><td>Concurrent activation</td><td>UNIQUE</td><td>second skipped</td><td>inherent</td></tr><tr><td>Member already onboarded</td><td>UNIQUE</td><td>409</td><td>use retry endpoint</td></tr><tr><td>Contract type missing</td><td>early validate</td><td>400</td><td>set contract first</td></tr><tr><td>Cross-tenant trigger</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>Module unavailable</td><td>sev-2; retry</td><td>pause saga</td><td>inherent</td></tr><tr><td>Compensation log corruption</td><td>sev-1 audit</td><td>manual review</td><td>bug fix</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Each step is async fn returning Result; step handler module groups all 12 (6 do + 6 undo).</li><li>§11.2 Orchestrator state machine: pending → in_progress → (completed | failed → compensating → compensated).</li><li>§11.3 Trace_id from saga propagates to each module's audit row (cross-module observability).</li><li>§11.4 Saga timeout cron checks for status='in_progress' AND started_at &lt; now() - 30min.</li><li>§11.5 memory audit body: saga_id, member_id, step, completed_count; failure_reason SHA256.</li></ul>\n<hr>\n<p><em>End of TASK-HR-007 spec.</em></p>";
