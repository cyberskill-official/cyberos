window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-098"]="<h1>TASK-IMP-098: coverage-scope helper maps a task diff to per-file coverage</h1>\n<h2>Summary</h2>\n<p>The coverage gate's unit of judgment is \"files this task touched\", but run-gates emits whole-workspace numbers and every gate so far mapped diff to coverage by hand. Ship <code>coverage-scope.mjs</code>: give it a task id (or an explicit base ref) and a coverage report, get back the touched-file list joined to per-file percentages, emitted as a coverage-gate@1 skeleton ready for the author skill to complete.</p>\n<h2>Problem</h2>\n<p>Manual mapping is exactly the fatigue-prone mechanical work the machine-floor doctrine (TASK-IMP-084) moves into tools: reading a diff range, filtering to source files, and cross-walking a coverage report is deterministic - a human or model doing it fresh each gate re-derives bytes a script can own.</p>\n<h2>Proposed Solution</h2>\n<p><code>node .cyberos/docs-tools/coverage-scope.mjs &lt;task-id&gt; [--base &lt;ref&gt;] [--coverage &lt;file&gt;] [--repo &lt;root&gt;]</code>. Base resolution: <code>--base</code> wins; otherwise find the entry-flip commit whose subject names the task id entering implementing (the corpus convention every batch commit follows), else fail with a clear message demanding --base. Touched files: <code>git diff --name-only &lt;base&gt;...HEAD</code> filtered to tracked source files. Coverage ingestion: c8/istanbul <code>coverage-summary.json</code> and <code>lcov.info</code> - the two shapes the detected stacks emit; anything else is refused by name. Output: a coverage-gate@1 skeleton (frontmatter with computed files_below_90pct, the per-file table, TODO markers for the judgment fields) to stdout or --out. Node stdlib only; build.sh vendors it with the guarded-copy pattern.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Teach run-gates.sh to scope. Rejected: run-gates runs the repo's own commands verbatim by design (TASK-CUO-207); scoping is a reporting concern layered on top.</li><li>Parse every coverage format. Rejected: the two named shapes cover the detected stacks; refusing others loudly beats guessing quietly.</li><li>Fold into ship-manifest.mjs. Rejected: manifests track run state; this reads git + a report and writes a doc skeleton - different lifecycle, own tool.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: against a fixture repo with a known diff and report, the emitted skeleton's touched list, per-file rows, and files_below_90pct match the fixture's expected bytes - suite-asserted every run. Baseline: fully manual mapping. Deadline: final acceptance.</li><li>Guardrail: run against the shipped sachviet batch reproduces the recorded per-file tables (ops-verified, recorded in the gate log - consumer-repo evidence the suite cannot carry).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the CLI, base resolution, the two report shapes, the skeleton emitter, fixture suite, build.sh vendor line.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Running the coverage command itself (run-gates' job).</li><li>Judgment fields of coverage-gate@1 (ecm_rows_uncovered mapping stays with the author skill).</li><li>Other coverage formats until a detected stack emits them.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>Shares tools/install/build.sh with TASK-IMP-093 - same agent, serial order per the batch plan.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMP-14 and the recorded sachviet gates; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-4 PLAN approved 2026-07-17 (§0a); both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The CLI MUST resolve the diff base as: explicit <code>--base</code> first; else the commit whose subject names <code>&lt;task-id&gt;</code> entering implementing; else fail loudly demanding --base (never guess a range).</li><li>1.2 Touched files MUST come from <code>git diff --name-only &lt;base&gt;...HEAD</code>, filtered to files existing at HEAD (deletions excluded from the coverage table but listed in the skeleton's notes).</li><li>1.3 The tool MUST ingest c8/istanbul coverage-summary.json and lcov.info, and MUST refuse any other input by name with a non-zero exit.</li><li>1.4 Output MUST be a coverage-gate@1 skeleton: frontmatter (tests_failed left TODO, files_below_90pct computed from the 90 threshold, ecm_rows_uncovered TODO), per-file table for every touched source file with its percentage or <code>no-coverage-data</code>, and the base/HEAD range recorded.</li><li>1.5 build.sh MUST vendor the tool (guarded copy) and the suite MUST gate the payload copy against a scratch build.</li><li>1.6 The suite MUST land at tools/install/tests/test_coverage_scope.sh (run_all glob discovery).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - base resolution: --base wins; subject-scan finds the flip commit; no-match fails loudly - test: <code>tools/install/tests/test_coverage_scope.sh::t01_base_resolution</code></li><li>[ ] AC 2 (traces_to: #1.2, #1.3, #1.4) - fixture repo + both report shapes -&gt; skeleton matches expected bytes incl. files_below_90pct and the deletion note - test: <code>tools/install/tests/test_coverage_scope.sh::t02_skeleton_from_fixture</code></li><li>[ ] AC 3 (traces_to: #1.3) - unknown report shape refused by name - test: <code>tools/install/tests/test_coverage_scope.sh::t03_unknown_report_refused</code></li><li>[ ] AC 4 (traces_to: #1.5, #1.6) - scratch payload carries the tool; suite glob-discovered - test: <code>tools/install/tests/test_coverage_scope.sh::t04_payload_vendored</code></li><li>[ ] AC 5 (guardrail) - sachviet batch-1 per-file tables reproduced - verify: recorded run output in the gate log (consumer-repo evidence; the fixture suite carries the automated half).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Touched file absent from the coverage report (doc, config, or untested source): row emitted with <code>no-coverage-data</code> - visible, never silently dropped (t02 asserts one such row).</li><li>File deleted in the range: excluded from the table, named in the notes (1.2, t02).</li><li>Multiple commits name the task id: the earliest implementing-entry match wins; ambiguity is reported in the skeleton's range note (t01 arm).</li><li>Percentages exactly at 90: not below threshold (strict less-than, matching the gate's wording \"below 90\").</li><li>Security-class: read-only over git and a named report file; no network; refuses paths outside the repo root.</li></ul>";
