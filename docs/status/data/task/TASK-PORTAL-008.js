window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PORTAL-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship DSAR self-service at <code>services/portal/src/dsar/</code> for GDPR Art. 15 + PDPL Art. 17 with 5 closed-enum request types, async processing via TASK-MCP-007 Tasks, bundle delivery with passphrase encryption, identity verification, 90-day per-subject rate limit, denial workflow with CFO+CLO sign-off, and 7 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>dsar_type</code> enum: <code>('access_report','portability_export','deletion_request','restriction_request','rectification_request')</code> per DEC-1221. CI cardinality test asserts 5.</li></ol>\n<ol><li><strong>MUST</strong> define closed <code>dsar_status</code> enum: <code>('received','identity_verification_pending','processing','ready_for_review','delivered','denied','expired')</code> per DEC-1222. CI cardinality test asserts 7.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_dsar_requests</code> table at migration <code>0016</code> with columns for request_id, tenant_id, requester_subject_id, dsar_type, status, requested_at, identity_verified_at, processing_started_at, ready_at, delivered_at, expires_at (30d post-delivery), bundle_s3_key, bundle_passphrase_kms_blob, denial_id (FK), trace_id. Append-only via REVOKE.</li></ol>\n<ol><li><strong>MUST</strong> define <code>portal_dsar_denials</code> at migration <code>0017</code> with denial reason (closed enum: <code>identity_unverifiable|contradicts_law_compliance|third_party_rights_conflict|manifestly_unfounded</code>), CFO + CLO signatures (both <code>subject_id</code> + <code>signed_at</code>), free-text justification. CHECK: both signatures required for status='denied'.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS scoped to <code>tenant_id AND requester_subject_id = current_setting('auth.subject_id')::uuid</code> (caller sees own DSARs only; tenant_admin via separate handler).</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/portal/dsar/request</code> body <code>{ dsar_type, scope_description?, passphrase_hash? }</code>. Handler:</li></ol>\n<ul><li>Validates dsar_type in closed enum.</li><li>Rate-limit check per DEC-1228 (one per 90 days per subject).</li><li>SSO-authenticated caller → status='processing' immediately per DEC-1223.</li><li>Email-password caller → status='identity_verification_pending' + email confirmation link via TASK-EMAIL-001.</li><li>Enqueues processing via TASK-MCP-007 task per DEC-1224.</li><li>Emit <code>portal.dsar_received</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> processor builds bundle per DEC-1225. Aggregates from PROJ/INV/DOC/CHAT/audit:</li></ol>\n<ul><li>Per-source JSON dump (all rows where the caller is owner/assignee/author/mentioned).</li><li>Per-source CSV for human readability.</li><li>Audit history JSON (events ABOUT the caller; never about other subjects per DEC-1229).</li><li>Signed manifest with SHA-256 + counts per source.</li><li>ZIPs everything; encrypts with age (caller-provided passphrase).</li><li>Uploads to S3 with 7-day TTL signed URL.</li><li>Transitions status='ready_for_review'.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/portal/dsar/{id}</code> for status polling. Returns current state. On <code>status='delivered'</code>: includes signed_url + manifest_sha256.</li></ol>\n<ol><li><strong>MUST</strong> support denial workflow per DEC-1230. <code>POST /v1/admin/dsar/{id}/deny</code> requires <code>cfo</code> + <code>clo</code> roles (dual-signature). Body <code>{ reason, justification }</code>. Both signatures populate denial row.</li></ol>\n<ol><li><strong>MUST</strong> support deletion via TASK-TEN-104 or TASK-AUTH-002 hard-purge per DEC-1227. <code>dsar_type='deletion_request'</code> triggers internal review queue (sev-1) before destructive action; operator manually invokes hard-purge after legal sign-off.</li></ol>\n<ol><li><strong>MUST</strong> rate-limit per DEC-1228 — second DSAR within 90 days returns 429.</li></ol>\n<ol><li><strong>MUST</strong> emit 7 memory audit kinds per DEC-1231: received, identity_verified, processing_started, ready_for_review, delivered, denied, expired — all sev-1 (regulatory-critical).</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub per task-audit skill rule 18 — requester_subject_id UUID retained; email hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST</strong> expire bundle at T+7 days post-delivery (signed URL + S3 object).</li></ol>\n<ol><li><strong>MUST NOT</strong> include audit rows about OTHER subjects per DEC-1229.</li></ol>\n<ol><li><strong>MUST NOT</strong> include audit rows that would leak third-party PII (e.g., a comment by another user about the requester — show the comment but redact the other user's identifying details).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why async via Tasks primitive (§1 #6, DEC-1224)?</strong> Bundle assembly = cross-table scan + cross-tenant filter + ZIP encryption — easily minutes to hours for active subjects. Sync processing = 30s gateway timeout; async via TASK-MCP-007 fits exactly. Reuses existing infrastructure.</p>\n<p><strong>Why passphrase encryption (§1 #7, DEC-1225)?</strong> S3 signed URL alone = anyone with URL access reads bundle. Passphrase = end-to-end encryption to the caller; even if URL leaks, encrypted bundle unreadable.</p>\n<p><strong>Why dual-sign denial (§1 #9, DEC-1230)?</strong> Denying a DSAR is legally significant — GDPR Art. 12 requires \"without undue delay and at the latest within one month\". Wrongful denial = fine. CFO + CLO dual-sign forces review.</p>\n<p><strong>Why 90-day rate limit (§1 #11, DEC-1228)?</strong> GDPR Art. 12(5) allows charging fees for \"manifestly unfounded or excessive\" requests including those repeated. 90 days = quarterly cadence reasonable; second request within 90d = excessive presumption.</p>\n<p><strong>Why exclude cross-subject audit (§1 #16, DEC-1229)?</strong> A requester's DSAR including audit rows about OTHER subjects = data leak. Filter to events WHERE actor=requester OR target=requester (direct involvement only).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0016_portal_dsar_requests.sql\nCREATE TYPE dsar_type AS ENUM ('access_report','portability_export','deletion_request','restriction_request','rectification_request');\nCREATE TYPE dsar_status AS ENUM ('received','identity_verification_pending','processing','ready_for_review','delivered','denied','expired');\n\nCREATE TABLE portal_dsar_requests (\n  request_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  requester_subject_id UUID NOT NULL,\n  dsar_type dsar_type NOT NULL,\n  status dsar_status NOT NULL DEFAULT 'received',\n  scope_description TEXT,\n  passphrase_hash CHAR(64),\n  requested_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  identity_verified_at TIMESTAMPTZ,\n  processing_started_at TIMESTAMPTZ,\n  ready_at TIMESTAMPTZ,\n  delivered_at TIMESTAMPTZ,\n  expires_at TIMESTAMPTZ,\n  bundle_s3_key TEXT,\n  bundle_passphrase_kms_blob BYTEA,\n  denial_id BIGINT,\n  trace_id CHAR(32)\n);\n-- Partial unique on active DSAR; 90d window enforced by daily prune job per §11.10\nCREATE UNIQUE INDEX uniq_dsar_active_per_subject\n  ON portal_dsar_requests(requester_subject_id)\n  WHERE status NOT IN ('delivered','denied','expired');\nALTER TABLE portal_dsar_requests ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_dsar_requests_rls ON portal_dsar_requests\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND requester_subject_id = current_setting('auth.subject_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND requester_subject_id = current_setting('auth.subject_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_dsar_requests FROM cyberos_app;\nGRANT UPDATE (status, identity_verified_at, processing_started_at, ready_at, delivered_at,\n              expires_at, bundle_s3_key, bundle_passphrase_kms_blob, denial_id)\n  ON portal_dsar_requests TO cyberos_app;\n\n-- 0017_portal_dsar_denials.sql\nCREATE TYPE dsar_denial_reason AS ENUM ('identity_unverifiable','contradicts_law_compliance','third_party_rights_conflict','manifestly_unfounded');\nCREATE TABLE portal_dsar_denials (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  request_id UUID NOT NULL REFERENCES portal_dsar_requests(request_id),\n  reason dsar_denial_reason NOT NULL,\n  justification TEXT NOT NULL,\n  cfo_subject_id UUID NOT NULL,\n  cfo_signed_at TIMESTAMPTZ NOT NULL,\n  clo_subject_id UUID NOT NULL,\n  clo_signed_at TIMESTAMPTZ NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  CHECK (cfo_subject_id != clo_subject_id)  -- two distinct people\n);\nALTER TABLE portal_dsar_denials ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_dsar_denials_rls ON portal_dsar_denials\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_dsar_denials FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/portal/dsar/request                       (caller)\nGET    /v1/portal/dsar/{id}                           (caller polling)\nGET    /v1/admin/tenants/{tid}/dsar                   (tenant_admin list)\nPOST   /v1/admin/dsar/{id}/deny                       (cfo + clo dual-sign)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>dsar_type cardinality</strong> — 5 values exactly.</li><li><strong>dsar_status cardinality</strong> — 7 values exactly.</li><li><strong>SSO caller skips identity verify</strong> — IdP-auth caller goes straight to status='processing'.</li><li><strong>Email-password caller requires verify</strong> — non-SSO caller gets identity_verification_pending + email link.</li><li><strong>Bundle contents</strong> — bundle includes caller's own PROJ/INV/DOC/CHAT rows + audit history.</li><li><strong>Cross-subject audit excluded</strong> — bundle audit history excludes events about other subjects.</li><li><strong>90-day rate limit</strong> — second DSAR within window → 429.</li><li><strong>Denial dual-signature</strong> — denial without both CFO + CLO signatures → 400.</li><li><strong>Deletion request triggers review</strong> — <code>deletion_request</code> type creates sev-1 internal review queue.</li><li><strong>30-day SLA tracked</strong> — daily job flags DSARs &gt; 25d in non-terminal status (5-day warning); &gt; 30d alerts sev-1.</li><li><strong>Bundle expires at 7d</strong> — signed URL + S3 object removed.</li><li><strong>Passphrase encryption</strong> — bundle ZIP encrypted with age + caller passphrase.</li><li><strong>Manifest signature</strong> — included; verifiable.</li><li><strong>7 memory audit kinds emitted</strong> — all 7 lifecycle events emit.</li><li><strong>PII scrubbed in audit</strong> — email_hash16 only in chain.</li><li><strong>Cross-tenant denied</strong> — caller from tenant X can't see tenant Y DSARs.</li><li><strong>Trace_id threaded end-to-end</strong>.</li><li><strong>Tenant_admin list</strong> — separate endpoint, tenant_admin role required.</li><li><strong>Partial-unique index 90d</strong> — second active DSAR rejected at INSERT.</li><li><strong>Distinct CFO/CLO subjects</strong> — CHECK constraint prevents same person dual-signing.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// 5.1 dsar_create_test\n#[tokio::test]\nasync fn sso_caller_skips_identity_verify() {\n    let ctx = TestContext::with_sso_subject().await;\n    let r = ctx.post_dsar(\"access_report\", None).await;\n    assert_eq!(r.status(), 201);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"status\"], \"processing\");\n}\n\n// 5.2 cross-subject excluded\n#[tokio::test]\nasync fn bundle_excludes_other_subjects_audit() {\n    let ctx = TestContext::new().await;\n    ctx.seed_audit_for_subject(ctx.subject_a, \"event-a\").await;\n    ctx.seed_audit_for_subject(ctx.subject_b, \"event-b\").await;\n    let dsar_id = ctx.as_subject_a().post_dsar(\"access_report\", Some(\"pass\")).await;\n    ctx.await_dsar_ready(dsar_id).await;\n    let bundle = ctx.download_dsar_bundle(dsar_id, \"pass\").await;\n    let audit = bundle.read_json(\"audit/history.json\").await;\n    assert!(audit.iter().any(|e| e[\"kind\"] == \"event-a\"));\n    assert!(!audit.iter().any(|e| e[\"kind\"] == \"event-b\"));\n}\n\n// 5.3 rate limit 90d\n#[tokio::test]\nasync fn second_dsar_within_90d_blocked() {\n    let ctx = TestContext::new().await;\n    ctx.post_dsar(\"access_report\", None).await;\n    let r = ctx.post_dsar(\"access_report\", None).await;\n    assert_eq!(r.status(), 429);\n}\n\n// 5.4 denial dual-sign\n#[tokio::test]\nasync fn denial_requires_cfo_and_clo() {\n    let ctx = TestContext::new().await;\n    let dsar = ctx.post_dsar(\"access_report\", None).await;\n    let r = ctx.as_cfo().post_deny(dsar, \"identity_unverifiable\", \"no docs\").await;\n    assert_eq!(r.status(), 400);  // only one signature\n    let r2 = ctx.as_cfo_and_clo().post_deny(dsar, \"identity_unverifiable\", \"no docs\").await;\n    assert_eq!(r2.status(), 200);\n}\n\n// 5.5 type enum cardinality\n#[tokio::test]\nasync fn dsar_type_has_5_values() {\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\"SELECT unnest(enum_range(NULL::dsar_type))::text\").fetch_all(&amp;ctx.pool).await.unwrap();\n    assert_eq!(labels.len(), 5);\n}\n\n// 5.6 status enum cardinality\n// 5.7 30d SLA flag\n// 5.8 bundle passphrase encryption\n// 5.9 cross-tenant deny\n// 5.10 audit emission</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-PORTAL-001 (data sources to aggregate). <strong>Cross-module:</strong> TASK-PORTAL-003 (SSO identity), TASK-PORTAL-004 (subject lifecycle), TASK-MCP-007 (Tasks for async bundle), TASK-EMAIL-001 (verification + delivery emails), TASK-DOC-001 (S3 bundle storage), TASK-AUTH-101 (cfo + clo roles), TASK-AI-003 (audit kinds), TASK-MEMORY-111 (PII scrub), TASK-OBS-007 (sev-1 SLA alarms). <strong>Downstream:</strong> None.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>portal.dsar_delivered</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.dsar_delivered\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"trace_id\": \"...\",\n  \"occurred_at\": \"2026-06-15T...\",\n  \"payload\": {\n    \"request_id\": \"0190...\",\n    \"requester_subject_id_hash16\": \"f8a1...\",\n    \"dsar_type\": \"access_report\",\n    \"bundle_size_bytes\": 4823104,\n    \"expires_at\": \"2026-06-22T...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multilingual narrative (\"what we collected\") — slice 3.</li><li><strong>Deferred:</strong> Granular consent withdrawal per data-category — slice 3.</li><li><strong>Deferred:</strong> Automated identity verification via government-ID OCR — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Identity verification email fails</td><td>TASK-EMAIL-001 error</td><td>Status stays pending; daily reminder + 30d expiry</td><td>Caller requests resend</td></tr><tr><td>Bundle assembly task fails</td><td>TASK-MCP-007 task failed</td><td>Sev-1 alert; status='received' again; operator investigates</td><td>Manual retry</td></tr><tr><td>30-day SLA missed</td><td>daily SLA check</td><td>Sev-1 alert + clo notification</td><td>Operator escalates</td></tr><tr><td>Bundle download URL leaked</td><td>inherent risk; passphrase mitigates</td><td>Encrypted bundle unreadable without passphrase</td><td>Inherent E2EE</td></tr><tr><td>Deletion request without legal sign-off</td><td>sev-1 review queue</td><td>Operator+CLO review before hard-purge</td><td>Manual review process</td></tr><tr><td>Cross-tenant DSAR submission</td><td>RLS rejects</td><td>403</td><td>Caller's own tenant only</td></tr><tr><td>Same person CFO+CLO dual-sign</td><td>CHECK constraint</td><td>INSERT fails</td><td>Different signatories required</td></tr><tr><td>Bundle expired before download</td><td>scheduled cleanup at T+7d</td><td>404 on signed URL; caller requests new DSAR</td><td>DSAR re-submission</td></tr><tr><td>Subject hard-purged mid-DSAR-processing</td><td>task failure on missing data</td><td>Sev-1; partial bundle delivered with note</td><td>Inherent edge case</td></tr><tr><td>Rate-limit hit at 90d boundary</td><td>counter</td><td>429</td><td>Caller waits</td></tr><tr><td>Passphrase forgotten</td><td>inherent</td><td>Bundle unrecoverable; new DSAR required</td><td>E2EE design tradeoff</td></tr><tr><td>Manifest signature verification fails</td><td>client-side check</td><td>Caller notified; sev-2 alert</td><td>Re-deliver</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Bundle ZIP uses <code>zip</code> crate; encryption via <code>age</code> crate; passphrase = scrypt-derived from caller input.</p>\n<p><strong>§11.2</strong> Manifest signed with per-tenant Ed25519 key from KMS; client verifies with tenant's public key (published).</p>\n<p><strong>§11.3</strong> Task processor uses TASK-MCP-007 worker pool; per-module=<code>portal</code>; max_concurrent=2 (heavy I/O).</p>\n<p><strong>§11.4</strong> Email delivery via TASK-EMAIL-001 with priority=high; identity-verify email has 24h link expiry.</p>\n<p><strong>§11.5</strong> Audit cross-subject filter SQL: <code>WHERE actor_subject_id = $1 OR resource_subject_id = $1</code> (only direct involvement).</p>\n<p><strong>§11.6</strong> 30-day SLA tracked via daily job comparing requested_at + 25/30 days; sev-2 at warning, sev-1 at breach.</p>\n<p><strong>§11.7</strong> S3 lifecycle policy auto-deletes bundle at T+7d; double-protection vs application-side cleanup.</p>\n<p><strong>§11.8</strong> Deletion-request review queue posts a Slack/CHAT notification to legal team (out-of-band).</p>\n<p><strong>§11.9</strong> Per-tenant bundle key prefix <code>dsar/{tenant_id}/{request_id}/bundle.zip.age</code> for forensic clarity.</p>\n<p><strong>§11.10</strong> Rate-limit partial-unique index requires periodic prune (90d window enforcement via daily job).</p>\n<hr>\n<p><em>End of TASK-PORTAL-008 spec.</em></p>";
