window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-102"]="<h1>TASK-IMP-102: Audits bind the normative half, not bytes the workflow rewrites</h1>\n<h2>Summary</h2>\n<p>An audit's byte-binding is supposed to answer one question: does this audit describe the spec on disk? Today it cannot. <code>audited_file_sha256</code> covers the whole file including <code>status</code> and <code>shipped</code> - fields ship-tasks rewrites at every phase - and authoring hashes the spec before flipping status, so the audited bytes never reach a commit. Add <code>audited_body_sha256_prefix</code> over the NORMATIVE half (body + frontmatter minus the lifecycle-mutable fields), teach task-reconcile to prefer it, and keep the legacy field accepted so the existing corpus stays readable.</p>\n<h2>Problem</h2>\n<p>TASK-IMP-100's first live run flagged TASK-IMP-092 - shipped correctly through both human gates - as drifted. The instrument was right about the evidence and wrong about the conclusion, because the evidence it was handed cannot mean what it claims. A hash that no commit carries is the 086 class: a claim about bytes nobody can check. Every audit in the corpus carries one.</p>\n<h2>Proposed Solution</h2>\n<p>Define the normative half explicitly: the spec body plus its frontmatter minus <code>status</code>, <code>shipped</code>, <code>routed_back_count</code>, <code>memory_chain_hash</code> - everything the audit actually judged, and nothing the workflow rewrites afterwards. task-audit's contract gains <code>audited_body_sha256_prefix</code> (16 hex) alongside the retained <code>audited_file_sha256_prefix</code>, with <code>re_entrancy</code> and <code>fixity_notes</code> re-stated against the body hash - the property that is true. task-reconcile's R1 prefers the body field when present (a direct, commit-independent comparison), falls back to today's audit-commit reconstruction for legacy audits, and stops reporting a binding gap when the body field answers the question.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Hash after the status flip. Rejected: it makes the whole-file hash true for exactly one moment, and the next phase flip breaks it again - the field would still be unverifiable for every task past its first transition.</li><li>Move status out of the spec into a sidecar. Rejected: frontmatter status is the record of truth (STATUS-REFERENCE §1); relocating it to protect a hash inverts the priority.</li><li>Rewrite the corpus's audits to carry the new field. Rejected: historical audits describe historical specs; the legacy path reads them correctly and marks them legacy - honest and cheap.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: an audit carrying the body field verifies its binding directly - a lifecycle flip leaves R1 green with no binding-gap note, and a clause edit reds it - suite-asserted every run. Baseline: 100 % of audits carry an unverifiable whole-file hash. Deadline: final acceptance.</li><li>Guardrail: legacy audits (no body field) still resolve via the audit-commit path with the gap named - the existing corpus does not become unreadable.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the task-audit contract fields, the normative-half definition, task-reconcile R1's preference order, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Backfilling the field into existing audits (legacy path handles them).</li><li>task-lint enforcement of the new field (a rubric change; separate decision).</li><li>Any change to what an audit judges - only what it records about what it judged.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>depends_on TASK-IMP-100 (R1 is the consumer). Same batch, serial - and per the new depends_on evidence gate, 100's coverage-gate artefact is its evidence.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from TASK-IMP-100's dogfood finding; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> the fix-now decision is the operator's recorded 2026-07-17 answer; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 task-audit's contract MUST define the normative half as the spec body plus frontmatter minus <code>status</code>, <code>shipped</code>, <code>routed_back_count</code>, <code>memory_chain_hash</code>, and MUST record <code>audited_body_sha256_prefix</code> (16 hex) over it.</li><li>1.2 <code>audited_file_sha256_prefix</code> MUST be retained (provenance of the exact bytes seen), and the contract MUST state that it is NOT a verifiable binding after any lifecycle flip - the body field is the binding.</li><li>1.3 <code>re_entrancy</code> and <code>fixity_notes</code> MUST be re-stated against the body hash, so the skill claims only what holds.</li><li>1.4 task-reconcile R1 MUST prefer <code>audited_body_sha256_prefix</code> when present: match -&gt; pass with no binding-gap note; mismatch -&gt; red naming normative drift. Absent -&gt; today's audit-commit path, gap noted as legacy.</li><li>1.5 Suite arms MUST cover: body field present + lifecycle flip -&gt; pass, no gap note; body field present + clause edit -&gt; red; body field absent (legacy) -&gt; the audit-commit path with the gap named.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.4, #1.5) - body field + lifecycle flip -&gt; R1 pass, zero binding-gap note - test: <code>tools/install/tests/test_task_reconcile.sh::t06_body_binding_preferred</code></li><li>[ ] AC 2 (traces_to: #1.4, #1.5) - body field + normative edit -&gt; R1 red, drift named - test: <code>tools/install/tests/test_task_reconcile.sh::t06_body_binding_preferred (drift arm)</code></li><li>[ ] AC 3 (traces_to: #1.4, #1.5) - legacy audit (no body field) -&gt; audit-commit path, gap named as legacy - test: <code>tools/install/tests/test_task_reconcile.sh::t06_body_binding_preferred (legacy arm)</code></li><li>[ ] AC 4 (traces_to: #1.1, #1.2, #1.3) - the contract records the body field, retains the file field with its caveat, and re-states re_entrancy/fixity against the body hash - verify: recorded greps in the gate log (prose contract; same rationale as TASK-IMP-090 AC 1).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Audit carrying BOTH fields where the file field matches too (audit written post-flip): body field decides; no gap note - the file field is provenance, not a gate (AC 1's shape).</li><li>Spec whose frontmatter lacks a lifecycle field entirely: the normalizer drops what is present and hashes the rest - absence is not drift (covered by the normalizer's field-list semantics, exercised in t06).</li><li>A future lifecycle field added to the template: the normalizer's list is the single place to extend; the contract names it so the next author knows where to look.</li><li>Legacy audit whose commit is unreachable (shallow clone): the existing \"binding unverifiable\" note stands - a note, never a verdict (t04's existing arm).</li><li>Security-class: none - hashing and prose; no execution surface.</li></ul>";
