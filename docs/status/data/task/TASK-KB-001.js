window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-KB-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The KB service <strong>MUST</strong> ship the Document schema as the canonical structured-knowledge surface for the tenant. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>documents</code> with: <code>id UUID PRIMARY KEY</code>, <code>tenant_id UUID NOT NULL</code>, <code>slug TEXT NOT NULL</code>, <code>language language_code NOT NULL</code>, <code>title TEXT NOT NULL</code>, <code>category document_category NOT NULL</code>, <code>permission document_permission NOT NULL</code>, <code>allowed_role_codes TEXT[] NOT NULL DEFAULT '{}'</code>, <code>current_version_id UUID NOT NULL REFERENCES document_versions(id) DEFERRABLE INITIALLY IMMEDIATE</code>, <code>translation_of UUID REFERENCES documents(id)</code>, <code>status document_status NOT NULL DEFAULT 'active'</code>, <code>owner_subject_id UUID REFERENCES auth.subjects(id)</code>, <code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>updated_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>. Full DDL in §3.1.</li></ol>\n<ol><li><strong>MUST</strong> define <code>document_versions</code> with: <code>id UUID PRIMARY KEY</code>, <code>document_id UUID NOT NULL REFERENCES documents(id) ON DELETE RESTRICT</code>, <code>tenant_id UUID NOT NULL</code>, <code>version_number INT NOT NULL</code>, <code>markdown_body TEXT NOT NULL CHECK (length(markdown_body) BETWEEN 1 AND 500000)</code>, <code>frontmatter_yaml TEXT NOT NULL</code>, <code>body_sha256 CHAR(64) NOT NULL</code>, <code>created_at TIMESTAMPTZ NOT NULL DEFAULT now()</code>, <code>created_by_subject_id UUID NOT NULL REFERENCES auth.subjects(id)</code>, <code>change_summary TEXT CHECK (change_summary IS NULL OR length(change_summary) BETWEEN 1 AND 500)</code>.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>document_category</code> Postgres enum with exactly 5 values (per DEC-240): <code>'how_to'</code>, <code>'reference'</code>, <code>'decision_log'</code>, <code>'policy'</code>, <code>'runbook'</code>. Adding a 6th category is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>document_permission</code> Postgres enum with exactly 3 values (per DEC-241): <code>'public'</code>, <code>'org_only'</code>, <code>'role_restricted'</code>. Adding a 4th tier is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>language_code</code> Postgres enum with exactly 2 values (per DEC-242): <code>'vi'</code>, <code>'en'</code>. Adding zh/ja/etc. is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>document_status</code> Postgres enum with exactly 3 values: <code>'active'</code>, <code>'archived'</code>, <code>'draft'</code>. <code>draft</code> is the create-state until first publish; <code>active</code> is the normal state; <code>archived</code> removes from default search results but retains rendering.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> clauses on <code>documents</code> AND <code>document_versions</code>. Policy: <code>tenant_id = current_setting('auth.tenant_id')::uuid</code>. Cross-tenant reads return 0 rows; cross-tenant writes fail <code>permission_denied</code>.</li></ol>\n<ol><li><strong>MUST</strong> be <strong>append-only</strong> on <code>document_versions</code> at the SQL-grant layer (per DEC-247 + task-audit skill rule 12). <code>REVOKE UPDATE, DELETE ON document_versions FROM cyberos_app;</code>. Every \"edit\" creates a new version row and updates <code>documents.current_version_id</code> to point at it.</li></ol>\n<ol><li><strong>MUST</strong> enforce <strong>slug uniqueness per (tenant_id, language)</strong> (per DEC-244). <code>CREATE UNIQUE INDEX uniq_doc_slug ON documents (tenant_id, language, slug);</code>. Same slug in <code>vi</code> + <code>en</code> is allowed and indicates translation pairing (verified by translation_of FK).</li></ol>\n<ol><li><strong>MUST</strong> validate <code>translation_of</code> FK at INSERT/UPDATE: target document MUST exist in the same <code>tenant_id</code> AND have a different <code>language</code> (per DEC-245). Same-language self-link → 400 <code>translation_must_cross_language</code>; cross-tenant link → 400 <code>translation_cross_tenant</code>. A <code>BEFORE INSERT OR UPDATE</code> trigger enforces.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>role_restricted</code> permission tier: <code>allowed_role_codes</code> MUST be non-empty AND every element MUST match the TASK-AUTH-101 closed role enum (per DEC-250). Unknown role → 400 <code>unknown_role: &lt;code&gt;</code>. Empty array with permission='role_restricted' → 400 <code>role_restricted_requires_roles</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship the frontmatter validator at <code>services/kb/src/frontmatter.rs</code>. Required keys: <code>title</code>, <code>category</code>, <code>language</code>, <code>permission</code>. Optional keys: <code>tags</code> (string array ≤ 20 elements, each 1–32 chars), <code>owner_subject_id</code> (UUID), <code>translation_of</code> (slug — resolves to document_id at INSERT time), <code>summary</code> (1–200 chars), <code>applicability_tags</code> (object with <code>provider</code>, <code>region</code>, <code>severity</code> — only for <code>category=runbook</code>). Unknown top-level keys → 400 <code>unknown_frontmatter_key: &lt;name&gt;</code>.</li></ol>\n<ol><li><strong>MUST</strong> compute <code>body_sha256</code> server-side on every version write: <code>SHA-256(markdown_body)</code> as 64-char lowercase hex. Used by TASK-KB-002's render-cache invalidation and by ingestion replay. Identical body in successive saves → still a new version row (the operator wanted to record a change for some reason — maybe metadata-only changes).</li></ol>\n<ol><li><strong>MUST</strong> emit memory audit row <code>kb.document_created</code> on document creation (first version). The row carries <code>{document_id, tenant_id, slug, language, category, permission, owner_subject_id_hash16, created_by_subject_id_hash16, body_sha256, version_number, ts_ns}</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit <code>kb.document_versioned</code> on every subsequent version write. Same shape as <code>kb.document_created</code> plus <code>prev_version_id</code> and <code>change_summary</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit <code>kb.document_acl_changed</code> on every permission or allowed_role_codes change. Carries <code>{document_id, old_permission, new_permission, old_allowed_role_codes, new_allowed_role_codes, changed_by_subject_id_hash16, ts_ns}</code>. This is a sev-2 audit (TASK-OBS-007 notification target — ACL changes deserve operator attention).</li></ol>\n<ol><li><strong>MUST</strong> emit <code>kb.document_archived</code> on transition to <code>status='archived'</code>. Carries <code>{document_id, archived_by_subject_id_hash16, reason TEXT, ts_ns}</code>.</li></ol>\n<ol><li><strong>MUST</strong> maintain <code>current_version_id</code> pointer atomically on every version save: in one transaction, INSERT into <code>document_versions</code> AND UPDATE <code>documents.current_version_id</code>. The FK is <code>DEFERRABLE INITIALLY IMMEDIATE</code> so the document row can be UPDATEd with a still-uncommitted version id within the same transaction (Postgres-specific).</li></ol>\n<ol><li><strong>MUST</strong> expose REST handlers:</li></ol>\n<ul><li><code>POST /v1/kb/documents</code> — create document + initial version. Caller <code>Resource::KbDocument + Action::Write</code>.</li><li><code>GET /v1/kb/documents/{id}</code> — return doc + current version. Caller <code>Action::Read</code>; permission tier additionally gates (public always allowed; org_only requires same-tenant subject; role_restricted requires intersection of caller roles with <code>allowed_role_codes</code>).</li><li><code>GET /v1/kb/documents?slug=&lt;&gt;&amp;language=&lt;&gt;&amp;category=&lt;&gt;&amp;status=&lt;&gt;</code> — list with cursor pagination.</li><li><code>POST /v1/kb/documents/{id}/versions</code> — new version (same permission as create).</li><li><code>PATCH /v1/kb/documents/{id}</code> — update metadata (title, permission, allowed_role_codes, owner_subject_id, translation_of); MUST NOT modify markdown_body (use POST versions for body changes).</li><li><code>POST /v1/kb/documents/{id}/archive</code> — transition status to archived; reason required.</li></ul>\n<ol><li><strong>MUST</strong> complete create/get handlers in ≤ 100 ms p95. <code>documents_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> support idempotent creation via <code>Idempotency-Key</code> header (same semantics as TASK-AUTH-002 §1 #6). Repeat POST with same key + same body → return existing document.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>kb.document.{create,get,list,version,archive,patch}</code> with attributes: <code>tenant_id</code>, <code>document_id</code>, <code>slug</code>, <code>category</code>, <code>permission</code>, <code>outcome</code> (success | unknown_category | unknown_role | translation_cross_tenant | translation_same_language | body_too_large | permission_denied | not_found).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>kb_document_create_total{outcome, category, permission}</code> (counter).</li><li><code>kb_document_version_total{outcome}</code> (counter).</li><li><code>kb_document_count{tenant_id, category, status}</code> (gauge).</li><li><code>kb_document_acl_changes_total{from_tier, to_tier}</code> (counter).</li><li><code>kb_document_archives_total{tenant_id}</code> (counter).</li></ul>\n<ol><li><strong>MUST</strong> ship <code>current_documents_view</code> filtering <code>status != 'archived'</code> and <code>active_documents_view</code> filtering <code>status = 'active'</code>. Downstream tasks querying \"what is searchable?\" use <code>active_documents_view</code>; \"what is still resolvable for rendering?\" uses <code>current_documents_view</code>.</li></ol>\n<ol><li><strong>MUST</strong> preserve the SQL function <code>document_version_chain(doc_id UUID) RETURNS SETOF document_versions</code> returning all versions ordered by <code>version_number ASC</code>. Used by TASK-KB-002's \"history\" UI and by Q&amp;A retrieval that may cite an older version.</li></ol>\n<ol><li><strong>MUST</strong> ensure <code>version_number</code> is monotonic per document: a <code>BEFORE INSERT</code> trigger sets <code>NEW.version_number = COALESCE(MAX(version_number) FROM document_versions WHERE document_id = NEW.document_id, 0) + 1</code>. The first version is always 1; gaps are impossible.</li></ol>\n<ol><li><strong>MUST</strong> ship a <code>frontmatter_round_trip_test</code>: parse → emit → parse again produces the same struct. Catches any drift in the YAML round-tripper that would corrupt frontmatter on save.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why immutable versions instead of UPDATE-in-place (DEC-243, DEC-247)?</strong> Every AI answer that cites a KB doc cites a <em>version</em> — \"according to runbook X version 7, the remediation is…\". If the doc is silently edited, the citation becomes false. Immutable versions preserve the citation forever; the audit chain (memory) records every save. The cost is one new row per save (small); the benefit is \"Q&amp;A answers are always defensibly grounded.\"</p>\n<p><strong>Why closed category enum (DEC-240)?</strong> Categories drive downstream behaviour: TASK-KB-008's runbook router filters <code>category='runbook'</code>; TASK-KB-007's \"Ask this page\" weights by category; OBS auto-triage prefers runbook + reference. Free-form categories would mean each consumer needs its own normalisation (drift); the closed 5-value enum is the contract. Adding a 6th is an ADR — forces consideration of cross-consumer impact.</p>\n<p><strong>Why closed permission tier at 3 levels (DEC-241)?</strong> Three tiers cover the natural permission shapes: <code>public</code> (Trust Center pages opt-in for external visibility), <code>org_only</code> (any subject in the tenant), <code>role_restricted</code> (specific TASK-AUTH-101 roles). A 4th tier (e.g. <code>per-subject</code>) would be ABAC creep — the closed enum prevents the slide.</p>\n<p><strong>Why per-tenant per-language slug uniqueness (DEC-244)?</strong> Same slug in <code>vi</code> + <code>en</code> is the natural translation pair indicator — <code>/policy/leave-types</code> exists in both languages with the same slug, and the <code>translation_of</code> FK confirms the pairing. Uniqueness scoped to tenant + language allows both rows to coexist. Without the language scope, the second translation would conflict with the first.</p>\n<p><strong>Why translation_of crosses languages (DEC-245)?</strong> A translation is by definition a cross-language pair. Same-language self-link makes no semantic sense; cross-tenant link would leak content via the FK. The trigger enforces both. The implementation cost is one trigger; the benefit is the translation_of FK is always meaningful.</p>\n<p><strong>Why frontmatter required keys at 4 (<code>title</code>, <code>category</code>, <code>language</code>, <code>permission</code>)?</strong> These are the four columns that the schema also stores; the frontmatter is a YAML view of the same data so editors can author both prose + metadata in one file. Required-key validation at the API boundary catches typos (<code>titlle:</code>) before they hit storage. Optional keys cover author intent (tags, owner) but don't drive schema columns.</p>\n<p><strong>Why <code>applicability_tags</code> only for runbooks (§1 #12)?</strong> Runbooks (TASK-KB-008) need provider/region/severity tags so the OBS auto-router can filter. Other categories don't need them; allowing them everywhere would invite operators to put them on how-tos (unused but noisy). Schema-level optional + category-conditional enforcement keeps the surface focused.</p>\n<p><strong>Why <code>body_sha256</code> server-side computed (§1 #13)?</strong> Two reasons. (1) TASK-KB-002's render cache uses this as the cache key; trusting client-supplied hash would let a misbehaving client invalidate other people's caches. (2) Ingestion replay (TASK-KB-005's BGE-M3 path) hashes the body to detect \"is this version actually different from last embedded?\". The server is the only trusted hasher.</p>\n<p><strong>Why identical body still creates a new version (§1 #13)?</strong> Operators may \"re-publish\" a doc to bump the timestamp (e.g. confirming a policy is still current). The cost of an extra row is trivial; preventing it would force operators to make trivial edits (whitespace change) just to trigger the version. The change_summary field captures the WHY.</p>\n<p><strong>Why ACL change is a sev-2 memory row (§1 #16, DEC-249)?</strong> Permission tier transitions affect data exposure. Moving a doc from <code>org_only</code> to <code>public</code> is a Trust Center publication event — operator should review. Moving from <code>role_restricted</code> to <code>org_only</code> widens visibility — same. Sev-2 emission ensures TASK-OBS-007 surfaces ACL changes in operator digests. Routine creates/edits are sev-3 (informational only).</p>\n<p><strong>Why <code>status='draft' | 'active' | 'archived'</code> and not boolean (§1 #6)?</strong> Draft is the create-state when the doc isn't ready to be findable (TASK-KB-004 search excludes drafts). Archived is \"no longer current but rendering preserved\" — old policies referenced by historical citations stay accessible. Boolean active/inactive collapses these to one bit and loses the draft/archived distinction.</p>\n<p><strong>Why archived docs still render (DEC-251)?</strong> Citation integrity. A KB doc cited by a Q&amp;A answer 6 months ago must still render today even if archived — the answer's citation is fixed at that version. Search defaults to active-only; rendering accepts any status.</p>\n<p><strong>Why <code>current_version_id</code> FK is DEFERRABLE INITIALLY IMMEDIATE (§1 #18)?</strong> Atomic save: INSERT version + UPDATE document pointer in one transaction. The document row's FK targets a row that's only fully visible after the version INSERT commits within the transaction. Standard immediate FK would reject. DEFERRABLE INITIALLY IMMEDIATE evaluates at statement end — by then both rows exist.</p>\n<p><strong>Why <code>version_number</code> monotonic via trigger (§1 #26)?</strong> Application-layer numbering races: two concurrent saves could both compute <code>MAX + 1</code> simultaneously and produce duplicates. The trigger runs at INSERT time inside the same transaction; serialisable isolation ensures correctness even under contention.</p>\n<p><strong>Why frontmatter as TEXT alongside columns (§1 #2)?</strong> Editors want WYSIWYG markdown + YAML editing; the frontmatter is the editable representation. Columns are the queryable normalised form. Both are kept; a save updates both atomically. The cost is duplication; the benefit is \"search is fast (column queries) AND editing preserves the YAML the author wrote.\"</p>\n<p><strong>Why role_restricted validates against TASK-AUTH-101 enum (§1 #11, DEC-250)?</strong> Free-form role strings drift; an <code>\"allowed_role_codes\": [\"devops\"]</code> row that AUTH doesn't recognise is silent over-permission. Validating against the AUTH closed enum at write time catches typos at the boundary.</p>\n<p><strong>Why archive requires a reason (§1 #19)?</strong> Archiving is consequential — the doc disappears from search; readers who relied on it discover the gap later. Requiring a reason captures the WHY (superseded by doc-X, no longer applicable, decision reversed). The memory audit row preserves it permanently.</p>\n<p><strong>Why body bounds 1–500,000 chars (§1 #2)?</strong> Below 1 = empty doc (caller should DELETE the row, not save an empty version). Above 500K = pathological copy-paste from a foreign source; TASK-KB-002's renderer would OOM on &gt;MB markdown. 500K is ~100 pages of dense prose — well beyond any reasonable single doc.</p>\n<p><strong>Why no slug-rename handler (§1 #19)?</strong> Slug is the URL identity; renaming would break every external link, every memory citation. The operator wanting to rename creates a new doc + redirect (slice 2+). Slug is effectively immutable post-create.</p>\n<p><strong>Why PATCH excludes markdown_body (§1 #19)?</strong> Body changes go through versions, not PATCH. Allowing PATCH to mutate body would either silently create a new version (confusing) or UPDATE the version row (violates immutability). Separating endpoints clarifies semantics.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0001 — documents + versions</h3>\n<pre><code class=\"language-sql\">-- services/kb/migrations/0001_documents.sql\n\nBEGIN;\n\nCREATE TYPE document_category   AS ENUM ('how_to', 'reference', 'decision_log', 'policy', 'runbook');\nCREATE TYPE document_permission AS ENUM ('public', 'org_only', 'role_restricted');\nCREATE TYPE language_code       AS ENUM ('vi', 'en');\nCREATE TYPE document_status     AS ENUM ('draft', 'active', 'archived');\n\nCREATE TABLE document_versions (\n    id                     UUID         PRIMARY KEY,\n    document_id            UUID         NOT NULL,  -- FK added after documents created\n    tenant_id              UUID         NOT NULL,\n    version_number         INT          NOT NULL,\n    markdown_body          TEXT         NOT NULL CHECK (length(markdown_body) BETWEEN 1 AND 500000),\n    frontmatter_yaml       TEXT         NOT NULL,\n    body_sha256            CHAR(64)     NOT NULL CHECK (body_sha256 ~ '^[0-9a-f]{64}$'),\n    change_summary         TEXT         CHECK (change_summary IS NULL OR length(change_summary) BETWEEN 1 AND 500),\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    created_by_subject_id  UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT\n);\n\nCREATE TABLE documents (\n    id                     UUID         PRIMARY KEY,\n    tenant_id              UUID         NOT NULL,\n    slug                   TEXT         NOT NULL CHECK (slug ~ '^[a-z0-9][a-z0-9-/]*[a-z0-9]$' AND length(slug) BETWEEN 2 AND 200),\n    language               language_code NOT NULL,\n    title                  TEXT         NOT NULL CHECK (length(title) BETWEEN 1 AND 200),\n    category               document_category NOT NULL,\n    permission             document_permission NOT NULL,\n    allowed_role_codes     TEXT[]       NOT NULL DEFAULT '{}'::TEXT[],\n    current_version_id     UUID         NOT NULL REFERENCES document_versions(id) DEFERRABLE INITIALLY IMMEDIATE,\n    translation_of         UUID         REFERENCES documents(id) DEFERRABLE INITIALLY IMMEDIATE,\n    status                 document_status NOT NULL DEFAULT 'draft',\n    owner_subject_id       UUID         REFERENCES auth.subjects(id),\n    created_at             TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    updated_at             TIMESTAMPTZ  NOT NULL DEFAULT now()\n);\n\n-- Now add the document FK to versions (was deferred because of circular dep)\nALTER TABLE document_versions ADD CONSTRAINT document_versions_doc_fk\n    FOREIGN KEY (document_id) REFERENCES documents(id) ON DELETE RESTRICT;\n\n-- Per-tenant per-language slug uniqueness\nCREATE UNIQUE INDEX uniq_doc_slug ON documents (tenant_id, language, slug);\nCREATE INDEX documents_tenant_category_idx ON documents (tenant_id, category, status);\nCREATE INDEX documents_tenant_status_idx ON documents (tenant_id, status);\nCREATE INDEX document_versions_doc_version_idx ON document_versions (document_id, version_number DESC);\n\n-- RLS (per task-audit skill rule 13)\nALTER TABLE documents          ENABLE ROW LEVEL SECURITY;\nALTER TABLE document_versions  ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY documents_tenant_isolation ON documents\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nCREATE POLICY document_versions_tenant_isolation ON document_versions\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Append-only enforcement on versions (DEC-247)\nREVOKE UPDATE, DELETE ON document_versions FROM cyberos_app;\n\n-- translation_of cross-language + same-tenant enforcement (DEC-245)\nCREATE OR REPLACE FUNCTION enforce_translation_of() RETURNS TRIGGER AS $$\nDECLARE target RECORD;\nBEGIN\n    IF NEW.translation_of IS NULL THEN RETURN NEW; END IF;\n    IF NEW.translation_of = NEW.id THEN\n        RAISE EXCEPTION 'translation_self_link' USING ERRCODE = 'P0020';\n    END IF;\n    SELECT tenant_id, language INTO target FROM documents WHERE id = NEW.translation_of;\n    IF NOT FOUND THEN\n        RAISE EXCEPTION 'translation_target_missing' USING ERRCODE = 'P0021';\n    END IF;\n    IF target.tenant_id != NEW.tenant_id THEN\n        RAISE EXCEPTION 'translation_cross_tenant' USING ERRCODE = 'P0022';\n    END IF;\n    IF target.language = NEW.language THEN\n        RAISE EXCEPTION 'translation_must_cross_language' USING ERRCODE = 'P0023';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_documents_translation_of BEFORE INSERT OR UPDATE ON documents\n    FOR EACH ROW EXECUTE FUNCTION enforce_translation_of();\n\n-- role_restricted requires non-empty allowed_role_codes (DEC-250)\nCREATE OR REPLACE FUNCTION enforce_role_restricted_codes() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.permission = 'role_restricted' AND COALESCE(array_length(NEW.allowed_role_codes, 1), 0) = 0 THEN\n        RAISE EXCEPTION 'role_restricted_requires_roles' USING ERRCODE = 'P0024';\n    END IF;\n    -- Handler additionally validates each code against TASK-AUTH-101 enum.\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_documents_role_restricted BEFORE INSERT OR UPDATE ON documents\n    FOR EACH ROW EXECUTE FUNCTION enforce_role_restricted_codes();\n\n-- version_number monotonic per document (§1 #26)\nCREATE OR REPLACE FUNCTION assign_version_number() RETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.version_number IS NULL OR NEW.version_number = 0 THEN\n        NEW.version_number := COALESCE(\n            (SELECT MAX(version_number) FROM document_versions WHERE document_id = NEW.document_id),\n            0\n        ) + 1;\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_document_versions_assign_number BEFORE INSERT ON document_versions\n    FOR EACH ROW EXECUTE FUNCTION assign_version_number();\n\n-- documents.updated_at touched on update\nCREATE OR REPLACE FUNCTION touch_documents_updated_at() RETURNS TRIGGER AS $$\nBEGIN NEW.updated_at := now(); RETURN NEW; END;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_documents_updated_at BEFORE UPDATE ON documents\n    FOR EACH ROW EXECUTE FUNCTION touch_documents_updated_at();\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0002 — views + chain walker</h3>\n<pre><code class=\"language-sql\">-- services/kb/migrations/0002_document_views.sql\n\nBEGIN;\n\n-- \"Still resolvable for rendering\" — excludes only fully-deleted (which don't exist; archived stays)\nCREATE VIEW current_documents_view AS\n    SELECT * FROM documents WHERE status != 'archived';\n\n-- \"What is searchable + active\" — excludes draft + archived\nCREATE VIEW active_documents_view AS\n    SELECT * FROM documents WHERE status = 'active';\n\n-- Walk all versions of a document in order (oldest first)\nCREATE OR REPLACE FUNCTION document_version_chain(p_doc_id UUID)\nRETURNS SETOF document_versions AS $$\n    SELECT * FROM document_versions WHERE document_id = p_doc_id ORDER BY version_number ASC\n$$ LANGUAGE sql STABLE;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Rust types</h3>\n<pre><code class=\"language-rust\">// services/kb/src/types.rs\nuse chrono::{DateTime, Utc};\nuse serde::{Deserialize, Serialize};\nuse sqlx::{FromRow, Type};\nuse uuid::Uuid;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"document_category\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum DocumentCategory { HowTo, Reference, DecisionLog, Policy, Runbook }\n\nimpl DocumentCategory {\n    pub const ALL: &amp;'static [DocumentCategory] = &amp;[\n        DocumentCategory::HowTo, DocumentCategory::Reference, DocumentCategory::DecisionLog,\n        DocumentCategory::Policy, DocumentCategory::Runbook,\n    ];\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"document_permission\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum DocumentPermission { Public, OrgOnly, RoleRestricted }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"language_code\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum LanguageCode { Vi, En }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Type, Serialize, Deserialize)]\n#[sqlx(type_name = \"document_status\", rename_all = \"snake_case\")]\n#[serde(rename_all = \"snake_case\")]\npub enum DocumentStatus { Draft, Active, Archived }\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct Document {\n    pub id: Uuid,\n    pub tenant_id: Uuid,\n    pub slug: String,\n    pub language: LanguageCode,\n    pub title: String,\n    pub category: DocumentCategory,\n    pub permission: DocumentPermission,\n    pub allowed_role_codes: Vec&lt;String&gt;,\n    pub current_version_id: Uuid,\n    pub translation_of: Option&lt;Uuid&gt;,\n    pub status: DocumentStatus,\n    pub owner_subject_id: Option&lt;Uuid&gt;,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub updated_at: DateTime&lt;Utc&gt;,\n}\n\n#[derive(Debug, FromRow, Serialize, Deserialize)]\npub struct DocumentVersion {\n    pub id: Uuid,\n    pub document_id: Uuid,\n    pub tenant_id: Uuid,\n    pub version_number: i32,\n    pub markdown_body: String,\n    pub frontmatter_yaml: String,\n    pub body_sha256: String,\n    pub change_summary: Option&lt;String&gt;,\n    pub created_at: DateTime&lt;Utc&gt;,\n    pub created_by_subject_id: Uuid,\n}</code></pre>\n<h3>3.4 — Frontmatter validator</h3>\n<pre><code class=\"language-rust\">// services/kb/src/frontmatter.rs\nuse serde::{Deserialize, Serialize};\nuse serde_yaml;\nuse crate::types::*;\n\n#[derive(Debug, Deserialize, Serialize)]\npub struct Frontmatter {\n    pub title: String,\n    pub category: DocumentCategory,\n    pub language: LanguageCode,\n    pub permission: DocumentPermission,\n    #[serde(default)]\n    pub tags: Vec&lt;String&gt;,\n    #[serde(default)]\n    pub owner_subject_id: Option&lt;uuid::Uuid&gt;,\n    #[serde(default)]\n    pub translation_of: Option&lt;String&gt;,        // slug; resolved to UUID at INSERT\n    #[serde(default)]\n    pub summary: Option&lt;String&gt;,\n    #[serde(default)]\n    pub applicability_tags: Option&lt;ApplicabilityTags&gt;,\n    // Unknown keys: caught by serde(deny_unknown_fields)\n}\n\n#[derive(Debug, Deserialize, Serialize)]\npub struct ApplicabilityTags {\n    pub provider: Option&lt;String&gt;,             // \"aws\" | \"gcp\" | \"azure\" | ...\n    pub region: Option&lt;String&gt;,\n    pub severity: Option&lt;String&gt;,             // \"p0\" | \"p1\" | \"p2\" | \"p3\"\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum FrontmatterError {\n    #[error(\"yaml_parse_error: {0}\")]\n    Yaml(#[from] serde_yaml::Error),\n    #[error(\"unknown_frontmatter_key: {0}\")]\n    UnknownKey(String),\n    #[error(\"title_too_long: {0}\")]\n    TitleTooLong(usize),\n    #[error(\"summary_too_long: {0}\")]\n    SummaryTooLong(usize),\n    #[error(\"too_many_tags: {0}\")]\n    TooManyTags(usize),\n    #[error(\"tag_invalid: {0}\")]\n    TagInvalid(String),\n    #[error(\"applicability_tags_only_runbook\")]\n    ApplicabilityTagsOnlyRunbook,\n}\n\npub fn parse_and_validate(yaml: &amp;str) -&gt; Result&lt;Frontmatter, FrontmatterError&gt; {\n    let fm: Frontmatter = serde_yaml::from_str(yaml)?;\n    if fm.title.len() &gt; 200 { return Err(FrontmatterError::TitleTooLong(fm.title.len())); }\n    if let Some(s) = &amp;fm.summary { if s.len() &gt; 200 { return Err(FrontmatterError::SummaryTooLong(s.len())); } }\n    if fm.tags.len() &gt; 20 { return Err(FrontmatterError::TooManyTags(fm.tags.len())); }\n    for t in &amp;fm.tags {\n        if t.is_empty() || t.len() &gt; 32 { return Err(FrontmatterError::TagInvalid(t.clone())); }\n    }\n    if fm.applicability_tags.is_some() &amp;&amp; fm.category != DocumentCategory::Runbook {\n        return Err(FrontmatterError::ApplicabilityTagsOnlyRunbook);\n    }\n    Ok(fm)\n}</code></pre>\n<h3>3.5 — REST handler excerpt</h3>\n<pre><code class=\"language-rust\">// services/kb/src/handlers/documents.rs\nuse axum::{Json, extract::{Path, State}, http::StatusCode};\nuse sha2::{Digest, Sha256};\nuse crate::types::*;\nuse crate::frontmatter::parse_and_validate;\nuse crate::audit::doc_events;\nuse cyberos_auth::rbac::{Resource, Action, Role};\n\n#[derive(Deserialize)]\npub struct CreateDocumentRequest {\n    pub slug: String,\n    pub language: LanguageCode,\n    pub title: String,\n    pub category: DocumentCategory,\n    pub permission: DocumentPermission,\n    pub allowed_role_codes: Vec&lt;String&gt;,\n    pub markdown_body: String,\n    pub frontmatter_yaml: String,\n    pub owner_subject_id: Option&lt;Uuid&gt;,\n    pub translation_of_slug: Option&lt;String&gt;,\n}\n\npub async fn create_document(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n    Json(req): Json&lt;CreateDocumentRequest&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;Document&gt;), ApiError&gt; {\n    state.matrix.snapshot().require_permission(&amp;claims.roles(), Resource::KbDocument, Action::Write)?;\n    let fm = parse_and_validate(&amp;req.frontmatter_yaml)?;\n\n    // Validate allowed_role_codes against TASK-AUTH-101 closed enum.\n    for code in &amp;req.allowed_role_codes {\n        if code.parse::&lt;Role&gt;().is_err() {\n            return Err(ApiError::UnknownRole(code.clone()));\n        }\n    }\n    if req.permission == DocumentPermission::RoleRestricted &amp;&amp; req.allowed_role_codes.is_empty() {\n        return Err(ApiError::RoleRestrictedRequiresRoles);\n    }\n\n    let doc_id = Uuid::new_v4();\n    let version_id = Uuid::new_v4();\n    let body_sha = hex::encode(Sha256::digest(req.markdown_body.as_bytes()));\n\n    // Resolve translation_of slug → document_id (if specified)\n    let translation_of_id = if let Some(slug) = &amp;req.translation_of_slug {\n        Some(state.repo.find_by_slug_other_lang(claims.tenant_id(), slug, req.language).await?)\n    } else { None };\n\n    let mut tx = state.db.begin().await?;\n    sqlx::query(r#\"\n        INSERT INTO document_versions (id, document_id, tenant_id, version_number,\n            markdown_body, frontmatter_yaml, body_sha256, created_by_subject_id)\n        VALUES ($1, $2, $3, NULL, $4, $5, $6, $7)\n    \"#)\n    .bind(version_id).bind(doc_id).bind(claims.tenant_id())\n    .bind(&amp;req.markdown_body).bind(&amp;req.frontmatter_yaml).bind(&amp;body_sha)\n    .bind(claims.subject_id())\n    .execute(&amp;mut *tx).await?;\n\n    let doc = sqlx::query_as!(Document, r#\"\n        INSERT INTO documents (id, tenant_id, slug, language, title, category, permission,\n            allowed_role_codes, current_version_id, translation_of, status, owner_subject_id)\n        VALUES ($1, $2, $3, $4::language_code, $5, $6::document_category, $7::document_permission,\n                $8, $9, $10, 'draft'::document_status, $11)\n        RETURNING *\n    \"#, /* ... */).fetch_one(&amp;mut *tx).await?;\n\n    doc_events::emit_document_created(&amp;mut tx, &amp;doc, &amp;body_sha, claims.subject_id()).await?;\n    tx.commit().await?;\n    Ok((StatusCode::CREATED, Json(doc)))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Category enum closed at 5</strong> — <code>DocumentCategory::ALL.len() == 5</code>; Postgres enum has exactly 5 labels.</li><li><strong>Permission tier closed at 3</strong> — same shape.</li><li><strong>Language closed at 2</strong> — same shape.</li><li><strong>Status closed at 3</strong> — same shape.</li><li><strong>RLS isolates by tenant</strong> — query as tenant-A returns 0 documents of tenant-B.</li><li><strong>Create document happy path</strong> — 201 with Document JSON; 1 row in documents + 1 in document_versions; <code>kb.document_created</code> memory row emitted.</li><li><strong>Slug uniqueness per (tenant, language)</strong> — second create with same (tenant, language, slug) → 409 <code>slug_taken</code>.</li><li><strong>Same slug different language allowed</strong> — create with <code>language=vi</code> then <code>language=en</code> same slug → both succeed.</li><li><strong>translation_of self-link rejected</strong> — set <code>translation_of = self.id</code> → 400 <code>translation_self_link</code>.</li><li><strong>translation_of same-language rejected</strong> — link to another doc with same language → 400 <code>translation_must_cross_language</code>.</li><li><strong>translation_of cross-tenant rejected</strong> — link to doc in different tenant → 400 <code>translation_cross_tenant</code>.</li><li><strong>role_restricted with empty roles</strong> → 400 <code>role_restricted_requires_roles</code>.</li><li><strong>role_restricted with unknown role</strong> → 400 <code>unknown_role: &lt;code&gt;</code>.</li><li><strong>UPDATE document_versions blocked</strong> — <code>UPDATE document_versions SET markdown_body = 'x' WHERE id = $1</code> as cyberos_app → permission denied.</li><li><strong>DELETE document_versions blocked</strong> — same.</li><li><strong>New version creates monotonic version_number</strong> — first version = 1; second = 2; concurrent saves both succeed with sequential numbers.</li><li><strong>current_version_id points at latest</strong> — after 3 versions, <code>documents.current_version_id</code> = id of version 3.</li><li><strong>body_sha256 server-computed</strong> — request with client-supplied hash field is ignored; server computes from body.</li><li><strong>Markdown body &lt; 1 char</strong> → 400 <code>body_too_short</code> (DB CHECK fires).</li><li><strong>Markdown body &gt; 500_000 chars</strong> → 400 <code>body_too_large</code> (DB CHECK fires).</li><li><strong>Frontmatter unknown key rejected</strong> — YAML with <code>xyz: abc</code> → 400 <code>unknown_frontmatter_key</code>.</li><li><strong>Frontmatter applicability_tags on non-runbook rejected</strong> — category=policy with applicability_tags → 400.</li><li><strong>Archive emits memory row</strong> — POST /archive → status=archived; <code>kb.document_archived</code> row with reason.</li><li><strong>ACL change emits sev-2 memory row</strong> — PATCH permission tier → <code>kb.document_acl_changed</code> row.</li><li><strong>active_documents_view excludes draft + archived</strong> — query returns only status='active' docs.</li><li><strong>document_version_chain returns oldest-first</strong> — chain of 3 versions → 3 rows in order [1, 2, 3].</li><li><strong>Perf budget &lt; 100 ms p95</strong> — <code>documents_perf_test</code> 1000 iterations.</li><li><strong>Idempotent create</strong> — same Idempotency-Key + same body → same document.</li><li><strong>OTel span <code>kb.document.create</code> emitted</strong> — with <code>outcome=success</code>.</li><li><strong>Counter <code>kb_document_create_total{outcome=success, category=runbook, permission=org_only}</code> increments</strong> — per create.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/kb/tests/category_enum_closed_test.rs\n#[test]\nfn category_enum_has_exactly_5_values() {\n    assert_eq!(DocumentCategory::ALL.len(), 5);\n}\n\n#[sqlx::test]\nasync fn pg_category_enum_has_exactly_5_labels(pool: sqlx::PgPool) {\n    let labels: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT unnest(enum_range(NULL::document_category))::text ORDER BY 1\"\n    ).fetch_all(&amp;pool).await.unwrap();\n    assert_eq!(labels, vec![\"decision_log\", \"how_to\", \"policy\", \"reference\", \"runbook\"]);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/kb/tests/version_append_only_test.rs\n#[sqlx::test]\nasync fn update_blocked(pool: sqlx::PgPool) {\n    set_role_app(&amp;pool).await;\n    let id = seed_version(&amp;pool).await;\n    let err = sqlx::query(\"UPDATE document_versions SET markdown_body = 'x' WHERE id = $1\")\n        .bind(id).execute(&amp;pool).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"permission denied\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/kb/tests/translation_of_test.rs\n#[sqlx::test]\nasync fn same_language_link_rejected(ctx: TestCtx) {\n    let a = ctx.create_doc(LanguageCode::Vi, \"/policy/leave\").await;\n    let b = ctx.create_doc(LanguageCode::Vi, \"/policy/leave2\").await;\n    let err = ctx.patch_translation_of(b, a).await.unwrap_err();\n    assert!(format!(\"{err}\").contains(\"translation_must_cross_language\"));\n}\n\n#[sqlx::test]\nasync fn cross_language_pair_allowed(ctx: TestCtx) {\n    let vi = ctx.create_doc(LanguageCode::Vi, \"/policy/leave\").await;\n    let en = ctx.create_doc(LanguageCode::En, \"/policy/leave\").await;\n    ctx.patch_translation_of(en, vi).await.unwrap();\n}</code></pre>\n<pre><code class=\"language-rust\">// services/kb/tests/version_chain_test.rs\n#[sqlx::test]\nasync fn version_numbers_monotonic(ctx: TestCtx) {\n    let doc = ctx.create_doc(LanguageCode::Vi, \"/x\").await;\n    let v2 = ctx.new_version(doc.id, \"body v2\").await;\n    let v3 = ctx.new_version(doc.id, \"body v3\").await;\n    assert_eq!(v2.version_number, 2);\n    assert_eq!(v3.version_number, 3);\n    let chain: Vec&lt;i32&gt; = ctx.version_chain(doc.id).await;\n    assert_eq!(chain, vec![1, 2, 3]);\n    let current: Uuid = ctx.get_current_version_id(doc.id).await;\n    assert_eq!(current, v3.id);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton. The 4 memory row builders in <code>audit/doc_events.rs</code> follow the canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-AUTH-003</strong> — RLS enforcement.</li><li><strong>TASK-AUTH-101</strong> — RBAC catalogue (<code>Resource::KbDocument + Action::Read/Write/Admin</code>) + role validation for <code>allowed_role_codes</code>.</li></ul>\n<p><strong>Downstream (7 placeholders):</strong></p>\n<ul><li><strong>TASK-KB-002</strong> — server-side renderer (markdown → HTML); consumes <code>markdown_body</code> + <code>body_sha256</code> for cache.</li><li><strong>TASK-KB-003</strong> — three permission tiers + share-link tokens; consumes <code>permission</code> + <code>allowed_role_codes</code>.</li><li><strong>TASK-KB-004</strong> — FTS5 + PGroonga lexical search; indexes <code>active_documents_view</code>.</li><li><strong>TASK-KB-005</strong> — BGE-M3 semantic search via memory Layer 2 ingest; consumes versioned bodies.</li><li><strong>TASK-KB-007</strong> — \"Ask this page\" Q&amp;A with span citations; consumes documents + linked translation_of.</li><li><strong>TASK-KB-008</strong> — runbook category with applicability tags for OBS triage.</li><li><strong>TASK-KB-009</strong> — dual-language <code>translation_of</code> link + locale-aware reader display.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AI-003</strong> — memory audit bridge; receives <code>kb.document_created</code>, <code>kb.document_versioned</code>, <code>kb.document_acl_changed</code>, <code>kb.document_archived</code>.</li><li><strong>TASK-MEMORY-111</strong> — PII scrubbing for body before memory Layer 2 ingest (TASK-KB-005 path).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/kb/documents request</h3>\n<pre><code class=\"language-json\">{\n  \"slug\": \"policy/leave-types\",\n  \"language\": \"en\",\n  \"title\": \"Leave Types Policy\",\n  \"category\": \"policy\",\n  \"permission\": \"org_only\",\n  \"allowed_role_codes\": [],\n  \"markdown_body\": \"# Leave Types\\n\\nThis tenant offers 8 leave types per Decree 145/2020 ...\",\n  \"frontmatter_yaml\": \"title: Leave Types Policy\\ncategory: policy\\nlanguage: en\\npermission: org_only\\ntags: [hr, leave, decree-145]\\nowner_subject_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\\n\",\n  \"owner_subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\"\n}</code></pre>\n<h3>8.2 — 201 CREATED response</h3>\n<pre><code class=\"language-json\">{\n  \"id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"slug\": \"policy/leave-types\",\n  \"language\": \"en\",\n  \"title\": \"Leave Types Policy\",\n  \"category\": \"policy\",\n  \"permission\": \"org_only\",\n  \"allowed_role_codes\": [],\n  \"current_version_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8N\",\n  \"translation_of\": null,\n  \"status\": \"draft\",\n  \"owner_subject_id\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"created_at\": \"2026-05-16T10:00:00Z\",\n  \"updated_at\": \"2026-05-16T10:00:00Z\"\n}</code></pre>\n<h3>8.3 — kb.document_created memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"kb.document_created\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"document_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"slug\": \"policy/leave-types\",\n  \"language\": \"en\",\n  \"category\": \"policy\",\n  \"permission\": \"org_only\",\n  \"owner_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"created_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"body_sha256\": \"b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9\",\n  \"version_number\": 1,\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — kb.document_acl_changed memory row (sev-2)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"kb.document_acl_changed\",\n  \"severity\": \"sev-2\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"document_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"old_permission\": \"org_only\",\n  \"new_permission\": \"public\",\n  \"old_allowed_role_codes\": [],\n  \"new_allowed_role_codes\": [],\n  \"changed_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.5 — kb.document_archived memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"kb.document_archived\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"document_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"archived_by_subject_id_hash16\": \"8a7c8c8012344567\",\n  \"reason\": \"Superseded by /policy/leave-types-v2 after 2026 reorganisation\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Notion import (preserve links + categories)</strong> — task-KB-2xx (slice 2+).</li><li><strong>Slug-rename with redirect</strong> — slice 2+; slice 1 treats slug as effectively immutable.</li><li><strong>Trust Center opt-in flag at tenant level</strong> — task-KB-2xx.</li><li><strong>Per-page or per-tree markdown export</strong> — task-KB-2xx.</li><li><strong>Bulk frontmatter migration</strong> — slice 2+ tool.</li><li><strong>Dead-link detection nightly job</strong> — task-KB-2xx.</li><li><strong>Comments / annotations on docs</strong> — out of scope; PROJ handles comments.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>UPDATE on document_versions</td><td>SQL grant</td><td>Permission denied</td><td>None — designed</td></tr><tr><td>DELETE on document_versions</td><td>SQL grant</td><td>Permission denied</td><td>None — designed</td></tr><tr><td>Duplicate slug per (tenant, language)</td><td>UNIQUE index</td><td>409 slug_taken</td><td>Use different slug</td></tr><tr><td>translation_of self-link</td><td>trigger <code>translation_self_link</code></td><td>400</td><td>Fix payload</td></tr><tr><td>translation_of same-language</td><td>trigger <code>translation_must_cross_language</code></td><td>400</td><td>Fix payload</td></tr><tr><td>translation_of cross-tenant</td><td>trigger <code>translation_cross_tenant</code></td><td>400</td><td>Fix payload</td></tr><tr><td>role_restricted with empty roles</td><td>trigger <code>role_restricted_requires_roles</code></td><td>400</td><td>Add at least one role</td></tr><tr><td>Unknown role in allowed_role_codes</td><td>handler validates against TASK-AUTH-101 enum</td><td>400 unknown_role</td><td>Use valid role</td></tr><tr><td>Markdown body &lt; 1 char</td><td>DB CHECK</td><td>400 body_too_short</td><td>Add content</td></tr><tr><td>Markdown body &gt; 500_000 chars</td><td>DB CHECK</td><td>400 body_too_large</td><td>Split into multiple docs</td></tr><tr><td>Frontmatter unknown key</td><td>serde(deny_unknown_fields)</td><td>400 unknown_frontmatter_key</td><td>Remove typo</td></tr><tr><td>Frontmatter title &gt; 200 chars</td><td>validator</td><td>400 title_too_long</td><td>Shorten</td></tr><tr><td>Frontmatter tags &gt; 20</td><td>validator</td><td>400 too_many_tags</td><td>Reduce</td></tr><tr><td>Frontmatter tag length out of bounds</td><td>validator</td><td>400 tag_invalid</td><td>Fix</td></tr><tr><td>applicability_tags on non-runbook</td><td>validator</td><td>400 applicability_tags_only_runbook</td><td>Remove or change category</td></tr><tr><td>body_sha256 client-supplied mismatch</td><td>server overrides</td><td>None</td><td>Designed</td></tr><tr><td>Cross-tenant version FK</td><td>RLS denies SELECT</td><td>0 rows</td><td>None — designed</td></tr><tr><td>version_number collision under concurrent saves</td><td>trigger computes serially within tx</td><td>None — atomic</td><td>Designed</td></tr><tr><td>Current_version_id points at deleted version</td><td>FK ON DELETE RESTRICT prevents</td><td>DELETE fails</td><td>None — designed</td></tr><tr><td>Archive without reason</td><td>handler required field</td><td>400 missing_reason</td><td>Provide</td></tr><tr><td>ACL change without sev-2 emission</td><td>audit test asserts</td><td>CI fails</td><td>Fix builder</td></tr><tr><td>Status transition draft→archived without going active</td><td>FSM allows (no intermediate state required)</td><td>None</td><td>Designed</td></tr><tr><td>Frontmatter YAML malformed</td><td>serde_yaml::Error</td><td>400 yaml_parse_error</td><td>Fix YAML</td></tr><tr><td>Slug regex violation</td><td>DB CHECK (<code>^[a-z0-9][a-z0-9-/]*[a-z0-9]$</code>)</td><td>INSERT fails</td><td>Use kebab/path-friendly slug</td></tr><tr><td>Title &gt; 200 chars</td><td>DB CHECK</td><td>INSERT fails</td><td>Shorten</td></tr><tr><td>Slug too short (&lt; 2 chars)</td><td>DB CHECK</td><td>INSERT fails</td><td>Use longer slug</td></tr><tr><td>change_summary &gt; 500 chars</td><td>DB CHECK</td><td>INSERT fails</td><td>Shorten</td></tr><tr><td>Concurrent translation_of pair creation (A→B + B→A)</td><td>Each insert sees a stable target</td><td>Both succeed — pair is symmetric in intent but FK is one-directional</td><td>None — operator may set both directions deliberately</td></tr><tr><td>PII in markdown_body not scrubbed for memory ingest</td><td>TASK-MEMORY-111 in TASK-KB-005's path</td><td>Out of scope here</td><td>TASK-KB-005</td></tr><tr><td>Document deleted while versions exist</td><td>FK ON DELETE RESTRICT</td><td>DELETE fails</td><td>Archive instead</td></tr><tr><td>Subject deleted while documents reference owner_subject_id</td><td>FK on owner without RESTRICT; SET NULL acceptable</td><td>None</td><td>Designed (owner is optional)</td></tr><tr><td>Subject deleted while versions reference created_by</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Designed</td></tr><tr><td>current_documents_view performance on 100K docs</td><td>Standard tenant index supports</td><td>OK</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Immutable versions are the design contract</strong> — every AI citation references a version_id; mutating would invalidate citations silently.</li><li><strong>Two enums for slug pairs</strong>: per-tenant per-language uniqueness allows <code>/policy/leave-types</code> in <code>vi</code> AND <code>en</code> simultaneously; the natural translation pair indicator.</li><li><strong>frontmatter as both YAML + columns</strong>: YAML for the author (editor experience), columns for queries (TASK-KB-004 search filters). Both updated atomically.</li><li><strong><code>serde(deny_unknown_fields)</code> on Frontmatter struct</strong> — catches typos at the API boundary; downstream consumers don't have to defend.</li><li><strong><code>applicability_tags</code> validator runs ONLY when category == Runbook</strong> — avoids forcing every doc to specify a tag they don't need.</li><li><strong>body_sha256 server-computed via <code>sha2::Sha256</code></strong> — deterministic; hex-encoded; matches the format other modules use.</li><li><strong><code>document_version_chain</code> is a SQL function</strong> — usable from views and from app code; same semantics either way.</li><li><strong><code>translation_of</code> is a self-FK on documents</strong> — not on the version. Translations are paired at the document level; both translations have their own version chains.</li><li><strong>Cross-language pairing is operator-confirmed</strong> — TASK-KB-009 (slice 4+) may auto-suggest pairings; slice 1 is manual.</li><li><strong>Slug regex <code>^[a-z0-9][a-z0-9-/]*[a-z0-9]$</code></strong> — allows path-like slugs (<code>policy/leave-types</code>) while preventing trailing/leading slashes or hyphens.</li><li><strong>archive vs delete</strong>: archive preserves rendering + citations; delete would require cleanup of every citation (which we can't enumerate). Archive is the only way to \"remove\" a doc.</li><li><strong><code>updated_at</code> touched by trigger</strong> — single source of truth for staleness; TASK-KB-005's ingestion checks this for re-embed.</li><li><strong><code>change_summary</code> 1–500 chars</strong> — short enough to discourage prose, long enough for \"Updated section 3 to reflect Decree 145 amendment\".</li><li><strong>Permission validation at retrieval time, not after</strong> — TASK-KB-003 enforces; this task provides the data. Acceptable: this task's GET handler also enforces (defence in depth).</li><li><strong><code>allowed_role_codes TEXT[]</code> not enum array</strong> — Postgres enums don't compose well into arrays; storing as text and validating at boundary is simpler.</li><li><strong><code>status='draft'</code> is the create default</strong> — operators must explicitly publish (slice 2 publish handler); prevents accidental exposure.</li><li><strong><code>current_documents_view</code> excludes only archived</strong> — drafts ARE current (for the author who's editing); they're just not searchable (active_documents_view excludes them).</li><li><strong><code>tenant_id</code> denormalised on <code>document_versions</code></strong> — RLS policy can filter without join.</li><li><strong><code>current_version_id</code> FK is <code>DEFERRABLE INITIALLY IMMEDIATE</code></strong> — needed for atomic INSERT-version-then-UPDATE-pointer pattern in one transaction.</li><li><strong><code>document_versions</code> PK is UUID not BIGSERIAL</strong> — UUIDs allow client-generated ids for idempotency; BIGSERIAL would create races on retry.</li><li><strong>Markdown size cap 500K = ~100 pages</strong> — well beyond practical single-doc limit; protects renderer from OOM.</li><li><strong>owner_subject_id is OPTIONAL</strong> — some docs are tenant-owned (no specific human owner); allowing NULL avoids forcing a fake owner.</li><li><strong><code>kb.document_acl_changed</code> is sev-2</strong> — operator notification; routine creates are sev-3 (informational only).</li></ul>\n<hr>\n<p><em>End of TASK-KB-001.</em></p>";
