window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-131"]="<h1>TASK-IMP-131: Add a <code>memory</code> verb to <code>cs</code>, gated on local availability</h1>\n<h2>Summary</h2>\n<p>Add <code>memory</code> as a top-level verb in <code>cs</code>'s dispatch table that subprocess-dispatches into the locally available <code>cyberos-memory</code> BRAIN-store CLI when present, and fails with a clear, actionable message when it is not — because the npm payload does not currently vendor that CLI's implementation, only its protocol spec.</p>\n<h2>Problem</h2>\n<p><code>docs/plans/PLAN-cli-module-namespacing-2026-07-22/plan.md</code> §5 item 3 asks for a <code>memory</code> verb under <code>cs</code> that reaches <code>modules/memory</code>'s BRAIN-store operations, leaving the dispatch mechanism to implementation. <code>cli.mjs</code> already dispatches to two external processes this way — <code>mcp</code> (a Node subprocess, <code>cli.mjs:69</code>) and <code>gates</code> (a bash script inside the TARGET repo's vendored machine, <code>cli.mjs:78</code>) — so a subprocess-dispatch verb is a well-precedented shape, not a new pattern.</p>\n<p>What is NOT precedented, and was not established by the plan, is that the actual <code>cyberos-memory</code> Python package is reachable from an installed <code>cs</code>. Reading <code>build.sh:25,159,161-162</code> shows the payload's <code>memory/</code> directory is populated with exactly three files — <code>AGENTS.md</code>, <code>memory.schema.json</code>, <code>memory.invariants.yaml</code> — the Layer-1 memory <em>protocol</em> that gets vendored into a consumer repo's own <code>.cyberos/memory/</code> for humans and agents to follow. None of <code>modules/memory/cyberos/</code>'s actual Python source ships in the npm package. <code>cyberos-memory</code> (<code>modules/memory/pyproject.toml:6</code>) is also confirmed not published to PyPI (plan §2). So on a machine where a user only ran <code>npm install -g @cyberskill/cyberos</code> (or <code>npx cyberos install</code>, soon <code>npx cs install</code>), there is no <code>cyberos-memory</code> for a <code>memory</code> verb to reach — <code>python -m cyberos</code> (<code>modules/memory/cyberos/__main__.py</code>'s own documented invocation) will not resolve to anything meaningful unless that machine separately has the internal package pip-installed from a full monorepo checkout.</p>\n<h2>Proposed Solution</h2>\n<p>Add a <code>memory</code> entry to <code>cli.mjs</code>'s dispatch: on <code>cs memory &lt;args...&gt;</code>, first resolve whether a working <code>cyberos-memory</code> is reachable — try the console-script name <code>cyberos</code> is already taken by the OLD public bin and by the BRAIN CLI itself, so resolution must not rely on bare <code>$PATH</code> lookup of <code>cyberos</code> (that is the exact collision this whole plan exists to remove); instead resolve via <code>python3 -m cyberos --help</code> succeeding in the current environment (matching the module's own documented invocation form), or a <code>CYBEROS_MEMORY_STORE</code>-adjacent override the operator sets explicitly. If that resolution succeeds, <code>spawnSync(\"python3\", [\"-m\", \"cyberos\", ...rest], { stdio: \"inherit\" })</code>. If it fails, print a clear message naming that <code>cyberos-memory</code> is an internal package not bundled with this CLI today, and exit non-zero — never a raw Python traceback or a silent no-op.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Vendor <code>modules/memory</code>'s full Python source (and its dependencies — <code>msgspec</code>, <code>cryptography</code>, etc. per <code>pyproject.toml</code>'s dynamic dependency file) into the npm payload so <code>cs memory</code> works out of the box for any installer. Rejected for THIS task: this is the packaging expansion the plan's Option A cost warning anticipated (\"makes Python a hard runtime dependency of what is currently a pure-Node CLI\") taken to its full conclusion, and is a materially larger lift (vendoring a Python runtime + deps inside an npm package, or shipping a bundled interpreter) than \"add a dispatch entry.\" Flagged as an explicit follow-up decision in Scope, not silently absorbed into this task's estimate.</li><li>Make <code>cs memory</code> fail closed with a permanent \"not available\" message regardless of local environment, deferring even the gated dispatch to a future task. Rejected: the plan's item 3 asks for a real verb now, and gating on local availability (rather than refusing to try) still delivers value today for the only population that currently has a working BRAIN-store install — CyberSkill's own engineers — while being honest with everyone else.</li><li>Resolve availability by checking for the <code>cyberos</code> bin on <code>$PATH</code> and assuming it is the memory CLI if found. Rejected: this is exactly the ambient-name collision this entire plan exists to eliminate; a <code>$PATH</code> lookup for <code>cyberos</code> could still resolve to a leftover old install of the RENAMED public CLI on a machine that hasn't fully re-provisioned, silently mis-dispatching.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: on a machine with <code>cyberos-memory</code> pip-installed and <code>python3 -m cyberos</code> resolving, <code>cs memory doctor</code> (or any memory subcommand) reaches the real BRAIN store and returns its actual output, by the same release that ships TASK-IMP-130's rename. Baseline today: <code>memory</code> is not in <code>cli.mjs</code>'s <code>SCRIPTS</code> table at all, so <code>cyberos memory</code> (or, post-rename, <code>cs memory</code>) currently falls through to the \"unknown command\" branch regardless of what is installed locally.</li><li>Guardrail: on a machine WITHOUT <code>cyberos-memory</code> installed, <code>cs memory &lt;anything&gt;</code> exits with code <code>2</code> (the same code <code>cli.mjs</code> already uses for \"unknown command\" and \"gates missing\" — a recognised-but-unusable command, not a crash) and a message identifying memory as an internal package not bundled with this install — never a Python traceback, never a silent success, never the generic \"unknown command\" text (since <code>memory</code> IS a known command; it's the backing tool that's missing).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the <code>memory</code> dispatch entry in <code>cli.mjs</code>, its <code>python3 -m cyberos</code> resolution check and clear failure message, and the doc updates in <code>help.sh</code>/<code>docs/index.md</code> describing the new verb and its local-availability caveat.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Vendoring <code>modules/memory</code>'s Python implementation and its dependencies into the npm payload so <code>cs memory</code> works without a separate local install — a materially larger packaging decision, explicitly deferred (see Alternatives Considered).</li><li>Any change to <code>modules/memory</code>'s own CLI behaviour, schema, or the BRAIN store's internals.</li><li>Publishing <code>cyberos-memory</code> to PyPI — the plan confirms no such plan exists.</li><li>The <code>cuo</code> verb — that is TASK-IMP-132.</li></ul>\n<h2>Dependencies</h2>\n<p>Depends on TASK-IMP-130 (the <code>cs</code> bin rename must land first; this task adds a verb to the SAME dispatch table <code>cli.mjs</code> exposes under the new name). Blocks TASK-IMP-134's end-to-end regression, which must exercise <code>cs memory &lt;cmd&gt;</code> per the plan's own success criterion (plan §6 item 7).</p>\n<p><strong>Relationship to TASK-IMP-076.</strong> That task established <code>cli.mjs</code>'s <code>spawnSync</code>-based dispatch pattern for <code>mcp</code> and <code>gates</code>; this task reuses the same pattern for a third external-process verb rather than inventing a new dispatch shape.</p>\n<p><strong>Sibling-task coordination with TASK-IMP-132.</strong> Both this task and TASK-IMP-132 add a new top-level entry to the same <code>cli.mjs</code> dispatch table and depend only on TASK-IMP-130, not on each other — nothing stops them from being implemented in parallel. Whichever lands second MUST rebase its dispatch-table addition against the first rather than assuming a clean apply; this is a merge-conflict/sequencing risk worth naming explicitly rather than leaving implicit, since neither task's spec alone would surface it.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill inside Cowork.</li><li><strong>Scope:</strong> the finding that the npm payload does not vendor <code>modules/memory</code>'s Python source was made by reading <code>build.sh</code>'s actual file-copy lines during authoring, not carried over from the plan's own text (the plan does not state this). Every <code>source_pages</code> line was read at HEAD in this checkout.</li><li><strong>Human review:</strong> task decomposition approved at the 2026-07-22 PLAN gate. The gated-availability scope decision (rather than full vendoring) is an authoring-time call, flagged in the batch report for the operator to revisit.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>cli.mjs</code>'s dispatch table MUST recognise <code>memory</code> as a known top-level command (not fall through to the \"unknown command\" branch).</li><li>1.2 On <code>cs memory &lt;args&gt;</code>, the CLI MUST attempt to resolve a working <code>cyberos-memory</code> via <code>python3 -m cyberos --help</code> (or equivalent resolution) before dispatching, and MUST NOT resolve availability by a bare <code>$PATH</code> lookup of the name <code>cyberos</code>.</li><li>1.3 When resolution succeeds, the CLI MUST spawn <code>python3 -m cyberos &lt;args&gt;</code> with inherited stdio and MUST forward the child process's exit code.</li><li>1.4 When resolution fails, the CLI MUST print a message identifying <code>cyberos-memory</code> as an internal package not bundled with this install, and MUST exit with code <code>2</code> (matching <code>cli.mjs</code>'s existing convention for a recognised-but-unusable command) without invoking Python.</li><li>1.5 <code>help.sh</code> and <code>docs/index.md</code> MUST document the <code>memory</code> verb and MUST state its local-availability gating, not present it as universally available.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - two runs against the SAME <code>cs memory --help</code> invocation: (a) with no <code>python3</code> stub present, output does NOT contain \"unknown command 'memory'\"; (b) with a working <code>python3 -m cyberos</code> stub present, its stub output IS printed - proving <code>memory</code> is routed to the dispatch/resolution logic in both cases, not merely that the unknown-command message is absent - test: <code>tools/install/tests/test_cli_memory_verb.sh::t01_memory_is_known_command</code></li><li>[ ] AC 2 (traces_to: #1.2) - with a fake <code>cyberos</code> executable on <code>$PATH</code> that prints <code>WRONG-PATH-DISPATCH</code> and exits 0, alongside a working <code>python3 -m cyberos</code> stub that prints <code>CORRECT-DISPATCH</code>, <code>cs memory doctor</code> prints <code>CORRECT-DISPATCH</code> and never prints <code>WRONG-PATH-DISPATCH</code> - proves resolution goes through the <code>python3 -m cyberos</code> check, not a bare <code>$PATH</code> lookup of <code>cyberos</code> - test: <code>tools/install/tests/test_cli_memory_verb.sh::t02_resolution_not_via_path_cyberos</code></li><li>[ ] AC 3 (traces_to: #1.3) - with a stub <code>python3 -m cyberos</code> that echoes its args and exits 3, <code>cs memory foo bar</code> prints the echoed args and the CLI process exits 3 - test: <code>tools/install/tests/test_cli_memory_verb.sh::t03_dispatch_forwards_args_and_exit_code</code></li><li>[ ] AC 4 (traces_to: #1.4) - with no <code>python3</code> on <code>$PATH</code> at all, <code>cs memory doctor</code> exits with code exactly <code>2</code>, prints a message containing \"cyberos-memory\" and \"not bundled\" (or equivalent), and produces no Python traceback text - test: <code>tools/install/tests/test_cli_memory_verb.sh::t04_missing_python_clear_error</code></li><li>[ ] AC 5 (traces_to: #1.5) - <code>help.sh</code>'s output and <code>docs/index.md</code> both mention <code>memory</code> as a verb and both contain a caveat sentence about local availability (grep for a shared marker string) - test: <code>tools/install/tests/test_cli_memory_verb.sh::t05_docs_state_gating</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><code>python3</code> exists on <code>$PATH</code> but <code>cyberos-memory</code> is not installed under it: <code>python3 -m cyberos --help</code> exits non-zero (module not found) - the resolution check MUST treat this the same as \"no python3 at all,\" not attempt to run the real subcommand anyway and surface a raw <code>ModuleNotFoundError</code>.</li><li>A machine with only <code>python</code> (no <code>python3</code>) on <code>$PATH</code>: out of scope for this task's resolution check - <code>modules/memory</code>'s own docstring documents <code>python -m cyberos</code>, but this task standardises on <code>python3</code> to match <code>build.sh:273</code>'s existing convention; a <code>python</code>-only machine is treated as unavailable, not probed further.</li><li>Arguments containing spaces or shell metacharacters passed to <code>cs memory &lt;args&gt;</code>: forwarded via <code>spawnSync</code>'s argv array (not a shell string), so no additional escaping is needed and none should be added - matches the existing <code>mcp</code>/<code>gates</code> dispatch pattern already in <code>cli.mjs</code>.</li><li>Running <code>cs memory</code> with no further arguments: forwarded as <code>python3 -m cyberos</code> with an empty arg list, whose own <code>argparse</code>-based <code>--help</code>-on-no-args behaviour (if any) is <code>modules/memory</code>'s concern, not this task's - this task does not special-case the empty-args case beyond normal forwarding.</li><li>Resolution as specified (<code>python3 -m cyberos --help</code> succeeds) does not verify the resolved module is genuinely <code>cyberos-memory</code> rather than some unrelated Python package coincidentally also importable as <code>cyberos</code> on that machine - a real if low-probability misdispatch risk. Accepted, named limitation for this task's scope: adding a signature check (e.g. matching a known string in the real CLI's own <code>--help</code> output) is deferred as disproportionate hardening for a first cut; a future task can add it if the risk is ever observed in practice.</li><li>Security-class: this task adds a new subprocess-spawn path from an untrusted <code>$PATH</code> resolution (<code>python3</code>). Unlike <code>mcp</code> (spawns a fixed script inside the payload) and <code>gates</code> (spawns a fixed script inside the target repo's vendored machine), <code>memory</code> spawns whatever <code>python3</code> resolves to on the operator's own machine - explicitly the same trust boundary the operator's shell already has, and no new privilege; the CLI does not search a wider or different <code>$PATH</code> than the shell it was invoked from.</li></ul>";
