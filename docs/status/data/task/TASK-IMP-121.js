window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-121"]="<h1>TASK-IMP-121: Uninstall must leave the repo as it found it</h1>\n<h2>Summary</h2>\n<p>Uninstall removes the machine and leaves things pointing at it. A byte-level harness on a fresh git repo found five dangling symlinks, two MCP registrations naming a deleted server, an empty <code>.gitignore</code> install created, and a hook that gains one newline per install/uninstall cycle. None of it damages operator content - the harness recorded zero casualties and zero mutations across six cases - but the repo is left broken rather than merely untidy.</p>\n<h2>Problem</h2>\n<p><code>rm -rf \"$CY\"</code> (<code>:151</code>) deletes <code>.cyberos/</code>. Four things survive that shouldn't:</p>\n<ol><li><strong>Dangling skill symlinks.</strong> <code>.claude/skills/ship-tasks</code>, <code>.codex/skills/ship-tasks</code>, <code>.grok/skills/ship-tasks</code>, <code>.commandcode/skills/ship-tasks</code>, <code>.opencode/skill/ship-tasks</code> all point at <code>../../.cyberos/plugin/skills/ship-tasks</code>. Install creates all five. Uninstall's loop considers only <code>.agents/skills/</code> and <code>.claude/skills/</code>, and <code>:114</code> exempts <code>ship-tasks</code> explicitly (<code>[ \"$_sc\" != \"ship-tasks\" ]</code>). So one is left by decision and four are never looked at.</li><li><strong>Dead MCP registrations.</strong> <code>.mcp.json</code> and <code>.cursor/mcp.json</code> register <code>node .cyberos/mcp/cyberos-mcp.mjs</code>. Uninstall never unregisters. Every agent reading those configs tries to launch a missing server on every startup - in a repo the operator just cleaned.</li><li><strong>An empty <code>.gitignore</code>.</strong> Install creates the file where none existed; uninstall strips the managed block (<code>:81</code>) and leaves a 0-line file behind.</li><li><strong>A hook that grows.</strong> Install appends its block to an operator's pre-existing <code>pre-commit</code>; uninstall strips the block (<code>:70</code>, honestly reported) but leaves the separator newline. Proven to accumulate linearly: 29 -&gt; 30 -&gt; 31 -&gt; 32 bytes over three cycles.</li></ol>\n<p>The ownership test this needs already exists and is already proven: <code>:114</code> matches <code>readlink</code> against <code>*\".cyberos/plugin/skills/$_sc\"</code> and used it to correctly remove <code>task-author</code> and <code>task-audit</code> in the same run that left <code>ship-tasks</code> dangling. The discipline is present; it is applied to two channels out of six and to no other artefact class.</p>\n<p><code>:111</code>'s exemption exists to avoid clobbering operator files - the right instinct, recorded in section 6. But a symlink whose target is inside our own machine is not an operator file: <code>readlink</code> proves ownership, which is exactly why <code>:114</code> trusts that test for its siblings. Leaving a pointer to a file we deleted is not restraint.</p>\n<h2>Proposed Solution</h2>\n<p>Extend the existing readlink ownership test to every channel install writes, and apply the same provable-ownership discipline to the MCP registrations and the <code>.gitignore</code>. Strip the hook block with its separator so the restore is byte-exact. Ownership must be PROVEN per artefact (readlink target inside <code>.cyberos/</code>, or our marker) - never inferred from a path's existence. Anything not provably ours stays, and is named as kept per TASK-IMP-106.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Remove the whole agent surface on uninstall. Rejected: <code>.cursorrules</code>, <code>GEMINI.md</code> and the rules pointers may predate install or carry operator edits; unprovable ownership is not a licence to delete. This is TASK-IMP-094's recorded reasoning and it still holds.</li><li>Leave the dangling links and let TASK-IMP-106's summary name them. Rejected: 106 §1.5 forbids changing behaviour, so it can only describe the breakage honestly. A truthful report of a broken repo is not a fixed repo.</li><li>Track install's creations in a manifest and reverse it exactly. Rejected as scope creep here - it is the right shape for TASK-IMP-122's content fingerprint, and this task must not pre-empt that design.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: zero dangling symlinks and zero registrations naming a removed path, asserted by resolving every surviving pointer after uninstall. Baseline: 5 and 2.</li><li>Guardrail: zero casualties - no path present before install may be missing or mutated after uninstall. Baseline: 0, and it must stay 0.</li><li>Guardrail: byte-count of an operator's hook is constant across N cycles. Baseline: +1 per cycle.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>uninstall.sh</code> cleanup of the six skill channels, the two MCP registrations, the install-created <code>.gitignore</code>, and the hook block separator. Suite arms for each.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>What uninstall KEEPS by design (<code>docs/tasks/</code>, <code>docs/status/</code>, <code>CHANGELOG.md</code>, <code>AGENTS.md</code>/pointer files, BRAIN). Correct, and untouched.</li><li>The rules pointers (<code>.cursorrules</code>, <code>.windsurfrules</code>, <code>GEMINI.md</code>, <code>.github/copilot-instructions.md</code>, <code>.agents/rules/</code>, <code>.devin/rules/</code>, <code>.cursor/rules/</code>). Agent surface, kept per <code>:92-93</code>.</li><li>Payload staleness detection - TASK-IMP-122.</li><li>The summary's wording - TASK-IMP-106 owns it.</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-106: both edit <code>uninstall.sh</code> and <code>test_install_hygiene.sh</code>, so they serialise. More importantly 106's AC 3 pins \"the set of files present after uninstall is byte-identical to today's behavior\" (<code>t22_uninstall_behavior_unchanged</code>) - a test that freezes the defects above as correct. This task MUST re-point <code>t22</code> at the corrected set; shipping 121 without doing so turns 106's green suite red. Per TASK-IMP-101's evidence gate, 106's coverage-gate artefact is the evidence.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from a byte-level harness run on host macOS against <code>dist/cyberos</code> 1.0.0; every claim above is a recorded measurement, not a reading of the script. Implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> scope and the <code>:111</code> refinement approved at the 2026-07-18 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 After uninstall, NO symlink under any installed skill channel (<code>.agents/skills/</code>, <code>.claude/skills/</code>, <code>.codex/skills/</code>, <code>.grok/skills/</code>, <code>.commandcode/skills/</code>, <code>.opencode/skill/</code>) may remain whose <code>readlink</code> target resolves inside the removed <code>.cyberos/</code>. Ownership MUST be established by the readlink target, not by the entry's name.</li><li>1.2 An entry under those channels whose ownership is NOT provable (a real directory without our marker, or a symlink pointing outside <code>.cyberos/</code>) MUST be left in place and reported as kept.</li><li>1.3 After uninstall, NO MCP registration written by install (<code>.mcp.json</code>, <code>.cursor/mcp.json</code>) may name a path inside the removed <code>.cyberos/</code>. A registration whose command does not reference <code>.cyberos/</code> is operator work and MUST be left untouched; a file that becomes empty of registrations as a result MUST NOT survive if install created it.</li><li>1.4 A <code>.gitignore</code> that install created MUST NOT survive uninstall as an empty file. A <code>.gitignore</code> that predates install MUST survive byte-identical apart from the removal of the managed block.</li><li>1.5 Stripping the managed block from an operator's pre-existing hook MUST restore that hook byte-identical to its pre-install content, including trailing bytes.</li><li>1.6 Uninstall MUST be idempotent over cycles: for any N &gt;= 1, N install/uninstall cycles MUST leave an operator's pre-existing hook byte-identical to its pre-install content.</li><li>1.7 No path present before install may be missing or mutated after uninstall.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - after uninstall on a repo where install wrote all six channels, resolving every surviving entry under those channels yields zero unresolvable targets; the test MUST FAIL if any one channel is left unhandled - test: <code>tools/install/tests/test_install_hygiene.sh::t23_no_dangling_skill_links</code></li><li>[ ] AC 2 (traces_to: #1.2) - an operator-owned unmarked skill dir and a symlink pointing outside <code>.cyberos/</code> both survive byte-identical - test: <code>tools/install/tests/test_install_hygiene.sh::t24_unprovable_ownership_kept</code></li><li>[ ] AC 3 (traces_to: #1.3) - after uninstall, no registration in <code>.mcp.json</code> or <code>.cursor/mcp.json</code> names a path under <code>.cyberos/</code>, and a pre-existing registration naming something else survives byte-identical - test: <code>tools/install/tests/test_install_hygiene.sh::t25_no_dead_mcp_registrations</code></li><li>[ ] AC 4 (traces_to: #1.4) - a <code>.gitignore</code> created by install does not survive; a <code>.gitignore</code> that predated install survives with only the managed block removed - test: <code>tools/install/tests/test_install_hygiene.sh::t26_gitignore_created_vs_preexisting</code></li><li>[ ] AC 5 (traces_to: #1.5, #1.6) - an operator's hook is byte-identical to its pre-install bytes after 1 cycle AND after 3 cycles; the test MUST compare bytes, not line count, and MUST FAIL on the current +1-newline-per-cycle behaviour - test: <code>tools/install/tests/test_install_hygiene.sh::t27_hook_restore_byte_exact_and_idempotent</code></li><li>[ ] AC 6 (traces_to: #1.7) - no path present before install is missing or mutated after uninstall, across a fresh-git repo, a repo with a foreign hook, and a repo with <code>core.hooksPath</code> set - test: <code>tools/install/tests/test_install_hygiene.sh::t28_no_casualties</code></li><li>[ ] AC 7 (traces_to: #1.1, #1.3, #1.4) - TASK-IMP-106's <code>t22_uninstall_behavior_unchanged</code> is re-pointed at the corrected set and passes; the suite MUST NOT contain an assertion that the pre-121 leftovers are correct - test: <code>tools/install/tests/test_install_hygiene.sh::t22_uninstall_behavior_unchanged</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A channel dir that is empty after its managed entry is removed: pruned only if install created it, by the same rule <code>:105-107</code> already applies to <code>.agents/skills</code>; an operator's own empty dir is not ours to remove.</li><li>A symlink into <code>.cyberos/</code> whose target was ALREADY absent before uninstall (a prior partial install): still provably ours by readlink, still removed. Unresolvable is not the same as unowned.</li><li><code>.mcp.json</code> containing both our registration and an operator's: our key is removed, theirs survives, the file survives. Only a file install created AND that is now empty of registrations is removed.</li><li>An operator hook whose pre-install content already ended in multiple newlines: restore is byte-exact to THAT content - the rule is \"identical to what was there\", never \"normalised\".</li><li><code>CYBEROS_UNINSTALL_KEEP_BRAIN=0</code>: <code>.cyberos/</code> is fully removed, so every pointer into it dangles. This is the strictest case for 1.1/1.3 and MUST be covered, not the default-BRAIN case alone.</li><li>Uninstall run twice: the second run finds nothing of ours and removes nothing; 1.6's byte-identity still holds.</li><li>Security-class: ownership is decided by <code>readlink</code> output and our own marker file, never by a path pattern an operator could forge by naming a directory <code>ship-tasks</code>. No user-supplied string is interpolated into a removal command; nothing printed is executed.</li></ul>";
