window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-121"]="<h1>TASK-IMP-121: Uninstall must leave the repo as it found it</h1>\n<h2>Summary</h2>\n<p>Uninstall deletes the machine and leaves things pointing at it. A byte-level harness on a fresh git repo found five dangling skill symlinks and two MCP registrations naming a server that is gone. Two further defects are byte-level: stripping our managed block from an operator's pre-commit hook leaves one newline behind every cycle, and doing the same to a pre-existing <code>.gitignore</code> leaves one behind once. Nothing here destroys operator content - the harness recorded zero casualties across its six cases - but the repo is left broken rather than merely untidy, and two files an operator owns do not come back byte-identical.</p>\n<h2>Problem</h2>\n<p>Measured 2026-07-18. <code>uninstall.sh:151</code> runs <code>rm -rf \"$CY\"</code>; <code>:155-158</code> then recreates <code>.cyberos/memory/</code> and moves the stashed BRAIN back. <strong><code>.cyberos/</code> survives by default</strong> - what is gone in both modes is <code>plugin/</code>, <code>mcp/</code>, <code>cuo/</code>, <code>lib/</code>, <code>docs-tools/</code> and the root scripts. Every pointer install writes targets <code>.cyberos/plugin/skills/...</code> or <code>.cyberos/mcp/cyberos-mcp.mjs</code>, and both live in the part that is removed under <code>CYBEROS_UNINSTALL_KEEP_BRAIN=1</code> and <code>=0</code> alike. So the question is never \"does <code>.cyberos/</code> still exist\" - it is \"does this pointer name a path we deleted\", and that is answered by the pointer's own text.</p>\n<p><strong>1. Five dangling skill symlinks.</strong> Install writes six channels: <code>.agents/skills/</code>, <code>.claude/skills/</code>, <code>.grok/skills/</code>, <code>.commandcode/skills/</code>, <code>.codex/skills/</code>, <code>.opencode/skill/</code> (<code>:623-627</code>, <code>:631-632</code>, <code>:650-679</code>; note OpenCode's singular <code>skill</code>). Uninstall's loop (<code>:94</code>) looks at two. <code>.agents/skills/ship-tasks</code> is removed by <code>:98-99</code>; <code>.claude/skills/{task-author,task-audit}</code> by <code>:113-114</code>. <code>.claude/skills/ship-tasks</code> is exempted by name at <code>:112</code> (<code>[ \"$_sc\" != \"ship-tasks\" ]</code>), and <code>.grok</code>, <code>.commandcode</code>, <code>.codex</code>, <code>.opencode</code> are never looked at - <code>grep -ci</code> returns <strong>0</strong> for each of them in <code>uninstall.sh</code>. Five survivors, all <code>-&gt; ../../.cyberos/plugin/skills/ship-tasks</code>, all confirmed dangling.</p>\n<p><strong>2. Two dead MCP registrations.</strong> <code>.mcp.json</code> and <code>.cursor/mcp.json</code> both register <code>node .cyberos/mcp/cyberos-mcp.mjs</code> (<code>:685-688</code>). Uninstall never unregisters - <code>grep -ci mcp</code> returns 0. Every agent reading those configs launches a missing server on every startup, in a repo the operator just cleaned.</p>\n<p><strong>3. The hook gains a newline every cycle.</strong> Install appends its block with <code>cat &gt;&gt; \"$hk\" &lt;&lt;'HOOK'</code> whose heredoc's <strong>first line is blank</strong> (<code>:851-853</code>), so the file gains <code>\\n# &gt;&gt;&gt; cyberos-status-hook v2</code>. <code>uninstall.sh:68</code>'s sed range starts <strong>at</strong> the marker, so the blank above it survives. Proven by the harness at 29 -&gt; 30 -&gt; 31 -&gt; 32 bytes over three cycles, final tail <code>exit 0\\n\\n\\n\\n</code>; reproduced this round at 7 -&gt; 8 -&gt; 9 -&gt; 10 on a 7-byte fixture. Unbounded: nothing ever trims it.</p>\n<p><strong>4. The same defect in <code>.gitignore</code>, which the first draft missed.</strong> <code>install.sh:747</code> writes <code>[ -s \"$gi.cyberos.tmp\" ] &amp;&amp; printf '\\n'</code> before the block, and <code>uninstall.sh:80</code>'s sed range also starts at the marker. Measured by replaying both verbatim: a pre-existing <code>.gitignore</code> goes <strong>14 -&gt; 15 bytes</strong> and stays at 15 across three cycles - <code>install.sh:745</code>'s awk trims the trailing blank run on re-install, so this leak is one-time rather than accumulating. It is still a mutation of an operator's file, and it is why the fix belongs to the strip, not to the hook.</p>\n<p>The <code>[ -s ]</code> guard at <code>:747</code> also explains the separate <code>.gitignore</code> symptom the harness saw: where install <strong>created</strong> the file (<code>:706</code>), the tmp is empty, no separator is written, and the strip leaves a 0-byte file.</p>\n<p><strong>Against section 6.</strong> <code>uninstall.sh:162</code> reads <code># 6. skill symlinks into .cyberos (dangling) - leave dirs; operator cleans</code>. The dangling is <strong>known and deliberate</strong>, and <code>:111</code>'s comment records the same posture for <code>.claude/skills/ship-tasks</code>. This is a decision to overturn, not a gap to fill. It is overturned because its premise does not survive contact with <code>:113-114</code>. The restraint section 6 encodes is the right instinct about <em>operator files</em> - and a symlink whose target text names our own machine is not an operator file. <code>:113-114</code> already trusts exactly that proof to remove <code>task-author</code> and <code>task-audit</code>, in the same run that leaves <code>ship-tasks</code> dangling. Section 6 is not restraint applied consistently; it is one channel's entry escaping a test its two siblings pass. Leaving a pointer to a file we deleted protects nobody.</p>\n<p><strong>What ownership can and cannot be proven from.</strong> The discipline <code>:98-99</code> and <code>:113-114</code> use is: read the artefact, decide from what it says. It generalises to every artefact here - a marker-delimited block, an MCP command naming our path, a <code>.cyberos-owned</code> file - and it stops exactly at the container. Whether install <em>created</em> <code>.gitignore</code> or <code>.mcp.json</code> is not recorded anywhere: <code>install.sh</code>'s <code>wrote</code>, <code>AGENT_FILES</code> and <code>SKILL_DIRS</code> (<code>:479</code>, <code>:580</code>, <code>:621</code>, <code>:686</code>) are shell variables that die with the process, and the only on-disk marker, <code>.cyberos-owned</code>, is written solely into <code>.agents/skills</code> copy-fallback dirs (<code>:672-675</code>). An operator who ran <code>touch .gitignore</code> before install is byte-indistinguishable from install's <code>: &gt; \"$gi\"</code>. So no clause below conditions on it.</p>\n<p><strong>Two proofs, not one, and they do not generalise alike.</strong> The direct form (<code>:613</code>) writes a target naming <code>.cyberos/plugin/skills/&lt;entry&gt;</code>: that text names the machine we deleted, so it proves ownership for <em>any</em> entry under <em>any</em> channel. The chained form (<code>:662</code>) writes <code>.agents/skills/&lt;cmd&gt; -&gt; ../../.claude/skills/&lt;cmd&gt;</code>, which names no <code>.cyberos/</code> path at all - it is ours only <em>by construction</em>, and only for the three names <code>:649</code>'s <code>SHARED_CMDS</code> lists. Generalising the chained pattern to every entry in the directory would keep the pattern and destroy the justification: an operator's <code>.agents/skills/my-notes -&gt; ../../.claude/skills/my-notes</code> matches it exactly and is not ours. The two forms are therefore separated below, and only the direct one is name-blind.</p>\n<h2>Proposed Solution</h2>\n<p>Extend the readlink ownership test to every channel install writes, and apply the same read-the-artefact discipline to the MCP registrations and to both managed blocks. Four proofs, each evaluated at uninstall time from the artefact itself:</p>\n<table><thead><tr><th>artefact</th><th>proof it is ours</th><th>generalises?</th></tr></thead><tbody><tr><td>skill symlink, direct form</td><td><code>readlink</code> target text naming <code>.cyberos/plugin/skills/&lt;entry&gt;</code> (<code>:98</code>)</td><td>yes - the text names our deleted machine</td></tr><tr><td>skill symlink, chained form</td><td>ours by construction (<code>install.sh:662</code>)</td><td>no - only the three <code>:649</code> names, only under <code>.agents/skills/</code></td></tr><tr><td><code>.agents/skills</code> copy dir</td><td>the <code>.cyberos-owned</code> file it carries (<code>:101</code>)</td><td>yes - the marker is ours</td></tr><tr><td><code>.gitignore</code> / hook block</td><td>the <code># &gt;&gt;&gt;</code> / <code># &lt;&lt;&lt;</code> marker pair delimiting it</td><td>yes - the markers are ours</td></tr><tr><td>MCP registration</td><td>the command naming <code>.cyberos/mcp/cyberos-mcp.mjs</code></td><td>yes - the command names our deleted machine</td></tr></tbody></table>\n<p><strong>The predicate is textual, never resolution.</strong> <code>readlink</code> returns a string; a target naming our machine proves ownership whether or not it still resolves. This is what makes the rule identical under <code>CYBEROS_UNINSTALL_KEEP_BRAIN=1</code> (where <code>.cyberos/</code> survives with BRAIN inside) and <code>=0</code> (where it does not), and it is why an operator's own broken symlink pointing elsewhere is untouched: unresolvable is not the same as unowned, in either direction.</p>\n<p><strong>What is provably ours is removed; the container is not.</strong> The block comes out of <code>.gitignore</code>, the file stays. The registration comes out of <code>.mcp.json</code>, the file stays with an empty registry. The symlink goes, the channel dir stays. Removing a container would require knowing install created it, which nothing records - so the line is drawn where the proof runs out. An emptied container is inert; a pointer to a deleted file is not. This rule is applied to all six channel dirs, which costs <code>:118-119</code>'s <code>rmdir</code> of <code>.agents/skills</code>: that rmdir deletes a container for emptiness, which is the one move the rule forbids, and it is a live casualty for an operator whose empty <code>.agents/skills</code> predates install.</p>\n<p><strong>Ownership and capability are different questions, and <code>.mcp.json</code> needs both.</strong> What proves the <em>registration</em> is ours is its command text, exactly as <code>readlink</code> output proves a symlink. What byte-identity to <code>install.sh:685</code>'s form decides is something else entirely: whether <code>uninstall.sh</code> can rewrite the file <strong>without a JSON parser it does not have</strong>. So byte-identity is a capability gate, not an ownership test - which is why the prior draft's own objection (\"byte-identity proves the content is ours; it cannot prove the file is\") is correct and does not bite: nothing below uses byte-identity to prove the file is ours, and the file is never deleted either way.</p>\n<p><strong>Strip the separator with the block, by bytes and not by lines.</strong> Install always appends exactly one <code>\\n</code> immediately before the <code># &gt;&gt;&gt;</code> marker (<code>:851-853</code>, <code>:913-915</code>: the heredoc's first line is blank), so the byte immediately before the marker is always ours. The strip must delete <strong>that byte</strong>, not that line. Measured: the line-oriented rule restores a newline-terminated hook exactly but adds a byte to a hook with no trailing newline (6B -&gt; 7B); the byte-oriented rule is byte-exact for both, and for a hook ending in three newlines, and for a hook with content above and no trailing newline.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Have install write a receipt of what it created, so uninstall can know rather than guess.</strong> The operator's instinct, floated for this task and not decided; adopted in spirit for TASK-IMP-122, where the underivable fact (what <code>install.sh</code> vendors) is load-bearing and has no safe fallback. It is rejected here because the fact it would record is not load-bearing and the fallback is safe. For every artefact install's own text already marks, the only clause a receipt changes is \"delete the container\": everything else is already decided from the artefact. And a receipt never decides <em>that</em> alone - an operator who adds <code>node_modules/</code> outside our markers owns that file whatever the receipt says, so the rule is always \"receipt says created <strong>and</strong> the file is now empty\", and the emptiness test does the work in every case. There is exactly one clause a receipt would genuinely decide: §3's <code>CYBEROS_COPY_SKILLS=1</code> gap, where an unmarked copy under <code>.claude/skills/</code> is provable from nothing on disk today. That is a real exception to the sentence above, and it is not enough - because a <code>.cyberos-owned</code> marker decides the same case, install already writes one at <code>:672-675</code>, and a marker is local to the artefact where a receipt is a new cross-script contract. Both are <code>install.sh</code> changes and both are out of scope here. Against that, a receipt costs a format, staleness semantics across re-installs, <code>install.sh</code> in <code>modified_files</code> - and a new casualty vector: a receipt that wrongly claims creation deletes an operator's file, which is the one outcome §1.9 forbids and which no mechanism can do today. Note also what a receipt does <strong>not</strong> rescue: it says nothing about how to remove one key from JSON an operator has since edited (§1.5), and nothing about <code>.gitignore</code>'s pre-install shape (§3), which install destroyed before uninstall ever ran. Trading a guardrail for tidiness is the wrong direction.</li><li><strong>Delete an emptied container when it is byte-identical to install's exact output.</strong> Rejected: that is the TASK-IMP-118 defect in miniature. Install's <code>.mcp.json</code> is a fixed 5-line, 105-byte string (measured) and <code>install.sh:689</code>'s own summary points operators at <code>.cyberos/mcp/README.md</code> to hand-register (<code>:683</code>'s comment says the same), so an operator-authored file can be byte-identical to ours. Byte-identity proves the <em>content</em> is ours; it cannot prove the <em>file</em> is. §1.4 uses it only as a capability gate, never as a licence to delete.</li><li><strong>Trim trailing blank lines after the strip instead of consuming the separator.</strong> Rejected: an operator hook whose pre-install content legitimately ends in three newlines would come back with one. Measured - the correct rule removes exactly one <em>byte</em> immediately above the marker, and that is byte-exact for the three-newline shape (9B -&gt; 9B) as well as the no-newline shape (6B -&gt; 6B).</li><li><strong>Keep the prior draft's line-oriented strip and record the no-trailing-newline hook as impossible.</strong> Rejected because the impossibility is false, and measurement says so: the byte-oriented rule inverts that shape (6B -&gt; 6B). The error was a level confusion - the rule was reasoned about in line-space, where the no-newline case has no blank line to consume, and \"my line rule cannot\" was generalised to \"no rule can\". Install's <code>cat &gt;&gt;</code> is injective on hook content, so an inverse must exist; only the line-oriented expression of it fails.</li><li><strong>Scope §1.6's container rule to five channel dirs and leave <code>:118-119</code> alone.</strong> Rejected: it makes the clause satisfiable without making the behaviour defensible. <code>:118</code> deletes an operator's pre-existing empty <code>.agents/skills</code> today, which §1.9 forbids regardless of what §1.6 says, so scoping the container rule around it would leave the casualty and merely stop naming it. Either the container rule is principled or it is not; <code>.agents/skills</code> is a container by exactly the argument that keeps <code>.claude/skills</code>.</li><li><strong>Remove the whole agent surface on uninstall.</strong> Rejected: <code>.cursorrules</code>, <code>GEMINI.md</code> and the rules pointers may predate install or carry operator edits; unprovable ownership is not a licence to delete. TASK-IMP-094's recorded reasoning, and it still holds.</li><li><strong>Leave the dangling links and let TASK-IMP-106's summary name them.</strong> Rejected: 106 §1.5 forbids changing behaviour, so it can only describe the breakage honestly. A truthful report of a broken repo is not a fixed repo.</li><li><strong>Effort 8 -&gt; 9 hours.</strong> Breakdown: channel loop rewrite (direct form, all six, name-blind) 1h; chained/direct split 0.5h; MCP partition 1.5h; byte-exact strip 1.5h; <code>:118-119</code> removal 0.5h; <code>t22</code> rename + re-point 0.5h; suite 3.5h.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: zero surviving pointers naming a path under <code>.cyberos/</code>, asserted by reading every surviving symlink target and every MCP command after uninstall. Baseline: 5 dangling symlinks + 2 dead registrations, measured 2026-07-18.</li><li>Primary: an operator's pre-existing hook and <code>.gitignore</code> are byte-identical to their pre-install bytes after 1 and after 3 cycles, asserted with <code>cmp</code>, over the shapes §1.7 binds on. Baseline: hook +1 per cycle (7 -&gt; 8 -&gt; 9 -&gt; 10 on a 7-byte fixture; 29 -&gt; 30 -&gt; 31 -&gt; 32 in the harness); <code>.gitignore</code> +1 once (14 -&gt; 15).</li><li>Guardrail: zero casualties - no path present before install is missing, and none is mutated except by the removal of content proven ours (§1.9). Baseline: 0 across the harness's six cases; it must stay 0, and the leak fixes must not create one.</li><li>Guardrail: every container install writes into still exists after uninstall, including <code>.agents/skills</code>. Baseline: five of six present today; <code>.agents/skills</code> is removed by <code>:118</code> when it empties - this metric exists to keep the fix from over-reaching and to close that one case.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>uninstall.sh</code>'s handling of the six skill channels, the two MCP registrations, the separator on both managed-block strips, and <code>:118-119</code>'s <code>rmdir</code>; the suite arms for each; the rename and re-point of TASK-IMP-106's <code>t22</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li><code>install.sh</code>. This task changes nothing on the install side. Two consequences are accepted rather than fixed: <code>.gitignore</code>'s pre-install shape is destroyed by <code>:733-746</code>'s awk before uninstall runs (§3), and the <code>CYBEROS_COPY_SKILLS=1</code> copy carries no marker (§3).</li><li>What uninstall KEEPS by design (<code>docs/tasks/</code>, <code>docs/status/</code>, <code>CHANGELOG.md</code>, <code>AGENTS.md</code>/pointer files, BRAIN). Correct, and untouched.</li><li>The rules pointers (<code>.cursorrules</code>, <code>.windsurfrules</code>, <code>GEMINI.md</code>, <code>.github/copilot-instructions.md</code>, <code>.agents/rules/</code>, <code>.devin/rules/</code>, <code>.cursor/rules/</code>). Agent surface, kept for the same reason as <code>CLAUDE.md</code>. (<code>uninstall.sh:92-93</code>'s comment records only three of these; the rest are covered by the same reasoning, not by that citation.)</li><li>Payload staleness detection - TASK-IMP-122.</li><li>The summary's wording - TASK-IMP-106 owns it.</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-106: both edit <code>uninstall.sh</code> and <code>test_install_hygiene.sh</code>, so they serialise. 106 lands first, creating <code>t22_uninstall_behavior_unchanged</code>, whose AC 3 pins \"the set of files present after uninstall is byte-identical to today's behavior\" - a test that freezes every defect above as correct. This task MUST re-point and rename it (§1.10); shipping 121 without doing so turns 106's green suite red. 106 §1.5 keeps its verification: the re-pointed test still fails any summary change that alters what uninstall removes or keeps, which is the whole of what §1.5 asks. Per TASK-IMP-101's evidence gate, 106's coverage-gate artefact is the evidence.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> rewritten in full 2026-07-18 by a third author after a second independent audit failed the prior rewrite at 6/10. The 6/10 draft had corrected all six inherited citations and then originated five defects of its own: a FALSE impossibility proof in §3, an unsatisfiable AC 5, a contradiction between AC 1 and AC 2, an unreconciled §1.3-vs-§1.8, and a seventh mis-citation. Every line number in this document was re-resolved against source at HEAD by this author, including the ones inherited as correct. Every measurement quoted was re-run by this author; the numbers that survived are marked re-measured, and the one claim that did not survive (the impossibility) is retracted in §3 with the measurement that refutes it. Two findings are this author's own and were not in either audit: <code>.gitignore</code>'s pre-install shape is destroyed by <code>install.sh:733-746</code>, which is a real impossibility the prior draft attached to the wrong file; and §1.9-as-written contradicted §1.1, not only §1.4. One limit of this author's own proposed rule was found by testing it rather than assuming it, and is recorded in §3.</li><li><strong>Human review:</strong> scope and the <code>:112</code> refinement approved at the 2026-07-18 PLAN gate; both HITL gates are recorded human verdicts. The receipt decision is the prior author's, kept here on a narrowed argument. Bringing <code>:118-119</code> in scope is this author's decision and is NOT covered by the PLAN gate's recorded scope - it needs an operator verdict before implementation.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 After uninstall, no symlink under any channel install writes (<code>.agents/skills/</code>, <code>.claude/skills/</code>, <code>.grok/skills/</code>, <code>.commandcode/skills/</code>, <code>.codex/skills/</code>, <code>.opencode/skill/</code>) may remain whose <code>readlink</code> target text names <code>.cyberos/plugin/skills/&lt;entry&gt;</code>. Ownership MUST be decided from the target TEXT, never from whether the target resolves and never from the entry's name; no entry may be exempt by name, which removes <code>:112</code>'s <code>ship-tasks</code> guard.</li><li>1.2 The CHAINED form (<code>readlink</code> target naming <code>.claude/skills/&lt;entry&gt;</code>, which names no <code>.cyberos/</code> path and is ours only by construction) MUST be removed ONLY for the three names <code>install.sh:649</code> writes (<code>ship-tasks</code>, <code>task-author</code>, <code>task-audit</code>) and ONLY under <code>.agents/skills/</code>, because that is the exact set install creates it for (<code>:662</code>); it MUST NOT be generalised to other entries or other channels.</li><li>1.3 An entry under those channels whose ownership is proven by neither §1.1 nor §1.2 nor a <code>.cyberos-owned</code> marker it carries (<code>:101</code>) MUST be left byte-identical and reported as kept - including a real directory without our marker, a symlink whose target names neither form, and a BROKEN symlink whose target names neither form.</li><li>1.4 Where <code>.mcp.json</code> or <code>.cursor/mcp.json</code> carries a registration whose command names <code>.cyberos/mcp/cyberos-mcp.mjs</code> AND the file is byte-identical to the form <code>install.sh:685</code> generates, that registration MUST be removed, leaving the file present and carrying a registry with no entries. The command text proves the registration is ours; byte-identity is only the gate that lets <code>uninstall.sh</code> rewrite the file without a JSON parser.</li><li>1.5 Where <code>.mcp.json</code> or <code>.cursor/mcp.json</code> carries our registration but is NOT byte-identical to that form, the file has content install did not write; uninstall MUST NOT edit it, and MUST report the dead registration and the file's path so the operator can remove it by hand.</li><li>1.6 A container install writes into MUST survive uninstall even when emptied of our content: <code>.gitignore</code> (including one install created at <code>:706</code>), <code>.mcp.json</code>, <code>.cursor/mcp.json</code>, and each of §1.1's six channel directories INCLUDING <code>.agents/skills</code>. No container may be removed on the grounds that it is now empty, which removes <code>uninstall.sh:118-119</code>'s <code>rmdir</code> of <code>.agents/skills</code> and <code>.agents</code>.</li><li>1.7 Stripping a managed block MUST restore the file byte-identical to its pre-install content by deleting the block together with the single <code>\\n</code> byte install writes immediately before the <code># &gt;&gt;&gt;</code> marker (<code>install.sh:851-853</code>/<code>:913-915</code> for the hook, <code>:747</code> for <code>.gitignore</code>) - exactly one byte, never a line, and never a trailing-blank normalisation. For the pre-commit hook this binds on ALL pre-install content, including content with no trailing newline. For <code>.gitignore</code> it binds where the pre-install content is newline-terminated and does not end in a blank run; §3 records why the remaining <code>.gitignore</code> shapes are unrecoverable from <code>uninstall.sh</code> and which install-side line destroys them.</li><li>1.8 Uninstall MUST be idempotent over cycles: for any N &gt;= 1, N install/uninstall cycles MUST leave an operator's pre-existing hook and <code>.gitignore</code> byte-identical to their pre-install content, over the shapes §1.7 binds on.</li><li>1.9 No path present before install may be MISSING after uninstall, and none may be MUTATED except by the removal of content proven ours by its own text under §1.1-§1.5. The exception is on content only: no file and no directory present before install may be deleted, whatever its content proves.</li><li>1.10 The suite MUST NOT assert that the pre-121 leftovers are correct. TASK-IMP-106's <code>t22_uninstall_behavior_unchanged</code> MUST be re-pointed at the set §1.1-§1.7 define and renamed, because a test whose baseline has deliberately moved cannot honestly be named <code>_unchanged</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - after an install writing all six channels, every surviving symlink under them is read with <code>readlink</code> and NONE has a target naming <code>.cyberos/plugin/skills/&lt;entry&gt;</code>; the test MUST assert all six channels separately and MUST FAIL if any entry is exempt by name, which today's <code>:112</code> guard makes true of <code>ship-tasks</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t23_direct_target_links_removed_all_channels</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>.agents/skills/{ship-tasks,task-author,task-audit}</code> chained links are removed, AND an operator's <code>.agents/skills/my-notes -&gt; ../../.claude/skills/my-notes</code> survives byte-identical; the test MUST FAIL if the chained rule removes any name outside <code>install.sh:649</code>'s three or fires under any channel but <code>.agents/skills/</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t24_chained_form_bounded_to_shared_cmds</code></li><li>[ ] AC 3 (traces_to: #1.3) - an operator's unmarked skill dir, an operator symlink whose target names neither form, AND an operator's BROKEN symlink whose target names neither form all survive byte-identical and are named as kept; the test MUST FAIL if an unresolvable target alone causes removal - test: <code>tools/install/tests/test_install_hygiene.sh::t25_unprovable_entries_kept_and_named</code></li><li>[ ] AC 4 (traces_to: #1.4) - where <code>.mcp.json</code> and <code>.cursor/mcp.json</code> are byte-identical to install's 105-byte form, after uninstall each file EXISTS, parses, carries zero registry entries, and contains no string naming <code>.cyberos/</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t26_sole_cyberos_registration_removed</code></li><li>[ ] AC 5 (traces_to: #1.5) - an <code>.mcp.json</code> carrying our registration plus an operator's own is byte-identical after uninstall AND the dead registration is reported with the file's path; the test MUST FAIL if the file is edited at all - test: <code>tools/install/tests/test_install_hygiene.sh::t27_operator_edited_mcp_kept_and_named</code></li><li>[ ] AC 6 (traces_to: #1.6) - a <code>.gitignore</code> install created, an <code>.mcp.json</code> emptied of our registration, and each of the six channel dirs INCLUDING an emptied <code>.agents/skills</code> all still EXIST after uninstall; the test MUST FAIL if any is removed for being empty, which today's <code>:118-119</code> makes true of <code>.agents/skills</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t28_containers_survive_when_emptied</code></li><li>[ ] AC 7 (traces_to: #1.7) - pre-existing hooks ending in no newline, one newline, and three newlines, plus a newline-terminated <code>.gitignore</code>, are each byte-identical to their pre-install bytes after one cycle, asserted with <code>cmp</code>; the test MUST FAIL on today's leak (hook 7 -&gt; 8, <code>.gitignore</code> 14 -&gt; 15) and on any rule that restores the no-newline hook to 7 bytes - test: <code>tools/install/tests/test_install_hygiene.sh::t29_block_strip_restores_byte_exact</code></li><li>[ ] AC 8 (traces_to: #1.8) - the hook (all three shapes) and the <code>.gitignore</code> are byte-identical to their pre-install bytes after each of 1, 2 and 3 cycles, witnessing the fixed point §1.8 asserts for all N; the test MUST compare bytes and not line counts, and MUST FAIL on today's 7 -&gt; 8 -&gt; 9 -&gt; 10 hook accumulation - test: <code>tools/install/tests/test_install_hygiene.sh::t30_strip_idempotent_over_cycles</code></li><li>[ ] AC 9 (traces_to: #1.9) - no path present before install is missing after uninstall, and none is mutated except by removal of content proven ours, across a fresh-git repo, a repo with a foreign hook, a repo with <code>core.hooksPath</code> set, a repo with a pre-existing EMPTY <code>.agents/skills</code>, a repo with a pre-existing hand-registered <code>.mcp.json</code>, and a repo uninstalled with <code>CYBEROS_UNINSTALL_KEEP_BRAIN=0</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t31_no_casualties</code></li><li>[ ] AC 10 (traces_to: #1.10) - <code>t22</code> is renamed <code>t22_uninstall_removal_set_pinned</code>, pins the set §1.1-§1.7 define, and passes; the test MUST FAIL if any assertion in the suite still pins a pre-121 leftover as correct - test: <code>tools/install/tests/test_install_hygiene.sh::t22_uninstall_removal_set_pinned</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>RETRACTED: \"an operator hook with NO trailing newline cannot be restored byte-exact.\"</strong> The prior draft asserted this as an impossibility proof and it is FALSE. Install's <code>cat &gt;&gt;</code> appends exactly <code>\\n</code> + block (<code>:851-853</code>: the heredoc's first line is blank), so the byte immediately before the marker is ALWAYS ours, for every shape. Deleting <strong>that byte</strong> rather than <strong>that line</strong> inverts the append. Measured this round, install replayed verbatim then each strip: line-oriented 6B -&gt; 7B DIFFERS / byte-oriented 6B -&gt; 6B BYTE-EXACT; newline-terminated 7B -&gt; 7B both; three-newline 9B -&gt; 9B both; no-newline-with-content-above 24B -&gt; 25B line / 24B -&gt; 24B byte. The information is not destroyed at append time - it survives as the presence or absence of the blank line above the marker, and install's append is injective on hook content. The prior author's error was a level confusion: the rule was reasoned about in line-space, where the no-newline case has no blank line to consume, and \"my line rule cannot\" became \"no rule can\". §1.7 therefore binds on ALL pre-install hook content.</li><li><strong>The impossibility is real, but it is <code>.gitignore</code>'s and its cause is on the install side.</strong> <code>install.sh:733-746</code>'s awk does not append to <code>.gitignore</code>, it REWRITES it: <code>:744</code> buffers each line and <code>print</code>s it back with a terminator, and <code>:745</code> trims the trailing blank run. Measured with that awk applied ALONE (no block, no separator): <code>*.log\\n</code> (6B) -&gt; 6B unchanged; <code>*.log\\n\\n\\n</code> (8B) -&gt; 6B; <code>*.log</code> (5B) -&gt; 6B. Three distinct pre-install files collapse to one post-install state, so no uninstall-side rule can tell them apart - this is the pigeonhole the prior draft claimed for the hook, sitting on the other file. §1.7 is scoped to the newline-terminated, no-trailing-blank-run shape for <code>.gitignore</code> because that is the measured domain where install is injective. Closing the rest means changing <code>install.sh:744-745</code>, which is out of scope here; recorded as an uncovered gap, not handed to a clause that cannot honour it.</li><li><strong>Limit of the byte-oriented rule as expressed in awk, found by testing it rather than assuming it.</strong> Where an operator has added content BELOW our block and that content lacks a trailing newline, an awk END clause re-adds one: measured want 17B, got 18B. Where the below-content is newline-terminated it is byte-exact (18B -&gt; 18B). Install always appends at EOF, so this shape only arises from an operator edit made between install and uninstall, and the residue lands on that later content, not on the pre-install bytes §1.7 and §1.9 speak about. Named because the rule is this author's own and its limits are the first thing that should be checked, not the last.</li><li><code>CYBEROS_UNINSTALL_KEEP_BRAIN=0</code> vs the default: in the default path <code>.cyberos/</code> SURVIVES (<code>:151</code> removes it, <code>:155-158</code> recreates it with BRAIN inside); under <code>=0</code> it does not. §1.1's predicate is textual, so it is the same rule in both, and both are covered by AC 9. This is the premise the first draft inverted; the fix is not to pick the other mode but to stop conditioning on the mode at all.</li><li>A symlink into <code>.cyberos/</code> whose target was ALREADY absent before uninstall (a prior partial install): still provably ours by target text, still removed. Unresolvable is not unowned.</li><li><strong>The chained form is bounded by name, and that is not a name exemption.</strong> <code>.agents/skills/&lt;cmd&gt; -&gt; ../../.claude/skills/&lt;cmd&gt;</code> names no <code>.cyberos/</code> path, so it is ours by construction (install writes exactly that shape at <code>:662</code>, for exactly the three names at <code>:649</code>) rather than by target text. §1.1's \"no entry may be exempt by name\" is therefore about the DIRECT form only - it is what removes <code>:112</code>'s guard. Widening the chained rule to every entry under <code>.agents/skills/</code> would keep the pattern and lose the justification: an operator's <code>.agents/skills/my-notes -&gt; ../../.claude/skills/my-notes</code> matches it exactly. Bounding it to <code>:649</code>'s three names under <code>.agents/skills/</code> is precisely today's shipped behaviour at <code>:94</code>+<code>:98</code> - genuinely carried forward unchanged, and now stated as a scope limit rather than left as an accident of the loop.</li><li><code>CYBEROS_COPY_SKILLS=1</code>: the five native channels' copy fallback (<code>:613</code>'s <code>|| cp -R</code>) writes NO <code>.cyberos-owned</code> marker - only <code>.agents/skills</code> gets one (<code>:672-675</code>). So a copied <code>ship-tasks</code> under <code>.claude/skills/</code> is not provably ours and §1.3 keeps and reports it. It is untidy, not broken: a copy is self-contained and dangles at nothing. Closing it means marking the copy at install time, which is an <code>install.sh</code> change and out of scope here. This is the one case a receipt would genuinely decide - see Alternatives, where a marker is preferred and both are deferred.</li><li><strong><code>.mcp.json</code> an operator hand-registered BEFORE install, byte-identical to ours.</strong> <code>install.sh:687</code> is create-if-absent, so install leaves it untouched; <code>:689</code>'s summary points operators at <code>.cyberos/mcp/README.md</code> to hand-register, so this is a supported path. §1.4 fires and removes the registration, mutating a path present before install. That is permitted by §1.9's carve-out and by nothing else: the registration's command names the machine we deleted, so the CONTENT is provably ours even though the FILE is not, and the file itself survives (§1.6). AC 9 pins the boundary - the file must still exist afterwards. An operator who wants the registration kept should keep the server.</li><li><code>.mcp.json</code> containing both our registration and an operator's: §1.5, not §1.4 - the file is not byte-identical to install's form, so it is left alone and the dead registration is reported. Surgically removing one key from arbitrary JSON needs a parser <code>uninstall.sh</code> does not have and <code>node</code> it must not require; reporting is what it can honestly do, and TASK-IMP-106's kept-list is the idiom for saying it.</li><li><strong>An operator's pre-existing EMPTY <code>.agents/skills</code> (or <code>.agents</code>).</strong> <code>uninstall.sh:118-119</code> rmdirs both when they empty, with no ownership check - rmdir's failure on a non-empty dir is the only guard, and it does not fire when the dir is an operator's and happens to be empty. That is a §1.9 casualty today, not a tension: a path present before install goes missing. §1.6 removes the rmdir rather than carving the clause around it. The cost is accepted and named: where install CREATED <code>.agents/skills</code>, an empty dir is now left behind, exactly as <code>.claude/skills</code> and the other four already are. An emptied container is inert.</li><li>An operator hook whose pre-install content already ended in multiple newlines: restore is byte-exact to THAT content - §1.7 consumes exactly one byte, never normalises. Measured byte-exact for a hook ending in three newlines (9B -&gt; 9B). Note this does NOT hold for <code>.gitignore</code>, where <code>:745</code> already trimmed those newlines at install time - see the second bullet.</li><li><code>CYBEROS_GLOBAL_SKILLS=1</code> copies <code>ship-tasks</code> into <code>$HOME/.claude/skills</code> and three siblings (<code>:634-637</code>); uninstall never touches them. Outside the repo is outside this task's contract (\"leave the repo as it found it\"); named so it is not mistaken for coverage.</li><li>Uninstall run twice: the second run finds nothing provably ours and removes nothing; §1.8's byte-identity still holds.</li><li>§1.9 compares post-uninstall bytes to PRE-INSTALL bytes, which is only meaningful for a file the operator did not edit between install and uninstall. An operator who edits their hook after install owns that edit and uninstall must not revert it; AC 9's fixtures do not edit between the two, and that is an assumption of the measurement, not a proven property of the code.</li><li>Security-class: ownership is decided by <code>readlink</code> output, our own marker file, our marker pair, and a command string naming our path - never by a path pattern an operator could forge by naming a directory <code>ship-tasks</code>. No user-supplied string is interpolated into a removal command; nothing printed is executed.</li></ul>";
