window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-121"]="<h1>TASK-IMP-121: Uninstall must leave the repo as it found it</h1>\n<h2>Summary</h2>\n<p>A sibling task, TASK-IMP-126, has shipped (status <code>done</code>) and made uninstall the inverse of install for three artifacts: the dangling skill symlinks across every agent family, the two dead MCP registrations, and the pre-commit hook's leading blank separator. This task is what 126 did NOT fix - the uninstall-completeness remainder - and it leads with a live DATA-LOSS bug 126 never touched.</p>\n<p><code>uninstall.sh:141-142</code> <code>rmdir</code>s <code>.agents/skills</code> and then <code>.agents</code> for emptiness alone, with no ownership test, so an operator's PRE-EXISTING empty <code>.agents/skills</code> is destroyed on uninstall - reproduced end to end at HEAD, and <code>.agents</code> with it when <code>CYBEROS_AGENTS</code> filters the antigravity pointer off. Two smaller defects are byte-level and remain after 126: uninstall's <code>.gitignore</code> strip (<code>:106</code>, still the old <code>sed</code>) leaves the one separator byte install writes, and 126's shipped hook-strip - byte-exact for a newline-terminated hook - still adds one byte to a foreign hook that has no trailing newline. So a directory an operator owns does not come back at all, and two operator files do not come back byte-identical.</p>\n<h2>Problem</h2>\n<p>Measured at HEAD (payload built with <code>build.sh</code>; real <code>install.sh</code>/<code>uninstall.sh</code>; GNU awk 5.1.0, bash 5.1). TASK-IMP-126 already removed every dangling skill symlink (its §1.2, <code>uninstall.sh:122-166</code>), both MCP registrations (its §1.1, <code>uninstall.sh:168-186</code>), and the hook separator for a newline-terminated hook (its §1.3, the awk at <code>uninstall.sh:87-93</code>). Those three are OUT of this task. What is left is one data-loss bug and two byte-leaks.</p>\n<p><strong>1. A directory an operator owns is destroyed - the lead, and a live data-loss bug.</strong> <code>uninstall.sh:141-142</code> runs <code>rmdir \"$root/.agents/skills\"</code> then <code>rmdir \"$root/.agents\"</code>, each <code>2&gt;/dev/null || true</code>. Neither carries an ownership test; <code>rmdir</code>'s refusal on a non-empty directory is the ONLY guard, and it does not fire when the directory is an operator's and happens to be empty. Reproduced end to end: a fresh git repo with an operator's pre-existing empty <code>.agents/skills</code>, default install (which drops three symlinks into it), then uninstall - the symlinks are removed and <code>.agents/skills</code> is <code>rmdir</code>'d GONE. In the default config <code>.agents</code> survives, but only incidentally: the <code>:597</code> antigravity pointer writes <code>.agents/rules/cyberos.md</code>, no uninstall path removes it, so <code>.agents</code> is never empty and <code>:142</code> no-ops. Filter antigravity off (<code>CYBEROS_AGENTS=agents</code>) and <code>find .agents</code> after uninstall returns nothing - the operator's <code>.agents</code> goes too. This is a casualty a byte-level harness that never seeds a pre-existing <code>.agents/skills</code> cannot see.</p>\n<p><strong>Install already records ownership, one directory too low.</strong> The discipline uninstall uses everywhere else is: read the artifact, decide from what it says. For a copy-fallback skill dir that discipline is a <code>.cyberos-owned</code> marker: <code>install.sh:684</code> writes one into each <code>.agents/skills/&lt;cmd&gt;</code> copy dir, and <code>uninstall.sh:130</code>/<code>:161</code> read it to remove only installer copies. But that marker sits on the CHILD skill dir (<code>$_sdest</code>, <code>install.sh:662</code>), never on the parent <code>.agents/skills</code> or on <code>.agents</code> - and in a default (symlink) install it is not written at all. So the two directories <code>:141-142</code> can delete carry no proof of who created them, which is exactly why the <code>rmdir</code> is blind. The fix has two halves: install writes a parent marker when - and only when - install itself creates the directory; uninstall then removes those two dirs only when they carry that marker and are empty.</p>\n<p><strong>The create-test must precede the <code>mkdir</code>, and each level needs its own.</strong> Measured: <code>mkdir -p</code> on an already-existing directory exits 0 silently, so a marker written after <code>mkdir -p \"$d\"</code> would stamp an operator's pre-existing directory as ours - the current bug inverted, and worse, because it would manufacture a licence to delete. And <code>mkdir -p a/b</code> creates both levels, so a marker in <code>.agents/skills</code> proves nothing about <code>.agents</code>; compounding it, <code>.agents</code> has a SECOND creator at <code>install.sh:575</code> (the <code>:597</code> antigravity pointer, <code>mkdir -p</code> on dirname <code>.agents/rules</code>) which runs before <code>:669</code>'s <code>mkdir -p \"$root/.agents/skills\"</code>. Each directory must be tested for its own prior existence before the first line that could create it.</p>\n<p><strong>Two dirs get markers; five get their errant <code>rmdir</code> removed.</strong> <code>grep -n rmdir uninstall.sh</code> returns <code>:141</code>, <code>:142</code>, <code>:165</code> and <code>:226</code> (<code>:226</code> is the BRAIN-restore tmpdir, unrelated). <code>:141-142</code> are the <code>.agents</code> pair, which install CREATES and wants to reclaim cleanly, so they get the marker mechanism (§1.1-1.3): delete only what we proved we created. <code>:165</code> is the per-family loop's <code>rmdir \"$root/$_sd\"</code> over the five native channel parents (<code>.claude/skills</code>, <code>.grok/skills</code>, <code>.commandcode/skills</code>, <code>.codex/skills</code>, <code>.opencode/skill</code>) - added by TASK-IMP-126 and absent pre-126, and reproduced destroying an operator's pre-existing empty <code>.claude/skills</code> end to end at HEAD. Those parents were never reclaimed pre-126, so the fix is not a marker but the REMOVAL of the blind <code>rmdir</code> (§1.6), restoring the pre-126 leave-in-place. A marker there would be a file nothing reads - a new permanent leftover; removing the <code>rmdir</code> is both smaller and the pre-126 status quo. So the same data-loss class (<code>rmdir</code> on emptiness with no ownership test) is closed at all four sites: the <code>.agents</code> pair by marker gate, the five native parents by deleting the <code>rmdir</code> IMP-126 added.</p>\n<p><strong>2. The <code>.gitignore</code> separator leak.</strong> <code>install.sh:756</code> writes <code>[ -s \"$gi.cyberos.tmp\" ] &amp;&amp; printf '\\n'</code> before the managed block, and <code>uninstall.sh:106</code>'s <code>sed</code> range starts AT the <code># &gt;&gt;&gt; cyberos</code> marker, so the separator above it survives. IMP-126 fixed the hook this way but not <code>.gitignore</code>. Measured by replaying real install+uninstall on a pre-existing newline-terminated <code>.gitignore</code>: <strong>20 -&gt; 21 bytes</strong>, holding at 21 across three cycles (<code>install.sh:742-754</code>'s awk trims the trailing blank run on re-install, so the leak is one-time, not accumulating). Still a mutation of an operator's file, and the fix belongs to the strip.</p>\n<p><strong>3. The hook, only for content with no trailing newline.</strong> IMP-126's awk (<code>uninstall.sh:87-93</code>) is byte-exact for a foreign hook that ends in a newline (measured 34 -&gt; 34), which is what its arm <code>t_hook_strip_byte_identical_across_cycles</code> proves. It is NOT byte-exact for a hook with no trailing newline: measured <strong>33 -&gt; 34 bytes</strong> (<code>+1</code>). Install's <code>cat &gt;&gt;</code> appends its leading separator directly after the last unterminated line, so there is no blank LINE for the awk to hold, and awk's <code>print</code> re-terminates that last surviving line. The information is not destroyed - install's append stays invertible - but only a byte-level strip (delete the block plus the one newline byte immediately before the <code># &gt;&gt;&gt;</code> marker) recovers it; the line-oriented awk cannot.</p>\n<h2>Proposed Solution</h2>\n<p>Four independent, testable changes, each grounded in a re-verified HEAD citation.</p>\n<p><strong>Gate the container <code>rmdir</code> on a parent marker install writes on creation (the data-loss fix).</strong> <code>install.sh</code> writes a <code>.cyberos-owned</code> marker into <code>.agents/skills</code>, and separately into <code>.agents</code>, when and only when install itself creates that directory - deciding creation by testing the directory's own existence BEFORE the <code>mkdir -p</code> that could create it (<code>:669</code> for <code>.agents/skills</code>; <code>:575</code>-via-<code>:597</code>, which runs first, for <code>.agents</code>). <code>uninstall.sh:141-142</code>'s unconditional <code>rmdir</code> is replaced by: remove the parent marker, then <code>rmdir</code> only if the directory now carries our marker and is empty; a directory with no marker is kept and reported as kept. Where install created the directory, uninstall takes it back and leaves nothing behind; where the operator's directory predated install, it survives. This is TASK-IMP-094's mechanism applied one directory up.</p>\n<table><thead><tr><th>directory</th><th>proof it is ours</th><th>consequence</th></tr></thead><tbody><tr><td>operator's pre-existing <code>.agents/skills</code></td><td>no parent marker</td><td>kept + reported</td></tr><tr><td>install-created <code>.agents/skills</code></td><td>parent marker (§1.1)</td><td>marker removed, then <code>rmdir</code> if empty</td></tr><tr><td>operator's pre-existing <code>.agents</code></td><td>no parent marker</td><td>kept + reported</td></tr><tr><td>install-created <code>.agents</code> (marker kept dir)</td><td>parent marker (§1.1)</td><td>marker removed even though the dir is kept (rules pointer survives)</td></tr></tbody></table>\n<p><strong>Delete the native-parent <code>rmdir</code> IMP-126 added (the other half of the data-loss close).</strong> <code>uninstall.sh:165</code>'s per-family <code>rmdir \"$root/$_sd\"</code> removes an operator's pre-existing empty <code>.claude/skills</code> (and the other four native parents) once our managed link is cleared - the same blind-emptiness deletion as <code>:141-142</code>, introduced by IMP-126 and absent pre-126. Remove that one line (§1.6), so an emptied native channel dir is left in place like any other inert container. No marker is written or read there: pre-126 uninstall never reclaimed these, so leaving them is both correct and the status quo, and it keeps IMP-126's link removal intact.</p>\n<p><strong>Strip the <code>.gitignore</code> separator with the block, by the byte.</strong> The <code>.gitignore</code> strip must also delete the single <code>\\n</code> byte install writes immediately before the <code># &gt;&gt;&gt; cyberos</code> marker (<code>install.sh:756</code>), restoring a pre-existing newline-terminated <code>.gitignore</code> byte-identical across any number of cycles. Same look-behind shape IMP-126 already uses for the hook, ported to the <code>.gitignore</code> strip.</p>\n<p><strong>Make the hook strip byte-exact for a hook with no trailing newline.</strong> Refine the strip so a foreign hook is byte-identical to its pre-install content even when that content has no trailing newline: delete the block together with the one separator byte and do not re-terminate the last surviving line. The newline-terminated case (already byte-exact, IMP-126) must stay byte-exact.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Fold this back into TASK-IMP-126.</strong> Impossible: 126 is <code>done</code> and shipped. This is the remainder task, and it leads with a defect 126's cone (symlinks/MCP/hook-core) never addressed.</li><li><strong>Option A - never delete the parent (remove <code>:141-142</code> outright).</strong> Safe and honest, but where install CREATED <code>.agents/skills</code> it leaves an empty directory behind in a repo it promised to leave as it found it - a permanent false positive traded for a transient one. Rejected: measurement shows creation is knowable (install already writes a child marker), so the blanket refusal is not forced.</li><li><strong>Option B - leave the bug, scope the clause around <code>:141-142</code>.</strong> Rejected by the operator, and it fails on its own terms: a path present before install still goes missing, so re-scoping the clause hides the casualty rather than closing it.</li><li><strong>Have install write a RECEIPT/manifest of what it created.</strong> Rejected in favour of the marker: the marker is local to the artifact, needs no parser, survives the operator moving the repo, and install ALREADY writes one at <code>:684</code>. Both share the one hazard - a record that wrongly claims creation would delete an operator's dir - and both close it the same way: test existence BEFORE <code>mkdir</code>, never after.</li><li><strong>Fix the <code>.gitignore</code> and hook leaks on the install side (drop the separator).</strong> Rejected for the same reason IMP-126 rejected it for the hook: the separator is a deliberate readability break in files an operator reads, and the defect is that uninstall does not undo what install does - the inverse belongs in uninstall, next to the strip that already exists. Rewriting install's <code>.gitignore</code> awk (<code>:742-754</code>) is also a strictly larger change than the two-line strip refinement.</li><li><strong>Trim trailing blank lines after the strip instead of consuming exactly one byte.</strong> Rejected: an operator hook or <code>.gitignore</code> whose pre-install content legitimately ends in blanks would come back with fewer. The correct rule removes exactly one byte immediately above the marker.</li><li><strong>Mark all seven <code>rmdir</code>'d dirs, not just the <code>.agents</code> pair.</strong> The five native channel parents ARE <code>rmdir</code>'d - at <code>:165</code>, by IMP-126's per-family loop - so they are in the data-loss class too (the audit-caught correction). But marking them is the wrong fix: pre-126 uninstall never removed them, so the minimal correct close is to DELETE the <code>rmdir</code> IMP-126 added (§1.6), not to add five markers that read to nothing and survive as new leftovers. Markers are reserved for the <code>.agents</code> pair, which install genuinely creates and wants to reclaim; the native parents just stop being pruned.</li><li><strong>Effort 9 hours.</strong> install-side create-detection + the two parent markers (two dirs, two creation sites, pre-<code>mkdir</code> tests) 1.5h; uninstall-side marker-gated prune + marker-removal sequencing 1h; delete the native-parent <code>rmdir</code> at <code>:165</code> (§1.6) 0.5h; <code>.gitignore</code> byte-exact strip 1h; hook no-trailing-newline refinement 1h; six new suite arms (each a distinct fixture) 4h. Sum 9. Lower than the pre-126 draft's 12h because the symlink-loop rewrite, the MCP partition and the hook separator core all shipped with 126.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: every container that existed BEFORE install still exists after uninstall - including an operator's pre-existing empty <code>.agents/skills</code>, <code>.agents</code>, and any native channel dir (<code>.claude/skills</code>, <code>.grok/skills</code>, <code>.commandcode/skills</code>, <code>.codex/skills</code>, <code>.opencode/skill</code>) - and every <code>.agents</code>-pair container install created and marked is either removed with its marker or kept and reported. Baseline measured at HEAD: an operator's pre-existing empty <code>.agents/skills</code> is DELETED by <code>:141</code> and their empty <code>.claude/skills</code> by <code>:165</code> (both reproduced end to end); with <code>CYBEROS_AGENTS=agents</code> their <code>.agents</code> is deleted by <code>:142</code> as well.</li><li>Primary: an operator's pre-existing newline-terminated <code>.gitignore</code>, and a foreign pre-commit hook with no trailing newline, are byte-identical to their pre-install bytes after 1 and after 3 cycles, asserted with <code>cmp</code>. Baseline: <code>.gitignore</code> 20 -&gt; 21 (one-time); hook 33 -&gt; 34 (one-time +1, stays 34 across cycles).</li><li>Guardrail: no <code>.cyberos-owned</code> file exists anywhere under the repo after uninstall, including on an install-created <code>.agents</code> that is KEPT because its rules pointer survives. Baseline: not applicable today (install writes markers only into copy dirs uninstall removes wholesale); it becomes load-bearing the moment §1.1 writes markers into two dirs uninstall may keep.</li><li>Guardrail: the create-detection marks NO pre-existing directory - a marker on an operator's dir would satisfy the removal half while manufacturing the casualty this task exists to remove.</li><li>Guardrail: the three IMP-126 artifacts stay fixed - zero surviving skill symlink targets naming <code>.cyberos/</code>, zero cyberos MCP registrations, and a newline-terminated foreign hook still byte-identical across cycles. This task must not regress 126.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>uninstall.sh:141-142</code>'s container <code>rmdir</code>, now gated on a parent <code>.cyberos-owned</code> marker with the marker removed before the <code>rmdir</code>; <code>install.sh</code>'s create-detection and the two parent markers it writes (<code>.agents/skills</code> at <code>:669</code>; <code>.agents</code> at <code>:575</code>-via-<code>:597</code>); <code>uninstall.sh:165</code>'s per-family <code>rmdir</code> over the five native channel parents, REMOVED so an operator's pre-existing empty native dir survives; <code>uninstall.sh:106</code>'s <code>.gitignore</code> strip, made byte-exact for the separator; <code>uninstall.sh:87-93</code>'s hook strip, made byte-exact for a no-trailing-newline hook; and new arms in <code>test_install_hygiene.sh</code> for each.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li><strong>The three artifacts TASK-IMP-126 shipped, explicitly out.</strong> (a) The dangling skill symlinks across all agent families (IMP-126 §1.2, <code>uninstall.sh:122-166</code>). (b) The two dead MCP registrations <code>.mcp.json</code> / <code>.cursor/mcp.json</code> (IMP-126 §1.1, <code>uninstall.sh:168-186</code>). (c) The pre-commit hook separator leak for a NEWLINE-TERMINATED foreign hook (IMP-126 §1.3). This task inherits (c)'s awk and only closes its no-trailing-newline remainder; it does not re-spec the separator core.</li><li><strong>The child copy-fallback marker at <code>install.sh:684</code>.</strong> It stays where it is and keeps proving the ENTRY copy is ours (IMP-094, read by <code>uninstall.sh:130</code>/<code>:161</code>). This task's parent marker is additional and distinct.</li><li><strong><code>.gitignore</code> shapes other than newline-terminated.</strong> A <code>.gitignore</code> with no trailing newline, or one ending in a blank run, is collapsed to one post-install state by <code>install.sh:742-754</code>'s awk BEFORE uninstall runs - a true pigeonhole no uninstall-side rule can invert. §1.4 is scoped to the shape where install is injective; closing the rest means changing install's rewrite, a larger change (§3).</li><li><strong><code>CYBEROS_COPY_SKILLS=1</code> copies under the five native channels</strong> are entry-level and carry IMP-126's <code>.cyberos-owned</code> marker, removed by IMP-126's family loop; §1.6 changes only the parent-dir <code>rmdir</code>, not that entry cleanup, so folding entry-level copy policy in would change no deletion this task authorises.</li><li><strong><code>t22_uninstall_behavior_unchanged</code> is NOT re-pointed.</strong> Measured self-relative (it compares the real uninstall against a summary-stripped copy of the same script) and immune to this task's edits, which sit before the <code>:230</code> summary anchor. Its baseline does not move and its name stays honest.</li><li>What uninstall keeps by design (<code>docs/tasks/</code>, <code>docs/status/</code>, <code>CHANGELOG.md</code>, the agent/rules pointer files, BRAIN) and the <code>$HOME</code> global skills behind <code>CYBEROS_GLOBAL_SKILLS=1</code> (outside the repo, outside the \"leave the repo as it found it\" contract).</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-106 (status <code>done</code>): 106 landed the uninstall summary, the <code>_removed_list</code>/<code>_kept</code> reporters, and the <code>t22</code> arm on the same <code>uninstall.sh</code> + <code>test_install_hygiene.sh</code> surface this task edits, so 106's file state is the base these edits build on. The pre-126 justification for the dependency - \"121 must re-point 106's <code>t22</code>\" - is RETIRED: re-verified at HEAD, <code>t22</code> is self-relative and unaffected by this task, so no re-point is authored. Shares surface with TASK-IMP-094 (the <code>.cyberos-owned</code> marker mechanism this task lifts one directory up) and TASK-IMP-126 (the sibling that shipped the symlink/MCP/hook-core; this task is its remainder).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> whole-document rewrite to a NARROWED scope after TASK-IMP-126 shipped. IMP-126 (<code>done</code>) made uninstall the inverse of install for the skill symlinks, the MCP registrations, and the newline-terminated hook separator; those three are removed from this task, which becomes the remainder and leads with the data-loss <code>rmdir</code>. EVERY <code>file:line</code> citation was re-derived at HEAD by this author - IMP-126 added ~52 lines to <code>uninstall.sh</code> (230 -&gt; 282), so every pre-126 <code>uninstall.sh</code> number was stale (the <code>rmdir</code> moved :118-119 -&gt; :141-142; the <code>.gitignore</code> strip is now :106; the hook awk is :87-93; and IMP-126 also ADDED a new native-parent <code>rmdir</code> at :165). Facts re-measured by this author against the real payload: the data-loss deletion reproduced end to end in both <code>CYBEROS_AGENTS</code> configs; the two <code>.agents</code> creators (<code>install.sh:575</code>-via-<code>:597</code> and <code>:669</code>) and their order; the marker written one level too low (<code>:684</code> into <code>$_sdest</code>); both <code>mkdir -p</code> traps; the <code>.gitignore</code> leak (20 -&gt; 21 bytes, one-time); the required HOOK byte-exactness test (newline-terminated 34 -&gt; 34 EXACT, no-trailing-newline 33 -&gt; 34 DIFFERS, so the hook stays in scope as §1.5); and <code>mcp_json</code>'s 105 bytes. <strong>One pre-126 claim did not survive re-verification and is dropped rather than carried:</strong> that <code>t22</code> pins the leftover set as \"today's behavior\" and must be re-pointed - at HEAD <code>t22</code> compares the real uninstall against a summary-stripped copy of itself, is immune to edits before the <code>:230</code> anchor, and needs no change.</li><li><strong>Human review:</strong> the option-C decision (delete a container only when we can prove we created it) is a recorded operator verdict carried from the pre-126 lineage and not relitigated. The scope split against IMP-126 was set at that task's PLAN gate. Both HITL gates remain recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>install.sh</code> MUST write a <code>.cyberos-owned</code> marker into <code>.agents/skills</code> and, separately, into <code>.agents</code>, when and only when install itself CREATES that directory, deciding creation by testing that directory's own existence BEFORE any <code>mkdir -p</code> that could create it (<code>:669</code> for <code>.agents/skills</code>; <code>:575</code> reached via the <code>:597</code> antigravity pointer, which runs first, for <code>.agents</code>). A directory that existed before install MUST NOT be marked - <code>mkdir -p</code> succeeds silently on an existing directory and creates every level of a chain, so a post-<code>mkdir</code> test would falsely mark an operator's directory. Each marker's text MUST name the directory it marks and MUST carry the adoption sentence <code>install.sh:681-684</code> already gives the child copy dirs.</li><li>1.2 <code>uninstall.sh</code> MUST replace the unconditional <code>rmdir</code> at <code>:141-142</code> so that <code>.agents/skills</code> and <code>.agents</code> are removed only when the directory carries the <code>.cyberos-owned</code> marker §1.1 writes AND is empty once our content and that marker are gone; emptiness alone MUST NOT authorise removing either directory, so an unmarked <code>.agents/skills</code> or <code>.agents</code> MUST be kept and reported as kept.</li><li>1.3 <code>uninstall.sh</code> MUST remove a <code>.cyberos-owned</code> marker it finds on <code>.agents/skills</code> or on <code>.agents</code> BEFORE attempting that directory's <code>rmdir</code> and whether or not the <code>rmdir</code> then succeeds, so no marker survives uninstall even where the directory is kept (an install-created <code>.agents</code> whose <code>:597</code> rules pointer keeps it non-empty); and a directory whose marker the operator has deleted MUST be kept and reported as kept, honouring the adoption promise for the parent.</li><li>1.4 <code>uninstall.sh</code>'s <code>.gitignore</code> strip (<code>:106</code>) MUST also delete the single <code>\\n</code> separator byte <code>install.sh:756</code> writes immediately before the <code># &gt;&gt;&gt; cyberos</code> marker, restoring a pre-existing newline-terminated <code>.gitignore</code> byte-identical to its pre-install content, for any N &gt;= 1 install/uninstall cycles. §3 records why the non-newline-terminated <code>.gitignore</code> shapes are unrecoverable from <code>uninstall.sh</code> and which install-side line destroys them.</li><li>1.5 <code>uninstall.sh</code>'s hook strip (<code>:87-93</code>) MUST restore a foreign pre-commit hook byte-identical to its pre-install content even when that content has NO trailing newline - deleting the managed block together with the single separator byte install writes before the <code># &gt;&gt;&gt;</code> marker and NOT re-terminating the last surviving line - while keeping the newline-terminated case (already byte-exact under IMP-126) byte-exact.</li><li>1.6 <code>uninstall.sh</code> MUST NOT <code>rmdir</code> any of the five native channel parents (<code>.claude/skills</code>, <code>.grok/skills</code>, <code>.commandcode/skills</code>, <code>.codex/skills</code>, <code>.opencode/skill</code>): the per-family loop's <code>rmdir \"$root/$_sd\"</code> at <code>:165</code> (added by TASK-IMP-126, which pre-126 uninstall did not have) MUST be removed, so an operator's pre-existing EMPTY native channel dir survives uninstall - the same emptied-container-is-inert rule this task applies to <code>.gitignore</code> and <code>.mcp.json</code>, and the same data-loss class as <code>:141-142</code>. These parents are NOT marked (install writes no parent marker there and none is needed once the blind <code>rmdir</code> is gone); removing the <code>rmdir</code> restores the pre-126 behavior of leaving them in place.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - after install into a repo whose <code>.agents</code> and <code>.agents/skills</code> PRE-EXISTED, NEITHER carries <code>.cyberos-owned</code>; after install into a repo with neither directory, BOTH carry it with text naming the directory and containing the adoption sentence; the arm MUST cover <code>.agents</code> created alone via the <code>:597</code> pointer under <code>CYBEROS_AGENTS=antigravity</code> and <code>.agents</code> created as a side effect of <code>:669</code>'s <code>mkdir -p</code> chain under <code>CYBEROS_AGENTS=agents</code>, and MUST FAIL if any pre-existing directory is marked - test: <code>tools/install/tests/test_install_hygiene.sh::t23_parent_marker_written_only_on_create</code></li><li>[ ] AC 2 (traces_to: #1.2) - after install into a repo whose <code>.agents/skills</code> and <code>.agents</code> PRE-EXISTED empty, both still EXIST after uninstall and are reported kept; after install into a repo with neither, both are REMOVED; the arm MUST FAIL if emptiness alone removes an unmarked dir, which today's <code>:141-142</code> makes true (reproduced 5a/5b) - test: <code>tools/install/tests/test_install_hygiene.sh::t24_containers_removed_only_when_marked</code></li><li>[ ] AC 3 (traces_to: #1.3) - after uninstall NO <code>.cyberos-owned</code> file exists anywhere under the repo, including on an install-created <code>.agents</code> that is KEPT because its <code>:597</code> rules pointer survives; and an install-created <code>.agents/skills</code> whose marker the operator deleted still EXISTS and is reported kept; the arm MUST FAIL if a marker survives on a kept directory or if an adopted directory is removed - test: <code>tools/install/tests/test_install_hygiene.sh::t25_marker_removed_and_adoption_honoured</code></li><li>[ ] AC 4 (traces_to: #1.4) - a pre-existing newline-terminated <code>.gitignore</code> is byte-identical to its pre-install bytes after 1 and after 3 cycles, asserted with <code>cmp</code>; the arm MUST FAIL on today's 20 -&gt; 21 leak and MUST leave an install-CREATED <code>.gitignore</code> (empty tmp, no separator) equally clean - test: <code>tools/install/tests/test_install_hygiene.sh::t26_gitignore_strip_byte_exact</code></li><li>[ ] AC 5 (traces_to: #1.5) - a foreign pre-commit hook with NO trailing newline is byte-identical to its pre-install bytes after uninstall and across 3 cycles, asserted with <code>cmp</code>, AND the newline-terminated control stays byte-identical; the arm MUST FAIL on today's 33 -&gt; 34 leak and on any rule that adds a byte to the no-trailing-newline shape - test: <code>tools/install/tests/test_install_hygiene.sh::t27_hook_strip_byte_exact_no_trailing_newline</code></li><li>[ ] AC 6 (traces_to: #1.6) - after install into a repo whose native channel dir (e.g. <code>.claude/skills</code>) PRE-EXISTED empty, that directory still EXISTS after uninstall; the arm MUST FAIL on today's <code>:165</code> <code>rmdir</code>, which destroys it (reproduced end to end at HEAD), and MUST confirm the managed skill link inside it is still removed (this task must not regress IMP-126's link cleanup) - test: <code>tools/install/tests/test_install_hygiene.sh::t28_native_channel_parent_survives</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong><code>mkdir -p</code> succeeds silently on an existing directory</strong>, so a marker written after the <code>mkdir</code> would stamp an operator's pre-existing dir as ours - the bug inverted, and worse, because it manufactures a licence to delete. Measured exit 0. The create-test in §1.1 therefore runs BEFORE the <code>mkdir</code>, in the shape <code>[ -d \"$d\" ] || { mkdir -p \"$d\"; marker \"$d\"; }</code>.</li><li><strong><code>mkdir -p a/b</code> creates both levels</strong>, so <code>.agents</code> cannot inherit <code>.agents/skills</code>'s proof; and <code>.agents</code> has a second creator at <code>install.sh:575</code> (via the <code>:597</code> pointer, dirname <code>.agents/rules</code>) that runs before <code>:669</code>. Measured with <code>CYBEROS_AGENTS=antigravity</code>: <code>.agents</code> + <code>.agents/rules/cyberos.md</code> created with NO <code>.agents/skills</code>. §1.1 tests each directory's own existence before any <code>mkdir</code> that could create it.</li><li><strong><code>:142</code> is reachable, so gating it is not dead code.</strong> In the default config <code>.agents</code> survives regardless of any marker, because the <code>:597</code> pointer's <code>.agents/rules/cyberos.md</code> keeps it non-empty and no uninstall path removes it. But measured with <code>CYBEROS_AGENTS=agents</code> (antigravity filtered off), <code>find .agents</code> after uninstall returns NOTHING - the operator's own <code>.agents</code> and <code>.agents/skills</code> are both destroyed. Both dirs need their own marker and their own gate.</li><li><strong>The marker on a KEPT directory would be a new leftover, so §1.3 removes it either way.</strong> Where install created <code>.agents</code> and the antigravity pointer is installed, uninstall keeps <code>.agents</code> (its rules pointer survives) - a marker left inside would be a permanent file this task's title forbids. The sequencing is forced in the other direction too: the marker is a file, so while it is present the dir is never empty and the <code>rmdir</code> could never fire.</li><li><strong>Re-install after adoption keeps the adoption.</strong> If the operator deletes <code>.agents/skills/.cyberos-owned</code> to adopt the dir and later re-installs, §1.1's <code>[ -d \"$d\" ]</code> test is TRUE, so install neither <code>mkdir</code>s nor re-marks, and the dir stays adopted. The same test makes the marker write idempotent.</li><li><strong>The <code>.gitignore</code> pigeonhole is install's, not uninstall's.</strong> <code>install.sh:742-754</code>'s awk REWRITES <code>.gitignore</code> and trims the trailing blank run, so <code>*.log\\n</code>, <code>*.log\\n\\n\\n</code> and <code>*.log</code> (no trailing newline) all collapse to one post-install state that no uninstall-side rule can tell apart. §1.4 is scoped to the newline-terminated, no-trailing-blank-run shape - the measured domain where install is injective. Closing the rest means changing <code>install.sh:744-754</code>, out of scope.</li><li><strong>The <code>.gitignore</code> install CREATED (empty tmp) has no separator to leak.</strong> <code>install.sh:715</code> seeds it, <code>install.sh:756</code>'s <code>[ -s tmp ]</code> guard writes no separator, and the strip leaves it clean. AC 4 pins that this stays true.</li><li><strong>A hook whose pre-install content already ended in multiple newlines</strong> must come back byte-exact to THAT content - §1.5 consumes exactly one byte, never normalises. (This does NOT hold for <code>.gitignore</code>, where <code>install.sh:754</code> already trimmed trailing blanks at install time.)</li><li><strong>The native parent that held ONLY our link becomes an inert empty dir, and that is correct.</strong> IMP-126's family loop still removes the managed skill link inside a native channel dir; §1.6 stops only the PARENT <code>rmdir</code>, so a <code>.claude/skills</code> that contained nothing but our link is left behind empty. That is the same emptied-container-is-inert outcome as an emptied <code>.gitignore</code> or <code>.mcp.json</code> - a dir we no longer own the contents of, left in place because we cannot prove we created the dir itself. AC 6 pins that the operator's pre-existing native dir survives AND that the link inside is still gone (no IMP-126 regression).</li><li><strong>Security-class.</strong> Uninstall removes a container only when it carries OUR <code>.cyberos-owned</code> marker AND is empty; ownership is decided by the presence of that marker FILE (content install wrote), never by a path pattern or a directory NAME an operator could forge - so naming a directory <code>.agents/skills</code> or <code>ship-tasks</code> cannot by itself induce a deletion. No operator-supplied string is interpolated into the <code>rmdir</code>/<code>rm</code> targets; the two paths are fixed literals under <code>$root</code>, on the same in-repo confinement the other helpers use; nothing read from disk is executed. An operator who plants a marker in their own directory adopts the install semantics deliberately; §1.1 never writes one into a directory that predated install.</li></ul>";
