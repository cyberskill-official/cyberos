window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-121"]="<h1>TASK-IMP-121: Uninstall must leave the repo as it found it</h1>\n<h2>Summary</h2>\n<p>Uninstall deletes the machine and leaves things pointing at it. A byte-level harness on a fresh git repo found five dangling skill symlinks and two MCP registrations naming a server that is gone. Two further defects are byte-level: stripping our managed block from an operator's pre-commit hook leaves one newline behind every cycle, and doing the same to a pre-existing <code>.gitignore</code> leaves one behind once. Nothing here destroys operator content - the harness recorded zero casualties across its six cases - but the repo is left broken rather than merely untidy, and two files an operator owns do not come back byte-identical.</p>\n<h2>Problem</h2>\n<p>Measured 2026-07-18. <code>uninstall.sh:151</code> runs <code>rm -rf \"$CY\"</code>; <code>:155-158</code> then recreates <code>.cyberos/memory/</code> and moves the stashed BRAIN back. <strong><code>.cyberos/</code> survives by default</strong> - what is gone in both modes is <code>plugin/</code>, <code>mcp/</code>, <code>cuo/</code>, <code>lib/</code>, <code>docs-tools/</code> and the root scripts. Every pointer install writes targets <code>.cyberos/plugin/skills/...</code> or <code>.cyberos/mcp/cyberos-mcp.mjs</code>, and both live in the part that is removed under <code>CYBEROS_UNINSTALL_KEEP_BRAIN=1</code> and <code>=0</code> alike. So the question is never \"does <code>.cyberos/</code> still exist\" - it is \"does this pointer name a path we deleted\", and that is answered by the pointer's own text.</p>\n<p><strong>1. Five dangling skill symlinks.</strong> Install writes six channels: <code>.agents/skills/</code>, <code>.claude/skills/</code>, <code>.grok/skills/</code>, <code>.commandcode/skills/</code>, <code>.codex/skills/</code>, <code>.opencode/skill/</code> (<code>:623-627</code>, <code>:631-632</code>, <code>:650-679</code>; note OpenCode's singular <code>skill</code>). Uninstall's loop (<code>:94</code>) looks at two. <code>.agents/skills/ship-tasks</code> is removed by <code>:98-99</code>; <code>.claude/skills/{task-author,task-audit}</code> by <code>:113-114</code>. <code>.claude/skills/ship-tasks</code> is exempted by name at <code>:112</code> (<code>[ \"$_sc\" != \"ship-tasks\" ]</code>), and <code>.grok</code>, <code>.commandcode</code>, <code>.codex</code>, <code>.opencode</code> are never looked at - <code>grep -ci</code> returns <strong>0</strong> for each of them in <code>uninstall.sh</code>. Five survivors, all <code>-&gt; ../../.cyberos/plugin/skills/ship-tasks</code>, all confirmed dangling.</p>\n<p><strong>2. Two dead MCP registrations.</strong> <code>.mcp.json</code> and <code>.cursor/mcp.json</code> both register <code>node .cyberos/mcp/cyberos-mcp.mjs</code> (<code>:685-688</code>). Uninstall never unregisters - <code>grep -ci mcp</code> returns 0. Every agent reading those configs launches a missing server on every startup, in a repo the operator just cleaned.</p>\n<p><strong>3. The hook gains a newline every cycle.</strong> Install appends its block with <code>cat &gt;&gt; \"$hk\" &lt;&lt;'HOOK'</code> whose heredoc's <strong>first line is blank</strong> (<code>:851-853</code>), so the file gains <code>\\n# &gt;&gt;&gt; cyberos-status-hook v2</code>. <code>uninstall.sh:68</code>'s sed range starts <strong>at</strong> the marker, so the blank above it survives. Proven by the harness at 29 -&gt; 30 -&gt; 31 -&gt; 32 bytes over three cycles, final tail <code>exit 0\\n\\n\\n\\n</code>; reproduced independently today at 41 -&gt; 42 -&gt; 43 -&gt; 44 with the same tail. Unbounded: nothing ever trims it.</p>\n<p><strong>4. The same defect in <code>.gitignore</code>, which the draft missed.</strong> <code>install.sh:747</code> writes <code>[ -s \"$gi.cyberos.tmp\" ] &amp;&amp; printf '\\n'</code> before the block, and <code>uninstall.sh:80</code>'s sed range also starts at the marker. Measured today by replaying both verbatim: a pre-existing <code>.gitignore</code> goes <strong>14 -&gt; 15 bytes</strong> and stays at 15 across three cycles - <code>install.sh:745</code>'s awk trims the trailing blank run on re-install, so this leak is one-time rather than accumulating. It is still a mutation of an operator's file, and it is why the fix belongs to the strip, not to the hook.</p>\n<p>The <code>[ -s ]</code> guard at <code>:747</code> also explains the separate <code>.gitignore</code> symptom the harness saw: where install <strong>created</strong> the file (<code>:706</code>), the tmp is empty, no separator is written, and the strip leaves a 0-byte file.</p>\n<p><strong>Against section 6.</strong> <code>uninstall.sh:162</code> reads <code># 6. skill symlinks into .cyberos (dangling) - leave dirs; operator cleans</code>. The dangling is <strong>known and deliberate</strong>, and <code>:111</code>'s comment records the same posture for <code>.claude/skills/ship-tasks</code>. This is a decision to overturn, not a gap to fill. It is overturned because its premise does not survive contact with <code>:113-114</code>. The restraint section 6 encodes is the right instinct about <em>operator files</em> - and a symlink whose target text names our own machine is not an operator file. <code>:113-114</code> already trusts exactly that proof to remove <code>task-author</code> and <code>task-audit</code>, in the same run that leaves <code>ship-tasks</code> dangling. Section 6 is not restraint applied consistently; it is one channel's entry escaping a test its two siblings pass. Leaving a pointer to a file we deleted protects nobody.</p>\n<p><strong>What ownership can and cannot be proven from.</strong> The discipline <code>:98-99</code> and <code>:113-114</code> use is: read the artefact, decide from what it says. It generalises to every artefact here - a marker-delimited block, an MCP command naming our path, a <code>.cyberos-owned</code> file - and it stops exactly at the container. Whether install <em>created</em> <code>.gitignore</code> or <code>.mcp.json</code> is not recorded anywhere: <code>install.sh</code>'s <code>wrote</code>, <code>AGENT_FILES</code> and <code>SKILL_DIRS</code> (<code>:479</code>, <code>:580</code>, <code>:621</code>, <code>:686</code>) are shell variables that die with the process, and the only on-disk marker, <code>.cyberos-owned</code>, is written solely into <code>.agents/skills</code> copy-fallback dirs (<code>:672-675</code>). An operator who ran <code>touch .gitignore</code> before install is byte-indistinguishable from install's <code>: &gt; \"$gi\"</code>. So no clause below conditions on it.</p>\n<h2>Proposed Solution</h2>\n<p>Extend the readlink ownership test to every channel install writes, and apply the same read-the-artefact discipline to the MCP registrations and to both managed blocks. Four proofs, each evaluated at uninstall time from the artefact itself:</p>\n<table><thead><tr><th>artefact</th><th>proof it is ours</th></tr></thead><tbody><tr><td>skill symlink</td><td><code>readlink</code> target text naming <code>.cyberos/plugin/skills/&lt;entry&gt;</code>, or the chained <code>.claude/skills/&lt;entry&gt;</code> form (<code>:98</code>)</td></tr><tr><td><code>.agents/skills</code> copy dir</td><td>the <code>.cyberos-owned</code> file it carries (<code>:101</code>)</td></tr><tr><td><code>.gitignore</code> / hook block</td><td>the <code># &gt;&gt;&gt;</code> / <code># &lt;&lt;&lt;</code> marker pair delimiting it</td></tr><tr><td>MCP registration</td><td>the command naming <code>.cyberos/mcp/cyberos-mcp.mjs</code></td></tr></tbody></table>\n<p><strong>The predicate is textual, never resolution.</strong> <code>readlink</code> returns a string; a target naming our machine proves ownership whether or not it still resolves. This is what makes the rule identical under <code>CYBEROS_UNINSTALL_KEEP_BRAIN=1</code> (where <code>.cyberos/</code> survives with BRAIN inside) and <code>=0</code> (where it does not), and it is why an operator's own broken symlink pointing elsewhere is untouched: unresolvable is not the same as unowned, in either direction.</p>\n<p><strong>What is provably ours is removed; the container is not.</strong> The block comes out of <code>.gitignore</code>, the file stays. The registration comes out of <code>.mcp.json</code>, the file stays with an empty registry. The symlink goes, the channel dir stays. Removing a container would require knowing install created it, which nothing records - so the line is drawn where the proof runs out. An emptied container is inert; a pointer to a deleted file is not.</p>\n<p><strong>Strip the separator with the block.</strong> Both sed ranges must also consume the single blank line install writes immediately above the <code># &gt;&gt;&gt;</code> marker - exactly one, because install writes exactly one. Verified today: byte-exact for a hook ending in one newline and for one ending in three.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Have install write a receipt of what it created, so uninstall can know rather than guess.</strong> The operator's instinct, floated for this task and not decided; adopted in spirit for TASK-IMP-122, where the underivable fact (what <code>install.sh</code> vendors) is load-bearing and has no safe fallback. It is rejected here because the fact it would record is not load-bearing and the fallback is safe. The only clause a receipt changes is \"delete the container\": everything else is already decided from the artefact. And a receipt never decides it alone - an operator who adds <code>node_modules/</code> outside our markers owns that file whatever the receipt says, so the rule is always \"receipt says created <strong>and</strong> the file is now empty\", and the emptiness test does the work in every case. So the receipt buys the deletion of a 0-byte file. It costs a new cross-script artefact, a format, staleness semantics across re-installs, <code>install.sh</code> in <code>modified_files</code> - and a new casualty vector: a receipt that wrongly claims creation deletes an operator's file, which is the one outcome §1.8 forbids and which no mechanism can do today. Note also what a receipt does <strong>not</strong> rescue: it says nothing about how to remove one key from JSON an operator has since edited (§1.4), and nothing about a hook that arrived without a trailing newline (§3) - both survive it untouched. Trading a guardrail for tidiness is the wrong direction.</li><li><strong>Delete an emptied container when it is byte-identical to install's exact output.</strong> Rejected: that is the TASK-IMP-118 defect in miniature. Install's <code>.mcp.json</code> is a fixed 5-line string and <code>install.sh:684</code>'s own summary points operators at <code>.cyberos/mcp/README.md</code> to hand-register, so an operator-authored file can be byte-identical to ours. Byte-identity proves the <em>content</em> is ours; it cannot prove the <em>file</em> is.</li><li><strong>Trim trailing blank lines after the strip instead of consuming the separator.</strong> Rejected: an operator hook whose pre-install content legitimately ends in three newlines would come back with one. Measured today - the correct rule removes exactly one blank line immediately above the marker, and that is byte-exact for both shapes.</li><li><strong>Remove the whole agent surface on uninstall.</strong> Rejected: <code>.cursorrules</code>, <code>GEMINI.md</code> and the rules pointers may predate install or carry operator edits; unprovable ownership is not a licence to delete. TASK-IMP-094's recorded reasoning, and it still holds.</li><li><strong>Leave the dangling links and let TASK-IMP-106's summary name them.</strong> Rejected: 106 §1.5 forbids changing behaviour, so it can only describe the breakage honestly. A truthful report of a broken repo is not a fixed repo.</li><li><strong>Effort 5 -&gt; 8 hours.</strong> The 5 predated three of this task's obligations. Breakdown: channel loop rewrite 1h; MCP partition 1.5h; separator-exact strip 1.5h; <code>t22</code> rename + re-point 0.5h; suite 3.5h.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: zero surviving pointers naming a path under <code>.cyberos/</code>, asserted by reading every surviving symlink target and every MCP command after uninstall. Baseline: 5 dangling symlinks + 2 dead registrations, measured 2026-07-18.</li><li>Primary: an operator's pre-existing hook and <code>.gitignore</code> are byte-identical to their pre-install bytes after 1 and after 3 cycles, asserted with <code>cmp</code>. Baseline: hook +1 per cycle (29 -&gt; 30 -&gt; 31 -&gt; 32, harness); <code>.gitignore</code> +1 once (14 -&gt; 15, measured today).</li><li>Guardrail: zero casualties - no path present before install may be missing or mutated after uninstall. Baseline: 0 across the harness's six cases; it must stay 0, and the two leak fixes must not create one.</li><li>Guardrail: every container install writes into still exists after uninstall. Baseline: all present today (the defect is what is left <em>in</em> them, not that they go missing) - this metric exists to keep the fix from over-reaching.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>uninstall.sh</code>'s handling of the six skill channels, the two MCP registrations, and the separator on both managed-block strips; the suite arms for each; the rename and re-point of TASK-IMP-106's <code>t22</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li><code>install.sh</code>. This task changes nothing on the install side - see Alternatives on the receipt, and §3 on the no-trailing-newline hook, which is the one defect here that would need it.</li><li>What uninstall KEEPS by design (<code>docs/tasks/</code>, <code>docs/status/</code>, <code>CHANGELOG.md</code>, <code>AGENTS.md</code>/pointer files, BRAIN). Correct, and untouched.</li><li>The rules pointers (<code>.cursorrules</code>, <code>.windsurfrules</code>, <code>GEMINI.md</code>, <code>.github/copilot-instructions.md</code>, <code>.agents/rules/</code>, <code>.devin/rules/</code>, <code>.cursor/rules/</code>). Agent surface, kept for the same reason as <code>CLAUDE.md</code>. (<code>uninstall.sh:92-93</code>'s comment records only three of these; the rest are covered by the same reasoning, not by that citation.)</li><li><code>uninstall.sh:118-119</code>'s existing <code>rmdir</code> of <code>.agents/skills</code> and <code>.agents</code>. Existing shipped behaviour, not a defect this task found. It sits in mild tension with §1.5's container rule; §3 records that rather than smuggling a change in.</li><li>Payload staleness detection - TASK-IMP-122.</li><li>The summary's wording - TASK-IMP-106 owns it.</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-106: both edit <code>uninstall.sh</code> and <code>test_install_hygiene.sh</code>, so they serialise. 106 lands first, creating <code>t22_uninstall_behavior_unchanged</code>, whose AC 3 pins \"the set of files present after uninstall is byte-identical to today's behavior\" - a test that freezes every defect above as correct. This task MUST re-point and rename it (§1.9); shipping 121 without doing so turns 106's green suite red. 106 §1.5 keeps its verification: the re-pointed test still fails any summary change that alters what uninstall removes or keeps, which is the whole of what §1.5 asks. Per TASK-IMP-101's evidence gate, 106's coverage-gate artefact is the evidence.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> rewritten in full 2026-07-18 by a different author after an independent audit failed the prior draft at 4/10 for writing against a <code>.cyberos/</code> that survives, asserting a weaker predicate in AC 1 than §1.1 specified, conditioning §1.3/§1.4 on a fact nothing records, and mis-citing six lines. This is a whole-document rewrite, not a patch. Every line number and every number above was re-verified against source at HEAD by this author during this rewrite; the six mis-cited lines are corrected in <code>source_pages</code> with the true mechanism named beside each. Two claims are new measurements of my own (the <code>.gitignore</code> separator leak; the byte-exactness of the candidate strip), and one clause was narrowed because measurement showed it could not be honoured as written (§1.6, §3). Claims that could not be measured are recorded as gaps in §3 rather than assigned to a clause.</li><li><strong>Human review:</strong> scope and the <code>:112</code> refinement approved at the 2026-07-18 PLAN gate; both HITL gates are recorded human verdicts. The receipt decision is this author's, with rationale in Alternatives; the operator floated it and left it open.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 After uninstall, no symlink under any channel install writes (<code>.agents/skills/</code>, <code>.claude/skills/</code>, <code>.grok/skills/</code>, <code>.commandcode/skills/</code>, <code>.codex/skills/</code>, <code>.opencode/skill/</code>) may remain whose <code>readlink</code> target names <code>.cyberos/plugin/skills/&lt;entry&gt;</code> or the chained <code>.claude/skills/&lt;entry&gt;</code> form (<code>:98</code>). Ownership MUST be decided from the target TEXT, never from whether the target resolves and never from the entry's name; no entry may be exempt by name, which removes <code>:112</code>'s <code>ship-tasks</code> guard.</li><li>1.2 An entry under those channels whose ownership is proven by neither §1.1's target test nor a <code>.cyberos-owned</code> marker it carries (<code>:101</code>) MUST be left byte-identical and reported as kept - including a real directory without our marker, a symlink pointing outside <code>.cyberos/</code>, and a BROKEN symlink pointing outside <code>.cyberos/</code>.</li><li>1.3 Where <code>.mcp.json</code> or <code>.cursor/mcp.json</code> is byte-identical to the form <code>install.sh:685</code> generates - our registration and nothing else - the registration naming <code>.cyberos/mcp/</code> MUST be removed, leaving the file present and carrying a registry with no entries.</li><li>1.4 Where <code>.mcp.json</code> or <code>.cursor/mcp.json</code> carries our registration but is NOT byte-identical to that form, the file has content install did not write; uninstall MUST NOT edit it, and MUST report the dead registration and the file's path so the operator can remove it by hand.</li><li>1.5 A container install writes into MUST survive uninstall even when emptied of our content: <code>.gitignore</code> (including one install created at <code>:706</code>), <code>.mcp.json</code>, <code>.cursor/mcp.json</code>, and each of §1.1's six channel directories. No container may be removed on the grounds that it is now empty.</li><li>1.6 Stripping a managed block from a file that predates install MUST restore that file byte-identical to its pre-install content, consuming the single blank separator install writes immediately above the <code># &gt;&gt;&gt;</code> marker (<code>install.sh:747</code> for <code>.gitignore</code>, <code>:851-853</code>/<code>:913-915</code> for the hook) and consuming exactly one, so content that legitimately ended in blank lines survives. This clause binds where the pre-install content ends with a newline; §3 records why the remaining case is not recoverable from <code>uninstall.sh</code>.</li><li>1.7 Uninstall MUST be idempotent over cycles: for any N &gt;= 1, N install/uninstall cycles MUST leave an operator's pre-existing hook and <code>.gitignore</code> byte-identical to their pre-install content.</li><li>1.8 No path present before install may be missing or mutated after uninstall.</li><li>1.9 The suite MUST NOT assert that the pre-121 leftovers are correct. TASK-IMP-106's <code>t22_uninstall_behavior_unchanged</code> MUST be re-pointed at the set §1.1-§1.6 define and renamed, because a test whose baseline has deliberately moved cannot honestly be named <code>_unchanged</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - after an install writing all six channels, every surviving symlink under them is read with <code>readlink</code> and NONE has a target naming <code>.cyberos/plugin/skills/&lt;entry&gt;</code> or <code>.claude/skills/&lt;entry&gt;</code>; the test MUST assert all six channels separately and MUST FAIL if any entry is exempt by name, which today's <code>:112</code> guard makes true of <code>ship-tasks</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t23_no_managed_skill_links_survive</code></li><li>[ ] AC 2 (traces_to: #1.2) - an operator's unmarked skill dir, an operator symlink resolving outside <code>.cyberos/</code>, AND an operator's BROKEN symlink pointing outside <code>.cyberos/</code> all survive byte-identical and are named as kept; the test MUST FAIL if an unresolvable target alone causes removal - test: <code>tools/install/tests/test_install_hygiene.sh::t24_unprovable_entries_kept_and_named</code></li><li>[ ] AC 3 (traces_to: #1.3) - where <code>.mcp.json</code> and <code>.cursor/mcp.json</code> are byte-identical to install's generated form, after uninstall each file EXISTS, parses, carries zero registry entries, and contains no string naming <code>.cyberos/</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t25_sole_cyberos_registration_removed</code></li><li>[ ] AC 4 (traces_to: #1.4) - an <code>.mcp.json</code> carrying our registration plus an operator's own is byte-identical after uninstall AND the dead registration is reported with the file's path; the test MUST FAIL if the file is edited at all - test: <code>tools/install/tests/test_install_hygiene.sh::t26_operator_edited_mcp_kept_and_named</code></li><li>[ ] AC 5 (traces_to: #1.5) - a <code>.gitignore</code> install created, an <code>.mcp.json</code> reduced to an empty registry, and each of the six channel dirs all still EXIST after uninstall; the test MUST FAIL if any is removed for being empty - test: <code>tools/install/tests/test_install_hygiene.sh::t27_emptied_containers_survive</code></li><li>[ ] AC 6 (traces_to: #1.6) - a pre-existing newline-terminated pre-commit hook AND a pre-existing newline-terminated <code>.gitignore</code> are byte-identical to their pre-install bytes after one cycle, asserted with <code>cmp</code>; the test MUST include a hook whose content already ends in three newlines and MUST FAIL on today's leak (hook 29 -&gt; 30, <code>.gitignore</code> 14 -&gt; 15) - test: <code>tools/install/tests/test_install_hygiene.sh::t28_block_strip_restores_byte_exact</code></li><li>[ ] AC 7 (traces_to: #1.7) - after 3 install/uninstall cycles the hook and the <code>.gitignore</code> are still byte-identical to their pre-install bytes; the test MUST compare bytes and not line counts, and MUST FAIL on today's 29 -&gt; 30 -&gt; 31 -&gt; 32 hook accumulation - test: <code>tools/install/tests/test_install_hygiene.sh::t29_strip_idempotent_over_cycles</code></li><li>[ ] AC 8 (traces_to: #1.8) - no path present before install is missing or mutated after uninstall across a fresh-git repo, a repo with a foreign hook, a repo with <code>core.hooksPath</code> set, and a repo uninstalled with <code>CYBEROS_UNINSTALL_KEEP_BRAIN=0</code> - test: <code>tools/install/tests/test_install_hygiene.sh::t30_no_casualties</code></li><li>[ ] AC 9 (traces_to: #1.9) - <code>t22</code> is renamed <code>t22_uninstall_removal_set_pinned</code>, pins the set §1.1-§1.6 define, and passes; the test MUST FAIL if any assertion in the suite still pins a pre-121 leftover as correct - test: <code>tools/install/tests/test_install_hygiene.sh::t22_uninstall_removal_set_pinned</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><code>CYBEROS_UNINSTALL_KEEP_BRAIN=0</code> vs the default: in the default path <code>.cyberos/</code> SURVIVES (<code>:151</code> removes it, <code>:155-158</code> recreates it with BRAIN inside); under <code>=0</code> it does not. §1.1's predicate is textual, so it is the same rule in both, and both are covered by AC 8. This is the premise the prior draft inverted; the fix is not to pick the other mode but to stop conditioning on the mode at all.</li><li>A symlink into <code>.cyberos/</code> whose target was ALREADY absent before uninstall (a prior partial install): still provably ours by target text, still removed. Unresolvable is not unowned.</li><li>The CHAINED form's proof is weaker than the direct form's: <code>.agents/skills/&lt;cmd&gt; -&gt; ../../.claude/skills/&lt;cmd&gt;</code> names no <code>.cyberos/</code> path, so it is ours by construction (install writes exactly that shape at <code>:662</code>) rather than by target. An operator symlink of that exact shape would be removed. This is TASK-IMP-094's existing shipped behaviour at <code>:98</code>, carried forward unchanged and named here rather than discovered later.</li><li><code>CYBEROS_COPY_SKILLS=1</code>: the five native channels' copy fallback (<code>:613</code>'s <code>|| cp -R</code>) writes NO <code>.cyberos-owned</code> marker - only <code>.agents/skills</code> gets one (<code>:672-675</code>). So a copied <code>ship-tasks</code> under <code>.claude/skills/</code> is not provably ours and §1.2 keeps and reports it. It is untidy, not broken: a copy is self-contained and dangles at nothing. Closing it means marking the copy at install time, which is an <code>install.sh</code> change and out of scope here.</li><li><strong>An operator hook with NO trailing newline: byte-exact restore is IMPOSSIBLE from <code>uninstall.sh</code> and §1.6 does not claim it.</strong> Install's <code>cat &gt;&gt;</code> appends <code>\\n# &gt;&gt;&gt; ...</code>; against <code>exit 0</code> (no newline) that <code>\\n</code> becomes the terminator of the operator's last line, and the result <code>exit 0\\n# &gt;&gt;&gt; ...</code> is indistinguishable from what a newline-terminated file with no separator would produce. The information is destroyed at append time, so no uninstall-side rule can invert it - measured today at 16 -&gt; 17 bytes with the candidate strip. The residue is a +1-byte gain on a file that is not a POSIX text file, and closing it means changing install's append. Recorded as an uncovered gap, not handed to a clause that cannot honour it.</li><li><code>uninstall.sh:118-119</code> already <code>rmdir</code>s <code>.agents/skills</code> and <code>.agents</code> when they empty, with no ownership check - <code>rmdir</code>'s failure on a non-empty dir is the only guard. That is in mild tension with §1.5, which keeps the other five channel dirs. It is existing shipped behaviour, this task does not extend it to the five, and it is named here rather than quietly widened or quietly reversed.</li><li><code>.mcp.json</code> containing both our registration and an operator's: §1.4, not §1.3 - the file is not byte-identical to install's form, so it is left alone and the dead registration is reported. Surgically removing one key from arbitrary JSON needs a parser <code>uninstall.sh</code> does not have and <code>node</code> it must not require; reporting is what it can honestly do, and TASK-IMP-106's kept-list is the idiom for saying it.</li><li>An operator hook whose pre-install content already ended in multiple newlines: restore is byte-exact to THAT content - §1.6 consumes exactly one blank line, never normalises. Measured byte-exact today for a hook ending in three newlines.</li><li><code>CYBEROS_GLOBAL_SKILLS=1</code> copies <code>ship-tasks</code> into <code>$HOME/.claude/skills</code> and three siblings (<code>:634-637</code>); uninstall never touches them. Outside the repo is outside this task's contract (\"leave the repo as it found it\"); named so it is not mistaken for coverage.</li><li>Uninstall run twice: the second run finds nothing provably ours and removes nothing; §1.7's byte-identity still holds.</li><li>Security-class: ownership is decided by <code>readlink</code> output, our own marker file, our marker pair, and a command string naming our path - never by a path pattern an operator could forge by naming a directory <code>ship-tasks</code>. No user-supplied string is interpolated into a removal command; nothing printed is executed.</li></ul>";
