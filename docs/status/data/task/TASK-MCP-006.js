window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MCP-006"]="<h1>TASK-MCP-006: MCP destructive-tool gating via elicitation confirmation</h1>\n<h2>Summary</h2>\n<p>Ship gateway-entry gating so a <code>destructiveHint=true</code> <code>tools/call</code> is held for a TASK-MCP-008 confirmation elicitation before the gateway forwards to the module. As-built surface is a single <code>gating.rs</code> decision module plus <code>ToolAnnotations</code> in <code>annotations.rs</code>, wired from <code>router.rs</code> (in-memory store or <code>elicitation_pg</code> when a DB + authenticated caller are present). Non-destructive tools fast-path through; decline returns an in-band <code>user_rejected</code> tool error.</p>\n<h2>Problem</h2>\n<p>MCP 2025-11-25 advertises tool annotations, but without a gateway gate a destructive tool still executes on the first <code>tools/call</code>. Client UX prompts are optional; the server must refuse to forward until the caller confirms. The original engineering-spec imagined a 7-file <code>gating/</code> tree, per-tenant YAML policy, bypass tokens, confirm-TTL tables, and ten <code>gating_*</code> integration test files under <code>services/mcp/</code> — none of that matches the shipped <code>services/mcp-gateway/</code> crate.</p>\n<h2>Proposed Solution</h2>\n<p>Keep the decision pure in <code>services/mcp-gateway/src/gating.rs</code>:</p>\n<ul><li><code>evaluate(destructive, confirmed) -&gt; ConfirmationOutcome::{Proceed, Declined, NeedsConfirmation}</code></li><li><code>held_result</code> / <code>held_result_parts</code> — non-error <code>tools/call</code> result carrying <code>elicitation_required</code> + confirmation schema</li><li><code>user_rejected_result</code> — in-band error when the caller declines</li><li><code>confirmation_prompt(tool_name)</code> — fixed prompt shape for the elicitation</li></ul>\n<p><code>router.rs</code> consults <code>entry.annotations.destructive_hint</code> on <code>tools/call</code>. On hold it creates a confirmation elicitation (<code>ElicitationStore</code> or <code>elicitation_pg::create_confirmation</code>), returns the held result, and on re-invoke with <code>_meta.confirmation_id</code> re-evaluates and either forwards or aborts. <code>annotations.rs</code> exposes <code>ToolAnnotations</code> (including <code>read_only_idempotent</code> / <code>destructive</code> constructors) for <code>tools/list</code> and the gate.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Full per-tenant YAML policy + confirm-TTL ack table + bypass scope.</strong> Rejected for this adopt: not shipped; ledgered under Out of scope. Confirmation via TASK-MCP-008 elicitation is the as-built confirm path.</li><li><strong>Gate on <code>openWorldHint</code> as well as <code>destructiveHint</code> (DEC-1041 original wording).</strong> Deferred: router today checks only <code>destructive_hint</code>. Documented as Out of scope rather than claimed done.</li><li><strong>Separate <code>services/mcp/src/gating/{decision,policy,confirm,elicit,bypass,drift_detector}.rs</code> tree.</strong> Rejected: as-built is one <code>gating.rs</code> module inside <code>mcp-gateway</code>.</li><li><strong>Fail-open when DB path lacks caller ids.</strong> Rejected: as-built fails closed (never forwards a destructive call without a resolvable confirmation owner).</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: every <code>destructiveHint</code> <code>tools/call</code> without a valid confirmation is held with <code>elicitation_required</code>; confirmed calls forward; declined calls return <code>user_rejected</code>; read-only tools never hold.</li><li>Guardrail: <code>cargo test -p cyberos-mcp-gateway</code> stays green for gating + annotations + router destructive-gate tests; no claim of deferred policy/bypass/drift surfaces.</li></ul>\n<h2>Scope</h2>\n<p>In scope (as-built under <code>services/mcp-gateway/</code>):</p>\n<ul><li><code>src/gating.rs</code> — evaluate / held_result / user_rejected_result + unit tests</li><li><code>src/annotations.rs</code> — <code>ToolAnnotations</code> + constructors used by the gate</li><li><code>src/router.rs</code> — destructive hold / confirm / decline wire-up with TASK-MCP-008 (in-memory + <code>elicitation_pg</code> when DB present)</li><li>Router tests covering read-only fast path and the destructive confirmation cycle</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Per-tenant <code>mcp_gating_policy</code> YAML / admin CRUD / NATS hot-reload</li><li>Bypass token (<code>mcp_gating_bypass</code>) and audit-only shadow mode</li><li>Separate <code>mcp_pending_confirmations</code> confirm-TTL table (as-built uses elicitation rows)</li><li><code>openWorldHint</code> gating (only <code>destructive_hint</code> is checked today)</li><li>Annotation drift detector / nightly job</li><li>Confirmation rate limit</li><li>Memory audit-sampling story for <code>mcp.tool_gating_*</code> kinds</li><li>Separate 7-file <code>gating/</code> module tree under <code>services/mcp/</code></li><li>Claimed <code>gating_*</code> integration test filenames that do not exist (cite real in-crate / router tests instead)</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-MCP-001, TASK-MCP-004]</code> — protocol <code>tools/call</code> path and OAuth-authenticated callers for the DB store-of-record path. Soft: <strong>TASK-MCP-008</strong> supplies confirmation elicitation (in-memory + <code>elicitation_pg</code> + migration <code>0016</code>). Related: TASK-MCP-007 (future long-running destructive confirm-at-start is not required for this adopt).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>services/mcp-gateway/src/gating.rs</code> MUST expose <code>evaluate(destructive: bool, confirmed: Option&lt;bool&gt;) -&gt; ConfirmationOutcome</code> where non-destructive always <code>Proceed</code>, destructive+<code>None</code> is <code>NeedsConfirmation</code>, destructive+<code>Some(true)</code> is <code>Proceed</code>, and destructive+<code>Some(false)</code> is <code>Declined</code>.</li><li>1.2 On <code>NeedsConfirmation</code>, the gateway MUST return a non-error <code>tools/call</code> result whose structured content sets <code>elicitation_required: true</code> and carries a confirmation-typed elicitation (via <code>held_result</code> / <code>held_result_parts</code>), and MUST NOT forward to the module.</li><li>1.3 On <code>Declined</code>, the gateway MUST return <code>user_rejected_result()</code> (<code>is_error: true</code>, <code>user_rejected: true</code>) and MUST NOT forward.</li><li>1.4 Tools with <code>destructive_hint=false</code> MUST fast-path through the gate without creating an elicitation.</li><li>1.5 When the DB store-of-record is active, confirmation create/lookup MUST go through <code>elicitation_pg</code> scoped to the authenticated caller; if caller ids cannot be resolved for a destructive call, the gateway MUST fail closed (never forward).</li><li>1.6 <code>ToolAnnotations</code> MUST serialize MCP hint names (<code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code>) and expose the <code>destructive</code> / <code>read_only_idempotent</code> constructors used by registration and tests.</li><li>1.7 This adopt MUST NOT claim deferred surfaces in Out of scope as shipped.</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - <code>evaluate</code> truth table matches Proceed / NeedsConfirmation / Declined - test: <code>services/mcp-gateway/src/gating.rs::non_destructive_always_proceeds</code></li><li>[ ] AC 2 (traces_to: #1.1) - destructive holds then proceeds or declines per confirmation - test: <code>services/mcp-gateway/src/gating.rs::destructive_holds_then_proceeds_or_declines</code></li><li>[ ] AC 3 (traces_to: #1.2) - held result carries elicitation_required and is not an error - test: <code>services/mcp-gateway/src/gating.rs::held_result_carries_the_elicitation_and_is_not_an_error</code></li><li>[ ] AC 4 (traces_to: #1.3) - user_rejected result is an in-band error - test: <code>services/mcp-gateway/src/gating.rs::user_rejected_result_is_an_in_band_error</code></li><li>[ ] AC 5 (traces_to: #1.6) - annotation constructors flip the correct hints - test: <code>services/mcp-gateway/src/annotations.rs::destructive_flips_correct_hints</code></li><li>[ ] AC 6 (traces_to: #1.4) - read-only tool forwards through the gate - test: <code>services/mcp-gateway/src/router.rs::read_only_tool_forwards_through_the_gate</code></li><li>[ ] AC 7 (traces_to: #1.2) - destructive tool without confirmation is held - test: <code>services/mcp-gateway/src/router.rs::destructive_tool_without_confirmation_is_held</code></li><li>[ ] AC 8 (traces_to: #1.1,#1.5) - destructive tool with confirmation forwards - test: <code>services/mcp-gateway/src/router.rs::destructive_tool_with_confirmation_forwards</code></li><li>[ ] AC 9 (traces_to: #1.3) - declined confirmation aborts cleanly - test: <code>services/mcp-gateway/src/router.rs::destructive_tool_declined_aborts_cleanly</code></li><li>[ ] AC 10 (traces_to: #1.7) - Out of scope lists deferred policy/bypass/drift/openWorld/phantom gating_* tests; no AC claims them shipped - verify: <code>docs/tasks/mcp/TASK-MCP-006-tool-annotation-gating/spec.md</code> Scope → Out of scope</li></ul>\n<h2>Verification</h2>\n<p>Run from repo root / <code>services/</code>:</p>\n<pre><code class=\"language-bash\">cd services &amp;&amp; cargo test -p cyberos-mcp-gateway gating\ncd services &amp;&amp; cargo test -p cyberos-mcp-gateway annotations\ncd services &amp;&amp; cargo test -p cyberos-mcp-gateway --lib router::tests::destructive_\ncd services &amp;&amp; cargo test -p cyberos-mcp-gateway --lib router::tests::read_only_tool_forwards_through_the_gate</code></pre>\n<p>Real in-crate tests (do <strong>not</strong> cite non-existent <code>gating_*</code> integration files):</p>\n<table><thead><tr><th>Path</th><th>Covers</th></tr></thead><tbody><tr><td><code>services/mcp-gateway/src/gating.rs</code> (<code>non_destructive_always_proceeds</code>, <code>destructive_holds_then_proceeds_or_declines</code>, <code>held_result_carries_the_elicitation_and_is_not_an_error</code>, <code>user_rejected_result_is_an_in_band_error</code>)</td><td>Pure gate decision + result shapes</td></tr><tr><td><code>services/mcp-gateway/src/annotations.rs</code> (<code>read_only_idempotent_flips_correct_hints</code>, <code>destructive_flips_correct_hints</code>)</td><td>Hint constructors</td></tr><tr><td><code>services/mcp-gateway/src/router.rs</code> (<code>read_only_tool_forwards_through_the_gate</code>, <code>destructive_tool_without_confirmation_is_held</code>, <code>destructive_tool_with_confirmation_forwards</code>, <code>destructive_tool_declined_aborts_cleanly</code>)</td><td>End-to-end tools/call gating with in-memory elicitation</td></tr></tbody></table>\n<p>Persistence of confirmations is verified under TASK-MCP-008 (<code>elicitation_pg</code> + <code>db_slice_test.rs::elicitation_persists_seals_and_is_caller_scoped</code>, Postgres-gated / <code>#[ignore]</code> until a pool is available).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) on branch <code>batch/9a-mcp</code>, rewriting the engineering-spec body into task@1 grammar against as-built <code>services/mcp-gateway/</code> sources.</li><li><strong>Scope:</strong> Spec re-scoped to shipped gating surface; deferred policy/bypass/drift/openWorld/audit-sampling ledgered under Out of scope; paths corrected from <code>services/mcp/</code> to <code>services/mcp-gateway/</code>; ACs cite real in-crate tests only.</li><li><strong>Human review:</strong> Required at the two HITL gates; this file is the batch/9a-mcp adopt rework (<code>entered_via: rework</code>, <code>routed_back_count: 1</code>).</li></ul>\n<hr>\n<p><em>batch/9a-mcp adopt — TASK-MCP-006 re-spec against as-built mcp-gateway gating.</em></p>";
