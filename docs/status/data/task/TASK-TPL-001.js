window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TPL-001"]="<h1>TASK-TPL-001: templates module (CDS shells)</h1>\n<h2>§1 - Description</h2>\n<p>One module owns the HTML faces of every deliverable, so renderers and skills compose pages from audited shells instead of inventing markup.</p>\n<p>Normative clauses:</p>\n<ol><li>A module <code>modules/templates/</code> MUST exist with <code>MODULE.md</code> (scope: presentation shells only - no content generation, no data reads) and the file set in <code>new_files</code>.</li><li>CDS MUST be vendored, not linked: <code>cds/tokens.css</code> and <code>cds/glass.css</code> copied verbatim from <code>@cyberskill/tokens</code> / <code>@cyberskill/react</code> dist of design-system v1.3.0, with <code>cds/PROVENANCE.md</code> recording source repo, version, commit, copy date, and the re-vendor procedure. No CDN reference anywhere.</li><li>A contract <code>template@1</code> MUST be defined at <code>contracts/TEMPLATE.md</code>: a template is an HTML file with named slots <code>{{slot:&lt;name&gt;}}</code> (text-safe) and <code>{{slot:&lt;name&gt;:html}}</code> (pre-rendered HTML), a required <code>data-template-id</code> on the root element, and a self-containment rule - rendered output MUST work from file:// with no external network fetch (styles inlined or relative).</li><li>Three shells MUST ship: <code>deliverable.html</code> (any single artefact page: header with id/status/module badges, meta strip, body slot, assets-aware figure styling, footer provenance), <code>status-hub.html</code> (command-deck strip + tab bar + three tab panels; hash-routed, JS-free fallback), <code>catalog.html</code> (card grid with facet strip).</li><li>All shell styling MUST use <code>--cs-*</code> tokens (or derive from them); the only hex literals allowed live inside the vendored <code>cds/*.css</code>. Typography per CDS (<code>--cs-font-family-ui</code>, Be Vietnam Pro stack).</li><li>Templates MUST be render-engine-agnostic: consumable by a node builder or by an agent doing string substitution - slot substitution is defined as plain string replacement with HTML-escaping for text slots, and the contract carries the exact escape set.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Vendoring pins the look to an audited CDS release (the design system's own doctrine: three canonical files, everything else regenerable) while keeping deliverables buildable offline. Slots-as-string-replacement keeps the contract usable by both tooling and doc-driven agents - same reason the skill chain is markdown-first.</p>\n<h2>§3 - Contract</h2>\n<p><code>template@1</code>: <code>{{slot:title}}</code>, <code>{{slot:body:html}}</code>, <code>data-template-id=\"deliverable@1\"</code>; escape set: <code>&amp; &lt; &gt; \"</code> for text slots. Full grammar in contracts/TEMPLATE.md.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Vendored, pinned, provenanced</strong> (§1 #2) - tokens.css/glass.css byte-match the pinned upstream dist; PROVENANCE.md names version+commit; grep finds no CDN/external URL in any shell.</li><li><strong>Contract complete</strong> (§1 #3, #6) - TEMPLATE.md defines slot grammar, escape set, data-template-id rule, self-containment rule; each shell carries its id.</li><li><strong>Shells parse and self-contain</strong> (§1 #4) - each shell substituted with fixture slots yields HTML whose only asset references are inline or relative.</li><li><strong>Token-only styling</strong> (§1 #5) - no hex color outside cds/*.css (checker-level grep).</li></ol>\n<h2>§5 - Verification</h2>\n<p><code>tools/docs-site/tests/test_templates_module.sh</code>: t01_vendored_pinned, t02_contract_complete, t03_shells_selfcontained, t04_token_only. (AC 1-4.)</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Copy dist css from the design-system clone at the pinned commit; shells authored once with CDS variables; contract mirrors the skills' contracts formatting.</p>\n<h2>§7 - Dependencies</h2>\n<p>None upstream. Blocks TASK-DOCS-005/006 (they consume the shells). TASK-SKILL-120 cites the contract from authoring skills.</p>\n<h2>§8 - Example payloads</h2>\n<p><code>&lt;article data-template-id=\"deliverable@1\"&gt;&lt;h1&gt;{{slot:title}}&lt;/h1&gt;&lt;section&gt;{{slot:body:html}}&lt;/section&gt;&lt;/article&gt;</code></p>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Style-packs (CDS Part 22) are future scope; the shells take the default pack.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Upstream CDS drifts - PROVENANCE re-vendor procedure + byte-match test catch silent edits.</li><li>Template edited into non-self-contained form - AC 3 fixture render fails.</li><li>Slot injection (unescaped user text) - text slots escape <code>&amp; &lt; &gt; \"</code>; html slots are builder-owned only, stated in the contract.</li><li>Shell forked per consumer - data-template-id + the contract make forks visible in review.</li><li>Dark-mode regressions - tokens.css light default; [data-theme] opt-in preserved as vendored.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep shells minimal; page-specific layout belongs to the consumer's slot content, not new shells.</p>\n<p><em>End of TASK-TPL-001.</em></p>";
