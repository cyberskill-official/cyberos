window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-202"]="<h1>TASK-SKILL-202: Skill quality floor - NFR stub disposition, untrusted-content backport, full pair parity</h1>\n<h2>Summary</h2>\n<p>Three quality gaps ship in every consumer payload: four ~20-line NFR skill stubs are vendored as if they were real skills (no contract, no HITL rules - an agent routed to them will improvise \"NFR certification\" with false confidence); 24 of the 56 vendored skills - including the highest-exposure repo-readers - carry neither the <code>untrusted_inputs</code> frontmatter contract nor a <code>references/UNTRUSTED_CONTENT.md</code>, so prompt-injection discipline is absent exactly where arbitrary consumer-repo text is read; and the pair-parity checker holds only 11 of the 25 vendored author/audit pairs to its file-class floor. This task delists the stubs, backports the injection discipline from the exemplary skills to the 20 non-stub gaps, expands parity SCOPE to all pairs, and adds a skill-floor lint so undersized skills can never be silently vendored again.</p>\n<h2>Problem</h2>\n<p>Audit findings H7 + H8, verified first-hand 2026-07-23:</p>\n<ol><li><strong>Stub skills vendored as real (H7).</strong> <code>build.sh:91-94</code> vendors <code>nfr-certification-author</code>, <code>nfr-evaluator</code>, <code>nfr-test-runner</code>, <code>nfr-regression-handler</code> - each a 20-22 line SKILL.md with a description and one paragraph, no envelopes, no invariants, no acceptance material. <code>certify-nfrs.md</code> routes to them. The chain-coverage checker exempts them as \"unpaired by design\", which answers the pairing question while leaving the deeper one - they are not <em>skills</em>, they are name reservations shipped as product.</li><li><strong>Injection discipline missing on 24/56 (H8).</strong> The repo's own best skills (task-author, task-audit) carry a two-part discipline: <code>untrusted_inputs</code> frontmatter (wrap-marker, injection scan, surface-to-human) and a <code>references/UNTRUSTED_CONTENT.md</code> procedure. 24 vendored skills carry neither - including <code>repo-context-map-author</code> and <code>edge-case-matrix-author</code>, which exist specifically to read arbitrary consumer-repo files, the canonical injection vector.</li><li><strong>Parity floor covers 11 of 25 pairs.</strong> <code>check-pair-parity.sh</code> SCOPE (<code>:13-14</code>) names 11 pairs; the payload carries 25. The 14 outside SCOPE can ship without envelopes, rubrics, or failure-mode references and nothing fails.</li></ol>\n<h2>Proposed Solution</h2>\n<p>Delist the four NFR stubs from <code>build.sh</code>'s <code>VENDORED_SKILLS</code> and remove their <code>chain-allowlist.txt</code> exemptions (the allowlist's own rot-warning rule then keeps the file honest); the source dirs remain in <code>modules/skill/</code> as unvendored scaffolds. Give <code>certify-nfrs.md</code> an explicit \"NFR skills not yet shipped - this workflow requires their full implementation\" notice at its routing step so the workflow degrades loudly, not improvisationally. Backport the injection discipline to the 20 remaining gap skills by instantiating the task-author pattern: an <code>untrusted_inputs</code> frontmatter block (wrap_in_marker <code>untrusted_content</code>, injection_scan required, on_marker_hit surface_to_human) plus a <code>references/UNTRUSTED_CONTENT.md</code> adapted to each skill's actual input surface (repo files for the repo-readers; artefact bodies for the audits). Expand <code>check-pair-parity.sh</code> SCOPE to all 25 vendored pairs, authoring the missing file classes for the 14 newly-scoped pairs (envelopes, PIPELINE/INVARIANTS or RUBRIC/AUDIT_LOOP/REPORT_FORMAT, FAILURE_MODES, acceptance/README) at parity with the existing deepened pairs. Add <code>tools/install/check-skill-floor.sh</code> - a vendoring-time lint asserting every vendored SKILL.md meets a minimum floor (&gt;= 60 body lines below frontmatter AND required sections present: a contract/usage heading, an operating-principles or MUST/MUST-NOT block; threshold chosen to be far below any real skill, far above any stub) - wired into <code>build.sh</code> so an undersized skill fails the build, with a test suite covering positive/negative paths.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Implement the four NFR skills instead of delisting.</strong> Rejected for this task: each needs a real contract (envelopes, rubric or pipeline, HITL points, acceptance tests) - four skill-authoring projects. Shipping the floor must not wait on them; the delisting leaves named scaffolds and a loud workflow notice, and re-vendoring is one build.sh line when they are real.</li><li><strong>Delete the NFR source dirs entirely.</strong> Rejected: the name reservations and descriptions are genuine design intent (TASK-CUO-209 vendored them deliberately); deletion loses that record. Delisting removes the false product claim while keeping the intent discoverable.</li><li><strong>One shared UNTRUSTED_CONTENT.md included by reference.</strong> Rejected: the discipline's value is per-skill specificity (WHAT input is untrusted and WHERE the wrap happens differs between a repo-reader and an artefact-audit); a generic include invites cargo-cult compliance. The frontmatter block is uniform; the reference doc is per-skill.</li><li><strong>Grow SCOPE incrementally as pairs deepen (status quo).</strong> Rejected: that is the current design and it produced 14 unheld pairs in production; \"grows as pairs are deepened\" has no forcing function. Inverting it (all pairs in SCOPE; exemptions must be argued) makes shallowness the visible state.</li><li><strong>Line-count-only stub lint.</strong> Rejected: trivially gamed by padding and misses the real question (does a contract exist); the floor pairs a line minimum with required-section presence, both mechanical.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release, a fresh payload build vendors zero SKILL.md files below the floor (checker green in build), all 25 pairs pass <code>check-pair-parity.sh</code> with SCOPE = the full pair set, and every vendored skill whose inputs include repo or artefact text carries both halves of the injection discipline. Baselines today: 4 stubs vendored, 11/25 pairs scoped, 24 skills missing both halves.</li><li>Guardrail: no vendored skill's existing behavior contract changes (the backport adds frontmatter + a reference doc; it rewrites no skill body logic), and payload build time stays within its current budget (+ the checker's ~1s).</li></ul>\n<h2>Scope</h2>\n<p>In scope: build.sh delisting + floor-checker wiring, chain-allowlist cleanup, certify-nfrs.md notice, 20 injection-discipline backports, 14 pair deepenings + SCOPE expansion, the new checker + test suite, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Implementing the four NFR skills (future work; unblocked and unowned by this task).</li><li>Rewriting any skill's body prose beyond adding the discipline block/reference and the parity file classes.</li><li>The G7/G8 benchmark-gate CI meta-definitions - TASK-IMP-140 adopts this task's checkers as its G7/G8 mechanisms; soft forward reference, no cycle.</li><li>Skill <em>quality</em> judgment beyond the mechanical floor (SKB-family rubric work stays with the skill-bundle tasks TASK-SKILL-111..115).</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. TASK-SKILL-118 (done) established the pair file-class policy and the parity checker this task expands; TASK-CUO-209 (done) vendored the NFR stubs this task delists - both are context, neither needs reopening; their decisions are superseded knowingly and the delisting names TASK-CUO-209 in its CHANGELOG line. TASK-IMP-140's G7/G8 gates run this task's checkers in CI - forward reference only.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> the stub line counts, the 24-of-56 measurement (both halves grepped per skill dir against the built payload), the 25-pair census, and the SCOPE list were all measured first-hand at HEAD; the plan's 21 and 24 figures are corrected to the measured 24 and 25 with the discrepancy recorded in <code>source_decisions</code>.</li><li><strong>Human review:</strong> the hardening plan was operator-approved 2026-07-23; the delist-not-implement default is a recorded decision for the review acceptance gate.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>build.sh</code> MUST NOT vendor <code>nfr-certification-author</code>, <code>nfr-evaluator</code>, <code>nfr-test-runner</code>, or <code>nfr-regression-handler</code> (removed from <code>VENDORED_SKILLS</code>), and <code>chain-allowlist.txt</code> MUST drop their four UNPAIRED exemption lines in the same change (an allowlist entry whose name no payload dir matches is rot by the file's own rule).</li><li>1.2 <code>certify-nfrs.md</code> MUST state at its skill-routing step that the four NFR skills are not yet shipped and the workflow requires their full implementation before it can run - a loud degradation replacing today's silent improvisation surface.</li><li>1.3 Every remaining vendored skill that reads repo files or artefact bodies as input - the 20 measured gap skills, enumerated in source_pages - MUST carry BOTH an <code>untrusted_inputs</code> frontmatter block (wrap_in_marker <code>untrusted_content</code>, injection_scan required, on_marker_hit surface_to_human, matching the task-author pattern) AND a <code>references/UNTRUSTED_CONTENT.md</code> adapted to that skill's specific input surface.</li><li>1.4 <code>check-pair-parity.sh</code> SCOPE MUST enumerate every author/audit pair present in the vendored payload (25 at authoring time), and the 14 newly-scoped pairs MUST carry the AUTHOR_CLASSES / AUDIT_CLASSES file sets so the expanded check passes - authored at parity with the existing deepened pairs, not as empty placeholder files.</li><li>1.5 A new checker <code>tools/install/check-skill-floor.sh &lt;skills-dir&gt;</code> MUST fail (distinct non-zero exit) when any vendored SKILL.md has fewer than 60 non-frontmatter lines OR lacks the required floor sections, and <code>build.sh</code> MUST run it against the assembled payload so an undersized skill fails the build. Placeholder-syntax detection stays with the existing sweep tooling; this floor is size + structure.</li><li>1.6 The new suite <code>tools/install/tests/test_skill_floor.sh</code> MUST cover: floor-checker pass on the real payload, fail on a synthetic stub fixture, fail on a missing-section fixture, delisting (payload contains no nfr-* skill dir), allowlist cleanliness, and parity-SCOPE completeness (SCOPE equals the payload's measured pair set - a pair vendored but unscoped fails).</li><li>1.7 <code>CHANGELOG.md</code> MUST record the delisting (naming TASK-CUO-209 as the superseded vendoring decision), the injection-discipline backport count, and the parity expansion.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - a scratch payload build contains no <code>cuo/skills/nfr-*</code> directory and <code>chain-allowlist.txt</code> contains no <code>nfr-</code> line; the chain-coverage checker stays green (no rot warnings) - test: <code>tools/install/tests/test_skill_floor.sh::t01_nfr_delisted_clean</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>certify-nfrs.md</code> contains the not-yet-shipped notice at its routing step - test: <code>tools/install/tests/test_skill_floor.sh::t02_workflow_degrades_loud</code></li><li>[ ] AC 3 (traces_to: #1.3) - for each of the 20 enumerated gap skills: SKILL.md frontmatter parses and contains the <code>untrusted_inputs</code> block with the three required keys, and <code>references/UNTRUSTED_CONTENT.md</code> exists non-empty and names that skill's own input surface (not a byte-copy of another skill's file) - test: <code>tools/install/tests/test_skill_floor.sh::t03_injection_discipline_backported</code></li><li>[ ] AC 4 (traces_to: #1.4) - <code>check-pair-parity.sh</code> against the built payload exits 0 with SCOPE size equal to the payload's pair count, and deleting one required class file from a newly-scoped pair in a scratch copy makes it exit 10 naming the file - test: <code>tools/install/tests/test_skill_floor.sh::t04_full_scope_parity</code></li><li>[ ] AC 5 (traces_to: #1.5) - <code>check-skill-floor.sh</code> exits 0 on the real payload, non-zero on a fixture stub (&lt; 60 lines) and on a fixture missing the required sections, and a build with an injected stub fixture fails - test: <code>tools/install/tests/test_skill_floor.sh::t05_floor_checker_blocks_build</code></li><li>[ ] AC 6 (traces_to: #1.6) - the suite runs green under <code>bash scripts/tests/run_all.sh</code> discovery (glob <code>tools/install/tests/test_*.sh</code>) - test: <code>tools/install/tests/test_skill_floor.sh::t06_registered_by_glob</code></li><li>[ ] AC 7 (traces_to: #1.7) - CHANGELOG's top entry names the delisting with TASK-CUO-209, a backport count of 20, and the SCOPE expansion - test: <code>tools/install/tests/test_skill_floor.sh::t07_changelog_records_floor</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>A consumer repo already installed with the NFR stubs:</strong> uninstall/reinstall replaces <code>.cyberos/cuo/skills/</code> wholesale, so the stubs disappear on next update; an install that never updates keeps them - acceptable, since the loud certify-nfrs notice ships in the same payload that removes the skills.</li><li><strong>The 60-line floor vs legitimately small future skills:</strong> a real single-purpose skill below 60 lines would fail the build - by design, loudly, at vendoring time, where the author can either meet the floor or argue an explicit exemption in the checker (the exemption list starts empty; adding to it is a reviewed change).</li><li><strong>Byte-copied UNTRUSTED_CONTENT.md across skills:</strong> AC 3 requires each reference doc to name its own skill's input surface; the test compares docs pairwise for full-content identity to catch copy-paste compliance (identical general sections are fine; full-file identity is not).</li><li><strong>SCOPE drift after this task:</strong> a future pair vendored without SCOPE membership fails t04's completeness half (SCOPE must equal the measured pair set), inverting the old grow-when-remembered design.</li><li><strong><code>task-reconcile</code> and <code>workflow-improver</code> (repo-readers, not pairs):</strong> both are in the 20-skill backport set via 1.3 (which keys on input surface, not pairing); neither is touched by SCOPE (parity is a pair property).</li><li><strong>Security-class:</strong> this task ADDS trust-boundary discipline and removes improvisation surface; the checkers execute no vendored content (they read and pattern-match only). The backported frontmatter changes no tool grants for any skill.</li></ul>";
