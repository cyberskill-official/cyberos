window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-126"]="<h1>TASK-IMP-126: Uninstall completeness - MCP, dangling skill links, hook newline</h1>\n<h2>Summary</h2>\n<p><code>uninstall.sh</code> is not the inverse of <code>install.sh</code> in three places. It never removes the MCP registration files install writes, it cleans only two of the agent families' skill entries and leaves the rest pointing at the deleted machine, and its hook-strip leaves a blank line behind that accumulates on every install/uninstall cycle. Make uninstall the exact inverse of install for these three artifacts, proven by a fixture that installs, uninstalls, and asserts the tree is clean.</p>\n<h2>Problem</h2>\n<p>Three verified gaps, each a place where install writes and uninstall does not remove.</p>\n<p><strong>Dead MCP registrations.</strong> <code>install.sh:694-697</code> writes <code>.mcp.json</code> (and <code>.cursor/mcp.json</code> when the cursor agent is selected), each pointing at <code>.cyberos/mcp/cyberos-mcp.mjs</code>. <code>uninstall.sh</code> contains zero MCP handling (<code>grep -ci mcp</code> = 0). After uninstall removes <code>.cyberos/</code>, those files survive and register an MCP server whose entry point no longer exists - a broken registration every MCP-capable agent reads.</p>\n<p><strong>Dangling skill links.</strong> <code>install.sh:632-637</code> installs the workflow skills for the claude-code, grok, command-code, codex, and opencode families, each a symlink (or copy-fallback) into <code>.cyberos/plugin/skills/</code>. <code>uninstall.sh:98-127</code> removes only the <code>.agents/skills</code> trio and the <code>.claude/skills</code> create-tasks pair, and <code>:125</code> explicitly leaves <code>.claude/skills/ship-tasks</code>. The grok, command-code, codex, and opencode entries are never touched. After <code>.cyberos/</code> is removed, every untouched managed link dangles.</p>\n<p><strong>Hook-strip newline leak.</strong> <code>install.sh:860-861</code> appends the managed block to a foreign pre-commit hook via a heredoc whose first line is a blank separator, writing <code>\\n# &gt;&gt;&gt; cyberos-status-hook v2 ...&gt;&gt;&gt;\\n...\\n# &lt;&lt;&lt; cyberos-status-hook &lt;&lt;&lt;\\n</code>. <code>uninstall.sh:78</code> strips the marked range with <code>sed '/# &gt;&gt;&gt; .../,/# &lt;&lt;&lt; ...&lt;&lt;&lt;/d'</code> - inclusive of both markers but not the leading blank. The separator survives, so each install/uninstall cycle on a foreign hook accumulates one blank line at the append point. The v1-&gt;v2 upgrade path (<code>install.sh:856</code>) shares the pattern.</p>\n<h2>Proposed Solution</h2>\n<p>Make uninstall the inverse of install for all three, keeping the whole change in <code>uninstall.sh</code> plus arms in the existing <code>test_install_hygiene.sh</code>:</p>\n<ul><li>Remove <code>.mcp.json</code> and <code>.cursor/mcp.json</code> when they are the cyberos-written form, never when they are an operator's own file (same ownership discipline the skill and hook sections already use - match the exact content install writes, or a marker).</li><li>Extend the skill-cleanup loop to cover every family install writes (grok, command-code, codex, opencode) plus <code>.claude/skills/ship-tasks</code>, removing an entry only when it is ours by construction: a symlink whose <code>readlink</code> target resolves into <code>.cyberos/plugin/skills</code>, or a copy carrying the <code>.cyberos-owned</code> marker.</li><li>Widen the hook-strip so it consumes the leading blank separator, leaving a foreign hook byte-identical to its pre-install content across any number of cycles.</li></ul>\n<h2>Alternatives Considered</h2>\n<ul><li>Fix the newline on the install side (drop the leading blank from the heredoc). Rejected: the blank is a deliberate readability separator in a hook an operator reads, and the defect is that uninstall does not undo what install does - the inverse belongs in uninstall, next to the strip that already exists.</li><li>Three separate tasks, one per finding. Rejected at the PLAN gate: all three live in <code>uninstall.sh</code> and would serialise on one file with no benefit (recorded 2026-07-19).</li><li>Have uninstall shell out to a manifest of what install wrote. Rejected: no such manifest exists, and ownership-by-construction (readlink target / marker / exact content) is the discipline the file already uses and is enough.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a fixture install then uninstall leaves zero cyberos-owned artifacts - no <code>.mcp.json</code>/<code>.cursor/mcp.json</code> we wrote, no skill link resolving into the removed machine, and a foreign hook byte-identical to before install - suite-asserted. Baseline: all three survive today.</li><li>Guardrail: no operator file is removed - an operator's own <code>.mcp.json</code>, an unmarked <code>.agents/skills/&lt;cmd&gt;</code> dir, and the foreign hook's own lines all stay (the existing spec 1.3 \"never touch operator files\" promise is not weakened).</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>uninstall.sh</code>'s MCP-file removal, the skill-cleanup coverage, the hook-strip separator, and arms in <code>test_install_hygiene.sh</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any change to <code>install.sh</code> (what install writes is correct; uninstall is what is missing).</li><li>The <code>$HOME</code> global installs behind <code>CYBEROS_GLOBAL_SKILLS=1</code> (opt-in, default off) - a follow-up may cover them, but the default-path fleet does not create them and this task stays on the always-created artifacts.</li><li>The <code>rules_sha</code> fingerprint (TASK covered separately; judged by-design at the PLAN gate).</li><li>Re-installing the fleet (that is Goal 4, an operator-gated action).</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Touches <code>uninstall.sh</code> (shared surface with TASK-IMP-083 hook logic and TASK-IMP-094 skill logic - both already landed; this extends their inverse coverage).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the 2026-07-19 fleet-health pass; each of the three gaps was verified against the live <code>install.sh</code> and <code>uninstall.sh</code> at HEAD with the exact line numbers cited in source_pages. The handoff's \"5 dangling symlinks\" count was deliberately not adopted as a normative number - it cannot be verified without a live install, so 1.2 specifies the invariant (zero dangling links) and the test proves it against a fixture.</li><li><strong>Human review:</strong> scope and granularity approved at the 2026-07-19 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>uninstall.sh</code> MUST remove <code>.mcp.json</code> and, when present, <code>.cursor/mcp.json</code> when they are the cyberos-written form (their content matches what <code>install.sh</code> writes, or they carry a cyberos marker), and MUST NOT remove an operator's own file of either name.</li><li>1.2 <code>uninstall.sh</code> MUST remove every managed skill entry install created that resolves into the removed machine - across all agent families install writes (claude-code, grok, command-code, codex, opencode) plus <code>.claude/skills/ship-tasks</code> - leaving zero symlinks that point into <code>.cyberos/plugin/skills</code> after the machine is removed. Ownership MUST be proven by construction (readlink target resolving into <code>.cyberos</code>, or the <code>.cyberos-owned</code> marker); an unmarked operator dir MUST be left in place.</li><li>1.3 The hook-strip MUST be the exact inverse of install's append: it MUST also remove the leading blank separator install writes before the <code>&gt;&gt;&gt;</code> marker, so a foreign pre-commit hook is byte-identical to its pre-install content after uninstall, and stays byte-identical across any number of install/uninstall cycles.</li><li>1.4 None of 1.1-1.3 may remove an operator file: an operator-authored <code>.mcp.json</code>, an <code>.agents/skills/&lt;cmd&gt;</code> dir without the <code>.cyberos-owned</code> marker, or any line of a foreign hook outside the managed block MUST survive uninstall.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - a fixture install writes <code>.mcp.json</code>; uninstall removes it; a pre-existing operator <code>.mcp.json</code> is left untouched - test: <code>tools/install/tests/test_install_hygiene.sh::t_mcp_registration_removed</code></li><li>[ ] AC 2 (traces_to: #1.2) - after a fixture install then uninstall, zero skill links resolve into the removed <code>.cyberos/plugin/skills</code> (checked across every family install writes) - test: <code>tools/install/tests/test_install_hygiene.sh::t_no_dangling_skill_links</code></li><li>[ ] AC 3 (traces_to: #1.3) - install then uninstall then install then uninstall on a foreign pre-commit hook leaves it byte-identical to the original (no accumulated blank line) - test: <code>tools/install/tests/test_install_hygiene.sh::t_hook_strip_byte_identical_across_cycles</code></li><li>[ ] AC 4 (traces_to: #1.4) - an operator <code>.mcp.json</code>, an unmarked <code>.agents/skills/&lt;cmd&gt;</code> dir, and a foreign hook's own lines all survive uninstall - test: <code>tools/install/tests/test_install_hygiene.sh::t_operator_files_preserved</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The v1-&gt;v2 hook upgrade path (<code>install.sh:856</code>) shares the leading-blank append shape; the widened strip MUST heal a v1 leftover too, not only a v2 block.</li><li><code>.cursor/mcp.json</code> exists only when the cursor agent was selected at install; uninstall MUST remove it when present and MUST NOT fail when absent.</li><li>Skill entries that landed as copy-fallbacks (no symlink support, or <code>CYBEROS_COPY_SKILLS=1</code>) are removed only when they carry the <code>.cyberos-owned</code> marker - a copy an operator made is byte-indistinguishable without it and MUST stay.</li><li>A <code>hooksPath</code> repo (core.hooksPath set) writes the hook outside <code>.git/hooks</code>; the strip MUST operate on the effective hooks dir the same way install and the existing strip do.</li><li>Security-class: uninstall reads paths and file content and executes nothing. Paths are confined under the repo root on the same <code>relUnderRoot</code> rule the other helpers use; a crafted target cannot walk out.</li></ul>";
