window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-103"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CUO service <strong>MUST</strong> ship trace replay rows at <code>services/cuo/src/trace/</code> capturing prompt+model+temp+seed per AI call + replay test, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>trace_call_kind</code> against closed enum per DEC-2331.</li></ol>\n<ol><li><strong>MUST</strong> capture per call at <code>writer.rs::write(call)</code> per DEC-2330:</li></ol>\n<ul><li>prompt (full text)</li><li>model (e.g. \"claude-sonnet-4-6\")</li><li>temperature</li><li>seed (or null if vendor doesn't support)</li><li>response (full output)</li></ul>\n<ol><li><strong>MUST</strong> replay at <code>replay.rs::replay(trace_id)</code> per DEC-2333:</li></ol>\n<ul><li>Recall prompt + model + temp + seed</li><li>Re-invoke AI provider with same params</li><li>Compare output bytes</li><li>Match → audit \"match\"; differ → audit \"drift\" sev-2</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0003</code>: ``<code>sql CREATE TABLE cuo_trace_rows ( trace_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, run_id UUID NOT NULL, checkpoint_id UUID,  -- TASK-CUO-102 ref kind TEXT NOT NULL CHECK (kind IN ('router_decision','tool_call','response_generation','validation_check')), prompt_text TEXT NOT NULL, model TEXT NOT NULL, temperature NUMERIC(5,4) NOT NULL, seed BIGINT, response_text TEXT NOT NULL, correction_of UUID REFERENCES cuo_trace_rows(trace_id), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ) PARTITION BY RANGE (created_at); CREATE INDEX trace_run_idx ON cuo_trace_rows(tenant_id, run_id, created_at); ALTER TABLE cuo_trace_rows ENABLE ROW LEVEL SECURITY; CREATE POLICY trace_rls ON cuo_trace_rows USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON cuo_trace_rows FROM cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/cuo/trace/{id}/replay     (CDO triggers replay test) GET  /v1/cuo/runs/{run_id}/trace   (list trace rows) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2334. PII per TASK-MEMORY-111: prompt + response SHA-256 hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from supervisor → AI call → writer → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior trace per DEC-2332 (REVOKE UPDATE/DELETE).</li></ol>\n<ol><li><strong>MUST NOT</strong> skip seed capture per DEC-2330 (even if null, document why).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why prompt + model + temp + seed (DEC-2330)?</strong> Reproducibility — these 4 fields are the AI call's inputs; with seed, output deterministic.</p>\n<p><strong>Why immutable (DEC-2332)?</strong> Audit lineage; without immutability, replay becomes circular.</p>\n<p><strong>Why drift detection (DEC-2333)?</strong> Vendor models version-drift; without drift alert, replay quietly produces different output.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample trace row:</p>\n<pre><code class=\"language-json\">{\n  \"trace_id\": \"uuid\",\n  \"run_id\": \"uuid\",\n  \"kind\": \"router_decision\",\n  \"prompt_text\": \"Given user request: ... Choose tool from: ...\",\n  \"model\": \"claude-sonnet-4-6\",\n  \"temperature\": 0.0,\n  \"seed\": 42,\n  \"response_text\": \"calendar.list_events\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>trace_call_kind enum cardinality 4</strong>. 2. <strong>prompt + model + temp + seed captured</strong>. 3. <strong>Immutable rows</strong>. 4. <strong>Replay test exists</strong>. 5. <strong>Match → audit emitted</strong>. 6. <strong>Drift → sev-2 audit</strong>. 7. <strong>4 memory audit kinds emitted</strong>. 8. <strong>PII scrubbed (prompt + response SHA256)</strong>. 9. <strong>RLS denies cross-tenant</strong>. 10. <strong>Trace_id preserved</strong>. 11. <strong>Append-only via REVOKE</strong>. 12. <strong>Monthly partitioning</strong>. 13. <strong>Replay async (LLM call expensive)</strong>. 14. <strong>rust_decimal for temperature</strong>. 15. <strong>bigint for seed</strong>. 16. <strong>NULL seed allowed (vendor-dependent)</strong>. 17. <strong>Correction_of for re-recorded traces</strong>. 18. <strong>CDO-only replay trigger</strong>. 19. <strong>Run-scoped query indexed</strong>. 20. <strong>Partition retention same as TASK-CUO-102 (7 years)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn replay_byte_identical_with_seed() {\n    let ctx = TestContext::with_trace_seed_42().await;\n    let r = ctx.replay(ctx.trace_id).await;\n    assert_eq!(r.status, \"match\");\n}\n\n#[tokio::test]\nasync fn drift_detected_when_response_differs() {\n    let ctx = TestContext::with_trace().await;\n    ctx.mock_ai_returns_different_response().await;\n    let r = ctx.replay(ctx.trace_id).await;\n    assert_eq!(r.status, \"drift\");\n    let audits = ctx.fetch_memory_audits(\"cuo.trace_replay_drift\").await;\n    assert!(!audits.is_empty());\n}\n\n#[tokio::test]\nasync fn immutable_no_update() {\n    let ctx = TestContext::with_trace().await;\n    let r = ctx.try_update_trace(ctx.trace_id).await;\n    assert!(r.is_err());\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-CUO-102. <strong>Cross-module:</strong> TASK-AI-003 (LLM calls captured), TASK-AUTH-101 (CDO role), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Mutation attempt</td><td>REVOKE</td><td>DB error</td><td>inherent</td></tr><tr><td>Replay drift</td><td>comparison</td><td>sev-2</td><td>investigate vendor</td></tr><tr><td>Vendor model deprecated</td><td>replay fail</td><td>sev-2</td><td>model upgrade plan</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>NULL seed (vendor)</td><td>OK</td><td>replay marked non-deterministic</td><td>inherent</td></tr><tr><td>Prompt &gt; 100k chars</td><td>validate</td><td>reject; sev-2</td><td>reduce</td></tr><tr><td>Replay rate limit</td><td>inherent</td><td>retry</td><td>inherent</td></tr><tr><td>Concurrent trace</td><td>append</td><td>inherent</td><td>inherent</td></tr><tr><td>Decimal precision</td><td>rust_decimal</td><td>inherent</td><td>inherent</td></tr><tr><td>Partition fill</td><td>monthly</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Seed defaults to 0 if vendor supports; null if not (e.g. older Claude models).</li><li>§11.2 Replay is async — LLM call latency; result populated when complete.</li><li>§11.3 memory audit body: trace_id, run_id, kind; prompt + response SHA256.</li><li>§11.4 Drift alert via TASK-CHAT-005 to CDO with link to trace + replay diff.</li><li>§11.5 Partition same as TASK-CUO-102 — monthly, 7-year retention.</li></ul>\n<hr>\n<p><em>End of TASK-CUO-103 spec.</em></p>";
