window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-KB-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The KB service <strong>MUST</strong> ship runbook tagging at <code>services/kb/src/runbook/</code> with 3-dim tags (provider/region/severity), OBS triage match, 2 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate enums per DEC-1951/1952/1953.</li></ol>\n<ol><li><strong>MUST</strong> define table extension at migration <code>0008</code>: ```sql ALTER TABLE kb_documents ADD COLUMN is_runbook BOOLEAN NOT NULL DEFAULT false; ALTER TABLE kb_documents ADD COLUMN runbook_providers TEXT[]; ALTER TABLE kb_documents ADD COLUMN runbook_regions TEXT[]; ALTER TABLE kb_documents ADD COLUMN runbook_severities TEXT[];</li></ol>\n<p>   ALTER TABLE kb_documents ADD CONSTRAINT runbook_providers_valid      CHECK (runbook_providers IS NULL OR runbook_providers &lt;@ ARRAY['aws','gcp','azure','vercel','supabase','custom']::TEXT[]);    ALTER TABLE kb_documents ADD CONSTRAINT runbook_regions_valid      CHECK (runbook_regions IS NULL OR runbook_regions &lt;@ ARRAY['global','sg-1','eu-1','us-1','vn-1']::TEXT[]);    ALTER TABLE kb_documents ADD CONSTRAINT runbook_severities_valid      CHECK (runbook_severities IS NULL OR runbook_severities &lt;@ ARRAY['sev1_critical','sev2_high','sev3_medium','sev4_low']::TEXT[]);</p>\n<p>   CREATE INDEX runbook_provider_idx ON kb_documents USING GIN (runbook_providers) WHERE is_runbook = true;    CREATE INDEX runbook_region_idx ON kb_documents USING GIN (runbook_regions) WHERE is_runbook = true;    CREATE INDEX runbook_severity_idx ON kb_documents USING GIN (runbook_severities) WHERE is_runbook = true;</p>\n<p>   GRANT UPDATE (is_runbook, runbook_providers, runbook_regions, runbook_severities) ON kb_documents TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> match for incident at <code>tag_matcher.rs::match(incident_provider, incident_region, incident_severity)</code> per DEC-1954:</li></ol>\n<ul><li>SELECT runbooks WHERE provider IN (incident_provider, 'custom') AND region IN (incident_region, 'global') AND severity IN (incident_severity, ...higher_severities)</li><li>Order by specificity (exact match &gt; global)</li></ul>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text PUT    /v1/kb/docs/{id}/runbook-tags    body: {providers, regions, severities} GET    /v1/kb/runbooks/match?provider=aws&amp;region=sg-1&amp;severity=sev1_critical </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 2 memory audit kinds per DEC-1955. PII per TASK-MEMORY-111: tag enums + counts ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from OBS-007 incident → matcher → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> accept invalid enum values per DEC-1951/1952/1953 (CHECK constraints).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 3 dims (DEC-1950)?</strong> Triage filtering needs all three — same-severity AWS runbook irrelevant if incident is GCP.</p>\n<p><strong>Why arrays (DEC-1950)?</strong> A runbook may apply to multiple providers (e.g. \"S3 / GCS bucket misconfigured\") — array models that naturally.</p>\n<p><strong>Why specificity ranking (DEC-1954)?</strong> Multiple matches likely — global runbook + region-specific; show specific first.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample runbook tag set:</p>\n<pre><code class=\"language-json\">PUT /v1/kb/docs/{id}/runbook-tags\n{\n  \"providers\": [\"aws\"],\n  \"regions\": [\"sg-1\"],\n  \"severities\": [\"sev1_critical\", \"sev2_high\"]\n}</code></pre>\n<p>Sample match query:</p>\n<pre><code class=\"language-json\">GET /v1/kb/runbooks/match?provider=aws&amp;region=sg-1&amp;severity=sev1_critical\n\nResponse:\n{\n  \"matches\": [\n    {\"doc_id\": \"uuid\", \"title\": \"AWS SG-1 critical outage runbook\", \"specificity_score\": 3},\n    {\"doc_id\": \"uuid\", \"title\": \"Global AWS outage runbook\", \"specificity_score\": 1}\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>provider enum cardinality 6</strong>. 2. <strong>region enum cardinality 5</strong>. 3. <strong>severity enum cardinality 4</strong>. 4. <strong>CHECK constraints enforce enum values</strong>. 5. <strong>Multi-tag match (provider AND region AND severity)</strong>. 6. <strong>Global tag matches any incident region</strong>. 7. <strong>Custom provider catches non-cloud-vendor incidents</strong>. 8. <strong>GIN indexes on each tag array</strong>. 9. <strong>Specificity ranking</strong>. 10. <strong>2 memory audit kinds emitted</strong>. 11. <strong>PII: tag enums (public) ok</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>CTO-only tag write</strong>. 15. <strong>TASK-OBS-007 integration tested</strong>. 16. <strong>Non-runbook docs not indexed (WHERE is_runbook)</strong>. 17. <strong>Empty tag arrays = no match (not all match)</strong>. 18. <strong>Append-only via REVOKE except 4 tag cols</strong>. 19. <strong>Multiple incident tags supported</strong>. 20. <strong>Severity escalation (higher sev runbooks shown for sev1)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn multi_tag_match_returns_specific_first() {\n    let ctx = TestContext::with_runbooks_aws_sg_and_global().await;\n    let r = ctx.match_runbooks(\"aws\", \"sg-1\", \"sev1_critical\").await;\n    assert!(r.matches[0].title.contains(\"SG-1\"));\n}\n\n#[tokio::test]\nasync fn global_tag_catches_any_region() {\n    let ctx = TestContext::with_global_runbook().await;\n    let r = ctx.match_runbooks(\"aws\", \"us-1\", \"sev1_critical\").await;\n    assert!(!r.matches.is_empty());\n}\n\n#[tokio::test]\nasync fn invalid_enum_rejected() {\n    let r = ctx.set_runbook_tags(ctx.doc_id, vec![\"invalid_provider\"], vec![\"sg-1\"], vec![\"sev1\"]).await;\n    assert!(r.is_err());\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-KB-001, TASK-OBS-007. <strong>Cross-module:</strong> TASK-MEMORY-111 (audit).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid enum tag</td><td>CHECK</td><td>400</td><td>use valid</td></tr><tr><td>Empty tag arrays</td><td>inherent</td><td>no match</td><td>inherent</td></tr><tr><td>Incident region missing</td><td>use 'global'</td><td>inherent</td><td>inherent</td></tr><tr><td>Multiple matches same specificity</td><td>tied</td><td>inherent</td><td>inherent</td></tr><tr><td>GIN index slow</td><td>EXPLAIN</td><td>tune</td><td>inherent</td></tr><tr><td>Tag updated mid-incident</td><td>snapshot at match time</td><td>inherent</td><td>inherent</td></tr><tr><td>Cross-tenant runbook leak</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Non-runbook doc tagged</td><td>is_runbook=true required</td><td>400</td><td>set flag</td></tr><tr><td>Severity hierarchy unclear</td><td>order: sev1&gt;sev2&gt;sev3&gt;sev4</td><td>inherent</td><td>inherent</td></tr><tr><td>Custom provider catch-all</td><td>inherent</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 GIN array index: <code>WHERE is_runbook = true</code> reduces index size 100x for non-runbook-heavy KBs.</li><li>§11.2 Specificity score: 3 for triple match, 2 for double, 1 for single (global).</li><li>§11.3 memory audit body: doc_id, tags; incident match audit includes incident_id from OBS-007.</li><li>§11.4 Tag updates surface in TASK-KB-007 Q&amp;A (runbook context for incident questions).</li><li>§11.5 Auto-suggest tags via TASK-AI-003 on doc save — CTO confirms.</li></ul>\n<hr>\n<p><em>End of TASK-KB-008 spec.</em></p>";
