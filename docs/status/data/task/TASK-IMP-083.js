window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-083"]="<h1>TASK-IMP-083: install lands the status-sync hook where core.hooksPath points</h1>\n<h2>Summary</h2>\n<p>The installer writes its status-sync pre-commit hook to <code>.git/hooks/pre-commit</code> unconditionally. Git runs hooks from <code>core.hooksPath</code> when that config is set, so on any repo using a custom hooks directory (the cyberos repo itself is one) the installed hook is inert and the group-A status sync silently dies. Teach install and uninstall to resolve the effective hooks directory and apply the existing ownership rules there, with the summary naming the real path written.</p>\n<h2>Problem</h2>\n<p>Step 6b hardcodes the location:</p>\n<p>&lt;untrusted_content source=\"tools/install/install.sh step 6b\"&gt; hk=\"$root/.git/hooks/pre-commit\" mkdir -p \"$root/.git/hooks\" &lt;/untrusted_content&gt;</p>\n<p><code>git config core.hooksPath</code> is never read (zero matches in the file). A repo configured with, say, <code>.githooks</code> - a common pattern, and exactly how the cyberos repo is set up - receives a hook git will never execute. The failure is silent: install prints \"pre-commit hook v2 installed\", the operator believes status sync is live, and <code>docs/status/</code> quietly lags every backlog write. The install.sh authors have already documented this exact bug class elsewhere in the file (\"a guard that skips when its own tool is missing is indistinguishable from success\").</p>\n<h2>Proposed Solution</h2>\n<p>Resolve the effective hooks directory once: <code>hooks_dir=\"$(git -C \"$root\" config core.hooksPath || true)\"</code>; empty means <code>.git/hooks</code> as today; a relative value resolves against <code>$root</code>; an absolute value is used as is. Then run the UNCHANGED ownership state machine (absent or ours-outright -&gt; standalone v2; ours-v1-append -&gt; upgrade; foreign -&gt; marked append; ours-v2 -&gt; keep) against <code>&lt;hooks_dir&gt;/pre-commit</code>, <code>mkdir -p</code>-ing the directory exactly as the current code does for <code>.git/hooks</code>. The summary line names the path actually written. Uninstall resolves the same way so it removes or unappends from where install wrote.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Warn only, keep writing <code>.git/hooks</code>. Rejected: a warning fixes nothing; the repo still ships with dead sync, and the operator has to hand-port the hook. The resolution is three lines.</li><li>Write BOTH locations. Rejected: git executes exactly one; the inert copy is litter that confuses the next ownership scan and doubles the uninstall surface.</li><li>Refuse to install the hook when hooksPath is set. Rejected: hooksPath repos are precisely the disciplined repos most likely to want the sync; refusing punishes them for configuring git properly.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: on a hooksPath repo, a backlog-touching commit regenerates and stages <code>docs/status/</code> - hook fires from the configured directory. Baseline (today): hook never fires on such repos. Deadline: this task's final acceptance.</li><li>Guardrail: on repos without hooksPath, the step's output and written bytes stay identical to today (regression case in the suite, asserted on every run).</li></ul>\n<h2>Scope</h2>\n<p>In scope: hooks-directory resolution in install step 6b and the matching resolution in uninstall; one summary line; hygiene-test scenarios.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>The hook bodies (standalone v2, append v2) - byte-identical to today.</li><li>Husky/pre-commit-framework adapters (their directories are covered generically by hooksPath resolution; framework-native config formats are a separate item if ever wanted).</li><li>The run-gates and docs-site sync paths (untouched).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>None upstream. Cone-disjoint from TASK-IMP-082 (renderer) and TASK-IMP-084 (docs-tools + skill prose); ships in the same parallel batch.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted by the model from IMPROVEMENT_HANDOFF.md IMP-02 plus direct source verification; implementation follows under ship-tasks supervision.</li><li><strong>Human review:</strong> PLAN approved by the operator on 2026-07-16; spec audit and both HITL acceptance gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 install MUST resolve the effective hooks directory as: <code>core.hooksPath</code> when set and non-empty (relative values resolved against the repo root, absolute used as is), else <code>.git/hooks</code>.</li><li>1.2 The existing hook ownership state machine MUST run unchanged against <code>&lt;hooks_dir&gt;/pre-commit</code>: absent or ours-outright writes standalone v2; a foreign hook receives the marked POSIX append block; an ours-v1 append upgrades to v2; an ours-v2 is kept. The hooks directory is created with <code>mkdir -p</code> when missing.</li><li>1.3 The install summary's auto-sync line MUST name the path actually written (e.g. <code>pre-commit hook v2 installed at .githooks/pre-commit</code>).</li><li>1.4 When <code>core.hooksPath</code> is unset, every written byte and every summary word of step 6b MUST be identical to today's behavior.</li><li>1.5 uninstall MUST resolve the hooks directory the same way and remove the standalone form or strip the appended block from <code>&lt;hooks_dir&gt;/pre-commit</code>, never touching <code>.git/hooks/pre-commit</code> when hooksPath points elsewhere. In the same change, uninstall's ownership test MUST become the exact line-2 check install.sh already uses (<code>_cyberos_owns_hook</code>), replacing its current <code>head -5 | grep</code> heuristic - today a FOREIGN hook shorter than five lines that carries our appended block matches the heuristic and is deleted whole (uninstall.sh:24-28), destroying the user's hook: the exact silent-data-loss class install.sh's own comment documents and fixed on the install side.</li><li>1.6 Non-git repos MUST keep today's skip behavior (\"skipped (not a git checkout)\").</li><li>1.7 Hygiene coverage MUST land as a new scenario block in <code>tools/install/tests/test_install_hygiene.sh</code> (t05): hooksPath set + no hook file -&gt; standalone lands there and fires on a backlog commit; hooksPath set + foreign hook -&gt; append there, foreign exit code preserved; hooksPath unset -&gt; regression-identical.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: §1 #1.1, #1.2) - hook lands in the configured directory - test: <code>tools/install/tests/test_install_hygiene.sh::t05_hookspath_standalone</code></li><li>[ ] AC 2 (traces_to: §1 #1.2) - foreign hook in hooksPath dir gets the marked append, exit code preserved - test: <code>tools/install/tests/test_install_hygiene.sh::t05_hookspath_foreign_append</code></li><li>[ ] AC 3 (traces_to: §1 #1.4) - unset hooksPath is byte-regression-identical - test: <code>tools/install/tests/test_install_hygiene.sh::t05_no_hookspath_regression</code></li><li>[ ] AC 4 (traces_to: §1 #1.5) - uninstall removes from the configured directory - test: <code>tools/install/tests/test_install_hygiene.sh::t05_hookspath_uninstall</code></li><li>[ ] AC 5 (traces_to: §1 #1.3) - summary names the real path - test: <code>tools/install/tests/test_install_hygiene.sh::t05_summary_names_path</code></li><li>[ ] AC 6 (traces_to: §1 #1.7) - the new scenarios run inside the existing hygiene suite - verify: <code>bash scripts/tests/run_all.sh</code> shows test_install_hygiene.sh ok with t05 counted (ops check recorded in the gate log).</li><li>[ ] AC 7 (traces_to: §1 #1.6) - non-git target keeps the skip line - test: <code>tools/install/tests/test_install_hygiene.sh::t05_non_git_skip</code></li><li>[ ] AC 8 (traces_to: §1 #1.5) - uninstall on a 3-line foreign hook carrying our appended block strips only the block, foreign body byte-preserved - test: <code>tools/install/tests/test_install_hygiene.sh::t05_short_foreign_uninstall_preserved</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Relative hooksPath (<code>.githooks</code>) vs absolute (<code>/tmp/hooks</code>) - both resolved; relative anchored at root, not at CWD (t05 exercises relative; absolute covered by the same resolver line, code-reviewed).</li><li>hooksPath configured but directory absent - <code>mkdir -p</code> creates it, matching today's <code>.git/hooks</code> treatment; git would use it the moment it exists.</li><li>hooksPath set to an empty string - treated as unset (<code>git config</code> returns empty; the resolver's emptiness check catches both).</li><li>Worktrees: <code>git -C root config</code> reads the shared config, hooksPath applies worktree-wide - correct by construction; noted for reviewers, no special code.</li><li>A hooksPath repo that ALREADY has an inert CyberOS hook at <code>.git/hooks/pre-commit</code> from an older install - out of scope to migrate automatically (touching a path git ignores is harmless); the summary's named path makes the situation visible. Recorded as a known leftover, not a failure.</li><li>Uninstall meets a short foreign hook (under five lines) that carries our appended block: the current head-5 heuristic classifies it as ours and deletes the whole file - after this task the exact line-2 test strips only our block (AC 8). Found during this task's own authoring audit, 2026-07-16.</li><li>Security-class: the append block stays POSIX-sh only and preserves the foreign exit code (existing invariant, re-asserted by AC 2); no new execution surface is introduced.</li></ul>\n<h2>4. Out of scope / non-goals</h2>\n<p>Duplicated intentionally with <code>## Scope</code> for template conformance: hook bodies, framework adapters, and legacy-location migration are untouched.</p>\n<h2>5. Protected invariants this task must not weaken</h2>\n<ul><li>Never clobber a foreign hook (the length-independent ownership test stays exact).</li><li>The appended block remains POSIX sh and preserves the foreign exit code.</li><li>Payload sync doctrine: rebuild dist, version-sync, full suite before commit.</li><li>HITL: both human-acceptance gates are recorded verdicts; the agent never sets done.</li></ul>\n<p><em>End of TASK-IMP-083.</em></p>";
