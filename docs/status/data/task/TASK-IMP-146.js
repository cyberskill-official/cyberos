window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-146"]="<h1>TASK-IMP-146: memory-append --dry-run</h1>\n<h2>Summary</h2>\n<p><code>memory-append.mjs append</code> has no rehearsal mode: the only way to learn whether a <code>status_overridden</code> payload will be accepted is to append it, and the chain is append-only, so a probe row is permanent. Add <code>--dry-run</code>, which runs every refusal the real path runs and reports the seq, chain hash and memory path the append would produce — touching no byte of the store.</p>\n<h2>Problem</h2>\n<p>The gated-flip path (<code>backlog-mutate flip … --verdict-by --verdict-evidence</code>) appends one <code>status_overridden</code> row per HITL verdict. Its payload has five required non-empty string fields, a safe-token constraint on <code>task_id</code>, and preconditions on the store itself (lease free, chain verifies, HEAD agrees with the rows). All of those are checked inside <code>cmdAppend</code>, and the only way to exercise them is to perform the append.</p>\n<p>Batch/8 shipping did exactly that and left probe rows on the live BRAIN store, which the post-1.2.0 plan called out as Wave 2 work (\"optional: <code>memory-append --dry-run</code>; avoid probe rows on the live store\"). <code>TASK-CUO-305</code> folded the other four friction items into <code>ship-tasks.md</code> and did not carry this one. Doctrine §6.5 forbids tail rewrites, so the operator's only alternative today is to copy the whole store and probe the copy — which also drops the one precondition that matters most, the live store's own state.</p>\n<h2>Proposed Solution</h2>\n<p>Add a <code>--dry-run</code> flag to <code>append</code>. In dry-run mode <code>cmdAppend</code> performs, in order: the same closed-kind / JSON-object / <code>status_overridden</code>-field / safe-token refusals it always performs before any write; a lease <em>inspection</em> that refuses with the same exit 3 when the lease is held (without minting one); the full chain walk and HEAD/tip reconciliation (reporting, never re-publishing, a one-behind HEAD); and then the seq, chain hash, prev_chain and memory path it would write. It creates no store root, writes no <code>.lock</code>, bootstraps no scaffold, sweeps no tmp litter, appends no frame, publishes no HEAD and rebuilds no <code>peaks.bin</code>. Against a store that does not exist, it reports the bootstrap it would perform instead of performing it.</p>\n<p>The lease check is factored out of <code>acquireLease</code> into an <code>inspectLease</code> helper so the dry-run and the real path share one refusal rule rather than growing a second copy of it.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong><code>--dry-run</code> as a separate <code>plan</code> command.</strong> Rejected: the flag composes with the same positional grammar and <code>--json</code> envelope the real command already has; a second command would duplicate the argument handling and the refusal ladder.</li><li><strong>Copy the store to a temp dir and append there.</strong> Rejected: that is today's workaround. It costs an O(store) copy, and it cannot check the live store's lease or HEAD — the two preconditions most likely to be the reason a real append refuses.</li><li><strong>Validate the payload only (no chain walk).</strong> Rejected: payload validation is the cheap half. The refusals that actually surprise an operator mid-flip are lock-held and HEAD/tip disagreement, and both are store-state checks.</li><li><strong>Let the dry run acquire and release the lease for fidelity.</strong> Rejected: a lease write is a store mutation, and a rehearsal that mutates the thing it is rehearsing against fails its one promise. Inspecting the lease gives the same refusal without the write.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a dry run against the live store returns the projected seq/chain and leaves every byte under the store root — <code>HEAD</code>, <code>.lock</code>, <code>audit/</code>, <code>audit/mmr/peaks.bin</code> — bit-identical, proven by a recursive hash before and after.</li><li>Guardrail: the real append path's exit codes, stdout and <code>--json</code> envelope are unchanged for every existing test in <code>tools/install/tests/test_memory_append.sh</code>.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the flag, the <code>inspectLease</code> refactor, help/header documentation, suite coverage, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>A dry-run mode for <code>verify</code> — verify already only reports (§6.5).</li><li>Any change to exit codes, output shape or behaviour of the real append path.</li><li>Teaching <code>backlog-mutate.mjs</code> to call the dry run before a flip (a possible follow-up; this task ships the capability, not a new gate).</li></ul>\n<h2>Dependencies</h2>\n<p>None. <code>TASK-IMP-141</code> (MMR sync) is <code>done</code> and its <code>syncMmrPeaks</code> call is one of the writes the dry run must skip.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) auditing the post-1.2.0 plan's Wave 2 residuals against live <code>main</code>.</li><li><strong>Scope:</strong> the plan's own framing was treated as a claim to verify, not as an input. <strong>re-derived and CONFIRMED:</strong> <code>cmdAppend</code> at HEAD performs seven distinct store writes — store-root <code>mkdir</code>, <code>.lock</code> lease, bootstrap scaffold, stale-tmp sweep, segment, <code>HEAD</code> publish and <code>peaks.bin</code> rebuild — each read directly out of the function. <strong>re-derived and CORRECTED:</strong> the plan calls this \"optional\"; the lease and HEAD/tip checks live inside the same function as the writes, so the copy-the-store workaround cannot exercise them — the item is a capability gap, not a convenience. <strong>measured and ADDED:</strong> <code>acquireLease</code> today mixes inspection and minting in one function, which is why this task factors out <code>inspectLease</code> rather than duplicating the staleness rule. No claim is made about paths outside <code>memory-append.mjs</code>.</li><li><strong>Human review:</strong> session operator instruction (2026-07-25) to close genuine plan gaps; HITL verdicts recorded with evidence at both gates.</li></ul>\n<h2>1. Description</h2>\n<ul><li>1.1 <code>append</code> MUST accept <code>--dry-run</code>. With it set, the command MUST NOT create, modify, delete or rename any path under the store root — specifically no store-root <code>mkdir</code>, no <code>.lock</code> write, no bootstrap scaffold, no stale-tmp sweep, no segment write, no <code>HEAD</code> publish and no <code>audit/mmr/peaks.bin</code> rebuild.</li><li>1.2 Every refusal the real path raises before writing MUST still be raised, with the same exit code: unknown kind, non-JSON or non-object payload, a <code>status_overridden</code> payload missing a required non-empty string field, an unsafe task token and a bad <code>--now</code> (2); a held or unparseable lease (3); a chain that does not verify or a HEAD that disagrees with the rows (4).</li><li>1.3 The lease rule MUST be single-sourced: <code>acquireLease</code> and the dry-run check MUST share one <code>inspectLease</code> implementation, so a change to staleness or TTL semantics cannot apply to one path and not the other.</li><li>1.4 On success the dry run MUST report the seq, chain hash, prev_chain, kind, memory path and actor the real append would produce for the same inputs, clock and store state.</li><li>1.5 With <code>--json</code>, the envelope MUST carry <code>dry_run: true</code> alongside the projected fields; without it, the prose line MUST say plainly that nothing was written.</li><li>1.6 A dry run against a store root that does not exist MUST exit 0, state that the real append would bootstrap the store, and leave the path absent.</li><li>1.7 A HEAD that is one behind the intact rows MUST be reported as the re-publish the real append would perform, not performed.</li><li>1.8 <code>--help</code>, the header usage block and <code>CHANGELOG.md</code> Unreleased MUST document the flag.</li></ul>\n<h2>Acceptance Criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.4, #1.5) — a dry run on a populated store projects the seq and chain a subsequent real append actually writes, emits <code>dry_run: true</code> under <code>--json</code>, and leaves a recursive hash of the store root identical before and after - test: <code>tools/install/tests/test_memory_append.sh::t06_dry_run_writes_nothing</code></li><li>[ ] AC 2 (traces_to: #1.2, #1.3) — every refusal class exits with its documented code under <code>--dry-run</code> and leaves the store hash unchanged, and both paths route through the single <code>inspectLease</code> helper - test: <code>tools/install/tests/test_memory_append.sh::t07_dry_run_refuses_identically</code></li><li>[ ] AC 3 (traces_to: #1.6, #1.7) — a dry run against a nonexistent store root exits 0, reports the bootstrap and creates no path; a one-behind HEAD is reported rather than re-published - test: <code>tools/install/tests/test_memory_append.sh::t08_dry_run_bootstrap_and_head_report</code></li><li>[ ] AC 4 (traces_to: #1.8) — <code>--help</code> names <code>--dry-run</code> and CHANGELOG Unreleased records it - test: <code>tools/install/tests/test_memory_append.sh::t09_dry_run_documented</code></li></ul>\n<h2>Test plan</h2>\n<ol><li><code>bash tools/install/tests/test_memory_append.sh</code></li><li><code>bash tools/install/tests/test_hitl_lock.sh</code></li><li><code>bash scripts/tests/run_all.sh</code></li><li><code>bash .cyberos/cuo/gates/run-gates.sh</code></li></ol>\n<h2>3. Edge cases</h2>\n<ul><li><strong>A concurrent writer appends between the dry run and the real append.</strong> The projected seq/chain then differ from what lands — inherent to a rehearsal, and the dry run's own refusal ladder is what protects the real call. The output says \"would write\", not \"will\".</li><li><strong>The clock moves between the rehearsal and the real call.</strong> <code>ts_ns</code> is inside the hashed record, so an unpinned wall clock gives a different chain hash for the same payload. The projection is exact under the tool's documented determinism contract (same store, args, actor and <code>--now</code>/<code>CYBEROS_NOW</code>); the seq and path are exact either way.</li><li><strong>Store root exists but is empty (no HEAD, no rows).</strong> Same as the nonexistent case: report the bootstrap, create nothing — the real path's bootstrap branch is the one being described.</li><li><strong>A store with audit rows but no HEAD.</strong> Refuses with exit 4 exactly as the real path does; the dry run must not \"helpfully\" treat it as a bootstrap.</li><li><strong>A compacted (<code>.binlog.zst</code>) store.</strong> <code>listSegments</code> refuses with exit 4 during the walk, which the dry run inherits unchanged.</li><li><strong>Security-class:</strong> the flag strictly reduces what the tool may do; it grants no new read, adds no network or secret surface, and cannot be used to write a row that the normal path would refuse.</li></ul>";
