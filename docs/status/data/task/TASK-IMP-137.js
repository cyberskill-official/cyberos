window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-137"]="<h1>TASK-IMP-137: Install portability - MCP loopback+token, shasum fallback, atomic vendor</h1>\n<h2>Summary</h2>\n<p>Five verified portability/exposure defects in the install channel: the MCP server's <code>--http</code> mode binds every interface with zero auth while serving repo-rewriting, shell-running tools; <code>bootstrap.sh</code> hardcodes GNU <code>sha256sum</code>, breaking checksum verification on stock macOS; the payload's <code>engines</code> field admits Node 18 while shipping a subpackage that demands 24; the README documents a GitHub Action channel at a path install never creates; and the vendor step's <code>rm -rf</code> + <code>cp -R</code> leaves a reader-visible window where <code>.cyberos/</code> is missing or partial. This task closes all five: loopback-by-default + optional bearer token, <code>shasum</code> fallback, one engine floor, a real <code>ci/</code> vendor + docs truth, and stage-then-swap vendoring.</p>\n<h2>Problem</h2>\n<p>All verified first-hand 2026-07-23:</p>\n<ol><li><strong>LAN exposure by default (audit C4).</strong> <code>cyberos-mcp.mjs:219</code> calls <code>.listen(port)</code> with no host, binding <code>0.0.0.0</code>. <code>POST /mcp</code> accepts unauthenticated JSON-RPC and the tool set includes <code>task_install</code> (rewrites the repo) and <code>task_gates</code> (runs <code>run-gates.sh</code>, which <code>eval</code>s configured commands). Anyone on the local network can drive both. The code comment defers auth to a production reverse proxy, but the <em>default</em> posture is the exposure.</li><li><strong>Checksum verification fails on macOS.</strong> <code>bootstrap.sh:23</code> pipes to <code>sha256sum -c</code>; stock macOS ships <code>shasum</code>, not GNU coreutils. The security step of the curl|bash channel errors out on the platform the project is developed on - inviting users to bypass it.</li><li><strong>Engines contradiction.</strong> The generated payload admits <code>node &gt;=18</code> (<code>build.sh:358</code>); the shipped <code>mcp/package.json</code> demands <code>&gt;=24 &lt;25</code>; every <code>.nvmrc</code> pins 24.18.0. A Node-18 user passes npm's engine check and then runs components nobody has ever tested on 18.</li><li><strong>Phantom CI channel.</strong> README:158 tells operators to point a workflow at the composite action \"after <code>install.sh</code> has committed <code>.cyberos/</code> to the repo\" - but install.sh never copies <code>ci/</code>, so the documented path does not exist in any installed repo.</li><li><strong>Partial-vendor window.</strong> Between <code>rm -rf \"$CY/cuo\"</code> and the completed <code>cp -R</code>, an agent reading <code>.cyberos/</code> (which agents do constantly - it is their entry point) sees a half-machine. The TASK-IMP-103 lock serializes installers only.</li></ol>\n<h2>Proposed Solution</h2>\n<p><strong>MCP:</strong> <code>--http</code> binds <code>127.0.0.1</code> by default; a new <code>--host &lt;addr&gt;</code> flag opts into wider binding and its help text names the exposure; when the env var <code>CYBEROS_MCP_TOKEN</code> is set non-empty, every <code>POST /mcp</code> must carry <code>Authorization: Bearer &lt;token&gt;</code> (401 otherwise), while <code>GET /healthz</code> stays open for probes; binding non-loopback WITHOUT a token prints a loud warning. <strong>bootstrap.sh:</strong> detect <code>sha256sum</code> else fall back to <code>shasum -a 256</code>; fail with a clear message only when neither exists. <strong>Engines:</strong> the generated payload package.json pins <code>\"node\": \"&gt;=24 &lt;25\"</code>, matching the .nvmrc floor and the mcp subpackage. <strong>CI channel:</strong> install.sh vendors <code>ci/</code> into <code>.cyberos/ci/</code> (ownership-marked like the other vendored trees), and README:158's instructions are corrected to reference the installed path with a working <code>uses:</code> example. <strong>Atomic vendor:</strong> each vendored subtree is staged as <code>\"$CY/&lt;name&gt;.tmp.&lt;nonce&gt;\"</code> then swapped into place (<code>rm -rf</code> old + <code>mv</code> staged) so the reader-visible gap per subtree shrinks from the full copy duration to a rename; the existing install lock continues to serialize installers. A new suite covers all five behaviors.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Full TLS/OAuth on the MCP HTTP mode.</strong> Rejected for this task: the connector is a local-first developer channel; loopback default + optional bearer token closes the unauthenticated-LAN hole without dragging in cert management. Production deployments keep the documented reverse-proxy story, now as defense-in-depth rather than the only defense.</li><li><strong>Deny <code>--http</code> entirely unless a token is set.</strong> Rejected: loopback-only unauthenticated use (the overwhelmingly common local case) is not the vulnerability; the LAN binding is. Requiring tokens for localhost adds friction with no threat-model gain.</li><li><strong>Relax mcp/package.json to <code>&gt;=18</code> instead of raising the payload floor.</strong> Rejected: no CI or developer machine tests Node 18 (every .nvmrc is 24.x); an engines field is a compatibility <em>promise</em>, and promising untested compatibility is the stub-workflow lie in a different file. Raising the floor is honest; users on older Node get a clear npm engines error instead of a runtime surprise.</li><li><strong>Docs-only fix for the CI channel (point at the npm package path).</strong> Rejected: a composite action consumed from the repo's own committed tree (<code>.cyberos/ci/github-action</code>) is the standard, zero-download pattern and matches what the README already promised; making the promise true is smaller than re-teaching it.</li><li><strong>Whole-<code>.cyberos/</code> staged swap for atomicity.</strong> Rejected: <code>.cyberos/</code> contains machine-local state that must survive installs (<code>config.yaml</code>, <code>gates.env</code> backups, <code>memory/store/</code>); swapping the whole dir risks the config-wipe class. Per-subtree swaps touch exactly the machine-owned trees.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release - <code>--http</code> with no flags refuses remote connections (bind 127.0.0.1, verified by connect attempt from a non-loopback source address in the test harness); with <code>CYBEROS_MCP_TOKEN</code> set, tokenless POSTs get 401; <code>bootstrap.sh</code> completes checksum verification on a machine with only <code>shasum</code>; the payload package.json engines equals <code>&gt;=24 &lt;25</code>; <code>.cyberos/ci/github-action/action.yml</code> exists after a scratch install; and no reader observes a missing vendored subtree during an install loop (sampled reader in the test). Baselines today: all five fail.</li><li>Guardrail: stdio MCP mode (the default channel) is byte-for-byte unaffected; existing install suites (<code>test_install_hygiene.sh</code>, <code>test_install_lock.sh</code>, <code>test_e2e_skeleton.sh</code>) stay green.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the five fixes above, their README/mcp-README corrections, the new test suite, CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>TLS termination, OAuth flows, or multi-user token management for the MCP connector (reverse-proxy guidance remains the production story).</li><li>The fail-closed gates behavior and gates.env header - TASK-CUO-302.</li><li>Uninstall-side preservation of <code>.cyberos/ci/</code> (uninstall completeness is TASK-IMP-126's domain; the new tree is ownership-marked so existing uninstall logic handles it).</li><li>The G16 reinstall-idempotency benchmark definition - TASK-IMP-140 (its checker exercises the atomic-vendor behavior this task ships; soft forward reference, no cycle).</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. TASK-IMP-076 (done) shipped the MCP server and its <code>--http</code> mode; TASK-IMP-103 (done) shipped the installer lock whose reader gap this task closes the other half of. TASK-IMP-140's G16 gate builds on the atomic-vendor guarantee - forward reference only.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> the bind call, the missing auth check, the <code>sha256sum</code> hardcode, the three engines values, the phantom <code>ci/</code> path, and the rm/cp window were each verified by direct source read at HEAD; the payload files list (<code>\"ci\"</code> present in npm <code>files</code>, absent from install.sh's vendor list) was cross-checked both sides.</li><li><strong>Human review:</strong> the hardening plan was operator-approved 2026-07-23; the engines-up and vendor-ci decisions are recorded in <code>source_decisions</code> for the review acceptance gate.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>cyberos-mcp.mjs --http</code> MUST bind <code>127.0.0.1</code> when no host is specified; a new <code>--host &lt;addr&gt;</code> argument MUST be the only way to bind any other address, and starting non-loopback without a token MUST print a warning naming the exposure.</li><li>1.2 When the environment variable <code>CYBEROS_MCP_TOKEN</code> is set non-empty, every <code>POST /mcp</code> request MUST be rejected 401 (JSON-RPC error body) unless it carries <code>Authorization: Bearer &lt;token&gt;</code> with an exact token match; <code>GET /healthz</code> MUST remain unauthenticated. Token comparison MUST be constant-time-safe in intent (no early-exit substring tricks) though the threat model is LAN, not timing labs.</li><li>1.3 <code>bootstrap.sh</code> MUST verify the payload checksum via <code>sha256sum -c</code> when available, else <code>shasum -a 256 -c</code>, and MUST abort with a message naming both tools when neither exists. The fallback MUST verify, not skip - absence of GNU coreutils is not permission to trust the network.</li><li>1.4 The payload package.json generated by <code>build.sh</code> MUST declare <code>\"engines\": { \"node\": \"&gt;=24 &lt;25\" }</code>, matching the repo's <code>.nvmrc</code> floor and the shipped mcp subpackage.</li><li>1.5 <code>install.sh</code> MUST vendor the payload's <code>ci/</code> tree into <code>.cyberos/ci/</code> (ownership-marked consistently with the other vendored trees), and <code>tools/install/README.md</code>'s GitHub Action section MUST reference the installed path with a valid <code>uses: ./.cyberos/ci/github-action</code> example.</li><li>1.6 Each vendored subtree replacement in <code>install.sh</code> MUST be staged (<code>cp -R</code> into <code>\"$CY/&lt;name&gt;.tmp.&lt;nonce&gt;\"</code> first) and swapped into place so the reader-visible absence window per subtree is bounded by directory rename/move operations, not by copy duration. Stray <code>*.tmp.*</code> staging dirs from killed installs MUST be cleaned at the next install start.</li><li>1.7 <code>CHANGELOG.md</code> MUST record all five changes, marking the engines raise and the loopback default as breaking for consumers who relied on Node 18 or LAN binding.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - <code>--http</code> with no host accepts loopback connections and refuses non-loopback (asserted via the OS-reported bound address AND a failed connect from a secondary address where the harness supports it); <code>--host 0.0.0.0</code> binds wide and tokenless startup prints the exposure warning - test: <code>tools/install/tests/test_install_portability.sh::t01_loopback_default</code></li><li>[ ] AC 2 (traces_to: #1.2) - with <code>CYBEROS_MCP_TOKEN=secret</code>: tokenless POST /mcp gets 401, wrong token gets 401, correct Bearer succeeds, GET /healthz succeeds tokenless - test: <code>tools/install/tests/test_install_portability.sh::t02_bearer_token_enforced</code></li><li>[ ] AC 3 (traces_to: #1.3) - on a PATH without <code>sha256sum</code> but with <code>shasum</code>, bootstrap's verification step succeeds against a good archive and FAILS against a corrupted one (the fallback verifies); with neither tool, it aborts naming both - test: <code>tools/install/tests/test_install_portability.sh::t03_shasum_fallback_verifies</code></li><li>[ ] AC 4 (traces_to: #1.4) - a scratch payload's package.json engines equals <code>&gt;=24 &lt;25</code> exactly - test: <code>tools/install/tests/test_install_portability.sh::t04_engines_unified</code></li><li>[ ] AC 5 (traces_to: #1.5) - after a scratch install, <code>.cyberos/ci/github-action/action.yml</code> exists, and README's section contains the <code>uses: ./.cyberos/ci/github-action</code> form with no remaining claim that dist paths work post-install - test: <code>tools/install/tests/test_install_portability.sh::t05_ci_channel_real</code></li><li>[ ] AC 6 (traces_to: #1.6) - a reader loop polling <code>.cyberos/cuo/ship-tasks.md</code> existence during 20 reinstall iterations observes zero absences; a simulated kill between stage and swap leaves the OLD tree intact and the next install cleans the stray staging dir - test: <code>tools/install/tests/test_install_portability.sh::t06_atomic_swap_no_reader_gap</code></li><li>[ ] AC 7 (traces_to: #1.7) - CHANGELOG's top entry mentions all five changes and the word \"breaking\" for engines + binding - test: <code>tools/install/tests/test_install_portability.sh::t07_changelog_five_changes</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>Agent UIs that connected to the LAN-bound port yesterday:</strong> after upgrade they must either run on the same host (loopback) or pass <code>--host</code> deliberately - the CHANGELOG breaking note plus the startup warning carry the migration; silence would re-create the exposure by habit.</li><li><strong>Token set but empty (<code>CYBEROS_MCP_TOKEN=\"\"</code>):</strong> treated as unset (no auth), because an empty bearer token is unusable as a credential; the mcp README documents this explicitly.</li><li><strong><code>/healthz</code> information disclosure:</strong> the health body includes server name/version only (as today); the token gate deliberately excludes it so probes work, and no tool metadata is served there.</li><li><strong>macOS with Homebrew coreutils installed:</strong> <code>sha256sum</code> exists and wins - the fallback ordering preserves today's behavior wherever it already worked.</li><li><strong>Node 18 consumer pinned by their own CI:</strong> npm refuses install with a clear engines error - the intended outcome; the CHANGELOG names the floor and the .nvmrc value to adopt.</li><li><strong>Kill mid-swap (between rm of old and mv of staged):</strong> the window is the pathological remnant - the test's kill simulation targets stage-complete/pre-swap (old tree intact); a kill inside the rm+mv pair itself is bounded by two syscalls and the next install's staging cleanup + full re-vendor restores the machine; the install lock keeps a second installer out of the gap either way.</li><li><strong>Security-class:</strong> this task is itself a security fix (default exposure removal + verified checksums). The bearer token lives in an env var, never in repo files; tests use throwaway values; no token is logged (the warning names the CONDITION, not the secret).</li></ul>";
