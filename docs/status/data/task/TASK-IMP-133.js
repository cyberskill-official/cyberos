window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-133"]="<h1>TASK-IMP-133: Homebrew tap: update cyberos-cli.rb for the cs rename</h1>\n<h2>Summary</h2>\n<p>Once TASK-IMP-130 ships an npm release with the renamed <code>cs</code> bin, the separate <code>cyberskill-official/homebrew-tap</code> repo's <code>Formula/cyberos-cli.rb</code> needs its pinned version bumped to that release and its <code>test do</code> assertion updated from <code>bin/\"cyberos\"</code> to <code>bin/\"cs\"</code> — done together, since bumping one without the other makes <code>brew test</code> fail.</p>\n<h2>Problem</h2>\n<p><code>Formula/cyberos-cli.rb</code> (<code>cyberskill-official/homebrew-tap</code>, re-fetched fresh 2026-07-22) pins <code>url \"https://registry.npmjs.org/@cyberskill/cyberos/-/cyberos-1.0.9.tgz\"</code> and asserts <code>bin/\"cyberos\"</code> exists and is executable (lines 15-16, 26-32). <code>bin.install_symlink Dir[\"#{libexec}/bin/*\"]</code> (line 23) itself needs no code change — it will pick up whatever bin name a newly-pinned npm tarball declares, automatically. But the Formula is pinned to a SPECIFIC already-released tarball (1.0.9), which still ships the old <code>cyberos</code> bin (TASK-IMP-130 has not shipped yet as of this task's authoring). Updating only the <code>test do</code> block's string to <code>bin/\"cs\"</code> while leaving <code>url</code>/<code>sha256</code> pointed at 1.0.9 would make <code>brew test</code> fail every time — the installed payload would still only contain a <code>cyberos</code> bin, and the test would assert for a <code>cs</code> bin that isn't there. The plan (§7) already flagged that this Formula \"will silently start asserting against a binary that no longer exists,\" but the specific mechanism — that the fix requires bumping the pinned release, not just editing a string — is a fact this task's authoring surfaced by reading the Formula directly, not something the plan itself stated.</p>\n<h2>Proposed Solution</h2>\n<p>Once an npm release carrying the <code>cs</code> bin (TASK-IMP-130) is published, update <code>Formula/cyberos-cli.rb</code> in the SAME commit: (1) <code>url</code> to that release's tarball URL, (2) <code>sha256</code> to that tarball's actual digest (re-derived via the Formula's own documented method, line 11: <code>curl -sL -o t.tgz \"$(npm view @cyberskill/cyberos dist.tarball)\" &amp;&amp; sha256sum t.tgz</code>), (3) the <code>test do</code> block's two assertions from <code>bin/\"cyberos\"</code> to <code>bin/\"cs\"</code>, and (4) the header comment's \"They share the name 'cyberos' upstream\" sentence, which becomes inaccurate once the upstream bin is <code>cs</code> — reworded to describe the current state accurately. The Formula's own name (<code>cyberos-cli</code>, <code>brew install cyberos-cli</code>) does NOT change — that identifier disambiguates this Formula from the tap's separate <code>cyberos</code> GUI Cask (line 4-8) and is independent of the wrapped CLI's own bin name.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Update the Formula's test assertion immediately, ahead of TASK-IMP-130 shipping. Rejected: there would be nothing yet to test against — <code>brew test</code> would fail deterministically until a real <code>cs</code>-bin release exists, for no benefit over waiting.</li><li>Rename the Formula itself (e.g. to <code>cs-cli</code>) to match the new bin name. Rejected: out of scope per the plan, and the Formula's name was already a deliberate disambiguation choice (TASK-APP-006, cited in the file's own header) independent of the wrapped tool's bin name; renaming it would be a second breaking change (<code>brew install cyberos-cli</code> stops resolving) layered onto the one this plan already accepts.</li><li>Leave the header comment as-is since it's \"just a comment.\" Rejected: the comment specifically explains a naming decision by asserting a fact (upstream shares the name \"cyberos\") that this very task makes false; leaving it uncorrected actively misleads the next person who reads it to understand why the Formula is named the way it is.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: after this change, <code>brew install cyberos-cli</code> (or <code>brew test cyberos-cli</code> in CI) installs a working <code>cs</code> binary and the Formula's own test passes. Baseline today: the Formula asserts <code>bin/\"cyberos\"</code>, which will stop existing the moment a <code>cs</code>-bin npm release ships, with nothing in this Formula ready for that day.</li><li>Guardrail: <code>url</code>/<code>sha256</code> and the <code>test do</code> assertions are bumped in the SAME commit — never a state where one has moved to the new bin name and the other has not, which is exactly the deterministic-<code>brew test</code>-failure window this task exists to avoid ever existing.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>Formula/cyberos-cli.rb</code>'s <code>url</code>, <code>sha256</code>, <code>test do</code> block, and header comment, in <code>cyberskill-official/homebrew-tap</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Renaming the Formula itself away from <code>cyberos-cli</code>.</li><li>The tap's separate <code>cyberos</code> GUI Cask — a different product, unaffected by this rename (Formula header comment lines 4-6 already establish this distinction).</li><li><code>depends_on \"node\"</code> or the <code>install</code> method's <code>npm install</code> invocation — neither references the bin name and neither needs to change.</li><li>Publishing the actual npm release this task depends on — that is TASK-IMP-130's job; this task only reacts to it once it exists.</li></ul>\n<h2>Dependencies</h2>\n<p>Depends on TASK-IMP-130 — specifically, on a PUBLISHED npm release carrying the <code>cs</code> bin existing, not merely on TASK-IMP-130's code being merged. This task cannot land (in the sense of passing <code>brew test</code>) until that release exists, even though its diff could technically be drafted earlier. This task is a soft (non-status-gating) prerequisite for TASK-IMP-134's manual release-time checklist, which the plan (§6 item 7) states must include \"a fresh Homebrew install once the tap is updated\" — TASK-IMP-134's own <code>depends_on</code> deliberately excludes this task so its fully-automated offline portion isn't blocked on this task's externally-gated completion (see TASK-IMP-134's Dependencies section).</p>\n<p><strong>Cross-repo note.</strong> This task's spec, audit, and backlog row live in <code>cyberskill-official/cyberos</code>'s <code>docs/tasks/</code> (this repo's <code>.cyberos/</code> machine is what generated it), but its actual code change lands in the separate <code>cyberskill-official/homebrew-tap</code> repository, which has no <code>.cyberos/</code> machine of its own. <code>/ship-tasks</code> driving this task will need to operate against a checkout of <code>homebrew-tap</code>, not this repo — flagged explicitly since every other task in this batch is a same-repo change and this one is not.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill inside Cowork.</li><li><strong>Scope:</strong> <code>Formula/cyberos-cli.rb</code>'s content was re-fetched via a fresh shallow clone of <code>cyberskill-official/homebrew-tap</code> during this authoring session, not carried over from an earlier read in the same conversation without re-verification, per the anti-fabrication discipline for cross-repo evidence.</li><li><strong>Human review:</strong> task decomposition and cross-repo placement approved at the 2026-07-22 PLAN gate.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>Formula/cyberos-cli.rb</code>'s <code>url</code> and <code>sha256</code> MUST be updated together, in the same commit, to point at the first published npm release of <code>@cyberskill/cyberos</code> that ships the <code>cs</code> bin.</li><li>1.2 In that same commit, the <code>test do</code> block's two <code>bin/\"cyberos\"</code> assertions MUST become <code>bin/\"cs\"</code>.</li><li>1.3 The header comment's sentence asserting the CLI and the Cask \"share the name 'cyberos' upstream\" MUST be reworded to state the current, accurate naming (the CLI's public bin is <code>cs</code>; the GUI Cask remains a separate product named <code>cyberos</code>).</li><li>1.4 The Formula's own name (<code>cyberos-cli</code>, i.e. <code>brew install cyberos-cli</code>) MUST NOT change.</li><li>1.5 This task MUST NOT be merged before a real npm release containing the <code>cs</code> bin exists — the commit may be prepared in advance, but landing it early would deterministically break <code>brew test</code> (traced by AC 2: a passing local <code>brew test</code> run is only possible once such a release genuinely exists at the pinned <code>url</code>, so AC 2 discharges this clause directly rather than needing a separate procedural check).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - <code>Formula/cyberos-cli.rb</code>'s <code>url</code> references a version string matching a real published <code>@cyberskill/cyberos</code> release that contains a <code>\"cs\"</code> key in that release's <code>package.json</code> <code>bin</code> field, <code>sha256</code> matches that exact tarball's digest, and <code>git log -p</code> for the landing commit shows both fields changed together in one commit (not staged across two) - test: manual, ops flow (<code>homebrew-tap</code> has no CI workflow at all, confirmed by directory listing, so no automated check can run against the live npm registry from either repo) - the release engineer re-runs the Formula's own documented derivation command (line 11), diffs the result against what's committed, and confirms via <code>git log</code> that url+sha256 landed in the same commit as this task's other changes, as part of the release checklist</li><li>[ ] AC 2 (traces_to: #1.2, #1.5) - <code>brew install --build-from-source cyberos-cli &amp;&amp; brew test cyberos-cli</code>, run locally by the implementer before opening the PR (there is no CI in <code>homebrew-tap</code> to run this automatically — confirmed no <code>.github/workflows/</code> exists in this repo), passes against the updated Formula - test: documented in the PR description as a local run's output, not inferred from a CI check that does not exist; this same passing run is the evidence clause 1.5 requires (a real release must exist for <code>brew test</code> to succeed at all)</li><li>[ ] AC 3 (traces_to: #1.3) - a grep of the header comment block (lines 1-8) for the literal string <code>They share the name \"cyberos\" upstream</code> returns zero matches, and the same block contains the string `<code> </code>cs<code> </code><code> adjacent to a mention of \"bin\" or \"command\" - test: </code>grep -c 'They share the name \"cyberos\" upstream' Formula/cyberos-cli.rb<code> returns </code>0<code> AND </code>grep -c '<code>cs</code>' Formula/cyberos-cli.rb<code> returns </code>&gt;=1`, run as part of the same PR-review pass as AC 2 (still no CI; a reviewer command, not an automated gate)</li><li>[ ] AC 4 (traces_to: #1.4) - the Formula's class name (<code>CyberosCli</code>) and filename (<code>cyberos-cli.rb</code>) are unchanged in the diff - test: <code>git diff --stat</code> for this change shows no rename, only in-place edits to <code>cyberos-cli.rb</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>None of the five tasks in this batch (TASK-IMP-130 through 134) has its own acceptance criterion requiring an actual npm release be CUT and PUBLISHED — TASK-IMP-130's own ACs only prove a scratch build's <code>package.json</code> has the right <code>bin</code> field, not that a release reaches the registry. Cutting a release is an operational step (<code>scripts/release.sh</code>, per this repo's own README \"Versioning\" section) outside any single task's scope, but this task and TASK-IMP-134 both silently depend on it having happened. Named here explicitly rather than assumed, since it is a real gap in the plan's task set (plan §6) that this task's authoring surfaced, not something this task can close on its own - flagged in the batch report for the operator's awareness.</li><li>If TASK-IMP-130 ships but the package NAME also somehow changes (rejected in TASK-IMP-130's own Alternatives Considered, but if a future decision reverses that) - this task's <code>url</code> would need to reference the new package name too, not just a new version of the same package; not applicable under TASK-IMP-130's current, approved decision, but named here since this task's own correctness depends on that upstream decision holding.</li><li>A user with an existing <code>cyberos-cli</code> Homebrew install upgrades via <code>brew upgrade</code>: Homebrew's own symlink-relinking on upgrade is standard Homebrew behaviour, not something this Formula controls beyond declaring the correct bin via <code>install_symlink</code> - no special handling needed in this task.</li><li>This task's own spec, audit, and backlog entry exist only in <code>cyberskill-official/cyberos</code>'s <code>docs/tasks/</code> - if that BACKLOG row is marked <code>done</code> before the actual <code>homebrew-tap</code> PR merges, the two repos' records would disagree about whether the work shipped. <code>/ship-tasks</code>'s human final-acceptance gate is the safeguard here - it MUST NOT be granted until the <code>homebrew-tap</code> PR is confirmed merged, not merely opened.</li><li>Security-class: this task edits <code>sha256</code> and <code>url</code> fields that Homebrew uses to verify tarball integrity before install - an incorrect (rather than merely outdated) <code>sha256</code> would cause <code>brew install</code> to fail closed (checksum mismatch), not install something unverified, so the failure mode of a mistake here is safe (loud failure) rather than silent (installing unverified content).</li></ul>";
