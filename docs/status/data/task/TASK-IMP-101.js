window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-101"]="<h1>TASK-IMP-101: ship-tasks reconcile entry phase and depends_on evidence gating</h1>\n<h2>Summary</h2>\n<p>TASK-IMP-100 builds the measuring instrument; this task wires it into doctrine. ship-tasks gains a conditional entry phase - encountering a task whose status claims work this workflow cannot vouch for (past ready_to_implement with no manifest, a stale manifest, or missing phase artefacts) triggers task-reconcile and presents the report at a HITL gate with the three-way fork; and a depends_on evidence gate - a task is not started while an upstream done dependency lacks evidence (workflow artefacts or an accepted reconcile verdict), overridable only by the human at the gate. workflow_version 2.6.4 to 2.7.0: a new mechanism, not a wording fix.</p>\n<h2>Problem</h2>\n<p>The state engine's trust model had two legs (own manifests, own gates) and silently extended that trust to any status cell it read. After 086, \"the index says done\" is known to be a claim, not a fact - the workflow text must say what happens when claims outrun evidence, and must stop new work from stacking on unverified foundations by default.</p>\n<h2>Proposed Solution</h2>\n<p>Two normative additions in ship-tasks.md, plus chain wiring: (a) <strong>Reconcile entry §</strong>: the trigger conditions, the invocation (<code>node .cyberos/docs-tools/task-reconcile.mjs &lt;id&gt; --run-tests</code>), the HITL gate presenting the report's single recommendation with the fork {resume_at_phase / route_back / adopt_candidate - backfill artefacts then re-enter}, the rule that the agent NEVER executes any branch without the recorded verdict, and route_back's mechanics (§1.3, routed_back_count += 1). (b) <strong>depends_on evidence gate §</strong>: before starting any task, each depends_on id with status done MUST carry evidence - a coverage-gate artefact in either artefact home, or a reconcile-report with a human-accepted verdict; otherwise the task is BLOCKED and the block is presented at a gate where the human may override (override recorded as memory.status_overridden). (c) skill_chain gains conditional step 0 (skill: task-reconcile, condition: entry state drifted) with reconcile_report in outputs. t14 gates the two passages + 2.7.0 in source and payload; the t12/t09 exact pins move to 2.7.0 (the known pair, disclosed).</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Warn-only deps gating. Rejected by the recorded decision: propagating onto unverified foundations is the expensive failure; override keeps velocity available deliberately.</li><li>Hard-block without override. Rejected: historical done tasks predate these artefact conventions; a human override with a recorded row is the honest escape hatch.</li><li>Reconcile as a separate operator command only (no chain wiring). Rejected: the state engine is where drifted claims are MET; an unwired tool is advice, not behavior.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: the vendored workflow carries both passages at 2.7.0 with step 0 in the chain, suite-asserted every run. Baseline: trust extended to any status cell; deps are a scheduling hint. Deadline: final acceptance.</li><li>Guardrail: t01-t13 stay green (the additions must not disturb helper or doctrine gates).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the two §§, step 0 chain entry, version bump, pin moves, t14.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>The reconcile tool itself (TASK-IMP-100).</li><li>Retroactively reconciling the historical corpus (operator-initiated, per task, when it matters).</li><li>New lifecycle statuses (fork outcomes map onto STATUS-REFERENCE §1 as-is).</li></ul>\n<h2>Dependencies</h2>\n<ul><li>depends_on TASK-IMP-100 (chain step 0 names the skill; the SKILL dir must exist for chain-coverage gates) - same agent, serial order, per the batch plan.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the recorded operator decisions; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-5 PLAN approved 2026-07-17 via recorded HITL decision; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 ship-tasks.md MUST gain the reconcile entry §: trigger conditions (status past ready_to_implement AND (no manifest OR manifest verify fails OR required phase artefacts missing)), the tool invocation, the three-way HITL fork, the no-silent-execution rule, and route_back mechanics per STATUS-REFERENCE §1.3.</li><li>1.2 ship-tasks.md MUST gain the depends_on evidence gate §: done upstreams require a coverage-gate artefact (either artefact home) or a human-accepted reconcile-report; otherwise the task is blocked, and override is a recorded human act (memory.status_overridden).</li><li>1.3 skill_chain MUST gain conditional step 0 (skill: task-reconcile) with reconcile_report added to outputs.</li><li>1.4 workflow_version MUST bump to 2.7.0; the t12 and t09_doctrine_wiring exact pins MUST move with it (disclosed).</li><li>1.5 The suite MUST gate both passages, step 0, and the version in source AND scratch payload (new t14 in test_workflow_helpers.sh), with t01-t13 green.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2, #1.3, #1.5) - both passages + step 0 present in source and payload at 2.7.0 - test: <code>tools/install/tests/test_workflow_helpers.sh::t14_reconcile_entry_and_deps_gate</code></li><li>[ ] AC 2 (traces_to: #1.4) - version pins moved, suite green - test: <code>tools/install/tests/test_workflow_helpers.sh::t12_doctrine_view_rules_vendored (pin at 2.7.0)</code></li><li>[ ] AC 3 (traces_to: #1.5) - t01-t13 undisturbed - test: <code>tools/install/tests/test_workflow_helpers.sh::t01_manifest_lifecycle (representative; suite runs as one)</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Task with a VALID manifest whose frontmatter status looks past the manifest's step: existing resume semantics own it (manifest outranks the cell); reconcile entry only fires when resume's own preconditions fail - stated in the § to prevent double-handling.</li><li>depends_on naming an off-ramped task (closed/duplicate): not \"done\" - the gate treats it as unmet dependency and surfaces it (existing eligibility semantics; restated).</li><li>Historical done tasks with artefacts in docs/tasks/.workflow/&lt;id&gt;/ (the corpus norm before per-folder artefacts): the coverage-gate check accepts both homes (1.2), so the gate does not false-block the existing corpus.</li><li>Security-class: none - doctrine prose + test pins; the tool it names is read-only by 100's contract.</li></ul>";
