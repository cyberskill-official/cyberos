window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The rate-card layer <strong>MUST</strong> persist per-engagement billing rates with effective-date versioning. The contract:</p>\n<ol><li><strong>MUST</strong> define <code>rate_cards</code> table with columns: <code>id UUID PK</code>, <code>engagement_id UUID FK</code>, <code>role</code> (enum), <code>currency</code> (enum), <code>hourly_rate_minor</code> (BIGINT — amount in minor units: VND has 0 decimals = whole VND; USD has 2 = cents), <code>billable_default BOOLEAN</code>, <code>effective_from DATE</code>, <code>effective_to DATE</code> (nullable; null = current), <code>created_at TIMESTAMPTZ</code>, <code>created_by_subject_id UUID</code>, <code>tenant_id UUID</code>.</li><li><strong>MUST</strong> support 6 canonical roles: <code>Engineer</code>, <code>Designer</code>, <code>Pm</code>, <code>Qa</code>, <code>Analyst</code>, <code>Exec</code>. v1 frozen; new roles via v2 migration.</li><li><strong>MUST</strong> support 5 currencies: <code>VND</code> (0 decimals), <code>USD</code> (2), <code>SGD</code> (2), <code>EUR</code> (2), <code>JPY</code> (0).</li><li><strong>MUST</strong> treat rate cards as APPEND-ONLY. <code>UPDATE rate_cards SET hourly_rate_minor = ...</code> is forbidden by RLS+role policy. Supersession = INSERT new row with <code>effective_from = today + 1</code> AND <code>UPDATE prior.effective_to = today</code>.</li><li><strong>MUST</strong> enforce uniqueness: only ONE active rate card per <code>(engagement_id, role, currency)</code> tuple AT ANY GIVEN TIME. Overlapping intervals → 409 CONFLICT.</li><li><strong>MUST</strong> expose <code>lookup_at(engagement_id, role, currency, at_date)</code> returning the rate card row whose <code>effective_from &lt;= at_date AND (effective_to IS NULL OR effective_to &gt; at_date)</code>. None → 404.</li><li><strong>MUST</strong> expose REST endpoints:</li></ol>\n<ul><li><code>POST /api/proj/engagements/:eng/rate-cards</code> — create or supersede. Idempotent on <code>Idempotency-Key</code>.</li><li><code>GET /api/proj/engagements/:eng/rate-cards?at=YYYY-MM-DD&amp;role=engineer&amp;currency=VND</code> — lookup.</li><li><code>GET /api/proj/engagements/:eng/rate-cards/history</code> — full versioned history.</li><li><code>PATCH /api/proj/engagements/:eng/rate-cards/:id</code> — ONLY <code>billable_default</code> mutable (rate fields immutable per #4); other PATCH fields rejected.</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit rows:</li></ol>\n<ul><li><code>proj.rate_card_created</code> on new row.</li><li><code>proj.rate_card_superseded</code> on supersession with payload <code>{old_card_id, new_card_id, old_rate_minor, new_rate_minor, currency, role, effective_from}</code>.</li><li><code>proj.rate_card_billable_default_changed</code> on PATCH.</li></ul>\n<ol><li><strong>MUST</strong> enforce RLS per TASK-AUTH-003 — operators can only see/write rate cards for their tenant's engagements.</li><li><strong>MUST</strong> validate amounts non-negative (<code>hourly_rate_minor &gt;= 0</code>); zero allowed (pro bono work).</li><li><strong>MUST</strong> emit OTel metric <code>proj_rate_cards_active{currency, role}</code> (gauge — count of currently-active cards per cell).</li><li><strong>SHOULD</strong> support <code>cyberos rate-cards export &lt;engagement_id&gt;</code> CSV dump for finance reconciliation.</li><li><strong>MUST</strong> support <code>POST /api/proj/engagements/:eng/rate-cards/preview</code> — dry-run that returns what would happen (new card + supersede chain) without persisting. Used by operators before committing rate changes.</li><li><strong>MUST</strong> support retroactive corrections via a separate <code>proj.rate_card_corrected</code> audit row kind. Corrections require admin role AND <code>correction_reason</code> text. The corrected row is a NEW row with <code>effective_from &lt; today</code>; the old row's <code>effective_to</code> is set to the new <code>effective_from</code>; the audit references both. Corrections are rare and audited heavily.</li><li><strong>MUST</strong> expose a \"default rate card pack\" at engagement creation: tenant-admin defines <code>cyberos_proj_tenant_settings.default_rate_card_pack</code> (JSONB with role × currency × rate). New engagements get these as their initial rate cards. Operators can override per-engagement.</li><li><strong>MUST</strong> support role aliasing for migrations: <code>cyberos_proj_role_aliases</code> table maps deprecated names (e.g. <code>developer</code> → <code>engineer</code>). Aliases resolve at the handler layer; storage always uses canonical roles.</li><li><strong>MUST</strong> validate that <code>effective_from &lt;= today + 365 days</code>: rate cards more than a year in the future suggest typo. Reject with 422 <code>effective_from_too_far</code>.</li><li><strong>MUST</strong> record <code>archived_at TIMESTAMPTZ</code> separate from supersession: an engagement-archived state (TASK-PROJ-001) sets all the engagement's active rate cards' <code>archived_at = NOW()</code>; they remain queryable for historical reference but are excluded from <code>proj_rate_cards_active</code> gauge.</li><li><strong>MUST</strong> support per-engagement rate-card lock: <code>cyberos_proj_engagement_settings.rate_cards_locked = true</code> rejects all create/supersede operations for that engagement (compliance reason: rate change requires legal review). Lock is operator-toggled with audit trail.</li><li><strong>MUST</strong> validate currency consistency with engagement: if engagement has <code>default_currency = VND</code>, rate cards in other currencies emit a SEV-3 warning <code>proj.rate_card_currency_mismatch</code> (informational; doesn't reject; operators may legitimately have multi-currency engagements).</li><li><strong>MUST</strong> support <code>?include_archived=true</code> query parameter on GET history endpoint; default behaviour excludes archived cards.</li><li><strong>MUST</strong> include <code>created_via_ip + created_via_user_agent</code> in audit payload (NOT in the rate_cards row itself — too sensitive for general reads) for forensic traceability of rate changes.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why versioning via supersession (§1 #4, DEC-260)?</strong> UPDATE of <code>hourly_rate_minor</code> retroactively re-prices old timesheets (a $100/h entry from January suddenly worth $150/h after a March raise). Append-only with <code>effective_from</code> means: timesheets reference the rate card that was active at the time of work — historically immutable. The auditor's question \"what was the rate on March 15?\" has one answer.</p>\n<p><strong>Why per-row currency (§1 #3, DEC-261)?</strong> Real engagements span currencies — a SG-based client pays the engineer in USD but the local PM in VND. Per-engagement currency would force collapse to one; per-row supports the actual case.</p>\n<p><strong>Why minor units in BIGINT (§1 #1)?</strong> Floats are unsafe for money (0.1 + 0.2 ≠ 0.3). VND/JPY have 0 decimals → minor = whole. USD/SGD/EUR have 2 → minor = cents. Storage as BIGINT covers up to 9.2 quintillion minor — far beyond any conceivable hourly rate.</p>\n<p><strong>Why 6 canonical roles (§1 #2)?</strong> Empirical from VN consultancy market: engineer + designer + PM + QA + analyst covers ~95% of billable roles; exec for senior advisory. More granular roles (senior-vs-junior engineer) belong in <code>Member.tier</code> (different task), not rate cards.</p>\n<p><strong>Why only <code>billable_default</code> mutable (§1 #7)?</strong> The rate is the contractual artifact — once a rate-card is referenced by a billable entry, changing it would be retroactive fraud. The default flag is a UX hint (\"when creating a new task in this engagement, default-bill it\") — changing it doesn't change historical entries.</p>\n<p><strong>Why audit supersede separately (§1 #8)?</strong> Auditors investigating \"did this client get billed correctly through the rate change\" follow the supersede chain. Generic <code>rate_card_created</code> audit rows lose the linkage; a dedicated kind with <code>old → new</code> payload makes the chain queryable.</p>\n<p><strong>Why preview endpoint (§1 #13)?</strong> Rate-card changes are high-stakes (affects billing for hundreds of timesheets). Preview lets operators verify the resulting supersede chain before committing. Drying-run prevents accidental retro overlaps.</p>\n<p><strong>Why retroactive correction support (§1 #14)?</strong> Real-world: rate was misconfigured at engagement start; discovered weeks later after billing. Need to correct the historical record AND audit the correction. A separate audit row kind makes this surgical.</p>\n<p><strong>Why default rate-card pack (§1 #15)?</strong> Tenants engaging multiple clients with similar rate structures (a consultancy with standard engineering/PM/QA tiers) shouldn't re-enter the same 6 rates per engagement. Pack at engagement creation = one-line setup.</p>\n<p><strong>Why role aliasing (§1 #16)?</strong> When migrating from another tool (or renaming roles internally), operators have legacy data referencing <code>developer</code> etc. Aliasing at the handler layer lets data migrate without database churn.</p>\n<p><strong>Why 365-day-future cap (§1 #17)?</strong> A rate card with <code>effective_from = 2030-01-01</code> is almost certainly a typo (operator meant 2026). The cap catches it before the rate ships.</p>\n<p><strong>Why archived_at separate from supersede (§1 #18)?</strong> Supersession means \"this rate was replaced\"; archival means \"this engagement is no longer active.\" They're different states with different operator implications. Archived cards should disappear from active counts but remain queryable.</p>\n<p><strong>Why rate-cards lock (§1 #19)?</strong> Enterprise contracts often require legal sign-off for rate changes; the lock enforces that workflow at the data layer.</p>\n<p><strong>Why currency-consistency warning (§1 #20)?</strong> Operator setting a USD rate on a VND-default engagement is probably intentional (cross-border contractor) but worth surfacing. SEV-3 informational doesn't reject; just visible in operator dashboards.</p>\n<p><strong>Why IP/UA in audit only (§1 #22)?</strong> Rate-card rows are read frequently (lookup_at on every billable entry); embedding IP/UA in the table bloats the hot path. Audit rows are read rarely (forensics); fine to be richer.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0005_rate_cards.sql\n\nCREATE TABLE rate_cards (\n    id                 UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    engagement_id      UUID NOT NULL REFERENCES engagements(id),\n    role               TEXT NOT NULL CHECK (role IN ('engineer','designer','pm','qa','analyst','exec')),\n    currency           TEXT NOT NULL CHECK (currency IN ('VND','USD','SGD','EUR','JPY')),\n    hourly_rate_minor  BIGINT NOT NULL CHECK (hourly_rate_minor &gt;= 0),\n    billable_default   BOOLEAN NOT NULL DEFAULT true,\n    effective_from     DATE NOT NULL,\n    effective_to       DATE,\n    created_at         TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    created_by_subject_id UUID NOT NULL,\n    tenant_id          UUID NOT NULL,\n    UNIQUE (engagement_id, role, currency, effective_from)\n);\n\n-- Partial-unique: at most one ACTIVE card per (engagement, role, currency)\nCREATE UNIQUE INDEX uniq_active_rate_card\n    ON rate_cards (engagement_id, role, currency)\n    WHERE effective_to IS NULL;\n\nCREATE INDEX idx_rate_cards_lookup ON rate_cards (engagement_id, role, currency, effective_from, effective_to);\n\nALTER TABLE rate_cards ENABLE ROW LEVEL SECURITY;\nCREATE POLICY rate_cards_tenant_isolation ON rate_cards\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Rust types + handlers</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/rate_card/mod.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq, Hash)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum Role { Engineer, Designer, Pm, Qa, Analyst, Exec }\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq, Hash)]\n#[sqlx(type_name = \"TEXT\")]\npub enum Currency { VND, USD, SGD, EUR, JPY }\n\nimpl Currency {\n    pub fn decimals(self) -&gt; u8 {\n        match self { Currency::VND | Currency::JPY =&gt; 0, _ =&gt; 2 }\n    }\n}\n\n#[derive(Clone, Debug, Serialize, sqlx::FromRow)]\npub struct RateCard {\n    pub id:                    uuid::Uuid,\n    pub engagement_id:         uuid::Uuid,\n    pub role:                  Role,\n    pub currency:              Currency,\n    pub hourly_rate_minor:     i64,\n    pub billable_default:      bool,\n    pub effective_from:        chrono::NaiveDate,\n    pub effective_to:          Option&lt;chrono::NaiveDate&gt;,\n    pub created_at:            chrono::DateTime&lt;chrono::Utc&gt;,\n    pub created_by_subject_id: uuid::Uuid,\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum RateCardError {\n    #[error(\"overlap with existing active card\")] OverlapConflict,\n    #[error(\"rate fields immutable; only billable_default can be patched\")] ImmutableField,\n    #[error(\"not found\")] NotFound,\n    #[error(\"negative amount: {0}\")] NegativeAmount(i64),\n    #[error(\"db: {0}\")] Db(String),\n}</code></pre>\n<pre><code class=\"language-rust\">// services/proj-sync/src/rate_card/handlers.rs\npub async fn create_or_supersede(\n    pool: &amp;sqlx::PgPool,\n    engagement_id: uuid::Uuid,\n    req: CreateReq,\n    subject: uuid::Uuid,\n) -&gt; Result&lt;RateCard, RateCardError&gt; {\n    if req.hourly_rate_minor &lt; 0 { return Err(RateCardError::NegativeAmount(req.hourly_rate_minor)); }\n    let mut tx = pool.begin().await.map_err(|e| RateCardError::Db(e.to_string()))?;\n\n    // Close prior active card (effective_to = effective_from - 1 day)\n    let prior: Option&lt;RateCard&gt; = sqlx::query_as(\n        \"SELECT * FROM rate_cards\n         WHERE engagement_id = $1 AND role = $2 AND currency = $3 AND effective_to IS NULL\n         FOR UPDATE\"\n    ).bind(engagement_id).bind(req.role).bind(req.currency).fetch_optional(&amp;mut *tx).await\n        .map_err(|e| RateCardError::Db(e.to_string()))?;\n\n    if let Some(p) = &amp;prior {\n        if p.effective_from &gt;= req.effective_from { return Err(RateCardError::OverlapConflict); }\n        sqlx::query(\"UPDATE rate_cards SET effective_to = $1 WHERE id = $2\")\n            .bind(req.effective_from).bind(p.id)\n            .execute(&amp;mut *tx).await.map_err(|e| RateCardError::Db(e.to_string()))?;\n    }\n\n    // Insert new card\n    let new_card: RateCard = sqlx::query_as(\n        \"INSERT INTO rate_cards (engagement_id, role, currency, hourly_rate_minor, billable_default,\n                                  effective_from, created_by_subject_id, tenant_id)\n         VALUES ($1,$2,$3,$4,$5,$6,$7, current_setting('app.tenant_id')::uuid)\n         RETURNING *\"\n    ).bind(engagement_id).bind(req.role).bind(req.currency).bind(req.hourly_rate_minor)\n     .bind(req.billable_default).bind(req.effective_from).bind(subject)\n     .fetch_one(&amp;mut *tx).await.map_err(|e| RateCardError::Db(e.to_string()))?;\n\n    tx.commit().await.map_err(|e| RateCardError::Db(e.to_string()))?;\n\n    if let Some(p) = prior {\n        emit_memory_row(\"proj.rate_card_superseded\", serde_json::json!({\n            \"old_card_id\": p.id, \"new_card_id\": new_card.id,\n            \"old_rate_minor\": p.hourly_rate_minor, \"new_rate_minor\": new_card.hourly_rate_minor,\n            \"currency\": req.currency, \"role\": req.role,\n            \"effective_from\": req.effective_from,\n        })).await;\n    } else {\n        emit_memory_row(\"proj.rate_card_created\", serde_json::json!({\n            \"card_id\": new_card.id, \"engagement_id\": engagement_id,\n            \"role\": req.role, \"currency\": req.currency,\n            \"hourly_rate_minor\": new_card.hourly_rate_minor,\n        })).await;\n    }\n    Ok(new_card)\n}\n\npub async fn lookup_at(\n    pool: &amp;sqlx::PgPool,\n    engagement_id: uuid::Uuid,\n    role: Role,\n    currency: Currency,\n    at: chrono::NaiveDate,\n) -&gt; Result&lt;RateCard, RateCardError&gt; {\n    sqlx::query_as(\n        \"SELECT * FROM rate_cards\n         WHERE engagement_id = $1 AND role = $2 AND currency = $3\n           AND effective_from &lt;= $4 AND (effective_to IS NULL OR effective_to &gt; $4)\n         LIMIT 1\"\n    ).bind(engagement_id).bind(role).bind(currency).bind(at)\n    .fetch_optional(pool).await\n    .map_err(|e| RateCardError::Db(e.to_string()))?\n    .ok_or(RateCardError::NotFound)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Create rate card</strong> — POST → 201; row in <code>rate_cards</code>; <code>effective_to</code> is null.</li><li><strong>Supersede on second create</strong> — second POST same role/currency with later <code>effective_from</code> → prior row's <code>effective_to</code> set; new row inserted; only one active.</li><li><strong>Overlap rejected</strong> — second POST with <code>effective_from &lt;= prior.effective_from</code> → 409.</li><li><strong>lookup_at picks active card</strong> — three cards across time; lookup at midpoint date returns the right one.</li><li><strong>lookup_at returns 404 outside range</strong> — no active card on date → 404.</li><li><strong>Negative amount rejected</strong> — <code>hourly_rate_minor: -1</code> → 422.</li><li><strong>PATCH rate fields rejected</strong> — PATCH <code>hourly_rate_minor</code> → 422 <code>immutable_field</code>.</li><li><strong>PATCH billable_default succeeds</strong> — PATCH <code>billable_default: false</code> → 200; original other fields preserved.</li><li><strong>History endpoint returns all versions</strong> — 3 supersessions → 3 rows ordered by <code>effective_from</code> ASC.</li><li><strong>memory audit <code>rate_card_created</code></strong> — first create → row.</li><li><strong>memory audit <code>rate_card_superseded</code></strong> — supersede → row with old + new card IDs + delta.</li><li><strong>memory audit <code>rate_card_billable_default_changed</code></strong> — PATCH default → row.</li><li><strong>RLS isolates tenants</strong> — tenant A cannot read tenant B's cards.</li><li><strong>Multi-currency on same engagement</strong> — eng X with VND + USD cards simultaneously → both active.</li><li><strong>Idempotency-Key honoured</strong> — same key + same body → returns prior outcome; same key + diff body → 409.</li><li><strong>OTel gauge <code>proj_rate_cards_active</code></strong> — 3 cards across 3 cells → gauge values match.</li><li><strong>CSV export</strong> — <code>cyberos rate-cards export &lt;eng&gt;</code> → ordered CSV with all fields.</li><li><strong>Preview returns supersede chain without persist</strong> — POST /preview → returns new+prior with effective_to update simulated; no DB change (AC for §1 #13).</li><li><strong>Retroactive correction emits dedicated audit</strong> — admin POST with <code>correction_reason</code> + past <code>effective_from</code> → <code>proj.rate_card_corrected</code> row with both old + new IDs + reason (AC for §1 #14).</li><li><strong>Retroactive correction by non-admin rejected</strong> — non-admin POST with past effective_from → 403 (AC for §1 #14).</li><li><strong>Default rate-card pack applied to new engagement</strong> — engagement creation with <code>tenant.default_rate_card_pack</code> set → engagement has those cards (AC for §1 #15).</li><li><strong>Role alias resolves to canonical</strong> — POST with <code>role=developer</code> (alias for engineer) → stored as <code>engineer</code>; audit + GET return canonical (AC for §1 #16).</li><li><strong>effective_from &gt; today+365 rejected</strong> — POST with <code>effective_from=2030-01-01</code> → 422 <code>effective_from_too_far</code> (AC for §1 #17).</li><li><strong>Engagement archive sets archived_at on cards</strong> — archive engagement → all active cards' archived_at populated (AC for §1 #18).</li><li><strong>Archived cards excluded from active gauge</strong> — gauge value drops after engagement archive (AC for §1 #18).</li><li><strong>Locked engagement rejects supersede</strong> — set <code>rate_cards_locked=true</code>; POST → 409 <code>rate_cards_locked</code> (AC for §1 #19).</li><li><strong>Currency mismatch emits warning</strong> — POST USD card on VND-default engagement → SEV-3 audit <code>proj.rate_card_currency_mismatch</code>; card still created (AC for §1 #20).</li><li><strong>History include_archived flag</strong> — GET ?include_archived=true returns archived cards; default excludes them (AC for §1 #21).</li><li><strong>Audit captures IP + UA</strong> — POST sets <code>created_via_ip</code> + <code>created_via_user_agent</code> in audit payload; absent in rate_cards row (AC for §1 #22).</li><li><strong>VND/JPY 0-decimal storage</strong> — POST with <code>hourly_rate_minor=500000</code> for VND stores as 500000 (== 500,000 VND); USD 5000 = $50 (AC for §1 #1).</li><li><strong>Idempotency same key same body returns same outcome</strong> — POST with same <code>Idempotency-Key</code> + identical body → same response (AC for §1 #15).</li><li><strong>Idempotency same key different body returns 409</strong> — same key + modified body → 409 <code>idempotency_body_mismatch</code> (AC for §1 #15).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/proj/tests/status_fsm_test.rs\n\n#[tokio::test]\nasync fn create_and_supersede() {\n    let env = TestEnv::new().await;\n    let eng = env.create_engagement().await;\n    let card1 = env.create_rate_card(eng, Role::Engineer, Currency::VND, 500_000, \"2026-01-01\").await.unwrap();\n    let card2 = env.create_rate_card(eng, Role::Engineer, Currency::VND, 600_000, \"2026-04-01\").await.unwrap();\n\n    let card1_refreshed: RateCard = sqlx::query_as(\"SELECT * FROM rate_cards WHERE id = $1\")\n        .bind(card1.id).fetch_one(&amp;env.pool).await.unwrap();\n    assert_eq!(card1_refreshed.effective_to, Some(chrono::NaiveDate::from_ymd_opt(2026, 4, 1).unwrap()));\n    assert!(card2.effective_to.is_none());\n\n    let supersede_row = env.memory.latest(\"proj.rate_card_superseded\").await;\n    assert_eq!(supersede_row[\"payload\"][\"old_rate_minor\"], 500_000);\n    assert_eq!(supersede_row[\"payload\"][\"new_rate_minor\"], 600_000);\n}\n\n#[tokio::test]\nasync fn lookup_at_picks_active() {\n    let env = TestEnv::new().await;\n    let eng = env.create_engagement().await;\n    env.create_rate_card(eng, Role::Engineer, Currency::VND, 500_000, \"2026-01-01\").await.unwrap();\n    env.create_rate_card(eng, Role::Engineer, Currency::VND, 600_000, \"2026-04-01\").await.unwrap();\n\n    let feb = lookup_at(&amp;env.pool, eng, Role::Engineer, Currency::VND, \"2026-02-15\".parse().unwrap()).await.unwrap();\n    assert_eq!(feb.hourly_rate_minor, 500_000);\n    let may = lookup_at(&amp;env.pool, eng, Role::Engineer, Currency::VND, \"2026-05-15\".parse().unwrap()).await.unwrap();\n    assert_eq!(may.hourly_rate_minor, 600_000);\n}\n\n#[tokio::test]\nasync fn rate_field_immutable() {\n    let env = TestEnv::new().await;\n    let card = env.create_rate_card_default().await.unwrap();\n    let err = patch_rate_card(&amp;env.pool, card.id, json!({\"hourly_rate_minor\": 999})).await.unwrap_err();\n    assert!(matches!(err, RateCardError::ImmutableField));\n}\n\n#[tokio::test]\nasync fn multi_currency_same_engagement() {\n    let env = TestEnv::new().await;\n    let eng = env.create_engagement().await;\n    let vnd = env.create_rate_card(eng, Role::Engineer, Currency::VND, 500_000, \"2026-01-01\").await.unwrap();\n    let usd = env.create_rate_card(eng, Role::Engineer, Currency::USD, 5000, \"2026-01-01\").await.unwrap();\n    assert!(vnd.effective_to.is_none() &amp;&amp; usd.effective_to.is_none());\n}\n\n#[tokio::test]\nasync fn rls_isolates_tenants() {\n    let env_a = TestEnv::for_tenant(\"A\").await;\n    let env_b = TestEnv::for_tenant(\"B\").await;\n    let eng_a = env_a.create_engagement().await;\n    env_a.create_rate_card(eng_a, Role::Engineer, Currency::VND, 500_000, \"2026-01-01\").await.unwrap();\n\n    let cross: Result&lt;RateCard, _&gt; = sqlx::query_as(\"SELECT * FROM rate_cards WHERE engagement_id = $1\")\n        .bind(eng_a).fetch_one(&amp;env_b.pool).await;\n    assert!(cross.is_err());\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-001</strong> — <code>engagements</code> table FK target.</li><li><strong>TASK-PROJ-006 (downstream)</strong> — billable cascade uses <code>lookup_at</code> to resolve rates.</li><li><strong>TASK-PROJ-007 (downstream)</strong> — billing modes consume rate cards.</li><li><strong>TASK-AUTH-003</strong> — RLS on <code>rate_cards</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.rate_card_superseded\",\n  \"payload\": {\n    \"old_card_id\":    \"rc-...\",\n    \"new_card_id\":    \"rc-...\",\n    \"old_rate_minor\": 500000,\n    \"new_rate_minor\": 600000,\n    \"currency\":       \"VND\",\n    \"role\":           \"engineer\",\n    \"effective_from\": \"2026-04-01\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Per-member rate overrides (Alice's senior engineer rate ≠ junior default) — slice 3+; lives in TASK-PROJ-006 cascade.</li><li>Tiered rates (first 40h/week at X, overtime at Y) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Concurrent supersede same cell</td><td><code>FOR UPDATE</code> lock</td><td>Second caller sees prior's updated effective_to</td><td>Retries succeed</td></tr><tr><td>Overlap detected</td><td>effective_from check</td><td>409</td><td>Caller adjusts date</td></tr><tr><td>Negative amount</td><td>CHECK constraint</td><td>422</td><td>Caller fixes</td></tr><tr><td>PATCH non-billable_default field</td><td>handler whitelist</td><td>422</td><td>Caller uses correct field</td></tr><tr><td>Engagement deleted (cascade?)</td><td>FK ON DELETE RESTRICT</td><td>Cannot delete eng with cards</td><td>Operator archives engagement</td></tr><tr><td>Currency drift (insert \"USDT\")</td><td>CHECK constraint</td><td>422</td><td>Caller uses valid enum</td></tr><tr><td>Role drift</td><td>CHECK constraint</td><td>422</td><td>Caller uses valid enum</td></tr><tr><td>Effective_from in past with active card</td><td>overlap check</td><td>409 if overlap</td><td>Caller adjusts</td></tr><tr><td>Active card without effective_to (orphan)</td><td>partial unique enforces ≤1</td><td>None</td><td>None</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows / 404</td><td>None</td></tr><tr><td>audit emit fails</td><td>MemoryWriter Err</td><td>Card created; audit lost; sev-2</td><td>Operator restores memory</td></tr><tr><td>Lookup date is null</td><td>type system rejects</td><td>422</td><td>Caller provides date</td></tr><tr><td>BIGINT overflow</td><td>i64 ≈ 9.2 quintillion minor</td><td>unrealistic</td><td>None</td></tr><tr><td>ENG belongs to different tenant</td><td>RLS catches</td><td>404</td><td>Caller uses correct tenant context</td></tr><tr><td>Preview includes side effect (bug)</td><td>invariant test ensures no DB change</td><td>None</td><td>Author fixes</td></tr><tr><td>Retroactive correction without correction_reason</td><td>handler check</td><td>400 <code>correction_reason_required</code></td><td>Caller adds reason</td></tr><tr><td>Retroactive correction by non-admin</td><td>RBAC</td><td>403</td><td>Operator escalates</td></tr><tr><td>Default rate-card pack with invalid role</td><td>validate at engagement creation</td><td>422</td><td>Operator fixes tenant settings</td></tr><tr><td>Default pack updated mid-creation</td><td>engagement uses snapshot at start</td><td>None</td><td>None</td></tr><tr><td>Role alias maps to retired canonical</td><td>alias config flagged</td><td>422</td><td>Operator updates aliases</td></tr><tr><td>Role alias cycles (a → b → a)</td><td>resolver detects</td><td>500 + SEV-1</td><td>Operator</td></tr><tr><td>effective_from = today's date with active card</td><td>overlap check</td><td>409 if overlap; OK if no prior</td><td>Caller</td></tr><tr><td>effective_from &gt; today + 365d</td><td>range check</td><td>422</td><td>Caller</td></tr><tr><td>Engagement archived while rate change in flight</td><td>tx ordering: archive blocks supersede</td><td>409</td><td>Operator</td></tr><tr><td>Rate-card lock toggled during in-flight POST</td><td>lock check at top of handler</td><td>mid-flight may succeed; next rejects</td><td>None</td></tr><tr><td>Currency mismatch SEV-3 firing too often</td><td>dedup at OBS layer</td><td>None</td><td>Operator tunes</td></tr><tr><td>include_archived with large engagement</td><td>bounded by tenant limit (10k cards)</td><td>pagination</td><td>None</td></tr><tr><td>Audit IP captured incorrectly (proxy IP, not client)</td><td>use X-Forwarded-For if trusted</td><td>None</td><td>Operator validates trust chain</td></tr><tr><td>Audit UA truncated at 1KB</td><td>bounded; tail discarded</td><td>None</td><td>None</td></tr><tr><td>Concurrent supersede + correction</td><td>tx serialises via FOR UPDATE</td><td>one wins</td><td>None</td></tr><tr><td>Correction with effective_from after correction date</td><td>invalid: corrections are retroactive</td><td>400</td><td>Caller</td></tr><tr><td>Correction overlaps with already-superseded chain</td><td>resolver fans through chain</td><td>409 if overlap</td><td>Caller</td></tr><tr><td>Default pack with all currencies for one role</td><td>engagement gets N cards (one per currency)</td><td>as designed</td><td>None</td></tr><tr><td>Tenant deletes default pack mid-creation</td><td>engagement defaults to no pack</td><td>None</td><td>None</td></tr><tr><td>Concurrent engagement creation with same default pack</td><td>each engagement independent</td><td>None</td><td>None</td></tr><tr><td>OTel gauge update fails</td><td>counter not updated; logged</td><td>None</td><td>None</td></tr><tr><td>Rate-card row &gt; 10KB (huge metadata in audit)</td><td>metadata not in row; only audit</td><td>None</td><td>None</td></tr><tr><td>CSV export contains comma in display fields</td><td>RFC 4180 quoting</td><td>None</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><code>hourly_rate_minor</code> is BIGINT (signed i64) — supports up to ~9.2 quintillion minor units. JPY at 0 decimals + i64 covers any conceivable rate.</li><li>The partial-unique index on <code>(engagement_id, role, currency) WHERE effective_to IS NULL</code> enforces \"≤ 1 active card per cell\" without preventing historical rows.</li><li><code>effective_to = effective_from + 1 day</code> would be cleaner (closed-open interval) but Postgres DATE arithmetic makes the half-open <code>effective_to &gt; $at</code> query natural; we use closed-open.</li><li>The <code>Currency::decimals()</code> helper is used by TASK-PROJ-007 + task-INV when rendering amounts; minor unit display formula = <code>value / 10^decimals</code>.</li><li>Idempotency uses the same <code>admin_idempotency_keys</code> table pattern as TASK-AUTH-001.</li><li>Concurrency: <code>FOR UPDATE</code> lock on prior card row prevents two callers from both reading \"no prior\" and both inserting.</li><li>Audit row currency stored as enum string (matches CHECK constraint).</li><li>CSV export is the operator escape hatch; finance teams sometimes prefer Excel pivot over SQL.</li><li>The preview endpoint is implemented as the same handler with a <code>commit=false</code> branch; this guarantees preview output exactly matches what commit would produce. No drift between paths.</li><li>Retroactive correction is intentionally heavier (admin-only, requires reason, separate audit kind) because retroactive billing changes touch invoiced periods. The friction is the feature.</li><li>Default rate-card pack at engagement creation deliberately copies into per-engagement rows (not references). Tenant changes to the pack don't affect already-created engagements.</li><li>Role aliases are runtime-resolved (not DB-rewritten) because DB rewrite would break audit trails. Audit shows the canonical role; UI shows the alias if configured.</li><li>The 365-day-future cap is a sanity check, not a contract enforcement; if a tenant genuinely needs further-out rates, an operator unlocks via tenant settings.</li><li><code>archived_at</code> vs supersession: supersede means \"replaced by a newer rate\"; archive means \"engagement is done.\" A card can be both superseded AND archived. Both states are preserved.</li><li>The rate-cards-locked flag is a per-engagement compliance hold; we considered a per-card lock but operators wanted engagement-level (atomic).</li><li>Currency-mismatch is SEV-3 (informational) because: (a) multi-currency engagements are legitimate; (b) operators reading dashboards see it and decide; (c) hard-reject would block valid use cases.</li><li>IP/UA in audit only (not in row) keeps the hot read path lean; only forensic queries need these fields.</li><li>We considered storing rates as <code>pg_numeric(20,8)</code> instead of BIGINT minor units; BIGINT is faster + matches the canonical pattern in TASK-AUTH-001 + TASK-PROJ-001.</li><li><code>FOR UPDATE</code> lock is on the prior active row, not all rows. Lock scope is bounded.</li><li>We don't support rate_card deletion — even archive doesn't delete; data is forever-queryable for audit reconstruction.</li><li>Engagement deletion (TASK-PROJ-001) is <code>ON DELETE RESTRICT</code> for rate cards: operators must explicitly archive cards before engagement deletion.</li><li>Per-engagement rate-card export (CSV) is the only export path; cross-engagement export deferred (slice 4+).</li><li>The <code>proj_rate_cards_active</code> gauge updates on every create/supersede/archive; computation = <code>SELECT COUNT(*) FROM rate_cards WHERE effective_to IS NULL AND archived_at IS NULL</code>.</li><li>We chose 6 canonical roles based on Vietnamese consultancy market survey; tenants outside that set can use the role-aliases mechanism rather than custom roles.</li><li>The <code>effective_from</code> is DATE (not TIMESTAMPTZ); rate changes apply at midnight tenant-local time. Sub-day precision adds complexity without operational value.</li><li>For correction workflows, the previously-active card's <code>effective_to</code> is set to the new card's <code>effective_from</code>. This means a correction inserted at <code>effective_from = 2026-03-15</code> closes the prior at <code>2026-03-14</code>; new card opens <code>2026-03-15</code>. Half-open interval semantics.</li><li>Default rate-card pack JSONB schema is open (per-tenant extension); tooling validates structure at engagement creation.</li><li>We chose to keep the role enum closed (no operator-defined roles) to prevent rate-card sprawl; the alias mechanism handles compat naming.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-005.</em></p>";
