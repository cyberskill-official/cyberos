window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-011"]="<h1>TASK-IMP-011: Structured gate-failure taxonomy</h1>\n<h2>Summary</h2>\n<p>When the machine-gate floor goes RED, operators and automation today only see human prose (<code>FAIL test</code>, <code>GATES: RED</code>). This task makes every gate failure emit a structured class and a durable JSON summary so regressions can be mined without scraping logs.</p>\n<h2>Problem</h2>\n<p><code>run-gates.sh</code> prints PASS/FAIL/SKIP lines but does not classify failures. Downstream tooling (auto-revert, dashboards, triage) cannot tell a lint miss from a coverage regression or an empty floor without brittle string matching.</p>\n<h2>Proposed Solution</h2>\n<p>Extend <code>tools/install/gates/run-gates.sh</code> so each configured gate step maps to a fixed taxonomy class. On any RED outcome, write <code>.cyberos/last-gate-failure.json</code> (schema <code>gate-failure@1</code>) listing every failed gate with class, command, and source, and emit one <code>GATE_FAILURE_JSON:{...}</code> line on stdout for log miners. Green runs MUST remove a stale failure file so \"last failure\" always means the most recent RED.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Emit only stderr prose codes.</strong> Rejected: not machine-parseable across shells/CI wrappers.</li><li><strong>Full OpenTelemetry span per gate.</strong> Rejected: out of 1.x payload scope; JSON summary is enough to mine.</li><li><strong>Per-ecosystem subclass taxonomy (cargo/clippy/etc.).</strong> Rejected: 1.x floor is build|lint|test|coverage|doctor|caf|awh; subclasses belong in later waves.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a forced failing gate writes <code>gate-failure@1</code> JSON with the matching class; suite asserts taxonomy present.</li><li>Guardrail: green runs stay exit 0 and clear any prior failure file; exit codes 1/2/3 unchanged.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>run-gates.sh</code> taxonomy + failure artifact; install suite tests; CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Platform cargo llvm-cov / caf finding taxonomies.</li><li>Changing HITL verdict mechanics.</li><li>Shipping a dashboard consumer (miners are out of band).</li></ul>\n<h2>Dependencies</h2>\n<p>None. Soft: TASK-IMP-026 may read the failure artifact; not required to ship 011.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent authoring against live <code>run-gates.sh</code> in batch/12c.</li><li><strong>Scope:</strong> 1.x payload gate steps only; classes adjusted to real floor.</li><li><strong>Human review:</strong> session operator (Stephen Cheng) via batch/12c HITL continuum.</li></ul>\n<h2>§1 - Description (normative)</h2>\n<ol><li>Every gate step <code>run-gates.sh</code> executes (build, lint, test, coverage, caf, awh, doctor) MUST map to exactly one failure class from the closed set: <code>build | lint | test | coverage | doctor | caf | awh | empty-floor | other</code>.</li><li>When any configured gate command exits non-zero, the runner MUST record that failure (class, gate name, command, provenance source) and continue remaining gates (today's behavior), then exit 1.</li><li>On exit 1 (one or more gate failures) OR exit 3 (empty floor), the runner MUST write <code>$root/.cyberos/last-gate-failure.json</code> with schema <code>gate-failure@1</code> containing at least: <code>schema</code>, <code>exit_code</code>, <code>failures</code> (array of <code>{class,gate,cmd,source}</code>), and for empty-floor a single failure with <code>class: empty-floor</code>.</li><li>On those RED exits the runner MUST also print exactly one stdout line matching <code>GATE_FAILURE_JSON:</code> followed by the same JSON object (compact, single line).</li><li>On exit 0 (GREEN or EMPTY-ACKNOWLEDGED) the runner MUST delete <code>.cyberos/last-gate-failure.json</code> if present.</li><li>Exit codes MUST remain: 0 green/ack, 1 gate failed, 2 missing/malformed config, 3 empty floor.</li><li>The taxonomy MUST be covered by <code>tools/install/tests/test_gate_failure_taxonomy.sh</code> with scenarios that force a failure and assert class + artifact presence.</li></ol>\n<h2>Acceptance criteria</h2>\n<ul><li>[x] AC1: Forced <code>gates.test: \"false\"</code> yields exit 1, <code>last-gate-failure.json</code> with <code>class: test</code>, and a <code>GATE_FAILURE_JSON:</code> stdout line. (t01)</li><li>[x] AC2: Empty floor yields exit 3 and failure class <code>empty-floor</code>. (t02)</li><li>[x] AC3: Green run deletes a pre-existing <code>last-gate-failure.json</code>. (t03)</li><li>[x] AC4: Unknown/custom gate label (if any) maps to <code>other</code> without crashing. (t04)</li><li>[x] AC5: Payload build still vendors <code>cuo/gates/run-gates.sh</code> from <code>tools/install/gates/run-gates.sh</code>. (t05)</li></ul>\n<h2>Test plan</h2>\n<p><code>bash tools/install/tests/test_gate_failure_taxonomy.sh</code> via <code>scripts/tests/run_all.sh</code> glob.</p>";
