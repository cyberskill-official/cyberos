window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-118"]="<h1>TASK-SKILL-118: Thin-pair contract parity</h1>\n<h2>§1 - Description</h2>\n<p>Six pairs at the heart of /ship-tasks run on a SKILL.md and trigger tests alone: no rubric an auditor can score against, no pipeline, no envelopes, no invariants. The audits therefore re-derive their bar from prose every run. This task raises all six to the file-level contract the four full pairs already have.</p>\n<p>Normative clauses:</p>\n<ol><li>Each of the six author skills (<code>repo-context-map</code>, <code>edge-case-matrix</code>, <code>mock-contract-test</code>, <code>observability-injection</code>, <code>backlog-state-update</code>, <code>coverage-gate</code>) MUST gain: <code>PIPELINE.md</code> (phased steps incl. HALT points), <code>INVARIANTS.md</code>, <code>envelopes/input.json</code> + <code>envelopes/output.json</code>, <code>references/FAILURE_MODES.md</code>, and <code>acceptance/README.md</code> - matching the file classes of <code>task-author</code>.</li><li>Each of the six audit skills MUST gain: <code>RUBRIC.md</code> (versioned <code>&lt;name&gt;_rubric@1.0</code>, rule families with stable rule IDs, /10 scoring, 10/10 pass bar), <code>AUDIT_LOOP.md</code>, <code>REPORT_FORMAT.md</code>, <code>envelopes/input.json</code> + <code>envelopes/output.json</code>, and <code>acceptance/README.md</code> - matching the file classes of <code>task-audit</code>.</li><li>The rubrics MUST encode exactly the gates already normative in each pair's SKILL.md prose, with rule IDs: edge-case-matrix (&gt;= 1 row per category; SECURITY rows point at real test paths; DEGRADATION rows carry detection + recovery; total_rows &gt;= 8 for MUST tasks), observability-injection (&gt;= 1 log point per state transition, &gt;= 1 span per external IO, &gt;= 1 counter per error branch, branch_coverage &gt;= 80%, redaction policy when PII in scope), coverage-gate (tests_failed == 0; files_below_90pct empty; ecm_rows_uncovered empty; raw terminal present + non-truncated; §1-clause test closure), backlog-state-update (status in the 10-value enum; line_number resolves; old_line byte-match; evidence rows resolve; mutation_kind == status-cell-only), mock-contract-test (&gt;= 1 request/response pair; error_modes cover every SECURITY/DEGRADATION matrix row; swap_target is a real symbol; sunset criterion observable; contract tests pass against the mock), repo-context-map (three baseline patterns present; pinned_in references resolve; schemas present when task declares migrations; module-placement warning null or escalated).</li><li>Numeric gates MUST be expressed as named constants at the top of each RUBRIC.md, with the coverage threshold noted as overridable by <code>.cyberos/config.yaml</code> <code>coverage_threshold</code> once TASK-CUO-207 lands (default 90 preserved).</li><li>Artefact schemas MUST stay at @1 - additive documentation only; no change to any emitted artefact shape, so already-shipped tasks' artefacts remain valid.</li><li>A script <code>tools/install/check-pair-parity.sh &lt;skills-dir&gt;</code> MUST verify, for every author/audit pair present: authors carry the #1 file classes, audits carry the #2 file classes; missing files exit 10 as <code>PARITY &lt;skill&gt;: missing &lt;file&gt;</code>. <code>build.sh</code> MUST run it over the vendored set after the chain-coverage check (TASK-SKILL-116 ordering).</li><li>Each rewritten SKILL.md MUST keep its existing trigger description contract (TASK-SKILL-111/112/113 conventions) - descriptions, trigger tests, and frontmatter shape unchanged except for pointers to the new files.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Parity is defined by pointing at the four existing full pairs rather than inventing a new standard - the repo already voted. Encoding prose gates as rubric rule IDs is what makes audit verdicts reproducible across sessions and agents (the auditor cites SPK-style IDs instead of paraphrasing prose). The parity checker turns the definition of done into a machine gate, and covers TASK-SKILL-117's new pair and TASK-CUO-209's expansion at no extra cost.</p>\n<h2>§3 - Contract</h2>\n<p>Rubric header convention (all six):</p>\n<pre><code class=\"language-markdown\"># &lt;name&gt;_rubric@1.0\nconstants: TOTAL_ROWS_MIN=8 (MUST tasks) | BRANCH_COVERAGE_MIN=80 | COVERAGE_THRESHOLD=90 (config-overridable, TASK-CUO-207)\nfamilies: &lt;ABBR&gt;-STRUCT | &lt;ABBR&gt;-GATE | &lt;ABBR&gt;-TRACE   (per-pair families as needed)\nverdict: pass requires 10/10; any family failure -&gt; fail; ambiguity -&gt; needs_human</code></pre>\n<p><code>check-pair-parity.sh &lt;skills-dir&gt;</code>: exit 0 | exit 10 with <code>PARITY</code> lines | exit 2 unreadable dir.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>All 12 skill dirs carry their file classes</strong> (§1 #1, #2) - <code>check-pair-parity.sh modules/skill</code> reports zero PARITY lines for the six pairs (and the four full pairs still pass).</li><li><strong>Every prose gate has a rule ID</strong> (§1 #3) - for each of the six audits, every numeric/enum gate quoted from its SKILL.md appears in RUBRIC.md with a stable rule ID; spot-list in each RUBRIC.md's changelog block maps prose -&gt; rule.</li><li><strong>Constants are declared once</strong> (§1 #4) - the numeric gates appear as named constants; coverage rubric names the TASK-CUO-207 override hook.</li><li><strong>No artefact shape change</strong> (§1 #5) - each pair's SKILL.md artefact section is diff-identical for the artefact fields before/after (documentation-only diff).</li><li><strong>Parity checker catches a miss</strong> (§1 #6) - deleting one RUBRIC.md in a scratch copy exits 10 naming the pair and file; <code>build.sh</code> propagates the failure.</li><li><strong>Trigger contracts untouched</strong> (§1 #7) - all six author trigger-test suites pass unmodified (descriptions and trigger tests byte-identical except added file pointers).</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/install/tests/test_pair_parity.sh\nt01_all_pairs_parity_clean()     # AC 1\nt02_prose_gate_rule_ids()        # AC 2  (grep each rubric for its constants + rule-ID table)\nt03_constants_block()            # AC 3\nt04_artefact_sections_stable()   # AC 4  (git diff scoped to artefact-spec heading ranges is empty)\nt05_checker_catches_missing()    # AC 5\nt06_trigger_tests_unchanged()    # AC 6  (sha256 of the six TRIGGER_TESTS.md before/after)</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>Full file matrix = 72 files across the 12 skill dirs (6 per author, 6 per audit, per §1 #1-#2); frontmatter <code>new_files</code> lists the rubric/pipeline spine plus tooling, and the parity checker is the completeness authority (AC 1 gates the whole matrix, so nothing hides behind the abbreviated list). Per pair: derive PIPELINE.md phases from the SKILL.md's own step prose; INVARIANTS.md lifts MUST/MUST NOT lines; envelopes copy the shape of the nearest full pair (backlog-state-update mirrors task-audit's, coverage-gate mirrors implementation-plan-audit's); RUBRIC.md per §3 header + one family table per gate group. Order of work: backlog-state-update and coverage-gate first (they gate every ship run), then edge-case-matrix, observability-injection, mock-contract-test, repo-context-map.</p>\n<h2>§7 - Dependencies</h2>\n<p>None upstream. TASK-CUO-207 later flips COVERAGE_THRESHOLD to config-driven (the rubric already names the hook). TASK-CUO-209 vendors the enriched pairs as-is. TASK-CUO-205 will bump backlog-state-update to @2 (insert-row) - land THIS task first so the @2 change edits a full contract, not a thin one.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ bash tools/install/check-pair-parity.sh modules/skill\nPARITY coverage-gate-audit: missing RUBRIC.md\nPARITY coverage-gate-audit: missing REPORT_FORMAT.md\n$ echo $?\n10</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Shared references (HITL_PROTOCOL.md, ANTI_FABRICATION.md, UNTRUSTED_CONTENT.md) stay per-skill copies for now, matching the existing full pairs; deduplication into a shared dir is a separate refactor candidate, deliberately not smuggled in here.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Rubric invents a stricter bar than the SKILL.md prose - AC 2's prose-&gt;rule mapping table makes each rule cite its prose source; unsourced rules are review findings.</li><li>Envelope drift from actual artefacts - envelopes are copied from the nearest full pair then field-checked against each SKILL.md artefact section (AC 4 protects the section).</li><li>Parity checker too rigid for legitimately different skills (backlog-state-update has no references/ need) - the checker checks FILE CLASSES per side as listed in §1; the list IS the policy, adjustable only by editing this task's clauses.</li><li>Vendored copies go stale - build.sh copies from modules/skill on every build; TASK-IMP-068's gate rebuilds on every touch of modules/skill/**.</li><li>Six-pair scope creep into content rewrites - clause #7 pins descriptions and trigger tests byte-stable; the task adds files, it does not re-author skills.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep rule-ID prefixes distinct per pair (RCM-, ECM-, MCT-, OBS-, BSU-, COV-) so audit reports stay greppable across a ship run's artefact trail. The parity checker's file-class lists live at the top of the script as two arrays - one place to evolve the convention.</p>\n<p><strong>Post-ship amendment (2026-07-12, TASK-IMP-071 leg):</strong> AC 4's t04 guard false-fired on three legitimate mid-flight citation mutations (the point-in-time-guard class). Amended to an at-rest guard: dirty-worktree files warn and defer to the committed state; CI always checks clean trees, so the additive-only guarantee holds where it matters.</p>\n<p><em>End of TASK-SKILL-118.</em></p>";
