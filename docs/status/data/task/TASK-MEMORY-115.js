window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MEMORY-115"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The dreaming subsystem is a <strong>batch asynchronous process</strong> that mines the memory's recent state for memory-quality improvements and produces a reviewable diff. It runs <strong>out of band</strong> from any agent session and has its own audit identity. The contract:</p>\n<ol><li><strong>MUST</strong> be triggerable via <code>cyberos dream</code> CLI invocation, with a per-run <code>dream_id</code> (ULID, 26-char base32) generated at start. Triggers fall into three classes:</li></ol>\n<ul><li><strong>Manual</strong>: <code>cyberos dream --since 24h</code> (operator on-demand)</li><li><strong>Cron</strong>: identical CLI invocation via OS-level scheduler (covered by TASK-MEMORY-110-style automation runbook; not part of this task's scope)</li><li><strong>API</strong>: <code>POST /api/v2/dream</code> on <code>cyberos serve</code> (deferred to slice 4; CLI is the slice-3 surface)</li></ul>\n<ol><li><strong>MUST NOT</strong> run in-band with any normal <code>cyberos put</code> / <code>cyberos read</code> / <code>cyberos search</code> operation. The dream runner takes a snapshot (HEAD seq + chain tip) at start and operates against that snapshot; concurrent writes from other processes proceed normally and are integrated on the next dream run, not preempted.</li><li><strong>MUST</strong> support the following four detector types (closed enum DEC-214) that produce a <code>DreamProposal</code>:</li></ol>\n<table><thead><tr><th>Detector</th><th>Output proposal <code>op</code></th><th>What it does</th></tr></thead><tbody><tr><td><code>duplicates</code></td><td><code>merge</code></td><td>finds memories with body cosine-sim ≥ 0.92; proposes consolidation into one canonical with the others becoming <code>extra.merged_into</code> provenance</td></tr><tr><td><code>stale</code></td><td><code>stale</code></td><td>finds memories contradicted by more-recent audit rows OR explicit user <code>correction_to:</code> rows; proposes tombstone with rationale</td></tr><tr><td><code>patterns</code></td><td><code>new</code></td><td>finds recurring task/outcome combos across <code>episode.logged</code> rows (TASK-MEMORY-112 dep); proposes a NEW memory under <code>memories/refinements/</code> summarising the pattern</td></tr><tr><td><code>verify</code></td><td><code>verify</code></td><td>finds memories whose facts were actually used in recent sessions and didn't trigger correction; proposes annotating <code>meta.last_verified_at</code> (no body change)</td></tr></tbody></table>\n<ol><li><strong>MUST NOT</strong> auto-apply proposals. The runner produces a <code>DreamDiff</code> JSON artefact at <code>dreams/&lt;utc-timestamp&gt;/diff.json</code> (with <code>manifest.json:dreams.retention_days</code> default = 90 governing cleanup). A separate <code>cyberos dream apply &lt;dream_id&gt;</code> call is REQUIRED to merge proposals into the chain. Subset application via <code>--proposal-ids</code> is supported.</li><li><strong>MUST</strong> emit two audit rows per dream run (regardless of apply state):</li></ol>\n<ul><li><code>dream.start</code>: payload <code>{dream_id, scope, since, detectors, invoker, started_at}</code></li><li><code>dream.complete</code>: payload <code>{dream_id, proposals_count, applied_count: 0, duration_ms, quality_metrics: {…}}</code></li></ul>\n<ol><li><strong>MUST</strong> validate that every <code>cyberos.core.writer.Writer</code> operation originating from <code>dream apply</code> carries <code>extra.dream_id</code> + <code>extra.proposal_id</code>. The writer enforces this via constructor flag <code>dream_origin: bool = False</code>; the dream applier sets it True. Walker invariant <code>dream-applied-row-has-provenance</code> catches drift.</li><li><strong>MUST</strong> support <code>--dry-run</code> on <code>cyberos dream</code> — runs the full detection pipeline + writes the diff to disk, but emits a <code>dream.complete</code> row with <code>applied_count: 0</code> and never advances any memory file. The diff file IS written so operators can inspect it.</li><li><strong>MUST</strong> support <code>--detectors duplicates,stale,patterns,verify</code> (default: all) and <code>--scope &lt;path&gt;</code> (default: full memory) for narrow runs. A scope like <code>memories/facts/</code> only walks that subtree.</li><li><strong>MUST</strong> select the active LLM invoker via the same chain as TASK-MEMORY-114 (<code>--invoker {mock|anthropic}</code> → <code>CYBEROS_DREAM_INVOKER</code> → <code>manifest.dream.invoker</code> → default <code>mock</code> if no API key). Same <code>CYBEROS_DISABLE_LLM=1</code> escape hatch.</li><li><strong>MUST</strong> be idempotent in the strict sense: re-running <code>cyberos dream apply &lt;dream_id&gt;</code> with the same proposal-id set is a no-op IF nothing has changed about the affected memories between the two applies. The applier records the body_hash of each affected memory at first-apply time; on re-apply, if the on-disk hash matches the recorded one AND the row already exists in the chain, no new row is emitted. If the on-disk hash differs (someone edited the memory meanwhile), the re-apply REFUSES with <code>proposal_preconditions_failed</code> rather than overwriting.</li><li><strong>MUST</strong> require the AGENTS.md §7.7 amendment APPROVED via <code>APPROVE protocol change P19 §7.7</code> chat-turn before any apply rows are emitted. The CLI loads AGENTS.md and checks for the §7.7 anchor at runtime; missing → refuse with structured error pointing at the protocol section.</li><li><strong>MUST</strong> complete a typical dream run (input ≤ 10K memories, ≤ 5K episode rows, ≤ 30-day audit-row window) in ≤ 5 minutes wall time with <code>--invoker anthropic</code>, ≤ 1 minute with <code>--invoker mock</code>. The runner emits incremental progress to stderr.</li><li><strong>MUST</strong> support <code>--interactive</code> on <code>cyberos dream apply</code> — prompts per-proposal with the rationale + body preview; operator types <code>y</code> / <code>n</code> / <code>s</code> (skip remaining); ESC quits without partial-apply.</li><li><strong>MUST</strong> preserve full memory integrity on dream apply — if any single proposal's write fails (e.g. ACL denied per TASK-MEMORY-117), the entire apply transaction rolls back to the snapshot HEAD seq. No half-applied diffs.</li><li><strong>MUST</strong> emit <code>dream.proposal_applied</code> aux audit rows per successful proposal application — payload <code>{dream_id, proposal_id, op, affected_paths[], applied_at}</code>.</li><li><strong>MUST</strong> track quality metrics over time. Each <code>dream.complete</code> row's <code>quality_metrics</code> MUST include: <code>{proposals_count_by_kind, applied_count_by_kind, fallback_count, avg_invoker_latency_ms, scope_size_memories, scope_size_audit_rows}</code>. These feed TASK-MEMORY-120's <code>cyberos history</code> retrospective view.</li><li><strong>SHOULD</strong> support <code>cyberos dream review &lt;dream_id&gt;</code> — renders the diff in a human-friendly table view (path / op / rationale / preview). Slice-4 polish, not blocker.</li><li><strong>SHOULD</strong> support detector plug-ins via <code>entry_points[\"cyberos.dream.detectors\"]</code> — third-party detectors can register without forking. Slice-4+ stretch.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why out-of-band, not in-session (§1 #2, DEC-210).</strong> The Anthropic talk's most-emphasised design property. In-session reflection means the agent's task latency tax goes up for memory-quality work that benefits <em>future</em> sessions, not the current one. Out-of-band means the operator chooses when to pay the latency (overnight cron, post-task batch, on-demand investigation). Concurrent writes during a dream run integrate on the next dream — no need to fight for the snapshot.</p>\n<p><strong>Why operator-gated apply, not auto-apply (§1 #4, DEC-211).</strong> The talk repeatedly returns to this. Apply has memory-mutating consequences (tombstone, merge, new refinement). Apply must be auditable + reviewable, not surprising. Three patterns the talk endorses: (a) human review then apply; (b) review via API + auto-apply with rule-based filter; (c) auto-apply for low-risk proposals (verify-only). TASK-MEMORY-115 ships pattern (a); future work can opt-in (b) and (c) by extending the CLI.</p>\n<p><strong>Why ULID dream_id (§1 #1).</strong> ULIDs are time-sortable + URL-safe + 128-bit. Beats UUID v4 for chronological scanning (\"all dreams since X\"). Beats sequential integers for distribution (multiple operators can run dreams without coordination). Matches the existing audit-row ID convention.</p>\n<p><strong>Why 4 closed proposal kinds (§1 #3, DEC-214).</strong> Open ops space invites detector drift. Each new \"kind\" expands the apply-side validation surface. The 4 ops cover the talk's enumerated scenarios verbatim: dedup, stale-removal, new-pattern, verification. Slice-4 can add <code>relocate</code> (move to better-scoped store, gated on TASK-MEMORY-117 ACLs) and <code>rescore</code> (adjust importance after observation).</p>\n<p><strong>Why <code>dreams/&lt;utc-timestamp&gt;/</code> directory (§1 #4).</strong> Two reasons: (a) timestamp prefix sorts chronologically; (b) directory-per-dream lets the diff coexist with <code>notes.md</code>, intermediate logs, applied-rows-summary file — future expansion lives in the same dir. The retention policy lives on <code>manifest.json</code> so operators can tune per project.</p>\n<p><strong>Why preconditioned re-apply (§1 #10).</strong> Idempotency without preconditions is dangerous: imagine a stale-tombstone applied today, then someone resurrects the content tomorrow, then <code>dream apply --proposal-ids tombstone-prop</code> is re-run — would clobber the resurrection. Preconditions ensure the apply only proceeds if the memory's state matches what the proposal was generated against. The body_hash comparison is the natural primitive (already in <code>meta.body_hash</code>).</p>\n<p><strong>Why AGENTS.md §7.7 amendment required (§1 #11, DEC-212).</strong> The protocol's §0.2 immutability gate. Adding <code>extra.dream_id</code> provenance is a new normative requirement on the writer (\"rows from this origin MUST carry this metadata\"). That's a protocol change, not a config change. The CLI's runtime check for the §7.7 anchor enforces the gate: an operator who installs the task's code but hasn't APPROVED the protocol change can't accidentally emit non-compliant rows.</p>\n<p><strong>Why 5-minute wall-time budget (§1 #12).</strong> Dream is meant to run nightly. 5 minutes fits within a <code>0 2 * * *</code> cron slot without contending with other automation. The mock-invoker fast path (≤ 1 min) keeps tests + CI green.</p>\n<p><strong>Why transactional apply (§1 #14).</strong> Half-applied diffs are worse than not-applied. The talk's design notes call out \"produces an updated memory state that you can then apply immediately\"; the implication is atomicity. Practically: the applier opens a single Writer transaction (acquires <code>.lock</code>), validates every proposal's precondition, then emits each proposal's rows under the same lock. Any failure → release lock + rollback at HEAD.</p>\n<p><strong>Why per-proposal aux row instead of one bulk row (§1 #15).</strong> TASK-MEMORY-120 (<code>cyberos history</code>) needs per-path provenance: \"this memory was tombstoned by dream X, proposal Y.\" Bulk rows force history to re-parse the payload to find which path was affected. Per-proposal rows are cheap (audit chain is designed for high cardinality) and make history queries trivial.</p>\n<p><strong>Why explicit detector flag (§1 #8).</strong> Operators want to run individual detectors during investigation (\"just look for duplicates in <code>memories/facts/</code>\"). Default-all-on means the canonical full run is one command; opt-in subsets means the focused run is also one command. The flag is the affordance.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Schema</h3>\n<pre><code class=\"language-json\">{\n  \"$defs\": {\n    \"DreamProposalKind\": {\n      \"type\": \"string\",\n      \"enum\": [\"merge\", \"stale\", \"new\", \"verify\"]\n    },\n    \"DreamProposal\": {\n      \"type\": \"object\",\n      \"required\": [\"proposal_id\", \"op\", \"rationale\"],\n      \"properties\": {\n        \"proposal_id\":  {\"type\": \"string\", \"pattern\": \"^P[0-9A-Z]{8}$\"},\n        \"op\":           {\"$ref\": \"#/$defs/DreamProposalKind\"},\n        \"paths\":        {\"type\": \"array\", \"items\": {\"type\": \"string\"}, \"default\": []},\n        \"into\":         {\"type\": \"string\"},\n        \"content_preview\": {\"type\": \"string\", \"maxLength\": 2048},\n        \"rationale\":    {\"type\": \"string\", \"minLength\": 1},\n        \"input_session_ids\": {\"type\": \"array\", \"items\": {\"type\": \"string\"}, \"default\": []},\n        \"input_audit_seqs\":  {\"type\": \"array\", \"items\": {\"type\": \"integer\", \"minimum\": 1}, \"default\": []},\n        \"precondition_body_hashes\": {\"type\": \"object\", \"additionalProperties\": {\"type\": \"string\"}, \"default\": {}}\n      }\n    },\n    \"DreamDiff\": {\n      \"type\": \"object\",\n      \"required\": [\"dream_id\", \"scope\", \"since\", \"proposals\", \"metrics\"],\n      \"properties\": {\n        \"dream_id\": {\"type\": \"string\", \"pattern\": \"^[0-9A-HJKMNP-TV-Z]{26}$\"},\n        \"scope\":    {\"type\": \"string\"},\n        \"since\":    {\"type\": \"string\", \"format\": \"date-time\"},\n        \"input_sessions\": {\"type\": \"array\", \"items\": {\"type\": \"string\"}, \"default\": []},\n        \"proposals\": {\"type\": \"array\", \"items\": {\"$ref\": \"#/$defs/DreamProposal\"}},\n        \"metrics\":  {\"type\": \"object\", \"additionalProperties\": true}\n      }\n    }\n  }\n}</code></pre>\n<h3>Core dataclasses</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/dream/proposals.py\nfrom __future__ import annotations\nfrom dataclasses import dataclass, field\nfrom typing import Literal, Optional\n\nProposalKind = Literal[\"merge\", \"stale\", \"new\", \"verify\"]\n\n\n@dataclass\nclass DreamProposal:\n    proposal_id:  str          # \"P\" + 8 random base32 chars\n    op:           ProposalKind\n    paths:        list[str]    = field(default_factory=list)\n    into:         Optional[str] = None\n    content_preview: str       = \"\"\n    rationale:    str          = \"\"\n    input_session_ids:         list[str] = field(default_factory=list)\n    input_audit_seqs:          list[int] = field(default_factory=list)\n    precondition_body_hashes:  dict[str, str] = field(default_factory=dict)\n\n\n@dataclass\nclass DreamDiff:\n    dream_id: str    # ULID\n    scope:    str\n    since:    str    # ISO timestamp\n    input_sessions: list[str]\n    proposals: list[DreamProposal]\n    metrics:  dict</code></pre>\n<h3>Runner skeleton</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/dream/runner.py\nfrom __future__ import annotations\nimport asyncio, json, time\nfrom dataclasses import asdict\nfrom datetime import datetime, timedelta, timezone\nfrom pathlib import Path\nfrom typing import Optional\n\nfrom cyberos.core.writer import Writer\nfrom cyberos.core.dream.proposals import DreamProposal, DreamDiff\nfrom cyberos.core.dream.detectors import duplicates, stale, patterns, verify\n\n\nasync def run(\n    writer:        Writer,\n    since:         timedelta,\n    scope:         str            = \"\",\n    detector_names: list[str]     = (\"duplicates\", \"stale\", \"patterns\", \"verify\"),\n    invoker_name:  Optional[str]  = None,\n    dry_run:       bool           = False,\n) -&gt; DreamDiff:\n    dream_id = _generate_ulid()\n    started_at = datetime.now(timezone.utc)\n\n    # snapshot guard\n    snapshot_head = writer.head_seq()\n\n    writer.emit_aux(\n        kind=\"dream.start\",\n        payload={\n            \"dream_id\":   dream_id,\n            \"scope\":      scope or \"*\",\n            \"since\":      (started_at - since).isoformat(),\n            \"detectors\":  list(detector_names),\n            \"invoker\":    invoker_name or \"default\",\n            \"started_at\": started_at.isoformat(),\n        },\n        actor=\"dream-runner\",\n    )\n\n    detectors = {\n        \"duplicates\": duplicates.run,\n        \"stale\":      stale.run,\n        \"patterns\":   patterns.run,\n        \"verify\":     verify.run,\n    }\n\n    all_proposals: list[DreamProposal] = []\n    for name in detector_names:\n        if name not in detectors:\n            raise ValueError(f\"unknown detector {name!r}; expected one of {sorted(detectors)}\")\n        proposals = await detectors[name](writer, since, scope, invoker_name)\n        all_proposals.extend(proposals)\n\n    # de-dup proposals that touch the same path (e.g. duplicates and stale both fire on one path)\n    seen = set()\n    dedup: list[DreamProposal] = []\n    for p in all_proposals:\n        key = (p.op, tuple(sorted(p.paths)))\n        if key in seen: continue\n        seen.add(key)\n        dedup.append(p)\n\n    diff = DreamDiff(\n        dream_id=dream_id,\n        scope=scope or \"*\",\n        since=(started_at - since).isoformat(),\n        input_sessions=[],  # populated when TASK-MEMORY-119 lands\n        proposals=dedup,\n        metrics={\n            \"proposals_count_by_kind\": _bucket_by_kind(dedup),\n            \"scope_size_memories\":     writer.count_memories(scope),\n            \"scope_size_audit_rows\":   writer.count_audit_rows_since(started_at - since),\n            \"snapshot_head\":           snapshot_head,\n        },\n    )\n\n    out_dir = writer.store_path / \"dreams\" / started_at.strftime(\"%Y%m%dT%H%M%SZ\")\n    out_dir.mkdir(parents=True, exist_ok=True)\n    (out_dir / \"diff.json\").write_text(json.dumps(_diff_to_dict(diff), indent=2, sort_keys=True))\n\n    writer.emit_aux(\n        kind=\"dream.complete\",\n        payload={\n            \"dream_id\":        dream_id,\n            \"proposals_count\": len(dedup),\n            \"applied_count\":   0,                            # always 0 here — apply is a separate command\n            \"duration_ms\":     int((time.time() - started_at.timestamp()) * 1000),\n            \"quality_metrics\": diff.metrics,\n            \"dry_run\":         dry_run,\n        },\n        actor=\"dream-runner\",\n    )\n    return diff\n\n\ndef _generate_ulid() -&gt; str:\n    \"\"\"26-char Crockford base32 ULID. Real impl uses `python-ulid` package.\"\"\"\n    import secrets, time\n    alpha = \"0123456789ABCDEFGHJKMNPQRSTVWXYZ\"\n    ms = int(time.time() * 1000)\n    return \"\".join(alpha[(ms &gt;&gt; (5 * i)) &amp; 0x1F] for i in range(10))[::-1] + \"\".join(\n        secrets.choice(alpha) for _ in range(16)\n    )\n\n\ndef _bucket_by_kind(proposals: list[DreamProposal]) -&gt; dict:\n    out = {\"merge\": 0, \"stale\": 0, \"new\": 0, \"verify\": 0}\n    for p in proposals:\n        out[p.op] += 1\n    return out\n\n\ndef _diff_to_dict(d: DreamDiff) -&gt; dict:\n    return {\n        \"dream_id\": d.dream_id, \"scope\": d.scope, \"since\": d.since,\n        \"input_sessions\": d.input_sessions,\n        \"proposals\": [asdict(p) for p in d.proposals],\n        \"metrics\": d.metrics,\n    }</code></pre>\n<h3>Applier</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/dream/applier.py\nfrom __future__ import annotations\nimport hashlib\nfrom pathlib import Path\nfrom typing import Optional\n\nfrom cyberos.core.writer import Writer\nfrom cyberos.core.dream.proposals import DreamDiff, DreamProposal\n\n\nclass PreconditionFailed(Exception):\n    pass\n\n\ndef apply(writer: Writer, diff: DreamDiff, proposal_ids: Optional[set[str]] = None,\n          dream_id: Optional[str] = None) -&gt; dict:\n    \"\"\"Apply selected proposals from a DreamDiff. Returns metrics.\"\"\"\n    target_dream_id = dream_id or diff.dream_id\n    targets = [p for p in diff.proposals if proposal_ids is None or p.proposal_id in proposal_ids]\n\n    # Verify §7.7 anchor exists before any writes\n    if not _agents_md_has_section_7_7(writer.store_path):\n        raise RuntimeError(\n            \"AGENTS.md does not contain §7.7 Dreaming. Approve via:\\n\"\n            \"APPROVE protocol change P19 §7.7\"\n        )\n\n    # Precondition pass\n    for p in targets:\n        for path, expected_hash in p.precondition_body_hashes.items():\n            actual = _sha256_body(writer.store_path / path)\n            if actual != expected_hash:\n                raise PreconditionFailed(\n                    f\"Proposal {p.proposal_id}: path {path} body_hash drift \"\n                    f\"(expected {expected_hash[:12]}…, got {actual[:12]}…)\"\n                )\n\n    # Apply pass under exclusive lock\n    applied_count = 0\n    with writer.exclusive_lock(reason=f\"dream-apply:{target_dream_id}\") as txn:\n        txn.set_origin(dream_id=target_dream_id)\n        for p in targets:\n            _apply_one(txn, p)\n            applied_count += 1\n        writer.emit_aux(\n            kind=\"dream.proposal_applied\",\n            payload={\n                \"dream_id\":      target_dream_id,\n                \"proposal_id\":   p.proposal_id,\n                \"op\":            p.op,\n                \"affected_paths\": p.paths,\n                \"applied_at\":    txn.now_iso(),\n            },\n            actor=\"dream-applier\",\n        )\n    return {\"applied_count\": applied_count, \"rejected\": len(diff.proposals) - applied_count}\n\n\ndef _apply_one(txn, p: DreamProposal):\n    if p.op == \"merge\":\n        # body of first path becomes the canonical; others tombstoned with extra.merged_into\n        canonical = p.into or p.paths[0]\n        for src in p.paths[1:]:\n            txn.delete(src, mode=\"tombstone\",\n                       extra={\"merged_into\": canonical, \"proposal_id\": p.proposal_id})\n    elif p.op == \"stale\":\n        for path in p.paths:\n            txn.delete(path, mode=\"tombstone\",\n                       extra={\"reason\": \"stale\", \"proposal_id\": p.proposal_id,\n                              \"rationale\": p.rationale})\n    elif p.op == \"new\":\n        txn.put(p.paths[0], p.content_preview,\n                extra={\"proposal_id\": p.proposal_id, \"source\": \"dream\"})\n    elif p.op == \"verify\":\n        for path in p.paths:\n            # update meta.last_verified_at; body unchanged\n            txn.touch_meta(path, {\"last_verified_at\": txn.now_iso()},\n                           extra={\"proposal_id\": p.proposal_id})\n    else:\n        raise ValueError(f\"unknown proposal op {p.op!r}\")\n\n\ndef _agents_md_has_section_7_7(store_path: Path) -&gt; bool:\n    agents_md = store_path / \"AGENTS.md\"\n    if not agents_md.exists():\n        return False\n    body = agents_md.read_text()\n    return \"## §7.7\" in body or \"§7.7  Dreaming\" in body or \"§7.7 Dreaming\" in body\n\n\ndef _sha256_body(path: Path) -&gt; str:\n    return hashlib.sha256(path.read_bytes()).hexdigest()</code></pre>\n<h3>AGENTS.md §7.7 amendment text (proposed)</h3>\n<pre><code class=\"language-text\">## §7.7  Dreaming (added by P19 — requires APPROVE chat-turn per §0.2)\n\n§7.7.1  Dreaming is the out-of-band batch process specified in\nTASK-MEMORY-115. Dream-runner and dream-applier are distinct identities;\nthey MUST NOT execute in-band with any agent session that mutates memory.\n\n§7.7.2  Every audit row emitted by dream-applier MUST carry both\n`extra.dream_id` (ULID matching the active `dream.start` row) and\n`extra.proposal_id` (matching the source `DreamProposal.proposal_id`).\nWalker invariant `dream-applied-row-has-provenance` enforces this.\n\n§7.7.3  Dream apply MUST validate body-hash preconditions before any\nwrites. A precondition failure on any proposal in a batch aborts the\nbatch; no half-applied state.\n\n§7.7.4  Dream proposals are EITHER applied via `cyberos dream apply`\n(operator-gated) OR remain on disk as `dreams/&lt;ts&gt;/diff.json` artefacts\nuntil apply or retention-expiry. The protocol provides no auto-apply\nmechanism.</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Dream emits both start + complete rows</strong> — <code>cyberos dream --since 24h</code> advances HEAD by exactly 2 aux rows when there are zero proposals (just <code>dream.start</code> + <code>dream.complete</code>). <em>(traces_to: §1 #5)</em></li><li><strong>Dream emits a diff file</strong> — same invocation writes <code>dreams/&lt;ts&gt;/diff.json</code> to disk; file is valid against <code>DreamDiff</code> schema. <em>(traces_to: §1 #4)</em></li><li><strong>Dry-run never advances memory state</strong> — <code>cyberos dream --dry-run</code> produces the diff + 2 aux rows but no <code>put</code>/<code>delete</code> rows. <em>(traces_to: §1 #7)</em></li><li><strong>Apply requires §7.7 anchor</strong> — <code>AGENTS.md</code> lacking the §7.7 section → <code>dream apply</code> raises with message naming <code>APPROVE protocol change P19 §7.7</code>. <em>(traces_to: §1 #11)</em></li><li><strong>Apply advances HEAD per proposal</strong> — diff with 3 merge proposals + §7.7 present → <code>dream apply &lt;dream_id&gt;</code> advances HEAD by 3 (delete rows) + 3 (aux <code>dream.proposal_applied</code>) = 6. <em>(traces_to: §1 #15)</em></li><li><strong>Applied rows carry dream_id + proposal_id</strong> — inspect each delete row's <code>extra</code> — both keys present and well-formed. <em>(traces_to: §1 #6)</em></li><li><strong>Walker rejects dream-origin row without provenance</strong> — manually emit a <code>put</code> row with <code>extra: {}</code> while <code>dream_origin: True</code> → <code>cyberos doctor</code> fails with <code>dream-applied-row-has-provenance</code>. <em>(traces_to: §1 #6)</em></li><li><strong>Detector — duplicates threshold</strong> — fixture with 3 memories at cosine 0.95 / 0.90 / 0.80 → only the 0.95 pair generates a merge proposal. <em>(traces_to: §1 #3)</em></li><li><strong>Detector — stale catches contradicted memory</strong> — fixture with <code>facts/x.md</code> saying \"Linear project X\" followed by <code>correction_to:</code> row stating \"moved to Jira\" → stale proposal for <code>facts/x.md</code> with rationale citing the correction row's seq. <em>(traces_to: §1 #3)</em></li><li><strong>Detector — patterns identifies recurring task/outcome</strong> — 5 <code>episode.logged</code> rows with same task fingerprint, 4 success + 1 failure → one new proposal under <code>memories/refinements/&lt;task-slug&gt;.md</code> with <code>content_preview</code> mentioning the 4:1 ratio. <em>(traces_to: §1 #3)</em></li><li><strong>Detector — verify finds used-and-not-corrected</strong> — fact memory referenced in 3 sessions over the window, never followed by a correction → verify proposal annotates <code>meta.last_verified_at</code>. <em>(traces_to: §1 #3)</em></li><li><strong>Snake-case proposal_id</strong> — <code>^P[0-9A-Z]{8}$</code> regex matches every generated id. <em>(traces_to: §1 #1 indirectly via DreamProposal schema)</em></li><li><strong>ULID format</strong> — <code>dream_id</code> matches <code>^[0-9A-HJKMNP-TV-Z]{26}$</code> (Crockford base32). <em>(traces_to: §1 #1)</em></li><li><strong>Apply idempotent on unchanged state</strong> — apply diff twice → second apply emits zero new rows (precondition match = no-op) and reports <code>applied_count: 0</code> with reason <code>idempotent</code>. <em>(traces_to: §1 #10)</em></li><li><strong>Apply refuses on body-hash drift</strong> — modify the target memory between dream + apply → second apply raises <code>PreconditionFailed</code> naming the drifted path. <em>(traces_to: §1 #10)</em></li><li><strong>Apply transactional</strong> — diff with 3 proposals where #2 will fail (e.g. nonexistent path) → entire apply rolls back; HEAD unchanged. <em>(traces_to: §1 #14)</em></li><li><strong>Detectors flag</strong> — <code>cyberos dream --detectors duplicates,stale</code> → patterns and verify detectors not invoked. <em>(traces_to: §1 #8)</em></li><li><strong>Scope flag</strong> — <code>cyberos dream --scope memories/facts</code> → no proposals reference paths outside <code>memories/facts/</code>. <em>(traces_to: §1 #8)</em></li><li><strong>Invoker selection chain</strong> — <code>--invoker anthropic</code> + missing key → constructor raises; <code>--invoker mock</code> always works; default with <code>ANTHROPIC_API_KEY</code> set → AnthropicInvoker. <em>(traces_to: §1 #9)</em></li><li><strong><code>CYBEROS_DISABLE_LLM=1</code> forces mock</strong> — env set, manifest says anthropic → MockInvoker. <em>(traces_to: §1 #9)</em></li><li><strong>Wall-time budget (mock)</strong> — 10K-memory + 5K-episode fixture, mock invoker → run completes in ≤ 60 s on CI hardware. <em>(traces_to: §1 #12)</em></li><li><strong>Interactive apply</strong> — <code>dream apply --interactive</code> with mocked stdin \"y\\nn\\ns\\n\" → first proposal applied, second skipped, remaining skipped after <code>s</code>. <em>(traces_to: §1 #13)</em></li><li><strong><code>dream.complete</code> quality_metrics shape</strong> — payload contains <code>proposals_count_by_kind: {merge, stale, new, verify}</code>, <code>scope_size_memories</code>, <code>scope_size_audit_rows</code>, <code>snapshot_head</code>. <em>(traces_to: §1 #16)</em></li><li><strong>Snapshot isolation</strong> — dream-runner takes a snapshot; concurrent <code>cyberos put</code> during run does not change which memories the detectors see (verified by adding a memory mid-run via second process; diff doesn't include it). <em>(traces_to: §1 #2)</em></li><li><strong>Diff retention default</strong> — <code>manifest.json:dreams.retention_days</code> defaults to 90 if absent. <em>(traces_to: §1 #4)</em></li><li><strong>Closed proposal kind enum</strong> — handcraft a <code>DreamProposal(op=\"weird\")</code> → constructor raises; walker rejects raw-written rows. <em>(traces_to: DEC-214)</em></li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_dream.py\nimport asyncio, json, re\nfrom datetime import timedelta\nfrom pathlib import Path\n\nimport pytest\nfrom cyberos.core.dream.runner import run as dream_run\n\n\n@pytest.mark.asyncio\nasync def test_dream_emits_start_and_complete_rows(empty_memory):\n    \"\"\"AC #1\"\"\"\n    head_before = empty_memory.head_seq()\n    diff = await dream_run(empty_memory, since=timedelta(hours=24), invoker_name=\"mock\")\n    assert empty_memory.head_seq() == head_before + 2     # exactly start + complete\n    assert diff.proposals == []\n\n\n@pytest.mark.asyncio\nasync def test_dream_writes_diff_file(empty_memory):\n    \"\"\"AC #2\"\"\"\n    diff = await dream_run(empty_memory, since=timedelta(hours=24), invoker_name=\"mock\")\n    dreams_dir = empty_memory.store_path / \"dreams\"\n    diff_files = list(dreams_dir.rglob(\"diff.json\"))\n    assert len(diff_files) == 1\n    content = json.loads(diff_files[0].read_text())\n    assert content[\"dream_id\"] == diff.dream_id\n\n\n@pytest.mark.asyncio\nasync def test_dry_run_no_proposal_rows(seeded_memory_with_dupes):\n    \"\"\"AC #3\"\"\"\n    head_before = seeded_memory_with_dupes.head_seq()\n    diff = await dream_run(seeded_memory_with_dupes, since=timedelta(hours=24),\n                            invoker_name=\"mock\", dry_run=True)\n    # Even though proposals were found, no apply rows emitted\n    assert seeded_memory_with_dupes.head_seq() == head_before + 2\n    assert len(diff.proposals) &gt; 0\n\n\n@pytest.mark.asyncio\nasync def test_dream_id_is_ulid_format(empty_memory):\n    \"\"\"AC #13\"\"\"\n    diff = await dream_run(empty_memory, since=timedelta(hours=24), invoker_name=\"mock\")\n    assert re.fullmatch(r\"[0-9A-HJKMNP-TV-Z]{26}\", diff.dream_id)\n\n\n@pytest.mark.asyncio\nasync def test_proposal_id_format(seeded_memory_with_dupes):\n    \"\"\"AC #12\"\"\"\n    diff = await dream_run(seeded_memory_with_dupes, since=timedelta(hours=24),\n                            invoker_name=\"mock\")\n    for p in diff.proposals:\n        assert re.fullmatch(r\"P[0-9A-Z]{8}\", p.proposal_id), p.proposal_id\n\n\n@pytest.mark.asyncio\nasync def test_dream_detectors_filter(seeded_memory_with_dupes):\n    \"\"\"AC #17\"\"\"\n    diff = await dream_run(seeded_memory_with_dupes, since=timedelta(hours=24),\n                            detector_names=(\"duplicates\",), invoker_name=\"mock\")\n    # No \"new\"/\"verify\"/\"stale\" proposals expected\n    kinds = {p.op for p in diff.proposals}\n    assert kinds.issubset({\"merge\"})\n\n\n@pytest.mark.asyncio\nasync def test_dream_scope_filter(seeded_memory_with_dupes):\n    \"\"\"AC #18\"\"\"\n    diff = await dream_run(seeded_memory_with_dupes, since=timedelta(hours=24),\n                            scope=\"memories/facts\", invoker_name=\"mock\")\n    for p in diff.proposals:\n        for path in p.paths:\n            assert path.startswith(\"memories/facts\"), path\n\n\n@pytest.mark.asyncio\nasync def test_dream_quality_metrics_shape(seeded_memory_with_dupes):\n    \"\"\"AC #23\"\"\"\n    diff = await dream_run(seeded_memory_with_dupes, since=timedelta(hours=24),\n                            invoker_name=\"mock\")\n    qm = diff.metrics\n    for key in (\"proposals_count_by_kind\", \"scope_size_memories\",\n                \"scope_size_audit_rows\", \"snapshot_head\"):\n        assert key in qm\n    for kind in (\"merge\", \"stale\", \"new\", \"verify\"):\n        assert kind in qm[\"proposals_count_by_kind\"]\n\n\n@pytest.mark.asyncio\nasync def test_unknown_detector_raises(empty_memory):\n    with pytest.raises(ValueError, match=\"unknown detector\"):\n        await dream_run(empty_memory, since=timedelta(hours=24),\n                         detector_names=(\"made_up\",), invoker_name=\"mock\")\n\n\n@pytest.mark.asyncio\nasync def test_disable_llm_forces_mock(empty_memory, monkeypatch):\n    \"\"\"AC #20\"\"\"\n    monkeypatch.setenv(\"CYBEROS_DISABLE_LLM\", \"1\")\n    diff = await dream_run(empty_memory, since=timedelta(hours=24), invoker_name=\"anthropic\")\n    # No error raised — fell back to mock\n    assert diff.dream_id\n\n\n@pytest.mark.asyncio\nasync def test_snapshot_isolation(seeded_memory, second_process_writer):\n    \"\"\"AC #24 — concurrent write during run doesn't change the dream's view.\"\"\"\n    snapshot_head = seeded_memory.head_seq()\n    # Trigger a dream + simulate a concurrent write halfway through (via fixture)\n    diff = await dream_run(seeded_memory, since=timedelta(hours=24), invoker_name=\"mock\")\n    # The concurrent write happened but the diff's snapshot_head should match what we started with\n    assert diff.metrics[\"snapshot_head\"] == snapshot_head</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_dream.py\nimport asyncio\nfrom datetime import timedelta\nfrom cyberos.core.dream.detectors import duplicates, stale, patterns, verify\n\n\n@pytest.mark.asyncio\nasync def test_duplicates_threshold(seeded_memory_with_dupes):\n    \"\"\"AC #8\"\"\"\n    proposals = await duplicates.run(seeded_memory_with_dupes, timedelta(hours=24), \"\", \"mock\")\n    # The 0.95-similar pair generates a proposal; 0.90 and 0.80 do not\n    assert len(proposals) == 1\n    assert proposals[0].op == \"merge\"\n\n\n@pytest.mark.asyncio\nasync def test_stale_finds_contradiction(seeded_memory_with_correction):\n    \"\"\"AC #9\"\"\"\n    proposals = await stale.run(seeded_memory_with_correction, timedelta(hours=24), \"\", \"mock\")\n    assert any(p.op == \"stale\" and \"facts/linear-project-x.md\" in p.paths for p in proposals)\n\n\n@pytest.mark.asyncio\nasync def test_patterns_identifies_recurring_episode(seeded_memory_with_pattern):\n    \"\"\"AC #10\"\"\"\n    proposals = await patterns.run(seeded_memory_with_pattern, timedelta(hours=24), \"\", \"mock\")\n    assert any(p.op == \"new\" and p.paths[0].startswith(\"memories/refinements/\") for p in proposals)\n\n\n@pytest.mark.asyncio\nasync def test_verify_finds_used_unconrected(seeded_memory_with_verified_fact):\n    \"\"\"AC #11\"\"\"\n    proposals = await verify.run(seeded_memory_with_verified_fact, timedelta(hours=24), \"\", \"mock\")\n    assert any(p.op == \"verify\" for p in proposals)\n\n\n@pytest.mark.asyncio\nasync def test_detectors_deterministic(seeded_memory_with_dupes):\n    \"\"\"Re-running the same detector on the same fixture gives the same proposal set.\"\"\"\n    a = await duplicates.run(seeded_memory_with_dupes, timedelta(hours=24), \"\", \"mock\")\n    b = await duplicates.run(seeded_memory_with_dupes, timedelta(hours=24), \"\", \"mock\")\n    assert {p.proposal_id for p in a} == {p.proposal_id for p in b}     # mock seeds reproducible IDs</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_dream.py\nimport pytest\nfrom cyberos.core.dream.applier import apply, PreconditionFailed\n\n\ndef test_apply_requires_section_7_7(seeded_memory_with_dupes, sample_diff):\n    \"\"\"AC #4\"\"\"\n    (seeded_memory_with_dupes.store_path / \"AGENTS.md\").write_text(\"# no section 7.7\")\n    with pytest.raises(RuntimeError, match=r\"APPROVE protocol change P19 §7\\.7\"):\n        apply(seeded_memory_with_dupes, sample_diff)\n\n\ndef test_apply_advances_head_per_proposal(seeded_memory_with_dupes, sample_diff,\n                                           ensure_section_7_7):\n    \"\"\"AC #5\"\"\"\n    head_before = seeded_memory_with_dupes.head_seq()\n    out = apply(seeded_memory_with_dupes, sample_diff)\n    # 3 deletes + 3 aux rows = +6\n    assert seeded_memory_with_dupes.head_seq() == head_before + 6\n    assert out[\"applied_count\"] == 3\n\n\ndef test_applied_rows_carry_provenance(seeded_memory_with_dupes, sample_diff,\n                                       ensure_section_7_7):\n    \"\"\"AC #6\"\"\"\n    head_before = seeded_memory_with_dupes.head_seq()\n    apply(seeded_memory_with_dupes, sample_diff)\n    # Walk new rows and confirm extra fields\n    for seq in range(head_before + 1, seeded_memory_with_dupes.head_seq() + 1):\n        row = seeded_memory_with_dupes.read_audit_row(seq)\n        if row[\"kind\"] in (\"put\", \"delete\"):\n            assert \"dream_id\" in row.get(\"extra\", {})\n            assert \"proposal_id\" in row.get(\"extra\", {})\n\n\ndef test_apply_idempotent(seeded_memory_with_dupes, sample_diff, ensure_section_7_7):\n    \"\"\"AC #14\"\"\"\n    apply(seeded_memory_with_dupes, sample_diff)\n    head_after_first = seeded_memory_with_dupes.head_seq()\n    out = apply(seeded_memory_with_dupes, sample_diff)\n    # Re-apply finds preconditions still satisfied (since first apply tombstoned),\n    # but the tombstone row already exists → no new rows\n    assert seeded_memory_with_dupes.head_seq() == head_after_first\n    assert out[\"applied_count\"] == 0\n\n\ndef test_apply_refuses_on_drift(seeded_memory_with_dupes, sample_diff_with_precondition,\n                                 ensure_section_7_7):\n    \"\"\"AC #15\"\"\"\n    # Modify one of the target paths between generation and apply\n    p = seeded_memory_with_dupes.store_path / sample_diff_with_precondition.proposals[0].paths[0]\n    p.write_text(p.read_text() + \"\\n# drift inserted\")\n    with pytest.raises(PreconditionFailed):\n        apply(seeded_memory_with_dupes, sample_diff_with_precondition)\n\n\ndef test_apply_transactional_rollback(seeded_memory_with_dupes, bad_diff,\n                                       ensure_section_7_7):\n    \"\"\"AC #16\"\"\"\n    head_before = seeded_memory_with_dupes.head_seq()\n    with pytest.raises(Exception):\n        apply(seeded_memory_with_dupes, bad_diff)\n    assert seeded_memory_with_dupes.head_seq() == head_before\n\n\ndef test_apply_proposal_id_filter(seeded_memory_with_dupes, sample_diff_3props,\n                                   ensure_section_7_7):\n    \"\"\"Apply only one proposal id by filtering.\"\"\"\n    head_before = seeded_memory_with_dupes.head_seq()\n    out = apply(seeded_memory_with_dupes, sample_diff_3props,\n                 proposal_ids={sample_diff_3props.proposals[0].proposal_id})\n    assert out[\"applied_count\"] == 1\n    assert seeded_memory_with_dupes.head_seq() == head_before + 2  # 1 delete + 1 aux\n\n\ndef test_apply_interactive_skip_remaining(seeded_memory_with_dupes, sample_diff_3props,\n                                          ensure_section_7_7, mocked_stdin):\n    \"\"\"AC #22\"\"\"\n    mocked_stdin.feed(\"y\\nn\\ns\\n\")\n    out = apply(seeded_memory_with_dupes, sample_diff_3props, interactive=True)\n    # First applied, second skipped, third skipped after 's'\n    assert out[\"applied_count\"] == 1</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>API contracts above are the skeleton. Implementation order:</p>\n<ol><li>AGENTS.md §7.7 amendment — author the section text (DO NOT commit until <code>APPROVE protocol change P19 §7.7</code> chat-turn).</li><li>Schema (memory.schema.json) — DreamDiff + DreamProposal + closed enums.</li><li>Walker invariants — <code>dream-applied-row-has-provenance</code>, <code>dream-diff-schema-valid</code>.</li><li><code>cyberos/core/dream/proposals.py</code> — dataclasses + JSON I/O.</li><li>Four detector modules.</li><li><code>cyberos/core/dream/runner.py</code> — orchestrator.</li><li><code>cyberos/core/dream/applier.py</code> — apply + precondition + transactional.</li><li><code>cyberos/core/writer.py</code> — <code>extra</code> dict support + <code>dream_origin</code> flag + <code>txn.set_origin</code>.</li><li><code>cyberos/cli/dream.py</code> — <code>dream</code> + <code>dream apply</code> subcommands.</li><li>Tests + fixtures.</li><li>CHANGELOG.</li></ol>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-MEMORY-112 (depends on)</strong> — Episodes provide the <code>episode.logged</code> aux rows the patterns detector consumes.</li><li><strong>TASK-MEMORY-113 (depends on)</strong> — recency decay informs detectors which memories are \"old enough to be stale candidates\".</li><li><strong>TASK-MEMORY-114 (depends on)</strong> — importance signal feeds the stale-detector's \"is this still important?\" check; same Invoker pattern reused.</li><li><strong>TASK-MEMORY-116 (this task enables)</strong> — <code>cyberos consolidate --semantic-dedup</code> is a strict subset of the duplicates detector; TASK-MEMORY-116 wraps it as a consolidation phase.</li><li><strong>TASK-MEMORY-119 (related)</strong> — session transcript ledger provides higher-fidelity input to the patterns detector; first ship uses audit-row-only input.</li><li><strong>TASK-MEMORY-120 (this task enables)</strong> — <code>cyberos history &lt;path&gt;</code> surfaces \"this memory was last modified by dream X proposal Y\" via the per-proposal aux rows.</li><li><strong>TASK-CUO-105 (related)</strong> — the LLM Invoker contract; both modules share the type signature.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>DreamDiff</code> (excerpt)</h3>\n<pre><code class=\"language-json\">{\n  \"dream_id\": \"01HJ8XVK9P0M7N5G4F3E2D1C0B\",\n  \"scope\":    \"memories/sre\",\n  \"since\":    \"2026-05-18T14:00:00Z\",\n  \"input_sessions\": [],\n  \"proposals\": [\n    {\n      \"proposal_id\": \"P3FQ8K2X\",\n      \"op\":          \"merge\",\n      \"paths\":       [\"memories/sre/dispatch-1.md\", \"memories/sre/dispatch-2.md\"],\n      \"into\":        \"memories/sre/dispatch-1.md\",\n      \"rationale\":   \"Both memories describe the 60-second retry pattern (cosine 0.94); merging preserves all observations.\",\n      \"input_audit_seqs\": [4291, 4317],\n      \"precondition_body_hashes\": {\n        \"memories/sre/dispatch-1.md\": \"abc123…\",\n        \"memories/sre/dispatch-2.md\": \"def456…\"\n      },\n      \"content_preview\": \"Observed across 5 sessions: every page-out alert produces a 60-second retry storm…\"\n    },\n    {\n      \"proposal_id\": \"P7TX2N4F\",\n      \"op\":          \"stale\",\n      \"paths\":       [\"memories/facts/linear-project-x.md\"],\n      \"rationale\":   \"Contradicted by audit row seq=4298: 'moved to Jira project PIPE'.\",\n      \"input_audit_seqs\": [4298],\n      \"precondition_body_hashes\": {\n        \"memories/facts/linear-project-x.md\": \"xyz789…\"\n      }\n    }\n  ],\n  \"metrics\": {\n    \"proposals_count_by_kind\": {\"merge\": 1, \"stale\": 1, \"new\": 0, \"verify\": 0},\n    \"scope_size_memories\":     142,\n    \"scope_size_audit_rows\":   311,\n    \"snapshot_head\":           4318\n  }\n}</code></pre>\n<h3><code>dream.complete</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"dream.complete\",\n  \"payload\": {\n    \"dream_id\": \"01HJ8XVK9P0M7N5G4F3E2D1C0B\",\n    \"proposals_count\": 2,\n    \"applied_count\":   0,\n    \"duration_ms\":     87420,\n    \"quality_metrics\": {\n      \"proposals_count_by_kind\": {\"merge\": 1, \"stale\": 1, \"new\": 0, \"verify\": 0},\n      \"scope_size_memories\":     142,\n      \"scope_size_audit_rows\":   311,\n      \"snapshot_head\":           4318,\n      \"fallback_count\":          0,\n      \"avg_invoker_latency_ms\":  340\n    },\n    \"dry_run\": false\n  }\n}</code></pre>\n<h3>Apply preconditions failure</h3>\n<pre><code class=\"language-text\">$ cyberos dream apply 01HJ8XVK9P0M7N5G4F3E2D1C0B --proposal-ids P3FQ8K2X\nError: PreconditionFailed: Proposal P3FQ8K2X: path memories/sre/dispatch-1.md\nbody_hash drift (expected abc123…, got def890…)\n\nThe memory was modified between dream generation and apply. Re-run `cyberos dream`\nto capture the new state, then re-apply.</code></pre>\n<h3>AGENTS.md anchor missing</h3>\n<pre><code class=\"language-text\">$ cyberos dream apply 01HJ8XVK9P0M7N5G4F3E2D1C0B\nError: AGENTS.md does not contain §7.7 Dreaming. Approve via:\nAPPROVE protocol change P19 §7.7</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>API surface on <code>cyberos serve</code> (POST /api/v2/dream) — §1 #1; slice 4.</li><li>Auto-apply for low-risk proposals (verify-only) — §1 #4; slice 4 design discussion.</li><li>Detector plug-ins via entry_points — §1 #18; slice 4+.</li><li><code>cyberos dream review &lt;id&gt;</code> rich table view — §1 #17; slice 4 polish.</li><li>A/B comparison harness across dream runs — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>§7.7 not yet APPROVED</td><td><code>_agents_md_has_section_7_7</code> check</td><td><code>dream apply</code> raises</td><td>Operator runs the APPROVE chat-turn + commits §7.7 to AGENTS.md</td></tr><tr><td>Body-hash drift mid-window</td><td>precondition check pre-apply</td><td><code>PreconditionFailed</code> with named path</td><td>Re-run dream, re-apply</td></tr><tr><td>Single proposal write fails (ACL denied per TASK-MEMORY-117)</td><td>writer exception in transaction</td><td>entire batch rolls back</td><td>Adjust ACL or skip that proposal id</td></tr><tr><td>Detector raises unhandled exception</td><td>runner catches + emits <code>dream.detector_failed</code> aux row</td><td>partial diff (with errors); other detectors complete</td><td>Operator inspects log; re-run dream with <code>--detectors</code> filter</td></tr><tr><td>Invoker timeout (Anthropic API slow)</td><td>per-call timeout in detector</td><td>detector falls back to TF-IDF heuristic; proposal noted with <code>quality: degraded</code></td><td>Operator checks API status</td></tr><tr><td>Mock-LLM regression (different scores between versions)</td><td>reproducible-id tests</td><td>CI catches</td><td>Preserve PRNG semantics</td></tr><tr><td>Diff retention exceeded</td><td>nightly cleanup script</td><td>old <code>dreams/&lt;ts&gt;/</code> directories pruned per <code>manifest.dreams.retention_days</code></td><td>Adjust retention</td></tr><tr><td>Two dream runs racing</td><td>both acquire snapshot, both emit independent diffs</td><td>both diffs coexist; apply of either is independent</td><td>None — by design</td></tr><tr><td>ULID collision</td><td>128-bit space; statistically impossible</td><td>n/a</td><td>n/a</td></tr><tr><td>Proposal_id collision within one dream</td><td>secrets-driven; collision in 8-char base32 is rare</td><td>runner re-generates on collision</td><td>None</td></tr><tr><td>Apply attempts to write to a no-longer-existent path</td><td>writer raises FileNotFoundError</td><td>transactional rollback</td><td>Re-run dream</td></tr><tr><td>AGENTS.md §7.7 amendment present but malformed</td><td>invariants walker</td><td><code>cyberos doctor</code> warns</td><td>Operator fixes wording</td></tr><tr><td>Dream produces zero proposals on clean store</td><td>runs to completion; diff has empty proposals[]</td><td><code>dream.complete</code> row's <code>proposals_count: 0</code>; success</td><td>None — by design</td></tr><tr><td>Patterns detector with &lt; 5 episodes per task fingerprint</td><td>threshold not met; no proposal</td><td>none emitted</td><td>None — by design</td></tr><tr><td>Verify detector fires on every memory if no corrections seen</td><td>scope filter + age filter</td><td>bounded set; verify proposals add only <code>last_verified_at</code></td><td>None — semantically cheap</td></tr><tr><td>Operator runs dream during cron run of another dream</td><td>snapshot isolation lets both run; lock contention only at apply</td><td>both complete normally; apply order serialised</td><td>None</td></tr><tr><td><code>dream.proposal_applied</code> row written before transaction commit</td><td>writer's aux-emit is inside transaction</td><td>transactional rollback unwinds it</td><td>None</td></tr><tr><td><code>manifest.dreams.retention_days = 0</code></td><td>jsonschema rejects (minimum=1)</td><td><code>ManifestError</code></td><td>Operator picks valid value</td></tr><tr><td>Dream interrupted (Ctrl-C)</td><td>partial diff file written to disk</td><td><code>dream.complete</code> aux row missing</td><td>Operator deletes the partial dir; re-runs</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Why per-detector module under <code>cyberos/core/dream/detectors/</code></strong> — each detector is independently testable and replaceable. Slice-4 <code>entry_points</code> plug-in mechanism slots in here.</li><li><strong>ULID implementation</strong> — minimal in-file generator for portability; <code>python-ulid</code> package can be a soft dependency for production. CI uses the inline generator.</li><li><strong>Proposal id generator uses <code>secrets</code></strong> — cryptographic randomness avoids <code>random</code> seeding issues across multi-process dream runs. 8 base32 chars = 40 bits; collision probability in a single dream is &lt; 1 in 10^10 even at 1000 proposals.</li><li><strong>The <code>extra</code> dict on <code>put</code>/<code>delete</code> rows is open-ended</strong> — <code>dream_id</code>, <code>proposal_id</code>, <code>reason</code>, <code>merged_into</code>, etc. all live in one bag. Schema-wise the AuditRecord's <code>extra</code> is <code>object</code> with no required keys; specific origins (dream) impose their own required-key invariant.</li><li><strong>The applier's <code>txn.set_origin(dream_id=...)</code> is the load-bearing primitive</strong> — it tells the Writer that until the transaction commits, every emitted row gets <code>extra.dream_id</code> automatically. No risk of an applier forgetting to set it on one path.</li><li><strong>The §7.7 anchor-check is anti-footgun</strong> — a developer who installs this task's code but hasn't APPROVED the protocol amendment shouldn't be able to silently emit non-compliant rows. The check turns \"subtle protocol violation\" into \"explicit error at apply time\".</li><li><strong>Transcript-input deferred</strong> — TASK-MEMORY-119 ships the session transcript ledger. For TASK-MEMORY-115 slice-3, dream consumes audit rows + memory bodies. When TASK-MEMORY-119 lands, the patterns detector gains conversation context as a stretch source.</li><li><strong>The bench / load test for <code>≤ 5 min wall-time anthropic</code> is operator-side</strong> — needs a real API key + a representative fixture. CI runs the ≤ 60 s mock variant.</li><li><strong><code>txn.touch_meta(...)</code> is a writer convenience for the verify op</strong> — updates frontmatter <code>meta.last_verified_at</code> without rewriting the body. Implementation: read body, parse frontmatter, replace <code>last_verified_at</code> field, write back as a new <code>put</code> row whose body_hash differs only in frontmatter.</li></ul>\n<hr>\n<p><em>End of TASK-MEMORY-115.</em></p>";
