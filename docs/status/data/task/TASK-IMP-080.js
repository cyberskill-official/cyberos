window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-080"]="<h2>§1</h2>\n<ol><li><code>apps/console/web/</code> (the tracked vite output the VPS serves via git pull) <strong>MUST</strong> be rebuilt so <code>version.json</code> carries the current VERSION - done: <code>{\"build\":\"20260712204020\",\"version\":\"1.0.0\"}</code>; the new service-worker cache id makes connected clients pick up the deploy via the existing update banner (useUpdateCheck polls version.json).</li><li><code>check-version-sync.sh</code> <strong>MUST</strong> cover the served bundle as artifact 7: <code>apps/console/web/version.json .version == VERSION</code>, failing with the exact rebuild command. This makes bundle lag loud at every pre-commit, payload-gate, version bump, and release run that already calls the gate (TASK-IMP-068 wiring).</li><li>The check reads the REPO copy, not the payload - the served bundle is a repo-side channel like the status page, and it rides the same gate chain rather than a new one.</li></ol>\n<p><em>Lean profile: one gate check + one regenerated bundle; defect, fix, and both gate directions machine-verified in-session.</em></p>\n<h2>§2 — Why this shape</h2>\n<p>The badge pipeline itself was already correct (VersionBadge -&gt; /version.json -&gt; stamp-sw.mjs -&gt; root VERSION); what was missing is anything that forces the TRACKED output to be regenerated after a version change. The gate converts \"someone must remember to rebuild\" into \"the next commit fails with the command to run\" - same posture as every other artifact in the sync check. A CI leg that rebuilds and ships the bundle (docs-job style, via ship.sh) would remove even that manual step; recorded in §9 as the structural follow-up rather than done here, because it changes the serving model (git-pull-tracked dir) that deploy.sh's caddy bind documents.</p>\n<h2>§5 (run 2026-07-13)</h2>\n<ul><li>Rebuild: <code>npm run build</code> green (tsc + vite 142ms + stamp-sw); version.json 0.1.0 -&gt; 1.0.0, sw cache <code>cyberos-shell-20260712204020</code>; js asset hashes unchanged. PASS</li><li>Positive gate: <code>sync OK 1.0.0 across 7 artifacts</code>. PASS</li><li>Negative gate: version.json temporarily set to 0.9.9 -&gt; <code>DRIFT ... 0.9.9 != 1.0.0 (stale served bundle - rebuild: cd apps/web &amp;&amp; npm run build)</code>, exit 10; file restored. PASS</li><li>Sandbox note: the build needed <code>@rolldown/binding-linux-arm64-gnu</code> added to node_modules (mac-installed tree, linux sandbox) - <code>--no-save</code>, lock/manifest untouched.</li><li>Testing pass 2026-07-13 (post gate-1 \"approve all\"): positive gate re-run green (<code>sync OK 1.0.0 across 7 artifacts</code>); negative gate re-run green (version.json set to 0.9.9 -&gt; <code>DRIFT ... 0.9.9 != 1.0.0 (stale served bundle - rebuild: cd apps/web &amp;&amp; npm run build)</code>, exit 10, file restored, <code>git status --short</code> on the file clean after restore).</li></ul>\n<h2>§9</h2>\n<ul><li>Structural follow-up: a CI leg that rebuilds apps/web on <code>apps/web/**</code>/VERSION changes and ships it like the docs job (ship.sh pattern), retiring the tracked-output model. Decide together with the deploy.sh caddy-bind implications; not blocking 1.0.0.</li><li>The old sw cache id lingers in clients until their next poll; the update banner handles it (existing machinery, no change).</li></ul>\n<h2>§10</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Recovery</th></tr></thead><tbody><tr><td>version bump without web rebuild</td><td>gate check 7 fails the next commit/CI run with the command</td><td>run the printed rebuild, commit</td></tr><tr><td>version.json deleted/mangled</td><td>check 7 explicit missing/unreadable branch</td><td>rebuild</td></tr><tr><td>rebuild forgotten AND gate bypassed</td><td>impossible via the normal chain (pre-commit + payload-gate + version.yml + release all call the gate)</td><td>any one path fires</td></tr><tr><td>badge shows stale after deploy</td><td>sw cache id changed -&gt; update banner prompts reload</td><td>client reload</td></tr></tbody></table>\n<p><em>End of TASK-IMP-080.</em></p>";
