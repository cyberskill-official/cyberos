window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A shared Rust crate <code>cyberos-obs-sdk</code> <strong>MUST</strong> expose RED-metric helpers; every CyberOS service <strong>MUST</strong> emit these metrics on every request. Each component:</p>\n<ol><li><strong>MUST</strong> expose <code>red::record_request(service, route, tenant_id, status, duration_ms, extra_labels)</code> API. The first 5 params are mandatory; <code>extra_labels: &amp;[(&amp;str, String)]</code> is optional per-service customisation.</li><li><strong>MUST</strong> emit 3 metrics per request:</li></ol>\n<ul><li><code>cyberos_requests_total{service, route, tenant_id, status_class}</code> (counter; increments by 1 per call).</li><li><code>cyberos_errors_total{service, route, tenant_id, error_class}</code> (counter; increments only on errors; <code>error_class</code> ∈ <code>client_error | server_error</code>).</li><li><code>cyberos_duration_ms{service, route, tenant_id}</code> (histogram; bucket boundaries in §1 #4).</li></ul>\n<ol><li><strong>MUST</strong> label by <code>status_class</code> (string: <code>\"2xx\" | \"3xx\" | \"4xx\" | \"5xx\" | \"other\"</code>) NOT raw status code. Per DEC-152: raw status (200, 201, 204, ...) would multiply cardinality by ~30; the class-level granularity is sufficient for SLO calculation.</li><li><strong>MUST</strong> use standardised histogram bucket boundaries: <code>[1, 2.5, 5, 10, 25, 50, 100, 250, 500, 1000, 2500, 5000, 10000]</code> ms. Per DEC-153: cross-service p95 aggregation requires identical bucket boundaries (Prometheus's <code>histogram_quantile</code> won't merge differently-bucketed histograms correctly).</li><li><strong>MUST</strong> be called from every HTTP/RPC handler in every CyberOS service. The <code>#[red_instrument(service = \"...\", route = \"...\")]</code> proc macro on axum handlers is the standard application path; manual calls are reserved for non-HTTP code paths (background jobs, scheduled tasks).</li><li><strong>MUST</strong> add ≤ 100ns overhead per call (atomic counter increment + HDR-histogram bucket increment + Vec push for label array). The OTel SDK in async mode is built for this. Benchmarked via <code>red_overhead_test.rs</code>.</li><li><strong>MUST</strong> integrate via OTel SDK (NOT direct prometheus crate). Same emission path as TASK-AI-022 traces; same OTLP exporter; same backend.</li><li><strong>MUST</strong> support custom dimensions per service via <code>extra_labels</code>. AI Gateway adds <code>model_alias</code>; CHAT adds <code>workspace_id</code>; AUTH adds <code>endpoint_type</code> (admin vs token-issue). Custom labels MUST pass cardinality guard (§1 #9).</li><li><strong>MUST</strong> enforce a cardinality guard: refuses to register a metric series with &gt; 1000 unique label combinations per service. Cardinality explosion is the most common observability failure mode (Prometheus stores one time-series per label combo; thousands of series per metric balloons storage). The guard logs <code>cardinality_overflow_blocked</code> and emits <code>obs_sdk_cardinality_blocked_total{service, metric}</code> counter; sev-2 alarm.</li><li><strong>MUST</strong> init OTel SDK at service boot via <code>obs_sdk::init(service_name, version)</code>. The init function configures the OTLP exporter to point at the OBS collector (TASK-OBS-001) using the per-service bearer token from env.</li><li><strong>MUST</strong> be CI-gated by <code>instrument_completeness_test</code>: an integration test in <code>cyberos-obs-sdk/tests/</code> AST-walks every CyberOS service's handler files and asserts each axum-handler-shaped function has <code>#[red_instrument]</code>. Missing macro → CI failure with file:line of the offending handler.</li><li><strong>SHOULD</strong> emit OTel metrics about itself:</li></ol>\n<ul><li><code>obs_sdk_record_calls_total{service}</code> (counter; total record_request calls).</li><li><code>obs_sdk_record_latency_ns</code> (histogram; per-call overhead).</li><li><code>obs_sdk_cardinality_blocked_total{service, metric}</code> (counter; sev-2 alarm).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why RED specifically (DEC-150)?</strong> RED (Rate, Errors, Duration) is the standard service-level SLO trinity. Three metrics tell ops everything they need to triage: how busy am I, what's failing, how slow am I. USE (Utilization, Saturation, Errors) is the alternative for resource-level monitoring; RED is the right level for service-oriented systems.</p>\n<p><strong>Why a shared crate (DEC-151)?</strong> Every service emitting metrics with slightly different label sets would make cross-service queries impossible. The shared crate enforces one label vocabulary (<code>service</code>, <code>route</code>, <code>tenant_id</code>, <code>status_class</code>, <code>error_class</code>); operators learn one query language.</p>\n<p><strong>Why status_class not raw status (DEC-152)?</strong> Cardinality. Each unique label combo = one Prometheus time-series. Raw status × routes × tenants = N × M × T series; status_class × routes × tenants = 4 × M × T (4 = 2xx/3xx/4xx/5xx). 30× cardinality reduction with negligible operational cost (operators rarely care about 201 vs 200; they care about 2xx vs 4xx).</p>\n<p><strong>Why standardised histogram buckets (DEC-153)?</strong> Cross-service aggregation requires identical bucket boundaries. <code>histogram_quantile(0.95, sum by (service) (rate(cyberos_duration_ms_bucket[5m])))</code> only works if every service's histogram uses the same buckets. The 13 standard buckets cover 1ms-10s with reasonable resolution at each magnitude.</p>\n<p><strong>Why the macro form (#[red_instrument]) (§1 #5)?</strong> Manual calls scatter <code>record_request</code> lines across the codebase, easy to forget. The proc macro wraps the handler function transparently — no per-handler boilerplate. The CI completeness test (§1 #11) ensures the macro is applied; together they enforce instrumentation as a structural property.</p>\n<p><strong>Why ≤100ns overhead (§1 #6)?</strong> Per-request metric overhead must be invisible. At 1000 req/s, 100ns × 1000 = 100µs/sec — 0.01% CPU. Above 100ns, the overhead becomes measurable; above 1µs, observably slows the service. The OTel Rust SDK is built for this overhead profile.</p>\n<p><strong>Why cardinality guard (§1 #9)?</strong> The most common observability failure: a developer adds <code>user_id</code> as a metric label, intending to track per-user latency. With 10K users + 50 routes × 50 tenants = 25M time-series. Prometheus storage explodes; queries slow to seconds. The cardinality guard refuses to register such metrics — operators see the rejection at deploy time, not after Prometheus crashes.</p>\n<p><strong>Why CI lint for instrument completeness (§1 #11)?</strong> Without enforcement, instrumentation discipline drifts. New handlers ship without <code>#[red_instrument]</code>; the metric coverage gradually shrinks. The CI lint catches uninstrumented handlers at PR time — a code-review surface that's unambiguous.</p>\n<p><strong>Why custom dimensions per service (§1 #8)?</strong> Some services have natural per-request dimensions worth tracking. AI Gateway: <code>model_alias</code> (chat.fast vs chat.smart). CHAT: <code>workspace_id</code>. AUTH: <code>endpoint_type</code>. The shared label vocabulary covers the basics; per-service custom dims add detail without forcing every service to carry every dimension.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/src/red.rs\nuse opentelemetry::{global, KeyValue, metrics::{Counter, Histogram, Meter}};\nuse std::sync::OnceLock;\n\nconst HISTOGRAM_BUCKETS_MS: &amp;[f64] = &amp;[1.0, 2.5, 5.0, 10.0, 25.0, 50.0, 100.0, 250.0, 500.0, 1000.0, 2500.0, 5000.0, 10000.0];\n\nstatic REQUESTS: OnceLock&lt;Counter&lt;u64&gt;&gt; = OnceLock::new();\nstatic ERRORS: OnceLock&lt;Counter&lt;u64&gt;&gt; = OnceLock::new();\nstatic DURATION: OnceLock&lt;Histogram&lt;f64&gt;&gt; = OnceLock::new();\n\npub fn init(service: &amp;str, version: &amp;str) {\n    let meter = global::meter(\"cyberos\");\n    REQUESTS.set(meter.u64_counter(\"cyberos_requests_total\").build()).unwrap_or(());\n    ERRORS.set(meter.u64_counter(\"cyberos_errors_total\").build()).unwrap_or(());\n    DURATION.set(meter.f64_histogram(\"cyberos_duration_ms\")\n        .with_unit(\"ms\")\n        .with_boundaries(HISTOGRAM_BUCKETS_MS.to_vec())\n        .build()).unwrap_or(());\n    cardinality_guard::init(service);\n}\n\npub fn record_request(\n    service: &amp;str, route: &amp;str, tenant_id: &amp;str,\n    status: u16, duration_ms: u32, extra_labels: &amp;[(&amp;str, String)],\n) {\n    let status_class = status_class(status);\n    let mut labels = vec![\n        KeyValue::new(\"service\", service.to_string()),\n        KeyValue::new(\"route\", route.to_string()),\n        KeyValue::new(\"tenant_id\", tenant_id.to_string()),\n        KeyValue::new(\"status_class\", status_class.to_string()),\n    ];\n    for (k, v) in extra_labels {\n        labels.push(KeyValue::new(k.to_string(), v.clone()));\n    }\n    if !cardinality_guard::check(service, \"cyberos_requests_total\", &amp;labels) {\n        return;   // refused — would overflow cardinality\n    }\n    REQUESTS.get().unwrap().add(1, &amp;labels);\n    DURATION.get().unwrap().record(duration_ms as f64, &amp;labels);\n    if status &gt;= 400 {\n        let mut err_labels = labels.clone();\n        let error_class = if status &gt;= 500 { \"server_error\" } else { \"client_error\" };\n        err_labels.push(KeyValue::new(\"error_class\", error_class.to_string()));\n        ERRORS.get().unwrap().add(1, &amp;err_labels);\n    }\n}\n\nfn status_class(status: u16) -&gt; &amp;'static str {\n    match status {\n        200..=299 =&gt; \"2xx\", 300..=399 =&gt; \"3xx\",\n        400..=499 =&gt; \"4xx\", 500..=599 =&gt; \"5xx\",\n        _ =&gt; \"other\",\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/src/macros.rs\n#[proc_macro_attribute]\npub fn red_instrument(args: TokenStream, item: TokenStream) -&gt; TokenStream {\n    let attrs: RedInstrumentArgs = parse_macro_input!(args);   // service = \"...\", route = \"...\"\n    let func = parse_macro_input!(item as ItemFn);\n    quote! {\n        async fn #func_name(#args) -&gt; #return_type {\n            let __t0 = std::time::Instant::now();\n            let __tenant_id = extract_tenant_id_from_request(&amp;request);   // helper or fallback\n            let __result = (#original_body)(request).await;\n            let __status = match &amp;__result {\n                Ok(resp) =&gt; resp.status().as_u16(),\n                Err(e)   =&gt; 500,\n            };\n            cyberos_obs_sdk::red::record_request(\n                #service, #route, &amp;__tenant_id, __status,\n                __t0.elapsed().as_millis() as u32, &amp;[],\n            );\n            __result\n        }\n    }.into()\n}</code></pre>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/src/cardinality_guard.rs\nuse std::collections::HashMap;\nuse std::sync::Mutex;\n\nconst MAX_CARDINALITY_PER_METRIC: usize = 1000;\n\nstatic SEEN: Mutex&lt;HashMap&lt;String, HashSet&lt;String&gt;&gt;&gt; = Mutex::new(HashMap::new());\n\npub fn check(service: &amp;str, metric: &amp;str, labels: &amp;[KeyValue]) -&gt; bool {\n    let key = format!(\"{service}:{metric}\");\n    let label_combo = labels.iter().map(|kv| format!(\"{}={}\", kv.key, kv.value.as_str())).collect::&lt;Vec&lt;_&gt;&gt;().join(\",\");\n    let mut seen = SEEN.lock().unwrap();\n    let entry = seen.entry(key.clone()).or_default();\n    if entry.contains(&amp;label_combo) { return true; }\n    if entry.len() &gt;= MAX_CARDINALITY_PER_METRIC {\n        eprintln!(\"cardinality_overflow_blocked: {service}/{metric} at {} unique combos\", entry.len());\n        cardinality_blocked_counter().add(1, &amp;[KeyValue::new(\"service\", service.to_string()), KeyValue::new(\"metric\", metric.to_string())]);\n        return false;\n    }\n    entry.insert(label_combo);\n    true\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>100 chat calls produce 100 <code>cyberos_requests_total{service=ai-gateway,...}</code> increments</strong>.</li><li><strong>5xx errors produce both requests AND errors increments</strong> — <code>errors_total{error_class=\"server_error\"}</code> increments by 1.</li><li><strong>4xx errors produce errors with error_class=client_error</strong>.</li><li><strong>Histogram bucket fills correct</strong> — call with duration 7ms increments the <code>le=\"10\"</code> bucket.</li><li><strong>Per-tenant labels visible</strong> — PromQL <code>cyberos_duration_ms{tenant_id=\"org:cyberskill\"}</code> returns data.</li><li><strong>Status class derivation correct</strong> — 200/201/204 all → <code>status_class=2xx</code>.</li><li><strong>Macro applied to all handlers</strong> — <code>instrument_completeness_test</code> AST-walks every CyberOS service's handlers; asserts every axum-handler-shaped fn has <code>#[red_instrument]</code>.</li><li><strong>Overhead &lt; 100ns per call</strong> — <code>red_overhead_test</code> benchmark assertion.</li><li><strong>Cardinality guard refuses &gt; 1000 combos</strong> — synthetic test inserts 1001 unique combos; 1001st call returns false; <code>obs_sdk_cardinality_blocked_total</code> increments; sev-2 alarm.</li><li><strong>Custom dimensions work</strong> — <code>extra_labels: &amp;[(\"model_alias\", \"chat.smart\".into())]</code> appears as label in emitted metric.</li><li><strong>Standardised buckets across services</strong> — query <code>cyberos_duration_ms_bucket{service=ai-gateway} or cyberos_duration_ms_bucket{service=auth-service}</code> returns same <code>le</code> values.</li><li><strong>OTel SDK init successful</strong> — boot logs <code>obs_sdk_initialized service=ai-gateway version=0.4.1</code>.</li><li><strong>Unrecognised status returns \"other\" class</strong> — status=999 → <code>status_class=other</code>.</li><li><strong>Concurrent record_request thread-safe</strong> — 100 threads × 1000 calls; final counter = 100,000.</li><li><strong>Macro preserves handler signature</strong> — handler's params + return type unchanged after macro expansion.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/tests/red_test.rs\n#[test]\nfn record_emits_counter_and_histogram() {\n    let exporter = test_helper::test_exporter();\n    obs_sdk::red::init(\"test\", \"0.0.0\");\n\n    obs_sdk::red::record_request(\"test\", \"/foo\", \"tenant_a\", 200, 42, &amp;[]);\n\n    let m = exporter.find_counter(\"cyberos_requests_total\")\n        .with_labels(&amp;[(\"service\", \"test\"), (\"route\", \"/foo\"), (\"tenant_id\", \"tenant_a\"), (\"status_class\", \"2xx\")])\n        .unwrap();\n    assert_eq!(m.value(), 1);\n\n    let h = exporter.find_histogram(\"cyberos_duration_ms\")\n        .with_labels(&amp;[(\"service\", \"test\"), (\"route\", \"/foo\"), (\"tenant_id\", \"tenant_a\")])\n        .unwrap();\n    assert!(h.bucket_count_at(50.0) &gt;= 1);\n}\n\n#[test]\nfn server_error_emits_error_counter() {\n    obs_sdk::red::record_request(\"test\", \"/foo\", \"t\", 503, 100, &amp;[]);\n    let err = test_helper::find_counter(\"cyberos_errors_total\")\n        .with_labels(&amp;[(\"error_class\", \"server_error\")]).unwrap();\n    assert_eq!(err.value(), 1);\n}\n\n#[test]\nfn client_error_emits_error_counter_with_client_class() {\n    obs_sdk::red::record_request(\"test\", \"/foo\", \"t\", 404, 50, &amp;[]);\n    let err = test_helper::find_counter(\"cyberos_errors_total\")\n        .with_labels(&amp;[(\"error_class\", \"client_error\")]).unwrap();\n    assert_eq!(err.value(), 1);\n}\n\n#[test]\nfn status_class_derivation() {\n    for (status, expected) in &amp;[(200, \"2xx\"), (201, \"2xx\"), (301, \"3xx\"), (404, \"4xx\"), (503, \"5xx\"), (999, \"other\")] {\n        assert_eq!(red::status_class(*status), *expected);\n    }\n}\n\n#[test]\nfn cardinality_guard_blocks_at_1001() {\n    obs_sdk::cardinality_guard::reset();\n    for i in 0..1000 {\n        obs_sdk::red::record_request(\"test\", &amp;format!(\"/route_{i}\"), \"t\", 200, 1, &amp;[]);\n    }\n    let allowed = obs_sdk::cardinality_guard::check(\"test\", \"cyberos_requests_total\", &amp;[KeyValue::new(\"route\", \"/route_1000\".to_string())]);\n    assert!(!allowed);\n    let blocked: u64 = test_helper::counter_value(\"obs_sdk_cardinality_blocked_total\", &amp;[(\"service\", \"test\"), (\"metric\", \"cyberos_requests_total\")]);\n    assert!(blocked &gt; 0);\n}\n\n#[bench]\nfn red_overhead_under_100ns(b: &amp;mut Bencher) {\n    obs_sdk::red::init(\"test\", \"0.0.0\");\n    b.iter(|| obs_sdk::red::record_request(\"test\", \"/foo\", \"t\", 200, 1, &amp;[]));\n    // Cargo bench output: each iter measured; assert &lt; 100ns\n}\n\n#[tokio::test]\nasync fn concurrent_record_thread_safe() {\n    let mut joinset = tokio::task::JoinSet::new();\n    for _ in 0..100 {\n        joinset.spawn(async {\n            for _ in 0..1000 {\n                obs_sdk::red::record_request(\"test\", \"/c\", \"t\", 200, 1, &amp;[]);\n            }\n        });\n    }\n    while let Some(_) = joinset.join_next().await {}\n    let m = test_helper::counter_value(\"cyberos_requests_total\",\n        &amp;[(\"service\", \"test\"), (\"route\", \"/c\"), (\"status_class\", \"2xx\")]);\n    assert_eq!(m, 100_000);\n}</code></pre>\n<pre><code class=\"language-rust\">// crates/cyberos-obs-sdk/tests/instrument_completeness_test.rs\n#[test]\nfn every_axum_handler_has_red_instrument() {\n    use std::path::Path;\n    use syn::visit::Visit;\n    let services = [\"services/ai-gateway\", \"services/auth\", \"services/chat\", \"services/memory\"];\n    let mut missing = vec![];\n    for service in services {\n        for entry in walkdir::WalkDir::new(format!(\"{service}/src/handlers\")) {\n            let entry = entry.unwrap();\n            if !entry.path().extension().map(|e| e == \"rs\").unwrap_or(false) { continue; }\n            let src = std::fs::read_to_string(entry.path()).unwrap();\n            let file: syn::File = syn::parse_str(&amp;src).unwrap();\n            for item in &amp;file.items {\n                if let syn::Item::Fn(f) = item {\n                    if is_axum_handler(f) {\n                        let has_macro = f.attrs.iter().any(|a| a.path().is_ident(\"red_instrument\"));\n                        if !has_macro {\n                            missing.push(format!(\"{}::{} (line {})\", entry.path().display(), f.sig.ident, f.sig.span().start().line));\n                        }\n                    }\n                }\n            }\n        }\n    }\n    assert!(missing.is_empty(), \"axum handlers missing #[red_instrument]:\\n  {}\", missing.join(\"\\n  \"));\n}</code></pre>\n<pre><code class=\"language-bash\">cargo test -p cyberos-obs-sdk\ncargo bench -p cyberos-obs-sdk red_overhead</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/handlers/chat.rs (after macro applied)\n#[red_instrument(service = \"ai-gateway\", route = \"/v1/chat/completions\")]\nasync fn handle_chat(req: ChatCompleteRequest, headers: HeaderMap) -&gt; Result&lt;HttpResponse, ApiError&gt; {\n    // existing logic unchanged\n}\n\n// Custom dimension example\n#[red_instrument(service = \"ai-gateway\", route = \"/v1/chat/completions\",\n                 extra_labels = \"&amp;[(\\\"model_alias\\\", req.model_alias.clone())]\")]\nasync fn handle_chat(req: ChatCompleteRequest, ...) -&gt; ... { ... }</code></pre>\n<p>Boot:</p>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/main.rs\nfn main() {\n    cyberos_obs_sdk::red::init(\"ai-gateway\", env!(\"CARGO_PKG_VERSION\"));\n    // ... rest of boot ...\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-OBS-001</strong> — OTel collector live (otherwise metrics queue locally).</li><li><strong>TASK-OBS-002 (downstream)</strong> — Tenant proxy depends on tenant_id label being on every metric.</li><li><strong>TASK-OBS-007 (downstream)</strong> — Alertmanager rules query RED metrics.</li><li>Crates: <code>opentelemetry@0.21</code>, <code>opentelemetry-otlp</code>, <code>once_cell</code>, <code>proc-macro2</code>, <code>syn@2</code>, <code>quote</code>, <code>walkdir@2</code> (test-only), <code>criterion@0.5</code> (bench).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>PromQL queries operators run</h3>\n<pre><code class=\"language-promql\"># Error rate by service\nsum(rate(cyberos_errors_total[5m])) by (service)\n\n# p95 latency per route\nhistogram_quantile(0.95, sum(rate(cyberos_duration_ms_bucket[5m])) by (route, le))\n\n# 5xx rate per tenant\nsum(rate(cyberos_errors_total{error_class=\"server_error\"}[5m])) by (tenant_id)\n\n# RED triplet for ai-gateway\nrate(cyberos_requests_total{service=\"ai-gateway\"}[5m])\nrate(cyberos_errors_total{service=\"ai-gateway\"}[5m])\nhistogram_quantile(0.95, rate(cyberos_duration_ms_bucket{service=\"ai-gateway\"}[5m]))</code></pre>\n<h3>Cardinality-guard alert</h3>\n<pre><code class=\"language-text\">WARN  service=ai-gateway metric=cyberos_requests_total\n      cardinality_overflow_blocked at 1000 unique label combos\nsev-2 obs_sdk_cardinality_blocked_total{service=ai-gateway, metric=cyberos_requests_total} incremented</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Per-handler SLO targets in code annotations — slice 4+.</li><li>Synthetic-traffic generator for SLO measurement under no-load — slice 5+.</li><li>Streaming-response duration measurement (start vs first byte vs last byte) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>OTel SDK panic on init</td><td>Process exit at boot</td><td>Service refuses to start</td><td>Operator investigates SDK version</td></tr><tr><td>OTel SDK runtime error</td><td>Catch-all in record</td><td>Log + continue (no-op)</td><td>Investigate SDK</td></tr><tr><td>High cardinality (&gt;1000 combos)</td><td>cardinality_guard.check</td><td>Refuse to register; sev-2 alarm</td><td>Engineer fixes label set</td></tr><tr><td>Service forgets to instrument</td><td><code>instrument_completeness_test</code></td><td>PR blocked</td><td>Engineer adds macro</td></tr><tr><td>Service forgets <code>obs_sdk::init</code> at boot</td><td><code>record_request</code> panic on <code>unwrap()</code></td><td>Boot fails</td><td>Operator adds init call</td></tr><tr><td>Standardised buckets violated (operator changes per service)</td><td>cross-service aggregation breaks</td><td>Detection in dashboards</td><td>Lint enforcement</td></tr><tr><td>Macro doesn't preserve handler signature</td><td>Compile error</td><td>PR blocked</td><td>Fix macro impl</td></tr><tr><td>Concurrent record race</td><td>Atomic counters</td><td>No race</td><td>By design</td></tr><tr><td>Tenant_id label missing</td><td>Macro extracts; falls back to \"unknown\"</td><td>Sev-3 alarm if \"unknown\" &gt; 1%</td><td>Operator investigates extraction</td></tr><tr><td>Histogram bucket overflow (latency &gt; 10s)</td><td><code>+Inf</code> bucket increments</td><td>Sev-3 alarm; investigate slow handler</td><td>By design</td></tr><tr><td>Custom dimension explodes cardinality</td><td>cardinality_guard catches</td><td>Refuse to register</td><td>Engineer fixes custom-label set</td></tr><tr><td>Metric not in OTel collector pipeline</td><td>Collector config missing exporter</td><td>Metrics not visible in Prometheus</td><td>Operator updates collector config</td></tr><tr><td>Status code 0 (timeout pre-response)</td><td>status_class derives \"other\"</td><td>Visible as \"other\"</td><td>By design</td></tr><tr><td>Macro syntax breakage on edge-case handler</td><td>Compile error</td><td>PR blocked</td><td>Fix macro impl</td></tr><tr><td>Disabled in test (no metrics emitted)</td><td>Tests use no-op exporter</td><td>N/A</td><td>By design</td></tr><tr><td>OTel collector down → metrics queue</td><td>OTel SDK buffers</td><td>Local buffer; eventual delivery</td><td>TASK-OBS-001 file_storage</td></tr><tr><td>Cardinality 999 → adds 1 → 1000 → at limit</td><td>Allow that 1000th combo</td><td>By design (the 1001st is blocked)</td><td>By design</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The macro form (<code>#[red_instrument]</code>) keeps call sites clean. Manual calls are reserved for non-HTTP code paths (background jobs).</li><li>Histogram buckets MUST match across services for cross-service aggregation. The 13 standard boundaries cover 1ms-10s with reasonable resolution at each magnitude.</li><li>Cardinality guard at 1000 unique combos is conservative. Slice 1 services typically have 5-10 routes × 50 tenants × 4 status classes = 2000 combos. Above 1000 per metric, operators investigate; the limit can be raised with explicit task amendment.</li><li>The <code>instrument_completeness_test</code> is the structural enforcement of \"every handler is instrumented.\" Without it, drift is inevitable.</li><li>Status_class labels (2xx/3xx/4xx/5xx/other) trade fine-grained visibility for cardinality. Operators rarely care about 201 vs 200; they care about 2xx vs 4xx. The trade-off is worth it.</li><li>The <code>extra_labels</code> parameter is the per-service flexibility surface. AI Gateway adds <code>model_alias</code>; CHAT adds <code>workspace_id</code>. Each service's PR includes the rationale for new labels.</li><li>Overhead &lt; 100ns is achievable because OTel SDK uses lock-free atomic counters + HDR-style histogram bucket increments. The benchmark catches regressions.</li><li>The OTel SDK init at boot configures the OTLP exporter. If the OBS collector is down at boot, OTel buffers locally; TASK-OBS-001's file_storage extension persists across restart.</li><li>Future SLO annotations (<code>#[red_instrument(slo_p95 = \"100ms\")]</code>) would let the macro auto-generate Prometheus rules. Slice 4+ work.</li></ul>\n<hr>\n<p><em>End of TASK-OBS-003. Status: draft (10/10 target).</em></p>";
