window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-003"]="<h1>TASK-OBS-003: RED metrics via cyberos-obs-sdk</h1>\n<h2>Summary</h2>\n<p>Per-service RED (rate / errors / duration) metrics ship from <code>services/shared/cyberos-obs-sdk/</code>: <code>red::record_request</code> with <code>status_class</code> labels (DEC-152), thirteen standard histogram buckets (DEC-153), a 1000-series cardinality guard, and axum middleware (<code>red_mw</code> / <code>RedState</code> / <code>TenantCtx</code>) in <code>layer.rs</code> — <strong>not</strong> a <code>#[red_instrument]</code> proc-macro. Wired into <strong>auth</strong>, <strong>memory</strong>, and <strong>ai-gateway</strong> HTTP stacks; chat is not instrumented here (pinned image, no src). OTLP export is configured at service boot via <code>init()</code> when <code>OBS_OTLP_ENDPOINT</code> is set. Live end-to-end validation against a running TASK-OBS-001 collector stack remains a follow-on wiring step.</p>\n<h2>Problem</h2>\n<p>The original engineering-spec claimed <code>crates/cyberos-obs-sdk/</code>, a <code>macros.rs</code> proc-macro, an AST <code>instrument_completeness_test</code>, chat-service wiring, and standalone integration test files — none match the as-built tree. The body used <code>## §N</code> grammar (FM-004). The live crate lives under <code>services/shared/cyberos-obs-sdk/</code> with axum middleware (ADR-OBS-003-001) and inline module tests instead of separate <code>tests/*.rs</code> files.</p>\n<h2>Proposed Solution</h2>\n<p>Adopt the as-built layout:</p>\n<ul><li><code>red.rs</code> — <code>record_request</code>, <code>status_class</code>, <code>error_class</code>, <code>HISTOGRAM_BUCKETS_MS</code> (13 boundaries), <code>init()</code> installing OTel instruments</li><li><code>cardinality_guard.rs</code> — per-service/per-metric 1000-combo budget with idempotent re-seen combos</li><li><code>layer.rs</code> — <code>red_mw</code> axum middleware; handlers supply <code>TenantCtx</code> via response extensions</li><li>Service wiring: <code>auth</code> (TenantCtx from JWT claims), <code>memory</code> (x-tenant-id header), <code>ai-gateway</code> (<code>server/mod.rs</code> layer + <code>cyberos_gateway.rs</code> init)</li><li>AWH: full <code>cargo test -p cyberos-obs-sdk</code> plus held-out <code>acceptance-obs-sdk-cardinality</code></li></ul>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Resume the old engineering-spec as-is.</strong> Rejected: FM-004 blocks re-entry; <code>crates/</code> path and macro story lie.</li><li><strong>Per-handler <code>#[red_instrument]</code> proc-macro.</strong> Rejected for slice-1: axum middleware gives structural route coverage (ADR-OBS-003-001).</li><li><strong>Instrument chat in this slice.</strong> Rejected: chat is a pinned image without Rust sources in this repo.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: inline tests prove status_class derivation, standard buckets, cardinality guard block at 1001st combo, and middleware transparency; AWH <code>acceptance-obs-sdk-cardinality</code> stays green.</li><li>Guardrail: auth, memory, and ai-gateway each install <code>RedState</code> + <code>red_mw</code> at router build time.</li></ul>\n<h2>Scope</h2>\n<p>In scope (as-built):</p>\n<ul><li><code>services/shared/cyberos-obs-sdk/src/{red,layer,cardinality_guard}.rs</code> + <code>lib.rs</code></li><li><code>init()</code> + OTLP meter provider wiring at service boot (env-gated)</li><li>Axum <code>red_mw</code> middleware wired in auth, memory, ai-gateway</li><li>Inline <code>#[test]</code> / <code>#[tokio::test]</code> modules in red.rs, cardinality_guard.rs, layer.rs</li><li>AWH tasks <code>obs-sdk-rust</code> and <code>acceptance-obs-sdk-cardinality</code></li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li><code>crates/cyberos-obs-sdk/</code> path or <code>src/macros.rs</code> proc-macro <code>#[red_instrument]</code></li><li><code>tests/red_test.rs</code>, <code>tests/macro_test.rs</code>, <code>tests/instrument_completeness_test.rs</code> from the old spec</li><li>Chat-service instrumentation (no Rust sources in repo)</li><li>Live end-to-end OTLP validation against deploy/obs collector (requires TASK-OBS-001 collector process slice + env wiring)</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: [TASK-OBS-001]</code>. Soft: TASK-OBS-002 tenant proxy expects <code>tenant_id</code> on every metric series; TASK-OBS-007 alert rules query RED metrics.</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>red::record_request</code> MUST emit <code>cyberos_requests_total</code>, <code>cyberos_errors_total</code> (4xx/5xx only), and <code>cyberos_duration_ms</code> labelled by <code>service</code>, <code>route</code>, <code>tenant_id</code>, and <code>status_class</code> (not raw status).</li><li>1.2 <code>HISTOGRAM_BUCKETS_MS</code> MUST be exactly thirteen boundaries <code>[1, 2.5, 5, 10, 25, 50, 100, 250, 500, 1000, 2500, 5000, 10000]</code> ms (DEC-153).</li><li>1.3 <code>cardinality_guard::check</code> MUST refuse the 1001st distinct label combo per service/metric and remain idempotent for already-seen combos.</li><li>1.4 <code>layer::red_mw</code> MUST record RED metrics per HTTP request without changing the handler response; <code>TenantCtx</code> supplies tenant_id when present.</li><li>1.5 <code>init(service, version)</code> MUST install OTel counters/histograms on the global meter; OTLP export is configured at service boot when <code>OBS_OTLP_ENDPOINT</code> is set.</li><li>1.6 auth, memory, and ai-gateway HTTP routers MUST install <code>RedState::new(&lt;service&gt;)</code> + <code>red_mw</code> middleware.</li><li>1.7 This adopt MUST NOT claim <code>crates/cyberos-obs-sdk</code>, proc-macro instrumentation, chat wiring, or live E2E OTLP stack validation as shipped.</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - status_class covers every band - test: <code>services/shared/cyberos-obs-sdk/src/red.rs::status_class_covers_every_band</code></li><li>[ ] AC 2 (traces_to: #1.1) - client vs server error_class split - test: <code>services/shared/cyberos-obs-sdk/src/red.rs::error_class_splits_client_and_server</code></li><li>[ ] AC 3 (traces_to: #1.2) - thirteen standard histogram buckets - test: <code>services/shared/cyberos-obs-sdk/src/red.rs::buckets_are_the_thirteen_standard_boundaries</code></li><li>[ ] AC 4 (traces_to: #1.3) - cardinality guard blocks 1001st combo - test: <code>services/shared/cyberos-obs-sdk/src/cardinality_guard.rs::blocks_past_the_budget_and_is_idempotent_under_it</code></li><li>[ ] AC 5 (traces_to: #1.3) - AWH held-out cardinality invariant - verify: <code>cd services &amp;&amp; cargo test -p cyberos-obs-sdk blocks_past_the_budget_and_is_idempotent_under_it</code></li><li>[ ] AC 6 (traces_to: #1.4) - middleware leaves handler status/body unchanged - test: <code>services/shared/cyberos-obs-sdk/src/layer.rs::middleware_is_transparent_to_the_response</code></li><li>[ ] AC 7 (traces_to: #1.5,#1.6) - auth, memory, ai-gateway call init and install red_mw - verify: <code>services/auth/src/main.rs</code>, <code>services/memory/src/main.rs</code>, <code>services/ai-gateway/src/bin/cyberos_gateway.rs</code>, <code>services/ai-gateway/src/server/mod.rs</code></li><li>[ ] AC 8 (traces_to: #1.7) - Out of scope lists crates/ path, macros, chat, live E2E; new_files cite services/shared only - verify: this spec Scope / new_files</li></ul>\n<h2>Verification</h2>\n<pre><code class=\"language-bash\">cd services &amp;&amp; cargo test -p cyberos-obs-sdk\ncd services &amp;&amp; cargo test -p cyberos-obs-sdk blocks_past_the_budget_and_is_idempotent_under_it</code></pre>\n<table><thead><tr><th>Path</th><th>Covers</th></tr></thead><tbody><tr><td><code>src/red.rs</code> inline tests</td><td>status_class, error_class, buckets, safe no-op before init</td></tr><tr><td><code>src/cardinality_guard.rs</code> inline tests</td><td>1000-combo cap, per-service isolation, label order</td></tr><tr><td><code>src/layer.rs</code> inline tests</td><td>TenantCtx defaults, middleware transparency</td></tr><tr><td><code>services/auth/src/handlers.rs</code></td><td>auth RedState + red_mw wiring</td></tr><tr><td><code>services/memory/src/main.rs</code></td><td>memory RedState + red_mw wiring</td></tr><tr><td><code>services/ai-gateway/src/server/mod.rs</code></td><td>ai-gateway RedState + red_mw wiring</td></tr></tbody></table>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) on branch <code>batch/9b-obs</code>.</li><li><strong>Scope:</strong> Re-spec/adopt against as-built <code>services/shared/cyberos-obs-sdk/</code> + auth/memory/ai-gateway middleware wiring; deferred proc-macro, chat, and live OTLP E2E ledgered Out of scope.</li><li><strong>Human review:</strong> Required at the two HITL gates (<code>entered_via: rework</code>, <code>routed_back_count: 1</code>).</li></ul>\n<hr>\n<p><em>batch/9b-obs adopt — TASK-OBS-003 re-spec against as-built cyberos-obs-sdk middleware path.</em></p>";
