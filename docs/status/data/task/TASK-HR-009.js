window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-HR-009"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The HR service <strong>MUST</strong> ship termination workflow at <code>services/hr/src/termination/</code> with GL/BL branch + CFO+CEO dual sign + cascade to ESOP/AUTH/PORTAL/PROJ, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>termination_kind</code> against closed enum per DEC-1871, <code>termination_stage</code> per DEC-1872.</li></ol>\n<ol><li><strong>MUST</strong> require dual sign-off per DEC-1873 at <code>dual_sign_gate.rs::can_execute(termination)</code>:</li></ol>\n<ul><li>Both CFO + CEO must sign.</li><li>Either rejection halts.</li><li>Same person cannot sign both roles (separation of duties).</li></ul>\n<ol><li><strong>MUST</strong> branch GL/BL per DEC-1870:</li></ol>\n<ul><li>Good Leaver (voluntary/redundancy/retirement): full ESOP vesting up to termination_date; standard severance.</li><li>Bad Leaver (misconduct/breach): ESOP forfeiture per board policy; severance per VN Art. 41 (no severance if cause).</li><li>Mutual: negotiated.</li></ul>\n<ol><li><strong>MUST</strong> cascade on executed per DEC-1874 at <code>cascade_executor.rs::execute(termination)</code>:</li></ol>\n<ul><li>TASK-ESOP-005 vesting halt + forfeiture per GL/BL</li><li>TASK-AUTH-101 deprovision (all roles revoked)</li><li>TASK-PORTAL-008 offer DSAR export to ex-member</li><li>TASK-PROJ-013 reassign open issues to manager</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0008</code>: ``<code>sql CREATE TABLE hr_terminations ( termination_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_id UUID NOT NULL UNIQUE,  -- one termination per member kind TEXT NOT NULL CHECK (kind IN ('good_leaver_voluntary','good_leaver_redundancy','good_leaver_retirement','bad_leaver_misconduct','bad_leaver_breach_contract','mutual_separation')), stage TEXT NOT NULL DEFAULT 'initiated' CHECK (stage IN ('initiated','cfo_signed','ceo_signed','executed','dispute')), termination_date DATE NOT NULL, initiated_by UUID NOT NULL, cfo_signed_by UUID, cfo_signed_at TIMESTAMPTZ, ceo_signed_by UUID, ceo_signed_at TIMESTAMPTZ, executed_at TIMESTAMPTZ, reason TEXT, dispute_reason TEXT, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); ALTER TABLE hr_terminations ENABLE ROW LEVEL SECURITY; CREATE POLICY term_rls ON hr_terminations USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON hr_terminations FROM cyberos_app; GRANT UPDATE (stage, cfo_signed_by, cfo_signed_at, ceo_signed_by, ceo_signed_at, executed_at, dispute_reason) ON hr_terminations TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST   /v1/hr/terminations                  (CHRO initiates) POST   /v1/hr/terminations/{id}/cfo-sign    (CFO) POST   /v1/hr/terminations/{id}/ceo-sign    (CEO) POST   /v1/hr/terminations/{id}/dispute     (member-self or counsel) GET    /v1/hr/terminations/{id}             (status) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1875. PII per TASK-MEMORY-111: reason+dispute_reason hashed; ids ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from initiate → sign → execute → cascade → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> execute without both signatures per DEC-1873.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow same person both CFO+CEO roles per DEC-1873.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip cascade on execute per DEC-1874.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why GL/BL branch (DEC-1870)?</strong> ESOP forfeiture + severance differ materially; legal exposure if applied wrong.</p>\n<p><strong>Why dual sign (DEC-1873)?</strong> Termination = significant financial+legal action; single-signer risk = fraud or bias.</p>\n<p><strong>Why separation of duties (DEC-1873)?</strong> Same person signing both roles = no real second check; explicit constraint.</p>\n<p><strong>Why cascade (DEC-1874)?</strong> Ex-member retaining access = security disaster; auto-revoke on execute closes the loop.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST   /v1/hr/terminations                body: {member_id, kind, termination_date, reason}\nPOST   /v1/hr/terminations/{id}/cfo-sign\nPOST   /v1/hr/terminations/{id}/ceo-sign\nPOST   /v1/hr/terminations/{id}/dispute   body: {dispute_reason}\nGET    /v1/hr/terminations/{id}</code></pre>\n<p>Sample termination:</p>\n<pre><code class=\"language-json\">{\n  \"member_id\": \"uuid\",\n  \"kind\": \"good_leaver_voluntary\",\n  \"termination_date\": \"2026-06-30\",\n  \"reason\": \"Career change to startup\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>kind enum cardinality 6</strong>. 2. <strong>stage enum cardinality 5</strong>. 3. <strong>CFO+CEO dual sign required</strong>. 4. <strong>Same person can't sign both</strong>. 5. <strong>GL → ESOP fully vested up to term_date</strong>. 6. <strong>BL → ESOP forfeiture per policy</strong>. 7. <strong>Cascade to ESOP-005 + AUTH-101 + PORTAL-008 + PROJ-013</strong>. 8. <strong>5 memory audit kinds emitted</strong>. 9. <strong>PII scrubbed (reason SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>CHRO initiate, CFO sign, CEO sign — role-gated</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>UNIQUE on member_id (one termination per)</strong>. 14. <strong>Append-only via REVOKE except 7 cols</strong>. 15. <strong>Dispute halts execution</strong>. 16. <strong>AUTH deprovision verified post-execute</strong>. 17. <strong>DSAR offer email sent</strong>. 18. <strong>Open issues reassigned to manager</strong>. 19. <strong>Cascade failure → sev-1 + rollback termination</strong>. 20. <strong>No termination during probation without specific kind</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn dual_sign_required_for_execute() {\n    let ctx = TestContext::with_initiated_termination().await;\n    ctx.cfo_sign(ctx.term_id).await;\n    let r = ctx.try_execute(ctx.term_id).await;\n    assert!(r.is_err());  // CEO not signed\n    ctx.ceo_sign(ctx.term_id).await;\n    let r2 = ctx.execute(ctx.term_id).await;\n    assert!(r2.is_ok());\n}\n\n#[tokio::test]\nasync fn same_person_both_roles_rejected() {\n    let ctx = TestContext::with_initiated_termination().await;\n    ctx.cfo_sign_as(ctx.user_a, ctx.term_id).await;\n    let r = ctx.try_ceo_sign_as(ctx.user_a, ctx.term_id).await;\n    assert!(r.is_err());\n}\n\n#[tokio::test]\nasync fn cascade_revokes_auth() {\n    let ctx = TestContext::with_dual_signed_termination().await;\n    ctx.execute(ctx.term_id).await;\n    let auth_status = ctx.fetch_member_auth(ctx.member_id).await;\n    assert_eq!(auth_status.active, false);\n}\n\n#[tokio::test]\nasync fn good_leaver_full_vesting() {\n    let ctx = TestContext::with_member_partially_vested().await;\n    let r = ctx.execute_gl_termination(ctx.member_id, \"2026-06-30\").await;\n    let esop = ctx.fetch_esop_vesting(ctx.member_id).await;\n    assert_eq!(esop.vested_at(ctx.term_date), esop.entitled_at(ctx.term_date));\n}\n\n// 5.5..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-HR-001. <strong>Cross-module:</strong> TASK-AUTH-101 (CFO/CEO/CHRO roles + deprovision), TASK-ESOP-005 (GL/BL branch), TASK-PORTAL-008 (DSAR), TASK-PROJ-013 (issue reassign), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>One signer missing</td><td>gate check</td><td>execute rejected 409</td><td>get sign</td></tr><tr><td>Same-person dual</td><td>validate</td><td>reject 403</td><td>use different signer</td></tr><tr><td>Cascade ESOP fails</td><td>sev-1</td><td>rollback termination stage</td><td>retry execute</td></tr><tr><td>Cascade AUTH fails</td><td>sev-1</td><td>termination halted</td><td>retry</td></tr><tr><td>Dispute during sign</td><td>stage→dispute</td><td>halt execute</td><td>resolve dispute</td></tr><tr><td>Termination date past</td><td>warn</td><td>allow (backdated terms exist)</td><td>inherent</td></tr><tr><td>Re-termination attempt</td><td>UNIQUE</td><td>409</td><td>inherent</td></tr><tr><td>Concurrent sign</td><td>UPDATE WHERE pending</td><td>first wins</td><td>inherent</td></tr><tr><td>BL without misconduct evidence</td><td>warn audit</td><td>still execute (HR judgment)</td><td>inherent</td></tr><tr><td>Reason missing</td><td>optional but warn</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Cascade is transactional — all-or-nothing; rollback termination if any cascade step fails.</li><li>§11.2 BL ESOP forfeiture per board policy doc (stored in tenant config).</li><li>§11.3 Severance computation: GL = per VN Art. 46 (0.5mo per year); BL = none if cause.</li><li>§11.4 memory audit body: termination_id, member_id, kind, stage; reasons SHA256.</li><li>§11.5 Dispute opens 30-day window before execute; can convert to mutual_separation if resolved.</li></ul>\n<hr>\n<p><em>End of TASK-HR-009 spec.</em></p>";
