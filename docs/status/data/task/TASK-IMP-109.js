window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-109"]="<h1>TASK-IMP-109: Standing goals - re-verify done, forever</h1>\n<h2>Summary</h2>\n<p><code>done</code> is terminal and never re-checked. TRACE-004 proves every clause had a passing test on the day it shipped and nothing looks again, so a task shipped in batch 1 could be broken today and the corpus would still show it green. At the <code>done</code> flip, enrol the task's cited tests as a standing goal, and re-run the goals on demand. Detection only: a violated goal becomes a <code>type: bug</code> task through the normal loop, never an auto-fix.</p>\n<h2>Problem</h2>\n<p>A goal verified once is an assumption with a timestamp. Every acceptance this run recorded is exactly that: true on 2026-07-17, unexamined on 2026-07-18.</p>\n<p><code>task-reconcile</code> (v2.7.0) does not close this. It measures drift when a task RE-ENTERS the workflow - it is a turnstile, not a sentinel. A <code>done</code> task that never comes back is never looked at again by anything.</p>\n<p>The corpus already demonstrates the failure mode: TASK-IMP-086 recorded acceptance evidence for rows that were not in the committed file, and nothing re-checked until an external reviewer asked. That was caught by luck and a bot. The 35 done improvement tasks and 176 done tasks overall currently rest on the same footing.</p>\n<h2>Proposed Solution</h2>\n<p>At the <code>done</code> flip, ship-tasks writes <code>docs/goals/&lt;task-id&gt;.md</code> carrying the task's §1 cited tests as its predicate - free, because TRACE-004 already collected exactly that list. A <code>verify-goals.mjs</code> runner re-runs each predicate, flips a failing goal to <code>violated</code>, and appends to a goal ledger. A violated goal produces a finding, and the fix goes through create-tasks -&gt; ship-tasks like any other bug.</p>\n<p>Only mechanically re-runnable predicates are enrolled. ACs carrying a justified <code>verify:</code> rather than <code>test:</code> cannot be re-run by a script, and the goal file MUST say so rather than pretend - an honest gap beats a fake predicate.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Re-run the whole suite instead of per-task goals. Rejected: it answers \"is the repo green\", not \"is TASK-X still true\", and it cannot name which acceptance decayed.</li><li>Auto-fix on violation. Rejected outright: the sentinel detects, the pipeline fixes. An auto-fix on a violated acceptance is the machine grading its own homework at the one moment nobody is watching.</li><li>Re-open the original task on violation. Rejected: <code>done</code> is terminal for a reason, and reviving a shipped task destroys the record of what was accepted. A new <code>type: bug</code> task preserves both.</li><li>Cron the runner. Rejected in scope: scheduling is a host decision and CyberOS is invoked, not daemonised. The runner is a command; when it runs is the operator's business.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a task flipped to <code>done</code> gains a goal file whose predicate is its §1 cited tests, and breaking one of those tests flips the goal to <code>violated</code> on the next run - suite-asserted. Baseline: <code>done</code> is never re-checked.</li><li>Guardrail: a violated goal changes no task status and writes no code - it emits a finding and nothing else.</li></ul>\n<h2>Scope</h2>\n<p>In scope: goal emission at the <code>done</code> flip, <code>verify-goals.mjs</code>, the goal ledger, the report, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any auto-fix, auto-revert, or status change on violation.</li><li>Scheduling (cron, CI wiring) - the runner is a command.</li><li>Enrolling <code>verify:</code>-only ACs as predicates (they are not mechanically re-runnable; the goal names the gap).</li><li>Retiring goals automatically - retirement is a human decision, logged.</li></ul>\n<h2>Dependencies</h2>\n<p>None mechanically. Consumes the §1 cited tests TRACE-004 already collects.</p>\n<p><strong>Serialisation note:</strong> touches <code>STATUS-REFERENCE.md</code> and <code>ship-tasks.md</code>, both shared with TASK-IMP-108 (and ship-tasks with 113, 114, 115). Parent-serialised per §11a; never concurrent.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §11 IMP-29, itself derived from the agentic-OS builder's guide (Avid, 2026-07-06) BUILD 5; verified against ship-tasks.md and task-reconcile.mjs on merged main.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 On the <code>done</code> flip, ship-tasks MUST write <code>docs/goals/&lt;task-id&gt;.md</code> carrying: the predicate (the task's §1 cited tests), <code>born</code>, <code>source</code> (the task id), <code>status: satisfied</code>, <code>last_pass</code>, and <code>on_violation</code> (default: report, never auto-fix).</li><li>1.2 The predicate MUST be derived from the §1 cited tests TRACE-004 already verified - this task MUST NOT invent a predicate the task never claimed.</li><li>1.3 ACs whose evidence is a justified <code>verify:</code> rather than a <code>test:</code> MUST NOT be enrolled as predicates, and the goal file MUST name them as not mechanically re-verifiable.</li><li>1.4 A task reaching <code>done</code> with zero mechanically re-runnable predicates MUST still get a goal file, marked <code>predicate: none</code> with the reason - the absence is the finding.</li><li>1.5 <code>verify-goals.mjs</code> MUST re-run each non-retired goal's predicate, flip failures to <code>status: violated</code>, refresh <code>last_pass</code> on success, and append one row per goal to <code>docs/goals/.ledger.tsv</code>.</li><li>1.6 <code>verify-goals.mjs</code> MUST exit non-zero when any goal is violated, and MUST print each violated goal with its task id and the failing predicate.</li><li>1.7 A violated goal MUST NOT change any task's status, MUST NOT modify code, and MUST NOT re-open the source task. The remedy is a new <code>type: bug</code> task through create-tasks.</li><li>1.8 A predicate exceeding its timeout MUST be treated as violated and named as a timeout - an unrunnable predicate is not a passing one.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2) - a <code>done</code> flip writes a goal file whose predicate is the task's §1 cited tests - test: <code>tools/install/tests/test_verify_goals.sh::t01_done_emits_goal</code></li><li>[ ] AC 2 (traces_to: #1.5, #1.6) - breaking a cited test flips the goal to violated, appends a ledger row, and exits non-zero naming the task - test: <code>tools/install/tests/test_verify_goals.sh::t02_broken_test_violates</code></li><li>[ ] AC 3 (traces_to: #1.5) - a passing goal refreshes <code>last_pass</code> and stays satisfied - test: <code>tools/install/tests/test_verify_goals.sh::t03_passing_refreshes</code></li><li>[ ] AC 4 (traces_to: #1.3, #1.4) - a <code>verify:</code>-only AC is not enrolled and is named; a task with no runnable predicate still gets a <code>predicate: none</code> goal - test: <code>tools/install/tests/test_verify_goals.sh::t04_unrunnable_named_not_faked</code></li><li>[ ] AC 5 (traces_to: #1.7) - a violated goal changes no task status and writes no code - test: <code>tools/install/tests/test_verify_goals.sh::t05_detection_only</code></li><li>[ ] AC 6 (traces_to: #1.8) - a predicate that hangs is violated and named as a timeout - test: <code>tools/install/tests/test_verify_goals.sh::t06_timeout_is_violation</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A cited test that is later legitimately renamed: the goal breaks and is violated. Correct - the acceptance now cites a test that does not exist, and that IS a finding. The remedy is an operator retiring or amending the goal, logged.</li><li>A flaky cited test: it violates intermittently and poisons the ledger. Quarantine (<code>status: retired</code>, reason recorded), never silent deletion - a goal deleted without a reason is the 086 pattern.</li><li>176 existing <code>done</code> tasks have no goal files: they are not backfilled by this task (out of scope) and the runner MUST NOT claim coverage it does not have. The report states how many <code>done</code> tasks have no goal.</li><li>A goal whose predicate needs a service, credential, or network: it fails in a clean checkout. 1.3's rule applies - if it is not cheap, deterministic, and read-only, it is not a predicate.</li><li>Predicate confinement: goals are files in the repo, and their predicates are commands. They MUST be confined and tracked-checked exactly as TASK-IMP-100's rung-5 requires - a goal file is INPUT, and a crafted predicate must never execute. This is the same defect the batch-5 review caught in <code>task-reconcile</code>, and it MUST NOT be re-introduced here.</li><li>Security-class: HIGH by construction - this task executes commands read from files. Every predicate MUST resolve under the repo root, MUST be <code>git ls-tree HEAD</code>-tracked, and MUST run without a shell where possible. This is the task's central risk and its central test.</li></ul>";
