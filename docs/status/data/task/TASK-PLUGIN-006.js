window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLUGIN-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PLUGIN module <strong>MUST</strong> emit memory audit rows for every plugin lifecycle event. The emitter at <code>services/plugin-host/src/audit/</code> writes to the memory HTTP endpoint, queues failures to a durable Postgres outbox, and retries with exponential backoff for up to 24 hours.</p>\n<ol><li><strong>MUST</strong> emit exactly 6 audit kinds per DEC-2450:</li></ol>\n<ul><li><code>plugin.installed</code> — on first successful OAuth-PKCE token exchange (per TASK-PLUGIN-005 clause 9)</li><li><code>plugin.updated</code> — on version bump install replacing a prior version of the same plugin_id</li><li><code>plugin.uninstalled</code> — on revoke + grant deletion</li><li><code>plugin.invoked</code> — on every successful tool call (one row per <code>tools/call</code>)</li><li><code>plugin.auth_refreshed</code> — on every refresh-token rotation</li><li><code>plugin.scope_denied</code> — on every scope check failure</li></ul>\n<ol><li><strong>MUST</strong> treat audit emission as REQUIRED per DEC-2451 — a tool call response MUST NOT be returned to the host until either (a) the audit row is acknowledged by memory, OR (b) the row is durably queued in the outbox. Failures in both paths MUST surface as <code>upstream_unavailable</code> error class.</li></ol>\n<ol><li><strong>MUST</strong> persist the outbox in Postgres per DEC-2452 at <code>plugin_host.audit_outbox</code> (schema in §3). Outbox survives process restart. A dedicated worker drains the outbox in FIFO order per (tenant_id, plugin_id) shard.</li></ol>\n<ol><li><strong>MUST</strong> compose an idempotency key per DEC-2453: <code>key = SHA256_HEX(kind || \"\\x1f\" || tenant_id || \"\\x1f\" || subject_id || \"\\x1f\" || trace_id || \"\\x1f\" || event_timestamp_ms)</code>. The key is sent to memory as the <code>Idempotency-Key</code> HTTP header. memory dedups: same key + same body → 200 with previously-issued seq; same key + different body → 409 (must not happen if implementation correct, but defends against bugs).</li></ol>\n<ol><li><strong>MUST</strong> implement retry per DEC-2454: exponential backoff <code>[250ms, 1s, 4s, 16s, 64s, 256s, 1024s, 4096s, ...]</code>; up to 24 hours total. After 24h, mark row <code>failed</code> and emit OTel alert (TASK-OBS-007 picks up).</li></ol>\n<ol><li><strong>MUST</strong> scrub user data from audit bodies per DEC-2455. The audit row body MUST include exactly these fields (and nothing else):</li></ol>\n<ul><li><code>plugin_id</code>, <code>plugin_version</code> — from manifest</li><li><code>tenant_id</code>, <code>subject_id</code> — from JWT</li><li><code>trace_id</code> — request trace identifier</li><li><code>outcome</code> — \"success\" | one of 4 error classes from TASK-PLUGIN-002 clause 7</li><li>For <code>plugin.invoked</code> only: <code>tool_name</code> (SEP-986 name; not the tool's input/output)</li><li>For <code>plugin.scope_denied</code> only: <code>missing_scopes</code> (array of scope strings)</li><li>For <code>plugin.installed</code> only: <code>granted_scopes</code> (array)</li><li><code>duration_ms</code> for <code>plugin.invoked</code> The body MUST NOT contain: tool input arguments, tool output content, workflow inputs, audit row contents read by the plugin, file paths or filenames the plugin touched.</li></ul>\n<ol><li><strong>MUST</strong> emit an OTel span per DEC-2456 for every audit emission, both success + failure. Span attributes: <code>cyberos.audit_kind</code>, <code>cyberos.idempotency_key</code>, <code>cyberos.outcome</code> (success / retry_queued / failed), <code>cyberos.tenant_id</code>, <code>cyberos.trace_id</code>, <code>cyberos.duration_ms</code>. Failures additionally tag <code>error.kind</code> (network / 4xx / 5xx).</li></ol>\n<ol><li><strong>MUST</strong> acquire memory's <code>actor_id</code> from the AUTH JWT's <code>sub</code> claim. Audit rows MUST be attributed to the authenticated user, not to the plugin itself. (The plugin appears in the body via <code>plugin_id</code>.)</li></ol>\n<ol><li><strong>MUST</strong> order audit rows so that <code>plugin.installed</code> is emitted BEFORE the first <code>plugin.invoked</code> for that grant. The emitter blocks the first invocation until install audit is acknowledged or queued.</li></ol>\n<ol><li><strong>MUST</strong> preserve audit emission across plugin version upgrades. When a manifest with same <code>id</code> but higher <code>version</code> installs, emit <code>plugin.updated</code> with old + new version in body; do NOT emit <code>plugin.uninstalled</code> + <code>plugin.installed</code>.</li></ol>\n<ol><li><strong>MUST</strong> retry transient failures (HTTP 5xx, connection refused, timeout) but NOT permanent failures (4xx other than 429). 4xx non-429 are programming errors and MUST be surfaced via OTel for human investigation.</li></ol>\n<ol><li><strong>MUST NOT</strong> drop audit rows on any failure mode per DEC-2451. The only sanctioned drop is after 24h retry exhaustion, and that requires an OTel alert + a failed-row in the outbox for forensics.</li></ol>\n<ol><li><strong>MUST NOT</strong> include user data in audit body per clause 6 + DEC-2455.</li></ol>\n<ol><li><strong>MUST NOT</strong> retry on 4xx non-429 — those are programming errors. Retrying a malformed request indefinitely is waste.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 6 audit kinds, locked (DEC-2450)?</strong> Each represents a distinct lifecycle moment that compliance frameworks (SOC 2 CC6.1, ISO 27001 A.9.2) require visibility into. Locking the set means downstream memory consumers (dashboards, alerting rules, exports) can rely on the schema. Future additions need successor task.</p>\n<p><strong>Why audit-then-respond, not audit-after-respond (DEC-2451, clause 2)?</strong> Audit-after means a process crash between response and audit emission loses the row. Audit-then-respond means a crash loses the response, but the audit is queued — client retries safely (idempotent). For an audit-chained product, missing audits are worse than missing responses.</p>\n<p><strong>Why Postgres-backed outbox (DEC-2452, clause 3)?</strong> In-memory queue loses on process restart. Filesystem queue requires its own atomicity model. Postgres is already in the bridge's stack (TASK-PLUGIN-002 task table); reusing it is free.</p>\n<p><strong>Why hashed idempotency key (DEC-2453, clause 4)?</strong> memory-side dedup requires a stable key for the same event. Composing from (kind, tenant, subject, trace, ts_ms) makes the key globally unique while remaining deterministic — same event produces same hash whether emitted on first attempt or 100th retry.</p>\n<p><strong>Why exponential backoff up to 24h (DEC-2454, clause 5)?</strong> Faster gives up too early — extended memory outage (4h+ during a maintenance window) would discard real rows. Slower wastes outbox space. 24h is the SOC 2 audit-completeness window most operators target.</p>\n<p><strong>Why aggressive body scrubbing (DEC-2455, clause 6)?</strong> Audit chain is read by tenant admins, compliance reviewers, and (post-export) by GDPR DSAR fulfillers. Including tool input/output would leak end-user content into the audit chain. Strategy §2 \"audit-chained\" requires audit be SAFE to read, not just present.</p>\n<p><strong>Why required OTel emission on emission failure (DEC-2456, clause 7)?</strong> Audit outages are the most dangerous failure — they're silent. OTel emission ensures TASK-OBS-007 + the dashboards know a tenant's audit chain is degraded. Without it, memory-write failures hide for hours.</p>\n<p><strong>Why subject_id, not plugin_id, as actor (clause 8)?</strong> Audit rows are attributed to the human/service that took the action. The plugin is the conduit, not the actor. This matches Strategy §2 model: every action traceable to a human; plugin context is metadata.</p>\n<p><strong>Why install audit before first invocation (clause 9)?</strong> Without ordering, an admin auditing a grant sees <code>plugin.invoked</code> before <code>plugin.installed</code> — confusing. Forcing the order makes the audit chain narratively coherent.</p>\n<p><strong>Why plugin.updated instead of uninstall+install (clause 10)?</strong> Upgrades are common and benign. Treating them as uninstall+install would: (a) churn the audit chain; (b) lose the version history in a single row; (c) double the rows. plugin.updated captures the same information cleanly.</p>\n<p><strong>Why don't retry 4xx non-429 (clause 11, 14)?</strong> 4xx (other than rate-limit 429) means the request is wrong. Retrying produces identical wrongness. The right response is to surface the failure to a human via OTel and fix the bug.</p>\n<p><strong>Why duration_ms only on invoked (clause 6)?</strong> Other kinds are instantaneous events. Invocation is the only kind that has measurable duration. Keep body minimal for the rest.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Postgres outbox schema</h3>\n<pre><code class=\"language-sql\">-- migrations/0002_audit_outbox.sql\nCREATE TABLE plugin_host.audit_outbox (\n  outbox_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id UUID NOT NULL,\n  plugin_id TEXT NOT NULL,\n  kind TEXT NOT NULL CHECK (kind IN (\n    'plugin.installed','plugin.updated','plugin.uninstalled',\n    'plugin.invoked','plugin.auth_refreshed','plugin.scope_denied'\n  )),\n  idempotency_key TEXT NOT NULL UNIQUE,\n  body JSONB NOT NULL,\n  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','acked','failed')),\n  next_retry_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  retry_count INT NOT NULL DEFAULT 0,\n  last_error TEXT,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  acked_at TIMESTAMPTZ,\n  failed_at TIMESTAMPTZ,\n  trace_id CHAR(32) NOT NULL\n);\nALTER TABLE plugin_host.audit_outbox ENABLE ROW LEVEL SECURITY;\nCREATE POLICY outbox_rls ON plugin_host.audit_outbox\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nCREATE INDEX ON plugin_host.audit_outbox (status, next_retry_at) WHERE status = 'pending';\nCREATE INDEX ON plugin_host.audit_outbox (tenant_id, plugin_id, created_at DESC);</code></pre>\n<h3>Rust emitter trait</h3>\n<pre><code class=\"language-rust\">// services/plugin-host/src/audit/mod.rs\n#[async_trait]\npub trait AuditEmitter: Send + Sync {\n    async fn emit(&amp;self, event: AuditEvent) -&gt; Result&lt;AuditAck, AuditError&gt;;\n}\n\n#[derive(Debug, Clone, Serialize)]\npub struct AuditEvent {\n    pub kind: AuditKind,\n    pub tenant_id: Uuid,\n    pub subject_id: Uuid,\n    pub plugin_id: String,\n    pub plugin_version: String,\n    pub trace_id: String,\n    pub timestamp_ms: i64,\n    pub body_specific: AuditBodySpecific,  // enum per kind\n}\n\n#[derive(Debug, Clone, Serialize)]\npub enum AuditKind {\n    Installed, Updated, Uninstalled, Invoked, AuthRefreshed, ScopeDenied,\n}</code></pre>\n<h3>Idempotency key composer</h3>\n<pre><code class=\"language-rust\">// services/plugin-host/src/audit/idempotency.rs\npub fn compose_key(e: &amp;AuditEvent) -&gt; String {\n    let mut h = Sha256::new();\n    h.update(e.kind.as_str().as_bytes());\n    h.update(b\"\\x1f\");\n    h.update(e.tenant_id.as_bytes());\n    h.update(b\"\\x1f\");\n    h.update(e.subject_id.as_bytes());\n    h.update(b\"\\x1f\");\n    h.update(e.trace_id.as_bytes());\n    h.update(b\"\\x1f\");\n    h.update(&amp;e.timestamp_ms.to_be_bytes());\n    hex::encode(h.finalize())\n}</code></pre>\n<h3>Retry backoff schedule</h3>\n<pre><code class=\"language-rust\">// services/plugin-host/src/audit/retry.rs\npub const BACKOFF_SECONDS: &amp;[u64] = &amp;[\n    0,        // first attempt: immediate\n    1,        // retry 1: +1s after first\n    4,        // retry 2: +4s\n    16,       // ...\n    64,\n    256,\n    1024,\n    4096,\n    16384,    // retry 8: ~4.5h\n    65536,    // retry 9: ~18h\n    86400,    // retry 10: 24h cap\n];\n// After 24h total → status='failed'; alert.</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>plugin.invoked row emitted on tool call success</strong> — call tool, then query memory for kind='plugin.invoked' → row exists.</li><li><strong>plugin.installed row emitted before first invoked</strong> — DB query ordered by seq: installed.seq &lt; invoked.seq.</li><li><strong>plugin.updated emitted on version bump</strong> — install v1.0.0 then v1.1.0; check single plugin.updated row, no extra installed/uninstalled.</li><li><strong>plugin.uninstalled emitted on revoke</strong> — revoke + remove grant; check row.</li><li><strong>plugin.auth_refreshed emitted on refresh</strong> — refresh token; check row.</li><li><strong>plugin.scope_denied emitted on denial</strong> — call without scope; check row.</li><li><strong>Idempotency key dedups duplicate emit</strong> — emit same event twice; memory returns same seq both times; one outbox row.</li><li><strong>Outbox persists across restart</strong> — kill bridge, restart, queued rows still in outbox table; worker drains.</li><li><strong>Retry backoff respects schedule</strong> — fail memory for 5 minutes; observe retries at 0/1/4/16/64/256s offsets.</li><li><strong>24h failure marks status='failed'</strong> — fail memory persistently; after 24h simulated, row status='failed'.</li><li><strong>Body scrubbing — no tool input</strong> — invoke tool with sensitive input; body MUST NOT contain input.</li><li><strong>Body scrubbing — no tool output</strong> — same; body MUST NOT contain output.</li><li><strong>Body has all 6 required fields for invoked</strong> — plugin_id, plugin_version, tenant_id, subject_id, trace_id, outcome, tool_name, duration_ms.</li><li><strong>OTel span emitted on success</strong> — find span with cyberos.audit_kind=plugin.invoked + outcome=success.</li><li><strong>OTel span emitted on retry-queued</strong> — fail memory; find span outcome=retry_queued.</li><li><strong>OTel span emitted on 24h failure</strong> — find span outcome=failed + error.kind set.</li><li><strong>4xx non-429 not retried</strong> — emit with mocked 400; retry_count stays 0; status='failed' immediately.</li><li><strong>429 IS retried</strong> — emit with mocked 429; retry_count grows.</li><li><strong>RLS prevents cross-tenant outbox read</strong> — tenant A cannot SELECT tenant B's rows.</li><li><strong>actor_id is subject_id from JWT</strong> — audit row's actor_id matches JWT.sub.</li><li><strong>Audit-then-respond ordering</strong> — tool call response not returned until audit acked or queued.</li><li><strong>Concurrent emit of same idempotency key</strong> — two simultaneous emits with same key collapse to one outbox row.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/audit_emission_happy_path_test.rs\n#[tokio::test]\nasync fn invoked_row_emitted_on_tool_call() {\n    let ctx = TestContext::with_memory_mock().await;\n    ctx.bridge.tools_call(\"cyberos.cuo.list_personas\", json!({})).await;\n    let rows = ctx.memory.fetch_rows(kind=\"plugin.invoked\").await;\n    assert_eq!(rows.len(), 1);\n    let body = &amp;rows[0][\"body\"];\n    assert_eq!(body[\"plugin_id\"], \"cyberos\");\n    assert_eq!(body[\"tool_name\"], \"cyberos.cuo.list_personas\");\n    assert_eq!(body[\"outcome\"], \"success\");\n    assert!(body[\"duration_ms\"].is_number());\n    assert!(body[\"trace_id\"].is_string());\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/audit_emission_outbox_durability_test.rs\n#[tokio::test]\nasync fn outbox_survives_restart() {\n    let ctx = TestContext::with_memory_unavailable().await;\n    ctx.bridge.tools_call(\"cyberos.cuo.list_personas\", json!({})).await;\n    let outbox = ctx.db.fetch_all(\"SELECT * FROM plugin_host.audit_outbox WHERE status='pending'\").await;\n    assert_eq!(outbox.len(), 1);\n    ctx.bridge.shutdown().await;\n    let ctx2 = TestContext::resume(&amp;ctx).await;\n    ctx2.memory.recover();\n    ctx2.wait_for_drain(Duration::from_secs(5)).await;\n    let pending = ctx2.db.fetch_all(\"SELECT * FROM plugin_host.audit_outbox WHERE status='pending'\").await;\n    assert_eq!(pending.len(), 0);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/audit_emission_idempotency_test.rs\n#[tokio::test]\nasync fn duplicate_emit_collapses_to_one_row() {\n    let ctx = TestContext::with_memory_mock().await;\n    let event = make_event_at(\"2026-05-19T10:00:00Z\");\n    ctx.bridge.audit.emit(event.clone()).await.unwrap();\n    ctx.bridge.audit.emit(event.clone()).await.unwrap();\n    let count = ctx.db.fetch_one(\"SELECT count(*) FROM plugin_host.audit_outbox WHERE idempotency_key=$1\", &amp;[&amp;key_of(&amp;event)]).await;\n    assert_eq!(count, 1);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/audit_emission_no_user_data_leak_test.rs\n#[tokio::test]\nasync fn body_does_not_contain_tool_input() {\n    let ctx = TestContext::with_memory_mock().await;\n    ctx.bridge.tools_call(\"cyberos.cuo.execute_workflow\", json!({\n        \"persona\": \"chief-technology-officer\",\n        \"workflow\": \"adr-quick-capture\",\n        \"inputs\": {\"title\": \"SECRET-PROJECT-X\", \"context\": \"extremely confidential\"}\n    })).await;\n    let rows = ctx.memory.fetch_rows(kind=\"plugin.invoked\").await;\n    let body_json = serde_json::to_string(&amp;rows[0][\"body\"]).unwrap();\n    assert!(!body_json.contains(\"SECRET-PROJECT-X\"), \"tool input leaked into audit body\");\n    assert!(!body_json.contains(\"extremely confidential\"), \"tool input leaked\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/plugin-host/tests/audit_emission_retry_backoff_test.rs\n#[tokio::test]\nasync fn retry_follows_exponential_backoff() {\n    let ctx = TestContext::with_failing_memory().await;\n    ctx.bridge.tools_call(\"cyberos.cuo.list_personas\", json!({})).await;\n    let attempt_times: Vec&lt;i64&gt; = ctx.memory.recorded_attempts();\n    let intervals: Vec&lt;i64&gt; = attempt_times.windows(2).map(|w| w[1] - w[0]).collect();\n    // First 5 intervals should approximate [1s, 4s, 16s, 64s, 256s] (within ±300ms jitter)\n    assert!(approx_eq(intervals[0], 1000, 300));\n    assert!(approx_eq(intervals[1], 4000, 300));\n    assert!(approx_eq(intervals[2], 16000, 600));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Schema + traits + helpers in §3 are the skeleton. Audit module is ~500 Rust lines.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> TASK-PLUGIN-002 (bridge calls emitter on each tool call); TASK-MEMORY-101 (Layer-2 ingest pipeline, shipped).</li><li><strong>Downstream:</strong> TASK-PLUGIN-008 (marketplace audit chain — publish events use the same emitter for plugin.published).</li><li><strong>Cross-module:</strong> TASK-MEMORY-104 (memory HTTP REST, shipped); TASK-OBS-001 (OTel collector for clause 7 spans); TASK-OBS-007 (alertmanager picks up 24h-failure rows per DEC-2454).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<p>(See §3 for schema. Sample bodies in TASK-PLUGIN-005 §8.)</p>\n<h3><code>plugin.invoked</code> body</h3>\n<pre><code class=\"language-json\">{\n  \"plugin_id\": \"cyberos\",\n  \"plugin_version\": \"1.0.0\",\n  \"tool_name\": \"cyberos.cuo.execute_workflow\",\n  \"outcome\": \"success\",\n  \"duration_ms\": 47230,\n  \"trace_id\": \"01HX...\",\n  \"tenant_id\": \"11111111-...\",\n  \"subject_id\": \"22222222-...\"\n}</code></pre>\n<h3><code>plugin.updated</code> body</h3>\n<pre><code class=\"language-json\">{\n  \"plugin_id\": \"cyberos\",\n  \"plugin_version\": \"1.0.1\",\n  \"previous_version\": \"1.0.0\",\n  \"outcome\": \"success\",\n  \"trace_id\": \"01HX...\",\n  \"tenant_id\": \"...\",\n  \"subject_id\": \"...\"\n}</code></pre>\n<h3>Outbox row in retry-queued state</h3>\n<pre><code class=\"language-json\">{\n  \"outbox_id\": \"...\",\n  \"kind\": \"plugin.invoked\",\n  \"idempotency_key\": \"a1b2c3...\",\n  \"status\": \"pending\",\n  \"retry_count\": 3,\n  \"next_retry_at\": \"2026-05-19T10:01:04Z\",\n  \"last_error\": \"connection refused: memory.cyberskill.world:443\",\n  \"body\": { ... as above ... }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<ul><li>~~Should outbox have a max size?~~ → No hard cap; rely on the 24h retry timeout to bound queue size. Hard cap would force dropping rows, violating DEC-2451.</li><li>~~Should we attempt to emit one final row on graceful shutdown?~~ → No; on shutdown, in-flight emissions complete or queue to outbox via existing path. Special-cased \"shutdown emit\" complicates correctness.</li><li>~~Should rate-limit 429 reset the retry counter?~~ → No; counts toward 24h cap. 429 storms are operational issues that need investigation, not silent re-queuing.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>memory HTTP 5xx</td><td>client response code</td><td>queue to outbox</td><td>Worker retries with backoff</td></tr><tr><td>memory HTTP 4xx (non-429)</td><td>client response code</td><td>queue + mark failed immediately</td><td>Operator inspects via OTel; fix bug</td></tr><tr><td>memory HTTP 429 (rate limit)</td><td>client response code</td><td>queue with backoff</td><td>Inherent — workers retry</td></tr><tr><td>Network partition to memory</td><td>reqwest connect error</td><td>queue to outbox</td><td>Worker retries on reconnect</td></tr><tr><td>Outbox DB unavailable</td><td>sqlx error</td><td>tool call fails (upstream_unavailable)</td><td>Bridge alarms via OTel; admin recovers DB</td></tr><tr><td>Idempotency key collision (different bodies)</td><td>memory 409</td><td>outbox row marked failed; alert</td><td>Programming bug — investigate</td></tr><tr><td>Process restart with in-flight emit</td><td>startup detects pending outbox rows</td><td>drain queue on boot</td><td>Inherent</td></tr><tr><td>24h retry exhaustion</td><td>retry_count reaches schedule end</td><td>mark failed; alert via OTel</td><td>Operator investigates memory outage</td></tr><tr><td>User-data leak in body</td><td>unit test fails</td><td>CI blocks merge</td><td>Author scrubs</td></tr><tr><td>OTel exporter down</td><td>exporter buffer fills</td><td>spans dropped, emit continues</td><td>OTel recovers; metric gap acceptable</td></tr><tr><td>Cross-tenant outbox read</td><td>RLS check fails</td><td>0 rows returned</td><td>Inherent (RLS)</td></tr><tr><td>Concurrent retry of same row</td><td>row UPDATE WHERE status='pending' AND outbox_id=X</td><td>second updater gets 0 rows</td><td>Inherent — first wins</td></tr><tr><td>Plugin manifest declares wrong audit endpoint</td><td>manifest validation at install</td><td>install fails</td><td>Author fixes manifest URL</td></tr><tr><td>Bridge unable to compose idempotency key (missing field)</td><td>composer assertion</td><td>tool call fails internal_error</td><td>Programming bug — investigate</td></tr><tr><td>Clock skew between bridge + memory</td><td>timestamp mismatch</td><td>dedup may fail</td><td>NTP; memory tolerates ±30s</td></tr><tr><td>Audit row body size &gt; memory limit</td><td>response 413</td><td>mark failed</td><td>Body is bounded by spec; not expected; investigate if hit</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 <strong>Audit-then-respond implementation.</strong> <code>tools/call.rs</code> calls <code>emit_audit_blocking(event).await?</code> BEFORE sending the response frame. <code>emit_audit_blocking</code> returns Ok once either memory acks (200) or outbox enqueue succeeds (Ok(QueuedForRetry)).</li></ul>\n<ul><li>§11.2 <strong>Worker drain pattern.</strong> A dedicated tokio task <code>audit::retry::worker_loop</code> polls <code>SELECT * FROM audit_outbox WHERE status='pending' AND next_retry_at &lt;= now() ORDER BY next_retry_at LIMIT 100</code> every second. For each row, attempt memory emit; on success update status='acked', acked_at=now(); on failure compute next_retry_at, increment retry_count.</li></ul>\n<ul><li>§11.3 <strong>Backoff jitter.</strong> Add ±20% random jitter to each next_retry_at delta to avoid thundering herd if many emits failed at the same wall-clock minute.</li></ul>\n<ul><li>§11.4 <strong>Why idempotency key uses timestamp_ms.</strong> Without timestamp, retrying the SAME logical event would produce the same key — fine. But two distinct events with identical (kind, tenant, subject, trace) would collide. Adding timestamp_ms (millisecond resolution) makes the key unique while still deterministic.</li></ul>\n<ul><li>§11.5 <strong>RLS on outbox.</strong> Worker process operates with <code>cyberos_outbox_worker</code> role that has elevated SELECT/UPDATE on the outbox table but no other tables. Tenant-scoped reads use the normal RLS-respecting role for /admin endpoints.</li></ul>\n<ul><li>§11.6 <strong>OTel attribute redaction.</strong> Idempotency key is hash of identity-sensitive fields. OK to log directly — it's not a secret. Trace_id is also OK. BUT body content is NOT logged in OTel — only kind + outcome + retry_count.</li></ul>\n<ul><li>§11.7 <strong>Worker concurrency control.</strong> Single worker per bridge process. With multiple bridge instances behind a load balancer, each works on its own outbox slice (modulo by tenant_id hash). Postgres FOR UPDATE SKIP LOCKED prevents two workers grabbing the same row.</li></ul>\n<ul><li>§11.8 <strong>Graceful shutdown.</strong> On SIGTERM: stop accepting new emit calls; wait up to 30s for in-flight emits to complete; flush outbox writes; exit. Remaining in-flight emits become outbox rows.</li></ul>\n<ul><li>§11.9 <strong>Schema migration.</strong> <code>0002_audit_outbox.sql</code> runs ahead of bridge startup. Bridge startup verifies the schema exists; failure logs and exits non-zero (Fargate restarts; not a self-heal loop).</li></ul>\n<ul><li>§11.10 <strong>Manual replay tool.</strong> <code>cyberos-plugin-host audit-replay --outbox-id &lt;uuid&gt;</code> reruns a specific row. Used by operators for one-off recovery after memory bug fixes.</li></ul>\n<hr>\n<p><em>End of TASK-PLUGIN-006 spec.</em></p>";
