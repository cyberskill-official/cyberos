window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-108"]="<h1>TASK-IMP-108: Split the statuses doing two jobs</h1>\n<h2>Summary</h2>\n<p>Three findings are one defect: <code>draft</code> means four different things, <code>ready_to_implement</code> means two, and <code>routed_back_count</code> is written eighteen times and read as a limit zero times. Each is a word doing more than one job, and the fix in every case is to record the reason rather than mint a status. Add <code>draft_reason</code>, add <code>entered_via</code>, and give the route-back a ceiling that halts for the operator instead of grinding.</p>\n<h2>Problem</h2>\n<p>Verified on merged main:</p>\n<ul><li><strong><code>draft</code> (336 rows, the largest population in the corpus).</strong> A spec being written this minute, a 2026-07-08 migration stub whose clauses were never authored, a re-opened <code>cannot_reproduce</code> bug, and a deliberately parked idea all wear one word. Nothing distinguishes them, nothing ages them, and the status page reports a percentage against a denominator nobody believes.</li><li><strong><code>ready_to_implement</code>.</strong> Per §1.1 it is \"audited, never built\"; per §1.3 it is also the route-back target with <code>routed_back_count += 1</code> - a task with code, tests, a review packet, and a history of failure. The queue treats both identically and a reader cannot tell them apart without opening the frontmatter.</li><li><strong><code>routed_back_count</code>.</strong> 18 references in ship-tasks, every one an increment or a definition. No rule reads it as a limit. The 5-fail circuit breaker bounds the debugging cycle inside one testing phase; nothing bounds the number of times a task circles the whole loop.</li></ul>\n<p>The consequence is shared: work that keeps failing cannot be told from work that has not started, and nothing escalates.</p>\n<h2>Proposed Solution</h2>\n<p>Record reasons, do not mint statuses. <code>draft_reason</code> (enum: <code>authoring</code>, <code>migrated_stub</code>, <code>needs_spec</code>, <code>parked_idea</code>) says which kind of draft this is. <code>entered_via</code> (enum: <code>audit</code>, <code>rework</code>, <code>spec_rejected</code>) says which kind of <code>ready_to_implement</code> this is - and <code>spec_rejected</code> gives a wrong SPEC somewhere to go other than back to an implementer who will build the same wrong thing. At <code>routed_back_count &gt;= 3</code> ship-tasks HALTS at an operator gate presenting the route-back reasons side by side, with the verdicts re-enter / split / <code>on_hold</code> / <code>closed</code>.</p>\n<p>Three is a judgment call and the spec says so: it is the point at which \"the same task failed three different ways\" is evidence about the spec, not the implementation.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Remove <code>draft</code> (the original proposal). Rejected: it is the only status where a spec may be wrong. Every other status carries a warranty; delete it and <code>draft -&gt; ready_to_implement</code> collapses into one atomic act, which deletes the audit gate - the transition IS the gate. 336 rows would have to masquerade as something they are not.</li><li>Mint <code>draft_migrated</code>, <code>draft_parked</code>, <code>ready_to_rework</code>. Rejected: the enum's own doctrine (§1.2) is that <code>closed</code> meaning six things is a backlog you cannot learn from - the answer there was a field (<code>duplicate_of</code>), not more statuses. Same answer here.</li><li>Auto-close stale drafts. Rejected: a staleness report is information; auto-closing is the machine making a scope decision that belongs to the operator.</li><li>Read <code>routed_back_count &gt; 0</code> instead of adding <code>entered_via</code>. Retained as a fallback if the field proves redundant in review - the counter already carries the signal, and <code>entered_via</code> is only worth it for <code>spec_rejected</code>, which the counter cannot express.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: the queue can tell fresh work from thrash, and a third route-back halts for a human instead of re-entering - suite-asserted. Baseline: both are invisible today.</li><li>Guardrail: the status enum is unchanged (12 values), no task file changes status as a result of this task, and the existing route-back path still works for counts under the ceiling.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>draft_reason</code> and <code>entered_via</code> frontmatter fields + their FM rules, the route-back ceiling in ship-tasks, the staleness report on the status page, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any change to the status enum itself - the 12 values stay (TASK-IMP-DECIDED 2026-07-17: <code>implement</code> keeps its name).</li><li>Backfilling <code>draft_reason</code> across the 336 existing drafts - the field is optional and absent means unknown, which is honest. Backfill is an operator decision, possibly per module.</li><li>Auto-closing or auto-escalating anything.</li><li>Changing what the 5-fail debugging circuit breaker does.</li></ul>\n<h2>Dependencies</h2>\n<p>None logically. Fields are additive and optional.</p>\n<p><strong>Serialisation note:</strong> touches <code>STATUS-REFERENCE.md</code> (shared with TASK-IMP-109), <code>ship-tasks.md</code> (109, 113, 114, 115), <code>backlog-mutate.mjs</code> (105), <code>render-status-hub.mjs</code> (114), <code>test_workflow_evolution.py</code> (115). Different sections in each, but §11a forbids concurrent shared-tree writes: the parent serialises these, and they MUST NOT be swarm members of one round.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §13.2, §14.4, and §10 IMP-25 - three findings the handoff explicitly asks to be treated as one batch; verified against STATUS-REFERENCE.md and ship-tasks.md on merged main.</li><li><strong>Human review:</strong> the keep-<code>draft</code> and keep-<code>implement</code> decisions are the operator's recorded 2026-07-17 answers; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>task@1</code> MUST accept optional <code>draft_reason</code> with the closed enum <code>authoring | migrated_stub | needs_spec | parked_idea</code>. Absent MUST be legal and MUST mean unknown.</li><li>1.2 <code>task-lint</code> MUST reject a <code>draft_reason</code> value outside the enum, and MUST NOT require the field.</li><li>1.3 <code>task@1</code> MUST accept optional <code>entered_via</code> with the closed enum <code>audit | rework | spec_rejected</code>, carrying the same absent-is-legal rule.</li><li>1.4 <code>backlog-state-update-author</code> MUST set <code>entered_via: rework</code> on any §1.3 route-back it writes, alongside the existing <code>routed_back_count += 1</code>.</li><li>1.5 A task routed back because its SPEC is wrong (not its code) MUST be flipped to <code>draft</code> with <code>entered_via: spec_rejected</code> and re-audited before re-entering - it MUST NOT go to <code>ready_to_implement</code>, which would hand an unchanged wrong spec to an implementer.</li><li>1.6 ship-tasks MUST HALT at an operator gate when a task with <code>routed_back_count &gt;= 3</code> would re-enter, presenting every route-back reason on record and the verdicts re-enter / split / <code>on_hold</code> / <code>closed</code>. It MUST NOT re-enter without a recorded verdict.</li><li>1.7 The status page MUST render a staleness report: drafts grouped by <code>draft_reason</code> with age. It MUST NOT change any task's status.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2) - a spec with each enum value lints clean; an out-of-enum value reds; an absent field lints clean - test: <code>tools/install/tests/test_task_lint.sh::t09_draft_reason_enum</code></li><li>[ ] AC 2 (traces_to: #1.3, #1.4) - a route-back writes <code>entered_via: rework</code> and increments the counter in one mutation - test: <code>tools/install/tests/test_workflow_helpers.sh::t18_entered_via_on_rework</code></li><li>[ ] AC 3 (traces_to: #1.5) - a <code>spec_rejected</code> route-back lands at <code>draft</code>, not <code>ready_to_implement</code> - test: <code>tools/install/tests/test_workflow_helpers.sh::t19_spec_rejected_lands_draft</code></li><li>[ ] AC 4 (traces_to: #1.6) - a task at <code>routed_back_count: 3</code> halts with reasons presented and does not re-enter without a verdict - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_routeback_ceiling_halts</code></li><li>[ ] AC 5 (traces_to: #1.6) - a task at <code>routed_back_count: 2</code> re-enters normally (the ceiling is 3, not \"any\") - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_under_ceiling_reenters</code></li><li>[ ] AC 6 (traces_to: #1.7) - the staleness report renders drafts by reason and age, and no status changes - test: <code>tools/docs-site/tests/test_render_status_hub.sh::t08_draft_staleness_report</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The 336 existing drafts carry no <code>draft_reason</code>: they render as unknown, which is the truth. This task MUST NOT invent a reason for a task it did not author (that would be the <code># UNREVIEWED</code> mistake again, with better manners).</li><li><code>routed_back_count</code> at 3 with all three route-backs from the same cause (one flaky test): the gate still halts. A human reading three identical reasons decides in seconds, and the alternative is a loop that never asks.</li><li>A task manually flipped by an operator to <code>ready_to_implement</code> with no <code>entered_via</code>: legal (absent = unknown, 1.3), and the ceiling still applies from the counter.</li><li><code>spec_rejected</code> on a task whose spec is fine but whose ACs were mis-cited: still <code>draft</code> - re-authoring the AC block is a spec change, and the audit is what catches it.</li><li>Ceiling reached during a swarm batch: the halt belongs to the parent, per §11a - a sub-agent MUST NOT resolve it, because the verdict is the operator's.</li><li>A draft that is <code>authoring</code> and 200 days old: reported, never touched. The report is the finding; the operator is the fix.</li><li>Security-class: two enum fields and a counter comparison. No untrusted content is executed; the enums are closed, so a crafted value reds rather than routes.</li></ul>";
