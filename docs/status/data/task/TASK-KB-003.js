window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-KB-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The KB service <strong>MUST</strong> ship 3-tier permission system at <code>services/kb/src/permission/</code> with share-link tokens, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>visibility_tier</code> against closed enum per DEC-1901.</li></ol>\n<ol><li><strong>MUST</strong> gate access at <code>access_gate.rs::check(doc, user, share_token?)</code>:</li></ol>\n<ul><li>public: always allow</li><li>org_only: require valid session matching doc.tenant_id</li><li>role_restricted: require user has one of doc.allowed_roles</li><li>share_token (if provided): verify token signature + expiry + max_uses</li></ul>\n<ol><li><strong>MUST</strong> define table extension + share-link table at migration <code>0003</code>: ```sql ALTER TABLE kb_documents ADD COLUMN visibility_tier TEXT NOT NULL DEFAULT 'org_only' CHECK (visibility_tier IN ('public','org_only','role_restricted')); ALTER TABLE kb_documents ADD COLUMN allowed_roles TEXT[] NOT NULL DEFAULT '{}'; CREATE INDEX docs_visibility_idx ON kb_documents(tenant_id, visibility_tier); GRANT UPDATE (visibility_tier, allowed_roles) ON kb_documents TO cyberos_app;</li></ol>\n<p>   CREATE TABLE kb_share_links (      token_jti UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      doc_id UUID NOT NULL,      created_by UUID NOT NULL,      expires_at TIMESTAMPTZ NOT NULL,      max_uses INT NOT NULL DEFAULT 0,  -- 0 = unlimited      used_count INT NOT NULL DEFAULT 0,      revoked_at TIMESTAMPTZ,      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    CREATE INDEX share_links_doc_idx ON kb_share_links(tenant_id, doc_id);    ALTER TABLE kb_share_links ENABLE ROW LEVEL SECURITY;    CREATE POLICY share_links_rls ON kb_share_links      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON kb_share_links FROM cyberos_app;    GRANT UPDATE (used_count, revoked_at) ON kb_share_links TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> create share-links at <code>share_link.rs::create(doc_id, expires_in, max_uses?)</code> per DEC-1902 — CDO-only.</li></ol>\n<ol><li><strong>MUST</strong> validate share-link on use per DEC-1902:</li></ol>\n<ul><li>Signature valid</li><li>expires_at &gt; now()</li><li>max_uses=0 OR used_count &lt; max_uses</li><li>revoked_at IS NULL</li><li>On success: increment used_count atomically.</li></ul>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text PUT    /v1/kb/docs/{id}/visibility         body: {tier, allowed_roles?} POST   /v1/kb/docs/{id}/share-links        body: {expires_in_seconds, max_uses?} POST   /v1/kb/share-links/{jti}/revoke GET    /v1/kb/docs/{id}?share_token=...    (share-link access path) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-1904. PII per TASK-MEMORY-111: tier+role enums ok; share_token only jti in chain.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from gate → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> serve role_restricted to non-matching role per DEC-1903.</li></ol>\n<ol><li><strong>MUST NOT</strong> accept expired share-link per DEC-1902.</li></ol>\n<ol><li><strong>MUST NOT</strong> accept share-link past max_uses per DEC-1902.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 3 tiers (DEC-1900)?</strong> Industry-standard public/private/role split; covers 95% of KB use cases.</p>\n<p><strong>Why share-links (DEC-1902)?</strong> Customer collaboration needs external read access without account creation; tokens provide time-bounded grant.</p>\n<p><strong>Why max_uses (DEC-1902)?</strong> Single-use links (max_uses=1) prevent forwarding; mass-share allows broader distribution.</p>\n<p><strong>Why role match (DEC-1903)?</strong> Executive comp docs need ROOT-CHRO scope; engineering runbooks need engineer scope.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">PUT    /v1/kb/docs/{id}/visibility\nPOST   /v1/kb/docs/{id}/share-links\nPOST   /v1/kb/share-links/{jti}/revoke\nGET    /v1/kb/docs/{id}?share_token=&lt;jwt&gt;</code></pre>\n<p>Sample share-link creation:</p>\n<pre><code class=\"language-json\">{\n  \"expires_in_seconds\": 86400,\n  \"max_uses\": 5\n}</code></pre>\n<p>Response:</p>\n<pre><code class=\"language-json\">{\n  \"token_jti\": \"uuid\",\n  \"share_url\": \"https://kb.cyberskill.com/d/abc123?share_token=eyJhbGc...\",\n  \"expires_at\": \"2026-05-18T10:00:00Z\",\n  \"max_uses\": 5\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>visibility_tier enum cardinality 3</strong>. 2. <strong>public served unauth</strong>. 3. <strong>org_only requires session</strong>. 4. <strong>role_restricted requires role match</strong>. 5. <strong>Share-link JWT signed + verified</strong>. 6. <strong>expires_at enforced</strong>. 7. <strong>max_uses enforced (0=unlimited)</strong>. 8. <strong>Revoke endpoint works</strong>. 9. <strong>used_count incremented atomically</strong>. 10. <strong>4 memory audit kinds emitted</strong>. 11. <strong>Access denied → audit</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>CDO-only create + revoke</strong>. 14. <strong>Trace_id preserved</strong>. 15. <strong>Append-only share_links via REVOKE except used_count + revoked_at</strong>. 16. <strong>default tier = org_only on new doc</strong>. 17. <strong>Multiple share-links per doc supported</strong>. 18. <strong>Share-link survives doc version updates</strong>. 19. <strong>Revoked share-link cannot be undone</strong>. 20. <strong>token_jti UUID prevents collision</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn public_doc_unauth_serve() {\n    let ctx = TestContext::with_public_doc().await;\n    let r = ctx.fetch_doc_unauth(ctx.doc_id).await;\n    assert_eq!(r.status_code, 200);\n}\n\n#[tokio::test]\nasync fn role_restricted_denies_wrong_role() {\n    let ctx = TestContext::with_role_restricted_doc(vec![\"ROOT-CHRO\"]).await;\n    let r = ctx.fetch_doc_as(ctx.am_user, ctx.doc_id).await;\n    assert_eq!(r.status_code, 403);\n}\n\n#[tokio::test]\nasync fn share_link_expiry_enforced() {\n    let ctx = TestContext::with_share_link_expires_in(1).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    let r = ctx.fetch_via_share_token(ctx.token).await;\n    assert_eq!(r.status_code, 403);\n}\n\n#[tokio::test]\nasync fn share_link_max_uses_enforced() {\n    let ctx = TestContext::with_share_link_max_uses(3).await;\n    for _ in 0..3 { assert!(ctx.use_share_token().await.is_ok()); }\n    let r = ctx.use_share_token().await;\n    assert!(r.is_err());\n}\n\n// 5.5..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-KB-001. <strong>Cross-module:</strong> TASK-AUTH-101 (role check), TASK-MEMORY-111 (audit), TASK-AUTH-105 (KMS for JWT signing key).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid JWT signature</td><td>verify</td><td>401 + sev-2</td><td>inherent</td></tr><tr><td>Token expired</td><td>timestamp</td><td>403</td><td>new link</td></tr><tr><td>Max uses reached</td><td>check + increment</td><td>403</td><td>new link</td></tr><tr><td>Revoked share-link use</td><td>flag</td><td>403</td><td>new link</td></tr><tr><td>Role mismatch</td><td>check</td><td>403 + sev-3</td><td>request access</td></tr><tr><td>Public doc on non-public service</td><td>guard</td><td>inherent</td><td>inherent</td></tr><tr><td>Cross-tenant share-link</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Atomic increment race</td><td>row-level lock</td><td>inherent</td><td>inherent</td></tr><tr><td>JWT signing key rotation</td><td>dual-key window</td><td>inherent</td><td>inherent</td></tr><tr><td>Share-link to deleted doc</td><td>check</td><td>404</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 JWT signed with tenant-specific key via TASK-AUTH-105 KMS.</li><li>§11.2 Access gate is pure function: <code>(doc, user, token) → Allow | Deny(reason)</code>.</li><li>§11.3 used_count increment: <code>UPDATE ... SET used_count = used_count + 1 WHERE jti=$1 AND used_count &lt; max_uses RETURNING used_count</code>.</li><li>§11.4 memory audit body: doc_id, tier, decision; token jti only (no token value).</li><li>§11.5 Default tier on new doc = org_only (least-surprise for accidental publish risk).</li></ul>\n<hr>\n<p><em>End of TASK-KB-003 spec.</em></p>";
