window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PORTAL-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PORTAL service <strong>MUST</strong> ship SCIM 2.0 DELETE handler at <code>services/portal/src/scim/user_delete.rs</code> extending TASK-PORTAL-003's SCIM endpoint with session invalidation ≤ 30s p99, dual-channel session kill (JWT blacklist + WebSocket close), cascade revocation across PORTAL-005 + PORTAL-001 + CHAT-005 + MCP-006, 5-min grace period + admin restore, idempotency, per-Engagement isolation, nightly reconciliation, and 7 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> extend the SCIM endpoint at <code>DELETE /scim/v2/{engagement_slug}/Users/{id}</code> per RFC 7644 §3.6. Handler:</li></ol>\n<ul><li>Authenticated via per-Engagement SCIM bearer token (TASK-PORTAL-003 §1 #13).</li><li>Marks subject status <code>tombstoned</code> + sets <code>tombstoned_at = now()</code> in the SUBJECTS table (TASK-AUTH-002).</li><li>Enters sync phase per §1 #3 — completes within 1 second wall-clock.</li><li>Enqueues async phase per §1 #4 — completes within 30 seconds wall-clock end-to-end.</li><li>Returns <code>204 No Content</code> per RFC 7644 §3.6 (synchronous ack; async work continues).</li></ul>\n<ol><li><strong>MUST</strong> define <code>portal_jwt_blacklist</code> table at migration <code>0010</code>: <code>(jti UUID PRIMARY KEY, tenant_id UUID NOT NULL, subject_id UUID NOT NULL, engagement_id UUID, blacklisted_at TIMESTAMPTZ NOT NULL DEFAULT now(), expires_at TIMESTAMPTZ NOT NULL, reason TEXT NOT NULL)</code>. Postgres is the durable store; Redis is the hot-path cache. Both consulted at JWT validate; Postgres is the source of truth on cache miss.</li></ol>\n<ol><li><strong>MUST</strong> complete the sync phase within 1 second wall-clock per DEC-1096. The <code>deprovision/sync_phase.rs::execute(subject_id, engagement_id)</code>:</li></ol>\n<ul><li>UPDATE <code>subjects SET status='tombstoned', tombstoned_at=now() WHERE id=$1</code> (within TASK-AUTH-002 RLS scope).</li><li>SELECT all active JWT jtis for the subject from <code>auth_sessions WHERE subject_id=$1 AND revoked_at IS NULL</code>.</li><li>For each jti: INSERT into <code>portal_jwt_blacklist</code> (Postgres) + ZADD into Redis sorted-set <code>jwt_blacklist:&lt;tenant&gt;</code> with score=expiration_unix.</li><li>PUBLISH on Redis pub/sub channel <code>jwt_blacklist_update</code> so all gateway nodes update their in-memory caches within ~50ms.</li><li>Emit <code>portal.scim_user_deprovisioned</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> complete the async phase within 30 seconds end-to-end per DEC-1089. The <code>deprovision/async_phase.rs::execute(subject_id, engagement_id)</code>:</li></ol>\n<ul><li>In parallel, invoke each of the 4 cascade targets per DEC-1084 + §1 #6.</li><li>Iterate active WebSocket connections per §1 #5 + close them.</li><li>UPDATE <code>auth_sessions SET revoked_at=now(), revoked_reason='scim_deprovision' WHERE subject_id=$1 AND engagement_id=$2 AND revoked_at IS NULL</code>.</li><li>Compute end-to-end latency (sync phase start → last cascade complete) + emit OTel histogram + check 30s SLO.</li><li>On SLO breach: emit <code>portal.scim_deprovision_sla_breach</code> sev-1.</li></ul>\n<ol><li><strong>MUST</strong> force-close active WebSocket connections per DEC-1081 + DEC-1095. The <code>deprovision/websocket_killer.rs::close_for_subject(subject_id, engagement_id)</code>:</li></ol>\n<ul><li>Iterates <code>chat_active_connections</code> + <code>genie_active_connections</code> registries (in-memory + Redis-backed for multi-node).</li><li>For each connection matching <code>(subject_id, engagement_id)</code>: send WebSocket close frame with code <code>4001</code> + reason <code>\"scim_deprovision\"</code>.</li><li>Emit <code>portal.websocket_force_closed</code> memory row per closed connection (informational; sev-3; sampled at 10% for high-volume scenarios via TASK-OBS-006).</li></ul>\n<ol><li><strong>MUST</strong> cascade revoke 4 downstream resources per DEC-1084. The <code>deprovision/cascade.rs::run_all(subject_id, engagement_id)</code> invokes in parallel:</li></ol>\n<ul><li><strong>TASK-PORTAL-005 Genie sessions</strong>: <code>portal_genie_sessions</code> table — UPDATE <code>revoked_at=now()</code> WHERE subject_id + engagement_id; emit <code>portal.scim_cascade_revocation</code> with <code>target='genie_sessions'</code>.</li><li><strong>TASK-PORTAL-001 scoped views</strong>: invalidate any cached view tokens in Redis for this subject's Engagement membership; no DB write needed (views are computed; tombstoned subject's Engagement-membership join returns 0 rows).</li><li><strong>TASK-CHAT-005 channel memberships</strong>: <code>chat_channel_memberships</code> table — UPDATE <code>revoked_at=now()</code>; cascade emits a CHAT membership-revoked event for any active subscribers.</li><li><strong>TASK-MCP-006 pending confirmations</strong>: DELETE FROM <code>mcp_pending_confirmations</code> WHERE caller_subject_id=$1 AND consumed_at IS NULL (per MCP-006 grants table; tombstoned subject's pending acks are voided).</li></ul>\n<ol><li><strong>MUST</strong> consult the JWT blacklist on every JWT validate per DEC-1085. The <code>services/auth/src/jwt/validator.rs::validate(jwt)</code> modification:</li></ol>\n<ul><li>After standard signature + exp + aud checks: lookup <code>jwt.jti</code> in Redis blacklist set (<code>SISMEMBER jwt_blacklist:&lt;tenant&gt; &lt;jti&gt;</code>).</li><li>If present → return <code>Err(AuthError::JwtRevoked)</code> → handler returns 401 + <code>{ error: \"jwt_revoked\", reason: \"scim_deprovision\" }</code>.</li><li>On Redis miss + Postgres hit: backfill Redis (cache warm).</li></ul>\n<ol><li><strong>MUST</strong> support 5-min grace period per DEC-1083. Within 5 minutes of tombstone, SCIM PUT on the same user reactivates:</li></ol>\n<ul><li>Handler at <code>PUT /scim/v2/{engagement_slug}/Users/{id}</code>: if subject is tombstoned + <code>now() - tombstoned_at &lt; 5 min</code>, transition <code>status='active'</code> + <code>tombstoned_at=null</code> + reverse the JWT blacklist (DELETE rows + ZREM from Redis sorted-set + PUBLISH unblacklist event).</li><li>Cascade rollback: re-activate sessions + Genie subscriptions etc. WebSocket connections cannot be re-opened (already closed); client must re-connect (will succeed since blacklist cleared).</li><li>Emit <code>portal.scim_user_restored</code> sev-2 + payload <code>restored_via=scim_put_within_grace</code>.</li><li>Beyond 5 min: SCIM PUT returns <code>409 + grace_period_expired</code> + suggests using admin restore endpoint.</li></ul>\n<ol><li><strong>MUST</strong> expose admin restore at <code>POST /v1/admin/engagements/{eng_id}/subjects/{subject_id}/restore</code> per DEC-1092. Caller has <code>tenant_admin</code> role. Body: <code>{ reason }</code>. Handler:</li></ol>\n<ul><li>Validates subject status='tombstoned'.</li><li>Same logic as §1 #8 grace-period reversal (un-blacklist + cascade re-activate).</li><li>Emit <code>portal.scim_user_restored</code> sev-1 + payload <code>restored_via=admin_restore</code>.</li><li>Reason free-text persisted to <code>portal_restore_requests</code> (audit forensic).</li></ul>\n<ol><li><strong>MUST</strong> be idempotent on SCIM DELETE per DEC-1087:</li></ol>\n<ul><li>Re-DELETE on tombstoned subject → 204 (no state change; no re-emit of deprovision audit row to avoid double-counting).</li><li>Re-DELETE on hard-purged subject → 404 + <code>{ scim_type: \"noTarget\" }</code> per RFC 7644 §3.6.</li></ul>\n<ol><li><strong>MUST</strong> scope session invalidation per-Engagement per DEC-1086. Subject A is member of Engagement X + Y. SCIM DELETE on subject A within Engagement X's SCIM endpoint revokes ONLY X-scoped sessions (JWTs with <code>engagement_id=X</code> claim); Y-scoped sessions remain valid. Subject A's <code>auth_sessions</code> rows filtered <code>WHERE engagement_id = $X</code>.</li></ol>\n<ol><li><strong>MUST</strong> propagate JWT blacklist updates across gateway nodes within 50ms p95 per DEC-1085. The Redis pub/sub mechanism:</li></ol>\n<ul><li>Sync-phase publish: <code>PUBLISH jwt_blacklist_update &lt;jti_list_json&gt;</code>.</li><li>Each gateway node subscribes; on receive, updates in-memory cache.</li><li>Cache miss falls back to Redis SISMEMBER (~1ms) then Postgres SELECT (~5ms).</li><li>End-to-end visibility: &lt; 100ms p99 for blacklist effect.</li></ul>\n<ol><li><strong>MUST</strong> maintain <code>portal_deprovision_log</code> table at migration <code>0009</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, subject_id UUID NOT NULL, action TEXT NOT NULL CHECK (action IN ('soft_tombstone','hard_purge','restore','grace_undelete')), initiated_at TIMESTAMPTZ NOT NULL DEFAULT now(), completed_at TIMESTAMPTZ, sync_phase_duration_ms INT, async_phase_duration_ms INT, cascade_targets JSONB NOT NULL, sla_breach BOOLEAN NOT NULL DEFAULT false)</code>. Append-only. RLS scoped to <code>(tenant_id, engagement_id)</code>.</li></ol>\n<ol><li><strong>MUST</strong> maintain <code>portal_restore_requests</code> table at migration <code>0011</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, engagement_id UUID NOT NULL, subject_id UUID NOT NULL, requested_by_subject_id UUID NOT NULL, reason TEXT NOT NULL, requested_at TIMESTAMPTZ NOT NULL DEFAULT now())</code>. Append-only.</li></ol>\n<ol><li><strong>MUST</strong> rate-limit SCIM DELETE at 1000/min/Engagement per DEC-1091 — enterprise org-wide cleanup scenarios. Excess returns <code>429 + Retry-After: 60</code>.</li></ol>\n<ol><li><strong>MUST</strong> run nightly reconciliation per DEC-1097 + DEC-1094. The <code>reconciliation.rs::run_nightly()</code>:</li></ol>\n<ul><li>SELECT subjects WHERE status='tombstoned' AND tombstoned_at &lt; now() - interval '1 day'.</li><li>For each: check <code>auth_sessions WHERE subject_id=$1 AND revoked_at IS NULL</code> should return 0 rows.</li><li>Any active session for a tombstoned subject = sev-1 <code>portal.scim_orphan_session_detected</code> (not in 7-kind core list per DEC-1088; informational+forensic).</li><li>Auto-revoke the orphan + audit; emit one row per orphan.</li></ul>\n<ol><li><strong>MUST</strong> emit 7 memory audit row kinds per DEC-1088:</li></ol>\n<ul><li><code>portal.scim_user_deprovisioned</code> (sev-1 — material identity event)</li><li><code>portal.scim_user_restored</code> (sev-1 — counter-event)</li><li><code>portal.scim_cascade_revocation</code> (sev-2 — one per cascade target)</li><li><code>portal.jwt_blacklist_propagated</code> (sev-3 — high-volume; sampled at 1%)</li><li><code>portal.websocket_force_closed</code> (sev-3 — high-volume; sampled at 10%)</li><li><code>portal.scim_grace_period_expired</code> (sev-3 — informational)</li><li><code>portal.scim_deprovision_sla_breach</code> (sev-1 — SLO miss)</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub audit rows per DEC-1090 + task-audit skill rule 18. <code>subject_id</code> UUID retained (forensic anti-correlation needed); <code>email</code> PII-scrubbed via TASK-MEMORY-111 → <code>email_hash16</code>.</li></ol>\n<ol><li><strong>MUST</strong> thread W3C <code>traceparent</code> across SCIM DELETE → sync phase → async phase → cascade targets → reconciliation (task-audit skill rule 22-24). Single trace_id per deprovision operation.</li></ol>\n<ol><li><strong>MUST</strong> measure deprovision SLO via OTel histogram <code>portal_scim_deprovision_duration_seconds</code>. Alarm sev-1 if <code>p99 &gt; 30s sustained 5 min</code>.</li></ol>\n<ol><li><strong>MUST NOT</strong> hard-purge by default per DEC-1082. The <code>?action=hard_purge</code> query param is reserved for slice 3 GDPR Art. 17 explicit erasure flow (task-AUTH-2xx); slice 2 returns 501 <code>not_implemented</code> on hard_purge.</li></ol>\n<ol><li><strong>MUST NOT</strong> block sync phase on cascade completion per DEC-1096. Sync phase MUST return 204 within 1s; cascade async via <code>tokio::spawn</code> with deadline.</li></ol>\n<ol><li><strong>MUST NOT</strong> support webhook-based deprovision at slice 2 per DEC-1098. SCIM DELETE is the canonical deprovision channel; webhook-fan-in deferred.</li></ol>\n<ol><li><strong>SHOULD</strong> observe per-Engagement deprovision-volume metric <code>portal_scim_deprovision_total{engagement_id}</code>. Sudden spike (&gt;100/min sustained) may indicate IdP-side misconfig + warrant operator attention.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why 30s SLO (§1 #1, DEC-1080)?</strong> Enterprise procurement asks \"how quickly does a terminated employee lose access?\" The industry-standard answer is \"≤ 60 seconds\" (Okta + Azure benchmarks); 30s is competitive + achievable with the dual-channel architecture (JWT blacklist + WebSocket close). Anything slower = procurement objection.</p>\n<p><strong>Why dual-channel kill (§1 #5 + §1 #7, DEC-1081)?</strong> Stateless REST APIs use JWT bearer auth — JWT blacklist catches those. Long-lived WebSocket connections (CHAT, Genie) don't re-validate JWTs every message — they need explicit close frames. Both channels must be killed; missing either leaks access.</p>\n<p><strong>Why sync-then-async phase split (§1 #3 + §1 #4, DEC-1096)?</strong> SCIM clients (Okta, Azure) expect SCIM DELETE to return 204 quickly. Doing all the work (blacklist + cascade + WS close) synchronously would push response time to 5-10 seconds — SCIM clients time out at 30s default + Okta retries on timeout → duplicate deprovisions. Sync phase does the critical 1s work (tombstone + blacklist add); async phase handles the rest within the 30s SLO end-to-end.</p>\n<p><strong>Why grace period 5 min (§1 #8, DEC-1083)?</strong> IdP-side glitches happen — Okta + Azure both have documented incidents where SCIM DELETE fires spuriously due to integration bugs. Without a grace, the affected user is locked out + must be re-onboarded from scratch (loses CHAT history, Genie context, etc.). 5 min is enough to catch + reverse the glitch; short enough that an attacker who tricked IdP into firing DELETE can't undo from the user's side (user lacks SCIM PUT access; only IdP can re-add).</p>\n<p><strong>Why admin restore endpoint (§1 #9, DEC-1092)?</strong> Grace period covers minutes-after-DELETE. Beyond grace, restore requires admin intent (tenant_admin role). Engagement_admin too narrow (restore reaches across Engagement scope — Genie subscriptions span Engagements).</p>\n<p><strong>Why per-Engagement isolation (§1 #11, DEC-1086)?</strong> A consulting user may belong to Engagement X (Client A) + Engagement Y (Client B). Client A firing user via their SCIM endpoint must NOT terminate user's access to Client B. Per-Engagement scoping is the only correct semantic.</p>\n<p><strong>Why Redis blacklist + Postgres durable store (§1 #2 + §1 #7, DEC-1085)?</strong> Redis = fast (1ms lookups at scale); Postgres = durable (Redis is in-memory + can lose data on restart). Redis is the hot cache; Postgres is the source of truth. Cache-miss-recovery is fast (~5ms Postgres query); Redis pub/sub propagates new blacklist entries across all gateway nodes within ~50ms.</p>\n<p><strong>Why nightly reconciliation (§1 #16, DEC-1097)?</strong> Cascade revocation is best-effort — distributed system, partial failures possible. Reconciliation closes the loop: any tombstoned subject with still-active sessions = bug, auto-fix + alert.</p>\n<p><strong>Why WebSocket close code 4001 (§1 #5, DEC-1095)?</strong> RFC 6455 reserves 4000-4999 for application-defined codes. 4001 is our convention for \"session revoked\"; clients SHOULD reconnect (will fail re-auth + see clear 401). Custom code lets client UI surface a \"you've been signed out by your admin\" message rather than a generic \"connection lost\".</p>\n<p><strong>Why subject_id NOT scrubbed in audit (§1 #18, DEC-1090)?</strong> Identity events are forensic-critical. Without subject_id, \"who was deprovisioned at 09:14?\" is unanswerable. UUID is non-PII (random + non-correlatable to external identity); email IS PII (correlatable to person + service). Differential scrub.</p>\n<p><strong>Why soft tombstone default, hard purge gated (§1 #21, DEC-1082)?</strong> GDPR Art. 17 erasure is a heavyweight legal flow (verify request, document, propagate to processors, attest). Most deprovisions are operational (\"employee left\") not legal (\"erase me\"); soft tombstone preserves audit history without violating retention obligations. Hard purge is reserved for the explicit GDPR flow (task-AUTH-2xx slice 3).</p>\n<p><strong>Why SCIM-only at slice 2 (§1 #23, DEC-1098)?</strong> Multi-channel deprovision (SCIM + webhook + admin UI) compounds correctness risk (which channel wins on simultaneous events?). SCIM is the standard; we stick to it at slice 2. Webhook deprovision deferred.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema</h3>\n<pre><code class=\"language-sql\">-- 0009_portal_deprovision_log.sql\nCREATE TABLE portal_deprovision_log (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  subject_id UUID NOT NULL,\n  action TEXT NOT NULL CHECK (action IN ('soft_tombstone','hard_purge','restore','grace_undelete')),\n  initiated_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  completed_at TIMESTAMPTZ,\n  sync_phase_duration_ms INT,\n  async_phase_duration_ms INT,\n  cascade_targets JSONB NOT NULL DEFAULT '[]'::jsonb,\n  sla_breach BOOLEAN NOT NULL DEFAULT false,\n  trace_id CHAR(32)\n);\nCREATE INDEX idx_deprovision_subject ON portal_deprovision_log(subject_id, initiated_at DESC);\nALTER TABLE portal_deprovision_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_deprovision_log_rls ON portal_deprovision_log\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_deprovision_log FROM cyberos_app;\nGRANT UPDATE (completed_at, sync_phase_duration_ms, async_phase_duration_ms, cascade_targets, sla_breach)\n  ON portal_deprovision_log TO cyberos_app;\n\n-- 0010_portal_jwt_blacklist.sql\nCREATE TABLE portal_jwt_blacklist (\n  jti UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  subject_id UUID NOT NULL,\n  engagement_id UUID,\n  blacklisted_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  expires_at TIMESTAMPTZ NOT NULL,\n  reason TEXT NOT NULL\n);\nCREATE INDEX idx_jwt_blacklist_subject ON portal_jwt_blacklist(subject_id, blacklisted_at DESC);\nCREATE INDEX idx_jwt_blacklist_expiry ON portal_jwt_blacklist(expires_at);\nALTER TABLE portal_jwt_blacklist ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_jwt_blacklist_rls ON portal_jwt_blacklist\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_jwt_blacklist FROM cyberos_app;\nGRANT DELETE ON portal_jwt_blacklist TO cyberos_pruner;  -- auto-trim past expires_at\n\n-- 0011_portal_restore_requests.sql\nCREATE TABLE portal_restore_requests (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  subject_id UUID NOT NULL,\n  requested_by_subject_id UUID NOT NULL,\n  reason TEXT NOT NULL,\n  requested_at TIMESTAMPTZ NOT NULL DEFAULT now()\n);\nALTER TABLE portal_restore_requests ENABLE ROW LEVEL SECURITY;\nCREATE POLICY portal_restore_requests_rls ON portal_restore_requests\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON portal_restore_requests FROM cyberos_app;</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/portal/src/deprovision/mod.rs\n#[derive(Debug, serde::Serialize)]\npub struct DeprovisionResult {\n    pub subject_id: Uuid,\n    pub engagement_id: Uuid,\n    pub sync_phase_completed_at: DateTime&lt;Utc&gt;,\n    pub async_phase_completed_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    pub sla_breach: bool,\n    pub cascade_targets_completed: Vec&lt;CascadeTarget&gt;,\n}\n\n#[derive(Debug, serde::Serialize)]\npub enum CascadeTarget { GenieSessions, ScopedViews, ChannelMemberships, McpConfirmations }</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">DELETE /scim/v2/{engagement_slug}/Users/{id}                        (SCIM bearer)\nPUT    /scim/v2/{engagement_slug}/Users/{id}                        (SCIM bearer; grace-period reactivate)\nPOST   /v1/admin/engagements/{eng_id}/subjects/{subject_id}/restore (tenant_admin)\nGET    /v1/admin/tenants/{tenant_id}/deprovision-log                (tenant_admin or cfo)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>SCIM DELETE returns 204 &lt; 1s</strong> — DELETE handler sync phase completes within 1 second wall-clock.</li><li><strong>JWT invalidated within 30s</strong> — post-DELETE, REST API call with the user's prior JWT returns 401 <code>jwt_revoked</code> within 30 seconds end-to-end.</li><li><strong>WebSocket force-closed</strong> — active WS connection receives close frame code 4001 + reason <code>scim_deprovision</code>.</li><li><strong>Cascade to Genie sessions</strong> — user's <code>portal_genie_sessions</code> rows updated with <code>revoked_at</code>.</li><li><strong>Cascade to channel memberships</strong> — user's <code>chat_channel_memberships</code> updated with <code>revoked_at</code>.</li><li><strong>Cascade to MCP pending confirmations</strong> — user's <code>mcp_pending_confirmations</code> deleted.</li><li><strong>Idempotent re-DELETE</strong> — DELETE on already-tombstoned subject returns 204 + no audit re-emit.</li><li><strong>Idempotent re-DELETE post-purge</strong> — DELETE on hard-purged subject returns 404 <code>noTarget</code>.</li><li><strong>Grace-period SCIM PUT reactivates</strong> — PUT within 5 min restores subject + reverses blacklist + emits <code>portal.scim_user_restored</code>.</li><li><strong>Beyond-grace SCIM PUT rejected</strong> — PUT 6 min post-DELETE returns 409 <code>grace_period_expired</code>.</li><li><strong>Admin restore tenant_admin only</strong> — engagement_admin POST restore returns 403; tenant_admin returns 200 + audit row.</li><li><strong>Per-Engagement isolation</strong> — user member of Eng X + Y; DELETE in X revokes only X's sessions; Y sessions still valid.</li><li><strong>Rate-limit 1000/min/Engagement</strong> — 1001st DELETE in 60s returns 429.</li><li><strong>JWT blacklist propagation &lt; 50ms p95</strong> — multi-node test: blacklist add on node A is visible to node B within 50ms.</li><li><strong>30s SLO breach alerted</strong> — fixture-injected delay &gt; 30s triggers <code>portal.scim_deprovision_sla_breach</code> sev-1.</li><li><strong>Nightly reconciliation catches orphans</strong> — fixture-tombstoned subject with active session → reconciliation auto-revokes + emits sev-1 orphan row.</li><li><strong>7 memory audit kinds emitted</strong> — full lifecycle covers all 7 (deprovisioned + restored + cascade × 4 targets + propagated + force_closed + grace_expired + sla_breach).</li><li><strong>PII scrub</strong> — audit row carries <code>email_hash16</code> not raw email; <code>subject_id</code> UUID retained.</li><li><strong>Hard purge returns 501 at slice 2</strong> — <code>?action=hard_purge</code> query param returns 501 <code>not_implemented</code>.</li><li><strong>Trace_id end-to-end</strong> — single trace_id across SCIM DELETE + sync phase + async phase + cascade row + reconciliation row.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>scim_delete_invalidates_jwt_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn jwt_invalidated_within_30s() {\n    let ctx = TestContext::with_engagement_subject_with_jwt(\"alice@acme.com\").await;\n    let jwt = ctx.subject_jwt.clone();\n\n    // pre-DELETE: JWT works\n    let r0 = ctx.get(\"/v1/portal/profile\").bearer_auth(&amp;jwt).send().await.unwrap();\n    assert_eq!(r0.status(), 200);\n\n    // SCIM DELETE\n    let start = Instant::now();\n    let r1 = ctx.scim_delete_user(ctx.subject_id).await;\n    assert_eq!(r1.status(), 204);\n    assert!(start.elapsed() &lt; Duration::from_secs(1));\n\n    // poll until JWT rejected (max 30s)\n    let deadline = Instant::now() + Duration::from_secs(30);\n    loop {\n        let r = ctx.get(\"/v1/portal/profile\").bearer_auth(&amp;jwt).send().await.unwrap();\n        if r.status() == 401 { break; }\n        if Instant::now() &gt;= deadline { panic!(\"JWT still valid after 30s\"); }\n        tokio::time::sleep(Duration::from_millis(100)).await;\n    }\n}</code></pre>\n<h3>5.2 <code>scim_delete_closes_websocket_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn websocket_force_closed_on_deprovision() {\n    let ctx = TestContext::with_engagement_subject_with_jwt(\"alice@acme.com\").await;\n    let mut ws = ctx.open_chat_websocket(&amp;ctx.subject_jwt).await;\n    ctx.scim_delete_user(ctx.subject_id).await;\n    let close_event = tokio::time::timeout(Duration::from_secs(30), ws.next_close_frame()).await\n        .expect(\"WS close frame not received within 30s\");\n    assert_eq!(close_event.code, 4001);\n    assert_eq!(close_event.reason, \"scim_deprovision\");\n}</code></pre>\n<h3>5.3 <code>scim_delete_cascade_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn cascade_revokes_all_4_targets() {\n    let ctx = TestContext::with_full_session(\"alice@acme.com\").await;  // creates Genie + CHAT + MCP confirms\n    ctx.scim_delete_user(ctx.subject_id).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;  // allow async phase\n\n    let genie_revoked: Option&lt;DateTime&lt;Utc&gt;&gt; = sqlx::query_scalar(\n        \"SELECT revoked_at FROM portal_genie_sessions WHERE subject_id=$1\"\n    ).bind(ctx.subject_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(genie_revoked.is_some());\n\n    let chat_memberships: Vec&lt;Option&lt;DateTime&lt;Utc&gt;&gt;&gt; = sqlx::query_scalar(\n        \"SELECT revoked_at FROM chat_channel_memberships WHERE subject_id=$1\"\n    ).bind(ctx.subject_id).fetch_all(&amp;ctx.pool).await.unwrap();\n    assert!(chat_memberships.iter().all(|r| r.is_some()));\n\n    let mcp_confirms: i64 = sqlx::query_scalar(\n        \"SELECT count(*) FROM mcp_pending_confirmations WHERE caller_subject_id=$1\"\n    ).bind(ctx.subject_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(mcp_confirms, 0);\n}</code></pre>\n<h3>5.4 <code>scim_delete_30s_sla_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn 30s_sla_p99_under_load() {\n    let ctx = TestContext::with_n_subjects(100).await;\n    let mut durations = Vec::new();\n    for subject_id in ctx.subject_ids.iter() {\n        let start = Instant::now();\n        ctx.scim_delete_user(*subject_id).await;\n        ctx.wait_until_fully_revoked(*subject_id).await;\n        durations.push(start.elapsed());\n    }\n    durations.sort();\n    let p99 = durations[(durations.len() as f64 * 0.99) as usize];\n    assert!(p99 &lt; Duration::from_secs(30), \"actual p99 {:?}\", p99);\n}</code></pre>\n<h3>5.5 <code>scim_grace_period_undelete_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn put_within_grace_reactivates() {\n    let ctx = TestContext::with_engagement_subject_with_jwt(\"alice@acme.com\").await;\n    ctx.scim_delete_user(ctx.subject_id).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    let r = ctx.scim_put_user(ctx.subject_id, json!({\"userName\": \"alice\"})).await;\n    assert_eq!(r.status(), 200);\n\n    let status: String = sqlx::query_scalar(\"SELECT status FROM subjects WHERE id=$1\")\n        .bind(ctx.subject_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(status, \"active\");\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.scim_user_restored\"\n        &amp;&amp; r.payload[\"restored_via\"] == \"scim_put_within_grace\"));\n}\n\n#[tokio::test]\nasync fn put_beyond_grace_rejected() {\n    let ctx = TestContext::with_engagement_subject_with_jwt(\"alice@acme.com\").await;\n    ctx.scim_delete_user(ctx.subject_id).await;\n    ctx.travel_clock_forward(Duration::from_secs(360)).await;  // 6 min\n\n    let r = ctx.scim_put_user(ctx.subject_id, json!({\"userName\": \"alice\"})).await;\n    assert_eq!(r.status(), 409);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"grace_period_expired\");\n}</code></pre>\n<h3>5.6 <code>scim_admin_restore_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn admin_restore_tenant_admin_only() {\n    let ctx = TestContext::with_engagement_subject_with_jwt(\"alice@acme.com\").await;\n    ctx.scim_delete_user(ctx.subject_id).await;\n    ctx.travel_clock_forward(Duration::from_secs(360)).await;\n\n    let eng_admin_jwt = ctx.mint_jwt_with_role(ctx.tenant, \"engagement_admin\");\n    let r = ctx.post(&amp;format!(\"/v1/admin/engagements/{}/subjects/{}/restore\", ctx.eng_id, ctx.subject_id))\n        .bearer_auth(eng_admin_jwt).json(&amp;json!({\"reason\": \"reinstated\"})).send().await.unwrap();\n    assert_eq!(r.status(), 403);\n\n    let tenant_admin_jwt = ctx.mint_jwt_with_role(ctx.tenant, \"tenant_admin\");\n    let r = ctx.post(&amp;format!(\"/v1/admin/engagements/{}/subjects/{}/restore\", ctx.eng_id, ctx.subject_id))\n        .bearer_auth(tenant_admin_jwt).json(&amp;json!({\"reason\": \"reinstated\"})).send().await.unwrap();\n    assert_eq!(r.status(), 200);\n}</code></pre>\n<h3>5.7 <code>scim_per_engagement_isolation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn delete_in_one_engagement_preserves_other() {\n    let ctx = TestContext::with_subject_in_two_engagements(\"alice@acme.com\", \"eng-x\", \"eng-y\").await;\n    let jwt_x = ctx.mint_jwt_for(ctx.subject_id, ctx.eng_x_id).await;\n    let jwt_y = ctx.mint_jwt_for(ctx.subject_id, ctx.eng_y_id).await;\n\n    ctx.scim_delete_user_in_engagement(ctx.subject_id, \"eng-x\").await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n\n    let r_x = ctx.get(\"/v1/portal/profile\").bearer_auth(&amp;jwt_x).send().await.unwrap();\n    assert_eq!(r_x.status(), 401);  // X-scoped JWT revoked\n    let r_y = ctx.get(\"/v1/portal/profile\").bearer_auth(&amp;jwt_y).send().await.unwrap();\n    assert_eq!(r_y.status(), 200);  // Y-scoped JWT still valid\n}</code></pre>\n<h3>5.8 <code>scim_orphan_reconciliation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn nightly_reconciliation_catches_orphan_session() {\n    let ctx = TestContext::with_engagement_subject_with_jwt(\"alice@acme.com\").await;\n    // Tombstone subject without going through normal SCIM (simulate cascade failure)\n    sqlx::query(\"UPDATE subjects SET status='tombstoned', tombstoned_at=now() WHERE id=$1\")\n        .bind(ctx.subject_id).execute(&amp;ctx.pool).await.unwrap();\n    // Session row remains active (orphan)\n\n    ctx.travel_clock_forward(Duration::from_days(1)).await;\n    ctx.run_reconciliation_job().await;\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"portal.scim_orphan_session_detected\" &amp;&amp; r.severity == 1));\n    let session_revoked: Option&lt;DateTime&lt;Utc&gt;&gt; = sqlx::query_scalar(\n        \"SELECT revoked_at FROM auth_sessions WHERE subject_id=$1\"\n    ).bind(ctx.subject_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(session_revoked.is_some());\n}</code></pre>\n<h3>5.9 <code>scim_jwt_blacklist_propagation_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn blacklist_propagates_under_50ms_p95() {\n    let ctx = TestContext::with_two_gateway_nodes().await;\n    let subject = ctx.provision_subject(\"alice@acme.com\").await;\n    let jwt = ctx.mint_jwt_for(subject, ctx.eng_id).await;\n\n    let mut latencies = Vec::new();\n    for _ in 0..100 {\n        ctx.add_to_blacklist_via_node_a(&amp;jwt).await;\n        let start = Instant::now();\n        loop {\n            let r = ctx.get_via_node_b(\"/v1/portal/profile\").bearer_auth(&amp;jwt).send().await.unwrap();\n            if r.status() == 401 { latencies.push(start.elapsed()); break; }\n            if start.elapsed() &gt; Duration::from_millis(500) { panic!(\"propagation &gt; 500ms\"); }\n        }\n        ctx.remove_from_blacklist(&amp;jwt).await;\n    }\n    latencies.sort();\n    let p95 = latencies[(latencies.len() as f64 * 0.95) as usize];\n    assert!(p95 &lt; Duration::from_millis(50));\n}</code></pre>\n<h3>5.10 <code>scim_audit_emission_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn full_lifecycle_emits_7_kinds() {\n    let ctx = TestContext::with_full_session(\"alice@acme.com\").await;\n    ctx.scim_delete_user(ctx.subject_id).await;\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    ctx.travel_clock_forward(Duration::from_secs(360)).await;\n    let _ = ctx.scim_put_user(ctx.subject_id, json!({\"userName\":\"alice\"})).await;  // grace expired\n    ctx.admin_restore(ctx.subject_id).await;\n\n    let kinds: Vec&lt;&amp;str&gt; = ctx.memory_rows().await.iter().map(|r| r.kind.as_str()).collect();\n    for expected in &amp;[\n        \"portal.scim_user_deprovisioned\", \"portal.scim_user_restored\",\n        \"portal.scim_cascade_revocation\", \"portal.jwt_blacklist_propagated\",\n        \"portal.websocket_force_closed\", \"portal.scim_grace_period_expired\",\n    ] {\n        assert!(kinds.contains(expected), \"missing {expected}\");\n    }\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<h3>6.1 Deprovision orchestrator (sync + async split)</h3>\n<pre><code class=\"language-rust\">// services/portal/src/deprovision/mod.rs\npub async fn run(ctx: AppCtx, engagement_id: Uuid, subject_id: Uuid) -&gt; Result&lt;(), DeprovError&gt; {\n    let start = Instant::now();\n    let trace_id = otel_trace_id();\n\n    // Sync phase &lt; 1s\n    let sync_start = Instant::now();\n    sync_phase::execute(&amp;ctx, subject_id, engagement_id, &amp;trace_id).await?;\n    let sync_ms = sync_start.elapsed().as_millis() as i32;\n\n    // Insert deprovision log row at sync-phase completion\n    let log_id = ctx.repo.deprovision_log.insert(\n        engagement_id, subject_id, \"soft_tombstone\", sync_ms, &amp;trace_id\n    ).await?;\n\n    // Async phase via tokio::spawn with 30s deadline\n    let async_ctx = ctx.clone();\n    tokio::spawn(async move {\n        let async_start = Instant::now();\n        let result = tokio::time::timeout(\n            Duration::from_secs(30),\n            async_phase::execute(&amp;async_ctx, subject_id, engagement_id, &amp;trace_id),\n        ).await;\n        let async_ms = async_start.elapsed().as_millis() as i32;\n        let breach = result.is_err() || async_start.elapsed() &gt; Duration::from_secs(30);\n        async_ctx.repo.deprovision_log.complete(log_id, async_ms, breach).await.ok();\n        if breach {\n            emit_audit(&amp;async_ctx, \"portal.scim_deprovision_sla_breach\", json!({\n                \"subject_id\": subject_id, \"engagement_id\": engagement_id,\n                \"duration_ms\": async_ms,\n            })).await;\n        }\n    });\n\n    // Caller (SCIM handler) returns 204 immediately after sync phase\n    Ok(())\n}</code></pre>\n<h3>6.2 JWT blacklist (Redis + Postgres)</h3>\n<pre><code class=\"language-rust\">// services/portal/src/deprovision/jwt_blacklist.rs\npub async fn add(ctx: &amp;AppCtx, jti: Uuid, tenant: Uuid, subject: Uuid, eng: Uuid, expires_at: DateTime&lt;Utc&gt;) -&gt; Result&lt;(), Err&gt; {\n    sqlx::query(\"INSERT INTO portal_jwt_blacklist (jti, tenant_id, subject_id, engagement_id, expires_at, reason)\n                 VALUES ($1, $2, $3, $4, $5, 'scim_deprovision')\n                 ON CONFLICT (jti) DO NOTHING\")\n        .bind(jti).bind(tenant).bind(subject).bind(eng).bind(expires_at)\n        .execute(&amp;ctx.pool).await?;\n\n    let mut conn = ctx.redis.get().await?;\n    let score = expires_at.timestamp() as f64;\n    conn.zadd::&lt;_, _, _, ()&gt;(format!(\"jwt_blacklist:{tenant}\"), jti.to_string(), score).await?;\n    conn.publish::&lt;_, _, ()&gt;(\"jwt_blacklist_update\", serde_json::to_string(&amp;jti)?).await?;\n    Ok(())\n}\n\npub async fn is_blacklisted(ctx: &amp;AppCtx, jwt_claims: &amp;JwtClaims) -&gt; Result&lt;bool, Err&gt; {\n    let mut conn = ctx.redis.get().await?;\n    let in_redis: bool = conn.zscore::&lt;_, _, Option&lt;f64&gt;&gt;(\n        format!(\"jwt_blacklist:{}\", jwt_claims.tenant_id),\n        jwt_claims.jti.to_string()\n    ).await?.is_some();\n    if in_redis { return Ok(true); }\n    // Postgres fallback (Redis cold cache)\n    let pg_hit: Option&lt;(Uuid,)&gt; = sqlx::query_as(\n        \"SELECT jti FROM portal_jwt_blacklist WHERE jti=$1 AND expires_at &gt; now()\"\n    ).bind(jwt_claims.jti).fetch_optional(&amp;ctx.pool).await?;\n    if pg_hit.is_some() {\n        // backfill Redis\n        let _ = conn.zadd::&lt;_, _, _, ()&gt;(\n            format!(\"jwt_blacklist:{}\", jwt_claims.tenant_id),\n            jwt_claims.jti.to_string(), jwt_claims.exp as f64\n        ).await;\n    }\n    Ok(pg_hit.is_some())\n}</code></pre>\n<h3>6.3 WebSocket killer</h3>\n<pre><code class=\"language-rust\">// services/portal/src/deprovision/websocket_killer.rs\npub async fn close_for_subject(ctx: &amp;AppCtx, subject_id: Uuid, engagement_id: Uuid) -&gt; Vec&lt;Uuid&gt; {\n    let connections = ctx.ws_registry.list_for_subject(subject_id, engagement_id).await;\n    let mut closed = Vec::new();\n    for conn in connections {\n        let close = CloseFrame { code: 4001, reason: \"scim_deprovision\".into() };\n        if conn.send_close(close).await.is_ok() {\n            closed.push(conn.id);\n        }\n    }\n    closed\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>TASK-PORTAL-003</strong> External IdP + SCIM JIT — SCIM Users endpoint extended here for DELETE.</li></ul>\n<p><strong>Cross-module (related_tasks):</strong></p>\n<ul><li><strong>TASK-PORTAL-001</strong> Scoped views — cascade target.</li><li><strong>TASK-PORTAL-005</strong> Branded Genie chat — cascade target.</li><li><strong>TASK-AUTH-002</strong> Subject create — subject tombstone semantics.</li><li><strong>TASK-AUTH-004</strong> JWT mint + validate — blacklist consulted at validate.</li><li><strong>TASK-AUTH-101</strong> RBAC — role gates.</li><li><strong>TASK-TEN-104</strong> Lifecycle — deprovision may trigger if last admin lost.</li><li><strong>TASK-CHAT-005</strong> CHAT membership — cascade target.</li><li><strong>TASK-AI-003</strong> memory audit-row bridge — 7 new kinds.</li><li><strong>TASK-MEMORY-111</strong> PII scrubbing — email hash in chain.</li><li><strong>TASK-OBS-007</strong> Auto-runbook — sev-1 SLA-breach + orphan-session alerts route to CHAT.</li></ul>\n<p><strong>Downstream (blocks):</strong> None.</p>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>portal.scim_user_deprovisioned</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.scim_user_deprovisioned\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.portal.scim\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:32.847Z\",\n  \"payload\": {\n    \"engagement_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n    \"subject_id\": \"7c4e9a2b-1d3f-4e6a-bccc-000000000042\",\n    \"email_hash16\": \"f8a1b2c3d4e5f607\",\n    \"external_id_hash16\": \"9c4e7a8b6d2f1e3a\",\n    \"action\": \"soft_tombstone\",\n    \"sync_phase_duration_ms\": 87,\n    \"active_jwts_blacklisted_count\": 3,\n    \"active_websockets_to_close_count\": 2,\n    \"grace_period_until\": \"2026-05-17T09:19:32.847Z\"\n  }\n}</code></pre>\n<h3>8.2 <code>portal.scim_deprovision_sla_breach</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"portal.scim_deprovision_sla_breach\",\n  \"severity\": 1,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.portal.scim\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:15:05.118Z\",\n  \"payload\": {\n    \"subject_id\": \"7c4e...\",\n    \"engagement_id\": \"0190...\",\n    \"actual_duration_ms\": 34218,\n    \"sla_ms\": 30000,\n    \"cascade_targets_pending\": [\"genie_sessions\"],\n    \"remediation\": \"nightly_reconciliation_will_clean_up\"\n  }\n}</code></pre>\n<h3>8.3 Admin restore request</h3>\n<pre><code class=\"language-json\">{\n  \"reason\": \"Wrong user deprovisioned by Azure AD glitch; user verified active employee\"\n}</code></pre>\n<h3>8.4 WebSocket close frame</h3>\n<pre><code class=\"language-text\">WebSocket Close frame:\n  Code: 4001\n  Reason: \"scim_deprovision\"\n\nClient receives + should reconnect; re-auth fails with 401 jwt_revoked.\nClient UI: \"You've been signed out by your administrator.\"</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 2. Deferred:</p>\n<ul><li><strong>Deferred:</strong> Hard purge (GDPR Art. 17) flow — slice 3, task-AUTH-2xx (placeholder).</li><li><strong>Deferred:</strong> Webhook-based deprovision (non-SCIM channel) — slice 3.</li><li><strong>Deferred:</strong> Multi-actor deprovision approval (M-of-N for executive accounts) — slice 3.</li><li><strong>Deferred:</strong> Deprovision dry-run mode (preview cascade impact) — slice 3.</li><li><strong>Deferred:</strong> SCIM Bulk endpoint (RFC 7644 §3.7) — slice 3, task-PORTAL-3xx.</li><li><strong>Deferred:</strong> Per-Engagement grace period override (some Engagements want 0 min, some want 30 min) — slice 3.</li><li><strong>Deferred:</strong> Deprovision report export (PDF for compliance) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Sync phase exceeds 1s</td><td>OTel histogram alert</td><td>204 still returned; sev-2 alert</td><td>Operator investigates DB/Redis latency</td></tr><tr><td>Async phase exceeds 30s</td><td>deadline timeout</td><td><code>sla_breach=true</code> in log row; sev-1 audit</td><td>Nightly reconciliation cleans up; investigate cause</td></tr><tr><td>Redis pub/sub propagation &gt; 50ms</td><td>latency metric</td><td>Gateway nodes catch up via SISMEMBER fallback; sev-2 alert</td><td>Redis cluster investigation</td></tr><tr><td>Postgres unavailable during blacklist write</td><td>sqlx error</td><td>Redis-only blacklist works (best-effort); sev-1 alert; nightly reconciliation re-syncs to Postgres</td><td>DB recovery</td></tr><tr><td>Cascade target throws (e.g. CHAT service down)</td><td>per-target error caught</td><td>Other targets proceed; failed target retried via reconciliation</td><td>CHAT recovers; reconciliation re-attempts</td></tr><tr><td>WebSocket close fails (connection already broken)</td><td>send_close returns Err</td><td>Logged; counts as success (no-op)</td><td>Inherent</td></tr><tr><td>SCIM PUT within grace race with concurrent admin restore</td><td>tx isolation</td><td>Last writer wins; both emit audit rows; idempotent</td><td>Inherent</td></tr><tr><td>Hard_purge requested at slice 2</td><td>query param check</td><td>501 + <code>not_implemented_slice_2</code></td><td>Tenant_admin uses GDPR flow when shipped</td></tr><tr><td>Re-DELETE on tombstoned subject</td><td>status check</td><td>204 + no re-emit</td><td>Inherent idempotency</td></tr><tr><td>Re-DELETE on purged subject</td><td>status check</td><td>404 + <code>noTarget</code></td><td>Inherent</td></tr><tr><td>Per-Engagement isolation bypass (cross-engagement blacklist)</td><td>scope check</td><td>Sev-1 alert; engagement-other JWTs unaffected</td><td>Investigate handler; fix</td></tr><tr><td>Orphan session detected at reconciliation</td><td>nightly job</td><td>Auto-revoke + sev-1 audit</td><td>Inherent</td></tr><tr><td>Reconciliation job itself fails</td><td>OBS alert</td><td>Sev-1; manual reconciliation via CLI</td><td>Operator runs reconciliation manually</td></tr><tr><td>Rate-limit hit (1000/min/Eng)</td><td>counter</td><td>429 + Retry-After</td><td>Caller backs off; legitimate org-wide cleanup spread</td></tr><tr><td>Multi-node propagation race (node A blacklists, node B serves old JWT briefly)</td><td>latency window</td><td>&lt; 50ms p95 window; acceptable</td><td>Inherent</td></tr><tr><td>Restore endpoint called on non-tombstoned subject</td><td>status check</td><td>409 + <code>not_tombstoned</code></td><td>Caller verifies state first</td></tr><tr><td>Restore endpoint called on hard-purged subject</td><td>status check</td><td>404 + <code>noTarget</code></td><td>Hard purge is one-way</td></tr><tr><td>WebSocket close code 4001 misinterpreted by client</td><td>client-side bug</td><td>Client may retry; will fail 401 + see clearer error</td><td>Client UI improvements</td></tr><tr><td>Grace-period clock skew across nodes</td><td>NTP-bounded</td><td>&lt; 1s skew; 5-min grace tolerates</td><td>Inherent</td></tr><tr><td>MCP confirmation cascade race (concurrent confirm + delete)</td><td>atomic SELECT FOR UPDATE</td><td>First writer wins; second blocked</td><td>Inherent</td></tr><tr><td><code>cyberos_pruner</code> role not present for blacklist auto-trim</td><td>migration gate</td><td>Trimming via cyberos_app role at degraded mode</td><td>Operator adds role</td></tr><tr><td>Forced WS close on a paused connection</td><td>TCP-level retransmit</td><td>Eventually delivered; close still effective</td><td>Inherent</td></tr><tr><td>Audit row insert fails post-deprovision</td><td>Postgres error</td><td>Subject still tombstoned (sync phase committed); sev-2 alert; OBS log retains forensic trace</td><td>Operator backfills audit if needed</td></tr><tr><td>Foreign-Engagement JWT presented during deprovision window</td><td>scope check at validator</td><td>Correct rejection (foreign JWT was already invalid in this Engagement)</td><td>Inherent</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Redis sorted-set storage for blacklist — auto-pruning past expires_at via <code>ZREMRANGEBYSCORE</code> scheduled task hourly.</p>\n<p><strong>§11.2</strong> WebSocket close code 4001 — chosen from RFC 6455 application-reserved range (4000-4999); documented in our public API docs so client libraries can handle it.</p>\n<p><strong>§11.3</strong> Tombstone vs delete: subjects table has a <code>status</code> column with <code>tombstoned</code> value; row remains for audit forensic (and possible grace-period restore).</p>\n<p><strong>§11.4</strong> Grace period clock-skew tolerance: 5-min window with up to 1s NTP skew across nodes is safe; clients shouldn't be cutting it close.</p>\n<p><strong>§11.5</strong> The async cascade phase uses <code>futures::future::join_all</code> for parallel target invocation; failures per-target are logged but don't abort other targets.</p>\n<p><strong>§11.6</strong> Reconciliation job runs nightly at 03:00 UTC; light query (subject status='tombstoned' + active session count); typically &lt; 30s for 10k tombstoned subjects.</p>\n<p><strong>§11.7</strong> PII hash function: <code>email_hash16 = encode(substring(digest(lower(email) || global_salt, 'sha256') from 1 for 8), 'hex')</code> — matches TASK-TEN-101 + TASK-PORTAL-003 patterns.</p>\n<p><strong>§11.8</strong> The <code>auth.subjects.status</code> enum is shared with TASK-AUTH-002; this task adds <code>tombstoned</code> value via migration referenced in modified_files.</p>\n<p><strong>§11.9</strong> <code>auth_sessions</code> table also shared with TASK-AUTH-004; column <code>revoked_at</code> + <code>revoked_reason</code> added via migration; TASK-AUTH-004 modified to set them.</p>\n<p><strong>§11.10</strong> Per-Engagement JWT scoping: each JWT carries <code>engagement_id</code> claim (TASK-PORTAL-003 derivative); blacklist scoped to <code>(tenant_id, engagement_id)</code> partition.</p>\n<p><strong>§11.11</strong> Cascade target ordering: parallel (no order); each target is idempotent; safe to retry on partial failure.</p>\n<p><strong>§11.12</strong> The reconciliation job's missing-cascade auto-fix triggers another audit row (<code>portal.scim_cascade_revocation</code> via reconciliation path); deduped by <code>(subject_id, target)</code> last-30d.</p>\n<p><strong>§11.13</strong> Restore endpoint reactivates subject status + clears blacklist + restores cascade — but does NOT re-open closed WebSocket connections (impossible; client must re-connect).</p>\n<p><strong>§11.14</strong> The 30s SLO is end-to-end (SCIM DELETE accept → last cascade complete); intermediate phases have their own SLOs (sync 1s, blacklist propagation 50ms p95).</p>\n<p><strong>§11.15</strong> Hard_purge query param reserved for slice 3 GDPR flow; rejecting with 501 makes the missing capability visible (vs silent fall-through to soft tombstone).</p>\n<p><strong>§11.16</strong> Restore via admin endpoint is rate-limited at 10/min/tenant (default; tenant_admin can tune in slice 3) — unusual operation, should not be high-volume.</p>\n<p><strong>§11.17</strong> Per-Engagement audit-log RLS prevents one Engagement's deprovision history from being read by another Engagement's tenant_admin (cross-Engagement read leak is a regulatory concern).</p>\n<p><strong>§11.18</strong> WebSocket killer iterates Redis-backed connection registry (multi-node) for completeness; missing one node's connections = blacklist still catches at next message (JWT validate hits blacklist).</p>\n<p><strong>§11.19</strong> Trace_id propagation: SCIM DELETE handler generates trace_id; passes via <code>tracing::instrument</code> context into sync phase + async spawn + cascade targets; all audit rows carry the same trace_id.</p>\n<p><strong>§11.20</strong> The 7-kind core list (§1 #17) plus <code>portal.scim_orphan_session_detected</code> (§1 #16) totals 8 memory audit kinds for this task; TASK-AI-003 closed-set extension covers all 8.</p>\n<p><strong>§11.21</strong> Per-PSP-like multi-tenant abstraction not needed here — SCIM is the single canonical channel; webhook-based deprovision deferred per DEC-1098.</p>\n<hr>\n<p><em>End of TASK-PORTAL-004 spec.</em></p>";
