window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MEMORY-104"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A Tauri 2.x desktop app <strong>MUST</strong> bundle the memory sync daemon + a minimal UI for inspecting + searching local memory. Each component:</p>\n<ol><li><strong>MUST</strong> ship for macOS (universal Apple Silicon + Intel; signed + notarised), Windows (x64; EV-cert signed), Linux (.deb + AppImage). Each platform's release verified by OS-native code-signing checks.</li><li><strong>MUST</strong> auto-update via Tauri's built-in updater. Update manifests signed with a release-signing Ed25519 key; client verifies signature before applying. Signature failure → rollback + sev-2 alert; user notified via in-app banner.</li><li><strong>MUST</strong> run memory-sync (TASK-MEMORY-103) as an internal Tauri-managed process via <code>tauri::async_runtime::spawn</code>. The supervisor monitors health; auto-restart on panic with exponential backoff.</li><li><strong>MUST</strong> expose a system tray icon with quick actions:</li></ol>\n<ul><li>Open memory main window.</li><li>Force sync now.</li><li>Show recent memories (last 10 from local Layer 1).</li><li>Toggle sync (pause/resume).</li><li>Quit.</li></ul>\n<ol><li><strong>MUST</strong> show on the main dashboard:</li></ol>\n<ul><li>Current chain head (hex; copyable).</li><li>Last sync time + duration.</li><li>Disputed pair count (badge; clicks to TASK-MEMORY-105 resolution UI).</li><li>sync_class breakdown (count of shareable vs private rows).</li><li>Total memory count + storage used.</li><li>Cloud memory connection state (online/offline).</li></ul>\n<ol><li><strong>MUST</strong> support local search via TASK-MEMORY-108 API. Search box on dashboard; results pane with memory previews; click → open memory file.</li><li><strong>MUST</strong> request macOS Full Disk Access at first run (one-time; persisted by macOS). Without FDA, the app cannot read <code>~/.cyberos/memory/store/</code> (System Integrity Protection blocks). The prompt directs user to System Settings &gt; Privacy &amp; Security &gt; Full Disk Access &gt; [Memory.app toggle].</li><li><strong>MUST</strong> sandbox per OS:</li></ol>\n<ul><li>macOS: hardened runtime + entitlements (no JIT, no debug, FDA only).</li><li>Windows: AppContainer with capabilities (file system access for <code>%USERPROFILE%/.cyberos/memory/store/</code>, network for Cloud memory).</li><li>Linux: AppArmor profile (where supported).</li></ul>\n<ol><li><strong>MUST</strong> support quick-capture: tray-accessible textbox; user types → app writes a <code>quick_note</code> memory row with auto-tag (date, source: tray, originator_device).</li><li><strong>MUST</strong> sign update manifests with the release-signing Ed25519 key (separate from memory signing key). Public key embedded in app binary at compile time; rotation requires app rebuild + re-release.</li><li><strong>MUST</strong> persist user settings (Cloud memory URL, sync interval, opt-in to crash reporting) in OS-standard config dir (<code>~/Library/Application Support/cyberos/memory/</code> macOS; <code>%APPDATA%/cyberos/memory/</code> Windows; <code>~/.config/cyberos/memory/</code> Linux).</li><li><strong>MUST</strong> support headless mode (Linux daemon only; no UI) via <code>--headless</code> flag for server installs.</li><li><strong>MUST</strong> target bundle size ≤ 30MB on macOS, ≤ 25MB on Windows. Tauri produces small binaries by design; size budget catches accidental dependency bloat.</li><li><strong>MUST</strong> report crashes via opt-in sentry-rust integration. Default opt-OUT (privacy-respecting); user can enable in Settings.</li><li><strong>SHOULD</strong> support multi-window (search in separate window; main dashboard always primary).</li><li><strong>SHOULD</strong> support localisation infrastructure for slice-3+ Vietnamese UI (i18n keys + translation files).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why Tauri 2.x over Electron (DEC-190)?</strong> Tauri produces 5-10× smaller binaries (Electron ~150MB; Tauri ~25MB). Native performance (Rust shell + native WebView vs Chromium). Rust-native means embedding memory daemon directly without IPC overhead. Trade-off: WebView2/WKWebView is platform-specific (vs Chromium's uniformity); mitigated by Svelte's compatibility.</p>\n<p><strong>Why signed + notarised on macOS (§1 #1)?</strong> macOS Gatekeeper blocks unsigned apps with scary warnings. Notarisation registers the app with Apple's malware-scan service. Without these, users can't install easily; with them, install is one-click.</p>\n<p><strong>Why EV cert on Windows (§1 #1)?</strong> Standard code-signing certs trigger SmartScreen warnings until reputation builds (months of installs). EV certs skip SmartScreen entirely. Cost is higher ($300-500/year vs $50) but worth it for first-impression.</p>\n<p><strong>Why auto-update via Tauri (DEC-193)?</strong> Manual updates miss security patches. Tauri's updater + signed manifests = secure, automatic. Rollback on signature failure prevents the \"compromised update server pushes malicious binary\" attack.</p>\n<p><strong>Why Full Disk Access prompt (DEC-192)?</strong> macOS SIP blocks app access to <code>~/.cyberos/memory/store/</code> by default. FDA is one-time per user; persists. The prompt is annoying once, then invisible. Without FDA, the app can't read its own data — broken UX.</p>\n<p><strong>Why hardened sandbox per OS (§1 #8)?</strong> Compromised app shouldn't be able to read other apps' data, install kernel extensions, etc. Hardened runtime + entitlements (macOS) + AppContainer (Windows) limits blast radius. Trade-off: more development friction; worth it for security posture.</p>\n<p><strong>Why quick-capture in tray (§1 #9)?</strong> Friction kills ad-hoc note-taking. Tray-accessible textbox = instant capture (Cmd+Shift+Space global hotkey, opens textbox). User types, presses Enter, row written. The auto-tag (date + source: tray) saves the manual taxonomy.</p>\n<p><strong>Why bundle size budget ≤ 30MB (§1 #13)?</strong> Download UX matters. 100MB+ binaries take minutes to download on slow connections; users abandon install. 30MB downloads in &lt;30s on most connections.</p>\n<p><strong>Why opt-in crash reporting (§1 #14)?</strong> Default-on crash reporting violates privacy expectations (memory is the user's personal memory app). Opt-in respects user agency; UX still benefits from crashes when users opt in.</p>\n<p><strong>Why headless mode (§1 #12)?</strong> Linux server installs (e.g., always-on home server) don't need UI. The <code>--headless</code> flag runs sync daemon only; no tray; no main window. Useful for ops infrastructure.</p>\n<p><strong>Why update-manifest signing (§1 #10)?</strong> Update server compromise (Cloudflare R2 breach) could push malicious binary. Signature on manifest + verification before applying = the user's app refuses tampered updates. Public key embedded in app binary means even a compromised update server can't trick the client.</p>\n<p><strong>Why config in OS-standard dirs (§1 #11)?</strong> Each OS has conventions; respecting them means standard backup tools work, OS migration tools handle config, IT admins know where to look.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/memory/desktop/src-tauri/src/commands.rs\nuse tauri::State;\n\n#[tauri::command]\nasync fn search_memory(query: String, state: State&lt;'_, AppState&gt;) -&gt; Result&lt;Vec&lt;SearchResult&gt;, String&gt; {\n    state.memory_search.search(&amp;query).await.map_err(|e| e.to_string())\n}\n\n#[tauri::command]\nasync fn write_quick_note(text: String, state: State&lt;'_, AppState&gt;) -&gt; Result&lt;String, String&gt; {\n    let row = QuickNoteRow {\n        text, ts_ns: chrono::Utc::now().timestamp_nanos(),\n        source: \"tray\".into(), device_id: state.device_id,\n        auto_tags: vec![\"quick_note\".into(), today_str()],\n    };\n    state.local_memory.write(row).await.map_err(|e| e.to_string())?;\n    Ok(\"written\".into())\n}\n\n#[tauri::command]\nasync fn get_sync_state(state: State&lt;'_, AppState&gt;) -&gt; Result&lt;SyncState, String&gt; {\n    Ok(SyncState {\n        chain_head: state.local_memory.chain_head().await,\n        last_sync_at: state.sync_supervisor.last_sync_at().await,\n        last_sync_duration_ms: state.sync_supervisor.last_sync_duration_ms().await,\n        disputed_pair_count: state.local_memory.disputed_pair_count().await,\n        sync_class_breakdown: state.local_memory.sync_class_counts().await,\n        cloud_state: state.sync_supervisor.cloud_state().await,\n    })\n}\n\n#[derive(Serialize)]\npub struct SyncState {\n    pub chain_head: String,\n    pub last_sync_at: Option&lt;DateTime&lt;Utc&gt;&gt;,\n    pub last_sync_duration_ms: u32,\n    pub disputed_pair_count: u32,\n    pub sync_class_breakdown: HashMap&lt;String, u32&gt;,\n    pub cloud_state: String,    // \"online\" | \"offline\" | \"syncing\"\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory/desktop/src-tauri/src/sync_supervisor.rs\npub struct SyncSupervisor {\n    handle: Arc&lt;Mutex&lt;Option&lt;tokio::task::JoinHandle&lt;()&gt;&gt;&gt;&gt;,\n    last_sync_at: Arc&lt;RwLock&lt;Option&lt;DateTime&lt;Utc&gt;&gt;&gt;&gt;,\n    cloud_state: Arc&lt;RwLock&lt;CloudState&gt;&gt;,\n}\n\nimpl SyncSupervisor {\n    pub async fn start(&amp;self, local: Arc&lt;LocalMemory&gt;, cloud: Arc&lt;CloudMemoryClient&gt;) {\n        let handle = tokio::spawn(async move {\n            let mut backoff = Duration::from_secs(1);\n            loop {\n                match memory_sync::sync_loop(&amp;local, &amp;cloud).await {\n                    Ok(()) =&gt; break,\n                    Err(e) =&gt; {\n                        tracing::warn!(error = %e, \"sync_loop crashed; restarting after {backoff:?}\");\n                        tokio::time::sleep(backoff).await;\n                        backoff = (backoff * 2).min(Duration::from_secs(300));\n                    }\n                }\n            }\n        });\n        *self.handle.lock().await = Some(handle);\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory/desktop/src-tauri/src/main.rs\n#[derive(clap::Parser)]\nstruct Cli {\n    #[arg(long)] headless: bool,\n}\n\n#[tokio::main]\nasync fn main() {\n    let cli = Cli::parse();\n\n    let local_memory = Arc::new(LocalMemory::open(memory_dir()).await.unwrap());\n    let cloud = Arc::new(CloudMemoryClient::connect_from_config().await.unwrap());\n    let supervisor = SyncSupervisor::new();\n    supervisor.start(local_memory.clone(), cloud).await;\n\n    if cli.headless {\n        // Linux server mode: no UI; just sync.\n        loop { tokio::time::sleep(Duration::from_secs(60)).await; }\n    }\n\n    tauri::Builder::default()\n        .plugin(tauri_plugin_updater::Builder::new().build())\n        .plugin(tauri_plugin_dialog::init())\n        .plugin(tauri_plugin_notification::init())\n        .setup(|app| {\n            permissions::request_full_disk_access(app)?;\n            tray::install(app)?;\n            Ok(())\n        })\n        .manage(AppState {\n            local_memory, sync_supervisor: supervisor,\n            memory_search: Arc::new(MemorySearch::new()),\n            device_id: load_device_id(),\n        })\n        .invoke_handler(tauri::generate_handler![search_memory, write_quick_note, get_sync_state])\n        .run(tauri::generate_context!())\n        .expect(\"error running memory app\");\n}</code></pre>\n<pre><code class=\"language-json\">// services/memory/desktop/src-tauri/tauri.conf.json (excerpt)\n{\n  \"tauri\": {\n    \"bundle\": {\n      \"identifier\": \"world.cyberos.memory\",\n      \"category\": \"Productivity\",\n      \"macOS\": {\n        \"frameworks\": [],\n        \"providerShortName\": \"CyberSkill\",\n        \"signingIdentity\": \"Developer ID Application: CyberSkill (TEAMID)\",\n        \"entitlements\": \"entitlements.plist\",\n        \"minimumSystemVersion\": \"12.0\"\n      },\n      \"windows\": {\n        \"certificateThumbprint\": \"&lt;EV cert thumbprint&gt;\",\n        \"digestAlgorithm\": \"sha256\",\n        \"timestampUrl\": \"http://timestamp.digicert.com\",\n        \"wix\": { \"language\": \"en-US\" }\n      }\n    },\n    \"updater\": {\n      \"active\": true,\n      \"endpoints\": [\"https://releases.cyberos.world/memory/{{target}}/{{current_version}}\"],\n      \"dialog\": false,\n      \"pubkey\": \"ed25519-public-key-base64...\"\n    }\n  }\n}</code></pre>\n<pre><code class=\"language-yaml\"># services/memory/desktop/.github/workflows/release.yml\nname: Release Memory App\non:\n  push: { tags: ['memory-v*'] }\n\njobs:\n  build:\n    strategy:\n      matrix:\n        platform: [macos-latest, windows-latest, ubuntu-latest]\n    runs-on: ${{ matrix.platform }}\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions-rust-lang/setup-rust-toolchain@v1\n      - uses: pnpm/action-setup@v3\n      - run: pnpm install\n      - name: Build\n        working-directory: apps/memory\n        run: cargo tauri build\n      - name: Sign macOS\n        if: matrix.platform == 'macos-latest'\n        run: ./scripts/sign-and-notarize-macos.sh\n        env:\n          APPLE_ID: ${{ secrets.APPLE_ID }}\n          APPLE_PASSWORD: ${{ secrets.APPLE_NOTARIZE_PASSWORD }}\n      - name: Sign Windows\n        if: matrix.platform == 'windows-latest'\n        run: ./scripts/sign-windows.sh\n      - name: Upload to R2\n        run: aws s3 cp ./target/release/bundle/ s3://releases-cyberos/memory/ --recursive\n        env:\n          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY }}\n          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_KEY }}\n          AWS_ENDPOINT_URL: https://&lt;account&gt;.r2.cloudflarestorage.com\n      - name: Generate signed update manifest\n        run: |\n          ./scripts/generate-update-manifest.sh \\\n              --version $GITHUB_REF_NAME \\\n              --signing-key /tmp/release-signing-key.ed25519 \\\n              --output update-manifest.json\n          aws s3 cp update-manifest.json s3://releases-cyberos/memory/latest.json</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>App installs on macOS (drag-to-Applications) without Gatekeeper warning.</li><li>App installs on Windows (.msi double-click) without SmartScreen warning.</li><li>App installs on Linux (.deb + AppImage both work).</li><li>Updater pulls new release within 24h of publish (synthetic test publishes v1.1; v1.0 client picks up).</li><li>Update signature failure → rollback + sev-2 alert + in-app banner.</li><li>Sync daemon visible in tray; toggle works (pause/resume).</li><li>Local search returns memories ≤ 250ms p95.</li><li>Disputed-pair count badge updates in real-time as new conflicts arise.</li><li>Quick-capture textbox writes a <code>quick_note</code> memory row with auto-tags.</li><li>Signed release verifies on every OS — <code>codesign --verify Memory.app</code> passes; Windows EV cert valid; Linux .deb dpkg-sig valid.</li><li>macOS Full Disk Access prompt appears on first run; persists after grant.</li><li>Sync supervisor restarts daemon on crash with exponential backoff.</li><li>Bundle size ≤ 30MB macOS / ≤ 25MB Windows.</li><li><code>--headless</code> flag runs sync daemon only; no UI.</li><li>Crash reporting opt-in default off; can be toggled in Settings.</li><li>Multi-window: search in separate window works.</li><li>Tray quick action \"Recent memories\" shows last 10 rows from local Layer 1.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># Manual installation tests (D = demonstration)\n# macOS\ncd apps/memory &amp;&amp; cargo tauri build --target universal-apple-darwin\ncodesign --verify --deep --strict ./src-tauri/target/release/bundle/macos/Memory.app\nspctl --assess --type exec ./src-tauri/target/release/bundle/macos/Memory.app\n\n# Windows\ncd apps/memory &amp;&amp; cargo tauri build\nsigntool verify /pa ./src-tauri/target/release/bundle/msi/Memory.msi\n\n# Linux\ncd apps/memory &amp;&amp; cargo tauri build\ndpkg-deb --info ./src-tauri/target/release/bundle/deb/memory_*.deb</code></pre>\n<pre><code class=\"language-rust\">// services/memory/desktop/src-tauri/tests/sync_supervisor_test.rs\n#[tokio::test]\nasync fn supervisor_restarts_daemon_on_crash() {\n    let supervisor = SyncSupervisor::new();\n    let local = Arc::new(test_local_memory());\n    let cloud = Arc::new(MockCloud::start());\n\n    test_helper::inject_sync_panic_after(Duration::from_secs(1));\n    supervisor.start(local, cloud).await;\n    tokio::time::sleep(Duration::from_secs(5)).await;\n    assert!(supervisor.is_running().await);   // restarted after panic\n}</code></pre>\n<pre><code class=\"language-typescript\">// services/memory/desktop/src/lib/__tests__/quick_capture.test.ts\ntest('quick_capture writes a row via Tauri command', async () =&gt; {\n  const { invoke } = await import('@tauri-apps/api/core');\n  await invoke('write_quick_note', { text: 'test note' });\n  const state = await invoke('get_sync_state');\n  expect(state.sync_class_breakdown.private).toBeGreaterThan(0);\n});</code></pre>\n<pre><code class=\"language-bash\"># Auto-update test (D)\n# Publish v1.0; install on test machine\n# Publish v1.1\n# Wait ≤ 24h\n# Verify client auto-updates to v1.1</code></pre>\n<pre><code class=\"language-bash\"># Headless mode test\nmemory --headless &amp;\nsleep 5\nps aux | grep memory   # should be running\nls ~/.cyberos/memory/store/audit/  # should have latest binlog</code></pre>\n<pre><code class=\"language-bash\"># Bundle size test\ncd apps/memory &amp;&amp; cargo tauri build\nls -lh ./src-tauri/target/release/bundle/macos/Memory.app\n# Assert: &lt;= 30MB\nls -lh ./src-tauri/target/release/bundle/msi/Memory.msi\n# Assert: &lt;= 25MB</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-rust\">// services/memory/desktop/src-tauri/src/tray.rs\npub fn install(app: &amp;mut tauri::App) -&gt; tauri::Result&lt;()&gt; {\n    let tray_menu = Menu::new()\n        .add_item(MenuItem::with_id(\"open\", \"Open memory\"))\n        .add_item(MenuItem::with_id(\"quick_capture\", \"Quick Capture...\"))\n        .add_separator()\n        .add_item(MenuItem::with_id(\"force_sync\", \"Force Sync Now\"))\n        .add_item(MenuItem::with_id(\"toggle_sync\", \"Pause Sync\"))\n        .add_separator()\n        .add_item(MenuItem::with_id(\"recent\", \"Recent Memories\"))\n        .add_separator()\n        .add_item(MenuItem::with_id(\"quit\", \"Quit\"));\n    let _tray = TrayIconBuilder::with_id(\"main\")\n        .menu(&amp;tray_menu)\n        .icon(app.default_window_icon().unwrap().clone())\n        .on_menu_event(handle_tray_event)\n        .build(app)?;\n    Ok(())\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-MEMORY-103</strong> — sync daemon embedded.</li><li><strong>TASK-MEMORY-105 (downstream)</strong> — disputed-pair UI link.</li><li><strong>TASK-MEMORY-108</strong> — search API.</li><li>Crates: <code>tauri@2.0</code>, <code>tauri-plugin-updater</code>, <code>tauri-plugin-dialog</code>, <code>tauri-plugin-notification</code>, <code>tokio</code>, <code>clap@4</code>, <code>sentry@0.32</code> (opt-in).</li><li>Apple Developer Program membership (Apple ID + signing cert).</li><li>Windows EV code-signing cert.</li><li>Cloudflare R2 for releases.</li><li>Svelte 5 + Tailwind 4.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Quick-capture row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"quick_note\",\n  \"ts_ns\": 1747526400000000000,\n  \"body\": \"Remember to follow up with Stephen about the Bedrock cost spike.\",\n  \"meta\": {\n    \"sync_class\": \"private\",\n    \"auto_tags\": [\"quick_note\", \"2026-05-15\"]\n  },\n  \"extra\": {\n    \"source\": \"tray\",\n    \"originator_device_id\": \"device-mbp\"\n  }\n}</code></pre>\n<h3>Sync state response</h3>\n<pre><code class=\"language-json\">{\n  \"chain_head\": \"a3f9c8d7e6b5a4f3...\",\n  \"last_sync_at\": \"2026-05-15T14:00:30.123Z\",\n  \"last_sync_duration_ms\": 2150,\n  \"disputed_pair_count\": 2,\n  \"sync_class_breakdown\": { \"shareable\": 1247, \"private\": 384 },\n  \"cloud_state\": \"online\"\n}</code></pre>\n<h3>Update manifest (from R2)</h3>\n<pre><code class=\"language-json\">{\n  \"version\": \"1.1.0\",\n  \"notes\": \"Bug fixes + perf\",\n  \"pub_date\": \"2026-05-15T00:00:00Z\",\n  \"platforms\": {\n    \"darwin-x86_64\":   { \"url\": \"https://releases.cyberos.world/memory/1.1.0/Memory-darwin-x86_64.app.tar.gz\", \"signature\": \"...\" },\n    \"darwin-aarch64\":  { \"url\": \"https://releases.cyberos.world/memory/1.1.0/Memory-darwin-aarch64.app.tar.gz\", \"signature\": \"...\" },\n    \"windows-x86_64\":  { \"url\": \"https://releases.cyberos.world/memory/1.1.0/Memory-windows-x86_64.msi\", \"signature\": \"...\" }\n  }\n}</code></pre>\n<h3>Update signature failure log</h3>\n<pre><code class=\"language-text\">ERROR update_signature_invalid version=1.1.0\n      expected_pubkey=ed25519-... actual=...\n      Rollback applied; user notified via in-app banner\nsev-2 memory_app_update_signature_failures_total incremented</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Mobile app (iOS + Android) — slice 4+; different runtime (Swift / Kotlin).</li><li>Plugin system (third-party Tauri plugins) — slice 5+.</li><li>memory UI for Windows ARM — slice 5+ when demand.</li><li>App Store distribution — slice 5+ (Apple Mac App Store gatekeeping).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Full Disk Access not granted</td><td>Memory can't read ~/.cyberos/memory/store/</td><td>App shows banner asking user to grant</td><td>User clicks System Settings</td></tr><tr><td>Updater fails</td><td>network error</td><td>Sev-2 metric; manual update via \"Check for Updates\"</td><td>Operator action</td></tr><tr><td>Update signature invalid</td><td>ed25519 verify fails</td><td>Rollback applied; sev-2 alarm</td><td>Operator investigates manifest source</td></tr><tr><td>WebView2 missing (Windows)</td><td>Tauri prompts user to install</td><td>One-time install</td><td>User action</td></tr><tr><td>WKWebView crash (macOS)</td><td>Tauri watchdog</td><td>Restart WebView</td><td>Self-heals</td></tr><tr><td>Sync daemon crash</td><td>tokio task panic</td><td>Supervisor restarts with exponential backoff</td><td>Self-heals</td></tr><tr><td>macOS Gatekeeper blocks</td><td>Notarisation expired or invalid</td><td>User can't open app</td><td>Re-notarise + push update</td></tr><tr><td>Windows SmartScreen warning</td><td>EV cert reputation</td><td>\"More info\" workaround for users</td><td>Build cert reputation OR re-issue</td></tr><tr><td>Linux .deb missing dependency</td><td>apt install error</td><td>User installs missing</td><td>One-time</td></tr><tr><td>Bundle size &gt; budget</td><td>CI check at build</td><td>Fail</td><td>Engineer optimises deps</td></tr><tr><td>Crash report sent without opt-in</td><td>privacy violation</td><td>Sev-1 incident</td><td>Investigate; force opt-out by default</td></tr><tr><td>Tray icon disappears</td><td>OS settings issue</td><td>User restarts app</td><td>OS-specific</td></tr><tr><td>Multi-window state inconsistent</td><td>known limitation</td><td>Refresh button</td><td>Slice 3+ fix</td></tr><tr><td>Headless mode + UI flag passed</td><td>clap error</td><td>Exit 1</td><td>User fixes args</td></tr><tr><td>Auto-update during memory write</td><td>race</td><td>Update applied at next launch</td><td>By design</td></tr><tr><td>Cloud memory config wrong</td><td>sync daemon errors</td><td>Settings UI shows error</td><td>User fixes config</td></tr><tr><td>Device ID collision (extremely unlikely)</td><td>UUID randomness</td><td>N/A</td><td>N/A</td></tr><tr><td>Tauri 2.x API breaking change</td><td>compile error</td><td>PR blocked</td><td>Engineer pins version</td></tr><tr><td>Sentry-rust panic on disabled</td><td>catch</td><td>No-op</td><td>By design</td></tr><tr><td>Localisation file missing</td><td>fallback to English</td><td>Sev-3 alarm</td><td>Translation engineer</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>Tauri 2.x produces 5-10× smaller binaries than Electron — critical for download UX.</li><li>Apple notarisation can take 10-30 min; CI workflow waits for completion before tagging release as \"ready.\"</li><li>Windows EV cert ($300-500/year) is worth the SmartScreen-bypass.</li><li>Cloudflare R2 chosen over S3 for cost (no egress fees) + global CDN.</li><li>Update-manifest signing uses a SEPARATE Ed25519 key from memory signing key. Compromise of one doesn't compromise the other.</li><li>Full Disk Access on macOS is the hardest UX friction — first-run banner with screenshot directs user; once granted, persists across updates.</li><li>AppContainer on Windows limits damage from compromised app — file system access scoped to user profile only.</li><li>Headless mode covers Linux server installs (always-on home server). No UI overhead; just sync.</li><li>Bundle size budget enforced in CI prevents accidental dependency bloat (e.g., adding a 50MB image library).</li><li>Crash reporting opt-in respects user agency (memory is personal memory; default-on telemetry feels invasive).</li><li>Multi-window support is slice-2 work; multi-window state coordination is non-trivial (each window has own search; main window is primary).</li><li>Localisation infrastructure (i18n keys + translation files) is slice-2 wiring; actual VN translations are slice-3+ when content stabilises.</li></ul>\n<hr>\n<p><em>End of TASK-MEMORY-104. Status: done (implemented 2026-05-23).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>The desktop app lives at services/memory/desktop (Svelte + Tauri 2) exactly as the paths above say; noted here because a 2026-07-02 sweep initially looked for apps/tauri.</p>";
