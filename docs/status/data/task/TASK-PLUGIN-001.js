window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLUGIN-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The PLUGIN module <strong>MUST</strong> ship the manifest schema and reference packer at <code>modules/plugin/cyberos_plugin/</code>. The schema is JSONSchema 2020-12 at <code>modules/plugin/manifest.schema.json</code>; the packer is Python at <code>modules/plugin/cyberos_plugin/packer.py</code> with CLI entrypoint <code>cyberos-plugin pack</code>. Together they define how a CyberOS plugin bundle is described, validated, and produced.</p>\n<ol><li><strong>MUST</strong> validate canonical manifests against <code>manifest.schema.json</code> per DEC-2400. Validation runs at three sites: (a) author time via <code>cyberos-plugin validate manifests/&lt;id&gt;@&lt;version&gt;.plugin.json</code>; (b) pack time via <code>cyberos-plugin pack</code> (refuses to emit a bundle if the manifest doesn't validate); (c) install time via the host runtime (each adapter inherits the schema). Validation errors MUST be human-readable with the failing JSON-pointer and the constraint that failed.</li></ol>\n<ol><li><strong>MUST</strong> declare <code>schema_version</code> as the const <code>\"1.0.0\"</code> per DEC-2401. Schema evolutions are handled by writing task-PLUGIN-001a (then b, then c) — never by widening the v1 schema in place. This keeps every shipped plugin pinned to a known schema generation.</li></ol>\n<ol><li><strong>MUST</strong> enforce the <code>id</code> pattern <code>^[a-z0-9][a-z0-9-]{1,62}[a-z0-9]$</code> per DEC-2402 — lowercase kebab-case, 3-64 characters, no leading/trailing hyphen. This pattern is OCI-image-name compatible so marketplace artefacts can be addressed as <code>oci://plugins.cyberskill.world/&lt;id&gt;:&lt;version&gt;</code> without transformation.</li></ol>\n<ol><li><strong>MUST</strong> enforce SemVer 2.0 on the <code>version</code> field per DEC-2403. The validator accepts <code>MAJOR.MINOR.PATCH</code>, <code>MAJOR.MINOR.PATCH-PRERELEASE</code> (e.g. <code>1.0.0-beta.1</code>), and <code>MAJOR.MINOR.PATCH+BUILD</code> (e.g. <code>1.0.0+sha.abc123</code>). Build metadata MUST NOT affect compatibility — two manifests with <code>version: \"1.0.0+a\"</code> and <code>version: \"1.0.0+b\"</code> are semantically equal.</li></ol>\n<ol><li><strong>MUST</strong> restrict <code>capabilities</code> to the closed enum of 7 keys per DEC-2404: <code>read_memory</code>, <code>write_memory</code>, <code>execute_workflow</code>, <code>list_skills</code>, <code>invoke_skill</code>, <code>publish_skill</code>, <code>route_natural_language</code>. <code>additionalProperties</code> is false. Adding a capability requires bumping the schema version per DEC-2401.</li></ol>\n<ol><li><strong>MUST</strong> enforce the SEP-986 tool name pattern <code>^cyberos\\.[a-z][a-z0-9]*\\.[a-z][a-z0-9_]*$</code> per DEC-2405. Each tool's <code>name</code> MUST start with <code>cyberos.</code>, then a module slug, then a verb-underscore-noun pair (e.g. <code>cyberos.cuo.execute_workflow</code>, <code>cyberos.memory.read_audit</code>, <code>cyberos.skill.list_catalog</code>).</li></ol>\n<ol><li><strong>MUST</strong> enforce <code>auth.method == \"oauth-pkce\"</code> as a JSONSchema const per DEC-2406. Other auth methods (api-key, basic-auth, bearer-static) MUST be rejected at validation. Future versions MAY add methods via task-PLUGIN-001a but v1 is locked.</li></ol>\n<ol><li><strong>MUST</strong> require <code>signature.rekor_uuid</code> and <code>signature.sigstore_bundle</code> per DEC-2407 — these fields are at the top of <code>manifest.schema.json#/required</code>. A manifest cannot validate without a Sigstore Rekor transparency-log entry referenced. The packer (clause 11) refuses to emit unsigned bundles.</li></ol>\n<ol><li><strong>MUST</strong> validate capability/scope coherence: a manifest declaring <code>capabilities.write_memory: true</code> MUST also list at least one tool whose <code>scopes</code> array contains <code>cyberos:memory:write</code>. This is a JSONSchema-extension check in <code>validator.py</code>, not pure schema. Reason: hosts surface declared capabilities at install time; if the declaration doesn't match the actual scopes requested, users grant under false pretences.</li></ol>\n<ol><li><strong>MUST</strong> produce reproducible bundles per DEC-2409. <code>packer.py</code> MUST: (a) set every zip entry's <code>mtime</code> to a fixed epoch (1980-01-01 00:00:00 UTC — the zip-format minimum); (b) sort entries by path; (c) use fixed permissions (0o644 for files, 0o755 for directories); (d) include no machine names, user IDs, or local paths in any file. Two builds of the same manifest from the same source MUST produce identical SHA-256.</li></ol>\n<ol><li><strong>MUST</strong> expose CLI <code>cyberos-plugin pack &lt;manifest&gt; [--out &lt;path&gt;]</code> and <code>cyberos-plugin validate &lt;manifest&gt;</code> and <code>cyberos-plugin doctor &lt;bundle&gt;</code>. <code>pack</code> produces a zip containing <code>plugin.json</code> + <code>commands/</code> + <code>skills/</code> + adapter assets. <code>validate</code> is dry-run schema check. <code>doctor</code> opens an existing bundle and checks the 8 INTEROP invariants.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow <code>additionalProperties</code> at the top level of the manifest per DEC-2400. Unknown fields are rejected to keep the contract closed. Adapter-specific fields go under <code>targets[].extensions{}</code> (introduced in TASK-PLUGIN-007).</li></ol>\n<ol><li><strong>MUST NOT</strong> silently coerce types (e.g. <code>version: 1.0</code> → <code>\"1.0.0\"</code>). Type mismatches fail validation with a clear error.</li></ol>\n<ol><li><strong>MUST NOT</strong> accept manifests where any tool's <code>annotations.destructive == true</code> but <code>scopes</code> does not include a corresponding write scope. Per TASK-MCP-006 tool gating, destructive tools require explicit scope coverage.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why JSONSchema 2020-12 (DEC-2400)?</strong> Latest stable JSONSchema draft; broad tooling support (Python <code>jsonschema</code> 4.x, Rust <code>jsonschema</code>, JS <code>ajv</code> 8.x); supports <code>const</code>, <code>additionalProperties: false</code>, pattern, format, enum — every constraint we need. Older drafts (draft-07) lack some constraints we use; draft-2019-09 is superseded.</p>\n<p><strong>Why const schema_version (DEC-2401)?</strong> Allowing a range (e.g. <code>^1\\\\..*$</code>) invites silent compatibility drift — fields appear in v1.3 manifests that v1.0 validators don't recognise. Constant versions force every new field to ship under a new task with explicit ecosystem migration guidance. This is the Lockfile pattern, applied to plugin schemas.</p>\n<p><strong>Why OCI-compatible id pattern (DEC-2402)?</strong> Strategy §4 Level 3 envisions a marketplace addressable as <code>oci://plugins.cyberskill.world/&lt;id&gt;:&lt;version&gt;</code>. If <code>id</code> accepts characters OCI rejects (uppercase, underscores, dots), the marketplace adapter has to transform before push, breaking round-trip identity. Keeping the patterns aligned avoids the transformation entirely.</p>\n<p><strong>Why SemVer 2.0 (DEC-2403)?</strong> Plugin consumers (hosts) need a well-known compatibility model. SemVer is the industry default. Pre-release identifiers (<code>1.0.0-beta.1</code>) are essential for staged rollouts. Build metadata is semantically irrelevant per SemVer spec — two builds of the same source can carry different <code>+sha.x</code> and still be the same plugin.</p>\n<p><strong>Why closed capabilities enum (DEC-2404)?</strong> Hosts render the capability list at install time so the user knows what they're granting. If capabilities are an open string set, malicious plugins could declare misleading capability names (<code>harmless_memory_lookup</code> instead of <code>read_memory</code>) that users skim past. A closed enum forces every capability to be reviewed and named in this task.</p>\n<p><strong>Why SEP-986 enforced at manifest level (DEC-2405, clause 6)?</strong> TASK-MCP-003 already enforces this at the gateway. Enforcing it at the manifest schema level catches violations earlier (author time, not runtime) and surfaces them in the same validator output as other schema errors. Defense-in-depth.</p>\n<p><strong>Why OAuth-PKCE only in v1 (DEC-2406, clause 7)?</strong> Long-lived secrets in bundles are a supply-chain liability — anyone who exfiltrates a bundle gets the key. OAuth-PKCE forces a per-install handshake against <code>auth.cyberskill.world</code>, with token rotation. The threat model is \"stolen bundle MUST NOT yield credentials.\" API keys violate that; OAuth-PKCE preserves it.</p>\n<p><strong>Why required Rekor UUID (DEC-2407, clause 8)?</strong> Strategy §2 lists \"open audit chain\" as one of CyberOS's four defensible positions. A plugin without a Rekor anchor has no audit chain — anyone could swap the bytes after publication. Making the field required at the schema level means the absence is caught immediately, not at publish time.</p>\n<p><strong>Why Python reference packer (DEC-2408, clause 11)?</strong> Matches the CUO + memory convention (Python reference impl + Rust production binary). Python is faster to iterate, easier to fuzz, and <code>jsonschema</code> is mature. The Rust production binary (<code>services/plugin-host/</code>) lands in TASK-PLUGIN-007 alongside the multi-runtime adapters.</p>\n<p><strong>Why reproducibility (DEC-2409, clause 10)?</strong> Sigstore Rekor proves \"this artefact was signed by X at time Y.\" That proof is only useful if a verifier can rebuild the bundle locally and confirm the hash matches. Non-reproducible bundles break that round-trip — you can verify the signature, but you can't verify the bytes came from the source. Reproducibility makes Sigstore actually work.</p>\n<p><strong>Why JSON-pointer error messages (clause 1)?</strong> Plugin authors will hit validation errors. <code>path /tools/3/name: pattern '^cyberos\\\\..*$' violated</code> is actionable. <code>validation failed</code> is not.</p>\n<p><strong>Why capability/scope coherence check (clause 9)?</strong> A common bug pattern: developer adds a capability declaration but forgets to add the corresponding scope in <code>tools[*].scopes</code>. Host surfaces the wrong consent UI. The cross-field check catches this at validate time.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3><code>manifest.schema.json</code> (already at <code>modules/plugin/manifest.schema.json</code>)</h3>\n<p>The full schema is in the file. Required top-level keys: <code>schema_version</code>, <code>id</code>, <code>version</code>, <code>name</code>, <code>description</code>, <code>authors</code>, <code>license</code>, <code>capabilities</code>, <code>tools</code>, <code>auth</code>, <code>audit</code>, <code>targets</code>, <code>signature</code>. See the file for the full property definitions.</p>\n<h3>Python packer surface (<code>cyberos_plugin/packer.py</code>)</h3>\n<pre><code class=\"language-python\">from dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Sequence\n\n@dataclass(frozen=True)\nclass PackResult:\n    bundle_path: Path\n    sha256: str            # reproducible hash\n    size_bytes: int\n    tools_count: int\n    commands_count: int\n    skills_count: int\n\ndef pack(\n    manifest_path: Path,\n    target: str = \"claude-code\",      # one of {claude-code,cursor,cowork,codex-cli}\n    out_dir: Path = Path(\"dist\"),\n    *,\n    fail_on_unsigned: bool = True,    # DEC-2407\n    reproducible: bool = True,        # DEC-2409\n) -&gt; PackResult: ...\n\ndef validate(manifest_path: Path) -&gt; Sequence[ValidationError]: ...\n\ndef doctor(bundle_path: Path) -&gt; Sequence[InvariantViolation]: ...</code></pre>\n<h3>CLI surface (<code>cyberos_plugin/cli.py</code>)</h3>\n<pre><code class=\"language-text\">$ cyberos-plugin pack &lt;manifest&gt; [--target {claude-code,cursor,cowork,codex-cli}] [--out &lt;dir&gt;]\n$ cyberos-plugin validate &lt;manifest&gt;\n$ cyberos-plugin doctor &lt;bundle.plugin&gt;\n$ cyberos-plugin --version</code></pre>\n<h3>Minimal valid manifest</h3>\n<pre><code class=\"language-json\">{\n  \"schema_version\": \"1.0.0\",\n  \"id\": \"cyberos\",\n  \"version\": \"1.0.0\",\n  \"name\": \"CyberOS\",\n  \"description\": \"Persona-aware orchestration + memory + skills for any agentic IDE.\",\n  \"authors\": [{\"name\": \"CyberSkill Software\", \"url\": \"https://cyberskill.world\"}],\n  \"license\": \"Apache-2.0\",\n  \"capabilities\": {\"read_memory\": true, \"execute_workflow\": true, \"list_skills\": true},\n  \"tools\": [\n    {\n      \"name\": \"cyberos.cuo.list_personas\",\n      \"description\": \"List the 47 active CyberOS personas available for orchestration.\",\n      \"input_schema\": {\"type\": \"object\", \"properties\": {}},\n      \"scopes\": [\"cyberos:cuo:list\"]\n    }\n  ],\n  \"auth\": {\n    \"method\": \"oauth-pkce\",\n    \"authorize_url\": \"https://auth.cyberskill.world/v1/oauth/authorize\",\n    \"token_url\": \"https://auth.cyberskill.world/v1/oauth/token\",\n    \"scopes\": [\"cyberos:cuo:list\"]\n  },\n  \"audit\": {\n    \"endpoint\": \"https://memory.cyberskill.world/v1/audit\",\n    \"kinds\": [\"plugin.installed\", \"plugin.invoked\", \"plugin.uninstalled\", \"plugin.updated\"]\n  },\n  \"targets\": [\"claude-code\"],\n  \"signature\": {\n    \"sigstore_bundle\": \"&lt;base64-encoded-sigstore-bundle&gt;\",\n    \"rekor_uuid\": \"24296fb24b8ad77a...\"\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Schema rejects missing schema_version</strong> — fixture <code>invalid_missing_schema_version.json</code> fails validation with <code>path /: required property 'schema_version' missing</code>.</li><li><strong>Schema rejects schema_version != \"1.0.0\"</strong> — fixture with <code>schema_version: \"1.1.0\"</code> fails with const violation.</li><li><strong>Schema rejects uppercase id</strong> — fixture with <code>id: \"CyberOS\"</code> fails the pattern.</li><li><strong>Schema rejects id &lt; 3 chars</strong> — fixture with <code>id: \"ab\"</code> fails.</li><li><strong>Schema rejects malformed version</strong> — fixture with <code>version: \"1.0\"</code> fails the SemVer pattern.</li><li><strong>Schema accepts SemVer pre-release</strong> — <code>version: \"1.0.0-beta.1\"</code> passes.</li><li><strong>Schema accepts SemVer build metadata</strong> — <code>version: \"1.0.0+sha.abc123\"</code> passes.</li><li><strong>Schema rejects unknown capability</strong> — fixture with <code>capabilities.hack_the_planet: true</code> fails additionalProperties.</li><li><strong>Schema rejects non-SEP-986 tool name</strong> — <code>tools[0].name: \"foo.bar\"</code> fails pattern.</li><li><strong>Schema rejects auth.method != \"oauth-pkce\"</strong> — <code>auth.method: \"api-key\"</code> fails const.</li><li><strong>Schema rejects missing signature.rekor_uuid</strong> — fixture <code>invalid_missing_signature.json</code> fails.</li><li><strong>Validator catches capability/scope mismatch</strong> — manifest declaring <code>write_memory: true</code> but no tool with <code>cyberos:memory:write</code> scope fails with a custom error message.</li><li><strong>Validator catches destructive-without-write-scope</strong> — tool with <code>annotations.destructive: true</code> and no write scope fails.</li><li><strong>Packer refuses unsigned bundle when fail_on_unsigned=True</strong> — call packer with manifest missing signature → raises <code>UnsignedManifestError</code>.</li><li><strong>Packer produces reproducible SHA-256</strong> — two <code>pack()</code> calls on the same manifest from the same source produce identical SHA-256.</li><li><strong>Packer entries have epoch mtime</strong> — <code>unzip -l bundle.plugin</code> shows all entries timestamped 1980-01-01.</li><li><strong>Packer entries are sorted by path</strong> — <code>unzip -l</code> shows ascending path order.</li><li><strong>Packer file permissions are 0o644</strong> — <code>unzip -l</code> shows <code>-rw-r--r--</code> on every file.</li><li><strong>Packer rejects unknown target</strong> — <code>pack(..., target=\"bogus\")</code> raises <code>UnknownTargetError</code>.</li><li><strong>CLI pack returns exit 0 on success</strong> — <code>cyberos-plugin pack manifests/cyberos@1.0.0.plugin.json</code> writes a file and exits 0.</li><li><strong>CLI validate returns exit 1 on failure</strong> — <code>cyberos-plugin validate invalid_missing_signature.json</code> prints error to stderr, exits 1.</li><li><strong>CLI doctor catches missing Rekor on existing bundle</strong> — running doctor on a bundle whose plugin.json has signature stripped fails.</li><li><strong>Validation errors include JSON-pointer paths</strong> — error messages contain <code>/tools/0/name</code> style references, not \"line 47.\"</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># tests/test_schema_required_fields.py\nimport json, jsonschema, pathlib\n\nSCHEMA = json.loads(pathlib.Path(\"modules/plugin/manifest.schema.json\").read_text())\n\ndef test_minimal_valid_passes():\n    m = json.loads(pathlib.Path(\"tests/fixtures/valid_minimal_plugin.json\").read_text())\n    jsonschema.validate(m, SCHEMA)  # no exception\n\ndef test_missing_schema_version_fails():\n    m = json.loads(pathlib.Path(\"tests/fixtures/valid_minimal_plugin.json\").read_text())\n    del m[\"schema_version\"]\n    with pytest.raises(jsonschema.ValidationError) as e:\n        jsonschema.validate(m, SCHEMA)\n    assert \"schema_version\" in str(e.value)\n\ndef test_schema_version_const():\n    m = _load_minimal()\n    m[\"schema_version\"] = \"1.1.0\"\n    with pytest.raises(jsonschema.ValidationError):\n        jsonschema.validate(m, SCHEMA)</code></pre>\n<pre><code class=\"language-python\"># tests/test_schema_tool_name_pattern.py\n@pytest.mark.parametrize(\"name,valid\", [\n    (\"cyberos.cuo.execute_workflow\", True),\n    (\"cyberos.memory.read_audit\", True),\n    (\"cyberos.skill.list_catalog\", True),\n    (\"CyberOS.cuo.execute\", False),         # uppercase\n    (\"cyberos.execute\", False),             # missing module segment\n    (\"foo.bar.baz\", False),                 # wrong prefix\n    (\"cyberos..execute_workflow\", False),   # empty module\n    (\"cyberos.cuo.123_execute\", False),     # verb starts with digit\n])\ndef test_tool_name_pattern(name, valid):\n    m = _load_minimal()\n    m[\"tools\"][0][\"name\"] = name\n    if valid:\n        jsonschema.validate(m, SCHEMA)\n    else:\n        with pytest.raises(jsonschema.ValidationError):\n            jsonschema.validate(m, SCHEMA)</code></pre>\n<pre><code class=\"language-python\"># tests/test_packer_reproducible.py\ndef test_pack_is_reproducible(tmp_path):\n    m = tmp_path / \"manifest.json\"; m.write_text(MINIMAL_MANIFEST)\n    r1 = pack(m, out_dir=tmp_path / \"a\")\n    r2 = pack(m, out_dir=tmp_path / \"b\")\n    assert r1.sha256 == r2.sha256\n\ndef test_pack_epoch_mtime(tmp_path):\n    r = pack(_minimal(tmp_path), out_dir=tmp_path / \"out\")\n    import zipfile\n    with zipfile.ZipFile(r.bundle_path) as zf:\n        for info in zf.infolist():\n            assert info.date_time == (1980, 1, 1, 0, 0, 0)</code></pre>\n<pre><code class=\"language-python\"># tests/test_packer_signature_required.py\ndef test_pack_refuses_unsigned(tmp_path):\n    m = tmp_path / \"m.json\"\n    raw = json.loads(MINIMAL_MANIFEST); del raw[\"signature\"]; m.write_text(json.dumps(raw))\n    with pytest.raises(UnsignedManifestError):\n        pack(m, fail_on_unsigned=True)</code></pre>\n<pre><code class=\"language-bash\"># tests/test_cli_pack_smoke.sh\ncyberos-plugin pack modules/plugin/manifests/cyberos@1.0.0.plugin.json \\\n    --target claude-code --out /tmp/pack-test\ntest -f /tmp/pack-test/cyberos-1.0.0.plugin\ncyberos-plugin doctor /tmp/pack-test/cyberos-1.0.0.plugin\necho $?  # → 0</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton — <code>packer.py</code>, <code>validator.py</code>, <code>reproducible.py</code>, <code>cli.py</code> are thin orchestrators around <code>jsonschema.validate</code> + <code>zipfile.ZipFile</code>. The custom validator additions live in <code>validator.py::check_capability_scope_coherence</code>.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none — this is the keystone task with no task-level dependencies.</li><li><strong>Downstream:</strong> every other task in this module reads <code>manifest.schema.json</code> for its contract. TASK-PLUGIN-002 reads <code>tools[*]</code> for what MCP tools to register. TASK-PLUGIN-003 reads <code>commands[*]</code>. TASK-PLUGIN-004 reads <code>skills[*]</code>. TASK-PLUGIN-005 reads <code>auth.*</code>. TASK-PLUGIN-006 reads <code>audit.*</code>. TASK-PLUGIN-007 reads <code>targets[*]</code>. TASK-PLUGIN-008 reads <code>marketplace.*</code>.</li><li><strong>Cross-module:</strong> TASK-MCP-003 (SEP-986 tool naming — clause 6 enforces it), TASK-MCP-006 (tool annotation gating — clause 14 enforces it), TASK-SKILL-111 (description enrichment — manifest <code>description</code> 60-480 char range matches the trigger-discovery discipline).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Valid minimal manifest (see §3 for content)</h3>\n<h3>Validation error example</h3>\n<pre><code class=\"language-json\">{\n  \"errors\": [\n    {\n      \"json_pointer\": \"/tools/0/name\",\n      \"constraint\": \"pattern\",\n      \"expected\": \"^cyberos\\\\.[a-z][a-z0-9]*\\\\.[a-z][a-z0-9_]*$\",\n      \"actual\": \"execute_workflow\",\n      \"message\": \"Tool name 'execute_workflow' must follow SEP-986: 'cyberos.{module}.{verb}_{noun}'. Per TASK-MCP-003.\"\n    },\n    {\n      \"json_pointer\": \"/signature/rekor_uuid\",\n      \"constraint\": \"required\",\n      \"message\": \"Missing Rekor transparency-log UUID. Bundles must be Sigstore-signed per TASK-PLUGIN-001 §1 clause 8.\"\n    }\n  ]\n}</code></pre>\n<h3>Pack result</h3>\n<pre><code class=\"language-json\">{\n  \"bundle_path\": \"dist/cyberos-1.0.0.plugin\",\n  \"sha256\": \"a1b2c3d4e5f6...\",\n  \"size_bytes\": 184320,\n  \"tools_count\": 8,\n  \"commands_count\": 4,\n  \"skills_count\": 12\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<ul><li>~~Should we support api-key auth in v1?~~ → No, oauth-pkce only per DEC-2406. Re-evaluated for task-PLUGIN-001a (post-v1 schema bump).</li><li>~~Should reproducibility be opt-in?~~ → No, default-on per DEC-2409. The opt-out <code>reproducible=False</code> exists for debugging only.</li><li>~~Should the packer ship as Rust to match production?~~ → No, Python reference first per DEC-2408. Rust binary lands in TASK-PLUGIN-007 alongside multi-runtime adapter work.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Manifest missing required field</td><td>JSONSchema validation</td><td><code>cyberos-plugin pack</code> exits 1 with <code>/&lt;field&gt;: required</code></td><td>Author adds field</td></tr><tr><td>Tool name violates SEP-986</td><td>JSONSchema pattern</td><td>exit 1 with <code>/tools/N/name: pattern</code></td><td>Author renames per cyberos.{module}.{verb}_{noun}</td></tr><tr><td>schema_version mismatch</td><td>JSONSchema const</td><td>exit 1 with <code>/schema_version: const</code></td><td>Author downgrades to \"1.0.0\" or waits for task-PLUGIN-001a</td></tr><tr><td>Capability declared without matching scope</td><td>validator.py custom check</td><td>exit 1 with <code>capability '&lt;x&gt;' declared but no tool has scope '&lt;y&gt;'</code></td><td>Author adds the scope to one of the tools</td></tr><tr><td>Destructive tool without write scope</td><td>validator.py custom check</td><td>exit 1 with <code>tool '&lt;n&gt;' marked destructive but lacks write scope</code></td><td>Author adds the write scope OR un-marks the annotation</td></tr><tr><td>Unsigned manifest at pack time</td><td>packer.py check</td><td>raises <code>UnsignedManifestError</code></td><td>Author runs Sigstore sign first, then re-packs</td></tr><tr><td>Reproducibility broken by clock-injection bug</td><td>CI: two consecutive <code>pack()</code> calls compare SHA-256</td><td>CI fails</td><td>Investigate which file source injects mtime; remove</td></tr><tr><td>Adapter for <code>--target</code> not yet implemented</td><td><code>pack(target=\"goose\")</code> in P1</td><td>raises <code>UnknownTargetError</code></td><td>User picks a P1 target or waits for P2</td></tr><tr><td>Bundle SHA-256 mismatch on verifier rebuild</td><td>Sigstore verifier rebuilds bundle, compares hash</td><td>verifier rejects</td><td>Investigate non-determinism in packer; report bug</td></tr><tr><td>Mtimes injected by network filesystem</td><td><code>pack()</code> on NFS sees host clocks</td><td>reproducible check fails</td><td>Use local fs for packing; CI runs on local tmpfs</td></tr><tr><td>Marketplace upload of non-validated bundle</td><td><code>cyberos-plugin publish</code> re-runs validate before upload</td><td>exit 1</td><td>Author re-runs pack with valid manifest</td></tr><tr><td>Future schema field appears in v1 manifest</td><td>additionalProperties: false</td><td>exit 1 with <code>unknown property</code></td><td>Field MUST land via task-PLUGIN-001a, not in v1</td></tr><tr><td>Empty tools array</td><td><code>tools.minItems: 1</code></td><td>exit 1</td><td>A plugin with zero tools is meaningless; add at least one</td></tr><tr><td>Description shorter than 60 chars</td><td><code>description.minLength: 60</code></td><td>exit 1</td><td>Author writes meaningful description</td></tr><tr><td>Tool input_schema not a JSONSchema object</td><td>JSONSchema type</td><td>exit 1</td><td>Author fixes input_schema</td></tr><tr><td>Cross-platform path separator in commands/file</td><td>validator: enforces forward slashes</td><td>exit 1</td><td>Author uses POSIX paths in manifest</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 <strong>JSONSchema library choice.</strong> Python <code>jsonschema</code> 4.21+ for validation. Custom checks (capability/scope coherence, destructive/write-scope coherence) live in <code>validator.py</code> as separate functions called after <code>jsonschema.validate</code>. Reason: JSONSchema can't express cross-field constraints cleanly; mixing schema + custom is the standard pattern.</li></ul>\n<ul><li>§11.2 <strong>Reproducible zip implementation.</strong> Use <code>zipfile.ZipFile</code> with <code>ZipInfo</code> objects constructed manually. Set <code>date_time = (1980, 1, 1, 0, 0, 0)</code> (zip-format epoch), <code>external_attr</code> to <code>(0o644 &lt;&lt; 16)</code> for files and <code>(0o755 &lt;&lt; 16) | 0x10</code> for dirs. Iterate entries in sorted order. Force <code>compress_type=ZIP_DEFLATED, compresslevel=6</code> to avoid level-default churn across zlib versions.</li></ul>\n<ul><li>§11.3 <strong>CLI ergonomics.</strong> Use <code>argparse</code> (stdlib) not <code>click</code> to avoid the dependency. Subcommands: <code>pack</code>, <code>validate</code>, <code>doctor</code>. Each returns exit 0 on success, exit 1 on validation failure, exit 2 on usage error.</li></ul>\n<ul><li>§11.4 <strong>Error message format.</strong> Every validation error MUST include the JSON pointer to the failing field. Errors include a hint pointing back to this task (e.g. \"per TASK-PLUGIN-001 clause 6\") so the author can find the rationale.</li></ul>\n<ul><li>§11.5 <strong>Performance.</strong> Validation runs in &lt;100ms for manifests with up to 200 tools (typical). The reproducibility-stripping pass is O(n_files) — for a 12-skill plugin with ~50 files, packing completes in &lt;200ms.</li></ul>\n<ul><li>§11.6 <strong>Schema versioning escape valve.</strong> When v2 lands (task-PLUGIN-001a), the validator will accept both <code>schema_version: \"1.0.0\"</code> (forwarded to v1 validation rules) and <code>schema_version: \"2.0.0\"</code> (v2 rules). The dual-validator lives in <code>validator.py::validate_dispatch</code>. v1 plugins remain installable indefinitely.</li></ul>\n<ul><li>§11.7 <strong>Marketplace pre-check.</strong> When TASK-PLUGIN-008 lands, <code>cyberos-plugin publish</code> runs <code>validate</code> before upload. This is duplicative with packer-time validation but is mandatory because plugins may be uploaded by tools other than the official packer.</li></ul>\n<ul><li>§11.8 <strong>Why not use Pydantic.</strong> Pydantic is great for runtime models but JSONSchema is the wire format. Maintaining both Pydantic models and JSONSchema would require synchronisation. The cost of the synchronisation is higher than the ergonomic benefit Pydantic provides for a once-per-pack validation.</li></ul>\n<ul><li>§11.9 <strong>Sigstore verification at install time.</strong> Hosts that implement signature verification (Claude Code planned, Cursor TBD) re-anchor the Rekor entry independently. The host MUST NOT trust the <code>sigstore_bundle</code> field blindly — it MUST fetch the entry from Rekor by UUID and verify. The bundle field is included for offline-first reasons.</li></ul>\n<ul><li>§11.10 <strong>Manifest as code.</strong> The shipped manifests at <code>modules/plugin/manifests/*.plugin.json</code> are checked into git. They are the canonical source of truth for what the CyberOS plugin contains. The packer reads from there; it does not synthesise manifests from runtime introspection. Reason: deterministic builds + reviewable in diff.</li></ul>\n<hr>\n<p><em>End of TASK-PLUGIN-001 spec.</em></p>";
