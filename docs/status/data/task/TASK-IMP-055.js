window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-055"]="<h1>TASK-IMP-055: Spec-only module manifest</h1>\n<h2>Summary</h2>\n<p>Publish an honest machine-readable + human table of which <code>modules/*</code> trees ship code vs docs/skills-only (deep-audit R9).</p>\n<h2>Problem</h2>\n<p>Many module folders look like packages but only carry CHANGELOG/audit stubs. Agents invent import paths that do not exist.</p>\n<h2>Proposed Solution</h2>\n<p><code>modules/manifest.yaml</code> + <code>docs/modules/MANIFEST.md</code> listing every top-level module with <code>ships_code</code> or <code>docs_skills_only</code>.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Delete docs-only module folders — rejected: they still anchor task modules.</li><li>Generate-only with no human table — rejected: agents need a readable index.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Manifest lists every <code>modules/*</code> directory exactly once with a kind.</li><li>Kinds match on-disk reality for cuo/memory/skill/templates vs the rest.</li></ul>\n<h2>Scope</h2>\n<p>In scope: yaml + markdown manifest. Out of scope: rewriting module READMEs.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Session agent; HITL Stephen Cheng (session operator).</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 MUST add <code>modules/manifest.yaml</code> with <code>version</code>, per-module <code>id</code> + <code>kind</code>.</li><li>1.2 MUST add <code>docs/modules/MANIFEST.md</code> human table cross-linking the yaml.</li><li>1.3 MUST classify <code>cuo</code>, <code>memory</code>, <code>skill</code>, <code>templates</code> as <code>ships_code</code> and the remaining top-level modules as <code>docs_skills_only</code> for CyberOS 1.x.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC 1 (traces_to: #1.1–#1.3) - yaml + md present; module set matches <code>modules/*</code></li><li>[x] AC 2 (traces_to: #1.3) - four ships_code ids are exactly cuo/memory/skill/templates</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><code>tools/install/mcp/</code> is not a <code>modules/mcp</code> code tree — note in manifest.</li></ul>";
