window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> expose <code>alias::resolve(alias, policy) -&gt; Result&lt;ResolvedModel, AliasError&gt;</code> that maps a closed-set logical alias (<code>chat.smart</code>, <code>chat.fast</code>, etc.) to a concrete <code>(provider, model)</code> tuple via the tenant's <code>TenantPolicy.ai_policy</code>. Given a string alias and a reference to the active tenant policy, the function:</p>\n<ol><li><strong>MUST</strong> accept exactly the 6 aliases in the slice-2 closed set: <code>chat.smart</code>, <code>chat.fast</code>, <code>chat.long</code>, <code>embed.standard</code>, <code>embed.code</code>, <code>rerank.fast</code>. Unknown aliases MUST return <code>Err(AliasError::UnknownAlias { alias, supported })</code> where <code>supported</code> is the full enumeration. The closed-set check is the first operation in <code>resolve</code> (cheap reject path).</li><li><strong>MUST</strong> consult <code>policy.ai_policy.alias_overrides</code> FIRST, before primary/fallback. An override pins an alias to a specific <code>(provider, model)</code> regardless of the primary provider — useful for \"for this tenant, <code>chat.long</code> MUST go to Anthropic native, not Bedrock.\" The override path bypasses fallback chain entirely.</li><li><strong>MUST</strong> prefer the policy's <code>primary_provider.model_alias_map</code> lookup if no override exists. If <code>model_alias_map[alias]</code> is present, return that model under the primary provider with <code>fallback_position: 0</code>.</li><li><strong>MUST</strong> fall through to <code>fallback_chain</code> in order if the primary doesn't carry the alias. Each fallback's <code>model_alias_map</code> is consulted; the first hit wins. The fallback index (1, 2, …) is returned as <code>fallback_position</code> so callers can attribute failover.</li><li><strong>MUST</strong> validate that the resolved <code>(provider, model)</code> exists in the cost table (TASK-AI-007). If <code>cost_table::lookup(&amp;provider_kind, &amp;model)</code> returns <code>None</code>, the function MUST return <code>Err(AliasError::ResolvedModelMissingCostEntry { provider, model })</code>. The cost-entry check prevents resolution-time success that would fail seconds later at the precheck cost-estimate step.</li><li><strong>MUST</strong> validate that the resolved provider satisfies <code>policy.ai_policy.zdr_required</code>. If <code>zdr_required: true</code> and the resolved provider's <code>is_zdr</code> attestation (TASK-AI-015 source of truth) is <code>false</code>, return <code>Err(AliasError::ZdrViolation)</code>. The ZDR check runs after cost-entry validation so that a missing-cost-entry error takes precedence (more actionable).</li><li><strong>MUST</strong> validate that the resolved provider's <code>region</code> matches <code>policy.ai_policy.residency</code>. If <code>residency: sg-1</code> but the resolved provider is <code>Bedrock</code> configured for <code>us-east-1</code>, return <code>Err(AliasError::ResidencyViolation)</code>. Providers without regional pinning (e.g., Anthropic native) pass the residency check by default unless <code>policy.residency_requires_regional_provider: true</code> (slice-2 optional flag).</li><li><strong>MUST</strong> return <code>ResolvedModel { provider_kind, region, model, fallback_position, is_zdr, latency_class }</code> on success. Each field carries information needed by downstream code: <code>provider_kind</code> drives the <code>Provider</code> trait dispatch in TASK-AI-008; <code>region</code> is logged in audit rows; <code>model</code> is the literal string passed to the provider SDK; <code>fallback_position</code> flags \"this call used a fallback\" in OBS dashboards; <code>is_zdr</code> propagates to the audit row's <code>extra.is_zdr</code> field; <code>latency_class</code> informs TASK-AI-008's per-call timeout.</li><li><strong>MUST</strong> complete synchronously in under <strong>1ms p95</strong> for any input. All state is in-memory (policy is <code>Arc&lt;TenantPolicy&gt;</code> from TASK-AI-005's cache; cost table is <code>ArcSwap</code> from TASK-AI-007's cache; ZDR table is <code>ArcSwap</code> from TASK-AI-015's cache). No I/O, no Postgres, no network.</li><li><strong>MUST NOT</strong> mutate the policy under any code path. The contract is read-only; mutating would surprise concurrent readers (ArcSwap guarantees consistent snapshots only if no in-place writes happen).</li><li><strong>MUST</strong> be safe under concurrent invocation from many tokio tasks. The <code>ArcSwap::load()</code> semantics guarantee each call sees a consistent policy snapshot; if TASK-AI-005 hot-reloads policy mid-call, the in-flight call uses the snapshot it loaded, and the next call sees the new policy.</li><li><strong>MUST</strong> be deterministic — same <code>(alias, policy_snapshot)</code> always returns the same <code>ResolvedModel</code> or the same <code>AliasError</code>. Determinism is a CI gate: a property test (§4 AC #11) asserts 10,000 random runs over 100 random fixtures produce stable outputs.</li><li><strong>MUST</strong> be called from <code>cost_ledger::precheck</code> (TASK-AI-001 §6) BEFORE the cost-estimate step. TASK-AI-001's skeleton is updated to call <code>alias::resolve</code> and pass the <code>ResolvedModel</code> to the rest of the precheck.</li><li><strong>MUST</strong> emit OTel metrics on every call: <code>ai_alias_resolutions_total{alias,resolved_provider,fallback_position}</code> (counter), <code>ai_alias_resolution_failures_total{alias,reason}</code> (counter; reason ∈ <code>unknown_alias</code> / <code>cost_missing</code> / <code>zdr</code> / <code>residency</code> / <code>no_provider_has_alias</code>), <code>ai_alias_resolution_latency_ns</code> (histogram with buckets at 100ns / 500ns / 1µs / 5µs / 10µs).</li><li><strong>SHOULD</strong> expose <code>alias::supported_aliases() -&gt; &amp;'static [&amp;'static str]</code> so operator CLI (TASK-AI-021 <code>models list</code>) and the operator UI can enumerate the closed set without re-reading the const.</li><li><strong>MUST</strong> be callable without trace-context parameters — it has no I/O and emits no audit row of its own — AND its return value <code>ResolvedModel</code> MUST be carried through to <code>Provider::complete()</code> so the inbound <code>traceparent</code> (read at the trust boundary in <code>cost_ledger::precheck</code>) propagates to the downstream HTTP call per task-audit skill §3.7 rule 22. TASK-AI-022 §1 #3 owns the propagation contract; this task's responsibility is to NOT strip the span context from the caller's flow. A spec violation: passing <code>ResolvedModel</code> by value to a function that then opens a new span without inheriting from the caller.</li></ol>\n<p>This task provides the abstraction layer that every consumer module (CUO Phase 2, KB ingest, CHAT, OBS auto-triage, PROJ Genie inline) uses to ask for an LLM call without naming a provider. The provider naming is a tenant-policy concern, not an application concern. Once TASK-AI-006 ships, the gateway has a single chokepoint for cross-cutting checks (cost-table, ZDR, residency); TASK-AI-008 routes the resolved tuple, TASK-AI-015/016 enforce policy at the point of resolution, and adding a new provider (Vertex, on-prem Llama, etc.) is a one-file additive change.</p>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a closed alias set of exactly 6?</strong> Six aliases is the right grain at slice 2. Too few (<code>fast</code>, <code>smart</code>) loses precision — a caller writing \"chat with this 100k-token document\" can't tell the gateway it needs long-context behaviour. Too many (one per provider's marketing name like <code>claude-3-5-sonnet-20241022-v2:0</code>) reproduces the vendor lock-in we're trying to escape — the caller now hard-codes Anthropic naming. The 6-tier set (smart/fast/long for chat, standard/code for embed, fast for rerank) covers every reasonable LLM workload we identified across the 22 module pages. Adding a 7th requires a const edit, a task, and a schema update — that's the right level of friction.</p>\n<p><strong>Why pure synchronous, no I/O?</strong> This is on the precheck hot path of every chat call. Precheck's budget is 50ms p95 (TASK-AI-001 §1 #7). Adding a Postgres roundtrip would consume ~5ms; adding an HTTP roundtrip would consume ~20ms. Neither is acceptable when the entire alias resolution can be done in &lt;1µs of HashMap lookups against in-memory caches. Sub-microsecond latency makes the alias layer \"free\" — every consumer can call it without measuring impact.</p>\n<p><strong>Why override beats primary?</strong> A tenant in <code>sg-1</code> residency might still want a specific call (one persona, one workflow) to use Anthropic native instead of Bedrock for regulatory or quality reasons — e.g., \"for client-facing legal drafts, the persona MUST use Anthropic's longer-context claude-3-5-sonnet, not Bedrock's claude-3-haiku, because legal quality matters more than residency.\" The override path lets policy thread that needle per-alias without restructuring the whole <code>primary_provider</code> config (which would affect every other call). Without override, the only escape hatch would be a per-call API parameter — which defeats the abstraction and re-introduces the lock-in.</p>\n<p><strong>Why does this task enforce ZDR + residency?</strong> Both are tenant-policy invariants. Violating either is a contract breach with regulatory consequences (PDPL Art. 6, Decree 53/2022, GDPR Art. 5(1)(f)). Catching the violation at alias resolution (before the provider call) is much cheaper than catching it after the bytes are in flight — the precheck is the hot path; the provider call is 100-1000x more expensive. TASK-AI-015 and TASK-AI-016 add the <em>attestation surface</em> (which providers/models are ZDR, which regions count as sg-1); this task is the <em>enforcement point</em> that consumes those attestations.</p>\n<p><strong>Why expose <code>fallback_position</code> in the result?</strong> Three reasons. (1) memory audit row includes it (DEC-072 — every AI call's provenance is auditable; \"this call used fallback #2 because primary was open\" is part of the chain). (2) OBS dashboards filter by \"calls that used a fallback\" to detect primary-provider degradation before the SLA alarm fires. (3) TASK-AI-008's circuit breaker uses <code>fallback_position</code> to skip the open breakers — if <code>fallback_position: 0</code> is open, resolve picks 1.</p>\n<p><strong>Why expose <code>latency_class</code> and not raw latency budgets?</strong> Different providers have different latency profiles for the same logical alias. <code>chat.smart</code> on Bedrock-Claude-3.5-Sonnet is ~2-4s p95; on Anthropic-native-Claude-3.5-Sonnet it's ~1.5-3s; on a future fast-path provider it might be ~800ms. The downstream caller (TASK-AI-008 router, TASK-AI-010 streaming) needs to know what latency budget to set for the call, but doesn't need the exact number — <code>Standard | Fast | Slow</code> is enough resolution. The mapping from latency class to actual budget is config (<code>policy.ai_policy.call_timeout_seconds</code> from TASK-AI-005), not hard-coded here.</p>\n<p><strong>Why not just use LiteLLM's model registry?</strong> LiteLLM is Python and stateful; we're Rust and the alias registry is part of a deterministic policy snapshot. LiteLLM's model registry is also unversioned — a <code>pip install --upgrade litellm</code> could silently change which model <code>gpt-4</code> resolves to. Our closed set with explicit version pins (e.g., <code>anthropic.claude-3-5-sonnet-20241022-v2:0</code>) makes the contract explicit and version-locked. We've borrowed the <em>idea</em> of LiteLLM's alias→model mapping; we've not borrowed its implementation.</p>\n<p><strong>Why is the alias-resolution decision auditable?</strong> Every <code>ai.precheck</code> memory row (TASK-AI-001 §1 #6) includes the resolved provider + model. A future auditor asking \"what model did the gateway use for tenant X's call at 14:32 yesterday?\" gets the answer from the chain. Without aliasing, the answer is \"whatever the caller wrote in the request payload\" — which a malicious caller could spoof. With aliasing, the gateway is the authority.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<h3>Public function signatures</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/alias.rs\n\n/// Resolve a closed-set alias to a concrete (provider, model) tuple via tenant policy.\n///\n/// This is the only public entry point. Callers MUST NOT bypass this function\n/// (e.g., reading `policy.ai_policy.primary_provider.model_alias_map` directly).\npub fn resolve(\n    alias: &amp;str,\n    policy: &amp;TenantPolicy,\n) -&gt; Result&lt;ResolvedModel, AliasError&gt;;\n\n/// Return the closed set of supported aliases (for operator CLI / UI enumeration).\npub fn supported_aliases() -&gt; &amp;'static [&amp;'static str];\n\n/// Static const declaring the closed set.\npub const SUPPORTED_ALIASES: &amp;[&amp;str] = &amp;[\n    \"chat.smart\",\n    \"chat.fast\",\n    \"chat.long\",\n    \"embed.standard\",\n    \"embed.code\",\n    \"rerank.fast\",\n];</code></pre>\n<h3>Return + error types</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/alias/types.rs\n\n#[derive(Debug, Clone)]\npub struct ResolvedModel {\n    pub provider_kind: ProviderKind,           // bedrock | anthropic | openai | vertex | bge\n    pub region: Option&lt;String&gt;,                // None for providers without regional pinning\n    pub model: String,                         // e.g. \"anthropic.claude-3-5-sonnet-20241022-v2:0\"\n    pub fallback_position: u8,                 // 0 = primary, 1 = first fallback, 2 = second, ...\n    pub is_zdr: bool,                          // whether the resolved provider is ZDR-attested\n    pub latency_class: LatencyClass,           // fast | standard | slow (drives TASK-AI-008 timeout)\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub enum AliasError {\n    /// The alias isn't in SUPPORTED_ALIASES.\n    UnknownAlias {\n        alias: String,\n        supported: Vec&lt;String&gt;,\n    },\n    /// The resolved (provider, model) pair has no cost-table entry — TASK-AI-007 source of truth.\n    ResolvedModelMissingCostEntry {\n        provider: ProviderKind,\n        model: String,\n    },\n    /// Policy requires ZDR but the resolved provider isn't attested.\n    ZdrViolation {\n        resolved_provider: ProviderKind,\n        resolved_model: String,\n    },\n    /// Policy's residency pin doesn't match the resolved provider's region.\n    ResidencyViolation {\n        resolved_region: Option&lt;String&gt;,\n        policy_residency: Residency,\n    },\n    /// Walked primary + all fallbacks; no provider has this alias in its model_alias_map.\n    NoProviderHasAlias {\n        alias: String,\n        providers_tried: u8,\n    },\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum LatencyClass {\n    Fast,       // typically &lt;2s p95 (haiku, gpt-4o-mini, embeds, rerank)\n    Standard,   // typically &lt;5s p95 (sonnet, gpt-4o)\n    Slow,       // typically &lt;30s p95 (opus, long-context chat)\n}\n\nimpl LatencyClass {\n    /// Map a class to the timeout budget config field — TASK-AI-008 reads this.\n    pub fn timeout_budget_seconds(&amp;self, policy: &amp;TenantPolicy) -&gt; u32 {\n        match self {\n            Self::Fast =&gt; policy.ai_policy.call_timeout_seconds.min(15),\n            Self::Standard =&gt; policy.ai_policy.call_timeout_seconds,\n            Self::Slow =&gt; policy.ai_policy.call_timeout_seconds.max(60),\n        }\n    }\n}</code></pre>\n<h3>Alias registry (closed set with intent annotations)</h3>\n<table><thead><tr><th>Alias</th><th>Use case</th><th>Typical latency class</th><th>Typical token budget</th></tr></thead><tbody><tr><td><code>chat.smart</code></td><td>Default chat / Genie / orchestrator (CUO Phase 2 LLM)</td><td>Standard</td><td>8k in + 4k out</td></tr><tr><td><code>chat.fast</code></td><td>Ambient digests, low-stakes summaries, OBS triage</td><td>Fast</td><td>4k in + 1k out</td></tr><tr><td><code>chat.long</code></td><td>Document Q&amp;A, 100k+ context, legal/contract review</td><td>Slow</td><td>100k in + 8k out</td></tr><tr><td><code>embed.standard</code></td><td>KB ingest, memory Layer 2 vectors, persona-keyword updates</td><td>Fast</td><td>8k tokens per batch</td></tr><tr><td><code>embed.code</code></td><td>KB-code embeddings, code-search corpus</td><td>Fast</td><td>4k tokens per batch</td></tr><tr><td><code>rerank.fast</code></td><td>BGE-rerank cross-encoder; KB top-K refinement</td><td>Fast</td><td>(query, candidate[≤50])</td></tr></tbody></table>\n<h3>TenantPolicy schema additions (extending TASK-AI-005's <code>AiPolicy</code>)</h3>\n<pre><code class=\"language-yaml\"># config/tenants/&lt;tenant_id&gt;.yaml — slice-2 additions\nai_policy:\n  # … existing fields from TASK-AI-005 …\n\n  alias_overrides:                           # OPTIONAL — per-alias override beats primary + fallback\n    chat.long:                                # alias → override target\n      provider:\n        kind: anthropic\n        model_alias_map:\n          chat.long: claude-3-5-sonnet-20241022   # 200k context, native (not Bedrock)\n    embed.code:\n      provider:\n        kind: openai\n        model_alias_map:\n          embed.code: text-embedding-3-large   # better at code than BGE-M3 for some tenants\n\n  residency_requires_regional_provider: false   # OPTIONAL — defaults false (anthropic native passes residency)\n                                                 # set true to force regional pinning (rejects no-region providers)</code></pre>\n<h3>Provider trait surface required by <code>resolve()</code></h3>\n<pre><code class=\"language-rust\">// In TASK-AI-005's policy schema, every Provider variant must implement:\npub trait Provider {\n    fn kind(&amp;self) -&gt; ProviderKind;\n    fn region(&amp;self) -&gt; Option&lt;String&gt;;\n    fn model_for_alias(&amp;self, alias: &amp;str) -&gt; Option&lt;&amp;str&gt;;\n    // NOTE: is_zdr is intentionally NOT on the Provider trait. The single source\n    // of truth for ZDR attestation is TASK-AI-015's zdr::is_zdr(&amp;kind, model)\n    // function. Allowing per-provider claims in policy YAML would create two\n    // potentially-disagreeing sources; we prevent that at the type level by\n    // removing the method.\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>Happy path — primary</strong> — Given <code>policy.primary_provider = Bedrock { region: \"ap-southeast-1\", model_alias_map: { \"chat.smart\": \"anthropic.claude-3-5-sonnet-20241022-v2:0\" }, ... }</code>, <code>resolve(\"chat.smart\", &amp;policy)</code> MUST return <code>Ok(ResolvedModel { provider_kind: Bedrock, region: Some(\"ap-southeast-1\"), model: \"anthropic.claude-3-5-sonnet-20241022-v2:0\", fallback_position: 0, is_zdr: true, latency_class: Standard })</code>.</li><li><strong>Fallback resolution</strong> — Given primary doesn't have <code>chat.long</code> in its <code>model_alias_map</code>, and <code>fallback_chain[0] = Anthropic { model_alias_map: { \"chat.long\": \"claude-3-5-sonnet-20241022\" }, ... }</code>, <code>resolve(\"chat.long\", &amp;policy)</code> MUST return <code>Ok(ResolvedModel { provider_kind: Anthropic, fallback_position: 1, latency_class: Slow, ... })</code>.</li><li><strong>Override beats primary</strong> — Given <code>policy.alias_overrides[\"chat.long\"] = anthropic.claude-3-5-sonnet-20241022</code> AND primary's <code>model_alias_map[\"chat.long\"] = bedrock:claude-3-haiku</code>, <code>resolve(\"chat.long\", &amp;policy)</code> MUST return the override's <code>(Anthropic, claude-3-5-sonnet-20241022)</code>, not the primary's Bedrock entry. <code>fallback_position: 0</code> (override always reports as position 0).</li><li><strong>Unknown alias</strong> — <code>resolve(\"chat.tiny\", &amp;policy)</code> MUST return <code>Err(AliasError::UnknownAlias { alias: \"chat.tiny\", supported: vec![\"chat.smart\", \"chat.fast\", \"chat.long\", \"embed.standard\", \"embed.code\", \"rerank.fast\"] })</code>.</li><li><strong>ZDR violation</strong> — Given <code>policy.ai_policy.zdr_required: true</code> AND resolved provider is <code>OpenAI { gpt-4o, is_zdr: false }</code>, <code>resolve(\"chat.smart\", &amp;policy)</code> MUST return <code>Err(AliasError::ZdrViolation { resolved_provider: OpenAI, resolved_model: \"gpt-4o\" })</code>. No <code>ResolvedModel</code> is constructed.</li><li><strong>Residency violation</strong> — Given <code>policy.residency: Sg1</code> AND resolved provider is <code>Bedrock { region: \"us-east-1\", ... }</code>, <code>resolve(\"chat.smart\", &amp;policy)</code> MUST return <code>Err(AliasError::ResidencyViolation { resolved_region: Some(\"us-east-1\"), policy_residency: Sg1 })</code>.</li><li><strong>Cost-table validation</strong> — Given <code>cost_table::lookup(&amp;Bedrock, \"claude-99-fake-model\")</code> returns <code>None</code> (a model name not in cost_rates.yaml), <code>resolve</code> of an alias pointing to that model MUST return <code>Err(AliasError::ResolvedModelMissingCostEntry { provider: Bedrock, model: \"claude-99-fake-model\" })</code>.</li><li><strong>No provider has alias</strong> — Given <code>chat.smart</code> is missing from primary's map AND every fallback's map, <code>resolve(\"chat.smart\", &amp;policy)</code> MUST return <code>Err(AliasError::NoProviderHasAlias { alias: \"chat.smart\", providers_tried: &lt;primary_count + fallback_count&gt; })</code>.</li><li><strong>Override misses cost-table</strong> — Given override points to a model with no cost-table entry, return <code>Err(ResolvedModelMissingCostEntry)</code>. The override path does NOT fall through to primary on cost-table miss; failure is terminal.</li><li><strong>Override misses ZDR</strong> — Given override target is non-ZDR and <code>zdr_required: true</code>, return <code>Err(ZdrViolation)</code>. Override doesn't bypass policy invariants.</li></ol>\n<p>10a. <strong>Override misses residency</strong> — Given override target's region (e.g., <code>Bedrock us-east-1</code>) doesn't match <code>policy.residency: Sg1</code>, <code>resolve</code> MUST return <code>Err(ResidencyViolation { resolved_region: Some(\"us-east-1\"), policy_residency: Sg1 })</code> from the override path. Same terminal semantics as #9 and #10 — no fallthrough to primary.</p>\n<ol><li><strong>Determinism property test</strong> — <code>proptest!</code> runs 10,000 random <code>(alias, policy)</code> pairs; for each pair, calling <code>resolve</code> 100 times produces byte-identical <code>Result</code> (compared via <code>Debug</code> formatting). 0 flaky outcomes.</li><li><strong>Latency budget</strong> — Benchmark 100,000 sequential <code>resolve()</code> calls on a warm policy snapshot. Total elapsed MUST be &lt; 100ms (&lt; 1µs per call). Recorded via <code>cargo bench</code> Criterion benchmark.</li><li><strong>Concurrent safety</strong> — 16 tokio tasks each call <code>resolve()</code> 10,000 times concurrently against the same <code>Arc&lt;TenantPolicy&gt;</code>. Zero panics; results match the single-threaded baseline.</li><li><strong>OBS metrics emitted</strong> — After 100 resolutions composed of 90 primary + 10 first-fallback + 0 errors, <code>ai_alias_resolutions_total{fallback_position=\"0\"}</code> increments by 90, <code>{fallback_position=\"1\"}</code> by 10. Failure counter never increments on success path.</li><li><strong>Empty fallback chain</strong> — Given <code>policy.fallback_chain: []</code> AND primary doesn't have the alias, return <code>Err(NoProviderHasAlias { providers_tried: 1 })</code> (primary counted, but no fallbacks tried).</li><li><strong>Override without provider field</strong> — A malformed override YAML missing the inner <code>provider</code> field MUST be rejected at TASK-AI-005 load time (not at <code>resolve</code> runtime). <code>resolve</code> assumes well-formed input.</li></ol>\n<hr>\n<h2>§5 — Verification method</h2>\n<p><strong>Integration test:</strong> <code>services/ai-gateway/tests/alias_resolution_test.rs</code></p>\n<pre><code class=\"language-rust\">use cyberos_ai_gateway::alias;\nuse cyberos_ai_gateway::alias::{AliasError, ResolvedModel, LatencyClass};\nuse cyberos_ai_gateway::policy::*;\nuse rust_decimal_macros::dec;\n\n#[test]\nfn resolves_chat_smart_to_bedrock_via_primary() {\n    let policy = test_policy_with_bedrock_primary();\n    let r = alias::resolve(\"chat.smart\", &amp;policy).unwrap();\n    assert_eq!(r.provider_kind, ProviderKind::Bedrock);\n    assert_eq!(r.model, \"anthropic.claude-3-5-sonnet-20241022-v2:0\");\n    assert_eq!(r.region, Some(\"ap-southeast-1\".to_string()));\n    assert_eq!(r.fallback_position, 0);\n    assert!(r.is_zdr);\n    assert_eq!(r.latency_class, LatencyClass::Standard);\n}\n\n#[test]\nfn falls_through_to_anthropic_for_chat_long() {\n    let mut policy = test_policy_with_bedrock_primary();\n    // Primary doesn't have chat.long; fallback (Anthropic native) does\n    policy.ai_policy.primary_provider.model_alias_map.remove(\"chat.long\");\n    policy.ai_policy.fallback_chain.push(Provider::Anthropic {\n        model_alias_map: hashmap! {\n            \"chat.long\".into() =&gt; \"claude-3-5-sonnet-20241022\".into(),\n        },\n    });\n    let r = alias::resolve(\"chat.long\", &amp;policy).unwrap();\n    assert_eq!(r.provider_kind, ProviderKind::Anthropic);\n    assert_eq!(r.fallback_position, 1);\n}\n\n#[test]\nfn override_beats_primary() {\n    let mut policy = test_policy_with_bedrock_primary();\n    policy.ai_policy.alias_overrides = Some(hashmap! {\n        \"chat.long\".into() =&gt; OverrideTarget {\n            provider: Provider::Anthropic {\n                model_alias_map: hashmap! { \"chat.long\".into() =&gt; \"claude-3-5-sonnet-20241022\".into() },\n            },\n        },\n    });\n    let r = alias::resolve(\"chat.long\", &amp;policy).unwrap();\n    assert_eq!(r.provider_kind, ProviderKind::Anthropic);\n    assert_eq!(r.fallback_position, 0);  // override always reports as position 0\n}\n\n#[test]\nfn unknown_alias_errors_with_supported_list() {\n    let policy = test_policy_with_bedrock_primary();\n    let err = alias::resolve(\"chat.tiny\", &amp;policy).unwrap_err();\n    match err {\n        AliasError::UnknownAlias { alias, supported } =&gt; {\n            assert_eq!(alias, \"chat.tiny\");\n            assert_eq!(supported.len(), 6);\n            assert!(supported.contains(&amp;\"chat.smart\".to_string()));\n        }\n        _ =&gt; panic!(\"expected UnknownAlias, got {:?}\", err),\n    }\n}\n\n#[test]\nfn zdr_required_but_provider_not_zdr_errors() {\n    let mut policy = test_policy_with_bedrock_primary();\n    policy.ai_policy.zdr_required = true;\n    policy.ai_policy.primary_provider = Provider::OpenAI {  // OpenAI default = not ZDR\n        model_alias_map: hashmap! { \"chat.smart\".into() =&gt; \"gpt-4o\".into() },\n    };\n    let err = alias::resolve(\"chat.smart\", &amp;policy).unwrap_err();\n    assert!(matches!(err, AliasError::ZdrViolation { resolved_provider: ProviderKind::OpenAI, .. }));\n}\n\n#[test]\nfn residency_pin_mismatch_errors() {\n    let mut policy = test_policy_with_bedrock_primary();\n    policy.ai_policy.residency = Residency::Eu1;   // Frankfurt\n    // primary is Bedrock in ap-southeast-1 — mismatch\n    let err = alias::resolve(\"chat.smart\", &amp;policy).unwrap_err();\n    assert!(matches!(err, AliasError::ResidencyViolation { policy_residency: Residency::Eu1, .. }));\n}\n\n#[test]\nfn cost_table_missing_errors() {\n    let mut policy = test_policy_with_bedrock_primary();\n    policy.ai_policy.primary_provider.model_alias_map.insert(\n        \"chat.smart\".into(), \"fake-model-not-in-cost-table\".into(),\n    );\n    let err = alias::resolve(\"chat.smart\", &amp;policy).unwrap_err();\n    assert!(matches!(err, AliasError::ResolvedModelMissingCostEntry { .. }));\n}\n\n#[test]\nfn empty_fallback_returns_no_provider() {\n    let mut policy = test_policy_with_bedrock_primary();\n    policy.ai_policy.primary_provider.model_alias_map.remove(\"chat.long\");\n    policy.ai_policy.fallback_chain.clear();\n    let err = alias::resolve(\"chat.long\", &amp;policy).unwrap_err();\n    assert!(matches!(err, AliasError::NoProviderHasAlias { providers_tried: 1, .. }));\n}\n\n// Property test for determinism\nproptest! {\n    #[test]\n    fn determinism_property(alias in alias_strategy(), policy in policy_strategy()) {\n        let r1 = alias::resolve(&amp;alias, &amp;policy);\n        let r2 = alias::resolve(&amp;alias, &amp;policy);\n        let r3 = alias::resolve(&amp;alias, &amp;policy);\n        prop_assert_eq!(format!(\"{:?}\", r1), format!(\"{:?}\", r2));\n        prop_assert_eq!(format!(\"{:?}\", r2), format!(\"{:?}\", r3));\n    }\n}</code></pre>\n<p><strong>Benchmark:</strong> <code>services/ai-gateway/benches/alias_resolution_bench.rs</code></p>\n<pre><code class=\"language-rust\">use criterion::{black_box, criterion_group, criterion_main, Criterion};\nuse cyberos_ai_gateway::alias;\n\nfn bench_resolve(c: &amp;mut Criterion) {\n    let policy = test_policy_with_bedrock_primary();\n    c.bench_function(\"alias::resolve chat.smart\", |b| {\n        b.iter(|| alias::resolve(black_box(\"chat.smart\"), black_box(&amp;policy)));\n    });\n}\n\ncriterion_group!(benches, bench_resolve);\ncriterion_main!(benches);</code></pre>\n<p>Run via:</p>\n<pre><code class=\"language-bash\">cd /Users/stephencheng/Projects/CyberSkill/cyberos\ncargo test -p cyberos-ai-gateway alias\ncargo bench -p cyberos-ai-gateway alias_resolution_bench</code></pre>\n<p><strong>CI gate:</strong> the integration test file is required on every PR touching <code>services/ai-gateway/src/alias/**</code>. Benchmark regression &gt; 20% fails the PR.</p>\n<hr>\n<h2>§6 — Implementation skeleton (suggested scaffold for AI-agent code-gen)</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/alias.rs\n\nuse crate::alias::types::{AliasError, LatencyClass, ResolvedModel};\nuse crate::cost_table;\nuse crate::policy::{Provider, ProviderKind, Residency, TenantPolicy};\nuse crate::zdr;\nuse std::time::Instant;\n\nmod metrics {\n    use once_cell::sync::Lazy;\n    use prometheus::{register_counter_vec, register_histogram, CounterVec, Histogram};\n\n    pub static ALIAS_RESOLUTIONS: Lazy&lt;CounterVec&gt; = Lazy::new(|| {\n        register_counter_vec!(\n            \"ai_alias_resolutions_total\",\n            \"Successful alias resolutions by alias, provider, and fallback position\",\n            &amp;[\"alias\", \"resolved_provider\", \"fallback_position\"]\n        ).unwrap()\n    });\n\n    pub static ALIAS_FAILURES: Lazy&lt;CounterVec&gt; = Lazy::new(|| {\n        register_counter_vec!(\n            \"ai_alias_resolution_failures_total\",\n            \"Failed alias resolutions by alias and failure reason\",\n            &amp;[\"alias\", \"reason\"]\n        ).unwrap()\n    });\n\n    pub static ALIAS_LATENCY_NS: Lazy&lt;Histogram&gt; = Lazy::new(|| {\n        register_histogram!(\n            \"ai_alias_resolution_latency_ns\",\n            \"Latency of alias::resolve calls in nanoseconds\",\n            vec![100.0, 500.0, 1_000.0, 5_000.0, 10_000.0]\n        ).unwrap()\n    });\n}\n\npub const SUPPORTED_ALIASES: &amp;[&amp;str] = &amp;[\n    \"chat.smart\", \"chat.fast\", \"chat.long\",\n    \"embed.standard\", \"embed.code\", \"rerank.fast\",\n];\n\npub fn supported_aliases() -&gt; &amp;'static [&amp;'static str] {\n    SUPPORTED_ALIASES\n}\n\npub fn resolve(alias: &amp;str, policy: &amp;TenantPolicy) -&gt; Result&lt;ResolvedModel, AliasError&gt; {\n    let started = Instant::now();\n\n    // 1. Closed-set check — cheapest reject path\n    if !SUPPORTED_ALIASES.contains(&amp;alias) {\n        metrics::ALIAS_FAILURES.with_label_values(&amp;[alias, \"unknown_alias\"]).inc();\n        return Err(AliasError::UnknownAlias {\n            alias: alias.to_string(),\n            supported: SUPPORTED_ALIASES.iter().map(|s| s.to_string()).collect(),\n        });\n    }\n\n    // 2. Override path — beats primary + fallback\n    if let Some(overrides) = &amp;policy.ai_policy.alias_overrides {\n        if let Some(override_target) = overrides.get(alias) {\n            let r = check_and_build(alias, &amp;override_target.provider, 0, policy)?;\n            record_success(alias, &amp;r, started);\n            return Ok(r);\n        }\n    }\n\n    // 3. Primary path\n    let primary = &amp;policy.ai_policy.primary_provider;\n    if let Some(model) = primary.model_for_alias(alias) {\n        let r = check_and_build_with_model(alias, primary, model, 0, policy)?;\n        record_success(alias, &amp;r, started);\n        return Ok(r);\n    }\n\n    // 4. Fallback chain (in order)\n    let mut providers_tried: u8 = 1;  // primary counted\n    for (idx, fb) in policy.ai_policy.fallback_chain.iter().enumerate() {\n        providers_tried = providers_tried.saturating_add(1);\n        if let Some(model) = fb.model_for_alias(alias) {\n            let r = check_and_build_with_model(alias, fb, model, (idx + 1) as u8, policy)?;\n            record_success(alias, &amp;r, started);\n            return Ok(r);\n        }\n    }\n\n    metrics::ALIAS_FAILURES.with_label_values(&amp;[alias, \"no_provider_has_alias\"]).inc();\n    Err(AliasError::NoProviderHasAlias {\n        alias: alias.to_string(),\n        providers_tried,\n    })\n}\n\nfn check_and_build(\n    alias: &amp;str,\n    provider: &amp;Provider,\n    fallback_position: u8,\n    policy: &amp;TenantPolicy,\n) -&gt; Result&lt;ResolvedModel, AliasError&gt; {\n    let model = provider\n        .model_for_alias(alias)\n        .ok_or_else(|| AliasError::NoProviderHasAlias {\n            alias: alias.to_string(),\n            providers_tried: 1,\n        })?;\n    check_and_build_with_model(alias, provider, model, fallback_position, policy)\n}\n\nfn check_and_build_with_model(\n    alias: &amp;str,\n    provider: &amp;Provider,\n    model: &amp;str,\n    fallback_position: u8,\n    policy: &amp;TenantPolicy,\n) -&gt; Result&lt;ResolvedModel, AliasError&gt; {\n    let kind = provider.kind();\n    let region = provider.region();\n\n    // Cost-table check (TASK-AI-007) — first because most common config error\n    if cost_table::lookup(&amp;kind, model).is_none() {\n        metrics::ALIAS_FAILURES.with_label_values(&amp;[alias, \"cost_missing\"]).inc();\n        return Err(AliasError::ResolvedModelMissingCostEntry {\n            provider: kind,\n            model: model.to_string(),\n        });\n    }\n\n    // ZDR check (TASK-AI-015 attestation table — single source of truth).\n    // Provider's own `is_zdr` field (if present in policy schema) is NOT consulted here.\n    if policy.ai_policy.zdr_required &amp;&amp; !zdr::is_zdr(&amp;kind, model) {\n        metrics::ALIAS_FAILURES.with_label_values(&amp;[alias, \"zdr\"]).inc();\n        return Err(AliasError::ZdrViolation {\n            resolved_provider: kind,\n            resolved_model: model.to_string(),\n        });\n    }\n\n    // Residency check (TASK-AI-016 matcher)\n    if let Some(region_str) = &amp;region {\n        if !crate::residency::matches(policy.ai_policy.residency, region_str) {\n            metrics::ALIAS_FAILURES.with_label_values(&amp;[alias, \"residency\"]).inc();\n            return Err(AliasError::ResidencyViolation {\n                resolved_region: Some(region_str.clone()),\n                policy_residency: policy.ai_policy.residency,\n            });\n        }\n    } else if policy.ai_policy.residency_requires_regional_provider.unwrap_or(false) {\n        // Provider has no region (e.g., Anthropic native), but policy requires regional\n        return Err(AliasError::ResidencyViolation {\n            resolved_region: None,\n            policy_residency: policy.ai_policy.residency,\n        });\n    }\n\n    Ok(ResolvedModel {\n        provider_kind: kind,\n        region,\n        model: model.to_string(),\n        fallback_position,\n        // ISS-001 fix: single source of truth for is_zdr — TASK-AI-015's attestation table.\n        // Provider's own `is_zdr` field (from policy YAML) is NOT consulted here.\n        is_zdr: zdr::is_zdr(&amp;kind, model),\n        latency_class: latency_class_for_alias(alias),\n    })\n}\n\nfn latency_class_for_alias(alias: &amp;str) -&gt; LatencyClass {\n    match alias {\n        \"chat.long\" =&gt; LatencyClass::Slow,\n        \"chat.smart\" =&gt; LatencyClass::Standard,\n        \"chat.fast\" | \"embed.standard\" | \"embed.code\" | \"rerank.fast\" =&gt; LatencyClass::Fast,\n        _ =&gt; unreachable!(\"alias already validated against SUPPORTED_ALIASES\"),\n    }\n}\n\nfn record_success(alias: &amp;str, r: &amp;ResolvedModel, started: Instant) {\n    let elapsed_ns = started.elapsed().as_nanos() as f64;\n    metrics::ALIAS_RESOLUTIONS\n        .with_label_values(&amp;[alias, &amp;format!(\"{:?}\", r.provider_kind), &amp;r.fallback_position.to_string()])\n        .inc();\n    metrics::ALIAS_LATENCY_NS.observe(elapsed_ns);\n}</code></pre>\n<p><em>Scaffold above is suggestive, not normative. AC §4 is the contract.</em></p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Code dependencies (must exist before this task can build):</strong></p>\n<ul><li><strong>TASK-AI-005</strong> — <code>TenantPolicy</code> struct + the <code>alias_overrides</code>, <code>residency_requires_regional_provider</code> field additions. The schema lives in <code>services/ai-gateway/src/policy/schema.rs</code>. This task adds 2 fields if not already present.</li><li><strong>TASK-AI-007</strong> — <code>cost_table::lookup</code> function. Used at step 5 of every resolution.</li><li><strong>TASK-AI-015</strong> — <code>zdr::is_zdr</code> function (attestation source of truth). Used at step 6.</li><li><strong>TASK-AI-016</strong> — <code>residency::matches</code> function. Used at step 7.</li><li><strong>TASK-AI-022</strong> — W3C trace-propagation contract. This task's §1 #16 asserts <code>alias::resolve</code> does not strip span context from its caller; TASK-AI-022 owns the propagation contract for the outbound <code>Provider::complete()</code> call that follows.</li></ul>\n<p><strong>Concept dependencies (must be agreed before this task can be audited):</strong></p>\n<ul><li>The closed alias set is fixed at 6 for slice 2. Adding <code>chat.toolcall</code>, <code>embed.multimodal</code>, or <code>chat.fast-long</code> is a separate task.</li><li><code>latency_class_for_alias</code> mapping is opinionated; the latency-class → actual-budget translation is a policy field, not hardcoded here.</li><li><code>fallback_position</code> semantics: override → 0, primary → 0, fallback[N] → N+1. The \"override reports as 0\" choice matches TASK-AI-008's circuit-breaker logic.</li></ul>\n<p><strong>Operational dependencies:</strong></p>\n<ul><li>The policy cache (TASK-AI-005's <code>ArcSwap&lt;HashMap&lt;String, Arc&lt;TenantPolicy&gt;&gt;&gt;</code>) must be initialised before any <code>resolve()</code> call. Startup order: cost-table → ZDR table → policy loader → router.</li><li>The cost table and ZDR table are independent ArcSwaps — both must be loaded.</li></ul>\n<p><strong>Crate dependencies (in <code>Cargo.toml</code>):</strong></p>\n<ul><li><code>prometheus</code> (for OTel metric helpers)</li><li><code>proptest</code> (dev-dep, for §5 property test)</li><li><code>criterion</code> (dev-dep, for §5 benchmark)</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Caller in TASK-AI-001 (precheck)</h3>\n<pre><code class=\"language-rust\">let resolved = alias::resolve(&amp;req.model_alias, &amp;policy)\n    .map_err(|e| match e {\n        AliasError::UnknownAlias { .. } =&gt; PrecheckError::CostEstimateFailed { reason: \"unknown_alias\".into() },\n        AliasError::ZdrViolation { .. } =&gt; PrecheckError::CostEstimateFailed { reason: \"zdr\".into() },\n        // ... map other variants\n        _ =&gt; PrecheckError::CostEstimateFailed { reason: \"alias_failed\".into() },\n    })?;\n\n// Continue with the resolved (provider_kind, model) for cost estimation:\nlet cost_per_1k = cost_table::lookup(&amp;resolved.provider_kind, &amp;resolved.model)\n    .ok_or_else(|| PrecheckError::CostEstimateFailed { reason: \"no_cost_entry\".into() })?;</code></pre>\n<h3>Output (happy path — primary)</h3>\n<pre><code class=\"language-rust\">ResolvedModel {\n    provider_kind: ProviderKind::Bedrock,\n    region: Some(\"ap-southeast-1\".to_string()),\n    model: \"anthropic.claude-3-5-sonnet-20241022-v2:0\".to_string(),\n    fallback_position: 0,\n    is_zdr: true,\n    latency_class: LatencyClass::Standard,\n}</code></pre>\n<h3>Output (fallback — primary missed)</h3>\n<pre><code class=\"language-rust\">ResolvedModel {\n    provider_kind: ProviderKind::Anthropic,\n    region: None,                  // Anthropic native has no regional pinning\n    model: \"claude-3-5-sonnet-20241022\".to_string(),\n    fallback_position: 1,          // bedrock primary didn't have chat.long\n    is_zdr: true,\n    latency_class: LatencyClass::Slow,\n}</code></pre>\n<h3>Output (override beats primary)</h3>\n<pre><code class=\"language-rust\">ResolvedModel {\n    provider_kind: ProviderKind::Anthropic,\n    region: None,\n    model: \"claude-3-5-sonnet-20241022\".to_string(),\n    fallback_position: 0,          // override always reports as position 0\n    is_zdr: true,\n    latency_class: LatencyClass::Slow,\n}</code></pre>\n<h3>Error — unknown alias</h3>\n<pre><code class=\"language-rust\">Err(AliasError::UnknownAlias {\n    alias: \"chat.tiny\".to_string(),\n    supported: vec![\n        \"chat.smart\".to_string(), \"chat.fast\".to_string(), \"chat.long\".to_string(),\n        \"embed.standard\".to_string(), \"embed.code\".to_string(), \"rerank.fast\".to_string(),\n    ],\n})</code></pre>\n<h3>Error — ZDR violation</h3>\n<pre><code class=\"language-rust\">Err(AliasError::ZdrViolation {\n    resolved_provider: ProviderKind::OpenAI,\n    resolved_model: \"gpt-4o\".to_string(),\n})</code></pre>\n<h3>Error — residency violation</h3>\n<pre><code class=\"language-rust\">Err(AliasError::ResidencyViolation {\n    resolved_region: Some(\"us-east-1\".to_string()),\n    policy_residency: Residency::Sg1,\n})</code></pre>\n<h3>Error — cost-table missing</h3>\n<pre><code class=\"language-rust\">Err(AliasError::ResolvedModelMissingCostEntry {\n    provider: ProviderKind::Bedrock,\n    model: \"claude-99-fake-model\".to_string(),\n})</code></pre>\n<h3>memory audit row carrying resolved metadata (via TASK-AI-001 precheck row)</h3>\n<pre><code class=\"language-json\">{\n  \"seq\": 18421,\n  \"ts_ns\": 1763112131000000000,\n  \"op\": \"put\",\n  \"path\": \"memories/decisions/ai-invocations/1763112131000_org-cyberskill_01HZK9R7A2B4C8D6.md\",\n  \"extra\": {\n    \"kind\": \"ai.precheck\",\n    \"tenant_id\": \"org:cyberskill\",\n    \"agent_persona\": \"cuo-cpo@0.4.1\",\n    \"model_alias\": \"chat.smart\",\n    \"resolved_provider\": \"bedrock\",\n    \"resolved_model\": \"anthropic.claude-3-5-sonnet-20241022-v2:0\",\n    \"resolved_region\": \"ap-southeast-1\",\n    \"fallback_position\": 0,\n    \"is_zdr\": true,\n    \"latency_class\": \"standard\",\n    \"estimated_usd\": 0.0085,\n    \"current_spent_usd\": 47.23\n  },\n  \"prev_chain\": \"...\",\n  \"chain\": \"...\"\n}</code></pre>\n<h3>OBS metric snapshot after 100 resolutions</h3>\n<pre><code># HELP ai_alias_resolutions_total Successful alias resolutions\n# TYPE ai_alias_resolutions_total counter\nai_alias_resolutions_total{alias=\"chat.smart\",resolved_provider=\"Bedrock\",fallback_position=\"0\"} 78\nai_alias_resolutions_total{alias=\"chat.smart\",resolved_provider=\"Anthropic\",fallback_position=\"1\"} 7\nai_alias_resolutions_total{alias=\"chat.fast\",resolved_provider=\"Bedrock\",fallback_position=\"0\"} 12\nai_alias_resolutions_total{alias=\"chat.long\",resolved_provider=\"Anthropic\",fallback_position=\"0\"} 3\n\n# HELP ai_alias_resolution_latency_ns Latency of alias::resolve calls\n# TYPE ai_alias_resolution_latency_ns histogram\nai_alias_resolution_latency_ns_bucket{le=\"100\"} 87\nai_alias_resolution_latency_ns_bucket{le=\"500\"} 99\nai_alias_resolution_latency_ns_bucket{le=\"1000\"} 100</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. No deferred questions.</p>\n<p>For reference, the questions considered + resolved during authoring:</p>\n<ol><li><strong>~~Should latency_class be exposed in ResolvedModel or computed by caller?~~</strong> — RESOLVED: exposed in struct. Callers shouldn't re-derive; mapping is TASK-AI-006's concern.</li><li><strong>~~Should override path consult ZDR / residency / cost-table checks?~~</strong> — RESOLVED: yes (§1 #2 + ACs #9, #10). Override is a policy decision; policy invariants still apply.</li><li><strong>~~What's the override fallback semantics?~~</strong> — RESOLVED: override is terminal. If override fails ZDR / residency / cost, error returns immediately; we do NOT fall through to primary. Rationale: the override exists because the operator explicitly wanted that target; falling through would silently subvert their intent.</li><li><strong>~~Should we cache resolved results?~~</strong> — RESOLVED: no. The resolution is sub-microsecond. Caching adds invalidation complexity for zero perceptible gain.</li><li><strong>~~How does <code>residency_requires_regional_provider</code> interact with Anthropic native?~~</strong> — RESOLVED: default false (Anthropic native passes residency by being region-less). Setting true rejects region-less providers — useful for the most-strict residency tenants. Documented in §1 #7 + §3 schema.</li></ol>\n<hr>\n<h2>§10 — Failure modes inventory (all error paths)</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Return</th><th>HTTP map</th><th>Recovery</th></tr></thead><tbody><tr><td>Unknown alias</td><td>Closed-set check at step 1</td><td><code>Err(UnknownAlias)</code> with supported list</td><td>Caller maps to <code>400 BAD_REQUEST</code></td><td>Caller fixes; static check possible at compile time via const</td></tr><tr><td>ZDR violation (primary)</td><td><code>zdr::is_zdr</code> returns false + <code>zdr_required: true</code></td><td><code>Err(ZdrViolation { resolved_provider, resolved_model })</code></td><td><code>403 FORBIDDEN</code> (<code>zdr_violation</code>)</td><td>Operator switches provider or relaxes <code>zdr_required</code>; TASK-AI-015 attestation refresh</td></tr><tr><td>ZDR violation (override)</td><td>Same check on override target</td><td><code>Err(ZdrViolation)</code> (no fallthrough)</td><td><code>403</code></td><td>Operator removes override or chooses ZDR target</td></tr><tr><td>Residency violation (regional mismatch)</td><td><code>residency::matches</code> returns false</td><td><code>Err(ResidencyViolation { resolved_region, policy_residency })</code></td><td><code>403 FORBIDDEN</code></td><td>Operator pins provider to the right region</td></tr><tr><td>Residency violation (no region on provider)</td><td><code>residency_requires_regional_provider: true</code> + provider has no region</td><td><code>Err(ResidencyViolation { resolved_region: None, .. })</code></td><td><code>403</code></td><td>Operator chooses regional provider or relaxes the flag</td></tr><tr><td>Cost-table missing entry</td><td><code>cost_table::lookup</code> returns None</td><td><code>Err(ResolvedModelMissingCostEntry { provider, model })</code></td><td><code>503 SERVICE_UNAVAILABLE</code> (<code>no_cost_table_for_model</code>)</td><td>Operator adds entry to <code>cost_rates.yaml</code>; gateway hot-reloads</td></tr><tr><td>No provider has alias</td><td>Walked primary + all fallbacks; none match</td><td><code>Err(NoProviderHasAlias { alias, providers_tried })</code></td><td><code>503</code> (<code>no_provider_for_alias</code>)</td><td>Operator updates <code>model_alias_map</code> in policy YAML</td></tr><tr><td>Empty fallback chain + primary miss</td><td>Special case of above; <code>providers_tried: 1</code></td><td>Same as above</td><td><code>503</code></td><td>Operator adds at least one fallback or fixes primary</td></tr><tr><td>Concurrent policy hot-reload</td><td><code>ArcSwap::load()</code> snapshot semantics</td><td>In-flight call uses pre-reload snapshot; next call sees new</td><td>(no error)</td><td>By design — no torn read possible</td></tr><tr><td>Policy not initialised</td><td><code>OnceCell::get()</code> returns None (TASK-AI-005 init pending)</td><td>This case is impossible at runtime — TASK-AI-005 init blocks gateway startup</td><td>(gateway exits 1)</td><td>Operator fixes config; redeploys</td></tr><tr><td>Override YAML malformed</td><td>Rejected at TASK-AI-005 load time</td><td>(never reaches resolve)</td><td>(gateway exits 1)</td><td>Operator fixes YAML; redeploys</td></tr><tr><td><code>model_for_alias</code> returns empty string</td><td>Defensive: treated as miss → <code>NoProviderHasAlias</code></td><td>Same as above</td><td><code>503</code></td><td>Operator fixes policy YAML (empty model string is a config error)</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes (informational, no normative force)</h2>\n<ul><li>This task is intentionally small in code (6h effort) because the complexity lives in the data model (TASK-AI-005's policy schema) and the cross-cutting attestation tables (TASK-AI-007 cost, TASK-AI-015 ZDR, TASK-AI-016 residency). The function itself is ~150 lines of Rust.</li><li>After this task ships, every other AI Gateway task uses <code>alias::resolve()</code> instead of inline alias logic. A refactor PR will touch TASK-AI-001's §6 skeleton to call <code>resolve()</code> rather than the inline <code>resolve_model_alias</code> placeholder. The refactor is mechanical.</li><li>The closed alias set is intentional friction — adding a new alias (<code>chat.toolcall</code>, <code>embed.multimodal</code>, <code>chat.fast-long</code>) requires updating the const, the schema, the latency-class mapping, and the test suite. This is the right amount of pushback against ad-hoc alias proliferation.</li><li>The \"override reports as <code>fallback_position: 0</code>\" choice is debatable. The alternative — reporting some marker like <code>255</code> — would make \"this call used an override\" visible in OBS. The current choice prioritises simplicity (no special case). If operators report that override visibility is needed, we add a <code>was_override: bool</code> field in slice 4 (task-AI-006a).</li><li>Latency budget on this function is so tight that the OBS histogram has nano-second buckets (100ns / 500ns / 1µs / 5µs / 10µs). Any call landing in the &gt;10µs bucket triggers an investigation — likely a CPU pegged elsewhere.</li><li>The latency-class → timeout-budget mapping (<code>LatencyClass::timeout_budget_seconds</code>) is a convenience helper for TASK-AI-008. The policy field <code>call_timeout_seconds</code> is still the canonical config; this helper just translates it.</li><li>TASK-AI-006 + TASK-AI-007 + TASK-AI-015 + TASK-AI-016 together form the \"policy enforcement layer\" that sits between consumers and the provider layer. Treat the four as a unit — none ships alone.</li></ul>\n<hr>\n<p><em>End of TASK-AI-006. Status: draft (10/10 target, expanded from compressed first-pass per workflow correction 2026-05-15). Run <code>task-audit</code> next: <code>cargo run -p cyberos-skill-cli -- run task-audit --input '{\"artefact_paths\": [\"docs/tasks/ai/TASK-AI-006-model-alias-resolution/spec.md\"]}'</code></em></p>";
