window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-APP-001"]="<h1>TASK-APP-001 - Desktop CyberOS operations UI</h1>\n<h2>Context</h2>\n<p>Employees should trigger CyberOS operations from the desktop app instead of the terminal: build the distributable payload, pick a project folder, check its installed vs available version, and init/update it. The desktop app is the Tauri + Svelte shell at <code>services/memory/desktop</code>; operations wrap the existing tooling (<code>tools/install/build.sh</code>, <code>init.sh</code>, <code>init.sh --check</code>) so the UI and the CLI can never behave differently.</p>\n<h2>1. Normative clauses</h2>\n<ol><li>The app MUST expose an Operations view with: (a) \"Build payload\" - runs <code>tools/install/build.sh</code> in the configured CyberOS checkout; (b) a project picker - a scanned list of git repositories under <code>~/Projects</code> (showing each repo's installed CyberOS version) plus manual path entry (a native folder dialog is an optional follow-up); (c) \"Check\" - runs <code>init.sh --check &lt;project&gt;</code> and shows installed vs available version and whether an update exists; (d) \"Init / Update\" - runs <code>init.sh &lt;project&gt;</code> and shows the summary.</li><li>All operations MUST shell out to the canonical scripts (no reimplementation of init logic in the app). The CyberOS checkout path MUST be configurable in the UI and persisted; it defaults to <code>~/Projects/CyberSkill/cyberos</code>.</li><li>Command output (stdout+stderr) MUST be shown in the UI, and a non-zero exit MUST surface as a visible failure state - never silently swallowed.</li><li>The Tauri commands MUST validate that the configured checkout contains <code>tools/install/build.sh</code> (and the payload <code>dist/cyberos/init.sh</code> for check/init) before running, returning a structured error otherwise.</li><li>Init MUST NOT run against the CyberOS checkout itself (guard: target != checkout root).</li><li>No operation pushes, commits, or deletes anything in the target project; the commands only run the existing scripts, which are non-destructive and idempotent by design.</li></ol>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] From the app: build payload succeeds and reports the payload path + version.</li><li>[ ] Pick a project folder, Check shows <code>installed=X available=Y</code> (or \"not initialised\"), Init lays down <code>.cyberos/</code> and reports the summary; re-running Init is a clean update (store untouched).</li><li>[ ] Failure paths render the error output (e.g. missing checkout, script failure).</li><li>[ ] <code>cargo check</code> green on the tauri crate; frontend builds.</li></ul>\n<h2>3. Gate</h2>\n<p>Machine: <code>cargo check</code> (desktop src-tauri) + frontend build + manual smoke on the operator's Mac. Review + final acceptance: HITL per STATUS-REFERENCE §1.4.</p>";
