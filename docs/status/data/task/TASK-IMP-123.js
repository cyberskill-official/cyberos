window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-123"]="<h1>TASK-IMP-123: phase must have a mechanism or be removed</h1>\n<h2>Summary</h2>\n<p><code>phase</code> reads like a release gate and schedules nothing. It is absent from the task template, absent from the machine floor, and absent from the batch selector; its only consumers are the docs-site renderers, which draw it as a badge. On 2026-07-18 it let TASK-IMP-111 - explicitly <code>phase: post-1.0.0</code> - take batch 1 and block every <code>pre-1.0.0 release</code> task behind it. This task decides whether <code>phase</code> earns a mechanism or leaves the corpus.</p>\n<h2>Problem</h2>\n<p>Measured on main <code>0ddcf73b</code>:</p>\n<ul><li><code>grep -c phase tools/install/docs-tools/batch-select.mjs</code> -&gt; 0</li><li><code>grep -c phase tools/install/docs-tools/task-lint.mjs</code> -&gt; 0</li><li><code>phase</code> does not appear in <code>tools/install/templates/TASK-TEMPLATE.md</code></li><li>the only readers are <code>tools/docs-site/render-task-catalog.mjs</code> (a badge) and <code>tools/docs-site/nfr-extract.mjs</code></li></ul>\n<p>So <code>phase</code> is an undeclared, unvalidated, free-text field that no decision reads. Two consequences, both observed:</p>\n<ol><li><strong>It misleads.</strong> <code>batch-select</code> sorts on <code>priority</code> alone (<code>:88</code>). TASK-IMP-111 (<code>p1</code>, <code>post-1.0.0</code>) outranked TASK-IMP-106 and -107 (<code>p2</code>, <code>pre-1.0.0 release</code>) and, its cone spanning all three services, excluded all ten other eligible tasks. The one task marked \"not for this release\" was scheduled first and blocked the release queue. An author who wrote release intent into <code>phase</code> had no way to know nothing would read it.</li><li><strong>It is two fields wearing one name.</strong> Improvement tasks use <code>pre-1.0.0 release</code> / <code>post-1.0.0</code> - a release gate. Product tasks (INV/OBS/TEN) use <code>P0</code> / <code>P2</code> - a rollout wave, matching <code>render-module-changelog.mjs</code>'s module map. TASK-IMP-117..120 have no <code>phase</code> at all. Nothing reconciles them, and the docs-site renders both as the same badge.</li></ol>\n<p>This is the shape TASK-IMP-116 was created to close for <code>## 11a</code>'s batch default: a promise with no mechanism under it. The immediate scheduling harm was addressed on 2026-07-18 by moving the release intent into <code>priority</code>, the field that does schedule (commit <code>d19362ad</code>) - a fix that works and leaves a known cost: <code>priority</code> now carries two meanings, importance and release-gating, which is the ambiguity <code>phase</code> existed to separate.</p>\n<h2>Proposed Solution</h2>\n<p>DECISION REQUIRED, and the task must not pre-judge it. Two defensible ends, and the one thing that is not defensible is the status quo:</p>\n<ul><li><strong>(a) Give it a mechanism.</strong> Add <code>phase</code> to the template with a closed enum, lint it, reconcile the two vocabularies across the corpus, and give <code>batch-select</code> a documented rule for how phase and priority compose.</li><li><strong>(b) Remove it.</strong> Delete <code>phase</code> from the corpus and the renderers; <code>priority</code> carries scheduling and the release checklist carries release gating. Costs the badge, ends the ambiguity.</li></ul>\n<p>The operator picks at the PLAN gate of the implementing run. Whichever is chosen, the acceptance below is symmetric: after this task, no field named <code>phase</code> may exist that a reader could reasonably believe schedules something while nothing reads it.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Teach <code>batch-select</code> to read <code>phase</code> as-is. Rejected: with two live vocabularies and four tasks missing the field, a scheduler reading it would have to guess what <code>P2</code> means against <code>post-1.0.0</code>, and guessing is how it misled in the first place.</li><li>Leave it as a badge and document that it is decorative. Rejected: the badge is exactly what makes an author believe it is load-bearing. A field that looks like a gate and is documented elsewhere as inert is a trap with a footnote.</li><li>Fold into TASK-IMP-119 (cone drift audit). Rejected: 119 audits declared cones against touched files - a different question about a different field.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: no field exists that appears to gate scheduling while nothing reads it - verified by an assertion tied to whichever branch is chosen, not by inspection.</li><li>Guardrail: whichever branch, the batch selected for the current queue MUST be explainable from the fields that remain.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the decision, and its execution across template, lint, batch-select, the corpus, and the renderers.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Reverting <code>d19362ad</code>'s priority flips. They stand regardless; if (a) is chosen, whether to move the intent back is a separate call.</li><li><code>priority</code>'s own semantics.</li><li>The docs-site's visual design.</li></ul>\n<h2>Dependencies</h2>\n<p>None. Independent of TASK-IMP-119 and TASK-IMP-116 despite the shared family.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from greps and a measured batch-select run on merged main; the decision itself is deliberately left to the operator. Implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> filed at the 2026-07-18 PLAN gate as <code>[DECISION]</code>; the (a)/(b) call is the operator's at the implementing run's PLAN gate. Both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The implementing run MUST halt at its PLAN gate for the operator to choose branch (a) mechanism or (b) removal. The run MUST NOT choose on the operator's behalf.</li><li>1.2 If (a): <code>phase</code> MUST be added to <code>TASK-TEMPLATE.md</code> with a closed enum, and <code>task-lint</code> MUST reject a value outside it.</li><li>1.3 If (a): every task in the corpus MUST carry a <code>phase</code> value inside the enum - the two live vocabularies reconciled, and the four tasks now missing the field given one.</li><li>1.4 If (a): <code>batch-select</code> MUST apply a documented rule composing <code>phase</code> and <code>priority</code>, and MUST state that rule where a reader of its output can find it.</li><li>1.5 If (b): <code>phase</code> MUST be removed from every task in the corpus and from every renderer that reads it, and <code>task-lint</code> MUST reject <code>phase</code> as an unknown key so it cannot return by habit.</li><li>1.6 Either branch MUST leave <code>docs/status/</code> renderable and the site build green.</li><li>1.7 Neither branch may leave a field that a task author could reasonably read as scheduling while no scheduler reads it.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - the run's PLAN gate records an operator verdict naming (a) or (b) before any file is written - verify: the run's ship-manifest carries the recorded HITL verdict naming the chosen branch, and its timestamp precedes the first file write (ops check recorded in the gate log; a human verdict cannot be discharged by a test, and no test is claimed for it).</li><li>[ ] AC 2 (traces_to: #1.2, #1.5) - branch (a): a task with <code>phase: nonsense</code> fails task-lint non-zero. Branch (b): a task carrying any <code>phase</code> key fails task-lint non-zero. The assertion MUST be on the exit code, not on printed text - test: <code>tools/install/tests/test_batch_select.sh::t30_phase_enum_or_absent_enforced</code></li><li>[ ] AC 3 (traces_to: #1.3, #1.5) - the whole corpus satisfies the chosen branch: zero tasks outside the enum (a), or zero tasks carrying <code>phase</code> (b) - test: <code>tools/install/tests/test_batch_select.sh::t31_corpus_conforms</code></li><li>[ ] AC 4 (traces_to: #1.4) - branch (a) only: a fixture where phase and priority disagree selects the batch the documented rule predicts, and the test MUST FAIL if <code>batch-select</code> ignores <code>phase</code> - test: <code>tools/install/tests/test_batch_select.sh::t32_phase_composes_with_priority</code></li><li>[ ] AC 5 (traces_to: #1.7) - the regression that motivated this task cannot recur: a fixture reproducing the 2026-07-18 queue (a p1 post-1.0.0 task with a three-service cone against p2 pre-1.0.0 tasks) MUST NOT silently schedule the post-1.0.0 task first - under (a) the rule prevents it; under (b) no field claims otherwise and the test asserts <code>phase</code> is absent from the corpus. A rule that cannot fail the case that motivated it is decoration - test: <code>tools/install/tests/test_batch_select.sh::t33_motivating_case_cannot_recur</code></li><li>[ ] AC 6 (traces_to: #1.6) - <code>bash tools/docs-site/build.sh</code> exits 0 and the status page renders after the change - test: <code>tools/install/tests/test_batch_select.sh::t34_site_still_builds</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A consumer repo whose tasks carry <code>phase</code> values from neither vocabulary: branch (a)'s enum MUST be chosen knowing it lints third-party corpora; branch (b) removes the field from ours and MUST NOT silently rewrite theirs.</li><li>The four tasks with no <code>phase</code> (TASK-IMP-117..120): under (a) they need a value, and inventing one is an authoring decision, not a migration's - the run MUST surface them rather than default them.</li><li>Product tasks' <code>P0</code>/<code>P2</code>: these mirror <code>render-module-changelog.mjs</code>'s module map, so (b) MUST confirm nothing else reads them before deleting.</li><li>A task in flight when the migration lands: the corpus check runs against the tree at the time, and a task authored mid-migration with the old shape fails lint - which is the mechanism working, not a bug.</li><li><code>done</code> tasks: 183 of them carry <code>phase</code>; whether history is rewritten or grandfathered is a scope call for the PLAN gate and MUST NOT be assumed. Retroactively editing shipped specs on a rule they predate is the machine making a scope decision that belongs to the operator (same reasoning as TASK-IMP-118 §3 row 11).</li><li>Security-class: none. No untrusted input, no execution path.</li></ul>";
