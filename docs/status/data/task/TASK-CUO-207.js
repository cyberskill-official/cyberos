window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-207"]="<h1>TASK-CUO-207: Gate autodetect portability + per-repo config</h1>\n<h2>§1 - Description</h2>\n<p>Make /init produce working gates on the stacks CyberSkill's client projects actually use, and give every repo one sanctioned place to override what detection gets wrong - so vendored files stay pristine and updates never clobber local decisions.</p>\n<p>Normative clauses:</p>\n<ol><li>Gate autodetection MUST extend to, in documented order after the existing Rust/Node/Python detectors: Go (<code>go.mod</code> -&gt; build <code>go build ./...</code>, lint <code>go vet ./...</code>, test <code>go test ./...</code>, coverage <code>go test -coverprofile</code>), Maven (<code>pom.xml</code> -&gt; <code>mvn -q -DskipTests package</code> / <code>mvn -q verify</code>), Gradle (<code>build.gradle</code> or <code>build.gradle.kts</code>, preferring <code>./gradlew</code> when present -&gt; <code>build</code> / <code>test</code>), .NET (<code>*.sln</code> or <code>*.csproj</code> -&gt; <code>dotnet build</code> / <code>dotnet test</code>), PHP (<code>composer.json</code> -&gt; <code>composer validate --strict</code> plus <code>vendor/bin/phpunit</code> when present), Ruby (<code>Gemfile</code> -&gt; <code>bundle exec rspec</code> when spec/ exists, else <code>bundle exec rake test</code> when a Rakefile exists). Multi-stack repos MUST union the detected gates; detection MUST never invent a command whose tool marker file is absent.</li><li>A per-repo config file <code>.cyberos/config.yaml</code> MUST be honored by <code>run-gates.sh</code> when present, with keys: <code>gates.build</code>, <code>gates.lint</code>, <code>gates.test</code>, <code>gates.coverage</code> (string commands; each overrides ONLY its own gate), <code>coverage_threshold</code> (integer, default 90), <code>task_template</code> (<code>engineering-spec@1</code> | <code>task@1</code>, consumed by TASK-CUO-208), <code>profile</code> (<code>full</code> | <code>reduced</code>). Unknown keys MUST warn, not fail.</li><li><code>init.sh</code> MUST scaffold <code>.cyberos/config.yaml</code> exactly once (never clobber an existing one, same discipline as BACKLOG/AGENTS), pre-filled with every value commented out and the DETECTED commands written as comments beside each key, so the file documents what will run by default.</li><li><code>run-gates.sh</code> MUST resolve each gate as: config value if set, else autodetected, else absent - and MUST print one provenance line per gate before running it: <code>gate &lt;name&gt;: &lt;command&gt; (source: config|autodetect:&lt;stack&gt;|absent)</code>.</li><li><code>coverage_threshold</code> MUST flow to the coverage gate: <code>run-gates.sh</code> exposes it (env <code>CYBEROS_COVERAGE_THRESHOLD</code>) and the coverage-gate skill contract reads it, defaulting to 90 when unset (hook already named by TASK-SKILL-118's rubric constants).</li><li>Repos where nothing is detected and no config exists MUST keep today's reduced-floor behavior with an explicit message naming the config file as the fix.</li><li>Config parsing MUST be dependency-free (grep/sed-level YAML subset: top-level and one nesting level, scalar values); a malformed config MUST fail gates loudly with the offending line, never half-apply.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Config-over-autodetect (per key, not all-or-nothing) matches how real repos deviate: usually one gate is special, the rest are standard. Scaffolding the config WITH detection results as comments makes /init self-documenting on day one and keeps the file inert until the operator uncomments a line - update-safe by construction. The provenance line kills the classic debugging question (\"which command even ran?\") across a fleet of differently-shaped repos.</p>\n<h2>§3 - Contract</h2>\n<pre><code class=\"language-yaml\"># .cyberos/config.yaml (scaffolded form, everything commented)\n# gates:\n#   build: \"go build ./...\"        # autodetected: go\n#   lint: \"go vet ./...\"           # autodetected: go\n#   test: \"go test ./...\"          # autodetected: go\n#   coverage: \"go test -coverprofile=coverage.out ./...\"   # autodetected: go\n# coverage_threshold: 90\n# fr_template: engineering-spec@1\n# profile: full</code></pre>\n<p>Provenance output: <code>gate test: go test ./... (source: autodetect:go)</code> | <code>gate lint: make lint (source: config)</code> | <code>gate coverage: (source: absent)</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Each new stack detects</strong> (§1 #1) - fixture repos for Go, Maven, Gradle (with and without wrapper), .NET, PHP (with and without phpunit), Ruby (rspec and rake variants) each yield the specified commands and nothing else.</li><li><strong>Multi-stack unions</strong> (§1 #1) - a fixture with <code>go.mod</code> + <code>package.json</code> yields both stacks' gates, deduplicated by gate name with both provenance lines.</li><li><strong>No marker, no command</strong> (§1 #1) - a PHP fixture without <code>vendor/bin/phpunit</code> gets <code>composer validate --strict</code> only.</li><li><strong>Config overrides per key</strong> (§1 #2, #4) - config setting only <code>gates.lint</code> leaves build/test/coverage autodetected; provenance lines show <code>config</code> for lint and <code>autodetect</code> for the rest.</li><li><strong>Scaffold once, never clobber</strong> (§1 #3) - first init writes the commented config with detected values; a hand-edited config survives a re-init byte-identical.</li><li><strong>Threshold flows</strong> (§1 #5) - <code>coverage_threshold: 85</code> surfaces as <code>CYBEROS_COVERAGE_THRESHOLD=85</code> in the gate environment; unset -&gt; 90.</li><li><strong>Reduced floor preserved with pointer</strong> (§1 #6) - an empty fixture repo reports the floor message naming <code>.cyberos/config.yaml</code>.</li><li><strong>Malformed config fails loudly</strong> (§1 #7) - a config with a tab-indented or unparseable line fails <code>run-gates.sh</code> citing the line number; no gate runs.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/cyberos-init/tests/test_gate_autodetect.sh\nt01_stack_matrix()               # AC 1  (8 fixture repos, expected command sets)\nt02_multistack_union()           # AC 2\nt03_marker_gating()              # AC 3\nt04_config_per_key_override()    # AC 4\nt05_scaffold_once()              # AC 5\nt06_threshold_env()              # AC 6\nt07_reduced_floor_message()      # AC 7\nt08_malformed_config_loud()      # AC 8</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p><code>init.sh</code>: extend the detector case-block; emit the commented config via heredoc when absent. <code>run-gates.sh</code>: minimal reader (<code>cfg_get key</code> via awk over the two-level subset), resolution order per gate, provenance echo, threshold export. README: stack table + config reference.</p>\n<h2>§7 - Dependencies</h2>\n<p>Blocks TASK-CUO-208 (it reads <code>task_template</code> from this config). TASK-SKILL-118's coverage rubric names the threshold hook this task turns on. No dependency on Wave A.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ bash .cyberos/cuo/gates/run-gates.sh\ngate build: ./gradlew build (source: autodetect:gradle)\ngate lint: (source: absent)\ngate test: ./gradlew test (source: autodetect:gradle)\ngate coverage: mvn -q verify (source: config)</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Container-based stacks (Docker-only repos) stay reduced-floor by design; a <code>gates.*</code> config line is their sanctioned path - documenting a docker-compose example in README is part of #1's doc work, not a new detector. JVM coverage is deliberately undetected (jacoco/kover wiring is repo-specific); the scaffolded config's commented <code>gates.coverage</code> line is the sanctioned path there too.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Wrong tool version on the machine (gradle vs gradlew drift) - wrapper-preferred rule (#1) plus provenance line make the executed command visible; config overrides when the default is wrong.</li><li>Autodetect finds a marker in a vendored subdir (<code>node_modules/package.json</code>) - detectors MUST scan the repo root only (as today); fixture t03 includes a nested marker that must not fire.</li><li>Config injection (config runs arbitrary commands) - accepted by design: the config is repo-committed and operator-owned, same trust level as a Makefile; the provenance line keeps it visible.</li><li>YAML subset surprises (quotes, colons in commands) - the reader supports quoted scalars; t08 covers an unsupported construct failing loudly rather than mis-parsing.</li><li>Threshold set above 100 or non-integer - reader validates 1..100 integer; else loud fail (t08 family).</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep detector order stable and documented in README (first-party stacks first); the union rule means order only affects provenance labels, never which gates exist. The config reader lives in run-gates.sh (payload-vendored) so target repos need no extra file.</p>\n<p><em>End of TASK-CUO-207.</em></p>";
