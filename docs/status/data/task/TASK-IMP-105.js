window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-105"]="<h1>TASK-IMP-105: Task-id allocation rule + next-id helper</h1>\n<h2>Summary</h2>\n<p><code>task-author</code> has no rule for choosing a task id - the model picks, and the only safety net is <code>backlog-mutate</code>'s uniqueness gate, which fires at INSERT after the spec folder is already on disk. Two authoring runs against one module can therefore both pick the same id, write two folders, and leave an orphan when the second insert refuses. State the allocation rule in the skill and make it executable as <code>backlog-mutate next-id &lt;module&gt;</code>.</p>\n<h2>Problem</h2>\n<p><code>grep</code> for an allocation rule in <code>task-author/SKILL.md</code> finds nothing: no \"scan existing ids\", no \"next available\". The uniqueness pre-image gate in <code>backlog-mutate.mjs:258</code> refuses with exit 7 (<code>row already present ... uniqueness pre-image violated</code>) - correct, but late: the spec files exist by then, and the operator is left with a half-landed task and no instruction.</p>\n<p>Authoring is usually serial, which is why this has not bitten. This run interleaved five batches and got lucky. The pattern this run keeps re-learning is that every mechanical rule left in prose gets re-derived wrongly eventually, and every one moved into a tool stops being a question.</p>\n<h2>Proposed Solution</h2>\n<p>Add the rule to <code>task-author</code>: allocate by scanning <code>docs/tasks/&lt;module&gt;/</code> for the highest existing stem and taking the next, and re-scan immediately before writing rather than trusting an id chosen at PLAN time. Add <code>backlog-mutate next-id &lt;module&gt;</code> so the rule is executable rather than remembered - it reads the same corpus the insert gate reads, so allocation and admission cannot disagree. The gate stays exactly as it is: this narrows the window, it does not replace the check.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>A lock around authoring. Rejected: heavier than the defect, and it would serialise an operation that is legitimately parallel across different modules.</li><li>Random or timestamp ids. Rejected: the corpus's id-ascending ordering is load-bearing for the regenerator's row grammar and for humans reading the backlog.</li><li>Rely on the insert gate alone (status quo). Rejected: it refuses correctly but only after the files exist, which converts a preventable collision into a cleanup.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: <code>next-id</code> returns the correct next stem for a populated module, an empty module, and a module with gaps - suite-asserted. Baseline: no allocation rule exists at all.</li><li>Guardrail: the uniqueness gate still refuses a duplicate insert (this task must not weaken it), asserted by the existing exit-7 arm.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the allocation rule in <code>task-author/SKILL.md</code>, <code>backlog-mutate next-id &lt;module&gt;</code>, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any change to the uniqueness gate's behavior - it remains the authority.</li><li>Cross-module id coordination (ids are per-module by construction).</li><li>Reserving ids ahead of authoring (a queue this backlog does not need).</li></ul>\n<h2>Dependencies</h2>\n<p>None logically.</p>\n<p><strong>Serialisation note:</strong> touches <code>backlog-mutate.mjs</code> (shared with TASK-IMP-108, which adds <code>entered_via</code> to the same writer). Parent-serialised per §11a.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md IMP-26, verified against task-author/SKILL.md and backlog-mutate.mjs on merged main; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>task-author/SKILL.md</code> MUST state the allocation rule: the next id for a module is the highest existing stem in <code>docs/tasks/&lt;module&gt;/</code> plus one, and it MUST be re-scanned immediately before writing files rather than reused from PLAN time.</li><li>1.2 <code>backlog-mutate</code> MUST expose <code>next-id &lt;module&gt;</code> printing the next free stem to stdout and exiting 0.</li><li>1.3 <code>next-id</code> MUST derive from the same corpus the insert gate reads, so an id it returns cannot be rejected by the gate for non-uniqueness in the same instant.</li><li>1.4 <code>next-id</code> on a module with no tasks MUST return that module's first stem and exit 0 (an empty module is not an error).</li><li>1.5 <code>next-id</code> MUST ignore gaps: it returns highest+1, never the lowest free number, because reusing a retired id makes two different tasks share a name in the history.</li><li>1.6 The uniqueness pre-image gate MUST remain unchanged and MUST remain the authority on admission.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.2, #1.3) - <code>next-id improvement</code> on the live corpus returns the highest+1 stem and exits 0 - test: <code>tools/install/tests/test_workflow_helpers.sh::t15_next_id_populated</code></li><li>[ ] AC 2 (traces_to: #1.4) - <code>next-id &lt;empty-module&gt;</code> returns the first stem and exits 0 - test: <code>tools/install/tests/test_workflow_helpers.sh::t16_next_id_empty_module</code></li><li>[ ] AC 3 (traces_to: #1.5) - a corpus with a gap yields highest+1, not the gap - test: <code>tools/install/tests/test_workflow_helpers.sh::t17_next_id_ignores_gaps</code></li><li>[ ] AC 4 (traces_to: #1.6) - the existing exit-7 uniqueness refusal still fires unchanged - test: <code>tools/install/tests/test_workflow_helpers.sh::t07_insert_uniqueness_refusal</code></li><li>[ ] AC 5 (traces_to: #1.1) - the skill states the rule including the re-scan-before-write requirement - verify: recorded grep in the gate log (prose contract; same rationale as TASK-IMP-090 AC 1).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Folder present on disk but no BACKLOG row (a half-landed task from the exact collision this fixes): <code>next-id</code> MUST count the folder - the folder is the task, the row is the index, and skipping it would hand out the colliding id again.</li><li>Malformed stem in the module (hand-created folder not matching the grammar): skip it with a note on stderr; one bad folder must not stop allocation.</li><li>Two <code>next-id</code> calls racing: both return the same stem. This narrows the window, it does not close it - the gate remains the authority (1.6), and this is why 1.6 exists.</li><li>A module whose name does not yet exist as a directory: treated as empty per 1.4.</li><li>Security-class: reads directory names, prints a stem. No untrusted content is executed; the module argument MUST be confined under <code>docs/tasks/</code> on the same <code>relUnderRoot</code> rule the other helpers use, so a crafted <code>../</code> argument cannot walk out.</li></ul>";
