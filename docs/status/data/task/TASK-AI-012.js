window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-012"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The Presidio sidecar (TASK-AI-011) <strong>MUST</strong> register 6 Vietnamese-specific PII recognizers. Each recognizer:</p>\n<ol><li><strong>MUST</strong> match the documented pattern with <strong>recall ≥ 99% per entity type AND ≥ 99% aggregate</strong> on the 200-sample VN PII test set (TASK-AI-013 enforces the CI gate; this task provides the recognizers and the fixture file). The per-type floor catches the case where one recognizer regresses to 90% recall while others compensate to keep the aggregate above 99%.</li><li><strong>MUST</strong> assign a confidence score in the range <code>[0.0, 1.0]</code> per Presidio convention. The placeholder format is <code>&lt;VN_&lt;TYPE&gt;_&lt;N&gt;&gt;</code> where TYPE ∈ <code>CCCD</code>, <code>MST</code>, <code>PHONE</code>, <code>NDD</code>, <code>ADDRESS</code>, <code>BANK_ACCOUNT</code>. Scores MUST come from the shared <code>confidence.py</code> constants — no inline numeric literals in recognizer classes.</li><li><strong>MUST</strong> support both pre-composed and combining-form Vietnamese characters (Unicode normalisation handled by Presidio's analyzer pipeline; recognizers receive NFC-normalized text).</li><li><strong>MUST NOT</strong> false-positive on plain Vietnamese text. The 200-sample test set MUST include negative examples (Vietnamese names without CCCD, addresses without ward/district structure, dates that look like 12-digit numbers, VND amounts that look like phone numbers).</li><li><strong>MUST</strong> validate <code>VN_CCCD</code> first 3 digits against the closed list of valid province codes (001-099) sourced from <code>province_codes.py</code>. Invalid province code → match dropped (treated as a 12-digit number, not a CCCD).</li><li><strong>MUST</strong> validate <code>VN_MST</code> against the 10-digit (entity) or 13-digit (entity-branch with hyphen) format; the first 2 digits MUST be a valid province code. The 10-digit and 13-digit variants share the same recognizer with two patterns at differentiated confidence.</li><li><strong>MUST</strong> distinguish <code>VN_PHONE</code> from <code>VN_MST</code> at recognizer registration order — phone runs FIRST so a 10-digit <code>0901234567</code> is matched as phone (high confidence with <code>+84</code> or <code>0</code> prefix), not as MST. The recognizer registration order is asserted in <code>test_vn_recognizers.py</code>.</li><li><strong>MUST</strong> support <code>VN_NDD</code> (Người đại diện / Legal representative) as a label-bound name extraction. The pattern matches <code>(Người đại diện|NĐD|Legal representative)\\s*[:.]?\\s*(&lt;PERSON_NAME&gt;)</code> and emits the captured name only. Without the label, fallback to Presidio's standard PERSON recognizer (which doesn't carry the \"this is a legal representative\" semantic).</li><li><strong>MUST</strong> support partial redaction for <code>VN_ADDRESS</code> — when the recognizer matches <code>&lt;street&gt;, &lt;ward&gt;, &lt;district&gt;, &lt;city&gt;</code>, the placeholder MAY be <code>&lt;VN_ADDRESS_1&gt;</code> (full redact) OR <code>&lt;VN_ADDRESS_CITY_ONLY_1&gt;</code> (preserve city, redact street/ward/district) depending on the <code>policy.ai_policy.vn_address_partial_redact: bool</code> flag. Default is full redact for slice 3.</li><li><strong>MUST</strong> distinguish <code>VN_BANK_ACCOUNT</code> (10-14 digits, often near <code>STK|tài khoản|account number</code>) from <code>VN_CCCD</code> (always 12 digits with province-code prefix) and <code>VN_MST</code> (10/13 digits with province-code prefix). Bank account regex includes a context-keyword boost and bank-name proximity (Vietcombank, BIDV, Techcombank, Sacombank, Agribank, MBBank, VPBank, ACB).</li><li><strong>MUST</strong> be deterministic — same input string → same recognizer matches with same confidence scores. Presidio's analyzer iterates recognizers in registration order; stable order is asserted in <code>test_recognizer_registration_order</code>.</li><li><strong>MUST NOT</strong> make any network call. All recognizers are pure regex + lookup tables. The province-code list is statically embedded; bank-name list is statically embedded.</li><li><strong>MUST</strong> emit <code>recognizer_registration_failed</code> on sidecar startup if any of the 6 recognizers fails to register (e.g., regex compile error, validation method missing). The sidecar MUST refuse to start — partial registration would silently miss PII types.</li><li><strong>MUST</strong> publish recognizer-version metadata via <code>GET /recognizers/version</code> returning <code>{ \"VN_CCCD\": \"1.0.0\", \"VN_MST\": \"1.0.0\", ... }</code>. TASK-AI-013's recall test asserts the version is the expected one for the fixture set; mismatched versions fail the CI gate.</li><li><strong>SHOULD</strong> log each registered recognizer's name + pattern count at INFO level on sidecar startup so operators can verify the expected 6 recognizers loaded. Logging the patterns themselves is OK (regex strings, not PII).</li><li><strong>MUST</strong> produce per-type redacted display forms for downstream audit-row emission per task-audit skill §3.6 rule 20. The Rust gateway-side helper <code>cyberos_pii::vn::redact_for_audit::&lt;T&gt;(value: &amp;str) -&gt; String</code> provides these formatters: <code>VN_MST</code> → <code>&lt;first-2&gt;******&lt;last-2&gt;</code> (e.g. <code>03******78</code> for 0312345678); <code>VN_CCCD</code> → <code>&lt;first-3&gt;******&lt;last-3&gt;</code> (e.g. <code>031******678</code>); <code>VN_PHONE</code> → <code>&lt;first-2&gt;***&lt;last-4&gt;</code> (e.g. <code>09***1234</code>); <code>VN_BANK_ACCOUNT</code> → <code>***&lt;last-4&gt;</code> (e.g. <code>***6789</code>). The TASK-AI-003 memory-emit path MUST call them when serialising <code>extra.{mst,cccd,phone,bank_account}_redacted</code>. AC #18 verifies via a round-trip test that no audit row written during a VN-PII detection contains any digit sequence longer than 4 consecutive digits of the original.</li><li><strong>MUST</strong> consult <code>policy.ai_policy.pii_allowlist: Vec&lt;String&gt;</code> (compiled to <code>Vec&lt;Regex&gt;</code> at policy-load time per TASK-AI-005) per task-audit skill §3.6 rule 21. Before emitting any <code>RecognizerResult</code> for an entity type ∈ <code>VN_MST | VN_CCCD | VN_PHONE | VN_BANK_ACCOUNT</code>, the recognizer pipeline MUST check whether the matched text matches ANY allowlist regex for the active tenant. If yes, suppress the result (no redaction; PII flows to LLM). The audit-row <code>extra.pii_allowlist_hit_count: u32</code> records how many suppressions happened per call so operators can audit allowlist usage. Use case: KYC vendor tenant where MST IS the subject matter, not collateral PII.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why custom recognizers instead of relying on Presidio's PERSON/LOCATION?</strong> Presidio's English recognizers don't know Vietnam-specific structure. CCCD numbers are particularly tricky — a 12-digit number could be a timestamp (Unix epoch nanos), a phone (some legacy formats), or a CCCD. Without context-aware recognizers we measured 30%+ false negatives on real VN customer data; the 99% recall floor is unachievable without these custom recognizers.</p>\n<p><strong>Why context-sensitive matching (e.g., 'CCCD: ' prefix boost)?</strong> Real VN documents structure PII with leading labels (Vietnamese forms, contracts, invoices). Matching the label boosts confidence and reduces false positives — <code>\"Sinh năm 1990\"</code> (born in 1990) wouldn't match <code>\\b\\d{12}\\b</code> but might match if the labeled-prefix variant didn't exist alongside. Two-tier confidence (high-with-label, low-without) lets the analyzer make the right trade-off.</p>\n<p><strong>Why preserve structure in <code>VN_ADDRESS</code> partial redaction?</strong> Some workflows need the city but not the street (\"send invoice to Ho Chi Minh City\" is fine; \"send to 123 Nguyễn Văn A street\" is PII). Partial redaction at <code>&lt;VN_ADDRESS_CITY_ONLY&gt;</code> placeholder preserves the city-level data for product analytics while redacting street/ward. Slice 3 ships full-redact-only; slice 5 enables the partial mode behind a tenant policy flag.</p>\n<p><strong>Why a single <code>province_codes.py</code> source-of-truth file?</strong> CCCD AND MST both validate against province codes. Without a shared list, we'd have two copies that drift over time. When Vietnam's government adds a new province code (or merges provinces), one update to one file suffices. The list is small (~99 entries) so embedding it statically is fine — no DB lookup, no network call.</p>\n<p><strong>Why a shared <code>confidence.py</code> for score constants?</strong> Magic-number scores in recognizer classes (<code>score=0.99</code>, <code>score=0.5</code>) are tuning knobs. When we calibrate against the 200-sample test set and find the high-confidence threshold should be 0.95 (not 0.99) to balance precision/recall, we want one place to update — not 6 recognizer files. The constants module is the dial; the recognizers reference it.</p>\n<p><strong>Why does recognizer registration order matter for VN_PHONE vs VN_MST?</strong> Both can match a 10-digit string. Without ordered registration, Presidio's analyzer may emit BOTH a <code>VN_PHONE</code> and a <code>VN_MST</code> for the same span — the anonymizer then picks one arbitrarily. Registering phone FIRST means a labeled phone (<code>SĐT: 0901234567</code>) wins over the unlabeled MST pattern, which is the conservative call (over-redacting a phone as a phone is fine; under-redacting an MST as nothing is bad).</p>\n<p><strong>Why does sidecar refuse to start on registration failure (§1 #13)?</strong> Partial registration is silent PII passthrough. If <code>VnAddressRecognizer</code> fails to register (e.g., a regex compile error after a refactor), the sidecar would happily process requests but VN addresses would not be redacted. Operators wouldn't know until a customer complaint. Failing-loud at startup forces the operator to fix the recognizer before any traffic flows.</p>\n<p><strong>Why a recognizer-version endpoint (§1 #14)?</strong> Three reasons. (1) TASK-AI-013's recall test fixture is bound to specific recognizer versions; mismatched versions could give false confidence in CI. (2) Operators investigating a recall regression can check \"did we actually deploy the new VN_CCCD v1.1 with the fix?\". (3) Tenant-facing audit reports cite the recognizer version so customers know what was active during their data processing.</p>\n<p><strong>Why no network calls in recognizers (§1 #12)?</strong> Recognizers run on the hot path (per-request, per-prompt). A network call would add 50-200ms latency and a failure mode (network down → recognizer skips → PII passthrough). Pure-Python regex + static lookup is the only design that meets the 30ms p95 budget from TASK-AI-011 §1 #6.</p>\n<p><strong>Why label-bound NĐD extraction instead of named-entity NER?</strong> Vietnamese names have low entropy from a NER perspective — Nguyễn Văn A is structurally identical to \"Mr. Smith\" in English NER. Label-bound matching (\"preceded by NĐD: or Người đại diện:\") catches the cases where the name carries legal-representative meaning. Without the label, fall through to Presidio's PERSON recognizer (still redacted, just not tagged as NDD).</p>\n<hr>\n<h2>§3 — API contract (Python recognizer classes)</h2>\n<h3>Shared modules</h3>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/province_codes.py\n\"\"\"\nSingle source of truth for Vietnamese province codes.\nUsed by VN_CCCD (first 3 digits) and VN_MST (first 2 digits) validation.\nSource: General Statistics Office of Vietnam, 2024 administrative divisions.\n\"\"\"\n\nVALID_PROVINCE_CODES_3DIGIT = frozenset([\n    \"001\", \"002\", \"004\", \"006\", \"008\", \"010\", \"011\", \"012\", \"014\", \"015\", \"017\",\n    \"019\", \"020\", \"022\", \"024\", \"025\", \"026\", \"027\", \"030\", \"031\", \"033\", \"034\",\n    # ... (62 more)\n])\n\nVALID_PROVINCE_CODES_2DIGIT = frozenset(\n    code[1:] for code in VALID_PROVINCE_CODES_3DIGIT\n)\n\nVN_BANK_NAMES = frozenset([\n    \"Vietcombank\", \"BIDV\", \"Techcombank\", \"Sacombank\", \"Agribank\",\n    \"MBBank\", \"VPBank\", \"ACB\", \"VIB\", \"TPBank\", \"SHB\", \"HDBank\",\n    # ... (~30 banks)\n])</code></pre>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/confidence.py\n\"\"\"\nShared confidence-score constants. Tuned against the 200-sample VN PII test set.\nUpdate one file when calibrating recall/precision trade-offs.\n\"\"\"\n\nCONFIDENCE_HIGH = 0.99   # explicit label match (CCCD:, MST:, NĐD:, STK:)\nCONFIDENCE_MED  = 0.85   # strong contextual match (e.g., bank name nearby)\nCONFIDENCE_LOW  = 0.50   # bare pattern match without context</code></pre>\n<h3>Recognizer class signatures</h3>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/vn_cccd.py\nfrom presidio_analyzer import Pattern, PatternRecognizer\nfrom .province_codes import VALID_PROVINCE_CODES_3DIGIT\nfrom .confidence import CONFIDENCE_HIGH, CONFIDENCE_LOW\n\nclass VnCccdRecognizer(PatternRecognizer):\n    \"\"\"VN_CCCD — 12-digit citizen identity number; first 3 digits are province code.\"\"\"\n\n    VERSION = \"1.0.0\"\n\n    PATTERNS = [\n        Pattern(\n            name=\"vn_cccd_strict_with_context\",\n            regex=r\"(?:CCCD|cccd|Căn cước công dân|căn cước|định danh)\\s*[:\\.]?\\s*(\\d{12})\",\n            score=CONFIDENCE_HIGH,\n        ),\n        Pattern(\n            name=\"vn_cccd_plain\",\n            regex=r\"\\b\\d{12}\\b\",\n            score=CONFIDENCE_LOW,\n        ),\n    ]\n\n    SUPPORTED_LANGUAGES = [\"en\", \"vi\"]\n\n    def __init__(self):\n        super().__init__(\n            supported_entity=\"VN_CCCD\",\n            patterns=self.PATTERNS,\n            context=[\"CCCD\", \"căn cước\", \"định danh\", \"cmnd\"],\n            supported_language=\"vi\",\n        )\n\n    def validate_result(self, pattern_text: str) -&gt; bool:\n        \"\"\"Province code validation: first 3 digits must be in the valid set.\"\"\"\n        return pattern_text[:3] in VALID_PROVINCE_CODES_3DIGIT</code></pre>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/vn_mst.py\nclass VnMstRecognizer(PatternRecognizer):\n    \"\"\"VN_MST — 10 or 13-digit tax code; 13-digit has hyphen between 10 and 3-digit branch.\"\"\"\n    VERSION = \"1.0.0\"\n    PATTERNS = [\n        Pattern(\"vn_mst_with_context_10\",\n            r\"(?:MST|mst|Mã số thuế|mã thuế)\\s*[:\\.]?\\s*(\\d{10})\",\n            CONFIDENCE_HIGH),\n        Pattern(\"vn_mst_with_context_13\",\n            r\"(?:MST|mst|Mã số thuế)\\s*[:\\.]?\\s*(\\d{10})-(\\d{3})\",\n            CONFIDENCE_HIGH),\n        Pattern(\"vn_mst_plain_10\",\n            r\"\\b\\d{10}\\b\",\n            CONFIDENCE_LOW),\n        Pattern(\"vn_mst_plain_13\",\n            r\"\\b\\d{10}-\\d{3}\\b\",\n            CONFIDENCE_LOW),\n    ]\n    def __init__(self):\n        super().__init__(\n            supported_entity=\"VN_MST\",\n            patterns=self.PATTERNS,\n            context=[\"MST\", \"mã số thuế\", \"thuế\"],\n        )\n    def validate_result(self, pattern_text: str) -&gt; bool:\n        # First 2 digits are the province code (without leading 0).\n        digits = pattern_text.replace(\"-\", \"\")[:2]\n        return digits in VALID_PROVINCE_CODES_2DIGIT</code></pre>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/vn_phone.py\nclass VnPhoneRecognizer(PatternRecognizer):\n    \"\"\"VN_PHONE — +84 or 0-prefix mobile/landline.\"\"\"\n    VERSION = \"1.0.0\"\n    PATTERNS = [\n        Pattern(\"vn_phone_84_mobile\",\n            r\"\\+84\\s?(?:9\\d{8}|3\\d{8}|7\\d{8}|8\\d{8}|5\\d{8})\",\n            CONFIDENCE_HIGH),\n        Pattern(\"vn_phone_0_mobile\",\n            r\"0(?:9\\d{8}|3\\d{8}|7\\d{8}|8\\d{8}|5\\d{8})\\b\",\n            CONFIDENCE_HIGH),\n        Pattern(\"vn_phone_landline\",\n            r\"0(?:2[0-9])\\d{7,8}\\b\",\n            CONFIDENCE_MED),\n    ]\n    def __init__(self):\n        super().__init__(\n            supported_entity=\"VN_PHONE\",\n            patterns=self.PATTERNS,\n            context=[\"SĐT\", \"điện thoại\", \"phone\", \"mobile\"],\n        )</code></pre>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/vn_ndd.py\nfrom presidio_analyzer import EntityRecognizer, RecognizerResult\n\nclass VnNddRecognizer(EntityRecognizer):\n    \"\"\"VN_NDD — label-bound legal representative name extraction.\"\"\"\n    VERSION = \"1.0.0\"\n    LABEL_REGEX = r\"(?:Người đại diện|NĐD|Legal representative)\\s*[:\\.]?\\s*([A-ZÀÁẢÃẠ][\\w\\sÀ-ỹ]+?)(?=[\\n,.;]|$)\"\n\n    def __init__(self):\n        super().__init__(supported_entities=[\"VN_NDD\"], supported_language=\"vi\")\n\n    def analyze(self, text, entities, nlp_artifacts=None):\n        import re\n        results = []\n        for m in re.finditer(self.LABEL_REGEX, text):\n            results.append(RecognizerResult(\n                entity_type=\"VN_NDD\",\n                start=m.start(1),\n                end=m.end(1),\n                score=CONFIDENCE_HIGH,\n            ))\n        return results</code></pre>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/vn_address.py\nclass VnAddressRecognizer(PatternRecognizer):\n    \"\"\"VN_ADDRESS — multi-segment Vietnamese postal address.\"\"\"\n    VERSION = \"1.0.0\"\n    PATTERNS = [\n        Pattern(\"vn_address_full\",\n            r\"\\d+\\s+[\\w\\sÀ-ỹ]+,\\s*(?:Phường|Xã|Quận|Huyện|Tp\\.?|TP\\.?|Thành phố)[^,]+,\\s*(?:Quận|Huyện|Tp\\.?|TP\\.?)[^,]+,\\s*(?:Tp\\.?|TP\\.?|Thành phố)[\\w\\sÀ-ỹ\\.]+\",\n            CONFIDENCE_HIGH),\n        Pattern(\"vn_address_partial\",\n            r\"(?:Quận|Huyện|Tp\\.?|TP\\.?|Phường|Xã)\\s+[\\w\\sÀ-ỹ\\d]+\",\n            CONFIDENCE_MED),\n    ]\n    def __init__(self):\n        super().__init__(\n            supported_entity=\"VN_ADDRESS\",\n            patterns=self.PATTERNS,\n            context=[\"địa chỉ\", \"address\"],\n        )</code></pre>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/recognizers/vn_bank.py\nfrom .province_codes import VN_BANK_NAMES\n\nclass VnBankAccountRecognizer(PatternRecognizer):\n    \"\"\"VN_BANK_ACCOUNT — 10-14 digit account; bank-name proximity boost.\"\"\"\n    VERSION = \"1.0.0\"\n    BANK_NAMES_RE = r\"(?:\" + \"|\".join(VN_BANK_NAMES) + r\")\"\n    PATTERNS = [\n        Pattern(\"vn_bank_with_context\",\n            rf\"(?:STK|tài khoản|account number|số tài khoản)\\s*(?:{BANK_NAMES_RE})?\\s*[:\\.]?\\s*(\\d{{10,14}})\",\n            CONFIDENCE_HIGH),\n        Pattern(\"vn_bank_with_bank_name\",\n            rf\"{BANK_NAMES_RE}[\\w\\s:,]*?(\\d{{10,14}})\",\n            CONFIDENCE_MED),\n        Pattern(\"vn_bank_plain\",\n            r\"\\b\\d{10,14}\\b\",\n            CONFIDENCE_LOW),\n    ]\n    def __init__(self):\n        super().__init__(\n            supported_entity=\"VN_BANK_ACCOUNT\",\n            patterns=self.PATTERNS,\n            context=[\"STK\", \"tài khoản\", \"account\", \"số tài khoản\"],\n        )\n    def validate_result(self, pattern_text: str) -&gt; bool:\n        # Distinguish from VN_CCCD (12 digits with province-code prefix) and VN_MST.\n        if len(pattern_text) == 12 and pattern_text[:3] in VALID_PROVINCE_CODES_3DIGIT:\n            return False  # likely a CCCD; let the CCCD recognizer handle it\n        if len(pattern_text) == 10 and pattern_text[:2] in VALID_PROVINCE_CODES_2DIGIT:\n            return False  # likely an MST\n        return True</code></pre>\n<h3>Sidecar registration</h3>\n<pre><code class=\"language-python\"># services/ai-gateway/pii/presidio_sidecar.py (additions)\nfrom recognizers import (\n    VnCccdRecognizer, VnMstRecognizer, VnPhoneRecognizer,\n    VnNddRecognizer, VnAddressRecognizer, VnBankAccountRecognizer,\n)\n\n# §1 #7 registration order: phone before MST so labeled phones win the tie-break.\nVN_RECOGNIZERS = [\n    VnPhoneRecognizer(),       # FIRST (wins over MST for 10-digit phone numbers)\n    VnCccdRecognizer(),        # 12-digit (distinct length from MST)\n    VnMstRecognizer(),\n    VnNddRecognizer(),\n    VnAddressRecognizer(),\n    VnBankAccountRecognizer(), # LAST (broad pattern; let specific recognizers run first)\n]\n\n# ISS-003 fix: idempotency guard against double-registration.\n# Presidio's add_recognizer doesn't dedupe; calling twice would add duplicates → double analysis.\n_REGISTERED = False\n\ndef register_vn_recognizers(analyzer: AnalyzerEngine):\n    \"\"\"§1 #13: refuse to start if any registration fails. Idempotent: subsequent calls no-op with WARN.\"\"\"\n    global _REGISTERED\n    if _REGISTERED:\n        logger.warning(\"register_vn_recognizers called twice; ignoring second invocation\")\n        return\n    for rec in VN_RECOGNIZERS:\n        try:\n            analyzer.registry.add_recognizer(rec)\n            logger.info(f\"registered {rec.__class__.__name__} v{rec.VERSION} \"\n                        f\"({len(getattr(rec, 'PATTERNS', []))} patterns)\")\n        except Exception as e:\n            raise RuntimeError(f\"recognizer_registration_failed: {rec.__class__.__name__}: {e}\")\n    _REGISTERED = True\n\ndef reset_for_tests():\n    \"\"\"Test-only: reset the registration guard. NOT for production use.\"\"\"\n    global _REGISTERED\n    _REGISTERED = False\n\n@app.get(\"/recognizers/version\")\ndef recognizer_versions():\n    \"\"\"§1 #14: version endpoint for TASK-AI-013 recall-gate.\"\"\"\n    return {rec.supported_entities[0]: rec.VERSION for rec in VN_RECOGNIZERS}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>CCCD with context</strong> — <code>\"CCCD: 012345678901\"</code> → match <code>VN_CCCD</code> with <code>score &gt;= 0.99</code>.</li><li><strong>CCCD without context</strong> — <code>\"012345678901\"</code> alone → match <code>VN_CCCD</code> with <code>score &gt;= 0.50</code> AND province-code validation passes.</li><li><strong>CCCD with invalid province code</strong> — <code>\"CCCD: 999345678901\"</code> → NO match (province code 999 not in valid set; <code>validate_result</code> rejects).</li><li><strong>MST 10-digit with context</strong> — <code>\"MST: 0301234567\"</code> → match <code>VN_MST</code> with <code>score &gt;= 0.99</code>.</li><li><strong>MST 13-digit with branch</strong> — <code>\"0301234567-001\"</code> → match <code>VN_MST</code> (13-digit pattern).</li><li><strong>VN phone +84 mobile</strong> — <code>\"+84901234567\"</code> → match <code>VN_PHONE</code> with <code>score &gt;= 0.99</code>.</li><li><strong>VN phone 0-prefix</strong> — <code>\"0901234567\"</code> → match <code>VN_PHONE</code> (NOT <code>VN_MST</code> — phone runs first per §1 #7).</li><li><strong>NĐD pattern</strong> — <code>\"NĐD: Nguyễn Văn A\"</code> → match <code>VN_NDD</code> capturing <code>\"Nguyễn Văn A\"</code> with <code>score &gt;= 0.99</code>.</li><li><strong>VN address full</strong> — <code>\"123 Nguyễn Thị Minh Khai, Phường Bến Nghé, Quận 1, TP. Hồ Chí Minh\"</code> → match <code>VN_ADDRESS</code> with <code>score &gt;= 0.99</code>.</li><li><strong>VN bank with context</strong> — <code>\"STK Vietcombank: 1234567890\"</code> → match <code>VN_BANK_ACCOUNT</code> with <code>score &gt;= 0.99</code>.</li><li><strong>No false positive on dates</strong> — <code>\"Ngày 12/05/2026\"</code> MUST NOT match any VN recognizer.</li><li><strong>No false positive on VND amounts</strong> — <code>\"100,000,000 VND\"</code> MUST NOT match <code>VN_PHONE</code> or <code>VN_BANK_ACCOUNT</code>.</li><li><strong>Recall ≥ 99% on 200-sample fixture</strong> — Run all 6 recognizers against <code>fixtures/vn_pii_200_samples.yaml</code>; correctly identified ≥ 198/200 across all types.</li><li><strong>Recognizer registration order asserted</strong> — <code>VN_RECOGNIZERS[0].supported_entities[0] == \"VN_PHONE\"</code> (phone first); <code>VN_RECOGNIZERS[-1].supported_entities[0] == \"VN_BANK_ACCOUNT\"</code> (bank last).</li><li><strong>Sidecar refuses to start on registration failure</strong> — Inject a regex compile error into <code>VnAddressRecognizer.PATTERNS</code>; sidecar startup raises <code>RuntimeError(\"recognizer_registration_failed: ...\")</code>; HTTP server never binds.</li><li><strong>Version endpoint returns 6 entries</strong> — <code>GET /recognizers/version</code> returns JSON with keys <code>VN_CCCD</code>, <code>VN_MST</code>, <code>VN_PHONE</code>, <code>VN_NDD</code>, <code>VN_ADDRESS</code>, <code>VN_BANK_ACCOUNT</code>; values are semver strings.</li><li><strong>Determinism</strong> — Calling <code>analyze()</code> twice on the same input produces identical (entity_type, start, end, score) tuples.</li><li><strong>Audit-row redacted forms (task-audit skill §3.6 rule 20)</strong> — Round-trip test: detect <code>\"MST 0312345678 CCCD 079123456789 phone 0901234567 bank 1234567890\"</code>. Emit memory row via TASK-AI-003. Assert <code>row.extra.mst_redacted == \"03******78\"</code> AND <code>row.extra.cccd_redacted == \"079******789\"</code> AND <code>row.extra.phone_redacted == \"09***4567\"</code> AND <code>row.extra.bank_account_redacted == \"***7890\"</code>. Also assert <code>row.extra</code> JSON contains NO digit-substring of length &gt; 4 from the original.</li><li><strong>PII allowlist suppression (task-audit skill §3.6 rule 21)</strong> — Test fixture <code>policy_with_allowlist.yaml</code> sets <code>pii_allowlist: [\"^03\\\\d{8}$\"]</code>. Analyze text containing <code>\"MST 0312345678\"</code> and <code>\"MST 0412345678\"</code>. First match is suppressed (no <code>&lt;VN_MST_N&gt;</code> placeholder; raw value flows through); second match is redacted normally. <code>extra.pii_allowlist_hit_count == 1</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<p><strong>Positive fixture tests:</strong> <code>services/ai-gateway/pii/test_vn_recognizers.py</code></p>\n<pre><code class=\"language-python\">import pytest\nfrom presidio_analyzer import AnalyzerEngine\nfrom recognizers import (\n    VnCccdRecognizer, VnMstRecognizer, VnPhoneRecognizer,\n    VnNddRecognizer, VnAddressRecognizer, VnBankAccountRecognizer,\n    VN_RECOGNIZERS,\n)\nfrom recognizers.confidence import CONFIDENCE_HIGH, CONFIDENCE_MED, CONFIDENCE_LOW\n\n@pytest.fixture\ndef analyzer():\n    a = AnalyzerEngine()\n    for rec in VN_RECOGNIZERS:\n        a.registry.add_recognizer(rec)\n    return a\n\ndef test_cccd_with_context(analyzer):\n    results = analyzer.analyze(text=\"CCCD: 012345678901\", language=\"vi\", entities=[\"VN_CCCD\"])\n    assert len(results) == 1\n    assert results[0].score &gt;= CONFIDENCE_HIGH\n\ndef test_cccd_without_context_validates_province(analyzer):\n    # 012 is province code 12 (Lai Châu) — valid\n    results = analyzer.analyze(text=\"012345678901\", language=\"vi\", entities=[\"VN_CCCD\"])\n    assert len(results) == 1\n    assert results[0].score &gt;= CONFIDENCE_LOW\n\ndef test_cccd_invalid_province_rejected(analyzer):\n    # 999 is not a valid province code\n    results = analyzer.analyze(text=\"999345678901\", language=\"vi\", entities=[\"VN_CCCD\"])\n    assert len(results) == 0\n\ndef test_mst_10_digit(analyzer):\n    results = analyzer.analyze(text=\"MST: 0301234567\", language=\"vi\", entities=[\"VN_MST\"])\n    assert len(results) == 1\n    assert results[0].score &gt;= CONFIDENCE_HIGH\n\ndef test_mst_13_digit_with_branch(analyzer):\n    results = analyzer.analyze(text=\"0301234567-001\", language=\"vi\", entities=[\"VN_MST\"])\n    assert len(results) &gt;= 1\n\ndef test_vn_phone_84_prefix(analyzer):\n    results = analyzer.analyze(text=\"+84901234567\", language=\"vi\", entities=[\"VN_PHONE\"])\n    assert len(results) == 1\n    assert results[0].score &gt;= CONFIDENCE_HIGH\n\ndef test_vn_phone_beats_mst_for_10_digit(analyzer):\n    \"\"\"§1 #7: phone runs first; 0901234567 should match VN_PHONE, not VN_MST.\"\"\"\n    results = analyzer.analyze(text=\"0901234567\", language=\"vi\",\n                               entities=[\"VN_PHONE\", \"VN_MST\"])\n    types = {r.entity_type for r in results}\n    assert \"VN_PHONE\" in types\n    # MST may still match at low confidence; the anonymizer picks the higher-score one.\n    phone_score = max(r.score for r in results if r.entity_type == \"VN_PHONE\")\n    mst_score = max((r.score for r in results if r.entity_type == \"VN_MST\"), default=0)\n    assert phone_score &gt;= mst_score\n\ndef test_vn_ndd_label_bound(analyzer):\n    results = analyzer.analyze(text=\"NĐD: Nguyễn Văn A\",\n                               language=\"vi\", entities=[\"VN_NDD\"])\n    assert len(results) == 1\n    assert results[0].score &gt;= CONFIDENCE_HIGH\n\ndef test_vn_address_full(analyzer):\n    text = \"123 Nguyễn Thị Minh Khai, Phường Bến Nghé, Quận 1, TP. Hồ Chí Minh\"\n    results = analyzer.analyze(text=text, language=\"vi\", entities=[\"VN_ADDRESS\"])\n    assert len(results) &gt;= 1\n    assert results[0].score &gt;= CONFIDENCE_HIGH\n\ndef test_vn_bank_account_with_context(analyzer):\n    results = analyzer.analyze(text=\"STK Vietcombank: 1234567890\",\n                               language=\"vi\", entities=[\"VN_BANK_ACCOUNT\"])\n    assert len(results) &gt;= 1\n    assert results[0].score &gt;= CONFIDENCE_HIGH\n\ndef test_recognizer_registration_order():\n    \"\"\"§1 #7: VN_PHONE first, VN_BANK_ACCOUNT last.\"\"\"\n    assert VN_RECOGNIZERS[0].supported_entities[0] == \"VN_PHONE\"\n    assert VN_RECOGNIZERS[-1].supported_entities[0] == \"VN_BANK_ACCOUNT\"\n\n# ISS-001 fix: AC #16 requires version-endpoint shape verification.\ndef test_version_endpoint_returns_six_entries():\n    \"\"\"AC #16: every registered recognizer's version is exposed via GET /recognizers/version.\"\"\"\n    from fastapi.testclient import TestClient\n    from presidio_sidecar import app\n    client = TestClient(app)\n    resp = client.get(\"/recognizers/version\")\n    assert resp.status_code == 200\n    body = resp.json()\n    expected_keys = {\"VN_CCCD\", \"VN_MST\", \"VN_PHONE\", \"VN_NDD\", \"VN_ADDRESS\", \"VN_BANK_ACCOUNT\"}\n    assert set(body.keys()) == expected_keys\n    import re\n    semver = re.compile(r\"^\\d+\\.\\d+\\.\\d+$\")\n    for entity, version in body.items():\n        assert semver.match(version), f\"{entity}: {version!r} is not semver\"\n\ndef test_determinism(analyzer):\n    \"\"\"§1 #11 + AC #17: same input twice → identical results.\"\"\"\n    text = \"CCCD: 012345678901, MST: 0301234567\"\n    r1 = analyzer.analyze(text=text, language=\"vi\", entities=[\"VN_CCCD\", \"VN_MST\"])\n    r2 = analyzer.analyze(text=text, language=\"vi\", entities=[\"VN_CCCD\", \"VN_MST\"])\n    r1_tuples = sorted([(r.entity_type, r.start, r.end, r.score) for r in r1])\n    r2_tuples = sorted([(r.entity_type, r.start, r.end, r.score) for r in r2])\n    assert r1_tuples == r2_tuples</code></pre>\n<p><strong>Negative fixture tests:</strong> <code>services/ai-gateway/pii/test_vn_no_false_positives.py</code></p>\n<pre><code class=\"language-python\">def test_dates_do_not_false_positive(analyzer):\n    for text in [\"Ngày 12/05/2026\", \"Sinh năm 1990\", \"Hợp đồng số 2024-001\"]:\n        results = analyzer.analyze(text=text, language=\"vi\",\n                                   entities=[\"VN_CCCD\", \"VN_MST\", \"VN_PHONE\"])\n        assert all(r.score &lt; CONFIDENCE_HIGH for r in results), \\\n            f\"unexpected high-confidence match in: {text!r}\"\n\ndef test_vnd_amounts_do_not_false_positive(analyzer):\n    for text in [\"100,000,000 VND\", \"Phí 50.000 VNĐ\", \"1,234,567,890 đồng\"]:\n        results = analyzer.analyze(text=text, language=\"vi\",\n                                   entities=[\"VN_PHONE\", \"VN_BANK_ACCOUNT\"])\n        assert all(r.score &lt; CONFIDENCE_MED for r in results), \\\n            f\"unexpected match in VND amount: {text!r}\"\n\ndef test_plain_vietnamese_names_do_not_match_ndd(analyzer):\n    \"\"\"A bare name without the NĐD label should not trigger VN_NDD.\"\"\"\n    results = analyzer.analyze(text=\"Khách hàng Nguyễn Văn A đã thanh toán\",\n                               language=\"vi\", entities=[\"VN_NDD\"])\n    assert len(results) == 0</code></pre>\n<p><strong>No-network-imports lint test:</strong> <code>services/ai-gateway/pii/test_no_network_imports.py</code></p>\n<pre><code class=\"language-python\"># ISS-002 fix: §1 #12 mandates no network calls in recognizers.\n# This AST-walk lint catches forbidden imports at PR time.\nimport ast\nfrom pathlib import Path\n\nFORBIDDEN_IMPORTS = {\n    \"requests\", \"urllib\", \"urllib2\", \"urllib3\", \"httpx\", \"aiohttp\",\n    \"http\", \"socket\", \"ssl\", \"ftplib\", \"smtplib\",\n}\n\ndef test_no_network_imports_in_recognizers():\n    \"\"\"§1 #12: recognizer modules MUST be pure regex + lookup tables.\"\"\"\n    recognizers_dir = Path(__file__).parent / \"recognizers\"\n    failures = []\n    for py_file in recognizers_dir.glob(\"*.py\"):\n        tree = ast.parse(py_file.read_text())\n        for node in ast.walk(tree):\n            if isinstance(node, ast.Import):\n                for alias in node.names:\n                    top = alias.name.split(\".\")[0]\n                    if top in FORBIDDEN_IMPORTS:\n                        failures.append(f\"{py_file.name}: forbidden import: {alias.name}\")\n            elif isinstance(node, ast.ImportFrom):\n                top = (node.module or \"\").split(\".\")[0]\n                if top in FORBIDDEN_IMPORTS:\n                    failures.append(f\"{py_file.name}: forbidden import-from: {node.module}\")\n    assert not failures, \"Network imports detected in recognizers:\\n\" + \"\\n\".join(failures)</code></pre>\n<p><strong>Recall-floor test:</strong> <code>services/ai-gateway/pii/test_vn_recall_floor.py</code></p>\n<pre><code class=\"language-python\">import yaml\nfrom pathlib import Path\n\ndef test_recall_at_least_99_percent_per_type(analyzer):\n    \"\"\"AC #13 (post-ISS-004): per-type AND aggregate recall floors at ≥99%.\n    Per-type guard catches a single recognizer regressing while others compensate.\"\"\"\n    from collections import defaultdict\n    fixture_path = Path(__file__).parent / \"fixtures\" / \"vn_pii_200_samples.yaml\"\n    samples = yaml.safe_load(fixture_path.read_text())\n\n    correct_by_type = defaultdict(int)\n    total_by_type = defaultdict(int)\n\n    for sample in samples[\"samples\"]:\n        text = sample[\"text\"]\n        for expected in sample[\"expected_entities\"]:\n            total_by_type[expected] += 1\n            results = analyzer.analyze(text=text, language=\"vi\", entities=[expected])\n            if any(r.entity_type == expected for r in results):\n                correct_by_type[expected] += 1\n\n    failures = []\n    for entity_type, total in total_by_type.items():\n        if total == 0:\n            continue\n        recall = correct_by_type[entity_type] / total\n        if recall &lt; 0.99:\n            failures.append(\n                f\"{entity_type}: recall={recall:.4f} ({correct_by_type[entity_type]}/{total})\"\n            )\n    assert not failures, \"Per-type recall floor violated:\\n\" + \"\\n\".join(failures)\n\n    # Aggregate floor too.\n    total = sum(total_by_type.values())\n    correct = sum(correct_by_type.values())\n    aggregate = correct / total if total else 1.0\n    assert aggregate &gt;= 0.99, f\"aggregate recall {aggregate:.4f} below 0.99 floor\"</code></pre>\n<p><strong>Sidecar startup test:</strong> <code>services/ai-gateway/pii/test_sidecar_startup.py</code></p>\n<pre><code class=\"language-python\">def test_sidecar_refuses_to_start_on_registration_failure():\n    \"\"\"AC #15: any recognizer registration error → sidecar startup raises.\"\"\"\n    from unittest.mock import patch\n    with patch.object(VnAddressRecognizer, 'PATTERNS',\n                      [Pattern(\"bad\", \"(unbalanced\", 0.5)]):\n        analyzer = AnalyzerEngine()\n        with pytest.raises(RuntimeError, match=\"recognizer_registration_failed\"):\n            register_vn_recognizers(analyzer)</code></pre>\n<pre><code class=\"language-bash\">cd services/ai-gateway/pii\npytest -v\npytest -v test_vn_recall_floor.py   # the recall gate (TASK-AI-013 reuses this)</code></pre>\n<p>CI gate: pytest runs on every PR touching <code>services/ai-gateway/pii/**</code>. Recall gate failure (&lt; 99%) blocks merge.</p>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3 for the recognizer class skeletons. The integration into the sidecar is also in §3 (<code>register_vn_recognizers</code> function).</p>\n<p>The fixture file structure:</p>\n<pre><code class=\"language-yaml\"># services/ai-gateway/pii/fixtures/vn_pii_200_samples.yaml\n# 200 samples covering 6 PII types + negative examples.\n# Curated quarterly from anonymised CyberSkill customer data + synthetic edge cases.\nsamples:\n  - text: \"CCCD: 012345678901\"\n    expected_entities: [\"VN_CCCD\"]\n    expected_count: 1\n  - text: \"MST 0301234567\"\n    expected_entities: [\"VN_MST\"]\n    expected_count: 1\n  # ... (198 more)\n  # Negative examples interleaved:\n  - text: \"Hợp đồng số 2024-001 ngày 12/05/2026\"\n    expected_entities: []\n    expected_count: 0</code></pre>\n<p>Test runner output:</p>\n<pre><code class=\"language-text\">$ pytest test_vn_recognizers.py -v\ntest_cccd_with_context PASSED\ntest_cccd_without_context_validates_province PASSED\ntest_cccd_invalid_province_rejected PASSED\ntest_mst_10_digit PASSED\n... (12 tests)\ntest_vn_recall_floor.py::test_recall_at_least_99_percent PASSED  # 199/200 = 0.995</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<h3>Code dependencies (other tasks/modules)</h3>\n<ul><li><strong>TASK-AI-011</strong> — Presidio sidecar must exist; this task adds recognizers TO it. The <code>PiiType</code> Rust enum already includes the VN variants (declared in TASK-AI-011 §3 for ABI stability).</li><li><strong>TASK-AI-013 (downstream)</strong> — Recall-floor CI gate consumes the <code>vn_pii_200_samples.yaml</code> fixture and runs <code>test_vn_recall_floor.py</code> as the gate.</li><li><strong>TASK-AI-005</strong> — <code>TenantPolicy.ai_policy.pii_redaction_extra</code> lists the VN entities to enable per-tenant; this task's recognizers handle them once the policy field reaches the sidecar.</li></ul>\n<h3>Concept dependencies (shared types)</h3>\n<ul><li><code>VALID_PROVINCE_CODES_3DIGIT</code> and <code>VALID_PROVINCE_CODES_2DIGIT</code> from <code>province_codes.py</code> are the SINGLE SOURCE for province code validation across <code>VN_CCCD</code>, <code>VN_MST</code>, and <code>VN_BANK_ACCOUNT</code> recognizers.</li><li><code>CONFIDENCE_HIGH/MED/LOW</code> from <code>confidence.py</code> are the SINGLE SOURCE for score constants.</li><li><code>VN_BANK_NAMES</code> from <code>province_codes.py</code> is shared between the <code>VN_BANK_ACCOUNT</code> recognizer's regex generation and (future) per-bank precision tuning.</li><li>The <code>VN_&lt;TYPE&gt;</code> entity names match the <code>PiiType::from_presidio()</code> Rust mapping (TASK-AI-011 §3).</li></ul>\n<h3>Operational / external</h3>\n<ul><li><code>presidio-analyzer==2.2.x</code> (recognizer base classes).</li><li><code>pytest&gt;=7.0</code>, <code>pyyaml&gt;=6.0</code> (test runner + fixture loader).</li><li>spaCy Vietnamese model (<code>vi_core_news_lg</code>) for the analyzer pipeline (loaded by Presidio at engine init).</li><li>Static lookup tables only — no DB, no HTTP, no external API.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Input</h3>\n<pre><code class=\"language-text\">Khách hàng: Nguyễn Văn A\nCCCD: 012345678901\nMST: 0301234567\nSĐT: 0901234567\nSTK Vietcombank: 1234567890\nĐịa chỉ: 123 Lê Lợi, Phường Bến Nghé, Quận 1, TP. Hồ Chí Minh\nNĐD: Trần Thị B</code></pre>\n<h3>Sidecar response (analyzer items)</h3>\n<pre><code class=\"language-json\">[\n  {\"entity\": \"PERSON\", \"start\": 12, \"end\": 24, \"original\": \"Nguyễn Văn A\", \"score\": 0.85},\n  {\"entity\": \"VN_CCCD\", \"start\": 31, \"end\": 43, \"original\": \"012345678901\", \"score\": 0.99},\n  {\"entity\": \"VN_MST\", \"start\": 50, \"end\": 60, \"original\": \"0301234567\", \"score\": 0.99},\n  {\"entity\": \"VN_PHONE\", \"start\": 67, \"end\": 77, \"original\": \"0901234567\", \"score\": 0.99},\n  {\"entity\": \"VN_BANK_ACCOUNT\", \"start\": 100, \"end\": 110, \"original\": \"1234567890\", \"score\": 0.99},\n  {\"entity\": \"VN_ADDRESS\", \"start\": 123, \"end\": 188, \"original\": \"123 Lê Lợi, ... TP. Hồ Chí Minh\", \"score\": 0.99},\n  {\"entity\": \"VN_NDD\", \"start\": 195, \"end\": 207, \"original\": \"Trần Thị B\", \"score\": 0.99}\n]</code></pre>\n<h3>Redacted output</h3>\n<pre><code class=\"language-text\">Khách hàng: &lt;PERSON_1&gt;\nCCCD: &lt;VN_CCCD_1&gt;\nMST: &lt;VN_MST_1&gt;\nSĐT: &lt;VN_PHONE_1&gt;\nSTK Vietcombank: &lt;VN_BANK_ACCOUNT_1&gt;\nĐịa chỉ: &lt;VN_ADDRESS_1&gt;\nNĐD: &lt;VN_NDD_1&gt;</code></pre>\n<h3>Version endpoint</h3>\n<pre><code class=\"language-bash\">$ curl http://127.0.0.1:5050/recognizers/version\n{\n  \"VN_CCCD\": \"1.0.0\",\n  \"VN_MST\": \"1.0.0\",\n  \"VN_PHONE\": \"1.0.0\",\n  \"VN_NDD\": \"1.0.0\",\n  \"VN_ADDRESS\": \"1.0.0\",\n  \"VN_BANK_ACCOUNT\": \"1.0.0\"\n}</code></pre>\n<h3>Sidecar startup log (success)</h3>\n<pre><code class=\"language-text\">INFO  registered VnPhoneRecognizer v1.0.0 (3 patterns)\nINFO  registered VnCccdRecognizer v1.0.0 (2 patterns)\nINFO  registered VnMstRecognizer v1.0.0 (4 patterns)\nINFO  registered VnNddRecognizer v1.0.0 (1 pattern)\nINFO  registered VnAddressRecognizer v1.0.0 (2 patterns)\nINFO  registered VnBankAccountRecognizer v1.0.0 (3 patterns)\nINFO  uvicorn running on http://127.0.0.1:5050</code></pre>\n<h3>Sidecar startup log (failure on §1 #13)</h3>\n<pre><code class=\"language-text\">ERROR recognizer_registration_failed: VnAddressRecognizer: re.error: missing ), unterminated subpattern\nRuntimeError: recognizer_registration_failed: VnAddressRecognizer: ...\n[uvicorn never binds; sidecar exits with code 1]</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. Items deferred to later tasks:</p>\n<ul><li>Partial-redact policy (<code>vn_address_partial_redact: true</code>) — slice 5.</li><li>Per-tenant custom recognizers (proprietary product SKUs as PII) — slice 5.</li><li>Khmer / Lao / Thai recognizers for ASEAN expansion — slice 6 (TASK-AI-022 area).</li><li>Recognizer-version pinning per tenant (auditability across upgrades) — out of scope.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Recognizer regex matches but <code>validate_result</code> fails (e.g., wrong province code)</td><td><code>validate_result</code> returns False</td><td>Match dropped (no false positive); item excluded from analyzer results</td><td>Self-resolves</td></tr><tr><td>Edge case not in 200-sample fixture (new VN PII format)</td><td>Production discovery via TASK-AI-013 alarm</td><td>Recognizer misses; sev-2 alert</td><td>Add to recognizer + extend fixture; redeploy</td></tr><tr><td>Confidence threshold too low</td><td>High false-positive rate in production OBS</td><td>Operator tunes <code>confidence.py</code> constant</td><td>Iterative tuning; redeploy</td></tr><tr><td>Recognizer crash on malformed input</td><td>Exception in <code>analyze</code></td><td>Presidio catches; sidecar logs error; item not contributed to result set</td><td>Sev-2 log; investigate input that triggered</td></tr><tr><td>Recognizer registration fails on sidecar startup</td><td><code>register_vn_recognizers</code> raises <code>RuntimeError</code></td><td>Sidecar refuses to start (per §1 #13); HTTP server never binds</td><td>Operator fixes recognizer; restart sidecar</td></tr><tr><td>Province code list out of date (Vietnam adds new province)</td><td>Operator notices recall regression</td><td>Update <code>province_codes.py</code>; rebuild sidecar image</td><td>Quarterly review process</td></tr><tr><td>Bank name list missing a new bank</td><td>Customer reports their bank's account not redacted</td><td>Add to <code>VN_BANK_NAMES</code>; redeploy</td><td>Customer-feedback loop</td></tr><tr><td>Recognizer version drift between deployed sidecar and TASK-AI-013 fixture</td><td><code>GET /recognizers/version</code> mismatch in CI</td><td>TASK-AI-013 recall gate fails</td><td>Bump fixture version; re-curate samples</td></tr><tr><td>Two recognizers fire on the same span (PHONE + MST)</td><td>Anonymizer picks higher-score</td><td>Per §1 #7 ordering, PHONE wins for 10-digit</td><td>By design</td></tr><tr><td>Vietnamese diacritic normalization mismatch</td><td>Presidio analyzer normalizes to NFC; recognizer regexes assume NFC</td><td>Match works</td><td>If a recognizer uses non-NFC chars in its regex → false negative; fixed by NFC-normalizing the regex</td></tr><tr><td>spaCy <code>vi_core_news_lg</code> model missing</td><td>sidecar startup fails</td><td>Sidecar refuses to start</td><td>Operator pulls sidecar image with model bundled</td></tr><tr><td><code>analyze()</code> non-deterministic (regression)</td><td><code>test_determinism</code> fails in CI</td><td>PR blocked</td><td>PR rework</td></tr><tr><td><code>vi_core_news_lg</code> produces different POS tags across runs</td><td>Token-level non-determinism</td><td>Reproducible only with explicit <code>nlp</code> randomness seed</td><td>Seed in sidecar startup</td></tr><tr><td><code>pii_redaction_extra</code> policy field missing VN_* entries</td><td>Tenant policy doesn't enable VN recognizers</td><td>VN PII passes through</td><td>Operator updates tenant policy YAML</td></tr><tr><td>Recognizer dropped from <code>VN_RECOGNIZERS</code> list (regression)</td><td><code>test_recognizer_registration_order</code> fails</td><td>PR blocked</td><td>PR rework</td></tr><tr><td>Province code lookup uses wrong digit slice (off-by-one)</td><td>Validation rejects valid CCCDs → recall regression in fixture test</td><td>TASK-AI-013 alarm</td><td>PR rework</td></tr><tr><td>Future PR adds network call in a recognizer</td><td><code>test_no_network_imports.py</code> AST-walk lint</td><td>PR fails on CI before merge</td><td>ISS-002 fix</td></tr><tr><td><code>register_vn_recognizers</code> called twice (test loop, sidecar reload)</td><td><code>_REGISTERED</code> global guard</td><td>Second call is no-op with WARN log</td><td>ISS-003 fix; tests use <code>reset_for_tests()</code></td></tr><tr><td>Single recognizer regresses to 90% recall, others compensate</td><td>per-type recall test</td><td>PR fails with per-type breakdown showing which recognizer regressed</td><td>ISS-004 fix</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>Province code validation in <code>VN_CCCD</code> and <code>VN_MST</code> is the highest-value precision lever. Without it, 12-digit numeric strings false-positive at ~30% on real customer data.</li><li>The 200-sample fixture is hand-curated from anonymised CyberSkill customer data + synthetic edge cases. It MUST be regenerated quarterly; the regeneration script lives in <code>services/ai-gateway/pii/scripts/regen_fixture.py</code> and uses internal customer data (under DPA) plus public sources (Vietnam tax-code samples from gov.vn).</li><li>The <code>VN_NDD</code> recognizer is intentionally narrow (label-bound). Without the label, a bare name like \"Nguyễn Văn A\" falls through to Presidio's standard <code>PERSON</code> recognizer — still redacted, just without the legal-representative semantic tag. This trade-off favours precision over recall on the NDD-specific tag.</li><li>Recognizer ordering matters: <code>VN_PHONE</code> runs first so a 10-digit <code>0901234567</code> matches as phone (high confidence) before MST tries to match (low confidence on bare number). <code>VN_BANK_ACCOUNT</code> runs last because its broad 10-14 digit pattern would otherwise grab CCCDs and MSTs.</li><li>The <code>confidence.py</code> constants are tuning knobs. Calibration runs quarterly against the 200-sample fixture: we measure precision (false-positive rate) and recall (true-positive rate) at HIGH/MED/LOW thresholds and adjust if drift exceeds 1 percentage point.</li><li>The version endpoint (<code>/recognizers/version</code>) is small — six entries — but it's the auditability primitive that lets tenants verify \"what version of the redactor processed our prompts during the period of our DPA\". Useful in compliance reviews.</li><li>Future expansion (slice 6, TASK-AI-022): Khmer / Lao / Thai recognizers for ASEAN tenants. The pattern is the same — language-specific regexes + label-bound matching + shared confidence constants. The sidecar architecture handles multilingual recognizers natively (just register additional ones).</li><li>The <code>test_vn_no_false_positives.py</code> test file is the sibling of <code>test_vn_recognizers.py</code>. Both run on every PR. Negative tests catch the most-common regression: a regex tightening that breaks recall, or a regex loosening that breaks precision.</li></ul>\n<hr>\n<p><em>End of TASK-AI-012. Status: draft (10/10 target).</em></p>";
