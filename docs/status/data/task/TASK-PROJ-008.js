window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The history-event layer <strong>MUST</strong> record every mutation to issue rows AND link to a corresponding memory audit row via chain_anchor. The contract:</p>\n<ol><li><strong>MUST</strong> define <code>history_event</code> table with columns: <code>id UUID PK</code>, <code>issue_id UUID FK</code>, <code>seq BIGSERIAL</code>, <code>field TEXT</code>, <code>before JSONB</code>, <code>after JSONB</code>, <code>mutation_kind TEXT</code> (<code>scalar_lww | status_transition | crdt_snapshot | comment_added | comment_edited | comment_deleted</code>), <code>by_subject_id UUID</code>, <code>occurred_at_ns BIGINT</code>, <code>chain_anchor TEXT</code> (SHA-256 hex from memory row), <code>memory_row_id TEXT</code>, <code>tenant_id UUID</code>.</li><li><strong>MUST</strong> emit one history_event per mutation. EVERY mutating handler (status transition, scalar LWW, comment CRUD, snapshot persist) MUST call <code>history::emit_mutation()</code> AS PART OF THE SAME TRANSACTION.</li><li><strong>MUST</strong> link the history_event ↔ memory row bidirectionally:</li></ol>\n<ul><li>history_event row carries <code>memory_row_id</code> and <code>chain_anchor</code>.</li><li>memory audit row's payload carries <code>history_event_id</code> and <code>history_event_seq</code>.</li><li>Operators can pivot from either side.</li></ul>\n<ol><li><strong>MUST</strong> compute a per-field diff:</li></ol>\n<ul><li>Scalars: <code>before</code> = prior value JSON; <code>after</code> = new value JSON.</li><li>Y.Text fields (<code>description</code>, <code>comment.body</code>): <code>before</code> = SHA-256 of prior content; <code>after</code> = SHA-256 of new content; raw content NOT stored (privacy + size). The Y.Doc snapshots (TASK-PROJ-003) are the canonical text history.</li><li>Arrays (<code>labels</code>, <code>comments</code>): <code>before</code> and <code>after</code> are JSON arrays; diff applied client-side for visualization.</li></ul>\n<ol><li><strong>MUST</strong> make history table APPEND-ONLY:</li></ol>\n<ul><li>DELETE forbidden by RLS policy (only <code>cyberos_admin</code> role; never <code>cyberos_app</code>).</li><li>UPDATE forbidden on all fields except none (no field is mutable).</li><li>INSERT only via <code>history::emit_mutation()</code>.</li></ul>\n<ol><li><strong>MUST</strong> verify <code>chain_anchor</code> matches actual memory-chain hash on query:</li></ol>\n<ul><li>GET history endpoint walks <code>chain_anchor</code> per row → re-fetches memory row → asserts SHA-256(canonical(memory_row_minus_chain) || prev_chain) == chain_anchor.</li><li>Mismatch → 500 with <code>{\"error\":\"chain_tampered\",\"issue_id\":..,\"event_seq\":..}</code>; sev-1 alarm.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>GET /api/proj/issues/:id/history</code> returning the full timeline ordered by <code>seq ASC</code>. Response includes both raw history_event data AND verified chain_anchor flag per row.</li><li><strong>MUST</strong> support <code>?since=&lt;seq&gt;</code> parameter for incremental loading.</li><li><strong>MUST</strong> emit <code>proj.issue_mutated</code> memory audit row per mutation with payload <code>{issue_id, field, mutation_kind, before_hash, after_hash, by_subject_id, history_event_id, history_event_seq, trace_id}</code>.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_history_events_total{mutation_kind}</code> (counter).</li><li><code>proj_chain_anchor_verifications_total{result}</code> (counter; result ∈ ok | mismatch).</li></ul>\n<ol><li><strong>MUST</strong> RLS-enforce (TASK-AUTH-003).</li><li><strong>MUST</strong> cache <code>chain_verified</code> flag per history_event row for 60s in memory (per request worker) to amortise memory round-trips on hot endpoints (issue page load fetches 50+ history events).</li><li><strong>MUST</strong> support a background <code>chain-anchor-sweep</code> job (cron, hourly) that walks recent history rows + verifies chain anchors WITHOUT a user-driven query. Mismatches emit SEV-1 audit row <code>proj.chain_tampered</code>. This is proactive detection.</li><li><strong>MUST</strong> support pagination on history query: <code>?limit=N&amp;before_seq=M</code> (descending; reverse-chronological) and <code>?limit=N&amp;after_seq=M</code> (ascending; for delta loads). Default limit 50; max 500.</li><li><strong>MUST</strong> include <code>mutator_session_id</code> field correlating with TASK-PROJ-002 decision_session_id — one PATCH that mutates 3 fields produces 3 history events sharing a session_id.</li><li><strong>MUST</strong> support history \"summary\" endpoint that aggregates per-day per-field counts: <code>GET /api/proj/issues/:id/history/summary?from=&amp;to=</code> → <code>[{date, field, mutation_count}]</code>. For visual sparklines.</li><li><strong>MUST</strong> redact PII in <code>before</code>/<code>after</code> JSON for scalar fields whose name is in the per-tenant <code>pii_field_allowlist</code> (e.g. <code>assignee_email</code>, <code>client_phone</code>). Redacted form stored; raw form available only to admin via separate audit-fetch endpoint.</li><li><strong>MUST</strong> include <code>mutation_source</code> field: <code>web | mobile | api | cli | bridge_import | bulk_admin</code>. Operators investigating \"who/what made this change\" need the channel context.</li><li><strong>MUST</strong> validate <code>before</code> value matches the issue's current state at mutation time (optimistic-concurrency check): if <code>current_value != provided_before</code>, emit <code>before_mismatch</code> audit + return 409. Prevents lost-update bugs.</li><li><strong>MUST</strong> support <code>?include_session_summary=true</code> query parameter that adds a per-session aggregation to the response (<code>sessions: [{session_id, mutations: [...]}]</code>) so UI can render \"Alice made 3 changes in this PATCH.\"</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why dual-write (DEC-290)?</strong> memory audit rows live in the memory chain (Layer 1 immutable, tenant-cross-cutting); history_event lives in Postgres (queryable, indexed by issue_id, tenant-scoped). The pair gives queryability + immutability — querying history is fast; the memory row is the cryptographic proof.</p>\n<p><strong>Why chain_anchor (DEC-291)?</strong> Without it, a sufficiently-privileged operator could delete a history_event row and the memory audit row separately, leaving no detection. The anchor binds them: deleting the history row leaves a memory row pointing to it (detectable); tampering the memory row breaks the chain hash (detectable per TASK-MEMORY-101).</p>\n<p><strong>Why hash Y.Text content (§1 #4)?</strong> Issue descriptions can contain sensitive content (employee feedback, customer data). Storing raw before/after in history bloats the database AND duplicates the canonical Y.Doc snapshots (TASK-PROJ-003). Hash is sufficient for \"did the content change?\" — clients fetch the actual text from Y.Doc snapshots.</p>\n<p><strong>Why append-only (§1 #5)?</strong> History tables that allow UPDATE/DELETE become \"history-of-history\" rabbit holes. Append-only is the universal pattern for audit logs; aligns with memory chain semantics.</p>\n<p><strong>Why query-time chain_anchor verify (§1 #6)?</strong> Catches tampering at query time, not just at write time. Operators investigating \"did anyone alter the audit trail\" see verification status per row. Mismatch is loud (sev-1).</p>\n<p><strong>Why same transaction (§1 #2)?</strong> If history_event INSERT fails after memory row already emitted, the two diverge silently. Transactional pair = atomicity: both or neither.</p>\n<p><strong>Why 60s cache (§1 #12)?</strong> Issue page load fetches 50+ history rows; without cache, that's 50+ memory round-trips × ~10ms = 500ms page-load delay. 60s TTL is short enough that recent tampering surfaces quickly.</p>\n<p><strong>Why proactive sweep (§1 #13)?</strong> Tampering of an old row may not be detected until a query hits it. Hourly sweep ensures detection latency ≤ 1 hour. Critical for compliance audit posture.</p>\n<p><strong>Why pagination (§1 #14)?</strong> Long-lived issues accumulate thousands of mutations; unbounded fetch is slow + breaks clients. Default 50 + max 500 covers typical UI; before/after_seq enables infinite-scroll patterns.</p>\n<p><strong>Why session_id correlation (§1 #15)?</strong> Operator changes 3 fields in one PATCH; the 3 history events should be visually grouped (\"Alice changed status + assignee + estimate\"). Session_id is the correlation key.</p>\n<p><strong>Why summary endpoint (§1 #16)?</strong> Sparkline UI (\"activity on this issue over last 30 days\") needs aggregated counts, not full event detail. Pre-aggregation at server saves N round-trips.</p>\n<p><strong>Why PII redaction (§1 #17)?</strong> Audit trails persist for years; sensitive scalars (employee emails, client phone) shouldn't proliferate. Per-tenant allowlist respects compliance scope.</p>\n<p><strong>Why mutation_source (§1 #18)?</strong> Investigating \"this issue's estimate keeps changing\"; knowing it came from bulk_admin vs web tells operators where to look (script bug vs operator typo).</p>\n<p><strong>Why optimistic-concurrency check (§1 #19)?</strong> Two operators editing the same field concurrently produce lost updates. Provided <code>before</code> value as guard rejects the second.</p>\n<p><strong>Why session summary in query (§1 #20)?</strong> Without server-side aggregation, clients re-aggregate from raw events; inefficient + risk of inconsistency. One pre-aggregated response.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0008_history_events.sql\n\nCREATE TABLE history_event (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    issue_id        UUID NOT NULL,\n    seq             BIGSERIAL NOT NULL,\n    field           TEXT NOT NULL,\n    before          JSONB,\n    after           JSONB,\n    mutation_kind   TEXT NOT NULL CHECK (mutation_kind IN\n                    ('scalar_lww','status_transition','crdt_snapshot','comment_added','comment_edited','comment_deleted')),\n    by_subject_id   UUID NOT NULL,\n    occurred_at_ns  BIGINT NOT NULL,\n    chain_anchor    TEXT NOT NULL,\n    memory_row_id    TEXT NOT NULL,\n    tenant_id       UUID NOT NULL,\n    UNIQUE (issue_id, seq)\n);\nCREATE INDEX idx_history_event_issue ON history_event (issue_id, seq DESC);\nCREATE INDEX idx_history_event_by_subject ON history_event (by_subject_id, occurred_at_ns DESC);\n\nALTER TABLE history_event ENABLE ROW LEVEL SECURITY;\nCREATE POLICY history_event_tenant_isolation ON history_event\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);\n\n-- INSERT-only: revoke UPDATE/DELETE from cyberos_app role\nREVOKE UPDATE, DELETE ON history_event FROM cyberos_app;</code></pre>\n<h3>Rust API</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/history/mod.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum MutationKind {\n    ScalarLww, StatusTransition, CrdtSnapshot,\n    CommentAdded, CommentEdited, CommentDeleted,\n}\n\n#[derive(Clone, Debug, Serialize, sqlx::FromRow)]\npub struct HistoryEvent {\n    pub id:              uuid::Uuid,\n    pub issue_id:        uuid::Uuid,\n    pub seq:             i64,\n    pub field:           String,\n    pub before:          Option&lt;serde_json::Value&gt;,\n    pub after:           Option&lt;serde_json::Value&gt;,\n    pub mutation_kind:   MutationKind,\n    pub by_subject_id:   uuid::Uuid,\n    pub occurred_at_ns:  i64,\n    pub chain_anchor:    String,\n    pub memory_row_id:    String,\n}\n\npub async fn emit_mutation(\n    tx: &amp;mut sqlx::PgTransaction&lt;'_&gt;,\n    issue_id: uuid::Uuid,\n    field: &amp;str,\n    before: serde_json::Value,\n    after: serde_json::Value,\n    mutation_kind: MutationKind,\n    by_subject_id: uuid::Uuid,\n) -&gt; Result&lt;HistoryEvent, HistoryError&gt; {\n    let occurred_at_ns = chrono::Utc::now().timestamp_nanos_opt().unwrap();\n    let before_hash = hash_json(&amp;before);\n    let after_hash  = hash_json(&amp;after);\n\n    // 1. Insert history_event with PLACEHOLDER chain_anchor (filled after memory emit)\n    let event_id = uuid::Uuid::new_v4();\n    let seq: i64 = sqlx::query_scalar(\n        \"INSERT INTO history_event (id, issue_id, field, before, after, mutation_kind,\n                                     by_subject_id, occurred_at_ns, chain_anchor, memory_row_id, tenant_id)\n         VALUES ($1, $2, $3, $4, $5, $6, $7, $8, '', '', current_setting('app.tenant_id')::uuid)\n         RETURNING seq\"\n    ).bind(event_id).bind(issue_id).bind(field).bind(&amp;before).bind(&amp;after)\n     .bind(mutation_kind).bind(by_subject_id).bind(occurred_at_ns)\n     .fetch_one(&amp;mut **tx).await?;\n\n    // 2. Emit memory row (returns chain_anchor)\n    let payload = serde_json::json!({\n        \"issue_id\": issue_id, \"field\": field, \"mutation_kind\": mutation_kind,\n        \"before_hash\": before_hash, \"after_hash\": after_hash,\n        \"by_subject_id\": by_subject_id, \"history_event_id\": event_id, \"history_event_seq\": seq,\n        \"trace_id\": current_trace_id(),\n    });\n    let memory_emit = memory_writer::emit_tx(\"proj.issue_mutated\", payload).await?;\n\n    // 3. Update history_event with the actual chain_anchor + memory_row_id\n    sqlx::query(\n        \"UPDATE history_event SET chain_anchor = $1, memory_row_id = $2 WHERE id = $3\"\n    ).bind(&amp;memory_emit.chain_anchor).bind(&amp;memory_emit.row_id).bind(event_id)\n     .execute(&amp;mut **tx).await?;\n\n    Ok(HistoryEvent {\n        id: event_id, issue_id, seq, field: field.into(),\n        before: Some(before), after: Some(after),\n        mutation_kind, by_subject_id, occurred_at_ns,\n        chain_anchor: memory_emit.chain_anchor, memory_row_id: memory_emit.row_id,\n    })\n}\n\nfn hash_json(v: &amp;serde_json::Value) -&gt; String {\n    let canon = serde_json::to_vec(v).unwrap();\n    hex::encode(sha2::Sha256::digest(&amp;canon))\n}</code></pre>\n<h3>Query handler</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/history/query.rs\npub async fn list_history(\n    pool: &amp;sqlx::PgPool,\n    issue_id: uuid::Uuid,\n    since: Option&lt;i64&gt;,\n) -&gt; anyhow::Result&lt;Vec&lt;HistoryEventWithVerification&gt;&gt; {\n    let rows: Vec&lt;HistoryEvent&gt; = sqlx::query_as(\n        \"SELECT * FROM history_event\n         WHERE issue_id = $1 AND seq &gt; COALESCE($2, 0)\n         ORDER BY seq ASC\"\n    ).bind(issue_id).bind(since).fetch_all(pool).await?;\n\n    let mut out = Vec::with_capacity(rows.len());\n    for row in rows {\n        let verified = verify_chain_anchor(&amp;row).await;\n        if !verified {\n            metrics::counter!(\"proj_chain_anchor_verifications_total\", \"result\" =&gt; \"mismatch\").increment(1);\n            tracing::error!(issue_id = %issue_id, seq = row.seq, \"chain_anchor mismatch\");\n            // Sev-1 alarm via OBS\n        } else {\n            metrics::counter!(\"proj_chain_anchor_verifications_total\", \"result\" =&gt; \"ok\").increment(1);\n        }\n        out.push(HistoryEventWithVerification { event: row, chain_verified: verified });\n    }\n    Ok(out)\n}\n\nasync fn verify_chain_anchor(row: &amp;HistoryEvent) -&gt; bool {\n    // Fetch memory row by memory_row_id; recompute chain hash\n    match memory_reader::fetch_row(&amp;row.memory_row_id).await {\n        Ok(br) =&gt; br.chain_anchor() == row.chain_anchor,\n        Err(_) =&gt; false,  // Row missing = tampered\n    }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Status transition emits history_event</strong> — issue status change → 1 history_event row with mutation_kind=status_transition.</li><li><strong>Scalar LWW emits history_event</strong> — title PATCH → 1 row with mutation_kind=scalar_lww, before/after scalars.</li><li><strong>CRDT snapshot emits history_event</strong> — description Y.Doc snapshot → 1 row with mutation_kind=crdt_snapshot; before/after = content hashes (not raw text).</li><li><strong>Comment add emits history_event</strong> — POST comment → mutation_kind=comment_added; before=null; after={id, author, hash}.</li><li><strong>history_event linked to memory row</strong> — row's <code>memory_row_id</code> matches a memory row with <code>kind=\"proj.issue_mutated\"</code>.</li><li><strong>memory row links back</strong> — memory row's <code>payload.history_event_id</code> matches the history_event UUID.</li><li><strong>chain_anchor verified</strong> — happy query → all rows <code>chain_verified: true</code>.</li><li><strong>chain_anchor mismatch detected</strong> — manually tamper a memory row → query returns <code>chain_verified: false</code>; metric increments.</li><li><strong>Append-only enforced</strong> — DELETE/UPDATE on history_event by cyberos_app role → permission denied.</li><li><strong>seq monotonic per issue</strong> — 10 mutations → seq 1..10 consecutive.</li><li><strong>Same-transaction pair</strong> — if memory emit fails, history_event also rolls back; observed via DB inspection after forced failure.</li><li><strong><code>?since=&lt;seq&gt;</code> returns delta</strong> — query with since=5 → returns rows 6..N.</li><li><strong>Y.Text hash, not raw</strong> — description history rows have hash strings (not raw markdown) in before/after.</li><li><strong>RLS tenant isolation</strong> — tenant A's history invisible to tenant B.</li><li><strong>OTel metric per mutation_kind</strong> — counter <code>proj_history_events_total{mutation_kind=\"scalar_lww\"}</code> increments on scalar PATCH.</li><li><strong>chain_verified cached for 60s</strong> — second query within 60s avoids re-fetching memory row; cache hit metric increments (AC for §1 #12).</li><li><strong>Background sweep detects tamper</strong> — tamper a row at T0; sweep at T+1h emits <code>proj.chain_tampered</code> SEV-1 (AC for §1 #13).</li><li><strong>Pagination returns ≤ limit</strong> — GET ?limit=50 with 200 events → 50 returned; before_seq cursor included (AC for §1 #14).</li><li><strong>Session_id correlates multi-field PATCH</strong> — PATCH changing status+assignee+estimate → 3 history events sharing session_id (AC for §1 #15).</li><li><strong>Summary endpoint aggregates per day</strong> — GET /history/summary?from=&amp;to= → [{date, field, count}] (AC for §1 #16).</li><li><strong>PII redaction applied</strong> — tenant <code>pii_field_allowlist: [assignee_email]</code>; PATCH email → history row shows <code>&lt;EMAIL&gt;</code> in before/after; admin endpoint shows raw (AC for §1 #17).</li><li><strong>mutation_source captured</strong> — PATCH via API → mutation_source=api; via CLI → mutation_source=cli (AC for §1 #18).</li><li><strong>Optimistic-concurrency check enforces</strong> — PATCH with stale <code>before</code> value → 409 <code>before_mismatch</code>; audit row emitted (AC for §1 #19).</li><li><strong>Session summary in response</strong> — GET ?include_session_summary=true → response has sessions array grouping events (AC for §1 #20).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn status_transition_emits_history_and_memory_pair() {\n    let env = TestEnv::new().await;\n    let issue = env.create_issue_in_status(IssueStatus::Todo).await;\n    let _ = apply_transition(&amp;env.pool, issue, IssueStatus::InProgress, env.alice(), None).await.unwrap();\n    let events: Vec&lt;HistoryEvent&gt; = sqlx::query_as(\"SELECT * FROM history_event WHERE issue_id = $1\")\n        .bind(issue).fetch_all(&amp;env.pool).await.unwrap();\n    assert_eq!(events.len(), 1);\n    assert_eq!(events[0].mutation_kind, MutationKind::StatusTransition);\n    assert!(!events[0].chain_anchor.is_empty());\n\n    let memory_row = env.memory.find_by_id(&amp;events[0].memory_row_id).await.unwrap();\n    assert_eq!(memory_row[\"payload\"][\"history_event_id\"], events[0].id.to_string());\n}\n\n#[tokio::test]\nasync fn description_history_stores_hash_not_raw() {\n    let env = TestEnv::new().await;\n    let issue = env.create_issue().await;\n    env.update_description(issue, \"secret content\").await;\n    let event: HistoryEvent = sqlx::query_as(\"SELECT * FROM history_event WHERE issue_id = $1 AND field = 'description'\")\n        .bind(issue).fetch_one(&amp;env.pool).await.unwrap();\n    let after = event.after.unwrap();\n    let after_str = serde_json::to_string(&amp;after).unwrap();\n    assert!(!after_str.contains(\"secret content\"));\n    assert!(after_str.contains(\"\\\"\"));   // hash string present\n}\n\n#[tokio::test]\nasync fn chain_anchor_tamper_detected() {\n    let env = TestEnv::new().await;\n    let issue = env.create_issue_with_2_mutations().await;\n    env.memory.tamper_row_for_test(/* the second mutation's memory row */).await;\n\n    let history = list_history(&amp;env.pool, issue, None).await.unwrap();\n    let tampered_count = history.iter().filter(|h| !h.chain_verified).count();\n    assert!(tampered_count &gt;= 1);\n}\n\n#[tokio::test]\nasync fn append_only_enforced_for_cyberos_app() {\n    let env = TestEnv::new().await;\n    let issue = env.create_issue_with_mutation().await;\n    let res: Result&lt;sqlx::postgres::PgQueryResult, _&gt; = sqlx::query(\"DELETE FROM history_event WHERE issue_id = $1\")\n        .bind(issue).execute(env.pool_as(\"cyberos_app\")).await;\n    assert!(res.is_err());   // permission denied\n}\n\n#[tokio::test]\nasync fn since_parameter_returns_delta() {\n    let env = TestEnv::new().await;\n    let issue = env.create_issue_with_n_mutations(10).await;\n    let delta = list_history(&amp;env.pool, issue, Some(5)).await.unwrap();\n    assert_eq!(delta.len(), 5);\n    assert!(delta.iter().all(|h| h.event.seq &gt; 5));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + DB above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-001</strong> — issues table FK.</li><li><strong>TASK-PROJ-003</strong> — CRDT snapshot trigger.</li><li><strong>TASK-PROJ-004</strong> — status transition handler.</li><li><strong>TASK-MEMORY-101</strong> — MemoryWriter::emit_tx provides chain_anchor + row_id.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.issue_mutated\",\n  \"payload\": {\n    \"issue_id\":           \"iss-...\",\n    \"field\":              \"status\",\n    \"mutation_kind\":      \"status_transition\",\n    \"before_hash\":        \"9b0e8c5...\",\n    \"after_hash\":         \"ab12cd...\",\n    \"by_subject_id\":      \"7e57c0de-...\",\n    \"history_event_id\":   \"0e3b1a2c-...\",\n    \"history_event_seq\":  42,\n    \"trace_id\":           \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Background chain_anchor verification scan (proactive vs query-time) — slice 3+; current is reactive.</li><li>History compaction (after 5 years, keep only summary rows) — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>memory emit fails mid-tx</td><td>sqlx tx rollback</td><td>history_event row not committed</td><td>None automatic; caller retries</td></tr><tr><td>chain_anchor recompute differs</td><td>verify_chain_anchor returns false</td><td>sev-1 alarm; row marked unverified</td><td>Investigate tamper; engage incident response</td></tr><tr><td>memory row deleted (impossible per AGENTS.md §3.5)</td><td>fetch_row Err</td><td>sev-1; marked unverified</td><td>None automatic</td></tr><tr><td>history_event DELETE attempted</td><td>RLS / role permission</td><td>permission denied; logged</td><td>None — by design</td></tr><tr><td>Concurrent mutations same field</td><td>LWW handles</td><td>each gets distinct seq; both recorded</td><td>None</td></tr><tr><td>Y.Doc snapshot diff-hash collision</td><td>impractical (SHA-256)</td><td>None</td><td>None</td></tr><tr><td>Query without since returns 10K+ rows</td><td>unbounded</td><td>Slow query; add pagination</td><td>Slice 3+ paginate</td></tr><tr><td>Tampered chain anchor by privileged role</td><td>verify catches at query</td><td>Detected; sev-1</td><td>Investigate</td></tr><tr><td>Multiple history rows for one mutation</td><td>shouldn't happen if handlers correct</td><td>Audit catches; integrity check</td><td>Code review</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>OTel exporter unavailable</td><td>buffered then dropped</td><td>Logged</td><td>Restore TASK-OBS-001</td></tr><tr><td>Chain reader unreachable</td><td>verify returns false (treat as missing)</td><td>Marked unverified</td><td>Investigate TASK-MEMORY-101</td></tr><tr><td>Encrypted body field (future)</td><td>hash applies to ciphertext</td><td>Hash is over what's stored</td><td>By design</td></tr><tr><td>Cache hit returns stale verified=true for tampered row</td><td>60s window</td><td>Detected within 1h via sweep</td><td>None — by design</td></tr><tr><td>Sweep job fails</td><td>logged + retry on next cron</td><td>None visible until next sweep</td><td>Operator investigates</td></tr><tr><td>Pagination cursor invalid (seq doesn't exist)</td><td>empty result</td><td>400</td><td>Caller adjusts</td></tr><tr><td>session_id missing on PATCH</td><td>server generates one</td><td>each event has unique session</td><td>None</td></tr><tr><td>Summary endpoint scan-heavy on huge issue</td><td>aggregate-as-you-go via cursor</td><td>bounded time</td><td>None</td></tr><tr><td>PII field allowlist update</td><td>next mutation honours new list</td><td>None</td><td>None</td></tr><tr><td>Operator request raw PII via admin endpoint without auth</td><td>RBAC enforces</td><td>403</td><td>None</td></tr><tr><td>mutation_source missing (legacy clients)</td><td>default \"api\"</td><td>None</td><td>None</td></tr><tr><td>Optimistic-concurrency provided <code>before</code> is stale</td><td>409</td><td>Caller re-reads + retries</td><td>None</td></tr><tr><td>Same session_id reused across PATCHes (bug)</td><td>dedup at handler</td><td>one prevails; SEV-3 warn</td><td>Author fixes</td></tr><tr><td>Tamper of cache (in-process memory)</td><td>cache is process-local</td><td>replicated risk</td><td>None</td></tr><tr><td>Sweep finds tamper but memory row missing</td><td>treat as tamper</td><td>SEV-1</td><td>Operator investigates</td></tr><tr><td>pii_field_allowlist with regex</td><td>not supported (literal names only)</td><td>422 if regex provided</td><td>Operator uses literal field names</td></tr><tr><td>Summary aggregation over &gt; 1y range</td><td>timeout</td><td>400 with suggest narrower</td><td>Caller narrows</td></tr><tr><td>include_session_summary with 1000 events</td><td>response size &gt; 1MB</td><td>truncated; 413 if &gt; 5MB</td><td>Caller paginates</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The <code>emit_mutation</code> API takes a <code>&amp;mut sqlx::PgTransaction</code> to ensure same-tx semantics; callers' handlers are responsible for starting + committing the tx.</li><li>memory emit returns <code>(chain_anchor, row_id)</code> synchronously per TASK-MEMORY-101's writer protocol; the history_event UPDATE after emit fills these in.</li><li><code>hash_json</code> uses serde_json's default serialization (no sort_keys); for canonical hashing across runs, callers should pre-canonicalise. The hash is for \"did it change,\" not for cross-system reproducibility.</li><li>Y.Text content hashes use SHA-256 over the encoded Y.Doc update bytes (deterministic via Yjs binary format).</li><li><code>cyberos_app</code> is the runtime SQL role (TASK-AUTH-003); <code>cyberos_ops</code> (admin) bypasses the REVOKE.</li><li>Chain verification on every query is O(N) memory reads; for hot endpoints (issue page load), cache verified flags for 60s.</li><li><code>proj_chain_anchor_verifications_total{result=\"mismatch\"}</code> &gt; 0 triggers TASK-OBS-007 sev-1 alert.</li><li>Future: per-field \"annotations\" (operator's explanation of why a mutation happened) — slice 3+.</li><li>The 60s cache is per-worker (not shared across pods); if pods are sticky-routed per tenant, cache hit rate is high. Cross-pod cache would add Redis dependency without justified benefit.</li><li>Background sweep is intentionally simple: walks <code>WHERE seq &gt; &lt;last_swept_seq&gt;</code> per tenant, verifies, advances watermark. Sub-minute completion for typical tenants.</li><li>Pagination cursor (before_seq / after_seq) was chosen over offset because: (a) offset is unstable when concurrent inserts happen; (b) cursor is keyset-based, O(log n) index lookup.</li><li>session_id correlation with TASK-PROJ-002 decision_session_id: both come from the same source (request-scoped header or client-generated); the proj handlers propagate.</li><li>Summary endpoint uses Postgres <code>generate_series</code> + JOIN for efficient per-day aggregation; bounded by date range.</li><li>PII field allowlist is per-tenant (different tenants have different sensitivity); literal field names only (no regex) to keep the allowlist auditable.</li><li>mutation_source defaults to \"api\" for backward compat; explicit values come from request headers (e.g. <code>X-Cyberos-Source: web</code>).</li><li>Optimistic-concurrency <code>before</code> value is the bug-prevention mechanism; without it, \"last-write-wins\" loses data silently. With it, the concurrent loser gets a 409 and can re-read.</li><li>The session summary aggregation is computed in Postgres via window functions; bounded by event count.</li><li>We considered storing the full memory row alongside history_event to avoid the memory round-trip on verify, but rejected: duplication = drift risk; the round-trip is fast (cached after first).</li><li>Chain verification result is per-row; partial mismatch in a result set still returns the OK rows (per-row verified flag). Clients decide what to do with mixed results.</li><li>The <code>proj.chain_tampered</code> SEV-1 audit row from the sweep includes the offending issue_id, seq, expected_chain, actual_chain — operators have full context.</li><li>We rejected hashing the raw <code>before</code>/<code>after</code> JSON for scalars too (only Y.Text gets hashed) because scalar diffs are operator-meaningful (\"estimate: 5 → 8\") and the bytes are small.</li><li>The summary endpoint's date range is bounded at 1 year to keep response time bounded; longer ranges should use TASK-MEMORY-108 search directly.</li><li>The mutator_source label is bounded (6 enum values) for metric cardinality safety.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-008.</em></p>";
