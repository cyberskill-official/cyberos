window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-135"]="<h1>TASK-IMP-135: Publish npm release shipping bin.cs for the rename</h1>\n<h2>Summary</h2>\n<p>Cut and publish the first <code>@cyberskill/cyberos</code> npm release whose <code>bin</code> field is <code>cs</code> (not <code>cyberos</code>), via this repo's existing OIDC <code>release.yml</code> pipeline — closing the plan-level gap TASK-IMP-133's audit ISS-005 named, so the Homebrew tap follow-up has a real registry artifact to pin.</p>\n<h2>Problem</h2>\n<p>TASK-IMP-130's acceptance criteria prove a scratch build's <code>package.json</code> declares <code>bin.cs</code>. They do not require that build to reach the npm registry. TASK-IMP-133's entire premise — bump <code>Formula/cyberos-cli.rb</code>'s <code>url</code>/<code>sha256</code> and assert <code>bin/\"cs\"</code> — and TASK-IMP-134's manual release-time checklist both assume a published release with <code>bin.cs</code> already exists. As of 2026-07-23 the live registry still serves <code>@cyberskill/cyberos@1.0.9</code> with <code>bin.cyberos</code>, while the rename code lives only on the open PR branch. TASK-IMP-133's own audit (ISS-005) and Edge cases section named this as an unowned gap in the five-task batch; leaving it unowned means IMP-133 stays permanently blocked with no task whose <code>done</code> criterion is \"the registry artifact exists.\"</p>\n<h2>Proposed Solution</h2>\n<p>Own the operational cut through the audited release path already documented in <code>docs/deploy/RELEASE.md</code> and implemented by <code>.github/workflows/release.yml</code>:</p>\n<ol><li>Land the rename code (TASK-IMP-130 and siblings) on <code>main</code> via the normal PR merge — never a direct push to <code>main</code>.</li><li>Let <code>version.yml</code> / <code>scripts/cyberos-version.mjs</code> produce the next platform <code>VERSION</code> (projected <code>1.1.0</code> minor from the rename <code>feat</code> commits), or apply an explicit <code>Release-As:</code> / <code>--set</code> if the operator chooses a different SemVer; promote the <code>[Unreleased]</code> Breaking rename entry into that dated CHANGELOG section.</li><li>Tag <code>v$(cat VERSION)</code> at the bump commit and push the tag so <code>release.yml</code> fires natively (TASK-IMP-071).</li><li>Wait for the <code>npm</code> job (OIDC trusted publishing, <code>id-token: write</code>, no <code>NPM_TOKEN</code>) to publish <code>@cyberskill/cyberos@&lt;version&gt;</code>.</li><li>Verify live via <code>npm view</code> that the published version's <code>bin</code> contains <code>cs</code> and does not contain <code>cyberos</code>.</li></ol>\n<p>That live registry fact is the sole publish evidence this task accepts — a local <code>npm pack</code> / scratch tarball does not satisfy it.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Leave the gap as an Edge case note on TASK-IMP-133 and wait for an ad-hoc operator release. Rejected: Stephen's 2026-07-23 judgment explicitly directed that the gap must not stay unowned; IMP-133 and IMP-134's manual checklist otherwise have no status-gating owner for their shared precondition.</li><li>Publish from a developer laptop with <code>npm login</code> + <code>npm publish</code>. Rejected: <code>docs/deploy/RELEASE.md</code> documents that the package uses trusted publishing (OIDC) pinned to <code>release.yml</code>; a laptop publish either requires a token the docs say to revoke, or fights the \"Require 2FA and disallow tokens\" lock. Not the audited path.</li><li>Fold publish verification into TASK-IMP-130 by amending its ACs. Rejected: IMP-130 is already <code>done</code> with scratch-build ACs; reopening it conflates \"code correct in tree\" with \"artifact on registry,\" and would invalidate the TRACE-004 evidence already recorded for its offline tests.</li><li>Cut the release from the open PR branch without merging to <code>main</code>. Rejected: <code>release.yml</code> checks out the tag ref; tags on a non-main tip that never merges leave <code>main</code> without the rename while the registry claims it shipped — the continuous-delivery and version-bump contracts both assume the bump commit lives on <code>main</code>.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: within one release cycle after TASK-IMP-130's code is on <code>main</code>, <code>npm view @cyberskill/cyberos version</code> returns a version strictly newer than <code>1.0.9</code> whose <code>bin</code> object has key <code>cs</code> and lacks key <code>cyberos</code>. Baseline today: <code>1.0.9</code> / <code>bin.cyberos</code>.</li><li>Guardrail: that version was published by the <code>release.yml</code> <code>npm</code> job for tag <code>v&lt;version&gt;</code> (OIDC), not by a local <code>npm publish</code> — confirmed by a successful Actions run for that tag, not by trusting a developer machine's npm auth state.</li></ul>\n<h2>Scope</h2>\n<p>In scope: merging the rename PR to <code>main</code> (or confirming it is already merged), the VERSION/CHANGELOG cut for the release that carries <code>bin.cs</code>, pushing the <code>v*</code> tag that fires <code>release.yml</code>, and verifying the live npm package's <code>bin</code> field.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any further change to <code>tools/install/build.sh</code>'s <code>bin</code> field or CLI dispatch — that is TASK-IMP-130 (already done).</li><li>Updating <code>Formula/cyberos-cli.rb</code> in <code>homebrew-tap</code> — that is TASK-IMP-133, which this task unblocks.</li><li>Running TASK-IMP-134's manual clean-machine checklist — still a release-process step owned by that spec's Edge cases, not by this task's status transition.</li><li>Publishing desktop/mobile native installers beyond whatever <code>release.yml</code> already does for the same tag — incidental; this task's acceptance criteria are npm-bin only.</li><li>A local <code>npm publish</code> from a developer laptop, inventing an <code>NPM_TOKEN</code>, or renaming <code>release.yml</code> (the filename is part of the OIDC trust pin).</li></ul>\n<h2>Dependencies</h2>\n<p>Depends on TASK-IMP-130 — the code that emits <code>bin.cs</code> must exist in the tree that the release tag points at. Soft coordination with TASK-IMP-131/132/134: those are already done on the same PR branch and should land with 130; this task does not list them in <code>depends_on</code> because the publish contract is specifically \"the built payload's bin is cs,\" which 130 alone defines.</p>\n<p>Blocks TASK-IMP-133 — that task MUST NOT merge a Formula pin to a <code>cs</code>-bin tarball until this task's live <code>npm view</code> evidence exists.</p>\n<p><strong>Relationship to TASK-IMP-069 / TASK-IMP-071.</strong> IMP-069 added the payload release assets and the npm job shape; IMP-071 made <code>git push origin vX.Y.Z</code> fire <code>release.yml</code> natively (no <code>[skip ci]</code> brake). This task consumes both — it does not re-implement them.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Composer (Cursor agent) continuing the CyberOS <code>task-author</code> discipline after Stephen's 2026-07-23 judgment call.</li><li><strong>Scope:</strong> every <code>source_pages</code> line was re-read or re-queried in this session (live <code>npm view</code>, <code>cyberos-version.mjs --check</code>, <code>release.yml</code> npm job, IMP-133 ISS-005) rather than carried forward from a prior agent's notes alone.</li><li><strong>Human review:</strong> scope authorized by Stephen's explicit \"do as your judgment\" on the npm-release halt; PLAN-style approval treated as given for this single gap-closing task under that instruction.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The rename code from TASK-IMP-130 MUST be present on <code>main</code> at the commit the release tag points to — landed via PR merge, never a direct push to <code>main</code>.</li><li>1.2 Platform <code>VERSION</code> MUST equal the SemVer that will be tagged and published, and <code>CHANGELOG.md</code> MUST contain a dated <code>## [&lt;VERSION&gt;]</code> section that includes the Breaking <code>cyberos</code>→<code>cs</code> rename entry (promoted out of <code>[Unreleased]</code>).</li><li>1.3 A git tag <code>v$(cat VERSION)</code> MUST exist and MUST point at the bump commit that carries that VERSION; pushing that tag MUST be what triggers <code>.github/workflows/release.yml</code> (native <code>push: tags</code> path per TASK-IMP-071).</li><li>1.4 The <code>release.yml</code> <code>npm</code> job for that tag MUST conclude <code>success</code>, publishing <code>@cyberskill/cyberos@&lt;VERSION&gt;</code> via OIDC trusted publishing (<code>id-token: write</code>) — not via a long-lived npm token and not via a laptop <code>npm publish</code>.</li><li>1.5 After the job succeeds, a live <code>npm view @cyberskill/cyberos@&lt;VERSION&gt; bin</code> MUST show a <code>cs</code> key and MUST NOT show a <code>cyberos</code> key. A local scratch build or <code>npm pack</code> artifact MUST NOT be accepted as substitute evidence for this clause.</li><li>1.6 This task MUST NOT mark itself <code>done</code> until clause 1.5's live registry evidence exists. It MUST NOT mark TASK-IMP-133 <code>done</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - <code>git merge-base --is-ancestor &lt;IMP-130-landing-commit&gt; origin/main</code> exits 0, and <code>git show origin/main:tools/install/build.sh</code> contains the literal <code>\"cs\": \"cli/bin/cli.mjs\"</code> bin entry - test: shell: <code>git fetch origin main &amp;&amp; git merge-base --is-ancestor \"$(git log origin/main --grep='TASK-IMP-130' --format=%H | head -1)\" origin/main</code> exits 0 AND <code>git show origin/main:tools/install/build.sh | grep -F '\"cs\": \"cli/bin/cli.mjs\"'</code> exits 0</li><li>[ ] AC 2 (traces_to: #1.2) - for <code>V=$(git show origin/main:VERSION | tr -d '[:space:]')</code>, <code>CHANGELOG.md</code> on <code>origin/main</code> contains a heading line matching <code>## [$V]</code> and that section (until the next <code>## </code> heading) contains both the substring <code>cyberos</code> and the substring `<code> </code>cs<code> </code><code> in the rename Breaking bullet - test: shell: extract the dated section for </code>$V<code> and </code>grep -F<code> for the rename markers; fail if the bullet remains only under </code>## [Unreleased]`</li><li>[ ] AC 3 (traces_to: #1.3, #1.4) - tag <code>v$V</code> exists on <code>origin</code>, <code>git rev-list -n1 v$V</code> equals the commit where <code>VERSION</code> became <code>$V</code>, and the GitHub Actions run of workflow <code>release.yml</code> for that tag has job <code>npm</code> with <code>conclusion=success</code> - test: <code>gh api \"/repos/cyberskill-official/cyberos/actions/runs?event=push&amp;per_page=20\" --jq ...</code> (or <code>gh run list --workflow=release.yml --branch \"v$V\"</code>) filters to that tag's run and asserts the npm job success</li><li>[ ] AC 4 (traces_to: #1.5) - <code>npm view @cyberskill/cyberos@\"$V\" version</code> prints <code>$V</code>, and <code>npm view @cyberskill/cyberos@\"$V\" bin</code> JSON-parses to an object with key <code>cs</code> and without key <code>cyberos</code> - test: <code>node -e 'const b=JSON.parse(require(\"child_process\").execSync(\"npm view @cyberskill/cyberos@'\"$V\"' bin --json\",\"utf8\")); if(!b.cs||b.cyberos) process.exit(1)'</code></li><li>[ ] AC 5 (traces_to: #1.5, #1.6) - AC 4's evidence is recorded against the live registry (command output from <code>npm view</code>, not from a local <code>dist/</code> or <code>.tgz</code>), and this task's frontmatter <code>status</code> remains non-<code>done</code> until that output is captured; TASK-IMP-133's frontmatter <code>status</code> is unchanged by this task - test: verify: the ship-tasks / final-acceptance note for this task pastes the <code>npm view</code> stdout, and <code>git diff</code> for this task's landing commit does not flip TASK-IMP-133's status</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>If PR #109 (or its successor) is not yet mergeable (failing gates, review block, merge conflict), this task HALTs at clause 1.1 — authoring and prep are done; the operator must unblock the merge. Do not force-merge past red gates.</li><li>If <code>version.yml</code>'s Deploy Key / ruleset bypass is missing, the auto-bump may compute <code>1.1.0</code> in the run summary without pushing — then the operator (or this task's implementer with an explicit, reviewed bump commit on a PR) must land <code>VERSION</code>/<code>CHANGELOG</code> via PR before tagging. Still no direct push to <code>main</code>.</li><li>If <code>@cyberskill/cyberos@$V</code> is already on the registry when the npm job runs, <code>release.yml</code> intentionally no-ops the publish (<code>already published — nothing to do</code>). That satisfies clause 1.4's job-success requirement only if AC 4 still shows <code>bin.cs</code> for that version — a previously published wrong-bin version under the same SemVer is a hard failure requiring a new SemVer, not a re-tag.</li><li>If OIDC trusted-publishing config on npmjs.com drifts (wrong workflow filename, missing <code>id-token: write</code>, Node too old), the npm job fails with <code>ENEEDAUTH</code> / similar — this task HALTs and surfaces the exact Actions log; do not fall back to a token publish.</li><li>Security-class: this task publishes a public npm package. The audited path uses short-lived OIDC; introducing a long-lived <code>NPM_TOKEN</code> to \"just get it out\" is explicitly forbidden by this task's non-goals and by <code>docs/deploy/RELEASE.md</code>.</li></ul>\n<hr>\n<p><em>End of TASK-IMP-135.</em></p>";
