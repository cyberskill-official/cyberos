window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-007"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The billing-mode layer <strong>MUST</strong> support exactly three modes per engagement with mode-aware rollups feeding TASK-INV-001 invoice generation. The contract:</p>\n<ol><li><strong>MUST</strong> define <code>billing_modes</code> table with columns: <code>id UUID PK</code>, <code>engagement_id UUID FK</code>, <code>mode</code> (<code>time_and_materials | fixed_fee | retainer</code>), <code>effective_from DATE</code>, <code>effective_to DATE</code> (nullable), <code>config JSONB</code> (mode-specific), <code>created_at</code>, <code>created_by_subject_id</code>, <code>tenant_id</code>. Versioned same as rate cards (TASK-PROJ-005); INSERT-only.</li><li><strong>MUST</strong> support <code>TimeAndMaterials</code> mode with config <code>{minimum_billable_increment_minutes: 15, weekly_cap_hours?: i32}</code>. Rollup = sum of billable time entries × applicable rate card.</li><li><strong>MUST</strong> support <code>FixedFee</code> mode with config <code>{total_amount_minor: i64, currency, milestones: [{id, name, target_pct_complete: f32, target_date, amount_minor: i64}]}</code>. Rollup = sum of completed-milestone amounts (where milestone is \"done\" per TASK-PROJ-004 issue.status). Sum of milestone amounts MUST equal total_amount_minor (validated at config insert).</li><li><strong>MUST</strong> support <code>Retainer</code> mode with config <code>{monthly_cap_minutes: i32, currency, base_amount_minor: i64, overage_rate_minor_per_hour: i64, rollover_months: u8}</code>. Rollup = always invoice base_amount_minor per month; if <code>consumed_minutes_this_period &gt; cap + rollover_credit</code>, emit overage line at <code>overage_rate_minor_per_hour × (excess_minutes / 60)</code>.</li><li><strong>MUST</strong> expose <code>rollup_invoiceable(engagement_id, period_start, period_end) -&gt; InvoiceRollup</code> returning ordered list of <code>InvoiceLine { line_kind, description, quantity, unit_rate_minor, amount_minor, currency, source_refs }</code> where source_refs cross-references to time entries / milestone IDs.</li><li><strong>MUST</strong> record mode changes via supersession (new row + close prior <code>effective_to</code>). Mode-change DURING an open invoice period prorates: portion before change billed under old mode, portion after under new.</li><li><strong>MUST</strong> emit memory audit rows:</li></ol>\n<ul><li><code>proj.billing_mode_set</code> on initial mode set.</li><li><code>proj.billing_mode_changed</code> on supersession with <code>{old_mode, new_mode, effective_from, prorating_applied}</code>.</li><li><code>proj.milestone_invoiced</code> per Fixed-Fee milestone rollup.</li><li><code>proj.retainer_overage_emitted</code> per Retainer overage line.</li><li><code>proj.retainer_rollover_carry_forward</code> per period transition with unused credit.</li></ul>\n<ol><li><strong>MUST</strong> validate Fixed-Fee <code>milestones[*].amount_minor</code> sums to <code>total_amount_minor</code> exactly (no rounding).</li><li><strong>MUST</strong> track Retainer <code>consumed_minutes_this_period</code> + <code>rollover_credit_minutes</code> in dedicated table <code>retainer_state(engagement_id, period_year_month, consumed_minutes, rollover_credit_minutes, base_amount_invoiced, overage_minutes_invoiced, computed_at, tenant_id)</code>.</li><li><strong>MUST</strong> support <code>cyberos engagements set-mode &lt;eng&gt; &lt;mode&gt;</code> CLI for ops with idempotency.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_billing_rollup_duration_seconds{mode}</code> (histogram).</li><li><code>proj_retainer_overage_total{engagement_id_bucket}</code> (counter — overage frequency dashboard).</li><li><code>proj_fixed_fee_milestones_invoiced_total</code> (counter).</li></ul>\n<ol><li><strong>MUST</strong> RLS-enforce (TASK-AUTH-003).</li><li><strong>MUST</strong> apply mid-period mode-change proration when the rollup period straddles a mode-change date. Specifically: split the period at the mode-change boundary; compute rollup independently for each sub-period under its mode; combine into a single <code>InvoiceRollup</code> with <code>prorated: true</code> marker and per-sub-period source_refs.</li><li><strong>MUST</strong> support <code>cancel_milestone(milestone_id, reason)</code> — Fixed-Fee admin can cancel an in-progress milestone, redistributing its amount across remaining milestones OR refunding to client. Emits <code>proj.milestone_cancelled</code> audit + reduces total_amount_minor.</li><li><strong>MUST</strong> support <code>add_milestone(after_milestone_id, milestone)</code> — Fixed-Fee admin can add a milestone post-contract (e.g. scope-creep agreement), with <code>total_amount_minor</code> adjustment. Emits <code>proj.milestone_added</code>. Invariant: new milestone count + amounts must satisfy §1 #8 sum.</li><li><strong>MUST</strong> track <code>engagement_in_overage_streak</code> for Retainer mode: count of consecutive periods with overage; SEV-3 alert at 3 (suggests retainer cap is too low for actual usage).</li><li><strong>MUST</strong> support per-engagement <code>retainer_holiday</code> window: operator can mark Dec 24 – Jan 2 as a holiday window where the retainer base is prorated (charged base × workdays_in_period / total_workdays_in_month). Stored in <code>retainer_holidays</code> table.</li><li><strong>MUST</strong> complete <code>rollup_invoiceable</code> within 500ms p95 for engagements with ≤ 1000 time entries in the period. Larger engagements emit SEV-3 warning + the call still completes.</li><li><strong>MUST</strong> support <code>?preview=true</code> flag on rollup endpoint: returns the rollup but does NOT persist <code>retainer_state</code> (used by operator UI for what-if scenarios).</li><li><strong>MUST</strong> validate currency consistency across all time entries in a T&amp;M period: if entries reference multiple currencies (e.g. mix of USD + VND rate cards), the rollup MUST emit separate InvoiceLines per currency with a <code>proj.billing_mixed_currency_period</code> SEV-2 warning.</li><li><strong>MUST</strong> include an <code>engagement_metadata</code> field in <code>InvoiceRollup</code>: <code>{client_name, engagement_name, billing_address, vat_number}</code> from engagement (TASK-PROJ-001) for downstream invoice rendering (TASK-INV-001).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why exactly three modes (DEC-281)?</strong> These are the industry-standard contract types. Hybrid models (e.g. \"fixed-fee with overage T&amp;M\") compose from these primitives via mode-change + proration. Adding a 4th mode invites combinatorial complexity; we ship the empirical 95%.</p>\n<p><strong>Why versioned mode changes (§1 #1, DEC-280)?</strong> Same logic as rate cards (TASK-PROJ-005): UPDATE corrupts historical billing. A client invoiced at \"T&amp;M\" in Q1 then \"Fixed-Fee\" in Q2 must show both modes in their respective periods.</p>\n<p><strong>Why milestone sum invariant (§1 #8)?</strong> Fixed-Fee clients pay a TOTAL; milestones are intermediate progress payments. If milestones sum ≠ total, the engagement either over-bills (sum &gt; total) or leaves money on the table (sum &lt; total). The invariant catches config errors early.</p>\n<p><strong>Why retainer rollover (§1 #4)?</strong> Clients hate \"use it or lose it\" — slow months should subsidise busy months. Configurable rollover (3 months default) balances client value with finance team's accrual closing. After rollover_months periods, unused credit drops (finite tracking).</p>\n<p><strong>Why proration on mode change (§1 #6)?</strong> Mid-period mode changes are common (small T&amp;M engagement converts to retainer once volume stabilises). Without proration, the client either over-pays (charged full retainer + T&amp;M hours for switching month) or under-pays (T&amp;M month suddenly billed as flat retainer).</p>\n<p><strong>Why InvoiceLine carries source_refs (§1 #5)?</strong> Auditors and clients ask \"what hours produced this invoice line?\" Each line references the underlying time-entries (T&amp;M), milestone (Fixed-Fee), or period (Retainer). Reverse-traceability built in.</p>\n<p><strong>Why mid-period proration (§1 #13)?</strong> Engagements convert modes mid-month often (small T&amp;M proves out, converts to Fixed-Fee). Charging full retainer for a 2-week period feels unfair; charging T&amp;M rates for a Fixed-Fee period feels wrong. Proration splits the difference correctly.</p>\n<p><strong>Why milestone cancellation + add (§1 #14, #15)?</strong> Real engagements scope-creep or descope. Cancellation refunds; addition extends. Both adjust total_amount_minor; both emit memory audits for finance reconciliation.</p>\n<p><strong>Why overage-streak tracking (§1 #16)?</strong> A retainer that consistently overages is mispriced; the operator should renegotiate. 3-period streak is the empirical signal that \"this is structural, not noise.\"</p>\n<p><strong>Why retainer holidays (§1 #17)?</strong> Tet, Christmas, summer breaks: retainer base prorated for partial-availability periods. Without holidays, clients pay full retainer for half-month coverage. Operator config flexibility wins.</p>\n<p><strong>Why 500ms p95 rollup budget (§1 #18)?</strong> Rollup is in the invoice-generation hot path; operators triggering monthly invoices for 100 engagements need it fast. 500ms × 100 = 50s total — acceptable. Larger engagements get a warning but still complete.</p>\n<p><strong>Why preview-only mode (§1 #19)?</strong> What-if analysis: \"if we switched to retainer next month, what would the invoice look like?\" Without preview, operators have to do destructive state writes to find out.</p>\n<p><strong>Why mixed-currency split (§1 #20)?</strong> A single InvoiceLine can't carry multiple currencies; mixed-currency periods must split into per-currency lines. SEV-2 warning surfaces the rare case (operators may have set up multi-currency rate cards inadvertently).</p>\n<p><strong>Why engagement_metadata in rollup (§1 #21)?</strong> Downstream invoice rendering needs client name + billing address + VAT number. Pre-fetched in the rollup so TASK-INV-001 doesn't re-query engagement.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/proj-sync/migrations/0007_billing_modes.sql\n\nCREATE TABLE billing_modes (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    engagement_id   UUID NOT NULL REFERENCES engagements(id),\n    mode            TEXT NOT NULL CHECK (mode IN ('time_and_materials','fixed_fee','retainer')),\n    config          JSONB NOT NULL,\n    effective_from  DATE NOT NULL,\n    effective_to    DATE,\n    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    created_by_subject_id UUID NOT NULL,\n    tenant_id       UUID NOT NULL,\n    UNIQUE (engagement_id, effective_from)\n);\nCREATE UNIQUE INDEX uniq_active_billing_mode\n    ON billing_modes (engagement_id) WHERE effective_to IS NULL;\nCREATE INDEX idx_billing_modes_lookup\n    ON billing_modes (engagement_id, effective_from, effective_to);\n\nCREATE TABLE retainer_state (\n    engagement_id            UUID NOT NULL REFERENCES engagements(id),\n    period_year_month        TEXT NOT NULL,  -- \"2026-05\"\n    consumed_minutes         INT NOT NULL DEFAULT 0,\n    rollover_credit_minutes  INT NOT NULL DEFAULT 0,\n    base_amount_invoiced     BIGINT NOT NULL DEFAULT 0,\n    overage_minutes_invoiced INT NOT NULL DEFAULT 0,\n    computed_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    tenant_id                UUID NOT NULL,\n    PRIMARY KEY (engagement_id, period_year_month)\n);\n\nALTER TABLE billing_modes  ENABLE ROW LEVEL SECURITY;\nALTER TABLE retainer_state ENABLE ROW LEVEL SECURITY;\nCREATE POLICY billing_modes_tenant_isolation ON billing_modes\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);\nCREATE POLICY retainer_state_tenant_isolation ON retainer_state\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Rust API</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/billing_mode/mod.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum BillingMode { TimeAndMaterials, FixedFee, Retainer }\n\n#[derive(Clone, Debug, Serialize, Deserialize)]\n#[serde(tag = \"mode\", rename_all = \"snake_case\")]\npub enum ModeConfig {\n    TimeAndMaterials { minimum_billable_increment_minutes: i32, weekly_cap_hours: Option&lt;i32&gt; },\n    FixedFee { total_amount_minor: i64, currency: crate::rate_card::Currency, milestones: Vec&lt;Milestone&gt; },\n    Retainer { monthly_cap_minutes: i32, currency: crate::rate_card::Currency,\n               base_amount_minor: i64, overage_rate_minor_per_hour: i64, rollover_months: u8 },\n}\n\n#[derive(Clone, Debug, Serialize, Deserialize)]\npub struct Milestone {\n    pub id:                 String,\n    pub name:               String,\n    pub target_pct_complete: f32,\n    pub target_date:        chrono::NaiveDate,\n    pub amount_minor:       i64,\n}\n\n#[derive(Clone, Debug, Serialize)]\npub struct InvoiceRollup {\n    pub engagement_id: uuid::Uuid,\n    pub period_start:  chrono::NaiveDate,\n    pub period_end:    chrono::NaiveDate,\n    pub mode:          BillingMode,\n    pub lines:         Vec&lt;InvoiceLine&gt;,\n    pub total_minor:   i64,\n    pub currency:      crate::rate_card::Currency,\n}\n\n#[derive(Clone, Debug, Serialize)]\npub struct InvoiceLine {\n    pub line_kind:        LineKind,\n    pub description:      String,\n    pub quantity:         f64,\n    pub unit_rate_minor:  i64,\n    pub amount_minor:     i64,\n    pub source_refs:      Vec&lt;SourceRef&gt;,\n}\n\n#[derive(Clone, Copy, Debug, Serialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum LineKind { Hours, Milestone, RetainerBase, RetainerOverage }\n\n#[derive(Clone, Debug, Serialize)]\n#[serde(tag = \"kind\", rename_all = \"snake_case\")]\npub enum SourceRef {\n    TimeEntry { id: uuid::Uuid },\n    Milestone { id: String },\n    Period    { year_month: String },\n}</code></pre>\n<h3>Rollup orchestrator</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/billing_mode/rollup.rs\npub async fn rollup_invoiceable(\n    pool: &amp;sqlx::PgPool,\n    engagement_id: uuid::Uuid,\n    period_start: chrono::NaiveDate,\n    period_end: chrono::NaiveDate,\n) -&gt; anyhow::Result&lt;InvoiceRollup&gt; {\n    let mode_row = active_mode_at(pool, engagement_id, period_start).await?;\n    let config: ModeConfig = serde_json::from_value(mode_row.config)?;\n\n    let lines = match config {\n        ModeConfig::TimeAndMaterials { .. } =&gt; {\n            t_and_m::compute_lines(pool, engagement_id, period_start, period_end).await?\n        }\n        ModeConfig::FixedFee { milestones, currency, .. } =&gt; {\n            fixed_fee::compute_lines(pool, engagement_id, &amp;milestones, currency, period_start, period_end).await?\n        }\n        ModeConfig::Retainer { monthly_cap_minutes, currency, base_amount_minor, overage_rate_minor_per_hour, rollover_months } =&gt; {\n            retainer::compute_lines(pool, engagement_id, monthly_cap_minutes, currency,\n                                    base_amount_minor, overage_rate_minor_per_hour, rollover_months,\n                                    period_start, period_end).await?\n        }\n    };\n    let total: i64 = lines.iter().map(|l| l.amount_minor).sum();\n    let currency = lines.first().map(|l| l.source_refs.first()).flatten().map(|_| determine_currency(&amp;config)).unwrap_or(crate::rate_card::Currency::VND);\n    Ok(InvoiceRollup { engagement_id, period_start, period_end,\n                       mode: mode_row.mode, lines, total_minor: total, currency })\n}</code></pre>\n<h3>Retainer overage compute</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/billing_mode/retainer.rs\npub async fn compute_lines(\n    pool: &amp;sqlx::PgPool,\n    eng: uuid::Uuid,\n    cap_min: i32,\n    currency: crate::rate_card::Currency,\n    base_minor: i64,\n    over_rate_per_hour_minor: i64,\n    rollover_months: u8,\n    period_start: chrono::NaiveDate,\n    _period_end: chrono::NaiveDate,\n) -&gt; anyhow::Result&lt;Vec&lt;InvoiceLine&gt;&gt; {\n    let ym = period_start.format(\"%Y-%m\").to_string();\n    let consumed: i32 = sqlx::query_scalar!(\n        \"SELECT COALESCE(SUM(duration_minutes), 0)::int4\n         FROM time_entries WHERE engagement_id = $1\n           AND start_at &gt;= $2 AND start_at &lt; $3\n           AND billable_snapshot = true\",\n        eng, period_start, period_start + chrono::Duration::days(31)\n    ).fetch_one(pool).await?;\n\n    // Carry-over from prior periods, up to rollover_months back\n    let credit = compute_rollover_credit(pool, eng, period_start, rollover_months, cap_min).await?;\n    let effective_cap = cap_min + credit;\n    let overage_min = (consumed - effective_cap).max(0);\n    let overage_amount = (overage_min as i64 * over_rate_per_hour_minor) / 60;\n\n    // Persist retainer_state for next period's rollover compute\n    sqlx::query!(\n        \"INSERT INTO retainer_state (engagement_id, period_year_month, consumed_minutes, rollover_credit_minutes,\n                                      base_amount_invoiced, overage_minutes_invoiced, tenant_id)\n         VALUES ($1,$2,$3,$4,$5,$6, current_setting('app.tenant_id')::uuid)\n         ON CONFLICT (engagement_id, period_year_month) DO UPDATE\n           SET consumed_minutes = EXCLUDED.consumed_minutes,\n               rollover_credit_minutes = EXCLUDED.rollover_credit_minutes,\n               base_amount_invoiced = EXCLUDED.base_amount_invoiced,\n               overage_minutes_invoiced = EXCLUDED.overage_minutes_invoiced,\n               computed_at = NOW()\",\n        eng, ym, consumed, credit, base_minor, overage_min\n    ).execute(pool).await?;\n\n    let mut lines = vec![InvoiceLine {\n        line_kind: LineKind::RetainerBase,\n        description: format!(\"Retainer fee — {ym}\"),\n        quantity: 1.0,\n        unit_rate_minor: base_minor,\n        amount_minor: base_minor,\n        source_refs: vec![SourceRef::Period { year_month: ym.clone() }],\n    }];\n\n    if overage_min &gt; 0 {\n        lines.push(InvoiceLine {\n            line_kind: LineKind::RetainerOverage,\n            description: format!(\"Retainer overage — {:.2} hours @ {}/h\",\n                                 overage_min as f64 / 60.0,\n                                 format_minor(over_rate_per_hour_minor, currency)),\n            quantity: overage_min as f64 / 60.0,\n            unit_rate_minor: over_rate_per_hour_minor,\n            amount_minor: overage_amount,\n            source_refs: vec![SourceRef::Period { year_month: ym.clone() }],\n        });\n        emit_memory_row(\"proj.retainer_overage_emitted\", serde_json::json!({\n            \"engagement_id\": eng, \"period\": ym, \"overage_minutes\": overage_min,\n            \"overage_amount_minor\": overage_amount,\n        })).await;\n    }\n    Ok(lines)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>T&amp;M rollup sums billable hours</strong> — 40 billable hours @ 500_000 VND/h → 1 line × 20_000_000 VND.</li><li><strong>T&amp;M ignores non-billable</strong> — 40h billable + 10h non-billable → only 40h billed.</li><li><strong>Fixed-Fee milestones invariant</strong> — config with milestones summing ≠ total → 422 at insert.</li><li><strong>Fixed-Fee rollup invoices completed milestones</strong> — milestone X done in period → its amount_minor in InvoiceLine; not-done milestone absent.</li><li><strong>Retainer base always emitted</strong> — period with 0 consumed → 1 line × base_amount_minor.</li><li><strong>Retainer overage</strong> — consumed = 60h, cap = 40h → overage = 20h × overage_rate.</li><li><strong>Retainer rollover credit</strong> — prior period unused 10h; cap 40h → effective cap = 50h; consumed 45h → no overage.</li><li><strong>Retainer rollover expires after N months</strong> — unused credit &gt; rollover_months old → drops.</li><li><strong>Mode change at period midpoint prorates</strong> — T&amp;M Jan 1-15, FixedFee Jan 16-31 → 2 InvoiceRollups OR 1 with proration markers.</li><li><strong>Two active modes forbidden</strong> — second mode insert with overlapping effective dates → 409.</li><li><strong>memory audit on mode change</strong> — POST new mode → <code>proj.billing_mode_changed</code> row.</li><li><strong>memory audit on milestone invoiced</strong> — milestone done → <code>proj.milestone_invoiced</code> row.</li><li><strong>memory audit on retainer overage</strong> — overage &gt; 0 → <code>proj.retainer_overage_emitted</code>.</li><li><strong>InvoiceLine.source_refs traceable</strong> — T&amp;M line carries time_entry IDs; FixedFee carries milestone ID; Retainer carries period.</li><li><strong>CLI set-mode idempotent</strong> — same Idempotency-Key + same body → returns prior.</li><li><strong>OTel metric <code>proj_retainer_overage_total</code></strong> — overage emitted → counter increments.</li><li><strong>RLS tenant isolation</strong> — tenant A's retainer_state invisible to tenant B.</li><li><strong>Mid-period mode-change prorates</strong> — period Jan 1-31, mode changed Jan 15 from T&amp;M to FixedFee → InvoiceRollup <code>prorated: true</code> with sub-period lines (AC for §1 #13).</li><li><strong>Milestone cancellation redistributes</strong> — cancel milestone (50M) → remaining milestones' amounts adjusted OR total reduced; <code>proj.milestone_cancelled</code> audit (AC for §1 #14).</li><li><strong>Milestone addition extends total</strong> — add 25M milestone → total_amount_minor += 25M; invariant satisfied; <code>proj.milestone_added</code> audit (AC for §1 #15).</li><li><strong>Overage streak alarm at 3</strong> — 3 consecutive overage periods → SEV-3 audit <code>proj.retainer_overage_streak</code> (AC for §1 #16).</li><li><strong>Retainer holiday prorates base</strong> — Dec holiday window covers 7 of 22 workdays → base × 15/22 in line (AC for §1 #17).</li><li><strong>Rollup latency p95 &lt; 500ms</strong> — 1000 time entries → p95 &lt; 500ms; &gt; 1000 → SEV-3 warning (AC for §1 #18).</li><li><strong>Preview does not persist retainer_state</strong> — POST /rollup?preview=true → retainer_state unchanged after call (AC for §1 #19).</li><li><strong>Mixed currency in T&amp;M period</strong> — entries in USD + VND → 2 InvoiceLines (one per currency) + SEV-2 <code>proj.billing_mixed_currency_period</code> (AC for §1 #20).</li><li><strong>engagement_metadata populated</strong> — rollup response includes client_name + billing_address + vat_number (AC for §1 #21).</li><li><strong>CLI set-mode validates milestone sum</strong> — bad config → CLI exits 1 with clear error (AC for §1 #8 + #15).</li><li><strong>Audit <code>proj.billing_mode_set</code> on first mode</strong> — initial mode → memory row with mode + config (AC for §1 #7).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn t_and_m_rollup_excludes_non_billable() {\n    let env = TestEnv::new().await;\n    let eng = env.bootstrap_t_and_m().await;\n    env.add_time_entry(eng, hours(40), billable=true).await;\n    env.add_time_entry(eng, hours(10), billable=false).await;\n    let r = rollup_invoiceable(&amp;env.pool, eng, \"2026-05-01\".parse().unwrap(), \"2026-06-01\".parse().unwrap()).await.unwrap();\n    let total_hours: f64 = r.lines.iter().filter(|l| l.line_kind == LineKind::Hours).map(|l| l.quantity).sum();\n    assert!((total_hours - 40.0).abs() &lt; 0.01);\n}\n\n#[tokio::test]\nasync fn fixed_fee_milestone_sum_invariant() {\n    let env = TestEnv::new().await;\n    let eng = env.create_engagement().await;\n    let bad_config = ModeConfig::FixedFee {\n        total_amount_minor: 100_000_000, currency: Currency::VND,\n        milestones: vec![\n            milestone(\"m1\", 50_000_000), milestone(\"m2\", 40_000_000),  // sums to 90M, not 100M\n        ],\n    };\n    let err = set_mode(&amp;env.pool, eng, bad_config).await.unwrap_err();\n    assert!(matches!(err, BillingError::MilestoneSumMismatch { .. }));\n}\n\n#[tokio::test]\nasync fn retainer_overage_emitted_when_consumed_exceeds_cap() {\n    let env = TestEnv::new().await;\n    let eng = env.bootstrap_retainer(cap_minutes=2400, base=30_000_000, over_rate_per_h=750_000).await;\n    env.add_time_entry(eng, hours(50)).await;  // 50h = 3000 min; cap = 2400; overage = 600 min = 10h\n    let r = rollup_invoiceable(&amp;env.pool, eng, \"2026-05-01\".parse().unwrap(), \"2026-06-01\".parse().unwrap()).await.unwrap();\n    let overage = r.lines.iter().find(|l| l.line_kind == LineKind::RetainerOverage).unwrap();\n    assert!((overage.quantity - 10.0).abs() &lt; 0.01);\n    assert_eq!(overage.amount_minor, 7_500_000);  // 10h × 750k\n}\n\n#[tokio::test]\nasync fn retainer_rollover_credit_applied() {\n    let env = TestEnv::new().await;\n    let eng = env.bootstrap_retainer(cap_minutes=2400, base=30_000_000, over_rate_per_h=750_000).await;\n    env.set_retainer_state(eng, \"2026-04\", consumed=1800, rollover_credit=0).await;  // 10h unused\n    env.add_time_entry_in_period(eng, hours(45), \"2026-05\").await;\n    let r = rollup_invoiceable(&amp;env.pool, eng, \"2026-05-01\".parse().unwrap(), \"2026-06-01\".parse().unwrap()).await.unwrap();\n    let overage_line = r.lines.iter().find(|l| l.line_kind == LineKind::RetainerOverage);\n    // Effective cap = 40h + 10h rollover = 50h; consumed 45h → no overage\n    assert!(overage_line.is_none());\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + DB schema above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-005</strong> — rate cards (T&amp;M uses lookup_at).</li><li><strong>TASK-PROJ-006</strong> — billable cascade (snapshot consumed in T&amp;M rollup).</li><li><strong>TASK-INV-001 (downstream)</strong> — invoice generation consumes <code>InvoiceRollup</code>.</li><li><strong>TASK-TIME-005</strong> — time-entry snapshot field.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.retainer_overage_emitted\",\n  \"payload\": {\n    \"engagement_id\":         \"eng-...\",\n    \"period\":                \"2026-05\",\n    \"overage_minutes\":       600,\n    \"overage_amount_minor\":  7500000\n  }\n}</code></pre>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.milestone_invoiced\",\n  \"payload\": {\n    \"engagement_id\":   \"eng-...\",\n    \"milestone_id\":    \"m2\",\n    \"milestone_name\":  \"Phase 2 — Backend complete\",\n    \"amount_minor\":    25000000,\n    \"completed_at\":    \"2026-05-15\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Hybrid mode (Fixed-Fee + T&amp;M overage) — slice 4+; compose from mode-change.</li><li>Multi-currency within one engagement — slice 4+; current MVP one currency per active mode.</li><li>Variable retainer caps (Q1 high, Q2 low) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Milestone sum mismatch</td><td>invariant check at insert</td><td>422</td><td>Caller adjusts amounts</td></tr><tr><td>Two active modes</td><td>partial-unique constraint</td><td>409 on insert</td><td>Caller supersedes prior first</td></tr><tr><td>No mode at period</td><td>active_mode_at returns None</td><td>404</td><td>Operator sets mode</td></tr><tr><td>Retainer rollover compute fails</td><td>sqlx Err</td><td>500</td><td>Operator investigates</td></tr><tr><td>Time entries in period missing currency</td><td>rollup defaults to engagement's currency</td><td>Mismatch warning</td><td>Caller validates currency consistency</td></tr><tr><td>Overage rate = 0 with cap exceeded</td><td>overage_amount = 0; line still emitted</td><td>client sees \"0 VND overage\"</td><td>By design</td></tr><tr><td>Retainer base = 0</td><td>line emitted with amount 0</td><td>client sees 0-amount line</td><td>Edge case for pro-bono retainer</td></tr><tr><td>Mode change without proration</td><td>period straddles change</td><td>rollup uses mode-at-period-start</td><td>Slice 4+ proration</td></tr><tr><td>memory audit fails</td><td>rollup still returns; audit lost</td><td>sev-2 alarm</td><td>Operator restores</td></tr><tr><td>Concurrent rollup calls</td><td>retainer_state ON CONFLICT DO UPDATE</td><td>last writer wins for state; rollup result deterministic</td><td>None</td></tr><tr><td>Invoice already generated for period</td><td>duplicate invoice attempt</td><td>caller's TASK-INV-001 dedup catches</td><td>None at this task</td></tr><tr><td>RLS bypass</td><td>RLS policy</td><td>0 rows</td><td>None</td></tr><tr><td>Config JSON malformed</td><td>serde Err</td><td>422</td><td>Caller fixes</td></tr><tr><td>Milestone has target_pct_complete &gt; 100</td><td>should be rejected at insert</td><td>422</td><td>Caller fixes</td></tr><tr><td>Period straddles mode change without proration support</td><td>proration applied per §1 #13</td><td>sub-period lines</td><td>None</td></tr><tr><td>Milestone cancelled during invoiced period</td><td>refund line emitted</td><td>downstream credit-note</td><td>Manual reconciliation</td></tr><tr><td>Milestone added with <code>amount &gt; remaining_unbilled</code></td><td>invariant check</td><td>422</td><td>Caller</td></tr><tr><td>Retainer overage streak = 0 then 1 then 0</td><td>streak resets on no-overage period</td><td>None</td><td>None</td></tr><tr><td>Holiday window overlaps period boundary</td><td>prorate based on workdays in period × in-window</td><td>None</td><td>None</td></tr><tr><td>Rollup latency &gt; 500ms but &lt; 5s</td><td>SEV-3 warning; call completes</td><td>None</td><td>Operator</td></tr><tr><td>Rollup latency &gt; 5s</td><td>timeout; 504</td><td>None</td><td>Operator scales</td></tr><tr><td>Preview mode with concurrent real rollup</td><td>preview reads state but doesn't write</td><td>safe</td><td>None</td></tr><tr><td>Mixed-currency T&amp;M without warning enabled</td><td>dedup warnings at OBS</td><td>None</td><td>None</td></tr><tr><td>engagement_metadata fetch fails</td><td>rollup degraded (metadata null)</td><td>downstream invoice has placeholder</td><td>Operator</td></tr><tr><td>Holiday window in past</td><td>applied at lookup time</td><td>as-is</td><td>None</td></tr><tr><td>Tax/VAT lookup deferred to TASK-INV-001</td><td>rollup excludes taxes</td><td>None</td><td>By design</td></tr><tr><td>Two mode changes in one period</td><td>proration handles N segments</td><td>sub-period lines per change</td><td>None</td></tr><tr><td>Mode change effective in future</td><td>active_mode_at returns prior; new mode visible after effective_from</td><td>None</td><td>None</td></tr><tr><td>Retainer base prorated for partial-month engagement (new client mid-month)</td><td>proration applied</td><td>None</td><td>None</td></tr><tr><td>Retainer rollover with cap_change between periods</td><td>use cap-at-period for compute</td><td>None</td><td>None</td></tr><tr><td>Cancel milestone after invoice already issued</td><td>requires manual credit-note</td><td>SEV-2</td><td>Operator</td></tr><tr><td>Add milestone retroactively (effective in past)</td><td>timestamp validates; rejected</td><td>422</td><td>Caller</td></tr><tr><td>Currency change in mode supersession</td><td>new mode's currency; warning emitted</td><td>None</td><td>None</td></tr><tr><td>Engagement archived during rollup</td><td>active_mode_at returns the archived-period mode</td><td>uses historical mode</td><td>None</td></tr><tr><td>Concurrent rollup + mode change</td><td>mode change waits for in-flight rollups</td><td>brief lock</td><td>None</td></tr><tr><td>Time entry without billable_snapshot (pre-TASK-PROJ-006)</td><td>excluded from rollup</td><td>SEV-3 warning if found</td><td>Operator backfills</td></tr><tr><td>Period start == period end</td><td>empty rollup</td><td>None</td><td>None</td></tr><tr><td>Period end before start</td><td>422</td><td>None</td><td>Caller</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The <code>config JSONB</code> column carries mode-specific structure; serde tagged enum makes (de)serialisation type-safe.</li><li>Retainer state is materialised per period (one row per engagement per month) — sled-style per-doc but in Postgres for transactional reads with billing rollups.</li><li>Rollover credit compute walks back N months (configured per engagement); cap memoisation in retainer_state avoids recomputation.</li><li>T&amp;M rollup groups time entries by <code>(role, member_id)</code> to produce one InvoiceLine per cell; aggregation per TASK-INV-001 spec.</li><li>Fixed-Fee milestones tied to issue IDs (each milestone has 1+ issues that must be \"done\" per TASK-PROJ-004 lifecycle); status change triggers rollup re-computation on next invoice.</li><li>The <code>proj.retainer_rollover_carry_forward</code> audit row is emitted by the next period's rollup, not the current — bookkeeping aid for finance close.</li><li><code>format_minor(amount, currency)</code> is a helper from <code>cyberos-vn-common</code> that renders correctly per currency decimals (VND no decimals; USD 2).</li><li>Mid-period proration splits the period at every mode-change boundary; for N changes in one period, rollup is computed for N+1 sub-periods. The <code>prorated: true</code> flag tells the invoice renderer to show sub-period detail.</li><li>Milestone cancellation has two refund modes: (a) refund-to-client (issues credit note via TASK-INV-001); (b) redistribute remaining milestone amounts (e.g. cancel m3 worth 20M, add 20M to m4-m5 pro-rata). Default is (a); operator chooses at cancellation time.</li><li>Milestone addition can occur post-contract (scope-creep agreement); the addition adjusts <code>total_amount_minor</code> and emits an audit row with <code>delta_amount</code> for finance reconciliation.</li><li>Overage-streak tracking is a per-engagement counter persisted in <code>retainer_state</code>; a no-overage period resets it.</li><li>Retainer holidays are inclusive intervals; the prorate formula uses workdays-in-period (Mon-Fri) excluding holidays.</li><li>The 500ms p95 budget covers: 1 mode lookup + N time-entry queries + 1 milestone scan + retainer_state read/write. Typical engagement (100 entries) is ~50ms.</li><li>Preview mode short-circuits the <code>INSERT ... ON CONFLICT</code> on <code>retainer_state</code>; everything else runs identically. Guarantees preview matches commit (no path divergence).</li><li>Mixed-currency T&amp;M periods are rare but legal; we emit separate lines + SEV-2 warning to surface the case (operators usually fix it by normalising rate-card currency).</li><li>engagement_metadata is fetched in parallel with the rollup compute via <code>tokio::join!</code> to avoid serial latency.</li><li>We considered keeping <code>total_amount_minor</code> mutable on Fixed-Fee config but rejected: every change generates a new mode row (supersession) to preserve audit history. UI sugar can hide the supersession from operators.</li><li>Rollover credit is per-period (one row per month); we don't materialise the full credit-history graph — just the immediate prior period's surplus.</li><li>The 3-month default rollover was chosen because: (a) most contracts allow 90-day rollover; (b) longer rollover bloats retainer_state queries; (c) finance teams want quarterly close.</li><li>We chose Postgres JSONB over a separate per-mode table because the three modes have disjoint configurations; JOIN gymnastics weren't worth the schema purity.</li><li>T&amp;M weekly_cap_hours (optional config) lets operators cap a contractor's billable hours per week (anti-fraud); cap exceeded emits SEV-3 + still rolls up but with a warning line.</li><li>We use <code>chrono::NaiveDate</code> for period boundaries (date-only); time-zone is the engagement's contracted billing timezone (tracked separately).</li><li>Retainer base prorating for partial-month new clients: if engagement starts mid-month, base is <code>base × days_remaining / days_in_month</code>.</li><li>Mode change effective in the future is allowed (e.g. scheduled Q1 → Q2 transition); rollup uses <code>active_mode_at</code> for the period start.</li><li>Cancellation of an already-invoiced milestone requires manual credit-note via TASK-INV-001 — automated handling deferred to slice 4+.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-007.</em></p>";
