window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-120"]="<h1>TASK-IMP-120 - the index moves without the truth, in both directions</h1>\n<h2>Summary</h2>\n<p>Make <code>backlog-mutate flip</code> REFUSE unless the task's frontmatter already carries the target status. The frontmatter is the record of truth and BACKLOG.md is its index; nothing binds the two writes, so they diverge silently. Twice on 2026-07-17, in opposite directions, from two different actors.</p>\n<h2>Problem</h2>\n<p><code>STATUS-REFERENCE.md</code> §1 is unambiguous: frontmatter IS the record of truth, BACKLOG.md is only its index. <code>backlog-mutate</code> executes the index write with optimistic concurrency, a pre-image check, a 3-line footprint ceiling and a full retally. The frontmatter write is a separate hand edit with no mechanism at all.</p>\n<p>Two instances, one day, opposite directions:</p>\n<table><thead><tr><th>when</th><th>actor</th><th>what moved</th><th>what did not</th></tr></thead><tbody><tr><td>TASK-IMP-116</td><td>me</td><td>the index, twice (<code>reviewing -&gt; ready_to_test -&gt; testing</code>)</td><td>the frontmatter, which still said <code>reviewing</code></td></tr><tr><td>TASK-IMP-028</td><td>a swarm sub-agent</td><td>the frontmatter (<code>draft -&gt; duplicate</code>, per 110 §1.7)</td><td>the index, which still said <code>draft</code></td></tr></tbody></table>\n<p>Neither is carelessness. 116's own coverage gate caught the first by comparing Totals against an independent frontmatter count - 544 index rows, 543 specs. The second was caught the same way, at the batch close. Both times the reconcile was a manual step someone happened to run.</p>\n<p>The asymmetry is the tell: the index write is a hardened tool and the truth write is a <code>sed</code>. We built the mechanism on the derived artefact and left the authoritative one bare.</p>\n<p>TASK-IMP-116's goal file already names this gap and the candidate fix. This task is that fix.</p>\n<h2>Proposed Solution</h2>\n<p><code>flip &lt;id&gt; &lt;from&gt; &lt;to&gt;</code> reads the task's frontmatter FIRST. If it does not already say <code>&lt;to&gt;</code>, the flip REFUSES with exit 6 (the existing pre-image refusal code) and names both values. The truth becomes the precondition rather than the afterthought, and the index can only ever catch up to it - never lead it.</p>\n<p>This makes the order structural instead of advisory: write the truth, then run the tool. A caller who does it backwards gets a refusal, not a divergence.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Have <code>flip</code> WRITE the frontmatter too. Rejected: then one tool owns both the truth and its index, and a bug in it corrupts the record with no second opinion. The tool's whole value is that it is narrow and refuses; widening it to write specs makes it the thing it guards against.</li><li>A reconcile step at each flip. Rejected: <code>task-reconcile.mjs</code> already exists and already finds this - it just is not required, so it is not run. Another optional check is another thing to skip.</li><li>Lint it. Rejected: the divergence is between two files at one instant, not a property of either.</li><li>Leave it; the gates catch it. Rejected: both instances were caught by a manual reconcile someone chose to run. That is luck with a good habit attached.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Replaying 116's exact sequence (index flipped, frontmatter untouched) REFUSES at the first flip instead of succeeding twice.</li><li>Replaying 028's (frontmatter flipped, index untouched) leaves the index refusable, so the divergence cannot be committed.</li><li><code>insert</code> keeps its current behaviour: it creates a row for a spec that already exists, so the truth is already there by construction. No new refusal there.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Claude (Fable 5), 2026-07-17 hardening run, via create-tasks.</li><li>Scope: spec drafted by the agent. Both instances in the table are the agent's own - one written directly, one by a sub-agent it dispatched - and both were found by reconciling rather than by any gate. The fix was already named in TASK-IMP-116's goal file before the second instance occurred, which is why this is a task rather than a discovery.</li><li>Human review: @stephencheng recorded the decision to task it at the batch close gate.</li></ul>\n<h2>Dependencies</h2>\n<p>None. <code>backlog-mutate</code> already parses frontmatter for the insert path and already has exit 6 for pre-image refusals. This adds a precondition, not a mechanism.</p>\n<h2>Scope</h2>\n<p>In scope: the <code>flip</code> precondition in <code>backlog-mutate.mjs</code>, its suite arms, and the ordering contract in <code>backlog-state-update-author/SKILL.md</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Making <code>flip</code> write frontmatter. See Alternatives - that is the failure mode, not the fix.</li><li>Any change to <code>insert</code>. A row is inserted for a spec that exists; the truth precedes it already.</li><li>The status page. Group A already binds the page to the cell; this binds the cell to the truth.</li><li>Retroactively reconciling the corpus. It agrees today, on all eight live statuses. Keeping it that way is this task; proving it stays that way is TASK-IMP-109's territory.</li></ul>\n<h2>1. Clauses</h2>\n<p>1.1 <code>flip &lt;id&gt; &lt;from&gt; &lt;to&gt;</code> MUST read <code>&lt;id&gt;</code>'s spec frontmatter and REFUSE with exit 6 unless <code>status</code> already equals <code>&lt;to&gt;</code>, naming both the frontmatter value and the requested target. Test: <code>t14_flip_refuses_when_truth_disagrees</code></p>\n<p>1.2 When the frontmatter DOES equal <code>&lt;to&gt;</code>, flip proceeds exactly as today - same pre-image checks, same 3-line footprint, same retally. This adds a precondition and changes nothing else. Test: <code>t15_flip_proceeds_when_truth_agrees</code></p>\n<p>1.3 A spec that cannot be found or read MUST refuse, not proceed. An unreadable truth is not a matching truth. Test: <code>t16_unreadable_spec_refuses</code></p>\n<p>1.4 <code>insert</code> MUST be unaffected. Test: <code>t17_insert_unchanged</code></p>\n<p>1.5 <code>backlog-state-update-author/SKILL.md</code> MUST state the ordering as a contract: write the truth, then the index; the tool enforces it. Both vendored copies. Test: <code>t18_skill_states_the_order</code></p>\n<h2>3. Edge case matrix</h2>\n<table><thead><tr><th>#</th><th>Category</th><th>Trigger</th><th>Expected</th><th>Test</th></tr></thead><tbody><tr><td>1</td><td>NULL/EMPTY</td><td>spec has no <code>status</code> field</td><td>refuse - absent is not agreement</td><td>t16</td></tr><tr><td>2</td><td>NULL/EMPTY</td><td>spec file missing entirely</td><td>refuse, name the path</td><td>t16</td></tr><tr><td>3</td><td>BOUNDS</td><td>frontmatter equals <code>&lt;to&gt;</code> exactly</td><td>proceed</td><td>t15</td></tr><tr><td>4</td><td>BOUNDS</td><td>frontmatter equals <code>&lt;from&gt;</code> (caller ran it backwards)</td><td>refuse - this is the whole defect</td><td>t14</td></tr><tr><td>5</td><td>MALFORMED</td><td><code>status: done  # comment</code> (FM-001, live in 501 specs)</td><td>trailing comment stripped before compare</td><td>t14</td></tr><tr><td>6</td><td>MALFORMED</td><td>two <code>status:</code> lines</td><td>refuse - ambiguous truth is not truth</td><td>t16</td></tr><tr><td>7</td><td>MALFORMED</td><td>status value with trailing whitespace</td><td>trimmed, compares equal</td><td>t15</td></tr><tr><td>8</td><td>CONCURRENT</td><td>frontmatter changes between read and write</td><td>existing row pre-image still catches it</td><td>t15</td></tr><tr><td>9</td><td>SECURITY</td><td>id resolves to a spec outside docs/tasks</td><td>refuse - <code>relUnderRoot</code>, as everywhere else</td><td>t16</td></tr><tr><td>10</td><td>DEGRADATION</td><td>corpus has duplicate ids for <code>&lt;id&gt;</code></td><td>refuse - existing duplicate-row refusal path</td><td>t16</td></tr><tr><td>11</td><td>DEGRADATION</td><td>a status not in STATUS_ORDER</td><td>refuse before touching the file</td><td>t14</td></tr></tbody></table>\n<h2>4. Out of scope / non-goals</h2>\n<p>See \"## Scope -&gt; ### Out of scope / Non-Goals\" above.</p>\n<h2>Acceptance criteria</h2>\n<ul><li>AC1 (traces_to #1.1): flip refuses, exit 6, naming both values. Test: <code>t14_flip_refuses_when_truth_disagrees</code>.</li><li>AC2 (traces_to #1.2): flip proceeds unchanged when the truth agrees. Test: <code>t15_flip_proceeds_when_truth_agrees</code>.</li><li>AC3 (traces_to #1.3): unreadable or ambiguous truth refuses. Test: <code>t16_unreadable_spec_refuses</code>.</li><li>AC4 (traces_to #1.4): insert unaffected. Test: <code>t17_insert_unchanged</code>.</li><li>AC5 (traces_to #1.5): the skill states the ordering contract, both copies. Test: <code>t18_skill_states_the_order</code>.</li><li>AC6: replaying TASK-IMP-116's sequence (index flipped, frontmatter untouched) refuses at the FIRST flip. A fix that cannot stop the case that motivated it is decoration. Test: <code>t19_replays_the_116_divergence</code>.</li></ul>";
