window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-106"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CUO supervisor <strong>MUST</strong> ship 5 workflow Handler subclasses at <code>modules/cuo/cuo/core/handlers/</code> dispatched from workflow <code>pattern:</code> frontmatter, with 8 memory audit kinds, and updated workflow YAML in the 9 affected catalog workflows.</p>\n<ol><li><strong>MUST</strong> validate <code>workflow_pattern</code> against closed enum per DEC-2381 (cardinality 6, default <code>linear</code>).</li></ol>\n<ol><li><strong>MUST</strong> dispatch in <code>dispatch.py::pick_handler(workflow)</code> per DEC-2387:</li></ol>\n<ul><li>Read <code>workflow.frontmatter.pattern</code> (default <code>linear</code>)</li><li>Return matching Handler subclass instance</li><li>Linear pattern → existing <code>execute_chain()</code> (unchanged path)</li><li>All others → new Handler subclass</li></ul>\n<ol><li><strong>MUST</strong> implement <code>TimeCriticalHandler</code> per DEC-2382:</li></ol>\n<ul><li>Bypass any queueing/batching/work-stealing</li><li>Read <code>workflow.frontmatter.sla_minutes</code></li><li>Start timer, walk chain</li><li>If <code>total_duration_ms &gt; sla_minutes * 60 * 1000</code>: emit <code>cuo.time_critical_sla_breach</code> memory row with <code>extra.breach_severity = (actual - sla) / sla</code></li><li>Affected workflows: <code>chief-privacy-officer/breach-response-cycle</code> (sla_minutes: 240 = 4h), <code>chief-communications-officer/per-crisis-response</code> (sla_minutes: 120 = 2h), <code>chief-trust-officer/per-trust-incident-update</code> (sla_minutes: 240)</li></ul>\n<ol><li><strong>MUST</strong> implement <code>PerInstanceHandler</code> per DEC-2383:</li></ol>\n<ul><li>Read <code>workflow.frontmatter.instance_descriptor</code> (list of dicts)</li><li>For each instance: invoke <code>execute_chain()</code> with <code>inputs.merged(instance)</code></li><li>Collect ChainResults in list; build fan-in summary <code>ChainResult</code> with <code>outcome=\"COMPLETED_BATCH\"</code>, <code>per_instance: list[ChainResult]</code></li><li>Emit one <code>cuo.per_instance_iteration</code> row per instance + one <code>cuo.per_instance_summary</code> row for the batch</li><li>Affected workflow: <code>chief-sales-officer/quarterly-account-plan</code> (10–20 top-tier accounts per quarter)</li></ul>\n<ol><li><strong>MUST</strong> implement <code>MultiOutputHandler</code> per DEC-2384:</li></ol>\n<ul><li>Run chain end-to-end ONCE</li><li>Read <code>workflow.frontmatter.output_recipients</code> (list of <code>{recipient_id, format, delivery_method}</code>)</li><li>For each recipient: render final step's output through <code>recipient.format</code>, deliver via <code>recipient.delivery_method</code></li><li>Emit one <code>cuo.multi_output_fanout</code> memory row per recipient</li><li>Affected workflow: <code>chief-legal-officer/quarterly-regulatory-cycle</code> (1 source artefact → N regulator filings)</li></ul>\n<ol><li><strong>MUST</strong> implement <code>SequentialApprovalHandler</code> per DEC-2385:</li></ol>\n<ul><li>Read <code>workflow.frontmatter.gates</code> (list of <code>{approver_persona, approver_workflow}</code>)</li><li>Execute the gating workflow first (the approver's chain)</li><li>If approver chain <code>outcome != COMPLETED</code>: halt parent chain with <code>outcome=BLOCKED</code>, emit <code>cuo.sequential_approval_halted</code></li><li>If approver chain emits explicit approval audit kind: proceed with gated chain, emit <code>cuo.sequential_approval_resumed</code></li><li>Affected pair: <code>chief-ethics-officer/per-model-card-ethics-sign-off</code> gates <code>chief-ai-officer/per-model-card-release</code></li></ul>\n<ol><li><strong>MUST</strong> implement <code>PersonaPairHandler</code> per DEC-2386:</li></ol>\n<ul><li>Read <code>workflow.frontmatter.peer_persona</code> + <code>workflow.frontmatter.shared_artefact</code></li><li>Run primary chain up to declared handoff step</li><li>Pause, dispatch to peer persona's matching workflow (looked up by shared_artefact content_hash)</li><li>Receive peer's contribution; resume primary chain</li><li>Emit one <code>cuo.persona_pair_handoff</code> row per peer-direction transition</li><li>Affected pairs:</li><li><code>chief-revenue-officer/churn-collaboration</code> ↔ <code>chief-customer-officer/churn-collaboration</code> (shared: churn cohort analysis)</li><li><code>chief-marketing-officer/content-strategy</code> ↔ <code>chief-communications-officer/distribution-strategy</code> (shared: campaign plan)</li><li><code>chief-risk-officer/postmortem-risk-lens</code> ↔ <code>chief-technology-officer/postmortem-engineering-lens</code> (shared: incident report)</li><li><code>chief-customer-officer/customer-360-cx-lens</code> ↔ <code>chief-data-officer/customer-360-data-lens</code> (shared: customer profile)</li></ul>\n<ol><li><strong>MUST</strong> preserve audit-chain integrity: all 8 new memory audit kinds (DEC-2388) routed through <code>cyberos.core.writer.Writer</code> (no direct file writes).</li></ol>\n<ol><li><strong>MUST</strong> wire handler dispatch into <code>cli.py execute</code> subcommand: when workflow has <code>pattern != linear</code>, log <code># dispatched to &lt;HandlerClass&gt;</code> before invoking.</li></ol>\n<ol><li><strong>MUST</strong> version-bump <code>modules/cuo/pyproject.toml</code> from <code>3.0.0a3</code> to <code>3.0.0a4</code>.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate <code>skill_chain[]</code> at runtime (forbidden by CUO AGENTS.md §A.12).</li></ol>\n<ol><li><strong>MUST NOT</strong> bypass HITL halts in sequential_approval (the approval gate IS a HITL pause).</li></ol>\n<ol><li><strong>MUST NOT</strong> drop the <code>cuo.handler_dispatched</code> memory row for any non-linear pattern execution.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why dispatch by frontmatter <code>pattern:</code> field (DEC-2387)?</strong> Workflow author declares the pattern in YAML; the supervisor reads it and picks the Handler. Keeps the linear/default path unchanged (zero performance regression for 185/194 workflows) and makes the special cases self-documenting.</p>\n<p><strong>Why one Handler subclass per pattern (DEC-2381)?</strong> Each pattern has distinct invariants — time-critical wants SLA tracking, per-instance wants fan-in summary, multi-output wants fan-out delivery, sequential-approval wants HITL gates, persona-pair wants peer handoff. Lumping them into a generic handler with a giant switch statement loses these invariants in code review.</p>\n<p><strong>Why peer lookup by <code>shared_artefact.content_hash</code> (DEC-2386)?</strong> Persona-pair handoffs are about shared artefact ownership, not about routing strings. Looking up by content hash ensures both personas see the same artefact even if their workflows name it differently.</p>\n<p><strong>Why version <code>3.0.0a4</code> not <code>3.1.0</code>?</strong> Phase 4 is alpha-grade like Phase 1–3 — handler implementations are basic; production hardening (retries, timeouts, observability) comes in <code>3.2.0</code>.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Workflow frontmatter additions (per affected workflow):</p>\n<pre><code class=\"language-yaml\"># Time-critical workflow\npattern: time_critical\nsla_minutes: 240</code></pre>\n<pre><code class=\"language-yaml\"># Per-instance workflow\npattern: per_instance\ninstance_descriptor:\n  source: workflow.inputs.account_list\n  fields: [account_id, account_name, account_tier]</code></pre>\n<pre><code class=\"language-yaml\"># Multi-output workflow\npattern: multi_output\noutput_recipients:\n  - { recipient_id: \"vn-mst\", format: \"filing-xml-mst\", delivery_method: \"email\" }\n  - { recipient_id: \"vn-mof\", format: \"filing-pdf-mof\", delivery_method: \"portal\" }</code></pre>\n<pre><code class=\"language-yaml\"># Sequential-approval workflow (the gated one)\npattern: sequential_approval\ngates:\n  - { approver_persona: \"chief-ethics-officer\", approver_workflow: \"per-model-card-ethics-sign-off\" }</code></pre>\n<pre><code class=\"language-yaml\"># Persona-pair workflow\npattern: persona_pair\npeer_persona: \"cco-customer\"\npeer_workflow: \"churn-collaboration\"\nshared_artefact: \"churn-cohort-analysis\"\nhandoff_step: 4</code></pre>\n<p>CLI surface:</p>\n<pre><code class=\"language-text\">cyberos-cuo execute &lt;persona&gt;/&lt;workflow&gt;  # auto-detects pattern from frontmatter\n  → dispatched to TimeCriticalHandler   (when pattern: time_critical)\n  → dispatched to PerInstanceHandler    (when pattern: per_instance)\n  → dispatched to MultiOutputHandler    (when pattern: multi_output)\n  → dispatched to SequentialApprovalHandler  (when pattern: sequential_approval)\n  → dispatched to PersonaPairHandler    (when pattern: persona_pair)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>workflow_pattern enum cardinality 6</strong>.</li><li><strong>Default pattern (linear) routes through existing execute_chain() unchanged</strong> — no perf regression for 185 affected workflows.</li><li><strong>TimeCriticalHandler emits sla_breach when duration &gt; limit</strong>.</li><li><strong>TimeCriticalHandler bypasses any scheduling layer</strong> — invokes synchronously.</li><li><strong>PerInstanceHandler iterates exactly len(instance_descriptor) times</strong>.</li><li><strong>PerInstanceHandler fan-in summary <code>outcome=COMPLETED_BATCH</code> when all succeed; <code>outcome=PARTIAL</code> when any fail</strong>.</li><li><strong>MultiOutputHandler renders final-step output once per recipient</strong>.</li><li><strong>MultiOutputHandler emits 1 memory row per recipient</strong>.</li><li><strong>SequentialApprovalHandler halts on approver failure</strong>.</li><li><strong>SequentialApprovalHandler resumes on approver success</strong>.</li><li><strong>PersonaPairHandler routes to peer at declared handoff_step</strong>.</li><li><strong>PersonaPairHandler shared_artefact content_hash matches across peer chains</strong>.</li><li><strong>All 8 new memory audit kinds emit through cyberos.core.writer.Writer</strong>.</li><li><strong>CLI <code>execute</code> prints <code># dispatched to &lt;HandlerClass&gt;</code> for non-linear patterns</strong>.</li><li><strong>9 affected workflows updated with correct <code>pattern:</code> frontmatter</strong>.</li><li><strong>Existing 21/22 tests still pass post-change</strong>.</li><li><strong>6 new test files green</strong> (one per handler + dispatch).</li><li><strong>pyproject.toml version bumped to 3.0.0a4</strong>.</li><li><strong>CUO docs site §12 Roadmap updated</strong> — Phase 4 marked shipped.</li><li><strong>No workflow's skill_chain[] mutated at runtime</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/cuo/tests/test_applier_paths.py\ndef test_dispatch_default_is_linear():\n    \"\"\"Workflows without a pattern: field route to LinearHandler (= existing execute_chain).\"\"\"\n    from cuo.core.handlers.dispatch import pick_handler\n    workflow_dict = {\"frontmatter\": {}, \"body\": \"...\"}\n    handler = pick_handler(workflow_dict)\n    assert handler.__class__.__name__ == \"LinearHandler\"\n\ndef test_dispatch_reads_pattern_frontmatter():\n    \"\"\"Workflows with pattern: time_critical route to TimeCriticalHandler.\"\"\"\n    from cuo.core.handlers.dispatch import pick_handler\n    workflow_dict = {\"frontmatter\": {\"pattern\": \"time_critical\", \"sla_minutes\": 240}}\n    handler = pick_handler(workflow_dict)\n    assert handler.__class__.__name__ == \"TimeCriticalHandler\"\n    assert handler.sla_minutes == 240\n\n\n# modules/cuo/tests/test_proposal_applier.py\ndef test_time_critical_emits_sla_breach_when_slow(tmp_memory):\n    \"\"\"If actual_duration &gt; sla, memory gets a cuo.time_critical_sla_breach row.\"\"\"\n    from cuo.core.handlers.time_critical import TimeCriticalHandler\n    handler = TimeCriticalHandler(sla_minutes=1)  # 1 minute SLA\n    # Mock chain that takes 90 seconds\n    result = handler.execute(slow_chain_fixture, memory_root=tmp_memory)\n    breach_rows = [r for r in tmp_memory.audit_rows() if r.extra.get(\"kind\") == \"cuo.time_critical_sla_breach\"]\n    assert len(breach_rows) == 1\n    assert breach_rows[0].extra[\"breach_severity\"] &gt; 0.5\n\n\n# tests/test_per_instance_handler.py\ndef test_per_instance_iterates_once_per_account():\n    \"\"\"instance_descriptor with 5 accounts → 5 chain invocations + 1 summary.\"\"\"\n    from cuo.core.handlers.per_instance import PerInstanceHandler\n    instances = [{\"account_id\": f\"acct-{i}\"} for i in range(5)]\n    handler = PerInstanceHandler(instance_descriptor=instances)\n    result = handler.execute(cso_sales_workflow_fixture)\n    assert result.outcome == \"COMPLETED_BATCH\"\n    assert len(result.per_instance) == 5\n\n\n# tests/test_multi_output_handler.py\ndef test_multi_output_fanout_to_recipients():\n    \"\"\"3 recipients → final step output rendered 3 times + 3 memory rows.\"\"\"\n    from cuo.core.handlers.multi_output import MultiOutputHandler\n    recipients = [\n        {\"recipient_id\": \"vn-mst\", \"format\": \"xml\", \"delivery_method\": \"email\"},\n        {\"recipient_id\": \"vn-mof\", \"format\": \"pdf\", \"delivery_method\": \"portal\"},\n        {\"recipient_id\": \"vn-sbv\", \"format\": \"json\", \"delivery_method\": \"api\"},\n    ]\n    handler = MultiOutputHandler(output_recipients=recipients)\n    result = handler.execute(clo_legal_workflow_fixture, memory_root=tmp_memory)\n    fanout_rows = [r for r in tmp_memory.audit_rows() if r.extra.get(\"kind\") == \"cuo.multi_output_fanout\"]\n    assert len(fanout_rows) == 3\n\n\n# modules/cuo/tests/test_proposal_applier.py\ndef test_sequential_approval_halts_on_ethics_reject():\n    \"\"\"If ethics-sign-off chain fails, model-card-release does NOT execute.\"\"\"\n    from cuo.core.handlers.sequential_approval import SequentialApprovalHandler\n    handler = SequentialApprovalHandler(gates=[{\n        \"approver_persona\": \"chief-ethics-officer\",\n        \"approver_workflow\": \"per-model-card-ethics-sign-off\"\n    }])\n    # Mock approver chain that fails\n    result = handler.execute(caio_per_model_card_release_fixture, approver_outcome=\"FAILED\")\n    assert result.outcome == \"BLOCKED\"\n    halt_rows = [r for r in tmp_memory.audit_rows() if r.extra.get(\"kind\") == \"cuo.sequential_approval_halted\"]\n    assert len(halt_rows) == 1\n\n\n# modules/cuo/tests/test_proposal_applier.py\ndef test_persona_pair_handoff_at_declared_step():\n    \"\"\"At handoff_step, primary pauses + peer invoked + result threaded back.\"\"\"\n    from cuo.core.handlers.persona_pair import PersonaPairHandler\n    handler = PersonaPairHandler(\n        peer_persona=\"chief-customer-officer\",\n        peer_workflow=\"churn-collaboration\",\n        shared_artefact=\"churn-cohort-analysis\",\n        handoff_step=4,\n    )\n    result = handler.execute(cro_revenue_churn_fixture)\n    handoff_rows = [r for r in tmp_memory.audit_rows() if r.extra.get(\"kind\") == \"cuo.persona_pair_handoff\"]\n    assert len(handoff_rows) &gt;= 1\n    # Verify shared artefact content hash matches across both legs\n    primary_hash = result.shared_artefact_hash\n    peer_hash = result.peer_artefact_hash\n    assert primary_hash == peer_hash</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-CUO-104 (topological chain walk), TASK-CUO-105 (per-step rollback — sequential_approval halt may trigger rollback of completed steps).</p>\n<p><strong>Cross-module:</strong> TASK-SKILL-001 (skill registry for peer-workflow lookup), TASK-MEMORY-111 (PII scrubbing for SLA-breach reason field).</p>\n<p><strong>Downstream:</strong> None — Phase 4 closes the supervisor design. Future work (TASK-CUO-107+) shifts to production hardening (retries, observability, multi-tenant).</p>\n<hr>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Unknown <code>pattern:</code> value in workflow frontmatter</td><td>dispatch.py rejects</td><td>refuse to execute; emit <code>cuo.handler_dispatch_failed</code></td><td>author fixes frontmatter</td></tr><tr><td>time_critical chain hangs past SLA</td><td>timer in TimeCriticalHandler</td><td>sla_breach row emitted; chain continues (don't kill, deliver late + audit)</td><td>operator reviews breach in memory</td></tr><tr><td>per_instance empty descriptor</td><td>empty list check</td><td>refuse to execute; outcome=BLOCKED</td><td>author populates descriptor</td></tr><tr><td>multi_output zero recipients</td><td>empty list check</td><td>refuse to execute; outcome=BLOCKED</td><td>author adds recipients</td></tr><tr><td>sequential_approval approver chain has no halting step</td><td>approver chain returns COMPLETED without explicit approval audit</td><td>treat as auto-approved + log warning</td><td>operator decides if approval is implicit-OK</td></tr><tr><td>persona_pair peer workflow not found</td><td>catalog lookup miss</td><td>outcome=FAILED; emit <code>cuo.persona_pair_peer_not_found</code></td><td>author fixes peer_persona/peer_workflow</td></tr><tr><td>persona_pair shared_artefact hash mismatch</td><td>content_hash comparison</td><td>outcome=FAILED; emit <code>cuo.persona_pair_artefact_drift</code></td><td>author reconciles peer workflows</td></tr><tr><td>Handler raises uncaught exception</td><td>supervisor try/except</td><td>outcome=FAILED with stack trace in <code>notes</code></td><td>bug report + fix</td></tr><tr><td>Concurrent execution of same persona_pair from both sides</td><td>content_hash dedup</td><td>second invocation joins first's result</td><td>inherent</td></tr></tbody></table>";
