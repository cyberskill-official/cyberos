window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-110"]="<h1>TASK-IMP-110: Outer loop - propose skill amendments from run evidence</h1>\n<h2>Summary</h2>\n<p>Every ingredient of a learning loop exists and nothing consumes them: human verdicts at two gates, <code>routed_back_count</code>, route-back reasons, <code>memory.status_overridden</code>, retrospectives, reconcile reports. All written down, none read back. Add a <code>workflow-improver</code> skill that reads the last N runs' evidence and proposes <code>skill-amendment@1</code> records - landed as DRAFT tasks, never applied directly.</p>\n<h2>Problem</h2>\n<p>This run proves the gap twice over. An external reviewer found the same defect class - a spec promise with no implementation under it - in three consecutive rounds, and a human noticed the pattern, not the system. Every correction the operator made across five batches (\"drop section 4\", \"gate depends_on\", \"fix IMP-19 now\") lives in a gate log, not in a skill. The next run re-derives them or does not.</p>\n<p>TASK-IMP-028 (<code>ACE-style skill curation loop</code>) has been in the backlog as a stub since 2026-07-08 - filed, never authored. The idea was ours before it was the article's; what is missing is the authoring.</p>\n<h2>Proposed Solution</h2>\n<p><code>workflow-improver</code> reads a bounded window of run evidence - gate logs, route-back reasons, <code>status_overridden</code> rows, reconcile reports - clusters recurring shapes, and emits at most three <code>skill-amendment@1</code> proposals per run: which skill, which passage, what evidence (quoted rows with ids), what changes. Each lands as a DRAFT task through create-tasks. It never edits a skill.</p>\n<p>The cap is deliberate. An improver that proposes twenty amendments produces a review nobody does, and an unreviewed amendment to a skill is worse than no amendment - it is doctrine nobody agreed to.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Open a PR against the skill directly (the article's design). Rejected: our doctrine is that a human accepts every change, and a skill edit is a doctrine change. It proposes a task; the operator ships it through the normal loop.</li><li>Let it apply amendments below a confidence threshold. Rejected: confidence is the model's opinion of itself, which is precisely what the two-gate design exists to not trust.</li><li>Read the whole corpus every run. Rejected: unbounded input, unbounded cost, and recency is what makes a pattern actionable.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: run against this run's own corpus, it independently proposes at least the amendments the operator made by hand - they are on the record, so this is a real eval, not a fixture. Baseline: nothing reads the evidence at all.</li><li>Guardrail: it never writes to <code>modules/**</code> or any SKILL.md; every output is a proposal, and a clean window says so rather than manufacturing three.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the <code>workflow-improver</code> skill, the <code>skill-amendment@1</code> shape, the evidence readers, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Applying any amendment automatically.</li><li>Editing skills, rubrics, or workflows directly.</li><li>Proposing amendments to a repo's product code - this reads OUR loop's exhaust, not a consumer's codebase.</li><li>Model-quality evals (TASK-IMP-113's job).</li></ul>\n<h2>Dependencies</h2>\n<p>None mechanically. Consumes artefacts that exist.</p>\n<p><strong>Serialisation note:</strong> touches <code>build.sh</code> (shared with TASK-IMP-111 - both extend VENDORED_SKILLS). Parent-serialised per §11a.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §8 IMP-20 and §9.2, merging the never-authored draft TASK-IMP-028; sources are the cloud-factory articles (Zach Lloyd, 2026-07-15) and this run's own gate logs.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>workflow-improver</code> MUST read a bounded evidence window (default: the last 20 completed tasks) comprising gate logs, route-back reasons, <code>memory.status_overridden</code> rows, and reconcile reports.</li><li>1.2 It MUST emit at most 3 <code>skill-amendment@1</code> proposals per run, each naming: the target skill, the target passage, the evidence (quoted rows with their ids), and the proposed change.</li><li>1.3 Every proposal MUST cite at least 2 independent evidence rows. A pattern seen once is an anecdote.</li><li>1.4 It MUST NOT write to <code>modules/**</code>, any <code>SKILL.md</code>, any rubric, or any workflow file.</li><li>1.5 Proposals MUST land as <code>status: draft</code> tasks via create-tasks, and MUST NOT be self-audited into <code>ready_to_implement</code>.</li><li>1.6 A window with no qualifying pattern MUST report \"no amendment proposed\" and emit nothing - it MUST NOT pad to the cap.</li><li>1.7 TASK-IMP-028 MUST be flipped to <code>duplicate</code> with <code>duplicate_of: TASK-IMP-110</code>, since this task is its authored form.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2, #1.3) - given a fixture window with a 3-occurrence pattern, it proposes an amendment citing &gt;=2 evidence rows by id - test: <code>tools/install/tests/test_workflow_improver.sh::t01_pattern_proposes_with_evidence</code></li><li>[ ] AC 2 (traces_to: #1.3) - a single-occurrence pattern yields no proposal - test: <code>tools/install/tests/test_workflow_improver.sh::t02_anecdote_rejected</code></li><li>[ ] AC 3 (traces_to: #1.2) - a window with 8 patterns yields exactly 3 proposals, highest-evidence first - test: <code>tools/install/tests/test_workflow_improver.sh::t03_cap_enforced</code></li><li>[ ] AC 4 (traces_to: #1.4) - a run leaves <code>modules/**</code> byte-identical - test: <code>tools/install/tests/test_workflow_improver.sh::t04_never_writes_skills</code></li><li>[ ] AC 5 (traces_to: #1.6) - a clean window reports no proposal and writes nothing - test: <code>tools/install/tests/test_workflow_improver.sh::t05_clean_window_silent</code></li><li>[ ] AC 6 (traces_to: #1.5) - proposals land at <code>draft</code> - test: <code>tools/install/tests/test_workflow_improver.sh::t06_proposals_land_draft</code></li><li>[ ] AC 7 (traces_to: #1.7) - TASK-IMP-028 is <code>duplicate</code> with a resolving <code>duplicate_of</code> - verify: recorded frontmatter read in the gate log plus the FM-113 lint pass on 028 (a corpus state change, not a code path).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The window contains a pattern the operator already fixed: it proposes a redundant amendment. Acceptable and cheap to reject - and it is evidence the improver reads the same signals a human did, which is the point.</li><li>Evidence rows contradicting each other (an override reversed later): both cited, no proposal - a contradiction is not a pattern, and the improver must not pick a side.</li><li>A skill named in a proposal that no longer exists: the proposal reds at audit like any other spec citing a dead path. TRACE already covers it.</li><li>Gate logs are prose written by a model: they are UNTRUSTED INPUT. Quoted evidence MUST be reproduced verbatim with its id and MUST NOT be interpolated into any command. Same rule as TASK-IMP-100's rung-5.</li><li>Security-class: reads model-written prose from the repo. No execution of anything it reads; the evidence window is confined under <code>docs/tasks/**</code> via <code>relUnderRoot</code>.</li></ul>";
