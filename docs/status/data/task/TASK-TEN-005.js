window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship vertical-pack pricing add-on at <code>services/ten/src/packs/</code> with flat per-pack-per-month pricing, multi-currency catalog, per-tenant override, tier-gating, 14-day soft uninstall, install limit, prorated billing via TASK-TEN-003/102, and 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>pack_install_status</code> enum: <code>('pending','active','uninstalled','suspended_billing_failed','blocked_tier')</code> per DEC-1304. Cardinality asserts 5.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vertical_pack_installs</code> at migration <code>0023</code>: <code>(install_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, pack_id TEXT NOT NULL, status pack_install_status NOT NULL DEFAULT 'pending', installed_at TIMESTAMPTZ NOT NULL DEFAULT now(), activated_at TIMESTAMPTZ, soft_uninstalled_at TIMESTAMPTZ, hard_uninstalled_at TIMESTAMPTZ, current_period_start TIMESTAMPTZ, current_period_end TIMESTAMPTZ, billing_currency billing_currency_enum NOT NULL, last_charge_amount_minor BIGINT, last_charge_at TIMESTAMPTZ, trace_id CHAR(32))</code>. Partial unique <code>(tenant_id, pack_id) WHERE status NOT IN ('uninstalled','hard_uninstalled')</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vertical_pack_price_catalog</code> at migration <code>0024</code>: <code>(pack_id TEXT NOT NULL, currency billing_currency_enum NOT NULL, min_tier plan_tier NOT NULL, monthly_price_minor BIGINT NOT NULL CHECK (monthly_price_minor &gt; 0), version INT NOT NULL, effective_from TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY (pack_id, currency, version))</code>. Versioned for price-change grandfathering.</li></ol>\n<ol><li><strong>MUST</strong> define <code>vertical_pack_overrides</code> at migration <code>0025</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID NOT NULL, pack_id TEXT NOT NULL, currency billing_currency_enum NOT NULL, monthly_price_minor BIGINT NOT NULL, justification TEXT NOT NULL, set_by_subject_id UUID NOT NULL, set_at TIMESTAMPTZ NOT NULL DEFAULT now(), expires_at TIMESTAMPTZ)</code>. Partial unique <code>(tenant_id, pack_id, currency) WHERE expires_at IS NULL OR expires_at &gt; now()</code>.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS on all 3 tables scoped to tenant_id; <code>vertical_pack_overrides</code> requires <code>cfo</code> role to insert.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tid}/packs/install</code> body <code>{ pack_id }</code>. Handler:</li></ol>\n<ul><li>Validates pack exists in TASK-SKILL-107 registry.</li><li>Tier-gate check per §1 #7.</li><li>50-pack-limit check per DEC-1307.</li><li>Duplicate check (partial unique).</li><li>Resolve price per §1 #8.</li><li>Prorate invoice via TASK-TEN-003 (stripe-rail) or TASK-TEN-102 (vnd-rail).</li><li>INSERT install row with status='pending' → transition to 'active' on billing confirmation.</li><li>Emit <code>ten.pack_installed</code>.</li></ul>\n<ol><li><strong>MUST</strong> tier-gate per DEC-1306. Lookup pack's <code>min_tier</code>; compare with <code>tenants.plan_tier</code>. If insufficient → 403 + <code>tier_upgrade_required</code> + emit <code>ten.pack_tier_blocked</code> sev-2.</li></ol>\n<ol><li><strong>MUST</strong> resolve price per <code>price_resolver.rs::resolve(tenant, pack, currency)</code>:</li></ol>\n<ul><li>Check <code>vertical_pack_overrides</code> first (per-tenant, currency-matched, not expired) → use override price.</li><li>Else lookup <code>vertical_pack_price_catalog</code> latest version effective at <code>tenants.installed_at</code> per DEC-1309 grandfathering (within 90d use install-time version).</li><li>Else error <code>no_price_for_pack_currency_combo</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tid}/packs/{install_id}/uninstall</code> per DEC-1305:</li></ol>\n<ul><li>Transition status='uninstalled', <code>soft_uninstalled_at=now()</code>.</li><li>Skill bundle remains accessible 14 days (TASK-SKILL-107 reads our status).</li><li>At T+14d hard-uninstall job: <code>hard_uninstalled_at=now()</code> + skill bundle deactivated.</li><li>Stripe: prorate credit at uninstall; VND: skip next monthly charge.</li><li>Emit <code>ten.pack_uninstalled</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/admin/tenants/{tid}/packs/{install_id}/reinstall</code> within 14d window:</li></ol>\n<ul><li>Reverses soft-uninstall: status='active' + <code>soft_uninstalled_at=NULL</code>.</li><li>No new billing (period still active).</li><li>Emit <code>ten.pack_installed</code> with <code>via_reinstall=true</code>.</li></ul>\n<ol><li><strong>MUST</strong> monthly recurring charge at billing-cycle-anchor (consistent with TASK-TEN-003 DEC-788). Per active install, dispatch charge via configured rail. On failure: increment retry; after TASK-TEN-003-style 3 retries → status='suspended_billing_failed' + emit <code>ten.pack_billing_failed</code> sev-1.</li></ol>\n<ol><li><strong>MUST</strong> support per-tenant override via <code>POST /v1/admin/tenants/{tid}/packs/{pack_id}/override</code> (cfo role) body <code>{ currency, monthly_price_minor, justification, expires_at? }</code>. Emit <code>ten.pack_price_overridden</code> sev-1.</li></ol>\n<ol><li><strong>MUST</strong> enforce 50-pack-install limit per DEC-1307. <code>COUNT(*) WHERE tenant_id=$1 AND status IN ('pending','active') ≥ 50</code> → 403 + <code>install_limit_exceeded</code> (Enterprise tier can request via support).</li></ol>\n<ol><li><strong>MUST</strong> grandfather pack pricing per DEC-1309. When catalog version changes:</li></ol>\n<ul><li>Existing installs continue paying their install-time price for 90 days.</li><li>At T+90d: next billing uses new price + emit <code>ten.pack_price_changed</code> (informational not in 5-core).</li><li>New installs immediately use new price.</li></ul>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1308:</li></ol>\n<ul><li><code>ten.pack_installed</code> (sev-2)</li><li><code>ten.pack_uninstalled</code> (sev-2)</li><li><code>ten.pack_billing_failed</code> (sev-1)</li><li><code>ten.pack_price_overridden</code> (sev-1)</li><li><code>ten.pack_tier_blocked</code> (sev-2)</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub: justification_sha256 only in chain; raw in DB.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow per-seat pack pricing per DEC-1300.</li></ol>\n<ol><li><strong>MUST NOT</strong> install pack below min_tier per DEC-1306.</li></ol>\n<ol><li><strong>MUST NOT</strong> charge VND tenant via Stripe rail or vice versa (consistent with TASK-TEN-003 DEC-784 + TASK-TEN-102 DEC-973).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why flat per-pack pricing (§1 #1, DEC-1300)?</strong> Consultancies operate 1-to-many: one partner with the Legal Pack covers 50 clients. Per-seat would multiply the cost by 50× → packs become uneconomic for the core target segment.</p>\n<p><strong>Why 90-day grandfather (§1 #14, DEC-1309)?</strong> Mid-period price hikes feel like bait-and-switch. 90 days = one quarterly review cycle for the tenant to budget the new price.</p>\n<p><strong>Why 50-pack limit (§1 #13, DEC-1307)?</strong> Skill bundle loading at ~200KB per pack × 50 = 10MB per tenant — tolerable. Beyond 50 = bundle bloat + UX confusion (which pack does what?).</p>\n<p><strong>Why 14d soft uninstall (§1 #9, DEC-1305)?</strong> Accidental uninstall is real (admin click error; sales demo cleanup). 14d grace = users can reinstall without re-onboarding skill state.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0023_vertical_pack_installs.sql\nCREATE TYPE pack_install_status AS ENUM ('pending','active','uninstalled','suspended_billing_failed','blocked_tier');\n\nCREATE TABLE vertical_pack_installs (\n  install_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  pack_id TEXT NOT NULL,\n  status pack_install_status NOT NULL DEFAULT 'pending',\n  installed_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  activated_at TIMESTAMPTZ,\n  soft_uninstalled_at TIMESTAMPTZ,\n  hard_uninstalled_at TIMESTAMPTZ,\n  current_period_start TIMESTAMPTZ,\n  current_period_end TIMESTAMPTZ,\n  billing_currency billing_currency_enum NOT NULL,\n  install_time_price_version INT NOT NULL,\n  last_charge_amount_minor BIGINT,\n  last_charge_at TIMESTAMPTZ,\n  trace_id CHAR(32)\n);\nCREATE UNIQUE INDEX uniq_active_pack_install\n  ON vertical_pack_installs(tenant_id, pack_id)\n  WHERE status NOT IN ('uninstalled');\nALTER TABLE vertical_pack_installs ENABLE ROW LEVEL SECURITY;\nCREATE POLICY vertical_pack_installs_rls ON vertical_pack_installs\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE DELETE ON vertical_pack_installs FROM cyberos_app;\nGRANT UPDATE (status, activated_at, soft_uninstalled_at, hard_uninstalled_at,\n              current_period_start, current_period_end, last_charge_amount_minor, last_charge_at)\n  ON vertical_pack_installs TO cyberos_app;\n\n-- 0024_vertical_pack_price_catalog.sql\nCREATE TABLE vertical_pack_price_catalog (\n  pack_id TEXT NOT NULL,\n  currency billing_currency_enum NOT NULL,\n  min_tier plan_tier NOT NULL,\n  monthly_price_minor BIGINT NOT NULL CHECK (monthly_price_minor &gt; 0),\n  version INT NOT NULL,\n  effective_from TIMESTAMPTZ NOT NULL DEFAULT now(),\n  PRIMARY KEY (pack_id, currency, version)\n);\nREVOKE UPDATE, DELETE ON vertical_pack_price_catalog FROM cyberos_app;\n\n-- 0025_vertical_pack_overrides.sql\nCREATE TABLE vertical_pack_overrides (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  pack_id TEXT NOT NULL,\n  currency billing_currency_enum NOT NULL,\n  monthly_price_minor BIGINT NOT NULL,\n  justification TEXT NOT NULL,\n  set_by_subject_id UUID NOT NULL,\n  set_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  expires_at TIMESTAMPTZ\n);\nCREATE UNIQUE INDEX uniq_active_pack_override\n  ON vertical_pack_overrides(tenant_id, pack_id, currency)\n  WHERE expires_at IS NULL OR expires_at &gt; now();\nALTER TABLE vertical_pack_overrides ENABLE ROW LEVEL SECURITY;\nCREATE POLICY vertical_pack_overrides_rls ON vertical_pack_overrides\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON vertical_pack_overrides FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/admin/tenants/{tid}/packs/install\nPOST   /v1/admin/tenants/{tid}/packs/{install_id}/uninstall\nPOST   /v1/admin/tenants/{tid}/packs/{install_id}/reinstall\nPOST   /v1/admin/tenants/{tid}/packs/{pack_id}/override     (cfo)\nGET    /v1/admin/tenants/{tid}/packs\nGET    /v1/admin/packs/catalog                              (public-read)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>pack_install_status cardinality 5</strong>.</li><li><strong>Install creates row + prorated invoice</strong> — pack installed mid-period charges prorated amount.</li><li><strong>Tier gate</strong> — install below min_tier → 403 + <code>tier_upgrade_required</code> + audit.</li><li><strong>50-pack limit</strong> — 51st install → 403 + <code>install_limit_exceeded</code>.</li><li><strong>Per-tenant override</strong> — cfo override → resolver uses override price.</li><li><strong>Override expires</strong> — past <code>expires_at</code> → resolver falls back to catalog.</li><li><strong>Soft uninstall 14d</strong> — uninstalled pack still in skill registry until T+14d.</li><li><strong>Hard uninstall T+14d</strong> — job removes pack from registry; skill bundle deactivated.</li><li><strong>Reinstall within 14d</strong> — status returns 'active' without re-billing.</li><li><strong>Billing failure 3 retries</strong> — third failure → status='suspended_billing_failed' + sev-1 audit.</li><li><strong>Grandfathered pricing</strong> — pack price changed; existing installs charged old price for 90d.</li><li><strong>New install uses new price</strong> — after catalog version bump, new install charged new price immediately.</li><li><strong>VND tenant uses TASK-TEN-102 rail</strong> — pack billing routes through VND PSPs.</li><li><strong>USD tenant uses Stripe rail</strong>.</li><li><strong>Cross-rail rejected</strong> — VND tenant cannot install pack priced only in USD → 400.</li><li><strong>Per-seat attempt rejected</strong> — API endpoint has no seat_count field; pricing is flat.</li><li><strong>5 memory audit kinds emitted</strong> — full lifecycle.</li><li><strong>Cfo-only override</strong> — non-cfo override attempt → 403.</li><li><strong>RLS isolation</strong> — tenant A's installs invisible to tenant B.</li><li><strong>PII scrub</strong> — justification_sha256 in chain only.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn install_charges_prorated() {\n    let ctx = TestContext::with_stripe_tenant(\"acme\", PlanTier::Team).await;\n    ctx.seed_pack_catalog(\"legal-pack\", BillingCurrency::Usd, PlanTier::Team, 9900).await;\n    ctx.travel_to_mid_period().await;\n    let r = ctx.install_pack(ctx.tenant_id, \"legal-pack\").await;\n    assert_eq!(r.status(), 201);\n    let charge: i64 = ctx.last_stripe_invoice_amount().await;\n    assert!(charge &lt; 9900 &amp;&amp; charge &gt; 4000); // prorated half-month\n}\n\n#[tokio::test]\nasync fn tier_gate_blocks_starter() {\n    let ctx = TestContext::with_stripe_tenant(\"starter-co\", PlanTier::Starter).await;\n    ctx.seed_pack_catalog(\"enterprise-pack\", BillingCurrency::Usd, PlanTier::Enterprise, 99900).await;\n    let r = ctx.install_pack(ctx.tenant_id, \"enterprise-pack\").await;\n    assert_eq!(r.status(), 403);\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.pack_tier_blocked\"));\n}\n\n#[tokio::test]\nasync fn soft_uninstall_then_reinstall() {\n    let ctx = TestContext::new().await;\n    let install_id = ctx.install_pack_for_test().await;\n    ctx.uninstall_pack(install_id).await;\n    assert_eq!(ctx.load_status(install_id).await, \"uninstalled\");\n    let r = ctx.reinstall_pack(install_id).await;\n    assert_eq!(r.status(), 200);\n    assert_eq!(ctx.load_status(install_id).await, \"active\");\n}\n\n#[tokio::test]\nasync fn grandfathered_for_90d() {\n    let ctx = TestContext::new().await;\n    ctx.seed_pack_catalog_v1(\"legal-pack\", BillingCurrency::Usd, PlanTier::Team, 9900).await;\n    let install_id = ctx.install_pack_for_test().await;\n    ctx.bump_catalog_version(\"legal-pack\", 14900).await;  // price up 50%\n\n    ctx.travel_clock_forward(Duration::from_days(60)).await;\n    let charge1 = ctx.run_monthly_billing(install_id).await;\n    assert_eq!(charge1, 9900);  // grandfathered\n\n    ctx.travel_clock_forward(Duration::from_days(40)).await;  // now T+100d\n    let charge2 = ctx.run_monthly_billing(install_id).await;\n    assert_eq!(charge2, 14900);  // new price\n}\n\n// 5.5 cfo-only override\n// 5.6 50-pack limit\n// 5.7 billing failure suspends\n// 5.8 cross-rail rejection\n// 5.9 cardinality enum\n// 5.10 audit emission</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-TEN-002 (plan tiers — min_tier semantics), TASK-SKILL-107 (pack registry — pack_id source). <strong>Cross-module:</strong> TASK-TEN-003 (stripe rail), TASK-TEN-102 (vnd rail), TASK-TEN-004 (metering integration), TASK-INV-001 (invoice line items), TASK-AUTH-101 (cfo role), TASK-AI-003, TASK-MEMORY-111. <strong>Downstream:</strong> None.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>ten.pack_installed</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.pack_installed\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.tenant_admin.456\",\n  \"trace_id\": \"...\",\n  \"occurred_at\": \"2026-05-17T...\",\n  \"payload\": {\n    \"install_id\": \"0190...\",\n    \"pack_id\": \"cyberos.packs.legal\",\n    \"billing_currency\": \"USD\",\n    \"monthly_price_minor\": 9900,\n    \"prorated_charge_minor\": 4523,\n    \"via_reinstall\": false,\n    \"price_version\": 3\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Annual pack pricing — slice 3.</li><li><strong>Deferred:</strong> Pack bundles (\"Legal + Compliance + Contracts\" discount) — slice 3.</li><li><strong>Deferred:</strong> Free-trial periods on packs — slice 3.</li><li><strong>Deferred:</strong> Pack revenue-share with external publishers (slice 4 marketplace).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Pack not in registry</td><td>TASK-SKILL-107 lookup</td><td>404</td><td>Caller fixes pack_id</td></tr><tr><td>Tier insufficient</td><td>check</td><td>403 + tier_blocked audit</td><td>Caller upgrades plan</td></tr><tr><td>50-pack limit hit</td><td>count</td><td>403 + enterprise_override_required</td><td>Enterprise tenant contacts sales</td></tr><tr><td>Duplicate install</td><td>partial unique</td><td>409 + already_installed</td><td>Inherent</td></tr><tr><td>Billing failure</td><td>rail returns error</td><td>Retries; status=suspended_billing_failed after 3</td><td>CFO investigates</td></tr><tr><td>Override expires mid-period</td><td>resolver fallback</td><td>Next billing uses catalog price</td><td>Inherent</td></tr><tr><td>Cross-rail attempt</td><td>rail guard</td><td>400 + wrong_billing_rail</td><td>Caller uses correct rail</td></tr><tr><td>Pack catalog price missing for currency</td><td>resolver miss</td><td>400 + no_price_for_combo</td><td>Publisher seeds price</td></tr><tr><td>Hard-uninstall job stuck</td><td>watchdog</td><td>sev-2 alert</td><td>Manual cleanup</td></tr><tr><td>Override above plan-cap</td><td>validation</td><td>400 + override_invalid</td><td>CFO uses sane price</td></tr><tr><td>Soft-uninstall reinstall after 14d</td><td>window check</td><td>410 + window_expired; new install required</td><td>Fresh install</td></tr><tr><td>Catalog version effective_from in future</td><td>filter</td><td>Version not applied yet</td><td>Inherent</td></tr><tr><td>Grandfather window race (T+89d vs T+90d)</td><td>clock-based</td><td>Inherent boundary; alert if billing event near boundary</td><td>Inherent</td></tr><tr><td>Cfo override without justification</td><td>validation</td><td>400 + justification_required</td><td>Inherent</td></tr><tr><td>Pack uninstalled while billing in flight</td><td>tx isolation</td><td>Last writer wins; pro-rate credit calculated</td><td>Inherent</td></tr><tr><td>Currency change attempt on existing install</td><td>billing_currency immutable per TASK-TEN-003</td><td>Schema rejection</td><td>Inherent</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Pack install creates a row in <code>vertical_pack_installs</code> AND notifies TASK-SKILL-107 to activate the bundle.</p>\n<p><strong>§11.2</strong> Per-currency price catalog typically has 4-5 entries per pack (USD/EUR/SGD/GBP/VND).</p>\n<p><strong>§11.3</strong> Override expires_at supports time-bound discounts (\"50% off for 6 months\").</p>\n<p><strong>§11.4</strong> Billing-cycle anchor for pack matches tenant's main subscription anchor.</p>\n<p><strong>§11.5</strong> Hard-uninstall job runs hourly; sweeps <code>soft_uninstalled_at &lt; now() - 14d</code>.</p>\n<p><strong>§11.6</strong> Grandfather window per-install: <code>install_time_price_version</code> column locks the version for 90d.</p>\n<p><strong>§11.7</strong> Cross-rail check uses TASK-TEN-003 + TASK-TEN-102 guards directly.</p>\n<p><strong>§11.8</strong> Per-pack revenue tracked separately for publisher revenue-share (slice 4).</p>\n<p><strong>§11.9</strong> Skill registry consumes install status via API; TASK-SKILL-107 derived.</p>\n<p><strong>§11.10</strong> Override CHECK constraint: cannot exceed 2× catalog price (defensive — typos shouldn't 10× the bill).</p>\n<hr>\n<p><em>End of TASK-TEN-005 spec.</em></p>";
