window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-021"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> ship a <code>cyberos-ai</code> operator CLI binary providing ops-level commands. Each subcommand obeys the following:</p>\n<ol><li><strong>MUST</strong> be deterministic: same inputs (CLI args + Postgres + memory state) → same outputs. Idempotency is required for read commands; mutating commands must emit the same audit row regardless of repeat invocation.</li><li><strong>MUST</strong> print human-readable output by default using <code>comfy-table</code> for tabular data; the <code>--json</code> flag produces machine-readable output validated against a JSON schema versioned per command (per §1 #8).</li><li><strong>MUST</strong> support <code>--tenant &lt;id&gt;</code> filtering on every relevant subcommand (usage, policy, invoice, audit-trail). Without <code>--tenant</code>, the command operates across all tenants (admin-token required per §1 #6).</li><li><strong>MUST</strong> emit a memory audit row for ANY mutating operation (policy set, breaker reset, failover drill, expiry repair, memory emit) via the dedicated <code>canonical::cli_*</code> builders. The row carries <code>command</code>, <code>args</code>, <code>operator_id</code> (from token), <code>request_id</code>, <code>outcome</code>, AND a SHA-256 of the full command line for replay identification. Audit-before-action invariant from TASK-AI-001 §1 #6 applies.</li><li><strong>MUST</strong> refuse mutating operations without an explicit <code>--confirm</code> flag. Without <code>--confirm</code>, the command prints a DIFF (current state → proposed state) and exits with code 4 (<code>DESTRUCTIVE_WITHOUT_CONFIRM</code>). The diff is human-readable AND parseable (operators can pipe to <code>tee</code> to log review evidence).</li><li><strong>MUST</strong> authenticate the operator via <code>CYBEROS_AI_OPERATOR_TOKEN</code> environment variable (short-lived JWT signed by the deployment secret). The token carries <code>operator_id</code> (kebab-case email) and <code>roles</code> (<code>read | mutate | admin</code>). Read-only commands accept any role; mutating commands require <code>mutate</code> or <code>admin</code>; the <code>failover drill</code> and <code>expiry repair</code> commands require <code>admin</code>. Missing or invalid token exits with code 2 (<code>AUTH_FAILED</code>); insufficient role exits with code 2 with a clear message.</li><li><strong>MUST</strong> emit standardised exit codes per the shared <code>cyberos-cli-exit::ExitCode</code> re-export (cross-CLI contract; see also TASK-AUTH-006):</li></ol>\n<ul><li><code>0</code> — success</li><li><code>1</code> — user error (invalid args, validation failure, no such tenant)</li><li><code>2</code> — auth failed OR insufficient role</li><li><code>3</code> — remote unreachable (Postgres, memory, gateway)</li><li><code>4</code> — destructive operation without <code>--confirm</code></li><li><code>5</code> — already initialised (reserved — bootstrap-only path; not raised by operator CLI commands but value is reserved cluster-wide)</li><li><code>6</code> — schema violation (input YAML invalid)</li><li><code>7</code> — internal error</li><li><code>6</code> — internal error (panic, unexpected state) Exit codes are part of the CLI's stable contract; scripts and CI pipelines depend on them.</li></ul>\n<ol><li><strong>MUST</strong> version <code>--json</code> output schemas. Each command's JSON output starts with <code>{\"schema_version\":\"v1\",...}</code>; consumers parse the version first and select the appropriate parser. Schema files live in <code>cli/json_schemas/&lt;command&gt;.v1.json</code> (JSON Schema draft-07). Bumping a schema requires explicit task amendment AND inclusion of the prior schema for one release cycle.</li><li><strong>MUST</strong> include the subcommand catalogue (each subcommand has <code>--help</code> listing its specific flags):</li></ol>\n<table><thead><tr><th>Command</th><th>Purpose</th><th>Mutating</th><th>Required role</th><th>Audit row kind</th></tr></thead><tbody><tr><td><code>cyberos-ai usage [--tenant &lt;id&gt;] [--month YYYY-MM]</code></td><td>MTD spend, call count, top models</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai models list</code></td><td>List supported aliases × providers × models</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai models pricing</code></td><td>Show cost-table rates</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai policy set &lt;tenant&gt; --field=value [...] --confirm</code></td><td>Update tenant policy fields</td><td>Yes</td><td>mutate</td><td><code>ai.cli_policy_updated</code></td></tr><tr><td><code>cyberos-ai policy validate &lt;yaml-file&gt;</code></td><td>Validate without applying</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai policy diff &lt;tenant&gt; --vs &lt;yaml-file&gt;</code></td><td>Compare against pending change</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai failover drill &lt;provider:model&gt; --duration &lt;s&gt; --confirm</code></td><td>Force a 5xx storm to test failover</td><td>Yes</td><td>admin</td><td><code>ai.cli_failover_drill</code></td></tr><tr><td>`cyberos-ai invoice export &lt;tenant&gt; --period YYYY-MM --format csv\\</td><td>json\\</td><td>pdf`</td><td>Generate invoice</td><td>No</td><td>read</td><td><code>ai.cli_invoice_exported</code> (informational)</td></tr><tr><td><code>cyberos-ai breaker status</code></td><td>Show all breaker states</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai breaker reset &lt;provider:model&gt; --confirm</code></td><td>Force breaker to Closed</td><td>Yes</td><td>mutate</td><td><code>ai.cli_breaker_reset</code></td></tr><tr><td><code>cyberos-ai expiry status</code></td><td>Hold-expiry job health</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai expiry repair --confirm</code></td><td>Dedupe duplicate <code>ai.hold_expired</code> rows (TASK-AI-004)</td><td>Yes</td><td>admin</td><td><code>ai.cli_expiry_repaired</code></td></tr><tr><td><code>cyberos-ai memory emit --dry-run &lt;yaml&gt;</code></td><td>Validate canonical-row payload</td><td>No</td><td>read</td><td>—</td></tr><tr><td><code>cyberos-ai memory emit --confirm &lt;yaml&gt;</code></td><td>Emit a manual canonical row</td><td>Yes</td><td>admin</td><td><code>ai.cli_memory_emitted</code></td></tr><tr><td><code>cyberos-ai memory audit-trail &lt;tenant&gt; --since &lt;iso8601&gt;</code></td><td>Search memory ai.* rows</td><td>No</td><td>read</td><td>—</td></tr></tbody></table>\n<ol><li><strong>MUST</strong> support <code>policy set</code> taking multiple <code>--field=value</code> flags in one invocation: <code>policy set org:cyberskill --cap-usd=200 --zdr-required=true --residency=eu-1 --confirm</code>. The diff (printed without <code>--confirm</code> AND echoed with <code>--confirm</code>) shows BEFORE/AFTER for every changed field. Atomicity: either all fields apply OR none (single Postgres transaction).</li><li><strong>MUST</strong> apply a safety guard to <code>failover drill</code>: in a production environment (env var <code>CYBEROS_DEPLOYMENT_TIER=production</code>), the command additionally requires <code>--prod-confirmed-aware</code> flag AND prompts for an interactive Y/N confirmation with the deployment tier displayed. Drills in <code>production</code> are not forbidden (they're sometimes necessary) but must be deliberate.</li><li><strong>MUST</strong> produce parseable diff output for <code>policy set</code> without <code>--confirm</code>. The diff is printed in unified-diff-like format AND, with <code>--json</code>, also as a structured JSON: <code>{\"changes\":[{\"field\":\"cap_usd\",\"before\":150,\"after\":200},...]}</code>. Operators can pipe to <code>tee policy_change.diff</code> for change-management evidence.</li><li><strong>MUST</strong> include <code>policy validate</code> as a no-network operation: parses the YAML, runs TASK-AI-005's schema validator, prints errors (line-by-line if multiple) OR success. Useful for pre-deploy CI gates that validate policy YAMLs before merge.</li><li><strong>MUST</strong> redact secrets from CLI output: tenant policy fields marked <code>secret: true</code> in TASK-AI-005's schema (e.g., API keys for tenant-supplied managed-provider credentials) are displayed as <code>&lt;REDACTED&gt;</code> in both human and JSON output. Output of <code>policy diff</code> MUST NOT echo the secret value; operators see \"secret-changed\" indicator without the value.</li><li><strong>SHOULD</strong> generate shell completions for bash, zsh, and fish via <code>clap_complete</code>. The <code>cyberos-ai completions &lt;shell&gt;</code> subcommand outputs the completion script.</li><li><strong>SHOULD</strong> emit OTel metrics on CLI use:</li></ol>\n<ul><li><code>cyberos_ai_cli_invocations_total{command, outcome, role}</code> (counter).</li><li><code>cyberos_ai_cli_latency_ms{command}</code> (histogram; surfaces slow commands).</li><li><code>cyberos_ai_cli_destructive_without_confirm_total{command}</code> (counter; tracks operator-error frequency).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a single CLI binary?</strong> One install target, one mental model for ops. Operators learn <code>cyberos-ai &lt;subcommand&gt;</code> once and discover features via <code>--help</code>. Scattered scripts (<code>update_cap.sh</code>, <code>reset_breaker.sh</code>, <code>dedupe_holds.sh</code>) become a documentation problem the moment there are more than three; a single binary with subcommands is a navigation problem solved by clap. The packaging story (one Rust binary, statically linked) ships in the existing release artefact pipeline without separate dependency management.</p>\n<p><strong>Why JSON output mode (§1 #2)?</strong> Future scripts + dashboards consume CLI output. Without <code>--json</code>, parsing the human-readable output is fragile (table layout changes, columns reordered, Unicode in cell values). JSON output is a stable contract. Versioning the schema (§1 #8) lets us evolve fields without breaking existing consumers. The split (human default + JSON opt-in) means interactive ops sessions are pleasant AND scripts are robust.</p>\n<p><strong>Why audit rows on mutating operations (§1 #4)?</strong> The same audit-before-action invariant that the gateway itself enforces. Operator mutations are part of the system's history; \"who reset the breaker last Tuesday\" must be answerable from the memory chain. Without CLI audit rows, mutations become invisible (the gateway records the EFFECT but not the OPERATOR). The SHA-256 of the command line in the audit row is a small but useful detail — it lets a forensic replay reconstruct the exact invocation.</p>\n<p><strong>Why <code>--confirm</code> requirement (§1 #5)?</strong> A keystroke error like <code>cyberos-ai policy set org:cyberskill --cap-usd 0 --confirm</code> (intended <code>200</code>) instantly zeros a tenant's budget. Two-step confirmation (run-first-without-confirm-to-see-diff, then run-with-confirm) prevents the worst class of operator errors. The diff is the safety net; the operator visually verifies the change before re-running. Exit code 4 is distinct from \"user error\" (exit 1) so scripts can detect \"operator forgot --confirm\" specifically.</p>\n<p><strong>Why operator token authentication (§1 #6)?</strong> The CLI runs from operator workstations / bastion hosts; binary access alone shouldn't grant mutate authority (a compromised laptop ≠ compromised production). The short-lived JWT model (token expires in 8 hours; operator refreshes via internal SSO) limits blast radius. Role-gating prevents read-only operators from accidentally invoking <code>failover drill</code> (which is admin-only).</p>\n<p><strong>Why standardised exit codes (§1 #7)?</strong> CI scripts wrap CLI calls; they need to distinguish failure modes. \"User error\" (exit 1) means fix the args; \"auth failed\" (exit 2) means refresh the token; \"remote unreachable\" (exit 3) means a transient infrastructure issue (retry). Without distinct codes, every failure looks the same and scripts can't react appropriately.</p>\n<p><strong>Why versioned JSON schemas (§1 #8)?</strong> The JSON output IS the API for consumer scripts. Adding a field is non-breaking; removing or renaming a field IS breaking. Schema versions make this explicit: <code>\"schema_version\":\"v1\"</code> consumers parse v1; future v2 consumers parse v2; both versions can coexist for one release cycle. Without versioning, a subtle schema change breaks every downstream script silently.</p>\n<p><strong>Why safety guard on <code>failover drill</code> (§1 #11)?</strong> A drill deliberately trips a circuit breaker — provider calls fail for the drill's duration. In <code>staging</code>, this is fine (no real users). In <code>production</code>, this affects real tenants for the drill window. The double-opt-in (<code>--confirm</code> + <code>--prod-confirmed-aware</code> + interactive prompt) makes production drills DELIBERATE — operators have to consciously choose to impact production traffic. Production drills aren't forbidden (sometimes you need to validate failover under real load) but they require ceremony.</p>\n<p><strong>Why secret redaction (§1 #14)?</strong> Tenant policies may carry tenant-supplied API keys (for managed-provider credentials the tenant owns). Echoing those in CLI output (especially in shared sessions, logged commands, or piped output) is a data-exposure incident. The <code>secret: true</code> schema annotation + <code>&lt;REDACTED&gt;</code> rendering is the boundary; operators see \"the field changed\" without seeing the value.</p>\n<p><strong>Why <code>policy diff</code> as a separate command (§1 #9)?</strong> <code>policy set --confirm</code> shows the diff after the fact (in audit + echo). <code>policy diff &lt;tenant&gt; --vs &lt;file&gt;</code> shows the diff WITHOUT mutating. This supports change-management workflows: \"what would this YAML change?\" → review diff → approve → apply via <code>policy set</code>. Separating preview from apply is operational hygiene.</p>\n<p><strong>Why is this MUST priority (vs the COULD on TASK-AI-020)?</strong> Ops tasks aren't optional. TASK-AI-004's crash-recovery path explicitly REQUIRES <code>expiry repair</code> to clean up duplicate hold-expired rows after a crash. Without the CLI, the dedupe is manual SQL — error-prone, no audit trail, founder-only. TASK-AI-021 is in the critical path for Slice 5 ops.</p>\n<p><strong>Why <code>memory emit --dry-run</code> (§1 #9)?</strong> Manual canonical-row emission is rare but necessary (e.g., backfilling rows missed during an outage). <code>--dry-run</code> validates the YAML payload against the row's schema without writing — operators can construct + validate before emitting. Same precedence as <code>policy validate</code>.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<h3>CLI structure (clap)</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cli/mod.rs\nuse clap::{Parser, Subcommand, Args};\nuse chrono::NaiveDate;\n\n#[derive(Parser)]\n#[command(name = \"cyberos-ai\", version)]\npub struct Cli {\n    #[arg(long, global = true)] pub json: bool,\n    #[arg(long, global = true)] pub confirm: bool,\n    #[command(subcommand)] pub command: Command,\n}\n\n#[derive(Subcommand)]\npub enum Command {\n    Usage(UsageArgs),\n    Models(ModelsArgs),\n    Policy(PolicyArgs),\n    Failover(FailoverArgs),\n    Invoice(InvoiceArgs),\n    Breaker(BreakerArgs),\n    Expiry(ExpiryArgs),\n    Memory(MemoryArgs),\n    Completions(CompletionsArgs),\n}\n\n#[derive(Args)]\npub struct UsageArgs {\n    #[arg(long)] pub tenant: Option&lt;String&gt;,\n    #[arg(long)] pub month: Option&lt;NaiveDate&gt;,\n}\n\n#[derive(Args)]\npub struct PolicyArgs {\n    #[command(subcommand)] pub action: PolicyAction,\n}\n\n#[derive(Subcommand)]\npub enum PolicyAction {\n    Set { tenant: String,\n          #[arg(long)] cap_usd: Option&lt;f64&gt;,\n          #[arg(long)] zdr_required: Option&lt;bool&gt;,\n          #[arg(long)] residency: Option&lt;Residency&gt;,\n          #[arg(long)] allowed_personas: Option&lt;Vec&lt;String&gt;&gt; },\n    Validate { yaml_file: PathBuf },\n    Diff { tenant: String, #[arg(long = \"vs\")] vs: PathBuf },\n}\n\n#[derive(Args)]\npub struct FailoverArgs {\n    #[command(subcommand)] pub action: FailoverAction,\n}\n\n#[derive(Subcommand)]\npub enum FailoverAction {\n    Drill {\n        target: String,                                   // \"bedrock:claude-3-5-sonnet\"\n        #[arg(long, default_value_t = 60)] duration: u32,  // seconds\n        #[arg(long)] prod_confirmed_aware: bool,\n    },\n}\n\n// ... similar Args structs for Invoice, Breaker, Expiry, Memory ...</code></pre>\n<h3>Exit codes</h3>\n<p>Re-exported from the shared crate <strong><code>cyberos-cli-exit</code></strong> (single source of truth across all CyberOS CLIs — see also TASK-AUTH-006). CI scripts depend on these numerical values; they are a stable contract once locked.</p>\n<pre><code class=\"language-rust\">// crates/cyberos-cli-exit/src/lib.rs  (shared across services/ai-gateway, services/auth, etc.)\n#[repr(i32)]\npub enum ExitCode {\n    Ok                          = 0,\n    UserError                   = 1,\n    AuthFailed                  = 2,\n    RemoteUnreachable           = 3,\n    DestructiveWithoutConfirm   = 4,\n    AlreadyInitialised          = 5,\n    SchemaViolation             = 6,\n    InternalError               = 7,\n}\n\n// services/ai-gateway/src/cli/exit_codes.rs\npub use cyberos_cli_exit::ExitCode;</code></pre>\n<blockquote><p><strong>Note on numeric stability:</strong> values 0–7 are normative across all CyberOS CLIs.  Module-specific codes start at <code>100</code> (AI), <code>200</code> (AUTH), <code>300</code> (memory), <code>400</code> (OBS) — any per-module extension must avoid collision with the shared range above.</p></blockquote>\n<h3>Authentication</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cli/auth.rs\n\npub struct OperatorClaims {\n    pub operator_id: String,           // \"&lt;localpart&gt;@cyberos.world\"\n    pub roles: Vec&lt;Role&gt;,              // [\"read\", \"mutate\"] or [\"admin\"]\n    pub exp: i64,                      // unix timestamp\n}\n\n#[derive(Debug, Clone, PartialEq, Eq)]\npub enum Role { Read, Mutate, Admin }\n\npub fn parse_token(token: &amp;str) -&gt; Result&lt;OperatorClaims, AuthError&gt; {\n    let claims: OperatorClaims = jsonwebtoken::decode(token, &amp;PUBKEY, &amp;Validation::default())?\n        .claims;\n    Ok(claims)\n}\n\npub fn require_role(claims: &amp;OperatorClaims, needed: Role) -&gt; Result&lt;(), AuthError&gt; {\n    if claims.roles.contains(&amp;Role::Admin) { return Ok(()); }\n    if needed == Role::Read { return Ok(()); }   // any role can read\n    if needed == Role::Mutate &amp;&amp; claims.roles.contains(&amp;Role::Mutate) { return Ok(()); }\n    Err(AuthError::InsufficientRole { has: claims.roles.clone(), needs: needed })\n}</code></pre>\n<h3>JSON schema versioning</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cli/json_schemas.rs\npub fn validate_output&lt;T: Serialize&gt;(command: &amp;str, version: &amp;str, value: &amp;T) -&gt; Result&lt;(), String&gt; {\n    let schema_path = format!(\"src/cli/json_schemas/{command}.{version}.json\");\n    let schema = serde_json::from_str(&amp;std::fs::read_to_string(schema_path).unwrap()).unwrap();\n    let compiled = jsonschema::JSONSchema::compile(&amp;schema).unwrap();\n    let json = serde_json::to_value(value).unwrap();\n    let result = compiled.validate(&amp;json);\n    if let Err(errors) = result {\n        let msgs: Vec&lt;String&gt; = errors.map(|e| e.to_string()).collect();\n        return Err(msgs.join(\"; \"));\n    }\n    Ok(())\n}</code></pre>\n<h3>Audit row builders</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/memory_writer.rs (additions)\npub mod canonical {\n    pub fn cli_policy_updated(\n        operator_id: &amp;str, tenant: &amp;str, changes: &amp;[FieldChange],\n        command_sha256: &amp;str, request_id: &amp;str,\n    ) -&gt; AuditRow {\n        AuditRow {\n            kind: \"ai.cli_policy_updated\".into(),\n            payload: serde_json::json!({\n                \"operator_id\": operator_id,\n                \"tenant\": tenant,\n                \"changes\": changes,\n                \"command_sha256\": command_sha256,\n                \"request_id\": request_id,\n            }),\n            ..Default::default()\n        }\n    }\n\n    pub fn cli_breaker_reset(operator_id: &amp;str, target: &amp;str, command_sha256: &amp;str, request_id: &amp;str) -&gt; AuditRow {\n        AuditRow { kind: \"ai.cli_breaker_reset\".into(), payload: serde_json::json!({\n            \"operator_id\": operator_id, \"target\": target,\n            \"command_sha256\": command_sha256, \"request_id\": request_id,\n        }), ..Default::default() }\n    }\n\n    pub fn cli_failover_drill(operator_id: &amp;str, target: &amp;str, duration_s: u32,\n                              tier: &amp;str, command_sha256: &amp;str, request_id: &amp;str) -&gt; AuditRow {\n        AuditRow { kind: \"ai.cli_failover_drill\".into(), payload: serde_json::json!({\n            \"operator_id\": operator_id, \"target\": target, \"duration_s\": duration_s,\n            \"deployment_tier\": tier, \"command_sha256\": command_sha256, \"request_id\": request_id,\n        }), ..Default::default() }\n    }\n\n    pub fn cli_expiry_repaired(operator_id: &amp;str, deduped_count: u32,\n                               command_sha256: &amp;str, request_id: &amp;str) -&gt; AuditRow {\n        AuditRow { kind: \"ai.cli_expiry_repaired\".into(), payload: serde_json::json!({\n            \"operator_id\": operator_id, \"deduped_count\": deduped_count,\n            \"command_sha256\": command_sha256, \"request_id\": request_id,\n        }), ..Default::default() }\n    }\n\n    pub fn cli_memory_emitted(operator_id: &amp;str, emitted_kind: &amp;str,\n                             command_sha256: &amp;str, request_id: &amp;str) -&gt; AuditRow {\n        AuditRow { kind: \"ai.cli_memory_emitted\".into(), payload: serde_json::json!({\n            \"operator_id\": operator_id, \"emitted_kind\": emitted_kind,\n            \"command_sha256\": command_sha256, \"request_id\": request_id,\n        }), ..Default::default() }\n    }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong><code>cyberos-ai --version</code> returns binary version</strong> — Exit 0 with version string.</li><li><strong><code>cyberos-ai usage --tenant org:test</code></strong> prints MTD spend, call count, top 5 models. Exit 0.</li><li><strong><code>cyberos-ai usage ... --json</code></strong> returns JSON validating against <code>usage.v1.json</code> schema.</li><li><strong><code>cyberos-ai policy set &lt;t&gt; --cap-usd 200</code></strong> without <code>--confirm</code> prints diff + exits with code 4.</li><li><strong><code>cyberos-ai policy set &lt;t&gt; --cap-usd 200 --confirm</code></strong> updates Postgres + emits <code>ai.cli_policy_updated</code> memory row.</li><li><strong>Multi-field policy set atomic</strong> — <code>policy set &lt;t&gt; --cap-usd=200 --zdr-required=true --confirm</code> updates BOTH fields in a single transaction; partial failure rolls back.</li><li><strong><code>cyberos-ai breaker status</code></strong> shows current breaker state for every (provider, model) registered.</li><li><strong><code>cyberos-ai breaker reset bedrock:claude-3-5-sonnet --confirm</code></strong> transitions Open→Closed + emits <code>ai.cli_breaker_reset</code>.</li><li><strong><code>cyberos-ai invoice export &lt;t&gt; --period 2026-05 --format json</code></strong> produces all <code>ai.invocation</code> rows for that period.</li><li><strong><code>cyberos-ai expiry repair --confirm</code></strong> removes duplicate <code>ai.hold_expired</code> rows (TASK-AI-004 dedupe).</li><li><strong>Each subcommand has <code>--help</code></strong> listing flags + exits 0.</li><li><strong>Auth: missing token exits 2</strong> — Unset <code>CYBEROS_AI_OPERATOR_TOKEN</code>; any subcommand exits with code 2 and message <code>auth_failed: missing CYBEROS_AI_OPERATOR_TOKEN</code>.</li><li><strong>Auth: insufficient role exits 2</strong> — read-only token + <code>policy set --confirm</code> exits 2 with <code>insufficient_role: needed mutate; have [read]</code>.</li><li><strong><code>failover drill</code> in production requires <code>--prod-confirmed-aware</code></strong> — <code>CYBEROS_DEPLOYMENT_TIER=production</code> + drill without flag exits 4 with <code>production drill requires --prod-confirmed-aware AND interactive Y confirmation</code>.</li><li><strong><code>policy diff</code> no-mutate</strong> — Reads tenant policy from Postgres, parses YAML file, prints unified-diff format without writing; exits 0.</li><li><strong><code>policy validate</code> no-network</strong> — Parses YAML; calls TASK-AI-005 schema validator; prints errors line-by-line on failure (exit 5) or <code>valid</code> on success (exit 0).</li><li><strong>JSON output validates against versioned schema</strong> — <code>cli_json_schema_test.rs</code> runs each subcommand with <code>--json</code>; output passes JSON Schema validation against <code>cli/json_schemas/&lt;command&gt;.v1.json</code>.</li><li><strong>Secret redaction</strong> — Policy field marked <code>secret: true</code> is displayed as <code>&lt;REDACTED&gt;</code> in both human and JSON output; <code>policy diff</code> shows <code>secret-changed: true</code> indicator without the value.</li><li><strong>Shell completions emit valid scripts</strong> — <code>cyberos-ai completions bash</code> produces a sourceable bash completion script.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/tests/otel_test.rs\nuse std::process::Command;\nuse assert_cmd::prelude::*;\n\n#[test]\nfn version_returns_binary_version() {\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.arg(\"--version\").assert().success().stdout(predicates::str::contains(\"cyberos-ai\"));\n}\n\n#[test]\nfn usage_prints_table_for_tenant() {\n    set_test_token_with_role(\"read\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"usage\", \"--tenant\", \"org:test\"]).assert().success()\n        .stdout(predicates::str::contains(\"PERIOD:\"))\n        .stdout(predicates::str::contains(\"CAP:\"))\n        .stdout(predicates::str::contains(\"SPENT:\"));\n}\n\n#[test]\nfn policy_set_without_confirm_exits_4_with_diff() {\n    set_test_token_with_role(\"mutate\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"policy\", \"set\", \"org:test\", \"--cap-usd\", \"200\"])\n        .assert().code(4)\n        .stdout(predicates::str::contains(\"cap_usd: 150 → 200\"));\n}\n\n#[test]\nfn policy_set_with_confirm_updates_and_emits_audit() {\n    set_test_token_with_role(\"mutate\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"policy\", \"set\", \"org:test\", \"--cap-usd\", \"200\", \"--confirm\"])\n        .assert().success();\n    let row = memory_test_helper::find_latest_row(\"ai.cli_policy_updated\").unwrap();\n    assert_eq!(row.payload[\"tenant\"], \"org:test\");\n    assert_eq!(row.payload[\"changes\"][0][\"field\"], \"cap_usd\");\n    assert_eq!(row.payload[\"changes\"][0][\"before\"], 150.0);\n    assert_eq!(row.payload[\"changes\"][0][\"after\"], 200.0);\n}\n\n#[test]\nfn missing_token_exits_2() {\n    std::env::remove_var(\"CYBEROS_AI_OPERATOR_TOKEN\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"usage\", \"--tenant\", \"org:test\"])\n        .assert().code(2)\n        .stderr(predicates::str::contains(\"auth_failed\"));\n}\n\n#[test]\nfn insufficient_role_exits_2() {\n    set_test_token_with_role(\"read\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"policy\", \"set\", \"org:test\", \"--cap-usd\", \"200\", \"--confirm\"])\n        .assert().code(2)\n        .stderr(predicates::str::contains(\"insufficient_role\"));\n}\n\n#[test]\nfn failover_drill_in_production_requires_extra_flag() {\n    set_test_token_with_role(\"admin\");\n    std::env::set_var(\"CYBEROS_DEPLOYMENT_TIER\", \"production\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"failover\", \"drill\", \"bedrock:claude-3-5-sonnet\", \"--duration\", \"30\", \"--confirm\"])\n        .assert().code(4)\n        .stderr(predicates::str::contains(\"--prod-confirmed-aware\"));\n}\n\n#[test]\nfn breaker_reset_with_confirm_emits_audit() {\n    set_test_token_with_role(\"mutate\");\n    breaker::force_open(\"bedrock:claude-3-5-sonnet\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"breaker\", \"reset\", \"bedrock:claude-3-5-sonnet\", \"--confirm\"])\n        .assert().success();\n    assert_eq!(breaker::state(\"bedrock:claude-3-5-sonnet\"), BreakerState::Closed);\n    let row = memory_test_helper::find_latest_row(\"ai.cli_breaker_reset\").unwrap();\n    assert_eq!(row.payload[\"target\"], \"bedrock:claude-3-5-sonnet\");\n}\n\n#[test]\nfn expiry_repair_dedupes_duplicate_rows() {\n    set_test_token_with_role(\"admin\");\n    test_helper::insert_duplicate_hold_expired_rows(5);\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    cmd.args(&amp;[\"expiry\", \"repair\", \"--confirm\"])\n        .assert().success()\n        .stdout(predicates::str::contains(\"deduped: 5\"));\n    let row = memory_test_helper::find_latest_row(\"ai.cli_expiry_repaired\").unwrap();\n    assert_eq!(row.payload[\"deduped_count\"], 5);\n}\n\n#[test]\nfn json_output_validates_against_usage_v1_schema() {\n    set_test_token_with_role(\"read\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    let output = cmd.args(&amp;[\"usage\", \"--tenant\", \"org:test\", \"--json\"])\n        .output().unwrap();\n    let json: serde_json::Value = serde_json::from_slice(&amp;output.stdout).unwrap();\n    assert_eq!(json[\"schema_version\"], \"v1\");\n    let schema = serde_json::from_str(&amp;std::fs::read_to_string(\"src/cli/json_schemas/usage.v1.json\").unwrap()).unwrap();\n    let compiled = jsonschema::JSONSchema::compile(&amp;schema).unwrap();\n    assert!(compiled.is_valid(&amp;json));\n}\n\n#[test]\nfn secret_redacted_in_output() {\n    set_test_token_with_role(\"read\");\n    test_helper::set_tenant_secret_field(\"org:test\", \"openai_api_key\", \"sk-real-key\");\n    let mut cmd = Command::cargo_bin(\"cyberos-ai\").unwrap();\n    let stdout = String::from_utf8(cmd.args(&amp;[\"policy\", \"validate\", \"tenants/org_test/policy.yaml\"])\n        .output().unwrap().stdout).unwrap();\n    assert!(stdout.contains(\"&lt;REDACTED&gt;\"));\n    assert!(!stdout.contains(\"sk-real-key\"));\n}</code></pre>\n<pre><code class=\"language-bash\">cargo build --bin cyberos-ai --release\n./target/release/cyberos-ai --version\n./target/release/cyberos-ai usage --tenant org:test --month 2026-05 --json | jq .schema_version\ncargo test -p cyberos-ai-gateway cli</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/bin/cyberos_ai.rs\nuse cyberos_ai_gateway::cli::{Cli, Command, exit_codes::ExitCode, auth};\nuse clap::Parser;\nuse std::process;\n\n#[tokio::main]\nasync fn main() {\n    let cli = Cli::parse();\n    let token = std::env::var(\"CYBEROS_AI_OPERATOR_TOKEN\").unwrap_or_default();\n    let claims = match auth::parse_token(&amp;token) {\n        Ok(c) =&gt; c,\n        Err(e) =&gt; {\n            eprintln!(\"auth_failed: {e}\");\n            process::exit(ExitCode::AuthFailed as i32);\n        }\n    };\n    let pool = build_pool().await.unwrap_or_else(|e| {\n        eprintln!(\"remote_unreachable: {e}\");\n        process::exit(ExitCode::RemoteUnreachable as i32);\n    });\n\n    let result = match cli.command {\n        Command::Usage(args)        =&gt; cli::usage::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Models(args)       =&gt; cli::models::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Policy(args)       =&gt; cli::policy::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Failover(args)     =&gt; cli::failover::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Invoice(args)      =&gt; cli::invoice::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Breaker(args)      =&gt; cli::breaker::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Expiry(args)       =&gt; cli::expiry::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Memory(args)        =&gt; cli::memory::run(args, &amp;cli, &amp;claims, &amp;pool).await,\n        Command::Completions(args)  =&gt; cli::completions::run(args).await,\n    };\n    match result {\n        Ok(_)  =&gt; process::exit(0),\n        Err(e) =&gt; {\n            eprintln!(\"{e}\");\n            process::exit(e.exit_code() as i32);\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<h3>Code dependencies (other tasks/modules)</h3>\n<ul><li><strong>TASK-AI-005</strong> — <code>policy::validate_yaml()</code> + <code>policy::apply()</code> exposed to the CLI; this task depends on the schema being stable.</li><li><strong>TASK-AI-008</strong> — <code>breaker::status_all()</code> + <code>breaker::reset(target)</code>; the breaker module exposes admin entry-points.</li><li><strong>TASK-AI-001</strong> — Cost-ledger Postgres tables for <code>usage</code> + <code>invoice</code> aggregations.</li><li><strong>TASK-AI-002</strong> — <code>ai.invocation</code> rows are the source for invoice export.</li><li><strong>TASK-AI-003</strong> — memory audit-row bridge; this task adds <code>canonical::cli_*</code> builders.</li><li><strong>TASK-AI-004</strong> — <code>expiry_repair</code> directly closes TASK-AI-004's slice-1 dedup limitation (AC #9).</li><li><strong>TASK-AI-007</strong> — <code>cost_table::lookup()</code> for <code>models pricing</code>.</li><li><strong>TASK-AI-014</strong> — Persona-handle parsing for <code>policy set --allowed-personas</code>.</li><li><strong>TASK-AI-022 (downstream)</strong> — OTel CLI metrics consumed by the operator dashboard.</li></ul>\n<h3>Concept dependencies (shared types)</h3>\n<ul><li><code>Role::{Read, Mutate, Admin}</code> is the operator-permission primitive; tokens carry roles.</li><li><code>ExitCode</code> enum is a stable contract; CI scripts depend on numerical values.</li><li>Versioned JSON schemas (<code>&lt;command&gt;.v1.json</code>) are the consumer-facing API.</li><li><code>FieldChange { field, before, after }</code> is the diff primitive used by <code>policy set</code>/<code>diff</code> AND audit rows.</li><li><code>command_sha256</code> is the audit-replay primitive (full command line hashed).</li></ul>\n<h3>Operational / external</h3>\n<ul><li>Rust crates: <code>clap@4</code> (with <code>derive</code> feature), <code>clap_complete@4</code> (shell completions), <code>comfy-table@7</code>, <code>tokio</code>, <code>sqlx</code>, <code>anyhow</code>, <code>jsonwebtoken</code>, <code>jsonschema@0.18</code>, <code>serde</code>, <code>serde_json</code>, <code>serde_yaml</code>, <code>chrono</code>, <code>predicates</code> (test-only), <code>assert_cmd</code> (test-only).</li><li>Internal SSO that issues operator tokens (out of scope; assumed available).</li><li><code>wkhtmltopdf</code> (or <code>weasyprint</code>) on the operator's machine for <code>invoice export --format pdf</code>.</li><li>Postgres + memory reachability from the operator's host (typically via bastion / VPN).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>usage</code> human output</h3>\n<pre><code class=\"language-text\">$ cyberos-ai usage --tenant org:cyberskill --month 2026-05\nPERIOD: 2026-05  (1-15 of 31 days; period in progress)\nTENANT: org:cyberskill\nCAP:    $150.00\nSPENT:  $47.23  (31.5% of cap)\nCALLS:  3,421   (avg $0.0138 per call)\n\nTop 5 models by spend:\nMODEL                                       SPEND     CALLS    AVG\nanthropic.claude-3-5-sonnet-20241022-v2:0   $32.10    1,205    $0.0266\nanthropic.claude-3-haiku-20240307-v1:0      $ 8.45    1,890    $0.0045\ngpt-4o-mini                                 $ 4.20      280    $0.0150\ntext-embedding-3-small                      $ 2.48        4    $0.6200\nbge-m3                                      $ 0.00       42    $0.0000  (self-hosted)</code></pre>\n<h3><code>usage --json</code> output (validates against <code>usage.v1.json</code>)</h3>\n<pre><code class=\"language-json\">{\n  \"schema_version\": \"v1\",\n  \"tenant\": \"org:cyberskill\",\n  \"month\": \"2026-05\",\n  \"cap_usd\": 150.0,\n  \"spent_usd\": 47.23,\n  \"spent_pct\": 31.5,\n  \"calls\": 3421,\n  \"top_models_by_spend\": [\n    {\"model\": \"anthropic.claude-3-5-sonnet-20241022-v2:0\", \"spend_usd\": 32.10, \"calls\": 1205},\n    {\"model\": \"anthropic.claude-3-haiku-20240307-v1:0\",     \"spend_usd\": 8.45,  \"calls\": 1890}\n  ]\n}</code></pre>\n<h3><code>policy set --confirm</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ai.cli_policy_updated\",\n  \"ts_ns\": 1747526400000000000,\n  \"payload\": {\n    \"operator_id\": \"stephen@cyberos.world\",\n    \"tenant\": \"org:cyberskill\",\n    \"changes\": [\n      { \"field\": \"cap_usd\", \"before\": 150.0, \"after\": 200.0 },\n      { \"field\": \"zdr_required\", \"before\": false, \"after\": true }\n    ],\n    \"command_sha256\": \"4b8c0d2f...\",\n    \"request_id\": \"cli_01HZK...\"\n  }\n}</code></pre>\n<h3><code>policy set</code> diff (no <code>--confirm</code>)</h3>\n<pre><code class=\"language-text\">$ cyberos-ai policy set org:cyberskill --cap-usd 200 --zdr-required true\nDIFF for tenant org:cyberskill:\n  cap_usd:        150 → 200\n  zdr_required:   false → true\n\nTo apply, re-run with --confirm\nexit 4</code></pre>\n<h3><code>breaker status</code> human output</h3>\n<pre><code class=\"language-text\">$ cyberos-ai breaker status\nPROVIDER     MODEL                                STATE    FAILURES   NEXT_HALF_OPEN\nbedrock      anthropic.claude-3-5-sonnet-...      Open     5 / 60s    in 18s\nbedrock      anthropic.claude-3-haiku-...         Closed   0          —\nanthropic    claude-3-5-sonnet-20241022           Closed   0          —\nopenai       gpt-4o                               HalfOpen 1 / 60s    probe in flight\nbge          bge-m3                               Closed   0          —</code></pre>\n<h3><code>failover drill</code> audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ai.cli_failover_drill\",\n  \"payload\": {\n    \"operator_id\": \"stephen@cyberos.world\",\n    \"target\": \"bedrock:claude-3-5-sonnet-20241022-v2:0\",\n    \"duration_s\": 60,\n    \"deployment_tier\": \"staging\",\n    \"command_sha256\": \"9a7b...\",\n    \"request_id\": \"cli_drill_01...\"\n  }\n}</code></pre>\n<h3><code>expiry repair</code> output</h3>\n<pre><code class=\"language-text\">$ cyberos-ai expiry repair --confirm\nScanning for duplicate ai.hold_expired rows…\nFound 5 duplicates:\n  hold_id=01HZK... (2 copies)\n  hold_id=01HZL... (3 copies)\nDeduped: 5 rows removed\nAudit: ai.cli_expiry_repaired emitted</code></pre>\n<h3>Production drill safety guard output</h3>\n<pre><code class=\"language-text\">$ cyberos-ai failover drill bedrock:claude-3-5-sonnet --duration 60 --confirm\nERROR: production drill requires --prod-confirmed-aware AND interactive Y confirmation\n       you are about to deliberately fail provider calls for 60s in PRODUCTION\n       affected tenants: 47\n       affected aliases: chat.smart, chat.long\nexit 4</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved at authoring time. Items deferred to later tasks:</p>\n<ul><li>Multi-operator collaboration (e.g., one operator initiates a drill, another approves) — out of scope; current model is single-operator-per-command.</li><li>Web-based operator UI alongside the CLI — slice 6+; CLI is the slice-5 surface.</li><li><code>cyberos-ai watch</code> for real-time tail of OBS metrics — out of scope; <code>tail -f</code> on the OBS log suffices for slice 5.</li><li><code>cyberos-ai backfill</code> for historical cost-ledger reconstruction — slice 6+; current model is forward-only audit.</li><li>Per-tenant operator scoping (an operator with mutate rights only for <code>tenant_a</code>) — slice 6+; current model is global mutate role.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Postgres unreachable</td><td>sqlx connect error</td><td>CLI exits 3 (RemoteUnreachable) with clear message</td><td>Operator investigates DB connectivity</td></tr><tr><td>memory unreachable</td><td>memory_writer error</td><td>Mutating commands fail (exit 3); read-only succeed (degraded)</td><td>Operator investigates memory</td></tr><tr><td>Invalid YAML in <code>policy validate</code></td><td>parse error</td><td>Print errors line-by-line; exit 5 (SchemaViolation)</td><td>Operator fixes file</td></tr><tr><td>Missing <code>--confirm</code> on destructive</td><td>clap arg check</td><td>Print diff + exit 4</td><td>Operator re-runs with <code>--confirm</code></td></tr><tr><td>Output format mismatch (<code>--json</code> requested but error path uses text)</td><td>Always-JSON-on-error rule</td><td>Errors emit JSON when <code>--json</code> set</td><td>By design</td></tr><tr><td>Missing <code>CYBEROS_AI_OPERATOR_TOKEN</code></td><td>auth check</td><td>Exit 2 with <code>auth_failed: missing token</code></td><td>Operator obtains token via SSO</td></tr><tr><td>Expired token</td><td>JWT exp validation</td><td>Exit 2 with <code>auth_failed: token expired</code></td><td>Operator refreshes token</td></tr><tr><td>Insufficient role (read-only attempts mutate)</td><td>role check</td><td>Exit 2 with <code>insufficient_role: needed mutate; have [read]</code></td><td>Operator obtains escalated token</td></tr><tr><td><code>failover drill</code> in production without safety flags</td><td>env-aware safety guard</td><td>Exit 4 with affected-tenants + affected-aliases displayed</td><td>Operator adds <code>--prod-confirmed-aware</code> AND confirms interactively</td></tr><tr><td><code>policy set</code> partial failure (one field invalid)</td><td>atomic transaction rolls back</td><td>Exit 5 (SchemaViolation); no fields applied</td><td>Operator fixes the bad field; re-runs</td></tr><tr><td><code>expiry repair</code> finds zero duplicates</td><td>informational</td><td>Exit 0 with <code>deduped: 0</code></td><td>By design</td></tr><tr><td><code>memory emit --confirm</code> row schema invalid</td><td>canonical-row validator</td><td>Exit 5 (SchemaViolation)</td><td>Operator fixes YAML payload</td></tr><tr><td>JSON output schema drift</td><td><code>cli_json_schema_test</code> fails in CI</td><td>PR blocked</td><td>Bump schema version OR fix output</td></tr><tr><td>Secret leaks in CLI output</td><td><code>cli_secret_redaction_test</code> asserts <code>&lt;REDACTED&gt;</code></td><td>Test fails → PR blocked</td><td>Add field to redaction list</td></tr><tr><td>Operator token compromised</td><td>Audit chain shows mutations from unexpected operator_id</td><td>Sev-1 incident</td><td>Rotate token; investigate</td></tr><tr><td>Long-running command (e.g., invoice export 100K rows)</td><td>timeout in CI</td><td>Exit 6 (InternalError)</td><td>Operator investigates query plan; pagination</td></tr><tr><td><code>policy diff</code> shows secret-changed without value</td><td>by design (§1 #14)</td><td>Operator sees boolean indicator</td><td>By design</td></tr><tr><td>Shell completion script malformed</td><td><code>cyberos-ai completions bash</code> test asserts validity</td><td>Test fails</td><td>Fix clap_complete generation</td></tr><tr><td>Stale Postgres connection (long-lived CLI session)</td><td>sqlx pool reconnect</td><td>Self-resolves</td><td>By design</td></tr><tr><td><code>failover drill</code> interactive prompt skipped via piped input</td><td>tty check</td><td>If non-tty AND production, exit 4 unconditionally</td><td>Operator runs from interactive shell</td></tr><tr><td>Audit row emit failure during destructive operation</td><td>memory_writer error AFTER mutation</td><td>Sev-1 alert: \"mutation succeeded but audit row failed\"</td><td>Operator manually emits via <code>memory emit</code></td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>The CLI is the single point of ops surface. Avoid scattered scripts; everything goes through <code>cyberos-ai</code>. New ops capabilities ship as new subcommands, not as standalone tools.</li><li><code>failover drill</code> is the most novel command — it deliberately trips a circuit breaker to verify failover works under controlled conditions. Useful PRE-incident; sev-2 OBS event records the drill so operators reviewing post-incident logs can distinguish drills from real failures.</li><li><code>expiry repair</code> directly closes the slice-1 limitation in TASK-AI-004 AC #9 (manual SQL was the workaround). The CLI command makes this a routine ops task instead of a database query.</li><li>The role-gating model (read / mutate / admin) is intentionally simple. Per-tenant scoping (an operator who can only mutate <code>tenant_a</code>) is slice 6+; the slice-5 model is \"global mutate\" with per-command audit attribution.</li><li>The <code>command_sha256</code> field in audit rows is small but powerful — a forensic investigator can reconstruct the exact CLI invocation from the chain. Without it, \"operator X reset breaker Y\" is the audit; with it, \"operator X ran exactly <code>cyberos-ai breaker reset bedrock:claude-3-5-sonnet --confirm</code> as <code>command_sha256:&lt;hash&gt;</code>\" is the audit.</li><li>The <code>--prod-confirmed-aware</code> flag for <code>failover drill</code> is deliberately verbose. The flag name itself is the safety: nobody types it accidentally. Combined with the interactive Y prompt, accidental production drills become structurally impossible.</li><li>The JSON schema versioning (§1 #8) is the consumer-script stability primitive. Without it, every minor output change risks breaking downstream automation. With it, schema evolution is opt-in.</li><li>The <code>secret: true</code> schema annotation (§1 #14) is the operator-data-exposure boundary. Tenant-supplied API keys are real secrets; CLI output flows through ops sessions, terminals, ssh logs — anywhere unintended audiences might see. Defaulting to <code>&lt;REDACTED&gt;</code> is the safe choice; explicit <code>--show-secrets</code> (admin-only, audit-emitting) could be added in slice 6 if needed.</li><li><code>policy diff</code> is the change-management primitive. Production policy changes typically go: PR with new YAML → ops reviews diff via <code>policy diff</code> → approval → operator applies via <code>policy set --confirm</code>. The diff command is the review boundary.</li><li>Shell completions (§1 #15) are a small UX win but a real one — operators discover commands and flags via tab-completion rather than re-reading <code>--help</code>.</li></ul>\n<hr>\n<p><em>End of TASK-AI-021. Status: draft (10/10 target).</em></p>";
