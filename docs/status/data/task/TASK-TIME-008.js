window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TIME-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TIME service <strong>MUST</strong> ship expense capture pipeline at <code>services/time/src/expense/</code> with photo upload → async OCR via TASK-MCP-007 → hóa đơn or generic parsing → Member confirm → policy validation → optional invoice attach, 7-kind enum, 5-state status enum, and 6 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>expense_kind</code> enum: <code>('meal','transport','accommodation','supplies','communication','other_billable','other_non_billable')</code> per DEC-1451. Cardinality 7.</li></ol>\n<ol><li><strong>MUST</strong> define closed <code>expense_status</code> enum: <code>('pending_ocr','pending_member_confirm','confirmed','rejected','invoiced')</code> per DEC-1452. Cardinality 5.</li></ol>\n<ol><li><strong>MUST</strong> define <code>expenses</code> table at migration <code>0009</code>: full row with photo_s3_key, ocr_raw_jsonb, parsed_fields_jsonb (merchant, total_minor, currency, issued_at, vat_amount_minor, mst), confirmed_fields_jsonb (Member-edited final), kind, status, member_subject_id, engagement_id, billable flag, invoice_line_id (when invoiced), trace_id.</li></ol>\n<ol><li><strong>MUST</strong> define <code>expense_policies</code> table at migration <code>0010</code> per DEC-1454: per (engagement, kind) → max_amount_minor, requires_receipt boolean, requires_approval_above_minor, default_billable.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/time/expenses/upload</code> body <code>{ engagement_id, kind, expected_currency }</code>. Handler:</li></ol>\n<ul><li>Returns TASK-DOC-001 presigned S3 URL.</li><li>Creates expense row status='pending_ocr'.</li><li>Emits <code>time.expense_captured</code> sev-3.</li></ul>\n<ol><li><strong>MUST</strong> trigger async OCR via TASK-MCP-007 Tasks on photo-upload-complete webhook from S3:</li></ol>\n<ul><li>Task invokes AWS Textract <code>AnalyzeDocument</code> with <code>FORMS</code> + <code>TABLES</code> features.</li><li>Parses via §1 #7 or §1 #8.</li><li>Transitions status='pending_member_confirm'.</li><li>Notifies Member (push via TASK-PORTAL-007 PWA push).</li><li>Emits <code>time.expense_ocr_completed</code> sev-3 OR <code>time.expense_ocr_failed</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> parse hóa đơn (VN tenants) per DEC-1456 via <code>hoadon_parser.rs</code>:</li></ol>\n<ul><li>Extract MST: regex <code>(?:MST|Mã số thuế)[: ]*(\\d{10,13})</code>.</li><li>Extract total VND: regex over amount-formatted fields.</li><li>Extract issued_at: DD/MM/YYYY pattern.</li><li>Extract supplier_name: header line.</li><li>Extract VAT: 10% line item.</li><li>Confidence scores per field.</li></ul>\n<ol><li><strong>MUST</strong> parse generic receipt (non-VN tenants) per DEC-1456 via <code>generic_parser.rs</code>. Universal fields: merchant_name, total, currency, date.</li></ol>\n<ol><li><strong>MUST</strong> require Member confirm per DEC-1453. <code>POST /v1/time/expenses/{id}/confirm</code> body <code>{ confirmed_fields, kind_override?, billable_override? }</code>. Handler:</li></ol>\n<ul><li>Validates expense status='pending_member_confirm'.</li><li>Persists Member-edited final values.</li><li>Policy check per §1 #10.</li><li>Transitions status='confirmed'.</li><li>Emits <code>time.expense_confirmed</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> validate against engagement policy per DEC-1454. If <code>total_minor &gt; policy.max_amount_minor</code> → return 412 + <code>policy_cap_exceeded</code>. If <code>&gt; policy.requires_approval_above_minor</code> → status remains 'pending_member_confirm' with <code>requires_admin_approval=true</code> field; engagement_admin approves separately.</li></ol>\n<ol><li><strong>MUST</strong> support reject <code>POST /v1/time/expenses/{id}/reject</code> body <code>{ reason }</code>. Transitions status='rejected'. Emits <code>time.expense_rejected</code> sev-3.</li></ol>\n<ol><li><strong>MUST</strong> support invoice attach <code>POST /v1/time/expenses/{id}/attach-to-invoice</code> body <code>{ invoice_id }</code> per DEC-1457 derivative. Caller has <code>cfo</code> or <code>engagement_admin</code>. Handler:</li></ol>\n<ul><li>Validates expense status='confirmed' + billable=true.</li><li>Validates invoice status='draft' or 'ready_for_review'.</li><li>Creates invoice_line row (line_kind='expense_reimbursement') via TASK-INV-001.</li><li>Transitions expense status='invoiced' + populates invoice_line_id.</li><li>Emits <code>time.expense_invoiced</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> emit 6 memory audit kinds per DEC-1457. PII-scrub merchant/supplier name via TASK-MEMORY-111 — hash only in chain.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> persist without Member confirm (per DEC-1453).</li></ol>\n<ol><li><strong>MUST NOT</strong> auto-bill &gt; policy cap (per DEC-1454).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why Member confirm mandatory (§1 #9, DEC-1453)?</strong> OCR errors are common (~5-15% field accuracy issues). Auto-billing wrong amounts to clients = trust + revenue + legal risk. Human-in-loop gate.</p>\n<p><strong>Why TASK-MCP-007 async (§1 #6, DEC-1455)?</strong> Textract analyse is 5-30s; sync would tie up gateway workers. Tasks primitive fits.</p>\n<p><strong>Why VN-specific parser (§1 #7, DEC-1456)?</strong> Hóa đơn fields are regulatory-defined (Decree 123); generic OCR misses MST + VAT structure. VN parser produces tax-compliant data; generic parser is fallback.</p>\n<p><strong>Why per-engagement policies (§1 #10, DEC-1454)?</strong> Different engagements have different reimbursement rules. Per-engagement config respects this; without it, one engagement's rules apply to all (wrong).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0009_expenses.sql\nCREATE TYPE expense_kind AS ENUM ('meal','transport','accommodation','supplies','communication','other_billable','other_non_billable');\nCREATE TYPE expense_status AS ENUM ('pending_ocr','pending_member_confirm','confirmed','rejected','invoiced');\n\nCREATE TABLE expenses (\n  expense_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  member_subject_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  kind expense_kind NOT NULL,\n  status expense_status NOT NULL DEFAULT 'pending_ocr',\n  photo_s3_key TEXT NOT NULL,\n  ocr_raw_jsonb JSONB,\n  parsed_fields_jsonb JSONB,\n  confirmed_fields_jsonb JSONB,\n  total_minor BIGINT,\n  currency billing_currency_enum,\n  issued_at TIMESTAMPTZ,\n  merchant_name TEXT,\n  is_billable BOOLEAN,\n  invoice_line_id UUID,\n  requires_admin_approval BOOLEAN NOT NULL DEFAULT false,\n  reject_reason TEXT,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  confirmed_at TIMESTAMPTZ,\n  invoiced_at TIMESTAMPTZ,\n  trace_id CHAR(32)\n);\nALTER TABLE expenses ENABLE ROW LEVEL SECURITY;\nCREATE POLICY expenses_rls ON expenses\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND (member_subject_id = current_setting('auth.subject_id')::uuid\n              OR EXISTS (SELECT 1 FROM subject_roles WHERE subject_id = current_setting('auth.subject_id')::uuid AND role IN ('engagement_admin','cfo'))))\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND member_subject_id = current_setting('auth.subject_id')::uuid);\nREVOKE DELETE ON expenses FROM cyberos_app;\nGRANT UPDATE (status, ocr_raw_jsonb, parsed_fields_jsonb, confirmed_fields_jsonb,\n              total_minor, currency, issued_at, merchant_name, is_billable,\n              invoice_line_id, requires_admin_approval, reject_reason,\n              confirmed_at, invoiced_at) ON expenses TO cyberos_app;\n\n-- 0010_expense_policies.sql\nCREATE TABLE expense_policies (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  kind expense_kind NOT NULL,\n  max_amount_minor BIGINT,\n  requires_receipt BOOLEAN NOT NULL DEFAULT true,\n  requires_approval_above_minor BIGINT,\n  default_billable BOOLEAN NOT NULL DEFAULT false,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  UNIQUE (engagement_id, kind)\n);\nALTER TABLE expense_policies ENABLE ROW LEVEL SECURITY;\nCREATE POLICY expense_policies_rls ON expense_policies\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\nREVOKE UPDATE, DELETE ON expense_policies FROM cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/time/expenses/upload                            (member)\nPOST   /v1/time/expenses/{id}/confirm                      (member)\nPOST   /v1/time/expenses/{id}/reject                       (member)\nPOST   /v1/time/expenses/{id}/approve                      (engagement_admin)\nPOST   /v1/time/expenses/{id}/attach-to-invoice            (cfo or engagement_admin)\nGET    /v1/time/expenses?status=...&amp;engagement_id=...      (member or admin)\nPOST   /v1/admin/engagements/{id}/expense-policy            (engagement_admin)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>expense_kind cardinality 7</strong>.</li><li><strong>expense_status cardinality 5</strong>.</li><li><strong>Upload returns presigned URL</strong> + status=pending_ocr.</li><li><strong>Async OCR completes</strong> — Textract result populates parsed_fields_jsonb.</li><li><strong>Hóa đơn MST extracted</strong> — VN-format receipt → MST in parsed_fields.</li><li><strong>Generic receipt fallback</strong> — non-VN photo → merchant/total/date extracted.</li><li><strong>Member confirm required</strong> — no persistence until confirm.</li><li><strong>Policy cap enforced</strong> — exceeds max → 412.</li><li><strong>Above approval threshold</strong> — requires_admin_approval=true; status stays pending.</li><li><strong>Invoice attach creates line</strong> — confirmed expense → invoice line via TASK-INV-001.</li><li><strong>Reject transitions</strong> — Member rejects → status=rejected.</li><li><strong>6 memory audit kinds emitted</strong>.</li><li><strong>PII scrub merchant</strong> — name_hash16 in chain only.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>RLS Member-scoped (engagement_admin/cfo broader)</strong>.</li><li><strong>Photo upload max 25 MiB</strong> — S3 upload size limit.</li><li><strong>OCR failure path</strong> — Textract error → status remains pending_ocr; manual entry alternative.</li><li><strong>VN tenant uses hóa đơn parser</strong> — residency='vn-1' → hoadon_parser invoked.</li><li><strong>Non-VN uses generic</strong> — residency≠'vn-1' → generic_parser.</li><li><strong>Cross-tenant denied via RLS</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn upload_returns_presigned_url() {\n    let ctx = TestContext::with_member().await;\n    let r = ctx.post_expense_upload(ctx.eng_id, \"meal\", \"VND\").await;\n    assert_eq!(r.status(), 201);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert!(body[\"upload_url\"].as_str().unwrap().starts_with(\"https://\"));\n    assert_eq!(body[\"status\"], \"pending_ocr\");\n}\n\n#[tokio::test]\nasync fn vn_hoadon_parsed() {\n    let ctx = TestContext::with_vn_member().await;\n    let expense_id = ctx.upload_and_simulate_ocr_hoadon_image().await;\n    ctx.run_ocr_task(expense_id).await;\n    let parsed: serde_json::Value = sqlx::query_scalar(\"SELECT parsed_fields_jsonb FROM expenses WHERE expense_id=$1\")\n        .bind(expense_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert!(parsed[\"mst\"].is_string());\n    assert!(parsed[\"total_vnd\"].is_number());\n}\n\n#[tokio::test]\nasync fn member_confirm_required_before_invoice() {\n    let ctx = TestContext::with_member().await;\n    let expense_id = ctx.complete_ocr().await;\n    let r = ctx.attach_to_invoice(expense_id, ctx.invoice_id).await;\n    assert_eq!(r.status(), 412);  // not yet confirmed\n    ctx.member_confirm(expense_id, /*overrides*/ json!({})).await;\n    let r2 = ctx.attach_to_invoice(expense_id, ctx.invoice_id).await;\n    assert_eq!(r2.status(), 201);\n}\n\n#[tokio::test]\nasync fn policy_cap_blocks() {\n    let ctx = TestContext::with_meal_policy_cap(100_000).await;\n    let expense_id = ctx.complete_ocr_with_amount(150_000).await;\n    let r = ctx.member_confirm(expense_id, json!({})).await;\n    assert_eq!(r.status(), 412);\n}\n\n// 5.5..5.10: enum cardinality, reject, RLS, audit emit, OCR fail</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-CRM-010 (engagement context). <strong>Cross-module:</strong> TASK-DOC-001 (S3 storage), TASK-MCP-007 (async OCR task), TASK-PORTAL-007 (push notification), TASK-INV-001 (invoice attach), TASK-AI-003, TASK-MEMORY-111.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>time.expense_ocr_completed</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.expense_ocr_completed\",\n  \"severity\": 3,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.time.ocr\",\n  \"trace_id\": \"...\",\n  \"payload\": {\n    \"expense_id\": \"0190...\",\n    \"member_subject_id_hash16\": \"f8a1...\",\n    \"parsed_total_minor\": 125000,\n    \"parsed_currency\": \"VND\",\n    \"merchant_name_hash16\": \"9c4e...\",\n    \"ocr_confidence_avg\": 87\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Multi-receipt batch upload — slice 3.</li><li><strong>Deferred:</strong> Per-Member spending dashboard — slice 3.</li><li><strong>Deferred:</strong> Smart-categorise (ML kind prediction) — slice 3.</li><li><strong>Deferred:</strong> Foreign-currency auto-convert at upload — slice 3 (TASK-INV-002 derivative).</li><li><strong>Deferred:</strong> Mileage tracking (transport with start/end coords) — slice 3.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Textract API quota exceeded</td><td>API error</td><td>Status remains pending_ocr; sev-2 alert; manual fallback</td><td>Quota raised or batch later</td></tr><tr><td>Photo upload &gt; 25 MiB</td><td>S3 size check</td><td>413</td><td>Member compresses</td></tr><tr><td>Photo not a receipt (random image)</td><td>confidence &lt; 30%</td><td>Parsed_fields empty; Member edits manually</td><td>Inherent</td></tr><tr><td>MST extraction false-positive (10-digit string elsewhere)</td><td>confidence score</td><td>Member catches at confirm</td><td>Inherent</td></tr><tr><td>Currency mismatch with engagement</td><td>confirm validation</td><td>400 + currency_mismatch</td><td>Member fixes</td></tr><tr><td>Policy cap retro-applied</td><td>check at confirm time</td><td>Inherent</td><td>Member splits or routes to non-billable</td></tr><tr><td>Approval required but engagement_admin unavailable</td><td>sev-3 reminder</td><td>Pending indefinitely until admin acts</td><td>Email reminder</td></tr><tr><td>Invoice attach with wrong currency</td><td>check at attach</td><td>400</td><td>Inherent</td></tr><tr><td>OCR task lost (TASK-MCP-007 crash)</td><td>task retry</td><td>Re-run</td><td>Inherent</td></tr><tr><td>Duplicate expense detection (same receipt twice)</td><td>hash photo bytes</td><td>Sev-3 alert; user prompted</td><td>Inherent</td></tr><tr><td>Multi-language receipt (English VN mix)</td><td>parser fallback</td><td>Generic parser tries first</td><td>Member edits</td></tr><tr><td>Hóa đơn lacks MST (informal receipt)</td><td>extraction null</td><td>Member fills</td><td>Inherent</td></tr><tr><td>Cross-tenant access</td><td>RLS</td><td>403</td><td>Inherent</td></tr><tr><td>Member subject_id changes (rare)</td><td>FK soft</td><td>Expense retained</td><td>Inherent forensic</td></tr><tr><td>Expense rejected after invoice attach</td><td>state check</td><td>409 — already invoiced</td><td>Use correction_to via TASK-INV-001</td></tr><tr><td>TASK-PORTAL-007 push not sent</td><td>best-effort</td><td>Email fallback via TASK-EMAIL-001</td><td>Inherent</td></tr><tr><td>Concurrent confirm + reject</td><td>tx isolation</td><td>First wins</td><td>Inherent</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> AWS Textract FORMS + TABLES features; per-page pricing.</p>\n<p><strong>§11.2</strong> Hóa đơn parser regex maintained in const HOADON_PATTERNS; quarterly review.</p>\n<p><strong>§11.3</strong> Confidence per field; aggregated for UI display.</p>\n<p><strong>§11.4</strong> S3 photo expires 90d post-confirm (storage cost).</p>\n<p><strong>§11.5</strong> Member confirm UI shows OCR result side-by-side with photo for verification.</p>\n<p><strong>§11.6</strong> Policy check evaluates at confirm (not OCR) — allows kind change to affect policy.</p>\n<p><strong>§11.7</strong> Invoice attach delegates to TASK-INV-001 line-add endpoint.</p>\n<p><strong>§11.8</strong> Push notification via TASK-PORTAL-007 with payload <code>{ kind: \"ocr_complete\", expense_id }</code>.</p>\n<p><strong>§11.9</strong> PII: merchant + supplier names hashed; total amount retained in chain (financial context).</p>\n<p><strong>§11.10</strong> Cross-tenant denied via RLS + explicit subject check.</p>\n<hr>\n<p><em>End of TASK-TIME-008 spec.</em></p>";
