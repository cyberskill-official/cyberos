window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EMAIL-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The EMAIL service + portal-web frontend <strong>MUST</strong> ship Missive-style UX — shared inbox, thread state + assignment, internal comments, Genie panel, keyboard shortcuts, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define <code>thread_state</code> per DEC-1610 + DEC-1613 — <code>open | assigned | snoozed | closed | archived</code>. Validated against closed enum (cardinality 5).</li></ol>\n<ol><li><strong>MUST</strong> expose thread-state APIs: ``<code>text POST   /v1/email/threads/{id}/assign        (body: {user_id}) POST   /v1/email/threads/{id}/snooze        (body: {wake_at}) POST   /v1/email/threads/{id}/close POST   /v1/email/threads/{id}/reopen </code>``</li></ol>\n<ol><li><strong>MUST</strong> support internal comments at <code>threads/internal_comments.rs</code>:</li></ol>\n<ul><li><code>POST /v1/email/threads/{id}/comments</code> body <code>{ body, mentions[] }</code></li><li>Renders in thread view inline (visual distinction)</li><li>NEVER included in email reply quote per DEC-1612</li></ul>\n<ol><li><strong>MUST</strong> define table extension at migration <code>0012</code>: ```sql ALTER TABLE threads ADD COLUMN state TEXT NOT NULL DEFAULT 'open' CHECK (state IN ('open','assigned','snoozed','closed','archived')); ALTER TABLE threads ADD COLUMN assigned_to UUID; ALTER TABLE threads ADD COLUMN snoozed_until TIMESTAMPTZ; ALTER TABLE threads ADD COLUMN closed_at TIMESTAMPTZ; ALTER TABLE threads ADD COLUMN closed_by UUID; CREATE INDEX threads_state_assigned_idx ON threads(tenant_id, state, assigned_to) WHERE state IN ('open','assigned'); GRANT UPDATE (state, assigned_to, snoozed_until, closed_at, closed_by, updated_at) ON threads TO cyberos_app;</li></ol>\n<p>   CREATE TABLE thread_comments (      comment_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      thread_id UUID NOT NULL,      author_id UUID NOT NULL,      body TEXT NOT NULL,      mentions UUID[] NOT NULL DEFAULT '{}',      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    ALTER TABLE thread_comments ENABLE ROW LEVEL SECURITY;    CREATE POLICY thread_comments_rls ON thread_comments      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON thread_comments FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> wake snoozed threads at <code>snoozed_until</code> via TASK-MCP-007 cron — flip <code>state</code> to <code>open</code>, notify assignee via TASK-CHAT-005.</li></ol>\n<ol><li><strong>MUST</strong> render frontend at <code>services/portal-web/src/email/</code>:</li></ol>\n<ul><li><code>InboxView.tsx</code>: channel selector (left), thread list (middle)</li><li><code>ThreadView.tsx</code>: thread rendering + reply composer (right or full)</li><li><code>AssignmentPicker.tsx</code>: avatar grid + search</li><li><code>SnoozePicker.tsx</code>: chips (1h, 4h, tomorrow, next week, custom)</li><li><code>InternalCommentEditor.tsx</code>: separate composer, visual distinction</li><li><code>GenieActionsPanel.tsx</code>: streams from TASK-EMAIL-008 + quick-actions</li></ul>\n<ol><li><strong>MUST</strong> wire keyboard shortcuts per DEC-1615 at <code>keyboard_shortcuts.ts</code>:</li></ol>\n<ul><li><code>j/k</code> navigate</li><li><code>r</code> reply, <code>R</code> reply-all</li><li><code>f</code> forward</li><li><code>a</code> assign focus</li><li><code>z</code> snooze focus</li><li><code>e</code> archive/close</li><li><code>g</code> Genie panel focus</li><li><code>Escape</code> clears focus</li><li>All disabled when text input focused</li></ul>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1616. PII per TASK-MEMORY-111: comment body SHA-256 hashed; mentions (uuids) ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from UI action → backend mutation → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> include internal comments in email reply quote per DEC-1612 — <code>Reply</code> composer pulls thread.messages only, not thread.comments.</li></ol>\n<ol><li><strong>MUST NOT</strong> send email notification on assignment per DEC-1611 — in-app + TASK-CHAT-005 mention only.</li></ol>\n<ol><li><strong>MUST NOT</strong> show closed/archived threads in default inbox view — separate filter chip.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why shared inbox (DEC-1610)?</strong> Single-user email apps (Apple Mail) can't support team handoff; Missive's channel model is industry-validated.</p>\n<p><strong>Why one assignee (DEC-1611)?</strong> Multiple assignees → diffusion of responsibility; Missive's single-assignee model proves better SLA.</p>\n<p><strong>Why no email notif on assignment (DEC-1611)?</strong> Customer doesn't need to see \"Stephen reassigned to Lisa\" emails; internal-only via TASK-CHAT-005.</p>\n<p><strong>Why never include internal comments in reply (DEC-1612)?</strong> Single most catastrophic bug class in collab email tools; hard contract.</p>\n<p><strong>Why keyboard shortcuts (DEC-1615)?</strong> Power users 3-5x faster than mouse; Missive's shortcut grammar is well-known.</p>\n<p><strong>Why Genie panel right-side (DEC-1614)?</strong> Active context without disrupting reading flow; consistent with TASK-PORTAL-005 chat layout.</p>\n<hr>\n<h2>§3 — API contract (see §1.2 + §1.3)</h2>\n<p>Sample thread state response:</p>\n<pre><code class=\"language-json\">{\n  \"thread_id\": \"uuid\",\n  \"state\": \"assigned\",\n  \"assigned_to\": \"uuid\",\n  \"assigned_to_name\": \"Lisa Nguyen\",\n  \"snoozed_until\": null,\n  \"message_count\": 5,\n  \"internal_comment_count\": 2,\n  \"last_message_at\": \"2026-05-17T10:00:00Z\"\n}</code></pre>\n<p>Sample comment add:</p>\n<pre><code class=\"language-json\">POST /v1/email/threads/{id}/comments\n{\n  \"body\": \"@Lisa can you handle this? Customer is asking about pricing\",\n  \"mentions\": [\"uuid-lisa\"]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>5 thread states + cardinality test</strong>. 2. <strong>One assignee at a time</strong>. 3. <strong>Assignment no customer-facing email</strong>. 4. <strong>Internal comments visible in thread view</strong>. 5. <strong>Internal comments NEVER in Reply quote</strong>. 6. <strong>Snooze wakes at <code>wake_at</code> via cron</strong>. 7. <strong>Snoozed thread invisible in default inbox</strong>. 8. <strong>5 memory audit kinds emitted</strong>. 9. <strong>PII scrubbed (comment body SHA256)</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>Mentions trigger TASK-CHAT-005 notification</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>Keyboard shortcuts work (j/k/r/f/a/z/e/g)</strong>. 14. <strong>Shortcuts disabled in text inputs</strong>. 15. <strong>Genie panel streams TASK-EMAIL-008 proposals</strong>. 16. <strong>Channel selector lists tenant inboxes</strong>. 17. <strong>Reply composer pulls thread.messages only (no comments)</strong>. 18. <strong>Append-only thread_comments table</strong>. 19. <strong>Closed/archived hidden by default</strong>. 20. <strong>Wake from snooze sends TASK-CHAT-005 ping to assignee</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn assign_no_customer_email() {\n    let ctx = TestContext::with_thread().await;\n    ctx.assign_thread(ctx.thread_id, ctx.user_b).await;\n    let sent_emails = ctx.outbound_send_count().await;\n    assert_eq!(sent_emails, 0);\n}\n\n#[tokio::test]\nasync fn internal_comment_not_in_reply() {\n    let ctx = TestContext::with_thread_and_comments().await;\n    let reply_quote = ctx.compose_reply_quote(ctx.thread_id).await;\n    assert!(!reply_quote.contains(\"INTERNAL_FLAG_XYZ\"));\n}\n\n#[tokio::test]\nasync fn snooze_wakes_at_target() {\n    let ctx = TestContext::with_thread().await;\n    let wake = Utc::now() + Duration::seconds(2);\n    ctx.snooze_thread(ctx.thread_id, wake).await;\n    tokio::time::sleep(Duration::from_secs(3)).await;\n    ctx.run_snooze_cron().await;\n    let t: Thread = ctx.fetch_thread(ctx.thread_id).await;\n    assert_eq!(t.state, \"open\");\n}\n\n// 5.4..5.10</code></pre>\n<pre><code class=\"language-ts\">test('keyboard shortcut r opens reply composer', async ({page}) =&gt; {\n  await page.goto('/email/inbox');\n  await page.keyboard.press('j');  // focus first thread\n  await page.keyboard.press('r');\n  await expect(page.locator('[data-testid=reply-composer]')).toBeVisible();\n});</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-EMAIL-001, TASK-EMAIL-009. <strong>Cross-module:</strong> TASK-EMAIL-008 (Genie panel), TASK-EMAIL-006 (CRM contact display), TASK-EMAIL-007 (convert button), TASK-CHAT-005 (mention notif), TASK-CUO-101 (panel embedding), TASK-MCP-007 (snooze cron), TASK-MEMORY-111 (PII).</p>\n<h2>§8 — Sample payloads (see §3)</h2>\n<h2>§9 — Open questions</h2>\n<p>None blocking — Missive is the gold-standard reference.</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Concurrent assignment</td><td>optimistic lock on version</td><td>second 409</td><td>UI refresh + retry</td></tr><tr><td>Snooze cron missed run</td><td>last_run check</td><td>wake on next boot</td><td>inherent</td></tr><tr><td>Mention user doesn't exist</td><td>validate</td><td>filter + sev-3 audit</td><td>data fix</td></tr><tr><td>Internal comment &gt;10k chars</td><td>validate</td><td>400</td><td>use TASK-DOC-001 for long</td></tr><tr><td>Reply quote source includes comment (bug)</td><td>test guard</td><td>hard CI block</td><td>tests catch</td></tr><tr><td>TASK-CHAT-005 unreachable</td><td>mention notif retry</td><td>inherent</td><td>inherent</td></tr><tr><td>Genie panel stream disconnects</td><td>reconnect</td><td>retry</td><td>inherent</td></tr><tr><td>Snoozed past 1 year</td><td>warn at create</td><td>UI alert</td><td>manual confirm</td></tr><tr><td>Channel ACL mismatch</td><td>RLS</td><td>403</td><td>request access</td></tr><tr><td>Cross-tenant URL guess</td><td>RLS</td><td>404</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Thread state machine: open ↔ assigned, any → snoozed → wake → open/assigned, open/assigned → closed → reopen ↔ archived.</li><li>§11.2 Internal comment markdown rendering via <code>marked</code> + DOMPurify sanitize.</li><li>§11.3 Keyboard shortcuts: use <code>mousetrap</code> or custom dispatcher; respect <code>&lt;input&gt;</code>/<code>&lt;textarea&gt;</code> focus.</li><li>§11.4 Genie panel uses SSE/WebSocket for live TASK-EMAIL-008 stream.</li><li>§11.5 memory audit body: thread_id, state, assignee uuid; comment body SHA256.</li><li>§11.6 Snooze cron via TASK-MCP-007 <code>kind: 'email.snooze_wake'</code>, runs every 5min.</li></ul>\n<hr>\n<p><em>End of TASK-EMAIL-003 spec.</em></p>";
