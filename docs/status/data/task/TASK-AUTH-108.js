window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-108"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> extend TASK-AUTH-004 JWT shape with Lumi-specific claims for cross-tenant sync identity. Each requirement:</p>\n<ol><li><strong>MUST</strong> add the following 5 claims to JWTs issued via the <code>/v1/auth/lumi/issue</code> endpoint (per DEC-420):</li></ol>\n<ul><li><code>agent_persona: TEXT</code> — format <code>cuo-&lt;persona-key&gt;@&lt;semver&gt;</code> per DEC-421.</li><li><code>tenant_residency: TEXT</code> — one of <code>vn-1 | sg-1 | eu-1 | us-1</code> per DEC-422.</li><li><code>lumi_org_tenant: TEXT</code> — slug of the org-tenant this Lumi persona represents (per DEC-423).</li><li><code>persona_version: TEXT</code> — semver matching TASK-AI-014 pinned version (per DEC-424).</li><li><code>sync_class_allowed: TEXT[]</code> — closed-set array per DEC-425. Existing TASK-AUTH-004 claims (<code>sub</code>, <code>tid</code>, <code>iss</code>, <code>iat</code>, <code>exp</code>, <code>nbf</code>, <code>roles</code>, <code>rbac_v</code>) preserved unchanged.</li></ul>\n<ol><li><strong>MUST</strong> issue Lumi tokens with distinct <code>iss: https://lumi.cyberos.world</code> and <code>aud: https://memory.cyberos.world/sync</code> (per DEC-426 + DEC-427). Per-tenant tokens (TASK-AUTH-004) continue using <code>iss: https://auth.&lt;tenant&gt;.cyberos.world</code> and tenant-specific audiences. Mismatch on either → 401 + <code>auth.lumi_token_rejected</code> memory row.</li></ol>\n<ol><li><strong>MUST</strong> enforce that <code>agent_persona</code> claim's persona-key ∈ TASK-AUTH-101 agent-persona-role-family enum (per DEC-421). Verifier parses prefix <code>cuo-</code> + key + <code>@</code> + semver; invalid format → 401 <code>agent_persona_malformed</code>; unknown persona-key → 401 <code>agent_persona_unknown</code>.</li></ol>\n<ol><li><strong>MUST</strong> verify <code>persona_version</code> against the live version registry from TASK-AI-014 (per DEC-424 + DEC-429). Verifier compares token's <code>persona_version</code> against <code>cuo-&lt;persona-key&gt;@&lt;live-semver&gt;</code> from the registry; if token's version is more than 2 minor versions behind → 401 <code>persona_version_stale</code> + emit <code>auth.lumi_persona_version_stale</code> memory row (sev-2 — signals refresh-token reuse beyond rotation window).</li></ol>\n<ol><li><strong>MUST</strong> enforce <code>sync_class_allowed</code> is a closed-set array (per DEC-425). Allowed values: <code>'private'</code>, <code>'shareable'</code>, <code>'publishable'</code>, <code>'shared'</code>, <code>'client-visible'</code>. Unknown values → 401 <code>sync_class_unknown</code>. Empty array → 401 <code>sync_class_empty</code> (token must permit at least one class).</li></ol>\n<ol><li><strong>MUST</strong> verify <code>tenant_residency</code> claim matches the routing residency of the request (per DEC-422). The memory sync endpoint's host determines the expected residency (e.g. <code>memory.vn-1.cyberos.world</code> expects <code>tenant_residency=vn-1</code>); mismatch → 451 <code>unavailable_for_legal_reasons</code> + emit <code>auth.lumi_token_rejected</code> memory row with <code>reason='residency_mismatch'</code>.</li></ol>\n<ol><li><strong>MUST</strong> pin Lumi JWT alg at RS256 (per DEC-431). Reject alg=none ALWAYS. Reject alg=HS256 ALWAYS (JWT-confusion attack: attacker uses JWKS public key as HS256 secret). Unknown alg → 401 <code>unsupported_alg</code>.</li></ol>\n<ol><li><strong>MUST</strong> restrict Lumi token issuance to subjects whose <code>roles</code> claim contains <code>agent-persona</code> per TASK-AUTH-101 (per DEC-432). Human subjects (those without <code>agent-persona</code> role) attempting issuance → 403 <code>human_cannot_issue_lumi</code>. Delegation flow (a human authorising a Lumi issuance on their behalf) deferred to task-AUTH-2xx.</li></ol>\n<ol><li><strong>MUST</strong> include <code>anchor_chain_hash: TEXT</code> claim (per DEC-433) — the source tenant's memory chain head at token issuance time. The memory sync endpoint validates this against the current chain head; if the source chain has diverged → 409 <code>chain_diverged</code> + emit <code>auth.lumi_token_rejected</code> with <code>reason='chain_diverged'</code>.</li></ol>\n<ol><li><strong>MUST</strong> define <code>lumi_token_issuance_log</code> table: <code>(id BIGSERIAL PRIMARY KEY, issuer_subject_id UUID NOT NULL REFERENCES auth.subjects(id), agent_persona TEXT NOT NULL, persona_version TEXT NOT NULL, source_tenant_id UUID NOT NULL, lumi_org_tenant TEXT NOT NULL, residency TEXT NOT NULL, sync_class_allowed TEXT[] NOT NULL, anchor_chain_hash TEXT NOT NULL, ttl_seconds INT NOT NULL DEFAULT 3600, issued_at TIMESTAMPTZ NOT NULL DEFAULT now(), token_jti TEXT NOT NULL UNIQUE)</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> (per DEC-430).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with both <code>USING</code> and <code>WITH CHECK</code> on <code>lumi_token_issuance_log</code>. Policy: <code>source_tenant_id = current_setting('auth.tenant_id')::uuid OR current_setting('auth.is_root_admin', true) = 'true'</code> (root-admin can see all for audit).</li></ol>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/lumi/issue</code> handler. Body: <code>{agent_persona: \"&lt;key&gt;\", persona_version: \"&lt;semver&gt;\", lumi_org_tenant: \"&lt;slug&gt;\", sync_class_allowed: [...], anchor_chain_hash: \"&lt;hex&gt;\"}</code>. Caller MUST have role <code>agent-persona</code> per TASK-AUTH-101. Validates all fields against closed sets + persona_version against registry. Returns <code>{token: \"&lt;jwt&gt;\", expires_at: &lt;iso8601&gt;}</code>. Emits <code>auth.lumi_token_issued</code> memory row.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>GET /v1/auth/lumi/verify</code> handler used by memory sync + downstream consumers. Header: <code>Authorization: Bearer &lt;token&gt;</code>. Returns <code>{valid: bool, claims: &lt;object&gt;, reason: &lt;text?&gt;}</code>. Internal-use only (not exposed externally); the verifier library is the canonical path.</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit row kinds (per DEC-428):</li></ol>\n<ul><li><code>auth.lumi_token_issued</code> — every successful issuance; carries persona, version, residency, sync_class, ttl, jti.</li><li><code>auth.lumi_token_verified</code> — sampled at 1% for cost reasons (high-volume); 100% on outcome != success.</li><li><code>auth.lumi_token_rejected</code> — every verification failure; carries reason (residency_mismatch | chain_diverged | persona_version_stale | unsupported_alg | aud_mismatch | iss_mismatch | sync_class_unknown | agent_persona_unknown).</li><li><code>auth.lumi_persona_version_stale</code> — sev-2 alarm trigger; emitted when verifier sees a persona_version &gt; 2 minor versions behind.</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>lumi_org_tenant</code> slug via TASK-MEMORY-111 before chain commit (treated as PII at the cross-tenant boundary).</li></ol>\n<ol><li><strong>MUST</strong> complete Lumi token issuance in ≤ 100 ms p95 (signing key + JWKS already in process). Verification in ≤ 50 ms p95 (signature verify + claim parse + persona-version lookup from in-memory registry).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>auth.lumi.{issue,verify}</code> with attributes: <code>tenant_id</code>, <code>agent_persona</code>, <code>persona_version</code>, <code>lumi_org_tenant</code>, <code>outcome</code> (success | residency_mismatch | chain_diverged | persona_version_stale | aud_mismatch | iss_mismatch | unsupported_alg | sync_class_unknown | agent_persona_unknown | human_cannot_issue).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_lumi_issuance_total{tenant_id, agent_persona, outcome}</code> (counter).</li><li><code>auth_lumi_verification_total{outcome, reason}</code> (counter).</li><li><code>auth_lumi_persona_version_stale_total{persona, stale_versions_behind}</code> (counter; sev-2 alarm at sustained &gt; 5/h).</li><li><code>auth_lumi_active_tokens{tenant_id, persona}</code> (gauge — periodic count of unexpired tokens from log).</li><li><code>auth_lumi_token_latency_ms{op}</code> (histogram; op ∈ {issue, verify}).</li></ul>\n<ol><li><strong>MUST</strong> sign Lumi JWT with the SAME RS256 keypair used by TASK-AUTH-004 (issuer-distinct via the <code>iss</code> claim; key-distinct deferred to slice 2). The kid is the same; the iss differs — verifiers must check both.</li></ol>\n<ol><li><strong>MUST</strong> include <code>jti: TEXT</code> claim (JWT ID — RFC 7519) as a UUIDv4. Stored in <code>lumi_token_issuance_log.token_jti</code> for revocation lookup. Revocation API ships in task-AUTH-2xx; this task ships the column.</li></ol>\n<ol><li><strong>MUST</strong> ship the <code>LumiClaims</code> Rust struct with all 5 new claims + existing 8 TASK-AUTH-004 claims. Total 13 claims. Serialisation order: existing 8 first (preserves backward compat for any TASK-AUTH-004 verifier inspecting only the standard claims), then Lumi additions.</li></ol>\n<ol><li><strong>MUST</strong> validate that <code>lumi_org_tenant</code> slug matches the pattern <code>^[a-z][a-z0-9-]{2,40}[a-z0-9]$</code> per TASK-TEN-001's slug regex. Malformed → 401 <code>lumi_org_tenant_malformed</code>.</li></ol>\n<ol><li><strong>MUST</strong> support TTL configurable via env <code>LUMI_TOKEN_TTL_SECONDS</code> with default 3600 (per DEC-429). Operators MAY shorten for high-security tenants; values &lt; 300 (5min) → reject env config at startup.</li></ol>\n<ol><li><strong>MUST</strong> include <code>anchor_chain_hash</code> validation flow: the memory sync endpoint (out of scope here, ships in task-MEMORY-2xx) reads the claim + compares against the current chain head; this task ships the claim emission + verifier-side parsing.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>sync_class_allowed</code> ⊆ what the source tenant's policy permits. The per-tenant policy YAML (TASK-AI-005) declares per-tenant <code>lumi_sync_max_classes</code>. If requested array exceeds policy → 403 <code>sync_class_exceeds_policy</code> + emit <code>auth.lumi_token_rejected</code> with reason.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why distinct iss for Lumi tokens (DEC-426)?</strong> Per-tenant AUTH compromise should not let attackers forge cross-tenant Lumi tokens. Distinct iss means even if a tenant's AUTH cluster is breached, the attacker still needs to forge tokens with <code>iss=https://lumi.cyberos.world</code> — that's a separate trust anchor. Verifiers check iss + tenant_id mapping; mismatch fails closed.</p>\n<p><strong>Why distinct aud (DEC-427)?</strong> Audience-bound tokens (RFC 7519 §4.1.3) prevent token misuse: a Lumi token meant for <code>memory.cyberos.world/sync</code> cannot be replayed against <code>api.cyberos.world/v1/...</code>. Bounding audience to sync endpoints contains the blast radius of any compromise.</p>\n<p><strong>Why alg=HS256 rejected (DEC-431, §1 #7)?</strong> Classic JWT-confusion attack: attacker takes a JWKS public key, signs a token using HS256 with the public key as the HMAC secret, and the verifier (if it accepts HS256) validates because the \"secret\" matches. The defense is to pin alg=RS256 — rejecting HS256 always means the attack cannot succeed regardless of verifier bugs.</p>\n<p><strong>Why human subjects can't issue Lumi tokens at slice 1 (DEC-432, §1 #8)?</strong> Lumi is an AI persona; tokens represent AI-initiated cross-tenant operations. A human tenant-admin authenticating + then issuing a Lumi token would be claiming to be an AI agent — defeats the EU AI Act Art. 13 transparency requirement (which clearly distinguishes AI actors from human actors). Restricting issuance to <code>agent-persona</code> role subjects keeps the boundary clean. Delegation (human authorises a specific Lumi sync on their behalf) is a slice 3 ADR-gated flow.</p>\n<p><strong>Why persona_version staleness check at 2 minor versions (DEC-424, §1 #4)?</strong> Persona prompts evolve; a 2-minor-version-stale token suggests the verifier's persona registry has updated but the token-issuing process hasn't refreshed. Tolerance of 2 minors covers normal rotation lag (~hours); beyond that signals operational issue. Sev-2 alarm prompts ops investigation.</p>\n<p><strong>Why sync_class_allowed closed enum (DEC-425, §1 #5)?</strong> AGENTS.md §15 defines the closed sync-class set. Allowing arbitrary strings would let tokens claim privileges that don't exist (silently treated as \"no privilege\" or worse). Closed-set validation at issuance time catches typos at the boundary.</p>\n<p><strong>Why anchor_chain_hash claim (DEC-433, §1 #9)?</strong> Lumi sync writes to memory. If an attacker captures a Lumi token and replays it after the source tenant's chain has advanced (e.g. with newer rows), the replay would write at a stale head — silently corrupting the chain. The anchor_chain_hash binds the token to a specific chain state; mismatch rejects the replay.</p>\n<p><strong>Why same signing key as TASK-AUTH-004 at slice 1 (§1 #19)?</strong> Operationally simpler; one key rotation flow. Cryptographically the distinct iss + aud claims provide the security boundary (an attacker forging an TASK-AUTH-004 token can't get the iss accepted by the Lumi verifier). Distinct key per identity-class is a slice 2 hardening.</p>\n<p><strong>Why TTL 1 hour configurable (DEC-429, §1 #23)?</strong> Lumi sync is a periodic activity (typically every 5-15 minutes). 1-hour TTL gives reasonable refresh cadence without making compromised tokens long-lived. Operators with high-security needs can tighten via env; lower bound of 5 minutes prevents pathological config.</p>\n<p><strong>Why jti claim with log row (§1 #20)?</strong> Revocation requires looking up \"is this jti revoked?\". Storing every issued jti in the log enables this. The revocation API (task-AUTH-2xx) consults the log. Slice 1 ships the column + log; revocation lookup is slice 2.</p>\n<p><strong>Why tenant_residency check at verifier (§1 #6, DEC-422)?</strong> Decree 53/2022 + GDPR + DORA require data residency enforcement at every operation. If a Lumi token issued in vn-1 hits a sync endpoint in eu-1, that's a cross-residency operation — must be rejected even if the token is otherwise valid. The 451 status code (RFC 7725) is the spec-correct response for legal-reason rejection.</p>\n<p><strong>Why <code>lumi_org_tenant</code> is a separate claim from <code>tid</code> (DEC-423, §1 #1)?</strong> <code>tid</code> is the regular tenant id of the issuing AUTH; <code>lumi_org_tenant</code> is the slug of the org-tenant this Lumi persona represents. These can differ: a Lumi sync from a customer tenant pushing to the CyberSkill org-tenant has <code>tid=&lt;customer-uuid&gt;</code> and <code>lumi_org_tenant=cyberskill</code>. Two-field design makes the cross-tenant relationship explicit.</p>\n<p><strong>Why <code>auth.lumi_token_verified</code> sampled at 1% (§1 #14)?</strong> High-volume hot path; 100% sampling would flood memory. 1% gives statistical coverage; 100% on <code>outcome != success</code> ensures every failure is captured for debugging.</p>\n<p><strong>Why <code>sync_class_allowed</code> validated against per-tenant policy (§1 #25)?</strong> A tenant may restrict their data's max sync class (e.g. \"we never publish externally — sync_class cap = 'shared'\"). The issuance handler enforces; tokens cannot grant more than policy permits. Defense in depth — the memory sync endpoint also checks at write time, but rejecting at issuance time saves the round-trip.</p>\n<p><strong>Why slug pattern validation for <code>lumi_org_tenant</code> (§1 #22)?</strong> Slug is used in cross-tenant routing + memory audit paths; malformed values cause hard-to-debug failures downstream. Pattern validation at JWT issuance time + JWT verification time catches typos at the boundary.</p>\n<p><strong>Why total 13 claims (§1 #21)?</strong> Extending TASK-AUTH-004's 8 with 5 Lumi-specific claims is the minimum complete set. Adding fewer would force consumers to infer; adding more bloats the token. The serialisation order preserves TASK-AUTH-004-compatible read paths.</p>\n<p><strong>Why same kid for TASK-AUTH-004 + Lumi at slice 1 (§1 #19)?</strong> Simplification — JWKS verification just needs to find the kid; iss + aud provide the boundary. Per-identity-class kid (slice 2) provides defense-in-depth at the cost of dual-key rotation procedures.</p>\n<p><strong>Why sev-2 alarm on persona_version_stale &gt; 5/h (§1 #14, §1 #18)?</strong> Normal rotation produces 0-1 stale tokens/hour; &gt; 5/h sustained signals (a) version-registry update not propagated to issuer, (b) compromised token reuse beyond refresh window, (c) misconfigured caching. Sev-2 prompts operator investigation.</p>\n<p><strong>Why no Lumi token revocation handler at slice 1 (§1 #20)?</strong> Revocation is a separable concern — needs a CRL endpoint + propagation mechanism + cache invalidation. Slice 1 ships the jti column; task-AUTH-2xx ships the revocation flow. Until then, short TTL (1h) is the mitigation.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0013</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0013_lumi_token_issuance_log.sql\n\nBEGIN;\n\nCREATE TABLE lumi_token_issuance_log (\n    id                       BIGSERIAL    PRIMARY KEY,\n    issuer_subject_id        UUID         NOT NULL REFERENCES auth.subjects(id) ON DELETE RESTRICT,\n    agent_persona            TEXT         NOT NULL CHECK (agent_persona ~ '^cuo-[a-z][a-z0-9-]*@[0-9]+\\.[0-9]+\\.[0-9]+$'),\n    persona_version          TEXT         NOT NULL,\n    source_tenant_id         UUID         NOT NULL,\n    lumi_org_tenant          TEXT         NOT NULL CHECK (lumi_org_tenant ~ '^[a-z][a-z0-9-]{2,40}[a-z0-9]$'),\n    residency                TEXT         NOT NULL CHECK (residency IN ('vn-1','sg-1','eu-1','us-1')),\n    sync_class_allowed       TEXT[]       NOT NULL CHECK (array_length(sync_class_allowed, 1) &gt;= 1),\n    anchor_chain_hash        TEXT         NOT NULL CHECK (anchor_chain_hash ~ '^[0-9a-f]{64}$'),\n    ttl_seconds              INT          NOT NULL DEFAULT 3600 CHECK (ttl_seconds BETWEEN 300 AND 86400),\n    issued_at                TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    token_jti                TEXT         NOT NULL UNIQUE\n);\n\nCREATE INDEX lumi_log_tenant_issued_idx ON lumi_token_issuance_log (source_tenant_id, issued_at DESC);\nCREATE INDEX lumi_log_persona_idx ON lumi_token_issuance_log (agent_persona, persona_version);\n\nALTER TABLE lumi_token_issuance_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY lumi_log_tenant_iso ON lumi_token_issuance_log\n    USING (source_tenant_id = current_setting('auth.tenant_id')::uuid\n           OR current_setting('auth.is_root_admin', true) = 'true')\n    WITH CHECK (source_tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON lumi_token_issuance_log FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.2 — Lumi claims struct</h3>\n<pre><code class=\"language-rust\">// services/auth/src/lumi/claims.rs\nuse serde::{Deserialize, Serialize};\nuse uuid::Uuid;\nuse crate::lumi::sync_class::SyncClass;\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct LumiClaims {\n    // TASK-AUTH-004 baseline claims\n    pub sub: Uuid,\n    pub tid: Uuid,\n    pub iss: String,        // always \"https://lumi.cyberos.world\" for Lumi tokens\n    pub aud: String,        // always \"https://memory.cyberos.world/sync\"\n    pub iat: i64,\n    pub exp: i64,\n    pub nbf: i64,\n    pub roles: Vec&lt;String&gt;,\n    pub rbac_v: u32,\n    pub jti: String,        // RFC 7519 JWT ID (UUIDv4)\n\n    // Lumi-specific claims (DEC-420)\n    pub agent_persona: String,         // \"cuo-&lt;key&gt;@&lt;semver&gt;\"\n    pub tenant_residency: String,      // vn-1 | sg-1 | eu-1 | us-1\n    pub lumi_org_tenant: String,       // slug\n    pub persona_version: String,       // semver\n    pub sync_class_allowed: Vec&lt;SyncClass&gt;,\n    pub anchor_chain_hash: String,     // 64-hex\n}\n\nimpl LumiClaims {\n    pub fn validate_shape(&amp;self) -&gt; Result&lt;(), LumiClaimError&gt; {\n        if self.iss != \"https://lumi.cyberos.world\" {\n            return Err(LumiClaimError::IssMismatch);\n        }\n        if self.aud != \"https://memory.cyberos.world/sync\" {\n            return Err(LumiClaimError::AudMismatch);\n        }\n        crate::lumi::persona_version::validate_format(&amp;self.agent_persona)?;\n        crate::lumi::sync_class::validate_array(&amp;self.sync_class_allowed)?;\n        if !slug_re().is_match(&amp;self.lumi_org_tenant) {\n            return Err(LumiClaimError::LumiOrgTenantMalformed);\n        }\n        if !chain_hash_re().is_match(&amp;self.anchor_chain_hash) {\n            return Err(LumiClaimError::AnchorChainHashMalformed);\n        }\n        Ok(())\n    }\n}</code></pre>\n<h3>3.3 — Sync class enum</h3>\n<pre><code class=\"language-rust\">// services/auth/src/lumi/sync_class.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]\n#[serde(rename_all = \"kebab-case\")]\npub enum SyncClass {\n    Private,\n    Shareable,\n    Publishable,\n    Shared,\n    ClientVisible,\n}\n\nimpl SyncClass {\n    pub const ALL: &amp;'static [SyncClass] = &amp;[\n        SyncClass::Private, SyncClass::Shareable, SyncClass::Publishable,\n        SyncClass::Shared, SyncClass::ClientVisible,\n    ];\n}\n\npub fn validate_array(arr: &amp;[SyncClass]) -&gt; Result&lt;(), LumiClaimError&gt; {\n    if arr.is_empty() {\n        return Err(LumiClaimError::SyncClassEmpty);\n    }\n    Ok(())\n}</code></pre>\n<h3>3.4 — Verifier</h3>\n<pre><code class=\"language-rust\">// services/auth/src/lumi/verifier.rs\nuse jsonwebtoken::{Algorithm, DecodingKey, Validation, decode_header, decode};\nuse crate::lumi::claims::LumiClaims;\nuse crate::lumi::persona_version::PersonaVersionRegistry;\n\nconst LUMI_ISS: &amp;str = \"https://lumi.cyberos.world\";\nconst LUMI_AUD: &amp;str = \"https://memory.cyberos.world/sync\";\nconst PERSONA_VERSION_STALE_THRESHOLD: u32 = 2; // minor versions behind\n\npub fn verify_lumi_token(\n    token: &amp;str,\n    jwks: &amp;crate::oidc::jwks::JwksCache,\n    persona_registry: &amp;PersonaVersionRegistry,\n    expected_residency: &amp;str,\n) -&gt; Result&lt;LumiClaims, LumiVerifyError&gt; {\n    // 1. Header check — alg pinned at RS256 (DEC-431)\n    let header = decode_header(token).map_err(|_| LumiVerifyError::TokenMalformed)?;\n    if header.alg != Algorithm::RS256 {\n        return Err(LumiVerifyError::UnsupportedAlg(format!(\"{:?}\", header.alg)));\n    }\n    let kid = header.kid.as_deref().ok_or(LumiVerifyError::KidMissing)?;\n    let jwk = jwks.get(kid).ok_or(LumiVerifyError::UnknownKid)?;\n\n    // 2. Build decoder\n    let key = DecodingKey::from_rsa_components(\n        jwk.n.as_deref().ok_or(LumiVerifyError::JwkMalformed)?,\n        jwk.e.as_deref().ok_or(LumiVerifyError::JwkMalformed)?,\n    ).map_err(|_| LumiVerifyError::JwkMalformed)?;\n\n    let mut validation = Validation::new(Algorithm::RS256);\n    validation.set_issuer(&amp;[LUMI_ISS]);\n    validation.set_audience(&amp;[LUMI_AUD]);\n    validation.leeway = 60;\n\n    // 3. Decode + validate signature/iss/aud/exp/nbf\n    let decoded = decode::&lt;LumiClaims&gt;(token, &amp;key, &amp;validation)\n        .map_err(|e| LumiVerifyError::TokenValidation(format!(\"{e:?}\")))?;\n\n    let claims = decoded.claims;\n    claims.validate_shape().map_err(LumiVerifyError::Shape)?;\n\n    // 4. Residency check (DEC-422)\n    if claims.tenant_residency != expected_residency {\n        return Err(LumiVerifyError::ResidencyMismatch {\n            expected: expected_residency.into(), got: claims.tenant_residency.clone(),\n        });\n    }\n\n    // 5. Persona-version staleness check (DEC-424)\n    let live_version = persona_registry\n        .get_live_version(&amp;claims.agent_persona)\n        .ok_or_else(|| LumiVerifyError::PersonaUnknown(claims.agent_persona.clone()))?;\n    let behind = persona_registry.minor_versions_behind(&amp;claims.persona_version, &amp;live_version);\n    if behind &gt; PERSONA_VERSION_STALE_THRESHOLD {\n        return Err(LumiVerifyError::PersonaVersionStale {\n            token_version: claims.persona_version.clone(),\n            live_version,\n            minor_versions_behind: behind,\n        });\n    }\n\n    Ok(claims)\n}</code></pre>\n<h3>3.5 — Issuer handler</h3>\n<pre><code class=\"language-rust\">// services/auth/src/handlers/lumi.rs\nuse axum::{Json, extract::State, http::StatusCode};\nuse cyberos_auth::rbac::Role;\nuse crate::lumi::{claims::LumiClaims, sync_class::SyncClass, repo, audit};\n\n#[derive(Deserialize)]\npub struct IssueRequest {\n    pub agent_persona: String,\n    pub persona_version: String,\n    pub lumi_org_tenant: String,\n    pub sync_class_allowed: Vec&lt;SyncClass&gt;,\n    pub anchor_chain_hash: String,\n}\n\n#[derive(Serialize)]\npub struct IssueResponse {\n    pub token: String,\n    pub expires_at: chrono::DateTime&lt;chrono::Utc&gt;,\n    pub jti: String,\n}\n\npub async fn issue_lumi_token(\n    State(state): State&lt;AppState&gt;,\n    claims: crate::jwt::Claims,\n    Json(req): Json&lt;IssueRequest&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;IssueResponse&gt;), LumiIssueError&gt; {\n    // (1) Caller must have agent-persona role (DEC-432)\n    if !claims.roles().contains(&amp;Role::AgentPersona) {\n        return Err(LumiIssueError::HumanCannotIssue);\n    }\n\n    // (2) Validate persona_version against registry\n    state.persona_registry.validate_known(&amp;req.agent_persona, &amp;req.persona_version)?;\n\n    // (3) Validate sync_class_allowed against per-tenant policy (TASK-AI-005)\n    let policy = state.tenant_policy.load(claims.tenant_id()).await?;\n    state.lumi.validate_sync_class_against_policy(&amp;req.sync_class_allowed, &amp;policy)?;\n\n    // (4) Resolve residency\n    let residency = state.residency.resolve(claims.tenant_id()).await?;\n\n    // (5) Issue token\n    let jti = uuid::Uuid::new_v4().to_string();\n    let ttl = state.config.lumi_token_ttl_seconds;\n    let exp = chrono::Utc::now() + chrono::Duration::seconds(ttl as i64);\n    let lumi_claims = LumiClaims {\n        sub: claims.subject_id(), tid: claims.tenant_id(),\n        iss: \"https://lumi.cyberos.world\".into(),\n        aud: \"https://memory.cyberos.world/sync\".into(),\n        iat: chrono::Utc::now().timestamp(),\n        exp: exp.timestamp(),\n        nbf: chrono::Utc::now().timestamp(),\n        roles: vec![\"agent-persona\".into()],\n        rbac_v: state.rbac_matrix.snapshot().version,\n        jti: jti.clone(),\n        agent_persona: req.agent_persona.clone(),\n        tenant_residency: residency.code.clone(),\n        lumi_org_tenant: req.lumi_org_tenant.clone(),\n        persona_version: req.persona_version.clone(),\n        sync_class_allowed: req.sync_class_allowed.clone(),\n        anchor_chain_hash: req.anchor_chain_hash.clone(),\n    };\n    let token = state.signer.sign_rs256(&amp;lumi_claims).await?;\n\n    // (6) Persist to log + emit memory audit\n    let mut tx = state.db.begin().await?;\n    repo::insert_issuance_log(&amp;mut tx, &amp;lumi_claims, claims.subject_id(), &amp;jti, ttl).await?;\n    audit::emit_lumi_token_issued(&amp;mut tx, &amp;lumi_claims, claims.subject_id()).await?;\n    tx.commit().await?;\n\n    Ok((StatusCode::CREATED, Json(IssueResponse { token, expires_at: exp, jti })))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>iss locked at https://lumi.cyberos.world</strong> — token with different iss → 401 iss_mismatch.</li><li><strong>aud locked at https://memory.cyberos.world/sync</strong> — different aud → 401 aud_mismatch.</li><li><strong>alg=RS256 enforced</strong> — HS256 or none → 401 unsupported_alg.</li><li><strong>agent_persona format <code>cuo-&lt;key&gt;@&lt;semver&gt;</code></strong> — malformed → 401 agent_persona_malformed.</li><li><strong>agent_persona unknown key</strong> → 401 agent_persona_unknown.</li><li><strong>tenant_residency in closed enum</strong> — outside set → 451 residency_mismatch.</li><li><strong>tenant_residency mismatch with route</strong> — 451 unavailable_for_legal_reasons.</li><li><strong>sync_class_allowed closed enum</strong> — unknown value → 401 sync_class_unknown.</li><li><strong>sync_class_allowed empty</strong> → 401 sync_class_empty.</li><li><strong>persona_version 2 minor versions behind</strong> → 401 persona_version_stale + sev-2 audit.</li><li><strong>persona_version &gt; 2 behind</strong> → 401; counter increments.</li><li><strong>Human cannot issue Lumi token</strong> — caller without agent-persona role → 403 human_cannot_issue.</li><li><strong>anchor_chain_hash 64-hex format</strong> — malformed → 401 anchor_chain_hash_malformed.</li><li><strong>lumi_org_tenant slug pattern</strong> — malformed → 401 lumi_org_tenant_malformed.</li><li><strong>POST issue happy path</strong> — agent-persona role + valid request → 201 + token + log row + memory row.</li><li><strong>lumi_token_issuance_log append-only</strong> — UPDATE/DELETE blocked from cyberos_app.</li><li><strong>jti uniqueness enforced</strong> — duplicate jti at insert → 23505.</li><li><strong>TTL default 3600</strong> — exp = iat + 3600.</li><li><strong>TTL configurable via env</strong> — LUMI_TOKEN_TTL_SECONDS=600 → token expires at 600s.</li><li><strong>TTL &lt; 300 rejected at startup</strong> — service refuses to start.</li><li><strong>sync_class_allowed exceeds policy</strong> → 403 sync_class_exceeds_policy.</li><li><strong>OTel span emitted</strong> — auth.lumi.issue / verify with outcome attr.</li><li><strong>Counter <code>auth_lumi_issuance_total{outcome=success}</code> increments</strong> per successful issuance.</li><li><strong>Counter <code>auth_lumi_persona_version_stale_total</code> increments</strong> on stale-version rejection.</li><li><strong>Perf budget &lt; 100ms p95 issue / 50ms p95 verify</strong> — perf tests assert.</li><li><strong><code>auth.lumi_token_issued</code> memory row carries jti + persona + version + residency + sync_class + ttl</strong>.</li><li><strong><code>auth.lumi_token_rejected</code> memory row carries reason</strong> — for every failure path.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/lumi_alg_confusion_test.rs\n#[test]\nfn hs256_token_rejected() {\n    let token = mock_hs256_token();   // attacker forges using public key as HMAC secret\n    let result = cyberos_auth::lumi::verifier::verify_lumi_token(\n        &amp;token, &amp;mock_jwks(), &amp;mock_registry(), \"vn-1\",\n    );\n    assert!(matches!(result, Err(LumiVerifyError::UnsupportedAlg(_))));\n}\n\n#[test]\nfn alg_none_rejected() {\n    let token = mock_none_alg_token();\n    let result = cyberos_auth::lumi::verifier::verify_lumi_token(\n        &amp;token, &amp;mock_jwks(), &amp;mock_registry(), \"vn-1\",\n    );\n    assert!(matches!(result, Err(LumiVerifyError::UnsupportedAlg(_))));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/lumi_human_cannot_issue_test.rs\n#[tokio::test]\nasync fn human_subject_cannot_issue_lumi(ctx: TestCtx) {\n    let human_token = ctx.issue_tenant_admin_token().await;   // role=tenant-admin, no agent-persona\n    let resp = ctx.post(\"/v1/auth/lumi/issue\", &amp;valid_body())\n        .header(\"Authorization\", format!(\"Bearer {human_token}\"))\n        .await;\n    assert_eq!(resp.status(), 403);\n    assert_eq!(resp.json::&lt;serde_json::Value&gt;().await.unwrap()[\"error\"], \"human_cannot_issue_lumi\");\n}\n\n#[tokio::test]\nasync fn agent_persona_can_issue_lumi(ctx: TestCtx) {\n    let agent_token = ctx.issue_agent_persona_token().await;\n    let resp = ctx.post(\"/v1/auth/lumi/issue\", &amp;valid_body())\n        .header(\"Authorization\", format!(\"Bearer {agent_token}\"))\n        .await;\n    assert_eq!(resp.status(), 201);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/lumi_persona_version_stale_test.rs\n#[test]\nfn version_3_minors_behind_rejected() {\n    let live_version = \"1.5.0\";\n    let token_version = \"1.2.0\";   // 3 minors behind\n    let result = cyberos_auth::lumi::verifier::verify_lumi_token(\n        &amp;mock_token_with_version(token_version), &amp;mock_jwks(),\n        &amp;mock_registry_with_live(live_version), \"vn-1\",\n    );\n    assert!(matches!(result, Err(LumiVerifyError::PersonaVersionStale { .. })));\n}\n\n#[test]\nfn version_2_minors_behind_accepted() {\n    let live_version = \"1.5.0\";\n    let token_version = \"1.3.0\";   // 2 minors behind — at threshold\n    let result = cyberos_auth::lumi::verifier::verify_lumi_token(\n        &amp;mock_token_with_version(token_version), &amp;mock_jwks(),\n        &amp;mock_registry_with_live(live_version), \"vn-1\",\n    );\n    assert!(result.is_ok());\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/admin_deny_list_test.rs\n#[test]\nfn vn1_token_at_eu1_endpoint_rejected() {\n    let token = mock_token_with_residency(\"vn-1\");\n    let result = cyberos_auth::lumi::verifier::verify_lumi_token(\n        &amp;token, &amp;mock_jwks(), &amp;mock_registry(), \"eu-1\",\n    );\n    assert!(matches!(result, Err(LumiVerifyError::ResidencyMismatch { .. })));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/lumi_sync_class_closed_test.rs\n#[test]\nfn empty_sync_class_rejected() {\n    let result = cyberos_auth::lumi::sync_class::validate_array(&amp;[]);\n    assert!(matches!(result, Err(LumiClaimError::SyncClassEmpty)));\n}\n\n#[test]\nfn unknown_sync_class_rejected_at_deserialise() {\n    let json = r#\"[\"private\", \"made-up-class\"]\"#;\n    let r: Result&lt;Vec&lt;cyberos_auth::lumi::sync_class::SyncClass&gt;, _&gt; = serde_json::from_str(json);\n    assert!(r.is_err());\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 4 memory row builders follow the canonical pattern.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-AUTH-101</strong> — RBAC catalogue (agent-persona role + role enum).</li></ul>\n<p><strong>Downstream:</strong> none at slice 1 (this task provides the JWT shape that downstream memory sync + task-AUTH-2xx revocation consume).</p>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AUTH-004</strong> — JWT baseline + signing key + JWKS.</li><li><strong>TASK-AI-014</strong> — persona-version registry (consumed by staleness check).</li><li><strong>TASK-AI-016</strong> — residency policy.</li><li><strong>TASK-AI-005</strong> — per-tenant policy YAML (consumed for sync_class policy gate).</li><li><strong>TASK-AI-003</strong> — memory audit bridge.</li><li><strong>TASK-MEMORY-111</strong> — PII scrubbing of lumi_org_tenant.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/auth/lumi/issue request</h3>\n<pre><code class=\"language-json\">{\n  \"agent_persona\": \"cuo-cpo@0.4.1\",\n  \"persona_version\": \"0.4.1\",\n  \"lumi_org_tenant\": \"cyberskill\",\n  \"sync_class_allowed\": [\"shareable\", \"publishable\"],\n  \"anchor_chain_hash\": \"b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9\"\n}</code></pre>\n<h3>8.2 — 201 CREATED response</h3>\n<pre><code class=\"language-json\">{\n  \"token\": \"eyJhbGciOiJSUzI1NiIsImtpZCI6IjFhYjJjM2Q0IiwidHlwIjoiSldUIn0...\",\n  \"expires_at\": \"2026-05-16T11:00:00Z\",\n  \"jti\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\"\n}</code></pre>\n<h3>8.3 — Decoded Lumi JWT claims</h3>\n<pre><code class=\"language-json\">{\n  \"sub\": \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\",\n  \"tid\": \"5e8f1d2a-...\",\n  \"iss\": \"https://lumi.cyberos.world\",\n  \"aud\": \"https://memory.cyberos.world/sync\",\n  \"iat\": 1747920731,\n  \"exp\": 1747924331,\n  \"nbf\": 1747920731,\n  \"roles\": [\"agent-persona\"],\n  \"rbac_v\": 2,\n  \"jti\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"agent_persona\": \"cuo-cpo@0.4.1\",\n  \"tenant_residency\": \"vn-1\",\n  \"lumi_org_tenant\": \"cyberskill\",\n  \"persona_version\": \"0.4.1\",\n  \"sync_class_allowed\": [\"shareable\", \"publishable\"],\n  \"anchor_chain_hash\": \"b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9\"\n}</code></pre>\n<h3>8.4 — auth.lumi_token_issued memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.lumi_token_issued\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"issuer_subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"agent_persona\": \"cuo-cpo@0.4.1\",\n  \"persona_version\": \"0.4.1\",\n  \"lumi_org_tenant_hash16\": \"abc123def4567890\",\n  \"residency\": \"vn-1\",\n  \"sync_class_allowed\": [\"shareable\", \"publishable\"],\n  \"ttl_seconds\": 3600,\n  \"token_jti\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.5 — auth.lumi_persona_version_stale memory row (sev-2)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.lumi_persona_version_stale\",\n  \"severity\": \"sev-2\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"agent_persona\": \"cuo-cpo\",\n  \"token_version\": \"0.2.0\",\n  \"live_version\": \"0.4.1\",\n  \"minor_versions_behind\": 2,\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Lumi token revocation API + CRL endpoint</strong> — task-AUTH-2xx.</li><li><strong>Distinct kid for Lumi vs TASK-AUTH-004</strong> — slice 2.</li><li><strong>Human delegation flow (human authorises specific Lumi sync)</strong> — slice 3 ADR.</li><li><strong>Cross-Lumi token chain (Lumi A → Lumi B)</strong> — task-MEMORY-2xx.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>HS256 alg in token</td><td>header check</td><td>401 unsupported_alg</td><td>Designed</td></tr><tr><td>alg=none in token</td><td>header check</td><td>401 unsupported_alg</td><td>Designed</td></tr><tr><td>iss mismatch</td><td>validation</td><td>401 iss_mismatch</td><td>Designed</td></tr><tr><td>aud mismatch</td><td>validation</td><td>401 aud_mismatch</td><td>Designed</td></tr><tr><td>Unknown kid</td><td>JWKS lookup</td><td>401 unknown_kid</td><td>Designed</td></tr><tr><td>Token expired</td><td>exp + leeway</td><td>401 token_expired</td><td>Re-issue</td></tr><tr><td>persona_version &gt; 2 behind</td><td>registry compare</td><td>401 persona_version_stale + sev-2</td><td>Refresh token</td></tr><tr><td>Unknown agent_persona key</td><td>registry lookup</td><td>401 agent_persona_unknown</td><td>Designed</td></tr><tr><td>Residency mismatch</td><td>endpoint vs claim</td><td>451 unavailable_for_legal_reasons</td><td>Use correct region</td></tr><tr><td>Empty sync_class_allowed</td><td>validator</td><td>401 sync_class_empty</td><td>Designed</td></tr><tr><td>Unknown sync_class value</td><td>serde + validator</td><td>401 sync_class_unknown</td><td>Designed</td></tr><tr><td>Human subject issues</td><td>role check</td><td>403 human_cannot_issue_lumi</td><td>Use agent-persona subject</td></tr><tr><td>Slug malformed</td><td>regex</td><td>401 lumi_org_tenant_malformed</td><td>Fix slug</td></tr><tr><td>anchor_chain_hash malformed</td><td>regex</td><td>401 anchor_chain_hash_malformed</td><td>Fix hash</td></tr><tr><td>sync_class exceeds policy</td><td>policy lookup</td><td>403 sync_class_exceeds_policy</td><td>Reduce array</td></tr><tr><td>jti collision</td><td>UNIQUE</td><td>23505</td><td>Should never happen (UUIDv4)</td></tr><tr><td>TTL &lt; 300 in env</td><td>startup check</td><td>service refuses</td><td>Fix env</td></tr><tr><td>persona registry unreachable</td><td>registry error</td><td>500 + sev-2</td><td>Investigate</td></tr><tr><td>KMS signing key disabled</td><td>signer error</td><td>500 + sev-1</td><td>Rotate key</td></tr><tr><td>Append-only log UPDATE from app</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>RLS bypass</td><td>USING</td><td>0 rows</td><td>Designed</td></tr><tr><td>Cross-tenant log read</td><td>RLS</td><td>0 rows unless root-admin</td><td>Designed</td></tr><tr><td>Token forwarded with different residency in URL</td><td>endpoint check</td><td>451</td><td>Designed</td></tr><tr><td>Refresh path missing</td><td>TASK-AUTH-004 dependency</td><td>Existing path</td><td>Designed</td></tr><tr><td>&gt; 5/h stale-version sustained</td><td>OBS rule</td><td>sev-2 alarm</td><td>Investigate</td></tr><tr><td>jsonwebtoken crate version drift</td><td>tests pin</td><td>CI fails</td><td>Pin version</td></tr><tr><td>concurrent issuance with same jti</td><td>UUID v4</td><td>Collision astronomically improbable</td><td>None</td></tr><tr><td>Policy YAML unreachable</td><td>tenant config</td><td>500 + sev-3</td><td>Restore config</td></tr><tr><td>Subject deleted while log refs</td><td>FK RESTRICT</td><td>DELETE fails</td><td>Soft-delete subject first</td></tr><tr><td>chain_hash regex too permissive</td><td>unit test</td><td>CI fails</td><td>Tighten regex</td></tr><tr><td>sub_claim leaked in memory</td><td>PII scrub</td><td>Pre-commit</td><td>Designed</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Lumi JWT additive on TASK-AUTH-004</strong> — 5 new claims, 8 existing preserved; 13 total.</li><li><strong>Distinct iss + aud</strong> — security boundary that survives per-tenant AUTH compromise.</li><li><strong>alg=RS256 pinned</strong> — JWT-confusion attack defense.</li><li><strong>persona_version staleness 2-minor tolerance</strong> — covers normal rotation lag.</li><li><strong>sync_class_allowed closed enum</strong> — AGENTS.md §15 set.</li><li><strong>anchor_chain_hash claim</strong> — binds token to specific chain state; replay defense.</li><li><strong>Same signing key as TASK-AUTH-004 at slice 1</strong> — operational simplicity; distinct iss provides boundary.</li><li><strong>jti UUIDv4 + UNIQUE constraint</strong> — revocation lookup target.</li><li><strong>TTL 1h default, configurable 5min-24h</strong> — short-lived; mitigates lack of revocation at slice 1.</li><li><strong>Human cannot issue at slice 1</strong> — EU AI Act Art. 13 clean transparency.</li><li><strong>Residency check at verifier</strong> — Decree 53 + GDPR + DORA enforcement.</li><li><strong>Per-tenant sync_class policy enforced at issuance</strong> — defense in depth with memory sync write-time check.</li><li><strong>Append-only log via SQL grant</strong> — forensic record.</li><li><strong>4 memory audit kinds</strong> — issued / verified / rejected / version_stale.</li><li><strong>PII scrub lumi_org_tenant</strong> — cross-tenant boundary.</li><li><strong>OTel sampling 1% verify</strong> — high-volume hot path; 100% on non-success.</li><li><strong>Sev-2 alarm on stale-version &gt; 5/h</strong> — operator investigation prompt.</li><li><strong>Distinct kid for Lumi</strong> — slice 2 hardening.</li><li><strong>Revocation API</strong> — task-AUTH-2xx; jti column ships here.</li></ul>\n<hr>\n<p><em>End of TASK-AUTH-108.</em></p>";
