window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-138"]="<h1>TASK-IMP-138: Platform entry-point identity - thin spine vs explicit dual identity</h1>\n<h2>Summary</h2>\n<p>On every consumer repo, <code>install.sh</code> writes root <code>AGENTS.md</code> as a thin workflow spine (task law, gates, HITL, memory pointer). On this platform repo, root <code>AGENTS.md</code> is the full Layer-1 <em>memory protocol</em>, <code>CLAUDE.md</code> duplicates it wholesale, and every pointer file (<code>.cursorrules</code>, <code>GEMINI.md</code>, <code>.windsurfrules</code>, copilot instructions, cursor rules) tells agents that root <code>AGENTS.md</code> is the first half of \"canonical instructions\". An agent on the platform repo that loads only the most-native file gets memory law and can miss task/HITL law entirely - the inverse of the installer's own documented rationale (\"a root protocol file... would bury the workflow every agent needs\"). This task resolves the identity - but WHICH resolution is a structural operator decision, presented here as a fork and deliberately not resolved by the author.</p>\n<h2>Implementation status: BLOCKED - operator decision fork</h2>\n<p><strong>Implementation MUST NOT begin until the operator picks Branch A or Branch B below (recorded as a source_decisions entry on this spec plus the standard HITL record).</strong> Everything else in this spec - the invariant clauses, the ACs, the test suite - is written to hold under either branch, so the audit gate passes now and the build queue simply cannot pick the task up until the fork is closed. This block is the task's own §9.1-rule-5 marker: BLOCKED on decision \"platform entry-point identity, Branch A vs Branch B\".</p>\n<p><strong>Branch A - thin spine everywhere.</strong> The platform repo's root <code>AGENTS.md</code> becomes the same thin workflow spine consumers get; the memory protocol's normative source moves to <code>modules/memory/cyberos/data/AGENTS.md</code> (already the vendored source of truth for installs), with <code>.cyberos/memory/AGENTS.md</code> as its installed copy; <code>install.sh</code>'s <code>is_platform_repo()</code> exception (:515, :336) is deleted; <code>CLAUDE.md</code> becomes a pointer. Consequence: one identity everywhere, but a structural move of the protocol's normative home - links, tooling, and §0.4-adjacent wording that assume \"root AGENTS.md is the protocol\" must be swept (the walker cites <code>AGENTS.md §3</code>; memory docs reference sections by bare <code>AGENTS.md</code>).</p>\n<p><strong>Branch B - explicit dual identity.</strong> Root <code>AGENTS.md</code> stays the protocol on the platform repo, but the identity is declared everywhere it matters: root <code>AGENTS.md</code> gains a first-screen preamble (\"on this platform repo this file is the Layer-1 memory protocol; task/HITL law lives at <code>.cyberos/AGENT-ENTRY.md</code> - read that first for work\"), every pointer file's wording changes from \"Canonical instructions: AGENTS.md (root) and ...\" to name the workflow spine FIRST and label root AGENTS.md as the memory protocol, and <code>CLAUDE.md</code> either becomes a pointer or carries a marked-copy banner + a drift check against AGENTS.md. Consequence: no structural move, smaller diff, but the platform keeps a root file whose name means something different than it does on every consumer repo - permanently carried complexity.</p>\n<h2>Problem</h2>\n<p>Audit finding H6, verified first-hand 2026-07-23: the five pointer files all present root <code>AGENTS.md</code> as canonical instructions; on this repo that file is the memory protocol (<code>AGENTS.md:1</code>), and <code>CLAUDE.md</code> is a full second copy of it with no divergence guard. The installer itself implements the opposite default for consumers and documents why (<code>install.sh:444-447</code>), keeping the platform as a special case (<code>:515</code>) that nothing at the reading surfaces explains. Concrete failure modes: an agent reading only root <code>AGENTS.md</code> (the file the most tools read natively) learns memory law but not the two HITL gates or the never-push rule from the workflow side; and the AGENTS.md/CLAUDE.md byte-duplication forks silently the first time someone edits one (the protocol has already been amended repeatedly - P19..P22 - so edits are routine).</p>\n<h2>Proposed Solution</h2>\n<p>Present the fork (above), and once the operator picks a branch, implement it plus the branch-independent invariants: (1) an agent that reads only root <code>AGENTS.md</code> on this repo encounters, within the first screen of text, where task/HITL law lives; (2) every pointer file names the workflow spine unambiguously and describes root <code>AGENTS.md</code>'s actual identity truthfully; (3) the memory protocol has exactly one normative source and every other copy declares itself a copy (pointer or marked duplicate with a drift check); (4) a grep-based test suite (<code>scripts/tests/test_entrypoint_identity.sh</code>) pins all three so the identity cannot silently regress. The suite is written to assert the invariants, not the branch, so it survives the decision.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Author resolves the fork (pick Branch A now).</strong> Rejected: the plan's approval boundary explicitly reserves this as an operator decision; Branch A moves the normative home of a protocol document that §0.2 protects with an amendment gate - exactly the kind of structural change an author must not smuggle through a task spec.</li><li><strong>Split into two tasks (one per branch), park both, close the loser.</strong> Rejected: two mutually-exclusive ready tasks in one backlog invite an agent to pick one by queue order - the opposite of an operator gate. One task, one recorded decision, one implementation.</li><li><strong>Do nothing; agents should read both files.</strong> Rejected: the audit's finding is precisely that \"should\" has no mechanism. The pointer files say \"canonical\", agents act on the first file loaded, and the failure mode (missing HITL law) is the highest-consequence class this hardening wave exists for.</li><li><strong>Fix only CLAUDE.md duplication and leave the identity question.</strong> Rejected: the duplication is a symptom; the identity mismatch between platform and consumer repos is the cause, and patching around it leaves H6 open.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: within one release of the operator decision - an agent-eyes-first read of root <code>AGENTS.md</code> reaches task/HITL law (pointer or preamble) within the first 30 lines; all five pointer files describe the spine truthfully; exactly one normative protocol source exists with every copy self-declaring; <code>test_entrypoint_identity.sh</code> green in <code>run_all.sh</code>. Baseline today: 0 of the three invariants hold and no test exists.</li><li>Guardrail: no change to CONSUMER install behavior in Branch B (byte-identical spine); in Branch A, consumer installs are also unchanged (they already get the spine) - only the platform repo's own files move. The memory test suite stays green under either branch.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the operator decision record; root <code>AGENTS.md</code>, <code>CLAUDE.md</code>, and the five pointer files on this repo; <code>install.sh</code>'s platform exception (Branch A deletes it; Branch B keeps + documents it); the invariant test suite; CHANGELOG.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any change to the memory protocol's CONTENT (§0-§18 text) - this task moves/labels the container, never edits the law; protocol changes stay behind §0.2's approval grammar.</li><li>Consumer-repo pointer file wording shipped by the installer (already truthful for consumers; if the operator wants consumer wording updates they ride the normal payload release, not this task).</li><li>The BRAIN store layout and doctor wiring - TASK-MEMORY-303 (related: its INTEROP.md + this task's identity work both reduce the \"which document governs me\" ambiguity, from different ends).</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking on other tasks; blocked on the OPERATOR FORK above (this is a decision dependency, not a task dependency - deliberately not encoded in <code>depends_on</code>, which the queue reads as task-graph edges). Related: TASK-MEMORY-303 (memory-side contract hardening in the same wave).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> the root/CLAUDE duplication, all five pointer files' wording, and the installer's spine + platform-exception code were verified first-hand at HEAD; the two branches are constructed from the installer's own documented rationale and the audit's H6 finding.</li><li><strong>Human review:</strong> the hardening plan was operator-approved 2026-07-23 with T8 explicitly reserved as an operator fork; this spec implements that reservation by specifying both branches and blocking on the pick.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Implementation MUST NOT begin until an operator decision selecting Branch A or Branch B is recorded on this spec (a dated <code>source_decisions</code> entry) - the fork is the operator's, and an implementer arriving via the queue MUST halt at this clause if the record is absent.</li><li>1.2 After implementation, root <code>AGENTS.md</code> on this repo MUST surface where task/HITL law lives within its first 30 lines - as the whole file (Branch A: it IS the spine) or as a preamble pointer (Branch B).</li><li>1.3 Every pointer file (<code>.cursorrules</code>, <code>.cursor/rules/cyberos.mdc</code>, <code>GEMINI.md</code>, <code>.github/copilot-instructions.md</code>, <code>.windsurfrules</code>, <code>CLAUDE.md</code>) MUST name <code>.cyberos/AGENT-ENTRY.md</code> as the workflow entry and MUST describe root <code>AGENTS.md</code>'s identity truthfully for whichever branch was chosen. The \"Canonical instructions: AGENTS.md (root) and ...\" wording MUST NOT survive unqualified under Branch B.</li><li>1.4 The memory protocol MUST have exactly one normative source file after this task (Branch A: <code>modules/memory/cyberos/data/AGENTS.md</code>; Branch B: root <code>AGENTS.md</code>), and every other copy MUST declare itself a copy - a pointer, or a marked duplicate protected by a drift check that fails CI when the copies diverge.</li><li>1.5 Under Branch A only: <code>install.sh</code>'s <code>is_platform_repo()</code> AGENTS.md exception MUST be removed, and the references that assume a root-file protocol home (walker citation strings, memory docs) MUST be swept to the new home. Under Branch B only: the exception MUST gain a comment pointing at this task's decision record.</li><li>1.6 A new suite <code>scripts/tests/test_entrypoint_identity.sh</code> MUST assert the branch-independent invariants mechanically: first-30-lines task-law reachability in root AGENTS.md; spine naming in all six files of 1.3; single-normative-source (exactly one file without a copy/pointer marker); and (when a marked duplicate exists) the drift check runs and passes. It registers via the <code>run_all.sh</code> glob.</li><li>1.7 <code>CHANGELOG.md</code> MUST record the chosen branch and the decision date.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - the spec carries a dated operator decision entry naming the chosen branch BEFORE any implementation commit touches the files in 1.3-1.5 (verified by inspection of the spec's git history at review) - test: <code>scripts/tests/test_entrypoint_identity.sh::t01_decision_recorded</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>head -30 AGENTS.md</code> contains a reference to <code>.cyberos/AGENT-ENTRY.md</code> (or the spine content itself) - test: <code>scripts/tests/test_entrypoint_identity.sh::t02_first_screen_reaches_task_law</code></li><li>[ ] AC 3 (traces_to: #1.3) - each of the six files names <code>.cyberos/AGENT-ENTRY.md</code>, and under Branch B none carries the unqualified \"Canonical instructions: AGENTS.md (root)\" phrasing - test: <code>scripts/tests/test_entrypoint_identity.sh::t03_pointers_truthful</code></li><li>[ ] AC 4 (traces_to: #1.4) - exactly one protocol file lacks a copy/pointer marker; every other file containing the protocol's H1 carries one; when a marked duplicate exists, the drift check passes and a mutated scratch copy makes it fail - test: <code>scripts/tests/test_entrypoint_identity.sh::t04_single_normative_source</code></li><li>[ ] AC 5 (traces_to: #1.5) - Branch A: <code>is_platform_repo</code> no longer special-cases AGENTS.md and a repo-wide grep finds no stale root-protocol assumption in the swept references; Branch B: the exception carries the decision-record comment - test: <code>scripts/tests/test_entrypoint_identity.sh::t05_branch_consistency</code></li><li>[ ] AC 6 (traces_to: #1.6, #1.7) - the suite is discovered green by <code>bash scripts/tests/run_all.sh</code>, and CHANGELOG's top entry names the chosen branch - test: <code>scripts/tests/test_entrypoint_identity.sh::t06_registered_and_recorded</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>The queue picks this task before the fork is decided:</strong> clause 1.1 is the halt - the implementer's first action is checking the decision record and halting to ask when absent. This is the designed outcome, not a failure; the task exists to force exactly one recorded decision.</li><li><strong>Branch A and §0.4's resolution language:</strong> the protocol text itself describes store resolution, not its own file location, so moving the normative home does not amend the law - but 1.5's sweep must verify no §-citation (\"AGENTS.md §3\" in walker/invariants strings) becomes ambiguous; where it would, the citation gains the explicit path.</li><li><strong>Tools that hard-read CLAUDE.md expecting full protocol text (Branch A):</strong> any such reader gets a pointer instead; the memory module's own data copy remains complete, so programmatic consumers (installer, walker) are unaffected - only prompt-layer readers change, which is the point.</li><li><strong>Divergence between AGENTS.md and CLAUDE.md discovered DURING implementation:</strong> the implementer must surface the diff to the operator before unifying - one of the copies contains edits the other never got, and choosing silently which survives is a protocol-content decision (§0.2 territory), not a container decision.</li><li><strong>Cursor's always-applied rules load both files today:</strong> after either branch the loaded set shrinks or gains pointers; the test suite's first-30-lines check keeps the workflow law reachable regardless of which file a tool loads first.</li><li><strong>Security-class:</strong> documentation/identity restructuring only; no execution surface, no data movement beyond text files; the §11 injection posture is unchanged (pointer files remain trusted repo content, not untrusted input).</li></ul>";
