window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-ESOP-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The ESOP service <strong>MUST</strong> ship SP grant schema at <code>services/esop/src/grant/</code> with 5-kind enum + immutable params + status lifecycle, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>grant_kind</code> against closed enum per DEC-2251, <code>grant_status</code> per DEC-2252.</li></ol>\n<ol><li><strong>MUST</strong> define table at migration <code>0001</code>: ``<code>sql CREATE TABLE esop_sp_grants ( grant_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_id UUID NOT NULL, kind TEXT NOT NULL CHECK (kind IN ('founder','employee_initial','employee_refresher','advisor','board')), total_shares BIGINT NOT NULL CHECK (total_shares &gt; 0), vest_months INT NOT NULL DEFAULT 48 CHECK (vest_months &gt; 0), cliff_months INT NOT NULL DEFAULT 12 CHECK (cliff_months &gt;= 0 AND cliff_months &lt;= vest_months), strike_price_vnd BIGINT NOT NULL CHECK (strike_price_vnd &gt;= 0), grant_date DATE NOT NULL, vest_start_date DATE NOT NULL, status TEXT NOT NULL DEFAULT 'pending_signing' CHECK (status IN ('pending_signing','active','fully_vested','cancelled_unvested','accelerated')), granted_by UUID NOT NULL, ceo_signed_at TIMESTAMPTZ, member_signed_at TIMESTAMPTZ, activated_at TIMESTAMPTZ, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now() ); CREATE INDEX grants_member_idx ON esop_sp_grants(tenant_id, member_id, created_at DESC); ALTER TABLE esop_sp_grants ENABLE ROW LEVEL SECURITY; CREATE POLICY grants_rls ON esop_sp_grants USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON esop_sp_grants FROM cyberos_app; GRANT UPDATE (status, ceo_signed_at, member_signed_at, activated_at) ON esop_sp_grants TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> require CEO sign + member sign before status=active per DEC-2253.</li></ol>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/esop/grants                       (CEO creates) POST /v1/esop/grants/{id}/ceo-sign POST /v1/esop/grants/{id}/member-sign      (member self) POST /v1/esop/grants/{id}/cancel           (CEO; pre-cliff only) GET  /v1/esop/grants/{id}                   (member-self or CFO/CEO) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-2254. PII per TASK-MEMORY-111: total_shares SHA256; member_id (uuid) ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from create → sign → activate → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior grant params per DEC-2250 (REVOKE UPDATE except 4 status cols).</li></ol>\n<ol><li><strong>MUST NOT</strong> activate without both CEO + member signs.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why 5 kinds (DEC-2251)?</strong> Covers founders, regular employees (initial + refresh), advisors, board — bounded.</p>\n<p><strong>Why default 4y + 12mo cliff (DEC-2250)?</strong> Industry standard for early-stage equity.</p>\n<p><strong>Why vest_start_date != grant_date (DEC-2253)?</strong> Late-signing grants back-date to employment start for fairness.</p>\n<p><strong>Why immutable (DEC-2250)?</strong> Equity grants = legal commitment; mutation = fraud risk.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample grant:</p>\n<pre><code class=\"language-json\">POST /v1/esop/grants\n{\n  \"member_id\": \"uuid\",\n  \"kind\": \"employee_initial\",\n  \"total_shares\": 10000,\n  \"vest_months\": 48,\n  \"cliff_months\": 12,\n  \"strike_price_vnd\": 1000,\n  \"grant_date\": \"2026-05-17\",\n  \"vest_start_date\": \"2026-01-15\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>grant_kind enum cardinality 5</strong>. 2. <strong>grant_status enum cardinality 5</strong>. 3. <strong>Total_shares &gt; 0 CHECK</strong>. 4. <strong>Vest_months &gt; 0 CHECK</strong>. 5. <strong>Cliff_months ≤ vest_months CHECK</strong>. 6. <strong>Strike_price_vnd ≥ 0</strong>. 7. <strong>Defaults vest=48, cliff=12</strong>. 8. <strong>CEO + member sign required for activate</strong>. 9. <strong>5 memory audit kinds emitted</strong>. 10. <strong>PII scrubbed (total_shares SHA256)</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>CEO-only create + cancel</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Append-only via REVOKE except 4 status cols</strong>. 15. <strong>Cancel allowed pre-cliff only</strong>. 16. <strong>bigint shares</strong>. 17. <strong>vest_start_date locked at creation</strong>. 18. <strong>status workflow: pending_signing → active → fully_vested | cancelled | accelerated</strong>. 19. <strong>Member-self can view own</strong>. 20. <strong>Cross-member view requires CFO audit (TASK-ESOP-007)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn defaults_4y_12mo_cliff() {\n    let g = ctx.create_grant_minimal(ctx.member_id, 10000).await;\n    assert_eq!(g.vest_months, 48);\n    assert_eq!(g.cliff_months, 12);\n}\n\n#[tokio::test]\nasync fn dual_sign_to_activate() {\n    let g = ctx.create_grant(ctx.member_id, 10000).await;\n    ctx.ceo_sign(g.id).await;\n    assert_eq!(ctx.fetch_grant(g.id).await.status, \"pending_signing\");\n    ctx.member_sign(g.id).await;\n    assert_eq!(ctx.fetch_grant(g.id).await.status, \"active\");\n}\n\n#[tokio::test]\nasync fn immutable_post_create() {\n    let g = ctx.create_grant(...).await;\n    let r = ctx.try_update_total_shares(g.id, 20000).await;\n    assert!(r.is_err());\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-HR-001. <strong>Downstream:</strong> TASK-ESOP-002 (vesting), TASK-ESOP-003 (valuation), TASK-ESOP-004 (put-option), TASK-ESOP-005 (GL/BL). <strong>Cross-module:</strong> TASK-AUTH-101 (CEO role), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Kind invalid</td><td>CHECK</td><td>400</td><td>use valid</td></tr><tr><td>Total_shares ≤ 0</td><td>CHECK</td><td>400</td><td>use positive</td></tr><tr><td>Cliff &gt; vest</td><td>CHECK</td><td>400</td><td>reduce cliff</td></tr><tr><td>Mutation attempt</td><td>REVOKE</td><td>DB error</td><td>inherent</td></tr><tr><td>Cancel post-cliff</td><td>validate</td><td>409</td><td>use TASK-ESOP-005</td></tr><tr><td>Cross-tenant grant</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Vest_start in distant past</td><td>warn</td><td>allow (audit visible)</td><td>inherent</td></tr><tr><td>Member declines</td><td>manual</td><td>inherent</td><td>re-grant</td></tr><tr><td>Concurrent sign</td><td>inherent</td><td>last-writer-wins for sign timestamps</td><td>inherent</td></tr><tr><td>Bigint overflow</td><td>bigint VND</td><td>inherent</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Vest start may precede grant_date (back-dating common for late paperwork).</li><li>§11.2 Status lifecycle: pending_signing → active (after dual-sign) → fully_vested (TASK-ESOP-002 cron) or cancelled (TASK-ESOP-005 BL).</li><li>§11.3 memory audit body: grant_id, member_id, kind, status; share counts SHA256.</li><li>§11.4 Cancel allowed only pre-cliff — post-cliff requires TASK-ESOP-005 GL/BL flow.</li><li>§11.5 Member sign captured separately (legal vs operational); both required.</li></ul>\n<hr>\n<p><em>End of TASK-ESOP-001 spec.</em></p>";
