window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-074"]="<h2>§1 — Description</h2>\n<p><strong>Group A — status page auto-sync</strong></p>\n<ol><li>Any commit whose staged changes touch <code>docs/tasks/**</code>, <code>CHANGELOG.md</code>, or <code>VERSION</code> <strong>MUST</strong> regenerate <code>docs/status/</code> (via the existing <code>migrate-frs.sh --page</code> fast path) and stage the result in the same commit, wired into <code>.githooks/pre-commit</code>. The status page can never again lag the task frontmatter it renders.</li><li>The regeneration <strong>MUST</strong> be best-effort-loud: a render failure prints a warning naming the fix command but does not block the commit (matching run-gates.sh's posture; a docs-render bug must not dead-lock all task work).</li><li><code>ship-tasks.md</code> <strong>MUST</strong> record this as a rule of the backlog-state-update steps: every status mutation rides with a fresh status page.</li></ol>\n<p><strong>Group B — batch / parallel shipping</strong></p>\n<ol><li>The workflow (bumped to v2.5.0) <strong>MUST</strong> define batch selection: the eligible set is every <code>ready_to_implement</code> task whose <code>depends_on</code> are all <code>done</code>; a batch is a maximal subset whose members are pairwise independent — no <code>depends_on</code>/<code>blocks</code> edge between members AND no overlap between their declared cones (<code>new_files</code> + <code>modified_files</code> + <code>service</code>). Overlapping tasks stay serial in priority order.</li><li>Batched execution <strong>MUST</strong> keep per-task artefacts, per-task manifests, and per-task HITL verdicts (a single human reply MAY record verdicts for many tasks at once, e.g. \"approve all\" - one utterance, N recorded verdicts), while phases MAY be executed and committed batch-wide instead of one-by-one.</li><li><strong>Unlock rescan:</strong> whenever any task reaches <code>done</code>, the workflow <strong>MUST</strong> re-scan the backlog for tasks whose <code>depends_on</code> just became fully satisfied and append the newly-eligible, cone-independent ones to the running batch queue (no operator prompt - EXECUTION-DISCIPLINE §1 continuation).</li><li>Batching <strong>MUST NOT</strong> weaken HITL: the two human-acceptance gates still apply to every task individually; batch = fewer round-trips, identical guarantees.</li></ol>\n<p><strong>Group C — rules-to-channels distribution sync</strong></p>\n<ol><li>The payload's <code>manifest.yaml</code> <strong>MUST</strong> carry a deterministic <code>rules_sha</code> — a content fingerprint over the rule trees the payload distributes (<code>cuo/</code>, <code>plugin/</code>, <code>mcp/</code>, <code>cli/</code>, <code>memory/</code>) — so every channel (standalone/self-hosted <code>.cyberos</code>, Claude plugin, MCP server, npx CLI) can detect rule drift even when VERSION is unchanged. <code>check-version-sync.sh</code> <strong>MUST</strong> fail if <code>rules_sha</code> is missing/empty.</li><li>The auto-hook chain <strong>MUST</strong> cover build, release, and deploy: (build) the existing pre-commit payload rebuild on <code>modules/**</code>/<code>tools/cyberos-install/**</code> changes + payload-gate on push; (release) the existing release.yml payload job publishing stamped payload assets per tag; (deploy) deploy.yml's docs job <strong>MUST</strong> additionally trigger on <code>modules/cuo/**</code> and <code>modules/skill/**</code> so rule changes refresh the published site without waiting for a release. The chain is documented in the workflow spec so it is discoverable, not tribal.</li><li><code>cyberos update</code> / <code>init.sh --check</code> remain the pull side: they already compare payload versions; <code>rules_sha</code> gives them (and any plugin/MCP consumer) a finer-grained drift signal to compare against. Extending their comparison logic is explicitly a follow-up once this fingerprint exists in the wild.</li></ol>\n<p><em>Length note: this task consciously invokes the sanctioned pure-infra profile — every clause is hook/CI/doc wiring over already-existing machinery, with the §5 checks runnable in-session; the fuller 300-line bar buys nothing here that §5 does not already prove.</em></p>\n<h2>§2 — Why this design</h2>\n<p>The status-page fix reuses the exact regeneration path run-gates.sh already trusts (<code>migrate-frs.sh --page</code>) instead of inventing a second renderer — the bug was missing wiring, not missing tooling. Batch semantics are codified from this session's real 5-task run (queue → per-task artefacts → one \"approve all\"/\"accept all\" verdict pair), which worked but was improvised; v2.5.0 makes it the sanctioned default and adds the unlock rescan the manual run lacked. The rules fingerprint goes in <code>manifest.yaml</code> because that file already rides every channel and is already gate-checked — one new field, zero new distribution surface.</p>\n<h2>§3 — API contract</h2>\n<ul><li>pre-commit block: trigger regex <code>^(docs/tasks/|CHANGELOG\\.md$|VERSION$)</code>; call <code>\"$root/.cyberos/migrate-frs.sh\" --page \"$root\"</code> (fallback <code>tools/cyberos-install/migrate-frs.sh</code> for this self-hosting repo); then <code>git add docs/status/</code>.</li><li>manifest.yaml gains: <code>rules_sha: &lt;64-hex&gt;</code> computed as <code>sha256(sorted per-file sha256 list over cuo/ plugin/ mcp/ cli/ memory/ in $out)</code> — deterministic across runs/platforms.</li><li>ship-tasks.md: <code>workflow_version: 2.5.0</code>; new §11a (batch selection + unlock rescan), §1 note on status sync, new \"Distribution sync\" subsection under Cross-references.</li><li>deploy.yml docs paths += <code>modules/cuo/**</code>, <code>modules/skill/**</code>.</li></ul>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>Committing a change under <code>docs/tasks/</code> regenerates and stages <code>docs/status/</code> in that same commit (hook run observed; <code>git show --stat</code> includes docs/status/index.html alongside the task change).</li><li><code>bash -n .githooks/pre-commit</code> passes; a render failure path prints a warning and exits 0 (non-blocking).</li><li>Two consecutive payload builds produce identical <code>rules_sha</code>; editing any file under <code>modules/cuo/</code> changes it.</li><li><code>check-version-sync.sh</code> fails against a payload whose manifest lacks <code>rules_sha</code>, passes against a fresh build.</li><li><code>ship-tasks.md</code> parses as v2.5.0 with §11a present; batch rules restate both HITL gates unchanged.</li><li>deploy.yml YAML-parses with the two added path globs.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\">bash -n .githooks/pre-commit\nbash tools/cyberos-install/build.sh /tmp/p1 &gt;/dev/null &amp;&amp; bash tools/cyberos-install/build.sh /tmp/p2 &gt;/dev/null\ngrep \"^rules_sha:\" /tmp/p1/manifest.yaml /tmp/p2/manifest.yaml   # identical, non-empty\nbash tools/cyberos-install/check-version-sync.sh /tmp/p1            # PASS incl. rules_sha assert\nsed -i 's/^rules_sha:.*/rules_sha: \"\"/' /tmp/p1/manifest.yaml &amp;&amp; ! bash tools/cyberos-install/check-version-sync.sh /tmp/p1   # negative\ngrep -n \"workflow_version: 2.5.0\" modules/cuo/chief-technology-officer/workflows/ship-tasks.md\npython3 -c \"import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))\"</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>Covered by §3 — five files, all edits; no new scripts beyond the hook block.</p>\n<h2>§7 — Dependencies</h2>\n<p>Upstream none; downstream: a follow-up task may teach <code>cyberos update</code>/plugin/MCP clients to compare <code>rules_sha</code> (clause 10). Batched with TASK-IMP-075 (non-overlapping cones: workflow/hooks/CI-docs vs apps/desktop Rust) — the first sanctioned use of §11a.</p>\n<h2>§8 — Example payloads</h2>\n<p><code>rules_sha: 3f9c…64hex</code> in manifest.yaml; hook output: <code>cyberos: docs/status regenerated + staged (task sources changed)</code>.</p>\n<h2>§9 — Open questions</h2>\n<ul><li>Client-side <code>rules_sha</code> comparison in <code>cyberos update</code>/plugin/MCP (clause 10) — follow-up task once the field ships.</li><li>Whether batch commits should also batch across MODULES with distinct gate profiles — deferred; v2.5.0 scopes batching to cone-independence only.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Render script errors during commit</td><td>warning printed, commit proceeds (clause 2)</td><td>page stale for one commit</td><td>run-gates.sh next run heals; fix renderer</td></tr><tr><td>Two batched tasks secretly touch the same file despite declared cones</td><td>git conflict/second edit visible in review packet diff</td><td>reviewer rejects at gate 1</td><td>route one back; cones corrected in frontmatter</td></tr><tr><td>Unlock rescan picks a task whose spec drifted since audit</td><td>ship-manifest task_sha256 staleness rule (TASK-CUO-206) triggers restart at step 1</td><td>no stale-spec shipping</td><td>existing manifest machinery</td></tr><tr><td>rules_sha nondeterminism across OS (sort/locale)</td><td>AC #3 double-build check; LC_ALL=C forced in build.sh</td><td>none if caught</td><td>pinned locale in the hash pipeline</td></tr><tr><td>Channels ignore rules_sha (no client logic yet)</td><td>clause 10 documents it as pull-side follow-up</td><td>drift detectable, not yet auto-acted-on</td><td>follow-up task</td></tr><tr><td>deploy docs job now triggers more often</td><td>Actions usage visible</td><td>slightly more CI minutes</td><td>acceptable; docs job is minutes-cheap</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<p>Batched with TASK-IMP-075 per §11a. Hash pipeline uses <code>LC_ALL=C sort</code> + <code>sha256sum</code> (ubuntu CI) with <code>shasum -a 256</code> fallback for macOS operators. HITL gates restated verbatim in §11a so batching can never be cited to skip a verdict.</p>\n<p><em>End of TASK-IMP-074.</em></p>";
