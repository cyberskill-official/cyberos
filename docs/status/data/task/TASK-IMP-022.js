window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-022"]="<h1>TASK-IMP-022 — ban defensive asserts</h1>\n<h2>Summary</h2>\n<p>Add the mechanical floor R13 asked for: a lint over the gated test corpus that fails on assertions no input can falsify — a disjunction satisfied by either arm, a statically-true expression, a count comparison that holds at zero, a probe whose exit status is discarded. Six such assertions exist on <code>main</code> today; all six are replaced with an assertion of the one behaviour the code actually has, so the lint lands at zero findings and zero waivers. R13's second ask — the review rule — lands as <code>TRACE-008</code> in <code>audit_rubric@2.0</code>, completing the TRACE-006 / TRACE-007 family.</p>\n<h2>Problem</h2>\n<p>R13 (<code>docs/strategy/cyberos-deep-audit-and-auto-evolution-plan-2026-07-06.md:50</code>): the memory-writer contract bug shipped because a test asserted <code>processed == 3 || failed &gt; 0</code>. That predicate is true whenever the writer FAILED. The suite that existed to catch the defect stayed green straight through it, and the same document's line 125 draws the conclusion — \"an eval suite with defensive asserts is a placebo.\"</p>\n<p>Nothing in the chain looks for this shape. The machine floor (<code>task-lint</code>) reads specs, not test bodies. TRACE-004 asks whether a cited test PASSES — a defensive assertion passes by construction, so TRACE-004 is satisfied <em>most</em> strongly by the worst tests. TRACE-006 (TASK-IMP-118) asks whether a test's assertion is weaker than its clause's verb, but it is judgment-family and unmechanizable by construction, so nothing runs it on 125 test files. The result on <code>main</code> is six live assertions that no code change can falsify:</p>\n<table><thead><tr><th>file:line</th><th>assertion</th><th>why it cannot fail</th></tr></thead><tbody><tr><td><code>modules/cuo/tests/test_baseline.py:149</code></td><td><code>any(\"review_overdue\" in w for w in warnings) or any(... in issues)</code></td><td>green whichever channel fires — the channel IS the contract</td></tr><tr><td><code>modules/cuo/tests/test_placeholder_check.py:139</code></td><td><code>\"yaml_parse\" in error or error == \"frontmatter_not_dict\"</code></td><td>green on a classification the fixture never produces</td></tr><tr><td><code>modules/cuo/tests/test_type_discriminator.py:62</code></td><td><code>\"rubrics/{type}.md\" in skill or \"rubrics/common\" in skill</code></td><td>green with either half of the composition deleted</td></tr><tr><td><code>modules/memory/tests/core/test_crypto_mode.py:208</code></td><td><code>\"no binlog segments\" in details or \"sth_only\" in details</code></td><td>green on both paths the test exists to tell apart</td></tr><tr><td><code>modules/memory/tests/core/test_import.py:219</code></td><td><code>not binlog.exists() or binlog.stat().st_size == 0</code></td><td>green on a dry run that created the file</td></tr><tr><td><code>modules/memory/tests/core/test_store_acl.py:239</code></td><td><code>not check(...).allowed or check(...).mode == \"read\"</code></td><td>green on a refusal that matched no ACL row at all</td></tr></tbody></table>\n<p>Two of these are further evidence for <em>how</em> the check must be built. <code>test_store_acl.py:239</code> is split across lines with a backslash and does not match <code>assert .* or .*</code>; <code>test_sync_conflicts.py:97</code> contains the literal <code>\"dropbox-or-gdrive\"</code> and does. R13 asks for a grep-audit, and a grep-audit over this corpus is wrong in both directions.</p>\n<h2>Proposed Solution</h2>\n<p><strong><code>scripts/check_defensive_asserts.sh</code></strong> — bash wrapper over a <code>python3</code> scanner, same shape as the <code>check_doc_anchors.sh</code> precedent (exit 0 clean · exit 10 with <code>DEFENSIVE &lt;file&gt;:&lt;line&gt; [&lt;rule&gt;] &lt;detail&gt;</code> lines · <code>--list</code> census, always 0). Five rules: <code>DA-001</code> disjunctive Python assert, <code>DA-002</code> statically-true Python assert, <code>DA-003</code> shell probe whose status is discarded, <code>DA-004</code> shell numeric comparison that holds at zero, <code>DA-005</code> a waiver with no reason.</p>\n<p>The Python side walks the AST and matches on <code>BoolOp(Or)</code>, so the string-literal false positive and the line-continued false negative are both handled by construction rather than by tuning a regex. The shell side is a line scanner making only claims decidable from one line, and it skips heredoc bodies — a heredoc body is data, not shell the file executes, which is also what lets this lint's own suite carry the negative fixtures that prove it fires.</p>\n<p><strong><code>scripts/tests/test_assert_lint.sh</code></strong> — ten checks, discovered by <code>run_all.sh</code>'s glob (so pre-commit and the <code>suite-gate</code> workflow both run it without a second registration). Every detector is proved from both sides: a fixture it must flag and a near-miss it must not. <code>t08</code> asserts the live corpus is clean; that is the gate. <code>t09</code> asserts the lint's shell roots are exactly <code>run_all.sh</code>'s globbed roots, so the audited set and the gated set cannot drift apart.</p>\n<p><strong><code>TRACE-008</code> in <code>modules/skill/task-audit/RUBRIC.md</code> §9</strong> — R13's review rule, sited beside its siblings: TRACE-006 fails an assertion weaker than its clause's VERB, TRACE-007 fails a derivation narrower than its claim's SCOPE, TRACE-008 fails an assertion with no falsifying input at all. It is the only one of the three with a mechanical floor, because \"true for every input\" is decidable for a closed set of shapes while \"weaker than this verb\" is not.</p>\n<p><strong>Six replacements.</strong> Each of the six is replaced by asserting the one behaviour the code has, established by running it and reading the result — not by widening the assertion until it holds, and not by waiving it.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>A regex grep, as R13 literally specifies.</strong> Rejected on measurement, not on taste: over this corpus a regex produces two false positives (<code>\"dropbox-or-gdrive\"</code>, and an <code>or</code> inside an assert message) and one false negative (the line-continued disjunction in <code>test_store_acl.py</code>). A lint whose first run is 33% noise is a lint that gets <code># noqa</code>'d. The AST costs no more code and is right in both directions.</li><li><strong>Waive the six and ship the lint clean-by-exemption.</strong> Rejected: the exemptions would be the deliverable. Every one of the six had a determinate answer that took a single probe run to establish, which is exactly what R13's review rule asks an author to do.</li><li><strong>A sibling <code>defensive-assert-exemptions.txt</code>, matching <code>check_doc_anchors.sh</code>.</strong> Rejected: a path-granular exemption suits \"this document is a historical archive\"; an assert waiver is line-granular, and a reviewer reading the assertion must be able to see why the disjunction is admissible without opening a second file. Waivers are inline and must carry a reason of at least 12 characters — a reasonless waiver is <code>DA-005</code>, a finding in its own right.</li><li><strong>Infer shell polarity (<code>if &lt;A||B&gt;; then ok</code> is weak, <code>… then fail</code> is a stronger conjunctive assertion).</strong> Rejected for this pass: it needs the then-branch, and the four live <code>if [ ! -f A ] || [ ! -f B ]; then fail</code> sites in the corpus are the CORRECT form. Guessing wrong would flag good tests, and a lint that flags good tests is uninstalled. Named as out-of-scope rather than silently omitted; TRACE-008's judgment half covers it.</li><li><strong>Extend <code>task-lint.mjs</code>.</strong> Rejected: <code>task-lint</code> audits spec documents. Pointing it at test bodies would give one tool two corpora and two failure vocabularies.</li><li><strong>Do nothing; rely on TRACE-006.</strong> Rejected: TRACE-006 is judgment-family and fires at the spec gate on the clauses of one task. Six defensive assertions survived it on <code>main</code>, which is the measurement.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: <code>bash scripts/check_defensive_asserts.sh</code> exits 0 over 125 gated test files with <strong>0 findings and 0 waivers</strong>, and exits 10 on each of the five detector fixtures. A lint that cannot fail is the defect this task is about, so <code>t01</code>–<code>t07</code> exist to prove it can.</li><li>Guardrail: <code>bash scripts/tests/run_all.sh</code> shows no suite red that was green on <code>main</code> at <code>bb161013</code>, and the <code>modules/cuo</code> + <code>modules/memory</code> pytest suites hold at their pre-change pass counts (276 and 521). Replacing a disjunction with a stronger assertion must not be paid for with a broken suite.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>scripts/check_defensive_asserts.sh</code>; <code>scripts/tests/test_assert_lint.sh</code>; the <code>TRACE-008</code> row and subsection in <code>modules/skill/task-audit/RUBRIC.md</code> §9; the six replacement assertions; the CHANGELOG entry.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li><strong><code>services/**</code>.</strong> Rust <code>assert!</code> / <code>assert_eq!</code>, Go, and platform Python are outside CyberOS 1.x per <code>docs/batches/batch-10e-imp-stub-wont-do.md</code>. The bound is stated in the lint header and in TRACE-008 rather than left to be discovered: a green floor there means \"not looked at\".</li><li><strong>Assertions inside an embedded interpreter heredoc</strong> (<code>python3 - &lt;&lt;'PY'</code> within a <code>.sh</code> suite). Skipped by construction, stated as a bound.</li><li><strong>Shell polarity inference</strong> — see Alternatives.</li><li><strong>Vendoring the lint into the payload</strong> (<code>.cyberos/docs-tools/</code>). This gates the cyberos repo's own corpus first; shipping it to consumer repos means holding arbitrary corpora to these five shapes, which is its own decision with its own blast radius.</li><li><strong>A corpus sweep of the 180+ <code>done</code> tasks against TRACE-008.</strong> Same call TASK-IMP-118 made for TRACE-006: sizing that belongs to the operator, not to this task.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. <code>run_all.sh</code>'s glob (TASK-IMP-107/128) is the registration path for the new suite; <code>audit_rubric@2.0</code> §9 already carries TRACE-006 and TRACE-007, and TRACE-008 is one more row plus one more subsection.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Opus 5), authoring <code>TASK-IMP-022</code> from the R13 source line after it had sat as an unauthored draft stub since the 2026-07-08 migration.</li><li><strong>Scope:</strong> R13's framing was treated as a claim to verify, not as an input. <strong>re-derived and CONFIRMED:</strong> the R13 defect shape is live in this repo — <code>bash scripts/check_defensive_asserts.sh</code> on <code>bb161013</code> returns six <code>DA-001</code> findings, each listed in ## Problem with its file and line, and each probed by running the code under test to establish the single true behaviour. <strong>re-derived and CORRECTED:</strong> R13 specifies a *grep*-audit for or-conditions. Measured against this corpus a grep is wrong in both directions — it flags <code>assert sources == {\"dropbox-or-gdrive\", \"syncthing\"}</code> (<code>test_sync_conflicts.py:97</code>) and misses the backslash-continued disjunction at <code>test_store_acl.py:239</code>. The implementation parses instead, and <code>t02</code>/<code>t03</code> pin both directions on disk. <strong>measured and ADDED:</strong> the shell half finds <strong>zero</strong> live findings across all 56 shell suites; the 11 <code>|| true</code> sites in them are output captures and cleanup, none an assertion. <code>DA-003</code>/<code>DA-004</code> therefore ship as forward guards with no current bite, which is said here rather than implied by a green run. Also added: heredoc-body skipping, discovered when the lint's own fixture file flagged itself.</li><li><strong>Human review:</strong> @stephencheng recorded the session HITL verdict for <code>batch/12g-imp-022</code>, including confirmation of <code>ai_authorship</code> and <code>eu_ai_act_risk_class</code> (the two <code># UNREVIEWED</code> markers the 2026-07-14 migration left on this stub). Recorded at <code>docs/batches/batch-12g-imp-022-session-hitl.md</code>.</li></ul>\n<h2>1. Clauses</h2>\n<ul><li>1.1 <code>scripts/check_defensive_asserts.sh</code> MUST scan the gated test corpus and exit 10 with one <code>DEFENSIVE &lt;file&gt;:&lt;line&gt; [&lt;rule&gt;] &lt;detail&gt;</code> line per finding, 0 when clean, and MUST support <code>--list</code> for a findings-plus-waivers census that always exits 0. Test: <code>t01_da001_flags_disjunction</code></li><li>1.2 <code>DA-001</code> MUST flag any Python <code>assert</code> whose test expression contains a disjunction, including one split across physical lines, and MUST NOT flag an <code>assert</code> whose only <code>or</code> is inside a string literal. The detector MUST parse the file; a textual match MUST NOT be the implementation. Tests: <code>t02_da001_ignores_or_inside_a_string</code>, <code>t03_da001_catches_multiline_disjunction</code></li><li>1.3 <code>DA-002</code> MUST flag a Python <code>assert</code> that is statically true — truthy constant, non-empty literal container, lambda or f-string, <code>len(...) &gt;= 0</code>, <code>len(...) &gt; -1</code> — and MUST NOT flag <code>assert False</code>, which is an unreachable marker rather than a defensive assertion. Test: <code>t04_da002_vacuous_only</code></li><li>1.4 <code>DA-003</code> MUST flag a bare shell probe (<code>grep</code>, <code>rg</code>, <code>test</code>, <code>[</code>, <code>[[</code>, <code>diff</code>, <code>cmp</code>) whose exit status is discarded by <code>|| true</code> or <code>|| :</code>, and MUST NOT flag an output capture such as <code>n=\"$(grep -c x f)\" || true</code>. Heredoc bodies MUST NOT be scanned: they are data, not shell the file executes. Test: <code>t05_da003_swallowed_probe</code></li><li>1.5 <code>DA-004</code> MUST flag a shell numeric comparison that holds at zero (<code>-ge 0</code>, <code>-gt -1</code>) — the AUTH-005 #6 shape, a test that passes when nothing happened. Test: <code>t06_da004_vacuous_numeric</code></li><li>1.6 A waiver (<code># defensive-assert-ok: &lt;reason&gt;</code> on the flagged line or the line above) MUST suppress a finding only when the reason is at least 12 characters; a shorter or absent reason MUST itself be reported as <code>DA-005</code>. Every active waiver MUST be printed on every run. Test: <code>t07_waiver_requires_a_reason</code></li><li>1.7 The live corpus MUST be clean — zero findings and zero waivers — and each of the six pre-existing <code>DA-001</code> sites MUST be replaced by an assertion of the single behaviour the code produces, established by running it. Widening an assertion or waiving it MUST NOT be used in place of a replacement. Test: <code>t08_live_corpus_clean</code>, plus the <code>modules/cuo</code> and <code>modules/memory</code> pytest suites</li><li>1.8 The lint's shell roots MUST be exactly the roots <code>scripts/tests/run_all.sh</code> globs. A suite the runner reaches but the lint does not is gated and unaudited; the reverse is a dead scan. Test: <code>t09_roots_match_runner</code></li><li>1.9 <code>modules/skill/task-audit/RUBRIC.md</code> §9 MUST carry <code>TRACE-008</code> as a table row and as a subsection naming: what makes an assertion unfalsifiable, the mechanical floor that enforces the decidable part, its kinship to TRACE-006, and R13 as its origin. Test: <code>t10_rubric_carries_trace_008</code></li><li>1.10 The suite MUST prove each detector from both sides — a fixture it flags and a near-miss it does not. A lint against defensive assertions asserted defensively would be self-refuting. Tests: <code>t01</code>–<code>t07</code></li></ul>\n<h2>3. Edge case matrix</h2>\n<table><thead><tr><th>#</th><th>Category</th><th>Trigger</th><th>Expected</th><th>Test</th></tr></thead><tbody><tr><td>1</td><td>NULL/EMPTY</td><td>a test file with no <code>assert</code> at all</td><td>scanned, no finding, counted in the file total</td><td>t02</td></tr><tr><td>2</td><td>NULL/EMPTY</td><td>a corpus root that does not exist on disk</td><td><code>WARN</code> on stderr, scan continues, exit unaffected</td><td>t01</td></tr><tr><td>3</td><td>BOUNDS</td><td><code>assert False</code></td><td>not flagged — an unreachable marker, not a defensive assertion</td><td>t04</td></tr><tr><td>4</td><td>BOUNDS</td><td>waiver reason of exactly 12 characters</td><td>suppresses; 11 characters is <code>DA-005</code></td><td>t07</td></tr><tr><td>5</td><td>BOUNDS</td><td>disjunction nested inside a comprehension (<code>any(x or y for …)</code>)</td><td>flagged — the <code>BoolOp</code> is reachable in the assert's test</td><td>t01</td></tr><tr><td>6</td><td>MALFORMED</td><td>a Python test file that does not parse</td><td>reported as <code>DA-000</code> at the syntax-error line, never skipped silently</td><td>t02</td></tr><tr><td>7</td><td>MALFORMED</td><td><code>or</code> inside an assert's message string, not its test</td><td>not flagged — the AST separates test from message</td><td>t02</td></tr><tr><td>8</td><td>MALFORMED</td><td>disjunction continued with <code>\\</code> across two lines</td><td>flagged, reported at the <code>assert</code>'s own line</td><td>t03</td></tr><tr><td>9</td><td>SECURITY</td><td>a waiver used to silence a genuine defect</td><td>bounded, not prevented: the reason is mandatory, printed on every run, and <code>t08</code> pins the corpus at zero waivers, so the first one has to be argued in review</td><td>t07, t08</td></tr><tr><td>10</td><td>SECURITY</td><td>a `\\</td><td>\\</td><td>true` inside a heredoc fixture</td><td>not flagged — heredoc bodies are data; the bound is stated so it cannot be mistaken for coverage</td><td>t05</td></tr><tr><td>11</td><td>CONCURRENT</td><td>two suites adding test files between a scan and a commit</td><td>no shared state; the lint re-walks the tree per run and pre-commit runs it on the staged tree</td><td>t08</td></tr><tr><td>12</td><td>DEGRADATION</td><td><code>run_all.sh</code> gains a fourth globbed root</td><td><code>t09</code> goes red until the lint's root list is extended — the drift is caught, not inherited</td><td>t09</td></tr><tr><td>13</td><td>DEGRADATION</td><td>Rust/Go assertions under <code>services/**</code></td><td>out of scope by declaration; a green run does not claim them</td><td>— (stated bound)</td></tr></tbody></table>\n<h2>4. Out of scope / non-goals</h2>\n<p>See \"## Scope → ### Out of scope / Non-Goals\" above.</p>\n<h2>Acceptance criteria</h2>\n<ul><li>AC1 (traces_to 1.1): the lint exists, exits 10 with a per-finding <code>DEFENSIVE</code> line on the R13 shape, and exits 0 on a clean tree. Test: <code>t01_da001_flags_disjunction</code>.</li><li>AC2 (traces_to 1.2): a string containing <code>or</code> is not flagged. Test: <code>t02_da001_ignores_or_inside_a_string</code>.</li><li>AC3 (traces_to 1.2): a line-continued disjunction is flagged. Test: <code>t03_da001_catches_multiline_disjunction</code>.</li><li>AC4 (traces_to 1.3): three statically-true shapes are flagged; <code>assert False</code> is not. Test: <code>t04_da002_vacuous_only</code>.</li><li>AC5 (traces_to 1.4): a bare swallowed probe is flagged; an output capture is not. Test: <code>t05_da003_swallowed_probe</code>.</li><li>AC6 (traces_to 1.5): <code>-ge 0</code> is flagged. Test: <code>t06_da004_vacuous_numeric</code>.</li><li>AC7 (traces_to 1.6): a reasoned waiver suppresses and prints; a bare one becomes <code>DA-005</code>. Test: <code>t07_waiver_requires_a_reason</code>.</li><li>AC8 (traces_to 1.7): the live corpus scans clean at zero findings and zero waivers, and both pytest suites hold at their pre-change pass counts. Test: <code>t08_live_corpus_clean</code> + <code>modules/cuo</code> (276 passed, 2 skipped) + <code>modules/memory</code> (521 passed, 5 skipped, 1 pre-existing red on <code>bb161013</code>). Pre-change and post-change runs side by side: <code>testing-evidence.md</code> §3.</li><li>AC9 (traces_to 1.8): the lint's shell roots equal <code>run_all.sh</code>'s globbed roots. Test: <code>t09_roots_match_runner</code>.</li><li>AC10 (traces_to 1.9, 1.10): <code>TRACE-008</code> is in the rubric with its substance named, and every detector is proved from both sides. Test: <code>t10_rubric_carries_trace_008</code> + <code>t01</code>–<code>t07</code>.</li></ul>";
