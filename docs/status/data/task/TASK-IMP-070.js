window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-070"]="<h1>TASK-IMP-070: Remote update awareness</h1>\n<h2>§1 - Description</h2>\n<p><code>init.sh --check</code> compares a target repo's <code>.cyberos/VERSION</code> against the LOCAL payload's VERSION, and the <code>/update</code> command trusts that answer. Once TASK-IMP-069 publishes every release, the honest comparison is against the latest published version. This task adds that third data point and rewrites the verdict logic around it.</p>\n<p>Normative clauses:</p>\n<ol><li>A script <code>tools/cyberos-init/check-latest.sh</code> MUST resolve the latest published version and print exactly one line <code>latest=&lt;X.Y.Z|unknown&gt; source=&lt;url|offline&gt;</code>. Resolution order: <code>$CYBEROS_RELEASE_ENDPOINT</code> when set (an https URL or a local file path returning either a bare <code>X.Y.Z</code> or GitHub's <code>/releases/latest</code> JSON, from which <code>tag_name</code> minus the <code>v</code> is taken), else the repo's public <code>releases/latest</code> page redirect (Location header names the tag; immune to API rate limits), with the <code>releases/latest</code> API URL as fallback (amended post-ship 2026-07-12 after a live 403 rate-limit). Total network budget MUST be &lt;= 3 seconds (curl <code>--max-time 3</code>); any failure MUST yield <code>latest=unknown source=offline</code> with exit 0 - the script never breaks a caller.</li><li><code>init.sh --check &lt;repo&gt;</code> MUST report three values - <code>installed</code> (<code>&lt;repo&gt;/.cyberos/VERSION</code>), <code>payload</code> (the local payload's VERSION), <code>latest</code> (via check-latest.sh, skipped when <code>CYBEROS_OFFLINE=1</code>) - followed by exactly one verdict line: <code>verdict=up_to_date</code> (installed == latest, or latest unknown and installed == payload), <code>verdict=repo_stale</code> (installed &lt; payload or installed &lt; latest), or <code>verdict=payload_stale</code> (payload &lt; latest). <code>installed &gt;= latest</code> MUST count as up to date - the check never advises a downgrade. Each non-clean verdict MUST print the exact next command (<code>bash &lt;payload&gt;/init.sh &lt;repo&gt;</code> for repo_stale; the TASK-IMP-069 download one-liner or <code>build.sh</code> for payload_stale).</li><li>Version comparison MUST be numeric semver (major, minor, patch), not string comparison.</li><li><code>plugin/commands/update.md</code> MUST direct the agent to run the extended <code>--check</code> and act on the verdict: on <code>payload_stale</code>, fetch the latest release payload (or rebuild from a current checkout) BEFORE re-running init, and never report \"up to date\" from the local-payload comparison alone. On <code>latest=unknown</code> it MUST say the remote check was skipped and the answer is only as fresh as the local payload.</li><li><code>plugin/commands/changelog.md</code> MUST, when <code>latest</code> is newer than <code>installed</code>, link the GitHub Releases page as the span to read (installed+1 .. latest), in addition to the local <code>.cyberos/manifest.yaml</code> details.</li><li>Offline behavior MUST be first-class: <code>CYBEROS_OFFLINE=1</code> (or network failure) degrades <code>--check</code> to today's local comparison plus an explicit <code>latest=unknown source=offline</code> note. Exit code semantics of <code>--check</code> (0 = ran) MUST NOT change.</li><li>Nothing in this task mutates a target repo; the check remains read-only and auto-update stays out of scope.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The three-value report makes the two failure directions distinguishable: a stale TARGET repo (fix: rerun init) versus a stale LOCAL payload (fix: download/rebuild), which today collapse into one misleading \"up to date\". A separate <code>check-latest.sh</code> keeps network code out of <code>init.sh</code>'s critical path, gives the desktop Ops tab (TASK-APP-001) and <code>/update</code> one shared resolver, and makes the endpoint overridable so tests run on <code>file://</code>-style fixtures with zero network.</p>\n<h2>§3 - Contract</h2>\n<pre><code>check-latest.sh\n  env: CYBEROS_RELEASE_ENDPOINT (optional), CYBEROS_OFFLINE=1 -&gt; immediate unknown\n  stdout: latest=1.8.0 source=https://api.github.com/repos/cyberskill-official/cyberos/releases/latest\n      or: latest=unknown source=offline\n  exit: always 0\n\ninit.sh --check &lt;repo&gt;   (extended output, order fixed)\n  installed=1.2.0\n  payload=1.7.0\n  latest=1.8.0 source=&lt;url&gt;\n  verdict=repo_stale\n  next: bash &lt;payload&gt;/init.sh &lt;repo&gt;</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Endpoint override, bare version</strong> (§1 #1) - with <code>CYBEROS_RELEASE_ENDPOINT</code> pointing at a fixture file containing <code>1.8.0</code>, the script prints <code>latest=1.8.0</code> with that source.</li><li><strong>Endpoint override, GitHub JSON</strong> (§1 #1) - with a fixture containing <code>{\"tag_name\": \"v1.8.0\", ...}</code>, the script prints <code>latest=1.8.0</code>.</li><li><strong>Failure degrades, never breaks</strong> (§1 #1, #6) - with an unreachable endpoint, output is <code>latest=unknown source=offline</code>, exit 0, and <code>--check</code> still completes with a verdict.</li><li><strong>Three values + verdict, all four states</strong> (§1 #2) - fixtures produce each verdict: installed==payload==latest -&gt; <code>up_to_date</code>; installed&lt;payload -&gt; <code>repo_stale</code> with the init command; payload&lt;latest -&gt; <code>payload_stale</code> with the fetch instruction; latest unknown + installed==payload -&gt; <code>up_to_date</code> with the offline note.</li><li><strong>Numeric semver compare</strong> (§1 #3) - 1.10.0 ranks above 1.9.0 (string compare would invert it).</li><li><strong>CYBEROS_OFFLINE honored</strong> (§1 #6) - with <code>CYBEROS_OFFLINE=1</code>, no network attempt is made (endpoint fixture untouched) and the offline note appears.</li><li><strong>/update doc drives the verdicts</strong> (§1 #4) - update.md names the three values, the two stale verdicts with their distinct actions, and forbids the local-only \"up to date\" claim.</li><li><strong>/changelog links the release span</strong> (§1 #5) - changelog.md instructs linking the Releases page when latest &gt; installed.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/cyberos-init/tests/test_check_latest.sh\n# Fixtures under $TMP: bare-version file, tag_name JSON file, unreachable path,\n# plus a scratch repo + scratch payload for the --check verdict matrix.\n\nt01_bare_version_endpoint()      # AC 1\nt02_github_json_endpoint()       # AC 2\nt03_unreachable_degrades()       # AC 3\nt04_verdict_matrix()             # AC 4  (4 sub-cases, exact verdict + next line asserted)\nt05_numeric_semver()             # AC 5\nt06_offline_env()                # AC 6\nt07_update_doc_contract()        # AC 7  (greps update.md for the three keys + both verdicts)\nt08_changelog_doc_contract()     # AC 8</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p><code>check-latest.sh</code>: ~40 lines - endpoint resolution, <code>curl -sf --max-time 3</code> (or <code>cat</code> for a local path), parse bare/JSON, print. <code>init.sh --check</code>: keep existing reads, add the resolver call + a <code>ver_ge()</code> numeric comparator + verdict table from §3.</p>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-IMP-069 (there must BE a published latest). TASK-APP-001's Check button inherits the richer output for free since it shells out to <code>init.sh --check</code>.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ CYBEROS_OFFLINE=1 bash dist/cyberos/init.sh --check ~/Projects/clientA\ninstalled=1.6.0\npayload=1.7.0\nlatest=unknown source=offline\nverdict=repo_stale\nnext: bash dist/cyberos/init.sh /Users/stephen/Projects/clientA</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. A cached last-known-latest under <code>~/.cache/cyberos/</code> was considered and dropped: a 3-second budget per explicit check does not need a cache, and a cache adds a staleness surface of its own.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>GitHub API rate limit (60/h unauthenticated) - resolver returns unknown; verdict falls back to local compare with the offline note. Never a hard failure.</li><li>Endpoint returns HTML (proxy portal) - parse yields no semver; treated as unknown, not as a garbage version (regex-gated).</li><li>Pre-release tags (v1.8.0-rc1) - resolver takes <code>tag_name</code> as-is; non <code>X.Y.Z</code> strings are rejected by the same regex and reported unknown (pre-releases are not \"latest\" for updaters).</li><li>Clock-skew style confusion (installed NEWER than latest, e.g. developing ahead of the last tag) - <code>installed &gt; latest</code> maps to <code>up_to_date</code> (never advise a downgrade).</li><li>Two payloads on one machine (repo checkout + downloaded release) - the report prints which payload path it compared, so the operator sees which source answered.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the output keys machine-parseable (<code>key=value</code>, one per line) - TASK-APP-001 parses them, and future telemetry can too. update.md and changelog.md are agent-facing prose; keep the verdict names verbatim so agents can branch on them.</p>\n<p><em>End of TASK-IMP-070.</em></p>";
