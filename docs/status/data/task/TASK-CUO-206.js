window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-206"]="<h1>TASK-CUO-206: Ship run-state manifest</h1>\n<h2>§1 - Description</h2>\n<p>Give /ship-tasks the same re-entrancy anchor its authoring sibling already has: a per-task manifest that records which of the 31 steps completed, over which artefacts, so a new session resumes instead of re-deriving - plus deterministic queue selection when no task id is given.</p>\n<p>Normative clauses:</p>\n<ol><li>A contract <code>ship-manifest@1</code> MUST be defined at <code>modules/skill/contracts/task/SHIP-MANIFEST.md</code> with fields: <code>manifest_version</code> (const <code>ship-manifest@1</code>), <code>task_id</code>, <code>task_sha256</code> (hash of the task spec file at run start - a later mismatch marks the whole manifest stale), <code>workflow_version</code> (from the workflow doc), <code>started_at</code>, <code>updated_at</code>, <code>current_step</code> (1..31), <code>routed_back_count</code>, <code>steps[]</code> - each <code>{index, skill, status (pending|done|failed|skipped-conditional), artefact_path, artefact_sha256, verdict, completed_at}</code> - and <code>hitl</code> (<code>{gate: null|review_approval|final_acceptance, requested_at}</code>).</li><li>The ship workflow MUST write the manifest to <code>docs/tasks/.workflow/&lt;task-ID&gt;.ship.json</code> after EVERY completed, failed, or conditionally-skipped step, using two-phase atomic writes (<code>.tmp.&lt;nonce&gt;</code> then rename), mirroring the memory-protocol write discipline.</li><li>On invocation for a task whose manifest exists with matching <code>workflow_version</code>, ship MUST resume at the first non-done step AFTER re-verifying every recorded <code>artefact_sha256</code> against disk; a mismatch marks that step and all later steps stale (redo from the earliest stale step). A <code>workflow_version</code> mismatch MUST route to needs_human, never a silent mixed-version run.</li><li>Invoked WITHOUT a task id, ship MUST select deterministically: among tasks at <code>ready_to_implement</code> whose <code>depends_on</code> are all <code>done</code>, order by priority (MUST before SHOULD before COULD), then <code>created</code> ascending, then id ascending; the selection and its reasoning line MUST be echoed to the operator before step 1 runs.</li><li>Manifests MUST be gitignored via a scaffolded <code>docs/tasks/.workflow/.gitignore</code> (content: <code>*.ship.json</code>); task frontmatter and BACKLOG.md remain the only committed state. <code>/init</code> MUST scaffold the same ignore file in target repos.</li><li>On the task reaching <code>done</code> (HITL gate 2 passed), ship MUST delete the manifest; on route-back to <code>ready_to_implement</code>, ship MUST keep it with <code>routed_back_count</code> incremented (the next run starts fresh at step 1 by §1 #3's staleness rule but retains the count and history).</li><li>The workflow doc MUST gain a <code>## Resume semantics</code> section and EXECUTION-DISCIPLINE.md a pointer to it; the plugin wrapper SKILL.md MUST mention resume-on-restart so agents look for the manifest before starting step 1.</li><li>HITL gates MUST NOT be inferable from the manifest alone: resuming at a gate step re-requests the human approval; a recorded <code>hitl.requested_at</code> never substitutes for the approval itself.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>The manifest is a cache of proven work, never an authority: every resume re-hashes artefacts, and human gates always re-ask. That keeps the two-source-of-truth risk (manifest vs backlog) at zero - if the manifest lies or is deleted, the worst case is redoing work, the exact status quo. JSON-on-disk with atomic writes copies the pattern already proven by the authoring manifest; queue selection turns \"pick the next eligible one\" from prose into an algorithm agents apply identically across repos.</p>\n<h2>§3 - Contract</h2>\n<pre><code class=\"language-json\">{\n  \"manifest_version\": \"ship-manifest@1\",\n  \"task_id\": \"TASK-TEN-208\",\n  \"task_sha256\": \"4c1e...\",\n  \"workflow_version\": \"2.3.1\",\n  \"started_at\": \"2026-07-12T10:00:00+07:00\",\n  \"updated_at\": \"2026-07-12T11:42:10+07:00\",\n  \"current_step\": 11,\n  \"routed_back_count\": 0,\n  \"steps\": [\n    {\"index\": 1, \"skill\": \"repo-context-map-author\", \"status\": \"done\",\n     \"artefact_path\": \"docs/tasks/.workflow/TASK-TEN-208.rcm.md\",\n     \"artefact_sha256\": \"9f2c...\", \"verdict\": \"pass\", \"completed_at\": \"2026-07-12T10:12:00+07:00\"},\n    {\"index\": 3, \"skill\": \"architecture-decision-record-author\", \"status\": \"skipped-conditional\",\n     \"artefact_path\": null, \"artefact_sha256\": null, \"verdict\": null, \"completed_at\": \"2026-07-12T10:13:00+07:00\"}\n  ],\n  \"hitl\": {\"gate\": null, \"requested_at\": null}\n}</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Schema is normative and validated</strong> (§1 #1) - SHIP-MANIFEST.md defines every field with types/enums; the §3 example and the fixtures validate against it programmatically.</li><li><strong>Write-after-every-step, atomically</strong> (§1 #2) - the workflow doc mandates the write points and the tmp+rename discipline; no step's completion is unrecorded.</li><li><strong>Resume skips proven work</strong> (§1 #3) - fixture: manifest with steps 1-10 done and artefacts intact -&gt; resume plan says step 11; corrupting step 5's artefact makes the resume plan restart at 5 with 5..31 stale.</li><li><strong>Version mismatch halts</strong> (§1 #3) - manifest at 2.3.0 vs workflow 2.3.1 -&gt; needs_human, no auto-run.</li><li><strong>Queue selection is total and deterministic</strong> (§1 #4) - fixture backlog (mixed statuses, unmet depends_on, tied priorities) yields one defined winner; re-running yields the same; the reasoning line matches the fixture expectation.</li><li><strong>Gitignore scaffolding</strong> (§1 #5) - the ignore file exists with <code>*.ship.json</code>; <code>git status</code> in a fixture repo shows no manifest after a simulated run; init.sh scaffolds it in a scratch target.</li><li><strong>Terminal handling</strong> (§1 #6) - done deletes the manifest; route-back keeps it with the incremented count (fixture pair).</li><li><strong>Human gates re-ask on resume</strong> (§1 #8) - resuming a manifest parked at step 19/31 (gates) produces a fresh approval request; the doc forbids treating requested_at as approval.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-python\"># modules/cuo/tests/test_ship_manifest.py\ndef test_schema_fields_and_example_validate():      # AC 1\ndef test_atomic_write_discipline_documented():      # AC 2  (workflow doc contains the write-point + tmp/rename clauses)\ndef test_resume_plan_intact_and_stale():            # AC 3  (pure function over fixture manifests + artefact dir)\ndef test_workflow_version_mismatch_needs_human():   # AC 4\ndef test_queue_selection_total_order():             # AC 5  (fixture task set -&gt; expected id; idempotent)\ndef test_gitignore_scaffold():                      # AC 6\ndef test_done_deletes_routeback_keeps():            # AC 7\ndef test_hitl_reask_on_resume():                    # AC 8  (doc assertion + fixture plan marks gate pending)</code></pre>\n<p>(The resume planner and queue selector are specified in the workflow doc precisely enough to implement as small pure helpers under modules/cuo for testability; agents follow the same algorithm doc-driven in reduced profile.)</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>SHIP-MANIFEST.md mirrors MANIFEST_SCHEMA.md's structure (field table, lifecycle, atomicity, staleness). Workflow doc: add manifest write-points to the step protocol preamble, the Resume semantics section (staleness rule, version rule, gate re-ask), and the queue algorithm where the doc currently says \"next eligible task\".</p>\n<h2>§7 - Dependencies</h2>\n<p>None hard. TASK-CUO-207's config later adds nothing here (manifest location is fixed). Interacts with TASK-CUO-205 only at the shared backlog-write skill, unchanged for ship. TASK-SKILL-118's coverage-gate rubric constants are read at their steps regardless of resume.</p>\n<h2>§8 - Example payloads</h2>\n<p>Resume echo line (operator-facing):</p>\n<pre><code>resume TASK-TEN-208: steps 1-10 verified (10 artefacts, hashes OK), continuing at step 11/31 (observability-injection-author). routed_back_count=0</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Cross-repo parallel shipping (two agents, two different tasks, one repo) is naturally safe - one manifest per task; two agents on the SAME task is out of scope and remains an operator error the backlog's status cell already surfaces.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Crash between artefact write and manifest write - resume re-verifies hashes; the missing manifest entry means the step re-runs, idempotent by skill design.</li><li>Manifest edited by hand to skip a gate - §1 #8: gates re-ask regardless of manifest content; the manifest cannot authorize anything.</li><li>Stale manifest after task spec edits (task re-audited mid-flight) - covered by the schema's <code>task_sha256</code> root field (§1 #1): mismatch at resume marks every step stale, forcing a clean re-run against the revised spec.</li><li>.workflow dir deleted - clean restart from step 1; no correctness loss (cache semantics).</li><li>Clock skew across sessions - ordering uses step indices, not timestamps; timestamps are informational only.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the manifest strictly derived (cache) - the words \"record of truth\" appear only next to task frontmatter in every doc touched. The queue reasoning line format is part of the contract (operators grep session logs for it).</p>\n<p><em>End of TASK-CUO-206.</em></p>";
