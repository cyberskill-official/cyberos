window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-122"]="<h1>TASK-IMP-122: rules_sha must be recomputed, not recalled</h1>\n<h2>Summary</h2>\n<p>CyberOS already ships a rule-content fingerprint. <code>rules_sha</code> (TASK-IMP-074) is computed at build time over the payload and written into <code>manifest.yaml</code>; three components compare it against an install, and none of them re-hashes that install. But no component ever re-hashes the installed tree - each greps the stored token out of <code>.cyberos/manifest.yaml</code> and compares it to the token in the payload's manifest. Two tokens from one build always agree, whatever happens to the files afterward. So the check answers \"did these manifests come from the same build?\" and is read as \"do these bytes still match?\".</p>\n<h2>Problem</h2>\n<p>Measured on this repo, 2026-07-18.</p>\n<p><strong>Defect 1 - the fingerprint is recalled, not recomputed.</strong> <code>build.sh:354</code> computes <code>rules_sha</code> over the assembled output. <code>install.sh</code> vendors the manifest containing it. Then:</p>\n<table><thead><tr><th>component</th><th>what it does</th></tr></thead><tbody><tr><td><code>version.sh:61-65, :89-93</code></td><td><code>_rs()</code> greps <code>^rules_sha:</code> from each manifest; <code>verdict=\"rules_drift\"</code> on mismatch</td></tr><tr><td><code>lib/update-check.sh:8-11, :76-96</code></td><td><code>_cyberos_rules_sha()</code> greps the manifest; emits <code>RULE DRIFT ... (same version, different rules)</code></td></tr><tr><td><code>audit-fleet.sh:13, :19, :37</code></td><td><code>_rs()</code> greps the manifest; compares each fleet repo's token to an expected one</td></tr></tbody></table>\n<p>A grep for any re-hash of the installed tree (<code>find</code>/<code>shasum</code> over <code>$CY</code>) across all of them returns <strong>empty</strong>. Nothing recomputes. (<code>check-version-sync.sh</code> reads <code>rules_sha</code> too, at <code>:56</code>, but is NOT a drift comparator and is out of scope: <code>grep -c '\\.cyberos'</code> on it returns <strong>0</strong> - it compares payload stamps to the root <code>VERSION</code> and has no installed side at all.) <code>update-check.sh:76</code> states the correct thesis - <em>\"VERSION is a promise; rules_sha is the evidence\"</em> - and then greps a token that is a promise for exactly the same reason VERSION is: it was written once, at build, by the thing being checked.</p>\n<p>The consequence is not theoretical. On 2026-07-18 this repo's own <code>.cyberos</code> differed from <code>dist/</code> in six vendored artefacts while <code>VERSION</code> read <code>1.0.0</code> and <code>rules_sha</code> matched on both sides. The installed <code>batch-select.mjs</code> was the pre-PR#53 build carrying the undeclared-cone bug, and returned a different batch than source. Every check reported current.</p>\n<p><strong>Defect 2 - the cone excludes shipped trees.</strong> <code>build.sh:354</code> hashes <code>find cuo plugin mcp cli memory</code>. Measured: <code>docs-tools/</code> and <code>lib/</code> are present in <code>dist/cyberos/</code> and absent from that list. Four of the six artefacts that actually drifted (<code>batch-select.mjs</code>, <code>render-status-hub.mjs</code>, <code>verify-goals.mjs</code>, <code>workflow-improve.mjs</code>) live in <code>docs-tools/</code>. So even a recomputing check would miss them until the cone is widened. The two defects are independent and both must close, or the fix reports current on the evidence that motivated it.</p>\n<p><strong>Why REPAIR and not a second mechanism.</strong> TASK-IMP-104 §1.2 forbids install carrying a second implementation, and 104 explicitly rejected content hashing for ITS question: <em>\"Compare a manifest hash rather than a version. Rejected: overkill for a monotonic version line, and it answers 'different' rather than 'older', which is not the question.\"</em> 104 guards ORDER and is correct as shipped; \"different\" is precisely this task's question. <code>rules_sha</code> is the right mechanism, in the right place, doing the wrong thing. This task repairs it.</p>\n<h2>Proposed Solution</h2>\n<p>Recompute the fingerprint over the installed tree at comparison time, using the same function and the same cone as the build, and compare that against the payload's manifest token. Widen the cone to every directory the payload ships. Report the differing paths, not just a verdict. Keep each component's existing exit contract.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Add a per-file manifest beside <code>rules_sha</code>. Rejected: a second comparator for the same question, which TASK-IMP-104 §1.2 forbids by name, and the first draft of this spec proposed it only because it had not found <code>rules_sha</code>.</li><li>Bump VERSION on every payload change. Rejected: conflates release identity with build identity, forces a bump for a comment fix, and still cannot see a vendor step that silently drops a file.</li><li>Compare mtimes. Rejected: not content; survives no copy or checkout faithfully; would have reported the 2026-07-18 drift as fine.</li><li>Re-vendor on every <code>.cyberos</code> use. Rejected: install is not free, and a guard that silently repairs drift teaches nobody that the channel leaked.</li><li>Widen the cone only, leaving the token stored. Rejected: it fixes which files are covered and leaves the check comparing two copies of one build-time answer. Defect 1 survives untouched.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a vendored file mutated by one byte after install, with both VERSIONs and both stored tokens equal, is reported as drift and named. Baseline: reported <code>up_to_date</code> - reproduced synthetically 2026-07-18; the original six-artefact instance was repaired by <code>e2504cf3</code> and is no longer live evidence.</li><li>Guardrail: a freshly installed, unmodified machine reports current - no false drift, or the check gets ignored.</li><li>Guardrail: every directory present in the payload is inside the cone, enforced at build.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the cone at <code>build.sh:354</code>; recomputation in <code>version.sh</code>, <code>lib/update-check.sh</code>, <code>audit-fleet.sh</code>; a suite. <code>check-version-sync.sh</code> is deliberately NOT in scope: it has no installed side (<code>grep -c '\\.cyberos'</code> = 0).</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>TASK-IMP-104's ordering guard. Correct as shipped; untouched.</li><li>Install-generated and operator-owned paths (<code>gates.env</code>, <code>config.yaml</code>, <code>.update-check-cache</code>, <code>memory/store/</code>). Normatively excluded by §1.3, not merely noted.</li><li>Auto-repairing drift. Reporting is the deliverable; <code>install.sh</code> already re-vendors.</li><li>Tamper-evidence. §1.8 makes the prohibition normative rather than a hope in prose.</li></ul>\n<h2>Dependencies</h2>\n<p>None. TASK-IMP-104's guard runs before the lock and is untouched.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> rewritten 2026-07-18 after an independent audit failed the first draft at 4/10 for asserting \"nothing compares content\". Every claim here was re-measured against source that day; the four consumers and the cone exclusions are grep output, not recall.</li><li><strong>Human review:</strong> scope approved at the 2026-07-18 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Every component that reports rule drift between an installed machine and a payload (<code>version.sh</code>, <code>lib/update-check.sh</code>, <code>audit-fleet.sh</code>) MUST derive the installed side by RECOMPUTING the fingerprint over the installed tree. No component may derive the installed side by reading a stored token out of the installed manifest.</li><li>1.2 The cone is a single explicit list, declared ONCE in a shared file that <code>build.sh</code> and every comparator read. It is a maintained list, not a derivation: <code>install.sh</code> vendors from three separate places (<code>:185-198</code> and <code>:432</code>), conditionally (<code>[ -d ]</code> / <code>[ -f ]</code> guards), inside a loop (<code>:432</code> iterates <code>AGENTS.md memory.schema.json memory.invariants.yaml</code>), and under environment control (<code>memory/</code> only when <code>CYBEROS_NO_MEMORY != 1</code>). No static read of that file yields the set. (Operator decision, 2026-07-18: a maintained list reconciled by a build check, rather than a bash analyser.)</li><li>1.3 The cone MUST cover the vendored trees <code>cuo</code>, <code>plugin</code>, <code>mcp</code>, <code>lib</code>, <code>docs-tools</code> and the vendored files under <code>memory/</code> (<code>:185-198</code>, <code>:432</code>), and MUST exclude four classes: (a) install-generated or operator-owned paths never copied - <code>memory/store/</code>, <code>gates.env</code>, <code>config.yaml</code>, <code>.update-check-cache</code>, <code>AGENT-ENTRY.md</code>, <code>gates.env.bak.*</code>, <code>.install.lock</code>; (b) <code>manifest.yaml</code>, which is COPIED (<code>:188</code>) but MUST be excluded because it CONTAINS <code>rules_sha</code> - <code>build.sh:354</code> computes the value and <code>:357</code> writes the file holding it, so covering it makes the value depend on itself; (c) <code>VERSION</code>, which is COPIED (<code>:189</code>) but is TASK-IMP-104's axis, not this one; (d) the six vendored root scripts (<code>:190-195</code>), which are the machine's own installer surface and change on every payload edit.</li><li>1.4 A build check MUST reconcile the declared list against <code>install.sh</code>: a path <code>install.sh</code> vendors that is neither in the cone NOR in §1.3's four exclusion classes MUST fail the build. The exclusions are declared alongside the cone, so <code>manifest.yaml</code>, <code>VERSION</code> and the root scripts are vendored-and-excluded BY NAME rather than by omission - a vendored path that nobody has classified is the failure this catches. The check reconciles a maintained list against reality; it does not derive the list, and MUST NOT be written so that it compares the list to itself.</li><li>1.5 A recomputed installed fingerprint differing from the payload's MUST be reported as drift, INCLUDING when both VERSION strings and both stored tokens are equal.</li><li>1.6 Path-naming is decided per INVOCATION, never per component, because reachability is a property of how a tool is called: <code>version.sh</code> run as <code>.cyberos/version.sh</code> has <code>$here == $CY</code> and NO payload tree, while <code>bash &lt;payload&gt;/version.sh &lt;repo&gt;</code> has one; <code>lib/update-check.sh</code> sourced from <code>.cyberos/lib/</code> has <code>self_root == $CY</code> and no tree in its PRIMARY mode; <code>audit-fleet.sh</code> resolves a REAL tree at <code>dist/cyberos/</code> by default (<code>:16-18</code>) and has only a token under <code>CYBEROS_EXPECT_RULES_SHA</code>. Where a payload tree IS reachable, the report MUST name every differing path. Where only a token is reachable, the report MUST state that it cannot name paths rather than implying a complete report.</li><li>1.7 An installed tree byte-identical to the payload ACROSS THE CONE MUST be reported as current. \"Byte-identical across the cone\" is the whole test: paths outside the cone (<code>ci/</code>, <code>cli/</code>, <code>template/</code>, which ship and are never installed; <code>memory/store/</code>, which installs and never ships) MUST NOT affect the verdict.</li><li>1.8 Recomputation MUST use <code>build.sh</code>'s own <code>_rsha()</code> (<code>sha256sum</code> on Linux, <code>shasum -a 256</code> on macOS) and its <code>LC_ALL=C</code> sort, so a byte-identical tree yields a byte-identical fingerprint on both platforms.</li><li>1.9 A comparison that cannot be computed (manifest absent or unreadable on either side, cone unreadable) MUST yield the verdict <code>unknown</code>. No component may yield a current verdict on a comparison it could not perform.</li><li>1.10 Exit contracts: <code>lib/update-check.sh</code> remains soft-by-default (<code>:18</code>) and MUST NOT change its exit semantics. <code>audit-fleet.sh</code> MUST NOT retain its present fail-open behaviour (<code>:19</code>, \"rule-drift check DISABLED\"); with no expected token it MUST yield <code>unknown</code> per 1.9 rather than silently passing.</li><li>1.11 No component's output may describe the fingerprint as detecting tampering, since anything able to rewrite a vendored file can rewrite the manifest beside it.</li><li>1.12 A comparison MUST NOT modify the installed machine. <code>lib/update-check.sh</code> alone MAY continue writing its existing <code>$cy/.update-check-cache</code> (<code>:99</code>); no other component may write anything.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - for EACH of the three comparators, mutating one byte of a vendored file while leaving BOTH stored manifest tokens untouched is reported as drift; the test MUST exercise all three by name and MUST FAIL if any one still answers from the stored token - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t01_all_three_recompute_not_recall</code></li><li>[ ] AC 2 (traces_to: #1.2) - the cone is read by BOTH <code>build.sh</code> and every comparator from ONE declared file; the test MUST FAIL if any of them carries its own copy of the list, and MUST FAIL against today's <code>build.sh:354</code>, which inlines <code>find cuo plugin mcp cli memory</code> - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t02_one_shared_cone_definition</code></li><li>[ ] AC 3 (traces_to: #1.3) - the cone covers <code>cuo plugin mcp lib docs-tools</code> and the vendored <code>memory/</code> files (mutating one file in EACH yields drift; the test MUST FAIL on today's cone, which omits <code>lib</code> and <code>docs-tools</code>); and each of the four exclusion classes is exercised - a generated path (<code>gates.env</code>), the CIRCULAR one (mutating <code>manifest.yaml</code>'s own <code>rules_sha:</code> line), <code>VERSION</code>, and a root script - each yields NO drift - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t03_cone_covers_vendored_and_excludes_four_classes</code></li><li>[ ] AC 4 (traces_to: #1.4) - adding a <code>cp</code> of a NEW path into <code>install.sh</code> that is in neither the cone nor an exclusion class fails the build; and the check is not self-comparing - the test MUST FAIL if the reconciler is rewritten to read its expected set from the same list it validates - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t04_reconciler_catches_unclassified_vendored_path</code></li><li>[ ] AC 5 (traces_to: #1.5) - drift is reported with both VERSION strings AND both stored tokens equal - the exact 2026-07-18 shape; the test MUST FAIL if any component reports current - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t05_equal_tokens_still_drift</code></li><li>[ ] AC 6 (traces_to: #1.6) - per INVOCATION, not per component: <code>bash &lt;payload&gt;/version.sh &lt;repo&gt;</code> (tree reachable) names all THREE mutated files and MUST FAIL if only the first is named; <code>bash .cyberos/version.sh</code> (no tree) states it cannot name paths; <code>audit-fleet.sh</code> on its DEFAULT (tree at <code>dist/cyberos/</code>) names them, and under <code>CYBEROS_EXPECT_RULES_SHA</code> states it cannot - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t06_naming_follows_invocation_not_component</code></li><li>[ ] AC 7 (traces_to: #1.7) - a freshly installed unmodified machine reports current from every component; the test MUST FAIL on today's cone, which yields <code>ae756045</code> over the install vs <code>66bb0459</code> over the payload on a byte-identical tree - caused INDEPENDENTLY by <code>cli/</code> (coned, never installed) AND by <code>memory/store/</code> (install-generated inside a coned dir, 3 payload files vs 8 installed); pruning either alone still mismatches, and the AC MUST exercise both - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t07_clean_install_is_current</code></li><li>[ ] AC 8 (traces_to: #1.8) - the recomputed fingerprint equals <code>build.sh</code>'s for the same tree, and a fixture forcing the <code>shasum</code>/<code>sha256sum</code> branch yields the same digest as the other; the test MUST FAIL if either platform branch diverges - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t08_cross_platform_digest_stable</code></li><li>[ ] AC 9 (traces_to: #1.9) - with the payload manifest removed, separately with it unreadable, separately with the INSTALLED manifest removed, and separately with the cone unreadable, every component emits the verdict <code>unknown</code> and none emits a current verdict; the assertion is on the emitted VERDICT (1.9's verb) - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t09_uncomputable_is_unknown_not_current</code></li><li>[ ] AC 10 (traces_to: #1.10) - <code>update-check.sh</code> keeps its exit semantics in EVERY mode it binds (<code>soft</code> exits 0 on drift, <code>strict</code> exits non-zero, <code>always</code>, <code>off</code>) - a regression guardrail, stated as such; and <code>audit-fleet.sh</code> with no expected token emits <code>unknown</code> and does NOT pass, which MUST FAIL against today's <code>:19</code> \"rule-drift check DISABLED\" warning-and-continue - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t10_exit_contracts</code></li><li>[ ] AC 11 (traces_to: #1.11) - no component's output matches tamper/integrity/authenticity wording on a drift run, an <code>unknown</code> run, OR a current run - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t11_no_tamper_claims</code></li><li>[ ] AC 12 (traces_to: #1.12) - the installed tree is byte-identical before and after a comparison from EACH component; <code>.update-check-cache</code> is excluded for <code>lib/update-check.sh</code> ONLY, and the test MUST FAIL if <code>version.sh</code> or <code>audit-fleet.sh</code> writes it - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t12_check_is_read_only</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Payload NEWER than installed with VERSION differing: TASK-IMP-104's ordering guard owns the refusal; this check reports drift and does not duplicate it.</li><li>Operator hand-edited a vendored file: reported as drift and named. The report says what differs, never who differed it (§1.11 covers the wording).</li><li>A vendored file that is legitimately empty: hashed like any other. Empty is a content.</li><li>First install, no installed tree: §1.9's <code>unknown</code>, not drift and not current.</li><li><strong>A vendor step that silently drops a file</strong> (<code>build.sh:198</code>'s <code>[ -f ... ] &amp;&amp; cp</code> - the <code>workflow-improve.mjs</code> case): the file is absent from the payload, so absent from the payload fingerprint, so a matching install is NOT drift. <strong>Nothing in this task catches that.</strong> §1.4's cone check is directory-and-path level against <code>install.sh</code>'s copy list; a conditional <code>cp</code> whose guard is false still LISTS the path, so the cone agrees and the file is simply missing from both sides. Recorded as an uncovered gap, not handed to a clause that cannot honour it. It is the sibling defect and wants its own task.</li><li><code>audit-fleet.sh</code> with a reachable payload rather than a bare token: it MAY name paths per §1.6's first arm; the clause requires the disclaimer only when no tree is reachable.</li><li>macOS vs Linux: §1.8 pins <code>_rsha()</code> and <code>LC_ALL=C</code>. This is the <code>stat -c/-f</code> class of defect TASK-IMP-103 already paid for once.</li><li>Security-class: the fingerprint is a staleness signal, not an integrity guarantee. §1.11 makes that normative and AC 11 tests it across all three verdicts.</li></ul>";
