window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-122"]="<h1>TASK-IMP-122: Payload staleness must be detectable</h1>\n<h2>Summary</h2>\n<p>An installed <code>.cyberos/</code> can differ arbitrarily from the payload it claims to be, and every check we own reports it current. Measured on this repo: <code>.cyberos/VERSION</code> and <code>dist/cyberos/VERSION</code> both read <code>1.0.0</code> while six vendored artefacts differed - including a <code>batch-select.mjs</code> whose known parallel-corruption bug had already been fixed and merged. Nothing compares content, so nothing could see it.</p>\n<h2>Problem</h2>\n<p><code>version.sh</code>, <code>lib/update-check.sh</code> and TASK-IMP-104's downgrade guard all compare <code>VERSION</code> strings. VERSION answers \"which release is this\", never \"are these the bytes of that release\". A payload that is stale but same-versioned is therefore indistinguishable from a current one.</p>\n<p>Measured drift on this repo, VERSION identical throughout:</p>\n<table><thead><tr><th>artefact</th><th>installed state</th></tr></thead><tbody><tr><td><code>cuo/ship-tasks.md</code></td><td>missing <code>## 11d. Batch economics</code> (TASK-IMP-114)</td></tr><tr><td><code>docs-tools/batch-select.mjs</code></td><td>pre-PR#53: no <code>declares()</code>, so an undeclared cone read as EMPTY and joined every batch</td></tr><tr><td><code>docs-tools/render-status-hub.mjs</code></td><td>knew 3 of 4 status-page inputs; its <code>fp-</code> stamp did not cover <code>docs/batches/</code></td></tr><tr><td><code>docs-tools/verify-goals.mjs</code></td><td>drifted</td></tr><tr><td><code>cuo/skills/workflow-improver</code></td><td>ABSENT</td></tr><tr><td><code>docs-tools/workflow-improve.mjs</code></td><td>ABSENT</td></tr></tbody></table>\n<p>The consequences were not theoretical. The stale <code>batch-select</code> returned <code>{106, INV-004, 115}</code> where the current one returns <code>{106, 115}</code> - it admitted a task with an undeclared cone into a parallel swarm batch, which is precisely the failure PR #53 was merged to prevent. The stale renderer produced a committed status page (<code>d19362ad</code>) whose <code>fp-</code> stamp omitted an input, which the current renderer's own comment names as \"a stamp that lies about what determined the page\".</p>\n<p>TASK-IMP-104 already guards the ORDER of versions and works. This is a different axis: EQUAL version, different bytes. The fix shape is already proven in this repo - TASK-IMP-082 replaced a HEAD-sha stamp with an <code>fp-</code> content fingerprint for exactly this reason, on exactly this kind of question.</p>\n<h2>Proposed Solution</h2>\n<p><code>build.sh</code> emits a manifest of per-file hashes into the payload. <code>install.sh</code> vendors it. <code>version.sh</code> and <code>update-check.sh</code> compare the installed manifest against the payload's and report DRIFTED, naming the differing paths, rather than reporting current on a VERSION match alone. The manifest covers exactly what <code>build.sh</code> vendors, so a file the vendor list omits is absent from the manifest and cannot be silently blessed.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Bump VERSION on every payload change. Rejected: it conflates release identity with build identity, forces a version bump for a comment fix, and still says nothing when a vendor step silently omits a file.</li><li>Compare mtimes. Rejected: mtime is not content, survives no copy or checkout faithfully, and would have reported this exact drift as fine.</li><li>Re-vendor unconditionally on every <code>.cyberos</code> use. Rejected: install is not free, and a guard that fixes drift without reporting it teaches nobody that the channel leaked.</li><li>Fold into TASK-IMP-104. Rejected: 104 guards ordering and is correct as shipped; widening it to content would re-open a passing task to add an orthogonal axis.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a payload/installed pair that differs in any vendored byte is reported DRIFTED with the differing paths named. Baseline: reported \"up to date\" across six drifted artefacts.</li><li>Guardrail: a byte-identical pair reports current - no false drift, or the check gets ignored.</li><li>Guardrail: the manifest covers every path <code>build.sh</code> vendors - a file added to the vendor list without the manifest is a build failure, not a silent gap.</li></ul>\n<h2>Scope</h2>\n<p>In scope: manifest emission in <code>build.sh</code>, vendoring in <code>install.sh</code>, comparison in <code>version.sh</code> and <code>lib/update-check.sh</code>, suite.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Auto-repairing drift. Reporting is the deliverable; re-vendoring is the operator's call and <code>install.sh</code> already does it.</li><li>TASK-IMP-104's ordering guard. Untouched.</li><li>Signing or tamper-evidence. This detects staleness, not attack; a manifest beside the payload proves neither authorship nor integrity against an adversary, and MUST NOT be described as if it did.</li></ul>\n<h2>Dependencies</h2>\n<p>None. Deliberately independent of TASK-IMP-104: that guard runs before the lock and stays as-is, and this check is additive.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from measured drift on this repo at HEAD <code>d19362ad</code>, verified by running both the installed and source <code>batch-select.mjs</code> and diffing their output. Implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> scope approved at the 2026-07-18 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>build.sh</code> MUST emit a manifest into the payload listing every vendored path with a content hash.</li><li>1.2 The manifest MUST cover exactly the set <code>build.sh</code> vendors: a vendored path absent from the manifest MUST fail the build.</li><li>1.3 <code>install.sh</code> MUST vendor the manifest into <code>.cyberos/</code>.</li><li>1.4 <code>version.sh</code> and <code>lib/update-check.sh</code> MUST compare the installed manifest against the payload's and report DRIFTED, naming each differing path, when any hash differs - INCLUDING when the two VERSION strings are equal.</li><li>1.5 A byte-identical installed/payload pair MUST report current.</li><li>1.6 A missing or unreadable manifest on either side MUST report UNKNOWN and MUST NOT report current. A check that cannot run is not a pass.</li><li>1.7 The comparison MUST NOT modify the installed machine.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - a built payload contains a manifest, and installing it puts the manifest in <code>.cyberos/</code> - test: <code>tools/install/tests/test_payload_fingerprint.sh::t01_manifest_emitted_and_vendored</code></li><li>[ ] AC 2 (traces_to: #1.4) - mutating ONE byte of ONE vendored file in an installed <code>.cyberos/</code>, leaving both VERSION strings equal, is reported DRIFTED and the mutated path is named; the test MUST FAIL if the check reports current - test: <code>tools/install/tests/test_payload_fingerprint.sh::t02_equal_version_different_bytes_is_drift</code></li><li>[ ] AC 3 (traces_to: #1.4) - a file present in the payload and ABSENT from the installed machine is reported DRIFTED and named (the <code>workflow-improver</code> shape) - test: <code>tools/install/tests/test_payload_fingerprint.sh::t03_absent_file_is_drift</code></li><li>[ ] AC 4 (traces_to: #1.5) - a freshly installed machine reports current, not drift - test: <code>tools/install/tests/test_payload_fingerprint.sh::t04_fresh_install_reports_current</code></li><li>[ ] AC 5 (traces_to: #1.6) - a removed manifest reports UNKNOWN and exits non-zero; the assertion MUST be on the exit code, not on the printed text - test: <code>tools/install/tests/test_payload_fingerprint.sh::t05_missing_manifest_fails_closed</code></li><li>[ ] AC 6 (traces_to: #1.2) - adding a vendored path to <code>build.sh</code> without the manifest fails the build - test: <code>tools/install/tests/test_payload_fingerprint.sh::t06_vendor_list_and_manifest_agree</code></li><li>[ ] AC 7 (traces_to: #1.7) - the installed tree is byte-identical before and after a comparison run - test: <code>tools/install/tests/test_payload_fingerprint.sh::t07_check_is_read_only</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Payload NEWER than installed, VERSION differs: TASK-IMP-104's ordering guard owns that call; this check reports drift and does not duplicate the refusal.</li><li>Installed machine hand-edited by an operator: reported DRIFTED and named - correct. The check reports what differs, never who differed it, and MUST NOT accuse.</li><li><code>gates.env</code>, <code>config.yaml</code>, <code>.update-check-cache</code>, <code>memory/store/</code> are install-generated or operator-owned, not vendored: they MUST NOT appear in the manifest, or every install reports drift against itself immediately.</li><li>A vendored file that is legitimately empty: hashed like any other; empty is a content, not an absence.</li><li>First install (no installed manifest to compare): 1.6's UNKNOWN, not drift and not current.</li><li>Security-class: the manifest is a staleness signal, not an integrity guarantee - anything that can rewrite a vendored file can rewrite the manifest beside it. The output MUST NOT be worded as tamper detection.</li></ul>";
