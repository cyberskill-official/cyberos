window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-122"]="<h1>TASK-IMP-122: rules_sha must be recomputed, not recalled</h1>\n<h2>Summary</h2>\n<p>CyberOS already ships a rule-content fingerprint. <code>rules_sha</code> (TASK-IMP-074) is computed at build time over the payload and written into <code>manifest.yaml</code>. Three components compare it against an installed machine, and every one of them answers the installed side by grepping the stored token out of <code>.cyberos/manifest.yaml</code> rather than by re-hashing the installed files. Two tokens from one build always agree, whatever happens to those files afterward. So the check answers \"did these manifests come from the same build?\" and is read as \"do these bytes still match?\".</p>\n<h2>Problem</h2>\n<p>Measured on this repo at HEAD, 2026-07-18. Every number below was re-measured by this author.</p>\n<p><strong>Defect 1 - the fingerprint is recalled, not recomputed.</strong> <code>build.sh:354</code> computes <code>rules_sha</code> over the assembled output. <code>install.sh:188</code> vendors the manifest containing it. Then:</p>\n<table><thead><tr><th>component</th><th>what it does</th></tr></thead><tbody><tr><td><code>version.sh:59-63, :89-93</code></td><td><code>_rs()</code> greps <code>^rules_sha:</code> from each manifest; <code>verdict=\"rules_drift\"</code> on mismatch</td></tr><tr><td><code>lib/update-check.sh:8-14, :82-97</code></td><td><code>_cyberos_rules_sha()</code> greps a manifest; <code>:101-105</code> emits <code>RULE DRIFT ... (same version, different rules)</code></td></tr><tr><td><code>audit-fleet.sh:13, :36-37</code></td><td><code>_rs()</code> greps <code>$cy/manifest.yaml</code>; compares each fleet repo's token to an expected one</td></tr></tbody></table>\n<p><code>grep -nE 'shasum|sha256sum'</code> across all three returns <strong>empty</strong>. Nothing recomputes. (<code>check-version-sync.sh</code> reads <code>rules_sha</code> too, at <code>:56</code>, but only asserts it is 64-hex (<code>:57-58</code>); it is not a drift comparator and is out of scope: <code>grep -c '\\.cyberos'</code> on it returns <strong>0</strong> - it compares payload stamps to the root <code>VERSION</code> and has no installed side at all.) <code>update-check.sh:76</code> states the correct thesis - <em>\"VERSION is a promise; rules_sha is the evidence\"</em> - and then greps a token that is a promise for exactly the same reason VERSION is: it was written once, at build, by the thing being checked.</p>\n<p>The sharpest form of this is measurable today. Run as <code>bash .cyberos/version.sh</code>, <code>$here</code> resolves to <code>$CY</code>, so <code>:64</code> reads <code>$root/.cyberos/manifest.yaml</code> and <code>:65</code> reads <code>$here/manifest.yaml</code> - <strong>the same file</strong>. <code>:66-68</code> cannot rescue it, because <code>CYBEROS_PAYLOAD</code> is consulted only when <code>$here/manifest.yaml</code> yields nothing, and it always yields something on an installed machine. So <code>:89</code>'s <code>[ \"$inst_sha\" != \"$pay_sha\" ]</code> is unreachable: that invocation compares a file to itself and <strong>can never report <code>rules_drift</code></strong>. Verified by running it - both sides print <code>66bb0459...</code>, <code>verdict=up_to_date</code>.</p>\n<p>The consequence is not theoretical. On 2026-07-18 this repo's own <code>.cyberos</code> differed from <code>dist/</code> in six vendored artefacts while <code>VERSION</code> read <code>1.0.0</code> and <code>rules_sha</code> matched on both sides. The installed <code>batch-select.mjs</code> was the pre-PR#53 build carrying the undeclared-cone bug, and returned a different batch than source. Every check reported current.</p>\n<p><strong>Defect 2 - the cone is the wrong set.</strong> <code>build.sh:354</code> hashes <code>find cuo plugin mcp cli memory</code>. Measured against the installed tree:</p>\n<pre><code>cone today (build.sh:354)  : cuo plugin mcp cli memory\nvendored set (install.sh)  : cuo plugin mcp memory docs-tools lib + 6 root scripts\n  in cone, NEVER installed : cli            -&gt; guarantees false drift\n  installed, NOT in cone   : docs-tools lib -&gt; the blind spot (4 of the 6 measured drifts)\n  never in cone at all     : the 6 root scripts (:190-195)</code></pre>\n<p>Four of the six artefacts that actually drifted (<code>batch-select.mjs</code>, <code>render-status-hub.mjs</code>, <code>verify-goals.mjs</code>, <code>workflow-improve.mjs</code>) live in <code>docs-tools/</code>. So even a recomputing check would miss them. And <code>cli/</code> is in the cone and is never installed, so a recomputing check with today's cone would report drift on a <strong>perfect</strong> install. The two defects are independent and both must close, or the fix either misses the evidence that motivated it or cries wolf on every machine.</p>\n<p><strong>Why REPAIR and not a second mechanism.</strong> TASK-IMP-104 §1.2 forbids install carrying a second implementation, and 104 explicitly rejected content hashing for ITS question: <em>\"Compare a manifest hash rather than a version. Rejected: overkill for a monotonic version line, and it answers 'different' rather than 'older', which is not the question.\"</em> 104 guards ORDER and is correct as shipped; \"different\" is precisely this task's question. <code>rules_sha</code> is the right mechanism, in the right place, doing the wrong thing. This task repairs it.</p>\n<h2>Proposed Solution</h2>\n<p>Split the two sides of the comparison and treat them differently, because they are not symmetric.</p>\n<p><strong>The installed side is ALWAYS recomputed</strong> - hashed from the files on disk at comparison time, never read from <code>.cyberos/manifest.yaml</code>. That is the whole of Defect 1.</p>\n<p><strong>The reference side is a token that <code>build.sh</code> wrote over a payload tree.</strong> A stored token is trustworthy <em>as a digest of the payload</em>, because build computed it from that payload's bytes; it is untrustworthy <em>as a digest of the install</em>, because nothing recomputed it after the copy. Where a payload tree is also reachable, the check additionally diffs that tree to name the differing paths; where only a token is reachable, it reports drift without naming and says so. This makes <code>bash .cyberos/version.sh</code> useful for the first time: recomputing the installed tree against the token its own manifest carries catches post-install mutation, and <code>manifest.yaml</code>'s exclusion from the cone is what keeps that from being circular.</p>\n<p><strong>Fix the cone to the vendored set</strong> - what <code>install.sh</code> installs, not what the payload ships - declared once in a shared file that <code>build.sh</code> and every comparator read, with a build check that reconciles it against a real install in both directions. Move <code>_rsha()</code> into that same shared file, because <code>build.sh</code> is never vendored and the two installed comparators cannot reach it. Report the differing paths where a tree allows it. Keep each component's existing exit contract.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Add a per-file manifest beside <code>rules_sha</code>.</strong> Rejected: a second comparator for the same question, which TASK-IMP-104 §1.2 forbids by name, and the first draft of this spec proposed it only because it had not found <code>rules_sha</code>.</li><li><strong>Bump VERSION on every payload change.</strong> Rejected: conflates release identity with build identity, forces a bump for a comment fix, and still cannot see a vendor step that drops a file.</li><li><strong>Compare mtimes.</strong> Rejected: not content; survives no copy or checkout faithfully; would have reported the 2026-07-18 drift as fine.</li><li><strong>Re-vendor on every <code>.cyberos</code> use.</strong> Rejected: install is not free, and a guard that silently repairs drift teaches nobody that the channel leaked.</li><li><strong>Widen the cone only, leaving the token stored.</strong> Rejected: it fixes which files are covered and leaves the check comparing two copies of one build-time answer. Defect 1 survives untouched.</li><li><strong>Derive the cone by static analysis of <code>install.sh</code>.</strong> Rejected by the operator, and the code says why: the copies are conditionally guarded (<code>:187-198</code>), one iterates a loop variable (<code>:431-432</code>), and one branch is env-gated (<code>memory/</code> only when <code>CYBEROS_NO_MEMORY != 1</code>). No static read of that file yields the set.</li><li><strong>NEW3-004, the element grammar: <code>dir:</code> / <code>file:</code> / <code>prune:</code>, with memory as <code>dir:memory</code> + <code>prune:memory/store/</code>.</strong> Chosen over the file-granular reading (<code>file:memory/AGENTS.md</code> and its two siblings). Both hash identically today - each resolves to the same 3 files per side - so the tie breaks on what happens when they diverge. Under <code>file:</code> × 3, a fourth file added to the payload's <code>memory/</code> that <code>:431-432</code>'s hardcoded loop does not copy is invisible to every check, forever. Under <code>dir:memory</code>, the cone resolves to 4 while the install has 3, and §1.6's second direction fails the build - the divergence surfaces at build time, on the machine of the person who caused it, instead of becoming permanent drift on every install that no re-install can clear. The file-granular reading also makes <code>prune:memory/store/</code> dead text for §1.4, since nothing then enumerates it - which was the audit's charge; under the chosen grammar it is load-bearing in both §1.4 and §1.6. Three kinds and not two, because <code>install.sh</code> has exactly three copy shapes: <code>cp -R</code> on a directory, <code>cp</code> on a named file, and install-generated content nested inside a coned directory.</li><li><strong>NEW3-005, <code>_rsha()</code>: move it into the shared cone file rather than leave it inline at <code>build.sh:353</code>.</strong> <code>build.sh</code> is never vendored - measured absent from both <code>dist/cyberos/</code> and <code>.cyberos/</code> - so <code>.cyberos/version.sh</code> and <code>.cyberos/lib/update-check.sh</code> cannot reach it, and \"use <code>build.sh</code>'s own <code>_rsha()</code>\" was unsatisfiable for two of the three comparators. The alternative, letting each comparator define its own, is the exact duplication §1.2 forbids for the cone and would let the two platform branches drift apart silently. The shared file goes in <code>lib/</code>, which <code>install.sh:197</code> already vendors wholesale, following <code>lib/version-compare.sh</code> - TASK-IMP-104's \"ONE comparator, sourced\", already resolved two ways by <code>version.sh:78</code> and <code>update-check.sh:62</code>. The file lands inside the cone, so it fingerprints itself; it contains no digest, so nothing is circular.</li><li><strong>NEW3-007, effort: 6 -&gt; 16 hours.</strong> 6 was carried unchanged across four revisions and was sized for \"widen a <code>find</code> and add a re-hash\". The task now also creates a shared vendored library, rewires four callers onto it, adds a reconciler that performs a real install, and edits <code>install.sh</code> to vendor the library. Breakdown: shared lib 2h; <code>build.sh</code> rewire 1h; reconciler 3h; <code>version.sh</code> 2h; <code>update-check.sh</code> 2h; <code>audit-fleet.sh</code> 2h; <code>install.sh</code> 0.5h; suite 5h.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a vendored file mutated by one byte after install, with both VERSIONs and both stored tokens equal, is reported as drift and named. Baseline: reported <code>up_to_date</code> - reproduced synthetically 2026-07-18; the original six-artefact instance was repaired by <code>e2504cf3</code> and is no longer live evidence.</li><li>Guardrail: a freshly installed, unmodified machine reports current from every component. Measured target: the corrected cone yields <code>102dc507</code> over both <code>dist/cyberos</code> and <code>.cyberos</code> (1525 files per side). Baseline: today's cone yields <code>66bb0459</code> vs <code>ae756045</code> on that same byte-identical pair - a recomputing check with today's cone would report drift on a perfect install.</li><li>Guardrail: every path <code>install.sh</code> VENDORS is either inside the cone or inside a named exclusion class, enforced at build. (Not \"every directory the payload ships\": that formulation is retracted by the ADDENDUM and is what forced <code>ci/</code>, <code>cli/</code> and <code>template/</code> into the cone and guaranteed self-drift.)</li></ul>\n<h2>Scope</h2>\n<p>In scope: the cone and <code>_rsha()</code> at <code>build.sh:353-355</code>, relocated to a new shared vendored library; recomputation and path-naming in <code>version.sh</code>, <code>lib/update-check.sh</code>, <code>audit-fleet.sh</code>; the build reconciler; <code>install.sh</code> vendoring the new library; a suite. <code>check-version-sync.sh</code> is deliberately NOT in scope: it has no installed side (<code>grep -c '\\.cyberos'</code> = 0) and only shape-checks the token.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>TASK-IMP-104's ordering guard. Correct as shipped; untouched.</li><li>Install-generated and operator-owned paths (<code>gates.env</code>, <code>config.yaml</code>, <code>.update-check-cache</code>, <code>AGENT-ENTRY.md</code>, <code>memory/store/</code>). Normatively excluded by §1.5, not merely noted.</li><li>Auto-repairing drift. Reporting is the deliverable; <code>install.sh</code> already re-vendors.</li><li>Tamper-evidence. §1.14 makes the prohibition normative rather than a hope in prose.</li><li>Build-side conditional copies that silently drop a file from the payload (<code>build.sh:198</code>). See §3 - a recorded, uncovered gap, not a promise this task can keep.</li></ul>\n<h2>Dependencies</h2>\n<p>None. TASK-IMP-104's guard runs before the lock and is untouched, and the new shared library sits beside <code>lib/version-compare.sh</code> without altering it.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Opus 4.8) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> rewritten in full 2026-07-18 after four independent audits (4/10, 6/10, 6/10, 6/10) found that the prior author patched only what each audit named. This is a whole-document rewrite, not a patch. Every numeric and line-number claim was re-measured against source at HEAD by this author during this rewrite - including those inherited from the evidence file, four of which were wrong (<code>build.sh:357</code>, <code>memory/store/</code> 3-vs-8, \"three separate places\", <code>audit-fleet.sh:19</code>). Claims that could not be measured are marked as gaps in §3 rather than asserted.</li><li><strong>Human review:</strong> scope approved at the 2026-07-18 PLAN gate; both HITL gates are recorded human verdicts. The comparable-set, maintained-list and report-promise decisions are the operator's, recorded in <code>source_decisions</code>; the grammar, <code>_rsha()</code> and effort decisions are this author's, with rationale in Alternatives.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Every component that reports rule drift between an installed machine and a payload (<code>version.sh</code>, <code>lib/update-check.sh</code>, <code>audit-fleet.sh</code>) MUST derive the INSTALLED side by RECOMPUTING the fingerprint over the installed tree at comparison time. No component may derive the installed side by reading a stored token out of <code>.cyberos/manifest.yaml</code>. The REFERENCE side MAY be a stored token, because <code>build.sh</code> computed it over a payload tree: a token is a faithful digest of the payload and never of the install.</li><li>1.2 The cone AND <code>_rsha()</code> MUST each be declared exactly ONCE, in a single shared file (<code>lib/rules-cone.sh</code>) that <code>build.sh</code>, <code>version.sh</code>, <code>lib/update-check.sh</code> and <code>audit-fleet.sh</code> all read by sourcing, and which <code>install.sh</code> MUST vendor so the two installed comparators can reach it. No component may carry its own copy of the list or its own definition of the digest function. The list is MAINTAINED, not derived: no static read of <code>install.sh</code> yields the vendored set, because its copies are conditionally guarded (<code>:187-198</code>), one iterates a loop variable (<code>:431-432</code>), and <code>memory/</code> is env-gated on <code>CYBEROS_NO_MEMORY</code>.</li><li>1.3 Every entry in the shared file MUST carry exactly one of three kinds: <code>dir:&lt;path&gt;</code> (every file beneath it, recursively), <code>file:&lt;path&gt;</code> (exactly that path), or <code>prune:&lt;path&gt;</code> (every file beneath it is REMOVED from the resolved set). An entry with no kind, or with an unrecognised kind, MUST fail the build rather than be silently skipped.</li><li>1.4 The cone MUST be exactly the vendored set: <code>dir:cuo</code>, <code>dir:plugin</code>, <code>dir:mcp</code>, <code>dir:lib</code>, <code>dir:docs-tools</code>, <code>dir:memory</code> (<code>:185-198</code>, <code>:430-433</code>), the six root scripts as <code>file:</code> entries - <code>install.sh uninstall.sh version.sh status.sh help.sh check-latest.sh</code> (<code>:190-195</code>) - and <code>prune:memory/store/</code>. The root scripts ARE in the cone, per the ADDENDUM: they are vendored, and they are byte-identical after a faithful install (measured). \"They change on every payload edit\" is true of the entire cone and is not a reason to exclude anything.</li><li>1.5 The cone MUST exclude three classes, each declared BY NAME alongside it: (a) install-generated or operator-owned paths that appear under <code>.cyberos/</code> and are never copied from the payload - <code>memory/store/</code> (<code>:430</code>, <code>:441-447</code>), <code>gates.env</code> (<code>:282-290</code>), <code>config.yaml</code> (<code>:320-335</code>), <code>.update-check-cache</code> (<code>update-check.sh:99</code>), <code>AGENT-ENTRY.md</code> (heredoc-generated at <code>:484</code>), <code>gates.env.bak.*</code> (<code>:199</code>), <code>.install.lock</code> (<code>:105</code>); (b) <code>manifest.yaml</code>, which IS copied (<code>:188</code>) but MUST be excluded because it CONTAINS the value - <code>build.sh:354</code> computes <code>rules_sha</code> and <code>:358</code> writes the file holding it, so covering it would make the value depend on itself; (c) <code>VERSION</code>, which IS copied (<code>:189</code>) but is TASK-IMP-104's axis, not this one. Classes (b) and (c) MUST be excluded by NAME rather than by omission, so that §1.6 can distinguish a deliberate exclusion from an unclassified path.</li><li>1.6 A build check MUST reconcile the declared list against what <code>install.sh</code> actually installs, in BOTH directions, and MUST fail the build on either. Direction 1: a path present under <code>$CY</code> after an install that is in neither the cone nor an exclusion class MUST fail the build. Direction 2: a path the cone resolves against the payload that is ABSENT from <code>$CY</code> after an install MUST fail the build - this is the direction that catches <code>cli</code>, which is in today's cone and is never vendored, is the measured cause of the <code>66bb0459</code> vs <code>ae756045</code> false drift, and which nothing else catches structurally.</li><li>1.7 The reconciler MUST obtain <code>install.sh</code>'s vendored set by EXECUTING a real install of the freshly built payload into a temporary root and enumerating the resulting <code>$CY</code>, never by parsing <code>install.sh</code>. This is what §1.2 means by reconciling a maintained list against reality: running the loop at <code>:431-432</code> resolves its three names without the reconciler hardcoding them, and running the guards at <code>:187-198</code> resolves them without interpreting them. The reconciler MUST run with <code>CYBEROS_NO_MEMORY</code> unset (the default), MUST enumerate <code>$CY</code> ONLY and not the wider temp repo that <code>install.sh</code> also writes to (<code>:611</code>, <code>:636</code>, <code>:666</code>), and MUST NOT be written so that it derives its expected set from the same list it validates.</li><li>1.8 A recomputed installed fingerprint differing from the reference MUST be reported as drift, INCLUDING when both VERSION strings and both stored manifest tokens are equal.</li><li>1.9 Path-naming MUST be decided per INVOCATION, never per component, because reachability of a payload TREE is a property of how a tool is called. Where a payload tree IS reachable, the report MUST name every differing path. Where only a token is reachable, the report MUST state that it cannot name paths, rather than implying a complete report. The arms, measured: <code>bash &lt;payload&gt;/version.sh &lt;repo&gt;</code> has a tree (<code>$here</code> is the payload); <code>bash .cyberos/version.sh</code> has NONE (<code>$here == $CY</code>, and <code>CYBEROS_PAYLOAD</code> at <code>:44</code>/<code>:66</code> is a fallback that a present installed manifest pre-empts); <code>lib/update-check.sh</code> has a tree when <code>CYBEROS_PAYLOAD</code> is set (<code>:84</code> takes PRECEDENCE over <code>self_root</code>) or when sourced from a payload's <code>lib/</code> (<code>:86</code>), and has none when sourced from <code>.cyberos/lib/</code> with <code>CYBEROS_PAYLOAD</code> unset (<code>self_root == $CY</code>); <code>audit-fleet.sh</code> has a tree in its DEFAULT mode (<code>:16-17</code> resolve <code>$_self/dist/cyberos</code>) and only a token under <code>CYBEROS_EXPECT_RULES_SHA</code> (<code>:14</code>, which short-circuits <code>:15</code>'s resolution entirely).</li><li>1.10 An installed tree byte-identical to the payload ACROSS THE CONE MUST be reported as current by every component. \"Byte-identical across the cone\" is the whole test: paths outside the cone MUST NOT affect the verdict - neither <code>ci/</code>, <code>cli/</code> and <code>template/</code>, which ship and are never installed, nor <code>memory/store/</code>, which installs and never ships.</li><li>1.11 Recomputation MUST use the shared <code>_rsha()</code> (<code>sha256sum</code> on Linux, <code>shasum -a 256</code> on macOS) and its <code>LC_ALL=C</code> sort, so that a byte-identical tree yields a byte-identical fingerprint on both platforms and on both sides of every comparison.</li><li>1.12 A comparison that cannot be computed (the reference manifest absent or unreadable, the installed tree unreadable, or the shared cone file unreadable) MUST yield the verdict <code>unknown</code>. No component may yield a current verdict on a comparison it could not perform.</li><li>1.13 Exit contracts: <code>lib/update-check.sh</code> remains soft-by-default (<code>:18</code>) and MUST NOT change its exit semantics in any mode it binds. <code>audit-fleet.sh</code> MUST NOT retain its present fail-open behaviour (<code>:19</code>, \"rule-drift check DISABLED\"); with no expected token it MUST yield <code>unknown</code> per §1.12 rather than warning and silently passing.</li><li>1.14 No component's output may describe the fingerprint as detecting tampering, since anything able to rewrite a vendored file can rewrite the manifest beside it.</li><li>1.15 A comparison MUST NOT modify the installed machine. <code>lib/update-check.sh</code> alone MAY continue writing its existing <code>$cy/.update-check-cache</code> (<code>:99</code>, reached only when none of the three early returns at <code>:19</code>, <code>:28</code> and <code>:39</code> fires); no other component may write anything.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - for EACH of the three comparators, mutating one byte of a vendored file while leaving BOTH stored manifest tokens untouched is RECOMPUTED to a differing digest and reported as drift; the test MUST exercise all three by name and MUST FAIL if any one still answers the installed side from the stored token - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t01_installed_side_recomputed_not_recalled</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>lib/rules-cone.sh</code> is the ONLY in-tree definition of both the cone and <code>_rsha()</code>, is sourced by <code>build.sh</code>, <code>version.sh</code>, <code>lib/update-check.sh</code> and <code>audit-fleet.sh</code>, and is present at <code>.cyberos/lib/rules-cone.sh</code> after an install; the test MUST FAIL if any caller carries a second copy of either, MUST FAIL against today's <code>build.sh:354</code> which inlines <code>find cuo plugin mcp cli memory</code>, and MUST FAIL if <code>install.sh</code> does not vendor it - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t02_one_shared_vendored_declaration</code></li><li>[ ] AC 3 (traces_to: #1.3) - a cone entry with no kind prefix, and separately one with an unrecognised kind, each FAIL the build rather than being skipped; and each of the three kinds resolves as specified - <code>dir:</code> pulls in a nested file, <code>file:</code> pulls in exactly one path and not its siblings, <code>prune:</code> removes a file beneath a coned dir - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t03_element_grammar_dir_file_prune</code></li><li>[ ] AC 4 (traces_to: #1.4) - the cone resolves to exactly the vendored set: mutating one file under EACH of <code>cuo plugin mcp lib docs-tools memory</code> and EACH of the six root scripts yields drift (12 arms, each asserted separately); the test MUST FAIL on today's cone, which omits <code>lib</code>, <code>docs-tools</code> and all six root scripts, and MUST FAIL if <code>cli</code> is present in the cone - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t04_cone_is_exactly_the_vendored_set</code></li><li>[ ] AC 5 (traces_to: #1.5) - each exclusion class yields NO drift when its member changes, tested by a member that requires an ACTIVE prune and not only by trivial members: class (a) via <code>memory/store/</code> (adding a file INSIDE the coned <code>dir:memory</code>) AND via <code>gates.env</code>, <code>config.yaml</code>, <code>.update-check-cache</code> and <code>AGENT-ENTRY.md</code>; class (b) via mutating <code>manifest.yaml</code>'s own <code>rules_sha:</code> line; class (c) via <code>VERSION</code>; and the test MUST FAIL if class (b) or (c) is excluded by omission rather than by a named entry - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t05_exclusions_named_and_pruned</code></li><li>[ ] AC 6 (traces_to: #1.6) - BOTH directions fail the build, each asserted separately: direction 1, adding a <code>cp</code> of a NEW path into <code>install.sh</code> that is in neither cone nor exclusions fails the build naming that path; direction 2, a cone entry the install does not produce fails the build, exercised with the REAL <code>cli</code> fixture (add <code>dir:cli</code> to the cone, build, assert non-zero exit naming <code>cli</code>), and the test MUST FAIL if the build passes with <code>cli</code> coned - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t06_reconciler_fails_both_directions</code></li><li>[ ] AC 7 (traces_to: #1.7) - the reconciler obtains its expected set by EXECUTING install.sh into a temp root: the test asserts a temp <code>$CY</code> was created and enumerated, asserts the <code>:431-432</code> memory loop resolved to its three files with no memory filename hardcoded in the reconciler, and MUST FAIL if the reconciler is rewritten to parse <code>install.sh</code> textually, if it derives its expected set from the list it validates, or if it enumerates any path outside <code>$CY</code> - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t07_reconciler_runs_install_does_not_parse_it</code></li><li>[ ] AC 8 (traces_to: #1.8) - drift is reported with both VERSION strings AND both stored tokens equal - the exact 2026-07-18 shape; the test MUST FAIL if any component reports current - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t08_equal_tokens_still_drift</code></li><li>[ ] AC 9 (traces_to: #1.9) - per INVOCATION and not per component, all seven measured arms asserted separately: <code>bash &lt;payload&gt;/version.sh &lt;repo&gt;</code> names all THREE mutated files and MUST FAIL if only the first is named; <code>bash .cyberos/version.sh</code> states it cannot name paths AND still reports the drift AND MUST FAIL if setting <code>CYBEROS_PAYLOAD</code> changes either (it is a fallback, pre-empted at <code>:66</code>); <code>update-check.sh</code> with <code>CYBEROS_PAYLOAD</code> set names paths, sourced from a payload's <code>lib/</code> names paths, and sourced from <code>.cyberos/lib/</code> without it states it cannot; <code>audit-fleet.sh</code> on its DEFAULT names them, and under <code>CYBEROS_EXPECT_RULES_SHA</code> states it cannot - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t09_naming_follows_invocation_not_component</code></li><li>[ ] AC 10 (traces_to: #1.10) - a freshly installed unmodified machine is reported CURRENT by every component, asserting the measured <code>102dc507</code> on both sides over 1525 files per side; the test MUST FAIL on today's cone, which yields <code>66bb0459</code> over the payload vs <code>ae756045</code> over the install on that byte-identical pair - caused INDEPENDENTLY by <code>cli/</code> (coned, never installed) AND by <code>memory/store/</code> (install-generated inside a coned dir, 0 payload files vs 5 installed), and the AC MUST exercise both by asserting all four measured combinations: today <code>66bb0459</code>/<code>ae756045</code>, prune-cli-only <code>1f05a84f</code>/<code>ae756045</code>, prune-store-only <code>66bb0459</code>/<code>1f05a84f</code>, prune-both <code>1f05a84f</code>/<code>1f05a84f</code> - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t10_clean_install_is_current</code></li><li>[ ] AC 11 (traces_to: #1.11) - every comparator RESOLVES <code>_rsha()</code> from the shared file and carries no second definition (identity of implementation, not merely a matching digest), the recomputed fingerprint EQUALS <code>build.sh</code>'s for the same tree, and a fixture forcing the <code>shasum -a 256</code> branch yields the same digest as one forcing <code>sha256sum</code>; the test MUST FAIL if either platform branch diverges or if any comparator defines its own <code>_rsha</code> - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t11_shared_rsha_identity_and_cross_platform</code></li><li>[ ] AC 12 (traces_to: #1.12) - with the reference manifest removed, separately with it unreadable, separately with the installed tree unreadable, and separately with <code>lib/rules-cone.sh</code> unreadable, every component emits the verdict <code>unknown</code> and none emits a current verdict; the assertion is on the emitted VERDICT (§1.12's verb) - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t12_uncomputable_is_unknown_not_current</code></li><li>[ ] AC 13 (traces_to: #1.13) - <code>update-check.sh</code> keeps its exit semantics in EVERY mode it binds (<code>soft</code> returns 0 on drift, <code>strict</code> returns non-zero, <code>always</code> ignores the throttle, <code>0</code>/<code>off</code>/<code>false</code> returns 0 immediately) - a regression guardrail, stated as such; and <code>audit-fleet.sh</code> with no expected token emits <code>unknown</code> and does NOT pass, which MUST FAIL against today's <code>:19</code> warning-and-continue - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t13_exit_contracts</code></li><li>[ ] AC 14 (traces_to: #1.14) - no component's output matches tamper/integrity/authenticity wording on a drift run, an <code>unknown</code> run, OR a current run - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t14_no_tamper_claims</code></li><li>[ ] AC 15 (traces_to: #1.15) - the installed tree is byte-identical before and after a comparison from EACH component; <code>.update-check-cache</code> is written by <code>lib/update-check.sh</code> ONLY, and the test MUST FAIL if <code>version.sh</code> or <code>audit-fleet.sh</code> writes it or anything else - test: <code>tools/install/tests/test_rules_sha_recompute.sh::t15_check_is_read_only</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Payload NEWER than installed with VERSION differing: TASK-IMP-104's ordering guard owns the refusal; this check reports drift and does not duplicate it.</li><li>Operator hand-edited a vendored file: reported as drift and named. The report says what differs, never who differed it (§1.14 covers the wording).</li><li>A vendored file that is legitimately empty: hashed like any other. Empty is a content.</li><li>First install, no installed tree: §1.12's <code>unknown</code>, not drift and not current.</li><li><strong>A BUILD-side conditional copy that silently drops a file from the payload</strong> (<code>build.sh:198</code>'s <code>[ -f \"$here/docs-tools/workflow-improve.mjs\" ] &amp;&amp; cp ...</code> - the measured <code>workflow-improve.mjs</code> case): the file never reaches the payload, so it is absent from the payload fingerprint AND from the install, and both sides agree. <strong>Nothing in this task catches that.</strong> §1.6's reconciler cannot either: it resolves the cone against the payload, and a file missing from the payload is missing from that resolution too. Recorded as an uncovered gap, not handed to a clause that cannot honour it. It is the sibling defect and wants its own task.</li><li><strong>A payload file under a coned dir that <code>install.sh</code> does not vendor</strong> is caught at BUILD by §1.6's direction 2, not at comparison time - deliberately. The build fails for the person who caused the divergence, instead of every installed machine reporting a drift that no re-install can clear. This is the <code>dir:memory</code> grammar decision doing its work.</li><li><code>audit-fleet.sh</code> with a reachable payload rather than a bare token: it MAY name paths per §1.9's first arm; the clause requires the disclaimer only when no tree is reachable.</li><li><code>CYBEROS_NO_MEMORY=1</code> installs: <code>memory/</code> is absent from <code>$CY</code> by design, so §1.6's direction 2 would fail the build if the reconciler ran under it. §1.7 pins the reconciler to the default environment; the env-gated variant is a documented install mode, not a cone violation, and this task does not extend the reconciler to cover it. A machine installed with <code>CYBEROS_NO_MEMORY=1</code> will report drift against a memory-carrying payload, correctly and unhelpfully - out of scope here.</li><li>macOS vs Linux: §1.11 pins the shared <code>_rsha()</code> and <code>LC_ALL=C</code>. This is the <code>stat -c/-f</code> class of defect TASK-IMP-103 already paid for once.</li><li>Security-class: the fingerprint is a staleness signal, not an integrity guarantee. §1.14 makes that normative and AC 14 tests it across all three verdicts.</li></ul>";
