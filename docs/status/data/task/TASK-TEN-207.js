window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-207"]="<h1>TASK-TEN-207: api_calls overage → 402</h1>\n<h2>Summary</h2>\n<p>Before <code>next.run</code> in <code>verify_jwt</code>, evaluate api_calls overage against plan-tier caps. On <code>OverageDecision::Block</code>, return <code>402</code> with <code>{error:\"overage_blocked\",axis:\"api_calls\",current,cap}</code> and do not emit.</p>\n<h2>Problem</h2>\n<p>Emit path bills successes but never rejects over-cap traffic (TEN-205 OOS).</p>\n<h2>Proposed Solution</h2>\n<ol><li><code>cyberos_metering::admission::admit_quantity</code> pure helper over <code>evaluate()</code>.</li><li><code>metering_admit.rs</code>: resolve plan_tier (Pg or test override), current usage (InMemory sum / optional Pg period_sum), policy default <code>warn</code> (testable override to <code>block</code>).</li><li><code>verify_jwt</code>: after deny-list, before <code>next.run</code> — if Block → 402.</li><li>Warn decisions: allow request; emit still on success (no memory audit in host-d).</li><li>Tests: seed usage at cap + Block policy → 402; under cap → 200-path emit ok.</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>402 on response path after handler.</strong> Rejected: TEN-004 §1 #11 rejects upstream; work must not run.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: over-cap + block → 402, zero new emit.</li><li>Guardrail: under-cap requests unchanged.</li></ul>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Admission helper + middleware hook + tests for api_calls only.</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>ai_tokens overage at ai-gateway</li><li>Tenant overage_policy column migration (host-d uses default + test override)</li><li>Memory audit for warn/block</li><li>GET /v1/usage</li></ul>\n<h2>Dependencies</h2>\n<ul><li>TASK-TEN-206 (usage sum + durable path).</li><li>TASK-TEN-002 caps (<code>caps_for</code>).</li></ul>\n<h2>Acceptance Criteria</h2>\n<ol><li><strong>Block</strong> — current+1 &gt; cap + policy block → 402 body shape.</li><li><strong>No emit on 402</strong> — recorder length unchanged.</li><li><strong>Under cap</strong> — admission allows; success still emits (TEN-205).</li><li><strong>Warn policy</strong> — over threshold does not 402.</li><li><strong>Enterprise unlimited api</strong> — cap None → always allow.</li><li><strong>Default policy warn</strong> — production default does not 402 without override.</li></ol>\n<h2>Verification</h2>\n<pre><code class=\"language-sh\">cd services &amp;&amp; cargo test -p cyberos-metering -p cyberos-auth -- --test-threads=1\nbash .cyberos/cuo/gates/run-gates.sh</code></pre>\n<hr>\n<p><em>End of TASK-TEN-207.</em></p>";
