window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-071"]="<h1>TASK-IMP-071: Durable release trigger</h1>\n<h2>§1 - Description</h2>\n<ol><li>version.yml's bump commit MUST NOT carry <code>[skip ci]</code>; the message stays <code>chore(release): vX.Y.Z</code>.</li><li>The workflow's own loop protection MUST rest solely on the existing job guard (<code>!startsWith(head_commit.message, 'chore(release):')</code>), documented as the single brake.</li><li>The TASK-DOCS-007 §1 #4c deploy-dispatch workaround MUST be removed (deploy.yml's VERSION path filter now fires natively on the bump push); the <code>actions: write</code> permission it needed goes with it.</li><li>TASK-IMP-068 §1 #7's inline proof MUST remain (belt-and-suspenders now that payload-gate also sees bump commits), with its comment updated; both affected shipped tasks carry post-ship amendment notes.</li><li>RELEASE.md MUST document the new model: <code>git push origin vX.Y.Z</code> triggers the release; dispatch remains the fallback/retry path.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>[skip ci] suppressed MORE than the loop it guarded against - it silenced tag pushes and the docs deploy, costing a manual dispatch per release plus a dispatch workaround. The message-prefix guard was already the real loop brake; removing the blunt instrument leaves exactly one, documented mechanism.</p>\n<h2>§3 - Contract</h2>\n<p>Bump commit: <code>chore(release): X.Y.Z</code> (no suffix). Loop brake: version.yml job <code>if</code> guard only.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>No [skip ci] anywhere live</strong> (§1 #1) - grep on version.yml's commit line is clean; RELEASE.md describes the new model.</li><li><strong>Guard is sole + documented</strong> (§1 #2) - the job <code>if</code> survives; comments name it the single brake.</li><li><strong>Workaround retired</strong> (§1 #3) - no <code>gh workflow run deploy.yml</code> in version.yml; <code>actions: write</code> gone.</li><li><strong>Amendments recorded</strong> (§1 #4) - TASK-IMP-068 + TASK-DOCS-007 specs carry the notes.</li></ol>\n<h2>§5 - Verification</h2>\n<p>Grep-level asserts (executable): <code>! grep -q 'skip ci]' .github/workflows/version.yml</code> commit line; <code>grep -q \"single brake\\|ONLY loop guard\" version.yml</code>; <code>! grep -q \"workflow run deploy\" version.yml</code>; amendment greps on both specs. Live proof: the next bump push must show payload-gate + deploy runs on the bump commit, and the next <code>git push origin vX.Y.Z</code> must start release.yml without dispatch (operator-observed, recorded at the next release).</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Three-line yaml change + comment updates + doc + two amendment notes.</p>\n<h2>§7 - Dependencies</h2>\n<p>None. Interacts with TASK-IMP-068 (inline proof kept) and TASK-DOCS-007 (workaround retired).</p>\n<h2>§8 - Example payloads</h2>\n<p><code>chore(release): 0.5.0</code> -&gt; payload-gate ✓, deploy(docs) ✓, version.yml skipped by guard; <code>git push origin v0.5.0</code> -&gt; release run starts.</p>\n<h2>§9 - Open questions</h2>\n<p>None blocking.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Guard weakened in a future edit -&gt; bump loop; the comment names it the single brake and RELEASE.md repeats it.</li><li>A human commit starting <code>chore(release):</code> skips version.yml -&gt; by design (that IS the release-commit namespace).</li><li>payload-gate red on a bump commit -&gt; impossible-by-construction drift becomes visible instead of silent; exactly what the gate is for.</li><li>Rulesets block the bump push -&gt; unchanged degrade-to-warning path.</li><li>Old tags on [skip ci] commits -&gt; historical; dispatch fallback still cuts them.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Batch-mode ship under the operator's standing verdict; live-proof clause lands at the next release (operator observation recorded then).</p>\n<p><em>End of TASK-IMP-071.</em></p>";
