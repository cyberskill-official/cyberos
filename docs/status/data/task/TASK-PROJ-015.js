window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-015"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The Timeline view <strong>MUST</strong> render issues as horizontal bars on a day-grid × assignee swimlane. The contract:</p>\n<ol><li><strong>MUST</strong> present X-axis = days within cycle window (defaults: cycle.starts_at → cycle.ends_at; configurable zoom day/week/month).</li><li><strong>MUST</strong> present Y-axis = swimlanes, one per assignee active in cycle (i.e. assigned to ≥1 issue in the cycle); ordered alphabetically; \"Unassigned\" lane at top.</li><li><strong>MUST</strong> render each issue as a horizontal bar positioned by <code>starts_at..ends_at</code>. Bar height proportional to swimlane row height; bar colour from status enum (TASK-PROJ-018 design tokens).</li><li><strong>MUST</strong> support drag-to-move: dragging mid-bar changes both starts_at and ends_at by Δdays.</li><li><strong>MUST</strong> support edge-drag-to-resize: left edge changes starts_at; right edge changes ends_at. Minimum bar = 1 day; resize past min snaps.</li><li><strong>MUST</strong> PATCH date changes via TASK-PROJ-003 LWW scalar handlers (issue.starts_at + issue.ends_at are LWW fields).</li><li><strong>MUST</strong> render milestone markers from TASK-PROJ-007 Fixed-Fee config: vertical gold-bordered line at <code>target_date</code>; hover tooltip shows milestone name + amount.</li><li><strong>MUST</strong> show today-indicator: vertical thin line marking current date; subtle pulse animation.</li><li><strong>MUST</strong> support keyboard navigation parity:</li></ol>\n<ul><li>Tab cycles swimlanes + bars.</li><li>J/K move swimlanes; H/L move within swimlane.</li><li>Shift+→/← extend/shrink bar by 1 day.</li><li>Shift+Cmd+→/← move bar by 1 day.</li><li>Enter opens Brief Modal.</li></ul>\n<ol><li><strong>MUST</strong> lazy-render swimlanes off-screen via IntersectionObserver; ≥ 60fps with 50+ swimlanes.</li><li><strong>MUST</strong> emit memory audit <code>proj.timeline_bar_moved</code> per resize/move with <code>{issue_id, field, before, after, was_keyboard, trace_id}</code>.</li><li><strong>MUST</strong> emit OTel client metrics:</li></ol>\n<ul><li><code>proj_timeline_render_p95_ms</code>.</li><li><code>proj_timeline_resize_latency_ms</code>.</li></ul>\n<ol><li><strong>MUST</strong> RLS-enforce (issues + assignees only for tenant).</li><li><strong>MUST</strong> pass axe-core (per TASK-PROJ-018 a11y CI).</li><li><strong>MUST</strong> support workload overlap visualisation: when multiple bars in same swimlane overlap (one assignee has 3 concurrent issues), stack them vertically OR show a \"3\" badge on the densest period.</li><li><strong>MUST</strong> support <code>?from=&amp;to=</code> URL params overriding cycle window (operator scrolls beyond cycle); preserve in URL for shareability.</li><li><strong>MUST</strong> support snap-to-week + snap-to-month at week/month zoom: drag end snaps to nearest week boundary; precise date entry via Brief Modal.</li><li><strong>MUST</strong> support per-assignee filter: <code>?assignee=&lt;uuid&gt;</code> shows only that swimlane (full-width). Useful for 1:1 reviews.</li><li><strong>MUST</strong> include dependency arrows between issues (basic, slice-3 minimal version): when issue A <code>depends_on</code> issue B, render thin arrow from B's right edge to A's left edge. Full Gantt is TASK-PROJ-016.</li><li><strong>MUST</strong> support \"show non-active members\" toggle: by default, only members with ≥1 issue in cycle; toggle shows all team members (even with no issues) to plan capacity.</li><li><strong>MUST</strong> highlight bars that span weekends/holidays differently (faded weekend background) so operators see when work is planned across non-business days.</li><li><strong>MUST</strong> support keyboard reordering of swimlanes: focused swimlane + Shift+Up/Down moves it; persists in <code>cyberos_proj_timeline_swimlane_order</code> per user.</li><li><strong>MUST</strong> include cycle goal/theme banner above the timeline: shows cycle name + goal text + days-remaining count.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why day-grid × assignee (DEC-360)?</strong> Two questions ops ask: \"what is everyone doing this week\" + \"when does X land.\" Swimlane answers both — vertical scan shows team coverage; horizontal scan shows duration.</p>\n<p><strong>Why LWW resize (DEC-361)?</strong> Date changes are simple scalars; TASK-PROJ-003 already provides LWW infrastructure. Two users dragging same bar → last-writer-wins with subject_id tie-break.</p>\n<p><strong>Why milestone markers (DEC-362)?</strong> Fixed-Fee engagements bill at milestones; visible markers show \"are we tracking to next milestone.\" Without them, milestones are invisible until billing closes.</p>\n<p><strong>Why zoom (§1 #1)?</strong> A 6-month engagement at day-resolution = 180 columns; too wide. Week-zoom = 26 columns. Month-zoom = 6. Operator chooses based on planning horizon.</p>\n<p><strong>Why lazy swimlane render (§1 #10)?</strong> 50-person teams produce 50 swimlanes × 60 days × 5 bars/lane = thousands of DOM nodes. Off-screen lanes don't render until scrolled to.</p>\n<p><strong>Why 1-day minimum (§1 #5)?</strong> Sub-day estimates aren't tracked (time-entries are; but issue planning works in days). Snap-to-minimum prevents zero-width bars.</p>\n<p><strong>Why overlap visualisation (§1 #15)?</strong> Operator scanning workload should see \"Alice has 3 concurrent issues\" — overlap signals over-commitment.</p>\n<p><strong>Why URL window override (§1 #16)?</strong> Operators want to scroll past the cycle boundary (look at next cycle's work-in-flight); URL preserves the view for sharing.</p>\n<p><strong>Why snap-to-week/month (§1 #17)?</strong> At week zoom, single-day precision in drag is hard; snap-to-week matches the visual grid. Precise dates available in modal.</p>\n<p><strong>Why per-assignee filter (§1 #18)?</strong> 1:1 reviews focus on one person's work; full-width view = readable.</p>\n<p><strong>Why minimal dependency arrows here (§1 #19)?</strong> Operators in slice-3 want basic \"X depends on Y\" visualisation; full Gantt (critical path, parallel chains) is TASK-PROJ-016.</p>\n<p><strong>Why show non-active members (§1 #20)?</strong> Capacity planning: \"who's free to take this on?\" requires seeing empty swimlanes.</p>\n<p><strong>Why weekend highlight (§1 #21)?</strong> Work-planning-on-weekends often unintentional; visual cue surfaces.</p>\n<p><strong>Why kbd swimlane reorder (§1 #22)?</strong> Operators prioritise lane order (team-lead first, etc.); keyboard parity for ordering.</p>\n<p><strong>Why cycle goal banner (§1 #23)?</strong> Context-setting: what is this cycle for? Days remaining = urgency signal.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/Timeline/Timeline.tsx\ntype Zoom = 'day' | 'week' | 'month';\n\nexport function Timeline({ cycleId }: { cycleId: string }) {\n  const [zoom, setZoom] = useState&lt;Zoom&gt;('day');\n  const cycle = useCycle(cycleId);\n  const issues = useIssuesForCycle(cycleId);\n  const milestones = useMilestonesForCycle(cycleId);\n  const swimlanes = useMemo(() =&gt; groupBySwimlane(issues), [issues]);\n\n  return (\n    &lt;div className=\"timeline\" role=\"application\" aria-label=\"Timeline view\"&gt;\n      &lt;ZoomControls value={zoom} onChange={setZoom} /&gt;\n      &lt;DayGrid start={cycle.starts_at} end={cycle.ends_at} zoom={zoom}&gt;\n        {milestones.map(m =&gt; &lt;MilestoneMarker key={m.id} date={m.target_date} milestone={m} /&gt;)}\n      &lt;/DayGrid&gt;\n      &lt;TodayIndicator /&gt;\n      {swimlanes.map(lane =&gt; (\n        &lt;Swimlane key={lane.assigneeId ?? 'unassigned'} lane={lane}&gt;\n          {lane.issues.map(issue =&gt; &lt;IssueBar key={issue.id} issue={issue} zoom={zoom} /&gt;)}\n        &lt;/Swimlane&gt;\n      ))}\n    &lt;/div&gt;\n  );\n}</code></pre>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/Timeline/IssueBar.tsx\nexport function IssueBar({ issue, zoom }: { issue: Issue; zoom: Zoom }) {\n  const [draftDates, setDraftDates] = useState&lt;{starts_at: Date; ends_at: Date}&gt;({\n    starts_at: issue.starts_at, ends_at: issue.ends_at,\n  });\n\n  const left  = computeOffset(draftDates.starts_at, zoom);\n  const width = computeWidth(draftDates.starts_at, draftDates.ends_at, zoom);\n\n  return (\n    &lt;div className=\"issue-bar\"\n         style={{ left, width, backgroundColor: tokens.status[issue.status] }}\n         role=\"button\" tabIndex={0}\n         aria-label={`${issue.title} from ${fmt(issue.starts_at)} to ${fmt(issue.ends_at)}`}\n         onKeyDown={handleKbd}&gt;\n      &lt;ResizeHandle edge=\"left\"  onDrag={Δ =&gt; setStartsAt(addDays(issue.starts_at, Δ))} /&gt;\n      &lt;span className=\"title\"&gt;{issue.title}&lt;/span&gt;\n      &lt;ResizeHandle edge=\"right\" onDrag={Δ =&gt; setEndsAt(addDays(issue.ends_at, Δ))} /&gt;\n    &lt;/div&gt;\n  );\n\n  async function setStartsAt(newDate: Date) {\n    setDraftDates(d =&gt; ({ ...d, starts_at: newDate }));\n    const res = await writeScalarLWW(issue.id, 'starts_at', newDate.toISOString(), jwt);\n    if (!res.accepted) { rollbackDates(); toast('Stale write; refreshed'); }\n    emitMemory('proj.timeline_bar_moved', { issue_id: issue.id, field: 'starts_at',\n                                            before: issue.starts_at, after: newDate,\n                                            was_keyboard: false });\n  }\n\n  function handleKbd(e: React.KeyboardEvent) {\n    if (e.shiftKey &amp;&amp; e.key === 'ArrowRight') {\n      setEndsAt(addDays(issue.ends_at, 1));\n    } else if (e.shiftKey &amp;&amp; e.key === 'ArrowLeft') {\n      setEndsAt(addDays(issue.ends_at, -1));\n    }\n    // ... Cmd+Shift+Arrow for move, etc.\n  }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Day-grid renders cycle window</strong> — cycle Jan 1 → Mar 31 → 90 day columns at day-zoom.</li><li><strong>Swimlanes for active assignees</strong> — fixture: 3 members assigned → 3 lanes (+ Unassigned if applicable).</li><li><strong>Issue bar positioned correctly</strong> — starts_at Mar 5, ends_at Mar 10 → bar at days 64..68 from cycle start.</li><li><strong>Drag move shifts both dates</strong> — drag bar 3 days right → starts_at + 3, ends_at + 3; PATCH fires.</li><li><strong>Edge-resize updates one date</strong> — drag right edge → ends_at only updates.</li><li><strong>1-day minimum enforced</strong> — drag right edge left past starts_at → snaps to starts_at + 1.</li><li><strong>Milestone markers render</strong> — Fixed-Fee milestones → gold lines at target_date.</li><li><strong>Today indicator pulses</strong> — current date → animated vertical line.</li><li><strong>Zoom day→week</strong> — bar width recomputes; columns coalesce.</li><li><strong>Kbd: Shift+→ extends end by 1 day</strong> — focused bar + shortcut → ends_at +1.</li><li><strong>Kbd: Cmd+Shift+→ moves bar 1 day forward</strong> — both dates +1.</li><li><strong>Lazy swimlane render</strong> — 50 swimlanes; ~10 visible → only those rendered.</li><li><strong>LWW reject on stale</strong> — concurrent edit detected → rollback + toast.</li><li><strong>memory audit per move</strong> — <code>proj.timeline_bar_moved</code> row.</li><li><strong>OTel resize latency metric</strong> — drag completion → histogram populated.</li><li><strong>axe-core passes</strong> — no critical/serious violations.</li><li><strong>Hover milestone tooltip</strong> — hover gold line → tooltip with name + amount_minor formatted.</li><li><strong>RLS isolates</strong> — tenant A's cycle invisible to tenant B.</li><li><strong>Overlap stacking</strong> — 3 concurrent bars in one swimlane → stacked vertically; badge if &gt; 5 (AC for §1 #15).</li><li><strong>URL window override</strong> — <code>?from=2026-01-01&amp;to=2026-06-30</code> → renders extended window; URL preserved (AC for §1 #16).</li><li><strong>Snap-to-week at week zoom</strong> — drag to mid-week at week zoom → snaps to nearest week boundary (AC for §1 #17).</li><li><strong>Per-assignee filter</strong> — <code>?assignee=&lt;uuid&gt;</code> → single swimlane full-width (AC for §1 #18).</li><li><strong>Dependency arrow rendered</strong> — A depends_on B → arrow from B's right to A's left (AC for §1 #19).</li><li><strong>Show non-active members toggle</strong> — toggle on → empty swimlanes appear (AC for §1 #20).</li><li><strong>Weekend background faded</strong> — Sat/Sun column has faded bg color (AC for §1 #21).</li><li><strong>Kbd swimlane reorder</strong> — focused swimlane + Shift+Up → moves up; persists (AC for §1 #22).</li><li><strong>Cycle banner shows goal + days remaining</strong> — banner above timeline (AC for §1 #23).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">test('issue bar positions by starts_at/ends_at', () =&gt; {\n  const cycle = mkCycle({ starts_at: '2026-01-01', ends_at: '2026-03-31' });\n  const issue = mkIssue({ starts_at: '2026-01-05', ends_at: '2026-01-10' });\n  render(&lt;Timeline cycleId={cycle.id} /&gt;);\n  const bar = screen.getByLabelText(/from 2026-01-05/);\n  expect(parseInt(bar.style.left)).toBeCloseTo(4 * DAY_PX, 0);\n  expect(parseInt(bar.style.width)).toBeCloseTo(5 * DAY_PX, 0);\n});\n\ntest('edge-resize updates ends_at only', async () =&gt; {\n  const { user } = render(&lt;Timeline cycleId={cycle.id} /&gt;);\n  const bar = screen.getByTestId('issue-bar-iss-1');\n  const rightHandle = within(bar).getByTestId('resize-right');\n  await user.drag(rightHandle, { delta: { x: 2 * DAY_PX, y: 0 } });\n  expect(mockPatch).toHaveBeenCalledWith('iss-1', 'ends_at', expect.any(String));\n  expect(mockPatch).not.toHaveBeenCalledWith('iss-1', 'starts_at', expect.anything());\n});\n\ntest('1-day minimum snap', async () =&gt; {\n  const issue = mkIssue({ starts_at: '2026-01-05', ends_at: '2026-01-10' });\n  const { user } = render(&lt;Timeline cycleId={cycle.id} /&gt;);\n  const rightHandle = screen.getByTestId('resize-right');\n  await user.drag(rightHandle, { delta: { x: -100 * DAY_PX, y: 0 } });\n  // ends_at can't go past starts_at + 1\n  expect(mockPatch).toHaveBeenLastCalledWith('iss-1', 'ends_at', '2026-01-06T...');\n});\n\ntest('milestone markers from fixed-fee', () =&gt; {\n  const eng = mkEngagement({ billing_mode: FixedFee, milestones: [\n    { id: 'm1', target_date: '2026-02-15', amount_minor: 50_000_000 }\n  ]});\n  render(&lt;Timeline cycleId={cycle.id} /&gt;);\n  expect(screen.getByTestId('milestone-marker-m1')).toBeInTheDocument();\n});\n\ntest('kbd Shift+Right extends', async () =&gt; {\n  const { user } = render(&lt;Timeline cycleId={cycle.id} /&gt;);\n  const bar = screen.getByTestId('issue-bar-iss-1');\n  bar.focus();\n  await user.keyboard('{Shift&gt;}{ArrowRight}{/Shift}');\n  expect(mockPatch).toHaveBeenCalledWith('iss-1', 'ends_at', expect.stringMatching(/2026-01-11/));\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Sketches above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-002</strong> — WebSocket sync.</li><li><strong>TASK-PROJ-003</strong> — LWW scalar (starts_at, ends_at).</li><li><strong>TASK-PROJ-007</strong> — milestone source.</li><li><strong>TASK-PROJ-014</strong> — kbd-nav patterns reused.</li><li><strong>TASK-PROJ-018</strong> — design tokens.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.timeline_bar_moved\",\n  \"payload\": {\n    \"issue_id\": \"iss-...\",\n    \"field\": \"ends_at\",\n    \"before\": \"2026-03-10\",\n    \"after\": \"2026-03-12\",\n    \"was_keyboard\": true,\n    \"trace_id\": \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Dependency arrows between issues (Gantt-style) — TASK-PROJ-016.</li><li>Multi-cycle view (combine adjacent cycles) — slice 4+.</li><li>Workload heatmap (overlapping bars per assignee) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Drag past cycle boundary</td><td>clamp at boundary</td><td>Visual snap</td><td>None</td></tr><tr><td>Concurrent resize</td><td>LWW reject</td><td>Rollback + toast</td><td>User refreshes</td></tr><tr><td>Issue without dates</td><td>omit from timeline</td><td>Visible in Unassigned lane</td><td>Operator sets dates</td></tr><tr><td>100+ swimlanes</td><td>lazy + scroll</td><td>Smooth</td><td>None</td></tr><tr><td>Milestone after cycle.ends_at</td><td>render at right edge</td><td>Visible</td><td>None</td></tr><tr><td>Locale RTL</td><td>flex reverses; bars maintain order</td><td>Usable</td><td>None</td></tr><tr><td>Touch device</td><td>TouchSensor</td><td>Drag works</td><td>None</td></tr><tr><td>Browser zoom &gt; 200%</td><td>reflow</td><td>Usable</td><td>None</td></tr><tr><td>Date in past clamp at cycle.starts_at</td><td>render</td><td>Bar starts at edge</td><td>None</td></tr><tr><td>WS disconnect</td><td>offline banner</td><td>Drags queued</td><td>Reconnect drains</td></tr><tr><td>Mid-drag tab close</td><td>server state untouched</td><td>Local UI snaps back on remount</td><td>None</td></tr><tr><td>Milestone target_date null</td><td>skip render</td><td>None</td><td>None</td></tr><tr><td>Cycle duration &gt; 1 year</td><td>day-zoom unusable</td><td>UI defaults to week-zoom</td><td>None</td></tr><tr><td>Issue ends_at &lt; starts_at (data bug)</td><td>clamp + log warning</td><td>Single-day bar</td><td>Operator fixes data</td></tr><tr><td>Overlap &gt; 10 concurrent bars</td><td>badge shows count; \"10+\"</td><td>Operator clicks to expand</td><td>None</td></tr><tr><td>URL window &gt; 2 years</td><td>warn at load</td><td>renders but slow</td><td>Operator narrows</td></tr><tr><td>Snap-to-week with operator wanting day precision</td><td>modal entry available</td><td>None</td><td>Operator uses modal</td></tr><tr><td>Per-assignee filter with no issues</td><td>empty timeline + helpful copy</td><td>None</td><td>None</td></tr><tr><td>Dependency arrow with cycle (A→B→A)</td><td>arrow rendered with dashed style</td><td>visible cycle warning</td><td>Operator fixes</td></tr><tr><td>Dependency arrow crossing many swimlanes</td><td>rendered with curve</td><td>None</td><td>None</td></tr><tr><td>Show non-active toggle with 100+ members</td><td>virtualised</td><td>None</td><td>None</td></tr><tr><td>Weekend bg color clashes with bar color</td><td>accessibility check</td><td>None</td><td>Operator changes theme</td></tr><tr><td>Swimlane reorder race with WS update</td><td>local order preserved</td><td>None</td><td>None</td></tr><tr><td>Cycle goal text &gt; 500 chars</td><td>truncated with tooltip</td><td>None</td><td>None</td></tr><tr><td>Days remaining negative (cycle in past)</td><td>banner shows \"ended N days ago\"</td><td>None</td><td>None</td></tr><tr><td>RTL locale</td><td>timeline reverses; bars maintain order</td><td>None</td><td>None</td></tr><tr><td>Dependency arrow with non-rendered issue (collapsed lane)</td><td>arrow goes to lane edge</td><td>partial visualisation</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>Day-pixel constant <code>DAY_PX</code> configured per zoom: day=40px, week=80px (per 7 days), month=200px.</li><li><code>IntersectionObserver</code> watches swimlane wrappers; off-screen → suspend rendering inner bars.</li><li>IssueBar uses <code>position: absolute</code> within Swimlane (<code>position: relative</code>); left/width derived from dates.</li><li>Today indicator is a thin vertical div with <code>animation: pulse 2s infinite</code>.</li><li>LWW PATCH endpoint reused from TASK-PROJ-003 §3 (same as Kanban scalar updates).</li><li>Milestone markers render absolutely positioned by date; hover via :hover tooltip.</li><li>Awareness presence (other users editing same issue dates) shown via colored outline on bar — slice 4+; deferred from MVP.</li><li>Overlap stacking uses sub-row layout within the swimlane; max 3 rows visible, then \"+N\" badge.</li><li>URL window override (<code>?from=&amp;to=</code>) takes precedence over cycle bounds; cycle bounds are the default.</li><li>Snap-to-week/month uses Math.round on day delta; precision dates entered via Brief Modal (TASK-PROJ-017).</li><li>Per-assignee filter renders single full-width swimlane; bar bg color changes to indicate filtered view.</li><li>Dependency arrow rendering uses SVG <code>&lt;line&gt;</code> elements; computed positions from bar bounding rects.</li><li>Show non-active toggle persists in tenant settings; default off (less clutter).</li><li>Weekend background uses CSS <code>:nth-child</code> of day-grid; static color from TASK-PROJ-018 tokens.</li><li>Swimlane reorder uses drag-handle + Shift+Up/Down kbd; persisted per-user in <code>cyberos_proj_timeline_swimlane_order</code>.</li><li>Cycle banner is sticky-top during scroll; days-remaining computed live; pulses red when ≤ 2 days.</li><li>Operators dragging bars near cycle edges trigger automatic horizontal scroll; bounded at cycle window unless URL override.</li><li>We considered animating bar transitions on WS update (smooth shift) but kept instant for clarity; flash animation slice 4+.</li><li>Dependency arrows have a max length; cross-swimlane arrows render with arched paths to avoid overlap chaos.</li><li>Per-assignee filter shows the assignee's avatar prominently + their cumulative hours from time entries (TASK-TIME-005).</li><li>The 1-day minimum bar applies even during keyboard resize (Shift+Left past 1 day → no-op + flash).</li><li>Cycle goal banner text is editable inline by tenant admin; emits memory <code>proj.cycle_goal_updated</code> audit.</li><li>We don't show milestones in the swimlane area (only on date axis); they're date-based, not assignee-based.</li><li>The today-indicator pulse animation is reduced to subtle if <code>prefers-reduced-motion</code> is set.</li><li>Day-zoom &gt; 90 days OR week-zoom &gt; 2 years triggers a UX warning suggesting different zoom.</li><li>IssueBar tooltip on hover shows full issue details (assignee, estimate, blocker count, dependencies).</li><li>Overlap badge color reflects severity (3 overlaps = warning yellow; 5+ = red).</li><li>Drag preview ghost (showing target dates) appears during drag; replaces actual bar on release.</li><li>Cycle banner shows decision_session count + completed-issues count as live stats.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-015.</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Client code lives under apps/web/src (there is no web/proj-client/).</p>";
