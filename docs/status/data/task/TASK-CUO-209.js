window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-209"]="<h1>TASK-CUO-209: Full-SDP vendoring by default</h1>\n<h2>§1 - Description</h2>\n<p>A repo that installs CyberOS should be able to run the whole software development process from the same payload - author an SOW or PRD upstream of /create-tasks, and produce deployment checklists, release notes, runbooks, and retrospectives downstream of /ship-tasks - without anyone hand-copying skills out of the monorepo.</p>\n<p>Normative clauses:</p>\n<ol><li>The vendored skill set in <code>build.sh</code> MUST expand from the current 20 to the full SDP catalog: the existing 10 pairs, plus <code>debugging-cycle</code> (TASK-SKILL-116) and <code>architectural-spike</code> (TASK-SKILL-117) pairs, plus these pairs from <code>modules/skill/</code>: <code>statement-of-work</code>, <code>product-requirements-document</code>, <code>software-requirements-specification</code>, <code>software-design-document</code>, <code>threat-model</code>, <code>test-strategy</code>, <code>deployment-checklist</code>, <code>release-notes</code>, <code>runbook</code>, <code>retrospective</code>, <code>postmortem</code>, <code>decommissioning</code> - and the four NFR skills (<code>nfr-certification-author</code>, <code>nfr-evaluator</code>, <code>nfr-test-runner</code>, <code>nfr-regression-handler</code>). Author/audit twins ship together per the TASK-SKILL-116 pair rule; the NFR four ride the chain-allowlist mechanism as intentionally unpaired entries.</li><li>The vendored-set definition in <code>build.sh</code> MUST become a readable list (one name per line in a heredoc or adjacent manifest block with a per-line stage comment), replacing the single hardcoded string - the list is the reviewable contract for what ships.</li><li><code>manifest.yaml</code>'s <code>author_audit_skills</code> count MUST be computed from the built payload at build time, never hardcoded; the same computed number MUST drive the <code>profile: full</code> determination.</li><li>The payload GUIDE (<code>dist</code> GUIDE.md source) MUST gain a lifecycle map: one row per SDP stage 1..14 -&gt; skill pair -&gt; invoked by (<code>/create-tasks</code>, <code>/ship-tasks</code>, or standalone-on-request), so operators see which stages the two commands automate and which they invoke ad hoc.</li><li>Both checks from the sibling tasks MUST pass over the expanded set: chain coverage (TASK-SKILL-116) and pair parity where the pair is at full contract (TASK-SKILL-118) - thin-but-shipped upstream pairs are permitted at their current completeness (parity applies per-pair as they are deepened; the checker's scope list says which pairs are held to full parity).</li><li>Build output MUST report payload size (bytes of the payload dir and of cyberos.plugin) on every build; the plugin zip MUST stay under 2 MB with the expanded set (current: ~322 KB), asserted in the build.</li><li>Reduced-profile behavior MUST be unchanged: a payload built without skill bodies still degrades to the doc-driven floor; <code>/init</code> behavior in target repos is unchanged apart from the larger <code>cuo/skills/</code> tree.</li><li>The two workflow docs MUST NOT change in this task - upstream/downstream skills vendor as standalone-invocable; wiring them into new workflow steps is future work by separate task. (Amended post-ship 2026-07-12: the guard is a point-in-time scope clause proven by this task's commits in git history; the suite's t08 now asserts the durable form - both workflow docs vendored intact in the payload - so later tasks may legitimately evolve the docs. Surfaced by TASK-CUO-206.)</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Vendor-by-default was the operator's explicit call: the plugin's value across many repos is the whole process, and an opt-in flag would leave most installs at stages 5-10 forever. Expanding the SET while freezing the WORKFLOWS (#8) keeps risk low - nothing changes for existing runs; new capability arrives as invocable skills. The readable list + computed counts turn build.sh's weakest spot (a drifting hardcoded string, root cause of the debugging-cycle gap) into reviewed data.</p>\n<h2>§3 - Contract</h2>\n<p><code>build.sh</code> vendored-set block (shape):</p>\n<pre><code class=\"language-bash\">VENDORED_SKILLS=\"\nstatement-of-work-author            # SDP 1\nstatement-of-work-audit             # SDP 1\nproduct-requirements-document-author # SDP 2\n...\nTask-author              # SDP 5\n...\ndecommissioning-audit               # SDP 14\nnfr-certification-author            # SDP 4 (allowlisted unpaired)\n\"</code></pre>\n<p>Build report line: <code>cyberos: done. profile=full skills=&lt;computed&gt; payload=&lt;bytes&gt; plugin_zip=&lt;bytes&gt;</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>All stages ship</strong> (§1 #1) - after <code>build.sh</code>, every pair named in #1 exists (SKILL.md present) under both <code>cuo/skills/</code> and <code>plugin/skills/</code>; a stage-&gt;dir spot matrix in the test enumerates all 14 stages.</li><li><strong>Set is data, not a string</strong> (§1 #2) - the vendored set reads as the one-per-line block with stage comments; the debugging-cycle regression (delete a line) is caught by the TASK-SKILL-116 check at build time.</li><li><strong>Counts computed</strong> (§1 #3) - <code>manifest.yaml</code> skill count equals <code>ls</code>-derived reality for two differently-sized builds (full and a fixture-trimmed one); no literal count remains in the heredoc.</li><li><strong>Lifecycle map present and total</strong> (§1 #4) - GUIDE.md's map has exactly 14 stage rows, each naming its pair and its invoker; no stage row says TBD.</li><li><strong>Sibling checks green over the expanded set</strong> (§1 #5) - chain-coverage exits 0 (with the NFR/gate allowlist entries) and pair-parity exits 0 over its scoped list.</li><li><strong>Size budget</strong> (§1 #6) - the build prints both sizes and fails if the plugin zip exceeds 2 MB; current expanded build passes.</li><li><strong>Reduced floor intact</strong> (§1 #7) - a skill-less fixture build still yields <code>profile: reduced</code> and a working doc-driven payload.</li><li><strong>Workflows untouched</strong> (§1 #8) - ship-tasks.md and create-tasks.md are diff-clean in this task's commits (git history); the regression suite asserts the durable form (docs vendored intact in the payload) post-amendment.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/install/tests/test_full_sdp_payload.sh\nt01_stage_matrix_ships()         # AC 1\nt02_set_is_reviewable_data()     # AC 2\nt03_counts_computed()            # AC 3\nt04_lifecycle_map_total()        # AC 4\nt05_sibling_checks_green()       # AC 5\nt06_size_budget()                # AC 6\nt07_reduced_floor_intact()       # AC 7\nt08_workflows_diff_clean()       # AC 8</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>build.sh: replace the set string with the commented block; compute counts via <code>find ... -name SKILL.md | wc -l</code>; size report + budget check; GUIDE source gains the 14-row table (stage, pair, invoker, artefact name).</p>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-SKILL-116 (chain/pair checker + allowlist mechanics; also the single writer earlier on build.sh's set - land 116 first, then this expands the same block) and TASK-SKILL-117 (spike pair must exist to vendor). TASK-SKILL-118 deepens pairs independently; its parity scope list grows as pairs reach full contract.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>cyberos: done. profile=full skills=52 payload=2731008 plugin_zip=897412</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Which upstream artefacts /create-tasks should CONSUME automatically (e.g. detect an SRS and chain from it - it already accepts one as input) versus leave standalone stays future workflow work per #8.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Plugin bloat degrades agent skill-selection (too many similar descriptions) - descriptions already pass TASK-SKILL-111 trigger-enrichment conventions; the 2 MB budget plus the plugin's namespaced skill names keep the surface navigable. If selection quality regresses, the fallback is documented: trim the PLUGIN copy while keeping the payload copy (one-line build.sh split), by follow-up task.</li><li>A vendored upstream pair is thin and an operator expects full-contract behavior - the GUIDE map marks contract level per pair (full/thin) so expectations are set at install time.</li><li>modules/skill renames a vendored dir - chain-coverage/pair checks fail the build at the renaming commit (TASK-IMP-068 path filter covers modules/skill/**).</li><li>Size creep over releases - the printed sizes land in every build log and the 2 MB assert stops the slow boil.</li><li>NFR four mistaken for author/audit pairs - the allowlist entry carries the reason string; pair check reads it and skips them by name, not by pattern.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the vendored block sorted by SDP stage, not alphabetically - the review diff should read as the lifecycle. GUIDE map's \"invoker\" column values are exactly three strings (the two commands or <code>standalone</code>) so docs stay greppable.</p>\n<p><em>End of TASK-CUO-209.</em></p>";
