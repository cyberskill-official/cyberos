window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-134"]="<h1>TASK-IMP-134: End-to-end regression for the cs/memory/cuo rename</h1>\n<h2>Summary</h2>\n<p>Prove the combined result of TASK-IMP-130/131/132 — the renamed <code>cs</code> bin with <code>memory</code> and <code>cuo</code> both added to the same dispatch table — works as an integrated whole via an offline, in-repo test, and separately checklist the genuinely network/Homebrew-dependent \"clean machine\" portion the plan describes, which cannot run in this repo's CI.</p>\n<h2>Problem</h2>\n<p>TASK-IMP-130, 131, and 132 each have their own acceptance criteria proving their own change in isolation. None of them proves the three changes compose correctly once all land — in particular, TASK-IMP-131 and TASK-IMP-132 both edit the same <code>cli.mjs</code> dispatch table object and were flagged in their own specs as having a sibling merge-conflict risk (each depends only on TASK-IMP-130, not on each other, so nothing stops them being authored/implemented in parallel). An e2e test that builds the payload fresh after all four tasks land and exercises <code>cs install</code>, <code>cs memory</code>, and <code>cs cuo</code> together in one process is the only check that would catch a dispatch-table integration break that per-task unit tests would miss.</p>\n<p>Separately, the plan's own item 7 describes a \"clean machine\" regression involving a real Homebrew install and a real npm release — this repo's only existing e2e precedent, <code>test_e2e_skeleton.sh</code>, is explicit that it runs \"with NO model, NO network, NO credentials.\" The plan's literal ask does not fit that convention, and TASK-IMP-133's own audit (ISS-005) already found that no task in this batch actually covers cutting the release this regression would need. Presenting a network-dependent AC as if this repo's CI could run it would be the same class of fabricated-citation defect TASK-IMP-133's audit found and fixed.</p>\n<h2>Proposed Solution</h2>\n<p>Two halves, not one:</p>\n<ol><li><strong>Offline integration test (this task's own automatable deliverable).</strong> A new test, <code>tools/install/tests/test_cs_rename_e2e.sh</code>, following <code>test_e2e_skeleton.sh</code>'s established shape (build from source into a scratch dir, no network) that builds a scratch payload after TASK-IMP-130/131/132's changes are present, then in one continuous run: confirms the generated <code>package.json</code>'s <code>bin</code> is <code>cs</code> (not <code>cyberos</code>); confirms <code>cs -h</code>'s usage text lists <code>memory</code> and <code>cuo</code> alongside <code>install</code>/<code>uninstall</code>/<code>version</code>/<code>status</code>/<code>create</code>/<code>gates</code>/<code>mcp</code>/<code>help</code>; confirms <code>cs memory &lt;cmd&gt;</code> and <code>cs cuo &lt;name&gt;</code> both dispatch correctly using the same stub techniques TASK-IMP-131/132's own tests established, run back-to-back against the SAME built payload (proving the combined <code>SCRIPTS</code>-table object is well-formed, not just each entry individually reachable).</li><li><strong>Manual release-time checklist (not an automated AC).</strong> A short checklist, recorded in this task's own body rather than as a testable AC, for whoever performs the actual release: install the released <code>cs</code> via the updated Homebrew formula (TASK-IMP-133) on a real machine, confirm <code>cyberos</code> (bare) resolves only to a locally pip-installed <code>cyberos-memory</code> if present (no competing public-CLI claim on that name), and confirm <code>cs memory &lt;cmd&gt;</code> reaches the same store. This half is explicitly NOT gated by this task's own <code>ready_to_implement</code> → <code>done</code> transition, since it cannot happen until a real release exists (the same operational gap TASK-IMP-133's audit named).</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li>Author a single AC requiring a real Homebrew install on a clean VM, matching the plan's literal wording. Rejected: this repo's CI has no such capability and no prior task in this codebase (including <code>test_e2e_skeleton.sh</code>, the closest precedent) does this — authoring an AC nothing can mechanically satisfy would be the same fabricated-test-authority problem TASK-IMP-133's audit caught and fixed, repeated here.</li><li>Skip the offline portion entirely and treat this task as pure documentation/checklist. Rejected: the dispatch-table integration risk (TASK-IMP-131/132's sibling merge-conflict flag) is real and IS testable offline today; dropping it would leave the one distinctly valuable, achievable check this task can add unbuilt.</li><li>Fold this task's offline portion into TASK-IMP-131 or 132 individually instead of a separate task. Rejected: neither of those tasks' own scope is \"prove the combination of both plus 130 works together\" — that is inherently a task that can only exist once all three have landed, matching the plan's own dependency ordering (item 7 listed last, after items 1-6).</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: <code>bash tools/install/tests/test_cs_rename_e2e.sh</code> passes in CI, offline, after TASK-IMP-130/131/132 land. Baseline today: no such test exists, and none of the three per-task test suites runs all three verbs against one shared built payload in sequence.</li><li>Guardrail: the manual release-time checklist (this task's §3 edge cases / body text) is followed at least once before the release containing TASK-IMP-130's own CHANGELOG rename entry (clause 1.6 of that task) is tagged and published - a concrete, checkable trigger event rather than a subjective \"announced as complete\" - tracked as a release-process step, not as this task's own <code>done</code> criterion.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>tools/install/tests/test_cs_rename_e2e.sh</code> (new), and the manual release-time checklist recorded in this spec's body.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any code change to <code>cli.mjs</code>, <code>build.sh</code>, or the memory/cuo dispatch logic itself — this task only tests what TASK-IMP-130/131/132 already implement.</li><li>Actually performing the manual release-time checklist — that is a release-process action, not a deliverable this task's own status transition depends on.</li><li>Cutting the npm release or merging the Homebrew tap PR — those are TASK-IMP-130's and TASK-IMP-133's concerns respectively (and, per TASK-IMP-133's audit ISS-005, not fully owned by any task in this batch).</li></ul>\n<h2>Dependencies</h2>\n<p>Depends on TASK-IMP-130, 131, and 132 — the automated offline portion (clauses 1.1-1.5) only needs their CODE to exist, not TASK-IMP-133's completion. TASK-IMP-133 is deliberately NOT in <code>depends_on</code>: its own audit (ISS-005) established that it cannot reach <code>done</code> until an npm release is actually cut and published, an externally-gated, indefinite-duration event. Making this task's <code>depends_on</code> include TASK-IMP-133 would block this task's own immediately-implementable, fully-automated portion behind that external gate for no reason — only the MANUAL checklist in Edge Cases (clause 1.6, not gated by this task's status transitions) actually needs TASK-IMP-133 to have shipped. Nothing in this batch depends on this task (<code>blocks: []</code>).</p>\n<p><strong>Relationship to TASK-IMP-107.</strong> That task built <code>test_e2e_skeleton.sh</code>, the offline e2e precedent this task's own test file follows structurally (build-from-source into scratch, no network, no credentials) — including its file-level pattern of building ONE scratch payload before any check function runs, which this task's own clause 1.1 depends on directly (see §1.1's revised wording).</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill inside Cowork.</li><li><strong>Scope:</strong> the offline-vs-network split was authored after reading <code>test_e2e_skeleton.sh</code>'s actual header comment and cross-referencing TASK-IMP-133's audit finding (ISS-005) authored earlier in this same batch, not asserted independently.</li><li><strong>Human review:</strong> task decomposition approved at the 2026-07-22 PLAN gate. The offline/manual split is an authoring-time call, flagged for the operator in the batch report.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>tools/install/tests/test_cs_rename_e2e.sh</code> MUST build exactly one scratch payload from source, at file level before any check function is defined or called — matching <code>test_sync_host_plugins.sh</code>'s own established pattern (\"building scratch payload...\" once, consumed by every <code>tNN_*</code> function below it) — and every check below MUST consume that SAME built payload; no check function may perform its own independent build.</li><li>1.2 The test MUST confirm the built payload's <code>package.json</code> declares <code>bin: {\"cs\": ...}</code> and does NOT declare a <code>cyberos</code> bin key.</li><li>1.3 The test MUST confirm <code>cs -h</code>'s usage output lists all of <code>install</code>, <code>uninstall</code>, <code>version</code>, <code>status</code>, <code>create</code>, <code>gates</code>, <code>mcp</code>, <code>help</code>, <code>memory</code>, and <code>cuo</code>.</li><li>1.4 The test MUST confirm, against the ONE built payload, that both <code>cs memory &lt;cmd&gt;</code> (via a stub <code>python3</code>) and <code>cs cuo &lt;name&gt;</code> (via the redirect-stub check) behave correctly when invoked back-to-back in the same test run.</li><li>1.5 The test MUST run offline — no network access, no real npm registry query, no real Homebrew invocation — matching <code>test_e2e_skeleton.sh</code>'s own established convention.</li><li>1.6 This task's spec MUST record the manual release-time checklist as body text, explicitly marked as not gating this task's own <code>ready_to_implement</code> → <code>done</code> transition.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC 1 (traces_to: #1.1) - a grep of <code>tools/install/tests/test_cs_rename_e2e.sh</code>'s own source for build invocations (<code>bash \"$BUILD\"</code> or equivalent) returns exactly <code>1</code>, and that invocation appears before the first <code>t0[0-9]_</code> function definition in the file - test: <code>grep -c 'bash \"\\$BUILD\"' tools/install/tests/test_cs_rename_e2e.sh</code> returns <code>1</code>, combined with a line-number comparison against the first <code>t0</code> function definition</li><li>[x] AC 2 (traces_to: #1.2) - <code>$TMP/payload/package.json</code>'s <code>bin</code> object has key <code>cs</code> and does not have key <code>cyberos</code> - test: <code>tools/install/tests/test_cs_rename_e2e.sh::t02_bin_is_cs_only</code></li><li>[x] AC 3 (traces_to: #1.3) - <code>node $TMP/payload/cli/bin/cli.mjs -h</code> output contains all ten command names listed in clause 1.3, matched with word-boundary-safe patterns (e.g. <code>grep -wo</code>) so that a substring coincidence (e.g. \"create\" inside a hypothetical unrelated \"created\") cannot produce a false pass - test: <code>tools/install/tests/test_cs_rename_e2e.sh::t03_usage_lists_all_ten_verbs</code></li><li>[x] AC 4 (traces_to: #1.4) - within the same test run, a stub-<code>python3</code> <code>cs memory doctor</code> call and a <code>cs cuo plan</code> call both produce their expected outputs, run in sequence against the one built payload from AC 1-3 - test: <code>tools/install/tests/test_cs_rename_e2e.sh::t04_memory_and_cuo_both_work_on_shared_build</code></li><li>[x] AC 5 (traces_to: #1.5) - the test file contains no invocation of <code>curl</code>, <code>npm view</code>, <code>npm install &lt;remote&gt;</code>, or <code>brew</code> anywhere in its body - test: <code>grep -Ec 'curl |npm view|npm install [^-]|brew ' tools/install/tests/test_cs_rename_e2e.sh</code> returns <code>0</code></li><li>[x] AC 6 (traces_to: #1.6) - a grep of THIS spec file for the literal heading text <code>Manual release-time checklist</code> returns at least <code>1</code> match - test: <code>grep -c 'Manual release-time checklist' docs/tasks/improvement/TASK-IMP-134-cs-rename-e2e-regression/spec.md</code> returns <code>&gt;=1</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>If TASK-IMP-131 or TASK-IMP-132 ships with a different exact dispatch-table shape than either task's own spec describes (e.g. a routing function instead of a flat object key), this test's assertions about <code>cli.mjs</code>'s internal structure (if any are added beyond black-box CLI invocation) would need updating - mitigated by keeping all assertions black-box (invoke the CLI, read stdout/exit code) rather than parsing <code>cli.mjs</code>'s source, so implementation-detail changes in HOW the dispatch is built don't break this test as long as the observable behaviour matches.</li><li><strong>Manual release-time checklist</strong> (not gated by this task's own acceptance criteria — see clause 1.6 and Scope): (1) confirm the npm release referenced by the updated Homebrew Formula (TASK-IMP-133) is live via <code>npm view @cyberskill/cyberos version</code>; (2) on a machine with <code>cyberos-memory</code> separately pip-installed, <code>brew install cyberos-cli</code> then confirm <code>cyberos</code> (bare) resolves to the pip-installed memory CLI and <code>cs</code> resolves to the Homebrew-installed public CLI, with neither shadowing the other; (3) confirm <code>cs memory doctor</code> on that same machine reaches the real BRAIN store and its output matches what <code>cyberos doctor</code> (the direct invocation) would show for the same store path.</li><li>A machine with NEITHER <code>cyberos</code> nor <code>cs</code> previously installed (the actual \"clean machine\" the plan's wording literally describes): the manual checklist's step 2 is vacuously about \"no collision\" since there is nothing pre-existing to collide with - the meaningful clean-machine claim is narrower than the plan's wording suggests, and is really about a machine that has BOTH the old internal tool and the new public one, which is the founder's own originally-reported scenario (plan §2).</li><li>Security-class: this task adds a new offline test file only; it introduces no new runtime code path and no new attack surface.</li></ul>";
