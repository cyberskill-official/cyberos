window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-132"]="<h1>TASK-IMP-132: Add a <code>cuo</code> verb to <code>cs</code> as a redirect stub</h1>\n<h2>Summary</h2>\n<p>Add <code>cuo</code> as a top-level verb in <code>cs</code> that recognises the four LLM-orchestrated workflow names (<code>plan</code>, <code>create-tasks</code>, <code>ship-tasks</code>, <code>improve</code>) and prints which slash command to run inside an agent session, rather than attempting to execute anything standalone.</p>\n<h2>Problem</h2>\n<p><code>docs/plans/PLAN-cli-module-namespacing-2026-07-22/plan.md</code> §2 identifies <code>modules/cuo</code> as backing four multi-step, LLM-orchestrated workflows — <code>/plan</code>, <code>/create-tasks</code>, <code>/ship-tasks</code>, <code>/improve</code> — each involving interview, drafting, self-audit, and HITL approval gates. These are not deterministic scripts <code>cli.mjs</code> can <code>spawnSync</code> and get a meaningful result from outside an agent session; running <code>cyberos-cuo</code>'s own Python entry point directly (as README.md:47 shows for local dev, <code>cyberos-cuo list-personas</code>) exposes internal persona/workflow machinery, not the guided, gated experience the slash commands provide. Today, <code>cs</code> (post TASK-IMP-130) has no <code>cuo</code> verb at all — a user who types <code>cs cuo plan</code> gets \"unknown command,\" with no signal that the capability exists under a different invocation.</p>\n<h2>Proposed Solution</h2>\n<p>Add a <code>cuo</code> entry to <code>cli.mjs</code>'s dispatch. <code>cs cuo &lt;name&gt;</code> where <code>&lt;name&gt;</code> is one of <code>plan</code>, <code>create-tasks</code>, <code>ship-tasks</code>, <code>improve</code> prints a short message naming the matching slash command (<code>/plan</code>, <code>/create-tasks</code>, <code>/ship-tasks</code>, <code>/improve</code> respectively) to run inside a Claude Code (or other agent) session, and exits <code>0</code> — it is documentation output, not an error. <code>cs cuo</code> with no name, or an unrecognised name, lists all four valid names. No subprocess is spawned; no attempt is made to run the workflow headlessly.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Attempt to drive the CUO workflow headlessly from the CLI (e.g. shelling out to <code>cyberos-cuo</code>'s Python entry point with a scripted agent loop). Rejected explicitly by the plan (§5 item 4: \"do not implement standalone execution in this task\") — building real headless/agentic execution is deferred as its own follow-up decision (plan §5, out of scope bullet), not something this task should quietly attempt.</li><li>Have <code>cs cuo &lt;name&gt;</code> shell out to <code>cyberos-cuo list-personas</code> or similar to at least show <em>something</em> real rather than a static message. Rejected: this would expose internal persona/workflow machinery not meant for direct end-user consumption, and risks implying standalone execution works when it does not.</li><li>Recognise install/uninstall/version/status/help under <code>cuo</code> as well, for a single unified namespace. Rejected: those five are deterministic shell scripts with their own existing flat <code>cs</code> verbs (<code>cli.mjs:19-26</code>); nesting them under <code>cuo</code> too would contradict plan §2's own framing of what CUO backs, and would make <code>cs cuo install</code> seem like a legitimate synonym for <code>cs install</code> when it is not.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: <code>cs cuo plan</code>, <code>cs cuo create-tasks</code>, <code>cs cuo ship-tasks</code>, and <code>cs cuo improve</code> each print the correct matching slash command name and exit <code>0</code>. Baseline today: <code>cuo</code> is not in <code>cli.mjs</code>'s <code>SCRIPTS</code> table; any <code>cyberos cuo ...</code> (or post-rename <code>cs cuo ...</code>) invocation falls through to \"unknown command.\"</li><li>Guardrail: <code>cs cuo</code> with no argument, and <code>cs cuo bogus-name</code>, both list all four valid names and exit <code>0</code> — neither is treated as an error, since the stub's entire job is orientation, not validation.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the <code>cuo</code> dispatch entry in <code>cli.mjs</code> recognising exactly <code>plan</code>, <code>create-tasks</code>, <code>ship-tasks</code>, <code>improve</code>; its no-argument/unrecognised-argument listing behaviour; and the <code>help.sh</code>/<code>docs/index.md</code> mentions of the new verb.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any standalone or headless execution of a CUO workflow — explicitly deferred by the plan.</li><li>Recognising <code>install</code>/<code>uninstall</code>/<code>version</code>/<code>status</code>/<code>help</code> under the <code>cuo</code> namespace — they already have their own flat <code>cs</code> verbs and are not CUO-backed work.</li><li>Changes to <code>modules/cuo</code>'s own Python implementation or persona system.</li><li>The <code>memory</code> verb — that is TASK-IMP-131.</li></ul>\n<h2>Dependencies</h2>\n<p>Depends on TASK-IMP-130 (adds a verb to the same <code>cli.mjs</code> dispatch table under the renamed bin). Blocks TASK-IMP-134's end-to-end regression.</p>\n<p><strong>Relationship to TASK-IMP-076.</strong> Same dispatch-table precedent as TASK-IMP-131 (<code>mcp</code>/<code>gates</code> established the <code>spawnSync</code>-based pattern this family of verbs follows); this task, notably, does NOT spawn any subprocess at all — it is the one verb in this batch that is pure static text, which is itself a deliberate scope decision (see Alternatives Considered), not an oversight.</p>\n<p><strong>Sibling-task coordination with TASK-IMP-131.</strong> Both tasks add a new entry to the same <code>cli.mjs</code> dispatch table and depend only on TASK-IMP-130; see TASK-IMP-131's Dependencies section for the shared merge-conflict/sequencing note — it applies symmetrically to this task and is not repeated in full here to avoid two specs disagreeing if one is edited later.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill inside Cowork.</li><li><strong>Scope:</strong> the four-name CUO-backed subset was cross-checked directly against <code>tools/install/plugin/commands/</code>'s actual file listing and <code>README.md:28</code>'s broader slash-command list, not assumed from the plan's prose alone — the plan's own §2 citation was the primary source, confirmed rather than merely repeated.</li><li><strong>Human review:</strong> task decomposition approved at the 2026-07-22 PLAN gate.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>cli.mjs</code>'s dispatch table MUST recognise <code>cuo</code> as a known top-level command.</li><li>1.2 <code>cs cuo plan</code> MUST print a message naming <code>/plan</code> as the slash command to run, and MUST exit <code>0</code>.</li><li>1.3 <code>cs cuo create-tasks</code>, <code>cs cuo ship-tasks</code>, and <code>cs cuo improve</code> MUST each print a message naming their respective matching slash command (<code>/create-tasks</code>, <code>/ship-tasks</code>, <code>/improve</code>) and MUST each exit <code>0</code>.</li><li>1.4 <code>cs cuo</code> with no argument MUST print a listing of all four valid names and MUST exit <code>0</code> (orientation, not an error — matching the bare top-level <code>cs</code> invocation's own exit-<code>0</code> convention at <code>cli.mjs:59</code>).</li><li>1.4a <code>cs cuo &lt;unrecognised-name&gt;</code> MUST print the same listing but MUST exit with code <code>2</code> (matching <code>cli.mjs</code>'s established convention for a usage mistake, <code>cli.mjs:87</code>) — distinct from the bare-invocation case, since a mistyped name is a usage error a caller may want to detect, while a bare invocation asking for orientation is not.</li><li>1.5 <code>cs cuo &lt;any argument&gt;</code> MUST NOT spawn any subprocess, MUST NOT invoke Python, and MUST NOT attempt to execute a CUO workflow, and MUST NOT probe whether <code>cyberos-cuo</code> is locally installed — unlike TASK-IMP-131's <code>memory</code> verb, which does detect and report local tool availability, the plan constrains <code>cuo</code> more tightly (\"do not implement standalone execution,\" no mechanism left open for later): this task's stub prints fixed text only, with no local-environment awareness at all. The two sibling verbs are deliberately asymmetric because the plan treats them differently, not by oversight.</li><li>1.6 <code>help.sh</code> and <code>docs/index.md</code> MUST document the <code>cuo</code> verb and MUST describe it, on the same line or the immediately adjacent line as the mention, as a redirect/orientation aid rather than standalone execution. <code>docs/index.md:27</code>'s \"the same eight commands\" sentence MUST be updated to the correct count once <code>cuo</code> (and, per TASK-IMP-131, <code>memory</code>) are added — whichever of TASK-IMP-131/132 lands second is responsible for the numeral, not just appending its own verb's name.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.2, #1.1) - <code>cs cuo plan</code> output contains the literal substring <code>/plan</code> and the process exits <code>0</code> - this alone proves <code>cuo</code> was recognised as a known command (clause 1.1), since an unrecognised top-level command falls through to the \"unknown command\" branch and could never reach this output - test: <code>tools/install/tests/test_cli_cuo_verb.sh::t01_plan_redirect_and_recognition</code></li><li>[ ] AC 2 (traces_to: #1.3) - each of <code>cs cuo create-tasks</code>, <code>cs cuo ship-tasks</code>, <code>cs cuo improve</code> prints its matching slash-command substring and exits <code>0</code> - test: <code>tools/install/tests/test_cli_cuo_verb.sh::t02_other_three_redirects</code></li><li>[ ] AC 3 (traces_to: #1.4) - <code>cs cuo</code> (no args) prints all four valid names and exits <code>0</code> - test: <code>tools/install/tests/test_cli_cuo_verb.sh::t03_bare_invocation_lists_and_exits_0</code></li><li>[ ] AC 4 (traces_to: #1.4a) - <code>cs cuo nonexistent-workflow</code> prints all four valid names and exits with code exactly <code>2</code> - test: <code>tools/install/tests/test_cli_cuo_verb.sh::t04_unrecognised_name_lists_and_exits_2</code></li><li>[ ] AC 5 (traces_to: #1.5) - with tripwire <code>python3</code> and <code>bash</code> stand-ins on <code>$PATH</code> that each write a marker file if invoked, running <code>cs cuo plan</code>, <code>cs cuo create-tasks</code>, <code>cs cuo ship-tasks</code>, and <code>cs cuo improve</code> in sequence leaves both marker files absent afterward - test: <code>tools/install/tests/test_cli_cuo_verb.sh::t05_no_subprocess_spawned</code> (mirrors the tripwire-binary style already used in TASK-IMP-131's test suite, matching this repo's bash-test-harness convention rather than a JS-level module spy)</li><li>[ ] AC 6 (traces_to: #1.6) - <code>help.sh</code> output and <code>docs/index.md</code> each mention <code>cuo</code> with a redirect-describing word on the same or adjacent line, and <code>docs/index.md</code>'s command-count sentence reads a number matching the actual verb count in <code>cli.mjs</code>'s <code>SCRIPTS</code> table at the time of the check (not hardcoded to eight) - test: <code>tools/install/tests/test_cli_cuo_verb.sh::t06_docs_describe_as_redirect_and_count_correct</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Argument casing or a leading slash typed by habit (<code>cs cuo /plan</code> instead of <code>cs cuo plan</code>) - out of scope for this task to normalise; the stub matches the four bare names exactly and falls into the \"unrecognised\" listing branch (AC 4) for anything else, which itself still orients the user correctly.</li><li><code>cs cuo</code> invoked from a plain terminal with no agent session active at all: the printed message still names the slash command correctly - the stub does not attempt to detect whether an agent session exists, since doing so is unnecessary complexity for a text-printing command with no side effects either way.</li><li>A future fifth CUO-backed workflow is added to <code>modules/cuo</code> without a corresponding update to this stub's four-name list: the stub silently under-lists it. Not a defect this task introduces, but named here as a maintenance risk - whichever task adds a fifth CUO workflow should update this list, and TASK-IMP-134's regression does not itself guard against this drift (it only proves the current four resolve correctly).</li><li>Security-class: this verb's entire behaviour is printing a fixed string keyed on <code>argv[1]</code> - it reads no file, spawns no process, and accepts no input that reaches a shell or filesystem call, so it carries no new attack surface beyond argument parsing itself.</li></ul>";
