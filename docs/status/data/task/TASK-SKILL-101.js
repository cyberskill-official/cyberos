window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-101"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The SKILL host <strong>MUST</strong> emit memory audit rows before AND after every skill invocation. Each invocation:</p>\n<ol><li><strong>MUST</strong> emit <code>skill.invoked_started</code> BEFORE dispatch with payload: <code>skill_id</code>, <code>version</code>, <code>caller_persona</code>, <code>args_hash</code> (SHA-256 of canonical JSON), <code>tenant_id</code>, <code>trace_id</code>, <code>request_id</code>.</li><li><strong>MUST</strong> emit <code>skill.invoked_completed</code> AFTER dispatch with payload: <code>skill_id</code>, <code>outcome</code> (<code>success | error | panic | timeout</code>), <code>duration_ms</code>, <code>result_hash</code> (SHA-256 of canonical-JSON output, or empty on error/panic), <code>error_message</code> (if applicable), <code>trace_id</code>, <code>request_id</code>.</li><li><strong>MUST NOT</strong> emit args raw — only <code>args_hash</code>. Args may contain tenant-business-sensitive data (queries, document content); chain must not become a parallel data store. Same for results — only hash.</li><li><strong>MUST</strong> propagate <code>trace_id</code> from caller's OTel context (TASK-AI-022). Both audit rows carry the same <code>trace_id</code>; investigators can join LLM trace + skill trace + audit row in memory.</li><li><strong>MUST</strong> use TASK-AI-003 <code>memory_writer::emit()</code> for both rows. Failure to emit <code>_started</code> → skill NOT dispatched (caller gets 503). Failure to emit <code>_completed</code> → log sev-1 + DO NOT reverse skill effect (skills may have side effects; reversing isn't generally possible).</li><li><strong>MUST</strong> complete audit emission within 50ms p95 (matches TASK-AI-003 budget). Above 50ms, OBS sev-3 alarm.</li><li><strong>MUST</strong> wrap skill dispatch in <code>catch_unwind</code> so panic is captured + emits <code>_completed</code> row with <code>outcome: panic</code>. Without panic-catch, <code>_started</code> rows accumulate without matching <code>_completed</code> (audit chain inconsistency).</li><li><strong>MUST</strong> support concurrent invocations — different skill calls use independent memory write paths; no serialised lock around emit.</li><li><strong>MUST</strong> include <code>tenant_id</code> in both rows for tenant-scoped audit queries.</li><li><strong>MUST</strong> record duration even on panic/error (clock measurement around <code>catch_unwind</code>); <code>duration_ms</code> reflects actual runtime.</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>skill_invoked_total{skill_id, outcome}</code> (counter).</li><li><code>skill_invoked_duration_ms{skill_id}</code> (histogram).</li><li><code>skill_audit_emit_failures_total{stage}</code> (counter; stage ∈ start | completed; sev-1).</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why before AND after rows (DEC-200)?</strong> Single completed-only row hides duration of in-flight invocations. Single started-only row leaves orphans on crashes. Both rows let auditors answer \"what skills are currently running?\" + \"what skills completed in the period?\" + \"what skills crashed?\".</p>\n<p><strong>Why args_hash not raw (DEC-200)?</strong> Args may contain tenant-business semantics (queries about specific products, document content). Storing raw in audit chain creates a parallel data store. Hash preserves uniqueness for forensic correlation without leaking content.</p>\n<p><strong>Why panic emits completed row (DEC-201)?</strong> Without it, <code>_started</code> rows accumulate without matching <code>_completed</code> — audit chain becomes inconsistent (\"which skills are running? unclear, some died.\"). The panic-completed row IS the truth: \"this skill crashed; recorded.\"</p>\n<p><strong>Why trace_id propagation (DEC-202)?</strong> Skill calls happen as part of LLM workflows. The same trace_id ties the LLM call (Tempo) + LangSmith trace + skill audit row + downstream effects. Without propagation, the chain breaks at the skill boundary.</p>\n<p><strong>Why _started fails block dispatch (§1 #5)?</strong> Without auditable record, skill side effects are invisible. Refusing dispatch when audit can't record preserves auditability invariant. The trade-off is unavailability during memory outages — acceptable because audit chain integrity is non-negotiable.</p>\n<p><strong>Why _completed failure doesn't reverse (§1 #5)?</strong> Skills may have side effects (sent email, updated row). Reversing isn't generally possible. Logging sev-1 + leaving the side effect = honest about the gap. Operator investigates via Layer 1 chain.</p>\n<p><strong>Why concurrent invocations independent (§1 #8)?</strong> Serialised lock around memory_writer would bottleneck high-throughput skill invocation. Independent emit paths allow parallel skills to audit independently.</p>\n<p><strong>Why tenant_id in both rows (§1 #9)?</strong> Audit queries are typically tenant-scoped (\"what did tenant X's skills do last week?\"). Without tenant_id, the query requires joining against subjects/personas — slow + complex.</p>\n<p><strong>Why duration even on panic (§1 #10)?</strong> Forensic question: \"did the skill panic immediately or after 30s?\" Duration tells the story. Captured around <code>catch_unwind</code> to include the panic-handling time.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/skill-host/src/memory_bridge.rs\nuse std::time::Instant;\nuse std::panic::AssertUnwindSafe;\nuse futures::future::FutureExt;\n\npub async fn invoke_with_audit(\n    skill_id: &amp;str, version: &amp;str, args: serde_json::Value,\n    ctx: &amp;InvocationContext,\n) -&gt; Result&lt;SkillOutput, SkillError&gt; {\n    let args_hash = sha256_canonical(&amp;args)?;\n    let request_id = ulid::Ulid::new().to_string();\n    let start_row = audit_canonical::skill_invoked_started(\n        skill_id, version, &amp;ctx.caller_persona, &amp;args_hash,\n        ctx.tenant_id, &amp;ctx.trace_id, &amp;request_id,\n    );\n    memory_writer::emit(start_row).await\n        .map_err(|e| SkillError::AuditEmitFailed { stage: \"start\", reason: e.to_string() })?;\n\n    let t0 = Instant::now();\n    let dispatch_result = std::panic::catch_unwind(AssertUnwindSafe(|| {\n        tokio::task::block_in_place(|| {\n            tokio::runtime::Handle::current().block_on(skill_supervisor::dispatch(skill_id, args))\n        })\n    }));\n    let duration_ms = t0.elapsed().as_millis() as u32;\n\n    let (outcome, result_hash, error_msg, ret) = match dispatch_result {\n        Ok(Ok(output)) =&gt; {\n            let h = sha256_canonical(&amp;output.body)?;\n            (\"success\", h, None, Ok(output))\n        }\n        Ok(Err(e)) =&gt; (\"error\", String::new(), Some(e.to_string()), Err(e)),\n        Err(panic_payload) =&gt; {\n            let msg = panic_to_string(&amp;panic_payload);\n            (\"panic\", String::new(), Some(msg.clone()),\n             Err(SkillError::Panicked { message: msg }))\n        }\n    };\n\n    let completed_row = audit_canonical::skill_invoked_completed(\n        skill_id, outcome, duration_ms, &amp;result_hash, error_msg.as_deref(),\n        &amp;ctx.trace_id, &amp;request_id,\n    );\n    if let Err(e) = memory_writer::emit(completed_row).await {\n        tracing::error!(error = %e, request_id, skill_id, \"audit_emit_completed_failed; sev-1\");\n        metrics::audit_emit_failure(\"completed\");\n    }\n\n    metrics::skill_invoked(skill_id, outcome, duration_ms);\n    ret\n}</code></pre>\n<pre><code class=\"language-rust\">// services/skill-host/src/invocation_context.rs\npub struct InvocationContext {\n    pub caller_persona: String,        // e.g., \"cuo-cpo@0.4.1\"\n    pub tenant_id: Uuid,\n    pub trace_id: String,              // hex 32-char from TASK-AI-022\n}\n\nimpl InvocationContext {\n    pub fn from_otel_context(persona: &amp;str, tenant: Uuid) -&gt; Self {\n        let trace_id = opentelemetry::Context::current().span().span_context().trace_id();\n        Self {\n            caller_persona: persona.to_string(),\n            tenant_id: tenant,\n            trace_id: format!(\"{trace_id:032x}\"),\n        }\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/skill-host/src/audit_canonical.rs\npub fn skill_invoked_started(\n    skill_id: &amp;str, version: &amp;str, caller_persona: &amp;str, args_hash: &amp;str,\n    tenant_id: Uuid, trace_id: &amp;str, request_id: &amp;str,\n) -&gt; AuditRow {\n    AuditRow {\n        kind: \"skill.invoked_started\".into(),\n        payload: serde_json::json!({\n            \"skill_id\": skill_id, \"version\": version,\n            \"caller_persona\": caller_persona,\n            \"args_hash\": args_hash,\n            \"tenant_id\": tenant_id,\n            \"trace_id\": trace_id,\n            \"request_id\": request_id,\n        }),\n        ..Default::default()\n    }\n}\n\npub fn skill_invoked_completed(\n    skill_id: &amp;str, outcome: &amp;str, duration_ms: u32,\n    result_hash: &amp;str, error_message: Option&lt;&amp;str&gt;,\n    trace_id: &amp;str, request_id: &amp;str,\n) -&gt; AuditRow {\n    AuditRow {\n        kind: \"skill.invoked_completed\".into(),\n        payload: serde_json::json!({\n            \"skill_id\": skill_id, \"outcome\": outcome,\n            \"duration_ms\": duration_ms,\n            \"result_hash\": result_hash,\n            \"error_message\": error_message,\n            \"trace_id\": trace_id,\n            \"request_id\": request_id,\n        }),\n        ..Default::default()\n    }\n}\n\npub fn sha256_canonical(value: &amp;serde_json::Value) -&gt; Result&lt;String, SkillError&gt; {\n    let bytes = serde_jcs::to_vec(value).map_err(|e| SkillError::Canonicalisation(e.to_string()))?;\n    Ok(hex::encode(sha256(&amp;bytes)))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>Skill invocation emits 2 memory rows (started + completed).</li><li>args_hash matches canonical SHA-256 of args.</li><li>Failure to emit started → skill NOT dispatched + caller sees <code>AuditEmitFailed</code>.</li><li>Failure to emit completed → sev-1 log + skill effects NOT reversed.</li><li>trace_id propagated through both rows.</li><li>Concurrent invocations: 100 parallel skill calls produce 200 rows (100 started + 100 completed); no race-induced loss.</li><li>Skill panic → <code>_completed</code> row emitted with <code>outcome: panic</code> + duration_ms recorded.</li><li>Compensation-related skills (e.g., cuo.cfo.payroll-draft) emit rows even though body excluded from memory.</li><li>Skill error (Result::Err) → <code>_completed</code> with <code>outcome: error</code> + <code>error_message</code>.</li><li>Skill timeout → <code>_completed</code> with <code>outcome: timeout</code>.</li><li>Audit emit p95 &lt; 50ms.</li><li>tenant_id present in both rows.</li><li>duration_ms accurate on panic (within 10ms of actual).</li><li>result_hash empty on error/panic.</li><li>OTel metric <code>skill_audit_emit_failures_total</code> increments on failure.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn skill_invocation_emits_both_rows() {\n    let ctx = test_context();\n    let result = invoke_with_audit(\"test_skill\", \"1.0.0\", json!({\"x\": 1}), &amp;ctx).await.unwrap();\n    let started = memory_test_helper::find_latest(\"skill.invoked_started\").unwrap();\n    let completed = memory_test_helper::find_latest(\"skill.invoked_completed\").unwrap();\n    assert_eq!(started.payload[\"request_id\"], completed.payload[\"request_id\"]);\n    assert_eq!(started.payload[\"skill_id\"], \"test_skill\");\n    assert_eq!(completed.payload[\"outcome\"], \"success\");\n}\n\n#[tokio::test]\nasync fn args_hash_matches_canonical_sha256() {\n    let ctx = test_context();\n    let args = json!({\"q\": \"test\"});\n    invoke_with_audit(\"noop\", \"1.0.0\", args.clone(), &amp;ctx).await.unwrap();\n    let row = memory_test_helper::find_latest(\"skill.invoked_started\").unwrap();\n    let expected = sha256_canonical(&amp;args).unwrap();\n    assert_eq!(row.payload[\"args_hash\"], expected);\n}\n\n#[tokio::test]\nasync fn started_emit_failure_blocks_dispatch() {\n    memory_test_helper::inject_emit_failure();\n    let result = invoke_with_audit(\"test_skill\", \"1.0.0\", json!({}), &amp;test_context()).await;\n    assert!(matches!(result, Err(SkillError::AuditEmitFailed { stage: \"start\", .. })));\n    let dispatched = supervisor_test_helper::dispatch_count();\n    assert_eq!(dispatched, 0);\n    memory_test_helper::clear_emit_failure();\n}\n\n#[tokio::test]\nasync fn skill_panic_emits_completed_with_outcome_panic() {\n    let ctx = test_context();\n    let result = invoke_with_audit(\"panicking_skill\", \"1.0.0\", json!({}), &amp;ctx).await;\n    assert!(matches!(result, Err(SkillError::Panicked { .. })));\n    let completed = memory_test_helper::find_latest(\"skill.invoked_completed\").unwrap();\n    assert_eq!(completed.payload[\"outcome\"], \"panic\");\n    assert!(completed.payload[\"duration_ms\"].as_u64().unwrap() &lt; 1000);\n}\n\n#[tokio::test]\nasync fn trace_id_propagated_through_both_rows() {\n    let trace_id = \"0af7651916cd43dd8448eb211c80319c\";\n    let ctx = InvocationContext { trace_id: trace_id.into(), ..test_context() };\n    invoke_with_audit(\"noop\", \"1.0.0\", json!({}), &amp;ctx).await.unwrap();\n    let started = memory_test_helper::find_latest(\"skill.invoked_started\").unwrap();\n    let completed = memory_test_helper::find_latest(\"skill.invoked_completed\").unwrap();\n    assert_eq!(started.payload[\"trace_id\"], trace_id);\n    assert_eq!(completed.payload[\"trace_id\"], trace_id);\n}\n\n#[tokio::test]\nasync fn 100_concurrent_invocations_produce_200_rows() {\n    let mut joinset = tokio::task::JoinSet::new();\n    for i in 0..100 {\n        joinset.spawn(async move {\n            invoke_with_audit(\"noop\", \"1.0.0\", json!({\"i\": i}), &amp;test_context()).await\n        });\n    }\n    while let Some(r) = joinset.join_next().await { r.unwrap().unwrap(); }\n    let started = memory_test_helper::count_rows_since(\"skill.invoked_started\", recent()).await;\n    let completed = memory_test_helper::count_rows_since(\"skill.invoked_completed\", recent()).await;\n    assert_eq!(started, 100);\n    assert_eq!(completed, 100);\n}\n\n#[tokio::test]\nasync fn audit_emit_p95_under_50ms() {\n    let mut samples = vec![];\n    for _ in 0..200 {\n        let t0 = std::time::Instant::now();\n        invoke_with_audit(\"noop\", \"1.0.0\", json!({}), &amp;test_context()).await.unwrap();\n        samples.push(t0.elapsed().as_millis() as u64);\n    }\n    samples.sort();\n    let p95 = samples[(samples.len() as f64 * 0.95) as usize];\n    assert!(p95 &lt; 100, \"p95 {p95}ms exceeds 100ms (incl skill noop)\");\n}\n\n#[tokio::test]\nasync fn completed_emit_failure_logs_sev1_no_revert() {\n    memory_test_helper::inject_emit_failure_for_kind(\"skill.invoked_completed\");\n    let _ = invoke_with_audit(\"noop\", \"1.0.0\", json!({}), &amp;test_context()).await.unwrap();\n    let metric: u64 = otel_test_helper::counter_value(\"skill_audit_emit_failures_total\", &amp;[(\"stage\", \"completed\")]);\n    assert!(metric &gt; 0);\n    // Side effect (noop = nothing) NOT reverted; this would be the assertion in a real skill\n    memory_test_helper::clear_emit_failure();\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-AI-003</strong> — memory_writer::emit() bridge.</li><li><strong>TASK-AI-022</strong> — trace_id propagation.</li><li><strong>TASK-OBS-005</strong> — TraceContext correlation.</li><li><strong>TASK-SKILL-102 (downstream)</strong> — OCI registry uses these audit rows.</li><li>Crates: <code>tokio</code>, <code>serde</code>, <code>serde_json</code>, <code>serde_jcs</code>, <code>sha2</code>, <code>hex</code>, <code>ulid@1</code>, <code>tracing</code>, <code>futures@0.3</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Started row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"skill.invoked_started\",\n  \"payload\": {\n    \"skill_id\": \"obs.triage-alert\",\n    \"version\": \"1.0.0\",\n    \"caller_persona\": \"cuo-cpo@0.4.1\",\n    \"args_hash\": \"4b8c0d2f1a7e9c3b...\",\n    \"tenant_id\": \"550e...\",\n    \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n    \"request_id\": \"01HZK...\"\n  }\n}</code></pre>\n<h3>Completed row (success)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"skill.invoked_completed\",\n  \"payload\": {\n    \"skill_id\": \"obs.triage-alert\",\n    \"outcome\": \"success\",\n    \"duration_ms\": 87,\n    \"result_hash\": \"9d6e3a2b...\",\n    \"error_message\": null,\n    \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n    \"request_id\": \"01HZK...\"\n  }\n}</code></pre>\n<h3>Completed row (panic)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"skill.invoked_completed\",\n  \"payload\": {\n    \"skill_id\": \"broken_skill\",\n    \"outcome\": \"panic\",\n    \"duration_ms\": 12,\n    \"result_hash\": \"\",\n    \"error_message\": \"panicked at 'unwrap on None'\",\n    \"trace_id\": \"...\",\n    \"request_id\": \"...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Streaming audit (long-running skills emit progress) — slice 4+.</li><li>Skill output truncation in audit (currently only hash) — slice 4+ with <code>?include_output_preview=true</code>.</li><li>Per-skill retention policies — slice 5+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>memory unreachable on _started</td><td>memory_writer error</td><td>Invocation refused; caller 503</td><td>Operator investigates memory</td></tr><tr><td>memory unreachable on _completed</td><td>memory_writer error</td><td>Sev-1 log; skill effect not reversed</td><td>Operator investigates</td></tr><tr><td>memory slow on _started (&gt;50ms)</td><td>latency histogram</td><td>Invocation proceeds (latency-tolerated); sev-3 alarm</td><td>Investigate memory_writer</td></tr><tr><td>Skill panics mid-execution</td><td>catch_unwind</td><td>Completed row emitted with outcome=panic</td><td>By design</td></tr><tr><td>Skill timeouts</td><td>dispatch timeout</td><td>Completed with outcome=timeout</td><td>By design</td></tr><tr><td>Concurrent invocations</td><td>independent emit paths</td><td>All rows emitted</td><td>By design</td></tr><tr><td>args_hash collision (cryptographic ~10⁻³⁰)</td><td>N/A</td><td>N/A</td><td>By design</td></tr><tr><td>Trace_id missing</td><td>empty string in payload</td><td>Audit complete; correlation broken</td><td>Caller fixes upstream OTel context</td></tr><tr><td>canonical-JSON serialise fails</td><td>error</td><td>Invocation refused</td><td>Caller fixes args</td></tr><tr><td>Skill returns invalid JSON</td><td>downstream parse error</td><td>outcome=error</td><td>By design</td></tr><tr><td>Audit emit during shutdown</td><td>memory_writer in-flight</td><td>Tx may abort</td><td>By design (not catastrophic)</td></tr><tr><td>Compensation skill's body excluded</td><td>hash still computed; row still emitted</td><td>By design</td><td>Auditable without leaking</td></tr><tr><td>Result hash empty for non-error</td><td>sanity check fails</td><td>Sev-1 alarm</td><td>Investigate skill output</td></tr><tr><td>duration_ms negative (clock skew)</td><td>sanity check</td><td>Sev-3</td><td>Investigate clock</td></tr><tr><td>Concurrent emit on same trace_id</td><td>independent paths</td><td>Both succeed</td><td>By design</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>Both _started and _completed rows are emitted for EVERY invocation. Operators querying \"currently running skills\" use <code>_started</code> minus matching <code>_completed</code>.</li><li>args_hash + result_hash preserve uniqueness for forensic correlation without leaking content.</li><li>Panic catch via <code>catch_unwind</code> ensures audit chain consistency — no orphan _started rows.</li><li>trace_id propagation enables joining LLM trace + LangSmith + skill audit + downstream effects.</li><li>_started failure blocks dispatch (auditability invariant); _completed failure doesn't reverse (side effects irreversible).</li><li>Concurrent invocations independent — no lock serialisation around memory_writer.</li><li>Compensation-skill bodies excluded from memory (DEC-036) but invocation rows still emitted (the FACT of the call is auditable).</li><li>duration_ms includes panic-handling time — accurate on all paths.</li><li>Result hash empty on error/panic distinguishes \"skill ran with no output\" from \"skill failed.\"</li></ul>\n<hr>\n<p><em>End of TASK-SKILL-101. Status: draft (10/10 target).</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>skill-host was consolidated into services/skill-broker (no separate crate).</p>";
