window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-105"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AUTH service <strong>MUST</strong> ship passkey enrolment + login via WebAuthn discoverable credentials (resident keys) with autofill UI + downgrade-resistance + closed enrolment FSM + recovery warnings. Each requirement:</p>\n<ol><li><strong>MUST</strong> define <code>passkey_enrolment_state</code> table: <code>(id UUID PRIMARY KEY, tenant_id UUID NOT NULL, subject_id UUID NOT NULL REFERENCES auth.subjects(id), factor_id UUID REFERENCES mfa_factors(id), challenge BYTEA NOT NULL, passkey_origin passkey_origin, state TEXT NOT NULL CHECK (state IN ('requested','confirmed','abandoned')), requested_at TIMESTAMPTZ NOT NULL DEFAULT now(), abandoned_at TIMESTAMPTZ, confirmed_at TIMESTAMPTZ, expires_at TIMESTAMPTZ NOT NULL)</code>. Inserted at enrolment start; transitions to <code>confirmed</code> on first successful use within 24h, <code>abandoned</code> if expires_at passes without confirmation.</li></ol>\n<ol><li><strong>MUST</strong> declare the closed <code>passkey_origin</code> Postgres enum with exactly 3 values (per DEC-541): <code>'platform_synced'</code>, <code>'platform_local'</code>, <code>'cross_platform'</code>. Adding a 4th is an ADR.</li></ol>\n<ol><li><strong>MUST</strong> define <code>passkey_lifecycle_log</code> table: <code>(id BIGSERIAL, tenant_id UUID, subject_id UUID, factor_id UUID, event TEXT NOT NULL, passkey_origin passkey_origin, source_ip_hash16 TEXT, ts TIMESTAMPTZ)</code>. <code>REVOKE UPDATE, DELETE FROM cyberos_app</code> (per DEC-547).</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS with <code>USING + WITH CHECK</code> on both tables; root-admin escape clause.</li></ol>\n<ol><li><strong>MUST</strong> require discoverable credentials at enrolment (per DEC-540). WebAuthn creation challenge MUST set <code>authenticatorSelection: { residentKey: \"required\", userVerification: \"required\" }</code>. Authenticators not supporting resident keys (rare in 2026) → 400 <code>authenticator_unsupported</code>.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/passkey/enrol/begin</code> handler (per DEC-545):</li></ol>\n<ul><li>Generates WebAuthn creation challenge with <code>residentKey=required + userVerification=required + attestation=none</code>.</li><li>INSERT <code>passkey_enrolment_state</code> row with <code>state='requested'</code>, <code>expires_at = now() + 24 hours</code>.</li><li>Returns <code>PublicKeyCredentialCreationOptions</code> for browser to invoke <code>navigator.credentials.create()</code>.</li><li>Emit <code>auth.passkey_enrolment_requested</code> memory row.</li></ul>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/passkey/enrol/finish</code> handler (per DEC-545):</li></ol>\n<ul><li>Receives <code>PublicKeyCredential</code> from browser.</li><li>Validates attestation per TASK-AUTH-102's WebAuthn module.</li><li>Detects <code>passkey_origin</code> from AAGUID lookup (per origin.rs map).</li><li>Creates <code>mfa_factors</code> row with <code>factor_kind = webauthn_platform</code> or <code>webauthn_cross_platform</code> per TASK-AUTH-102.</li><li>UPDATE <code>passkey_enrolment_state</code> to <code>state='confirmed', confirmed_at=now()</code>.</li><li>Emit <code>auth.passkey_enrolment_confirmed</code> memory row.</li><li>If subject has zero recovery codes → return WARNING in response body suggesting recovery-code generation (per DEC-548).</li></ul>\n<ol><li><strong>MUST</strong> ship background job to mark abandoned enrolments. Hourly: <code>UPDATE passkey_enrolment_state SET state='abandoned', abandoned_at=now() WHERE state='requested' AND expires_at &lt; now()</code>. Per row, emit <code>auth.passkey_enrolment_abandoned</code> memory row (sev-3 informational).</li></ol>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/passkey/login/begin</code> handler (per DEC-540):</li></ol>\n<ul><li>Generates WebAuthn assertion challenge with <code>allowCredentials: []</code> (empty — discoverable credential mode).</li><li><code>userVerification: \"required\"</code>.</li><li>Returns <code>PublicKeyCredentialRequestOptions</code> for browser.</li><li>Does NOT require subject email/username at this step (resident-key resolution).</li></ul>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/passkey/login/finish</code> handler:</li></ol>\n<ul><li>Receives assertion from browser including <code>userHandle</code> (= subject_id).</li><li>Lookup <code>mfa_factors</code> by credential_id; validate signature via TASK-AUTH-102's verifier.</li><li>Counter-monotonicity check (per TASK-AUTH-102 §1 #10).</li><li>On success: issue TASK-AUTH-004 JWT; emit <code>auth.passkey_login_succeeded</code>; update <code>mfa_factors.last_used_at</code>.</li><li>On fail: emit <code>auth.passkey_login_failed</code> with reason; counter increment for TASK-AUTH-102 lockout.</li></ul>\n<ol><li><strong>MUST</strong> support <strong>autofill conditional mediation</strong> (per DEC-542 + DEC-551):</li></ol>\n<ul><li><code>POST /v1/auth/passkey/autofill-options</code> returns options with <code>mediation: \"conditional\"</code> for browsers supporting WebAuthn autofill (Chrome 108+, Safari 16+).</li><li><code>user_verification: \"required\"</code> is mandatory for conditional mediation per W3C spec — enforced at handler.</li><li>On successful autofill login, emit <code>auth.passkey_autofill_used</code> memory row (informational; useful for adoption metrics).</li></ul>\n<ol><li><strong>MUST</strong> enforce <strong>downgrade-resistance</strong> (per DEC-543 + DEC-550). When subject attempts password-only login:</li></ol>\n<ul><li>JWT issuer (TASK-AUTH-004) consults <code>downgrade_gate::is_passkey_required(subject_id)</code>.</li><li>Predicate: subject has ≥ 1 confirmed passkey AND per-tenant policy doesn't have a per-subject opt-out flag set.</li><li>True → 401 <code>passkey_required</code> + emit <code>auth.passkey_downgrade_blocked</code> memory row (sev-2 per DEC-550).</li><li>Repeated attempts: alarm sev-2 at &gt; 5 within 1 hour for a subject (phishing signal).</li></ul>\n<ol><li><strong>MUST</strong> enforce max 5 active passkeys per subject (per DEC-544). 6th enrolment attempt → 409 <code>passkey_limit_exceeded</code> with body listing existing passkey display_names. Removal of an existing passkey first → enrolment proceeds.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>DELETE /v1/auth/passkey/factors/{id}</code> handler (per DEC-552):</li></ol>\n<ul><li>Caller MUST present a fresh MFA proof in the request (header <code>X-MFA-Challenge-Token: &lt;recently-verified-challenge-id&gt;</code>). Challenge must be &lt; 5 minutes old AND must have been verified for THIS subject.</li><li>Missing/stale proof → 401 <code>recent_mfa_required</code>.</li><li>On success: soft-delete passkey (mfa_factors.status → 'removed'); emit <code>auth.passkey_removed</code> memory row; if this was the last passkey for the subject + per-tenant policy requires passkey → emit warning to operator.</li></ul>\n<ol><li><strong>MUST</strong> detect passkey_origin via AAGUID lookup (per DEC-541). The origin.rs module ships a map of well-known AAGUIDs → origin:</li></ol>\n<ul><li>Apple iCloud Keychain AAGUID → <code>platform_synced</code>.</li><li>Google Password Manager (Chrome on Android) → <code>platform_synced</code>.</li><li>Windows Hello + Microsoft sync → <code>platform_synced</code>.</li><li>macOS Touch ID local (no iCloud sync) → <code>platform_local</code>.</li><li>YubiKey series → <code>cross_platform</code>.</li><li>Unknown AAGUID → <code>cross_platform</code> (conservative default; logged for analyst review).</li></ul>\n<ol><li><strong>MUST</strong> consult per-tenant <code>passkey_required_for_roles: [&lt;role&gt;...]</code> policy from tenant_policy YAML (per DEC-549). If subject's roles intersect this set AND subject has zero passkeys → JWT issuance returns 401 <code>passkey_enrolment_required</code> with hint to call enrol/begin. Founder role always per TASK-AUTH-101 DEC-128.</li></ol>\n<ol><li><strong>MUST</strong> emit 8 memory audit row kinds (per DEC-546):</li></ol>\n<ul><li><code>auth.passkey_enrolment_requested</code> — enrol/begin.</li><li><code>auth.passkey_enrolment_confirmed</code> — enrol/finish success.</li><li><code>auth.passkey_enrolment_abandoned</code> — sev-3 informational.</li><li><code>auth.passkey_login_succeeded</code> — login/finish success.</li><li><code>auth.passkey_login_failed</code> — login/finish failure.</li><li><code>auth.passkey_removed</code> — DELETE success.</li><li><code>auth.passkey_downgrade_blocked</code> — password-login rejected (sev-2).</li><li><code>auth.passkey_autofill_used</code> — conditional mediation success.</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>display_name</code> (inherited from TASK-AUTH-102's display_name) before chain commit.</li></ol>\n<ol><li><strong>MUST</strong> ship <code>GET /v1/auth/passkey/factors</code> returning subject's passkeys: <code>[{factor_id, display_name, passkey_origin, enrolled_at, last_used_at, status}]</code>. NEVER expose credential public key or AAGUID via API (operator-only via direct DB query).</li></ol>\n<ol><li><strong>MUST</strong> enforce no downgrade-resistance bypass via env (per DEC-543). The <code>downgrade_gate::is_passkey_required</code> function is hard-coded — no env override accepted. Per-subject opt-out flag exists in the schema (defaulting to FALSE) but setting it requires root-admin role + sev-2 audit row.</li></ol>\n<ol><li><strong>MUST</strong> complete enrolment + login handlers in ≤ 200 ms p95 (excludes browser-side WebAuthn ceremony). <code>passkey_perf_test</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>auth.passkey.{enrol_begin,enrol_finish,login_begin,login_finish,remove,downgrade_blocked,autofill}</code> with <code>outcome</code> attribute (success | abandoned | timeout | invalid_attestation | limit_exceeded | downgrade_blocked | recent_mfa_required | passkey_required | passkey_enrolment_required).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>auth_passkey_enrolment_total{tenant_id, passkey_origin, outcome}</code> (counter).</li><li><code>auth_passkey_login_total{tenant_id, passkey_origin, outcome}</code> (counter).</li><li><code>auth_passkey_downgrade_blocked_total{tenant_id, subject_id_hash16}</code> (counter — sev-2 alarm at &gt; 5/h per subject).</li><li><code>auth_passkey_active_count{tenant_id, passkey_origin}</code> (gauge — adoption metric).</li><li><code>auth_passkey_autofill_used_total{tenant_id}</code> (counter — UX adoption metric).</li><li><code>auth_passkey_enrolment_latency_ms</code> (histogram).</li></ul>\n<ol><li><strong>MUST</strong> warn during enrolment if subject has zero MFA recovery codes (per DEC-548 + §1 #7). Response body includes <code>recovery_warning: true, recommended_action: \"POST /v1/auth/mfa/recovery-codes/regen\"</code>. Frontend SHOULD display warning prominently.</li></ol>\n<ol><li><strong>MUST</strong> support <strong>per-subject opt-out flag</strong> (<code>mfa_factors.passkey_downgrade_optout BOOLEAN DEFAULT false</code>). Setting requires root-admin role + sev-2 memory row + reason. Used for exceptional cases (e.g. legacy integration that cannot WebAuthn).</li></ol>\n<ol><li><strong>MUST</strong> ship <code>POST /v1/auth/passkey/downgrade-optout</code> for root-admin to set the flag. Body <code>{subject_id, enabled, reason}</code>. Validates reason (1–500 chars); emit <code>auth.passkey_downgrade_optout_changed</code> audit row.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why discoverable credentials mandatory (DEC-540, §1 #5)?</strong> Discoverable credentials enable resident-key UX — user picks \"Sign in with passkey\" without typing username first. This is the passkey UX that competes with password autofill. Non-resident keys require username first → worse UX than passwords → adoption fails. Requiring resident-key at enrolment ensures the UX promise holds.</p>\n<p><strong>Why 3-value closed passkey_origin enum (DEC-541)?</strong> Operator analytics need origin granularity: \"how many users are on platform_synced (recoverable via cloud)?\" vs \"platform_local (single-device risk)?\" vs \"cross_platform (hardware key)?\". Adding a 4th (e.g. <code>external_browser_extension</code> for password managers) is an ADR — most fit the 3 categories.</p>\n<p><strong>Why autofill conditional mediation (DEC-542, §1 #11)?</strong> Browser autofill is the primary discovery surface for new passkey users. Without it, users don't know they have passkeys until prompted. Conditional mediation surfaces in password field dropdown — zero new UI required. Adoption metric <code>auth_passkey_autofill_used_total</code> tracks this.</p>\n<p><strong>Why downgrade-resistance (DEC-543, §1 #12)?</strong> Phishing remains the dominant credential attack. If user enrols a passkey then later types password on a phishing site, password works (no passkey enforcement) — passkey investment was waste. Downgrade-resistance: once you have a passkey, password login is rejected. Forces phishers to attack the passkey itself (which is unphishable by design).</p>\n<p><strong>Why per-subject opt-out exists but defaults FALSE (DEC-543, §1 #25)?</strong> Exceptional cases (legacy CLI tools that can't WebAuthn, edge-case integration). Defaulting FALSE means the security stance is \"passkey-required-when-enrolled\"; opt-out is explicit + audited + root-admin gated.</p>\n<p><strong>Why sev-2 alarm on downgrade-blocked (DEC-550, §1 #12)?</strong> Repeated password attempts on a passkey-enrolled subject signal (a) phishing-via-password, (b) credential-stuffing attack, (c) legitimate user confusion. All three warrant operator notice within an hour.</p>\n<p><strong>Why max 5 passkeys per subject (DEC-544, §1 #13)?</strong> Typical user: personal phone + work phone + personal laptop + work laptop + recovery hardware key = 5. &gt; 5 is power-user; ADR to raise. Bound prevents unbounded growth of the credential list (UX + storage).</p>\n<p><strong>Why removal requires fresh MFA (DEC-552, §1 #14)?</strong> Session hijack scenario: attacker steals session token + uses it to remove victim's passkeys + enrol their own. Requiring fresh MFA (&lt; 5 min) for removal prevents this — attacker without the second factor can't wipe.</p>\n<p><strong>Why warn on enrolment if zero recovery codes (DEC-548, §1 #24)?</strong> Passkey-only user without recovery codes who loses ALL devices → permanent lockout. Recovery codes are the bridge. The warning prompts user to set them up; explicit operator UX choice (don't silently leave users at risk).</p>\n<p><strong>Why per-tenant <code>passkey_required_for_roles</code> policy (DEC-549, §1 #16)?</strong> Different tenants have different risk tolerances. Tenant A: all employees require passkey. Tenant B: only financial-role subjects. Per-tenant config gives flexibility; founder role hard-codes baseline.</p>\n<p><strong>Why AAGUID-based origin detection (DEC-541, §1 #15)?</strong> AAGUID is the W3C-defined authenticator identifier. Apple, Google, Microsoft publish their AAGUIDs. Map them at enrolment time to populate origin column. Unknown AAGUID → conservative <code>cross_platform</code> default (e.g. brand-new YubiKey model).</p>\n<p><strong>Why login/begin without email (§1 #9)?</strong> Discoverable credential = browser already knows which subject's key matches the relying party. Asking for email first defeats the resident-key UX advantage. The <code>userHandle</code> field in the assertion response carries subject_id.</p>\n<p><strong>Why same WebAuthn factor table from TASK-AUTH-102 (§1 #7, modified_files)?</strong> Passkeys ARE WebAuthn credentials. Storing in the same <code>mfa_factors</code> table preserves the unified factor view (subject's full credential inventory in one place). The passkey-specific lifecycle log is supplementary.</p>\n<p><strong>Why enrolment FSM with abandonment timeout (DEC-545, §1 #8)?</strong> Users start enrolment, get distracted, never finish. Unconfirmed rows would pile up. Hourly job marks them abandoned with audit row — clean lifecycle, no orphans.</p>\n<p><strong>Why opt-out audit row at sev-2 (§1 #20)?</strong> Operator setting opt-out for a subject = relaxing a security control. Sev-2 ensures it's visible in OBS digests; reason field captures the justification.</p>\n<p><strong>Why credential public key never exposed via API (§1 #19)?</strong> Public key + signature counter = enough for offline attestation forgery if combined with stolen private key. Defense-in-depth: API never returns the public key; operator queries DB directly with audit trail.</p>\n<p><strong>Why \"passkey_enrolment_required\" 401 (§1 #16)?</strong> Subject with required role but no passkey would otherwise see opaque 401 on JWT issuance. Specific error code + hint to call enrol/begin gives clear remediation. Frontend handles by redirecting to enrol UX.</p>\n<p><strong>Why downgrade_gate cached inline at TASK-AUTH-004 issuer (§1 #20)?</strong> Hot path — JWT issuance happens on every login. Cache (60s TTL matching TASK-AUTH-109 pattern) keeps it fast. Invalidate on passkey enrol/remove + opt-out change.</p>\n<p><strong>Why platform_synced vs platform_local distinction (DEC-541)?</strong> platform_synced = recoverable via cloud (lose device → restore on new device). platform_local = device-bound (lose device → lose credential). Operator analytics + UX warnings differ between the two.</p>\n<p><strong>Why TASK-AUTH-105 has empty <code>blocks: []</code>?</strong> No downstream tasks explicitly depend on passkeys at slice 1 — passkeys are the UX layer on top of TASK-AUTH-102 WebAuthn. Future tasks (task-PORTAL-*) implicitly benefit but don't have a direct dependency.</p>\n<p><strong>Why explicit \"recovery_warning\" in enrol response (§1 #24)?</strong> Default frontend UI shows the warning; tenants with custom UX can decide how to surface (popover, modal, banner). Backend signals the intent; frontend executes.</p>\n<p><strong>Why no SLO / passkey-side logout (§1 #14)?</strong> Passkey is for authentication; session lifecycle is TASK-AUTH-004's concern. Logout = expire token; no passkey-specific action needed.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — Migration 0025 — enrolment_state</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0025_passkey_enrolment_state.sql\n\nBEGIN;\n\nCREATE TYPE passkey_origin AS ENUM ('platform_synced', 'platform_local', 'cross_platform');\n\nCREATE TABLE passkey_enrolment_state (\n    id                  UUID         PRIMARY KEY,\n    tenant_id           UUID         NOT NULL,\n    subject_id          UUID         NOT NULL REFERENCES auth.subjects(id),\n    factor_id           UUID         REFERENCES mfa_factors(id),\n    challenge           BYTEA        NOT NULL,\n    passkey_origin      passkey_origin,\n    state               TEXT         NOT NULL CHECK (state IN ('requested','confirmed','abandoned')) DEFAULT 'requested',\n    requested_at        TIMESTAMPTZ  NOT NULL DEFAULT now(),\n    abandoned_at        TIMESTAMPTZ,\n    confirmed_at        TIMESTAMPTZ,\n    expires_at          TIMESTAMPTZ  NOT NULL\n);\n\nCREATE INDEX passkey_enrolment_state_state_idx ON passkey_enrolment_state (state, expires_at);\nCREATE INDEX passkey_enrolment_state_subject_idx ON passkey_enrolment_state (subject_id, state);\n\nALTER TABLE passkey_enrolment_state ENABLE ROW LEVEL SECURITY;\nCREATE POLICY passkey_enrolment_state_tenant_iso ON passkey_enrolment_state\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid\n           OR current_setting('auth.is_root_admin', true) = 'true')\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\n-- Extend mfa_factors (from TASK-AUTH-102) with passkey-specific columns\nALTER TABLE mfa_factors ADD COLUMN passkey_origin passkey_origin;\nALTER TABLE mfa_factors ADD COLUMN passkey_downgrade_optout BOOLEAN NOT NULL DEFAULT false;\nALTER TABLE mfa_factors ADD COLUMN optout_reason TEXT CHECK (optout_reason IS NULL OR length(optout_reason) BETWEEN 1 AND 500);\n\nCOMMIT;</code></pre>\n<h3>3.2 — Migration 0026 — lifecycle log</h3>\n<pre><code class=\"language-sql\">-- services/auth/migrations/0026_passkey_lifecycle_log.sql\n\nBEGIN;\n\nCREATE TABLE passkey_lifecycle_log (\n    id                  BIGSERIAL    PRIMARY KEY,\n    tenant_id           UUID         NOT NULL,\n    subject_id          UUID         NOT NULL,\n    factor_id           UUID,\n    event               TEXT         NOT NULL,\n    passkey_origin      passkey_origin,\n    source_ip_hash16    TEXT,\n    ts                  TIMESTAMPTZ  NOT NULL DEFAULT now()\n);\n\nCREATE INDEX passkey_lifecycle_log_subject_idx ON passkey_lifecycle_log (subject_id, ts DESC);\nCREATE INDEX passkey_lifecycle_log_event_idx ON passkey_lifecycle_log (event, ts DESC);\n\nALTER TABLE passkey_lifecycle_log ENABLE ROW LEVEL SECURITY;\nCREATE POLICY passkey_lifecycle_log_tenant_iso ON passkey_lifecycle_log\n    USING (tenant_id = current_setting('auth.tenant_id')::uuid\n           OR current_setting('auth.is_root_admin', true) = 'true')\n    WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);\n\nREVOKE UPDATE, DELETE ON passkey_lifecycle_log FROM cyberos_app;\n\nCOMMIT;</code></pre>\n<h3>3.3 — Origin detection</h3>\n<pre><code class=\"language-rust\">// services/auth/src/passkey/origin.rs\nuse uuid::Uuid;\nuse std::collections::HashMap;\nuse once_cell::sync::Lazy;\nuse crate::passkey::PasskeyOrigin;\n\nstatic AAGUID_MAP: Lazy&lt;HashMap&lt;Uuid, PasskeyOrigin&gt;&gt; = Lazy::new(|| {\n    let mut m = HashMap::new();\n    // Apple iCloud Keychain (well-known AAGUIDs)\n    m.insert(Uuid::parse_str(\"dd4ec289-e01d-41c9-bb89-70fa845d4bf2\").unwrap(), PasskeyOrigin::PlatformSynced);\n    m.insert(Uuid::parse_str(\"adce0002-35bc-c60a-648b-0b25f1f05503\").unwrap(), PasskeyOrigin::PlatformSynced);\n    // Google Password Manager (Chrome on Android)\n    m.insert(Uuid::parse_str(\"ea9b8d66-4d01-1d21-3ce4-b6b48cb575d4\").unwrap(), PasskeyOrigin::PlatformSynced);\n    // Windows Hello (with Microsoft sync)\n    m.insert(Uuid::parse_str(\"08987058-cadc-4b81-b6e1-30de50dcbe96\").unwrap(), PasskeyOrigin::PlatformSynced);\n    // YubiKey 5 series (cross-platform hardware key examples)\n    m.insert(Uuid::parse_str(\"ee882879-721c-4913-9775-3dfcce97072a\").unwrap(), PasskeyOrigin::CrossPlatform);\n    m.insert(Uuid::parse_str(\"fa2b99dc-9e39-4257-8f92-4a30d23c4118\").unwrap(), PasskeyOrigin::CrossPlatform);\n    m\n});\n\npub fn detect(aaguid: Uuid) -&gt; PasskeyOrigin {\n    *AAGUID_MAP.get(&amp;aaguid).unwrap_or(&amp;PasskeyOrigin::CrossPlatform)   // conservative default\n}</code></pre>\n<h3>3.4 — Enrolment FSM</h3>\n<pre><code class=\"language-rust\">// services/auth/src/passkey/enrolment.rs\nuse chrono::{DateTime, Duration, Utc};\nuse sqlx::PgPool;\nuse uuid::Uuid;\n\npub const ENROLMENT_TTL_HOURS: i64 = 24;\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum EnrolmentState { Requested, Confirmed, Abandoned }\n\npub async fn begin_enrolment(\n    pool: &amp;PgPool,\n    tenant_id: Uuid,\n    subject_id: Uuid,\n    challenge: &amp;[u8],\n) -&gt; anyhow::Result&lt;Uuid&gt; {\n    let id = Uuid::new_v4();\n    let expires_at = Utc::now() + Duration::hours(ENROLMENT_TTL_HOURS);\n    sqlx::query(r#\"\n        INSERT INTO passkey_enrolment_state (id, tenant_id, subject_id, challenge, state, expires_at)\n        VALUES ($1, $2, $3, $4, 'requested', $5)\n    \"#).bind(id).bind(tenant_id).bind(subject_id).bind(challenge).bind(expires_at)\n       .execute(pool).await?;\n    Ok(id)\n}\n\npub async fn confirm_enrolment(\n    pool: &amp;PgPool,\n    enrolment_id: Uuid,\n    factor_id: Uuid,\n    passkey_origin: super::PasskeyOrigin,\n) -&gt; anyhow::Result&lt;()&gt; {\n    let updated = sqlx::query(r#\"\n        UPDATE passkey_enrolment_state\n        SET state='confirmed', confirmed_at=now(), factor_id=$2, passkey_origin=$3::passkey_origin\n        WHERE id=$1 AND state='requested' AND expires_at &gt; now()\n    \"#).bind(enrolment_id).bind(factor_id).bind(format!(\"{passkey_origin:?}\").to_lowercase())\n       .execute(pool).await?;\n    if updated.rows_affected() == 0 {\n        anyhow::bail!(\"enrolment_not_pending_or_expired\");\n    }\n    Ok(())\n}\n\npub async fn run_abandonment_job(pool: &amp;PgPool) -&gt; anyhow::Result&lt;usize&gt; {\n    let updated = sqlx::query(r#\"\n        UPDATE passkey_enrolment_state SET state='abandoned', abandoned_at=now()\n        WHERE state='requested' AND expires_at &lt; now()\n    \"#).execute(pool).await?;\n    Ok(updated.rows_affected() as usize)\n}</code></pre>\n<h3>3.5 — Downgrade gate</h3>\n<pre><code class=\"language-rust\">// services/auth/src/passkey/downgrade_gate.rs\nuse sqlx::PgPool;\nuse uuid::Uuid;\nuse std::sync::Arc;\nuse tokio::sync::RwLock;\nuse std::collections::HashMap;\nuse std::time::{Duration, Instant};\n\npub struct DowngradeGate {\n    cache: Arc&lt;RwLock&lt;HashMap&lt;Uuid, (bool, Instant)&gt;&gt;&gt;,\n}\n\nconst CACHE_TTL_SECONDS: u64 = 60;\n\nimpl DowngradeGate {\n    pub fn new() -&gt; Self {\n        Self { cache: Arc::new(RwLock::new(HashMap::new())) }\n    }\n\n    /// Returns true if the subject has ≥1 active passkey AND no opt-out flag.\n    pub async fn is_passkey_required(&amp;self, subject_id: Uuid, db: &amp;PgPool) -&gt; bool {\n        if let Some((req, t)) = self.cache.read().await.get(&amp;subject_id) {\n            if t.elapsed() &lt; Duration::from_secs(CACHE_TTL_SECONDS) {\n                return *req;\n            }\n        }\n        let row: Option&lt;(i64, bool)&gt; = sqlx::query_as(r#\"\n            SELECT COUNT(*) AS pk_count,\n                   COALESCE(BOOL_OR(passkey_downgrade_optout), false) AS has_optout\n            FROM mfa_factors\n            WHERE subject_id = $1\n              AND status = 'active'\n              AND factor_kind IN ('webauthn_platform','webauthn_cross_platform')\n        \"#).bind(subject_id).fetch_optional(db).await.ok().flatten();\n\n        let required = match row {\n            Some((count, has_optout)) =&gt; count &gt; 0 &amp;&amp; !has_optout,\n            None =&gt; false,\n        };\n        self.cache.write().await.insert(subject_id, (required, Instant::now()));\n        required\n    }\n\n    pub async fn invalidate(&amp;self, subject_id: Uuid) {\n        self.cache.write().await.remove(&amp;subject_id);\n    }\n}</code></pre>\n<h3>3.6 — Enrol begin handler</h3>\n<pre><code class=\"language-rust\">// services/auth/src/handlers/passkey.rs (excerpt)\nuse axum::{Json, extract::State, http::StatusCode};\nuse crate::passkey::{enrolment, audit};\n\n#[derive(Serialize)]\npub struct EnrolBeginResponse {\n    pub enrolment_id: uuid::Uuid,\n    pub public_key_creation_options: serde_json::Value,\n    pub recovery_warning: bool,\n    pub recommended_action: Option&lt;String&gt;,\n}\n\npub async fn enrol_begin(\n    State(state): State&lt;AppState&gt;,\n    claims: Claims,\n) -&gt; Result&lt;(StatusCode, Json&lt;EnrolBeginResponse&gt;), ApiError&gt; {\n    // (1) Check passkey count vs 5-cap\n    let active_count: i64 = sqlx::query_scalar(\n        \"SELECT COUNT(*) FROM mfa_factors\n         WHERE subject_id=$1 AND status='active' AND factor_kind IN ('webauthn_platform','webauthn_cross_platform')\"\n    ).bind(claims.subject_id()).fetch_one(&amp;state.db).await?;\n    if active_count &gt;= 5 {\n        return Err(ApiError::PasskeyLimitExceeded);\n    }\n\n    // (2) Generate WebAuthn creation challenge\n    let challenge = state.webauthn.start_passkey_registration(claims.subject_id(), claims.tenant_id()).await?;\n\n    // (3) Persist enrolment state\n    let enrolment_id = enrolment::begin_enrolment(&amp;state.db, claims.tenant_id(), claims.subject_id(), challenge.challenge_bytes()).await?;\n\n    // (4) Recovery-code warning\n    let has_recovery: bool = sqlx::query_scalar(\n        \"SELECT EXISTS(SELECT 1 FROM mfa_recovery_codes WHERE subject_id=$1 AND consumed=false)\"\n    ).bind(claims.subject_id()).fetch_one(&amp;state.db).await?;\n\n    // (5) Audit\n    audit::emit_passkey_enrolment_requested(claims.tenant_id(), claims.subject_id()).await;\n\n    Ok((StatusCode::CREATED, Json(EnrolBeginResponse {\n        enrolment_id,\n        public_key_creation_options: challenge.public_key_credential_creation_options(),\n        recovery_warning: !has_recovery,\n        recommended_action: if !has_recovery { Some(\"POST /v1/auth/mfa/recovery-codes/regen\".into()) } else { None },\n    })))\n}</code></pre>\n<h3>3.7 — Login finish handler</h3>\n<pre><code class=\"language-rust\">// services/auth/src/handlers/passkey.rs (continued)\n#[derive(Deserialize)]\npub struct LoginFinishRequest {\n    pub assertion_response: serde_json::Value,\n}\n\npub async fn login_finish(\n    State(state): State&lt;AppState&gt;,\n    Json(req): Json&lt;LoginFinishRequest&gt;,\n) -&gt; Result&lt;Json&lt;LoginResponse&gt;, ApiError&gt; {\n    // (1) Extract credential_id + userHandle (subject_id) from assertion\n    let cred_id = extract_credential_id(&amp;req.assertion_response)?;\n    let user_handle = extract_user_handle(&amp;req.assertion_response)?;   // subject_id\n\n    // (2) Lookup factor\n    let factor = state.repo.find_factor_by_credential_id(&amp;cred_id).await?\n        .ok_or(ApiError::PasskeyNotFound)?;\n    if factor.subject_id != user_handle {\n        return Err(ApiError::PasskeyUserHandleMismatch);\n    }\n\n    // (3) Verify signature via TASK-AUTH-102 webauthn module + counter-monotonicity\n    let result = state.webauthn.verify_assertion(&amp;factor, &amp;req.assertion_response).await;\n    match result {\n        Ok(new_counter) =&gt; {\n            state.repo.update_factor_counter_and_last_used(factor.id, new_counter).await?;\n            audit::emit_passkey_login_succeeded(factor.tenant_id, factor.subject_id, factor.id, factor.passkey_origin).await;\n            let token = state.jwt_issuer.issue_for_subject(factor.subject_id, factor.tenant_id).await?;\n            Ok(Json(LoginResponse { access_token: token }))\n        }\n        Err(crate::mfa::webauthn::VerifyError::ClonedAuthenticator) =&gt; {\n            // Per TASK-AUTH-102 §1 #10 — revoke factor + sev-1 audit\n            state.repo.revoke_factor(factor.id, \"cloned_authenticator_detected\").await?;\n            audit::emit_passkey_login_failed(factor.tenant_id, factor.subject_id, factor.id, \"cloned_authenticator\").await;\n            Err(ApiError::ClonedAuthenticatorDetected)\n        }\n        Err(e) =&gt; {\n            audit::emit_passkey_login_failed(factor.tenant_id, factor.subject_id, factor.id, &amp;format!(\"{e:?}\")).await;\n            Err(ApiError::PasskeyLoginFailed)\n        }\n    }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>PasskeyOrigin enum closed at 3</strong> — platform_synced, platform_local, cross_platform.</li><li><strong>Enrolment requires resident-key + UV-required</strong> — challenge config asserted.</li><li><strong>Enrol begin → 201 + enrolment_id + challenge</strong> — happy path.</li><li><strong>Enrol finish within 24h confirms</strong> — state → confirmed.</li><li><strong>Enrol abandonment after 24h</strong> — hourly job sets state=abandoned + emits sev-3 audit.</li><li><strong>Enrol exceeding 5-cap rejected</strong> → 409 passkey_limit_exceeded.</li><li><strong>Recovery warning emitted when zero codes</strong> — response carries recovery_warning=true.</li><li><strong>Login begin returns options with allowCredentials=[] (discoverable mode)</strong>.</li><li><strong>Login finish with valid signature → 200</strong> + JWT issued + <code>auth.passkey_login_succeeded</code> row.</li><li><strong>Login finish with cloned authenticator</strong> → factor revoked + sev-1 audit per TASK-AUTH-102.</li><li><strong>Login finish with bad signature</strong> → 401 + <code>auth.passkey_login_failed</code>.</li><li><strong>Password login blocked when passkey enrolled</strong> → 401 passkey_required + <code>auth.passkey_downgrade_blocked</code> (sev-2).</li><li><strong>Per-subject opt-out flag bypasses downgrade-resistance</strong> → password login OK.</li><li><strong>Per-subject opt-out requires root-admin + reason</strong> → 403 if non-root-admin.</li><li><strong>Opt-out emits sev-2 audit row</strong>.</li><li><strong>Conditional mediation requires UV=required</strong> — handler rejects without UV.</li><li><strong>Conditional mediation autofill_used emits memory row</strong>.</li><li><strong>Per-tenant passkey_required_for_roles enforced</strong> — subject with role in policy + no passkey → 401 passkey_enrolment_required.</li><li><strong>Founder role always requires passkey</strong> (DEC-128).</li><li><strong>Removal requires X-MFA-Challenge-Token &lt; 5min old</strong> → 401 recent_mfa_required if absent.</li><li><strong>Removal soft-deletes + emits <code>auth.passkey_removed</code></strong>.</li><li><strong>AAGUID origin detection</strong> — Apple/Google/Microsoft AAGUIDs map to platform_synced; YubiKey → cross_platform; unknown → cross_platform.</li><li><strong>GET /factors lists passkeys</strong> — public_key never exposed.</li><li><strong>Counter <code>auth_passkey_downgrade_blocked_total</code> sev-2 alarm at &gt; 5/h per subject</strong>.</li><li><strong>Counter <code>auth_passkey_autofill_used_total</code> increments</strong> on conditional mediation success.</li><li><strong>append-only lifecycle_log</strong> — UPDATE/DELETE blocked.</li><li><strong>Perf p95 &lt; 200ms</strong> — enrol + login handlers.</li><li><strong>OTel span emitted per handler</strong> — outcome attribute.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/auth/tests/passkey_downgrade_block_test.rs\n#[tokio::test]\nasync fn password_login_blocked_after_passkey_enrol(ctx: TestCtx) {\n    let subject = ctx.create_test_subject_with_password().await;\n    // Initially: password login succeeds\n    let r1 = ctx.password_login(&amp;subject.email, \"test_password\").await;\n    assert!(r1.is_ok());\n    // Enrol passkey\n    ctx.enrol_passkey_for(subject.id).await;\n    // Now password login must fail\n    let r2 = ctx.password_login(&amp;subject.email, \"test_password\").await;\n    assert!(matches!(r2, Err(e) if format!(\"{e:?}\").contains(\"passkey_required\")));\n    let rows = ctx.memory_audit_rows(\"auth.passkey_downgrade_blocked\").await;\n    assert!(!rows.is_empty());\n    assert_eq!(rows[0][\"severity\"], \"sev-2\");\n}\n\n#[tokio::test]\nasync fn opt_out_allows_password_login(ctx: TestCtx) {\n    let subject = ctx.create_subject_with_passkey_enrolled().await;\n    ctx.post_as_root_admin(\"/v1/auth/passkey/downgrade-optout\",\n        json!({\"subject_id\": subject.id, \"enabled\": true, \"reason\": \"legacy CLI integration\"})).await.unwrap();\n    let r = ctx.password_login(&amp;subject.email, \"test_password\").await;\n    assert!(r.is_ok());\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/passkey_max_5_per_subject_test.rs\n#[tokio::test]\nasync fn sixth_enrolment_rejected(ctx: TestCtx) {\n    let subject = ctx.create_test_subject().await;\n    for _ in 0..5 { ctx.enrol_passkey_for(subject.id).await; }\n    let err = ctx.try_enrol_passkey_for(subject.id).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"passkey_limit_exceeded\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/passkey_removal_requires_reauth_test.rs\n#[tokio::test]\nasync fn remove_without_recent_mfa_rejected(ctx: TestCtx) {\n    let subject = ctx.create_subject_with_passkey().await;\n    let token = ctx.issue_token_for(subject.id).await;\n    // Try removal without X-MFA-Challenge-Token header\n    let err = ctx.delete_passkey(&amp;subject.passkey_id, &amp;token, None).await.unwrap_err();\n    assert!(format!(\"{err:?}\").contains(\"recent_mfa_required\"));\n}\n\n#[tokio::test]\nasync fn remove_with_recent_mfa_succeeds(ctx: TestCtx) {\n    let subject = ctx.create_subject_with_passkey().await;\n    let token = ctx.issue_token_for(subject.id).await;\n    let mfa_token = ctx.verify_mfa_get_challenge_token(subject.id).await;\n    let r = ctx.delete_passkey(&amp;subject.passkey_id, &amp;token, Some(&amp;mfa_token)).await;\n    assert!(r.is_ok());\n    let rows = ctx.memory_audit_rows(\"auth.passkey_removed\").await;\n    assert!(!rows.is_empty());\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/passkey_abandonment_24h_test.rs\n#[tokio::test]\nasync fn enrolment_abandoned_after_24h(ctx: TestCtx) {\n    let subject = ctx.create_test_subject().await;\n    let enrolment_id = ctx.begin_enrolment_for(subject.id).await;\n    ctx.advance_clock_hours(25).await;\n    let abandoned = cyberos_auth::passkey::enrolment::run_abandonment_job(&amp;ctx.pool).await.unwrap();\n    assert_eq!(abandoned, 1);\n    let state = ctx.fetch_enrolment_state(enrolment_id).await;\n    assert_eq!(state.state, \"abandoned\");\n    let rows = ctx.memory_audit_rows(\"auth.passkey_enrolment_abandoned\").await;\n    assert!(!rows.is_empty());\n}</code></pre>\n<pre><code class=\"language-rust\">// services/auth/tests/passkey_origin_enum_closed_test.rs\n#[test]\nfn origin_enum_exactly_3() {\n    use cyberos_auth::passkey::PasskeyOrigin;\n    assert_eq!(PasskeyOrigin::ALL.len(), 3);\n}\n\n#[test]\nfn unknown_aaguid_defaults_cross_platform() {\n    let unknown = uuid::Uuid::new_v4();\n    let origin = cyberos_auth::passkey::origin::detect(unknown);\n    assert_eq!(origin, cyberos_auth::passkey::PasskeyOrigin::CrossPlatform);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; 8 memory row builders follow canonical pattern; webauthn-rs handles cryptographic plumbing via TASK-AUTH-102.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-AUTH-102</strong> — TOTP + WebAuthn MFA (passkeys ARE WebAuthn discoverable credentials; reuses factor table + verifier).</li></ul>\n<p><strong>Downstream:</strong> none at slice 1.</p>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AUTH-002</strong> — subject (PRIMARY KEY for factor + enrolment rows).</li><li><strong>TASK-AUTH-004</strong> — JWT issuer (downgrade-gate hook).</li><li><strong>TASK-AUTH-101</strong> — RBAC (founder = passkey-required; root-admin for opt-out + force-remove).</li><li><strong>TASK-AI-003</strong> — memory audit bridge.</li><li><strong>TASK-MEMORY-111</strong> — PII scrubbing (display_name).</li><li><strong>TASK-AI-005</strong> — per-tenant policy YAML (passkey_required_for_roles).</li><li><strong>TASK-OBS-007</strong> — sev-2 alarm rules.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — POST /v1/auth/passkey/enrol/begin response</h3>\n<pre><code class=\"language-json\">{\n  \"enrolment_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"public_key_creation_options\": {\n    \"rp\": { \"id\": \"cyberos.world\", \"name\": \"CyberOS\" },\n    \"user\": { \"id\": \"&lt;base64url-subject-id&gt;\", \"name\": \"alice@acme.example\", \"displayName\": \"Alice Vu\" },\n    \"challenge\": \"&lt;base64url-32-bytes&gt;\",\n    \"pubKeyCredParams\": [{\"type\":\"public-key\",\"alg\":-7},{\"type\":\"public-key\",\"alg\":-257}],\n    \"authenticatorSelection\": {\n      \"residentKey\": \"required\",\n      \"userVerification\": \"required\",\n      \"authenticatorAttachment\": null\n    },\n    \"attestation\": \"none\",\n    \"timeout\": 60000\n  },\n  \"recovery_warning\": true,\n  \"recommended_action\": \"POST /v1/auth/mfa/recovery-codes/regen\"\n}</code></pre>\n<h3>8.2 — auth.passkey_enrolment_confirmed memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.passkey_enrolment_confirmed\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"factor_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"passkey_origin\": \"platform_synced\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.3 — auth.passkey_downgrade_blocked memory row (sev-2)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.passkey_downgrade_blocked\",\n  \"severity\": \"sev-2\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"source_ip_hash16\": \"fed0987654321abc\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.4 — POST downgrade-optout</h3>\n<pre><code class=\"language-json\">{\n  \"subject_id\": \"9b1deb4d-...\",\n  \"enabled\": true,\n  \"reason\": \"Legacy CLI integration requires password auth; exception approved by CSEC ticket SEC-2026-Q3-042\"\n}</code></pre>\n<h3>8.5 — auth.passkey_autofill_used memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"auth.passkey_autofill_used\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"passkey_origin\": \"platform_synced\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Multi-tenant passkey portability</strong> — same physical key across tenants treated as distinct credentials at slice 1; cross-tenant binding at slice 3.</li><li><strong>Passkey backup/recovery via cross-platform key</strong> — recovery codes are the primary fallback; cross-platform-key-as-recovery is task-AUTH-2xx UX.</li><li><strong>Passkey enrolment UI on mobile native app</strong> — slice 2 (web flow only at slice 1).</li><li><strong>Sync detection from authenticator metadata</strong> — extension protocols not standardised yet; AAGUID lookup is the slice 1 proxy.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Enrolment timeout &gt; 24h</td><td>abandonment job</td><td>state=abandoned + audit</td><td>Re-initiate</td></tr><tr><td>Enrolment exceeds 5-cap</td><td>handler check</td><td>409 passkey_limit_exceeded</td><td>Remove old passkey first</td></tr><tr><td>Discoverable credential not supported</td><td>challenge fail</td><td>400 authenticator_unsupported</td><td>Use different authenticator</td></tr><tr><td>Counter monotonicity violation (cloned key)</td><td>TASK-AUTH-102 verifier</td><td>Factor revoked + sev-1 audit</td><td>Re-enrol</td></tr><tr><td>Password login on passkey-enrolled subject</td><td>downgrade_gate</td><td>401 passkey_required + sev-2</td><td>Use passkey</td></tr><tr><td>Opt-out by non-root-admin</td><td>role check</td><td>403</td><td>Designed</td></tr><tr><td>Opt-out without reason</td><td>validation</td><td>400</td><td>Designed</td></tr><tr><td>Removal without recent MFA</td><td>header check</td><td>401 recent_mfa_required</td><td>Verify MFA first</td></tr><tr><td>AAGUID unknown</td><td>conservative default</td><td>origin=cross_platform</td><td>Designed</td></tr><tr><td>Conditional mediation without UV=required</td><td>handler</td><td>400</td><td>Designed</td></tr><tr><td>Per-tenant policy requires passkey + subject has none</td><td>JWT issuer</td><td>401 passkey_enrolment_required</td><td>Enrol</td></tr><tr><td>Founder bypass attempt</td><td>hard-coded</td><td>401</td><td>Designed</td></tr><tr><td>Enrol/finish after 24h expiry</td><td>confirm_enrolment check</td><td>\"enrolment_not_pending_or_expired\"</td><td>Re-initiate</td></tr><tr><td>Concurrent enrolment for same subject</td><td>DB row insert</td><td>OK (multiple pending)</td><td>None</td></tr><tr><td>Append-only log UPDATE from app</td><td>SQL grant</td><td>permission denied</td><td>Designed</td></tr><tr><td>Public key exposed in API</td><td>handler omits</td><td>Never returned</td><td>Designed</td></tr><tr><td>RLS bypass</td><td>USING</td><td>0 rows</td><td>Designed</td></tr><tr><td>Cross-tenant passkey</td><td>credential_id UNIQUE per subject</td><td>409 if reused</td><td>Designed</td></tr><tr><td>user_handle mismatch in login</td><td>handler check</td><td>401</td><td>Designed</td></tr><tr><td>memory audit fail mid-tx</td><td>rollback</td><td>500</td><td>memory_writer health</td></tr><tr><td>Cache stale during opt-out toggle</td><td>60s TTL + invalidate</td><td>Brief delay</td><td>Designed</td></tr><tr><td>&gt; 5/h downgrade-blocked sustained</td><td>OBS rule</td><td>sev-2</td><td>Investigate phishing</td></tr><tr><td>Lost all devices + no recovery codes</td><td>None (designed risk)</td><td>Permanent lockout</td><td>Recovery codes mandatory in enrol warning</td></tr><tr><td>Browser doesn't support conditional mediation</td><td>feature detection at frontend</td><td>Fallback to explicit \"Use passkey\" button</td><td>None — designed</td></tr><tr><td>Subject deleted while passkeys exist</td><td>FK RESTRICT</td><td>DELETE auth.subjects fails</td><td>Soft-delete subject</td></tr><tr><td>Concurrent removal + enrolment</td><td>row lock</td><td>Serial</td><td>Designed</td></tr><tr><td>OTel span attribute missing</td><td>otel_test</td><td>CI fails</td><td>Fix</td></tr><tr><td>Enrolment challenge replay</td><td>challenge UNIQUE per row</td><td>Already-confirmed challenge → no-op</td><td>Designed</td></tr><tr><td>Per-subject <code>passkey_downgrade_optout</code> flipped without audit</td><td>trigger</td><td>sev-2 row mandatory</td><td>Designed</td></tr><tr><td>Counter alarms not firing</td><td>OBS rule</td><td>CI fails</td><td>Coordinate with TASK-OBS-007</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Passkey = WebAuthn discoverable credential</strong> — same factor table as TASK-AUTH-102.</li><li><strong>Closed 3-value passkey_origin enum</strong> — adoption analytics driver.</li><li><strong>Autofill conditional mediation</strong> — primary discovery surface for new users.</li><li><strong>Downgrade-resistance</strong> — once enrolled, password login blocked; opt-out is exception path.</li><li><strong>Max 5 per subject</strong> — typical user count.</li><li><strong>Removal requires fresh MFA (&lt; 5min)</strong> — session-hijack defense.</li><li><strong>AAGUID lookup for origin detection</strong> — Apple/Google/Microsoft + YubiKey known IDs.</li><li><strong>Recovery warning at enrol</strong> — passkey-only + no recovery = lockout risk.</li><li><strong>Per-tenant <code>passkey_required_for_roles</code> YAML</strong> — flexibility; founder hard-coded.</li><li><strong>opt-out flag + root-admin + audit</strong> — explicit exception path.</li><li><strong>Append-only lifecycle log</strong> — forensic.</li><li><strong>8 memory audit kinds</strong> — selective operator queries.</li><li><strong>Sev-2 alarm on &gt; 5/h downgrade-blocked</strong> — phishing signal.</li><li><strong>Conditional mediation requires UV=required</strong> — W3C spec compliance.</li><li><strong>userHandle = subject_id</strong> — resident-key login resolves subject without username input.</li><li><strong>Public key never exposed via API</strong> — defense in depth.</li><li><strong>Cache 60s on downgrade gate</strong> — matches TASK-AUTH-109 pattern.</li><li><strong>Hourly abandonment job</strong> — clean lifecycle, no orphan rows.</li><li><strong>No SLO</strong> — passkey is auth, not session.</li><li><strong>AAGUID unknown defaults cross_platform</strong> — conservative.</li><li><strong>PII scrub display_name</strong> — chain holds scrubbed.</li></ul>\n<hr>\n<p><em>End of TASK-AUTH-105.</em></p>";
