window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-202"]="<h2>Summary</h2>\n<p>Wave 3 of the continuous-improvement harness: when a <code>refinement_proposal@1</code> is approved (via <code>cyberos-cuo proposal apply &lt;id&gt;</code>), this task's applier reads the proposal's <code>## Suggested change</code> diff, classifies it as <strong>minor</strong> / <strong>major</strong> / <strong>safety</strong> per the target skill's <code>human_fine_tune.review_required</code> policy, and either:</p>\n<ul><li><strong>Minor + <code>on_minor_bump: false</code></strong> → applies the diff automatically, bumps the skill's <code>metadata.version</code> patch number, writes <code>memory_refinement_entry</code> aux row + CHANGELOG entry.</li><li><strong>Major OR <code>on_*: true</code></strong> → queues under <code>&lt;proposals_root&gt;/pending_approval/&lt;stripe&gt;-&lt;ts&gt;.md</code>, requires explicit <code>cyberos-cuo proposal approve &lt;id&gt;</code> from Stephen before applying.</li></ul>\n<p>This is the first wave where the harness can mutate the codebase. Heavily gated by classification, version bumps, and audit emission.</p>\n<h2>Problem</h2>\n<p>After TASK-CUO-201, the harness queues good proposals into <code>docs/proposals/open/</code>. But every proposal requires Stephen to manually edit the skill file, bump the version, run tests, and commit. For boring proposals (typo fix in a description, wording polish on a rule rationale), this is a tax on attention with no real risk. For risky proposals (new rule_id, removal of an existing rule, contract version bump), the manual gate is essential.</p>\n<p>Without automatic application of low-risk changes, the harness becomes a backlog rather than an active improvement loop.</p>\n<h2>Proposed Solution</h2>\n<h3>§1 Normative requirements</h3>\n<ol><li><strong>MUST</strong> ship <code>cuo/core/proposal_applier.py</code> with <code>apply_proposal(proposal_path) -&gt; ApplyResult</code> that reads the proposal, classifies the diff, and either applies-or-queues.</li><li><strong>MUST</strong> ship <code>cuo/core/version_bump.py</code> with <code>bump(skill_path, level: \"patch\" | \"minor\" | \"major\") -&gt; str</code> that rewrites the <code>metadata.version</code> field in SKILL.md frontmatter and returns the new version.</li><li><strong>MUST</strong> classify diffs into 4 buckets:</li></ol>\n<ul><li><code>cosmetic</code> (description wording, comments, formatting only) → patch bump, auto-apply.</li><li><code>rule_addition</code> (new SEC/QA/TRACE rule entry) → minor bump, <strong>major</strong> review-required class → queue.</li><li><code>rule_removal</code> (deletes a rule from RUBRIC) → major bump, queue.</li><li><code>contract_field_change</code> (SKILL.md <code>cyberos-template</code> or contract <code>template.md</code> field shape change) → major bump, queue.</li></ul>\n<ol><li><strong>MUST</strong> honour the target skill's <code>human_fine_tune.review_required</code> flags. Any <code>True</code> flag matching the proposal's bucket forces queue regardless of bucket default.</li><li><strong>MUST</strong> write proposals that auto-apply to <code>&lt;proposals_root&gt;/applied/&lt;stripe&gt;-&lt;ts&gt;.md</code> after the apply; queued ones to <code>&lt;proposals_root&gt;/pending_approval/&lt;stripe&gt;-&lt;ts&gt;.md</code>.</li><li><strong>MUST</strong> emit <code>cuo.proposal_applied</code> memory aux row per auto-apply; payload <code>{stripe_id, skill_path, bump_level, old_version, new_version, diff_summary}</code>. Emit <code>cuo.proposal_queued</code> for queued ones.</li><li><strong>MUST</strong> add CLI subcommands:</li></ol>\n<ul><li><code>cyberos-cuo proposal apply &lt;stripe&gt;</code> — runs the applier (auto-apply OR queue depending on classification).</li><li><code>cyberos-cuo proposal approve &lt;stripe&gt;</code> — moves a <code>pending_approval/</code> proposal to <code>applied/</code> AND runs the apply (this is the explicit HITL gate).</li><li><code>cyberos-cuo proposal classify &lt;stripe&gt;</code> — dry-run; shows the bucket + bump level + would-be action.</li></ul>\n<ol><li><strong>MUST</strong> run the skill's acceptance/TRIGGER_TESTS.md tests against the new version BEFORE writing the applied state — if any test fails, abort + queue + emit <code>cuo.proposal_apply_failed</code>.</li><li><strong>MUST</strong> append a <code>CHANGELOG.md</code> entry per applied proposal with the format: <code>### YYYY-MM-DD — [SKILL] &lt;skill_name&gt; v&lt;old&gt; → v&lt;new&gt;</code>, followed by the diff summary.</li><li><strong>MUST NOT</strong> apply ANY proposal whose <code>## Risk class</code> body section equals <code>\"safety\"</code> automatically — <code>on_safety_change: true</code> is the protocol's defence-in-depth and overrides bucket classification.</li></ol>\n<h3>§2 Bump-level table</h3>\n<table><thead><tr><th>diff bucket</th><th>default bump</th><th>review_required override</th></tr></thead><tbody><tr><td><code>cosmetic</code> (description / comment)</td><td><code>patch</code></td><td>none</td></tr><tr><td><code>wording_polish</code> (rule rationale prose)</td><td><code>patch</code></td><td>none</td></tr><tr><td><code>threshold_tune</code> (numeric threshold in <code>self_audit</code> block)</td><td><code>minor</code></td><td><code>on_minor_bump: true</code> → queue</td></tr><tr><td><code>rule_addition</code></td><td><code>minor</code></td><td><code>on_rubric_rule_added: true</code> → queue</td></tr><tr><td><code>rule_removal</code></td><td><code>major</code></td><td>always queue</td></tr><tr><td><code>contract_field_change</code></td><td><code>major</code></td><td>always queue</td></tr><tr><td><code>safety_class</code> (anything marked <code>## Risk class: safety</code> in proposal body)</td><td><code>major</code></td><td>always queue, NEVER auto</td></tr></tbody></table>\n<h2>Alternatives Considered</h2>\n<ol><li><strong>All proposals require HITL</strong> — defeats the purpose of the harness for boring fixes.</li><li><strong>All proposals auto-apply</strong> — unacceptable risk; rule removal could destabilise audits silently.</li><li><strong>Slack-based approval inline</strong> — out of scope; CLI-based HITL is good enough for Stephen's solo workflow today.</li></ol>\n<h2>Success Metrics</h2>\n<table><thead><tr><th>metric</th><th>baseline</th><th>target</th><th>deadline</th></tr></thead><tbody><tr><td>% of proposals auto-applied (vs. queued)</td><td>0%</td><td>30–60%</td><td>30 days post-ship</td></tr><tr><td>% auto-applied proposals reverted by Stephen within 24h (false-positive auto-apply)</td><td>n/a</td><td>&lt; 5%</td><td>continuous</td></tr><tr><td>Median time from proposal-created to applied (for auto-bucket)</td><td>n/a</td><td>&lt; 5 min</td><td>continuous</td></tr></tbody></table>\n<h2>Scope</h2>\n<p>In scope: classifier, applier, version-bumper, test-gate, CLI, audit row emission.</p>\n<h3>Out of scope</h3>\n<ul><li>Workflow chain edits (Wave 4 — TASK-CUO-203)</li><li>Multi-skill atomic apply (transactional cross-file changes)</li><li>Cross-tenant proposal propagation</li></ul>\n<h2>Dependencies</h2>\n<ul><li><strong>TASK-CUO-201</strong> — proposals must exist before they can be applied.</li></ul>\n<h2>AI Risk Assessment</h2>\n<h3>Data Sources</h3>\n<p>The applier reads two trusted sources only: the proposal markdown file itself (authored by an LLM under operator review per TASK-CUO-201), and the target SKILL.md / RUBRIC.md files (operator-authored). No external network calls. No untrusted user input enters the applier.</p>\n<h3>Human Oversight</h3>\n<p><code>major</code> / <code>safety</code> class diffs ALWAYS queue under <code>pending_approval/</code> and never apply without an explicit <code>cyberos-cuo proposal approve &lt;stripe&gt;</code> command — that command IS the human oversight gate. <code>minor</code> class diffs honour each skill's per-skill <code>human_fine_tune.review_required</code> flag set; if the target skill declares <code>on_minor_bump: true</code>, the operator's review remains required even though the diff bucket would otherwise auto-apply. The pre-apply TRIGGER_TESTS gate (§1 #8) is a second oversight layer — even auto-approved diffs revert if the skill's own acceptance tests fail post-bump.</p>\n<h3>Failure Modes</h3>\n<p>(a) <strong>False-positive auto-apply</strong> — classifier mis-categorises a <code>rule_addition</code> as <code>cosmetic</code> and the diff lands automatically. Mitigation: the success-metrics %-reverted-within-24h target (&lt; 5%) is monitored; if breached, the classifier is downgraded to \"always queue\" until the regression is found. (b) <strong>Test-gate flake</strong> — TRIGGER_TESTS sporadically fails post-bump and the apply aborts; the operator sees <code>cuo.proposal_apply_failed</code> rows and can manually re-apply. (c) <strong>Concurrent apply</strong> — two operators apply different proposals to the same skill simultaneously; the version_bump is keyed on the SKILL.md's current <code>metadata.version</code> string, so the second apply fails its byte-for-byte precondition check (analogous to TASK-MEMORY-118's <code>put_if</code>). (d) <strong>Partial CHANGELOG</strong> — if the apply succeeds but CHANGELOG.md write fails, the audit row is emitted but no human-readable trace; mitigation: idempotent <code>cyberos-cuo proposal verify &lt;stripe&gt;</code> reconciles.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Anthropic Claude.</li><li><strong>Scope:</strong> §1 normative clauses, §2 bump-level table, §4 ACs, §5 named test entries, alternatives, AI Risk Assessment.</li><li><strong>Human review:</strong> Stephen Cheng reviewed; the safety-class never-auto rule (§1 #10) is explicitly operator-mandated.</li></ul>\n<h2>§4 Acceptance Criteria</h2>\n<ol><li>A proposal classified <code>cosmetic</code> against a skill with <code>on_minor_bump: false</code> auto-applies; SKILL.md <code>metadata.version</code> patch increments by 1. <em>(traces_to: §1 #3, #4)</em></li><li>A proposal classified <code>rule_addition</code> with <code>on_rubric_rule_added: true</code> moves to <code>pending_approval/</code>; SKILL.md is unchanged until <code>cyberos-cuo proposal approve</code> runs. <em>(traces_to: §1 #4, #7)</em></li><li>A proposal with <code>## Risk class: safety</code> in body NEVER auto-applies regardless of classifier bucket; goes straight to <code>pending_approval/</code>. <em>(traces_to: §1 #10)</em></li><li>Pre-apply test gate runs acceptance/TRIGGER_TESTS.md against the new skill version; if any TRIGGER_TEST fails, the apply aborts cleanly + emits <code>cuo.proposal_apply_failed</code>. <em>(traces_to: §1 #8)</em></li><li>Each auto-applied proposal emits exactly one <code>cuo.proposal_applied</code> aux row + appends one <code>CHANGELOG.md</code> entry. <em>(traces_to: §1 #6, #9)</em></li><li>Each queued proposal emits exactly one <code>cuo.proposal_queued</code> aux row. <em>(traces_to: §1 #6)</em></li><li><code>cyberos-cuo proposal classify &lt;id&gt;</code> is read-only — produces the classifier output WITHOUT mutating any file. <em>(traces_to: §1 #7)</em></li><li>Version-bump pathing: <code>cosmetic</code>/<code>wording_polish</code> → patch; <code>threshold_tune</code>/<code>rule_addition</code> → minor; <code>rule_removal</code>/<code>contract_field_change</code> → major. <em>(traces_to: §2)</em></li><li><code>cyberos-cuo proposal approve &lt;id&gt;</code> after a queue: the file moves from <code>pending_approval/</code> → <code>applied/</code> AND the diff applies; both happen in a single transaction (one fails → both revert). <em>(traces_to: §1 #7)</em></li><li>After an apply, running <code>cyberos-cuo proposal list --status applied</code> includes the stripe with its new SKILL.md version footer. <em>(traces_to: §1 #5)</em></li></ol>\n<h2>§5 Verification</h2>\n<ul><li><code>modules/cuo/tests/test_proposal_applier.py::test_cosmetic_auto_applies</code> (AC #1, #8)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_rule_addition_queues</code> (AC #2)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_safety_class_never_auto</code> (AC #3)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_test_gate_blocks_bad_apply</code> (AC #4)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_audit_rows_emitted</code> (AC #5, #6)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_classify_is_read_only</code> (AC #7)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_bump_levels</code> (AC #8)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_approve_transactional</code> (AC #9)</li><li><code>modules/cuo/tests/test_proposal_applier.py::test_post_apply_list</code> (AC #10)</li></ul>";
