window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-302"]="<h1>TASK-CUO-302: Fail-closed machine gates - RED when zero gate commands are configured</h1>\n<h2>Summary</h2>\n<p><code>run-gates.sh</code> is the machine-gate floor for every task lifecycle transition, and today it reports <code>GATES: GREEN</code> and exits 0 when not a single gate command is configured. This repo is itself the proof: autodetect returned <code>unknown</code> on the polyglot monorepo, all four floor commands are empty, and the flagship repo gates nothing while reporting green. This task makes the empty floor RED by default, adds an explicit acknowledged-empty escape hatch, teaches autodetect a monorepo fallback so this repo detects its own suite, and stops the <code>gates.env</code> header from inviting edits that the next reinstall wipes.</p>\n<h2>Problem</h2>\n<p>Three verified defects compound into audit finding C1 (a core safety promise is false):</p>\n<ol><li><strong>Fail-open floor.</strong> <code>gate()</code> treats an empty command as <code>SKIP</code> (<code>tools/install/gates/run-gates.sh:57</code>), and after the per-gate loop the all-empty case prints an advisory line (<code>GATES: floor only - nothing detected and no overrides</code>, line 75) and then falls through to <code>GATES: GREEN</code> with exit 0 (lines 77-79). Green-is-necessary doctrine (AGENT-ENTRY.md #3) assumes green <em>means something</em>; an all-skip green is vacuous, and both HITL acceptance gates downstream inherit that false confidence.</li><li><strong>Autodetect has no monorepo fallback.</strong> <code>install.sh</code> probes package.json scripts and per-ecosystem markers; on this polyglot repo it lands <code>ECOSYSTEM=unknown</code> and seeds nothing. The repo has an obvious canonical suite entrypoint (<code>scripts/tests/run_all.sh</code>, 42 suites) that autodetect never looks for.</li><li><strong>The header invites edits the machine wipes.</strong> The generated <code>gates.env</code> says <code>(edit freely)</code> (<code>install.sh:299</code>) while reinstall regenerates the file and moves the old one to a <code>.bak</code> (<code>install.sh:326</code> even says durable overrides belong in <code>config.yaml</code>). An operator followed the header's advice; the reinstall wiped a working <code>TEST_CMD</code> which now survives only in <code>gates.env.bak.1784761166</code>.</li></ol>\n<h2>Proposed Solution</h2>\n<p>Make the all-empty floor exit RED with a message that names the two real fixes (<code>.cyberos/config.yaml</code> <code>gates.*</code> keys, or re-running install after adding ecosystem markers) and the explicit escape hatch <code>CYBEROS_ALLOW_EMPTY_GATES=1</code> for repos that genuinely have nothing to run (docs-only repos). The escape hatch prints a loud <code>GATES: EMPTY-ACKNOWLEDGED</code> line so an acknowledged-empty run is never confusable with a green one. Teach the <code>install.sh</code> autodetect an ordered monorepo fallback probe - <code>scripts/tests/run_all.sh</code> first, then <code>Makefile</code> <code>test:</code> target - seeding <code>TEST_CMD</code> with provenance <code>SRC_TEST=\"fallback:&lt;probe&gt;\"</code> so <code>run-gates.sh</code>'s existing provenance line shows where the command came from. Reword the generated <code>gates.env</code> header from \"edit freely\" to \"machine-owned; regenerated on every install - durable overrides belong in .cyberos/config.yaml (gates.build/lint/test/coverage)\". Ship a CHANGELOG entry marking the RED-on-empty behavior as breaking for consumer repos that relied on floor-only green.</p>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Warn loudly but stay green on empty.</strong> Rejected: that is exactly today's behavior (line 75 already warns); the audit demonstrated the warning changes nothing because green is what the workflow reads.</li><li><strong>Put the acknowledged-empty flag in <code>.cyberos/config.yaml</code>.</strong> Rejected: a config key is set once and outlives its justification silently; an env var must be re-asserted per invocation (or visibly exported in CI), which keeps the acknowledgment honest. The config file remains the home for <em>commands</em>, not for permission to run none.</li><li><strong>Hard-require <code>.cyberos/config.yaml</code> gate keys everywhere.</strong> Rejected: breaks zero-config installs on the many repos where autodetect works today (node/python/go/rust/etc. per TASK-CUO-207); the floor should fail only when there is genuinely nothing to run.</li><li><strong>Autodetect fallback tries every shell file under <code>scripts/</code>.</strong> Rejected: guessing arbitrary scripts as test commands executes untrusted-shaped code on install; the fallback probes a closed, documented list of canonical entrypoints only (<code>scripts/tests/run_all.sh</code>, <code>Makefile</code> with a <code>test:</code> target).</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release, a scratch install onto a repo with no detectable ecosystem followed by <code>bash .cyberos/cuo/gates/run-gates.sh</code> exits non-zero with the RED-empty message, and this repo's own gates run exits 0 only because the monorepo fallback seeded <code>TEST_CMD=\"bash scripts/tests/run_all.sh\"</code>. Baseline today: both exit 0 with all-empty commands.</li><li>Guardrail: zero regressions in the existing gate behavior for configured repos - a repo with at least one configured command keeps today's exact PASS/FAIL/exit semantics (existing suites <code>test_gates_config.sh</code> class stay green).</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>tools/install/gates/run-gates.sh</code> (RED-on-empty + acknowledged-empty ack line), <code>tools/install/install.sh</code> (fallback autodetect probes + <code>gates.env</code> header rewording), <code>tools/install/README.md</code> (document the new failure mode + escape hatch), CHANGELOG entry, and a new test suite covering all three behaviors.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Preserving operator edits to <code>gates.env</code> across reinstall (the header fix makes the ownership honest; durable-override preservation across <em>uninstall</em> is TASK-IMP-129's scope).</li><li>Any change to the CAF/AWH optional gates (<code>CAF_ENABLED</code>/<code>AWH_ENABLED</code> semantics unchanged; their empty-command handling is unchanged because they are opt-in, not the floor).</li><li>The <code>cyberos doctor</code> gate wiring for memory-installed repos - that is TASK-MEMORY-303's scope.</li><li>CI wiring of the gate benchmark (G1) checker beyond this task's own test suite - the benchmark-gates program is TASK-IMP-140.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Builds directly on TASK-CUO-207 (done), which shipped the <code>.cyberos/config.yaml</code> gates layer and the per-gate autodetect provenance (<code>SRC_*</code>) this task extends with a <code>fallback:</code> source tier. TASK-IMP-129 (draft) makes <code>config.yaml</code> survive uninstall - complementary, no ordering constraint. TASK-IMP-140's benchmark gate G1 (\"Gate-floor non-vacuous\") is <em>verified by</em> the test this task ships; listed in <code>related_tasks</code> as a soft forward reference, no cycle.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill in Cursor, as the task-authoring wave of the 2026-07-23 hardening plan.</li><li><strong>Scope:</strong> every <code>source_pages</code> line was read at HEAD in this checkout during authoring; the fail-open exit path, the all-empty local <code>gates.env</code>, the header/notice contradiction, and the <code>.bak</code>-only surviving <code>TEST_CMD</code> were verified first-hand, not carried from the audit report.</li><li><strong>Human review:</strong> the hardening plan (including this task's scope bullet) was operator-approved on 2026-07-23; the escape-hatch-as-env-var design call is recorded in <code>source_decisions</code> for the reviewer to revisit at the review acceptance gate.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>run-gates.sh</code> MUST exit with the distinct code 3 (RED, empty floor) when all four floor commands (build, lint, test, coverage) resolve to empty after the config.yaml and gates.env layers are applied, unless <code>CYBEROS_ALLOW_EMPTY_GATES</code> is set to the literal value <code>1</code>. Exit 3 is deliberately distinct from 1 (a configured gate failed) and 2 (missing/malformed config), so automation can tell \"gates ran and failed\" from \"nothing was configured\". Any value other than the literal <code>1</code> (including <code>true</code>, <code>yes</code>, <code>0</code>) MUST behave as unset. The existing per-gate <code>SKIP</code> behavior for a <em>subset</em> of empty gates is unchanged - the floor fails only when it is entirely vacuous.</li><li>1.2 The RED-on-empty message MUST name both durable fixes (set <code>gates.build/lint/test/coverage</code> in <code>.cyberos/config.yaml</code>, or re-run install so autodetect can seed commands) and the escape hatch by exact name. A failure an operator cannot act on from the message alone is a support ticket, not a gate.</li><li>1.3 When <code>CYBEROS_ALLOW_EMPTY_GATES=1</code> is set and the floor is empty, <code>run-gates.sh</code> MUST print a distinct <code>GATES: EMPTY-ACKNOWLEDGED</code> line (not <code>GATES: GREEN</code>) before exiting 0, so logs can never conflate an acknowledged-empty run with a green run.</li><li>1.4 <code>install.sh</code> autodetect MUST gain a monorepo fallback tier: when no ecosystem probe seeds a test command, probe an ordered, closed list - <code>scripts/tests/run_all.sh</code> (seed <code>TEST_CMD=\"bash scripts/tests/run_all.sh\"</code>), then a <code>Makefile</code> containing a <code>test:</code> target (seed <code>TEST_CMD=\"make test\"</code>) - recording provenance <code>SRC_TEST=\"fallback:run_all\"</code> or <code>SRC_TEST=\"fallback:make\"</code> respectively. The fallback MUST NOT execute the probed files at install time.</li><li>1.5 The generated <code>gates.env</code> header MUST NOT say \"edit freely\"; it MUST state that the file is machine-owned and regenerated on every install, and that durable overrides belong in <code>.cyberos/config.yaml</code> (<code>gates.*</code> keys). The wording change applies to the generator in <code>install.sh</code>; installed copies pick it up on next install.</li><li>1.6 <code>CHANGELOG.md</code> MUST gain an entry documenting RED-on-empty as a breaking behavior change for consumer repos, naming <code>CYBEROS_ALLOW_EMPTY_GATES=1</code> as the migration path for intentionally gate-less repos.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - on a scratch install with an all-empty floor, <code>run-gates.sh</code> exits exactly 3; with any one command configured it keeps today's semantics (0 on pass, 1 on gate failure); <code>CYBEROS_ALLOW_EMPTY_GATES=true</code> and <code>=0</code> still exit 3 - test: <code>tools/install/tests/test_fail_closed_gates.sh::t01_empty_floor_exits_red</code></li><li>[ ] AC 2 (traces_to: #1.2) - the RED output names <code>.cyberos/config.yaml</code>, re-install, and <code>CYBEROS_ALLOW_EMPTY_GATES</code> all three, asserted as substrings - test: <code>tools/install/tests/test_fail_closed_gates.sh::t02_red_message_actionable</code></li><li>[ ] AC 3 (traces_to: #1.3) - with <code>CYBEROS_ALLOW_EMPTY_GATES=1</code> and empty floor: exit 0, output contains <code>GATES: EMPTY-ACKNOWLEDGED</code>, and does NOT contain <code>GATES: GREEN</code> - test: <code>tools/install/tests/test_fail_closed_gates.sh::t03_ack_line_distinct</code></li><li>[ ] AC 4 (traces_to: #1.4) - installing onto a fixture repo that has <code>scripts/tests/run_all.sh</code> but no detectable ecosystem seeds <code>TEST_CMD=\"bash scripts/tests/run_all.sh\"</code> with <code>SRC_TEST=\"fallback:run_all\"</code>; the Makefile probe seeds <code>make test</code> on a Makefile-only fixture; install runs neither probe target - test: <code>tools/install/tests/test_fail_closed_gates.sh::t04_monorepo_fallback_seeds_test_cmd</code></li><li>[ ] AC 5 (traces_to: #1.5) - the generated <code>gates.env</code> on a scratch install contains no <code>edit freely</code> substring and does contain the machine-owned + config.yaml wording - test: <code>tools/install/tests/test_fail_closed_gates.sh::t05_header_machine_owned</code></li><li>[ ] AC 6 (traces_to: #1.6) - <code>CHANGELOG.md</code>'s top entry mentions the RED-on-empty change, the word \"breaking\", and <code>CYBEROS_ALLOW_EMPTY_GATES</code> - test: <code>tools/install/tests/test_fail_closed_gates.sh::t06_changelog_breaking_entry</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li><strong>CAF/AWH enabled, floor empty:</strong> an operator with <code>CAF_ENABLED=true</code> but an empty floor still fails 1.1 - the optional gates are additive, not a substitute for the floor. The RED message applies unchanged; acknowledging empty while running CAF-only is expressible via the env var and is the operator's explicit call.</li><li><strong>Malformed <code>.cyberos/config.yaml</code>:</strong> already exits 2 before any gate runs (<code>run-gates.sh</code> MALFORMED guard); this task's empty-floor check MUST evaluate only after the config layer parsed, so the two failure modes keep distinct exit paths and messages.</li><li><strong>Docs-only consumer repo:</strong> legitimately has nothing to run; the documented path is exporting <code>CYBEROS_ALLOW_EMPTY_GATES=1</code> (e.g. in the repo's CI env). The ack line keeps the honesty visible in every log.</li><li><strong><code>gates.env</code> predating this change:</strong> an installed repo that upgrades its payload but has not re-run install still has the old header text; the RED-on-empty behavior comes from the vendored <code>run-gates.sh</code> (rebuilt payload) regardless, so enforcement does not wait on the header fix. Stale-header-only trees are acceptable; behavior is not.</li><li><strong>Fallback probe on a repo with BOTH <code>run_all.sh</code> and a Makefile:</strong> the ordered list wins - <code>run_all.sh</code> seeds and the Makefile is not consulted; a fixture asserts the precedence so the order is contractual, not incidental.</li><li><strong>Security-class:</strong> the fallback seeds a command <em>string</em> into <code>gates.env</code> but never executes it at install time (1.4); execution happens only inside <code>run-gates.sh</code> under the operator's existing <code>eval</code> trust model for gate commands. No new execution surface is introduced at install time.</li></ul>";
