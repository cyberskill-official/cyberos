window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CHAT-268"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<ol><li>The chat service <strong>MUST</strong> expose <code>POST /v1/chat/blocks</code> (block a person), <code>DELETE /v1/chat/blocks/:subject_id</code> (unblock), and <code>GET /v1/chat/blocks</code> (the caller's own block list). All three are scoped to the calling subject: a block belongs to the person who created it and nobody else can read, create, or remove it.</li></ol>\n<ol><li>A block <strong>MUST</strong> be one-directional and private. It records \"A no longer wishes to receive B's content\". It <strong>MUST NOT</strong> notify B, appear in any list B can read, or change anything B observes about their own account. B continues to see the channel, continues to see their own messages in it, and is told nothing.</li></ol>\n<ol><li>A subject <strong>MUST NOT</strong> be able to block themselves. The service returns <code>400</code>.</li></ol>\n<ol><li>Blocking <strong>MUST</strong> be enforced on the server, at every one of these four fan-out points, and <strong>MUST NOT</strong> rely on the client filtering anything:</li></ol>\n<ul><li>the message-list query,</li><li>the realtime (WebSocket) fan-out,</li><li>the notification and push fan-out,</li><li>the direct-message list. A block implemented only in the client is not a block; it is a CSS rule that anyone can open the network tab and defeat, and it fails the store policy it exists to satisfy.</li></ul>\n<ol><li>In a <strong>shared group channel</strong>, a message from a blocked person <strong>MUST</strong> be returned to the blocker with <code>body</code>, <code>attachments</code>, and <code>reactions</code> withheld, and a <code>blocked_sender: true</code> flag set. The message's position in the channel and its id <strong>MUST</strong> be preserved. The client renders a collapsed placeholder with an explicit \"show anyway\" affordance.</li></ol>\n<ol><li>In a <strong>direct message channel</strong> between the blocker and the blocked person, messages from the blocked person <strong>MUST NOT</strong> be returned to the blocker at all - not collapsed, not flagged, not present. The DM <strong>MUST</strong> disappear from the blocker's DM list while the block stands.</li></ol>\n<ol><li>The blocked person <strong>MUST</strong> still be able to post. Their message is persisted normally and they see it in their own client. It is simply never delivered to the blocker: no row in the blocker's message list, no WebSocket frame, no notification, no push. The service <strong>MUST NOT</strong> return an error to the blocked sender.</li></ol>\n<ol><li>Consequently, the service <strong>MUST NOT</strong> disclose the existence of a block to the blocked person through <em>any</em> channel: not a status code, not an error string, not a missing-read-receipt, not a delivery indicator. See §2 for why this is the single most important security property of this task.</li></ol>\n<ol><li>Reactions and mentions authored by a blocked person <strong>MUST</strong> be suppressed for the blocker: their reaction is not counted in the folded reaction set the blocker receives, and their <code>@mention</code> of the blocker raises no notification.</li></ol>\n<ol><li>Blocking <strong>MUST NOT</strong> remove either party from any channel, and <strong>MUST NOT</strong> be visible to other members of that channel. Channel membership and moderation are the administrator's business (TASK-CHAT-269); blocking is the individual's.</li></ol>\n<ol><li>Unblocking <strong>MUST</strong> restore everything immediately and completely. Messages the blocked person sent during the block become visible in place, in their original position, on the blocker's next fetch. Nothing is lost and nothing is back-dated.</li></ol>\n<ol><li>Blocks <strong>MUST NOT</strong> apply to the moderation queue. When an administrator reviews a report in TASK-CHAT-269, they see the reported content in full regardless of any block either party holds. An administrator who has blocked someone must still be able to adjudicate a report about them.</li></ol>\n<ol><li>Blocks <strong>MUST</strong> be tenant-scoped under row-level security with both <code>USING</code> and <code>WITH CHECK</code>, like every other chat table.</li></ol>\n<ol><li>Every block and unblock <strong>MUST</strong> emit exactly one audit row - <code>chat.subject_blocked</code> or <code>chat.subject_unblocked</code> - carrying the blocker as actor and the blocked subject id. The row <strong>MUST NOT</strong> be readable by the blocked person through any surface exposed to them.</li></ol>\n<ol><li>The web client <strong>MUST</strong> offer Block / Unblock in the member list and in the profile popover, and <strong>MUST</strong> confirm before applying it. Every string <strong>MUST</strong> ship in <code>en</code> and <code>vi</code>.</li></ol>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why the blocked person is never told (§1 #7, #8).</strong> This is the clause the task turns on, and it is counter-intuitive, so it is worth being blunt. The obvious design is to refuse the blocked person's message with a <code>403</code> - it is honest, it is simple, and it is dangerous. Telling a harasser \"you have been blocked\" is an escalation trigger: it converts a person who was being ignored into a person who knows they were rejected, and the documented pattern is that they escalate through another channel. Every mature messaging product - Signal, WhatsApp, iMessage - lets the blocked sender believe the message went out. So do we. The message is persisted, the sender sees it in their own client, and it simply never arrives. The blocker is protected, and the blocked person has nothing to react to.</p>\n<p><strong>Why group-channel messages are collapsed rather than deleted (§1 #5).</strong> Removing a blocked person's messages outright silently rewrites the channel's history for one participant: replies to a vanished message become nonsense, thread counts stop matching, and the blocker ends up more confused than protected. A collapsed placeholder preserves the shape of the conversation, tells the truth (\"someone you blocked said something here\"), and leaves the choice to reveal in the hands of the person who made the block. It is <em>their</em> block; they are allowed to un-hide their own view.</p>\n<p><strong>Why DM messages are removed entirely rather than collapsed (§1 #6).</strong> In a group channel, a collapsed row is context - it explains a gap in a conversation you are still part of. In a DM there is no conversation left to contextualise: a column of \"blocked message\" placeholders is not information, it is a drip-feed of the harassment you asked to stop. The whole DM leaves the list.</p>\n<p><strong>Why enforcement is at four points and not one (§1 #4).</strong> Because there are four ways content reaches a person, and filtering three of them is filtering none. It is entirely possible to hide the messages from the list and still push a phone notification carrying the blocked person's name and the first 80 characters of their message onto the blocker's lock screen. That is not a hypothetical - it is the default behaviour of the existing <code>notify.rs</code> fan-out, which selects channel members and does not know blocks exist. All four are named in §1 #4 and each has its own acceptance criterion, so a future contributor who adds a fifth fan-out path has a checklist telling them what they just broke.</p>\n<p><strong>Why blocks do not apply to the moderation queue (§1 #12).</strong> The likeliest reporter of a person is the same person who blocked them. If the block also hid the content from the moderation queue, the administrator - who may well be that same person in a small workspace - would be adjudicating a report they cannot read. The queue is a distinct surface with a distinct purpose and it renders raw.</p>\n<p><strong>Why blocking does not remove anyone from a channel (§1 #10).</strong> Because that is a moderation action with consequences for everyone else in the channel, and one member should not be able to trigger it unilaterally. Blocking changes what <em>you</em> see. Removal changes what <em>everyone</em> sees, and belongs to the administrator.</p>\n<h2>§3 - API contract</h2>\n<h3>Migration</h3>\n<pre><code class=\"language-sql\">-- services/chat/migrations/0014_chat_blocks.sql\n-- TASK-CHAT-268: one row per (blocker, blocked) pair. Directional: A blocking B says nothing about\n-- whether B blocks A. Private: only the blocker can ever read their own rows.\n\nCREATE TABLE IF NOT EXISTS chat_blocks (\n    tenant_id           UUID NOT NULL,\n    blocker_subject_id  UUID NOT NULL,\n    blocked_subject_id  UUID NOT NULL,\n    created_at          TIMESTAMPTZ NOT NULL DEFAULT now(),\n\n    PRIMARY KEY (blocker_subject_id, blocked_subject_id),\n    CONSTRAINT chat_blocks_not_self CHECK (blocker_subject_id &lt;&gt; blocked_subject_id)\n);\n\n-- The hot path is \"give me every subject THIS caller has blocked\", run once per message-list,\n-- realtime fan-out, and notification fan-out. The PK already serves it (blocker is the leading\n-- column); this index serves the reverse question the notification fan-out asks: \"of the members\n-- about to be notified, which of them have blocked the sender?\"\nCREATE INDEX IF NOT EXISTS chat_blocks_blocked_idx\n    ON chat_blocks (blocked_subject_id, blocker_subject_id);\n\nALTER TABLE chat_blocks ENABLE ROW LEVEL SECURITY;\nALTER TABLE chat_blocks FORCE  ROW LEVEL SECURITY;\nCREATE POLICY chat_blocks_tenant_scoped ON chat_blocks\n    FOR ALL\n    USING (\n        tenant_id::text = current_setting('app.current_tenant_id', true)\n        OR current_setting('app.current_tenant_id', true) = '00000000-0000-0000-0000-000000000000'\n    )\n    WITH CHECK (\n        tenant_id::text = current_setting('app.current_tenant_id', true)\n        OR current_setting('app.current_tenant_id', true) = '00000000-0000-0000-0000-000000000000'\n    );\n\nGRANT SELECT, INSERT, DELETE ON chat_blocks TO cyberos_app;\nGRANT SELECT ON chat_blocks TO cyberos_ro;</code></pre>\n<h3>Endpoints</h3>\n<pre><code>POST   /v1/chat/blocks               { \"subject_id\": \"&lt;uuid&gt;\" }   -&gt; 204 (idempotent)\nDELETE /v1/chat/blocks/:subject_id                                -&gt; 204 (idempotent)\nGET    /v1/chat/blocks                                            -&gt; 200 [ { subject_id, created_at } ]\n\n400  \"cannot block yourself\"\n401  token invalid</code></pre>\n<p>Both mutations are idempotent by design: blocking someone twice is a no-op, and unblocking someone you never blocked is a no-op. Neither returns <code>404</code> or <code>409</code>, because a distinguishable response would let a caller enumerate their own block state through side effects - and, worse, invites a client to render an error for a state the user does not care about.</p>\n<h3>The blocked-set, and where it is applied</h3>\n<pre><code class=\"language-rust\">// services/chat/src/blocks.rs\n\n/// Every subject the caller has blocked. Read ONCE per request and threaded through the four\n/// enforcement points (§1 #4). A HashSet, not a per-message query: the message list is the hot\n/// path and N+1 here would be the most expensive thing in the service.\npub async fn blocked_by(\n    tx: &amp;mut sqlx::PgConnection,\n    blocker: Uuid,\n) -&gt; Result&lt;HashSet&lt;Uuid&gt;, (StatusCode, String)&gt; {\n    let rows: Vec&lt;(Uuid,)&gt; = sqlx::query_as(\n        \"SELECT blocked_subject_id FROM chat_blocks WHERE blocker_subject_id = $1\")\n        .bind(blocker).fetch_all(&amp;mut *tx).await.map_err(db::internal)?;\n    Ok(rows.into_iter().map(|(id,)| id).collect())\n}\n\n/// The reverse question, asked by the notification fan-out: of these recipients, which have\n/// blocked this sender? Returns the recipients to SKIP.\npub async fn blockers_of(\n    tx: &amp;mut sqlx::PgConnection,\n    sender: Uuid,\n    candidates: &amp;[Uuid],\n) -&gt; Result&lt;HashSet&lt;Uuid&gt;, (StatusCode, String)&gt; {\n    let rows: Vec&lt;(Uuid,)&gt; = sqlx::query_as(\n        \"SELECT blocker_subject_id FROM chat_blocks\n          WHERE blocked_subject_id = $1 AND blocker_subject_id = ANY($2)\")\n        .bind(sender).bind(candidates).fetch_all(&amp;mut *tx).await.map_err(db::internal)?;\n    Ok(rows.into_iter().map(|(id,)| id).collect())\n}</code></pre>\n<h3>Enforcement point 1 - the message list</h3>\n<pre><code class=\"language-rust\">// services/chat/src/messages.rs :: list\n\nlet blocked = blocks::blocked_by(&amp;mut tx, caller).await?;\nlet is_dm = channel.kind == \"direct\";\n\nlet messages: Vec&lt;Message&gt; = rows\n    .into_iter()\n    // §1 #6 - in a DM, a blocked sender's messages are not returned at all.\n    .filter(|m| !(is_dm &amp;&amp; blocked.contains(&amp;m.sender_subject_id)))\n    .map(|mut m| {\n        // §1 #5 - in a group channel, the row survives; its content does not.\n        if blocked.contains(&amp;m.sender_subject_id) {\n            m.body = String::new();\n            m.attachments = vec![];\n            m.reactions = vec![];\n            m.blocked_sender = true;\n        }\n        // §1 #9 - a blocked person's reaction is not counted for the blocker.\n        m.reactions.retain(|r| !r.reactor_ids.iter().any(|id| blocked.contains(id)));\n        m\n    })\n    .collect();</code></pre>\n<h3>Enforcement point 2 - the realtime fan-out</h3>\n<pre><code class=\"language-rust\">// services/chat/src/realtime.rs\n// The per-channel broadcast is one-to-many, so the block check CANNOT live at the send site: it\n// has to live at each subscriber. Each socket carries its own blocked-set, refreshed on block /\n// unblock (the mutation publishes an invalidation on the subscriber's own control channel).\n\nif sock.blocked.contains(&amp;frame.sender_subject_id) {\n    if sock.channel_kind == ChannelKind::Direct { continue; }        // §1 #6 - drop entirely\n    frame = frame.redacted();                                        // §1 #5 - collapse\n}</code></pre>\n<h3>Enforcement point 3 - the notification and push fan-out</h3>\n<pre><code class=\"language-rust\">// services/chat/src/notify.rs :: fan_out\n// This is the point that was silently broken before this task: the fan-out selects channel members\n// and pushes to their devices. It did not know blocks existed, so a blocked person's message would\n// still have arrived on the blocker's lock screen carrying their name and the first line of text.\n\nlet mut recipients = channel_members_except(&amp;mut tx, channel_id, sender).await?;\nlet blockers = blocks::blockers_of(&amp;mut tx, sender, &amp;recipients).await?;   // §1 #4, #9\nrecipients.retain(|r| !blockers.contains(r));</code></pre>\n<h3>Enforcement point 4 - the DM list</h3>\n<pre><code class=\"language-rust\">// services/chat/src/channels.rs :: list\n// §1 #6 - a DM with a blocked person leaves the blocker's list entirely while the block stands.\n\nlet blocked = blocks::blocked_by(&amp;mut tx, caller).await?;\ndms.retain(|dm| !blocked.contains(&amp;dm.partner_subject_id));</code></pre>\n<h3>Wire shape</h3>\n<pre><code class=\"language-rust\">#[derive(Serialize)]\npub struct Message {\n    // ... existing fields ...\n    /// True when the caller has blocked this message's sender AND this is a group channel.\n    /// The client renders a collapsed row with a \"show anyway\" affordance. Never true in a DM,\n    /// because a blocked sender's DM messages are not returned at all.\n    #[serde(default, skip_serializing_if = \"std::ops::Not::not\")]\n    pub blocked_sender: bool,\n}</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Block is directional and private</strong> - after A blocks B, <code>GET /v1/chat/blocks</code> as B returns an empty list, and nothing B can fetch differs from before the block.</li><li><strong>Self-block is refused</strong> - blocking your own subject id returns <code>400</code> and writes no row.</li><li><strong>Both mutations are idempotent</strong> - blocking twice returns <code>204</code> twice and leaves one row; unblocking someone never blocked returns <code>204</code> and changes nothing.</li><li><strong>Group-channel messages are collapsed, not removed</strong> - B's message in a shared channel is returned to A with <code>blocked_sender: true</code>, an empty <code>body</code>, no attachments, no reactions, and its original id and position.</li><li><strong>DM messages are removed entirely</strong> - B's messages in the A-B DM are absent from A's message list: not collapsed, not flagged, not present.</li><li><strong>The DM leaves A's list</strong> - the A-B DM does not appear in A's <code>GET /v1/chat/channels</code> while the block stands.</li><li><strong>The blocked sender is never told</strong> - B's <code>POST</code> of a message to the DM returns <code>201</code> exactly as before; B's own message list still contains it; no response field, status code, or timing differs from the unblocked case.</li><li><strong>The realtime socket drops it</strong> - with A's socket open on the shared channel, a message posted by B produces a redacted frame for A in a group channel and <strong>no frame at all</strong> in a DM.</li><li><strong>No notification, no push</strong> - a message from B, including one that <code>@mentions</code> A, produces zero notification rows and zero push sends targeting A's devices.</li><li><strong>Reactions from B are not counted</strong> - B reacting to a message A can see leaves A's folded reaction count unchanged.</li><li><strong>Unblock restores everything in place</strong> - after <code>DELETE</code>, A's next fetch returns B's messages from <em>during</em> the block with full bodies, in their original positions, and the DM reappears in A's list.</li><li><strong>The moderation queue is unaffected</strong> - an administrator who has blocked B still sees B's reported content in full in the TASK-CHAT-269 queue.</li><li><strong>Neither party leaves the channel</strong> - after A blocks B, both remain members and every other member's view is byte-identical to before.</li><li><strong>Cross-tenant isolation holds</strong> - a block row in tenant A is invisible to any query run with tenant B's GUC.</li><li><strong>Exactly one audit row per mutation</strong> - <code>chat.subject_blocked</code> / <code>chat.subject_unblocked</code>, actor = blocker; the idempotent no-op emits nothing.</li><li><strong>Both locales render</strong> - every block/unblock string resolves in <code>en</code> and <code>vi</code>.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-rust\">// services/chat/tests/blocks.rs\n\n#[tokio::test]\nasync fn group_message_is_collapsed_dm_message_is_gone() {          // AC 4, 5, 6\n    let app = harness().await;\n    let ch = app.channel(\"general\", &amp;[ALICE, BOB]).await;\n    let dm = app.dm(ALICE, BOB).await;\n    let gm = app.post_as(BOB, ch, \"group text\").await;\n    let dmm = app.post_as(BOB, dm, \"dm text\").await;\n\n    app.block_as(ALICE, BOB).await;\n\n    let group = app.list_as(ALICE, ch).await;\n    let m = group.iter().find(|m| m.id == gm).expect(\"row must survive\");\n    assert!(m.blocked_sender);\n    assert_eq!(m.body, \"\");\n    assert!(m.attachments.is_empty());\n\n    let direct = app.list_as(ALICE, dm).await;\n    assert!(direct.iter().all(|m| m.id != dmm), \"DM message must be absent, not collapsed\");\n\n    let channels = app.channels_as(ALICE).await;\n    assert!(!channels.iter().any(|c| c.id == dm), \"the DM must leave the list\");\n}\n\n#[tokio::test]\nasync fn the_blocked_sender_observes_nothing() {                    // AC 7 - the security property\n    let app = harness().await;\n    let dm = app.dm(ALICE, BOB).await;\n\n    let before = app.post_as_full(BOB, dm, \"one\").await;\n    app.block_as(ALICE, BOB).await;\n    let after = app.post_as_full(BOB, dm, \"two\").await;\n\n    assert_eq!(before.status(), after.status());                    // both 201\n    assert_eq!(before.json_shape(), after.json_shape());            // same fields, same types\n    // B still sees both of their own messages.\n    let bobs_view = app.list_as(BOB, dm).await;\n    assert_eq!(bobs_view.len(), 2);\n    // And nothing anywhere in B's world names the block.\n    assert!(!app.everything_visible_to(BOB).await.contains(\"block\"));\n}\n\n#[tokio::test]\nasync fn realtime_drops_in_dm_and_redacts_in_group() {              // AC 8\n    let app = harness().await;\n    let ch = app.channel(\"general\", &amp;[ALICE, BOB]).await;\n    let dm = app.dm(ALICE, BOB).await;\n    app.block_as(ALICE, BOB).await;\n\n    let mut sock = app.socket_as(ALICE).await;\n    app.post_as(BOB, ch, \"group\").await;\n    let frame = sock.next_frame().await.expect(\"a redacted frame arrives\");\n    assert!(frame.blocked_sender &amp;&amp; frame.body.is_empty());\n\n    app.post_as(BOB, dm, \"dm\").await;\n    assert!(sock.next_frame_timeout(Duration::from_millis(500)).await.is_none(),\n            \"a DM from a blocked sender must produce NO frame at all\");\n}\n\n#[tokio::test]\nasync fn no_notification_and_no_push_not_even_for_a_mention() {     // AC 9 - the point that was broken\n    let app = harness().await;\n    let ch = app.channel(\"general\", &amp;[ALICE, BOB]).await;\n    app.register_device(ALICE, \"android\", \"tok-a\").await;\n    app.block_as(ALICE, BOB).await;\n\n    app.post_as(BOB, ch, \"hey @alice look at this\").await;\n\n    assert_eq!(app.notifications_for(ALICE).await.len(), 0);\n    assert_eq!(app.pushes_to(\"tok-a\").await.len(), 0);\n}\n\n#[tokio::test]\nasync fn blocked_reactions_are_not_counted() {                      // AC 10\n    let app = harness().await;\n    let ch  = app.channel(\"general\", &amp;[ALICE, BOB, CAROL]).await;\n    let msg = app.post_as(CAROL, ch, \"hello\").await;\n    app.react_as(BOB, msg, \"+1\").await;\n    app.block_as(ALICE, BOB).await;\n\n    let seen = app.list_as(ALICE, ch).await;\n    let m = seen.iter().find(|m| m.id == msg).unwrap();\n    assert!(m.reactions.is_empty(), \"a blocked person's reaction must not be counted\");\n}\n\n#[tokio::test]\nasync fn unblock_restores_in_place() {                              // AC 11\n    let app = harness().await;\n    let ch = app.channel(\"general\", &amp;[ALICE, BOB]).await;\n    let a1 = app.post_as(ALICE, ch, \"first\").await;\n    app.block_as(ALICE, BOB).await;\n    let b1 = app.post_as(BOB, ch, \"during the block\").await;\n    let a2 = app.post_as(ALICE, ch, \"third\").await;\n    app.unblock_as(ALICE, BOB).await;\n\n    let seen = app.list_as(ALICE, ch).await;\n    assert_eq!(seen.iter().map(|m| m.id).collect::&lt;Vec&lt;_&gt;&gt;(), vec![a1, b1, a2]);   // in place\n    let m = seen.iter().find(|m| m.id == b1).unwrap();\n    assert_eq!(m.body, \"during the block\");                                        // in full\n    assert!(!m.blocked_sender);\n}\n\n#[tokio::test]\nasync fn moderation_queue_ignores_blocks() {                        // AC 12\n    let app = harness().await;\n    let ch  = app.channel(\"general\", &amp;[ADMIN, BOB]).await;\n    let msg = app.post_as(BOB, ch, \"reported text\").await;\n    app.report_as(ADMIN, json!({\"target_kind\":\"message\",\"target_message_id\":msg,\"reason\":\"hate\"})).await;\n    app.block_as(ADMIN, BOB).await;\n\n    let queue = app.moderation_queue_as(ADMIN).await;               // TASK-CHAT-269\n    assert_eq!(queue[0].snapshot_body, \"reported text\");            // full content, block or not\n}\n\n#[tokio::test]\nasync fn idempotent_mutations_and_self_block_guard() {              // AC 2, 3, 15\n    let app = harness().await;\n    assert_eq!(app.block_as_raw(ALICE, ALICE).await.status(), StatusCode::BAD_REQUEST);\n\n    assert_eq!(app.block_as_raw(ALICE, BOB).await.status(), StatusCode::NO_CONTENT);\n    assert_eq!(app.block_as_raw(ALICE, BOB).await.status(), StatusCode::NO_CONTENT);\n    assert_eq!(app.block_rows(ALICE).await.len(), 1);\n    assert_eq!(app.audit_rows(\"chat.subject_blocked\").await.len(), 1);   // no-op emits nothing\n\n    assert_eq!(app.unblock_as_raw(ALICE, CAROL).await.status(), StatusCode::NO_CONTENT);\n    assert_eq!(app.audit_rows(\"chat.subject_unblocked\").await.len(), 0);\n}</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>(The four enforcement points in §3 are the skeleton. What remains is socket-state invalidation, which is the only genuinely stateful part and the easiest thing to get wrong - see §10 row 5.)</p>\n<pre><code class=\"language-rust\">// services/chat/src/blocks.rs :: block\n// After committing the block, publish an invalidation to the BLOCKER's own control topic so every\n// socket they hold refreshes its blocked-set. Without this, an open tab keeps receiving the blocked\n// person's frames until it reconnects - which is a live, observable failure of §1 #4.\nstate.realtime.invalidate_blocks(blocker).await;</code></pre>\n<h2>§7 - Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none. <code>chat_channels.kind</code>, <code>chat_channel_members</code>, <code>chat_devices</code>, the realtime broadcast, and <code>notify.rs</code> all exist.</li><li><strong>Downstream:</strong> TASK-CHAT-269 must explicitly <em>not</em> apply <code>blocked_by</code> when rendering the moderation queue (§1 #12, AC #12).</li><li><strong>Sibling:</strong> TASK-CHAT-267 (reporting) is independent. They share only the member-list entry point.</li><li><strong>Cross-module:</strong> two new audit <code>event_type</code> values (<code>chat.subject_blocked</code>, <code>chat.subject_unblocked</code>). No schema change in the memory module.</li></ul>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">POST /v1/chat/blocks\n{ \"subject_id\": \"b7c8d9e0-1f2a-4b3c-8d4e-5f60718293a4\" }\n204 No Content</code></pre>\n<p>A group-channel message from a blocked sender, as returned to the blocker:</p>\n<pre><code class=\"language-json\">{\n  \"id\": \"6b1f4e0a-7c2d-4d19-9b0e-1f2a3c4d5e6f\",\n  \"channel_id\": \"3f5a1b2c-9d8e-4c7b-a6f5-e4d3c2b1a098\",\n  \"sender_subject_id\": \"b7c8d9e0-1f2a-4b3c-8d4e-5f60718293a4\",\n  \"body\": \"\",\n  \"attachments\": [],\n  \"reactions\": [],\n  \"blocked_sender\": true,\n  \"created_at\": \"2026-07-11T05:22:19Z\"\n}</code></pre>\n<p>The same message, as returned to <em>everyone else</em> in the channel - unchanged:</p>\n<pre><code class=\"language-json\">{\n  \"id\": \"6b1f4e0a-7c2d-4d19-9b0e-1f2a3c4d5e6f\",\n  \"body\": \"the actual text\",\n  \"attachments\": [ { \"id\": \"…\", \"filename\": \"shot.png\" } ],\n  \"reactions\": [ { \"emoji\": \"+1\", \"count\": 2 } ],\n  \"created_at\": \"2026-07-11T05:22:19Z\"\n}</code></pre>\n<p>The audit row:</p>\n<pre><code class=\"language-json\">{\n  \"event_type\": \"chat.subject_blocked\",\n  \"payload\": { \"blocked_subject_id\": \"b7c8d9e0-1f2a-4b3c-8d4e-5f60718293a4\" }\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<p><strong>Deferred:</strong></p>\n<ul><li><em>Workspace-wide block (\"mute across every surface\")</em> - once CyberOS grows modules beyond chat, a block should plausibly hide the person from mentions in PROJ, from the memory search index, and so on. That is a cross-module contract and belongs in its own task. Chat-scoped is what the store policy requires and what ships here.</li><li><em>Auto-expiring blocks</em> - \"block for 7 days\" is a real de-escalation tool. Deferred: <code>created_at</code> is present, an <code>expires_at</code> is additive.</li><li><em>Administrator visibility of blocks</em> - an admin arguably wants to know that six people have blocked the same person, because that is the strongest possible signal in a small workspace. Deliberately deferred, and it needs a decision, not a design: it partially breaks §1 #2 (privacy of the block), and the trade is real. Raised as a decision for TASK-CHAT-269's follow-up.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Block implemented only in the client</td><td>AC 4, 5, 8, 9 all assert on the <em>server</em> response</td><td>Content would still cross the wire and be visible in the network tab</td><td>Every enforcement point is server-side; the client never filters</td></tr><tr><td>Notification fan-out ignores blocks</td><td>AC 9 asserts zero pushes, including for an <code>@mention</code></td><td>Blocked person's name and text on the blocker's lock screen</td><td><code>blockers_of</code> is applied in <code>notify.rs</code> before recipient retention</td></tr><tr><td>Blocked sender receives a 403 and learns they are blocked</td><td>AC 7 asserts identical status and body shape before and after</td><td>Escalation to another channel; the exact harm the block exists to prevent</td><td>Silent-drop delivery model (§1 #7); no error path exists</td></tr><tr><td>Timing side-channel discloses the block</td><td>Delivery path length is the same either way (the message is written normally)</td><td>Blocked person cannot infer the block from latency</td><td>The block is applied at <em>read</em> fan-out, not at write</td></tr><tr><td>Open WebSocket keeps delivering after the block lands</td><td>AC 8 with a socket opened <em>before</em> the block</td><td>Blocker keeps seeing frames until they reconnect</td><td><code>invalidate_blocks</code> publishes to the blocker's control topic on mutation (§6)</td></tr><tr><td>Blocked-set fetched per message (N+1)</td><td>Message list is the hot path; one <code>HashSet</code> per request</td><td>Latency regression on every channel open</td><td><code>blocked_by</code> is read once per request and threaded through</td></tr><tr><td>Reaction from a blocked person leaks their existence via a count</td><td>AC 10</td><td>Blocker sees \"2 reactions\" where they should see 1</td><td>Reactions are filtered by reactor id, not just message sender</td></tr><tr><td>Blocker blocks themselves</td><td><code>chat_blocks_not_self</code> CHECK</td><td><code>400</code></td><td>Constraint is in the DB, not only the handler</td></tr><tr><td>Unblock loses messages sent during the block</td><td>AC 11 asserts exact ordering and full bodies</td><td>Silent data loss from the blocker's view</td><td>Nothing is deleted; the block filters reads only</td></tr><tr><td>Moderation queue applies the block and hides the evidence</td><td>AC 12</td><td>Admin cannot adjudicate a report about someone they blocked</td><td>TASK-CHAT-269 does not call <code>blocked_by</code></td></tr><tr><td>Block leaks to other channel members</td><td>AC 13 asserts byte-identical views for third parties</td><td>Social fallout inside a small workspace</td><td>The block is applied per-caller, never at write</td></tr><tr><td>Cross-tenant block row read</td><td>RLS <code>USING</code> + <code>WITH CHECK</code>, <code>FORCE</code>d; AC 14</td><td>Zero rows</td><td>Policy mirrors every other chat table</td></tr><tr><td>A fifth fan-out path is added later and forgets blocks</td><td>§1 #4 enumerates the four; each has an AC</td><td>A new leak</td><td>The enumeration is the checklist; a new path needs a new AC</td></tr><tr><td>DM channel accumulates undelivered messages during a long block</td><td>Storage only; the blocker never reads them</td><td>Unbounded-ish growth in a pathological case</td><td>Bounded by the existing per-message size cap and by retention (TASK-CHAT-240)</td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li><strong>Why <code>blocked_sender</code> is <code>skip_serializing_if</code> false rather than always present.</strong> The flag is absent from every message a normal reader sees, so the wire shape for the 99.99% case is unchanged and no existing client parser has to learn a new field. A client that does not know about blocking sees exactly what it saw before - an empty body - and degrades to rendering an empty row rather than crashing.</li></ul>\n<ul><li><strong>Why the block set is threaded rather than queried at each enforcement point.</strong> Three of the four points (list, realtime, notify) sit on the hot path. Reading the set once per request and passing a <code>HashSet&lt;Uuid&gt;</code> costs one index scan and a few hundred bytes. Querying per message would be an N+1 on the single most-called endpoint in the service.</li></ul>\n<ul><li><strong>The realtime invalidation is the one piece of genuinely mutable state.</strong> Each socket caches its owner's blocked-set at connect. A block placed in tab A must invalidate tab B's socket, or tab B keeps delivering. The mutation publishes to the <em>blocker's</em> control topic - not the channel's - because a block is nobody else's business.</li></ul>\n<ul><li><strong>Why <code>blockers_of</code> takes a candidate list rather than scanning.</strong> In a large channel the fan-out already knows exactly who it is about to notify. Asking \"of these 200 people, which have blocked this sender\" is one indexed query against <code>chat_blocks_blocked_idx</code>; asking \"who has blocked this sender\" globally would return a set we would then have to intersect anyway.</li></ul>\n<ul><li><strong>No <code>blocked_at</code> on the message.</strong> It is tempting to record when a message was hidden from whom, and it would be a small privacy disaster: it is a per-message log of one person's block state, sitting in a table the administrator can read.</li></ul>\n<p><em>End of TASK-CHAT-268.</em></p>";
