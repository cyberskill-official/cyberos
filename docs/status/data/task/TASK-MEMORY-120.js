window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MEMORY-120"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The history projection is a <strong>read-only</strong> view over the existing audit chain that surfaces per-file version + attribution. No new audit data is created. The contract:</p>\n<ol><li><strong>MUST</strong> be read-only. Every history operation reads existing audit-chain rows; no new put/move/delete/aux row is emitted by <code>cyberos history</code>. (DEC-260.)</li><li><strong>MUST</strong> support <code>cyberos history &lt;path&gt; [--limit &lt;N&gt;] [--chronological] [--json] [--no-follow-moves] [--show-body] [--since &lt;duration&gt;]</code>. Default: most-recent-first, 10 entries, no body bytes, follow moves.</li><li><strong>MUST</strong> return entries with at minimum these fields per row: ``<code>python @dataclass class HistoryEntry: seq:             int ts:              datetime         # UTC kind:            str              # \"put\" | \"move\" | \"delete\" | aux-row kinds actor:           str body_hash:       Optional[str]    # absent for non-mutating rows frontmatter_diff: Optional[dict]  # field-level diff vs previous version body_diff:       Optional[str]    # unified diff vs previous version (if --show-body) extra:           dict             # extra fields from the row (dream_id, session_id, etc.) </code>``</li><li><strong>MUST</strong> render the <code>extra</code> field's content inline in the human-formatted output, with these recognised annotation patterns:</li></ol>\n<ul><li><code>extra.dream_id</code> → \"via dream &lt;id&gt;\"</li><li><code>extra.proposal_id</code> → \"(proposal &lt;id&gt;)\"</li><li><code>extra.session_id</code> → \"during session &lt;id&gt;\"</li><li><code>extra.invocation</code> (e.g. \"consolidate\") → \"via &lt;invocation&gt;\"</li><li><code>extra.imported_from</code> → \"imported from &lt;fingerprint&gt;\"</li><li><code>extra.merged_into</code> → \"merged into &lt;path&gt;\" These annotations make the audit-chain richness operator-readable without requiring <code>--json</code> raw inspection.</li></ul>\n<ol><li><strong>MUST</strong> follow move chains by default. <code>cyberos history dst.md</code> walks back; when a <code>move(src, dst)</code> row is encountered, history continues walking under <code>src.md</code>'s prior history. Repeats for chained moves. <code>--no-follow-moves</code> cuts the walk at the move boundary.</li><li><strong>MUST</strong> support <code>--since 24h | 7d | 30d | &lt;ISO timestamp&gt;</code> to bound the walk. Default: no time bound (full chain history for that path).</li><li><strong>MUST</strong> compute per-version body diffs from adjacent <code>put</code> rows on the same path. The diff format default is <strong>unified-diff</strong> (operator-readable); <code>--json</code> returns structured <code>{added: [line, ...], removed: [line, ...], context: [...]}</code>. Frontmatter diffs are always structured (field-level adds / removes / changes).</li><li><strong>MUST</strong> include rows for ALL chain kinds that touch the path, not just <code>put/move/delete</code>. Specifically: <code>episode.logged</code> (TASK-MEMORY-112), <code>memory.importance_scored</code> (TASK-MEMORY-114), <code>dream.proposal_applied</code> (TASK-MEMORY-115), <code>memory.acl_denied</code> and <code>memory.precondition_failed</code> (TASK-MEMORY-117/118 attempt rows). These rows surface as \"annotation events\" — they appear in the history but don't trigger a body diff.</li><li><strong>MUST</strong> render tombstone correctly — a <code>delete(path, tombstone)</code> row appears as a history entry with <code>kind: \"delete\"</code> and <code>extra.mode: \"tombstone\"</code>; subsequent re-creation via <code>put</code> is the next entry. <code>delete(path, purge)</code> rows surface but with body redacted (per §3.6 the <em>fact</em> of purge is auditable; the body bytes are gone).</li><li><strong>MUST</strong> add a REST endpoint <code>GET /api/v2/memories/&lt;path&gt;/history</code> on <code>cyberos serve</code> that returns JSON. Same query parameters as CLI (<code>limit</code>, <code>chronological</code>, <code>follow_moves</code>, <code>since</code>, <code>show_body</code>).</li><li><strong>MUST</strong> complete <code>cyberos history &lt;path&gt;</code> in ≤ 200 ms p95 on a 100,000-row chain (chain-walk is O(N) but bounded by index; the existing <code>cyberos search</code> performance budget is the benchmark — history's projection should not be slower per-row than search).</li><li><strong>MUST</strong> support <code>cyberos history --all-paths [--limit 100]</code> for cluster-level view: emits a chronological/reverse list of every chain row touching any memory file. Useful for \"what's been happening lately?\" overview.</li><li><strong>MUST</strong> display correctly for paths that never existed — <code>cyberos history memories/never-was.md</code> returns empty list (not an error). The CLI prints \"No history for this path.\" <code>--json</code> returns <code>[]</code>.</li><li><strong>SHOULD</strong> support <code>cyberos history --filter actor=&lt;x&gt;</code> and <code>--filter session_id=&lt;y&gt;</code> for narrow operator queries. Slice-4 polish — basic implementation acceptable in slice-3, full grammar in slice-4.</li><li><strong>SHOULD</strong> support a <code>tail</code>-style mode <code>cyberos history --follow</code> that watches the chain for new rows touching the path and prints them live. Slice-4+ stretch.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why read-only (§1 #1, DEC-260).</strong> History is <em>querying</em> state, not changing it. Emitting audit rows for \"I looked at history\" creates audit-row noise without value — every CI run, every operator <code>--help</code>, every dashboard refresh would pollute the chain. The exception (per AGENTS.md §3.2) is that <code>view</code> MAY emit a row, but history doesn't trigger that either since it's a pure projection, not a memory file read.</p>\n<p><strong>Why follow moves by default (§1 #5, DEC-263).</strong> Operators thinking about <code>memories/facts/x.md</code> mentally include \"what this memory used to be called.\" If history stops at the move boundary, the operator has to remember the old path to see the prior history — defeats the purpose. Following moves transparently joins the chain so the operator sees the full history as one timeline. <code>--no-follow-moves</code> is the explicit \"I want just the post-move history\" escape.</p>\n<p><strong>Why render <code>extra</code> annotations inline (§1 #4).</strong> The audit chain accumulates rich provenance under <code>extra</code> (dream_id, session_id, invocation, imported_from, ...). If the human-format output is just <code>[seq | ts | kind | actor]</code> and the operator has to <code>--json</code> to see the extras, the rich data is hidden. Inline rendering makes it discoverable — \"ah, this row was written by dream X during session Y.\"</p>\n<p><strong>Why include non-mutation rows (§1 #8).</strong> A memory's history isn't just its writes; it's the events around it. <code>memory.importance_scored</code> happened at write time. <code>memory.acl_denied</code> records that someone TRIED to write but couldn't. <code>dream.proposal_applied</code> records a logical change even when no body bytes changed. Operators investigating \"what's happened to this memory?\" want all of these. The full event stream is the history.</p>\n<p><strong>Why unified-diff for body (§1 #7, DEC-262).</strong> Default to operator-readable. Most operators see history in a terminal; unified diff is what <code>git log -p</code> shows, which is the mental model. <code>--json</code> is for tooling. Structured frontmatter diff is always structured because frontmatter is structured data (field-level diff is more informative than line-level diff of YAML).</p>\n<p><strong>Why ≤ 200 ms p95 (§1 #11).</strong> History is an interactive command; operators run it in a tight loop while debugging. &gt; 200 ms feels laggy. The existing audit binlog reader does ~50 µs per row; 100K rows = ~5 ms in the best case. Allowing 200 ms is 40× headroom for projection / diff / formatting.</p>\n<p><strong>Why <code>--all-paths</code> cluster view (§1 #12).</strong> Common operator question: \"what's been happening in this memory over the last hour?\" Without <code>--all-paths</code>, the operator has to do per-path history or read the raw binlog. Cluster view is one command + filter.</p>\n<p><strong>Why no protocol amendment (§1 — implicit).</strong> This task adds zero new normative writer behaviour, no new audit kinds, no new schema fields. It's pure projection over existing data. AGENTS.md unchanged.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>History dataclass</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/history.py\nfrom __future__ import annotations\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Optional\n\n\n@dataclass\nclass HistoryEntry:\n    seq:             int\n    ts:              datetime\n    kind:            str\n    actor:           str\n    body_hash:       Optional[str]   = None\n    frontmatter_diff: Optional[dict]  = None\n    body_diff:       Optional[str]    = None\n    extra:           dict            = field(default_factory=dict)</code></pre>\n<h3>Walk function</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/history.py — continued\nfrom cyberos.core.reader import AuditChainReader\n\n\ndef walk(\n    store_path:      Path,\n    target_path:     str,\n    follow_moves:    bool = True,\n    since:           Optional[datetime] = None,\n    limit:           Optional[int] = None,\n    show_body:       bool = False,\n) -&gt; list[HistoryEntry]:\n    chain  = AuditChainReader(store_path)\n    paths  = {target_path}                            # accumulates moved-from paths if follow_moves\n    entries: list[HistoryEntry] = []\n    prev_bodies: dict[str, str] = {}                  # path → previous body text for diffing\n\n    for row in chain.iter_rows(reverse=True):\n        ts = datetime.fromisoformat(row[\"ts\"]) if isinstance(row[\"ts\"], str) else row[\"ts\"]\n        if since and ts &lt; since:\n            break\n        if not _touches_path(row, paths):\n            continue\n        entry = _row_to_entry(row, prev_bodies, show_body)\n        entries.append(entry)\n        if follow_moves and row[\"kind\"] == \"move\":\n            # The move's prior path is also part of the chain\n            paths.add(row[\"payload\"][\"src\"])\n        if limit and len(entries) &gt;= limit:\n            break\n\n    # We collected reverse-chronologically; default render order in entries is most-recent-first\n    return entries\n\n\ndef _touches_path(row: dict, paths: set[str]) -&gt; bool:\n    kind = row[\"kind\"]\n    payload = row.get(\"payload\", {})\n    if kind in (\"put\", \"delete\"):\n        return payload.get(\"path\") in paths\n    if kind == \"move\":\n        return payload.get(\"src\") in paths or payload.get(\"dst\") in paths\n    # aux kinds carrying a `path` field\n    return payload.get(\"path\") in paths</code></pre>\n<h3>CLI</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/cli/history.py\nimport argparse, json, sys\nfrom datetime import datetime, timedelta, timezone\nfrom pathlib import Path\n\nfrom cyberos.core.history import walk, HistoryEntry\n\n\ndef add_args(sub: argparse.ArgumentParser) -&gt; None:\n    sub.add_argument(\"path\", help=\"Memory path under &lt;memory-root&gt;/\")\n    sub.add_argument(\"--limit\",          type=int, default=10)\n    sub.add_argument(\"--chronological\",  action=\"store_true\")\n    sub.add_argument(\"--json\",           action=\"store_true\")\n    sub.add_argument(\"--no-follow-moves\", action=\"store_true\")\n    sub.add_argument(\"--show-body\",      action=\"store_true\")\n    sub.add_argument(\"--since\",          help=\"24h | 7d | 30d | ISO timestamp\")\n\n\ndef _parse_since(s: str) -&gt; datetime:\n    if s.endswith(\"h\"): return datetime.now(timezone.utc) - timedelta(hours=int(s[:-1]))\n    if s.endswith(\"d\"): return datetime.now(timezone.utc) - timedelta(days=int(s[:-1]))\n    return datetime.fromisoformat(s)\n\n\ndef run(store: Path, args: argparse.Namespace) -&gt; int:\n    since = _parse_since(args.since) if args.since else None\n    entries = walk(\n        store_path=store, target_path=args.path,\n        follow_moves=not args.no_follow_moves,\n        since=since, limit=args.limit, show_body=args.show_body,\n    )\n    if args.chronological:\n        entries.reverse()\n    if args.json:\n        print(json.dumps([_entry_to_json(e) for e in entries], default=str, indent=2))\n        return 0\n    if not entries:\n        print(f\"No history for {args.path!r}.\")\n        return 0\n    for e in entries:\n        _render_human(e, show_body=args.show_body)\n    return 0\n\n\ndef _render_human(e: HistoryEntry, show_body: bool) -&gt; None:\n    annotations = []\n    if \"dream_id\" in e.extra:    annotations.append(f\"via dream {e.extra['dream_id'][:8]}…\")\n    if \"proposal_id\" in e.extra: annotations.append(f\"(proposal {e.extra['proposal_id']})\")\n    if \"session_id\" in e.extra:  annotations.append(f\"during session {e.extra['session_id']}\")\n    if \"invocation\" in e.extra:  annotations.append(f\"via {e.extra['invocation']}\")\n    if \"imported_from\" in e.extra: annotations.append(f\"imported from {e.extra['imported_from']}\")\n    if \"merged_into\" in e.extra: annotations.append(f\"merged into {e.extra['merged_into']}\")\n    annot_str = \" \" + \" \".join(annotations) if annotations else \"\"\n    body_hash_short = (e.body_hash[:8] + \"…\") if e.body_hash else \"—\"\n    print(f\"[{e.seq:&gt;6}] {e.ts.isoformat()} {e.kind:&lt;28} {e.actor:&lt;16} body={body_hash_short}{annot_str}\")\n    if e.frontmatter_diff:\n        for op, fields in e.frontmatter_diff.items():\n            for field, val in fields.items():\n                marker = {\"added\": \"+\", \"removed\": \"-\", \"changed\": \"~\"}.get(op, \"?\")\n                print(f\"         {marker} {field}: {val!r}\")\n    if show_body and e.body_diff:\n        for line in e.body_diff.splitlines():\n            print(f\"         {line}\")</code></pre>\n<h3>REST endpoint</h3>\n<pre><code class=\"language-python\"># modules/memory/cyberos/core/serve.py — addition\n@app.get(\"/api/v2/memories/{path:path}/history\")\nasync def memory_history(\n    path:          str,\n    limit:         int  = 10,\n    chronological: bool = False,\n    follow_moves:  bool = True,\n    since:         Optional[str] = None,\n    show_body:     bool = False,\n):\n    since_dt = _parse_since(since) if since else None\n    entries = walk(\n        store_path=app.state.store_path, target_path=path,\n        follow_moves=follow_moves, since=since_dt, limit=limit, show_body=show_body,\n    )\n    if chronological:\n        entries.reverse()\n    return {\"path\": path, \"entries\": [_entry_to_json(e) for e in entries]}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Single-write memory</strong> — write once + history → 1 entry, kind=put, body_diff=None (no prior version). <em>(traces_to: §1 #2, §1 #3)</em></li><li><strong>Multi-write memory</strong> — write 3 times → 3 entries (most-recent-first); entries 1 and 2 have body_diff against the prior version; entry 3 has body_diff=None. <em>(traces_to: §1 #3, §1 #7)</em></li><li><strong><code>--chronological</code> reverses order</strong> — same fixture → 3 entries oldest-first. <em>(traces_to: §1 #2, DEC-261)</em></li><li><strong><code>--limit N</code> caps results</strong> — write 5 times → <code>--limit 2</code> returns 2 entries. <em>(traces_to: §1 #2)</em></li><li><strong><code>--since 24h</code> filters</strong> — write today + write 2 days ago + history --since 24h → only today's entry. <em>(traces_to: §1 #6)</em></li><li><strong><code>--show-body</code> includes unified diff</strong> — multi-version fixture → diff text present in output; format is unified-diff (headers like <code>--- a/...</code>, <code>+++ b/...</code>). <em>(traces_to: §1 #7, DEC-262)</em></li><li><strong><code>--json</code> returns structured</strong> — output parses as JSON array; each entry has the documented HistoryEntry fields. <em>(traces_to: §1 #2, §1 #7)</em></li><li><strong>Follow-moves on default</strong> — write to src.md, move src.md → dst.md, write to dst.md, history dst.md → 3 entries (covers both paths). <em>(traces_to: §1 #5, DEC-263)</em></li><li><strong><code>--no-follow-moves</code> cuts at boundary</strong> — same fixture + flag → only the post-move entries (2 of 3). <em>(traces_to: §1 #5)</em></li><li><strong>Dream-applied annotation rendered</strong> — fixture with a <code>delete</code> row carrying <code>extra.dream_id</code> → human output contains \"via dream &lt;id&gt;…\". <em>(traces_to: §1 #4)</em></li><li><strong>Session_id annotation rendered</strong> — fixture with put row carrying <code>extra.session_id</code> → output contains \"during session &lt;id&gt;\". <em>(traces_to: §1 #4)</em></li><li><strong>Importance_scored annotation surfaces</strong> — fixture with <code>memory.importance_scored</code> aux row for path → appears as history entry with kind == <code>memory.importance_scored</code>. <em>(traces_to: §1 #4, §1 #8)</em></li><li><strong>Tombstone renders correctly</strong> — write + delete(tombstone) → history has 2 entries; delete entry has <code>extra.mode: \"tombstone\"</code>. <em>(traces_to: §1 #9)</em></li><li><strong>Purged row body redacted</strong> — write + delete(purge) → delete entry present; body_hash present but body_diff is None (body bytes gone). <em>(traces_to: §1 #9)</em></li><li><strong>Frontmatter diff structured</strong> — write with <code>importance: 0.5</code>, then update to <code>importance: 0.8</code> → second entry's frontmatter_diff contains <code>{changed: {importance: {old: 0.5, new: 0.8}}}</code>. <em>(traces_to: §1 #7)</em></li><li><strong>Multi-kind events appear</strong> — fixture with put + episode.logged + memory.importance_scored + memory.acl_denied for same path → all 4 appear in history (sorted by seq descending). <em>(traces_to: §1 #8)</em></li><li><strong>REST endpoint matches CLI</strong> — <code>GET /api/v2/memories/&lt;path&gt;/history?limit=3</code> returns JSON with same shape as <code>cyberos history &lt;path&gt; --limit 3 --json</code>. <em>(traces_to: §1 #10)</em></li><li><strong>Latency p95 ≤ 200 ms on 100K rows</strong> — bench script measures CLI invocation on fixture. <em>(traces_to: §1 #11)</em></li><li><strong><code>--all-paths</code></strong> — option works; returns chronological list across all paths. <em>(traces_to: §1 #12)</em></li><li><strong>Path never existed</strong> — <code>cyberos history memories/never.md</code> returns empty list (CLI prints \"No history…\"; JSON returns <code>[]</code>). <em>(traces_to: §1 #13)</em></li><li><strong>Read-only — no new audit rows</strong> — head_seq before/after <code>cyberos history ...</code> is identical. <em>(traces_to: §1 #1, DEC-260)</em></li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_history.py\nimport json, pytest, subprocess\nfrom datetime import datetime, timezone\nfrom cyberos.core.history import walk\n\n\ndef test_single_write(seeded_memory):\n    \"\"\"AC #1\"\"\"\n    seeded_memory.put(\"memories/facts/single.md\", \"body v1\", meta={})\n    entries = walk(seeded_memory.store_path, \"memories/facts/single.md\")\n    assert len(entries) == 1\n    assert entries[0].kind == \"put\"\n    assert entries[0].body_diff is None\n\n\ndef test_multi_write_diffs(seeded_memory):\n    \"\"\"AC #2\"\"\"\n    seeded_memory.put(\"memories/facts/x.md\", \"v1\", meta={})\n    seeded_memory.put(\"memories/facts/x.md\", \"v2\", meta={})\n    seeded_memory.put(\"memories/facts/x.md\", \"v3\", meta={})\n    entries = walk(seeded_memory.store_path, \"memories/facts/x.md\")\n    assert len(entries) == 3\n    # Most-recent-first; entry 0 has no diff (it's the latest, no NEWER version to diff)\n    assert entries[2].body_diff is None\n    assert entries[1].body_diff is not None\n    assert entries[0].body_diff is not None\n\n\ndef test_chronological_reverses(seeded_memory):\n    \"\"\"AC #3\"\"\"\n    seeded_memory.put(\"memories/facts/x.md\", \"v1\", meta={})\n    seeded_memory.put(\"memories/facts/x.md\", \"v2\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/x.md\")\n    e_rev = list(reversed(e))\n    assert e[0].seq &gt; e[1].seq\n    assert e_rev[0].seq &lt; e_rev[1].seq\n\n\ndef test_limit_caps(seeded_memory):\n    \"\"\"AC #4\"\"\"\n    for i in range(5):\n        seeded_memory.put(\"memories/facts/lim.md\", f\"v{i}\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/lim.md\", limit=2)\n    assert len(e) == 2\n\n\ndef test_since_filters(seeded_memory, mock_time):\n    \"\"\"AC #5\"\"\"\n    mock_time.set(\"2026-05-17T00:00:00Z\")\n    seeded_memory.put(\"memories/facts/old.md\", \"old\", meta={})\n    mock_time.set(\"2026-05-19T00:00:00Z\")\n    seeded_memory.put(\"memories/facts/old.md\", \"new\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/old.md\",\n             since=datetime(2026, 5, 18, tzinfo=timezone.utc))\n    assert len(e) == 1\n    assert e[0].seq &gt; 1\n\n\ndef test_show_body_diff(seeded_memory):\n    \"\"\"AC #6\"\"\"\n    seeded_memory.put(\"memories/facts/d.md\", \"line a\\nline b\", meta={})\n    seeded_memory.put(\"memories/facts/d.md\", \"line a\\nline c\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/d.md\", show_body=True)\n    assert \"+line c\" in e[1].body_diff\n    assert \"-line b\" in e[1].body_diff\n\n\ndef test_json_output(seeded_memory, tmp_path):\n    \"\"\"AC #7\"\"\"\n    seeded_memory.put(\"memories/facts/j.md\", \"x\", meta={})\n    result = subprocess.run([\n        \"python\", \"-m\", \"cyberos\", \"--store\", str(seeded_memory.store_path),\n        \"history\", \"memories/facts/j.md\", \"--json\"\n    ], capture_output=True, text=True)\n    data = json.loads(result.stdout)\n    assert isinstance(data, list)\n    assert data[0][\"kind\"] == \"put\"\n\n\ndef test_dream_annotation(seeded_memory_with_dream_apply):\n    \"\"\"AC #10\"\"\"\n    e = walk(seeded_memory_with_dream_apply.store_path, \"memories/facts/dream-touched.md\")\n    assert any(\"dream_id\" in entry.extra for entry in e)\n\n\ndef test_session_annotation(seeded_memory_with_active_session):\n    \"\"\"AC #11\"\"\"\n    e = walk(seeded_memory_with_active_session.store_path, \"memories/facts/sess-touched.md\")\n    assert any(\"session_id\" in entry.extra for entry in e)\n\n\ndef test_importance_scored_appears(seeded_memory_with_scoring):\n    \"\"\"AC #12\"\"\"\n    e = walk(seeded_memory_with_scoring.store_path, \"memories/facts/scored.md\")\n    assert any(entry.kind == \"memory.importance_scored\" for entry in e)\n\n\ndef test_tombstone_render(seeded_memory):\n    \"\"\"AC #13\"\"\"\n    seeded_memory.put(\"memories/facts/t.md\", \"body\", meta={})\n    seeded_memory.delete(\"memories/facts/t.md\", mode=\"tombstone\")\n    e = walk(seeded_memory.store_path, \"memories/facts/t.md\")\n    assert any(entry.kind == \"delete\" and entry.extra.get(\"mode\") == \"tombstone\" for entry in e)\n\n\ndef test_purged_body_redacted(seeded_memory):\n    \"\"\"AC #14\"\"\"\n    seeded_memory.put(\"memories/facts/p.md\", \"secret\", meta={})\n    seeded_memory.delete(\"memories/facts/p.md\", mode=\"purge\",\n                         reason=\"GDPR Article 17 request\")\n    e = walk(seeded_memory.store_path, \"memories/facts/p.md\")\n    delete_entry = next(entry for entry in e if entry.kind == \"delete\")\n    assert delete_entry.body_diff is None         # body bytes gone\n\n\ndef test_frontmatter_diff_structured(seeded_memory):\n    \"\"\"AC #15\"\"\"\n    seeded_memory.put(\"memories/facts/fm.md\", \"body\", meta={\"importance\": 0.5})\n    seeded_memory.put(\"memories/facts/fm.md\", \"body\", meta={\"importance\": 0.8})\n    e = walk(seeded_memory.store_path, \"memories/facts/fm.md\")\n    diff = e[1].frontmatter_diff\n    assert diff[\"changed\"][\"importance\"][\"old\"] == 0.5\n    assert diff[\"changed\"][\"importance\"][\"new\"] == 0.8\n\n\ndef test_multi_kind_events(seeded_memory_with_all_kinds):\n    \"\"\"AC #16\"\"\"\n    e = walk(seeded_memory_with_all_kinds.store_path, \"memories/facts/multi.md\")\n    kinds = {entry.kind for entry in e}\n    expected = {\"put\", \"episode.logged\", \"memory.importance_scored\", \"memory.acl_denied\"}\n    assert expected.issubset(kinds)\n\n\ndef test_never_existed_path(empty_memory):\n    \"\"\"AC #20\"\"\"\n    e = walk(empty_memory.store_path, \"memories/facts/never.md\")\n    assert e == []\n\n\ndef test_read_only_no_new_rows(seeded_memory):\n    \"\"\"AC #21\"\"\"\n    head_before = seeded_memory.head_seq()\n    walk(seeded_memory.store_path, \"memories/facts/anything.md\")\n    assert seeded_memory.head_seq() == head_before\n\n\ndef test_rest_endpoint_matches_cli(running_cyberos_serve):\n    \"\"\"AC #17\"\"\"\n    import requests\n    cli_json = subprocess.run([\n        \"python\", \"-m\", \"cyberos\", \"--store\", str(running_cyberos_serve.store_path),\n        \"history\", \"memories/facts/x.md\", \"--limit\", \"3\", \"--json\"\n    ], capture_output=True, text=True).stdout\n    rest_json = requests.get(\n        f\"{running_cyberos_serve.url}/api/v2/memories/memories/facts/x.md/history?limit=3\"\n    ).text\n    assert json.loads(cli_json) == json.loads(rest_json)[\"entries\"]</code></pre>\n<pre><code class=\"language-python\"># modules/memory/tests/core/test_history.py\nimport pytest\nfrom cyberos.core.history import walk\n\n\ndef test_follow_moves_default(seeded_memory):\n    \"\"\"AC #8\"\"\"\n    seeded_memory.put(\"memories/facts/src.md\", \"v1\", meta={})\n    seeded_memory.move(\"memories/facts/src.md\", \"memories/facts/dst.md\")\n    seeded_memory.put(\"memories/facts/dst.md\", \"v2\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/dst.md\", follow_moves=True)\n    assert len(e) == 3                                      # put-src + move + put-dst\n\n\ndef test_no_follow_moves(seeded_memory):\n    \"\"\"AC #9\"\"\"\n    seeded_memory.put(\"memories/facts/src.md\", \"v1\", meta={})\n    seeded_memory.move(\"memories/facts/src.md\", \"memories/facts/dst.md\")\n    seeded_memory.put(\"memories/facts/dst.md\", \"v2\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/dst.md\", follow_moves=False)\n    # Only post-move events (the move row itself + the new put on dst)\n    assert all(\"src\" not in (entry.extra.get(\"from\", \"\") or \"\") for entry in e)\n\n\ndef test_chained_moves(seeded_memory):\n    \"\"\"Chained: a.md -&gt; b.md -&gt; c.md\"\"\"\n    seeded_memory.put(\"memories/facts/a.md\", \"v1\", meta={})\n    seeded_memory.move(\"memories/facts/a.md\", \"memories/facts/b.md\")\n    seeded_memory.move(\"memories/facts/b.md\", \"memories/facts/c.md\")\n    seeded_memory.put(\"memories/facts/c.md\", \"v2\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/c.md\", follow_moves=True)\n    # put on a + 2 moves + put on c = 4\n    assert len(e) == 4\n\n\ndef test_deleted_then_recreated(seeded_memory):\n    seeded_memory.put(\"memories/facts/r.md\", \"v1\", meta={})\n    seeded_memory.delete(\"memories/facts/r.md\", mode=\"tombstone\")\n    seeded_memory.put(\"memories/facts/r.md\", \"v2\", meta={})\n    e = walk(seeded_memory.store_path, \"memories/facts/r.md\")\n    assert len(e) == 3\n    kinds = [entry.kind for entry in e]\n    assert kinds == [\"put\", \"delete\", \"put\"][::-1]            # most-recent-first</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>API + tests above are the skeleton. Order:</p>\n<ol><li><code>cyberos/core/history.py</code> — <code>walk()</code> + <code>HistoryEntry</code>.</li><li>Diff computation helpers (unified diff for body, field-level for frontmatter).</li><li><code>cyberos/cli/history.py</code> — CLI subcommand.</li><li><code>__main__.py</code> — wire <code>cyberos history</code>.</li><li><code>cyberos/core/serve.py</code> — REST endpoint.</li><li>Tests + fixtures.</li><li>CHANGELOG.</li></ol>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-MEMORY-108 (related)</strong> — audit chain reader infrastructure is reused. History is a different projection over the same data.</li><li><strong>TASK-MEMORY-112 (related)</strong> — <code>episode.logged</code> aux rows are rendered as history entries.</li><li><strong>TASK-MEMORY-113 (related)</strong> — <code>meta.importance</code> field diffs surface in <code>frontmatter_diff</code>.</li><li><strong>TASK-MEMORY-114 (related)</strong> — <code>memory.importance_scored</code> rows are rendered as history entries.</li><li><strong>TASK-MEMORY-115 (related)</strong> — <code>dream.proposal_applied</code> rows surface; <code>extra.dream_id</code> annotation rendered.</li><li><strong>TASK-MEMORY-117 (related)</strong> — <code>memory.acl_denied</code> rows surface as attempt-history.</li><li><strong>TASK-MEMORY-118 (related)</strong> — <code>memory.precondition_failed</code> rows surface similarly.</li><li><strong>TASK-MEMORY-119 (related)</strong> — <code>extra.session_id</code> annotation rendered.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>CLI human output</h3>\n<pre><code class=\"language-text\">$ cyberos history memories/sre/dispatch-1.md --limit 6\n[  4319] 2026-05-19T08:00:42Z put                          stephen          body=f3a9b2c1…\n         ~ importance: 0.5 → 0.78\n         + last_verified_at: '2026-05-19T08:00:42Z'\n[  4316] 2026-05-19T07:30:00Z memory.importance_scored      claude-code-hook body=—\n[  4291] 2026-05-18T20:55:13Z put                          stephen          body=abc123de… during session sre-investigation-2026-05-19\n[  4275] 2026-05-18T20:45:00Z delete                       dream-applier    body=def456…  via dream 01HJ8XVK… (proposal P3FQ8K2X)\n[  4274] 2026-05-18T20:44:55Z dream.proposal_applied       dream-applier    body=—\n[  4112] 2026-05-15T14:20:00Z put                          stephen          body=def456…</code></pre>\n<h3>CLI --json output</h3>\n<pre><code class=\"language-json\">[\n  {\n    \"seq\": 4319,\n    \"ts\": \"2026-05-19T08:00:42Z\",\n    \"kind\": \"put\",\n    \"actor\": \"stephen\",\n    \"body_hash\": \"f3a9b2c1d4e5f6789a0b1c2d3e4f56789a0b1c2d3e4f56789a0b1c2d3e4f5678\",\n    \"frontmatter_diff\": {\n      \"changed\": {\"importance\": {\"old\": 0.5, \"new\": 0.78}},\n      \"added\":   {\"last_verified_at\": \"2026-05-19T08:00:42Z\"}\n    },\n    \"extra\": {\"actor\": \"stephen\"}\n  }\n]</code></pre>\n<h3>REST response</h3>\n<pre><code class=\"language-json\">GET /api/v2/memories/memories/sre/dispatch-1.md/history?limit=3\n\n{\n  \"path\": \"memories/sre/dispatch-1.md\",\n  \"entries\": [ ... ]\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Filter grammar <code>--filter actor=x</code> — §1 #14; slice 4 polish.</li><li><code>--follow</code> tail mode — §1 #15; slice 4+.</li><li>Two-way diff against arbitrary timestamps (<code>--from X --to Y</code>) — slice 4+.</li><li>Graph visualization of move chains — slice 4+ (could go on <code>cyberos serve</code> dashboard).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Path never existed</td><td>walk returns empty</td><td>CLI prints \"No history\"</td><td>None — by design</td></tr><tr><td>Path under symlink</td><td>resolver follows</td><td>works</td><td>None</td></tr><tr><td>Audit chain partial corruption</td><td>reader raises on bad frame</td><td>partial history rendered with WARN</td><td>Operator runs <code>cyberos doctor</code></td></tr><tr><td>Very long history (&gt;100K rows)</td><td><code>--limit</code> + lazy walk</td><td>bounded output</td><td>Operator uses <code>--limit</code> + <code>--since</code></td></tr><tr><td>Body too large for diff (&gt;1MB)</td><td>diff fallback to byte-level</td><td>\"binary file (use --no-show-body)\"</td><td>Operator skips --show-body</td></tr><tr><td>Frontmatter unparseable</td><td>diff fallback to \"frontmatter unparseable\"</td><td>history still renders</td><td>Operator inspects raw row via <code>cyberos read</code></td></tr><tr><td>Move chain with cycle</td><td>bounded by N-row walk; cycle caught</td><td>warning printed; first cycle break</td><td>walker <code>chain-no-cycles</code> already covers</td></tr><tr><td><code>extra</code> dict with arbitrary keys</td><td>renderer ignores unknown keys</td><td>works</td><td>None</td></tr><tr><td>Concurrent write during history</td><td>reader snapshots HEAD; consistent view</td><td>works</td><td>None</td></tr><tr><td><code>--since</code> future-dated</td><td>walk returns empty</td><td>\"No history\"</td><td>Operator fixes timestamp</td></tr><tr><td><code>--limit 0</code></td><td>edge case; returns empty</td><td>\"No history\"</td><td>Operator picks N ≥ 1</td></tr><tr><td>Tombstone followed by re-create</td><td>both entries rendered</td><td>works</td><td>None</td></tr><tr><td>Purge body redacted</td><td>body_diff = None</td><td>rendered correctly</td><td>None</td></tr><tr><td>REST endpoint with unknown <code>path</code></td><td>200 + <code>entries: []</code></td><td>empty array</td><td>None — by design</td></tr><tr><td>Slash-encoding in REST path</td><td>FastAPI <code>path:path</code> handles</td><td>works</td><td>None</td></tr><tr><td>Time zone mismatch</td><td>walker uses UTC; CLI prints ISO with offset</td><td>unambiguous</td><td>None</td></tr><tr><td>--json with --show-body on huge memory</td><td>bytes returned</td><td>works; user might want to omit --show-body</td><td>Operator inspects --json --no-show-body first</td></tr><tr><td><code>--all-paths</code> on massive memory</td><td>bounded by <code>--limit</code></td><td>works</td><td>Operator pages with <code>--since</code></td></tr><tr><td>Filter with unknown key</td><td>error in slice 4 surface (slice-3 ignores unknown filters)</td><td>works in slice-3</td><td>None until slice 4</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Walk is reverse-chronological by default</strong> because the most-recent-first ordering is what operators want; reversing once at the end is O(N) and cheap.</li><li><strong>Move-following is the natural way operators think about history.</strong> Counter-intuitively, the easier mental model is \"history of this name\" but operators thinking about content want \"history of this concept\" — which moves and re-renames are part of. Hence default = follow.</li><li><strong>Frontmatter diff</strong> uses dict-based field-level comparison; line-level diff would be misleading because YAML serialisation is non-canonical (key order, whitespace).</li><li><strong>Body diff via Python's <code>difflib.unified_diff</code></strong> — bounded, well-known format. JSON-mode delegates to the same engine but emits structured <code>{added: [], removed: [], context: []}</code> arrays.</li><li><strong>Purged rows surface but with redacted body</strong> — required by §3.6 (\"the fact of purge is itself a ledger leaf and is not erasable\"). History honours this.</li><li><strong><code>--all-paths</code></strong> uses the same <code>walk()</code> engine without the path filter — single-pass over the chain. The CLI applies path filtering downstream if needed.</li><li><strong>REST endpoint shares the CLI's <code>walk()</code> engine</strong> so behaviour is identical; AC #17 asserts this.</li><li><strong>The <code>_render_human</code> helper</strong> is intentionally simple; rich formatting (colour, alignment) lives in a separate <code>--pretty</code> flag for slice-4. Slice-3 ships terminal-readable plain text.</li></ul>\n<hr>\n<p><em>End of TASK-MEMORY-120.</em></p>";
