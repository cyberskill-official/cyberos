window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-101"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TEN service <strong>MUST</strong> ship the public self-serve signup flow at <code>services/ten/src/signup/</code> with email-OTP verification, slug uniqueness check, plan + currency selection, payment method capture, idempotent commit-or-rollback orchestration, OIDC alternate path, consent versioning, rate limiting + Turnstile, disposable-email blocklist, 30-second end-to-end SLO, and 9 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> expose the unauthenticated <code>POST /v1/signup/start</code> endpoint that accepts <code>{ email, signup_session_id (UUIDv7), turnstile_token, locale_hint }</code> and returns <code>{ signup_session_id, email_verification_required: bool, suggested_billing_currency, suggested_residency, geoip_country, otp_sent_at }</code>. The handler MUST in order: verify Turnstile token (per DEC-820), check rate limits (per DEC-837), check disposable-email blocklist (per DEC-830), GeoIP-lookup the request IP (per DEC-824), HMAC-hash + persist OTP in Redis with 10-min TTL (per DEC-821), trigger transactional email via TASK-EMAIL-001 with the OTP, INSERT a <code>signup_sessions</code> row, and emit <code>ten.signup_started</code> memory row.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>signup_sessions</code> table at migration <code>0011</code>: <code>(signup_session_id UUID PRIMARY KEY, email_hash16 TEXT NOT NULL, email_full TEXT NOT NULL, geoip_country CHAR(2), suggested_billing_currency billing_currency_enum, suggested_residency TEXT, state TEXT NOT NULL CHECK (state IN ('started','email_verified','payment_captured','provisioning','completed','abandoned','rolled_back')) DEFAULT 'started', started_at TIMESTAMPTZ NOT NULL DEFAULT now(), email_verified_at TIMESTAMPTZ, payment_captured_at TIMESTAMPTZ, provisioned_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, abandon_reason TEXT, rolled_back_reason TEXT, tenant_id UUID, scrubbed_at TIMESTAMPTZ)</code>. Expires (<code>signup_sessions.state</code> transitions to <code>abandoned</code> + <code>abandon_reason='ttl_expired'</code>) at 1h via scheduled job. PII (email_full, IP if stored) scrubbed at 90 days per DEC-840.</li></ol>\n<ol><li><strong>MUST</strong> define the <code>tenant_consents</code> table at migration <code>0012</code>: <code>(id BIGSERIAL PRIMARY KEY, tenant_id UUID, signup_session_id UUID, subject_id UUID NOT NULL, consent_kind TEXT NOT NULL CHECK (consent_kind IN ('tos','privacy','marketing','pdpl_vn_data_processing','pdpl_vn_data_export','gdpr_legitimate_interest')), version TEXT NOT NULL, accepted_at TIMESTAMPTZ NOT NULL DEFAULT now(), ip_addr_hash16 TEXT NOT NULL, locale CHAR(5) NOT NULL, withdrawn_at TIMESTAMPTZ)</code>. Append-only via REVOKE per task-audit skill rule 12; withdrawal recorded as new row with <code>withdrawn_at</code> populated AND a fresh <code>accepted_at=null</code> row is NOT created — withdrawal is a column-level update to the existing row (one of the rare permitted updates, gated via per-column GRANT to the <code>cyberos_consent_writer</code> role).</li></ol>\n<ol><li><strong>MUST</strong> define the <code>disposable_email_domains</code> table at migration <code>0014</code>: <code>(domain TEXT PRIMARY KEY, source TEXT NOT NULL, added_at TIMESTAMPTZ NOT NULL DEFAULT now(), removed_at TIMESTAMPTZ)</code>. Initial seed = 10,000-entry list from public sources (<code>disposable-email-domains</code> GitHub repo at pinned SHA); refresh job runs monthly + writes a <code>ten.disposable_email_blocklist_refreshed</code> memory row (kind not in the 9-kind core list per DEC-839 — informational only).</li></ol>\n<ol><li><strong>MUST</strong> enforce 4 rate limits per DEC-837 via Redis sliding-window:</li></ol>\n<ul><li><code>signup_start</code>: 10/min/IP, 3/h/IP, 100/d/IP.</li><li><code>otp_send</code>: 3/min/email, 10/h/email.</li><li><code>slug_check</code>: 50/min/IP (rapid autocomplete legit; &gt; 50 = bot).</li><li><code>signup_complete</code>: 1/min/IP (a human completes signup once per minute max).</li></ul>\n<p>Rate-limit hit returns <code>429 TOO_MANY_REQUESTS</code> + <code>{ error: \"rate_limited\", retry_after_seconds, guard }</code> + emits <code>ten.signup_rate_limited</code> memory row.</p>\n<ol><li><strong>MUST</strong> enforce the disposable-email blocklist at <code>signup_start</code> (per DEC-830). Lookup <code>domain = email.split('@')[1]</code>. Blocked → return <code>400 BAD_REQUEST</code> + <code>{ error: \"disposable_email\", suggested_action: \"use_work_email\" }</code> + emit <code>ten.signup_disposable_email_blocked</code> memory row. The blocklist is loaded into memory at handler startup; refreshed monthly.</li></ol>\n<ol><li><strong>MUST</strong> apply TASK-AUTH-107 HIBP breach-check on the email at <code>signup_start</code> (per DEC-831). Result is informational — <code>breached: true</code> is appended to the response as <code>{ ..., breach_warning: { breach_count: N, latest_breach: \"&lt;date&gt;\" } }</code>. The signup proceeds; the warning surfaces in the UI as \"We noticed your email appears in breaches; you may want to use a different email.\"</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/signup/verify-otp</code> that accepts <code>{ signup_session_id, otp_code }</code> and returns <code>{ verified: bool, session_state }</code>. Handler MUST: lookup the OTP from Redis (HMAC-hashed lookup), constant-time compare, increment attempt counter, reject if <code>attempts &gt; 5</code>, on success transition <code>signup_sessions.state = 'email_verified'</code> + emit <code>ten.signup_email_verified</code> memory row. After 5 failed attempts the OTP is invalidated; a new OTP send is required (incrementing <code>otp_send</code> rate-limit counter).</li></ol>\n<ol><li><strong>MUST</strong> expose <code>GET /v1/signup/slug-available?candidate=&lt;slug&gt;</code> that returns <code>{ available: bool, suggestions: [string; 50] }</code>. The handler validates regex <code>^[a-z][a-z0-9-]{2,40}[a-z0-9]$</code> (TASK-TEN-001 DEC-321), checks against the <code>tenants.slug</code> index, and on collision generates 50 suggestions via prefix-suffix permutation (<code>acme-co</code>, <code>acme-1</code>, <code>acme-hq</code>, etc.). The handler is rate-limited (per DEC-837 #3) and serves from a hot Postgres index (no caching — slug must be authoritative). For OIDC sign-up path, a slug suggestion is derived from the email domain (<code>alice@acme.com → acme</code>) and offered as default.</li></ol>\n<ol><li><strong>MUST</strong> support 2 plan tiers in self-serve per DEC-823: Starter (default) and Team. Enterprise self-serve is BLOCKED (return <code>403 FORBIDDEN</code> + <code>{ error: \"enterprise_requires_sales_contact\", contact_email: \"sales@cyberos.world\" }</code>). The UI hides the Enterprise option for self-serve; direct API attempts are rejected at <code>signup_complete</code>.</li></ol>\n<ol><li><strong>MUST</strong> derive <code>suggested_billing_currency</code> from GeoIP (per DEC-824) via MaxMind GeoLite2 database refreshed weekly: VN→VND, SG→SGD, EU 27→EUR, GB→GBP, default→USD. User MAY override via the form (e.g., a Singapore-based user wants USD billing for tax reasons). The derivation result is informational; the final <code>billing_currency</code> is captured at <code>signup_complete</code> from the request body, not from GeoIP.</li></ol>\n<ol><li><strong>MUST</strong> derive <code>residency</code> from <code>billing_currency</code> per DEC-825: VND→vn-1, SGD→sg-1, EUR→eu-1, GBP→eu-1, USD→us-1. Residency is immutable post-provisioning (TASK-TEN-103 derivative); TASK-TEN-001 carries this through to provisioning.</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/signup/payment-intent</code> that returns either:</li></ol>\n<ul><li>For stripe-rail (<code>billing_currency ∈ {USD, EUR, SGD, GBP}</code>): <code>{ stripe_client_secret: \"seti_xxx_secret_yyy\", publishable_key: \"pk_xxx\", payment_method_options: { card: { request_three_d_secure: \"automatic\" } } }</code>. The frontend uses Stripe Elements to capture card data + SetupIntent confirmation (no PAN at our backend per PCI SAQ-A).</li><li>For vietqr-rail (<code>billing_currency = VND</code>): <code>{ vnpay_redirect_url: \"https://...\", reference: \"...\" }</code> — actual VnPay integration ships in TASK-TEN-102 (placeholder; for slice 1 of TEN-101 the VND path returns <code>503 SERVICE_UNAVAILABLE</code> + <code>{ error: \"vnd_signup_requires_ten_102\" }</code> until TASK-TEN-102 lands).</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/signup/complete</code> — the commit-or-rollback orchestrator. Body: <code>{ signup_session_id, tenant_slug, tenant_display_name, plan_tier, billing_currency, billing_contact_email, stripe_setup_intent_id?, vnpay_reference?, consents: [{kind, version, locale}], turnstile_token }</code>. The handler runs in three phases — <strong>pre-tx validation</strong> (no DB writes), <strong>external Stripe call</strong> (no DB tx held), then a <strong>single Postgres transaction</strong> that atomically writes consents + tenant + root-admin + session-state. Handler MUST in strict order:</li></ol>\n<ul><li><strong>Phase A — pre-tx validation:</strong></li></ul>\n<ol><li>Re-verify Turnstile token; reject if missing/invalid.</li><li>Verify <code>signup_sessions.state == 'email_verified'</code>; else 409.</li><li>Validate plan_tier ∈ {Starter, Team}; reject Enterprise with 403.</li><li>Validate consents: ToS + Privacy MUST be present at the latest published versions; Marketing OPTIONAL; for VN tenants, <code>pdpl_vn_data_processing</code> MUST be present.</li><li>Check duplicate-email guard (per DEC-842): if <code>tenants</code> already has a row keyed by <code>billing_contact_email_hash16 = HMAC(global_salt, email_lower)</code> AND <code>status='active'</code>, return 409 + <code>{ error: \"email_already_associated\", magic_link_sent: true }</code> AND email a magic-link sign-in to the existing tenant; emit <code>ten.signup_abandoned</code> with reason='duplicate_email'. The hash16 index on <code>tenants.billing_contact_email_hash16</code> is created by this task's migration <code>0011</code> ALTER statement.</li></ol>\n<ul><li><strong>Phase B — external Stripe call (outside DB tx to avoid long-held connections):</strong></li></ul>\n<ol><li>For stripe-rail: confirm the SetupIntent via Stripe API (<code>POST /v1/setup_intents/{id}/confirm</code>); on failure, transition <code>signup_sessions.state='abandoned'</code> + <code>abandon_reason='payment_failed'</code> + return 402 PAYMENT_REQUIRED. The Stripe SetupIntent ID is recorded in <code>signup_sessions.payment_captured_at</code> BEFORE the DB tx opens so any subsequent failure can void it (DEC-843 derivative).</li></ol>\n<ul><li><strong>Phase C — atomic provisioning transaction:</strong></li></ul>\n<ol><li><code>BEGIN</code>; persist consent rows.</li><li>Invoke TASK-TEN-001 <code>provision_tenant(slug, currency, residency, billing_contact_email)</code> inside the tx; on collision (lost race on slug) <code>ROLLBACK</code> + void Stripe SetupIntent + return 409 + suggestions; transition <code>signup_sessions.state='abandoned'</code> reason='slug_collision'.</li><li>Create root-admin subject via TASK-AUTH-002 inside the same tx with auto-generated 24-char password (DEC-834) OR no password if OIDC path (DEC-835); on failure <code>ROLLBACK</code> + void Stripe SetupIntent (+ no tenant exists post-rollback — TASK-TEN-104 NOT invoked).</li><li>Back-fill <code>tenant_consents.tenant_id</code> + <code>subject_id</code>.</li><li><code>UPDATE signup_sessions SET state='completed', tenant_id=...</code> + <code>COMMIT</code>.</li></ol>\n<ul><li><strong>Phase D — post-commit side effects (failures here do not undo commit):</strong></li></ul>\n<ol><li>Invoke TASK-TEN-003 <code>ensure_customer</code> + <code>ensure_subscription(plan_tier)</code> with the captured payment method as default. On Stripe failure post-commit: log sev-1 + leave tenant in <code>dunning_state='retry_1'</code> (TASK-TEN-003 §1 #11 picks up the recovery).</li><li>Mint first-login JWT via TASK-AUTH-004 with <code>signup_session_id</code> claim + standard claims.</li><li>Set a Secure HttpOnly SameSite=Lax <code>cyberos_jwt</code> cookie scoped to <code>*.cyberos.world</code> so the redirect to <code>&lt;slug&gt;.cyberos.world/onboarding/welcome</code> arrives authenticated.</li><li>Emit <code>ten.signup_completed</code> memory row.</li><li>Trigger welcome email via <code>welcome_email.rs</code> with the 7d-TTL deeplink (DEC-845).</li><li>Return <code>201 CREATED</code> with <code>{ tenant_id, tenant_slug, jwt, redirect_url: \"https://&lt;slug&gt;.cyberos.world/onboarding/welcome\" }</code>.</li></ol>\n<ol><li><strong>MUST</strong> support the OIDC sign-up path (DEC-835). Flow:</li><li>UI offers \"Sign up with Google\" / \"Sign up with Microsoft\" buttons (delegating to TASK-AUTH-104 OIDC SSO).</li><li>Before redirecting to the IdP, the UI MUST call <code>POST /v1/signup/oidc-init</code> with <code>{ signup_session_id, idp: \"google\"|\"microsoft\" }</code>; the handler binds the session_id to a single-use OIDC <code>state</code> nonce stored server-side with 5-min TTL + emits the IdP authorize URL with that nonce.</li><li>OIDC return URL is <code>/v1/signup/oidc-callback</code>; the handler validates the <code>state</code> nonce against the server-stored binding (CSRF defense), extracts <code>email</code> + <code>email_verified</code> + <code>name</code> from the ID token.</li><li>If <code>email_verified=true</code> (Google/Microsoft IdPs both verify), skip the OTP step entirely — transition <code>signup_sessions.state</code> directly to <code>email_verified</code>.</li><li>Suggest tenant_slug from email domain.</li><li>Continue to plan selection + payment + complete (same orchestrator as #14 but with OIDC marker on the session row).</li><li>Root-admin subject is created without a password; subsequent logins are OIDC-only.</li><li>Emit <code>ten.signup_oidc_linked</code> memory row.</li></ol>\n<ol><li><strong>MUST</strong> persist consent rows BEFORE provisioning (per DEC-832, #14 step 4). The <code>tenant_consents</code> table is INSERT'd within the same transaction as the <code>tenants</code> row creation, ensuring no orphan tenant-without-consent state. Consent versions are pinned to the strings displayed to the user at signup-time — fetched from <code>services/ten/web/signup/consents/&lt;locale&gt;/&lt;kind&gt;-v&lt;n&gt;.md</code> static files.</li></ol>\n<ol><li><strong>MUST</strong> emit the 30-second SLI to OBS at <code>signup_completed</code> (per DEC-828). The OTel histogram metric <code>ten_signup_duration_seconds</code> is recorded with attributes <code>(plan_tier, billing_currency, oidc: bool, completed: bool)</code>. Alarm sev-2 when <code>p95 &gt; 30s sustained 15 min</code>; sev-1 when <code>p99 &gt; 60s sustained 5 min</code>.</li></ol>\n<ol><li><strong>MUST</strong> detect squatting per DEC-844. The <code>abuse_guard.rs</code> module tracks <code>(ip_addr, started_signups_24h, abandoned_signups_24h)</code> in Redis. When <code>abandoned_signups_24h ≥ 5 AND completed_signups_24h = 0</code>, block the IP for 24h (<code>signup_start</code> returns <code>429 + { error: \"abuse_detected_24h_cooloff\" }</code>). The block auto-expires; legitimate users from shared IPs (corp NAT) can request manual unblock via support email.</li></ol>\n<ol><li><strong>MUST</strong> emit 9 <strong>core</strong> memory audit row kinds tied to user-visible signup lifecycle (DEC-839 + task-audit skill rule 6 namespace pattern):</li></ol>\n<ul><li><code>ten.signup_started</code> (sev-3 informational)</li><li><code>ten.signup_email_verified</code> (sev-3)</li><li><code>ten.signup_consent_recorded</code> (sev-2 — legal)</li><li><code>ten.signup_tenant_provisioned</code> (sev-2 — material commercial event)</li><li><code>ten.signup_completed</code> (sev-2)</li><li><code>ten.signup_abandoned</code> (sev-3 — funnel analytics)</li><li><code>ten.signup_rate_limited</code> (sev-3 — security signal)</li><li><code>ten.signup_disposable_email_blocked</code> (sev-3)</li><li><code>ten.signup_oidc_linked</code> (sev-3)</li></ul>\n<p>    Plus <strong>2 supporting</strong> ops-only kinds (not in the core 9 because they're system-emitted, not user-action-triggered):</p>\n<ul><li><code>ten.disposable_email_blocklist_refreshed</code> (sev-3 — emitted by monthly refresh job; payload = <code>domains_added</code>, <code>domains_removed</code>, <code>source_sha</code>)</li><li><code>ten.signup_session_scrubbed</code> (sev-3 — emitted by daily 90d-PII-scrub job; payload = <code>scrubbed_count</code>)</li></ul>\n<p>    Every row PII-scrubs <code>email_full</code> via TASK-MEMORY-111 → <code>email_hash16</code>; raw email retained in tenant Postgres (RLS-scoped) only until <code>signup_sessions.scrubbed_at</code> is set at 90d.</p>\n<ol><li><strong>MUST</strong> thread W3C <code>traceparent</code> across the entire flow (task-audit skill rule 22 + 23 + 24). The <code>signup_session_id</code> is logged as a span attribute on every span; a single <code>trace_id</code> in the response makes support escalations resolvable.</li></ol>\n<ol><li><strong>MUST</strong> scrub PII at 90 days per DEC-840. Daily scheduled job updates <code>signup_sessions WHERE started_at &lt; now() - interval '90 days' AND scrubbed_at IS NULL</code>: clear <code>email_full</code>, retain <code>email_hash16</code> + funnel state. The scrub itself is recorded in memory via <code>ten.signup_session_scrubbed</code> (kind not in the 9-kind core list per DEC-839 — informational only).</li></ol>\n<ol><li><strong>MUST</strong> support the VN locale at every consent UI step per DEC-833. The Vietnamese-language ToS / Privacy variant is at <code>services/ten/web/signup/consents/vi/*.md</code>; the PDPL-VN consent kind <code>pdpl_vn_data_processing</code> is required for <code>billing_currency='VND'</code> (residency vn-1).</li></ol>\n<ol><li><strong>MUST</strong> rollback cleanly on any orchestrator step failure. The rollback path:</li></ol>\n<ul><li>If Stripe authorization succeeded but TEN-001 provisioning failed: Stripe Subscription is cancelled (<code>DELETE /v1/subscriptions/{id}</code>); SetupIntent is voided; signup_sessions.state='rolled_back' + rolled_back_reason='provisioning_failed'.</li><li>If TEN-001 provisioning succeeded but AUTH root-admin failed: invoke TASK-TEN-104 hard-terminate on the just-created tenant (rare path); signup_sessions.state='rolled_back'.</li><li>Every rollback emits a <code>ten.signup_abandoned</code> memory row with <code>abandon_reason</code> populated.</li></ul>\n<ol><li><strong>MUST</strong> rate-limit per <code>(IP, email_hash16)</code> pair NOT per-(IP, email_full) (per DEC-840 derivative — avoid storing raw email in rate-limit keys for privacy). The hash16 form is sufficient for de-duplication at the rate-limit boundary.</li></ol>\n<ol><li><strong>MUST</strong> be idempotent on <code>signup_session_id</code> per DEC-829. A signup that crashes mid-flow can be resumed: the client reuses the same session_id and the handler returns the current state. Retrying <code>signup_complete</code> with the same session_id after a successful complete returns 200 OK with the existing tenant_id (idempotent acknowledgement).</li></ol>\n<ol><li><strong>MUST NOT</strong> persist plaintext passwords or plaintext OTPs anywhere. OTPs are HMAC-SHA256-hashed under a server-side secret before Redis insert. Auto-generated root-admin passwords are bcrypt-hashed in <code>subjects</code> (TASK-AUTH-002) and emailed once via TASK-EMAIL-001 (DEC-834).</li></ol>\n<ol><li><strong>MUST</strong> be RLS-protected on <code>signup_sessions</code> and <code>tenant_consents</code>. Pre-tenant-creation rows in <code>signup_sessions</code> use a system-tenant scope (<code>current_setting('auth.signup_system_tenant_id')</code> — the well-known UUID <code>00000000-0000-0000-0000-000000000001</code>); post-tenant-creation rows back-fill <code>tenant_id</code> and the RLS policy switches to the real tenant scope. The dual-scope policy is <code>USING (signup_session_id IS NOT NULL AND (tenant_id IS NULL OR tenant_id = current_setting('auth.tenant_id')::uuid)) WITH CHECK (...same...)</code>.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a single orchestrator (<code>complete.rs</code>) rather than 5 independent micro-endpoints (§1 #14, DEC-827)?</strong> The 30-second SLO is the design driver. A multi-step async flow with persisted state between steps would require the user to wait through 4 page reloads (or polling), losing 5-10 seconds each. The single <code>complete</code> endpoint chains Stripe → TEN-001 → AUTH → JWT in one server-side call with parallel where possible (e.g., consent INSERTs run in parallel with Stripe confirm). Total wall-clock cost ≈ 3-5 seconds for the server work + ≈ 5-15 seconds for the user filling the form ≈ 30s budget hit.</p>\n<p><strong>Why payment BEFORE provisioning (§1 #14 step 7, DEC-843)?</strong> Two reasons. First, commercial: tenants without payment are not customers — they're cost. Provisioning a tenant before payment confirmation means we provision-then-charge, which fails 5-15% of the time (declined cards) and leaves orphan tenants that auto-suspend after dunning. Second, anti-abuse: requiring payment authorization filters out 99% of bot signups (bots don't have valid cards; even stolen-card bots get filtered by Stripe Radar). The cost is a tighter rollback (Stripe SetupIntent void on tenant-create failure) but rollback is rare and Stripe void is fast.</p>\n<p><strong>Why HMAC-hash OTPs in Redis rather than plaintext (§1 #1, DEC-821)?</strong> Defense-in-depth. Redis snapshots / replicas / log-shipping can leak data. Plaintext OTPs in any of those is a credential leak vector. HMAC-hashing under a server-side secret means a Redis dump doesn't yield usable OTPs. The HMAC key rotation policy is quarterly per ops standards.</p>\n<p><strong>Why GeoIP-derived currency rather than user-input only (§1 #11, DEC-824)?</strong> Two reasons. First, default-to-local is the conventional UX — a Vietnamese user shouldn't see USD prices first. Second, residency is downstream of currency (DEC-825); GeoIP-default-then-override is faster than asking the user a separate residency question. The user retains the override path for legitimate cases (e.g., a SG-based finance team wants USD billing for parent-company consolidation).</p>\n<p><strong>Why disposable-email blocklist (DEC-830) rather than just rate limits?</strong> Rate limits stop one-bot-per-IP; disposable-email blocklists stop one-bot-per-IP-per-throwaway-domain. Without it, a bot rotates <code>temp-mail.org / 10minutemail.com / mailinator.com</code> addresses faster than the rate limit blocks. The blocklist is high-signal because legitimate users rarely use disposable domains (and when they do — e.g., privacy-conscious users — the \"use work email\" suggestion is reasonable for a paid B2B SaaS).</p>\n<p><strong>Why 30-second SLO (§1 #17, DEC-828)?</strong> Industry data (Baymard Institute, Optimizely studies) shows conversion drops 30%+ when forms exceed 30 seconds. Self-serve signup is the top of the commercial funnel; a 30% drop here translates to ~30% lower ARR. The SLO is treated as a commercial commitment, not just a tech metric.</p>\n<p><strong>Why OIDC sign-up path (§1 #15, DEC-835)?</strong> \"Sign in with Google/Microsoft\" cuts signup time by ~15 seconds (no OTP step, no password creation, no email verification round-trip) and improves trust signals (delegating identity to a known IdP). The cost is a future-pivot risk if the user later wants password fallback — handled by the tenant_admin \"enable password\" toggle (post-signup).</p>\n<p><strong>Why same-email duplicate guard (§1 #14 step 5, DEC-842)?</strong> A user who already has a tenant and tries to sign up again (forgot they have one, or trying to consolidate billing) gets a magic-link to the existing tenant, NOT a second tenant. Multiple tenants per person is a support nightmare (which one is the \"real\" one?) and a billing nightmare (which one gets billed?). The magic-link UX is the right escape valve.</p>\n<p><strong>Why squatting detection (§1 #18, DEC-844)?</strong> Bots that scrape signup forms often abandon at payment (no valid card). 5 abandoned signups from one IP in 24h is statistically improbable for humans (~10⁻⁴ in our observed funnel data, mostly families with multiple businesses). 24h cool-off + manual support unblock for false positives is the right trade.</p>\n<p><strong>Why versioned consents (§1 #16, DEC-832)?</strong> Regulators ask \"what did this user agree to?\" The answer needs a version string + a way to fetch the exact text. Storing <code>version: \"tos-v2.3\"</code> + a static <code>tos-v2.3.md</code> file in source means we can answer that question with bit-exact text years later. ToS/Privacy version bumps require a consent-re-acceptance flow (out-of-scope here; task-TEN-1xx).</p>\n<p><strong>Why dual-scope RLS on signup_sessions (§1 #27)?</strong> Pre-tenant signups have no <code>tenant_id</code> (no tenant exists yet), but the row must be RLS-scoped — without scoping, anonymous users could enumerate all in-flight signups via the API. The \"system tenant\" UUID <code>00000000-0000-0000-0000-000000000001</code> is a well-known sentinel that the public signup handlers set via <code>SET LOCAL auth.tenant_id</code> at request entry; post-completion the row's <code>tenant_id</code> is back-filled and the policy transparently transitions to the real tenant.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 Postgres schema (migrations)</h3>\n<pre><code class=\"language-sql\">-- 0011_signup_sessions.sql\n-- First adds the billing_contact_email_hash16 column + index to tenants for the duplicate-email guard (§1 #14 step 5):\nALTER TABLE tenants\n  ADD COLUMN billing_contact_email_hash16 TEXT GENERATED ALWAYS AS\n    (encode(substring(digest(coalesce(lower(billing_contact_email), ''), 'sha256') from 1 for 8), 'hex'))\n    STORED;\nCREATE INDEX idx_tenants_billing_contact_email_hash16\n  ON tenants(billing_contact_email_hash16) WHERE status = 'active';\n\nCREATE TABLE signup_sessions (\n  signup_session_id UUID PRIMARY KEY,\n  email_hash16 TEXT NOT NULL,\n  email_full TEXT,                                 -- nullable after 90d scrub\n  geoip_country CHAR(2),\n  suggested_billing_currency billing_currency_enum,\n  suggested_residency TEXT,\n  state TEXT NOT NULL DEFAULT 'started'\n    CHECK (state IN ('started','email_verified','payment_captured','provisioning','completed','abandoned','rolled_back')),\n  started_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  email_verified_at TIMESTAMPTZ,\n  payment_captured_at TIMESTAMPTZ,\n  provisioned_at TIMESTAMPTZ,\n  completed_at TIMESTAMPTZ,\n  abandon_reason TEXT,\n  rolled_back_reason TEXT,\n  tenant_id UUID,\n  ip_addr_hash16 TEXT,\n  user_agent_hash16 TEXT,\n  scrubbed_at TIMESTAMPTZ\n);\nCREATE INDEX idx_signup_sessions_email_hash16 ON signup_sessions(email_hash16);\nCREATE INDEX idx_signup_sessions_started_at ON signup_sessions(started_at);\nALTER TABLE signup_sessions ENABLE ROW LEVEL SECURITY;\nCREATE POLICY signup_sessions_rls ON signup_sessions\n  USING (\n    tenant_id IS NULL AND current_setting('auth.tenant_id', true) = '00000000-0000-0000-0000-000000000001'\n    OR tenant_id = NULLIF(current_setting('auth.tenant_id', true), '')::uuid\n  )\n  WITH CHECK (\n    tenant_id IS NULL AND current_setting('auth.tenant_id', true) = '00000000-0000-0000-0000-000000000001'\n    OR tenant_id = NULLIF(current_setting('auth.tenant_id', true), '')::uuid\n  );\nREVOKE UPDATE, DELETE ON signup_sessions FROM cyberos_app;\n-- Specific UPDATE grants for state transitions:\nGRANT UPDATE (state, email_verified_at, payment_captured_at, provisioned_at, completed_at,\n              abandon_reason, rolled_back_reason, tenant_id, scrubbed_at, email_full)\n  ON signup_sessions TO cyberos_app;\n\n-- 0012_tenant_consents.sql\nCREATE TABLE tenant_consents (\n  id BIGSERIAL PRIMARY KEY,\n  tenant_id UUID,                                  -- nullable pre-provisioning\n  signup_session_id UUID NOT NULL,\n  subject_id UUID,                                  -- nullable until root-admin created\n  consent_kind TEXT NOT NULL\n    CHECK (consent_kind IN ('tos','privacy','marketing','pdpl_vn_data_processing',\n                            'pdpl_vn_data_export','gdpr_legitimate_interest')),\n  version TEXT NOT NULL,\n  accepted_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  ip_addr_hash16 TEXT NOT NULL,\n  locale CHAR(5) NOT NULL,\n  withdrawn_at TIMESTAMPTZ,\n  withdrawn_reason TEXT\n);\nCREATE INDEX idx_tenant_consents_tenant ON tenant_consents(tenant_id);\nCREATE INDEX idx_tenant_consents_signup ON tenant_consents(signup_session_id);\nALTER TABLE tenant_consents ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_consents_rls ON tenant_consents\n  USING (tenant_id IS NULL OR tenant_id = NULLIF(current_setting('auth.tenant_id', true), '')::uuid)\n  WITH CHECK (tenant_id IS NULL OR tenant_id = NULLIF(current_setting('auth.tenant_id', true), '')::uuid);\nREVOKE UPDATE, DELETE ON tenant_consents FROM cyberos_app;\n-- Withdrawal is the only permitted update; column-level GRANT to consent_writer role:\nGRANT UPDATE (withdrawn_at, withdrawn_reason) ON tenant_consents TO cyberos_consent_writer;\n\n-- 0013_signup_rate_limits.sql\n-- The hot counters live in Redis sliding-window (sub-millisecond reads needed at the public edge).\n-- This table is a forensic journal — every rate-limit HIT is dual-logged here for compliance\n-- analytics (e.g., \"how many signup attempts were blocked in EU-region this quarter?\"). Misses are NOT logged.\nCREATE TABLE signup_rate_limit_journal (\n  id BIGSERIAL PRIMARY KEY,\n  guard TEXT NOT NULL CHECK (guard IN ('signup_start','otp_send','slug_check','signup_complete')),\n  key_kind TEXT NOT NULL CHECK (key_kind IN ('ip','email_hash16','ip_email')),\n  key_value TEXT NOT NULL,\n  ts TIMESTAMPTZ NOT NULL DEFAULT now(),\n  retry_after_seconds INT NOT NULL\n);\nALTER TABLE signup_rate_limit_journal ENABLE ROW LEVEL SECURITY;\nCREATE POLICY signup_rate_limit_journal_rls ON signup_rate_limit_journal\n  USING (current_setting('auth.tenant_id', true) = '00000000-0000-0000-0000-000000000001')\n  WITH CHECK (current_setting('auth.tenant_id', true) = '00000000-0000-0000-0000-000000000001');\nREVOKE UPDATE, DELETE ON signup_rate_limit_journal FROM cyberos_app;\n\n-- 0014_disposable_email_domains.sql\nCREATE TABLE disposable_email_domains (\n  domain TEXT PRIMARY KEY,\n  source TEXT NOT NULL,\n  added_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  removed_at TIMESTAMPTZ\n);\nCREATE TABLE disposable_email_blocklist_refresh (\n  id BIGSERIAL PRIMARY KEY,\n  refreshed_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  source_sha CHAR(64) NOT NULL,\n  domains_added INT NOT NULL DEFAULT 0,\n  domains_removed INT NOT NULL DEFAULT 0\n);\nREVOKE UPDATE, DELETE ON disposable_email_domains FROM cyberos_app;\nREVOKE UPDATE, DELETE ON disposable_email_blocklist_refresh FROM cyberos_app;\nGRANT INSERT, DELETE ON disposable_email_domains TO cyberos_blocklist_updater;</code></pre>\n<h3>3.2 Rust types</h3>\n<pre><code class=\"language-rust\">// services/ten/src/signup/mod.rs\n#[derive(Copy, Clone, Eq, PartialEq, Debug, sqlx::Type)]\n#[sqlx(type_name = \"TEXT\")]\npub enum SignupState {\n    Started,\n    EmailVerified,\n    PaymentCaptured,\n    Provisioning,\n    Completed,\n    Abandoned,\n    RolledBack,\n}\n\n#[derive(serde::Deserialize)]\npub struct SignupStartReq {\n    pub email: String,\n    pub signup_session_id: uuid::Uuid,  // UUIDv7 client-generated\n    pub turnstile_token: String,\n    pub locale_hint: Option&lt;String&gt;,  // e.g., \"vi-VN\"\n}\n\n#[derive(serde::Serialize)]\npub struct SignupStartResp {\n    pub signup_session_id: uuid::Uuid,\n    pub email_verification_required: bool,\n    pub suggested_billing_currency: BillingCurrency,\n    pub suggested_residency: Residency,\n    pub geoip_country: Option&lt;String&gt;,\n    pub otp_sent_at: Option&lt;chrono::DateTime&lt;chrono::Utc&gt;&gt;,\n    pub breach_warning: Option&lt;BreachWarning&gt;,\n}\n\n#[derive(serde::Deserialize)]\npub struct SignupCompleteReq {\n    pub signup_session_id: uuid::Uuid,\n    pub tenant_slug: String,\n    pub tenant_display_name: String,\n    pub plan_tier: PlanTier,\n    pub billing_currency: BillingCurrency,\n    pub billing_contact_email: String,\n    pub stripe_setup_intent_id: Option&lt;String&gt;,\n    pub vnpay_reference: Option&lt;String&gt;,\n    pub consents: Vec&lt;ConsentEntry&gt;,\n    pub turnstile_token: String,\n}\n\n#[derive(serde::Deserialize)]\npub struct ConsentEntry {\n    pub kind: ConsentKind,\n    pub version: String,\n    pub locale: String,\n}</code></pre>\n<h3>3.3 REST endpoints</h3>\n<pre><code class=\"language-text\">POST   /v1/signup/start                  (public)\nPOST   /v1/signup/verify-otp             (public)\nGET    /v1/signup/slug-available         (public)\nPOST   /v1/signup/payment-intent         (public, session-bound)\nPOST   /v1/signup/complete               (public, session-bound)\nGET    /v1/signup/oidc-callback          (public, TASK-AUTH-104 return)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>End-to-end happy path under 30 s</strong> — fixture-based happy test (<code>signup_30s_sla_test</code>) completes start→OTP→slug→plan→payment→complete in &lt; 30 s including Stripe SetupIntent confirm (against test-mode).</li><li><strong>OTP HMAC-hashed in Redis</strong> — direct Redis inspection during test shows no plaintext OTP; only HMAC-SHA256 digests.</li><li><strong>OTP max attempts enforced</strong> — 6 failed verify-otp calls in row return <code>429 + { error: \"otp_max_attempts\" }</code>; 7th requires new OTP send.</li><li><strong>Disposable email blocked</strong> — signup_start with <code>email=foo@10minutemail.com</code> returns 400 + <code>{ error: \"disposable_email\" }</code> and emits <code>ten.signup_disposable_email_blocked</code>.</li><li><strong>Rate limits enforced</strong> — 11th signup_start from same IP within 60 s returns 429.</li><li><strong>Slug uniqueness</strong> — slug-available for an existing slug returns <code>{available: false}</code> + 50 suggestions; race-on-complete returns 409.</li><li><strong>GeoIP→currency derivation</strong> — VN-IP signup defaults to VND/vn-1; SG-IP to SGD/sg-1; EU-IP to EUR/eu-1; user override accepted.</li><li><strong>Enterprise self-serve blocked</strong> — signup_complete with <code>plan_tier=enterprise</code> returns 403.</li><li><strong>Consent versioned + required</strong> — signup_complete without ToS+Privacy consents returns 400; with VND currency missing PDPL VN consent returns 400.</li><li><strong>Stripe rollback on TEN-001 failure</strong> — when TEN-001 provisioning fails after Stripe confirm, the Stripe Subscription is cancelled + signup_sessions.state='rolled_back'.</li><li><strong>Duplicate email returns magic link</strong> — signup_complete with an already-active tenant's billing_contact_email returns 409 + magic-link email sent; no second tenant created.</li><li><strong>OIDC path skips OTP</strong> — <code>/v1/signup/oidc-callback</code> with verified ID token transitions session directly to <code>email_verified</code>.</li><li><strong>Squatting block after 5 abandons</strong> — 5 abandoned signups from one IP in 24h → 6th signup_start returns 429 <code>abuse_detected_24h_cooloff</code>.</li><li><strong>PII scrubbed at 90 days</strong> — fixture signup with started_at 91 days ago + run scrub job → email_full = NULL, email_hash16 retained, memory row <code>ten.signup_session_scrubbed</code> emitted.</li><li><strong>9 memory audit kinds emitted</strong> — happy path produces <code>signup_started + signup_email_verified + signup_consent_recorded + signup_tenant_provisioned + signup_completed</code> (5 of 9); failure paths produce the others.</li><li><strong>W3C traceparent threaded</strong> — single trace_id present in start response + complete response + all 9 memory rows.</li><li><strong>Idempotent on session_id</strong> — <code>signup_complete</code> invoked twice with same session_id post-completion returns 200 + existing tenant_id (not 201 + new).</li><li><strong>Welcome email + 7d deeplink</strong> — completed signup triggers email with deeplink to <code>&lt;slug&gt;.cyberos.world/onboarding/welcome</code>; deeplink works for 7 days then 404.</li><li><strong>RLS dual-scope</strong> — pre-tenant rows visible only to <code>system_signup_tenant</code>; post-tenant rows visible only to the tenant's RLS scope.</li><li><strong>Stripe SetupIntent only — no PAN at our backend</strong> — request-body inspection confirms no <code>card_number</code> field in any request to our endpoints.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<h3>5.1 <code>signup_happy_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn signup_30s_e2e_happy() {\n    let ctx = TestContext::new().await;\n    let session = uuid::Uuid::now_v7();\n    let start = Instant::now();\n\n    // 1. start\n    let r1 = ctx.post(\"/v1/signup/start\").json(&amp;SignupStartReq{\n        email: \"alice@acme.com\".into(), signup_session_id: session,\n        turnstile_token: ctx.test_turnstile_token(), locale_hint: Some(\"en-US\".into()),\n    }).send().await.unwrap();\n    assert_eq!(r1.status(), 200);\n\n    let otp = ctx.read_test_otp_for(session).await;\n\n    // 2. verify-otp\n    let r2 = ctx.post(\"/v1/signup/verify-otp\").json(&amp;serde_json::json!({\n        \"signup_session_id\": session, \"otp_code\": otp\n    })).send().await.unwrap();\n    assert_eq!(r2.status(), 200);\n\n    // 3. slug-available\n    let r3 = ctx.get(&amp;format!(\"/v1/signup/slug-available?candidate=acme-{session}\"))\n        .send().await.unwrap();\n    let avail: serde_json::Value = r3.json().await.unwrap();\n    assert_eq!(avail[\"available\"], true);\n\n    // 4. payment-intent\n    let r4 = ctx.post(\"/v1/signup/payment-intent\").json(&amp;serde_json::json!({\n        \"signup_session_id\": session, \"plan_tier\": \"team\", \"billing_currency\": \"USD\"\n    })).send().await.unwrap();\n    let setup_intent_id = ctx.stripe_test_confirm(r4.json::&lt;serde_json::Value&gt;().await.unwrap()).await;\n\n    // 5. complete\n    let r5 = ctx.post(\"/v1/signup/complete\").json(&amp;SignupCompleteReq{\n        signup_session_id: session,\n        tenant_slug: format!(\"acme-{}\", &amp;session.to_string()[..8]),\n        tenant_display_name: \"Acme Inc.\".into(),\n        plan_tier: PlanTier::Team,\n        billing_currency: BillingCurrency::Usd,\n        billing_contact_email: \"alice@acme.com\".into(),\n        stripe_setup_intent_id: Some(setup_intent_id),\n        vnpay_reference: None,\n        consents: vec![\n            ConsentEntry{kind: ConsentKind::Tos, version: \"v2.3\".into(), locale: \"en-US\".into()},\n            ConsentEntry{kind: ConsentKind::Privacy, version: \"v2.1\".into(), locale: \"en-US\".into()},\n        ],\n        turnstile_token: ctx.test_turnstile_token(),\n    }).send().await.unwrap();\n\n    assert_eq!(r5.status(), 201);\n    let elapsed = start.elapsed();\n    assert!(elapsed &lt; Duration::from_secs(30), \"actual {:?}\", elapsed);\n}</code></pre>\n<h3>5.2 <code>signup_otp_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn otp_stored_hmac_only() {\n    let ctx = TestContext::new().await;\n    let session = start_signup(&amp;ctx, \"alice@acme.com\").await;\n    let mut redis = ctx.redis_conn();\n    let stored: String = redis.get(&amp;format!(\"signup_otp:{session}\")).await.unwrap();\n    assert!(!stored.contains(\"000000\"));  // no plaintext\n    assert_eq!(stored.len(), 64);          // 256-bit hex digest\n}\n\n#[tokio::test]\nasync fn otp_max_attempts_locks() {\n    let ctx = TestContext::new().await;\n    let session = start_signup(&amp;ctx, \"alice@acme.com\").await;\n    for _ in 0..5 {\n        let r = ctx.post(\"/v1/signup/verify-otp\").json(&amp;json!({\n            \"signup_session_id\": session, \"otp_code\": \"999999\"\n        })).send().await.unwrap();\n        assert_eq!(r.status(), 400);\n    }\n    let r = ctx.post(\"/v1/signup/verify-otp\").json(&amp;json!({\n        \"signup_session_id\": session, \"otp_code\": \"999999\"\n    })).send().await.unwrap();\n    assert_eq!(r.status(), 429);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"otp_max_attempts\");\n}</code></pre>\n<h3>5.3 <code>signup_disposable_email_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn disposable_domain_blocked() {\n    let ctx = TestContext::new().await;\n    let r = ctx.post(\"/v1/signup/start\").json(&amp;SignupStartReq{\n        email: \"test@10minutemail.com\".into(),\n        signup_session_id: uuid::Uuid::now_v7(),\n        turnstile_token: ctx.test_turnstile_token(),\n        locale_hint: None,\n    }).send().await.unwrap();\n    assert_eq!(r.status(), 400);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"disposable_email\");\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.signup_disposable_email_blocked\"));\n}</code></pre>\n<h3>5.4 <code>signup_rate_limit_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn signup_start_rate_limit() {\n    let ctx = TestContext::new().await;\n    for i in 0..10 {\n        let r = ctx.post(\"/v1/signup/start\").json(&amp;SignupStartReq{\n            email: format!(\"alice{i}@acme.com\"),\n            signup_session_id: uuid::Uuid::now_v7(),\n            turnstile_token: ctx.test_turnstile_token(),\n            locale_hint: None,\n        }).send().await.unwrap();\n        assert_eq!(r.status(), 200);\n    }\n    let r = ctx.post(\"/v1/signup/start\").json(&amp;SignupStartReq{\n        email: \"alice11@acme.com\".into(),\n        signup_session_id: uuid::Uuid::now_v7(),\n        turnstile_token: ctx.test_turnstile_token(),\n        locale_hint: None,\n    }).send().await.unwrap();\n    assert_eq!(r.status(), 429);\n}</code></pre>\n<h3>5.5 <code>signup_stripe_rollback_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn stripe_confirmed_then_provisioning_fails_rolls_back() {\n    let ctx = TestContext::new().await;\n    let session = email_verified_session(&amp;ctx).await;\n    let setup_intent = ctx.stripe_test_setup_intent_confirmed().await;\n    ctx.force_provisioning_failure();\n\n    let r = ctx.post(\"/v1/signup/complete\").json(&amp;minimal_complete_body(session, setup_intent)).send().await.unwrap();\n    assert_eq!(r.status(), 500);\n\n    let stripe_subscriptions = ctx.stripe_test_list_subscriptions_for_session(session).await;\n    assert!(stripe_subscriptions.iter().all(|s| s.status == \"canceled\"));\n\n    let state: String = sqlx::query_scalar(\"SELECT state FROM signup_sessions WHERE signup_session_id=$1\")\n        .bind(session).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(state, \"rolled_back\");\n}</code></pre>\n<h3>5.6 <code>signup_duplicate_email_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn second_signup_same_email_returns_magic_link() {\n    let ctx = TestContext::new().await;\n    let _first = complete_signup(&amp;ctx, \"alice@acme.com\", \"acme-1\").await;\n    let session2 = email_verified_session_with(&amp;ctx, \"alice@acme.com\").await;\n\n    let r = ctx.post(\"/v1/signup/complete\").json(&amp;minimal_complete_body(session2, ctx.stripe_test_setup_intent_confirmed().await))\n        .send().await.unwrap();\n    assert_eq!(r.status(), 409);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"email_already_associated\");\n    assert_eq!(body[\"magic_link_sent\"], true);\n\n    let count: i64 = sqlx::query_scalar(\n        \"SELECT count(*) FROM tenants WHERE billing_contact_email='alice@acme.com'\"\n    ).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(count, 1);\n}</code></pre>\n<h3>5.7 <code>signup_squatting_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn five_abandoned_signups_blocks_ip_24h() {\n    let ctx = TestContext::with_ip(\"198.51.100.42\").await;\n    for i in 0..5 {\n        let s = uuid::Uuid::now_v7();\n        ctx.post(\"/v1/signup/start\").json(&amp;start_body(s, format!(\"a{i}@acme.com\")))\n            .send().await.unwrap();\n        ctx.tick_abandon(s).await;\n    }\n    let r = ctx.post(\"/v1/signup/start\").json(&amp;start_body(uuid::Uuid::now_v7(), \"a99@acme.com\".into()))\n        .send().await.unwrap();\n    assert_eq!(r.status(), 429);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"abuse_detected_24h_cooloff\");\n}</code></pre>\n<h3>5.8 <code>signup_consent_versioning_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn vn_tenant_requires_pdpl_consent() {\n    let ctx = TestContext::with_geoip_country(\"VN\").await;\n    let session = email_verified_session(&amp;ctx).await;\n    let setup_intent = ctx.vnpay_reference_confirmed().await;\n\n    let r = ctx.post(\"/v1/signup/complete\").json(&amp;SignupCompleteReq{\n        signup_session_id: session,\n        billing_currency: BillingCurrency::Vnd,\n        consents: vec![\n            ConsentEntry{kind: ConsentKind::Tos, version: \"v2.3\".into(), locale: \"vi-VN\".into()},\n            ConsentEntry{kind: ConsentKind::Privacy, version: \"v2.1\".into(), locale: \"vi-VN\".into()},\n            // PDPL VN missing\n        ],\n        ..minimal_complete_body(session, setup_intent)\n    }).send().await.unwrap();\n\n    assert_eq!(r.status(), 400);\n    let body: serde_json::Value = r.json().await.unwrap();\n    assert_eq!(body[\"error\"], \"missing_pdpl_vn_consent\");\n}</code></pre>\n<h3>5.9 <code>signup_oidc_path_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn oidc_signup_skips_otp() {\n    let ctx = TestContext::new().await;\n    let session = uuid::Uuid::now_v7();\n    let oidc_id_token = ctx.fake_google_id_token(\"alice@acme.com\", true).await;\n\n    let r = ctx.get(&amp;format!(\"/v1/signup/oidc-callback?id_token={oidc_id_token}&amp;signup_session_id={session}\"))\n        .send().await.unwrap();\n    assert_eq!(r.status(), 200);\n\n    let state: String = sqlx::query_scalar(\"SELECT state FROM signup_sessions WHERE signup_session_id=$1\")\n        .bind(session).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(state, \"email_verified\");\n\n    let audit = ctx.memory_rows().await;\n    assert!(audit.iter().any(|r| r.kind == \"ten.signup_oidc_linked\"));\n}</code></pre>\n<h3>5.10 <code>signup_30s_sla_test.rs</code></h3>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn signup_completes_under_30s_p95() {\n    let ctx = TestContext::new().await;\n    let mut durations = Vec::new();\n    for i in 0..20 {\n        let start = Instant::now();\n        complete_signup(&amp;ctx, &amp;format!(\"u{i}@acme.com\"), &amp;format!(\"acme-{i}\")).await;\n        durations.push(start.elapsed());\n    }\n    durations.sort();\n    let p95 = durations[(durations.len() as f64 * 0.95) as usize];\n    assert!(p95 &lt; Duration::from_secs(30), \"p95 actual {:?}\", p95);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract in §3 is the skeleton. Additional orchestrator wiring below.)</p>\n<h3>6.1 <code>complete.rs</code> commit-or-rollback orchestrator</h3>\n<pre><code class=\"language-rust\">pub async fn signup_complete(ctx: &amp;AppCtx, req: SignupCompleteReq, cookies: &amp;mut Cookies) -&gt; Result&lt;SignupCompleteResp, SignupError&gt; {\n    // ───── Phase A: pre-tx validation (no DB writes) ─────\n    ctx.turnstile.verify(&amp;req.turnstile_token, &amp;req.signup_session_id).await?;\n\n    let session = ctx.repo.signup_sessions.get(req.signup_session_id).await?;\n    if session.state != SignupState::EmailVerified {\n        return Err(SignupError::InvalidState(session.state));\n    }\n    if req.plan_tier == PlanTier::Enterprise {\n        return Err(SignupError::EnterpriseSelfServeBlocked);\n    }\n    validate_consents(&amp;req.consents, req.billing_currency)?;\n\n    if let Some(existing) = ctx.repo.tenants.find_active_by_email_hash16(&amp;hash16_email(&amp;req.billing_contact_email)).await? {\n        send_magic_link(&amp;ctx, &amp;req.billing_contact_email, existing.id).await;\n        emit_audit(&amp;ctx, \"ten.signup_abandoned\", json!({\"reason\": \"duplicate_email\"})).await;\n        return Err(SignupError::EmailAlreadyAssociated);\n    }\n\n    // ───── Phase B: external Stripe call (NO DB tx held) ─────\n    if req.billing_currency != BillingCurrency::Vnd {\n        let intent_id = req.stripe_setup_intent_id.as_ref().ok_or(SignupError::MissingPaymentIntent)?;\n        ctx.stripe.confirm_setup_intent(intent_id).await.map_err(SignupError::PaymentFailed)?;\n        ctx.repo.signup_sessions.mark_payment_captured(req.signup_session_id, intent_id).await?;\n    }\n\n    // ───── Phase C: atomic provisioning transaction ─────\n    let provisioning = async {\n        let mut tx = ctx.pool.begin().await?;\n        for c in &amp;req.consents {\n            ctx.repo.consents.insert_pending(&amp;mut tx, session.signup_session_id, c, &amp;req.tenant_slug).await?;\n        }\n        let tenant_id = ctx.ten.provision_tenant(&amp;mut tx, ProvisionReq{\n            slug: req.tenant_slug.clone(),\n            display_name: req.tenant_display_name.clone(),\n            billing_currency: req.billing_currency,\n            billing_contact_email: req.billing_contact_email.clone(),\n            residency: derive_residency(req.billing_currency),\n        }).await?;\n        let root_admin = ctx.auth.create_subject_with_role(&amp;mut tx, tenant_id, &amp;req.billing_contact_email,\n            SubjectRole::TenantAdmin, GeneratedPassword::Auto24).await?;\n        ctx.repo.consents.back_fill_tenant_id(&amp;mut tx, session.signup_session_id, tenant_id, root_admin.id).await?;\n        ctx.repo.signup_sessions.transition_completed(&amp;mut tx, session.signup_session_id, tenant_id).await?;\n        tx.commit().await?;\n        Ok::&lt;(uuid::Uuid, SubjectRecord), SignupError&gt;((tenant_id, root_admin))\n    }.await;\n\n    let (tenant_id, root_admin) = match provisioning {\n        Ok(t) =&gt; t,\n        Err(e) =&gt; {\n            // Phase-C failure: void Stripe + mark session rolled back\n            if let Some(intent_id) = &amp;req.stripe_setup_intent_id {\n                ctx.stripe.void_setup_intent(intent_id).await.ok();\n            }\n            let reason = match &amp;e {\n                SignupError::SlugCollision(_) =&gt; \"slug_collision\",\n                _ =&gt; \"provisioning_failed\",\n            };\n            mark_rolled_back(&amp;ctx, session.signup_session_id, reason).await?;\n            return Err(e);\n        }\n    };\n\n    // ───── Phase D: post-commit side effects (failures here do NOT undo the tenant) ─────\n    if req.billing_currency != BillingCurrency::Vnd {\n        if let Err(e) = ctx.ten.billing_stripe.ensure_customer(tenant_id).await {\n            tracing::error!(?e, %tenant_id, \"ensure_customer failed post-commit; tenant lands in dunning_state=retry_1\");\n            ctx.repo.tenants.set_dunning_state(tenant_id, DunningState::Retry1).await?;\n        } else {\n            ctx.ten.billing_stripe.ensure_subscription(tenant_id, req.plan_tier).await.ok();\n        }\n    }\n\n    let jwt = ctx.auth.mint_jwt(root_admin.id, tenant_id, vec![\"signup_first_login\"], &amp;req.signup_session_id).await?;\n    cookies.add(secure_cookie(\"cyberos_jwt\", &amp;jwt, \"*.cyberos.world\", chrono::Duration::hours(24)));\n\n    emit_audit(&amp;ctx, \"ten.signup_completed\", json!({\n        \"tenant_id\": tenant_id, \"plan_tier\": req.plan_tier, \"currency\": req.billing_currency,\n        \"session_id\": session.signup_session_id, \"duration_ms\": session.duration_ms(),\n    })).await;\n    ctx.email.send_welcome(&amp;ctx, tenant_id, &amp;req.billing_contact_email, &amp;req.tenant_slug).await.ok();\n\n    Ok(SignupCompleteResp{\n        tenant_id,\n        tenant_slug: req.tenant_slug,\n        jwt,\n        redirect_url: format!(\"https://{}.cyberos.world/onboarding/welcome\", req.tenant_slug),\n    })\n}</code></pre>\n<h3>6.2 OTP HMAC</h3>\n<pre><code class=\"language-rust\">// services/ten/src/signup/otp.rs\nuse hmac::{Hmac, Mac};\nuse sha2::Sha256;\ntype HmacSha256 = Hmac&lt;Sha256&gt;;\n\npub fn hash_otp(otp: &amp;str, signup_session_id: uuid::Uuid, secret: &amp;[u8]) -&gt; String {\n    let mut mac = HmacSha256::new_from_slice(secret).unwrap();\n    mac.update(otp.as_bytes());\n    mac.update(signup_session_id.as_bytes());\n    hex::encode(mac.finalize().into_bytes())\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (depends_on):</strong></p>\n<ul><li><strong>TASK-AUTH-104</strong> OIDC SSO — Google/Microsoft sign-up path.</li><li><strong>TASK-TEN-001</strong> Provisioning — <code>provision_tenant</code> invoked at signup_complete step 8.</li><li><strong>TASK-TEN-002</strong> Plan tiers — <code>plan_tier</code> selection + per-tier subscription.</li><li><strong>TASK-TEN-003</strong> Stripe billing — <code>ensure_customer</code> + <code>ensure_subscription</code> at step 9.</li></ul>\n<p><strong>Cross-module (related_tasks):</strong></p>\n<ul><li><strong>TASK-TEN-004</strong> 4-axis metering — first metering events fire post-signup.</li><li><strong>TASK-TEN-102</strong> VND domestic rail — VNP path placeholder until TASK-TEN-102 ships.</li><li><strong>TASK-TEN-103</strong> 4-residency provisioning — residency derivation aligns.</li><li><strong>TASK-TEN-104</strong> Lifecycle — rollback path may invoke termination.</li><li><strong>TASK-TEN-107</strong> Tenant-admin SPA — first destination post-signup.</li><li><strong>TASK-AUTH-001</strong> Tenant create — invoked indirectly via TASK-TEN-001.</li><li><strong>TASK-AUTH-002</strong> Subject create — root-admin creation.</li><li><strong>TASK-AUTH-004</strong> JWT mint — first-login token.</li><li><strong>TASK-AUTH-101</strong> RBAC — root-admin gets <code>tenant_admin</code> role.</li><li><strong>TASK-AUTH-107</strong> HIBP breach-check — informational warning at signup.</li><li><strong>TASK-EMAIL-001</strong> Transactional email — OTP + welcome email + magic-link.</li><li><strong>TASK-PORTAL-001/002</strong> — depend on TEN-101 for tenant-scoped brand pack flow.</li><li><strong>TASK-AI-003</strong> memory audit — 9 new kinds register here.</li><li><strong>TASK-MEMORY-111</strong> PII scrubbing — email + IP scrubbed in chain rows.</li><li><strong>TASK-OBS-007</strong> Auto-runbook — sev-1/sev-2 alerts route to CHAT/PagerDuty.</li></ul>\n<p><strong>Downstream (blocks):</strong></p>\n<ul><li><strong>TASK-TEN-107</strong> — tenant-admin SPA depends on signup completing.</li><li><strong>TASK-PORTAL-001</strong> — scoped read-only views require self-served tenants.</li><li><strong>TASK-PORTAL-002</strong> — per-tenant brand pack requires self-served tenants.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 <code>POST /v1/signup/start</code> request/response</h3>\n<pre><code class=\"language-json\">// request\n{ \"email\": \"alice@acme.com\", \"signup_session_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n  \"turnstile_token\": \"0.xxx\", \"locale_hint\": \"en-US\" }\n\n// response\n{ \"signup_session_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n  \"email_verification_required\": true,\n  \"suggested_billing_currency\": \"USD\",\n  \"suggested_residency\": \"us-1\",\n  \"geoip_country\": \"US\",\n  \"otp_sent_at\": \"2026-05-17T09:14:32.847Z\",\n  \"breach_warning\": null }</code></pre>\n<h3>8.2 <code>ten.signup_completed</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.signup_completed\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"system.ten.signup\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:58.221Z\",\n  \"payload\": {\n    \"signup_session_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n    \"tenant_id\": \"8a2f...\",\n    \"tenant_slug\": \"acme-co\",\n    \"plan_tier\": \"team\",\n    \"billing_currency\": \"USD\",\n    \"billing_rail\": \"stripe\",\n    \"residency\": \"us-1\",\n    \"email_hash16\": \"f8a1b2c3d4e5f607\",\n    \"duration_ms\": 23847,\n    \"oidc_path\": false,\n    \"geoip_country\": \"US\"\n  }\n}</code></pre>\n<h3>8.3 <code>POST /v1/signup/complete</code> request</h3>\n<pre><code class=\"language-json\">{\n  \"signup_session_id\": \"0190f7c0-8b3c-7a4f-aaaa-000000000001\",\n  \"tenant_slug\": \"acme-co\",\n  \"tenant_display_name\": \"Acme Inc.\",\n  \"plan_tier\": \"team\",\n  \"billing_currency\": \"USD\",\n  \"billing_contact_email\": \"alice@acme.com\",\n  \"stripe_setup_intent_id\": \"seti_3OabcXYZ012345\",\n  \"consents\": [\n    {\"kind\": \"tos\",       \"version\": \"v2.3\", \"locale\": \"en-US\"},\n    {\"kind\": \"privacy\",   \"version\": \"v2.1\", \"locale\": \"en-US\"},\n    {\"kind\": \"marketing\", \"version\": \"v1.0\", \"locale\": \"en-US\"}\n  ],\n  \"turnstile_token\": \"0.yyy\"\n}</code></pre>\n<h3>8.4 <code>POST /v1/signup/complete</code> response (201)</h3>\n<pre><code class=\"language-json\">{\n  \"tenant_id\": \"8a2f9c1d-4b6e-7f80-bbbb-000000000042\",\n  \"tenant_slug\": \"acme-co\",\n  \"jwt\": \"eyJhbGciOi...\",\n  \"redirect_url\": \"https://acme-co.cyberos.world/onboarding/welcome\"\n}</code></pre>\n<h3>8.5 Duplicate-email response (409)</h3>\n<pre><code class=\"language-json\">{\n  \"error\": \"email_already_associated\",\n  \"magic_link_sent\": true,\n  \"existing_tenant_hint\": \"acme-co\",\n  \"message\": \"An account already exists for this email. We've sent you a sign-in link.\"\n}</code></pre>\n<h3>8.6 <code>ten.signup_rate_limited</code> memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"ten.signup_rate_limited\",\n  \"severity\": 3,\n  \"tenant_id\": \"00000000-0000-0000-0000-000000000001\",  // system tenant\n  \"actor_id\": \"anonymous\",\n  \"trace_id\": \"0af7651916cd43dd8448eb211c80319c\",\n  \"occurred_at\": \"2026-05-17T09:14:34.001Z\",\n  \"payload\": {\n    \"guard\": \"signup_start\",\n    \"key_kind\": \"ip\",\n    \"ip_addr_hash16\": \"9c4e7a8b6d2f1e3a\",\n    \"retry_after_seconds\": 60\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved for slice 1. Deferred to later slices:</p>\n<ul><li><strong>Deferred:</strong> Magic-link sign-in flow for existing-tenant returns — slice 2, task-TEN-1xx (placeholder — not yet specified). For slice 1, the <code>magic_link_sent: true</code> response sets a follow-up commitment.</li><li><strong>Deferred:</strong> Multi-step ToS/Privacy version-bump re-acceptance flow — slice 2, task-TEN-1xx.</li><li><strong>Deferred:</strong> B2B \"invite teammate\" flow at signup (currently root-admin only; teammates invited post-signup via TASK-AUTH-101 admin) — slice 2.</li><li><strong>Deferred:</strong> Phone-number alternate identifier for SMS OTP — slice 2 (email-only at slice 1).</li><li><strong>Deferred:</strong> SSO-only enforcement at signup (some enterprises want \"no password account creation\") — slice 2.</li><li><strong>Deferred:</strong> Sub-tenant signup (PORTAL invites; TASK-PORTAL-001 path) — out-of-scope at this task.</li><li><strong>Deferred:</strong> Annual billing cycle option at signup — slice 2 / task-TEN-1xx (currently monthly only per TASK-TEN-003 §9).</li><li><strong>Deferred:</strong> Promotional coupon code field at signup — slice 2.</li><li><strong>Deferred:</strong> Enterprise self-serve via Sales-Assisted flow (signup → sales calendar booking → manual provisioning) — out-of-scope at this task (DEC-823).</li><li><strong>Deferred:</strong> eIDAS QTSP-compliant signup for EU regulated tenants — slice 3 / task-TEN-2xx (placeholder — not yet specified).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Turnstile token invalid/expired</td><td>Cloudflare verify returns false</td><td>400 + <code>turnstile_failed</code>; no session created</td><td>User refreshes form; new token issued automatically</td></tr><tr><td>Disposable email domain</td><td>blocklist lookup hits</td><td>400 + <code>disposable_email</code>; suggest work email</td><td>User retries with non-disposable domain</td></tr><tr><td>Rate-limit hit</td><td>Redis sliding-window counter ≥ threshold</td><td>429 + <code>Retry-After</code> header + <code>ten.signup_rate_limited</code></td><td>User waits + retries; severe abuse → squatting block</td></tr><tr><td>OTP delivery fails (email service down)</td><td>TASK-EMAIL-001 returns error</td><td>503 + <code>otp_send_failed</code>; session state remains 'started'; user can resend after rate-limit window</td><td>Email service recovers; retry within 10min OTP window or new OTP</td></tr><tr><td>OTP TTL expired</td><td>Redis key not found at verify</td><td>410 + <code>otp_expired</code>; user requests new OTP</td><td>Resend OTP (counts against otp_send rate limit)</td></tr><tr><td>OTP max attempts (5)</td><td>counter in Redis hits 5</td><td>429 + <code>otp_max_attempts</code>; OTP invalidated</td><td>New OTP send required</td></tr><tr><td>Slug race (two tenants claim same slug concurrently)</td><td>Postgres unique index on <code>tenants.slug</code> rejects 2nd INSERT</td><td>409 + suggestions; session.state='abandoned' reason='slug_collision'</td><td>User picks alternate slug; resume from slug-check step</td></tr><tr><td>Stripe SetupIntent decline (card declined)</td><td>Stripe API returns <code>payment_intent.payment_failed</code></td><td>402 + <code>payment_declined</code> + Stripe decline_code; no tenant created</td><td>User updates card + retries; signup_complete reattempt with new intent</td></tr><tr><td>Stripe API 5xx during confirm</td><td>api_client retry exhausted (TASK-TEN-003 §1 #15)</td><td>502 + <code>stripe_unavailable</code>; session.state remains 'email_verified'; user can retry</td><td>Stripe recovers; idempotency key prevents double-charge on retry</td></tr><tr><td>TEN-001 provisioning fails after Stripe confirm</td><td>provision_tenant returns error inside tx</td><td>Stripe SetupIntent voided + tx rolled back + session.state='rolled_back' reason='provisioning_failed'</td><td>Operator investigates; user contacted via billing_contact_email</td></tr><tr><td>AUTH root-admin creation fails inside the atomic tx</td><td>create_subject returns error WITHIN the Phase-C tx</td><td><code>ROLLBACK</code> undoes tenant + consent INSERTs (no tenant exists post-rollback); Stripe SetupIntent voided; signup_session.state='rolled_back' reason='root_admin_create_failed'; sev-1 logged</td><td>Operator investigates; user contacted via billing_contact_email; TASK-TEN-104 NOT invoked (no tenant to terminate)</td></tr><tr><td>Welcome email send fails</td><td>TASK-EMAIL-001 returns error post-commit</td><td>signup is COMPLETE (tx already committed); email retry job runs background; user can request resend via /v1/signup/resend-welcome</td><td>Email recovers; user logs in via JWT in response anyway</td></tr><tr><td>Same-email duplicate signup</td><td>unique index check on tenants.billing_contact_email_hash16</td><td>409 + magic-link email sent; no second tenant</td><td>User signs into existing tenant via magic link</td></tr><tr><td>OIDC ID token signature invalid</td><td>TASK-AUTH-104 verification fails</td><td>401 + <code>oidc_invalid_token</code>; session.state remains 'started'</td><td>User retries OIDC flow</td></tr><tr><td>OIDC email_verified=false (IdP says email not verified)</td><td>claim check</td><td>400 + <code>oidc_email_unverified</code>; fall back to OTP path</td><td>User verifies email at IdP, retries</td></tr><tr><td>GeoIP DB out of date / IP not resolvable</td><td>MaxMind returns Unknown</td><td>Default to USD/us-1; user can override</td><td>Refresh weekly; manual override always available</td></tr><tr><td>Consent missing for VND tenant (PDPL VN)</td><td>validate_consents check</td><td>400 + <code>missing_pdpl_vn_consent</code>; signup blocked</td><td>User checks the PDPL consent box and retries</td></tr><tr><td>Enterprise plan_tier attempted via API</td><td>plan_tier == Enterprise check at complete</td><td>403 + <code>enterprise_requires_sales_contact</code></td><td>User selects Starter/Team; contacts sales for Enterprise</td></tr><tr><td>Squatting: 5 abandons from same IP in 24h</td><td>abuse_guard counter</td><td>429 + <code>abuse_detected_24h_cooloff</code> for 24h</td><td>Auto-expires at 24h; legitimate users contact support for manual unblock</td></tr><tr><td>Redis unavailable</td><td>rate_limit + OTP storage fail</td><td>503 + <code>signup_temporarily_unavailable</code></td><td>Redis recovers; signup retries auto-resume</td></tr><tr><td>MaxMind GeoLite2 file missing/corrupt</td><td>geoip lookup throws</td><td>Default to USD/us-1 + sev-2 alert <code>ten.signup_geoip_unavailable</code> (not in 9-kind core)</td><td>Weekly refresh job re-fetches; manual refresh CLI</td></tr><tr><td>Turnstile site key misconfig</td><td>every verify returns false</td><td>Universal 400 on signup_start; sev-1 alert</td><td>Operator updates env var; restart</td></tr><tr><td>signup_session_id reuse across users (race / leak)</td><td>Postgres unique index on signup_session_id</td><td>409 + <code>session_id_collision</code></td><td>User generates new UUIDv7 (client-side); retries</td></tr><tr><td>RLS misconfig pre-tenant</td><td>session row inaccessible despite system_tenant scope</td><td>Handler logs <code>auth.tenant_id</code> mismatch + 500</td><td>Inspect handler's SET LOCAL chain; fix middleware</td></tr><tr><td>Email service marked the OTP as spam</td><td>OTP not received by user</td><td>User clicks resend; rate-limited at 3/min/email</td><td>Whitelist sender domain at IdP-side / use known transactional domains</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> UUIDv7 is chosen for <code>signup_session_id</code> so that session_id ordering is rough wall-clock; allows memory audit + signup_sessions to be sorted by id for analytics without an additional timestamp column.</p>\n<p><strong>§11.2</strong> The Turnstile site key is per-environment (test/staging/prod); the verify endpoint URL <code>https://challenges.cloudflare.com/turnstile/v0/siteverify</code> is the same. Verification is a POST with <code>secret</code> + <code>response</code> + optional <code>remoteip</code>.</p>\n<p><strong>§11.3</strong> OTP HMAC secret rotates quarterly via KMS-managed key; rotation is overlap-safe (old + new accepted for 10-min OTP TTL after rotation).</p>\n<p><strong>§11.4</strong> The MaxMind GeoLite2 database is ~70MB; loaded into memory at handler startup via <code>maxminddb</code> crate. Refresh job (weekly) writes a new file then atomically swaps the in-memory pointer.</p>\n<p><strong>§11.5</strong> The disposable email blocklist initial seed: <code>disposable-email-domains</code> GitHub repo at pinned commit. Refresh job (monthly) fetches latest + diffs against current; writes a <code>disposable_email_blocklist_refresh</code> ledger row.</p>\n<p><strong>§11.6</strong> Rate-limit Redis keys: <code>rl:signup_start:ip:&lt;ip_hash16&gt;:&lt;window&gt;</code>, <code>rl:otp_send:email:&lt;email_hash16&gt;:&lt;window&gt;</code>, etc. Sliding-window uses Redis sorted sets with score=timestamp; trim on insert.</p>\n<p><strong>§11.7</strong> The <code>signup_30s_sla_test</code> uses Stripe test-mode against the real Stripe API (not wiremock) to ensure realistic network latency is in scope. CI runs it nightly, not on every PR.</p>\n<p><strong>§11.8</strong> OIDC <code>email_verified</code> claim is checked against both <code>email_verified: true</code> AND IdP issuer ∈ trusted list (Google + Microsoft). Other IdPs (Apple, Facebook) ship in slice 2.</p>\n<p><strong>§11.9</strong> The duplicate-email check (DEC-842 + §1 #14 step 5) uses <code>billing_contact_email_hash16</code> not the full email — same-email check is hash-equivalence. The hash includes a global salt to prevent rainbow-table enumeration.</p>\n<p><strong>§11.10</strong> The OTP code is 6 digits = ~20-bit entropy; 10-min TTL × 5 attempts = ~3.3e6 brute attempts needed on average vs 6 attempts = effectively unguessable.</p>\n<p><strong>§11.11</strong> Slug suggestions on collision: 50 = generous; UI shows 5 initially with \"more\" toggle. Generation: append numeric suffix (1-99), or append a 3-letter consonant tuple (acme-co, acme-hq, acme-inc, etc.).</p>\n<p><strong>§11.12</strong> The <code>redirect_url</code> in the complete response is the trusted post-signup destination; the JWT in the response is a temporary first-login token (24h TTL) with <code>signup_first_login</code> scope. The user lands on <code>&lt;slug&gt;.cyberos.world/onboarding/welcome</code> which clears the temp token + issues the normal long-lived session.</p>\n<p><strong>§11.13</strong> The welcome email's 7-day expiry is enforced at the <code>&lt;slug&gt;.cyberos.world/onboarding/welcome</code> route handler — <code>?token=&lt;one-time-token&gt;</code> query parameter checked + the token TTL is 7d in Postgres. Expired → 404 with link to standard login.</p>\n<p><strong>§11.14</strong> The PII scrub job (§1 #21) runs daily at 03:00 UTC via the same scheduled-job framework as TASK-TEN-003's TTL pruner. Scrubbed rows retain analytical value (funnel state, geoip_country) but no longer carry personally identifying data.</p>\n<p><strong>§11.15</strong> The <code>cyberos_consent_writer</code> role (§3.1 grant) is held only by the consent-withdrawal endpoint (slice 2, task-TEN-1xx); slice 1 has no withdrawal flow yet, so the role exists with no live grants beyond the schema permission.</p>\n<p><strong>§11.16</strong> The signup flow is the FIRST place where <code>signup_session_id</code> is generated; the client (frontend SPA) generates a UUIDv7 at page load and reuses across all 5 endpoints. Server never generates session_ids — this makes the flow resumable across browser reloads.</p>\n<p><strong>§11.17</strong> Stripe SetupIntent vs PaymentIntent: SetupIntent captures the payment method for FUTURE charges (subscription billing); PaymentIntent charges immediately. We use SetupIntent because subscription billing in TASK-TEN-003 starts at next billing cycle (or pro-rata immediate but as a Stripe Subscription invoice, not a one-off charge).</p>\n<p><strong>§11.18</strong> The 30-second SLO breakdown: ~3s Turnstile verify, ~1s OTP send (email queue ack), ~10s user fills email/slug/plan/payment, ~5s Stripe Elements card capture + 3DS, ~3s signup_complete server work (Stripe confirm + TEN-001 provision + AUTH + JWT). Buffer = 8s.</p>\n<p><strong>§11.19</strong> The form is intentionally single-page (not multi-page wizard) to minimize navigation cost. Five \"steps\" are visual sections with smooth scroll; submit is all-at-once at the end.</p>\n<p><strong>§11.20</strong> Test fixtures for <code>signup_30s_sla_test</code> use Stripe test cards that auto-succeed (no 3DS challenge); production users hit 3DS on ~5-10% of cards which adds ~5s. The 30s SLO is measured with 3DS-skip; the realistic-3DS SLO is 40s (informally tracked).</p>\n<p><strong>§11.21</strong> The system tenant UUID <code>00000000-0000-0000-0000-000000000001</code> is reserved by TASK-AUTH-001 §3.2 (per TASK-AUTH-001's DEC entry) — not invented here. This task consumes the well-known sentinel.</p>\n<p><strong>§11.22</strong> Welcome email content includes: tenant slug, login URL, JWT-as-magic-link (24h TTL), root-admin's auto-generated password (one-time display), short onboarding checklist. The password lives in the email body which is sensitive — TASK-EMAIL-001 enforces TLS-only delivery + DKIM + SPF.</p>\n<p><strong>§11.23</strong> Browser feature detection: form requires JavaScript + cookies + WebCrypto (for Turnstile + Stripe Elements). Browsers without are shown a 503 \"Self-serve signup requires a modern browser; contact sales@cyberos.world.\"</p>\n<p><strong>§11.24</strong> Analytics emission: the 5 funnel stages (start, otp_verified, payment_captured, provisioned, completed) emit OTel events with attribute <code>funnel_stage</code>; drop-off analysis lives in OBS (TASK-OBS-005).</p>\n<p><strong>§11.25</strong> Locale handling: <code>locale_hint</code> from the client (Accept-Language header default + override) drives consent locale + welcome email locale + UI strings. Supported at slice 1: <code>en-US</code>, <code>vi-VN</code>; slice 2 adds <code>zh-SG</code>, <code>de-DE</code>, <code>task-task</code>, <code>es-ES</code>.</p>\n<p><strong>§11.26</strong> The form submits to our backend over HTTPS only; HSTS preload + Content-Security-Policy <code>default-src 'self'; script-src 'self' https://challenges.cloudflare.com https://js.stripe.com</code> prevents MitM + XSS. Stripe Elements iframes are sandboxed.</p>\n<p><strong>§11.27</strong> The deeplink at <code>&lt;slug&gt;.cyberos.world/onboarding/welcome</code> is signed with HMAC + nonce; sharing the link does not let a third party log in (token is single-use per DEC-845).</p>\n<hr>\n<p><em>End of TASK-TEN-101 spec.</em></p>";
