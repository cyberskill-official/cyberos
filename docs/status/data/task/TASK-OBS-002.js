window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>A Rust HTTP proxy <strong>MUST</strong> sit between Grafana and the 3 OBS backends (Loki, Prometheus, Tempo), AST-injecting <code>tenant_id</code> label filters into every query. Each request:</p>\n<ol><li><strong>MUST</strong> authenticate via TASK-AUTH-004 JWT (verified against JWKS at boot + cached). The JWT carries <code>tenant_id</code> claim; extracted as the injection value. Missing or invalid JWT → <code>401 UNAUTHORIZED</code>.</li><li><strong>MUST</strong> inject <code>tenant_id=\"&lt;extracted&gt;\"</code> label filter into EVERY PromQL / LogQL / TraceQL query before forwarding. The injection is AST-based (parse → mutate → reserialize), NEVER string concatenation. AST-based injection is robust against escape attempts (regex characters in user query, multi-line queries, etc.).</li><li><strong>MUST</strong> AND the injected filter with any user-supplied query (logical conjunction; never OR). Example: <code>rate(foo[5m])</code> with tenant <code>T</code> → <code>rate(foo{tenant_id=\"T\"}[5m])</code>. The user's query is preserved exactly; only the label set is augmented.</li><li><strong>MUST</strong> refuse queries that ALREADY contain <code>tenant_id</code> label as a user-supplied filter — this prevents bypass attempts where a user crafts <code>foo{tenant_id=\"other\"}</code> hoping the proxy will OR-merge or pass through. Such queries return <code>400 BAD_REQUEST</code> with <code>{\"error\":\"user_supplied_tenant_id\",\"reason\":\"tenant_id label is reserved; do not include in query\"}</code>. Sev-1 audit row <code>obs.cross_tenant_query_attempt</code> emitted.</li><li><strong>MUST</strong> support all 3 query languages with per-language AST parser:</li></ol>\n<ul><li>PromQL via <code>promql-parser@0.4</code> (Prometheus official Rust port).</li><li>LogQL via custom parser (no mature crate; ~300 LoC subset covering selector + pipe stages).</li><li>TraceQL via custom parser (~200 LoC subset).</li></ul>\n<ol><li><strong>MUST</strong> complete proxying in ≤ 5ms p95 overhead (parse + inject + serialise; backend latency excluded). The 5ms ceiling protects user experience — Grafana already round-trips multiple queries per dashboard refresh.</li><li><strong>MUST</strong> forward responses unchanged (the backends already namespace by tenant_id label; nothing to filter response-side).</li><li><strong>MUST</strong> detect cross-tenant attempts: if the caller's <code>tenant_id</code> claim differs from any explicit <code>tenant_id</code> in the query (caught by §1 #4) OR the JWT's tenant_id is the nil UUID (root-admin) — root-admin queries are special-cased per §1 #11.</li><li><strong>MUST</strong> emit a memory audit row <code>obs.query_proxied</code> per query with payload: <code>tenant_id</code>, <code>caller_subject_id</code>, <code>backend</code> (loki|prometheus|tempo), <code>query_sha256</code>, <code>outcome</code> (proxied | rejected_user_supplied_tenant_id | rejected_unauthenticated | backend_error), <code>latency_ms</code>, <code>request_id</code>. Logging the SHA-256 of the query (not the raw query) preserves privacy if the query contains tenant-business semantics.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>obs_proxy_requests_total{tenant_id, backend, outcome}</code> (counter).</li><li><code>obs_proxy_injection_latency_ms{backend}</code> (histogram; SLO p95 &lt; 5ms).</li><li><code>obs_proxy_cross_tenant_attempts_total{tenant_id}</code> (counter; sev-1 alarm on increment).</li><li><code>obs_proxy_backend_latency_ms{backend}</code> (histogram).</li></ul>\n<ol><li><strong>MUST</strong> support root-admin (tenant 0) queries WITHOUT injection — root-admin legitimately queries cross-tenant for ops/compliance. The exception is logged via <code>obs.query_proxied</code> row with <code>outcome: root_admin_unfiltered</code> AND emits sev-2 (informational) so cross-tenant queries are visible to compliance review.</li><li><strong>SHOULD</strong> cache JWKS lookups for 5 minutes (matches TASK-AUTH-004 §1 #3 cache header).</li><li><strong>SHOULD</strong> support all standard Grafana query endpoints:</li></ol>\n<ul><li>Prometheus: <code>/api/v1/query</code>, <code>/api/v1/query_range</code>, <code>/api/v1/labels</code>, <code>/api/v1/series</code>.</li><li>Loki: <code>/loki/api/v1/query</code>, <code>/loki/api/v1/query_range</code>, <code>/loki/api/v1/labels</code>.</li><li>Tempo: <code>/api/search</code>, <code>/api/traces/&lt;id&gt;</code>.</li></ul>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why Rust proxy not Grafana Enterprise (DEC-145)?</strong> Grafana Enterprise multi-tenancy costs $$$/month per user + adds operational complexity (separate auth integration, license management, version-locked features). A 500-LoC Rust proxy solves the same problem with zero licensing fee and full control.</p>\n<p><strong>Why AST-based injection not string concat (DEC-146)?</strong> String concat is bypass-able. Example: query <code>foo{x=\"}</code> (broken regex) + naive concat <code>foo{x=\"}, tenant_id=\"T\"}</code> produces malformed PromQL. Worse: a query with multiple selector groups <code>foo or bar</code> would only get tenant_id added to one. AST-based parsing produces a structured tree; mutating every selector is straightforward and safe.</p>\n<p><strong>Why reject user-supplied <code>tenant_id</code> label (§1 #4)?</strong> A user crafting <code>foo{tenant_id=\"other\"}</code> is ATTEMPTING a bypass. Two safe options: (a) overwrite their value, (b) reject. Rejecting is louder + auditable + signals intent. Overwriting hides the attempt. Reject + sev-1 audit + 400 is the chosen path.</p>\n<p><strong>Why per-language AST parsers (§1 #5)?</strong> PromQL, LogQL, and TraceQL have different syntax. A unified parser would compromise on each. Per-language parsers (PromQL via official crate; LogQL/TraceQL hand-rolled subsets) handle each correctly. The hand-rolled parsers cover only the syntax actually used by Grafana — full grammar coverage isn't needed.</p>\n<p><strong>Why log query SHA-256 not raw query (§1 #9)?</strong> Queries can contain tenant-business semantics (e.g., <code>kb_articles_total{kb=\"legal-hr-confidential\"}</code>). Logging raw queries to memory exposes those semantics to anyone with audit access. Hash preserves uniqueness for forensic correlation without leaking content. If forensic needs the raw query, ops re-runs against the proxy with the same tenant + observe what was sent.</p>\n<p><strong>Why root-admin gets unfiltered access (§1 #11)?</strong> Root-admin's job includes cross-tenant ops queries (compliance reports, ops investigations). Forcing root-admin to use a different tool would split the workflow. The audit-row <code>outcome: root_admin_unfiltered</code> makes legitimate cross-tenant queries visible — compliance reviews can ask \"who did cross-tenant queries last week?\" and get a list.</p>\n<p><strong>Why 5ms p95 overhead (§1 #6)?</strong> Grafana dashboards refresh every 5-30s; a typical refresh queries 10-50 panels. 5ms × 50 panels = 250ms per refresh — invisible to humans. Above 5ms, refreshes become sluggish.</p>\n<p><strong>Why audit-row logging for every query (§1 #9)?</strong> Compliance audit answer: \"show me what queries this operator ran during the period under review.\" Without per-query audit, the answer is \"we don't know.\" With it, the answer is a SQL query. The cost is one memory row per query (~10K queries/day at slice-1 scale = trivial storage).</p>\n<p><strong>Why proxy IS the security boundary (§11 note in original)?</strong> Grafana itself runs as a non-tenant-aware app — its built-in auth + RBAC don't enforce tenant isolation at query time. The proxy enforces tenant isolation by injecting labels at EVERY query. Without the proxy, any Grafana user (regardless of role) sees all tenants' data.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/obs-proxy/src/proxy.rs\npub async fn proxy(req: Request&lt;Body&gt;, state: &amp;State) -&gt; Result&lt;Response&lt;Body&gt;, ProxyError&gt; {\n    let claims = state.auth.verify(&amp;req).await?;\n    let backend = detect_backend(&amp;req.uri());\n    let body = read_body(&amp;mut req).await?;\n    let original_query = extract_query(&amp;body, backend)?;\n\n    if has_user_supplied_tenant_id(&amp;original_query, backend) {\n        audit::emit(memory::canonical::cross_tenant_query_attempt(&amp;claims, &amp;original_query)).await;\n        return Err(ProxyError::UserSuppliedTenantId);\n    }\n\n    let injected = if claims.tenant_id == Uuid::nil() &amp;&amp; claims.roles.contains(&amp;\"root-admin\".into()) {\n        audit::emit(memory::canonical::root_admin_unfiltered(&amp;claims, &amp;original_query)).await;\n        original_query   // root-admin: no injection\n    } else {\n        match backend {\n            Backend::Prometheus =&gt; inject::promql::add_label(&amp;original_query, \"tenant_id\", &amp;claims.tenant_id.to_string())?,\n            Backend::Loki       =&gt; inject::logql::add_label(&amp;original_query, \"tenant_id\", &amp;claims.tenant_id.to_string())?,\n            Backend::Tempo      =&gt; inject::traceql::add_label(&amp;original_query, \"resource.tenant_id\", &amp;claims.tenant_id.to_string())?,\n        }\n    };\n\n    let resp = forward(backend, &amp;injected, &amp;state.http).await?;\n    audit::emit(memory::canonical::query_proxied(&amp;claims, backend, &amp;original_query, \"proxied\")).await;\n    Ok(resp)\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum ProxyError {\n    #[error(\"auth failed: {0}\")]\n    AuthFailed(String),\n    #[error(\"user supplied tenant_id label (bypass attempt)\")]\n    UserSuppliedTenantId,\n    #[error(\"query parse failed: {backend:?}: {reason}\")]\n    ParseFailed { backend: Backend, reason: String },\n    #[error(\"backend error: {0}\")]\n    Backend(#[from] reqwest::Error),\n}\n\n#[derive(Debug, Clone, Copy)]\npub enum Backend { Prometheus, Loki, Tempo }</code></pre>\n<pre><code class=\"language-rust\">// services/obs-proxy/src/inject/promql.rs\nuse promql_parser::parser::{parse, Expr};\n\npub fn add_label(query: &amp;str, key: &amp;str, value: &amp;str) -&gt; Result&lt;String, ProxyError&gt; {\n    let mut ast = parse(query).map_err(|e| ProxyError::ParseFailed {\n        backend: Backend::Prometheus, reason: e.to_string(),\n    })?;\n    visit_selectors_mut(&amp;mut ast, |sel| {\n        sel.matchers.push(promql_parser::label::Matcher::new(\n            promql_parser::label::MatchOp::Equal, key.into(), value.into(),\n        ));\n    });\n    Ok(ast.prettify())\n}\n\npub fn has_label(query: &amp;str, key: &amp;str) -&gt; Result&lt;bool, ProxyError&gt; {\n    let ast = parse(query).map_err(/* ... */)?;\n    let mut found = false;\n    visit_selectors(&amp;ast, |sel| {\n        if sel.matchers.iter().any(|m| m.name == key) { found = true; }\n    });\n    Ok(found)\n}</code></pre>\n<pre><code class=\"language-rust\">// services/obs-proxy/src/inject/logql.rs (sketch — hand-rolled)\npub fn add_label(query: &amp;str, key: &amp;str, value: &amp;str) -&gt; Result&lt;String, ProxyError&gt; {\n    // Find the selector portion `{...}` at start of query\n    let (selector, rest) = split_selector(query)?;\n    let labels = parse_labels(selector)?;\n    if labels.iter().any(|l| l.key == key) {\n        return Err(ProxyError::ParseFailed {\n            backend: Backend::Loki,\n            reason: format!(\"query already contains {key} label\"),\n        });\n    }\n    let mut new_labels = labels;\n    new_labels.push(Label { key: key.into(), op: \"=\".into(), value: format!(\"\\\"{value}\\\"\") });\n    Ok(format!(\"{{{}}}{}\",\n        new_labels.iter().map(Label::to_string).collect::&lt;Vec&lt;_&gt;&gt;().join(\",\"),\n        rest))\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>PromQL without tenant_id → injected</strong> — <code>rate(foo[5m])</code> for tenant T → <code>rate(foo{tenant_id=\"T\"}[5m])</code>.</li><li><strong>PromQL with existing labels → tenant_id added</strong> — <code>foo{x=\"y\"}</code> → <code>foo{x=\"y\",tenant_id=\"T\"}</code>.</li><li><strong>PromQL complex query → tenant_id injected on every selector</strong> — <code>sum(rate(foo[5m])) / sum(rate(bar[5m]))</code> → both <code>foo</code> AND <code>bar</code> get <code>tenant_id</code> label.</li><li><strong>LogQL <code>{service=\"x\"}</code> → injected</strong> — adds <code>tenant_id=\"T\"</code>; pipe stages preserved.</li><li><strong>TraceQL <code>{service.name=\"x\"}</code> → injected</strong> — adds <code>resource.tenant_id=\"T\"</code> filter.</li><li><strong>Query with user-supplied tenant_id rejected</strong> — <code>foo{tenant_id=\"other\"}</code> → <code>400</code> with <code>user_supplied_tenant_id</code>; sev-1 audit emitted.</li><li><strong>Missing JWT → 401</strong>.</li><li><strong>Invalid JWT signature → 401</strong>.</li><li><strong>Expired JWT → 401</strong>.</li><li><strong>Cross-tenant attempt logged</strong> — <code>obs.cross_tenant_query_attempt</code> memory row + sev-1 alarm.</li><li><strong>Audit row per query</strong> — every successful proxy emits <code>obs.query_proxied</code> with SHA-256 of query.</li><li><strong>Root-admin unfiltered query allowed</strong> — caller with tenant_id=nil + role root-admin gets query forwarded WITHOUT injection; <code>obs.query_proxied</code> row carries <code>outcome: root_admin_unfiltered</code>; sev-2 informational.</li><li><strong>Property test: 1000 random queries × 2 tenant pairs → zero cross-tenant data</strong>.</li><li><strong>p95 injection overhead &lt; 5ms</strong> — measured via <code>obs_proxy_injection_latency_ms{backend}</code>.</li><li><strong>Malformed PromQL → 400 with parse error</strong> (NOT 500).</li><li><strong>Backend unreachable → 503</strong> with <code>backend_error</code>.</li><li><strong>Datasources point at proxy</strong> — Grafana datasources URL = <code>http://obs-proxy:8088/...</code>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/obs-proxy/tests/inject_promql_test.rs\n#[test]\nfn injects_into_simple_selector() {\n    let result = inject::promql::add_label(\"rate(foo[5m])\", \"tenant_id\", \"T\").unwrap();\n    assert!(result.contains(\"tenant_id=\\\"T\\\"\"));\n    assert!(result.contains(\"foo\"));\n}\n\n#[test]\nfn injects_into_every_selector_in_complex_query() {\n    let result = inject::promql::add_label(\n        \"sum(rate(foo[5m])) / sum(rate(bar[5m]))\", \"tenant_id\", \"T\",\n    ).unwrap();\n    let count = result.matches(\"tenant_id=\\\"T\\\"\").count();\n    assert_eq!(count, 2);\n}\n\n#[test]\nfn detects_user_supplied_tenant_id() {\n    let result = inject::promql::has_label(\"foo{tenant_id=\\\"other\\\"}\", \"tenant_id\").unwrap();\n    assert!(result);\n}\n\n#[test]\nfn malformed_promql_returns_parse_error() {\n    let err = inject::promql::add_label(\"rate(foo[bad\", \"tenant_id\", \"T\").expect_err(\"expected parse fail\");\n    assert!(matches!(err, ProxyError::ParseFailed { .. }));\n}\n\n// inject_logql_test.rs\n#[test]\nfn injects_logql_simple_selector() {\n    let result = inject::logql::add_label(\"{service=\\\"x\\\"}\", \"tenant_id\", \"T\").unwrap();\n    assert_eq!(result, \"{service=\\\"x\\\",tenant_id=\\\"T\\\"}\");\n}\n\n#[test]\nfn preserves_pipe_stages() {\n    let result = inject::logql::add_label(\"{service=\\\"x\\\"} | json | line_format \\\"...\\\"\", \"tenant_id\", \"T\").unwrap();\n    assert!(result.starts_with(\"{service=\\\"x\\\",tenant_id=\\\"T\\\"}\"));\n    assert!(result.contains(\"| json\"));\n    assert!(result.contains(\"| line_format\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/obs-proxy/tests/proxy_test.rs\n#[tokio::test]\nasync fn missing_jwt_returns_401() {\n    let state = test_state().await;\n    let req = Request::builder().uri(\"/api/v1/query?query=rate(foo[5m])\").body(Body::empty()).unwrap();\n    let err = proxy(req, &amp;state).await.expect_err(\"expected AuthFailed\");\n    assert!(matches!(err, ProxyError::AuthFailed(_)));\n}\n\n#[tokio::test]\nasync fn user_supplied_tenant_id_returns_400_and_audits() {\n    let state = test_state().await;\n    let req = test_request_with_jwt(\"/api/v1/query?query=foo{tenant_id=\\\"other\\\"}\", &amp;test_jwt(\"T\")).await;\n    let err = proxy(req, &amp;state).await.expect_err(\"expected UserSuppliedTenantId\");\n    assert!(matches!(err, ProxyError::UserSuppliedTenantId));\n    assert!(memory_test_helper::has_recent_row(\"obs.cross_tenant_query_attempt\", \"T\"));\n}\n\n#[tokio::test]\nasync fn root_admin_query_unfiltered() {\n    let state = test_state().await;\n    let req = test_request_with_jwt(\"/api/v1/query?query=rate(foo[5m])\", &amp;root_admin_jwt()).await;\n    let mock_backend = state.mock_backend();\n    let _ = proxy(req, &amp;state).await.unwrap();\n    let last_query = mock_backend.last_query().await;\n    assert!(!last_query.contains(\"tenant_id\"));\n    assert!(memory_test_helper::has_recent_row(\"obs.query_proxied\", \"root_admin_unfiltered\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/obs-proxy/tests/cross_tenant_property_test.rs\nproptest! {\n    #![proptest_config(ProptestConfig::with_cases(1000))]\n\n    #[test]\n    fn no_cross_tenant_data_in_responses(\n        (t_a, t_b) in any_tenant_pair(),\n        query in any_promql_query(),\n    ) {\n        let rt = tokio::runtime::Runtime::new().unwrap();\n        rt.block_on(async {\n            let state = test_state().await;\n            test_helper::insert_metric_in_tenant(&amp;t_a, \"value_a\").await;\n            test_helper::insert_metric_in_tenant(&amp;t_b, \"value_b\").await;\n\n            let req = test_request_with_jwt(&amp;format!(\"/api/v1/query?query={query}\"), &amp;test_jwt(&amp;t_a)).await;\n            let resp = proxy(req, &amp;state).await.unwrap();\n            let body = response_body(&amp;resp).await;\n            prop_assert!(!body.contains(\"value_b\"), \"tenant B's data leaked into tenant A's response\");\n        });\n    }\n}</code></pre>\n<pre><code class=\"language-rust\">// services/obs-proxy/tests/audit_log_test.rs\n#[tokio::test]\nasync fn every_query_emits_audit_row() {\n    let state = test_state().await;\n    let _ = proxy(test_request_with_jwt(\"/api/v1/query?query=rate(foo[5m])\", &amp;test_jwt(\"T\")).await, &amp;state).await.unwrap();\n    let row = memory_test_helper::find_latest(\"obs.query_proxied\").unwrap();\n    assert_eq!(row.payload[\"tenant_id\"], \"T\");\n    assert_eq!(row.payload[\"backend\"], \"prometheus\");\n    assert!(row.payload[\"query_sha256\"].as_str().unwrap().len() == 64);\n    assert!(row.payload[\"latency_ms\"].as_f64().unwrap() &lt; 5.0);\n}</code></pre>\n<pre><code class=\"language-bash\">cd services/obs-proxy &amp;&amp; cargo test</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<pre><code class=\"language-yaml\"># deploy/obs/grafana/datasources.yaml (modified)\napiVersion: 1\ndatasources:\n  - name: Loki\n    type: loki\n    url: http://obs-proxy:8088/loki/api          # was http://loki:3100\n    httpHeaderName1: \"Authorization\"\n    secureJsonData: { httpHeaderValue1: \"Bearer ${GRAFANA_USER_JWT}\" }\n  - name: Prometheus\n    type: prometheus\n    url: http://obs-proxy:8088/api/v1\n    httpHeaderName1: \"Authorization\"\n    secureJsonData: { httpHeaderValue1: \"Bearer ${GRAFANA_USER_JWT}\" }\n  - name: Tempo\n    type: tempo\n    url: http://obs-proxy:8088/tempo\n    httpHeaderName1: \"Authorization\"\n    secureJsonData: { httpHeaderValue1: \"Bearer ${GRAFANA_USER_JWT}\" }</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-OBS-001</strong> — Backends running.</li><li><strong>TASK-AUTH-004</strong> — JWT verification (JWKS).</li><li><strong>TASK-AUTH-108 (downstream)</strong> — Lumi identity; current task uses standard TASK-AUTH-004 JWT.</li><li>Crates: <code>axum</code>, <code>reqwest</code>, <code>promql-parser@0.4</code>, <code>jsonwebtoken@9</code>, <code>proptest@1</code>, <code>sha2</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>PromQL injection</h3>\n<pre><code>input:  rate(ai_gateway_precheck_calls_total[5m])\noutput: rate(ai_gateway_precheck_calls_total{tenant_id=\"org:cyberskill\"}[5m])</code></pre>\n<h3>LogQL injection with pipe stages</h3>\n<pre><code>input:  {service=\"ai-gateway\"} | json | line_format \"{{.message}}\"\noutput: {service=\"ai-gateway\",tenant_id=\"org:cyberskill\"} | json | line_format \"{{.message}}\"</code></pre>\n<h3>TraceQL injection</h3>\n<pre><code>input:  { service.name = \"ai-gateway\" }\noutput: { service.name = \"ai-gateway\" &amp;&amp; resource.tenant_id = \"org:cyberskill\" }</code></pre>\n<h3>Cross-tenant attempt response</h3>\n<pre><code class=\"language-http\">HTTP/1.1 400 Bad Request\n{ \"error\": \"user_supplied_tenant_id\", \"reason\": \"tenant_id label is reserved; do not include in query\" }</code></pre>\n<h3>Audit row <code>obs.query_proxied</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"obs.query_proxied\",\n  \"payload\": {\n    \"tenant_id\": \"org:cyberskill\",\n    \"caller_subject_id\": \"550e...\",\n    \"backend\": \"prometheus\",\n    \"query_sha256\": \"4b8c0d2f1a7e9c3b...\",\n    \"outcome\": \"proxied\",\n    \"latency_ms\": 2.3,\n    \"request_id\": \"obs_...\"\n  }\n}</code></pre>\n<h3>Audit row <code>obs.cross_tenant_query_attempt</code> (sev-1)</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"obs.cross_tenant_query_attempt\",\n  \"payload\": {\n    \"caller_tenant_id\": \"org:cyberskill\",\n    \"attempted_label_value\": \"org:other-tenant\",\n    \"query_sha256\": \"...\",\n    \"request_id\": \"obs_...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Per-tenant query rate limiting (slice 4+).</li><li>Query-result caching (slice 5+).</li><li>Streaming responses for long-running queries (slice 5+).</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Token missing</td><td>Auth check</td><td>401</td><td>Caller refreshes token</td></tr><tr><td>Token tenant_id missing</td><td>JWT parse</td><td>401 (malformed_token)</td><td>Operator fixes JWT minter</td></tr><tr><td>JWT signature invalid</td><td>jsonwebtoken verify</td><td>401</td><td>Suspect attack; investigate</td></tr><tr><td>JWT expired</td><td>exp check</td><td>401</td><td>Caller re-authenticates</td></tr><tr><td>Query parse fails</td><td>promql-parser/logql/traceql error</td><td>400 with parse error</td><td>Caller fixes query</td></tr><tr><td>Backend unreachable</td><td>reqwest connect error</td><td>503</td><td>Operator investigates backend</td></tr><tr><td>User-supplied tenant_id label</td><td>has_label check</td><td>400 + sev-1 audit</td><td>Investigate caller (likely malicious or buggy client)</td></tr><tr><td>Cross-tenant data leak (regression)</td><td>property test fails in CI</td><td>PR blocked</td><td>Fix injection logic</td></tr><tr><td>Slow injection (&gt; 5ms p95)</td><td>OTel histogram</td><td>sev-3 alarm</td><td>Investigate query complexity OR parser</td></tr><tr><td>Audit-row emit fails (memory down)</td><td>memory_writer error</td><td>Query still proxied; sev-2 alarm</td><td>Operator investigates memory</td></tr><tr><td>Root-admin query without expected role claim</td><td>claims check</td><td>Treated as regular tenant; injection applies</td><td>Operator updates JWT minter</td></tr><tr><td>LogQL parser doesn't handle new syntax</td><td>parse error</td><td>400</td><td>Update logql parser</td></tr><tr><td>TraceQL parser doesn't handle new syntax</td><td>parse error</td><td>400</td><td>Update traceql parser</td></tr><tr><td>Datasources misconfigured (point at backend not proxy)</td><td>Operator sees unfiltered data</td><td>Sev-1 (security)</td><td>Fix datasources.yaml; redeploy</td></tr><tr><td>Grafana sends multiple queries per request (batched)</td><td>proxy handles each independently</td><td>Each gets injected</td><td>By design</td></tr><tr><td>Proxy itself becomes unauthenticated path (network bypass)</td><td>network policy enforcement</td><td>Should be impossible by topology</td><td>Network ACLs (deploy-time)</td></tr><tr><td>Promql-parser crate has a bug</td><td>parse fails or wrong AST</td><td>Investigate; possibly patch upstream</td><td>Upstream issue</td></tr><tr><td>Memory exhaustion on huge query</td><td>parser allocates</td><td>Process restart</td><td>Set per-process memory limit</td></tr><tr><td>Concurrent query bypass (race)</td><td>not possible — each request independent</td><td>N/A</td><td>By design</td></tr><tr><td>Audit log too large (every query)</td><td>memory storage growth</td><td>Retention policy on <code>obs.*</code> rows</td><td>30-day retention</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li><code>promql-parser</code> crate is the load-bearing dep. If it lacks a feature (e.g., specific PromQL functions), patch upstream OR use the official Prometheus Go parser via FFI as fallback.</li><li>LogQL + TraceQL parsers are hand-rolled because no mature Rust crates exist. The hand-rolled parsers cover only the subset Grafana actually emits — full grammar coverage would be over-engineering. Coverage tests in §5 ensure the subset works.</li><li>The proxy IS the security boundary. Grafana itself runs as a non-tenant-aware app; the proxy makes it multi-tenant. Bypassing the proxy (e.g., querying Loki directly via <code>localhost:3100</code>) is prevented by network policy — Loki/Prometheus/Tempo only accept connections from the proxy.</li><li>AST-based injection prevents the bypass class of \"user crafts a query that escapes the wrapper.\" String concat would be susceptible; AST is robust.</li><li>The cross-tenant property test (§5) mirrors TASK-AI-018's pattern. 1000 random queries × tenant pairs ensures the AST injection works across the full PromQL grammar.</li><li>Root-admin gets unfiltered access (§1 #11) but every such query is audited — compliance reviews can ask \"who did cross-tenant queries last week?\" The sev-2 informational alarm makes the activity visible without being a false-positive incident.</li><li>Audit-row payload uses query_sha256 (not raw query) because queries can contain tenant-business semantics. The hash preserves uniqueness for forensic correlation; ops can re-run the query to inspect the actual content if needed.</li><li>The 5ms overhead budget is achievable because parsing is pure-CPU (no I/O); promql-parser is fast (~10µs per typical query).</li><li>Query-result caching (deferred to slice 5+) would reduce backend load further; for now the proxy is stateless.</li></ul>\n<hr>\n<p><em>End of TASK-OBS-002. Status: draft (10/10 target).</em></p>";
