window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-116"]="<h1>TASK-SKILL-116: Vendor debugging-cycle + chain-coverage check</h1>\n<h2>§1 - Description</h2>\n<p>The ship workflow's 31-step <code>skill_chain</code> is the contract for what the payload must carry, but the vendored skill list in <code>build.sh</code> is a hand-maintained string that silently drifted from it. This task vendors the missing pair and adds a coverage check that fails the build whenever the two disagree.</p>\n<p>Normative clauses:</p>\n<ol><li>The vendored skill set in <code>build.sh</code> MUST include <code>debugging-cycle-author</code> and <code>debugging-cycle-audit</code>, copied into both <code>cuo/skills/</code> and <code>plugin/skills/</code> like the existing pairs.</li><li>A script <code>tools/cyberos-install/check-chain-coverage.sh &lt;payload-dir&gt;</code> MUST extract every skill referenced by the vendored workflow docs and verify each has a directory containing a <code>SKILL.md</code> in BOTH <code>&lt;payload&gt;/cuo/skills/</code> and <code>&lt;payload&gt;/plugin/skills/</code>. Extraction is defined per doc kind: <code>skill: &lt;name&gt;</code> entries in <code>&lt;payload&gt;/cuo/ship-tasks.md</code>'s skill_chain, and backtick-quoted tokens matching <code>[a-z0-9]+(-[a-z0-9]+)*-(author|audit)</code> in <code>&lt;payload&gt;/plugin/commands/*.md</code> (deterministic token grammar, not prose guessing). Any miss MUST exit 10 listing <code>MISSING &lt;skill&gt; (referenced by &lt;doc&gt;)</code> per line.</li><li>Exemptions MUST be declared in <code>tools/cyberos-install/chain-allowlist.txt</code> (one name + one <code># reason</code> per line; initial entries: <code>awh-gate</code>, <code>caf-gate</code>). An allowlist entry exempts its name from BOTH rule kinds - MISSING (script-backed steps with no skill dir) and UNPAIRED (intentionally single skills, e.g. the four NFR skills once TASK-CUO-209 vendors them) - the reason string says which. An allowlisted name that nothing references and no payload dir matches MUST warn (stderr) so the allowlist cannot rot silently.</li><li>The check MUST also enforce pair completeness: for every vendored <code>&lt;name&gt;-author</code> there MUST be a vendored <code>&lt;name&gt;-audit</code> and vice versa; non-allowlisted violations exit 10 as <code>UNPAIRED &lt;skill&gt;</code>.</li><li><code>build.sh</code> MUST run <code>check-chain-coverage.sh</code> against its own output as its final step and propagate a failure - a payload that under-covers its own workflow can no longer be produced. A reduced-profile payload (zero vendored skills - the documented doc-driven floor) is exempt: the check prints <code>chain SKIP: reduced profile</code> and exits 0; partial vendoring still fails. (Amended post-ship 2026-07-12: TASK-CUO-209 t07 surfaced that the check as first shipped broke the reduced floor.)</li><li>The check MUST be pure read-only over the payload dir (no repo access needed), so TASK-IMP-068's CI gate and TASK-IMP-069's release job get it for free via the build.</li></ol>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>The pair is vendored</strong> (§1 #1) - after <code>build.sh</code>, <code>debugging-cycle-author/SKILL.md</code> and <code>debugging-cycle-audit/SKILL.md</code> exist under both <code>cuo/skills/</code> and <code>plugin/skills/</code> in the payload.</li><li><strong>Chain extraction is real parsing, not a fixed list</strong> (§1 #2) - adding a fake <code>skill: nonexistent-author</code> line to the workflow doc in a scratch payload makes the check exit 10 naming <code>nonexistent-author</code> and the doc.</li><li><strong>A dropped pair fails the build</strong> (§1 #2, #5) - removing <code>debugging-cycle-author debugging-cycle-audit</code> from the vendored set makes <code>build.sh</code> itself exit non-zero with the two MISSING lines.</li><li><strong>Allowlist works both ways</strong> (§1 #3) - <code>awh-gate</code>/<code>caf-gate</code> produce no failure; an allowlist entry naming a skill no doc references produces a stderr warning and exit stays 0.</li><li><strong>Pair completeness</strong> (§1 #4) - a scratch payload with only <code>repo-context-map-author</code> present exits 10 with <code>UNPAIRED repo-context-map-author</code>.</li><li><strong>Read-only over the payload</strong> (§1 #6) - running the check from an empty cwd against a copied payload dir succeeds; the payload's mtimes/bytes are unchanged after a run.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/cyberos-install/tests/test_chain_coverage.sh\n# Builds one scratch payload, then mutates copies of it per case.\n\nt01_pair_vendored()              # AC 1\nt02_parses_chain_not_list()      # AC 2\nt03_dropped_pair_fails_build()   # AC 3  (patched build.sh in a temp checkout)\nt04_allowlist_both_ways()        # AC 4\nt05_unpaired_detected()          # AC 5\nt06_readonly_check()             # AC 6  (sha256 of payload tree before/after)</code></pre>\n<h2>§2 - Why this design</h2>\n<p>Deriving the vendored set FROM the chain automatically was considered and rejected: the payload legitimately carries skills no chain references yet (TASK-CUO-209 vendors the full SDP set), so the honest relation is \"chain is a subset of payload\", enforced by a checker, with an explicit allowlist for script-backed steps. Checking pairs (#4) in the same pass kills the sibling bug class (author without audit) at zero extra cost.</p>\n<h2>§3 - Contract</h2>\n<pre><code>check-chain-coverage.sh &lt;payload-dir&gt;\n  exit 0   chain covered, pairs complete (prints \"chain OK: N referenced, M vendored, K allowlisted\")\n  exit 10  MISSING &lt;skill&gt; (referenced by &lt;doc&gt;) | UNPAIRED &lt;skill&gt;   (one per line)\n  exit 2   payload dir or workflow doc unreadable</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>Extraction: <code>grep -Eo 'skill: *[a-z0-9-]+' &lt;doc&gt; | awk '{print $2}' | sort -u</code> over the chain doc + command docs; set-compare against <code>ls &lt;payload&gt;/cuo/skills/</code>; suffix swap for pair check. <code>build.sh</code> gains the pair in its set string and the final check invocation.</p>\n<h2>§7 - Dependencies</h2>\n<p>None upstream. Blocks TASK-CUO-209 (the expanded vendored set lands behind this checker). Related TASK-IMP-068 (gate runs the build, so this check rides along) and TASK-SKILL-118 (parity of pair CONTENTS; this task guarantees pair PRESENCE).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ bash tools/cyberos-install/check-chain-coverage.sh dist/cyberos\nMISSING debugging-cycle-author (referenced by cuo/ship-tasks.md)\nMISSING debugging-cycle-audit (referenced by cuo/ship-tasks.md)\n$ echo $?\n10</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. When TASK-CUO-209 vendors additional workflow docs, clause #2's doc list grows by construction (it scans <code>plugin/commands/*.md</code> and the vendored cuo workflow file set, not a hardcoded pair).</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Workflow doc renames its <code>skill:</code> key or format - extraction finds zero references; the check MUST treat \"0 referenced skills\" as exit 2 (structure changed under it), never as a pass.</li><li>Skill dir exists but is empty - presence test is <code>SKILL.md</code> inside the dir, not the dir itself.</li><li>Allowlist typo (<code>awhgate</code>) - the referenced <code>awh-gate</code> is then unmatched -&gt; exit 10; typo cannot cause silent skips.</li><li>Case drift (<code>Debugging-Cycle-Author</code>) - names are compared lowercase-exact; a mismatch is a MISSING, surfacing the drift.</li><li>Payload built by an older build.sh (no check embedded) - CI (TASK-IMP-068) runs the current checkout's check against the fresh build, so stale build scripts cannot bypass it on main.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the output grep-stable (<code>MISSING </code>/<code>UNPAIRED </code> prefixes); TASK-CUO-209's expansion test keys on them. The allowlist file ships in the repo, not the payload - the check runs where the build runs.</p>\n<p><em>End of TASK-SKILL-116.</em></p>";
