window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-144"]="<h1>TASK-IMP-144: 1.5.0 transition-locked state engine (draft)</h1>\n<p>CyberOS stays on the <strong>1.x</strong> line. This milestone follows 1.4.x (formerly mislabeled \"v4.0\" in early roadmap notes).</p>\n<h2>Problem</h2>\n<p>TASK-CUO-303 mechanically locks <code>backlog-mutate</code> for the two HITL transitions, but an agent can still edit <code>spec.md</code> frontmatter <code>status:</code> and regenerate BACKLOG — R-EXT-01 residual. ship-tasks.md records this as the accepted residual until a transition-locked engine lands.</p>\n<h2>Scope (draft)</h2>\n<ol><li>Single state-engine API owns every status cell mutation (frontmatter + index) with the same transition table and HITL gates.</li><li>Regenerators refuse to invent transition edges; they only reflect engine-committed state.</li><li>Capability-scoped agent identities (memory ACL + backlog writes) as a follow-on milestone inside 1.5.x.</li></ol>\n<h2>Non-goals</h2>\n<ul><li>Replacing ship-tasks skill chain.</li><li>Cross-tool conformance kit (separate 1.5.x track).</li></ul>";
