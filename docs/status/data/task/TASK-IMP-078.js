window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-078"]="<h2>§1</h2>\n<ol><li><code>stamp-release-version.mjs</code> <strong>MUST</strong> accept <code>--store-monotonic</code>: effective build number = <code>max(BUILD_NUMBER, floor(now/60s))</code>. Without the flag, behavior is byte-identical to today (committed baseline, <code>--check</code> drift detection, version.yml bump commits all unchanged).</li><li><code>release.yml</code>'s android and iOS stamp steps <strong>MUST</strong> pass the flag; no other call site does. The high-water-mark guard keeps validating the FILE value (the committed floor), and the effective value can only be &gt;= it.</li><li><code>BUILD_NUMBER</code> keeps its existing role and is NOT bumped here: one mechanism owns re-tag safety (the wall-clock floor), one owns the committed baseline (version bumps). A second incrementer was considered and rejected - it re-introduces the operator-memory dependency this task removes.</li><li>Wall-clock minutes, not commit timestamps: a re-tag of the SAME commit must still get a fresh number (commit-time would collide; run-time cannot). ~29.7M in 2026 vs Play's 2100000000 cap - headroom measured in millennia.</li></ol>\n<p><em>Lean profile: one flag + two step args; defect, fix, and both-mode behavior machine-verified in-session; store acceptance proven by the next tag run.</em></p>\n<h2>§5 (run 2026-07-13)</h2>\n<ul><li><code>node --check</code> PASS; plain <code>--check</code> run: \"all release artifacts already match VERSION\" (baseline untouched). PASS</li><li><code>--apply --store-monotonic</code>: stamped versionCode/CURRENT_PROJECT_VERSION 10706 -&gt; 29731417 (&gt; 10706, &lt; 2.1e9), both pbxproj occurrences; files then restored to committed baseline. PASS</li><li>release.yml YAML parses; both stamp steps carry the flag (<code>grep -c 'store-monotonic' .github/workflows/release.yml</code> == 2 run-lines + comments). PASS</li><li>Testing pass 2026-07-13 (post gate-1 \"approve all\"): full battery re-run green; fresh floor stamped 29731447 (&gt; the earlier test's 29731417 - the clock floor visibly advances between runs), baseline restored.</li></ul>\n<h2>§9</h2>\n<ul><li>Two jobs in one run stamp slightly different numbers (independent clocks). Cosmetic only - Play and ASC never compare; recorded, not fixed.</li><li>Should <code>cyberos-version.mjs</code> fast-forward BUILD_NUMBER to the last shipped effective number at the next real version bump? Not required for correctness (the floor always wins at release time); revisit only if a human-readable committed number regains value.</li></ul>\n<h2>§10</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Recovery</th></tr></thead><tbody><tr><td>two release runs inside the same minute</td><td>store rejects the second upload, loud</td><td>re-run: next minute, next number</td></tr><tr><td>runner clock skew backwards</td><td>floor still &gt;= committed BUILD_NUMBER; worst case equals a consumed number -&gt; loud store rejection</td><td>re-run after NTP settles</td></tr><tr><td>flag typo'd/dropped in a future workflow edit</td><td>next re-tag reproduces the 10706-class failure loudly at upload</td><td>re-add flag; this spec's §5 grep pins the 2 call sites</td></tr><tr><td>someone passes the flag in version.yml</td><td>committed files would carry a wall-clock number - visible in the bump commit diff, and <code>--check</code> flags drift on the next run</td><td>revert; flag is release.yml-only by §1 clause 2</td></tr></tbody></table>\n<p><em>End of TASK-IMP-078.</em></p>";
