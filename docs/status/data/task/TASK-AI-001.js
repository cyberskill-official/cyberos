window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AI-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The AI Gateway service <strong>MUST</strong> expose a <code>cost_ledger::precheck(request)</code> function that runs synchronously before any LLM-provider call. Given a <code>ChatCompleteRequest</code> with <code>tenant_id</code> and <code>agent_persona</code>, the function:</p>\n<ol><li><strong>MUST</strong> estimate the request's USD cost from the prompt token count × the provider's per-1k-token rate at the resolved model alias.</li><li><strong>MUST</strong> read the tenant's <code>monthly_cap_usd</code> and current MTD <code>spent_usd</code> from the <code>cost_ledger</code> table (Postgres).</li><li><strong>MUST</strong> refuse with <code>402 PAYMENT_REQUIRED</code> if <code>estimated_cost + current_spent &gt; monthly_cap_usd × 1.0</code> (the hard floor).</li><li><strong>SHOULD</strong> emit a <code>warn_threshold_crossed</code> event to OBS if <code>current_spent</code> crosses <code>monthly_cap_usd × warn_threshold</code> (default 0.80).</li><li><strong>MUST</strong> create a <code>cost_ledger_hold</code> row in Postgres with <code>estimated_usd</code>, <code>tenant_id</code>, <code>idempotency_key</code>, <code>expires_at = now() + 60s</code> before returning <code>allow</code>.</li><li><strong>MUST</strong> emit one <code>ai.precheck</code> audit row on the local memory via the canonical Writer subprocess before returning <code>allow</code> (audit-before-action invariant).</li><li><strong>MUST</strong> return synchronously within 50ms p95 — the precheck is on the hot path of every chat call.</li><li><strong>MUST</strong> load the cost-rate table from <code>services/ai-gateway/config/cost_rates.yaml</code> at gateway startup (loader machinery from TASK-AI-005 is reused). The hardcoded <code>const HashMap</code> is REMOVED for slice 1; cost rates live in YAML from day 1.</li><li><strong>MUST</strong> refuse with <code>503 POLICY_NOT_FOUND</code> when <code>policy::load_for_tenant()</code> returns <code>PolicyError::PolicyMissing</code>. No silent default policy. The error body MUST carry <code>{error: \"tenant_policy_missing\", tenant_id, contact: \"ops@cyberos.world\"}</code>.</li><li><strong>MUST</strong> validate the <code>idempotency_key</code> as a free-string of length 1..=64 ASCII printable characters. Longer or shorter MUST return <code>400 BAD_REQUEST</code> with <code>{error: \"invalid_idempotency_key\"}</code>. The Writer's canonical-JSON serialiser rejects non-printable characters by design.</li><li><strong>MUST</strong> emit the <code>ai.precheck</code> memory audit row synchronously (subprocess fork; ~30ms latency). TASK-AI-008 (slice 2 PyO3 spike) explores in-process PyO3 for the latency gain; until then, the audit-before-action invariant outweighs the latency cost.</li><li><strong>MUST</strong> execute the cap-check and hold-creation inside a single Postgres <code>BEGIN ... COMMIT</code> block. The <code>SELECT … FOR UPDATE</code> on the <code>cost_ledger</code> row prevents two concurrent calls from both passing the cap-check when the sum of their estimated costs would exceed the cap. Without this serialisation, two near-simultaneous calls each see <code>current.spent_usd = $X</code> and each pass the check, then both insert holds — eventually exceeding cap.</li><li><strong>MUST</strong> check <code>policy.ai_policy.allowed_personas</code> (if non-null) against <code>req.agent_persona</code>; refuse with <code>403 FORBIDDEN</code> and <code>{error: \"persona_not_allowed\", allowed: [...]}</code> if not in the list. This enforces the per-tenant persona-pinning policy from TASK-AI-005's schema.</li><li><strong>SHOULD</strong> emit OTel metrics: <code>ai_gateway_precheck_calls_total{outcome}</code> (counter; outcome ∈ allow/refuse/error), <code>ai_gateway_precheck_latency_ms</code> (histogram), <code>ai_gateway_holds_created_total</code> (counter), <code>ai_gateway_budget_warns_total{tenant_id}</code> (counter).</li></ol>\n<p>This is the cost-of-everything gate per the AI Gateway page §0 Role 1 and §2.5. Every consumer (CUO, KB, CHAT, PROJ inline genie, OBS auto-triage) inherits this gate; there is no bypass path.</p>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>The naive design</strong> would compute cost post-hoc — let the provider charge, then check budget. That works until the first tenant blows past their cap mid-month and discovers it on the invoice. The trust loss is permanent.</p>\n<p><strong>The two-phase design</strong> (precheck + post-reconcile, this task + TASK-AI-002) borrows from database transaction theory: estimate the cost first, hold it, then reconcile. Holds expire after 60s if the post-check never arrives — defensive. The pre-call cost overhead is ~5ms of Postgres I/O.</p>\n<p><strong>The hard-stop default at 1.0×</strong> (not 1.1× or \"soft warn\") is deliberate. Soft caps in finance always become hard losses when the tenant disputes the overage. The override path exists (<code>policy.emergency_override: true</code>) but requires CFO sign-off recorded in memory — <em>not</em> an automatic fallback.</p>\n<p><strong>Why this is TASK-AI-001 specifically</strong> (not TASK-AUTH-001): per research review §2.4, AI Gateway ships at P0 · slice 1 <em>before</em> AUTH. The X-Tenant header (HMAC-signed by a deployment secret) substitutes for JWT auth in slice 1; once AUTH ships at P0 · slice 2, the tenant_id source switches to the AUTH JWT claim per AI Gateway page §2.5. This task therefore uses the X-Tenant header path; TASK-AI-006 (slice 2) replaces it with JWT extraction.</p>\n<hr>\n<h2>§3 — API contract (formal spec for AI-agent implementers)</h2>\n<h3>Public function signature</h3>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cost_ledger.rs\npub async fn precheck(\n    req: &amp;ChatCompleteRequest,\n    pool: &amp;PgPool,\n    policy: &amp;TenantPolicy,\n) -&gt; Result&lt;PrecheckOutcome, PrecheckError&gt;;\n\npub enum PrecheckOutcome {\n    Allow { hold_id: Uuid, estimated_usd: Decimal, ttl_seconds: u32 },\n    Refuse { reason: RefuseReason, current_spent_usd: Decimal, cap_usd: Decimal },\n}\n\npub enum RefuseReason {\n    BudgetCapExceeded,\n    TenantSuspended,           // reserved for slice 2 (TenantPolicy.suspended flag)\n    ProviderUnavailable,       // resolved-provider cost-table missing\n    InvalidIdempotencyKey,\n    PersonaNotAllowed,         // §1 #13 — policy.allowed_personas restricts the persona\n}\n\npub enum PrecheckError {\n    DbError(sqlx::Error),\n    MemoryWriterFailed { stderr: String },\n    PolicyLoadFailed,\n    CostEstimateFailed { reason: String },\n}</code></pre>\n<h3>HTTP behavior on the chat handler</h3>\n<table><thead><tr><th>Caller intent</th><th>HTTP response</th></tr></thead><tbody><tr><td><code>precheck → Allow</code> then provider call succeeds</td><td><code>200 OK</code> + chat response body; post-reconcile fires (TASK-AI-002)</td></tr><tr><td><code>precheck → Refuse{BudgetCapExceeded}</code></td><td><code>402 PAYMENT_REQUIRED</code> + <code>{error: \"budget_cap_exceeded\", current_spent_usd, cap_usd, suggest: \"downshift to chat.fast or wait until next cycle\"}</code></td></tr><tr><td><code>precheck → Refuse{TenantSuspended}</code></td><td><code>403 FORBIDDEN</code> + <code>{error: \"tenant_suspended\", contact: \"billing@...\"}</code></td></tr><tr><td><code>precheck → Refuse{ProviderUnavailable}</code></td><td><code>503 SERVICE_UNAVAILABLE</code> + <code>{error: \"no_cost_table_for_model\", model: \"...\"}</code> (don't proceed without cost data)</td></tr><tr><td><code>precheck → Err(_)</code></td><td><code>500 INTERNAL_SERVER_ERROR</code> + opaque <code>request_id</code> (do NOT leak internals)</td></tr></tbody></table>\n<h3>Postgres schema (<code>migrations/0001_cost_ledger.sql</code>)</h3>\n<pre><code class=\"language-sql\">CREATE TABLE cost_ledger (\n    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id       TEXT NOT NULL,\n    period          DATE NOT NULL,             -- first-of-month UTC\n    spent_usd       NUMERIC(12,4) NOT NULL DEFAULT 0,\n    monthly_cap_usd NUMERIC(12,2) NOT NULL,\n    UNIQUE (tenant_id, period)\n);\n\nCREATE TABLE cost_ledger_hold (\n    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    tenant_id        TEXT NOT NULL,\n    idempotency_key  TEXT NOT NULL,\n    estimated_usd    NUMERIC(12,4) NOT NULL,\n    resolved_provider TEXT NOT NULL,\n    resolved_model   TEXT NOT NULL,\n    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    expires_at       TIMESTAMPTZ NOT NULL,\n    state            TEXT NOT NULL CHECK (state IN ('held','reconciled','expired','refused')),\n    UNIQUE (tenant_id, idempotency_key)\n);\n\nCREATE INDEX cost_ledger_period_idx ON cost_ledger (tenant_id, period);\nCREATE INDEX cost_ledger_hold_expiry_idx ON cost_ledger_hold (expires_at) WHERE state = 'held';</code></pre>\n<h3>Tenant policy YAML shape (TASK-AI-005 builds the loader; this task consumes it)</h3>\n<pre><code class=\"language-yaml\"># config/tenants/&lt;tenant_id&gt;.yaml (slice-1 location; moves to TEN module at P2)\ntenant_id: org:cyberskill\nai_policy:\n  monthly_cap_usd: 150\n  warn_threshold: 0.80\n  hard_stop: true\n  emergency_override:\n    enabled: true\n    requires: [\"cfo_signoff\", \"audit_row\"]</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria (testable, ordered, numbered)</h2>\n<ol><li><strong>Happy path (allow)</strong> — Given tenant <code>org:test-a</code> with <code>monthly_cap_usd: 100</code> and <code>spent_usd: 50</code>, when <code>precheck()</code> is called with an estimated cost of $5, it MUST return <code>Allow { hold_id, estimated_usd: 5.00, ttl_seconds: 60 }</code> and MUST insert exactly one row into <code>cost_ledger_hold</code> with <code>state='held'</code>.</li><li><strong>Refuse on over-budget</strong> — Same tenant with <code>spent_usd: 98</code>, estimated cost $5, MUST return <code>Refuse { reason: BudgetCapExceeded }</code>; MUST NOT insert any hold row; the HTTP handler MUST return <code>402 PAYMENT_REQUIRED</code>.</li><li><strong>Exact-cap edge</strong> — Tenant with <code>spent_usd: 95</code>, estimated cost $5.00, MUST return <code>Allow</code> (boundary inclusive: spent + estimated == cap is permitted; only strictly-over refused).</li><li><strong>Idempotent retry</strong> — Calling <code>precheck</code> with the same <code>idempotency_key</code> twice MUST return the existing hold (same <code>hold_id</code>), MUST NOT insert a second row.</li><li><strong>Audit row emitted</strong> — Every <code>Allow</code> MUST be preceded by a memory audit row at <code>memories/decisions/ai-invocations/&lt;ts_ns&gt;_&lt;tenant&gt;_&lt;idempotency_key&gt;.md</code> with kind <code>ai.precheck</code>, <code>extra.tenant_id</code>, <code>extra.estimated_usd</code>, <code>extra.resolved_provider</code>. If memory Writer fails, the function MUST return <code>Err(MemoryWriterFailed)</code> and MUST NOT return <code>Allow</code>.</li><li><strong>Hold TTL</strong> — A <code>cost_ledger_hold</code> row with <code>expires_at &lt; NOW()</code> and <code>state='held'</code> MUST be transitioned to <code>state='expired'</code> by the cleanup job within 60s of expiry (cleanup job is TASK-AI-004; this task only writes the row correctly).</li><li><strong>Provider cost-table missing</strong> — If <code>policy.primary_provider</code> resolves to a model alias with no entry in the cost-table fixture, <code>precheck()</code> MUST return <code>Refuse { reason: ProviderUnavailable }</code>; MUST emit a <code>Provider-Unavailable</code> warn-level log to OBS.</li><li><strong>Latency budget</strong> — On a warm Postgres pool, <code>precheck()</code> MUST complete within 50ms p95 over a 1000-call integration test. Measured via <code>tokio::time::Instant</code>.</li></ol>\n<hr>\n<h2>§5 — Verification method</h2>\n<p><strong>Integration test:</strong> <code>services/ai-gateway/tests/cost_precheck_test.rs</code></p>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn precheck_allows_under_budget() {\n    let env = TestEnv::new().await;\n    env.seed_tenant(\"org:test-a\", monthly_cap = 100, spent_usd = 50);\n    let req = chat_request(\"org:test-a\", prompt_tokens = 1000, model = \"chat.smart\");\n\n    let outcome = cost_ledger::precheck(&amp;req, &amp;env.pool, &amp;env.policy).await.unwrap();\n\n    assert!(matches!(outcome, PrecheckOutcome::Allow { .. }));\n    assert_eq!(env.count_holds(\"org:test-a\").await, 1);\n    assert!(env.memory_has_row(\"ai.precheck\", &amp;req.idempotency_key).await);\n}</code></pre>\n<p>Full test suite (8 cases, one per acceptance criterion) at the same file. Run via:</p>\n<pre><code class=\"language-bash\">cd /Users/stephencheng/Projects/CyberSkill/cyberos\ncargo test -p cyberos-ai-gateway cost_precheck</code></pre>\n<p>CI gate: this test file is in the <code>cyberos-ai-gateway</code> package; CI runs it on every PR touching <code>services/ai-gateway/**</code>. Failure blocks merge.</p>\n<hr>\n<h2>§6 — Implementation skeleton (suggested scaffold for AI-agent code-gen)</h2>\n<pre><code class=\"language-rust\">// services/ai-gateway/src/cost_ledger.rs\n\nuse sqlx::{PgPool, Postgres, Transaction};\nuse uuid::Uuid;\nuse rust_decimal::Decimal;\nuse chrono::{DateTime, Utc};\n\nconst HOLD_TTL_SECONDS: u32 = 60;\nconst IDEMPOTENCY_KEY_MAX_LEN: usize = 64;\n\npub async fn precheck(\n    req: &amp;ChatCompleteRequest,\n    pool: &amp;PgPool,\n    policy: &amp;TenantPolicy,\n) -&gt; Result&lt;PrecheckOutcome, PrecheckError&gt; {\n    // 0. Validate idempotency key (§1 #10)\n    validate_idempotency_key(&amp;req.idempotency_key)\n        .map_err(|reason| PrecheckError::CostEstimateFailed { reason })?;\n\n    // 0b. Persona pinning check (§1 #13)\n    if let Some(allowed) = &amp;policy.ai_policy.allowed_personas {\n        if !allowed.contains(&amp;req.agent_persona) {\n            return Ok(PrecheckOutcome::Refuse {\n                reason: RefuseReason::PersonaNotAllowed,\n                current_spent_usd: dec!(0),\n                cap_usd: policy.ai_policy.monthly_cap_usd,\n            });\n        }\n    }\n\n    // 1. Resolve provider + model from alias\n    let (provider, model) = resolve_model_alias(&amp;req.model_alias, policy)\n        .ok_or(PrecheckError::CostEstimateFailed { reason: \"no_provider\".into() })?;\n\n    // 2. Estimate cost\n    let cost_per_1k = cost_table::lookup(&amp;provider, &amp;model)\n        .ok_or_else(|| PrecheckError::CostEstimateFailed { reason: \"no_cost_entry\".into() })?;\n    let estimated_usd = (Decimal::from(req.prompt_tokens) / Decimal::from(1000)) * cost_per_1k.input\n                      + (Decimal::from(req.expected_completion_tokens) / Decimal::from(1000)) * cost_per_1k.output;\n\n    // 3. Open transaction (§1 #12 — cap check + hold insert must be serialised per tenant)\n    let mut tx: Transaction&lt;'_, Postgres&gt; = pool.begin().await?;\n\n    // 3a. Read current MTD spend INSIDE the transaction with FOR UPDATE row lock\n    let current = sqlx::query_as::&lt;_, CostLedgerRow&gt;(\n        \"INSERT INTO cost_ledger (tenant_id, period, monthly_cap_usd) \\\n         VALUES ($1, date_trunc('month', NOW())::date, $2) \\\n         ON CONFLICT (tenant_id, period) DO UPDATE SET tenant_id = EXCLUDED.tenant_id \\\n         RETURNING *\"\n    ).bind(&amp;req.tenant_id).bind(policy.ai_policy.monthly_cap_usd).fetch_one(&amp;mut *tx).await?;\n\n    // 3b. Re-lock the row explicitly (the UPSERT above already locked it via ON CONFLICT,\n    //     but make the intent explicit for future readers)\n    sqlx::query(\"SELECT 1 FROM cost_ledger WHERE tenant_id = $1 AND period = date_trunc('month', NOW())::date FOR UPDATE\")\n        .bind(&amp;req.tenant_id).execute(&amp;mut *tx).await?;\n\n    // 4. Cap check (boundary inclusive — `spent + estimated == cap` is permitted)\n    if current.spent_usd + estimated_usd &gt; policy.ai_policy.monthly_cap_usd {\n        tx.rollback().await?;   // release the row lock; no hold is created\n        return Ok(PrecheckOutcome::Refuse {\n            reason: RefuseReason::BudgetCapExceeded,\n            current_spent_usd: current.spent_usd,\n            cap_usd: policy.ai_policy.monthly_cap_usd,\n        });\n    }\n\n    // 5. Insert hold INSIDE the same transaction (idempotent via UNIQUE on (tenant_id, idempotency_key))\n    let hold_id = sqlx::query_scalar::&lt;_, Uuid&gt;(\n        \"INSERT INTO cost_ledger_hold (tenant_id, idempotency_key, estimated_usd, resolved_provider, resolved_model, expires_at, state) \\\n         VALUES ($1, $2, $3, $4, $5, NOW() + INTERVAL '60 seconds', 'held') \\\n         ON CONFLICT (tenant_id, idempotency_key) DO UPDATE SET state = cost_ledger_hold.state \\\n         RETURNING id\"\n    ).bind(&amp;req.tenant_id).bind(&amp;req.idempotency_key).bind(estimated_usd)\n     .bind(&amp;provider).bind(&amp;model).fetch_one(&amp;mut *tx).await?;\n\n    // 6. Emit memory audit row BEFORE commit (audit-before-action invariant — if memory fails,\n    //    we rollback the hold). The Writer subprocess holds &lt;memory-root&gt;/.lock for its own\n    //    serialization; the Postgres tx holds the cost_ledger row lock. Two orthogonal locks.\n    memory_writer::emit(\n        canonical::precheck(\n            &amp;req.tenant_id, &amp;req.agent_persona, &amp;req.model_alias,\n            &amp;provider, &amp;model, estimated_usd, current.spent_usd,\n            &amp;req.idempotency_key,\n        ),\n    ).await.map_err(|e| {\n        // memory failure rolls back the hold; precheck refuses the call\n        // (tx will rollback when dropped at function end since commit didn't happen)\n        PrecheckError::MemoryWriterFailed { stderr: e.to_string() }\n    })?;\n\n    // 7. Commit — the hold + ledger row lock are now durable\n    tx.commit().await?;\n\n    // 8. (post-commit) Emit OBS metric — non-critical, fire-and-forget\n    metrics::PRECHECK_CALLS.with_label_values(&amp;[\"allow\"]).inc();\n    metrics::HOLDS_CREATED.inc();\n\n    Ok(PrecheckOutcome::Allow { hold_id, estimated_usd, ttl_seconds: HOLD_TTL_SECONDS })\n}\n\nfn validate_idempotency_key(key: &amp;str) -&gt; Result&lt;(), String&gt; {\n    if key.is_empty() || key.len() &gt; IDEMPOTENCY_KEY_MAX_LEN {\n        return Err(\"invalid_idempotency_key: length must be 1..=64\".into());\n    }\n    if !key.chars().all(|c| c.is_ascii_graphic()) {\n        return Err(\"invalid_idempotency_key: charset must be ASCII printable\".into());\n    }\n    Ok(())\n}</code></pre>\n<p><em>Scaffold above is suggestive, not normative. The acceptance criteria (§4) are the contract.</em></p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Code dependencies (must exist before this task can build):</strong></p>\n<ul><li>memory module — shipped. The <code>cyberos.core.writer.Writer</code> subprocess CLI MUST be available at <code>cyberos memory put</code>. Used in step §6 of the skeleton.</li><li><code>cyberos-ai-gateway</code> crate skeleton — created by TASK-AI-000 (if needed) or as part of this task's first commit. Cargo workspace member.</li></ul>\n<p><strong>Concept dependencies (must be agreed before this task can be audited):</strong></p>\n<ul><li>Model-alias resolution rules (TASK-AI-005 will formalise; for slice 1 use a hardcoded enum).</li><li>Cost-table source (TASK-AI-007; for slice 1 hardcode 3 providers' rates as a const HashMap).</li></ul>\n<p><strong>Operational dependencies:</strong></p>\n<ul><li>A Postgres instance reachable at <code>DATABASE_URL</code>. For the integration test, the CI uses a Postgres 16 container via <code>testcontainers-rs</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Request (HTTP body)</h3>\n<pre><code class=\"language-http\">POST /v1/chat/completions HTTP/1.1\nHost: ai.cyberos.com\nX-Tenant: org:cyberskill                     # slice-1 only; replaced by JWT at P0 · slice 2\nX-CyberOS-Module: cuo                         # which module is the cost centre\nX-Idempotency-Key: 01HZK9R7A2B4C8D6E1F3G5H8\nContent-Type: application/json\n\n{\n  \"model\": \"chat.smart\",\n  \"messages\": [\n    {\"role\": \"system\", \"content\": \"You are Genie. Be concise.\"},\n    {\"role\": \"user\", \"content\": \"summarise Q1 OKRs\"}\n  ],\n  \"agent_persona\": \"cuo-cpo@0.4.1\",\n  \"max_tokens\": 1024,\n  \"stream\": false\n}</code></pre>\n<h3>Response — Allow path (HTTP 200, after provider call succeeds — TASK-AI-002 handles the actual provider call + post-reconcile)</h3>\n<pre><code class=\"language-json\">{\n  \"id\": \"ai_01HZK9R7A2B4C8D6\",\n  \"model\": \"bedrock:anthropic.claude-3.5-sonnet\",\n  \"choices\": [{\"message\":{\"role\":\"assistant\",\"content\":\"Q1 OKRs are…\"}}],\n  \"usage\": {\n    \"prompt_tokens\": 120,\n    \"completion_tokens\": 450,\n    \"usd_cost\": 0.0078,\n    \"cache_state\": \"miss\",\n    \"hold_id\": \"01HZK9R8M3X5C8Q4\",\n    \"failover_path\": \"primary\"\n  }\n}</code></pre>\n<h3>Response — Refuse path (HTTP 402)</h3>\n<pre><code class=\"language-json\">{\n  \"error\": \"budget_cap_exceeded\",\n  \"current_spent_usd\": 148.20,\n  \"cap_usd\": 150.00,\n  \"estimated_usd\": 5.50,\n  \"suggest\": \"downshift to chat.fast or wait until 2026-06-01\"\n}</code></pre>\n<h3>memory audit row written</h3>\n<pre><code class=\"language-json\">{\n  \"seq\": 18421,\n  \"ts_ns\": 1763112131000000000,\n  \"op\": \"put\",\n  \"path\": \"memories/decisions/ai-invocations/1763112131_org-cyberskill_01HZK9R7A2B4C8D6.md\",\n  \"extra\": {\n    \"kind\": \"ai.precheck\",\n    \"tenant_id\": \"org:cyberskill\",\n    \"agent_persona\": \"cuo-cpo@0.4.1\",\n    \"model_alias\": \"chat.smart\",\n    \"resolved_provider\": \"bedrock\",\n    \"resolved_model\": \"anthropic.claude-3.5-sonnet\",\n    \"estimated_usd\": 0.0085,\n    \"current_spent_usd\": 47.23\n  },\n  \"prev_chain\": \"...\",\n  \"chain\": \"...\"\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved 2026-05-15. Promoted to §1 normative clauses:</p>\n<ol><li><strong>~~Cost-table source~~</strong> → §1 #8 (YAML config at <code>services/ai-gateway/config/cost_rates.yaml</code>).</li><li><strong>~~Missing tenant policy~~</strong> → §1 #9 (<code>503 POLICY_NOT_FOUND</code>; no silent default).</li><li><strong>~~Idempotency-Key format~~</strong> → §1 #10 (free-string 1..=64 ASCII printable).</li><li><strong>~~Audit-row sync vs async~~</strong> → §1 #11 (synchronous in slice 1; TASK-AI-008 spikes PyO3).</li></ol>\n<hr>\n<h2>§10 — Failure modes inventory (all error paths)</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>HTTP</th><th>Recovery</th></tr></thead><tbody><tr><td>Tenant policy file missing</td><td><code>policy::load_for_tenant</code> returns <code>PolicyMissing</code></td><td><code>503 POLICY_NOT_FOUND</code></td><td>Operator adds the YAML; gateway re-reads on file-watch event (TASK-AI-005 §1 #4)</td></tr><tr><td>Tenant policy invalid YAML</td><td>Caught at gateway startup by <code>init_loader</code>; gateway exits 1</td><td>Gateway doesn't serve</td><td>Operator fixes YAML; redeploy. Loud failure by design (no silent defaults)</td></tr><tr><td>Budget cap exceeded</td><td><code>current.spent_usd + estimated_usd &gt; monthly_cap_usd</code></td><td><code>402 PAYMENT_REQUIRED</code></td><td>Operator raises cap or tenant waits to next period; TASK-AI-021 CLI exposes <code>cyberos-ai policy raise-cap</code></td></tr><tr><td>Cost-table missing model</td><td><code>cost_table::lookup</code> returns None</td><td><code>503 SERVICE_UNAVAILABLE</code> (<code>no_cost_table_for_model</code>)</td><td>Operator adds model to <code>cost_rates.yaml</code>; gateway hot-reloads</td></tr><tr><td>Persona not allowed</td><td><code>policy.allowed_personas</code> set + <code>req.agent_persona</code> not in list</td><td><code>403 FORBIDDEN</code> (<code>persona_not_allowed</code>)</td><td>Operator updates policy or caller fixes agent_persona</td></tr><tr><td>Invalid idempotency key</td><td>Length/charset check fails</td><td><code>400 BAD_REQUEST</code> (<code>invalid_idempotency_key</code>)</td><td>Caller fixes; retry with valid key</td></tr><tr><td>Tenant suspended (slice-2 future)</td><td>Policy carries <code>suspended: true</code></td><td><code>403 FORBIDDEN</code> (<code>tenant_suspended</code>)</td><td>Out of scope for slice 1; placeholder reserved</td></tr><tr><td>memory Writer subprocess fails</td><td><code>memory_writer::emit</code> returns Err</td><td><code>500 INTERNAL_SERVER_ERROR</code> (opaque request_id)</td><td>Operator investigates via OBS; precheck refuses the call (audit-before-action invariant)</td></tr><tr><td>Postgres pool exhausted / unreachable</td><td><code>sqlx::Error</code> from any query</td><td><code>500 INTERNAL_SERVER_ERROR</code> (opaque request_id)</td><td>Operator investigates; failure surfaces in OBS</td></tr><tr><td>Concurrent cap-race (two prechecks for same tenant)</td><td>Detected by <code>FOR UPDATE</code> row lock</td><td>One Allow, one Refuse</td><td>Postgres MVCC handles correctly; verified by AC #1 of the property-test</td></tr><tr><td>Hold UNIQUE constraint hit on idempotency_key</td><td>Retry with same <code>idempotency_key</code> from caller</td><td>Returns existing hold</td><td>Idempotent by design (§1 #4 of TASK-AI-001 — return the existing hold_id)</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes (informational, no normative force)</h2>\n<ul><li>Slice 1 of AI Gateway is 5 tasks (TASK-AI-001..005). This one is the gate; TASK-AI-002 is the post-reconcile; TASK-AI-003 is the memory audit-bridge; TASK-AI-004 is the hold-expiry cleanup job; TASK-AI-005 is the tenant-policy loader.</li><li>After this task ships, every other module (CUO, KB, CHAT, …) inherits the cost gate automatically by calling <code>ai_gateway::chat_complete()</code> instead of a raw provider SDK. The \"no SDK in any other module\" rule (AI Gateway page §0) is enforced architecturally, not by lint.</li><li>The 50ms p95 latency budget is tight but achievable: Postgres read (~5ms) + Postgres write (~10ms) + memory Writer subprocess (~30ms). The subprocess is the bottleneck — see §9 Q4.</li><li>This task's verification test fixture should land in <code>services/ai-gateway/tests/fixtures/cost_ledger/</code> and become a reusable property-test seed for TASK-AI-002 + TASK-AI-004.</li></ul>\n<hr>\n<p><em>End of TASK-AI-001. Run <code>task-audit</code> next: <code>cargo run -p cyberos-skill-cli -- run task-audit --input '{\"fr_path\": \"docs/tasks/ai/TASK-AI-001-cost-ledger-precheck/spec.md\"}'</code></em></p>";
