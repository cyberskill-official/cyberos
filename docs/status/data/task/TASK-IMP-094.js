window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-094"]="<h1>TASK-IMP-094: Channels, shared .agents/skills dir plus Devin and Windsurf rules</h1>\n<h2>Summary</h2>\n<p>The install's agent-surface block predates two convention moves the 2026-07-16 research recorded: the Agent Skills open standard's shared <code>.agents/skills/</code> dir (one dir now read by Codex, Copilot, Cursor, Gemini CLI, and OpenCode) and the Windsurf-to-Devin rebrand (<code>.devin/rules/</code> preferred, <code>.windsurf/rules/</code> fallback, legacy <code>.windsurfrules</code> still read). Extend step 5b with the shared skills entries and the two rules pointers, keep the legacy file, and teach the gitignore block and uninstall the new paths.</p>\n<h2>Problem</h2>\n<p>Every convention miss is an agent that cannot discover the workflow: a consumer opening the repo in Devin Desktop today finds only the legacy filename, and the five agents reading the shared skills dir find nothing at all.</p>\n<h2>Proposed Solution</h2>\n<p>Step 5b additions, all create-if-absent and honoring CYBEROS_AGENTS filtering: (a) <code>.agents/skills/&lt;cmd&gt;</code> entries for the three commands, as relative symlinks to the existing <code>.claude/skills/&lt;cmd&gt;</code> copies with a plain-copy fallback where symlinks are unavailable; (b) <code>.devin/rules/cyberos.md</code> and <code>.windsurf/rules/cyberos.md</code> pointer files with the same body as the other rules pointers; (c) the legacy <code>.windsurfrules</code> stays. The managed-gitignore block gains the new paths; uninstall strips them with the rest of the surface. test_channels.sh and hygiene t01 learn the new expectations.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Replace <code>.windsurfrules</code> with the new paths. Rejected: the research says the legacy file is still read; removal breaks users who have not updated, for zero gain.</li><li>Copies instead of symlinks in <code>.agents/skills/</code>. Kept only as the fallback: symlinks keep the skills single-sourced under <code>.claude/skills/</code>; copies drift.</li><li>Waiting for the C1 pre-tag re-verify to do it all at once. Rejected: E3 is a checklist gate line for exactly this implementation; C1 re-verifies freshness at tag time regardless.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a scratch install shows the shared skills entries and both rules pointers; a second install changes nothing (idempotence) - suite-asserted every run. Baseline: none of the three paths exist. Deadline: final acceptance.</li><li>Guardrail: uninstall leaves no orphan under any new path, and the managed gitignore block round-trips (strip + regenerate) cleanly.</li></ul>\n<h2>Scope</h2>\n<p>In scope: step 5b additions, gitignore block, uninstall strip, suite extensions.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Re-verifying the whole channel matrix (release row C1, operator, at tag time).</li><li>New skill content - the shared dir points at the existing skills.</li><li>MCP channel changes.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>Shares install.sh with TASK-IMP-095/096 - one agent, serial, per the batch plan.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the recorded channel research and IMP-08; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-4 PLAN approved 2026-07-17 (§0a); both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Install MUST create (if absent) <code>.agents/skills/</code> entries for the three commands as relative symlinks to their <code>.claude/skills/</code> counterparts, falling back to copies where symlink creation fails; CYBEROS_AGENTS filtering applies.</li><li>1.2 Install MUST create (if absent) <code>.devin/rules/cyberos.md</code> and <code>.windsurf/rules/cyberos.md</code> pointer files, keeping <code>.windsurfrules</code>.</li><li>1.3 The managed gitignore block MUST cover the new paths, and uninstall MUST remove them (including the <code>.agents/skills</code> entries) without touching operator files.</li><li>1.4 Re-install MUST be idempotent across all new paths (no duplicates, no churn).</li><li>1.5 Coverage MUST land as extensions to test_channels.sh and test_install_hygiene.sh t01.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2) - scratch install shows shared skills entries + both pointers, legacy kept - test: <code>tools/install/tests/test_channels.sh::t_shared_skills_and_devin_rules</code></li><li>[ ] AC 2 (traces_to: #1.4) - second install byte-idempotent on the new paths - test: <code>tools/install/tests/test_channels.sh::t_channel_idempotence</code></li><li>[ ] AC 3 (traces_to: #1.3, #1.5) - gitignore block covers new paths; uninstall strips them; coverage lands in the two named suites - test: <code>tools/install/tests/test_install_hygiene.sh::t01 (extended assertions)</code></li><li>[ ] AC 4 (traces_to: #1.1) - symlink targets resolve inside the repo (no dangling links) - test: <code>tools/install/tests/test_channels.sh::t_shared_skills_resolve</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Filesystem without symlink support (or Windows checkout): copy fallback engages; t_shared_skills_resolve accepts either form but never a dangling link.</li><li>Operator already created <code>.devin/rules/</code> with own files: create-if-absent adds only cyberos.md; nothing else touched (idempotence arm).</li><li>CYBEROS_AGENTS excludes an agent family: its paths are not created (existing filter semantics, asserted for one exclusion).</li><li>Security-class: pointer files carry repo-relative prose only; symlinks are relative and inside the repo - no absolute-path leakage.</li></ul>";
