window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-141"]="<h1>TASK-IMP-141: MMR sync for memory-append</h1>\n<h2>Problem</h2>\n<p>Every gated HITL flip (<code>reviewing→ready_to_test</code>, <code>testing→done</code>) appends a <code>status_overridden</code> row via <code>tools/install/docs-tools/memory-append.mjs</code>. That tool advanced the binlog + HEAD but did <strong>not</strong> update <code>audit/mmr/peaks.bin</code>. Doctor <code>ledger-mmr-cross-check</code> then went RED, and with CUO-302's doctor gate fail-closed, <code>run-gates.sh</code> failed until a human rebuilt peaks from the binlog (batch-8b/8c ship notes).</p>\n<h2>Fix</h2>\n<p>After every successful append, rebuild <code>audit/mmr/peaks.bin</code> from every on-disk payload using the same peak-stack algorithm as <code>modules/memory/cyberos/core/mmr.py</code> (Node stdlib only — docs-tools convention). Catch-up heals a previously stale peaks file.</p>\n<h2>Acceptance</h2>\n<ol><li><code>bash tools/install/tests/test_memory_append.sh</code> includes <code>t05_mmr_peaks_stay_in_sync</code> and passes.</li><li>After N appends, peaks <code>leaf_count</code> equals HEAD.</li><li>A deliberately stale peaks.bin is healed by the next append.</li><li>CHANGELOG Unreleased notes the fix.</li></ol>";
