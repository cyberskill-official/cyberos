window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-101"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CUO service <strong>MUST</strong> ship the LangGraph supervisor that adds the Phase 2 LLM cascade on top of the Phase 1 rule router, with persona-aware routing, memory audit per decision, and EU AI Act Art. 12/13/26 compliance. Each requirement:</p>\n<ol><li><strong>MUST</strong> implement the supervisor as a <code>langgraph.graph.StateGraph</code> over the <code>CuoState</code> TypedDict (defined in <code>state.py</code>) with exactly 5 nodes (per DEC-168): <code>parse</code>, <code>rule_score</code>, <code>branch</code>, <code>invoke</code>, <code>record</code>. The <code>branch</code> node is the conditional entry point that selects among four downstream paths: <code>auto</code>, <code>ask</code>, <code>cascade</code>, <code>defer</code>. The <code>cascade</code> path runs the LLM and re-enters <code>branch</code> with the LLM's confidence — no infinite loop because <code>cascade</code> is taken at most once per request (enforced by a <code>cascade_taken: bool</code> flag in state).</li></ol>\n<ol><li><strong>MUST</strong> route every NL request through the supervisor when invoked via <code>cyberos-cuo supervisor route --query \"&lt;text&gt;\"</code>. The CLI is the canonical entry point; library users invoke <code>from cyberos.cuo.supervisor import run_supervisor</code>.</li></ol>\n<ol><li><strong>MUST</strong> make the <code>parse</code> node the first step. It NFC-normalises the query (preserving diacritics for VN region scoring per existing Phase 1 behaviour), constructs the query envelope <code>{query, tenant_id, subject_id, persona_key, ts_ns, request_id}</code>, and validates <code>persona_key ∈ {genie, ceo, coo, cfo, cmo, cto, chro, cso, clo, cdo, cpo}</code>. Unknown persona → <code>BAD_REQUEST</code> (exit 65) and a <code>cuo.persona_unknown</code> memory audit row.</li></ol>\n<ol><li><strong>MUST</strong> make the <code>rule_score</code> node run the existing Phase 1 rule scorer (<code>cyberos.cuo.core.router.score_one_off</code>) unchanged. The result populates <code>state['rule_scores']: list[Candidate]</code> sorted by descending confidence. This guarantees backward-compatibility with the Phase 1 deterministic golden-fixture suite (15 rule-path fixtures still pass).</li></ol>\n<ol><li><strong>MUST</strong> make the <code>branch</code> node select the next path using the <strong>confidence-band table</strong> (per DEC-162):</li></ol>\n<ul><li>top candidate confidence ≥ <strong>0.70</strong> → <code>auto</code> path (invoke).</li><li>top candidate confidence ∈ [0.50, 0.70) → <code>ask</code> path (return top 3 alternatives; no invocation).</li><li>top candidate confidence ∈ [0.10, 0.50) <strong>AND</strong> <code>cascade_taken == false</code> → <code>cascade</code> path (LLM).</li><li>top candidate confidence &lt; 0.10 → <code>defer</code> path (return decision <code>{routed: false, alternatives: []}</code>; no invocation).</li><li><code>cascade_taken == true</code> AND LLM-output confidence &lt; 0.70 → fall through to <code>ask</code>.</li></ul>\n<ol><li><strong>MUST</strong> make the <code>llm_cascade</code> node call the LLM through the <code>litellm_proxy</code> (which routes through AI Gateway TASK-AI-008, never direct provider call — per DEC-161). The cascade:</li></ol>\n<ul><li>Sends a structured prompt containing the query envelope + the top 5 rule-scored candidates + the persona-specific system prompt.</li><li>Expects a <strong>structured response</strong> validated by Pydantic (per DEC-163): <code>LlmRoutingPick {skill_name: str, arguments: dict, rationale: str, confidence: float}</code>. Schema-conformance failure → fall through to <code>ask</code> (never silently invoke).</li><li>Has a <strong>hard 3-second budget</strong> (per DEC-169). Timeout → fall through to <code>ask</code>; emit <code>cuo.llm_cascade_timeout</code> memory audit row.</li><li>Sets <code>cascade_taken = true</code> AND <code>state['llm_pick'] = LlmRoutingPick(...)</code> AND re-enters <code>branch</code> with the LLM-derived confidence.</li></ul>\n<ol><li><strong>MUST</strong> make the <code>invoke</code> node delegate skill execution to the skill module's CLI per cuo/docs/AGENTS.md §0.5 (CUO does NOT implement skill execution). The node captures <code>stdout</code>, <code>stderr</code>, <code>exit_code</code>, <code>duration_ms</code> into <code>state['invocation_result']</code>. If the chosen skill is annotated <code>destructive: true</code> in the catalog, the capability broker (TASK-SKILL-104) MUST gate via Elicitation flow; the supervisor refuses to bypass — even at confidence 1.0 (per DEC-170).</li></ol>\n<ol><li><strong>MUST</strong> make the <code>record</code> node emit exactly one <code>cuo.routing_decision</code> memory audit row per request, in EVERY path (auto, ask, cascade, defer). The row carries:</li></ol>\n<ul><li><code>tenant_id</code>, <code>subject_id_hash16</code>, <code>persona_key</code>, <code>persona_version</code>, <code>agent_persona_jwt_iss</code> (from JWT).</li><li><code>query</code> (NFC-normalised; PII-scrubbed via TASK-MEMORY-111 before commit if persona requires).</li><li><code>rule_scores</code> (top 3 with confidence values).</li><li><code>path_taken</code> ∈ {<code>auto</code>, <code>ask</code>, <code>cascade</code>, <code>defer</code>, <code>cascade_then_ask</code>}.</li><li><code>llm_pick</code> (present iff <code>path_taken</code> involved cascade).</li><li><code>invocation_result</code> (present iff path = <code>auto</code> or <code>cascade_then_auto</code>).</li><li><code>cuo_state_v</code> (per DEC-167, currently <code>1</code>).</li><li><code>request_id</code>, <code>trace_id</code> (W3C-formatted, lower-hex 32-char per task-audit skill rule 24).</li><li><code>ts_ns_start</code>, <code>ts_ns_end</code>.</li></ul>\n<ol><li><strong>MUST</strong> load the <strong>11-persona catalogue</strong> from <code>cuo/cuo/supervisor/persona.py</code>: Genie + 10 C-level (CEO, COO, CFO, CMO, CTO, CHRO, CSO, CLO, CDO, CPO). Each persona has: <code>key</code>, <code>display_name</code>, <code>keyword_bank</code> (list of trigger words), <code>system_prompt</code>, <code>defer_to_human_matrix</code> (list of operation types the persona MUST refuse to auto-invoke regardless of confidence). The matrix is <strong>intrinsic</strong> to the persona — not overridable by config (per DEC-164 + EU AI Act Art. 26).</li></ol>\n<ol><li><strong>MUST</strong> validate the caller's JWT carries <code>agent_persona: cuo-&lt;persona-key&gt;@&lt;semver&gt;</code> matching the requested persona. Missing or mismatched persona claim → <code>403 FORBIDDEN</code> (exit 77) with body <code>{\"error\":\"persona_mismatch\",\"claimed\":\"&lt;x&gt;\",\"requested\":\"&lt;y&gt;\"}</code>. Validation uses TASK-AUTH-101's <code>RoleMatrix</code> for <code>agent-persona</code> role + TASK-AI-014's persona-version stamping.</li></ol>\n<ol><li><strong>MUST</strong> route all LLM calls through the <strong><code>litellm_proxy</code></strong> module — a thin LiteLLM-shaped client that forwards to AI Gateway TASK-AI-008 (POST <code>/v1/ai/chat</code>). The proxy MUST NOT include direct provider SDKs (no <code>import boto3</code>, no <code>import anthropic</code>, no <code>import openai</code>). The architectural test <code>test_supervisor_litellm_routes_via_gateway</code> asserts this by AST-walking the supervisor package and rejecting forbidden imports.</li></ol>\n<ol><li><strong>MUST</strong> use a <strong>Postgres checkpointer</strong> for LangGraph state persistence per EU AI Act Art. 12 (logging requirement). Slice 2 (this task) ships an in-memory checkpointer scaffold — the production Postgres-backed checkpointer ships in TASK-CUO-102. The state schema is versioned: <code>cuo_state_v = 1</code> (per DEC-167); replays from <code>cuo_state_v</code> within ±2 of current are tolerated, beyond rejected with <code>state_version_unsupported</code>.</li></ol>\n<ol><li><strong>MUST</strong> support <strong>transparent end-of-response disclosure</strong> (EU AI Act Art. 13): every supervisor return value includes <code>transparency: {skill_chosen, confidence, alternatives: list[{skill, confidence}], path_taken, llm_used: bool}</code> so the caller can render the disclosure to the user. CHAT/EMAIL/PROJ surfaces consume this for the \"🤖 routed via Genie → cfo persona → vietnam-vat-invoice@1.2 · 0.84\" footer.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>cuo.supervisor.route</code> per request with attributes: <code>tenant_id</code>, <code>subject_id_hash16</code>, <code>persona_key</code>, <code>path_taken</code>, <code>confidence</code>, <code>llm_used</code>, <code>outcome</code> (success | routed_false | invoke_error | persona_mismatch | timeout | unknown_persona). Sampling: 1% steady-state; 100% on non-success outcomes.</li></ol>\n<ol><li><strong>MUST</strong> emit child spans for each node entry/exit: <code>cuo.supervisor.node.parse</code>, <code>.rule_score</code>, <code>.branch</code>, <code>.llm_cascade</code>, <code>.invoke</code>, <code>.record</code>. Spans carry the W3C <code>traceparent</code> propagated from caller; supervisor extends the trace through to the AI Gateway call so a single trace covers query → LLM → invoke → audit.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>cuo_supervisor_route_total{outcome, persona, path_taken}</code> (counter).</li><li><code>cuo_supervisor_route_latency_ms{path_taken}</code> (histogram; SLOs: auto p95 &lt; 50ms, ask p95 &lt; 50ms, cascade p95 &lt; 3500ms, defer p95 &lt; 30ms).</li><li><code>cuo_supervisor_llm_cascade_total{outcome}</code> (counter; outcome ∈ {success, timeout, schema_violation, gateway_error}).</li><li><code>cuo_supervisor_persona_defer_blocks_total{persona, operation}</code> (counter — defer-matrix refusals).</li><li><code>cuo_supervisor_destructive_block_total{skill}</code> (counter — capability-broker refusals reflected back).</li></ul>\n<ol><li><strong>MUST</strong> guarantee <strong>replay-equivalence</strong> for the rule path: same <code>(query, persona_key, catalog_snapshot_hash)</code> → identical decision. The <code>test_supervisor_idempotency</code> test asserts this on the 15-fixture golden set; CI fails on any non-determinism.</li></ol>\n<ol><li><strong>MUST NOT</strong> invoke a skill on the <strong>defer path</strong>. The decision returned has <code>routed: false</code>, <code>alternatives: [&lt;top 3&gt;]</code>, and the caller (CHAT, EMAIL, etc.) renders the alternatives for the operator to pick — never auto-invoking. This is the EU AI Act Art. 26 hard guarantee.</li></ol>\n<ol><li><strong>MUST</strong> treat the <strong><code>defer_to_human_matrix</code></strong> as ROLE-INTRINSIC: even if the supervisor scores <code>vietnam-vat-invoice@1.2 + 0.95</code> for a CFO persona, if <code>cfo.defer_to_human_matrix</code> lists <code>invoice_emit</code>, the supervisor refuses to auto-invoke and returns <code>{routed: false, reason: \"persona_defer_matrix\", operation: \"invoice_emit\"}</code>. Emit <code>cuo.persona_defer_block</code> memory audit row.</li></ol>\n<ol><li><strong>MUST</strong> support <strong>two invocation modes</strong> via CLI flags:</li></ol>\n<ul><li><code>--invoke</code> (default true): runs through to <code>invoke</code> node when path = <code>auto</code> or <code>cascade_then_auto</code>.</li><li><code>--record</code> (default true): runs through to <code>record</code> node and emits the memory row. Both flags MAY be <code>--no-invoke</code> / <code>--no-record</code> for dry-run analysis (e.g. \"what would the supervisor do without actually invoking?\"). Dry-run results are NOT recorded (no memory row); they emit a <code>cuo.dry_run</code> OTel span with low sampling.</li></ul>\n<ol><li><strong>MUST</strong> complete the <strong>rule path (auto/ask/defer)</strong> in ≤ 50 ms p95 measured at supervisor entry → return. The LLM cascade path is budgeted at ≤ 3500 ms p95 (3000 ms LLM + 500 ms supervisor overhead). Performance test <code>test_supervisor_perf_rule_path</code> asserts the rule path; cascade-path perf is asserted in integration tests via a mocked AI Gateway response.</li></ol>\n<ol><li><strong>MUST</strong> ship the CLI subcommand <code>cyberos-cuo supervisor route --query \"&lt;text&gt;\" [--persona &lt;key&gt;] [--invoke|--no-invoke] [--record|--no-record] [--json]</code>. Exit codes (from <code>cyberos-cli-exit</code> shared crate per task-audit skill rule 9): 0 success-invoked, 1 success-but-ask, 2 success-but-defer, 64 invalid-argument, 65 invalid-data (unknown persona), 73 cant-create (audit failure), 75 temp-fail (timeout), 77 permission-denied (persona mismatch).</li></ol>\n<ol><li><strong>MUST</strong> validate the LLM cascade output against the <code>LlmRoutingPick</code> Pydantic schema. The schema requires <code>skill_name: str</code> (must match a known catalog skill), <code>arguments: dict</code> (must be JSON-serialisable), <code>rationale: str</code> (1–500 chars), <code>confidence: float</code> (0.0–1.0). Validation failure → re-emit prompt once with stricter instructions; second failure → fall through to <code>ask</code>. The retry counter is in state; max 1 retry.</li></ol>\n<ol><li><strong>MUST</strong> PII-scrub the <code>query</code> field of the memory audit row using <code>cyberos-memory-pii</code> rules per task-audit skill rule 18, BEFORE chain commit. The original query is retained in the supervisor's OTel span (transient, &lt; 30-day retention via TASK-OBS-006 tail sampling); the memory row holds the scrubbed form for long-term storage.</li></ol>\n<ol><li><strong>MUST</strong> support <strong>multi-step chain entry stub</strong>: the supervisor returns <code>{decision, next_step: null}</code> at slice 2. TASK-CUO-104 (topological chain walk, slice 3+) consumes <code>next_step</code> to compose multi-skill flows. The slice-2 stub MUST set <code>next_step = null</code> unconditionally — never a stale value.</li></ol>\n<ol><li><strong>MUST</strong> emit <code>cuo.routing_decision</code> row carrying <code>next_step: null</code> at slice 2; the field is <strong>PRESENT but null</strong> (not absent). This contract guarantees forward-compatibility with TASK-CUO-104's chain-walk consumer; absent-field would force TASK-CUO-104 to handle both shapes.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why LangGraph and not a hand-rolled state machine (§1 #1, DEC-160)?</strong> LangGraph gives us four things that a hand-rolled state machine would have to re-invent: (1) durable checkpointing — the state is persistable per node entry, satisfying EU AI Act Art. 12's \"replay every decision\" requirement; (2) conditional edges with a typed router — the confidence-band branch is one declaration, not 5 if/elif arms scattered through code; (3) a standard idiom — engineers familiar with LangGraph from agentic workflows can read the supervisor in a day; (4) instrumentation hooks — every node entry/exit is observable without wrapping each function manually. The cost is a Python dependency on <code>langgraph</code> (0.2.x); the benefit is that the supervisor's topology is the source of truth, not buried in code.</p>\n<p><strong>Why LiteLLM as the LLM abstraction and not direct SDKs (§1 #11, DEC-161)?</strong> LiteLLM provides a unified interface across providers (OpenAI, Anthropic, Bedrock) — but we don't want CUO making direct provider calls because (a) the AI Gateway TASK-AI-008 already provides multi-provider routing, failover, and cost ledger; (b) direct CUO→provider calls bypass the cost ledger, breaking budget accounting; (c) two competing routing systems is a maintenance nightmare. The solution: the <code>litellm_proxy</code> module is shaped like the LiteLLM API but forwards every call to the AI Gateway. CUO benefits from LiteLLM's prompt/response normalisation; the AI Gateway remains the single egress point for provider calls. The architectural test (<code>test_supervisor_litellm_routes_via_gateway</code>) AST-walks the supervisor package and rejects any direct provider import — this is enforced at CI, not by reviewer vigilance.</p>\n<p><strong>Why the confidence bands ≥0.70 / 0.50–0.70 / 0.10–0.50 / &lt;0.10 (§1 #5, DEC-162)?</strong> These come from the website docs §architecture and reflect a year of routing experiments. 0.70 is \"confident enough to act without asking\" — the rule scorer's top fixture matches all score above 0.70. The 0.50–0.70 band is \"good guess but not certain\" — surfacing alternatives is cheap and lets the operator confirm. The 0.10–0.50 band is the \"ambiguous tail\" where LLM cascade adds value — below 0.10, the candidates are noise and the LLM would hallucinate; the right action is defer-to-human. The numeric thresholds are hard-coded constants (not env-tuned) for replay determinism — Phase 2 ships them as the published contract, and tuning happens in subsequent tasks with explicit version bumps.</p>\n<p><strong>Why a hard 3-second LLM cascade budget (§1 #6, DEC-169)?</strong> User-facing latency budget for an ambiguous query is ~5 seconds total (perceived as \"thinking, not broken\"). 3 seconds for the LLM, 500 ms for supervisor overhead, 500 ms for skill invocation, 500 ms slack. Past 3 seconds the LLM is rarely going to produce a meaningfully better pick; the failure mode (timeout → fall through to <code>ask</code>) is benign — the user sees alternatives and picks. Better to fail fast and clearly than spin indefinitely and confuse the user.</p>\n<p><strong>Why structured Pydantic output and never freeform text (§1 #6, DEC-163)?</strong> Freeform LLM responses force regex parsing → high failure rate, security concerns (prompt injection in the rationale text could be acted upon). Structured output via Pydantic schemas: (a) the LLM is constrained at prompt time (\"respond with JSON matching schema X\"); (b) parsing failure is unambiguous (schema validation error); (c) the <code>rationale</code> field is bounded (1–500 chars), preventing prompt-injection-via-rationale. If the LLM produces non-conforming output, fall through to <code>ask</code> — never silently invoke based on unparseable text.</p>\n<p><strong>Why the defer-to-human matrix is intrinsic to the persona (§1 #19, DEC-164)?</strong> The EU AI Act Art. 26 human-oversight guarantee says \"operators retain the right to refuse AI-initiated actions on high-risk operations.\" Encoding the matrix in <code>persona.py</code> as data (<code>cfo.defer_to_human_matrix = [\"invoice_emit\", \"wire_transfer\", ...]</code>) and never letting config override is the difference between \"we are compliant\" and \"we claim to be compliant but config can disable it.\" The principle: matrix is code, not configuration. The cost is one Python file edit per matrix change (ADR-required); the benefit is that the gate cannot be turned off via env vars or runtime knobs.</p>\n<p><strong>Why every decision emits a <code>cuo.routing_decision</code> memory row (§1 #8, DEC-165)?</strong> EU AI Act Art. 12 logging requires every AI decision retained 6 months minimum AND replayable. The memory audit chain (Layer-1 memory per AGENTS.md §6) provides both: rows are append-only, chained via SHA-256 prev_chain, and PII-scrubbed before commit. Emitting on EVERY path (auto, ask, cascade, defer) — not just successful invocations — is the design assertion that \"defer\" is itself an AI decision worth auditing (the AI decided NOT to act). The row's <code>path_taken</code> enum lets analysts query \"show me all decisions where cascade was triggered but fell through to ask.\"</p>\n<p><strong>Why the 11-persona catalogue (§1 #9)?</strong> Genie is the unspecialised entry point — the persona that handles general queries before a specialist takes over. The 10 C-level personas (CEO, COO, CFO, CMO, CTO, CHRO, CSO, CLO, CDO, CPO) match CyberSkill's organisational structure and the BACKLOG's role-RBAC catalogue. Each persona has its own keyword bank (CFO's bank includes \"invoice\", \"VAT\", \"BHXH\", \"P1/P2/P3\"; CTO's includes \"deploy\", \"rollback\", \"incident\") so the rule scorer can adjust weights per persona without bloating one giant keyword list. The catalogue is closed — adding an 11th specialist persona is an ADR, same discipline as RBAC.</p>\n<p><strong>Why route LLM calls THROUGH the AI Gateway and not direct (DEC-161)?</strong> Three reasons converge: (1) the AI Gateway is the cost-ledger authority — every LLM call is preflight-budgeted via TASK-AI-001 and post-call reconciled via TASK-AI-002; bypassing it breaks budget accounting. (2) the AI Gateway is the residency-pinning authority (TASK-AI-016) — direct CUO→provider calls would not honour tenant residency policy. (3) the AI Gateway is the failover authority (TASK-AI-008) — implementing failover in CUO too would duplicate logic and create drift. The <code>litellm_proxy</code> module solves the LiteLLM-style ergonomics on the CUO side while preserving the AI Gateway as single egress.</p>\n<p><strong>Why slice-2 ships only the in-memory checkpointer (§1 #12)?</strong> Splitting the Postgres-backed checkpointer to TASK-CUO-102 keeps this task focused on the supervisor topology + LLM cascade. The in-memory checkpointer is functionally complete for tests; production deployment of slice 2 runs without persistent checkpointing (state lost on supervisor restart — acceptable because each request is independent). TASK-CUO-102 adds Postgres persistence for replay + EU AI Act Art. 12 full compliance.</p>\n<p><strong>Why <code>cuo_state_v</code> field in state and memory row (§1 #12, DEC-167)?</strong> Slice 3+ will add fields to the state (multi-step <code>next_step</code>, chain context, etc.). Embedded version lets the supervisor reject state replays from incompatible versions cleanly — and lets the memory row's analyst tooling filter by state-schema version. ±2 version tolerance (current is 1; tolerate 1–3) allows rolling upgrades without breaking replay during deployment windows.</p>\n<p><strong>Why 5 nodes exactly and no more (§1 #1, DEC-168)?</strong> Graph topology is a contract. New nodes (e.g. \"post-process LLM response\" or \"rate-limit check\") add hidden state transitions that consumers can't reason about. Five nodes is the minimum complete set: parse → score → decide → act → record. Anything else is a refactor — and a refactor of the supervisor is a new task, not a code change.</p>\n<p><strong>Why no fallback to direct provider on AI Gateway failure (§1 #11)?</strong> AI Gateway failure should be a sev-1 alarm, not silently routed-around. If CUO falls back to direct provider on gateway failure, the cost ledger and residency pinning are silently bypassed — and operators never see the gateway outage because CUO masks it. The right behaviour: cascade fails → fall through to <code>ask</code> (no LLM input but user sees alternatives); the OTel <code>cuo_supervisor_llm_cascade_total{outcome=gateway_error}</code> counter triggers an alarm.</p>\n<p><strong>Why <code>next_step: null</code> field explicitly present (§1 #25, §1 #26)?</strong> TASK-CUO-104 ships multi-step chain walks; the consumer reads <code>decision.next_step</code> to plan the next supervisor invocation. If slice 2 omits the field, TASK-CUO-104 must handle both shapes (field-present + field-absent), which is two code paths to test forever. Setting it explicitly to <code>null</code> makes the contract one-shape — forward-compatible without ambiguity.</p>\n<p><strong>Why persona JWT validation at every state entry (§1 #10, DEC-166)?</strong> The persona claim is the \"who is acting\" identity. Validating it at supervisor entry prevents a <code>genie</code> JWT from triggering a <code>cfo</code>-persona routing path — which would bypass the CFO's defer-to-human matrix. The check is at the parse node; missing/mismatched persona → fail fast at entry, never reach the scorer.</p>\n<p><strong>Why the LLM cascade emits a <code>cuo.llm_cascade_timeout</code> row on timeout (§1 #6)?</strong> Timeouts are AI decisions (\"AI failed to provide structured pick within budget\"). The memory row preserves the fact that an LLM was consulted and timed out; useful for SRE analysis (\"which queries consistently timeout\") and EU AI Act Art. 12 (\"show me decisions where LLM was attempted but produced no usable output\"). The cost of the row is ~700 bytes; the benefit is full audit reconstruction.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — CuoState schema</h3>\n<pre><code class=\"language-python\"># cuo/cuo/supervisor/state.py\nfrom typing import TypedDict, Literal, NotRequired\nfrom pydantic import BaseModel\n\nCUO_STATE_V = 1\nCASCADE_THRESHOLD_LOW = 0.10\nCASCADE_THRESHOLD_HIGH = 0.50\nASK_THRESHOLD = 0.70\n\nPathTaken = Literal[\"auto\", \"ask\", \"cascade\", \"defer\", \"cascade_then_auto\", \"cascade_then_ask\"]\n\nclass Candidate(BaseModel):\n    skill_name: str\n    confidence: float            # 0.0 – 1.0\n    arguments: dict\n    score_components: dict       # name_match, keyword_hits, region_bonus, etc.\n\nclass LlmRoutingPick(BaseModel):\n    skill_name: str\n    arguments: dict\n    rationale: str               # 1–500 chars\n    confidence: float\n\nclass InvocationResult(BaseModel):\n    skill_name: str\n    exit_code: int\n    stdout: str\n    stderr: str\n    duration_ms: float\n\nclass TransparencyDisclosure(BaseModel):\n    skill_chosen: str | None\n    confidence: float\n    alternatives: list[Candidate]\n    path_taken: PathTaken\n    llm_used: bool\n\nclass CuoState(TypedDict):\n    # — request envelope —\n    query: str\n    tenant_id: str\n    subject_id: str\n    persona_key: str\n    request_id: str\n    ts_ns_start: int\n    cuo_state_v: int             # always CUO_STATE_V; for replay-version-checks\n\n    # — scoring + routing —\n    rule_scores: list[Candidate]\n    branch_decision: PathTaken\n    cascade_taken: bool          # set true after first cascade entry; prevents re-entry\n\n    # — LLM cascade —\n    llm_pick: NotRequired[LlmRoutingPick]\n    llm_attempts: int            # max 2 (first + 1 retry); see §1 #23\n    llm_started_at: NotRequired[float]\n\n    # — invocation —\n    invocation_result: NotRequired[InvocationResult]\n\n    # — output —\n    transparency: NotRequired[TransparencyDisclosure]\n    next_step: None              # slice 2 always null; TASK-CUO-104 fills\n\n    # — bookkeeping —\n    ts_ns_end: NotRequired[int]\n    audit_emitted: bool</code></pre>\n<h3>3.2 — Graph construction</h3>\n<pre><code class=\"language-python\"># cuo/cuo/supervisor/graph.py\nfrom langgraph.graph import StateGraph, END\nfrom cyberos.cuo.supervisor.state import CuoState, PathTaken\nfrom cyberos.cuo.supervisor.nodes import parse, rule_score, branch, llm_cascade, invoke, record\n\ndef build_supervisor_graph() -&gt; StateGraph:\n    g = StateGraph(CuoState)\n\n    # Node registration — exactly 5 user-visible nodes (parse, rule_score, branch, invoke, record).\n    # llm_cascade is reached via conditional edge from `branch`; it re-enters `branch`.\n    g.add_node(\"parse\", parse.parse_node)\n    g.add_node(\"rule_score\", rule_score.rule_score_node)\n    g.add_node(\"branch\", branch.branch_node)\n    g.add_node(\"llm_cascade\", llm_cascade.cascade_node)\n    g.add_node(\"invoke\", invoke.invoke_node)\n    g.add_node(\"record\", record.record_node)\n\n    g.set_entry_point(\"parse\")\n    g.add_edge(\"parse\", \"rule_score\")\n    g.add_edge(\"rule_score\", \"branch\")\n\n    # Conditional edges from `branch` based on path_taken.\n    def route_from_branch(state: CuoState) -&gt; str:\n        pt: PathTaken = state[\"branch_decision\"]\n        if pt == \"auto\" or pt == \"cascade_then_auto\":\n            return \"invoke\"\n        if pt == \"cascade\":\n            return \"llm_cascade\"\n        if pt in {\"ask\", \"defer\", \"cascade_then_ask\"}:\n            return \"record\"\n        raise ValueError(f\"unknown path_taken: {pt}\")\n\n    g.add_conditional_edges(\"branch\", route_from_branch, {\n        \"invoke\": \"invoke\",\n        \"llm_cascade\": \"llm_cascade\",\n        \"record\": \"record\",\n    })\n    g.add_edge(\"llm_cascade\", \"branch\")  # cascade re-enters branch with LLM confidence\n    g.add_edge(\"invoke\", \"record\")\n    g.add_edge(\"record\", END)\n\n    return g.compile(checkpointer=InMemoryCheckpointer())  # Postgres in TASK-CUO-102</code></pre>\n<h3>3.3 — Branch node logic</h3>\n<pre><code class=\"language-python\"># cuo/cuo/supervisor/nodes/branch.py\nfrom cyberos.cuo.supervisor.state import (\n    CuoState, PathTaken, CASCADE_THRESHOLD_LOW, CASCADE_THRESHOLD_HIGH, ASK_THRESHOLD\n)\n\ndef branch_node(state: CuoState) -&gt; CuoState:\n    top = state[\"rule_scores\"][0] if state[\"rule_scores\"] else None\n    cascade_taken = state.get(\"cascade_taken\", False)\n\n    # If LLM cascade returned a pick, use its confidence for branching.\n    if cascade_taken and state.get(\"llm_pick\"):\n        conf = state[\"llm_pick\"].confidence\n        if conf &gt;= ASK_THRESHOLD:\n            state[\"branch_decision\"] = \"cascade_then_auto\"\n        else:\n            state[\"branch_decision\"] = \"cascade_then_ask\"\n        return state\n\n    if top is None or top.confidence &lt; CASCADE_THRESHOLD_LOW:\n        state[\"branch_decision\"] = \"defer\"\n    elif top.confidence &gt;= ASK_THRESHOLD:\n        state[\"branch_decision\"] = \"auto\"\n    elif top.confidence &gt;= CASCADE_THRESHOLD_HIGH:\n        state[\"branch_decision\"] = \"ask\"\n    else:  # 0.10 ≤ conf &lt; 0.50\n        if cascade_taken:\n            state[\"branch_decision\"] = \"ask\"  # already cascaded once; never twice\n        else:\n            state[\"branch_decision\"] = \"cascade\"\n    return state</code></pre>\n<h3>3.4 — LLM cascade node</h3>\n<pre><code class=\"language-python\"># cuo/cuo/supervisor/nodes/llm_cascade.py\nimport asyncio\nimport time\nfrom pydantic import ValidationError\nfrom cyberos.cuo.supervisor.state import CuoState, LlmRoutingPick\nfrom cyberos.cuo.supervisor.litellm_proxy import litellm_call_via_gateway\nfrom cyberos.cuo.supervisor.persona import get_persona\nfrom cyberos.cuo.supervisor.audit import emit_cascade_timeout_row\n\nLLM_BUDGET_SECONDS = 3.0\nMAX_LLM_ATTEMPTS = 2\n\nasync def cascade_node(state: CuoState) -&gt; CuoState:\n    state[\"cascade_taken\"] = True\n    state[\"llm_started_at\"] = time.monotonic()\n    persona = get_persona(state[\"persona_key\"])\n    prompt = build_cascade_prompt(state, persona)\n\n    for attempt in range(1, MAX_LLM_ATTEMPTS + 1):\n        state[\"llm_attempts\"] = attempt\n        try:\n            response_text = await asyncio.wait_for(\n                litellm_call_via_gateway(prompt, tenant_id=state[\"tenant_id\"]),\n                timeout=LLM_BUDGET_SECONDS,\n            )\n        except asyncio.TimeoutError:\n            emit_cascade_timeout_row(state)\n            # Fall through to ask (do not set llm_pick).\n            return state\n\n        try:\n            pick = LlmRoutingPick.model_validate_json(response_text)\n        except ValidationError:\n            if attempt &lt; MAX_LLM_ATTEMPTS:\n                prompt = strengthen_prompt(prompt)\n                continue\n            # Final retry failed — fall through to ask.\n            return state\n\n        if pick.skill_name not in state[\"rule_scores_known_names\"]:\n            # LLM hallucinated a skill not in catalog — reject.\n            if attempt &lt; MAX_LLM_ATTEMPTS:\n                prompt = strengthen_prompt(prompt)\n                continue\n            return state\n\n        state[\"llm_pick\"] = pick\n        return state\n    return state</code></pre>\n<h3>3.5 — LiteLLM proxy</h3>\n<pre><code class=\"language-python\"># cuo/cuo/supervisor/litellm_proxy.py\n\"\"\"LiteLLM-shaped client that ALWAYS routes through AI Gateway TASK-AI-008.\n\nDirect provider SDKs (boto3, anthropic, openai) MUST NOT be imported in this package.\nThe architectural test `test_supervisor_litellm_routes_via_gateway` AST-walks the\npackage and rejects forbidden imports.\n\"\"\"\nimport httpx\nimport os\n\nAI_GATEWAY_URL = os.environ.get(\"CYBEROS_AI_GATEWAY_URL\", \"http://localhost:8080\")\n\nasync def litellm_call_via_gateway(prompt: str, tenant_id: str) -&gt; str:\n    \"\"\"Send a chat request to the AI Gateway and return the model's response text.\n\n    Mirrors LiteLLM's `litellm.acompletion()` shape so prompts can be authored\n    in the LiteLLM ecosystem and transparently routed through the gateway.\n    \"\"\"\n    async with httpx.AsyncClient(timeout=30.0) as client:\n        resp = await client.post(\n            f\"{AI_GATEWAY_URL}/v1/ai/chat\",\n            json={\n                \"tenant_id\": tenant_id,\n                \"model\": \"haiku@cuo-supervisor\",   # alias resolved by TASK-AI-006\n                \"messages\": [{\"role\": \"user\", \"content\": prompt}],\n                \"response_format\": {\"type\": \"json_object\"},\n                \"temperature\": 0.0,                  # deterministic for replay\n                \"max_tokens\": 500,\n            },\n            headers={\"X-Cuo-Decision-Id\": _new_decision_id()},\n        )\n        resp.raise_for_status()\n        return resp.json()[\"choices\"][0][\"message\"][\"content\"]</code></pre>\n<h3>3.6 — Persona catalogue</h3>\n<pre><code class=\"language-python\"># cuo/cuo/supervisor/persona.py\nfrom pydantic import BaseModel\n\nclass Persona(BaseModel):\n    key: str\n    display_name: str\n    keyword_bank: list[str]\n    system_prompt: str\n    defer_to_human_matrix: list[str]  # operation types this persona refuses to auto-invoke\n\nPERSONA_CATALOGUE = {\n    \"genie\": Persona(\n        key=\"genie\",\n        display_name=\"Genie\",\n        keyword_bank=[],  # base; no specialised keywords\n        system_prompt=\"You are Genie — the unspecialised CUO entry point. Route to a specialist persona when query intent is clear.\",\n        defer_to_human_matrix=[\"wire_transfer\", \"subject_role_grant_reserved\"],\n    ),\n    \"cfo\": Persona(\n        key=\"chief-financial-officer\",\n        display_name=\"CFO\",\n        keyword_bank=[\"invoice\", \"vat\", \"hoa don\", \"bhxh\", \"p1\", \"p2\", \"p3\", \"payroll\", \"approve\"],\n        system_prompt=\"You are the CFO persona. Refuse to auto-execute disbursements; require human sign-off.\",\n        defer_to_human_matrix=[\"wire_transfer\", \"invoice_emit\", \"payroll_commit\", \"esop_grant_signoff\", \"po_cancellation\"],\n    ),\n    \"cto\": Persona(\n        key=\"chief-technology-officer\",\n        display_name=\"CTO\",\n        keyword_bank=[\"deploy\", \"rollback\", \"incident\", \"p0\", \"p1\", \"tech-debt\", \"security\"],\n        system_prompt=\"You are the CTO persona. Refuse to auto-execute production deploys or rollbacks.\",\n        defer_to_human_matrix=[\"prod_deploy\", \"prod_rollback\", \"key_rotation\"],\n    ),\n    # ... 8 more personas (CEO, COO, CMO, CHRO, CSO, CLO, CDO, CPO) ...\n}\n\ndef get_persona(key: str) -&gt; Persona:\n    if key not in PERSONA_CATALOGUE:\n        raise ValueError(f\"unknown_persona: {key}\")\n    return PERSONA_CATALOGUE[key]</code></pre>\n<h3>3.7 — Audit row builder</h3>\n<pre><code class=\"language-python\"># cuo/cuo/supervisor/audit.py\nimport hashlib\nfrom cyberos.cuo.supervisor.state import CuoState\n\ndef build_routing_decision_row(state: CuoState) -&gt; dict:\n    \"\"\"Build the canonical `cuo.routing_decision` memory audit row.\n\n    Emitted by the `record` node on EVERY path (auto, ask, cascade, defer).\n    \"\"\"\n    pii_scrubbed_query = apply_memory_pii_rules(state[\"query\"])  # TASK-MEMORY-111\n    return {\n        \"kind\": \"cuo.routing_decision\",\n        \"tenant_id\": state[\"tenant_id\"],\n        \"subject_id_hash16\": hashlib.sha256(state[\"subject_id\"].encode()).hexdigest()[:16],\n        \"persona_key\": state[\"persona_key\"],\n        \"persona_version\": state.get(\"persona_version\"),\n        \"query\": pii_scrubbed_query,\n        \"rule_scores\": [c.model_dump() for c in state[\"rule_scores\"][:3]],\n        \"path_taken\": state[\"branch_decision\"],\n        \"llm_pick\": state[\"llm_pick\"].model_dump() if state.get(\"llm_pick\") else None,\n        \"invocation_result\": state[\"invocation_result\"].model_dump() if state.get(\"invocation_result\") else None,\n        \"cuo_state_v\": state[\"cuo_state_v\"],\n        \"next_step\": None,    # slice 2 contract; TASK-CUO-104 fills\n        \"request_id\": state[\"request_id\"],\n        \"trace_id\": current_trace_id_hex(),\n        \"ts_ns_start\": state[\"ts_ns_start\"],\n        \"ts_ns_end\": state.get(\"ts_ns_end\"),\n    }</code></pre>\n<h3>3.8 — CLI entry point</h3>\n<pre><code class=\"language-python\"># cuo/cuo/cli/supervisor.py\nimport asyncio\nimport click\nimport json\nfrom cyberos.cuo.supervisor import run_supervisor\n\n@click.group()\ndef supervisor():\n    \"\"\"CUO Phase 2 supervisor commands.\"\"\"\n\n@supervisor.command()\n@click.option(\"--query\", required=True, help=\"Natural-language request\")\n@click.option(\"--persona\", default=\"genie\")\n@click.option(\"--invoke/--no-invoke\", default=True)\n@click.option(\"--record/--no-record\", default=True)\n@click.option(\"--json/--no-json\", \"json_out\", default=False)\ndef route(query: str, persona: str, invoke: bool, record: bool, json_out: bool):\n    \"\"\"Route a query through the supervisor.\"\"\"\n    result = asyncio.run(run_supervisor(query=query, persona_key=persona, do_invoke=invoke, do_record=record))\n    if json_out:\n        click.echo(json.dumps(result.model_dump(), ensure_ascii=False, indent=2))\n    else:\n        click.echo(result.transparency.summary_line())\n    exit_code = {\n        \"auto\": 0, \"cascade_then_auto\": 0,\n        \"ask\": 1, \"cascade_then_ask\": 1,\n        \"defer\": 2,\n    }.get(result.path_taken, 65)\n    raise SystemExit(exit_code)</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Graph topology fixed at 5 nodes</strong> — <code>test_supervisor_graph::test_five_nodes_only</code> asserts <code>len(graph.nodes) == 5</code> with names <code>{parse, rule_score, branch, llm_cascade, invoke, record}</code> minus <code>llm_cascade</code> (auxiliary). User-visible decision points are 5.</li><li><strong>Rule path (≥ 0.70)</strong> — query \"validate MST 0301479073\" with cfo persona → score 0.85 → auto-invoke → <code>path_taken = \"auto\"</code> → exit 0.</li><li><strong>Ask path (0.50–0.70)</strong> — query \"send invoice\" → score 0.62 → return alternatives top 3 → <code>path_taken = \"ask\"</code> → exit 1.</li><li><strong>Cascade path (0.10–0.50)</strong> — query \"what should we do about the unpaid Q4 contract\" → rule score 0.30 → LLM cascade → LLM returns <code>{skill_name: \"inv-dunning-draft\", confidence: 0.85}</code> → <code>path_taken = \"cascade_then_auto\"</code> → exit 0.</li><li><strong>Defer path (&lt; 0.10)</strong> — query \"asdfghjkl\" → rule score 0.03 → <code>path_taken = \"defer\"</code> → exit 2.</li><li><strong>Cascade fall-through to ask</strong> — LLM returns confidence 0.40 → <code>path_taken = \"cascade_then_ask\"</code> → exit 1.</li><li><strong>No cascade re-entry</strong> — query in cascade band; first cascade returns 0.20; <code>cascade_taken = true</code> blocks re-entry → falls through to <code>ask</code>.</li><li><strong>LLM timeout</strong> — mocked AI Gateway delays 5s; supervisor falls through to <code>ask</code>; <code>cuo.llm_cascade_timeout</code> memory row emitted.</li><li><strong>LLM freeform rejected</strong> — mocked AI Gateway returns \"I think you should...\" → Pydantic validation fails twice → fall through to <code>ask</code>.</li><li><strong>LLM hallucinated skill rejected</strong> — LLM returns <code>{skill_name: \"make-up-skill\"}</code> not in catalog → reject → retry → fall through to <code>ask</code>.</li><li><strong>Persona matrix blocks auto-invoke</strong> — query routes to <code>vietnam-vat-invoice@1.2</code> with cfo persona at confidence 0.95; cfo.defer_to_human_matrix contains <code>invoice_emit</code> → refuse auto, return <code>{routed: false, reason: \"persona_defer_matrix\"}</code>; <code>cuo.persona_defer_block</code> memory row.</li><li><strong>Destructive skill bypass blocked</strong> — skill annotated <code>destructive: true</code> at confidence 1.0 → capability broker TASK-SKILL-104 gates via Elicitation; supervisor's invoke node refuses to override.</li><li><strong>Persona JWT mismatch</strong> — caller JWT has <code>agent_persona: cuo-cto@0.1.0</code> but requests <code>cfo</code> persona → 403 with <code>persona_mismatch</code>.</li><li><strong>Unknown persona</strong> — <code>--persona foo</code> → exit 65, <code>cuo.persona_unknown</code> memory row.</li><li><strong>Direct provider import forbidden</strong> — <code>test_supervisor_litellm_routes_via_gateway</code> AST-walks <code>cuo/cuo/supervisor/</code> for <code>import boto3 | import anthropic | import openai | from openai</code>; finding any → test fails.</li><li><strong>Cuo state version present</strong> — state contains <code>cuo_state_v == 1</code>; replay with <code>cuo_state_v == 4</code> rejected with <code>state_version_unsupported</code>.</li><li><strong>Audit row emitted every path</strong> — assert exactly one <code>cuo.routing_decision</code> row after <code>route</code> for each of {auto, ask, cascade, defer}.</li><li><strong>Audit row carries <code>next_step: null</code> explicitly</strong> — JSON serialised row has <code>\"next_step\": null</code> (key present), not absent.</li><li><strong>Audit row PII-scrubbed query</strong> — query containing email/phone is scrubbed in the memory row but preserved in OTel span (transient).</li><li><strong>Replay equivalence on rule path</strong> — 15-golden-fixture suite: run twice on identical catalog → byte-identical decision JSONs.</li><li><strong>Rule path &lt; 50ms p95</strong> — <code>test_supervisor_perf_rule_path</code> 1000 iterations; p95 &lt; 50 ms.</li><li><strong>Cascade path &lt; 3500ms p95</strong> — integration test with mocked AI Gateway returning at 2500 ms; p95 &lt; 3500 ms.</li><li><strong>OTel span <code>cuo.supervisor.route</code> emitted with all required attributes</strong> — <code>tenant_id</code>, <code>subject_id_hash16</code>, <code>persona_key</code>, <code>path_taken</code>, <code>confidence</code>, <code>llm_used</code>, <code>outcome</code>.</li><li><strong>OTel child spans per node</strong> — assert spans <code>cuo.supervisor.node.{parse,rule_score,branch,llm_cascade,invoke,record}</code> present in trace tree.</li><li><strong>Counter <code>cuo_supervisor_route_total{outcome=success, persona=cfo, path_taken=auto}</code> increments</strong> — every successful route bumps it.</li><li><strong>Counter <code>cuo_supervisor_persona_defer_blocks_total{persona=cfo, operation=invoice_emit}</code> increments</strong> — every defer-matrix block bumps it.</li><li><strong>CLI exit codes correct</strong> — auto→0, ask→1, defer→2, unknown_persona→65, persona_mismatch→77, timeout→75.</li><li><strong>Dry-run mode</strong> — <code>--no-invoke --no-record</code> returns decision without invocation and without memory row; OTel span <code>cuo.dry_run</code> emitted instead.</li><li><strong>Transparency disclosure present</strong> — every response includes <code>transparency: {skill_chosen, confidence, alternatives, path_taken, llm_used}</code>.</li><li><strong>EU AI Act Art. 12 replay</strong> — given a stored memory row + catalog snapshot at the row's timestamp, running the supervisor produces an identical <code>path_taken</code> and <code>llm_pick</code>.</li><li><strong>Caller <code>traceparent</code> propagated to AI Gateway</strong> — integration test: outbound request to gateway carries the same <code>traceparent</code> as the inbound caller request.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-python\"># cuo/tests/test_supervisor_graph.py\nfrom cyberos.cuo.supervisor.graph import build_supervisor_graph\n\ndef test_five_nodes_only():\n    g = build_supervisor_graph()\n    node_names = set(g.nodes.keys())\n    assert node_names == {\"parse\", \"rule_score\", \"branch\", \"llm_cascade\", \"invoke\", \"record\"}\n\ndef test_entry_point_is_parse():\n    g = build_supervisor_graph()\n    assert g.entry_point == \"parse\"</code></pre>\n<pre><code class=\"language-python\"># cuo/tests/test_supervisor_litellm_routes_via_gateway.py\nimport ast\nfrom pathlib import Path\n\nSUPERVISOR_DIR = Path(\"cuo/cuo/supervisor\")\nFORBIDDEN_IMPORTS = {\"boto3\", \"anthropic\", \"openai\", \"google.cloud.aiplatform\"}\n\ndef _collect_imports(tree: ast.AST) -&gt; set[str]:\n    out = set()\n    for node in ast.walk(tree):\n        if isinstance(node, ast.Import):\n            for alias in node.names: out.add(alias.name.split(\".\")[0])\n        elif isinstance(node, ast.ImportFrom):\n            if node.module: out.add(node.module.split(\".\")[0])\n    return out\n\ndef test_no_direct_provider_imports_in_supervisor():\n    found = set()\n    for py in SUPERVISOR_DIR.rglob(\"*.py\"):\n        tree = ast.parse(py.read_text())\n        found |= _collect_imports(tree)\n    leakage = found &amp; FORBIDDEN_IMPORTS\n    assert not leakage, f\"supervisor imports forbidden providers: {leakage}\"</code></pre>\n<pre><code class=\"language-python\"># cuo/tests/test_supervisor_persona_defer_matrix.py\nimport pytest\nfrom cyberos.cuo.supervisor import run_supervisor\n\n@pytest.mark.asyncio\nasync def test_cfo_persona_blocks_invoice_emit_even_at_high_confidence(mock_catalog):\n    mock_catalog.set_score(\"vietnam-vat-invoice@1.2\", 0.95)  # high confidence\n    result = await run_supervisor(\n        query=\"emit hoa don for ACME 4M VND\",\n        persona_key=\"chief-financial-officer\",\n        do_invoke=True, do_record=True,\n    )\n    assert result.routed is False\n    assert result.reason == \"persona_defer_matrix\"\n    assert result.operation == \"invoice_emit\"\n    # memory row was emitted\n    rows = memory_audit_rows_for_request(result.request_id)\n    assert any(r[\"kind\"] == \"cuo.persona_defer_block\" for r in rows)</code></pre>\n<pre><code class=\"language-python\"># cuo/tests/test_supervisor_audit_row.py\nimport pytest\nfrom cyberos.cuo.supervisor import run_supervisor\n\n@pytest.mark.parametrize(\"query, expected_path\", [\n    (\"validate MST 0301479073\",            \"auto\"),\n    (\"send invoice\",                       \"ask\"),\n    (\"what should we do about Q4 churn\",   \"cascade_then_auto\"),\n    (\"asdfghjkl\",                          \"defer\"),\n])\n@pytest.mark.asyncio\nasync def test_audit_row_emitted_on_every_path(query, expected_path, mock_catalog, memory_rows):\n    pre = len(memory_rows.by_kind(\"cuo.routing_decision\"))\n    await run_supervisor(query=query, persona_key=\"genie\")\n    post = memory_rows.by_kind(\"cuo.routing_decision\")\n    assert len(post) == pre + 1\n    assert post[-1][\"path_taken\"] == expected_path\n    assert \"next_step\" in post[-1] and post[-1][\"next_step\"] is None   # AC #18</code></pre>\n<pre><code class=\"language-python\"># cuo/tests/test_supervisor_idempotency.py\nimport json\nimport pytest\nfrom cyberos.cuo.supervisor import run_supervisor\n\n@pytest.mark.asyncio\nasync def test_replay_equivalence_on_15_golden_fixtures():\n    fixtures = load_golden_fixtures(\"cuo/tests/fixtures/golden_queries.jsonl\")\n    decisions = []\n    for query in fixtures:\n        r = await run_supervisor(query=query, persona_key=\"genie\", do_invoke=False, do_record=False)\n        decisions.append(r.model_dump_json(exclude={\"request_id\", \"ts_ns_start\", \"ts_ns_end\"}))\n    # Replay\n    decisions_replay = []\n    for query in fixtures:\n        r = await run_supervisor(query=query, persona_key=\"genie\", do_invoke=False, do_record=False)\n        decisions_replay.append(r.model_dump_json(exclude={\"request_id\", \"ts_ns_start\", \"ts_ns_end\"}))\n    assert decisions == decisions_replay, \"replay produced different decisions\"</code></pre>\n<pre><code class=\"language-python\"># cuo/tests/test_supervisor_perf_rule_path.py\nimport asyncio\nimport time\nimport pytest\nfrom cyberos.cuo.supervisor import run_supervisor\n\n@pytest.mark.asyncio\nasync def test_rule_path_under_50ms_p95():\n    latencies = []\n    for _ in range(1000):\n        t0 = time.monotonic()\n        await run_supervisor(query=\"validate MST 0301479073\", persona_key=\"chief-financial-officer\", do_invoke=False, do_record=False)\n        latencies.append((time.monotonic() - t0) * 1000.0)\n    latencies.sort()\n    p95 = latencies[int(len(latencies) * 0.95)]\n    assert p95 &lt; 50.0, f\"rule path p95 = {p95:.1f}ms &gt; 50ms budget\"</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton. The remaining 8 persona definitions in §3.6 — CEO/COO/CMO/CHRO/CSO/CLO/CDO/CPO — are filled in during implementation following the same shape as chief-financial-officer/cto.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream (this task depends on):</strong></p>\n<ul><li><strong>TASK-AI-008</strong> — multi-provider router; <code>litellm_proxy</code> forwards every cascade LLM call to the AI Gateway's <code>/v1/ai/chat</code> endpoint.</li></ul>\n<p><strong>Downstream (this task blocks — all 14 are placeholders / not yet specified):</strong></p>\n<ul><li><strong>TASK-CUO-102</strong> — Postgres checkpointer; replaces this task's in-memory checkpointer scaffold.</li><li><strong>TASK-CUO-103</strong> — Phase 2 trace rows with prompt + model + temperature + seed for replay.</li><li><strong>TASK-CUO-104</strong> — topological walk of <code>depends_on</code> chain; consumes this task's <code>next_step</code> field.</li><li><strong>TASK-CRM-005, TASK-CRM-006, TASK-CRM-007</strong> — CRM-side CUO skills (next-action, lead scoring, win/loss analysis).</li><li><strong>TASK-DOC-009</strong> — renewal proposal CUO draft.</li><li><strong>TASK-EMAIL-008</strong> — Genie subject-prefix routing.</li><li><strong>TASK-INV-010</strong> — dunning draft CUO.</li><li><strong>TASK-OKR-006, TASK-OKR-007</strong> — Monday digest + quarterly retro CUO drafts.</li><li><strong>TASK-PROJ-011, TASK-PROJ-012</strong> — blocker notification + cycle-review draft (already specced; they consume CUO via webhook).</li><li><strong>TASK-RES-004</strong> — hiring memo CUO draft.</li></ul>\n<p><strong>Cross-module (informational):</strong></p>\n<ul><li><strong>TASK-AI-014</strong> — persona-version stamping; <code>cuo-&lt;persona-key&gt;@&lt;semver&gt;</code> is validated at parse node.</li><li><strong>TASK-AUTH-101</strong> — RBAC catalogue; <code>agent-persona</code> role must be present in claims.</li><li><strong>TASK-AI-003</strong> — memory audit bridge; receives <code>cuo.routing_decision</code>, <code>cuo.llm_cascade_timeout</code>, <code>cuo.persona_defer_block</code>, <code>cuo.dry_run</code>, <code>cuo.persona_unknown</code>.</li><li><strong>TASK-SKILL-104</strong> — capability broker; gates destructive-skill invocation regardless of supervisor decision.</li><li><strong>TASK-MEMORY-111</strong> — PII detection; applied to <code>query</code> field before chain commit.</li><li><strong>TASK-AI-022</strong> — OTel trace emission; supervisor spans correlate via <code>traceparent</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — CLI invocation (rule path)</h3>\n<pre><code class=\"language-bash\">$ cyberos-cuo supervisor route --query \"validate MST 0301479073\" --persona cfo --json\n{\n  \"routed\": true,\n  \"skill_chosen\": \"vietnam-mst-validate@1.0\",\n  \"confidence\": 0.92,\n  \"path_taken\": \"auto\",\n  \"llm_used\": false,\n  \"alternatives\": [\n    {\"skill\": \"vietnam-bank-transfer@1.0\", \"confidence\": 0.18},\n    {\"skill\": \"crm-account-create@1.0\", \"confidence\": 0.12}\n  ],\n  \"invocation_result\": {\n    \"skill_name\": \"vietnam-mst-validate@1.0\",\n    \"exit_code\": 0,\n    \"stdout\": \"{\\\"mst\\\":\\\"0301479073\\\",\\\"valid\\\":true,\\\"company\\\":\\\"ACME JSC\\\"}\",\n    \"stderr\": \"\",\n    \"duration_ms\": 14.2\n  },\n  \"next_step\": null,\n  \"transparency\": {\n    \"skill_chosen\": \"vietnam-mst-validate@1.0\",\n    \"confidence\": 0.92,\n    \"path_taken\": \"auto\",\n    \"llm_used\": false\n  }\n}</code></pre>\n<h3>8.2 — Cascade-then-auto decision</h3>\n<pre><code class=\"language-bash\">$ cyberos-cuo supervisor route --query \"what should we do about the unpaid Q4 contract from ACME\" --persona genie --json\n{\n  \"routed\": true,\n  \"skill_chosen\": \"inv-dunning-draft@1.2\",\n  \"confidence\": 0.83,\n  \"path_taken\": \"cascade_then_auto\",\n  \"llm_used\": true,\n  \"llm_pick\": {\n    \"skill_name\": \"inv-dunning-draft@1.2\",\n    \"rationale\": \"Query mentions an unpaid contract; dunning draft is the canonical response.\",\n    \"confidence\": 0.83\n  },\n  \"alternatives\": [\n    {\"skill\": \"crm-deal-update@1.0\", \"confidence\": 0.31},\n    {\"skill\": \"email-genie-draft@1.0\", \"confidence\": 0.22}\n  ],\n  \"invocation_result\": { /* ... */ },\n  \"next_step\": null\n}</code></pre>\n<h3>8.3 — Defer decision (no LLM consulted)</h3>\n<pre><code class=\"language-bash\">$ cyberos-cuo supervisor route --query \"asdfghjkl\" --persona genie --json\n{\n  \"routed\": false,\n  \"path_taken\": \"defer\",\n  \"llm_used\": false,\n  \"alternatives\": [],\n  \"next_step\": null,\n  \"transparency\": {\n    \"skill_chosen\": null,\n    \"confidence\": 0.03,\n    \"path_taken\": \"defer\",\n    \"llm_used\": false\n  }\n}</code></pre>\n<h3>8.4 — <code>cuo.routing_decision</code> memory audit row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"cuo.routing_decision\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"persona_key\": \"chief-financial-officer\",\n  \"persona_version\": \"cuo-cfo@0.4.1\",\n  \"query\": \"validate MST [REDACTED-MST]\",\n  \"rule_scores\": [\n    {\"skill_name\": \"vietnam-mst-validate@1.0\", \"confidence\": 0.92, \"arguments\": {\"mst\": \"[REDACTED]\"}, \"score_components\": {\"name_match\": 5.0, \"keyword_hits\": 3.0, \"region_bonus\": 2.0}}\n  ],\n  \"path_taken\": \"auto\",\n  \"llm_pick\": null,\n  \"invocation_result\": {\"skill_name\": \"vietnam-mst-validate@1.0\", \"exit_code\": 0, \"duration_ms\": 14.2},\n  \"cuo_state_v\": 1,\n  \"next_step\": null,\n  \"request_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"trace_id\": \"4bf92f3577b34da6a3ce929d0e0e4736\",\n  \"ts_ns_start\": 1747920731000000000,\n  \"ts_ns_end\": 1747920731017200000\n}</code></pre>\n<h3>8.5 — <code>cuo.persona_defer_block</code> row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"cuo.persona_defer_block\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"persona_key\": \"chief-financial-officer\",\n  \"operation\": \"invoice_emit\",\n  \"rule_top_skill\": \"vietnam-vat-invoice@1.2\",\n  \"rule_top_confidence\": 0.95,\n  \"request_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.6 — <code>cuo.llm_cascade_timeout</code> row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"cuo.llm_cascade_timeout\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"persona_key\": \"genie\",\n  \"query\": \"the long ambiguous query that needed an LLM but timed out\",\n  \"elapsed_ms\": 3012,\n  \"attempts\": 1,\n  \"request_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Persona-specific keyword bank tuning</strong> — Phase 4 (slice 4+). Slice 2 ships seed banks; tuning via golden-fixture analysis lands later.</li><li><strong>Per-tenant confidence threshold override</strong> — slice 3+. Slice 2 hard-codes 0.10/0.50/0.70 for replay determinism.</li><li><strong>Multi-step chain walk</strong> — TASK-CUO-104. Slice 2 ships <code>next_step: null</code> stub.</li><li><strong>Postgres checkpointer</strong> — TASK-CUO-102. Slice 2 ships in-memory scaffold.</li><li><strong>LLM cascade replay</strong> — TASK-CUO-103 captures full prompt + seed for Art. 12 replay; slice 2 stores only model + path.</li><li><strong>Per-persona prompt iteration</strong> — Phase 4 (slice 4+). Slice 2 ships baseline system prompts.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>langgraph</code> import fails (uninstalled)</td><td>Service startup</td><td>Service refuses to start; CI catches before deploy</td><td><code>uv sync</code> reinstall</td></tr><tr><td>LangGraph version drift breaks graph compile</td><td><code>test_supervisor_graph</code></td><td>CI fails</td><td>Pin <code>langgraph==0.2.*</code> in pyproject.toml</td></tr><tr><td>Direct provider import added to supervisor</td><td><code>test_supervisor_litellm_routes_via_gateway</code> AST walk</td><td>CI fails</td><td>Remove direct import; use <code>litellm_proxy</code></td></tr><tr><td>AI Gateway unreachable</td><td><code>httpx.ConnectError</code> in cascade</td><td>Fall through to <code>ask</code>; emit <code>cuo.llm_cascade_timeout</code> with <code>error=gateway_unreachable</code></td><td>OBS sev-2; operator restores gateway</td></tr><tr><td>AI Gateway returns 5xx</td><td><code>resp.raise_for_status()</code></td><td>Fall through to <code>ask</code></td><td>OBS sev-3; gateway-side investigation</td></tr><tr><td>LLM returns malformed JSON</td><td>Pydantic ValidationError</td><td>Retry once with strengthened prompt; if 2nd fails, fall through to <code>ask</code></td><td>None — designed path</td></tr><tr><td>LLM hallucinates skill not in catalog</td><td><code>skill_name not in known_names</code> check</td><td>Retry once; fall through to <code>ask</code></td><td>None — designed path</td></tr><tr><td>LLM exceeds 3-second budget</td><td><code>asyncio.wait_for</code> TimeoutError</td><td>Fall through to <code>ask</code>; emit timeout row</td><td>None — designed path</td></tr><tr><td>LLM cascade re-entry loop</td><td><code>cascade_taken</code> flag check in branch</td><td>Forced to <code>ask</code> on second visit</td><td>None — invariant</td></tr><tr><td>Persona JWT missing</td><td>parse-node validation</td><td>403 <code>persona_mismatch</code></td><td>Caller re-auths with persona claim</td></tr><tr><td>Persona JWT version mismatch (<code>cuo-cfo@0.3.0</code> vs catalogue <code>cuo-cfo@0.4.1</code>)</td><td>parse-node check via TASK-AI-014</td><td>403 with <code>persona_version_mismatch</code></td><td>Refresh-token cycle</td></tr><tr><td>Unknown persona requested (<code>--persona foo</code>)</td><td>parse-node lookup</td><td>exit 65; <code>cuo.persona_unknown</code> row</td><td>Caller fixes persona key</td></tr><tr><td><code>cuo_state_v</code> mismatch on replay</td><td>checkpointer load</td><td><code>state_version_unsupported</code> error</td><td>Manually upgrade state via migration tool</td></tr><tr><td>Destructive skill picked at high confidence</td><td><code>invoke</code> node checks <code>destructive: true</code> annotation</td><td>Capability broker TASK-SKILL-104 gates via Elicitation</td><td>User confirms via Elicitation flow</td></tr><tr><td>Persona defer-matrix blocks high-confidence pick</td><td><code>branch</code> node consults persona.defer_to_human_matrix</td><td>Return <code>routed: false, reason: persona_defer_matrix</code></td><td>Operator handles manually</td></tr><tr><td>memory audit row commit fails</td><td>memory_writer error</td><td>Supervisor returns 500 <code>audit_failed</code>; invocation rolled back if <code>auto</code></td><td>OBS sev-1; memory_writer investigation</td></tr><tr><td>Audit row contains unscrubbed PII</td><td><code>cyberos-memory-pii</code> scrubber called pre-commit; CI test asserts</td><td>Pre-commit failure</td><td>Investigate; add PII rule</td></tr><tr><td>Concurrent supervisors invoke same destructive skill</td><td>Capability broker idempotency check</td><td>Second invocation refused via Elicitation idempotency</td><td>None — designed</td></tr><tr><td>Caller's <code>traceparent</code> malformed</td><td>parse-node validation</td><td>Generate fresh trace at trust boundary per task-audit skill rule 22</td><td>None — designed</td></tr><tr><td>Replay-equivalence broken on rule path</td><td><code>test_supervisor_idempotency</code></td><td>CI fails</td><td>Fix non-determinism (likely a dict iteration order or <code>time.now()</code> leak)</td></tr><tr><td>Rule path latency &gt; 50ms p95</td><td><code>test_supervisor_perf_rule_path</code></td><td>CI fails</td><td>Profile + optimise; common culprits: cold-import, JSON serialisation</td></tr><tr><td>Cascade path latency &gt; 3500ms p95</td><td>Integration test with mocked gateway</td><td>CI fails</td><td>Verify timeout budget; reduce prompt size</td></tr><tr><td>11-persona catalogue divergence between docs + code</td><td><code>test_persona_catalogue_matches_docs</code> (asserts 11 keys present)</td><td>CI fails</td><td>Sync persona.py with cuo.html §personas</td></tr><tr><td><code>defer_to_human_matrix</code> empty for persona</td><td>Persona schema requires ≥ 1 entry for C-level personas</td><td>Schema validation fails at module import</td><td>Fix persona definition</td></tr><tr><td><code>next_step</code> field absent (not null) in row</td><td>JSON-schema validation in memory_writer</td><td>Reject row</td><td>Fix audit.py builder</td></tr><tr><td>OTel span attributes missing</td><td><code>test_supervisor_otel_attributes</code></td><td>CI fails</td><td>Add missing attribute to span builder</td></tr><tr><td>Counter cardinality explosion (tenant_id label)</td><td>Cardinality budget alarm in OBS</td><td>sev-3</td><td>Aggregate by tenant_id only at Grafana query layer</td></tr><tr><td>Dry-run mode accidentally emits memory row</td><td><code>test_supervisor_dry_run_no_row</code></td><td>CI fails</td><td>Fix conditional in record node</td></tr><tr><td>TASK-CUO-102 ships Postgres checkpointer but state schema unchanged</td><td>Forward-compat by <code>cuo_state_v</code> tolerance ±2</td><td>Works without changes</td><td>None — designed</td></tr><tr><td>TASK-CUO-104 ships chain walk; this task's <code>next_step</code> always null</td><td>Forward-compat by always-null contract</td><td>TASK-CUO-104 fills the field</td><td>None — designed</td></tr><tr><td>Tenant residency policy denies LLM call</td><td>TASK-AI-016 returns 451 at gateway</td><td>Cascade fails → fall through to <code>ask</code>; emit timeout-shaped row with <code>error=residency_denied</code></td><td>Operator reviews residency policy</td></tr><tr><td>Cost ledger insufficient budget</td><td>TASK-AI-001 precheck rejects at gateway</td><td>Cascade fails → fall through to <code>ask</code>; emit row with <code>error=budget_exceeded</code></td><td>Operator tops up budget or operator overrides</td></tr><tr><td>Catalog snapshot hash mismatch between request + record</td><td>Catch in record node</td><td>sev-3 alarm; record row anyway with snapshot mismatch flagged</td><td>Investigate catalog reload race</td></tr><tr><td>State serialisation fails (non-JSON value in arguments)</td><td>Pydantic dump</td><td>500 <code>state_serialise_failed</code>; no memory row</td><td>Fix arguments schema</td></tr><tr><td><code>persona.py</code> missing a C-level persona</td><td><code>test_persona_catalogue_complete</code></td><td>CI fails</td><td>Add missing persona</td></tr><tr><td>Cascade prompt exceeds context window</td><td>Pre-prompt token estimator</td><td>Truncate from oldest rule_score; warn in span</td><td>None — designed</td></tr><tr><td>Replay across <code>cuo_state_v</code> ±3 attempted</td><td><code>state_version_unsupported</code> raised</td><td>500 error</td><td>Manual state migration tool</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>LangGraph is the orchestration framework, not the LLM</strong> — confusing the two trips up newcomers. LangGraph = state-machine framework with Postgres checkpointing; LiteLLM = LLM provider abstraction; AI Gateway = our actual LLM gateway. CUO is LangGraph orchestrating LiteLLM-shaped calls THROUGH the AI Gateway.</li><li><strong><code>asyncio.wait_for</code> is the right timeout primitive</strong>, not <code>asyncio.timeout()</code> (3.11+ context manager). <code>wait_for</code> cancels the underlying task cleanly; <code>timeout()</code> requires careful exception handling that's easy to get wrong.</li><li><strong><code>InMemoryCheckpointer</code> is intentional for slice 2</strong> — checkpointing is for state recovery on restart, but slice-2 requests are stateless per request (no multi-step chains yet). Adding the Postgres dependency now would buy us complexity without value; TASK-CUO-102 ships when chains land.</li><li><strong><code>temperature: 0.0</code> in litellm_proxy</strong> — replay determinism requires zero-temperature sampling. Cost: marginal quality loss on edge cases. Benefit: replay-equivalence claim is meaningful.</li><li><strong><code>response_format: {\"type\": \"json_object\"}</code></strong> — forces the LLM into structured-output mode at the provider; Pydantic validation is the second line of defence. Together they make the parse-success rate &gt; 99%.</li><li><strong><code>max_tokens: 500</code></strong> — bounds the LLM response size; <code>rationale</code> field is capped at 500 chars; <code>skill_name + arguments</code> together fit in ~200 tokens; 500 is safe with slack.</li><li><strong><code>X-Cuo-Decision-Id</code> header on the AI Gateway call</strong> — lets the gateway chain its own <code>ai.precheck/postcall_reconcile</code> rows under the same decision ID. Audit-trail follow-through across services.</li><li><strong><code>cascade_taken</code> flag is mutated, not removed</strong> — keeping the flag in state through the second branch entry lets the audit row capture \"cascade was attempted but didn't help\" — useful for the analyst's \"when does cascade rescue confidence\" report.</li><li><strong><code>llm_attempts</code> counter at most 2</strong> — first attempt + 1 retry with strengthened prompt. Tested in <code>test_supervisor_freeform_rejection</code>. More retries waste budget.</li><li><strong><code>PERSONA_CATALOGUE</code> is a module-level dict</strong> — loaded at import time. Not in a database. Adding a persona is an ADR + code change, same discipline as adding an RBAC role.</li><li><strong><code>defer_to_human_matrix</code> lists operation TYPES, not skill names</strong> — operations are abstract (e.g. <code>invoice_emit</code>); skills implement them. The skill catalogue carries <code>operation: invoice_emit</code> annotation; the supervisor consults the matrix by operation, not skill_name. Allows new skills implementing the same operation to inherit the matrix block.</li><li><strong>The graph is recompiled per process</strong> — not per request. <code>build_supervisor_graph()</code> is called once at module import; <code>run_supervisor</code> reuses the compiled graph. Performance: ~5 ms compile, amortised over thousands of requests.</li><li><strong><code>cuo_state_v = 1</code> is the published contract</strong> — future state schema changes must consider backward compatibility for replay; ±2 version tolerance is the published guarantee.</li><li><strong>W3C <code>traceparent</code> is generated at the trust boundary</strong> when caller doesn't provide one (per task-audit skill rule 22). The supervisor entry is a trust boundary — chat/email/etc. may or may not propagate.</li><li><strong><code>OTel sampling 1%</code> for steady-state spans, 100% for non-success</strong> — the high-volume happy path is sampled to keep storage costs sane; the rare error path is always captured for debugging. Matches TASK-OBS-006 tail-sampling pattern.</li><li><strong><code>Counter cardinality</code></strong> — <code>persona</code> label has 11 values (closed catalogue); <code>path_taken</code> has 6 values; <code>outcome</code> has ~6 values. Combined cardinality: ~400. Adding <code>tenant_id</code> would explode this — explicitly NOT included in label set; aggregate at Grafana query time.</li><li><strong><code>request_id</code> is ULID</strong> — sortable by time, 26-char, unique across services. Generated at supervisor entry; embedded in memory row + every child span.</li><li><strong>Persona JWT format <code>cuo-&lt;persona-key&gt;@&lt;semver&gt;</code></strong> — matches TASK-AI-014; the version part lets us roll out persona prompt changes without breaking existing tokens (with grace window).</li><li><strong><code>test_supervisor_litellm_routes_via_gateway</code> is an AST walker, not a runtime check</strong> — runtime would only catch the import if the path were exercised; AST walker catches all imports at CI before any code runs.</li><li><strong><code>apply_memory_pii_rules</code> is the TASK-MEMORY-111 entry point</strong> — same rules used by every memory audit row builder; consistent PII scrubbing across modules.</li><li><strong><code>current_trace_id_hex</code> formats via <code>{}</code> not <code>{:?}</code></strong> — task-audit skill rule 24; Display, not Debug.</li><li><strong>The CLI's exit codes follow <code>cyberos-cli-exit</code></strong> — shared crate per task-audit skill rule 9; tests assert numeric values to catch drift if the shared crate's mapping changes.</li><li><strong>Per-persona defer matrix coverage</strong> — <code>test_persona_catalogue_complete</code> asserts that every C-level persona's matrix has ≥ 1 entry (Genie's matrix is shorter — appropriate for the unspecialised entry point).</li><li><strong><code>max_tokens</code> budget vs <code>response_format: json_object</code></strong> — JSON mode forces structured output; max_tokens limits length. Edge case: LLM produces a valid JSON header <code>{\"skill_name\": \"...</code> and gets cut off mid-rationale. Pydantic validation fails on truncated JSON → fall through to retry. Tested.</li></ul>\n<hr>\n<p><em>End of TASK-CUO-101.</em></p>";
