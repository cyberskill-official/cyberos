window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MEMORY-105"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The <code>cyberos doctor</code> command <strong>MUST</strong> extend its invariant set with five new invariants specific to watched-folders. Each invariant is declared in <code>memory.invariants.yaml.d/watched-folders-v2.yaml</code> (included into the master <code>memory.invariants.yaml</code> via <code>include:</code> directive at the bottom of the master file). The five invariants:</p>\n<ol><li><strong>MUST</strong> add invariant <code>WatchedFolderManifestPresent</code> (severity <code>error</code>): for every entry in <code>manifest.watched_folders[]</code>, the file <code>&lt;folder&gt;/manifest.json</code> MUST exist, be readable, and parse as valid JSON conforming to <code>memory.schema.json#/definitions/WatchedFolderManifest</code>. Failure → agent state transitions to <code>FROZEN_RECOVERABLE</code> (per AGENTS.md §12); repair via <code>cyberos memory watch &lt;path&gt;</code> re-bootstraps the fragment OR <code>cyberos memory unwatch &lt;path&gt;</code> removes the registration.</li></ol>\n<ol><li><strong>MUST</strong> add invariant <code>WatchedFolderResolvable</code> (severity <code>error</code>): for every entry in <code>manifest.watched_folders[]</code>, the path MUST resolve through every symlink (per AGENTS.md §0.4) and the final target MUST exist (<code>stat()</code> succeeds, not ENOENT). Failure → <code>FROZEN_RECOVERABLE</code>; repair via remount the volume OR <code>cyberos memory unwatch &lt;path&gt;</code> if the folder is permanently gone.</li></ol>\n<ol><li><strong>MUST</strong> add invariant <code>WatchedFolderNotShadowed</code> (severity <code>error</code>): no two entries in <code>manifest.watched_folders[]</code> MAY resolve to the same realpath. Ambiguous registration is forbidden by construction. Failure → <code>FROZEN_RECOVERABLE</code>; repair via <code>cyberos memory unwatch</code> on one of the duplicates (the operator picks which).</li></ol>\n<ol><li><strong>MUST</strong> add invariant <code>WatchedFolderHEADConsistent</code> (severity <code>error</code>): for every watched folder, the folder's <code>HEAD</code> (8-byte LE u64 seq counter per AGENTS.md §2) MUST be <code>&lt;= manifest.root_head</code>. A folder cannot have advanced past the root's published chain tip (would imply time-travel writes). Failure → <code>FROZEN_HUMAN</code> (catastrophic divergence per AGENTS.md §12); repair requires explicit <code>cyberos doctor --repair --reason \"&lt;text&gt;\"</code>.</li></ol>\n<ol><li><strong>MUST</strong> add invariant <code>WatchedFolderManifestSchema</code> (severity <code>warning</code>): for every watched folder, the per-folder <code>manifest.json</code> fragment MUST validate against <code>memory.schema.json#/definitions/WatchedFolderManifest</code>. Unknown keys → warning only (forward-compat); missing required keys → error escalation to <code>WatchedFolderManifestPresent</code> failure.</li></ol>\n<ol><li><strong>MUST</strong> run the five new invariants AFTER the core invariants (per AGENTS.md §7.2 walk order) and BEFORE consolidation triggers (per §7.6). The execution order is locked in <code>doctor/mod.rs</code>'s <code>INVARIANT_ORDER</code> slice; tests assert it.</li></ol>\n<ol><li><strong>MUST</strong> emit per-invariant output to stdout (human) AND <code>cyberos doctor --json</code> (machine):</li></ol>\n<ul><li>Human format: <code>[&lt;severity&gt;] &lt;invariant-id&gt;: &lt;status&gt; — &lt;details&gt;</code> (one line each, terminal-aware colours).</li><li>JSON format: <code>{ \"id\": \"WatchedFolderManifestPresent\", \"severity\": \"error\", \"status\": \"pass\" | \"fail\" | \"skip\", \"details\": \"...\", \"affected_paths\": [...] }</code>.</li><li>Skipping is permitted IFF the dependency invariant has already failed (e.g. <code>WatchedFolderManifestSchema</code> skips when <code>WatchedFolderManifestPresent</code> failed for the same folder); <code>status: \"skip\"</code> carries <code>skipped_because: \"&lt;dependency-invariant-id&gt;\"</code>.</li></ul>\n<ol><li><strong>MUST</strong> be deterministic — running doctor twice on the same on-disk state produces byte-identical JSON output. Folder iteration is sorted by realpath; no <code>Date.now()</code>, no map-iteration-order assumptions.</li></ol>\n<ol><li><strong>MUST</strong> complete in ≤ 5 seconds for ≤ 50 watched folders on commodity hardware (M-series Mac or equivalent x86_64). Latency budget per invariant: ≤ 100ms for <code>WatchedFolderManifestPresent</code>/<code>Resolvable</code>/<code>NotShadowed</code>/<code>HEADConsistent</code>; ≤ 500ms for <code>WatchedFolderManifestSchema</code> (jsonschema validation is heavier). Asserted by <code>doctor_watched_folders_test.rs::latency_test</code>.</li></ol>\n<ol><li><strong>MUST</strong> integrate into <code>cyberos doctor</code> exit code: any error-severity failure → exit 2 (<code>InvariantFailure</code>); any warning-only failures → exit 0 with warnings printed; no failures → exit 0. Exit codes per the shared <code>cyberos-cli-exit::ExitCode</code> (slot reserved at <code>200</code> range for memory-specific codes per TASK-AI-021 §3 note; <code>200 = InvariantFailure</code>).</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>memory.doctor.invariant_check</code> per invariant call with attributes <code>invariant_id</code>, <code>severity</code>, <code>status</code>, <code>duration_ms</code>, <code>affected_path_count</code> (when applicable). Aggregated by TASK-OBS-005 for trend dashboards.</li></ol>\n<ol><li><strong>SHOULD</strong> support <code>--invariant &lt;id&gt;</code> filter to run a single invariant (operator debugging) and <code>--only watched-folders</code> filter to scope to this category (CI optimisation).</li></ol>\n<ol><li><strong>SHOULD</strong> emit metric <code>memory_doctor_invariants_failed_total{invariant_id, severity}</code> (counter) and <code>memory_doctor_invariant_duration_seconds{invariant_id}</code> (histogram with TASK-OBS-003 standardised buckets).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why declare invariants in YAML, not Rust?</strong> Per AGENTS.md §7 the consolidation walk reads <code>memory.invariants.yaml</code>. Keeping invariants as data (not code) means external tools (audit pipelines, third-party doctor wrappers) can read the catalogue without compiling Rust. The Rust impl is the <em>executor</em>; the YAML is the <em>spec</em>. Adding an invariant becomes a YAML edit + a Rust impl PR — discoverable + reviewable.</p>\n<p><strong>Why 5 specific invariants?</strong> Each catches a distinct watched-folder failure mode:</p>\n<ul><li><code>ManifestPresent</code> — operator forgot to commit/sync the per-folder manifest fragment.</li><li><code>Resolvable</code> — volume unmounted, symlink target moved, ENOSPC on the target FS.</li><li><code>NotShadowed</code> — operator ran <code>memory watch</code> twice with different relative paths that resolve to the same realpath.</li><li><code>HEADConsistent</code> — clock skew, manual file corruption, or a (bug-induced) write that bypassed the canonical writer.</li><li><code>ManifestSchema</code> — version drift between writer and reader; forward-compat probe.</li></ul>\n<p>Each failure mode has a distinct remediation, so they earn distinct invariants. Coalescing them into one \"folder OK\" invariant would hide which class of failure the operator is dealing with.</p>\n<p><strong>Why <code>FROZEN_HUMAN</code> for HEAD inconsistency (§1 #4)?</strong> A folder's HEAD exceeding root HEAD implies the folder was written-to OUTSIDE the canonical writer (per AGENTS.md §14.1 only the writer mutates HEAD). This is either a malicious actor or a catastrophic bug. The operator must explicitly intervene via <code>cyberos doctor --repair --reason \"&lt;text&gt;\"</code> so the action is auditable; auto-repair would erase the evidence.</p>\n<p><strong>Why ordering matters (§1 #6)?</strong> Core invariants (e.g. <code>LedgerChainIntact</code>) MUST pass first — if the audit chain itself is broken, watched-folder integrity is moot. Conversely, watched-folder invariants MUST run BEFORE consolidation triggers because consolidation walks all memories including those in watched folders; if a folder is unreachable, the walk would fail mid-way and corrupt the consolidation state.</p>\n<p><strong>Why deterministic JSON output (§1 #8)?</strong> CI gates downstream of doctor (e.g. TASK-MEMORY-110's health check, TASK-OBS-007's auto-triage) diff doctor's output across runs. Non-deterministic output → false-positive diffs → operators ignore real failures. Sort-by-realpath is the canonical iteration order.</p>\n<p><strong>Why 5-second latency budget (§1 #9)?</strong> Doctor runs as part of TASK-MEMORY-110's daemon health check at startup AND on a 60-second interval. 5 seconds is the upper bound that lets the daemon remain responsive; missing the budget triggers a sev-2 alarm. For ≤ 50 folders the budget is generous; the threshold exists to catch pathological cases (e.g. 10,000 dangling symlinks would blow the budget).</p>\n<p><strong>Why <code>--invariant &lt;id&gt;</code> filter (§1 #12)?</strong> Operator debugging workflow: \"doctor is failing on <code>WatchedFolderManifestSchema</code> — let me run just that one with <code>--json --verbose</code> to see exactly which folder fails.\" Without the filter, the operator re-runs all invariants (slow + noisy). Same for CI: <code>--only watched-folders</code> lets a folder-touching PR's CI skip the unrelated core-invariant suite (fast).</p>\n<p><strong>Why per-invariant OTel span (§1 #11)?</strong> Aggregated dashboards answer \"which invariant is the slowest 95p?\" and \"which invariant failed how often this week?\" Without per-invariant attribution, the only signal is \"doctor took N seconds today\" — not actionable.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Invariant YAML</h3>\n<pre><code class=\"language-yaml\"># services/memory/memory.invariants.yaml.d/watched-folders-v2.yaml\nschema_version: v2\ninvariants:\n  - id: WatchedFolderManifestPresent\n    severity: error\n    description: \"Every entry in manifest.watched_folders[] MUST have a readable manifest.json fragment at &lt;folder&gt;/manifest.json conforming to WatchedFolderManifest schema.\"\n    state_on_failure: FROZEN_RECOVERABLE\n    repair_hint: \"cyberos memory watch &lt;path&gt;   # re-bootstrap the fragment\"\n    fixture: tests/fixtures/wf_no_manifest/\n    order: 100   # after core (0..99)\n\n  - id: WatchedFolderResolvable\n    severity: error\n    description: \"Every watched folder path MUST resolve through symlinks to an extant inode (stat() != ENOENT).\"\n    state_on_failure: FROZEN_RECOVERABLE\n    repair_hint: \"remount the volume OR cyberos memory unwatch &lt;path&gt;\"\n    fixture: tests/fixtures/wf_dangling_symlink/\n    order: 101\n\n  - id: WatchedFolderNotShadowed\n    severity: error\n    description: \"No two entries in manifest.watched_folders[] MAY resolve to the same realpath.\"\n    state_on_failure: FROZEN_RECOVERABLE\n    repair_hint: \"cyberos memory unwatch &lt;one of the duplicates&gt;\"\n    fixture: tests/fixtures/wf_two_same_realpath/\n    order: 102\n\n  - id: WatchedFolderHEADConsistent\n    severity: error\n    description: \"Every watched folder's HEAD MUST be &lt;= manifest.root_head (no time-travel).\"\n    state_on_failure: FROZEN_HUMAN\n    repair_hint: \"cyberos doctor --repair --reason \\\"investigated: &lt;root cause&gt;\\\"\"\n    fixture: tests/fixtures/wf_head_ahead_of_root/\n    order: 103\n\n  - id: WatchedFolderManifestSchema\n    severity: warning\n    description: \"Every watched folder's manifest fragment MUST validate against WatchedFolderManifest schema; unknown keys are warned, missing required keys escalate to WatchedFolderManifestPresent.\"\n    state_on_failure: READY   # warning, not error\n    repair_hint: \"Verify cyberos version matches manifest fragment schema_version; upgrade or downgrade as appropriate.\"\n    fixture: tests/fixtures/wf_manifest_schema_drift/\n    order: 104</code></pre>\n<h3>Invariant Rust trait</h3>\n<pre><code class=\"language-rust\">// services/memory/src/doctor/invariants_v2.rs\nuse crate::doctor::{Severity, State, InvariantResult};\nuse crate::manifest::Manifest;\nuse std::path::{Path, PathBuf};\nuse std::time::Instant;\n\npub trait Invariant {\n    fn id(&amp;self) -&gt; &amp;'static str;\n    fn severity(&amp;self) -&gt; Severity;\n    fn check(&amp;self, manifest: &amp;Manifest, root: &amp;Path) -&gt; InvariantResult;\n}\n\npub struct WatchedFolderManifestPresent;\npub struct WatchedFolderResolvable;\npub struct WatchedFolderNotShadowed;\npub struct WatchedFolderHEADConsistent;\npub struct WatchedFolderManifestSchema;\n\nimpl Invariant for WatchedFolderManifestPresent {\n    fn id(&amp;self) -&gt; &amp;'static str { \"WatchedFolderManifestPresent\" }\n    fn severity(&amp;self) -&gt; Severity { Severity::Error }\n    fn check(&amp;self, manifest: &amp;Manifest, _root: &amp;Path) -&gt; InvariantResult {\n        let start = Instant::now();\n        let mut affected = Vec::new();\n        // Sort by realpath for deterministic output (§1 #8)\n        let mut folders: Vec&lt;_&gt; = manifest.watched_folders.iter().collect();\n        folders.sort_by_key(|wf| wf.realpath.clone());\n\n        for wf in folders {\n            let frag = wf.realpath.join(\"manifest.json\");\n            match std::fs::read(&amp;frag) {\n                Ok(bytes) =&gt; {\n                    if serde_json::from_slice::&lt;crate::manifest::WatchedFolderManifest&gt;(&amp;bytes).is_err() {\n                        affected.push(wf.realpath.clone());\n                    }\n                }\n                Err(_) =&gt; affected.push(wf.realpath.clone()),\n            }\n        }\n\n        if affected.is_empty() {\n            InvariantResult::pass(self.id(), start.elapsed())\n        } else {\n            InvariantResult::fail(self.id(), Severity::Error, start.elapsed())\n                .with_details(format!(\"{} watched folder(s) missing or unreadable manifest.json\", affected.len()))\n                .with_affected_paths(affected)\n                .with_state_transition(State::FrozenRecoverable)\n        }\n    }\n}\n\n// WatchedFolderResolvable: stat() the realpath via std::fs::metadata\n// (which follows symlinks). ENOENT → fail.\nimpl Invariant for WatchedFolderResolvable {\n    fn id(&amp;self) -&gt; &amp;'static str { \"WatchedFolderResolvable\" }\n    fn severity(&amp;self) -&gt; Severity { Severity::Error }\n    fn check(&amp;self, manifest: &amp;Manifest, _root: &amp;Path) -&gt; InvariantResult {\n        let start = Instant::now();\n        let mut affected = Vec::new();\n        let mut folders: Vec&lt;_&gt; = manifest.watched_folders.iter().collect();\n        folders.sort_by_key(|wf| wf.realpath.clone());\n\n        for wf in folders {\n            // canonicalize() resolves all symlinks per AGENTS.md §0.4\n            match std::fs::canonicalize(&amp;wf.path) {\n                Ok(real) =&gt; {\n                    if std::fs::metadata(&amp;real).is_err() {\n                        affected.push(wf.path.clone());\n                    }\n                }\n                Err(_) =&gt; affected.push(wf.path.clone()),\n            }\n        }\n        if affected.is_empty() {\n            InvariantResult::pass(self.id(), start.elapsed())\n        } else {\n            InvariantResult::fail(self.id(), Severity::Error, start.elapsed())\n                .with_affected_paths(affected)\n                .with_state_transition(State::FrozenRecoverable)\n        }\n    }\n}\n\n// WatchedFolderNotShadowed: build realpath → [orig_paths] map; any\n// realpath with &gt; 1 orig path is a shadow.\nimpl Invariant for WatchedFolderNotShadowed {\n    fn id(&amp;self) -&gt; &amp;'static str { \"WatchedFolderNotShadowed\" }\n    fn severity(&amp;self) -&gt; Severity { Severity::Error }\n    fn check(&amp;self, manifest: &amp;Manifest, _root: &amp;Path) -&gt; InvariantResult {\n        let start = Instant::now();\n        let mut by_realpath: std::collections::BTreeMap&lt;PathBuf, Vec&lt;PathBuf&gt;&gt; = Default::default();\n        for wf in &amp;manifest.watched_folders {\n            if let Ok(real) = std::fs::canonicalize(&amp;wf.path) {\n                by_realpath.entry(real).or_default().push(wf.path.clone());\n            }\n        }\n        let shadowed: Vec&lt;PathBuf&gt; = by_realpath\n            .into_iter()\n            .filter(|(_, v)| v.len() &gt; 1)\n            .flat_map(|(_, v)| v)\n            .collect();\n\n        if shadowed.is_empty() {\n            InvariantResult::pass(self.id(), start.elapsed())\n        } else {\n            InvariantResult::fail(self.id(), Severity::Error, start.elapsed())\n                .with_details(format!(\"{} path(s) resolve to a duplicated realpath\", shadowed.len()))\n                .with_affected_paths(shadowed)\n                .with_state_transition(State::FrozenRecoverable)\n        }\n    }\n}\n\n// WatchedFolderHEADConsistent: read folder/HEAD (LE u64) and compare\n// with manifest.root_head.\nimpl Invariant for WatchedFolderHEADConsistent {\n    fn id(&amp;self) -&gt; &amp;'static str { \"WatchedFolderHEADConsistent\" }\n    fn severity(&amp;self) -&gt; Severity { Severity::Error }\n    fn check(&amp;self, manifest: &amp;Manifest, _root: &amp;Path) -&gt; InvariantResult {\n        let start = Instant::now();\n        let mut affected = Vec::new();\n        let mut folders: Vec&lt;_&gt; = manifest.watched_folders.iter().collect();\n        folders.sort_by_key(|wf| wf.realpath.clone());\n\n        for wf in folders {\n            let head_path = wf.realpath.join(\"HEAD\");\n            if let Ok(bytes) = std::fs::read(&amp;head_path) {\n                if bytes.len() == 8 {\n                    let folder_head = u64::from_le_bytes(bytes.try_into().unwrap());\n                    if folder_head &gt; manifest.audit_chain_head {\n                        affected.push(wf.realpath.clone());\n                    }\n                }\n            }\n        }\n        if affected.is_empty() {\n            InvariantResult::pass(self.id(), start.elapsed())\n        } else {\n            InvariantResult::fail(self.id(), Severity::Error, start.elapsed())\n                .with_details(\"folder HEAD exceeds root chain tip — implies write outside canonical writer\".into())\n                .with_affected_paths(affected)\n                .with_state_transition(State::FrozenHuman)\n        }\n    }\n}</code></pre>\n<h3>Doctor integration</h3>\n<pre><code class=\"language-rust\">// services/memory/src/doctor/mod.rs (excerpt)\nconst INVARIANT_ORDER: &amp;[&amp;dyn Invariant] = &amp;[\n    // Core (0..99) — registered in core_invariants.rs\n    &amp;core::LedgerChainIntact,\n    &amp;core::ManifestParseable,\n    &amp;core::HEADExists,\n    // ... other core invariants ...\n\n    // Watched-folders (100..199) — registered here per TASK-MEMORY-105\n    &amp;invariants_v2::WatchedFolderManifestPresent,\n    &amp;invariants_v2::WatchedFolderResolvable,\n    &amp;invariants_v2::WatchedFolderNotShadowed,\n    &amp;invariants_v2::WatchedFolderHEADConsistent,\n    &amp;invariants_v2::WatchedFolderManifestSchema,\n];\n\npub fn run_all(manifest: &amp;Manifest, root: &amp;Path) -&gt; Vec&lt;InvariantResult&gt; {\n    INVARIANT_ORDER.iter().map(|inv| {\n        // Dependency-skip: WatchedFolderManifestSchema skips when\n        // WatchedFolderManifestPresent failed for the SAME folder set.\n        // (Generalised dependency table lives in InvariantResult::skipped_when)\n        inv.check(manifest, root)\n    }).collect()\n}</code></pre>\n<h3>CLI JSON output schema</h3>\n<pre><code class=\"language-json\">{\n  \"schema_version\": \"v1\",\n  \"command\": \"cyberos doctor\",\n  \"exit_code\": 2,\n  \"agent_state\": \"FROZEN_RECOVERABLE\",\n  \"invariants\": [\n    {\n      \"id\": \"LedgerChainIntact\",\n      \"severity\": \"error\",\n      \"status\": \"pass\",\n      \"duration_ms\": 42\n    },\n    {\n      \"id\": \"WatchedFolderManifestPresent\",\n      \"severity\": \"error\",\n      \"status\": \"fail\",\n      \"duration_ms\": 11,\n      \"details\": \"1 watched folder(s) missing or unreadable manifest.json\",\n      \"affected_paths\": [\"/Users/stephencheng/Documents/work-notes\"],\n      \"state_transition\": \"FROZEN_RECOVERABLE\",\n      \"repair_hint\": \"cyberos memory watch /Users/stephencheng/Documents/work-notes\"\n    },\n    {\n      \"id\": \"WatchedFolderManifestSchema\",\n      \"severity\": \"warning\",\n      \"status\": \"skip\",\n      \"skipped_because\": \"WatchedFolderManifestPresent\"\n    }\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>All 5 invariants registered</strong> — <code>cyberos doctor --list-invariants</code> includes <code>WatchedFolder{ManifestPresent,Resolvable,NotShadowed,HEADConsistent,ManifestSchema}</code> in the documented order.</li><li><strong>YAML matches Rust</strong> — <code>tests::yaml_matches_rust</code> walks <code>memory.invariants.yaml.d/watched-folders-v2.yaml</code> and asserts each declared invariant has a corresponding <code>impl Invariant</code> in <code>invariants_v2.rs</code>.</li><li><strong>Happy path: 0 failures</strong> — fixture <code>tests/fixtures/wf_happy/</code> with 3 valid watched folders; all 5 invariants pass; exit code 0.</li><li><strong>ManifestPresent fails on missing fragment</strong> — fixture deletes one folder's <code>manifest.json</code>; <code>WatchedFolderManifestPresent</code> fails; exit 2; affected_paths lists the folder.</li><li><strong>Resolvable fails on dangling symlink</strong> — fixture creates <code>wf/symlink → /nonexistent</code>; <code>WatchedFolderResolvable</code> fails; exit 2.</li><li><strong>NotShadowed fails on two registrations of same realpath</strong> — fixture registers <code>/Users/x/notes</code> and <code>/Users/x/notes/</code> and <code>/Users/x/./notes</code> (all same realpath); <code>WatchedFolderNotShadowed</code> fails with 3 affected paths.</li><li><strong>HEADConsistent fails on HEAD &gt; root</strong> — fixture manually writes a folder HEAD = root_head + 5; <code>WatchedFolderHEADConsistent</code> fails with state transition <code>FROZEN_HUMAN</code>; exit 2.</li><li><strong>ManifestSchema warns on unknown key</strong> — fixture adds <code>{\"future_key\": 42}</code> to a folder manifest; invariant reports <code>severity: warning</code>; exit code remains 0; warning printed.</li><li><strong>ManifestSchema escalates on missing required key</strong> — fixture removes <code>schema_version</code> from a folder manifest; <code>WatchedFolderManifestPresent</code> fails (not the schema-only warning); exit 2.</li><li><strong>Dependency skip works</strong> — when <code>ManifestPresent</code> fails for a folder, <code>ManifestSchema</code> reports <code>status: \"skip\"</code> with <code>skipped_because: \"WatchedFolderManifestPresent\"</code> for that folder only.</li><li><strong>Determinism: byte-identical JSON</strong> — <code>cyberos doctor --json</code> run twice on the same fixture produces byte-identical output (sorted paths, no clock-dependent fields).</li><li><strong>Latency budget</strong> — 50-folder fixture completes <code>doctor --only watched-folders</code> in ≤ 5 seconds wall-clock (<code>doctor_watched_folders_test::latency_test</code>).</li><li><strong>Filter: <code>--invariant &lt;id&gt;</code></strong> — <code>cyberos doctor --invariant WatchedFolderNotShadowed</code> runs only that invariant; other invariants absent from output.</li><li><strong>Filter: <code>--only watched-folders</code></strong> — runs all 5 watched-folder invariants; core invariants skipped.</li><li><strong>OTel span per invariant</strong> — exporter receives 5 spans named <code>memory.doctor.invariant_check</code> with attribute <code>invariant_id</code>; assert via in-memory exporter.</li><li><strong>Metric increments on failure</strong> — failure path increments <code>memory_doctor_invariants_failed_total{invariant_id, severity}</code>; assert via in-process Prometheus registry.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/memory/tests/doctor_watched_folders_test.rs\n\n#[test]\nfn all_invariants_registered() {\n    let ids = doctor::list_invariant_ids();\n    for required in [\n        \"WatchedFolderManifestPresent\",\n        \"WatchedFolderResolvable\",\n        \"WatchedFolderNotShadowed\",\n        \"WatchedFolderHEADConsistent\",\n        \"WatchedFolderManifestSchema\",\n    ] {\n        assert!(ids.contains(&amp;required), \"missing invariant: {required}\");\n    }\n}\n\n#[test]\nfn yaml_matches_rust() {\n    let yaml = include_str!(\"../memory.invariants.yaml.d/watched-folders-v2.yaml\");\n    let parsed: WatchedFoldersV2 = serde_yaml::from_str(yaml).unwrap();\n    let rust_ids: HashSet&lt;_&gt; = doctor::list_invariant_ids().into_iter().collect();\n    for inv in parsed.invariants {\n        assert!(rust_ids.contains(inv.id.as_str()),\n                \"YAML declares invariant {} with no Rust impl\", inv.id);\n    }\n}\n\n#[test]\nfn happy_path_exit_0() {\n    let fixture = TestFixture::wf_happy();\n    let result = doctor::run_all(&amp;fixture.manifest(), fixture.root());\n    let exit = doctor::exit_code(&amp;result);\n    assert_eq!(exit, 0);\n    assert!(result.iter().all(|r| matches!(r.status, Status::Pass)));\n}\n\n#[test]\nfn manifest_present_fails_on_missing_fragment() {\n    let mut fixture = TestFixture::wf_happy();\n    fs::remove_file(fixture.watched_folder(0).join(\"manifest.json\")).unwrap();\n    let result = doctor::run_all(&amp;fixture.manifest(), fixture.root());\n    let inv = result.iter().find(|r| r.id == \"WatchedFolderManifestPresent\").unwrap();\n    assert_eq!(inv.status, Status::Fail);\n    assert_eq!(inv.affected_paths.len(), 1);\n}\n\n#[test]\nfn not_shadowed_detects_three_paths_same_realpath() {\n    let fixture = TestFixture::wf_three_same_realpath();\n    let result = doctor::run_all(&amp;fixture.manifest(), fixture.root());\n    let inv = result.iter().find(|r| r.id == \"WatchedFolderNotShadowed\").unwrap();\n    assert_eq!(inv.status, Status::Fail);\n    assert_eq!(inv.affected_paths.len(), 3);\n}\n\n#[test]\nfn head_consistent_triggers_frozen_human() {\n    let mut fixture = TestFixture::wf_happy();\n    // Manually advance folder HEAD past root HEAD (simulating bypass writer)\n    let folder = fixture.watched_folder(0);\n    let bad_head = fixture.manifest().audit_chain_head + 5;\n    fs::write(folder.join(\"HEAD\"), bad_head.to_le_bytes()).unwrap();\n\n    let result = doctor::run_all(&amp;fixture.manifest(), fixture.root());\n    let inv = result.iter().find(|r| r.id == \"WatchedFolderHEADConsistent\").unwrap();\n    assert_eq!(inv.status, Status::Fail);\n    assert_eq!(inv.state_transition, Some(State::FrozenHuman));\n}\n\n#[test]\nfn manifest_schema_warning_keeps_exit_0() {\n    let mut fixture = TestFixture::wf_happy();\n    // Add unknown key (warning only)\n    let frag_path = fixture.watched_folder(0).join(\"manifest.json\");\n    let mut frag: serde_json::Value = serde_json::from_slice(&amp;fs::read(&amp;frag_path).unwrap()).unwrap();\n    frag[\"future_key\"] = serde_json::json!(42);\n    fs::write(&amp;frag_path, serde_json::to_vec_pretty(&amp;frag).unwrap()).unwrap();\n\n    let result = doctor::run_all(&amp;fixture.manifest(), fixture.root());\n    let exit = doctor::exit_code(&amp;result);\n    assert_eq!(exit, 0);  // warning-only does not change exit\n    let inv = result.iter().find(|r| r.id == \"WatchedFolderManifestSchema\").unwrap();\n    assert_eq!(inv.severity, Severity::Warning);\n}\n\n#[test]\nfn determinism_byte_identical_json() {\n    let fixture = TestFixture::wf_happy();\n    let json_1 = doctor::run_all_json(&amp;fixture.manifest(), fixture.root());\n    let json_2 = doctor::run_all_json(&amp;fixture.manifest(), fixture.root());\n    assert_eq!(json_1, json_2);\n}\n\n#[test]\nfn latency_test_50_folders_under_5_seconds() {\n    let fixture = TestFixture::wf_50_folders();\n    let start = Instant::now();\n    let _ = doctor::run_all(&amp;fixture.manifest(), fixture.root());\n    assert!(start.elapsed() &lt; Duration::from_secs(5));\n}</code></pre>\n<pre><code class=\"language-bash\"># CI integration (snippet from doctor.yml workflow)\n- name: doctor (watched-folders only)\n  run: |\n    cyberos doctor --only watched-folders --json &gt; doctor-report.json\n    # Fail CI if any error-severity invariant failed\n    jq -e '.invariants | map(select(.severity == \"error\" and .status == \"fail\")) | length == 0' doctor-report.json</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<pre><code class=\"language-rust\">// services/memory/src/doctor/mod.rs  (orchestrator excerpt)\n\nuse std::path::Path;\nuse crate::manifest::Manifest;\n\npub mod core;\npub mod invariants_v2;\npub mod cli;\n\n#[derive(Clone, Copy, Debug, PartialEq, Eq, serde::Serialize)]\n#[serde(rename_all = \"lowercase\")]\npub enum Severity { Error, Warning, Info }\n\n#[derive(Clone, Copy, Debug, PartialEq, Eq, serde::Serialize)]\n#[serde(rename_all = \"lowercase\")]\npub enum Status { Pass, Fail, Skip }\n\n#[derive(Clone, Copy, Debug, PartialEq, Eq, serde::Serialize)]\n#[serde(rename_all = \"SCREAMING_SNAKE_CASE\")]\npub enum State { Ready, FrozenRecoverable, FrozenHuman }\n\n#[derive(Clone, Debug, serde::Serialize)]\npub struct InvariantResult {\n    pub id: &amp;'static str,\n    pub severity: Severity,\n    pub status: Status,\n    pub duration_ms: u128,\n    #[serde(skip_serializing_if = \"Option::is_none\")] pub details: Option&lt;String&gt;,\n    #[serde(skip_serializing_if = \"Vec::is_empty\")]   pub affected_paths: Vec&lt;std::path::PathBuf&gt;,\n    #[serde(skip_serializing_if = \"Option::is_none\")] pub state_transition: Option&lt;State&gt;,\n    #[serde(skip_serializing_if = \"Option::is_none\")] pub skipped_because: Option&lt;String&gt;,\n    #[serde(skip_serializing_if = \"Option::is_none\")] pub repair_hint: Option&lt;String&gt;,\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory/src/doctor/cli.rs\n\nuse clap::Parser;\n\n#[derive(Parser)]\npub struct DoctorArgs {\n    /// Run a single invariant by id\n    #[arg(long)] pub invariant: Option&lt;String&gt;,\n    /// Run only invariants in a category (core | watched-folders)\n    #[arg(long)] pub only: Option&lt;String&gt;,\n    /// Output machine-readable JSON\n    #[arg(long)] pub json: bool,\n    /// List all registered invariants and exit 0\n    #[arg(long)] pub list_invariants: bool,\n}\n\npub fn main(args: DoctorArgs, manifest: &amp;Manifest, root: &amp;Path) -&gt; i32 {\n    if args.list_invariants {\n        for id in list_invariant_ids() { println!(\"{id}\"); }\n        return 0;\n    }\n    let results = run_filtered(args.invariant.as_deref(), args.only.as_deref(), manifest, root);\n    if args.json {\n        println!(\"{}\", serde_json::to_string_pretty(&amp;JsonReport::new(&amp;results)).unwrap());\n    } else {\n        for r in &amp;results { print_human(r); }\n    }\n    exit_code(&amp;results)\n}\n\npub fn exit_code(results: &amp;[InvariantResult]) -&gt; i32 {\n    if results.iter().any(|r| r.severity == Severity::Error &amp;&amp; r.status == Status::Fail) {\n        200  // shared cyberos-cli-exit::ExitCode (memory-range InvariantFailure)\n    } else { 0 }\n}</code></pre>\n<pre><code class=\"language-yaml\"># services/memory/memory.invariants.yaml (master file, append include)\ninclude:\n  - memory.invariants.yaml.d/core.yaml\n  - memory.invariants.yaml.d/watched-folders-v2.yaml</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-MEMORY-101 (upstream)</strong> — defines <code>manifest.watched_folders[]</code> schema, the data this task audits.</li><li><strong>TASK-MEMORY-102 (related)</strong> — <code>cyberos memory watch/unwatch</code> is the repair path for <code>WatchedFolderManifestPresent</code> failures.</li><li><strong>TASK-MEMORY-103 (related)</strong> — multi-device sync writes folder HEADs; if it bypasses the canonical writer, this task catches it.</li><li><strong>TASK-MEMORY-107 (downstream)</strong> — FS watcher's startup uses <code>doctor --only watched-folders</code> as a gate; refuses to start if any error-severity invariant fails.</li><li><strong>TASK-MEMORY-110 (downstream)</strong> — health-check daemon runs doctor on a 60-second interval and reports failures via OTel.</li><li><strong>TASK-OBS-003, TASK-OBS-005, TASK-OBS-007 (cross-module)</strong> — metrics + spans flow into observability pillar.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Happy doctor run (JSON)</h3>\n<pre><code class=\"language-json\">{\n  \"schema_version\": \"v1\",\n  \"command\": \"cyberos doctor --only watched-folders --json\",\n  \"exit_code\": 0,\n  \"agent_state\": \"READY\",\n  \"invariants\": [\n    {\"id\": \"WatchedFolderManifestPresent\", \"severity\": \"error\",   \"status\": \"pass\", \"duration_ms\": 12},\n    {\"id\": \"WatchedFolderResolvable\",      \"severity\": \"error\",   \"status\": \"pass\", \"duration_ms\":  8},\n    {\"id\": \"WatchedFolderNotShadowed\",     \"severity\": \"error\",   \"status\": \"pass\", \"duration_ms\":  6},\n    {\"id\": \"WatchedFolderHEADConsistent\",  \"severity\": \"error\",   \"status\": \"pass\", \"duration_ms\":  9},\n    {\"id\": \"WatchedFolderManifestSchema\",  \"severity\": \"warning\", \"status\": \"pass\", \"duration_ms\": 41}\n  ]\n}</code></pre>\n<h3>Failure cascade (human)</h3>\n<pre><code class=\"language-text\">[error] WatchedFolderManifestPresent: FAIL — 1 watched folder(s) missing or unreadable manifest.json\n        affected: /Users/stephencheng/Documents/work-notes\n        repair:   cyberos memory watch /Users/stephencheng/Documents/work-notes\n        state:    FROZEN_RECOVERABLE\n\n[error] WatchedFolderResolvable:      PASS  (8ms)\n[error] WatchedFolderNotShadowed:     PASS  (6ms)\n[error] WatchedFolderHEADConsistent:  PASS  (9ms)\n[warn]  WatchedFolderManifestSchema:  SKIP — skipped because WatchedFolderManifestPresent failed for the same folder\n\nAgent state: FROZEN_RECOVERABLE\nExit code:   200</code></pre>\n<h3><code>--invariant &lt;id&gt;</code> filter</h3>\n<pre><code class=\"language-bash\">$ cyberos doctor --invariant WatchedFolderNotShadowed --json\n{\n  \"schema_version\": \"v1\",\n  \"command\": \"cyberos doctor --invariant WatchedFolderNotShadowed --json\",\n  \"exit_code\": 0,\n  \"agent_state\": \"READY\",\n  \"invariants\": [\n    {\"id\": \"WatchedFolderNotShadowed\", \"severity\": \"error\", \"status\": \"pass\", \"duration_ms\": 6}\n  ]\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Invariant priorities + ordering knobs (operator might want to skip certain invariants in dev) — slice 3+.</li><li>Self-healing: should <code>WatchedFolderResolvable</code> auto-unwatch a 7-day-dangling folder? — slice 3+; needs UX design.</li><li><code>--repair</code> mode that re-bootstraps <code>manifest.json</code> from current folder state — slice 3+; conflicts with audit-before-action principle.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Watched folder unmounted</td><td><code>WatchedFolderResolvable</code> fails</td><td>exit 200, FROZEN_RECOVERABLE</td><td>Operator remounts volume + reruns doctor</td></tr><tr><td>Dangling symlink (target deleted)</td><td><code>WatchedFolderResolvable</code> fails</td><td>exit 200, FROZEN_RECOVERABLE</td><td>Operator <code>cyberos memory unwatch &lt;path&gt;</code></td></tr><tr><td>Two registrations to same realpath</td><td><code>WatchedFolderNotShadowed</code> fails</td><td>exit 200, FROZEN_RECOVERABLE</td><td>Operator <code>unwatch</code> one duplicate</td></tr><tr><td>Folder manifest.json deleted</td><td><code>WatchedFolderManifestPresent</code> fails</td><td>exit 200, FROZEN_RECOVERABLE</td><td>Operator <code>cyberos memory watch &lt;path&gt;</code> re-bootstraps</td></tr><tr><td>Folder manifest.json corrupt JSON</td><td><code>WatchedFolderManifestPresent</code> fails</td><td>exit 200, FROZEN_RECOVERABLE</td><td>Operator restores from backup OR re-watches</td></tr><tr><td>Folder manifest.json missing required key</td><td><code>WatchedFolderManifestPresent</code> fails</td><td>exit 200, FROZEN_RECOVERABLE</td><td>Operator upgrades / re-watches</td></tr><tr><td>Folder manifest.json has unknown key (forward-compat)</td><td><code>WatchedFolderManifestSchema</code> warns</td><td>exit 0, READY</td><td>Informational; consider upgrading reader</td></tr><tr><td>Folder HEAD &gt; root HEAD</td><td><code>WatchedFolderHEADConsistent</code> fails</td><td>exit 200, FROZEN_HUMAN</td><td>Manual <code>cyberos doctor --repair --reason \"&lt;text&gt;\"</code></td></tr><tr><td>Folder HEAD file size != 8 bytes</td><td><code>WatchedFolderHEADConsistent</code> skips that folder</td><td>exit 0 if no other failures</td><td>Operator investigates; may need <code>cyberos memory watch</code></td></tr><tr><td>Doctor itself crashes mid-run</td><td>panic handler emits sev-1 + exit 1</td><td>exit 1 (not 200)</td><td>Operator files bug; runs with <code>--verbose</code> for stack</td></tr><tr><td>Invariant exceeds latency budget</td><td>metric <code>memory_doctor_invariant_duration_seconds</code> p95 alarm</td><td>sev-2 alarm via TASK-OBS-007</td><td>Operator investigates folder count + filesystem health</td></tr><tr><td>50+ folders, slow filesystem</td><td><code>latency_test</code> may exceed 5s on CI</td><td>sev-2 alarm</td><td>Operator considers folder-count cap or fast-path</td></tr><tr><td>YAML invariant declared without Rust impl</td><td><code>yaml_matches_rust</code> test fails in CI</td><td>PR blocked</td><td>Author adds the impl OR removes from YAML</td></tr><tr><td>Rust impl present without YAML declaration</td><td>warning at <code>cyberos doctor</code> startup</td><td>exit 0; warning in logs</td><td>Author adds YAML entry</td></tr><tr><td>Manifest.watched_folders[] empty</td><td>All 5 invariants pass trivially</td><td>exit 0, READY</td><td>By design — no folders to audit</td></tr><tr><td>State transition collision (two invariants ask for different states)</td><td>doctor picks the strictest (HUMAN &gt; RECOVERABLE &gt; READY)</td><td>reported in JSON</td><td>By design</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The invariant catalog file (<code>watched-folders-v2.yaml</code>) MUST be loaded at startup; failing to find it is a hard error (the master <code>memory.invariants.yaml</code> references it via <code>include:</code>). This is symmetric with how <code>core.yaml</code> is loaded — both are first-class.</li><li><code>std::fs::canonicalize</code> is the chosen symlink-resolver; it returns the absolute realpath and follows every symlink. Per AGENTS.md §0.4 this is correct; do not roll a custom resolver.</li><li><code>WatchedFolderHEADConsistent</code> reads HEAD as 8 little-endian bytes per AGENTS.md §2. If the file is missing or has wrong size, the invariant logs a warning and treats the folder as \"unknown HEAD\" — does NOT fail (the missing-HEAD case is caught by <code>WatchedFolderManifestPresent</code> which requires the full fragment including HEAD).</li><li>The <code>Skip</code> status with <code>skipped_because</code> is a first-class result variant — not a missing entry. JSON consumers iterate the array; they should not assume \"missing = skip\".</li><li>The <code>state_transition</code> field is the AGENT state the failure WOULD trigger; the doctor itself does NOT mutate agent state. The <code>cyberos</code> runtime reads doctor's JSON output and applies the strictest transition. This preserves the invariant that doctor is read-only.</li><li>The <code>--invariant</code> and <code>--only</code> filters are CLI-only conveniences; programmatic callers use <code>doctor::run_filtered(invariant: Option&lt;&amp;str&gt;, only: Option&lt;&amp;str&gt;)</code>.</li><li>The CI integration uses <code>jq -e</code> to fail on any error-severity failure; warnings do not block CI but appear in the job log for operator review.</li><li>Latency budgets are per-invariant per-folder; the wall-clock test bounds total run time. Tests use a fixture generator that creates N folders deterministically (no Date.now()) so the test is reproducible.</li><li>The <code>tests/fixtures/wf_*</code> directories are committed to the repo (small JSON fragments + symlinks); they're the canonical demonstrations of each failure mode.</li></ul>\n<hr>\n<p><em>End of TASK-MEMORY-105.</em></p>";
