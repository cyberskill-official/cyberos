window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-OBS-001"]="<h1>TASK-OBS-001: OTel collector scaffold + LGTM compose</h1>\n<h2>Summary</h2>\n<p>Slice-1 ships the <code>cyberos-obs-collector</code> crate (<code>services/obs-collector/</code>): canonical <code>otel-collector-config.yaml</code>, bearer-token file shape, YAML contract validation in <code>config.rs</code>, token parsing in <code>auth.rs</code>, self-metric name constants in <code>metrics.rs</code>, and the <code>cyberos-obs</code> CLI (<code>validate-config</code>, <code>validate-tokens</code>). <code>deploy/obs/</code> provides the LGTM backends plus the TASK-OBS-002 <code>obs-proxy</code> in <code>docker-compose.yml</code> — <strong>not</strong> a live <code>otelcol-contrib</code> container in compose. Upstream otelcol process supervision, Helm charts, mTLS ingress, and operator rotation/healthcheck shell suites are explicitly deferred.</p>\n<h2>Problem</h2>\n<p>The original engineering-spec claimed flat <code>deploy/obs/otel-collector-config.yaml</code>, <code>loki-config.yaml</code>, <code>prometheus-config.yaml</code>, <code>rotate_tokens.sh</code>, <code>healthcheck.sh</code>, and three bash smoke suites that were never built. It used <code>## §N</code> body grammar (FM-004) and described a five-service compose with a running collector. The as-built surface is the obs-collector validation crate + nested deploy layout (<code>prometheus/</code>, <code>tempo/</code>, <code>grafana/provisioning/</code>) wired to obs-proxy — honest re-spec required before re-entry.</p>\n<h2>Proposed Solution</h2>\n<p>Adopt the as-built layout:</p>\n<ul><li><code>services/obs-collector/src/config.rs</code> — <code>validate()</code> enforces OTLP receiver, Loki + prometheusremotewrite + otlp/tempo exporters, <code>attributes/pii_scrub</code> on logs and traces pipelines, <code>bearertokenauth</code> + <code>file_storage</code> extensions</li><li><code>services/obs-collector/src/auth.rs</code> — <code>TokenFile::parse</code> for the canonical <code>service_name token</code> file format</li><li><code>services/obs-collector/config/otel-collector-config.yaml</code> — slice-1 canonical collector YAML (validated by CI/tests)</li><li><code>services/obs-collector/src/bin/cyberos_obs.rs</code> — pre-flight <code>validate-config</code> / <code>validate-tokens</code> subcommands</li><li><code>deploy/obs/docker-compose.yml</code> — Prometheus, Loki, Tempo, Grafana (datasources → obs-proxy), obs-proxy; <strong>no collector service</strong></li><li><code>deploy/obs/prometheus/prometheus.yml</code>, <code>deploy/obs/tempo/tempo.yaml</code>, <code>deploy/obs/grafana/provisioning/datasources/datasources.yaml</code></li></ul>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Resume the old engineering-spec as-is.</strong> Rejected: FM-004 blocks re-entry; paths and compose shape lie.</li><li><strong>Run otelcol-contrib inside compose in this slice.</strong> Rejected: slice-1 goal is contract validation + LGTM+proxy stack; process supervision lands next slice.</li><li>**Flat deploy/obs/*.yaml configs at repo root.** Rejected: as-built nests configs under <code>prometheus/</code>, <code>tempo/</code>, <code>grafana/provisioning/</code>, and keeps canonical collector YAML in the crate.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: invalid collector YAML (missing PII scrub) is rejected at <code>validate()</code>; canonical config and token file parse cleanly; AWH held-out <code>acceptance-obs-pii-scrub</code> stays green.</li><li>Guardrail: <code>deploy/obs/docker-compose.yml</code> starts LGTM + obs-proxy without claiming a collector container.</li></ul>\n<h2>Scope</h2>\n<p>In scope (as-built):</p>\n<ul><li><code>services/obs-collector/**</code> validation crate + canonical config + <code>tokens.example</code></li><li><code>deploy/obs/docker-compose.yml</code> LGTM backends + obs-proxy + Grafana provisioning</li><li><code>deploy/obs/prometheus/prometheus.yml</code>, <code>deploy/obs/tempo/tempo.yaml</code>, <code>deploy/obs/grafana/provisioning/datasources/datasources.yaml</code></li><li>Inline unit tests in <code>config.rs</code> and <code>auth.rs</code></li><li>AWH tasks <code>obs-collector-rust</code> and <code>acceptance-obs-pii-scrub</code></li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Phantom flat <code>deploy/obs/otel-collector-config.yaml</code>, <code>deploy/obs/loki-config.yaml</code>, or root-level prometheus/tempo YAML paths from the old spec</li><li><code>deploy/obs/scripts/rotate_tokens.sh</code>, <code>deploy/obs/scripts/healthcheck.sh</code>, and claimed <code>deploy/obs/tests/smoke_test.sh</code> / <code>auth_required_test.sh</code> / <code>buffer_survives_restart_test.sh</code> shell suites</li><li>Live <code>otelcol-contrib</code> process supervision inside compose, Helm charts, and mTLS ingress (next slice)</li><li>Running the collector as a compose service — compose is LGTM + obs-proxy only</li></ul>\n<h2>Dependencies</h2>\n<p><code>depends_on: []</code>. Soft: TASK-OBS-002 obs-proxy already consumes the LGTM backends in <code>deploy/obs/</code>; TASK-AI-022 typed attribute keys (caller-side PII prevention primary over DEC-143 collector scrub).</p>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>config::validate</code> MUST reject collector YAML missing OTLP receiver, Loki/prometheusremotewrite/otlp-tempo exporters, <code>bearertokenauth</code>, <code>file_storage</code>, or <code>pii_scrub</code> on logs and traces pipelines.</li><li>1.2 <code>auth::TokenFile::parse</code> MUST accept the canonical <code>service_name &lt;token&gt;</code> lines (comments and blank lines allowed) and reject malformed rows.</li><li>1.3 <code>cyberos-obs</code> MUST expose <code>validate-config</code> and <code>validate-tokens</code> subcommands that call <code>config::validate</code> and <code>auth::TokenFile::load</code> respectively.</li><li>1.4 The canonical collector YAML MUST live at <code>services/obs-collector/config/otel-collector-config.yaml</code>; committed token shape at <code>services/obs-collector/config/auth/tokens.example</code>.</li><li>1.5 <code>deploy/obs/docker-compose.yml</code> MUST provision Prometheus, Loki, Tempo, Grafana, and obs-proxy; it MUST NOT include a collector container in slice-1.</li><li>1.6 This adopt MUST NOT claim phantom deploy scripts, smoke shell suites, flat deploy config paths, or shipped otelcol supervision/Helm/mTLS.</li></ul>\n<h2>Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - canonical config passes validate - test: <code>services/obs-collector/src/config.rs::validate_accepts_canonical_config</code></li><li>[ ] AC 2 (traces_to: #1.1) - missing pii_scrub rejected - test: <code>services/obs-collector/src/config.rs::validate_rejects_missing_pii_scrub</code></li><li>[ ] AC 3 (traces_to: #1.2) - token file parses canonical rows - test: <code>services/obs-collector/src/auth.rs::parse_canonical</code></li><li>[ ] AC 4 (traces_to: #1.2) - extra columns rejected - test: <code>services/obs-collector/src/auth.rs::parse_rejects_extra_columns</code></li><li>[ ] AC 5 (traces_to: #1.3,#1.4) - AWH held-out PII-scrub invariant - verify: <code>cd services &amp;&amp; cargo test -p cyberos-obs-collector validate_rejects_missing_pii_scrub</code></li><li>[ ] AC 6 (traces_to: #1.5) - compose lists LGTM + obs-proxy without collector service - verify: <code>deploy/obs/docker-compose.yml</code></li><li>[ ] AC 7 (traces_to: #1.6) - Out of scope lists phantom flat configs and shell suites; new_files cite real paths only - verify: this spec Scope / new_files</li><li>[ ] AC 8 (traces_to: #1.3) - cyberos-obs binary exposes validate subcommands - verify: <code>services/obs-collector/src/bin/cyberos_obs.rs</code></li></ul>\n<h2>Verification</h2>\n<pre><code class=\"language-bash\">cd services &amp;&amp; cargo test -p cyberos-obs-collector\ncd services &amp;&amp; cargo test -p cyberos-obs-collector validate_rejects_missing_pii_scrub</code></pre>\n<table><thead><tr><th>Path</th><th>Covers</th></tr></thead><tbody><tr><td><code>src/config.rs</code> inline tests</td><td>YAML contract (canonical + missing pii_scrub)</td></tr><tr><td><code>src/auth.rs</code> inline tests</td><td>Bearer-token file parse + reject malformed lines</td></tr><tr><td><code>src/bin/cyberos_obs.rs</code></td><td>validate-config / validate-tokens CLI surface</td></tr><tr><td><code>config/otel-collector-config.yaml</code></td><td>Canonical slice-1 collector pipeline</td></tr><tr><td><code>deploy/obs/docker-compose.yml</code></td><td>LGTM + obs-proxy (no collector container)</td></tr></tbody></table>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Cursor agent (Composer) on branch <code>batch/9b-obs</code>.</li><li><strong>Scope:</strong> Re-spec/adopt against as-built obs-collector crate + deploy/obs LGTM compose; deferred otelcol supervision, Helm, mTLS, and shell smoke suites ledgered Out of scope.</li><li><strong>Human review:</strong> Required at the two HITL gates (<code>entered_via: rework</code>, <code>routed_back_count: 1</code>).</li></ul>\n<hr>\n<p><em>batch/9b-obs adopt — TASK-OBS-001 re-spec against as-built obs-collector + deploy/obs LGTM compose.</em></p>";
