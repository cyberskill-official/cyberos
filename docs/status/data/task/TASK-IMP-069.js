window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-069"]="<h1>TASK-IMP-069: Publish the payload on every release</h1>\n<h2>§1 - Description</h2>\n<p>Today the payload exists only as a gitignored local build; every consumer (Claude marketplace add, <code>.plugin</code> file pick, <code>init.sh</code>, <code>rollout.sh</code>) copies whatever a laptop last built. This task gives the payload one canonical, versioned, downloadable source: the GitHub Release for each <code>vX.Y.Z</code> tag.</p>\n<p>Normative clauses:</p>\n<ol><li>A script <code>tools/cyberos-install/release-assets.sh &lt;payload-dir&gt; &lt;out-dir&gt;</code> MUST produce, from a built payload: <code>cyberos-payload-&lt;ver&gt;.tar.gz</code> (deterministic: sorted paths, numeric owner 0:0, fixed mtime <code>2000-01-01T00:00:00Z</code>, gzip -n), <code>cyberos-&lt;ver&gt;.plugin</code> (byte-copy of <code>cyberos.plugin</code>), stable-name aliases <code>cyberos-payload.tar.gz</code> and <code>cyberos.plugin</code> (byte-identical copies, so <code>releases/latest/download/&lt;stable-name&gt;</code> always resolves), and <code>SHA256SUMS</code> covering all four. <code>&lt;ver&gt;</code> MUST be read from <code>&lt;payload-dir&gt;/VERSION</code>.</li><li><code>release-assets.sh</code> MUST exit 10 without producing output when <code>&lt;payload&gt;/VERSION</code>, root <code>VERSION</code>, and the intended tag do not all agree. The intended tag is <code>$TAG</code> when set (the release workflow sets it from <code>inputs.tag || ref_name</code>, covering workflow_dispatch where <code>GITHUB_REF_NAME</code> is the branch), else <code>$GITHUB_REF_NAME</code> when set; neither set (local run) skips the tag leg. (Amended post-ship 2026-07-12: release #35 failed on dispatch because the script read only GITHUB_REF_NAME.)</li><li><code>.github/workflows/release.yml</code> MUST gain a <code>payload</code> job that, for every <code>v*</code> tag: checks out, runs <code>build.sh</code> into a temp dir, runs <code>check-version-sync.sh</code> (TASK-IMP-068) against it, runs <code>release-assets.sh</code>, and uploads the four files + <code>SHA256SUMS</code> to that tag's GitHub Release. Default <code>GITHUB_TOKEN</code> with <code>contents: write</code> MUST suffice; no new secrets.</li><li>The <code>payload</code> job MUST fail when the pushed tag does not equal <code>v$(cat VERSION)</code> at that commit.</li><li><code>bootstrap.sh</code> MUST support fetching the payload from a URL: <code>CYBEROS_PAYLOAD_URL</code> overrides; default = the repo's <code>releases/latest/download/cyberos-payload.tar.gz</code>. It MUST download <code>SHA256SUMS</code> from the same location, verify the tarball's checksum before unpacking, and then run <code>init.sh</code> from the unpacked payload. Verification failure MUST abort before any file lands in the target repo.</li><li><code>rollout.sh</code> MUST accept, in place of a local payload dir, <code>--from-release [tag]</code>: download + verify once into a temp dir (per #5's mechanics), then proceed unchanged for every listed repo.</li><li><code>tools/cyberos-install/README.md</code> and <code>docs/deploy/RELEASE.md</code> MUST document the release-install paths for each channel: Claude Code (<code>/plugin marketplace add</code> on the unpacked payload), Claude desktop/Cowork (download <code>cyberos.plugin</code> from the release), curl bootstrap one-liner, and fleet <code>rollout.sh --from-release</code>. The \"available once you host a tarball\" placeholder MUST be replaced by the real URLs.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>GitHub Releases is the zero-infrastructure channel that matches the existing tag-driven <code>release.yml</code> and needs no hosting, keys, or registry. Deterministic tarballs mirror the memory module's export discipline and make the artifact reproducible from the tag alone. Stable-name aliases exist because GitHub's <code>latest/download/</code> URL requires a constant asset name; versioned twins keep every historical release independently fetchable. Checksums ship next to the assets so <code>bootstrap.sh</code> can verify without a signing infrastructure (cosign-grade signing stays with TASK-PLUGIN-008's marketplace, which this task deliberately does not replace).</p>\n<h2>§3 - Contract</h2>\n<pre><code>release-assets.sh &lt;payload-dir&gt; &lt;out-dir&gt;\n  exit 0   wrote 5 files into &lt;out-dir&gt; (2 versioned, 2 stable, SHA256SUMS)\n  exit 10  version disagreement (payload VERSION vs root VERSION vs $GITHUB_REF_NAME)\n  exit 2   payload missing/incomplete (no VERSION or no cyberos.plugin)\n\nbootstrap.sh                         # unchanged local behavior when a payload dir is passed\n  CYBEROS_PAYLOAD_URL=&lt;url&gt;          # tarball URL; SHA256SUMS fetched from its dirname\n  (no args, no local payload)       -&gt; latest-release default URL\n\nrollout.sh --from-release [vX.Y.Z] &lt;repo&gt; [&lt;repo&gt;...]</code></pre>\n<p>release.yml addition (shape):</p>\n<pre><code class=\"language-yaml\">  payload:\n    runs-on: ubuntu-latest\n    permissions: { contents: write }\n    steps:\n      - uses: actions/checkout@v4\n      - run: test \"v$(cat VERSION)\" = \"$GITHUB_REF_NAME\"\n      - run: bash tools/cyberos-install/build.sh \"$RUNNER_TEMP/payload\"\n      - run: bash tools/cyberos-install/check-version-sync.sh \"$RUNNER_TEMP/payload\"\n      - run: bash tools/cyberos-install/release-assets.sh \"$RUNNER_TEMP/payload\" \"$RUNNER_TEMP/assets\"\n      - run: gh release upload \"$GITHUB_REF_NAME\" \"$RUNNER_TEMP\"/assets/* --clobber\n        env: { GH_TOKEN: \"${{ github.token }}\" }</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Deterministic tarball</strong> (§1 #1) - running <code>release-assets.sh</code> twice on the same payload yields byte-identical <code>cyberos-payload-&lt;ver&gt;.tar.gz</code> (equal sha256).</li><li><strong>All five files, both name forms</strong> (§1 #1) - output contains versioned + stable tarball and plugin names plus <code>SHA256SUMS</code>; stable and versioned twins are byte-identical.</li><li><strong>Checksums verify</strong> (§1 #1) - <code>sha256sum -c SHA256SUMS</code> passes in the out-dir; corrupting one byte of the tarball makes it fail.</li><li><strong>Version triple-check</strong> (§1 #2) - payload 1.7.0 + root 1.7.0 + <code>GITHUB_REF_NAME=v1.6.0</code> exits 10 and writes nothing.</li><li><strong>Tag guard in CI</strong> (§1 #3, #4) - the <code>payload</code> job's first step compares <code>v$(cat VERSION)</code> to the tag; the job uploads exactly the five §1 #1 files with <code>--clobber</code>.</li><li><strong>bootstrap URL flow</strong> (§1 #5) - with <code>CYBEROS_PAYLOAD_URL=file://&lt;fixture&gt;/cyberos-payload.tar.gz</code> and a matching <code>SHA256SUMS</code>, bootstrap downloads, verifies, unpacks, and runs <code>init.sh</code> against the target repo (<code>.cyberos/VERSION</code> appears).</li><li><strong>bootstrap rejects a bad checksum</strong> (§1 #5) - with a tampered fixture tarball, bootstrap aborts before touching the target repo (no <code>.cyberos/</code> created).</li><li><strong>rollout from a release source</strong> (§1 #6) - <code>rollout.sh --from-release</code> against the file:// fixture initializes two scratch repos from one download (fixture served once; second repo reuses the temp payload).</li><li><strong>Docs list the four install paths with real URLs</strong> (§1 #7) - README and RELEASE.md contain the <code>releases/latest/download/</code> URLs and the placeholder sentence is gone.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># tools/cyberos-install/tests/test_release_assets.sh\n# Fixtures: scratch payload built by build.sh into $TMP; file:// URLs exercise the\n# download paths without network. Run: bash tools/cyberos-install/tests/test_release_assets.sh\n\nt01_deterministic_tarball()      # AC 1\nt02_five_files_two_name_forms()  # AC 2\nt03_sha256sums_roundtrip()       # AC 3\nt04_version_triple_check()       # AC 4  (GITHUB_REF_NAME mismatch -&gt; exit 10, empty out-dir)\nt05_workflow_shape()             # AC 5  (structural greps on release.yml: tag guard, upload step, --clobber)\nt06_bootstrap_url_happy_path()   # AC 6\nt07_bootstrap_bad_checksum()     # AC 7\nt08_rollout_from_release()       # AC 8\nt09_docs_real_urls()             # AC 9</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p><code>release-assets.sh</code>: version guard; <code>tar --sort=name --owner=0 --group=0 --mtime='2000-01-01 00:00:00Z' -cf - -C &lt;payload&gt; . | gzip -n</code>; copies; <code>sha256sum &gt; SHA256SUMS</code>. The upload step MUST be create-or-upload idempotent: <code>gh release create \"$GITHUB_REF_NAME\" --verify-tag --notes-from-tag 2&gt;/dev/null || true</code> before <code>gh release upload ... --clobber</code>, so the payload job never races the installer jobs on release creation. bootstrap: <code>curl -sfL</code> both files, <code>sha256sum -c --ignore-missing</code>, <code>tar -xzf</code> into mktemp, exec <code>init.sh</code>. rollout: argument branch that materializes the payload then falls through to the existing loop.</p>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-IMP-068 (<code>check-version-sync.sh</code> is the pre-upload gate). Blocks TASK-IMP-070 (remote update check needs a published \"latest\" to compare against). Related: TASK-PLUGIN-008 / TASK-SKILL-201 stay the long-term product registries; TASK-APP-001's Ops tab can later add a \"download latest release\" affordance on top of this channel.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code>$ ls assets/\nSHA256SUMS  cyberos-1.7.0.plugin  cyberos-payload-1.7.0.tar.gz  cyberos-payload.tar.gz  cyberos.plugin\n$ curl -sfL https://github.com/cyberskill-official/cyberos/releases/latest/download/cyberos-payload.tar.gz -o p.tgz</code></pre>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Whether to ALSO publish on every VERSION bump (not just tags) is deferred: tags stay the human release act per version.yml's \"auto version, manual release\" model.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Tag pushed at a commit whose VERSION lags (bot bump not yet merged) - the tag guard (§1 #4) fails the job with a one-line explanation instead of publishing mismatched assets.</li><li>Re-running a release (re-tag / workflow re-run) - <code>--clobber</code> makes uploads idempotent; determinism (#1) makes the re-uploaded bytes identical.</li><li>Partial upload (network drop mid-job) - assets are uploaded in one <code>gh release upload</code> invocation; a failed job leaves the release without <code>SHA256SUMS</code> at worst, and bootstrap refuses to install anything it cannot verify.</li><li>GitHub API rate-limited during bootstrap - curl fails, bootstrap aborts cleanly with the URL in the error; no partial <code>.cyberos/</code>.</li><li>A consumer pins <code>latest</code> but needs an old version - versioned asset names keep every release addressable; README documents the pinned form.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep asset names exactly as specified; TASK-IMP-070 and future npm packaging key off them. The tarball packs the payload CONTENTS at archive root (unpack -&gt; <code>init.sh</code> at top level), matching what <code>bootstrap.sh</code> expects from a local payload dir. Determinism flags (<code>--sort</code>, <code>--owner</code>, <code>--mtime</code>, <code>gzip -n</code>) are GNU tar/gzip semantics: the CI job runs on ubuntu; the test suite MUST skip the determinism case with a visible SKIP on non-GNU (macOS bsdtar) hosts rather than false-fail.</p>\n<p><em>End of TASK-IMP-069.</em></p>";
