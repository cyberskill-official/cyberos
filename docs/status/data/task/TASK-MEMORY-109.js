window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MEMORY-109"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The Claude Code hook capture service <strong>MUST</strong> integrate with Claude Code's documented hook system (<code>UserPromptSubmit</code>, <code>PostToolUse</code>, <code>Stop</code>) to emit memory audit rows for every meaningful Claude Code interaction. The contract:</p>\n<ol><li><strong>MUST</strong> install via <code>cyberos memory hook claude install &lt;project-path&gt;</code> which writes to <code>&lt;project-path&gt;/.claude/settings.json</code> (project-scope) OR <code>~/.claude/settings.json</code> (user-scope, with <code>--user</code> flag). The installer is idempotent: re-running with the same args is a no-op.</li><li><strong>MUST</strong> subscribe to exactly three Claude Code hook events:</li></ol>\n<ul><li><code>UserPromptSubmit</code> → emit <code>memory.claude_prompt</code> row with <code>{session_id, prompt_hash, prompt_redacted, cwd, trace_id, captured_at_ns}</code>. The <code>prompt_redacted</code> field carries the redacted prompt body (per TASK-MEMORY-111 ruleset); <code>prompt_hash</code> is <code>blake3(raw_prompt)</code> for dedup.</li><li><code>PostToolUse</code> → emit <code>memory.claude_tool_use</code> row with <code>{session_id, tool_name, tool_args_hash, outcome (success | error), duration_ms, trace_id}</code>. Tool args are HASHED, not stored (per DEC-151).</li><li><code>Stop</code> → emit <code>memory.claude_session_completed</code> row with <code>{session_id, prompt_count, tool_use_count, duration_ms, trace_id, last_assistant_message_redacted}</code>.</li></ul>\n<ol><li><strong>MUST NOT</strong> subscribe to <code>PreToolUse</code>, <code>Notification</code>, <code>SubagentStop</code>, or <code>SessionStart</code> — those are higher-frequency and offer marginal capture value vs the noise they add.</li><li><strong>MUST</strong> redact every prompt + assistant message BEFORE hashing or emitting per TASK-MEMORY-111's ruleset:</li></ol>\n<ul><li>AWS access-key / secret-key patterns → <code>&lt;AWS_KEY&gt;</code></li><li><code>Bearer &lt;token&gt;</code> → <code>Bearer &lt;REDACTED&gt;</code></li><li>GitHub PATs (<code>ghp_*</code>, <code>gho_*</code>, <code>ghu_*</code>, <code>ghs_*</code>, <code>ghr_*</code>) → <code>&lt;GH_PAT&gt;</code></li><li>OpenAI keys (<code>sk-*</code>) → <code>&lt;OPENAI_KEY&gt;</code></li><li>Anthropic keys (<code>sk-ant-*</code>) → <code>&lt;ANTHROPIC_KEY&gt;</code></li><li>Generic JWT-like patterns (3 base64 segments joined by dots) → <code>&lt;JWT&gt;</code></li><li>Vietnamese CCCD (12-digit) → <code>&lt;VN_CCCD&gt;</code></li><li>Email addresses (when not in @company.com allowlist) → <code>&lt;EMAIL&gt;</code></li><li>Phone numbers (E.164 or VN local form) → <code>&lt;PHONE&gt;</code></li></ul>\n<ol><li><strong>MUST</strong> fail-closed on redaction: if the redactor itself errors (regex panic, OOM), the hook emits a <code>memory.claude_capture_redaction_failed</code> row with <code>{session_id, error}</code> and exits 0 — never blocks Claude Code.</li><li><strong>MUST</strong> complete in ≤ 50ms p95 for the synchronous part (read stdin + redact + spawn emit). The actual memory write is delegated to a background tokio task via the TASK-MEMORY-107 capture daemon's local Unix socket (<code>/tmp/cyberos-memory-capture.sock</code>) — fire-and-forget. Hook process exits 0 once the message is enqueued.</li><li><strong>MUST</strong> correlate session-level rows via <code>session_id</code> (UUID; provided by Claude Code in the hook payload) AND a per-session W3C <code>trace_id</code> (32-char hex). The trace_id is generated at first hook invocation for a given session_id and cached in <code>/tmp/cyberos-memory-claude-traces/&lt;session_id&gt;</code> (file persists for 1 hour; pruned by TASK-MEMORY-110 sweeper).</li><li><strong>MUST</strong> read the hook payload as a single JSON object from stdin per Claude Code's documented schema. Schema validation is strict — unknown top-level keys are logged at WARN and ignored; missing required keys exits 0 with stderr WARN.</li><li><strong>MUST</strong> emit OTel span <code>memory.claude.hook</code> per invocation with attributes <code>hook_kind</code>, <code>session_id</code>, <code>duration_ms</code>, <code>redaction_match_count</code>.</li><li><strong>MUST NOT</strong> call <code>cyberos_memory_writer::MemoryWriter::emit</code> directly. The hook is a short-lived process; it MUST post to the long-running TASK-MEMORY-107 daemon via local Unix-domain socket. The daemon does the chain write. This decouples hook latency from chain commit latency.</li><li><strong>MUST</strong> be opt-in per project: <code>cyberos memory hook claude install &lt;project-path&gt;</code> writes the hook config; absent config = no hook = no capture. There is NO global default that captures every Claude Code project.</li><li><strong>MUST</strong> support <code>cyberos memory hook claude uninstall &lt;project-path&gt;</code> (reverse of install) and <code>cyberos memory hook claude status &lt;project-path&gt;</code> (prints installed hooks + last capture row for that project).</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>memory_claude_hook_total{kind, outcome}</code> (counter; outcome ∈ emitted | redaction_failed | socket_unavailable | invalid_payload).</li><li><code>memory_claude_hook_redactions_total{pattern}</code> (counter — operator visibility into what's being redacted).</li><li><code>memory_claude_hook_latency_seconds{kind}</code> (histogram; TASK-OBS-003 buckets).</li></ul>\n<ol><li><strong>SHOULD</strong> support <code>cyberos memory hook claude tail</code> — pretty-print the last N <code>memory.claude_*</code> rows for operator debugging.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why only three hook events (§1 #2 + #3)?</strong> Claude Code emits many hooks. The three we subscribe to are the load-bearing trio:</p>\n<ul><li><code>UserPromptSubmit</code> answers \"what was the user trying to do?\"</li><li><code>PostToolUse</code> answers \"how did the assistant act on it?\" (one row per tool, so we get a tool trace).</li><li><code>Stop</code> answers \"how did the session end and what was the resolution?\"</li></ul>\n<p>Other hooks are operationally noisy (<code>Notification</code>, <code>PreToolUse</code>) or rare (<code>SubagentStop</code>) — capturing them adds 5× chain pressure for ~5% incremental signal.</p>\n<p><strong>Why hash tool args, not store (§1 #2)?</strong> Tool args sometimes contain secrets — e.g. a <code>Bash</code> tool call like <code>curl -H \"Authorization: Bearer sk-abc\" ...</code>. We capture the <em>fact</em> the tool ran and the <em>hash</em> of its args (so we can dedup identical re-runs) but never the raw args. Operators investigating \"what was the call?\" can cross-reference the trace_id back to Claude Code's local log (which the user controls).</p>\n<p><strong>Why redact in the hook process, not in memory (§1 #4 + #5)?</strong> Redaction at write time means raw prompts never leave the user's machine — strongest privacy story. If we redacted server-side, the raw prompt would briefly exist in transit / in temporary buffers. The hook is the right boundary: redact before the bytes leave the local process tree.</p>\n<p><strong>Why fail-closed on redaction (§1 #5)?</strong> If we fail-OPEN (emit raw prompt on redactor failure), a buggy regex update could leak every prompt. Fail-CLOSED means the worst case is \"we lost one row of capture\" — recoverable. The <code>memory.claude_capture_redaction_failed</code> row tells the operator their redactor needs fixing, with no data leaked.</p>\n<p><strong>Why 50ms latency budget (§1 #6)?</strong> Hooks run on Claude Code's critical path. A 500ms hook makes the assistant feel sluggish; users disable it. 50ms is below human-noticeable. The redaction layer is ~5ms p95 (regex on ≤ 4KB prompt); the rest is JSON parse + socket send. Spec budgets 50ms p95 to leave headroom.</p>\n<p><strong>Why Unix socket to TASK-MEMORY-107 daemon (§1 #10)?</strong> Two reasons:</p>\n<ol><li>The hook is a short-lived process (spawned per event); opening a memory connection + writing chain rows + closing would take ~200ms — exceeds budget.</li><li>The daemon already has the chain-write machinery, the W3C trace context, the dedup cache. Reusing it via socket is one IPC vs. duplicating the writer stack.</li></ol>\n<p><strong>Why per-session trace_id cached in <code>/tmp</code> (§1 #7)?</strong> Within a Claude Code session, multiple hook invocations (1 UserPromptSubmit + N PostToolUse + 1 Stop) must share a trace_id so OBS dashboards can correlate them. The session_id is the natural key; <code>/tmp</code> is the right scope (per-machine, per-session, ephemeral). 1-hour TTL covers long sessions; pruned by TASK-MEMORY-110.</p>\n<p><strong>Why opt-in per project (§1 #11)?</strong> Some Claude Code projects are personal (researching a side project, debugging a friend's code) — operators don't want those captured. Per-project opt-in via <code>.claude/settings.json</code> is the right granularity: explicit consent, project-scoped, version-controllable.</p>\n<p><strong>Why <code>tool_args_hash</code> even though redacted (§1 #2)?</strong> Dedup. The same <code>Bash</code> invocation with the same args (e.g. <code>cargo test</code> run 10 times) should produce one logical \"tool was run\" memory, not 10. The hash key is <code>(tool_name, blake3(canonical_json(args)))</code>.</p>\n<p><strong>Why bullets vs prose in §1?</strong> Per the task template's normative structure; readability for compliance review.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Cargo.toml</h3>\n<pre><code class=\"language-toml\">[package]\nname        = \"cyberos-memory-claude-hook\"\nversion     = \"0.1.0\"\nedition     = \"2021\"\n\n[[bin]]\nname = \"cyberos-memory-claude-hook\"\npath = \"src/main.rs\"\n\n[dependencies]\nclap        = { version = \"4\", features = [\"derive\"] }\nserde       = { version = \"1\", features = [\"derive\"] }\nserde_json  = \"1\"\ntokio       = { version = \"1.40\", features = [\"rt-multi-thread\", \"macros\", \"net\", \"io-util\", \"time\"] }\nblake3      = \"1.5\"\nregex       = \"1.10\"\nonce_cell   = \"1.19\"\nanyhow      = \"1\"\nthiserror   = \"1\"\ntracing     = \"0.1\"\ntracing-opentelemetry = \"0.23\"\nuuid        = { version = \"1.10\", features = [\"v4\"] }</code></pre>\n<h3>Claude Code hook config (template)</h3>\n<pre><code class=\"language-jsonc\">// services/memory-claude-hook/install/.claude/settings.json.template\n{\n  \"hooks\": {\n    \"UserPromptSubmit\": [\n      { \"matcher\": \".*\", \"hooks\": [{ \"type\": \"command\", \"command\": \"cyberos-memory-claude-hook userpromptsubmit\" }] }\n    ],\n    \"PostToolUse\": [\n      { \"matcher\": \".*\", \"hooks\": [{ \"type\": \"command\", \"command\": \"cyberos-memory-claude-hook posttooluse\" }] }\n    ],\n    \"Stop\": [\n      { \"matcher\": \".*\", \"hooks\": [{ \"type\": \"command\", \"command\": \"cyberos-memory-claude-hook stop\" }] }\n    ]\n  }\n}</code></pre>\n<h3>CLI</h3>\n<pre><code class=\"language-rust\">// services/memory-claude-hook/src/main.rs\nuse clap::Parser;\nuse cyberos_cli_exit::ExitCode;\n\n#[derive(Parser)]\n#[command(name = \"cyberos-memory-claude-hook\")]\nstruct Cli {\n    #[command(subcommand)]\n    cmd: Cmd,\n}\n\n#[derive(Parser)]\nenum Cmd {\n    /// Process UserPromptSubmit JSON on stdin\n    Userpromptsubmit,\n    /// Process PostToolUse JSON on stdin\n    Posttooluse,\n    /// Process Stop JSON on stdin\n    Stop,\n}\n\n#[tokio::main(flavor = \"current_thread\")]\nasync fn main() -&gt; ExitCode {\n    let cli = Cli::parse();\n    // Read stdin as JSON\n    let mut buf = Vec::new();\n    if let Err(e) = tokio::io::AsyncReadExt::read_to_end(&amp;mut tokio::io::stdin(), &amp;mut buf).await {\n        eprintln!(\"WARN cyberos-memory-claude-hook: stdin read failed: {e}\");\n        return ExitCode::Ok;  // never block Claude Code\n    }\n    let payload: serde_json::Value = match serde_json::from_slice(&amp;buf) {\n        Ok(v) =&gt; v,\n        Err(e) =&gt; {\n            eprintln!(\"WARN cyberos-memory-claude-hook: invalid JSON: {e}\");\n            return ExitCode::Ok;\n        }\n    };\n    let kind = match cli.cmd {\n        Cmd::Userpromptsubmit =&gt; HookKind::UserPromptSubmit,\n        Cmd::Posttooluse      =&gt; HookKind::PostToolUse,\n        Cmd::Stop             =&gt; HookKind::Stop,\n    };\n    if let Err(e) = cyberos_memory_claude_hook::dispatch(kind, payload).await {\n        eprintln!(\"WARN cyberos-memory-claude-hook: dispatch failed: {e}\");\n        // still exit 0; redaction-failed audit row already emitted by dispatch on failure paths\n    }\n    ExitCode::Ok\n}</code></pre>\n<h3>Dispatch + emit</h3>\n<pre><code class=\"language-rust\">// services/memory-claude-hook/src/hook.rs\nuse serde::Deserialize;\nuse uuid::Uuid;\n\n#[derive(Clone, Copy, Debug)]\npub enum HookKind { UserPromptSubmit, PostToolUse, Stop }\n\n#[derive(Deserialize)]\n#[serde(rename_all = \"snake_case\")]\nstruct UserPromptSubmitPayload {\n    session_id:  Uuid,\n    prompt:      String,\n    cwd:         String,\n}\n#[derive(Deserialize)]\n#[serde(rename_all = \"snake_case\")]\nstruct PostToolUsePayload {\n    session_id:        Uuid,\n    tool_name:         String,\n    tool_args:         serde_json::Value,\n    outcome:           String,    // \"success\" | \"error\"\n    duration_ms:       u64,\n}\n#[derive(Deserialize)]\n#[serde(rename_all = \"snake_case\")]\nstruct StopPayload {\n    session_id:           Uuid,\n    prompt_count:         u32,\n    tool_use_count:       u32,\n    duration_ms:          u64,\n    last_assistant_message: Option&lt;String&gt;,\n}\n\npub async fn dispatch(kind: HookKind, payload: serde_json::Value) -&gt; anyhow::Result&lt;()&gt; {\n    let start = std::time::Instant::now();\n    let trace_id = trace_id_for_session(extract_session_id(&amp;payload)?).await?;\n\n    let row = match kind {\n        HookKind::UserPromptSubmit =&gt; {\n            let p: UserPromptSubmitPayload = serde_json::from_value(payload)?;\n            let (prompt_redacted, match_count) = crate::redact::redact(&amp;p.prompt);\n            serde_json::json!({\n                \"kind\": \"memory.claude_prompt\",\n                \"payload\": {\n                    \"session_id\":      p.session_id,\n                    \"prompt_hash\":     hex::encode(blake3::hash(p.prompt.as_bytes()).as_bytes()),\n                    \"prompt_redacted\": prompt_redacted,\n                    \"cwd\":             p.cwd,\n                    \"trace_id\":        trace_id,\n                    \"captured_at_ns\":  unix_ns(),\n                    \"redaction_match_count\": match_count,\n                }\n            })\n        }\n        HookKind::PostToolUse =&gt; {\n            let p: PostToolUsePayload = serde_json::from_value(payload)?;\n            let args_canonical = serde_json::to_string(&amp;p.tool_args)?;\n            let args_hash = hex::encode(blake3::hash(args_canonical.as_bytes()).as_bytes());\n            serde_json::json!({\n                \"kind\": \"memory.claude_tool_use\",\n                \"payload\": {\n                    \"session_id\":     p.session_id,\n                    \"tool_name\":      p.tool_name,\n                    \"tool_args_hash\": args_hash,\n                    \"outcome\":        p.outcome,\n                    \"duration_ms\":    p.duration_ms,\n                    \"trace_id\":       trace_id,\n                }\n            })\n        }\n        HookKind::Stop =&gt; {\n            let p: StopPayload = serde_json::from_value(payload)?;\n            let last_msg = p.last_assistant_message.as_deref().unwrap_or_default();\n            let (last_redacted, _) = crate::redact::redact(last_msg);\n            serde_json::json!({\n                \"kind\": \"memory.claude_session_completed\",\n                \"payload\": {\n                    \"session_id\":                     p.session_id,\n                    \"prompt_count\":                   p.prompt_count,\n                    \"tool_use_count\":                 p.tool_use_count,\n                    \"duration_ms\":                    p.duration_ms,\n                    \"last_assistant_message_redacted\": last_redacted,\n                    \"trace_id\":                       trace_id,\n                }\n            })\n        }\n    };\n    crate::emit::post_to_daemon(row).await?;\n    metric_record_latency(kind, start.elapsed());\n    Ok(())\n}\n\nasync fn trace_id_for_session(session_id: Uuid) -&gt; anyhow::Result&lt;String&gt; {\n    let path = std::path::Path::new(\"/tmp/cyberos-memory-claude-traces\").join(session_id.to_string());\n    if let Ok(bytes) = tokio::fs::read(&amp;path).await {\n        return Ok(String::from_utf8(bytes)?);\n    }\n    let new_tid = generate_trace_id();\n    let _ = tokio::fs::create_dir_all(path.parent().unwrap()).await;\n    tokio::fs::write(&amp;path, &amp;new_tid).await?;\n    Ok(new_tid)\n}</code></pre>\n<h3>Redactor</h3>\n<pre><code class=\"language-rust\">// services/memory-claude-hook/src/redact.rs\nuse once_cell::sync::Lazy;\nuse regex::Regex;\n\nstruct Rule { pat: Regex, replace: &amp;'static str, name: &amp;'static str }\n\nstatic RULES: Lazy&lt;Vec&lt;Rule&gt;&gt; = Lazy::new(|| vec![\n    Rule { pat: Regex::new(r\"AKIA[0-9A-Z]{16}\").unwrap(),                       replace: \"&lt;AWS_KEY&gt;\",        name: \"aws_access_key\" },\n    Rule { pat: Regex::new(r\"(?i)aws_secret(?:_access)?_key\\s*=\\s*\\S+\").unwrap(), replace: \"&lt;AWS_SECRET&gt;\",   name: \"aws_secret\" },\n    Rule { pat: Regex::new(r\"(?i)Bearer\\s+[A-Za-z0-9._\\-]{16,}\").unwrap(),     replace: \"Bearer &lt;REDACTED&gt;\", name: \"bearer_token\" },\n    Rule { pat: Regex::new(r\"gh[oprsu]_[A-Za-z0-9_]{16,}\").unwrap(),           replace: \"&lt;GH_PAT&gt;\",          name: \"github_pat\" },\n    Rule { pat: Regex::new(r\"sk-[A-Za-z0-9]{32,}\").unwrap(),                   replace: \"&lt;OPENAI_KEY&gt;\",      name: \"openai_key\" },\n    Rule { pat: Regex::new(r\"sk-ant-[A-Za-z0-9_-]{32,}\").unwrap(),             replace: \"&lt;ANTHROPIC_KEY&gt;\",   name: \"anthropic_key\" },\n    Rule { pat: Regex::new(r\"eyJ[A-Za-z0-9_-]+\\.eyJ[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+\").unwrap(),\n                                                                                replace: \"&lt;JWT&gt;\",            name: \"jwt\" },\n    Rule { pat: Regex::new(r\"\\b\\d{12}\\b\").unwrap(),                            replace: \"&lt;VN_CCCD&gt;\",         name: \"vn_cccd\" },\n    Rule { pat: Regex::new(r\"\\b[A-Za-z0-9._%+\\-]+@[A-Za-z0-9.\\-]+\\.[A-Za-z]{2,}\\b\").unwrap(),\n                                                                                replace: \"&lt;EMAIL&gt;\",          name: \"email\" },\n    Rule { pat: Regex::new(r\"\\+?\\d{1,3}[\\s\\-]?\\d{2,4}[\\s\\-]?\\d{3,4}[\\s\\-]?\\d{3,4}\").unwrap(),\n                                                                                replace: \"&lt;PHONE&gt;\",          name: \"phone\" },\n]);\n\npub fn redact(input: &amp;str) -&gt; (String, u32) {\n    let mut buf = input.to_string();\n    let mut match_count = 0u32;\n    for rule in RULES.iter() {\n        let before_len = buf.len();\n        buf = rule.pat.replace_all(&amp;buf, rule.replace).into_owned();\n        // crude count: each replacement changes length by (rule.replace.len() - matched.len());\n        // accept rough count as match metric\n        if buf.len() != before_len {\n            match_count += 1;\n            metrics::counter!(\"memory_claude_hook_redactions_total\", \"pattern\" =&gt; rule.name).increment(1);\n        }\n    }\n    (buf, match_count)\n}</code></pre>\n<h3>Socket emit</h3>\n<pre><code class=\"language-rust\">// services/memory-claude-hook/src/emit.rs\nuse tokio::io::AsyncWriteExt;\nuse tokio::net::UnixStream;\nuse std::time::Duration;\n\nconst SOCKET: &amp;str = \"/tmp/cyberos-memory-capture.sock\";\n\npub async fn post_to_daemon(row: serde_json::Value) -&gt; anyhow::Result&lt;()&gt; {\n    let mut stream = tokio::time::timeout(Duration::from_millis(40), UnixStream::connect(SOCKET)).await??;\n    let bytes = serde_json::to_vec(&amp;row)?;\n    let len = (bytes.len() as u32).to_be_bytes();\n    stream.write_all(&amp;len).await?;\n    stream.write_all(&amp;bytes).await?;\n    stream.flush().await?;\n    // Don't wait for ack — fire-and-forget per §1 #6\n    Ok(())\n}</code></pre>\n<h3>Installer</h3>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\n# services/memory-claude-hook/install/install-hooks.sh\n# Usage: install-hooks.sh &lt;project-path&gt;  OR  install-hooks.sh --user\n\nset -euo pipefail\n\nif [ \"${1:-}\" = \"--user\" ]; then\n  SETTINGS=\"$HOME/.claude/settings.json\"\nelse\n  PROJECT=\"${1:?usage: install-hooks.sh &lt;project-path&gt; | --user}\"\n  SETTINGS=\"$PROJECT/.claude/settings.json\"\nfi\nmkdir -p \"$(dirname \"$SETTINGS\")\"\n\nTEMPLATE=\"$(dirname \"$0\")/.claude/settings.json.template\"\n\n# Idempotent merge: if hooks already present with matching command, no-op\nif [ -f \"$SETTINGS\" ] &amp;&amp; jq -e '.hooks.UserPromptSubmit[0].hooks[0].command == \"cyberos-memory-claude-hook userpromptsubmit\"' \"$SETTINGS\" &gt; /dev/null 2&gt;&amp;1; then\n  echo \"✓ already installed (no changes): $SETTINGS\"\n  exit 0\nfi\n\nif [ -f \"$SETTINGS\" ]; then\n  # Merge: prepend our hooks; preserve user's existing other hooks\n  jq -s '.[0] * .[1]' \"$SETTINGS\" \"$TEMPLATE\" &gt; \"$SETTINGS.new\" &amp;&amp; mv \"$SETTINGS.new\" \"$SETTINGS\"\nelse\n  cp \"$TEMPLATE\" \"$SETTINGS\"\nfi\necho \"✓ installed hooks → $SETTINGS\"</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>UserPromptSubmit → memory.claude_prompt</strong> — fixture JSON on stdin → 1 row emitted to daemon socket; payload contains session_id, prompt_hash, prompt_redacted, cwd, trace_id, captured_at_ns.</li><li><strong>PostToolUse → memory.claude_tool_use</strong> — fixture JSON → 1 row; tool_args_hash present; raw tool_args NOT present.</li><li><strong>Stop → memory.claude_session_completed</strong> — fixture JSON → 1 row; prompt_count + tool_use_count + last_assistant_message_redacted present.</li><li><strong>Redaction: AWS access key</strong> — prompt containing <code>AKIA1234567890123456</code> → redacted to <code>&lt;AWS_KEY&gt;</code>; <code>redaction_match_count &gt;= 1</code>.</li><li><strong>Redaction: Bearer token</strong> — prompt containing <code>Authorization: Bearer abc123def456...</code> → <code>Bearer &lt;REDACTED&gt;</code>.</li><li><strong>Redaction: OpenAI key</strong> — <code>sk-abcdefghij1234567890abcdefghij12</code> → <code>&lt;OPENAI_KEY&gt;</code>.</li><li><strong>Redaction: Anthropic key</strong> — <code>sk-ant-api03-abc...</code> → <code>&lt;ANTHROPIC_KEY&gt;</code>.</li><li><strong>Redaction: GitHub PAT</strong> — <code>ghp_abc123...</code> → <code>&lt;GH_PAT&gt;</code>.</li><li><strong>Redaction: JWT</strong> — three-segment base64 → <code>&lt;JWT&gt;</code>.</li><li><strong>Redaction: VN CCCD</strong> — 12-digit number → <code>&lt;VN_CCCD&gt;</code>.</li><li><strong>Redaction: email</strong> — <code>user@example.com</code> → <code>&lt;EMAIL&gt;</code> (unless <code>@cyberskill.world</code> allowlisted per TASK-MEMORY-111).</li><li><strong>Redaction fail-closed</strong> — inject regex panic via test seam → <code>memory.claude_capture_redaction_failed</code> row emitted; hook exits 0.</li><li><strong>Same session → same trace_id</strong> — three hook invocations with same <code>session_id</code> → all three rows carry identical trace_id.</li><li><strong>Different sessions → different trace_ids</strong> — two sessions → two different trace_ids (32-char hex, no collision).</li><li><strong>Latency p95 ≤ 50ms</strong> — 100-trial test harness with 4KB prompt; p95 latency &lt; 50ms.</li><li><strong>Socket unavailable → graceful</strong> — daemon down; hook exits 0 with stderr WARN <code>socket_unavailable</code>; metric <code>memory_claude_hook_total{outcome=\"socket_unavailable\"}</code> increments.</li><li><strong>Invalid JSON on stdin → graceful</strong> — malformed JSON → exit 0 with stderr WARN; metric <code>outcome=\"invalid_payload\"</code>.</li><li><strong>Missing required field → graceful</strong> — JSON missing <code>session_id</code> → exit 0 with stderr WARN; no row emitted.</li><li><strong>Installer idempotent</strong> — run <code>install-hooks.sh</code> twice → second run is a no-op; <code>~/.claude/settings.json</code> byte-identical.</li><li><strong>Installer preserves user hooks</strong> — pre-existing <code>Stop</code> hook in user settings → after install, both user's hook AND ours are present; user's hook runs first.</li><li><strong>Uninstall removes our hooks</strong> — <code>cyberos memory hook claude uninstall &lt;path&gt;</code> → settings.json contains only the user's pre-existing hooks; ours removed.</li><li><strong>Status shows last capture</strong> — <code>cyberos memory hook claude status &lt;path&gt;</code> → prints \"installed\" + the timestamp + session_id of the last <code>memory.claude_*</code> row.</li><li><strong>OTel span per invocation</strong> — exporter receives a <code>memory.claude.hook</code> span per invocation with <code>hook_kind</code> + <code>session_id</code> attributes.</li><li><strong>Metric: redaction counters</strong> — running redactor 5× on a prompt with all 10 patterns → <code>memory_claude_hook_redactions_total{pattern=\"aws_access_key\"}</code> etc. increment.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/memory/tests/ingest_test.rs\n\n#[tokio::test]\nasync fn userpromptsubmit_emits_claude_prompt_row() {\n    let (daemon_socket, mut rx) = stub_daemon().await;\n    let payload = serde_json::json!({\n        \"session_id\": \"00000000-0000-0000-0000-000000000001\",\n        \"prompt\":     \"Fix the auth bug\",\n        \"cwd\":        \"/Users/x/proj\"\n    });\n    invoke_hook(HookKind::UserPromptSubmit, payload, &amp;daemon_socket).await;\n    let row = rx.recv().await.unwrap();\n    assert_eq!(row[\"kind\"], \"memory.claude_prompt\");\n    assert_eq!(row[\"payload\"][\"prompt_redacted\"], \"Fix the auth bug\");\n    assert!(row[\"payload\"][\"trace_id\"].as_str().unwrap().len() == 32);\n}\n\n#[tokio::test]\nasync fn redacts_aws_key_in_prompt() {\n    let (daemon_socket, mut rx) = stub_daemon().await;\n    let payload = serde_json::json!({\n        \"session_id\": \"00000000-0000-0000-0000-000000000002\",\n        \"prompt\":     \"use this key AKIA1234567890123456\",\n        \"cwd\":        \"/Users/x/proj\"\n    });\n    invoke_hook(HookKind::UserPromptSubmit, payload, &amp;daemon_socket).await;\n    let row = rx.recv().await.unwrap();\n    assert!(row[\"payload\"][\"prompt_redacted\"].as_str().unwrap().contains(\"&lt;AWS_KEY&gt;\"));\n    assert!(!row[\"payload\"][\"prompt_redacted\"].as_str().unwrap().contains(\"AKIA1234567890123456\"));\n    assert!(row[\"payload\"][\"redaction_match_count\"].as_u64().unwrap() &gt;= 1);\n}\n\n#[tokio::test]\nasync fn same_session_gets_same_trace_id() {\n    let (daemon_socket, mut rx) = stub_daemon().await;\n    let session = uuid::Uuid::new_v4();\n    for kind in [HookKind::UserPromptSubmit, HookKind::PostToolUse, HookKind::Stop] {\n        invoke_hook(kind, fixture_for(kind, session), &amp;daemon_socket).await;\n    }\n    let rows: Vec&lt;_&gt; = (0..3).map(|_| rx.try_recv().unwrap()).collect();\n    let trace_ids: std::collections::HashSet&lt;_&gt; = rows.iter().map(|r| r[\"payload\"][\"trace_id\"].clone()).collect();\n    assert_eq!(trace_ids.len(), 1);\n}\n\n#[tokio::test]\nasync fn latency_p95_under_50ms() {\n    let (daemon_socket, _rx) = stub_daemon().await;\n    let mut latencies = Vec::new();\n    for _ in 0..100 {\n        let start = std::time::Instant::now();\n        invoke_hook(HookKind::UserPromptSubmit, fixture_prompt_4kb(), &amp;daemon_socket).await;\n        latencies.push(start.elapsed());\n    }\n    latencies.sort();\n    let p95 = latencies[95];\n    assert!(p95 &lt; std::time::Duration::from_millis(50), \"p95 = {:?}\", p95);\n}\n\n#[tokio::test]\nasync fn socket_unavailable_exits_0() {\n    let payload = fixture_prompt(&amp;uuid::Uuid::new_v4());\n    let res = run_hook_subprocess(HookKind::UserPromptSubmit, payload, \"/tmp/no-such-socket\").await;\n    assert_eq!(res.exit_code, 0);\n    assert!(res.stderr.contains(\"socket_unavailable\") || res.stderr.contains(\"WARN\"));\n}</code></pre>\n<pre><code class=\"language-rust\">// services/memory-claude-hook/tests/redact_test.rs\n#[test] fn redacts_aws_access_key()    { let (out, n) = redact(\"AKIA1234567890ABCDEF\"); assert_eq!(out, \"&lt;AWS_KEY&gt;\"); assert_eq!(n, 1); }\n#[test] fn redacts_openai_key()        { let (out, _) = redact(\"sk-abcdefghij1234567890abcdefghij12\"); assert!(out.contains(\"&lt;OPENAI_KEY&gt;\")); }\n#[test] fn redacts_anthropic_key()     { let (out, _) = redact(\"sk-ant-api03-foobar1234567890abcdefghij\"); assert!(out.contains(\"&lt;ANTHROPIC_KEY&gt;\")); }\n#[test] fn redacts_jwt()               { let (out, _) = redact(\"eyJhbGciOiJIUzI1.eyJzdWIiOiIxMjM.SflKxw\"); assert!(out.contains(\"&lt;JWT&gt;\")); }\n#[test] fn redacts_github_pat()        { let (out, _) = redact(\"ghp_abcdefghij1234567890abcdefghij\"); assert!(out.contains(\"&lt;GH_PAT&gt;\")); }\n#[test] fn redacts_bearer()            { let (out, _) = redact(\"Authorization: Bearer abcdefghij1234\"); assert!(out.contains(\"Bearer &lt;REDACTED&gt;\")); }\n#[test] fn redacts_vn_cccd()           { let (out, _) = redact(\"CCCD 079123456789\"); assert!(out.contains(\"&lt;VN_CCCD&gt;\")); }\n#[test] fn no_false_positive_on_short_numeric() { let (out, n) = redact(\"issue #12345\"); assert_eq!(out, \"issue #12345\"); assert_eq!(n, 0); }</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Above §3 is the skeleton.)</p>\n<pre><code class=\"language-rust\">// Lib root\npub mod hook;\npub mod redact;\npub mod emit;\n\npub async fn dispatch(kind: hook::HookKind, payload: serde_json::Value) -&gt; anyhow::Result&lt;()&gt; {\n    hook::dispatch(kind, payload).await\n}</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-MEMORY-107 (upstream)</strong> — capture daemon owns the Unix socket; hook is a client.</li><li><strong>TASK-MEMORY-101</strong> — <code>MemoryWriter</code> (used by daemon, not directly by hook).</li><li><strong>TASK-MEMORY-111 (sibling)</strong> — PII redaction ruleset; this task uses a subset of those patterns.</li><li><strong>TASK-AI-022</strong> — W3C TraceContext convention.</li><li><strong>TASK-AI-014</strong> — <code>last_assistant_message</code> field could carry persona tagging in future.</li><li><strong>TASK-SKILL-101</strong> — skill invocations are a sibling capture path; this task captures only Claude Code's native tool use.</li><li><strong><code>cyberos-cli-exit</code></strong> — shared exit codes (hook always exits 0 — never block Claude Code).</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3><code>memory.claude_prompt</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.claude_prompt\",\n  \"payload\": {\n    \"session_id\":             \"0e3b1a2c-4f5d-6789-abcd-ef0123456789\",\n    \"prompt_hash\":            \"9b0e8c5...\",\n    \"prompt_redacted\":        \"Help me refactor auth using key &lt;AWS_KEY&gt;\",\n    \"cwd\":                    \"/Users/stephencheng/Projects/CyberSkill/cyberos\",\n    \"trace_id\":               \"0af7651916cd43dd8448eb211c80319c\",\n    \"captured_at_ns\":         1747407137483000000,\n    \"redaction_match_count\":  1\n  }\n}</code></pre>\n<h3><code>memory.claude_tool_use</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.claude_tool_use\",\n  \"payload\": {\n    \"session_id\":     \"0e3b1a2c-4f5d-6789-abcd-ef0123456789\",\n    \"tool_name\":      \"Edit\",\n    \"tool_args_hash\": \"ab12cd...\",\n    \"outcome\":        \"success\",\n    \"duration_ms\":    142,\n    \"trace_id\":       \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<h3><code>memory.claude_session_completed</code></h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"memory.claude_session_completed\",\n  \"payload\": {\n    \"session_id\":                    \"0e3b1a2c-4f5d-6789-abcd-ef0123456789\",\n    \"prompt_count\":                  3,\n    \"tool_use_count\":                12,\n    \"duration_ms\":                   847000,\n    \"last_assistant_message_redacted\": \"Done. Auth middleware refactored; tests pass.\",\n    \"trace_id\":                      \"0af7651916cd43dd8448eb211c80319c\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Subagent capture (<code>SubagentStop</code> hook) — slice 3+; needs design.</li><li>Tool-arg content preview (operator-only, project-scoped, signed) — slice 3+.</li><li>Cross-machine session correlation (same user, two laptops) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Hook process spawned but daemon down</td><td>UnixStream::connect ECONNREFUSED</td><td>Hook exits 0; stderr WARN <code>socket_unavailable</code>; metric increments</td><td>TASK-MEMORY-110 restarts daemon; next hook succeeds</td></tr><tr><td>Daemon socket exists but daemon hung</td><td>Connect succeeds; write times out at 40ms</td><td>Hook exits 0; row lost (single event); metric <code>outcome=\"emit_timeout\"</code></td><td>TASK-MEMORY-110 SIGTERM + restart daemon</td></tr><tr><td>Malformed JSON on stdin</td><td>serde_json::from_slice Err</td><td>Exit 0; stderr WARN; metric <code>outcome=\"invalid_payload\"</code></td><td>Operator updates Claude Code OR raises issue</td></tr><tr><td>Required field missing</td><td>serde_json::from_value Err</td><td>Exit 0; stderr WARN</td><td>Same as above</td></tr><tr><td>Redactor regex panic</td><td>std::panic::catch_unwind catches</td><td><code>memory.claude_capture_redaction_failed</code> row emitted; exit 0</td><td>Operator updates redactor</td></tr><tr><td>/tmp full</td><td>trace_id cache write fails</td><td>Hook still proceeds with generated trace_id (in-mem); next hook for same session may regenerate (correlation broken)</td><td>Operator frees /tmp</td></tr><tr><td>Same session_id reused across days</td><td>trace_id cache 1h TTL expired</td><td>New trace_id generated; rows from same logical session split across trace_ids</td><td>By design; operators correlate via session_id</td></tr><tr><td>Very large prompt (&gt; 1 MB)</td><td>Read succeeds; redact takes longer</td><td>Latency may exceed 50ms; row emitted; metric <code>latency_seconds</code> p99 alarm</td><td>Operator splits; or accept; or raises budget</td></tr><tr><td>Claude Code passes extra unknown fields</td><td>serde unknown-field handling</td><td>Logged at WARN; field ignored; row still emitted</td><td>None</td></tr><tr><td>Hook installed but user disabled hooks in Claude Code</td><td>Hook never invoked</td><td>No rows; no error</td><td>Operator re-enables OR uninstalls cyberos hook</td></tr><tr><td>install-hooks.sh run on missing /.claude dir</td><td>mkdir -p creates it; settings.json written</td><td>Success</td><td>None</td></tr><tr><td>install-hooks.sh: settings.json malformed</td><td>jq Err during merge</td><td>Exit 1; stderr message; user's file unchanged</td><td>Operator manually fixes</td></tr><tr><td>Two hooks for same event from two tools (ours + theirs)</td><td>Both run in order</td><td>Both succeed independently</td><td>By design; AC #20</td></tr><tr><td>Hook binary missing (uninstalled cyberos)</td><td>Claude Code logs hook spawn error</td><td>No rows; Claude Code session continues normally</td><td>Operator reinstalls</td></tr><tr><td>Tracing exporter unavailable</td><td>tracing-otel buffers</td><td>Hook continues; spans buffered; eventually dropped</td><td>Operator restarts TASK-OBS-001 collector</td></tr><tr><td>Session_id is not a valid UUID</td><td>Parse error</td><td>Exit 0; stderr WARN; metric <code>invalid_payload</code></td><td>Operator files Claude Code bug</td></tr><tr><td>Redaction yields empty string</td><td>Acceptable</td><td>Row emitted with empty <code>prompt_redacted</code>; <code>redaction_match_count</code> high</td><td>By design — operator sees N matches but text is fully redacted</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>The hook binary MUST be in <code>PATH</code> for Claude Code to find it. <code>cyberos memory hook claude install</code> updates <code>settings.json</code> with the absolute path discovered via <code>which cyberos-memory-claude-hook</code> (fallback: <code>/usr/local/bin/cyberos-memory-claude-hook</code>).</li><li><code>tokio::main(flavor = \"current_thread\")</code> is intentional — the hook is short-lived; a multi-thread runtime would add tens of ms of startup cost.</li><li>The Unix socket path <code>/tmp/cyberos-memory-capture.sock</code> is documented in TASK-MEMORY-107; both this task and the daemon must agree. Constant lives in <code>cyberos-memory-shared</code> crate.</li><li>Redaction patterns are intentionally simple regexes — fast (≤ 5ms p95) but with known false-positive characteristics (e.g. 12-digit number false-positives as CCCD). Operators can tune the ruleset; TASK-MEMORY-111 owns the canonical version.</li><li>The <code>tool_args_hash</code> uses <code>serde_json::to_string</code> (compact form, default key ordering); this is NOT canonical JSON. For cross-session dedup, future task may switch to canonical (RFC 8785).</li><li>The 1-hour TTL on trace_id cache is a balance: too short → sessions split across trace_ids; too long → /tmp accumulates files. TASK-MEMORY-110 sweeper deletes files older than 1h hourly.</li><li><code>metric_record_latency</code> is a thin wrapper around the <code>metrics</code> crate's histogram macros; the actual Prometheus exporter is in the daemon.</li><li>The <code>--user</code> flag installer scope is the right default for personal workflows; <code>--project</code> (no flag) is the right default for team-shared projects (the hook config commits to git).</li><li>The installer is bash, not Rust, because: (a) it's pre-rustup-availability; (b) <code>jq</code> is the standard tool for JSON merge; (c) ~30 lines is too small to justify a binary.</li></ul>\n<hr>\n<p><em>End of TASK-MEMORY-109.</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Shipped as modules/memory/cyberos/claude_code_hook.py (no separate services/memory-claude-hook crate).</p>";
