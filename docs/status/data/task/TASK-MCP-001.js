window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MCP-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The MCP Gateway service <strong>MUST</strong> ship compliance with MCP specification 2025-11-25 — <code>initialize</code> handshake, <code>tools/list</code>, <code>tools/call</code>, capabilities negotiation, Streamable HTTP transport. Each requirement:</p>\n<ol><li><strong>MUST</strong> advertise <code>protocolVersion: \"2025-11-25\"</code> in every <code>initialize</code> response (per DEC-260 + DEC-270). On client <code>initialize</code> request with a non-matching <code>protocolVersion</code>, return JSON-RPC error -32600 <code>protocol_version_mismatch</code> with body <code>{\"supported\":[\"2025-11-25\"]}</code>. Future versions will be added to the <code>supported</code> array; clients can negotiate the highest mutual version.</li></ol>\n<ol><li><strong>MUST</strong> implement the <strong>JSON-RPC 2.0</strong> wire protocol (per DEC-263). Requests, responses, errors, and batch requests conform to https://www.jsonrpc.org/specification. Specifically:</li></ol>\n<ul><li>Request: <code>{\"jsonrpc\":\"2.0\",\"method\":\"&lt;name&gt;\",\"params\":&lt;obj&gt;,\"id\":&lt;id&gt;}</code>.</li><li>Response: <code>{\"jsonrpc\":\"2.0\",\"result\":&lt;obj&gt;,\"id\":&lt;id&gt;}</code>.</li><li>Error: <code>{\"jsonrpc\":\"2.0\",\"error\":{\"code\":&lt;int&gt;,\"message\":\"&lt;text&gt;\",\"data\":&lt;obj?&gt;},\"id\":&lt;id|null&gt;}</code>.</li><li>Batch: a JSON array of request objects; response is a JSON array of response objects in the same order (notifications omitted from response).</li></ul>\n<ol><li><strong>MUST</strong> implement <strong>Streamable HTTP transport</strong> per spec 2025-11-25 (per DEC-261):</li></ol>\n<ul><li>POST <code>/mcp</code> with <code>Content-Type: application/json</code> for client→server requests; response is a single JSON document with <code>Content-Type: application/json</code>.</li><li>GET <code>/mcp</code> with <code>Accept: text/event-stream</code> opens a Server-Sent Events stream for server→client messages (notifications, sampling requests, elicitation requests).</li><li>The legacy HTTP+SSE (separate POST + GET pair) transport is NOT implemented.</li></ul>\n<ol><li><strong>MUST</strong> support <strong>session resumption</strong> via the <code>Mcp-Session-Id</code> request header. Initial <code>initialize</code> response sets <code>Mcp-Session-Id: &lt;uuid&gt;</code>; subsequent requests carry the same id. Server may reject expired session ids with HTTP 404 (client re-initialises).</li></ol>\n<ol><li><strong>MUST</strong> implement the <code>initialize</code> method returning capabilities (per DEC-266). The response advertises: ``<code>json { \"protocolVersion\": \"2025-11-25\", \"capabilities\": { \"tools\": {\"listChanged\": true}, \"prompts\": {\"listChanged\": true}, \"resources\": {\"listChanged\": true, \"subscribe\": true}, \"logging\": {} }, \"serverInfo\": { \"name\": \"cyberos.mcp-gateway\", \"version\": \"&lt;semver&gt;\", \"title\": \"CyberOS MCP Gateway\" }, \"instructions\": \"Federation of 22 CyberOS modules. All calls audit-chained. OAuth 2.1 PKCE auth via TASK-MCP-004.\" } </code>`<code> </code>sampling<code> and </code>roots` capabilities are deferred (slice 5+).</li></ol>\n<ol><li><strong>MUST</strong> implement <code>tools/list</code> returning the federated catalog (per DEC-262). Response shape per spec: ``<code>json { \"tools\": [ { \"name\": \"cyberos.memory.search_memory\", \"description\": \"Search memory audit-chained memories by query string.\", \"inputSchema\": {\"type\":\"object\",\"properties\":{\"query\":{\"type\":\"string\"}},\"required\":[\"query\"]}, \"annotations\": { \"title\": \"Search memory\", \"readOnlyHint\": true, \"destructiveHint\": false, \"idempotentHint\": true, \"openWorldHint\": false } } /* ... */ ], \"nextCursor\": \"&lt;opaque&gt;\"   // present when paginating } </code>`` Cursor pagination is mandatory when catalog &gt; 100 tools.</li></ol>\n<ol><li><strong>MUST</strong> implement <code>tools/call</code> dispatching to the owning module's MCP server (per DEC-262). Request: <code>{\"name\":\"cyberos.&lt;module&gt;.&lt;verb&gt;_&lt;noun&gt;\", \"arguments\":&lt;obj&gt;}</code>. Response per spec: ``<code>json { \"content\": [ {\"type\": \"text\", \"text\": \"...\"}, {\"type\": \"image\", \"data\": \"&lt;base64&gt;\", \"mimeType\": \"image/png\"}, {\"type\": \"resource\", \"resource\": {\"uri\":\"...\"}} ], \"isError\": false, \"structuredContent\": &lt;obj?&gt; } </code>`<code> </code>isError: true` on tool-side error; transport-level errors return JSON-RPC error responses instead.</li></ol>\n<ol><li><strong>MUST</strong> implement closed JSON-RPC error code mapping (per DEC-272):</li></ol>\n<ul><li><code>-32700 parse_error</code> — malformed JSON.</li><li><code>-32600 invalid_request</code> — request not a valid Request object; includes protocol_version_mismatch.</li><li><code>-32601 method_not_found</code> — method not implemented (e.g. <code>sampling/createMessage</code> at slice 4).</li><li><code>-32602 invalid_params</code> — bad arguments shape.</li><li><code>-32603 internal_error</code> — gateway-internal error.</li><li><code>-32001 unauthorized</code> — missing or invalid JWT, missing scope (per §1 #11).</li><li><code>-32002 rate_limited</code> — per (tenant, tool) rate limit exceeded (per DEC-268).</li><li><code>-32003 tool_not_found</code> — tool name not in registry.</li><li><code>-32004 module_unreachable</code> — owning module server returned 5xx or timed out.</li><li><code>-32005 elicitation_required</code> — destructive tool requires Elicitation flow (per TASK-MCP-006); slice 4 stub returns this when tool annotation <code>destructiveHint: true</code> AND <code>Elicitation-Confirmed: 1</code> header absent.</li></ul>\n<ol><li><strong>MUST</strong> support <strong>JSON-RPC batch requests</strong> (per DEC-263). Server receives a JSON array, dispatches each request concurrently (with per-tool isolation), and returns an array of responses in the <strong>same order</strong>. Notifications (no <code>id</code> field) are omitted from the response array.</li></ol>\n<ol><li><strong>MUST</strong> implement <code>notifications/initialized</code> (client → server, no response expected). After receiving it, the gateway considers the session active and may push server-initiated messages over the SSE stream.</li></ol>\n<ol><li><strong>MUST</strong> verify the OAuth 2.1 access token (TASK-AUTH-004 JWT) on every request EXCEPT <code>initialize</code>. Verification:</li></ol>\n<ul><li>JWT signature valid against the AUTH JWKS.</li><li><code>aud</code> claim matches <code>https://mcp.cyberos.com</code> (audience-bound per TASK-MCP-004).</li><li><code>exp</code> claim not past; <code>nbf</code> claim not in future.</li><li><code>scope</code> claim contains <code>mcp:tools</code> (baseline) AND any tool-specific scope per tool annotation. Missing/invalid → <code>-32001 unauthorized</code> with <code>data: {\"reason\":\"&lt;token_invalid|aud_mismatch|expired|insufficient_scope&gt;\", \"required_scopes\":[...]}</code>. The <code>initialize</code> exemption allows clients to negotiate before auth, but any subsequent method requires the token.</li></ul>\n<ol><li><strong>MUST</strong> rate-limit per (tenant_id, tool_name) using a sliding window (per DEC-268). Default: 100 calls/min; soft burst: 200 in any 30-sec window. Exceeded → <code>-32002 rate_limited</code> with <code>data: {\"retry_after_ms\": &lt;int&gt;}</code>. Per-tenant override via tenant policy YAML (out of scope here; task-MCP-2xx).</li></ol>\n<ol><li><strong>MUST</strong> emit <code>mcp.tool_call_started</code> memory audit row at the moment the gateway begins dispatch to the module server, AND <code>mcp.tool_call_completed</code> at completion (per DEC-265 + task-audit skill rule 26). Both rows carry: <code>{tenant_id, subject_id_hash16, tool_name, arguments_sha256, persona_version, request_id, trace_id, ts_ns}</code>. The completed row adds: <code>outcome</code> (success | tool_error | module_unreachable | timeout | rate_limited | unauthorized), <code>duration_ms</code>, <code>result_sha256</code> (SHA-256 of result JSON; for replay verification).</li></ol>\n<ol><li><strong>MUST</strong> propagate W3C <code>traceparent</code> header from inbound request to the outbound dispatch to the module server. If absent on inbound, generate fresh per task-audit skill rule 22.</li></ol>\n<ol><li><strong>MUST</strong> support cursor-based pagination on <code>tools/list</code> (per §1 #6). The <code>cursor</code> parameter is opaque to the client (base64-encoded internal pagination state); server returns <code>nextCursor: null</code> on last page.</li></ol>\n<ol><li><strong>MUST</strong> dispatch <code>tools/call</code> to the owning module server via HTTP POST <code>https://&lt;module&gt;.internal.cyberos/mcp</code> with timeout 30s. The module's response is wrapped in the gateway's JSON-RPC response (the gateway is transparent — adds audit metadata, doesn't transform the result).</li></ol>\n<ol><li><strong>MUST</strong> handle module-server timeouts with <code>-32004 module_unreachable</code> after 30s; the memory row carries <code>outcome=timeout</code>.</li></ol>\n<ol><li><strong>MUST</strong> maintain the federated tool catalog in-memory at the gateway. Modules register via <code>POST /v1/mcp/register</code> (TASK-MCP-002 ships the handler; this task ships the registry struct). The catalog is rebuilt at gateway start by polling each module's <code>/mcp/heartbeat</code> endpoint.</li></ol>\n<ol><li><strong>MUST</strong> annotate every tool in the registry with its capability requirements: <code>requires_scope: [...]</code>, <code>requires_persona: &lt;key?&gt;</code>, <code>module: &lt;module-name&gt;</code>, <code>endpoint: &lt;internal-url&gt;</code>. These are NOT exposed in <code>tools/list</code> response (internal use only); the public <code>annotations</code> field carries the spec-defined <code>destructiveHint</code>/<code>readOnlyHint</code>/<code>idempotentHint</code>/<code>openWorldHint</code>.</li></ol>\n<ol><li><strong>MUST</strong> complete <code>initialize</code> handshake in ≤ 100 ms p95 (no LLM, no DB read; just signature verify + capabilities serialise). <code>initialize_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> complete <code>tools/list</code> in ≤ 200 ms p95 from in-memory registry. <code>tools_list_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> complete <code>tools/call</code> dispatch (gateway-side only — excluding module execution) in ≤ 50 ms p95 (JWT verify + scope check + audit emit + outbound dispatch). <code>tools_call_perf_test</code> asserts.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel span <code>mcp.gateway.{initialize,tools_list,tools_call}</code> per request with attributes: <code>tenant_id</code>, <code>subject_id_hash16</code>, <code>tool_name</code>, <code>outcome</code>, <code>duration_ms</code>.</li></ol>\n<ol><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>mcp_gateway_request_total{method, outcome}</code> (counter).</li><li><code>mcp_gateway_request_latency_ms{method}</code> (histogram).</li><li><code>mcp_gateway_active_sessions{tenant_id}</code> (gauge — sessions with <code>Mcp-Session-Id</code> still valid).</li><li><code>mcp_gateway_tool_call_total{tool_name, outcome}</code> (counter).</li><li><code>mcp_gateway_rate_limit_hits_total{tenant_id, tool_name}</code> (counter).</li><li><code>mcp_gateway_module_unreachable_total{module}</code> (counter).</li></ul>\n<ol><li><strong>MUST</strong> expose <code>GET /mcp/healthz</code> returning <code>{\"status\":\"ok\",\"protocol_version\":\"2025-11-25\",\"registered_modules\":&lt;int&gt;,\"registered_tools\":&lt;int&gt;}</code> for liveness checks.</li></ol>\n<ol><li><strong>MUST</strong> publish the federation gateway at <code>https://mcp.cyberos.com/mcp</code> (production); local dev at <code>http://localhost:8090/mcp</code>.</li></ol>\n<ol><li><strong>MUST</strong> support graceful shutdown: SIGTERM triggers (a) refuse new requests with HTTP 503, (b) drain in-flight requests with 10s budget, (c) close SSE streams with <code>Connection: close</code>, (d) shut down. The shutdown deadline budget is configurable via env <code>MCP_GATEWAY_SHUTDOWN_BUDGET_SECONDS</code>.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why pin spec version 2025-11-25 (§1 #1, DEC-260)?</strong> The MCP spec evolves; pinning a version makes the gateway's contract explicit. Clients negotiate via <code>protocolVersion</code> field; mismatches are explicit (<code>-32600</code> with <code>supported</code> array) rather than silent. Future versions get added to the supported set as deliberately as a Rust crate gets a new minor — never silently accepted.</p>\n<p><strong>Why Streamable HTTP transport only (§1 #3, DEC-261)?</strong> The 2025-11-25 spec deprecates the legacy HTTP+SSE pair (separate POST + GET endpoints). Streamable HTTP unifies them: POST for client→server (request/response), GET for server→client (SSE stream of notifications + samplings + elicitations). Implementing both would double the transport code; deprecated path is a maintenance liability. Modern clients support Streamable HTTP; legacy clients should upgrade.</p>\n<p><strong>Why JSON-RPC 2.0 wire shape (§1 #2)?</strong> It's the MCP-mandated wire protocol. Reusing the standard gives us a parser library + standard error codes; deviation would break every client.</p>\n<p><strong>Why federation strategy in-memory (§1 #18, DEC-262)?</strong> 22 module servers × hundreds of tools each = thousands of catalog entries. Each <code>tools/list</code> walking 22 module HTTP calls would be intolerably slow (~1s). In-memory cache, refreshed on TASK-MCP-002 registration events, gives &lt; 200ms <code>tools/list</code>. Cache rebuild happens on gateway start + on every module heartbeat. Stale-by-up-to-heartbeat-interval is acceptable.</p>\n<p><strong>Why module-server timeout 30s (§1 #17)?</strong> Tool calls have varied latency: simple lookups ~50ms, LLM-backed tools 5-15s, long-running tasks &gt; 30s (those use the Tasks primitive per TASK-MCP-007). 30s captures the medium-tail without making short failures wait forever; long-running flows use Tasks to decouple.</p>\n<p><strong>Why audit pair started+completed (§1 #13, DEC-265)?</strong> Per task-audit skill rule 26 — operators tracing crashes need both bookends. Started without completed = crash; the operator gets <code>tool_name</code>, <code>tenant_id</code>, <code>arguments_sha256</code> to investigate. Completed-only would hide pre-execution crashes (panic in argument parsing, etc.).</p>\n<p><strong>Why <code>arguments_sha256</code> not full arguments in memory row (§1 #13)?</strong> Tool arguments may carry PII (search query, customer name). Storing them raw in the audit chain would create everywhere-PII. The hash is sufficient for \"did this exact call happen?\" replay; forensic operations join via tenant_id + tool_name + ts.</p>\n<p><strong>Why scope check enforced at gateway (§1 #11, DEC-267)?</strong> Per defense in depth: the gateway IS the trust boundary for external agents. Even if the module server has its own auth, the gateway's check catches issues earlier (cheaper) and uniformly (one place to update scope rules). Tool-specific scope requirements are part of registration.</p>\n<p><strong>Why <code>Mcp-Session-Id</code> header for resumption (§1 #4)?</strong> Clients reconnect (network blip, browser refresh). Without session resumption, every reconnect re-runs <code>initialize</code>; not catastrophic but adds latency. Session id is a UUID; gateway maintains session metadata (tenant_id, persona) in-memory; expired sessions force re-init.</p>\n<p><strong>Why batch dispatch concurrent with per-tool isolation (§1 #9, DEC-263)?</strong> Batch requests benefit from parallelism (5 unrelated reads run in 100ms instead of 500ms). Per-tool isolation means one tool's failure doesn't fail the batch; each gets its own response slot. JSON-RPC mandates same-order response array — preserved.</p>\n<p><strong>Why -32001..-32005 error codes (§1 #8, DEC-272)?</strong> JSON-RPC reserves -32768..-32000 for protocol; the spec allocates the application-error range -32000..-32099 to MCP. Our usage stays within that allocation. Specific codes (-32001 unauthorized, -32002 rate_limited, -32003 tool_not_found, etc.) give clients actionable error categories without scraping message text.</p>\n<p><strong>Why capabilities response carefully bounded (§1 #5, DEC-266)?</strong> Advertising a capability commits the gateway to implementing the corresponding methods. Slice 4 ships tools + prompts + resources + logging; sampling and roots are deferred. Clients introspect the capabilities map and stop calling unsupported methods — false advertisement causes runtime failures.</p>\n<p><strong>Why per-(tenant, tool) rate limit not per-tenant (§1 #12, DEC-268)?</strong> Some tools are cheap (lookups), some expensive (LLM-backed). A tenant making 100 cheap calls/min is fine; the same tenant making 100 LLM calls/min may exceed budget. Per-tool granularity lets policy be tool-aware.</p>\n<p><strong>Why federation in-memory and not Redis (§1 #18)?</strong> The catalog is small (~1MB for 1000 tools); in-process access is &lt; 1µs; Redis adds a network hop. The gateway is horizontally scalable — each instance maintains its own catalog from registry events; eventual consistency across instances is acceptable (registration events propagate via TASK-MCP-002).</p>\n<p><strong>Why dispatch to internal module endpoints over HTTP not gRPC (§1 #16)?</strong> HTTP is the spec-mandated transport for MCP; module servers ARE MCP servers (per TASK-MCP-002). Using gRPC internally would force every module to implement both. HTTP is consistent and observable (standard logs, traces, metrics).</p>\n<p><strong>Why <code>tools/list</code> cursor pagination (§1 #6, §1 #15)?</strong> Catalogs &gt; 100 tools (any module with a rich CRUD surface) produce &gt; 64KB responses. Single-page response inflates latency + memory; cursor pagination keeps each page under 50KB. Opaque cursor lets the gateway change pagination scheme without breaking clients.</p>\n<p><strong>Why <code>tools/call</code> dispatch &lt; 50ms gateway overhead (§1 #22)?</strong> Module execution dominates; gateway overhead should be invisible. 50ms = JWT verify (5ms) + scope check (1ms) + audit emit (10ms) + outbound HTTP setup (15ms) + slack (19ms). Past 50ms, the gateway is the bottleneck.</p>\n<p><strong>Why state-less gateway (§1 #4, DEC-271)?</strong> Session resumption uses session id, not in-process session state; Tasks state lives in TASK-MCP-007's persistent store. State-less means horizontal scaling is trivial (any instance can serve any request); operational outages don't lose in-flight work.</p>\n<p><strong>Why server-pushed messages via SSE not WebSocket (§1 #3)?</strong> Streamable HTTP spec mandates SSE for server→client. WebSocket would be bidirectional but the spec deliberately uses SSE + HTTP POST to keep transport simple — separable channels, standard HTTP semantics for the request channel.</p>\n<p><strong>Why audit BEFORE dispatch + AFTER completion not just at-completion (DEC-265)?</strong> Crashes during dispatch (network failure, malformed result) would silently drop the call; the started row preserves the fact of attempt. Operators looking for \"what tools were attempted on the affected tenant?\" see all of them, not just completions.</p>\n<p><strong>Why exempt <code>initialize</code> from auth (§1 #11)?</strong> Clients need to discover capabilities before knowing what scopes to request. <code>initialize</code> returns server capabilities + protocol version; this lets the client (a) verify protocol match, (b) prepare an auth flow appropriate to the discovered capabilities. Subsequent methods require auth.</p>\n<p><strong>Why 100 calls/min default (§1 #12)?</strong> Standard interactive use is &lt; 60 calls/min (one call per second is fast). 100 gives margin; 200 burst handles short rushes. Tenants needing more provision via per-tenant override.</p>\n<p><strong>Why <code>mcp.tool_call_started/completed</code> not <code>mcp.tool_invoked</code>?</strong> Per task-audit skill rule 26: pair-write history events. A single <code>tool_invoked</code> row would hide crashes — started without completed signals crash; both rows present = clean execution.</p>\n<p><strong>Why <code>result_sha256</code> on completed row (§1 #13)?</strong> Replay verification: given a stored memory row + the original arguments, re-running the tool should produce a result whose SHA-256 matches. Mismatch = the underlying tool changed; useful for EU AI Act Art. 12 replay claims.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>3.1 — JSON-RPC request shapes</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/protocol/jsonrpc.rs\nuse serde::{Deserialize, Serialize};\nuse serde_json::Value;\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct JsonRpcRequest {\n    pub jsonrpc: String,                       // always \"2.0\"\n    pub method: String,                        // e.g. \"tools/list\"\n    #[serde(default)]\n    pub params: Option&lt;Value&gt;,\n    #[serde(default)]\n    pub id: Option&lt;Value&gt;,                     // null = notification (no response)\n}\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct JsonRpcResponse {\n    pub jsonrpc: String,                       // always \"2.0\"\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub result: Option&lt;Value&gt;,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub error: Option&lt;JsonRpcError&gt;,\n    pub id: Value,\n}\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct JsonRpcError {\n    pub code: i32,\n    pub message: String,\n    #[serde(skip_serializing_if = \"Option::is_none\")]\n    pub data: Option&lt;Value&gt;,\n}\n\n#[derive(Debug, Clone)]\npub enum JsonRpcPayload {\n    Single(JsonRpcRequest),\n    Batch(Vec&lt;JsonRpcRequest&gt;),\n}\n\npub fn parse_payload(bytes: &amp;[u8]) -&gt; Result&lt;JsonRpcPayload, JsonRpcError&gt; {\n    let v: Value = serde_json::from_slice(bytes).map_err(|_| JsonRpcError {\n        code: -32700, message: \"parse_error\".into(), data: None,\n    })?;\n    match v {\n        Value::Array(arr) =&gt; {\n            let mut batch = Vec::with_capacity(arr.len());\n            for item in arr {\n                let req: JsonRpcRequest = serde_json::from_value(item).map_err(|_| JsonRpcError {\n                    code: -32600, message: \"invalid_request\".into(), data: None,\n                })?;\n                batch.push(req);\n            }\n            Ok(JsonRpcPayload::Batch(batch))\n        }\n        _ =&gt; {\n            let req: JsonRpcRequest = serde_json::from_value(v).map_err(|_| JsonRpcError {\n                code: -32600, message: \"invalid_request\".into(), data: None,\n            })?;\n            Ok(JsonRpcPayload::Single(req))\n        }\n    }\n}</code></pre>\n<h3>3.2 — Initialize handler</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/protocol/initialize.rs\nuse serde::{Deserialize, Serialize};\nuse serde_json::json;\nuse crate::protocol::capabilities::ServerCapabilities;\n\npub const MCP_PROTOCOL_VERSION: &amp;str = \"2025-11-25\";\n\n#[derive(Deserialize)]\npub struct InitializeParams {\n    #[serde(rename = \"protocolVersion\")]\n    pub protocol_version: String,\n    #[serde(rename = \"clientInfo\")]\n    pub client_info: ClientInfo,\n    #[serde(default)]\n    pub capabilities: serde_json::Value,\n}\n\n#[derive(Deserialize)]\npub struct ClientInfo { pub name: String, pub version: String }\n\n#[derive(Serialize)]\npub struct InitializeResult {\n    #[serde(rename = \"protocolVersion\")]\n    pub protocol_version: String,\n    pub capabilities: ServerCapabilities,\n    #[serde(rename = \"serverInfo\")]\n    pub server_info: ServerInfo,\n    pub instructions: String,\n}\n\n#[derive(Serialize)]\npub struct ServerInfo { pub name: String, pub version: String, pub title: String }\n\npub fn handle_initialize(params: InitializeParams) -&gt; Result&lt;InitializeResult, JsonRpcError&gt; {\n    if params.protocol_version != MCP_PROTOCOL_VERSION {\n        return Err(JsonRpcError {\n            code: -32600,\n            message: \"protocol_version_mismatch\".into(),\n            data: Some(json!({\"supported\": [MCP_PROTOCOL_VERSION]})),\n        });\n    }\n    Ok(InitializeResult {\n        protocol_version: MCP_PROTOCOL_VERSION.into(),\n        capabilities: ServerCapabilities::current(),\n        server_info: ServerInfo {\n            name: \"cyberos.mcp-gateway\".into(),\n            version: env!(\"CARGO_PKG_VERSION\").into(),\n            title: \"CyberOS MCP Gateway\".into(),\n        },\n        instructions: \"Federation of 22 CyberOS modules. All calls audit-chained. OAuth 2.1 PKCE auth via TASK-MCP-004.\".into(),\n    })\n}</code></pre>\n<h3>3.3 — Capabilities</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/protocol/capabilities.rs\nuse serde::Serialize;\nuse std::collections::HashMap;\n\n#[derive(Serialize)]\npub struct ServerCapabilities {\n    pub tools: ToolsCap,\n    pub prompts: ListChangedCap,\n    pub resources: ResourcesCap,\n    pub logging: HashMap&lt;String, serde_json::Value&gt;,    // empty obj\n}\n\n#[derive(Serialize)]\npub struct ToolsCap { #[serde(rename = \"listChanged\")] pub list_changed: bool }\n\n#[derive(Serialize)]\npub struct ListChangedCap { #[serde(rename = \"listChanged\")] pub list_changed: bool }\n\n#[derive(Serialize)]\npub struct ResourcesCap {\n    #[serde(rename = \"listChanged\")] pub list_changed: bool,\n    pub subscribe: bool,\n}\n\nimpl ServerCapabilities {\n    pub fn current() -&gt; Self {\n        Self {\n            tools: ToolsCap { list_changed: true },\n            prompts: ListChangedCap { list_changed: true },\n            resources: ResourcesCap { list_changed: true, subscribe: true },\n            logging: HashMap::new(),\n        }\n    }\n}</code></pre>\n<h3>3.4 — tools/list</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/protocol/tools_list.rs\nuse serde::{Deserialize, Serialize};\nuse crate::federation::registry::TOOL_REGISTRY;\nuse crate::annotations::ToolAnnotations;\n\n#[derive(Deserialize)]\npub struct ToolsListParams {\n    #[serde(default)]\n    pub cursor: Option&lt;String&gt;,\n}\n\n#[derive(Serialize)]\npub struct ToolsListResult {\n    pub tools: Vec&lt;ToolDescriptor&gt;,\n    #[serde(skip_serializing_if = \"Option::is_none\", rename = \"nextCursor\")]\n    pub next_cursor: Option&lt;String&gt;,\n}\n\n#[derive(Serialize)]\npub struct ToolDescriptor {\n    pub name: String,\n    pub description: String,\n    #[serde(rename = \"inputSchema\")]\n    pub input_schema: serde_json::Value,\n    pub annotations: ToolAnnotations,\n}\n\nconst PAGE_SIZE: usize = 100;\n\npub fn handle_tools_list(params: ToolsListParams, tenant_id: uuid::Uuid) -&gt; ToolsListResult {\n    let reg = TOOL_REGISTRY.load();\n    let tools_for_tenant = reg.visible_to_tenant(tenant_id);\n\n    let offset = params.cursor.as_deref()\n        .and_then(|c| base64::decode(c).ok())\n        .and_then(|b| std::str::from_utf8(&amp;b).ok().and_then(|s| s.parse::&lt;usize&gt;().ok()))\n        .unwrap_or(0);\n\n    let slice = tools_for_tenant.iter().skip(offset).take(PAGE_SIZE);\n    let descriptors: Vec&lt;ToolDescriptor&gt; = slice.map(|t| t.to_descriptor()).collect();\n    let next_cursor = if offset + descriptors.len() &lt; tools_for_tenant.len() {\n        Some(base64::encode((offset + descriptors.len()).to_string()))\n    } else { None };\n\n    ToolsListResult { tools: descriptors, next_cursor }\n}</code></pre>\n<h3>3.5 — tools/call dispatch</h3>\n<pre><code class=\"language-rust\">// services/mcp-gateway/src/protocol/tools_call.rs\nuse serde::{Deserialize, Serialize};\nuse sha2::{Digest, Sha256};\nuse crate::federation::{registry::TOOL_REGISTRY, dispatch::dispatch_to_module};\nuse crate::audit::mcp_events;\nuse crate::ratelimit::check_rate_limit;\nuse crate::auth::Claims;\n\n#[derive(Deserialize)]\npub struct ToolsCallParams {\n    pub name: String,\n    #[serde(default)]\n    pub arguments: serde_json::Value,\n}\n\n#[derive(Serialize)]\npub struct ToolsCallResult {\n    pub content: Vec&lt;ContentBlock&gt;,\n    #[serde(rename = \"isError\")]\n    pub is_error: bool,\n    #[serde(skip_serializing_if = \"Option::is_none\", rename = \"structuredContent\")]\n    pub structured_content: Option&lt;serde_json::Value&gt;,\n}\n\n#[derive(Serialize)]\n#[serde(tag = \"type\", rename_all = \"snake_case\")]\npub enum ContentBlock {\n    Text { text: String },\n    Image { data: String, mime_type: String },\n    Resource { resource: serde_json::Value },\n}\n\npub async fn handle_tools_call(\n    params: ToolsCallParams,\n    claims: &amp;Claims,\n    request_id: &amp;str,\n) -&gt; Result&lt;ToolsCallResult, JsonRpcError&gt; {\n    let reg = TOOL_REGISTRY.load();\n    let tool = reg.get(&amp;params.name).ok_or_else(|| JsonRpcError {\n        code: -32003, message: \"tool_not_found\".into(),\n        data: Some(serde_json::json!({\"name\": params.name})),\n    })?;\n\n    // Scope check\n    for required in &amp;tool.required_scopes {\n        if !claims.scopes().contains(required) {\n            return Err(JsonRpcError {\n                code: -32001, message: \"unauthorized\".into(),\n                data: Some(serde_json::json!({\"reason\":\"insufficient_scope\",\"required_scopes\": tool.required_scopes})),\n            });\n        }\n    }\n\n    // Rate limit\n    if !check_rate_limit(claims.tenant_id(), &amp;params.name).await {\n        return Err(JsonRpcError {\n            code: -32002, message: \"rate_limited\".into(),\n            data: Some(serde_json::json!({\"retry_after_ms\": 60_000})),\n        });\n    }\n\n    // Destructive-op gate (TASK-MCP-006 ships full Elicitation; slice 4 stub)\n    if tool.annotations.destructive_hint\n       &amp;&amp; claims.elicitation_confirmed_for(&amp;params.name).is_none() {\n        return Err(JsonRpcError {\n            code: -32005, message: \"elicitation_required\".into(),\n            data: Some(serde_json::json!({\"tool\": params.name})),\n        });\n    }\n\n    // Audit: started\n    let args_sha = hex::encode(Sha256::digest(serde_json::to_vec(&amp;params.arguments).unwrap()));\n    mcp_events::emit_tool_call_started(claims, &amp;params.name, &amp;args_sha, request_id).await;\n\n    // Dispatch\n    let t0 = std::time::Instant::now();\n    let result = dispatch_to_module(&amp;tool.endpoint, &amp;params.name, &amp;params.arguments).await;\n    let duration_ms = t0.elapsed().as_millis() as u64;\n\n    let (outcome, response) = match result {\n        Ok(r) =&gt; (\"success\", r),\n        Err(DispatchError::Timeout) =&gt; {\n            mcp_events::emit_tool_call_completed(claims, &amp;params.name, \"timeout\", duration_ms, None, request_id).await;\n            return Err(JsonRpcError {\n                code: -32004, message: \"module_unreachable\".into(),\n                data: Some(serde_json::json!({\"reason\":\"timeout\",\"module\": tool.module})),\n            });\n        }\n        Err(e) =&gt; {\n            mcp_events::emit_tool_call_completed(claims, &amp;params.name, \"module_error\", duration_ms, None, request_id).await;\n            return Err(JsonRpcError {\n                code: -32004, message: \"module_unreachable\".into(),\n                data: Some(serde_json::json!({\"reason\": format!(\"{e:?}\"),\"module\": tool.module})),\n            });\n        }\n    };\n\n    let result_sha = hex::encode(Sha256::digest(serde_json::to_vec(&amp;response).unwrap()));\n    mcp_events::emit_tool_call_completed(claims, &amp;params.name, outcome, duration_ms, Some(&amp;result_sha), request_id).await;\n    Ok(response)\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Initialize success</strong> — client sends <code>initialize</code> with <code>protocolVersion: \"2025-11-25\"</code> → 200 with capabilities + serverInfo.</li><li><strong>Initialize version mismatch</strong> — <code>protocolVersion: \"2024-01-01\"</code> → -32600 with <code>supported: [\"2025-11-25\"]</code>.</li><li><strong>Capabilities shape</strong> — response contains <code>tools</code>, <code>prompts</code>, <code>resources</code>, <code>logging</code>; no <code>sampling</code> or <code>roots</code>.</li><li><strong>JSON-RPC parse error</strong> — malformed JSON body → -32700 <code>parse_error</code>.</li><li><strong>JSON-RPC invalid request</strong> — missing <code>method</code> field → -32600 <code>invalid_request</code>.</li><li><strong>Method not found</strong> — <code>notifications/unknown_method</code> → -32601.</li><li><strong>Invalid params</strong> — <code>tools/call</code> with non-object params → -32602.</li><li><strong>Batch request</strong> — array of 3 valid requests → array of 3 responses in same order.</li><li><strong>Notification in batch</strong> — request with no <code>id</code> field → omitted from response.</li><li><strong>JWT missing on tools/call</strong> → -32001 with <code>reason: \"token_invalid\"</code>.</li><li><strong>JWT invalid signature</strong> → -32001 with <code>reason: \"token_invalid\"</code>.</li><li><strong>JWT audience mismatch</strong> → -32001 with <code>reason: \"aud_mismatch\"</code>.</li><li><strong>JWT scope missing</strong> → -32001 with <code>required_scopes</code> array.</li><li><strong>Initialize exempt from JWT</strong> — no JWT → <code>initialize</code> succeeds.</li><li><strong>tools/list cursor pagination</strong> — 250-tool catalog → first page 100 + next_cursor; second page 100 + next_cursor; third page 50 + no next_cursor.</li><li><strong>tools/list per-tenant filtering</strong> — tools requiring tenant-specific scope only appear when caller has that scope.</li><li><strong>tools/call happy path</strong> — valid name + arguments → dispatched to module; result returned with content array.</li><li><strong>tools/call tool not found</strong> → -32003.</li><li><strong>tools/call module unreachable</strong> — module returns 5xx within 30s → -32004.</li><li><strong>tools/call module timeout</strong> — module takes &gt; 30s → -32004 with <code>reason: \"timeout\"</code>.</li><li><strong>tools/call destructive without elicitation</strong> → -32005 <code>elicitation_required</code>.</li><li><strong>Rate limit hit</strong> — 101st call in 1 min for (tenant, tool) → -32002 with <code>retry_after_ms</code>.</li><li><strong>memory audit pair emitted</strong> — every tools/call writes both started + completed rows; started has <code>arguments_sha256</code>, completed has <code>outcome</code> + <code>duration_ms</code> + <code>result_sha256</code> (on success).</li><li><strong>W3C traceparent propagated</strong> — outbound dispatch carries the same <code>traceparent</code> as inbound.</li><li><strong>Mcp-Session-Id header set on initialize response</strong> — UUID format.</li><li><strong>Session re-init on expired Mcp-Session-Id</strong> — gateway returns 404; client re-initialises.</li><li><strong>Initialize p95 &lt; 100 ms</strong> — <code>initialize_perf_test</code>.</li><li><strong>tools/list p95 &lt; 200 ms</strong> — <code>tools_list_perf_test</code>.</li><li><strong>tools/call gateway-side p95 &lt; 50 ms</strong> — excluding module execution; <code>tools_call_perf_test</code>.</li><li><strong>GET /mcp/healthz</strong> — returns 200 with protocol_version + counts.</li><li><strong>Tool annotations present in tools/list</strong> — every tool descriptor has <code>annotations: {title, readOnlyHint, destructiveHint, idempotentHint, openWorldHint}</code>.</li><li><strong>OTel span emitted per request</strong> — <code>mcp.gateway.{initialize,tools_list,tools_call}</code> with <code>outcome</code> attribute.</li><li><strong>OTel counter <code>mcp_gateway_request_total{method=tools_call, outcome=success}</code> increments</strong> — per call.</li><li><strong>Graceful shutdown</strong> — SIGTERM → /healthz returns 503; in-flight requests drained ≤ 10 s; new requests rejected.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">// services/mcp-gateway/tests/initialize_test.rs\n#[tokio::test]\nasync fn initialize_returns_protocol_version() {\n    let gw = TestGateway::new().await;\n    let resp = gw.rpc(json!({\n        \"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"id\":1,\n        \"params\":{\"protocolVersion\":\"2025-11-25\",\"clientInfo\":{\"name\":\"test\",\"version\":\"1\"}}\n    })).await;\n    assert_eq!(resp[\"result\"][\"protocolVersion\"], \"2025-11-25\");\n    assert!(resp[\"result\"][\"capabilities\"][\"tools\"][\"listChanged\"].as_bool().unwrap());\n}\n\n#[tokio::test]\nasync fn initialize_version_mismatch_returns_supported_list() {\n    let gw = TestGateway::new().await;\n    let resp = gw.rpc(json!({\n        \"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"id\":1,\n        \"params\":{\"protocolVersion\":\"2024-01-01\",\"clientInfo\":{\"name\":\"test\",\"version\":\"1\"}}\n    })).await;\n    assert_eq!(resp[\"error\"][\"code\"], -32600);\n    assert_eq!(resp[\"error\"][\"data\"][\"supported\"][0], \"2025-11-25\");\n}</code></pre>\n<pre><code class=\"language-rust\">// services/mcp-gateway/tests/jsonrpc_batch_test.rs\n#[tokio::test]\nasync fn batch_request_returns_ordered_responses() {\n    let gw = TestGateway::new().await;\n    let batch = json!([\n        {\"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"id\":1,\"params\":{\"protocolVersion\":\"2025-11-25\",\"clientInfo\":{\"name\":\"a\",\"version\":\"1\"}}},\n        {\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"id\":2},\n        {\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"id\":3,\"params\":{\"cursor\":\"&lt;bad&gt;\"}}\n    ]);\n    let resp = gw.rpc_raw(batch).await;\n    let arr = resp.as_array().unwrap();\n    assert_eq!(arr.len(), 3);\n    assert_eq!(arr[0][\"id\"], 1);\n    assert_eq!(arr[1][\"id\"], 2);\n    assert_eq!(arr[2][\"id\"], 3);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/mcp-gateway/tests/audit_emission_test.rs\n#[tokio::test]\nasync fn tool_call_emits_started_and_completed_rows() {\n    let gw = TestGateway::with_module_mock(\"memory\", \"search_memory\", json!({\"hits\": []})).await;\n    let resp = gw.tools_call(\"cyberos.memory.search_memory\", json!({\"query\":\"x\"})).await;\n    let rows = gw.memory_audit_rows().await;\n    let started = rows.iter().find(|r| r[\"kind\"] == \"mcp.tool_call_started\").unwrap();\n    let completed = rows.iter().find(|r| r[\"kind\"] == \"mcp.tool_call_completed\").unwrap();\n    assert_eq!(started[\"tool_name\"], \"cyberos.memory.search_memory\");\n    assert!(started[\"arguments_sha256\"].is_string());\n    assert_eq!(completed[\"outcome\"], \"success\");\n    assert!(completed[\"result_sha256\"].is_string());\n    assert!(completed[\"duration_ms\"].as_u64().unwrap() &lt; 5_000);\n}</code></pre>\n<pre><code class=\"language-rust\">// services/mcp-gateway/tests/rate_limit_test.rs\n#[tokio::test]\nasync fn 101st_call_returns_rate_limited() {\n    let gw = TestGateway::with_module_mock(\"memory\", \"search_memory\", json!({\"hits\": []})).await;\n    for _ in 0..100 {\n        let resp = gw.tools_call(\"cyberos.memory.search_memory\", json!({\"query\":\"x\"})).await;\n        assert!(resp[\"result\"].is_object());\n    }\n    let over = gw.tools_call(\"cyberos.memory.search_memory\", json!({\"query\":\"x\"})).await;\n    assert_eq!(over[\"error\"][\"code\"], -32002);\n    assert!(over[\"error\"][\"data\"][\"retry_after_ms\"].as_u64().unwrap() &gt; 0);\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton; the federation registry struct, dispatch HTTP client, and SSE writer follow standard axum + tokio patterns.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong></p>\n<ul><li><strong>TASK-AUTH-004</strong> — JWT issuance + JWKS; this task verifies tokens against the JWKS.</li></ul>\n<p><strong>Downstream (5 placeholders):</strong></p>\n<ul><li><strong>TASK-MCP-002</strong> — per-module server registration + heartbeat; populates the federated registry.</li><li><strong>TASK-MCP-003</strong> — SEP-986 naming convention validator (<code>cyberos.&lt;module&gt;.&lt;verb&gt;_&lt;noun&gt;</code>).</li><li><strong>TASK-MCP-006</strong> — tool-annotation gating (destructive requires confirm or Elicitation).</li><li><strong>TASK-MCP-007</strong> — Tasks primitive (long-running work).</li><li><strong>TASK-MCP-008</strong> — Elicitation server-initiated request/response.</li></ul>\n<p><strong>Cross-module:</strong></p>\n<ul><li><strong>TASK-AUTH-101</strong> — RBAC; <code>Resource::McpTool + Action::Invoke</code> patterns; agent-persona role on every call.</li><li><strong>TASK-AI-003</strong> — memory audit bridge; receives <code>mcp.tool_call_started</code> + <code>mcp.tool_call_completed</code>.</li><li><strong>TASK-AI-022</strong> — OTel trace emission; traceparent propagation.</li><li><strong>TASK-MCP-004</strong> — OAuth 2.1 PKCE (this task consumes JWT shape; TASK-MCP-004 ships the issuance flow).</li><li><strong>TASK-MCP-005</strong> — Protected Resource Metadata; published at /.well-known/oauth-protected-resource.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>8.1 — initialize request</h3>\n<pre><code class=\"language-json\">{\n  \"jsonrpc\": \"2.0\",\n  \"method\": \"initialize\",\n  \"id\": 1,\n  \"params\": {\n    \"protocolVersion\": \"2025-11-25\",\n    \"clientInfo\": {\"name\": \"claude-desktop\", \"version\": \"0.7.0\"},\n    \"capabilities\": {}\n  }\n}</code></pre>\n<h3>8.2 — initialize response</h3>\n<pre><code class=\"language-json\">{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 1,\n  \"result\": {\n    \"protocolVersion\": \"2025-11-25\",\n    \"capabilities\": {\n      \"tools\": {\"listChanged\": true},\n      \"prompts\": {\"listChanged\": true},\n      \"resources\": {\"listChanged\": true, \"subscribe\": true},\n      \"logging\": {}\n    },\n    \"serverInfo\": {\"name\": \"cyberos.mcp-gateway\", \"version\": \"0.1.0\", \"title\": \"CyberOS MCP Gateway\"},\n    \"instructions\": \"Federation of 22 CyberOS modules. All calls audit-chained. OAuth 2.1 PKCE auth via TASK-MCP-004.\"\n  }\n}</code></pre>\n<h3>8.3 — tools/list response (excerpt)</h3>\n<pre><code class=\"language-json\">{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 2,\n  \"result\": {\n    \"tools\": [\n      {\n        \"name\": \"cyberos.memory.search_memory\",\n        \"description\": \"Search memory audit-chained memories by query string.\",\n        \"inputSchema\": {\"type\":\"object\",\"properties\":{\"query\":{\"type\":\"string\"}},\"required\":[\"query\"]},\n        \"annotations\": {\n          \"title\": \"Search memory\",\n          \"readOnlyHint\": true,\n          \"destructiveHint\": false,\n          \"idempotentHint\": true,\n          \"openWorldHint\": false\n        }\n      }\n    ],\n    \"nextCursor\": \"MTAw\"\n  }\n}</code></pre>\n<h3>8.4 — tools/call request</h3>\n<pre><code class=\"language-json\">{\n  \"jsonrpc\": \"2.0\",\n  \"method\": \"tools/call\",\n  \"id\": 3,\n  \"params\": {\n    \"name\": \"cyberos.memory.search_memory\",\n    \"arguments\": {\"query\": \"TASK-AUTH-101\"}\n  }\n}</code></pre>\n<h3>8.5 — tools/call response</h3>\n<pre><code class=\"language-json\">{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 3,\n  \"result\": {\n    \"content\": [\n      {\"type\": \"text\", \"text\": \"Found 3 memories matching 'TASK-AUTH-101'...\"}\n    ],\n    \"isError\": false,\n    \"structuredContent\": {\"hits\": [{\"id\": \"...\", \"score\": 0.92}]}\n  }\n}</code></pre>\n<h3>8.6 — mcp.tool_call_started memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"mcp.tool_call_started\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"tool_name\": \"cyberos.memory.search_memory\",\n  \"arguments_sha256\": \"b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9\",\n  \"persona_version\": \"cuo-cpo@0.4.1\",\n  \"request_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"trace_id\": \"4bf92f3577b34da6a3ce929d0e0e4736\",\n  \"ts_ns\": 1747920731000000000\n}</code></pre>\n<h3>8.7 — mcp.tool_call_completed memory row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"mcp.tool_call_completed\",\n  \"tenant_id\": \"5e8f1d2a-...\",\n  \"subject_id_hash16\": \"9b1deb4d3b7d4bad\",\n  \"tool_name\": \"cyberos.memory.search_memory\",\n  \"outcome\": \"success\",\n  \"duration_ms\": 47,\n  \"result_sha256\": \"a47c8e0c5f8a8e8e8c5f8a8e8e8c5f8a8e8e8c5f8a8e8e8c5f8a8e8e8c5f8a8e\",\n  \"request_id\": \"01HG7V8B0K8M4Z8Z8M8M8M8M8M\",\n  \"ts_ns\": 1747920731047000000\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>OAuth 2.1 PKCE flow</strong> — TASK-MCP-004 ships the issuance; this task consumes the JWT.</li><li><strong>Protected Resource Metadata (RFC 9728)</strong> — TASK-MCP-005 publishes at <code>.well-known/oauth-protected-resource</code>.</li><li><strong>Destructive-op Elicitation flow</strong> — TASK-MCP-006 ships the full flow; this task stubs with <code>-32005 elicitation_required</code>.</li><li><strong>Tasks primitive</strong> — TASK-MCP-007.</li><li><strong>Elicitation primitive</strong> — TASK-MCP-008.</li><li><strong>Sampling capability</strong> — slice 5+.</li><li><strong>Roots capability</strong> — slice 5+.</li><li><strong>Per-tenant rate-limit override</strong> — task-MCP-2xx.</li></ul>\n<p>All other questions resolved.</p>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Malformed JSON request</td><td><code>serde_json</code> parse</td><td>-32700 parse_error</td><td>None — designed</td></tr><tr><td>Missing <code>method</code> field</td><td>request shape validation</td><td>-32600 invalid_request</td><td>None — designed</td></tr><tr><td>Unknown method</td><td>router lookup</td><td>-32601 method_not_found</td><td>Implement method or upgrade client</td></tr><tr><td><code>tools/call</code> with non-object params</td><td>params parse</td><td>-32602 invalid_params</td><td>Fix client</td></tr><tr><td>protocolVersion mismatch</td><td>initialize</td><td>-32600 with supported list</td><td>Client upgrades protocol</td></tr><tr><td>JWT missing</td><td>auth middleware</td><td>-32001 token_invalid</td><td>Provide token</td></tr><tr><td>JWT signature invalid</td><td>JWKS verify</td><td>-32001 token_invalid</td><td>Re-auth</td></tr><tr><td>JWT expired</td><td>exp claim</td><td>-32001 token_invalid</td><td>Refresh token</td></tr><tr><td>JWT aud mismatch</td><td>aud check</td><td>-32001 aud_mismatch</td><td>Use correct audience</td></tr><tr><td>JWT scope missing</td><td>scope check</td><td>-32001 insufficient_scope + required_scopes</td><td>Request broader scope</td></tr><tr><td>Tool not found in registry</td><td>registry lookup</td><td>-32003 tool_not_found</td><td>Client lists tools</td></tr><tr><td>Module endpoint unreachable</td><td>HTTP connect error</td><td>-32004 module_unreachable</td><td>Operator investigates module</td></tr><tr><td>Module timeout &gt; 30s</td><td>tokio timeout</td><td>-32004 with reason=timeout</td><td>None — designed</td></tr><tr><td>Module returns 5xx</td><td>HTTP response</td><td>-32004 with reason=module_error</td><td>None — designed</td></tr><tr><td>Rate limit exceeded</td><td>sliding window</td><td>-32002 with retry_after_ms</td><td>Wait + retry</td></tr><tr><td>Destructive tool without Elicitation-Confirmed</td><td>annotations + header check</td><td>-32005 elicitation_required</td><td>Use TASK-MCP-006 flow</td></tr><tr><td>Audit row commit fails</td><td>memory_writer error</td><td>500 internal; tools/call fails</td><td>memory_writer health check</td></tr><tr><td>Batch with empty array</td><td>parse</td><td>-32600 invalid_request</td><td>Send non-empty</td></tr><tr><td>Notification dispatched twice</td><td>id-absence check</td><td>second invocation no-op</td><td>None — designed</td></tr><tr><td>SSE stream disconnected</td><td>client close</td><td>server drops stream gracefully</td><td>Client re-opens</td></tr><tr><td>Mcp-Session-Id expired</td><td>session map lookup</td><td>HTTP 404</td><td>Client re-initialises</td></tr><tr><td>Federation registry empty (no modules registered)</td><td>health check</td><td>tools/list returns empty</td><td>TASK-MCP-002 registration recovery</td></tr><tr><td>Module heartbeat missed</td><td>TASK-MCP-002 lifecycle</td><td>tools removed from registry; tools/list updates</td><td>Module recovers and re-registers</td></tr><tr><td>traceparent malformed</td><td>parse</td><td>regenerate at trust boundary per task-audit skill rule 22</td><td>None — designed</td></tr><tr><td>Mcp-Session-Id format invalid</td><td>UUID parse</td><td>400 invalid header</td><td>Caller fixes</td></tr><tr><td>Tool descriptor missing annotations</td><td>tool_annotations_test</td><td>CI fails</td><td>Fix tool registration</td></tr><tr><td>Streamable HTTP unsupported by client</td><td>content-type negotiation</td><td>415 unsupported_media_type</td><td>Client upgrades</td></tr><tr><td>Capabilities response wrong shape</td><td>spec_conformance_test</td><td>CI fails</td><td>Fix struct</td></tr><tr><td>Result content array empty</td><td>tool returns; gateway preserves</td><td>content: [] allowed</td><td>None — designed</td></tr><tr><td><code>result_sha256</code> mismatch on replay</td><td>analyst tooling</td><td>sev-3 alarm</td><td>Replay analysis</td></tr><tr><td>OTel span propagation broken</td><td>trace_id test</td><td>CI fails</td><td>Fix instrumentation</td></tr><tr><td>Concurrent batch dispatch overflow</td><td>tokio task limit</td><td>503 server_busy</td><td>Back-off</td></tr><tr><td>Graceful shutdown drain timeout &gt; 10s</td><td>shutdown handler force-aborts</td><td>in-flight requests fail with 503</td><td>Designed — operator monitors</td></tr><tr><td>Module returns malformed JSON</td><td>dispatch parse</td><td>-32004 reason=module_invalid_json</td><td>Module-side investigation</td></tr><tr><td>Tool argument &gt; 1MB</td><td>request body limit</td><td>413 payload_too_large</td><td>Caller chunks</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>Spec version pinning at 2025-11-25</strong> is the contract. Future versions add to <code>supported[]</code>; never silently accept.</li><li><strong>Streamable HTTP only</strong> — legacy HTTP+SSE is explicitly NOT implemented. Modern clients support; legacy clients upgrade.</li><li><strong>JSON-RPC 2.0 wire shape</strong> is non-negotiable; matches the spec exactly.</li><li><strong>Batch dispatch concurrent + per-tool isolated</strong> — one tool's failure doesn't fail the batch.</li><li><strong><code>Mcp-Session-Id</code> UUID per session</strong> — gateway-allocated; client preserves across requests; gateway expires after configurable TTL.</li><li><strong>JWT verified at gateway</strong> — defense in depth even though module servers may have their own auth.</li><li><strong>Audit rows are PAIR</strong> — <code>started</code> + <code>completed</code> per task-audit skill rule 26; operators tracing crashes need both.</li><li><strong><code>arguments_sha256</code> not raw args in memory</strong> — PII protection; tool-side handlers can write the args if appropriate.</li><li><strong><code>result_sha256</code> enables EU AI Act Art. 12 replay</strong> — re-running with stored args should produce matching hash.</li><li><strong><code>tools/list</code> cursor pagination</strong> — opaque base64-encoded offset; gateway can change scheme without breaking clients.</li><li><strong>Per-(tenant, tool) rate limit sliding window</strong> — Redis-backed in production (out of scope for slice 4; in-memory at slice 4 acceptable per spec scope).</li><li><strong>Federation registry in-memory</strong> — &lt; 1MB; refreshed on TASK-MCP-002 registration events; eventual consistency across gateway instances.</li><li><strong>Dispatch HTTP client reuses connection pool</strong> — reqwest with <code>pool_max_idle_per_host = 32</code>.</li><li><strong>Tool annotations are spec-defined</strong> (<code>title</code>, <code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code>) — exposed in <code>tools/list</code>; internal annotations (<code>required_scopes</code>, <code>required_persona</code>, <code>module</code>, <code>endpoint</code>) are NOT exposed.</li><li><strong><code>logging</code> capability is empty object</strong> — declares support without specific config; task-MCP-2xx ships log-level subscription.</li><li><strong><code>prompts</code> + <code>resources</code> capability flagged</strong> — Prompts and Resources methods are not implemented at slice 4 (deferred); the capability flag signals support; calling them returns -32601.</li><li><strong>Server graceful shutdown drains 10s</strong> — configurable via env; tokio drop graceful.</li><li><strong>GET /mcp/healthz</strong> is unauthenticated — liveness probe target for k8s.</li><li><strong>Audience-bound JWT</strong> — <code>aud: https://mcp.cyberos.com</code> per TASK-MCP-004; gateway rejects tokens for other audiences.</li><li><strong>Spec conformance test runs against modelcontextprotocol/inspector</strong> — full reference client validates the gateway end-to-end at CI.</li><li><strong>JSON-RPC errors -32001..-32099</strong> are MCP-specific (per spec); custom codes outside that range = bug.</li><li><strong><code>elicitation_required</code> -32005 is a stub at slice 4</strong> — TASK-MCP-006 ships the full flow with the <code>Elicitation-Confirmed</code> header check + back-and-forth.</li><li><strong><code>Mcp-Session-Id</code> allows re-init on expiry</strong> — client treats HTTP 404 on a request with valid id as \"re-initialise\"; not a hard error.</li><li><strong>Module endpoint discovered via TASK-MCP-002 registration</strong> — at slice 4 this task uses a static map for tests; production lookup via TASK-MCP-002.</li><li><strong>No persistence in this task</strong> — sessions are in-memory; rate-limit windows in-memory; registry in-memory. Persistence (Tasks store, session re-distribution across instances) lands in later tasks.</li></ul>\n<hr>\n<p><em>End of TASK-MCP-001.</em></p>";
