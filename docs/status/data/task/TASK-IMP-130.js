window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-130"]="<h1>TASK-IMP-130: Rename the public CyberOS CLI bin from <code>cyberos</code> to <code>cs</code></h1>\n<h2>Summary</h2>\n<p>The npm-published CyberOS CLI exposes itself as the bin command <code>cyberos</code>, which collides on <code>$PATH</code> with an unrelated internal tool that happens to already claim that name. This task renames the public bin to <code>cs</code>, updates every place the CLI describes its own invocation, and folds in a stale-domain fix discovered in the same files.</p>\n<h2>Problem</h2>\n<p><code>brew install --cask cyberos</code> followed by <code>brew trust</code> installs the Homebrew-packaged CLI correctly, but running <code>cyberos -h</code> afterward showed an unrelated 39-subcommand BRAIN-store CLI instead of the Homebrew-installed one. Root cause, confirmed by direct repo inspection (<code>docs/plans/PLAN-cli-module-namespacing-2026-07-22/plan.md</code> §2): the npm-published <code>@cyberskill/cyberos</code> package (<code>tools/install/build.sh:341-347</code>) declares its bin as <code>cyberos</code> (<code>tools/install/cli/bin/cli.mjs</code>), and a completely separate, PyPI-unpublished internal package (<code>modules/memory</code>, <code>cyberos-memory</code>) independently declares a console-script entry also named <code>cyberos</code> (<code>modules/memory/pyproject.toml:29</code>). Whichever one lands later on <code>$PATH</code> (in the reported case, a local/dev pyenv install of <code>modules/memory</code>) wins, and there is no error — just the wrong tool answering to the name, silently.</p>\n<p><code>cyberos-memory</code> is confirmed internal-only with no plan to publish (plan §2), so it was never actually the thing that needed to change to solve the founder's goal, which is \"the only public command is <code>cs</code>\" (plan §4). Renaming the public bin directly delivers that and needs no coordination with the internal package's own naming.</p>\n<h2>Proposed Solution</h2>\n<p>Change the npm package's <code>bin</code> field (generated by <code>build.sh:341-347</code>) from <code>{ \"cyberos\": \"cli/bin/cli.mjs\" }</code> to <code>{ \"cs\": \"cli/bin/cli.mjs\" }</code>, keeping the package name <code>@cyberskill/cyberos</code> unchanged (see Alternatives Considered). Update every place the CLI's own text describes its invocation — <code>cli.mjs</code>'s top comment and <code>usage()</code> output, <code>help.sh</code>'s \"Channels\" section, the plugin's <code>help.md</code>, <code>docs/index.md</code>'s \"same eight commands\" line, and the <code>npx cyberos ...</code> examples in <code>tools/install/README.md</code> — to read <code>cs</code> instead of <code>cyberos</code>. Add a CHANGELOG entry calling out the rename as breaking, per the plan's explicit instruction not to ship it as a silent swap (plan §7). While editing <code>cli.mjs</code>, <code>help.sh</code>, and <code>help.md</code> for the rename, also correct the stale domain string <code>https://cyberos.cyberskill.world/docs</code> found in all three to the canonical <code>https://os.cyberskill.world/docs</code> already in use elsewhere in the repo (e.g. <code>README.md:7</code>) since PR #107.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Rename the npm package name (<code>@cyberskill/cyberos</code> → <code>@cyberskill/cs</code> or similar) along with the bin. Rejected: npm allows a package's name to differ from the command(s) it installs (many published CLIs do this), and the package name is what appears in <code>npm install &lt;name&gt;</code> / the registry URL / <code>repository</code>/<code>homepage</code> metadata already tied to the OIDC trusted-publishing pipeline (plan §7 flags that pipeline as fragile — pinned to org+repo+workflow-filename trust). Renaming the package name changes more surface for zero user-facing benefit, since the thing a user types day to day is the bin command, not the package name.</li><li>Keep <code>cyberos</code> as an alias alongside <code>cs</code> during a transition window. Rejected per the plan (§7): the founder explicitly rejected keeping <code>cyberos</code> as an alias, since an alias would leave the exact collision this task exists to remove.</li><li>Rename <code>modules/memory</code>'s console-script entry instead of the public bin (the original, since-superseded plan revision). Rejected: superseded same day by the founder directly (plan §9 revision log) once it was confirmed <code>cyberos-memory</code> was never going to be published — renaming an internal-only tool to protect a name the public CLI was going to leave anyway solves nothing.</li><li>Leave the stale domain fix for a separate task. Rejected: all three instances are single-line strings inside files this task already opens for the rename; deferring them means a sixth reviewer touching the same three lines for an unrelated one-word change.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: by the next CyberOS release after 1.0.9, <code>npx cs install</code>, <code>npx cs -h</code>, and every subcommand in <code>cli.mjs</code>'s <code>SCRIPTS</code> table resolve correctly, with zero remaining <code>npx cyberos</code> or bare <code>cyberos &lt;verb&gt;</code> references in <code>tools/install/{cli/bin/cli.mjs,help.sh,plugin/commands/help.md,docs/index.md,README.md}</code>. Baseline today: all five files instruct the reader to type <code>cyberos</code>.</li><li>Guardrail: in that same release, the payload's <code>package.json</code> <code>name</code> field remains <code>@cyberskill/cyberos</code> (unchanged), so <code>npm install @cyberskill/cyberos</code> continues to resolve to the same package post-rename — only the invoked command changes, not the install target.</li></ul>\n<h2>Scope</h2>\n<p>In scope: <code>build.sh</code>'s package.json generation (<code>bin</code> field only — <code>name</code> stays), <code>cli.mjs</code>'s dispatch/usage text, <code>help.sh</code>, the plugin's <code>help.md</code>, <code>docs/index.md</code>, the <code>npx cyberos ...</code> examples in <code>tools/install/README.md</code>, a CHANGELOG entry, and the three stale-domain instances found in the same files touched for the rename.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Renaming <code>modules/memory</code>'s own console-script entry (<code>modules/memory/pyproject.toml:29</code>) — confirmed internal-only, never needed for this collision, left untouched per the plan.</li><li>Renaming the npm package name <code>@cyberskill/cyberos</code> — decided against; see Alternatives Considered.</li><li>Adding a <code>memory</code> or <code>cuo</code> verb to the renamed dispatch table — that is TASK-IMP-131 / TASK-IMP-132.</li><li>Updating <code>Formula/cyberos-cli.rb</code> in the separate <code>homebrew-tap</code> repo — that is TASK-IMP-133.</li><li>The end-to-end regression proving the rename works on a clean machine — that is TASK-IMP-134.</li><li>Any pre-existing inconsistency in <code>tools/install/README.md</code> unrelated to the <code>cyberos</code>→<code>cs</code> rename itself (e.g. lines 186-188 describe <code>npx cyberos-gates</code>/<code>npx cyberos-mcp</code> as if they were separate bin names, which does not match <code>cli.mjs</code>'s actual single-bin dispatch design) — flagged as a pre-existing documentation inconsistency, not introduced or fixed by this task.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking — this is the root task the other four in this batch depend on. Touches the OIDC trusted-publishing pipeline in <code>release.yml</code> (plan §7): a bin-name change should go through the normal release flow and be re-tested end to end by TASK-IMP-134, not verified in isolation here.</p>\n<p><strong>Relationship to TASK-IMP-076 (done).</strong> That task shipped the original <code>install</code>/<code>uninstall</code>/<code>version</code>/<code>status</code>/<code>help</code> root CLI surface and the <code>mcp</code>/<code>gates</code> verbs inside <code>cli.mjs</code> — the exact dispatch table and usage text this task renames. It established the \"three channels cannot drift\" design (plugin slash commands, <code>help.sh</code>, <code>cli.mjs</code> mirror the same command set 1:1) that this task's doc sweep must preserve under the new name, not just the bin field itself.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS <code>task-author</code> skill inside Cowork.</li><li><strong>Scope:</strong> every <code>source_pages</code> line above was read at HEAD in this checkout during authoring; the stale-domain finding in <code>help.sh</code> and <code>plugin/commands/help.md</code> (beyond the single <code>cli.mjs</code> instance already surfaced to the operator in conversation) was discovered while gathering these citations, not asserted from memory.</li><li><strong>Human review:</strong> task decomposition and scope approved at the 2026-07-22 PLAN gate (create-tasks). The package-name decision and the domain-fix scope addition are recorded above as authoring-time calls, not yet independently reviewed beyond that PLAN approval — flagged for the operator to revisit if either call is unwanted.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 The payload's generated <code>package.json</code> MUST declare its <code>bin</code> field as <code>{ \"cs\": \"cli/bin/cli.mjs\" }</code> and MUST NOT change the <code>name</code> field from <code>@cyberskill/cyberos</code>.</li><li>1.2 <code>cli.mjs</code>'s top comment and <code>usage()</code> output MUST refer to the invocation as <code>cs &lt;command&gt;</code> (or <code>npx cs &lt;command&gt;</code>), not <code>cyberos &lt;command&gt;</code>.</li><li>1.3 <code>help.sh</code>'s \"Channels\" section MUST describe the npm channel as <code>npx cs &lt;command&gt;</code>, not <code>npx cyberos &lt;command&gt;</code>.</li><li>1.4 The plugin's <code>help.md</code>, <code>docs/index.md</code>'s \"same eight commands\" line, and every <code>npx cyberos ...</code> example in <code>tools/install/README.md</code> MUST read <code>cs</code> in place of <code>cyberos</code>.</li><li>1.5 The domain string <code>https://cyberos.cyberskill.world/docs</code> MUST be corrected to <code>https://os.cyberskill.world/docs</code> in every file this task modifies that contains it (<code>cli.mjs</code>, <code>help.sh</code>, <code>plugin/commands/help.md</code>).</li><li>1.6 <code>CHANGELOG.md</code> MUST gain an entry documenting the rename as a breaking change, naming the old (<code>cyberos</code>) and new (<code>cs</code>) command explicitly.</li><li>1.7 This task MUST NOT modify <code>modules/memory/pyproject.toml</code>'s console-script entry.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - a scratch build's <code>dist/*/package.json</code> has <code>bin: {\"cs\": \"cli/bin/cli.mjs\"}</code> and <code>name: \"@cyberskill/cyberos\"</code> - test: <code>tools/install/tests/test_cli_rename.sh::t01_bin_renamed_to_cs</code></li><li>[ ] AC 2 (traces_to: #1.2) - <code>node dist/*/cli/bin/cli.mjs --help</code> prints usage text containing <code>cs &lt;command&gt;</code> and contains no substring <code>cyberos &lt;command&gt;</code> - test: <code>tools/install/tests/test_cli_rename.sh::t02_usage_text_says_cs</code></li><li>[ ] AC 3 (traces_to: #1.3) - <code>bash dist/*/help.sh</code> output contains <code>npx cs &lt;command&gt;</code> and does not contain <code>npx cyberos</code> - test: <code>tools/install/tests/test_cli_rename.sh::t03_help_sh_says_cs</code></li><li>[ ] AC 4 (traces_to: #1.4) - for each of <code>tools/install/plugin/commands/help.md</code>, <code>tools/install/docs/index.md</code>, and <code>tools/install/README.md</code>'s <code>npx cyberos install [dir]</code> example line: the file now contains the replacement <code>cs</code>-based text (<code>/help</code> → mentions <code>cs</code>; <code>docs/index.md</code> → <code>npx cs &lt;command&gt;</code>; <code>README.md</code> → <code>npx cs install [dir]</code>) AND a grep for the literal string <code>cyberos &lt;command&gt;</code> across the three returns zero matches - test: <code>tools/install/tests/test_cli_rename.sh::t04_docs_sweep_replaced_not_just_removed</code></li><li>[ ] AC 5 (traces_to: #1.5) - a grep of <code>cli.mjs</code>, <code>help.sh</code>, and <code>plugin/commands/help.md</code> for <code>cyberos.cyberskill.world</code> returns zero matches, and each now contains <code>os.cyberskill.world/docs</code> - test: <code>tools/install/tests/test_cli_rename.sh::t05_stale_domain_fixed</code></li><li>[ ] AC 6 (traces_to: #1.6) - <code>CHANGELOG.md</code>'s top entry mentions both <code>cyberos</code> and <code>cs</code> and the word \"breaking\" - test: <code>tools/install/tests/test_cli_rename.sh::t06_changelog_entry_present</code></li><li>[ ] AC 7 (traces_to: #1.7) - <code>git diff</code> for this change touches no file under <code>modules/memory/</code> - test: <code>tools/install/tests/test_cli_rename.sh::t07_memory_module_untouched</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A consumer repo that already ran <code>npx cyberos install</code> before this ships has an installed <code>.cyberos/</code> machine with no dependency on the bin name (the machine itself never shells out to <code>cyberos</code>/<code>cs</code> internally) - re-running install under the new bin name MUST behave identically to a fresh install.</li><li><code>bash dist/*/help.sh</code> and <code>bash dist/*/install.sh</code> (invoked directly, not through the npm bin) are unaffected by the bin rename - only the npm-published entry point's name changes, not the underlying shell scripts' own invocation.</li><li>A user who globally installed an earlier version of <code>@cyberskill/cyberos</code> (bin <code>cyberos</code>) and upgrades to the version carrying this rename: whether npm cleanly removes the now-undeclared <code>cyberos</code> bin symlink or leaves it dangling is version/install-method dependent and NOT independently verified by this task - this is exactly the gap TASK-IMP-134's clean-machine end-to-end regression exists to observe directly, rather than this task asserting a specific npm bin-symlink behaviour it has not verified. Either outcome is the accepted breaking-change cost the plan (§7) already recorded; only the precise mechanics are unverified here.</li><li>The Grok/Claude plugin channel (<code>grok plugin install</code>, <code>claude plugin install cyberos@cyberos</code>) is a <strong>separate</strong> name from the npm bin - the plugin identifier <code>cyberos@cyberos</code> (<code>tools/install/README.md:121,123,136</code>) is the Claude/Grok marketplace+plugin name, unaffected by this task; renaming it is out of scope and not implied by 1.1-1.6.</li><li>Security-class: this task only changes which string a package.json/shell script prints and which key an object uses; it grants no new filesystem or network capability and narrows nothing that needs a security review beyond the standard release-pipeline retest called out in Dependencies.</li></ul>";
