window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-076"]="<h2>§1 — Description</h2>\n<ol><li><code>update.sh</code>, <code>changelog.sh</code>, <code>help.sh</code> <strong>MUST</strong> exist at the payload root beside the already-shipping <code>init.sh</code>, directly runnable (<code>bash update.sh</code>), mirroring the plugin commands' semantics: update = read-only check by default with <code>--apply</code> to execute (thin wrapper over init.sh, which owns the logic); changelog = installed version + <code>rules_sha</code> + pointers, read from the manifest beside the script; help = the command surface.</li><li><code>build.sh</code> <strong>MUST</strong> ship the trio in every payload (copy + chmod + npm <code>files</code> array) and declare the two new channels in <code>manifest.yaml</code>: <code>root-cli</code>, <code>mcp-connector</code>.</li><li><code>cyberos-mcp.mjs</code> <strong>MUST</strong> gain a <code>--http [port]</code> mode: MCP streamable-HTTP style endpoint (POST = one JSON-RPC message or batch → <code>application/json</code>; notifications → 202 empty; non-POST → 405; <code>GET /healthz</code> probe), reusing the existing <code>handle()</code> dispatch verbatim so stdio and http can never drift. stdio stays the default; zero new dependencies.</li><li>A <code>docs/deploy/mcp-connector.md</code> runbook <strong>MUST</strong> capture the agent-UI hookup (Claude: Name + Remote MCP server URL + optional OAuth; Grok: Name + Server URL) and the production checklist: reverse-proxy TLS, supervisor, and the explicit warning that the transport ships unauthenticated - public exposure requires proxy-level auth since the tools execute repo workflows.</li><li>Grok's dialog placeholder suggests legacy <code>/sse</code>; whether it accepts streamable HTTP is <strong>confirmed at hookup time, not asserted</strong> - if legacy SSE is required, that transport is a recorded follow-up (§9).</li></ol>\n<p><em>Length note: sanctioned lean profile - every §5 check ran live in-session (scripts executed, endpoint curl-verified).</em></p>\n<h2>§2 — Why</h2>\n<p>update.sh wraps init.sh instead of duplicating logic (single source of truth for vendoring + --check). The http mode reuses <code>handle()</code> so a tool added once serves both transports. Auth is deliberately NOT hand-rolled into the node process: proxy-level auth at nginx is the checklist item - a bespoke token check in a zero-dep server is worse than the battle-tested proxy layer already fronting the VPS.</p>\n<h2>§4/§5 — Acceptance + verification (all run live 2026-07-13)</h2>\n<ol><li>Payload contains executable trio; <code>check-version-sync.sh</code> still green. ✅ (build to /tmp, ls + run)</li><li><code>help.sh</code> prints the surface; <code>changelog.sh</code> prints version + rules_sha from its own manifest; <code>update.sh</code> (no args, offline) emits init.sh --check's three-value report + verdict. ✅ (all executed)</li><li><code>--http</code>: <code>/healthz</code> 200 JSON; POST tools/list returns the 4 workflow tools; notification → 202; GET → 405. ✅ (curl-verified)</li><li>stdio unchanged (default branch untouched semantics). ✅ (code path conditional on --http only)</li><li>Runbook exists with hookup + security checklist. ✅</li></ol>\n<h2>§5b — Testing pass (2026-07-13, post gate-1 \"approve all\")</h2>\n<ul><li>Payload rebuilt to /tmp: help.sh 17-line surface exit 0; changelog.sh prints 1.0.0 + rules_sha + pointers; endpoint healthz 200 / tools-list 4 / notification 202 / GET 405. PASS</li><li>DEFECT CAUGHT + FIXED IN-PHASE: <code>update.sh --check</code> passed the flag through twice (<code>exec init.sh --check \"$@\"</code> without shifting), so init.sh read the second <code>--check</code> as its TARGET (\"cd: --: invalid option\"; root detection broken, installed=none from an installed repo). The no-arg default path had masked it at implementation time. Fix: case/shift in update.sh. Re-run, all arg shapes green - live report: installed=1.0.0 payload=1.0.0 latest=1.0.0 (GitHub releases reachable) verdict=up_to_date.</li></ul>\n<h2>§9 — Open questions</h2>\n<ul><li>Grok legacy-SSE transport if streamable HTTP is rejected at hookup (recorded, not assumed).</li><li>OAuth client id/secret support in the connector dialog - relevant only if Stephen wants Anthropic-managed auth instead of proxy auth; follow-up.</li><li>Public URL + reverse-proxy wiring on the VPS (operator step - checklist in the runbook).</li></ul>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Recovery</th></tr></thead><tbody><tr><td>endpoint exposed without auth</td><td>runbook checklist item 3 (explicit warning)</td><td>proxy auth before DNS</td></tr><tr><td>oversized POST</td><td>1MB cap destroys request</td><td>client retries within cap</td></tr><tr><td>batch of notifications only</td><td>202 empty per transport semantics</td><td>n/a - correct</td></tr><tr><td>trio run outside a payload</td><td>changelog/update print explicit error + exit 2</td><td>run from payload/.cyberos</td></tr><tr><td>stdio/http drift</td><td>impossible by construction - one handle()</td><td>n/a</td></tr></tbody></table>\n<p><em>End of TASK-IMP-076.</em></p>";
