window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-127"]="<h1>TASK-IMP-127: Payload build MUST read from git, not the working tree</h1>\n<h2>Summary</h2>\n<p><code>build.sh</code> assembles the payload by copying from the working tree, so any untracked or gitignored file sitting in <code>modules/</code> ships in the payload and changes <code>rules_sha</code>. The same commit therefore produces different fingerprints on different machines, which defeats the drift detection <code>rules_sha</code> exists to provide. Drive the payload copy from git so the payload is by construction the tracked content at that commit, and fail the build when the module tree is dirty.</p>\n<h2>Problem</h2>\n<p><code>rules_sha</code> is defined by TASK-IMP-074 as a content fingerprint over the distributed rule trees, and <code>update-check.sh</code> reports RULE DRIFT when an installed machine's fingerprint differs from the payload's at the same version. That signal is only meaningful if the fingerprint is a function of the commit.</p>\n<p>It is not. <code>build.sh:28-74</code> assembles the payload with <code>cp</code> / <code>cp -R</code> from <code>$repo/modules/...</code>, never consulting git, and <code>build.sh:376</code> computes <code>rules_sha</code> by walking the resulting output tree. Anything a developer happens to have in <code>modules/</code> is copied in and hashed.</p>\n<p>Measured on 2026-07-20 against <code>069d4dff</code>: a local build produced <code>a9c848e5</code>, CI produced <code>8745b0fd</code>. The diff is six files, all present locally and absent in CI, all inside the fingerprinted <code>cuo</code> tree - one <code>.DS_Store</code> (matched by <code>.gitignore:15</code>, so gitignored files are not excluded either) and five files of a <code>code_audit_validator.egg-info/</code> directory left by a local editable install. Zero content differences on any shared path: the payloads are functionally identical and only the fingerprint diverges.</p>\n<p>The consequence is a false positive that is indistinguishable from a true one. A fingerprint that varies with untracked local state cannot tell \"the rules changed\" from \"someone opened a folder in Finder\", so RULE DRIFT stops being evidence of anything. It is not theoretical - installing a locally built payload across the estate on 2026-07-20 put 20 repos on <code>a9c848e5</code>, every one of which would have reported drift against the released payload with no rule change behind it.</p>\n<h2>Proposed Solution</h2>\n<p>Make the tracked content at HEAD the only thing that can enter the payload:</p>\n<ul><li>Drive the payload copy from git rather than the filesystem - <code>git archive</code> for whole subtrees, or <code>git ls-files</code> to enumerate what may be copied - so untracked and gitignored files cannot be selected at all.</li><li>Fail the build when the module tree carries untracked files, rather than silently absorbing them, so the operator learns at build time instead of at a drift report weeks later.</li></ul>\n<h2>Alternatives Considered</h2>\n<ul><li>An <code>--exclude</code> list for <code>.DS_Store</code> and <code>*.egg-info</code>. Rejected: it fixes the two extensions observed today and stays silent on the next one. The defect is that the build selects by \"what is on disk\" instead of \"what is committed\"; excluding known offenders leaves that selection rule intact.</li><li>Compute <code>rules_sha</code> from git object hashes instead of the output tree. Rejected as the primary fix: it would make the fingerprint reproducible while still shipping junk files in the payload. Correct the payload, and the fingerprint follows.</li><li>Have the release job compare its <code>rules_sha</code> against a local build and fail on mismatch. Kept as a guardrail (below), not as the fix - it detects the divergence without preventing it.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a build from a dirty working tree (untracked <code>.DS_Store</code> and an <code>egg-info/</code> dir planted under <code>modules/</code>) produces a payload byte-identical to a build from a clean checkout of the same commit, and therefore the same <code>rules_sha</code>. Baseline today: the two differ by six files and the fingerprint changes.</li><li>Guardrail: no tracked file is dropped - the payload built from git contains exactly the file set the current build produces from a clean tree, so the fix cannot silently shrink the payload.</li></ul>\n<h2>Scope</h2>\n<p>In scope: how <code>build.sh</code> selects files for the payload, the dirty-tree guard, and arms in <code>test_release_assets.sh</code>.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>The definition of <code>rules_sha</code> itself (TASK-IMP-074) - the set of fingerprinted trees is unchanged.</li><li>The RULE DRIFT message wording in <code>lib/update-check.sh</code>.</li><li>Re-vendoring the fleet after the fix (an operator-gated action).</li><li>Removing the <code>.DS_Store</code> and <code>egg-info</code> files currently in the working tree - housekeeping, and the point of this task is that the build must not care whether they are there.</li></ul>\n<h2>Dependencies</h2>\n<p>None blocking. Depends conceptually on TASK-IMP-074, which defines the fingerprint this task makes reproducible.</p>\n<p><strong>Relationship to TASK-IMP-122 (p1, on_hold).</strong> Both tasks concern <code>rules_sha</code> correctness and they are complementary, not overlapping - an implementer MUST NOT treat either as subsuming the other:</p>\n<ul><li>TASK-IMP-122 governs the COMPARISON side: that the installed fingerprint is recomputed rather than read from a stored token, and which paths the cone covers. Its §1.6 Direction 1 fails the build on a path under <code>$CY</code> that no cone entry classifies.</li><li>This task governs the PRODUCTION side: which files may enter the payload at all.</li></ul>\n<p>122 does not fix this defect. The six contaminating files sit at <code>cuo/gates/caf/caf/...</code>, beneath 122's <code>dir:cuo</code> cone entry, so they are classified and hashed - Direction 1 passes them cleanly while they still corrupt the fingerprint. Conversely this task does not deliver 122: a payload built from git is reproducible, but a comparator that recalls a stored token still reports a build rather than a tree.</p>\n<p>They compose in one direction worth stating: once the payload cannot carry untracked files, 122's recomputation compares two trees that a clean checkout can reproduce, which is the premise 122's §1.10 (\"byte-identical across the cone\") assumes and cannot currently rely on.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from the 2026-07-20 estate sweep. The two fingerprints and the six-file diff were measured directly - a released payload was downloaded from the v1.0.0 assets and diffed against the local <code>dist/</code> tree - not inferred. The claim \"zero content differences on shared paths\" is the output of <code>diff -rq</code> over the five fingerprinted directories.</li><li><strong>Human review:</strong> scope and granularity approved at the 2026-07-20 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 <code>build.sh</code> MUST select payload content from git-tracked files at the built commit; a file that is untracked or gitignored MUST NOT be able to enter the payload, regardless of its presence in the working tree.</li><li>1.2 A build of a given commit MUST produce a byte-identical payload, and therefore an identical <code>rules_sha</code>, whether or not the working tree carries untracked or gitignored files under <code>modules/</code>.</li><li>1.3 <code>build.sh</code> MUST fail with a non-zero exit and name the offending paths when the module tree contains untracked files, rather than absorbing them into the payload.</li><li>1.4 The git-driven copy MUST NOT drop any tracked file the current working-tree copy produces from a clean checkout - the payload file set is unchanged for a clean build.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2) - a fixture plants an untracked <code>.DS_Store</code> and an untracked <code>egg-info/</code> dir under <code>modules/</code>, builds, and the payload and <code>rules_sha</code> are byte-identical to a build of the same commit from a clean tree - test: <code>tools/install/tests/test_release_assets.sh::t_build_ignores_untracked</code></li><li>[ ] AC 2 (traces_to: #1.3) - a build with untracked files under <code>modules/</code> exits non-zero and its stderr names at least one offending path - test: <code>tools/install/tests/test_release_assets.sh::t_build_fails_on_dirty_module_tree</code></li><li>[ ] AC 3 (traces_to: #1.4) - the file list of a git-driven payload from a clean checkout equals the file list the pre-change build produces from the same checkout - test: <code>tools/install/tests/test_release_assets.sh::t_payload_file_set_unchanged</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A gitignored file whose path is also tracked (added before the ignore rule) is tracked and MUST ship - \"tracked\" is the test, not \"unignored\".</li><li>A submodule or symlink under <code>modules/</code> must be handled the same way the current copy handles it; the selection rule changes, the materialisation does not.</li><li>Building from a detached HEAD or a tag (the release path dispatches against a tag) MUST work - the commit is whatever is checked out, not necessarily a branch tip.</li><li>Building from an archive export with no <code>.git</code> present (a consumer building from a tarball) MUST either work or fail with a message naming the missing repository, never silently fall back to the working-tree copy - a silent fallback would reintroduce 1.1.</li><li>The dirty-tree guard MUST scope to the module tree the payload is built from, not the whole repo: an untracked file under <code>docs/</code> or <code>/tmp</code> is irrelevant and MUST NOT fail the build.</li><li>Security-class: the build reads files and executes nothing from them. Enumerating via git rather than the filesystem narrows the attack surface - an attacker who can drop a file in the working tree can no longer get it into a signed release asset.</li></ul>";
