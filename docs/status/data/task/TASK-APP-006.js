window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-APP-006"]="<h2>§1 — Description</h2>\n<ol><li>CyberOS <strong>MUST</strong> gain two independent package-manager listing paths — Homebrew Cask (macOS) and winget (Windows) — each pointing at the existing GitHub Releases installer artifacts (the <code>.dmg</code> for Cask, matching TASK-APP-003's Developer-ID-signed channel not the Mac-App-Store <code>.pkg</code> channel; the <code>.msi</code>/NSIS <code>.exe</code> for winget, matching TASK-APP-004's GitHub-Releases NSIS channel not the MSIX Store channel) — not producing new build artifacts of their own.</li></ol>\n<ol><li>This task <strong>MUST</strong> be treated as explicitly out-of-scope-for \"store\" semantics, consistent with its task-list framing (\"package managers, not stores\"): neither Homebrew Cask nor winget performs app review, sandboxing enforcement, or discovery/search placement — both are community-maintained manifest registries that point at CyberOS's own already-built artifacts. Precisely: the <code>.dmg</code> and NSIS <code>.exe</code> this task's manifests point at are produced by the pre-existing, always-on <code>desktop</code> job in <code>release.yml</code> (confirmed lines 69–139, no opt-in gate), which runs independent of and predates both TASK-APP-003 and TASK-APP-004. TASK-APP-003 and TASK-APP-004 add <em>separate</em>, additionally-gated artifacts (a MAS <code>.pkg</code>, an MSIX package) this task does <strong>NOT</strong> consume — this task does not duplicate either task's signing decisions, and its manifests' correctness has no dependency on TASK-APP-003/004 landing at all.</li></ol>\n<ol><li><code>homebrew-cask-manifest/cyberos.rb</code> <strong>MUST</strong> declare <code>version</code>, <code>sha256</code>, <code>url</code> (pointing at a GitHub Releases <code>.dmg</code> asset URL, per the URL pattern already confirmed in production use by <code>tauri.conf.json</code>'s updater <code>endpoints</code> field: <code>https://github.com/cyberskill-official/cyberos/releases/.../download/&lt;file&gt;</code>), <code>name</code>, <code>desc</code>, <code>homepage</code>, and an <code>app \"CyberOS.app\"</code> stanza, plus a <code>livecheck</code> block pointing Homebrew's own automated version-checking bot at the GitHub Releases API so Homebrew's infrastructure can detect new CyberOS releases independent of CyberOS's own CI.</li></ol>\n<ol><li>The winget manifest set (<code>CyberSkill.CyberOS.yaml</code>, <code>CyberSkill.CyberOS.installer.yaml</code>, <code>CyberSkill.CyberOS.locale.en-US.yaml</code>) <strong>MUST</strong> follow Microsoft's current three-file manifest schema (<code>PackageIdentifier: CyberSkill.CyberOS</code>, <code>PackageVersion</code>, <code>InstallerType</code>, <code>InstallerUrl</code>, <code>InstallerSha256</code>) with the installer URL pointing at the equivalent GitHub Releases Windows artifact.</li></ol>\n<ol><li>Neither manifest <strong>MUST</strong> be submitted (i.e. no PR opened against <code>homebrew/homebrew-cask</code> or <code>microsoft/winget-pkgs</code>) as a direct or automatic consequence of this task landing — submission is a distinct, later, Stephen-gated action per §1's <code>disallowed_tools</code>, exactly mirroring the TASK-APP-005 Flathub-PR gate.</li></ol>\n<ol><li>CI <strong>MAY</strong> prepare (stage, locally validate, diff-generate) updated manifests on each tagged release behind two independent repo variables, <code>PKGMGR_CASK_RELEASE=true</code> and <code>PKGMGR_WINGET_RELEASE=true</code>, but <strong>MUST NOT</strong> call <code>gh pr create</code>, <code>brew bump-cask-pr</code>, <code>wingetcreate submit</code>, or any equivalent submission command against either external repo under any CI-flag state — the flags gate manifest <em>preparation</em>, never <em>submission</em>, which stays a manual, explicit, per-instance human action regardless of flag state.</li></ol>\n<ol><li>Both manifests' version/URL/hash fields <strong>MUST</strong> be kept re-derivable from the existing GitHub Releases artifacts and the existing VERSION-stamping mechanism (confirmed in <code>release.yml</code>: <code>node scripts/stamp-release-version.mjs --apply</code>) rather than hand-maintained as a second source of truth that can drift from the actual shipped version.</li></ol>\n<ol><li>A <code>docs/deploy/package-manager-submission.md</code> answer sheet <strong>MUST</strong> document Homebrew Cask's quality-bar checklist (stable URL, no interactive-only installer, no existing duplicate cask) and winget's validation-pipeline checklist (silent-install flag support, installer signature/hash correctness), plus the exact GitHub PAT scopes needed if update automation via <code>wingetcreate</code>/Homebrew tooling is later adopted.</li></ol>\n<ol><li>The Cask manifest's <code>zap trash:</code> uninstall-cleanup paths <strong>MUST</strong> be verified against what CyberOS's actual macOS build writes to disk (application-support directory, preferences plist) before the manifest is treated as final — §3's skeleton lists plausible candidates derived from the confirmed <code>os.cyberskill.world.desktop</code> Tauri identifier, not confirmed-observed paths, and an incorrect <code>zap</code> stanza either leaves stale files behind on uninstall or, worse, deletes paths CyberOS doesn't actually own.</li></ol>\n<h2>§2 — Why this design</h2>\n<p><strong>Why is the \"never submit automatically\" rule (§1 #5, #6) even stricter than TASK-APP-005's Flathub gate, extending to update PRs and not just the initial submission?</strong> Flathub's manifest lives in a repo CyberOS effectively owns once accepted (a per-app repo under the <code>flathub</code> GitHub org that Stephen would have write access to after initial approval), so <em>update</em> automation there is plausible future work with a narrower blast radius. Homebrew Cask and winget-pkgs are both large, shared, single monorepos (<code>homebrew/homebrew-cask</code>, <code>microsoft/winget-pkgs</code>) serving thousands of unrelated packages — an automation bug that fires against the wrong branch, includes malformed YAML, or double-submits has a shared-repo blast radius that a single-app-repo mistake on Flathub doesn't. Treating every PR — first submission and every subsequent version-bump — as requiring a fresh human go-ahead is the more conservative posture warranted by that difference, not an arbitrary inconsistency with TASK-APP-005.</p>\n<p><strong>Why isn't this task just \"TASK-APP-003 and TASK-APP-004's Homebrew/winget sections,\" given it reuses their artifacts?</strong> Package-manager listing is a genuinely separate audience and submission process from app-store review — a developer typing <code>brew install --cask cyberos</code> never interacts with the Mac App Store review pipeline TASK-APP-003 builds, and the two have independent quality bars, independent external repos, and independent (community-maintained, not Apple/Microsoft-controlled) review processes. Folding this into TASK-APP-003/004 would conflate \"get CyberOS into Apple's/Microsoft's curated stores\" with \"get CyberOS listed in a community package index,\" which are different enough asks that a shared task would either under-serve one or bloat past a coherent single scope — this is the same reasoning that kept TASK-APP-005's Snap Store and Flathub sub-tracks inside one task (both are Linux \"stores\" in the task list's own framing) while keeping this task (explicitly \"package managers, not stores\") separate.</p>\n<p><strong>Why bundle Homebrew Cask (macOS) and winget (Windows) into one task rather than splitting per-OS, mirroring how TASK-APP-005 bundled Snap Store and Flathub into one Linux task?</strong> Both mechanisms share an identical shape — a community-maintained manifest registry, external to CyberOS's own CI, pointing at an already-built GitHub Release artifact, gated by the identical \"prepare but never auto-submit\" policy (§1 #5, #6) — so drafting them as two separate tasks would be near-total duplication of §1's submission-safety language, §2's blast-radius reasoning, and §7's human-gate structure, with no meaningfully separable acceptance criteria or verification surface between the two. This mirrors TASK-APP-005's own reasoning for bundling Snap and Flathub rather than splitting by mechanism, and the same test applies in reverse to justify <em>not</em> also bundling this task with TASK-APP-005: Linux's two mechanisms and this task's two mechanisms serve genuinely different OS audiences with no shared CI job, shared manifest format, or shared external-repo relationship, so a four-way bundle would conflate unrelated platforms for no corresponding reduction in duplicated language.</p>\n<p><strong>Why does the winget manifest set split into three separate files (version, installer, locale) while the Cask manifest is a single file?</strong> This is Microsoft's own current <code>winget-pkgs</code> schema requirement, not a CyberOS design choice — the three-file split (a version manifest, one or more installer manifests, one or more locale manifests) lets a single package identity carry multiple installer architectures and multiple locale descriptions without duplicating shared fields, a structure Homebrew Cask's single-file DSL doesn't need because Cask has no equivalent multi-locale requirement. §3 shows only the installer file's skeleton because it's the file carrying the fields most likely to need re-derivation on every release (<code>InstallerUrl</code>, <code>InstallerSha256</code>); the version and locale files change far less often and are generated directly from Microsoft's current manifest templates at WORKER-phase time rather than hand-drafted here against a schema this task's authoring hasn't independently re-verified (§9).</p>\n<p><strong>Why keep version/URL/hash fields re-derivable rather than hand-maintained (§1 #7)?</strong> Both Homebrew Cask and winget manifests encode a SHA256 hash of the exact installer artifact — a hand-maintained hash is a classic source of silent staleness (a new release ships, the manifest isn't updated, and either the old version stays listed or, worse, the hash mismatches the actual current download and both package managers' own validation rejects the install). Deriving these fields from the same VERSION-stamping mechanism and <code>gh release</code> artifacts the existing pipeline already produces (rather than a parallel manually-tracked value) keeps the eventual automation (§9) mechanically simple: compute the hash of the artifact <code>gh release download</code> just fetched, don't ask a human to type it in.</p>\n<h2>§3 — API contract</h2>\n<p><code>homebrew-cask-manifest/cyberos.rb</code> (structural skeleton):</p>\n<pre><code class=\"language-ruby\">cask \"cyberos\" do\n  version \"1.0.0\"\n  sha256 \"REPLACE_WITH_SHA256_OF_RELEASE_DMG\"  # computed per §6, not hand-typed\n\n  url \"https://github.com/cyberskill-official/cyberos/releases/download/v#{version}/CyberOS_#{version}_universal.dmg\"\n  name \"CyberOS\"\n  desc \"CyberSkill's desktop client — Turn Your Will Into Real\"\n  homepage \"https://os.cyberskill.world/\"\n\n  livecheck do\n    url :url\n    strategy :github_latest\n  end\n\n  app \"CyberOS.app\"\n\n  zap trash: [\n    \"~/Library/Application Support/CyberOS\",\n    \"~/Library/Preferences/os.cyberskill.world.desktop.plist\",\n  ]\nend</code></pre>\n<p><code>winget-manifest/CyberSkill.CyberOS/CyberSkill.CyberOS.installer.yaml</code> (structural skeleton — the version and locale manifest siblings follow winget's standard three-file split, omitted here to avoid duplicating boilerplate the WORKER phase generates directly from Microsoft's current manifest templates):</p>\n<pre><code class=\"language-yaml\">PackageIdentifier: CyberSkill.CyberOS\nPackageVersion: 1.0.0\nInstallerType: nullsoft   # matches the existing NSIS output already confirmed in release.yml's desktop job (windows-latest leg, tauri-action default Windows target)\nInstallers:\n  - Architecture: x64\n    InstallerUrl: https://github.com/cyberskill-official/cyberos/releases/download/v1.0.0/CyberOS_1.0.0_x64-setup.exe\n    InstallerSha256: REPLACE_WITH_SHA256_OF_RELEASE_INSTALLER   # computed per §6, not hand-typed\n    InstallerSwitches:\n      Silent: \"/S\"          # NSIS's standard silent-install flag; MUST be confirmed against\n      SilentWithProgress: \"/S\"  # CyberOS's actual NSIS installer config at implementation time,\n                                 # not assumed — see §9.\nManifestType: installer\nManifestVersion: 1.6.0   # NOTE: this manifest schema version number and the exact\n                          # InstallerSwitches key structure/nesting shown above are this task's\n                          # best-available understanding, not independently re-verified against\n                          # Microsoft's live current schema during authoring — §9 requires\n                          # confirming both against winget-pkgs' current manifest templates\n                          # before this file is treated as final, matching the same discipline\n                          # applied to the uncertain Snapcraft architectures: form in TASK-APP-005.</code></pre>\n<p><code>.github/workflows/release-pkgmgr-pr.yml</code> (preparation-only skeleton — never submits, per §1 #5/#6):</p>\n<pre><code class=\"language-yaml\">name: release-pkgmgr-pr\non:\n  workflow_dispatch:\njobs:\n  prepare-cask-manifest:\n    if: vars.PKGMGR_CASK_RELEASE == 'true'\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Compute SHA256 of the release .dmg\n        env: { GH_TOKEN: \"${{ github.token }}\" }\n        run: |\n          TAG=\"${{ github.event.inputs.tag || github.ref_name }}\"\n          gh release download \"$TAG\" --repo cyberskill-official/cyberos --pattern \"*universal.dmg\" --output cyberos.dmg\n          sha256sum cyberos.dmg\n      - name: Render updated cyberos.rb (staged locally, NOT submitted)\n        run: |\n          echo \"manifest diff generation happens here; artifact is uploaded for Stephen's review,\n                no gh pr create / brew bump-cask-pr call exists in this job — see §1 #5/#6.\"\n      - uses: actions/upload-artifact@v4\n        with: { name: cyberos-cask-manifest-draft, path: homebrew-cask-manifest/cyberos.rb }\n  prepare-winget-manifest:\n    if: vars.PKGMGR_WINGET_RELEASE == 'true'\n    runs-on: windows-2022\n    steps:\n      - uses: actions/checkout@v4\n      - name: Compute SHA256 of the release installer\n        shell: pwsh\n        env: { GH_TOKEN: \"${{ github.token }}\" }\n        run: |\n          $tag = \"${{ github.event.inputs.tag || github.ref_name }}\"\n          gh release download $tag --repo cyberskill-official/cyberos --pattern \"*x64-setup.exe\" --output cyberos-setup.exe\n          Get-FileHash cyberos-setup.exe -Algorithm SHA256\n      - name: Render updated winget manifest set (staged locally, NOT submitted)\n        run: |\n          Write-Host \"manifest diff generation happens here; artifact uploaded for Stephen's review, no wingetcreate submit call exists in this job — see §1 #5/#6.\"\n      - uses: actions/upload-artifact@v4\n        with: { name: cyberos-winget-manifest-draft, path: winget-manifest/CyberSkill.CyberOS/ }</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Cask manifest is Ruby-syntax-valid and passes local audit</strong> — <code>brew audit --cask homebrew-cask-manifest/cyberos.rb</code> and <code>brew style --cask homebrew-cask-manifest/cyberos.rb</code> both exit 0 against a version of the manifest with a real, non-placeholder <code>sha256</code> (computed against an actual downloaded artifact, not the literal placeholder string).</li><li><strong>winget manifest set is schema-valid</strong> — Microsoft's manifest validation (exact current invocation confirmed at implementation time, §9) passes against all three files with a real, non-placeholder <code>InstallerSha256</code>.</li><li><strong>No submission command exists anywhere in this task's tooling</strong> — structurally guaranteed, not just documented, by two independent checks so a submission command isn't missed merely for not literally mentioning a target-repo string near it: (a) <code>grep -rE \"gh pr create|brew bump-cask-pr|wingetcreate submit\" .github/ tools/</code> across the <em>entire</em> CI/tooling surface, not gated on any repo-name precondition, returns zero matches; (b) a secondary, narrower check confirms no file mentioning <code>homebrew-cask</code> or <code>winget-pkgs</code> contains any of those same submission-command patterns, for defense in depth. Check (a) is the actual guarantee; check (b) exists only to catch the specific \"someone spelled the repo name and the submission call in the same file\" case with a clearer error message.</li><li><strong>Both CI prep jobs are inert by default</strong> — with <code>PKGMGR_CASK_RELEASE</code>/<code>PKGMGR_WINGET_RELEASE</code> unset or <code>false</code>, both <code>release-pkgmgr-pr.yml</code> jobs are skipped (verified the same way as TASK-APP-003/004/005 — unconditional anchor jobs plus <code>gh run view</code> conclusion checks, one per job since the two flags are independent).</li><li><strong>Version/hash fields are re-derived, never hand-typed in the shipped workflow output</strong> — the <code>prepare-cask-manifest</code>/<code>prepare-winget-manifest</code> jobs' uploaded draft artifacts contain a <code>sha256</code>/<code>InstallerSha256</code> value matching the actual <code>sha256sum</code>/<code>Get-FileHash</code> output computed in the same job run, not a value carried over from a prior manual edit.</li><li><strong>Answer sheet is complete</strong> — <code>docs/deploy/package-manager-submission.md</code> has a filled-in row for every Homebrew Cask quality-bar item and every winget validation-pipeline item, each marked <code>human-confirmed</code> or <code>not-applicable</code> with a reason.</li><li><strong>No credential material committed</strong> — the repo's existing secret-scan gate passes against every file this task adds.</li><li><strong>NSIS silent-install switches are confirmed, not assumed</strong> — <code>docs/deploy/package-manager-submission.md</code> records the result of an actual test invocation of CyberOS's real NSIS installer with <code>/S</code> (or whatever flag is confirmed correct) before the winget manifest's <code>InstallerSwitches</code> block is treated as final, since an incorrect silent-install flag causes winget's own automated validation pipeline to fail installs that require interactive confirmation.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-bash\"># AC #1 — Cask manifest local audit (requires a local Homebrew installation; this task does not\n# assume one exists in every CI environment, so this is documented as a local/manual verification\n# step alongside any CI equivalent implemented at WORKER-phase time)\nbrew audit --cask homebrew-cask-manifest/cyberos.rb\necho \"exit: $?\"  # MUST be 0\nbrew style --cask homebrew-cask-manifest/cyberos.rb\necho \"exit: $?\"  # MUST be 0\n\n# AC #3 — no submission command anywhere in the repo's tooling.\n# Check (a): the real guarantee — scan ALL of .github/ and tools/ for the submission\n# command patterns themselves, with no repo-name precondition that a submission call\n# could evade by simply not mentioning \"homebrew-cask\"/\"winget-pkgs\" nearby.\nif grep -rE \"gh pr create|brew bump-cask-pr|wingetcreate submit\" .github/ tools/ 2&gt;/dev/null; then\n  echo \"VIOLATION: submission command found (see matches above)\"\n  exit 1\nfi\n# Check (b): defense-in-depth — same pattern, scoped to files that also mention either\n# target repo by name, purely for a clearer violation message in that specific case.\nfor f in $(grep -rl \"homebrew-cask\\|winget-pkgs\" .github/ tools/ 2&gt;/dev/null); do\n  if grep -qE \"gh pr create|brew bump-cask-pr|wingetcreate submit\" \"$f\"; then\n    echo \"VIOLATION: submission command found in $f (repo-name-adjacent)\"\n    exit 1\n  fi\ndone\necho \"no submission commands found\"</code></pre>\n<pre><code class=\"language-yaml\"># AC #4 — both CI prep jobs inert-by-default, mirroring TASK-APP-003/004/005's pattern\n- name: Assert Cask prep job skipped when PKGMGR_CASK_RELEASE unset\n  run: |\n    gh run view ${{ github.run_id }} --json jobs -q \\\n      '.jobs[] | select(.name==\"prepare-cask-manifest\") | .conclusion' | grep -q skipped\n- name: Assert winget prep job skipped when PKGMGR_WINGET_RELEASE unset\n  run: |\n    gh run view ${{ github.run_id }} --json jobs -q \\\n      '.jobs[] | select(.name==\"prepare-winget-manifest\") | .conclusion' | grep -q skipped</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>The API contract in §3 covers both manifest skeletons and the preparation-only CI workflow. Two pieces are intentionally deferred rather than fully specified here:</p>\n<ul><li><strong>The exact winget manifest validation command</strong> (referenced in AC #2) — Microsoft ships this capability as part of the <code>winget-pkgs</code> repo's own CI tooling and/or the <code>winget validate</code> client subcommand; the precise current invocation should be confirmed against Microsoft's live <code>winget-pkgs</code> contribution documentation at implementation time rather than guessed, per the same discipline TASK-APP-005 applied to <code>flatpak-builder</code>'s uncertain flag.</li><li><strong>The manifest-diff-rendering logic</strong> in both <code>release-pkgmgr-pr.yml</code> jobs (currently a placeholder <code>echo</code>/<code>Write-Host</code> in §3) — templating a Ruby Cask file and three YAML winget files from the computed version/hash values is mechanical string substitution best implemented directly against the final manifest structure once §9's NSIS-switch and Homebrew <code>livecheck</code> questions resolve, rather than duplicated here against values that may still change.</li></ul>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>Upstream:</strong> none. Consumes GitHub Release artifacts produced by TASK-APP-003 (macOS <code>.dmg</code>) and TASK-APP-004/the existing <code>desktop</code> job (Windows NSIS <code>.exe</code>) without depending on either task's own gating flags (<code>MAS_RELEASE</code>, <code>MSSTORE_RELEASE</code>) — the GitHub-Releases artifacts this task points at are produced by the pre-existing, always-on <code>desktop</code> job, not the store-specific opt-in jobs those two tasks add.</li><li><strong>Downstream:</strong> none currently drafted.</li><li>If a future task adopts <code>wingetcreate</code>/Homebrew PR-automation tooling (§9, §11's PAT-scope note), it would depend on this task's manifest files and CI prep jobs existing first.</li><li>No such task is drafted, scheduled, or otherwise planned as of this writing — the dependency is noted here purely so a future author knows to add <code>depends_on: [TASK-APP-006]</code> rather than discovering the relationship after the fact.</li><li><strong>Cross-module:</strong> none.</li><li><strong>Human/account prerequisites:</strong> none for manifest preparation (no paid account, no Homebrew/Microsoft account needed to author or locally validate either manifest). <strong>Hard blocker on ever opening either PR:</strong> Stephen's explicit, fresh, per-instance chat-turn approval (§1 #5, §4 AC #3 — structurally guaranteed). If update automation via <code>wingetcreate</code>/Homebrew PR tooling is adopted later (§9), a scoped GitHub PAT becomes a prerequisite at that time, not before.</li></ul>\n<h2>§8 — Example payloads</h2>\n<p><code>brew audit --cask</code> failure output shape (illustrative — a plausible, not fabricated-as-real, Homebrew audit error for a placeholder hash):</p>\n<pre><code>Error: cyberos\n  sha256 \"REPLACE_WITH_SHA256_OF_RELEASE_DMG\" is not a valid SHA256 digest</code></pre>\n<p>Cask manifest submission-guard violation shape (AC #3's local check, illustrative):</p>\n<pre><code>VIOLATION: submission command found in .github/workflows/release-pkgmgr-pr.yml</code></pre>\n<p><code>winget validate</code>-style schema failure shape (illustrative — exact tool/invocation confirmed at implementation time per §6, not asserted here as the real Microsoft error text):</p>\n<pre><code>Manifest Validation Failed.\nField: InstallerSha256\nError: Value does not match a valid SHA256 hash</code></pre>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>CyberOS's actual NSIS silent-install switch</strong> — §3's winget manifest skeleton assumes the standard NSIS <code>/S</code> flag, which is the conventional default but depends on how CyberOS's specific NSIS installer script (generated by <code>tauri-action</code>/Tauri's NSIS templating) is configured; AC #8 requires this be confirmed by an actual test run before the manifest is treated as final, not assumed from NSIS convention alone.</li><li><strong>Winget manifest validation invocation</strong> — deferred per §6; the exact current command/CLI needs confirming against Microsoft's live <code>winget-pkgs</code> contribution docs.</li><li><strong>Whether to adopt <code>wingetcreate submit</code>/Homebrew PR-automation tooling for ongoing version-bump PRs, vs. Stephen manually running the equivalent commands per release</strong> — this task deliberately leaves the <em>submission</em> step manual regardless of the answer (§1 #6); the open question is only whether the <em>manifest-diff generation</em> step (§6) is eventually automated further, which is an implementation-convenience question, not a scope question this task needs to resolve now.</li><li><strong>Homebrew Cask <code>zap trash:</code> paths</strong> — §3's skeleton lists plausible uninstall-cleanup paths (<code>~/Library/Application Support/CyberOS</code>, a preferences plist matching the Tauri <code>identifier</code>) based on the confirmed <code>os.cyberskill.world.desktop</code> identifier convention, but these should be verified against what CyberOS's macOS build actually writes to disk before the cask is finalized, not assumed from the identifier string alone.</li><li><strong><code>related_tasks</code> references this task as the last item in the same batch as TASK-APP-003/004/005, all now landed</strong> — unlike TASK-APP-003/004/005's own §9 forward-reference notes, this task is authored last in the batch, so all its <code>related_tasks</code> entries resolve to already-existing files; no forward-reference disclosure is needed here, and this is noted for symmetry with the prior three tasks' §9 sections rather than because a gap exists.</li><li><strong>Whether <code>docs/deploy/package-manager-submission.md</code> should also catalog Homebrew Cask's and winget-pkgs' publicly documented rejection reasons for past duplicate-package or naming-collision submissions from other developers</strong>, to pre-empt known failure patterns before Stephen's first submission attempt — a nice-to-have research task with no scope-blocking dependency on this task landing, but worth doing before the first PR to reduce rejection/resubmission cycles against two external review queues CyberOS doesn't control the pace of.</li></ul>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Assumed NSIS <code>/S</code> silent-install flag is actually wrong for CyberOS's specific installer configuration</td><td>winget's own automated validation pipeline fails the submission, reporting an installer that requires interactive input</td><td>Submission rejected by winget-pkgs' CI before human review even begins</td><td>AC #8 requires confirming the real flag via an actual test run before the manifest is treated as final — this failure mode is the reason that AC exists rather than trusting NSIS convention</td></tr><tr><td>Homebrew Cask's <code>livecheck</code> block strategy (<code>:github_latest</code>) doesn't correctly detect CyberOS's release tagging convention (e.g. if tags aren't semver-clean)</td><td>Homebrew's own automated livecheck bot reports a livecheck failure on the cask, visible in Homebrew's CI dashboards, not CyberOS's</td><td>Cask stays technically installable but Homebrew's staleness-detection stops working for it, risking eventual flagging as outdated</td><td>Documented as a manual verification step in the answer sheet (AC #6) — confirm the release-tag format (<code>v1.0.0</code>, confirmed in the existing <code>release.yml</code>'s `github.event.inputs.tag \\</td><td>\\</td><td>github.ref_name<code> pattern) is compatible with the </code>:github_latest` livecheck strategy before finalizing</td></tr><tr><td><code>sha256sum</code>/<code>Get-FileHash</code> computed in the CI prep job (§3) doesn't match what a human manually re-downloading the same release artifact would get, due to a mid-release asset re-upload (<code>gh release upload --clobber</code>, confirmed used in the existing pipeline) changing the artifact after the prep job already ran</td><td>A subsequent PR submission (manual, by Stephen) using a stale hash from an earlier prep-job run gets rejected by Homebrew/winget's own hash-verification</td><td>Submission rejected, not silently accepted with a wrong hash (both ecosystems verify the hash against the actual downloaded bytes)</td><td>Prep jobs should be re-run against the final release artifacts immediately before Stephen prepares a submission, not relied on from an earlier CI run if the release was <code>--clobber</code>-updated since; documented in the answer sheet</td></tr><tr><td>Cask <code>zap trash:</code> paths (§1 #9, §9) are wrong — either incomplete (stale files survive uninstall) or, worse, overbroad (deletes a path CyberOS doesn't actually own, e.g. if the guessed preferences-plist name doesn't match what macOS actually wrote)</td><td>Not caught by <code>brew audit --cask</code>/<code>brew style --cask</code> (AC #1), which validate manifest syntax and metadata conventions, not runtime filesystem behavior; only caught by an actual <code>brew uninstall --zap --cask cyberos</code> test against a real installed copy</td><td>Silent data-cleanup bug shipped to end users — the failure mode is invisible until someone actually zap-uninstalls</td><td>§1 #9 makes this an explicit MUST-verify requirement precisely because AC #1's automated checks cannot catch it; the answer sheet (AC #6) records the result of a real zap-uninstall test as a required manual step, not an assumed pass</td></tr><tr><td>Two independent CI flags (<code>PKGMGR_CASK_RELEASE</code>, <code>PKGMGR_WINGET_RELEASE</code>) get accidentally coupled in a future <code>release-pkgmgr-pr.yml</code> refactor, so flipping one silently also runs the other</td><td>Regression caught only if a future PR's tests specifically re-verify both gates independently</td><td>Wrong preparation job runs unexpectedly (still not a submission, per §1 #6, so blast radius stays bounded even in this failure mode)</td><td>Same mitigation pattern as prior tasks in this batch: each gate's inert-by-default assertion (AC #4, both flags checked separately) is a standing regression test, not a one-time check</td></tr><tr><td>A future contributor, unaware of §1 #5/#6's stricter-than-TASK-APP-005 rule, adds a <code>wingetcreate submit</code>/<code>brew bump-cask-pr</code> call directly into <code>release-pkgmgr-pr.yml</code>, believing the existing <code>PKGMGR_*_RELEASE</code> flags make this safe to gate the same way TASK-APP-003/004's signing steps are gated</td><td>AC #3's structural grep-based guard starts failing the moment such a call appears, regardless of which flag or condition wraps it</td><td>Would silently convert a preparation-only job into an actual external-repo submission pipeline if merged unnoticed</td><td>AC #3 makes this a standing, automatically-enforced regression test rather than relying on institutional memory of §2's shared-monorepo blast-radius reasoning</td></tr><tr><td>Cask <code>app \"CyberOS.app\"</code> stanza assumes the <code>.dmg</code>'s internal app-bundle name matches exactly; a rename or Tauri config change could silently break this</td><td><code>brew install --cask cyberos</code> fails at the <code>app</code> stanza's copy step, caught by AC #1's local audit only if the audit actually mounts and inspects the <code>.dmg</code> (some <code>brew audit</code> checks are metadata-only and would not catch this)</td><td>Cask technically passes lint but fails to actually install for end users</td><td>Documented in the answer sheet as a required manual end-to-end install test (<code>brew install --cask ./cyberos.rb</code> against a locally-built <code>.dmg</code>) before ever proposing the PR, not relied on from <code>brew audit</code>/<code>brew style</code> alone</td></tr><tr><td>A future task that adopts <code>wingetcreate</code>/Homebrew PR-automation tooling (§9) provisions an overly broad GitHub PAT (e.g. full <code>repo</code> scope instead of the minimum fork-and-PR permission set the chosen tool actually needs)</td><td>Not caught by anything in this task's own CI, since this task ships no such automation itself — would only surface in a future security review of whatever token that later task actually provisions</td><td>An unnecessarily powerful credential sits in CI secrets, widening blast radius if ever leaked, disproportionate to what version-bump PR automation actually requires</td><td>§11 documents this as a requirement for whichever future task adds the automation: scope the PAT to the minimum permission set the chosen tool needs, not the broadest available token type</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li><strong>This task's \"never auto-submit\" rule is deliberately stricter than TASK-APP-005's Flathub gate</strong> (§2 explains why: shared-monorepo blast radius vs. a per-app repo) — implementers extending this pattern to future package-manager integrations should re-derive the right posture from each ecosystem's actual repo structure, not copy this task's specific rule mechanically without re-checking whether the same reasoning applies.</li><li><strong>Both manifest skeletons in §3 use placeholder hash strings (<code>REPLACE_WITH_SHA256_OF_RELEASE_DMG</code>, <code>REPLACE_WITH_SHA256_OF_RELEASE_INSTALLER</code>) rather than a plausible-looking fabricated hash</strong> — consistent with TASK-APP-004's <code>CHANGEME-PENDING-PARTNER-CENTER-RESERVATION</code> placeholder pattern, an unambiguous placeholder is safer than a real-looking-but-fake value that could be mistaken for actual data.</li><li><strong>The NSIS silent-switch and winget-validation-command open questions (§9) are both flagged for the same underlying reason</strong>: this task's authoring did not execute either tool against a real CyberOS build, so asserting exact behavior would cross from \"documented design\" into \"fabricated verification,\" which the authoring discipline forbids regardless of how conventional the assumed defaults are.</li><li><strong>If update automation via <code>wingetcreate</code>/Homebrew PR tooling is adopted later</strong> (§1 #8, §9, and the PAT-scope failure-mode row above), the resulting GitHub PAT MUST be stored using the same secret-manager pattern already established elsewhere in this batch (e.g. <code>MSSTORE_CERT_BASE64</code>, <code>MAS_CERT_BASE64</code>) — this task adds no such secret itself, consistent with §1 #6's stricter never-automate-submission posture.</li><li><strong>This task's answer sheet (<code>docs/deploy/package-manager-submission.md</code>) and TASK-APP-005's Linux submission answer sheet should be reviewed together, not independently</strong>, before Stephen's first actual PR to any of the four external repos (Snap Store, Flathub, <code>homebrew/homebrew-cask</code>, <code>microsoft/winget-pkgs</code>) — several of the human-confirmed items across both documents (stable download URL shape, VERSION-stamping mechanism, hash re-derivation discipline) trace back to the same underlying CI facts, and reviewing them together reduces the chance of an inconsistent answer between the two.</li></ul>\n<p><em>End of TASK-APP-006.</em></p>";
