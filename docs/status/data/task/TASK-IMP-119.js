window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-119"]="<h1>TASK-IMP-119 - a declared cone is a promise nothing checks</h1>\n<h2>Summary</h2>\n<p>Audit a task's ACTUAL writes against its DECLARED cone at the <code>implementing -&gt; ready_to_review</code> flip, and report every escape. batch-select plans parallelism from cones; nothing verifies them afterwards, so the safety argument for every swarm rests on an unchecked input.</p>\n<h2>Problem</h2>\n<p>batch-select proves two tasks are independent by intersecting their cones (<code>new_files ∪ modified_files ∪ service</code>). That proof is only as true as the cones. Nothing compares a cone to what the task actually wrote.</p>\n<p>Measured on the 2026-07-17 batch (TASK-IMP-110 + TASK-IMP-114), three files escaped BOTH declared cones:</p>\n<table><thead><tr><th>file</th><th>owner</th><th>why it escaped</th></tr></thead><tbody><tr><td><code>tools/install/docs-tools/workflow-improve.mjs</code></td><td>110</td><td>the cone declared a SKILL.md and a test; §1.4 says \"the tool MUST NOT write\" and AC 1-6 are shell-tested behaviour. Prose cannot satisfy them. The executable was never declared.</td></tr><tr><td><code>tools/install/tests/test_full_sdp_payload.sh</code></td><td>110</td><td>hard-asserts 53 vendored skills; 110's DECLARED build.sh edit makes it 54. An undeclared file broken by a declared change.</td></tr><tr><td><code>tools/docs-site/tests/test_render_status_hub.sh</code></td><td>114</td><td>AC 3 and AC 5 both cite it; <code>modified_files</code> omits it.</td></tr></tbody></table>\n<p>None collided, so the batch was safe by luck rather than by proof. <code>test_batch_economics.sh</code> (114, inside <code>tools/install</code>) would have raced TASK-IMP-106 (<code>service: tools/install</code>) had 106 been in the batch - and batch-select could not have known, because 114's cone said <code>modules/cuo</code>.</p>\n<p>This is the third level of one defect, all found the same day:</p>\n<ol><li>TASK-IMP-104: declared <code>install.sh</code>, edited two more files inside its service. Fix: fold <code>service</code> into the cone.</li><li>This afternoon: an undeclared cone is the EMPTY SET, which intersects nothing, so a silent spec was provably independent of everything and joined every batch. Fix: fail closed, ship alone.</li><li>This task: a DECLARED cone that does not match the writes. Nothing looks.</li></ol>\n<p>Levels 1 and 2 widened the net. This one closes the loop: it checks the input the whole mechanism trusts.</p>\n<h2>Proposed Solution</h2>\n<p><code>docs-tools/cone-audit.mjs</code>: given a task id and a base ref, diff <code>git diff --name-only &lt;base&gt;..HEAD</code> against the task's declared cone and report every path outside it. REPORT, not refuse - a write discovered mid-implementation is often a real finding (all three of today's were), and the remedy is to amend the cone, which is a spec edit and therefore a human's call.</p>\n<p>Wire it into ship-tasks at the <code>implementing -&gt; ready_to_review</code> flip, where the diff exists and the spec is still open for amendment.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Refuse the flip on any escape. Rejected for now: today's three escapes were all correct discoveries about wrong specs, and a hard block would have stopped three good implementations to enforce three bad cones. Revisit once escape rates are known - which is itself a §11d question.</li><li>Derive the cone from the diff instead of declaring it. Rejected: then the cone is whatever happened, and it can never be wrong, so batch-select's proof becomes circular. A cone is a prediction; its value is that it can be falsified.</li><li>Lint the cone at authoring. Rejected: unknowable. The author cannot know at PLAN time which files the implementation will touch - that is what implementation is.</li><li>Do nothing; escapes were benign. Rejected: they were benign by luck. The near-miss is on the record above.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Run against the 2026-07-17 batch, it names exactly the three files in the table and no others. A tool that cannot find the defect that motivated it is decoration.</li><li>Escapes become visible per batch, so the escape RATE becomes a number rather than an anecdote - which is what a decision about refusing needs.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li>Tools used: Claude (Fable 5), 2026-07-17 hardening run, via create-tasks.</li><li>Scope: spec drafted by the agent from a defect it measured mechanically while acting as the swarm parent. Both sub-agents disclosed their own escapes unprompted; the parent's diff check confirmed three and found no others. The cone data in the table is measured, not estimated.</li><li>Human review: @stephencheng recorded the decision to task this (report, not refuse) at the batch's close gate.</li></ul>\n<h2>Dependencies</h2>\n<p>None. batch-select (TASK-IMP-104, v2.8.0) already parses cones; <code>relUnderRoot</code> already exists in docs-tools. This adds a reader, no new guard and no new rule.</p>\n<h2>Scope</h2>\n<p>In scope: <code>docs-tools/cone-audit.mjs</code>, its suite, the build.sh vendor entry, and the ship-tasks §11a wiring note at the <code>implementing -&gt; ready_to_review</code> flip.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Refusing the flip. This reports. Turning a report into a gate is a separate decision that wants the escape rate first.</li><li>Amending any existing spec's cone. The three files above are findings for their owners, not this task's work.</li><li>Cone declaration at authoring time. Unknowable by construction; see Alternatives.</li></ul>\n<h2>1. Clauses</h2>\n<p>1.1 <code>cone-audit.mjs &lt;task-id&gt; [--base &lt;ref&gt;]</code> MUST read the task's declared cone from frontmatter (<code>new_files ∪ modified_files ∪ service</code>) and the actual writes from <code>git diff --name-only</code>, and report every written path not inside the cone. Test: <code>t01_escape_is_named</code></p>\n<p>1.2 A path is INSIDE the cone if it equals a declared entry or is nested under one. This MUST match batch-select's own containment rule exactly - two tools disagreeing about what a cone contains is worse than neither existing. Test: <code>t02_containment_matches_batch_select</code></p>\n<p>1.3 An UNDECLARED cone MUST report every write as an escape, not zero. Empty is not \"contains everything\" any more than it is \"contains nothing\" - it is unknown, and batch-select already refuses it. Test: <code>t03_undeclared_cone_escapes_everything</code></p>\n<p>1.4 <code>(none)</code> MUST be filtered from the cone, matching batch-select:51. A literal placeholder is not a path. Test: <code>t04_placeholder_is_not_a_path</code></p>\n<p>1.5 The tool MUST NOT write, refuse, or flip anything. It reports and exits 0 with escapes, 2 on usage. An escape is a FINDING for a human, not a failure. Test: <code>t05_reports_never_refuses</code></p>\n<p>1.6 Deterministic: same repo state + same args = byte-identical output. No wall clock in the artefact. Test: <code>t06_deterministic</code></p>\n<p>1.7 A path outside the repo root, or an unreadable spec, MUST be refused and named - never silently skipped. Test: <code>t07_guard_refuses_and_names</code></p>\n<h2>3. Edge case matrix</h2>\n<table><thead><tr><th>#</th><th>Category</th><th>Trigger</th><th>Expected</th><th>Test</th></tr></thead><tbody><tr><td>1</td><td>NULL/EMPTY</td><td>task wrote nothing</td><td>zero escapes, exit 0</td><td>t01</td></tr><tr><td>2</td><td>NULL/EMPTY</td><td>cone declared, diff empty</td><td>zero escapes</td><td>t01</td></tr><tr><td>3</td><td>NULL/EMPTY</td><td>cone entirely absent</td><td>every write is an escape</td><td>t03</td></tr><tr><td>4</td><td>BOUNDS</td><td>write exactly equals a declared file</td><td>inside</td><td>t02</td></tr><tr><td>5</td><td>BOUNDS</td><td>write nested under a declared service</td><td>inside</td><td>t02</td></tr><tr><td>6</td><td>BOUNDS</td><td>declared file, write is its PARENT dir</td><td>escape - narrower does not cover wider</td><td>t02</td></tr><tr><td>7</td><td>MALFORMED</td><td><code>(none)</code> in new_files</td><td>filtered, not treated as a path</td><td>t04</td></tr><tr><td>8</td><td>MALFORMED</td><td>cone entry with a trailing slash</td><td>normalised, matches batch-select</td><td>t02</td></tr><tr><td>9</td><td>MALFORMED</td><td>renamed file (git R status)</td><td>both old and new path considered</td><td>t01</td></tr><tr><td>10</td><td>CONCURRENT</td><td>run mid-swarm while a sibling writes</td><td>reports only THIS task's diff vs base</td><td>t06</td></tr><tr><td>11</td><td>SECURITY</td><td>task-id resolves outside the repo</td><td>REFUSED, named, not executed</td><td>t07</td></tr><tr><td>12</td><td>SECURITY</td><td>spec path is a symlink out of the corpus</td><td>REFUSED</td><td>t07</td></tr><tr><td>13</td><td>DEGRADATION</td><td>git absent / base ref missing</td><td>refuse and say so; never report zero escapes</td><td>t07</td></tr><tr><td>14</td><td>DEGRADATION</td><td>spec unreadable</td><td>refuse; unreadable is not \"clean\"</td><td>t07</td></tr></tbody></table>\n<h2>4. Out of scope / non-goals</h2>\n<p>See \"## Scope -&gt; ### Out of scope / Non-Goals\" above.</p>\n<h2>Acceptance criteria</h2>\n<ul><li>AC1 (traces_to #1.1): escapes are named. Test: <code>t01_escape_is_named</code>.</li><li>AC2 (traces_to #1.2): containment matches batch-select on a shared table of cases. Test: <code>t02_containment_matches_batch_select</code>.</li><li>AC3 (traces_to #1.3): an undeclared cone escapes everything. Test: <code>t03_undeclared_cone_escapes_everything</code>.</li><li>AC4 (traces_to #1.4): <code>(none)</code> is filtered. Test: <code>t04_placeholder_is_not_a_path</code>.</li><li>AC5 (traces_to #1.5): reports, never refuses or writes. Test: <code>t05_reports_never_refuses</code>.</li><li>AC6 (traces_to #1.6, #1.7): deterministic; guard refuses and names. Tests: <code>t06_deterministic</code>, <code>t07_guard_refuses_and_names</code>.</li><li>AC7: run against the 2026-07-17 batch (base = the batch's first commit), it names exactly <code>workflow-improve.mjs</code>, <code>test_full_sdp_payload.sh</code>, <code>test_render_status_hub.sh</code> and no others. A tool that cannot find the case that motivated it is decoration. Test: <code>t08_finds_the_motivating_case</code>.</li></ul>";
