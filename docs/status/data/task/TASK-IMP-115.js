window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-115"]="<h1>TASK-IMP-115: Effort tiering - advisory judgment metadata</h1>\n<h2>Summary</h2>\n<p>Every workflow step runs at whatever the host gives it. Nothing says which steps deserve expensive reasoning (task-audit's judgment half, spike options) and which are near-mechanical (backlog-mutate, coverage-scope - already scripts, correctly). Annotate each <code>skill_chain</code> step with <code>judgment: high | medium | mechanical</code> as ADVISORY metadata a host may use to route. No model strings, ever.</p>\n<h2>Problem</h2>\n<p>CyberOS is host-agnostic and gives a host no information to route with. The article's numbers are the argument: the same outcome costs $2.56/day or $34/day depending on which model reads the quiet ticks. This run's two spend cutoffs are the same failure with a different face.</p>\n<p>The constraint is equally clear: a <code>claude-fable-5</code> literal in a skill is a rule that expires. The payload cannot name models, prices, or effort levels - those are the host's facts, accurate the day they are written and wrong soon after.</p>\n<h2>Proposed Solution</h2>\n<p>One optional field per <code>skill_chain</code> step: <code>judgment: high | medium | mechanical</code>. <code>mechanical</code> means a deterministic helper does the work and a model is not deciding anything. <code>high</code> means the step's output is a judgment the workflow depends on. A host MAY route on it; nothing in the payload reads it. That is the whole change: information, not instruction.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Model strings or effort names per step. Rejected: host-specific and stale on arrival. The payload describes the work, the host picks the worker.</li><li>A cost budget in the workflow. Rejected: a payload that refuses to run because of a number it cannot verify is a payload that gets edited.</li><li>Infer from the step's skill name. Rejected: it is exactly the kind of implicit rule this run keeps finding wrong; if it matters, write it down.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: every <code>skill_chain</code> step carries a valid <code>judgment</code>, and the mechanical ones are precisely the steps whose work is done by a docs-tools helper - suite-asserted. Baseline: no such information exists.</li><li>Guardrail: no model string, price, or effort name appears anywhere in the payload - suite-asserted as a negative.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the <code>judgment</code> field on ship-tasks' skill_chain steps, its documentation, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Any routing, model selection, or effort setting - the payload informs, the host decides.</li><li>Model names, prices, or effort levels in the payload.</li><li>Applying the field to create-tasks or plan (extend once this proves useful).</li></ul>\n<h2>Dependencies</h2>\n<p>None. Additive and optional.</p>\n<p><strong>Serialisation note:</strong> touches <code>ship-tasks.md</code> (shared with 108, 109, 113, 114) and <code>test_workflow_evolution.py</code> (shared with 108). Parent-serialised per §11a.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md §11 IMP-31, adopting the guide's insight while rejecting its host-specific encoding.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 Each <code>skill_chain</code> step in ship-tasks MUST carry <code>judgment: high | medium | mechanical</code>.</li><li>1.2 <code>mechanical</code> MUST mean the step's work is performed by a deterministic helper with no model judgment in the result.</li><li>1.3 The field MUST be documented as ADVISORY: a host MAY route on it, and nothing in the payload may read it to decide anything.</li><li>1.4 No model string, price, or effort name may appear in the payload as a result of this task.</li><li>1.5 A step whose judgment level is genuinely ambiguous MUST be <code>medium</code> rather than guessed high - overstating a step's need is how the expensive default returns.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1) - every skill_chain step carries a value from the enum - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_every_step_has_judgment</code></li><li>[ ] AC 2 (traces_to: #1.2) - every step marked mechanical is one whose skill delegates to a docs-tools helper - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_mechanical_steps_are_helper_backed</code></li><li>[ ] AC 3 (traces_to: #1.4) - no model string, price, or effort literal in the payload - test: <code>modules/cuo/tests/test_workflow_evolution.py::test_no_host_specific_literals</code></li><li>[ ] AC 4 (traces_to: #1.3) - the field is documented as advisory and nothing in the payload reads it - verify: recorded grep in the gate log (a negative structural claim; same rationale as TASK-IMP-090 AC 1).</li><li>[ ] AC 5 (traces_to: #1.5) - no step is marked <code>high</code> without a named reason in the review; ambiguous steps carry <code>medium</code> - verify: recorded reviewer walk of the assigned levels in the gate log (a judgment claim about a prose table - no suite can decide whether a level was guessed; same rationale as TASK-IMP-090 AC 1).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>A step that is mechanical today and judgment tomorrow (a helper replaced by a model): the field is wrong until someone updates it, and AC 2's test is what catches the drift.</li><li>The conditional steps (0, 4, 7, 15): they carry the field like any other; a step that may not run still has a judgment level when it does.</li><li>A host that ignores the field entirely: correct, and the default. Advisory means ignorable.</li><li>A future workflow (plan, per TASK-IMP-111) adding steps: out of scope here, and the field's documentation says where it would extend.</li><li>Security-class: one enum field in a markdown table. No execution surface.</li></ul>";
