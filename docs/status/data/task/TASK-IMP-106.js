window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-106"]="<h1>TASK-IMP-106: Uninstall summary names what it kept</h1>\n<h2>Summary</h2>\n<p><code>uninstall.sh</code> deliberately keeps the operator's corpus, status page, CHANGELOG, and BRAIN - the right default - and never says so. Someone who uninstalls expects the repo to be clean and finds task folders and memory still there, with no way to know it was intentional. Add a summary that names what was removed, what was kept and why, and how to remove the kept material by hand.</p>\n<h2>Problem</h2>\n<p><code>uninstall.sh</code> references <code>docs/tasks</code> twice and leaves it alone by design: the corpus is the operator's work, not the machine's. But the summary says nothing about it. This is the same failure shape as TASK-IMP-095 (gates.env clobbered silently) and TASK-IMP-096 (non-git install silently useless) - a correct default, undocumented at the exact moment it surprises someone - and both of those proved worth fixing.</p>\n<p>Silence at that moment has a specific cost: the operator either assumes the uninstall failed and re-runs it, or deletes the corpus by hand to \"finish the job\" and loses the work the default existed to protect.</p>\n<h2>Proposed Solution</h2>\n<p>Print a two-part summary on successful uninstall. Removed: the vendored machine and the agent surface, each named. Kept: <code>docs/tasks/</code> (your corpus), <code>docs/status/</code>, <code>CHANGELOG.md</code>, <code>.cyberos/memory</code> (BRAIN) - with a one-line reason and the verbatim command to remove them for an operator who means it. Names come from what the run actually did, not a hard-coded list, so the summary cannot drift from the behavior.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>Prompt to delete the corpus. Rejected: uninstall is frequently non-interactive, and a prompt that defaults to deleting an operator's work is a footgun with a confirmation step.</li><li>A <code>--purge</code> flag that removes everything. Rejected here as scope creep - the defect is silence, not a missing capability. Worth its own task if anyone asks.</li><li>Documentation only (GUIDE note). Rejected: the surprise happens at the terminal, and that is where the answer has to be.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: a successful uninstall names every kept path with a reason and the removal command - suite-asserted against the real run's output. Baseline: zero mentions today.</li><li>Guardrail: the summary reflects what the run did (derived, not hard-coded) - a kept path absent from the repo is not claimed as kept.</li></ul>\n<h2>Scope</h2>\n<p>In scope: the uninstall summary block, suite arms.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Changing what uninstall removes or keeps - the state machine is correct and this task must not touch it.</li><li>A purge flag.</li><li>The install-side summary (TASK-IMP-096 already covers its case).</li></ul>\n<h2>Dependencies</h2>\n<p>depends_on TASK-IMP-103: both edit <code>uninstall.sh</code>, and 103 adds the lock-removal branch whose outcome this summary must report. Serialised, not parallel. Per TASK-IMP-101's evidence gate, 103's coverage-gate artefact is the evidence.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMPROVEMENT_HANDOFF.md IMP-27, verified against uninstall.sh on merged main; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> scope approved at the 2026-07-17 PLAN gate; both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 On successful uninstall, the summary MUST name what was removed (the vendored machine and the agent surface entries the run actually removed).</li><li>1.2 The summary MUST name each kept path with a one-line reason: <code>docs/tasks/</code> (the corpus), <code>docs/status/</code>, <code>CHANGELOG.md</code>, <code>.cyberos/memory</code> (BRAIN).</li><li>1.3 The summary MUST print the verbatim command an operator can run to remove the kept material themselves.</li><li>1.4 Kept paths MUST be derived from what exists after the run - a path that is not present MUST NOT be claimed as kept.</li><li>1.5 The summary MUST NOT change what uninstall removes or keeps.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.2, #1.3) - uninstall on a populated repo prints removed entries, all four kept paths with reasons, and the manual-removal command - test: <code>tools/install/tests/test_install_hygiene.sh::t20_uninstall_summary_names_kept</code></li><li>[ ] AC 2 (traces_to: #1.4) - uninstall on a repo with no <code>docs/status/</code> does not claim it as kept - test: <code>tools/install/tests/test_install_hygiene.sh::t21_uninstall_summary_derived_not_hardcoded</code></li><li>[ ] AC 3 (traces_to: #1.5) - the set of files present after uninstall is byte-identical to today's behavior - test: <code>tools/install/tests/test_install_hygiene.sh::t22_uninstall_behavior_unchanged</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Uninstall on a repo that was never installed: no removal summary - it MUST NOT print a kept list for a machine that was not there (nothing was kept; nothing was removed).</li><li>BRAIN present but empty: still named as kept - the directory is the operator's, and its emptiness is not the uninstaller's judgment to make.</li><li>Corpus present but <code>docs/status/</code> absent (never rendered): per 1.4, status is omitted from the kept list rather than claimed.</li><li>A partially-removed machine from an interrupted earlier uninstall: the summary reports what this run removed, not what it wished it had removed.</li><li>Security-class: prints paths that already exist in the repo; interpolates no user-supplied string into a command. The printed removal command is documentation, never executed by the script.</li></ul>";
