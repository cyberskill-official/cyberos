window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REW-008"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The REW service <strong>MUST</strong> ship quarterly P3 distribution at <code>services/rew/src/p3/</code> consuming TASK-LEARN-007 VP shares + dual-sign + BP debit + P3 add to next payroll, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>distribution_status</code> against closed enum per DEC-2221.</li></ol>\n<ol><li><strong>MUST</strong> calculate at <code>calculator.rs::calc(tenant, quarter, fund_vnd)</code> per DEC-2220:</li></ol>\n<ul><li>Receive VP shares from TASK-LEARN-007 handoff (sums to 1.0)</li><li>Per member: payout_vnd = fund_vnd * vp_share</li></ul>\n<ol><li><strong>MUST</strong> require CEO + CFO dual-sign at <code>dual_sign_gate.rs::can_execute(distribution)</code> per DEC-2222 — same-person rejected.</li></ol>\n<ol><li><strong>MUST</strong> be idempotent per DEC-2223 — UNIQUE(tenant, quarter).</li></ol>\n<ol><li><strong>MUST</strong> on execute:</li></ol>\n<ul><li>Debit BP balance (TASK-REW-007 credit_p3_distribution converted to BP-equiv)</li><li>Add to next payroll (TASK-REW-005) as P3 income kind</li></ul>\n<ol><li><strong>MUST</strong> define tables at migration <code>0008</code>: ```sql CREATE TABLE rew_p3_distributions ( distribution_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, quarter CHAR(7) NOT NULL, fund_vnd BIGINT NOT NULL CHECK (fund_vnd &gt; 0), status TEXT NOT NULL DEFAULT 'drafted' CHECK (status IN ('drafted','ceo_signed','cfo_signed','executed','paid','dismissed')), ceo_signed_by UUID, ceo_signed_at TIMESTAMPTZ, cfo_signed_by UUID, cfo_signed_at TIMESTAMPTZ, executed_at TIMESTAMPTZ, paid_at TIMESTAMPTZ, trace_id CHAR(32), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (tenant_id, quarter) ); ALTER TABLE rew_p3_distributions ENABLE ROW LEVEL SECURITY; CREATE POLICY p3_dist_rls ON rew_p3_distributions USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON rew_p3_distributions FROM cyberos_app; GRANT UPDATE (status, ceo_signed_by, ceo_signed_at, cfo_signed_by, cfo_signed_at, executed_at, paid_at) ON rew_p3_distributions TO cyberos_app;</li></ol>\n<p>   CREATE TABLE rew_p3_member_payouts (      payout_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      distribution_id UUID NOT NULL REFERENCES rew_p3_distributions(distribution_id),      member_id UUID NOT NULL,      vp_share NUMERIC(10,9) NOT NULL,      payout_vnd BIGINT NOT NULL,      PRIMARY KEY_ALT UNIQUE (distribution_id, member_id)    );    ALTER TABLE rew_p3_member_payouts ENABLE ROW LEVEL SECURITY;    CREATE POLICY payouts_rls ON rew_p3_member_payouts      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON rew_p3_member_payouts FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/rew/p3-distributions                  (CEO drafts; provides fund_vnd) POST /v1/rew/p3-distributions/{id}/ceo-sign POST /v1/rew/p3-distributions/{id}/cfo-sign POST /v1/rew/p3-distributions/{id}/execute     (auto if both signed) GET  /v1/rew/p3-distributions/{id}             (status + payouts) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-2224. PII per TASK-MEMORY-111: payout amounts SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from draft → sign → execute → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> execute without dual-sign per DEC-2222.</li></ol>\n<ol><li><strong>MUST NOT</strong> duplicate quarter per DEC-2223.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why VP share input (DEC-2220)?</strong> Fairness — distribution proportional to contribution per TASK-LEARN-007.</p>\n<p><strong>Why CEO + CFO dual-sign (DEC-2222)?</strong> CEO sets strategic fund; CFO confirms financial OK.</p>\n<p><strong>Why idempotent (DEC-2223)?</strong> Quarter is single financial event; double-pay = disaster.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample distribution draft:</p>\n<pre><code class=\"language-json\">POST /v1/rew/p3-distributions\n{\n  \"quarter\": \"2026-Q2\",\n  \"fund_vnd\": 500000000\n}</code></pre>\n<p>Sample executed status:</p>\n<pre><code class=\"language-json\">{\n  \"distribution_id\": \"uuid\",\n  \"quarter\": \"2026-Q2\",\n  \"fund_vnd\": 500000000,\n  \"status\": \"executed\",\n  \"payouts\": [\n    {\"member_id\": \"uuid\", \"vp_share\": 0.085, \"payout_vnd\": 42500000},\n    {\"member_id\": \"uuid\", \"vp_share\": 0.062, \"payout_vnd\": 31000000}\n  ]\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>distribution_status enum cardinality 6</strong>. 2. <strong>fund_vnd &gt; 0 CHECK</strong>. 3. <strong>VP shares from TASK-LEARN-007</strong>. 4. <strong>CEO+CFO dual-sign required</strong>. 5. <strong>Same-person rejected</strong>. 6. <strong>UNIQUE(tenant, quarter)</strong>. 7. <strong>5 memory audit kinds emitted</strong>. 8. <strong>PII scrubbed (amounts SHA256)</strong>. 9. <strong>RLS denies cross-tenant</strong>. 10. <strong>CEO-only draft</strong>. 11. <strong>Trace_id preserved</strong>. 12. <strong>Append-only via REVOKE except status cols</strong>. 13. <strong>bigint VND</strong>. 14. <strong>vp_share NUMERIC(10,9)</strong>. 15. <strong>Per-member payout row</strong>. 16. <strong>Execute debits BP ledger</strong>. 17. <strong>P3 added to next payroll</strong>. 18. <strong>Status workflow enforced</strong>. 19. <strong>Dismiss allowed pre-execute</strong>. 20. <strong>Sum of payouts ≈ fund_vnd (±1 VND tolerance)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn dual_sign_required_for_execute() {\n    let ctx = TestContext::with_drafted_distribution().await;\n    ctx.ceo_sign(ctx.dist_id).await;\n    let r = ctx.try_execute(ctx.dist_id).await;\n    assert!(r.is_err());  // CFO missing\n    ctx.cfo_sign(ctx.dist_id).await;\n    let r2 = ctx.execute(ctx.dist_id).await;\n    assert!(r2.is_ok());\n}\n\n#[tokio::test]\nasync fn payouts_sum_matches_fund() {\n    let ctx = TestContext::with_executed_distribution(dec!(500_000_000)).await;\n    let payouts = ctx.fetch_payouts(ctx.dist_id).await;\n    let total: i64 = payouts.iter().map(|p| p.payout_vnd).sum();\n    assert!((total - 500_000_000).abs() &lt;= 1);\n}\n\n#[tokio::test]\nasync fn idempotent_quarter() {\n    let ctx = TestContext::with_q1_distribution().await;\n    let r = ctx.try_draft_distribution(\"2026-Q1\").await;\n    assert!(r.is_err());  // UNIQUE\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-REW-007. <strong>Cross-module:</strong> TASK-LEARN-007 (VP shares), TASK-REW-005 (payroll injection), TASK-AUTH-101 (CEO/CFO), TASK-MCP-007 (trigger cron), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>TASK-LEARN-007 not emitted</td><td>catch</td><td>sev-2; await</td><td>manual trigger</td></tr><tr><td>One signer missing</td><td>gate</td><td>reject execute</td><td>wait</td></tr><tr><td>Same-person dual-sign</td><td>validate</td><td>403</td><td>different signer</td></tr><tr><td>Sum of payouts != fund</td><td>post-condition</td><td>sev-1; reject</td><td>bug fix</td></tr><tr><td>Duplicate quarter</td><td>UNIQUE</td><td>409</td><td>use different</td></tr><tr><td>BP debit fail</td><td>rollback</td><td>sev-1</td><td>retry</td></tr><tr><td>Payroll injection fail</td><td>sev-1</td><td>manual fix</td><td>inherent</td></tr><tr><td>Cross-tenant query</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Decimal precision</td><td>bigint VND + rust_decimal share</td><td>inherent</td><td>inherent</td></tr><tr><td>Concurrent execute</td><td>UPDATE WHERE pending</td><td>first wins</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Calculator: per_member_vnd = fund_vnd * vp_share; round to nearest VND; allocate residual to highest-share member to ensure sum matches.</li><li>§11.2 BP debit converts VND to BP-equiv via TASK-HR-005 policy <code>bp_vnd_rate</code>.</li><li>§11.3 memory audit body: distribution_id, quarter, members_count; amounts SHA256.</li><li>§11.4 Cron trigger: quarter+1d via TASK-MCP-007 reminding CEO to draft.</li><li>§11.5 Drafted state allows CEO to revise fund_vnd before signing.</li></ul>\n<hr>\n<p><em>End of TASK-REW-008 spec.</em></p>";
