window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TIME-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The TIME service <strong>MUST</strong> ship timer start/stop primitive at <code>services/time/src/timer/</code> with single-active enforcement, 10-min idle detection, 15-min commit snap, logout auto-commit, 5-state enum, and 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> define closed <code>timer_state</code> enum: <code>('running','paused_idle','paused_manual','committed','abandoned')</code> per DEC-1383. Cardinality 5.</li></ol>\n<ol><li><strong>MUST</strong> define <code>timers</code> table at migration <code>0002</code>: <code>(timer_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, member_subject_id UUID NOT NULL, engagement_id UUID NOT NULL, project_id UUID, task_id UUID, description TEXT, state timer_state NOT NULL DEFAULT 'running', started_at TIMESTAMPTZ NOT NULL DEFAULT now(), last_heartbeat_at TIMESTAMPTZ NOT NULL DEFAULT now(), idle_paused_at TIMESTAMPTZ, manual_paused_at TIMESTAMPTZ, committed_at TIMESTAMPTZ, committed_entry_id UUID, abandoned_at TIMESTAMPTZ, trace_id CHAR(32))</code>. Partial unique <code>(member_subject_id) WHERE state IN ('running','paused_idle','paused_manual')</code> enforces single-active per DEC-1380.</li></ol>\n<ol><li><strong>MUST</strong> enforce RLS scoped to <code>tenant_id</code> AND <code>member_subject_id = current_setting('auth.subject_id')::uuid</code> (Members see own timers only).</li></ol>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/time/timer/start</code> body <code>{ engagement_id, project_id?, task_id?, description? }</code>. Handler:</li></ol>\n<ul><li>Validates engagement_id in Member's engagements.</li><li>Checks existing active timer per §1 #5 — if exists, auto-commits it first.</li><li>INSERTs new timer row with state='running'.</li><li>Emits <code>time.timer_started</code> sev-3.</li></ul>\n<ol><li><strong>MUST</strong> auto-stop prior active timer per DEC-1380 on new start. Sequence:</li></ol>\n<ul><li>SELECT existing active timer for member.</li><li>If found: invoke commit handler (§1 #7) — produces TIME entry + state='committed'.</li><li>Then proceed to new INSERT.</li><li>Both operations in single transaction.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/time/timer/heartbeat</code> for client keep-alive. Body <code>{ timer_id }</code>. Handler:</li></ol>\n<ul><li>UPDATE <code>last_heartbeat_at = now()</code> if state='running'.</li><li>Idempotent at sub-second rate.</li><li>Returns current state for client UI.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/time/timer/stop</code> body <code>{ timer_id, final_description? }</code>. Handler:</li></ol>\n<ul><li>Validates timer belongs to caller.</li><li>Computes duration = <code>now() - started_at - sum(pause durations)</code>.</li><li>Snaps duration UP to nearest 15-min per DEC-1382.</li><li>Creates TIME entry via TASK-TIME-001 with snapped duration.</li><li>Transitions timer to state='committed' + <code>committed_entry_id</code> populated.</li><li>Emits <code>time.timer_committed</code> sev-2.</li></ul>\n<ol><li><strong>MUST</strong> detect idle per DEC-1381 via <code>idle_detector.rs::run_watchdog()</code>. Scheduled job runs every 60s:</li></ol>\n<ul><li>SELECT timers WHERE state='running' AND <code>last_heartbeat_at &lt; now() - 10 min</code>.</li><li>For each: transition to state='paused_idle' + <code>idle_paused_at = now()</code>.</li><li>Emits <code>time.timer_idle_paused</code> sev-3.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/time/timer/resume</code> body <code>{ timer_id, idle_decision }</code> where <code>idle_decision ∈ {include, exclude, partial}</code>. Handler:</li></ol>\n<ul><li>Validates state IN ('paused_idle', 'paused_manual').</li><li>Per idle_decision: include = re-start timer including idle period; exclude = subtract idle period from final duration; partial = prompt for split.</li><li>Transitions state='running' + <code>last_heartbeat_at = now()</code>.</li></ul>\n<ol><li><strong>MUST</strong> expose <code>POST /v1/time/timer/pause</code> body <code>{ timer_id, reason? }</code>. Handler:</li></ol>\n<ul><li>Transitions state='paused_manual' + <code>manual_paused_at = now()</code>.</li></ul>\n<ol><li><strong>MUST</strong> auto-commit on logout per DEC-1384. TASK-AUTH-004 logout handler invokes <code>time::timer::commit_all_for_subject(subject_id)</code>:</li></ol>\n<ul><li>SELECT timers WHERE member_subject_id=$1 AND state IN ('running','paused_idle','paused_manual').</li><li>For each: invoke commit handler.</li></ul>\n<ol><li><strong>MUST</strong> snap duration UP to nearest 15-min per DEC-1382. Implementation: <code>let snapped_seconds = ((raw_seconds + 899) / 900) * 900</code>.</li></ol>\n<ol><li><strong>MUST</strong> support timer abandon at <code>POST /v1/time/timer/abandon</code> body <code>{ timer_id, reason }</code>. Transitions state='abandoned' WITHOUT creating TIME entry. Use case: accidental timer start. Emits <code>time.timer_abandoned</code> sev-3.</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-1385:</li></ol>\n<ul><li><code>time.timer_started</code> (sev-3)</li><li><code>time.timer_stopped</code> (sev-3 — generic; logout path)</li><li><code>time.timer_idle_paused</code> (sev-3)</li><li><code>time.timer_committed</code> (sev-2 — material commercial)</li><li><code>time.timer_abandoned</code> (sev-3)</li></ul>\n<ol><li><strong>MUST</strong> PII-scrub <code>description</code> via TASK-MEMORY-111 — SHA256 in chain; raw in DB.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id end-to-end.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow multi-active timer per DEC-1380 (partial unique enforces).</li></ol>\n<ol><li><strong>MUST NOT</strong> snap duration DOWN (always round UP per DEC-1382 — Member-favourable).</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale)</h2>\n<p><strong>Why single-active timer (§1 #5, DEC-1380)?</strong> Multi-timer = dual-counting same wall-clock period. Industry standard (Toggl, Harvest, Clockify) all enforce single-active.</p>\n<p><strong>Why 15-min snap UP (§1 #12, DEC-1382)?</strong> Industry billing convention; rounding down would systematically undercount Member time. Snap-up = Member-favourable + reduces line-item count.</p>\n<p><strong>Why 10-min idle detection (§1 #8, DEC-1381)?</strong> Member walks away from desk. 10 min = enough for short bathroom break + not so long that forgotten timer accrues hours.</p>\n<p><strong>Why logout auto-commit (§1 #11, DEC-1384)?</strong> Orphan timers across sessions = data quality nightmare. End-of-session commit = clean state.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-sql\">-- 0002_timers.sql\nCREATE TYPE timer_state AS ENUM ('running','paused_idle','paused_manual','committed','abandoned');\n\nCREATE TABLE timers (\n  timer_id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  member_subject_id UUID NOT NULL,\n  engagement_id UUID NOT NULL,\n  project_id UUID,\n  task_id UUID,\n  description TEXT,\n  state timer_state NOT NULL DEFAULT 'running',\n  started_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  last_heartbeat_at TIMESTAMPTZ NOT NULL DEFAULT now(),\n  idle_paused_at TIMESTAMPTZ,\n  manual_paused_at TIMESTAMPTZ,\n  total_pause_seconds INT NOT NULL DEFAULT 0,\n  committed_at TIMESTAMPTZ,\n  committed_entry_id UUID,\n  abandoned_at TIMESTAMPTZ,\n  abandon_reason TEXT,\n  trace_id CHAR(32)\n);\nCREATE UNIQUE INDEX uniq_active_timer_per_member\n  ON timers(member_subject_id)\n  WHERE state IN ('running','paused_idle','paused_manual');\nCREATE INDEX idx_timers_heartbeat ON timers(last_heartbeat_at) WHERE state = 'running';\nALTER TABLE timers ENABLE ROW LEVEL SECURITY;\nCREATE POLICY timers_rls ON timers\n  USING (tenant_id = current_setting('auth.tenant_id')::uuid\n         AND member_subject_id = current_setting('auth.subject_id')::uuid)\n  WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid\n              AND member_subject_id = current_setting('auth.subject_id')::uuid);\nREVOKE DELETE ON timers FROM cyberos_app;\nGRANT UPDATE (state, last_heartbeat_at, idle_paused_at, manual_paused_at,\n              total_pause_seconds, committed_at, committed_entry_id, abandoned_at, abandon_reason)\n  ON timers TO cyberos_app;</code></pre>\n<p>Endpoints:</p>\n<pre><code class=\"language-text\">POST   /v1/time/timer/start\nPOST   /v1/time/timer/heartbeat\nPOST   /v1/time/timer/pause\nPOST   /v1/time/timer/resume\nPOST   /v1/time/timer/stop\nPOST   /v1/time/timer/abandon\nGET    /v1/time/timer/current</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>timer_state cardinality 5</strong>.</li><li><strong>Single active per Member</strong> — starting timer with existing active auto-commits prior.</li><li><strong>15-min snap UP</strong> — 23-min timer commits as 30-min entry.</li><li><strong>Logout auto-commits</strong> — session end transitions running timers to 'committed'.</li><li><strong>10-min idle pause</strong> — heartbeat absent 10 min → state='paused_idle'.</li><li><strong>Resume include vs exclude</strong> — <code>idle_decision='exclude'</code> subtracts idle from duration.</li><li><strong>Abandon no entry</strong> — abandoned timer produces no TIME entry.</li><li><strong>5 memory audit kinds emitted</strong>.</li><li><strong>RLS Member-scoped</strong> — caller sees own timers only.</li><li><strong>Heartbeat idempotent</strong> — repeated heartbeats update last_heartbeat_at without errors.</li><li><strong>Engagement membership validated</strong> — timer for non-member engagement → 403.</li><li><strong>PII scrub</strong> — description_sha256 in chain only.</li><li><strong>Trace_id end-to-end</strong>.</li><li><strong>Cross-tenant denied</strong> — RLS rejects.</li><li><strong>Snap respects pause time</strong> — running 60m + paused 15m + running 30m = 90m work; snaps to 90m exactly (multiple of 15).</li><li><strong>Project_id optional</strong> — timer without project_id allowed (general engagement work).</li><li><strong>Audit emission on each transition</strong> — every state change emits an audit.</li><li><strong>Concurrent start race</strong> — two simultaneous starts → partial unique constraint fires; one wins, other gets 409.</li><li><strong>Timer over 24h</strong> — single timer running 26h still commits (snapped to 24h)?... actually no, snap to 1560 minutes (26h * 60 = 1560).</li><li><strong>Description optional</strong> — empty description allowed at start; can be set at stop.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn single_active_enforced() {\n    let ctx = TestContext::with_member().await;\n    let t1 = ctx.start_timer().await;\n    let t2 = ctx.start_timer().await;\n    let state1: String = sqlx::query_scalar(\"SELECT state::text FROM timers WHERE timer_id=$1\")\n        .bind(t1).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(state1, \"committed\");\n}\n\n#[tokio::test]\nasync fn fifteen_min_snap_up() {\n    let ctx = TestContext::with_member().await;\n    let t = ctx.start_timer().await;\n    ctx.travel(Duration::from_secs(23 * 60)).await;\n    let r = ctx.stop_timer(t).await;\n    let entry_id: Uuid = r.json::&lt;serde_json::Value&gt;().await.unwrap()[\"committed_entry_id\"].as_str().unwrap().parse().unwrap();\n    let duration: i32 = sqlx::query_scalar(\"SELECT duration_seconds FROM time_entries WHERE entry_id=$1\")\n        .bind(entry_id).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(duration, 30 * 60);\n}\n\n#[tokio::test]\nasync fn idle_pause_after_10min() {\n    let ctx = TestContext::with_member().await;\n    let t = ctx.start_timer().await;\n    ctx.travel(Duration::from_secs(11 * 60)).await;\n    ctx.run_idle_watchdog().await;\n    let state: String = sqlx::query_scalar(\"SELECT state::text FROM timers WHERE timer_id=$1\")\n        .bind(t).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(state, \"paused_idle\");\n}\n\n#[tokio::test]\nasync fn logout_commits_running_timers() {\n    let ctx = TestContext::with_member().await;\n    let t = ctx.start_timer().await;\n    ctx.logout().await;\n    let state: String = sqlx::query_scalar(\"SELECT state::text FROM timers WHERE timer_id=$1\")\n        .bind(t).fetch_one(&amp;ctx.pool).await.unwrap();\n    assert_eq!(state, \"committed\");\n}\n\n// 5.5..5.10: enum cardinality, RLS, heartbeat, audit, resume decision, abandon</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-TIME-001 (TimeEntry schema). <strong>Cross-module:</strong> TASK-AUTH-004 (logout integration), TASK-PROJ-001 (project_id), TASK-AI-003, TASK-MEMORY-111.</p>\n<hr>\n<h2>§8 — Example payload</h2>\n<p><code>time.timer_committed</code>:</p>\n<pre><code class=\"language-json\">{\n  \"kind\": \"time.timer_committed\",\n  \"severity\": 2,\n  \"tenant_id\": \"8a2f...\",\n  \"actor_id\": \"user.member.456\",\n  \"trace_id\": \"...\",\n  \"payload\": {\n    \"timer_id\": \"0190...\",\n    \"engagement_id\": \"0190...\",\n    \"duration_snapped_seconds\": 1800,\n    \"description_sha256\": \"...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>Deferred:</p>\n<ul><li><strong>Deferred:</strong> Pomodoro mode (auto-pause after 25 min) — slice 2.</li><li><strong>Deferred:</strong> Multi-device same-user timer sync — slice 2.</li><li><strong>Deferred:</strong> Mobile background timer (battery + iOS background restrictions) — slice 2.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Concurrent start race</td><td>partial unique</td><td>409; one wins</td><td>Inherent</td></tr><tr><td>Heartbeat missed during network blip</td><td>watchdog</td><td>Idle pause triggers</td><td>Resume on reconnect</td></tr><tr><td>Logout race with manual stop</td><td>tx isolation</td><td>Last writer wins; idempotent commit</td><td>Inherent</td></tr><tr><td>Server clock skew</td><td>timestamp diff</td><td>Duration approximate; ≤ 1s skew acceptable</td><td>NTP</td></tr><tr><td>Browser tab background suspends heartbeat</td><td>client-side detection</td><td>Idle pause; user resumes on tab focus</td><td>Inherent</td></tr><tr><td>Long-running timer (24h+)</td><td>absolute cap</td><td>Hard-cap at 24h via VN Labour Code (TASK-TIME-007 derivative)</td><td>Member splits across days</td></tr><tr><td>Engagement permission lost mid-timer</td><td>RLS at commit</td><td>Commit may fail; sev-2 alert</td><td>Manual entry via TASK-TIME-003</td></tr><tr><td>Abandoned timer with significant accrued time</td><td>counter</td><td>Sev-3 audit; reviewable</td><td>Member confirms</td></tr><tr><td>Idle decision not specified at resume</td><td>required field</td><td>400</td><td>Inherent</td></tr><tr><td>Project_id deleted mid-timer</td><td>FK soft</td><td>Timer continues; FK enforced at commit</td><td>Manual entry alternative</td></tr><tr><td>Description PII-scrub fails</td><td>scrub error</td><td>Audit row dropped; raw retained in DB</td><td>Sev-3 alert</td></tr><tr><td>Multiple devices race start</td><td>partial unique</td><td>First wins; second gets 409</td><td>Inherent</td></tr><tr><td>Pause-resume-pause-resume cycle</td><td>total_pause_seconds accumulator</td><td>Tracked correctly</td><td>Inherent</td></tr><tr><td>Long pause (8h+ overnight)</td><td>watchdog</td><td>State remains paused; commit excludes by default</td><td>User decision at resume</td></tr><tr><td>Auto-commit creates 0-min entry</td><td>rare</td><td>Skipped (no entry created) + abandoned state</td><td>Inherent</td></tr><tr><td>Timer for archived engagement</td><td>engagement check at start</td><td>403 + engagement_archived</td><td>Use active engagement</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<p><strong>§11.1</strong> Watchdog runs as scheduled task; queries <code>timers WHERE state='running' AND last_heartbeat_at &lt; now() - 10min</code>.</p>\n<p><strong>§11.2</strong> 15-min snap formula: <code>let snapped = ((seconds + 899) / 900) * 900;</code> integer arithmetic.</p>\n<p><strong>§11.3</strong> Logout handler at TASK-AUTH-004 invokes <code>services/time/src/timer/commit.rs::commit_all_for_subject</code> synchronously before returning.</p>\n<p><strong>§11.4</strong> SPA widget polls <code>/timer/current</code> on load; sends heartbeat every 60s while page is active.</p>\n<p><strong>§11.5</strong> Heartbeat uses Page Visibility API to suspend when tab backgrounded (battery savings).</p>\n<p><strong>§11.6</strong> Single-active partial unique uses Postgres expression index; very fast lookup.</p>\n<p><strong>§11.7</strong> Pause durations tracked cumulatively in <code>total_pause_seconds</code> for stop-time computation.</p>\n<p><strong>§11.8</strong> TIME entry created at commit time via TASK-TIME-001 standard insert path.</p>\n<p><strong>§11.9</strong> Description PII-scrub via TASK-MEMORY-111 standard ruleset.</p>\n<p><strong>§11.10</strong> Cross-tenant isolation via RLS + explicit member_subject_id check.</p>\n<hr>\n<p><em>End of TASK-TIME-002 spec.</em></p>";
