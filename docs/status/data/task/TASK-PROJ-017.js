window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-017"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The Brief Modal <strong>MUST</strong> be a unified deep-view for one issue with collaborative description + comments + meta sidebar. The contract:</p>\n<ol><li><strong>MUST</strong> open via:</li></ol>\n<ul><li>Click on issue card in any view (Kanban / Timeline / Gantt).</li><li>Enter key on focused card.</li><li>Direct URL <code>/proj/issues/:id/brief</code>. Opening updates URL (history.pushState) so back-button + share-link work.</li></ul>\n<ol><li><strong>MUST</strong> render responsively:</li></ol>\n<ul><li>Mobile (&lt; 1024px): full-screen overlay; sidebar collapses to expandable section.</li><li>Desktop (≥ 1024px): right-side panel 480px wide; sidebar always-visible.</li></ul>\n<ol><li><strong>MUST</strong> bind description to Y.Text via TASK-PROJ-003 YjsProvider and TipTap + <code>@tiptap/extension-collaboration</code>. Concurrent edits converge per Yjs.</li><li><strong>MUST</strong> render comments as Y.Array; each comment is <code>Y.Map { id, author_id, body: Y.Text, created_at }</code>. New comment composer adds element to array; edit binds to body Y.Text.</li><li><strong>MUST</strong> render meta sidebar with LWW scalars (TASK-PROJ-003 §1 #6):</li></ol>\n<ul><li>Status (uses TASK-PROJ-014 StatusPicker).</li><li>Assignee dropdown.</li><li>Priority radio.</li><li>Estimate number input.</li><li>Labels multi-select.</li><li>Dates (starts_at + ends_at). Each field PATCHes via LWW endpoint; stale-write → toast + revert.</li></ul>\n<ol><li><strong>MUST</strong> render presence cursors:</li></ol>\n<ul><li>Other users editing this modal → their cursor position in description shown as labeled flag (name + color).</li><li>Cursor flag throttled at 30 Hz per TASK-PROJ-003 awareness.</li><li>Cursor expires 30s after last awareness heartbeat.</li></ul>\n<ol><li><strong>MUST</strong> provide a history drawer toggle (button + <code>H</code> shortcut):</li></ol>\n<ul><li>When open, side panel shows TASK-PROJ-008 history_event timeline.</li><li>Chain_anchor verification status per row (green check or red warn).</li><li>Click on history row scrolls description to that mutation's snapshot.</li></ul>\n<ol><li><strong>MUST</strong> support kbd shortcuts:</li></ol>\n<ul><li>Esc closes modal (no confirmation; CRDT auto-saves).</li><li>T puts cursor in title field (inline-edit).</li><li>C focuses new-comment composer.</li><li>H toggles history drawer.</li><li>Cmd+S explicit save (no-op visual feedback; \"auto-saved\" indicator).</li></ul>\n<ol><li><strong>MUST</strong> emit memory audit <code>proj.brief_modal_opened</code> per open with <code>{issue_id, by_subject_id, opened_from, trace_id}</code> where opened_from ∈ kanban | timeline | gantt | url | search.</li><li><strong>MUST</strong> RLS-enforce (issue + comments + history).</li><li><strong>MUST</strong> pass axe-core (focus-trap inside modal; restore focus on close; aria-modal=true).</li><li><strong>MUST</strong> emit OTel:</li></ol>\n<ul><li><code>proj_brief_modal_opens_total{opened_from}</code> (counter).</li><li><code>proj_brief_modal_render_p95_ms</code> (histogram).</li><li><code>proj_brief_modal_session_seconds</code> (histogram — engagement signal).</li></ul>\n<ol><li><strong>MUST</strong> support comment threading: each comment can be a reply to another via <code>reply_to_comment_id</code>; threading rendered with visual indentation (max depth 5).</li><li><strong>MUST</strong> support comment mentions: <code>@username</code> in comment body resolves to user; sends in-app notification to mentioned user via CUO triage.</li><li><strong>MUST</strong> support attachments on comments: file upload via task-FILES (max 25MB per file, 5 files per comment); previewable images/PDFs inline.</li><li><strong>MUST</strong> support reactions on comments: emoji picker; each comment shows reaction tallies; clicking re-toggles user's reaction.</li><li><strong>MUST</strong> support <code>@lumi</code> invocation in comments: routes to TASK-CHAT-008 sibling handler scoped to issue context (description + recent comments).</li><li><strong>MUST</strong> support \"link\" actions in sidebar: quick-add issue dependencies (TASK-PROJ-016) + memory-links (TASK-PROJ-009) without leaving modal.</li><li><strong>MUST</strong> support draft comment auto-save: composer text persists per-issue per-user in <code>localStorage</code>; on next modal open, restored.</li><li><strong>MUST</strong> support keyboard navigation through comments: J/K moves comment focus; Reply opens reply composer threaded under that comment.</li><li><strong>MUST</strong> show \"X is typing...\" indicator below comment composer when another user has the composer open; throttled per Yjs awareness.</li><li><strong>MUST</strong> include a \"follow / unfollow\" toggle: followers get CUO notifications on any update to this issue (comments, status, assignee changes).</li><li><strong>MUST</strong> support markdown shortcuts in the description editor: TipTap configures <code>**bold**</code> / <code>_italic_</code> / <code># heading</code> etc. matching standard markdown syntax.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why one modal for everything (DEC-380)?</strong> Three views (Kanban/Timeline/Gantt) all need deep-edit; unifying = one place for edits = no UX drift. URL-deep-linkable = shareable.</p>\n<p><strong>Why responsive split (DEC-382)?</strong> Mobile users need full screen for editing; desktop users want issue visible in board context while editing. 1024px is the standard tablet threshold.</p>\n<p><strong>Why TipTap (DEC-381)?</strong> Industry-standard React rich-text editor; first-class Yjs integration via <code>@tiptap/extension-collaboration</code>. Alternatives (Slate, Lexical) have less mature Yjs binding.</p>\n<p><strong>Why no unsaved state (§1 #8)?</strong> CRDT auto-saves every keystroke; \"save\" is mental. Esc-to-close without confirmation = trust the system. Cmd+S is muscle-memory affordance returning \"auto-saved\" toast.</p>\n<p><strong>Why presence cursors (§1 #6)?</strong> Two users editing same paragraph collide → CRDT resolves correctly but UX is confusing without seeing the other person. Labeled cursors = \"Bob is here\" signal.</p>\n<p><strong>Why history drawer toggle (§1 #7)?</strong> History is per-issue context but bulky. Default hidden; toggle reveals. Power users keep it open during reviews.</p>\n<p><strong>Why audit modal opens (§1 #9)?</strong> Per-issue engagement metrics inform UX. \"How often do users open issues from Kanban vs URL?\" informs onboarding flows.</p>\n<p><strong>Why focus-trap (§1 #11)?</strong> WCAG requires modals keep keyboard focus inside; releases on close. Standard accessibility pattern.</p>\n<p><strong>Why threading (§1 #13)?</strong> Long comment threads need reply context; flat list loses conversation structure.</p>\n<p><strong>Why mentions + notify (§1 #14)?</strong> Mention is the standard \"tag someone for attention\" pattern; notification closes the loop.</p>\n<p><strong>Why comment attachments (§1 #15)?</strong> Real workflows attach screenshots, logs, designs. Without inline upload = workflow friction.</p>\n<p><strong>Why comment reactions (§1 #16)?</strong> Lightweight signal (\"agree\", \"this\") without writing a reply; reduces comment noise.</p>\n<p><strong>Why @lumi in comments (§1 #17)?</strong> LLM-assisted clarification inline; doesn't require leaving the modal.</p>\n<p><strong>Why link actions in sidebar (§1 #18)?</strong> Adding dependencies/memory-links is workflow-adjacent; in-modal action eliminates context switch.</p>\n<p><strong>Why draft auto-save (§1 #19)?</strong> Operator drafting long comment + modal accidentally closes = lost text. localStorage = survives session.</p>\n<p><strong>Why kbd comment nav (§1 #20)?</strong> Power users review many comments; kbd parity for review workflow.</p>\n<p><strong>Why typing indicator (§1 #21)?</strong> Two users typing replies simultaneously waste effort; awareness signal prevents duplicate work.</p>\n<p><strong>Why follow/unfollow (§1 #22)?</strong> Operators want updates on issues they care about; default-following all might over-notify.</p>\n<p><strong>Why markdown shortcuts (§1 #23)?</strong> Markdown is the universal text-formatting language; operators expect it.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/BriefModal/Modal.tsx\nexport function BriefModal({ issueId, openedFrom }: { issueId: string; openedFrom: OpenedFrom }) {\n  const yjs = useYjsProvider(issueId);\n  const [historyOpen, setHistoryOpen] = useState(false);\n  const isMobile = useMediaQuery('(max-width: 1023px)');\n\n  useEffect(() =&gt; {\n    emitMemory('proj.brief_modal_opened', { issue_id: issueId, opened_from: openedFrom });\n    history.pushState({}, '', `/proj/issues/${issueId}/brief`);\n    return () =&gt; {\n      // Restore prior URL on close\n    };\n  }, []);\n\n  useKeyboardShortcuts({\n    Escape: closeModal,\n    T:      () =&gt; focusTitle(),\n    C:      () =&gt; focusCommentComposer(),\n    H:      () =&gt; setHistoryOpen(o =&gt; !o),\n    'Mod+S': () =&gt; toast('Auto-saved', 'success'),\n  });\n\n  return (\n    &lt;Dialog open={true} onOpenChange={closeModal}\n            className={isMobile ? 'fullscreen' : 'side-panel'}\n            aria-modal=\"true\" aria-labelledby=\"issue-title\"&gt;\n      &lt;FocusTrap&gt;\n        &lt;div className=\"brief-modal\"&gt;\n          &lt;Header issueId={issueId} /&gt;\n          &lt;Description yjs={yjs} /&gt;\n          &lt;PresenceCursors yjs={yjs} /&gt;\n          &lt;CommentThread yjs={yjs} /&gt;\n          &lt;MetaSidebar issueId={issueId} /&gt;\n          {historyOpen &amp;&amp; &lt;HistoryDrawer issueId={issueId} /&gt;}\n        &lt;/div&gt;\n      &lt;/FocusTrap&gt;\n    &lt;/Dialog&gt;\n  );\n}</code></pre>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/BriefModal/Description.tsx\nimport { useEditor, EditorContent } from '@tiptap/react';\nimport StarterKit from '@tiptap/starter-kit';\nimport Collaboration from '@tiptap/extension-collaboration';\nimport CollaborationCursor from '@tiptap/extension-collaboration-cursor';\n\nexport function Description({ yjs }: { yjs: YjsProvider }) {\n  const editor = useEditor({\n    extensions: [\n      StarterKit.configure({ history: false }),   // Yjs handles undo\n      Collaboration.configure({ document: yjs.doc, field: 'description' }),\n      CollaborationCursor.configure({\n        provider: yjs.wsProvider,\n        user: { name: currentUser.name, color: userColor(currentUser.id) },\n      }),\n    ],\n  });\n  return &lt;EditorContent editor={editor} className=\"description-editor\" /&gt;;\n}</code></pre>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/BriefModal/MetaSidebar.tsx\nexport function MetaSidebar({ issueId }: { issueId: string }) {\n  const issue = useIssue(issueId);\n  return (\n    &lt;aside className=\"meta-sidebar\"&gt;\n      &lt;StatusPicker issueId={issueId} current={issue.status} onChange={patchStatus} /&gt;\n      &lt;AssigneePicker issueId={issueId} current={issue.assignee_id} onChange={patchAssignee} /&gt;\n      &lt;PriorityPicker issueId={issueId} current={issue.priority} onChange={patchPriority} /&gt;\n      &lt;EstimateInput  issueId={issueId} current={issue.estimate} onChange={patchEstimate} /&gt;\n      &lt;LabelMultiSelect issueId={issueId} current={issue.labels} onChange={patchLabels} /&gt;\n      &lt;DateRange      issueId={issueId} starts={issue.starts_at} ends={issue.ends_at} onChange={patchDates} /&gt;\n    &lt;/aside&gt;\n  );\n\n  async function patchStatus(to: IssueStatus, reason?: string) {\n    const res = await postTransition(issueId, to, reason);\n    if (res.error === 'stale_write') { toast('Refreshed; please retry'); }\n  }\n  // ... similar patch functions for other fields, each calls writeScalarLWW\n}</code></pre>\n<pre><code class=\"language-tsx\">// web/proj-client/src/views/BriefModal/HistoryDrawer.tsx\nexport function HistoryDrawer({ issueId }: { issueId: string }) {\n  const history = useIssueHistory(issueId);\n  return (\n    &lt;div className=\"history-drawer\" role=\"region\" aria-label=\"Issue history\"&gt;\n      &lt;h3&gt;History&lt;/h3&gt;\n      &lt;ol&gt;\n        {history.map(h =&gt; (\n          &lt;li key={h.event.id}&gt;\n            &lt;span&gt;{h.event.mutation_kind}&lt;/span&gt;\n            &lt;span&gt;{h.event.field}&lt;/span&gt;\n            &lt;span&gt;by {h.event.by_subject_id}&lt;/span&gt;\n            {h.chain_verified\n              ? &lt;span aria-label=\"chain verified\" title=\"chain verified\"&gt;✓&lt;/span&gt;\n              : &lt;span aria-label=\"chain mismatch — sev-1 alert\" title=\"chain mismatch\" className=\"warn\"&gt;⚠&lt;/span&gt;}\n          &lt;/li&gt;\n        ))}\n      &lt;/ol&gt;\n    &lt;/div&gt;\n  );\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Open from Kanban Enter</strong> — focused card + Enter → modal opens; URL updates.</li><li><strong>Open from URL deep-link</strong> — visit <code>/proj/issues/iss-X/brief</code> → modal opens.</li><li><strong>Esc closes</strong> — modal closes; URL restored.</li><li><strong>Description CRDT</strong> — two users typing → both converge via Yjs.</li><li><strong>Comments Y.Array</strong> — add comment → appears in both users' modals real-time.</li><li><strong>Meta LWW: status</strong> — change status → POST transition; new value persists.</li><li><strong>Meta LWW: stale write</strong> — concurrent assignee change → second user gets stale_write; toast.</li><li><strong>Presence cursor visible</strong> — second user opens modal → their cursor appears with name + color.</li><li><strong>Presence cursor expires</strong> — second user closes browser → cursor gone within 30s.</li><li><strong>History drawer toggle (H)</strong> — H opens; H closes.</li><li><strong>Chain anchor verify icon</strong> — happy history → green check; tampered → red warn.</li><li><strong>Mobile full-screen</strong> — viewport &lt; 1024px → full-screen layout.</li><li><strong>Desktop side-panel</strong> — viewport ≥ 1024px → 480px right panel.</li><li><strong>Kbd T focuses title</strong> — modal open + T → title inline-edit focused.</li><li><strong>Kbd C focuses comment composer</strong> — C → focus on new comment input.</li><li><strong>Cmd+S shows auto-saved toast</strong> — no-op but feedback.</li><li><strong>Focus trap</strong> — Tab cycles within modal; doesn't escape to background.</li><li><strong>Focus restore on close</strong> — modal close → focus returns to opening element.</li><li><strong>memory audit modal_opened</strong> — per open → row with opened_from.</li><li><strong>OTel modal_opens_total counter</strong> — per open → counter increments.</li><li><strong>axe-core passes</strong> — aria-modal + focus-trap + labels correct.</li><li><strong>RLS isolates</strong> — tenant A's issue invisible to tenant B's modal request → 404.</li><li><strong>Comment thread depth ≤ 5</strong> — replies indent up to depth 5; beyond → flat with marker (AC for §1 #13).</li><li><strong>Mention notifies user</strong> — comment with <code>@alice</code> → CUO notification queued for alice (AC for §1 #14).</li><li><strong>Attachment uploads</strong> — drop file → task-FILES upload; preview inline (AC for §1 #15).</li><li><strong>Reaction toggle</strong> — click emoji → toggles user's reaction; tally updates (AC for §1 #16).</li><li><strong>@lumi in comment routes to handler</strong> — comment with @lumi → reply appears as Lumi-authored comment (AC for §1 #17).</li><li><strong>Sidebar quick-link actions</strong> — click \"Add Dep\" in sidebar → opens TASK-PROJ-016 dialog inline (AC for §1 #18).</li><li><strong>Draft auto-save survives close</strong> — draft text in composer; close modal; reopen → text restored (AC for §1 #19).</li><li><strong>Kbd J/K navigates comments</strong> — comment focused → J moves to next; K to prior (AC for §1 #20).</li><li><strong>Typing indicator visible</strong> — second user types in composer → first user sees \"X is typing...\" (AC for §1 #21).</li><li><strong>Follow toggle adds to followers</strong> — toggle → CUO notifications start for that user (AC for §1 #22).</li><li><strong>Markdown <code>**bold**</code> works</strong> — type <code>**bold**</code> in editor → renders bold (AC for §1 #23).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">test('Esc closes modal', async () =&gt; {\n  const { user } = render(&lt;BriefModal issueId=\"iss-1\" openedFrom=\"kanban\" /&gt;);\n  await user.keyboard('{Escape}');\n  await waitFor(() =&gt; expect(screen.queryByRole('dialog')).toBeNull());\n});\n\ntest('description converges with Yjs', async () =&gt; {\n  const { user: u1 } = render(&lt;BriefModal issueId=\"iss-1\" openedFrom=\"url\" /&gt;);\n  const editor = screen.getByRole('textbox');\n  await u1.type(editor, 'Hello from user 1');\n  // simulate user 2 in a parallel doc\n  const u2Doc = simulateYjsPeer('iss-1');\n  u2Doc.getText('description').insert(0, 'User 2 was here. ');\n  await waitFor(() =&gt; expect(editor).toHaveTextContent(/User 2 was here.*Hello from user 1/));\n});\n\ntest('LWW stale_write shows toast', async () =&gt; {\n  const { user } = render(&lt;BriefModal issueId=\"iss-1\" openedFrom=\"kanban\" /&gt;);\n  mockLww.fail409('status');\n  await user.click(screen.getByText('In Progress'));\n  expect(screen.getByRole('alert')).toHaveTextContent(/refreshed/i);\n});\n\ntest('history drawer toggle with H', async () =&gt; {\n  const { user } = render(&lt;BriefModal issueId=\"iss-1\" openedFrom=\"url\" /&gt;);\n  await user.keyboard('h');\n  expect(screen.getByRole('region', { name: 'Issue history' })).toBeInTheDocument();\n  await user.keyboard('h');\n  expect(screen.queryByRole('region', { name: 'Issue history' })).toBeNull();\n});\n\ntest('chain anchor mismatch shows warn icon', async () =&gt; {\n  mockHistory.tamperRow(2);\n  const { user } = render(&lt;BriefModal issueId=\"iss-1\" openedFrom=\"url\" /&gt;);\n  await user.keyboard('h');\n  const warns = screen.getAllByLabelText(/chain mismatch/);\n  expect(warns).toHaveLength(1);\n});\n\ntest('focus trap inside modal', async () =&gt; {\n  const { user } = render(&lt;BriefModal issueId=\"iss-1\" openedFrom=\"url\" /&gt;);\n  // Tab repeatedly; focus should never leave dialog\n  for (let i = 0; i &lt; 20; i++) await user.tab();\n  expect(screen.getByRole('dialog')).toContainElement(document.activeElement!);\n});</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(Sketches above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-002</strong> — WS.</li><li><strong>TASK-PROJ-003</strong> — Y.Doc + LWW + awareness.</li><li><strong>TASK-PROJ-004</strong> — status transitions (StatusPicker).</li><li><strong>TASK-PROJ-008</strong> — history_event source.</li><li><strong>TASK-PROJ-014</strong> — opener (Kanban).</li><li><strong>TASK-PROJ-018</strong> — design tokens.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.brief_modal_opened\",\n  \"payload\": {\n    \"issue_id\": \"iss-...\",\n    \"by_subject_id\": \"7e57c0de-...\",\n    \"opened_from\": \"kanban\",\n    \"trace_id\": \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Multiple modals side-by-side (split view) — slice 4+.</li><li>Modal-within-modal for linked issue navigation — slice 4+.</li><li>AI assistant inside modal (\"explain this issue\") — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>YjsProvider connect fails</td><td>error state</td><td>Read-only banner; description shows last snapshot</td><td>Reconnect drains</td></tr><tr><td>Issue deleted while modal open</td><td>404 on poll</td><td>Toast + auto-close</td><td>None</td></tr><tr><td>History fetch fails</td><td>error state</td><td>Banner; drawer empty</td><td>Retry</td></tr><tr><td>LWW stale_write</td><td>409</td><td>Toast; revert local field</td><td>User refreshes</td></tr><tr><td>Concurrent edit causes CRDT churn</td><td>Yjs handles</td><td>None</td><td>None</td></tr><tr><td>Comment composer disconnected</td><td>offline buffer</td><td>Comment queues; sent on reconnect</td><td>None</td></tr><tr><td>Presence flood</td><td>30Hz throttle</td><td>None</td><td>None</td></tr><tr><td>Mobile viewport switch</td><td>useMediaQuery handles</td><td>Layout reflows</td><td>None</td></tr><tr><td>Focus escape (axe)</td><td>a11y test catches</td><td>CI blocked</td><td>Fix focus-trap</td></tr><tr><td>Chain anchor mismatch</td><td>red warn icon</td><td>Sev-1 alarm via TASK-PROJ-008 metric</td><td>Operator investigates</td></tr><tr><td>Modal opened with invalid issueId</td><td>404</td><td>Toast + close</td><td>None</td></tr><tr><td>Browser back navigates away</td><td>URL state restored</td><td>None</td><td>None</td></tr><tr><td>Modal stuck (component crash)</td><td>error boundary</td><td>Recover</td><td>None</td></tr><tr><td>TipTap version mismatch</td><td>initial render fail</td><td>Sev-1</td><td>Pin version</td></tr><tr><td>Comment with embedded scripts</td><td>TipTap sanitisation</td><td>Safe</td><td>None</td></tr><tr><td>Comment thread depth &gt; 5</td><td>flat fallback with marker</td><td>None</td><td>None</td></tr><tr><td>Mention to non-existent user</td><td>passed verbatim; no notification</td><td>None</td><td>None</td></tr><tr><td>Attachment &gt; 25MB</td><td>rejected upfront</td><td>toast</td><td>Caller resizes</td></tr><tr><td>Reaction spam (1000s of clicks)</td><td>debounce + rate-limit</td><td>None</td><td>None</td></tr><tr><td>@lumi in comment with tenant lumi disabled</td><td>falls back to TASK-CHAT-008 behavior</td><td>None</td><td>None</td></tr><tr><td>Sidebar quick-link opens stale dependencies</td><td>refetch on open</td><td>None</td><td>None</td></tr><tr><td>Draft auto-save with multi-tab</td><td>localStorage shared; last-write-wins</td><td>None</td><td>None</td></tr><tr><td>Typing indicator stuck (user closed browser)</td><td>30s expiry</td><td>None</td><td>None</td></tr><tr><td>Follow toggle for already-followed</td><td>no-op</td><td>None</td><td>None</td></tr><tr><td>Markdown shortcut conflict (operator types ** literal)</td><td>escape support</td><td>None</td><td>None</td></tr><tr><td>Threaded reply notification spam</td><td>dedup per (user, comment)</td><td>None</td><td>None</td></tr><tr><td>Comment with mention + attachment + reaction</td><td>all work concurrently</td><td>None</td><td>None</td></tr><tr><td>LocalStorage full</td><td>warn; oldest drafts evicted</td><td>None</td><td>Operator clears</td></tr><tr><td>Mobile keyboard takes half screen</td><td>viewport adjust</td><td>None</td><td>None</td></tr><tr><td>Modal close mid-mention-resolution</td><td>notification still sent</td><td>None</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li>TipTap's StarterKit's history is disabled because Yjs provides its own undo via <code>Y.UndoManager</code>.</li><li>CollaborationCursor uses Yjs awareness state; user color derived from subject_id hash (consistent across sessions).</li><li>History drawer uses <code>useIssueHistory</code> hook with SWR caching; refetch on focus.</li><li>Focus-trap library: <code>@radix-ui/react-dialog</code> provides built-in focus management.</li><li>The <code>opened_from</code> value is set by the caller; URL opens default to \"url\".</li><li>The 480px desktop side-panel width is a design token (TASK-PROJ-018).</li><li>Modal session histogram captures engagement: how long users spend per issue.</li><li>Comments are appended to Y.Array; deletion via Y.Array.delete() preserves CRDT history.</li><li>Threading uses <code>reply_to_comment_id</code> field; rendering recursively indents up to depth 5; deeper threads flatten with \"deeply nested\" marker.</li><li>Mention regex matches <code>@[a-zA-Z0-9_]+</code>; resolves against users table for tenant.</li><li>Attachment upload uses task-FILES presigned URL flow; preview component handles common MIME types.</li><li>Reactions use Y.Map (emoji → Set of user_ids); CRDT handles concurrent toggles.</li><li>@lumi in comments routes to chat-lumi service with <code>context=\"issue-comment\"</code>; reply appears as a comment authored by Lumi system user.</li><li>Sidebar quick-link actions open inline dialogs (radix UI Dialog) without leaving modal.</li><li>Draft auto-save uses <code>localStorage[</code>draft_comment_${issueId}_${userId}<code>]</code>; debounced at 500ms.</li><li>Kbd J/K navigates comments in DOM order; Reply opens reply composer under focused comment.</li><li>Typing indicator uses Yjs awareness <code>composing: true</code> field; throttled and TTL'd.</li><li>Follow toggle creates row in <code>cyberos_proj_issue_followers</code> table; CUO triages notifications based on this list.</li><li>Markdown shortcuts use TipTap's <code>Markdown</code> extension; configured for standard CommonMark subset.</li><li>Reaction emoji picker uses <code>@emoji-mart/react</code> (lazy-loaded ~80KB).</li><li>Mention auto-complete uses <code>@tiptap/extension-mention</code>; suggests as user types.</li><li>Threaded reply UI collapses to \"N replies\" link when thread &gt; 10 comments; click expands.</li><li>Comment edit window: 15 minutes from create; after that, edit produces a \"edited at\" indicator + history row.</li><li>We considered comment soft-delete vs hard-delete; chose soft (mark deleted, body redacted) for audit trail.</li><li>Comment threading depth ≤ 5 calibrated against UX studies; deeper = unreadable.</li><li>Mention notifications respect notify_props (TASK-CHAT-011-style); user can disable mentions per-engagement.</li><li>The \"@lumi in comment\" feature is opt-in per engagement (per-tenant Lumi settings inherited).</li><li>Sidebar quick-links emit memory audit per action (link added/removed).</li><li>Draft auto-save is cleared on successful comment submit OR explicit \"discard draft\" button.</li><li>Reactions don't notify; they're lightweight feedback. Operators wanting notify use mentions.</li><li>Markdown shortcuts work in description editor AND comment composer; consistent UX.</li><li>The follow/unfollow toggle defaults to follow for issue authors + assignees; explicit follow for others.</li><li>Comment with mention + attachment + reaction emits multiple audit rows; correlated via comment_id.</li><li>Modal session tracking starts on first render, ends on close; histogram captures real engagement.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-017.</em></p>\n<h2>As built (2026-07-02)</h2>\n<p>Client code lives under apps/web/src (there is no web/proj-client/).</p>";
