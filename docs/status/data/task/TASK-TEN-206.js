window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TEN-206"]="<h1>TASK-TEN-206: Metering Pg Recorder + WAL drain</h1>\n<h2>Summary</h2>\n<p>Persist WAL / recorder events into <code>metering_events</code> via sqlx, expose period usage SUM for admission, and apply metering migrations in awh-gate CI.</p>\n<h2>Problem</h2>\n<p>TEN-204/205 emit into process-local InMemoryRecorder only. No durable floor; overage 402 cannot trust period counts.</p>\n<h2>Proposed Solution</h2>\n<ol><li><code>pg.rs</code>: async INSERT … ON CONFLICT DO NOTHING matching UNIQUE <code>(tenant_id, axis, idempotency_key)</code>.</li><li><code>usage.rs</code>: <code>period_sum(pool, tenant, axis, period_start)</code> → u64.</li><li><code>drain.rs</code>: pop WalQueue → record via sync <code>Recorder</code> (tests) and async Pg path.</li><li>awh-gate: <code>cd services/metering &amp;&amp; sqlx migrate run</code> after auth migrations.</li><li>Tests: drain→InMemory; Pg tests skip without <code>DATABASE_URL</code>.</li></ol>\n<h2>Alternatives Considered</h2>\n<ul><li><strong>Full MV + 5-min refresh.</strong> Deferred; SUM over <code>occurred_at &gt;= period_start</code> is enough for host-d admission.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: drain N WAL items → N Pg rows (dup keys → no second row).</li><li>Guardrail: migrate applies cleanly in CI.</li></ul>\n<h2>Scope</h2>\n<h3>In scope</h3>\n<ul><li>Pg insert + period sum + WAL drain + CI migrate + tests.</li></ul>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>402 admission (TASK-TEN-207)</li><li>Memory audit chain per event</li><li>Seats/storage snapshots / GET /v1/usage</li><li>INV Pg persist</li></ul>\n<h2>Dependencies</h2>\n<ul><li>TASK-TEN-004 migration <code>0001_metering_events.sql</code>.</li></ul>\n<h2>Acceptance Criteria</h2>\n<ol><li><strong>Pg insert</strong> idempotent on unique key.</li><li><strong>Drain</strong> empties WAL into recorder/Pg.</li><li><strong>period_sum</strong> returns aggregate quantity for axis in window.</li><li><strong>CI</strong> awh-gate runs metering <code>sqlx migrate run</code>.</li><li><strong>No DATABASE_URL</strong> — unit/drain tests still pass offline.</li></ol>\n<h2>Verification</h2>\n<pre><code class=\"language-sh\">cd services &amp;&amp; cargo test -p cyberos-metering -- --test-threads=1\nbash .cyberos/cuo/gates/run-gates.sh</code></pre>\n<hr>\n<p><em>End of TASK-TEN-206.</em></p>";
