window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>Every PROJ Issue state change <strong>MUST</strong> be a memory-anchored decision, not just a DB row update. The integration:</p>\n<ol><li><strong>MUST</strong> emit memory row <code>proj.decision</code> per Issue status transition (TASK-PROJ-001 §1 #6's <code>proj.issue_status_changed</code> is REPLACED by this richer row). Payload: <code>issue_id</code>, <code>from_status</code>, <code>to_status</code>, <code>reason</code>, <code>decided_by_subject_id</code>, <code>prior_decision_chain</code>, <code>cross_module_links</code>, <code>tenant_id</code>, <code>request_id</code>.</li><li><strong>MUST</strong> support optional <code>reason: string</code> (max 500 chars) on PATCH issue. Reason is the rationale for the transition; mandatory in some contexts (slice 3+ enforces for <code>done</code> transitions).</li><li><strong>MUST</strong> validate <code>reason</code> length: &gt; 500 chars → 400 with <code>reason_too_long</code>. Empty <code>reason</code> allowed (transition without explicit rationale).</li><li><strong>MUST</strong> be referenceable from CHAT via <code>#issue/&lt;id&gt;</code> syntax — chat module's link expander (TASK-CHAT-007) follows the link to the memory decision row.</li><li><strong>MUST</strong> include <code>prior_decision_chain</code> field referencing the chain hash of the previous <code>proj.decision</code> for the SAME issue. First transition → <code>null</code>. Subsequent transitions form a per-issue chain queryable as \"show all decisions on issue X\" via TASK-MEMORY-108.</li><li><strong>MUST</strong> apply audit-before-action invariant: the entire status change (DB UPDATE + memory row emit) runs in a single Postgres transaction; memory emit failure rolls back the DB UPDATE. Partial states forbidden.</li><li><strong>MUST</strong> default <code>sync_class: shareable</code> with <code>acl: []</code> (engagement members access via tenant scope). Tenant-admin can override <code>acl</code> to restrict (e.g., HR-confidential decisions on <code>acl: [\"@hr-team\"]</code>).</li><li><strong>MUST</strong> extract cross-module links from <code>reason</code> text: URLs matching <code>chat://thread/...</code>, <code>email://thread/...</code>, <code>meeting://...</code> patterns are parsed and stored as <code>cross_module_links</code> array. CHAT/email/meeting modules can resolve these links bidirectionally.</li><li><strong>MUST</strong> be queryable via TASK-MEMORY-108 — searching for <code>kind:proj.decision AND issue_id:&lt;id&gt;</code> returns the chronological per-issue chain.</li><li><strong>MUST</strong> include <code>decided_by_subject_id</code> from JWT claims. Audit shows WHO made the decision, not just that it happened.</li><li><strong>MUST</strong> complete decision emission within 100ms p95 (memory row write + chain fetch + DB update).</li><li><strong>SHOULD</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_decisions_emitted_total{tenant_id, transition}</code> (counter; transition like <code>triage_to_todo</code>).</li><li><code>proj_decisions_emit_latency_ms</code> (histogram).</li><li><code>proj_decisions_with_reason_total</code> (counter; trend who provides rationale).</li><li><code>proj_decisions_with_links_total{link_type}</code> (counter; cross-module integration health).</li></ul>\n<ol><li><strong>MUST</strong> redact PII from <code>reason</code> text before memory emit using TASK-MEMORY-111 ruleset (same redactor as TASK-CHAT-005). The redacted form is stored in the audit row; original is NOT retained. This is mandatory not optional — operators cannot disable.</li><li><strong>MUST</strong> include a <code>decision_id</code> (UUIDv7) as a top-level field in the payload, distinct from the memory row's internal chain hash. Operators and APIs reference decisions by decision_id; chain hash is implementation detail.</li><li><strong>MUST</strong> support decision retraction via separate <code>proj.decision_retracted</code> row kind (NOT mutation of the original). Retraction carries <code>{retracts_decision_id, retraction_reason, retracted_by_subject_id}</code>. The original decision row is immutable; the retraction is the supersession marker.</li><li><strong>MUST</strong> treat a bulk-status-change (multiple issues PATCHed in one request) as ONE memory row per issue, NOT one aggregate row. The audit trail must be per-issue queryable.</li><li><strong>MUST</strong> normalise cross-module links: <code>chat://thread/abc</code> and <code>chat://Thread/ABC</code> resolve to the same canonical form (lowercase + trim). Stored in canonical form to enable bidirectional lookup.</li><li><strong>MUST</strong> support an optional <code>decision_attributes</code> JSONB field on the payload for structured metadata: <code>{client_facing: true, requires_followup: false, deadline_iso: \"2026-06-01\"}</code>. Schema is open (per-tenant extension); reserved keys are <code>client_facing</code>, <code>requires_followup</code>, <code>deadline_iso</code>.</li><li><strong>MUST</strong> index <code>proj.decision</code> rows by <code>(tenant_id, issue_id, ts_ns)</code> in the memory search index (TASK-MEMORY-108). The per-issue chain query MUST return in O(log n) time, not O(n) scan.</li><li><strong>MUST</strong> enforce immutability: once emitted, a decision row's payload is frozen. Any attempt to PATCH or DELETE returns 405 Method Not Allowed. Modifications happen via retraction + new decision.</li><li><strong>MUST</strong> support per-tenant required-reason policy: <code>cyberos_proj_tenant_settings.require_reason_for = [\"done\"]</code> (array of statuses). When the target status is in the list, missing/empty reason → 400 <code>reason_required</code>. Default = empty array (no requirement); slice 3+ defaults to <code>[\"done\", \"cancelled\"]</code>.</li><li><strong>MUST</strong> include <code>chain_anchor_in_payload</code>: the chain hash of THIS row, included in the payload itself so the row is self-verifying. Recipient SHA-256s the payload-minus-chain_anchor and compares.</li><li><strong>MUST</strong> track <code>decision_session_id</code>: a single user PATCH that changes status + reason + assignee in one operation generates one memory row for the status change but they share a session_id with other ops (e.g. comment add). Allows reconstructing \"what happened in this PATCH request\" across multiple audit rows.</li><li><strong>MUST</strong> support per-engagement decision policy override: <code>cyberos_proj_engagement_settings.decision_acl_default</code> — engagements (e.g. legal-engagement) may default to <code>acl: [\"@legal-team\"]</code> instead of empty.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why decision-not-just-update (DEC-215)?</strong> A status change in DB is silent — no rationale captured, no chain entry. As a \"decision\" in memory, it's queryable, auditable, search-indexable, AND linked into the per-issue history. The decision IS the truth; the DB row is the projection.</p>\n<p><strong>Why audit-before-action (DEC-215)?</strong> Partial states (DB updated but memory not) corrupt the audit chain — the issue shows status=done but no decision row explains it. Single transaction with memory-emit-blocks-commit eliminates the partial state.</p>\n<p><strong>Why prior_decision_chain (DEC-216)?</strong> Per-issue history queries shouldn't require expensive joins. The prior_chain field forms a linked list per issue; following it gives the full transition history in chain order without joins.</p>\n<p><strong>Why cross-module link extraction (§1 #8)?</strong> Real decisions reference contextual conversations: \"marked done after Stephen approved in chat\" with <code>chat://thread/...</code> link. Extracting + storing as structured field lets CHAT/email/meeting modules resolve links bidirectionally — opening the chat thread shows \"this thread led to issue X being marked done.\"</p>\n<p><strong>Why default sync_class shareable (DEC-217)?</strong> Engagement members typically all see decisions. ACL restriction is the exception (HR-only, finance-only). Default shareable optimises for the common case.</p>\n<p><strong>Why max 500-char reason (§1 #3)?</strong> Reasons are rationale, not essays. Long-form decision documents go in memory as separate <code>decisions/</code> files. 500 chars enforces conciseness.</p>\n<p><strong>Why mandatory reason for <code>done</code> (slice 3+, §1 #2)?</strong> Marking work done without rationale loses retro context. Slice 1 ships optional; slice 3+ enforces for terminal transitions.</p>\n<p><strong>Why decided_by_subject_id (§1 #10)?</strong> Audit attribution. \"Stephen marked it done\" vs \"Alice marked it done\" matters for retro discussion + accountability.</p>\n<p><strong>Why 100ms emit budget (§1 #11)?</strong> Status changes are interactive (operator clicks \"Done\"). 100ms is invisible; longer feels sluggish. Budget covers DB UPDATE + memory write + chain fetch.</p>\n<p><strong>Why TASK-MEMORY-108 query integration (§1 #9)?</strong> Decisions become first-class searchable artefacts. \"Show me all decisions for engagement X\" or \"decisions by subject Y last quarter\" — answerable directly.</p>\n<p><strong>Why mandatory PII redaction (§1 #13)?</strong> Reason text is operator-typed; it may include client names, emails, phone numbers. memory audit retention is years; storing unredacted = years of accumulated PII risk. Redact at write-time, not at read-time, so the persisted form is always safe.</p>\n<p><strong>Why decision_id distinct from chain_hash (§1 #14)?</strong> Chain hashes are 64 hex chars (unwieldy in URLs); UUIDv7 is human-friendly + time-sortable. Decision_id is the public identifier; chain_hash is the cryptographic identifier.</p>\n<p><strong>Why retraction as separate row (§1 #15)?</strong> Audit trail integrity: mutating the original would break the chain hash. Retraction-as-new-row preserves the original AND records the supersession. Operators answering \"what was decided then; what was decided now\" see both.</p>\n<p><strong>Why one row per issue in bulk (§1 #16)?</strong> Per-issue queryability: an operator searching \"all decisions on issue X\" expects to find the bulk change. An aggregate row would hide individual issues' transitions.</p>\n<p><strong>Why normalised cross-module links (§1 #17)?</strong> Bidirectional lookup: CHAT module asks \"which decisions reference this thread?\" — works only if links are stored in canonical form. Without normalisation, <code>chat://Thread/ABC</code> and <code>chat://thread/abc</code> would miss each other.</p>\n<p><strong>Why open <code>decision_attributes</code> (§1 #18)?</strong> Per-tenant workflows have custom metadata needs (e.g. CRM-engagement decisions need <code>crm_account_id</code>); reserved keys cover the common case while leaving room.</p>\n<p><strong>Why O(log n) index (§1 #19)?</strong> Per-issue history queries are interactive (operator clicks \"see history\"). Linear scan over thousands of decisions = perceptible delay. Index ordering enables binary-search-style lookup.</p>\n<p><strong>Why immutability (§1 #20)?</strong> Audit chain integrity. If anyone could edit a decision after the fact, the chain hash + recipient verify.sh would diverge. Immutability is the foundation of tamper-evidence.</p>\n<p><strong>Why per-tenant required-reason (§1 #21)?</strong> Compliance-heavy tenants (legal, financial services) require rationale on every status change for SOX-like audit. SMB tenants opt out for speed. Per-tenant respects context.</p>\n<p><strong>Why chain_anchor_in_payload (§1 #22)?</strong> Audit row self-verification: a recipient with the payload alone can recompute + compare. Without it, recipient needs access to memory's chain index.</p>\n<p><strong>Why decision_session_id (§1 #23)?</strong> Operator workflows are often multi-mutation (\"update status + add comment + reassign\"); reconstructing \"what did Stephen do in that PATCH\" requires correlation across rows. session_id is the correlation key.</p>\n<p><strong>Why per-engagement ACL default (§1 #24)?</strong> Engagement-level policy override: a legal-engagement defaults to <code>[\"@legal-team\"]</code>; the engagement-creator chose that scope at engagement creation. Per-decision override still possible but rare.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-rust\">// services/proj/src/decisions.rs\nuse uuid::Uuid;\nuse chrono::{DateTime, Utc};\nuse serde::{Serialize, Deserialize};\n\n#[derive(Serialize)]\npub struct DecisionPayload {\n    pub issue_id: Uuid,\n    pub tenant_id: Uuid,\n    pub from_status: String,\n    pub to_status: String,\n    pub reason: Option&lt;String&gt;,\n    pub decided_by_subject_id: Uuid,\n    pub prior_decision_chain: Option&lt;String&gt;,        // hex 64\n    pub cross_module_links: Vec&lt;String&gt;,\n    pub request_id: String,\n}\n\npub async fn emit_decision_in_tx(\n    tx: &amp;mut PgTransaction&lt;'_&gt;,\n    issue_id: Uuid, tenant_id: Uuid,\n    from: IssueStatus, to: IssueStatus,\n    reason: Option&lt;&amp;str&gt;, decided_by: Uuid, request_id: &amp;str,\n) -&gt; Result&lt;(), DecisionError&gt; {\n    // §1 #3 reason validation\n    if let Some(r) = reason {\n        if r.len() &gt; 500 {\n            return Err(DecisionError::ReasonTooLong { actual: r.len() });\n        }\n    }\n\n    // §1 #5 fetch prior chain\n    let prior_chain = decision_chain::fetch_prior_chain(tx, issue_id).await?;\n\n    // §1 #8 extract cross-module links\n    let cross_module_links = reason\n        .map(|r| extract_links(r))\n        .unwrap_or_default();\n\n    let payload = DecisionPayload {\n        issue_id, tenant_id,\n        from_status: format!(\"{:?}\", from).to_lowercase(),\n        to_status: format!(\"{:?}\", to).to_lowercase(),\n        reason: reason.map(|s| s.to_string()),\n        decided_by_subject_id: decided_by,\n        prior_decision_chain: prior_chain,\n        cross_module_links,\n        request_id: request_id.into(),\n    };\n\n    let mut row = AuditRow {\n        kind: \"proj.decision\".into(),\n        payload: serde_json::to_value(&amp;payload).unwrap(),\n        meta: meta_with_acl_default(tenant_id),\n        ..Default::default()\n    };\n\n    // §1 #6 audit-before-action — emit_in_tx blocks on memory write\n    memory_writer::emit_in_tx(tx, row).await\n        .map_err(|e| DecisionError::MemoryEmitFailed(e.to_string()))?;\n\n    metrics::decision_emitted(tenant_id, &amp;format!(\"{from:?}_to_{to:?}\").to_lowercase(),\n                              reason.is_some(), cross_module_links.len() as u32);\n    Ok(())\n}\n\nfn extract_links(text: &amp;str) -&gt; Vec&lt;String&gt; {\n    static RE: LazyLock&lt;regex::Regex&gt; = LazyLock::new(|| {\n        regex::Regex::new(r\"(?:chat|email|meeting)://[a-zA-Z0-9_/\\-\\.]+\").unwrap()\n    });\n    RE.find_iter(text).map(|m| m.as_str().to_string()).collect()\n}\n\nfn meta_with_acl_default(tenant_id: Uuid) -&gt; serde_json::Value {\n    serde_json::json!({\n        \"sync_class\": \"shareable\",\n        \"acl\": [],\n    })\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum DecisionError {\n    #[error(\"reason too long: {actual} chars (max 500)\")]\n    ReasonTooLong { actual: usize },\n    #[error(\"memory emit failed: {0}\")]\n    MemoryEmitFailed(String),\n    #[error(\"db: {0}\")]\n    Db(#[from] sqlx::Error),\n}</code></pre>\n<pre><code class=\"language-rust\">// services/proj/src/decision_chain.rs\npub async fn fetch_prior_chain(tx: &amp;mut PgTransaction&lt;'_&gt;, issue_id: Uuid) -&gt; Result&lt;Option&lt;String&gt;, DecisionError&gt; {\n    let row: Option&lt;(Vec&lt;u8&gt;,)&gt; = sqlx::query_as(\n        \"SELECT chain FROM memory_outbox\n         WHERE kind = 'proj.decision'\n           AND payload_json::jsonb-&gt;&gt;'issue_id' = $1\n         ORDER BY created_at DESC LIMIT 1\",\n    ).bind(issue_id.to_string()).fetch_optional(&amp;mut **tx).await?;\n    Ok(row.map(|(chain,)| hex::encode(chain)))\n}</code></pre>\n<h3>decision_acl.rs — ACL evaluation + engagement override</h3>\n<pre><code class=\"language-rust\">// services/proj/src/decision_acl.rs\nuse uuid::Uuid;\n\npub struct DecisionAcl {\n    pub sync_class: SyncClass,\n    pub acl:        Vec&lt;String&gt;,\n}\n\npub enum SyncClass { Private, Shareable }\n\npub async fn resolve_decision_acl(\n    pool: &amp;sqlx::PgPool,\n    tenant_id: Uuid,\n    engagement_id: Option&lt;Uuid&gt;,\n    override_acl: Option&lt;Vec&lt;String&gt;&gt;,\n) -&gt; Result&lt;DecisionAcl, sqlx::Error&gt; {\n    // Per-decision override beats engagement default.\n    if let Some(acl) = override_acl {\n        return Ok(DecisionAcl {\n            sync_class: if acl.is_empty() { SyncClass::Shareable } else { SyncClass::Shareable },\n            acl,\n        });\n    }\n    // Engagement-level default.\n    if let Some(eng_id) = engagement_id {\n        let default_acl: Option&lt;Vec&lt;String&gt;&gt; = sqlx::query_scalar(\n            \"SELECT decision_acl_default FROM cyberos_proj_engagement_settings WHERE engagement_id = $1\"\n        ).bind(eng_id).fetch_optional(pool).await?;\n        if let Some(acl) = default_acl {\n            return Ok(DecisionAcl { sync_class: SyncClass::Shareable, acl });\n        }\n    }\n    // Tenant fallback.\n    Ok(DecisionAcl { sync_class: SyncClass::Shareable, acl: vec![] })\n}</code></pre>\n<h3>Required-reason policy + retraction + immutability</h3>\n<pre><code class=\"language-rust\">// services/proj/src/decisions.rs — additions\n\npub async fn check_required_reason(\n    pool: &amp;sqlx::PgPool,\n    tenant_id: Uuid,\n    to_status: IssueStatus,\n    reason: Option&lt;&amp;str&gt;,\n) -&gt; Result&lt;(), DecisionError&gt; {\n    let required_for: Vec&lt;String&gt; = sqlx::query_scalar(\n        \"SELECT COALESCE(require_reason_for, '{}') FROM cyberos_proj_tenant_settings WHERE tenant_id = $1\"\n    ).bind(tenant_id).fetch_optional(pool).await?.unwrap_or_default();\n\n    let target = format!(\"{:?}\", to_status).to_lowercase();\n    if required_for.contains(&amp;target) &amp;&amp; reason.map(str::trim).map(|s| s.is_empty()).unwrap_or(true) {\n        return Err(DecisionError::ReasonRequired { for_status: target });\n    }\n    Ok(())\n}\n\npub async fn emit_retraction(\n    tx: &amp;mut PgTransaction&lt;'_&gt;,\n    retracts_decision_id: Uuid,\n    tenant_id: Uuid,\n    retraction_reason: &amp;str,\n    retracted_by: Uuid,\n    request_id: &amp;str,\n) -&gt; Result&lt;(), DecisionError&gt; {\n    // Verify the original decision exists + is not already retracted.\n    let original = sqlx::query!(\n        \"SELECT payload, chain FROM memory_outbox\n         WHERE kind = 'proj.decision'\n           AND payload_json::jsonb-&gt;&gt;'decision_id' = $1\n           AND tenant_id = $2\"\n    , retracts_decision_id.to_string(), tenant_id).fetch_optional(&amp;mut **tx).await?;\n    let Some(original) = original else {\n        return Err(DecisionError::DecisionNotFound(retracts_decision_id));\n    };\n\n    let already_retracted: bool = sqlx::query_scalar(\n        \"SELECT EXISTS(SELECT 1 FROM memory_outbox\n             WHERE kind = 'proj.decision_retracted'\n               AND payload_json::jsonb-&gt;&gt;'retracts_decision_id' = $1)\"\n    ).bind(retracts_decision_id.to_string()).fetch_one(&amp;mut **tx).await?;\n    if already_retracted {\n        return Err(DecisionError::AlreadyRetracted(retracts_decision_id));\n    }\n\n    let redacted_reason = pii_redactor::redact(retraction_reason);\n    let payload = serde_json::json!({\n        \"retracts_decision_id\": retracts_decision_id,\n        \"retracts_chain\": hex::encode(original.chain),\n        \"retraction_reason\": redacted_reason,\n        \"retracted_by_subject_id\": retracted_by,\n        \"tenant_id\": tenant_id,\n        \"request_id\": request_id,\n    });\n    let row = AuditRow {\n        kind: \"proj.decision_retracted\".into(),\n        payload,\n        meta: meta_with_acl_default(tenant_id),\n        ..Default::default()\n    };\n    memory_writer::emit_in_tx(tx, row).await\n        .map_err(|e| DecisionError::MemoryEmitFailed(e.to_string()))?;\n    Ok(())\n}\n\n// Immutability: explicit handler that rejects mutation attempts.\npub async fn handle_decision_mutation_attempt() -&gt; Result&lt;axum::http::Response&lt;axum::body::Body&gt;, axum::http::StatusCode&gt; {\n    Err(axum::http::StatusCode::METHOD_NOT_ALLOWED)\n}</code></pre>\n<h3>Cross-module link normalisation</h3>\n<pre><code class=\"language-rust\">// services/proj/src/decisions.rs — additions\npub fn normalise_link(raw: &amp;str) -&gt; String {\n    // Split on first :// to keep scheme intact; lowercase scheme + path; trim.\n    if let Some(idx) = raw.find(\"://\") {\n        let (scheme, rest) = raw.split_at(idx);\n        let rest = &amp;rest[3..];\n        format!(\"{}://{}\", scheme.to_lowercase(), rest.trim().to_lowercase())\n    } else {\n        raw.trim().to_lowercase()\n    }\n}\n\npub fn extract_links_normalised(text: &amp;str) -&gt; Vec&lt;String&gt; {\n    static RE: LazyLock&lt;regex::Regex&gt; = LazyLock::new(|| {\n        regex::Regex::new(r\"(?:chat|email|meeting)://[a-zA-Z0-9_/\\-\\.]+\").unwrap()\n    });\n    let mut seen = std::collections::HashSet::new();\n    let mut out = Vec::new();\n    for m in RE.find_iter(text) {\n        let canon = normalise_link(m.as_str());\n        if seen.insert(canon.clone()) { out.push(canon); }\n    }\n    out\n}</code></pre>\n<h3>Self-verifying chain anchor + decision_id</h3>\n<pre><code class=\"language-rust\">// services/proj/src/decisions.rs — additions\npub fn build_decision_payload_with_anchor(\n    payload: &amp;mut DecisionPayload,\n) -&gt; serde_json::Value {\n    let decision_id = uuid::Uuid::now_v7();\n    let mut value = serde_json::to_value(&amp;*payload).unwrap();\n    value[\"decision_id\"] = serde_json::json!(decision_id);\n    let chain_anchor = {\n        let canon = serde_json::to_string(&amp;value).unwrap();\n        let mut h = sha2::Sha256::new();\n        h.update(canon.as_bytes());\n        hex::encode(h.finalize())\n    };\n    value[\"chain_anchor\"] = serde_json::json!(chain_anchor);\n    value\n}</code></pre>\n<h3>Schema additions</h3>\n<pre><code class=\"language-sql\">-- services/proj/sql/init-decision-policies.sql\nCREATE TABLE IF NOT EXISTS cyberos_proj_tenant_settings (\n    tenant_id            UUID PRIMARY KEY,\n    require_reason_for   TEXT[] NOT NULL DEFAULT '{}',\n    redact_reason        BOOLEAN NOT NULL DEFAULT true,\n    updated_at           TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE IF NOT EXISTS cyberos_proj_engagement_settings (\n    engagement_id        UUID PRIMARY KEY,\n    tenant_id            UUID NOT NULL,\n    decision_acl_default TEXT[] NOT NULL DEFAULT '{}',\n    updated_at           TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\n-- Index on the memory outbox for (tenant, issue, ts) per §1 #19.\nCREATE INDEX IF NOT EXISTS idx_memory_outbox_proj_decision\n    ON memory_outbox ((payload_json::jsonb-&gt;&gt;'tenant_id'),\n                     (payload_json::jsonb-&gt;&gt;'issue_id'),\n                     created_at DESC)\n    WHERE kind = 'proj.decision';</code></pre>\n<p>Integration in <code>handlers/issues.rs::patch_issue</code>:</p>\n<pre><code class=\"language-rust\">// Replace the prior `audit::emit_status_changed` call with:\nif let Some(new_status) = req.status {\n    if new_status != current.status {\n        status_fsm::validate_transition(current.status, new_status)?;\n        decisions::emit_decision_in_tx(\n            &amp;mut tx, current.id, claims.tenant_id,\n            current.status, new_status,\n            req.reason.as_deref(), claims.subject_id, request_id,\n        ).await?;\n    }\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li>PATCH issue status → memory row <code>proj.decision</code> emitted with full payload.</li><li>Reason field stored verbatim in audit row.</li><li>Prior decision chain links to previous transition for same issue.</li><li>First transition → <code>prior_decision_chain: null</code>.</li><li>Reason &gt; 500 chars → 400 <code>reason_too_long</code>.</li><li>Cross-module links extracted from reason text (<code>chat://</code>, <code>email://</code>, <code>meeting://</code>).</li><li>Audit-before-action: memory emit failure → DB UPDATE rolled back; status NOT changed.</li><li>Default <code>sync_class: shareable</code>, <code>acl: []</code>.</li><li>Tenant-admin can override ACL to restrict (e.g., <code>acl: [\"@hr-team\"]</code>) per-decision.</li><li>CHAT @lumi can search decisions via TASK-MEMORY-108: <code>kind:proj.decision AND issue_id:&lt;id&gt;</code>.</li><li>Cross-engagement search respects ACL — auditor sees only authorised decisions.</li><li>p95 decision emission &lt; 100ms.</li><li><code>decided_by_subject_id</code> from JWT claims.</li><li>Status-change without reason still emits decision (reason: null).</li><li>Same status PATCH (no transition) → no decision row.</li><li>Per-issue chain query returns transitions in chronological order.</li><li><strong>Reason redacted before emit</strong> — fixture: reason with email <code>alice@x.com</code> → audit row <code>reason</code> contains <code>&lt;EMAIL&gt;</code>; original NOT stored anywhere (AC for §1 #13).</li><li><strong>decision_id present in payload</strong> — every emitted row has <code>payload.decision_id</code> (UUIDv7) (AC for §1 #14).</li><li><strong>Retraction emits separate row</strong> — POST /v1/proj/decisions/&lt;id&gt;/retract with reason → <code>proj.decision_retracted</code> row references original via <code>retracts_decision_id</code>; original row unchanged (AC for §1 #15).</li><li><strong>Retraction of already-retracted decision rejected</strong> — second retract → 409 <code>already_retracted</code> (AC for §1 #15).</li><li><strong>Bulk PATCH = one row per issue</strong> — PATCH <code>/v1/proj/issues?ids=a,b,c</code> with same status → 3 memory rows, one per issue (AC for §1 #16).</li><li><strong>Cross-module links normalised</strong> — fixture: reason <code>chat://Thread/ABC and chat://thread/abc</code> → <code>cross_module_links: [\"chat://thread/abc\"]</code> (single deduped canonical) (AC for §1 #17).</li><li><strong>decision_attributes round-trips</strong> — POST with <code>decision_attributes: {client_facing: true}</code> → audit row preserves verbatim (AC for §1 #18).</li><li><strong>Index supports O(log n) per-issue lookup</strong> — EXPLAIN ANALYZE of <code>SELECT ... WHERE issue_id = $1 ORDER BY ts_ns DESC LIMIT 50</code> shows Index Scan, not Seq Scan (AC for §1 #19).</li><li><strong>PATCH /v1/proj/decisions/&lt;id&gt; returns 405</strong> — attempted decision row mutation rejected (AC for §1 #20).</li><li><strong>DELETE /v1/proj/decisions/&lt;id&gt; returns 405</strong> — attempted decision row deletion rejected (AC for §1 #20).</li><li><strong>Required-reason policy enforces</strong> — set <code>require_reason_for=['done']</code>; PATCH to done without reason → 400 <code>reason_required</code>; with reason → 200 (AC for §1 #21).</li><li><strong>chain_anchor in payload self-verifies</strong> — sha256(payload-minus-chain_anchor) == payload.chain_anchor (AC for §1 #22).</li><li><strong>decision_session_id correlates ops</strong> — multi-mutation PATCH produces multiple memory rows sharing <code>session_id</code> (AC for §1 #23).</li><li><strong>Engagement ACL default applied</strong> — set engagement <code>decision_acl_default=['@legal']</code>; PATCH issue in that engagement → audit row meta.acl=['@legal'] (AC for §1 #24).</li><li><strong>Per-decision override beats engagement default</strong> — same engagement but PATCH with explicit <code>acl: ['@stephen']</code> → meta.acl=['@stephen'] (AC for §1 #24).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn status_change_emits_decision_row() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    patch_issue(issue.id, PatchIssueRequest {\n        status: Some(IssueStatus::Todo), reason: Some(\"ready to start\".into()),\n        ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n\n    let row = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    assert_eq!(row.payload[\"issue_id\"], issue.id.to_string());\n    assert_eq!(row.payload[\"from_status\"], \"triage\");\n    assert_eq!(row.payload[\"to_status\"], \"todo\");\n    assert_eq!(row.payload[\"reason\"], \"ready to start\");\n}\n\n#[tokio::test]\nasync fn prior_decision_chain_links() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n\n    patch_status(&amp;issue, IssueStatus::Todo, &amp;claims, &amp;pool).await.unwrap();\n    let first = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    assert_eq!(first.payload[\"prior_decision_chain\"], serde_json::Value::Null);\n\n    patch_status(&amp;issue, IssueStatus::Doing, &amp;claims, &amp;pool).await.unwrap();\n    let second = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    let prior_chain = second.payload[\"prior_decision_chain\"].as_str().unwrap();\n    assert_eq!(prior_chain, hex::encode(first.chain));\n}\n\n#[tokio::test]\nasync fn reason_over_500_chars_returns_400() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    let big_reason = \"x\".repeat(501);\n    let err = patch_issue(issue.id, PatchIssueRequest {\n        status: Some(IssueStatus::Todo), reason: Some(big_reason),\n        ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.expect_err(\"expected ReasonTooLong\");\n    assert!(matches!(err, IssueError::Decision(DecisionError::ReasonTooLong { .. })));\n}\n\n#[tokio::test]\nasync fn cross_module_links_extracted() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Doing).await;\n    let reason = \"marked done after approval in chat://thread/abc and email://thread/xyz\";\n    patch_issue(issue.id, PatchIssueRequest {\n        status: Some(IssueStatus::Done), reason: Some(reason.into()), ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n\n    let row = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    let links = row.payload[\"cross_module_links\"].as_array().unwrap();\n    assert_eq!(links.len(), 2);\n    assert!(links.iter().any(|v| v.as_str().unwrap().starts_with(\"chat://\")));\n    assert!(links.iter().any(|v| v.as_str().unwrap().starts_with(\"email://\")));\n}\n\n#[tokio::test]\nasync fn memory_emit_failure_rolls_back_status() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    memory_test_helper::inject_emit_failure();\n\n    let err = patch_issue(issue.id, PatchIssueRequest {\n        status: Some(IssueStatus::Todo), ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.expect_err(\"expected MemoryEmitFailed\");\n    assert!(matches!(err, IssueError::Decision(DecisionError::MemoryEmitFailed(_))));\n\n    // Status NOT changed\n    let after: Issue = sqlx::query_as(\"SELECT * FROM issues WHERE id = $1\").bind(issue.id).fetch_one(&amp;pool).await.unwrap();\n    assert_eq!(after.status, IssueStatus::Triage);\n    memory_test_helper::clear_emit_failure();\n}\n\n#[tokio::test]\nasync fn no_decision_on_no_status_change() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Todo).await;\n    patch_issue(issue.id, PatchIssueRequest {\n        title: Some(\"retitled\".into()), ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n    let count = memory_test_helper::count_rows_since(\"proj.decision\", recent()).await;\n    assert_eq!(count, 0);   // no status change, no decision\n}\n\n#[tokio::test]\nasync fn default_sync_class_and_acl() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    patch_status(&amp;issue, IssueStatus::Todo, &amp;claims, &amp;pool).await.unwrap();\n    let row = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    assert_eq!(row.meta[\"sync_class\"], \"shareable\");\n    assert_eq!(row.meta[\"acl\"], serde_json::json!([]));\n}\n\n#[tokio::test]\nasync fn search_decisions_by_issue() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    patch_status(&amp;issue, IssueStatus::Todo, &amp;claims, &amp;pool).await.unwrap();\n    patch_status(&amp;issue, IssueStatus::Doing, &amp;claims, &amp;pool).await.unwrap();\n    patch_status(&amp;issue, IssueStatus::Done, &amp;claims, &amp;pool).await.unwrap();\n\n    let search_results = memory_search::search(&amp;format!(\"kind:proj.decision AND issue_id:{}\", issue.id), claims.tenant_id).await.unwrap();\n    assert_eq!(search_results.len(), 3);\n    // Verify chain links in chronological order\n    for window in search_results.windows(2) {\n        let earlier = &amp;window[0];\n        let later = &amp;window[1];\n        assert_eq!(later.payload[\"prior_decision_chain\"], hex::encode(&amp;earlier.chain));\n    }\n}\n\n#[tokio::test]\nasync fn ac17_reason_redacted() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    patch_issue(issue.id, PatchIssueRequest {\n        reason: Some(\"Email alice@cyberskill.world about this\".into()),\n        ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n    let row = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    let r = row.payload[\"reason\"].as_str().unwrap();\n    assert!(!r.contains(\"alice@cyberskill.world\"));\n    assert!(r.contains(\"&lt;EMAIL&gt;\"));\n}\n\n#[tokio::test]\nasync fn ac19_retraction_emits_separate_row() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Triage).await;\n    patch_status(&amp;issue, IssueStatus::Todo, &amp;claims, &amp;pool).await.unwrap();\n    let original = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    let decision_id = uuid::Uuid::parse_str(original.payload[\"decision_id\"].as_str().unwrap()).unwrap();\n\n    retract_decision(decision_id, \"wrong call; reverting\", &amp;claims).await.unwrap();\n    let retraction = memory_test_helper::find_latest(\"proj.decision_retracted\").unwrap();\n    assert_eq!(retraction.payload[\"retracts_decision_id\"], decision_id.to_string());\n    assert_eq!(retraction.payload[\"retraction_reason\"], \"wrong call; reverting\");\n\n    // Original row unchanged.\n    let original_again = memory_test_helper::find_by_decision_id(decision_id).unwrap();\n    assert_eq!(original_again.chain, original.chain);\n}\n\n#[tokio::test]\nasync fn ac20_already_retracted_rejected() {\n    let env = test_env_with_decision().await;\n    let decision_id = env.last_decision_id();\n    retract_decision(decision_id, \"first retract\", &amp;env.claims).await.unwrap();\n    let err = retract_decision(decision_id, \"second retract\", &amp;env.claims).await.expect_err(\"must fail\");\n    assert!(matches!(err, DecisionError::AlreadyRetracted(_)));\n}\n\n#[tokio::test]\nasync fn ac21_bulk_patch_one_row_per_issue() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    let issues: Vec&lt;_&gt; = (0..3).map(|_| create_test_issue(&amp;pool, &amp;claims, IssueStatus::Todo))\n        .collect::&lt;futures::future::JoinAll&lt;_&gt;&gt;().await;\n    let ids: Vec&lt;_&gt; = issues.iter().map(|i| i.id).collect();\n    bulk_patch_status(&amp;ids, IssueStatus::Doing, \"starting\", &amp;claims, &amp;pool).await.unwrap();\n    let rows = memory_test_helper::find_all_since(\"proj.decision\", recent()).await;\n    assert_eq!(rows.len(), 3);\n    let mut emitted_ids: Vec&lt;_&gt; = rows.iter()\n        .map(|r| uuid::Uuid::parse_str(r.payload[\"issue_id\"].as_str().unwrap()).unwrap()).collect();\n    emitted_ids.sort();\n    let mut expected_ids = ids.clone(); expected_ids.sort();\n    assert_eq!(emitted_ids, expected_ids);\n}\n\n#[rstest]\n#[case(\"chat://Thread/ABC and chat://thread/abc\",  vec![\"chat://thread/abc\"])]\n#[case(\"email://thread/XYZ\",                       vec![\"email://thread/xyz\"])]\n#[case(\"meeting://2026-05-16-standup\",             vec![\"meeting://2026-05-16-standup\"])]\n#[case(\"nothing here\",                             vec![])]\nfn ac22_cross_module_link_normalisation(#[case] reason: &amp;str, #[case] expected: Vec&lt;&amp;str&gt;) {\n    let links = extract_links_normalised(reason);\n    assert_eq!(links, expected);\n}\n\n#[tokio::test]\nasync fn ac27_required_reason_enforced() {\n    let (pool, claims) = test_setup_with_engagement().await;\n    set_tenant_require_reason(&amp;pool, claims.tenant_id, &amp;[\"done\"]).await;\n    let issue = create_test_issue(&amp;pool, &amp;claims, IssueStatus::Doing).await;\n\n    let err = patch_issue(issue.id, PatchIssueRequest {\n        status: Some(IssueStatus::Done), reason: None, ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.expect_err(\"expected ReasonRequired\");\n    assert!(matches!(err, IssueError::Decision(DecisionError::ReasonRequired { .. })));\n\n    // With reason → succeeds.\n    patch_issue(issue.id, PatchIssueRequest {\n        status: Some(IssueStatus::Done), reason: Some(\"client approved\".into()),\n        ..Default::default()\n    }, None, &amp;claims.claims, &amp;pool, \"req\").await.unwrap();\n}\n\n#[tokio::test]\nasync fn ac28_chain_anchor_self_verifies() {\n    let env = test_env_with_decision().await;\n    let row = env.last_decision_row();\n    let chain_anchor = row.payload[\"chain_anchor\"].as_str().unwrap();\n    let mut payload_minus_anchor = row.payload.clone();\n    payload_minus_anchor.as_object_mut().unwrap().remove(\"chain_anchor\");\n    let canon = serde_json::to_string(&amp;payload_minus_anchor).unwrap();\n    let computed = hex::encode(sha2::Sha256::digest(canon.as_bytes()));\n    assert_eq!(chain_anchor, computed);\n}\n\n#[tokio::test]\nasync fn ac30_engagement_acl_default_applied() {\n    let env = test_env().await;\n    let engagement = env.create_engagement_with_acl_default(vec![\"@legal\".into()]).await;\n    let issue = env.create_issue_in_engagement(engagement.id, IssueStatus::Triage).await;\n    patch_status(&amp;issue, IssueStatus::Todo, &amp;env.claims, &amp;env.pool).await.unwrap();\n    let row = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    assert_eq!(row.meta[\"acl\"], serde_json::json!([\"@legal\"]));\n}\n\n#[tokio::test]\nasync fn ac31_per_decision_override_beats_engagement() {\n    let env = test_env().await;\n    let engagement = env.create_engagement_with_acl_default(vec![\"@legal\".into()]).await;\n    let issue = env.create_issue_in_engagement(engagement.id, IssueStatus::Triage).await;\n    patch_issue(issue.id, PatchIssueRequest {\n        acl_override: Some(vec![\"@stephen\".into()]),\n        ..Default::default()\n    }, None, &amp;env.claims.claims, &amp;env.pool, \"req\").await.unwrap();\n    let row = memory_test_helper::find_latest(\"proj.decision\").unwrap();\n    assert_eq!(row.meta[\"acl\"], serde_json::json!([\"@stephen\"]));\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>See §3.</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-001</strong> — Issue schema + status FSM.</li><li><strong>TASK-AI-003</strong> — memory_writer for audit row.</li><li><strong>TASK-MEMORY-106</strong> — sync_class enforcement at sync boundary.</li><li><strong>TASK-MEMORY-108</strong> — search-by-kind queryable.</li><li>Crates: <code>regex@1</code>, <code>sqlx</code>, <code>tokio</code>, <code>serde</code>, <code>hex</code>.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<h3>Decision row</h3>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.decision\",\n  \"ts_ns\": 1747526400000000000,\n  \"meta\": {\n    \"sync_class\": \"shareable\",\n    \"acl\": []\n  },\n  \"payload\": {\n    \"issue_id\": \"issue-...\",\n    \"tenant_id\": \"550e...\",\n    \"from_status\": \"doing\",\n    \"to_status\": \"review\",\n    \"reason\": \"Implementation complete; awaiting Stephen review\",\n    \"decided_by_subject_id\": \"subject-stephen-...\",\n    \"prior_decision_chain\": \"a3f9c8d7e6b5a4f3...\",\n    \"cross_module_links\": [\"chat://thread/abc-123\"],\n    \"request_id\": \"req_...\"\n  }\n}</code></pre>\n<h3>PATCH with reason</h3>\n<pre><code class=\"language-http\">PATCH /v1/proj/issues/issue-... HTTP/1.1\nContent-Type: application/json\n{\n  \"status\": \"review\",\n  \"reason\": \"Implementation complete; see chat://thread/abc-123 for details\"\n}\n\n→ 200 OK + decision row emitted</code></pre>\n<h3>Search per-issue history</h3>\n<pre><code class=\"language-http\">GET /v1/memory/search?q=kind:proj.decision%20AND%20issue_id:issue-... HTTP/1.1\n→ 200 OK\n{\n  \"items\": [\n    { \"ts\": \"...\", \"from_status\": \"triage\", \"to_status\": \"todo\", ... },\n    { \"ts\": \"...\", \"from_status\": \"todo\", \"to_status\": \"doing\", ... },\n    { \"ts\": \"...\", \"from_status\": \"doing\", \"to_status\": \"review\", ... }\n  ]\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Mandatory reason for terminal transitions (slice 3+).</li><li>Decision-quality scoring (e.g., \"this decision was reverted within 7 days\") — slice 5+.</li><li>Per-decision attachments (file references) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>memory unreachable</td><td>memory_writer error</td><td>tx rollback; 503 audit-before-action</td><td>Operator investigates memory</td></tr><tr><td>Prior decision missing (first transition)</td><td>empty query</td><td>prior_decision_chain: null</td><td>By design</td></tr><tr><td>Reason exceeds 500 chars</td><td>length check</td><td>400 reason_too_long</td><td>Caller shortens</td></tr><tr><td>Cross-module link malformed (e.g., <code>chat:/</code>)</td><td>regex doesn't match</td><td>Not extracted; OK</td><td>By design</td></tr><tr><td>Decision emit succeeded but DB UPDATE failed</td><td>tx rollback</td><td>Both reverted</td><td>By design</td></tr><tr><td>Same status PATCH (no transition)</td><td>check before emit</td><td>No decision row</td><td>By design</td></tr><tr><td>Tenant-admin ACL change before emit</td><td>meta override applied</td><td>Restricted decision</td><td>By design</td></tr><tr><td>Concurrent PATCH races</td><td>optimistic lock at TASK-PROJ-001</td><td>First wins; second 412</td><td>By design</td></tr><tr><td>Decision search by issue returns out of order</td><td>memory search not chain-aware</td><td>Caller sorts by ts_ns</td><td>By design</td></tr><tr><td>Reason contains PII</td><td>not currently scrubbed</td><td>Stored verbatim</td><td>Slice 3+ adds Presidio scrub</td></tr><tr><td>Cross-tenant link reference</td><td>resolution out of scope</td><td>Caller discovers broken link</td><td>By design</td></tr><tr><td>memory slow (&gt;100ms emit)</td><td>OTel histogram</td><td>Sev-3 alarm</td><td>Investigate memory_writer</td></tr><tr><td>Concurrent decisions for same issue</td><td>independent emit</td><td>Both succeed; chain may interleave</td><td>By design</td></tr><tr><td>sync_class not respected at sync</td><td>TASK-MEMORY-106 enforces</td><td>Filtered at sync boundary</td><td>By design</td></tr><tr><td>ACL too restrictive</td><td>engagement member can't see own decision</td><td>Operator review</td><td>Manual fix</td></tr><tr><td>reason field deleted after emit</td><td>row immutable</td><td>N/A</td><td>By design</td></tr><tr><td>PII redaction crash (rare)</td><td>catch_unwind</td><td>emit fails; tx rolls back; SEV-2</td><td>Operator fixes redactor</td></tr><tr><td>Redacted reason loses semantic meaning</td><td>over-redaction</td><td>observable in audits</td><td>Operator tunes redactor</td></tr><tr><td>decision_id UUIDv7 collision (theoretical)</td><td>unique constraint</td><td>second attempt retries</td><td>None</td></tr><tr><td>Retraction of non-existent decision_id</td><td>lookup miss</td><td>404 decision_not_found</td><td>None</td></tr><tr><td>Bulk PATCH partial failure (issue B fails midway)</td><td>tx rolls back ALL</td><td>none of A/B/C committed</td><td>Caller retries</td></tr><tr><td>Cross-module link with non-allowed scheme (e.g. ftp://)</td><td>regex doesn't match</td><td>not extracted</td><td>By design</td></tr><tr><td>Cross-module link to deleted resource (e.g. archived chat thread)</td><td>not validated at extract</td><td>dangling link</td><td>Resolver handles at read time</td></tr><tr><td>decision_attributes contains reserved key with wrong type</td><td>passed through</td><td>downstream may mishandle</td><td>Slice 3+ adds JSON schema validation</td></tr><tr><td>Index not used (planner regression)</td><td>EXPLAIN ANALYZE catches</td><td>linear scan; slow</td><td>Operator runs ANALYZE</td></tr><tr><td>Immutability bypass via direct DB write</td><td>RLS+REVOKE prevents</td><td>rejected</td><td>None</td></tr><tr><td>Required-reason policy changed mid-PATCH</td><td>check uses snapshot at start</td><td>next PATCH uses new policy</td><td>None</td></tr><tr><td>Empty reason (\"\") with required-reason policy</td><td>trim().is_empty() == true</td><td>rejected</td><td>None</td></tr><tr><td>chain_anchor mismatch (corruption)</td><td>self-verify fails</td><td>recipient sees verification fail</td><td>Operator investigates</td></tr><tr><td>decision_session_id missing (legacy clients)</td><td>optional field; auto-generated</td><td>rows have unique session_ids</td><td>None</td></tr><tr><td>Engagement ACL default with deleted engagement</td><td>resolve_decision_acl returns tenant fallback</td><td>shareable + []</td><td>None</td></tr><tr><td>Per-decision ACL override with unknown subject_id</td><td>stored verbatim</td><td>resolver may not find match</td><td>Operator</td></tr><tr><td>Bulk PATCH &gt; 1000 issues</td><td>bounded at 1000 per request</td><td>413</td><td>Caller batches</td></tr><tr><td>Cross-module link extraction regex catastrophic backtracking</td><td>bounded character class</td><td>None</td><td>None</td></tr><tr><td>Concurrent retraction of same decision (race)</td><td>second sees <code>already_retracted</code></td><td>first wins</td><td>None</td></tr><tr><td>Decision row size &gt; 64KB (huge attributes)</td><td>bounded at 64KB</td><td>400</td><td>Caller</td></tr></tbody></table>\n<hr>\n<h2>§11 — Notes</h2>\n<ul><li>Decisions are first-class chain entries; DB row is the projection.</li><li>Audit-before-action prevents partial state corruption.</li><li>Per-issue chain via prior_decision_chain enables history queries without joins.</li><li>Cross-module link extraction creates structured references usable by other modules' UIs.</li><li>Default sync_class shareable matches engagement-member workflows.</li><li>500-char reason limit enforces conciseness; long-form rationales go in memory as separate decisions/ files.</li><li>Slice 3+ enforces reason for terminal transitions (<code>done</code> requires reason) via the per-tenant <code>require_reason_for</code> policy (default empty → slice 3+ defaults to <code>['done','cancelled']</code>).</li><li>decided_by_subject_id from JWT enables retro discussion + accountability.</li><li>100ms emit budget is interactive-UX-compatible.</li><li>The PII redactor for reason text reuses the TASK-MEMORY-111 ruleset; updates to the ruleset apply to PROJ without code changes.</li><li>We chose UUIDv7 for decision_id (not v4) so per-issue history queries can sort by decision_id directly (v7 is time-ordered).</li><li>Retraction as a separate row was chosen over mutation because: (a) audit-chain integrity is non-negotiable; (b) operators expect \"the original decision exists forever; supersession is a new fact\"; (c) it parallels TASK-CHAT-006's <code>chat.message_edited</code> pattern.</li><li>Bulk-PATCH emits one row per issue because per-issue queryability is the primary use case; aggregate rows would force consumers to deconstruct.</li><li>Cross-module link normalisation lower-cases the entire URL including the path. We considered case-preserving the path (some systems are case-sensitive) but the lookup-symmetry benefit outweighs the rare case-sensitive system.</li><li>The dedup-on-extract (HashSet) prevents <code>chat://x and chat://x and chat://x</code> producing three entries.</li><li>decision_attributes is an open-key JSONB to allow per-tenant extension without code changes; reserved keys (client_facing, requires_followup, deadline_iso) cover the common case.</li><li>The O(log n) per-issue index requires the per-tenant partition prefix; without it, the index would degenerate to O(n).</li><li>Immutability is enforced via Postgres GRANT/REVOKE (cyberos_app has SELECT + INSERT only on memory_outbox), not just handler code. Defense in depth.</li><li>The required-reason policy is per-status, not per-tenant boolean, so different statuses can have different requirements.</li><li>chain_anchor_in_payload lets recipients verify a single decision without crawling the whole chain — useful for embedding decisions in DSAR exports (TASK-CHAT-012 sibling pattern).</li><li>decision_session_id is generated client-side (or by the orchestrator TASK-CUO-101) and propagated; the proj handler trusts it.</li><li>Per-engagement ACL default eliminates per-issue boilerplate for engagement-level policies; per-decision override remains for exceptions.</li><li>We don't store the JWT itself in the audit row (only <code>decided_by_subject_id</code>) because JWTs may contain claims that shouldn't be in long-term audit trails.</li><li>The cross-module link extraction is best-effort: malformed links (<code>chat:/</code> with single slash) are ignored, not stored as invalid.</li><li>The 100ms p95 emit budget covers DB UPDATE + prior_chain fetch + PII redact + memory row write + chain commit. Each is ~20ms typical.</li><li>We considered streaming WAL changes to memory (like TASK-CHAT-005's bridge pattern) for decisions but rejected: decisions are interactive (operator clicks); synchronous emit gives immediate feedback.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-002. Status: done (implemented 2026-05-23).</em></p>";
