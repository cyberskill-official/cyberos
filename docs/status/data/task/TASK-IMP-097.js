window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-097"]="<h1>TASK-IMP-097: GUIDE gains the sandboxed-agent runbook section</h1>\n<h2>Summary</h2>\n<p>Three governed runs re-learned the same sandbox facts by hitting them. Write them down where consumers read: a short \"Running CyberOS under sandboxed agents\" section in the GUIDE source - command caps and background-process death, the local-clone pattern for build/test with a local-ref push back to the mounted repo, manual hook-obligation replay with <code>--no-verify</code> plus recorded evidence, and mount unlink/permission quirks - with a one-line cross-reference from ship-tasks.</p>\n<h2>Problem</h2>\n<p>Every fact in that list cost real time to discover and none of it is discoverable today except by reading incident addenda in task folders.</p>\n<h2>Proposed Solution</h2>\n<p>One section in tools/install/docs/index.md (which builds into the payload's GUIDE.md), written as a runbook: symptom, cause, working pattern. Explicit that the local-clone push is a local ref move - the no-push policy stays intact. ship-tasks.md gains one cross-reference line pointing constrained environments at the GUIDE section (prose pointer, no version bump - TASK-IMP-099 carries this round's bump). test_full_sdp_payload.sh gains a grep gate that the built GUIDE carries the section.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>A separate RUNBOOK.md. Rejected: the GUIDE is the one consumer document install ships and the checklist's D2 pass already reads it end-to-end; a second doc is a second place to go stale.</li><li>Doctrine passages in ship-tasks.md instead. Rejected: these are environment facts, not workflow rules; the workflow already carries the two normative rules (v2.6.3) and links out for the rest.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: the built payload's GUIDE.md carries the section with the local-clone pattern and the hook-replay rule, gated by the suite on every run. Baseline: zero consumer-facing documentation of any of it. Deadline: final acceptance.</li><li>Guardrail: ship-tasks.md carries exactly one cross-reference line (no duplicated rule text to drift).</li></ul>\n<h2>Scope</h2>\n<p>In scope: the GUIDE section, the ship-tasks cross-reference line, the payload grep gate.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>New workflow rules (v2.6.3 already carries the normative pair).</li><li>Automation of the local-clone pattern.</li></ul>\n<h2>Dependencies</h2>\n<ul><li>Shares ship-tasks.md with TASK-IMP-099 - one agent, serial, per the batch plan.</li></ul>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Claude (Fable 5) running the CyberOS task-author skill inside Cowork.</li><li><strong>Scope:</strong> spec drafted from IMP-13 and the recorded run observations; implementation under ship-tasks supervision.</li><li><strong>Human review:</strong> batch-4 PLAN approved 2026-07-17 (§0a); both HITL gates are recorded human verdicts.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 tools/install/docs/index.md MUST gain a \"Running CyberOS under sandboxed agents\" section covering: per-command time caps and background-process death (hook chains, package installs), the local-clone build/test pattern with local-ref push back to the mounted repo (explicitly not a remote push), manual hook-obligation replay with <code>--no-verify</code> plus recorded evidence, and mount unlink/permission quirks.</li><li>1.2 ship-tasks.md MUST gain one cross-reference line pointing constrained environments at the GUIDE section, without duplicating its content and without a workflow_version bump.</li><li>1.3 The built payload's GUIDE.md MUST carry the section, gated by a grep in test_full_sdp_payload.sh against a scratch build.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC 1 (traces_to: #1.1, #1.3) - scratch payload GUIDE carries the section incl. local-clone and hook-replay lines - test: <code>tools/install/tests/test_full_sdp_payload.sh (sandbox-runbook grep gate)</code></li><li>[ ] AC 2 (traces_to: #1.2) - ship-tasks.md carries exactly one cross-reference line - verify: recorded grep -c in the gate log (single prose line; a test for one line of prose is out of proportion, and t12's doctrine gate already pins the file's normative content).</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>The GUIDE is consumer-facing: the section names no internal session paths or tool brands beyond what reproduces anywhere (generic \"sandboxed agent\" framing).</li><li>Cross-reference placement: near the §11a/testing doctrine it complements, so readers meet rule and runbook together.</li><li>Security-class: none - documentation; the no-push policy is restated, not weakened.</li></ul>";
