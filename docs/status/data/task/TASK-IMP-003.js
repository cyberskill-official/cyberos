window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-003"]="<h1>TASK-IMP-003: Secret scanning in CI and pre-push</h1>\n<h2>Summary</h2>\n<p>Add SHA-pinned gitleaks CI, .gitleaks.toml allowlists, check-secrets.sh, and pre-push soft-skip when gitleaks is missing locally.</p>\n<h2>Problem</h2>\n<p>Secrets can land in git with no mechanical stop. Pre-push only gates Rust services/. Docs contain example tokens that look like secrets.</p>\n<h2>Proposed Solution</h2>\n<p>Pin gitleaks-action by SHA, allowlist known false-positive paths, wrap gitleaks with --probe-fixture fail proof, soft-skip in pre-push when binary absent.</p>\n<h2>Alternatives Considered</h2>\n<ul><li>GitHub secret scanning only. Rejected as sole control.</li><li>Hard-fail pre-push when gitleaks missing. Rejected; soft-skip + CI hard-fail.</li><li>TruffleHog. Deferred; gitleaks is R27.</li></ul>\n<h2>Success Metrics</h2>\n<ul><li>Primary: CI secret-scan SHA-pinned; probe fixture fails; clean tree with allowlists passes.</li><li>Guardrail: pre-push does not block when gitleaks absent.</li></ul>\n<h2>Scope</h2>\n<p>In scope: workflow, config, check script, pre-push, tests.</p>\n<h3>Out of scope / Non-Goals</h3>\n<ul><li>Rewriting historical example-token commits.</li><li>Enterprise GHAS configuration.</li></ul>\n<h2>Dependencies</h2>\n<p>None.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Composer (Cursor agent) under batch/12b operator mandate.</li><li><strong>Scope:</strong> gitleaks CI + pre-push soft-skip. <strong>re-derived and CONFIRMED:</strong> pre-push gates Rust services/ only; R27 names gitleaks. <strong>re-derived and CORRECTED:</strong> baseline scan reported 42 findings (docs/status + FR examples) — allowlists required. <strong>measured and ADDED:</strong> probe-fixture fail path and SHA-pinned Action were absent.</li><li><strong>Human review:</strong> Operator session HITL override for batch/12b; evidence under docs/batches/.</li></ul>\n<h2>1. Description (normative)</h2>\n<ul><li>1.1 A CI workflow MUST run gitleaks on push and pull_request and MUST fail when leaks are reported (no continue-on-error).</li><li>1.2 The gitleaks Action invocation MUST be pinned to a full commit SHA.</li><li>1.3 .gitleaks.toml MUST allowlist documented false-positive paths so the gate is enforceable.</li><li>1.4 .githooks/pre-push MUST invoke the secret checker when gitleaks is installed, and MUST soft-skip with a clear message when it is not.</li><li>1.5 tools/install/check-secrets.sh --probe-fixture MUST plant a fake AWS-shaped key in a temporary directory and exit non-zero when gitleaks is available.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC 1 (traces_to: #1.1) - hard-fail workflow — test: <code>tools/install/tests/test_secret_scan.sh::t_workflow_hard_fail</code></li><li>[x] AC 2 (traces_to: #1.2) - SHA pin — test: <code>tools/install/tests/test_secret_scan.sh::t_action_sha_pinned</code></li><li>[x] AC 3 (traces_to: #1.3) - allowlist — test: <code>tools/install/tests/test_secret_scan.sh::t_allowlist_present</code></li><li>[x] AC 4 (traces_to: #1.4) - pre-push soft-skip — test: <code>tools/install/tests/test_secret_scan.sh::t_pre_push_soft_skip</code></li><li>[x] AC 5 (traces_to: #1.5) - probe fixture — test: <code>tools/install/tests/test_secret_scan.sh::t_probe_fixture_fails</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Allowlists MUST be path-scoped; real secrets outside them MUST still fail CI.</li></ul>";
