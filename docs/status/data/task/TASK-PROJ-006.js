window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PROJ-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The billable-cascade resolver <strong>MUST</strong> compute <code>billable: bool</code> for each time-entry write by consulting 4 sources in strict precedence order:</p>\n<ol><li><strong>Tier 1 — Member-override</strong>: explicit per-member rule on this engagement (e.g. \"Alice is non-billable on this engagement\"). Schema: <code>member_billable_overrides(member_id, engagement_id, billable, reason, created_at, tenant_id)</code>.</li><li><strong>Tier 2 — Task-class override</strong>: explicit per-engagement per-task-class rule (e.g. \"research is non-billable on engagement X\"). Schema: <code>task_class_billable(engagement_id, task_class, billable, tenant_id)</code>.</li><li><strong>Tier 3 — Role-default</strong>: the matching rate card's <code>billable_default</code> (TASK-PROJ-005).</li><li><strong>Tier 4 — Fallback</strong>: <code>false</code>. Never silently bill.</li></ol>\n<p>The resolver:</p>\n<ol><li><strong>MUST</strong> return <code>BillableResolution { value: bool, source: Tier, tier_consulted: u8, snapshot_at: i64 }</code> — every resolution carries provenance.</li><li><strong>MUST</strong> stop at the FIRST applicable tier (no further consultation once a rule matches).</li><li><strong>MUST</strong> snapshot the resolved value AT WRITE TIME of the time-entry. Later changes to any tier do NOT retroactively modify existing entries.</li><li><strong>MUST</strong> emit <code>proj.billable_resolved</code> memory audit row per call with payload <code>{time_entry_id, member_id, engagement_id, task_class, role, billable_value, source_tier, snapshot_at_ns, trace_id}</code>.</li><li><strong>MUST</strong> validate <code>member_id</code> belongs to the engagement (cross-engagement override invalid).</li><li><strong>MUST</strong> be deterministic per <code>(member_id, task_class, role, engagement_id, at_date)</code> snapshot at AT-DATE; cascade reads rate card via <code>TASK-PROJ-005::lookup_at(at_date)</code> so the snapshot is reproducible.</li><li><strong>MUST</strong> emit OTel metrics:</li></ol>\n<ul><li><code>proj_billable_resolutions_total{value, source}</code> (counter; cardinality 2 × 4 = 8).</li><li><code>proj_billable_cascade_depth</code> (histogram).</li></ul>\n<ol><li><strong>MUST</strong> expose REST: <code>POST /api/proj/engagements/:eng/billable-cascade/resolve</code> with body <code>{member_id, task_class, role, at_date}</code> → 200 <code>BillableResolution</code>. Used by clients (Kanban, timeline) for preview before writing.</li><li><strong>MUST</strong> expose admin CRUD for Tier-1 + Tier-2 overrides:</li></ol>\n<ul><li><code>POST/DELETE /api/proj/engagements/:eng/member-overrides/:member_id</code></li><li><code>POST/DELETE /api/proj/engagements/:eng/task-class/:class</code></li></ul>\n<ol><li><strong>MUST</strong> emit memory audit on override CRUD: <code>proj.member_billable_override_set</code> / <code>proj.task_class_billable_set</code>.</li><li><strong>MUST</strong> RLS-enforce per tenant (TASK-AUTH-003).</li><li><strong>MUST</strong> validate the resolver inputs at handler boundary: <code>member_id</code> exists in tenant; <code>engagement_id</code> exists in tenant; <code>at_date</code> is not more than 5 years in the past; <code>currency</code> is in the rate-card's enum. Invalid inputs → 400 with structured error.</li><li><strong>MUST</strong> support a \"bulk-resolve\" endpoint: <code>POST /api/proj/engagements/:eng/billable-cascade/bulk</code> with <code>[{member_id, task_class, role, at_date}]</code> array (max 1000 items) → returns array of resolutions in input order. Used by timesheet-import flows.</li><li><strong>MUST</strong> mark resolved billable values as IMMUTABLE on the time entry row: any PATCH attempting to change <code>billable_snapshot</code> is rejected with 405 (force re-resolution via separate time-entry-recompute admin endpoint, which itself is audit-trail-heavy).</li><li><strong>MUST</strong> support per-engagement DEFAULT TIER-2 task-class billable: <code>cyberos_proj_engagement_settings.default_task_class_billable = {feature_work: true, research: false, ...}</code> applied to engagement creation if no per-class override exists. Tenant-admin sets the default.</li><li><strong>MUST</strong> track <code>proj_billable_resolution_latency_ms</code> histogram per call; p95 budget &lt; 20ms (cascade is in the time-entry hot path).</li><li><strong>MUST</strong> support querying historical resolutions: <code>GET /api/proj/engagements/:eng/billable-cascade/history?member=:id&amp;from=&amp;to=</code> returns memory audit rows. Useful for auditing \"why was this hour billed.\"</li><li><strong>MUST</strong> include <code>effective_overrides_applied</code> field in <code>BillableResolution</code>: even if Tier 3 matched, the response carries metadata about which other tiers existed but didn't match (e.g. \"rate card was used; member-override exists but matches the same value\"). Operator transparency.</li><li><strong>MUST</strong> support a tenant-level \"billable by default\" policy override of the §1 Tier-4 fallback: <code>cyberos_proj_tenant_settings.cascade_fallback_billable = false</code> (default false). Tenants who explicitly want default-billable can set true (e.g. internal-billing tenants). The default remains conservative.</li><li><strong>MUST</strong> validate <code>engagement_id</code> is not archived: resolutions on archived engagements are forbidden (would create new time entries on closed engagements). 409 <code>engagement_archived</code>.</li><li><strong>MUST</strong> support an \"explain\" mode on the resolve endpoint: <code>?explain=true</code> returns the full decision tree showing what each tier returned (matched/not-matched), not just the winner. Used by operator UI for debugging.</li></ol>\n<hr>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why 4 tiers (DEC-270, §1)?</strong> Real-world billable rules need granularity: org-level role defaults are too coarse (engineer is \"usually\" billable but specific engineers on specific eng aren't); per-member always-overridable is too granular (no defaults; every member needs explicit). Four tiers cover the cases without combinatorial explosion.</p>\n<p><strong>Why first-match wins (§1 #2)?</strong> Layered overrides need clear precedence. The pattern matches CSS specificity / IAM policy precedence — specific overrides general.</p>\n<p><strong>Why snapshot at write-time (DEC-271, §1 #3)?</strong> Retroactive rule changes break invoices already sent. A client paid for 100 hours billed at one rate; later flipping a member to non-billable would retroactively zero out a paid invoice. Snapshot = bill = invariant.</p>\n<p><strong>Why fallback <code>false</code> not <code>true</code> (DEC-272, §1)?</strong> Asymmetric risk: defaulting to billable means a tracking mistake bills a client (loss of trust + refund + admin overhead). Defaulting to non-billable means a missed bill (caught at invoice review). The latter is recoverable.</p>\n<p><strong>Why audit per resolution (§1 #4)?</strong> Auditors investigating \"why was this hour billed\" need the full chain: which tier matched, which rule, when. The audit row encodes the decision tree at the time of decision.</p>\n<p><strong>Why preview endpoint (§1 #8)?</strong> UX: when an engineer logs time on a task, the UI shows \"Billable: ✓ (via task_class override)\" so they know what they're committing to. Without preview, the UX shows nothing → surprise at invoice time.</p>\n<p><strong>Why input validation at handler (§1 #12)?</strong> Defence in depth: resolver assumes valid inputs (per the contract); handler validates so downstream resolver code is small + fast. Bad inputs returning 400 with structured errors is far better than silent fallthrough.</p>\n<p><strong>Why bulk-resolve (§1 #13)?</strong> Timesheet imports (CSV from external tools) have hundreds of entries; per-entry HTTP call = thousands of network round-trips. Bulk amortises the cost.</p>\n<p><strong>Why immutability on time entry (§1 #14)?</strong> Snapshot is the bill-truth; mutating it post-write retroactively re-bills. Force-re-resolution path is admin-only and audit-heavy because it's an exceptional operator workflow.</p>\n<p><strong>Why engagement-level default task-class billable (§1 #15)?</strong> Common case: every engagement has the same \"feature_work=billable, research=non-billable\" pattern. Without engagement defaults, operators set 8 task_class rows per engagement. Defaults = one set per tenant.</p>\n<p><strong>Why p95 &lt; 20ms (§1 #16)?</strong> Time-entry write is in the operator's UI critical path; &gt;20ms feels laggy. Cascade is 4 SQL queries worst-case; budget is generous.</p>\n<p><strong>Why historical resolution query (§1 #17)?</strong> Auditors investigating an invoice line item ask \"why was this billable?\" — answer requires the resolution that was made at write time. memory audit query gives the trace.</p>\n<p><strong>Why effective_overrides_applied metadata (§1 #18)?</strong> Operator transparency: \"Tier 3 matched (rate card default = true); a member override exists but also returns true, so no change.\" Without this, operators can't tell if the result is from the priority tier or a deeper one happening to agree.</p>\n<p><strong>Why per-tenant fallback override (§1 #19)?</strong> Internal-billing tenants (cost-center accounting) want default-billable; SaaS-revenue tenants want default-not-billable. One global default doesn't fit; per-tenant policy does. Default remains conservative.</p>\n<p><strong>Why archived-engagement rejection (§1 #20)?</strong> Closed engagements shouldn't accept new time entries (would re-open billing). Rejecting at the cascade layer catches operators who don't see the archived status in their UI.</p>\n<p><strong>Why explain mode (§1 #21)?</strong> Debugging cascade decisions in production: operator wants to know \"why did this resolve to false?\" — explain mode shows each tier's match/no-match status. Default mode is summary; explain is for diagnostics.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<h3>Migrations</h3>\n<pre><code class=\"language-sql\">-- 0006_member_billable_overrides.sql\nCREATE TABLE member_billable_overrides (\n    member_id      UUID NOT NULL,\n    engagement_id  UUID NOT NULL REFERENCES engagements(id),\n    billable       BOOLEAN NOT NULL,\n    reason         TEXT,\n    created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    created_by     UUID NOT NULL,\n    tenant_id      UUID NOT NULL,\n    PRIMARY KEY (member_id, engagement_id)\n);\nCREATE POLICY mbo_tenant_isolation ON member_billable_overrides\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);\n\n-- 0006_task_class_billable.sql\nCREATE TABLE task_class_billable (\n    engagement_id  UUID NOT NULL REFERENCES engagements(id),\n    task_class     TEXT NOT NULL CHECK (task_class IN\n                   ('feature_work','bug_fix','meeting','review','research','sales_call','admin','training')),\n    billable       BOOLEAN NOT NULL,\n    created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    created_by     UUID NOT NULL,\n    tenant_id      UUID NOT NULL,\n    PRIMARY KEY (engagement_id, task_class)\n);\nCREATE POLICY tcb_tenant_isolation ON task_class_billable\n    USING (tenant_id = current_setting('app.tenant_id')::uuid);</code></pre>\n<h3>Cascade resolver</h3>\n<pre><code class=\"language-rust\">// services/proj-sync/src/billable/mod.rs\nuse serde::{Deserialize, Serialize};\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, sqlx::Type, PartialEq, Eq, Hash)]\n#[serde(rename_all = \"snake_case\")]\n#[sqlx(type_name = \"TEXT\", rename_all = \"snake_case\")]\npub enum TaskClass {\n    FeatureWork, BugFix, Meeting, Review, Research, SalesCall, Admin, Training,\n}\n\n#[derive(Clone, Copy, Debug, Serialize, Deserialize, PartialEq, Eq)]\n#[serde(rename_all = \"snake_case\")]\npub enum Tier { MemberOverride, TaskClassOverride, RoleDefault, Fallback }\n\n#[derive(Clone, Debug, Serialize)]\npub struct BillableResolution {\n    pub value:           bool,\n    pub source:          Tier,\n    pub tier_consulted:  u8,             // 1..=4 (matches §1 #2 first-match-wins; this is the matching tier)\n    pub snapshot_at_ns:  i64,\n}\n\npub async fn resolve(\n    pool: &amp;sqlx::PgPool,\n    member_id: uuid::Uuid,\n    task_class: TaskClass,\n    role: crate::rate_card::Role,\n    engagement_id: uuid::Uuid,\n    at_date: chrono::NaiveDate,\n    currency: crate::rate_card::Currency,\n) -&gt; Result&lt;BillableResolution, BillableError&gt; {\n    let now_ns = chrono::Utc::now().timestamp_nanos_opt().unwrap();\n\n    // Tier 1: member-override\n    if let Some(row) = sqlx::query!(\n        \"SELECT billable FROM member_billable_overrides\n         WHERE member_id = $1 AND engagement_id = $2\",\n        member_id, engagement_id\n    ).fetch_optional(pool).await.map_err(map_db)? {\n        return Ok(BillableResolution {\n            value: row.billable, source: Tier::MemberOverride,\n            tier_consulted: 1, snapshot_at_ns: now_ns,\n        });\n    }\n\n    // Tier 2: task-class override\n    if let Some(row) = sqlx::query!(\n        \"SELECT billable FROM task_class_billable\n         WHERE engagement_id = $1 AND task_class = $2\",\n        engagement_id, task_class as TaskClass\n    ).fetch_optional(pool).await.map_err(map_db)? {\n        return Ok(BillableResolution {\n            value: row.billable, source: Tier::TaskClassOverride,\n            tier_consulted: 2, snapshot_at_ns: now_ns,\n        });\n    }\n\n    // Tier 3: role-default (rate card)\n    if let Ok(card) = crate::rate_card::lookup_at(pool, engagement_id, role, currency, at_date).await {\n        return Ok(BillableResolution {\n            value: card.billable_default, source: Tier::RoleDefault,\n            tier_consulted: 3, snapshot_at_ns: now_ns,\n        });\n    }\n\n    // Tier 4: fallback\n    Ok(BillableResolution {\n        value: false, source: Tier::Fallback,\n        tier_consulted: 4, snapshot_at_ns: now_ns,\n    })\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Tier 1 hits first</strong> — member-override = true, all else default → resolution.source = MemberOverride.</li><li><strong>Tier 1 false stops cascade</strong> — member-override = false → resolution.value = false, source = MemberOverride (does not fall through).</li><li><strong>Tier 2 hits when no Tier 1</strong> — no override + task_class_billable = true → source = TaskClassOverride.</li><li><strong>Tier 3 hits when no T1/T2</strong> — rate card default = true → source = RoleDefault.</li><li><strong>Tier 4 fallback</strong> — none of T1/T2/T3 match (e.g. no rate card for that role/date) → source = Fallback, value = false.</li><li><strong>Snapshot preserved on later override change</strong> — write entry at T1 with member-override = true; at T2 flip override to false; original entry's billable stays true (snapshotted).</li><li><strong>Cross-engagement override invalid</strong> — override row for (member, engagement_A) does NOT affect engagement_B.</li><li><strong>Preview endpoint</strong> — <code>POST /resolve</code> returns same value as actual write would.</li><li><strong>Audit row emitted</strong> — every resolve → <code>proj.billable_resolved</code> row with source_tier.</li><li><strong>Audit on override CRUD</strong> — <code>proj.member_billable_override_set</code> on POST.</li><li><strong>Counter increments</strong> — 100 resolves → counter <code>proj_billable_resolutions_total</code> sums to 100 across labels.</li><li><strong>RLS enforces</strong> — tenant A's override invisible to tenant B.</li><li><strong>Cascade depth metric</strong> — Tier 4 fallback → depth = 4; Tier 1 hit → depth = 1.</li><li><strong>Idempotent override POST</strong> — same Idempotency-Key → returns prior; same key + diff body → 409.</li><li><strong>Handler input validation</strong> — POST with <code>at_date=2010-01-01</code> (&gt;5y past) → 400 <code>at_date_too_old</code>; invalid member/engagement → 400 (AC for §1 #12).</li><li><strong>Bulk resolve handles 1000 items</strong> — POST /bulk with 1000-element array → 200 with 1000-element response in input order; &gt; 1000 → 413 (AC for §1 #13).</li><li><strong>Bulk preserves per-item independence</strong> — bulk with mix of valid + invalid → each item's status independent (AC for §1 #13).</li><li><strong>billable_snapshot PATCH rejected</strong> — direct PATCH of time entry's billable_snapshot → 405 (AC for §1 #14).</li><li><strong>Engagement-default task-class billable applied</strong> — tenant default <code>feature_work: true</code>; new engagement → has tier-2 row matching default; resolve returns true via Tier 2 (AC for §1 #15).</li><li><strong>Latency p95 &lt; 20ms</strong> — load test 1000 resolves → histogram p95 &lt; 20ms (AC for §1 #16).</li><li><strong>Historical query returns memory rows</strong> — GET /history → audit rows for member+time range (AC for §1 #17).</li><li><strong>effective_overrides_applied in response</strong> — resolve where Tier 1 + Tier 3 both exist and agree → metadata shows both (AC for §1 #18).</li><li><strong>Tenant fallback override honoured</strong> — set <code>cascade_fallback_billable=true</code>; Tier 4 fallback → value=true (AC for §1 #19).</li><li><strong>Archived engagement rejected</strong> — resolve on archived eng → 409 <code>engagement_archived</code> (AC for §1 #20).</li><li><strong>Explain mode returns full tree</strong> — POST /resolve?explain=true → response includes per-tier match/value; default mode returns summary only (AC for §1 #21).</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn tier_1_member_override_first_match() {\n    let env = TestEnv::new().await;\n    let (eng, alice) = env.bootstrap().await;\n    env.set_member_override(alice, eng, true).await;\n    env.set_task_class(eng, TaskClass::Research, false).await;\n    let res = resolve(&amp;env.pool, alice, TaskClass::Research, Role::Engineer, eng,\n                      \"2026-05-16\".parse().unwrap(), Currency::VND).await.unwrap();\n    assert_eq!(res.value, true);\n    assert_eq!(res.source, Tier::MemberOverride);\n    assert_eq!(res.tier_consulted, 1);\n}\n\n#[tokio::test]\nasync fn tier_2_task_class_when_no_member() {\n    let env = TestEnv::new().await;\n    let (eng, alice) = env.bootstrap().await;\n    env.set_task_class(eng, TaskClass::Meeting, false).await;\n    env.create_rate_card_default(eng, Role::Engineer, true).await;\n    let res = resolve(&amp;env.pool, alice, TaskClass::Meeting, Role::Engineer, eng,\n                      \"2026-05-16\".parse().unwrap(), Currency::VND).await.unwrap();\n    assert_eq!(res.source, Tier::TaskClassOverride);\n    assert_eq!(res.value, false);\n}\n\n#[tokio::test]\nasync fn tier_4_fallback_is_false() {\n    let env = TestEnv::new().await;\n    let (eng, alice) = env.bootstrap().await;\n    // No overrides; no rate card\n    let res = resolve(&amp;env.pool, alice, TaskClass::Research, Role::Engineer, eng,\n                      \"2026-05-16\".parse().unwrap(), Currency::VND).await.unwrap();\n    assert_eq!(res.source, Tier::Fallback);\n    assert_eq!(res.value, false);\n    assert_eq!(res.tier_consulted, 4);\n}\n\n#[tokio::test]\nasync fn snapshot_immutable_after_override_change() {\n    let env = TestEnv::new().await;\n    let (eng, alice) = env.bootstrap().await;\n    env.set_member_override(alice, eng, true).await;\n    let entry = env.create_time_entry(alice, eng, TaskClass::FeatureWork).await;\n    assert_eq!(entry.billable_snapshot, true);\n\n    env.set_member_override(alice, eng, false).await;\n    let refetched = env.read_time_entry(entry.id).await;\n    assert_eq!(refetched.billable_snapshot, true);   // snapshot preserved\n}</code></pre>\n<hr>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API + DB schema above.)</p>\n<hr>\n<h2>§7 — Dependencies</h2>\n<ul><li><strong>TASK-PROJ-005</strong> — rate card lookup at Tier 3.</li><li><strong>TASK-PROJ-007 (downstream)</strong> — billing modes consume resolved billable flag.</li><li><strong>TASK-TIME-005</strong> — time-entry snapshot field stores this resolution.</li><li><strong>TASK-AUTH-003</strong> — RLS.</li></ul>\n<hr>\n<h2>§8 — Example payloads</h2>\n<pre><code class=\"language-json\">{\n  \"kind\": \"proj.billable_resolved\",\n  \"payload\": {\n    \"time_entry_id\":    \"te-...\",\n    \"member_id\":        \"mb-...\",\n    \"engagement_id\":    \"eng-...\",\n    \"task_class\":       \"feature_work\",\n    \"role\":             \"engineer\",\n    \"billable_value\":   true,\n    \"source_tier\":      \"role_default\",\n    \"tier_consulted\":   3,\n    \"snapshot_at_ns\":   1747407137483000000,\n    \"trace_id\":         \"0af...\"\n  }\n}</code></pre>\n<hr>\n<h2>§9 — Open questions</h2>\n<p>All resolved. Deferred:</p>\n<ul><li>Per-member-per-task-class override (5th tier) — slice 4+; current 4 tiers cover empirical cases.</li><li>Time-of-day rules (after 6pm = non-billable) — slice 4+.</li></ul>\n<hr>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>No rate card for role+date</td><td>Tier 3 lookup_at Err</td><td>Falls through to Tier 4 (false)</td><td>Operator adds card OR accepts non-billable</td></tr><tr><td>Duplicate override on PK</td><td>unique constraint</td><td>409 on POST</td><td>Caller checks first</td></tr><tr><td>Cross-engagement override</td><td>row scoped to (member, engagement)</td><td>Other engagements unaffected</td><td>None</td></tr><tr><td>Race: resolve mid-override-update</td><td>each resolve uses current snapshot</td><td>Snapshot at resolve-time is consistent</td><td>None</td></tr><tr><td>Tier 1 override deleted</td><td>next resolve falls to Tier 2</td><td>New entries get new value</td><td>By design (snapshot protects historical)</td></tr><tr><td>Audit emit fails</td><td>Resolved value still returned; audit lost</td><td>sev-2</td><td>Operator restores memory</td></tr><tr><td>Currency mismatch in rate-card lookup</td><td>lookup returns None → fallback to Tier 4</td><td>False</td><td>Operator adds rate card</td></tr><tr><td>Tenant isolation broken (RLS bug)</td><td>property test catches</td><td>CI blocked</td><td>Author fixes</td></tr><tr><td>Idempotency-Key reuse with different body</td><td>handler check</td><td>409</td><td>Caller uses fresh key</td></tr><tr><td>Member not in engagement</td><td>should reject upstream</td><td>resolver doesn't validate; Tier 1 returns no row → falls through</td><td>Upstream caller validates</td></tr><tr><td>Storage Err mid-cascade</td><td>sqlx Err</td><td>500</td><td>Operator restores DB</td></tr><tr><td>Two tier-2 rules same engagement+class</td><td>PK prevents</td><td>409</td><td>None</td></tr><tr><td>Resolution at date in past</td><td>lookup_at uses historical card</td><td>Correct</td><td>None</td></tr><tr><td>TaskClass enum drift</td><td>CHECK constraint</td><td>422</td><td>Caller uses valid enum</td></tr><tr><td>Bulk-resolve &gt; 1000 items</td><td>bounded</td><td>413</td><td>Caller batches</td></tr><tr><td>Bulk-resolve item validation failure</td><td>per-item status</td><td>partial results</td><td>Caller</td></tr><tr><td>Engagement archived mid-resolve</td><td>check at top of resolver</td><td>409</td><td>Operator</td></tr><tr><td>Tenant fallback override = true</td><td>Tier 4 returns true</td><td>by config</td><td>None</td></tr><tr><td>Explain mode response &gt; 100KB</td><td>bounded by tier count (4)</td><td>None</td><td>None</td></tr><tr><td>Currency mismatch in rate-card lookup_at</td><td>Tier 3 returns None → Tier 4</td><td>falls through</td><td>Operator adds card</td></tr><tr><td>at_date in past after engagement archive</td><td>resolver returns archived error</td><td>409</td><td>None</td></tr><tr><td>Bulk-resolve includes deleted member</td><td>per-item 400</td><td>proceeds with others</td><td>None</td></tr><tr><td>Same task_class enum value different storage (case drift)</td><td>strict enum matching</td><td>422</td><td>Caller normalises</td></tr><tr><td>Resolution latency &gt; 20ms</td><td>OBS histogram</td><td>SEV-3 if sustained</td><td>Operator investigates</td></tr><tr><td>Resolver called &gt; tenant rate-limit</td><td>upstream limiter</td><td>429</td><td>Caller backs off</td></tr><tr><td>Tier 1 override row with <code>billable=null</code></td><td>NOT NULL constraint</td><td>rejected at insert</td><td>None</td></tr><tr><td>Audit row dedup (same time_entry_id twice)</td><td>none enforced</td><td>duplicates possible; downstream dedups</td><td>None</td></tr><tr><td>Concurrent override edit + resolve</td><td>resolver reads snapshot at time</td><td>both consistent</td><td>None</td></tr></tbody></table>\n<hr>\n<h2>§11 — Implementation notes</h2>\n<ul><li><code>BillableResolution.tier_consulted</code> is the matching tier (1..=4), not the depth searched. Both interpretations are useful; the field name reflects \"which tier produced the value.\"</li><li>The lookup query order matches §1: Tier 1 → 2 → 3 → 4. Each is a separate SQL — could be one big LEFT JOIN but readability wins for the 4-table case.</li><li>The fallback case is its own enum variant rather than a sentinel value — callers pattern-match cleanly.</li><li>Snapshot field lives on TASK-TIME-005's time entry row; this task's responsibility ends at returning <code>BillableResolution</code>.</li><li>The <code>proj.billable_resolved</code> audit row's <code>time_entry_id</code> is the downstream entry being created; if resolver is called for PREVIEW (without writing), <code>time_entry_id</code> is null.</li><li>Metrics cardinality bounded: 2 (value) × 4 (source) = 8 label combinations — safe.</li><li>Input validation at the handler boundary is per task-audit skill §3.4 rule 13 (RLS WITH CHECK + handler validation). Defence in depth.</li><li>Bulk-resolve cap of 1000 chosen against: (a) Postgres prepared-statement parameter limit ≈ 65k; bulk of 1000 × 4 params = 4000 well under; (b) operator workflow envelopes (CSV imports typically &lt; 500 rows).</li><li>The \"force re-resolution\" admin endpoint for §1 #14 is intentionally hidden in admin namespace because: re-resolution invalidates prior bill amounts; operator must explicitly choose.</li><li>Engagement-default task-class billable is an array of (task_class, billable) pairs in tenant settings; engagement creation iterates and inserts.</li><li>20ms p95 budget breaks down: ~5ms per query × 4 queries worst-case + ~0ms response build = 20ms. Tier-1 hit is ~5ms total (single query).</li><li>Historical query uses the memory_outbox partial index <code>WHERE kind='proj.billable_resolved'</code> for fast retrieval.</li><li><code>effective_overrides_applied</code> adds ~3 SQL queries (one per non-matching tier); we run them in parallel via <code>tokio::join!</code> to keep latency bounded.</li><li>The per-tenant fallback override defaults to <code>false</code> (conservative); only internal-billing tenants flip it.</li><li>Archived-engagement check is at the top of resolver (before any tier query) so we fail fast on the common error.</li><li>Explain mode is a UI affordance, not a contract guarantee — the structured response shape may evolve as new tiers are added (slice 4+).</li><li>The <code>billable_snapshot</code> field on time entry is the bill-truth; cascading downstream (TASK-PROJ-007 billing modes) trusts it without re-resolving.</li><li>We considered caching cascade resolutions but rejected: cache invalidation on override changes is complex, and the resolver is fast (~20ms p95). Cache adds complexity without justified gain.</li><li>The cascade order (Member → TaskClass → RoleDefault → Fallback) was chosen because: Member is the most specific (per-person decisions), TaskClass is mid (per-work-type), Role is general (per-job-function), Fallback is universal.</li><li>We rejected a 5th tier (per-member-per-task-class) because the 4 tiers cover ~99% of empirical cases; a 5th tier adds combinatorial complexity for the rare case.</li></ul>\n<hr>\n<p><em>End of TASK-PROJ-006.</em></p>";
