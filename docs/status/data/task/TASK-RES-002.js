window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-RES-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The RES service + portal-web frontend <strong>MUST</strong> ship allocation Gantt at <code>services/res/src/allocation/</code> with drag-rebalance + optimistic concurrency + pre-commit validation, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>allocation_change_status</code> against closed enum per DEC-2041.</li></ol>\n<ol><li><strong>MUST</strong> propose changes at <code>proposer.rs::propose(member_id, week, changes)</code> per DEC-2040 — changes array of <code>{project_id, delta_hours}</code>.</li></ol>\n<ol><li><strong>MUST</strong> validate at <code>validator.rs::validate(proposal)</code> per DEC-2043:</li></ol>\n<ul><li>Total hours stay within member capacity (TASK-RES-001)</li><li>TASK-RES-005 OT cap check</li><li>TASK-RES-003 thresholds (warn-not-block on 110%)</li><li>On violation: return rejection with reason.</li></ul>\n<ol><li><strong>MUST</strong> commit at <code>commit_handler.rs::commit(proposal, version)</code> per DEC-2044:</li></ol>\n<ul><li>Verify matrix row version matches (optimistic concurrency per DEC-2042)</li><li>Insert allocation_changes row (history)</li><li>UPDATE matrix row + increment version</li><li>Transaction: all or nothing</li></ul>\n<ol><li><strong>MUST</strong> define table at migration <code>0002</code>: ```sql ALTER TABLE res_capacity_matrix ADD COLUMN version INT NOT NULL DEFAULT 1; GRANT UPDATE (allocated_hours, version) ON res_capacity_matrix TO cyberos_app;</li></ol>\n<p>   CREATE TABLE res_allocation_changes (      change_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      member_id UUID NOT NULL,      iso_week CHAR(8) NOT NULL,      project_id UUID NOT NULL,      old_allocated_hours NUMERIC(5,2) NOT NULL,      new_allocated_hours NUMERIC(5,2) NOT NULL,      status TEXT NOT NULL        CHECK (status IN ('proposed','validated','committed','rejected','conflicted')),      rejection_reason TEXT,      proposed_by UUID NOT NULL,      committed_at TIMESTAMPTZ,      trace_id CHAR(32),      created_at TIMESTAMPTZ NOT NULL DEFAULT now()    );    CREATE INDEX changes_member_week_idx ON res_allocation_changes(tenant_id, member_id, iso_week, created_at DESC);    ALTER TABLE res_allocation_changes ENABLE ROW LEVEL SECURITY;    CREATE POLICY changes_rls ON res_allocation_changes      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON res_allocation_changes FROM cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/res/allocations/propose   body: {member_id, iso_week, changes: [...], expected_version} POST /v1/res/allocations/{id}/commit GET  /v1/res/allocations/changes?member_id=...&amp;iso_week=... </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2045. PII per TASK-MEMORY-111: hours SHA-256 hashed.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from UI action → propose → validate → commit → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> bypass validation per DEC-2043.</li></ol>\n<ol><li><strong>MUST NOT</strong> mutate prior change row per DEC-2044 (append-only).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why drag UI (DEC-2040)?</strong> Spreadsheet-style allocation is tedious; visual drag matches mental model.</p>\n<p><strong>Why optimistic concurrency (DEC-2042)?</strong> Multiple CHROs editing concurrently; version check prevents last-write-wins overwrite.</p>\n<p><strong>Why pre-commit validation (DEC-2043)?</strong> Better UX to surface OT cap violation at propose-time than after save.</p>\n<p><strong>Why append-only changes (DEC-2044)?</strong> Audit trail — \"who changed what when\" must be preserved.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample propose:</p>\n<pre><code class=\"language-json\">POST /v1/res/allocations/propose\n{\n  \"member_id\": \"uuid\",\n  \"iso_week\": \"2026-W20\",\n  \"expected_version\": 3,\n  \"changes\": [\n    {\"project_id\": \"uuid-a\", \"delta_hours\": -5},\n    {\"project_id\": \"uuid-b\", \"delta_hours\": +5}\n  ]\n}</code></pre>\n<p>Response (validated):</p>\n<pre><code class=\"language-json\">{\n  \"proposal_id\": \"uuid\",\n  \"status\": \"validated\",\n  \"warnings\": [{\"kind\": \"over_threshold\", \"current_pct\": 105}]\n}</code></pre>\n<p>Response (rejected):</p>\n<pre><code class=\"language-json\">{\n  \"status\": \"rejected\",\n  \"rejection_reason\": \"OT cap exceeded: weekly OT would be 14h, max 12h per Decree 145\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>allocation_change_status enum cardinality 5</strong>. 2. <strong>Drag UI updates state</strong>. 3. <strong>Pre-commit validation enforced</strong>. 4. <strong>OT cap rejection (TASK-RES-005)</strong>. 5. <strong>Over-threshold warning (TASK-RES-003 110%)</strong>. 6. <strong>Optimistic concurrency via version check</strong>. 7. <strong>Version mismatch → status=conflicted</strong>. 8. <strong>Transactional commit (all-or-nothing)</strong>. 9. <strong>4 memory audit kinds emitted</strong>. 10. <strong>PII scrubbed (hours SHA256)</strong>. 11. <strong>RLS denies cross-tenant</strong>. 12. <strong>CHRO/PM role only</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Append-only via REVOKE</strong>. 15. <strong>rust_decimal precision</strong>. 16. <strong>History query desc time</strong>. 17. <strong>Drag UI disables locked rows</strong>. 18. <strong>Commit dialog confirms changes</strong>. 19. <strong>WebSocket update broadcasts to other CHRO clients</strong>. 20. <strong>Undo via new compensating change</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn optimistic_concurrency_rejects_stale() {\n    let ctx = TestContext::with_matrix_v1().await;\n    let p1 = ctx.propose(ctx.member_id, this_week(), changes(), 1).await;\n    ctx.commit(p1.id).await;  // version → 2\n    let p2 = ctx.propose(ctx.member_id, this_week(), changes(), 1).await;\n    let r = ctx.try_commit(p2.id).await;\n    assert!(r.status == \"conflicted\");\n}\n\n#[tokio::test]\nasync fn ot_cap_rejection() {\n    let ctx = TestContext::member_at_60h_already().await;\n    let r = ctx.propose(ctx.member_id, this_week(), add_5h_more(), 1).await;\n    assert!(r.status == \"rejected\");\n    assert!(r.rejection_reason.contains(\"OT cap\"));\n}\n\n#[tokio::test]\nasync fn commit_atomic() {\n    let ctx = TestContext::with_matrix().await;\n    let p = ctx.propose(...).await;\n    ctx.simulate_db_failure_mid_commit().await;\n    let row = ctx.fetch_matrix_row(ctx.member_id, week).await;\n    assert_eq!(row.version, 1);  // unchanged\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-RES-001. <strong>Cross-module:</strong> TASK-RES-003 (threshold flags), TASK-RES-005 (OT cap), TASK-PROJ-001 (project context), TASK-AUTH-101 (CHRO/PM role), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Version mismatch</td><td>check</td><td>conflicted; UI refresh</td><td>retry</td></tr><tr><td>OT cap violation</td><td>validator</td><td>rejected</td><td>adjust</td></tr><tr><td>Negative allocation</td><td>validate</td><td>400</td><td>fix</td></tr><tr><td>DB transaction fail</td><td>rollback</td><td>sev-2; status=rejected</td><td>retry</td></tr><tr><td>Cross-tenant write</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>Decimal precision</td><td>rust_decimal</td><td>inherent</td><td>inherent</td></tr><tr><td>WebSocket disconnect</td><td>reconnect</td><td>stale view + sev-3</td><td>refresh</td></tr><tr><td>Drag past capacity bounds</td><td>UI clamp</td><td>inherent</td><td>inherent</td></tr><tr><td>Mass-allocation request (&gt;50 changes)</td><td>batch limit</td><td>400</td><td>split</td></tr><tr><td>Concurrent same-cell propose</td><td>UNIQUE on commit</td><td>first wins</td><td>retry</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Gantt UI built with d3 or recharts; drag handler emits debounced state.</li><li>§11.2 Version increment via <code>UPDATE ... SET version = version + 1 WHERE version = $expected RETURNING version</code>.</li><li>§11.3 Validation parallelizes OT + threshold checks for sub-100ms response.</li><li>§11.4 memory audit body: member_id, week, project_id, old/new hours SHA256.</li><li>§11.5 WebSocket broadcasts on commit; other UIs invalidate cache.</li></ul>\n<hr>\n<p><em>End of TASK-RES-002 spec.</em></p>";
