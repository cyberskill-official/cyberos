window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-ESOP-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The ESOP service <strong>MUST</strong> ship M&amp;A acceleration at <code>services/esop/src/ma/</code> with Board declaration + acceleration cron + 5-business-day member notice, 5 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>ma_event_status</code> against closed enum per DEC-2301.</li></ol>\n<ol><li><strong>MUST</strong> require board threshold per DEC-2302 (reuse TASK-ESOP-003 board threshold logic).</li></ol>\n<ol><li><strong>MUST</strong> run acceleration at <code>acceleration_runner.rs::accelerate(ma_event)</code> per DEC-2303:</li></ol>\n<ul><li>For all active grants: set vested = total_shares</li><li>Insert special accrual row (kind='ma_acceleration')</li><li>Update grant.status = 'accelerated'</li></ul>\n<ol><li><strong>MUST</strong> notify members within 5 business days at <code>member_notifier.rs::notify(ma_event)</code> per DEC-2300:</li></ol>\n<ul><li>For each affected member: send email via TASK-EMAIL-009</li><li>Track per-member notification_status</li></ul>\n<ol><li><strong>MUST</strong> define tables at migration <code>0006</code>: ```sql CREATE TABLE esop_ma_events ( ma_event_id UUID PRIMARY KEY, tenant_id UUID NOT NULL, event_description TEXT NOT NULL, declared_by UUID NOT NULL, declared_at TIMESTAMPTZ NOT NULL DEFAULT now(), status TEXT NOT NULL DEFAULT 'declared' CHECK (status IN ('declared','accelerating','members_notified','completed','dismissed')), acceleration_started_at TIMESTAMPTZ, all_notified_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, trace_id CHAR(32) ); ALTER TABLE esop_ma_events ENABLE ROW LEVEL SECURITY; CREATE POLICY ma_rls ON esop_ma_events USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); REVOKE UPDATE, DELETE ON esop_ma_events FROM cyberos_app; GRANT UPDATE (status, acceleration_started_at, all_notified_at, completed_at) ON esop_ma_events TO cyberos_app;</li></ol>\n<p>   CREATE TABLE esop_ma_board_signs (      sign_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      ma_event_id UUID NOT NULL REFERENCES esop_ma_events(ma_event_id),      board_member_id UUID NOT NULL,      signed_at TIMESTAMPTZ NOT NULL DEFAULT now(),      UNIQUE (ma_event_id, board_member_id)    );    ALTER TABLE esop_ma_board_signs ENABLE ROW LEVEL SECURITY;    CREATE POLICY ma_signs_rls ON esop_ma_board_signs      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON esop_ma_board_signs FROM cyberos_app;</p>\n<p>   CREATE TABLE esop_ma_member_notices (      notice_id UUID PRIMARY KEY,      tenant_id UUID NOT NULL,      ma_event_id UUID NOT NULL REFERENCES esop_ma_events(ma_event_id),      member_id UUID NOT NULL,      status TEXT NOT NULL DEFAULT 'pending'        CHECK (status IN ('pending','sent','failed')),      sent_at TIMESTAMPTZ,      email_message_id UUID,      UNIQUE (ma_event_id, member_id)    );    ALTER TABLE esop_ma_member_notices ENABLE ROW LEVEL SECURITY;    CREATE POLICY notices_rls ON esop_ma_member_notices      USING (tenant_id = current_setting('auth.tenant_id')::uuid)      WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid);    REVOKE UPDATE, DELETE ON esop_ma_member_notices FROM cyberos_app;    GRANT UPDATE (status, sent_at, email_message_id) ON esop_ma_member_notices TO cyberos_app;    ```</p>\n<ol><li><strong>MUST</strong> expose endpoints: ``<code>text POST /v1/esop/ma-events                       (CEO declares) POST /v1/esop/ma-events/{id}/board-sign       (board member) POST /v1/esop/ma-events/{id}/accelerate       (auto on threshold) GET  /v1/esop/ma-events/{id}                  (status) </code>``</li></ol>\n<ol><li><strong>MUST</strong> emit 5 memory audit kinds per DEC-2304. PII per TASK-MEMORY-111: descriptions SHA256.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from declare → sign → accelerate → notify → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> accelerate without board threshold per DEC-2302.</li></ol>\n<ol><li><strong>MUST NOT</strong> skip member notice per DEC-2300 (5-business-day deadline tracked).</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why board threshold (DEC-2302)?</strong> M&amp;A acceleration = major equity event; requires governance.</p>\n<p><strong>Why 5-business-day notice (DEC-2300)?</strong> Industry standard for change-of-control disclosure; allows members to plan.</p>\n<p><strong>Why per-member notice tracking (DEC-2300)?</strong> Audit trail proves notification delivered.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample M&amp;A event:</p>\n<pre><code class=\"language-json\">POST /v1/esop/ma-events\n{\n  \"event_description\": \"Acquisition by AcmeCorp, closing 2026-Q3\"\n}</code></pre>\n<p>Sample status:</p>\n<pre><code class=\"language-json\">{\n  \"ma_event_id\": \"uuid\",\n  \"status\": \"completed\",\n  \"board_signs_count\": 3,\n  \"members_notified_count\": 30,\n  \"completed_at\": \"2026-06-01T10:00:00Z\"\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>ma_event_status enum cardinality 5</strong>. 2. <strong>Board threshold (≥3 default)</strong>. 3. <strong>All active grants accelerated</strong>. 4. <strong>vested = total_shares set</strong>. 5. <strong>5-business-day notice tracked</strong>. 6. <strong>Per-member notice via TASK-EMAIL-009</strong>. 7. <strong>5 memory audit kinds emitted</strong>. 8. <strong>PII scrubbed (desc SHA256)</strong>. 9. <strong>RLS denies cross-tenant</strong>. 10. <strong>CEO-only declare</strong>. 11. <strong>Board member sign-only</strong>. 12. <strong>Trace_id preserved</strong>. 13. <strong>Append-only via REVOKE except status cols</strong>. 14. <strong>UNIQUE on (ma_event_id, board_member_id)</strong>. 15. <strong>UNIQUE on (ma_event_id, member_id) for notices</strong>. 16. <strong>status workflow enforced</strong>. 17. <strong>Per-member notice failure isolated</strong>. 18. <strong>Cancelled grants excluded</strong>. 19. <strong>bigint shares</strong>. 20. <strong>Dismiss allowed pre-acceleration</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn board_threshold_required() {\n    let ctx = TestContext::with_declared_ma().await;\n    ctx.board_sign(ctx.b1).await;\n    ctx.board_sign(ctx.b2).await;\n    let m = ctx.fetch_ma(ctx.ma_id).await;\n    assert_eq!(m.status, \"declared\");  // still\n    ctx.board_sign(ctx.b3).await;\n    let m2 = ctx.fetch_ma(ctx.ma_id).await;\n    assert_eq!(m2.status, \"accelerating\");\n}\n\n#[tokio::test]\nasync fn all_grants_accelerated() {\n    let ctx = TestContext::with_5_active_grants_and_ma_signed().await;\n    ctx.run_acceleration(ctx.ma_id).await;\n    for g_id in ctx.grant_ids() {\n        let g = ctx.fetch_grant(g_id).await;\n        assert_eq!(g.status, \"accelerated\");\n        let a = ctx.fetch_latest_accrual(g_id).await;\n        assert_eq!(a.vested_cumulative, g.total_shares);\n    }\n}\n\n#[tokio::test]\nasync fn members_notified_in_5_bd() {\n    let ctx = TestContext::with_accelerated_ma().await;\n    ctx.run_notifier(ctx.ma_id).await;\n    let notices = ctx.fetch_notices(ctx.ma_id).await;\n    let sent = notices.iter().filter(|n| n.status == \"sent\").count();\n    assert_eq!(sent, ctx.member_count());\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-ESOP-001. <strong>Cross-module:</strong> TASK-ESOP-002 (accrual rows), TASK-EMAIL-009 (notice), TASK-MCP-007 (deadline cron), TASK-AUTH-101 (CEO + board), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Board threshold not reached</td><td>inherent</td><td>stays declared</td><td>get signs</td></tr><tr><td>Acceleration mid-run crash</td><td>resume</td><td>partial</td><td>retry</td></tr><tr><td>Notice send fail per-member</td><td>per-row</td><td>isolate; retry</td><td>inherent</td></tr><tr><td>5-day deadline missed</td><td>cron alert</td><td>sev-1 + escalate</td><td>manual notify</td></tr><tr><td>Cancelled grant in active list</td><td>filter</td><td>skip</td><td>inherent</td></tr><tr><td>Cross-tenant declare</td><td>RLS</td><td>403</td><td>inherent</td></tr><tr><td>Duplicate sign</td><td>UNIQUE</td><td>second skip</td><td>inherent</td></tr><tr><td>Member has no email</td><td>sev-2</td><td>skip + manual notify</td><td>data fix</td></tr><tr><td>Concurrent acceleration</td><td>UPDATE WHERE</td><td>first wins</td><td>inherent</td></tr><tr><td>Dismiss post-acceleration</td><td>reject</td><td>409</td><td>inherent</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Cron via TASK-MCP-007 for 5-business-day countdown; sev-1 alert if deadline approaches with pending notices.</li><li>§11.2 Acceleration creates TASK-ESOP-002 accrual row with kind='ma_acceleration'.</li><li>§11.3 memory audit body: ma_event_id, status, counts; description SHA256.</li><li>§11.4 Per-member notice via TASK-EMAIL-009; email template includes vested shares + next steps.</li><li>§11.5 Business-day calc: VN public holidays from TASK-HR-005 policy.</li></ul>\n<hr>\n<p><em>End of TASK-ESOP-006 spec.</em></p>";
