window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DOCS-004"]="<h1>TASK-DOCS-004: Folder-per-task layout</h1>\n<h2>§1 - Description</h2>\n<p>Every task becomes a folder that can carry its own media, and the corpus becomes fully machine-readable while we are touching every file anyway.</p>\n<p>Normative clauses:</p>\n<ol><li>Layout MUST become <code>docs/tasks/&lt;module&gt;/&lt;STEM&gt;/spec.md</code> + <code>&lt;STEM&gt;/audit.md</code> (when an audit exists) where STEM is the current file stem (e.g. <code>TASK-AUTH-102-totp-webauthn-mfa</code>). An <code>assets/</code> subfolder is created ON DEMAND (first asset), not pre-created empty. <code>.workflow/</code> and <code>_audits/</code> trees are untouched.</li><li>A one-shot script <code>scripts/migrate_task_layout.py</code> MUST perform the move for every task + sibling audit via <code>git mv</code> semantics (history-preserving), idempotent (re-run = no-op), and MUST print a per-module summary. Legacy flat files MUST NOT remain.</li><li>Every tool that globs the old layout MUST be updated in the same change: <code>migrate_improvement_to_task.py</code> (regen + status flips), <code>render-roadmap.mjs</code>, <code>check_doc_anchors.sh</code> scan roots, the plugin command doc's path examples, and any test fixture that builds task trees. Discovery grammar becomes <code>&lt;module&gt;/*/spec.md</code> with <code>id</code> from frontmatter (stem from the folder name).</li><li><code>read_fm</code> MUST become loud: files whose frontmatter fails strict YAML are listed on stderr with the parse error (one line each) and counted in the regen summary; the silent <code>continue</code> is removed. The regen totals line MUST equal the roadmap's frontmatter-derived totals over the same corpus.</li><li>The 42 currently yaml-invalid task files MUST be repaired (minimal edits: quoting/structure only, no semantic changes) so the whole corpus parses strict-YAML; the repair list ships in the migration commit message or an appendix file.</li><li>Relative references INSIDE moved files (e.g. <code>../improvement/TASK-IMP-068...</code>) MUST keep resolving: the migrator rewrites one directory level (<code>../&lt;mod&gt;/&lt;file&gt;.md</code> -&gt; <code>../../&lt;mod&gt;/&lt;STEM&gt;/spec.md</code>) for repo-relative and sibling citations it can resolve; unresolvable relatives are listed for manual review, and <code>check_doc_anchors.sh</code> (extended to scan <code>docs/tasks/**/spec.md</code>) MUST exit 0 after migration.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Folder-per-task is the only layout that gives each spec a private asset namespace without a global assets dump. Doing the yaml repair in the same pass means the corpus goes from 91% to 100% machine-readable exactly once, and the backlog/roadmap split-brain (444 vs 486) dies at the root.</p>\n<h2>§3 - Contract</h2>\n<p>Discovery: <code>docs/tasks/&lt;module&gt;/&lt;STEM&gt;/spec.md</code>; audit sibling <code>audit.md</code>; assets under <code>&lt;STEM&gt;/assets/**</code>, referenced relatively from spec.md (<code>assets/&lt;file&gt;</code>).</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Complete move</strong> (§1 #1, #2) - after migration: zero <code>&lt;module&gt;/task-*.md</code> flat files; N folders == N pre-move tasks; audits paired; git log --follow works on a sampled spec.</li><li><strong>Idempotent</strong> (§1 #2) - second run exits 0 reporting nothing to do.</li><li><strong>Tooling green on new layout</strong> (§1 #3) - regen, roadmap, doc-anchors, and all repo suites pass post-move.</li><li><strong>Loud regen + reconciled totals</strong> (§1 #4, #5) - zero skipped files; BACKLOG totals == roadmap totals == 486 (или the true count at migration time).</li><li><strong>Repairs are minimal</strong> (§1 #5) - the yaml diff on the 42 files touches only frontmatter formatting (no value semantics changed - review-verified).</li><li><strong>Internal references resolve</strong> (§1 #6) - check_doc_anchors extended to the task tree exits 0.</li></ol>\n<h2>§5 - Verification</h2>\n<p><code>scripts/tests/test_task_layout.sh</code>: t01_no_flat_files, t02_folder_count_matches, t03_idempotent_rerun, t04_regen_loud_and_reconciled, t05_repairs_minimal (diff-scope), t06_anchors_green. (AC 1-6.)</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Migrator: walk modules, <code>git mv</code> file-&gt;folder/spec.md + audit-&gt;audit.md, rewrite one-level relatives, emit summary. Regen: glob change + loud read_fm. Roadmap: walk change (folder stem = id source fallback). Doc-anchors: add task tree root + spec.md grammar.</p>\n<h2>§7 - Dependencies</h2>\n<p>None upstream (foundation of Wave D). Blocks TASK-DOCS-005 (renderer walks folders) and TASK-SKILL-120 (skills scaffold folders).</p>\n<h2>§8 - Example payloads</h2>\n<p><code>docs/tasks/auth/TASK-AUTH-102-totp-webauthn-mfa/spec.md</code> + <code>audit.md</code>; regen stderr: <code>read_fm: 0 unparseable (was 42)</code>.</p>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Whether <code>.workflow/</code> bundles move INTO task folders is deliberately out of scope (session artefacts vs spec artefacts).</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Half-completed move (crash mid-run) - migrator is per-file atomic (mv then next) and idempotent; re-run completes the remainder.</li><li>Tool missed by #3 - the full-suite gate in AC 3 is the net; grep inventory for <code>task-*.md</code> globs ships in the migration notes.</li><li>Sandbox/CI path assumptions in tests - fixtures build the NEW layout only; legacy fixture helpers deleted same commit.</li><li>yaml repair changes meaning - AC 5 restricts to formatting; reviewer diff-scope check.</li><li>External links into old paths (workflow bundles, memory rows) - historical artefacts keep old paths as history; only LIVE contracts are swept (same doctrine as TASK-SKILL-119).</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Run the migrator FIRST in its commit, tooling updates in the same commit, so no commit exists where tools and layout disagree.</p>\n<p><em>End of TASK-DOCS-004.</em></p>";
