window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CUO-203"]="<h2>Summary</h2>\n<p>Wave 4 of the continuous-improvement harness extends TASK-CUO-200..202 from skill-level to workflow-level evolution. The harness aggregates per-workflow outcome distributions (COMPLETED / ROUTED_BACK / HITL_HALT / FAILED) over rolling windows, and when a workflow's ROUTED_BACK rate exceeds a threshold OR the same phase keeps tripping repeat failures across multiple tasks, it emits a <code>workflow_refinement_proposal@1</code> proposing chain edits — e.g. \"add a pre-review check before phase 8\", \"tighten the coverage-gate threshold from 90% to 95% per file\", \"swap the order of steps 11 and 12 so observability lands before impl-plan\".</p>\n<p>The stripe-dedup + auto-bump machinery from TASK-CUO-201/202 reuses cleanly — workflow stripes are just <code>&lt;workflow_id&gt;:&lt;phase&gt;:&lt;pattern_hash&gt;</code> and the applier extends to mutate workflow YAML frontmatter + skill_chain steps with the same major/minor/safety classification.</p>\n<h2>Problem</h2>\n<p>Skill-level evolution (TASK-CUO-201/202) catches \"skill X is failing too often\". But sometimes the root cause is the WORKFLOW shape — e.g. ship-tasks' <code>coverage-gate-audit</code> keeps tripping on the same module because <code>implementation-plan-audit</code> doesn't check coverage planning early enough. Fixing the skill itself doesn't help; the chain needs a pre-coverage check inserted at step 10.</p>\n<p>Without workflow-level evolution, structural issues that span multiple skills go unnoticed until Stephen manually correlates the per-skill reports and decides to redesign the chain. The harness should propose chain edits the same way it proposes skill edits.</p>\n<h2>Proposed Solution</h2>\n<h3>§1 Normative requirements</h3>\n<ol><li><strong>MUST</strong> ship <code>cuo/core/workflow_evolution.py</code> with <code>compute_workflow_metrics(audit_dir, window) -&gt; dict[str, WorkflowMetrics]</code> that aggregates per-workflow: total runs, COMPLETED count, ROUTED_BACK count (sum + per-task median), HITL_HALT count, average step count of failed runs, top-3 steps where failure occurred most often.</li><li><strong>MUST</strong> define workflow-level threshold signals (mirroring SKILL-level <code>self_audit.anomaly_signals</code>):</li></ol>\n<ul><li><code>routed_back_rate_above</code> (default 0.3 → 30% of runs route back)</li><li><code>hitl_halt_rate_above</code> (default 0.1 → 10% of runs halt for human)</li><li><code>repeat_phase_failure_above</code> (default 3 → same phase fails on 3+ different tasks in window)</li><li><code>chain_length_efficiency_below</code> (default 0.7 → average steps_run / chain_length below 70%)</li></ul>\n<ol><li><strong>MUST</strong> declare these signals in each workflow file's <code>self_audit:</code> frontmatter (similar to SKILL.md's). Workflow YAML extension: add the same <code>self_audit</code> + <code>human_fine_tune</code> blocks the SKILL.md frontmatter already has. (Schema change in <code>cuo/contracts/workflow/CONTRACT.md</code>.) <em>(traces_to: §1 #3 → AC #2)</em></li><li><strong>MUST</strong> compute workflow stripes via <code>compute_workflow_stripe(workflow_id, signal_id, failure_pattern) -&gt; str</code> with format <code>&lt;persona&gt;/&lt;workflow_slug&gt;:&lt;signal_id&gt;:&lt;pattern_hash&gt;</code>.</li><li><strong>MUST</strong> route workflow-refinement proposals to the SAME <code>docs/proposals/open/</code> directory as skill proposals (TASK-CUO-201). They are distinguishable by <code>frontmatter.kind: workflow_refinement</code> vs <code>skill_refinement</code>.</li><li><strong>MUST</strong> extend TASK-CUO-202's proposal classifier to handle workflow diffs:</li></ol>\n<ul><li><code>step_addition</code> (insert a new step in <code>skill_chain:</code>) — minor bump (workflow_version), queue.</li><li><code>step_removal</code> — major bump, queue.</li><li><code>step_reorder</code> — major bump, queue.</li><li><code>escalates_to_addition</code> — minor bump, queue.</li><li><code>pattern_change</code> (e.g. <code>linear</code> → <code>per_instance</code>) — major bump, queue.</li><li><code>condition_tune</code> (numeric threshold in a <code>condition:</code> clause) — minor bump, queue.</li><li>All workflow diffs default to QUEUE — workflows are higher-stakes than individual skills. Auto-apply requires explicit <code>--auto-workflow-diffs</code> flag.</li></ul>\n<ol><li><strong>MUST</strong> emit <code>cuo.workflow_refinement_emitted</code> memory aux row per new workflow proposal; <code>cuo.workflow_refinement_applied</code> per apply.</li><li><strong>MUST</strong> add CLI:</li></ol>\n<ul><li><code>cyberos-cuo workflow metrics --since 30d</code> — table of per-workflow outcome distribution.</li><li><code>cyberos-cuo workflow propose --workflow &lt;id&gt;</code> — manually trigger proposal authoring against one workflow.</li></ul>\n<ol><li><strong>SHOULD</strong> include per-task routed-back history in the workflow report — if task-X has routed back 3+ times in the window, the proposal body cites it as the canonical case study.</li><li><strong>MUST</strong> include in proposal body: <code>## Before</code> (current chain snippet), <code>## After</code> (proposed chain snippet), <code>## Rationale</code> (which signal tripped + evidence rows), <code>## Backward-compat notes</code> (whether the change breaks in-flight tasks at older chain positions).</li></ol>\n<h3>§2 Stripe collision with skill stripes</h3>\n<p>Skill stripes from TASK-CUO-201 have format <code>&lt;skill_slug&gt;:&lt;signal_id&gt;:&lt;hash&gt;</code>; workflow stripes have format <code>&lt;persona&gt;/&lt;workflow_slug&gt;:&lt;signal_id&gt;:&lt;hash&gt;</code>. The presence of <code>/</code> in workflow stripes makes them disjoint from skill stripes — no collision possible. <code>cyberos-cuo proposal list</code> SHOULD group by kind (skill vs workflow) for readability.</p>\n<h2>Alternatives Considered</h2>\n<ol><li><strong>Workflow proposals as a separate folder</strong> (<code>docs/workflow-proposals/</code>) — splits the operator surface area unnecessarily. Unified <code>docs/proposals/</code> with <code>kind:</code> discrimination is simpler.</li><li><strong>Only humans propose workflow edits</strong> — but workflow drift is exactly the kind of thing the harness is best at noticing across many tasks; humans see one task at a time.</li><li><strong>Tie workflow evolution to skill evolution (apply skill diff cascades into workflow diff)</strong> — too coupled; the propagation rules become fragile. Keep them independent.</li></ol>\n<h2>Success Metrics</h2>\n<table><thead><tr><th>metric</th><th>baseline</th><th>target</th><th>deadline</th></tr></thead><tbody><tr><td>Workflow refinement proposals produced per quarter</td><td>n/a</td><td>2–5 per workflow that ships &gt; 50 tasks</td><td>30 days post-ship</td></tr><tr><td>% of workflow proposals approved by Stephen</td><td>n/a</td><td>≥ 50%</td><td>continuous</td></tr><tr><td>ROUTED_BACK rate improvement for workflows with applied proposals</td><td>n/a</td><td>-25% in the following window</td><td>per-proposal</td></tr></tbody></table>\n<h2>Scope</h2>\n<p>In scope: per-workflow metrics aggregator, threshold-signal evaluator (reusing TASK-CUO-200's signal-function pattern), workflow stripe computer, proposal authoring (LLM with workflow self-reflection prompt), classifier extension, CLI subcommands, audit row emission.</p>\n<h3>Out of scope</h3>\n<ul><li>Multi-workflow refactors (single proposal touches &gt; 1 workflow)</li><li>Persona-level evolution (e.g. proposing to spawn a new persona)</li><li>Auto-runs of proposed chain edits on the CURRENT in-flight tasks (those use the chain as it was at start of their run; new chains apply to future invocations)</li></ul>\n<h2>Dependencies</h2>\n<ul><li><strong>TASK-CUO-200</strong> — read-only harness foundation</li><li><strong>TASK-CUO-201</strong> — proposal emitter + stripe dedup (reused for workflow stripes)</li><li><strong>TASK-CUO-202</strong> — proposal applier (extended in §1 #6 for workflow diff buckets)</li></ul>\n<h2>AI Risk Assessment</h2>\n<h3>Data Sources</h3>\n<p>The workflow-evolution harness reads three trusted sources: the memory audit chain (operator-controlled append-only log), workflow YAML frontmatter (operator-authored), and skill SKILL.md files (operator-authored, audit-gated). No external network calls. Proposal authoring runs an LLM with the failing workflow's own definition + the failing audit rows as context — no untrusted user input.</p>\n<h3>Human Oversight</h3>\n<p>ALL workflow-level diffs default to queue (§1 #6 says \"All workflow diffs default to QUEUE — workflows are higher-stakes than individual skills\"). Auto-apply requires an explicit <code>--auto-workflow-diffs</code> flag on <code>cyberos-cuo proposal apply</code>, AND the proposal bucket must still classify as <code>condition_tune</code> or below. Pattern changes (e.g. <code>linear → time_critical</code>) are MAJOR + always queue regardless of flags. Stripe-repeat-halt (inherited from TASK-CUO-201) means the harness halts immediately rather than proposing a second workflow edit for the same root cause.</p>\n<h3>Failure Modes</h3>\n<p>(a) <strong>Mid-flight task uses old chain</strong> — when a chain edit applies during an in-flight task run, the task continues using the chain it started with (snapshot at run-start). Risk: the operator expects the new chain to fix the in-flight task. Mitigation: §2 explicitly out-of-scope; only future invocations use the new chain. (b) <strong>Workflow stripe collision</strong> — workflow stripes use <code>&lt;persona&gt;/&lt;workflow_slug&gt;</code> format; the <code>/</code> makes them disjoint from skill stripes — verifiable via <code>compute_workflow_stripe</code> test (AC #5). (c) <strong>Proposed step references a non-existent skill</strong> — the validator (existing <code>validate_chain</code>) catches this BEFORE the applier writes; the proposal is rejected as ill-formed. (d) <strong>Backward-compat regression</strong> — the proposal body's <code>## Backward-compat notes</code> section (§1 #10) is mandatory; if the LLM omits it, the proposal fails its own structural validation.</p>\n<h2>AI Authorship Disclosure</h2>\n<ul><li><strong>Tools used:</strong> Anthropic Claude.</li><li><strong>Scope:</strong> §1 normative clauses (10 items), §2 stripe collision discussion, §4 ACs (10 items), §5 named tests, alternatives, AI Risk Assessment.</li><li><strong>Human review:</strong> Stephen Cheng reviewed. The \"all workflow diffs default to queue\" rule (§1 #6) is operator-mandated (workflows are higher-stakes than skills).</li></ul>\n<h2>§4 Acceptance Criteria</h2>\n<ol><li><code>cyberos-cuo workflow metrics --since 30d</code> outputs per-workflow rows: total_runs, completed, routed_back, hitl_halt, failed, average chain length used. <em>(traces_to: §1 #1, #8)</em></li><li>A workflow with 5 runs all COMPLETED has zero tripped signals. <em>(traces_to: §1 #2)</em></li><li>A workflow with 4 ROUTED_BACK out of 10 runs trips <code>routed_back_rate_above: 0.3</code> and emits one <code>workflow_refinement_proposal@1</code> to <code>docs/proposals/open/</code>. <em>(traces_to: §1 #2, #5, #7)</em></li><li>The emitted proposal's body has all 4 mandatory sections: Before / After / Rationale / Backward-compat. <em>(traces_to: §1 #10)</em></li><li>Workflow stripe format MUST be <code>&lt;persona&gt;/&lt;workflow_slug&gt;:&lt;signal_id&gt;:&lt;8 hex&gt;</code> — verifiable via regex. <em>(traces_to: §1 #4)</em></li><li>The classifier (extension of TASK-CUO-202) returns <code>step_addition</code> → <code>minor</code> + queue for a proposal that inserts a new step in the <code>skill_chain:</code> block. <em>(traces_to: §1 #6)</em></li><li>Repeat occurrence of the same workflow stripe (per TASK-CUO-201's halt-on-repeat rule) halts the workflow with <code>HITL_HALT</code>. <em>(traces_to: §1 #5 via TASK-CUO-201 §1 #7)</em></li><li>Per applied workflow proposal: exactly one <code>cuo.workflow_refinement_applied</code> aux row + <code>workflow_version</code> in YAML frontmatter is bumped per §2 rules. <em>(traces_to: §1 #6, #7)</em></li><li>A workflow proposal whose pattern change converts <code>linear</code> → <code>time_critical</code> is classified <code>major</code> + queued (not auto-applied even with <code>--auto-workflow-diffs</code>). <em>(traces_to: §1 #6)</em></li><li>The workflow report cites at least one specific task id per tripped signal — the operator can drill from the report into the task's debug trace. <em>(traces_to: §1 #9)</em></li></ol>\n<h2>§5 Verification</h2>\n<ul><li><code>modules/cuo/tests/test_workflow_evolution.py::test_metrics_aggregation</code> (AC #1, #2)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_routed_back_rate_trips</code> (AC #3)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_proposal_body_sections</code> (AC #4)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_workflow_stripe_format</code> (AC #5)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_step_addition_classifier</code> (AC #6)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_repeat_stripe_halts</code> (AC #7)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_apply_emits_audit_row</code> (AC #8)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_pattern_change_is_major</code> (AC #9)</li><li><code>modules/cuo/tests/test_workflow_evolution.py::test_report_cites_task_ids</code> (AC #10)</li></ul>";
