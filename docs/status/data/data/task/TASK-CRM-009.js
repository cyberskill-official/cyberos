window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CRM-009"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The CRM service <strong>MUST</strong> ship vietnam-bank-transfer@1 skill at <code>services/crm/src/vn/vietqr_skill.rs</code> generating VietQR PNG with embedded payment metadata, per-tenant bank config, 3 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> register skill <code>vietnam-bank-transfer@1</code> per DEC-1690.</li></ol>\n<ol><li><strong>MUST</strong> validate <code>qr_purpose</code> against closed enum per DEC-1691.</li></ol>\n<ol><li><strong>MUST</strong> require tenant bank config at table <code>tenant_bank_config</code>: ``<code>sql CREATE TABLE tenant_bank_config ( tenant_id UUID PRIMARY KEY, bank_bin TEXT NOT NULL,  -- 6-digit bank identifier per Napas account_number TEXT NOT NULL, account_holder_name TEXT NOT NULL, set_by UUID NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT now() ); ALTER TABLE tenant_bank_config ENABLE ROW LEVEL SECURITY; CREATE POLICY bank_config_rls ON tenant_bank_config USING (tenant_id = current_setting('auth.tenant_id')::uuid) WITH CHECK (tenant_id = current_setting('auth.tenant_id')::uuid); GRANT UPDATE (bank_bin, account_number, account_holder_name, set_by, updated_at) ON tenant_bank_config TO cyberos_app; </code>``</li></ol>\n<ol><li><strong>MUST</strong> gate bank config writes to CFO role per DEC-1693 via TASK-AUTH-101.</li></ol>\n<ol><li><strong>MUST</strong> generate at <code>qr_generator.rs::generate(tenant_config, amount, memo)</code> per VietQR spec DEC-1692 — output PNG bytes; embedded payload <code>BCD\\n01\\n{bank_bin}\\n{account_number}\\n{amount}\\n{memo}</code>.</li></ol>\n<ol><li><strong>MUST</strong> generate memo per DEC-1694 — template <code>{tenant_short}-{deal_id_first_8}</code>. Must match TASK-INV-005 reconciliation pattern.</li></ol>\n<ol><li><strong>MUST</strong> expose skill endpoint: ``<code>text POST   /v1/crm/skill/vietnam-bank-transfer body: {qr_purpose, amount_vnd, deal_id?, memo_override?} </code>``</li></ol>\n<ol><li><strong>MUST</strong> validate amount_vnd &gt; 0 and ≤ 1B VND (Napas max single transfer).</li></ol>\n<ol><li><strong>MUST</strong> emit 3 memory audit kinds per DEC-1695. PII per TASK-MEMORY-111: account_number SHA-256 hashed; amount SHA256 in chain.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from skill call → generator → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> generate without bank config per DEC-1693 — return 412 (Precondition Failed) with link to config.</li></ol>\n<ol><li><strong>MUST NOT</strong> allow non-CFO write per DEC-1693 — 403.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why VietQR (DEC-1690)?</strong> Napas-standard QR for VN domestic transfers; scannable by every VN banking app.</p>\n<p><strong>Why CFO-only bank config (DEC-1693)?</strong> Misconfigured bank routes payments to wrong account = direct money loss; CFO has authority.</p>\n<p><strong>Why memo template (DEC-1694)?</strong> TASK-INV-005 reconciles inbound VietQR payments by memo string; mismatch = orphan payment.</p>\n<p><strong>Why amount cap (1B VND, DEC implicit)?</strong> Napas spec; transfers &gt;1B require separate higher-ceiling rail (TASK-TEN-102 covers).</p>\n<hr>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-text\">POST   /v1/crm/skill/vietnam-bank-transfer\nPUT    /v1/crm/bank-config                (CFO-only)\nGET    /v1/crm/bank-config                (read by skill caller)</code></pre>\n<p>Sample skill call:</p>\n<pre><code class=\"language-json\">{\n  \"qr_purpose\": \"deal_collection\",\n  \"amount_vnd\": 50000000,\n  \"deal_id\": \"uuid\",\n  \"memo_override\": null\n}</code></pre>\n<p>Response:</p>\n<pre><code class=\"language-json\">{\n  \"qr_png_base64\": \"iVBORw0KGgo...\",\n  \"memo\": \"CSV-a1b2c3d4\",\n  \"amount_vnd\": 50000000,\n  \"expires_at\": null\n}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Skill registered as vietnam-bank-transfer@1</strong>. 2. <strong>Bank config required (412 if not set)</strong>. 3. <strong>CFO-only bank config (403 for others)</strong>. 4. <strong>PNG generated with VietQR payload</strong>. 5. <strong>Memo template applied (tenant_short + deal_id_8)</strong>. 6. <strong>Memo override accepted (optional)</strong>. 7. <strong>qr_purpose enum 3 + cardinality test</strong>. 8. <strong>Amount &gt; 0 enforced</strong>. 9. <strong>Amount ≤ 1B VND enforced</strong>. 10. <strong>3 memory audit kinds emitted</strong>. 11. <strong>PII scrubbed (account_number+amount SHA256)</strong>. 12. <strong>RLS denies cross-tenant</strong>. 13. <strong>Trace_id preserved</strong>. 14. <strong>Memo matches TASK-INV-005 regex</strong>. 15. <strong>PNG render deterministic for same input</strong>. 16. <strong>Bank config update increments updated_at</strong>. 17. <strong>bank_bin format 6-digit numeric</strong>. 18. <strong>account_number max 20 chars</strong>. 19. <strong>account_holder_name required</strong>. 20. <strong>No QR for non-VN tenant amounts (currency=VND only)</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn generates_png_with_correct_memo() {\n    let ctx = TestContext::with_bank_config().await;\n    let r = ctx.gen_qr(\"deal_collection\", 50_000_000, Some(ctx.deal_id), None).await;\n    assert!(r.qr_png_base64.starts_with(\"iVBOR\"));  // PNG magic\n    assert!(r.memo.starts_with(\"CSV-\"));\n    assert_eq!(r.memo.len(), 4 + 8);  // prefix + 8-char deal\n}\n\n#[tokio::test]\nasync fn rejects_without_bank_config() {\n    let ctx = TestContext::new_tenant().await;\n    let r = ctx.try_gen_qr(\"manual_request\", 1000, None, None).await;\n    assert_eq!(r.status_code, 412);\n}\n\n#[tokio::test]\nasync fn non_cfo_rejected_on_config_write() {\n    let ctx = TestContext::with_non_cfo_user().await;\n    let r = ctx.update_bank_config_as(ctx.am_user, \"970422\", \"123456\", \"Cyberskill JSC\").await;\n    assert_eq!(r.status_code, 403);\n}\n\n// 5.4..5.8 — amount limits, memo override, enum cardinality, audit</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-CRM-001. <strong>Cross-module:</strong> TASK-SKILL-108 (skill registry), TASK-AUTH-101 (CFO role), TASK-INV-005 (reconciliation memo regex), TASK-TEN-102 (&gt;1B rail), TASK-MEMORY-111 (PII).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Bank config missing</td><td>precondition</td><td>412 + config link</td><td>CFO configures</td></tr><tr><td>Amount &gt; 1B VND</td><td>validate</td><td>400 with rail-switch suggestion</td><td>use TASK-TEN-102</td></tr><tr><td>Amount ≤ 0</td><td>validate</td><td>400</td><td>provide valid</td></tr><tr><td>PNG render fail (qrcode crate err)</td><td>catch</td><td>500 + sev-2 audit</td><td>retry</td></tr><tr><td>Deal_id doesn't exist (purpose=deal_collection)</td><td>validate</td><td>404</td><td>provide valid</td></tr><tr><td>Memo length &gt;100 chars</td><td>validate</td><td>400 (Napas limit)</td><td>shorten</td></tr><tr><td>Bank_bin invalid (not 6-digit)</td><td>validate</td><td>reject config write</td><td>provide valid</td></tr><tr><td>Non-CFO write attempt</td><td>RLS + role</td><td>403</td><td>request CFO</td></tr><tr><td>Cross-tenant config view</td><td>RLS</td><td>0 rows</td><td>inherent</td></tr><tr><td>Currency != VND</td><td>future check</td><td>400</td><td>use other rail</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 PNG via <code>qrcode</code> Rust crate; resolution 512x512 default; deterministic for same input.</li><li>§11.2 Memo: <code>tenant_short</code> from <code>tenant.short_code</code> (3-char), <code>deal_id_8</code> = first 8 chars of UUID.</li><li>§11.3 memory audit body: qr_purpose, deal_id; account_number + amount SHA256.</li><li>§11.4 TASK-INV-005 regex: <code>^[A-Z]{3}-[a-z0-9]{8}$</code> — memo template must match.</li><li>§11.5 Napas bank_bin reference: https://api.vietqr.io/v2/banks (look-up table).</li></ul>\n<hr>\n<p><em>End of TASK-CRM-009 spec.</em></p>";
