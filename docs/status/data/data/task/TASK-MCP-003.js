window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-MCP-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<p>The MCP service <strong>MUST</strong> ship SEP-986 naming validator at <code>services/mcp/src/naming/</code> enforcing skill ID pattern at registration + CI gate, 4 memory audit kinds.</p>\n<ol><li><strong>MUST</strong> validate <code>sep986_verb</code> against closed enum per DEC-2361.</li></ol>\n<ol><li><strong>MUST</strong> parse + validate at <code>validator.rs::validate(skill_id)</code> per DEC-2360:</li></ol>\n<ul><li>Regex: <code>^cyberos\\.([a-z][a-z0-9_]*)\\.([a-z]+)_([a-z][a-z0-9_]*)$</code></li><li>Extract module, verb, noun</li><li>Module ∈ TASK-MCP-002 registered modules</li><li>Verb ∈ closed enum</li><li>Noun: snake_case identifier</li></ul>\n<ol><li><strong>MUST</strong> hook into TASK-MCP-001 skill registration per DEC-2362 — reject with sev-2 audit if invalid.</li></ol>\n<ol><li><strong>MUST</strong> ship CI grep workflow per DEC-2362 — <code>.github/workflows/mcp-sep986-check.yml</code>: ```yaml</li></ol>\n<ul><li>name: SEP-986 naming check run: bash scripts/check_sep986_naming.sh ```</li></ul>\n<ol><li><strong>MUST</strong> validate module per DEC-2363 at <code>module_registry.rs::is_valid_module(name)</code>:</li></ol>\n<ul><li>Hardcoded list: ten, hr, rew, email, inv, crm, doc, kb, okr, res, learn, esop, cuo, mcp, auth, memory, ai, time, proj, chat, obs, portal, skill</li><li>Unknown → reject</li></ul>\n<ol><li><strong>MUST</strong> emit 4 memory audit kinds per DEC-2364. PII: skill IDs (public) ok.</li></ol>\n<ol><li><strong>MUST</strong> thread trace_id from validation → audit.</li></ol>\n<ol><li><strong>MUST NOT</strong> register without validation per DEC-2362.</li></ol>\n<ol><li><strong>MUST NOT</strong> bypass CI gate per DEC-2362.</li></ol>\n<hr>\n<h2>§2 — Why this design</h2>\n<p><strong>Why SEP-986 (DEC-2360)?</strong> Consistent naming across modules; discovery by pattern; collision avoidance.</p>\n<p><strong>Why verb enum (DEC-2361)?</strong> Without bounded set, every developer invents synonyms (get/fetch/retrieve/read).</p>\n<p><strong>Why CI gate (DEC-2362)?</strong> Catches violations at PR review; runtime check is safety net.</p>\n<hr>\n<h2>§3 — API contract</h2>\n<p>Sample validation request:</p>\n<pre><code class=\"language-json\">POST /v1/mcp/naming/validate\n{\n  \"skill_id\": \"cyberos.calendar.list_events\"\n}\n\n{\"valid\": true, \"module\": \"calendar\", \"verb\": \"list\", \"noun\": \"events\"}</code></pre>\n<p>Invalid:</p>\n<pre><code class=\"language-json\">{\n  \"skill_id\": \"cyberos.calendar.getEvents\"   # camelCase noun + 'get' may be OK but format wrong\n}\n\n{\"valid\": false, \"error\": \"noun must be snake_case\"}</code></pre>\n<hr>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>sep986_verb enum cardinality 15</strong>. 2. <strong>Regex matches valid IDs</strong>. 3. <strong>Module ∈ registered list</strong>. 4. <strong>Verb ∈ closed enum</strong>. 5. <strong>Noun snake_case</strong>. 6. <strong>Registration gated</strong>. 7. <strong>CI grep catches violations</strong>. 8. <strong>4 memory audit kinds emitted</strong>. 9. <strong>PII: skill IDs (public) ok</strong>. 10. <strong>RLS denies cross-tenant</strong>. 11. <strong>Trace_id preserved</strong>. 12. <strong>Append-only validations log</strong>. 13. <strong>CI script in repo</strong>. 14. <strong>Bypass impossible (required check)</strong>. 15. <strong>Error messages specific</strong>. 16. <strong>Validation perf &lt; 1ms</strong>. 17. <strong>Module list maintained in code</strong>. 18. <strong>New module addition documented</strong>. 19. <strong>Capitalization rejected</strong>. 20. <strong>Reserved verb additions need RFC</strong>.</li></ol>\n<hr>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-rust\">#[tokio::test]\nasync fn valid_skill_id_passes() {\n    let ok = vec![\n        \"cyberos.calendar.list_events\",\n        \"cyberos.email.send_message\",\n        \"cyberos.inv.create_invoice\",\n    ];\n    for id in ok {\n        let r = validator::validate(id).await;\n        assert!(r.is_ok());\n    }\n}\n\n#[tokio::test]\nasync fn invalid_skill_id_rejected() {\n    let bad = vec![\n        \"cyberos.calendar.listEvents\",  // camelCase\n        \"calendar.list_events\",  // missing prefix\n        \"cyberos.unknown.list_things\",  // unknown module\n        \"cyberos.calendar.fetch_events\",  // wait, fetch IS in enum\n        \"cyberos.calendar.retrieve_events\",  // 'retrieve' not in enum\n    ];\n    let mut bad_count = 0;\n    for id in bad {\n        if validator::validate(id).await.is_err() { bad_count += 1; }\n    }\n    assert!(bad_count &gt;= 3);\n}\n\n#[tokio::test]\nasync fn ci_grep_catches_non_conforming() {\n    let test_dir = create_test_dir_with_code(r#\"const SKILL: &amp;str = \"myskill.getStuff\";\"#);\n    let r = run_shell(\"scripts/check_sep986_naming.sh\", test_dir);\n    assert!(!r.success());\n}\n\n// 5.4..5.10</code></pre>\n<hr>\n<h2>§7 — Dependencies</h2>\n<p><strong>Upstream:</strong> TASK-MCP-001. <strong>Cross-module:</strong> TASK-MCP-002 (module registry), TASK-MEMORY-111 (audit).</p>\n<h2>§10 — Failure modes</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Verb not in enum</td><td>regex + enum</td><td>reject; sev-2 audit</td><td>use valid verb</td></tr><tr><td>Module unknown</td><td>registry check</td><td>reject; sev-2</td><td>add to registry</td></tr><tr><td>Camelcase noun</td><td>regex</td><td>reject</td><td>use snake_case</td></tr><tr><td>Missing prefix</td><td>regex</td><td>reject</td><td>add cyberos.</td></tr><tr><td>CI script bug</td><td>tests</td><td>CI fails</td><td>fix script</td></tr><tr><td>Validator perf</td><td>benchmark</td><td>inherent</td><td>inherent</td></tr><tr><td>Cross-tenant validation</td><td>RLS</td><td>inherent</td><td>inherent</td></tr><tr><td>Concurrent register</td><td>inherent</td><td>each isolated</td><td>inherent</td></tr><tr><td>Verb enum extension</td><td>RFC process</td><td>inherent</td><td>governance</td></tr><tr><td>Module list drift</td><td>maintained</td><td>inherent</td><td>code review</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>§11.1 Regex compiled once; reused for perf.</li><li>§11.2 CI script greps <code>*.rs</code> and <code>*.toml</code> for skill_id constants; fails on non-conforming pattern.</li><li>§11.3 memory audit body: skill_id, validation result; no PII (skill IDs public).</li><li>§11.4 Module list reviewed quarterly; additions require module owner sign + RFC.</li><li>§11.5 Verb enum additions require SEP RFC + community discussion.</li></ul>\n<hr>\n<p><em>End of TASK-MCP-003 spec.</em></p>";
