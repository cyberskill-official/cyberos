window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CHAT-269"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<ol><li>The chat service <strong>MUST</strong> expose three administrator routes: <code>GET /v1/chat/admin/reports</code> (the queue), <code>GET /v1/chat/admin/reports/:id</code> (one report in context), and <code>POST /v1/chat/admin/reports/:id/resolve</code> (act on it).</li></ol>\n<ol><li>All three <strong>MUST</strong> be gated on the caller holding <code>tenant-admin</code> or <code>root-admin</code> in the token's <code>roles</code> claim, and the gate <strong>MUST</strong> fail closed: a token with no <code>roles</code> claim at all is refused with <code>403</code>, never granted. This is chat's first consumer of the TASK-AUTH-101 roles claim; there is no legacy path to preserve and no reason to be lenient.</li></ol>\n<ol><li>Channel-level roles (<code>owner</code>, <code>manager</code>, <code>member</code>) <strong>MUST NOT</strong> grant access to the queue. A channel owner is not a workspace moderator, and a report raised in a channel they own may well be <em>about</em> them.</li></ol>\n<ol><li>The queue <strong>MUST</strong> group reports by target: three people reporting the same message is one queue entry with <code>report_count: 3</code>, not three entries. Reviewing the same message three times is how a moderation queue trains its reviewer to stop reading it.</li></ol>\n<ol><li>The queue <strong>MUST</strong> order by severity first and age second. Severity is derived from <code>reason</code> in a fixed rank: <code>self_harm &gt; illegal &gt; violence &gt; sexual &gt; hate &gt; harassment &gt; spam &gt; other</code>. A <code>self_harm</code> report filed a minute ago outranks a <code>spam</code> report filed last week, and no amount of spam volume may push it down.</li></ol>\n<ol><li>The queue <strong>MUST</strong> be filterable by <code>status</code> and by <code>reason</code>, and <strong>MUST</strong> paginate by an opaque cursor. It <strong>MUST NOT</strong> offer an unpaginated \"all\" mode.</li></ol>\n<ol><li>The report detail <strong>MUST</strong> render the immutable snapshot captured by TASK-CHAT-267 §1 #4, and <strong>MUST</strong> mark plainly whether the original content still exists (<code>original_present: true | false</code>). A reviewer must be able to tell the difference between \"this is what they said\" and \"this is what they said, and they have since deleted it\" - the second is itself evidence.</li></ol>\n<ol><li>Surrounding context (the messages immediately before and after the reported one) <strong>MUST</strong> be returned <strong>only</strong> when both of these hold: the target is in a <strong>group</strong> channel, and the administrator is <strong>already a member of that channel</strong>. Otherwise the response carries the snapshot alone.</li></ol>\n<ol><li>In particular, a report about a <strong>direct message MUST NOT</strong> cause any part of that DM thread to be disclosed to the administrator beyond the single reported message. The reporter consented to disclosing <em>that message</em> by reporting it. They did not consent to handing their private correspondence to their employer, and a moderation feature that quietly does so is a surveillance feature wearing a safety badge.</li></ol>\n<ol><li>The blocked-set from TASK-CHAT-268 <strong>MUST NOT</strong> be applied to any of these three routes. An administrator who has blocked someone must still be able to adjudicate a report about them.</li></ol>\n<ol><li><code>POST .../resolve</code> <strong>MUST</strong> accept exactly one <code>action</code> from a closed set - <code>dismiss | delete_message | remove_member</code> - plus an optional <code>note</code> (≤ 1000 chars) recording the reviewer's reasoning.</li></ol>\n<ol><li><code>resolve</code> <strong>MUST</strong> be a compare-and-swap on <code>status = 'open'</code>. If two administrators act on the same report concurrently, exactly one <strong>MUST</strong> win; the loser <strong>MUST</strong> receive <code>200</code> carrying the winning resolution, <strong>MUST NOT</strong> re-apply the action, and <strong>MUST NOT</strong> emit a second audit row.</li></ol>\n<ol><li>When a report is resolved with <code>delete_message</code> or <code>remove_member</code>, every <strong>other</strong> open report against the same target <strong>MUST</strong> be resolved with the same action in the same transaction. A message can only be deleted once, and leaving five siblings open against a message that no longer exists is how a queue fills with ghosts.</li></ol>\n<ol><li><code>remove_member</code> <strong>MUST</strong> remove the reported subject from the channel the report came from - not from the workspace. Removing a person from the organisation is an identity operation that belongs to the AUTH module and to a human in HR, not to a chat moderation button.</li></ol>\n<ol><li>Every resolution <strong>MUST</strong> emit exactly one <code>chat.report_resolved</code> audit row carrying <code>report_id</code>, <code>action</code>, <code>sibling_report_ids</code>, and the administrator as actor. The underlying effect (<code>chat.message_deleted</code>, <code>chat.member_removed</code>) is emitted by the existing handler, so a resolution that deletes a message produces <strong>two</strong> rows, and that is correct: one records the decision, one records the effect.</li></ol>\n<ol><li>The audit row <strong>MUST NOT</strong> carry the snapshot body or the reviewer's <code>note</code>. Same reasoning as TASK-CHAT-267 §1 #8: the audit chain is the wrong place for content someone asked us to remove.</li></ol>\n<ol><li>Resolved reports, <strong>including their snapshots</strong>, <strong>MUST</strong> be purged 90 days after resolution. This is not optional tidiness: the snapshot is a copy of content the reporter wanted gone, and 90 days matches the retention window already published at <code>cyberskill.world/en/cyberos/delete-account</code>.</li></ol>\n<ol><li>The client <strong>MUST</strong> expose the queue only to administrators, and <strong>MUST NOT</strong> render a Moderation entry in the navigation for anyone else. A visible-but-403 route teaches everyone in the workspace that a moderation surface exists and that they are not trusted with it.</li></ol>\n<ol><li>The client <strong>MUST</strong> link to the published content policy (<code>https://cyberskill.world/{locale}/cyberos/content-policy</code>) from Settings, and the report dialog from TASK-CHAT-267 <strong>MUST</strong> carry the same link. Google Play requires the policy to exist and to be reachable; a policy nobody can find is not a policy.</li></ol>\n<ol><li><code>note</code> and <code>detail</code> <strong>MUST</strong> be rendered as text, never as HTML or markdown. They are attacker-controlled strings displayed in an administrator's browser.</li></ol>\n<ol><li>Every string <strong>MUST</strong> ship in <code>en</code> and <code>vi</code>.</li></ol>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why the DM carve-out (§1 #9).</strong> This is the clause worth arguing about, so here is the argument. Alice reports a harassing DM from Bob. The administrator needs to see that message - obviously. Does the administrator need to see the rest of Alice and Bob's DM history? No. But it is the single easiest thing to build (fetch the channel, page the messages), and the most natural thing to want (\"I need context\"), and it would quietly convert a safety feature into an employer surveillance tool: file a report, and your private messages become readable by your boss. That is a betrayal of the person who came to us for help, and it is the reason people do not report. The snapshot is what they consented to disclose. That is all they get.</p>\n<p><strong>Why the administrator must already be a channel member to see group context (§1 #8).</strong> Same principle, weaker case. A private channel the admin is not in is not their business, and a report should not be a skeleton key to it. If they need the context, they can join the channel - visibly, as an act with its own audit row - rather than acquiring a silent read.</p>\n<p><strong>Why grouping (§1 #4) and severity ordering (§1 #5) are normative and not \"nice to have\".</strong> A moderation queue is only as good as the attention of the person reading it, and the failure mode is not that they reject a good report - it is that they stop opening the queue. Three duplicate entries for one message, and a <code>self_harm</code> report buried under forty spam reports, are the two mechanisms by which that happens. Both are structural and both are cheap to prevent, so both are MUSTs.</p>\n<p><strong>Why <code>remove_member</code> removes from the channel, not the workspace (§1 #14).</strong> Because firing someone is not a chat feature. A single click in a chat UI should not be able to sever a person's access to the organisation's systems. Removing them from the channel where the harm happened stops the harm; the rest is a conversation between humans.</p>\n<p><strong>Why CAS on resolve (§1 #12), and why the loser gets a 200.</strong> Two admins opening the queue at the same time is the normal case in a small workspace, not an edge case. Without CAS, <code>delete_message</code> runs twice - the second deletion is a no-op, but the <em>audit chain</em> now claims two people independently decided to delete the same message, which is false, and the audit chain is the artefact we ask people to trust. The loser gets <code>200</code> and the winning resolution, because from their point of view the outcome they wanted has happened; an error would be technically accurate and practically useless.</p>\n<p><strong>Why resolved reports are purged after 90 days (§1 #17).</strong> The snapshot exists because we could not trust the sender not to destroy the evidence. Once the report is resolved, that justification expires, and what remains is a durable copy of exactly the content someone asked us to remove, sitting in a table their employer can read. The published deletion page already commits to a 90-day window for security-relevant logs. This matches it, deliberately: one published number, one behaviour.</p>\n<p><strong>Why the roles gate fails closed on a missing claim (§1 #2).</strong> TASK-AUTH-101 allows a 30-day grace window where a token may carry no <code>rbac_v</code>. Chat has never read <code>roles</code> before, so it has no legacy tokens to be gentle with. A missing claim is not \"unknown, therefore allow\"; it is \"unknown, therefore no\". Being lenient here would mean every pre-TASK-AUTH-101 token in circulation is a moderator.</p>\n<h2>§3 - API contract</h2>\n<h3>Retention migration</h3>\n<pre><code class=\"language-sql\">-- services/chat/migrations/0015_chat_reports_retention.sql\n-- TASK-CHAT-269 §1 #17. The snapshot is a copy of content someone asked us to consider removing. It is\n-- justified while the report is open (the sender can destroy the original) and unjustified once it is\n-- resolved. 90 days matches the window published at cyberskill.world/en/cyberos/delete-account.\n\nALTER TABLE chat_reports\n    ADD COLUMN IF NOT EXISTS purge_after TIMESTAMPTZ NULL;\n\nCREATE INDEX IF NOT EXISTS chat_reports_purge_idx\n    ON chat_reports (purge_after) WHERE purge_after IS NOT NULL;\n\n-- The purge itself is a job, not a trigger: a trigger would delete rows mid-transaction while an admin\n-- is reading them. Runs hourly; deletes the row outright (snapshot included), not just the snapshot,\n-- because a resolved report with no evidence is not a record of anything.</code></pre>\n<h3>Routes</h3>\n<pre><code>GET  /v1/chat/admin/reports?status=open&amp;reason=harassment&amp;cursor=&lt;opaque&gt;&amp;limit=25\nGET  /v1/chat/admin/reports/:id\nPOST /v1/chat/admin/reports/:id/resolve   { \"action\": \"delete_message\", \"note\": \"…\" }\n\n403  \"moderation requires tenant-admin\"    -- including a token with NO roles claim\n404  \"no such report\"                       -- and for a report in another tenant (never 403: do not\n                                            --  confirm that an id exists in a workspace you cannot see)\n200  on resolve, including the CAS loser (carries the winning resolution)</code></pre>\n<h3>The role gate</h3>\n<pre><code class=\"language-rust\">// services/chat/src/auth.rs\n\nconst MODERATOR_ROLES: [&amp;str; 2] = [\"tenant-admin\", \"root-admin\"];\n\n/// §1 #2 - fail closed. An absent `roles` claim is NOT a wildcard.\npub fn require_moderator(claims: &amp;Claims) -&gt; Result&lt;(), (StatusCode, String)&gt; {\n    if claims.roles.iter().any(|r| MODERATOR_ROLES.contains(&amp;r.as_str())) {\n        return Ok(());\n    }\n    Err((StatusCode::FORBIDDEN, \"moderation requires tenant-admin\".to_string()))\n}</code></pre>\n<h3>Severity</h3>\n<pre><code class=\"language-rust\">// services/chat/src/moderation.rs\n// §1 #5. An explicit rank, not a Debug-ordered enum: the ordering is a product decision and must be\n// legible to whoever changes it next.\nfn severity_rank(reason: &amp;str) -&gt; i16 {\n    match reason {\n        \"self_harm\"  =&gt; 0,\n        \"illegal\"    =&gt; 1,\n        \"violence\"   =&gt; 2,\n        \"sexual\"     =&gt; 3,\n        \"hate\"       =&gt; 4,\n        \"harassment\" =&gt; 5,\n        \"spam\"       =&gt; 6,\n        _            =&gt; 7,   // \"other\"\n    }\n}</code></pre>\n<h3>Queue query</h3>\n<pre><code class=\"language-sql\">-- §1 #4 grouping + §1 #5 ordering, in one pass. The GROUP BY is on the target triple, so three\n-- reports against one message fold into one row carrying report_count = 3.\nSELECT\n    min(r.id)                      AS lead_report_id,\n    r.target_kind,\n    r.target_message_id,\n    r.target_attachment_id,\n    r.target_subject_id,\n    count(*)                       AS report_count,\n    min(severity_rank(r.reason))   AS severity,      -- worst reason wins the group\n    max(r.created_at)              AS last_reported_at,\n    array_agg(DISTINCT r.reason)   AS reasons\nFROM chat_reports r\nWHERE r.status = 'open'\n  AND ($1::text IS NULL OR r.reason = $1)\nGROUP BY r.target_kind, r.target_message_id, r.target_attachment_id, r.target_subject_id\nORDER BY severity ASC, last_reported_at DESC\nLIMIT $2;</code></pre>\n<h3>Resolve</h3>\n<pre><code class=\"language-rust\">pub async fn resolve(\n    State(state): State&lt;AppState&gt;,\n    headers: HeaderMap,\n    Path(id): Path&lt;Uuid&gt;,\n    Json(req): Json&lt;Resolve&gt;,\n) -&gt; Result&lt;(StatusCode, Json&lt;Resolution&gt;), (StatusCode, String)&gt; {\n    let claims = auth::verify(&amp;state, &amp;headers)\n        .map_err(|e| (StatusCode::UNAUTHORIZED, e.to_string()))?;\n    auth::require_moderator(&amp;claims)?;                                    // §1 #2\n    let (tenant, admin) = (claims.tenant_id, claims.subject_id);\n\n    let mut tx = db::begin_tenant(&amp;state.pool, tenant).await?;\n\n    // §1 #12 - CAS. Exactly one concurrent resolver transitions the row.\n    let won: Option&lt;(String, Option&lt;Uuid&gt;, Option&lt;Uuid&gt;)&gt; = sqlx::query_as(\n        \"UPDATE chat_reports\n            SET status = $2, resolution = $3, resolved_at = now(),\n                resolved_by_subject_id = $4, purge_after = now() + interval '90 days'\n          WHERE id = $1 AND status = 'open'\n        RETURNING target_kind, target_message_id, target_subject_id\",\n    )\n    .bind(id)\n    .bind(if req.action == Action::Dismiss { \"dismissed\" } else { \"actioned\" })\n    .bind(req.action.as_label())\n    .bind(admin)\n    .fetch_optional(&amp;mut *tx)\n    .await\n    .map_err(db::internal)?;\n\n    let Some((kind, msg_id, subj_id)) = won else {\n        // Lost the race, or already resolved. Return the WINNING resolution with 200 - do not\n        // re-apply the action, do not emit a second audit row (§1 #12).\n        let existing = load_resolution(&amp;mut *tx, id).await?\n            .ok_or((StatusCode::NOT_FOUND, \"no such report\".to_string()))?;\n        tx.commit().await.map_err(db::internal)?;\n        return Ok((StatusCode::OK, Json(existing)));\n    };\n\n    // §1 #13 - close every sibling open report against the same target, in this transaction.\n    let siblings: Vec&lt;Uuid&gt; = sqlx::query_scalar(\n        \"UPDATE chat_reports SET status = $2, resolution = $3, resolved_at = now(),\n                resolved_by_subject_id = $4, purge_after = now() + interval '90 days'\n          WHERE status = 'open' AND id &lt;&gt; $1\n            AND target_kind = $5\n            AND target_message_id IS NOT DISTINCT FROM $6\n            AND target_subject_id IS NOT DISTINCT FROM $7\n        RETURNING id\",\n    )\n    .bind(id).bind(\"actioned\").bind(req.action.as_label()).bind(admin)\n    .bind(&amp;kind).bind(msg_id).bind(subj_id)\n    .fetch_all(&amp;mut *tx)\n    .await\n    .map_err(db::internal)?;\n\n    match req.action {\n        Action::Dismiss =&gt; {}\n        Action::DeleteMessage =&gt; {\n            let mid = msg_id.ok_or((StatusCode::BAD_REQUEST,\n                \"delete_message on a non-message report\".to_string()))?;\n            messages::soft_delete_as_moderator(&amp;mut tx, tenant, admin, mid).await?;   // emits chat.message_deleted\n        }\n        Action::RemoveMember =&gt; {\n            // §1 #14 - from the CHANNEL the report came from. Not from the workspace.\n            members::remove_as_moderator(&amp;mut tx, tenant, admin, report_channel(&amp;mut *tx, id).await?,\n                                         subj_id_or_sender(&amp;mut *tx, id).await?).await?;\n        }\n    }\n\n    tx.commit().await.map_err(db::internal)?;\n\n    // §1 #15, #16 - one row for the DECISION. No snapshot, no note.\n    audit::emit(&amp;state, tenant, admin, \"chat.report_resolved\", json!({\n        \"report_id\":          id,\n        \"action\":             req.action.as_label(),\n        \"sibling_report_ids\": siblings,\n    })).await;\n\n    Ok((StatusCode::OK, Json(Resolution { id, action: req.action, by: admin })))\n}</code></pre>\n<h3>Detail response</h3>\n<pre><code class=\"language-rust\">#[derive(Serialize)]\npub struct ReportDetail {\n    pub id: Uuid,\n    pub reason: String,\n    pub detail: Option&lt;String&gt;,\n    pub reported_at: DateTime&lt;Utc&gt;,\n    pub reporter_subject_id: Uuid,          // admins see who reported. The reported person never does.\n    pub report_count: i64,\n\n    /// The evidence (TASK-CHAT-267 §1 #4). Always present for message/attachment targets.\n    pub snapshot_body: Option&lt;String&gt;,\n    pub snapshot_sender_id: Option&lt;Uuid&gt;,\n    pub snapshot_taken_at: DateTime&lt;Utc&gt;,\n\n    /// §1 #7 - has the sender since removed it? That is itself evidence.\n    pub original_present: bool,\n\n    /// §1 #8, #9 - populated ONLY for a group-channel target whose channel the admin is already in.\n    /// Always empty for a DM. There is no flag to override this.\n    pub context: Vec&lt;Message&gt;,\n}</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>The role gate fails closed</strong> - a token with <code>roles: []</code>, and a token with no <code>roles</code> claim at all, both receive <code>403</code> on all three routes.</li><li><strong>A channel owner is not a moderator</strong> - a subject who is <code>owner</code> of the channel but holds no <code>tenant-admin</code> role receives <code>403</code>.</li><li><strong>Duplicate reports fold into one entry</strong> - three reports against one message produce one queue row with <code>report_count: 3</code> and <code>reasons</code> listing all three.</li><li><strong>Severity outranks age</strong> - a <code>self_harm</code> report filed one minute ago sorts above a <code>spam</code> report filed a week ago, and above forty of them.</li><li><strong>The queue is always paginated</strong> - no parameter combination returns an unbounded list; <code>limit</code> above the cap is clamped.</li><li><strong>The snapshot renders even when the original is gone</strong> - after the sender soft-deletes the reported message, the detail still returns <code>snapshot_body</code> in full and sets <code>original_present: false</code>.</li><li><strong>A DM report discloses only the reported message</strong> - <code>context</code> is empty, and no other message from that DM appears anywhere in the response.</li><li><strong>Group context requires membership</strong> - an admin who is not a member of the private channel gets <code>context: []</code>; an admin who is a member gets the surrounding messages.</li><li><strong>Blocks do not apply</strong> - an admin who has blocked the reported person still sees the full snapshot and full context.</li><li><strong>CAS: exactly one winner</strong> - two concurrent <code>resolve</code> calls on one report produce exactly one state change, one <code>chat.message_deleted</code>, and one <code>chat.report_resolved</code>; the loser receives <code>200</code> carrying the winner's action.</li><li><strong>Siblings are closed</strong> - resolving one of five open reports against the same message with <code>delete_message</code> leaves zero open reports against that message and lists the four sibling ids in the audit row.</li><li><strong><code>remove_member</code> removes from the channel only</strong> - the reported subject loses that channel's membership and retains every other channel and their account.</li><li><strong>A resolution emits two audit rows, not one</strong> - <code>chat.report_resolved</code> (the decision) and <code>chat.message_deleted</code> (the effect); a <code>dismiss</code> emits only the first.</li><li><strong>The audit row carries no content</strong> - <code>chat.report_resolved</code> contains neither <code>snapshot_body</code> nor <code>note</code>.</li><li><strong>Resolved reports carry a purge date</strong> - <code>purge_after</code> is set to resolution + 90 days on every resolution, including <code>dismiss</code>.</li><li><strong>Cross-tenant probing returns 404, not 403</strong> - a valid admin of tenant A requesting a report id belonging to tenant B receives <code>404</code>.</li><li><strong>The Moderation route is invisible to non-admins</strong> - the navigation entry is absent, not merely disabled, for a subject without the role.</li><li><strong><code>note</code> and <code>detail</code> render as text</strong> - a <code>note</code> containing <code>&lt;img src=x onerror=alert(1)&gt;</code> renders as literal characters.</li><li><strong>The content-policy link is present</strong> - Settings and the report dialog both link to <code>/{locale}/cyberos/content-policy</code>.</li><li><strong>Both locales render</strong> - every moderation string resolves in <code>en</code> and <code>vi</code>.</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-rust\">// services/chat/tests/moderation.rs\n\n#[tokio::test]\nasync fn role_gate_fails_closed() {                                  // AC 1, 2\n    let app = harness().await;\n    for tok in [app.token_with_roles(ALICE, &amp;[]),\n                app.token_without_roles_claim(ALICE),\n                app.token_with_roles(ALICE, &amp;[\"tenant-member\"])] {\n        assert_eq!(app.queue_with(tok).await.status(), StatusCode::FORBIDDEN);\n    }\n    // channel owner, no workspace role\n    let ch = app.channel(\"general\", &amp;[ALICE]).await;                 // ALICE is owner\n    assert_eq!(app.queue_with(app.token_with_roles(ALICE, &amp;[])).await.status(),\n               StatusCode::FORBIDDEN);\n    let _ = ch;\n    assert_eq!(app.queue_with(app.token_with_roles(ADMIN, &amp;[\"tenant-admin\"])).await.status(),\n               StatusCode::OK);\n}\n\n#[tokio::test]\nasync fn severity_outranks_age_and_duplicates_fold() {               // AC 3, 4\n    let app = harness().await;\n    let ch = app.channel(\"general\", &amp;[ALICE, BOB, CAROL, DAVE]).await;\n    let spam = app.post_as(BOB, ch, \"buy now\").await;\n    for who in [ALICE, CAROL, DAVE] {                                 // 3 reports, 1 target\n        app.report_as(who, json!({\"target_kind\":\"message\",\"target_message_id\":spam,\"reason\":\"spam\"})).await;\n    }\n    app.backdate_reports(Duration::days(7)).await;\n\n    let sh = app.post_as(BOB, ch, \"…\").await;\n    app.report_as(ALICE, json!({\"target_kind\":\"message\",\"target_message_id\":sh,\"reason\":\"self_harm\"})).await;\n\n    let q = app.queue_as(ADMIN).await;\n    assert_eq!(q.len(), 2, \"three spam reports must fold into ONE entry\");\n    assert_eq!(q[0].target_message_id, Some(sh), \"self_harm outranks week-old spam\");\n    assert_eq!(q[1].report_count, 3);\n}\n\n#[tokio::test]\nasync fn a_dm_report_discloses_only_the_reported_message() {         // AC 7 - the privacy property\n    let app = harness().await;\n    let dm = app.dm(ALICE, BOB).await;\n    app.post_as(ALICE, dm, \"private thing one\").await;\n    let bad = app.post_as(BOB, dm, \"the abusive line\").await;\n    app.post_as(ALICE, dm, \"private thing two\").await;\n\n    let r = app.report_as(ALICE, json!({\n        \"target_kind\":\"message\",\"target_message_id\":bad,\"reason\":\"harassment\"})).await.id();\n\n    let detail = app.report_detail_as(ADMIN, r).await;\n    assert_eq!(detail.snapshot_body.unwrap(), \"the abusive line\");\n    assert!(detail.context.is_empty(), \"a DM must disclose NO surrounding context\");\n    let body = detail.raw_json();\n    assert!(!body.contains(\"private thing one\"));\n    assert!(!body.contains(\"private thing two\"));\n}\n\n#[tokio::test]\nasync fn group_context_requires_membership() {                       // AC 8\n    let app = harness().await;\n    let ch  = app.channel(\"private-team\", &amp;[BOB, CAROL]).await;      // ADMIN is NOT a member\n    let msg = app.post_as(BOB, ch, \"reported\").await;\n    app.post_as(CAROL, ch, \"surrounding chatter\").await;\n    let r = app.report_as(CAROL, json!({\n        \"target_kind\":\"message\",\"target_message_id\":msg,\"reason\":\"hate\"})).await.id();\n\n    let d1 = app.report_detail_as(ADMIN, r).await;\n    assert!(d1.context.is_empty());\n    assert!(!d1.raw_json().contains(\"surrounding chatter\"));\n\n    app.join_channel(ADMIN, ch).await;                                // visible, audited act\n    let d2 = app.report_detail_as(ADMIN, r).await;\n    assert!(d2.context.iter().any(|m| m.body == \"surrounding chatter\"));\n}\n\n#[tokio::test]\nasync fn snapshot_renders_after_the_sender_deletes() {               // AC 6\n    let app = harness().await;\n    let ch  = app.channel(\"general\", &amp;[ALICE, BOB]).await;\n    let msg = app.post_as(BOB, ch, \"the evidence\").await;\n    let r = app.report_as(ALICE, json!({\n        \"target_kind\":\"message\",\"target_message_id\":msg,\"reason\":\"harassment\"})).await.id();\n\n    app.delete_as(BOB, msg).await;\n\n    let d = app.report_detail_as(ADMIN, r).await;\n    assert_eq!(d.snapshot_body.unwrap(), \"the evidence\");\n    assert!(!d.original_present);\n}\n\n#[tokio::test]\nasync fn concurrent_resolve_has_exactly_one_winner() {               // AC 10, 13\n    let app = harness().await;\n    let ch  = app.channel(\"general\", &amp;[ALICE, BOB]).await;\n    let msg = app.post_as(BOB, ch, \"delete me\").await;\n    let r = app.report_as(ALICE, json!({\n        \"target_kind\":\"message\",\"target_message_id\":msg,\"reason\":\"hate\"})).await.id();\n\n    let body = json!({\"action\":\"delete_message\"});\n    let (a, b) = tokio::join!(\n        app.resolve_as(ADMIN,   r, body.clone()),\n        app.resolve_as(ADMIN_2, r, body.clone()),\n    );\n    assert_eq!(a.status(), StatusCode::OK);\n    assert_eq!(b.status(), StatusCode::OK);\n    assert_eq!(app.audit_rows(\"chat.report_resolved\").await.len(), 1);\n    assert_eq!(app.audit_rows(\"chat.message_deleted\").await.len(), 1);\n}\n\n#[tokio::test]\nasync fn siblings_close_and_the_audit_lists_them() {                 // AC 11, 14\n    let app = harness().await;\n    let ch  = app.channel(\"general\", &amp;[ALICE, BOB, CAROL, DAVE, ERIN]).await;\n    let msg = app.post_as(BOB, ch, \"abuse\").await;\n    let ids: Vec&lt;_&gt; = [ALICE, CAROL, DAVE, ERIN].iter()\n        .map(|w| app.report_as(*w, json!({\n            \"target_kind\":\"message\",\"target_message_id\":msg,\"reason\":\"hate\"})))\n        .collect::&lt;FuturesOrdered&lt;_&gt;&gt;().collect().await;\n\n    app.resolve_as(ADMIN, ids[0], json!({\"action\":\"delete_message\",\"note\":\"clear breach\"})).await;\n\n    assert_eq!(app.open_reports_against(msg).await, 0);\n    let row = &amp;app.audit_rows(\"chat.report_resolved\").await[0];\n    assert_eq!(row[\"payload\"][\"sibling_report_ids\"].as_array().unwrap().len(), 3);\n    assert!(row.get(\"note\").is_none());\n    assert!(!row.to_string().contains(\"clear breach\"));\n    assert!(!row.to_string().contains(\"abuse\"));\n}\n\n#[tokio::test]\nasync fn remove_member_touches_the_channel_only() {                  // AC 12\n    let app = harness().await;\n    let ch1 = app.channel(\"general\", &amp;[ALICE, BOB]).await;\n    let ch2 = app.channel(\"random\",  &amp;[ALICE, BOB]).await;\n    let msg = app.post_as(BOB, ch1, \"abuse\").await;\n    let r = app.report_as(ALICE, json!({\n        \"target_kind\":\"message\",\"target_message_id\":msg,\"reason\":\"harassment\"})).await.id();\n\n    app.resolve_as(ADMIN, r, json!({\"action\":\"remove_member\"})).await;\n\n    assert!(!app.is_member(BOB, ch1).await);\n    assert!( app.is_member(BOB, ch2).await, \"removal is scoped to the channel\");\n    assert!( app.subject_exists(BOB).await, \"removal is NOT an identity operation\");\n}\n\n#[tokio::test]\nasync fn cross_tenant_probe_is_404_not_403() {                       // AC 16\n    let app = harness().await;\n    let r = app.seed_report_in(TENANT_B).await;\n    let res = app.report_detail_raw(app.admin_of(TENANT_A), r).await;\n    assert_eq!(res.status(), StatusCode::NOT_FOUND);   // NOT 403 - that would confirm the id exists\n}\n\n#[tokio::test]\nasync fn resolution_sets_the_purge_date() {                          // AC 15\n    let app = harness().await;\n    let r = app.seed_open_report().await;\n    app.resolve_as(ADMIN, r, json!({\"action\":\"dismiss\"})).await;\n    let purge: DateTime&lt;Utc&gt; = app.scalar(\"SELECT purge_after FROM chat_reports WHERE id=$1\", r).await;\n    assert!((purge - Utc::now() - Duration::days(90)).num_minutes().abs() &lt; 5);\n}</code></pre>\n<pre><code class=\"language-tsx\">// apps/web/src/routes/__tests__/Moderation.test.tsx                 // AC 17, 18, 19\ntest(\"the nav entry is absent, not disabled, for a non-admin\", () =&gt; {\n  const { queryByRole } = render(&lt;App user={{ roles: [\"tenant-member\"] }} /&gt;);\n  expect(queryByRole(\"link\", { name: /moderation/i })).toBeNull();\n});\n\ntest(\"a note is rendered as text, never as markup\", () =&gt; {\n  const { getByTestId } = render(\n    &lt;ReportCard report={{ note: '&lt;img src=x onerror=alert(1)&gt;' }} /&gt;);\n  expect(getByTestId(\"note\").textContent).toBe('&lt;img src=x onerror=alert(1)&gt;');\n  expect(getByTestId(\"note\").querySelector(\"img\")).toBeNull();\n});</code></pre>\n<h2>§6 - Implementation skeleton</h2>\n<p>(§3 is the skeleton. The purge job is the only piece not shown there.)</p>\n<pre><code class=\"language-rust\">// Hourly. A job, not a trigger: a trigger would delete rows out from under an admin mid-read.\npub async fn purge_resolved_reports(pool: &amp;PgPool) -&gt; anyhow::Result&lt;u64&gt; {\n    let n = sqlx::query(\"DELETE FROM chat_reports WHERE purge_after IS NOT NULL AND purge_after &lt; now()\")\n        .execute(pool).await?.rows_affected();\n    if n &gt; 0 { tracing::info!(target: \"cyberos_chat::moderation\", purged = n, \"resolved reports purged\"); }\n    Ok(n)\n}</code></pre>\n<h2>§7 - Dependencies</h2>\n<ul><li><strong>Upstream:</strong> TASK-CHAT-267 (<code>chat_reports</code> and its snapshot columns; this task is the only writer of <code>status</code>, <code>resolution</code>, <code>resolved_at</code>, <code>resolved_by_subject_id</code>, <code>purge_after</code>).</li><li><strong>Cross-module:</strong> TASK-AUTH-101 supplies the <code>roles</code> claim. Chat is its first consumer; no auth-side change is required, but if the role catalogue ever renames <code>tenant-admin</code>, <code>MODERATOR_ROLES</code> moves with it.</li><li><strong>Constrains:</strong> TASK-CHAT-268 - §1 #10 here requires that the blocked-set is <em>not</em> applied to these routes. That is asserted from both sides (AC #9 here, AC #12 there).</li><li><strong>External:</strong> the content policy page at <code>cyberskill.world/{locale}/cyberos/content-policy</code> must exist before §1 #19's link is anything but a 404. It ships in the landing-page repo alongside the privacy and account-deletion pages, not here.</li></ul>\n<h2>§8 - Example payloads</h2>\n<p>Queue entry (three people reported one message):</p>\n<pre><code class=\"language-json\">{\n  \"lead_report_id\": \"9d2c8f31-0b44-4a6e-8f77-2c1e5a9b3d40\",\n  \"target_kind\": \"message\",\n  \"target_message_id\": \"6b1f4e0a-7c2d-4d19-9b0e-1f2a3c4d5e6f\",\n  \"report_count\": 3,\n  \"severity\": 4,\n  \"reasons\": [\"hate\", \"harassment\"],\n  \"last_reported_at\": \"2026-07-11T05:31:02Z\"\n}</code></pre>\n<p>Detail for a DM report - note the empty context:</p>\n<pre><code class=\"language-json\">{\n  \"id\": \"9d2c8f31-…\",\n  \"reason\": \"harassment\",\n  \"reporter_subject_id\": \"a0b1c2d3-…\",\n  \"snapshot_body\": \"the abusive line\",\n  \"snapshot_sender_id\": \"b7c8d9e0-…\",\n  \"snapshot_taken_at\": \"2026-07-11T05:22:19Z\",\n  \"original_present\": false,\n  \"context\": []\n}</code></pre>\n<p>The two audit rows a <code>delete_message</code> resolution produces:</p>\n<pre><code class=\"language-json\">{ \"event_type\": \"chat.report_resolved\",\n  \"payload\": { \"report_id\": \"9d2c8f31-…\", \"action\": \"delete_message\",\n               \"sibling_report_ids\": [\"1a2b3c4d-…\", \"2b3c4d5e-…\"] } }\n\n{ \"event_type\": \"chat.message_deleted\",\n  \"payload\": { \"message_id\": \"6b1f4e0a-…\", \"by\": \"moderator\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<p><strong>Deferred:</strong></p>\n<ul><li><em>Escalation to CyberSkill.</em> Today a report terminates with the workspace administrator, which is what the published privacy policy says (\"your organisation is the controller\"). If we ever need to handle abuse <em>by</em> an administrator, or abuse crossing tenants, that is a new task <strong>and</strong> a privacy-policy change, in that order.</li><li><em>Administrator visibility of block counts.</em> TASK-CHAT-268 §9 raises it: \"six people have blocked this person\" is the strongest signal available in a small workspace, and surfacing it partially breaks the privacy of the block. It needs a decision from the operator, not a design. Not in this slice.</li><li><em>Reporter feedback (\"your report was actioned\").</em> Real need, but it re-introduces the oracle TASK-CHAT-267 §2 closed, and needs a notification surface. Deferred; the columns exist.</li><li><em>Moderation of AI-generated content.</em> CyberOS chat has AI features (<code>ai.rs</code>). A summary that reproduces abusive content is not currently reportable. Deferred; the target-shape constraint would need a fourth arm.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Missing <code>roles</code> claim treated as \"unknown, allow\"</td><td>AC 1 tests a token with no claim at all</td><td>Every legacy token in circulation would be a moderator</td><td><code>require_moderator</code> is a whitelist match; there is no else-allow branch</td></tr><tr><td>Channel owner assumed to be a moderator</td><td>AC 2</td><td>The person a report is <em>about</em> could resolve it</td><td>Channel roles are never consulted by these routes</td></tr><tr><td>DM thread disclosed to the administrator</td><td>AC 7 asserts on the raw JSON, not just the field</td><td>A safety feature becomes employer surveillance; people stop reporting</td><td><code>context</code> is unconditionally empty for <code>kind = 'direct'</code>; there is no override flag</td></tr><tr><td>Private-channel context disclosed to a non-member admin</td><td>AC 8</td><td>A report becomes a skeleton key into channels the admin is not in</td><td>Context requires an existing membership row</td></tr><tr><td>Two admins resolve at once, action applied twice</td><td>AC 10</td><td>Audit chain falsely records two independent decisions</td><td>CAS on <code>status = 'open'</code>; the loser returns the winner's resolution</td></tr><tr><td>Siblings left open against a deleted message</td><td>AC 11</td><td>Queue fills with ghosts; reviewer stops trusting it</td><td>Siblings closed in the same transaction</td></tr><tr><td><code>self_harm</code> buried under spam volume</td><td>AC 4</td><td>The one report that mattered is never read</td><td>Severity is the primary sort key; volume cannot outrank it</td></tr><tr><td>Duplicate entries for one message</td><td>AC 3</td><td>Reviewer fatigue - the real failure mode of every moderation queue</td><td><code>GROUP BY</code> on the target triple</td></tr><tr><td>Snapshot preserved forever</td><td>AC 15</td><td>A durable copy of the content someone wanted removed, readable by their employer</td><td><code>purge_after</code> on every resolution; hourly purge job</td></tr><tr><td>Purge trigger deletes a row an admin is reading</td><td>Job, not trigger; runs hourly</td><td>No mid-read deletion</td><td>§6</td></tr><tr><td><code>remove_member</code> interpreted as \"remove from workspace\"</td><td>AC 12 asserts the subject still exists and keeps other channels</td><td>A chat button could sever someone's access to the organisation</td><td><code>members::remove_as_moderator</code> is channel-scoped; identity lives in AUTH</td></tr><tr><td>Reviewer <code>note</code> used as an XSS vector</td><td>AC 18</td><td>Script execution in the admin's browser</td><td>Rendered as text; React escapes by default and the test pins it</td></tr><tr><td>Cross-tenant report id probed via 403 vs 404</td><td>AC 16</td><td>Confirms a report id exists in a workspace the caller cannot see</td><td>RLS returns zero rows; the handler maps that to <code>404</code></td></tr><tr><td>Blocked-set applied to the queue</td><td>AC 9</td><td>Admin cannot read a report about someone they blocked</td><td>These routes never call <code>blocked_by</code></td></tr><tr><td>Content-policy link 404s</td><td>AC 19 + the landing-page page must ship first</td><td>Play rejects: the policy must be reachable</td><td>Named as an external dependency in §7</td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li><strong>Why the queue groups in SQL rather than in Rust.</strong> The fold has to happen <em>before</em> the <code>LIMIT</code>, or page one shows three copies of one message and the reviewer never reaches page two. Grouping after pagination is the classic version of this bug and it is invisible in a test fixture with four rows.</li></ul>\n<ul><li><strong>Why severity is a function and not a column.</strong> A column would have to be backfilled on every rank change, and the rank <em>is</em> a product decision that will change. <code>severity_rank</code> is one <code>match</code>, one place to edit, and the ordering is legible to whoever reads it next. If the queue ever grows past a size where sorting on a computed expression matters, the column is an additive change.</li></ul>\n<ul><li><strong><code>IS NOT DISTINCT FROM</code> in the sibling update.</strong> The target columns are nullable and two of the three are null on any given row. <code>=</code> yields NULL against NULL and the sibling update would silently match nothing - the same trap as the partial unique index in TASK-CHAT-267, in a different guise.</li></ul>\n<ul><li><strong>Why the admin sees the reporter's identity.</strong> A moderation decision without knowing who is complaining is a decision made blind: three reports from one person who habitually reports their colleagues is a different situation from three reports by three people. The reporter is disclosed to the <em>administrator</em> and never to the reported person, which is the correct asymmetry.</li></ul>\n<ul><li><strong>The purge job deletes the row, not just the snapshot.</strong> A resolved report stripped of its evidence is not a record of anything - it is a row asserting that something happened, with nothing to show for it. The audit chain already holds the decision (<code>chat.report_resolved</code>, which by design carries no content). That is the durable record; the report row is the working copy.</li></ul>\n<p><em>End of TASK-CHAT-269.</em></p>";
