window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-SKILL-120"]="<h1>TASK-SKILL-120: Deliverable scaffolding wiring</h1>\n<h2>§1 - Description</h2>\n<p>The authoring path catches up with the new layout and the templates module, so every future deliverable is born in the right shape.</p>\n<p>Normative clauses:</p>\n<ol><li><code>/create-tasks</code> step flow MUST scaffold each new task as <code>&lt;module&gt;/&lt;STEM&gt;/spec.md</code> (+ <code>assets/</code> on first asset) and write the audit as <code>&lt;STEM&gt;/audit.md</code>; all path examples in the command doc updated.</li><li><code>task-author</code> MUST document the folder layout in its artefact section (spec.md naming, sibling audit, relative asset references) and name <code>modules/templates/contracts/TEMPLATE.md</code> as the presentation contract its output is rendered through (informative pointer - authoring stays markdown).</li><li><code>task-audit</code> MUST resolve <code>&lt;STEM&gt;/audit.md</code> as the report path for folder-layout tasks while keeping <code>.audit.md</code> sibling resolution for any legacy flat file it is pointed at (transition note, one release).</li><li>The ship workflow doc's artefact-path examples and the backlog-state-update evidence citations MUST use the folder paths; <code>init.sh</code> MUST scaffold <code>docs/tasks/</code> ready for folder-layout tasks (no flat-file example remains in scaffolded docs).</li><li>Asset discipline MUST be stated where authors read it (author SKILL.md + command doc): assets live in the task's own <code>assets/</code>, referenced relatively; no cross-task asset reaches into another task's folder.</li></ol>\n<h2>§2 - Why this design</h2>\n<p>Contracts drive agents; if the contracts still describe flat files, every new task fights the migration. One doc-wiring task right after the layout task keeps the fleet coherent.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>Command scaffolds folders</strong> (§1 #1) - command doc contains the folder grammar and no flat-file instruction (grep).</li><li><strong>Author/audit contracts updated</strong> (§1 #2, #3) - artefact sections name spec.md/audit.md + the TEMPLATE.md pointer; legacy transition note present in the audit skill.</li><li><strong>Ship + init coherent</strong> (§1 #4) - workflow doc examples use folder paths; init.sh-scaffolded docs carry none of the old grammar.</li><li><strong>Asset discipline stated</strong> (§1 #5) - both author-facing docs carry the own-assets rule (grep).</li></ol>\n<h2>§5 - Verification</h2>\n<p>Doc assertions (grep-level, executable in <code>scripts/tests/test_task_layout.sh</code> t07-t10 extension): command grammar, contract sections, workflow examples, asset rule. (AC 1-4.)</p>\n<h2>§3 - Contract</h2>\n<p>Scaffold grammar: <code>docs/tasks/&lt;module&gt;/&lt;STEM&gt;/spec.md</code>; audit: <code>&lt;STEM&gt;/audit.md</code>; assets: <code>&lt;STEM&gt;/assets/&lt;file&gt;</code> referenced as <code>assets/&lt;file&gt;</code>.</p>\n<h2>§6 - Implementation skeleton</h2>\n<p>Four doc edits + init.sh template sweep; the t07-t10 asserts ride the TASK-DOCS-004 suite.</p>\n<h2>§7 - Dependencies</h2>\n<p>TASK-DOCS-004 (layout must exist first). Informative link to TASK-TPL-001's contract.</p>\n<h2>§8 - Example payloads</h2>\n<p>Author scaffold: <code>mkdir -p docs/tasks/ten/TASK-TEN-301-rls-sweep &amp;&amp; $EDITOR .../spec.md</code></p>\n<h2>§9 - Open questions</h2>\n<p>None blocking. Whether OTHER deliverable classes (SOW, PRD...) also become folders is decided per-contract later; TEMPLATE.md already renders them.</p>\n<h2>§10 - Failure modes inventory</h2>\n<ol><li>Agent follows stale cached contract - trigger tests unchanged, but contract text is what ships in the payload; next /init update refreshes fleets.</li><li>Legacy flat task appears post-migration (old branch merge) - audit skill's transition note covers reading it; regen warns loudly (TASK-DOCS-004 #4) making it visible.</li><li>Asset reach-across - stated rule + review; renderer resolves only own-folder assets (TASK-DOCS-005 fails missing assets loudly).</li><li>init scaffolds diverge from repo docs - t09 greps the scaffolded output too.</li><li>Plugin copy drift - payload rebuild vendors the edited docs; version-sync gate carries them.</li></ol>\n<h2>§11 - Implementation notes</h2>\n<p>Keep the transition note dated; one release later it drops (same sunset pattern as backlog-state-update @1).</p>\n<p><em>End of TASK-SKILL-120.</em></p>";
