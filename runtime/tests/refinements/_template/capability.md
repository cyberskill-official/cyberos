# REF-NNN Capability eval

## What new behavior does this REF enable?
[describe]

## Test fixture
`capability.test.py` — pass criteria: a memory that would have been rejected pre-REF is now accepted (or vice versa).

## Pass / fail criteria
- PASS: ...
- FAIL: ...
