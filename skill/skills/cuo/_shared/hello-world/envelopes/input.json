{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cyberos.cyberskill.world/skills/cuo/_shared/hello-world/envelopes/input.json",
  "title": "hello-world input",
  "type": "object",
  "required": ["name", "output_path"],
  "additionalProperties": false,
  "properties": {
    "name":        {"type": "string", "minLength": 1, "maxLength": 100, "description": "Person to greet. Treated as untrusted text — wrapped in <untrusted_content/> before processing."},
    "output_path": {"type": "string", "minLength": 1, "description": "Where to write the greeting markdown file. Relative paths only; the runtime forbids paths outside the working directory."}
  }
}
