# memory-rebuild.yml — STUB
#
# Auto-generated 2026-05-17 from task build_envelope references.
# Declared by: TASK-MEMORY-102
#
# Implementation team: replace this stub with the real workflow YAML
# per the spec details in the declaring task(s) above.
# Many tasks embed the canonical workflow YAML in their §6 skeleton section.

name: memory-rebuild

on:
  pull_request:
    paths:
      # TODO: per declaring task
      - 'services/**'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: TODO — implement per task(s) TASK-MEMORY-102
        run: echo 'Stub — see task specs for canonical workflow YAML'
