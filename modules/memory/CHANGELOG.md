# Changelog — moved

This module's changelog has been consolidated into the repo-root [`CHANGELOG.md`](../../CHANGELOG.md) as of 2026-05-18.

All historical entries for this module are tagged `[MEMORY]` in the root changelog. Find them with:

```bash
grep '^## .*\[MEMORY\]' CHANGELOG.md
```

New module-scoped entries SHOULD be added to the root `CHANGELOG.md` with the `[MEMORY]` tag, sorted by date with the rest of the repo's history.
