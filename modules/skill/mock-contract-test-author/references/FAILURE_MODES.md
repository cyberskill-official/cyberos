# `mock-contract-test-author` - failure modes

1. Mock drifts into fantasy (shapes the real API never had) - contract pinned from provider docs cited in the artefact.
2. Error modes only cover happy-path variants - MCT-GATE-001 cross-checks the matrix rows.
3. swap_target renamed by refactor - TRACE resolution fails the audit at next touch.
4. Sunset never fires - the BACKLOG tag keeps it visible; FR-SKILL-119-class sweeps catch rot.
5. Mock passes tests the real service fails - the contract suite runs against the real service at integration; divergence routes back.
