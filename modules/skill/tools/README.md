# `skill/tools/` — registry + build helpers

- `registry.json` — the per-installation skill registry. Records which skills are installed, their content hashes, and any operator-granted capabilities. Consumed by `cyberos-skill list/info`.
