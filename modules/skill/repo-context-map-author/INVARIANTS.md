# `repo-context-map-author` - invariants

Lifted from SKILL.md's normative prose (TASK-SKILL-118 AC 2 discipline: no invariant without a prose source).

1. Scan BEFORE any code is generated for the task - the map is a precondition, not a retrospective.
2. Patterns cited MUST point at real files (pinned_in resolves at authoring time).
3. A placement mismatch is surfaced, never auto-corrected: catalogue placement is operator territory.
4. The map records what EXISTS; it proposes nothing (proposals belong to the ADR/spike chain).

Enforced at audit time by `repo-context-map-audit` per RUBRIC.md (repo_context_map_rubric@1.0).
