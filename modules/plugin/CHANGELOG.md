# CHANGELOG (moved)

The plugin module's changelog has been centralised — see the root `CHANGELOG.md` for all dated entries, sorted chronologically across every module.

This file is a pointer only. Per the 2026-05-18 CHANGELOG centralisation, every per-module CHANGELOG is now an 11-line pointer; the single source of truth is `../../CHANGELOG.md` at the repo root.

For plugin-module-specific entries, search the root CHANGELOG for `## plugin —` or `[PLUGIN]` tags.

*End of modules/plugin/CHANGELOG.md*
