# TASK-APP-006: winget installer manifest. InstallerSha256 is an UNAMBIGUOUS placeholder,
# re-derived from the actual GitHub Release artifact by release-pkgmgr-pr.yml (AC #5).
PackageIdentifier: CyberSkill.CyberOS
PackageVersion: 1.0.0
InstallerType: nullsoft   # matches the existing NSIS output confirmed in release.yml's desktop job
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/cyberskill-official/cyberos/releases/download/v1.0.0/CyberOS_1.0.0_x64-setup.exe
    InstallerSha256: REPLACE_WITH_SHA256_OF_RELEASE_INSTALLER   # computed per spec 6, not hand-typed
    InstallerSwitches:
      Silent: "/S"              # NSIS's standard silent flag; AC #8 requires confirming against
      SilentWithProgress: "/S"  # CyberOS's actual NSIS installer via a real test run (spec 9)
ManifestType: installer
ManifestVersion: 1.6.0   # NOTE: schema version + InstallerSwitches nesting are this task's
                         # best-available understanding, not independently re-verified against
                         # Microsoft's live current schema during authoring - spec 9 requires
                         # confirming both against winget-pkgs' current templates before this
                         # file is treated as final (same discipline as TASK-APP-005's
                         # snapcraft architectures hedge).
